---
title: "MMoA: An AI-Agent framework with recurrence for Memoried Mixure-of-Agent"
title_zh: MMoA：一种具有循环机制的AI代理框架，用于带记忆的混合代理
authors: Rui Chu
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19194v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于LLM生成的循环混合智能体框架
tldr: "现有混合代理（MoA）框架通过聚合多个大语言模型输出提升性能，但静态路由器未能充分利用时间与上下文依赖。本文提出MMoA，一种基于LSTM门控的递归MoA架构，通过递归路由器自适应调节代理贡献，实现上下文感知聚合。在AlpacaEval 2.0等基准上，MMoA取得与MoA相近准确率（胜率58.0% vs 59.8%），同时计算开销降低高达4.6%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19194v1/fig-001.webp\", \"caption\": \"Table 1: (a) AlpacaEval 2.0\", \"page\": 4, \"index\": 1, \"width\": 486, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2605.19194v1/fig-002.webp\", \"caption\": \"Table 2: (b) MT-Bench\", \"page\": 4, \"index\": 2, \"width\": 483, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.19194v1/fig-003.webp\", \"caption\": \"Figure 1: Effectiveness of Recurrence Router in MoA system: The router compared to the upper section which is the original MoA, the recurrence router (a Recurence architecture of neural network) in MMoA can find out the most valuable agents in the following layers\", \"page\": 2, \"index\": 3, \"width\": 461, \"height\": 308}]"
motivation: 现有MoA系统的静态路由器未能充分捕捉聚合层间的时间与上下文依赖，限制了性能。
method: 提出MMoA，在代理选择过程中集成基于LSTM的门控机制，递归路由器根据当前输入和历史路由决策动态调整代理贡献。
result: "MMoA在AlpacaEval 2.0上胜率58.0%（MoA为59.8%），运行效率提升高达4.6%。"
conclusion: MMoA为适应性的多代理大语言模型系统提供了可扩展且高效的方案。
---

## 摘要
混合代理（MoA）框架通过聚合多个代理的输出，在提升大型语言模型（LLM）性能方面显示出了潜力。然而，现有的MoA系统通常依赖于静态路由器，无法充分捕捉跨聚合层的时间依赖和上下文依赖。为解决这一局限，我们提出了MMoA，一种循环MoA架构，将基于LSTM的门控机制集成到代理选择过程中。循环路由器根据当前输入和历史路由决策自适应地调节代理的贡献，从而实现更具上下文感知的聚合。我们在标准指令遵循基准测试（包括AlpacaEval 2.0、MT-Bench和Arena-Hard）上评估了MMoA。结果表明，MMoA在达到与传统MoA相当精度的同时，通过动态激活更少的代理降低了计算开销。例如，在AlpacaEval 2.0上，MMoA的胜率为58.0%，而MoA为59.8%，同时运行效率提高了多达4.6%。这些结果表明，MMoA为自适应多代理LLM系统提供了一种可扩展且高效的方法。

## Abstract
The Mixture-of-Agents (MoA) framework has shown promise in improving large language model (LLM) performance by aggregating outputs from multiple agents. However, existing MoA systems often rely on static routers that do not fully capture temporal and contextual dependencies across aggregation layers. To address this limitation, we propose MMoA, a recurrent MoA architecture that integrates LSTM-based gating into the agent selection process. The recurrence router adaptively modulates agent contributions based on both current inputs and historical routing decisions, enabling more context-aware aggregation. We evaluate MMoA on standard instruction-following benchmarks, including AlpacaEval 2.0, MT-Bench, and Arena-Hard. The results show that MMoA achieves comparable accuracy to traditional MoA while reducing computational overhead by dynamically activating fewer agents. For example, on AlpacaEval 2.0, MMoA achieves a win rate of 58.0%, compared with 59.8% for MoA, while improving runtime efficiency by up to 4.6%. These results suggest that MMoA provides a scalable and efficient approach for adaptive multi-agent LLM systems.

---

## 论文详细总结（自动生成）

# MMoA: 一种具有循环机制的AI代理框架（用于带记忆的混合代理）—— 论文详细总结

## 1. 论文的核心问题与整体含义

- **研究动机**：现有的混合代理（Mixture-of-Agents, MoA）框架通过聚合多个大语言模型（LLM）输出来提升性能，但其路由器是**静态**的——不能根据上下文或历史路由信息动态调整各代理的贡献，导致聚合效率低下且计算开销固定。
- **整体含义**：本文旨在引入**递归路由机制**（基于LSTM），让路由器能够感知当前输入和之前层的路由决策，从而自适应地分配代理权重。这不仅保持了MoA的多代理优势，还通过减少激活的代理数来提升效率，为构建更智能、可扩展的多代理LLM系统提供新思路。

## 2. 论文提出的方法论

- **核心思想**：将MoE（混合专家）中常用的LSTM/RNN递归门控机制引入MoA框架，替代静态路由。通过维护一个隐藏状态，使路由器能够在不同聚合层之间传递信息，从而动态地为每个代理计算权重。
- **关键技术细节**：
  - 给定n个代理的输出`{A1(x), A2(x), ..., An(x)}`，先经过融合函数`f(·)`（如拼接+线性投影）得到统一表示`z_t`。
  - 将该表示送入LSTM，结合上一层的隐藏状态`h_{t-1}`，更新当前隐藏状态`h_t`：`h_t = LSTM(z_t, h_{t-1})`。
  - 通过线性变换+Softmax生成门控向量`g`（维度n）：`g = softmax(W_g * h_t + b_g)`。
  - 最终输出为各代理输出的加权和：`y = Σ g_i · A_i(x)`。
- **算法流程（文字说明）**：
  1. 输入初始提示`x`，初始化隐藏状态`h_0`。
  2. 对于每一层`l`（1到L）：
     - 让每个代理对上一层的输出`x^{(l-1)}`产生响应`a_i^{(l)}`。
     - 融合所有代理输出得到`z^{(l)}`。
     - 通过LSTM更新隐藏状态`h^{(l)}`。
     - 计算门控向量`g^{(l)}`。
     - 加权求和得到本层输出`y^{(l)}`，并作为下一层输入`x^{(l)}`。
  3. 最终返回`y^{(L)}`作为聚合结果。
- **路由器损失函数**：采用**期望任务损失**（加权各代理的损失）加**熵正则化**（鼓励探索），可选**负载均衡项**。损失为：`L_total = Σ p_i L_i + λH(p) + γL_load`，其中`p_i`是选择代理i的概率，`L_i`是代理i的损失，`H(p)`为熵。

## 3. 实验设计

- **基准数据集**：
  - AlpacaEval 2.0（805条指令，长度控制胜率LC）
  - Arena-Hard（500条多样查询）
  - MT-Bench（多轮对话，GPT-4打分0-10）
  - FLASK（12个技能维度详细评估）
- **对比方法**：
  - 标准MoA（静态聚合）
  - 消融模型（用简单线性层替代递归门控，去掉时间依赖）
  - 其他基线：GPT-4 Omni、GPT-4 Turbo、WizardLM、Qwen1.5、Llama 3、Mixtral等
- **实验设置**：遵循标准指令微调评估流程，保证公平比较。

## 4. 资源与算力

- **文中未明确说明**使用了多少GPU、具体型号、训练时长或推理硬件配置。仅提及是在“标准指令调优LLM开发”环境下进行，但无进一步算力细节。

## 5. 实验数量与充分性

- **实验数量**：在多个基准上进行了评估（AlpacaEval 2.0、MT-Bench、Arena-Hard、FLASK），报告了准确率和时间效率两方面的结果。同时进行了消融实验（替换为线性层）和不同代理数量（n=1,2,3,6）的时间对比。
- **充分性与公平性**：
  - 实验覆盖了常见的指令遵循基准，但未涵盖长上下文推理、工具使用、多语言或安全关键场景。
  - 时间效率实验设计了“多提议者”与“单提议者”对比，控制变量较好。
  - 准确率结果中，MMoA与MoA的差距很小（如AlpacaEval 2.0 LC胜率61.5% vs 65.1%），但作者指出这是可接受的效率-精度权衡。
  - 总体而言，实验设计较为规范，但**覆盖范围有限**（如缺失更多消融和敏感性分析），但仍可支撑核心结论。

## 6. 论文的主要结论与发现

- **核心结论**：MMoA通过递归门控路由器，在**保持与MoA相近精度**的同时，**显著降低了运行时开销**（动态激活更少代理）。
- **具体数据**：
  - AlpacaEval 2.0：MMoA LC胜率61.5%，整体胜率58.0%（MoA分别为65.1%和59.8%）。
  - MT-Bench：MMoA平均9.20（MoA 9.25），差距仅0.05。
  - 时间复杂度：从`O(nL)`降为约`O(n+L)`。在n=6时，MMoA相对运行时间为56.7%（基线为61.3%），效率提升4.6%。
- **贡献总结**：首次将路由器引入MoA框架用于代理选择，使MoA系统可以像语言模型一样被调优（Tune），提供了灵活性。

## 7. 优点

- **创新性**：将MoE中的递归路由机制迁移到MoA，填补了静态聚合与动态路由之间的空白。
- **高效性**：通过减少激活的代理数，实现了可量化的效率提升，而精度损失极小。
- **可调优性**：提出的损失函数允许通过梯度下降直接训练路由器，为MoA的端到端优化铺路。
- **简单易用**：LSTM模块轻量，集成方便，无需大幅修改现有MoA架构。

## 8. 不足与局限

- **实验覆盖不足**：仅在指令遵循基准上评估，未测试长上下文、工具使用、多语言、安全关键等场景。
- **路由器设计局限**：仅采用LSTM，未探索Transformer路由器、强化学习路由或token级路由等替代方案。
- **超参数敏感**：未提供对λ、γ等超参数以及代理池组成、聚合深度敏感性的完整分析。
- **缺乏深度质量分析**：未深入探讨失败模式、校准或鲁棒性。
- **算力资源未报告**：无法评估方法实际部署所需硬件成本。

（完）
