---
title: Value-Gradient Hypothesis of RL for LLMs
title_zh: 面向大语言模型的强化学习价值梯度假说
authors: "Arip Asadulaev, Daniil Ognev, Karim Salta, Martin Takac"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21654v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: "对LLM中无评论家RL方法（PPO, GRPO）的理论分析"
tldr: 本文提出RL后训练语言模型中critic-free方法（如PPO、GRPO）的价值梯度假说。通过可微rollout和加性噪声参数化证明actor更新在期望上近似价值梯度，并显示离散transformer的自动微分能近似价值信号，误差由采样间隔和策略熵控制。该理论将RL效果分解为价值梯度信号和可达奖励空间，从而给出RL沿预训练轨迹最有效的判据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21654v1/fig-001.webp\", \"caption\": \"Figure 3: Results of the RL upon various OLMO-2 pretraining checkpoints. The left image shows the consistency of the achieved gains via RL using a different number of GRPO steps. The right image shows the behavior of the different components of our study.\", \"page\": 8, \"index\": 1, \"width\": 818, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.21654v1/fig-002.webp\", \"caption\": \"Figure 5: Z-scores of the gain after RL vs. z-scores of the predicted RL impact. Correlation (left) and curve per checkpoint (right). The std bars on left indicate RL gain variance over the various training steps.\", \"page\": 9, \"index\": 2, \"width\": 826, \"height\": 286}]"
motivation: 解释critic-free RL方法（如PPO、GRPO）为什么有效以及何时带来最大收益。
method: 建立价值梯度视角，在可微rollout和加性噪声下证明actor更新类似价值梯度，并通过自动微分在离散transformer中近似价值信号。
result: RL的影响可分解为价值梯度信号和可达奖励头寸，误差受采样间隔和策略熵控制。
conclusion: 提出沿预训练轨迹RL最有效的准则，基于价值梯度信号和可达奖励空间。
---

## 摘要
强化学习显著提升了预训练语言模型，但为何无评论家方法（如PPO和GRPO）能有效工作，以及何时能带来最大增益，这些问题仍未得到充分研究。我们针对大语言模型后训练的强化学习提出了一种价值梯度视角。首先，在可微展开和加性噪声参数化下，我们证明演员更新在期望上类似于价值梯度：反向传播传播协态，其条件期望等于价值梯度。其次，对于离散Transformer策略，我们证明通过注意力的自动微分产生经验协态，该协态近似于这一价值信号，其误差由采样间隔和策略熵控制。这些结果将强化学习的影响分解为价值梯度信号和可达奖励空间，从而得出了在预训练轨迹上强化学习应最有效的准则。

## Abstract
Reinforcement learning substantially improves pretrained language models, but it remains understudied why critic-free methods such as PPO and GRPO work as well as they do, and when they should provide the largest gains. We develop a value-gradient perspective of critic-free RL for LLM post-training. First, under a differentiable rollout and additive-noise parameterization, we show that the actor update is value-gradient-like in expectation: the backward pass propagates costates whose conditional expectation equals the value gradient. Second, for discrete transformer policies, we show that autodifferentiation through attention produces empirical costates that approximate this value signal, with an error controlled by the sampling gap and policy entropy. These results motivate a decomposition of RL impact into value gradient signal and reachable reward headroom, yielding a criterion for when RL should be most effective along a pretraining trajectory.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在大语言模型（LLM）后训练中，强化学习方法（如PPO、GRPO）显著提升了模型性能，但这些方法在标准强化学习理论中属于“无评论家”（critic-free）方法，理论上应在长时序信用分配上失败。为何它们在LLM上反而有效？何时能带来最大收益？这一问题尚不清楚。
- **整体含义**：本文提出价值梯度假说，认为无评论家RL在LLM中并不缺乏价值信息——actor的更新通过反向传播携带了类似价值梯度的信号。这解释了其有效性，并给出了预测RL收益最大化的准则：RL最有效的检查点应同时具备较强的价值梯度信号和足够的奖励改进空间。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：将critic-free RL（PPO、GRPO）的actor更新解释为价值梯度（value gradient）的期望等价。在可微展开（differentiable rollout）和加性噪声参数化下，actor反向传播的协态（costate）条件期望等于价值函数的状态梯度。对于离散Transformer策略，自动微分（autodiff）通过注意力机制传播近似的协态，误差受采样间隔和策略熵控制。
- **关键技术细节**：
  - **可微展开**：定义可微的策略π_θ、转移f_θ和奖励r，且噪声服从独立于θ的分布。此时总回报R是可微函数，可通过BPTT计算参数梯度。
  - **协态（costate）**：λ_t = ∂R_t/∂s_t，满足递归λ_t = Dr(s_t,a_t) + γ(Df_θ)^⊤λ_{t+1}。其条件期望E[λ_t|s] = ∂V_t^π(s)/∂s，即价值梯度。
  - **SF到PD的桥梁（Lemma 1）**：对于移位/加性噪声策略，score-function梯度在期望上等于pathwise梯度，将离散采样下的SF更新转化为可微路径。
  - **离散GRPO的近似**：Transformer中，自动微分计算的经验协态满足类似递归ˆλ_t = bA_t·∂ℓ_t/∂h_t^(L) + J_{t+1←t}^⊤·ˆλ_{t+1}，其中J_{t+1←t}是通过注意力的雅可比矩阵（可微）。与精确BPTT的差距仅来自通过离散采样的路径，该差距被策略熵的上界控制（Proposition 2）。
  - **RL影响分解**：RL增益 ≈ 可用价值梯度信号(S_m(θ)) × 可达奖励头寸(H_α(θ))，其中S_m = Σ - Λ_m（Σ是价值梯度范数的期望，Λ_m是近似误差），H_α为指数平滑下可提升的奖励空间。预测最佳检查点应最大化该乘积。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

- **数据集/场景**：使用OLMo-2 1B模型在不同预训练检查点（50k至1M步，间隔50k）进行受控的标签复制任务。每个提示包含一个目标标签（A/B/C/D），模型需输出正确标签的概率。奖励定义为p_θ(y⋆|q)（可微）。
- **Benchmark**：比较每个检查点在GRPO微调前后的奖励变化，以实际RL增益（∆R）作为基准。
- **对比方法**：
  - 不同GRPO步数（K=10,20,25,30）下的RL增益。
  - 计算预测的RL影响得分（P_gap = (Σ-Λ_m)·H_α），与实测增益进行Spearman相关性比较。
  - 对比单纯使用预训练奖励（R_before）与组合预测（z(R_before)+z(P_gap)）对最终RL后奖励的预测能力。

### 4. 资源与算力

- **文中未明确提及**：论文没有具体说明使用的GPU型号、数量或训练时长。仅在实验部分提到使用OLMo-2 1B模型，每个检查点进行最多30步GRPO训练。算力信息未提供，无法评估资源消耗。

### 5. 实验数量与充分性

- **实验数量**：
  - 约20个预训练检查点（50k到1M步）。
  - 每个检查点测试4种GRPO步数（10,20,25,30），并报告平均增益。
  - 进行了熵边界验证（Fig 2）、成本态组件变化（Fig 3右侧）、预测与实际增益的相关性（Fig 1,4,5）。
  - 计算Spearman相关系数（P_gap与∆R约0.60，组合预测与R_after约0.73）。
- **充分性**：
  - 实验设计能直接测量成本态和熵，验证理论预测。
  - 但任务为受控标签复制，奖励可微，与真实LLM RL（如结果奖励、规则奖励、分类器奖励）存在差距。
  - 模型规模限于1B，未在更大模型或复杂推理任务上验证。
  - 参数（学习率、批量大小、组大小等）对结果有影响，论文已承认这一点。总体来看实验初步但合理，用于支持理论预测，但需要更广泛的验证。

### 6. 论文的主要结论与发现

- 无评论家RL（PPO/GRPO）在LLM中的成功并非例外，而是因为actor反向传播携带了近乎价值梯度的信号。
- 在可微连续松弛下，该信号在期望上是精确的；在离散Transformer中，通过注意力机制近似，误差受策略熵控制。
- RL收益可分解为价值梯度信号和可达奖励头寸，两者乘积可预测检查点的RL增益。
- 实验验证：预测得分与实际RL增益存在中等正相关（Spearman~0.60），组合预训练奖励后相关性更高（~0.73）。

### 7. 优点

- **理论创新**：首次系统地将无评论家RL在LLM中的成功归因于价值梯度信号，弥合了经典RL理论与LLM实践之间的gap。
- **数学严谨**：提供了协态递归、SF-PD等价、熵上界等严格推导，形成自洽的理论框架。
- **可验证预测**：提出的RL影响分解可直接计算并预测检查点优劣，实验设计能够直接测量成本态和熵，验证理论机制。
- **实用价值**：为选择最佳预训练检查点启动RL提供了理论依据，可能节省大量试错成本。

### 8. 不足与局限

- **实验环境受限**：任务为受控的标签复制，奖励直接取自模型logits（可微），与真实RL任务（如结果奖励、基于规则的奖励、带分类器的奖励）存在差异，结论泛化性存疑。
- **模型规模小**：仅使用1B模型，未在更大模型（如7B/70B）或实际推理任务（数学、编码）上验证。
- **参数敏感性**：RL学习率、批量大小、组大小、GRPO步数等参数影响观察到的相关性，论文未进行系统敏感度分析。
- **未与其他RL变体对比**：只测试了GRPO，未与PPO、REINFORCE等对比以区分共性。
- **理论假设强度**：可微展开、加性噪声策略、边界条件等假设在真实离散Transformer中并非完全成立，近似误差的实际大小需进一步量化。
- **算力信息缺失**：无法评估实验的可复现性和计算成本。

（完）
