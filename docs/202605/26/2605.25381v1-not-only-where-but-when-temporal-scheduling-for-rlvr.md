---
title: "Not only where, But when: Temporal Scheduling for RLVR"
title_zh: 不仅在于何处，更在于何时：面向RLVR的时间调度
authors: "Jinghao Zhang, Ruilin Li, Feng Zhao, Jiaqi Wang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25381v1"
tags: ["query:token"]
score: 9.0
evidence: LLM中RLVR信用分配的时间调度
tldr: 论文提出在带可验证奖励的强化学习（RLVR）中引入时间维度，动态调度信用分配标准，优先关注特定策略行为的令牌并逐步衰减至一般优化，从而提升训练稳定性和效率。使用轨迹百分位数区分策略行为，实验证明该方法优于标准优化，保持了更健康的策略进化动态。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25381v1/fig-001.webp\", \"caption\": \"Table 1: Performance across mathematical and general reasoning benchmarks. Bold indicates the best results among the comparison. “ ↪→Schedule ” represents applying the temporal schedule to corresponding credit allocation methods, while “ TP ” denotes the trajectory percentile.\", \"page\": 7, \"index\": 1, \"width\": 830, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2605.25381v1/fig-002.webp\", \"caption\": \"Figure 5: Training dynamics under temporal scheduling. (a) Train-time gradient norm. Temporal scheduling consistently yields larger and more concentrated optimization signals compared to standard GRPO, while TP-Schedule exhibits the most stable optimization trajectory. Random scheduling introduces unstable gradient spikes, suggesting that incoherent token organization corrupts the accumulated policy gradients. (b) Scheduled response length during optimization. Temporal scheduling progressively expands the optimization from specified token subsets toward broader trajectory, where TP-Schedule achieves slightly higher response length compared to entropy-based schedule.\", \"page\": 14, \"index\": 2, \"width\": 820, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2605.25381v1/fig-003.webp\", \"caption\": \"Figure 2: Policy behavior analysis and corresponding training dynamics. (a) Histogram of sampled token frequency across different trajectory positions. The x-axis represents the union of all sampled tokens, sorted by TP-Score (Eq. 5) to approximate the most likely position of each token along the trajectory. While different trajectory position exhibit substantially different policy behavior with respective token distribution. (b) Entropy dynamics of tokens at different trajectory percentiles. Tokens from different positions exhibit different entropy profiles, while temporal scheduling consistently preserves higher entropy throughout training. (c)-(e) Textual visualization of trajectory positions.\", \"page\": 3, \"index\": 3, \"width\": 820, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.25381v1/fig-004.webp\", \"caption\": \"Table 4: Training hyperparameters and temporal scheduling configurations used in our experiments, where underline denotes the default settings with variants for presentation\", \"page\": 13, \"index\": 4, \"width\": 620, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2605.25381v1/fig-005.webp\", \"caption\": \"Figure 3: Analysis of policy optimization under temporal scheduling. (a) Temporal scheduling consistently preserves higher policy entropy over full response under different credit allocation strategies. (b) Temporal scheduling over trajectory induces a structured shift in KL divergence from later to earlier percentiles over training, suggesting the non-uniformly optimized policy behaviors.\", \"page\": 8, \"index\": 5, \"width\": 820, \"height\": 325}]"
motivation: 现有RLVR中信用分配标准固定不变，限制了策略的弹性进化，需探索何时分配学习信号的重要性。
method: 引入时间维度，通过轨迹百分位数调度信用分配标准，先强调特定策略行为再逐渐过渡到一般优化。
result: 在数学和通用推理基准测试中，时间调度方法一致优于标准优化，且策略熵损失更小，动态更健康。
conclusion: 时间调度是RLVR优化中一个有前景的维度，能带来更稳定高效的学习。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为大型语言模型（LLMs）后训练的核心技术。虽然策略优化由全局广播标量奖励下的所有采样token驱动，但轨迹中表现出的异质性策略行为在很大程度上被忽视而未加以区分。现有工作通过信用分配解决这一问题，包括token级优势重加权和选择性token优化，然而，分配标准在整个训练过程中基本固定，限制了策略的弹性演化。在本工作中，我们认为学习信号何时调度与它们在token间何处分配同样重要，并引入时间维度，即在RLVR优化过程中调度信用分配标准。我们发现，优先关注以特定策略行为为重点的目标token，并逐渐向通用优化衰减，能够带来更稳定高效的学习动态。此外，我们表明简单的轨迹百分位数提供了区分策略行为的自然视角，并能与时间调度有效配合。我们的分析揭示，标准优化在同时容纳异质性行为时会显著牺牲策略熵，而时间调度则产生更健康的策略演化动态。在数学和通用推理基准上的实验显示出一致的改进，表明时间调度构成了一个有前景的优化维度。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a core technique for post-training of Large Language Models (LLMs). While policy optimization is driven by all sampled tokens under a globally broadcast scalar reward, the heterogeneous policy behaviors exhibited along trajectories are largely overlooked without differentiation. Existing works address this by credit allocation, including token-level advantage reweighting, and selective token optimization, however, the allocation criterion are principally stagnant throughout training, limiting resilient policy evolution. In this work, we argue that \textit{when} learning signals are scheduled can be as important as \textit{where} they are allocated across tokens, and introduce the temporal dimension that scheduling the credit allocation criteria over the course of RLVR optimization. We find that prioritizing targeted tokens emphasized with specific policy behaviors, and gradually attenuating toward general optimization leads to more stable and efficient learning dynamics. Furthermore, we show that simple trajectory percentiles provide a natural perspective for distinguishing policy behaviors, and works effectively with temporal scheduling. Our analysis reveals that standard optimization substantially sacrifices policy entropy when simultaneously accommodating heterogeneous behaviors, whereas temporal scheduling yields healthier policy evolution dynamics. Experiments across mathematical and general reasoning benchmarks demonstrate consistent improvements, suggesting that temporal scheduling constitutes a promising optimization dimension.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：在基于可验证奖励的强化学习（RLVR）中，现有的信用分配方法（如token级优势重加权、选择性token优化）通常在整个训练过程中采用固定的分配标准（“停滞的”分配准则），这限制了策略的弹性演化，导致性能瓶颈。
- **核心问题**：现有工作主要关注“在哪里”（where）分配学习信号，但忽略了“何时”（when）分配同样重要。作者提出引入**时间维度**，即动态调度信用分配标准，以解决固定标准导致的策略优化僵化问题。
- **整体含义**：通过在不同训练阶段强调不同的策略行为（如先聚焦于可靠的后缀生成，再逐步扩展至早期的推理框架），可以显著提升训练稳定性、效率和最终性能，为RLVR优化提供了一个新视角。

### 2. 方法论

- **核心思想**：时间调度（Temporal Scheduling）——在训练过程中，逐步改变信用分配标准，从最初侧重特定策略行为（如高熵token或后期token）逐渐过渡到对所有token进行一般优化。
- **关键技术细节**：
  - **调度框架**：对于每个token \(y_t\)，原始信用分配因子为 \(\hat{a}_t\)（由现有方法如熵代理或轨迹百分位数得到）。调度后的因子为 \(\hat{a}_t(\tau) = S(\tau) \hat{a}_t\)，其中 \(\tau \in [0,1]\) 是归一化训练进度，\(S(\tau)\) 是单调递减的调度函数（如线性、Sigmoid、Gamma）。
  - **连续重加权场景**：\(\hat{a}_t(\tau) = S(\tau) \psi(\mu_t)\)，其中 \(\psi(\mu_t)\) 是正标量。
  - **离散掩码场景**：\(\hat{a}_t(\tau) = \mathbb{I}[\mu_t \geq S(\tau) \cdot \epsilon]\)，通过逐渐降低阈值 \(\epsilon\) 来扩大参与优化的token集合。
  - **轨迹百分位数调度（TP-Schedule）**：将token的轨迹百分位数 \(\mu_t = t / |y|\) 作为自然区分策略行为的代理。初始化时只优化后端token（百分位数高），随着训练推进逐渐纳入前端token。其策略梯度公式（以GRPO为例）为：
    \[
    \nabla_\theta J_{\text{GRPO-schedule}} = \mathbb{E}\left[\frac{1}{G}\sum_{i=1}^G \hat{A}_i \cdot \frac{1}{|y_i|} \sum_{t=1}^{|y_i|} \mathbb{I}\left[\frac{t}{|y_i|} \geq S(\tau) \cdot 1\right] g_{i,t}(\theta)\right]
    \]
    其中 \(g_{i,t}\) 是token级策略梯度。
- **算法流程**：在每个训练步，计算当前进度 \(\tau\)，对每个采样trajectory的token计算调度后的信用因子（根据连续或离散方式），然后使用该因子加权（或掩码）后的梯度进行策略优化。

### 3. 实验设计

- **训练数据集**：从OpenMathReasoning和DeepMath-103K中精选的30K数学问题，经过去重和难度过滤，并使用DeepSeek-V3.2-Speciale合成验证过的推理轨迹。
- **测试基准**：
  - **数学推理（分布内）**：AIME24/25、HMMT25、Minerva、OlympiadBench。
  - **通用推理（分布外）**：Winogrande（常识推理）、GPQA-Diamond（STEM）、MuSR（多步软推理）。
- **对比方法**：
  - 基线和主对比：vanilla GRPO、Entropy-Adv（基于熵的优势重加权）、Forking-Tok（基于熵的选择性token优化）、GSPO（序列级策略优化）。
  - 对每种方法分别应用时间调度（标注为“→Schedule”）以检验互补性。TP-Schedule（轨迹百分位数+时间调度）作为独立方法。
- **模型规模**：Qwen3-4B和Qwen3-8B。
- **评估指标**：准确率Avg@16（AIME24/25, HMMT25）和Avg@4（其他）。

### 4. 资源与算力

- 论文在附录A.1中明确：实验使用**8块H200 GPU**，采用AdamW优化器，学习率 \(1\times10^{-6}\)，批量大小256，每组prompt采样8个rollouts。训练总步数未明确给出，但调度范围 \(\tau_{\text{high}}=0.8\) 说明训练步数 \(N\) 由该方法决定。另外使用最大模型长度40960，最大响应长度39936。
- 总体资源配置合理，但未提供具体训练时长或GPU小时数。

### 5. 实验数量与充分性

- **主要实验**：表1展示了在Qwen3-4B和Qwen3-8B上的完整结果，覆盖数学和通用推理共7个基准，对比了6种不同方法（含调度变体），共约12行结果。
- **消融实验**：
  - 调度函数选择（线性、Sigmoid、Gamma）对性能影响较小（表2）。
  - 时间范围 \(\epsilon_{\text{high}}\) 的敏感性（表3），最佳范围在0.6~0.8。
  - 不同信用分配代理（轨迹百分位数、熵、前缀、随机）在时间调度下的表现（图4）。
- **分析实验**：
  - 训练时策略熵动态（图3a），显示时间调度保留更高熵。
  - 不同轨迹百分位数上的KL散度变化（图3b），揭示结构化的非均匀优化。
  - 梯度范数和调度响应长度动态（图5，附录B）。
  - 案例研究（附录C），对比标准GRPO和时间调度在推理过程质量上的差异。
- **充分性评价**：实验覆盖了多个模型规模、多种RLVR算法、多种分配策略，消融和分析充分。但仅局限于数学和通用推理领域，未涉及代码生成、对话或更广泛的语言任务，且对比方法未包含最新的PRM或过程监督方法。总得来说实验设计公平、系统，验证了时间调度的有效性。

### 6. 主要结论与发现

- **时间调度显著提升性能**：TP-Schedule在Qwen3-4B上相比GRPO在数学推理上平均提升2.2%，通用推理上提升2.7%；在更复杂的credite allocation方法（Entropy-Adv、Forking-Tok）上应用调度也带来一致改进。
- **保留策略熵**：标准优化在容纳异质性策略行为时会导致策略熵大幅下降，而时间调度能显著缓解这一问题（图3a），保持更健康的优化动态。
- **轨迹百分位数是有效标准**：与熵等代理相比，轨迹百分位数提供简单自然的区分，且与时间调度配合最佳（图4）。
- **调度顺序至关重要**：从后期token逐步前向扩展（Suffix）优于从前向后（Prefix）（图4），随机调度则完全失效。
- **优化动态结构化**：时间调度导致KL散度从后期向前期渐进传播（图3b），表明策略行为被有组织地而非均匀地更新。

### 7. 优点

- **创新性**：首次将时间维度引入RLVR信用分配，挑战了“固定标准贯穿训练”的默认假设，视角新颖。
- **简洁有效**：仅需调整训练过程中的分配标准，无需额外模型或昂贵标注，易于集成到现有框架。
- **通用性**：验证了时间调度与多种信用分配范式（连续重加权、离散掩码）及多种RL算法（GRPO、GSPO）的兼容性。
- **实验严谨**：消融和分析实验（调度函数、范围、代理选择）全面，且提供案例研究展示对推理过程质量的改善。
- **分析深度**：通过熵、KL散度、梯度范数等指标揭示了调度如何影响优化动态，提供了理解依据。

### 8. 不足与局限

- **领域覆盖有限**：实验仅聚焦数学和部分通用推理，未在代码、知识问答、指令跟随等其他常见LLM后训练场景验证。
- **缺乏与更先进方法的比较**：未与Process Reward Models（PRM）、未来KL方法（如FIPO）或更复杂的token级分配方法进行比较，仅与基于熵的简单方法对比。
- **调度参数依赖**：时间调度的最佳范围（0.6~0.8）和调度函数未见跨数据集或跨任务广泛调优，可能对手动设置敏感。
- **轨迹百分位数的简化假设**：假设每个token的位置百分位数可直接反映其策略行为重要性，但在长生成中可能存在截断、重复等干扰，论文未讨论极端情况。
- **计算开销**：虽未额外增加复杂模型，但调度本身需要跟踪训练进度并计算掩码/权重，可能带来微小的调度开销；且批量大小和rollout数量固定，未探索更大规模下的效率。
- **随机调度退化**：图4显示随机调度导致性能严重下降，暗示调度必须有结构化的信号（如轨迹位置）才会有效，限制了方法的鲁棒性。

（完）
