---
title: "LEAD: Length-Efficient Adaptive and Dynamic Reasoning for Large Language Models"
title_zh: LEAD：大语言模型的高效长度自适应动态推理
authors: "Songtao Wei, Yi Li, Zhikai Li, Xu Hu, Yuede Ji, Guanpeng Li, Feng Chen, Carl Yang, Zhichun Guo, Bingzhe Li"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09806v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对思维链轨迹效率的强化学习
tldr: 针对大语言模型推理过程中思维链（CoT）过于冗长导致资源浪费的问题，本文提出LEAD框架。该方法摒弃了传统的静态长度惩罚，采用在线自适应机制，通过动态校准正确性与效率的平衡，并根据模型自身的正确输出为每个问题设定自适应目标长度。实验表明，LEAD在保持甚至提升数学推理准确率的同时，显著缩短了生成长度，实现了最优的准确率-效率平衡。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在平衡推理准确性与长度效率时，受限于静态奖励权重和全局长度约束，难以应对不同问题的复杂差异及训练过程中的非平稳性。
method: 提出LEAD框架，利用动态校准的潜在缩放不稳定性调整奖励权重，并基于模型自身正确样本在线估计每题的自适应目标长度以实施对称效率奖励。
result: 在五个数学推理基准测试中，LEAD在显著缩短输出长度的同时，取得了比其他强化学习效率优化方法更高的准确率和准确率-效率综合评分。
conclusion: LEAD证明了通过动态、自适应的奖励机制可以有效解决大模型“过度思考”的问题，在不牺牲推理能力的前提下实现了极高的计算效率。
---

## 摘要
大型推理模型（如 OpenAI o1 和 DeepSeek-R1）随着推理能力的提升，往往变得越来越冗长。这些膨胀的思维链（CoT）轨迹通常超出了基础问题的实际需求，浪费了计算资源、延迟和上下文预算。虽然在强化学习中引入基于长度的效率奖励提供了一种自然的补救措施，但现有方法面临两个基本挑战：正确性与效率之间的最佳平衡在整个训练过程中是非平稳的，且不同问题的内在推理预算差异巨大。依赖静态奖励权重和全局长度约束不可避免地会导致在准确率下降和压缩不足之间进行妥协。为了克服这些局限性，我们提出了 LEAD（Length-Efficient Adaptive and Dynamic reasoning），这是一种用在线自适应机制取代静态启发式策略的方法。LEAD 使用势能缩放不稳定性（Potential-Scaled Instability）在每一步动态校准正确性与效率之间的权衡，将优化能力引导至最具信息量的学习信号。此外，它根据模型自身的正确采样轨迹（rollouts）在线估计每个问题的自适应目标长度，并应用对称效率奖励来同时惩罚过度思考和过度压缩。在五个数学推理基准测试上的评估结果显示，LEAD 在经强化学习训练的高效推理方法中实现了最高的准确率和准确率-效率得分（Accuracy-Efficiency Score），同时生成的输出比基础模型显著缩短。

## Abstract
Large reasoning models, such as OpenAI o1 and DeepSeek-R1, tend to become increasingly verbose as their reasoning capabilities improve. These inflated Chain-of-Thought (CoT) trajectories often exceed what the underlying problems require, wasting compute, latency, and context budgets. While introducing length-based efficiency rewards during reinforcement learning offers a natural remedy, existing methods struggle with two fundamental challenges: the optimal balance between correctness and efficiency is non-stationary throughout training, and intrinsic reasoning budgets vary drastically across problems. Relying on static reward weights and global length constraints inevitably forces a compromise between degraded accuracy and unrealized compression. To overcome these limitations, we propose LEAD (Length-Efficient Adaptive and Dynamic reasoning), a method that replaces static heuristics with online, self-adaptive mechanisms. LEAD dynamically calibrates the correctness-efficiency trade-off at each step using a Potential-Scaled Instability, directing optimization capacity to the most informative learning signal. Furthermore, it estimates an adaptive per-problem target length online based on the model's own correct rollouts, applying a symmetric efficiency reward that penalizes both overthinking and over-compression. Evaluated on five mathematical reasoning benchmarks, LEAD achieves the highest accuracy and Accuracy-Efficiency Score among RL-trained efficient-reasoning methods while producing substantially shorter outputs than the base model.

---

## 论文详细总结（自动生成）

### 论文总结：LEAD：大语言模型的高效长度自适应动态推理

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理能力的提升（如 OpenAI o1 和 DeepSeek-R1），模型生成的思维链（CoT）轨迹变得越来越冗长。这种“过度思考”现象导致了计算资源、推理延迟和上下文预算的巨大浪费。
现有的强化学习（RL）优化方法通常采用静态的长度惩罚或全局长度预算，但存在两个核心挑战：
*   **非平稳性**：在训练过程中，正确性与效率之间的最佳平衡点是动态变化的。
*   **任务异质性**：不同难度的题目所需的推理步数差异巨大，统一的长度限制会导致简单题目压缩不足，而难题因过度压缩而导致准确率下降。

#### 2. 方法论：核心思想与关键技术
LEAD 框架通过**在线自适应机制**取代了静态启发式策略，核心包含三个组件：
*   **解耦组归一化（Decoupled Group Normalization）**：分别对正确性奖励和效率奖励进行组内归一化，防止高方差信号（如长度）掩盖低方差信号（如二元正确性）。
*   **动态奖励加权（PSI 控制器）**：引入“潜在缩放不稳定性”（Potential-Scaled Instability, PSI）。它根据奖励的**不稳定性**（变异系数）和**潜力**（距离奖励上限的距离）在线调整权重 $\lambda$。这使得模型在训练初期关注效率（压缩），在效率信号饱和后自动转向优化正确性。
*   **每题在线目标长度校准（Per-problem Target-Length Calibration）**：
    *   为每个问题 $q$ 动态估计目标长度 $L^*_q$（取当前模型生成的正确样本的平均长度）。
    *   采用**对称效率奖励**：不仅惩罚超过 $L^*_q$ 的冗长输出，也惩罚远低于 $L^*_q$ 的过短输出（防止模型通过走捷径而跳过必要的推理步骤）。

#### 3. 实验设计
*   **基础模型**：DeepSeek-R1-Distill-Qwen-1.5B 和 7B。
*   **数据集**：在 MATH（Level 3-5）子集上训练。
*   **Benchmark（评估集）**：MATH-500、AIME 2024、AIME 2025、AMC 2023、OlympiadBench。
*   **对比方法**：
    *   **GRPO**：标准强化学习基准。
    *   **GDPO**：解耦归一化但使用静态权重的方法。
    *   **DRPO**：针对正确响应应用全局长度惩罚的方法。
    *   **ShorterBetter**：使用样本最优长度（SOL）作为目标的静态权重方法。

#### 4. 资源与算力
论文提到了使用 **Verl** 框架和 **vLLM** 推理引擎进行训练。
*   **具体算力信息**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）及数量，也未提及总训练时长。
*   **超参数**：列出了学习率（1.5B 为 $2\times10^{-6}$，7B 为 $1\times10^{-6}$）、Batch Size（1.5B 为 128，7B 为 32）等细节。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1.5B 和 7B 两种参数规模的模型，并在 5 个主流数学竞赛级基准上进行了测试。
*   **消融实验**：非常充分。作者对比了静态权重与动态权重的差异，并对目标长度 $L^*_q$ 的不同聚合方式（均值、最小值、中位数、全样本均值）进行了深入探讨。
*   **客观性**：通过引入 **AES（准确率-效率得分）** 这一综合指标，客观地衡量了模型在缩短长度的同时保持准确率的能力，避免了单一指标的片面性。

#### 6. 主要结论与发现
*   **性能领先**：LEAD 在所有 RL 训练的效率优化方法中取得了最高的平均准确率和 AES 得分。
*   **自适应分配**：LEAD 能够根据题目难度分配 Token。对于难题，它保留了较长的推理轨迹；对于简单题，它实现了显著的压缩。
*   **训练动态**：PSI 控制器有效地实现了“先压缩、后精炼”的在线课程学习效果，无需人工干预权重调度。

#### 7. 优点
*   **无需调参**：通过在线 PSI 控制器解决了多目标优化中权重难以手动调节的问题。
*   **尊重难度差异**：通过每题独立的目标长度，解决了全局预算导致的“一刀切”问题。
*   **对称奖励设计**：惩罚过短输出有效防止了模型因追求效率而损失推理严谨性。

#### 8. 不足与局限
*   **任务局限性**：目前仅在数学推理（结果可验证）任务上验证，对于开放式生成或主观偏好任务，如何定义“正确性”和“目标长度”仍需探索。
*   **训练开销**：由于需要为每个问题生成多个采样（Rollouts）来估计目标长度，训练阶段的计算开销高于传统的监督微调。
*   **冷启动依赖**：如果模型对某个难题完全无法生成正确答案，则无法有效估计目标长度（此时会退回到默认的最大长度限制）。

（完）
