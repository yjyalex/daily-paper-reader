---
title: Learning and Reusing Policy Decompositions for Hierarchical Generalized Planning with LLM Agents
title_zh: 面向大语言模型智能体的分层泛化规划：学习与重用策略分解
authors: "Shirin Sohrabi, Haritha Ananthakrishnan, Harsha Kokel, Kavitha Srinivas, Michael Katz"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06957v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 大模型智能体的分层任务分解与策略学习
tldr: 该研究提出HCL-GP框架，将分层任务分解与广义规划结合，用于提升LLM智能体的策略学习能力。通过自动从成功执行中提取并参数化可重用组件，构建组件库以实现组合式策略生成。在AppWorld基准测试中，该方法显著提升了模型在处理复杂及未见任务时的准确率和效率，证明了经典规划理念与LLM结合的有效性。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体在复杂任务中缺乏可重用策略、难以泛化到新场景以及长程规划效率低下的问题。
method: 提出HCL-GP方法，通过自动分解任务、参数化组件并利用语义搜索构建可重用的分层策略库。
result: "在AppWorld测试中，该方法在挑战性任务上比静态合成提升了15.8%，并使开源模型在复杂场景下的成功率从接近零提升至62.5%。"
conclusion: 将经典规划中的分层分解与广义策略学习引入LLM智能体，能显著增强其在未知和复杂环境中的泛化能力与执行效率。
---

## 摘要
我们提出了一种动态策略学习方法，该方法结合了面向大语言模型（LLM）智能体的泛化规划与分层任务分解。我们的方法，即面向泛化策略的分层组件学习（HCL-GP），学习可在不同任务实例间泛化的参数化策略，并从成功的执行中自动提取可重用组件，将其组织成组件库以进行组合式策略生成。我们解决了三个挑战：（1）通过自动分解学习组件；（2）泛化组件以最大化重用；（3）通过语义搜索进行高效检索。在 AppWorld 基准测试上的评估结果显示，我们的方法在常规任务上达到了 98.2% 的准确率，在包含未见应用的挑战性任务上达到了 97.8%，在挑战性场景下比静态合成提高了 15.8 个百分点。对于开源模型，动态重用实现了 62.5% 的成功率，而没有重用时成功率接近于零。这表明经典规划概念可以有效地与 LLM 智能体集成，从而提高准确性和效率。

## Abstract
We present a dynamic policy-learning approach that combines generalized planning and hierarchical task decomposition for LLM-based agents. Our method, Hierarchical Component Learning for Generalized Policies (HCL-GP ), learns parameterized policies that generalize across task instances and automatically extracts reusable components from successful executions, organizing them into a component library for compositional policy generation. We address three challenges: (1) learning components through automated decomposition, (2) generalizing components to maximize reuse, and (3) efficient retrieval via semantic search. Evaluated on the AppWorld benchmark, our approach achieves 98.2% accuracy on normal tasks and 97.8% on challenge tasks with unseen applications, improving 15.8 points over static synthesis on challenging scenarios. For open-source models, dynamic reuse enables 62.5% success versus near-zero without reuse. This demonstrates that classical planning concepts can be effectively integrated with LLM agents for improved accuracy and efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：面向大语言模型智能体的分层泛化规划：学习与重用策略分解 (HCL-GP)

#### 1. 核心问题与研究动机
当前的 LLM 智能体在解决复杂任务时，通常将每个任务视为孤立事件，导致系统不断重复发现已有的解决方案模式，无法积累可重用的程序性知识。
*   **研究动机**：结合**广义规划（Generalized Planning）**和**分层任务网络（HTN）**的理念，使智能体能够生成参数化的策略（Policy），并从中提取可重用的逻辑组件。
*   **核心目标**：在无需符号化领域模型或手动编码分解结构的情况下，通过与环境交互动态诱导、验证并泛化可执行的策略组件，从而提升智能体在处理新领域和复杂任务时的效率与泛化能力。

#### 2. 方法论 (HCL-GP)
HCL-GP 框架由三个核心阶段组成，形成一个闭环的动态学习系统：

*   **策略生成 (Policy Generation)**：
    *   **任务抽象与参数化**：将自然语言指令转化为高层步骤、策略签名（参数接口）和具体的参数绑定。
    *   **组件检索**：利用语义搜索（基于 BAAI 嵌入模型）从已验证的组件库中检索最相关的 $k$ 个组件。
    *   **迭代验证与调试**：生成 Python 代码形式的策略，在模拟器中执行，并根据反馈（错误信息、执行轨迹）进行自我修复。
*   **组件学习 (Learning Components)**：
    *   **自动分解**：从成功的策略代码中识别并提取具有内聚逻辑的片段（如身份验证、数据过滤）。
    *   **验证**：将提取的组件替换回原策略，确保其在所有任务实例上依然成功。
*   **组件泛化 (Generalizing Components)**：
    *   **聚类与去重**：使用语义嵌入对候选组件进行聚类，识别功能相似的片段。
    *   **抽象化**：将特定领域的组件（如 `login_to_venmo`）泛化为通用组件（如 `login_to_app`），并通过验证循环确保泛化后的组件依然正确。

#### 3. 实验设计
*   **基准测试 (Benchmark)**：使用 **AppWorld**，这是一个模拟真实应用环境（如 Gmail, Spotify, Venmo）的复杂基准，要求智能体通过 API 调用解决多步任务。
*   **实验场景**：
    *   **Normal Test**：包含与训练集相似的应用。
    *   **Challenge Test**：包含训练集中未见过的应用（如 Amazon, Gmail），测试跨领域迁移能力。
*   **对比方法**：
    *   **Leaderboard 顶尖方法**：如 AgentRL (Qwen3-14B), IBM CUGA (GPT-4.1), LOOP (Qwen2.5-32B)。
    *   **消融实验 (GP Baseline)**：不带组件重用功能的广义规划基线。
    *   **不同模型**：Claude Sonnet 4.6（闭源强模型）和 GPT-OSS 120B（开源模型）。
*   **评估指标**：任务目标完成率 (TGC) 和场景目标完成率 (SGC)。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 硬件型号或训练时长。
*   **实现细节**：实验主要通过 API 调用 LLM（Claude 和 GPT-OSS）。组件库的初始“种子”是通过在 AppWorld 的 30 个训练场景上运行该架构生成的。
*   **检索技术**：使用了 FAISS 进行近似最近邻搜索，配合 BAAI/bge-large-en-v1.5 嵌入模型。

#### 5. 实验数量与充分性
*   **实验规模**：在训练集（30个场景）、常规测试集（56个场景）和挑战测试集（139个场景）上进行了全面评估。
*   **充分性**：
    *   **消融实验**：通过对比 HCL-GP 与 GP 基线，清晰展示了组件重用的价值。
    *   **跨模型验证**：证明了该方法对开源模型性能的巨大提升（从 0% 提升至 62.5%）。
    *   **成本分析**：提供了推理成本与准确率的权衡曲线，证明了重用组件能显著降低达到相同准确率所需的调试迭代次数和 Token 消耗。
*   **客观性**：使用了标准化的 AppWorld 评估器，并与当前 SOTA 方法进行了直接对比。

#### 6. 主要结论与发现
*   **性能领先**：HCL-GP 在常规任务上达到 98.2% SGC，在挑战任务上达到 97.8% SGC，显著优于所有基线。
*   **效率提升**：动态重用组件使系统能够更早地在调试预算内解决任务，尤其是在挑战性场景中，比静态合成提高了 15.8 个百分点。
*   **开源模型救星**：对于开源模型 GPT-OSS，在没有重用的情况下几乎无法解决任何场景，但通过 HCL-GP 实现了 62.5% 的成功率。
*   **跨应用迁移**：实验证明，从一组应用中学习到的泛化组件（如通用登录逻辑）可以有效迁移到全新的应用中。

#### 7. 优点
*   **经典与现代的融合**：成功将经典规划的分层分解思想引入 LLM 智能体，且无需手动编写符号模型。
*   **动态演进**：组件库不是静态的，而是随着任务解决过程不断丰富、去重和泛化。
*   **模型无关性**：架构设计具有通用性，可适配不同的 LLM 和执行环境。
*   **端到端验证**：所有学习到的知识都经过执行验证，确保了知识库的质量。

#### 8. 不足与局限
*   **元领域依赖**：假设存在一个定义良好的“元领域”（固定的 API 空间和验证机制），在完全开放、无反馈的环境中可能难以应用。
*   **场景边界假设**：依赖于预先定义的“场景”（Scenario）标签来识别相似任务，现实中可能需要自动识别任务间的结构相似性。
*   **可扩展性挑战**：随着组件库规模扩大，组件检索和泛化阶段的 Token 消耗会增加，可能受限于 LLM 的上下文窗口。
*   **泛化的偶然性**：目前的泛化主要依赖 LLM 的语义理解，缺乏更深层的逻辑保证，泛化过程具有一定的启发式特征。

（完）
