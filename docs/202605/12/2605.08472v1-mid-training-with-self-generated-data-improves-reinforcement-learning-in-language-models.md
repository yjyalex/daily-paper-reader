---
title: Mid-Training with Self-Generated Data Improves Reinforcement Learning in Language Models
title_zh: 使用自生成数据进行中期训练可提升语言模型中的强化学习效果
authors: "Aswin RRV, Jacob Dineen, Divij Handa, Mihir Parmar, Ben Zhou, Swaroop Mishra, Chitta Baral"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08472v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于推理强化学习的自生成数据
tldr: 本研究提出在强化学习（RL）之前引入“中期训练”阶段，利用自生成的多元化数据提升大语言模型的推理能力。研究者受波利亚解题启发，通过自举框架为每个问题生成多种正确解法并进行微调。理论分析与实验结果均表明，这种方法能帮助模型掌握多种解题路径，从而在数学推理、代码生成及叙事推理等任务中显著增强 RL 的训练效果。
selection_source: fresh_fetch
motivation: 现有强化学习训练往往受限于训练数据中解题方式的单一性，导致模型难以应对需要多种推理路径的复杂问题。
method: 采用受波利亚解题法启发的自举式数据生成框架，为每个问题生成多种解题变体，并在正式 RL 之前进行中期微调。
result: 该方法在数学推理、代码生成和叙事推理等多个基准测试中均实现了显著且一致的性能提升。
conclusion: 让模型在强化学习前通过自生成数据学习多种解题方法，能有效优化策略梯度更新并提升模型的泛化推理能力。
---

## 摘要
大语言模型（LLM）中强化学习（RL）的有效性取决于在 RL 之前和期间所使用数据的性质和多样性。特别是，推理问题通常可以通过依赖不同推理形式的多种方式来解决，而训练数据中仅包含有限范围的此类方法可能会限制 RL 的有效性。受此启发，我们研究了在 RL 训练之前的中间步骤——中期训练（mid-training）中使用多样化的自生成数据。具体而言，我们采用了一个受 George Polya 解题方法启发的自举（bootstrapped）数据生成框架，为训练数据中的每个问题生成多种变体的正确答案，然后进行微调。我们首先从理论角度阐述了在此类数据上进行中期训练如何改进 RL，并解释了策略梯度（policy-gradient）更新如何激励多种方法的结合。随后，我们通过实验证明，使用我们的中期训练数据进行初始化的 RL 训练模型，在各种数学推理基准测试以及代码生成和叙事推理等其他分布外（OOD）任务中均取得了持续的改进。总之，我们的调查研究表明，语言模型通过自生成数据学习多种解题方法有助于后续的 RL。

## Abstract
The effectiveness of Reinforcement Learning (RL) in Large Language Models (LLMs) depends on the nature and diversity of the data used before and during RL. In particular, reasoning problems can often be approached in multiple ways that rely on different forms of reasoning, and exposure to only a limited range of such approaches in the training data may limit the effectiveness of RL. Motivated by this, we investigate using diverse self-generated data during mid-training as an intermediate step before RL training. Specifically, we adopt a bootstrapped data-generation framework guided by George Polya's problem-solving approaches for generating multiple variants of correct answers for each question in the training data, and then perform fine-tuning. We first provide a theoretical perspective on how mid-training on such data improves RL and explain how policy-gradient updates can incentivize combining multiple approaches. We then empirically demonstrate that RL-trained models initialized with our mid-training data achieve consistent improvements across various mathematical reasoning benchmarks and other OOD tasks like code generation and narrative reasoning. Overall, our investigative study shows that a language model learning multiple problem-solving approaches, through self-generated data helps subsequent RL.

---

## 论文详细总结（自动生成）

以下是对论文《Mid-Training with Self-Generated Data Improves Reinforcement Learning in Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：强化学习（RL）在提升大语言模型（LLM）推理能力时，往往表现为“分布锐化”（Distributional Sharpening），即它倾向于放大模型中已有的行为，而非诱导产生全新的能力。如果基础模型在 RL 之前缺乏多样化的解题思路，RL 的效果将受到严重限制。
*   **研究动机**：复杂的推理问题通常有多种解法。作者认为，在正式 RL 之前引入一个“中期训练”（Mid-Training）阶段，通过让模型学习针对同一问题的多种解题策略（Heuristics），可以为后续的 RL 提供更丰富的先验知识，从而突破性能瓶颈。

### 2. 方法论
*   **核心思想**：利用模型自生成的、受启发式策略引导的多样化正确解法进行微调。
*   **关键技术细节**：
    *   **启发式引导生成**：参考乔治·波利亚（George Pólya）的《怎样解题》，构建了 64 种启发式策略（如：类比法、引入辅助元素、分解与重组、逆向思维等）。
    *   **自举式数据构造**：
        1.  使用基础模型配合启发式描述和少量示例（Few-shot）为每个问题生成 128 个候选答案。
        2.  **过滤与筛选**：首先通过规则验证答案正确性，然后使用奖励模型（Skywork-Reward-V2）评估生成的推理链对特定启发式策略的遵循程度。
        3.  为每个问题保留 $n$ 个（$n$ 可变）遵循不同策略的正确推理轨迹。
    *   **中期训练（MT）**：在这些自生成的多元化数据上对模型进行监督微调（SFT）。
    *   **理论支撑**：论文提出定理 4.1，证明在“多模态”（N-modal）分布下，策略梯度更新更加稳健，能防止模型过早陷入单一模式（Mode Collapse），并促进模型在 RL 过程中组合不同的解题策略。

### 3. 实验设计
*   **模型与种子数据**：以 Llama 3.2-3B-Instruct 和 Qwen2.5-7B-Instruct 为基础模型；使用 GSM8K 训练集作为种子问题。
*   **基准测试（Benchmarks）**：
    *   **数学推理**：MATH-500, AIME 2024/2025, AMC 2023, HMMT 2025, OlympiadBench。
    *   **分布外（OOD）任务**：HumanEval（代码生成）、MuSR（叙事推理）。
*   **对比方法**：
    *   **Zero-Shot**：无额外训练的基础模型。
    *   **STaR**：自学推理者（Self-Taught Reasoner）方法。
    *   **Vanilla RL**：直接在基础模型上进行 GRPO 强化学习。
    *   **STaR + RL**：在 STaR 微调后的模型上进行 RL。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   中期训练（MT）进行 1 个 Epoch（约 444 个优化步）。
    *   强化学习（RL）采用 GRPO 算法，在 DAPO-Math-17k 数据集上训练 1 个 Epoch。
    *   推理加速使用了 vLLM 框架。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 $n=1$ 到 $n=64$ 不同多样性梯度的对比实验。
*   **消融实验**：
    *   **策略数量 vs. 问题数量**：在固定训练样本总量的前提下，对比了“多问题+单解法”与“少问题+多解法”的效果。
    *   **正确性验证**：测试了如果中期训练使用错误但符合策略的推理链会发生什么。
    *   **蒸馏对比**：对比了自生成数据与从更强模型（如 QwQ-32B）蒸馏数据的效果。
*   **充分性评价**：实验设计非常严谨，不仅在数学领域验证，还跨越到代码和叙事推理，且通过理论推导与推理链分析（LLM-as-judge 及人工标注）验证了“行为组合”现象，客观性较强。

### 6. 主要结论与发现
*   **性能提升**：中期训练显著提升了 RL 的效果，尤其是在 **Pass@64**（高采样次数）下表现卓越。例如在 AIME 2025 上，Pass@64 从 12.84% 提升至 18.66%。
*   **行为组合**：RL 训练后的模型能够自发地将中期训练中学到的多种独立策略组合到同一个推理链中（Compositional Behavior）。
*   **多样性胜过数量**：在预算固定时，学习同一问题的多种解法比学习更多不同问题的单一解法对 RL 的帮助更大。
*   **泛化能力**：尽管启发式策略是针对数学设计的，但其诱导的推理能力成功泛化到了代码生成和叙事推理任务中。

### 7. 优点
*   **理论与实践结合**：不仅有显著的实验提升，还通过数学证明解释了为什么多模态分布有助于策略梯度更新。
*   **无需外部蒸馏**：证明了模型可以通过“自举”实现自我进化，减少了对 GPT-4 等超强教师模型的依赖。
*   **解题策略的系统化**：首次系统地将波利亚的启发式解题法引入 LLM 的中期训练流程。

### 8. 不足与局限
*   **Pass@1 提升有限**：虽然在高采样率（Pass@64）下进步巨大，但在单次推理（Pass@1）上的提升相对平缓。
*   **计算开销**：生成 128 倍的候选数据并进行筛选需要较大的前期推理算力。
*   **策略依赖**：目前的启发式策略仍带有较强的数学色彩，对于非逻辑推理类任务（如创意写作）的适用性尚未验证。
*   **超参数敏感**：实验发现最佳策略数 $n$ 似乎与 RL 的 Rollout 组大小 $g$ 存在耦合关系。

（完）
