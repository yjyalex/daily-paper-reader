---
title: "AIPO: : Learning to Reason from Active Interaction"
title_zh: AIPO：从主动交互中学习推理
authors: "Junnan Liu, Linhao Luo, Thuy-Trang Vu, Gholamreza Haffari"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08401v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过多智能体交互增强大模型推理
tldr: 针对大语言模型在强化学习中受限于自身能力边界的问题，本文提出AIPO框架。该框架通过在训练探索阶段引入主动多智能体交互，允许模型在遇到推理瓶颈时咨询验证、知识和推理智能体，从而获取细粒度引导。结合重要性采样和裁剪策略，AIPO有效扩展了模型的推理边界，在AIME、MATH500等多个基准测试中显著提升了性能，且推理时无需依赖外部智能体。
selection_source: fresh_fetch
motivation: 现有强化学习算法受限于模型自身能力边界，且依赖外部专家演示的方法存在样本效率低和引导空间静态的问题。
method: 提出AIPO框架，使策略模型在训练中能主动咨询验证、知识和推理智能体，并采用改进的重要性采样和裁剪策略来优化学习过程。
result: 在AIME、MATH500、GPQA-Diamond和LiveCodeBench等多个推理基准测试中，AIPO一致地提升了模型的推理性能和泛化能力。
conclusion: AIPO通过主动交互机制有效地扩展了大语言模型的推理能力边界，是一种鲁棒且高效的强化学习增强方案。
---

## 摘要
大语言模型（LLMs）的最新进展展示了卓越的推理能力，这在很大程度上受到具有可验证奖励的强化学习（RLVR）的推动。然而，现有的强化学习算法面临一个根本性的局限：它们的探索在很大程度上仍受限于策略模型固有的能力边界。尽管最近的方法引入了外部专家演示来扩展这一边界，但它们通常依赖于完整的轨迹级引导，这种方式样本效率低、信息稀疏，且可能将探索限制在静态的引导空间内。受多智能体系统潜力的启发，我们提出了 AIPO，这是一种增强的强化学习框架，通过在探索过程中进行主动的多智能体交互来提升 LLM 的推理能力。具体而言，AIPO 使策略模型在遇到推理瓶颈时，能够主动咨询三个功能性协作智能体：验证智能体（Verify Agent）、知识智能体（Knowledge Agent）和推理智能体（Reasoning Agent），从而获得细粒度且有针对性的引导，在训练过程中主动扩展其能力边界。我们还引入了定制的重要性采样系数以及裁剪策略，以缓解在从智能体提供的反馈中学习时出现的离策偏差（off-policy bias）和梯度消失问题。训练完成后，策略模型可以独立进行推理，而无需依赖协作智能体。在 AIME、MATH500、GPQA-Diamond 和 LiveCodeBench 等多种推理基准测试上的广泛实验表明，AIPO 持续提升了推理性能，在不同的策略模型和 RLVR 算法中表现出稳健的泛化性，并有效扩展了策略模型的推理能力边界。

## Abstract
Recent advances in large language models (LLMs) have demonstrated remarkable reasoning capabilities, largely stimulated by Reinforcement Learning with Verifiable Rewards (RLVR). However, existing RL algorithms face a fundamental limitation: their exploration remains largely constrained by the inherent capability boundary of the policy model. Although recent methods introduce external expert demonstrations to extend this boundary, they typically rely on complete trajectory-level guidance, which is sample-inefficient, information-sparse, and may confine exploration to a static guidance space. Inspired by the potential of multi-agent systems, we propose $\textbf{AIPO}$, an enhanced reinforcement learning framework that improves LLM reasoning through active multi-agent interaction during exploration. Specifically, AIPO enables the policy model to proactively consult three functional collaborative agents, $\textit{Verify Agent}$, $\textit{Knowledge Agent}$, and $\textit{Reasoning Agent}$, when encountering reasoning bottlenecks, thereby receiving fine-grained and targeted guidance to actively expand its capability boundary during training. We further introduce a tailored importance sampling coefficient together with a clipping strategy to mitigate the off-policy bias and gradient vanishing issues that arise when learning from agent-provided feedback. After training, the policy model performs reasoning independently without relying on collaborative agents. Extensive experiments on diverse reasoning benchmarks, including AIME, MATH500, GPQA-Diamond, and LiveCodeBench, show that AIPO consistently improves reasoning performance, generalizes robustly across different policy models and RLVR algorithms, and effectively expands the reasoning capability boundary of the policy model.

---

## 论文详细总结（自动生成）

### 论文总结：AIPO —— 从主动交互中学习推理

#### 1. 核心问题与整体含义（研究动机和背景）
现有的**具有可验证奖励的强化学习（RLVR）**框架（如 DeepSeek-R1 采用的技术）虽然能提升大语言模型（LLM）的推理能力，但存在一个根本性局限：**探索空间受限于模型预训练阶段形成的固有能力边界**。
*   **现有问题**：模型只能在自己“已知”的范围内搜索更优路径，难以突破知识盲区。
*   **传统方案的不足**：引入外部专家演示（Expert Demonstrations）的方法通常依赖完整的轨迹引导，存在样本效率低、信息稀疏、引导空间静态等问题，且难以提供中间步骤的细粒度反馈。
*   **研究目标**：提出一种主动交互策略，让模型在训练探索阶段能像人类一样，遇到瓶颈时主动寻求外部协助，从而突破自身能力上限。

#### 2. 方法论：AIPO (Active and Interactive Policy Optimization)
AIPO 的核心思想是在强化学习的 **Rollout（采样）阶段引入主动交互**，并在 **Optimization（优化）阶段进行离策（Off-policy）修正**。

*   **主动交互机制**：在训练采样时，策略模型可以根据需要主动调用三个功能智能体：
    1.  **Verify Agent（验证智能体）**：验证中间结论的正确性。
    2.  **Knowledge Agent（知识智能体）**：提供必要的领域知识或事实。
    3.  **Reasoning Agent（推理智能体）**：协助解决复杂的子问题。
*   **混合策略轨迹**：生成的推理轨迹包含“内部 Token”（模型自生）和“外部 Token”（智能体提供）。
*   **关键技术优化**：
    *   **修正的重要性采样系数**：为了处理外部 Token 带来的分布偏移，AIPO 将外部智能体的分布近似为独热分布（One-hot），简化了计算并解决了词表不一致问题。
    *   **下界裁剪策略（Lower-bound Clipping）**：针对外部 Token 容易出现的梯度消失问题（当模型对正确外部 Token 的预测概率极低时），引入了一个裁剪超参数 $\omega$，确保即使是模型原本“不会”的知识也能获得足够的梯度信号进行学习。
*   **推理独立性**：训练完成后，模型不再依赖外部智能体，而是内化了交互中学到的知识和策略。

#### 3. 实验设计
*   **数据集与基准测试**：
    *   **数学推理**：AIME 2024/2025、MATH500、LiveMathBench。
    *   **科学推理**：GPQA-Diamond。
    *   **代码推理**：MBPP、LiveCodeBench。
    *   **逻辑谜题**：Reasoning-Gym。
*   **对比方法**：
    *   基础模型（Original）、监督微调（SFT）。
    *   在线蒸馏（OPSD）。
    *   标准强化学习（GRPO、PRIME、Dr.GRPO）。
    *   离策增强强化学习（LUFFY）。
*   **实验设置**：使用 Qwen2.5 (7B/32B) 和 Llama-3.2 (3B) 作为策略模型，外部协作智能体包括同规模模型及更强大的 Qwen3-30B。

#### 4. 资源与算力
*   **硬件环境**：在配备 **NVIDIA A100 GPU** 的集群上运行。
*   **软件框架**：使用 veRL 和 vLLM 框架。
*   **训练细节**：
    *   训练步数：200 步。
    *   Batch Size：256；Group Size：8。
    *   **训练时长**：以 Qwen2.5-7B 为例，标准 RLVR 耗时约 12.6 小时，AIPO（使用同规模协作智能体）耗时约 **14.7 小时**，使用 Qwen3-30B 协作则需 **17.2 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 1.5B 到 32B 不同规模的模型，并在 8 个主流推理基准上进行了测试。
*   **充分性**：
    *   进行了**消融实验**，验证了重要性采样修正、裁剪策略以及不同类型智能体的贡献。
    *   验证了 AIPO 在**不同强化学习算法**（GRPO, DAPO, GSPO）上的通用性。
    *   通过 Pass@n 指标追踪了训练动态，证明了模型确实突破了初始能力边界。
*   **客观性**：实验设置了多次运行取平均值以减少方差，并对比了多种最先进的基线方法，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能提升**：AIPO 在所有基准测试中均显著优于标准 RL 和现有的离策增强方法。
*   **边界扩展**：AIPO 能够解决模型在初始状态下无法解决的问题，证明了主动交互能有效扩展模型的“能力天花板”。
*   **泛化能力**：该方法不仅在数学领域有效，在代码、科学和逻辑推理等域外（Out-of-domain）任务上也表现出强大的泛化性。
*   **协作效应**：协作智能体的能力越强，策略模型的提升越明显；即使协作智能体与策略模型同规模，AIPO 依然优于标准 RL。

#### 7. 优点
*   **交互灵活性**：相比于静态的专家轨迹，主动交互允许模型在“真正需要”的地方获得细粒度的帮助。
*   **训练效率**：通过修正的 IS 系数和裁剪策略，解决了学习外部 Token 时的数值不稳定和梯度消失问题。
*   **内化知识**：模型在测试时无需外部工具，实现了从“求助”到“自强”的转变。

#### 8. 不足与局限
*   **任务限制**：目前仅适用于具有**可验证奖励（Verifiable Rewards）**的任务（如数学、代码），对于主观性较强的开放式任务（如创意写作）尚未验证。
*   **语言限制**：实验主要集中在英文环境，跨语言推理能力有待进一步研究。
*   **算力开销**：虽然交互是主动的，但训练过程中仍需额外维护外部智能体的推理服务，增加了显存和计算开销。
*   **模型规模**：实验最高仅验证到 32B 规模，在超大规模模型（如 70B+ 或数百 B）上的表现尚不明确。

（完）
