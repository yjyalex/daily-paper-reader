---
title: "TMAS: Scaling Test-Time Compute via Multi-Agent Synergy"
title_zh: TMAS：通过多智能体协同扩展推理侧计算
authors: "George Wu, Nan Jing, Qing Yi, Chuan Hao, Ming Yang, Feng Chang, Yuan Wei, Jian Yang, Ran Tao, Bryan Dai"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10344v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 多智能体协同用于扩展推理时的测试时计算
tldr: 针对大语言模型推理阶段计算扩展中存在的并行轨迹协调弱、历史信息利用率低等问题，本文提出了TMAS框架。该框架通过多智能体协同组织推理过程，引入分层记忆机制（经验库和准则库）实现跨轨迹的信息共享与策略引导。结合混合奖励强化学习方案，TMAS在多个推理基准测试中显著提升了迭代扩展的效果和稳定性。
selection_source: fresh_fetch
motivation: 现有的推理阶段计算扩展方法在协调并行推理轨迹和有效利用历史信息方面存在不足，限制了探索与利用的平衡。
method: 提出了TMAS框架，利用多智能体协同、分层记忆机制（经验库与准则库）以及混合奖励强化学习来优化推理过程。
result: 在挑战性推理基准上的实验表明，TMAS在迭代扩展能力上优于现有基准，且训练方案提升了扩展的有效性与稳定性。
conclusion: TMAS通过结构化的多智能体协作和记忆管理，为提升大语言模型在推理阶段的计算扩展效率提供了有效路径。
---

## 摘要
推理侧扩展（Test-time scaling）已成为通过在推理过程中分配额外计算资源来提高大语言模型推理能力的有效范式。最近的结构化方法通过在多条轨迹、细化轮次和基于验证的反馈中组织推理，进一步推进了这一范式。然而，现有的结构化推理侧扩展方法要么对并行推理轨迹的协调较弱，要么依赖于噪声较多的历史信息，且未能明确决定应保留和重用哪些内容，从而限制了其平衡探索与利用的能力。在这项工作中，我们提出了 TMAS，这是一个通过多智能体协同扩展推理侧计算的框架。TMAS 将推理组织为专门智能体之间的协作过程，实现了跨智能体、轨迹和细化迭代的结构化信息流。为了支持有效的跨轨迹协作，TMAS 引入了分层记忆：经验库（experience bank）重用底层的可靠中间结论和局部反馈，而准则库（guideline bank）记录先前探索过的高层策略，以引导后续的展开（rollouts）避开冗余的推理模式。此外，我们为 TMAS 量身定制了一种混合奖励强化学习方案，该方案在保留基础推理能力的同时，增强了经验利用率，并鼓励在先前尝试过的解题策略之外进行探索。在挑战性推理基准测试上的广泛实验表明，TMAS 比现有的推理侧扩展基准实现了更强的迭代扩展，而混合奖励训练进一步提高了跨迭代扩展的有效性和稳定性。代码和数据可在 https://github.com/george-QF/TMAS-code 获取。

## Abstract
Test-time scaling has become an effective paradigm for improving the reasoning ability of large language models by allocating additional computation during inference. Recent structured approaches have further advanced this paradigm by organizing inference across multiple trajectories, refinement rounds, and verification-based feedback. However, existing structured test-time scaling methods either weakly coordinate parallel reasoning trajectories or rely on noisy historical information without explicitly deciding what should be retained and reused, limiting their ability to balance exploration and exploitation. In this work, we propose TMAS, a framework for scaling test-time compute via multi-agent synergy. TMAS organizes inference as a collaborative process among specialized agents, enabling structured information flow across agents, trajectories, and refinement iterations. To support effective cross-trajectory collaboration, TMAS introduces hierarchical memories: the experience bank reuses low-level reliable intermediate conclusions and local feedback, while the guideline bank records previously explored high-level strategies to steer subsequent rollouts away from redundant reasoning patterns. Furthermore, we design a hybrid reward reinforcement learning scheme tailored to TMAS, which jointly preserves basic reasoning capability, enhances experience utilization, and encourages exploration beyond previously attempted solution strategies. Extensive experiments on challenging reasoning benchmarks demonstrate that TMAS achieves stronger iterative scaling than existing test-time scaling baselines, while hybrid reward training further improves scaling effectiveness and stability across iterations. Code and data are available at https://github.com/george-QF/TMAS-code.

---

## 论文详细总结（自动生成）

这是一份关于论文《TMAS: Scaling Test-Time Compute via Multi-Agent Synergy》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**推理侧扩展（Test-Time Scaling, TTS）**，即在模型推理阶段通过增加计算资源（如生成更多候选路径、进行多轮迭代）来提升大语言模型（LLM）的复杂推理能力。
*   **研究动机**：现有的TTS方法（如并行采样或自我修正）存在两个主要缺陷：一是并行推理轨迹之间缺乏有效协调，信息共享不足；二是依赖于杂乱的历史信息，缺乏明确的机制来决定哪些信息应保留或重用。
*   **核心目标**：通过多智能体协同（Multi-Agent Synergy）和分层记忆管理，在推理过程中实现更好的“探索（Exploration）”与“利用（Exploitation）”平衡。

### 2. 方法论：核心思想与关键技术
TMAS框架将推理过程组织为一个由五个专门智能体协作的迭代系统：
*   **五大智能体角色**：
    1.  **解题智能体 (Solution Agent)**：生成候选解，根据$\epsilon$-greedy策略决定是利用已有经验还是探索新路径。
    2.  **验证智能体 (Verification Agent)**：对每个候选解进行多次独立验证并打分。
    3.  **总结智能体 (Summary Agent)**：整合验证反馈，识别逻辑漏洞。
    4.  **经验智能体 (Experience Agent)**：提取低层级的可靠中间结论和避坑指南。
    5.  **准则智能体 (Guideline Agent)**：抽象高层级的解题策略，记录已尝试过的路径。
*   **分层记忆机制**：
    *   **经验库 (Experience Bank)**：存储具体的、局部的推理信号（如已验证的中间步骤），用于后续的“利用”。
    *   **准则库 (Guideline Bank)**：记录全局策略，用于引导模型避开冗余模式，实现“非冗余探索”。
*   **混合奖励强化学习 (Hybrid Reward RL)**：
    *   **标准正确性奖励**：保证基础推理能力。
    *   **经验利用奖励**：如果模型在提供经验库后能解决原本无法解决的问题，则给予额外奖励。
    *   **新颖策略探索奖励**：奖励那些尝试了准则库之外的新解题路径且结果正确的行为。

### 3. 实验设计
*   **数据集**：主要在极具挑战性的数学竞赛题目上进行测试，包括 **IMO-AnswerBench-50**（国际数学奥林匹克水平）和 **HLE-Math-100**（Human's Last Exam 数学子集）。补充测试包括 AIME26 和 HMMT-25-Nov。
*   **基座模型**：Qwen3-30B-Thinking 和 Qwen3-4B-Thinking。
*   **对比基准 (Baselines)**：
    *   非迭代：Majority Vote (MV)。
    *   迭代类：Self-Refine, Verify-Refine (V-R), PaCoRe, RSE。

### 4. 资源与算力
*   **算力投入**：论文明确提到使用了 **256 张 NVIDIA H20 GPU** 进行强化学习训练。
*   **训练细节**：采用 FP8 量化以提高效率，训练批次大小为 128，每个 Prompt 采样 16 条轨迹，最大输出长度限制为 80K/128K tokens。
*   **数据构建**：使用 DeepSeek-V3.2 作为教师模型生成冷启动数据，构建了包含经验库和准则库上下文的训练集（约 4.4K 实例）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了 20 轮迭代的深度扩展测试，这在同类研究中属于较长的观察周期。
*   **消融实验**：针对经验库、准则库、探索系数 $\epsilon$、验证次数、并行解数量等关键变量均做了详细的消融分析。
*   **客观性**：实验涵盖了从 4B 到 30B 不同规模的模型，并对比了当前最先进的 TTS 框架（如 RSE 和 PaCoRe），结果显示 TMAS 在后期迭代中具有更强的持续增长能力，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **更强的扩展性**：TMAS 在长程迭代中表现优于所有基准方法，能够持续从额外的计算中获益，而其他方法往往在 5-10 轮后陷入瓶颈。
*   **RL 的巨大增益**：经过混合奖励 RL 训练的 4B 模型，在 19 轮迭代后的表现显著缩小了与 30B 模型的差距（在 IMO 任务上差距缩小了约 59%）。
*   **探索与利用的平衡**：适度的探索系数（$\epsilon=0.2$）和适量的验证次数（8次）是性能最优的关键。

### 7. 优点
*   **结构化记忆**：通过区分“低层经验”和“高层准则”，解决了多轮推理中信息过载和噪声干扰的问题。
*   **训练与推理协同**：不仅设计了推理框架，还通过专门的 RL 方案让模型学会如何“使用记忆”和“进行探索”。
*   **小模型潜力**：证明了通过精细的推理侧管理，较小规模的模型也能通过计算换取极高的推理质量。

### 8. 不足与局限
*   **验证瓶颈**：论文发现当问题难度超过模型能力边界时，验证智能体也会失效（给错误答案打高分），这种“共享能力边界”限制了系统的上限。
*   **计算成本**：多智能体、多并行、多迭代的模式带来了极高的推理延迟和计算成本，实际应用中需权衡成本效益。
*   **数据依赖**：RL 训练依赖于高质量的冷启动数据构建，这需要更强大的教师模型支持。

（完）
