---
title: "EvoMAS: Learning Execution-Time Workflows for Multi-Agent Systems"
title_zh: EvoMAS：学习多智能体系统的执行时工作流
authors: "Chengdong Xu, Kaiqiang Ke, Ziheng Liu, Jiaqi Wei, Zibo Shao, Weile Guo, Chao Yu"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08769v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于协作推理的执行时多智能体工作流构建
tldr: 针对大模型多智能体系统在长程任务中静态工作流难以适应动态需求的问题，本文提出EvoMAS框架。该框架将工作流构建视为元级序列决策问题，通过Planner-Evaluator-Updater流水线构建任务状态，并利用学习到的工作流适配器在执行过程中动态实例化特定阶段的智能体协作模式。实验证明，EvoMAS在GAIA、HLE等复杂基准测试中优于现有静态及自动化设计方法。
selection_source: fresh_fetch
motivation: 现有的多智能体系统多采用静态工作流，难以应对长程任务中随执行阶段不断变化的子目标和信息需求。
method: 提出一种执行时工作流构建框架，通过显式任务状态建模和基于策略梯度训练的适配器，动态从智能体池中选择并组织协作结构。
result: 在GAIA、HLE和DeepResearcher等多个复杂任务基准上，EvoMAS的性能显著超过了单智能体基线和最新的自动化多智能体设计方法。
conclusion: 显式任务状态构建与学习到的动态工作流适配相结合，能有效提升多智能体系统在复杂演化任务中的适应性和成功率。
---

## 摘要
基于大语言模型（LLM）的多智能体系统通过智能体专业化、工具使用和协作推理，在复杂任务上展现出强大的潜力。然而，大多数自动化的多智能体系统设计方法仍遵循单次（one-shot）范式：在执行前优化或选择工作流，并在整个任务过程中保持不变地重复使用。这种静态协调策略不适用于长程任务，因为这类任务的子目标、中间证据和信息需求会随着多个执行阶段而演变。我们提出了 EvoMAS，一个用于执行时多智能体工作流构建的框架。EvoMAS 将工作流构建建模为沿单一任务轨迹的元级序列决策问题。在每个阶段，它通过“规划器-评估器-更新器（Planner-Evaluator-Updater）”流水线构建显式的任务状态，并使用学习到的工作流适配器（Workflow Adapter）从固定的候选智能体池中实例化特定阶段的分层工作流。该适配器使用策略梯度进行训练，以稀疏且可验证的终端任务成功作为主要监督信号，同时在极难的稀疏奖励设置下单独分析基于评估器的过程奖励。在 GAIA、HLE 和 DeepResearcher 上的实验表明，EvoMAS 优于单智能体基准和最近的自动化多智能体工作流设计方法。我们的分析进一步表明，显式任务状态构建和学习到的工作流适配提供了互补的优势。额外结果表明，当终端成功极其稀疏时，过程奖励最为有用；定性案例研究说明了 EvoMAS 能够随着任务状态的演变调整智能体间的协调。

## Abstract
Large language model (LLM)-based multi-agent systems have shown strong potential on complex tasks through agent specialization, tool use, and collaborative reasoning. However, most automated multi-agent system design methods still follow a one-shot paradigm: a workflow is optimized or selected before execution and then reused unchanged throughout the task. This static coordination strategy is ill-suited for long-horizon tasks whose subgoals, intermediate evidence, and information needs evolve over multiple execution stages. We propose EvoMAS, a framework for execution-time multi-agent workflow construction. EvoMAS formulates workflow construction as a meta-level sequential decision problem along a single task trajectory. At each stage, it constructs an explicit task state through a Planner-Evaluator-Updater pipeline and uses a learned Workflow Adapter to instantiate a stage-specific layered workflow from a fixed pool of candidate agents. The adapter is trained with policy gradients using sparse, verifiable terminal task success as the main supervision signal, while evaluator-based process reward is analyzed separately under very-hard sparse-reward settings. Experiments on GAIA, HLE, and DeepResearcher show that EvoMAS outperforms single-agent baselines and recent automated multi-agent workflow design methods. Our analyses further show that explicit task-state construction and learned workflow adaptation provide complementary benefits. Additional results indicate that process reward is most useful when terminal success is extremely sparse, and qualitative case studies illustrate that EvoMAS adapts agent coordination as the task state evolves.

---

## 论文详细总结（自动生成）

以下是对论文《EvoMAS: Learning Execution-Time Workflows for Multi-Agent Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**多智能体系统（MAS）在处理长程、复杂任务时的灵活性不足**。
*   **背景**：现有的自动化 MAS 设计方法大多遵循“单次（One-shot）”范式，即在任务执行前确定一个固定的工作流（Workflow），并在整个执行过程中保持不变。
*   **动机**：长程任务（如深度研究、复杂推理）的子目标、中间证据和信息需求会随执行阶段动态演变。静态协调策略难以应对这种不确定性，因此需要一种能够在**执行时（Execution-time）**根据当前任务状态动态调整智能体协作结构的方法。

### 2. 方法论
EvoMAS 将工作流构建建模为一个**元级马尔可夫决策过程（Meta-level MDP）**，核心组件包括：
*   **任务状态构建（Planner–Evaluator–Updater）**：
    *   **Planner**：维护子任务计划，识别当前活跃子任务。
    *   **Evaluator**：评估上一步的执行结果，提供成功/失败/需改进的反馈。
    *   **Updater**：整合计划、反馈和中间产物，生成显式的结构化任务状态 $\tilde{s}_t$。
*   **执行时工作流适配（Workflow Adapter）**：
    *   从固定的候选智能体池（如搜索、推理、总结等智能体）中，利用**交叉注意力机制（Cross-attention）**计算任务状态与智能体嵌入之间的匹配度。
    *   采用**累积概率软采样（Cumulative-mass soft sampling）**动态实例化一个分层的工作流（DAG 结构）。
*   **策略优化**：
    *   使用 **REINFORCE（策略梯度）** 算法进行训练。
    *   主要监督信号是**稀疏且可验证的终端任务成功奖励**，在极端困难场景下引入基于评估器的过程奖励（Process Reward）辅助训练。

### 3. 实验设计
*   **数据集/场景**：
    *   **GAIA**：通用 AI 助手基准测试（Level 1-3）。
    *   **HLE (Humanity's Last Exam)**：极高难度的知识密集型推理任务。
    *   **DeepResearcher**：研究型问题解决任务。
    *   **BrowserComp**：复杂的网页浏览任务。
*   **对比方法**：
    *   **单智能体基线**：GPT-4o, GPT-4o-mini。
    *   **自动化 MAS 基线**：GPTSwarm, AFlow, G-Designer, MaAS（这些均为静态或初始化时优化的方法）。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该研究基于大语言模型（LLM）API（如 GPT-4o 和 GPT-4o-mini），其主要的资源消耗体现在 API 调用成本和推理 Token 数上。论文在附录中提供了模型标识符，但未给出训练所需的总算力小时数。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准测试上进行了评估，并针对 GAIA Level 3 等极难任务进行了专项分析。
*   **消融实验**：涵盖了任务状态构建的有效性、工作流学习动态、奖励信号（终端 vs 过程奖励）的影响、以及模型底座（Backbone）能力的敏感性分析。
*   **充分性与客观性**：实验设计较为全面，通过对比不同规模的智能体池（EvoMAS-4 vs EvoMAS-7）验证了框架的扩展性。对比基线包含了最新的自动化 MAS 研究，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **动态优于静态**：EvoMAS 在所有基准测试中均显著优于静态自动化 MAS 方法，平均成功率提升显著（如在 EvoMAS-7 配置下比最强基线高出 30.2%）。
*   **互补效应**：显式的任务状态跟踪与学习到的动态适配器共同作用，缺一不可。
*   **奖励信号的阈值**：在常规任务中，稀疏的终端奖励足以训练适配器；但在极难任务中，过程奖励（PRM）是实现“零的突破”的关键。
*   **自适应行为**：定性分析显示，EvoMAS 能在任务初期倾向于信息获取，在后期转向验证和总结，实现了执行时的角色演化。

### 7. 优点
*   **范式创新**：将 MAS 设计从“执行前静态优化”转向“执行时动态构建”，更符合复杂任务的本质。
*   **可解释性**：通过显式的 Planner-Evaluator 结构，使得系统的决策过程（为何选择特定智能体）具有一定的可追踪性。
*   **通用性**：不依赖于特定领域的硬编码规则，能够通过强化学习自动发现最优的协作模式。

### 8. 不足与局限
*   **智能体池限制**：目前仍依赖于预定义的固定候选智能体池，尚未实现智能体的动态创建或开放式增长。
*   **计算成本**：由于每个阶段都需要进行状态构建和工作流采样，其推理延迟和 API 成本可能高于简单的单智能体或固定工作流方法。
*   **评估器依赖**：系统的性能在很大程度上取决于 Evaluator 的准确性，如果评估器给出错误反馈，可能会导致错误的状态转移和工作流选择。
*   **搜索空间**：工作流的最大深度和层级结构仍是预设的，限制了更复杂拓扑结构的生成。

（完）
