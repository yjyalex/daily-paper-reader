---
title: Enhanced LLM Reasoning by Optimizing Reward Functions with Search-Driven Reinforcement Learning
title_zh: 通过搜索驱动的强化学习优化奖励函数以增强大语言模型的推理能力
authors: "Arash Ahmadi, Sarah Sharif, Yaser, Banad"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.02073v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过强化学习优化数学推理的奖励函数
tldr: 本研究针对大语言模型在数学推理任务中奖励函数设计困难的问题，提出了一种搜索驱动的强化学习框架。该框架将奖励函数本身作为优化对象，利用前沿模型生成候选奖励，并通过GRPO算法在Llama-3.2-3B模型上进行快速训练与验证。经过五轮迭代优化，该方法显著提升了模型在GSM8K数据集上的推理表现，证明了自动化搜索和反馈机制在优化奖励规范方面的有效性。
selection_source: fresh_fetch
motivation: 强化学习提升模型推理能力的效果高度依赖于奖励函数的设计，而人工设计高效的奖励函数具有挑战性。
method: 提出一种搜索驱动框架，通过前沿模型生成候选奖励，并利用GRPO算法进行多轮迭代筛选、排名和反馈优化。
result: 经过五轮迭代，最佳集成奖励配置在GSM8K上的F1分数达到0.795，较仅使用基础奖励的基准提升了0.19。
conclusion: 研究表明，通过排名反馈循环持续优化奖励函数，而非简单增加奖励信号，是驱动模型推理性能提升的核心因素。
---

## 摘要
数学推理是大语言模型的一个关键基准。强化学习是提升大语言模型推理能力的标准训练后机制，但其性能对驱动策略优化的奖励函数设计仍然非常敏感。本文介绍了一种搜索驱动的框架，将奖励规范本身视为优化对象。本研究关注的设定是：基础模型保持不变，而奖励规范是主要的剩余设计杠杆。候选奖励函数由前沿语言模型生成，经过自动验证，并通过在 Llama-3.2-3B-Instruct 基础模型上使用低秩自适应（LoRA）进行的 500 步群体相对策略优化（GRPO）训练运行进行筛选，最后根据 GSM8K 测试集上的 F1 分数进行排名。前几轮的排名摘要随后被反馈到下一轮生成中。经过五轮搜索，共产生了 50 个候选奖励。平均 F1 分数从第一轮的 0.596 上升到第五轮的 0.632，表现最好的单个奖励达到了 F1 = 0.787。研究评估了排名靠前奖励的七种集成配置。最佳集成实现了 F1 = 0.795（95% 自助法置信区间 [0.756, 0.832]）和 0.660 [0.635, 0.686] 的准确率，相比仅使用基础奖励的 GRPO 基准（F1 = 0.609）获得了 0.19 的 F1 绝对增益。带有 Bonferroni 校正的两两 McNemar 检验显示，所有包含五个或更多奖励的配置在 α = 0.05/21 水平上在统计学上没有显著差异。对最佳集成进行三次随机种子重训得到的 F1 为 0.785。随机抽取的 5 奖励对照组 F1 分数骤降至 0.047，这表明是排名反馈循环而非增加奖励数量的加性信号驱动了性能提升。

## Abstract
Mathematical reasoning is a key benchmark for large language models. Reinforcement learning is a standard post-training mechanism for improving the reasoning capabilities of large language models, yet performance remains sensitive to the design of the reward function that drives policy optimization. This paper introduces a search-driven framework that treats the reward specification itself as an object of optimization. The setting of interest is one in which the base model is held fixed and the reward specification is the primary remaining design lever. Candidate reward functions are generated by a frontier language model, validated automatically, screened through 500-step Group Relative Policy Optimization (GRPO) training runs on a Llama-3.2-3B-Instruct base model with Low-Rank Adaptation (LoRA), and ranked by F1 on the GSM8K test set. Ranked summaries from prior rounds are then fed back into the next round of generation. Over five rounds, the search produces 50 candidate rewards. The mean F1 rises from 0.596 in Round 1 to 0.632 in Round 5, and the top individual reward reaches F1 = 0.787. Seven ensemble configurations of top-ranked rewards are evaluated. The best ensemble achieves F1 = 0.795 (95% bootstrap CI [0.756, 0.832]) and accuracy 0.660 [0.635, 0.686], a 0.19 absolute F1 gain over a base-rewards-only GRPO baseline (F1 = 0.609). Pairwise McNemar tests with Bonferroni correction show all five-or-more-reward configurations are statistically indistinguishable at α = 0.05/21. A three-seed re-training of the best ensemble yields F1 of 0.785. A randomly drawn 5-reward control collapses to F1 = 0.047, which shows that the ranked-feedback loop, not the additive signal of having more rewards, drives the gain.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种通过搜索驱动的强化学习（RL）来自动化优化奖励函数，从而增强大语言模型（LLM）数学推理能力的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：强化学习是提升 LLM 推理能力的关键，但其效果高度依赖于**奖励函数（Reward Function）**的设计。人工设计奖励函数不仅费时费力，且容易导致“奖励作弊”（Reward Hacking）或只能提供粗粒度的反馈（如仅判断最终答案对错）。
*   **研究动机**：在基础模型固定的情况下，奖励规范是提升性能的核心杠杆。本文旨在探索是否可以通过自动化搜索和迭代反馈，让 AI 自己发现比人工设计更有效的奖励函数。

### 2. 方法论
该论文提出了一种**生成-验证-训练-排名（Generate-Validate-Train-Rank）**的闭环框架：
*   **核心思想**：将奖励函数视为优化对象，利用前沿 LLM（如 Kimi K2）作为生成器，通过多轮迭代搜索最佳的 Python 奖励代码。
*   **关键技术细节**：
    *   **GRPO 算法**：采用群体相对策略优化（Group Relative Policy Optimization），通过组内样本的相对奖励来估计优势函数（Advantage），无需额外的 Critic 网络，显著降低了显存开销。
    *   **迭代反馈循环**：共进行 5 轮搜索，每轮生成 10 个候选奖励。每一轮结束后，根据测试集表现对奖励进行排名，并将排名摘要反馈给生成器，指导下一轮生成更具针对性的奖励。
    *   **四阶段验证沙盒**：对生成的 Python 代码进行 AST 静态分析、锁定环境执行、命名空间限制和模拟运行，确保代码的安全性和功能正确性。
    *   **奖励集成**：将搜索到的多个高分奖励函数与基础奖励（格式检查、答案对错）进行加权求和，形成更强的训练信号。

### 3. 实验设计
*   **数据集**：使用 **GSM8K**（小学数学应用题数据集），包含 7,473 个训练样本和 1,319 个测试样本。
*   **基础模型**：Llama-3.2-3B-Instruct，配合 LoRA（低秩自适应）进行参数高效微调。
*   **对比基准（Benchmark）**：
    *   **Base-only**：仅使用基础奖励（格式和答案正确性）的 GRPO 训练。
    *   **Random-5**：随机抽取 5 个生成的奖励进行组合（用于验证反馈循环的必要性）。
    *   **Off-the-shelf 模型**：对比了 Qwen 3/3.5 (2B/4B) 和 Phi-4-Mini (3.8B) 等同规模的预训练模型。
    *   **集成策略**：对比了 Top-3、Top-5、Top-10 以及基于多样性选择的集成配置。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA RTX 5090 GPU** (32GB VRAM)。
*   **训练时长**：整个搜索过程（5 轮，50 个候选奖励的筛选及集成训练）总计耗时约 **40 小时**。
*   **训练参数**：每个候选奖励筛选训练 500 步，LoRA 秩 $r=32$，使用 8-bit 量化优化器。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 50 个独立的候选奖励函数，并评估了 7 种不同的集成配置。
*   **充分性与客观性**：
    *   使用了 **10,000 次自助法（Bootstrap）重采样**来计算 95% 置信区间。
    *   进行了 **McNemar 统计检验**（配合 Bonferroni 校正）以验证不同配置间的显著性差异。
    *   针对最佳配置进行了 **3 个不同随机种子**的重复实验，证明了结果的稳健性。
    *   设计了专门的**奖励黑客审计（Audit）**，分析模型是否通过“刷行数”等手段骗取奖励。

### 6. 主要结论与发现
*   **性能显著提升**：最佳集成配置在 GSM8K 上的 F1 分数达到 **0.795**，准确率达到 **0.660**，相比仅使用基础奖励的基准（F1=0.609）提升了 **0.19** 的绝对值。
*   **反馈循环的作用**：平均 F1 从第一轮的 0.596 稳步上升至第五轮的 0.632。随机组合奖励（Random-5）导致性能崩溃（F1=0.047），证明了**排名反馈**是驱动增益的核心，而非简单的奖励堆叠。
*   **奖励类型偏好**：表现最好的奖励通常是**过程导向**的（如检查推理步骤数、是否包含计算式），它们与结果导向的基础奖励形成了良好的互补。
*   **超越同类模型**：经过微调的 3B 模型在 GSM8K 上的表现超过了 Qwen 和 Phi 等 4B 规模的竞品。

### 7. 优点
*   **自动化与低成本**：将繁琐的奖励设计自动化，且能在单张消费级 GPU 上完成，适合学术界和中小型机构。
*   **可解释性**：生成的奖励是可读的 Python 代码，人类专家可以审计、修改和组合。
*   **统计严谨**：通过置信区间、显著性检验和多种子实验，确保了结论的科学性。
*   **安全性**：设计了完善的代码验证沙盒，防止恶意或错误代码干扰训练。

### 8. 不足与局限
*   **任务覆盖面有限**：目前仅在 GSM8K（数学推理）上进行了验证，尚未扩展到代码生成、创意写作或多轮对话等领域。
*   **潜在的格式依赖**：发现的奖励函数高度依赖于特定的 `<thinking>` 和 `<solution>` 标签格式，迁移到其他格式可能需要重新搜索。
*   **模型规模限制**：实验主要基于 3B 规模的模型，尚不清楚该方法在超大规模模型（如 70B+）上的边际收益是否同样显著。
*   **搜索空间**：目前的搜索主要基于文本描述和排名反馈，尚未引入更复杂的进化算法（如交叉、变异）。

（完）
