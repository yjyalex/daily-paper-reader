---
title: "Uni-Synergy: Bridging Understanding and Generation for Personalized Reasoning via Co-operative Reinforcement Learning"
title_zh: Uni-Synergy：通过协作强化学习桥接个性化推理中的理解与生成
authors: "Zijun Shen, Sihan Yang, Ruichuan An, Ziyu Guo, Hao Liang, Ming Lu, Renrui Zhang, Wentao Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10445v1"
tags: ["query:token"]
score: 9.0
evidence: 用于个性化推理和Token级对齐的协作强化学习
tldr: 针对统一多模态模型在个性化理解与生成之间存在鸿沟的问题，本文提出Sync-R1框架。该框架通过端到端强化学习，在统一的推理循环中协同优化理解与生成任务。通过Sync-GRPO算法和动态组缩放（DGS）策略，实现了高效的任务协同与收敛。实验证明，该方法在UnifyBench++等基准上达到了SOTA水平，显著提升了跨任务推理和个性化能力。
selection_source: fresh_fetch
motivation: 现有的多模态模型主要依赖隐式的监督微调，难以充分捕捉个性化理解与生成之间的协同潜力。
method: 提出Sync-R1框架，利用Sync-GRPO强化学习算法和动态组缩放（DGS）策略，在显式推理循环中联合优化理解与生成。
result: Sync-R1在包含复杂上下文的UnifyBench++基准测试中取得了SOTA性能，展现出卓越的跨任务推理和个性化能力。
conclusion: 通过强化学习显式桥接理解与生成，可以有效提升多模态模型在复杂个性化场景下的推理表现。
---

## 摘要
统一多模态模型（UMMs）在通用任务中表现出色，但在弥合个性化理解与生成之间的差距方面仍面临挑战。先前的研究主要依赖于通过监督微调实现的隐式标记级对齐，这未能充分捕捉理解与创作之间的潜在协同作用。在这项工作中，我们提出了 Sync-R1，这是一个端到端的强化学习框架，在单一且显式的推理循环中联合优化个性化理解与生成。通过这种统一的反馈过程，Sync-R1 使个性化理解能够指导内容创作，而生成的质量则在集成的奖励景观中反过来优化理解。为了高效地协调这种双任务协同，我们引入了 Sync-GRPO，这是一种利用集成奖励系统的强化学习方法。此外，我们提出了动态组缩放（DGS），它能自适应地过滤低潜力轨迹，以减少梯度方差并加速收敛。为了更好地反映现实世界的复杂性，我们推出了 UnifyBench++，其具有更密集的文本描述和更丰富的用户上下文。实验结果表明，Sync-R1 达到了最先进的性能，展示了卓越的跨任务推理和稳健的个性化能力，且无需复杂的冷启动程序。代码和 UnifyBench++ 数据集将在以下地址发布：https://github.com/arctanxarc/UniCTokens。

## Abstract
Unified Multimodal Models (UMMs) excel in general tasks but struggle to bridge the gap between personalized understanding and generation. Prior works largely rely on implicit token-level alignment via supervised fine-tuning, which fails to fully capture the potential synergy between comprehension and creation. In this work, we propose Sync-R1, an end-to-end reinforcement learning framework that jointly optimizes personalized understanding and generation within a single, explicit reasoning loop. Through this unified feedback process, Sync-R1 enables personalized comprehension to guide content creation, while the resulting generation quality reciprocally refines understanding within an integrated reward landscape. To efficiently orchestrate this dual-task synergy, we introduce Sync-GRPO, a reinforcement learning method utilizing an ensemble reward system. Furthermore, we propose Dynamic Group Scaling (DGS), which adaptively filters low-potential trajectories to reduce gradient variance and accelerate convergence. To better reflect real-world complexity, we introduce UnifyBench++, featuring denser textual descriptions and richer user contexts. Experimental results demonstrate that Sync-R1 achieves state-of-the-art performance, showcasing superior cross-task reasoning and robust personalization without requiring complex cold-start procedures. The code and the UnifyBench++ dataset will be released at: https://github.com/arctanxarc/UniCTokens.

---

## 论文详细总结（自动生成）

这是一份关于论文《Uni-Synergy: Bridging Understanding and Generation for Personalized Reasoning via Co-operative Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的统一多模态模型（UMMs）虽然在通用任务上表现优异，但在**个性化推理**（即如何将对特定用户上下文的理解转化为高质量的个性化内容生成）方面存在断层。
*   **研究动机**：
    *   **隐式对齐的局限**：传统方法依赖监督微调（SFT）进行隐式的 Token 级对齐，难以捕捉理解与生成之间的深层协同作用。
    *   **任务孤立**：现有模型往往将理解和生成视为独立目标，缺乏显式的反馈循环。
    *   **效率挑战**：多模态强化学习（RL）通常计算成本极高，难以在复杂推理任务中快速收敛。

### 2. 方法论
Sync-R1 框架的核心思想是通过**显式的推理循环**和**协作强化学习**来协同优化理解与生成。
*   **显式协同推理循环**：
    *   **阶段 I（个性化理解）**：模型从图像或文本属性中提取细粒度的概念知识（如“某人戴着红色帽子”）。
    *   **阶段 II（上下文引导生成）**：利用阶段 I 的推理结果构建复合提示词，指导图像生成。
*   **Sync-GRPO 算法**：
    *   基于组相对策略优化（GRPO），取消了价值函数依赖。
    *   **集成奖励系统**：结合了 TIER（文本逻辑一致性）、BER（跨模态语义对齐）、DER（身份保持）和 FER（面部合成质量）四种奖励专家。
*   **动态组缩放（DGS）**：
    *   在生成早期（约 20% 的去噪步骤）评估轨迹潜力，动态过滤低质量样本。
    *   **理论支撑**：通过减少梯度方差来提高随机梯度信噪比（SNR），加速收敛。

### 3. 实验设计
*   **数据集与基准**：
    *   **UnifyBench++**：在 UnifyBench 基础上增强，包含更密集的文本描述、更复杂的推理任务（如缺失信息推理）和多样化的用户上下文。
    *   **其他基准**：Yo’LLaVA、MC-LLaVA、DreamBench 等。
*   **对比方法**：
    *   **统一模型**：UniCTokens、Yo’Chameleon、Show-o。
    *   **专用模型**：理解类（Yo’LLaVA、MC-LLaVA、RAP-MLLM）、生成类（DreamBooth、Textual Inversion）。
    *   **上限参考**：GPT-4o。
*   **评估指标**：BLEU、GPT-4o 语义评分、CLIP-T/I 相似度等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA H100 GPU**。
*   **模型规模**：以 Show-o (1.3B 参数) 作为多模态骨干网络。
*   **训练细节**：RL 阶段设置组大小 $G=9$，训练步数约为 100 步。得益于 DGS 策略，训练速度比基准快了约 **1.9 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的定量对比，涵盖了理解、生成、递归推理等多个维度。
*   **消融实验**：
    *   验证了理解推理模块（VIA/TIA）的必要性。
    *   测试了 DGS 策略对收敛速度的影响。
    *   分析了不同奖励组件（TIER/BER/DER/FER）的贡献。
    *   探讨了不同初始化策略（如直接联合训练 vs 预训练）的鲁棒性。
*   **充分性评价**：实验设计非常全面且客观，不仅对比了同类统一模型，还跨类别挑战了专用模型，并提供了详细的定性可视化对比。

### 6. 主要结论与发现
*   **协同效应**：显式的理解过程能显著提升生成的忠实度，而生成的反馈也能反过来修正模型对概念的理解。
*   **SOTA 性能**：Sync-R1 在 UnifyBench++ 的所有指标上均达到领先水平，尤其在“密集推理生成”任务中提升显著（如 CLIP-T 提升约 14%）。
*   **RL 的优越性**：相比 SFT，强化学习更能激发模型固有的潜在协同能力，且无需昂贵的冷启动微调。
*   **效率提升**：DGS 策略证明了在多模态 RL 中，通过早期筛选高潜力轨迹可以大幅降低计算开销。

### 7. 优点
*   **架构创新**：首次将显式推理循环引入个性化多模态任务，打破了理解与生成的壁垒。
*   **算法高效**：Sync-GRPO 结合 DGS 解决了多模态 RL 训练慢、方差大的痛点。
*   **基准贡献**：推出的 UnifyBench++ 为个性化推理研究提供了更具挑战性的测试环境。
*   **通用性**：不依赖特定的初始化配方，具有很强的鲁棒性。

### 8. 不足与局限
*   **领域局限**：目前主要集中在单概念个性化，对于多概念组合或极其复杂的长程推理场景探讨较少。
*   **奖励依赖**：集成奖励系统依赖于多个预训练专家模型（如 Facenet, DINOv2），这些专家模型本身的偏差可能会传递给 Sync-R1。
*   **算力门槛**：尽管有 DGS 加速，但 H100 级别的算力需求对普通研究者仍有一定门槛。

（完）
