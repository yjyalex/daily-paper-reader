---
title: Interactive Critique-Revision Training for Reliable Structured LLM Generation
title_zh: 针对可靠结构化大语言模型生成的交互式批评-修正训练
authors: "Fei Xu Yu, Zuyuan Zhang, Mahdi Imani, Nathaniel D. Bastian, Tian Lan"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08327v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于可靠结构化大模型生成与推理的 GRPO
tldr: 针对结构化决策任务中LLM输出可靠性不足的问题，本文提出DPA-GRPO训练框架。该方法通过生成器与验证器的双角色博弈，引入结构化验证干预（SAC），利用成对动作组进行策略优化。实验证明，该方法在税务计算等复杂任务中显著提升了生成的准确性、一致性及修正行为的校准度，为可靠的结构化LLM生成提供了新方案。
selection_source: fresh_fetch
motivation: 现有的LLM自我修正方法多依赖启发式讨论或自我博弈，难以满足结构化决策任务对局部正确性、全局一致性和可审计性的严格要求。
method: 提出DPA-GRPO框架，通过生成器与验证器的博弈，利用成对反事实动作组进行KL正则化的组相对策略优化（GRPO）。
result: 在TaxCalcBench TY24基准测试中，该方法显著提升了Qwen3系列模型的结构化决策准确率，并改善了验证器的错误检测与生成器的修正行为。
conclusion: DPA-GRPO通过结构化干预和博弈论优化，有效增强了LLM在复杂结构化任务中的可靠性和自我修正能力。
---

## 摘要
在表单填写、合规性检查和维护报告等结构化决策工作流中，大语言模型（LLM）的输出必须满足局部正确、全局一致，并可针对特定任务规则进行审计。现有的优化方法通常依赖于启发式辩论、自我博弈或 LLM 生成的监督，从而产生了二阶保证问题。我们提出了 DPA-GRPO（双配对动作组相对策略优化），这是一种针对具有结构化验证器干预的双人生成器-验证器博弈的配对动作训练方法。生成器提出输出，并在受到质疑时可能对其进行修正；验证器要么保持沉默，要么提出包含主张、论据和证据的安全保证案例（SAC）。这些 SAC/非 SAC 以及 KEEP/REVISE 决策诱导了配对的反事实动作组，DPA-GRPO 利用这些动作组进行特定角色的 KL 正则化 GRPO 更新。我们分析了非正则化博弈，并证明在奖励严格较低的干预或修正动作上存在正概率会产生有利的单方面偏离。在标准的随机逼近假设下，DPA-GRPO 跟踪相应的博弈常微分方程（ODE），其孤立的渐近稳定极限点是平稳的，并且是角色局部最优下的候选局部均衡。在 TaxCalcBench TY24 上的实验表明，在 Qwen3-4B 和 Qwen3-8B 模型上，DPA-GRPO 相比于零样本生成和仅生成器强化学习（RL）基线，提高了结构化决策的准确性。训练增加了正确的沉默接受，减少了遗漏的错误，并改善了校准后的修正行为，表明生成器和验证器均获得了提升。

## Abstract
In structured decision-making workflows such as form filling, compliance checking, and maintenance reporting, LLM outputs must be locally correct, globally consistent, and auditable against task-specific rules. Existing refinement methods often rely on heuristic debate, self-play, or LLM-generated supervision, creating a second-order assurance problem. We propose DPA-GRPO (Dual Paired-Action Group-Relative Policy Optimization), a paired-action training method for a two-player generator--verifier game with structured verifier interventions. The generator proposes outputs and may revise them when challenged; the verifier either remains silent or raises a safety assurance case (SAC) containing a claim, argument, and evidence. These SAC/no-SAC and KEEP/REVISE decisions induce paired counterfactual action groups, which DPA-GRPO uses for role-specific KL-regularized GRPO updates. We analyze the unregularized game and show that positive probability on strictly lower-reward intervention or revision actions creates a profitable unilateral deviation. Under standard stochastic-approximation assumptions, DPA-GRPO tracks the corresponding game ODE, whose isolated asymptotically stable limit points are stationary and candidate local equilibria under role-wise local optimality. Experiments on TaxCalcBench TY24 show that DPA-GRPO improves structured decision accuracy over zero-shot generation and generator-only RL baselines across Qwen3-4B and Qwen3-8B. Training increases correct silent acceptance, reduces missed errors, and improves calibrated revision behavior, indicating gains for both generator and verifier.

---

## 论文详细总结（自动生成）

这是一份关于论文《Interactive Critique-Revision Training for Reliable Structured LLM Generation》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：尽管 LLM 在开放式生成中表现出色，但在需要高度准确性、全局一致性和可审计性的**结构化决策工作流**（如税务申报、合规检查）中仍然脆弱。
*   **核心问题**：现有的自我修正方法（如自我博弈、多智能体辩论）存在“二阶保证问题”，即验证器本身可能产生无根据的批评，而生成器可能盲目遵循错误反馈或拒绝正确建议。
*   **研究动机**：如何通过一种系统化的训练范式，让验证器学会提供有据可查的干预，并让生成器学会根据干预进行合理的修正。

### 2. 论文提出的方法论：DPA-GRPO
*   **核心思想**：将结构化生成建模为生成器（Generator, $G$）与验证器（Verifier, $V$）之间的**双人马尔可夫博弈**，并利用**安全保证案例（SAC）**作为交互媒介。
*   **关键技术细节**：
    *   **SAC 结构**：验证器的干预不再是简单的文本，而是包含**主张（Claim）**、**论据（Argument）**和**证据（Evidence）**的结构化记录。
    *   **双配对动作（Dual Paired-Action）**：
        *   **验证器动作组**：{提出 SAC, 保持沉默 (NS)}。
        *   **生成器动作组**：在收到 SAC 后，选择 {保留原稿 (KEEP), 采用修正 (REVISE)}。
    *   **DPA-GRPO 算法**：基于组相对策略优化（GRPO），利用上述成对的动作诱导反事实训练信号。通过计算成对动作的相对优势（Advantage），对 $G$ 和 $V$ 进行角色特定的 KL 正则化策略梯度更新。
    *   **博弈论分析**：论文证明了在非正则化博弈中，任何在低奖励动作上的正概率都会导致单方面偏离；在 KL 正则化下，训练过程会趋向于局部纳什均衡（Local Nash Equilibrium）。

### 3. 实验设计
*   **数据集/场景**：**TaxCalcBench TY24**，这是一个模拟美国联邦个人所得税（Form 1040）计算的基准测试，要求模型根据原始凭证填写复杂的税务表格。
*   **对比方法**：
    *   **零样本生成（Zero-shot）**。
    *   **单智能体强化学习基线**：包括 GRPO、DAPO 和 GSPO（这些方法仅训练生成器，不涉及验证器博弈）。
*   **评估指标**：结构化决策准确率（Exact Match）、案例分类诊断（Case Taxonomy）。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA RTX A6000 GPU**。
*   **训练细节**：采用 **LoRA** 微调技术（rank=8, alpha=32），使用 AdamW 优化器，学习率为 $10^{-5}$。
*   **模型规模**：实验基于 **Qwen3-4B** 和 **Qwen3-8B** 骨干模型。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模的骨干模型上进行了完整对比，并设置了 80/20 的训练/测试集划分。
*   **消融与诊断**：论文不仅对比了最终准确率，还详细分析了**案例分类（Case Taxonomy）**的分布变化（如 Case 1 正确接受、Case 4 遗漏错误等），验证了训练是否真正改善了交互动力学。
*   **客观性**：通过 Wilson 95% 置信区间评估二项分布的不确定性，实验设计较为严谨、公平。

### 6. 论文的主要结论与发现
*   **准确率提升**：DPA-GRPO 在 Qwen3-4B 上将准确率从 0.41 提升至 0.53，在 8B 模型上从 0.53 提升至 0.57，均优于仅训练生成器的 RL 基线。
*   **行为改善**：训练显著增加了“正确的沉默接受”（Case 1），减少了验证器的“漏检”（Case 4），并提高了生成器拒绝错误修正建议的能力（Case 6a）。
*   **校准效应**：证明了结构化干预（SAC）不仅能作为解释，还能作为有效的训练信号，引导模型在受到挑战时做出更合理的决策。

### 7. 优点
*   **理论支撑扎实**：将 LLM 训练与经典的博弈论（纳什均衡）和随机逼近理论相结合，提供了收敛性分析。
*   **可解释性强**：引入 SAC 机制，使验证器的反对意见具有结构化的证据链，便于人工审计。
*   **训练效率**：利用成对动作组简化了 GRPO 的实现，避免了复杂的价值函数估计。

### 8. 不足与局限
*   **领域局限性**：目前仅在税务计算（TaxCalcBench）这一高度确定性的领域验证，对于包含模糊性或主观性的结构化任务（如法律合同审查）效果尚待观察。
*   **计算开销**：相比单智能体训练，双人博弈需要同时维护和更新两个角色的策略，推理和采样成本更高。
*   **局部最优风险**：理论分析主要集中在局部均衡，在大规模参数空间下是否存在全局更优的协作模式仍是挑战。

（完）
