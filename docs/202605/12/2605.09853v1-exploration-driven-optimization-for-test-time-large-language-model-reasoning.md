---
title: Exploration-Driven Optimization for Test-Time Large Language Model Reasoning
title_zh: 探索驱动的大语言模型测试时推理优化
authors: "Changhao Li, Yuchen Zhuang, Chenxiao Gao, Haotian Sun, Rushi Qiang, Chao Zhang, Bo Dai"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09853v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 基于强化学习的LLM推理后训练与直接偏好优化
tldr: 本研究针对大语言模型推理中强化学习导致概率分布过尖锐、不利于推理时采样多样性的问题，提出了探索驱动优化（EDO）框架。通过将奖励偏置探索目标整合进迭代式后训练（如iDPO和GRPO），EDO在提升模型推理能力的同时保持了采样多样性。实验证明，该方法在多个推理基准测试中优于强基准模型，且在推理时缩放（如自一致性采样）场景下表现尤为突出，有效平衡了探索与利用。
selection_source: fresh_fetch
motivation: 旨在解决强化学习后训练导致模型概率分布收缩，进而限制推理时采样多样性及推理缩放潜力的问题。
method: 提出EDO框架，通过将探索性奖励偏置目标引入iDPO和GRPO，鼓励模型在迭代训练中探索更多样化的解。
result: "在多个推理基准上取得1.0-1.3%的提升，并在分布外任务中表现更优，同时保持了更高的模型熵和训练稳定性。"
conclusion: EDO为平衡大模型推理中的探索与利用提供了实用框架，特别是在依赖推理时缩放的应用场景中具有显著优势。
---

## 摘要
训练后技术结合推理时缩放显著增强了大语言模型（LLM）的推理和对齐能力。然而，存在一个根本性的矛盾：推理时方法受益于从相对平坦的概率分布中进行多样化采样，而基于强化学习（RL）的训练后处理本质上会使这些分布变得尖锐。为解决这一问题，我们提出了探索驱动优化（EDO），它将奖励偏置风格的探索目标扩展到迭代式训练后处理中，并将其整合到标准 RL 目标中，在鼓励采样解的多样性的同时，促进更有效的推理时计算。我们将 EDO 融入迭代直接偏好优化（iDPO）和群体相对策略优化（GRPO），产生了两个变体：ED-iDPO 和 ED-GRPO。大量实验表明，ED-iDPO 和 ED-GRPO 均表现出更强的解多样性和更高的推理能力，尤其是在与自洽性（self-consistency）等测试时计算技术结合使用时。在三个分布内推理基准测试中，EDO 相比最强基准实现了 1.0-1.3% 的提升，并在五个分布外任务上额外获得了 1.5% 的平均增益。除了准确率之外，EDO 还保留了模型熵并稳定了 RL 训练动态，突显了其在防止过度优化崩溃方面的有效性。综上所述，这些结果确立了 EDO 作为一个在 LLM 推理中平衡探索与利用的实用框架，特别是在依赖测试时缩放的场景下。

## Abstract
Post-training techniques combined with inference-time scaling significantly enhance the reasoning and alignment capabilities of large language models (LLMs). However, a fundamental tension arises: inference-time methods benefit from diverse sampling from a relatively flattened probability distribution, whereas reinforcement learning (RL)-based post-training inherently sharpens these distributions. To address this, we propose Exploration-Driven Optimization (EDO), which extends reward-biasing style exploration objectives to iterative post-training and integrates them into standard RL objectives, encouraging greater diversity in sampled solutions while facilitating more effective inference-time computation. We incorporate EDO into iterative Direct Preference Optimization (iDPO) and Group Relative Policy Optimization (GRPO), resulting in two variants: ED-iDPO and ED-GRPO. Extensive experiments demonstrate that both ED-iDPO and ED-GRPO exhibit greater solution diversity and improved reasoning abilities, particularly when combined with test-time computation techniques like self-consistency. Across three in-distribution reasoning benchmarks, EDO achieves a 1.0-1.3\% improvement over the strongest baselines, and delivers an additional 1.5\% average gain on five out-of-distribution tasks. Beyond accuracy, EDO preserves model entropy and stabilizes RL training dynamics, highlighting its effectiveness in preventing over-optimization collapse. Taken together, these results establish EDO as a practical framework for balancing exploration and exploitation in LLM reasoning, especially in settings that rely on test-time scaling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Exploration-Driven Optimization for Test-Time Large Language Model Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大语言模型（LLM）推理能力提升中的一个**根本性矛盾**：
*   **背景**：目前提升LLM推理主要依靠强化学习（RL）后训练和推理时缩放（Test-time Scaling，如Best-of-N采样、自一致性投票）。
*   **核心问题**：传统的RL训练（如DPO、GRPO）目标是最大化奖励，这会导致模型输出的概率分布变得非常“尖锐”（收敛到少数高频路径），即**过度利用（Exploitation）**。然而，推理时缩放技术依赖于从“平坦”的分布中采样出**多样化（Diversity）**的解，即需要充分的**探索（Exploration）**。
*   **研究动机**：开发一种既能保持高奖励对齐，又能防止概率分布塌陷、鼓励解空间探索的优化框架，以最大化推理时计算的收益。

### 2. 方法论：核心思想与技术细节
论文提出了**探索驱动优化（Exploration-Driven Optimization, EDO）**框架：
*   **核心思想**：在标准的RL目标函数中引入一个“奖励偏置”项，显式地鼓励当前策略偏离之前的策略，从而维持分布的平坦度。
*   **关键技术细节**：
    *   **目标函数重构**：在迭代优化过程中，除了让模型靠近参考模型（防止崩溃）外，增加了一个反向KL散度项 $-D_{KL}(\pi_{t-1} || \pi_\theta)$。这迫使当前策略 $\pi_\theta$ 探索不同于上一轮迭代 $\pi_{t-1}$ 的新路径。
    *   **算法集成**：
        *   **ED-iDPO**：将探索项整合进迭代直接偏好优化（iDPO），通过偏好对（Chosen/Rejected）和探索偏置共同更新。
        *   **ED-GRPO**：将探索项整合进群体相对策略优化（GRPO），在计算群体优势（Advantage）的同时，对Token级别的概率分布进行平滑处理。
    *   **推理时协同**：将EDO训练出的模型与自一致性（Self-Consistency）或过程奖励模型（PRM）引导的搜索相结合。

### 3. 实验设计
*   **数据集**：
    *   **分布内（ID）训练与评估**：GSM8K（小学数学）、MATH（竞赛数学）、s1K（1000条高质量推理数据）。
    *   **分布外（OOD）泛化评估**：AIME24、AIME25、MATH500、Minerva Math、Olympiad Bench。
*   **基准模型（Backbones）**：Qwen2.5-7B-Instruct、LLaMA3.1-8B-Instruct。
*   **对比方法**：
    *   基础推理：CoT（思维链）、Self-Consistency（自一致性）。
    *   训练基准：ETO（在线DPO变体）、原生GRPO、DAPO（一种改进探索的GRPO变体）。

### 4. 资源与算力
*   **硬件**：使用了 **4块 NVIDIA H100 80GB GPU**。
*   **训练时长**：
    *   **ED-iDPO**：每轮迭代约 20-30 分钟。
    *   **ED-GRPO**：在MATH/GSM8K上每轮约 30-40 分钟；在s1K上每轮约 2 小时。
*   **技术细节**：采用了 LoRA（rank=8）、Flash Attention、ZeRO 分布式训练以及优化器状态卸载（Offloading）技术。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小学水平到奥数水平的8个不同难度的数学推理数据集，实验组数较多。
*   **消融实验**：
    *   分析了迭代次数对性能的影响。
    *   对比了不同的推理时缩放策略（自一致性 vs. Best-of-N）。
    *   **熵演化分析**：通过可视化训练过程中模型熵的变化，证明了EDO能有效防止熵崩溃。
    *   **多样性度量**：使用了 Distinct-4 指标量化生成文本的多样性。
*   **客观性**：针对开放式答案，引入了 LLM-as-a-judge（使用 gemini-2.5-flash-lite）进行自动化评估，并进行了多随机种子实验以验证鲁棒性。整体实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：EDO在所有基准测试中均优于强基准模型。在ID任务上提升 1.0-1.3%，在OOD任务上平均提升 1.5%。
*   **多样性优势**：EDO显著提升了生成解的多样性（Distinct-4指标在s1K上提升了47%），这直接增强了推理时缩放的效果。
*   **训练稳定性**：EDO能有效缓解RL训练中的“过度优化”问题，避免模型在多轮迭代后性能倒退。
*   **协同效应**：模型分布越平坦，推理时增加采样次数带来的增益越明显。

### 7. 优点
*   **理论与实践结合**：从数学上推导了奖励偏置与探索的关系，并成功将其转化为易于实现的损失函数项。
*   **即插即用**：EDO不改变原有RL算法的核心逻辑，可以轻松集成到现有的iDPO或GRPO流水线中。
*   **泛化性强**：在多个模型架构（Qwen/LLaMA）和多个难度等级的任务上均表现出一致的改进。

### 8. 不足与局限
*   **算力与模型规模限制**：由于资源限制，实验主要在7B/8B模型上进行，尚未在更大规模（如70B+）模型上验证。
*   **搜索算法瓶颈**：论文中提到的树搜索变体（SearchLLM）在某些任务上效果不如简单的自一致性投票，反映出过程奖励模型（PRM）的训练仍具挑战性。
*   **任务领域**：实验高度集中在数学推理领域，对于代码生成、创意写作等其他需要探索能力的领域尚未进行验证。

（完）
