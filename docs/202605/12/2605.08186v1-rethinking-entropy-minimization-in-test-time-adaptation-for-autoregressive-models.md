---
title: Rethinking Entropy Minimization in Test-Time Adaptation for Autoregressive Models
title_zh: 重新审视自回归模型测试时自适应中的熵最小化
authors: "Wei-Ping Huang, Chee-En Yu, Guan-Ting Lin, Hung-yi Lee"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.08186v1"
tags: ["query:token"]
score: 9.0
evidence: Token级策略梯度损失与熵最小化
tldr: 本研究针对自回归模型在测试时自适应（TTA）中熵最小化应用不统一的问题，提出了一个严谨的数学框架。通过理论推导，将熵最小化目标分解为Token级别的策略梯度损失和熵损失，从而统一了以往基于伪标签或强化学习的启发式方法。在Whisper语音识别模型上的实验证明，该方法在噪声、口音等20多个领域均能显著提升性能，为生成式模型的测试时自适应提供了坚实的理论基础。
selection_source: fresh_fetch
motivation: 现有的自回归模型测试时自适应方法多依赖于启发式策略，缺乏统一且严谨的数学理论支撑。
method: 推导出一种适用于自回归模型的严谨熵最小化公式，并将其分解为Token级别的策略梯度损失和熵损失。
result: 在Whisper ASR模型的20多个不同领域（包括噪声、口音和多语言环境）中，该方法均表现出一致的性能提升。
conclusion: 该研究为自回归模型的测试时自适应提供了统一的理论框架，并验证了其在多样化实际场景下的有效性与鲁棒性。
---

## 摘要
通过熵最小化（EM）进行的测试时自适应（TTA）已在分类任务中证明了其有效性，但其在生成式自回归模型中的应用在理论上仍然是支离破碎的。现有方法通常依赖于不同的启发式策略，例如带有伪标签的强制教学（teacher forcing）或基于策略梯度的强化学习，缺乏统一的数学基础。在这项工作中，我们通过推导专门针对自回归模型的严格 EM 公式来解决这一差异。我们展示了精确的目标函数可以自然地分解为词元级（token-level）策略梯度损失和词元级熵损失，并将先前的方法重新解释为该统一公式的部分实现。以 Whisper ASR 为实验平台，我们证明了我们的方法在包括声学噪声、口音和多语言设置在内的 20 多个不同领域中一致地提高了性能。

## Abstract
Test-Time Adaptation (TTA) via entropy minimization (EM) has proven effective for classification tasks, yet its application to generative autoregressive models remains theoretically fragmented. Existing approaches typically rely on distinct heuristics, such as teacher forcing with pseudo labels or policy-gradient-based reinforcement learning, without a unified mathematical foundation. In this work, we resolve this discrepancy by deriving a rigorous formulation of EM tailored to autoregressive models. We show that the exact objective naturally decomposes into a token-level policy gradient loss and a token-level entropy loss, and we reinterpret prior methods as partial realizations of this unified formulation. Using Whisper ASR as a testbed, we demonstrate that our approach consistently improves performance across more than 20 diverse domains, including acoustic noise, accents, and multilingual settings.

---

## 论文详细总结（自动生成）

### 论文总结：重新审视自回归模型测试时自适应中的熵最小化

#### 1. 核心问题与研究动机
*   **核心问题**：在测试时自适应（TTA）中，熵最小化（EM）在分类任务中效果显著，但在生成式自回归（AR）模型中的应用缺乏统一的数学基础。
*   **研究动机**：现有针对自回归模型的 EM 方法多依赖于碎片化的启发式策略，主要分为两类：一是基于伪标签的“强制教学”（Teacher-forcing）风格；二是基于强化学习（RL）的策略梯度风格。这两者之间的数学联系不明确，且往往只捕捉到了完整概率结构的一部分。

#### 2. 方法论
*   **核心思想**：通过严谨的数学推导，建立自回归模型熵最小化的完整梯度表达式，并证明现有的启发式方法只是该统一目标的特例。
*   **关键技术细节**：
    *   **熵估计器**：定义了序列级（Sequence-level）和词元级（Token-level）两种熵估计器，并证明了词元级估计器是无偏的。
    *   **目标函数分解**：推导出词元级 EM 损失（$L_{tok EM}$）可分解为两个关键部分：
        1.  **词元级策略梯度损失（$L_{tok PG}$）**：通过调整生成轨迹的概率，使模型倾向于低不确定性的输出。
        2.  **词元级熵损失（$L_{tok ENT}$）**：直接最小化每个位置词元分布的熵。
    *   **统一框架**：指出以往基于伪标签的方法仅优化了 $L_{tok ENT}$，而某些 RL 方法仅优化了 $L_{tok PG}$。
    *   **实现技巧**：引入了 Leave-one-out 基准线以降低 RL 梯度的方差，并提出使用 **束搜索（Beam Search）** 代替随机采样来生成候选序列（EM-tok-b），以提高优化信号的质量。

#### 3. 实验设计
*   **实验场景**：自动语音识别（ASR）。
*   **基础模型**：Whisper（包括 Tiny, Small, Base, Large 多个版本）。
*   **数据集（共 23 个领域）**：
    *   **Corrupted LibriSpeech (LS-C)**：包含 10 种不同的环境噪声（如空调声、打字声等）。
    *   **L2Arctic**：包含 6 种非母语英语口音（如中文、阿拉伯语、韩语等）。
    *   **Multilingual LibriSpeech (MLS)**：包含 7 种语言的多语言环境。
*   **对比方法（Benchmark）**：
    *   **Source**：未经过自适应的原始模型。
    *   **Greedy-EM**：基于伪标签和强制教学的传统启发式 EM 方法。
    *   **EM-seq**：本文提出的序列级 EM 方法。
    *   **EM-tok**：本文提出的词元级 EM 方法。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **超参数**：每个样本进行 10 步迭代优化，采样数量 $G=16$，使用 AdamW 优化器，学习率为 1e-3。
*   **更新范围**：仅更新 Whisper 模型的 LayerNorm 参数。

#### 5. 实验数量与充分性
*   **实验规模**：在超过 20 个不同的领域（噪声、口音、语言）进行了验证，实验覆盖面广。
*   **消融实验**：
    *   对比了 $L_{tok PG}$ 和 $L_{tok ENT}$ 各自的贡献。
    *   分析了采样数量 $G$（1 到 64）对性能的影响。
    *   分析了自适应步数（1 到 20 步）的影响。
    *   验证了不同模型规模（Tiny 到 Large）的泛化性。
*   **评价**：实验设计非常充分且客观，通过多维度的对比验证了数学推导的正确性及其在实际应用中的优越性。

#### 6. 主要结论与发现
*   **数学一致性**：完整的 EM 目标函数（结合 PG 和 ENT 损失）在所有测试场景下均优于单一的启发式损失。
*   **词元级 vs 序列级**：在 ASR 任务中，词元级 EM 始终优于序列级 EM，因为 ASR 错误通常发生在词元层面，词元级优化能提供更细粒度的指导。
*   **束搜索的有效性**：虽然束搜索会引入一定的梯度偏差，但其提供的更高质量的轨迹信号在实践中显著提升了 TTA 的效果（EM-tok-b 表现最佳）。
*   **鲁棒性**：该方法在 Whisper 模型上表现出极强的鲁棒性，能有效纠正语法错误、语音误听以及自回归模型常见的重复循环问题。

#### 7. 优点
*   **理论贡献突出**：首次为自回归模型的 EM 提供了严谨的数学推导，统一了领域内长期存在的启发式分歧。
*   **通用性强**：虽然以 ASR 为测试床，但其推导的公式适用于任何自回归生成模型（如 LLM）。
*   **实证效果显著**：在 20 多个领域一致性地降低了字错率（WER），且在不同规模的模型上均有效。

#### 8. 不足与局限
*   **计算开销**：由于需要生成多个采样序列（如 $G=16$）并进行多次迭代，其推理延迟和显存占用显著高于单样本的启发式方法，难以满足极高实时性的需求。
*   **任务局限**：实验主要集中在 ASR 任务，虽然理论通用，但在其他自回归任务（如文本摘要、对话生成）上的表现尚待进一步验证。
*   **解码策略依赖**：性能在很大程度上受到解码策略（如束搜索）的影响，解码策略与自适应目标之间的深层交互仍需进一步研究。

（完）
