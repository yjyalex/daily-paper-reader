---
title: "Rethinking RL for LLM Reasoning: It's Sparse Policy Selection, Not Capability Learning"
title_zh: 重新思考大语言模型推理中的强化学习：是稀疏策略选择，而非能力学习
authors: "Ömer Faruk Akgül, Rajgopal Kannan, Willie Neiswanger, Viktor Prasanna"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06241v2"
tags: ["query:token"]
score: 9.0
evidence: 强化学习对大模型推理路径影响的Token级分析
tldr: "本研究重新审视了强化学习（RL）在提升大模型推理能力中的作用，发现其本质是“稀疏策略选择”而非“能力习得”。通过分析发现，RL的改进仅集中在1-3%的高熵决策点，且提升的Token均在原模型的前5个候选内。据此，作者提出了ReasonMaxxer，一种无需RL的极简方法，仅在熵门控决策点应用对比损失。该方法在多个基准测试中以千分之一的训练成本达到了甚至超过了全量RL的效果，显著降低了推理优化的门槛。"
selection_source: fresh_fetch
motivation: 旨在探究强化学习在提升大模型推理能力时，是教会了模型新策略，还是仅仅重新分配了模型已有的概率分布。
method: 提出ReasonMaxxer，通过识别高熵决策点并仅在这些关键位置应用对比损失，利用少量Base模型采样进行训练而无需在线生成。
result: 在六个数学推理基准上，ReasonMaxxer仅需数十分钟单卡训练便达到了与全量RL相当或更优的性能，训练成本降低了约三个数量级。
conclusion: 提升大模型推理能力的关键在于对少数关键决策点的稀疏策略修正，而非复杂的在线强化学习过程。
---

## 摘要
强化学习已成为提升大语言模型推理能力的标准方法，但越来越多的证据表明，RL 并没有教授新的策略；它只是在基座模型已包含的解法上重新分配概率质量。在这项工作中，我们提出疑问：如果 RL 仅仅是将模型引导至其已知的路径，那么 RL 优化循环本身是否必要？通过对多个模型系列和 RL 算法进行 Token 级分析，我们发现 RL 的有益足迹是一种稀疏且可预测的修正，集中在模型不确定该选择哪个分支的高熵决策点。仅有 1-3% 的 Token 位置受到影响，被提升的 Token 始终位于基座模型的前 5 个备选项中，并且在这些少数位置进行针对性修正可以因果性地恢复 RL 准确率提升的大部分，而随机修正则会失败。基座模型自身的熵可以在无需任何 RL 训练模型的情况下识别这些位置，且整个修正过程是低维的，可以用极小比例的模型参数来表示。这些发现将推理能力的提升重新定义为稀疏策略选择，而非能力获取。我们将这一见解转化为 ReasonMaxxer，这是一种极简的无 RL 方法，仅在熵门控决策点应用对比损失，使用数百次基座模型采样且无需在线生成。在三个模型系列、六个规模和六个数学推理基准测试中，ReasonMaxxer 达到或超过了完整 RL 的性能，同时仅需数十个问题和数分钟的单 GPU 训练，训练成本降低了约三个数量级。

## Abstract
Reinforcement learning has become the standard for improving reasoning in large language models, yet evidence increasingly suggests that RL does not teach new strategies; it redistributes probability mass over solutions the base model already contains. In this work, we ask: if RL merely steers the model toward paths it already knows, is the RL optimization loop itself necessary? Through token-level analysis across multiple model families and RL algorithms, we find that RL's beneficial footprint is a sparse, predictable correction concentrated at high-entropy decision points where the model is uncertain which branch to take. Only 1--3\% of token positions are affected, the promoted token always lies within the base model's top-5 alternatives, and targeted corrections at those few positions causally recover a large fraction of RL's accuracy gain, while random corrections fail. The base model's own entropy identifies these positions without any RL-trained model, and the entire correction is low-dimensional, representable in a tiny fraction of model parameters. These findings reframe reasoning improvement as sparse policy selection, not capability acquisition. We translate this insight into ReasonMaxxer, a minimal RL-free method that applies contrastive loss only at entropy-gated decision points, using a few hundred base-model rollouts and no online generation. Across three model families, six scales, and six math reasoning benchmarks, ReasonMaxxer matches or exceeds full RL performance while requiring only tens of problems and minutes of single-GPU training, a reduction in training cost of roughly three orders of magnitude.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking RL for LLM Reasoning: It's Sparse Policy Selection, Not Capability Learning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管强化学习（RL）已成为提升大语言模型（LLM）推理能力的标准手段（如 DeepSeek-R1, OpenAI o1），但其黑盒优化过程成本极高且原理不明。
*   **核心问题**：RL 究竟是教会了模型全新的推理策略，还是仅仅在模型已有的知识库中“重新分配概率”？如果 RL 只是在做路径选择，那么昂贵的在线 RL 循环是否可以被更简单的离线方法取代？

### 2. 方法论：ReasonMaxxer
*   **核心思想**：将推理能力的提升视为一种“稀疏策略选择”问题。通过识别模型在推理过程中的关键“决策点”，仅在这些点进行微调。
*   **关键技术细节**：
    1.  **问题筛选（能力边缘）**：仅选择基座模型在多次采样中表现出“有对有错”的问题，因为这些问题提供了区分正确与错误决策的对比信号。
    2.  **决策点识别（熵门控）**：利用基座模型自身的 Token 熵（Entropy）来定位。高熵位置代表模型在多个候选路径间犹豫不决，即 RL 真正起作用的“分叉路口”。
    3.  **优势加权对比损失 ($L_{dec}$)**：在识别出的决策点上，根据采样结果的正确性赋予权重。正确路径的 Token 被强化，错误路径的 Token 被抑制。
    4.  **基座锚定 ($L_{anchor}$)**：在非决策点位置，使用 KL 散度将模型输出锚定在原始基座模型上，以保持模型原有的语言能力并防止过拟合。
    5.  **低维适配**：采用 LoRA（Rank=32）进行参数高效微调，验证了 RL 的修正信号是低维且易于捕捉的。

### 3. 实验设计
*   **数据集/场景**：涵盖 6 个主流数学推理基准：MATH-500, GSM8K, AMC 2023, AIME 2024, Minerva Math, OlympiadBench。
*   **对比方法**：对比了多种主流 RL 算法训练的模型，包括 GRPO (SimpleRL-Zoo, Qwen3), PPO (Open-Reasoner-Zero), RLOO (PRIME) 以及 DeepScaleR 等。
*   **模型系列**：跨越 Qwen2.5 (1.5B/7B/32B), Qwen3 (0.6B/4B), Mistral-7B, DeepSeek-R1-Distill-1.5B 等多个家族和规模。

### 4. 资源与算力
*   **ReasonMaxxer 算力**：极低。在 NVIDIA RTX Pro 6000 (96GB) 上，训练仅需**数分钟至数小时**。
*   **成本对比**：ReasonMaxxer 的训练成本通常在 **$4 - $25** 之间；而对比的 RL 方法（如 GRPO/PPO）成本在 **$100 - $100,000** 之间。
*   **数据效率**：仅需 **50 个** 典型问题（约 1000 条采样序列）即可达到全量 RL 的效果。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 3 个模型家族、6 种参数规模，并在 6 个基准测试上进行了验证，实验组数充足。
*   **深度分析**：除了性能对比，还进行了 Token 级差异分析、因果干预实验（证明修正 1% 的 Token 即可恢复 RL 增益）以及 LoRA 秩的消融实验。
*   **客观性**：作者直接使用了社区公开的最优 RL 检查点作为 Baseline，确保了对比的公平性。

### 6. 主要结论与发现
*   **RL 的本质是稀疏修正**：RL 仅改变了 1-3% 的 Token 位置，且这些 Token 几乎都在基座模型原本的前 5 个候选内。
*   **决策点可预测**：RL 的修正点与基座模型的高熵位置高度重合，这意味着无需 RL 也能定位关键位置。
*   **低维性**：RL 对模型的改变非常微小，不到 0.5% 的参数变化即可复现其全部推理增益。
*   **RL 循环非必要**：ReasonMaxxer 以约 **1/1000** 的计算成本，在多个基准上达到或超过了全量 RL 的性能。

### 7. 优点
*   **揭示本质**：通过严谨的 Token 级分析，打破了“推理能力必须通过大规模 RL 习得”的固有认知。
*   **极致高效**：无需在线采样、无需 Critic 模型、无需复杂的奖励建模，极大地降低了推理模型优化的门槛。
*   **即插即用**：作为一种离线对比微调方法，它非常易于在现有基座模型上实施。

### 8. 不足与局限
*   **任务覆盖面**：目前仅集中在数学推理（有明确验证答案的任务），在代码、法律或无标准答案的开放式推理任务上的表现尚待验证。
*   **依赖基座质量**：该方法属于“挖掘”而非“创造”，如果基座模型在采样中从未出现过正确答案（Pass@k=0），该方法将失效。
*   **长链推理风险**：对于需要极长思维链的任务，仅靠 Token 级的对比是否能解决深层次的逻辑漂移仍有讨论空间。

（完）
