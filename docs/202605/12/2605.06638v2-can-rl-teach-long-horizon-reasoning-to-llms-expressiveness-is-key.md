---
title: Can RL Teach Long-Horizon Reasoning to LLMs? Expressiveness Is Key
title_zh: RL 能教会 LLM 长程推理吗？表达能力是关键
authors: "Tianle Wang, Zhaoyang Wang, Guangchen Lan, Xinpeng Wei, Sipeng Zhang, Guanwen Qiu, Abulhair Saparov"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06638v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 强化学习用于LLM的长程逻辑推理
tldr: 本研究探讨了强化学习能否提升大语言模型的长程推理能力。通过新开发的 ScaleLogic 框架，研究者发现 RL 训练计算量与推理深度呈幂律关系，且该关系的扩展效率取决于逻辑表达力。实验证明，高表达力的逻辑训练能显著增强模型在下游任务中的迁移效果。这表明 LLM 的长程推理瓶颈并非源于架构，而是可以通过改进训练数据和方法来解决。
selection_source: fresh_fetch
motivation: 探究大语言模型在长程推理上的局限性是源于 Transformer 架构的本质缺陷，还是训练数据与方法的不足。
method: 提出 ScaleLogic 合成推理框架，通过独立控制推理深度和逻辑表达力（如与、或、非、全称量词）来系统研究 RL 的扩展规律。
result: 发现 RL 训练计算量与推理深度遵循幂律关系，且逻辑表达力越高，下游任务的性能增益和迁移效率越显著。
conclusion: 实验证明 LLM 的长程推理短板并非架构限制，通过提升训练数据的表达力和优化训练方法可以有效解决。
---

## 摘要
强化学习（RL）已被应用于提升大语言模型（LLM）的推理能力，但由于缺乏受控且可扩展的环境，关于训练如何随任务难度扩展的系统性研究一直受到阻碍。观察到的 LLM 在长程推理方面的缺陷引发了这样一种可能性：即这些缺陷是自回归 Transformer 架构所固有的。我们引入了 ScaleLogic，这是一个合成逻辑推理框架，它提供了对两个难度维度的独立控制：所需证明规划的深度（即长程程度）以及底层逻辑的表达能力。我们提出的框架支持广泛的逻辑：从简单的仅含蕴涵的逻辑（“if-then”）到更具表达能力的一阶推理，包括合取（“and”）、析取（“or”）、否定（“not”）和全称量化（“for all”）。利用该框架，我们展示了 RL 训练计算量 $T$ 与推理深度 $D$ 之间遵循幂律关系（$T \propto D^γ$，$R^{2} > 0.99$），且缩放指数 $γ$ 随逻辑表达能力的增强而单调增加，从 $1.04$ 升至 $2.60$。在下游数学和通用推理基准测试中，与表达能力较低的设置相比，更具表达能力的训练设置能带来更大的性能提升（最高达 $+10.66$ 分）和更高效的计算迁移，这表明模型训练的内容（而非仅仅是训练量）决定了下游迁移的效果。我们进一步证明，这种幂律关系在多种 RL 方法中均成立，且基于课程的训练显著提高了缩放效率。更广泛地，我们的结果表明，LLM 在长程推理方面的缺陷并非底层架构所固有，可以通过改进训练方法和数据来解决。

## Abstract
Reinforcement learning (RL) has been applied to improve large language model (LLM) reasoning, yet the systematic study of how training scales with task difficulty has been hampered by the lack of controlled, scalable environments. Observed LLM shortcomings in long-horizon reasoning have raised the prospect that these shortcomings are fundamental to the autoregressive transformer architecture. We introduce ScaleLogic, a synthetic logical reasoning framework that offers independent control over two axes of difficulty: the depth of the required proof planning (i.e., the horizon) and the expressiveness of the underlying logic. Our proposed framework supports a wide range of logics: from simple implication-only logic ("if-then") towards more expressive first-order reasoning with conjunction ("and"), disjunction ("or"), negation ("not"), and universal quantification ("for all"). Using this framework, we show that the RL training compute $T$ follows a power law with respect to reasoning depth $D$ ($T \propto D^γ$, $R^{2} > 0.99$), and that the scaling exponent $γ$ increases monotonically with logical expressiveness, from $1.04$ to $2.60$. On downstream mathematics and general reasoning benchmarks, more expressive training settings yield both larger performance gains (up to $+10.66$ points) and more compute-efficient transfer compared to less expressive settings, demonstrating that what a model is trained on, not just how much it is trained, shapes downstream transfer. We further show that the power-law relationship holds across multiple RL methods, and curriculum-based training substantially improves scaling efficiency. More broadly, our results demonstrate that LLM shortcomings in long-horizon reasoning are not fundamental to the underlying architecture, and can be addressed by improved training methodology and data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can RL Teach Long-Horizon Reasoning to LLMs? Expressiveness Is Key》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管强化学习（RL）提升了 LLM 的推理能力，但模型在面对需要多步逻辑链的“长程推理（Long-Horizon Reasoning）”任务时，性能往往会剧烈下降。学术界对此存在争议：这究竟是自回归 Transformer 架构的本质缺陷，还是因为缺乏高质量、可控且可扩展的训练数据？
*   **核心问题**：RL 训练在长程推理上的扩展规律（Scaling Laws）是怎样的？任务的逻辑复杂性（表达能力）如何影响训练效率和下游任务的迁移效果？

### 2. 方法论
*   **核心思想**：提出 **ScaleLogic** 框架，通过合成逻辑推理问题，实现对“推理深度（$D$）”和“逻辑表达力”两个维度的独立、精确控制。
*   **关键技术细节**：
    *   **逆向证明树构建**：从结论出发，递归地生成前提条件，直到达到预设深度 $D$。这种方法保证了证明路径的唯一性和正确性。
    *   **逻辑表达力等级**：定义了五个递进的逻辑层级：
        1.  **仅含蕴涵（Implication-only）**：简单的 if-then 链。
        2.  **+合取（+Conjunction）**：引入“且”逻辑，需同时满足多个前提。
        3.  **+否定（+Negation）**：引入“非”逻辑，需追踪命题的正负极性。
        4.  **+析取（+Disjunction）**：引入“或”逻辑，需处理不确定性的分支。
        5.  **+量化（+Quantification）**：引入全称量词（$\forall$），模拟一阶逻辑推理。
    *   **RL 训练架构**：采用 **DAPO**（GRPO 的扩展版本）算法，利用可验证的二元奖励（答案正确与否）进行后训练（Post-training）。

### 3. 实验设计
*   **实验场景**：在 ScaleLogic 环境中进行 RL 训练，观察模型达到 90% 验证准确率所需的训练步数 $T$。
*   **下游基准测试（Benchmarks）**：为了验证迁移能力，使用了 8 个真实世界的推理榜单，包括：
    *   数学类：MATH-500, AIME 2024/2025, AMC 2023, Minerva, OlympiadBench。
    *   通用/科学类：GPQA-Diamond, MMLU-Pro (STEM 子集)。
*   **对比维度**：
    *   不同逻辑表达力层级的对比。
    *   不同 RL 算法（DAPO, GRPO, GSPO）的对比。
    *   不同训练分布（均匀分布、仅困难样本、课程学习）的对比。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA B200 (180G) GPU** 集群。
*   **软件框架**：基于开源 RL 库 **verl** 进行开发。
*   **模型规模**：主要实验基于 **Qwen3-4B**（非思考版），部分关键实验在 **Qwen3-8B** 上进行了复现以验证扩展性。
*   **训练时长**：文中未给出总小时数，但记录了训练步数（最高达 800 步以上，每步涉及数百个 Prompt 的采样与更新）。

### 5. 实验数量与充分性
*   **实验规模**：进行了数百组 RL 训练运行。涵盖了从 $D=4$ 到 $D=128$ 的不同深度，以及 5 种逻辑设置。
*   **充分性**：实验设计非常系统。不仅有核心的扩展曲线分析，还包含了：
    *   **消融实验**：验证了候选答案数量（$B$）和多实体设置对难度的影响。
    *   **算法鲁棒性**：在三种不同 RL 算法上验证了幂律的一致性。
    *   **分布分析**：对比了课程学习的效果。
    *   **OOD 泛化**：测试了模型在未见深度上的表现。
*   **客观性**：通过 Z3 求解器对合成数据进行了逻辑一致性审计，确保标签 100% 正确，排除了数据噪声干扰。

### 6. 主要结论与发现
*   **幂律扩展规律**：RL 训练计算量 $T$ 与推理深度 $D$ 遵循幂律关系 $T \propto D^\gamma$（$R^2 > 0.99$）。
*   **表达力决定指数**：逻辑表达力越高，缩放指数 $\gamma$ 越大（从 1.04 升至 2.60）。这意味着逻辑越复杂，增加推理深度的成本呈多项式级增长。
*   **表达力是迁移的关键**：在相同计算量下，训练数据的逻辑表达力越高，模型在下游真实任务（如 AIME）上的提升越显著（最高提升 +10.66 分）。
*   **架构并非瓶颈**：实验证明，通过改进训练数据和方法（如课程学习），现有的 Transformer 架构完全可以掌握长程推理。

### 7. 优点
*   **高受控性**：ScaleLogic 填补了数学/代码数据难以精确控制“难度”和“深度”的空白。
*   **系统性强**：首次定量化了逻辑复杂性如何影响 RL 的训练效率。
*   **启发性**：证明了“训练什么（表达力）”比“训练多少”对提升模型通用推理能力更重要。

### 8. 不足与局限
*   **合成与真实的鸿沟**：虽然下游任务有提升，但合成逻辑语言与自然语言推理之间仍存在语义差异。
*   **模型规模限制**：实验主要集中在 4B 和 8B 模型，尚未在 70B 或更大规模的模型上验证该幂律是否保持不变。
*   **理论解释缺失**：论文给出了经验性的幂律公式，但未从数学理论上解释为什么特定的逻辑算子（如全称量词）会导致特定的指数增长。
*   **推理形式单一**：目前的框架主要基于演绎推理，未涉及归纳推理或反证法等更复杂的思维模式。

（完）
