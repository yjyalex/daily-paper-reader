---
title: "When LLMs Team Up: A Coordinated Attack Framework for Automated Cyber Intrusions"
title_zh: 当大语言模型联手：一种用于自动化网络入侵的协同攻击框架
authors: "Minfeng Qi, Tianqing Zhu, Zijie Xu, Congcong Zhu, Qin Wang, Wanlei Zhou"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08763v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于网络入侵任务分解的协同多智能体框架
tldr: 针对自动化网络入侵任务中单LLM智能体易出现上下文偏移和错误传播的问题，本文提出了CAESAR多智能体协作框架。该框架将入侵工作流分解为五种特定角色，通过有界轮次协议、持久化知识库和验证器门控机制进行协调。在25个CTF任务上的评估表明，CAESAR显著提升了任务成功率并降低了性能波动，为监控和分析复杂的LLM协同攻击行为提供了有效的结构化信号。
selection_source: fresh_fetch
motivation: 单个LLM在处理复杂入侵任务时，因将证据提取、规划与执行压缩在单一上下文中，容易导致上下文漂移和错误累积。
method: 提出CAESAR框架，通过定义五种特定角色并结合知识库、工作空间及验证器门控机制，实现受控的多智能体协同分析。
result: 在25个CTF任务的测试中，CAESAR相比单智能体基准提升了成功率，减少了性能差异，尤其在多步漏洞利用任务中表现优异。
conclusion: 角色转换和知识提升等结构化信号为监控协同LLM智能体行为提供了比单纯检查提示词和输出更有效的手段。
---

## 摘要
自动化入侵式工作流要求大语言模型（LLM）智能体在有限预算下，对部分观测结果、工具输出和可执行工件进行推理。单个 LLM 实例通常将证据提取、规划、执行和验证压缩到同一个上下文中，这增加了上下文漂移和错误传播的风险。现有的基于 LLM 的多智能体系统支持通用协作，但它们没有显式地建模多阶段入侵工作流所特有的角色边界、工件溯源和成本约束。本文提出了 CAESAR，这是一个协同多智能体框架，用于对入侵式任务中的 LLM 智能体行为进行受控分析。CAESAR 将工作流分解为五种类型的角色，并通过一个有界轮次协议对它们进行协调，该协议包含持久化知识库、每轮工作区、验证器门控的知识提升以及能力令牌写入隔离。我们在涵盖五个类别和四个 LLM 后端的 25 个 CTF 任务上对 CAESAR 进行了评估。与预算和工具访问权限匹配的单智能体基准相比，CAESAR 提高了任务成功率并降低了性能方差，在需要多步漏洞利用组合的任务上提升更为显著。一项次要的模拟交互安全研究表明，这种角色结构可以迁移到代码原生层面之外。结果表明，角色转换、工件溯源和知识提升事件为监控协同 LLM 智能体行为提供了有用的结构化信号，超越了单纯的提示词和输出检查。数据集、实现代码和评估日志已发布在 https://github.com/Xu-Qiu/CMAS。

## Abstract
Automated intrusion-style workflows require LLM agents to reason over partial observations, tool outputs, and executable artifacts under bounded budgets. A single LLM instance often compresses evidence extraction, planning, execution, and validation into one context, which increases the risk of context drift and error propagation. Existing LLM-based multi-agent systems support general collaboration, but they do not explicitly model the role boundaries, artifact provenance, and cost constraints that characterize multi-stage intrusion workflows.   This paper presents CAESAR, a coordinated multi-agent framework for controlled analysis of LLM-agent behavior in intrusion-style tasks. CAESAR decomposes the workflow into five typed roles and coordinates them through a bounded round protocol with a persistent knowledge base, a per-round workspace, validator-gated knowledge promotion, and capability-token write isolation. We evaluate CAESAR on 25 CTF tasks across five categories and four LLM backends. Compared with a single-agent baseline under matched budgets and tool access, CAESAR improves task success and reduces performance variance, with larger gains on tasks requiring multi-step exploit composition. A secondary simulated interactional-security study suggests that the role structure can transfer beyond code-native surfaces. The results indicate that role transitions, artifact provenance, and knowledge-promotion events provide useful structural signals for monitoring coordinated LLM-agent behavior beyond individual prompt and output inspection. The dataset, implementation, and evaluation logs are released at https://github.com/Xu-Qiu/CMAS.

---

## 论文详细总结（自动生成）

这是一份关于论文《When LLMs Team Up: A Coordinated Attack Framework for Automated Cyber Intrusions》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**自动化网络入侵工作流**中大语言模型（LLM）的应用局限性。
*   **研究动机**：当前的单智能体（Single-agent）LLM 在处理复杂的网络攻击（如类 APT 攻击）时，往往将证据提取、规划、执行和验证压缩在单一上下文窗口中。这导致了“上下文漂移”、幻觉、长程相干性差以及错误累积等问题。
*   **核心问题**：如何通过多智能体协作模拟人类专家团队的专业分工，以解决单模型在异构领域（如二进制分析、内存破坏）中推理崩溃的问题，并提高攻击的成功率和效率。

### 2. 方法论：CAESAR 框架
论文提出了 **CAESAR**（协同对抗执行与战略推理）框架，其核心思想是**角色专业化**与**验证器门控的知识管理**。
*   **五大角色分工**：
    1.  **侦探 (Detective)**：从目标环境中提取证据（字符串、二进制特征等）。
    2.  **战略家 (Strategist)**：将证据组织成假设图（Hypothesis Graphs）。
    3.  **将军 (General)**：在预算约束（Token、时间、风险）下，将假设转化为可执行计划。
    4.  **执行者 (Executor)**：调用特定领域的工具（如 GDB、sqlmap）执行操作。
    5.  **验证器 (Validator)**：审计执行轨迹，仅将可靠的模式和反馈提升到持久化知识库中。
*   **关键技术细节**：
    *   **有界轮次协议**：攻击按轮次进行，每轮包含初始化、证据收集、假设构建、计划形成、任务执行和评估六个阶段。
    *   **知识提升机制**：验证器根据确定性、一致性和实用性对工件评分，只有超过阈值的知识才能进入持久化知识库（K），防止错误信息干扰后续决策。
    *   **预算感知规划**：将军角色通过效用函数在成功概率与资源消耗（Token、时间、检测风险）之间进行权衡。

### 3. 实验设计
*   **数据集/场景**：
    1.  **CTF 挑战**：选取了 AntCTF × D3CTF 2021 的 25 个真实任务，涵盖 Pwn（内存破坏）、Reverse（逆向）、Crypto（密码学）、Web（Web 安全）和 Misc（杂项）五个类别。
    2.  **社会工程学渗透**：一个模拟场景，测试框架在非代码原生攻击面上的迁移能力。
*   **Benchmark 与对比方法**：
    *   **后端模型**：使用了 GPT-5、Gemini 2.5、Grok-4 和 DeepSeek-R1 四种当代主流 LLM。
    *   **对比基准**：在相同工具访问权限和环境条件下，对比了**单智能体（Single-agent）**配置。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   实验通过 API 调用 LLM 后端，但在框架设计中引入了**预算向量 $\Gamma$**，明确限制了每轮的 Token 消耗、墙钟时间和检测风险，以此作为衡量效率的指标。

### 5. 实验数量与充分性
*   **实验规模**：针对 25 个任务，每个配置重复执行 5 次以消除随机性。跨越了 4 个不同的 LLM 后端，并进行了多轮迭代测试。
*   **充分性评价**：实验设计较为充分且客观。它不仅测试了最终的成功率，还分析了角色性能轨迹、跨后端的一致性以及时间效率。引入社会工程学案例进一步验证了框架的泛化性。

### 6 主要结论与发现
*   **协同优于单体**：在所有 CTF 类别中，CAESAR 的成功率均高于单智能体，且性能波动更小。
*   **知识积累效应**：性能随轮次增加而趋于稳定，证明验证器门控的存储机制有效抑制了错误放大。
*   **后端无关性**：性能提升在不同 LLM 后端上表现一致，说明增益源于**协作结构**而非单一模型的推理上限。
*   **防御启示**：当攻击者转向多智能体协作时，传统的基于内容的过滤（Content-based filtering）将失效，防御重点应转向对**角色动态和交互结构**的监控。

### 7. 优点（亮点）
*   **结构化审计**：通过类型化接口和工件溯源，使 LLM 的每一步决策都变得可审计、可追踪。
*   **鲁棒性**：验证器机制解决了 LLM 常见的幻觉问题，确保只有经过验证的路径会被保留。
*   **高度可移植**：角色定义为类型化算子，可以轻松更换底层 LLM 后端。

### 8. 不足与局限
*   **CTF 局限性**：CTF 任务是静态且完全可观测的，缺乏真实入侵中的动态防御者、持久化需求和横向移动挑战。
*   **缺乏真实防御对抗**：实验未在具有主动防御（如 EDR、欺骗环境）的生产网络中进行测试。
*   **协作假设**：协议假设所有智能体都是诚实协作的，未考虑智能体被劫持或存在拜占庭故障的情况。
*   **社会工程学模拟**：社工实验基于 LLM 模拟的人格，可能无法完全还原人类的心理复杂性和警惕性。

（完）
