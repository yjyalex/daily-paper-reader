---
title: "Beyond Pairs: Your Language Model is Secretly Optimizing a Preference Graph"
title_zh: 超越配对：你的语言模型正秘密地优化偏好图
authors: "Ning Liu, Chuanneng Sun, Kristina Klinkner, Shervin Malmasi"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08037v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大语言模型对齐的图直接偏好优化
tldr: 针对直接偏好优化（DPO）在处理多样本排序时存在的传递性丢失和优化不稳定问题，本文提出了GraphDPO。该方法将偏好数据建模为有向无环图（DAG），通过图结构化的Plackett-Luce目标函数聚合监督信号，并引入等价类构造和真值锚定机制。实验证明，GraphDPO在推理和程序合成任务上优于传统的成对和列表式对齐方法，提供了一种更稳健、可扩展的偏好建模方案。
selection_source: fresh_fetch
motivation: 传统的DPO将多样本排序拆分为独立样本对，导致丢失了样本间的传递性信息，并引入了冗余或冲突的监督信号，影响优化稳定性。
method: 提出GraphDPO，利用有向无环图建模偏好关系，通过图邻域聚合监督信号，并结合等价类构造和真值锚定来稳定训练过程。
result: 在推理和程序合成任务上的实验表明，GraphDPO的性能显著优于成对和列表式对齐目标，同时保持了线性的计算复杂度。
conclusion: 图结构化的偏好建模为语言模型对齐提供了一种比成对对齐更具扩展性、鲁棒性且能有效利用复杂偏好结构的替代方案。
---

## 摘要
直接偏好优化（DPO）利用成对偏好比较来对齐语言模型，为来自人类反馈的强化学习（RLHF）提供了一种简单且有效的替代方案。然而，在许多实际场景中，训练数据包含每个提示词的多次采样（rollouts），这诱导出了丰富的偏好结构，而成对 DPO 无法利用这些结构。将此类数据折叠为独立的配对会丢弃传递性，引入冗余或冲突的监督，并可能导致优化不稳定。我们提出了图直接偏好优化（GraphDPO），这是 DPO 的一种原则性泛化，它在由采样排名诱导的有向无环偏好图上运行。GraphDPO 将支配关系编码为边，并优化一个受 Plackett-Luce 启发的图结构目标函数，该函数聚合了图邻域上的监督，在强制执行传递性的同时，将标准 DPO 作为特例涵盖在内。为了处理离散或稀疏信号，我们引入了一种等价类构建方法，其中具有相同偏好的响应形成图层，且层内边贡献零损失，从而防止产生伪梯度。尽管利用了完整的图结构，GraphDPO 通过高效的 log-sum-exp 聚合，保持了每个提示词的线性复杂度。我们还通过插入经过验证的解作为支配节点，并应用退火调度来进一步整合可选的真值锚定（ground-truth anchoring），这在稳定早期训练的同时逐渐放宽了预言机（oracle）监督。在推理和程序合成任务上的实验证明了其卓越的性能，表明图结构偏好建模是成对和列表式对齐目标的具有扩展性且鲁棒的替代方案。

## Abstract
Direct Preference Optimization (DPO) aligns language models using pairwise preference comparisons, offering a simple and effective alternative to Reinforcement Learning (RL) from human feedback. However, in many practical settings, training data consists of multiple rollouts per prompt, inducing rich preference structure that pairwise DPO fails to exploit. Collapsing such data into independent pairs discards transitivity, introduces redundant or conflicting supervision, and can lead to unstable optimization. We propose Graph Direct Preference Optimization (GraphDPO), a principled generalization of DPO that operates over directed acyclic preference graphs induced by rollout rankings. GraphDPO encodes dominance relations as edges and optimizes a graph-structured Plackett--Luce-inspired objective that aggregates supervision over graph neighborhoods, enforcing transitivity while recovering standard DPO as a special case. To handle discrete or sparse signals, we introduce an equivalence-class construction where responses with identical preferences form graph layers, and intra-layer edges contribute zero loss, preventing spurious gradients. Despite leveraging full graph structure, GraphDPO maintains linear per-prompt complexity via efficient log-sum-exp aggregation. We further incorporate optional ground-truth anchoring by inserting verified solutions as dominant nodes and applying an annealed schedule that stabilizes early training while gradually relaxing oracle supervision. Experiments on reasoning and program synthesis tasks demonstrate superior performance, suggesting that graph-structured preference modeling is a scalable and robust alternative to pairwise and listwise alignment objectives.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Pairs: Your Language Model is Secretly Optimizing a Preference Graph》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于**现有的直接偏好优化（DPO）及其变体在处理多样本偏好数据时的局限性**。
*   **背景**：在实际训练中，一个 Prompt 通常会生成多个采样（Rollouts），这些采样之间存在复杂的排序或偏好结构。
*   **动机**：传统的 DPO 将这些多样本数据拆解为独立的“样本对”（Pairs），这会导致：
    1.  **丢失传递性**：无法利用 A > B > C 这种全局结构信息。
    2.  **监督冗余与冲突**：独立的配对可能引入相互矛盾的梯度信号。
    3.  **优化不稳定**：在处理具有相同偏好等级（如同样正确或错误）的样本时，缺乏有效的处理机制。
*   **含义**：本文提出将偏好建模为**有向无环图（DAG）**，通过图结构来统一成对（Pairwise）和列表式（Listwise）的偏好对齐。

### 2. 方法论：GraphDPO
GraphDPO 的核心思想是将每个 Prompt 的多个响应构建为一个偏好图，并在此图上进行全局一致的优化。
*   **偏好图表示**：节点为响应采样，有向边表示支配关系（即 $y_i \succ y_j$）。
*   **等价类构造（Equivalence Classes）**：为了处理离散信号（如数学题的对/错），将具有相同偏好水平的响应归为一类。仅在不同类之间建立边，类内不产生损失，从而避免了对相似样本进行强制排序产生的伪梯度。
*   **图结构目标函数**：
    *   受 Plackett-Luce 模型启发，为每个节点定义一个“被支配邻域”（Dominated Neighborhood）。
    *   **局部损失**：对于每个节点，计算其相对于其支配的所有节点的 log-sum-exp 概率。公式上，它鼓励当前响应的分数高于其所有“下属”响应的聚合分数。
    *   **线性复杂度**：通过前缀聚合技术，在层级 DAG 中实现了 $O(K)$ 的线性复杂度（$K$ 为采样数），避免了成对方法中 $O(K^2)$ 的计算开销。
*   **真值锚定（Ground-Truth Anchoring）**：将已知的正确答案（GT）作为图中的绝对支配节点。引入**退火调度**：训练初期给予 GT 极高权重以稳定模型，后期逐渐降低权重以允许模型探索更多样化的生成路径。

### 3. 实验设计
*   **数据集/场景**：
    1.  **数学推理**：GSM8K（基础）、MATH-500（高难度多步推理）。
    2.  **程序合成**：APPS（代码生成，基于单元测试反馈）。
*   **基座模型**：Qwen3-4B-Thinking-2507（推理增强型）和 Qwen3-4B-Instruct-2507（指令微调型）。
*   **对比方法（Baselines）**：
    *   **基础**：OTS（原始模型）、SFT（有监督微调）。
    *   **强化学习/对齐**：GRPO（组相对策略优化）、SPIN（自博弈）。
    *   **偏好优化变体**：MPO（多偏好优化）、SWEPO（加权组对齐）。
    *   **列表式方法**：PRO（偏好排名优化）、LiPO（列表式偏好优化）。

### 4. 资源与算力
*   **硬件**：使用了单节点 **8 张 NVIDIA H100 (80GB)** GPU。
*   **框架**：使用 FSDP2 进行模型分片，使用 vLLM 进行高效采样（Tensor Parallelism = 1）。
*   **训练细节**：Batch Size 为 32，使用 AdamW 优化器，学习率采用余弦退火策略。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了完整评估，每个任务均对比了 8 种以上的基准方法。
*   **消融实验**：
    *   验证了**有无真值锚定**的影响。
    *   测试了**采样组大小 $K$**（从 2 到 8）对性能的提升效果。
    *   分析了**锚定权重 $\lambda_{gt}$** 的敏感度。
*   **客观性**：GraphDPO 的结果报告了三次不同随机种子的均值和标准差，而基准方法由于计算资源限制采用了单次运行结果。实验设计涵盖了不同类型的任务（数学 vs 代码）和不同类型的模型，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：GraphDPO 在所有任务上均显著优于 DPO、GRPO 及列表式方法（如 LiPO）。在 MATH-500 上，GraphDPO 比最强的列表式基准高出约 3.2%。
*   **结构优势**：实验证明，尊重偏好的“部分排序”（Partial Order）和传递性比强制执行“全排序”（Total Order）更有效，尤其是在存在大量等价响应的情况下。
*   **锚定效应**：真值锚定在训练早期起到了关键的稳定作用，特别是在采样质量较低时，能有效防止模型偏离正确分布。
*   **扩展性**：随着采样数 $K$ 的增加，GraphDPO 的性能提升比成对方法更稳健。

### 7. 优点
*   **理论优雅**：将 DPO 泛化到图论框架，统一了多种偏好建模形式。
*   **计算高效**：尽管利用了复杂的图结构，但通过数学简化保持了线性复杂度，适合大规模生产环境。
*   **鲁棒性强**：等价类构造巧妙地解决了离散反馈（对/错）中的平局问题，减少了噪声干扰。

### 8. 不足与局限
*   **依赖采样质量**：虽然有锚定机制，但如果模型初始采样完全无法覆盖正确解空间，图结构的优势可能受限。
*   **超参数敏感性**：虽然文中做了敏感度分析，但退火调度的初始权重和衰减率可能仍需针对不同任务进行精细调优。
*   **应用范围**：目前主要在具有明确客观标准（数学、代码）的任务上验证，在主观性较强（如创意写作、对话风格）的任务中，如何定义“等价类”和“图边”可能更具挑战性。
*   **数据处理**：APPS 数据集经过了较多的人工过滤（去除了部分题目和示例），这可能导致其绝对分数与其他论文不可直接比对。

（完）
