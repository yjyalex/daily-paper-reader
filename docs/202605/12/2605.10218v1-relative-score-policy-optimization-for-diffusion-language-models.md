---
title: Relative Score Policy Optimization for Diffusion Language Models
title_zh: 扩散语言模型的相对分值策略优化
authors: "Zichao Yu, Shengze Xu, Bingqing Jiang, Wenyi Zhang, Difan Zou"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10218v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 改进扩散语言模型推理能力的策略优化方法
tldr: 针对扩散语言模型（dLLM）在强化学习后训练中因缺乏可计算的序列级对数概率比而导致训练不稳定的问题，本文提出了相对分值策略优化（RSPO）。该方法利用可验证奖励来校准噪声似然估计，将奖励优势转化为当前策略与参考策略之间相对对数比的目标值。实验证明，RSPO在数学推理和规划任务中表现优异，显著提升了dLLM的推理能力。
selection_source: fresh_fetch
motivation: 扩散语言模型在强化学习中由于缺乏精确的序列级对数概率比，导致基于ELBO的近似方法方差大且训练不稳定。
method: 提出RSPO算法，通过比较奖励优势与目标相对对数比的差距来校准噪声估计，从而稳定策略更新。
result: 在数学推理和规划基准测试中，RSPO显著提升了模型性能，尤其在规划任务上表现强劲。
conclusion: RSPO为扩散语言模型的强化学习提供了一种简单且有效的校准机制，克服了似然估计不准确带来的挑战。
---

## 摘要
扩散大语言模型（dLLMs）为并行且高效的文本生成提供了一条极具前景的路径，但提升其推理能力需要有效的后训练。带有可验证奖励的强化学习（RLVR）是实现这一目标的自然选择，然而，由于缺乏在标准策略优化中起核心作用的可处理序列级对数比（log-ratios），其在 dLLMs 中的应用受到了阻碍。缺乏可处理的序列级对数比迫使现有方法依赖于高方差的基于 ELBO 的近似，在这种情况下，高验证器奖励可能会放大不准确的分值估计，并导致强化学习训练不稳定。为了克服这一问题，我们提出了相对分值策略优化（RSPO），这是一种简单的 RLVR 方法，利用可验证奖励来校准 dLLMs 中带有噪声的似然估计。我们算法的核心基于一个关键观察：奖励优势不仅可以被解释为更新方向，还可以被视为当前策略与参考策略之间相对对数比的目标。因此，RSPO 通过比较奖励优势与奖励隐含的目标相对对数比来校准这种带有噪声的相对对数比估计，并根据当前估计值与目标值之间的差距（而非仅根据原始优势）来更新策略。在数学推理和规划基准测试上的实验表明，RSPO 在规划任务上取得了显著的提升，并在数学推理性能上具有竞争力。

## Abstract
Diffusion large language models (dLLMs) offer a promising route to parallel and efficient text generation, but improving their reasoning ability requires effective post-training. Reinforcement learning with verifiable rewards (RLVR) is a natural choice for this purpose, yet its application to dLLMs is hindered by the absence of tractable sequence-level log-ratios, which are central to standard policy optimization. The lack of tractable sequence-level log-ratios forces existing methods to rely on high-variance ELBO-based approximations, where high verifier rewards can amplify inaccurate score estimates and destabilize RL training. To overcome this issue, we propose \textbf{R}elative \textbf{S}core \textbf{P}olicy \textbf{O}ptimization (RSPO), a simple RLVR method that uses verifiable rewards to calibrate noisy likelihood estimates in dLLMs. The core of our algorithm relies on a key observation: a reward advantage can be interpreted not only as an update direction, but also as a target for the relative log-ratio between the current and reference policies. Accordingly, RSPO calibrates this noisy relative log-ratio estimate by comparing its reward advantage with the reward-implied target relative log-ratio, updating the policy according to the gap between the current estimate and the target rather than the raw advantage alone. Experiments on mathematical reasoning and planning benchmarks show that RSPO yields especially strong gains on planning tasks and competitive mathematical-reasoning performance.

---

## 论文详细总结（自动生成）

以下是对论文《Relative Score Policy Optimization for Diffusion Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
扩散大语言模型（dLLMs）通过迭代去噪生成文本，打破了自回归模型的从左到右生成模式，具有并行生成的潜力。然而，在进行强化学习（RL）后训练时，dLLMs 面临一个核心障碍：**无法直接计算序列级的对数概率比（log-ratios）**。
现有的方法通常使用证据下界（ELBO）作为似然的近似替代，但这种近似具有高方差和噪声。在带有可验证奖励的强化学习（RLVR）中，可靠的奖励信号与带有噪声的似然估计之间存在失配，导致高奖励可能会过度放大不准确的分值估计，从而使训练不稳定。

### 2. 方法论：核心思想与技术细节
论文提出了**相对分值策略优化（RSPO）**，其核心思想是将奖励优势（Reward Advantage）视为当前策略与参考策略之间相对对数比的“目标值”，而不仅仅是一个更新方向。

*   **核心步骤：**
    1.  **相对分值计算：** 计算当前模型与参考模型（Reference Model）之间的 ELBO 分值差异，并进行序列长度归一化。
    2.  **微批次中心化（Centering）：** 在同一提示词（Prompt）生成的响应组内，对相对分值进行中心化处理，以消除提示词相关的偏移量。
    3.  **残差反馈机制：** 引入反馈系数 $\lambda$，计算奖励隐含的目标分值与当前中心化分值之间的“残差”（Gap）。
    4.  **策略更新：** 传统的策略梯度使用固定奖励作为权重，而 RSPO 使用该“残差”作为反馈系数。如果当前分值低于目标，则增加权重；如果已超过目标，则减小或反向调整权重。
*   **理论支撑：** 论文证明了 RSPO 在一阶梯度上等价于一种匹配二次目标的校准损失，能够将策略引导至一个有限的相对分值目标，而非无限制地推高似然。

### 3. 实验设计
*   **数据集/场景：**
    *   **数学推理：** GSM8K、MATH500。
    *   **规划任务：** Countdown（倒计时游戏）、Sudoku（数独）。
*   **基准模型（Base Model）：** LLaDA-8B-Instruct（一种大规模扩散语言模型）。
*   **对比方法（Baselines）：** 包括 d1 (Diffu-GRPO)、VRPO、wd1、SAPO 和 TraceRL 等针对扩散模型的最新强化学习算法。

### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA H100-80G GPU**。
*   **训练细节：** 采用 LoRA（秩 r=128，缩放因子 $\alpha=64$）进行参数高效微调。
*   **时长：** 数学任务训练约 1.5k 步，规划任务训练约 2k 步。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了逻辑严密的数学推理和搜索空间巨大的规划任务，实验设置较为全面。
*   **消融实验：** 论文针对反馈系数 $\lambda$、参考模型减法、中心化操作等核心组件进行了详细的消融研究，验证了各部分的必要性。
*   **客观性：** 针对数独任务，作者专门按解法划分训练/测试集以防止记忆效应，体现了实验的严谨性。整体实验设计对比了多种 SOTA 方法，结果具有说服力。

### 6. 主要结论与发现
*   **规划任务表现强劲：** RSPO 在数独和倒计时任务上取得了巨大的性能提升（例如数独准确率从基准的 20% 左右提升至 90% 以上）。
*   **数学推理具竞争力：** 在 GSM8K 和 MATH500 上，RSPO 在短生成长度下表现优于所有基准，长生成长度下也保持了顶尖水平。
*   **训练稳定性：** 实验观察到 RSPO 的奖励增长曲线比对比方法更平滑且最终水平更高，证明了残差反馈机制在校准噪声估计方面的有效性。

### 7. 优点
*   **简单有效：** 算法实现简单，不需要复杂的轨迹追踪或大规模蒙特卡洛采样。
*   **理论完备：** 建立了从 KL 正则化策略改进到相对分值目标的理论联系，并提供了梯度等价性证明。
*   **解决痛点：** 直接针对扩散模型似然估计噪声大的问题，通过“目标校准”而非“盲目推高”来稳定训练。

### 8. 不足与局限
*   **超参数敏感性：** 反馈系数 $\lambda$ 需要手动调节，目前尚未实现自动自适应调整。
*   **局部理论：** 理论分析主要集中在一阶局部更新，缺乏全局收敛性的严格证明。
*   **任务依赖：** 虽然在规划任务上提升巨大，但在某些数学推理场景下（如长文本生成）的优势相对较小。
*   **符号约定：** 实现时对 ELBO 的正负号约定非常敏感，若处理不当会导致更新方向错误。

（完）
