---
title: "EquiMem: Calibrating Shared Memory in Multi-Agent Debate via Game-Theoretic Equilibrium"
title_zh: EquiMem：通过博弈论均衡校准多智能体辩论中的共享内存
authors: "Yuqiao Meng, Sakshi Sunil Narvekar, Luoxi Tang, Rupali Rajendra Vaje, Yingxue Zhang, Muchao Ye, Zhaohan Xi"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09278v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 通过博弈论校准多智能体辩论中的共享内存
tldr: 针对多智能体辩论（MAD）中共享内存易受错误信息污染的问题，本文提出EquiMem。该方法将内存更新建模为零信任博弈，利用博弈论均衡来校准内存信任度。EquiMem通过算法量化更新，无需LLM判断，在多种内存架构和基准测试中表现优异，且推理开销极低，显著提升了系统的鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体辩论系统中的共享内存容易因单个错误条目导致下游推理崩溃，而现有的防御手段往往依赖于存在相同失效模式的LLM判断。
method: 将内存更新建模为零信任博弈，利用智能体已有的检索查询和路径作为证据，通过博弈论均衡在推理阶段算法化地校准内存信任度。
result: 实验表明EquiMem在多种基准测试和内存架构上均优于现有防御机制，能有效抵御对抗性智能体，且推理开销几乎可以忽略不计。
conclusion: EquiMem为多智能体协作提供了一种无需额外LLM开销的内存校准方案，通过博弈论视角增强了共享内存系统的可靠性。
---

## 摘要
多智能体辩论（MAD）系统日益依赖共享内存来支持长程推理，但这种便利性也带来了一个关键漏洞：单个损坏的条目就可能污染下游的内存增强推理，而仅靠辩论本身无法过滤此类错误。现有的保护机制通过启发式方法或基于大语言模型（LLM）的验证来过滤条目，但它们依赖于具有相同失败模式的 AI 判断，并忽略了 MAD 中的跨智能体动态。我们通过将 MAD 中的内存更新建模为一个零信任内存博弈来填补这一空白，在该博弈中，不假设任何智能体是诚实的，博弈的均衡作为最优内存信任的指标。在这一均衡的指导下，我们提出了 EquiMem，这是一种推理阶段的校准机制，它根据共享内存状态对每次更新进行算法量化，利用智能体现有的检索查询和遍历路径作为证据，而不是征求任何 LLM 的判断。EquiMem 实现了针对嵌入式和基于图的内存的校准。在不同的基准测试、MAD 框架和内存架构中，它始终优于现有的保护机制，在对抗性智能体下保持鲁棒，且推理开销微乎其微。

## Abstract
Multi-agent debate (MAD) systems increasingly rely on shared memory to support long-horizon reasoning, but this convenience opens a critical vulnerability: a single corrupted entry can contaminate the downstream memory-augmented reasoning, and debate alone fails to filter such errors. Existing safeguards filter entries via heuristics or LLM-based validation, yet they rely on AI judgments that share the same failure modes and overlook the cross-agent dynamics of MAD. We address this gap by formulating memory updating in MAD as a zero-trust memory game, in which no agent is assumed honest and the game's equilibrium serves as an indicator of optimal memory trust. Guided by this equilibrium, we propose EquiMem, an inference-time calibration mechanism that quantifies each update algorithmically against the shared memory state, using agents' existing retrieval queries and traversal paths as evidence rather than soliciting any LLM judgment. EquiMem instantiates calibration for both embedding- and graph-based memory, and across diverse benchmarks, MAD frameworks, and memory architectures, it consistently outperforms existing safeguards, remains robust under adversarial agents, and incurs negligible inference overhead.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **EquiMem** 的新框架，旨在解决多智能体辩论（Multi-Agent Debate, MAD）系统中共享内存易受错误信息污染的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多智能体辩论系统中，多个智能体通常共享一个内存空间以支持长程推理。然而，这种机制存在严重漏洞：一旦某个智能体产生幻觉或错误信息并写入共享内存，这些“毒素”会迅速污染下游推理过程，导致整个系统崩溃。
*   **研究背景**：现有的防御手段（如启发式过滤或利用 LLM 作为裁判进行审核）存在局限性。LLM 裁判往往与生成错误的智能体具有相同的失效模式（即也会产生幻觉），且这些方法忽略了智能体之间动态博弈的本质，且增加了额外的推理开销。
*   **整体含义**：论文试图通过博弈论的视角，在不依赖额外 LLM 判断的情况下，通过算法校准内存条目的信任度，从而增强 MAD 系统的鲁棒性。

### 2. 方法论：核心思想与技术细节
EquiMem 的核心是将内存更新过程建模为一个**零信任博弈（Zero-Trust Memory Game）**。
*   **核心思想**：不假设任何智能体是诚实的。系统通过评估智能体提交更新时的“证据”（即其检索查询和在内存图中的遍历路径）与当前内存状态的一致性，利用博弈论中的均衡状态来量化该更新的可信度。
*   **关键技术细节**：
    *   **零信任建模**：将内存更新视为博弈，其中智能体是提议者，系统是验证者。
    *   **算法化校准（Inference-time Calibration）**：EquiMem 不调用 LLM 进行判断，而是根据智能体已有的检索行为（Retrieval Queries）和路径（Traversal Paths）进行数学计算。
    *   **两种内存架构支持**：
        *   **基于嵌入的内存（Embedding-based）**：通过计算更新向量与现有高置信度向量的余弦相似度和分布一致性来校准。
        *   **基于图的内存（Graph-based）**：通过分析路径的连通性和逻辑闭环来评估更新的合理性。
    *   **均衡引导**：利用博弈论均衡（Equilibrium）作为最优信任指标，动态调整每个条目在后续推理中的权重。

### 3. 实验设计
*   **数据集/场景**：主要针对复杂的多跳推理任务，使用了 **HotpotQA**、**MuSiQue** 和 **2WikiMultiHopQA** 等基准数据集。
*   **Benchmark 与对比方法**：
    *   **Baseline**：无内存模式、原始共享内存（无过滤）。
    *   **对比防御机制**：基于启发式的过滤（Heuristic-filter）、基于 LLM 的验证过滤（LLM-as-a-judge）。
    *   **对抗环境**：引入了“对抗性智能体”，专门生成误导性信息以测试系统的防御能力。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确提及具体的 GPU 型号、数量或训练时长。
*   **推理开销**：论文强调 EquiMem 是一种**推理阶段（Inference-time）**的机制，由于采用了算法化量化而非 LLM 判断，其**推理开销微乎其微（Negligible overhead）**，这与需要频繁调用 API 的 LLM 过滤方法形成鲜明对比。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了多种不同的基准测试集、多种 MAD 框架以及两种主流的内存架构（向量型和图型）。
*   **充分性评价**：通过在不同任务和对抗场景下的表现对比，实验设计较为全面。特别是引入对抗性智能体的实验，有力地证明了该方法在极端情况下的鲁棒性。实验结果显示 EquiMem 在所有配置下均优于现有防御机制，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：EquiMem 显著提高了 MAD 系统在多跳推理任务中的准确率，尤其是在存在错误信息干扰的情况下。
*   **鲁棒性强**：在对抗性攻击下，EquiMem 能够有效识别并抑制错误条目的权重，防止污染扩散。
*   **高效性**：证明了通过数学/博弈论手段校准内存的可行性，且无需支付昂贵的 LLM 验证成本。

### 7. 优点
*   **无需 LLM 裁判**：避开了“用 AI 监督 AI”可能导致的循环失效问题，降低了成本和延迟。
*   **理论支撑扎实**：引入博弈论均衡概念，为多智能体协作中的信任问题提供了严谨的数学框架。
*   **通用性强**：同时适配向量数据库和知识图谱两种内存模式，易于集成到现有的 MAD 框架中。

### 8. 不足与局限
*   **证据依赖性**：该方法高度依赖于智能体提供的检索路径和查询质量。如果智能体的检索行为本身极度匮乏或完全随机，校准效果可能会打折扣。
*   **冷启动问题**：在博弈初期，如果内存中缺乏足够的“锚点”信息，可能难以准确计算初始更新的均衡信任度。
*   **复杂任务适配**：虽然在多跳 QA 上表现优异，但在需要高度创造性或主观判断（而非事实检索）的任务中，如何定义“证据”和“均衡”仍具挑战。

（完）
