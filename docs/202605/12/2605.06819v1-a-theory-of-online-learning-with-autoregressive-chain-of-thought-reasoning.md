---
title: A Theory of Online Learning with Autoregressive Chain-of-Thought Reasoning
title_zh: 具有自回归思维链推理的在线学习理论
authors: "Ilan Doron-Arad, Idan Mehalel, Elchanan Mossel"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06819v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 自回归思维链推理与反馈模型的在线学习理论
tldr: 本研究探讨了自回归思维链推理的在线学习理论，重点分析了在不同反馈模式下学习未知下文生成器的错误界限。研究对比了仅观察最终输出的端到端模型与观察完整生成轨迹的思维链模型。结果表明，端到端模型的错误界限随生成步数呈对数增长，而思维链模型则能完全消除对步数的依赖，为理解大模型推理能力的学习效率提供了理论基础。
selection_source: fresh_fetch
motivation: 旨在研究自回归生成过程中生成步数对在线学习错误界限的影响，以及中间推理步骤对提升学习效率的作用。
method: 建立了在线学习框架，对比分析了端到端（仅看结果）和思维链（看完整轨迹）两种反馈模式下的错误界限增长率。
result: 证明了端到端模式下错误界限随步数呈对数级增长且存在上限，而思维链模式下错误界限与步数无关。
conclusion: 研究揭示了思维链推理在降低学习复杂度方面的关键作用，并解决了自回归线性阈值类学习中的多个理论开放问题。
---

## 摘要
自回归生成是大语言模型机制的核心。它可以被视为下一个标记（next-token）生成器的重复应用：从输入字符串（提示词）开始，生成器被应用 $M$ 步，最后一个生成的标记被视为最终输出。[Joshi et al., 2025] 提出了一个 PAC 模型，用于研究这一过程中产生的输入-输出映射的可学习性。我们开发了该框架的在线对应版本，重点关注学习由未知下一个标记生成器产生的最终输出的错误界限（mistake bound）。我们区分了两种反馈形式：在端到端（End-to-End）模型中，学习者在每一轮之后仅观察到经过 $M$ 步自回归步骤后产生的最终标记；在思维链（Chain-of-Thought）模型中，学习者还会被展示完整的 $M$ 步轨迹。我们的目标是理解最优错误界限如何依赖于生成步长 $M$，以及观察中间标记在多大程度上可以减少这种依赖。我们的主要结果表明，自回归学习的在线理论呈现出与 [Hanneke et al., 2026] 发现的统计理论类似的定性图景，但在生成步长的依赖规模上有所不同。在端到端模型中，我们证明了生成步长 $M$ 中可能出现的错误界限增长率的分类体系：基本上可以出现从常数到对数之间的任何增长率。我们进一步表明，这种对数上限是不可避免的。在思维链模型中，我们表明获取完整的生成轨迹可以完全消除对 $M$ 的依赖。我们还分析了自回归线性阈值类，并证明了最优错误界限，以及统计设置下的一个新下界。在此过程中，我们的结果解决了 [Joshi et al., 2025] 留下的一些开放性问题。

## Abstract
Autoregressive generation lies at the heart of the mechanism of large language models. It can be viewed as the repeated application of a next-token generator: starting from an input string (prompt), the generator is applied for $M$ steps, and the last generated token is taken as the final output. [Joshi et al., 2025] proposed a PAC model for studying the learnability of the input-output maps arising from this process. We develop an online analogue of this framework, focusing on the mistake bound of learning the final output induced by an unknown next-token generator.   We distinguish between two forms of feedback. In the End-to-End model, after each round the learner observes only the final token produced after $M$ autoregressive steps. In the Chain-of-Thought model, the learner is additionally shown the entire $M$-step trajectory. Our goal is to understand how the optimal mistake bound depends on the generation horizon $M$, and to what extent observing intermediate tokens can reduce this dependence.   Our main results show that the online theory of autoregressive learning exhibits a qualitative picture analogous to the statistical one found by [Hanneke et al., 2026], but with a different scale of dependence on the generation horizon. In the End-to-End model, we prove a taxonomy of possible mistake-bound growth rates in the generation horizon $M$: essentially any rate between constant and logarithmic can arise. We further show that this logarithmic ceiling is unavoidable. In the Chain-of-Thought model, we show that access to the full generated trajectory eliminates the dependence on $M$ altogether.   We also analyze autoregressive linear threshold classes, and prove optimal mistake bounds, as well as a new lower bound for the statistical setting. Along the way, our results resolve several questions left open by [Joshi et al., 2025].

---

## 论文详细总结（自动生成）

这篇论文《具有自回归思维链推理的在线学习理论》（A Theory of Online Learning with Autoregressive Chain-of-Thought Reasoning）是一篇深入探讨大语言模型（LLM）自回归生成机制在在线学习框架下复杂性的理论研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现代LLM的核心是自回归生成，即通过重复调用“下一个标记生成器”来产生输出。这种机制引入了生成步长 $M$（即推理链的长度）。
*   **核心问题**：在在线学习（Online Learning）设置下，学习一个未知的自回归生成器时，最优错误界限（Mistake Bound）如何随生成步长 $M$ 变化？
*   **研究动机**：区分“端到端（e2e）反馈”（仅看最终结果）和“思维链（CoT）反馈”（看完整推理轨迹）对学习效率的影响，理解中间推理步骤在降低学习难度方面的理论价值。

### 2. 方法论与核心思想
*   **模型定义**：
    *   **基础类 $F$**：定义为下一个标记生成器 $f: \Sigma^* \to \Sigma$ 的集合。
    *   **e2e模型**：学习者预测 $M$ 步后的最终标记，反馈也仅为该标记。
    *   **CoT模型**：学习者预测最终标记，但反馈是完整的 $M$ 步生成轨迹。
*   **关键技术细节**：
    *   **Littlestone维度 ($L(F)$)**：作为衡量在线学习复杂度的核心指标。
    *   **CoT上限证明**：通过设计一种类似于标准最优算法（SOA）的变体，证明了 $L(F_{CoT-M}) \le L(F)$，即 CoT 反馈能完全消除对 $M$ 的依赖。
    *   **e2e上限证明**：利用树覆盖（Tree-covering）参数和 Sauer-Shelah-Perles 引理的变体，证明了 $L(F_{e2e-M}) = O(L(F) \log M)$。
    *   **分类体系（Taxonomy）**：构造了特定的函数类，证明了在 e2e 模式下，错误界限可以呈现从常数到对数级的任何增长速率。
    *   **锁存引理（Latch Lemma）**：针对线性分类器，设计了一种机制，使输出位能在自回归过程中“拖动”，从而证明了其错误界限为 $\Theta(d^2)$。

### 3. 实验设计与 Benchmark
*   **研究性质**：本文是一篇**纯理论论文**，不涉及传统意义上的数据集实验。
*   **对比基准（Benchmark）**：
    *   对比了 [JVB+25] 提出的 PAC 学习框架下的统计样本复杂度。
    *   对比了 [HMM26] 在统计设置下的发现。
*   **分析场景**：
    *   **一般 Littlestone 类**：分析通用的函数类增长率。
    *   **自回归线性阈值类**：分析在 $R^d$ 空间下的线性分类器。
    *   **随机自回归生成器**：探讨非确定性生成情况下的遗憾界限（Regret Bound）。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何 GPU 或算力资源的使用。所有的结论均通过数学证明（如概率方法、组合数学推导）得出。

### 5. 实验数量与充分性
*   **理论充分性**：
    *   论文通过严密的数学推导覆盖了多种情况：确定性 vs 随机性、有限维度 vs 无限维度、e2e vs CoT。
    *   **分类体系的完备性**：证明了子对数增长率的分类体系，展示了理论框架的广泛适用性。
    *   **解决开放问题**：解决了 [JVB+25] 关于 e2e 统计样本复杂度是否依赖 $M$ 的悬而未决的问题。
*   **客观性**：结论基于数学公理和引理推导，具有极高的客观性和确定性。

### 6. 主要结论与发现
*   **CoT 的优越性**：观察中间推理步骤（CoT）可以将学习复杂度从依赖 $M$ 降低到仅依赖基础类本身的复杂度，这解释了为什么监督微调（SFT）推理轨迹对模型能力提升巨大。
*   **e2e 的对数 ceiling**：在端到端学习中，错误界限随 $M$ 的增长最高仅为对数级（$\log M$），且这一上限对所有有限 Littlestone 维度的类都成立。
*   **线性分类器特性**：对于 $d$ 维线性阈值函数，无论是否有 CoT，错误界限始终为 $\Theta(d^2)$，且在统计设置下样本复杂度下界为 $\Omega(d)$。
*   **计算复杂性分离**：在某些假设下，e2e 学习在计算上是硬的（Hard），而 CoT 反馈可以使学习变得在多项式时间内可行。

### 7. 优点
*   **理论深度**：建立了自回归在线学习的第一个完整理论框架。
*   **结构清晰**：对不同反馈模式下的增长率给出了精确的数学分类。
*   **启发性**：为理解 LLM 的“思维链”如何简化学习任务提供了坚实的理论支撑。

### 8. 不足与局限
*   **模型简化**：理论模型假设生成器是确定性的（虽然最后讨论了随机情况），且未考虑 Transformer 架构的具体注意力机制。
*   **应用限制**：Littlestone 维度在实际超大规模参数模型中难以直接测量，理论结果更多用于指导原则而非直接计算具体参数。
*   **字母表限制**：部分关于 e2e 增长率的复杂构造主要针对二元字母表，在多分类（Multiclass）设置下的扩展仍有待深入。

（完）
