---
title: Iterative Critique-and-Routing Controller for Multi-Agent Systems with Heterogeneous LLMs
title_zh: 针对异构大语言模型多智能体系统的迭代式评判与路由控制器
authors: "Wenzhi Fang, Liangqi Yuan, Guangchen Lan, Dong-Jun Han, Christopher G. Brinton"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08686v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 将多智能体协作建模为序列决策问题
tldr: 针对多智能体大模型系统现有控制器仅支持单次路由、缺乏迭代优化机制的问题，本文提出一种迭代式“评价与路由”控制器。该方法将多智能体协作建模为有限时界马尔可夫决策过程（MDP），通过评估中间草稿并动态选择后续智能体进行迭代改进。实验表明，该方法在多个推理基准测试中显著优于现有基准，且在大幅减少对最强模型调用的同时，实现了接近最强模型的性能。
selection_source: fresh_fetch
motivation: 现有的多智能体控制器多为单次路由设计，缺乏对中间结果的评价和迭代优化能力。
method: 将协作过程建模为MDP，利用策略梯度和拉格朗日松弛法优化控制器，实现动态评价、终止决策及智能体选择。
result: "在七个推理基准测试中，该方法在仅使用最强智能体不到25%次数的情况下，显著超越了现有SOTA基准。"
conclusion: 迭代式的评价与路由机制能有效提升异构多智能体系统的协作效率，在性能与成本之间取得更优平衡。
---

## 摘要
多智能体大语言模型（LLM）系统通常依赖控制器来协调一组异构模型，然而现有的控制器通常局限于单次路由：它们仅选择一次模型并直接返回其输出。这种仅限路由的设计缺乏评判中间草稿或支持迭代优化的机制。为了解决这一局限性，我们提出了一种评判与路由控制器，将多智能体协作建模为一个序列决策问题。在每一轮中，控制器评估当前草稿，决定停止还是继续，并在需要时选择下一个智能体进行进一步优化。我们将此过程建模为具有显式智能体利用率约束的有限时界马尔可夫决策过程（MDP），为跨轮次的控制器决策设计了复合奖励，并在拉格朗日松弛目标下通过策略梯度优化控制器。在多个异构多智能体系统和七个推理基准测试上的广泛实验表明，我们的方法始终优于最先进的基准方法，并显著缩小了与最强智能体之间的差距，而对其调用次数不足总调用次数的 25%。

## Abstract
Multi-agent large language model (LLM) systems often rely on a controller to coordinate a pool of heterogeneous models, yet existing controllers are typically limited to one-shot routing: they select a model once and return its output directly. Such routing-only designs provide no mechanism to critique intermediate drafts or support iterative refinement. To address this limitation, we propose a critique-and-routing controller that casts multi-agent coordination as a sequential decision problem. At each turn, the controller evaluates the current draft, decides whether to stop or continue, and, if needed, selects the next agent for further refinement. We formulate this process as a finite-horizon Markov Decision Process (MDP) with explicit agent-utilization constraints, design a composite reward for controller decisions across turns, and optimize the controller via policy gradients under a Lagrangian-relaxed objective. Extensive experiments across multiple heterogeneous multi-agent systems and seven reasoning benchmarks show that our method consistently outperforms state-of-the-art baselines and substantially narrows the gap to the strongest agent, while using it for fewer than 25% of total calls.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对异构大语言模型（LLM）多智能体系统的**迭代式评判与路由控制器（Iterative Critique-and-Routing Controller）**。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多智能体协调机制大多采用“单次路由（One-shot Routing）”模式，即控制器根据查询选择一个模型后直接返回结果。这种设计无法处理复杂推理任务，因为初始草稿可能存在错误，且缺乏反馈循环来支持后续的迭代改进。
*   **研究动机**：作者观察到，复杂的推理问题往往需要多轮交互和纠错。因此，需要一个既能“路由”（选择合适模型）又能“评判”（验证中间结果并提供反馈）的控制器，同时还要在提升质量和控制成本（避免过度依赖最强/最贵模型）之间取得平衡。

### 2. 方法论
*   **核心思想**：将多智能体协调建模为一个**有限时界马尔可夫决策过程（MDP）**。控制器在每一轮中扮演双重角色：**评判者（Critic）**评估当前答案的质量，**路由者（Router）**决定是终止流程还是调用下一个更合适的智能体进行优化。
*   **关键技术细节**：
    *   **状态空间（S）**：包含原始查询、系统提示词和上一轮生成的响应草稿。
    *   **动作空间（A）**：控制器的输出文本，包含思考过程、验证结论（正确/错误）以及下一步路由决策（选择哪个智能体）。
    *   **复合奖励函数**：设计了结合“路由奖励（Rr）”和“验证奖励（Rv）”的规则奖励。
    *   **拉格朗日松弛（Lagrangian Relaxation）**：为了防止系统崩溃到只调用最强模型，引入了显式的智能体利用率约束。通过拉格朗日乘子 $\xi_k$ 在奖励函数中惩罚过度使用的模型，将受限优化问题转化为无约束的奖励最大化问题。
    *   **策略优化**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行强化学习训练，利用组内相对优势来估计多轮交互的折扣回报。

### 3. 实验设计
*   **数据集/场景**：主要针对数学推理任务。
    *   **训练集**：MATH-lighteval 的训练部分。
    *   **测试基准（7个）**：MATH-lighteval (test)、MATH500、Omni-MATH、MinervaMath、AGI-Eval-MATH、AMC23、AIME24。
*   **系统配置**：
    *   **系统1**：Qwen2.5-7B 作为控制器；智能体池包含 Qwen3-30B、Qwen2.5-7B、Qwen2.5-1.5B。
    *   **系统2**：Qwen3-4B 作为控制器；智能体池包含 Qwen3-30B、Ministral-3-8B、Llama-3.2-1B。
*   **对比方法**：
    *   **Router-R1**：最先进的基于强化学习的多轮路由基准。
    *   **RouterDC / RoBERTa Router**：基于对比学习或分类器的传统路由方法。
    *   **Controller V1/V2**：将控制器本身作为独立求解器的变体。

### 4. 资源与算力
*   **硬件环境**：实验在一台拥有 **4 张 NVIDIA H100 (80GB)** GPU 的集群上运行。
*   **软件框架**：使用了 `verl` 强化学习框架。
*   **训练细节**：训练迭代次数在 400 到 600 次之间，批量大小为 128，组大小（Group Size）为 4。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同的异构系统配置下进行了测试，覆盖了从基础数学到奥数级别的 7 个主流基准测试。
*   **消融实验**：作者进行了关于“交互轮数（T）”和“拉格朗日乘子（$\xi$）”的消融研究，验证了多轮交互的增益以及负载均衡机制的有效性。
*   **客观性与公平性**：实验设置了明确的资源约束（如最强模型调用率限制在 25% 以内），并在相同约束下对比了多种 SOTA 基准，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能卓越**：该方法在所有基准测试中均显著优于现有基准。在系统 2 中，平均准确率比 Router-R1 高出 15.8%。
*   **效率平衡**：在仅使用最强智能体不到 25% 次数的情况下，性能大幅逼近（甚至在某些任务上匹配）了全量使用最强智能体的表现。
*   **迭代价值**：实验证明，从 1 轮增加到 2 轮交互时准确率提升最显著，随后收益趋于饱和，说明少量的迭代即可纠正大部分错误。

### 7. 优点（亮点）
*   **联合决策**：首次将验证（Critique）和路由（Routing）深度耦合在同一个 MDP 框架下。
*   **显式约束优化**：通过拉格朗日松弛法解决了多智能体系统中常见的“最强模型依赖”问题，实现了真正的负载均衡。
*   **泛化能力**：实验表明该控制器在未见过的难题基准（如 AIME24）上依然表现强劲。

### 8. 不足与局限
*   **领域局限性**：目前主要集中在数学等**可验证领域（Verifiable Domains）**，因为这些领域容易定义规则奖励。在创意写作等主观领域，需要引入 LLM-as-a-Judge，这会增加复杂性和偏差。
*   **推理延迟**：多轮交互虽然提升了质量，但不可避免地增加了推理延迟和总计算开销。
*   **训练成本**：作为一种基于强化学习的方法，其训练过程比简单的分类器路由要复杂且耗时。

（完）
