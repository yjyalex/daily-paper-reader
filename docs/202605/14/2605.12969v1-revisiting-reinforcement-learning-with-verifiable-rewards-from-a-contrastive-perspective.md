---
title: Revisiting Reinforcement Learning with Verifiable Rewards from a Contrastive Perspective
title_zh: 从对比视角重新审视具有可验证奖励的强化学习
authors: "Feng Zhang, Xinhong Ma, Ziqiang Dong, Xi Leng, Jianfei Zhao, Xin Sun, Yang Yang, Guanjun Jiang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.12969v1"
tags: ["query:token"]
score: 10.0
evidence: GRPO中的Token级重要性采样与信用分配
tldr: 本研究从对比学习视角重新审视了带可验证奖励的强化学习（RLVR），指出主流算法GRPO在评分对齐和信用分配方面的局限性。为此提出了ConSPO框架，通过长度归一化的序列对数概率和InfoNCE对比目标，结合课程学习边际机制，实现了更精准的策略优化。实验表明，ConSPO在多项数学推理基准测试中显著超越了现有强基准算法。
selection_source: fresh_fetch
motivation: 揭示了GRPO算法中基于剪切率的评分与生成似然不一致，且信用分配未能充分考虑正负样本间相对分差的问题。
method: 提出ConSPO框架，采用序列级对数概率评分并引入组内对比学习目标及课程调度边际来优化策略。
result: 在不同规模的模型和数据集上，ConSPO在复杂数学推理任务中的表现持续优于包括GRPO在内的多种RLVR基准。
conclusion: 将RLVR转化为对比优化问题并解决评分对齐与信用分配缺陷，是提升大语言模型推理能力的有效途径。
---

## 摘要
RLVR（具有可验证奖励的强化学习）已成为提升大语言模型（LLM）推理能力的广泛采用范式，而 GRPO 是其最具代表性的算法之一。在本文中，我们首先证明 GRPO 可以等效地重新表述为一种加权正负得分差的判别式形式。在此视角下，GRPO 增加了经过验证的正向采样（rollouts）的序列级得分，并降低了负向采样的得分，其中得分是经过裁剪的词元级重要性采样比率的平均值。这种重新表述揭示了 GRPO 的两个结构性局限：似然失配评分，即优化的是基于裁剪比率的代理得分而非生成似然；以及得分不敏感的信用分配，即在分配采样级信用时未考虑同组内正负采样之间的相对得分差距。为了解决这些局限，我们提出了 ConSPO，一个用于 RLVR 的对比序列级策略优化框架。ConSPO 将 GRPO 基于裁剪比率的得分替换为长度归一化的序列对数概率，使优化的采样得分与自回归生成中使用的似然保持一致。随后，它优化了一个组级 InfoNCE 风格的目标函数，将每个正向采样与同组内的负向干扰项进行对比，从而使信用分配依赖于它们的相对得分。这种对比形式放大了对区分度较差的正向采样的更新，同时将抑制性更新集中在高分的负向采样上。此外，ConSPO 引入了课程调度的间隔（margin），引导优化从训练初期的粗略正负排序转向后期更强的分离。在不同基座模型、参数规模和训练数据集上的广泛评估表明，ConSPO 在具有挑战性的数学推理基准测试中始终优于多个强有力的 RLVR 基准算法。

## Abstract
RLVR has become a widely adopted paradigm for improving LLMs' reasoning capabilities, and GRPO is one of its most representative algorithms. In this paper, we first show that GRPO admits an equivalent discriminative reformulation as a weighted positive-negative score difference. Under this view, GRPO increases sequence-level scores of verified positive rollouts and decreases those of negative rollouts, where the scores are averages of clipped token-level importance sampling ratios. This reformulation reveals two structural limitations of GRPO: likelihood-misaligned scoring, where clipped ratio-based surrogate scores are optimized instead of generation likelihoods, and score-insensitive credit assignment, where rollout-level credit is assigned without accounting for relative score gaps between positive and negative rollouts in the same group. To address these limitations, we propose ConSPO, a framework for Contrastive Sequence-level Policy Optimization in RLVR. ConSPO replaces GRPO's clipped ratio-based scores with length-normalized sequence log-probabilities, aligning the optimized rollout scores with the likelihoods used in autoregressive generation. It then optimizes a group-wise InfoNCE-style objective that contrasts each positive rollout against negative distractors from the same group, enabling credit assignment to depend on their relative scores. This contrastive formulation amplifies updates for poorly separated positives while concentrating suppressive updates on high-scoring negatives. Moreover, ConSPO introduces a curriculum-scheduled margin, guiding optimization from coarse positive-negative ordering in early training toward stronger separation in later stages. Extensive evaluations across diverse backbone models, parameter scales, and training datasets show that ConSPO consistently outperforms several strong RLVR baselines on challenging mathematical reasoning benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Revisiting Reinforcement Learning with Verifiable Rewards from a Contrastive Perspective》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：具有可验证奖励的强化学习（RLVR）已成为提升大语言模型（LLM）推理能力的标准范式，其中 **GRPO** 算法因其无需价值模型（Critic-free）的简洁性和扩展性而成为主流。
*   **核心问题**：作者通过数学推导发现 GRPO 本质上是在优化一个“加权的正负样本得分差”。基于此视角，论文指出了 GRPO 的两个核心缺陷：
    1.  **似然失配评分（Likelihood-misaligned scoring）**：GRPO 优化的是基于词元级裁剪比率的代理得分，而非生成时实际使用的序列似然概率，导致训练与推理不一致。
    2.  **得分不敏感的信用分配（Score-insensitive credit assignment）**：在二元奖励（对/错）下，GRPO 对同组内所有正样本（或负样本）分配相同的权重，忽略了它们之间具体的得分差距，无法针对性地优化那些“险些出错”的正样本或“高分”的负样本。

### 2. 方法论（ConSPO）
ConSPO（对比序列级策略优化）框架通过以下技术手段解决上述问题：
*   **似然对齐评分**：放弃裁剪比率，改用**长度归一化的序列对数概率**作为评分函数 $s_\theta(o, q)$，确保优化目标与自回归生成的似然尺度一致。
*   **组级对比目标（InfoNCE）**：引入 InfoNCE 风格的损失函数。对于每个正样本，将其与同组内的所有负样本进行对比。
    *   **技术细节**：通过 Softmax 归一化，使得梯度大小取决于正负样本间的相对分差。当正样本得分与负样本接近时，更新信号被放大；当两者已充分分离时，信号平滑衰减。
*   **课程调度边际（Curriculum-scheduled Margin）**：在对比目标中引入边际参数 $m_t$。
    *   **流程**：$m_t$ 随训练进程从 0 逐渐增加到目标值 $M$。初期允许模型进行粗略的正负排序，后期则强制要求正负样本之间有更大的分差，防止梯度过早消失。

### 3. 实验设计
*   **基座模型**：涵盖了不同规模和类型的模型，包括 DeepSeek-R1-Distill (Qwen-1.5B/7B, Llama-8B)、Qwen3-4B-Base 和 Llama-3.2-3B-Instruct。
*   **训练数据**：主要使用 DeepScaleR-Preview-Dataset（约 4 万道数学题），并使用 DAPO-Math-17k 验证泛化性。
*   **基准测试（Benchmarks）**：在 AIME (2024-2026)、HMMT 2025、MATH500、AMC 和 OlympiadBench 七个极具挑战性的数学推理榜单上进行评估。
*   **对比方法**：包括 GRPO、DAPO、Dr. GRPO、DisCO、GMPO、CISPO 和 SAPO 等 7 种最先进的 RLVR 变体。

### 4. 资源与算力
*   **硬件配置**：
    *   **1.5B、3B 和 4B 模型**：使用单机 8 张 NVIDIA H100 GPU。
    *   **7B 和 8B 模型**：使用 4 个节点，共计 32 张 NVIDIA H100 GPU。
*   **训练参数**：学习率 $2 \times 10^{-6}$，训练 4 个 epoch（Qwen3-4B 为 2 个），最大响应长度 4K 或 8K。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验（不同规模模型）、泛化性实验（不同基座与数据集）、消融实验（验证对比目标、评分函数、边际机制的有效性）以及参数敏感性分析（温度、边际值、预热比例）。
*   **充分性与公平性**：实验覆盖了从 1.5B 到 8B 的多个参数量级，且对比了近年来几乎所有主流的 GRPO 改进版本。为了降低小样本测试集的方差，作者在 AIME 等数据集上采用了 `avg@32` 的评估指标，确保了结果的客观性和统计显著性。

### 6. 主要结论与发现
*   **性能领先**：ConSPO 在所有测试模型和基准上均优于 GRPO 及其变体。例如，在 DeepSeek-R1-Distill-Qwen-1.5B 上，平均分比 GRPO 高出 4.4 分。
*   **对比学习的优势**：实验证明，基于组内相对得分的信用分配比基于组统计数据的分配更有效，尤其是在处理困难推理任务时。
*   **课程学习的必要性**：动态增加的边际（Margin）能显著提升模型在训练后期的分离能力，避免优化停滞。

### 7. 优点
*   **理论深刻**：通过对 GRPO 的判别式重构，从数学上清晰地揭示了现有算法的结构性缺陷。
*   **训练与推理对齐**：通过引入似然对齐评分，解决了强化学习中常见的训练目标与推理行为脱节的问题。
*   **即插即用**：ConSPO 保持了 GRPO 无需 Critic 模型的简洁性，易于集成到现有的 RLVR 工作流中。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅覆盖到 8B 参数模型，尚未在 32B 或 70B 以上的超大规模模型上验证其扩展性。
*   **领域局限**：主要集中在数学推理任务，虽然这是 RLVR 的核心场景，但尚未验证其在代码生成、通用对话或多模态推理中的表现。
*   **超参数敏感性**：对比温度 $\tau$ 和边际 $M$ 的选择对性能有一定影响，可能需要针对不同任务进行微调。

（完）
