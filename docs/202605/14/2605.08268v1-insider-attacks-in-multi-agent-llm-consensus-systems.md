---
title: Insider Attacks in Multi-Agent LLM Consensus Systems
title_zh: 多智能体大语言模型共识系统中的内部攻击
authors: "Xiaolin Sun, Zixuan Liu, Yibin Hu, Zizhan Zheng"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08268v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 研究多智能体 LLM 系统中的共识形成和对抗性攻击
tldr: 本研究探讨了大语言模型多智能体共识系统中的内部攻击风险。针对现有系统假设成员完全对齐的局限性，作者提出了一种恶意内部人员操纵框架。该方法将攻击建模为序列决策任务，利用世界模型学习良性智能体的潜在行为动态，并通过强化学习训练攻击策略。实验证明，该方法能显著降低共识达成率并延长分歧，揭示了多智能体协作系统在面对自适应内部攻击时的脆弱性。
selection_source: fresh_fetch
motivation: 现有大模型多智能体系统通常假设所有成员目标一致，忽略了恶意内部人员可能通过操纵共识过程来破坏系统目标的风险。
method: 提出一种基于世界模型的强化学习框架，通过学习良性智能体潜在行为状态的代理动力学来优化攻击策略。
result: 实验表明，该训练出的攻击者比直接使用恶意提示的基准方法能更有效地降低共识达成率并延长分歧时间。
conclusion: 结合潜在世界模型与强化学习是研究语言多智能体系统中自适应内部攻击的一种极具前景且有效的方向。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在多智能体系统中，智能体通过自然语言进行通信以共同解决任务。此类系统中的一项关键能力是共识形成，即智能体通过迭代地交换信息并更新决策，以达成共同的结果。然而，大多数现有的多智能体 LLM 框架都假设所有参与的智能体都与系统目标保持一致。在实践中，恶意内部人员可能会作为群体的合法成员参与其中，同时追求隐藏的对抗性目标。在这项工作中，我们研究了多智能体 LLM 共识系统中的内部操纵。我们将该问题形式化为一个序列决策任务，其中恶意智能体试图延迟或阻止良性智能体之间达成一致。为了使攻击优化变得可行，我们提出了一个基于世界模型的框架，该框架学习良性智能体潜在行为状态上的代理动力学，然后基于该学习模型使用强化学习训练攻击者。初步结果表明，与直接使用恶意提示词的基准方法相比，经过训练的攻击者能更有效地降低良性共识率并延长分歧时间。这些结果表明，将潜在世界模型与强化学习相结合，是基于语言的多智能体系统中自适应内部攻击的一个有前景的方向。

## Abstract
Large language models (LLMs) are increasingly deployed in multi-agent systems where agents communicate in natural language to solve tasks jointly. A key capability in such systems is consensus formation, where agents iteratively exchange messages and update decisions to reach a shared outcome. However, most existing multi-agent LLM frameworks assume that all participating agents are aligned with the system objective. In practice, a malicious insider may participate as a legitimate member of the group while pursuing a hidden adversarial goal. In this work, we study insider manipulation in multi-agent LLM consensus systems. We formalize the problem as a sequential decision-making task in which a malicious agent seeks to delay or prevent agreement among benign agents. To make attack optimization tractable, we propose a world-model-based framework that learns surrogate dynamics over the latent behavioral states of benign agents and then trains an attacker using reinforcement learning based on this learned model. Preliminary results show that the trained attacker reduces the benign consensus rate and prolongs disagreement more effectively than the direct malicious-prompt baseline. These results suggest that combining latent world models with reinforcement learning is a promising direction for adaptive insider attacks in language-based multi-agent systems.

---

## 论文详细总结（自动生成）

### 论文总结：多智能体大语言模型共识系统中的内部攻击

#### 1. 核心问题与研究动机
随着大语言模型（LLM）在多智能体系统（MAS）中的广泛应用，智能体通过自然语言协作达成“共识”成为核心机制。然而，现有研究大多假设所有智能体都是诚实且目标一致的。
*   **研究动机**：在现实场景中，系统中可能存在“内部攻击者”（Insider Attacker）。他们伪装成合法成员，利用自然语言（如说服、误导、战略性分歧）干扰共识过程，而无需破坏基础设施。
*   **核心问题**：恶意 LLM 智能体能否通过战略性操纵，在保持表面合规的同时，有效地延迟或阻止良性智能体达成共识？

#### 2. 方法论
论文将内部操纵建模为一个**序列决策问题**，并提出了一个基于世界模型的强化学习框架：
*   **潜在智能体世界模型 (Latent-Agent World Model)**：
    *   由于直接在 LLM 交互中训练 RL 采样效率极低且成本高昂，作者构建了一个代理模型（Surrogate Model）。
    *   该模型学习良性智能体的行为动力学，预测它们在接收到攻击者信息后的决策更新。
    *   **异质性建模**：考虑到不同智能体有不同性格（如固执、易受影响、中立），世界模型采用多头神经网络结构，根据智能体的“行为属性”预测其下一步动作。
*   **RL 攻击者优化**：
    *   使用 **DQN (Deep Q-Network)** 算法在模拟的世界模型环境中训练攻击策略。
    *   **奖励函数**：旨在惩罚共识达成，奖励持续的分歧状态。
*   **行为属性推理**：
    *   攻击者可能无法预知良性智能体的性格。作者设计了一个分类器（结合 BERT 提取文本特征和 GRU 提取轨迹特征），通过第一轮交互的历史记录推断对方的性格类型。

#### 3. 实验设计
*   **实验场景**：一维离散共识环境（0-20 的数轴）。智能体目标是移动到同一位置。
*   **智能体配置**：4 个智能体（3 个良性，1 个攻击者），底层模型为 **GPT-4o**。
*   **良性智能体性格**：
    *   **Stubborn (固执型)**：倾向于留在原地。
    *   **Suggestible (易受影响型)**：倾向于跟随他人。
    *   **Neutral (中立型)**：平衡自身与他人位置。
*   **对比基准 (Benchmarks)**：
    1.  **No Attacker**：无攻击者（基准协作表现）。
    2.  **LLM-Based Attacker**：仅通过恶意提示词（Prompt）驱动的攻击者。
    3.  **RL-Based Attacker**：已知对方性格并经过 RL 训练的攻击者。
    4.  **Guessed RL Attacker**：通过推理得出对方性格并执行 RL 策略的攻击者。
*   **评估指标**：共识率 (CR) 和平均回合数 (AER)。

#### 4. 资源与算力
*   **数据采集**：收集了 1000 条模拟交互轨迹用于训练世界模型。
*   **RL 训练**：在 8 个并行环境中进行了 5,000,000 (500万) 步的训练。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和具体训练时长，但提到了使用 Stable-Baselines3 框架和 BERT-base-uncased 模型。

#### 5. 实验数量与充分性
*   **实验规模**：针对 10 种不同的良性智能体性格组合进行了测试，每种组合运行 50 次采样，总计超过 500 次完整的 LLM 多轮交互实验。
*   **充分性评价**：
    *   **客观性**：通过引入“无攻击者”基准和“纯 Prompt 攻击者”对比，较好地证明了 RL 策略的增益。
    *   **局限性**：实验目前仅限于一维空间和固定性格模板，对于更复杂的开放式对话场景覆盖不足。

#### 6. 主要结论与发现
*   **攻击有效性**：RL 驱动的攻击者在降低共识率和延长分歧时间方面显著优于直接使用恶意 Prompt 的攻击者。
*   **世界模型作用**：学习到的世界模型能以较低的平均绝对误差 (MAE) 预测良性智能体的移动趋势，证明了在潜在空间建模 LLM 行为的可行性。
*   **性格推理**：属性分类器仅需一轮交互即可达到 100% 的性格识别准确率，使得“猜测型攻击者”的表现几乎等同于“已知信息攻击者”。
*   **系统脆弱性**：即使是看似简单的共识任务，在面对有策略的内部人员时也表现出极大的脆弱性。

#### 7. 优点
*   **视角新颖**：首次将 LLM 多智能体系统中的内部攻击形式化为 RL 优化问题。
*   **效率优化**：通过引入“世界模型”解决了 LLM 交互采样慢、成本高的问题，为未来研究提供了范式。
*   **异质性考虑**：考虑了智能体性格的多样性，使攻击模型更具自适应性。

#### 8. 不足与局限
*   **环境简化**：实验环境（一维数轴）过于理想化，与现实中复杂的语义共识（如法律谈判、代码协作）有差距。
*   **通信受限**：攻击者的动作空间被限制在预定义的模板中，未能充分发挥 LLM 生成自由文本进行深度欺骗的潜力。
*   **防御缺失**：论文重点在于“如何攻击”，尚未提出有效的检测或防御机制来应对此类内部威胁。

（完）
