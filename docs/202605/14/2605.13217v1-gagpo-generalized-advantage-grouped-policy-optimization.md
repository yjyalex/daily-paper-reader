---
title: "GAGPO: Generalized Advantage Grouped Policy Optimization"
title_zh: GAGPO：广义优势分组策略优化
authors: "Siyuan Zhu, Chao Yu, Rongxin Yang, Zongkai Liu, Jinjun Hu, Qiwen Chen, Yibo Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13217v1"
tags: ["query:token"]
score: 9.0
evidence: 大语言模型智能体强化学习中的步对齐时间信用分配
tldr: 针对大语言模型智能体在多轮交互中面临的稀疏奖励和信用分配难题，本文提出了一种无需评论家网络（critic-free）的强化学习方法 GAGPO。该方法通过构建非参数化的分组价值代理，实现了类似 TD/GAE 的时序优势计算，将轨迹级奖励有效回传至各个动作步骤。实验证明，GAGPO 在 ALFWorld 和 WebShop 等任务上优于强基线，具有更高的学习效率和更平滑的优化动态。
selection_source: fresh_fetch
motivation: 解决多轮交互环境下，大模型智能体因仅有轨迹级稀疏奖励而难以进行精确动作级信用分配的问题。
method: 提出 GAGPO 方法，利用采样轨迹构建非参数化分组价值代理，并结合优势归一化和动作级重要性采样实现时序信用分配。
result: 在 ALFWorld 和 WebShop 任务中，GAGPO 的性能超越了现有的强强化学习基线，且展现出更快的收敛速度。
conclusion: GAGPO 为多轮智能体强化学习提供了一个简单且有效的框架，证明了无需复杂价值模型也能实现精确的时序信用分配。
---

## 摘要
强化学习已成为大语言模型智能体后训练的一种强大范式，但在多轮环境中的信用分配（credit assignment）仍是一项挑战。智能体通常仅在回合结束时获得稀疏的轨迹级奖励，这使得确定哪些中间动作对成功或失败有所贡献变得十分困难。因此，在不依赖昂贵的辅助价值模型的情况下，将延迟的结果回传到各个决策步骤仍然是一个开放性问题。我们提出了广义优势分组策略优化（GAGPO），这是一种无需评论家（critic-free）的强化学习方法，用于精确的、步对齐的时间信用分配。GAGPO 从采样的展开（rollouts）中构建非参数化的分组价值代理，并利用其计算 TD/GAE 风格的时间优势，将结果监督信号沿时间递归地向后传播。结合分组优势归一化和动作级重要性采样比率，GAGPO 直接从多轮轨迹中提取稳定的局部优化信号。在 ALFWorld 和 WebShop 上的实验表明，GAGPO 优于强强化学习基线。进一步的分析表明，该方法具有更快的早期学习速度、更高的交互效率以及更平滑的优化动态，这表明 GAGPO 为多轮智能体强化学习提供了一个简单而有效的框架。

## Abstract
Reinforcement learning has become a powerful paradigm for post-training large language model agents, yet credit assignment in multi-turn environments remains a challenge. Agents often receive sparse, trajectory-level rewards only at the end of an episode, making it difficult to determine which intermediate actions contributed to success or failure. As a result, propagating delayed outcomes back to individual decision steps without relying on costly auxiliary value models remains an open problem. We propose Generalized Advantage Grouped Policy Optimization (GAGPO), a critic-free reinforcement learning method for precise, step-aligned temporal credit assignment. GAGPO constructs a non-parametric grouped value proxy from sampled rollouts and uses it to compute TD/GAE-style temporal advantages, recursively propagating outcome supervision backward through time. Combined with group-wise advantage normalization and an action-level importance ratio, GAGPO extracts stable, localized optimization signals directly from multi-turn trajectories. Experiments on ALFWorld and WebShop show that GAGPO outperforms strong reinforcement learning baselines. Further analyses demonstrate faster early-stage learning, improved interaction efficiency, and smoother optimization dynamics, suggesting that GAGPO offers a simple yet effective framework for multi-turn agentic reinforcement learning.

---

## 论文详细总结（自动生成）

以下是对论文《GAGPO: Generalized Advantage Grouped Policy Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在多轮交互任务中面临**信用分配（Credit Assignment）**难题。
*   **背景与动机**：
    *   **奖励稀疏且延迟**：智能体通常只在任务结束时获得一个轨迹级的成功/失败奖励，难以确定中间哪一步动作是关键。
    *   **现有方法局限**：传统的 PPO 需要额外的评论家（Critic）模型，增加了训练复杂度和显存开销；而现有的无需评论家的方法（如 GRPO、RLOO）多采用蒙特卡洛（MC）估计，导致方差大且缺乏时序上的信用传播。
    *   **目标**：开发一种无需辅助价值模型、能实现精确步对齐（Step-aligned）且具备时序传播能力的强化学习方法。

### 2. 方法论
GAGPO 的核心思想是**在无需学习 Critic 网络的情况下，利用采样分组构建非参数化的价值代理，实现时序优势估计**。
*   **核心步骤**：
    1.  **状态分组（Rollout Grouping）**：将不同采样轨迹中出现的相同环境状态 $s$ 归为一组 $G(s)$。
    2.  **非参数价值代理（Grouped Value Proxy）**：对于状态 $s$，其价值 $\bar{V}(s)$ 定义为该组内所有步骤后续折扣回报的平均值。这相当于用经验均值代替了参数化的 Critic。
    3.  **时序优势计算（TD/GAE-style）**：
        *   计算 TD 残差：$\delta_t = r_t + \gamma \bar{V}(s_{t+1}) - \bar{V}(s_t)$。
        *   利用 GAE 公式进行递归传播：$\hat{A}_t = \delta_t + \gamma \lambda \hat{A}_{t+1}$。这种方式能将终点奖励有效地回传到初始步骤。
    4.  **分组归一化（Group Normalization）**：在同一个采样组内对优势值进行标准化，以消除不同任务间的量级差异。
    5.  **动作级重要性采样（Action-level Ratio）**：将整个动作序列（多个 token）视为一个整体，计算长度归一化的重要性比率，避免 token 级别的梯度撕裂。

### 3. 实验设计
*   **实验场景**：
    *   **ALFWorld**：具身智能体任务，涉及在家庭环境中寻找、移动和操作物体。
    *   **WebShop**：在线购物任务，涉及多轮搜索、比较和选择商品。
*   **基准模型（Backbones）**：Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct。
*   **对比方法**：
    *   **提示词工程**：直接 Prompting、ReAct、Reflexion。
    *   **闭源模型**：GPT-4o、Gemini-2.5-Pro。
    *   **强化学习基线**：PPO（带 Critic）、RLOO、GRPO、GiGPO（之前的 SOTA 分组优化方法）。

### 4. 资源与算力
*   **论文说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：实验基于 `verl-agent` 框架，训练进行了 160 个步骤，设置了 2048 到 4096 的 Prompt 长度限制。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流智能体 Benchmark 上进行了测试，涵盖了 1.5B 和 7B 两种模型尺度。
*   **消融实验**：非常充分。作者分别验证了时序递归（$\lambda$ 参数）、动作级比率、分组归一化等组件的必要性。
*   **统计分析**：提供了 3 个随机种子的均值和标准差，并深入分析了梯度范数、信息熵下降速度以及优势值的分布情况（IQR 分析）。
*   **客观性**：通过与多种强基线（包括带 Critic 的 PPO）在相同 pipeline 下对比，证明了其优越性。

### 6. 主要结论与发现
*   **性能领先**：GAGPO 在所有指标上均优于现有的 RL 基线。在 ALFWorld 上，相比最强基线提升了约 5-7 个百分点；在 WebShop 上提升更显著。
*   **学习效率高**：GAGPO 在训练早期（前 120 步）的收敛速度明显快于 GRPO 和 GiGPO。
*   **交互更简洁**：训练后的智能体完成任务所需的平均步数更短，说明决策更高效。
*   **优化更稳定**：相比 GiGPO，GAGPO 的梯度波动更小，优势值的方差更低，避免了极端信用信号对策略的冲击。

### 7. 优点
*   **架构简洁**：无需训练和维护额外的 Critic 模型，显著降低了显存占用和工程复杂度。
*   **信用分配精准**：通过 TD/GAE 机制解决了分组优化中“奖励广播”导致的信号模糊问题。
*   **步对齐优化**：将动作序列作为整体处理，更符合智能体决策的本质，提高了训练稳定性。

### 8. 不足与局限
*   **依赖精确状态匹配**：目前的价值代理构建依赖于环境状态的文本完全匹配。在观察值具有随机性、连续性或部分可观测（POMDP）的环境中，这种匹配机制会失效。
*   **环境覆盖有限**：实验主要集中在文本交互环境，尚未在多模态或更复杂的长程任务（如软件操作）中验证。
*   **安全风险**：更强大的多轮智能体训练可能降低自主代理部署的门槛，若缺乏监管，可能执行不可靠或有害的动作序列。

（完）
