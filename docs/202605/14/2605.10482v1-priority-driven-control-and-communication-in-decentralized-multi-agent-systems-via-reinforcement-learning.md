---
title: Priority-Driven Control and Communication in Decentralized Multi-Agent Systems via Reinforcement Learning
title_zh: 分布式多智能体系统中基于强化学习的优先级驱动控制与通信
authors: "Qingyun Guo, Junyi Shi, Tomasz Piotr Kucner, Dominik Baumann"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10482v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统中通信的强化学习
tldr: 针对去中心化多智能体系统中通信带宽受限的问题，本文提出了一种模型无关的优先级驱动强化学习算法。该方法通过联合学习通信优先级和控制策略，有效解决了传统事件触发控制中依赖精确模型及混合动作空间难以优化的问题。实验证明，该算法在基准任务上优于现有基准方法，实现了高效的带宽利用与系统控制。
selection_source: fresh_fetch
motivation: 传统的事件触发控制通常依赖精确的系统模型，且在处理二值化通信决策时面临复杂的混合动作空间挑战。
method: 提出一种优先级驱动的强化学习算法，通过联合学习通信优先级和控制策略，将通信决策转化为优先级排序问题。
result: 在基准测试任务中的评估结果显示，该算法在性能表现上显著优于现有的基准方法。
conclusion: 该研究通过引入优先级机制，为去中心化多智能体系统在有限带宽下的高效通信与控制提供了一种有效的模型无关解决方案。
---

## 摘要
事件触发控制提供了一种机制，用于避免在网络化多智能体系统中过度使用受限的通信带宽。然而，大多数现有方法依赖于准确的系统模型，而这在实践中可能无法获得。在这项工作中，我们提出了一种无模型的优先级驱动强化学习算法，该算法在分布式多智能体系统中从数据中联合学习通信优先级和控制策略。通过学习通信优先级，我们规避了在具有二值通信决策的事件触发控制中典型的混合动作空间问题。我们在基准任务上评估了我们的算法，并证明其性能优于基准方法。

## Abstract
Event-triggered control provides a mechanism for avoiding excessive use of constrained communication bandwidth in networked multi-agent systems. However, most existing methods rely on accurate system models, which may be unavailable in practice. In this work, we propose a model-free, priority-driven reinforcement learning algorithm that learns communication priorities and control policies jointly from data in decentralized multi-agent systems. By learning communication priorities, we circumvent the hybrid action space typical in event-triggered control with binary communication decisions. We evaluate our algorithm on benchmark tasks and demonstrate that it outperforms the baseline method.

---

## 论文详细总结（自动生成）

以下是对论文《Priority-Driven Control and Communication in Decentralized Multi-Agent Systems via Reinforcement Learning》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在网络化多智能体系统（NCS）中，通信带宽通常是受限的共享资源。传统的**事件触发控制（ETC）**通过仅在必要时传输数据来节省带宽，但大多依赖精确的数学模型，难以处理高维非线性系统。
**研究动机：**
*   **模型依赖性：** 现有方法难以应对无精确模型的情况。
*   **混合动作空间挑战：** 使用强化学习（RL）实现ETC时，智能体需同时做出离散的通信决策（是否发送）和连续的控制决策，导致优化困难。
*   **中心化训练限制：** 许多多智能体强化学习（MARL）算法假设训练期间可无限制交换全局信息，这在实际带宽受限场景中不成立。
*   **网络过载风险：** 简单的二值化决策可能导致大量智能体同时通信，造成网络拥塞。

### 2. 论文提出的方法论
论文提出了一种**优先级驱动的去中心化强化学习框架（Control-Priority IPPO）**，核心思想是将通信决策转化为连续的“优先级”学习问题。
*   **核心思想：** 每个智能体不仅学习控制动作 $a_{i,c}$，还学习一个连续的通信优先级 $a_{i,p} \in (0, 1)$。系统根据优先级高低分配有限的 $L$ 个通信槽位。
*   **关键技术细节：**
    *   **动作空间简化：** 将原本“离散通信+连续控制”的混合空间统一为纯连续动作空间，降低了学习难度。
    *   **带宽分配机制：** 仅优先级最高的 $L$ 个智能体获准广播其观测信息。
    *   **奖励函数设计：** 在控制奖励 $r_{i,c}$ 的基础上引入通信惩罚 $- \xi a_{i,p}$，通过惩罚项抑制智能体盲目追求高优先级的倾向。
    *   **算法流程：** 基于独立近端策略优化（IPPO）。智能体仅依赖局部观测和接收到的 $L$ 个智能体的信息进行决策。训练过程不依赖全局状态，符合去中心化设定。

### 3. 实验设计
*   **实验场景：**
    1.  **覆盖控制（Coverage Control）：** 智能体需移动以覆盖随机分布的坐标点。
    2.  **编队控制（Formation Control）：** 智能体需围绕目标点形成正多边形。
    3.  **Multiwalker（高维任务）：** 多个双足机器人共同搬运一个包裹，需高度协同。
*   **Benchmark（基准方法）：** 采用**轮询通信（Round-Robin）的 IPPO**。在轮询模式下，智能体按固定顺序通信，不学习优先级。
*   **对比设定：** 为了公平性，论文给轮询基准分配了更多或相等的通信槽位（$L_1 \ge L_2$），以证明优先级机制在更少资源下能达到更好效果。
*   **网络环境：** 模拟了真实网络波动，包括 0.2 的丢包率和 1 个时间步的传输延迟。

### 4. 资源与算力
*   **算力说明：** 文中提到使用了 Aalto Science-IT 项目提供的计算资源。
*   **训练细节：** 所有环境均训练了 **1000 万步（10M steps）**。
*   **硬件型号：** 论文**未明确说明**具体的 GPU 型号或数量，也未提及具体的训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了低维（覆盖/编队）到高维（Multiwalker）任务，智能体数量从 3 个扩展到 8 个。
*   **充分性：** 实验包含了不同智能体数量（N=3, 6, 8）和不同带宽限制（L=1, 2）的组合。
*   **客观性：** 实验采用了 3 个随机种子并标注了标准差（阴影区域），且在对比中故意限制了所提方法的带宽资源，增强了结论的说服力。
*   **消融/分析：** 论文还展示了优先级随任务阶段变化的动态图，证明了优先级学习的有效性。

### 6. 论文的主要结论与发现
*   **性能优越：** 在所有测试场景中，优先级驱动的方法最终达到的平均奖励均显著高于轮询基准方法。
*   **带宽效率：** 相比于现有的 MAPPOC 等方法，该算法在更少的通信槽位下实现了更高的控制精度（如在 Multiwalker 中节省了约 66% 的带宽）。
*   **语义化优先级：** 实验观察到优先级与任务角色高度相关。例如在搬运任务中，处于起步、交接或收尾关键阶段的智能体会自动获得更高优先级。
*   **鲁棒性：** 算法在存在丢包和延迟的非理想网络环境下依然表现稳健。

### 7. 优点
*   **即插即用（Plug-and-play）：** 该框架可以轻松集成到任何去中心化 MARL 算法中，只需微调动作输出层。
*   **解决混合空间难题：** 通过连续优先级巧妙规避了离散通信决策带来的梯度优化难题。
*   **去中心化友好：** 训练和执行均不依赖全局信息，更符合实际分布式系统的部署需求。

### 8. 不足与局限
*   **优先级交换开销：** 虽然优先级数据量极小（3 bit），但算法仍假设所有智能体能先交换一次优先级信息以确定谁来通信，这在极端极低功耗场景下仍有开销。
*   **收敛速度：** 实验图表显示，由于需要同时学习通信策略，该方法在训练初期的收敛速度通常慢于固定顺序的轮询方法。
*   **可扩展性上限：** 尽管比中心化方法好，但其性能仍受限于底层 MARL 算法（如 IPPO）在超大规模智能体集群中的表现。
*   **超参数敏感性：** 通信惩罚系数 $\xi$ 的设置可能对最终性能有较大影响，文中未深入讨论该参数的敏感性。

（完）
