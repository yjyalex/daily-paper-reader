---
title: "$ξ$-DPO: Direct Preference Optimization via Ratio Reward Margin"
title_zh: ξ-DPO：通过比例奖励边际实现的直接偏好优化
authors: "Zhengyuan Fan, Zhonghua Wu, Yuxuan Du, Qun Chen"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.10981v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过比例奖励边际进行直接偏好优化
tldr: 针对SimPO中超参数β和γ难以调节且缺乏可解释性的问题，本文提出ξ-DPO。该方法通过等效变换将优化目标转为最小化奖励间隙与最优边际的距离，并引入比率奖励形式以消除β的影响。ξ-DPO定义了可解释的比率奖励边际ξ，可根据初始奖励分布直接确定，无需反复试错，显著提升了偏好优化的效率和稳定性。
selection_source: fresh_fetch
motivation: SimPO的超参数调节极具挑战，且其边际公式在不同数据集上的可解释性较差。
method: 提出ξ-DPO，通过比率奖励形式重新定义目标函数，并引入可直接确定的比率奖励边际ξ。
result: ξ-DPO成功消除了β的影响，并提供了一个有界且可解释的边际参数，简化了调参过程。
conclusion: ξ-DPO为偏好优化提供了一种更具解释性且易于部署的方案，有效解决了现有方法的调参难题。
---

## 摘要
无参考偏好优化已成为人类反馈强化学习（RLHF）的一种高效替代方案，其中简单偏好优化（SimPO）通过简单的目标函数消除了显式参考模型，展现出强大的性能。然而，SimPO 中超参数 β 和 γ 的联合调优仍然是一个核心挑战。我们认为，这一困难源于 SimPO 中的边际公式在具有不同奖励差距结构的跨数据集上不易解释。为了更好地理解这一问题，我们对 SimPO 进行了全面分析，发现 β 隐式地控制着样本过滤，而 γ 的效果则取决于数据集的奖励差距结构。受这些观察结果的启发，我们提出了 ξ-DPO：通过比例奖励边际实现的直接偏好优化。我们首先通过等价变换重新构建了偏好目标，将优化目标从最大化奖励差距的似然性转变为最小化奖励差距与最优边际之间的距离。然后，我们以被选回答与被拒回答之间的比例形式重新定义了奖励，这有效地抵消了 β 的影响，并产生了一个有界且可解释的边际。该边际被称为比例奖励边际，记作 ξ。与 SimPO 中的边际 γ 不同，ξ 显式地表示了被选回答与被拒回答之间期望的相对分离度，并且可以根据初始奖励差距分布来确定，从而避免了反复的试错调优。

## Abstract
Reference-free preference optimization has emerged as an efficient alternative to reinforcement learning from human feedback, with Simple Preference Optimization(SimPO) demonstrating strong performance by eliminating the explicit reference model through a simple objective. However, the joint tuning of the hyperparameters $β$ and $γ$ in SimPO remains a central challenge. We argue that this difficulty arises because the margin formulation in SimPO is not easily interpretable across datasets with different reward gap structures. To better understand this issue, we conduct a comprehensive analysis of SimPO and find that $β$ implicitly controls sample filtering, while the effect of $γ$ depends on the reward gap structure of the dataset. Motivated by these observations, we propose $ξ$-DPO: Direct preference optimization via ratio reward margin. We first reformulate the preference objective through an equivalent transformation, changing the optimization target from maximizing the likelihood of reward gaps to minimizing the distance between reward gaps and optimal margins. Then, we redefine the reward in a ratio form between the chosen and rejected, which effectively cancels the effect of $β$ and yields a bounded and interpretable margin. This margin is called the ratio reward margin and is denoted by $ξ$. Unlike the margin $γ$ in SimPO, $ξ$ explicitly represents the desired relative separation between chosen and rejected responses and can be determined from the initial reward gap distribution, avoiding repeated trial-and-error tuning. ....

---

## 论文详细总结（自动生成）

这是一份关于论文《ξ-DPO: Direct Preference Optimization via Ratio Reward Margin》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的无参考偏好优化方法（如 SimPO）虽然高效，但其超参数 $\beta$（缩放因子）和 $\gamma$（奖励边际）的联合调优极其困难，且在不同数据集上的可解释性较差。
*   **研究动机**：
    *   作者发现 $\beta$ 在 SimPO 中不仅是缩放因子，还隐式地起到了“样本过滤”的作用（通过 Sigmoid 函数的梯度饱和区过滤极端样本）。
    *   $\gamma$ 的设定高度依赖于数据集内在的奖励差距结构，缺乏统一的物理含义。
    *   现有的动态调参方法（如 AlphaDPO）往往需要重新引入参考模型，增加了计算开销。

### 2. 方法论
$\xi$-DPO 旨在通过重新定义目标函数来消除对 $\beta$ 的依赖，并提供一个可解释的边际参数 $\xi$。
*   **等价变换**：将优化目标从“最大化奖励差距的似然性”转变为“最小化奖励差距与目标边际之间的均方误差（MSE）”。
*   **比率奖励定义**：将奖励定义为被选回答（Chosen）与被拒回答（Rejected）的对数概率之比。通过这种归一化处理，超参数 $\beta$ 在公式中被抵消，奖励被限制在 $[0, 1]$ 区间内。
*   **比率奖励边际 $\xi$**：引入 $\xi$ 作为期望的相对分离度。与 SimPO 不同，$\xi$ 可以通过初始模型在数据集上的奖励差距分布的**分位数（Quantile）**直接确定（例如取 95% 或 99.9% 分位数），无需反复试错。
*   **LeakyReLU 激活函数**：为了防止“奖励退化”，使用 LeakyReLU 处理。当样本的奖励差距已经超过 $\xi$ 时，梯度趋近于零，避免将已经表现良好的样本强行“拉回”目标边际，从而保护模型的泛化能力。

### 3. 实验设计
*   **数据集**：使用了四个主流偏好数据集，包括 `llama3-ultrafeedback`、`mistral-instruct-ultrafeedback` 等，涵盖了高奖励差距（弱奖励模型构建）和低奖励差距（强奖励模型构建）两种场景。
*   **模型**：Llama3-8B-Instruct、Mistral-7B-Instruct、Gemma2-9B-Instruct。
*   **Benchmark**：AlpacaEval 2（胜率与长度控制胜率）、MT-Bench（综合评分）以及可验证的 Text-to-SQL 任务（Spider 数据集）。
*   **对比方法**：DPO, IPO, CPO, KTO, ORPO, R-DPO, SimPO, AlphaDPO 等。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA RTX PRO 6000 GPU**。
*   **训练细节**：Batch size 为 128，针对不同模型设置了不同的学习率（$6.0 \times 10^{-7}$ 到 $1.0 \times 10^{-6}$），采用余弦学习率调度和 10% 的预热期。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种架构的模型和 4 个数据集上进行了全面测试，并额外在 Text-to-SQL 任务上验证了迁移能力。
*   **消融与分析**：
    *   进行了 **$\xi$ 敏感性分析**，证明了只要 $\xi$ 设定在合理的分位数区间内，模型性能非常稳健。
    *   进行了 **LeakyReLU 消融实验**，证明了防止奖励退化的必要性。
    *   对比了无边际约束的方法（SimPER）。
*   **评价**：实验设计较为充分，涵盖了不同质量的数据源，对比基准涵盖了近年来几乎所有主流的偏好优化算法，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：$\xi$-DPO 在 AlpacaEval 2 上显著优于 SimPO 和 AlphaDPO，例如在 Mistral-Instruct 上比 AlphaDPO 提升了 12% 的相对胜率。
*   **调参简化**：成功将复杂的 $\beta, \gamma$ 联合调优简化为基于数据分布特征的单参数 $\xi$ 设定。
*   **奖励动态**：可视化显示 $\xi$-DPO 能让被选回答的奖励持续上升，而被拒回答的奖励持续下降，分离效果优于传统方法。

### 7. 优点
*   **高效率**：无需参考模型（Reference-free），节省显存和计算资源。
*   **强解释性**：$\xi$ 具有明确的物理含义（即期望的奖励比例差距），且能根据数据分布自动适配。
*   **鲁棒性**：通过 LeakyReLU 解决了过拟合和奖励退化问题，对超参数的微小波动不敏感。

### 8. 不足与局限
*   **对数概率下降现象**：作者提到在优化后期，目标策略的 $\log \pi_\theta$ 会出现剧烈下降，虽然不影响最终胜率，但其内在机制尚不明确。
*   **静态边际**：目前的 $\xi$ 是在训练前基于初始分布计算的静态值。作者指出，如果能在训练过程中动态更新奖励分布并调整 $\xi$，可能会有进一步的提升。
*   **实验覆盖**：虽然涵盖了 7B-9B 规模的模型，但尚未在更大规模（如 70B+）或多模态模型上验证其有效性。

（完）
