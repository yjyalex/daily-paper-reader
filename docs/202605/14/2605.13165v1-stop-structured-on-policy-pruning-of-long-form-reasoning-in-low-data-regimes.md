---
title: "STOP: Structured On-Policy Pruning of Long-Form Reasoning in Low-Data Regimes"
title_zh: STOP：低数据场景下长式推理的结构化同策略剪枝
authors: "Chenjun Xu, Zhennan Zhou, Zhan Su, Bill Howe, Lucy Lu Wang, Bingbing Wen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13165v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 剪枝长链推理以降低推理成本和延迟
tldr: 针对长链条思维（Long CoT）推理中存在的“过度思考”和推理冗余问题，本文提出了一种名为 STOP 的结构化策略内剪枝算法。该方法通过将推理轨迹映射为结构化树，并利用“最早正确节点”（ECN）准则剪除冗余的解题后推理。在低数据微调场景下，STOP 在保持准确率的同时显著降低了推理成本，为高效推理提供了新思路。
selection_source: fresh_fetch
motivation: 长链条推理模型常产生低效冗余内容，且在低数据环境下难以通过大规模蒸馏或复杂控制来优化推理效率。
method: 通过节点分割和分类构建结构化推理树，并利用 ECN 准则保留通往正确答案的最短前缀，实现对冗余推理的剪枝。
result: "在多个数学基准测试中，该方法在基本不损失准确率的前提下，将生成 Token 数量减少了 19.4% 至 42.4%。"
conclusion: STOP 证明了通过结构化策略内剪枝可以有效提升推理效率，减少分布偏移，并优化模型的推理资源分配。
---

## 摘要
长思维链（Long CoT）推理提升了多步问题的性能，但也引发了“过度思考”：模型往往会生成低收益的推理，从而增加了推理成本和延迟。这种低效在低数据微调场景中尤为严重，因为在实际应用中，推理模型是在有限监督下进行适配的，无法依赖大规模教师蒸馏或繁重的测试时控制。为了解决这一问题，我们提出了 STOP（结构化同策略剪枝），这是一种用于分析和剪枝长式推理轨迹的同策略算法。STOP 从模型中构建自蒸馏轨迹，然后通过节点分割、分类标注和推理树构建，将每条轨迹映射到结构化推理接口中。在此接口之上，我们引入了 ECN（最早正确节点），它保留了结束于最早节点的短前缀，该节点既能作为回答结论，又能产生正确的最终答案，从而在保持语义连续性的同时，消除了冗余的解题后推理。在 GSM8K、Math 500 和 AIME 2024 数据集上针对 DeepSeek-R1-Distill-Qwen-7B 和 DeepSeek-R1-Distill-LLaMA-3-8B 的实验表明，STOP 在低数据微调中将生成的 Token 数量减少了 19.4-42.4%，同时在很大程度上保持了准确性。除了效率提升外，我们的分析还表明，与教师引导的剪枝相比，STOP 引起的分布偏移要小得多，提高了生成推理的结构效率，并将推理精力从冗余的验证和回溯重新分配到更具生产力的探索中。

## Abstract
Long chain-of-thought (Long CoT) reasoning improves performance on multi-step problems, but it also induces overthinking: models often generate low-yield reasoning that increases inference cost and latency. This inefficiency is especially problematic in low-data fine-tuning regimes, where real applications adapt reasoning models with limited supervision and cannot rely on large-scale teacher distillation or heavy test-time control. To address this, we propose STOP (Structured On-policy Pruning), an on-policy algorithm for analyzing and pruning long-form reasoning traces. STOP constructs self-distilled traces from the model. Then it maps each trace into a structured reasoning interface through node segmentation, taxonomy annotation, and reasoning-tree construction. On top of this interface, we introduce ECN (Earliest Correct Node), which retains the shortest prefix ending at the earliest node that both functions as an answering conclusion and yields the correct final answer, removing redundant post-solution reasoning while preserving semantic continuity. Experiments on DeepSeek-R1-Distill-Qwen-7B and DeepSeek-R1-Distill-LLaMA-3-8B across GSM8K, Math 500, and AIME 2024 show that STOP reduces generated tokens by 19.4-42.4% while largely preserving accuracy in low-data fine-tuning. Beyond efficiency, our analyses show that STOP induces much smaller distributional shift than teacher-guided pruning, improves the structural efficiency of generated reasoning, and reallocates reasoning effort away from redundant verification and backtracking toward more productive exploration.

---

## 论文详细总结（自动生成）

以下是对论文《STOP: Structured On-Policy Pruning of Long-Form Reasoning in Low-Data Regimes》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注推理语言模型（RLMs）中的**“过度思考”（Overthinking）**现象。虽然长链条思维（Long CoT）能提升复杂问题的解决能力，但模型往往在得出正确答案后继续生成冗余的验证、回溯或重复逻辑，导致推理成本（Token 消耗）和延迟大幅增加。
在**低数据微调（Low-Data Fine-tuning）**场景下，现有的效率优化方法（如教师模型蒸馏、Token 预算限制或简单的压缩）存在局限：要么导致严重的分布偏移（学生学不像教师），要么破坏了逻辑连贯性。因此，研究如何以结构化、同策略（On-policy）的方式剪枝冗余推理，是提升模型部署效率的关键。

### 2. 方法论（STOP 框架）
STOP（Structured On-policy Pruning）包含三个紧密耦合的步骤：
*   **同策略自蒸馏轨迹构建（On-Policy Self-Distilled Trace Construction）：** 使用学生模型自身生成的成功推理轨迹作为监督信号。通过 Best-of-K 采样筛选出正确且长度适中的轨迹，确保训练数据符合模型原有的推理风格，减少分布偏移。
*   **结构化推理接口（Structured Reasoning Interface）：**
    *   **节点分割：** 利用话语标记（如 However, So）和标点符号将长文本切分为语义连贯的“节点”。
    *   **分类标注：** 使用外部模型（Gemini-2.5-Flash）将节点分为五类：回溯（Backtracking）、验证（Verification）、探索（Exploration）、澄清（Clarification）、结论（Conclusion）。
    *   **推理树构建：** 将节点序列映射为 n 叉树，以全局视角分析推理逻辑。
*   **ECN（最早正确节点）剪枝策略：**
    *   **核心思想：** 在结构化节点序列中，识别出**最早**出现的既是“回答结论”且“结果正确”的节点。
    *   **操作：** 保留从开头到该 ECN 节点的最短前缀，剪除之后的所有冗余内容。这种方法在缩短长度的同时，保证了推理链条在语义上的完整性。

### 3. 实验设计
*   **数据集：** 涵盖不同难度等级的数学推理任务：
    *   **GSM8K：** 基础数学应用题（简单）。
    *   **Math 500：** 中等难度数学题。
    *   **AIME 2024：** 美国数学邀请赛题目（极高难度）。
*   **实验模型：** DeepSeek-R1-Distill-Qwen-7B 和 DeepSeek-R1-Distill-LLaMA-3-8B。
*   **对比方法（Baselines）：**
    *   **Base：** 原始模型。
    *   **No Thinking：** 移除所有推理过程，仅保留答案。
    *   **Teacher-Guided (TG)：** 使用 DeepSeek-R1 完整版作为教师生成轨迹（包含 Full、随机剪枝、ECN 剪枝）。
    *   **Self-Distilled (SD)：** 包含 Full、Token Skip（压缩算法）、随机剪枝、以及本文的 **SD-ECN**。

### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA A100 (80GB)** GPU。
*   **训练细节：** 采用 LoRA 微调，最大序列长度 8192。
*   **数据量：** 仅使用约 1000 个样本进行低数据微调。
*   **标注成本：** 使用 Gemini-2.5-Flash API 进行结构化标注，总成本低于 20 美元。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 2 个模型家族、3 个不同难度的基准测试上进行了详尽对比。
*   **消融与分析：** 
    *   对比了不同数据规模（200, 500, Full）下的表现。
    *   进行了**推理树形态学分析**（节点数、深度、宽度、分支因子等）。
    *   进行了**人工评估**，验证了模型辅助标注的准确性（分类一致性达 88.9%，结论判断准确率达 100%）。
*   **评价：** 实验设计客观、公平，通过 KL 散度分析解释了为什么自蒸馏优于教师引导，论证充分。

### 6. 主要结论与发现
*   **效率显著提升：** 在保持竞争性准确率的前提下，STOP 将生成 Token 数量减少了 **19.4% 至 42.4%**。
*   **自蒸馏的优越性：** 在低数据场景下，同策略自蒸馏比教师引导的蒸馏更稳定，分布偏移（KL 散度）更小。
*   **推理行为重塑：** STOP 不仅仅是简单的截断，它诱导模型减少了冗余的“验证”和“回溯”，将推理资源重新分配到更有产出意义的“探索”步骤中。
*   **结构优化：** 剪枝后的推理树变得更加精简（从“丛生型”变为“目标导向型”），结构效率显著提高。

### 7. 优点
*   **创新性：** 提出了 ECN 这一基于语义节点的剪枝准则，而非传统的 Token 级或随机截断。
*   **实用性：** 针对低数据场景设计，微调成本极低，适合实际应用中的模型适配。
*   **深度分析：** 引入了推理树形态学指标，从认知科学和结构学角度解释了模型行为的变化，超越了单纯的性能指标。

### 8. 不足与局限
*   **Oracle 依赖：** ECN 识别在训练阶段依赖于地面真值（Ground Truth）或外部强模型判断，这意味着它是一个**训练时**的优化策略，而非直接的**推理时**动态停止规则（推理时仍需模型自行学会停止）。
*   **标注模型依赖：** 结构化接口的构建部分依赖于外部模型（如 Gemini），标注过程中的偏差可能会传播到微调阶段。
*   **任务局限：** 目前主要集中在数学推理任务，在创意写作或多轮对话等非结构化任务上的适用性尚未验证。

（完）
