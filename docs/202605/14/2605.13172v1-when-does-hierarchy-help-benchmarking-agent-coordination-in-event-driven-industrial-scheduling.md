---
title: When Does Hierarchy Help? Benchmarking Agent Coordination in Event-Driven Industrial Scheduling
title_zh: 层级结构何时有效？事件驱动工业调度中的智能体协作基准测试
authors: "Ziqi Wang, Yuhao Yang, Zhiwei Ling, Wenzhuo Qian, Hailiang Zhao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13172v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 层次化调度中智能体协作的基准测试
tldr: 本研究针对现有智能体基准测试在复杂耦合、动态演化环境下的协作评估不足，提出了分布式事件驱动调度基准（DESBench）。该基准模拟工业调度中的多时间尺度决策和约束，重点评估了中心化、层级化、对等化和全能化四种协作范式。研究发现，协作设计在效率、鲁棒性和约束对齐之间存在显著的结构性权衡，为未来开发更具适应性的多智能体系统提供了重要参考。
selection_source: fresh_fetch
motivation: 现有基准测试多关注弱耦合环境，缺乏对具有层级和耦合约束的动态共享系统中智能体协作的研究。
method: 提出分布式事件驱动调度基准（DESBench），通过模拟工业调度环境，评估中心化、层级化、对等化和全能化四种协作范式。
result: 实验揭示了不同范式的权衡：中心化鲁棒但扩展性差，层级化高效但存在对齐问题，对等化灵活但通信开销大，全能化约束满足度高但缺乏全局鲁棒性。
conclusion: 协作机制的设计从根本上塑造了智能体系统在复杂环境中的行为，未来研究需探索更具适应性和动态性的协作机制。
---

## 摘要
最近，智能体和多智能体系统在工具使用、推理和协作任务方面展现出强劲的性能。然而，现有的基准测试大多在弱耦合环境下评估任务完成情况，对于研究具有层级结构和耦合约束的共享、动态演化系统中的协作支持有限。这留下了一个尚未得到充分探索的重要问题：不同的协作范式在何时成功或失败？我们引入了分布式事件驱动调度基准测试（DESBench），用于评估层级化事件驱动调度中的智能体协作。该基准基于工业调度中的共享离散事件驱动环境，捕捉了多时间尺度决策、部分可观测性和动态耦合约束。我们定义了评估有效性、约束对齐、协作效率和鲁棒性的任务与指标，并重点关注四种代表性的协作范式：集中式、层级式、异构式（heterarchical）和全能式（holonic）。这些范式对应于不同的信息流、决策权限和冲突解决机制。我们的受控评估揭示了明显的协作权衡：集中式协作具有鲁棒性且通信效率高，但随难度增加的可扩展性较差；层级式协作通过分解提高了效率，但存在跨层级失调；异构式协作灵活但通信开销大；全能式协作能很好地满足约束，但丧失了全局鲁棒性。这些发现表明，协作设计从根本上塑造了复杂环境下的智能体系统行为，揭示了仅凭结果指标无法捕捉的结构性权衡，并强调了未来多智能体系统（MAS）研究中对更具自适应性、原则性和动态协作机制的迫切需求。

## Abstract
Recent advances in agent and multi-agent systems have shown strong performance on tool use, reasoning, and collaborative tasks. However, existing benchmarks mostly evaluate task completion in weakly coupled environments, and provide limited support for studying coordination in shared, dynamically evolving systems with hierarchy and coupled constraints. This leaves an important question underexplored: when do different coordination paradigms succeed or fail? We introduce Distributed Event-driven Scheduling Benchmark (DESBench), a benchmark for evaluating agent coordination in hierarchical event-driven scheduling. Built on a shared discrete-event driven environment in industrial scheduling, our benchmark captures multi-timescale decision making, partial observability, and dynamically coupled constraints. We define tasks and metrics that evaluate effectiveness, constraint alignment, coordination efficiency, and robustness, and focus on four representative coordination paradigms: centralized, hierarchical, heterarchical, and holonic. These paradigms correspond to distinct mechanisms of information flow, decision authority, and conflict resolution. Our controlled evaluations reveal clear coordination trade-offs: centralized coordination is robust and communication-efficient but scales poorly with difficulty; hierarchical coordination improves efficiency through decomposition but suffers from cross-level misalignment; heterarchical coordination is flexible but communication-heavy; and holonic coordination satisfies constraints well but loses global robustness. These findings demonstrate that coordination design fundamentally shapes agent system behavior in complex environments, revealing structural trade-offs that cannot be captured by outcome metrics alone and underscoring the imperative for more adaptive, principled, and dynamic coordination mechanisms in future MAS research.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **DESBench** 的新基准测试，旨在系统地评估和比较多智能体系统（MAS）在复杂、动态且具有层级结构的工业调度环境中的协作范式。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多智能体基准测试大多关注“弱耦合”环境（任务独立、交互简单），无法反映真实世界（如工业生产）中共享资源、动态演化、多层级决策及复杂约束耦合的挑战。
*   **研究动机**：在复杂的工业调度中，协作机制本身是决定系统性能的关键。研究者希望回答：**不同的协作范式（如集中式 vs. 分布式）在何种场景下有效，它们各自的权衡（Trade-offs）是什么？**

### 2. 方法论：DESBench 基准测试
*   **核心思想**：构建一个基于**离散事件驱动（Event-Driven）**的共享仿真环境，模拟工厂的“厂级（Plant）- 区域级（Area）- 单元级（Cell）”三层架构。
*   **关键技术细节**：
    *   **事件驱动机制**：系统状态不随固定时间步推进，而是由任务到达、加工完成、机器故障等离散事件触发决策。
    *   **四种协作范式**：
        1.  **集中式（Centralized）**：厂级智能体直接决策，拥有全局权限。
        2.  **层级式（Hierarchical）**：严格的自上而下指令链，任务逐层分解。
        3.  **异构式（Heterarchical）**：基于合同网协议（CNP）的对等协商，强调局部自主性。
        4.  **全能式（Holonic）**：结合层级结构与局部自治，支持局部故障恢复与重调度。
    *   **数学定义**：将调度实例定义为包含层级图 $H$、状态空间 $X$、事件集 $E$、动作域 $A$、协作协议 $\Gamma$ 和预算约束 $b$ 的多元组。
    *   **评估维度**：定义了四个维度的指标：有效性（完工时间等）、约束对齐（能源/碳排放超标）、协作效率（通信消息数）和鲁棒性（任务欠账）。

### 3. 实验设计
*   **场景与数据集**：基于真实工业数据集（如 Trentesaux 等人的柔性车间调度数据），设计了三种压力场景：分支压力（决策分支多）、强集群拉力（资源耦合紧）、后期任务承诺（长程决策风险）。
*   **智能体配置**：
    *   **LLM 骨干**：GPT-5.4、Gemini-3-Flash、Qwen-3.5。
    *   **编排框架**：LangGraph 和 AgentScope。
*   **对比方法**：在相同的物理仿真环境下，对比上述四种协作范式（$\Gamma_{cen}, \Gamma_{hie}, \Gamma_{het}, \Gamma_{hol}$）。

### 4. 资源与算力
*   **硬件环境**：实验在一台本地 Windows 工作站上完成，配置为 **Intel Core i7-12700K CPU 和 32 GB RAM**。
*   **算力需求**：论文指出该基准测试对硬件要求较低，因为它侧重于评估智能体的协作行为和推理决策，而非大规模模型训练。主要的计算开销在于调用 LLM API。

### 5. 实验数量与充分性
*   **实验规模**：每种范式在 3 种场景下进行评估，每个 LLM 和框架组合运行 10 个随机种子，总计 30 个测试回合（Episodes）。
*   **充分性与公平性**：实验涵盖了主流的闭源和开源大模型，并在两个不同的智能体框架上验证，确保了结论不依赖于特定的实现。通过固定物理环境参数（如故障率、预算），仅改变协作协议 $\Gamma$，实现了客观的受控对比。

### 6. 主要结论与发现
*   **集中式**：鲁棒性强且通信高效，但在复杂环境下调度效率（完工时间）较低，且难以应对大规模动态变化。
*   **层级式**：通过任务分解显著提升了调度效率，但容易出现跨层级的目标失调（Level Misalignment）。
*   **异构式**：极其灵活，能有效应对动态干扰，但通信开销巨大（消息数远超其他范式），容易产生协作噪声。
*   **全能式**：在约束满足（如节能）方面表现最好，但由于过度强调局部自治，往往牺牲了全局进度，导致鲁棒性最差（任务完成率低）。

### 7. 优点与亮点
*   **多维度评估**：不只看任务成功率，还深入分析了通信成本、资源约束和系统鲁棒性，揭示了“过程”与“结果”的背离。
*   **真实性**：引入了工业界真实的离散事件仿真逻辑，包括机器故障、运输延迟和缓冲区阻塞，比传统的简单网格环境更具说服力。
*   **框架无关性**：提供了一套标准接口，允许研究者在不同的 MAS 框架下测试新的协作协议。

### 8. 不足与局限
*   **模型依赖性**：实验结果受 LLM 本身推理能力的影响较大，某些范式的失败可能是由于模型无法处理复杂的 JSON 协议而非范式本身的问题。
*   **拓扑固定**：目前的实验主要集中在 A5C12（5 区域 12 单元）拓扑，对于更大规模（如成百上千个节点）的极端扩展性研究尚不充分。
*   **提示词敏感性**：虽然使用了统一的提示词模板，但不同模型对协作指令的理解偏差可能引入实验噪声。

（完）
