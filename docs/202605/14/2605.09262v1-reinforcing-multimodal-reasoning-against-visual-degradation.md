---
title: Reinforcing Multimodal Reasoning Against Visual Degradation
title_zh: 强化多模态推理以对抗视觉退化
authors: "Rui Liu, Dian Yu, Haolin Liu, Yucheng Shi, Tong Zheng, Runpeng Dai, Haitao Mi, Pratap Tokekar, Leoweiliang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09262v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习微调增强多模态推理鲁棒性
tldr: 针对多模态大模型（MLLM）在面对模糊、压缩等视觉退化时推理能力脆弱的问题，本文提出ROMA框架。该框架通过强化学习微调，利用双向前向传播策略和Token级KL惩罚，在不引入额外采样负担的情况下，增强了模型对视觉噪声的鲁棒性，同时有效避免了奖励中毒并保持了在清晰图像上的高性能。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习方法在处理真实世界视觉退化时表现不稳定，且直接在退化图像上进行采样会导致模型产生幻觉并破坏优化过程。
method: 提出ROMA框架，通过双向前向传播评估退化视图，并结合Token级代理KL惩罚、辅助策略梯度损失及正确性条件正则化来稳定优化动态。
result: "在Qwen3-VL模型上的实验表明，该方法在保持清晰图像准确率的同时，将已知和未知退化场景下的鲁棒性分别提升了2.4%和2.3%。"
conclusion: ROMA成功解决了多模态推理在视觉退化下的脆弱性问题，为提升大模型在复杂现实环境中的可靠性提供了有效的强化学习方案。
---

## 摘要
强化学习（RL）显著提升了多模态大语言模型（MLLM）的推理能力，但由此产生的策略在面对现实世界的视觉退化（如模糊、压缩伪影和低分辨率扫描）时仍然表现脆弱。先前来自计算机视觉和深度强化学习的鲁棒性技术依赖于静态数据增强或基于价值的正则化，这两者都无法直接迁移到自回归 MLLM 的无评论器（critic-free）强化学习微调中。针对此类损坏强化推理并非易事：在采样（rollout）过程中盲目注入退化视图会导致奖励中毒，即感知遮挡会触发幻觉轨迹并导致优化不稳定。我们提出了 ROMA，这是一个强化学习微调框架，它通过修改优化动态来强化对抗视觉退化的推理能力，同时保持在清晰输入下的性能。该框架采用双前向传递策略，利用教师强制（teacher forcing）根据清晰图像轨迹评估损坏视图，从而避免在退化输入上进行新的采样。为了保持分布一致性，我们针对最坏情况的增强应用了标记级（token-level）代理 KL 惩罚；为了防止正则化下的策略崩溃，我们引入了锚定在清晰图像优势（advantages）上的辅助策略梯度损失，以保留可靠的奖励信号；为了避免系统性的错误不变性，正确性条件正则化将约束限制在成功的轨迹上。在 Qwen3-VL 4B/8B 以及七个多模态推理基准测试中，我们的方法在已知损坏上比 GRPO 提升了 +2.4%，在未知损坏上提升了 +2.3%，同时保持了清晰图像的准确率。

## Abstract
Reinforcement Learning has significantly advanced the reasoning capabilities of Multimodal Large Language Models (MLLMs), yet the resulting policies remain brittle against real-world visual degradations such as blur, compression artifacts, and low-resolution scans. Prior robustness techniques from vision and deep RL rely on static data augmentation or value-based regularization, neither of which transfers cleanly to critic-free RL fine-tuning of autoregressive MLLMs. Reinforcing reasoning against such corruptions is non-trivial: naively injecting degraded views during rollout induces reward poisoning, where perceptual occlusions trigger hallucinated trajectories and destabilize optimization. We propose ROMA, an RL fine-tuning framework that modifies the optimization dynamics to reinforce reasoning against visual degradation while preserving clean-input performance. A dual-forward-pass strategy uses teacher forcing to evaluate corrupted views against clean-image trajectories, avoiding new rollouts on degraded inputs. For distributional consistency, we apply a token-level surrogate KL penalty against the worst-case augmentation; to prevent policy collapse under regularization, an auxiliary policy gradient loss anchored to clean-image advantages preserves a reliable reward signal; and to avoid systematically incorrect invariance, correctness-conditioned regularization restricts enforcement to successful trajectories. On Qwen3-VL 4B/8B across seven multimodal reasoning benchmarks, our method improves robustness by +2.4% on seen and +2.3% on unseen corruptions over GRPO while matching clean accuracy.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ROMA** 的强化学习（RL）微调框架，旨在提升多模态大语言模型（MLLM）在面对视觉退化（如模糊、噪声、压缩伪影）时的推理鲁棒性。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：当前的 MLLM 虽然通过强化学习显著提升了推理能力，但在现实场景中表现脆弱。当输入图像存在模糊、低分辨率或压缩损伤时，模型性能会剧烈下降。
*   **研究动机**：
    *   **架构不匹配**：传统的鲁棒性技术依赖于价值网络（Critic），而现代 MLLM 微调多采用无评论器的算法（如 GRPO），难以直接迁移。
    *   **奖励中毒（Reward Poisoning）**：如果在 RL 采样阶段直接使用退化图像，感知遮挡会导致模型产生幻觉轨迹，从而产生错误的奖励信号，导致优化不稳定或策略崩溃。
*   **整体含义**：ROMA 旨在不牺牲清晰图像性能的前提下，通过改进优化动态，使模型在视觉噪声环境下依然能保持逻辑一致的推理能力。

### 2. 方法论
ROMA 的核心思想是**“在清晰图像上采样，在退化图像上正则化”**。其关键技术包括：
*   **双向前向传播策略（Dual-Forward-Pass）**：
    *   第一遍：在清晰图像上进行标准的 RL 采样（Rollout），获取推理轨迹和优势值（Advantages）。
    *   第二遍：利用“教师强制”（Teacher Forcing），将相同的轨迹在多个退化视图下重新评估，计算 Token 级的概率分布。这避免了在退化图像上重新采样，从而解决了奖励中毒问题。
*   **最坏情况标记级不变性（Worst-Case Token-Level Invariance）**：
    *   在多个增强视图中，选择与清晰视图差异最大的（最坏情况）视图，施加 Token 级的 KL 散度惩罚，强制模型在噪声下保持分布一致。
*   **辅助策略梯度损失（Auxiliary Policy Gradient Loss）**：
    *   在退化视图上计算一个辅助的策略梯度损失，但其“优势值”锚定在清晰图像的反馈上。这为模型提供了在噪声下的主动学习信号，防止策略在过度正则化下崩溃。
*   **正确性条件正则化（Correctness-Conditioned Regularization）**：
    *   仅对那些在清晰图像上推理正确的轨迹施加不变性惩罚，避免模型“鲁棒地学习错误逻辑”。

### 3. 实验设计
*   **模型基础**：Qwen3-VL-4B 和 8B Instruct 版本。
*   **数据集**：训练使用 MMRL30k（约 3 万个样本）。
*   **评测基准（7 个）**：MathVista, WeMath, ChartQA, LogicVista, MMStar, VisualPuzzles, RealWorldQA。
*   **退化协议**：
    *   **已知退化（Seen）**：高斯噪声、模糊、JPEG 压缩、分辨率缩放。
    *   **未知退化（Unseen/OOD）**：运动模糊、椒盐噪声、斑点噪声、海报化、像素化。
*   **对比方法**：Base 模型、标准 GRPO，以及外部模型如 Vision-R1、NoisyRollout、PAPO 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到使用 EasyR1 框架，训练 120 个 step，学习率 1e-6，全局 Batch Size 为 128，每个输入生成 8 个 rollout。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种模型尺寸（4B/8B）、7 个主流多模态基准、9 种不同的视觉退化类型。
*   **消融实验**：针对“最坏情况策略”、“辅助 PG 损失”、“正确性条件”以及超参数（K 值、权重系数）均做了详细的消融分析。
*   **充分性评价**：实验设计较为充分，通过“已知”与“未知”退化的区分，客观地验证了模型的泛化能力（OOD 鲁棒性），而非仅仅是过拟合训练集中的噪声。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在 8B 模型上，ROMA 在已知退化上比 GRPO 提升了 **2.4%**，在未知退化上提升了 **2.3%**。
*   **保持清晰性能**：ROMA 在清晰图像上的表现（68.7%）与标准 GRPO（68.9%）基本持平，证明了该方法不会损害模型原有的推理能力。
*   **抗干扰能力强**：随着退化程度加剧（Level 1 到 Level 3），ROMA 的性能下降曲线比 Base 和 GRPO 更平缓。

### 7. 优点
*   **训练效率高**：通过 Teacher Forcing 机制，在不增加额外采样开销的情况下实现了多视图正则化。
*   **设计精巧**：解决了 RL 微调中常见的“奖励中毒”问题，通过锚定清晰图像的优势值，确保了优化目标的可靠性。
*   **泛化性强**：实验证明模型对未见过的噪声类型也具有良好的零样本迁移能力。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体的硬件资源消耗，难以评估其在大规模生产环境下的训练成本。
*   **模态局限**：目前仅针对静态图像退化，尚未扩展到视频等时序多模态数据的鲁棒性研究。
*   **训练数据规模**：30k 的训练数据相对较小，在大规模工业级数据集上的表现仍有待验证。

（完）
