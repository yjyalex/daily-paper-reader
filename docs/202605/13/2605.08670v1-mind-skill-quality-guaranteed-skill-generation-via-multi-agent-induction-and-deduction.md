---
title: "MIND-Skill: Quality-Guaranteed Skill Generation via Multi-Agent Induction and Deduction"
title_zh: MIND-Skill：通过多智能体归纳与演绎实现质量保证的技能生成
authors: "Yixuan Li, Mingshu Cai, Ziyang Xiao, Wanyuan Wang, Yanchen Deng, Bo An"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08670v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于多智能体归纳和演绎的框架，为复杂任务生成可重用的智能体技能。
tldr: 针对LLM智能体在处理复杂任务时缺乏领域特定知识的问题，本文提出MIND-Skill框架。该框架通过多智能体归纳与演绎机制，从成功轨迹中自动提取可复用的技能。利用归纳智能体抽象技能，演绎智能体验证技能，并结合TextGrad优化多种文本损失函数以确保技能质量。实验证明该方法在AppWorld等基准测试中显著优于现有技能生成方法。
selection_source: fresh_fetch
motivation: 现有的智能体技能库构建高度依赖人工，且缺乏自动生成高质量、可泛化技能的有效机制。
method: 提出由归纳和演绎智能体组成的框架，通过重构损失、结果损失和准则损失，并利用TextGrad进行联合优化来生成技能。
result: 在AppWorld和BFCL-v3基准测试中，MIND-Skill生成的技能在未见任务上的表现一致优于现有的技能生成方法。
conclusion: MIND-Skill通过自动化的归纳-演绎闭环，为提升LLM智能体在复杂现实任务中的自主解决能力提供了高质量的技能保障。
---

## 摘要
由大语言模型（LLM）驱动的 AI 智能体已成为自主解决问题的一种极具前景的范式，但它们在处理需要特定领域程序性知识的复杂、多步现实任务时仍然面临挑战。可重用的智能体技能封装了成功的问题解决策略，通过使智能体能够基于先前经验进行构建，提供了一种自然的补救方案。然而，策划此类技能在很大程度上仍是一项手动工作，需要人类专家将丰富的领域知识提炼为可操作的指南。在这项工作中，我们提出了 MIND-Skill（Multi-agent INduction and Deduction for Skills），这是一个能够从成功轨迹中自动归纳出具有鲁棒质量保证的可泛化技能的框架。MIND-Skill 由一个归纳智能体和一个演绎智能体组成，前者负责从成功轨迹中抽象出可重用技能，后者旨在通过遵循归纳出的技能来重建轨迹。为了保证生成技能的质量，我们引入了比较输入轨迹与重建轨迹的重建损失（reconstruction loss）、强制重建轨迹正确性的结果损失（outcome loss），以及根据预定义标准评估文档质量并规范生成技能抽象水平的评分量表损失（rubric loss）。这些文本损失通过 TextGrad 进行联合优化，生成的技能在优化过程中未见过的留出任务上进行评估。在 AppWorld 和 BFCL-v3 上的实验表明，MIND-Skill 始终优于同期的技能生成方法。

## Abstract
Large language model (LLM) powered AI agents have emerged as a promising paradigm for autonomous problem-solving, yet they continue to struggle with complex, multi-step real-world tasks that demand domain-specific procedural knowledge. Reusable agent skills, which encapsulate successful problem-solving strategies, offer a natural remedy by enabling agents to build on prior experience. However, curating such skills has largely remained a manual endeavor, requiring human experts to distill rich domain knowledge into actionable guidelines. In this work, we present $\textbf{M}$ulti-agent $\textbf{IN}$duction and $\textbf{D}$eduction for $\textbf{Skill}$s ($\textbf{MIND-Skill}$), a framework that automatically induces generalizable skills from successful trajectories with robust quality guarantees. MIND-Skill consists of an induction agent which is tasked to abstract reusable skills from successful trajectories, and a deduction agent which aims to reconstruct trajectories by following the induced skills. To guarantee the quality of the generated skills, we introduce a reconstruction loss that compares input and reconstructed trajectories, an outcome loss that enforces the correctness of the reconstructed trajectories, and a rubric loss that assesses the documentation quality and regularizes the abstraction level of the generated skills according to predefined criteria. These textual losses are jointly optimized with TextGrad, and the resulting skills are evaluated on held-out tasks unseen during optimization. Experiments on AppWorld and BFCL-v3 show that MIND-Skill consistently outperforms concurrent skill generation methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MIND-Skill** 的框架，旨在通过多智能体协作自动生成高质量、可泛化的智能体技能。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：虽然大语言模型（LLM）拥有丰富的陈述性知识，但在处理复杂的现实世界任务（如多步工具调用、API 使用）时，往往缺乏“程序性知识”（Procedural Knowledge）。
*   **核心痛点**：
    1.  **人工成本高**：目前高质量的智能体技能库（如标准操作程序 SOP）大多依赖专家手动编写。
    2.  **自动生成缺乏质量保证**：现有的自动生成方法（如零样本生成或轨迹蒸馏）通常是开环的，缺乏验证、修正和精炼机制。
    3.  **文档质量与抽象度失控**：生成的技能往往要么过于具体（导致无法泛化），要么过于抽象（缺乏指导性），且文档格式不规范。
*   **研究目标**：开发一个能够从成功执行轨迹中自动提取、验证并优化技能的闭环框架，确保生成的技能既能忠实还原成功策略，又具备良好的泛化性和文档质量。

### 2. 方法论：MIND-Skill 框架
MIND-Skill 的核心思想是**“归纳（Induction）- 演绎（Deduction）”**的闭环优化。
*   **双智能体架构**：
    *   **归纳智能体 (Induction Agent)**：负责从成功的任务轨迹中抽象出可重用的技能文档。它被要求识别非显性的程序惯例，同时抑制特定实例的细节。
    *   **演绎智能体 (Deduction Agent)**：处于冻结状态，仅根据归纳出的技能和任务说明，在真实环境中尝试重建原始轨迹。如果技能写得不好，演绎智能体将无法复现成功。
*   **三大文本损失函数 (Textual Losses)**：
    1.  **重构损失 ($L_{recon}$)**：由 LLM 裁判评估重建轨迹与原始成功轨迹在策略逻辑上的一致性。
    2.  **结果损失 ($L_{outcome}$)**：在真实环境中执行重建轨迹，根据是否通过单元测试或任务检查点来衡量。
    3.  **准则损失 ($L_{rubric}$)**：根据预定义的 5 个维度（独立性、可操作性、可迁移性、完整性、简洁性）评估技能文档的质量。
*   **优化流程**：利用 **TextGrad** 技术，将上述三种损失的文本反馈作为“梯度”，通过迭代（通常为 8 轮）不断精炼归纳智能体的提示词（Prompt），从而生成更高质量的技能。

### 3. 实验设计
*   **数据集/场景**：
    *   **AppWorld**：一个复杂的交互式编码基准测试，包含 9 个日常应用和 457 个 API，分为 Normal 和 Challenge 两个难度等级。
    *   **BFCL-v3 (Berkeley Function Calling Leaderboard)**：多轮函数调用基准测试。
*   **对比方法**：
    *   **基础模型**：ReAct, In-Context Learning (ICL)。
    *   **技能生成基准**：Skill-extract（单次归纳）、ACE（终身演化方法）、Trace2Skill（轨迹蒸馏方法）。
*   **评估指标**：任务目标完成率 (TGC) 和场景目标完成率 (SGC)。

### 4. 资源与算力
*   **模型使用**：实验主要使用了 **Qwen3.5-122B-A10B** 和 **GPT-5.4**（通过 OpenRouter API 调用）。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法基于 Prompt 优化和 API 调用，其开销主要体现在推理 Token 的消耗上。作者提到每项任务最多进行 8 轮迭代优化。

### 5. 实验数量与充分性
*   **实验规模**：在 AppWorld 的 90 个训练任务上提取技能，并在 168 个 Normal 任务和 417 个 Challenge 任务上进行测试；BFCL-v3 使用了 50 个训练实例和 150 个测试实例。
*   **充分性**：
    *   进行了**跨模型实验**（用 GPT 生成技能给 Qwen 使用），验证了框架的通用性。
    *   进行了**消融实验**，分别验证了三种损失函数的必要性。
    *   分析了**迭代次数**和**检索技能数量 (K)** 对性能的影响。
    *   实验设计涵盖了从简单到极难的多个维度，对比了当前最先进的（SOTA）同类方法，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：MIND-Skill 在所有基准测试中均显著优于现有方法，尤其在 AppWorld-Challenge 难度下，SGC 指标大幅领先。
*   **闭环优化的重要性**：相比单次提取（Skill-extract），经过迭代优化的技能在性能上有质的飞跃。
*   **弱模型逆袭**：使用较弱的模型（如 Qwen）配合 MIND-Skill 框架生成的技能，其表现可以媲美甚至超过由最强模型（如 GPT-5.4）生成的技能，证明了框架对模型能力的补偿作用。
*   **紧凑性**：生成的技能文档比 ACE 等方法更简洁，减少了推理时的上下文负担。

### 7. 优点（亮点）
*   **质量保证机制**：引入“演绎重建”作为硬性约束，确保了技能的“忠实度”和“可执行性”。
*   **多维度评估**：不仅关注任务成功率，还通过 Rubric Loss 关注文档的专业性和可读性。
*   **自动化程度高**：实现了从原始轨迹到高质量技能库的端到端自动构建，无需人工干预。
*   **TextGrad 的巧妙应用**：将离散的文本反馈转化为可优化的信号，解决了离散空间下技能精炼的难题。

### 8. 不足与局限
*   **依赖成功轨迹**：框架的前提是必须先有成功的执行轨迹。如果模型本身无法在某个领域完成任何任务，则无法启动技能归纳。
*   **推理成本**：虽然生成的技能文档紧凑，但生成过程涉及多次迭代和双智能体交互，初期构建技能库的 API 成本较高。
*   **潜在的安全风险**：自动化技能生成可能被用于生成恶意行为的 SOP，虽然文档是人类可读的（便于审计），但仍需额外的安全护栏。
*   **实时性限制**：该方法更倾向于离线构建技能库，对于需要实时学习新技能的极动态环境，其优化速度可能受限。

（完）
