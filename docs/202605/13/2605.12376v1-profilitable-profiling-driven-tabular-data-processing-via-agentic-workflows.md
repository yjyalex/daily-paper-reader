---
title: "ProfiliTable: Profiling-Driven Tabular Data Processing via Agentic Workflows"
title_zh: ProfiliTable：基于智能体工作流的剖析驱动型表格数据处理
authors: "Wei Liu, Yang Gu, Xi Yan, Zihan Nan, Beicheng Xu, Keyao Ding, Bin Cui, Wentao Zhang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12376v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于数据处理的自主多智能体框架
tldr: 针对表格处理中LLM难以处理模糊指令和复杂结构的问题，本文提出ProfiliTable框架。该框架采用多智能体协作模式，通过动态分析构建执行上下文，结合ReAct式探索、知识增强合成及反馈驱动的迭代优化，实现了高效且语义准确的自动化表格清洗与转换，显著提升了复杂任务的处理性能。
selection_source: fresh_fetch
motivation: 传统的LLM表格处理方法在面对模糊指令和复杂任务时，常因缺乏对数据的深度理解和结构化反馈而生成语义错误的代码。
method: 提出一种基于多智能体的工作流，整合了ReAct式数据探索分析器、算子增强的代码生成器以及闭环反馈的评估总结器。
result: 在包含18种任务类型的多样化基准测试中，ProfiliTable在复杂多步场景下的表现显著优于现有主流基准模型。
conclusion: 动态分析机制是实现从模糊用户意图到稳健、合规的表格转换代码自动生成的关键因素。
---

## 摘要
表格处理——包括清洗、转换、增强和匹配——是现实世界数据流水线中基础但易出错的阶段。虽然近期基于大语言模型（LLM）的方法在自动化此类任务方面展现出潜力，但由于指令模糊、任务结构复杂以及缺乏结构化反馈，它们在实践中往往表现不佳，导致生成的代码虽然语法正确但在语义上存在缺陷。为了应对这些挑战，我们提出了 ProfiliTable，这是一个以动态剖析为核心的自主多智能体框架，它通过交互式探索、知识增强合成和反馈驱动的优化，构建并迭代完善统一的执行上下文。ProfiliTable 集成了：(i) 一个执行 ReAct 风格数据探索以构建语义理解的剖析器（Profiler）；(ii) 一个检索精选算子以合成任务感知代码的生成器（Generator）；以及 (iii) 一个注入执行评分和诊断见解以实现闭环优化的评估器-总结器（Evaluator-Summarizer）循环。在涵盖 18 种表格任务类型的多样化基准测试上进行的广泛实验表明，ProfiliTable 始终优于强基准模型，特别是在复杂的多步场景中。这些结果强调了动态剖析在将模糊的用户意图可靠地转化为稳健且符合治理要求的表格转换方面的关键作用。

## Abstract
Table processing-including cleaning, transformation, augmentation, and matching-is a foundational yet error-prone stage in real-world data pipelines. While recent LLM-based approaches show promise for automating such tasks, they often struggle in practice due to ambiguous instructions, complex task structures, and the lack of structured feedback, resulting in syntactically correct but semantically flawed code. To address these challenges, we propose ProfiliTable, an autonomous multi-agent framework centered on dynamic profiling, which constructs and iteratively refines a unified execution context through interactive exploration, knowledge-augmented synthesis, and feedback-driven refinement. ProfiliTable integrates (i) a Profiler that performs ReAct-style data exploration to build semantic understanding, (ii) a Generator that retrieves curated operators to synthesize task-aware code, and (iii) an Evaluator-Summarizer loop that injects execution scores and diagnostic insights to enable closed-loop refinement. Extensive experiments on a diverse benchmark covering 18 tabular task types demonstrate that ProfiliTable consistently outperforms strong baselines, particularly in complex multi-step scenarios. These results highlight the critical role of dynamic profiling in reliably translating ambiguous user intents into robust and governance-compliant table transformations.

---

## 论文详细总结（自动生成）

以下是对论文《ProfiliTable: Profiling-Driven Tabular Data Processing via Agentic Workflows》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：表格数据处理（清洗、转换、增强、匹配）是数据科学中最耗时且易出错的环节。虽然大语言模型（LLM）在生成处理代码方面有潜力，但在实际应用中面临三大挑战：指令模糊、任务结构复杂、缺乏结构化反馈。
*   **核心问题**：现有的 LLM 方法往往生成“语法正确但语义错误”的代码。例如，面对“标准化货币列”的模糊指令，若不实际观察数据值（Profiling），模型无法得知具体的货币符号或格式，导致生成的映射逻辑失效。
*   **研究动机**：提出一种从“被动元数据消费”转向“主动动态剖析”的机制，通过智能体（Agent）与数据的交互来消除歧义。

### 2. 方法论
ProfiliTable 是一个自主多智能体框架，其核心思想是构建并迭代完善一个**统一的动态剖析上下文（Dynamic Profiling Context）**。
*   **核心流程**：
    1.  **解释器（Interpreter）**：解析用户意图，识别任务类型（单步或多步）。
    2.  **剖析器（Profiler）**：采用 **ReAct 模式**主动探索原始表。它不仅看表结构，还会执行采样、计算唯一值等轻量级操作，形成对数据质量和语义的深度理解。
    3.  **分解器（Decompositer）**：将复杂的多步任务拆解为有序的原子子任务。
    4.  **生成器（Generator）**：利用 **RAG（检索增强生成）**从精选算子库中检索匹配的模板，结合剖析结果合成可执行代码。
    5.  **评估器（Evaluator）**：在沙盒中运行代码，根据 Ground-truth 计算任务特定的评分（如 F1 分数）。
    6.  **总结器（Summarizer）**：再次通过 ReAct 模式检查**处理后的表**，验证是否符合原始意图，并提供诊断性见解。
    7.  **最终化器（Finalizer）**：根据评分阈值或最大重试次数决定停止迭代或返回最佳候选结果。
*   **公式化表达**：上下文 $P^{(t)}$ 随迭代演进，包含当前剖析总结、检索到的算子和反馈历史（含执行分、错误追踪和诊断见解）。

### 3. 实验设计
*   **基准测试（Benchmark）**：构建了一个包含 **18 种细分任务类型**的综合基准，涵盖表格清洗、转换、增强和匹配四大维度。
*   **对比方法**：
    *   **专用智能体**：DataGovAgent（数据治理）、CleanAgent（数据清洗）。
    *   **通用多智能体框架**：MetaGPT、ChatDev 2.0、CAMEL。
*   **评估指标**：
    *   **性能指标**：平均任务得分（ATS）、任务成功率（TSR）、部分成功率（PSR）、代码可运行率（CRR）、任务级可运行率（TRR）。
    *   **效率指标**：平均消耗 Token 数、平均耗时。

### 4. 资源与算力
*   **模型使用**：实验主要基于 **GPT-4o** 和 **GPT-5.2**（注：论文标注发布时间为 2026 年，此处 GPT-5.2 为文中设定的实验模型）。
*   **算力说明**：文中未明确提及具体的 GPU 硬件型号或训练时长，因为该框架主要依赖于商业 LLM 的 API 调用进行推理和交互。
*   **超参数设置**：RAG 检索 $k=2$ 个算子，相似度阈值 0.5，剖析循环最多 7 步，反馈优化最多 3 轮。

### 5. 实验数量与充分性
*   **实验规模**：在 18 种任务类型上进行了单步和多步场景的全面测试。
*   **消融实验**：分别移除了剖析器、反馈机制、RAG 和分解器，验证了各组件的必要性。
*   **超参数分析**：对检索数量 $k$ 和相似度阈值 $\theta$ 进行了敏感度分析。
*   **充分性评价**：实验设计较为客观、公平。通过对比不同能力的基准模型（GPT-4o vs GPT-5.2），证明了该框架的提升是模型无关的（Model-agnostic）。

### 6. 主要结论与发现
*   **性能领先**：ProfiliTable 在单步和多步任务中均显著优于所有基准模型。在多步任务中，其 ATS 得分比第二名高出约 10-30 个百分点。
*   **高可靠性**：在多步任务中实现了 **100% 的任务级可运行率（TRR）**，这对于生产环境部署至关重要。
*   **效率优势**：在性能-成本权衡（Pareto Frontier）分析中，ProfiliTable 在保持高准确率的同时，Token 消耗量处于较低水平。
*   **关键组件**：消融实验显示，**反馈机制（Feedback）**对性能影响最大，其次是动态剖析。

### 7. 优点
*   **动态性**：不同于传统的静态元数据读取，ReAct 驱动的剖析能根据任务需求按需探索数据，减少了冗余信息。
*   **闭环优化**：引入总结器对输出结果进行二次验证，模拟了人类“观察-假设-实现-验证-反思”的思维过程。
*   **鲁棒性**：通过算子库 RAG 减少了 LLM 的幻觉，确保生成的代码符合领域规范。

### 8. 不足与局限
*   **长链条瓶颈**：在超长步骤（如 7 步以上）的流水线中，性能仍会出现明显下降，存在错误累积风险。
*   **领域知识缺失**：对于某些极度模糊且缺乏领域常识（如特定行业对“NA”值的特殊处理逻辑）的指令，智能体仍难以完美处理。
*   **依赖性**：高度依赖底层 LLM 的推理能力，在弱模型上的表现尚未得到充分验证。

（完）
