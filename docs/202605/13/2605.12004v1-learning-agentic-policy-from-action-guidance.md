---
title: Learning Agentic Policy from Action Guidance
title_zh: 从动作引导中学习智能体策略
authors: "Yuxiang Ji, Zengbin Wang, Yong Wang, Shidong Yang, Ziyu Ma, Guanhua Chen, Zonghua Sun, Liaoni Wu, Xiangxiang Chu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12004v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用动作引导的大语言模型代理强化学习
tldr: 针对大语言模型智能体强化学习中基础策略探索能力受限的问题，本文提出 ActGuide-RL。该方法利用日常交互中的动作数据作为计划式参考引导，帮助智能体突破奖励状态的可达性障碍。通过混合策略训练和最小干预原则，将引导带来的探索收益内化到无引导策略中，在减少对大规模 SFT 数据依赖的同时，显著提升了智能体在搜索任务上的表现。
selection_source: fresh_fetch
motivation: 基础策略在无法触及奖励状态时难以产生有效的强化学习信号，且传统的迭代式监督微调成本过高。
method: 提出 ActGuide-RL 框架，将动作数据作为自适应回退引导注入训练，并通过混合策略优化将探索增益内化至原始策略。
result: "在 GAIA 和 XBench 基准测试中，该方法相比纯强化学习分别提升了 10.7% 和 19%，性能与 SFT+RL 流程相当。"
conclusion: 该研究提出了一种利用可扩展动作引导替代重度 SFT 数据的新范式，有效降低了智能体强化学习的冷启动门槛。
---

## 摘要
大语言模型（LLM）的智能体强化学习（RL）关键取决于基础策略的探索能力，因为训练信号仅在其能力范围内产生。对于基础策略无法到达奖励状态的任务，需要额外的训练或外部引导来恢复有效的学习信号。我们没有依赖昂贵的迭代监督微调（SFT），而是利用了日常人类交互中产生的丰富动作数据。我们提出了 ActGuide-RL，它将动作数据作为计划式参考引导注入，使智能体策略能够克服通往奖励状态的可达性障碍。随后，通过混合策略训练对有引导和无引导的采样（rollouts）进行联合优化，将探索收益内化回无引导策略中。基于对收益-风险权衡的理论和实证分析，我们采用了最小干预原则，仅在作为自适应回退方案时调用引导，在匹配任务难度的同时最小化离策略（off-policy）风险。在搜索智能体基准测试中，ActGuide-RL 相比于零样本强化学习（zero RL）有显著提升（在 GAIA 上提升 10.7 个百分点，在 XBench 上使用 Qwen3-4B 提升 19 个百分点），并且在无需任何冷启动的情况下，表现与 SFT+RL 流水线相当。这为智能体强化学习提供了一种新范式，即通过使用可扩展的动作引导来减少对大量 SFT 数据的依赖。

## Abstract
Agentic reinforcement learning (RL) for Large Language Models (LLMs) critically depends on the exploration capability of the base policy, as training signals emerge only within its in-capability region. For tasks where the base policy cannot reach reward states, additional training or external guidance is needed to recover effective learning signals. Rather than relying on costly iterative supervised fine tuning (SFT), we exploit the abundant action data generated in everyday human interactions. We propose \textsc{ActGuide-RL}, which injects action data as plan-style reference guidance, enabling the agentic policy to overcome reachability barriers to reward states. Guided and unguided rollouts are then jointly optimized via mixed-policy training, internalizing the exploration gains back into the unguided policy. Motivated by a theoretical and empirical analysis of the benefit-risk trade-off, we adopt a minimal intervention principle that invokes guidance only as an adaptive fallback, matching task difficulty while minimizing off-policy risk. On search-agent benchmarks, \textsc{ActGuide-RL} substantially improves over zero RL (+10.7 pp on GAIA and +19 pp on XBench with Qwen3-4B), and performs on par with the SFT+RL pipeline without any cold start. This suggests a new paradigm for agentic RL that reduces the reliance on heavy SFT data by using scalable action guidance instead.

---

## 论文详细总结（自动生成）

### 论文总结：从动作引导中学习智能体策略 (ActGuide-RL)

#### 1. 核心问题与整体含义（研究动机和背景）
智能体强化学习（Agentic RL）的效果高度依赖于基础策略的探索能力。当任务难度超出模型当前能力范围（即进入“非可达区域”/Out-region）时，模型无法在采样中触及奖励状态，导致优势估计坍缩为零梯度，训练陷入停滞。
*   **研究动机**：传统的解决方法是进行昂贵的监督微调（SFT）冷启动，但这增加了对高质量演示数据的依赖。
*   **核心问题**：如何利用互联网或日常交互中海量、易得但缺乏推理链条（CoT）的“纯动作数据”（Action Data）来引导强化学习，使其跨越探索障碍。

#### 2. 方法论：ActGuide-RL 框架
论文提出了 **ActGuide-RL**，其核心思想是将动作数据作为“计划式参考引导”注入训练过程。
*   **动作引导（Action Guidance）**：将动作序列（如 API 调用、点击流）作为参考计划拼接到 Prompt 中，帮助模型在采样时跨越关键的“可达性障碍”。
*   **最小干预原则（Minimal Intervention Principle）**：
    *   **自适应回退**：仅当无引导的采样全部失败时，才引入动作引导。
    *   **强度搜索**：通过二分查找确定最小的引导长度 $k^*$，既能保证触及奖励，又最大限度减少对原始分布的干扰。
*   **离策略内化（Off-Policy Internalization）**：
    *   采用混合策略优化，将有引导的采样视为离策略数据。
    *   通过调整重要性采样比率（Importance Ratio），将引导下获得的成功经验转移（内化）到无引导的目标策略中，确保推理时无需引导也能成功。

#### 3. 实验设计
*   **数据集/场景**：专注于**搜索智能体（Search-Agent）**领域。
    *   **域内基准（In-domain）**：GAIA（通用助手）、WebWalkerQA（复杂网页遍历）、XBench（生产力工具）、BrowseComp-ZH（中文网页浏览）。
    *   **域外泛化（Out-of-domain）**：GPQA（研究生水平问答）、TruthfulQA（事实性）、IFEval（指令遵循）。
*   **对比方法**：
    *   基础模型（Qwen2.5, Qwen3, DeepSeek-V3 等）。
    *   专门训练的智能体模型（WebSailor, ARPO, WebThinker）。
    *   **Vanilla RL**：不带引导的 GRPO 强化学习。
    *   **SFT + RL 流水线**：传统的两阶段训练模式。

#### 4. 资源与算力
*   **硬件环境**：训练和采样均在配备 **8 张 NVIDIA H20 GPU** 的节点上进行。
*   **奖励模型**：使用额外的 8 张 H20 GPU 节点部署 LLM-Judge（基于参考答案的二分类评判）。
*   **训练细节**：学习率 1e-6，KL 系数 0.001，训练数据 2000 条，总步数 64 步。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 8B 不同参数规模的 4 种基础模型。
*   **充分性**：
    *   进行了详尽的消融实验，验证了“最小干预”、“混合策略优化”和“自适应强度”的必要性。
    *   测试了对**动作噪声**的敏感度（在 10% 噪声下依然稳健）。
    *   对比了不同的引导注入方式（如前缀注入 vs. 参考计划注入）。
*   **客观性**：实验不仅关注域内性能提升，还评估了对通用能力（域外）的影响，证明了该方法不会像 SFT 那样导致严重的分布偏移或能力退化。

#### 6. 主要结论与发现
*   **显著提升**：在 Qwen3-4B 上，ActGuide-RL 相比纯 RL 在 GAIA 上提升了 **10.7%**，在 XBench 上提升了 **19%**。
*   **替代 SFT**：在无需任何 SFT 冷启动的情况下，ActGuide-RL 的表现与“SFT + RL”流水线相当，大幅降低了智能体训练的门槛。
*   **能力内化**：模型在训练过程中逐渐学会了复杂的长程交互（交互轮数和 Token 长度稳步增长），证明了引导信号被成功吸收。
*   **克服坍缩**：动作引导有效修复了“质量坍缩”问题，使模型能从原本无法解决的难题中获得学习信号。

#### 7. 优点
*   **数据效率高**：利用了易于大规模获取的纯动作轨迹，而非昂贵的专家推理演示。
*   **自适应性强**：最小干预原则平衡了“探索收益”与“离策略风险”，避免了过度引导导致的模型僵化。
*   **即插即用**：可以作为现有 RL 算法（如 PPO, GRPO）的增强模块。

#### 8. 不足与局限
*   **场景局限**：目前主要在搜索智能体（Search-Agent）上验证，尚未在 CLI、GUI 或具身智能等更复杂的交互环境中广泛测试。
*   **数据处理黑盒**：论文未详细讨论如何系统性地从海量日志中清洗和过滤出高质量的动作数据。
*   **引导粒度**：目前采用的是计划式引导，更细粒度的步骤级（Step-level）引导对性能的影响尚待研究。

（完）
