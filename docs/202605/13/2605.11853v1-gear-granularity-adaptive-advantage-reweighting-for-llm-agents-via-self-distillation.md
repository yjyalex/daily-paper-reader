---
title: "GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation"
title_zh: GEAR：通过自我蒸馏实现的 LLM 智能体粒度自适应优势重加权
authors: "Sijia Li, Yuchen Huang, Zifan Liu, Yanping Li, Jingjing Fu, Li Zhao, Jiang Bian, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11853v1"
tags: ["query:token"]
score: 10.0
evidence: 用于强化学习信用分配的 Token 级和片段级信号
tldr: "针对LLM智能体在强化学习中面临的粗粒度奖励和信用分配难题，本文提出GEAR框架。该方法通过自蒸馏机制，对比学生模型与教师模型的散度信号来识别语义偏差锚点，从而实现粒度自适应的优势重加权。在数学推理和工具使用等长程任务中，GEAR显著提升了模型性能，相比GRPO基准最高提升约20%，为复杂任务下的策略优化提供了更精准的监督信号。"
selection_source: fresh_fetch
motivation: 传统的强化学习主要依赖粗粒度的结果级奖励，难以在长程轨迹中实现精确的信用分配以指导策略更新。
method: 提出GEAR框架，利用自蒸馏产生的散度信号识别语义偏差，动态调整Token和片段级别的优势权重以重塑GRPO优势函数。
result: 在八项数学推理和工具使用基准测试中，GEAR一致优于GRPO及其他信用分配方法，在挑战性长程任务中表现尤为突出。
conclusion: 粒度自适应的信用分配方案能有效解决长程任务中的监督不足问题，是提升LLM智能体性能的有效途径。
---

## 摘要
强化学习已成为大语言模型（LLM）智能体广泛使用的后训练方法，其训练通常依赖于仅提供粗粒度监督的结果级奖励。虽然更细粒度的信用分配（credit assignment）有望实现有效的策略更新，但获取可靠的局部信用并将其分配到长程轨迹的正确部分仍是一个开放性挑战。在本文中，我们提出了 GEAR（粒度自适应优势重加权），这是一个自适应粒度的信用分配框架，它利用源自自我蒸馏的标记（token）级和片段（segment）级信号来重塑轨迹级的 GRPO 优势。GEAR 将在线策略学生模型与以地面真值（ground-truth）为条件的教师模型进行比较，以获得参考引导的散度信号，从而识别自适应片段边界并调节局部优势权重。这种散度通常在语义偏离开始时激增，而同一自回归延续中的后续标记可能会恢复到低散度。因此，GEAR 将此类激增视为自适应信用区域的锚点：在学生与教师保持一致的地方，保留标记级分辨率；在偏离的地方，GEAR 将相应的延续部分组合成一个自适应片段，并利用偏离点的散度来调节该片段的优势。在涵盖数学推理和智能体工具使用的八个基准测试中，使用 Qwen3 4B 和 8B 模型的实验表明，GEAR 一致优于标准的 GRPO、仅使用自我蒸馏的基准模型以及标记级或轮次级的信用分配方法。在 GRPO 基准准确率较低的任务中，提升尤为显著，最高比 GRPO 高出约 20%，这表明所提出的自适应重加权方案在更具挑战性的长程场景中特别有用。

## Abstract
Reinforcement learning has become a widely used post-training approach for LLM agents, where training commonly relies on outcome-level rewards that provide only coarse supervision. While finer-grained credit assignment is promising for effective policy updates, obtaining reliable local credit and assigning it to the right parts of the long-horizon trajectory remains an open challenge. In this paper, we propose Granularity-adaptivE Advantage Reweighting (GEAR), an adaptive-granularity credit assignment framework that reshapes the trajectory-level GRPO advantage using token- and segment-level signals derived from self-distillation. GEAR compares an on-policy student with a ground-truth-conditioned teacher to obtain a reference-guided divergence signal for identifying adaptive segment boundaries and modulating local advantage weights. This divergence often spikes at the onset of a semantic deviation, while later tokens in the same autoregressive continuation may return to low divergence. GEAR therefore treats such spikes as anchors for adaptive credit regions: where the student remains aligned with the teacher, token-level resolution is preserved; where it departs, GEAR groups the corresponding continuation into an adaptive segment and uses the divergence at the departure point to modulate the segment' s advantage. Experiments across eight mathematical reasoning and agentic tool-use benchmarks with Qwen3 4B and 8B models show that GEAR consistently outperforms standard GRPO, self-distillation-only baselines, and token- or turn-level credit-assignment methods. The gains are especially strong on benchmarks with lower GRPO baseline accuracy, reaching up to around 20\% over GRPO, suggesting that the proposed adaptive reweighting scheme is especially useful in more challenging long-horizon settings.

---

## 论文详细总结（自动生成）

### 论文总结：GEAR - 通过自我蒸馏实现的 LLM 智能体粒度自适应优势重加权

#### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）智能体的后训练中，强化学习（RL）通常依赖于**结果级奖励（Outcome-level Rewards）**。这种粗粒度的监督信号在面对长程（Long-horizon）任务（如多步数学推理或复杂的工具调用）时，存在严重的**信用分配（Credit Assignment）**难题：模型难以确定轨迹中哪些具体的 Token 或动作导致了最终的成功或失败。现有的细粒度方法要么依赖昂贵的步级人工标注，要么使用固定的划分单位（如 Token 或对话轮次），无法灵活捕捉语义层面的关键决策点。

#### 2. 方法论：核心思想与技术细节
GEAR 框架的核心思想是利用**自我蒸馏（Self-Distillation）**产生的信号，动态地重塑轨迹级的优势函数（Advantage），实现自适应粒度的信用分配。

*   **任务锚定的信用信号**：通过计算“在线策略学生模型”与“以地面真值（Ground-truth）为条件的教师模型”之间的**逆 KL 散度（Reverse KL Divergence）**。散度值的大小反映了学生模型偏离正确路径的程度。
*   **自适应粒度分段（Adaptive Segmentation）**：
    *   **触发点**：当逆 KL 散度出现激增（Spike）时，识别为语义偏离的起点（通常发生在“所以”、“但是”等逻辑转折词处）。
    *   **终止点**：利用 **Token 熵（Entropy）** 作为边界信号。当熵值显著增加（代表不确定性或语义转换完成）时，结束当前片段。
    *   **片段处理**：在偏离区域，将整个片段视为一个整体并共享偏离信号；在一致区域，则保留 Token 级的细粒度。
*   **优势重加权（Advantage Reweighting）**：
    *   在**成功轨迹**中，对高偏离（高 KL）的 Token/片段进行**降权**，防止模型强化不稳定的偏离行为。
    *   在**失败轨迹**中，对高偏离的 Token/片段进行**增权**，加大惩罚力度，引导模型纠正错误。
*   **算法流程**：在 GRPO 框架基础上，将原本统一的轨迹级优势 $A$ 乘以自适应权重 $W_t$，得到重加权后的优势 $\hat{A}_t$，用于策略更新。

#### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：GSM8K、MATH、MATH500、AIME 2024、AIME 2025。
    *   **智能体工具使用**：ToolSandbox（多工具调用）、BFCL（伯克利函数调用基准）、ACEBench（端到端成功率与过程准确率）。
*   **对比方法（Baselines）**：
    *   **GRPO**：标准的结果级强化学习方法。
    *   **OPSD / OPSD+RL**：纯自我蒸馏及其与 RL 结合的方法。
    *   **ARPO**：基于熵触发分支的改进方法。
    *   **MT-GRPO**：轮次级（Turn-level）信用分配方法。
*   **模型**：Qwen3-4B-Base 和 Qwen3-8B-Base。

#### 4. 资源与算力
*   **硬件**：使用了 **13 台 NVIDIA H100 GPU**（部分实验提到 8 台 H100）。
*   **训练时长**：
    *   数学推理任务：4B 模型训练一个 Epoch 约需 **26 小时**。
    *   工具使用任务：4B 模型训练一个 Epoch 约需 **10 小时**。
*   **效率**：计算开销与标准 GRPO 相当，因为不需要额外训练复杂的奖励模型（RM）。

#### 5. 实验数量与充分性
*   **实验规模**：在 8 个主流基准测试上进行了验证，涵盖了从基础数学到竞赛级数学，以及从简单函数调用到状态化环境的多种场景。
*   **消融实验**：非常充分。作者对比了“无片段重加权”、“无熵终止”、“无 KL 触发”、“固定工具调用分段”等多种变体，验证了动态分段机制的必要性。
*   **公平性**：所有方法均在相同的训练数据（5K 数学 + 4K 工具）和超参数框架下进行对比，并提供了跨领域泛化实验（混合训练），证明了方法的稳健性。

#### 6. 主要结论与发现
*   **性能提升显著**：GEAR 在所有基准上均优于 GRPO。在挑战性极高的 AIME 竞赛题上，相比 GRPO 提升了约 **10%-20%**。
*   **长程任务优势**：任务越复杂、轨迹越长，GEAR 的自适应信用分配效果越明显。
*   **泛化能力强**：跨领域训练的 GEAR 模型在未见过的任务上表现出更好的鲁棒性，说明它学到了更本质的推理逻辑而非简单的模式匹配。
*   **训练稳定性**：GEAR 在提升奖励的同时，保持了较高的策略熵，有效防止了策略过早坍缩。

#### 7. 优点
*   **无需额外标注**：巧妙利用自我蒸馏信号，避免了昂贵的步级过程奖励（PRM）标注。
*   **粒度自适应**：打破了固定单位（Token/Turn）的限制，通过 KL 和熵的组合动态捕捉语义块，更符合人类推理逻辑。
*   **即插即用**：作为一种优势重加权方案，可以轻松集成到现有的 RL 框架（如 GRPO、PPO）中。

#### 8. 不足与局限
*   **依赖地面真值**：GEAR 需要 Ground-truth 答案来构建教师模型，这限制了它在**开放式、无标准答案**任务（如创意写作、开放式对话）中的应用。
*   **超参数敏感性**：虽然论文给出了推荐值，但 KL 触发阈值 $\lambda_{KL}$ 和熵终止阈值 $\lambda_H$ 可能需要针对不同模型或任务进行微调。
*   **教师模型质量**：如果模型本身能力极弱，无法通过 Ground-truth 引导产生高质量的教师分布，则散度信号的可靠性会下降。

（完）
