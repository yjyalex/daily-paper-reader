---
title: "TokenRatio: Principled Token-Level Preference Optimization via Ratio Matching"
title_zh: TokenRatio：基于比例匹配的有原则的 Token 级偏好优化
authors: "Truong Nguyen, Tien-Phat Nguyen, Linh Ngo Van, Duy Minh Ho Nguyen, Khoa Doan, Trung Le"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12288v1"
tags: ["query:token"]
score: 10.0
evidence: 原则性的Token级偏好优化
tldr: 针对直接偏好优化（DPO）在处理序列级偏好时忽略逐标记决策过程的问题，本文提出了 Token-level Bregman Preference Optimization (TBPO)。该方法通过 Bregman 散度密度比匹配，将序列级比较转化为原则性的逐标记优化目标。TBPO 包含 TBPO-Q 和 TBPO-A 两种实现，在指令遵循和摘要等任务中显著提升了对齐质量、训练稳定性和输出多样性。
selection_source: fresh_fetch
motivation: 现有的 DPO 方法主要基于全序列偏好，未能充分利用生成过程中的逐标记决策信息，且现有标记级扩展缺乏明确的状态最优性保证。
method: 提出了 TBPO 框架，通过 Bregman 散度匹配密度比，并引入显式状态基准（TBPO-Q）或优势归一化（TBPO-A）来实现逐标记的偏好对齐。
result: 在指令遵循、安全性及摘要等多个基准测试中，TBPO 在对齐质量、训练稳定性和生成多样性方面均优于强序列级和标记级基线。
conclusion: TBPO 为标记级偏好优化提供了一种理论完备且简洁高效的方案，证明了在细粒度层面进行对齐能有效提升语言模型的性能。
---

## 摘要
直接偏好优化 (DPO) 是一种广泛使用的无需强化学习 (RL-free) 的方法，用于根据成对偏好对齐语言模型，但尽管生成是由逐 token 决策驱动的，它却是在完整序列上对偏好进行建模。现有的 token 级扩展通常将序列级 Bradley-Terry 目标分解到各个时间步，使得每个前缀（状态级）的最优性保持隐式。我们研究了如何仅使用标准的序列级成对比较来恢复 token 级的偏好最优性。我们引入了 Token 级 Bregman 偏好优化 (TBPO)，它在前缀条件下对下一 token 动作假设了一个 token 级的 Bradley-Terry 偏好模型，并推导出一个 Bregman 散度密度比匹配目标，该目标在保留 token 级模型诱导的最优策略并维持类似 DPO 的简洁性的同时，推广了逻辑/DPO 损失。我们引入了两种实例化方式：TBPO-Q，它显式地学习一个轻量级的状态基准 (state baseline)；以及 TBPO-A，它通过优势归一化 (advantage normalization) 消除了基准。在指令遵循、有用性/无害性以及摘要基准测试中，相比于强大的序列级和 token 级基线，TBPO 提高了对齐质量和训练稳定性，并增加了输出多样性。

## Abstract
Direct Preference Optimization (DPO) is a widely used RL-free method for aligning language models from pairwise preferences, but it models preferences over full sequences even though generation is driven by per-token decisions. Existing token-level extensions typically decompose a sequence-level Bradley-Terry objective across timesteps, leaving per-prefix (state-wise) optimality implicit. We study how to recover token-level preference optimality using only standard sequence-level pairwise comparisons. We introduce Token-level Bregman Preference Optimization (TBPO), which posits a token-level Bradley-Terry preference model over next-token actions conditioned on the prefix, and derive a Bregman-divergence density-ratio matching objective that generalizes the logistic/DPO loss while preserving the optimal policy induced by the token-level model and maintaining DPO-like simplicity. We introduce two instantiations: TBPO-Q, which explicitly learns a lightweight state baseline, and TBPO-A, which removes the baseline through advantage normalization. Across instruction following, helpfulness/harmlessness, and summarization benchmarks, TBPO improves alignment quality and training stability and increases output diversity relative to strong sequence-level and token-level baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《TokenRatio: Principled Token-Level Preference Optimization via Ratio Matching》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的直接偏好优化（DPO）及其变体主要在**序列级（Sequence-level）**上对人类偏好进行建模。然而，大语言模型（LLM）的生成过程是**逐标记（Token-level）**进行的。
*   **研究动机**：序列级目标忽略了生成过程中的局部决策，导致信用分配（Credit Assignment）不直接，且容易让早期的微小错误累积。
*   **核心挑战**：现有的标记级方法（如 TDPO）虽然将损失分解到了时间步，但其底层逻辑仍基于序列级的 Bradley-Terry 模型，使得“状态最优性”（State-wise Optimality）仅是隐式的。
*   **整体含义**：本文旨在建立一种有原则的、在每个生成状态下都显式追求最优的标记级偏好优化框架。

### 2. 方法论：核心思想与技术细节
论文提出了 **Token-level Bregman Preference Optimization (TBPO)** 框架：
*   **核心思想**：将偏好建模从全序列提升到“前缀+下一标记”的粒度。通过 Bregman 散度下的密度比匹配（Density Ratio Matching），直接从序列级偏好数据中恢复标记级的最优策略。
*   **关键技术实现**：
    *   **标记级 Bradley-Terry 模型**：定义在给定前缀 $s$ 下，选择动作 $a_w$ 优于 $a_l$ 的概率。
    *   **TBPO-Q**：使用状态-动作价值函数 $Q$ 作为评分函数。为了处理不同前缀带来的偏差，引入了一个轻量级的 MLP 头部作为**状态基准（State Baseline）**，用于预测配分函数的对数。
    *   **TBPO-A**：使用优势函数 $A$ 作为评分函数。通过**优势归一化**和 KL 散度估计器（K3 估计器）抵消状态项，无需额外学习基准头部。
    *   **Bregman 损失函数**：利用 Scaled Basu’s power divergence (SBA) 推广了 DPO 的逻辑损失，允许通过超参数 $\lambda$ 调节模型对噪声数据或不确定样本的关注度。

### 3. 实验设计
*   **模型底座**：Mistral 7B v0.1 和 Llama 3 8B。
*   **数据集**：UltraFeedback Binarized（训练）、Anthropic HH-RLHF（安全与有用性测试）、TL;DR（摘要任务，用于测试 OOD 泛化）。
*   **Benchmark**：
    *   **通用能力**：Open LLM Leaderboard（ARC, HellaSwag, MMLU, TruthfulQA, Winogrande, GSM8K）。
    *   **对齐质量**：MT-Bench（多轮对话）。
    *   **多样性**：预测熵（Predictive Entropy）、Self-BLEU、Distinct-1。
*   **对比方法**：SFT, DPO, TDPO, TIS-DPO, BPO-SBA。
*   **评估工具**：使用 Llama 3 70B, DeepSeek-V3, Mixtral-8x22B 作为 LLM 裁判。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H100 GPU**。
*   **配置**：
    *   基线模型（DPO/TDPO 等）使用 2 张 H100。
    *   TBPO 训练使用 **4 张 H100**。
*   **训练参数**：Batch size 为 32，学习率为 $5 \times 10^{-7}$，训练 1 个 epoch，最大序列长度 2048。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流开源模型系列，并在通用推理、指令遵循、安全性、摘要四个大类任务上进行了验证。
*   **充分性**：
    *   进行了**长度控制（Length-controlled）**的胜率分析，排除了模型通过变“啰嗦”来刷分的嫌疑。
    *   包含了**多样性分析**，证明了方法在对齐的同时不会导致模型输出坍缩。
    *   进行了 **OOD（领域外）实验**（在 UltraFeedback 上练，在 TL;DR 上测），验证了泛化性。
*   **客观性**：使用了多个不同的第三方 LLM 裁判，减少了单一裁判带来的偏见。

### 6. 主要结论与发现
*   **性能领先**：TBPO 在 Open LLM Leaderboard 上的平均分显著高于 DPO 和现有的标记级方法（如 TDPO）。
*   **帕累托改进**：TBPO 在保持更短输出长度的同时，获得了更高的胜率，打破了“越长越好”的偏见。
*   **多样性保持**：相比于 TIS-DPO 等方法，TBPO 在 Distinct-1 和预测熵指标上表现更好，生成的文本更具多样性且冗余度低。
*   **稳定性**：标记级的密度比匹配提供了更稳定的训练动态。

### 7. 优点
*   **理论完备**：基于 Bregman 散度提供了严谨的数学推导，证明了最小化该目标可恢复标记级最优策略。
*   **细粒度对齐**：解决了序列级 DPO 无法处理局部决策最优性的问题。
*   **灵活性**：TBPO-Q 和 TBPO-A 两种变体分别适应了显式基准学习和隐式优势归一化的需求。
*   **高效性**：虽然是标记级优化，但保持了类似 DPO 的简洁性，无需复杂的在线强化学习（PPO）。

### 8. 不足与局限
*   **计算开销**：TBPO-Q 需要额外训练一个 MLP 头部，虽然轻量，但增加了模型架构的复杂性。
*   **超参数敏感性**：Bregman 损失中的 $\lambda$ 和 $s$ 参数可能需要针对不同任务进行精细调优。
*   **裁判依赖**：实验高度依赖 LLM-as-a-judge，尽管使用了多个裁判，但仍可能受限于裁判模型本身的偏见。
*   **训练数据限制**：实验主要基于成对偏好数据，对于更复杂的排序数据或多维反馈的利用尚不明确。

（完）
