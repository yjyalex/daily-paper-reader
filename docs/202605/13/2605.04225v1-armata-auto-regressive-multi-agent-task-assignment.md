---
title: "ARMATA: Auto-Regressive Multi-Agent Task Assignment"
title_zh: ARMATA：自回归多智能体任务分配
authors: "Yazan Youssef, Aboelmagd Noureldin, Sidney Givigi"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04225v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 通过自回归框架进行多智能体任务分配与协调
tldr: "ARMATA是一个针对多智能体任务分配与路径规划的端到端自回归框架。它解决了传统方法中分配与路径规划脱节的问题，通过多阶段解码机制在单一自回归过程中同时生成分配决策和访问顺序。该方法在保持全局状态的同时平衡了工作负载与效率，相比传统求解器（如OR-Tools、CPLEX）在质量上提升了20%，且计算时间从小时级缩短至秒级。"
selection_source: fresh_fetch
motivation: 传统多智能体协调方法通常将区域分配与路径规划解耦，导致忽略了阶段间的依赖关系且缺乏全局上下文。
method: 提出一种集中式的全端到端自回归框架，利用多阶段解码机制在单一过程中统一处理高层分配与底层路径规划。
result: "实验表明该方法在解的质量上比Google OR-Tools和CPLEX等工业求解器提升了高达20%，并将计算时间从数小时缩短至数秒。"
conclusion: ARMATA通过联合优化分配与路径规划，在保证全局最优性的同时显著提升了多智能体系统的协调效率和求解速度。
---

## 摘要
在空间分布区域协调多智能体系统需要解决一个复杂的层级问题：首先在智能体之间分配区域（分配），随后确定最优访问顺序（路径规划）。现有方法通常将这些阶段解耦，忽略了阶段间的依赖关系，或者依赖于缺乏全局上下文的去中心化启发式方法。在这项工作中，我们提出了一个中心化的、完全端到端的自回归框架，用于联合生成分配决策和路径序列。我们方法的核心贡献是一个多阶段解码机制，它在保持中心化全局状态的同时，通过单次自回归过程统一了高层分配和底层路径规划。这使得模型能够隐式地平衡工作负载分布与路径规划效率，从而避免去中心化方法中常见的局部最优。广泛的实验表明，我们的方法显著优于各种基准方法，与 Google OR-Tools、IBM CPLEX 和 LKH-3 等工业求解器相比，解的质量提升了高达 20%，同时将计算时间从数小时缩短至数秒。

## Abstract
Coordinating multi-agent systems over spatially distributed areas requires solving a complex hierarchical problem: first distributing areas among agents (allocation) and subsequently determining the optimal visitation order (routing). Existing methods typically decouple these stages ignoring inter-stage dependencies or rely on decentralized heuristics that lack global context. In this work, we propose a centralized, fully end-to-end auto-regressive framework that jointly generates allocation decisions and routing sequences. The core contribution of our approach is a multi-stage decoding mechanism that unifies high-level allocation and low-level routing in a single autoregressive pass while maintaining a centralized global state. This enables the model to implicitly balance workload distribution with routing efficiency, avoiding local optima common in decentralized methods. Extensive experiments demonstrate that our method significantly outperforms diverse baselines, achieving up to a 20\% improvement in solution quality over industrial solvers such as Google OR-Tools, IBM CPLEX, and LKH-3, while reducing computation time from hours to seconds.

---

## 论文详细总结（自动生成）

### ARMATA: 自回归多智能体任务分配论文总结

#### 1. 核心问题与研究动机
论文关注的是**多智能体任务分配与路径规划（MATAR）**问题。在协调分布在空间中的多个智能体（如无人机、机器人）执行任务时，通常面临两个层级的挑战：
*   **分配（Allocation）**：决定哪个智能体负责哪些区域。
*   **路径规划（Routing）**：确定每个智能体访问其负责区域的最优顺序。

**研究动机**：传统方法（如精确数学求解器或启发式算法）通常将这两个阶段解耦，先分配再规划，这忽略了两者之间的紧密耦合关系，容易陷入局部最优。此外，精确求解器在处理大规模问题时存在指数级的时间复杂度，无法满足实时应用需求。

#### 2. 方法论：ARMATA 框架
ARMATA 提出了一种**中心化、全端到端的自回归框架**，旨在单次推理中同时解决分配和路径规划。

*   **核心思想**：利用深度强化学习（DRL）和神经组合优化（NCO），通过自回归方式序列化地生成决策。
*   **关键技术细节**：
    *   **双编码器结构**：
        *   **节点编码器（Nodes Encoder）**：使用门控图卷积网络（Gated GCN）提取待访问区域的几何特征和空间关系。
        *   **智能体编码器（Agents Encoder）**：同样使用 Gated GCN 捕捉智能体之间的相对位置和编队特征。
    *   **三阶段解码机制**：
        *   **网络 I（区域分配）**：基于当前全局状态和历史决策，为当前智能体选择下一个目标区域。
        *   **网络 II（起点选择）**：在选定区域内选择最优的进入点（如区域的某个角点）。
        *   **网络 III（扫描模式）**：决定在该区域内的移动模式（如扫描路径）。
    *   **训练算法**：采用 **REINFORCE** 策略梯度算法作为主要训练手段，并对比了 **PPO** 算法，以最小化所有智能体的总行驶距离为目标。

#### 3. 实验设计
*   **数据集/场景**：在单位正方形区域内随机生成智能体位置和待扫描区域。
*   **测试集划分**：
    *   **范围内（In-range）**：与训练规模相似（5智能体/40区域，8智能体/72区域）。
    *   **范围外（Out-of-range）**：用于测试泛化能力（4智能体/24区域，12智能体/180区域）。
*   **Benchmark 与对比方法**：
    *   **Google OR-Tools**：工业级启发式优化套件。
    *   **IBM CPLEX**：精确数学规划求解器。
    *   **LKH-3**：目前最先进的路径规划元启发式算法。

#### 4. 资源与算力
*   **硬件**：使用了一台配备 **NVIDIA GeForce RTX-3090 GPU** 和 **Intel i9 11900KF CPU** 的工作站。
*   **软件**：基于 PyTorch 2.1 和 OpenAI Gym 开发。
*   **训练时长**：模型训练了 100 个 epoch，每个 epoch 包含 128,000 个随机生成的样本。虽然未给出总训练小时数，但提到推理时间在秒级。

#### 5. 实验数量与充分性
*   **实验规模**：每轮测试包含 1280 个样本，涵盖了从 24 到 180 个区域的不同规模。
*   **消融实验**：
    *   对比了**固定起点 vs. 随机起点**对模型性能的影响。
    *   进行了**禁用智能体编码器**的消融实验，证明了捕捉智能体间空间关系的重要性（性能下降约 20%）。
*   **充分性评价**：实验设计较为全面，不仅对比了启发式和精确求解器，还验证了模型在未见过的更大规模问题上的泛化能力，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **解的质量**：ARMATA 在解的质量上比 LKH-3、CPLEX 和 OR-Tools 提升了 **14% - 26%**（最高达 35%）。
*   **计算效率**：ARMATA 实现了 **4 个数量级** 的加速。在 180 个区域的复杂场景下，CPLEX 需 400 秒以上，而 ARMATA 仅需 **0.0165 秒**。
*   **泛化能力**：模型在“范围外”的数据集上表现依然稳健，证明了其学习到了通用的分配与规划策略，而非简单的记忆。

#### 7. 优点
*   **端到端联合优化**：打破了分配与路由的壁垒，通过多阶段解码实现了真正的联合决策。
*   **实时性极强**：亚秒级的推理速度使其非常适合动态环境下的实时重规划。
*   **零样本泛化**：无需针对不同规模的问题重新训练，具有极高的灵活性。
*   **冲突规避**：自回归的序列决策机制天然避免了多个智能体被分配到同一任务的冲突。

#### 8. 不足与局限
*   **智能体顺序固定**：解码时智能体按固定顺序处理，可能限制了某些极端最优解的探索。
*   **场景单一性**：实验主要集中在区域扫描（Search and Rescue）场景，对于更复杂的动态障碍物或异构任务（如不同类型的任务需求）的覆盖较少。
*   **中心化依赖**：虽然训练和决策是中心化的，但在通信受限的极端去中心化环境下，该框架的应用可能受限。

（完）
