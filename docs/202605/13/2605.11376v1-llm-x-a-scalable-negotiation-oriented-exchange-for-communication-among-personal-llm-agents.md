---
title: "LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents"
title_zh: LLM-X：一种面向个人 LLM 智能体间通信的可扩展协商型交换机制
authors: "Giuliano Lorenzoni, Paulo Alencar, Donald Cowan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11376v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 面向个人大模型智能体通信的协商导向交换机制
tldr: 本文提出了LLM-X，一个专为个人LLM智能体设计的可扩展协商通信框架。该框架通过消息总线和路由机制实现了智能体间的直接结构化协作，填补了现有协议在智能体间复杂协商支持上的空白。研究贡献了包含联邦网关和策略执行的系统架构，并通过大规模多智能体实验验证了其在不同策略下的性能权衡与长期运行的稳定性。
selection_source: fresh_fetch
motivation: 现有的智能体协议多侧重于智能体与API的交互，缺乏支持大规模个人智能体之间进行直接、结构化协商和策略约束的专用通信环境。
method: 设计了包含联邦网关、主题路由和策略执行的LLM-X架构，并引入支持能力协商与合同网协调的类型化消息协议。
result: 实验显示严格的策略能提升公平性但会增加延迟，且系统在长达12小时的持续负载下保持了稳定的性能和受控的延迟漂移。
conclusion: LLM-X为大规模多智能体协商提供了稳健的基础设施，并揭示了协商策略、系统鲁棒性与运行效率之间的关键权衡关系。
---

## 摘要
我们提出了个人 LLM 交换机制（LLM-X），这是一种可扩展的、面向协商的环境，能够实现代表个人用户的多个个人智能体（LLM）群体之间的直接、结构化通信。与现有的侧重于智能体与 API 交互的以工具为中心的协议不同，LLM-X 引入了消息总线和路由底层架构，用于 LLM 之间的协作，并提供模式有效性和策略执行方面的保障。我们的贡献包括：(1) 一种包含联邦网关、基于主题的路由和策略执行的 LLM-X 架构；(2) 一种支持能力协商和合同网式协作的有类型消息协议；(3) 对大规模基于 LLM 的多智能体协商进行的首次实证评估。实验涵盖了 5、9 和 12 个智能体，在不同的协商策略（低、中、高）以及短期（分钟级）和长期（2小时、12小时）负载条件下进行。结果凸显了明显的策略-性能权衡：更严格的策略提高了鲁棒性和公平性，但增加了延迟和消息量。长期运行实验证实，LLM-X 在持续负载下保持稳定，且延迟漂移在有限范围内。

## Abstract
We propose a personal-LLM exchange (LLM-X), a scalable negotiation-oriented environment that enables direct, structured communication across populations of personal agents (LLMs), each representing an individual user. Unlike existing tool-centric protocols that focus on agent-API interaction, LLM-X introduces a message bus and routing substrate for LLM-to-LLM coordination with guarantees around schema validity and policy enforcement. We contribute: (1) an architecture for LLM-X comprising federated gateways, topic-based routing, and policy enforcement; (2) a typed message protocol supporting capability negotiation and contract-net-style coordination; and (3) the first empirical evaluation of LLM-based multi-agent negotiation at scale. Experiments span 5, 9, and 12 agents, under distinct negotiation policies (Low, Medium, High), and across both short-run (minutes) and long-run (2h, 12h) load conditions. Results highlight clear policy-performance trade-offs: stricter policies improve robustness and fairness but increase latencies and message volume. Extended runs confirm that LLM-X remains stable under sustained load, with bounded latency drift.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LLM-X** 的新型基础设施，旨在解决个人大模型智能体（Personal LLM Agents）之间缺乏标准化、可扩展且受控的通信机制的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 LLM 已广泛用作个人助手，但它们通常处于孤立状态，缺乏直接的智能体间（Agent-to-Agent）协作机制。
*   **核心问题**：现有的协议（如 MCP）多侧重于智能体与 API 或工具的交互，而非智能体之间的复杂协商。在大规模、异构 LLM 环境下，缺乏一个既能保证消息结构合法性，又能执行安全与公平策略的通信底层架构。
*   **整体含义**：LLM-X 提供了一个可扩展的协商导向交换环境，允许代表不同用户的智能体通过结构化协议进行直接对话、规划和达成共识。

### 2. 方法论
*   **核心思想**：构建一个基于消息总线和路由底层的交换系统，通过“联邦网关”和“策略引擎”实现类型化、受控的智能体间协商。
*   **关键技术细节**：
    *   **架构组成**：包括轻量级网关（负责 JWT 认证、模式验证、限流）、控制平面（管理注册与能力协商）和观测组件。
    *   **类型化协议**：使用 JSON Schema 定义消息信封，支持 CFP（招标）、Offer（报价）、Accept/Reject（接受/拒绝）和 Ack（确认）等动作。
    *   **协商模式**：支持经典的合同网（ContractNet）协议和 FIPA 交替报价标准。
    *   **策略引擎**：定义了三种决策策略：
        *   **Low（低）**：在超时前关闭，存在错过报价的风险。
        *   **Medium（中）**：一旦收到第一个有效报价即达成交易并关闭。
        *   **High（高）**：必须收集所有报价后才进行决策，确保公平与完整。

### 3. 实验设计
*   **实验场景**：模拟 5、9、12 个异构智能体在不同负载下的协商过程。
*   **对比维度**：
    *   **策略对比**：Low vs. Medium vs. High 策略对性能的影响。
    *   **规模对比**：不同智能体数量下的系统表现。
    *   **时长对比**：短期运行（2分钟）与长期运行（2小时、12小时）。
*   **Benchmark/指标**：吞吐量（每分钟消息数）、延迟（p50 和 p95 延迟）、轮次完成率、每轮报价分布。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **实现细节**：实验中的智能体采用 Python 包装器实现，以确保实验的可控性和可重复性。系统底层使用 NATS 消息中间件进行传输。虽然实验使用了模拟负载，但架构设计上完全兼容 OpenAI、Anthropic 等主流 LLM API。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组 2 分钟的短期实验（每组重复 3 次以确保一致性），以及针对 12 个智能体的高负载 2 小时和 12 小时压力测试。
*   **充分性评价**：实验设计较为充分，涵盖了从规模扩展性到长期稳定性的多个维度。通过对比不同策略下的延迟漂移和消息量，客观地揭示了系统在持续负载下的行为特征。

### 6. 主要结论与发现
*   **策略-性能权衡**：严格的策略（High）能显著提高协商的公平性和鲁棒性，但会带来更高的延迟和更大的消息通信开销。
*   **线性扩展性**：随着智能体数量增加（5 到 12 个），消息流量呈近线性增长，而延迟退化保持在可控范围内（毫秒级）。
*   **长期稳定性**：在 12 小时的持续运行中，系统未出现明显的延迟漂移或吞吐量饱和，证明了异步网关架构的稳健性。

### 7. 优点
*   **解耦设计**：将智能体的推理逻辑与通信协议解耦，使得 LLM-X 成为一个通用的、插件式的多智能体协作底层。
*   **安全性与合规性**：通过网关层的模式验证和策略强制执行，有效防止了格式错误的消息传播，并为缓解提示词注入（Prompt Injection）提供了结构化屏障。
*   **现实兼容性**：协议设计直接支持 JSON 负载，方便集成现有的商业 LLM API。

### 8. 不足与局限
*   **智能体实现偏差**：实验主要基于 Python 脚本模拟的智能体行为，虽然协议兼容真实 LLM，但尚未在大规模真实 LLM API 调用下进行端到端的成本和复杂推理延迟评估。
*   **规模上限**：实验最高仅测试了 12 个智能体，对于成百上千个智能体的大规模群体动态，其路由和策略执行的瓶颈尚不明确。
*   **对抗性测试不足**：虽然提到了安全机制，但缺乏针对恶意智能体试图绕过协议或进行复杂博弈攻击的深度防御实验。

（完）
