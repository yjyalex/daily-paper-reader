---
title: "Collective Alignment in LLM Multi-Agent Systems: Disentangling Bias from Cooperation via Statistical Physics"
title_zh: 大语言模型多智能体系统中的集体对齐：通过统计物理学将偏置与协作解耦
authors: Cristiano De Nobili
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10528v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 大模型多智能体系统的集体动力学
tldr: 本研究利用统计物理方法探讨了大语言模型（LLM）多智能体系统在2D晶格上的集体对齐行为。通过将智能体状态映射为二元变量并调节采样温度，研究者成功分离了社会从众性与内在偏见。实验分析了llama3.1、phi4-mini和mistral模型，发现其集体行为主要受内在偏见驱动而非邻居间的协作耦合，表现为场驱动的跨越而非真正的相变。该方法为评估多智能体共识的可靠性提供了定量诊断工具。
selection_source: fresh_fetch
motivation: 旨在通过统计物理框架量化区分LLM多智能体系统中的社会从众性与内在偏见，以理解集体对齐的动力学机制。
method: 在2D方格上部署相同LLM智能体，通过调节采样温度并观察基于邻居状态的二元决策更新，测量磁化率和易受性。
result: 所有测试模型均显示出温度驱动的有序-无序跨越，且其有效临界指数与2D伊辛模型不同，且内在偏见显著强于协作耦合。
conclusion: LLM多智能体的集体一致性主要源于模型固有的偏见而非智能体间的相互作用，这为评估多智能体系统的共识可靠性提供了新视角。
---

## 摘要
我们研究了二维方格点阵上基于大语言模型（LLM）的多智能体系统的涌现集体动力学，并提出了一种与模型无关的统计物理方法，用于将社会从众性与内在偏置解耦，计算临界指数，并探测多智能体系统的集体行为及可能的相变。在我们的框架中，$L\!\times\!L$ 点阵的每个节点都托管一个相同的 LLM 智能体，该智能体持有二值状态（$+1$/$-1$，映射为“是”/“否”），并根据四个最近邻状态作为条件查询模型来更新其状态。采样器温度 $T$ 作为唯一的控制参数。在三个开源权重模型（llama3.1:8b, phi4-mini:3.8b, mistral:7b）中，我们测量了旨在探测 $\mathbb{Z}_2$ 对称性的全局翻转协议下的磁化强度和磁化率。所有模型都表现出温度驱动的有序-无序交叉（crossovers）和磁化率峰值；在偶数 $L$ 点阵上的有限尺寸标度分析得出了模型相关的有效指数 $\gamma/\nu$，其数值接近但与二维伊辛（Ising）普适类（$\gamma/\nu=7/4$）不相容。我们的方法能够提取有效的 $\beta$ 加权耦合 $\tilde{J}(T)$ 和场 $\tilde{h}(T)$，它们可作为社会从众性和内在偏置的度量。在我们分析的模型中，我们发现集体对齐主要由内在偏置主导（$\tilde{h}\gg\tilde{J}$），而非由协作性的邻居耦合主导，从而产生场驱动的交叉而非真正的相变。这些有效参数在不同模型间存在定性差异，为 LLM 智能体提供了紧凑的集体行为指纹，并为多智能体共识和集体对齐的可靠性提供了定量诊断。

## Abstract
We investigate the emergent collective dynamics of LLM-based multi-agent systems on a 2D square lattice and present a model-agnostic statistical-physics method to disentangle social conformity from intrinsic bias, compute critical exponents, and probe the collective behavior and possible phase transitions of multi-agent systems. In our framework, each node of an $L\!\times\!L$ lattice hosts an identical LLM agent holding a binary state ($+1$/$-1$, mapped to yes/no) and updating it by querying the model conditioned on the four nearest-neighbor states. The sampler temperature $T$ serves as the sole control parameter. Across three open-weight models (llama3.1:8b, phi4-mini:3.8b, mistral:7b), we measure magnetization and susceptibility under a global-flip protocol designed to probe $\mathbb{Z}_2$ symmetry. All models display temperature-driven order-disorder crossovers and susceptibility peaks; finite-size scaling on even-$L$ lattices yields effective exponents $γ/ν$ whose values are model-dependent, close to but incompatible with the 2D Ising universality class ($γ/ν=7/4$). Our method enables the extraction of effective $β$-weighted couplings $\tilde{J}(T)$ and fields $\tilde{h}(T)$, which serve as a measure of social conformity and intrinsic bias. In the models we analyzed, we found that collective alignment is dominated by an intrinsic bias ($\tilde{h}\gg\tilde{J}$) rather than by cooperative neighbor coupling, producing field-driven crossovers instead of genuine phase transitions. These effective parameters vary qualitatively across models, providing compact collective-behavior fingerprints for LLM agents and a quantitative diagnostic for the reliability of multi-agent consensus and collective alignment.

---

## 论文详细总结（自动生成）

这篇论文通过统计物理学的视角，深入探讨了大语言模型（LLM）多智能体系统在集体决策中的动力学行为。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当多个 LLM 智能体达成共识时，这种“对齐”究竟是源于智能体之间真正的**协作与社会从众性**（Social Conformity），还是仅仅因为它们共享了底层的**内在偏见**（Intrinsic Bias）？
*   **研究动机**：随着多智能体系统（MAS）在辩论、决策和内容审核等领域的广泛应用，理解其共识的来源至关重要。如果共识仅由偏见驱动，那么增加智能体数量并不能提高决策的可靠性，反而会放大错误。

### 2. 方法论
*   **核心思想**：将 LLM 智能体放置在二维方格点阵（2D Lattice）上，模拟统计物理中的伊辛模型（Ising Model）。每个节点是一个智能体，其状态为二元（是/否）。
*   **关键技术细节**：
    *   **状态更新**：采用异步蒙特卡洛协议。随机选择一个智能体，根据其四个最近邻的状态生成提示词（Prompt），询问其意向状态。
    *   **控制参数**：采样温度 $T$ 是唯一的外部控制变量，用于调节决策的随机性。
    *   **全局翻转协议**：为了消除模型对特定标签（如更倾向于说“yes”）的固有偏好，研究者交替交换提示词中的标签含义，以探测系统的 $\mathbb{Z}_2$ 对称性。
    *   **参数提取**：通过对更新日志进行逻辑回归，提取两个关键参数：
        *   **有效耦合 $\tilde{J}$**：衡量邻居状态对智能体的影响（社会从众性）。
        *   **有效场 $\tilde{h}$**：衡量智能体固有的标签偏好（内在偏见）。
*   **公式模型**：$\text{logit} P(s'_i = +1 | k) \approx 2(\tilde{h} + \tilde{J}k)$，其中 $k$ 是邻居状态之和。

### 3. 实验设计
*   **实验对象**：三个开源模型：`llama3.1:8b`、`phi4-mini:3.8b` 和 `mistral:7b`。
*   **场景设置**：$L \times L$ 的方格点阵，具有周期性边界条件。
*   **对比基准**：经典的 **2D 伊辛模型普适类**（其理论临界指数 $\gamma/\nu = 1.75$）。
*   **测量指标**：磁化强度 $m(T)$（衡量对齐程度）、磁化率 $\chi(T)$（衡量系统对扰动的敏感度）以及有限尺寸标度（Finite-size scaling）。

### 4. 资源与算力
*   **硬件**：所有生产运行均在 **NVIDIA RTX 5090 GPU** 上执行，以确保硬件层面的随机性一致性（避免不同架构浮点运算差异的影响）。
*   **算力消耗**：实验规模巨大，总计进行了约 **10亿至30亿次** LLM 推理调用。这是限制晶格尺寸（$L \le 35$）的主要原因。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多个晶格尺寸（$L=10$ 到 $35$）、广泛的温度区间（$0.05$ 到 $3.2$）。
*   **重复性**：每个数据点至少使用 6 个独立种子进行平均，并区分了全局翻转扇区。
*   **充分性与客观性**：实验设计严谨，采用了物理学中标准的有限尺寸标度分析。虽然受限于算力，晶格尺寸跨度仅约半个数量级，但对于揭示模型间的定性差异已足够充分。

### 6. 主要结论与发现
*   **偏见主导共识**：在所有测试模型中，**$\tilde{h} \gg \tilde{J}$**。这意味着集体对齐主要由模型固有的偏见驱动，而非智能体间的相互影响。
*   **非伊辛普适性**：各模型的有效临界指数 $\gamma/\nu$ 各不相同（llama3.1 约为 1.02，phi4-mini 约为 1.75，mistral 约为 2.01），且均不符合真正的伊辛相变特征。
*   **模型指纹**：
    *   `llama3.1`：表现出最强的“社会性”（$\tilde{J}$ 最高），但仍被偏见主导。
    *   `phi4-mini`：几乎没有社会耦合，其看似符合伊辛指数的表现只是场衰减过快导致的巧合。
    *   `mistral`：偏见极强且持久，即使在高随机性下也难以消除。
*   **非平衡态特征**：观察到了晶格奇偶效应，表明 LLM 的更新过程不满足详细平衡（Detailed Balance），属于非平衡态动力学。

### 7. 优点
*   **模型无关性**：该方法不需要访问模型的权重或 Logits，仅凭文本输出日志即可分析，具有极强的通用性。
*   **跨学科创新**：成功将凝聚态物理的严谨工具引入 LLM 评估，提供了量化“社会行为”的新维度。
*   **诊断价值**：提出的 $\tilde{h}/\tilde{J}$ 比率可作为多智能体系统部署前的“共识可靠性”诊断工具。

### 8. 不足与局限
*   **算力瓶颈**：由于推理成本极高，无法测试更大的晶格尺寸，这限制了临界指数计算的精度。
*   **提示词敏感性**：实验结果可能高度依赖于具体的提示词表述，不同的 Prompt 可能会改变 $\tilde{h}$ 和 $\tilde{J}$ 的平衡。
*   **状态空间单一**：目前仅限于二元决策（是/否），尚未扩展到更复杂的连续或多元决策空间。
*   **应用限制**：研究主要针对相同模型的副本，未探讨异构智能体（不同模型混合）的集体行为。

（完）
