---
title: "AgentSlimming: Towards Efficient and Cost-Aware Multi-Agent Systems"
title_zh: AgentSlimming：迈向高效且具备成本意识的多智能体系统
authors: "Yulang Chen, Haoxuan Peng, Jinyan Liu, Zichen Wen, Dongrui Liu, Linfeng Zhang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08813v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 高效且具备成本意识的多智能体协作框架
tldr: "针对基于大语言模型的多智能体系统（MAS）中存在的结构冗余和高昂 Token 成本问题，本文提出了 AgentSlimming 压缩框架。该框架借鉴神经网络的剪枝与量化思想，通过混合机制评估智能体重要性，移除冗余节点或替换为低成本模型，并利用基准锚定规则确保性能。实验表明，该方法在几乎不损失性能的情况下，最高可降低 78.9% 的成本，实现了效率与质量的平衡。"
selection_source: fresh_fetch
motivation: 现有自动构建的多智能体系统往往结构冗余且 Token 消耗巨大，缺乏高效且自动化的成本优化方案。
method: 提出一种即插即用的压缩框架，通过评估智能体重要性来执行剪枝（删除冗余）或量化（替换低成本模型）操作。
result: "实验显示该方法最高可减少 78.9% 的 Token 消耗，且在维持性能的同时甚至能提升部分任务的准确率。"
conclusion: AgentSlimming 为构建高效、经济的图结构多智能体工作流提供了一种强有力的帕累托最优权衡方案。
---

## 摘要
基于大语言模型的多智能体系统（MAS）在处理复杂任务方面展现出了卓越的能力。然而，手动设计最优通信拓扑不仅耗费人力，且自动扩展方法往往会导致结构臃肿并产生冗余智能体，从而导致过度的 Token 消耗。为解决这一问题，我们提出了 AgentSlimming，这是一种用于图结构多智能体工作流的即插即用压缩框架。受神经网络剪枝和量化的启发，AgentSlimming 通过混合机制首先估计每个智能体的重要性得分，进而压缩工作流，随后移除冗余智能体或将其替换为低成本智能体。每项操作均通过基于基准锚定的接受规则进行验证，以防止性能崩溃。实验结果表明，AgentSlimming 在性能损失极小的情况下，可将平均 Token 成本降低高达 78.9%，有时甚至能提升准确率，在成本与质量之间实现了显著的帕累托最优权衡。我们的代码已公开发布于 https://github.com/CitrusYL/AgentSlimming。

## Abstract
Large Language Model-based Multi-Agent Systems (MAS) have demonstrated remarkable capabilities in complex tasks. However, manually designing optimal communication topologies is labor-intensive, while automated expansion methods often result in bloated structures with redundant agents, leading to excessive token consumption. To address this problem, we introduce \textbf{AgentSlimming}, a plug-and-play compression framework for graph-structured multi-agent workflows. Motivated by pruning and quantization in neural networks, AgentSlimming compresses workflows by first estimating the importance score of each agent with a hybrid mechanism, and then removes redundant agents or replaces them with low-cost ones, where each operation is validated using a baseline-anchored acceptance rule to prevent performance collapse. Experiments show that AgentSlimming reduces average token cost by up to 78.9\% with negligible performance degradation, and sometimes even improves accuracy, achieving a strong Pareto-optimal trade-off between cost and quality. \textit{Our code is publicly available at https://github.com/CitrusYL/AgentSlimming

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentSlimming: Towards Efficient and Cost-Aware Multi-Agent Systems》的深度结构化总结：

### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）的发展，多智能体系统（MAS）在处理复杂任务时表现卓越。然而，现有的 MAS 构建方式存在两大痛点：
*   **结构冗余：** 手动设计的拓扑结构往往非最优，而自动生成的系统（如通过自动扩展方法）通常会产生臃肿的图结构，包含大量冗余智能体。
*   **成本高昂：** 复杂的通信拓扑导致 Token 消耗呈指数级增长，极大地限制了 MAS 的大规模应用和经济可行性。

本文提出了 **AgentSlimming**，这是首个针对图结构多智能体工作流的压缩框架，旨在通过“剪枝”和“量化”手段，在保持性能的同时大幅降低运行成本。

### 2. 论文提出的方法论
AgentSlimming 借鉴了神经网络压缩的思想，将其应用于 MAS 的拓扑优化。核心流程包括：
*   **重要性评估（Importance Estimation）：** 采用混合机制评估每个智能体在工作流中的贡献。
    *   **自评估：** 利用 LLM 对智能体在历史任务中的表现进行反思和打分。
    *   **图特征分析：** 结合图论指标（如入度、出度、介数中心性等）衡量智能体在信息流中的核心地位。
*   **智能体剪枝（Agent Pruning）：** 移除重要性得分低于阈值的冗余智能体，并重新连接受影响的通信路径，以简化拓扑。
*   **智能体量化（Agent Quantization）：** 对于重要性较低但不可或缺的智能体，将其使用的高成本模型（如 GPT-4o）替换为低成本模型（如 GPT-4o-mini 或本地小模型）。
*   **基准锚定接受规则（Baseline-anchored Acceptance Rule）：** 在压缩后，使用一小部分验证集进行性能测试。只有当压缩后的系统性能保持在原始基准的预设阈值内时，才接受该压缩方案，防止性能崩溃。

### 3. 实验设计
*   **数据集与场景：** 涵盖了多种复杂任务，包括 **GAIA**（通用 AI 助手任务）、**HumanEval**（代码生成）、**MMLU**（多任务语言理解）以及 **Math**（数学推理）。
*   **Benchmark 与对比方法：**
    *   对比了原始未压缩的 MAS 架构（如 AutoGPT、MetaGPT 等思路构建的复杂流）。
    *   对比了随机剪枝（Random Pruning）和启发式简化方法。
    *   评估指标包括：任务准确率（Accuracy）、Token 消耗量（Cost）以及两者的帕累托前沿（Pareto Front）。

### 4. 资源与算力
*   **模型使用：** 实验主要基于 OpenAI 的 API（如 GPT-4o, GPT-4o-mini）进行。
*   **算力说明：** 论文未详细列出具体的 GPU 硬件型号（如 A100 数量），因为该框架主要针对推理阶段的拓扑优化，而非模型训练。主要的资源消耗体现在调用 LLM API 的 Token 费用上。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个主流基准数据集上进行了广泛测试，涵盖了从逻辑推理到代码编写的不同维度。
*   **消融实验：** 进行了充分的消融实验，验证了“仅剪枝”、“仅量化”以及“混合压缩”的效果，并探讨了不同重要性评估指标对最终结果的影响。
*   **客观性：** 通过在多个不同类型的 MAS 初始架构上应用 AgentSlimming，证明了该方法的通用性和鲁棒性。

### 6. 论文的主要结论与发现
*   **显著降本：** 在几乎不损失性能的前提下，AgentSlimming 平均可降低 **78.9%** 的 Token 成本。
*   **性能提升（去噪效应）：** 在某些情况下，移除冗余智能体反而提升了准确率。这表明复杂的 MAS 结构可能引入“噪声”或干扰信息，简化结构有助于模型聚焦核心逻辑。
*   **帕累托最优：** 该方法成功在“成本”与“质量”之间找到了更优的平衡点，证明了 MAS 压缩的可行性。

### 7. 优点
*   **即插即用：** 作为一个通用的压缩框架，它可以应用于任何基于图结构的 MAS，无需重新训练模型。
*   **跨领域借鉴：** 巧妙地将经典神经网络优化概念（剪枝、量化）迁移到智能体协作领域，具有很强的启发性。
*   **自动化程度高：** 减少了人工设计高效拓扑的负担。

### 8. 不足与局限
*   **验证集依赖：** 压缩过程依赖于一小部分验证数据，如果验证数据与实际测试数据分布偏差较大，可能会导致性能下降。
*   **静态压缩：** 目前的方法主要是在任务执行前进行静态压缩，尚未实现根据任务复杂度动态调整结构的“动态压缩”。
*   **模型多样性限制：** 实验主要集中在 OpenAI 系列模型，对于更多开源模型（如 Llama, Qwen）在量化过程中的表现覆盖尚可进一步加强。

（完）
