---
title: "Taming Extreme Tokens: Covariance-Aware GRPO with Gaussian-Kernel Advantage Reweighting"
title_zh: 驯服极端 Token：基于高斯核优势重加权的协方差感知 GRPO
authors: "Cheng Wang, Qin Liu, Wenxuan Zhou, Muhao Chen"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11538v1"
tags: ["query:token"]
score: 10.0
evidence: GRPO 推理中的 Token 级更新
tldr: 本研究针对大语言模型推理优化中的 GRPO 算法在探索与利用平衡上的不足，提出了一种基于协方差感知的改进方法。通过理论分析发现熵的变化受 token 概率与优势值协方差的影响，进而引入高斯核函数对极端 token 更新进行动态降权。该方法无需超参数，能有效减少训练不稳定性并稳定熵值，在多个推理基准测试中显著提升了模型性能。
selection_source: fresh_fetch
motivation: 现有的 GRPO 算法在训练过程中难以平衡探索与利用，导致极端 token 更新引发训练不稳定和性能受限。
method: 提出一种无超参数的协方差加权优化方法，利用高斯核函数动态降低极端 token 级更新的权重。
result: 实验表明，该方法在多个推理基准测试上优于原始 GRPO，并能在训练过程中有效稳定策略熵。
conclusion: 通过协方差感知的优势重加权，可以有效缓解强化学习中的不稳定性，提升大语言模型的推理能力。
---

## 摘要
组相对策略优化 (Group Relative Policy Optimization, GRPO) 已成为提升大语言模型推理能力的一种极具前景的方法。然而，它在训练过程中难以有效平衡探索与利用之间的权衡，往往导致性能欠佳。受“熵的变化受 Token 概率与其对应优势之间的协方差支配”这一理论洞察的启发，我们提出了一种无超参数的协方差加权优化方法，该方法通过高斯核动态降低极端 Token 级更新的权重。这种方法在保留信息丰富的学习信号的同时，自动减少了由探索-利用权衡引起的不稳定性。广泛的实证评估表明，与 GRPO 相比，我们的方法提升了各推理基准测试的下游性能，并随着训练的进行有效稳定了熵。

## Abstract
Group Relative Policy Optimization (GRPO) has emerged as a promising approach for improving the reasoning capabilities of large language models. However, it struggles to effectively balance the tradeoff between exploration and exploitation during training, often resulting in suboptimal performance. Motivated by the theoretical insight that changes in entropy are governed by the covariance between token probabilities and their corresponding advantages, we propose a hyperparameter-free, covariance-weighted optimization method that dynamically down-weights extreme token-level updates via a Gaussian kernel. This approach automatically reduces the instability caused by exploration-exploitation trade-off while preserving informative learning signals. Extensive empirical evaluations show that our approach improves downstream performance across reasoning benchmarks compared with GRPO, and effectively stablizes entropy as training progresses.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在强化学习训练中的稳定性问题，提出了一种改进的 GRPO 算法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：组相对策略优化（GRPO）在提升模型推理能力时，难以平衡**探索（Exploration）**与**利用（Exploitation）**。
*   **研究动机**：
    *   过度利用会导致模型对次优解过分自信，限制其发现更优推理路径的能力。
    *   过度探索则会导致训练动态不稳定，难以收敛。
    *   **理论发现**：策略熵的变化由 Token 对数概率与对应优势值（Advantage）之间的**协方差**决定。作者观察到，极少数具有极端协方差值的 Token 主导了策略更新，导致熵值剧烈波动和性能下降。

### 2. 方法论
*   **核心思想**：引入一种**协方差感知（Covariance-Aware）**的优势重加权机制，通过高斯核函数动态抑制极端 Token 的更新权重。
*   **关键技术细节**：
    1.  **协方差计算**：计算每个 Token 的中心化对数概率与中心化优势值之间的协方差 $c_{i,t}$。
    2.  **高斯核重加权**：使用高斯核函数 $w_{i,t} = \exp(-c_{i,t}^2 / 2\sigma^2)$ 计算权重，其中 $\sigma$ 是协方差的经验标准差。
    3.  **动态降权**：该机制会自动对偏离正常范围（高协方差）的 Token 进行指数级降权，而保留中等协方差 Token 的学习信号。
    4.  **无超参数**：由于 $\sigma$ 是根据当前数据动态计算的，该方法不需要手动调节超参数。
    5.  **归一化**：对权重进行归一化处理，以保持总体的损失期望不变。

### 3. 实验设计
*   **模型对象**：
    *   1.5B 规模：DeepSeek-R1-Distill-Qwen-1.5B。
    *   7B 规模：Qwen2.5-Math-7B-Instruct。
*   **数据集**：
    *   **训练集**：Open-RS（包含 7k 个高质量数学问题）。
    *   **评估集（Benchmark）**：AIME24、MATH-500、AMC23、Minerva、OlympiadBench。
*   **对比方法**：Vanilla GRPO（原始 GRPO）、Clip-Cov（另一种基于剪切的协方差干预方法）。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA H200 GPU** 进行训练和评估。
*   **训练配置**：学习率 1e-6，Batch Size 为 12，训练步数为 100 步，最大生成长度为 4096。
*   **软件框架**：基于 HuggingFace 的 `trl` 训练框架和 `lighteval` 评估框架。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流参数规模（1.5B 和 7B）的模型，并在 5 个权威数学推理基准上进行了测试。
*   **消融与分析**：
    *   对比了不同训练步数下的性能变化（验证了熵稳定与性能的关系）。
    *   统计了协方差值的分布情况，证明了“极端 Token”的存在及其对更新的贡献。
*   **充分性评价**：实验设计较为充分，不仅关注最终分数，还深入分析了训练过程中的熵动力学，对比了现有的改进方案（如 Clip-Cov），结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：CW-GRPO 在所有基准测试中均优于原始 GRPO。在 1.5B 模型上平均提升了 2.7 分，在 7B 模型上平均提升了 2.4 分。
*   **熵稳定性**：原始 GRPO 在训练后期会出现熵的不稳定（剧烈波动或下降），而 CW-GRPO 能将熵维持在合理水平，有效平衡了探索与利用。
*   **鲁棒性**：实验证明，稳定的熵直接关联到模型在不同训练阶段性能的一致性，避免了训练后期的性能退化。

### 7. 优点
*   **理论支撑扎实**：基于自然策略梯度下熵变化的数学推导，针对性地解决协方差异常问题。
*   **实现简单且高效**：作为一种重加权方案，易于集成到现有的 RL 框架中，且无需繁琐的超参数调优。
*   **即插即用**：在不同规模的模型上表现出一致的改进效果。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅覆盖到 7B 参数模型，尚未在更大规模（如 70B 或以上）的模型上验证其扩展性。
*   **任务领域单一**：主要集中在数学推理任务，虽然数学任务是检验 GRPO 的标准场景，但该方法在代码生成、通用对话或创意写作等其他强化学习场景下的表现尚待观察。
*   **训练步数较短**：实验中的训练步数（100-200步）相对较短，长期训练下的稳定性仍需进一步验证。

（完）
