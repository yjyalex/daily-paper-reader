---
title: Extending Confidence-Based Text2Cypher with Grammar and Schema Aware Filtering
title_zh: 通过语法和模式感知过滤扩展基于置信度的 Text2Cypher
authors: Makbule Gulcin Ozsoy
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10318v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 在LLM查询中强制执行结构化约束和模式一致性
tldr: 针对大型语言模型在生成图数据库查询语言（Text2Cypher）时缺乏结构约束的问题，本文提出了一种结合置信度评分、语法验证和模式约束的后生成过滤框架。该方法在推理阶段对生成的查询进行顺序过滤，旨在提高查询的语法正确性和模式一致性。研究表明，引入简单的结构化检查能显著提升Text2Cypher生成的可靠性，并揭示了不同约束对生成质量的影响。
selection_source: fresh_fetch
motivation: 现有的Text2Cypher方法往往忽略了语法和模式等结构化约束，导致生成的查询在实际数据库中难以执行。
method: 扩展了基于置信度的推理框架，通过语法验证和模式感知过滤对生成的候选查询进行多阶段筛选与聚合。
result: 实验证明语法过滤提升了语法有效性，而模式感知过滤进一步增强了查询的执行质量，尽管这会略微增加空预测率。
conclusion: 在测试阶段引入简单的结构化检查是提高Text2Cypher生成可靠性的有效手段，并明确了语法与模式约束对生成质量的不同贡献。
---

## 摘要
大语言模型（LLM）允许用户通过将问题翻译为可执行查询，从而使用自然语言查询数据库。尽管在 Text2SQL、Text2SPARQL 和 Text2Cypher 等任务上取得了显著进展，但大多数现有方法都集中在更好的提示工程、微调或迭代优化上。然而，它们通常不显式强制执行结构化约束，例如语法有效性和模式一致性。这可能会降低可靠性，因为生成的查询必须同时满足语法规则和数据库模式约束才能执行。在这项工作中，我们研究了如何在 Text2Cypher 的测试时推理中使用结构化约束。我们专注于生成后验证以提高查询的正确性。我们通过一个顺序过滤过程扩展了基于置信度的推理框架，该过程在最终聚合之前结合了置信度评分、语法验证和模式约束。这使我们能够分析不同约束类型如何影响生成的查询。我们对两个经过指令微调的模型进行的实验表明，基于语法的过滤提高了语法有效性。模式感知过滤通过强制与数据库结构保持一致，进一步提高了执行质量。然而，更强的过滤也会增加空预测的数量并降低执行覆盖率。总的来说，我们证明了在测试时添加简单的结构检查可以提高 Text2Cypher 生成的可靠性，并且我们更清晰地展示了语法和模式约束如何产生不同的贡献。

## Abstract
Large language models (LLMs) allow users to query databases using natural language by translating questions into executable queries. Despite strong progress on tasks such as Text2SQL, Text2SPARQL, and Text2Cypher, most existing methods focus on better prompting, fine-tuning, or iterative refinement. However, they often do not explicitly enforce structural constraints, such as syntactic validity and schema consistency. This can reduce reliability, since generated queries must satisfy both syntax rules and database schema constraints to be executable. In this work, we study how structured constraints can be used in test-time inference for Text2Cypher. We focus on post-generation validation to improve query correctness. We extend a confidence-based inference framework with a sequential filtering process that combines confidence scoring, grammar validation, and schema constraints before final aggregation. This lets us analyze how different constraint types affect generated queries. Our experiments with two instruction-tuned models show that grammar-based filtering improves syntactic validity. Schema-aware filtering further improves execution quality by enforcing consistency with the database structure. However, stronger filtering also increases the number of empty predictions and reduces execution coverage. Overall, we show that adding simple structural checks at test time improves the reliability of Text2Cypher generation, and we provide a clearer view of how syntax and schema constraints contribute differently.

---

## 论文详细总结（自动生成）

这是一份关于论文《Extending Confidence-Based Text2Cypher with Grammar and Schema Aware Filtering》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）在 Text2Cypher（自然语言转图查询语言）任务上表现出色，但生成的查询往往缺乏**语法有效性**和**模式（Schema）一致性**。
*   **研究动机**：现有的改进方法多集中在提示工程（Prompt Engineering）或模型微调上，而忽略了在**推理阶段（Test-time）**显式强制执行结构化约束的作用。
*   **背景**：生成的 Cypher 查询如果违反了图数据库的语法规则或节点/关系的连接模式，将导致查询无法执行或结果错误。本文旨在探索如何通过后生成（Post-generation）过滤机制来提升查询的可靠性。

### 2. 论文提出的方法论
论文提出了一种**顺序过滤管道（Sequential Filtering Pipeline）**，在基于置信度的推理框架基础上，按顺序应用多层约束：
*   **核心思想**：生成多个候选查询（Traces），通过层层筛选剔除低质量、语法错误和模式不符的查询，最后对剩余候选进行投票聚合。
*   **关键技术细节**：
    1.  **置信度过滤**：基于 DeepConf 框架，利用模型生成的概率得分过滤掉低置信度的候选。
    2.  **语法过滤（Grammar-based）**：
        *   *Naive 方式*：使用正则表达式检查括号匹配、关键词重复（如 `RETURN RETURN`）及基本子句存在性。
        *   *Formal 方式*：使用 ANTLR4 解析器根据 OpenCypher 标准语法进行严格校验。
    3.  **模式感知过滤（Schema-aware）**：检查查询中的关系方向是否与数据库定义的模式一致（例如：验证 `(:Source)-[:REL]->(:Target)` 的方向是否正确）。
    4.  **聚合**：对通过所有过滤器的候选查询进行多数投票，选出最终输出。

### 3. 实验设计
*   **数据集**：使用了 Text2Cypher 数据集的子集，包含 789 个测试样本，涉及三个具有挑战性的数据库（recommendations, companies, neoflix）。
*   **Benchmark 模型**：
    *   `google/gemma-2-9b-it` (Gemma2)
    *   `Qwen/Qwen2.5-7B-Instruct` (Qwen2.5)
*   **对比设置**：
    *   **采样多样性**：中等多样性（Temp=0.9）与高度多样性（Temp=1.2）。
    *   **推理模式**：离线模式（生成固定数量后过滤）与在线模式（解码时提前终止）。
    *   **消融实验**：对比了“仅置信度”、“置信度+语法”、“置信度+语法+模式”等不同组合。
*   **评估指标**：ROUGE-L（文本层面和执行结果层面）、执行成功率（Exec. Succ. Ratio）。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **vLLM** 库进行推理加速。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及实验的总耗时。由于涉及多次采样（Self-consistency），其推理阶段的算力消耗会显著高于单次生成的基准模型。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主流开源模型（Gemma2 和 Qwen2.5）进行了详尽的对比，涵盖了不同的采样参数和过滤策略组合。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅报告了性能的提升，还详细分析了错误类型（语法错误、运行错误、空预测等），并探讨了“过滤强度”与“执行覆盖率”之间的权衡。通过跨模型验证，证明了该方法的通用性。

### 6. 论文的主要结论与发现
*   **语法过滤的作用**：显著提升了查询的语法有效性，ANTLR4 形式化校验比 Naive 正则校验更有效。
*   **模式过滤的作用**：通过强制执行关系方向一致性，进一步提升了执行结果的准确性（ROUGE-L Exec 指标提升明显）。
*   **权衡关系**：更严格的过滤虽然提高了单体查询的质量，但也导致了**“空预测”**（即所有候选都被过滤掉）的增加，从而在某些情况下降低了执行覆盖率。
*   **多样性影响**：在采样多样性较高时，结构化过滤带来的增益更加显著，因为它能有效剔除高随机性产生的幻觉查询。

### 7. 优点
*   **即插即用**：作为一种推理阶段的策略，不需要重新训练模型或修改权重，易于集成到现有系统中。
*   **可靠性高**：结合了模型内部概率（置信度）与外部硬约束（语法和模式），双重保障了生成质量。
*   **分析深入**：对错误模式进行了细致分类，为后续研究提供了清晰的改进方向。

### 8. 不足与局限
*   **计算开销**：基于自一致性（Self-consistency）的方法需要生成多个候选，显著增加了推理延迟和计算成本。
*   **语法库差异**：实验中使用的 OpenCypher 语法与 Neo4j 实际支持的 Cypher v5 存在细微差异（如 `GROUP BY` 支持问题），导致部分通过校验的查询在执行时仍报错。
*   **空预测风险**：在模型本身能力较弱或问题极难时，严格的过滤可能导致系统无法给出任何回答。
*   **模式过滤深度**：目前的模式过滤主要集中在关系方向上，尚未涵盖更复杂的属性约束或路径约束。

（完）
