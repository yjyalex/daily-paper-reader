---
title: "AgentForesight: Online Auditing for Early Failure Prediction in Multi-Agent Systems"
title_zh: AgentForesight：多智能体系统中早期故障预测的在线审计
authors: "Boxuan Zhang, Jianing Zhu, Zeru Shi, Dongfang Liu, Ruixiang Tang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08715v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统中的失败预测
tldr: 针对大模型多智能体系统在长程任务中因单点错误导致轨迹级失败的问题，本文提出AgentForesight框架，将事后故障归因转变为在线审计。通过构建包含2000条标注轨迹的AFTraj-2K数据集，并采用粗到细的强化学习策略训练AgentForesight-7B模型，实现在任务执行过程中实时预测决定性错误。该方法在多个基准测试中显著优于GPT-4等闭源模型，为部署时的实时干预提供了可能。
selection_source: fresh_fetch
motivation: 现有的多智能体系统故障诊断多为事后分析，无法在错误发生时及时干预以防止失败连锁反应。
method: 提出在线审计框架，构建AFTraj-2K数据集，并利用包含风险预测先验和三轴奖励机制的强化学习算法训练7B规模的审计模型。
result: "AgentForesight-7B在性能上超过了GPT-4.1和DeepSeek-V4-Pro，性能提升高达19.9%，且步级定位误差降低了3倍。"
conclusion: 该研究成功将故障检测从后期归因推向前置审计，为提高多智能体系统的可靠性和实时干预能力提供了有效方案。
---

## 摘要
基于大语言模型（LLM）的多智能体系统越来越多地被部署在长程任务中，但单个决定性错误往往会被下游智能体接受，并级联导致轨迹层面的失败。现有工作将其定义为“事后故障归因”，即在轨迹结束后诊断负责的智能体和步骤。然而，这种范式失去了在轨迹仍在展开时进行干预的机会。在这项工作中，我们提出了 AgentForesight，这是一个将该问题重新定义为“在线审计”的框架：在轨迹展开的每一步，审计器仅观察当前前缀，并且必须在无法访问后续步骤的情况下，选择继续运行或在最早的决定性错误处发出警报。为此，我们构建了 AFTraj-2K，这是一个涵盖编程、数学和智能体领域的智能体轨迹语料库。在该语料库中，安全轨迹通过严格的筛选流程保留，而不安全轨迹则通过多个 LLM 裁判的共识，在其决定性错误发生的步骤处进行标注。基于此，我们开发了 AgentForesight-7B，这是一个紧凑的在线审计器。它采用由粗到精的强化学习方案进行训练：首先通过相邻的安全/不安全前缀对，使其在故障边界具备风险预测先验；随后在针对审计判定的“内容（what）”、“位置（where）”和“对象（who）”的三轴奖励机制下，将该先验细化为精确的步级定位。在 AFTraj-2K 和外部 Who&When 基准测试中，AgentForesight-7B 的表现优于包括 GPT-4.1 和 DeepSeek-V4-Pro 在内的领先闭源模型，实现了高达 +19.9% 的性能提升和 3 倍更低的步定位误差，实现了从“事后故障检测”到“部署时干预”的闭环。项目页面：https://zbox1005.github.io/agent-foresight/

## Abstract
LLM-based multi-agent systems are increasingly deployed on long-horizon tasks, but a single decisive error is often accepted by downstream agents and cascades into trajectory-level failure. Existing work frames this as \emph{post-hoc failure attribution}, diagnosing the responsible agent and step after the trajectory has ended. However, this paradigm forfeits any opportunity to intervene while trajectory is still unfolding. In this work, we introduce AgentForesight, a framework that reframes this problem as online auditing: at each step of an unfolding trajectory, an auditor observes only the current prefix and must either continue the run or alarm at the earliest decisive error, without access to future steps. To this end, we curate AFTraj-2K, a corpus of agentic trajectories across Coding, Math, and Agentic domains, in which safe trajectories are retained under a strict curation pipeline and unsafe trajectories are annotated at the step of their decisive error via consensus among multiple LLM judges. Built on that, we develop AgentForesight-7B, a compact online auditor trained with a coarse-to-fine reinforcement learning recipe that first equips it with a risk-anticipation prior at the failure boundary on adjacent safe/unsafe prefix pairs, then sharpens this prior into precise step-level localization under a three-axis reward jointly targeting the what, where, and who of an audit verdict. Across AFTraj-2K and an external Who\&When benchmark, AgentForesight-7B outperforms leading proprietary models, including GPT-4.1 and DeepSeek-V4-Pro, achieving up to +19.9% performance gain and 3$\times$ lower step localization error, opening the loop from post-hoc failures detection to enabling deployment-time intervention. Project page: https://zbox1005.github.io/agent-foresight/

---

## 论文详细总结（自动生成）

### AgentForesight: 多智能体系统早期故障预测的在线审计

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的多智能体系统（MAS）被广泛应用于软件开发、科学发现等长程任务，系统可靠性成为瓶颈。在 MAS 中，一个微小的“决定性错误”（Decisive Error）往往会被下游智能体吸收并级联放大，最终导致整个任务失败。
*   **现有局限**：目前的故障分析多为“事后归因”（Post-hoc），即在任务结束后才诊断错误。这种方式无法在错误发生时及时止损，且在实际部署中可能已造成不可逆的副作用。
*   **核心目标**：论文提出了**在线审计（Online Auditing）**范式，要求审计器在轨迹展开的每一步仅根据当前前缀信息做出“继续”或“报警”的决策，从而在错误传播前开启干预窗口。

#### 2. 方法论：核心思想与关键技术
论文提出了 **AgentForesight** 框架，包含数据集构建和模型训练两个核心部分：
*   **AFTRAJ-2K 数据集构建**：
    *   **安全轨迹筛选**：通过结果一致性、工具调用完整性和逻辑连贯性三道关卡，确保保留的轨迹在每一步都是真正安全的。
    *   **不安全轨迹标注**：结合“决定性错误注入”（在安全轨迹中人为引入错误并重跑）和“诊断流”（利用多个 LLM 裁判对自然失败轨迹进行投票验证），精准标注错误发生的步骤（Where）和责任智能体（Who）。
*   **AgentForesight-7B 训练流程（粗到细的 RL 方案）**：
    *   **阶段 1：故障边界对齐（BPPO）**。使用边界对偏好优化（Boundary-Pair Preference Optimization），让模型学习在决定性错误发生前后的“临界点”做出截然不同的判定，建立风险预测先验。
    *   **阶段 2：三轴奖励强化学习（GRPO）**。采用群组相对策略优化，通过三个维度定义奖励函数：
        1.  **What（结构）**：验证输出是否符合 JSON 格式及逻辑严密性。
        2.  **Where（时机）**：利用高斯函数奖励模型在精确步骤报警，惩罚过早或过晚的报警。
        3.  **Who（归因）**：奖励对责任智能体角色的准确识别。

#### 3. 实验设计
*   **数据集与场景**：
    *   **AFTRAJ-2K**：涵盖数学（MATH-500）、编程（HumanEval+/MBPP+）和综合智能体任务（GAIA/HotpotQA）。
    *   **Who&When**：作为外部基准测试，验证模型的泛化能力。
*   **对比方法**：
    *   **开源模型**：Llama-3.2-3B, Gemma-3-4B, Qwen2.5/3 系列。
    *   **闭源模型**：GPT-4.1, Gemini-3-Flash, Claude-Haiku-4.5, DeepSeek-V4-Pro 等。
    *   **方法论基准**：基于不确定性的 Perplexity-7B、思维树 ToT-7B、自我反思 Reflexion-7B 以及事后调试框架 AgentDebug-7B。

#### 4. 资源与算力
*   **硬件环境**：训练在 **2 台 NVIDIA H200 GPU** 上完成。
*   **软件框架**：使用了 `verl` 强化学习框架和 `vLLM` 推理加速引擎。
*   **训练细节**：阶段 1 训练 3 个 Epoch，阶段 2 训练 8 个 Epoch。推理时，7B 模型在 H200 上单次审计调用耗时约 1.03 秒。

#### 5. 实验数量与充分性
论文进行了详尽的实验验证：
*   **多维度评估**：不仅测试了 Exact-F1（精确步骤识别）和 ASS（绝对步骤偏移），还分析了误报率（FAR）与检测准确率的权衡。
*   **消融实验**：分别验证了阶段 1（BPPO）和阶段 2（GRPO）对最终性能的贡献，证明了两阶段方案的必要性。
*   **泛化性测试**：在完全未见的外部数据集 Who&When 上，AgentForesight-7B 依然保持领先，证明其并非过拟合于特定框架。
*   **客观性**：实验采用了严格的“步进式审计”协议，模拟真实部署环境，对比基准涵盖了当前最强的闭源模型，实验设计公平且客观。

#### 6. 主要结论与发现
*   **性能卓越**：AgentForesight-7B 在 AFTRAJ-2K 上达到了 66.44% 的 Exact-F1，比最强的闭源模型 DeepSeek-V4-Pro 高出 **19.9%**。
*   **定位精准**：其步骤定位误差（ASS）仅为 0.59，比闭源模型降低了 **3 倍**。
*   **部署可行性**：在保持高检测率的同时，误报率极低（2.37%），是唯一进入“可部署区域”的模型。
*   **模型规模不是唯一因素**：专门训练的 7B 模型在特定审计任务上可以显著超越参数量大得多的通用闭源模型。

#### 7. 优点
*   **范式创新**：将故障分析从“事后”推向“在线”，为 MAS 的实时干预提供了理论和技术基础。
*   **训练策略精巧**：两阶段训练成功解决了审计任务中奖励稀疏和边界模糊的问题。
*   **高质量数据**：构建了目前最严谨的 MAS 在线审计数据集，结合了人工干预注入和多裁判校验。

#### 8. 不足与局限
*   **计算开销**：虽然 7B 模型较轻量，但在长达数十步的轨迹中每一步都调用审计器，仍会增加整体推理成本。
*   **领域覆盖**：目前主要集中在编程、数学和 Web 任务，对于具身智能（Embodied AI）或超长程（数千步）任务的适用性尚待验证。
*   **干预策略缺失**：论文重点在于“如何发现错误”，但发现错误后如何进行最优的自动化修复（Intervention）未在本文深入探讨。

（完）
