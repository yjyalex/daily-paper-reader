---
title: "Rollout Pass-Rate Control: Steering Binary-Reward RL Toward Its Most Informative Regime"
title_zh: 采样通过率控制：将二元奖励强化学习引导至其信息量最大的区间
authors: "Tianshu Zhu, Wenyu Zhang, Xiaoying Zuo, Lun Tian, Haotian Zhao, Yucheng Zeng, Jingnan Gu, Daxiang Dong, Jianmin Wu, Dawei Yin, Dou Shen"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05112v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用RLOO和GRPO引导智能体任务中的二元奖励强化学习
tldr: "针对智能体强化学习中二元奖励信号弱、计算资源浪费的问题，本文提出“通过控制通过率来优化训练”的观点。研究发现50%的通过率能最大化奖励熵和对比信号。为此提出了前缀采样（Prefix Sampling）方法，通过重放成功或失败的前缀来引导轨迹分布，在SWE-bench等任务上显著提升了训练效率和模型性能。"
selection_source: fresh_fetch
motivation: 现有的智能体强化学习在轨迹通过率极高或极低时，二元奖励提供的对比信号极弱，导致大量计算资源被浪费。
method: "提出前缀采样（Prefix Sampling），通过重放成功前缀为失败组提供“起步”，或重放失败前缀为成功组设置“障碍”，将通过率引导至50%的最优区间。"
result: 在SWE-bench任务上，该方法使Qwen3-14B训练提速2.01倍，且验证集性能从0.273提升至0.295。
conclusion: 通过主动控制Rollout的通过率，可以显著增强强化学习的对比学习效果，实现训练效率与模型性能的双重提升。
---

## 摘要
SWE-bench 风格的智能体强化学习依赖于昂贵的有状态轨迹，然而，大量计算资源被浪费在通过率偏斜的采样组上，在这些组中，二元奖励提供的对比信号较弱。我们将这种低效性定义为一个通过率控制问题，并证明 50% 的通过率是信息量最大的运行点：它最大化了奖励熵、组过滤的存活概率、GRPO 框架下的 RLOO 优势能以及成功-失败的对比结构。在这一原则的指导下，我们提出了前缀采样（Prefix Sampling, PS），通过重放轨迹前缀将偏斜的组引导至该区间：成功的前缀作为大多数失败组的“领先起跑”，而失败的前缀则作为大多数成功组的“障碍”。在有状态的智能体环境中，前缀状态通过重放进行重建，而重放的 token 被排除在损失函数之外，从而将优化限制在当前策略生成的后续内容上。在 SWE-bench 风格的智能体强化学习中，PS 在 Qwen3-14B 上实现了 2.01 倍、在 Qwen3-32B 上实现了 1.55 倍的端到端实际耗时加速，同时保持或提升了最终的验证性能。对于 14B 模型，在 PS 下，SWE-bench Verified 的峰值从基线的 0.273 提升至 0.295。在 AIME 2025 上的额外数学推理实验显示了相同的通过率控制模式，并将收益分解为重放、双向覆盖和自适应控制。

## Abstract
SWE-bench-style agentic reinforcement learning relies on expensive stateful trajectories, yet substantial compute is wasted on sampled rollout groups with skewed pass rates, where binary rewards provide a weak contrastive signal. We frame this inefficiency as a pass-rate control problem and show that a 50% pass rate is the most informative operating point: it maximizes reward entropy, the probability of surviving group filtering, RLOO advantage energy under GRPO, and success--failure contrastive structure. Guided by this principle, we propose Prefix Sampling (PS), which replays trajectory prefixes to steer skewed groups toward this regime: successful prefixes serve as head starts for mostly failing groups, while failing prefixes serve as handicaps for mostly passing groups. In stateful agent environments, prefix states are reconstructed through replay while replayed tokens are excluded from the loss, restricting optimization to continuations generated by the current policy. On SWE-bench-style agentic RL, PS delivers end-to-end wall-clock speedups of 2.01x on Qwen3-14B and 1.55x on Qwen3-32B while preserving or improving final verified performance. For 14B, the SWE-bench Verified peak rises from the baseline peak of 0.273 to 0.295 under PS. Additional mathematical reasoning experiments on AIME 2025 show the same pass-rate control pattern and decompose the gains into replay, bidirectional coverage, and adaptive control.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rollout Pass-Rate Control: Steering Binary-Reward RL Toward Its Most Informative Regime》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在具有可验证奖励的强化学习（RLVR，如软件工程、数学推理）中，通常使用二元奖励（0或1）。当采样轨迹（Rollout）的通过率极高（全对）或极低（全错）时，组相对策略优化（GRPO）等算法无法获得有效的对比信号，导致大量计算资源被浪费在低信息量的采样上。
*   **研究背景**：随着智能体（Agent）任务变得更长、更复杂，生成轨迹的成本极高。现有的过滤机制（如丢弃全0或全1的组）虽然减少了无效更新，但未能主动将模型引导至高效学习区间。

### 2. 方法论：前缀采样（Prefix Sampling, PS）
*   **核心思想**：基于信息论证明 **50% 的通过率**是二元奖励 RL 的“黄金运行点”（最大化奖励熵、对比对数量和 RLOO 优势能）。PS 通过重放轨迹前缀，将偏斜的采样组拉回到 50% 附近。
*   **关键技术细节**：
    *   **双向引导**：针对“太难”的组（通过率 1/8, 2/8），重放一段成功的轨迹前缀作为“起步助推”；针对“太易”的组（通过率 6/8, 7/8），重放一段失败的前缀作为“障碍挑战”。
    *   **自适应控制（Adaptive Control）**：引入闭环控制器，根据每个桶（Bucket）的实时通过率反馈，动态调整重放前缀的长度比例（$r_b$），确保重采样后的通过率稳定在 0.5 附近。
    *   **有状态重放与掩码**：在智能体环境中，通过执行已记录的动作序列来重建环境状态（代码、上下文、工具输出）。在计算损失函数时，**掩码（Mask）掉重放的 Token**，仅对当前策略新生成的后续内容进行优化，确保信度分配的准确性。

### 3. 实验设计
*   **场景与数据集**：
    *   **智能体编码**：使用 SWE-bench Verified 评估模型解决真实 GitHub 问题的能力。
    *   **数学推理**：使用 AIME 2025 评估逻辑推理能力。
*   **基准模型（Backbones）**：Qwen3 系列（4B, 8B, 14B, 32B）。
*   **对比方法**：主要对比经过严格匹配的 **GRPO++ 基线**（DeepSWE 方案），确保在相同的 Prompt、奖励函数和采样预算（N=8）下进行公平比较。

### 4. 资源与算力
*   **硬件配置**：
    *   **4B 模型**：2 × 8 台 A800 GPU。
    *   **8B 模型**：4 × 8 台 A800 GPU。
    *   **14B/32B 模型**：4 × 8 台 H200 GPU。
*   **训练时长**：论文提供了端到端的墙钟时间（Wall-clock time）。例如，Qwen3-14B 在 PS 方法下达到基线峰值性能仅需约 66 小时，而基线需要 133 小时，实现了约 2 倍的加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 32B 四个参数量级的模型，验证了方法的跨尺度有效性。
*   **消融实验**：在 4B 模型上进行了详细消融，对比了“固定前缀长度”、“仅针对困难任务”以及“全功能自适应”等变体，证明了双向引导和自适应控制的必要性。
*   **客观性**：实验采用了多次评估取平均值（Avg8）的方式，并提供了置信区间和变异性分析，机制分析部分深入到了轨迹状态转移的微观层面，论证较为严谨。

### 6. 主要结论与发现
*   **效率大幅提升**：在 SWE-bench 任务上，Qwen3-14B 实现了 **2.01 倍**的端到端加速，32B 模型实现了 **1.55 倍**加速。
*   **性能突破**：PS 不仅训练更快，还能提升最终性能。14B 模型在 SWE-bench Verified 上的得分从 0.273 提升至 0.295。
*   **信号增强**：通过将通过率引导至 50%，有效增加了每一步训练中“有效组”（非全对全错）的数量，显著提升了奖励熵和对比学习信号。

### 7. 优点与亮点
*   **理论支撑扎实**：从信息论角度明确了 50% 通过率的最优性，为启发式采样提供了科学依据。
*   **系统实现巧妙**：通过“重放即执行”解决了智能体环境状态恢复的难题，无需复杂的快照（Snapshot）机制。
*   **自给自足**：不需要外部 Oracle（如强模型的推理链或标准答案前缀），完全依赖模型自身的采样轨迹进行自我进化。

### 8. 不足与局限
*   **任务类型限制**：该方法高度依赖于“二元奖励（Binary Reward）”和“可验证任务”，对于奖励连续或难以自动验证的任务（如创意写作）可能不适用。
*   **内存开销**：虽然论文提到前缀池占用内存较小（约 16MB），但在极大规模并发或超长序列任务中，存储和管理前缀轨迹仍会带来一定的系统复杂性。
*   **超参数敏感性**：虽然有自适应控制器，但初始步长、死区（Deadzone）等超参数仍可能需要针对不同任务进行微调。

（完）
