---
title: "Beyond Task Success: Measuring Workflow Fidelity in LLM-Based Agentic Payment Systems"
title_zh: 超越任务成功率：衡量基于大语言模型的智能体支付系统中的工作流保真度
authors: "Donghao Huang, Joon Kiat Chua, Zhaoxia Wang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06457v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 基于大语言模型的多智能体支付系统工作流保真度度量
tldr: 本研究针对基于大语言模型的多智能体支付系统，指出传统指标如任务成功率（TSR）无法捕捉工作流执行的合规性。为此，作者提出了“智能体成功率”（ASR）指标，通过在转换级别对比实际与预期的执行序列来衡量轨迹忠实度。在对18个模型的测试中，ASR揭示了部分模型在支付环节存在跳过确认步骤的隐蔽违规行为。通过ASR引导的优化，模型性能显著提升，证明了在受监管领域进行轨迹级评估的必要性。
selection_source: fresh_fetch
motivation: 现有的评估指标仅关注最终结果，无法衡量支付等受监管领域中智能体执行工作流的合规性和路径忠实度。
method: 提出智能体成功率（ASR）指标，通过在转换级别比较观察到的与预期的智能体执行序列来评估工作流保真度。
result: "ASR揭示了GPT-4.1等模型在保持高成功率的同时存在跳过检查点的隐蔽偏差，而基于ASR的优化使模型成功率最高提升了93.8%。"
conclusion: 在受监管的智能体系统中，轨迹级别的评估对于识别工作流偏差和确保系统符合业务逻辑至关重要。
---

## 摘要
基于大语言模型的多智能体系统正越来越多地应用于支付工作流，然而现有的主流指标，如任务成功率 (TSR) 和智能体交接 F1 分数 (HF1)，仅能捕捉最终结果或无序的路由决策。我们引入了智能体成功率 (ASR)，这是一种轨迹保真度指标，它在转换级别比较观察到的和预期的智能体执行序列，并将性能分解为转换召回率和转换精确率。将 ASR 应用于支付分层多智能体系统 (HMASP)，涵盖 18 个大语言模型和 90,000 个任务实例，结果显示 18 个模型中有 10 个在支付结账过程中系统性地跳过了确认检查点，这种偏差在 TSR 和 HF1 中都是不可见的，而有 8 个模型则完美地执行了该检查点。值得注意的是，GPT-4.1 尽管实现了完美的 TSR 和 HF1，却表现出隐藏的工作流捷径，而 GPT-5.2 则实现了完美的 ASR。在 ASR 诊断的指导下，提示词优化和确定性路由守卫带来了显著的 TSR 提升，对于此前表现不佳的模型，提升幅度高达 +93.8 个百分点，这证明了轨迹级评估在受监管领域是必不可少的。

## Abstract
LLM-based multi-agent systems are increasingly deployed for payment workflows, yet prevailing metrics, Task Success Rate (TSR) and Agent Handoff F1-Score (HF1), capture only final outcomes or unordered routing decisions. We introduce the Agentic Success Rate (ASR), a trajectory-fidelity metric that compares observed and expected agent execution sequences at the transition level, decomposing performance into Transition Recall and Transition Precision. Applied to the Hierarchical Multi-Agent System for Payments (HMASP) across 18 LLMs and 90,000 task instances, ASR reveals that 10 of 18 models systematically skip a confirmation checkpoint during payment checkout, a deviation invisible to both TSR and HF1, while 8 models enforce the checkpoint perfectly. Notably, GPT-4.1 exhibits hidden workflow shortcuts despite achieving perfect TSR and HF1, while GPT-5.2 achieves perfect ASR. Prompt refinements and deterministic routing guards guided by ASR diagnostics yield substantial TSR improvements, with gains up to +93.8 percentage points for previously struggling models, demonstrating that trajectory-level evaluation is essential in regulated domains.

---

## 论文详细总结（自动生成）

这篇论文针对基于大语言模型（LLM）的多智能体支付系统，提出了一种衡量工作流保真度的新指标，并对多种主流模型进行了大规模评估。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在支付等受监管领域，智能体不仅要“完成任务”，还必须“合规地完成任务”。现有的评估指标如**任务成功率（TSR）**仅关注最终结果，**智能体交接 F1 分数（HF1）**仅关注无序的路由决策，两者都无法捕捉执行过程中的**轨迹保真度（Trajectory Fidelity）**。
*   **研究背景**：随着智能体支付（Agentic Payments）规模的扩大（预计 2030 年达 1.7 万亿美元），审计合规性（如 PCI-DSS 要求）变得至关重要。如果智能体跳过了必要的确认步骤但依然完成了支付，虽然 TSR 是 100%，但在法律和安全层面是失败的。

### 2. 论文提出的方法论
*   **核心思想**：借鉴过程挖掘（Process Mining）中的一致性检查，提出**智能体成功率（ASR）**。它通过比较“预期执行序列”与“实际执行序列”的**转换级别（Transition-level，即 Bigram）**差异来衡量保真度。
*   **关键技术细节**：
    *   将执行轨迹转化为智能体对的集合（例如轨迹 [A, B, C] 转化为 {(A,B), (B,C)}）。
    *   **转换召回率 (TR)**：衡量预期步骤中被正确执行的比例（识别“跳步”）。
    *   **转换精确率 (TP)**：衡量实际执行步骤中属于预期的比例（识别“冗余步骤”）。
    *   **ASR 公式**：TR 和 TP 的调和平均数（F1-Score）。
*   **系统优化**：利用 ASR 作为诊断工具，通过**提示词工程**（明确角色边界）和**确定性路由守卫**（对关键路径进行硬性约束）来改进系统。

### 3. 实验设计
*   **场景与数据集**：基于 HMASP 框架，设置了 4 个支付场景：T1（卡片注册）、T2（卡片查询）、T3（支付处理）、T4（无关输入拒绝）。共 1,000 个测试数据点。
*   **Benchmark 与对比对象**：
    *   **模型**：横跨 18 个 LLM，包括 GPT-4.1、GPT-5.2、Llama 3.1 (8B/70B)、Qwen 2.5/3、Gemma 4 系列等。
    *   **对比指标**：TSR（任务成功率）、HF1（交接 F1 分数）与 ASR。
*   **对比方法**：将原始 HMASP 系统与经过 ASR 诊断优化的系统进行对比。

### 4. 资源与算力
*   **硬件环境**：本地实验在配备 **Apple M5 Max 芯片（128 GB 统一内存）**的设备上运行。
*   **软件框架**：使用 **Ollama v0.20.0** 进行推理。
*   **量化处理**：开源模型使用 4-bit 量化（GPT-OSS 使用 MXFP4，其余使用 Q4_K_M）；闭源模型通过 OpenAI API 访问。
*   **训练时长**：文中未提及训练时长，因为主要侧重于推理评估和系统工程优化。

### 5. 实验数量与充分性
*   **实验规模**：每个模型在每个场景下独立重复 5 次，总计 **90,000 个任务实例**（18 模型 × 1,000 数据点 × 5 重复）。
*   **充分性**：实验覆盖了从 7B 到 120B+ 不同参数规模的模型，且包含了最新的闭源和开源模型。通过多次重复实验并报告均值与标准差，确保了结果的统计可靠性。
*   **客观性**：对比了多种指标，揭示了传统指标的局限性，实验设计较为公正。

### 6. 论文的主要结论与发现
*   **ASR 的必要性**：18 个模型中有 10 个在 T3（支付）场景中出现了 ASR 缺口（0.01% - 5.45%），即使它们的 TSR 和 HF1 达到了 100%。这表明这些模型**系统性地跳过了用户确认步骤**。
*   **模型表现差异**：
    *   **GPT-4.1** 存在隐蔽的工作流捷径（TSR=100% 但 ASR<100%）。
    *   **GPT-5.2** 和 Gemma 4 等 8 个模型实现了完美的 ASR。
*   **工程优化的威力**：在 ASR 诊断指导下，通过改进提示词和增加路由守卫，Llama 3.1:8b 的 TSR 提升了 **93.8 个百分点**，证明了许多失败源于系统设计而非模型能力极限。

### 7. 优点
*   **实用性强**：直接针对金融支付领域的合规痛点，提出的指标具有很强的行业参考价值。
*   **诊断功能**：ASR 不仅是评估指标，还能精确定位智能体在哪里“抄了近道”或“绕了路”。
*   **大规模评估**：9 万次实例的测试量在智能体研究论文中属于较高水平，结论更具说服力。

### 8. 不足与局限
*   **架构依赖**：实验主要基于 HMASP 这一特定分层架构，ASR 在其他非线性或更复杂图结构智能体系统中的表现尚待验证。
*   **权重缺失**：目前的 ASR 对所有转换步骤视同仁，但在实际业务中，支付确认步骤的权重应远高于普通的日志记录步骤。
*   **提示词敏感性**：虽然通过工程手段提升了性能，但未深入探讨不同模型对 ASR 引导优化的敏感度差异。

（完）
