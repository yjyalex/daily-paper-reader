---
title: Improving the Efficiency of Language Agent Teams with Adaptive Task Graphs
title_zh: 通过自适应任务图提高语言智能体团队的效率
authors: "Elizabeth Mieczkowski, Alexander Ku, Tiwalayo Eisape, Dilip Arumugam, John Matters, Katherine M. Collins, Ilia Sucholutsky, Thomas L. Griffiths"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06320v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 通过自适应任务图协调大语言模型团队
tldr: 针对大语言模型智能体团队在协作中存在的结构化过度僵化或非结构化效率低下的问题，本文提出了 LATTE 框架。该框架受分布式系统启发，通过让智能体共同维护一个动态演化的协作图来管理子任务依赖和分配。实验表明，LATTE 在保持或提升准确率的同时，显著降低了资源消耗、通信成本和协作冲突，提高了团队整体效率。
selection_source: fresh_fetch
motivation: 现有的智能体协作模式在高度结构化的僵化与非结构化的低效之间难以平衡，导致资源浪费和协作冲突。
method: 提出 LATTE 框架，利用动态演化的协作图来编码子任务依赖、智能体分配及进度状态，实现自适应的任务分配。
result: 在多项协作任务中，LATTE 显著减少了 Token 使用量、运行时间和通信故障，且性能优于 MetaGPT 等主流架构。
conclusion: 通过动态协作图实现的自适应协调机制，能有效提升大语言模型智能体团队的运行效率和任务处理能力。
---

## 摘要
大语言模型 (LLM) 越来越多地以团队形式部署，但现有的协作方法往往处于两个极端。高度结构化的方法依赖于预先分配的固定角色、流水线或任务分解。相比之下，完全非结构化的团队虽然具备适应性和探索能力，但存在效率低下的问题，如错误传播、智能体间冲突以及资源浪费（以时间、Token 或文件操作衡量）。我们提出了用于任务演化的语言智能体团队 (LATTE)，这是一个受分布式系统启发的 LLM 团队协作框架，其中处理器必须在部分可观测性和通信限制下运行。在 LATTE 中，智能体团队协作构建并维护一个共享的、不断演化的协作图，该图编码了子任务依赖关系、单个智能体分配以及子任务进度的当前状态。该协议在保持一致性的同时，赋予智能体动态分配工作、调整协作和发现新任务的能力。在多项协作任务和多种基础模型中，我们证明了 LATTE 如何在减少 Token 使用量、实际运行时间、通信和协作失败（例如文件冲突和冗余输出）的同时，达到或超过标准设计的准确率，包括 MetaGPT、去中心化团队、自顶向下的领导者-执行者 (Leader-Worker) 层级结构以及静态分解。

## Abstract
Large language models (LLMs) are increasingly deployed in teams, yet existing coordination approaches often occupy two extremes. Highly structured methods rely on fixed roles, pipelines, or task decompositions assigned a priori. In contrast, fully unstructured teams enable adaptability and exploration but suffer from inefficiencies such as error propagation, inter-agent conflicts, and wasted resources (measured in time, tokens, or file operations). We introduce Language Agent Teams for Task Evolution (LATTE), a framework for coordinating LLM teams inspired by distributed systems, where processors must operate under partial observability and communication constraints. In LATTE, a team of agents collaboratively construct and maintain a shared, evolving coordination graph which encodes sub-task dependencies, individual agent assignment, and the current state of sub-task progress. This protocol maintains consistency while empowering agents to dynamically allocate work, adapt coordination, and discover new tasks. Across multiple collaborative tasks and a variety of base models, we demonstrate how LATTE reduces token usage, wall-clock time, communication, and coordination failures (e.g. file conflicts and redundant outputs) while matching or exceeding the accuracy of standard designs including MetaGPT, decentralized teams, top-down Leader-Worker hierarchies, and static decompositions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving the Efficiency of Language Agent Teams with Adaptive Task Graphs》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）团队在处理复杂任务（如软件开发、数据分析）时表现出巨大潜力，但现有的协作架构存在两个极端：
    *   **高度结构化**：依赖预定义的角色和流水线（如 MetaGPT），缺乏灵活性，且容易因领导者（Lead）的幻觉导致错误向下传播。
    *   **完全非结构化**：智能体自由协作，但容易产生资源浪费（Token/时间）、文件冲突、冗余输出和通信过载。
*   **核心问题**：如何在保持 LLM 智能体**灵活性（适应性）**的同时，引入必要的**结构化约束（一致性）**，以解决协作中的“人月神话”问题（如通信开销超线性增长、任务依赖错位等）。

### 2. 方法论：LATTE 框架
LATTE（Language Agent Teams for Task Evolution）受分布式系统启发，核心思想是让智能体团队共同维护一个**动态演化的协作图**。

*   **核心组件**：
    *   **动态协作图 ($G_t$)**：一个有向无环图（DAG），节点代表子任务，边代表依赖关系。每个节点包含状态（待处理、进行中、已完成等）和分配的智能体。
    *   **混合协作模型**：
        *   **领导者 (Lead)**：负责全局一致性，拥有修改图结构的特权操作（如分配任务、验证结果、释放掉队者）。
        *   **执行者 (Workers)**：负责具体执行，并能根据局部观察提出图更新建议（如发现新子任务）。
*   **关键技术细节（图变更算子）**：
    定义了 7 个具有明确前置/后置条件的算子：
    1.  `DISCOVER`：发现并添加新子任务。
    2.  `ASSIGN`：Lead 分配任务。
    3.  `CLAIM`：Worker 主动领取任务（实现自调度/工作窃取）。
    4.  `COMPLETE`：Worker 标记完成。
    5.  `RELEASE`：Lead 撤回停滞任务（解决掉队者问题）。
    6.  `CLOSE`：Lead 强制关闭任务。
    7.  `VERIFY`：Lead 插入验证节点（动态质量控制）。
*   **上下文范围控制 (Context Scoping)**：Worker 仅查看其子任务相关的局部上下文，Lead 仅查看协作图和消息摘要，避免长文本导致的推理性能下降。

### 3. 实验设计
*   **实验场景**：
    1.  **探索性数据分析 (EDA)**：处理模糊数据集，需动态发现分析维度。
    2.  **代码调试 (Debugging)**：修复信号处理库中的多个 Bug，强调并行与依赖处理。
    3.  **库扩展 (Library Extension)**：根据存根文件构建新模块，测试预定义任务的执行效率。
*   **对比基准 (Benchmarks)**：
    *   **MetaGPT**：基于固定 SOP 的流水线系统。
    *   **Leader-Worker**：传统的自顶向下层级结构。
    *   **Decentralized**：完全去中心化的对等团队。
    *   **Static Graph**：消融实验，仅保留初始规划，执行中不可更改图结构。
*   **基础模型**：使用了 Claude Sonnet 4-6 和 GPT-5.2（注：论文设定在 2026 年背景下）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该研究侧重于**推理阶段的编排框架**。
*   **实验成本**：
    *   Claude 系列实验：约 2136 次 API 调用，消耗 40.7M 输入 Token 和 8.5M 输出 Token（约 250 美元）。
    *   GPT 系列实验：约 4582 次 API 调用，消耗 83.1M 输入 Token 和 14M 输出 Token（约 260 美元）。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **300 组独立实验**（5 种团队结构 × 2 种模型 × 3 类任务 × 10 次重复）。
*   **充分性评价**：
    *   **多维指标**：不仅测量准确率，还引入了覆盖协作质量的细粒度指标（覆盖率、并发冲突、浪费字符数、闲置轮数、掉队者延迟）。
    *   **消融充分**：通过 Static Graph 对比证明了“动态演化”的必要性。
    *   **统计显著性**：使用了 Mann-Whitney U 检验来验证 LATTE 在成本和时间上的显著优势。实验设计客观且公平。

### 6. 主要结论与发现
*   **效率大幅提升**：LATTE 的平均 Token 成本仅为次优方法（Static Graph）的 47.5%，显著低于 MetaGPT 和去中心化团队。
*   **准确率领先**：在综合任务中达到 79.7% 的准确率，优于所有基准方法。
*   **协作冲突减少**：相比 Leader-Worker 架构，LATTE 将智能体间的文件覆盖冲突减少了 5.3 倍，并发写入冲突减少了 8.5 倍。
*   **自适应性**：Lead 能够通过 `RELEASE` 算子有效处理掉队者（36% 的实验触发了此操作），并通过 `VERIFY` 进行按需质量检查。

### 7. 优点
*   **可解释性强**：将原本黑盒的智能体协作过程显性化为动态图，便于审计和诊断。
*   **分布式系统视角**：引入“工作窃取”和“心跳监测”等成熟的分布式概念，解决了 LLM 团队常见的停滞和冗余问题。
*   **帕累托最优**：在准确率-效率权衡曲线上实现了最优表现。

### 8. 不足与局限
*   **规划开销**：对于非常简单或短小的任务，构建和维护图结构的初始开销（Planning Overhead）可能超过其带来的收益。
*   **领域局限**：目前的评估主要集中在代码和数据处理等具有明确子任务边界的领域，在更开放、非结构化的创意写作等任务上的表现尚待验证。
*   **团队规模固定**：实验中固定团队人数为 5 人，尚未深入探讨 LATTE 在超大规模（如数百个智能体）团队中的扩展性。

（完）
