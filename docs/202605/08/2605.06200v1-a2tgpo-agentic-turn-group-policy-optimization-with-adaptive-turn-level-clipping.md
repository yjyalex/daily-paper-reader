---
title: "A$^2$TGPO: Agentic Turn-Group Policy Optimization with Adaptive Turn-level Clipping"
title_zh: A$^2$TGPO：具有自适应轮次级裁剪的智能体轮次组策略优化
authors: "Dingwei Chen, Zefang Zong, Zhipeng Ma, Leo Luo, Yang Li, Chengming Li, Peng Chen, Jie Jiang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06200v1"
tags: ["query:token"]
score: 9.0
evidence: 智能体大模型的轮次级策略优化与内在过程信号
tldr: 针对智能体大模型在多轮交互中难以进行过程信用分配的问题，本文提出 A$^2$TGPO 框架。该方法利用信息增益（IG）作为内在过程信号，通过轮次组归一化、方差缩放的折扣累积以及自适应轮次级裁剪，解决了 IG 信号在不同深度和上下文下的不一致性及优势漂移问题，从而在不依赖外部奖励模型的情况下，更有效地优化多轮决策策略。
selection_source: fresh_fetch
motivation: 现有的多轮强化学习方法在过程信用分配上依赖外部模型或受限于轨迹多样性，且现有的信息增益信号存在归一化失真和优势漂移等系统性挑战。
method: 提出 A$^2$TGPO，通过在相同交互深度的轮次组内归一化 IG、按方差缩放累积优势，并根据 IG 大小动态调整 PPO 的裁剪范围。
result: 该方法通过更精准的轮次级信用分配，显著提升了智能体在复杂多轮任务中的学习效率和策略性能。
conclusion: A$^2$TGPO 为智能体大模型提供了一种高效的内在过程奖励机制，有效解决了多轮强化学习中的信用分配难题。
---

## 摘要
智能体大语言模型（LLM）的强化学习通常依赖于稀疏的轨迹级结果奖励，这使得在多轮交互中评估单个工具调用的贡献变得困难。现有的此类过程信用分配方法要么依赖于引入额外消耗的独立外部过程奖励模型，要么依赖于基于树的结构化展开，后者仅重新分配结果信号，同时限制了轨迹的多样性。一种极具前景的替代方案是利用策略对地面真值预测概率的每轮变化，即信息增益（IG），作为无需外部评估器的内在过程信号。然而，先前在强化学习训练循环中利用 IG 信号的工作面临三个系统性挑战：在面临异构位置上下文的轮次之间进行归一化可能会扭曲单个轮次的相对地位；累加变数量的项会导致优势幅度随轨迹深度漂移；以及固定的裁剪范围对具有截然不同 IG 信号的轮次采用相同的策略更新。在本文中，我们提出了 A$^2$TGPO（具有自适应轮次级裁剪的智能体轮次组策略优化），它保留了 IG 作为内在信号，但重新设计了其归一化、累加和消耗的方式：(i) 轮次组归一化：在每个（提示词，轮次索引）组内对 IG 进行归一化，使得每个轮次仅与处于相同交互深度的同类进行比较；(ii) 方差缩放的折扣累加：将累积的归一化 IG 除以累加项的平方根，以保持不同轮次位置之间的优势幅度具有可比性；(iii) 自适应轮次级裁剪：根据每个轮次的归一化 IG 调节其裁剪范围，为信息量大的轮次扩大更新区域，为信息量小的轮次缩小更新区域。

## Abstract
Reinforcement learning for agentic large language models (LLMs) typically relies on a sparse, trajectory-level outcome reward, making it difficult to evaluate the contribution of individual tool-calls within multi-turn interactions. Existing approaches to such process credit assignment either depend on separate external process reward models that introduce additional consumption, or tree-based structural rollout that merely redistributes the outcome signal while constraining trajectory diversity. A promising alternative leverages the per-turn change in the policy's predicted probability of the ground-truth, termed Information Gain (IG), as an intrinsic process signal without an external evaluator. However, prior work on leveraging IG signals within the RL training loop faces three systematic challenges: normalizing across turns that face heterogeneous positional contexts can distort the relative standing of individual turns, accumulating a variable number of terms causes advantage magnitudes to drift with trajectory depth, and a fixed clipping range governs policy updates identically for turns with vastly different IG signals. In this paper, we propose A$^2$TGPO (Agentic Turn-Group Policy Optimization with Adaptive Turn-level Clipping), which retains IG as the intrinsic signal but re-designs how it is normalized, accumulated, and consumed: (i) turn-group normalization: normalizes IG within each (prompt, turn-index) group so that each turn is compared only against peers at the same interaction depth; (ii) variance-rescaled discounted accumulation: divides cumulative normalized IG by square root of accumulated terms to keep advantage magnitudes comparable across turn positions; and (iii) adaptive turn-level clipping: modulates each turn's clipping range based on its normalized IG, widening the update region for informative turns and narrowing it for uninformative ones.

---

## 论文详细总结（自动生成）

以下是对论文《A$^2$TGPO: Agentic Turn-Group Policy Optimization with Adaptive Turn-level Clipping》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
智能体大语言模型（Agentic LLMs）在执行复杂任务时需要进行多轮工具调用。目前的强化学习（RL）训练主要依赖**轨迹级的结果奖励（Outcome Reward）**，这导致了严重的**过程信用分配（Process Credit Assignment）**难题：难以区分哪些中间步骤真正推动了任务解决，哪些步骤是冗余或有害的。
虽然先前有研究提出利用**信息增益（Information Gain, IG）**——即每一轮交互后模型预测正确答案概率的变化——作为内在奖励，但仍面临三大挑战：
*   **归一化失真**：将不同深度的轮次混合归一化，忽略了不同交互阶段上下文的异构性。
*   **优势漂移**：随着轨迹深度增加，累积的优势值幅度会发生偏移，导致训练不稳定。
*   **裁剪范围固定**：PPO 算法中固定的裁剪范围（Clipping Range）无法根据每轮信息量的大小动态调整更新强度。

### 2. 方法论：核心思想与关键技术
A$^2$TGPO 提出了一套重新设计 IG 信号处理流程的框架，核心包含三个组件：
*   **轮次组归一化（Turn-Group Normalization）**：
    不再进行全局归一化，而是将具有相同 `(prompt, turn-index)` 的轮次划分为一组。这意味着模型只将当前轮次的表现与在相同交互深度、面对相似上下文的“同行”进行比较，从而消除了位置偏差。
*   **方差缩放的折扣累加（Variance-rescaled Discounted Accumulation）**：
    在计算轮次级优势 $A_{i,t}$ 时，将折扣累积的 IG 信号除以剩余轮数 $n$ 的平方根（$\sqrt{n}$）。从理论上证明了这种缩放可以使不同深度的优势值保持在相似的方差水平，防止梯度爆炸或消失。
*   **自适应轮次级裁剪（Adaptive Turn-level Clipping）**：
    利用归一化后的 IG 信号，通过 Sigmoid 函数动态调整 PPO 的裁剪系数。对于信息增益高（表现好）的轮次，扩大裁剪范围以允许更大的策略更新；对于信息增益低或为负的轮次，收缩裁剪范围以限制其影响。
*   **算法流程**：
    1. 采样多轮交互轨迹。
    2. 计算每轮对 Ground-truth 答案的 IG。
    3. 执行轮次组归一化。
    4. 进行方差缩放的优势累加。
    5. 应用自适应裁剪进行策略更新。

### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个开放域问答（QA）基准测试。
    *   **多跳推理（Multi-hop）**：HotpotQA, 2WikiMultihopQA, MuSiQue, Bamboogle。
    *   **单跳推理（Single-hop）**：Natural Questions (NQ), TriviaQA, PopQA。
*   **基座模型**：Qwen3-4B、Qwen3-8B 和 Qwen2.5-7B。
*   **对比方法**：
    *   基础方法：ReAct（无 RL）。
    *   通用 RL 算法：GRPO, DAPO, GSPO。
    *   智能体专用 RL：GiGPO, IGPO, AEPO, Tree-GRPO。

### 4. 资源与算力
*   **硬件环境**：使用了单节点 **8 × NVIDIA H20 GPU**。
*   **训练细节**：基于 VeRL 框架实现。训练批次大小为 64，每个 Prompt 采样 16 条轨迹，最大工具调用轮数为 6。
*   **开销分析**：相比 GRPO，A$^2$TGPO 的单步训练时间仅增加约 **2.9%**。虽然计算 IG 需要额外的一次前向传播，但由于 A$^2$TGPO 学习到了更高效的终止策略（减少了冗余生成的 Token），抵消了大部分计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同规模/系列的基座模型上进行了全面测试，覆盖了从单跳到复杂多跳的多种任务。
*   **消融实验**：详细验证了轮次组归一化、方差缩放和自适应裁剪三个组件的各自贡献，证明了每个模块的必要性。
*   **客观性**：通过训练动态分析（如策略熵、优势值分布可视化）深入探讨了算法稳定的原因，实验设计严谨，对比基准包含了目前最先进的同类算法。

### 6. 主要结论与发现
*   **性能领先**：A$^2$TGPO 在所有基准测试中均优于现有方法，多跳任务平均提升 **+1.75**，单跳任务平均提升 **+1.69**。
*   **训练稳定性**：相比于容易发生熵塌陷（Entropy Collapse）的传统方法，A$^2$TGPO 能在训练过程中维持更健康的策略熵水平，保证了持续的探索能力。
*   **效率提升**：模型学会了在获取足够信息后及时停止，生成的轨迹更短且更精准。

### 7. 优点
*   **无需外部评估器**：完全依赖模型自身的内在信号（IG），避免了训练和部署额外过程奖励模型（PRM）的成本和奖励黑客（Reward Hacking）风险。
*   **理论支撑**：对优势值的方差缩放和归一化偏差进行了数学推导，增强了方法的说服力。
*   **即插即用**：作为一种策略优化层面的改进，可以很容易地集成到现有的智能体训练流程中。

### 8. 不足与局限
*   **依赖 Ground-truth**：计算 IG 信号需要已知的标准答案，这限制了其在无固定答案的开放式任务（如创意写作、开放式对话）中的应用。
*   **计算开销**：虽然通过生成效率抵消了部分开销，但每轮额外的一次 Logits 计算在极长轨迹的任务中仍可能成为瓶颈。
*   **任务范围**：目前主要验证在搜索增强的 QA 任务上，对于更复杂的环境交互（如 Web 导航、代码操作）的泛化性有待进一步验证。

（完）
