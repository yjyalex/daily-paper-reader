---
title: Coordination as an Architectural Layer for LLM-Based Multi-Agent Systems
title_zh: 协同作为基于大语言模型的多智能体系统的架构层
authors: "Maksym Nechepurenko, Pavel Shuvalov"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03310v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体大模型系统中的协作架构层
tldr: 针对多智能体LLM系统因协调缺陷导致的高失败率，本文提出将“协调”视为独立于逻辑和信息的架构层。通过在预测市场任务中测试五种协调配置，利用Brier评分的Murphy分解量化性能，研究揭示了不同配置在成本与质量权衡中的独特特征，为构建可预测的多智能体系统提供了方法论验证。
selection_source: fresh_fetch
motivation: 现有多智能体系统因缺乏协调层的原则性设计，导致在生产环境中失败率极高且难以预测。
method: 将协调作为可配置架构层，在预测市场任务中对比五种配置，并采用Murphy分解法分析Brier评分以区分校准度与判别力。
result: 实验确定了成本-质量的帕累托前沿，发现不同协调配置具有可辨识的性能特征，且部分配置在特定条件下表现优异。
conclusion: 协调应被视为独立的架构层进行研究，本文提出的方法论为多智能体系统的架构推理和性能预测奠定了基础。
---

## 摘要
基于大语言模型（LLM）的多智能体系统在生产环境中的失败率在 41% 到 87% 之间，这主要归因于协同缺陷而非基础模型的能力。现有的应对方案分为两类：一类是根据经验对失败模式进行分类，另一类是提供声明式编排框架作为工程工具；但两者都未能提供从协同配置到可预测失败模式特征的原则性映射。我们认为，协同应被视为一个可配置的架构层，它与智能体逻辑和信息访问相分离，从而实现架构推理，而不仅仅是提高工程生产力。我们通过预测市场上的信息控制设计来实例化这一观点：在五种参考协同配置中，使用单一 LLM、固定工具、固定的单次调用输出上限和固定的提示词模板，并将每个问题的总计算量视为内生架构输出。Brier 分数的 Murphy 分解将校准度与判别力分开，因此即使在总分一致的情况下，不同的配置也会留下可区分的特征。在模型训练截止日期（claude-opus-4-6）之后结算的 100 个 Polymarket 二元市场中，我们报告了 Murphy 特征、成本-质量帕累托前沿、类别条件分析以及自助法（bootstrap）效能预测。五个预设预测中有三个在方向上得到了验证；在此机制下，有两种配置在帕累托前沿占据主导地位；探索性自助法区间将共识对齐与其他配置区分开来，尽管在 n=100 时，两两测试未能通过 Bonferroni 校正。我们还在启用网络搜索的条件下，将相同的配置作为实时智能体部署在 Foresight Arena 上，作为一个并行积累的链上复制通道。测试框架、追踪数据集和生产智能体均已发布。我们将此定位为验证方法论的首次实例化，而非通用的跨模型结论。

## Abstract
Multi-agent LLM systems fail in production at rates between 41% and 87%, mostly due to coordination defects rather than base-model capability. Existing responses split between cataloguing failure modes empirically and shipping declarative orchestration frameworks as engineering tools; neither delivers a principled mapping from coordination configuration to predictable failure-mode signature. We argue that coordination should be treated as a configurable architectural layer, separable from agent logic and from information access, enabling architectural reasoning rather than only engineering productivity.   We instantiate this with an information-controlled design on prediction markets: a single LLM, fixed tools, fixed per-call output cap, and fixed prompt template across five reference coordination configurations, with total compute per question treated as an endogenous architectural output. The Murphy decomposition of the Brier score separates calibration from discriminative power, so configurations leave distinguishable signatures even when aggregate scores coincide.   On 100 Polymarket binary markets resolved after the model's training cutoff (claude-opus-4-6) we report Murphy signatures, a cost-quality Pareto frontier, category-conditioned analysis, and a bootstrap power-projection. Three of five pre-specified predictions are upheld in direction; two configurations dominate the Pareto frontier within this regime; exploratory bootstrap intervals separate consensus alignment from others, though pairwise tests do not survive Bonferroni correction at n=100. We also deploy the same configurations as live agents on Foresight Arena under web-search-enabled conditions, as an on-chain replication channel accumulating in parallel. Harness, trace dataset, and production agents are released. We position this as a methodology-validating first instantiation, not a general cross-model claim.

---

## 论文详细总结（自动生成）

这是一份关于论文《Coordination as an Architectural Layer for LLM-Based Multi-Agent Systems》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管基于 LLM 的多智能体系统（MAS）被广泛应用，但其在生产环境中的失败率极高（41%–87%）。研究表明，这些失败大多源于**协调缺陷**（如指令歧义、智能体间失调、验证缺失），而非基础模型的能力不足。
*   **研究动机**：目前的应对方案存在两个缺陷：一是仅对失败模式进行经验性分类，缺乏原理性预测；二是现有的声明式编排框架（如 AWS Strands, Microsoft Foundry）仅被视为工程便利，而非架构推理的工具。
*   **整体含义**：本文主张将“协调”视为一个独立的、可配置的**架构层**，通过将其与信息访问和智能体逻辑分离，实现对系统行为和失败模式的科学预测。

### 2. 论文提出的方法论
*   **核心思想**：提出三层解耦架构：**信息层**（工具/上下文）、**协调层**（通信拓扑/权限分配）和**智能体层**（LLM 逻辑）。
*   **协调层规范**：定义了七个关键要素：
    1.  **智能体端点**：输入输出模式。
    2.  **通信拓扑**：有向图结构。
    3.  **权限分布**：谁负责决策或聚合。
    4.  **同步机制**：事件驱动、轮次制或异步。
    5.  **聚合规则**：如均值、中位数、分层选择。
    6.  **终止条件**：最大轮次或收敛阈值。
    7.  **失败处理**：重试或回退策略。
*   **评估指标（Murphy 分解）**：使用 Brier 评分（$B$）的经典分解公式：$B = UNC + REL - RES$。
    *   **REL（Reliability/校准度）**：预测概率与实际频率的偏差（越低越好）。
    *   **RES（Resolution/判别力）**：区分不同结果的能力（越高越好）。
    *   **UNC（Uncertainty/不确定性）**：数据集固有的难度。

### 3. 实验设计
*   **场景与数据集**：使用 **Polymarket 预测市场**。选择了 100 个在模型训练截止日期（2025年8月）之后结算的二元市场，涵盖加密货币、政治、体育等 6 个类别。
*   **Benchmark**：以“市场共识价格”作为基准。
*   **对比方法（五种协调配置）**：
    1.  **独立集成 (IE)**：N个智能体独立响应，结果取平均。
    2.  **同行评审辩论 (PC)**：智能体互相观察并修订。
    3.  **编排者-专家 (OS)**：规划者分解任务并分发给专家。
    4.  **顺序流水线 (SP)**：研究 -> 分析 -> 预测的线性链条。
    5.  **共识对齐 (CA)**：迭代直至达成一致。
*   **信息控制**：严格控制变量，所有配置使用相同的 LLM（Claude Opus 4.6）、相同的工具集（禁用联网搜索以防信息泄露）和相同的提示词模板。

### 4. 资源与算力
*   **算力资源**：论文未提及具体的 GPU 硬件型号，因为实验是基于 **Anthropic API**（Claude Opus 4.6）进行的。
*   **实验成本与时长**：
    *   **总成本**：约 109.65 美元。
    *   **总时长**：3 小时 9 分钟。
    *   **Token 消耗**：各架构差异巨大，例如“顺序流水线”每市场消耗约 5.5 万 token，而“独立集成”仅约 1.5 万 token。

### 5. 实验数量与充分性
*   **实验规模**：共 100 个市场案例，每个案例运行 5 种架构，总计约 500 次完整的系统执行。
*   **充分性与公平性**：
    *   **公平性**：采用了“信息控制”原则，解决了以往研究中“架构增益可能来自更多信息获取”的偏见。
    *   **客观性**：使用了预测市场这一具有客观真值的场景，避免了“LLM-as-a-judge”的主观性。
    *   **统计学局限**：作者承认 n=100 的样本量在经过 Bonferroni 校正后，某些两两对比的显著性不足，但足以观察到明显的架构特征分离。

### 6. 论文的主要结论与发现
*   **架构特征验证**：五种架构在 REL-RES 空间中占据了不同的位置。例如，“共识对齐”表现出明显的判别力下降（多样性坍塌），而“顺序流水线”在校准度上表现最优。
*   **帕累托前沿**：实验发现了成本-质量的帕累托前沿，**“独立集成”**（低成本、中等质量）和**“顺序流水线”**（高成本、高质量）占据主导，而“编排者-专家”和“同行评审辩论”在当前设置下被证明是低效的（成本更高但质量更差）。
*   **负 Alpha 现象**：在禁用联网搜索的情况下，所有配置均未能超越市场共识，证明了信息获取对预测任务的决定性作用。

### 7. 优点：方法或实验设计上的亮点
*   **方法论严谨**：首次将协调层作为独立变量进行控制实验，填补了 MAS 架构推理的空白。
*   **引入 Murphy 分解**：不仅看总分，还通过校准度和判别力深入剖析了不同架构为何失败（例如是由于“过度自信”还是“缺乏区分度”）。
*   **开源贡献**：发布了包含 1710 万 token 的多智能体推理追踪数据集（Coordination Traces），为后续研究提供了宝贵资源。

### 8. 不足与局限
*   **样本量限制**：n=100 对于细微架构差异的统计分辨力有限。
*   **领域局限**：实验仅限于预测市场，其结论（如架构优劣排名）可能无法直接推广到代码生成或创意写作等任务。
*   **信息环境单一**：禁用联网搜索虽然保证了实验纯净，但也限制了智能体发挥 MAS 在复杂搜索任务中的潜力。
*   **模型依赖**：实验仅基于 Claude 系列模型，尚未验证这些架构特征在 GPT 或开源模型（如 Llama）上的跨模型一致性。

（完）
