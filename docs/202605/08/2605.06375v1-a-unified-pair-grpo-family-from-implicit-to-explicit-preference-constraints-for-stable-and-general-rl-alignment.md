---
title: "A Unified Pair-GRPO Family: From Implicit to Explicit Preference Constraints for Stable and General RL Alignment"
title_zh: 统一的 Pair-GRPO 家族：从隐式到显式偏好约束，实现稳定且通用的强化学习对齐
authors: Hao Yu
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06375v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大模型偏好强化学习对齐的统一框架
tldr: 针对大语言模型对齐中强化学习（RLHF）存在的不稳定、梯度方向模糊及高方差等问题，本文提出了统一的 Pair-GRPO 框架。该框架包含 Soft-Pair-GRPO 和 Hard-Pair-GRPO 两个变体，通过将二元偏好奖励引入 GRPO 结构并结合显式概率约束，实现了更稳定的策略更新。理论证明了其梯度等价性与收敛性，并在 LLM 对齐和通用强化学习任务中均取得了优于现有基准的效果。
selection_source: fresh_fetch
motivation: 旨在解决主流成对偏好学习范式中策略更新不稳定、梯度方向不明确以及梯度方差过高的问题。
method: 提出 Pair-GRPO 家族，利用二元偏好奖励替代标量奖励，并引入显式局部概率约束和约束 KL 拟合优化。
result: 在 HH-RLHF、UltraFeedback 及 MuJoCo 任务上的实验表明，该方法在对齐质量、胜率、稳定性和泛化性方面均优于现有 SOTA 基准。
conclusion: Pair-GRPO 框架为基于偏好的强化学习提供了坚实的理论保障和高效的实践方案，适用于 LLM 对齐及通用 RL 场景。
---

## 摘要
通过人类偏好强化学习（RLHF）进行的大语言模型（LLM）对齐，在主流的成对偏好学习范式中面临策略更新不稳定、梯度方向模糊、可解释性差以及梯度方差高等问题。为了系统地解决这些局限性，我们建立了一个以 Pair-GRPO 家族为核心的基于偏好的强化学习优化统一理论框架，该家族包含两个紧密耦合的变体：Soft-Pair-GRPO 和 Hard-Pair-GRPO。Soft-Pair-GRPO 是对组相对策略优化（GRPO）的极简修改，它将组归一化的标量奖励替换为二元成对偏好奖励，同时保留了 GRPO 的裁剪代理目标和 KL 正则化结构。我们证明了一个关键的梯度等价定理：在当前策略附近的一阶泰勒展开下，Soft-Pair-GRPO 的梯度是标准 GRPO 梯度的正标量倍数，这解释了其在丢弃连续奖励量级的情况下依然保持实证稳定性的原因。在此基础上，我们提出了 Hard-Pair-GRPO，这是一种引入了显式局部概率约束和约束 KL 拟合优化的先进变体，旨在进一步抑制梯度噪声和全局策略漂移。我们为这两个变体提供了全面的理论保证，包括单调策略提升、确定性梯度方向、梯度方差减少以及动态步长收敛。在标准 LLM 对齐基准（HH-RLHF、UltraFeedback）和 MuJoCo 连续控制任务 HalfCheetah-v4 上的广泛实验表明，我们的 Pair-GRPO 家族在对齐质量、人类偏好胜率、训练稳定性以及对通用强化学习的泛化能力方面，始终优于最先进的基准方法。消融研究验证了每个核心组件的关键贡献。

## Abstract
Large language model (LLM) alignment via reinforcement learning from human preferences (RLHF) suffers from unstable policy updates, ambiguous gradient directions, poor interpretability, and high gradient variance in mainstream pairwise preference learning paradigms. To systematically address these limitations, we establish a unified theoretical framework for preference-based RL optimization centered on the Pair-GRPO family, comprising two tightly coupled variants: Soft-Pair-GRPO and Hard-Pair-GRPO. Soft-Pair-GRPO is a minimal modification of Group Relative Policy Optimization (GRPO) that replaces group-normalized scalar rewards with binary pairwise preference rewards, retaining GRPO's clipped surrogate and KL-regularized structure. We prove a critical gradient equivalence theorem: under first-order Taylor expansion around the current policy, Soft-Pair-GRPO's gradient is a positive scalar multiple of standard GRPO's gradient, explaining its empirical stability despite discarding continuous reward magnitudes. Building on this foundation, we propose Hard-Pair-GRPO, an advanced variant introducing explicit local probability constraints and constrained KL-fitting optimization to further suppress gradient noise and global policy drift. We provide comprehensive theoretical guarantees for both variants--including monotonic policy improvement, deterministic gradient direction, gradient-variance reduction, and dynamic step-size convergence. Extensive experiments on standard LLM alignment benchmarks (HH-RLHF,UltraFeedback) and the MuJoCo continuous control task HalfCheetah-v4 demonstrate that our Pair-GRPO family consistently outperforms state-of-the-art baselines in alignment quality, human preference win rate, training stability, and generalization to general reinforcement learning. Ablation studies validate the critical contributions of each core component.

---

## 论文详细总结（自动生成）

这篇论文提出了一套名为 **Pair-GRPO** 的统一框架，旨在解决大语言模型（LLM）在人类偏好强化学习（RLHF）对齐过程中面临的稳定性差、梯度方差高以及奖励信号模糊等核心挑战。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：主流的 RLHF 方法（如 PPO 和 GRPO）依赖于连续的标量奖励信号。这种范式存在三个主要缺陷：
    1.  **梯度不稳定**：组归一化的标量奖励会导致全局概率分布的无限制偏移，产生高方差。
    2.  **奖励冗余**：人类偏好本质是二元的（A 优于 B），标量奖励的绝对数值往往是冗余且带有噪声的。
    3.  **策略漂移**：缺乏对特定偏好对的局部约束，导致无关响应干扰更新信号。
*   **整体含义**：论文通过建立 **Pair-GRPO 家族**，将偏好对齐从“隐式奖励加权”提升到“显式分布约束”，为稳定且通用的强化学习对齐提供了理论支撑和实践工具。

### 2. 方法论：核心思想与关键技术
该框架包含两个相互关联的变体：
*   **Soft-Pair-GRPO（隐式约束）**：
    *   **核心思想**：对标准 GRPO 的极简改进，将连续的组归一化奖励替换为二元奖励（$+1$ 表示优选，$ -1$ 表示被拒绝）。
    *   **梯度等价定理**：论文证明了在当前策略的一阶泰勒展开下，Soft-Pair-GRPO 的梯度方向与标准 GRPO 是一致的（正标量倍数关系）。这解释了为什么仅靠二元信号就能实现稳定的策略提升。
*   **Hard-Pair-GRPO（显式约束）**：
    *   **核心思想**：构建一个显式的目标分布 $\pi_{tar}$，该分布仅在偏好对（$a_p, a_r$）之间转移概率质量，而冻结所有其他无关响应。
    *   **关键技术**：
        1.  **局部概率偏移**：通过动态衰减的步长 $\delta_t$ 增加优选响应概率，减少拒绝响应概率。
        2.  **约束 KL 拟合**：将优化目标转化为最小化当前策略与目标分布之间的 KL 散度，并辅以信任区域（Trust-Region）约束。
        3.  **确定性梯度**：理论上消除了无关响应产生的噪声，实现了更低的梯度方差。

### 3. 实验设计
*   **数据集/场景**：
    *   **LLM 对齐**：使用了 **HH-RLHF**（10万样本）和 **UltraFeedback**（20万样本）。
    *   **通用强化学习**：使用了 MuJoCo 物理模拟环境中的 **HalfCheetah-v4** 任务，验证其在连续控制领域的泛化性。
*   **基准方法（Benchmarks）**：对比了标准 **GRPO**、**DPO**（直接偏好优化）、**ORPO** 以及传统的 **PPO**。
*   **评估指标**：自动对齐评分、人类偏好胜率（5分制 Likert 量表）、梯度范数方差、KL 散度标准差等稳定性指标。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 PyTorch 和 Hugging Face Transformers 生态系统，支持混合精度训练和梯度裁剪，并发布了可复现的代码。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 7B 参数的 LLaMA-2 模型对齐到经典的连续控制 RL 任务。
    *   进行了详细的**消融实验**，验证了动态步长衰减、信任区域约束和显式局部约束对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅验证了性能（胜率），还深入分析了训练稳定性（方差分析）。引入 MuJoCo 任务证明了该方法不局限于文本生成，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能层级**：实验结果显示出清晰的性能阶梯：**Hard-Pair-GRPO > Soft-Pair-GRPO > DPO/ORPO > 标准 GRPO**。
*   **稳定性提升**：Hard-Pair-GRPO 在梯度方差和 KL 漂移方面表现最稳，收敛速度最快且轨迹最平滑。
*   **奖励冗余性**：理论和实验均证明，标量奖励的绝对量级对于偏好学习并非必要，相对的二元顺序足以驱动高质量对齐。
*   **通用性**：该家族在离散动作空间（LLM）和连续动作空间（MuJoCo）中均优于现有 SOTA 方法。

### 7. 优点
*   **理论严密**：提出了梯度等价定理，为二元偏好奖励的有效性提供了数学解释。
*   **稳定性强**：通过显式分布约束显著降低了 RL 训练中常见的噪声和不稳定性。
*   **即插即用**：Soft 变体对现有 GRPO 框架改动极小，易于集成。
*   **跨领域验证**：成功将 LLM 对齐技术迁移至通用机器人控制任务。

### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的资源消耗报告，难以评估其在大规模模型（如 70B+）上的计算开销。
*   **任务覆盖面**：虽然引入了 MuJoCo，但仅测试了一个子任务（HalfCheetah），在更复杂的长程规划或多智能体任务中的表现尚待验证。
*   **超参数敏感性**：Hard 变体引入了动态步长 $\delta$ 和惩罚系数 $\alpha$，可能需要针对不同任务进行精细调优。

（完）
