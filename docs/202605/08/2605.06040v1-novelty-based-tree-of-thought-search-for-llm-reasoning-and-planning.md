---
title: Novelty-based Tree-of-Thought Search for LLM Reasoning and Planning
title_zh: 基于新颖性的思维树搜索在大语言模型推理与规划中的应用
authors: "Leon Hamm, Zlatan Ajanovic"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06040v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于新颖性的LLM推理与规划思维树搜索
tldr: 针对大语言模型在推理和规划任务中表现脆弱且成本高的问题，本文受规划领域宽度搜索启发，提出了一种基于“新颖性”的思维树搜索方法。该方法通过评估新思维节点相对于已搜索节点的唯一性来进行剪枝，从而缩小搜索空间。实验证明，该方法在降低总Token成本的同时，有效提升了模型在语言规划和通用推理任务中的效率与性能。
selection_source: fresh_fetch
motivation: 现有的思维树搜索方法在处理复杂任务时面临搜索空间过大、Token消耗高以及推理过程不够鲁棒的问题。
method: 提出一种基于LLM预训练知识的可衡量新颖性指标，用于识别并剪除搜索树中重复或冗余的思维分支。
result: 在多个推理和规划基准测试中，该方法显著减小了搜索树规模，在降低计算成本的同时提高了任务成功率。
conclusion: 引入新颖性剪枝机制能有效优化LLM的搜索效率，为实现更高效、更具类人水平的推理和规划提供了新思路。
---

## 摘要
尽管思维链、思维树或强化学习等进展提升了大语言模型（LLM）在推理和规划任务中的表现，但它们在许多领域仍然表现脆弱，尚未达到人类水平，且往往面临较高的时间和 Token 成本。受规划领域中基于宽度的搜索取得成功的启发，我们探索了如何将“新颖性”概念迁移到语言领域，以及它如何改进思维树推理。思维树依赖于构建由连续想法或思维组成的可能“路径”，这些路径是通过重复提示 LLM 生成的。本文提出了一个可测量的新颖性概念，用于描述新节点（思维）与搜索树中先前出现的节点相比的唯一性。新颖性通过提示 LLM 并利用其预训练中嵌入的通用知识进行估算。该指标随后可用于剪枝并缩小搜索范围。尽管这种方法在每个状态下引入了更多提示，但通过剪枝和减小整体树的规模，可以降低总体的 Token 成本。我们在基于语言的规划和通用推理的多个基准测试中对该方法进行了测试和比较。

## Abstract
Although advances such as chain-of-thought, tree-of-thought or reinforcement learning have improved the performance of LLMs in reasoning and planning tasks, they are still brittle and have not achieved human-level performance in many domains, and often suffer from high time and token costs. Inspired by the success of width-based search in planning, we explore how the concept of novelty can be transferred to language domains and how it can improve tree-of-thought reasoning. A tree of thoughts relies on building possible "paths" of consecutive ideas or thoughts. These are generated by repeatedly prompting an LLM. In our paper, a measurable concept of novelty is proposed that describes the uniqueness of a new node (thought) in comparison to nodes previously seen in the search tree. Novelty is estimated by prompting an LLM and making use of embedded general knowledge from pre-training. This metric can then be used to prune branches and reduce the scope of the search. Although this method introduces more prompts per state, the overall token cost can be reduced by pruning and reducing the overall tree size. This procedure is tested and compared using several benchmarks in language-based planning and general reasoning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合经典规划算法思想与大语言模型（LLM）的新型搜索框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 LLM 在简单任务中表现出色，但在复杂推理和长期规划任务中表现脆弱。现有的思维树（Tree of Thoughts, ToT）虽然引入了搜索机制，但面临指数级的搜索空间和极高的 Token 消耗。
*   **背景**：在经典规划领域，基于宽度的搜索（如迭代宽度算法 IW）通过“新颖性（Novelty）”剪枝，能高效解决许多复杂问题。
*   **核心问题**：如何将经典规划中的“新颖性”概念引入自然语言领域，利用 LLM 的语义理解能力进行状态剪枝，从而在降低 Token 成本的同时提升推理性能。

### 2. 论文提出的方法论
核心思想是将规划任务分解为四个由 LLM 驱动的子任务，并引入基于新颖性的剪枝机制：
*   **核心流程**：
    1.  **动作生成（Action Generation）**：LLM 根据当前状态生成可能的下一步动作。
    2.  **后继状态映射（Successor State Mapping）**：LLM 预测执行动作后的新状态。
    3.  **计划验证（Plan Verification）**：判断当前状态是否达成目标。
    4.  **新颖性估计（Novelty Estimation）**：这是核心创新点。LLM 接收当前新状态和已访问状态列表，通过提示词判断该状态是否具有“新颖性”（即是否包含之前未见的特征）。
*   **关键技术细节**：
    *   **二元判断**：由于 LLM 难以处理复杂的整数新颖性评分，研究者将其简化为“是/否”的二元提示。
    *   **领域无关性**：不使用 Few-shot（少样本）示例，仅通过环境规则的上下文提示，利用 LLM 的预训练知识来识别状态特征。
    *   **搜索策略**：支持深度优先搜索（DFS）和广度优先搜索（BFS），并利用新颖性剪枝使 BFS 在 LLM 场景下变得可行。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **Blocksworld（积木世界）**：经典的规划基准测试，用于评估动作生成和状态转换。
    *   **Logistics（物流领域）**：测试跨城市运输包裹的规划能力。
    *   **Game of 24（24点游戏）**：用于分析推理问题的潜在“宽度”结构。
    *   **MATH Benchmark (Level 5)**：测试该方法在非规划类、高难度数学推理任务中的泛化能力。
*   **对比方法**：
    *   **Naive Baseline**：直接提示 LLM 给出答案。
    *   **Basic ToT**：不带剪枝的标准思维树搜索。
    *   **Novelty-based ToT**：本文提出的带剪枝的方法。
*   **评估指标**：成功率（Perf）、平均 Token 消耗（ATU）、状态生成数量。

### 4. 资源与算力
*   **模型使用**：主要实验基于 **Qwen-14B**（通义千问），因为它可以在本地运行且推理速度较快。参考对比使用了 GPT-4o 和 GPT-4o-mini。
*   **算力说明**：文中提到 Qwen-14B 是在本地运行的，但**未明确说明具体的 GPU 型号、数量或总训练/推理时长**。

### 5. 实验数量与充分性
*   **实验规模**：在每个领域（积木、物流、数学）通常使用 50 个测试实例。
*   **充分性评价**：
    *   **多维度对比**：实验涵盖了不同的提示风格（PDDL 格式 vs 自然语言）、不同的思考模式（开启/关闭 Thinking 模式）以及不同的搜索算法（BFS vs DFS）。
    *   **客观性**：通过 PDDL 引擎生成地面真值（Ground Truth）进行自动化评估，减少了人工评分的偏差。
    *   **局限性**：虽然覆盖了多个领域，但每个领域的样本量（50个）相对较小，且主要集中在 14B 规模的模型上。

### 6. 主要结论与发现
*   **成本显著降低**：在配置得当的情况下，新颖性剪枝可将 Token 成本降低高达 **20 倍**，同时保持或提升成功率。
*   **BFS 的可行性**：新颖性剪枝极大地缩小了搜索空间，使得原本在 LLM 中因成本过高而无法使用的 BFS 变得实用。
*   **思考模式（Thinking Mode）的影响**：开启模型的内置推理链（Thinking tokens）能显著提升子任务（如动作生成）的准确性。
*   **脆弱性**：LLM 对提示词非常敏感。例如，在积木世界中，模型常混淆“拿起”和“拆解”动作，需要专门的提示词修正。

### 7. 优点
*   **跨学科融合**：成功将经典规划的“宽度搜索”理论应用于非结构化的自然语言推理。
*   **高效剪枝**：通过简单的二元新颖性判断，有效解决了 ToT 搜索空间爆炸的问题。
*   **泛化性**：方法设计尽量保持领域无关，在物流和数学竞赛题目中展现了开箱即用的潜力。

### 8. 不足与局限
*   **新颖性理解深度有限**：定性分析显示，LLM 的“新颖性判断”在很大程度上仍依赖于**重复检测（Duplicate Detection）**，而非深层的语义特征提取。
*   **配置依赖性强**：如果提示词或参数配置不当，新颖性查询本身会增加额外的 Token 开销，甚至导致搜索失败。
*   **模型规模限制**：在较小模型上表现不稳定，复杂的新颖性推理（NDAP 变体）在 14B 模型上几乎完全失败。
*   **应用限制**：对于状态空间极小或目标极其明确的任务，搜索带来的开销可能超过其收益。

（完）
