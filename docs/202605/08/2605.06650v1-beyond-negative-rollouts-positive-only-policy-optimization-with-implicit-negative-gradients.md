---
title: "Beyond Negative Rollouts: Positive-Only Policy Optimization with Implicit Negative Gradients"
title_zh: 超越负向采样：具有隐式负梯度的纯正向策略优化
authors: "Mingwei Xu, Hao Fang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06650v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用带有可验证奖励的强化学习增强大语言模型的推理能力
tldr: 针对大语言模型推理任务中负样本采样效率低且奖励稀疏的问题，本文提出POPO框架。该方法仅利用在线正样本进行策略优化，通过有界重要性采样产生隐式负梯度，并引入动量更新的孪生网络与相似度惩罚机制以稳定训练。实验证明POPO在数学竞赛任务上优于GRPO，为强化学习提供了更高效的优化路径。
selection_source: fresh_fetch
motivation: 传统的GRPO依赖负样本进行优势估计，但在稀疏奖励下，有限的负样本难以覆盖复杂的错误空间且缺乏失败程度的区分。
method: 提出POPO框架，仅使用正样本进行有界重要性采样，并结合动量更新的孪生策略网络和表征空间相似度惩罚来稳定优化过程。
result: "在Qwen系列模型上的实验显示，POPO在AIME 2025等数学基准测试中表现优异，准确率从GRPO的30.00%提升至36.67%。"
conclusion: POPO证明了仅依靠正样本也能实现高效的策略优化，为提升大语言模型的逻辑推理能力提供了一种更稳健且高效的新方案。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 因其确定性的验证方式，已成为增强大语言模型 (LLM) 推理能力的主流范式。社区见证了从近端策略优化 (PPO) 到群体相对策略优化 (GRPO) 的快速转变，其中 GRPO 通过对分组的正向和负向采样进行简单估计，简化了复杂的优势估计。然而，我们注意到负向采样可能无法体现失败严重程度的分级，且在稀疏二元奖励下，组合空间的巨大性使得惩罚少数采样的负样本难以覆盖有意义的奖励信号。在这项工作中，我们提出了纯正向策略优化 (POPO)，这是一种新型的 RLVR 框架，其中学习可以完全通过在线正向采样进行。具体而言，POPO 对正向采样集利用有界重要性采样。因此，梯度引导中不使用不相交的负向采样。我们展示了通过采样重分布强化正向概率，可以自然地产生隐式负梯度。接着，POPO 通过两种机制稳定策略优化。首先，它采用具有基于动量的自适应法则的孪生策略网络，以实现稳定的策略演进。其次，我们在孪生表示空间中用有界相似性惩罚项取代了 KL 散度。我们使用公开且成熟的文本 LLM 模型（例如 Qwen 系列），在各级数学基准测试中进行了广泛的实验。我们的实验表明，POPO 的性能与 GRPO 相当，甚至优于 GRPO。值得注意的是，我们展示了 POPO 使用 Qwen-Math-7B 在 AIME 2025 中可以达到 36.67%，优于 GRPO 的 30.00%。我们的消融实验和参数扫描研究进一步说明了 POPO 组件的必要性和鲁棒性。

## Abstract
Reinforcement learning with verifiable rewards (RLVR), due to the deterministic verification, becomes a dominant paradigm for enhancing the reasoning ability of large language models (LLMs). The community witnesses the rapid change from the Proximal Policy Optimization (PPO) to Group Relative Policy Optimization (GRPO), in which GRPO reduces the complicated advantage estimation with simple estimation over grouped positive and negative rollouts. However, we note that negative rollouts may admit no gradation of failure severity, and the combinatorial vastness makes penalizing a few sampled negatives unlikely to cover a meaningful reward signal under sparse binary rewards. In this work, we propose Positive-Only Policy Optimization (POPO), a novel RLVR framework in which learning can occur exclusively via online positive rollouts. Specifically, POPO utilizes bounded importance sampling over the positive rollout set. Thus, no disjoint negative rollouts are used for the gradient guidance. We show that implicit negative gradients can emerge naturally through reinforcing the positive probability via rollouts redistribution. Next, POPO stabilizes the policy optimization through two mechanisms. First, it applies a siamese policy network with a momentum-based adaptation law for stabilized policy evolution. Second, we replace the KL-divergence with a bounded similarity penalty term in the siamese representation space. We conduct extensive experiments using publicly available, well-established text-LLM models, e.g., the Qwen family, across all-level mathematical benchmarks. Our experiment demonstrates that POPO achieves performance comparable to, or even superior to GRPO. Notably, we show that POPO can achieve 36.67% in AIME 2025 with Qwen-Math-7B, outperforming GRPO 30.00%. Our ablation and sweep studies further illustrate the necessity and robustness of POPO components.

---

## 论文详细总结（自动生成）

这篇论文提出了 **POPO (Positive-Only Policy Optimization)**，这是一种针对大语言模型（LLM）推理任务的新型强化学习框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在具有可验证奖励的强化学习（RLVR）中，现有的主流方法（如 GRPO）依赖于对“正向采样（正确答案）”和“负向采样（错误答案）”的对比学习。
*   **研究动机**：
    *   **负样本空间巨大**：数学等推理任务的错误形式无穷无尽，仅惩罚采样到的少数负样本难以覆盖复杂的错误模式。
    *   **奖励稀疏且无梯度**：二元奖励（对/错）无法体现“错误程度”的区别。
    *   **借鉴自监督学习**：受视觉领域（如 BYOL, SimSiam）无需负样本也能学习强大表征的启发，作者探讨是否可以仅通过“成功经验”来优化策略。

### 2. 方法论：POPO 核心思想与技术细节
POPO 的核心在于**仅利用在线生成的正向采样进行学习**，并通过以下机制确保稳定性和有效性：
*   **自归一化权重（Self-Normalized Weight）**：在正样本集中进行有界重要性采样，优先强化那些模型已经有一定把握的正确答案，形成内部竞争。
*   **隐式负梯度（Implicit Negative Gradients）**：通过数学证明（定理 3.1），在 Softmax 归一化和熵正则化的共同作用下，强化正样本概率会自然地产生一种“概率税”，从而隐式地降低负样本的对数几率（Logits）。
*   **自适应锚点（Adaptive Anchor）**：采用**孪生网络（Siamese Network）**架构。包含一个在线策略网络（梯度更新）和一个目标策略网络（通过动量 EMA 更新），作为稳定的行为锚点。
*   **表征空间对齐（Representation-Space Alignment）**：放弃传统的概率空间 KL 散度，改用隐藏层表征空间的**余弦相似度惩罚**，并配合预测头（Predictor Head）和停止梯度（Stop-gradient）操作，防止模型坍缩。
*   **熵正则化**：通过熵奖励鼓励模型保持探索能力，防止过早收敛到单一路径。

### 3. 实验设计
*   **数据集**：统一使用 `DeepScaleR-Preview-Dataset`（约 4 万个数学问题）进行训练。
*   **评测基准**：涵盖小学到奥赛级别的数学测试，包括 MATH-500、AMC 23、AIME 2024/2025、OlympiadBench。
*   **对比方法**：Base 模型、SFT（监督微调）、GRPO、Dr.GRPO、DAPO、SAPO。
*   **测试模型**：Qwen2.5-Math (1.5B/7B)、DeepSeek-R1-Distill (1.5B/7B)、Llama-3.1-8B、DeepSeek-Math-7B。

### 4. 资源与算力
*   **硬件**：使用 1 个 Kubernetes 节点，配备 **4 张 NVIDIA A100 80GB GPU**。
*   **时长**：
    *   1.5B 模型：训练 1 个 Epoch 约需 **11 小时**。
    *   7B 模型：训练 1 个 Epoch 约需 **24 小时**。
    *   推理阶段：使用 vLLM 引擎，约需 15 分钟。

### 5. 实验数量与充分性
*   **实验规模**：在 6 种不同规模和类型的基座模型上进行了验证，涵盖了数学专用模型、蒸馏模型和通用模型。
*   **消融实验**：详细测试了重要性采样、权重重分布、动量更新、表征对齐等组件的必要性。
*   **参数扫描**：对预测层数、维度、动量系数等超参数进行了敏感度分析。
*   **评价**：实验设计较为充分且客观，通过在 AIME 2025（模型发布后的新题）上的表现排除了数据泄露的嫌疑，对比基准线清晰。

### 6. 主要结论与发现
*   **性能优异**：POPO 在多个基准上达到或超过了 GRPO 及其变体。在 AIME 2025 上，Qwen-Math-7B 使用 POPO 达到了 36.67% 的准确率，显著高于 GRPO 的 30.00%。
*   **任务越难优势越明显**：在复杂推理任务中，POPO 的提升幅度更大，验证了“负样本在复杂空间中信号微弱”的假设。
*   **无需负样本可行**：证明了通过概率重分布和表征对齐，仅靠正样本也能实现稳健的策略提升。

### 7. 优点
*   **理论完备**：不仅有实验，还提供了隐式负梯度和参数偏差有界的数学证明。
*   **训练稳定**：引入孪生网络和 EMA 机制，有效解决了纯正样本学习容易出现的模型坍缩或过拟合问题。
*   **效率潜力**：在采样阶段，如果能快速识别正样本，可能减少对大量无效负样本的处理开销。

### 8. 不足与局限
*   **蒸馏模型效果略逊**：在 DeepSeek-R1-Distill-7B 上，POPO 略微落后于 GRPO，作者推测是因为蒸馏模型本身熵较低，限制了在线探索能力。
*   **奖励类型受限**：目前仅针对稀疏的二元奖励（对/错），尚未扩展到密集奖励（Process Reward）场景。
*   **模型规模**：实验最高仅做到 7B/8B 规模，尚未在 32B 或 70B 等更大参数量的模型上验证扩展性。

（完）
