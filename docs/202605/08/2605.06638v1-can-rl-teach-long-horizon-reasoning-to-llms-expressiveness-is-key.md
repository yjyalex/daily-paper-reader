---
title: Can RL Teach Long-Horizon Reasoning to LLMs? Expressiveness Is Key
title_zh: RL 能教会 LLM 长程推理吗？表达能力是关键
authors: "Tianle Wang, Zhaoyang Wang, Guangchen Lan, Xinpeng Wei, Sipeng Zhang, Guanwen Qiu, Abulhair Saparov"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06638v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 强化学习应用于提升不同逻辑表达力下的 LLM 推理能力
tldr: 本研究通过 ScaleLogic 框架探讨强化学习如何提升 LLM 的长程推理能力。研究发现 RL 训练计算量与推理深度呈幂律关系，且该关系的扩展指数随逻辑表达力的增强而增大。实验表明，高表达力的逻辑训练能显著提升模型在数学等下游任务中的迁移效率，证明了训练内容的逻辑复杂性是提升推理能力的关键。
selection_source: fresh_fetch
motivation: 缺乏受控且可扩展的环境来系统研究强化学习在不同任务难度下提升 LLM 推理能力的扩展规律。
method: 提出 ScaleLogic 合成框架，独立控制推理深度和逻辑表达力（从简单蕴含到一阶逻辑）进行 RL 训练实验。
result: 发现训练计算量与推理深度满足幂律关系，且逻辑表达力越强，下游任务的性能增益和迁移效率越高。
conclusion: 逻辑表达力是提升长程推理能力的核心，且通过课程学习可以显著优化 RL 训练的扩展效率。
---

## 摘要
强化学习 (RL) 已被应用于提升大语言模型 (LLM) 的推理能力，但由于缺乏受控且可扩展的环境，关于训练如何随任务难度扩展的系统性研究一直受到阻碍。我们引入了 ScaleLogic，这是一个合成逻辑推理框架，它提供了对两个难度维度的独立控制：所需证明规划的深度（即长程性）和底层逻辑的表达能力。我们提出的框架支持广泛的逻辑：从简单的仅含蕴涵的逻辑（“如果-那么”）到更具表达能力的一阶推理，包括合取（“且”）、析取（“或”）、否定（“非”）和全称量化（“对于所有”）。利用该框架，我们展示了 RL 训练计算量 $T$ 与推理深度 $D$ 之间遵循幂律关系（$T \propto D^γ$，$R^{2} > 0.99$），且扩展指数 $γ$ 随逻辑表达能力的增强而单调增加，从 $1.04$ 增加到 $2.60$。在下游数学和通用推理基准测试中，与表达能力较低的设置相比，表达能力较高的训练设置不仅带来了更大的性能提升（最高达 $+10.66$ 分），而且实现了更具计算效率的迁移，这表明模型训练的内容（而不仅仅是训练量）决定了下游迁移的效果。我们进一步证明，这种幂律关系在多种 RL 方法中均成立，且基于课程的训练显著提高了扩展效率。

## Abstract
Reinforcement learning (RL) has been applied to improve large language model (LLM) reasoning, yet the systematic study of how training scales with task difficulty has been hampered by the lack of controlled, scalable environments. We introduce ScaleLogic, a synthetic logical reasoning framework that offers independent control over two axes of difficulty: the depth of the required proof planning (i.e., the horizon) and the expressiveness of the underlying logic. Our proposed framework supports a wide range of logics: from simple implication-only logic ("if-then") towards more expressive first-order reasoning with conjunction ("and"), disjunction ("or"), negation ("not"), and universal quantification ("for all"). Using this framework, we show that the RL training compute $T$ follows a power law with respect to reasoning depth $D$ ($T \propto D^γ$, $R^{2} > 0.99$), and that the scaling exponent $γ$ increases monotonically with logical expressiveness, from $1.04$ to $2.60$. On downstream mathematics and general reasoning benchmarks, more expressive training settings yield both larger performance gains (up to $+10.66$ points) and more compute-efficient transfer compared to less expressive settings, demonstrating that what a model is trained on, not just how much it is trained, shapes downstream transfer. We further show that the power-law relationship holds across multiple RL methods, and curriculum-based training substantially improves scaling efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can RL Teach Long-Horizon Reasoning to LLMs? Expressiveness Is Key》的结构化深度总结：

### 1. 核心问题与整体含义
论文探讨了**强化学习（RL）如何影响大语言模型（LLM）的长程推理能力**。尽管 RL 在数学和代码领域取得了进展，但由于缺乏能够独立控制“推理深度”和“逻辑复杂度”的受控环境，研究人员难以系统性地分析训练难度与计算量之间的扩展规律。
**核心动机**：研究 RL 训练在面对越来越难的长程推理问题时是如何扩展的，以及训练数据的逻辑表达力（Expressiveness）如何影响模型向真实世界任务的迁移能力。

### 2. 方法论
论文提出了 **SCALE LOGIC** 框架，这是一个可扩展、可验证且高度受控的合成逻辑推理环境。
*   **核心思想**：通过“逆向证明树构造”生成逻辑问题。从结论出发，递归地添加前提，直到达到预设的推理深度 $D$。
*   **两个控制维度**：
    1.  **推理深度 ($D$)**：控制证明链条的长度（即 Horizon）。
    2.  **逻辑表达力**：定义了五个递进的逻辑等级：
        *   **仅含蕴含 (Implication-only)**：简单的“如果-那么”。
        *   **+合取 (+Conjunction)**：引入“且”逻辑，需同时满足多个前提。
        *   **+否定 (+Negation)**：引入“非”逻辑，需追踪命题的正负极性。
        *   **+析取 (+Disjunction)**：引入“或”逻辑，需处理多种可能性。
        *   **+量化 (+Quantification)**：引入全称量化（对于所有），涉及一阶逻辑推理。
*   **RL 训练流程**：使用 **DAPO**（GRPO 的变体）算法，采用可验证的二元奖励（回答正确为 1，错误或格式不对为 0）。模型需要从多个选项中识别出唯一逻辑可导的结论。

### 3. 实验设计
*   **模型**：主要使用 **Qwen3-4B**（非思考版），部分实验在 **Qwen3-8B** 上重复以验证一致性。
*   **数据集/场景**：
    *   **合成数据**：SCALE LOGIC 生成的不同深度和表达力的逻辑题。
    *   **下游基准 (Benchmarks)**：AIME 2024/2025、AMC 2023、MATH-500、Minerva、OlympiadBench、GPQA-Diamond、MMLU-Pro (STEM 部分)。
*   **对比维度**：
    *   不同逻辑表达力等级的对比。
    *   不同训练分布（均匀分布、仅困难样本、课程学习）的对比。
    *   不同 RL 算法（DAPO、GRPO、GSPO）的对比。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA B200 (180G) GPU**。
*   **软件框架**：基于 **verl** 强化学习库。
*   **训练细节**：每个配置训练一个独立模型，训练步数根据达到 90% 验证准确率所需的时间计算。虽然未给出总机时，但实验涵盖了从深度 4 到 128 的多种组合，计算量巨大。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 5 种逻辑等级在多个深度下的扩展曲线绘制。
*   **充分性**：
    *   **多算法验证**：证明了幂律关系在不同 RL 算法下均成立。
    *   **跨规模验证**：在 4B 和 8B 模型上观察到了相似的趋势。
    *   **消融实验**：针对实体数量、候选选项数量等潜在干扰因素进行了消融。
    *   **客观性**：使用了 Z3 求解器对合成数据的逻辑正确性进行了审计，确保标签无误。

### 6. 主要结论与发现
1.  **幂律扩展规律**：RL 训练步数 $T$ 与推理深度 $D$ 满足幂律关系 $T \propto D^\gamma$ ($R^2 > 0.99$)。
2.  **表达力决定指数**：扩展指数 $\gamma$ 随逻辑表达力的增强而单调增加（从 **1.04** 增加到 **2.60**）。这意味着逻辑越复杂，增加推理深度带来的训练成本增长越剧烈。
3.  **表达力是迁移的关键**：在相同计算量下，**高表达力**数据的训练能带来显著更高的下游任务增益（平均提升达 **+10.66** 分），而低表达力训练很快会遇到瓶颈。
4.  **课程学习的有效性**：采用从易到难的课程学习（Curriculum Training）可以显著降低扩展指数 $\gamma$，提高训练效率。

### 7. 优点
*   **高受控性**：解决了数学/代码数据中深度与复杂度耦合的问题，实现了真正的变量分离研究。
*   **理论与实证结合**：首次量化了逻辑表达力对 RL 扩展效率的具体影响。
*   **实用性强**：证明了合成逻辑数据可以作为提升 LLM 通用推理能力的有效补丁，且指出了“训练内容”比“训练量”更重要。

### 8. 不足与局限
*   **合成与真实的鸿沟**：虽然下游任务有提升，但合成逻辑题与自然语言推理之间仍存在分布差异。
*   **模型规模限制**：实验最高只到 8B 模型，尚不确定在千亿级参数模型上 $\gamma$ 指数是否会发生质变。
*   **深度范围有限**：受限于算力，极高深度（如 $D > 128$）的实验未能覆盖，幂律关系在渐进意义上的表现仍需观察。
*   **缺乏理论推导**：论文给出了经验公式，但未从计算复杂性理论角度给出 $\gamma$ 随逻辑算子变化的严格理论解释。

（完）
