---
title: Policy-Guided Stepwise Model Routing for Cost-Effective Reasoning
title_zh: 策略引导的逐步模型路由，实现高性价比推理
authors: "Wenwen Si, Insup Lee, Osbert Bastani"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06116v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对推理任务中逐步模型路由的强化学习
tldr: 针对大语言模型推理成本高昂的问题，本文提出了一种策略引导的分步模型路由方法。该方法将推理过程建模为受限决策问题，通过强化学习训练小型控制策略，在推理过程中动态选择不同规模的模型处理中间思维链状态。实验表明，该方法在多个数学基准测试中显著优化了准确率与成本的权衡，性能优于手工规则且媲美大型过程奖励模型。
selection_source: fresh_fetch
motivation: 现有的推理路由方法依赖手工规则或昂贵的大型过程奖励模型，难以在推理性能与计算成本之间取得最优平衡。
method: 将分步模型路由建模为受限决策问题，利用强化学习训练小型控制策略，并结合阈值校准来动态调节性能与效率的权衡。
result: 在GSM8K、MATH500和OmniMath等数学基准上，该方法在保持高准确率的同时显著降低了推理成本，优于传统的手工路由策略。
conclusion: 策略引导的动态路由是一种高效且低成本的推理优化方案，能有效提升大语言模型在复杂推理任务中的性价比。
---

## 摘要
推理时计算显著提升了大语言模型（LLMs）在挑战性推理任务上的性能，但该策略会带来高昂的推理成本。一种解决方案是将中间思维链（CoT）状态路由至不同规模的语言模型；然而，现有方法要么依赖于限制性能的手工路由策略，要么依赖于训练大型过程奖励模型，而后者在许多应用场景中并不可行。我们将逐步模型路由建模为一个受限决策问题，通过强化学习训练小型控制策略，并结合阈值校准来优化性能与效率之间的权衡。我们在开源和闭源模型上，利用三个数学基准测试（GSM8K、MATH500 和 OmniMath）对该方法进行了验证。实验结果表明，与手工方法相比，我们的方法持续改进了准确率与成本的权衡，并达到了与需要训练大型过程奖励模型的方法相媲美的效果。

## Abstract
Inference-time computation has greatly enhanced the performance of large language models (LLMs) on challenging reasoning tasks, but this strategy can incur high inference costs. One solution is to route intermediate chain-of-thought (CoT) states to language models of different sizes; however, existing approaches rely on handcrafted routing strategies that limit performance, or on training large process reward models that may be infeasible in many applications. We formulate stepwise model routing as a constrained decision-making problem, which we solve by training a small control policy using reinforcement learning in conjunction with threshold calibration to tune the performance-efficiency tradeoff. We validate our method on three math benchmarks (GSM8K, MATH500, and OmniMath) on both open and closed models. Our method consistently improves the accuracy-cost tradeoff compared to handcrafted approaches, while achieving a comparable tradeoff to methods that require training large process reward models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Policy-Guided Stepwise Model Routing for Cost-Effective Reasoning》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着思维链（CoT）等推理技术的发展，大语言模型（LLM）在复杂任务上的表现显著提升，但随之而来的是推理成本（计算量和API费用）的剧增。
*   **核心问题**：如何在保证推理准确率的前提下，通过在不同规模的模型之间动态切换（路由），最大限度地降低推理成本？
*   **研究背景**：现有的逐步路由方法要么依赖于简单的手工规则（如设定固定的不确定性阈值），要么需要训练庞大且昂贵的过程奖励模型（PRM）。本文旨在寻找一种既能捕捉步骤间依赖关系，又足够轻量、无需大规模PRM训练的高性价比方案。

### 2. 方法论
论文将逐步模型路由建模为一个**受限马尔可夫决策过程（CMDP）**，并提出了一种策略引导的路由框架。
*   **核心思想**：训练一个小型神经网络作为“控制策略”，在推理的每一个步骤决定是继续使用廉价的小模型，还是升级（Escalate）到昂贵的大模型。
*   **关键技术细节**：
    *   **策略网络**：采用一个极小的三层多层感知机（MLP），输入特征包括当前步骤的置信度（Logits或API返回的Log-probs）、步骤索引、问题难度指标、当前步骤长度等。
    *   **强化学习优化**：使用带有 V-trace 离线策略校正的信任区域策略优化（类似CPO算法），在最小化推理成本的同时，满足预设的准确率约束（即相对于大模型的性能损失在 $\alpha$ 范围内）。
    *   **阈值校准**：在训练过程中动态调整决策阈值 $\kappa$，利用统计学中的保形预测（Conformal Prediction）思想，确保在测试集上能以高概率满足准确率要求。
    *   **局部验证辅助**：在训练阶段引入一个辅助的验证模型（如GPT-4o-mini）来检查步骤正确性，但这仅用于策略训练，不增加推理时的开销。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**：小学数学应用题。
    *   **MATH500**：高中水平数学竞赛题。
    *   **OmniMath**：奥林匹克竞赛级别的极难数学题。
*   **模型组合**：
    *   **开源设置**：Qwen2.5-Math-1.5B（小）与 7B（大）。
    *   **开源转闭源设置**：Qwen2.5-Math-7B（小）与 GPT-4o-mini（大）。
*   **对比方法（Baselines）**：
    *   **单一模型**：仅使用小模型或仅使用大模型。
    *   **SpecReason**：使用LLM作为裁判进行口头评分。
    *   **RSD (Reward-Guided Speculative Decoding)**：依赖于预训练的过程奖励模型（PRM）。
    *   **STEER**：基于高斯混合模型（GMM）的不确定性路由方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的GPU型号、数量或总训练时长。
*   **训练细节**：提到策略网络非常轻量（3层，每层64个隐藏单元），训练步数为1000步，学习率为 $10^{-3}$，Batch Size为5。由于策略网络极小且输入特征维度低，其训练和推理的计算开销相对于LLM本身可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在3个不同难度的数学基准上进行了测试，涵盖了从开源到闭源模型的多种组合。
*   **充分性**：实验对比了当前最先进的基于PRM的方法（RSD）和基于不确定性的方法（STEER），并报告了准确率、FLOPs消耗、API成本以及“准确率/成本比（A/C）”等多个维度。
*   **客观性**：基线方法均通过网格搜索选择了最优阈值以确保公平对比；作者还使用了 xVerify 这一专门的验证工具来减少数学格式不统一带来的评估偏差。

### 6. 主要结论与发现
*   **性能优越**：在所有数学基准上，该方法在相同的成本下实现了更高的准确率，或者在相同的准确率下显著降低了成本。
*   **超越手工规则**：相比于 STEER 等基于简单阈值的方法，策略引导的方法能更好地捕捉推理步骤间的序列依赖。
*   **媲美PRM**：在无需训练和运行大型PRM的情况下，该方法达到了与 RSD 相当的成本效益权衡。
*   **开源模型效果更佳**：在开源模型上（可获取完整Logits），路由效果优于仅能获取Top-5 Log-probs的闭源API环境。

### 7. 优点
*   **高性价比**：策略网络极小，推理时几乎不增加额外延迟和计算负担。
*   **自动化**：通过阈值校准自动匹配用户设定的准确率目标，无需人工反复调参。
*   **通用性**：框架不依赖于特定模型架构，可灵活应用于开源模型和闭源API。

### 8. 不足与局限
*   **API限制**：在闭源API环境下，由于只能获取有限的概率信息（Top-5），路由信号的质量受限，导致性能提升不如开源环境显著。
*   **领域局限**：实验目前仅集中在数学推理任务上，尚未在代码生成、创意写作等非结构化推理任务中验证。
*   **训练依赖**：虽然推理时很轻量，但训练阶段仍需要获取一定量的标注数据或使用更强的模型进行步骤级验证。

（完）
