---
title: "Schedule-and-Calibrate: Utility-Guided Multi-Task Reinforcement Learning for Code LLMs"
title_zh: 调度与校准：面向代码大语言模型的效用引导多任务强化学习
authors: "Yujia Chen, Yang Ye, Xiao Chu, Yuchi Ma, Cuiyun Gao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06111v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于代码生成和任务协调的多任务强化学习
tldr: 针对代码大模型在多任务强化学习中任务处理不均和优化策略固定的问题，本文提出ASTOR框架。该框架以“任务效用”为核心，通过层级化数据调度优化训练预算分配，并利用自适应策略优化动态调整KL正则化约束。实验证明，ASTOR在多项代码任务上显著优于单任务专家模型和现有基准，实现了高效且均衡的多任务性能提升。
selection_source: fresh_fetch
motivation: 现有的多任务强化学习方法对不同代码任务采取统一处理方式，忽略了任务间的学习潜力和协同效应，限制了模型的整体表现。
method: 提出ASTOR框架，包含层级化效用路由数据调度模块和自适应效用校准策略优化模块，实现动态的资源分配与更新约束调整。
result: "在两个主流大模型和四个代码任务上的实验显示，ASTOR性能超过了最佳单任务专家模型9.0%-9.5%，并优于最强MTRL基准7.5%-12.8%。"
conclusion: 通过效用驱动的协调机制，ASTOR成功解决了多任务代码强化学习中的资源分配与策略失衡问题，为构建全能型代码大模型提供了有效方案。
---

## 摘要
具有可验证奖励的强化学习（RL）已被证明在代码大语言模型（LLMs）的后训练中非常有效，但部署独立的特定任务专家模型会产生随任务数量增加而增长的成本，这促使了统一的多任务强化学习（MTRL）方法的发展。然而，现有的 MTRL 方法统一对待所有代码任务，依赖于共享优化策略下的固定数据课程，最终限制了多任务训练的效果。为了解决这些局限性，我们提出了 ASTOR，这是一个通过效用驱动协调实现的多任务代码强化学习框架。ASTOR 以任务效用（一种捕捉每个任务学习潜力和跨任务协同作用的信号）为核心，包含两个耦合模块：1) 分层效用路由数据调度模块，分层分配训练预算并优先处理信息丰富的提示词，引导训练转向最有价值的数据；2) 自适应效用校准策略优化模块，动态缩放每个任务的 KL 正则化，使更新约束与每个任务当前的训练状态相匹配。在两个广泛使用的 LLM 和四个代表性代码任务上的实验表明，ASTOR 在所有任务上持续改进了单一模型，性能优于最佳的特定任务专家模型 9.0%-9.5%，并超过了最强的 MTRL 基准模型 7.5%-12.8%。

## Abstract
Reinforcement learning (RL) with verifiable rewards has proven effective at post-training LLMs for coding, yet deploying separate task-specific specialists incurs costs that scale with the number of tasks, motivating a unified multi-task RL (MTRL) approach. However, existing MTRL methods treat all coding tasks uniformly, relying on fixed data curricula under a shared optimization strategy, ultimately limiting the effectiveness of multi-task training. To address these limitations, we propose ASTOR, a multi-tASk code reinforcement learning framework via uTility-driven coORdination. Centered on task utility, a signal capturing each task learning potential and cross-task synergy, ASTOR comprises two coupled modules: 1) Hierarchical Utility-Routed Data Scheduling module hierarchically allocates training budget and prioritizes informative prompts, steering training toward the most valuable data and 2) Adaptive Utility-Calibrated Policy Optimization module dynamically scales per-task KL regularization, matching update constraints to each tasks current training state. Experiments on two widely-used LLMs across four representative coding tasks demonstrate that ASTOR consistently improves a single model across all tasks, outperforming the best task-specific specialist by 9.0%-9.5% and surpassing the strongest MTRL baseline by 7.5%-12.8%.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ASTOR** 的多任务强化学习框架，旨在提升代码大语言模型（Code LLMs）在处理多种异构编程任务时的性能与效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在代码大模型领域，通过可验证奖励（如执行结果）进行强化学习（RL）已证明非常有效。
*   **核心问题**：
    *   **部署成本高**：为每个开发环节（生成、测试、文档等）训练专门的“专家模型”会导致部署成本随任务数量线性增长。
    *   **多任务学习（MTRL）瓶颈**：现有的多任务学习方法通常采用统一的训练策略，忽略了不同代码任务之间的**异构性**（如语法严谨的任务与语义灵活的任务对优化要求不同）以及任务间的**干扰或协同效应**。
*   **研究目标**：开发一个统一的模型，能够同时胜任多种代码任务，且性能超越独立的专家模型。

### 2. 方法论：ASTOR 框架
ASTOR 的核心思想是引入**任务效用（Task Utility）**信号，动态协调“学什么”和“怎么学”。
*   **任务效用定义**：由**学习潜力**（通过奖励方差衡量，反映模型在该任务上的提升空间）和**跨任务协同**（通过梯度余弦相似度衡量，反映该任务对其他任务的正向贡献）共同决定。
*   **关键技术模块**：
    1.  **层级化效用路由数据调度**：
        *   **任务级分配**：根据任务效用，通过 Softmax 函数动态分配训练预算（Quota），让模型更多地关注高潜力、高协同的任务。
        *   **提示词级优先化**：在任务内部，优先选择模型正在改进且仍有不确定性的提示词（Prompts）。
    2.  **自适应效用校准策略优化**：
        *   基于 GRPO 算法，为不同任务设置自适应的 **KL 正则化系数**。
        *   效用高的任务（学习信号集中）增加 KL 约束以防更新过猛；效用低的任务则放宽约束，允许更激进的探索。
*   **奖励系统**：构建了涵盖代码 I/O 预测（执行正确性）、代码生成（测试通过率）、单元测试生成（覆盖率）和提交信息生成（文本相似度）的四位一体可验证奖励体系。

### 3. 实验设计
*   **数据集/场景**：
    *   **Code I/O 预测**：LeetCode 数据。
    *   **代码生成**：LiveCodeBench 和 Codeforces。
    *   **单元测试生成**：真实 Java 开源仓库。
    *   **提交信息生成**：MCMD 数据集。
*   **Benchmark（评估基准）**：CRUXEval（推理）、Aider-Polyglot（生成）、Defects4J 2.0（测试覆盖率）、MCMDEval+（文档）。
*   **对比方法**：
    *   **STL（单任务学习）**：针对每个任务训练的独立专家模型。
    *   **Joint Learning**：简单的混合任务训练。
    *   **Curriculum Learning**：按固定顺序（如开发流程）进行的课程学习。
    *   **Model Merging**：将多个专家模型进行参数合并（TIES-Merging）。
*   **基础模型**：Qwen2.5-Coder-7B 和 Qwen3-8B。

### 4. 资源与算力
*   **硬件**：使用了 **32 台 Ascend 910B-B3 NPU**。
*   **训练参数**：全局 Batch Size 为 128，每个样本生成 8 个 Rollouts，训练时长为 400 个 Step。
*   **优化器**：AdamW，学习率为 5e-7，采用余弦退火调度。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同架构和规模的基础模型上进行了验证，涵盖了 4 类代表性软件开发任务。
*   **消融实验**：非常详尽，分别验证了数据调度（任务分配、提示词优先化）和策略优化（动态正则化、任务特定正则化）的有效性。
*   **敏感性分析**：对超参数 $\tau$（温度系数）和 $\lambda$（调整率）进行了测试。
*   **充分性评价**：实验设计客观公平，不仅对比了多任务基准，还挑战了单任务专家模型的性能上限，并分析了训练过程中的梯度相似度动态变化，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：ASTOR 在单一模型上实现了全任务提升，平均性能超过最佳单任务专家模型 **9.0%–9.5%**。
*   **超越基准**：相比最强的 MTRL 基准（Joint Learning），性能提升了 **7.5%–12.8%**。
*   **解决干扰**：实验证明，单元测试生成等任务与其他任务存在持久的梯度冲突，而 ASTOR 通过动态调整成功缓解了这种负迁移。

### 7. 优点与亮点
*   **动态协调机制**：不再依赖死板的固定课程，而是根据训练实时的梯度和奖励反馈动态调整，更符合人类学习规律。
*   **统一信号引导**：将数据调度和优化约束统一在“任务效用”这一信号下，逻辑自洽且高效。
*   **实用性强**：直接解决了代码模型部署中“专家模型过多”的痛点，具有很高的工业应用价值。

### 8. 不足与局限
*   **语言覆盖有限**：目前主要集中在 Python 和 Java，尚未验证在更多编程语言（如 Rust, Go）上的泛化性。
*   **文本奖励局限**：对于提交信息生成任务，仍依赖 BLEU/ROUGE 等表面指标，未能完全捕捉语义准确性。
*   **超参数依赖**：虽然有自适应机制，但任务特定的 KL 基础系数（$\beta_{base}$）仍需根据任务类型（代码 vs 文本）手动预设。

（完）
