---
title: "AEM: Adaptive Entropy Modulation for Multi-Turn Agentic Reinforcement Learning"
title_zh: AEM：面向多轮智能体强化学习的自适应熵调制
authors: "Haotian Zhao, Songlin Zhou, Yuxin Zhang, Stephen S. -T. Yau, Wenyu Zhang, Lun Tian, Tianshu Zhu, Yifeng Huang, Yucheng Zeng, Jingnan Gu, Daxiang Dong, Jianmin Wu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00425v2"
tags: ["query:token"]
score: 9.0
evidence: 长交互轨迹中单个步骤的信用分配
tldr: 针对多轮智能体强化学习中稀疏奖励导致的信用分配难题，本文提出了一种无需额外监督的自适应熵调制方法（AEM）。该方法将熵动力学分析从 Token 级提升至响应级，通过响应级不确定性代理来动态缩放优势函数，从而优化探索与利用的平衡。在 ALFWorld 和 SWE-bench 等多个基准测试中，AEM 显著提升了不同规模模型的性能。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在处理长轨迹多轮任务时，依赖复杂的中间监督信号且泛化性有限，难以有效解决稀疏奖励下的信用分配问题。
method: 提出 AEM 方法，通过在响应级别分析熵动力学并利用响应级不确定性代理来动态调整优势函数，实现从探索到利用的自然过渡。
result: "在 ALFWorld、WebShop 和 SWE-bench-Verified 等任务上，AEM 在 1.5B 到 32B 的模型中均取得了一致的性能提升，最高在 SOTA 框架基础上提升了 1.4%。"
conclusion: AEM 提供了一种无需额外监督信号的高效信用分配方案，通过响应级熵调制有效增强了 LLM 智能体在复杂多轮环境中的学习能力。
---

## 摘要
强化学习 (RL) 显著提升了大语言模型 (LLM) 智能体与环境交互及解决多轮任务的能力。然而，有效的智能体强化学习仍面临挑战：仅基于结果的稀疏奖励在长交互轨迹中为单个步骤分配信用（credit assignment）时提供的指导有限。现有方法通常引入密集的中间监督，例如过程奖励模型或辅助自监督信号，这增加了监督和调优的复杂性，并可能限制在不同任务和领域间的泛化能力。我们提出了 AEM，一种无需监督的信用分配方法，它在强化学习训练过程中自适应地调制熵动力学，以优化探索与利用之间的权衡。我们的分析将熵动力学从 token 级别提升到响应（response）级别，使不确定性估计与 LLM 智能体的有效动作粒度保持一致，并降低了对 token 级别采样噪声的敏感性。我们进一步证明，在自然梯度更新下，熵漂移受采样响应的优势值（advantage）与其相对惊奇度（surprisal）之间相互作用的影响。受此结果启发，AEM 推导出一个实用的响应级不确定性代理指标，并利用其对优势值进行重缩放，通过正负样本之间不断演变的平衡，实现从探索到利用的自然过渡。在 ALFWorld、WebShop 和 SWE-bench-Verified 上进行的广泛实验（模型规模从 1.5B 到 32B）表明，AEM 持续改进了强大的强化学习基线，在集成到最先进的软件工程强化学习训练框架中时，获得了 +1.4% 的提升。

## Abstract
Reinforcement learning (RL) has substantially improved the ability of large language model (LLM) agents to interact with environments and solve multi-turn tasks. However, effective agentic RL remains challenging: sparse outcome-only rewards provide limited guidance for assigning credit to individual steps within long interaction trajectories. Existing approaches often introduce dense intermediate supervision, such as process reward models or auxiliary self-supervised signals, which increases supervision and tuning complexity and may limit generalization across tasks and domains. We present AEM, a supervision-free credit assignment method that adaptively modulates entropy dynamics during RL training to improve the exploration-exploitation trade-off. Our analysis lifts entropy dynamics from the token level to the response level, aligning uncertainty estimation with the effective action granularity of LLM agents and reducing sensitivity to token-level sampling noise. We further show that entropy drift under natural-gradient updates is governed by the interaction between the sampled-response advantage and its relative surprisal. Motivated by this result, AEM derives a practical response-level uncertainty proxy and uses it to rescale advantages, leveraging the evolving balance between positive and negative samples to naturally transition from exploration to exploitation. Extensive experiments on ALFWorld, WebShop, and SWE-bench-Verified with models ranging from 1.5B to 32B demonstrate that AEM consistently improves strong RL baselines, including a +1.4\% gain when integrated into a state-of-the-art software-engineering RL training framework.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《AEM: Adaptive Entropy Modulation for Multi-Turn Agentic Reinforcement Learning》** 的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多轮智能体强化学习（Agentic RL）中的信用分配（Credit Assignment）**难题。在复杂的交互任务（如软件工程、网页导航）中，智能体通常只能在长轨迹结束时获得一个稀疏的“结果奖励”。这导致难以确定轨迹中哪些中间步骤对最终成功或失败做出了贡献。
*   **现有局限**：现有方法要么依赖昂贵的中间监督（如过程奖励模型 PRM），要么计算开销巨大（如树状搜索），或者对轨迹结构有严格假设。
*   **研究动机**：利用策略自身的**熵（Entropy）**作为内在信号。高熵代表探索性决策，低熵代表自信行为。作者希望通过调制熵动力学，在无需额外监督的情况下，优化探索与利用的平衡。

### 2. 方法论：核心思想与技术细节
论文提出了 **AEM（自适应熵调制）**，这是一种轻量级的插件式方法，核心思想是**根据响应级的不确定性动态缩放优势函数（Advantage）**。

*   **响应级分析（Response-level Analysis）**：作者认为 LLM 智能体的有效动作单位是完整的“响应”（包含思考和动作），而非单个 Token。因此，AEM 将熵的分析从 Token 级提升到响应级，减少了采样噪声。
*   **理论支撑**：论文证明了在自然梯度更新下，策略熵的漂移（Drift）由响应的**优势值（Advantage）**与其**相对惊奇度（Relative Surprisal）**的乘积决定。
*   **算法流程**：
    1.  **计算代理指标**：对每个响应计算其 Token 熵的平均值，作为响应级不确定性的代理指标。
    2.  **组内归一化**：在同一次采样的响应组（Group）内，对该指标进行 Min-Max 归一化。
    3.  **生成调制系数 ($\alpha$)**：通过一个单调递减函数（受温度参数 $\lambda$ 控制）将归一化后的熵转化为缩放系数。低惊奇度（更确定）的响应获得更大的 $\alpha$，高惊奇度（更不确定）的响应获得较小的 $\alpha$。
    4.  **优势值重缩放**：将原始优势值乘以 $\alpha$。
*   **自适应转换**：在训练初期（负样本多），AEM 倾向于增加熵以维持探索；在后期（正样本增加），它倾向于降低熵以促进收敛（利用）。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：文本具身决策任务（如在房间里找东西）。
    *   **WebShop**：网页购物导航任务。
    *   **SWE-bench-Verified**：真实的软件工程 GitHub 问题修复任务。
*   **基准模型**：Qwen2.5-1.5B/7B-Instruct，以及用于软件工程任务的 Qwen3-32B。
*   **对比方法**：
    *   **闭源模型**：GPT-5.2-Pro, Gemini-3-Pro。
    *   **强化学习基线**：PPO, GRPO, DAPO, GSPO。
    *   **SOTA 框架**：DeepSWE（针对软件工程的强化学习框架）。

### 4. 资源与算力
论文详细列出了算力配置：
*   **1.5B 模型**：使用 4 张 NVIDIA A800 GPU。
*   **7B 模型**：使用 8 张 NVIDIA A800 GPU。
*   **32B 模型（SWE-bench）**：使用 64 张 NVIDIA H200 GPU。
*   **训练时长**：ALFWorld 和 WebShop 训练 150 步；SWE-bench 训练 250 步。
*   **开销分析**：AEM 引入的额外计算延迟极低，仅占总训练时间的 **1.1%**，因为它不需要额外的模型前向传播。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 32B 不同参数规模的模型，并在三个完全不同领域的基准上进行了测试。
*   **消融实验**：作者设计了多组消融实验，包括：
    *   打乱调制系数（验证系数与响应的对应关系是否重要）。
    *   反向调制（验证熵调制方向的正确性）。
    *   不同的归一化策略（轨迹级 vs 组级 vs Batch 级）。
*   **客观性**：实验采用了多次运行取平均值（3 个随机种子）并标注了标准差，对比了多种主流的组优化 RL 算法（GRPO, DAPO 等），证明了 AEM 作为通用插件的有效性。

### 6. 主要结论与发现
*   **性能提升**：AEM 在所有基准上均一致提升了基线性能。在 ALFWorld 上，Qwen2.5-1.5B 的 GRPO 性能提升了 **8.8%**；在 SWE-bench 上，在 SOTA 框架 DeepSWE 基础上进一步提升了 **1.4%**。
*   **缓解熵塌陷**：AEM 有效防止了训练初期的过早熵塌陷（Premature Entropy Collapse），使模型在后期能达到更好的收敛状态。
*   **探索-利用转换**：实验观察到 AEM 确实引导模型经历了从早期高样态探索到后期高确定性利用的自然过渡。

### 7. 优点：亮点总结
*   **无需额外监督**：不需要过程奖励模型（PRM）或人工标注，完全依赖策略自身的内在信号。
*   **极低开销**：作为优势函数的重缩放器，几乎不增加训练负担，易于集成到现有 RL 框架（如 verl, rLLM）。
*   **理论完备**：从信息几何和自然梯度的角度推导了熵动力学，为启发式操作提供了坚实的数学基础。
*   **通用性强**：在不同规模模型和不同任务领域表现出极强的鲁棒性。

### 8. 不足与局限
*   **启发式代理**：虽然有理论支撑，但实际使用的“响应级熵均值”仍是一种启发式代理指标，可能无法完全捕捉复杂的策略分布。
*   **依赖组质量**：AEM 的效果依赖于同组采样响应的质量和多样性。如果组内样本极度同质化，归一化后的信号可能失效。
*   **超参数敏感性**：虽然论文使用了统一的 $\lambda=1$，但在极端任务下，温度参数 $\lambda$ 可能需要针对性调优以获得最佳的熵控制效果。

（完）
