---
title: "Correct Is Not Enough: Training Reasoning Planners with Executor-Grounded Rewards"
title_zh: 仅仅正确是不够的：利用基于执行器落地的奖励训练推理规划器
authors: "Tianyang Han, Hengyu Shi, Junjie Hu, Xu Yang, Zhiling Wang, Junhao Su"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03862v3"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用执行器落地奖励训练推理规划器以获得可靠的推理轨迹
tldr: 针对大语言模型推理中仅依赖最终答案正确性导致推理过程不可靠的问题，本文提出 TraceLift 框架。该框架将推理视为可消费的中间产物，通过结合基于准则的推理奖励模型与执行器性能提升（Uplift）来训练规划器。通过在数学和代码任务上的实验，证明了这种执行器驱动的奖励机制能有效减少逻辑漏洞，提升推理过程的质量和对后续任务的实用性。
selection_source: fresh_fetch
motivation: 仅依赖最终答案的奖励机制容易导致模型通过错误逻辑或捷径获得正确答案，无法确保推理过程的忠实性和对后续执行的有效性。
method: 提出 TraceLift 框架，通过冻结的执行器评估推理轨迹的实际增益，并结合包含高质量参考与扰动样本的 TRACELIFT-GROUPS 数据集进行奖励建模。
result: 在代码和数学基准测试中，该方法显著优于传统的仅基于执行结果的训练，证明了执行器驱动奖励在提升系统性能方面的优越性。
conclusion: 推理监督应当兼顾推理过程的形式质量及其对后续执行环节的实际辅助价值，而非仅仅关注最终答案的正确性。
---

## 摘要
带有可验证奖励的强化学习已成为提高大语言模型显式推理能力的常用方法，但仅凭最终答案的正确性无法揭示推理轨迹（reasoning trace）是否忠实、可靠，或者对使用它的模型是否有用。这种仅关注结果的信号可能会强化那些“因错误原因而得出正确结果”的轨迹，通过奖励捷径来夸大推理收益，并在多步系统中传播有缺陷的中间状态。为此，我们提出了 TraceLift，这是一个将推理视为可消耗中间产物的规划器-执行器（planner-executor）训练框架。在规划器训练期间，规划器输出带有标签的推理。一个冻结的执行器将这些推理转化为最终产物以获取验证器反馈，同时一种基于执行器落地的奖励对中间轨迹进行塑造。该奖励将基于评分细则（rubric-based）的推理奖励模型（RM）分数与在同一冻结执行器上测得的提升值（uplift）相乘，从而奖励那些既高质量又有用的轨迹。为了使推理质量直接可学习，我们引入了 TRACELIFT-GROUPS，这是一个基于数学和代码种子问题构建的、带有评分细则标注的纯推理数据集。每个样本都是一个同题组，包含一个高质量的参考轨迹和多个看似合理但有缺陷的轨迹，后者通过局部扰动降低了推理质量或对解法的支持度，同时保留了任务相关性。在代码和数学基准测试上的广泛实验表明，这种基于执行器落地的推理奖励比仅基于执行的训练更能提升两阶段规划器-执行器系统，这表明推理监督不仅应评估轨迹是否“看起来不错”，还应评估它是否对使用它的模型有所帮助。我们的代码可在以下地址获取：https://github.com/MasaiahHan/TraceLift

## Abstract
Reinforcement learning with verifiable rewards has become a common way to improve explicit reasoning in large language models, but final-answer correctness alone does not reveal whether the reasoning trace is faithful, reliable, or useful to the model that consumes it. This outcome-only signal can reinforce traces that are right for the wrong reasons, overstate reasoning gains by rewarding shortcuts, and propagate flawed intermediate states in multi-step systems. To this end, we propose TraceLift, a planner-executor training framework that treats reasoning as a consumable intermediate artifact. During planner training, the planner emits tagged reasoning. A frozen executor turns this reasoning into the final artifact for verifier feedback, while an executor-grounded reward shapes the intermediate trace. This reward multiplies a rubric-based Reasoning Reward Model (RM) score by measured uplift on the same frozen executor, crediting traces that are both high-quality and useful. To make reasoning quality directly learnable, we introduce TRACELIFT-GROUPS, a rubric-annotated reason-only dataset built from math and code seed problems. Each example is a same-problem group containing a high-quality reference trace and multiple plausible flawed traces with localized perturbations that reduce reasoning quality or solution support while preserving task relevance. Extensive experiments on code and math benchmarks show that this executor-grounded reasoning reward improves the two-stage planner-executor system over execution-only training, suggesting that reasoning supervision should evaluate not only whether a trace looks good, but also whether it helps the model that consumes it. Our code is available at: https://github.com/MasaiahHan/TraceLift

---

## 论文详细总结（自动生成）

这篇论文《Correct Is Not Enough: Training Reasoning Planners with Executor-Grounded Rewards》深入探讨了大型语言模型（LLM）在推理任务中“结果正确但逻辑错误”的问题，并提出了一种全新的训练框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在强化学习（RL）训练中，通常只根据最终答案的正确性（Outcome-only）给予奖励。这导致模型可能通过“错误的逻辑得出正确的答案”、利用捷径或掩盖中间错误。
*   **研究动机**：推理轨迹（Reasoning Trace）不应仅仅是解释，而应作为一种“可消费的中间产物”来辅助后续执行。
*   **整体含义**：论文旨在训练一个“规划器（Planner）”，使其生成的推理过程不仅在形式上符合逻辑规范，而且对后续的“执行器（Executor）”具有实际的指导价值。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入“执行器落地奖励（Executor-Grounded Reward）”，将推理质量的评估与执行器的实际表现提升（Uplift）挂钩。
*   **关键技术细节**：
    *   **TraceLift 框架**：采用“规划器-执行器”两阶段架构。规划器负责生成推理，冻结的执行器根据推理生成最终结果。
    *   **TraceLift-Groups 数据集**：包含 6,000 个推理组。每组包含一个高质量参考轨迹和多个经过局部扰动（如算术错误、逻辑跳跃、冗余信息等）的缺陷轨迹，用于训练推理奖励模型（Reasoning RM）。
    *   **推理奖励模型（Reasoning RM）**：基于多维度评分细则（Rubric）训练，评估任务理解、计划质量、步骤连贯性等。
    *   **奖励函数公式**：$R = 0.5 \cdot R_{exec} + 0.5 \cdot (RM \cdot Uplift)$。
        *   $R_{exec}$：最终答案是否正确的验证奖励。
        *   $RM$：推理奖励模型给出的质量分。
        *   $Uplift$：执行器在使用该推理轨迹后，相比于“无推理”基准在成功率上的提升值。
    *   **优化算法**：使用 GRPO（Group Relative Policy Optimization）进行策略优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **代码领域**：HumanEval, HumanEval+, MBPP-full, LiveCodeBench。
    *   **数学领域**：GSM8K, GSM-Hard, SVAMP, MATH500。
*   **Benchmark 与对比方法**：
    *   **Base**：未经过强化学习训练的基座模型。
    *   **Exec-only**：仅使用最终答案正确性作为奖励的传统 RL 训练。
    *   **对比模型**：Qwen2.5-7B, Llama3.1-8B, Qwen3-4B。
*   **评估协议**：固定执行器，仅评估规划器生成的推理轨迹对系统性能的贡献。

### 4. 资源与算力
*   **训练细节**：模型在 bf16 精度下训练 600 个 step，学习率为 $5 \times 10^{-6}$，温度系数为 0.5。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量（如 A100 或 H100 的具体颗数），但考虑到涉及 7B/8B 规模模型的全参数和 LoRA 强化学习训练，通常需要高性能计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、8 个主流基准测试，并进行了多项消融实验。
*   **充分性与客观性**：
    *   **消融实验**：对比了无 Uplift 权重、仅有 RM、不同采样次数 $K$ 等变体，证明了各组件的必要性。
    *   **多样性**：在代码和数学两个逻辑性极强的领域同时验证，结果具有一致性。
    *   **公平性**：所有对比实验均在相同的评估链条下进行，且使用了 3 个不同随机种子取平均值，实验设计较为严谨客观。

### 6. 主要结论与发现
*   **超越正确性**：TraceLift 在所有基准测试上均显著优于仅基于执行结果的训练（Exec-only）。
*   **推理长度与质量**：TraceLift 训练出的模型生成的推理轨迹比 Exec-only 更短（平均少约 30 个 token），但更有用，证明了该方法能抑制模型产生无意义的“废话”。
*   **Uplift 的关键作用**：单纯的推理质量评分（RM）如果不结合执行器的实际增益，效果甚至可能不如传统 RL。
*   **通用性**：即使在模型自问自答（Planner 和 Executor 是同一个模型）的场景下，该方法依然有效。

### 7. 优点
*   **视角独特**：将推理视为“可消费的接口”，解决了推理过程与结果脱节的痛点。
*   **数据质量高**：TraceLift-Groups 通过局部扰动生成负样本，为推理过程的细粒度监督提供了高质量数据。
*   **理论支撑**：附录中提供了详细的数学证明，分析了奖励函数的信用分配逻辑。

### 8. 不足与局限
*   **计算开销**：训练时为了估计 Uplift，需要对执行器进行多次采样（$K=3$），这增加了训练时的推理成本。
*   **领域局限**：目前主要集中在代码和数学等具有明确验证器（Verifier）的领域，在创意写作或开放式问答等主观性强的领域应用难度较大。
*   **依赖冻结执行器**：训练效果可能受限于所选执行器的能力上限。

（完）
