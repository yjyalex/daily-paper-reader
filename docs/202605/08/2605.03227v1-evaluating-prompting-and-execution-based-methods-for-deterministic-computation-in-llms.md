---
title: Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs
title_zh: 评估大语言模型中用于确定性计算的提示词与基于执行的方法
authors: Hongkun Yu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03227v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 评估LLM中的推理和确定性计算
tldr: 本研究系统评估了LLM在确定性计算任务（如二进制计数、算术运算）中的表现。通过对比CoT、Least-to-Most、PoT和SC等提示策略，发现传统提示法在精确计算上存在局限，而PoT通过生成可执行代码实现了完美准确率。研究还证明了训练小型专用模型生成代码是高效且可靠的替代方案，强调了外部工具在确定性任务中的重要性。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在处理需要精确、无误的确定性符号计算任务时的真实能力及局限性。
method: 引入合成数据集，对比评估了CoT、Least-to-Most、PoT、SC等多种提示策略，并训练了小型专用模型CodeT5-small。
result: "传统提示策略准确率有限且易累积错误，而PoT和微调的小型模型通过生成并执行代码达到了100%的准确率。"
conclusion: 大语言模型更倾向于模拟推理模式而非执行精确计算，结合外部执行工具或使用专用模型是解决确定性任务的更优方案。
---

## 摘要
大语言模型（LLMs）在自然语言理解和推理方面展现出强大的能力。然而，它们执行精确、确定性计算的能力仍不明确。在这项工作中，我们系统地评估了多种提示策略，包括思维链（CoT）、由简入繁分解（Least-to-Most decomposition）、程序思维（PoT）和自洽性（SC），在需要精确且无误输出的任务上的表现，这些任务包括二进制计数、最长子串检测和算术求值。为了支持这项研究，我们引入了一个包含多样化自然语言指令的合成数据集，从而能够对跨多种任务类型的精确计算进行受控评估。我们的结果表明，标准的提示方法在基于序列的任务上仅达到中等准确率。CoT 提供的改进有限，而 Least-to-Most 则受到错误累积的影响。相比之下，PoT 通过生成可执行代码并将计算委托给外部解释器，实现了完美的准确率。自洽性通过多数投票提高了鲁棒性，但带来了巨大的计算开销。我们进一步训练了一个小型领域特定模型（CodeT5-small）来生成可执行程序，该模型在所有任务的留出合成测试数据上均以极低的训练成本实现了完美的准确率。总的来说，我们的研究结果表明，LLMs 可能是在模拟推理模式，而非可靠地执行精确的符号计算。对于确定性任务，将 LLMs 与外部工具结合或使用专门模型提供了一种更可靠且高效的解决方案。

## Abstract
Large Language Models (LLMs) have demonstrated strong capabilities in natural language understanding and reasoning. However, their ability to perform exact, deterministic computation remains unclear. In this work, we systematically evaluate multiple prompting strategies, including Chain-of-Thought (CoT), Least-to-Most decomposition, Program-of-Thought (PoT), and Self-Consistency (SC), on tasks requiring precise and error-free outputs, including binary counting, longest substring detection, and arithmetic evaluation. To support this study, we introduce a synthetic dataset with diverse natural language instructions, enabling controlled evaluation of exact computation across multiple task types. Our results show that standard prompting methods achieve only moderate accuracy on sequence-based tasks. CoT provides limited improvement, while Least-to-Most suffers from error accumulation. In contrast, PoT achieves perfect accuracy by generating executable code and delegating computation to an external interpreter. Self-Consistency improves robustness through majority voting, but incurs substantial computational overhead. We further train a small domain-specific model (CodeT5-small) to generate executable programs, which achieves perfect accuracy on held-out synthetic test data across all tasks with minimal training cost. Overall, our findings suggest that LLMs may simulate reasoning patterns rather than reliably perform exact symbolic computation. For deterministic tasks, combining LLMs with external tools or using specialized models provides a more reliable and efficient solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）在自然语言理解和推理上表现出色，但其处理**确定性计算**（如精确计数、符号操纵、算术运算）的能力仍存疑。这类任务要求输出绝对精确，与语言模型的概率本质存在冲突。
*   **核心问题**：LLM 是在真正进行符号计算，还是仅仅在模拟推理模式？现有的提示策略（如 CoT、PoT）在处理这类需要“零误差”的任务时效果如何？

### 2. 方法论：核心思想与关键技术细节
论文对比了两种主要路径：**纯提示词方法**（依赖模型内部推理）和**基于执行的方法**（将计算委托给外部工具）。
*   **提示策略（Prompting Strategies）**：
    *   **Plain Prompt**：直接给出答案。
    *   **Chain-of-Thought (CoT)**：引导模型生成中间推理步骤。
    *   **Least-to-Most**：将复杂问题分解为子问题（如分段计数再求和）。
    *   **Self-Consistency (SC)**：通过多次采样（T=0.7）并进行多数投票来提高鲁棒性。
*   **基于执行的方法（Execution-Based）**：
    *   **Program-of-Thought (PoT)**：模型不直接输出答案，而是生成 Python 代码，通过外部解释器运行获取结果。
    *   **CodeT5-Based**：微调一个小型专用模型（CodeT5-small），使其学习从自然语言指令到可执行代码的映射。

### 3. 实验设计
*   **数据集**：构建了一个包含 3000 个样本的合成数据集，涵盖三类任务：
    1.  **二进制计数**：统计 80-120 位序列中 0 和 1 的个数。
    2.  **最长子串检测**：检测特定字符连续出现的最长长度（测试状态跟踪能力）。
    3.  **算术运算**：包含 3-6 个数字的四则运算（确保结果为整数）。
*   **Benchmark**：采用**精确匹配准确率（Exact-match Accuracy）**作为唯一指标。
*   **对比模型**：使用 `gpt-oss-120b` 进行提示词评估，使用 `CodeT5-small` 进行微调对比。

### 4. 资源与算力
*   **算力使用**：CodeT5-small 的训练在**单张 GPU**上完成。
*   **训练时长**：总训练时间约为 **3 分钟**（6 个 Epoch）。
*   **细节**：论文未明确标注具体的 GPU 型号（如 A100 或 V100），但强调了其训练成本极低。

### 5. 实验数量与充分性
*   **实验规模**：总计 3000 个合成样本。在 LLM 评估中，由于 API 成本和自洽性（SC）的多路调用，每项任务抽取了 50-100 个样本进行测试。
*   **充分性与客观性**：
    *   实验涵盖了从简单算术到复杂序列处理的不同难度梯度。
    *   通过引入多样化的自然语言指令（Generalized Instructions）来避免模型对特定模板的记忆，增强了评估的客观性。
    *   对比了当前主流的所有推理增强提示技术，实验设计较为全面。

### 6. 主要结论与发现
*   **LLM 的局限性**：在纯文本模式下，LLM 在长序列计数和状态跟踪任务上表现较差（准确率仅 0.4-0.6 左右），CoT 改进有限，Least-to-Most 甚至因错误累积导致性能下降。
*   **任务难度分级**：算术运算（1.0 准确率） $\ll$ 二进制计数 $<$ 最长子串检测。
*   **执行胜过推理**：PoT（程序思维）在所有任务中均达到 **100% 准确率**。
*   **小模型胜过大模型**：经过微调的 CodeT5-small（仅 60M 参数）在确定性任务上的表现远超 120B 参数的通用 LLM，证明了专用工具化模型的优越性。

### 7. 优点
*   **系统性强**：对多种提示策略在确定性计算这一细分领域进行了严谨的横向对比。
*   **方案实用**：证明了通过“LLM 生成代码 + 外部解释器”是解决精确计算问题的最可靠路径。
*   **开源贡献**：发布了合成数据集和评估代码，便于社区复现和扩展。

### 8. 不足与局限
*   **数据集单一**：完全依赖合成数据，缺乏真实世界中的噪声数据或模糊指令的测试。
*   **算术任务过简**：实验中所有方法在算术任务上均获得满分，这可能掩盖了模型在处理更复杂数学逻辑时的差异。
*   **模型覆盖**：主要测试了 `gpt-oss-120b`，未包含 GPT-4o 或 Claude 3.5 等闭源最强模型，结论的普适性在顶级模型上尚待验证。

（完）
