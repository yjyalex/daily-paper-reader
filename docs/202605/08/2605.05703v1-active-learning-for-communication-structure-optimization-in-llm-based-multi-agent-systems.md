---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统中通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 优化基于大语言模型的多智能体系统中的通信结构
tldr: 本研究针对大语言模型多智能体系统（LLM-MAS）中通信结构优化效率低的问题，提出了一种基于主动学习的任务选择框架。传统方法依赖随机采样，易受任务难度和领域差异影响，导致优化不稳定。该方法利用集成卡尔曼反演作为贝叶斯更新的近似，通过信息论准则主动识别对优化最有价值的任务，并结合代理模型和批量汤普森采样提升扩展性。实验证明，该方法在有限预算下能显著提升通信结构的优化效果，且在智能体攻击场景下表现出良好的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的LLM多智能体系统通信结构优化依赖随机采样，导致在有限预算下优化过程不稳定且对训练任务集高度敏感。
method: 提出一种基于集成卡尔曼反演的信息论任务选择框架，通过评估候选任务对图参数分布的改变程度来主动识别高价值任务。
result: 在良性环境和存在智能体攻击的场景下，该方法均能有效提升受限计算预算下的通信结构优化性能和稳定性。
conclusion: 通过主动学习识别关键任务，可以显著提高LLM多智能体系统通信结构优化的效率，是解决黑盒及噪声系统优化问题的有效途径。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 Token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相等，这使得在有限的训练预算下，优化过程往往不稳定且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论的任务选择框架。该方法通过评估候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（Ensemble Kalman Inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒和有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型及批量汤普森采样（Batch Thompson Sampling）相结合。我们在良性设置和存在智能体攻击的设置中验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

这是一份关于论文《Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型多智能体系统（LLM-MAS）中的通信结构优化**。
*   **研究动机**：虽然优化智能体间的通信图（如删除冗余连接、限制有害信息传播）能提升性能并节省 Token，但现有的优化方法通常依赖于从任务池中**随机采样**训练任务。
*   **核心挑战**：由于不同任务的难度和领域差异巨大，随机采样在有限预算下会导致优化过程极不稳定，且对特定训练集高度敏感。此外，LLM-MAS 是典型的“黑盒”且具有非导数、高噪声的特性，传统的基于梯度的任务选择方法难以奏效。
*   **整体含义**：本文旨在通过**主动学习（Active Learning）**，识别出对优化通信结构最有价值（信息量最大）的任务，从而在受限预算下实现更稳健、高效的系统优化。

### 2. 核心方法论
论文提出了一种基于集成的、信息论的主动学习框架，主要包含两个阶段：

*   **第一阶段：代表性选择（Representative Selection）**
    *   利用语义嵌入（Embedding）将任务向量化。
    *   采用贪婪的最远点采样（Greedy Farthest-Point Selection）从原始任务池中筛选出一个较小的代表性子集，确保任务的多样性。
*   **第二阶段：信息量选择（Informative Selection）**
    *   **核心技术：集成卡尔曼反演（Ensemble Kalman Inversion, EKI）**。这是一种无导数的近似贝叶斯更新方法。它通过一组“粒子”（参数集合）来表示通信图参数的分布。
    *   **任务效用评估**：通过计算任务观测前后，参数分布之间的 **Kullback–Leibler (KL) 散度**来衡量信息增益。KL 散度越大，说明该任务对优化通信结构提供的有用信息越多。
    *   **可扩展性优化**：引入**代理模型（Surrogate Modeling）**和**批量汤普森采样（Batch Thompson Sampling）**。利用高斯过程（GP）预测未评估任务的信息增益，从而避免对所有候选任务进行昂贵的 MAS 运行评估。

### 3. 实验设计
*   **数据集**：
    *   **MMLU**：大规模多任务语言理解基准（涵盖多个学科）。
    *   **GSM8K**：小学数学应用题数据集（侧重逻辑推理）。
*   **实验场景**：
    *   **良性设置（Benign）**：所有智能体均正常工作。
    *   **对抗设置（Adversarial/Attack）**：系统中存在恶意智能体，故意提供错误推理和误导信息。
*   **对比方法（Baselines）**：
    *   **No Train**：未优化的全连接结构。
    *   **Random**：随机选择任务进行训练（主流基准）。
    *   **EGL/EMC**：基于期望梯度长度或期望模型改变的传统主动学习方法。
    *   **Fisher-based Coreset**：基于费舍尔信息矩阵的子集选择方法。

### 4. 资源与算力
*   **智能体模型**：实验使用了 **GPT-4.1-nano** 作为智能体和决策者的底座模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于实验涉及大量的 LLM API 调用，其成本主要体现在 Token 消耗上。论文详细记录了不同方法在选择、训练和测试阶段的 Token 使用量（以百万为单位）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MMLU 和 GSM8K 上进行了多轮独立运行（每种设置通常包含 10 到 23 次独立实验），并提供了标准差和置信区间。
    *   进行了**消融实验**，验证了“代表性选择”与“信息量选择”各自的贡献。
    *   进行了**敏感性分析**，探讨了集成规模（Ensemble Size）和 EKI 迭代步数对结果的影响。
*   **充分性评价**：实验设计较为充分且客观。通过引入对抗攻击场景，验证了方法在极端情况下的鲁棒性。使用自助法（Bootstrap）计算置信区间，增强了结论的统计说服力。

### 6. 主要结论与发现
*   **性能提升**：在相同 Token 预算下，主动学习选择的任务比随机任务能带来更高的下游准确率（MMLU 攻击场景下提升 +1.45%）。
*   **稳定性增强**：主动学习显著降低了优化结果的方差，特别是在“最差 25%”的实验运行中，准确率提升显著（MMLU 良性场景下提升 +1.30%），有效防止了优化失败。
*   **防御能力**：在智能体攻击场景下，该方法能更有效地识别并切断通往恶意智能体的通信路径。
*   **效率**：单步 EKI 配合小规模集成（如 6 个粒子）就足以识别出高质量任务，证明了方法的实用性。

### 7. 优点与亮点
*   **无导数框架**：巧妙地利用 EKI 解决了 LLM-MAS 这种不可微、黑盒系统的参数更新评估问题。
*   **鲁棒性导向**：不仅关注平均性能，更关注优化过程的稳定性（减少 Lower-tail failures），这对于实际部署 MAS 至关重要。
*   **计算效率**：通过缓存预测结果和代理模型，大幅降低了评估任务信息增益时的计算开销。

### 8. 不足与局限
*   **依赖标签**：当前框架假设训练池中的任务都有标准答案（Ground Truth），对于完全无标签场景的适用性有待研究。
*   **集成规模限制**：虽然实验证明小规模集成有效，但在参数维度极高或后验分布极复杂的情况下，EKI 的近似精度可能会下降。
*   **泛化性验证**：实验主要集中在 GPT 系列模型上，尚未在开源模型（如 Llama）或其他更复杂的智能体架构（如具有长期记忆的系统）上广泛验证。

（完）
