---
title: "MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems"
title_zh: MASPO：基于大语言模型的多智能体系统联合提示词优化
authors: "Zhexuan Wang, Xuebo Liu, Li Wang, Zifei Shan, Yutong Wang, Zhenxi Song, Min Zhang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06623v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 基于大模型的多智能体系统联合提示词优化
tldr: 本研究针对基于大语言模型的多智能体系统（MAS）中提示词优化难题，提出了MASPO框架。传统方法难以协调局部智能体目标与全局系统目标，MASPO通过联合评估机制，根据下游智能体的成功率而非仅局部有效性来迭代优化提示词。结合数据驱动的进化束搜索，MASPO在6项任务中显著提升了系统性能，为复杂协作任务提供了高效的自动化提示词优化方案。
selection_source: fresh_fetch
motivation: 现有的多智能体系统提示词优化面临局部智能体目标与全局系统目标不一致的挑战。
method: 提出MASPO框架，利用联合评估机制衡量提示词对下游任务的贡献，并采用进化束搜索进行高效空间探索。
result: 在6个多样化任务上的实验表明，MASPO优于现有最先进方法，平均准确率提升了2.9。
conclusion: MASPO通过弥合局部交互与全局结果之间的差距，实现了多智能体系统提示词的有效联合优化。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在处理复杂协作任务方面展现出巨大潜力，其中智能体通常通过特定角色的提示词进行编排。虽然这些提示词的质量至关重要，但在相互协作的智能体之间进行联合优化仍是一项艰巨的挑战，这主要是由于局部智能体目标与整体系统目标之间的不一致。为解决这一问题，我们提出了 MASPO，这是一个旨在自动且迭代地优化整个系统提示词的新型框架。MASPO 的核心创新在于其联合评估机制，该机制不仅根据提示词的局部有效性进行评估，还评估其促进后续智能体下游任务成功的能力。这在不依赖地面真值（ground-truth）标签的情况下，有效地弥合了局部交互与全局结果之间的差距。此外，MASPO 采用数据驱动的进化束搜索（evolutionary beam search）以高效地在多维提示词空间中进行搜索。在 6 个不同任务上的广泛实证评估表明，MASPO 始终优于最先进的提示词优化方法，平均准确率提升了 2.9。我们在 https://github.com/wangzx1219/MASPO 发布了代码。

## Abstract
Large language model (LLM)-based Multi-agent systems (MAS) have shown promise in tackling complex collaborative tasks, where agents are typically orchestrated via role-specific prompts. While the quality of these prompts is pivotal, jointly optimizing them across interacting agents remains a non-trivial challenge, primarily due to the misalignment between local agent objectives and holistic system goals. To address this, we introduce MASPO, a novel framework designed to automatically and iteratively refine prompts across the entire system. A core innovation of MASPO is its joint evaluation mechanism, which assesses prompts not merely by their local validity, but by their capacity to facilitate downstream success for successor agents. This effectively bridges the gap between local interactions and global outcomes without relying on ground-truth labels. Furthermore, MASPO employs a data-driven evolutionary beam search to efficiently navigate the high-dimensional prompt space. Extensive empirical evaluations across 6 diverse tasks demonstrate that MASPO consistently outperforms state-of-the-art prompt optimization methods, achieving an average accuracy improvement of 2.9. We release our code at https://github.com/wangzx1219/MASPO.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：在基于大语言模型（LLM）的多智能体系统（MAS）中，系统性能高度依赖于每个智能体的提示词（Prompt）。然而，联合优化多个智能体的提示词面临三大难题：
    1.  **局部-全局不一致性（Local-Global Misalignment）**：中间智能体可能完美执行了局部指令，但其输出却误导了下游智能体，导致系统整体失败。
    2.  **信用分配难题（Credit Assignment）**：中间步骤往往缺乏地面真值（Ground-truth）标签，难以衡量单个智能体对最终结果的贡献。
    3.  **非平稳性（Non-stationarity）**：一个智能体提示词的改变会改变下游智能体的输入分布，导致优化环境不断变化。
*   **研究意义**：MASPO 旨在通过自动化的迭代框架，在无需大量标注数据的情况下，实现多智能体提示词的协同进化。

### 2. 方法论：核心思想与关键技术
MASPO 框架通过以下三个核心机制解决上述问题：
*   **多粒度联合评估机制（Joint Evaluation）**：
    *   设计了一个复合评分函数 $R$，综合考量三个维度：**局部有效性**（Local Validity，是否符合角色约束）、**前瞻潜力**（Lookahead Potential，对直接下游智能体的影响）和**全局对齐**（Global Alignment，对最终输出的贡献）。
    *   该机制通过 LLM 评估器（Evaluator）进行两两比较，无需依赖标准答案。
*   **失配感知采样（Misalignment-Aware Sampling）**：
    *   专门挖掘并存储“失配案例”：即那些局部表现良好但导致下游或全局失败的执行轨迹。
    *   在优化过程中，将这些案例作为 Few-shot 示例输入给优化器模型，引导其诊断并修复交互中的逻辑断点。
*   **进化束搜索与自适应动态（Evolutionary Beam Search）**：
    *   **坐标上升调度**：按照智能体通信图的拓扑顺序，轮流优化每个智能体。
    *   **束刷新机制（Beam Refresh）**：当上游智能体更新后，重新评估当前智能体束（Beam）中的候选提示词，以应对输入分布的偏移（协变量偏移），确保评分的实时有效性。

### 3. 实验设计
*   **数据集/场景**：涵盖三大领域共 6 个基准测试：
    *   **数学**：MATH-500, AGIEval-MATH, AQuA。
    *   **复杂推理**：GPQA-Diamond。
    *   **代码生成**：MBPP, HumanEval-ET。
*   **系统架构**：在**顺序型（Sequential）**和**层级型（Hierarchical）**两种 MAS 拓扑结构上进行了验证。
*   **对比方法（Baselines）**：
    *   基础方法：Vanilla, CoT, SC (Self-Consistency), Self-Refine。
    *   MAS 优化方法：AgentDropout, TPE (Tree-structured Parzen Estimator)。
    *   单智能体优化适配：SPO (Self-supervised Prompt Optimization)。

### 4. 资源与算力
*   **模型使用**：
    *   **智能体骨干模型**：Qwen3-8B。
    *   **优化器与评估器**：Gemini-2.5-Pro（作为驱动优化的“大脑”）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到优化过程中仅使用 50 个无标签样本作为样本池，每次迭代采样 10 个，旨在降低计算开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个主流数据集上进行了主实验。
    *   进行了详尽的**消融实验**（针对束搜索、调度策略、联合评估组件、失配采样等）。
    *   进行了**敏感性分析**（针对权重分配、前瞻深度、样本池大小等）。
    *   验证了**跨模型迁移性**（将 Qwen3 优化的提示词迁移至 DeepSeek-V3, GLM-4.6, Claude-Sonnet-4, Gemini-2.5-Pro）。
*   **客观性与公平性**：实验通过控制计算预算（API 调用次数和搜索深度）与基线方法进行对比，并使用了多种拓扑结构，证明了方法的普适性和稳健性。

### 6. 主要结论与发现
*   **性能提升**：MASPO 在所有任务上均优于现有最先进方法，平均准确率提升 **2.9%**，在 Sequential MAS 上最高提升达 **5.06%**。
*   **失配修复**：实验证明，显式注入失配案例能显著降低系统的“失配率”，有效解决了局部指令与全局目标脱节的问题。
*   **迁移价值**：在小模型（8B）上优化出的提示词，直接迁移到大模型（如 Claude 或 Gemini）上依然有效，这提供了一种低成本的优化范式。
*   **初始化鲁棒性**：即使在极简或错误的初始提示词下，MASPO 也能通过迭代恢复并提升性能。

### 7. 优点
*   **闭环优化**：通过前瞻潜力指标，首次在提示词优化中实现了类似强化学习的“信用分配”。
*   **无需标签**：完全基于 LLM 的自我评估，极大地扩展了在缺乏 Ground-truth 的复杂任务中的应用场景。
*   **拓扑无关性**：能够适应各种复杂的智能体协作图结构。

### 8. 不足与局限
*   **依赖强模型**：优化效果高度依赖于作为“优化器/评估器”的高级模型（如 Gemini-2.5-Pro）。虽然论文展示了 8B 模型的自优化能力，但性能增益会有所下降。
*   **计算延迟**：迭代式的束搜索和多维度评估意味着在优化阶段需要大量的 API 调用和推理时间。
*   **偏差风险**：评估过程可能受到 LLM 评估器固有偏见（如长度偏见、位置偏见）的影响，尽管论文通过两两比较缓解了这一问题。

（完）
