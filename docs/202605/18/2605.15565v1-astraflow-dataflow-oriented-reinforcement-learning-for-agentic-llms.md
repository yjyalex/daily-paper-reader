---
title: "AstraFlow: Dataflow-Oriented Reinforcement Learning for Agentic LLMs"
title_zh: AstraFlow：面向数据流的智能体大语言模型强化学习
authors: "Haizhong Zheng, Yizhuo Di, Jiahui Wang, Shuowei Jin, Xueshen Liu, Yongji Wu, Z. Morley Mao, Ion Stoica, Jiawei Zhao, Beidi Chen"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15565v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 面向多策略协作智能体训练的数据流强化学习系统
tldr: AstraFlow是一个面向数据流的强化学习系统，旨在解决智能体大语言模型训练成本高、系统扩展难的问题。它通过解耦生成、数据流管理和训练组件，取代了传统的以训练器为中心的控制架构。该系统原生支持多策略协作训练、弹性扩展及跨地域异构计算，在保持高精度的同时，将多策略训练速度提升了2.7倍，显著降低了系统工程负担。
selection_source: fresh_fetch
motivation: 现有的LLM强化学习系统因采用以训练器为中心的架构，难以高效支持复杂的智能体多策略协作训练及异构计算资源调度。
method: 提出一种数据流导向的架构，将生成服务、数据流管理和训练解耦为自治组件，通过原则性的抽象实现系统灵活性。
result: 在多项基准测试中，AstraFlow在无需修改系统代码的情况下实现了高效的弹性扩展，并将多策略协作训练速度提升了2.7倍。
conclusion: AstraFlow通过数据流导向的设计，为大规模、复杂智能体强化学习提供了一个高效、可扩展且通用的系统框架。
---

## 摘要
强化学习（RL）正越来越多地被用于提升大语言模型的推理、编程和工具使用能力，但智能体强化学习（agentic RL）的成本依然极其高昂。将强化学习扩展到智能体大语言模型（agentic LLMs）需要支持复杂的工作负载，包括多策略协同训练，同时高效利用弹性、异构和跨区域的计算资源。现有的 LLM RL 系统支持其中部分功能，但每次新增扩展通常都需要专门的系统工程工作。这种负担源于以训练器为中心的控制架构，以及缺乏对 RL 系统组件的原则性抽象。为了解决这些局限性，我们提出了 AstraFlow，这是一个面向数据流的 RL 系统，它用原则性的组件抽象取代了传统的以训练器为中心的控制。在 AstraFlow 中，采样服务（rollout services）、数据流管理和训练被解耦为自治组件，使系统能够原生支持复杂的多策略智能体 RL 工作负载，并高效利用多样化的计算资源。我们在数学、代码、搜索和 AgentBench 工作负载上对 AstraFlow 进行了评估，结果表明，同一系统在无需更改系统级代码的情况下，即可支持多策略训练、弹性扩展、异构跨区域执行以及可组合的数据算法。在多策略协同训练中，AstraFlow 达到了与现有 RL 系统相当或更好的准确率，同时将训练时间缩短了 2.7 倍。

## Abstract
Reinforcement learning (RL) is increasingly used to improve the reasoning, coding, and tool-use capabilities of large language models, but agentic RL remains prohibitively expensive. Scaling RL to agentic LLMs requires supporting complex workloads, including multi-policy collaborative training, while efficiently using elastic, heterogeneous, and cross-region compute resources. Existing LLM RL systems support some of these capabilities, but each new extension often requires dedicated system engineering. This burden arises from trainer-centered control architectures and the lack of principled abstractions for RL system components. To address these limitations, we propose AstraFlow, a dataflow-oriented RL system that replaces conventional trainer-centered control with principled component abstractions. In AstraFlow, rollout services, dataflow management, and training are decoupled into autonomous components, enabling the system to natively support complex multi-policy agentic RL workloads and efficiently exploit diverse compute resources. We evaluate AstraFlow across math, code, search, and AgentBench workloads, showing that the same system supports multi-policy training, elastic scaling, heterogeneous cross-region execution, and composable data algorithms without system-level code changes. In multi-policy collaborative training, AstraFlow achieves comparable or better accuracy than existing RL systems while speeding up training time by 2.7x.

---

## 论文详细总结（自动生成）

### AstraFlow: 面向数据流的智能体大语言模型强化学习总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 强化学习（RL）系统（如 `verl`, `AReaL`）大多采用“以训练器为中心”（Trainer-centered）的架构。在这种架构下，训练器负责协调采样调度、数据移动、策略优化和权重同步。
*   **研究动机**：
    *   **复杂性挑战**：智能体（Agentic）任务涉及多轮对话、工具调用及多策略协作，现有系统难以灵活支持多策略协同训练。
    *   **资源利用率低**：同步架构常导致昂贵的训练 GPU 在采样阶段闲置。
    *   **扩展性差**：在面对弹性资源、异构硬件或跨地域计算时，现有系统往往需要针对特定功能进行繁琐的系统级代码修改。

#### 2. 方法论
AstraFlow 提出了**面向数据流（Dataflow-oriented）**的架构，将系统解耦为三个自治组件：
*   **数据流层（Dataflow Layer）**：作为协调平面，管理 Prompt、轨迹、奖励和训练批次。它提供可编程接口，允许在不修改训练器的情况下实现数据算法（如动态采样、重放、过滤）。
*   **采样即服务（Rollout-as-a-Service, RaaS）**：将轨迹生成建模为纯推理服务。RaaS 节点独立运行，支持热插拔和弹性缩放，能够适配不同的推理后端。
*   **训练器抽象（Trainer Abstraction）**：训练器仅负责从数据流层拉取数据并发布更新后的权重。
*   **关键技术细节**：
    *   **全异步拉取式权重更新**：支持稀疏权重传输（Delta Weight Transfer），利用 RL 训练中权重更新的高稀疏性（通常 >98%），显著降低跨地域传输带宽需求。
    *   **多策略协作**：原生支持多个训练器和多个采样池的复杂交互，无需中心化控制。

#### 3. 实验设计
*   **应用场景**：
    *   **数学推理**：Solver + Verifier 协作。
    *   **代码生成**：Solver + Selector，以及 Solver + Test-Case Generator。
    *   **智能体任务**：AlfWorld, WebShop, 搜索增强 QA。
*   **基准测试（Benchmarks）**：AIME24/25, MATH500, Minerva, LiveCodeBench v5/v6, Codeforces, AgentBench。
*   **对比方法**：
    *   **系统性能对比**：与 `AReaL`（代表性的异步解耦系统）和 `verl`（同步系统）对比。
    *   **多智能体框架对比**：与 `Dr. MAS`（基于 `verl` 的多智能体 RL 扩展）对比。

#### 4. 资源与算力
论文详细列出了实验使用的硬件资源：
*   **GPU 型号**：NVIDIA H100 (80GB), H200 (141GB), A100, L40S。
*   **具体配置**：
    *   多策略数学训练：单节点 8×H100。
    *   代码生成训练：两节点 16×H200。
    *   跨地域模拟：三个节点（1 个本地训练节点，2 个远程采样节点），模拟 4Gbps 带宽和 300ms 延迟。
*   **模型规模**：涵盖 Qwen3-1.7B, 8B, 14B 以及 Qwen2.5-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括多策略协作（3 种工作流）、系统弹性（自动缩放）、异构/跨地域执行、以及数据算法灵活性（GRESO, 动态采样等）。
*   **充分性评价**：实验设计非常充分且具有客观性。不仅验证了模型最终的准确率（Accuracy），还深入分析了系统指标（迭代时间、GPU 利用率、权重稀疏度、带宽占用）。通过模拟真实世界的受限网络环境，证明了系统的实用价值。

#### 6. 主要结论与发现
*   **训练加速**：在多策略协同训练中，AstraFlow 相比现有系统实现了 **2.7 倍** 的训练速度提升。
*   **资源优化**：通过“智能体维护者”（Agentic Maintainer）实现的自动缩放，在不损失精度的情况下减少了 **13%** 的 GPU 小时数。
*   **网络鲁棒性**：得益于稀疏权重传输，系统能够在低带宽、高延迟的跨地域链路上稳定运行，且训练质量与本地环境相当。
*   **算法组合性**：证明了可以在数据流层轻松组合多种数据处理算法，而无需重写系统代码。

#### 7. 优点
*   **架构优雅**：通过原则性的抽象（Dataflow）解决了 RL 系统长期存在的耦合问题。
*   **原生支持多策略**：将多智能体 RL 从“系统级修改”降级为“工作流级配置”。
*   **高度灵活性**：支持异构硬件和弹性资源，非常适合云原生和分布式训练环境。
*   **创新性运维**：引入 LLM 智能体作为系统维护者来处理自动缩放，展示了“AI for System”的新范式。

#### 8. 不足与局限
*   **算法依赖**：作为一个系统论文，其性能提升高度依赖于具体工作负载的特征（如采样延迟、权重更新频率）。
*   **抽象开销**：虽然论文强调了效率，但在极高性能要求的单模型训练场景下，多层抽象可能引入微小的延迟开销（尽管实验显示与 AReaL 持平）。
*   **覆盖范围**：实验虽涵盖了数学和代码，但尚未在超长时序（Long-horizon）的 Web 智能体或具身智能（Robotics）环境中进行大规模验证。

（完）
