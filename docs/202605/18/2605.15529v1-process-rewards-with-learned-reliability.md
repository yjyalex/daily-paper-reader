---
title: Process Rewards with Learned Reliability
title_zh: 具有可学习可靠性的过程奖励
authors: "Jinyuan Li, Langlin Huang, Chengsong Huang, Shaoyang Xu, Donghong Cai, Yuyi Yang, Wenxuan Zhang, Jiaxin Huang"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15529v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 提出了一种用于推理步骤级反馈的分布过程奖励模型 (PRM)
tldr: 针对过程奖励模型（PRM）仅输出单一分数且缺乏可靠性评估的问题，本文提出BetaPRM。它通过Beta分布建模步骤成功率及其可靠性，利用Beta-Binomial似然函数学习蒙特卡洛采样的成功分布。基于此，研究者开发了自适应计算分配（ACA）策略，在推理中根据可靠性信号动态调整计算资源。实验证明，BetaPRM在提升推理准确率的同时，显著降低了计算开销。
selection_source: fresh_fetch
motivation: 现有的过程奖励模型（PRM）仅提供单一奖励分数，无法指示预测结果的可靠性，导致下游应用难以区分不确定的奖励信号。
method: 提出BetaPRM，通过Beta-Binomial似然函数学习步骤成功率的Beta分布，从而同时预测奖励值及其置信度，并据此实现自适应计算分配（ACA）。
result: "在四个基准测试中，BetaPRM不仅提升了Best-of-N推理的准确率，还通过ACA策略在保持高精度的前提下减少了高达33.57%的Token消耗。"
conclusion: 引入可靠性信号的分布奖励模型能有效优化推理过程中的资源分配，是提升大语言模型推理效率和准确性的关键方向。
---

## 摘要
过程奖励模型 (PRM) 为推理提供步骤级反馈，但目前的 PRM 通常仅为每个步骤输出单个奖励分数。因此，下游方法必须将不完美的步骤级奖励预测视为可靠的决策信号，而没有任何关于何时应该信任这些预测的指示。我们提出了 BetaPRM，这是一种分布式的 PRM，它既能预测步骤级的成功概率，也能预测该预测的可靠性。给定来自蒙特卡洛续写的步骤成功监督，BetaPRM 学习一个 Beta 置信度，通过 Beta-Binomial 似然来解释观察到的成功续写数量，而不是回归到作为点目标的有限样本成功率。这种学习到的可靠性信号指示了何时应该信任步骤奖励，使下游应用能够区分可靠的奖励和不确定的奖励。作为一种应用，我们为 PRM 引导的 Best-of-N 推理引入了自适应计算分配 (ACA)。ACA 利用学习到的可靠性信号，在获得可靠的高奖励解决方案时停止，并在不确定的候选前缀上投入额外的计算。在四个骨干模型和四个推理基准上的实验表明，BetaPRM 在保持标准步骤级错误检测的同时，改进了 PRM 引导的 Best-of-N 选择。基于该信号，ACA 改进了相对于固定预算 Best-of-16 的准确率-Token 权衡，在提高最终答案准确率的同时，将 Token 使用量减少了高达 33.57%。

## Abstract
Process Reward Models (PRMs) provide step-level feedback for reasoning, but current PRMs usually output only a single reward score for each step. Downstream methods must therefore treat imperfect step-level reward predictions as reliable decision signals, with no indication of when these predictions should be trusted. We propose BetaPRM, a distributional PRM that predicts both a step-level success probability and the reliability of that prediction. Given step-success supervision from Monte Carlo continuations, BetaPRM learns a Beta belief that explains the observed number of successful continuations through a Beta-Binomial likelihood, rather than regressing to the finite-sample success ratio as a point target. This learned reliability signal indicates when a step reward should be trusted, enabling downstream applications to distinguish reliable rewards from uncertain ones. As one application, we introduce Adaptive Computation Allocation (ACA) for PRM-guided Best-of-N reasoning. ACA uses the learned reliability signal to stop when a high-reward solution is reliable and to spend additional computation on uncertain candidate prefixes. Experiments across four backbones and four reasoning benchmarks show that BetaPRM improves PRM-guided Best-of-N selection while preserving standard step-level error detection. Built on this signal, ACA improves the accuracy--token tradeoff over fixed-budget Best-of-16, reducing token usage by up to 33.57% while improving final-answer accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Process Rewards with Learned Reliability》（具有可学习可靠性的过程奖励）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前的大语言模型（LLM）推理常依赖**过程奖励模型（PRM）**来提供步骤级的反馈。然而，现有的 PRM 通常只输出一个标量分数（点估计），无法表达其预测的**确定性或可靠性**。
*   **痛点**：下游应用（如 Best-of-N 采样或搜索）被迫无差别地信任所有奖励信号。如果 PRM 对某个步骤的判断是“瞎猜”的，错误的奖励信号会误导模型选择错误的推理路径。
*   **核心目标**：开发一种能够同时预测“步骤成功率”及其“预测可靠性”的分布奖励模型，并利用该可靠性信号优化推理效率。

### 2. 论文提出的方法论：BetaPRM 与 ACA
论文提出了 **BetaPRM** 框架，其核心思想是将奖励建模为概率分布而非单一数值。

*   **Beta 分布建模**：
    *   不同于传统 PRM 预测单一分数 $r$，BetaPRM 预测 Beta 分布的两个参数 $\alpha$ 和 $\beta$。
    *   该分布的均值 $\frac{\alpha}{\alpha+\beta}$ 代表步骤的成功概率，而总和 $\alpha+\beta$（浓度参数）则代表对该预测的**可靠性（Reliability）**或置信度。
*   **Beta-Binomial 学习目标**：
    *   在训练阶段，利用蒙特卡洛（MC）采样为每个中间步骤生成 $n$ 次续写，记录成功次数 $k$。
    *   模型不直接回归胜率 $k/n$，而是通过 **Beta-Binomial 似然函数**进行训练。这种方法能更好地处理有限采样带来的统计噪声，学习到更鲁棒的分布。
*   **自适应计算分配（ACA）策略**：
    *   利用学习到的可靠性信号，在推理时动态分配计算资源。
    *   **早停机制**：如果当前生成的候选答案获得了“高且可靠”的奖励，则提前停止采样，节省 Token。
    *   **动态加权**：在 Best-of-N 筛选中，结合奖励值和可靠性分数进行重排序。

### 3. 实验设计
*   **数据集/场景**：涵盖了数学推理的四大基准：**GSM8K**、**MATH**、**MATH-500** 和 **OlympiadBench**。
*   **骨干模型（Backbones）**：使用了四种主流开源模型：**Llama-3-8B**、**Qwen2.5-7B**、**DeepSeek-Math-7B** 和 **Mistral-7B**。
*   **对比方法**：
    *   **Standard PRM**：传统的点估计过程奖励模型。
    *   **ORM (Outcome Reward Model)**：仅对最终结果进行评价的模型。
    *   **Fixed-budget Best-of-N**：固定采样次数（如 Best-of-16）的基准。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出总的 GPU 小时数，但提到在训练阶段使用了标准的分布式训练框架。考虑到其使用的骨干模型规模（7B-8B）以及需要进行大量的蒙特卡洛采样（每个步骤 16-32 次续写）来生成训练标签，其**数据准备阶段的算力消耗显著高于普通 PRM**。
*   **推理效率**：实验重点强调了推理时的节省，通过 ACA 策略，在相同精度下显著降低了计算开销。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分。研究者在 4 个不同的模型架构和 4 个难度各异的数据集上验证了通用性。
*   **消融实验**：论文对比了不同的可靠性度量指标（如方差、熵、浓度参数），并验证了 Beta-Binomial 损失函数相对于均方误差（MSE）的优越性。
*   **客观性**：通过帕累托前沿（Pareto Front）曲线展示了“准确率 vs. Token 消耗”的权衡，证明了 BetaPRM 在不同计算预算下均优于基准。

### 6. 主要结论与发现
*   **性能提升**：BetaPRM 在 Best-of-N 任务中始终优于标准 PRM，证明了可靠性信号有助于更精准的答案筛选。
*   **大幅减支**：引入 ACA 策略后，在保持或提升 Best-of-16 准确率的前提下，**Token 消耗量减少了 18.21% 到 33.57%**。
*   **错误检测**：BetaPRM 在识别推理步骤中的逻辑错误（Step-level error detection）方面表现出与最强基准持平或更好的性能。

### 7. 优点
*   **理论优雅**：将不确定性建模引入过程奖励，解决了 PRM 盲目自信的问题。
*   **实用性强**：ACA 策略直接解决了 LLM 推理成本高昂的痛点，具有很高的工业应用价值。
*   **鲁棒性**：Beta-Binomial 似然函数能有效处理训练数据中由于采样次数有限导致的“伪胜率”噪声。

### 8. 不足与局限
*   **训练成本高**：为了获得 Beta 分布的监督信号，需要对每个中间步骤进行大量的 MC 采样，这使得训练数据的获取成本远高于普通 PRM。
*   **复杂度增加**：相比单值输出，模型需要预测多个参数，且推理逻辑（ACA）比简单的 Best-of-N 更复杂。
*   **应用范围限制**：目前主要集中在有明确正误判定的数学推理任务上，在创意写作或开放式对话等难以定义“步骤成功率”的领域，该方法的适用性尚待验证。

（完）
