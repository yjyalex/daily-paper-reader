---
title: Internalizing Curriculum Judgment for LLM Reinforcement Fine-Tuning
title_zh: 大语言模型强化微调中的课程判断内化
authors: "Han Zheng, Yining Ma, Karthick Gunasekaran, Bharathan Balaji, Zheng Du, Shiv Vitaladevuni, Cathy Wu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11235v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型强化微调的内化课程判断
tldr: "本研究针对大语言模型强化微调（RFT）中课程学习依赖外部判断导致的不匹配问题，提出了METIS框架。该框架将课程判断内化为模型的原生能力，利用提示词内的奖励方差作为信息量指标，通过上下文学习动态分配训练资源。此外，METIS通过联合优化标准奖励与自我判断奖励，实现了“学习如何学习”的元认知闭环。实验证明，该方法在数学、代码等多个基准测试中表现优异，且收敛速度提升高达67%。"
selection_source: fresh_fetch
motivation: 现有的强化微调课程学习方法依赖外部启发式规则或辅助模型，容易与策略模型的训练动态产生失配。
method: 提出METIS框架，通过上下文学习预测奖励方差来评估提示词信息量，并联合优化任务奖励与自我判断奖励以实现元认知。
result: "在数学推理、代码生成和函数调用等多个基准测试中，METIS不仅提升了模型性能，还将收敛速度加快了最高67%。"
conclusion: 该研究通过内化课程判断，为大语言模型强化微调建立了一种简单、闭环且高效的自主学习新范式。
---

## 摘要
在大语言模型强化微调（RFT）中，课程学习驱动了效率和性能的提升。然而，现有方法通过手工设计的启发式规则或辅助模型将课程判断外部化，这可能导致与策略训练动态的不对齐。在本文中，我们提出了 METIS（元认知内化自我判断），这是一个将课程判断内化为原生能力的新型框架。利用“提示词内奖励方差能有效衡量提示词信息量”这一关键观察，METIS 将最近的训练结果作为轻量级上下文学习示例来预测该指标。这种内在的自我判断随后动态地决定了训练分配。此外，METIS 通过联合优化标准 RFT 奖励和自我判断奖励，闭合了判断与优化之间的环路。这使得策略能够学习“下一步该学习什么”，形成一种元认知形式。在涵盖数学推理、代码生成到智能体函数调用的广泛离散和连续 RFT 基准测试中，METIS 始终表现出卓越的性能，同时将收敛速度提升了高达 67%。通过绕过手工设计的启发式规则和辅助模型，我们的工作为大语言模型强化微调建立了一个简单、闭环且高效的课程内化范式。

## Abstract
In LLM Reinforcement Fine-Tuning (RFT), curriculum learning drives both efficiency and performance. Yet, current methods externalize curriculum judgment via handcrafted heuristics or auxiliary models, risking misalignment with the policy's training dynamics. In this paper, we introduce METIS (METacognitive Internalized Self-judgment), a novel framework that internalizes curriculum judgment as a native capability. Leveraging a critical observation that within-prompt reward variance effectively gauges prompt informativeness, METIS predicts this metric based on recent training outcomes as lightweight in-context learning examples. This intrinsic self-judgment then dynamically dictates the training allocation. Moreover, METIS closes the loop between judgment and optimization by jointly optimizing the standard RFT rewards and a self-judgment reward. This allows the policy to learn what to learn next, as a form of metacognition. Across extensive discrete and continuous RFT benchmarks from mathematical reasoning, code generation, to agentic function-calling, METIS consistently delivers superior performance while accelerating convergence by up to 67%. By bypassing handcrafted heuristics and auxiliary models, our work establishes a simple, closed-loop, and highly efficient curriculum internalization paradigm for LLM reinforcement fine-tuning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Internalizing Curriculum Judgment for LLM Reinforcement Fine-Tuning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在 LLM 的强化微调（RFT）中，并非所有数据都同等重要。传统的课程学习（Curriculum Learning）旨在挑选处于模型“近侧发展区”的题目，但现有方法通常依赖**外部判断**（如手工设计的启发式规则或额外的辅助评分模型）。
*   **核心问题**：外部判断往往无法实时、准确地跟踪策略模型（Policy）快速变化的训练状态，导致训练效率低下或性能受限。
*   **整体含义**：论文提出将“判断学什么”的能力**内化**到模型自身，使其具备元认知（Metacognition）能力，从而实现闭环的自主课程学习。

### 2. 论文提出的方法论：METIS
*   **核心思想**：利用模型自身的**上下文学习（ICL）**能力，在正式训练前预测提示词（Prompt）的“信息量”，并根据预测结果分配计算资源。
*   **关键技术细节**：
    *   **信息量度量**：采用“提示词内奖励方差”（Within-prompt reward variance）作为代理指标。方差大意味着模型对该题目的表现不稳定（有对有错），学习信号最强；方差为 0 则意味着题目太易或太难，无学习价值。
    *   **自我判断（Self-judgment）**：模型通过 ICL 观察最近训练中的“提示词-方差”对（校准记忆），预测新候选题目的预期方差 $\hat{v}_\theta(x)$。
    *   **联合优化**：模型同时优化两个目标：
        1.  **任务奖励**：标准的强化学习损失，提升解题能力。
        2.  **判断奖励**：通过实际 Rollout 得到的方差与预测方差的均方误差来计算奖励 $R_{judge}$，训练模型预测得更准。
*   **算法流程**：从池中采样候选集 $\rightarrow$ 模型 ICL 预测方差 $\rightarrow$ 选取高方差题目进行 Rollout $\rightarrow$ 计算任务奖励和实际方差 $\rightarrow$ 联合更新模型参数 $\rightarrow$ 更新校准记忆。

### 3. 实验设计
*   **应用场景与数据集**：
    *   **数学推理（离散奖励）**：训练集 DAPO-17k；测试集 AIME24/25, MATH500, Minerva。
    *   **代码生成（连续奖励）**：训练集 CodeContests+；测试集 LiveCodeBench v6, HumanEval, MBPP, BigCodeBench。
    *   **智能体函数调用（多轮连续奖励）**：BFCL v3。
*   **对比方法（Baselines）**：
    *   **No Curriculum**：均匀采样。
    *   **SEC**：类别级调度（Category Schedule）。
    *   **ADCL**：基于手工启发式规则的难度调度（Prompt Heuristic）。
    *   **PCL**：使用独立的辅助模型进行评分（Auxiliary Model）。

### 4. 资源与算力
*   **硬件环境**：使用了 12 个节点，每个节点配备 8 张 NVIDIA H200 GPU（共计 **96 张 H200**）。
*   **训练时长**：总计消耗约 **20,000 H200 GPU 小时**。
*   **具体设置**：8B 参数模型单次运行上限为 40 小时，4B 模型为 20 小时。
*   **开销分析**：METIS 的每步额外计算开销仅为 **3.9%**，远低于需要额外 Rollout 或独立模型的基准方法。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大类任务、4 种不同规模/类型的基座模型（Qwen3-4B/8B, Llama-3.1-8B, DeepSeek-R1-Distill-Llama-8B）。
*   **消融实验**：详细探讨了 ICL 示例数量 $K$、判断损失权重 $\lambda$ 的影响，验证了“内化”和“闭环”设计的必要性。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了最终性能，还展示了训练过程中的收敛曲线和优势值（Advantage）变化，证明了 METIS 在不同奖励机制（离散 vs 连续）下的通用性。

### 6. 主要结论与发现
*   **性能卓越**：METIS 在所有基准测试中均达到或超过了最强基准（PCL），且无需额外模型。
*   **大幅加速**：相比无课程学习，METIS 在达到相同精度时可减少高达 **67%** 的墙钟时间。
*   **能力追踪**：实验证明 METIS 能够自动追踪模型的“能力边界”。在简单任务中，它能防止模型陷入已掌握的题目；在困难任务中，它能快速找到模型偶尔能做对的题目。

### 7. 优点
*   **简单高效**：无需手工设计复杂的难度函数，也无需维护昂贵的辅助模型。
*   **闭环元认知**：通过联合训练，模型不仅学会了解题，还学会了“评估自己”，实现了真正的自主学习。
*   **通用性强**：天然支持连续奖励（如代码测试通过率），解决了以往课程学习方法大多局限于二元（对/错）奖励的问题。

### 8. 不足与局限
*   **依赖基座能力**：METIS 依赖模型的 ICL 能力。如果基座模型太弱，无法理解方差预测任务，该方法可能失效。
*   **超参数敏感性**：判断损失的权重 $\lambda$ 需要精细调节，过大可能导致模型为了预测准确而放弃解题（走捷径）。
*   **稳定性量化缺失**：虽然观察到训练更稳定，但论文未提供关于训练方差或稳定性的形式化量化分析。

（完）
