---
title: Response-Conditioned Parallel-to-Sequential Orchestration for Multi-Agent Systems
title_zh: 多智能体系统中基于响应条件的并行到串行编排
authors: "Nurbek Tastan, Alex Iacob, Lorenzo Sani, Meghdad Kurmanji, Nicholas D. Lane, Samuel Horvath, Karthik Nandakumar"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15573v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 多智能体协作与通信的混合范式
tldr: 针对多智能体系统在并行模式（低延迟但准确率低）与串行模式（高准确率但高延迟）之间的权衡问题，本文提出了一种名为 Nexa 的混合协作框架。Nexa 通过一个轻量级的响应条件策略，根据初始并行响应动态预测稀疏的有向无环通信图，从而在必要时进行一轮串行优化。该方法无需外部评判模型，通过策略梯度优化，实现了在降低通信成本的同时提升任务准确率，且具有良好的泛化性。
selection_source: fresh_fetch
motivation: 现有的多智能体协作模式在最小化通信延迟与最大化响应准确率之间难以取得平衡。
method: 提出 Nexa 框架，利用轻量级 Transformer 策略根据并行响应动态生成通信拓扑，实现从并行到串行的按需转换。
result: 实验表明 Nexa 能在不同任务、智能体数量和底层模型下保持高效，显著降低了通信开销并提升了性能。
conclusion: Nexa 证明了通过响应条件化的动态拓扑调度，可以有效弥合多智能体系统中并行与串行模式的鸿沟。
---

## 摘要
多智能体系统可以通过多个大语言模型（LLM）智能体之间的协作来解决复杂任务。现有的协作框架通常以并行或串行模式运行。在并行模式下，智能体独立响应查询，随后对响应进行聚合。相比之下，串行系统允许智能体通过有向拓扑进行通信，并逐步相互优化。然而，这两种模式都难以在最小化通信和延迟的同时，最大化最终响应的准确性。在这项工作中，我们引入了一种名为 Nexa 的混合范式，这是一种可训练的基于响应条件的策略，弥合了两种模式之间的差距。Nexa 首先进行并行执行阶段，将生成的响应嵌入到共享语义空间中，然后预测一个稀疏的有向无环通信图。如果图为空，系统保持纯并行；如果非空，系统则执行一次串行消息传播。该策略是一个轻量级的 Transformer 模型，该方法避免了对外部 LLM 评判器或奖励模型的需求，也无需手工设计的测试时拓扑搜索。我们对这种混合执行问题进行了形式化，证明了生成的图在构造上是无环的，且该框架严格包含纯并行执行，并提出了一种基于策略梯度优化的训练程序。结果表明，Nexa 在一种设置下学习到的基于响应条件的策略，在智能体数量、任务或底层智能体发生变化时仍可复用，从而强调了所学通信策略的泛化性。

## Abstract
Multi-agent systems can solve complex tasks through collaboration between multiple Large Language Model agents. Existing collaboration frameworks typically operate in either a parallel or a sequential mode. In the parallel mode, agents respond independently to queries followed by aggregation of responses. In contrast, sequential systems allow agents to communicate via a directed topology and refine one another step by step. However, both modes are inadequate for achieving the desired objectives of minimizing communication and latency while simultaneously maximizing the accuracy of the final response. In this work, we introduce a hybrid paradigm called Nexa, a trainable response-conditioned policy that bridges the gap between the two modes. Nexa begins with a parallel execution stage, embeds the resulting responses into a shared semantic space, and then predicts a sparse directed acyclic communication graph. If the graph is empty, the system remains purely parallel; if it is non-empty, the system performs one sequential message propagation. The policy is a lightweight transformer model, and the method avoids the need for external LLM judges or reward models, as well as hand-crafted test-time topology search. We formalize this hybrid execution problem, show that the resulting graph is acyclic by construction, and that the framework strictly subsumes pure parallel execution, and present a training procedure based on policy-gradient optimization. Results demonstrate that the response-conditioned policy learned by Nexa under one setting can be reused when the number of agents, the task, or the underlying agent changes, thus emphasizing the generalizability of the learned communication policy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NEXA** 的多智能体系统（MAS）编排框架，旨在通过动态预测通信拓扑来优化大语言模型（LLM）智能体之间的协作。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多智能体协作模式通常在“并行”（独立响应后聚合）和“串行”（按拓扑结构逐步优化）之间二选一。并行模式延迟低但缺乏纠错能力；串行模式虽能提升准确率，但通信开销大、延迟高，且往往依赖预设或复杂的拓扑搜索。
*   **研究动机**：作者认为不应预设协作模式，而应根据智能体生成的**实际响应内容**来决定是否需要进一步的串行通信。如果初始响应一致性高，则保持并行；如果存在分歧或互补信息，则按需构建通信图进行一轮串行优化。

### 2. 方法论：NEXA 框架
NEXA 的核心思想是“基于响应条件的并行到串行转换”，具体分为五个阶段：
1.  **并行草案阶段**：所有智能体独立对查询 $Q$ 生成初始响应 $R^{(0)}$。
2.  **语义嵌入**：使用轻量级编码器（如 MiniLM）将响应映射到语义空间，并计算每个响应的“贡献得分”（基于与平均向量的余弦相似度）。
3.  **图策略预测**：
    *   使用一个轻量级的 **Transformer 编码器** 处理响应嵌入。
    *   根据贡献得分对智能体进行排序，确保通信始终从高贡献者流向低贡献者，从而在构造上保证**有向无环图（DAG）**的合法性。
    *   策略输出一个稀疏的邻接矩阵，决定哪些智能体需要接收来自上游的反馈。
4.  **混合执行**：
    *   如果预测的图为空，系统维持并行模式，直接进入聚合。
    *   如果非空，则执行一轮串行传播，选定的智能体根据父节点的响应更新自己的回答。
5.  **无评判聚合**：采用基于加权质心的选择机制（无需外部 LLM 评判器），选出与整体语义最接近的响应作为最终答案。

### 3. 实验设计
*   **数据集**：主要在 **AQUA-RAT**（数学推理）、**HumanEval**（代码生成）和 **GSM8K**（小学数学）上进行评估。此外还扩展到了 GSM-Hard 和 MMLU。
*   **基准方法（Benchmarks）**：
    *   基础方法：Single-agent, CoT, Self-Consistency (SC)。
    *   先进协作框架：**SelfOrg***, **GPTSwarm**, **AgentPrune**, **G-Designer**。
*   **评估维度**：准确率（Accuracy）、Token 消耗量（成本）、通信稀疏度以及跨任务/模型/规模的泛化能力。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 40GB GPU** 进行模型推理和策略训练。
*   **训练细节**：策略网络非常轻量（1层、1个注意力的 Transformer），训练采用 REINFORCE 算法，通常进行 50 次策略更新，Batch Size 为 32。
*   **时长**：文中未明确给出总训练时长，但强调了该策略是“轻量级”的，且避免了昂贵的 LLM 评判开销。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括：
    *   **主实验**：在三个核心任务上对比了 7 种基准方法。
    *   **泛化性实验**：测试了在智能体数量变化（5 到 20 个）、任务迁移（AQUA ↔ GSM8K）、模型规模迁移（1.5B 迁移至 7B）以及模型代际迁移（Qwen 2.5 迁移至 3.5）下的表现。
    *   **消融实验**：对比了不同的策略骨干（Transformer vs GNN）和优化算法（PG vs GRPO）。
*   **充分性评价**：实验设计非常全面且客观。特别是“跨轴泛化”实验有力地证明了 NEXA 学习到的是通用的协作逻辑，而非对特定任务或模型身份的过拟合。

### 6. 主要结论与发现
*   **性能优异**：NEXA 在平均准确率上达到 60.90%，优于所有对比的 SOTA 框架。
*   **成本效益高**：相比 SelfOrg* 减少了约 **35%** 的 Token 消耗，相比 GPTSwarm 减少了超过 **50%**。
*   **强泛化性**：在一个设置下训练的策略可以直接应用于不同数量的智能体或更强大的底层模型，且性能损失极小。
*   **稀疏通信**：NEXA 倾向于生成低边数的通信图，证明了其能够精准识别关键的通信需求。

### 7. 优点与亮点
*   **动态性**：不同于静态拓扑，NEXA 根据“智能体说了什么”来决定“谁该跟谁说”，更符合实际协作逻辑。
*   **轻量且独立**：不依赖外部 LLM 评判器，降低了推理延迟和成本；策略网络极小，易于部署。
*   **构造性 DAG**：通过贡献排序巧妙解决了循环依赖问题，无需复杂的后处理。
*   **身份无关性**：策略只看语义内容，不看智能体标签，这使其具备了极佳的跨模型迁移能力。

### 8. 不足与局限
*   **任务类型限制**：实验主要集中在有明确答案的推理和编程任务上，对于长文本生成、多轮对话或开放式规划等任务的适用性尚待验证。
*   **嵌入模型依赖**：系统的表现高度依赖于语义嵌入模型（MiniLM）的质量，如果嵌入无法捕捉任务相关的细微差别，策略可能会失效。
*   **单轮传播限制**：目前 NEXA 仅设计了一轮串行优化，对于极其复杂的、需要多轮迭代纠错的任务，可能需要扩展到多轮动态编排。

（完）
