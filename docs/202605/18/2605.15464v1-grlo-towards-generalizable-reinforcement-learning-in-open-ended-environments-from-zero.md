---
title: "GRLO: Towards Generalizable Reinforcement Learning in Open-Ended Environments from Zero"
title_zh: GRLO：迈向开放式环境中从零开始的可泛化强化学习
authors: "Shangjian Yin, Yu Fu, Yue Dong, Zhouxing Shi"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15464v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 面向推理后训练的可验证奖励强化学习
tldr: 本研究针对大语言模型后训练中强化学习（RL）计算成本高昂的问题，提出了GRLO方法。该方法通过在开放式环境中使用极少量的交互数据（5K条提示词）从零开始进行RLHF训练，旨在验证对话能力的提升能否隐式迁移至数学推理和代码生成等下游任务。实验表明，GRLO在极低的计算资源消耗下（仅需22.7 GPU小时），使Qwen3-4B模型的平均性能从24.1提升至63.1，显著优于高成本的领域内RLVR基准，为构建高效且通用的后训练模型提供了新路径。
selection_source: fresh_fetch
motivation: 现有的强化学习后训练方法（如RLVR）虽然在特定领域表现优异，但其巨大的计算开销和数据需求成为了广泛应用的障碍。
method: 提出GRLO框架，通过在开放式环境中使用少量交互数据进行从零开始的RLHF训练，研究对话能力的跨领域泛化性。
result: 在Qwen3-4B模型上，仅需5K提示词和22.7个GPU小时，性能便从24.1提升至63.1，计算成本比RLVR基准降低了68倍。
conclusion: GRLO证明了低成本的开放式RLHF训练能够有效提升模型的通用能力，是构建高性能后训练模型的一种简单且高效的方案。
---

## 摘要
后训练已成为解锁大语言模型能力的关键步骤，强化学习（RL）已成为一种至关重要的范式。近期的基于强化学习的后训练日益分为两种范式：基于人类反馈的强化学习（RLHF），即利用目标领域的人类偏好信号优化模型；以及基于可验证奖励的强化学习（RLVR），即在有验证器支持的环境中运行。后者在近期以推理为导向的后训练中占据主导地位，因为它在特定领域任务（如推理）上提供了更强的增益和更高的效率。然而，尽管领域内强化学习训练取得了显著的性能，但它仍需要大量的 GPU 计算资源，这仍然是广泛采用的主要障碍。在这项工作中，我们研究了在开放式环境中通过少量交互从零开始学习的 RLHF 的泛化能力，并调查了其显式获得的对话能力是否可以隐式地迁移到下游任务（如数学推理和代码生成），即 GRLO。具体而言，在 Qwen3-4B-Base 基座模型上，GRLO 仅需 5K 条提示词和 22.7 个 GPU 小时，就将所有领域的平均性能从 24.1 提升至 63.1，与强大的领域内 RLVR 基线相比，所需数据减少了约 46 倍，计算量减少了约 68 倍。所得模型甚至可以与 Qwen 发布的、需要更高训练成本的后训练模型相媲美。值得注意的是，随后的领域内 RLVR 阶段仅带来了选择性的增益，主要体现在更难的竞赛数学基准测试上。我们希望 GRLO 为构建具有广泛能力的后训练模型提供一种简单且高效的方案。我们的代码和数据可在 https://github.com/SJY8460/GRLO 获取。

## Abstract
Post-training has become a crucial step for unlocking the capabilities of large language models, with reinforcement learning (RL) emerging as a critical paradigm. Recent RL-based post-training has increasingly split into two paradigms: reinforcement learning from human feedback (RLHF), which optimizes models using human preference signals in target domains, and reinforcement learning from verifiable rewards (RLVR), which operates in verifier-backed environments. The latter has dominated recent reasoning-oriented post-training because it delivers stronger gains and higher efficiency on domain-specific tasks (e.g., reasoning). However, although in-domain RL training achieves promising performance, it still requires a substantial amount of GPU compute, which remains a major barrier to broad adoption. In this work, we study the generalization ability of RLHF learned from scratch from a small set of interactions in open-ended environments, and investigate whether the conversational abilities it explicitly acquires can implicitly transfer to downstream tasks such as mathematical reasoning and code generation, namely GRLO. Specifically, on Qwen3-4B-Base backbone, GRLO improves the average performance across all domains from 24.1 to 63.1 with only 5K prompts and 22.7 GPU hours, requiring about $46\times$ less data and $68\times$ less compute than a strong in-domain RLVR baseline. The resulting model is even competitive with Qwen's released post-trained models which required a much larger training cost. Notably, a subsequent in-domain RLVR stage brings only selective gains, mainly on harder competition-math benchmarks. We hope GRLO offers a simple and efficient recipe for building broadly capable post-trained models. Our code and data will be available at: \href{https://github.com/SJY8460/GRLO}{https://github.com/SJY8460/GRLO}.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **GRLO**（Generalizable Reinforcement Learning in Open-Ended Environments）的后训练方法，旨在通过极低成本的开放式环境强化学习，解锁大语言模型的通用能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的强化学习（RL）后训练主要分为 RLHF（侧重对话偏好）和 RLVR（侧重可验证的推理，如数学/代码）。RLVR 虽然效果好，但**计算成本极高**且**领域局限性强**（难以迁移到非验证领域）。
*   **研究动机**：探索是否可以通过在**少量、多样化、开放式**的提示词（Prompts）上进行从零开始的强化学习（RLHF-Zero），让模型获得的对话组织能力隐式地迁移到数学推理和代码生成等下游任务中，从而实现高效且通用的性能提升。

### 2. 方法论
*   **核心思想**：不依赖特定领域的验证器（Verifier），而是利用一个高质量的开源奖励模型（Reward Model），在包含科学分析、人文论证、政策研究等多样化主题的开放式环境中进行 PPO 优化。
*   **关键技术细节**：
    *   **环境构建**：精心筛选了约 5K 条合成的开放式提示词，涵盖政策/历史、生物医学、技术工程等领域，强调长篇推理和概念解释。
    *   **训练流程**：直接从基座模型（Base Model）开始，使用 PPO 算法进行优化。
    *   **目标函数**：采用标准的 KL 正则化 PPO 目标，通过奖励模型 $r_\phi(x, y)$ 对响应进行评分，不使用任何领域特定的正确性信号。
    *   **后续阶段（可选）**：在 GRLO 之后可以加入一个轻量级的领域内 RLVR 阶段，以进一步强化竞赛级数学能力。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **数学推理**：Math500, GPQA, AIME24/25, OlympiadBench, Minerva。
    *   **代码生成**：HumanEval, MBPP。
    *   **通用对话**：AlpacaEval 2 (Length-Controlled)。
*   **对比方法**：
    *   基座模型（Base）。
    *   官方发布的指令微调/后训练模型（如 Qwen3-4B-Non-thinking）。
    *   领域内 SFT（MathSFT, OpenSFT）。
    *   大规模开放式 RL 基准（General-Reasoner-4B）。
    *   直接进行领域内 RLVR 训练的模型。
*   **评估模型**：Qwen3-4B/8B, Qwen2.5-3B, Llama3.2-3B。

### 4. 资源与算力
*   **算力消耗**：在 Qwen3-4B 模型上，GRLO 仅需 **22.7 个 GPU 小时**。
*   **数据规模**：仅使用 **5K 条提示词**。
*   **效率对比**：相比于强大的 General-Reasoner-4B 基准，GRLO 使用的**数据量减少了约 46 倍**，**计算量减少了约 68 倍**，且性能相当甚至更优。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同的模型家族/尺寸上验证了方法的有效性。
*   **消融实验**：
    *   **训练环境**：对比了“开放式环境”与“纯数学环境”的效果。
    *   **优化器**：对比了 PPO 与 GRPO。
    *   **数据规模**：测试了从 1K 到 5K 数据的扩展曲线。
    *   **训练时长**：分析了不同 Epoch 对性能的影响。
*   **充分性评价**：实验设计较为全面，通过跨模型、跨任务的对比证明了结论的普适性，且通过长度分析排除了“性能提升仅靠增加字数”的嫌疑，实验结果客观公平。

### 6. 主要结论与发现
*   **跨领域迁移**：在开放式对话环境中的 RL 训练能显著提升数学和代码能力，证明了 RL 主要是通过重塑输出分布来挖掘模型预训练阶段已有的潜能。
*   **高效性**：极少量的数据（5K）和算力即可达到甚至超过大规模后训练模型的效果。
*   **RLVR 的角色**：领域内 RLVR 并非通用能力提升的主力，其作用更多体现在对极难竞赛题目的“补强”上。
*   **模型表现**：GRLO 处理后的 Qwen3-4B 平均分从 24.1 飙升至 63.1。

### 7. 优点
*   **极高效率**：为资源有限的研究者提供了一种低成本构建高性能通用模型的方法。
*   **通用性强**：不依赖于自动验证器，适用于无法轻易判断对错的开放式任务。
*   **响应质量高**：相比于产生冗长推理链的模型，GRLO 生成的内容更简洁且组织良好。

### 8. 不足与局限
*   **奖励模型依赖**：GRLO 的效果高度依赖于外部奖励模型（如 Skywork-Reward）的质量，若奖励模型存在偏见或能力不足，会直接限制模型上限。
*   **极端推理能力**：虽然通用能力提升显著，但在 AIME 等顶级数学竞赛题上，仍需结合 RLVR 才能达到顶尖水平。
*   **数据源偏差**：虽然使用了 5K 条提示词，但这些提示词的合成质量和分布对结果影响较大，尚未探讨在更差质量数据下的鲁棒性。

（完）
