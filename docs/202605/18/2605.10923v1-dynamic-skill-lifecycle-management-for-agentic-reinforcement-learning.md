---
title: Dynamic Skill Lifecycle Management for Agentic Reinforcement Learning
title_zh: 智能体强化学习的动态技能生命周期管理
authors: "Junhao Shen, Teng Zhang, Xiaoyan Zhao, Hong Cheng"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10923v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 智能体强化学习的动态技能生命周期管理
tldr: 针对大语言模型智能体在强化学习中外部技能管理效率低下的问题，本文提出SLIM框架。该框架将活跃技能集视为动态优化变量，通过留一法验证评估技能的边际贡献，并执行保留、退役或扩展操作。实验证明SLIM在ALFWorld和SearchQA任务上显著优于现有基准，揭示了技能内化与外部保留共存的动态演化规律。
selection_source: fresh_fetch
motivation: 现有方法假设技能集是静态累积或完全内化的，忽略了有限参数容量下技能贡献随任务阶段变化的动态性。
method: 提出SLIM框架，通过留一法验证评估技能边际贡献，动态执行高价值技能保留、冗余技能退役及缺失能力扩展。
result: 在ALFWorld和SearchQA基准测试中，SLIM比最优基准方法平均提升了7.1个百分点。
conclusion: 策略学习与外部技能保留并非互斥，SLIM为基于技能的智能体强化学习提供了一种更通用且高效的动态管理范式。
---

## 摘要
大语言模型智能体越来越多地依赖外部技能来解决复杂任务，技能作为模块化单元，扩展了智能体仅靠参数化记忆所能支持的能力。现有方法假设外部技能要么作为持久引导不断累积，要么被内化到策略中，最终导致零技能推理。我们认为这一假设过于苛刻，因为在参数容量有限且不同技能的边际贡献不均的情况下，最优的活跃技能集是非单调的，且取决于具体任务和阶段。在这项工作中，我们提出了 SLIM，一个用于智能体强化学习（RL）的动态技能生命周期管理框架，它将活跃的外部技能集视为一个与策略学习共同更新的动态优化变量。具体而言，SLIM 通过留一技能验证（leave-one-skill-out validation）来估计每个活跃技能的边际外部贡献，然后应用三种生命周期操作：保留高价值技能、在充分暴露后退役贡献变得微不足道的技能，以及在持续失败揭示能力覆盖缺失时扩展技能库。实验表明，SLIM 在 ALFWorld 和 SearchQA 上的表现平均比最佳基准线高出 7.1 个百分点。结果进一步表明，策略学习和外部技能保留并非互斥：部分技能被吸收到策略中，而其他技能则继续提供外部价值，这证明了 SLIM 是基于技能的智能体强化学习的一种更通用的范式。

## Abstract
Large language model agents increasingly rely on external skills to solve complex tasks, where skills act as modular units that extend their capabilities beyond what parametric memory alone supports. Existing methods assume external skills either accumulate as persistent guidance or internalized into the policy, eventually leading to zero-skill inference. We argue this assumption is overly restrictive, since with limited parametric capacity and uneven marginal contribution across skills, the optimal active skill set is non-monotonic, task- and stage-dependent. In this work, we propose SLIM, a framework of dynamic Skill LIfecycle Management for agentic reinforcement learning (RL), which treats the active external skill set as a dynamic optimization variable jointly updated with policy learning. Specifically, SLIM estimates each active skill's marginal external contribution through leave-one-skill-out validation, then applies three lifecycle operations: retaining high-value skills, retiring skills whose contribution becomes negligible after sufficient exposure, and expanding the skill bank when persistent failures reveal missing capability coverage. Experiments show that SLIM outperforms the best baselines by an average of 7.1% points across ALFWorld and SearchQA. Results further indicate that policy learning and external skill retention are not mutually exclusive: some skills are absorbed into the policy, while others continue to provide external value, supporting SLIM as a more general paradigm for skill-based agentic RL.

---

## 论文详细总结（自动生成）

以下是对论文《Dynamic Skill Lifecycle Management for Agentic Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大型语言模型（LLM）智能体在强化学习（RL）过程中如何管理“外部技能库”。
*   **研究动机**：现有的基于技能的智能体通常遵循两种单调范式：一是**持续累积**（如 SkillRL），将技能作为持久引导不断增加；二是**逐渐消除**（如 Skill0），试图将技能完全内化到模型参数中。
*   **核心矛盾**：作者认为这两种假设都过于极端。由于模型参数容量有限且不同技能的边际贡献不均，最优的活跃技能集应该是**非单调的**，且与任务阶段高度相关。
*   **整体含义**：论文提出了 **SLIM** 框架，旨在寻找智能体能力的“外部边界”——即哪些能力应该内化，哪些应该作为外部模块保留，哪些需要动态扩展。

### 2. 方法论：核心思想与技术细节
SLIM 将活跃技能集视为一个动态优化变量，与策略学习同步更新。其核心流程包括：
*   **分层技能检索**：根据任务类型检索通用的和任务特定的技能，减少搜索空间。
*   **边际外部贡献（MEC）估计**：这是 SLIM 的关键。通过**留一法验证（Leave-one-skill-out validation）**来计算：
    $$\Delta_t(s) = Perf(V_t(s); A_t) - Perf(V_t(s); A_t \setminus \{s\})$$
    即：在当前策略下，移除某个技能后验证集性能的下降程度。
*   **动态生命周期管理操作**：
    1.  **保留（Retain）**：若 MEC 显著为正，说明该技能仍有外部价值，继续保留。
    2.  **退役（Retire）**：若 MEC 接近零或为负，且经过足够次数的暴露（Exposure），则认为该技能已内化、冗余或过时，将其从活跃集中移除。
    3.  **扩展（Expand）**：若在有技能支持的情况下仍持续失败，则触发技能创造流程（利用 LLM 生成新技能）以覆盖能力缺失。
*   **交替优化**：在 GRPO 策略更新阶段固定技能集；在审计阶段固定策略，更新技能集。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：具身智能体任务，包含清洁、加热、冷却等 6 类家庭作业。
    *   **SearchQA**：搜索增强的问答任务，涵盖 NQ、HotpotQA、TriviaQA 等 7 个来源。
*   **对比方法（Baselines）**：
    *   **提示词方法**：Zero-Shot, Few-Shot（含带技能版本）。
    *   **智能体/记忆方法**：ReAct, Reflexion, Mem0, ExpeL。
    *   **强化学习方法**：标准的 **GRPO**、持续累积的 **SkillRL**、强制内化的 **Skill0**、以及 **EvolveR**。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **模型使用**：策略模型采用 **Qwen3-4B**，检索模型采用 **Qwen3-Embedding-0.6B**。
*   **训练时长**：ALFWorld 训练约 **20 小时**，SearchQA 训练约 **25 小时**。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量（如 8x A100），但提到总计算量级在 $10^{19}$ 到 $10^{20}$ FLOPs 之间。

### 5. 实验数量与充分性
*   **实验规模**：在两个截然不同的基准（具身交互 vs. 知识检索）上进行了大规模测试。
*   **消融实验**：非常充分。分别验证了“无退役”、“无扩展”、“随机审计”以及“固定技能数量”对性能的影响。
*   **客观性**：所有 RL 方法均不使用冷启动 SFT（监督微调），确保了公平竞争；使用了独立的训练/验证/测试集，并进行了 Bootstrap 鲁棒性分析。

### 6. 主要结论与发现
*   **性能领先**：SLIM 在两个基准上平均超越最佳基准线 **7.1%**。
*   **非单调演化**：实验观察到活跃技能数量呈现“先增后减再趋于稳定”的非单调轨迹，证明了动态管理的必要性。
*   **共存规律**：策略学习与外部技能保留并非互斥。部分技能（如高频通用技能）容易被内化，而长尾、复杂的程序性技能更适合作为外部模块保留。
*   **边界效应**：强制零技能推理（Skill0）会导致性能在后期骤降，而持续累积（SkillRL）会引入噪声和上下文负担。

### 7. 优点（亮点）
*   **范式创新**：打破了技能“必须全部内化”或“必须无限累积”的思维定式，提出了“外部能力边界”的概念。
*   **数据驱动的剪枝**：利用 MEC 这种量化指标来决定技能的去留，比简单的启发式规则更科学。
*   **鲁棒性强**：实验证明即使初始技能库存在噪声或缺失，SLIM 也能通过扩展和退役操作自我修复。

### 8. 不足与局限
*   **局部估计偏差**：MEC 采用的是留一法局部估计，可能无法捕捉到多个技能之间的复杂高阶交互作用。
*   **超参数敏感性**：保留、退役和扩展的阈值（$\tau$）需要针对不同领域进行验证集调优，迁移成本可能较高。
*   **审计开销**：虽然通过限制审计频率和候选数量降低了成本，但留一法验证在技能库极大时仍会带来额外的推理负担。

（完）
