---
title: "Route by State, Recover from Trace: STAR with Failure-Aware Markov Routing for Multi-Agent Spatiotemporal Reasoning"
title_zh: 按状态路由，从轨迹恢复：具有故障感知马尔可夫路由的 STAR 多智能体时空推理框架
authors: "Ruiyi Yang, Lihuan Li, Hao Xue, Flora D. Salim"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10057v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体时空推理的路由与协作
tldr: 针对复杂时空推理中多智能体协作的路由难题，本文提出STAR框架。该框架将智能体间的控制显式化为基于状态的转移策略，利用包含专家规则与从执行轨迹中学习到的故障恢复路径的路由矩阵进行调度。STAR能针对不同类型的失败（如格式错误、依赖缺失）采取差异化恢复措施，而非简单的重试。实验证明，这种感知故障的马尔可夫路由机制显著提升了系统在多个基准测试中的表现，尤其是在处理偏离常规路径的复杂任务时具有更强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通常将路由决策隐含在生成过程中，导致在面对不同类型的执行失败时难以进行针对性的恢复和优化。
method: 提出STAR框架，通过外部化的马尔可夫路由矩阵，结合专家定义的常规路径与从失败轨迹中学习到的状态感知恢复策略来管理智能体协作。
result: 在三个时空推理基准和八个大模型上的实验显示，STAR显著优于基线，特别是在需要纠正执行错误的非标路径查询中提升最为明显。
conclusion: 显式的、感知故障类型的路由机制而非单纯的智能体组合，是提升多智能体时空推理系统性能和可靠性的核心要素。
---

## 摘要
组合式时空推理通常需要系统调用多个异构专家，例如几何、时间、拓扑和轨迹智能体。一个核心问题是，当执行并非简单的成功或失败，而是以性质截然不同的方式失败时，系统应如何在专家之间进行路由。现有的工具增强型和多智能体大语言模型（LLM）系统通常将这种路由决策隐含在语言生成中，导致恢复过程具有随意性、难以解释且难以优化。本文提出了 STAR（时空智能体路由器），这是一种故障感知路由框架，它将智能体间的控制外化为一种基于当前智能体、任务类型和类型化执行状态的状态条件转移策略。STAR 的核心是一个智能体路由矩阵，它结合了专家指定的名义路线和从执行轨迹中学习到的恢复转移。由于该矩阵以不同的故障状态为条件，路由器可以针对格式错误的输出、缺失的依赖项以及工具与查询的不匹配做出不同的响应，而不是将它们统一归结为通用的重试信号。专家通过基于工具的“提取-计算-存储”协议执行，并将中间结果写入共享黑板以供下游融合。结果证明，在训练期间保留不成功的轨迹扩大了路由策略在错误状态上的支持集，从而实现了仅靠成功案例训练无法表示的恢复转移。在三个时空基准测试和八个骨干 LLM 上，STAR 优于多个基线模型，在执行偏离名义路由路径的查询上提升最为显著。针对路由器的消融实验和恢复分析进一步表明，类型化的故障感知路由，而非仅仅是专家的组合，是实现这些改进的关键因素。

## Abstract
Compositional spatiotemporal reasoning often requires a system to invoke multiple heterogeneous specialists, such as geometric, temporal, topological, and trajectory agents. A central question is how such a system should route among specialists when execution does not simply succeed or fail, but fails in qualitatively different ways. Existing tool-augmented and multi-agent LLM systems typically leave this routing decision implicit in language generation, making recovery ad hoc, difficult to interpret, and hard to optimize. This paper presents STAR (Spatio-Temporal Agent Router), a failure-aware routing framework that externalizes inter-agent control as a state-conditioned transition policy over the current agent, task type, and typed execution status. At the center of STARis an agent routing matrix that combines expert-specified nominal routes with recovery transitions learned from execution traces. Because the matrix conditions on distinct failure states, the router can respond differently to malformed outputs, missing dependencies, and tool--query mismatches, rather than collapsing them into a generic retry signal. Specialists execute through a tool-grounded extract--compute--deposit protocol and write intermediate results to a shared blackboard for downstream fusion. Results prove that retaining unsuccessful traces during training enlarges the support of the routing policy on error states, enabling recovery transitions that success-only training cannot represent. Across three spatiotemporal benchmarks and eight backbone LLMs, STAR improves over multiple baselines with the clearest gains on queries whose execution deviates from the nominal routing path. Router-specific ablations and recovery analyses further show that typed failure-aware routing, rather than specialist composition alone, is a key factor for these improvements.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STAR (Spatio-Temporal Agent Router)** 的框架，旨在解决多智能体系统在处理复杂时空推理任务时的协作与故障恢复问题。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在复杂的时空推理（如地理定位、轨迹分析、时间关系推理）中，通常需要调用多个异构专家智能体。现有的多智能体系统（MAS）或工具增强型 LLM 通常将“下一步该调用谁”的路由决策隐含在文本生成中。
*   **研究动机**：当执行过程中出现不同性质的失败（如输出格式错误、缺少上游依赖、工具与查询不匹配）时，隐含路由往往只能进行盲目的重试，缺乏针对性的恢复策略，且过程难以解释和优化。
*   **整体含义**：论文提出将智能体间的控制显式化为一个**状态感知的马尔可夫路由策略**，通过学习执行轨迹（包括失败轨迹）来构建一个能够应对多种故障模式的路由矩阵。

### 2. 论文提出的方法论
STAR 框架的核心思想是将专家协作建模为基于执行状态的转移过程。

*   **核心思想**：
    *   **状态感知路由**：路由决策不仅取决于当前智能体和任务类型，还取决于当前的**执行状态**（成功、格式错误、依赖缺失、工具不匹配）。
    *   **双系统内核**：
        *   **系统 1（名义路由）**：由专家定义的标准执行路径（针对成功状态）。
        *   **系统 2（恢复路由）**：从历史执行轨迹中学习到的恢复路径（针对错误状态）。
*   **关键技术细节**：
    *   **路由矩阵 $M$**：一个四维张量，索引为 `(当前智能体, 任务类型, 执行状态, 下一智能体)`。
    *   **执行协议（Extract-Compute-Deposit）**：智能体首先从查询中提取参数，调用确定性工具进行计算，最后将结果存入**共享黑板（Blackboard）**。
    *   **故障感知训练**：在构建路由矩阵时，不仅使用成功的轨迹，还保留不成功的轨迹，并赋予一定的权重 $\alpha$。这使得矩阵能够学习到在特定错误发生时，哪种替代路径最有效。
    *   **并行激活与 Pivot 机制**：在推理时，路由器可以根据概率阈值并行启动多个可能的后续智能体，并通过 Pivot 规则选择最优的控制状态。

### 3. 实验设计
*   **数据集/场景**：使用了三个主流的时空推理基准测试：
    1.  **STARK**：包含 26 种任务类型，涵盖空间/时间定位、预测和关系推理。
    2.  **STB24 (STBench)**：包含 11 种任务类型，侧重于城市计算和轨迹分析。
    3.  **STB26 (ST-Bench 2026)**：包含 4 种任务类型，侧重于因果推理和预测。
*   **对比方法**：
    *   **基础模型**：LLM-only (直接回答), CoT (思维链)。
    *   **智能体/提示词基线**：Reflexion (自我反思), ReAct (推理+行动), Tree-of-Thought (思维树), Graph-of-Thought (思维图), 以及标准的 Function-calling。
*   **骨干模型**：测试了 8 个模型，包括 Claude 3.5 Sonnet/Haiku、GPT-OSS-20B、Qwen3-8B、Llama 3.1/3.2 等。

### 4. 资源与算力
*   **算力支持**：论文提到研究得到了 Sharon AI 的支持，使用了 **NVIDIA H100 GPU** 进行实验。
*   **训练/推理细节**：
    *   论文未明确给出具体的训练总时长，但提供了推理时的平均延迟（如 Qwen3-8B 在 STAR 框架下平均延迟为 1.8s/query）。
    *   由于路由矩阵是通过统计轨迹计数生成的，其训练成本远低于模型微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 35 种细分任务类型，测试了超过 7,000 个查询。
*   **消融实验**：
    *   **框架级消融**：分别移除黑板、确定性工具、HEAD 分类器，验证各组件必要性。
    *   **路由器专项消融**：测试了“无状态感知”、“仅成功轨迹训练”、“随机路由”等变体。
*   **充分性与公平性**：实验在 8 个不同规模和家族的 LLM 上进行了验证，并提供了 95% 的 Wilson 置信区间。通过“跨基准迁移实验”验证了路由矩阵的泛化能力，实验设计较为客观、严谨。

### 6. 论文的主要结论与发现
*   **性能提升**：STAR 在所有基准测试中均显著优于基线。在 Qwen3-8B 上，相比 LLM-only，在 STARK 上提升了 17.6%，在 STB24 上提升了 44.4%。
*   **故障恢复的价值**：当执行偏离名义路径时，STAR 的优势最明显。特别是针对 `MISS`（工具不匹配）状态，学习到的恢复路径能使准确率回升至 80% 以上，甚至超过了无故障的基准线（因为纠正了初始的错误分配）。
*   **理论证明**：论文证明了保留不成功轨迹对于扩大路由策略在错误状态上的“支持集”是必不可少的（定理 1）。

### 7. 优点
*   **显式可解释性**：将复杂的智能体协作逻辑转化为可视化的概率矩阵，比黑盒式的 LLM 提示词更易于调试和优化。
*   **鲁棒性强**：通过区分不同类型的失败（FAIL vs BLOCK vs MISS），实现了精细化的恢复控制。
*   **高效性**：支持智能体的并行执行（Scatter-Gather），且路由逻辑本身计算开销极小。
*   **模块化**：通过共享黑板解耦了智能体的执行与控制，方便扩展新的专家工具。

### 8. 不足与局限
*   **依赖任务分类**：系统的第一步依赖于 HEAD 智能体对任务类型的准确分类，如果分类错误，后续路由可能受限。
*   **参数提取瓶颈**：实验显示 `FAIL`（格式错误）状态的恢复率相对较低，这通常是因为 LLM 在参数提取阶段存在根本性错误，路由层难以完全修复。
*   **分类体系限制**：目前的路由矩阵是基于预定义的任务分类学学习的，对于完全未见过的任务类型，其泛化能力仍有待进一步研究。

（完）
