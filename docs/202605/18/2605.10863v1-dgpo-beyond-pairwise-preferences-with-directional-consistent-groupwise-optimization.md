---
title: "DGPO: Beyond Pairwise Preferences with Directional Consistent Groupwise Optimization"
title_zh: DGPO：通过方向一致性组优化超越成对偏好
authors: "Mengyi Deng, Zhiwei Li, Xin Li, Tingyu Zhu, Yulan Yuan, Zhijiang Guo, Wei Wang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10863v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于推理多样性和对齐的组级偏好优化
tldr: 针对大语言模型在偏好优化中难以兼顾方向一致性与推理多样性的问题，本文提出DGPO框架。该方法通过构建包含正向和反向问答的结构化集合，在组级别聚合监督信号，并利用多候选比较显式建模方向感知对齐。DGPO通过边际似然目标区分连贯与不连贯的推理路径，显著提升了模型在多个基准测试中的推理性能和一致性。
selection_source: fresh_fetch
motivation: 现有的成对偏好优化方法在处理复杂推理时，难以有效捕捉方向一致性并维持推理路径的多样性。
method: 提出一种轻量级的组级优化框架，通过整合正反向问答实例并利用边际似然目标进行多候选比较。
result: "实验证明该方法在多个基准测试中均取得显著提升，最高可将模型平均准确率提高3.6%。"
conclusion: DGPO通过引入组级监督和方向感知对齐，成功超越了传统的成对偏好优化，增强了模型的推理鲁棒性。
---

## 摘要
尽管大语言模型（LLMs）取得了显著进展，但目前的偏好优化方法在保持推理多样性的同时，仍难以对齐方向一致性。为了解决这一局限性，我们提出了方向组偏好优化（DGPO），这是一个轻量级框架，它在组级别聚合监督信号，并通过多候选比较显式地建模方向感知对齐。DGPO 将正向和反向问答实例组织成结构化集合，并优化一个基于边际的似然目标，该目标将连贯的推理路径与不一致的备选方案区分开来。这种组级公式比成对目标捕获了更丰富的相对信息，并增强了跨多样化推理路径的一致性。实验结果表明，我们构建的反向数据在五个基准测试中带来了 3.2% 的平均提升，而 DGPO 在多个数据集和模型系列中进一步实现了持续增益，平均准确率提升高达 3.6%。

## Abstract
Although Large Language Models (LLMs) have made remarkable progress, current preference optimization methods still struggle to align directional consistency while preserving reasoning diversity. To address this limitation, we propose Directional-Groupwise Preference Optimization (DGPO), a lightweight framework that aggregates supervision signals at the group level and explicitly models direction-aware alignment through multi-candidate comparisons. DGPO organizes forward and reverse question-answer instances into structured sets and optimizes a margin-based likelihood objective that separates coherent reasoning paths from inconsistent alternatives. This group-wise formulation captures richer relative information than pairwise objectives and reinforces consistency across diverse reasoning pathways. Empirical results show that our constructed reverse data yields a 3.2% average improvement across five benchmarks, while DGPO further delivers consistent gains across multiple datasets and model families, achieving average accuracy improvements of up to 3.6%.

---

## 论文详细总结（自动生成）

这篇论文提出了 **DGPO（Directional-Groupwise Preference Optimization，方向一致性组偏好优化）**，旨在解决大语言模型（LLM）在偏好对齐过程中难以兼顾“方向一致性”与“推理多样性”的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的偏好优化方法（如 DPO）多基于**成对（Pairwise）**比较，且主要关注**正向推理**（从前提推结论）。这导致模型在面对**逆向推理**（从结论推前提）时表现不佳（即“逆转诅咒”），且容易忽略推理路径的多样性，导致对齐信号不够丰富。
*   **研究动机**：人类认知具有双向性。作者认为，通过引入正反向问题的组级（Groupwise）对比，可以增强模型对问题语义的深度理解，并在保持多种正确推理路径的同时，显式地建模推理的方向一致性。

### 2. 方法论：DGPO 框架
DGPO 的核心思想是将监督信号从“一对一”提升到“组对组”，并引入方向感知机制。
*   **数据构造**：
    *   基于 LIMO 数据集的 817 个精选问题，利用 DeepSeek V3 生成对应的**逆向问题**。
    *   利用 Qwen3-32B 为每个正向和逆向问题生成多个（通常为 3 个）推理路径，形成结构化组。
    *   **正向组 ($G^+$)**：当前方向下的正确解；**负向组 ($G^-$)**：反方向问题的解（在当前语境下是不连贯的）。
*   **一致性建模（Consistency Modeling）**：
    *   引入一个轻量级的**投影头（Projection Head）**，预测 Beta 分布的参数 ($\alpha, \beta$)。
    *   该分布用于估计解与问题之间的**方向一致性概率 $d$** 及其**不确定性 $\sigma^2$**。
*   **优化目标**：
    *   **组级得分聚合**：使用 `log-sum-exp` 算子对组内多个候选解的得分进行平滑聚合，这比硬最大值（Hard Max）更能保留多样性。
    *   **对比损失**：优化一个基于边际（Margin-based）的似然目标，拉开正向组与负向组的差距。
    *   **正则化**：引入方向性 KL 散度（约束一致性分布）和方差惩罚（稳定训练）。

### 3. 实验设计
*   **数据集与基准（Benchmarks）**：
    *   **领域内**：OpenAI Math 500。
    *   **近领域**：AIME-25（竞赛数学）。
    *   **跨领域/泛化**：GPQA（研究生水平科学问答）、Gaokao MathQA（高考数学）、LMGH（硬几何问题）。
*   **对比方法**：
    *   **基准线**：SFT（仅正向、仅逆向、混合数据）。
    *   **偏好优化变体**：Vanilla DPO、$\beta$-DPO、$\gamma$-DPO、SimPO。
*   **模型系列**：主要在 Qwen3-1.7B 系列（Base、SFT、Official RL 版）上进行验证。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A6000 GPU (48GB)**。
*   **训练细节**：
    *   SFT 阶段：3 个 Epoch，学习率 $1 \times 10^{-5}$，最大序列长度 11,000 序列。
    *   DGPO 阶段：2 个 Epoch，学习率 $1 \times 10^{-6}$ 至 $3 \times 10^{-6}$，最大序列长度 1,000 序列。
    *   **时长**：根据数据集大小，训练耗时在 1 到 4 小时之间。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从数据质量评估、方法对比、消融实验到缩放效应（Scaling Effects）分析的完整闭环。
*   **充分性与公平性**：
    *   针对不同能力的基座模型（Base vs. RL-aligned）均做了测试。
    *   **鲁棒性验证**：每个 DGPO 实验重复运行 3 次并报告均值和标准差，确保结果不是随机扰动。
    *   **消融实验**：分别验证了“不确定性正则化”和“一致性投影头”的必要性。
    *   实验设计客观，对比了当前主流的多种 DPO 变体。

### 6. 主要结论与发现
*   **DGPO 的优越性**：在多个基准测试中，DGPO 相比 SFT 和传统 DPO 实现了 **1% - 3.6%** 的平均准确率提升。
*   **逆向数据的价值**：仅通过逆向数据进行 SFT 就能带来约 3.2% 的提升，证明了双向推理训练的有效性。
*   **混合数据的冲突**：简单地将正反向数据混合进行 SFT 反而可能导致性能下降（干扰），而 DGPO 的组级对比能有效解决这种冲突。
*   **缩放效应**：增加逆向问题的组数在一定范围内能提升性能，但过多的多样性可能导致低参数量模型出现表征干扰，收益递减。

### 7. 优点与亮点
*   **轻量且高效**：在极小规模的数据集（817 个核心问题）上通过结构化构造实现了显著的对齐提升。
*   **建模深度**：不同于简单的成对比较，DGPO 引入了 Beta 分布来显式处理推理过程中的**不确定性**，使对齐信号更稳健。
*   **双向对齐**：通过正反向问题的“组间对抗”，迫使模型理解逻辑的因果倒置，从根本上缓解了“逆转诅咒”。

### 8. 不足与局限
*   **数据构造依赖**：逆向问题的生成依赖于强大的老师模型（如 DeepSeek V3），且并非所有数学问题都具备完美的逻辑可逆性，可能存在伪逆向问题。
*   **模型规模限制**：实验主要集中在 1.7B 参数的模型上，虽然证明了在小模型上的有效性，但在超大规模模型（如 70B+）上的表现和计算开销尚待验证。
*   **序列长度限制**：DGPO 训练时将序列长度限制在 1,000 字符，这可能限制了其在处理极长链条推理任务时的表现。

（完）
