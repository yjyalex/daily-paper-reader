---
title: "Pause and Reflect: Conformal Aggregation for Chain-of-Thought Reasoning"
title_zh: 停顿与反思：思维链推理中的符合聚合
authors: "Yu Gu, Zijun Yu, Vahid Partovi Nia, Masoud Asgharian"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14098v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 思维链推理路径的聚合
tldr: "针对思维链（CoT）推理中聚合不确定性导致的错误代价高昂问题，本文提出一种符合预测聚合方法。该方法通过加权分数聚合替代多数投票，并利用符合风险控制校准弃权规则，为“置信错误率”提供有限样本保证。该方法无需重训练，在多个基准测试中显著提升了选择性准确率，如在GSM8K上仅弃权5%即可达到90.1%的准确率。"
selection_source: fresh_fetch
motivation: 解决思维链推理中自一致性聚合产生的不确定性，特别是减少高置信度的错误答案。
method: 采用加权分数聚合推理路径，并结合符合风险控制建立可控的弃权机制。
result: 在四个基准测试和多个模型上，该方法成功将置信错误率控制在预设目标内，显著优于多数投票基准。
conclusion: 该推理时方法在不改变模型参数的情况下，通过校准弃权有效提升了复杂推理任务的可靠性。
---

## 摘要
结合自一致性（self-consistency）的思维链（CoT）推理通过聚合多个采样的推理路径来提升性能。在这种设定下，正确性不再取决于单一的推理轨迹，而是取决于对候选路径池的聚合规则，这使得聚合不确定性成为了核心挑战。在“自信的错误答案”比“弃权”代价高得多的场景中，这一问题尤为关键。我们为 CoT 推理引入了一种符合程序（conformal procedure），直接应对聚合不确定性。我们的方法用推理路径上的加权得分聚合取代了多数投票，并利用符合风险控制（conformal risk control）校准弃权规则。该方法为置信错误率（confident-error rate，即系统给出回答但结果错误的概率）提供了有限样本保证。我们进一步确定了得分可分性（score separability）是弃权能够证明提升选择性准确率的关键条件，并推导出了仅凭校准数据即可预测准确率增益的闭式表达式。该方法完全在推理阶段进行，无需重新训练。在四个基准测试、四个开源模型和三类得分指标上，实际的置信错误率与预设目标保持一致（在校准划分和测试集变异性的误差范围内）。相比于多数投票基准下 82% 的准确率，我们的方法在 GSM8K 上通过对不到 5% 的问题弃权，实现了 90.1% 的选择性准确率。

## Abstract
Chain-of-thought (CoT) reasoning with self-consistency improves performance by aggregating multiple sampled reasoning paths. In this setting, correctness is no longer tied to a single reasoning trace but to the aggregation rule over a pool of candidate paths, making aggregation uncertainty the central challenge. This issue is critical where confidently incorrect answers are far more costly than abstentions. We introduce a conformal procedure for CoT reasoning that directly addresses aggregation uncertainty. Our approach replaces majority voting with weighted score aggregation over reasoning paths and calibrates an abstention rule using conformal risk control. This approach leads to finite-sample guarantees on the confident-error rate--the probability that the system answers and is wrong. We further identify score separability as the key condition under which abstention provably improves selective accuracy, and derive closed-form expressions that predict accuracy gains from calibration data alone. The method is fully inference-time, and requires no retraining. Across four benchmarks, four open-source models, and three score classes, realized confident-error rates are consistent with the prescribed targets up to calibration-split and test-set variability. Our method achieves $90.1\%$ selective accuracy on GSM8K by abstaining on less than $5\%$ of problems, compared with $82\%$ accuracy under majority-voting baseline.

---

## 论文详细总结（自动生成）

### 论文总结：Pause and Reflect: Conformal Aggregation for Chain-of-Thought Reasoning

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的推理任务中，**思维链（CoT）结合自一致性（Self-Consistency）**已成为提升性能的标准做法。其核心逻辑是采样多个推理路径并取多数投票。然而，这种聚合方式存在一个关键风险：**聚合不确定性**。即多数投票选出的答案可能是“自信的错误”，这在医疗、法律或金融等错误代价极高的场景中是不可接受的。
论文的研究动机在于：如何量化聚合过程中的不确定性，并建立一种可靠的机制，让模型在不确定时选择“弃权”（Abstain），从而保证系统给出的回答具有可控的错误率。

#### 2. 论文提出的方法论
论文提出了一种名为 **“符合聚合”（Conformal Aggregation）** 的框架，其核心思想是将符合预测（Conformal Prediction）引入 CoT 路径的聚合过程：
*   **加权得分聚合（Weighted Aggregation）：** 放弃简单的多数投票，为每条推理路径分配质量得分 $q$（如奖励模型评分、LLM 评判或困惑度），通过权重函数 $w(q) = \exp(q)$ 计算加权票数。
*   **置信度度量（Confidence Measure）：** 定义胜出答案的加权票数占总票数的比例为置信度 $\nu$。
*   **符合风险控制（CRC）：** 利用一小部分标注好的校准数据（Calibration Data），通过符合风险控制算法寻找一个最优阈值 $\hat{\lambda}$。
*   **弃权规则：** 在推理时，如果置信度 $\nu > \hat{\lambda}$，则输出答案；否则系统选择弃权。
*   **理论支撑：** 引入“得分可分性”（Score Separability）概念，证明了只有当得分能区分正确和错误路径时，弃权才能提升选择性准确率，并给出了准确率增益的闭式预测公式。

#### 3. 实验设计
*   **数据集：** 涵盖数学推理（GSM8K, MATH, MATH-Hard）和多跳问答（HotpotQA）。
*   **模型：** 使用了四个开源模型：DeepSeek-R1-Distill-Qwen-1.5B、Qwen3-1.7B、Gemma-4-E4B、Qwen3-8B。
*   **Benchmark 与对比方法：**
    *   **多数投票（Majority Voting, MV）：** 标准的自一致性聚合。
    *   **Best-of-m：** 从 $m$ 个路径中选得分最高的一个。
    *   **得分家族：** 对比了 Reward（奖励模型）、Judge（LLM 评判）、Perplexity（困惑度）和 SC（自一致性相似度）四类评分指标。
*   **设置：** 每个问题采样 $m=16$ 条路径，校准集大小 $n_{cal}=200$。

#### 4. 资源与算力
*   **硬件：** 实验在单张 **NVIDIA A100 (80GB)** GPU 上运行。
*   **耗时：** 路径生成是主要耗时项（如 MATH-Hard 约 122 分钟），而评分过程相对较快（奖励模型评分通常在 1 小时内完成）。
*   **训练：** 该方法是**完全推理时（Inference-time）**的，不需要对模型进行任何重新训练或微调。

#### 5. 实验数量与充分性
论文进行了 10 组不同的“模型-数据集”配置实验，并针对每组配置进行了 20 次随机校准/测试划分以保证统计显著性。此外，还进行了丰富的消融实验，包括：
*   采样路径数量 $m$ 的影响（4 到 20 条）。
*   校准集大小 $n_{cal}$ 的影响（50 到 500 个）。
*   不同权重函数（线性、指数等）的对比。
*   不同评分模型（3B vs 8B 奖励模型）的性能对比。
实验设计覆盖了不同难度、不同领域和不同规模的模型，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **错误率可控：** 该方法能严格将“置信错误率”控制在用户预设的水平 $\alpha$ 之下。
*   **准确率显著提升：** 在 GSM8K 上，通过弃权不到 5% 的难题，准确率从 82% 提升至 90.1%。
*   **得分质量是关键：** 奖励模型（Reward）和 LLM 评判（Judge）在区分正确/错误路径方面远优于困惑度或简单的自一致性得分。
*   **预测准确：** 论文推导的闭式公式能仅凭校准数据就极高精度地预测测试集上的准确率增益。

#### 7. 优点
*   **非侵入性：** 无需修改模型参数，适用于任何能生成多条路径的预训练模型。
*   **理论严谨：** 提供了有限样本下的数学保证，而非启发式规则。
*   **实用性强：** 为开发者提供了一个可调的“旋钮”（$\alpha$），在可靠性与覆盖率（Yield）之间进行权衡。
*   **诊断工具：** 提出的“得分可分性”可以作为评估奖励模型好坏的有效指标。

#### 8. 不足与局限
*   **边际保证：** 符合预测提供的是平均错误率保证，而非针对单个特定输入的绝对保证。
*   **分布一致性要求：** 依赖于校准数据与测试数据同分布（Exchangeability）的假设，若发生严重的分布偏移，保证可能失效。
*   **任务限制：** 目前仅验证了具有明确答案（可提取、闭式）的任务，对于开放式生成任务（如创意写作）的适用性尚待研究。
*   **计算开销：** 虽然评分快，但采样多条 CoT 路径本身会增加推理成本。

（完）
