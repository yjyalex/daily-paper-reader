---
title: "DARE: Difficulty-Adaptive Reinforcement Learning with Co-Evolved Difficulty Estimation"
title_zh: DARE：具有协同演化难度估计的难度自适应强化学习
authors: "Yang Zhou, Can Jin, Zihan Dong, Zhepeng Wang, Yanting Yang, Shiyu Zhao, Lei Li, Runxue Bao, Yaochen Xie, Dimitris N. Metaxas"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09188v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过难度自适应数据选择的强化学习提升大语言模型推理能力
tldr: 针对大语言模型强化学习中样本效率低和推理成本高的问题，本文提出DARE框架。该框架通过自归一化重要性采样实现难度估计与策略的协同演化，并利用对称Beta分布保持样本多样性。DARE针对不同难度任务应用差异化训练策略和自适应计算分配，在提升模型推理准确性的同时，显著降低了简单任务的响应长度，实现了训练效率、最终性能和推理效率的全面提升。
selection_source: fresh_fetch
motivation: 现有的难度感知强化学习方法存在难度估计随策略漂移失效、性能提升有限以及无法优化推理效率等局限性。
method: 提出协同演化难度估计的DARE框架，采用自归一化重要性采样、对称Beta分布采样及针对不同难度的自适应计算分配策略。
result: 实验表明DARE在多个领域和模型上均优于现有方法，不仅提高了训练效率，还使模型在简单任务上更简洁、在困难任务上更准确。
conclusion: DARE通过动态难度感知和差异化训练，成功解决了强化学习中的样本效率与推理成本平衡问题。
---

## 摘要
强化学习提升了大语言模型的推理能力，但由于许多采样（rollouts）提供的学习信号较弱，其成本依然高昂且样本效率低下。感知难度的训练数据选择方法试图通过优先处理中等难度的提示词来解决这一问题，但我们的分析揭示了三个局限性：难度估计在策略漂移下变得不准确，仅靠数据选择带来的最终性能提升有限，且推理效率基本保持不变。这些发现表明，高效且有效的强化学习不仅需要按难度过滤：策略应当学会在解决难题的同时，对简单任务生成简洁的回复。为此，我们提出了 Dare，这是一个统一的框架，它通过自归一化重要性采样使难度估计与策略协同演化，通过对称 Beta 采样分布保持多样化的难度覆盖，并结合自适应计算分配对不同难度层级应用定制化的训练策略。在多个模型和领域进行的广泛实验表明，Dare 在训练效率、最终效果和推理效率方面始终优于现有方法，在简单任务上生成更简洁的回复，同时提高了难题的正确率。代码可在 https://github.com/EtaYang10th/DARE 获取。

## Abstract
Reinforcement learning improves the reasoning ability of large language models but remains costly and sample-inefficient, as many rollouts provide weak learning signals. Difficulty-aware data selection methods attempt to address this by prioritizing moderately difficult prompts, yet our analysis reveals three limitations: difficulty estimates become inaccurate under policy drift, data selection alone yields limited final-performance gains, and inference efficiency remains largely unchanged. These findings suggest that efficient and effective RL requires more than filtering by difficulty: the policy should learn to solve hard tasks while producing concise responses for easy ones. To this end, we propose **Dare**, a unified framework that co-evolves difficulty estimation with the policy via self-normalized importance sampling, maintains diverse difficulty coverage through a symmetric Beta sampling distribution, and applies tailored training strategies across difficulty tiers with adaptive compute allocation. Extensive experiments across multiple models and domains demonstrate that **Dare** consistently outperforms existing methods in training efficiency, final effectiveness, and inference efficiency, producing more concise responses on easy tasks while improving correctness on hard ones. Code is available at https://github.com/EtaYang10th/DARE.

---

## 论文详细总结（自动生成）

以下是对论文《DARE: Difficulty-Adaptive Reinforcement Learning with Co-Evolved Difficulty Estimation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
强化学习（RL）虽能显著提升大语言模型（LLM）的推理能力，但面临**成本高昂**和**样本效率低下**的挑战。现有的“难度感知”数据选择方法（如优先训练中等难度的题目）存在三大局限：
*   **策略漂移导致的估计失效**：随着模型能力的提升，原本“中等难度”的题目可能变得简单，静态或缓慢更新的难度估计器会变得不准确。
*   **性能增益有限**：单纯的数据过滤往往只能加速收敛，对最终模型能达到的最高准确率提升有限。
*   **推理效率无改善**：模型在简单任务上依然会生成冗长的思维链（CoT），浪费推理算力。

**DARE** 旨在通过一个统一框架，实现难度估计与策略的同步演化，并根据难度动态分配训练和推理资源。

### 2. 方法论：核心思想与技术细节
DARE 包含三个核心组件：
*   **协同演化难度估计（Co-Evolved Difficulty Estimation）**：
    *   使用 **FIFO 重放缓冲区**存储历史采样（rollouts）。
    *   引入**自归一化重要性采样（SNIS）**：通过计算当前策略与生成该采样时的行为策略之间的概率比，对旧样本进行加权，从而在不重新采样的情况下准确估计当前策略下的题目难度，解决了策略漂移问题。
*   **难度引导的动态数据选择**：
    *   采用**对称 Beta 分布**进行采样。相比于硬阈值过滤，该分布在优先选择中等难度（学习信号最强）样本的同时，保留了少量简单和困难样本，防止模型遗忘简单技能并保持对难题的探索。
*   **难度自适应策略优化**：将任务分为三层并应用不同策略：
    *   **简单任务**：减少采样次数，缩短长度限制，并引入**长度惩罚**，鼓励模型生成简洁的正确答案。
    *   **中等任务**：采用标准 GRPO 更新。
    *   **困难任务**：增加采样次数，提供**提示词增强（Hints）**，并引入**长度奖励**，鼓励模型进行更深度的思考和探索。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：MATH-500, GSM8K, AIME-AMC, Minerva, OlympiadBench。
    *   **代码生成**：HumanEval, MBPP, LiveCodeBench。
*   **模型规模**：Qwen2.5-Math (1.5B, 7B), SmolLM3-3B, Qwen2.5-Coder-1.5B。
*   **对比方法（Baselines）**：
    *   标准 GRPO（随机选择）。
    *   基于嵌入的预测（DOTS）、基于熵的估计（EDCO）、基于贝叶斯后验的估计（MoPPS）、LLM-Judge 等现有难度感知方法。

### 4. 资源与算力
*   论文在正文中详细记录了不同方法的 **GPU 显存占用**（约 25-50GB 范围）和 **CPU 内存占用**（见表 6）。
*   **训练时长**：通过实验曲线展示了 DARE 在达到相同准确率时所需的墙钟时间（Wall-clock time）显著低于基线方法（例如在某些任务上实现数倍的加速）。
*   **具体硬件**：文中未明确提及具体的 GPU 型号（如 A100 或 H100）及具体卡数，但提供了详细的超参数设置（如 Batch Size 512, Max Length 3072 等）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 的多个模型，跨越数学和代码两个主要推理领域。
*   **消融实验**：非常充分。分别验证了 SNIS 估计器、动态数据选择、简单任务惩罚、困难任务奖励等每个组件的贡献。
*   **客观性**：通过 MAE（平均绝对误差）和校准曲线量化了难度估计的准确性，并对比了不同方法在相同计算预算下的表现，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **估计更准**：DARE 的 SNIS 估计器在策略漂移下的误差显著低于所有现有方法，接近理论上限（Oracle）。
*   **训练更快、更强**：在相同训练步数下，DARE 达到的准确率更高；在达到相同准确率时，DARE 的训练时间更短。
*   **推理更高效**：DARE 训练的模型在保持高准确率的同时，显著降低了简单任务的平均输出 Token 数（推理成本降低），实现了“难题深思，易题简答”。

### 7. 优点
*   **闭环演化**：成功解决了 RL 训练中“难度是动态变化的”这一核心痛点。
*   **多维优化**：不仅关注准确率，还兼顾了训练效率和推理效率，具有很强的工程实用价值。
*   **泛化性强**：在数学和代码两个领域均表现出色，证明了框架的通用性。

### 8. 不足与局限
*   **超参数敏感性**：方法引入了多个阈值（如 $d_{easy}, d_{hard}$）和奖励系数（$\lambda_{easy}, \lambda_{hard}$），虽然论文给出了推荐值，但在新领域可能需要重新调优。
*   **依赖可验证奖励**：与大多数推理 RL 一样，该方法目前主要适用于有明确正确答案（Verifiable Reward）的任务（如数学、代码），在开放式写作或主观对话任务中的适用性尚未验证。
*   **冷启动依赖**：对于全新的提示词，仍需依赖初始的嵌入相似度预测进行冷启动。

（完）
