---
title: "Decouple before Integration: Test-time Synthesis of SFT and RLVR Task Vectors"
title_zh: 先解耦后集成：SFT 与 RLVR 任务向量的测试时合成
authors: "Chaohao Yuan, Chenghao Xiao, Yu Rong, Hong Cheng, Long-Kai Huang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00610v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: RLVR 在大模型后训练中增强了推理深度
tldr: 针对大语言模型后训练中 SFT（知识广度）与 RLVR（推理深度）难以融合的问题，本文提出 DoTS 框架。通过分析发现两者存在量级差异和梯度冲突，DoTS 采用“先解耦再合成”策略，在推理阶段利用任务向量算术融合独立训练的 SFT 和 RLVR 模型。该方法结合了稀疏化处理与贝叶斯优化，以极低成本实现了超越 SOTA 的性能。
selection_source: fresh_fetch
motivation: SFT 和 RLVR 在知识与推理上具有互补性，但直接整合会面临灾难性遗忘和严重的梯度冲突。
method: 提出 DoTS 框架，通过任务向量算术在推理时合成独立训练的 SFT 和 RLVR 检查点，并利用稀疏化和贝叶斯优化寻找最优组合系数。
result: "在多个数学推理基准测试中，DoTS 以仅 3% 的计算成本达到或超过了基于训练的集成方法，并展现出极强的泛化能力。"
conclusion: 任务向量合成是一种高效且低成本的 LLM 能力集成方案，能有效解决不同训练范式间的冲突问题。
---

## 摘要
SFT 和 RLVR 代表了大语言模型（LLM）后训练的两种基本且不同的范式，各自在不同维度上表现出色。SFT 扩展了知识广度，而 RLVR 增强了推理深度。然而，整合这些互补的优势仍然是一个巨大的挑战。顺序训练可能导致灾难性遗忘，而联合优化通常会遭受严重的梯度冲突。我们从任务向量的角度分析了 SFT 和 RLVR，并揭示了导致这些失败的三个结构性属性：30 倍的量级差异、45% 的符号干扰以及异构的模块级更新分布。这些发现表明 SFT 和 RLVR 难以直接整合，但也暗示这两种范式修改了模型中部分互补的组件。受这些观察结果的启发，我们提出了解耦测试时合成（Decoupled Test-time Synthesis, DoTS），这是一个事后框架，允许独立训练 SFT 和 RLVR 检查点，并仅在推理阶段通过任务向量算术合成它们的能力，而无需更新模型参数。为了减少干扰，DoTS 应用了带有范数保持重缩放的选择性稀疏化。然后，它在一小组未标记的查询上使用贝叶斯优化，在一致性和困惑度的帕累托前沿上搜索组合系数。实验表明，DoTS 在多个数学推理基准测试中达到或超过了基于训练的 SFT-RLVR 集成方法的性能，而计算成本仅为约 3%。当应用于更强大的后训练检查点时，DoTS 超越了现有最先进（SOTA）模型，并能在无需重新微调的情况下泛化到域外基准测试。代码可在 https://github.com/chaohaoyuan/DoTS 获取。

## Abstract
SFT and RLVR represent two fundamental yet distinct paradigms for LLM post-training, each excelling in distinct dimensions. SFT expands knowledge breadth while RLVR enhances reasoning depth. Yet integrating these complementary strengths remains a formidable challenge. Sequential training can cause catastrophic forgetting, and joint optimization often suffers from severe gradient conflicts. We analyze SFT and RLVR through the lens of task vectors and reveal three structural properties behind these failures: a 30* magnitude disparity, 45* sign interference, and heterogeneous module-wise update distributions. These findings show SFT and RLVR are difficult to integrate directly, but they also suggest that the two paradigms modify partly complementary components of the model. Motivated by these observations, we propose Decoupled Test-time Synthesis (DoTS), a post-hoc framework allows SFT and RLVR checkpoints to be trained independently and synthesizes their capabilities only at inference time via task vector arithmetic, without updating model parameters. To reduce interference, DOTS applies selective sparsification with norm-preserving rescaling. It then uses Bayesian optimization on a small set of unlabeled queries to search for combination coefficients on the Pareto frontier of consistency and perplexity. Empirically, \ours matches or exceeds the performance of training-based SFT--RLVR integration methods across multiple mathematical reasoning benchmarks, incurring only $\sim$3\% of the computational cost. When applied to stronger post-trained checkpoints, DOTS surpasses SOTA models and generalizes to out-of-domain benchmarks without re-tuning. Code is available at https://github.com/chaohaoyuan/DoTS.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）后训练中两种核心范式——监督微调（SFT）与带验证奖励的强化学习（RLVR）的集成难题，提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：SFT 擅长扩展知识广度，而 RLVR 擅长增强推理深度。然而，直接整合两者面临巨大挑战：**顺序训练**会导致灾难性遗忘（RL 覆盖 SFT 知识），而**联合优化**则会产生严重的梯度冲突。
*   **研究发现**：作者通过“任务向量”（Task Vectors）视角分析发现，SFT 和 RLVR 在参数空间存在三大结构性不兼容：
    1.  **量级差异巨大**：SFT 任务向量的 L2 范数约为 RLVR 的 30 倍。
    2.  **符号干扰严重**：约 45% 的参数更新方向相反。
    3.  **模块分布异构**：SFT 更新集中在 LayerNorm，而 RLVR 分布在 Attention 和 LM Head。
*   **整体含义**：既然训练时难以调和，不如“先解耦再集成”，在推理阶段通过任务向量算术进行无损合成。

### 2. 方法论：DoTS 框架
DoTS（Decoupled Test-time Synthesis）包含四个关键步骤：
1.  **选择性稀疏化（Selective Sparsification）**：仅保留每个任务向量中绝对值最大的前 $k\%$（默认 30%）参数，将低幅值的噪声和干扰项置零。
2.  **范数保持重缩放（Norm-preserving Rescaling）**：对稀疏化后的向量进行缩放，使其 L2 范数恢复到原始水平，确保信号强度不丢失。
3.  **难度感知数据选择**：从无标签查询池中筛选出难度适中的样本（基于 SFT 和 RLVR 模型输出的一致性得分），构建一个仅包含 64 个样本的微型适配集。
4.  **贝叶斯系数优化**：在适配集上，利用贝叶斯优化（TPE 算法）搜索两个标量系数 $\lambda_{SFT}$ 和 $\lambda_{RLVR}$。目标是在保持低困惑度（Perplexity）的同时，最大化模型输出的一致性。

### 3. 实验设计
*   **模型底座**：Qwen2.5-Math (7B/1.5B), LLaMA3.1-8B。
*   **数学推理基准**：AIME 2024/2025, AMC, MATH500, Minerva, OlympiadBench。
*   **域外（OOD）泛化基准**：ARC-C, GPQA, MMLU-Pro。
*   **对比方法**：
    *   基础模型及指令微调版。
    *   训练集成法：SFT+RL（顺序）、RL w/ SFT Loss（联合）、LUFFY、ReLIFT、ExGRPO。
    *   模型合并法：TIES-Merging、DARE。

### 4. 资源与算力
*   **DoTS 成本**：极低。仅需约 **20 GPU 小时**（用于 100 次贝叶斯优化试验，每次试验仅涉及 64 个样本的推理）。
*   **对比成本**：训练集成方法（如 LUFFY）通常需要 **600+ GPU 小时**，而顺序训练（SFT+RL）可能超过 1000 小时。
*   **硬件参考**：文中提到 RLVR 训练使用了 8 张 A800-80GB GPU，但 DoTS 本身仅需单卡即可在短时间内完成系数搜索。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 8B 不同规模的底座，并在 9 个不同的基准测试集上验证。
*   **消融实验**：详细测试了数据选择策略（随机 vs. 难度分层）、稀疏化比例、固定系数 vs. 搜索系数的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了性能，还验证了系数在不同任务间的**可迁移性**（数学任务搜到的系数直接用于 QA 任务），证明了该方法捕捉的是能力融合而非过拟合。

### 6. 主要结论与发现
*   **性能卓越**：DoTS 在数学推理任务上达到或超过了最先进的训练集成方法（如 LUFFY），平均分提升显著。
*   **成本极低**：仅需训练法约 **3%** 的计算资源和极少量的无标签数据。
*   **泛化性强**：在数学领域搜索到的系数可以直接应用于科学问答（GPQA 等），无需重新调整。
*   **互补性验证**：证实了 SFT 和 RLVR 修改的是模型不同的功能组件，通过合理的冲突控制可以实现“1+1>2”。

### 7. 优点
*   **高效性**：无需重新训练模型，极大地降低了后训练的门槛。
*   **冲突控制**：通过稀疏化有效解决了 SFT 和 RLVR 之间严重的符号冲突问题。
*   **即插即用**：可以灵活组合任何现有的 SFT 和 RLVR 检查点，具有极强的工程实用价值。

### 8. 不足与局限
*   **依赖源模型质量**：如果 SFT 或 RLVR 检查点本身质量极差，DoTS 的合成效果会受限（如在 LLaMA3.1-8B 上的初步实验所示）。
*   **标量限制**：目前仅搜索两个全局标量系数，未来若能实现层级（Layer-wise）或模块级（Module-wise）的细粒度系数搜索，性能可能进一步提升。
*   **任务范围**：主要集中在数学和逻辑推理，对于开放式生成或多模态任务的适用性尚待验证。

（完）
