---
title: "ResRL: Boosting LLM Reasoning via Negative Sample Projection Residual Reinforcement Learning"
title_zh: ResRL：通过负样本投影残差强化学习提升大语言模型推理能力
authors: "Zihan Lin, Xiaohan Wang, Jie Cao, Jiajun Chai, Li Wang, Xiaodong Lu, Wei Lin, Ran He, Guojun Yin"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00380v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 提出了残差强化学习，通过可验证奖励提升大语言模型推理能力
tldr: 针对大语言模型在强化学习推理任务中因过度激励正样本而导致的生成多样性不足及负样本抑制共享语义的问题，本文提出ResRL。该方法通过SVD低秩投影将负样本表示投影到正样本子空间，利用投影残差调节负梯度，从而解耦正负样本间的相似语义分布。实验证明ResRL在数学、代码等12项基准测试中显著提升了推理能力并保持了生成多样性。
selection_source: fresh_fetch
motivation: 现有的负样本强化学习方法在惩罚错误答案时，容易误伤正负样本间共享的语义分布，导致模型生成多样性受损。
method: 提出基于SVD低秩正向子空间的负样本投影残差强化学习，通过投影残差调制负梯度，实现正负样本语义的有效解耦。
result: "在数学、代码、智能体等12个基准测试上均优于强基线，其中数学推理能力的Avg@16和Pass@128分别提升了9.4%和7.0%。"
conclusion: ResRL通过精细化的梯度调节机制，在增强大模型推理性能的同时，有效解决了生成多样性与负样本惩罚之间的权衡问题。
---

## 摘要
具有可验证奖励的强化学习（RLVR）增强了大语言模型（LLMs）的推理能力，但由于对正奖励的过度激励，通常表现出有限的生成多样性。尽管负样本强化（NSR）等方法通过加大负样本的惩罚权重来缓解这一问题，但它们可能会抑制正负响应之间共享的语义分布。为了在不损失多样性的情况下提升推理能力，本文提出了负样本投影残差强化学习（ResRL），旨在解耦正负响应之间相似的语义分布。我们在理论上将延迟似然位移（LLD）与正负头梯度干扰联系起来，并推导出一个单次前向代理，该代理为表示对齐设定了上界，用以指导保守的优势重加权。随后，ResRL 将负标记（negative-token）的隐藏表示投影到基于 SVD 的低秩正向子空间上，并利用投影残差来调节负梯度，在保持多样性的同时提高了推理能力，并在涵盖数学、代码、智能体任务和函数调用的 12 个基准测试中平均优于强基准模型。值得注意的是，ResRL 在数学推理方面的 Avg@16 和 Pass@128 指标上分别超过 NSR 9.4% 和 7.0%。代码可在 https://github.com/1229095296/ResRL.git 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) enhances reasoning of Large Language Models (LLMs) but usually exhibits limited generation diversity due to the over-incentivization of positive rewards. Although methods like Negative Sample Reinforcement (NSR) mitigate this issue by upweighting penalty from negative samples, they may suppress the semantic distributions shared between positive and negative responses. To boost reasoning ability without losing diversity, this paper proposes negative sample projection Residual Reinforcement Learning (ResRL) that decouples similar semantic distributions among positive and negative responses. We theoretically link Lazy Likelihood Displacement (LLD) to negative-positive head-gradient interference and derive a single-forward proxy that upper-bounds representation alignment to guide conservative advantage reweighting. ResRL then projects negative-token hidden representations onto an SVD-based low-rank positive subspace and uses projection residuals to modulate negative gradients, improving reasoning while preserving diversity and outperforming strong baselines on average across twelve benchmarks spanning Mathematics, Code, Agent Tasks, and Function Calling. Notably, ResRL surpasses NSR on mathematical reasoning by 9.4\% in Avg@16 and 7.0\% in Pass@128. Code is available at https://github.com/1229095296/ResRL.git.

---

## 论文详细总结（自动生成）

### ResRL：通过负样本投影残差强化学习提升大语言模型推理能力

#### 1. 论文的核心问题与整体含义
论文关注的核心问题是**强化学习（RLVR）在提升大语言模型（LLM）推理能力时，如何平衡性能提升与生成多样性**。
*   **研究动机**：虽然像 GRPO 这样的强化学习方法能显著增强推理，但往往会导致生成多样性下降（众数崩溃）。现有的“负样本强化（NSR）”虽然通过惩罚错误答案缓解了多样性问题，但存在“梯度干扰”：错误答案中往往包含部分正确的推理步骤或通用的语义结构，无差别地惩罚负样本会误伤这些与正样本共享的有效语义，导致训练不稳定（即延迟似然位移 LLD 现象）。
*   **整体含义**：ResRL 旨在实现正负样本语义的“解耦”，即只惩罚负样本中那些真正导致错误的、独特的语义成分，而保护那些与正样本重合的有效推理路径。

#### 2. 论文提出的方法论
ResRL 的核心思想是利用**几何投影**来识别并保护共享语义。
*   **核心思想**：将负样本的隐藏层表示投影到由正样本构建的语义子空间中。投影后的“残差”（即正交于正样本空间的部分）代表了负样本特有的错误模式。
*   **关键技术细节**：
    1.  **正向子空间构建**：提取同一 Prompt 组内正样本的倒数第二层隐藏状态，通过层归一化（LayerNorm）和中心化处理，利用 **SVD（奇异值分解）** 提取前 $k$ 个主成分，构建一个低秩的正向语义子空间。
    2.  **残差计算**：将负样本的 Token 表示投影到该子空间，计算其**正交补能量（Orthogonal-complement energy）**，即投影残差。残差越大，说明该 Token 越偏离正确路径。
    3.  **动态梯度调制**：利用残差值对负样本的梯度进行重加权。残差大的 Token 赋予更高的惩罚权重，残差小的（与正样本相似的）Token 则降低惩罚，从而避免误伤共享语义。
    4.  **分位数门控**：采用组内相对分位数归一化，确保残差信号在不同 Prompt 之间具有鲁棒性。

#### 3. 实验设计
论文在 **12 个基准测试**上进行了广泛评估，涵盖四大领域：
*   **数学推理**：AIME 2024/2025、AMC 2023、MATH-500、Minerva、OlympiadBench。
*   **代码生成**：LiveCodeBench、CodeForces、HumanEval+。
*   **智能体任务**：ALFWorld、WebShop。
*   **函数调用**：BFCL（Berkeley Function Calling Leaderboard）。
*   **对比方法**：包括 GRPO、NSR（加权 Reinforce）、DAPO、FlowRL、EMPG、ResT 等强基线。
*   **基础模型**：使用了 Qwen3 系列（1.7B、4B、8B、32B）以及 Qwen2.5-7B-Instruct。

#### 4. 资源与算力
论文在附录中披露了详细的训练配置：
*   **硬件**：使用了 **Nvidia A100 GPU** 集群。
*   **配置**：每个节点 8 张 GPU，采用了张量并行（TP=8）。
*   **训练细节**：数学任务最大响应长度为 4096，代码任务为 8192。学习率设为 $1 \times 10^{-6}$，采用 AdamW 优化器。具体的训练时长未明确说明，但提到了模型训练至收敛。

#### 5. 实验数量与充分性
*   **实验规模**：论文不仅在 12 个数据集上进行了主实验，还针对 Qwen3 的四个不同参数规模（1.7B 到 32B）验证了可扩展性。
*   **消融实验**：非常充分。作者研究了 SVD 秩次 $k$ 的选择、隐藏层位置（倒数第二层 vs 最后一层）、分位数阈值、KL 散度惩罚的影响、SVD 采样预算以及 LayerNorm 的必要性。
*   **客观性**：通过 Avg@16（16 次独立生成的平均准确率）和 Pass@128 等指标同时衡量精度和多样性，评价维度全面且客观。

#### 6. 论文的主要结论与发现
*   **性能领先**：ResRL 在所有任务上均优于 GRPO 和 NSR。在数学任务上，相比 NSR，Avg@16 提升了 **9.4%**，Pass@128 提升了 **7.0%**。
*   **多样性保护**：ResRL 在提升 Pass@1 准确率的同时，Pass@k 曲线始终保持在高位，证明其在增强推理能力的同时没有牺牲生成多样性。
*   **理论验证**：实验证实了投影残差能有效缓解 LLD 现象，使训练更加稳定，且无需严苛的 KL 惩罚即可实现良好的探索。

#### 7. 优点
*   **理论支撑深厚**：将 LLD 现象与梯度干扰联系起来，并给出了严谨的数学证明和对齐上界推导。
*   **机制优雅高效**：通过单次前向代理（Single-forward proxy）避免了昂贵的 Token 级全参数梯度计算，SVD 采样机制保证了计算开销可控。
*   **跨领域通用性**：在数学、代码、智能体等多个异构任务上均表现出一致的优越性。

#### 8. 不足与局限
*   **计算开销**：虽然通过采样优化了 SVD，但在训练过程中每组 Prompt 仍需进行 SVD 分解，相比原生 GRPO 增加了一定的计算负担。
*   **依赖正样本**：该方法依赖于同一组采样中必须存在正样本（奖励为 1）。如果某组采样全部失败，ResRL 将退化为普通强化学习模式。
*   **超参数敏感性**：SVD 的秩次 $k$ 和分位数阈值对结果有一定影响，可能需要针对不同模型或任务进行微调。

（完）
