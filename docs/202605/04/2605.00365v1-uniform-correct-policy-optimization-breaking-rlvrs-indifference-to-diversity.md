---
title: "Uniform-Correct Policy Optimization: Breaking RLVR's Indifference to Diversity"
title_zh: 均匀正确策略优化：打破 RLVR 对多样性的漠视
authors: "Anamika Lochab, Bolian Li, Ruqi Zhang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00365v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于推理任务的可验证奖励强化学习 (RLVR) 与多样性崩溃
tldr: "针对强化学习（RLVR）在推理任务中虽能提升单次准确率但会导致多样性坍塌（Pass@K下降）的问题，本文揭示了GRPO等目标函数对正确解分布不敏感的根源。为此提出UCPO算法，通过引入条件均匀性惩罚，促使模型在多个正确解间均匀分配概率。实验证明，UCPO在保持Pass@1的同时，显著提升了数学推理任务的多样性和多样本覆盖率。"
selection_source: fresh_fetch
motivation: 现有的RLVR方法因对正确解的分布缺乏约束，导致模型概率过度集中于少数解，引发多样性坍塌并降低了多样本覆盖率。
method: 提出UCPO算法，在GRPO基础上增加条件均匀性惩罚项，通过重新分配梯度信号来鼓励模型在正确解集内实现均匀分布。
result: "在多个数学基准测试中，UCPO显著提高了Pass@K（如AIME24提升10%）和解的多样性，且保持了极具竞争力的Pass@1表现。"
conclusion: 通过优化正确解集的分布均匀性，可以有效解决RLVR中的多样性坍塌问题，提升大模型在复杂推理任务中的鲁棒性。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 在推理任务的单次尝试准确率 (Pass@1) 上取得了显著提升，但往往面临多样本覆盖率 (Pass@K) 下降的问题，这表明存在多样性崩溃。我们确定了这种退化的结构性原因：常见的 RLVR 目标（如 GRPO）对概率质量在正确解之间的分布方式漠不关心。结合随机训练动力学，这种漠视会导致一种自我强化的崩溃，即概率质量集中在正确输出的一个狭窄子集上，而其他有效的解决方案则被抑制。我们对这种崩溃机制进行了形式化，并进一步刻画了在鲁棒性和熵正则化最优性这两个互补准则下的最优策略结构，这两个准则均确定“均匀正确策略”为唯一最优。受此分析启发，我们提出了均匀正确策略优化 (UCPO)，这是对 GRPO 的一种改进，它在策略对正确解的分布上增加了一个条件均匀性惩罚。该惩罚将梯度信号重新分配给代表性不足的正确响应，鼓励在正确集合内均匀分配概率质量。在三个模型（1.5B-7B 参数）和五个数学推理基准测试中，UCPO 在保持具有竞争力的 Pass@1 的同时，提高了 Pass@K 和多样性，在 AIME24 的 Pass@64 上实现了高达 +10% 的绝对提升，并在正确集合内实现了高达 45% 的等式级多样性提升。代码可在 https://github.com/AnamikaLochab/UCPO 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has achieved substantial gains in single-attempt accuracy (Pass@1) on reasoning tasks, yet often suffers from reduced multi-sample coverage (Pass@K), indicating diversity collapse. We identify a structural cause for this degradation: common RLVR objectives, such as GRPO, are indifferent to how probability mass is distributed among correct solutions. Combined with stochastic training dynamics, this indifference induces a self-reinforcing collapse, in which probability mass concentrates on a narrow subset of correct outputs while alternative valid solutions are suppressed. We formalize this collapse mechanism and further characterize the optimal policy structure under two complementary criteria: robustness and entropy-regularized optimality, which identify the Uniform-Correct Policy as uniquely optimal. Motivated by this analysis, we propose Uniform-Correct Policy Optimization (UCPO), a modification to GRPO that adds a conditional uniformity penalty on the policy's distribution over correct solutions. The penalty redistributes gradient signal toward underrepresented correct responses, encouraging uniform allocation of probability mass within the correct set. Across three models (1.5B-7B parameters) and five mathematical reasoning benchmarks, UCPO improves Pass@K and diversity while maintaining competitive Pass@1, achieving up to +10\% absolute improvement on AIME24 at Pass@64 and up to 45\% higher equation-level diversity within the correct set. The code is available at https://github.com/AnamikaLochab/UCPO.

---

## 论文详细总结（自动生成）

### 论文总结：《Uniform-Correct Policy Optimization: Breaking RLVR's Indifference to Diversity》

#### 1. 核心问题与整体含义（研究动机和背景）
在推理任务（如数学、代码）中，**具有可验证奖励的强化学习（RLVR）**（如 DeepSeek 使用的 GRPO 算法）虽然能显著提升单次尝试准确率（Pass@1），但往往会导致**多样性坍塌（Diversity Collapse）**。表现为多样本覆盖率（Pass@K）在训练过程中不升反降，模型倾向于收敛到极少数特定的正确解路径上，而抑制了其他同样有效的解法。

论文指出，这种现象的根源在于：
*   **目标函数的漠视**：标准 RLVR 目标只关心“是否正确”，而不关心概率在不同正确解之间如何分配。
*   **自我强化的坍塌机制**：随机采样中的微小偏差会被梯度更新放大，导致“强者恒强”，最终概率质量集中在单一模式上。

#### 2. 方法论：UCPO 算法
论文提出了 **均匀正确策略优化（Uniform-Correct Policy Optimization, UCPO）**，旨在引导模型达到“均匀正确策略”（即：错误解概率为零，所有正确解概率相等）。

*   **核心思想**：在 GRPO 的基础上引入一个**条件均匀性惩罚项（Conditional Uniformity Penalty）**。
*   **关键技术细节**：
    *   **目标函数**：在最大化正确率的基础上，增加一个最小化“当前正确解分布”与“均匀分布”之间 KL 散度的项：$KL(u || q_\theta)$。
    *   **梯度重加权**：UCPO 并不改变总的梯度质量，而是重新分配正确解之间的梯度权重。
    *   **算法流程**：
        1.  针对每个 Prompt 采样多个 Rollout。
        2.  识别其中的正确解集合。
        3.  计算每个正确解的条件概率。
        4.  **逆概率加权**：给当前概率较低（被低估）的正确解分配更大的权重，给概率较高（占主导）的正确解分配较小的权重。
        5.  通过这种方式，梯度信号会强制模型在多个正确路径上“齐头并进”。

#### 3. 实验设计
*   **模型**：DeepSeek-R1-Distill-Qwen (1.5B 和 7B)、Qwen2.5-7B。
*   **数据集/Benchmark**：
    *   训练集：DeepScaleR 10K 竞赛数学题库。
    *   测试集：AIME 2024/2025、AMC 2023、MATH 500、OlympiadBench。
*   **对比方法**：
    *   标准 GRPO。
    *   多样性增强基准：KL-Cov（协方差控制）、Ent-Reg（全局熵正则化）、Ent-Adv（熵优势奖金）、Pass@K Training、FGRPO（焦距梯度缩放）。

#### 4. 资源与算力
论文明确说明了硬件配置：
*   **1.5B 模型**：使用 4 × NVIDIA H100 GPU。
*   **7B 模型**：使用 8 × NVIDIA H100 GPU。
*   **训练效率**：UCPO 的训练耗时与标准 GRPO 几乎完全一致（Wall-clock time 曲线重合），因为它仅涉及简单的标量重加权，不需要额外的模型前向传播或参考模型。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：覆盖了不同参数规模（1.5B 到 7B）和不同模型家族（DeepSeek 和 Qwen）。
*   **受控实验**：专门设计了一个“玩具环境”（Toy Environment），在完全可观测的概率分布下验证了坍塌机制的四个阶段。
*   **消融实验**：对超参数 $\tau$（均匀化强度）进行了详细的敏感度分析。
*   **多样性度量**：除了 Pass@K，还引入了“等式级多样性”指标，量化解题步骤的差异。

#### 6. 主要结论与发现
*   **Pass@K 显著提升**：在 AIME24 基准上，Pass@64 相比 GRPO 提升了 10% 的绝对值。
*   **多样性增强**：在正确解集合内，UCPO 生成的公式级多样性比 GRPO 高出 45%。
*   **Pass@1 保持稳定**：在大幅提升多样性的同时，单次准确率（Pass@1）依然保持了极强的竞争力，证明多样性并非以牺牲准确率为代价。
*   **理论验证**：证明了“均匀正确策略”在鲁棒性和熵正则化意义下都是最优的。

#### 7. 优点
*   **理论扎实**：不仅提出了算法，还从数学上形式化了 RLVR 的坍塌机制，并证明了最优策略结构。
*   **实现极其简单**：只需在现有 GRPO 代码中修改几行梯度加权逻辑，无需增加显存或计算开销。
*   **效果精准**：相比于全局熵正则化（容易引入错误解），UCPO 精确作用于正确解的内部分布。

#### 8. 不足与局限
*   **任务适用性**：UCPO 强依赖于“可验证奖励”（Verifiable Rewards），这意味着它主要适用于数学、代码等有客观标准答案的任务，在开放式写作或主观对话任务中难以直接应用。
*   **采样门槛**：如果基础模型（Base Model）在训练初期完全无法采样到某种正确解，UCPO 也无法通过重加权将其“救回”。
*   **超参数微调**：虽然 $\tau$ 有一定的鲁棒区间，但在不同任务或模型上可能仍需少量调试以达到最佳平衡。

（完）
