---
title: Scaling Video Understanding via Compact Latent Multi-Agent Collaboration
title_zh: 通过紧凑潜空间多智能体协作实现规模化视频理解
authors: "Kerui Chen, Jinglu Wang, Jianrong Zhang, Ming Li, Yan Lu, Hehe Fan"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00444v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于视频理解和推理的多智能体协作框架
tldr: 针对多模态大模型在长视频理解中受限于感知上下文预算的问题，本文提出MACF框架。该框架通过多智能体协作，将视频分段处理，并利用紧凑的潜空间通信协议实现全局推理。通过课程学习策略，MACF在保持低计算成本的同时，显著提升了长视频理解的准确性，在多个基准测试中超越了现有SOTA模型。
selection_source: fresh_fetch
motivation: 现有的长视频理解方法受限于感知上下文预算，且基于规则的预处理常导致信息丢失和高昂成本。
method: 提出MACF框架，通过将视频分段交给局部智能体处理，并利用共享嵌入空间中的紧凑潜代币进行跨智能体协作与全局推理。
result: 在多个视频理解基准测试中，MACF在相同预算约束下一致优于现有的SOTA多模态大模型和多智能体系统。
conclusion: 潜空间多智能体协作是一种高效且可扩展的长视频理解方案，能有效平衡感知精度与计算开销。
---

## 摘要
多模态大语言模型 (MLLMs) 推动了视觉语言理解的发展，但由于受限的感知上下文预算，在长视频任务中面临固有局限。现有的智能体方法通过基于规则的预处理来缓解这一问题，但往往面临信息丢失、高成本以及对文本中间体的依赖。我们提出了 MACF，这是一个端到端的多智能体协作框架，它将单个智能体的感知预算与全局视频复杂度解耦，在保持视觉保真度的同时实现了可扩展的视频理解。MACF 将视频划分为若干片段分配给具有局部预算的智能体，并通过一种智能体原生的潜空间通信协议实现整体推理。每个智能体将局部观测编码为共享嵌入空间中紧凑且足以完成任务的 token，从而允许中央协调器进行高效且保信的协作。我们引入了一种课程训练策略，逐步强化语义对齐、证据总结和跨智能体协作。在多种视频理解基准测试上的广泛实验表明，在相同的预算约束下，MACF 始终优于最先进的 MLLM 和多智能体系统，证明了我们的潜空间协作在可扩展视频理解方面的有效性。

## Abstract
Multi-modal large language models (MLLMs) advance vision language understanding but face inherent limitations in long-video tasks due to bounded perception context budgets. Existing agentic methods mitigate this via rule-based preprocessing, yet often suffer from information loss, high cost, and reliance on textual intermediates. We propose MACF, an end-to-end Multi-Agent Collaboration Framework that decouples per-agent perception budgets from global video complexity, enabling scalable video understanding while preserving visual fidelity. MACF partitions videos into segments for locally budgeted agents and enables holistic reasoning via an agent-native latent communication protocol. Each agent encodes partial observations into compact, task-sufficient tokens in a shared embedding space, allowing efficient and information-preserving collaboration by a central coordinator. We introduce a curriculum training strategy that progressively enforces semantic alignment, evidence summarization, and cross-agent coordination. Extensive experiments on diverse video understanding benchmarks show that MACF consistently outperforms state-of-the-art MLLMs and multi-agent systems under identical budget constraints, demonstrating the effectiveness of our latent collaboration for scalable video understanding.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MACF**（Multi-Agent Collaboration Framework）的端到端多智能体协作框架，旨在解决多模态大语言模型（MLLM）在处理长视频时面临的感知上下文预算受限问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：MLLM 在处理长视频或高分辨率视频时，受限于输入上下文的长度（感知预算）。
*   **研究动机**：
    *   **现有方法的缺陷**：传统的“感知采样”会丢失时空细节；“基于检索”的方法依赖文本中间体（如 Caption），成本高且容易丢失视觉保真度。
    *   **瓶颈**：视频信息密度低但冗余度高，如何在有限的计算预算下，既保留关键视觉细节又能实现全局长程推理？
    *   **目标**：将全局视频的复杂度与单个智能体的局部感知预算解耦。

### 2. 方法论
*   **核心思想**：采用“分而治之”的策略，将视频切片分配给多个局部智能体，通过一种**智能体原生的潜空间（Latent）通信协议**进行协作。
*   **关键技术细节**：
    *   **分布式局部感知**：将视频 $V$ 划分为 $M$ 个片段，每个局部智能体 $A_m$ 在其像素预算 $B_{per}$ 内处理一个片段。
    *   **潜空间通信代币（Communication Tokens）**：局部智能体不发送文本，而是将视觉证据压缩为 $K$ 个连续的潜空间向量（Tokens）。这些 Token 携带了查询相关的视觉语义。
    *   **中央协调器（Coordinator）**：接收所有局部智能体的潜空间 Token，在通信预算 $B_{com}$ 内进行全局信息聚合和最终推理。
*   **课程训练策略（Three-stage Curriculum）**：
    1.  **语义对齐（Stage 1）**：利用 Caption 任务让潜空间 Token 在语义上与协调器对齐。
    2.  **证据总结（Stage 2）**：通过图像问答（Image-QA）训练智能体提取与查询相关的关键证据。
    3.  **跨智能体协作（Stage 3）**：在视频问答（Video-QA）数据集上进行端到端训练，学习全局证据聚合。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **Video-MME**：综合性多模态视频评估。
    *   **LongVideoBench**：长上下文交错视频理解。
    *   **LVBench**：极长视频理解基准。
    *   **MLVU-Test**：多任务长视频理解测试。
*   **对比方法**：
    *   **闭源模型**：GPT-4o。
    *   **开源 SOTA**：Qwen2.5-VL (7B/72B), LLaVA-OneVision, InternVL2.5 等。
    *   **多智能体基准**：MapReduce（基于文本通信）、LatentMAS（基于 KV-Cache 共享）。

### 4. 资源与算力
*   **算力设备**：使用了 **4 × NVIDIA A100 (80GB)** GPU。
*   **训练细节**：
    *   局部智能体输入 16 帧，分辨率 224×224。
    *   训练阶段 $M=4$（4个智能体），推理阶段扩展至 $M=6$。
    *   使用了 LoRA 微调技术。
    *   论文未明确给出总训练时长，但提到了训练数据的规模（如 LLaVA-Video-178K 的子集）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流长视频基准上进行了测试。
    *   **消融实验**：验证了三个训练阶段的必要性、通信 Token 数量 $K$ 的影响、局部智能体数量 $M$ 的扩展性。
    *   **模型泛化性**：在 Qwen3、Qwen2.5 和 LLaVA-OV 三种不同的 Backbones 上验证了框架的有效性。
*   **客观性与公平性**：实验在**相同的感知预算（16×224×224 像素）**约束下进行对比，确保了与单模型及其他多智能体系统的公平竞争。

### 6. 主要结论与发现
*   **性能领先**：MACF 在所有基准测试中均显著优于同等预算下的 MLLM。例如在 MLVU-Test 上比基座模型 Qwen3-VL-8B 提升了 **7.7%**。
*   **通信效率**：潜空间通信比文本通信（MapReduce）更保真，比 KV-Cache 共享（LatentMAS）更节省带宽且延迟更低。
*   **扩展性**：随着局部智能体数量 $M$ 的增加，性能持续提升，证明了该框架处理超长视频的潜力。

### 7. 优点
*   **端到端优化**：避免了传统方法中繁琐的预处理和不可导的文本中间层。
*   **信息保真**：潜空间 Token 能够捕捉文本难以描述的细粒度视觉特征（如运动、纹理）。
*   **模型无关性**：框架具有通用性，可以适配不同的 VLM 架构，甚至支持异构智能体协作。
*   **低延迟**：相比于需要自回归生成的文本通信，潜空间 Token 的提取和传输速度极快。

### 8. 不足与局限
*   **训练复杂性**：三阶段课程学习虽然稳定了优化，但增加了训练流程的复杂度和数据准备成本。
*   **静态切片**：目前采用均匀的时间切片，对于节奏变化剧烈的视频，可能不如动态采样有效。
*   **协调器瓶颈**：虽然解耦了感知预算，但协调器的上下文窗口（通信预算）依然是最终的物理上限。

（完）
