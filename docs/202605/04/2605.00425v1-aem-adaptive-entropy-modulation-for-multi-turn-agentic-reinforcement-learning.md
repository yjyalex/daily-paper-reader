---
title: "AEM: Adaptive Entropy Modulation for Multi-Turn Agentic Reinforcement Learning"
title_zh: AEM：面向多轮智能体强化学习的自适应熵调制
authors: "Haotian Zhao, Yuxin Zhang, Songlin Zhou, Stephen S. -T. Yau, Wenyu Zhang, Lun Tian, Tianshu Zhu, Yifeng Huang, Yucheng Zeng, Jingnan Gu, Daxiang Dong, Jianmin Wu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00425v1"
tags: ["query:token"]
score: 9.0
evidence: 使用强化学习对智能体轨迹中的单个步骤进行信用分配
tldr: 针对大语言模型智能体在多轮任务中面临的稀疏奖励和信用分配难题，本文提出了一种无需额外监督的自适应熵调制方法（AEM）。该方法通过将熵分析从标记级别提升至响应级别，利用优势函数与相对响应惊奇度的乘积来动态调整探索与利用的平衡。实验证明，AEM在多个基准测试中显著提升了模型性能，尤其在复杂的软件工程任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的多轮强化学习方法依赖复杂的中间监督信号，且在稀疏奖励环境下难以进行有效的信用分配。
method: 提出AEM方法，通过在响应级别自适应调制熵动力学，并利用推导出的实用代理指标来重塑训练过程中的探索与利用平衡。
result: "在1.5B到32B参数规模的模型上，AEM在多个基准测试中均取得提升，其中在挑战性极高的SWE-bench-Verified上提升了1.4%。"
conclusion: AEM提供了一种高效且无需额外监督的信用分配方案，有效优化了智能体在复杂多轮交互任务中的强化学习训练。
---

## 摘要
强化学习 (RL) 显著提升了大语言模型 (LLM) 智能体与环境交互及解决多轮任务的能力。然而，有效的训练仍然具有挑战性，因为稀疏且仅基于结果的奖励使得难以将信用分配给智能体动作轨迹中的各个步骤。一种常见的补救措施是引入密集的中间监督，例如过程奖励模型或辅助自监督信号，但这增加了监督和调优的复杂性，且通常在不同任务和领域间的泛化能力较差。本文提出了 AEM，一种无需监督的信用分配方法，它在强化学习训练过程中自适应地调制熵动力学，以实现更有效的探索与利用权衡。在理论上，我们将熵分析从 token 级别提升到响应级别，以减少 token 采样方差，并证明了自然梯度下的熵漂移本质上由优势函数与相对响应惊奇度 (surprisal) 的乘积决定。具体而言，我们推导出一个实用的代理指标来重塑训练动力学，从而实现从探索到利用的自然过渡。在各种基准测试和参数范围从 1.5B 到 32B 的模型上进行的广泛实验证明了 AEM 的有效性，包括在极具挑战性的 SWE-bench-Verified 基准测试中，将其集成到最先进的基准模型时获得了显著的 1.4% 提升。

## Abstract
Reinforcement learning (RL) has significantly advanced the ability of large language model (LLM) agents to interact with environments and solve multi-turn tasks. Yet effective training remains challenging, as sparse, outcome-only rewards make it difficult to assign credit to individual steps in an agent's action trajectory. A common remedy is to introduce dense intermediate supervision, such as process reward models or auxiliary self-supervised signals, but this increases supervision and tuning complexity and often generalizes poorly across tasks and domains. This paper presents AEM, a supervision-free credit assignment method that adaptively modulates entropy dynamics during RL training to achieve a more effective exploration-exploitation trade-off. Theoretically, we elevate entropy analysis from the token level to the response level to reduce token sampling variance and show that entropy drift under natural gradients is intrinsically governed by the product of the advantage and the relative response surprisal. Specifically, we derive a practical proxy to reshape training dynamics, enabling a natural transition from exploration to exploitation. Extensive experiments across various benchmarks and models ranging from 1.5B to 32B parameters demonstrate the effectiveness of AEM, including a notable 1.4 percent gain when integrated into a state-of-the-art baseline on the highly challenging SWE-bench-Verified benchmark.

---

## 论文详细总结（自动生成）

以下是对论文《AEM: Adaptive Entropy Modulation for Multi-Turn Agentic Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）智能体执行多轮交互任务时，环境反馈通常是稀疏且仅基于最终结果的（Outcome-only rewards）。这导致难以将信用（Credit）准确分配给轨迹中的中间步骤，使得模型难以区分哪些动作是关键的、哪些是冗余的或错误的。
*   **研究背景**：现有的信用分配方法要么依赖昂贵的外部监督（如过程奖励模型 PRM），要么依赖复杂的自监督信号或高昂的树状搜索计算。本文旨在寻找一种**无需额外监督、计算开销低且具有理论支撑**的信用分配机制。

### 2. 方法论：自适应熵调制（AEM）
*   **核心思想**：利用策略自身的“熵”作为内在信号。熵反映了模型在每个决策点的确定性。AEM 通过在响应级别（Response-level）自适应地调制优势函数（Advantage），来引导策略从早期的“广泛探索”自然过渡到后期的“精准利用”。
*   **关键技术细节**：
    *   **响应级熵分析**：将熵分析从 token 级别提升到响应级别，证明了响应级熵能有效减少采样方差，且全局熵的漂移受“优势函数”与“相对响应惊奇度（Surprisal）”乘积的支配。
    *   **调制系数 $\alpha$**：为每个响应计算一个缩放因子 $\alpha$。对于组内惊奇度较低（更确定）的响应，赋予更大的权重；反之则减小权重。
    *   **自适应机制**：
        *   **训练早期**：负样本较多，AEM 倾向于增加熵，防止过早收敛（防止熵崩溃），促进探索。
        *   **训练后期**：正样本增加，AEM 倾向于降低熵，增强对高质量路径的利用，加速收敛。
    *   **算法流程**：作为插件集成在 GRPO 或 PPO 等算法之上，在计算优势函数后，根据响应的平均 token 熵进行组内归一化，生成调制系数并重塑优势信号。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：文本驱动的具身智能体任务（如清洁、放置物品）。
    *   **WebShop**：真实的在线购物交互任务（搜索、选择商品）。
    *   **SWE-bench-Verified**：极具挑战性的软件工程任务，要求智能体解决真实的 GitHub Issue。
*   **对比基准（Baselines）**：
    *   **闭源模型**：GPT-5.2-Pro, Gemini-3-Pro。
    *   **强化学习方法**：PPO, GRPO, DAPO, GSPO 以及针对软件工程优化的 DeepSWE。
    *   **模型规模**：涵盖 Qwen2.5-1.5B/7B 以及 Qwen3-32B。

### 4. 资源与算力
*   **算力投入**：
    *   **1.5B 模型**：使用 4 × NVIDIA A800 GPU。
    *   **7B 模型**：使用 8 × NVIDIA A800 GPU。
    *   **32B 模型（SWE-bench）**：使用 64 × NVIDIA H200 GPU。
*   **训练时长**：ALFWorld 和 WebShop 训练 150 个 step；SWE-bench 训练 250 个 step。
*   **开销分析**：AEM 引入的额外计算开销极低，仅占总训练时间的 **1.1%**，因为它不需要额外的模型前向传播。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的基准测试上进行了实验，涵盖了从轻量级（1.5B）到中量级（32B）的模型。
*   **充分性**：所有实验均采用 3 个随机种子取平均值，并展示了完整的训练曲线。
*   **公平性**：对比了多种最先进的 RL 变体（如 DAPO, GSPO），证明了 AEM 作为插件的普适性。消融实验验证了熵调制方向与理论推导的一致性。

### 6. 主要结论与发现
*   **性能提升**：AEM 在所有基准测试中均一致提升了基础 RL 算法的性能。在 ALFWorld 上最高提升 **8.8%**，在 WebShop 上提升约 **5%**。
*   **SOTA 突破**：在复杂的 SWE-bench-Verified 任务中，在强大的 DeepSWE 基准上进一步提升了 **1.4%** 的成功率。
*   **训练动力学**：AEM 有效缓解了强化学习中常见的“早期熵崩溃”问题，使模型在后期能收敛到更高质量的策略。

### 7. 优点
*   **无需监督**：不需要外部奖励模型或人工标注，完全依赖策略自身的统计特性。
*   **轻量高效**：计算开销几乎可以忽略不计，易于在大规模分布式训练中部署。
*   **理论完备**：基于信息几何和自然梯度理论推导，而非纯启发式调优。
*   **通用性强**：可作为通用插件适配多种主流的策略梯度 RL 算法。

### 8. 不足与局限
*   **代理指标偏差**：由于在开放式生成中精确计算响应级熵不可行，文中使用了基于长度归一化的 token 熵均值作为代理指标，这在某些极端分布下可能存在启发式偏差。
*   **依赖组内多样性**：AEM 的效果受限于采样组（Group）的多样性，如果采样出的响应高度同质化，调制信号的质量会下降。
*   **超参数敏感性**：虽然文中固定了温度参数 $\lambda=1$，但在不同任务中可能仍存在微调空间。

（完）
