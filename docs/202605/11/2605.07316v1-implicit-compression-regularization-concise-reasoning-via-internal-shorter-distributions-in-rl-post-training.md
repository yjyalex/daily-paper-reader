---
title: "Implicit Compression Regularization: Concise Reasoning via Internal Shorter Distributions in RL Post-Training"
title_zh: 隐式压缩正则化：通过强化学习后训练中的内部更短分布实现简洁推理
authors: "Chen Wang, Hexuan Deng, Yining Zhang, Yuchen Zhang, Jionghao Bai, Zhaochun Li, Ge Lan, Yue Wang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07316v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 利用可验证奖励的强化学习实现简洁推理
tldr: 针对强化学习提升大模型推理能力时导致的“过度思考”问题，本文提出了隐式压缩正则化（ICR）。研究发现过度思考阶段最短正确回答是天然的压缩目标，ICR 利用采样组内最短正确样本构建虚拟分布作为正则化信号，引导模型生成简洁准确的推理轨迹。实验表明，ICR 在保持或提升准确率的同时显著缩短了回复长度，实现了更优的准确率-长度平衡。
selection_source: fresh_fetch
motivation: 现有的长度惩罚或提前退出策略在缓解模型过度思考时，容易导致准确率下降或产生思考不足的问题。
method: 提出隐式压缩正则化（ICR），利用采样组内最短正确回答诱导的虚拟分布作为信号，引导策略向简洁且正确的轨迹演进。
result: 在多个数学和知识密集型基准测试中，ICR 在缩短回复长度的同时维持或提升了准确率，优化了性能与效率的平衡。
conclusion: 通过利用模型内部生成的短且正确的样本进行正则化，可以有效缓解过度思考并实现更高效的推理。
---

## 摘要
具有可验证奖励的强化学习提升了大语言模型（LLM）的推理能力，但往往会导致“过度思考”（overthinking），即模型生成不必要的冗长推理轨迹。现有方法主要依赖长度惩罚或早停（early-exit）策略；然而，前者可能会降低准确率并导致“思考不足”（underthinking），而后者则假设推理轨迹的大部分内容可以被安全截断。为了在没有这些限制的情况下获得压缩信号，我们重新审视了现有压缩方法的训练动态。我们观察到，长度与准确率的相关性最初为负，但在压缩过程中持续增加，这表明较短的回答最初更有可能正确，但随着策略转向思考不足，这种特性逐渐消失。基于这一观察，我们将过度思考形式化：负相关表示处于过度思考状态，而正相关则表示思考不足。在过度思考时，最短的正确回答在期望上短于组平均回答长度，这使得它们成为同策略（on-policy）采样中已经存在的天然压缩目标。因此，我们提出了隐式压缩正则化（ICR），这是一种同策略正则化方法，其压缩信号来自采样组中最短正确回答所诱导的虚拟更短分布，从而引导策略生成简洁且正确的轨迹。训练动态表明，ICR 在压缩过程中保持了更好的长度-准确率相关性，说明短回答依然与正确性保持良好的一致性，而不会漂移至思考不足。在三个推理骨干模型以及多个数学和知识密集型基准测试上的实验表明，ICR 在保持或提高准确率的同时，一致地缩短了回答长度，实现了更强的准确率-长度帕累托前沿。

## Abstract
Reinforcement learning with verifiable rewards improves LLM reasoning but often induces overthinking, where models generate unnecessarily long reasoning traces. Existing methods mainly rely on length penalties or early-exit strategies; however, the former may degrade accuracy and induce underthinking, whereas the latter assumes that substantial portions of reasoning traces can be safely truncated. To obtain a compression signal without these limitations, we revisit the training dynamics of existing compression methods. We observe that the length--accuracy correlation is initially negative but continually increases during compression, indicating that shorter responses are initially more likely to be correct but gradually lose this property as the policy moves toward underthinking. Based on this observation, we formalize overthinking: a negative correlation indicates an overthinking regime, while a positive one indicates underthinking. When overthinking, the shortest correct responses are shorter than the group-average response length in expectation, making them natural compression targets already present in on-policy rollouts. We therefore propose \emph{Implicit Compression Regularization} (ICR), an on-policy regularization method whose compression signal comes from a virtual shorter distribution induced by the shortest correct responses in rollout groups, guiding the policy toward concise yet correct trajectories. Training dynamics show that ICR maintains a better length--accuracy correlation during compression, indicating that short responses remain better aligned with correctness instead of drifting toward underthinking. Experiments on three reasoning backbones and multiple mathematical and knowledge-intensive benchmarks show that ICR consistently shortens responses while preserving or improving accuracy, achieving a stronger accuracy--length Pareto frontier.

---

## 论文详细总结（自动生成）

### 论文总结：隐式压缩正则化（ICR）

#### 1. 核心问题与整体含义（研究动机和背景）
在利用可验证奖励进行强化学习（RLVR）的后训练阶段，大语言模型（LLM）虽然推理能力显著增强，但常会出现**“过度思考”（Overthinking）**现象：模型生成冗长的推理轨迹，包含大量冗余步骤、重复自我反思或对简单问题的过度推导。这不仅增加了推理成本，还可能因引入干扰信息而降低准确率。
现有的解决方法（如长度惩罚或早停策略）存在局限：长度惩罚往往会损害准确率并导致“思考不足”；早停策略则假设推理轨迹可以被安全截断，这在信息密集的复杂任务中并不成立。本文旨在寻找一种无需显式长度惩罚或硬截断、且能保持准确率的压缩信号。

#### 2. 论文提出的方法论
*   **核心观察**：研究者发现，在训练初期，采样组内的长度与准确率呈负相关（短回答更可能正确），但随着压缩进行，这种相关性会趋向于零甚至变正。
*   **过度思考的定义**：论文将“负相关”定义为过度思考区间，将“正相关”定义为思考不足区间。在过度思考区间，组内最短的正确样本是天然的压缩目标。
*   **ICR 方法**：
    *   **核心思想**：不直接惩罚长度，而是通过正则化项，引导模型向采样组内已有的“最短且正确”的推理路径靠拢。
    *   **技术细节**：在 GRPO（组相对策略优化）的目标函数基础上，增加一个隐式压缩正则项。
    *   **算法流程**：对于每个查询，采样一组回答；识别出该组中既获得了正确奖励（Verifiable Reward）又是长度最短的样本集合 $S(q)$；仅对这些样本施加额外的策略梯度更新，从而诱导出一个虚拟的“更短分布”来指导策略优化。

#### 3. 实验设计
*   **骨干模型**：Qwen3-4B、Qwen3-8B、DeepSeek-R1-Distill-Qwen-7B。
*   **数据集/场景**：
    *   **数学推理**：AIME24/25、HMMT25、GSM8K、Math500、AMC23、Olympiad。
    *   **知识密集型（泛化性测试）**：ARC-Challenge、MMLU-Pro、SuperGPQA。
*   **对比方法（Baselines）**：
    *   Vanilla RL：GRPO。
    *   长度惩罚类：LP-F（固定参考惩罚）、LP-G（组内归一化惩罚，如 Kimi-k1.5 风格）、Shorter-Better。
    *   推理时压缩类：LC-R1（早停/截断策略）。

#### 4. 资源与算力
*   **硬件**：使用了 8 张 NVIDIA A800 GPU (40GB)。
*   **框架**：基于 EasyR1 和 VeRL 框架实现。
*   **训练细节**：Batch size 为 128，组大小 $G=8$，最大响应长度 8192，学习率 $1 \times 10^{-6}$。文中明确列出了详细的超参数配置表。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个不同规模和来源的骨干模型，并在 10 多个主流基准测试上进行了验证。
*   **消融实验**：非常充分。作者对比了“选择组内所有样本”、“选择负样本”、“仅使用正则项而不使用 GRPO 目标”等变体，证明了“最短且正确”这一筛选机制的必要性。
*   **客观性**：通过展示“准确率-长度帕累托前沿”曲线，客观地反映了不同方法在效率与性能之间的权衡，而非单一指标的对比。

#### 6. 主要结论与发现
*   **无损压缩**：ICR 在显著缩短推理长度（如在 Qwen3-4B 上缩短约 24%）的同时，准确率不仅没有下降，反而有所提升。
*   **帕累托前沿优化**：相比显式的长度惩罚，ICR 实现了更优的准确率-长度平衡，避免了模型滑向“思考不足”。
*   **兼容性**：ICR 可以与轻量级的长度惩罚（LP-F）结合使用，在需要极度压缩的场景下表现依然稳健。
*   **泛化性**：该方法在非数学的通用知识任务上也表现出良好的压缩效果和性能保持能力。

#### 7. 优点
*   **同策略（On-policy）特性**：压缩信号完全来自模型自身的采样，不引入外部模型或分布偏移。
*   **非侵入性**：不需要修改奖励函数结构，也不需要对推理轨迹进行硬截断，保护了推理逻辑的完整性。
*   **零额外成本**：仅需在训练时增加一个简单的组内筛选步骤，不增加采样或计算负担。
*   **理论洞察**：通过长度-准确率相关性动态地定义了过度思考，为该领域的后续研究提供了新视角。

#### 8. 不足与局限
*   **粒度较粗**：目前的相关性分析和正则化是在批次（Batch）和组（Group）级别进行的，无法针对单个推理步骤或特定查询进行精细化的冗余检测。
*   **非强制性控制**：ICR 提供的是“软信号”，无法像长度惩罚那样强制模型达到特定的目标长度（Budget）。
*   **依赖采样多样性**：如果采样组内没有出现较短且正确的样本，ICR 将无法发挥作用，这在极难题目上可能存在冷启动问题。

（完）
