---
title: "Rethinking Importance Sampling in LLM Policy Optimization: A Cumulative Token Perspective"
title_zh: 重新审视大语言模型策略优化中的重要性采样：累积 Token 视角
authors: "Yuheng Zhang, Chenlu Ye, Shuowei Jin, Changlong Yu, Wei Xiong, Saurabh Sahu, Nan Jiang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07331v1"
tags: ["query:token"]
score: 10.0
evidence: 大语言模型策略优化中的 Token 级重要性采样
tldr: 针对大语言模型强化学习微调中的重要性采样（IS）偏差与方差权衡问题，本文提出了CTPO算法。该方法引入累积Token IS比率，证明了其在提供无偏前缀修正的同时，方差低于全序列比率。结合位置自适应裁剪技术，CTPO在数学推理任务上显著优于GRPO和GSPO等基线，为策略优化提供了更稳健的理论基础和更优的实验表现。
selection_source: fresh_fetch
motivation: 现有的Token级IS比率因忽略前缀分布不匹配而产生偏差，而全序列IS比率则因乘性累积导致方差过大。
method: 提出CTPO算法，采用累积Token IS比率进行无偏梯度估计，并结合随序列位置动态扩展的自适应裁剪机制。
result: 在多个数学推理基准测试中，CTPO在不同参数规模的模型上均超越了GRPO和GSPO等强基线方法。
conclusion: 累积Token IS比率是解决LLM策略梯度估计中偏差-方差困境的有效理论方案，能显著提升复杂推理任务的微调效果。
---

## 摘要
强化学习，包括带有可验证奖励的强化学习（RLVR），已成为大语言模型（LLM）后训练的一种强大方法。这些方法的核心在于离策策略梯度（off-policy policy-gradient）估计中重要性采样（IS）比率的设计。现有方法面临着根本性的偏置-方差权衡：PPO 和 GRPO 采用的 Token 级 IS 比率因忽略前缀状态分布不匹配而引入偏置；全序列比率提供了精确的轨迹级修正，但由于逐 Token 比率的乘法累积而具有高方差；而 GSPO 通过长度归一化提高了数值稳定性，但代价是偏离了精确的全序列 IS 修正。在这项工作中，我们确定了累积 Token IS 比率（即直到位置 $t$ 的逐 Token 比率之积）是解决这一困境的理论化方案。我们证明，在 Token 级策略梯度公式下，该比率为每个 Token 级梯度项提供了无偏的前缀修正，且其方差严格低于全序列比率。基于这一见解，我们提出了 CTPO（Cumulative Token Policy Optimization），它将累积 Token IS 比率与位置自适应裁剪相结合，后者根据累积对数比率的自然 $\sqrt{t}$ 增长来缩放对数空间裁剪边界。这在不同 Token 位置上实现了更一致的正规化。我们在工具集成推理设置下的多个具有挑战性的数学推理基准测试中实现并评估了 CTPO，在两种模型规模上均取得了优于强基线 GRPO 和 GSPO 的最佳平均性能。代码将发布在 https://github.com/horizon-llm/CTPO。

## Abstract
Reinforcement learning, including reinforcement learning with verifiable rewards (RLVR), has emerged as a powerful approach for LLM post-training. Central to these approaches is the design of the importance sampling (IS) ratio used in off-policy policy-gradient estimation. Existing methods face a fundamental bias-variance dilemma: token-level IS ratios, as adopted by PPO (Schulman et al., 2017) and GRPO (Shao et al., 2024), introduce bias by ignoring prefix state distribution mismatch; full sequence ratios provide exact trajectory-level correction but suffer from high variance due to the multiplicative accumulation of per-token ratios, while GSPO (Zheng et al., 2025) improves numerical stability via length normalization at the cost of deviating from the exact full-sequence IS correction. In this work, we identify the cumulative token IS ratio, the product of per-token ratios up to position $t$, as a theoretically principled solution to this dilemma. We prove that, under the token-level policy-gradient formulation, this ratio provides an unbiased prefix correction for each token-level gradient term and has strictly lower variance than the full sequence ratio. Building on this insight, we propose CTPO (Cumulative Token Policy Optimization), which combines the cumulative token IS ratio with position-adaptive clipping that scales log-space clip bounds according to the natural $\sqrt{t}$ growth of the cumulative log-ratio. This yields more consistent regularization across token positions. We implement and evaluate CTPO in the tool-integrated reasoning setting on several challenging mathematical reasoning benchmarks, achieving the best average performance across both model scales compared with strong GRPO and GSPO baselines. Code will be available at https://github.com/horizon-llm/CTPO.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）强化学习后训练中的重要性采样（Importance Sampling, IS）问题提出了创新的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 LLM 的强化学习（如 RLVR）中，通常使用离策（Off-policy）策略梯度方法。其核心挑战在于如何设计**重要性采样比率**来修正行为策略与目标策略之间的分布偏差。
*   **现有方法的困境：**
    *   **Token 级比率（如 GRPO/PPO）：** 只考虑当前 Token 的概率比，忽略了前缀状态分布的不匹配，导致估计有**偏差**。
    *   **全序列比率：** 虽然理论上无偏，但由于逐 Token 比率的乘法累积，导致**方差极大**，训练不稳定。
    *   **长度归一化比率（如 GSPO）：** 提高了稳定性，但不再对应精确的 IS 修正，依然存在偏差。
*   **研究动机：** 寻找一种既能提供无偏修正，又能有效控制方差的理论化 IS 方案。

### 2. 论文提出的方法论：CTPO
论文提出了 **CTPO（Cumulative Token Policy Optimization）**，其核心包含两个关键技术：
*   **累积 Token IS 比率（Cumulative Token IS Ratio）：**
    *   定义位置 $t$ 的比率 $\rho^{cum}_t$ 为从起始到位置 $t$ 的所有逐 Token 比率的乘积。
    *   **理论证明：** 论文证明了该比率在 Token 级策略梯度公式下是**无偏的**（因为它正确修正了前缀轨迹概率），且其方差**严格低于**全序列比率（因为它剔除了与当前位置无关的后续 Token 的噪声）。
*   **位置自适应裁剪（Position-Adaptive Clipping）：**
    *   由于累积比率的对数空间方差随位置 $t$ 线性增长，固定裁剪范围会导致序列后端的 Token 被过度裁剪。
    *   **技术细节：** CTPO 将裁剪边界设定为随 $\sqrt{t}$ 缩放。公式上，对数空间的阈值 $\epsilon(t) = \epsilon \cdot t^p$（通常 $p=0.5$），从而在整个序列长度上实现更一致的正规化效果。

### 3. 实验设计
*   **场景：** 工具集成推理（Tool-Integrated Reasoning, TIR），模型通过编写并运行 Python 代码来解决数学问题。
*   **模型：** Qwen3-4B 和 Qwen3-14B。
*   **数据集：** 使用 DeepScaleR（约 40K 数学题目）进行训练。
*   **基准测试（Benchmarks）：** AIME 2025、AIME 2026、HMMT 2025、BRUMO 2025。
*   **对比方法：** 强基线 GRPO（Token 级 IS）和 GSPO（长度归一化序列级 IS）。
*   **评价指标：** avg@32（采样 32 次的平均通过率）。

### 4. 资源与算力
*   **硬件：** 实验在单节点上运行，配备 **8 张 NVIDIA H100 或 H200 GPU**。
*   **训练配置：** Prompt Batch Size 为 512，每个 Prompt 生成 8 个响应，学习率为 $1 \times 10^{-6}$。
*   **框架：** 使用 VERL 强化学习框架实现。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两种不同参数规模的模型（4B 和 14B）以及四个高难度竞赛级数学基准。
*   **消融实验：** 专门对比了“位置自适应裁剪”与“固定裁剪”的效果，验证了自适应策略的必要性。
*   **分析深度：** 提供了训练动态分析，包括准确率曲线、裁剪比例（Clip Fraction）和响应长度变化。
*   **评价：** 实验设计较为充分且客观，通过对比不同维度的基线方法，清晰地展示了 CTPO 在偏差-方差权衡上的优势。

### 6. 主要结论与发现
*   **性能领先：** CTPO 在所有基准测试中均优于 GRPO 和 GSPO。在 Qwen3-4B 上平均提升 3.7 个百分点，在 14B 上提升 3.3 个百分点。
*   **理论验证：** 实验观察到累积对数比率的方差确实随 $t$ 线性增长，验证了理论假设。
*   **裁剪一致性：** 位置自适应裁剪能保持稳定的裁剪率（约 5-10%），而固定裁剪在序列末端的裁剪率会飙升至 20%，抑制了有效学习。

### 7. 优点
*   **理论严谨：** 为 LLM 策略优化提供了一个介于 Token 级和序列级之间的“理论中道”，并给出了严格的数学证明。
*   **解决实际痛点：** 针对长文本生成中 IS 比率失效的问题，提出了简单且有效的 $\sqrt{t}$ 缩放修正。
*   **通用性：** 该方法不依赖于特定的奖励模型，适用于任何具有 Token 级 MDP 结构的 LLM 强化学习任务。

### 8. 不足与局限
*   **任务局限：** 实验主要集中在数学推理和工具集成任务上，尚未在通用对话或创意写作等非验证性奖励场景中广泛测试。
*   **超参数敏感性：** 虽然提出了自适应裁剪，但基础阈值 $\epsilon$ 和幂次 $p$ 仍可能需要针对不同长度分布的任务进行微调。
*   **计算开销：** 相比简单的 GRPO，计算累积比率虽增加了极小的计算量，但在极长序列（如数万 Token）下的数值稳定性仍需进一步观察。

（完）
