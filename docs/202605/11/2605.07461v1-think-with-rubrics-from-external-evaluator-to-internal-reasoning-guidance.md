---
title: "Think-with-Rubrics: From External Evaluator to Internal Reasoning Guidance"
title_zh: Think-with-Rubrics：从外部评估器到内部推理引导
authors: "Jiachen Yu, Zhihao Xu, Junjie Wang, Yujiu Yang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07461v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 将评分标准集成到推理上下文中以进行强化学习引导
tldr: 本研究针对现有大模型在处理开放式任务时仅将评分标准（Rubric）作为外部评估器的局限，提出了Think-with-Rubrics范式。该方法将评分标准生成融入推理上下文，使模型在生成回答前先自主构建标准，从而将外部评估转化为内部引导。通过训练验证器对标准与回答的一致性进行联合监督，该方法显著提升了模型在指令遵循任务中的表现，为增强大语言模型的推理能力提供了新思路。
selection_source: fresh_fetch
motivation: 现有的评分标准框架通常将其视为独立的外部评估工具，无法在模型生成过程中提供主动的实时引导。
method: 将评分标准生成整合进推理链条，要求模型先生成标准再生成响应，并引入专门的验证器进行联合监督训练。
result: 实验显示该方法在多个基准测试上比以黄金标准作为奖励的基线模型平均提升了3.87分。
conclusion: Think-with-Rubrics 通过优化自生成标准的质量和增强响应的内部一致性，成功实现了从外部评估到内部推理引导的转变。
---

## 摘要
评分规约（Rubrics）已被广泛用于评估不可验证的开放式任务，近期研究将其纳入强化学习的奖励系统中。然而，现有框架通常仅将评分规约视为与策略主要推理轨迹脱节的外部评估器。这种设计将评分规约局限于事后衡量，使其无法主动引导模型的生成过程。在这项工作中，我们提出了 Think-with-Rubrics，这是一种用于指令遵循任务的新型范式。Think-with-Rubrics 将评分规约的生成集成到推理上下文中，将评分规约从独立的产物转变为大语言模型（LLM）生成的内部引导。在训练过程中，LLM 依次生成评分规约和回复，而经过训练的评分规约验证器通过评估答案与自生成规约或标准规约（golden rubrics）之间的一致性来提供联合监督。在多个基准测试上的实验表明，Think-with-Rubrics 的表现始终优于由标准规约监督的 Rubric-as-Reward 基准，平均提高 3.87 分。我们还讨论了 Think-with-Rubrics 提升模型性能的机制。实验结果表明，来自标准规约和自生成规约的监督分别通过提高自生成规约的质量和增加回复的内部一致性，增强了 Think-with-Rubrics 的性能。

## Abstract
Rubrics have been extensively utilized for evaluating unverifiable, open-ended tasks, with recent research incorporating them into reward systems for reinforcement learning. However, existing frameworks typically treat rubrics only as external evaluator disjointed from the policy's primary reasoning trace. Such design confines rubrics to post-hoc measurement, leaving them unable to actively guide the model's generation process. In this work, we introduce Think-with-Rubrics, a novel paradigm for instruction following tasks. Think-with-Rubrics integrates rubric generation into the reasoning context, transforming the rubric from an independent artifact into an internal guidance of LLM's generation. During training, LLM sequentially generates a rubric followed by a response, while a trained rubric verifier provides joint supervision by evaluating the consistency between the answer and the self-generated / golden rubrics. Experiments across multiple benchmarks demonstrate that Think-with-Rubrics consistently outperforms the Rubric-as-Reward baseline supervised by golden rubrics by an average of 3.87 points. We have also discussed the mechanism by which Think-with-Rubrics enhances model performance. Experimental results demonstrate that supervision from golden rubrics and self-generated rubrics enhances the performance of Think-with-Rubrics by improving the quality of self-generated rubrics and increasing the internal consistency of responses respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《Think-with-Rubrics: From External Evaluator to Internal Reasoning Guidance》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在处理开放式、不可验证的任务（如创意写作或复杂指令遵循）时，研究者通常使用**评分标准（Rubrics）**作为强化学习（RL）的奖励信号。然而，现有方法（如 Rubric-as-Reward）将评分标准视为**外部评估器**，即模型先生成答案，再由外部系统根据标准打分。
*   **研究动机**：作者认为这种“事后评估”无法在生成过程中主动引导模型。受人类元认知（先思考要求再行动）的启发，本文提出将评分标准从外部工具转化为**内部推理引导**，让模型在生成答案前先显式地写出评分标准。

### 2. 方法论：核心思想与技术细节
论文提出了 **Think-with-Rubrics** 范式，将模型生成过程重构为：`输入指令 -> 生成自建评分标准 -> 生成最终答案`。

*   **核心思想**：将评分标准集成到模型的推理轨迹（Reasoning Trace）中，使其成为思考过程的一部分。
*   **训练流程**：
    1.  **SFT Warm-up（第一阶段）**：使用从强模型（如 Gemini-2.5-Flash）蒸馏出的数据，训练模型适应 `<rubric> ... </rubric> <answer> ... </answer>` 的输出格式。
    2.  **RL Training（第二阶段）**：使用 DAPO 算法进行强化学习。
*   **奖励系统（Reward System）**：
    *   **黄金标准一致性奖励 ($R_{gold}$)**：评估答案是否符合预设的正确评分标准。
    *   **自生成标准一致性奖励 ($R_{self}$)**：评估答案是否符合模型**自己刚刚写下**的评分标准（增强内部一致性）。
    *   **格式奖励 ($R_{fmt}$)**：确保模型生成的标准数量适中（防止奖励作弊，如生成极简单的标准）。
*   **评分验证器（Verifier）**：训练了一个专门的 Qwen3-8B 验证器，用于判断答案是否满足各项标准。

### 3. 实验设计
*   **数据集**：使用 **Openrubrics** 数据集进行训练（5k 用于 SFT，27k 用于 RL）。
*   **Benchmark（基准测试）**：
    *   **IFEval**：评估严格的指令遵循能力。
    *   **IFBench**：评估可验证的指令遵循。
    *   **InfoBench**：评估信息提取与遵循。
*   **对比方法**：
    *   **Rubric-as-Reward (Direct answer)**：传统的 RL 训练，不生成标准。
    *   **Rubric-as-Reward (With normal thinking)**：模型先进行普通思维链（CoT）推理再回答。
    *   **Think-with-Rubrics (Ours)**：包括仅使用黄金奖励、仅使用自生成奖励以及混合奖励的不同变体。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **SFT 阶段**：使用 4 台 NVIDIA **H20** GPU。
    *   **RL 阶段**：使用 8 台 NVIDIA **H20** GPU。
*   **训练时长**：
    *   SFT 训练 200 步。
    *   RL 训练约 120 步，单次完整 RL 训练耗时约 **16 小时**。
*   **模型规模**：主要在 Qwen3-8B-Base 上实验，并在 Qwen3-4B-Base 上验证了可扩展性。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流指令遵循基准上进行了测试，并涵盖了 8B 和 4B 两种模型尺寸。
*   **消融实验**：非常充分。作者研究了奖励权重比例（$\alpha:\beta$）、硬规则与原则的权重比、以及不同奖励组合对性能的影响。
*   **机制分析**：通过 RewardBench-2 评估了模型生成标准的质量，并分析了“内部一致性”得分，证明了实验结论的客观性。

### 6. 主要结论与发现
*   **性能提升**：Think-with-Rubrics 在 8B 模型上比基线平均高出 **3.87 分**，在 4B 模型上高出 **4.16 分**。
*   **结构化思考优于自由思考**：将 Rubric 作为思考过程的效果优于普通的自然语言 CoT，且推理长度更短（更高效）。
*   **自进化潜力**：即使**仅使用自生成标准**进行监督，其表现也接近甚至超过了仅使用黄金标准的监督，说明模型可以通过“自我约束”实现性能进化。
*   **双重作用**：黄金标准监督提升了 Rubric 的生成质量，而自生成标准监督提升了模型执行自己标准的忠实度。

### 7. 优点
*   **范式创新**：成功将 Rubric 从“评估工具”转变为“生成引导”，弥合了评价与生成之间的鸿沟。
*   **可解释性强**：模型在回答前列出标准，使用户能清晰看到模型打算如何处理任务。
*   **训练效率**：通过联合监督，避免了传统方法中 Rubric 与策略优化脱节的问题。

### 8. 不足与局限
*   **推理开销**：由于模型必须先生成一段 Rubric 文本，增加了推理时的 Token 消耗和延迟（Latency）。
*   **任务覆盖面**：目前仅在指令遵循（Instruction Following）任务上验证，尚未在创意写作、长文摘要或多轮对话等更宽泛的领域进行测试。
*   **格式依赖**：模型性能高度依赖于 SFT 阶段对格式的掌握，若格式解析失败，奖励机制将失效。

（完）
