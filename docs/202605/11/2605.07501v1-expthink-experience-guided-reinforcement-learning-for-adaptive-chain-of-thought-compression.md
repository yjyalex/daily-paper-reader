---
title: "ExpThink: Experience-Guided Reinforcement Learning for Adaptive Chain-of-Thought Compression"
title_zh: ExpThink：用于自适应思维链压缩的经验引导强化学习
authors: "Tingcheng Bian, Yuzhe Zhang, Jing Jin, Jinchang Luo, MingQuan Cheng, Haiwei Wang, Wenyuan Jiang, Miaohui Wang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07501v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于自适应思维链压缩的强化学习框架
tldr: "针对大推理模型（LRM）思维链（CoT）冗长导致的高延迟和高成本问题，本文提出 ExpThink 强化学习框架。该框架通过经验引导的奖励塑造和难度自适应优势函数，实现了动态的 CoT 压缩。在保证甚至提升准确率的前提下，ExpThink 将平均响应长度缩减了高达 77%，显著提升了推理效率，在多个数学推理基准测试中优于现有方法。"
selection_source: fresh_fetch
motivation: 现有的 CoT 压缩方法采用静态长度惩罚，忽略了模型能力的动态变化和题目难度的差异。
method: 提出经验引导奖励塑造以动态收紧长度阈值，并结合难度自适应优势函数来平衡不同难度题目下的准确率与简洁度。
result: "在多个数学基准测试中，该方法在减少 77% 长度的同时提升了准确率，准确率-效率比最高提升了 3 倍。"
conclusion: ExpThink 证明了通过自进化课程学习和难度感知梯度调节，可以在不牺牲推理能力的情况下实现极高的 CoT 压缩效率。
---

## 摘要
大型推理模型（LRM）通过扩展的思维链（CoT）推理实现了强大的性能，但面临着过高的 Token 消耗和高推理延迟的问题。现有的用于 CoT 压缩的强化学习（RL）方法依赖于统一、静态的长度惩罚，忽略了模型能力的动态变化和题目难度的差异。我们提出了 ExpThink，这是一个通过两种互补机制解决上述两个维度的强化学习框架。首先，经验引导的奖励塑造（experience-guided reward shaping）会跟踪每个问题迄今为止发现的最短正确解，并应用三级奖励：简洁的正确回答获得全额奖励，冗长的正确回答获得折扣奖励，错误回答则获得零奖励。该阈值随模型能力的提升而自动收紧，形成了一个无需手动调度的自进化课程。其次，难度自适应优势（difficulty-adaptive advantage）用正确计数归一化取代了标准差归一化，产生了随难度单调缩放的梯度，从而放大在难题上的学习以保持准确性，同时抑制在简单题上的梯度以鼓励简洁。这些机制共同强化了“准确性优先、压缩次之”的训练目标。在多个数学推理基准测试上的实验表明，ExpThink 在提高准确率的同时，将平均回答长度减少了高达 77%，其准确率-效率比（准确率除以平均 Token 数）比原始基准高出 3 倍，且在两项指标上均优于现有的基于强化学习的压缩方法。

## Abstract
Large reasoning models (LRMs) achieve strong performance via extended chain-of-thought (CoT) reasoning, yet suffer from excessive token consumption and high inference latency. Existing reinforcement learning (RL) approaches for CoT compression rely on uniform, static length penalties that neglect model capability dynamics and problem-level difficulty variation. We propose \textbf{ExpThink}\xspace, an RL framework that addresses both dimensions through two complementary mechanisms. First, \emph{experience-guided reward shaping} tracks the shortest correct solution found so far for each problem and applies a three-tier reward: full credit for concise correct responses, discounted credit for verbose correct ones, and zero for incorrect ones. The threshold tightens automatically with model improvement, forming a self-evolving curriculum that requires no manual scheduling. Second, \emph{difficulty-adaptive advantage} replaces standard deviation normalization with correct-count normalization, yielding monotonically difficulty-scaled gradients that amplify learning on hard problems to preserve accuracy while suppressing gradients on easy ones to encourage brevity. Together, these mechanisms enforce an accuracy-first, compression-second training objective. Experiments on multiple mathematical reasoning benchmarks demonstrate that \textbf{ExpThink}\xspace reduces average response length by up to 77\% while simultaneously improving accuracy, achieving up to $3\times$ higher accuracy-efficiency ratio (accuracy divided by average token count) than the vanilla baseline and outperforming existing RL-based compression methods on both metrics.

---

## 论文详细总结（自动生成）

以下是对论文《ExpThink: Experience-Guided Reinforcement Learning for Adaptive Chain-of-Thought Compression》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
大型推理模型（LRM，如 OpenAI-o1、DeepSeek-R1）虽然通过长思维链（CoT）获得了强大的逻辑推理能力，但其推理过程往往过于冗长，导致极高的 Token 消耗和推理延迟。
**现有研究的局限性：** 现有的强化学习（RL）压缩方法通常采用静态的长度惩罚（Length Penalty），忽略了两个关键维度：
*   **模型能力的动态性：** 随着训练进行，模型本可以更简洁地解决问题，但静态惩罚无法捕捉这种进步。
*   **题目难度的差异性：** 简单题和难题被施加了相同的压缩压力，容易导致难题的准确率受损。
**研究目标：** 开发一种能够根据模型当前能力和题目难度自适应调整压缩策略的 RL 框架。

### 2. 方法论：ExpThink 框架
ExpThink 引入了两个核心机制来打破传统 RL 压缩的“周期独立性”：

*   **经验引导的奖励塑造（Experience-Guided Reward Shaping）：**
    *   **经验池（Experience Buffer）：** 记录每个题目在所有历史训练周期中出现过的**最短正确解长度** ($l^*$)。
    *   **自进化课程：** 随着模型进步，$l^*$ 会不断收紧，形成一个自动演进的压缩目标，无需人工设定进度表。
    *   **三级奖励机制：** 
        1.  **全额奖励 (1.0)：** 回答正确且长度 $\le l^*$（鼓励简洁）。
        2.  **折扣奖励 (如 0.5)：** 回答正确但长度 $> l^*$（保留正确信号，但施加压缩压力）。
        3.  **零奖励 (0)：** 回答错误。

*   **难度自适应优势估计（Difficulty-Adaptive Advantage）：**
    *   **核心公式改进：** 改变了 GRPO 中标准的标准差归一化，转而使用**正确答案计数** ($|C_q|$) 进行归一化。
    *   **逻辑：** 在难题上（正确数少），分母小，梯度被放大，模型优先学习“如何做对”；在简单题上（正确数多），分母大，梯度被抑制，模型重点转向“如何变短”。这实现了“准确率第一，压缩第二”的动态平衡。

### 3. 实验设计
*   **基准模型：** DeepSeek-R1-Distill-Qwen-1.5B/7B、Qwen3-8B。
*   **训练数据集：** DeepScaleR（约 4 万个经过难度校准的数学问题）。
*   **评估基准：**
    *   **域内（数学）：** AIME24, AMC23, MATH-500, Minerva Math, OlympiadBench。
    *   **域外（泛化）：** LiveCodeBench（编程）, GPQA-Diamond（科学）, MMLU（综合）。
*   **对比方法：** LC-R1, Laser, AutoThink, AdaptThink, JET 等主流 RL 压缩算法。

### 4. 资源与算力
*   **硬件：** 使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **技术栈：** 采用 FSDP（全分片数据并行）和 CPU Offloading 以处理长上下文。
*   **训练细节：** 训练时长为 300 个更新步（Steps），全局 Batch Size 为 512，最大序列长度 16,384。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多尺度验证：** 覆盖了从 1.5B 到 8B 不同规模的模型。
*   **广泛的基准：** 包含 5 个数学专用库和 3 个跨领域库，验证了泛化性。
*   **消融实验：** 针对折扣奖励系数 ($r_{pen}$) 和优势函数类型进行了详细对比，证明了各组件的有效性。
*   **推理缩放分析：** 探讨了压缩后的模型在 Majority@k 投票下的性能表现。

### 6. 主要结论与发现
*   **高效压缩：** 在 Qwen3-8B 上，平均响应长度减少了 **77%**，同时准确率提升了 **5.33%**。
*   **智能密度提升：** 智能每 Token 比率（IPT）最高提升了 **3 倍**。
*   **消除“过度思考”：** 实验显示 ExpThink 显著减少了“Wait”、“But”、“Hmm”等表示犹豫和自我纠错的关键词（减少约 78.8%）。
*   **推理预算优化：** 由于单次推理变短，在相同 Token 预算下，ExpThink 可以进行更多次的并行采样投票，性能远超原始模型。

### 7. 优点与亮点
*   **自进化机制：** 巧妙利用历史最短路径作为动态阈值，解决了 RL 训练中历史信息被丢弃的问题。
*   **难度感知：** 通过改进优势函数，实现了无需手动调参的难度自适应学习。
*   **双赢局面：** 打破了“压缩必损准确率”的固有印象，实现了准确率与简洁度的同步提升。

### 8. 不足与局限
*   **任务依赖性：** 目前主要针对有标准答案（Verifiable Rewards）的任务（如数学、编程），对于开放式写作或无客观标准答案的任务，该框架的适用性尚未验证。
*   **超参数敏感性：** 容忍度因子 $\alpha$ 和折扣奖励 $r_{pen}$ 仍需经验性选择，缺乏跨领域的系统性敏感度研究。
*   **计算开销：** 维护经验池（Experience Buffer）虽然开销较小，但在超大规模数据集上的扩展性有待进一步观察。

（完）
