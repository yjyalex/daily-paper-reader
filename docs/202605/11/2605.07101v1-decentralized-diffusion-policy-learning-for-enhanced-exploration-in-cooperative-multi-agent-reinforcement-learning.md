---
title: Decentralized Diffusion Policy Learning for Enhanced Exploration in Cooperative Multi-agent Reinforcement Learning
title_zh: 合作式多智能体强化学习中用于增强探索的去中心化扩散策略学习
authors: "Yuyang Zhang, Haldun Balim, Na Li"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07101v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于协作多智能体强化学习的去中心化扩散策略
tldr: 针对协作式多智能体强化学习中高斯策略表达能力有限导致探索不足的问题，本文提出去中心化扩散策略学习（DDPL）。该方法利用扩散概率模型参数化智能体策略，以捕捉多峰动作分布并增强探索。通过引入具有理论保证的重要性采样评分匹配（ISSM）技术，实现了扩散策略的高效在线训练。在多个主流连续动作MARL基准测试中，DDPL展现出优于传统方法的性能。
selection_source: fresh_fetch
motivation: 传统DecSPG算法受限于高斯策略的表达能力，在处理复杂多智能体交互和大规模智能体系统时探索效率低下。
method: 采用扩散概率模型（DDPM）作为策略表示，并提出重要性采样评分匹配（ISSM）方法以支持高效的在线策略更新。
result: 在MAPE、Multi-agent MuJoCo、IsaacLab和JAX-SC2等多个实验环境中，DDPL一致性地取得了比基准算法更好的表现。
conclusion: 通过引入高表达能力的扩散模型和高效的训练机制，DDPL有效解决了多智能体强化学习中的探索难题。
---

## 摘要
合作式多智能体强化学习（MARL）涉及复杂的智能体交互，需要有效的探索策略。一类著名的 MARL 算法，即去中心化 Softmax 策略梯度（DecSPG），通过基于能量的策略更新来解决这一问题。然而在实践中，这类基于能量的策略难以维护，通常被投影到高斯策略类中。在这项工作中，我们证明了高斯策略有限的表达能力严重阻碍了 DecSPG 中的探索，且这种局限性随着智能体数量的增加而恶化。为解决这一问题，我们提出了去中心化扩散策略学习（DDPL），该方法使用去噪扩散概率模型对每个智能体的策略进行参数化。这是一种具有强大表达能力的生成模型，能够捕捉多峰动作分布以增强探索。DDPL 通过重要性采样评分匹配（ISSM）实现扩散策略的高效在线训练，这是一种具有理论保证的新型训练方法。我们在具有代表性的连续动作 MARL 基准测试中评估了 DDPL，包括多智能体粒子环境、多智能体 MuJoCo、IsaacLab 以及 JAX 重新实现的星际争霸多智能体挑战赛，并观察到了持续的性能提升。

## Abstract
Cooperative multi-agent reinforcement learning (MARL) involves complex agent interactions and requires effective exploration strategies. A prominent class of MARL algorithms, decentralized softmax policy gradient (DecSPG), addresses this through energy-based policy updates. In practice, however, such energy-based policies are intractable to maintain and are commonly projected onto the Gaussian policy class. In this work, we show that the limited expressiveness of Gaussian policies severely hinders exploration in DecSPG, and this limitation worsens as the number of agents grows. To address this issue, we propose decentralized diffusion policy learning (DDPL), which parameterizes each agent's policy with a denoising diffusion probabilistic model, an expressive generative model that captures multi-modal action distributions for enhanced exploration. DDPL enables efficient online training of diffusion policies via importance sampling score matching (ISSM), a novel training method with theoretical guarantee. We evaluate DDPL on representative continuous-action MARL benchmarks, including multi-agent particle environment, multi-agent MuJoCo, IsaacLab, and JAX-reimplemented StarCraft multi-agent challenge, and observe consistently improved performance.

---

## 论文详细总结（自动生成）

这篇论文由哈佛大学的研究团队提出，旨在解决合作式多智能体强化学习（MARL）中由于策略表达能力不足导致的探索困境。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在合作式 MARL 中，智能体需要在高维联合动作空间中进行有效探索以发现最优协调模式。
*   **研究背景**：现有的去中心化 Softmax 策略梯度（DecSPG）算法理论上能收敛至纳什均衡，但在实践中，由于基于能量的策略难以直接处理，通常被投影为**高斯策略**。
*   **动机**：作者发现高斯策略的单峰性（Unimodal）严重限制了探索能力。论文通过理论证明，在特定环境下，高斯策略成功的概率随智能体数量 $n$ 呈指数级衰减（至多 $0.45^n$）。因此，需要一种能够捕捉多峰（Multi-modal）分布的高表达能力策略表示。

### 2. 方法论：去中心化扩散策略学习（DDPL）
*   **核心思想**：利用**去噪扩散概率模型（DDPM）**来参数化每个智能体的去中心化策略，以利用其强大的多峰分布拟合能力增强探索。
*   **关键技术细节**：
    *   **扩散策略表示**：每个智能体 $i$ 维护一个扩散模型，通过反向去噪过程生成动作。
    *   **重要性采样评分匹配（ISSM）**：这是本文的核心创新。传统的扩散模型训练需要目标分布的样本，但在在线 RL 中目标分布（基于能量的更新目标）是未知的。ISSM 通过重要性采样技术，利用当前策略生成的样本进行加权评分匹配，从而在不需要目标分布样本的情况下实现策略更新。
    *   **理论保证**：论文证明了 ISSM 具有样本复杂度保证，并证明了在扩散更新存在误差的情况下，算法仍能收敛至近似纳什均衡（NE-gap 有界）。
*   **算法流程**：
    1.  智能体根据当前扩散策略进行采样并与环境交互。
    2.  估计每个智能体的平均 Q 函数。
    3.  使用 ISSM 目标函数更新扩散策略的评分网络（Score Network），权重由 Q 值决定。

### 3. 实验设计
*   **实验场景**：涵盖了 8 个具有代表性的连续动作 MARL 任务：
    *   **Multi-agent MuJoCo (MaMuJoCo)**：Ant (2x4, 4x2, 8x1), HumanoidStandup。
    *   **MPE**：Simple-Reference。
    *   **IsaacLab**：双影手协作（ShadowHandOver）。
    *   **SMAX (JAX 版星际争霸)**：3s_vs_5z, 6h_vs_8z。
*   **Benchmark 与对比方法**：
    *   **同策略（On-policy）基准**：MAPPO, HAPPO。
    *   **异策略（Off-policy）基准**：HASAC。
    *   **消融对比**：将 DDPL 中的扩散策略替换为高斯策略的变体。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA RTX 5090 GPU** 和 AMD EPYC 9275F CPU。
*   **训练时长**：每个随机种子的训练时间在 **1 到 8 小时**之间（取决于任务复杂度）。
*   **环境步数**：MPE 为 $5 \times 10^6$ 步，MaMuJoCo/IsaacLab 为 $1 \times 10^7$ 步，SMAX 为 $5 \times 10^7$ 步。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 8 个不同难度的任务实验，每个任务运行 3 个随机种子。
*   **充分性**：
    *   **对比充分**：涵盖了当前最主流的 MARL 算法（MAPPO/HAPPO/HASAC）。
    *   **消融实验**：专门对比了扩散策略与高斯策略在相同框架下的表现，验证了多峰性的必要性。
    *   **敏感性分析**：测试了扩散步数 $H$ 和噪声调度（Noise Schedule）对性能的影响，证明了算法的鲁棒性。
    *   **效率分析**：对比了训练和推理的墙钟时间，证明了 ISSM 相比其他扩散 RL 方法（如 DACER, QVPO）具有更高的训练效率。

### 6. 主要结论与发现
*   **性能领先**：DDPL 在 8 个任务中的 7 个任务上达到了最高回报，尤其在复杂的 SMAX 任务中，性能提升超过 130%。
*   **探索优势**：实验观察到 DDPL 在训练初期由于广泛探索可能进度较慢，但随后能迅速发现高回报的协调模式，而高斯基准往往陷入局部最优。
*   **多峰性验证**：可视化结果证实，扩散策略在训练过程中确实维持了多峰分布，这对于跨越次优均衡至关重要。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还提供了关于高斯策略失效的理论分析以及 ISSM 的收敛性证明。
*   **高效在线训练**：ISSM 解决了扩散模型在在线强化学习中训练难、耗时长的痛点。
*   **通用性强**：在多个物理模拟器和游戏环境中表现一致，证明了方法的泛化能力。

### 8. 不足与局限
*   **去中心化限制**：目前采用完全去中心化的策略，未充分利用网络化 MARL 中可能存在的低成本通信机制。
*   **任务类型**：研究重点在于“相同利益”（Identical-interest）的合作博弈，尚未扩展到一般和博弈或竞争性博弈。
*   **计算开销**：尽管比其他扩散 RL 方法快，但相比简单的高斯策略（如 MAPPO），扩散策略的推理和训练计算成本仍然较高。

（完）
