---
title: "Not All Tokens Learn Alike: Attention Entropy Reveals Heterogeneous Signals in RL Reasoning"
title_zh: 并非所有 Token 的学习方式都相同：注意力熵揭示了强化学习推理中的异构信号
authors: "Gengyang Li, Zheng-Fan Wu, Siqi Bao, Yunfang Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07660v1"
tags: ["query:token"]
score: 10.0
evidence: 推理中的Token级强化学习信号
tldr: 本研究探讨了强化学习（RL）在提升大模型推理能力时，不同Token学习信号的异质性。通过引入“注意力熵”指标，研究发现Token可分为低熵的“锚点”和高熵的“探索者”。锚点提供稳定梯度，而探索者包含复杂推理信号但波动大。基于此发现，作者提出动态熵感知加权策略，显著提升了模型在推理任务上的表现。
selection_source: fresh_fetch
motivation: 旨在理解强化学习后训练过程中，Token级别的学习信号如何影响大语言模型的推理能力及其异质性来源。
method: 利用注意力熵衡量Token上下文支持的集中程度，并对比分析了不同熵值Token的梯度稳定性及对模型性能的贡献。
result: 发现低熵Token提供稳定优化基础，而高熵Token虽不稳定但含关键推理信号，且动态重采样策略能有效提升Qwen3-8B的推理表现。
conclusion: 注意力熵揭示了Token级别RL信号的优化结构，证明了非均匀处理Token对提升推理模型后训练效率的重要性。
---

## 摘要
基于强化学习（RL）的后训练已成为提升大语言模型推理能力的关键方法，但其 Token 级的学习信号仍未被充分理解。本研究通过注意力熵（attention entropy）来探讨这些信号的异构性，注意力熵衡量了每个响应 Token 的上下文支持是集中的还是分散的。我们首先证明 Token 级的 RL 目标是稀疏可估计的：均匀随机选取 20% 的 Token 子集即可保留大部分全 Token 留出集性能，这表明 Token 级更新中存在大量冗余。然而，基于熵结构的子集表现截然不同。低注意力熵的 Token（我们称之为“锚点”，anchors）依赖于集中的支持，产生与全 Token 更新一致的稳定梯度，并提供可靠的优化骨干，但在更难的基准测试中往往会遇到瓶颈。高注意力熵的 Token（我们称之为“探索者”，explorers）聚合了更分散的上下文，并诱导产生更大但更不稳定的梯度。仅使用“探索者”进行训练通常是不稳定的，但少数成功的运行表明，在优化保持稳定的情况下，这些 Token 可能包含有用的硬推理信号。我们通过证据收集分析、熵动力学、梯度几何诊断以及对照实验来支持这种“锚点-探索者”光谱，结果表明位置、预测熵和损失归一化无法解释观察到的这种不对称性。最后，一种动态的熵感知软重加权干预在最强设置下将 Qwen3-8B-Base 的留出集平均性能从 34.39 提升至 37.40。这些发现表明，注意力熵揭示了 Token 级 RL 信号中与优化相关的结构，而均匀的 Token 平均可能会掩盖推理后训练中具有意义的异构性。

## Abstract
Reinforcement-learning-based post-training has become a key approach for improving the reasoning ability of large language models, but its token-level learning signals remain poorly understood. This work studies their heterogeneity through attention entropy, which measures how concentrated or diffuse the contextual support is for each response token.   We first show that token-level RL objectives are sparsely estimable: uniformly random 20 percent token subsets preserve much of the full-token held-out performance, suggesting substantial redundancy in token-level updates. However, entropy-structured subsets behave very differently. Low-attention-entropy tokens, which we call anchors, rely on concentrated support, produce stable gradients aligned with full-token updates, and provide a reliable optimization backbone, but tend to plateau on harder benchmarks. High-attention-entropy tokens, which we call explorers, aggregate more diffuse context and induce larger but more volatile gradients. Explorer-only training is unstable on average, though rare successful runs suggest that these tokens may contain useful hard-reasoning signals when optimization remains stable.   We support this anchor-explorer spectrum with evidence-gathering analyses, entropy dynamics, gradient-geometry diagnostics, and controls showing that position, predictive entropy, and loss normalization do not explain the observed asymmetry. Finally, a dynamic entropy-aware soft-reweighting intervention improves Qwen3-8B-Base from 34.39 to 37.40 held-out average in the strongest setting. These findings suggest that attention entropy reveals optimization-relevant structure in token-level RL signals, and that uniform token averaging can obscure meaningful heterogeneity in reasoning post-training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Not All Tokens Learn Alike: Attention Entropy Reveals Heterogeneous Signals in RL Reasoning》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在大语言模型（LLM）的强化学习（RL）后训练（如 DeepSeek-R1 范式）中，**Token 级别的学习信号是否存在异质性**。
*   **研究动机**：目前的 RL 训练通常对推理路径中的所有 Token 进行均匀加权（Uniform Averaging），但作者怀疑并非所有 Token 对模型推理能力的提升贡献相同。
*   **核心问题**：是否存在一种诊断工具能识别哪些 Token 提供稳定的优化基础，哪些 Token 携带更难但更有价值的推理信号？

### 2. 提出方法论
作者引入了**注意力熵（Attention Entropy）**作为诊断指标，并基于此提出了优化光谱理论。

*   **核心指标：注意力熵**
    *   **计算方式**：对某一层的注意力概率分布计算信息熵。为了消除序列长度的影响，使用归一化熵 $H_{norm} = H_{raw} / \log N_t$（$N_t$ 为可见上下文长度）。
    *   **分类**：
        *   **锚点 Token (Anchors)**：低注意力熵。依赖集中的上下文支持，梯度稳定且与全局梯度对齐度高，是优化的“骨干”。
        *   **探索者 Token (Explorers)**：高注意力熵。聚合分散的上下文信息，梯度模长更大但方向波动剧烈，包含解决难题的关键信号。
*   **关键技术：动态熵感知软重加权 (Dynamic Entropy-Aware Soft-Reweighting)**
    *   **核心思想**：不再均匀对待所有 Token，而是根据训练阶段动态调整权重。
    *   **Low2High 调度策略**：训练初期给低熵的“锚点”更高权重以稳定模型基础；随着训练进行（Warmup 后），逐渐增加高熵“探索者”的权重，以捕捉复杂的推理逻辑。

### 3. 实验设计
*   **模型与框架**：主要使用 **Qwen3-8B-Base**，基于 VeRL 框架和 DAPO 优化目标。
*   **数据集**：训练集采用 DeepScaler 和 MATH 的混合数据。
*   **Benchmark（基准测试）**：
    *   **AIME**（高难度数学竞赛）
    *   **OlympiadBench**（奥数水平题目）
    *   **Minerva**（定量推理）
    *   **MATH**（标准数学题库）
*   **对比方法**：
    *   **Full-token DAPO**（全 Token 均匀训练，基准线）。
    *   **Random-20%**（随机抽取 20% Token 训练）。
    *   **Anchor-only / Explorer-only**（仅训练特定熵值的 Token）。
    *   **对照组**：基于位置（前/后）、预测熵（Next-token uncertainty）和损失大小的 Token 选择策略。

### 4. 资源与算力
论文对算力使用有明确说明：
*   **硬件**：使用 NVIDIA **H800 (80GB)** GPU 集群。
*   **规模**：
    *   选择性训练探测实验：2 个节点（16 张 GPU），每组运行约 22 小时。
    *   完整干预实验（Table 1）：4 个节点（32 张 GPU），每组运行约 18 小时。
*   **总算力消耗**：作者估计整个研究过程（含消融实验、多种子运行等）总计消耗约 **10,000 至 20,000 GPU 小时**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融和验证，包括 8 个独立种子的 Explorer 训练稳定性测试、3 个种子的干预实验重复、以及在 Qwen3-14B 和 Qwen2.5-7B 上的迁移验证。
*   **客观性与公平性**：
    *   **对照严谨**：通过位置控制和预测熵控制，排除了“注意力熵只是位置或不确定性的代名词”这一假设。
    *   **归一化控制**：专门讨论了损失归一化（Denominator）对实验结果的影响，确保性能提升非数值缩放导致。
    *   **多层探测**：测试了浅层、中层、深层作为熵来源的效果，证明了现象的普遍性。

### 6. 主要结论与发现
1.  **稀疏可估计性**：仅用 20% 的随机 Token 就能恢复全 Token 训练约 97% 的性能，说明 Token 级更新存在大量冗余。
2.  **优化光谱**：锚点 Token 负责稳定性，探索者 Token 负责上限。仅训练探索者会导致模型崩溃（如长度缩减、奖励停滞），但在稳定路径下，它们能显著提升 AIME 等难题的表现。
3.  **干预有效性**：采用 Low2High 动态加权策略，Qwen3-8B 的平均分从 34.39 提升至 37.40，尤其在 AIME 上提升显著（从 5.83 升至 9.75）。
4.  **预测熵 vs 注意力熵**：两者相关性极低（r = -0.19），证明注意力熵捕捉的是内部上下文聚合模式，而非简单的预测不确定性。

### 7. 优点
*   **视角新颖**：首次从注意力熵的角度剖析 RL 推理训练的 Token 级动力学，而非仅仅关注策略熵。
*   **诊断深入**：结合了梯度几何（余弦相似度、投影比）和证据收集模式（支持位置数量），提供了机械论层面的解释。
*   **实用性强**：提出的软重加权方案简单易行，无需额外的奖励模型或复杂的采样过程。

### 8. 不足与局限
*   **层选择的启发式**：虽然测试了不同层，但目前仍需预先指定某一层作为熵来源，缺乏自动选择最优层或跨层融合的机制。
*   **任务局限**：实验主要集中在数学推理（Verifiable Rewards），在非数学类推理（如法律、创意写作）中的适用性尚未验证。
*   **调度参数敏感**：Low2High 的具体切换时机（Warmup 步数）可能需要针对不同模型和数据进行微调。
*   **因果定位**：虽然观察到了相关性，但尚未在 Transformer 内部头（Heads）级别实现完全的因果干预定位。

（完）
