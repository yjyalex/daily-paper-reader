---
title: "HMACE: Heterogeneous Multi-Agent Collaborative Evolution for Combinatorial Optimization"
title_zh: HMACE：用于组合优化的异构多智能体协同演化
authors: "Yuping Yan, Jirui Han, Fei Ming, Yuanshuai Li, Yaochu Jin"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07214v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于生成式优化任务的多智能体协同框架
tldr: HMACE是一个针对组合优化问题（COP）的异构多智能体协作进化框架。针对现有LLM启发式设计方法中工作流僵化、易陷入局部最优的问题，该框架将进化过程分解为由提议者、生成者、评估者和反射者组成的协作循环。通过行为感知检索和轻量级过滤，HMACE在TSP、BPP等多个任务上以更低的Token消耗实现了优于现有单/多智能体基准的性能。
selection_source: fresh_fetch
motivation: 现有的基于大语言模型的启发式设计方法通常依赖单一且僵化的工作流，限制了探索能力并容易导致过早收敛。
method: 提出一种异构多智能体协作进化框架，通过提议、生成、评估和反射四个角色的分工协作，结合记忆存档更新来引导启发式搜索。
result: 在TSP、Online BPP等典型组合优化问题上，HMACE以显著更少的Token消耗（如TSP仅需0.13M）达到了最低的平均误差。
conclusion: HMACE通过组织化设计和多智能体协作，有效提升了自动化启发式设计的质量与效率，为解决复杂组合优化问题提供了新范式。
---

## 摘要
大语言模型（LLM）最近已成为解决 NP 难组合优化问题（COP）中自动启发式设计的有前景的范式。尽管取得了这些进展，现有的基于 LLM 的方法通常依赖于受刚性模板限制的单一工作流，从而限制了记忆引导的探索，并导致过早收敛到局部最优。为了设计一种自主且协同的架构，我们引入了 HMACE，这是一个异构多智能体协同演化框架，它将启发式搜索重新概念化为一个组织设计问题。HMACE 将每一代演化分解为一个自主的、角色专门化的循环，包含四个协调的智能体：用于策略探索的建议者（Proposer）、用于可执行启发式合成的生成者（Generator）、用于经验评估的评估者（Evaluator）以及用于基于存档的记忆更新的反射者（Reflector）。通过结合行为感知检索、轻量级候选过滤和基于适应度的存档更新，HMACE 引导搜索向多样化且有前景的启发式行为发展，同时避免冗余评估。对包括 TSP、在线 BPP、MKP 和 PFSP 在内的代表性 COP 进行的广泛评估表明，与最先进的单智能体和多智能体基准相比，HMACE 实现了良好的质量-效率权衡。在匹配的 LLM 驱动参考对比中，HMACE 在 TSP 和在线 BPP 上实现了最低的平均差距（分别为 0.464% 和 0.223%），而这两项任务仅分别需要 0.13M 和 0.42M 个 token，显著少于对比基准。

## Abstract
Large Language Models have recently emerged as a promising paradigm for automated heuristic design for NP-hard combinatorial optimization problems. Despite this progress, existing LLM-based methods typically rely on monolithic workflows constrained by rigid templates, thereby restricting memory-guided exploration and triggering premature convergence to local optima. To design an autonomous and collaborative architecture, we introduce HMACE, a Heterogeneous Multi-Agent Collaborative Evolution framework that reconceptualizes heuristic search as an organizational design problem. HMACE decomposes each evolutionary generation into an autonomous, role-specialized loop with four coordinated agents: a Proposer for strategy exploration, a Generator for executable heuristic synthesis, an Evaluator for empirical assessment, and a Reflector for archive-backed memory update. By coupling behavior-aware retrieval, lightweight candidate filtering, and fitness-grounded archive updates, HMACE guides the search toward diverse and promising heuristic behaviors while avoiding redundant evaluations. Extensive evaluations on representative COPs, including TSP, Online BPP, MKP, and PFSP, show that HMACE achieves a favorable quality-efficiency trade-off compared to state-of-the-art single-agent and multi-agent baselines. In the matched LLM-driven reference comparison, HMACE achieves the lowest average gaps on TSP and Online BPP (0.464\% and 0.223\%, respectively), while requiring only 0.13M and 0.42M tokens for the two tasks, substantially fewer than the compared baselines.

---

## 论文详细总结（自动生成）

以下是对论文《HMACE: Heterogeneous Multi-Agent Collaborative Evolution for Combinatorial Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**自动化启发式算法设计（Automatic Heuristic Design）**，旨在解决复杂的组合优化问题（COPs）。
*   **研究动机**：虽然大语言模型（LLM）在生成启发式代码方面表现出色，但现有方法存在两个瓶颈：
    1.  **工作流僵化**：多采用单一智能体（Monolithic）或固定模板，缺乏系统性的回溯和多样化探索，容易陷入局部最优。
    2.  **角色干扰**：将规划、生成、评估和反思等异构认知任务强加给单个智能体，会导致性能下降。
*   **核心思想**：将启发式搜索重新定义为一个**组织设计问题**，通过构建一个由四个专门化角色组成的异构多智能体系统（HMACE），实现自主且协同的进化搜索。

### 2. 方法论：核心思想与关键技术
HMACE 引入了一个异构协作进化循环，其核心组件包括：
*   **四种专门化智能体角色**：
    1.  **提议者 (Proposer)**：基于当前种群和从存档中检索到的多样化示例，提出自然语言形式的策略建议。
    2.  **生成者 (Generator)**：将策略建议转化为可执行的 Python 启发式代码。
    3.  **评估者 (Evaluator)**：在训练集上运行代码，输出**适应度得分 ($f_h$)** 和 **行为向量 ($\phi_h$)**。
    4.  **反射者 (Reflector)**：维护一个基于行为的存档（Archive），执行行为感知检索，并根据适应度更新存档，引导后续搜索。
*   **关键技术细节**：
    *   **行为感知检索**：利用 CVT-MAP-Elites 算法将启发式算法映射到行为空间，确保检索到的示例具有行为多样性，防止搜索空间坍缩。
    *   **轻量级过滤 (Filter)**：在昂贵的 LLM 评估前，通过确定性规则剔除格式错误或重复的代码，节省 Token 预算。
    *   **双信号反馈**：不仅关注算法“好不好”（适应度），还关注算法“怎么做”（行为特征），从而实现质量与多样性的平衡。

### 3. 实验设计
*   **数据集/场景**：
    *   **TSP (旅行商问题)**：使用 TSPLIB 中的 77 个实例（50 到 20,000 城市）。
    *   **Online BPP (在线装箱问题)**：使用 Weibull-5k 分布。
    *   **MKP (多维背包问题)**：10 个合成实例。
    *   **PFSP (置换流水车间调度问题)**：Taillard 基准实例。
*   **对比基准 (Baselines)**：
    *   **传统算法**：Concorde, OR-Tools, Nearest Neighbor, Best Fit 等。
    *   **单智能体 LLM 方法**：FunSearch, EoH。
    *   **搜索控制器增强方法**：ReEvo, HSEvo, MCTS-AHD, MoH。
    *   **多智能体方法**：CORAL, DRAGON。

### 4. 资源与算力
*   **硬件**：服务器配备 NVIDIA L40 GPU 和 Intel Gold 6330 CPU (2.00GHz)。
*   **LLM 底座**：主要实验使用了 **GPT-5.4**（注：论文发表时间设定为 2026 年）。
*   **运行配置**：每个任务设置 1 小时的时间限制，使用 12 个并行评估进程。
*   **Token 消耗**：HMACE 在 TSP 上仅消耗 0.13M tokens，在 BPP 上消耗 0.42M tokens，显著低于对比方法。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 类典型的组合优化问题，每类问题包含多个规模（如 TSP 从 20 到 1000 城市）。
*   **消融实验**：针对“随机检索 vs 行为感知检索”、“有无过滤机制”、“不同筛选策略”以及“单调用 vs 多智能体”进行了详细对比。
*   **客观性**：每个实验运行 3 个随机种子并报告均值和标准差。通过“匹配 LLM 预算”的对比方式，确保了效率评估的公平性。实验设计全面，论证了角色分工和记忆机制的有效性。

### 6. 主要结论与发现
*   **性能领先**：HMACE 在 TSP 和 Online BPP 上达到了最低的平均 Gap（误差），优于所有已知的单智能体和多智能体基准。
*   **极高的效率**：在达到相同或更好性能的前提下，HMACE 的 Token 消耗仅为竞争对手（如 CORAL）的几分之一甚至十几分之一。
*   **鲁棒性**：通过行为存档机制，HMACE 能有效跳出局部最优，在搜索后期仍能保持改进动力。

### 7. 优点
*   **组织化架构**：成功解耦了复杂的认知任务，消除了角色干扰，使 LLM 能够专注于特定职能。
*   **质量-效率权衡**：通过轻量级过滤和行为感知检索，极大地优化了 API 调用成本，具有很强的实用价值。
*   **行为多样性**：引入质量-多样性（Quality-Diversity）搜索思想，使启发式算法的进化不再局限于单一路径。

### 8. 不足与局限
*   **工作流固定**：目前的智能体协作模式是预定义的阶段性流水线，缺乏根据搜索动态自动调整角色的灵活性。
*   **长期记忆挑战**：随着搜索进行，存档数据会不断累积，如何动态精简记忆以避免 LLM 上下文碎片化仍是一个挑战。
*   **应用范围**：目前主要集中在离散组合优化，尚未在连续优化或更广泛的实时决策场景中验证。

（完）
