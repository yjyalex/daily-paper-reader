---
title: "Prune-OPD: Efficient and Reliable On-Policy Distillation for Long-Horizon Reasoning"
title_zh: Prune-OPD：面向长程推理的高效且可靠的同策略蒸馏
authors: "Zhicheng Yang, Zhijiang Guo, Yifan Song, Minrui Xu, Yongxin Wang, Yiwei Wang, Xiaodan Liang, Jing Tang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07804v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 长程推理的在线策略蒸馏
tldr: "本研究针对长程推理任务中策略内蒸馏（OPD）存在的“前缀漂移”问题，即学生模型生成的轨迹偏离教师思路导致奖励失效且浪费计算资源。为此提出了 Prune-OPD 框架，通过实时监测学生与教师预测的局部兼容性，动态截断不可靠的生成并下调后续奖励权重。实验表明，该方法在 AMC 和 AIME 等挑战性基准上，不仅提升了推理性能，还将训练时间显著缩短了 37.6% 至 68.0%，实现了计算资源与监督质量的高效对齐。"
selection_source: fresh_fetch
motivation: 解决长程推理任务中，学生模型轨迹偏离教师思路导致密集奖励失去局部可利用性及计算资源浪费的问题。
method: 引入 Prune-OPD 框架，通过 top-k 重合度等指标实时监测前缀漂移，动态截断不可靠的生成并降低其奖励权重。
result: "在多个数学竞赛基准测试中，该方法在提升模型性能的同时，将训练时间大幅缩短了 37.6% 至 68.0%。"
conclusion: Prune-OPD 通过将计算资源重新分配给可靠且可利用的教师监督信号，显著提升了长程推理模型蒸馏的效率和可靠性。
---

## 摘要
同策略蒸馏（OPD）利用密集的教师奖励来增强推理模型。然而，将 OPD 扩展到长程任务时暴露了一个关键缺陷：随着学生生成的序列前缀不可避免地偏离教师的思维过程，教师的密集奖励失去了局部可利用性。在这些“偏移”的轨迹上继续生成和评估 token 不仅会降低奖励质量，还会导致巨大的计算浪费。为解决这一问题，我们引入了 Prune-OPD，这是一个将训练预算与监督质量动态对齐的框架。通过持续监测学生和教师预测之间的局部兼容性（例如，通过 top-k 重叠），Prune-OPD 可以实时检测前缀偏移事件。一旦检测到严重偏移，它会单调地降低后续不可靠奖励的权重，并触发动态展开截断。这使得训练过程能够停止徒劳的生成，并将计算资源严格重新分配给可靠的教师监督。在多种教师-学生组合中，Prune-OPD 始终使计算与监督可靠性保持一致。当前缀偏移导致密集的教师奖励不可靠时，它在保持甚至通常提高挑战性基准测试（AMC、AIME、HMMT）性能的同时，将训练时间缩短了 37.6%--68.0%。当学生与教师的兼容性保持较高水平时，它通过扩大训练窗口自动保留长上下文监督。这些结果表明，Prune-OPD 改进 OPD 并非通过盲目缩短展开长度，而是通过将计算重新分配给局部可利用的教师奖励。

## Abstract
On-policy distillation (OPD) leverages dense teacher rewards to enhance reasoning models. However, scaling OPD to long-horizon tasks exposes a critical flaw: as the student's generated prefix inevitably diverges from the teacher's thought process, the teacher's dense reward loses local exploitability. Continuing to generate and evaluate tokens on these ``drifted'' trajectories not only degrades reward quality but also incurs massive computational waste. To address this, we introduce \textbf{Prune-OPD}, a framework that dynamically aligns training budgets with supervision quality. By continuously monitoring the local compatibility between student and teacher predictions (e.g., via top-$k$ overlap), Prune-OPD detects prefix-drift events in real time. Upon detecting severe drift, it monotonically down-weights subsequent unreliable rewards and triggers dynamic rollout truncation. This allows the training process to halt futile generation and reallocate compute strictly to reliable teacher supervision. Across diverse teacher-student combinations, Prune-OPD consistently aligns computation with supervision reliability. When prefix drift makes dense teacher rewards unreliable, it reduces training time by 37.6\%--68.0\% while preserving, and often improving, performance on challenging benchmarks (AMC, AIME, HMMT). When student-teacher compatibility remains high, it automatically preserves long-context supervision by expanding the training window. These results suggest that Prune-OPD improves OPD not by blindly shortening rollouts, but by reallocating computation toward locally exploitable teacher rewards.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Prune-OPD** 的新型训练框架，旨在解决长程推理任务中同策略蒸馏（On-Policy Distillation, OPD）的效率与可靠性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程推理（如复杂的数学证明）中，学生模型生成的“思维链”前缀会逐渐偏离教师模型的逻辑路径（即“前缀漂移”）。
*   **研究动机**：
    *   **奖励失效**：当学生模型偏离后，教师模型在当前状态下的密集奖励（Dense Reward）不再具有局部可利用性，甚至可能提供错误的指导信号。
    *   **计算浪费**：现有的 OPD 方法通常采用固定的生成长度（Rollout Budget），在已经偏离的轨迹上继续生成和评估 token 会浪费大量算力。
    *   **权衡困境**：短固定预算会丢失长程推理信号，长固定预算则效率极低。

### 2. 方法论：Prune-OPD 框架
核心思想是**动态对齐训练预算与监督质量**，只在教师模型能提供可靠指导的地方投入算力。
*   **局部兼容性监测**：在每个 token 生成位置 $\tau$，计算学生和教师预测分布的 **Top-k 重合率（Overlap Ratio）**。如果重合率低于阈值 $\gamma$，则记录一次“前缀漂移”事件。
*   **累积可靠性权重**：
    *   定义一个单调递减的权重 $R_\tau$，随着漂移事件的累积而降低。
    *   最终损失权重 $L_\tau = R_\tau + w_{base}$，确保即使在不可靠区域也保留微弱的基础监督，避免硬截断导致的训练不稳定。
*   **动态响应预算控制**：
    *   算法会统计批次中“有效可靠长度”达到当前上限的比例。
    *   **动态调整**：如果大部分样本在当前长度内都是可靠的，则自动增加生成上限（最高可达 12k+ tokens）；反之，则缩减上限以节省算力。
*   **算法流程**：采样 -> 计算重合率 -> 更新可靠性权重 -> 缩放 OPD 奖励 -> 根据可靠长度动态调整下一步的生成预算。

### 3. 实验设计
*   **数据集**：使用 **DAPO-Math-17K** 进行训练。
*   **评估基准（Benchmarks）**：AMC23、AIME24/25、HMMT24/25（均为高难度数学竞赛题目）。
*   **对比方法**：
    *   标准 OPD（全量计算）。
    *   固定长度截断（如强制截断在 4k tokens）。
    *   不同兼容性指标（Top-k 重合率 vs. Top-p 动作接受度）。
*   **模型组合**：测试了 5 组教师-学生对，包括 DeepSeek-R1-Distill (1.5B/7B)、Qwen3 (1.7B/4B) 以及 Skywork 等不同规模和系列的组合。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和数量（如 H100 数量），但详细记录了**训练时长**的对比。
*   **效率提升**：在低兼容性场景下，Prune-OPD 将训练时间缩短了 **37.6% 至 68.0%**。例如，某组实验从 12.5 小时缩减至 4.0 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 组不同的教师-学生架构组合，验证了方法的通用性。
*   **消融实验**：对重合率阈值 $\gamma$（0.6 到 0.9）、权重衰减率、以及不同的兼容性度量指标进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分，不仅关注最终精度，还深入分析了训练过程中的动态变化（如有效长度随步数的变化曲线），证明了性能提升并非偶然，而是源于对不可靠梯度的过滤。

### 6. 主要结论与发现
*   **高效性**：Prune-OPD 在大幅减少计算量的同时，保持了甚至提升了模型在数学竞赛上的准确率。
*   **自适应性**：当学生与教师高度兼容时，系统会自动保持长窗口监督；只有在发生漂移时才触发截断。
*   **梯度去噪**：在某些 Qwen3 实验中，Prune-OPD 显著提升了准确率，这表明过滤掉漂移后的“垃圾奖励”起到了梯度去噪的作用，防止了模型被不可靠的教师信号误导。

### 7. 优点
*   **动态性**：打破了固定 Rollout 长度的传统做法，实现了“按需分配算力”。
*   **即插即用**：作为 OPD 的修改器，不需要改变教师模型的结构，易于集成到现有的强化学习或蒸馏流水线中。
*   **可靠性**：通过单调递减权重而非硬截断，平衡了训练的探索性与稳定性。

### 8. 不足与局限
*   **任务局限**：目前仅在数学推理任务上验证，尚未在多轮对话、创意写作或智能体（Agent）任务中测试。
*   **线性假设**：权重衰减采用的是简单的线性裁剪，可能无法完美捕捉复杂的逻辑恢复过程（即学生模型在偏离后又绕回正确路径的情况）。
*   **超参数依赖**：虽然有消融实验，但阈值 $\gamma$ 和衰减率 $w_{drop}$ 仍需根据不同的教师-学生对进行微调。

（完）
