---
title: Structured Role-Aware Policy Optimization for Multimodal Reasoning
title_zh: 面向多模态推理的结构化角色感知策略优化
authors: "Bingqing Jiang, Difan Zou"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07274v1"
tags: ["query:token"]
score: 10.0
evidence: 强化学习中的角色感知Token级信用分配
tldr: 针对多模态大模型在强化学习中仅依赖序列级奖励、难以区分感知与推理贡献的问题，本文提出结构化角色感知策略优化（SRPO）。该方法将响应分解为感知和推理令牌，通过自蒸馏策略对比，在不改变奖励函数的前提下，为不同角色的令牌分配差异化权重。实验证明，SRPO显著提升了模型在多模态推理任务中的证据可靠性和准确性。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习采用序列级奖励，无法区分感知令牌与推理令牌的贡献，导致模型可能在缺乏视觉证据的情况下得出正确答案。
method: 提出SRPO方法，利用视觉依赖性和逻辑一致性进行自蒸馏对比，将序列级优势细化为角色感知的令牌级优势。
result: 在多个多模态推理基准测试中，SRPO在不引入外部奖励模型的情况下，显著增强了模型基于证据的推理能力。
conclusion: 转向角色感知的令牌级信用分配是提升多模态大模型推理可靠性的关键路径。
---

## 摘要
基于可验证奖励的强化学习（RLVR），特别是结合群体相对策略优化（GRPO），在提升大视觉语言模型（LVLMs）的推理能力方面展现出巨大潜力。然而，在多模态推理中，最终答案的奖励通常是在序列层面分配的，并未区分不同 token 的功能角色，这使得难以确定正确答案是否得到了任务相关视觉证据的支持。在本文中，我们从角色感知 token 级信度分配的角度重新审视了多模态 RLVR，其中结构化响应被分解为用于提取视觉证据的感知 token 和用于根据该证据推导答案的推理 token。基于这一视角，我们提出了结构化角色感知策略优化（SRPO），它在不改变奖励函数的情况下，将序列级的 GRPO 优势细化为角色感知的 token 级优势。具体而言，SRPO 通过使用自蒸馏的同策略对比来分配特定角色的信度：感知 token 根据其在原始视觉输入与损坏视觉输入下的视觉依赖性进行强化，而推理 token 则根据其与生成的感知内容的一致性进行强化。这些特定角色的信号通过共享的轨迹级基准进一步统一，产生正向 token 权重，在调整相对更新幅度的同时保留了原始 GRPO 的奖励和优化方向，且无需外部奖励模型或独立的教师模型。在多种多模态推理基准测试上的实验表明，SRPO 提升了基于证据的推理能力，强调了在可靠的多模态推理中，从统一的序列级信度转向角色感知优化的重要性。

## Abstract
Reinforcement learning from verifiable rewards (RLVR), especially with Group Relative Policy Optimization (GRPO), has shown strong potential for improving the reasoning capabilities of large vision-language models (LVLMs). However, in multimodal reasoning, final-answer rewards are typically assigned at the sequence level and do not distinguish the functional roles of different tokens, making it difficult to determine whether a correct answer is supported by task-relevant visual evidence. In this paper, we revisit multimodal RLVR from the perspective of role-aware token-level credit assignment, where structured responses are decomposed into perception tokens for extracting visual evidence and reasoning tokens for deriving answers from that evidence. Based on this perspective, we propose Structured Role-aware Policy Optimization (SRPO), which refines the sequence-level GRPO advantage into role-aware token-level advantages without changing the reward function. Specifically, SRPO assigns role-specific credit by using self-distilled on-policy contrasts: perception tokens are emphasized according to their visual dependency under original versus corrupted visual inputs, while reasoning tokens are emphasized according to their consistency with the generated perception. These role-specific signals are further unified through a shared trajectory-level baseline, yielding positive token weights that adjust relative update magnitudes while preserving the original GRPO reward and optimization direction, without requiring external reward models or separate teachers. Experiments across diverse multimodal reasoning benchmarks show that SRPO improves evidence-grounded reasoning, highlighting the importance of moving beyond uniform sequence-level credit toward role-aware optimization for reliable multimodal reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structured Role-Aware Policy Optimization for Multimodal Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机）
多模态大模型（LVLMs）在进行复杂推理时，常面临“答案正确但过程错误”的问题。现有的强化学习方法（如 GRPO）通常采用**序列级（Sequence-level）**的奖励分配，即只要最终答案正确，整个响应序列的所有 token 都会获得相同的正向反馈。
*   **研究动机**：在多模态场景下，模型可能通过语言先验或捷径（Shortcuts）猜对答案，而忽视了视觉证据。
*   **核心目标**：建立一种**角色感知（Role-aware）的 token 级信用分配机制**，区分“感知（Perception）”和“推理（Reasoning）”两种不同功能的 token，确保模型既能看清图像，又能基于图像逻辑进行推理。

### 2. 方法论：SRPO 核心思想与技术细节
SRPO 在不改变原始奖励函数和不引入外部教师模型的前提下，通过**自蒸馏（Self-distilled）**的方式对 token 权重进行重分配：
*   **结构化响应分解**：要求模型生成包含 `<perception>`（提取视觉证据）和 `<reasoning>`（基于证据推导答案）两个阶段的响应。
*   **感知 token 的信用分配**：
    *   **视觉依赖性对比**：对比模型在“原始图像”和“掩码图像（随机遮盖 50% 补丁）”下生成相同感知 token 的对数似然差。差异越大，说明该 token 对视觉信息的依赖越强，信用越高。
*   **推理 token 的信用分配**：
    *   **接地一致性对比**：对比模型在“包含已生成的感知内容”和“不包含感知内容”两种上下文下生成推理 token 的似然差。这衡量了推理过程是否真正受益于之前的视觉感知。
*   **统一轨迹级调制**：
    *   将上述两种角色的原始得分在整条轨迹上进行均值中心化处理，并映射为正向的乘法权重 $w_{i,t}$（范围限制在 [0.8, 1.2]）。
    *   **优化目标**：在 GRPO 的代理损失函数中引入该权重，并加入一个轻量级的**响应 token 不确定性惩罚（熵正则项）**，以增强训练稳定性。

### 3. 实验设计
*   **数据集**：在 **ViRL39K**（包含 3.9 万个可验证的多模态推理问题）上进行训练。
*   **基准测试（Benchmarks）**：涵盖 9 个主流榜单，分为数学类（MathVista, MathVision, We-Math, MathVerse, DynaMath）和通用类（MMMU-Pro, MM-Vet, LogicVista, NaturalBench）。
*   **对比方法**：
    *   **基础模型**：Qwen2.5-VL-3B/7B。
    *   **强化学习基准**：GRPO, DAPO。
    *   **前沿多模态推理模型**：Vision-SR1, PAPO, PRCO, VPPO, Perception-R1 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练配置**：
    *   训练 2 个 Epoch。
    *   Rollout Batch Size 为 384，每条提示词采样 8 个响应。
    *   最大响应长度为 2048 tokens。
*   **框架**：基于 EasyR1 训练框架实现。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同参数规模（3B 和 7B）的模型上验证，涵盖了从数学到逻辑、从自然图像到图表的广泛场景。
*   **消融实验**：非常充分。分别验证了“感知信用”、“推理信用”、“统一调制机制”以及“熵正则系数 $\eta$”对结果的影响。
*   **鲁棒性分析**：通过 Pass@k 评估了模型在不同采样预算下的表现，并进行了视觉条件受损（如掩码、错配图像）下的诊断测试。
*   **客观性**：使用了 VLMEvalKit 进行标准化评估，并采用 GPT-4o-mini 作为部分主观题的裁判，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：SRPO 在所有基准测试中均显著优于原始 GRPO 和 DAPO。在 Qwen2.5-VL-7B 上，平均准确率从 47.07% 提升至 54.30%。
*   **视觉接地增强**：SRPO 有效减少了模型依赖语言捷径的现象，使模型生成的感知内容与视觉证据结合更紧密。
*   **训练稳定性**：引入的响应 token 不确定性惩罚有效缓解了 token 级重分配带来的训练波动。

### 7. 优点（亮点）
*   **无需外部依赖**：不需要额外的奖励模型（RM）或强大的教师模型（如 GPT-4V）进行标注，完全依靠模型自身的同策略对比实现。
*   **角色感知**：首次明确区分了多模态推理中感知与推理 token 的不同功能，并设计了针对性的信用分配逻辑。
*   **方向保持**：通过正向权重调制，在优化 token 贡献的同时，严格保留了原始可验证奖励引导的优化方向。

### 8. 不足与局限
*   **计算开销**：虽然不需要外部模型，但为了计算 token 权重，每个训练步需要进行多次额外的推理（Rescoring），增加了训练时的计算负担。
*   **格式依赖**：该方法高度依赖于模型生成结构化响应（`<perception>` 和 `<reasoning>` 标签）的能力，对于无法遵循此格式的模型可能失效。
*   **场景覆盖**：实验主要集中在短视频或静态图像的逻辑/数学推理，对于长视频理解或极大规模交互场景的适用性尚待验证。

（完）
