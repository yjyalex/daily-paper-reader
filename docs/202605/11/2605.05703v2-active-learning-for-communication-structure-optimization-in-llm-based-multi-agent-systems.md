---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v2"
tags: ["query:mas-gen"]
score: 10.0
evidence: 多智能体系统中的通信结构优化
tldr: 本研究针对大模型多智能体系统（LLM-MAS）通信结构优化中随机任务采样效率低且不稳定的问题，提出了一种基于主动学习的任务选择框架。该方法利用集成卡尔曼反演作为贝叶斯更新的近似，通过信息论指标识别对优化通信结构最有价值的任务。此外，结合嵌入表示选择和代理模型提升了系统的可扩展性。实验证明，该方法在有限预算下能显著提升系统性能并增强对智能体攻击的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体通信结构优化方法依赖随机采样，忽略了任务难度和领域差异导致的优化不稳定性和低效率问题。
method: 提出一种基于集成卡尔曼反演的信息论主动学习框架，通过评估候选任务对图参数分布的改变程度来筛选高价值任务。
result: 在良性环境和智能体攻击场景下的实验均验证了该方法在受限计算预算下优化通信结构的有效性。
conclusion: 通过主动识别关键任务，可以显著提高大模型多智能体系统通信结构优化的效率、稳定性和鲁棒性。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相同，这使得在有限训练预算下的优化往往不稳定，且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论的任务选择框架。该方法通过衡量候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（ensemble Kalman inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒且含有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型和批量汤普森采样相结合。我们在良性设置和存在智能体攻击的设置下验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

### 论文总结：基于大语言模型的多智能体系统通信结构优化的主动学习

#### 1. 核心问题与研究动机
*   **核心问题**：在基于大语言模型的多智能体系统（LLM-MAS）中，优化智能体间的通信结构（即谁在什么时候和谁说话）能显著提升性能并降低成本。然而，现有的优化方法通常依赖于**随机采样**训练任务。
*   **研究动机**：
    *   **任务差异性**：不同任务对优化通信结构的贡献（信息量）不同，随机采样在有限预算下会导致优化过程不稳定，且对训练集高度敏感。
    *   **高昂成本**：LLM-MAS 的运行涉及多次模型调用，在大规模任务池上进行全量训练或评估极其昂贵。
    *   **黑盒挑战**：MAS 的交互过程通常是随机、不可导且呈黑盒特性的，传统的基于梯度的任务选择方法难以直接应用。

#### 2. 核心方法论
论文提出了一个**基于集成的、信息论的主动学习框架**，旨在识别对通信结构参数更新贡献最大的任务。
*   **核心思想**：通过衡量候选任务引起的通信结构参数分布的“信息增益”（KL 散度）来评估任务价值。
*   **关键技术细节**：
    *   **集成卡尔曼反演（EKI）**：这是该方法的核心。它将参数分布表示为一组粒子（Ensemble），利用 EKI 作为贝叶斯更新的无导数近似，通过一次前向评估即可估计参数后验分布的变化，避免了复杂的端到端梯度计算。
    *   **两阶段选择策略**：
        1.  **代表性选择**：利用文本嵌入（Embedding）和贪婪最远点采样，从原始大池中筛选出具有多样性的中间候选池。
        2.  **信息量选择**：在中间池中，利用 EKI 计算任务的信息增益。
    *   **代理模型与批量采样**：为了进一步提升效率，引入了偏最小二乘法（PLS）降维和高斯过程（GP）代理模型，结合汤普森采样（Thompson Sampling）来预测未评估任务的信息增益，从而减少昂贵的 MAS 实际运行次数。

#### 3. 实验设计
*   **数据集**：
    *   **MMLU**：大规模多任务语言理解基准（涵盖多个学科）。
    *   **GSM8K**：小学数学应用题数据集（侧重逻辑推理）。
*   **实验场景**：
    *   **良性设置**：所有智能体均正常工作。
    *   **对抗设置（智能体攻击）**：系统中存在恶意智能体，故意提供错误推理和误导信息。
*   **对比方法（Baselines）**：
    *   **No Train**：不进行结构优化（全连接）。
    *   **Random Train**：随机选择任务进行结构优化。
    *   **EGL/EMC**：基于期望梯度长度或期望模型变化的传统主动学习方法。
    *   **Fisher Coreset**：基于费舍尔信息矩阵的协同集选择方法。

#### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4.1-nano** 作为智能体和决策者的底座模型。
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 数量）或总训练时长。由于主要基于 OpenAI 的 API 进行实验，其成本主要体现在 Token 使用量上。论文详细记录了不同阶段（选择、训练、测试）的 Token 消耗情况。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 MMLU 和 GSM8K 两个数据集进行了详尽测试。
    *   每个设置下进行了 **10 到 23 次独立运行**（使用不同随机种子），并提供了标准差和四分位数（Q1）分析。
    *   包含**消融实验**（验证代表性选择与信息量选择的各自贡献）和**灵敏度分析**（验证集成规模 $J$ 和 EKI 迭代步数的影响）。
*   **充分性评价**：实验设计较为充分，通过引入自助法（Bootstrap）置信区间增强了结果的可信度。对比了多种主动学习基准，证明了 EKI 方法在黑盒 MAS 环境下的优越性。

#### 6. 主要结论与发现
*   **性能提升**：在对抗设置下，主动学习显著优于随机训练。在 MMLU 上平均准确率提升 **+1.45%**，在 GSM8K 上提升 **+0.93%**。
*   **稳定性增强**：主动学习显著降低了优化结果的方差，特别是在“最差 25%”的任务表现上提升明显（MMLU 提升 **+1.30%**），有效防止了优化失败的情况。
*   **鲁棒性**：在存在恶意攻击时，该方法能更有效地识别并切断有害的通信路径。
*   **效率**：相比于简单增加随机训练任务的数量，主动学习在相同的 Token 预算下实现了更高的准确率。

#### 7. 优点与亮点
*   **无导数优化**：巧妙地将 EKI 引入主动学习，解决了 LLM-MAS 这种不可导黑盒系统的任务评估难题。
*   **鲁棒性关注**：不仅关注平均性能，还深入探讨了优化过程的稳定性和在对抗环境下的表现，这在 MAS 研究中具有重要现实意义。
*   **可扩展性设计**：通过代理模型和两阶段筛选，使得该方法能够处理大规模的任务池。

#### 8. 不足与局限
*   **集成规模限制**：虽然实验证明小规模集成（如 $J=6$）有效，但在参数维度极高或后验分布极度复杂时，EKI 的近似效果可能下降。
*   **标签依赖**：目前的方法假设候选池中的任务带有标准答案（用于计算性能得分），在完全无标签的场景下需要额外的验证步骤。
*   **计算开销**：虽然比全量训练省钱，但主动学习本身的选择过程（Selection Cost）仍需消耗一定量的 Token，主要适用于结构优化后会被多次复用的场景。

（完）
