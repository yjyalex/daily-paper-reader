---
title: "$f$-Divergence Regularized RLHF: Two Tales of Sampling and Unified Analyses"
title_zh: $f$-散度正则化的 RLHF：两种采样策略与统一分析
authors: "Di Wu, Chengshuai Shi, Jing Yang, Cong Shen"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06977v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 具有一般f-散度正则化目标的在线RLHF理论框架
tldr: RLHF通常使用KL散度正则化，但其他f-散度的理论研究尚不充分。本文提出了一个统一的理论框架，研究通用f-散度正则化的在线RLHF。作者提出了两种基于不同采样原理的算法：一种扩展了乐观原则，另一种利用了最优策略对奖励扰动的敏感性。理论证明这两种算法均能达到对数级遗憾和反比例级次优性间隙，为通用f-散度正则化下的RLHF提供了首个性能保证。
selection_source: fresh_fetch
motivation: 现有的RLHF研究主要局限于逆KL散度，缺乏对通用f-散度正则化在在线学习场景下的统一理论分析。
method: 提出了两种基于不同采样原理的算法，分别利用乐观探索奖励和最优策略对奖励扰动的敏感性。
result: 理论证明了所提算法在在线RLHF中可实现O(log T)的遗憾界和O(1/T)的次优性间隙。
conclusion: 本研究为通用f-散度正则化的RLHF建立了首个理论性能界限，证明了该框架在在线学习中的高效性。
---

## 摘要
基于人类反馈的强化学习 (RLHF) 已成为大语言模型后训练的基石技术。虽然大多数现有方法依赖于反向 KL 正则化，但最近的实证研究已开始探索将其他散度（如正向 KL、卡方散度）作为 RLHF 中的正则化项。然而，对于通用 $f$-散度正则化的统一理论理解仍有待深入探索。为了填补这一空白，本研究为具有通用 $f$-散度正则化目标的在线 RLHF 开发了一个全面的理论框架。我们没有单独处理每种可能的散度函数，而是从整个函数类的整体视角出发，提出了基于不同采样原理的两种算法。第一种算法通过精心设计的探索奖励扩展了经典的乐观原则，而第二种算法则引入了一种新方法，利用了 $f$-散度正则化下最优策略对奖励扰动的敏感性。理论分析表明，可以实现 $O(\log T)$ 的累积遗憾和 $O(1/T)$ 的次优性间隙，证明了这两种算法的效率，并且据我们所知，这是首次为通用 $f$-散度正则化下的在线 RLHF 建立性能界限。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) has become a cornerstone technique for post-training large language models. While most existing approaches rely on the reverse KL-regularization, recent empirical studies have begun exploring alternative divergences (e.g., forward KL, chi-squared) as regularizers in RLHF. However, a unified theoretical understanding of general $f$-divergence regularization remains under-explored. To fill this gap, this work develops a comprehensive theoretical framework for online RLHF with a general $f$-divergence regularized objective. Rather than treating each possible divergence function individually, we adopt a holistic perspective across the entire function class and propose two algorithms based on distinct sampling principles. The first extends the classical optimism principle with a carefully designed exploration bonus, while the second introduces a new method that exploits the sensitivity of the optimal policy to reward perturbations under $f$-divergence regularization. Theoretical analysis shows that $O(\log T)$ regret and $O(1/T)$ sub-optimality gap are achievable, establishing provable efficiency of both algorithms and, to the best of our knowledge, the first performance bounds for online RLHF under general $f$-divergence regularization.

---

## 论文详细总结（自动生成）

这是一份关于论文《f-Divergence Regularized RLHF: Two Tales of Sampling and Unified Analyses》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的 RLHF（基于人类反馈的强化学习）主要依赖于**逆 KL 散度**作为正则化项。然而，KL 正则化在处理稀疏离线数据时容易出现“过优化”问题，导致模型在奖励模型误差较大的区域过度拟合。
*   **核心问题**：虽然实证研究表明其他 $f$-散度（如正向 KL、卡方散度等）能提高鲁棒性，但目前缺乏一个**统一的理论框架**来理解通用 $f$-散度正则化在**在线 RLHF** 设置下的表现。
*   **整体含义**：本文旨在填补这一理论空白，为通用 $f$-散度正则化的在线 RLHF 提供首个性能保证（遗憾界和次优性间隙）。

### 2. 方法论：核心思想与关键技术
论文提出了两种基于不同采样原理的在线学习算法：
*   **最优策略的闭式解**：推导出在通用 $f$-散度下，最优策略 $\pi_r$ 与参考策略 $\pi_0$ 的关系为 $\pi_r(a|x) = \pi_0(a|x) f'^{-1}(\eta(r(x,a) - \lambda_r(x)))$，其中 $f'^{-1}$ 是 $f$ 导数的逆函数。
*   **算法 1：基于乐观原则的探索 (Optimism-based)**
    *   **核心思想**：扩展“在不确定性面前保持乐观”的原则。
    *   **技术细节**：通过最大似然估计（MLE）更新奖励模型，并构造一个基于 Eluder 维度的置信区间奖励项（Bonus）。将此乐观奖励加到估计奖励上，诱导模型探索不确定性高的区域。
*   **算法 2：基于导数的探索 (Derivative-based)**
    *   **核心思想**：提出“**导数即不确定性**”的新颖见解。发现最优策略对奖励的敏感度取决于 $f$ 导数的逆函数的稳定性。
    *   **技术细节**：设计了一种混合采样策略，包含三个部分：基础探索策略 $\pi'$（基于 $f$ 的二阶导数）、正向扰动策略 $\pi^+$ 和负向扰动策略 $\pi^-$。该方法无需显式求解复杂的置信区间，计算效率更高。

### 3. 实验设计
*   **实验场景**：模拟线性上下文多臂老虎机（Contextual Bandits）场景。
*   **参数设置**：特征维度为 25，固定动作空间大小为 10，奖励函数基于 Bradley-Terry (BT) 模型。
*   **Benchmark 与对比方法**：
    *   **对比方法**：贪婪采样 (Greedy Sampling)、均匀采样 (Uniform Sampling) 以及本文提出的两种算法。
    *   **正则化项选择**：测试了卡方混合 KL 散度 ($f = (x-1)^2 + x \log x$) 和 $f = x \log x - \log x$。
*   **评估指标**：单步遗憾 (Single-step regret) 和累积遗憾 (Cumulative regret)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练时长。
*   **原因分析**：由于本文侧重于理论证明和基础模拟实验，其计算需求主要集中在数值求解 $\lambda$ 参数和 MLE 优化上，通常在标准 CPU 或单块 GPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组模拟实验，涵盖了不同的 $f$-散度函数，并对每种算法在不同迭代次数下的收敛性进行了可视化。
*   **充分性评价**：
    *   **理论验证充分**：实验结果与理论推导的 $O(\log T)$ 遗憾界高度吻合。
    *   **对比公平**：在相同的线性特征和偏好模型下对比了主流采样策略。
    *   **局限性**：实验主要集中在合成数据和线性模型上，缺乏在大规模真实语言模型（LLM）上的微调实验。

### 6. 主要结论与发现
*   **理论保证**：证明了基于乐观原则的算法可达到 $O(\log T)$ 的累积遗憾；基于导数的算法可达到 $O(1/T)$ 的次优性间隙。
*   **统一框架**：这是首个为通用 $f$-散度在线 RLHF 提供可证明效率的研究。
*   **散度影响**：发现特定的 $f$-散度（如卡方混合 KL）在理论常数项上优于标准 KL 散度，实验也证实了这些散度具有更小的次优性间隙。
*   **采样策略**：基于导数的采样策略在实验中表现优于传统的乐观原则，且计算开销更小。

### 7. 优点
*   **理论深度**：将 RLHF 的分析从特定的 KL 散度推广到了通用的 $f$-散度类，具有极强的普适性。
*   **创新视角**：首次提出将 $f$ 的导数解释为不确定性信号，为强化学习的探索机制提供了新的数学工具。
*   **计算效率**：算法 2 避免了求解复杂的置信区间优化问题，更具工程落地潜力。

### 8. 不足与局限
*   **场景限制**：目前的研究主要集中在单步决策（上下文老虎机），尚未完全扩展到多步决策的复杂强化学习场景。
*   **假设依赖**：理论分析依赖于奖励函数的实现性假设（Realizability）和有限的奖励函数类。
*   **实战缺失**：论文缺乏在真实 LLM 任务（如摘要生成、对话对齐）上的大规模实证，其在非线性奖励模型（如深度神经网络）下的表现仍需验证。

（完）
