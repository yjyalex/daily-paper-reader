---
title: "Adaptive Negative Reinforcement for LLM Reasoning:Dynamically Balancing Correction and Diversity in RLVR"
title_zh: 大语言模型推理的自适应负强化：在 RLVR 中动态平衡纠错与多样性
authors: "Yash Ingle, Jaival Chauhan, Ankit Yadav, Sudhakar Mishra"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07137v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对大语言模型推理和可验证奖励强化学习的自适应负强化
tldr: 本研究针对大语言模型推理中的负样本强化（NSR）方法进行了改进。针对现有方法固定惩罚力度且忽视错误差异的问题，提出了自适应负样本强化（A-NSR）和置信度加权负强化（CW-NSR）。A-NSR 通过时间调度动态调整惩罚权重以稳定训练，而 CW-NSR 根据模型置信度对错误路径进行差异化惩罚。实验证明，该方法在 MATH 和 AIME 等复杂推理任务上显著提升了模型性能并有效防止过拟合。
selection_source: fresh_fetch
motivation: 现有的负样本强化技术采用固定惩罚权重且未区分错误类型，导致训练初期不稳定且容易抑制模型的探索多样性。
method: 引入了随训练阶段动态调整惩罚力度的 A-NSR，以及根据模型输出概率对高置信度错误施加更大惩罚的 CW-NSR。
result: 在 MATH、AIME 2025 和 AMC23 等高难度推理数据集上的实验表明，该方法在 Qwen2.5 架构下取得了优异的推理表现。
conclusion: 通过动态平衡纠错与多样性，自适应负强化机制为提升大语言模型的逻辑推理能力提供了一种高效且稳健的方案。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 已成为提升大语言模型 (LLM) 推理能力的极有效方法。最近的研究表明，负样本强化 (NSR)——其侧重于惩罚错误步骤而非仅仅奖励正确步骤——在整个 Pass@k 谱系上的表现可以媲美甚至超越 PPO 和 GRPO 等更复杂的框架。然而，目前的 NSR 技术通常在整个训练过程中应用固定惩罚，并对每个错误响应赋予相同的权重。为了解决这些局限性，我们提出了 NSR 框架的两个扩展：自适应负样本强化 (A-NSR)。A-NSR 不使用固定的更新规则，而是使用随时间变化的调度函数。在初始训练阶段，系统重点关注纠正错误以稳定模型；随着训练的进行，它转向更细微且受控的更新。我们还引入了置信度加权负强化 (CW-NSR)，其运行原理是不同的错误具有不同的重要程度。CW-NSR 根据模型的归一化序列似然分配特定的惩罚权重。如果模型对错误路径高度自信，它将受到更大的惩罚；而对于不确定的错误（即模型实际上正在进行探索），惩罚则较轻。我们的形式化分析展示了这些机制如何控制 Token 级更新，使模型能够利用先验引导的概率重分布，同时提供一种天然的过拟合防御机制。我们在 MATH、AIME 2025 和 AMC23 等困难推理数据集上评估了这些方法，使用的是 Qwen2.5-Math-1.5B 架构。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a highly effective method for improving the reasoning abilities of Large Language Models (LLMs). Recent research shows that Negative Sample Reinforcement (NSR) -- which focuses on penalizing incorrect steps rather than simply rewarding correct ones -- can match or even exceed the performance of more complex frameworks like PPO and GRPO across the entire Pass@k spectrum. However, current NSR techniques usually apply a fixed penalty throughout the training process and treat every incorrect response with the same weight.   To address these limitations, we propose two extensions to the NSR framework: Adaptive Negative Sample Reinforcement. Rather than using a fixed update rule, A-NSR uses time-dependent scheduling functions. In the initial training phases, the system focuses heavily on correcting errors to stabilize the model. As training continues, it shifts toward more subtle and controlled updates. We also introduce Confidence-Weighted Negative Reinforcement, which operates on the principle that different mistakes carry different levels of importance. CW-NSR assigns specific penalty weights based on the model's normalized sequence likelihood. If the model is highly confident in a wrong path, it receives a larger penalty and for uncertain errors -- where the model is effectively exploring -- are penalized less strictly. Our formal analysis shows how these mechanisms govern token-level updates, allowing the model to leverage prior-guided probability redistribution while providing a natural defense against overfitting. We evaluated these methods on difficult reasoning datasets, including MATH, AIME 2025, and AMC23, using the Qwen2.5-Math-1.5B architecture.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在推理任务中的强化学习优化提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在具有可验证奖励的强化学习（RLVR）中，**负样本强化（NSR）**（即惩罚错误答案）被证明比单纯奖励正确答案更有效。然而，现有的 NSR 方法（如 W-REINFORCE）通常采用**固定的惩罚权重**，且对所有错误样本“一视同仁”。
*   **研究动机**：作者认为这种固定方式忽略了两个关键维度：
    1.  **时间维度**：模型在训练初期的错误较多，需要强力纠错；后期准确率提高，过度惩罚会抑制推理的多样性。
    2.  **样本维度**：模型“自信地犯错”（高置信度错误）通常反映了系统性逻辑缺陷，应重罚；而“不确定的猜测错误”可能只是探索过程中的随机波动，应轻罚。

### 2. 论文提出的方法论
论文提出了两个独立且互补的扩展框架：

*   **自适应负样本强化 (A-NSR)**：
    *   **核心思想**：引入随训练步数 $t$ 变化的调度函数 $\lambda(t)$（正样本权重）和 $\beta(t)$（负样本权重）。
    *   **技术细节**：设计了三种调度方案：
        *   **指数衰减/线性增加**：负样本惩罚随时间指数衰减，正样本奖励线性增加，实现从“强力纠错”到“平衡微调”的过渡。
        *   **余弦退火**：平滑调整权重。
        *   **性能驱动**：根据当前 Batch 的准确率动态调整惩罚力度。
*   **置信度加权负强化 (CW-NSR)**：
    *   **核心思想**：根据模型生成错误序列时的置信度（Confidence）来分配惩罚权重。
    *   **技术细节**：
        *   使用序列 Token 概率的几何平均值作为**置信度分数**（Conf），该分数与序列长度无关。
        *   定义**硬度权重** $w(y) = \max(\epsilon, \text{Conf}(y)^\alpha)$。当模型对错误路径越自信，惩罚系数越大。
        *   通过梯度分析证明，该方法在惩罚错误的同时，能更好地保留模型已有的先验知识。

### 3. 实验设计
*   **基础模型**：Qwen2.5-Math-1.5B。
*   **数据集**：MATH（训练与测试）、AIME 2025、AMC23。
*   **Benchmark（基准）**：主要对比 **W-REINFORCE**（由 Zhu et al. 2025 提出，是目前 NSR 领域的 SOTA 方法，优于 PPO 和 GRPO）。
*   **评估指标**：Pass@k（k 范围从 1 到 256），用于衡量模型在不同采样预算下的推理准确率和多样性。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA A100 Tensor Dual GPU (80 GB VRAM)。
*   **时长**：单次实验执行时间约为 50 到 60 小时。
*   **超参数**：学习率 1e-6，Batch Size 1024，训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验组数**：在三个主流数学竞赛数据集上进行了完整测试，并绘制了详细的 Pass@k 曲线。
*   **充分性评价**：
    *   **优点**：对比了不同难度的基准（从基础 MATH 到极难的 AIME 2025），实验覆盖了从低采样（Pass@1）到高采样（Pass@256）的全谱系，能够客观反映模型在准确性与多样性之间的平衡。
    *   **局限**：由于算力限制，A-NSR 在 MATH 数据集上的 Pass@256 评估未完成（因涉及 2250 万个 Token 的推理，耗时过长）；且实验仅限于 1.5B 小参数模型，未在更大规模模型（如 7B 或 72B）上验证。

### 6. 主要结论与发现
*   **A-NSR 的优势**：在低采样预算（低 k 值）下表现优异，尤其在 AMC23 数据集上，A-NSR 在所有 Pass@k 级别上均显著超过了 W-REINFORCE。
*   **CW-NSR 的优势**：在极高难度的 AIME 2025 上表现突出，证明了针对“自信错误”进行惩罚能有效提升模型处理复杂逻辑的能力。
*   **整体提升**：动态调整负强化信号比固定权重更能平衡模型的纠错能力与输出多样性，有效防止了模型在强化学习过程中的过拟合。

### 7. 优点
*   **理论支撑扎实**：提供了详细的 Token 级梯度动力学分析，证明了新方法如何保持 NSR 的优良属性（如先验引导的概率重分布）。
*   **即插即用**：方法不改变优化算法本身（如 PPO/GRPO），而是通过修改奖励结构实现，易于集成到现有框架。
*   **关注样本差异**：首次在 RLVR 中引入了基于置信度的样本级加权，符合人类学习“反思深刻错误”的直觉。

### 8. 不足与局限
*   **长程稳定性**：论文提到在极长时间的训练中，自适应调度可能带来潜在的不稳定性，需进一步研究。
*   **奖励稀疏性限制**：目前仅适用于可验证的稀疏奖励（对/错），难以直接应用于过程监控（Process-based）或密集奖励场景。
*   **算力约束下的验证不足**：未能在更大参数规模的模型上进行消融实验，且部分数据集的高阶评估因耗时问题缺失。

（完）
