---
title: "DGPO: Distribution Guided Policy Optimization for Fine Grained Credit Assignment"
title_zh: DGPO：用于细粒度信用分配的分布引导策略优化
authors: "Hongbo Jin, Rongpeng Zhu, Zhongjing Du, Xu Jiang, Jingqi Tian, Qiaoman Zhang, Jiayu Ding"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03327v2"
tags: ["query:token"]
score: 10.0
evidence: 思维链推理步骤的细粒度信用分配
tldr: DGPO是一种新型无评论家强化学习框架，旨在解决大语言模型在复杂推理任务中信用分配粗糙和KL散度导致的不稳定性问题。它通过引入有界的Hellinger距离替代KL散度，并结合熵门控机制，将序列级奖励动态分配至各个Token，实现了细粒度的信用分配。在AIME等挑战性基准测试中，DGPO显著提升了模型的推理性能，达到了新的SOTA水平。
selection_source: fresh_fetch
motivation: 现有的强化学习算法在长链条推理中存在序列级信用分配粗糙以及KL散度引发的梯度不稳定和探索受限问题。
method: 提出分布引导策略优化（DGPO），利用有界Hellinger距离量化Token级探索，并通过熵门控机制根据策略的不确定性动态重新分配序列级优势。
result: "在Qwen2.5-32B架构上，DGPO在AIME2024和AIME2025基准测试中分别取得了60.0%和46.0%的准确率，大幅超越了DAPO等竞争基准。"
conclusion: DGPO通过细粒度的信用分配和稳定的分布引导，在无需额外价值网络的情况下显著增强了大语言模型的复杂推理能力。
---

## 摘要
强化学习对于对齐大语言模型以执行复杂推理任务至关重要。然而，现有的算法（如组相对策略优化，GRPO）受限于粗粒度的序列级信用分配，难以在长思维链（Chain of Thought）生成中分离出关键的推理步骤。此外，标准的无界 Kullback-Leibler (KL) 散度惩罚会导致严重的梯度不稳定和模式寻求保守性，最终抑制了对新颖推理轨迹的探索。为了克服这些局限性，我们提出了分布引导策略优化（DGPO），这是一种新型的无 Critic 强化学习框架，它将分布偏差重新解释为引导信号，而非僵化的惩罚。DGPO 使用有界的 Hellinger 距离取代了不稳定的 KL 散度，从而在没有梯度爆炸风险的情况下安全地量化 Token 级的探索。为了有效区分真实的推理突破与幻觉噪声，我们提出了一种熵门控机制，根据策略的认知不确定性来缩放这种偏差。通过根据这些门控分数将粗粒度的序列级优势动态地重新分配给单个 Token，DGPO 极大地激励了关键的探索性步骤，同时抑制了无根据的低熵偏差。因此，DGPO 完全消除了传统的 Token 级 KL 惩罚，并在没有额外价值网络计算开销的情况下实现了细粒度的信用重新分配。广泛的实证评估表明，DGPO 在无 Critic 对齐方面达到了新的 SOTA 水平。值得注意的是，在 Qwen2.5-32B 架构上，DGPO 在具有挑战性的 AIME2024 和 AIME2025 基准测试中分别实现了 60.0% 和 46.0% 的 Avg@32 准确率，显著优于 DAPO 等竞争基线。

## Abstract
Reinforcement learning is crucial for aligning large language models to perform complex reasoning tasks. However, current algorithms such as Group Relative Policy Optimization suffer from coarse grained, sequence level credit assignment, which severely struggles to isolate pivotal reasoning steps within long Chain of Thought generations. Furthermore, the standard unbounded Kullback Leibler divergence penalty induces severe gradient instability and mode seeking conservatism, ultimately stifling the discovery of novel reasoning trajectories. To overcome these limitations, we introduce Distribution Guided Policy Optimization, a novel critic free reinforcement learning framework that reinterprets distribution deviation as a guiding signal rather than a rigid penalty. DGPO replaces the volatile KL divergence with the bounded Hellinger distance to safely quantify token level exploration without the risk of gradient explosion. To effectively distinguish genuine reasoning breakthroughs from hallucinatory noise, we propose an entropy gating mechanism that scales this deviation by the policy`s epistemic uncertainty. By dynamically redistributing the coarse sequence-level advantage to individual tokens based on these gated scores, DGPO heavily incentivizes critical exploratory steps while suppressing unwarranted, low-entropy deviations. Consequently, DGPO completely eliminates the traditional token-level KL penalty and achieves fine-grained credit reallocation without the computational overhead of an additional value network. Extensive empirical evaluations demonstrate that DGPO sets a new state-of-the-art for critic free alignment. Notably, on the Qwen2.5-32B architecture, DGPO achieves 60.0% Avg@32 accuracy and 46.0% Avg@32 accuracy on the challenging AIME2024 and AIME2025 benchmarks respectively, substantially outperforming competitive baselines like DAPO.

---

## 论文详细总结（自动生成）

这是一份关于论文《DGPO: Distribution-Guided Policy Optimization for Fine-Grained Credit Assignment》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在复杂推理任务（如数学竞赛）中对齐的两个核心痛点：
*   **粗粒度信用分配问题**：现有的无 Critic 强化学习框架（如 GRPO）将单一的序列级奖励均匀分配给成千上万个 Token，导致模型无法区分关键推理步骤与冗余的过渡性词汇。
*   **KL 散度的局限性**：传统的无界 KL 散度惩罚过于保守，会严厉惩罚任何偏离参考模型的行为。这不仅导致梯度不稳定（梯度爆炸风险），还抑制了模型探索新颖推理路径的能力。

**DGPO 的核心含义**是将分布偏差从“惩罚项”转变为“引导信号”，通过细粒度的 Token 级信用重新分配来释放模型的深度推理潜力。

### 2. 方法论：核心思想与技术细节
DGPO 提出了一种无需额外价值网络（Value Network）的细粒度信用分配方案：
*   **有界 Hellinger 距离**：取代无界的 KL 散度。Hellinger 距离的值域在 $[0, 1]$ 之间，能够安全地量化 Token 级的探索程度，避免了梯度爆炸，同时鼓励探索。
*   **策略熵门控机制（Entropy Gating）**：为了防止模型因“自信的幻觉”而获得奖励，DGPO 将 Hellinger 距离与 Token 的香农熵（Shannon Entropy）结合。只有当模型在不确定性较高（高熵）且偏离参考分布时，才被视为有效的探索。
*   **优势动态重新分配**：
    1.  计算每个 Token 的得分 $s_{i,t}$（Hellinger 距离与熵的乘积）。
    2.  通过温度缩放的 Softmax 函数将序列级优势 $A_i$ 转化为 Token 级权重 $w_{i,t}$。
    3.  确保序列内权重均值为 1，从而在不改变总梯度量级的情况下，将更多“信用”分配给关键的探索性 Token。
*   **目标函数**：完全消除了显式的 Token 级 KL 惩罚项，将其约束隐含在优势分配逻辑中。

### 3. 实验设计
*   **数据集**：使用公开的 **DAPO-17K** 数据集进行训练。
*   **基准测试（Benchmarks）**：极具挑战性的数学竞赛题目 **AIME 2024** 和 **AIME 2025**。
*   **对比方法**：
    *   **GRPO**：标准的组相对策略优化。
    *   **DAPO**：目前最先进的无 Critic 对齐基线之一。
    *   **FIPO**：另一种改进的策略优化方法。
*   **模型架构**：Qwen2.5-32B-Base（主要）和 Qwen2.5-7B-Math（用于消融实验）。

### 4. 资源与算力
*   **硬件环境**：使用了 2 个节点，每个节点配备 **8 张 NVIDIA H20 (96GB) GPU**。
*   **软件框架**：使用 DeepSpeed ZeRO-3 进行分布式训练。
*   **训练效率**：DGPO 的训练吞吐量为 188 Tokens/s/GPU。相比 vanilla GRPO，其时间开销仅增加 **3.6%**，而内存占用几乎持平（约 46.5 GB/GPU），远低于需要 Critic 网络的 PPO（72.4 GB/GPU）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 不同参数规模的模型，验证了算法的可扩展性。
*   **消融实验**：系统地验证了“熵门控机制”和“Hellinger 距离”的必要性，证明了缺少任何一项都会导致性能显著下降。
*   **超参数敏感性分析**：对重新分配温度 $\tau$ 和熵门控因子 $\kappa$ 进行了详细测试。
*   **评价指标**：采用了 Avg@32（32 次采样的平均准确率）、Cons@32（多数投票）和 Pass@32 等多维度指标，实验设计客观且充分。

### 6. 主要结论与发现
*   **性能突破**：在 Qwen2.5-32B 上，DGPO 在 AIME 2024 上达到 **60.0%** 的 Avg@32 准确率，在 AIME 2025 上达到 **46.0%**，显著超越了 DAPO。
*   **收敛更快**：细粒度的信用分配使得模型能够更有效地从奖励信号中学习，收敛速度优于序列级方法。
*   **稳定性**：Hellinger 距离成功解决了长序列生成中的梯度不稳定问题。
*   **定性发现**：可视化分析证实，DGPO 能够自动识别并强化数学推导中的关键步骤（如关键代换），而降低对语法过渡词的关注。

### 7. 优点
*   **高效性**：在不增加额外参数（无需 Value Network 或 Reward Model 实时推理）的情况下实现了过程级（Process-level）的监督效果。
*   **理论严谨**：通过数学证明展示了 Hellinger 距离在处理概率趋近于 0 时的梯度界限优越性。
*   **即插即用**：可以轻松集成到现有的 GRPO 框架中，计算开销极小。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在数学推理任务上，尚未在代码生成、创意写作或通用指令遵循等领域进行广泛验证。
*   **超参数依赖**：虽然实验给出了推荐范围，但在不同任务或模型架构下，可能仍需微调温度系数 $\tau$ 和熵门控因子 $\kappa$ 以达到最佳平衡。
*   **幻觉风险**：尽管有熵门控，但在极端情况下，模型仍可能对某些错误的“新颖”路径产生过高的信用分配。

（完）
