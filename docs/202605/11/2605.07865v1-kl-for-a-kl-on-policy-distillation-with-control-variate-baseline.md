---
title: "KL for a KL: On-Policy Distillation with Control Variate Baseline"
title_zh: KL 换 KL：带有控制变量基准的同策蒸馏
authors: "Minjae Oh, Sangjun Song, Gyubin Choi, Yunho Choi, Yohan Jo"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07865v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 推理领域中作为策略梯度强化学习的在线蒸馏
tldr: 针对大语言模型在推理领域的在线蒸馏（OPD）训练不稳定的问题，本文提出了vOPD方法。该方法将OPD视为策略梯度强化学习，并引入控制变量基准（Value Function）来降低梯度方差。通过将学生与教师模型间的逐标记负向逆KL散度作为闭式解基准，vOPD在不增加额外模型或推理开销的前提下，实现了高效且稳定的训练，在数学和科学推理任务上表现优异。
selection_source: fresh_fetch
motivation: 在线蒸馏（OPD）因单样本蒙特卡洛估计的高方差导致训练不稳定，且现有稳定化方法计算开销大或存在偏差。
method: 提出vOPD，将OPD建模为强化学习，利用逐标记负向逆KL散度作为控制变量基准来减少梯度方差，且无需额外评论家模型。
result: 在数学和科学推理基准测试中，vOPD一致优于原始OPD，并达到了与高开销全词表基准相当的性能。
conclusion: vOPD通过原理性的强化学习方差缩减技术，为在线蒸馏提供了一种既高效又稳定的优化方案。
---

## 摘要
同策蒸馏（OPD）已成为大语言模型（尤其是推理领域）的一种主流后训练范式。然而，由于其单样本蒙特卡洛估计器的高梯度方差，OPD 在实践中仍然不稳定，且稳定训练的方法尚不成熟。我们提出了 vOPD（带有控制变量基准的同策蒸馏），它将 OPD 视为策略梯度强化学习（RL），并通过引入 RL 文献中典型的控制变量基准（通常是价值函数）来使其稳定。我们证明了 OPD 价值函数具有闭式解，即学生模型与教师模型之间逐 token 的负向逆 KL 散度，这可以直接从已计算的前向传播中获得，无需额外的评论家（critic）网络或推理。现有的稳定方法要么计算整个词表上的完整 token 级逆 KL，这增加了显著的开销；要么将其限制在 top-k 支撑集内，从而使目标函数产生偏差。vOPD 相比之下保留了轻量级的单样本估计器，将价值函数作为分离的基准减去，在降低方差的同时保持梯度无偏。此外，我们表明基准的 top-k 近似可以进一步降低成本而不损害性能。在数学和科学推理基准测试中，vOPD 一致优于原始 OPD，并达到了最昂贵的词表全量基准的水平，通过基于原理的 RL 方差缩减技术为同策蒸馏提供了一种高效的稳定方案。

## Abstract
On-Policy Distillation (OPD) has emerged as a dominant post-training paradigm for large language models, especially for reasoning domains. However, OPD remains unstable in practice due to the high gradient variance of its single-sample Monte Carlo estimator, and recipes for stable training are still immature. We propose vOPD (On-Policy Distillation with a control variate baseline), which casts OPD as policy-gradient RL and stabilizes it by introducing a control variate baseline-canonically a value function -- from the RL literature. We show that the OPD value function admits a closed form as the per-token negative reverse KL divergence between the student and the teacher, available directly from the already-computed forward pass with no additional critic or inference. Existing stabilization methods either compute the full token-level reverse KL over the entire vocabulary, adding significant overhead, or restrict it to a top-k support, biasing the objective. vOPD instead preserves the lightweight single-sample estimator, subtracting the value function as a detached baseline to keep the gradient unbiased while reducing variance. Furthermore, we show that a top-k approximation of the baseline further lowers cost without compromising performance. Across mathematical and scientific reasoning benchmarks, vOPD consistently outperforms vanilla OPD and matches the most expensive full-vocabulary baseline, offering an efficient stabilization of On-Policy Distillation through principled RL variance reduction.

---

## 论文详细总结（自动生成）

以下是对论文《KL for a KL: On-Policy Distillation with Control Variate Baseline》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在提升大语言模型（LLM）推理能力时，同策蒸馏（On-Policy Distillation, OPD）因其能提供密集的标记级（token-level）信号，比稀疏奖励的强化学习（RLVR）训练更快。
*   **核心问题**：OPD 在实践中极不稳定。其主流实现采用单样本蒙特卡洛估计，导致梯度方差极高。现有的稳定化方案要么计算开销巨大（全词表计算），要么会引入梯度偏差（Top-k 截断目标函数）。
*   **研究动机**：寻找一种既能保持梯度无偏、计算开销低，又能有效降低方差的 OPD 稳定化方法。

### 2. 方法论
*   **核心思想**：将 OPD 重新建模为“策略梯度强化学习”，并引入强化学习中经典的**控制变量基准（Control Variate Baseline）**——即价值函数（Value Function）来削减方差。
*   **关键技术细节**：
    *   **闭式解基准**：论文证明了对于 OPD 任务，其最优价值函数具有闭式解，即学生模型与教师模型之间逐标记的**负向逆 KL 散度**。
    *   **vOPD 算法**：在计算梯度时，从原始奖励（$\log \pi_T - \log \pi_\theta$）中减去上述 KL 基准。由于该基准与当前采样的具体标记无关，因此它是一个**无偏估计器**。
    *   **Top-k 近似**：为了进一步提升效率，可以使用 Top-k 标记来近似计算 KL 基准。不同于以往改变目标的 Top-k 方法，vOPD 仅将其作为分离的基准（detached baseline），因此即使 $k$ 很小，梯度依然保持无偏。
    *   **无需额外开销**：该基准直接从前向传播的概率分布中得出，无需训练额外的 Critic 模型，也不增加反向传播的标记数量。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：MATH500, Minerva Math, AMC23, AIME24/25。
    *   **科学推理**：SciKnowEval (化学), GPQA-Diamond。
*   **对比方法（Baselines）**：
    *   **Vanilla OPD**：标准的单样本蒙特卡洛 OPD。
    *   **OPD full-V**：全词表 OPD（计算量最大，理论方差最低）。
    *   **OPD top-k**：限制在 Top-k 支撑集上的 OPD（有偏差）。
*   **实验模型**：涵盖 Qwen3 系列（0.6B, 1.7B, 4B）和 Olmo-3-7B，测试了同尺寸蒸馏、尺寸错配蒸馏等多种组合。

### 4. 资源与算力
*   **硬件使用**：使用了 NVIDIA **H200 NVL** 或 **A100 NVL** GPU。
*   **训练时长**：根据模型大小（1.7B 到 7B），单次训练任务耗时在 **2 到 6 小时**之间。
*   **效率提升**：在 4B 模型规模下，vOPD top-k 的运行速度比 OPD full-V 快约 **57.7%**，且性能持平。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种模型配置、6 个主流推理基准上进行了测试，实验设计较为全面。
*   **充分性分析**：
    *   **消融实验**：对 Top-k 的超参数 $k$ 进行了详细消融（$k=5$ 到 $100$），证明了方法的鲁棒性。
    *   **深度分析**：提供了梯度范数（Gradient Norm）随训练变化的曲线，以及奖励与优势（Advantage）分布的对比图，从理论和实证两方面解释了方差缩减的原理。
    *   **客观性**：对比了不同家族（Qwen, Olmo）的模型，证明了方法的通用性。

### 6. 主要结论与发现
*   **性能卓越**：vOPD 在所有推理任务上一致优于原始 OPD，平均准确率提升高达 3% 以上，在 MATH500 上最高提升 6.2%。
*   **稳定性极佳**：vOPD 的梯度范数比原始 OPD 低 1-2 个数量级，有效抑制了训练中的不稳定性。
*   **高效无偏**：vOPD top-k 仅需极小的计算增量即可达到全词表 OPD 的效果，且由于其无偏特性，显著优于传统的 Top-k 截断方法。
*   **正则化效应**：vOPD 能够自动识别并抑制那些学生与教师分布严重不匹配的“破坏性标记”。

### 7. 优点
*   **理论优雅**：巧妙地利用了 OPD 目标函数的数学特性，推导出无需额外训练的闭式价值函数。
*   **即插即用**：不需要复杂的 Critic 网络架构，易于集成到现有的 RL 训练框架（如 TRL）中。
*   **高性价比**：在不增加反向传播负担的前提下，实现了与最昂贵方法相当的性能。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅验证到 7B 参数模型，尚未在超大规模模型（如 70B+）上验证。
*   **教师依赖**：作为蒸馏方法，其上限仍受限于教师模型的能力。
*   **实现细节**：虽然理论上更高效，但实际墙钟时间（Wall-clock time）高度依赖于推理引擎（如 vLLM）和框架的优化程度。
*   **目标局限**：目前主要针对标记级 KL 优化，尚未探讨序列级（sequence-level）KL 目标的扩展。

（完）
