---
title: "Rubric-Grounded RL: Structured Judge Rewards for Generalizable Reasoning"
title_zh: 基于评分量表的强化学习：用于可泛化推理的结构化裁判奖励
authors: "Manish Bhattarai, Ismael Boureima, Nishath Rajiv Ranasinghe, Scott Pakin, Dan O'Malley"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08061v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用结构化评判奖励进行强化学习，以提升LLM的泛化推理能力
tldr: 本研究针对强化学习中二元或整体评分奖励信号不足的问题，提出了“基于量规的强化学习（Rubric-Grounded RL）”框架。该方法将奖励分解为多个加权且可验证的准则，利用冻结的LLM裁判根据科学文献背景对模型输出进行多维度评分。通过在Llama-3.1-8B上应用GRPO算法，模型不仅在特定量规任务上表现优异，还在GSM8K、MATH和GPQA等通用推理基准上实现了显著的性能提升，证明了结构化奖励对增强模型泛化推理能力的有效性。
selection_source: fresh_fetch
motivation: 传统的二元或整体评分奖励机制缺乏细粒度的优化信号，难以有效引导模型学习复杂的推理逻辑。
method: 提出一种基于量规的强化学习框架，利用LLM裁判根据从科学文献中提取的多维度、结构化标准为模型提供部分信用奖励。
result: "实验表明，该方法在留存量规评估中获得71.7%的奖励，并在GSM8K、MATH及GPQA等多个外部推理基准上超越了基础模型。"
conclusion: 结构化且基于文档的奖励机制能够有效提升模型的任务表现，并诱导出可迁移至其他领域的通用推理能力。
---

## 摘要
我们认为，将奖励分解为加权的、可验证的标准，并使用大语言模型（LLM）裁判对其进行评分，可以提供一种部分得分的优化信号：每个回答不再是二元结果或单一的整体评分，而是根据多个特定任务的标准进行评分。我们形式化了“基于评分量表的强化学习（RL）”：这是一个框架，其中策略针对由冻结的 LLM 裁判生成的结构化、多标准奖励进行优化，该裁判以策略从未见过的辅助背景信息为条件。我们通过从科学技术信息办公室（OSTI）衍生的约 10 万份科技文档语料库中提取评分量表，并使用群体相对策略优化（GRPO）训练 Llama-3.1-8B-Instruct，从而实例化了该框架。通过基于 GRPO 的训练，该模型在留出评分量表评估中实现了 71.7% 的归一化奖励。经过 GRPO 微调的策略在四个非训练语料库衍生的推理基准测试（GSM8K、MATH、GPQA Main 和 GPQA Diamond）上，相较于基础模型也有所提升。这些结果证明，结构化的、基于文档的奖励可以提高留出评分量表的表现，并诱导出超出用于构建训练环境的语料库之外的可迁移推理行为。

## Abstract
We argue that decomposing reward into weighted, verifiable criteria and using an LLM judge to score them provides a partial-credit optimization signal: instead of a binary outcome or a single holistic score, each response is graded along multiple task-specific criteria. We formalize \emph{rubric-grounded reinforcement learning (RL)}: a framework in which the policy is optimized against a structured, multi-criterion reward produced by a frozen LLM judge that conditions on auxiliary grounding the policy never sees. We instantiate the framework by deriving rubrics from an Office of Scientific and Technical Information (OSTI)-derived corpus of roughly 100,000 scientific and technical documents and training Llama-3.1-8B-Instruct with Group Relative Policy Optimization (GRPO). With GRPO-based training, the model achieves $71.7\%$ normalized reward on held-out rubric evaluation. The GRPO-tuned policy also improves over the base model on four reasoning benchmarks not derived from the training corpus -- GSM8K, MATH, GPQA Main, and GPQA Diamond. These results provide evidence that structured, document-grounded rewards can improve held-out rubric performance and induce transferable reasoning behaviors beyond the corpus used to construct the training environment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rubric-Grounded RL: Structured Judge Rewards for Generalizable Reasoning》的结构化总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）强化学习（RL）中**奖励设计（Reward Design）的局限性**展开研究。传统的 RLHF 或 RLAIF 通常将复杂的质量评估压缩为单一的标量分数（如二元对错或整体偏好），这导致优化信号稀疏且模糊。
**核心意义**：提出了一种“基于评分量表的强化学习（Rubric-Grounded RL）”框架，通过将奖励分解为多个加权、可验证的细分准则（Criteria），为模型提供“部分得分（Partial-Credit）”的优化信号，从而引导模型学习更具泛化性的推理行为。

### 2. 方法论
*   **核心思想**：利用一个冻结的 LLM 作为“裁判（Judge）”，根据预设的结构化评分量表（Rubric）对模型生成的多个候选答案进行多维度打分。
*   **关键技术细节**：
    *   **信息不对称设计**：在训练时，裁判可以访问辅助背景文档（Grounding），而策略模型（Policy）只能看到问题。这迫使模型学习通用的推理模式，而非简单的信息检索。
    *   **评分量表定义**：每个量表包含多个准则 $c_j$，每个准则由权重 $w_j$、描述、必要元素、关键词和验证方法组成。
    *   **奖励计算**：总奖励是各准则得分的加权和并进行归一化：$r(x, y) = \frac{1}{W} \sum_{j=1}^{M} s_j$。
    *   **优化算法**：采用**群体相对策略优化（GRPO）**。该算法通过同一提示词下的多个生成样本的相对得分来计算优势函数（Advantage），无需额外的价值函数网络（Value Function）。
*   **算法流程**：
    1.  从科技文档中离线合成“问题-背景-量表”三元组。
    2.  策略模型根据问题生成 $G$ 个响应。
    3.  裁判模型结合背景文档和量表对响应进行多维度打分。
    4.  计算组内相对优势，通过 GRPO 更新策略模型参数。

### 3. 实验设计
*   **数据集**：
    *   **训练/内域评估**：基于 OSTI（美国科学技术信息办公室）的约 10 万份科技文档构建的语料库。
    *   **外域泛化评估（Benchmarks）**：GSM8K（数学）、MATH（困难数学）、GPQA Main 和 GPQA Diamond（研究生级科学问题）。
*   **对比方法**：
    *   **Base**：Llama-3.1-8B-Instruct 原始模型。
    *   **SFT**：在相同 OSTI 数据上进行过监督微调的模型。
    *   **Ours (GRPO)**：应用了基于量规强化学习的模型。

### 4. 资源与算力
*   **模型规模**：策略模型为 Llama-3.1-8B-Instruct；合成量表使用 GPT-OSS-120B；在线裁判使用 GPT-OSS-20B。
*   **算力说明**：文中提到使用了分布式训练框架 NeMo-RL，并部署了 32 个并行裁判工作节点（Nworkers = 32）以处理推理延迟。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）、具体数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 万量级的训练数据，并在 4 个主流推理基准上进行了测试。
*   **充分性**：实验对比了 Base 和 SFT 基准，展示了从内域奖励提升到外域能力迁移的过程。
*   **客观性**：使用了不包含在训练集中的第三方基准测试，证明了能力的泛化而非死记硬背。
*   **局限性**：实验仅在 8B 规模的模型上进行，且缺乏不同裁判模型对结果影响的消融实验，也未提供多次运行的方差分析。

### 6. 主要结论与发现
*   **性能显著提升**：GRPO 训练后的模型在留出量表测试中获得了 71.7% 的奖励，远高于 Base (26.1%) 和 SFT (41.8%)。
*   **泛化能力迁移**：尽管训练数据是科技文档 Q&A，但模型在数学（MATH +2.8%）和科学推理（GPQA +8% 以上）上均有提升。
*   **部分得分的价值**：结构化奖励提供了更高分辨率的测量，使得模型在生成的早期阶段即使不能完全答对，也能通过满足部分准则获得优化信号。

### 7. 优点
*   **可解释性强**：奖励不再是黑盒，而是由具体的准则（如术语准确性、逻辑连贯性）组成。
*   **数据效率**：通过自动化流水线从现有文档生成量表，无需大规模人工标注。
*   **鲁棒性**：通过多准则平均，降低了单一裁判调用产生的随机噪声。

### 8. 不足与局限
*   **裁判依赖**：模型性能高度依赖于裁判模型和量表生成的质量，存在“奖励黑客（Reward Hacking）”或继承裁判偏见的风险。
*   **计算成本**：GRPO 需要对每个提示词生成大量样本并进行多次裁判调用，推理开销巨大。
*   **任务限制**：目前仅验证了单轮对话和科技/推理类任务，在开放式创作或多轮对话中的适用性待验证。
*   **评估单一**：缺乏人工评估来验证裁判打分与人类偏好的一致性。

（完）
