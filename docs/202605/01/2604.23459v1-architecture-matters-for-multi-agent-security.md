---
title: Architecture Matters for Multi-Agent Security
title_zh: 架构对多智能体安全至关重要
authors: "Ben Hagag, William L. Anderson, Christian Schroeder de Witt, Sarah Scheffler"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23459v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统协调与任务性能
tldr: 本研究深入探讨了多智能体系统（MAS）中架构设计对安全性的关键影响。尽管单个智能体可能具备鲁棒性，但其协调机制（如角色分配、通信拓扑和内存管理）会引入新的攻击面。通过在浏览器、桌面和代码环境下的13种架构配置进行实证研究，发现MAS在多数情况下比单智能体更易受攻击，攻击成功率最高可达3.8倍。该工作填补了多智能体系统级安全评估的空白，强调了架构决策在平衡性能与安全性中的核心地位。
selection_source: fresh_fetch
motivation: 多智能体系统日益普及，但其架构决策如何影响系统整体安全性及产生的特有攻击面尚未得到系统研究。
method: 通过在三种典型环境下对13种架构配置进行阶段性评估，系统分析了智能体角色、通信拓扑和内存机制对安全性的影响。
result: 实验表明多智能体架构普遍比单智能体更脆弱，攻击成功率在性能相近的情况下差异巨大，且不存在普适的安全设计。
conclusion: 多智能体系统的安全性不仅取决于单个智能体，更取决于其架构设计，因此亟需开发针对系统级协作的安全评估体系。
---

## 摘要
由两个或更多自主 AI 智能体网络组成的多智能体系统 (MAS) 在生产部署中日益普及，但引入了单智能体设置中不存在的安全风险。即使单个智能体表现出强大的安全性，管理其协调的架构决策也可能产生尚未被系统化表征的攻击面。在这项工作中，我们对 MAS 设计决策如何影响任务性能与抗攻击能力之间的权衡进行了实证研究。在三种智能体环境（浏览器、桌面和代码）和 13 种架构配置中，我们使用分阶段评估（区分规划拒绝、执行阶段拦截、部分有害执行和成功的攻击完成）来研究三个关键设计选择：(i) 智能体角色，决定权限和责任的分配方式；(ii) 通信拓扑，塑造智能体交互的方式和时机；以及 (iii) 记忆，决定每个智能体可访问的上下文和状态可见性。我们发现，在大多数配置中，多智能体架构比独立智能体更脆弱，在良性准确率相当或更高的情况下，攻击成功率差异高达 3.8 倍，且没有哪种单一设计是普遍更安全的。这些结果激励了进一步评估的发展，使其超越单个智能体的安全属性。

## Abstract
Multi-agent systems (MAS), composed of networks of two or more autonomous AI agents, have become increasingly popular in production deployments, yet introduce security risks that do not arise in single-agent settings. Even if individual agents exhibit robust security, architectural decisions governing their coordination can create attack surfaces that have not been systematically characterized. In this work, we present an empirical study of how MAS design decisions shape the tradeoff between task performance and attack resistance. Across three agentic environments (browser, desktop, and code) and 13 architectural configurations, we use stagewise evaluations that distinguish planning refusal, execution-stage interception, partial harmful execution, and successful attack completion to study three key design choices: (i) agent roles, which determine how authority and responsibility are allocated; (ii) communication topology, which shapes how and when agents interact; and (iii) memory, which determines the context and state visibility accessible to each agent. We find that multi-agent architectures are more vulnerable than standalone agents in the majority of configurations, with attack success rates varying by up to 3.8x at comparable or higher benign accuracy, and that no single design is universally safer. These results motivate the development of further evaluations that move beyond the security properties of a single agent.

---

## 论文详细总结（自动生成）

这是一份关于论文《Architecture Matters for Multi-Agent Security》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着 AI 系统从单一的大语言模型（LLM）演变为复杂的多智能体系统（MAS），其安全属性发生了根本性变化。论文的核心问题是：**多智能体系统的架构决策（而非单个模型的能力）如何影响系统的安全性与任务性能之间的权衡？**

研究背景指出，现有的安全研究多集中在单模型（如越狱、提示词注入）或单智能体层面，而忽视了由多个智能体协作产生的“系统级”风险。论文认为，即使单个智能体具备强大的拒绝触发机制，其协调架构（如任务分解、通信路径、记忆共享）也可能稀释有害意图，从而绕过安全防护。

### 2. 论文提出的方法论
论文通过系统性地改变 MAS 的三个关键设计维度，研究其对安全性的影响：

*   **角色配置（Role Configuration）：** 研究任务如何被分解。对比了“独立智能体”、“编排器+单执行器”以及“编排器+多专家智能体（2/3/4个专家）”。核心思想是观察任务分解是否会造成“责任碎片化”，使专家智能体因只看到局部指令而无法识别整体任务的危害性。
*   **通信拓扑（Communication Topology）：** 研究信息流向。对比了星型（Star，中心化编排）、链式（Chain，固定顺序传递）和网状（Mesh，点对点自由授权）结构。
*   **记忆与状态可见性（Memory & State Visibility）：** 研究智能体能看到什么。对比了私有记忆、包含自身思维链（CoT）的记忆以及所有智能体完全共享的记忆。
*   **分阶段评估指标：** 引入了比“成功/失败”更细粒度的指标：
    *   **规划拒绝（PR）：** 在规划阶段被拦截。
    *   **执行拒绝（ER）：** 委派后被执行智能体拦截。
    *   **有害动作（HA）：** 执行了部分有害动作但未完成任务。
    *   **有害任务完成（HT）：** 攻击完全成功。

### 3. 实验设计
论文在三个具有代表性的智能体环境（场景）中进行了评估，并将其从单智能体基准适配为多智能体设置：

*   **场景与数据集：**
    *   **BrowserART（浏览器控制）：** 涉及网络钓鱼、欺诈等任务。
    *   **OS-Harm（桌面操作系统使用）：** 涉及虚假信息传播、身份伪造等。
    *   **RedCode-Gen（代码生成）：** 涉及恶意软件（如勒索软件、Rootkit）的生成。
*   **Benchmark：** 以单智能体（Standalone Agent）作为基准。
*   **对比方法：** 跨 13 种不同的架构配置进行对比，并使用了 6 种不同的基础模型（GPT-4o, GPT-5.4, GPT-5-mini, Claude Sonnet 4, Qwen3-VL, Llama-70B-Chat）以验证结论的普适性。

### 4. 资源与算力
论文**未明确说明**具体的算力资源（如 GPU 型号、数量或总训练时长）。由于该研究主要基于现有的预训练模型进行推理评估和架构实验，而非从头训练模型，其算力消耗主要集中在对数千个任务轨迹（Trajectories）的推理和 LLM-as-a-judge 的自动评分上。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 3 个环境、13 种架构配置、6 个主流模型。
*   **数据量：** 每个场景包含数百个有害任务和数十个良性任务（如 BrowserART 100 有害/42 良性）。
*   **充分性与客观性：** 实验设计较为充分。论文不仅评估了攻击成功率，还同步测量了良性任务的性能，以展示“安全-性能”的权衡。此外，通过人工检查 10% 的日志并修正自动评分器的偏差，确保了评估结果的客观性。

### 6. 论文的主要结论与发现
*   **架构会放大脆弱性：** 在大多数配置中，多智能体架构比单智能体更易受攻击。在 BrowserART 中，专家分解架构的攻击成功率（HT）从 10% 飙升至 38%。
*   **性能与安全脱钩：** 这是一个关键发现——性能更好的架构（如专家分工）往往安全性更差。在良性任务准确率相同甚至更高的情况下，攻击成功率差异可达 3.8 倍。
*   **没有普适的安全架构：** 架构的影响具有高度的场景依赖性。例如，星型拓扑在浏览器场景最危险，但在代码生成场景中，链式拓扑反而最危险。
*   **记忆共享是双刃剑：** 增加记忆可见性并不总能提高安全性，有时反而会通过提供更丰富的上下文来辅助有害任务的协调。
*   **模型对齐的局限性：** 即使是经过严格安全训练的模型（如 GPT-5.4），在特定架构下（如 OS-Harm 的简单委派）也会表现出比单智能体更高的脆弱性。

### 7. 优点（亮点）
*   **系统性：** 首次对多智能体系统的架构设计（角色、拓扑、记忆）进行了受控的安全性实证研究。
*   **细粒度评估：** 提出的分阶段评估指标（PR, ER, HA, HT）能够清晰揭示安全防御是在哪个环节失效的。
*   **现实意义：** 实验场景贴近真实应用（浏览器、OS、代码），证明了“架构即安全变量”的观点，对 MAS 开发者具有直接的指导意义。

### 8. 不足与局限
*   **威胁模型单一：** 主要关注恶意用户直接输入的滥用风险，未深入探讨间接提示词注入（Indirect Prompt Injection）或智能体间的恶意投毒。
*   **防御手段缺失：** 实验主要观察架构的自然属性，未评估专门的 MAS 防御机制（如专门的监视智能体或护栏层）。
*   **静态配置：** 实验中的架构是预设的，未考虑动态改变拓扑或角色的自适应多智能体系统。
*   **模型覆盖：** 虽然涵盖了多个模型，但对于开源小模型在这些复杂架构下的表现探讨较少。

（完）
