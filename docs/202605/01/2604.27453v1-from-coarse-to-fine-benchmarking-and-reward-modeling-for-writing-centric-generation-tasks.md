---
title: "From Coarse to Fine: Benchmarking and Reward Modeling for Writing-Centric Generation Tasks"
title_zh: 从粗到细：以写作为中心的生成任务的基准测试与奖励建模
authors: "Qingyu Ren, Tianjun Pan, Xingzhou Chen, Xuhong Wang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27453v1"
tags: ["query:token"]
score: 9.0
evidence: 针对写作任务的细粒度强化学习训练框架
tldr: 针对大语言模型在生成式写作任务中难以遵循细粒度需求的问题，本文提出了细粒度评估基准 WEval 和强化学习训练框架 WRL。WEval 通过多任务和多需求类型系统评估奖励模型，而 WRL 通过选择性丢弃指令需求构建正负样本，实现更精确的奖励建模。该研究填补了写作任务中细粒度评估与训练的空白，显著提升了模型的写作表现。
selection_source: fresh_fetch
motivation: 现有的评估基准和奖励模型多为粗粒度，无法有效衡量和引导模型对特定写作需求的遵循。
method: 提出了细粒度评估流水线 WEval 和通过选择性丢弃指令需求来构建对比样本的训练框架 WRL。
result: 实验证明该模型在多个写作基准上取得了显著进步，并表现出极强的泛化性能。
conclusion: 从粗粒度转向细粒度的评估与建模是提升大模型写作任务性能的关键方向。
---

## 摘要
大语言模型在文本生成方面取得了显著进展，但在生成式写作任务中仍面临挑战。在评估方面，现有的基准测试对写作奖励模型的评估较为粗略，未能从特定要求的角度衡量性能。在训练方面，现有的训练方法要么采用 LLM-as-a-judge 方法，要么训练粗粒度的奖励模型，缺乏细粒度的需求遵循奖励建模。为了解决这些问题，我们提出了一个用于写作奖励模型的细粒度评估流水线 WEval，以及一个细粒度的强化学习训练框架 WRL。WEval 的评估数据涵盖了多个任务类别和需求类型，通过衡量奖励模型排名与金标准排名之间的相关性，实现了对写作奖励模型的系统评估。WRL 通过选择性地丢弃指令要求来构建正负样本，从而实现更精确的奖励模型训练。实验表明，我们的模型在各种写作基准测试中取得了显著改进，并表现出强大的泛化能力。代码和数据已在 https://github.com/Rainier-rq1/From_Coarse_to_Fine 公开。

## Abstract
Large language models have achieved remarkable progress in text generation but still struggle with generative writing tasks. In terms of evaluation, existing benchmarks evaluate writing reward models coarsely and fail to measure performance from the perspective of specific requirements. In terms of training, existing training methods either use LLM-as-a-judge approaches or train coarse-grained reward models, lacking fine-grained requirement-adherence reward modeling. To address these issues, we propose a fine-grained evaluation pipeline WEval for writing reward models and a fine-grained reinforcement learning training framework WRL. The evaluation data of WEval covers multiple task categories and requirement types, enabling systematic evaluation of writing reward models by measuring the correlation between the rankings of the reward model and gold rankings. WRL constructs positive and negative samples by selectively dropping instruction requirements, allowing for more precise reward model training. Experiments show that our models achieve substantial improvements across various writing benchmarks and exhibit strong generalization. The code and data are publicly available at \href{https://github.com/Rainier-rq1/From_Coarse_to_Fine}{https://github.com/Rainier-rq1/From\_Coarse\_to\_Fine}.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Coarse to Fine: Benchmarking and Reward Modeling for Writing-Centric Generation Tasks》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：尽管大语言模型（LLM）在通用任务上表现优异，但在复杂的生成式写作任务（如创意写作、报告生成）中仍面临挑战，主要表现为难以严格遵循指令中的细粒度约束（如字数、特定格式、风格要求）。
*   **核心问题**：
    1.  **评估缺失**：现有的奖励模型（RM）评估基准（如 RewardBench）过于粗粒度，侧重于知识、推理和安全性，无法衡量模型对写作中特定需求的遵循能力。
    2.  **训练局限**：现有的强化学习（RL）训练要么依赖昂贵且可能存在偏见的“LLM-as-a-judge”，要么使用仅关注流畅度、连贯性等宏观属性的粗粒度奖励模型，缺乏对需求达成度的精准反馈。

### 2. 方法论
论文提出了一个从评估到训练的完整闭环框架：
*   **WEval（细粒度评估流水线）**：
    *   **核心思想**：利用“需求丢弃”（Requirement Dropout）构建天然的金标准排序。
    *   **技术细节**：从复杂指令中提取 $n$ 个原子需求（内容、风格、格式、长度）。通过随机丢弃 $0$ 到 $n-1$ 个需求，生成一系列质量递减的候选回答。
    *   **评估指标**：使用相关性（Correlation）、指令级一致性（IL）和提示词级一致性（PL）来衡量奖励模型给出的评分排序与金标准排序（需求满足越多，排名越靠前）之间的吻合度。
*   **WRL（细粒度强化学习训练框架）**：
    *   **RM 训练**：基于上述 Dropout 机制构建正负样本对，采用 Bradley-Terry (BT) 损失函数训练写作专用奖励模型（如 Qwen2.5-7B-Instruct）。
    *   **策略优化**：使用训练好的细粒度 RM 作为奖励信号，通过 **GRPO（群体相对策略优化）** 算法对策略模型进行强化学习训练。

### 3. 实验设计
*   **数据集**：
    *   **种子指令**：源自 LMSYS-1M、WildChat 和 PRISM。
    *   **评估场景**：涵盖学术工程、金融商业、政治法律、文学艺术、教育、广告营销 6 大领域。
*   **Benchmark**：
    *   **领域内**：WritingBench、LongWriter、Arena-Write。
    *   **领域外（泛化性）**：DeepResearch Bench-RACE、FINDER_DEFT（研究报告生成）。
*   **对比方法**：
    *   **闭源模型**：o3-mini、GPT-4o、Claude-3.7-Sonnet、o1-Preview。
    *   **开源模型**：DeepSeek-R1、Qwen3 系列、Llama-3 系列。
    *   **写作增强模型**：LongWriter 系列、LongWriter-Zero。

### 4. 资源与算力
*   **硬件环境**：实验使用了单节点 **8 张 NVIDIA H200 GPU**。
*   **训练细节**：
    *   使用 VeRL 框架进行 GRPO 训练。
    *   最大序列长度为 12,000 tokens。
    *   全局 Batch Size 为 96，Group Size 为 5。
    *   训练时长未在文中明确给出，但提到了在 WildChat 的 13,221 条指令子集上进行训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括对 10 余种主流模型的基准测试、针对不同参数规模（1.5B 到 14B）模型的 WRL 提升测试、以及针对 RM 本身的性能评估。
*   **消融实验**：对比了 SFT、LLM-as-a-judge RL 与 WRL 的效果，验证了细粒度 RM 的必要性。
*   **客观性与公平性**：通过在多个独立基准（包括人类偏好基准 Arena-Write）上进行测试，并使用不同的教师模型（GPT-4o, Gemini）构建评估集以验证鲁棒性，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能提升**：WRL 显著提升了基础模型的写作能力。例如，Qwen2.5-7B 经过训练后在 WritingBench 上的得分提升了 7.4 分，甚至在某些指标上超越了 GPT-4o 和 o1-Preview。
*   **评估一致性**：WEval 的评估结果与下游 RL 训练的实际表现高度一致，证明了该评估流水线能有效预测 RM 在实际训练中的效用。
*   **泛化能力**：尽管训练数据主要针对通用写作，但模型在 DeepResearch 等专业研究报告生成任务上也表现出显著的性能增益。

### 7. 优点
*   **创新机制**：提出的“需求丢弃”机制巧妙地解决了写作任务中缺乏高质量偏好数据的问题，实现了自动化的金标准排序构建。
*   **细粒度反馈**：相比传统的粗粒度评分，该方法能为模型提供关于“哪些具体约束未满足”的精准信号。
*   **实用性强**：训练框架 WRL 具有模型无关性，可应用于不同架构和规模的 LLM。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 14B 及以下规模的模型，尚未在超大规模参数模型（如 70B+ 或 400B+）上验证 WRL 的边际收益。
*   **需求类型覆盖**：虽然涵盖了四大类需求，但现实世界中的写作约束可能更加复杂多变（如逻辑谬误检测、特定文化禁忌等），WEval 尚不能完全覆盖。
*   **计算开销**：尽管比 LLM-as-a-judge 便宜，但训练专门的细粒度 RM 并进行强化学习仍需要可观的算力支持。

（完）
