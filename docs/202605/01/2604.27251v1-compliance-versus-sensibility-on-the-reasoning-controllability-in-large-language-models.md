---
title: "Compliance versus Sensibility: On the Reasoning Controllability in Large Language Models"
title_zh: 遵循性与合理性：论大语言模型中的推理可控性
authors: "Xingwei Tan, Marco Valentino, Mahmud Elahi Akhter, Yuxiang Zhou, Maria Liakata, Nikolaos Aletras"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27251v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 对大模型推理可控性与逻辑模式的系统研究
tldr: 本研究系统探讨了大语言模型在推理模式上的可控性，通过引入“推理冲突”实验，分析模型在面对违背任务常理的逻辑指令（如归纳、演绎、溯因）时的表现。研究发现模型倾向于优先考虑任务合理性而非指令合规性，且这种倾向受内部参数记忆驱动。通过探测实验发现推理类型在模型层中线性编码，利用激活干预手段可显著提升模型的指令遵循能力，为增强LLM的推理可控性和忠实度提供了新路径。
selection_source: fresh_fetch
motivation: 旨在探究LLM能否将基础推理模式与具体问题实例解耦，以评估并提升模型在推理过程中的可控性。
method: 通过设置推理冲突场景，强制模型使用与任务不匹配的逻辑图式，并结合置信度分析、线性探测及激活干预进行研究。
result: "模型普遍优先选择任务合理的推理模式而非遵循冲突指令，但通过干预模型内部编码可将指令遵循度提升高达29%。"
conclusion: 研究证明LLM推理虽锚定于具体实例，但通过机械干预可有效解耦逻辑图式与数据，实现更强的可控性和泛化性。
---

## 摘要
大语言模型（LLMs）已知通过预训练数据中共享的推理模式获得推理能力，并通过思维链（CoT）实践进一步激发。然而，归纳、演绎和溯因等基本推理模式是否能与特定的问题实例解耦，仍然是模型可控性以及揭示推理可控性的关键挑战。在本文中，我们通过推理冲突的视角对这一问题进行了首次系统性研究：即通过强制执行偏离目标任务预期逻辑图式的指令，引发参数信息与上下文信息之间的显式张力。我们的评估表明，LLMs 始终优先考虑合理性而非遵循性，尽管存在冲突指令，仍倾向于采用适用于任务的推理模式。值得注意的是，任务准确性并不严格由合理性决定，模型在即使使用冲突模式时也往往能保持高性能，这表明其对内化参数记忆的依赖随模型规模的增加而增强。我们进一步证明，推理冲突在内部是可检测的，因为在冲突发生期间置信度分数会显著下降。探测实验证实，推理类型在中后期层中呈线性编码，表明了激活层级可控性的潜力。利用这些见解，我们引导模型转向遵循性，使指令遵循度提高了多达 29%。总的来说，我们的研究结果表明，虽然 LLM 的推理锚定在具体实例上，但主动的机制干预可以有效地将逻辑图式与数据解耦，为提高可控性、忠实度和泛化性提供了一条路径。

## Abstract
Large Language Models (LLMs) are known to acquire reasoning capabilities through shared inference patterns in pre-training data, which are further elicited via Chain-of-Thought (CoT) practices. However, whether fundamental reasoning patterns, such as induction, deduction, and abduction, can be decoupled from specific problem instances remains a critical challenge for model controllability, and for shedding light on reasoning controllability. In this paper, we present the first systematic investigation of this problem through the lens of reasoning conflicts: an explicit tension between parametric and contextual information induced by mandating logical schemata that deviate from those expected for a target task. Our evaluation reveals that LLMs consistently prioritize sensibility over compliance, favoring task-appropriate reasoning patterns despite conflicting instructions. Notably, task accuracy is not strictly determined by sensibility, with models often maintaining high performance even when using conflicting patterns, suggesting a reliance on internalized parametric memory that increases with model size. We further demonstrate that reasoning conflicts are internally detectable, as confidence scores significantly drop during conflicting episodes. Probing experiments confirm that reasoning types are linearly encoded from middle-to-late layers, indicating the potential for activation-level controllability. Leveraging these insights, we steer models towards compliance, increasing instruction following by up to 29%. Overall, our findings establish that while LLM reasoning is anchored to concrete instances, active mechanistic interventions can effectively decouple logical schemata from data, offering a path toward improved controllability, faithfulness, and generalizability.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在推理过程中的“可控性”进行了深入的系统性研究，探讨了当用户指令要求的推理方式与任务本身的逻辑需求发生冲突时，模型如何抉择。

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 在逻辑推理上表现出色，但其推理模式（如演绎、归纳、溯因）是与具体问题深度耦合的，还是可以作为独立工具被灵活调用？
*   **核心问题**：当面临“推理冲突”（Reasoning Conflict）——即用户强制要求使用一种逻辑上不适合当前问题的推理模式时，模型会优先选择“遵循指令”（Compliance）还是“保持逻辑合理性”（Sensibility）？
*   **研究意义**：这对于理解 LLM 的推理机制、提高模型的可控性、忠实度以及在对抗性指令下的鲁棒性具有重要意义。

### 2. 方法论
*   **推理冲突构建**：研究者为演绎（Deduction）、归纳（Induction）和溯因（Abduction）任务设计了专门的提示词（Prompt），强制模型在处理某类任务时使用不匹配的推理模式。
*   **分类框架**：将模型生成的思维链（CoT）分为四类：
    1.  **合理且遵循（S ∩ C）**：指令正确且模型遵循。
    2.  **合理但不遵循（S ∩ ¬C）**：指令错误，模型无视指令，坚持使用正确的推理模式。
    3.  **遵循但不合理（¬S ∩ C）**：模型盲目遵循错误的推理指令。
    4.  **既不合理也不遵循（¬S ∩ ¬C）**：模型产生了无关的推理。
*   **LLM-as-a-Judge**：利用 GPT-4o 等前沿模型作为裁判，自动识别模型实际生成的推理类型，并辅以人工校验。
*   **机制分析与干预**：
    *   **线性探测（Probing）**：在模型的残差流（Residual Stream）中训练分类器，检测推理指令和实际推理模式的编码位置。
    *   **对比激活叠加（CAA）**：通过提取“遵循”与“不遵循”样本的激活差异向量，在推理阶段动态干预模型隐藏层，强制提升其遵循度。

### 3. 实验设计
*   **数据集**：
    *   **FOLIO**：一阶逻辑推理（演绎）。
    *   **SPR**：序列模式识别（归纳）。
    *   **$\alpha$NLI**：溯因常识推理（溯因）。
    *   **RECV**：基于证据的真实世界声明验证（混合类型）。
*   **对比模型**：
    *   **开源系列**：OLMo (7B/32B), Llama 3.1/3.3 (8B/70B), Qwen 3 (8B/32B)。
    *   **闭源前沿**：GPT-5.1 (文中称谓), Gemini 3-Flash。
*   **基准测试**：以 Zero-shot CoT 为基础，对比不同推理指令下的准确率、遵循率和合理率。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA GH200 (96G)、A100 (80G) 以及 AMD MI300X (192G) GPU。
*   **推理框架**：使用 vLLM 进行高效推理。
*   **训练细节**：探测实验使用了逻辑回归（Logistic Regression）；干预实验使用了 EasyEdit 2.0 框架进行激活转向（Steering）。文中未明确给出总训练时长，但指出转向向量是从训练集中采样生成的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、8 个不同规模的模型、4 个核心数据集。
*   **重复性**：每个实验均使用了 3 个随机种子进行采样。
*   **充分性评价**：实验设计非常全面，不仅有宏观的行为评估（准确率、遵循率），还有微观的机制分析（探测、置信度分析）和闭环的优化验证（激活转向），逻辑链条完整，实验结果具有统计学意义。

### 6. 主要结论与发现
*   **合理性优先**：LLM 普遍优先选择“合理性”而非“遵循性”。当指令要求错误的推理模式时，模型往往会无视指令，回归到任务所需的正确逻辑上。
*   **规模效应**：模型越大，逻辑合理性越强，但指令遵循性并不随规模线性增长，而是更多取决于微调策略。
*   **内部可检测性**：当发生推理冲突时，模型的置信度分数（p(True)）会显著下降，表明模型内部能感知到指令与逻辑的矛盾。
*   **层级编码**：推理模式的信号主要编码在模型的中后期层（Middle-to-late layers）。
*   **可控性提升**：通过激活转向（CAA），可以在不微调参数的情况下，将模型的推理指令遵循度提升最高 **29%**。

### 7. 优点
*   **视角新颖**：首次系统性地定义并量化了 LLM 的“推理冲突”。
*   **方法科学**：结合了行为科学（Prompting）与机械解释性（Mechanistic Interpretability），不仅发现了现象，还解释了原因并提供了解决方案。
*   **实用性强**：提出的激活转向方法为实现更细粒度的模型控制提供了轻量化路径。

### 8. 不足与局限
*   **任务局限**：主要集中在基础的逻辑推理类型，尚未扩展到更复杂的数学推理或多步规划任务。
*   **副作用**：激活转向虽然提升了遵循度，但在某些情况下会导致模型忽略格式要求（Task-neglect），产生格式错误的输出。
*   **裁判偏见**：虽然进行了人工校验，但依赖 LLM 作为裁判仍可能存在潜在的系统性偏差。
*   **模型版本**：文中提到的 GPT-5.1 和 Gemini 3-Flash 命名较为超前，可能指代特定的预览版或内部版本，读者需注意其时效性。

（完）
