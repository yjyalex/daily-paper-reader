---
title: Intrinsic Mutual Information as a Modulator for Preference Optimization
title_zh: 内在互信息作为偏好优化的调节器
authors: "Peng Liao, Peijia Zheng, Lingbo Li, Shangsong Liang, Lin Chen"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24804v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型对齐的偏好优化框架
tldr: "针对离线偏好优化（如DPO）中超参数调优耗时的问题，本文提出RMiPO框架。该方法利用响应级内在互信息作为调制器，动态解耦偏好贡献，在几乎不增加计算成本的前提下实现了高效优化。实验表明，RMiPO在性能上优于现有方法，且训练开销降低了15%以上，为大模型对齐提供了更轻量、更自动化的方案。"
selection_source: fresh_fetch
motivation: 现有的离线偏好优化方法高度依赖繁琐的超参数调优，导致对齐大语言模型时产生巨大的时间开销。
method: 提出RMiPO框架，利用响应级内在互信息动态调制偏好贡献，实现轻量且高效的偏好优化。
result: "实验证明RMiPO在性能上持续优于现有方法，同时将训练开销降低了15%以上。"
conclusion: RMiPO通过互信息调制有效减少了对超参数调优的依赖，为提升大模型对齐效率提供了有效途径。
---

## 摘要
离线偏好优化方法（如直接偏好优化，DPO）在使大语言模型（LLM）与人类价值观对齐方面具有显著优势。然而，使用这些方法实现最佳性能通常涉及额外的超参数调优，从而导致巨大的时间开销。尽管先前的工作提出了一系列改进，但这些方法的有效性仍然有限，且未能完全消除对超参数调优的依赖。在这项工作中，我们提出了 RMiPO，这是一个轻量级且高效的离线偏好优化框架。RMiPO 利用内在的响应级互信息进行带有超参数调节的偏好优化，以极小的额外计算成本动态解耦偏好贡献。广泛的实验结果表明，RMiPO 在现有方法的基础上实现了持续优越的性能，同时将训练开销降低了 15% 以上。我们的代码可在 https://github.com/liavonpenn/rmipo 获取。

## Abstract
Offline preference optimization methods, such as Direct Preference Optimization (DPO), offer significant advantages in aligning Large Language Models (LLMs) with human values. However, achieving optimal performance with these methods typically involves additional hyperparameter tuning, resulting in substantial time overhead. Although prior work has proposed a range of improvements, these methods remain limited in effectiveness and have not fully eliminated reliance on hyperparameter tuning. In this work, we propose RMiPO, a lightweight and efficient framework for offline preference optimization. RMiPO leverages intrinsic Response-level Mutual information for Preference Optimization with hyperparameter modulation, dynamically decoupling preference contributions at negligible additional computational cost. Extensive experimental results demonstrate that RMiPO achieves consistently superior performance over existing methods while reducing training overhead by more than 15\%. Our code is available at https://github.com/liavonpenn/rmipo.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RMiPO**（Intrinsic Response-level Mutual information for Preference Optimization）的新型离线偏好优化框架，旨在解决大语言模型（LLM）对齐过程中超参数调优成本过高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的离线偏好优化方法（如 DPO, SimPO）虽然有效，但其性能高度依赖于超参数（如 $\beta$ 和 $\gamma$）的精细调优。这种调优过程极其耗时且计算成本高昂。
*   **研究动机**：
    *   **超参数敏感性**：作者发现奖励边界（Margin）参数 $\gamma$ 对对齐效果起主导作用，但固定值难以适应所有样本。
    *   **信号干扰**：现有的自适应方法多基于对数似然差（Log-likelihood difference），这容易受到“响应流行度偏差”（Popularity Bias）的影响，即模型倾向于选择高频但无意义的通用回复。
    *   **效率瓶颈**：许多改进方法需要引入参考模型（Reference Model），增加了显存占用和计算开销。

### 2. 方法论
*   **核心思想**：利用**点向互信息（PMI）**来衡量响应与提示词之间的内在语义关联，并以此作为信号动态调节每个样本的奖励边界 $\gamma$。
*   **关键技术细节**：
    *   **解耦偏差**：通过贝叶斯分解，证明了 PMI 能够抵消响应的先验概率（流行度），从而只保留语义相关性。
    *   **动态边界调节**：提出一种基于指数衰减的调节机制。当正负样本的 PMI 差异（$\Delta PMI$）较大时（即语义区分度高），减小 $\gamma$ 以避免过度优化；当差异较小时（即样本模糊），增大 $\gamma$ 以强化学习信号。
    *   **公式体现**：$\gamma(x, y_w, y_l) = \gamma_{min} + (\gamma_{max} - \gamma_{min}) \cdot \exp(-\max(0, \Delta PMI))$。
    *   **算法流程**：1. 计算策略模型对响应的条件概率和先验概率；2. 计算 $\Delta PMI$；3. 映射得到样本级的动态 $\gamma$；4. 结合长度归一化的奖励差进行梯度更新。

### 3. 实验设计
*   **数据集**：
    *   **UltraFeedback-Binarized**：用于通用的指令遵循训练。
    *   **Anthropic-HH (Helpful and Harmless)**：用于安全对齐评估。
*   **Benchmark（基准测试）**：
    *   **AlpacaEval 2.0**（胜率评估）。
    *   **MT-Bench**（多轮对话评分）。
    *   **Open LLM Leaderboard v2**（包含 IFEval, BBH, MATH, GPQA, MUSR, MMLU-PRO 等下游任务）。
*   **对比方法**：包括标准方法（DPO, SimPO, IPO, KTO, CPO, SLiC）以及针对超参数敏感性的改进方法（$\alpha$-DPO, $\beta$-DPO, $\epsilon$-DPO, SimPER）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A800 (80GB) GPU**。
*   **训练时长**：RMiPO 在 Llama3-8B 上的训练时间约为 **6.75 小时**。
*   **效率对比**：相比 DPO 及其变体（约 8-10 小时），RMiPO 减少了约 15%-20% 的训练时间，且峰值显存占用从 82% 降至 62%（因为无需参考模型）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Mistral-7B 和 Llama3-8B 两种主流架构，并分别在 Base 和 Instruct 版本上进行了验证。
*   **充分性**：
    *   **消融实验**：验证了动态边界（GM）、互信息信号（MI）和指数衰减（ED）三个组件的必要性。
    *   **鲁棒性分析**：测试了不同调度策略（线性、余弦等）和边界范围（$\gamma_{min}, \gamma_{max}$）的影响。
    *   **密度分析**：通过奖励分布曲线证明了 RMiPO 生成的奖励更接近真实的人类偏好分布。
    *   **客观性**：实验设置遵循了标准的对齐手册（Alignment Handbook），对比基线均经过了充分的超参数搜索，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：RMiPO 在 AlpacaEval 2 和 MT-Bench 上一致优于现有 SOTA 方法，尤其在 Llama3-8B-Instruct 上达到了 49.1% 的胜率。
*   **泛化性强**：在数学推理（MATH）和复杂指令遵循（IFEval）等下游任务中表现出更强的稳定性。
*   **自适应有效性**：实验观察到模型自动学习出的 $\gamma$ 分布中心与人工搜索出的最优固定 $\gamma$ 值高度吻合，证明了其自动调优的有效性。

### 7. 优点
*   **轻量化与高效**：无需参考模型，显著降低了显存和计算开销。
*   **理论支撑强**：利用信息论中的 PMI 概念解决了偏好优化中的流行度偏差问题，具有良好的解释性。
*   **易于部署**：消除了繁琐的超参数搜索过程，使对齐过程更加自动化。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 和 8B 参数规模的模型上，尚未在百亿或千亿级（如 70B+）模型上进行大规模验证。
*   **超参数依赖未完全消除**：虽然解决了 $\gamma$ 的调优，但仍需设定 $\beta$ 的大致范围以及 $\gamma$ 的上下界（尽管实验证明其对上下界不敏感）。
*   **离线限制**：目前仅限于离线偏好数据，尚未扩展到实时交互的在线强化学习场景。

（完）
