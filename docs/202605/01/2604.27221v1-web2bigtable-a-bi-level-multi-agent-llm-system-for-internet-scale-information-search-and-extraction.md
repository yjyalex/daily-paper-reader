---
title: "Web2BigTable: A Bi-Level Multi-Agent LLM System for Internet-Scale Information Search and Extraction"
title_zh: Web2BigTable：一种用于互联网规模信息搜索与提取的双层多智能体大语言模型系统
authors: "Yuxuan Huang, Yihang Chen, Zhiyuan He, Yuxiang Chen, Ka Yiu Lee, Huichi Zhou, Weilin Luo, Meng Fang, Jun Wang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27221v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于结构化聚合和推理的多智能体框架
tldr: Web2BigTable是一个双层多智能体框架，旨在解决互联网规模下的深度推理和广度结构化信息提取难题。它通过上层协调器分解任务与下层执行器并行处理，结合闭环验证、外部存储和共享工作区，实现了高效的搜索与数据聚合。该系统在WideSearch和XBench-DeepSearch基准测试中均取得SOTA性能，显著提升了复杂Web搜索任务的成功率和准确性。
selection_source: fresh_fetch
motivation: 现有Web搜索智能体在处理需要跨实体一致性的广度聚合任务和长路径深度推理任务时表现不佳。
method: 提出一种双层架构，利用上层协调器分解任务，下层智能体通过共享工作区和闭环验证机制并行执行并动态优化。
result: "在WideSearch基准上，成功率达到38.50%（是次优系统的7.5倍），并在深度搜索任务中达到73.0%的准确率。"
conclusion: Web2BigTable通过多智能体协同和自我演化机制，有效统一了互联网规模下的深度搜索与广度提取需求。
---

## 摘要
智能体网络搜索日益面临两种截然不同的需求：针对单一目标的深度推理，以及跨多个实体和异构来源的结构化聚合。目前的系统在这两个方面都面临挑战。面向广度的任务要求输出符合模式、具有广泛覆盖范围和跨实体一致性，而面向深度的任务则需要对长且具有分支的搜索轨迹进行连贯推理。我们介绍了 Web2BigTable，这是一个支持上述两种模式的“网络到表格”搜索多智能体框架。Web2BigTable 采用双层架构，其中上层编排器将任务分解为子问题，下层工作智能体并行解决这些问题。通过“运行-验证-反思”的闭环过程，该框架利用持久且人类可读的外部存储器，随着时间的推移共同改进任务分解和执行，并对每个单一智能体进行自我进化更新。在执行过程中，工作智能体通过共享工作区进行协作，使部分发现可见，从而减少冗余探索、调和冲突证据，并适应新出现的覆盖缺口。Web2BigTable 在 WideSearch 上创下了新的当前最佳水平，Avg@4 成功率达到 38.50（是排名第二的 5.10 的 7.5 倍），行 F1 分数为 63.53（比排名第二的高出 25.03），项 F1 分数为 80.12（比排名第二的高出 14.42）。它还泛化到 XBench-DeepSearch 上的深度导向搜索，达到了 73.0 的准确率。代码可在 https://github.com/web2bigtable/web2bigtable 获取。

## Abstract
Agentic web search increasingly faces two distinct demands: deep reasoning over a single target, and structured aggregation across many entities and heterogeneous sources. Current systems struggle on both fronts. Breadth-oriented tasks demand schema-aligned outputs with wide coverage and cross-entity consistency, while depth-oriented tasks require coherent reasoning over long, branching search trajectories. We introduce \textbf{Web2BigTable}, a multi-agent framework for web-to-table search that supports both regimes. Web2BigTable adopts a bi-level architecture in which an upper-level orchestrator decomposes the task into sub-problems and lower-level worker agents solve them in parallel. Through a closed-loop run--verify--reflect process, the framework jointly improves decomposition and execution over time via persistent, human-readable external memory, with self-evolving updates to each single-agent. During execution, workers coordinate through a shared workspace that makes partial findings visible, allowing them to reduce redundant exploration, reconcile conflicting evidence, and adapt to emerging coverage gaps. Web2BigTable sets a new state of the art on WideSearch, reaching an Avg@4 Success Rate of \textbf{38.50} ($7.5\times$ the second best at 5.10), Row F1 of \textbf{63.53} (+25.03 over the second best), and Item F1 of \textbf{80.12} (+14.42 over the second best). It also generalises to depth-oriented search on XBench-DeepSearch, achieving 73.0 accuracy. Code is available at https://github.com/web2bigtable/web2bigtable.

---

## 论文详细总结（自动生成）

这是一份关于论文《Web2BigTable: A Bi-Level Multi-Agent LLM System for Internet-Scale Information Search and Extraction》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的智能体（Agent）网络搜索面临两大挑战：**深度搜索**（对单一目标进行复杂推理）和**广度搜索**（跨异构来源对大量实体进行结构化聚合）。
*   **核心痛点**：现有的单体智能体系统在处理大规模“网络到表格”（Web-to-Table）任务时，容易出现上下文饱和、错误累积以及初始规划无法适应搜索中新发现的问题。
*   **整体含义**：论文提出了 **Web2BigTable**，这是一个双层多智能体框架，旨在通过任务分解、异步协作和基于外部存储的自我演化机制，同时解决 Web 搜索的深度与广度需求。

### 2. 方法论
*   **双层架构（Bi-Level Architecture）**：
    *   **上层编排器（Orchestrator）**：负责将复杂的全局查询分解为多个独立的子任务（如按实体、按时间或按类别分解）。
    *   **下层工作智能体（Workers）**：并行执行子任务，利用搜索和提取工具获取信息。
*   **关键技术细节**：
    *   **自我演化机制（Run-Verify-Reflect）**：系统通过“运行-验证-反思”闭环不断进化。在训练阶段，系统对比输出与标准答案（Gold Standard），将错误模式总结为人类可读的“技能”（Skills）并存入外部存储。
    *   **双重外部存储**：
        1.  **长期语义存储（Skill Banks）**：包含编排技能（$S_o$）和执行技能（$S_w$），随训练迭代但不涉及模型参数微调。
        2.  **短期工作存储（Shared Workboard）**：一个基于 Markdown 的共享协作板，工作智能体在此记录中间发现、避免重复劳动并填补覆盖缺口。
    *   **异步协作**：利用文件锁和标签分区技术，允许工作智能体在不冲突的情况下共享进度，实现“异步共识”。

### 3. 实验设计
*   **数据集/场景**：
    *   **WideSearch**：侧重广度，包含 200 个手动策划的任务（中英各半），要求提取数百行结构化数据。
    *   **XBench-DeepSearch**：侧重深度，要求多步推理和跨源验证。
*   **对比方法**：
    *   **单体智能体**：如 Claude 3.5 Sonnet (Thinking)、GPT-4o、Gemini 1.5 Pro 等。
    *   **端到端系统**：OpenAI、Google 和 Anthropic 的原生搜索/研究产品。
    *   **多智能体框架**：在标准编排框架下运行的上述前沿模型。

### 4. 资源与算力
*   **模型配置**：编排器使用 **GPT-4o mini**，工作智能体使用 **Gemini 1.5 Flash**。作者特意选择轻量级模型以证明性能提升源于框架设计而非模型规模。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到训练阶段仅需 20 个基准相关的查询即可完成技能演化，暗示其训练成本较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 WideSearch 上进行了 4 次独立运行，报告了 Avg@4（平均值）和 Max@4（最大值）。
    *   进行了详尽的**消融实验**，分别验证了“学习到的编排技能”、“共享工作板”和“工人技能演化”的贡献。
    *   跨两个截然不同的基准（广度 vs 深度）进行验证。
*   **充分性评价**：实验设计较为充分且客观。通过对比“框架+弱模型”与“强模型单体”的表现，有力证明了方法的有效性。

### 6. 主要结论与发现
*   **性能突破**：在 WideSearch 上，成功率（SR）达到 38.50%，是次优系统（5.10%）的 **7.5 倍**；行 F1 和项 F1 分别提升了 25% 和 14% 以上。
*   **泛化能力**：在深度搜索基准 XBench 上达到 73.0 的准确率，超越了 Minimax-M2 等专用深度研究系统。
*   **核心驱动力**：消融实验显示，**学习到的编排技能**对性能提升贡献最大，其次是共享工作板的协作机制。

### 7. 优点
*   **双层演化**：通过外部存储实现技能积累，无需昂贵的梯度更新（Fine-tuning），使系统具备持续改进的能力。
*   **高效协作**：共享工作板解决了多智能体搜索中的冗余和不一致问题，实现了信息的动态级联。
*   **可解释性**：存储的技能以 Markdown 形式存在，人类可读、可编辑、版本可控。
*   **成本效益**：证明了通过优秀的架构设计，轻量级模型也能在复杂任务上超越顶级闭源模型。

### 8. 不足与局限
*   **训练依赖**：自我演化过程依赖于高质量的“标准答案”表格，在某些极端冷门或实时变动的领域，获取这些标注数据可能存在困难。
*   **延迟与成本**：虽然使用了轻量模型，但多智能体并行及多次迭代反思可能会带来较高的推理延迟和 API 调用成本。
*   **评估偏差**：虽然使用了 LLM-as-a-judge 处理自由文本，但在语义判断上仍可能存在潜在的评估偏差。

（完）
