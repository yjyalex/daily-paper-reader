---
title: Leveraging Verifier-Based Reinforcement Learning in Image Editing
title_zh: 在图像编辑中利用基于验证器的强化学习
authors: "Hanzhong Guo, Jie Wu, Jie Liu, Yu Gao, Zilyu Ye, Linxiao Yuan, Xionghui Wang, Yizhou Yu, Weilin Huang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27505v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用思维链的基于验证器的推理奖励模型
tldr: 本研究针对图像编辑中缺乏鲁棒奖励模型的问题，提出了Edit-R1框架。该框架引入了基于思维链（CoT）的推理奖励模型（RRM），将编辑指令分解为多个准则进行细粒度评估。通过SFT和GCPO算法优化RRM，并利用GRPO算法进一步提升下游图像编辑模型的性能。实验证明，该方法在可解释性和准确性上优于现有视觉语言模型，显著增强了图像编辑效果。
selection_source: fresh_fetch
motivation: 现有的图像编辑奖励模型通常只给出整体评分而缺乏细粒度检查，导致奖励存在偏差且难以处理复杂的编辑指令。
method: 提出Edit-R1框架，构建基于CoT的推理奖励模型（RRM），并结合GCPO和GRPO算法分别优化奖励模型和下游编辑模型。
result: Edit-RRM在编辑评估任务上超越了Seed-1.5-VL等强力模型，并展现出随参数量增加而性能提升的缩放趋势。
conclusion: 通过将简单评分器转变为具备推理能力的验证器，Edit-R1为图像编辑任务提供了更精准、可解释的强化学习优化路径。
---

## 摘要
尽管基于人类反馈的强化学习（RLHF）已成为文本生成图像的关键范式，但其在图像编辑中的应用在很大程度上仍未得到充分探索。一个关键瓶颈是缺乏适用于所有编辑任务的鲁棒通用奖励模型。现有的编辑奖励模型通常只给出总体评分而缺乏详细检查，忽略了不同的指令要求，从而导致奖励偏差。为解决这一问题，我们认为关键在于从简单的评分器转向推理验证器。我们推出了 Edit-R1，这是一个构建基于思维链（CoT）验证器的推理奖励模型（RRM），并将其用于下游图像编辑的框架。Edit-RRM 将指令分解为不同的原则，根据每个原则评估编辑后的图像，并将这些检查汇总为可解释的细粒度奖励。为了构建这种 RRM，我们首先应用有监督微调（SFT）作为“冷启动”来生成 CoT 奖励轨迹。然后，我们引入了组对比偏好优化（GCPO），这是一种利用人类成对偏好数据来增强我们逐点 RRM 的强化学习算法。在构建 RRM 之后，我们使用 GRPO 利用这种不可微但功能强大的奖励模型来训练编辑模型。大量实验表明，作为特定于编辑的奖励模型，我们的 Edit-RRM 超越了 Seed-1.5-VL 和 Seed-1.6-VL 等强大的视觉语言模型（VLM），并且我们观察到了明显的扩展趋势，性能随参数量从 3B 增加到 7B 而持续提升。此外，Edit-R1 为 FLUX.1-kontext 等编辑模型带来了增益，突显了其在增强图像编辑方面的有效性。

## Abstract
While Reinforcement Learning from Human Feedback (RLHF) has become a pivotal paradigm for text-to-image generation, its application to image editing remains largely unexplored. A key bottleneck is the lack of a robust general reward model for all editing tasks. Existing edit reward models usually give overall scores without detailed checks, ignoring different instruction requirements and causing biased rewards. To address this, we argue that the key is to move from a simple scorer to a reasoning verifier. We introduce Edit-R1, a framework that builds a chain-of-thought (CoT) verifier-based reasoning reward model (RRM) and then leverages it for downstream image editing. The Edit-RRM breaks instructions into distinct principles, evaluates the edited image against each principle, and aggregates these checks into an interpretable, fine-grained reward. To build such an RRM, we first apply supervised fine-tuning (SFT) as a ``cold-start'' to generate CoT reward trajectories. Then, we introduce Group Contrastive Preference Optimization (GCPO), a reinforcement learning algorithm that leverages human pairwise preference data to reinforce our pointwise RRM. After building the RRM, we use GRPO to train editing models with this non-differentiable yet powerful reward model. Extensive experiments demonstrate that our Edit-RRM surpasses powerful VLMs such as Seed-1.5-VL and Seed-1.6-VL as an editing-specific reward model, and we observe a clear scaling trend, with performance consistently improving from 3B to 7B parameters. Moreover, Edit-R1 delivers gains to editing models like FLUX.1-kontext, highlighting its effectiveness in enhancing image editing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging Verifier-Based Reinforcement Learning in Image Editing》（在图像编辑中利用基于验证器的强化学习）的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然强化学习（RLHF）在文本生成图像（T2I）中取得了巨大成功，但在**图像编辑**领域应用受限。核心瓶颈在于缺乏鲁棒的奖励模型（Reward Model, RM）。
*   **核心问题**：现有的编辑奖励模型通常是“黑盒评分器”，仅给出整体分数，缺乏对指令遵循、未编辑区域保留及图像质量的细粒度检查，容易产生奖励偏差或幻觉。
*   **整体含义**：论文提出 **Edit-R1** 框架，主张将奖励模型从简单的“评分器”转变为“**推理验证器**”。通过引入思维链（CoT）和原则分解，使模型在评分前先进行逻辑推理，从而提供更精准、可解释的反馈。

### 2. 方法论
Edit-R1 包含两个核心阶段：构建推理奖励模型（RRM）和优化下游编辑模型。
*   **推理奖励模型 (Edit-RRM)**：
    *   **原则分解**：将编辑指令分解为“保留（Keep）”、“遵循（Follow）”和“质量（Quality）”三个维度的可验证原则。
    *   **冷启动 SFT**：利用外部强力 VLM 作为“质量控制法官”，从 200 万个样本中筛选出高质量的“思维+评分”轨迹进行有监督微调。
    *   **GCPO（组对比偏好优化）**：论文提出的一种新型 RL 算法。针对人类成对偏好数据（A 比 B 好），让模型对 A 和 B 分别生成 $N$ 条推理轨迹，通过计算跨组的胜率/败率作为奖励信号，优化逐点（Pointwise）推理模型。
*   **下游编辑模型优化**：
    *   **Flow-GRPO**：利用训练好的、不可微但强大的 RRM 作为奖励信号，采用组相对策略优化（GRPO）算法对 FLUX 或 Qwen 等扩散/流匹配编辑模型进行后训练。

### 3. 实验设计
*   **数据集**：
    *   **RRM 训练**：200K 原始样本（包含随机子集和由 GPT-4o 筛选的“困难”子集），10K 人类标注的偏好对用于 GCPO。
    *   **评估基准**：内部 5,000 样本的偏好测试集、公开的 **EditRewardBench**。
    *   **下游任务**：使用 **GEdit-Bench-EN** 和 **Emu Edit Test Set** 评估编辑效果。
*   **对比方法**：
    *   **奖励模型对比**：Seed-1.5-VL、Seed-1.6-VL、EditScore-7B 等。
    *   **编辑模型对比**：FLUX.1-kontext、Qwen-Image-Edit、Bagel、Step-Edit 等。
*   **评估指标**：准确率（Accuracy）、语义一致性（SC）、感知质量（PQ）及整体得分（O）。

### 4. 资源与算力
*   **模型基础**：RRM 基于 **Qwen-VL-2.5**（3B 和 7B 版本）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。但提到在 GRPO 训练中使用了组大小 $G=24$，并指出由于采用了高效的打包推理（Packed Inference），训练成本在实践中是“可控的”。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 7B 的模型缩放实验、SFT 与 RL 阶段的消融实验、不同 VLM 组件（Think vs Verify）的对比。
*   **充分性**：实验设计较为全面。不仅在内部数据集上验证，还通过第三方公开基准（EditRewardBench）证明了模型的泛化能力。
*   **客观性**：引入了人类评估（GSB Score）来补充自动指标 GPT-4 评分的不足，确保了评估的公正性。

### 6. 主要结论与发现
*   **性能领先**：7B 版本的 Edit-RRM 在偏好预测准确率上达到 82.22%，显著超过了 Seed-1.5-VL 等闭源 API。
*   **缩放效应**：奖励模型的性能随参数量增加（3B 到 7B）呈现明显的提升趋势。
*   **下游增益**：使用 Edit-RRM 优化后，FLUX.Kontext 的整体得分从 5.77 提升至 6.24，尤其在“动作改变（Motion Change）”等高难度任务上进步显著（提升 15.2%）。
*   **GCPO 有效性**：证明了通过对比推理轨迹，可以有效纠正 SFT 模型中的幻觉问题（如属性泄漏）。

### 7. 优点
*   **可解释性强**：通过 CoT 轨迹，用户可以清晰看到模型为何给出特定分数，增加了奖励机制的透明度。
*   **细粒度评估**：原则分解机制解决了传统模型“顾此失彼”的问题，平衡了修改与保留的关系。
*   **算法创新**：GCPO 巧妙地解决了如何用成对偏好数据优化逐点推理模型的技术难题。

### 8. 不足与局限
*   **推理开销**：由于 RRM 需要生成长文本推理轨迹（CoT），其推理速度比直接输出分数的模型慢，增加了训练时的计算负担。
*   **不可微限制**：RRM 是非微分的，这意味着它不能直接用于需要梯度回传的 RL 算法（如 REFL），必须依赖 GRPO 等采样类算法。
*   **数据依赖**：模型高度依赖于第一阶段原则分解的质量，如果分解不当，后续推理可能偏离目标。

（完）
