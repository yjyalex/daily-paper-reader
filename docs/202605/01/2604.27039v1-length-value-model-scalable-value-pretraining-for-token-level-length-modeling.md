---
title: "Length Value Model: Scalable Value Pretraining for Token-Level Length Modeling"
authors: "Zhen Zhang, Changyi Yang, Zijie Xia, Zhen Yang, Chengzhi Liu, Zhaotiao Weng, Yepeng Liu, Haobo Chen, Jin Pan, Chenyang Zhao, Yuheng Bu, Alkesh Patel, Zhe Gan, Xin Eric Wang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27039v1"
tags: ["query:token"]
score: 9.0
evidence: 每个token分配常数负奖励的token级框架
tldr: 本研究针对自回归模型中生成长度对推理成本和性能的关键影响，提出了长度价值模型（LenVM）。该模型将长度建模转化为Token级别的价值估计问题，通过为每个生成的Token分配负奖励来预测剩余生成长度。LenVM具有无标注、高密度和可扩展的特点，在精确长度匹配、推理效率权衡及长度预测等任务上表现优异，显著提升了模型对生成长度的控制能力和可解释性。
selection_source: fresh_fetch
motivation: 现有方法主要在粗粒度的序列级别进行长度建模，缺乏对Token级别生成长度的细粒度控制和推理成本优化。
method: 将长度建模表述为价值估计问题，通过为每个Token分配恒定负奖励，训练模型预测代表剩余生成长度的折扣回报。
result: 在LIFEBench任务中将7B模型的长度得分从30.9提升至64.8，并在GSM8K任务中实现了极佳的性能与效率权衡。
conclusion: LenVM证明了Token级价值信号能有效建模生成长度，为未来强化学习训练和生成动态分析提供了通用的长度建模框架。
---

## Abstract
Token serves as the fundamental unit of computation in modern autoregressive models, and generation length directly influences both inference cost and reasoning performance. Despite its importance, existing approaches lack fine-grained length modeling, operating primarily at the coarse-grained sequence level. We introduce the Length Value Model (LenVM), a token-level framework that models the remaining generation length. By formulating length modeling as a value estimation problem and assigning a constant negative reward to each generated token, LenVM predicts a bounded, discounted return that serves as a monotone proxy for the remaining generation horizon. This formulation yields supervision that is annotation-free, dense, unbiased, and scalable. Experiments on LLMs and VLMs demonstrate LenVM provides a highly effective signal at inference time. On the LIFEBench exact length matching task, applying LenVM to a 7B model improves the length score from 30.9 to 64.8, significantly outperforming frontier closed-source models. Furthermore, LenVM enables continuous control over the trade off between performance and efficiency. On GSM8K at a budget of 200 tokens, LenVM maintains 63% accuracy compared to 6 percent for token budget baseline. It also accurately predicts total generation length from the prompt boundary. Finally, LenVM's token-level values offer an interpretable view of generation dynamics, revealing how specific tokens shift reasoning toward shorter or longer regimes. Results demonstrate that LenVM supports a broad range of applications and token length can be effectively modeled as a token-level value signal, highlighting the potential of LenVM as a general framework for length modeling and as a length-specific value signal that could support future RL training. Code is available at https://github.com/eric-ai-lab/Length-Value-Model.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Length Value Model (LenVM)** 的新型 Token 级别长度建模框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自回归模型（LLMs/VLMs）中，生成长度是计算的核心单位，直接影响**推理成本**（延迟、内存、算力）和**推理性能**（长思维链通常性能更好）。
*   **现状：** 现有的长度控制方法（如 Prompt 指令、序列级惩罚、预解码预测）多处于粗粒度的序列级别，缺乏对生成过程中每一小步（Token 级别）的细粒度感知。
*   **核心问题：** 如何在 Token 级别准确建模并预测剩余的生成长度，从而实现更精准的长度控制和推理效率优化？

### 2. 论文提出的方法论
LenVM 将长度建模转化为强化学习中的**价值估计（Value Estimation）**问题。
*   **核心思想：** 为每个生成的 Token 分配一个恒定的负奖励 $r_t = -(1-\gamma)$。这样，从当前步骤 $t$ 到结束的折扣回报（Discounted Return）$G_t$ 就成为了剩余长度 $L-t$ 的单调代理函数。
*   **关键公式：** $G_t = -(1 - \gamma^{L-t})$。该值被限制在 $(-1, 0)$ 之间，解决了原始长度数值范围波动剧烈的问题。
*   **技术细节：**
    *   **架构：** 在 LLM/VLM 的最后一层隐藏状态上添加一个标量价值头（2层 MLP）。
    *   **训练目标：** 使用均方误差（MSE）损失函数，让模型预测每个 Token 位置的 $G_t$。
    *   **优势：** 无需人工标注（自动从采样序列获取长度）、密度高（每个 Token 都有监督信号）、无偏且易于扩展。

### 3. 实验设计
论文进行了多维度的实验验证：
*   **数据集：** 涵盖代码（OpenCodeReasoning-2）、指令遵循（WildChat）和数学（DeepMath-103K）三大领域。
*   **应用场景与 Benchmark：**
    1.  **长度受控生成：** 在 **LIFEBench** 上测试“等于”、“最多”、“至少”三种长度约束。
    2.  **性能-效率权衡：** 在 **GSM8K、MATH500、MathVista** 上测试通过 LenVM 引导解码来平衡准确率与 Token 预算。
    3.  **长度预测：** 测试从 Prompt 边界预测完整输出长度的准确性（MRE 指标）。
*   **对比方法：** 包括 GPT-4o、Claude 3.5/4、Gemini 等闭源模型（通过 Prompt 控制）以及硬截断（Hard Token Budget）基准。

### 4. 资源与算力
*   **模型规模：** 实验涵盖了从 1.5B 到 32B 不等的模型（基于 Qwen2.5 和 Qwen3 系列）。
*   **训练细节：** 使用 LlamaFactory 框架，学习率为 $2 \times 10^{-5}$，Batch Size 为 1024，采用 BF16 精度。
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长，但提到在 100k-1.4M 规模的数据集上训练了 2 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模：** 做了大量实验，包括跨领域（数学、代码、视觉语言）、跨模型规模（1.5B 到 32B）以及消融实验（目标表示法、折扣因子 $\gamma$、数值精度等）。
*   **客观性：** 实验设计较为公平，对比了当前最强的闭源模型，并展示了 LenVM 在不同任务下的 Pareto 前沿（性能 vs 长度），证明了其在不同场景下的普适性。

### 6. 主要结论与发现
*   **精准控制：** 在 LIFEBench 的精确长度匹配任务中，LenVM 将 7B 模型的得分从 30.9 提升至 64.8，远超闭源模型。
*   **效率优化：** 在相同 Token 预算下，LenVM 引导的解码比简单截断保留了更高的准确率（如 GSM8K 在 200 Token 预算下准确率从 6% 提升至 63%）。
*   **可扩展性：** 验证了 LenVM 的损失随模型规模、数据量和采样数增加而持续下降，符合 Scaling Law。
*   **可解释性：** 发现某些 Token（如 "but", "wait", "ah"）会导致预测长度增加，而（"therefore", "clearly"）则预示着即将结束。

### 7. 优点
*   **建模巧妙：** 将长度问题转化为 RL 价值函数，利用了成熟的 Bellman 递归思想。
*   **无需标注：** 训练数据完全自给自足，极大地降低了构建成本。
*   **连续控制：** 提供了平滑调节性能与效率的“旋钮”，而非简单的硬截断。
*   **通用性：** 证明了该方法在纯文本 LLM 和多模态 VLM 上同样有效。

### 8. 不足与局限
*   **推理开销：** 在解码过程中运行 LenVM 价值头需要额外的计算，虽然开销较小，但仍会增加推理延迟。
*   **依赖基座：** LenVM 的表现高度依赖于基座模型隐藏层所包含的信息质量。
*   **RL 训练缺失：** 论文目前主要展示了 LenVM 在推理阶段的控制作用，虽然理论上可以作为 RL 的价值基准（Value Baseline），但尚未在实际的 PPO 强化学习训练中进行端到端验证。

（完）
