---
title: Joint Training of Multi-Token Prediction in Reinforcement Learning via Optimal Coefficient Calibration
title_zh: 通过最优系数校准实现强化学习中多令牌预测的联合训练
authors: "Zili Wang, Jiajun Chai, Lin Chen, Xiaohan Wang, Shiming Xiang, Guojun Yin"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28184v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习用于大语言模型推理
tldr: 本研究从优化角度重新审视强化学习中的多令牌预测联合训练失败的原因，将MTP对RL目标的影响分解为一阶相关和二阶扰动惩罚，并基于此提出最优系数校准（OCC）方法，通过在线跟踪最优系数实现联合训练，在数学推理基准上达到或超过分离基线的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28184v1/fig-001.webp\", \"caption\": \"Figure 1: Left: Accuracy on AIME24. CE loss shows great degradation, while policy loss initially surpasses but degrades as training progresses. In contrast, OCC sustains gains throughout training and consistently outperforms. Middle & Right: Illustrations of the MTP training regimes. Detach training stops the gradient from MTP into the main model. Joint MTP-RL training allows this gradient to flow back into the main model, enabling joint updates.\", \"page\": 2, \"index\": 1, \"width\": 946, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2605.28184v1/fig-002.webp\", \"caption\": \"Table 2: Comparison of policy loss with λ ∈ {0.1, 0.2, 0.5, 1.0} against Detach and OCC.\", \"page\": 8, \"index\": 2, \"width\": 825, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.28184v1/fig-003.webp\", \"caption\": \"Figure 2: Illustration of the training dynamics of coefficient components. (a) Curves of |c| of full model, CE loss, and alignment proxy. While the correlation of CE loss (green curve) remains near zero, the full model correlation of policy loss declines steadily. The proxy |ĉt| effectively captures the drift trends. (b) Curves of v2 of full model and variance proxy. v̂2t also captures v2 and stabilizes around a similar magnitude in the later stage. (c) Curves of first-order correlation term and second-order penalty term. The first-order correlation term decays while the second-order penalty stays persistent. Their crossing flips ∆MTP from positive gain to negative gain.\", \"page\": 4, \"index\": 3, \"width\": 929, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.28184v1/fig-004.webp\", \"caption\": \"Figure 3: Evolution of the policy-aligned gain across training steps (Eq. (10)). Each curve is a downwardopening parabola in λ, coloured from early (dark purple) to late (bright yellow) steps. The vertical dashed line marks a fixed λ: at early steps, the intersection point of λ and the parabola lies above zero (positive gain), but as training progresses the parabola drifts so that the intersection point falls under zero (negative gain).\", \"page\": 5, \"index\": 4, \"width\": 450, \"height\": 337}]"
motivation: 当前RL实践中联合训练MTP与RL会导致性能下降，需要理解失败原因并提出改进方法。
method: 对MTP在RL目标中的每步影响进行优化分解，提出OCC自适应方案，通过log概率代理在线校准系数。
result: 在六个竞赛级数学推理基准上，OCC一致匹配或超过分离梯度基线。
conclusion: 通过最优系数校准可以实现有效的联合MTP-RL训练，避免性能退化。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为提升大语言模型推理能力的标准范式，而多令牌预测（MTP）则是预训练中广泛采用的模块。将两者结合是自然的方法，但当前的强化学习实践分离了MTP梯度，因为联合训练会降低性能。我们从优化角度重新审视这一失败。我们证明，MTP在RL目标上的每步效应可以分解为两项：一阶相关项和二阶扰动惩罚项。这一分解统一了三种MTP训练模式：分离（Detach）、交叉熵损失和策略损失，并解释了每种模式成功或失败的原因。对策略损失的进一步分析表明，尽管它符合直觉，但性能仍然下降：相关项衰减而二次惩罚项持续存在。在分析指导下，我们提出最优系数校准（OCC），一种自适应方案，通过对数概率代理以可忽略的成本在线追踪最优系数。在六个竞赛级数学推理基准上，OCC一致匹配或超过分离基线，实现了改进的联合MTP-RL训练性能。

## Abstract
Reinforcement Learning from Verifiable Rewards (RLVR) has emerged as the standard paradigm for improving reasoning capability of large language models, while Multi-Token Prediction (MTP) has been a widely adopted module in pretraining. Combining them is a natural approach, yet current RL practices detach MTP gradients because joint training degrades the performance. We revisit this failure from an optimization perspective. We show that the per-step effect of MTP on the RL objective can be decomposed into two terms: a first-order correlation and a second-order perturbation penalty. This decomposition unifies three MTP training regimes: Detach, Cross-Entropy loss, and Policy loss, and explains why each succeeds or fails. Further analysis of policy loss reveals that, although it aligns with intuition, performance still degrades: the correlation term decays while the quadratic penalty persists. Guided by the analysis, we propose Optimal Coefficient Calibration (OCC), an adaptive scheme that tracks the optimal coefficient online via a log-probability proxy at negligible cost. Across six competition-level mathematical reasoning benchmarks, OCC consistently matches or exceeds the detach baseline, delivering improved joint MTP-RL training performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在大语言模型的后训练中，强化学习（RL）与多令牌预测（MTP）联合训练时会导致主模型性能严重下降，因此现有框架（如veRL、slime）都推荐将MTP梯度从主模型分离（Detach）。但分离后MTP只能通过事后微调适配主模型，丧失了多令牌监督的潜在收益。
- **核心问题**：为什么联合MTP-RL训练会失败？如何设计一种原则性的策略使其成功？
- **整体含义**：作者从优化角度分析了失败原因，将MTP对RL目标的影响分解为一阶相关项和二阶惩罚项，并基于此提出了最优系数校准（OCC）方法，实现了联合训练且性能稳定提升。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 理论分析框架（L-平滑性分解）
- 假设RL目标函数J(θ)是L-平滑的，则参数更新θ_{t+1}=θ_t+η(g_RL+λ g_MTP)后的每步改进下界为：
  \[
  \Delta_{\text{MTP}} = \eta\lambda(1-L\eta)\underbrace{\langle g_{\text{RL}}, g_{\text{MTP}}\rangle}_{\text{一阶相关项}} - \frac{L\eta^2\lambda^2}{2}\underbrace{\|g_{\text{MTP}}\|^2}_{\text{二阶惩罚项}}
  \]
- **三种训练模式的统一解释**：
  - **Detach**：g_MTP=0 ⇒ Δ_MTP=0，不影响主模型。
  - **Cross-Entropy Loss**：由于RL优势函数均值为0，且交叉熵梯度与RL梯度不相关，一阶项期望为0，只剩负的惩罚项，导致性能退化。
  - **Policy Loss**：MTP使用与主模型相同的RL目标，初期梯度方向一致（相关项大），改进为正；但后期相关项衰减，惩罚项持续存在，导致性能先升后降（相变）。

### 2.2 最优系数校准（OCC）
- **核心思想**：每步动态调整MTP系数λ，使Δ_MTP最大化。最优λ* = (1-Lη)c/(Lη v²)，其关键取决于比率 c/v²。
- **关键技术细节**：
  - **对数概率代理**：避免计算昂贵的全模型梯度，使用小步更新下对数概率变化δ = log π_θ - log π_old作为代理。
  - **在线统计量**：对齐代理 ĉ_t = ⟨δ_RL, δ_MTP⟩，方差代理 v̂²_t = ∥δ_MTP∥²。
  - **自适应系数**：λ_t = λ⁺ · ĉ_t / (v̂²_t + ε)，其中λ⁺为吸收平滑因子的预设标量（默认1.0），ε为防止除零的微小常数。
- **复杂度**：仅需计算对数概率变化，无需完整反向传播，每步仅增加约0.04秒（与Detach的8.11秒相比几乎可忽略），而全模型梯度需45.06秒（5.6倍慢）。

## 3. 实验设计：数据集、Benchmark、对比方法

### 3.1 训练数据
- DAPO-Math-17k（17k数学推理提示），沿用前期工作中的标准设置。

### 3.2 评估基准
- AIME24、AIME25（竞赛级数学）、AMC、MATH、Minerva、OlympiadBench，共6个基准，覆盖不同难度和来源。
- 指标：avg@32准确率（32次采样平均）。

### 3.3 对比方法
- **Detach**：MTP梯度与主模型分离（当前默认做法）。
- **CE Loss**：MTP使用交叉熵损失，梯度回传主模型（系数固定0.1）。
- **Policy Loss**：MTP使用与主模型相同的RL目标，系数固定0.1。
- **OCC**：本文提出的自适应方法。

### 3.4 模型与算法
- **基模型**：MiMo-7B-RL（7B参数稠密模型，带MTP头）和GLM-4.5-Air（106B-A12B MoE模型，带MTP头）。
- **RL算法**：DAPO（主要）、GSPO（验证算法无关性）。

## 4. 资源与算力

- **硬件**：128块NVIDIA H20 GPU。
- **超参数**：训练200步，batch size 128，学习率1e-6，温度1.0。
- **训练时间**：每步约8秒（Detach和OCC），全模型梯度计算每步约45秒（5.6倍慢）。总训练时间未明确给出，但可根据步数估算（约200步×8秒≈27分钟）。

## 5. 实验数量与充分性

- **主要实验结果**（Table 1）：在2种模型×2种RL算法×6个基准上，共4组对比（Detach/CE/Policy/OCC），结果明确：CE最差，Policy先优后劣，OCC最好。
- **消融实验**：
  - 图2、图8验证对数概率代理与真实验证梯度相关项的保真度（相关系数0.78和0.74）。
  - 图4展示λ_t的在线自适应曲线（开始大、后期小）。
  - 图5对比训练时间开销。
  - 图6展示训练奖励曲线（CE退化、Policy先升后降、OCC最高）。
  - 图7、Table 2比较Policy Loss不同固定系数（λ=0.1/0.2/0.5/1.0），均不如OCC。
  - Table 3进行λ⁺敏感性分析（0.1~2.0），1.0最优。
- **充分性评价**：实验设计全面，覆盖不同模型规模、RL算法、多基准、消融验证，对比公平，结论可靠。但仅涉及数学推理领域，未涉及其他RLHF场景。

## 6. 论文的主要结论与发现

1. **CE Loss联合训练必然退化**：因一阶相关项为零，只留下负的惩罚项。
2. **Policy Loss联合训练先升后降**：初期梯度方向一致，后期相关项衰减而惩罚项持续，导致相变。
3. **固定系数无法应对相变**：任何静态λ在后期都导致负收益。
4. **OCC自适应校准有效**：通过低成本的对数概率代理动态调整λ，一致匹配或超过Detach基线，平均准确率提升2.8~4.3个百分点（具体数值见Table 1）。
5. **模型与算法通用性**：在MiMo-7B和GLM-4.5-Air、DAPO和GSPO上都有效。
6. **计算开销极小**：仅增加不到1%的训练时间，远小于计算全模型梯度。

## 7. 优点

- **理论创新**：首次从优化角度统一解释三种MTP训练模式的行为，揭示了联合训练失败的数学根源（相变）。
- **方法简洁有效**：OCC仅需计算对数概率变化，无需修改训练框架或增加大量计算，即可稳定提升性能。
- **实验扎实**：涵盖多种模型规模（7B稠密模型到106B MoE）、多种RL算法（DAPO/GSPO）、6个基准，消融充分验证代理保真度与敏感性。
- **实用性强**：可即插即用到现有RL训练流程（如veRL），解除了当前强制梯度分离的限制。

## 8. 不足与局限

- **理论假设限制**：L-平滑假设在非凸深度学习中可能不精确，推导的下界提供的是定性指导而非定量保证。
- **代理近似局限**：对数概率代理是一阶近似，在大学习率或激进策略更新下可能失真。
- **实验范围局限**：仅验证了数学推理（可验证奖励），未推广到RLHF（基于学习奖励模型）或开放生成任务。
- **预设超参数λ⁺**：虽然对λ⁺（0.1~2.0）鲁棒，但最优值可能随训练配置变化，需预先实验选定。
- **未讨论MTP头自身训练效果**：仅关注主模型性能，未分析MTP头本身的预测精度变化。

（完）
