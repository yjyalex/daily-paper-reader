---
title: Tree-based Credit Assignment for Multi-Agent Memory System
title_zh: 面向多智能体记忆系统的基于树的信用分配
authors: "Marina Mao, Alexandr Liu, Pengbo Li, Siheng Li, Bo Zhou, Xiang Wang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04811v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体记忆系统的强化学习信用分配
tldr: 针对大模型长程任务中的多智能体记忆系统（构建、摘要、检索），现有强化学习方法存在奖励信号模糊或标注成本高的问题。本文提出 TreeMem，通过将多智能体流水线扩展为树状结构，利用蒙特卡洛平均估算各智能体对最终奖励的贡献，实现无需特定任务标注的精准信用分配，显著提升了记忆系统的性能。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆系统优化方法要么奖励信号过于粗糙，要么依赖高成本的人工标注，难以有效提升各环节智能体的协作效率。
method: 提出 TreeMem 框架，将智能体交互过程建模为树状结构，通过对后续分支进行蒙特卡洛采样来计算每个智能体的特定信用评分。
result: 在长程任务基准测试中，TreeMem 优于现有的强基准模型，证明了树状信用分配在多智能体记忆系统中的有效性。
conclusion: TreeMem 成功将全局奖励转化为智能体特定的优化信号，为异构多智能体系统的协同进化提供了一种高效且无需额外标注的方案。
---

## 摘要
记忆系统被广泛用于增强大语言模型（LLMs）处理长程任务的能力，通常被组织为包含记忆构建、摘要和检索智能体的多智能体流水线。为了增强该系统，现有的基于强化学习（RL）的方法要么对所有智能体统一应用最终的下游任务奖励（如问答准确率），这种方式较为粗糙且模糊；要么为不同子任务上的智能体设计特定任务的奖励，这需要昂贵的标注（如关键证据）且难以可靠地定义。为了解决这些局限性，我们提出了面向多智能体记忆系统的基于树的信用分配（TreeMem），它在无需特定任务标注的情况下，从最终奖励中推导出特定于智能体的信用。具体而言，TreeMem 将多智能体流水线（构建者-摘要者-检索者）扩展为树状结构，其中每个智能体的输出都被扩展为多个后续分支。每个智能体的贡献通过对其后续分支进行蒙特卡洛平均来估计，从而捕捉中间智能体动作如何影响最终奖励。这将粗糙的最终奖励转化为特定于智能体的优化信号。这些信号随后被用于同时更新所有智能体的策略，帮助异构智能体有效地实现专业化。在长程基准测试上的实验表明，TreeMem 相比强基准模型提升了记忆系统的性能，验证了树状结构信用分配在多智能体记忆系统中的有效性。

## Abstract
Memory systems are widely adopted to enhance LLMs for long-horizon tasks, and are commonly organized as multi-agent pipelines with memory building, summarizing, and retrieval agents. To empower this system, existing RL-based methods either apply final downstream task rewards (e.g., QA accuracy) for all agents uniformly, which are coarse and ambiguous, or design task-specific rewards for agents on different subtasks, which require costly annotations (e.g., key evidence) and are difficult to define reliably. To address these limitations, we propose Tree-based Credit Assignment for Multi-Agent Memory Systems (TreeMem), which derives agent-specific credit from the final reward without task-specific annotations. Specifically, TreeMem extends the multi-agent pipeline (builder--summarizer--retrieval) into a tree structure, where each agent's outputs are expanded into multiple subsequent branches. The contribution of each agent is estimated via Monte Carlo averaging over its subsequent branches, capturing how intermediate agent actions may influence the final reward. This converts the coarse final reward into agent-specific optimization signals. These signals are then used to update all agent policies simultaneously, helping heterogeneous agents specialize effectively. Experiments on long-horizon benchmarks show that TreeMem improves memory system performance over strong baselines, validating the effectiveness of tree-structured credit assignment for the multi-agent memory system.

---

## 论文详细总结（自动生成）

以下是对论文《Tree-based Credit Assignment for Multi-Agent Memory System》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在处理长程任务时，常采用由**构建（Builder）**、**摘要（Summarizer）**和**检索（Retrieval）**智能体组成的多智能体记忆系统。然而，优化这些异构智能体面临“信用分配”难题：
*   **现有方法一（统一奖励）：** 仅使用最终任务结果（如问答准确率）奖励所有智能体。这导致信号过于粗糙且模糊，无法指导特定角色的专业化。
*   **现有方法二（特定任务奖励）：** 为每个环节设计中间奖励（如证据覆盖率）。这需要高昂的人工标注成本，且容易引发“奖励作弊（Reward Hacking）”，即智能体优化了中间指标却损害了最终表现。

**TreeMem 的核心动机**是在无需额外人工标注的情况下，从最终奖励中精准推导出每个智能体的贡献度（信用），从而实现多智能体系统的协同进化。

### 2. 方法论：核心思想与技术细节
TreeMem 将多智能体流水线建模为**树状结构**，通过分支采样和回溯平均来估算信用：
*   **树状展开（Tree-Structured Rollout）：**
    *   **构建层：** 针对同一历史背景采样 $G$ 个构建动作。
    *   **摘要层：** 针对每个构建动作采样 $J$ 个摘要分支。
    *   **响应层：** 针对每个摘要采样 $K$ 个最终回答分支。
    *   这种结构形成了一个包含 $G \times J \times K$ 条完整轨迹的候选树。
*   **信用分配（Credit Assignment）：**
    *   **响应者信用：** 直接取最终奖励 $R$。
    *   **摘要者信用：** 对其后续的 $K$ 个响应分支奖励取平均。
    *   **构建者信用：** 对其后续所有的 $J \times K$ 个分支奖励取平均，并加入**长度惩罚项**（防止其通过简单复制长历史来规避摘要压力）。
*   **策略优化：** 采用类似 GRPO 的组相对优化策略。从树中随机抽取轨迹组，将上述估算的信用归一化为优势函数（Advantage），同时更新三个智能体的策略，促使它们在各自职责上实现专业化。

### 3. 实验设计
*   **数据集：**
    *   **PersonaMem：** 核心测试集，包含 32K、128K、1M 三种长度的历史对话，涵盖 7 类个性化问答任务。
    *   **LongMemEval & LOCOMO：** 用于验证通用长程记忆能力的外部基准。
*   **对比方法（Baselines）：**
    *   **上下文方法：** 直接输入（Base）、原始 RAG。
    *   **提示词驱动记忆：** CAM、MemoryBank、A-Mem、Mem0。
    *   **RL 优化记忆：** Mem1、Mem-$\alpha$、Memory-R1、CoMAM（最强竞争对手）。
*   **模型骨干：** 使用 Qwen2.5 和 Llama-3 系列模型（3B/7B/8B 规模）。

### 4. 资源与算力
*   **硬件设施：** 论文明确提到使用了由 **8 台 NVIDIA A100 GPU** 组成的集群。
*   **训练细节：** 训练周期为 5 个 Epoch，Batch Size 为 128，最大输出长度 3k Token。
*   **效率分析：** 虽然树状采样增加了计算量，但论文指出通过采样优化，单步训练时长仅略高于基础 RL 方法，且由于信用分配更精准，模型收敛速度更快。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 32K 到 1M Token 的极端长文本场景，实验结果基于至少 3 次独立运行的平均值。
*   **消融实验：** 针对“构建者”和“摘要者”分别做了移除树状信用的对比实验，验证了每一环智能体都能从该机制中获益。
*   **敏感度分析：** 对分支参数 $G, J, K$ 进行了详尽的网格搜索，证明了系统在不同超参数下的鲁棒性。
*   **客观性：** 引入了 GPT-4o 作为第三方评测（LLM-Judge），并对比了 Token 效率（压缩比），实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能领先：** TreeMem 在所有长程基准测试中均显著优于现有 RL 优化方法，准确率提升幅度在 3.2% 至 8.8% 之间。
*   **专业化诱导：** 树状信用分配能有效引导智能体各司其职。例如，构建者学会了提取关键事实而非冗余信息，摘要者学会了更高层次的抽象。
*   **Token 效率：** 相比其他方法，TreeMem 生成的记忆更简洁（Token 占用更少），但保留的信息价值更高。

### 7. 优点
*   **无需标注：** 解决了多智能体系统依赖中间环节人工标注的痛点。
*   **端到端对齐：** 信用分配直接源于最终任务奖励，确保了中间动作的优化方向与最终目标一致。
*   **结构优雅：** 将复杂的信用分配问题转化为直观的蒙特卡洛树搜索/平均问题，具有较强的理论解释性。

### 8. 不足与局限
*   **训练开销：** 尽管论文声称效率可控，但 $G \times J \times K$ 的分支采样在训练阶段依然比单轨迹 RL 消耗更多的推理算力。
*   **任务局限：** 目前主要在问答（QA）类任务上验证，对于更复杂的开放式生成或多步规划任务，树的深度和宽度可能需要指数级增加。
*   **冷启动依赖：** 初始阶段如果随机采样的分支全部失败（奖励全为 0），系统可能难以获得有效的启动信号。

（完）
