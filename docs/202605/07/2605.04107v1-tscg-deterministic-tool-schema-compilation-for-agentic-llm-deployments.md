---
title: "TSCG: Deterministic Tool-Schema Compilation for Agentic LLM Deployments"
title_zh: TSCG：面向智能体大语言模型部署的确定性工具模式编译
authors: Furkan Sakizli
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.04107v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 将JSON模式转换为Token高效的结构化文本
tldr: "TSCG是一个确定性工具模式编译器，旨在解决生产级智能体框架中JSON格式与大模型理解不匹配的问题。针对小模型在处理复杂工具调用时的高失败率，TSCG通过8个可组合算子将JSON转换为高效的结构化文本，在不依赖模型微调或运行时搜索的情况下，显著提升了模型（尤其是4B-14B模型）的工具调用准确率，并实现了超过50%的Token节省。"
selection_source: fresh_fetch
motivation: 现有的JSON工具模式格式是为机器解析设计的，而非为语言模型理解设计，导致小模型在处理大规模工具目录时表现极差。
method: 提出一种包含8个可组合算子的确定性编译器，将JSON模式转换为Token效率高且易于模型理解的结构化文本。
result: "在多个基准测试中，TSCG使Phi-4等模型在复杂场景下的准确率从0%提升至84%以上，并节省了52-57%的Token消耗。"
conclusion: TSCG证明了表示形式的改变是提升模型工具调用能力的关键，为不同规模模型的生产级部署提供了高效、轻量级的解决方案。
---

## 摘要
生产级智能体框架（OpenAI 函数调用、Anthropic 工具使用、MCP）将工具模式（tool schemas）以 JSON 格式传输，这种格式是为机器解析而设计的，而非为了让语言模型理解。对于小模型（4B-14B），在生产级目录规模下，这种协议不匹配是导致工具使用失败的主要原因。我们提出了 TSCG，一种确定性工具模式编译器，它在 API 边界处解决这种不匹配问题，在无需模型访问、微调或运行时搜索的情况下，将 JSON 模式转换为 Token 高效的结构化文本。TSCG 结合了八个可组合算子，并具有形式化的压缩界限（在格式良好的模式上 ≥51%）。在 TSCG-Agentic-Bench（约 19,000 次调用，12 个模型，5 个场景）上，TSCG 在 20 个工具的情况下将 Phi-4 14B 的准确率从 0% 恢复到 84.4%（在 50 个工具时为 90.3%），并在 BFCL 上的三个模型中实现了 108-181% 的准确率保留率。格式与压缩的分解分析（R^2 从 0.88 降至 0.03）确立了表示形式的变化是其主导机制。在三个前沿模型上的逐算子隔离实验揭示了三种不同的算子响应特征：算子渴求型（Opus 4.7）、算子敏感型（GPT-5.2）和算子鲁棒型（Sonnet 4），为各模型的部署提供了指导。扩展性实验表明，尽管在轻量级合成目录上趋于饱和，但在重型生产级 MCP 模式（约 10,500 个输入 Token 时 +5.0 个百分点）上，准确率优势依然存在，且全程节省了 52-57% 的 Token。合成基准测试可以推广到真实的 MCP 模式，准确率误差在 0.1 以内。TSCG 以一个 1,200 行、零依赖的 TypeScript 软件包形式发布。

## Abstract
Production agent frameworks (OpenAI Function Calling, Anthropic Tool Use, MCP) transmit tool schemas as JSON, a format designed for machine parsing, not for interpretation by language models. For small models (4B-14B), this protocol mismatch accounts for the majority of tool-use failure at production catalog sizes. We present TSCG, a deterministic tool-schema compiler that resolves this mismatch at the API boundary, converting JSON schemas into token-efficient structured text without model access, fine-tuning, or runtime search. TSCG combines eight composable operators with a formal compression bound (>=51% on well-formed schemas).   On TSCG-Agentic-Bench (about 19,000 calls, 12 models, 5 scenarios), TSCG restores Phi-4 14B from 0% to 84.4% accuracy at 20 tools (90.3% at 50 tools) and achieves 108-181% accuracy-retained ratio across three models on BFCL. Format-versus-compression decomposition (R^2=0.88 -> 0.03) establishes representation change as the dominant mechanism. Per-operator isolation across three frontier models reveals three distinct operator-response profiles: operator-hungry (Opus 4.7), operator-sensitive (GPT-5.2), and operator-robust (Sonnet 4), providing per-model deployment guidance. Scaling experiments show accuracy advantages persisting on heavy production MCP schemas (+5.0 pp at about 10,500 input tokens) despite saturation on light synthetic catalogs, with 52-57% token savings throughout. The synthetic benchmark generalizes to real MCP schemas within 0.1 accuracy points. TSCG ships as a 1,200-line zero-dependency TypeScript package.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TSCG（Token-Context Semantic Grammar）** 的确定性工具模式编译器，旨在优化大语言模型（LLM）在智能体（Agent）部署中的工具调用效率与准确性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的智能体框架（如 OpenAI、Anthropic、MCP）普遍使用 **JSON 格式**传输工具定义（Tool Schemas）。然而，JSON 是为机器解析设计的，而非为自回归语言模型理解设计的。
*   **研究背景**：
    *   **能力断层**：对于 4B–14B 的小型模型，当工具数量增加时，JSON 格式的复杂性会导致调用准确率崩溃（从 >15 个工具开始降至 0-49%）。
    *   **资源浪费**：JSON 结构冗余，单次调用可能消耗 3,000–25,000 个 Token。
    *   **协议不匹配**：作者认为这是一个“协议适配”问题，而非单纯的提示词压缩问题。

### 2. 方法论：核心思想与关键技术
TSCG 的核心思想是通过一个**确定性的编译器**，在 API 边界将 JSON 模式转换为 Token 高效且符合模型注意力机制的结构化文本。
*   **八大可组合算子**：
    1.  **TAS (Tokenizer-Aligned Syntax)**：选择 Token 数量最少的定界符（如将 `->` 替换为单个 Token 的符号）。
    2.  **CFL (Constraint-First Layout)**：利用“注意力锚点”效应，将输出约束置于提示词开头。
    3.  **CFO (Causal-Forward Ordering)**：按拓扑顺序排列多步操作，确保因果可访问性。
    4.  **SDM (Semantic Density Maximization)**：移除 100 多种无意义的填充词（如礼貌用语、冗余连接词）。
    5.  **DRO (Delimiter-Role Optimization)**：用紧凑的定界符替换冗长的结构化短语。
    6.  **CCP (Causal Closure Principle)**：在末尾添加闭合块，利用模型的近期偏见（Recency Bias）。
    7.  **CAS (Causal Access Score)**：根据重要性和位置距离对工具进行重新排序。
    8.  **SAD-F (Selective Anchor Duplication)**：在预算范围内重复关键信息锚点。
*   **算法流程**：采用 10 步转换流水线（从解析、压缩到结构化重组），整个过程是确定性的（相同输入必得相同输出），无需模型访问或微调。

### 3. 实验设计
*   **数据集/场景**：构建了 **TAB (TSCG-Agentic-Bench)** 基准测试，包含约 19,000 次 API 调用。
    *   **场景 A-E**：涵盖 Claude 代码目录、MCP 服务器集合、扩展性压力测试（25-100 个工具）、小模型阈值测试等。
    *   **外部验证**：使用 Berkeley Function Calling Leaderboard (BFCL) 和 GSM8K 进行交叉验证。
*   **对比方法**：
    *   **Natural (FC)**：供应商原生的函数调用 API。
    *   **Natural (text)**：将完整 JSON 嵌入文本。
    *   **Naive Truncation**：仅保留签名，删除描述。
    *   **LLMLingua-2**：通用的提示词压缩方法。
*   **测试模型**：涵盖 12 个模型，包括前沿模型（GPT-5.2, Claude 4.7 Opus, Sonnet 4）和本地小模型（Phi-4 14B, Mistral 7B, Gemma 3, Qwen3 等）。

### 4. 资源与算力
*   **硬件环境**：本地模型运行在 **2 × NVIDIA 5070 Ti (16GB)** 上，使用 Ollama (Q4_K_M 量化)。
*   **运行效率**：TSCG 编译器本身是一个 1,200 行的 TypeScript 包，执行时间 **<1ms**。相比之下，LLMLingua-2 处理相同提示词需要 42.5 秒。
*   **API 使用**：前沿模型通过 Anthropic 和 OpenAI 的官方 API 访问。

### 5. 实验数量与充分性
*   **实验规模**：总计约 **19,000 次调用**，这在同类论文中属于非常大规模的评估。
*   **充分性**：
    *   进行了**逐算子隔离实验**（Leave-one-in），分析了每个算子对不同模型的贡献。
    *   进行了**格式与压缩的分解分析**，证明了“表示形式改变”是性能提升的主因。
    *   使用了 Holm–Bonferroni 校正的统计显著性检验，实验设计客观且严谨。

### 6. 主要结论与发现
*   **小模型救星**：TSCG 使 Phi-4 14B 在 20 个工具场景下的准确率从 **0% 提升至 84.4%**。
*   **Token 节省**：在保持或提升准确率的同时，平均节省 **50-72%** 的 Token 消耗。
*   **模型特征分类**：
    *   **算子渴求型 (Opus 4.7)**：所有算子均有正面贡献。
    *   **算子敏感型 (GPT-5.2)**：对特定排序算子敏感，需精简配置。
    *   **算子鲁棒型 (Sonnet 4)**：对大多数转换具有免疫性，表现稳定。
*   **机制发现**：对于小模型，性能提升主要来自“JSON 到文本”的格式转换；对于前沿模型，提升则来自结构优化。

### 7. 优点
*   **确定性与轻量化**：不依赖 GPU 推理，无随机性，极易集成到现有生产环境。
*   **理论支撑**：算子设计基于 Transformer 的注意力机制（如 Attention Sink）和 BPE 分词特性。
*   **显著的经济效益**：通过大幅减少 Token 使用量，直接降低了智能体运行成本。

### 8. 不足与局限
*   **语言限制**：目前仅针对**英语**进行了优化和测试，多语言支持需额外开发。
*   **小模型退化风险**：在工具数量极少（<10个）的特定情况下，过度压缩可能导致小模型准确率轻微下降。
*   **基准测试偏差**：虽然进行了外部验证，但核心 TAB 基准是自建的，可能存在一定的场景偏向。
*   **模型更新风险**：随着前沿模型（如 GPT-5.5 等）的迭代，其内置的 JSON 处理能力可能会增强，从而缩小 TSCG 的优势空间。

（完）
