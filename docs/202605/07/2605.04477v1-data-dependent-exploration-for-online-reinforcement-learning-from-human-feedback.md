---
title: Data-dependent Exploration for Online Reinforcement Learning from Human Feedback
title_zh: 面向在线人类反馈强化学习的数据依赖型探索
authors: "Zhen-Yu Zhang, Yuting Tang, Jiandong Zhang, Lanjihong Ma, Masashi Sugiyama"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04477v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 在线人类反馈强化学习（RLHF）的数据依赖探索
tldr: 本研究针对在线人类反馈强化学习（RLHF）中的探索难题，提出了DEPO方法。传统方法因依赖难以准确估计的策略内期望，易导致模型过早忽略潜在高价值区域。DEPO通过利用历史数据构建数据相关的不确定性奖励，引导模型探索高不确定性区域。理论证明其具有更紧的遗憾界，实验表明其在多个基准测试中显著提升了样本效率。
selection_source: fresh_fetch
motivation: 现有的在线RLHF探索策略难以从有限的历史偏好数据中可靠估计不确定性，导致模型过早放弃探索潜在的高价值行为。
method: 提出DEPO方法，利用历史数据构建额外的不确定性奖励，鼓励模型向高不确定性且潜在高价值的区域进行探索。
result: 理论上证明了该算法具有数据相关的遗憾界，实验上在多个基准测试中一致优于强基线模型，显著提高了样本效率。
conclusion: DEPO是一种简单且可扩展的在线RLHF探索方法，能有效平衡探索与利用，提升大语言模型的对齐效率。
---

## 摘要
在线人类反馈强化学习（RLHF）已成为对齐大语言模型（LLM）的一种极具前景的范式，它通过在训练过程中持续收集新的偏好反馈来实现。在此设定下，一个基础性挑战是探索，这要求算法能够使 LLM 生成具有信息量的比较，从而提高在线 RLHF 的样本效率。现有的探索策略通常通过同策略（on-policy）期望来获取奖励，而这些期望很难从训练期间有限的历史偏好数据中进行可靠估计；结果，策略可能会过早地降低那些可能包含高价值行为的未充分探索区域的权重。在本文中，我们提出了面向偏好优化的数据依赖型探索（DEPO），这是一种简单且可扩展的方法，它利用历史数据为高不确定性区域构建额外的不确定性奖励，从而鼓励对潜在高价值数据的探索。在理论上，我们为所提算法提供了一个数据依赖型遗憾界（regret bound），证明了它能够适应学习任务本身的难度，并且在实践中比最坏情况下的界限更紧。在实验上，所提方法在各个基准测试中始终优于强基准模型，证明了样本效率的提升。

## Abstract
Online reinforcement learning from human feedback (RLHF) has emerged as a promising paradigm for aligning large language models (LLMs) by continuously collecting new preference feedback during training. A foundational challenge in this setting is exploration, which requires algorithms that enable the LLMs to generate informative comparisons that improve sample-efficiency in online RLHF. Existing exploration strategies often derive bonuses via on-policy expectations, which are difficult to estimate reliably from the limited historical preference data available during training; as a result, the policy can prematurely down-weight under-explored regions that may contain high-value behaviors. In this paper, we propose data-dependent exploration for preference optimization (DEPO), a simple and scalable method that leverages historical data to construct an extra uncertainty bonus for high-uncertainty regions, encouraging exploration toward potentially high-value data. Theoretically, we provide a data-dependent regret bound for the proposed algorithm, showing that it adapts to the hardness of the learning task itself and can be tighter than worst-case bounds in practice. Empirically, the proposed method consistently outperforms strong baselines across benchmarks, demonstrating improved sample efficiency.

---

## 论文详细总结（自动生成）

这篇论文由来自 RIKEN、东京大学等机构的研究者撰写，提出了一种名为 **DEPO** 的新算法，旨在解决在线人类反馈强化学习（RLHF）中的探索难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在线 RLHF 通过在训练中实时收集偏好反馈来对齐大语言模型（LLM）。然而，现有的探索策略（如 XPO, POPO）通常依赖“同策略（on-policy）”期望来计算探索奖励。
*   **核心问题**：由于训练初期的历史偏好数据覆盖范围有限，同策略估计往往不准确。这导致模型可能过早地判定某些未充分探索的区域为“低价值”，从而停止采样，错失潜在的高价值行为。
*   **整体含义**：论文旨在引入一种**数据依赖型（Data-dependent）**的探索机制，通过追踪历史数据在特征空间中的覆盖情况，显式地引导模型去探索那些“数据稀缺”但“潜力巨大”的区域。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：借鉴多臂老虎机中的**上置信界（UCB）**原理，在表示空间（Representation Space）中构建探索奖励。
*   **关键技术细节**：
    1.  **特征提取**：利用 LLM 最后一层隐藏状态的均值池化作为响应的表示 $\phi(x, y)$，并使用随机投影降维。
    2.  **成对特征**：将两个候选响应的特征拼接，形成成对特征向量 $\psi(x, y, y')$。
    3.  **协方差矩阵更新**：维护一个正则化协方差矩阵 $V_t$，记录历史所有比较过的样本在特征空间中的分布。
    4.  **数据依赖奖励**：探索奖励 $b_t$ 定义为基于 $V_t^{-1}$ 的椭圆置信半径。当某个方向的特征在历史数据中出现较少时，$V_t^{-1}$ 对应的项就大，从而产生更高的探索奖励。
    5.  **优化目标**：在 DPO（直接偏好优化）的目标函数基础上，增加由该置信半径加权的探索项。
*   **算法流程**：在每一轮迭代中，采样 Prompt 并生成响应，查询人类（或模型）偏好，更新历史数据库和协方差矩阵的逆（通过 Sherman-Morrison 公式高效更新），随后优化策略。

### 3. 实验设计
*   **数据集/场景**：
    *   **通用能力**：MMLU, GPQA, TruthfulQA, GSM8k。
    *   **对话与指令遵循**：AlpacaEval 2.0 (AE2), MT-bench。
    *   **特定领域**：IID（领域内对齐）、Alpaca（通用对齐）。
    *   **代码生成**：LiveCodeBench（使用执行结果作为反馈）。
*   **基准模型（Base Model）**：Llama-3-8B-SFT。
*   **对比方法（Baselines）**：
    *   **DPO**：无显式探索的基准。
    *   **XPO**：基于隐式 $Q^*$ 近似的探索方法。
    *   **POPO**：基于偏好概率的探索方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到了使用双精度浮点数进行协方差矩阵运算以保证数值稳定性，特征维度投影至 512 维。实验进行了 3 轮在线迭代。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础知识、逻辑推理到代码生成的 8 个主流基准测试，并进行了 3 轮迭代的动态演进观察。
*   **消融实验**：针对探索强度参数 $c_b$、采样策略（同策略 vs 混合采样）以及 KL 散度系数 $\beta$ 进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅对比了最终性能，还展示了训练过程中的胜率变化趋势。引入代码生成任务（LiveCodeBench）证明了该方法在可验证任务上的有效性，增强了实验的说服力。

### 6. 主要结论与发现
*   **性能领先**：DEPO 在几乎所有基准测试中均优于 DPO、XPO 和 POPO，尤其是在需要深度探索的复杂任务（如代码、数学）上提升显著。
*   **样本效率**：通过数据依赖的奖励，DEPO 能在更少的迭代次数内达到更高的对齐质量。
*   **理论验证**：理论分析给出的遗憾界（Regret Bound）表明，DEPO 的收敛速度取决于任务的“表示多样性”，在实际应用中比传统的最坏情况界限更紧。

### 7. 优点
*   **自适应性**：探索奖励随历史数据动态调整，避免了盲目探索。
*   **计算高效**：利用 Sherman-Morrison 公式更新矩阵逆，无需每轮进行高昂的矩阵求逆运算，适合大规模 LLM 训练。
*   **理论与实践结合**：既有严谨的遗憾界证明，又有在 8B 参数模型上的强力实验支撑。

### 8. 不足与局限
*   **表示依赖性**：算法的效果高度依赖于预定义的特征提取方式（如均值池化和随机投影），如果表示空间不能很好地捕捉语义差异，探索效率会下降。
*   **超参数敏感性**：虽然有消融实验，但探索权重 $\alpha$ 和置信半径缩放因子 $c_b$ 在不同任务间可能需要精细调优。
*   **算力透明度**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
