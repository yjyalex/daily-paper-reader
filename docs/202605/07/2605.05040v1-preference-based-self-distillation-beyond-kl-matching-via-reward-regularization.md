---
title: "Preference-Based Self-Distillation: Beyond KL Matching via Reward Regularization"
title_zh: 基于偏好的自蒸馏：通过奖励正则化超越 KL 匹配
authors: "Xin Yu, Liuchen Liao, Yiwen Zhang, Yingchen Yu, Lingzhou Xue, Qinzhen Guo"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05040v1"
tags: ["query:token"]
score: 9.0
evidence: 用于推理性能的密集Token级训练信号
tldr: 针对在线自蒸馏中KL匹配导致的不稳定和推理性能下降问题，本文提出基于偏好的自蒸馏（PBSD）框架。该方法不再直接匹配教师分布，而是通过奖励正则化目标推导出优化的目标策略，利用奖励加权的教师分布引导学习。实验证明，PBSD在数学推理和工具使用任务上显著提升了模型性能和训练稳定性，且在特定条件下优于外部教师蒸馏。
selection_source: fresh_fetch
motivation: 传统的在线自蒸馏依赖KL匹配，存在训练不稳定、缺乏探索多样性以及推理性能随时间退化的问题。
method: 提出PBSD框架，通过奖励正则化目标推导出奖励加权的教师分布，并优化教师与学生样本间的偏好差距。
result: 在多个模型规模的数学推理和工具使用基准测试中，PBSD在性能和训练稳定性上均优于现有的自蒸馏基准方法。
conclusion: PBSD通过引入奖励引导的偏好学习，克服了传统自蒸馏的局限性，为提升大语言模型的推理能力提供了一种高效且稳定的方案。
---

## 摘要
同策蒸馏（On-policy distillation）是强化学习的一种高效替代方案，能够提供密集的词元级（token-level）训练信号。然而，由于其对更强大的外部教师模型的依赖，促使了近期关于同策自蒸馏的研究，即同一个模型在不同的提示词上下文下同时充当教师和学生。然而，现有的自蒸馏方法在很大程度上将学习简化为针对上下文增强的教师模型的 KL 匹配。这种方法通常面临训练不稳定的问题，并可能随着时间的推移降低推理性能。此外，利用提示词增强从同一模型进行自蒸馏，缺乏真实外部教师所能提供的探索多样性。为了解决这些局限性，我们超越了固定教师的 KL 匹配，提出了基于偏好的自蒸馏（PBSD），从奖励正则化的视角重新审视同策自蒸馏。我们没有直接匹配教师分布，而是推导出一个奖励正则化目标，其解析最优解是一个经过奖励重加权的教师分布，从而在该目标下产生一个证明优于原始教师的目标策略。在实践中，PBSD 在保持同策学生采样的同时，优化了教师和学生样本之间的偏好间隙。我们通过对诱导偏好学习问题的统计分析来支持这一框架，正式确定了在我们的设定下，同策自蒸馏何时优于从外部教师学习。在多个模型规模的数学推理和工具使用基准测试上的实验表明，PBSD 在同类基准方法中始终保持最强的平均性能，在保持词元效率的同时，展现出比以往自蒸馏基准更好的训练稳定性。

## Abstract
On-policy distillation is an efficient alternative to reinforcement learning, offering dense token-level training signals. However, its reliance on a stronger external teacher has driven recent work on on-policy self-distillation, where the same model serves as both teacher and student under different prompt contexts. Yet, existing self-distillation methods largely reduce learning to KL matching toward the context-augmented teacher model. This approach often suffers from training instability and can degrade reasoning performance over time. Moreover, self-distillation from the same model with prompt augmentation lacks the exploratory diversity provided by a genuine external teacher. To address these limitations, we move beyond fixed-teacher KL matching and propose \textbf{P}reference-\textbf{B}ased \textbf{S}elf-\textbf{D}istillation (\textbf{PBSD}), which revisits on-policy self-distillation through a reward-regularized perspective. Instead of directly matching the teacher distribution, we derive a reward-regularized objective whose analytic optimum is a reward-reweighted teacher distribution, yielding a target policy provably superior to the original teacher under this objective. Practically, PBSD optimizes preference gaps between teacher and student samples while maintaining on-policy student sampling. We support this framework with a statistical analysis of the induced preference-learning problem, formally establishing when on policy self-distillation is preferable to learning from an external teacher in our setting. Experiments on mathematical reasoning and tool-use benchmarks across multiple model scales demonstrate that PBSD consistently achieves the strongest average performance among comparable baselines, showing improved training stability over prior self-distillation baselines while preserving token efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：基于偏好的自蒸馏（PBSD）

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）后训练阶段的**同策自蒸馏（On-policy Self-Distillation）**。
*   **背景**：传统的同策蒸馏依赖强大的外部教师模型，成本高且存在词表不兼容等问题。自蒸馏通过给同一模型提供“特权上下文”（如思维链示例）来充当教师，解决了外部依赖。
*   **核心问题**：现有的自蒸馏方法大多采用 **KL 散度匹配**（让学生直接模仿教师的概率分布）。这种方式存在三大缺陷：
    1.  **训练不稳定**：推理性能常随训练进行而退化。
    2.  **推理质量下降**：模型倾向于生成更短、过度自信但缺乏自我纠错能力的推理轨迹。
    3.  **缺乏探索性**：由于教师和学生参数相同，仅靠提示词增强提供的监督信号多样性不足。

#### 2. 方法论：核心思想与技术细节
论文提出了 **PBSD（Preference-Based Self-Distillation）**，将自蒸馏从“分布匹配”提升为“奖励优化”。
*   **核心思想**：不再让学生盲目模仿教师，而是通过一个**奖励正则化目标**，推导出最优目标策略应该是“经过奖励重加权的教师分布”。这意味着学生应该学习教师生成的样本中那些具有更高潜在奖励的部分。
*   **关键技术细节**：
    *   **奖励加权目标**：通过数学证明（Proposition 1），最优策略 $\pi^*$ 与教师策略 $\pi_{teach}$ 的关系为 $\pi^* \propto \pi_{teach} \cdot \exp(r/\beta)$。这表明目标策略在教师的支持域内，向高奖励区域偏移。
    *   **偏好学习转化**：由于潜在奖励 $r$ 不可直接观测，PBSD 将其转化为偏好优化问题。它将教师生成的响应视为正例（$y^+$），学生同策采样的响应视为负例（$y^-$）。
    *   **损失函数**：采用类似 DPO 的对数几率损失（Logistic Loss），通过优化教师与学生样本之间的偏好间隙（Margin）来更新参数。
    *   **统计分析**：论文通过统计学证明，当教师和学生分布较近（自蒸馏场景）且教师能提供多样化方向时，PBSD 的样本复杂度更优，能比外部教师蒸馏更有效地收敛。

#### 3. 实验设计
*   **任务场景**：
    1.  **数学推理**：评估模型在复杂逻辑下的思维链能力。
    2.  **工具使用（Tool Use）**：评估模型将用户意图转化为结构化 API 调用（Agent 能力）的准确性。
*   **数据集与 Benchmark**：
    *   数学：OpenThoughts 训练集（30K）；测试集包括 AIME 2024、AIME 2025、HMMT 2025。
    *   工具使用：ToolAlpaca 训练集（4K）；测试集包含 94 个复杂场景。
*   **对比方法**：
    *   基础模型（Qwen3-Instruct）。
    *   监督微调（SFT）。
    *   强化学习方法：GRPO、DAPO。
    *   自蒸馏基准：OPSD（直接 KL 匹配）、SDFT、SRPO。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   采用 LoRA（Rank=64, Alpha=128）进行参数高效微调。
    *   使用 AdamW 优化器，bfloat16 精度，FlashAttention 2 加速。
    *   训练步数为 500 步，每 50 步进行一次评估。
    *   推理端使用 vLLM 框架以提高同策采样效率。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 **Qwen3 的三个规模（1.7B, 4B, 8B）**，验证了方法的可扩展性。
*   **充分性**：
    *   实验不仅对比了最终精度，还通过消融实验分析了**训练稳定性**（展示了 PBSD 不会像 OPSD 那样在后期性能崩塌）。
    *   进行了**词元效率（Token Efficiency）**分析，证明 PBSD 在达到相同精度时比 GRPO 等强化学习方法节省大量算力。
    *   包含**个案研究（Case Study）**，分析了 30 个具体难题的解决情况。
*   **公平性**：所有对比方法在相同的算力预算、基础模型和超参数框架下运行，确保了结果的客观性。

#### 6. 主要结论与发现
*   **性能领先**：PBSD 在所有模型规模和所有任务（数学与工具使用）上均取得了最强的平均性能。例如在 Qwen3-8B 上，数学平均分比最强基准 OPSD 高出 1.0 分。
*   **稳定性卓越**：与传统的 KL 匹配自蒸馏不同，PBSD 在整个训练周期内性能持续提升，没有出现明显的退化现象。
*   **保留探索性**：PBSD 生成的推理轨迹长度适中，保留了必要的思维步骤，避免了因过度模仿教师而导致的推理链萎缩。
*   **理论验证**：理论分析与实验结果一致，证明了在自蒸馏框架下，利用偏好学习比单纯的分布模仿更具统计稳健性。

#### 7. 优点
*   **理论与实践结合**：从奖励正则化的数学推导出发，为自蒸馏提供了坚实的理论基础。
*   **高效性**：继承了自蒸馏的词元效率，无需像传统 RL 那样进行大规模的奖励评估和重复采样。
*   **通用性**：不仅适用于长链推理，在结构化的工具调用任务中同样表现出色。
*   **稳定性**：解决了在线蒸馏中常见的“灾难性遗忘”或性能退化问题。

#### 8. 不足与局限
*   **依赖上下文质量**：PBSD 的效果高度依赖于“特权上下文”能否诱导出高质量的教师响应。如果提示词增强无法显著提升教师表现，则偏好信号会变弱。
*   **局部假设**：理论分析基于局部 MLE 复杂性，假设模型在目标参数附近是良态的，在极端分布偏移下的表现仍需进一步探索。
*   **算力门槛**：虽然比 RL 高效，但在线采样（On-policy rollout）仍然比离线 SFT 消耗更多推理算力。

（完）
