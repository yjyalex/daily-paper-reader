---
title: "Power Distribution Bridges Sampling, Self-Reward RL, and Self-Distillation"
title_zh: 幂分布连接了采样、自奖励强化学习与自蒸馏
authors: "Akiyoshi Tomihari, Issei Sato"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04542v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习与LLM推理能力的关系
tldr: 本研究揭示了幂分布（Power Distribution）是连接采样、自奖励KL正则化强化学习与自蒸馏的关键桥梁。研究证明幂分布是特定RL目标的闭式最优解，并据此提出幂自蒸馏方法，将高成本的幂采样过程转化为离线监督训练。实验表明，该方法在推理任务中能以更低的推理成本实现与幂采样相当或更优的性能，同时阐明了真实奖励提升与自奖励相关性的内在逻辑。
selection_source: fresh_fetch
motivation: 旨在理清强化学习、推理采样与蒸馏技术在提升大语言模型性能中的内在联系与统一框架。
method: 提出幂自蒸馏（Power Self-Distillation），通过将幂分布作为目标分布进行离线蒸馏，将采样成本摊销至训练阶段。
result: 实验证明幂自蒸馏在推理任务上能以极低的推理成本达到甚至超过幂采样的性能表现。
conclusion: 幂分布统一了采样与强化学习，证明了自蒸馏可以作为实现自奖励强化和性能提升的有效手段。
---

## 摘要
最近的分析质疑强化学习（RL）是否是大语言模型（LLM）强大推理能力的根本原因。与此同时，蒸馏和推理时采样（包括幂采样）已成为提高 LLM 性能的有效手段。然而，RL、蒸馏和采样之间的关系仍不明确。在本研究中，我们关注幂分布（即幂采样的目标分布），并证明了幂分布连接了采样、自奖励 KL 正则化 RL 和自蒸馏。从采样角度看，我们展示了在缺乏可能后缀信息的情况下，廉价的局部近似无法重现序列级幂分布。从 RL 角度看，当模型的序列级对数概率被用作奖励时，幂分布是 KL 正则化 RL 的闭式优化器。这一发现引出了幂自蒸馏，这是一种离线蒸馏替代方案，它共享相同的目标分布，并将幂采样的成本摊销到对教师样本的监督训练中。我们进一步表明，幂自蒸馏可以实现自奖励锐化，而下游真实奖励的提升则受幂分布下真实奖励与自奖励之间协方差的支配。推理任务上的实验支持了我们的分析：幂采样提高了自奖励，真实奖励的增益取决于其与自奖励的一致性，且幂自蒸馏能以更低的推理成本达到或超过幂采样的性能。

## Abstract
Recent analyses question whether reinforcement learning (RL) is responsible for strong reasoning in large language models (LLMs). At the same time, distillation and inference-time sampling, including power sampling, have emerged as effective ways to improve LLM performance. However, the relationship among RL, distillation, and sampling remains unclear. In this study, we focus on the power distribution, the target distribution of power sampling, and show that the power distribution bridges sampling, self-reward KL-regularized RL, and self-distillation. From the sampling perspective, we show that inexpensive local approximations cannot reproduce sequence-level power without information about possible suffixes. From the RL perspective, the power distribution is the closed-form optimizer of KL-regularized RL when the model's sequence-level log-probabilities are used as the reward. This identification leads to power self-distillation, an offline distillation surrogate that shares the same target distribution and amortizes the cost of power sampling into supervised training on teacher samples. We further show that power self-distillation can achieve self-reward sharpening, while improvement in a downstream true reward is governed by the covariance between true reward and self-reward under the power distribution. Experiments on reasoning tasks support our analysis: power sampling raises self-reward, true-reward gains depend on alignment with self-reward, and power self-distillation can match or exceed the performance of power sampling at much lower inference cost.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）中的采样、强化学习（RL）与自蒸馏技术进行了深入的理论统一与实证研究。以下是该论文的结构化总结：

### 1. 研究动机与核心问题
*   **背景**：近期研究质疑 RL 是否是 LLM 涌现推理能力的根本原因，认为其本质可能只是“分布锐化”（Distribution Sharpening）。同时，推理时计算（如幂采样）和蒸馏技术也能显著提升性能，但三者之间的内在联系尚不清晰。
*   **核心问题**：能否找到一个统一的框架来解释采样、RL 和蒸馏？能否将高昂的推理时采样成本转化为更高效的离线训练？
*   **整体含义**：论文证明了**幂分布（Power Distribution）**是连接这三者的桥梁，并提出了一种通过离线蒸馏来模拟复杂采样效果的新方法。

### 2. 方法论：幂自蒸馏（Power Self-Distillation）
*   **核心思想**：将幂分布 $\pi_\alpha(y|x) \propto \pi(y|x)^\alpha$ 识别为自奖励 KL 正则化 RL 的闭式最优解，并以此作为教师分布进行蒸馏。
*   **关键技术细节**：
    *   **理论关联**：证明了当奖励函数 $r$ 为模型的自对数概率（Self-reward）时，KL 正则化 RL 的最优策略即为幂分布。
    *   **局部近似的局限性**：通过数学证明（命题 1 和 2）指出，简单的逐 Token 温度缩放无法替代序列级的幂采样，因为后者需要考虑后续序列（Suffix）的信息。
    *   **算法流程**：
        1.  **离线采样**：使用 Metropolis-Hastings (MH) 等算法从原模型 $\pi$ 的幂分布中抽取样本。
        2.  **监督微调（SFT）**：在这些样本上对模型进行标准监督训练，将推理时的计算压力“摊销”到训练阶段。
    *   **性能预测**：提出“协方差条件”，即真实奖励的提升取决于幂分布下真实奖励与自奖励之间的协方差。

### 3. 实验设计
*   **数据集**：
    *   数学推理：MATH (MATH500)。
    *   代码生成：HumanEval, MBPP。
    *   科学问答：GPQA。
*   **模型**：Qwen2.5-Math-7B, Qwen2.5-7B, Phi-3.5-mini-instruct。
*   **对比方法（Baselines）**：
    *   标准采样（Standard Sampling）。
    *   逐 Token 温度缩放（Temperature Scaling）。
    *   推理时幂采样（Power Sampling, MH 算法）。
    *   随机初始化模型（RandW，作为负对照）。

### 4. 资源与算力
*   **硬件**：使用配备 8 张 NVIDIA A100 (40GB) GPU 的节点。
*   **耗时**：
    *   **训练**：每个任务的 SFT 过程在单卡上不到 1 小时即可完成。
    *   **采样（教师生成）**：这是主要开销，每个数据集的幂采样生成需要超过 1 天的时间。
*   **总算力**：累计消耗约数百个 A100 GPU 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构/规模的模型和 4 个主流推理基准测试。
*   **消融与验证**：
    *   进行了不同温度/幂指数 $\alpha$ 的对比。
    *   设计了合成奖励实验来验证协方差理论。
    *   使用了随机权重模型排除偶然性。
*   **评价**：实验设计客观、公平，通过多模型、多任务的交叉验证，充分证明了幂自蒸馏在不同场景下的普适性。

### 6. 主要结论与发现
*   **统一性**：幂分布确实是采样、自奖励 RL 和自蒸馏的共同目标。
*   **性能提升**：幂自蒸馏后的模型在标准采样下，性能往往能达到甚至超过原模型进行高成本幂采样的水平。
*   **推理效率**：蒸馏模型在推理时仅需标准自动回归解码，极大地降低了推理延迟和计算成本。
*   **奖励对齐**：只有当模型的自奖励（似然概率）与真实奖励（正确性）正相关时，这种锐化机制才能提升下游任务表现。

### 7. 优点
*   **理论深刻**：为 LLM 的自改进（Self-improvement）提供了坚实的数学基础，解释了为什么“左手倒右手”的训练能起作用。
*   **实用性强**：提供了一种将昂贵的推理时搜索转化为廉价模型权重的可行路径。
*   **洞察力**：明确了逐 Token 采样与序列级采样的本质区别（后缀熵的影响）。

### 8. 不足与局限
*   **依赖基座能力**：如果基座模型本身极弱（如随机权重），自蒸馏无法凭空产生推理能力。
*   **采样成本转移**：虽然降低了推理成本，但离线生成高质量幂采样数据的过程依然非常耗时。
*   **数据规模**：实验中使用的训练集规模较小（约 500 样本），在大规模数据上的扩展性（Scaling Laws）尚待进一步验证。

（完）
