---
title: "Moira: Language-driven Hierarchical Reinforcement Learning for Pair Trading"
title_zh: Moira：面向配对交易的语言驱动分层强化学习
authors: "Polydoros Giannouris, Yuechen Jiang, Lingfei Qian, Yuyan Wang, Xueqing Peng, Jimin Huang, Guojun Xiong, Sophia Ananiadou"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01954v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用带有LLM参数化策略的分层强化学习进行复杂推理。
tldr: 本研究针对配对交易中高层语义选择与底层执行之间的分层决策难题，提出了Moira框架。该框架将配对交易建模为分层强化学习问题，利用大语言模型（LLM）分别担任高层和底层策略。通过轨迹和情节级的文本反馈进行提示词优化，而非梯度微调，有效解决了信用分配和延迟反馈问题。实验证明，该方法在真实市场数据上优于传统及现有LLM基准，展示了语言驱动分层强化学习在复杂金融场景下的有效性。
selection_source: fresh_fetch
motivation: 旨在解决配对交易中由于高层抽象选择与底层执行相互交织导致的信用分配困难及反馈延迟问题。
method: 提出一种由大语言模型驱动的分层强化学习框架，通过纯提示词更新和文本反馈来优化高低两层策略。
result: 在真实市场数据实验中，Moira框架的表现一致优于传统配对交易方法和现有的基于LLM的基准模型。
conclusion: 通过显式分离抽象层与执行层并利用语言反馈进行优化，可以显著降低分层决策中的非平稳性并增强模型的适应能力。
---

## 摘要
许多序列决策问题表现出分层结构，其中高层语义选择约束了下游动作，且反馈具有延迟性和模糊性。由于信用分配（credit assignment）问题，在此类场景中进行学习具有挑战性：性能下降可能源于错误的抽象、次优的执行或两者的相互作用。我们通过配对交易（pair trading）来研究这一挑战，该领域自然地结合了用于资产对选择的长周期语义推理和部分可观测性下的短周期执行。我们将配对交易建模为一个分层强化学习问题，并提出了一种语言驱动的优化框架，其中高层和底层策略均由大语言模型（LLMs）参数化，并完全通过提示词（prompt）更新进行优化。我们的方法利用预训练的 LLM 作为分层策略，并使用轨迹级和回合级的文本反馈来调整抽象和执行，而无需进行基于梯度的微调。通过显式地将抽象选择与执行分离，该框架减少了跨层级的非平稳性，并能够在延迟反馈下实现有针对性的自适应。在真实市场数据上的实验表明，该方法相比传统基准和基于 LLM 的基准均有持续提升，证明了语言驱动的分层强化学习的有效性。

## Abstract
Many sequential decision-making problems exhibit hierarchical structure, where high-level semantic choices constrain downstream actions and feedback is delayed and ambiguous. Learning in such settings is challenging due to credit assignment: performance degradation may arise from flawed abstractions, suboptimal execution, or their interaction. We study this challenge through pair trading, a domain that naturally combines long-horizon semantic reasoning for asset pair selection with short-horizon execution under partial observability. We formulate pair trading as a hierarchical reinforcement learning problem and propose a language-driven optimization framework in which both high-level and low-level policies are parameterized by large language models (LLMs) and optimized exclusively through prompt updates. Our approach leverages pretrained LLMs as hierarchical policies and uses trajectory- and episode-level textual feedback to adapt abstractions and execution without gradient-based fine-tuning. By explicitly separating abstraction selection from execution, the framework reduces non-stationarity across hierarchical levels and enables targeted adaptation under delayed feedback. Experiments on real-world market data show consistent improvements over traditional and LLM-based baselines, demonstrating the effectiveness of language-driven hierarchical reinforcement learning.

---

## 论文详细总结（自动生成）

以下是对论文《Moira: Language-driven Hierarchical Reinforcement Learning for Pair Trading》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**配对交易（Pair Trading）**中的复杂决策挑战。配对交易包含两个层级：高层的**资产对选择**（基于长期经济逻辑）和底层的**交易执行**（基于短期市场噪声）。
*   **核心痛点：** 信用分配（Credit Assignment）难题。当交易亏损时，很难确定是因为选股逻辑错误（抽象层失败）、交易时机不对（执行层失败），还是随机噪声干扰。
*   **研究动机：** 传统的强化学习（RL）或端到端大模型（LLM）方法往往将这两层混为一谈，导致反馈信号模糊。本文旨在利用 LLM 的语义推理能力，通过**语言作为接口**来显式分离抽象与执行，解决延迟反馈下的策略优化问题。

### 2. 方法论
Moira 框架将配对交易建模为**分层强化学习（HRL）**问题，完全通过**提示词优化（Prompt Optimization）**而非梯度微调来更新策略。
*   **核心思想：** 使用两个 LLM 代理，分别担任“选择器（Selector）”和“交易员（Trader）”，并引入“批评者（Critic）”通过文本反馈驱动策略演进。
*   **关键技术细节：**
    *   **高层选择器（Selector）：** 观察长期价格统计和新闻摘要，选择资产对并生成逻辑说明（Rationale）。它在每个回合（Episode）开始时运行一次。
    *   **底层交易员（Trader）：** 在固定资产对下，观察短期价差和局部新闻，执行买入/卖出/持有动作。
    *   **两阶段文本优化：**
        1.  **策略批评（Critique）：** Critic LLM 分析交易轨迹（Trajectory），识别错误模式，生成自然语言改进建议（即“文本梯度”）。
        2.  **提示词更新（Update）：** 将建议整合进原始提示词，生成更精细的交易规则（如：引入止损、观察新闻催化剂等）。
*   **算法流程：** 交易员在回合内进行多次子回合（Sub-episode）迭代优化；选择器则根据回合结束后的累计收益进行跨回合优化。

### 3. 实验设计
*   **数据集：** 美国股票市场真实数据（2025年1月至6月），涵盖 10 只高流动性股票（如 AAPL, NVDA, TSLA 等），包含每日收盘价及多源新闻摘要。
*   **Benchmark（基准方法）：**
    *   **统计方法：** GGR（最小距离法）、协整检验（Cointegration）、相关性分析（Correlation）。
    *   **强化学习方法：** TRIALS（一种现有的分层 RL 框架）。
    *   **LLM 基准：** Flat-LLM（不分层的单一 LLM 策略）。
*   **评估指标：** 年化收益率（AR）、夏普比率（SR）、索提诺比率（Sortino）、最大回撤（MDD）、卡玛比率（CR）等。

### 4. 资源与算力
*   **模型使用：** 实验主要基于 **DeepSeek-V3** API 进行。
*   **算力说明：** 论文**未明确说明**具体的 GPU 硬件型号或训练时长。由于该方法采用“纯提示词优化”而非梯度下降，其核心开销在于 API 调用产生的 Token 消耗（文中附录 C 提供了 Token 使用量分析，显示主要成本在于处理新闻上下文）。

### 5. 实验数量与充分性
*   **实验规模：** 进行了主实验对比、消融实验（验证分层结构和提示词优化的必要性）、提示词敏感性分析（验证鲁棒性）以及学习曲线分析。
*   **充分性评价：** 实验设计较为**充分且客观**。通过对比“仅选择不调优”和“仅调优不选择”的变体，清晰证明了分层架构的价值。敏感性分析通过多种语义等价的初始提示词进行测试，证明了性能提升源于优化过程而非“提示词工程”的运气。但受限于 API 成本，资产池规模（10只股票）相对较小。

### 6. 主要结论与发现
*   **性能卓越：** Moira 在年化收益（59.11%）和夏普比率（3.791）上显著优于所有统计基准和 RL 基准。
*   **分层必要性：** 扁平化（Flat）的 LLM 策略表现极差，证明了将长周期逻辑与短周期执行分离的必要性。
*   **语义优化的力量：** 随着优化步数的增加，LLM 自动演化出了复杂的交易规则（如：从简单的价差交易进化到结合新闻催化剂、动态止损和冷静期管理）。
*   **数据效率：** 相比需要数年数据训练的 RL 方法，Moira 仅需少量（约30天）的形成期数据即可达到高性能。

### 7. 优点
*   **可解释性强：** 策略以自然语言规则形式存在，人类可直接阅读和理解交易逻辑的演变。
*   **无需微调：** 避免了昂贵的梯度计算和专家标注数据，利用预训练模型的推理能力实现快速自适应。
*   **语义信用分配：** 通过文本反馈解决了传统数值奖励难以区分“选错股”还是“做错单”的问题。

### 8. 不足与局限
*   **资产规模限制：** 实验仅在 10 只股票的小范围内验证，在大规模资产池（如标普500）中的组合爆炸问题尚待研究。
*   **理论保证缺失：** 文本反馈作为“梯度”的收敛性和理论性质仍是一个开放性问题，缺乏严谨的数学证明。
*   **API 依赖与延迟：** 依赖外部 LLM API 可能面临推理延迟和成本问题，在极高频交易场景下可能不适用。
*   **回测偏差：** 实验周期较短（半年），且未深入探讨极端市场波动（如金融危机）下的鲁棒性。

（完）
