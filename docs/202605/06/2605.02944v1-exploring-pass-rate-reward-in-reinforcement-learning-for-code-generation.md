---
title: Exploring Pass-Rate Reward in Reinforcement Learning for Code Generation
title_zh: 探索代码生成强化学习中的通过率奖励
authors: "Xin-Ye Li, Ren-Biao Liu, Yun-Ji Zhang, Hui Sun, Zheng Xie, Ming Li"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.02944v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习算法与奖励机制在代码生成推理中的对比
tldr: 本研究探讨了在代码生成任务中，使用测试用例通过率（pass-rate）作为强化学习奖励的效果。虽然通过率奖励能缓解二进制奖励的稀疏性问题，但在对GRPO和RLOO等无评论员算法的实验中发现，它并不能稳定提升最终性能。分析表明，通过率并非衡量代码完全正确性的准确指标，且部分通过的解可能产生相互抵消的梯度方向，导致优化效率降低。
selection_source: fresh_fetch
motivation: 旨在解决代码生成强化学习中二进制奖励过于稀疏的问题，并验证测试用例通过率作为替代奖励的实际效果。
method: 在GRPO和RLOO等无评论员强化学习框架下，对比分析了通过率奖励与二进制奖励在多种基础模型上的表现。
result: 实验发现通过率奖励虽然提高了奖励密度，但在最终性能上并未能可靠地优于传统的二进制奖励。
conclusion: 通过率奖励因与完全正确性目标失配且易引发梯度冲突，不足以有效提升代码生成能力，需设计更一致的奖励机制。
---

## 摘要
基于单元测试反馈的强化学习（RL）已成为提升大语言模型（LLMs）代码生成能力的标准化后训练方案。然而，“通过所有测试”的二元奖励可能非常稀疏，在没有任何采样解能通过所有测试的挑战性问题上，无法产生学习信号。一种常见的补救措施是使用测试用例通过率作为代理奖励。在这项工作中，我们研究了代码生成中无评论员强化学习（critic-free RL，如 GRPO 和 RLOO）的通过率奖励，并在不同基座模型和算法中发现了一个一致的模式：尽管缓解了奖励稀疏性，但在严格的对照实验中，通过率奖励并不能比二元奖励可靠地提高最终性能。为了理解这种差异，我们分析了奖励密度及由此产生的梯度方向。我们发现，虽然通过率奖励更密集，但其诱导的梯度更新并不能持续地将概率质量移向全通过解。这是因为测试用例通过率是衡量完全正确性进展的一个失准代理，且同一组内的部分通过解可能会产生相互抵消的冲突梯度方向。总之，我们的结果表明，在无评论员强化学习中，通过率奖励不足以改进代码生成，并激励人们设计能使优化与完全正确性目标更好对齐的奖励机制。

## Abstract
Reinforcement learning (RL) from unit-test feedback has become a standard post-training recipe for improving large language models (LLMs) on code generation. However, the pass-all-tests binary reward can be sparse, yielding no learning signal on challenging problems where none of the sampled solutions passes all tests. A common remedy is to use the test-case pass rate as a surrogate reward.   In this work, we study pass-rate rewards in critic-free RL for code generation (e.g., GRPO and RLOO) and report a consistent pattern across base models and algorithms: despite alleviating reward sparsity, pass-rate rewards do not reliably improve final performance over binary rewards in rigorous controlled experiments.   To understand this discrepancy, we analyze reward density and the resulting gradient directions. We find that pass-rate rewards are denser, but the induced gradient updates do not consistently move probability mass toward full-pass solutions. This arises because test-case pass rate is a miscalibrated surrogate for progress toward full correctness, and partial-pass solutions within the same group can induce conflicting gradient directions that cancel out.   Overall, our results suggest that, in critic-free RL, pass-rate rewards are insufficient to improve code generation and motivate reward designs that better align optimization with the goal of full correctness.

---

## 论文详细总结（自动生成）

这篇论文对代码生成任务中强化学习（RL）的奖励机制进行了深入探讨，特别是针对“测试用例通过率”这一常用奖励信号的有效性提出了质疑。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在代码生成的强化学习中，传统的“二进制奖励”（只有通过所有测试才给分）存在严重的**奖励稀疏性**问题，导致模型在难题上难以获得学习信号。
*   **核心问题**：学术界和工业界常采用“测试用例通过率”（Pass-Rate）作为替代奖励以增加信号密度，但这种做法是否真的能提升模型的最终性能？
*   **整体含义**：论文通过严谨的对照实验证明，在无评论员（Critic-free）强化学习框架下，通过率奖励虽然增加了奖励密度，但并不能稳定提升模型性能，甚至在某些指标上不如二进制奖励。

### 2. 核心方法论
论文在统一的实验框架下对比了多种奖励设计：
*   **奖励函数定义**：
    *   **二进制奖励 ($R_{bin}$)**：通过全部测试用例为 1，否则为 0。
    *   **通过率奖励 ($R_{pass}$)**：通过的测试用例数除以总测试用例数。
    *   **加权通过率奖励**：根据测试用例的难度（由多个模型评估得出）分配权重，难点用例权重更高。
    *   **两阶段奖励**：训练初期使用通过率奖励进行热身，后期切换为二进制奖励。
*   **强化学习算法**：采用了两种主流的无评论员算法：
    *   **GRPO (Group Relative Policy Optimization)**：通过组内奖励归一化来估计优势（Advantage）。
    *   **RLOO (REINFORCE Leave-One-Out)**：通过留一法基准线来获得无偏梯度估计。
*   **分析技术**：使用**梯度探测（Gradient Probing）**技术，测量不同奖励诱导的梯度更新如何影响模型对“正确参考答案”的预测概率。

### 3. 实验设计
*   **数据集**：
    *   **LiveCodeBench (LCB)**：包含 2023 年至 2025 年的竞赛题目，防止数据污染。
    *   **LeetCodeDataset (LC)**：包含 2641 个训练题目和 228 个测试题目。
*   **基座模型**：
    *   推理模型：DeepSeek-R1-Distill-Qwen-7B。
    *   通用模型：Qwen3-4B、Qwen2.5-7B-Instruct。
*   **对比基准**：以二进制奖励作为 Baseline，对比通过率奖励及其变体在 Pass@1、Pass@4、Pass@16 等指标上的表现。

### 4. 资源与算力
*   **训练细节**：每个实验训练 768 个梯度更新步，Batch Size 为 64 个问题，每个问题采样 $N=16$ 个响应（Rollouts）。
*   **算力说明**：论文提到使用了 `verl` 分布式训练框架，但**未明确说明具体的 GPU 型号和数量**。考虑到模型规模（4B 到 7B）和采样量，预计消耗了相当规模的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构/规模的基座模型、2 种 RL 算法、2 个主流数据集以及 4 种奖励变体。
*   **充分性与客观性**：实验设计非常严谨，采用了严格的**在线（On-policy）训练**模式以排除重要性采样带来的干扰。此外，论文不仅关注最终分数，还进行了深度的机制分析（如任务重合度分析、梯度冲突分析），实验结果具有高度的说服力和客观性。

### 6 主要结论与发现
*   **性能天花板一致**：通过率奖励虽然在训练初期收敛更快，但最终的性能上限（Pass@k）并未超过二进制奖励。
*   **奖励失准（Miscalibration）**：高通过率并不等同于接近正确答案。某些错误的解（如暴力破解或针对测试用例的过拟合）可能获得高通过率，但其逻辑与正确解完全背离。
*   **组内梯度冲突**：在同一组采样中，部分通过的解会产生方向相反的梯度。这些“推-拉”效应在组内相互抵消，导致模型无法有效地向正确解的方向优化。
*   **任务重叠度极高**：两种奖励方式最终解决的问题集合高度重合（约 97%），说明通过率奖励并未能帮助模型“解锁”更多难题。

### 7. 优点
*   **挑战直觉**：挑战了“奖励越密集越好”的直觉，为代码生成 RL 领域提供了重要的负面结果参考。
*   **深度机制分析**：通过 LLM-as-judge 对错误类型进行分类，并结合梯度探测，从微观层面解释了为什么通过率奖励会失效。
*   **严谨的对照**：控制变量做得非常好，确保了结论的可靠性。

### 8. 不足与局限
*   **场景局限**：实验主要集中在 Python 竞赛编程题目上，尚未验证在更复杂的软件工程任务（如多文件修复、库级别开发）中的表现。
*   **算法局限**：主要针对无评论员（Critic-free）算法，对于带有独立 Critic 网络的 PPO 算法，通过率奖励是否能通过价值函数学习到更有用的信号尚待研究。
*   **奖励设计空间**：虽然测试了加权通过率，但仍属于基于结果的奖励（Outcome Reward），未涉及过程奖励（Process Reward）。

（完）
