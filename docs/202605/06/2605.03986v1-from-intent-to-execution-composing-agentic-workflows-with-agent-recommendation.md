---
title: "From Intent to Execution: Composing Agentic Workflows with Agent Recommendation"
title_zh: 从意图到执行：通过智能体推荐构建智能体工作流
authors: "Kishan Athrey, Ramin Pishehvar, Brian Riordan, Mahesh Viswanathan"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03986v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 自动化的多智能体任务分解与协调
tldr: 本研究针对多智能体系统（MAS）构建过程中依赖人工规划和选型的局限，提出了一种自动化构建框架。该框架集成了LLM规划器、动态调用图和基于两阶段检索（快速检索+LLM重排序）的智能体推荐系统。通过引入批评智能体对推荐结果进行全局审核，实现了从用户意图到执行流的端到端自动化，显著提升了系统构建的效率、召回率和可扩展性。
selection_source: fresh_fetch
motivation: 传统多智能体系统的构建高度依赖人工进行任务规划、智能体选择和执行图创建，难以应对复杂且多变的规模化应用需求。
method: 提出一种包含LLM规划器、两阶段智能体推荐系统及批评智能体的自动化框架，通过自然语言描述实现任务与智能体的动态匹配。
result: 实验表明，该方法在召回率上优于现有技术，且在处理大规模智能体注册库时表现出更强的鲁棒性和可扩展性。
conclusion: 引入批评智能体进行全局评估是提升智能体选择准确性的关键，该框架为实现从意图到执行的端到端自动化多智能体系统提供了有效路径。
---

## 摘要
使用 AI 智能体构建的多智能体系统 (MAS) 能够满足多种用户意图，可用于设计和构建一系列相关应用。然而，目前此类 MAS 的创建涉及手动编写计划、手动选择合适的智能体以及手动创建执行图。本文介绍了一种用于自动创建多智能体系统的框架，该框架用自动化流程取代了多个手动步骤。所提出的框架由软件模块和工作流组成，用于编排所需的特定任务应用。这些模块包括：一个基于大语言模型 (LLM) 的规划器、一组以自然语言描述的任务、一个动态调用图、一个用于将智能体映射到任务的编排器，以及一个从本地和全局智能体注册表中寻找最合适智能体的智能体推荐器。智能体推荐器采用了一个两阶段信息检索 (IR) 系统，由一个快速检索器和一个基于 LLM 的重排序器组成。我们进行了一系列实验，探索了嵌入器、重排序器、智能体描述增强以及监督评价智能体 (critique agent) 的选择。我们对该系统进行了端到端的基准测试，评估了所提方法在规划、智能体选择和任务完成方面的综合表现。实验结果表明，我们的方法在召回率方面优于现有技术，且与以往方法相比更具鲁棒性和可扩展性。评价智能体根据整体计划全面重新评估智能体和工具的推荐。我们证明了引入评价智能体能进一步提高召回率得分，这表明对基于任务的智能体选择进行全面审查和修正是构建端到端多智能体系统的关键步骤。

## Abstract
Multi-Agent Systems (MAS) built using AI agents fulfill a variety of user intents that may be used to design and build a family of related applications. However, the creation of such MAS currently involves manual composition of the plan, manual selection of appropriate agents, and manual creation of execution graphs. This paper introduces a framework for the automated creation of multi-agent systems which replaces multiple manual steps with an automated framework. The proposed framework consists of software modules and a workflow to orchestrate the requisite task- specific application. The modules include: an LLM-derived planner, a set of tasks described in natural language, a dynamic call graph, an orchestrator for map agents to tasks, and an agent recommender that finds the most suitable agent(s) from local and global agent registries. The agent recommender uses a two-stage information retrieval (IR) system comprising a fast retriever and an LLM-based re-ranker. We implemented a series of experiments exploring the choice of embedders, re- rankers, agent description enrichment, and supervising critique agent. We benchmarked this system end-to-end, evaluating the combination of planning, agent selection, and task completion, with our proposed approach. Our experimental results show that our approach outperforms the state-of-the- art in terms of the recall rate and is more robust and scalable compared to previous approaches. The critique agent holistically reevaluates both agent and tool recommendations against the overall plan. We show that the inclusion of the critique agent further enhances the recall score, proving that the comprehensive review and revision of task-based agent selection is an essential step in building end-to-end multi-agent systems.

---

## 论文详细总结（自动生成）

这篇论文由思科系统（Cisco Systems）的研究团队撰写，提出了一种名为 **AutoMAS** 的框架，旨在实现从用户自然语言意图到多智能体系统（MAS）执行的端到端自动化构建。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前构建多智能体系统（MAS）的过程高度依赖人工，包括手动分解任务、手动从成千上万个候选智能体中挑选合适的组件、以及手动设计执行逻辑图。随着专用智能体数量的爆炸式增长，这种人工模式无法扩展。
*   **核心问题**：如何在海量的智能体注册库中，根据用户模糊的意图，自动、高效、且鲁棒地规划任务并匹配最合适的智能体？
*   **整体含义**：论文提出了一套自动化流水线，解决了“智能体大海捞针”的选择难题，并引入动态调用图和批评机制，确保生成的系统既能满足性能约束（如低延迟、低成本），又具备容错能力。

### 2. 方法论
AutoMAS 框架包含以下关键技术模块：
*   **LLM 规划器 (Planner)**：将用户意图（如“帮我做一个订餐应用”）分解为自然语言描述的子任务序列或有限状态机（FSM）。
*   **智能体推荐器 (Agent Recommender, AR)**：
    *   **描述增强 (Enrichment)**：在索引阶段，利用 LLM 为每个智能体生成多个模拟查询（Synthetic Queries），丰富其元数据，提升检索覆盖率。
    *   **两阶段检索**：第一阶段使用混合搜索（向量嵌入 + 关键词）快速召回候选集；第二阶段使用高性能 LLM（如 GPT-4o 或 o1）进行语义重排序。
*   **变量调用图 (Variable Call Graph, VCG)**：根据推荐器返回的多个候选智能体，构建具备冗余路径的执行图。系统可根据实时环境（如某智能体宕机）或用户约束（如“选最便宜的”）动态切换执行路径。
*   **批评智能体 (Critique Agent)**：作为质量把关者，从全局视角评估推荐的智能体是否符合输入输出兼容性、安全标签及用户定义的成本/延迟约束。

### 3. 实验设计
*   **数据集**：
    *   **ToolE (MetaTool)**：包含 199 个工具和超过 20,000 个查询对，用于测试智能体推荐的准确性。
    *   **TaskBench**：包含 Dailylife、HuggingFace 和 Multimedia 三个领域的 API（共 103 个工具，17,000 个查询），用于评估端到端的任务规划与组合能力。
*   **对比方法 (Baselines)**：
    *   **Re-Invoke**：一种基于查询重写的工具检索方法。
    *   **Retriever-only**：仅使用向量检索的基准模型。
    *   **LLM-as-a-Judge**：使用 LLM 作为裁判，对比预测计划与标准答案（Oracle）的语义一致性。
*   **评估指标**：nDCG、Recall@K、mAP（检索指标）；Rouge、BERTScore、Edit Distance、Sequence Similarity（规划指标）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：实验主要基于 OpenAI 的 API 接口，使用了 `text-embedding-3-small/large` 进行嵌入，以及 `gpt-4o` 和 `o1` 作为重排序器和规划器。由于采用的是检索增强（RAG）架构而非从头训练大模型，其算力消耗主要集中在推理和索引阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对嵌入模型、重排序模型、混合搜索参数（Alpha 值）进行了多组消融实验。
    *   针对“描述增强”模块，测试了不同数量（3, 5, 10 个）模拟查询对性能的影响。
    *   在 TaskBench 的三个子集上进行了数万次查询的端到端测试。
*   **充分性评价**：实验设计较为充分且客观。通过引入“结构化预测评估”和“LLM 裁判”，弥补了传统文本匹配指标（如 Rouge）在评估逻辑流时的不足。

### 6. 主要结论与发现
*   **检索性能**：两阶段检索（检索 + 重排序）显著提升了精度，结合“描述增强”技术后，Recall 和 mAP 均优于 SOTA 方法（如 Re-Invoke）。
*   **批评机制的价值**：引入批评智能体能有效过滤不兼容的智能体，将 Recall@1 进一步提升了约 3.5%。
*   **可扩展性**：相比于将所有工具描述塞进 LLM 上下文的方法，AutoMAS 的检索架构在处理大规模智能体库时更具成本和延迟优势。
*   **LLM 偏见**：发现重排序器有时存在指令遵循问题，例如即使要求“高性能”，模型仍可能因预训练偏见而倾向于选择“低成本”智能体。

### 7. 优点
*   **端到端自动化**：实现了从模糊意图到结构化执行图的完整闭环。
*   **动态性与鲁棒性**：VCG 允许系统在运行时根据约束条件动态优化路径，而非死板的静态工作流。
*   **冷启动优化**：通过合成查询增强智能体描述，有效解决了智能体文档描述过于简略导致的检索失败问题。

### 8. 不足与局限
*   **候选集限制**：批评智能体目前只能在检索到的候选集中进行调整，如果第一阶段检索（Retriever）未能召回正确智能体，后续环节无法补救。
*   **语义指标局限性**：实验发现 Rouge 和 BERTScore 难以准确衡量工具调用的正确性，虽然引入了 LLM 裁判，但仍存在一定的评估主观性。
*   **偏见处理**：模型对成本/性能权衡的内在偏见尚未完全通过 Prompt 工程解决，可能需要更精细的微调。

（完）
