---
title: "Correct Is Not Enough: Training Reasoning Planners with Executor-Grounded Rewards"
title_zh: 仅有正确是不够的：利用执行器驱动的奖励训练推理规划器
authors: "Tianyang Han, Hengyu Shi, Junjie Hu, Xu Yang, Zhiling Wang, Junhao Su"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03862v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 基于执行器反馈的中间推理路径奖励
tldr: 针对大语言模型推理中仅依赖最终答案正确性导致推理过程不可靠、存在逻辑捷径等问题，本文提出了 TraceLift 框架。该框架将推理视为中间产物，通过“执行器锚定奖励”——即结合推理质量评分与执行器性能提升的综合指标——来训练规划器。研究还引入了 TRACELIFT-GROUPS 数据集以增强推理质量的可学习性。实验表明，该方法在代码和数学任务中显著提升了推理的忠实度与系统整体性能。
selection_source: fresh_fetch
motivation: 传统的仅基于结果的奖励信号容易导致模型产生逻辑错误或投机取巧的推理过程，缺乏对推理质量的直接监督。
method: 提出 TraceLift 框架，采用结合了推理奖励模型评分与执行器实际增益的锚定奖励机制，并利用包含扰动样本的 TRACELIFT-GROUPS 数据集进行训练。
result: 在代码和数学基准测试中，该方法在规划器-执行器系统上的表现优于仅依赖执行结果的训练方案。
conclusion: 有效的推理监督应当兼顾推理过程的逻辑质量及其对后续任务执行的实际贡献。
---

## 摘要
带有可验证奖励的强化学习已成为提高大语言模型显式推理能力的常用方法，但仅凭最终答案的正确性无法揭示推理轨迹（reasoning trace）是否忠实、可靠，或者对使用它的模型是否有用。这种仅关注结果的信号可能会强化“因错误原因而得出正确结果”的轨迹，通过奖励捷径来夸大推理收益，并在多步系统中传播有缺陷的中间状态。为此，我们提出了 TraceLift，这是一个将推理视为可消耗中间产物的规划器-执行器（planner-executor）训练框架。在规划器训练期间，规划器输出带有标签的推理。一个冻结的执行器将这些推理转化为最终产物以获取验证器反馈，同时通过执行器驱动的奖励来塑造中间轨迹。该奖励将基于评分细则（rubric）的推理奖励模型（RM）分数与在同一冻结执行器上测得的提升值（uplift）相乘，从而奖励那些既高质量又实用的轨迹。为了使推理质量直接可学习，我们引入了 TRACELIFT-GROUPS，这是一个基于数学和代码种子问题构建的、带有评分细则注释的纯推理数据集。每个样本都是一个同题组，包含一个高质量的参考轨迹和多个看似合理但有缺陷的轨迹，这些缺陷轨迹通过局部扰动降低了推理质量或对解法的支持度，同时保留了任务相关性。在代码和数学基准测试上的广泛实验表明，这种执行器驱动的推理奖励比仅基于执行的训练更能提升两阶段规划器-执行器系统，这表明推理监督不仅应评估轨迹是否“看起来不错”，还应评估它是否对使用它的模型有所帮助。

## Abstract
Reinforcement learning with verifiable rewards has become a common way to improve explicit reasoning in large language models, but final-answer correctness alone does not reveal whether the reasoning trace is faithful, reliable, or useful to the model that consumes it. This outcome-only signal can reinforce traces that are right for the wrong reasons, overstate reasoning gains by rewarding shortcuts, and propagate flawed intermediate states in multi-step systems. To this end, we propose TraceLift, a planner-executor training framework that treats reasoning as a consumable intermediate artifact. During planner training, the planner emits tagged reasoning. A frozen executor turns this reasoning into the final artifact for verifier feedback, while an executor-grounded reward shapes the intermediate trace. This reward multiplies a rubric-based Reasoning Reward Model (RM) score by measured uplift on the same frozen executor, crediting traces that are both high-quality and useful. To make reasoning quality directly learnable, we introduce TRACELIFT-GROUPS, a rubric-annotated reason-only dataset built from math and code seed problems. Each example is a same-problem group containing a high-quality reference trace and multiple plausible flawed traces with localized perturbations that reduce reasoning quality or solution support while preserving task relevance. Extensive experiments on code and math benchmarks show that this executor-grounded reasoning reward improves the two-stage planner-executor system over execution-only training, suggesting that reasoning supervision should evaluate not only whether a trace looks good, but also whether it helps the model that consumes it.

---

## 论文详细总结（自动生成）

以下是对论文《Correct Is Not Enough: Training Reasoning Planners with Executor-Grounded Rewards》的结构化总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
当前大语言模型（LLM）主要通过强化学习（RL）和可验证奖励（如答案正确性）来提升推理能力。然而，论文指出“仅关注结果”的信号存在严重缺陷：
*   **逻辑欺骗：** 模型可能通过错误的推理步骤得到正确答案（Right for the wrong reasons）。
*   **推理捷径：** 奖励信号可能强化模型投机取巧的行为，而非真正的逻辑构建。
*   **缺乏实用性：** 生成的推理轨迹可能对后续执行任务的模型（执行器）没有实际帮助。
**核心目标：** 训练一种推理规划器（Planner），使其生成的推理轨迹不仅在逻辑上高质量，而且能切实提升后续执行器（Executor）完成任务的成功率。

### 2. 论文提出的方法论
论文提出了 **TraceLift** 框架，将推理视为一种“可消耗的中间产物”。
*   **核心思想：** 引入“执行器锚定奖励”（Executor-Grounded Reward），将推理质量的内在评估与对执行器的实际增益（Uplift）相结合。
*   **关键技术细节：**
    1.  **规划器-执行器架构：** 规划器负责生成推理轨迹，一个冻结的执行器（Frozen Executor）根据该轨迹生成最终结果。
    2.  **TraceLift-Groups 数据集：** 包含 6,000 个推理组。每组包含一个高质量参考轨迹和多个通过局部扰动（如算法错误、计算失误）生成的缺陷轨迹，并由 LLM 根据多维度准则（Rubric）评分。
    3.  **推理奖励模型（Reasoning RM）：** 在上述数据集上训练，学习评估推理的内在质量（如理解力、策略、连贯性等）。
    4.  **奖励公式：** 
        $$R = 0.5 \times \text{Verifier\_Feedback} + 0.5 \times (\text{Reasoning\_RM\_Score} \times \text{Executor\_Uplift})$$
        其中，**Executor Uplift** 是指“有推理引导”与“无推理引导”时执行器成功率的差值。这确保了只有既高质量又对执行器有用的推理才会获得高奖励。

### 3. 实验设计
*   **数据集/场景：**
    *   **代码任务：** HumanEval, HumanEval+, MBPP-full, LiveCodeBench。
    *   **数学任务：** GSM8K, GSM-Hard, SVAMP, MATH500。
*   **Benchmark：** 以“Base 模型（两阶段推理）”和“仅执行奖励（Exec-only RL）”作为主要基准。
*   **对比方法：** 
    *   **Exec-only RL：** 仅使用最终答案正确性作为奖励。
    *   **No-uplift：** 仅使用推理 RM 评分，不结合执行器增益。
    *   **RM-uplift only：** 仅使用增益加权的 RM 评分，不包含最终正确性奖励。
    *   **LLM-as-judge：** 直接使用提示词驱动的 LLM 评分作为奖励。

### 4. 资源与算力
*   **模型规模：** 实验涵盖了 Qwen2.5-7B、Llama3.1-8B 和 Qwen3-4B 三个模型系列。
*   **训练细节：** 使用 GRPO 算法，训练 600 个 step，学习率为 $5 \times 10^{-6}$，bf16 精度。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号和数量，但提到在训练期间为了估计 Uplift，每个推理轨迹需要进行 $K=3$ 次执行器采样。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个模型家族、8 个主流基准测试上进行了广泛测试，并针对每个结论提供了 3 次不同种子的平均值。
*   **消融实验：** 针对执行器采样次数 $K$、奖励组成部分、LoRA 与全参数微调、推理长度与效用的关系等进行了深入的消融研究。
*   **充分性评价：** 实验设计非常充分且客观。通过跨领域（代码与数学）和跨模型规模的验证，证明了方法的普适性。同时，通过“直接自解”协议验证了模型并未因适配外部执行器而丧失自身能力。

### 6. 论文的主要结论与发现
1.  **TraceLift 显著优于仅结果奖励：** 在所有模型和基准测试中，TraceLift 均提升了系统的最终表现。
2.  **正确是不够的：** 实验证明，如果奖励不锚定在执行器的实际增益上，模型生成的推理可能看起来很完美但实际无效。
3.  **抑制冗长：** 相比 Exec-only RL，TraceLift 训练出的模型生成的推理更简洁（平均短 30 个 token），且效用更高，避免了 RL 训练中常见的“复读机”或过度冗长现象。
4.  **Uplift 信号的关键性：** 消融实验显示，移除 Uplift 权重会导致性能大幅下降，甚至低于基础 Exec-only 方法。

### 7. 优点（亮点）
*   **创新的奖励机制：** 首次提出将推理质量评分与执行器增益（Uplift）相乘，解决了推理轨迹的“信用分配”难题。
*   **高质量数据集：** TraceLift-Groups 通过局部扰动生成负样本，为推理过程的细粒度监督提供了极佳的资源。
*   **模块化视角：** 将推理视为规划器与执行器之间的接口，这种解耦有助于构建更可靠的复合 AI 系统。

### 8. 不足与局限
*   **计算开销：** 训练期间需要多次调用执行器来估计 Uplift，这显著增加了训练时的推理成本。
*   **执行器依赖：** 规划器可能会过度适配（Overfit）特定的冻结执行器，如果更换执行器，推理轨迹的有效性可能会下降。
*   **RM 偏差风险：** 推理 RM 的训练依赖于 LLM Judge 的初始标注，可能继承标注模型的偏见或幻觉。
*   **场景限制：** 目前主要集中在代码和数学等具有明确验证器（Verifier）的领域，在开放式写作或主观推理任务中的适用性尚待验证。

（完）
