---
title: "FINER-SQL: Boosting Small Language Models for Text-to-SQL"
title_zh: FINER-SQL：提升小语言模型在 Text-to-SQL 任务中的表现
authors: "Thanh Dat Hoang, Thanh Trung Huynh, Matthias Weidlich, Thanh Tam Nguyen, Tong Chen, Hongzhi Yin, Quoc Viet Hung Nguyen"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03465v1"
tags: ["query:token"]
score: 9.0
evidence: 基于细粒度执行的强化学习用于 Text-to-SQL 推理
tldr: 针对大语言模型在Text-to-SQL任务中计算成本高和隐私泄露的问题，本文提出FINER-SQL框架，旨在提升小语言模型（SLM）的性能。该框架利用细粒度执行反馈的强化学习，通过引入记忆奖励和原子奖励，将稀疏的二元监督转化为连续的学习信号。实验表明，3B规模的模型在BIRD和Spider基准上达到了与大模型相当的性能，同时显著降低了推理延迟，为高效私密的工业部署提供了可能。
selection_source: fresh_fetch
motivation: 传统强化学习在Text-to-SQL中因稀疏的二元奖励导致小模型训练不稳定且推理能力弱。
method: 提出基于细粒度执行反馈的强化学习框架，通过记忆奖励对齐推理轨迹并利用原子奖励提供操作级的局部信用评分。
result: "3B参数模型在BIRD和Spider上分别达到67.73%和85%的执行准确率，性能媲美大型模型且推理速度更快。"
conclusion: FINER-SQL为实现高效、私密且高性能的Text-to-SQL生成提供了一条极具成本效益的技术路径。
---

## 摘要
大语言模型推动了 Text-to-SQL 生成领域的重大进展。然而，它们面临计算成本高、延迟长以及数据隐私等问题，这使得它们在许多实际应用中并不实用。一个自然的替代方案是使用小语言模型（SLMs），它们能够实现高效且私密的本地部署。然而，SLMs 通常面临推理能力弱和指令遵循能力差的问题。基于稀疏二值奖励（0/1）的传统强化学习方法在生成的 SQL 错误时几乎无法提供学习信号，导致训练不稳定或崩溃。为了克服这些问题，我们提出了 FINER-SQL，这是一个可扩展且可重用的强化学习框架，通过细粒度的执行反馈来增强 SLMs。FINER-SQL 基于群体相对策略优化（GRPO），用密集且可解释的奖励取代了稀疏监督，即使对于错误的 SQL 也能提供连续反馈。它引入了两个关键的奖励函数：记忆奖励，通过将推理与验证轨迹对齐以保持语义稳定性；以及原子奖励，通过衡量操作级重叠，为结构正确但尚不完整的 SQL 提供部分分数。这种方法将离散的正确性转化为连续的学习过程，实现了稳定且无需评论者网络（critic-free）的优化。在 BIRD 和 Spider 基准测试上的实验表明，FINER-SQL 使用 3B 模型实现了高达 67.73% 和 85% 的执行准确率——在匹配更大规模 LLMs 性能的同时，将推理延迟降低至 5.57 秒/样本。这些结果凸显了一条通往高性能 Text-to-SQL 生成的低成本且保护隐私的路径。我们的代码可在 https://github.com/thanhdath/finer-sql 获取。

## Abstract
Large language models have driven major advances in Text-to-SQL generation. However, they suffer from high computational cost, long latency, and data privacy concerns, which make them impractical for many real-world applications. A natural alternative is to use small language models (SLMs), which enable efficient and private on-premise deployment. Yet, SLMs often struggle with weak reasoning and poor instruction following. Conventional reinforcement learning methods based on sparse binary rewards (0/1) provide little learning signal when the generated SQLs are incorrect, leading to unstable or collapsed training. To overcome these issues, we propose FINER-SQL, a scalable and reusable reinforcement learning framework that enhances SLMs through fine-grained execution feedback. Built on group relative policy optimization, FINER-SQL replaces sparse supervision with dense and interpretable rewards that offer continuous feedback even for incorrect SQLs. It introduces two key reward functions: a memory reward, which aligns reasoning with verified traces for semantic stability, and an atomic reward, which measures operation-level overlap to grant partial credit for structurally correct but incomplete SQLs. This approach transforms discrete correctness into continuous learning, enabling stable, critic-free optimization. Experiments on the BIRD and Spider benchmarks show that FINER-SQL achieves up to 67.73\% and 85\% execution accuracy with a 3B model -- matching much larger LLMs while reducing inference latency to 5.57~s/sample. These results highlight a cost-efficient and privacy-preserving path toward high-performance Text-to-SQL generation. Our code is available at https://github.com/thanhdath/finer-sql.

---

## 论文详细总结（自动生成）

这是一份关于论文《FINER-SQL: Boosting Small Language Models for Text-to-SQL》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
Text-to-SQL 任务旨在将自然语言转换为可执行的 SQL 语句。目前该领域由大语言模型（LLMs）主导，但 LLM 面临**计算成本高、推理延迟长、数据隐私风险**（需调用外部 API）等问题。
小语言模型（SLMs，如 0.5B-3B）虽然适合本地私密部署，但存在两大痛点：
*   **推理能力弱**：难以处理复杂的模式链接和逻辑组合。
*   **强化学习（RL）训练困难**：传统的 RL 依赖“执行正确与否”的二元奖励（0 或 1）。由于 SLM 生成的初始 SQL 大多错误，导致奖励信号极其稀疏，训练容易陷入停滞或崩溃。

### 2. 方法论：核心思想与关键技术
论文提出了 **FINER-SQL** 框架，通过**细粒度执行反馈**来增强 SLM。其核心流程分为两个阶段：

*   **阶段一：多样化推理蒸馏（Distillation）**
    *   从 DeepSeek-R1、GPT-4o 等多个顶级模型中提取推理链（Thought Traces）和 SQL，构建“推理库”。
    *   对 SLM 进行监督微调（SFT），使其学会 `<think> 推理 </think> SQL` 的结构化输出格式。

*   **阶段二：基于细粒度反馈的强化学习（RL）**
    *   采用**群体相对策略优化（GRPO）**算法，无需额外的 Critic 模型，降低显存占用。
    *   **核心奖励函数设计：**
        1.  **记忆奖励（Memory Reward）**：利用向量数据库（ChromaDB）存储验证过的正确推理轨迹。即使 SQL 执行失败，只要推理逻辑与库中成功的轨迹语义相似，也会给予奖励，从而稳定推理方向。
        2.  **原子奖励（Atomic Reward）**：将 SQL 解析为原子操作（如 SELECT_COL, JOIN, WHERE_PRED 等）。通过计算预测 SQL 与参考 SQL 在操作级别的重叠度（Jaccard 相似度），为“部分正确”的 SQL 提供连续的信用评分。
        3.  **格式奖励与执行奖励**：确保输出符合 XML 标签规范，并根据 SQL 是否可执行、结果是否匹配给予阶梯式奖励。

### 3. 实验设计
*   **数据集**：使用了 Text-to-SQL 领域最具挑战性的两个基准：**BIRD**（大规模、跨领域、含外部知识）和 **Spider**（跨领域复杂查询）。
*   **Benchmark 与对比方法**：
    *   **闭源模型**：DIN-SQL + GPT-4, CHASE-SQL + Gemini 1.5 等。
    *   **开源大模型**：CodeS (7B-15B), Alpha-SQL (7B-32B), SQL-R1 (7B-14B), ExCoT (70B) 等。
    *   **小模型基准**：T5-3B, CodeS-1B/3B 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA A6000 (48GB)** GPU。
*   **训练细节**：
    *   SFT 阶段：学习率 $2 \times 10^{-5}$，训练 2 个 epoch。
    *   GRPO 阶段：学习率 $8 \times 10^{-6}$，每样本生成 32 个 rollout，全局 Batch Size 为 32。
    *   **时长**：3B 模型的完整训练过程（SFT + RL）在 **2 天内**完成，0.5B 和 1.5B 模型速度更快。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **主实验**：在 BIRD 和 Spider 两个数据集上验证了不同参数规模（0.5B, 1.5B, 3B）的性能。
*   **消融实验**：分别移除了记忆奖励、原子奖励和格式奖励，验证了各组件对降低语法错误率和提升准确率的贡献。
*   **敏感性分析**：测试了采样温度、候选数量（Pass@K）、记忆检索数量 $k$ 对结果的影响。
*   **效率分析**：对比了推理延迟、显存占用和部署成本。
*   **定性分析**：展示了 RL 训练后推理链如何从冗长混乱变得简洁高效。
**评价**：实验设计非常充分，涵盖了从性能到效率、从宏观指标到微观行为的全面评估，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：FINER-SQL 3B 模型在 BIRD 上达到 **67.73%** 的执行准确率，超越了许多 14B 甚至 70B 的开源模型，并接近 GPT-4 的水平。
*   **高效率**：3B 模型的推理延迟仅为 **5.57s/样本**，显存占用约 **10GB**，意味着可以在消费级显卡（如 RTX 4090/5070）上流畅运行。
*   **推理演化**：经过 RL 训练，SLM 的推理长度显著缩短（从平均 3K+ 字符降至 0.8K 左右），逻辑变得更加目标导向。
*   **解决稀疏奖励**：原子奖励和记忆奖励成功解决了 SLM 在 RL 过程中的冷启动和训练崩溃问题。

### 7. 优点与亮点
*   **奖励创新**：记忆奖励（Memory Reward）通过语义对齐解决了“过程监督”难题，而无需昂贵的逐步人工标注。
*   **结构化反馈**：原子奖励（Atomic Reward）比传统的 Token 匹配（如 BLEU）更符合 SQL 的逻辑结构，对语法变体更具鲁棒性。
*   **实用性极强**：针对 SLM 进行优化，直接回应了工业界对低成本、高隐私、低延迟 Text-to-SQL 方案的需求。

### 8. 不足与局限
*   **依赖高质量蒸馏源**：第一阶段的 SFT 效果高度依赖于教师模型（如 DeepSeek-R1）生成的推理质量。
*   **记忆库维护**：虽然向量数据库检索很快，但随着训练进行，如何动态更新和去重记忆库中的推理轨迹仍存在一定的工程复杂度。
*   **空结果风险**：论文提到对于返回空结果的 SQL 存在“虚假正确”的风险，虽然通过原子奖励缓解，但在极端噪声数据下的表现仍需观察。

（完）
