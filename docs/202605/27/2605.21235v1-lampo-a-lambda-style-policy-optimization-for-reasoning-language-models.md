---
title: "LamPO: A Lambda Style Policy Optimization for Reasoning Language Models"
title_zh: "LamPO: 一种用于推理语言模型的Lambda风格策略优化"
authors: "Zhe Yuan, Yipeng Zhou, Jinghan Li, Xinyuan Chen, Bowen Deng, Zhiqian Chen, Liang Zhao"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21235v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用配对优势的Lambda风格策略优化，用于推理语言模型
tldr: 针对强化学习中的组相对目标（如GRPO）忽略候选响应间细粒度关系、导致信用分配弱的问题，提出LamPO方法，采用成对分解优势替代标量优势，结合对数概率差异的置信度权重和可选ROUGE-L稠密奖励，在数学和科学推理任务上优于GRPO等现有方法，训练更稳定、样本效率更高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21235v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison on reasoning benchmarks. The best result in each model block is bolded.\", \"page\": 8, \"index\": 1, \"width\": 562, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.21235v1/fig-002.webp\", \"caption\": \"Figure 1: Training dynamics on Qwen3-1.7B, Qwen3-4B, and Phi-4-mini. Left panels show reward accuracy; right panels show mean completion length.\", \"page\": 10, \"index\": 2, \"width\": 743, \"height\": 598}]"
motivation: 现有组相对目标因使用标量统计量而丢失候选响应间的细粒度排序信息，在稀疏结果奖励下信用分配不足。
method: LamPO通过成对分解优势聚合组内响应间奖励差异，并利用序列对数概率差异计算置信度权重，同时保留无评论家和裁剪更新的PPO风格结构，可选添加基于ROUGE-L的稠密辅助奖励。
result: 在AIME24、AIME25、MATH-500和GPQA-Diamond上，使用Qwen3-1.7B、Qwen3-4B和Phi-4-mini模型，LamPO一致优于GRPO及近期RLVR变体，训练动态更稳定、样本效率更高。
conclusion: LamPO通过成对分解和置信度加权有效提升稀疏奖励下的信用分配，为推理语言模型提供了一种更优的策略优化方法。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为在数学、编码和科学问答等任务上改进推理语言模型的有效范式。然而，广泛使用的群体相对目标（如GRPO）用标量统计量汇总每个采样组，从而丢弃了候选响应之间的细粒度关系信息。这在稀疏结果奖励下削弱了信用分配，尤其是当多个生成的解决方案在推理质量上只有细微差异时。我们提出**LamPO**，一种**Lambda风格策略优化**方法，用*成对分解优势*替代标量群体优势。LamPO聚合每个响应组内的成对奖励差距，并通过从序列对数概率差异计算出的置信度感知权重来调节每个比较，同时保留了PPO风格优化的无评论家和裁剪更新结构。当参考解可用时，我们进一步添加一个基于ROUGE-L的轻量级密集辅助奖励以减少奖励稀疏性。在AIME24、AIME25、MATH-500和GPQA-Diamond上使用Qwen3-1.7B、Qwen3-4B和Phi-4-mini的实验表明，LamPO一致优于GRPO和最近的RLVR变体，具有更稳定的训练动态和更好的样本效率。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become an effective paradigm for improving reasoning language models on tasks such as mathematics, coding, and scientific question answering. However, widely used group-relative objectives, such as GRPO, summarize each sampled group with scalar statistics and therefore discard fine-grained relational information among candidate responses. This weakens credit assignment under sparse outcome rewards, especially when multiple generated solutions differ only subtly in reasoning quality. We propose \textbf{LamPO}, a \textbf{Lambda-Style Policy Optimization} method that replaces scalar group advantages with a \emph{Pairwise Decomposed Advantage}. LamPO aggregates pairwise reward gaps within each response group and modulates each comparison by a confidence-aware weight computed from sequence log-probability differences, while retaining the critic-free and clipped-update structure of PPO-style optimization. When reference solutions are available, we further add a lightweight ROUGE-L-based dense auxiliary reward to reduce reward sparsity. Experiments on AIME24, AIME25, MATH-500, and GPQA-Diamond with Qwen3-1.7B, Qwen3-4B, and Phi-4-mini show that LamPO consistently improves over GRPO and recent RLVR variants, with more stable training dynamics and better sample efficiency.

---

## 论文详细总结（自动生成）

# LamPO: 一种用于推理语言模型的Lambda风格策略优化 — 详细中文总结

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：具有可验证奖励的强化学习（RLVR）已成为改进推理语言模型（如数学、编码、科学问答）的有效范式。其中，组相对策略优化（GRPO）因无需训练价值模型、使用组内奖励标准化而广泛使用。
- **核心问题**：GRPO等组相对目标将每个采样组压缩为标量统计量（均值和标准差），丢弃了候选响应间的细粒度排序关系。在稀疏的二值结果奖励下，许多错误轨迹获得相同反馈，导致信用分配不足。特别是当多个生成解仅在推理质量上有细微差异时，标量基线无法区分它们。
- **整体含义**：本文指出现有方法的这一根本局限性，并提出LamPO，通过保留组内成对关系来提供更丰富的学习信号，从而提升推理性能。

---

## 2. 方法论：核心思想、关键技术细节、公式与算法流程

### 核心思想
- 用**成对分解优势（Pairwise Decomposed Advantage, PDA）**替代标量组相对优势。PDA通过聚合组内所有其他响应与当前响应的奖励差距，并用序列对数概率差异的Sigmoid函数作为置信度权重，从而保留排序信息。
- 保持无评论家和裁剪更新结构（类似PPO），并可选添加基于ROUGE-L的密集辅助奖励来缓解奖励稀疏性。

### 关键技术细节
1. **策略定义**：自回归语言模型π_θ，输出序列o，奖励R(o)。
2. **成对分解优势（公式8）**：
   $$A_\lambda(o_i) = \frac{1}{G-1} \sum_{j \neq i} (R(o_i) - R(o_j)) \cdot \sigma\left(\frac{\Delta s_{ij}}{\tau}\right)$$
   其中$\Delta s_{ij} = \log \pi_{\theta_{old}}(o_i|q) - \log \pi_{\theta_{old}}(o_j|q)$，$\sigma$为Sigmoid，$\tau$为温度参数。
   - 奖励差距$(R(o_i)-R(o_j))$提供学习方向。
   - 置信权重$\sigma(\Delta s_{ij}/\tau)$反映策略当前对两个响应的相对偏好，$\tau$控制敏感度。
3. **裁剪目标（公式9）**：与GRPO相同形式的PPO式裁剪，但使用$A_\lambda$替代$\hat{A}_i$。
4. **完整目标（公式10）**：包含KL正则化项，$\beta D_{KL}(\pi_\theta \|\pi_{ref})$。
5. **密集参考重叠奖励（公式11-12）**：当参考解$y^*$可用时，添加$R_{sem}(o_i) = \text{ROUGE-L}_{F1}(o_i, y^*)$，最终奖励$R(o_i) = R_{ext}(o_i) + \lambda_{sem} R_{sem}(o_i)$。
6. **梯度解释（公式13）**：LamPO更新显式分解为成对奖励比较，每个项由奖励差和置信权重驱动。

### 算法流程（Algorithm 1）
- 输入：数据集D，策略π_θ，参考策略π_ref，组大小G，温度τ，KL系数β。
- 重复：
  - 采样一批提示B。
  - 对每个提示q：
    - 用旧策略生成G个响应O_q。
    - 计算奖励（含辅助奖励）。
    - 计算每个响应序列对数概率分数。
    - 对每个i，遍历所有j≠i，累加$\Delta R_{ij} \cdot \sigma(\Delta s_{ij}/\tau)$，归一化为$A_\lambda(o_i)$。
    - 可选组内归一化优势。
  - 更新策略最大化公式(10)。
  - 更新旧策略。

---

## 3. 实验设计

### 训练数据集
- **Mixture-of-Thoughts**：约35万道带验证推理轨迹的问题。

### 评估基准（Benchmarks）
- AIME24、AIME25（数学竞赛）
- MATH-500（数学推理）
- GPQA-Diamond（研究生级科学问答）

### 对比方法
- CoT（思维链提示）
- GRPO（基线组相对优化）
- DAPO、GSPO、SimpleRL-Zoo、PRIME、RLPR（近年RLVR变体）

### 模型骨架
- Qwen3-1.7B、Qwen3-4B、Phi-4-mini

### 默认超参数
- 组大小G=8，温度τ=1.5，语义奖励系数λ_sem=1.0（当参考解可用时）。基线使用相同设置。

---

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长、总计算量等具体资源信息。**
- 仅提到组大小G=8，以及训练步数（例如Qwen3-1.7B训练约2000步，Qwen3-4B约250步等从图中可看出，但未给出精确数值）。未披露分布式配置或硬件细节。

---

## 5. 实验数量与充分性

### 实验数量
- **主实验（表1）**：3个模型 × 4个基准 × 8种方法（Qwen3-1.7B对比7种基线，Qwen3-4B和Phi-4-mini对比GRPO和CoT），共计8+2+2=12组比较，LamPO在所有设置中均取得最佳或并列最佳。
- **消融实验（表2）**：在Qwen3-1.7B上，研究了温度τ（4个值）和语义奖励系数λ_sem（2个值），共5组。
- **训练动态分析（图1）**：3个模型的准确率奖励和完成长度曲线，对比GRPO。

### 充分性与公平性
- **充分性**：覆盖了不同规模模型（1.7B、4B、小型）、不同任务（数学竞赛、标准数学、科学问答），消融研究了两个关键超参数。对比方法包括近期代表性RLVR变体，实验设计较为完整。
- **客观公平**：所有方法使用相同的采样和优化设置（除非特殊说明），评估指标为标准准确率。但未进行统计显著性检验（如多次运行标准差），且仅报告单次结果（可能受随机性影响）。这点可视为轻微不足。

---

## 6. 论文的主要结论与发现

- LamPO在所有评估基准和模型上一致优于GRPO及近期RLVR基线，平均准确率提升约1.5–2.2个点。
- 成对分解优势是主要改进来源：消融显示移除稠密奖励后性能下降但仍有提升，证明核心贡献来自PDA。
- 温度τ=1.5表现最佳；τ过小或过大都会降低性能。
- 训练动态更稳定：LamPO产生更平滑的奖励提升和更稳定的完成长度，尤其在小模型上。
- 方法通用性好：跨不同模型族（Qwen3、Phi-4）均有效。

---

## 7. 优点

- **创新性**：明确指出标量化弊病，提出保留组内排序信息的成对优势，并创新性地利用对数概率差异作为置信度权重。
- **实用性**：无评论家、保持PPO裁剪结构，易于集成到现有框架；额外成本仅O(G²)且G较小（通常8-16），可接受。
- **鲁棒性**：通过置信度加权自动调节不同比较对的影响力，避免低置信度对引入噪声；可搭配稠密奖励进一步改善。
- **实验验证充分**：多个基线、多种数据集、不同模型规模，并提供消融和训练动态。
- **论文写作清晰**：动机、方法、实验各部分脉络清楚。

---

## 8. 不足与局限

- **计算复杂度**：O(G²)的成对比较在组大小较大时可能成为瓶颈（尤其结合大量采样）。文中承认需要近似方法扩展。
- **对奖励质量的依赖**：成对奖励差距依赖于奖励信号的可靠性。若奖励函数有噪声或主观性强，可能放大错误。
- **辅助奖励的局限性**：ROUGE-L仅捕捉词汇重叠，不能衡量推理正确性；且要求参考解存在，限制了应用场景。
- **实验覆盖不足**：
  - 未涉及开放域对话、长程规划、多模态推理等更广泛任务。
  - 未进行多次随机种子实验以评估变异性。
  - 仅报告单次训练结果，缺乏统计显著性分析。
- **未提及资源消耗**：缺少算力细节，影响可复现性评估。
- **潜在偏差**：训练数据Mixture-of-Thoughts的分布可能与评估基准存在重叠，但未讨论数据泄露风险。

（完）
