---
title: "The Constraint Tax: Measuring Validity-Correctness Tradeoffs in Structured Outputs for Small Language Models"
title_zh: 约束税：衡量小语言模型结构化输出中的有效性与正确性权衡
authors: Jaideep Ray
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.26128v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 结构化输出约束对小型语言模型准确性的影响
tldr: "本文研究小语言模型（SLM）在结构化输出约束下的性能权衡。针对子3B参数模型，提出“约束税”测量协议，评估硬约束对答案准确性和执行准确性的影响。通过15,000次实验，发现硬约束使模式有效性从61.5%提升至100%，但答案准确性从19.7%降至11.0%，错误有效输出从49.5%增至88.9%。提出“自由推理、延迟约束”设计模式，建议系统分别报告模式有效性、答案准确性、执行准确性和错误有效输出率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26128v1/fig-001.webp\", \"caption\": \"Figure 2: Main GPU experiment. Hard answer-only schema decoding reaches 100% validity, but answer accuracy falls and the wrong-valid-schema rate rises sharply.\", \"page\": 4, \"index\": 1, \"width\": 499, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.26128v1/fig-002.webp\", \"caption\": \"Table 2: Output modes used by the benchmark. The modes separate prompt-only formatting, regex constraints, direct schemas, structured rationales, typed traces, and delayed packaging.\", \"page\": 3, \"index\": 2, \"width\": 520, \"height\": 452}]"
motivation: 常规假设认为硬输出约束能提高可靠性而不改变答案质量，但该假设对小语言模型不成立。
method: 提出约束税测量协议，在固定模型、任务分布和问题实例下分离结构化输出约束导致的准确性和执行性损失。
result: "硬约束使模式有效性升至100%，但答案准确性从19.7%降至11.0%，错误有效输出从49.5%增至88.9%。"
conclusion: 生产系统应分别报告不同指标，并采用“自由推理、延迟约束”的设计模式。
---

## 摘要
生产级LLM系统越来越需要机器可读的输出：JSON对象、类型化轨迹、正则表达式约束的字段和工具调用模式。本文针对设备端和低成本的小语言模型（SLM）部署，其中低于3B参数的模型在隐私、延迟和商品硬件方面具有吸引力，但在满足模式的同时解决任务的能力有限。通常的工程假设是，硬输出约束在不改变底层答案的情况下提高可靠性。我们证明这一假设对小模型是不安全的。我们引入了“约束税”，一种测量协议，用于在固定模型、固定任务分布和固定问题实例下，隔离由结构化输出约束导致的答案和可执行准确率损失。在Qwen2.5-0.5B、Qwen2.5-1.5B和SmolLM2-1.7B的15,000次商品GPU生成实验中，仅答案的硬模式解码将模式有效性从61.5%提升到100.0%，但答案准确率从19.7%降至11.0%，并且错误有效模式输出从49.5%增加到88.9%。最接近的工业类比是确定性日历工具调用任务：Qwen2.5-1.5B在仅提示JSON下达到91.5%的可执行准确率，但在相同硬工具调用模式下仅为48.0%，而两种模式均为100.0%模式有效。错误是语义性的，而非结构性的。我们还表明，3B边界仍然支付直接模式税，并且延迟封装支持一种建设性设计模式：自由推理，后期约束。实际结论是直接的：生产系统应分别报告模式有效性、答案准确率、可执行准确率和错误有效模式率。

## Abstract
Production LLM systems increasingly require machine-readable outputs: JSON objects, typed traces, regex-constrained fields, and tool-call schemas. This paper targets on-device and low-cost small language model (SLM) deployments, where sub-3B models are attractive for privacy, latency, and commodity hardware but have limited capacity to satisfy schemas while solving tasks. The usual engineering assumption is that hard output constraints improve reliability without changing the underlying answer. We show that this assumption is unsafe for small models. We introduce \emph{constraint tax}, a measurement protocol for isolating the answer and executable-accuracy loss caused by structured-output constraints at fixed model, fixed task distribution, and fixed problem instances. Across 15,000 commodity-GPU generations with Qwen2.5-0.5B, Qwen2.5-1.5B, and SmolLM2-1.7B, hard answer-only schema decoding raises schema validity from 61.5\% to 100.0\%, but lowers answer accuracy from 19.7\% to 11.0\% and increases wrong-valid-schema outputs from 49.5\% to 88.9\%. The strongest industry analogue is a deterministic calendar tool-call task: Qwen2.5-1.5B achieves 91.5\% executable accuracy with prompt-only JSON but only 48.0\% under the same hard tool-call schema, while both modes are 100.0\% schema-valid. The error is semantic, not structural. We also show that the 3B boundary still pays a direct-schema tax and that delayed packaging supports a constructive design pattern: reason free, constrain late. The practical conclusion is direct: production systems should report schema validity, answer accuracy, executable accuracy, and wrong-valid-schema rate separately.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：小语言模型（SLM，参数<3B）在设备端或低成本部署中，强制使用结构化输出约束（如JSON Schema、正则表达式、工具调用模式）时，是否会在修复格式问题的同时损害答案的语义正确性？
- **背景**：生产系统常将结构化输出作为服务合约，认为硬约束仅改善包装，不影响答案质量。然而，小模型解码容量有限，约束会与任务推理竞争资源，导致“有效但错误”的输出（Wrong-Valid-Schema）。本文旨在量化这种权衡，并提出新的评估框架。

### 2. 论文提出的方法论

- **核心思想**：定义“约束税”（Constraint Tax）作为衡量约束导致的准确率损失。将结构化输出视为全系统接口，分离语法正确性与语义正确性。
- **关键技术细节**：
  - **绝对约束税**：\( \text{Tax} = \max(0, \text{Acc}_{\text{baseline}} - \text{Acc}_{\text{constrained}}) \)
  - **归一化约束税**：\( \text{Tax}_{\text{norm}} = \frac{\text{Tax}}{\max(\epsilon,\text{Acc}_{\text{baseline}})} \)
  - **输出模式**：对比freeform、prompt_json、answer_only_schema、delayed_constraint等多种接口，区分“自由推理，后期约束”与“直接硬约束”。
  - **指标分离**：记录Schema有效性、答案正确性、可执行准确率、错误有效输出率、延迟/Token数。
  - **错误分类**：输出被分为correct_valid、invalid_json、wrong_answer_valid_schema等类别。
- **算法流程**：固定问题实例，变化输出控制干预，先无约束生成（freeform/prompt）再硬约束生成，通过同一parser+executor+trace checker记录语法和语义结果。

### 3. 实验设计

- **数据集/场景**：
  - 5个合成确定性任务族：arithmetic_two_step、symbolic_string、object_tracking、boolean_logic、tool_call_argument。
  - 生产类比：日历工具调用任务（calendar tool-call），要求输出包含tool名称和6个参数。
- **Benchmark与对比方法**：
  - 主要GPU运行：5种模式（freeform、freeform_direct、freeform_brief_reasoning、prompt_json、answer_only_schema），3个SLM（Qwen2.5-0.5B/1.5B、SmolLM2-1.7B），15,000次生成。
  - 日历任务：对比prompt_json、hard tool-call schema、delayed_packaging。
  - 3B边界：Qwen2.5-3B。
  - 本地扩展接口：4个小模型，20样本/任务族，包含rationale schema、typed trace、delayed constraint等。
- **可复现性**：所有数字来自生成的JSONL/CSV，后端使用vLLM/SGLang，MLX/HF Transformers。

### 4. 资源与算力

- **主要GPU运行**：商品GPU（未明确型号，支持vLLM/SGLang），单张卡即可完成。
- **本地研究**：Mac-local使用MLX或Hugging Face Transformers。
- **算力描述**：强调低计算成本，未提供具体GPU数、训练时长；但15,000次生成可在商品GPU上快速完成。
- **注意**：论文未明确GPU型号、数量、耗时，仅提及“commodity GPU”。

### 5. 实验数量与充分性

- **实验数量**：
  - 主运行：5个模型×5个任务族×1000实例×5种模式 = 15,000次生成（每种模式/模型组合1000实例）。
  - 日历任务：200实例/模式（两种直接生成模式 + 延迟封装）。
  - 3B边界：Qwen2.5-3B × 1000实例 × 2模式。
  - 本地扩展：4模型 × 5任务 × 20实例 × 8种模式 ≈ 3200次生成（但每个模式样本量较小）。
- **充分性评估**：
  - **优点**：任务覆盖算术、符号、逻辑、对象追踪、工具调用等多类型推理；15,000次生成提供了统计稳定性；95% bootstrap置信区间报告。
  - **不足**：合成任务为主，缺乏真实用户负载；模型覆盖仅4个小模型（+1个3B边界），未涵盖更大模型或LLaMA类；消融实验不够详尽（仅对比模式，未系统改变约束强度）；本地扩展研究样本量小（20例/任务），作为设计证据而非显著性证明。

### 6. 论文的主要结论与发现

- **硬约束提升有效性但降低正确性**：answer_only_schema使有效性从61.5%→100%，但答案准确率从19.7%→11.0%，错误有效输出率从49.5%→88.9%。
- **语义错误主导**：日历任务中，两种模式均100%有效，但硬schema可执行准确率从91.5%跌至48.0%（-43.5 pts），错误集中在duration字段。
- **约束税在3B边界仍然存在**：Qwen2.5-3B硬schema仍损失15.3 pts答案准确率，增加31.6 pts错误有效输出。
- **延迟约束模式有效**：delayed_constraint在本地扩展研究中达到40.7%正确率且100%有效，优于直接硬约束。
- **后端依赖性**：SmolLM2在vLLM和SGLang上准确率不同（18.7% vs 25.7%），说明约束解码是系统属性。
- **部署建议**：分别报告schema有效性、答案准确率、可执行准确率、错误有效输出率；优先使用“自由推理，后期约束”模式。

### 7. 优点

- **指标分离创新**：明确区分有效性（parse能否通过）与正确性（答案是否对），强调“错误有效输出”这一被忽视的指标。
- **系统级测量**：将约束解码视为全服务路径，包括模型、提示、解码引擎、schema、验证栈，并复制后端差异。
- **低成本可复现**：使用商品GPU和开源工具，实验设计透明，结果基于确定性任务避免法官偏见。
- **实用设计模式**：“reason free, constrain late”原则可直接指导生产部署，且通过实验验证其可行性。
- **统计严谨**：提供95% bootstrap置信区间，配对比较使用相同问题实例。

### 8. 不足与局限

- **任务代表性有限**：合成任务无法涵盖真实用户多样性和长尾分布，日历任务虽然是工业类比，仍为受控模拟。
- **模型覆盖窄**：仅三种小模型（均来自Qwen和SmolLM系列）和一个3B边界，缺乏对LLaMA、Gemma等大模型的测试，且未建立缩放律。
- **样本量不均**：本地扩展研究每个模式仅20个样本，不能视为统计验证，仅为设计证据。
- **实验设计完整度**：未进行系统的消融实验（如不同约束强度、不同解码器、不同prompt格式），未对比训练数据影响。
- **时效性问题**：结构化输出实现（vLLM/SGLang）快速变化，结果可能随版本更新而变化（论文自身也指出了后端依赖）。
- **未考虑更大模型**：未证明该现象在更大模型（如7B+）上是否消失或减轻。

（完）
