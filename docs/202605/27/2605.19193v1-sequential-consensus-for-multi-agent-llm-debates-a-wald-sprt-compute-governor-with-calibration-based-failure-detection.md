---
title: "Sequential Consensus for Multi-Agent LLM Debates: A Wald-SPRT compute governor with calibration-based failure detection"
title_zh: 多智能体LLM辩论的序贯共识：基于Wald-SPRT的计算调控器与校准型故障检测
authors: Andrea Morandi
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19193v1"
tags: ["query:mas-gen"]
score: 8.0
evidence: 用于多智能体LLM辩论的Wald-SPRT计算调控器，带有共识评分
tldr: 多智能体LLM辩论中，固定轮次导致计算浪费。本文提出将Wald序贯概率比检验(SPRT)作为计算调控器，通过LLM评判的共识分数动态决定停止轮次。在GSM8K上以3.7倍调用减少实现接近固定轮次的准确率，在MMLU上校准失效时自动触发上限，展示了序贯检验作为低成本计算控制和故障检测层的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19193v1/fig-001.webp\", \"caption\": \"TABLE III REAL-LLM EVALUATION ON MMLU (200 ATTEMPTED RANDOM ITEMS, ALL-SUBJECTS TEST SPLIT) AND GSM8K (200 ATTEMPTED RANDOM ITEMS, TEST SPLIT). THE n COLUMN REPORTS VALID/EVALUABLE ITEMS AFTER ANSWER PARSING AND FILTERING. AGENTS: GPT-5, CLAUDE-OPUS-4-6, GEMINI-2.5-PRO. JUDGE: CLAUDE-OPUS-4-6. SPRT THRESHOLDS α = β = 0.05, Rmax = 8, CALIBRATION FIT ON A DISJOINT n = 40 SUBSET PER TASK. AVG CALLS COUNTS THE CALLS REQUIRED BY EACH PROTOCOL; THE FIXED-5 BASELINE DOES NOT RUN A PER-ROUND STOPPING JUDGE.\", \"page\": 7, \"index\": 1, \"width\": 627, \"height\": 194}]"
motivation: 固定轮次的多智能体辩论在简单问题上浪费计算，而在困难问题上计算不足。
method: 将Wald序贯概率比检验(SPRT)作为计算调控器，每轮后LLM评判给出共识分数，累积对数似然比决定是否停止。
result: "在GSM8K上平均1.01轮（4.06次调用），准确率97.0% vs 固定5轮99.0%（15次调用），调用减少3.7倍；MMLU上校准失效时99.5%项目达到上限，成本2.1倍。"
conclusion: 经典序贯检验可作为多智能体LLM系统的廉价计算控制和故障检测层，而非提高准确性。
---

## 摘要
多智能体LLM辩论可提升事实性与推理能力，但多数方法采用固定轮次，导致简单问题计算浪费、困难问题计算不足。我们将Wald序贯概率比检验（SPRT）适配为LLM辩论的即插即用计算调控器。每轮辩论后，LLM裁判对最新智能体立场给出[0,1]共识得分；Wald监控器在Beta似然族下累积“有用收敛”与“尚未有用”的对数似然比，当任一边界被越过时停止，或在最大轮数R_max时返回上限的最佳结果。在独立同分布假设下，该规则继承SPRT的第一类/第二类错误保证；实际部署中，校准本身更为重要，因为它估计裁判得分是否在特定领域真正区分有用与无用的收敛。我们评估了两个路径：（i）在标定Beta模型下进行蒙特卡罗研究，刻画工作曲线、错误率、上限行为及敏感性；（ii）在200个MMLU和200个GSM8K题目上，使用三个异构智能体（gpt-5、claude-opus-4-6、gemini-2.5-pro）及claude-opus-4-6裁判进行实际LLM评估，采用不重叠的40题校准子集。在GSM8K上，规则平均1.01轮（4.06次LLM调用）达到97.0%准确率，而固定5轮辩论需15次调用、准确率99.0%：调用次数减少3.7倍，准确率下降2个百分点。在MMLU上，校准后KL散度近乎为零，规则在99.5%的题目上触达上限，成本为2.1倍。结论并非SPRT使辩论更准确，而是经典序贯检验可作为多智能体LLM系统的低成本计算控制与故障检测层。

## Abstract
Multi-agent LLM debate improves factuality and reasoning, but most recipes pick a fixed round count, over-spending on easy items and under-spending on hard ones. We adapt Wald's Sequential Probability Ratio Test (SPRT) as a plug-in compute governor for LLM debates. After each round, an LLM judge emits a [0,1] consensus score on the latest agent positions; a Wald monitor accumulates the log-likelihood ratio of "useful convergence" vs "not yet useful" under a Beta likelihood family, and stops when either boundary is crossed or returns a capped best-effort outcome at R_max. Under i.i.d. assumptions the rule inherits SPRT type-I/type-II error guarantees; in deployment the calibration itself is the more important object, since it estimates whether the judge score actually separates useful from unhelpful convergence in a given domain. We evaluate two tracks: (i) a Monte-Carlo study under calibrated Beta models characterising working curves, error rates, capping behaviour, and sensitivity; and (ii) a real-LLM evaluation on 200 attempted MMLU and 200 attempted GSM8K items with three heterogeneous agents (gpt-5, claude-opus-4-6, gemini-2.5-pro) and a claude-opus-4-6 judge, using disjoint 40-item calibration subsets. On GSM8K the rule stops in 1.01 average rounds (4.06 LLM calls) at 97.0% accuracy vs 99.0% for fixed-5 debate at 15 calls: a 3.7x call reduction at -2pp accuracy. On MMLU the calibrated KL collapses to about 0 and the rule caps on 99.5% of items at 2.1x cost. The takeaway is not that SPRT makes debate more accurate, but that a classical sequential test serves as a cheap compute-control and failure-detection layer for multi-agent LLM systems.