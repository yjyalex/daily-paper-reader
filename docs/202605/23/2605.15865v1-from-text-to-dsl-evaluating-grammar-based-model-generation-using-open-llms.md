---
title: "From Text to DSL: Evaluating Grammar-Based Model Generation Using Open LLMs"
title_zh: 从文本到领域特定语言：评估使用开放大语言模型的基于语法的模型生成
authors: "Junaid Baber, Nicolas Hili, Didier Schwab, Léo Challier, Cécilia Satrin"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15865v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 评估基于语法的解码在大模型领域特定语言生成中的效率
tldr: "12b、mistral:7b-instruct）性能接近甚至超过大型模型，验证了使用小型开源LLM进行DSL生成的可行性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15865v1/fig-001.webp\", \"caption\": \"Figure 5: Stacked bar chart of averaged evaluation scores for 18 DSL models with fewer than 8 billion parameters, designed for an online ice cream parlour.\", \"page\": 11, \"index\": 1, \"width\": 718, \"height\": 641}, {\"url\": \"assets/figures/arxiv/2605.15865v1/fig-002.webp\", \"caption\": \"Figure 2: Preview of LARK based parser implementation that identifies syntax errors with token level precision and provides context-specific error reporting, (a) shows syntax errors, (b) shows semantic issues identified.\", \"page\": 7, \"index\": 2, \"width\": 1004, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2605.15865v1/fig-003.webp\", \"caption\": \"Figure 4: Stacked bar chart of averaged evaluation scores for 26 DSL models designed for an online ice cream parlour, (a) shows models by score and (b) shows model by size.\", \"page\": 10, \"index\": 3, \"width\": 993, \"height\": 576}, {\"url\": \"assets/figures/arxiv/2605.15865v1/fig-004.webp\", \"caption\": \"Table 1: Evaluated LLMs grouped by architecture. The gray-shaded models successfully generated syntactically valid DSLs recognized by the Lark parser.\", \"page\": 4, \"index\": 4, \"width\": 972, \"height\": 1406}, {\"url\": \"assets/figures/arxiv/2605.15865v1/fig-005.webp\", \"caption\": \"Figure 3: Overview of the human evaluation interface. Panel (a) displays the available evaluation experiments, panel (b) captures participant demographic information, and panel (c) presents the model outputs along with corresponding evaluation criteria. Users can access the original prompt used for model generation by clicking the help icon. 9\", \"page\": 9, \"index\": 5, \"width\": 856, \"height\": 1280}, {\"url\": \"assets/figures/arxiv/2605.15865v1/fig-006.webp\", \"caption\": \"Figure 1: Overview of the proposed model-based approach based on LLM for DSL creation.\", \"page\": 5, \"index\": 6, \"width\": 876, \"height\": 767}]"
motivation: 评估开源LLM是否能在无需微调下通过少量示例提示生成符合DSL的模型，以降低对昂贵闭源模型的依赖。
method: 使用少量示例提示，让LLM从零生成UI和数据模型，结合自动解析和专家反馈评估语法、语义和一致性。
result: "12b, mistral:7b-instruct）在质量上接近或匹配大型模型（如32B参数）。"
conclusion: 小型开源LLM可作为经济高效且可部署的替代方案，用于模型驱动工程中的DSL生成。
---

## 摘要
大语言模型在自动化模型驱动软件工程任务方面展现出越来越大的潜力，特别是从自然语言生成符合领域特定语言的模型。然而，现有方法大多依赖大型专有模型，其高成本和有限的部署能力阻碍了更广泛的采用。本文评估了不同规模（0.5B至32B参数）的开源大语言模型是否能够仅通过少量样本提示（无需微调）生成符合领域特定语言的模型。我们的评估聚焦于模型驱动工程的关键需求，包括语法有效性、语义完整性以及模型间引用一致性。我们扩展了先前的工作，从基于固定预定义数据模式生成用户界面模型转变为完全从头生成用户界面和数据模型。这一转变有两个目的：首先，它凸显了大语言模型推断领域特定关系并在多个互联模型之间保持一致性的能力；其次，它允许我们通过测试不同性质和结构角色的模型的领域特定语言生成来泛化早期的发现。我们的结构化评估结合了自动解析和专家反馈，覆盖39个大语言模型，结果表明多个紧凑模型（如\texttt{gemma3:12b}、\texttt{mistral:7b-instruct}）接近或匹配更大模型的质量。这些发现证明了在模型驱动工程工作流中使用较小的开源大语言模型进行语法合规的领域特定语言生成的可行性，为封闭大语言模型提供了一种经济高效且可部署的替代方案。

## Abstract
Large Language Models (LLMs) have shown increasing potential in automating model-driven software engineering tasks, particularly in generating models conforming to Domain Specific Languages (DSLs) from natural language. While most existing approaches rely on large proprietary models, their high cost and limited deployability hinder broader adoption.   In this paper, we evaluate whether open-source LLMs of varying sizes (0.5B to 32B parameters) can generate DSL-conformant models using only few-shot prompting, without any fine-tuning. Our evaluation focuses on key model-driven engineering (MDE) requirements, including syntactic validity, semantic completeness, and inter-model reference consistency.   We extend our prior work by moving from generating user interface models (referred to as "UI models" in this paper) over fixed, predefined data schemas ("data models") to generating both the UI and data models entirely from scratch. This shift serves two purposes: first, it highlights the LLM's ability to infer domain-specific relationships and maintain consistency across multiple interconnected models; second, it allows us to generalize earlier findings by testing DSL generation across models of different natures and structural roles. Our structured evaluation combines automatic parsing and expert feedback across 39 LLMs, revealing that several compact models (e.g., \texttt{gemma3:12b}, \texttt{mistral:7b-instruct}) approach or match the quality of much larger models.   These findings demonstrate the feasibility of using smaller, open-source LLMs for grammar-conformant DSL generation in MDE workflows, offering a cost-effective and deployable alternative to closed LLMs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型在模型驱动工程中生成符合领域特定语言语法和语义的模型时，主要依赖昂贵且难以部署的封闭商用模型（如GPT-4）。能否使用开源、参数规模较小的语言模型，仅通过少样本提示（few-shot prompting）而无需微调，来完成这一任务？
- **背景**：现有研究虽展示了LLM在MDE中的潜力，但多假设大型商用模型和固定的预定义数据模式。本文旨在验证更开放、低成本的方案，推动无代码/低代码平台的可及性。

## 2. 论文提出的方法论
### 核心思想
- 构建一个从自然语言描述到DSL模型的端到端管道：用户输入 → 提取数据模型 → 构建集成化的提示（含语法规则、示例、数据模型） → LLM生成DSL代码 → 语法验证与语义评估。
- 不进行任何微调，仅通过提示工程和自动重试机制引导LLM。

### 关键技术细节
1. **数据模型生成**：先让LLM从用户需求中推断出实体、属性、关系，形成结构化数据模型（人工审核确保语法正确）。
2. **统一提示模板**：包含DSL语法（Xtext风格）、示例模型、生成的数据模型、输出规则（无注释、严格卡点表示法、包括`isId`、`<>-->`等）。
3. **自动语法验证**：基于LARK框架的LALR解析器，支持token级别错误报告。
4. **自动重试与温度调节**：
   - 首次使用较高温度（0.7-1.0）鼓励创造性。
   - 语法失败后，后续重试降低温度至0.1，提高确定性。
   - 最多重试N次（具体数值未明确，设为“up to N times”），全部失败则标记为语法无效，不进入语义评估。
5. **语义评估**：由三位DSL领域专家根据四个维度（语义正确性、概念识别、完整性、高级特性）进行5分Likert量表打分。使用自建Web平台收集反馈。

## 3. 实验设计
- **数据集/场景**：使用一个典型应用场景——在线冰淇淋店网站的自然语言描述。未提及多场景测试，论文称“test suite of natural language scenarios drawn from multiple domains”，但仅报告了冰淇淋店场景的具体结果。
- **Benchmark**：无特定外部基准，而是对比39个不同架构和参数规模的开源LLM（0.5B - 32B）的表现，包括LLaMA、Qwen2、Phi、Gemma、GraniteMoE等家族。
- **对比方法**：所有模型使用完全相同的提示模板和评估流程，仅改变LLM本身。这是模型间的横向对比，未与基于微调的方法或商用模型直接比较。

## 4. 资源与算力
- **明确说明**：论文未指定使用的GPU型号、数量或推理时长。部分模型通过公共API调用，部分本地使用量化版本（如GGUF）。
- **结论**：未提供具体算力开销，因此无法定量评估效率。

## 5. 实验数量与充分性
- **实验数量**：共39个模型，每个模型在相同场景下生成DSL。经语法验证后，只有26个模型至少产生一次语法正确的输出，并进入语义评估。语义评估由三位专家对每个模型进行四个维度打分（每个模型得到一个平均分）。
- **充分性分析**：
  - **优势**：覆盖了广泛的模型家族和参数规模，重试机制增加了评估的稳定性，语法自动验证+人工语义评估结合比较全面。
  - **不足**：
    - 仅使用一个应用场景（冰淇淋店），泛化性存疑。论文提到“多个领域”，但报告结果仅展示此场景。
    - 语义评估专家数量少（3位），未报告评分者间信度（如Cohen's Kappa），可能存在主观偏差。
    - 未进行消融实验（如去掉重试机制、不同提示设计的影响）。
    - 未与微调方法或商用模型（如GPT-4）在同一任务上直接对比，公平性难以客观判断。

## 6. 论文的主要结论与发现
- **小型模型可行**：多个紧凑模型（如`gemma3:12b`、`mistral:7b-instruct`、`notus:latest`）在语义质量上接近或超过更大参数模型（如`phi4:latest`、`deepseek-r1:14b`）。
- **指令调优帮助显著**：指令调优的模型通常比基础模型表现更一致、准确。
- **提示工程有效**：仅通过精心设计的提示（包含示例、语法规则、输出约束）和自动重试，无需微调即可让小型LLM生成语法有效、语义较完整的DSL。
- **模型大小非决定性因素**：某些7B模型表现优于14B甚至32B模型，说明架构和训练质量比纯参数数量更重要。
- **高级特性表现差异大**：部分模型能主动添加枚举、子集关系等高级构造，部分仅生成基本实体。

## 7. 优点
- **系统性评估**：大规模对比39个开源LLM，覆盖不同系列和规模，结果具有参考价值。
- **实际可行性验证**：证明了使用轻量开源模型替代昂贵闭源模型的潜力，为低成本MDE工具提供依据。
- **管道设计完善**：结合自动语法解析（含重试+温度调节）和人工语义评估的双轨验证，比较客观。
- **开源可复现**：公布了代码和实验配置（GitHub链接），便于社区验证和扩展。
- **扩展性**：从固定模式到完全自我生成数据模型的升级提高了任务难度和现实意义。

## 8. 不足与局限
- **场景单一**：仅报告了单一DSL（冰淇淋店）的结果，未评估跨领域泛化能力（如医疗、金融等领域）。
- **语义评估局限性**：仅三位专家，未统计一致性；Likert量表的主观性可能引入偏差。
- **未处理模型间引用一致性**：论文提到“inter-model reference consistency”是目标，但对同时生成UI和数据模型时引用关系是否正确的评估不够深入（语义评估包含完整性但未专门分析跨模型引用错误）。
- **未比较微调方法**：仅评估零微调情况，未与对模型进行领域微调后的性能对比，结论有局限。
- **语法验证假设依赖单一DSL**：若推广到其他DSL语法，LARK解析器需重新定义，提示模板也需调整，通用性需进一步验证。
- **排除偏差**：无法通过语法验证的13个模型直接被排除在语义评估外，这些模型在最基础的语法合规上就失败，可能它们是更差的选择，但未进一步分析这些模型失败的原因。

（完）
