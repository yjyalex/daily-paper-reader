---
title: "NeuroMAS: Multi-Agent Systems as Neural Networks with Joint Reinforcement Learning"
title_zh: NeuroMAS：将多智能体系统视为具有联合强化学习的神经网络
authors: "Haoran Lu, Luyang Fang, Wenxuan Zhong, Ping Ma"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.16757v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 将多智能体系统视为神经网络并通过联合强化学习训练
tldr: 本文提出NeuroMAS，将多智能体语言系统视为可训练的类神经网络架构，智能体为节点，文本信号为边，节点无角色但有结构感知，通过强化学习训练实现通信、专业化和协调。该形式将多智能体设计从工作流工程转向架构设计，实验表明NeuroMAS优于基线，且组织扩展具有路径依赖性，从小系统渐进扩展更有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16757v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Overview of the NeuroMAS Method. (b) Conceptual landscape of LLM multi-agent systems. The horizontal axis represents frozen versus trainable within-agent parameters, and the vertical axis represents fixed versus scalable and/or flexible between-agent topology. Positions are illustrative rather than quantitative.\", \"page\": 2, \"index\": 1, \"width\": 820, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2605.16757v1/fig-002.webp\", \"caption\": \"Table 1: Accuracy and computational cost across reasoning, domain knowledge, and code-generation benchmarks. NeuroMAS-c denotes a NeuroMAS topology with c agents, including the output aggregation agent. LLM Calls reports the number of model invocations used per input example.\", \"page\": 11, \"index\": 2, \"width\": 814, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2605.16757v1/fig-003.webp\", \"caption\": \"Table 2: Backbone robustness results on ARC-Challenge using Gemma-3-1B-IT as the backbone [Gemma Team et al., 2025]. Fixed-backbone methods do not update the underlying LLM parameters, while trained-backbone methods optimize trainable adapters during post-training.\", \"page\": 12, \"index\": 3, \"width\": 518, \"height\": 370}]"
motivation: 现有多智能体语言系统通常依赖手工设计工作流，缺乏可训练性和可扩展性。
method: 提出NeuroMAS方法，将多智能体系统建模为神经网络架构，智能体节点通过强化学习联合训练，自动学习通信和协作。
result: 在多个任务上，NeuroMAS显著优于推理时间和训练的多智能体基线，并发现从小系统渐进扩展更有效。
conclusion: 学习型神经多智能体系统是大语言模型有前景的扩展方向。
---

## 摘要
多智能体语言系统通常被构建为手工设计的工作流，其中智能体被赋予语义角色，通信协议被预先指定。我们提出NeuroMAS，该方法首先将多智能体语言系统视为一种可训练、可扩展的类神经网络架构，其中LLM智能体作为节点，中间文本信号作为边。在NeuroMAS中，智能体节点没有角色但具有结构感知能力：拓扑结构仅决定信息通常如何流动，而强化学习训练则决定节点如何通信、特化和协调。这种公式将多智能体设计从工作流工程转向架构设计，其中深度、宽度、连接性和增长协议成为可扩展的能力来源。此外，我们提供了一个理论视角，表明当任务允许层次分解时，这种模块化文本计算在参数效率上更优。实验表明，NeuroMAS在推理时间和训练后的多智能体基线上均有显著提升。我们进一步发现，组织扩展具有路径依赖性：较大的系统从头开始训练可能具有挑战性，但从较小的训练系统逐步增长时变得可行。这些结果表明，学习型神经多智能体系统是LLM的一个有前景的扩展轴。

## Abstract
Multi-agent language systems are often built as hand-designed workflows, where agents are assigned semantic roles and communication protocols are specified in advance. We propose NeuroMAS, a method that first treats a multi-agent language system as a trainable and scalable neural-network-like architecture with LLM agents as nodes and intermediate textual signals as edges. In NeuroMAS, agent nodes are role-free but structure-aware: the topology only determines how information can flow in general, while reinforcement learning training determines how nodes communicate, specialize, and coordinate. This formulation shifts multi-agent design from workflow engineering toward architecture design, where depth, width, connectivity, and growth protocol become scalable sources of capability. Further, we provide a theoretical perspective showing why such modular textual computation is more parameter-efficient when tasks admit hierarchical decompositions. Experiments show that NeuroMAS improves significantly over both inference-time and trained multi-agent baselines. We further find that organizational scaling is path-dependent: larger systems can be challenging to train from scratch, but become feasible when grown progressively from smaller trained systems. These results suggest that learned neural multi-agent systems are a promising scaling axis for LLMs.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有多智能体语言系统（MAS）通常依赖手工设计的工作流，为每个智能体分配语义角色（如规划者、求解者、批评者）并预先指定通信协议。这种设计缺乏可训练性和可扩展性，组织本身未成为可优化的对象。
- **整体含义**：论文提出将多智能体系统视为一种可训练、可扩展的类神经网络架构，其中智能体节点为角色自由但有结构感知的模块，通过强化学习联合训练，使通信、专业化和协调自动涌现。目标是将MAS设计从“工作流工程”转变为“神经拓扑设计”，开辟除模型规模缩放之外的替代扩展轴。

### 论文提出的方法论
#### 核心思想
- **角色自由但结构感知的节点**：不分配手工语义角色，节点仅知道其在架构中的位置和输出格式要求，功能特化由训练决定。
- **文本值分层架构**：节点按层组织，层间通过文本消息传递。输入经过多层隐藏节点处理，最后由输出聚合节点生成答案。类比神经网络，但隐藏状态为离散文本序列。
- **联合强化学习训练**：所有节点共享同一终端奖励（最终答案正确性），采用REINFORCE（得分函数）估计梯度，每个节点根据自身生成文本的对数概率更新参数，不反向传播经过中间文本。

#### 关键技术细节
- **节点策略**：共享冻结的LLM骨干（如Qwen3-0.6B），每个节点附加独立的LoRA适配器作为可训练参数。节点策略表示为 π_v(·|C_v) = π_0(·|C_v; α_v)，仅α_v可训练。
- **前向传播**：按层依次生成文本，每层节点解析来自上一层的消息，输出格式化消息给下一层；最终层消息输入输出节点生成答案。
- **渐进增长**：从小训练系统逐步扩展为大系统。将已训练节点的适配器参数迁移到扩展架构中对应位置，新节点初始化为零适配器（即暂时表现如骨干模型），然后继续联合训练。
- **理论分析**：提供参数效率定理，表明当任务具有层次分解结构时，模块化多智能体架构比非结构化的单智能体模型能更有效地利用参数（误差率 O(K^{1+1/a} q^{-1/a}) < O(q^{-1/b})，b > a）。

### 实验设计
#### 数据集/场景
- **推理基准**：ARC-Challenge（科学问答）、Navigate（BIG-Bench Hard中的空间导航推理）。
- **领域知识**：MMLU的三个子集：抽象代数、大学物理、专业医学。
- **代码生成**：HumanEval。
- **骨干鲁棒性**：额外使用Gemma-3-1B-IT在ARC-Challenge上验证。

#### 对比方法
- **固定骨干基线**（不更新模型参数）：直接提示、Self-Refine、Self-Check、MoA、GoA、GPTSwarm、AgentNet。
- **训练骨干基线**（更新适配器）：MALT、CoLLM-CC、Single-LLM RL（与NeuroMAS同训练方式但单智能体）、不同拓扑的NeuroMAS变体（-3、-5、-7）。

#### 评估指标
- 准确率（经过输出规范化后的精确匹配）。
- 计算成本：总训练参数量（LoRA参数）和LLM调用次数。

### 资源与算力
- 论文提到使用 **NVIDIA RTX 5090** 和 **NVIDIA H100** GPU进行实验，但未明确说明使用的具体数量、训练时长。
- 实验在资源约束条件下进行（小骨干模型，轻量训练），因此整体算力需求相对较低。

### 实验数量与充分性
- **实验数量**：主实验在6个基准上进行（表1），骨干鲁棒性实验（表2），渐进增长消融（表3），以及参数计数控制比较（Single-LLM RL与NeuroMAS-3参数量相同）。
- **充分性与客观性**：
  - **覆盖面**：涵盖推理、知识、代码生成三大类任务，包含多种基线（固定和训练方法），比较全面。
  - **消融严格**：通过参数匹配（6.9M参数）证明增益非来自更多参数，而是组织本身；通过渐进增长消融证明扩展路径重要性。
  - **公平性**：所有训练方法使用相同的冻结骨干、REINFORCE目标和计算量匹配比较。但评估子集相对较小（论文承认此局限），且部分任务（如Navigate）规模有限。

### 论文的主要结论与发现
- **方法有效**：NeuroMAS在六个基准上均优于最强的固定骨干基线以及单模型RL基线。例如NeuroMAS-3在ARC上比单模型RL高10.5个百分点。
- **参数控制证实组织增益**：相同的6.9M参数下，NeuroMAS-3（3个节点）显著优于Single-LLM RL（1个节点），说明分布式模块化计算不可等价于单模型更大适配器。
- **扩展路径依赖**：更大拓扑从零训练性能下降（如Navigate上NeuroMAS-5从零训练41.0% vs 渐进增长48.0%），而渐进增长能有效提升性能。
- **骨干鲁棒性**：在Gemma-3-1B-IT上同样有效，NeuroMAS-3达到44.5%优于所有基线。

### 优点
- **方法论创新**：首次将多智能体系统视为可训练的类神经网络架构，统一了组织设计和参数优化，思路新颖。
- **理论支撑**：提供了参数效率定理，从理论上解释了模块化多智能体的优势（当任务可分解时）。
- **严格消融设计**：通过参数计数匹配、渐进增长对比、骨干变化验证，增强了实验结果的说服力。
- **角色自由概念**：简化了多智能体设计，无需手工标注角色，自动学习特化。
- **渐进增长策略**：为实际扩展多智能体系统提供了可行路径。

### 不足与局限
- **实验覆盖有限**：仅使用小骨干模型（<1B参数），在更大模型上的表现未知；评估集规模较小（部分任务仅数百样本），统计显著性和泛化性受限制。
- **推理成本增加**：每个示例需多次LLM调用（c次），实际应用中可能带来延迟和开销。
- **理论假设限制**：参数效率定理依赖于任务有层次分解结构且局部误差稳定叠加，并非所有任务满足；不能保证算法实际找到最优分解。
- **优化风险**：更大系统可能更不稳定，渐进增长虽有效但需额外训练步骤，且节点初始化策略可能影响最终质量。
- **未分析协作涌现机制**：论文未深入分析节点具体学习到的策略，特化是否真正体现分工尚不明确。
- **二进制奖励局限**：对于开放式任务（如代码生成）可能不够精细，可能影响训练效率。

（完）
