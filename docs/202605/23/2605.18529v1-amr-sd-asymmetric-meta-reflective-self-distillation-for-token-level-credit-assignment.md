---
title: "AMR-SD: Asymmetric Meta-Reflective Self-Distillation for Token-Level Credit Assignment"
title_zh: AMR-SD：基于非对称元反思自蒸馏的令牌级信用分配
authors: "Zhenlin Wei, Pu Jian, Yingzhuo Deng, Xiaohan Wang, Jiajun Chai, Zhexin Hu, Wei Lin, Shanbin Zhang, Guojun Yin"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18529v1"
tags: ["query:token"]
score: 9.0
evidence: 通过非对称元反射自蒸馏实现令牌级信用分配
tldr: 为解决大语言模型在复杂推理中因序列级奖励导致的信用分配瓶颈，本文提出非对称元反射自蒸馏（AMR-SD），通过反射瓶颈生成苏格拉底式提示和批评，结合因果信息增益与ReLU门控阈值实现稀疏精确的token级优势调制，在多个基准上显著优于现有方法，防止训练后期崩溃。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18529v1/fig-001.webp\", \"caption\": \"Figure 1: In standard on-policy self-distillation, the student generates a response, which a teacher evaluates via forced-decoding with privileged information to produce token-level probabilities for student alignment.\", \"page\": 1, \"index\": 1, \"width\": 453, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2605.18529v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison on SciKnowEval and Tool Use. Accuracy at early and late training stages is reported to illustrate initial convergence speed versus long-horizon stability. Best results are in bold.\", \"page\": 6, \"index\": 2, \"width\": 967, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.18529v1/fig-003.webp\", \"caption\": \"Figure 2: Training dynamics on SciKnowEval Biology (left, Qwen2.5-7B-Instruct) and Physics (right, Qwen3-8B). Unlike baselines that exhibit early gains followed by plateaus or regression, AMR-SD achieves superior stability and avoids late-stage performance degradation across different model architectures.\", \"page\": 6, \"index\": 3, \"width\": 931, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.18529v1/fig-004.webp\", \"caption\": \"Figure 3: Training dynamics using Qwen3-8B in thinking mode. Left: Evaluation accuracy (acc@16) on the AIME 2024 benchmark over 150 training steps. AMR-SD maintains stable, state-of-the-art performance, whereas RLSD suffers from a severe performance collapse post-annealing. Right: Average training reward on the DAPO-Math-17k dataset. The evaluation collapse of RLSD is accompanied by a sharp degradation in training reward, indicating an intrinsic optimization failure. AMR-SD, by contrast, maintains stable reward growth, successfully bridging in-domain optimization with out-of-domain generalization.\", \"page\": 16, \"index\": 4, \"width\": 932, \"height\": 286}]"
motivation: 标准RLVR算法采用序列级奖励导致严重信用分配瓶颈，且直接暴露于原始解决方案引发过条件化、答案泄露和后期训练崩溃。
method: 提出非对称元反射自蒸馏，在自教师和学生间插入反射瓶颈，将验证器/同伴/参考反馈压缩为自生成提示和批评，并通过带ReLU门控阈值的因果信息增益实现token级优势调制。
result: 在科学、数学和工具使用基准上显著优于现有基线，实现稳健长程稳定性并成功防止后期崩溃。
conclusion: AMR-SD通过反射瓶颈和因果信息增益有效解决了信用分配问题，提升了复杂推理任务的训练稳定性。
---

## 摘要
大型语言模型（LLM）在复杂推理中的对齐高度依赖于基于可验证奖励的强化学习（RLVR）。然而，GRPO等标准算法对序列级奖励统一应用于所有令牌，造成了严重的信用分配瓶颈。尽管在线自蒸馏试图通过让自教师学习特权上下文来解决这一问题，但直接暴露于原始标准答案往往会导致过条件的教师分布、隐性答案泄露以及后期训练崩溃。为了克服这些局限，我们提出了非对称元反思自蒸馏（AMR-SD）。AMR-SD不直接基于原始参考轨迹，而是引入一个反思瓶颈：它将来自验证器结果、同伴执行轨迹或参考反馈的诊断信号压缩为简洁的自生成苏格拉底式提示与批判。此外，我们引入因果信息增益（CIG）与非对称ReLU门控阈值，将这些反思转化为稀疏、高精度的令牌级优势调节。结合时间退火机制，该方法在保留基础环境奖励的同时过滤了分布噪声。在科学、数学及工具使用基准上的实验表明，AMR-SD显著优于现有基线，实现了稳健的长程稳定性，并成功防止了后期训练崩溃。

## Abstract
The alignment of Large Language Models (LLMs) for complex reasoning heavily relies on Reinforcement Learning with Verifiable Rewards (RLVR). However, standard algorithms like GRPO apply sequence-level rewards uniformly to all tokens, creating a severe credit-assignment bottleneck. While on-policy self-distillation attempts to resolve this by conditioning a self-teacher on privileged contexts, direct exposure to raw oracle solutions often induces over-conditioned teacher distributions, implicit answer leakage, and late-stage training collapse. To overcome these limitations, we propose Asymmetric Meta-Reflective Self-Distillation (AMR-SD). Instead of conditioning directly on raw reference traces, AMR-SD inserts a reflection bottleneck: it compresses diagnostic signals -- from verifier outcomes, peer rollouts, or reference feedback -- into concise, self-generated Socratic hints and critiques. Furthermore, we introduce Causal Information Gain (CIG) with an asymmetric, ReLU-gated threshold to translate these reflections into sparse, highly precise token-level advantage modulations. Combined with temporal annealing, this mechanism preserves the base environmental reward while filtering out distributional noise. Experiments across scientific, mathematical, and tool-use benchmarks demonstrate that AMR-SD significantly outperforms existing baselines, achieving robust long-horizon stability and successfully preventing late-stage collapse.

---

## 论文详细总结（自动生成）

### 论文中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLM）在复杂推理任务中对齐主要依赖基于可验证奖励的强化学习（RLVR）。然而，标准算法如GRPO在序列级别应用统一奖励，将所有令牌视作同等重要，导致严重的“信用分配瓶颈”——关键逻辑推导与表面风格选择获得相同的奖励信号，阻碍优化效果。
- **背景**：在线自蒸馏（On-Policy Self-Distillation）试图通过让模型自身（教师）在特权信息（如真实解）条件下为学生生成密集令牌级监督信号。但直接暴露原始标准解会导致教师分布过条件化、隐性答案泄露，并引发后期训练崩溃。
- **目标**：解决上述瓶颈，实现稳定、精确的令牌级信用分配，同时避免信息泄露和训练不稳定。

#### 2. 方法论
- **核心思想**：提出非对称元反思自蒸馏（AMR-SD），在教师与学生之间插入一个“反思瓶颈”，将各类诊断信号（验证器结果、同伴执行轨迹、参考反馈）压缩为自生成的苏格拉底式提示（hint）和批评（critique），作为教师的调节条件。然后通过因果信息增益（CIG）结合非对称ReLU门控阈值进行稀疏、精确的令牌级优势调制。
- **关键技术细节**：
  1. **元反思阶段（Meta-Reflection）**：对于成功轨迹，模型生成简洁的提示（hint）；对于失败轨迹，生成批评（critique）。这些元反思替代原始标准解作为教师条件，缩小信息不对称，避免过条件化。
  2. **因果信息增益（CIG）**：衡量教师（带特权信息）与学生（无特权）在令牌级别的对数似然比：
     \[
     I^{CIG}_t = \log \frac{\pi_{\theta_{sg}}(a_t|s_t,c_i)}{\pi_\theta(a_t|s_t)}
     \]
     并做钳制（clip）到 \([-κ, κ]\)（κ=5），保证数值稳定性。
  3. **非对称令牌级优势调节**：通过一个ReLU门控阈值τ和一个非对称调制标量Δ_t：
     \[
     \Delta_t = \mathbb{I}_{A_i\ge0}\cdot \lambda_{\text{eff}}\max(0, \hat{I}^{CIG}_t-\tau) + \mathbb{I}_{A_i<0}\cdot \gamma_{\text{eff}}\max(0, -\hat{I}^{CIG}_t-\tau)
     \]
     仅当CIG超过阈值（正奖励轨迹中CIG>τ，负奖励轨迹中CIG<-τ）时才产生调节，确保稀疏性，避免稀释原始环境奖励。
  4. **时间退火（Temporal Annealing）**：随训练步数线性衰减调节系数（λ_eff, γ_eff），在后期完全过渡到纯环境奖励，防止对衰退的蒸馏信号过拟合。
  5. **优化目标**：基于PPO剪辑目标，使用调整后的令牌级优势 \(\hat{A}_{i,t} = A_i\cdot(1+\Delta_t)\) 进行策略优化。
- **算法流程**（文字说明）：每次迭代中，冻结旧策略作为教师；采样G条轨迹，计算序列级优势；对每条轨迹，根据优势符号生成元反思；进行强制解码得到条件概率；计算每个令牌的CIG，应用非对称调节得到令牌级优势；最后用剪辑重要性采样更新策略。

#### 3. 实验设计
- **数据集/场景**：
  - 科学推理与工具使用：SciKnowEval（含化学、物理、生物、材料科学四个学科）和ToolAlpaca（工具使用）。
  - 数学推理：DAPO-Math-17k数据集训练，基准包括AIME 2024/2025、AMC 2023、HMMT。
- **基准模型**：Qwen3-8B、Qwen2.5-7B-Instruct。
- **对比方法**：GRPO（序列级基线）、SDPO（在线自蒸馏）、RLSD（基于奖励锚定的连续缩放方法）。SDPO在数学推理中因性能崩溃被排除。
- **评估指标**：acc@16（16次采样平均准确率）。科学任务报告训练步骤15和75的准确率以展示收敛速度与长期稳定性。

#### 4. 资源与算力
- **文中未明确说明具体GPU型号、数量或总训练时长**。仅提到训练框架为verl，使用FSDP分布式训练和vLLM加速的rollout生成，数学模型训练步数150、科学任务75步，超参数如批次大小（科学256，数学512）、学习率1e-6、G=8等。硬件信息缺失，但论文指出AMR-SD训练速度约为GRPO的80%以上（数学任务前50步：GRPO和RLSD 18小时，AMR-SD 22小时）。

#### 5. 实验数量与充分性
- 实验覆盖多个领域：科学（4个学科×2个模型）、工具使用、数学（4个基准×1个模型），共约10+个设置。
- 消融实验：分析了Meta-Reflection、CIG阈值τ、非对称ReLU门控、时间退火四个组件的影响。
- 额外分析：CIG令牌分布分析（n=50,000令牌），反思质量与推理效率分析（n=480轨迹，反射标记频率比较），训练动态曲线（AIME 2024准确率与奖励）。
- 充分性评价：实验较全面，对比了最新基线，消融设计合理，且进行了统计显著性（如提及差距百分比）和动态分析。但未进行多随机种子重复实验，且仅使用Qwen系列模型，泛化性有待验证。总体客观性较好。

#### 6. 主要结论与发现
- AMR-SD在科学、数学、工具使用基准上显著优于GRPO、SDPO、RLSD，尤其在最难基准（AIME24/25、HMMT）上提升明显（AIME24 69.6% vs GRPO 66.7%）。
- 训练后期稳定性：AMR-SD避免了RLSD和SDPO常见的后期崩溃，实现了持续改进的奖励曲线。
- 推理效率改善：相比基础模型和GRPO，AMR-SD减少了冗长输出但保留了关键反思能力（反思标记频率105次/轨迹，适中）；RLSD由于直接答案泄露使反思频率大幅下降至74次/次，损害探索。
- 消融证实每个组件（元反思、CIG阈值、非对称门控、退火）均贡献显著。

#### 7. 优点
- **创新性**：提出“反思瓶颈”替代直接特权信息输入，有效缓解信息泄露和过条件化，方法新颖且有效。
- **鲁棒性**：通过稀疏的CIG调节和时间退火，在长期训练中保持稳定，防止崩溃，实用性强。
- **全面性**：在科学、数学、工具使用多方面验证，且做了详细的消融和分布分析，结论可靠。
- **可解释性**：CIG分布的左偏分析为超参数选择（λ>γ）提供理论依据。
- **训练效率**：尽管有额外生成阶段，但速度可控（约慢20%），且长期优势弥补开销。

#### 8. 不足与局限
- **依赖反思质量**：AMR-SD的有效性高度依赖于模型自生成元反思的质量。对于缺乏内省能力（链式思维较弱）的模型（如Qwen2.5-7B-Instruct或关闭思考模式的Qwen3-8B），反思质量不足，方法无法超越GRPO。
- **推理开销**：元反思生成引入额外的自回归步骤，增加训练时间（约20%），在资源严格受限场景可能成为实际问题。
- **硬件信息缺失**：实验未报告GPU型号和数量，影响算力需求评估和可复现性。
- **模型泛化性不足**：仅测试了Qwen系列两个模型（7B/8B），未验证在更大模型或其他骨干（如Llama）上的表现。
- **实验公平性**：对比的SDPO在数学推理中因崩溃被排除，可能忽略其潜在优势；且未进行多重随机种子重复实验，统计显著性不够。
- **偏差风险**：CIG分布极度左偏，虽然通过不对称系数平衡，但极端值可能仍影响学习动态；超参数τ、λ、γ的选择依赖于分布分析，在不同任务可能有偏好。

（完）
