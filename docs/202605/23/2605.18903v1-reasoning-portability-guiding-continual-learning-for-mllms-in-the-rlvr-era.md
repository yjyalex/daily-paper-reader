---
title: "Reasoning Portability: Guiding Continual Learning for MLLMs in the RLVR Era"
title_zh: 推理可迁移性：在RLVR时代指导多模态大语言模型的持续学习
authors: "Qiuhe Hong, Yuyang Liu, Shuo Yang, Tiantian Peng, Fei Zhu, Yonghong Tian"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.18903v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于强化学习与可验证奖励的持续学习与推理可迁移性
tldr: "本研究针对多模态大语言模型（MLLMs）在持续学习场景下结合可验证奖励强化学习（RLVR）的挑战，提出基于推理可迁移性（RP）的动态平衡持续学习方法（RDB-CL）。该方法通过度量样本级别先前策略的可重用性，在RLVR训练中自适应调整KL正则化强度，使高RP样本保留已有推理路径，低RP样本探索新路径。实验表明，该方法较原始RLVR基线最后准确率提高12.0%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18903v1/fig-001.webp\", \"caption\": \"Figure 1: Reasoning Portability guides per-sample adaptation in policy space. (a): A portabilityagnostic constraint with static KL is uniform across samples, yielding either under-adaptation or forgetting. (b): RDB-CL introduces reasoning portability (PRP: positive, NRP: negative) to modulate adaptation, steering updates toward the plasticity-stability trade-off.\", \"page\": 2, \"index\": 1, \"width\": 612, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.18903v1/fig-002.webp\", \"caption\": \"Figure 12: Qualitative comparison under representative low reasoning confidence scenarios. Lowconfidence cases typically arise from two sources: domain-gap yet sound reasoning(Top) and incoherent reasoning(Bottom). After training, RDB-CL generates more concise and reliable reasoning traces, while GRPO tends to produce incorrect or irrelevant text.\", \"page\": 19, \"index\": 2, \"width\": 809, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2605.18903v1/fig-003.webp\", \"caption\": \"Figure 13: Qualitative comparison under representative high reasoning confidence scenarios. Highconfidence cases typically fall into two types: (Top) cases where the policy successfully produces task-solving reasoning for new tasks, and (Bottom) cases with seemingly sound reasoning that still fail due to missing task-specific, non-reasoning knowledge.\", \"page\": 20, \"index\": 3, \"width\": 807, \"height\": 700}, {\"url\": \"assets/figures/arxiv/2605.18903v1/fig-004.webp\", \"caption\": \"Figure 4: Workflow of RDB-CL. The previous policy πθt−1 generates reference responses for the current task Tt. Their RP proxy Cθt−1 (rt−1|x) is used to classify samples as PRP or NRP by τ . PRP inherits the baseline KL anchor β0 to preserve the previous policy’s reasoning, while NRP receives a relaxed anchor βLRC < β0 to permit exploration of new reasoning pathways.\", \"page\": 5, \"index\": 4, \"width\": 814, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2605.18903v1/fig-005.webp\", \"caption\": \"Figure 9: Heatmap of pairwise feature-distance changes among VizWiz samples across sequentially trained models with different KL coefficient β. From left to right: (1) ckpt1 → ckpt2 (after VizWiz → after ImageNet), β = 0.03; (2) ckpt1 → ckpt2 , β = 0.15; (3) ckpt2 → ckpt3 (after ImageNet → after IconQA), β = 0.03; (4) ckpt2 → ckpt3 , β = 0.15. All models are trained with GRPO. A stronger KL constraint suppresses feature drift, whereas a weaker one allows greater adaptability.\", \"page\": 16, \"index\": 5, \"width\": 766, \"height\": 190}]"
motivation: 现有持续学习方法难以适应MLLMs与RLVR结合的新范式，且答案级信号在分布外样本上不可靠，需要更稳健的推理级约束。
method: 提出推理可迁移性（RP）指标衡量样本级别策略可重用性，并设计RDB-CL方法根据RP动态调整KL正则化权重，实现推理路径的保留与探索平衡。
result: "在持续学习基准上，RDB-CL最后准确率比原始RLVR基线提升12.0%，并持续优于其他基线方法。"
conclusion: 推理级可迁移性能有效指导MLLMs的持续学习，RDB-CL通过动态正则化平衡了知识保留与探索，为RLVR时代下的持续学习提供了新范式。
---

## 摘要
持续学习中的视觉语言模型旨在不断适应新的多模态任务，同时保留先前知识。将多模态大语言模型与基于可验证奖励的强化学习相结合的新兴范式，为引导持续适应提出了新的模式。推理能力的进步使得在推理层面施加约束成为可能。我们形式化定义了可迁移性，即先前策略行为在新任务上可重用性的样本级度量，并实验表明，推理级信号在分布外样本上保持可靠，而答案级信号则不然。我们将此实现为推理可迁移性，并提出了基于推理的动态平衡持续学习方法，该方法根据推理可迁移性调节基于可验证奖励的强化学习中的每个样本的KL散度正则化：对于高可迁移性样本，紧密锚点保留可重用的推理；对于低可迁移性样本，宽松锚点允许探索新的推理路径。实验表明，我们的方法持续优于基线，在原始RLVR基线基础上将最后准确率提升了12.0%。

## Abstract
Vision-Language Models in Continual Learning (VLM-CL) aim to continuously adapt to new multimodal tasks while retaining prior knowledge. The emerging paradigm that couples Multimodal Large Language Models (MLLMs) with Reinforcement Learning with Verifiable Rewards (RLVR) calls for a new pattern to guide continual adaptation. Advances in reasoning capability now make it feasible to impose constraints at the reasoning level. We formalize portability, a sample-level measure of how reusable the previous policy's behavior is on a new task, and empirically show that reasoning-level signals remain reliable on out-of-distribution samples while answer-level signals do not. We instantiate this as Reasoning Portability (RP) and propose Reasoning-based Dynamic Balance Continual Learning (RDB-CL), which modulates the per-sample Kullback-Leibler regularization in RLVR according to RP: a tight anchor preserves reusable reasoning on high-RP samples, while a relaxed anchor on low-RP samples permits exploration of new reasoning pathways. Experiments show that RDB-CL consistently outperforms baselines, improving Last accuracy by +12.0% over the vanilla RLVR baseline.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：多模态大语言模型与基于可验证奖励的强化学习相结合的新范式（RLVR）正在兴起，如GRPO；持续学习需要使模型适应新任务同时防止忘记旧知识。
- **问题**：传统持续学习方法（如参数空间EWC、特征空间LwF）针对早期架构，难以迁移到现代MLLMs：MLLMs的核心能力是高层次推理而非表示提取，且RLVR的目标是轨迹级而非表示级。需要一种适应新范式的可靠信号来指导持续适应。
- **关键洞察**：作者形式化定义了“可迁移性”（portability）——先前策略的行为在新任务样本上可重用的样本级度量。并实证发现，在分布外样本上，推理级置信度比答案级置信度更可靠，因此可以用推理级信号作为可迁移性的代理。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用推理可迁移性（Reasoning Portability, RP）在每样本上动态调节KL正则化强度，实现知识保留与探索的平衡。
- **关键技术细节**：
  - **RP定义**：采用推理置信度 C_{θ_{t-1}}(r_{t-1}|x) 作为RP的代理，通过P(True)方法测量。
  - **样本分层**：根据阈值τ将新任务样本分为两类：
    - 负向RP（NRP）：置信度<τ → 需要探索新推理路径，采用宽松KL锚点。
    - 正向RP（PRP）：置信度≥τ → 保留可重用推理，采用紧密KL锚点。
  - **动态KL系数**：β_LRC = max(clip_min, [1_{C≥τ} + C·1_{C<τ}])·β0，其中β0是基础KL权重，clip_min是最小裁剪值。
  - **最终目标函数**：替换GRPO中的静态β为动态β_LRC，公式：J_RDBCL(θ) = -1/G Σ_i [1/|oi| Σ_t rθ A_i,t - β_LRC D_KL(πθ || π_{θ_{t-1}})]。
- **算法流程（文字描述）**：对于每个新任务，从先前策略采样参考输出并计算每个样本的RP；根据RP分类获得β_LRC；从当前策略采样输出；使用动态β_LRC的GRPO目标更新参数。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：选自CoIN和UCIT 持续学习基准，包括VizWiz、ImageNet、IconQA三个VQA任务，具有明显领域差异和低零样本性能。训练集从各任务选取1.5k/2.7k/2.3k样本，测试集保留原量（3.6k/5.0k/0.7k）。
- **任务顺序**：Order I: VizWiz → ImageNet → IconQA；Order II: VizWiz → IconQA → ImageNet。
- **基准与对比方法**：
  - 零样本性能（base model）
  - SFT（多任务联合 & 顺序）
  - 架构式持续学习方法：MoELoRA、HiDe-LLaVA（使用扩展参数空间）
  - GRPO及其变体：GRPO、GRPO+EWC、GRPO+LwF
  - 核心方法：RDB-CL（基于GRPO）
- **指标**：Average（各任务平均准确率）、Last（最终任务后所有任务的平均准确率）、BwT（后向迁移，负值表示遗忘）、Finetune（每任务刚训练后表现）。

## 4. 资源与算力
- **硬件**：单节点，6× NVIDIA A6000 GPUs。
- **训练时间开销**：RDB-CL相比原始GRPO增加约13.6%的训练时间（主要由于额外的置信度评估前向传播）。
- **未明确**：总训练时长、每任务训练轮数等具体时间未给出。

## 5. 实验数量与充分性
- **主要实验**：两种任务顺序下对比9种方法，展示Average、Last、BwT指标。
- **大规模验证**：
  - 更长序列（7个任务，加入TextVQA、ScienceQA、VQAv2、GQA）验证长期稳定性。
  - 更大每任务样本量（3倍）验证对数据量的鲁棒性。
- **消融与敏感性**：
  - 推理置信度 vs 答案置信度调制（表2）
  - 动态KL vs 静态KL（不同系数比较，图7）
  - 超参数τ（4个值）和clip_min（4个值）的敏感性分析（表3、4）
- **多样性保留**：Pass@k实验评估策略多样性（图8）。
- **框架泛化**：扩展到DAPO框架（表5）。
- **定性分析**：低置信度与高置信度样本的案例研究（图3、12、13）。
- **评估充分性**：实验覆盖多种场景、消融和超参数分析，结果客观公平，对比基线包括传统CL方法和现代MLLM-CL方法。

## 6. 论文的主要结论与发现
- 推理级置信度在分布外样本上保持可靠的可分离性，而答案级置信度噪声大，因此推理级信号更适合作为持续学习指导。
- RDB-CL在两种任务顺序下均优于原始GRPO：Average accuracy提升5.0%，Last accuracy提升12.0%，BwT改善33.4%。
- 动态KL调制优于任何静态β选择，且不依赖于特定超参数设置（在宽范围内有效）。
- RDB-CL保持策略多样性（Pass@k上升），防止探索崩溃，从而支持持续适应。
- 方法可泛化至其他RLVR框架（如DAPO），增益一致。

## 7. 优点
- **新颖性**：首次从推理层面识别可迁移性信号，突破传统参数/特征/答案级约束的局限。
- **简洁高效**：无需额外参数、内存或推理延迟，仅增加约13.6%训练时间。
- **实验全面**：覆盖多种任务顺序、长序列、大样本、消融、超参数分析、跨框架验证及定性分析，结果稳定。
- **方法通用**：可融入各种KL正则化的RLVR框架，不限于GRPO。
- **实际价值**：解决了RLVR时代MLLMs持续学习中的关键平衡问题，适用于存储和隐私受限场景。

## 8. 不足与局限
- **计算预算限制**：评估限于VQA任务，领域覆盖不足；未来可扩展到更多任务、纯文本LLM持续学习等。
- **可迁移性估计的局限性**：仅使用推理置信度（P(True)）作为估计，其他可能的估计如校准不确定性、验证器信号、自一致性等未探索。
- **调制机制简单**：当前样本级标量调制基于硬阈值和线性缩放，是启发式的；更细粒度或基于学习的调制（如动态阈值）是未来方向。
- **应用限制**：仅针对RLVR框架，尚未在传统SFT或其他RL范式下验证。

（完）
