---
title: "MetaCogAgent: A Metacognitive Multi-Agent LLM Framework with Self-Aware Task Delegation"
title_zh: "MetaCogAgent: 一种具有自我意识任务委派的元认知多智能体大语言模型框架"
authors: "Chenyu Wang, Yang Shu"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17292v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 具有自我意识任务委派的多智能体LLM框架
tldr: "针对现有多智能体LLM系统因忽略智能体自我能力评估而导致过度自信执行超出专长任务的问题，受认知科学元认知理论启发，提出MetaCogAgent框架。每个智能体配备元认知自评估单元，通过结合口头不确定性与历史能力档案估算任务置信度，并采用自适应委派协议将低置信任务路由给更合适智能体，同时利用控制论反馈迭代学习能力边界。在MetaCogEval基准（700任务，5维度）上，MetaCogAgent达82.4%准确率，比最优路由基线高8.7%，且API调用更少。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17292v1/fig-001.webp\", \"caption\": \"Fig. 1. MetaCogAgent architecture. Each agent’s Metacognitive Unit (MCU) produces confidence scores. Low-confidence tasks (c < θ ′) are routed to the Delegation Hub for reassignment. The Result Merger provides feedback to update capability profiles Pi.\", \"page\": 2, \"index\": 1, \"width\": 508, \"height\": 438}]"
motivation: 现有多智能体系统依据预定角色分配任务，未考虑智能体能否准确评估自身能力边界，导致其在超出专业领域时过度自信执行。
method: MetaCogAgent为每个智能体配备元认知自评估单元，通过口头不确定性与历史档案评估任务置信度；低置信任务经跨智能体评估自适应委派给更优智能体；并通过控制论反馈迭代优化能力边界模型。
result: "在MetaCogEval基准上，MetaCogAgent准确率82.4%，比最优基线高8.7%，API调用比AutoGen少5%、比集成投票少34%。"
conclusion: 消融实验证实自评估、自适应委派及能力边界学习三个元认知组件均显著贡献于系统性能，验证了元认知机制在多智能体LLM中的有效性。
---

## 摘要
多智能体大语言模型系统通过智能体协作在解决复杂任务方面展现出潜力。然而，现有框架基于预定义角色分配任务，未考虑智能体能否准确评估自身能力边界，导致其在专业领域之外的任务中过度自信地执行。受认知科学中元认知理论的启发，我们提出MetaCogAgent，一种多智能体大语言模型框架，其中每个智能体配备元认知自我评估单元，在执行前评估任务与能力的匹配度。该框架引入三项贡献：（1）一种自我评估机制，通过结合言语不确定性与历史能力档案来估计每个任务的置信度；（2）一种自适应委派协议，通过跨智能体评估将低置信度任务路由到更合适的智能体；（3）一种能力边界学习模块，通过控制论反馈迭代优化每个智能体的能力模型。在我们构建的MetaCog-Eval基准测试（涵盖5个认知维度的700个任务）上的实验表明，MetaCogAgent实现了82.4%的任务准确率——比最佳路由基线高出8.7%——同时API调用次数比AutoGen少5%，比集成投票少34%。消融研究证实，每个元认知组件都对整体系统性能有贡献。

## Abstract
Multi-agent large language model (LLM) systems have shown promise for solving complex tasks through agent collaboration. However, existing frameworks assign tasks based on predefined roles without considering whether an agent can accurately assess its own competence boundaries, leading to overconfident execution on tasks beyond its expertise. Inspired by metacognition theory from cognitive science, we propose MetaCogAgent, a multi-agent LLM framework where each agent is equipped with a Metacognitive Self-Assessment Unit that evaluates task-capability alignment before execution. The framework introduces three contributions: (1) a self-assessment mechanism that estimates per-task confidence by combining verbalized uncertainty with historical capability profiles; (2) an adaptive delegation protocol that routes low-confidence tasks to better-suited agents through cross-agent evaluation; and (3) a capability boundary learning module that iteratively refines each agent's competence model via cybernetic feedback. Experiments on our constructed MetaCog-Eval benchmark (700 tasks across 5 cognitive dimensions) demonstrate that MetaCogAgent achieves 82.4% task accuracy -- 8.7% above the best routing baseline -- while using 5% fewer API calls than AutoGen and 34% fewer than ensemble voting. Ablation studies confirm that each metacognitive component contributes to overall system performance.

---

## 论文详细总结（自动生成）

### 论文的中文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有多智能体大语言模型（LLM）系统在任务分配时仅依据预定义角色，忽略智能体对自身能力边界的认知。当逻辑推理型智能体被分配到编码任务时，它会盲目执行（“元认知盲区”），导致错误级联。
- **整体含义**：受认知科学中元认知理论（“思考自己的思考”）启发，本文提出让每个智能体在执行任务前评估自身能力，实现自我意识任务委派。这从根本上提升了多智能体系统的可靠性、任务适应性和资源利用效率。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：为每个智能体配备元认知单元（MCU），包含三个组件：
  1. **自我评估**：结合言语置信度（`cv`）与历史能力档案置信度（`cp`），计算复合置信度 `ci(tk)=λ·cv + (1-λ)·cp`。引入**元认知冲突检测**：`δ=|cv-cp|`，当δ超过阈值θδ=0.3时动态收紧委派阈值 `θ'=θ+γ·δ`（γ=0.2），体现“二阶自我怀疑”。
  2. **自适应委派协议**：若 `ci < θ'`，智能体将任务广播给所有其他智能体进行跨评估，选择置信度最高的智能体执行；若所有智能体置信度均低于θ，则进入协作模式（加权投票）。
  3. **能力边界学习**：任务完成后，通过控制论反馈更新能力档案：`p(t+1) = p(t) + α·(rk - p(t))`（α=0.1，EMA更新），近似贝叶斯后验均值，使档案逐步逼近真实能力边界。
- **算法流程**（Algorithm 1）：
  - 调度器轮询分配任务 → 智能体自我评估置信度 → 冲突检测调整阈值 → 若置信度足够则直接执行，否则触发委派跨评估 → 选定最优或协作投票 → 评估结果并更新能力档案。

#### 3. 实验设计：数据集、基准、对比方法
- **数据集**：自建 **MetaCog-Eval** 基准，共700个任务，覆盖5个认知维度（逻辑推理、知识检索、代码生成、数学计算、常识推理），每个维度含易/中/难三级各40个任务，另加100个跨域任务。任务由GPT-4生成，经两名人工标注验证（Cohen's κ=0.81）。
- **对比方法**：共6个基线
  - Single-Agent（单一GPT-4）
  - Round-Robin（轮询分配）
  - Random-Routing（随机分配）
  - Skill-Fixed（基于关键词的规则路由）
  - Majority-Vote（所有智能体执行后多数投票）
  - AutoGen（默认对话协作框架）
- **实验设置**：3个专业智能体（推理、检索、编码），均为GPT-4实例；超参数默认θ=0.5，λ=0.6，α=0.1，θδ=0.3，γ=0.2。

#### 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。系统基于GPT-4 API调用实现，智能体间通过API交互。算力消耗以API调用次数衡量：MetaCogAgent使用1382次，比AutoGen（1456）少5%，比Majority-Vote（2100）少34%。未涉及大规模模型训练，仅在线更新能力档案（EMA更新，计算开销可忽略）。

#### 5. 实验数量与充分性
- **实验组数**：主实验（表II）、难度分层（表III）、维度分析（图2）、消融研究（表IV，5个变体）、置信度校准（图3，ECE指标）、委派行为分析（委派率、流向模式）、灵敏度分析（表V，θ、λ、α各5个取值）、定性案例。共约10组独立分析。
- **充分性与公平性**：
  - 消融实验逐一移除关键组件（自评估、自适应委派、边界学习、跨评估、言语置信度），验证每个组件的贡献，设计完整。
  - 灵敏度分析覆盖核心超参数，表明方法对参数不敏感（α在0.01~0.3间准确率≥80%）。
  - 与基线在同一基准上对比，基线涵盖简单路由、规则路由、集成方法、SOTA框架，对比公平。
  - 不足之处：所有结果基于单次运行，未报告方差或统计显著性；基准数据集自建且依赖GPT-4，可能存在分布偏差。

#### 6. 论文的主要结论与发现
- **主结果**：MetaCogAgent在MetaCog-Eval上达到82.4%准确率，比最佳路由基线AutoGen高8.7%，比集成投票高5.3%；API调用更少（1382次）。
- **组件贡献**：移除自我评估使准确率下降6.8%，移除自适应委派下降5.1%，移除边界学习下降3.2%，证实每个模块有效。
- **校准质量**：期望校准误差（ECE）为0.087，表明置信度可靠预测性能。
- **委派模式**：委派率随难度增加（Easy 10.5% → Hard 41.5%），跨域任务委派率最高（63%），且83.5%的委派任务被正确路由。
- **能力档案收敛**：无显式指定下，智能体档案自动专业化（如推理智能体LR=0.89，CG=0.62；编码智能体反之）。

#### 7. 优点
- **方法论创新**：首次将元认知机制引入多智能体LLM，填补“自我能力评估”空白。
- **设计精巧**：冲突检测实现二阶元认知，控制论反馈实现持续边界学习，理论上有贝叶斯解释。
- **实验全面**：消融、灵敏度、校准分析完备，委派行为分析深入（流向模式、跨域特点）。
- **效率高**：在提升准确率的同时减少API调用，具备实际部署价值。
- **可解释性**：置信度与委派决策透明，定性案例生动展示避免错误的过程。

#### 8. 不足与局限
- **规模有限**：仅3个智能体，对更大规模智能体群体的可扩展性未验证。
- **静态假设**：能力档案学习假设能力分布平稳，未处理提示修改、模型更新等非平稳场景。
- **基准偏差**：MetaCog-Eval依赖GPT-4生成及验证，可能引入系统偏差；未在公开基准（如MMLU、HumanEval）上测试。
- **缺乏统计健壮性**：所有结果单次运行，无方差或置信区间报告。
- **计算成本隐蔽**：虽然API调用次数减少，但跨评估需要额外短推理调用，且每次委派最多N-1次，实际延迟可能增加。
- **适用条件**：在智能体能力同质时收益有限，需能力异构性。

（完）
