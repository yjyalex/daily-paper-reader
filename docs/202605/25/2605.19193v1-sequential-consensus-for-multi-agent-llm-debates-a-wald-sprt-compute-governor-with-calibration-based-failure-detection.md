---
title: "Sequential Consensus for Multi-Agent LLM Debates: A Wald-SPRT compute governor with calibration-based failure detection"
title_zh: 多智能体LLM辩论的顺序共识：一种带有基于校准的故障检测的Wald-SPRT计算调控器
authors: Andrea Morandi
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19193v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 提出基于Wald SPRT的多智能体LLM辩论顺序共识机制
tldr: "多智能体LLM辩论通常使用固定轮数，导致简单问题过度计算、困难问题计算不足。本文提出将Wald的SPRT作为计算调控器，每轮后由LLM裁判输出共识分数，累积似然比以决定是否停止。在GSM8K上平均1.01轮停止，准确率97.0%，对比固定5轮15次调用准确率99.0%，调用减少3.7倍；在MMLU上因校准失效，99.5%项目达到最大轮数。该方法提供低成本的计算控制和故障检测。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19193v1/fig-001.webp\", \"caption\": \"TABLE III REAL-LLM EVALUATION ON MMLU (200 ATTEMPTED RANDOM ITEMS, ALL-SUBJECTS TEST SPLIT) AND GSM8K (200 ATTEMPTED RANDOM ITEMS, TEST SPLIT). THE n COLUMN REPORTS VALID/EVALUABLE ITEMS AFTER ANSWER PARSING AND FILTERING. AGENTS: GPT-5, CLAUDE-OPUS-4-6, GEMINI-2.5-PRO. JUDGE: CLAUDE-OPUS-4-6. SPRT THRESHOLDS α = β = 0.05, Rmax = 8, CALIBRATION FIT ON A DISJOINT n = 40 SUBSET PER TASK. AVG CALLS COUNTS THE CALLS REQUIRED BY EACH PROTOCOL; THE FIXED-5 BASELINE DOES NOT RUN A PER-ROUND STOPPING JUDGE.\", \"page\": 7, \"index\": 1, \"width\": 627, \"height\": 194}]"
motivation: 固定轮数的多智能体辩论在简单问题上浪费计算，在困难问题上计算不足。
method: 采用Wald的SPRT作为计算调控器，每轮后LLM裁判输出共识分数，在Beta似然族下累积对数似然比，超过边界即停止或达到最大轮数。
result: "GSM8K上平均1.01轮（4.06次调用）达97.0%准确率，比固定5轮（15次调用）减少3.7倍调用，准确率仅降2pp；MMLU上校准失效，99.5%项目达到最大轮数。"
conclusion: SPRT并非提高辩论准确性，而是作为一种低成本的计算控制和故障检测层，适用于多智能体LLM系统。
---

## 摘要
多智能体LLM辩论提高了事实性和推理能力，但大多数配方选择固定的轮次计数，在简单问题上过度花费，而在困难问题上花费不足。我们将Wald的序贯概率比检验（SPRT）适配为LLM辩论的即插即用计算调控器。每轮之后，一个LLM评判者针对最新的智能体立场给出一个[0,1]的共识分数；一个Wald监控器在Beta似然族下累积“有用收敛”相对于“尚未有用”的对数似然比，并在任一边界被跨越时停止，或者在R_max时返回一个带上限的最佳努力结果。在独立同分布假设下，该规则继承了SPRT的第一类/第二类错误保证；在部署中，校准本身是更重要的对象，因为它估计在给定领域内评判者分数是否真正区分了有用和无用的收敛。我们评估了两个轨道：(i) 在校准的Beta模型下进行蒙特卡洛研究，刻画工作曲线、错误率、上限行为和敏感性；(ii) 在200个尝试的MMLU和200个尝试的GSM8K项目上使用三个异构智能体（gpt-5、claude-opus-4-6、gemini-2.5-pro）和一个claude-opus-4-6评判者进行真实LLM评估，使用不相交的40个项目校准子集。在GSM8K上，该规则平均在1.01轮（4.06次LLM调用）内停止，准确率为97.0%，而固定5轮辩论在15次调用下准确率为99.0%：调用减少3.7倍，准确率下降2个百分点。在MMLU上，校准后的KL散度几乎坍缩到0，该规则在99.5%的项目上以2.1倍成本达到上限。关键不在于SPRT使辩论更准确，而在于经典的序贯检验为多智能体LLM系统提供了一个低成本的计算控制和故障检测层。

## Abstract
Multi-agent LLM debate improves factuality and reasoning, but most recipes pick a fixed round count, over-spending on easy items and under-spending on hard ones. We adapt Wald's Sequential Probability Ratio Test (SPRT) as a plug-in compute governor for LLM debates. After each round, an LLM judge emits a [0,1] consensus score on the latest agent positions; a Wald monitor accumulates the log-likelihood ratio of "useful convergence" vs "not yet useful" under a Beta likelihood family, and stops when either boundary is crossed or returns a capped best-effort outcome at R_max. Under i.i.d. assumptions the rule inherits SPRT type-I/type-II error guarantees; in deployment the calibration itself is the more important object, since it estimates whether the judge score actually separates useful from unhelpful convergence in a given domain. We evaluate two tracks: (i) a Monte-Carlo study under calibrated Beta models characterising working curves, error rates, capping behaviour, and sensitivity; and (ii) a real-LLM evaluation on 200 attempted MMLU and 200 attempted GSM8K items with three heterogeneous agents (gpt-5, claude-opus-4-6, gemini-2.5-pro) and a claude-opus-4-6 judge, using disjoint 40-item calibration subsets. On GSM8K the rule stops in 1.01 average rounds (4.06 LLM calls) at 97.0% accuracy vs 99.0% for fixed-5 debate at 15 calls: a 3.7x call reduction at -2pp accuracy. On MMLU the calibrated KL collapses to about 0 and the rule caps on 99.5% of items at 2.1x cost. The takeaway is not that SPRT makes debate more accurate, but that a classical sequential test serves as a cheap compute-control and failure-detection layer for multi-agent LLM systems.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多智能体LLM辩论已被证明能提升事实性和推理能力，但现有方法普遍采用固定轮数（如2-5轮），导致在简单问题上过度计算、在困难问题上计算不足。缺乏自适应停止机制。
- **核心问题**：如何根据每轮辩论的共识信号，在保证一定错误率的前提下，动态决定何时停止辩论，从而节约计算资源。
- **整体含义**：本文将经典的Wald序贯概率比检验（SPRT）作为即插即用的计算调控器，应用于多智能体LLM辩论，使其能自适应地终止，并在校准失效时发出故障信号。最终目标不是提高准确性，而是提供低成本的**计算控制与故障检测层**。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：每轮辩论后，由LLM裁判对当前智能体立场输出一个[0,1]共识分数；将该分数视为序贯观测，利用SPRT在Beta似然族下累积对数似然比，当累积量超过上界（接受H1：已共识）或下界（接受H0：未共识）时立即停止；否则继续至最大轮数R_max。
- **关键技术细节**：
  - **二元假设检验**：H0（未共识）与H1（已共识），共识分数在各自假设下服从Beta分布。
  - **累积对数似然比**：Λ_R = Σ_{r=1}^R log[f1(s_r)/f0(s_r)]，边界A = log((1-β)/α)，B = log(β/(1-α))。
  - **校准**：在不相交的小规模校准集上，通过矩估计拟合Beta参数（对每个任务领域分别拟合），其中f0来自于错误答案的共识分数，f1来自于正确答案的共识分数（即“有用收敛”）。
  - **算法流程**（Algorithm 1）：初始化Λ=0，每轮获得共识分数并裁剪到(ε,1-ε)，更新Λ，检查是否超越边界；若达到R_max未决策则作为“capped”输出最佳努力结果。
- **理论保证**：在i.i.d.假设下，第一类错误≤α，第二类错误≤β；期望轮数与KL散度成反比。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 真实LLM评估：MMLU（200条随机题目，多选）和GSM8K（200条，数学应用题）。每个任务使用不相交的40条校准子集。
  - 模拟实验：使用三个规划目标校准（MMLU、GSM8K、JudgeBench），生成Beta分布随机序列。
- **基准**：
  - B1：单轮投票（无辩论，3个智能体各自回答后多数投票，3次LLM调用）。
  - B2：固定5轮辩论（Society-of-Minds风格，无提前停止，15次LLM调用）。
  - B3：本文的SPRT规则（α=β=0.05，R_max=8）。
- **智能体与裁判**：三个异构智能体（gpt-5、claude-opus-4-6、gemini-2.5-pro），裁判使用claude-opus-4-6（温度0，严格JSON输出）。
- **模拟实验**：每个假设下N=50000次蒙特卡洛轨迹，使用规划校准的Beta参数。

## 4. 资源与算力

- **未明确说明**：论文未提供使用的GPU型号、数量或训练/推理总时长。仅提及使用OpenAI兼容网关调用gpt-5、claude-opus-4-6、gemini-2.5-pro，以及裁判claude-opus-4-6，推断为云端API调用，未涉及本地GPU资源。实验总轮数（200条MMLU+200条GSM8K乘以三种协议）使得LLM调用次数约为数千次，但无具体硬件信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 模拟实验：3个任务校准，每个假设下50000条轨迹，共6组大规模模拟。
  - 真实LLM评估：2个任务（MMLU、GSM8K），每个任务200条题目，比较3种协议。还进行了敏感性分析、AR(1)相关性测试、不同难度分档等。
- **充分性分析**：
  - **充分**：模拟覆盖了各种校准情形（良好分离、差分离、非i.i.d.序列），真实评估验证了两种截然不同的场景（GSM8K上有效，MMLU上校准失败），并与多种简单基线（固定阈值、稳定性阈值）对比。
  - **客观公平**：使用固定随机种子、严格JSON解析、相同提示模板、相同硬件环境（API）。承认在MMLU上B1已经接近天花板，B2未带来改进，但仍客观报告了所有结果。
  - **不足**：未测试其他任务类型（如JudgeBench偏好判断）、未系统评估裁判与智能体重叠的影响、未探索不同K值（智能体数量）的敏感性。

## 6. 论文的主要结论与发现

- **主要结论**：SPRT-on-共识分数可以作为有效的**计算调控器**和**校准诊断**。当共识分数与正确性相关时（如GSM8K），大幅降低LLM调用（3.7倍），仅损失2个百分点准确率；当校准失效（MMLU上KL≈0）时，SPRT正确报告不可停止，导致2.1倍成本增加。
- **具体发现**：
  - GSM8K：B3平均1.01轮（4.06调用），准确率97.0%；B2为15调用99.0%。调用减少3.7倍，准确率下降2pp。
  - MMLU：校准后KL≈0，99.5%的项目达到R_max=8，平均31.96调用，准确率94.2%，与B2（93.8%）无显著差异，但成本上升2.1倍。
  - 模拟下SPRT相对固定5轮平均节省约50%轮次，错误率低于名义边界。
  - 非i.i.d.相关性（ρ≤0.6）时错误率适度膨胀（≤1.5×名义值），仍为可行近似。
- **核心信息**：该方法不是提升准确性，而是提供计算控制和故障检测；校准本身是诊断工具，决定该领域是否适合提前停止。

## 7. 优点

- **新颖性**：首次将序贯统计检验（SPRT）引入多智能体LLM辩论的自适应停止，与现有固定轮次工作正交。
- **即插即用**：算法仅需在两行代码内实现，可无修改地集成到任何辩论编排器。
- **理论保证**：在i.i.d.假设下提供第一类/第二类错误界，期望轮数有KL散度下界。
- **校准诊断价值**：通过校准分离度（KL）自动预警方法在哪些领域无效（如MMLU），避免盲目部署。
- **计算高效**：在可行任务中实现3.7倍LLM调用减少，同时保持高准确率。
- **实验设计全面**：包含模拟和真实评估，覆盖多种校准、非i.i.d.敏感性、错误率分析、决策类型细分。
- **结果透明**：明确报告了负面结果（MMLU上的校准失效）并进行了解释，未隐瞒。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅测试了MMLU（多选）和GSM8K（算术），缺少对偏好判断任务（如JudgeBench）和长文本生成任务的真实评估。
  - 未评估不同智能体数量K（仅用K=3）或不同裁判/智能体组合的影响。
  - 未在工业规模生产环境中验证。
- **假设限制**：
  - i.i.d.假设明显违反（辩论轮次间相关），论文虽进行了相关性分析，但仍是近似。
  - Beta分布是经验选择，真实评分分布可能偏离。
  - 校准需要该领域标签，实际中获取困难；校准样本仅40条，对MMLU不够用。
- **方法局限**：
  - 裁判为单一故障点（若裁判偏向高分/低分，会误导停止）。
  - 无法防止对抗性智能体故意制造虚假共识欺骗裁判（Goodhart效应）。
  - 对多选任务（MMLU）基本无效，需更精细的裁判设计。
- **结论的统计力量**：在主要结果中，B3与B2在GSM8K上的准确率差异（2pp）未达到统计显著（约1.4σ），因此声称“节省计算而损失少量准确率”需更多证据。
- **其他**：未讨论token成本的具体经济账，仅提供LLM调用次数；未探索更复杂的动态校准或自适应似然模型。

（完）
