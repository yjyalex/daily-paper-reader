---
title: "LamPO: A Lambda Style Policy Optimization for Reasoning Language Models"
title_zh: LamPO：一种面向推理语言模型的Lambda风格策略优化
authors: "Zhe Yuan, Yipeng Zhou, Jinghan Li, Xinyuan Chen, Bowen Deng, Zhiqian Chen, Liang Zhao"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21235v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 面向推理模型的Lambda风格策略优化，使用配对分解优势
tldr: 针对GRPO等组相对目标忽略候选响应间细粒度关系导致稀疏奖励下信用分配弱的问题，提出LamPO方法，采用成对分解优势替代标量优势，并引入基于对数概率差异的置信度权重和可选ROUGE-L稠密奖励。在多个推理基准上使用不同规模模型，LamPO相比GRPO等取得更优性能，训练更稳定，样本效率更高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21235v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison on reasoning benchmarks. The best result in each model block is bolded.\", \"page\": 8, \"index\": 1, \"width\": 562, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.21235v1/fig-002.webp\", \"caption\": \"Figure 1: Training dynamics on Qwen3-1.7B, Qwen3-4B, and Phi-4-mini. Left panels show reward accuracy; right panels show mean completion length.\", \"page\": 10, \"index\": 2, \"width\": 743, \"height\": 598}]"
motivation: 现有基于组的策略优化目标（如GRPO）用标量统计量总结采样组，丢弃了候选响应间的细粒度关系，在稀疏结果奖励下削弱了信用分配。
method: LamPO用成对分解优势聚合组内成对奖励差异，并通过序列对数概率差异计算置信度权重调制每个比较，保留PPO的无评论家和裁剪更新结构，可添加ROUGE-L稠密辅助奖励。
result: 在AIME24等四个基准上使用Qwen3-1.7B等三个模型，LamPO一致优于GRPO及近期RLVR变体，训练更稳定且样本效率更高。
conclusion: LamPO通过成对分解优势和置信度权重，有效改善推理语言模型在稀疏奖励下的策略优化，提升性能和训练稳定性。
---

## 摘要
基于可验证奖励的强化学习(RLVR)已成为在数学、编码和科学问答等任务上改进推理语言模型的有效范式。然而，广泛使用的群体相对目标（如GRPO）通过标量统计对每个采样组进行汇总，因此丢弃了候选回答之间的细粒度关系信息。这在稀疏结果奖励下削弱了信用分配，尤其是当多个生成的解决方案仅在推理质量上存在细微差异时。我们提出了	extbf{LamPO}，一种	extbf{Lambda风格策略优化}方法，用	extit{成对分解优势}替换标量群体优势。LamPO聚合每个回答组内的成对奖励差距，并通过从序列对数概率差异计算出的置信度感知权重调制每个比较，同时保留PPO风格优化的无评论家和裁剪更新结构。当参考解可用时，我们进一步添加一个轻量级的基于ROUGE-L的密集辅助奖励以减少奖励稀疏性。在使用Qwen3-1.7B、Qwen3-4B和Phi-4-mini进行的AIME24、AIME25、MATH-500和GPQA-Diamond实验表明，LamPO在GRPO和最近的RLVR变体上持续改进，具有更稳定的训练动态和更好的样本效率。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become an effective paradigm for improving reasoning language models on tasks such as mathematics, coding, and scientific question answering. However, widely used group-relative objectives, such as GRPO, summarize each sampled group with scalar statistics and therefore discard fine-grained relational information among candidate responses. This weakens credit assignment under sparse outcome rewards, especially when multiple generated solutions differ only subtly in reasoning quality. We propose \textbf{LamPO}, a \textbf{Lambda-Style Policy Optimization} method that replaces scalar group advantages with a \emph{Pairwise Decomposed Advantage}. LamPO aggregates pairwise reward gaps within each response group and modulates each comparison by a confidence-aware weight computed from sequence log-probability differences, while retaining the critic-free and clipped-update structure of PPO-style optimization. When reference solutions are available, we further add a lightweight ROUGE-L-based dense auxiliary reward to reduce reward sparsity. Experiments on AIME24, AIME25, MATH-500, and GPQA-Diamond with Qwen3-1.7B, Qwen3-4B, and Phi-4-mini show that LamPO consistently improves over GRPO and recent RLVR variants, with more stable training dynamics and better sample efficiency.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：基于可验证奖励的强化学习（RLVR）在数学、代码、科学问答等推理任务中显著提升了语言模型的性能。其中，组相对策略优化（GRPO）因无需值函数模型且训练稳定而被广泛采用。
- **核心问题**：GRPO 等方法将每个采样组内的候选回答压缩为标量统计量（均值和标准差），丢弃了回答之间的细粒度关系信息。在稀疏的结果奖励（如仅对/错）下，许多部分正确或接近正确的回答得到相同反馈，导致信用分配弱化，尤其是当多个生成解仅推理质量存在细微差异时。
- **整体含义**：提出 LamPO（Lambda 风格策略优化），通过保留组内成对关系来提供更丰富的学习信号，从而提升 RLVR 在推理任务上的性能、训练稳定性和样本效率。

### 2. 论文提出的方法论
- **核心思想**：用 **成对分解优势（Pairwise Decomposed Advantage, PDA）** 替代 GRPO 中标量化的组归一化优势。PDA 聚合组内所有回答对之间的奖励差值，并用基于序列对数概率差异的置信度权重调制每个比较，同时保留无评论家（critic-free）和 PPO 风格裁剪更新的结构。
- **关键技术细节**：
  - 对于每个回答 \( o_i \)，定义其旧策略下的序列对数概率 \( s_i = \log \pi_{\theta_{\text{old}}}(o_i \mid q) \)。
  - 对任意一对回答 \( (o_i, o_j) \)，计算奖励差 \( R(o_i)-R(o_j) \) 和置信度权重 \( \sigma(\Delta s_{ij}/\tau) \)，其中 \( \sigma \) 是 sigmoid 函数，\( \tau \) 是温度参数。
  - LamPO 优势：\( A_\lambda(o_i) = \frac{1}{G-1}\sum_{j\neq i} (R(o_i)-R(o_j)) \sigma(\Delta s_{ij}/\tau) \)。
  - 策略目标保留 PPO 风格裁剪，并加入 KL 正则化项 \( \beta D_{\text{KL}}(\pi_\theta \| \pi_{\text{ref}}) \)。
  - **可选辅助奖励**：当有参考解时，添加基于 ROUGE-L 的稠密奖励 \( R_{\text{sem}}(o_i) = \text{ROUGE-L}_\text{F1}(o_i, y^*) \)，最终奖励为 \( R(o_i) = R_{\text{ext}}(o_i) + \lambda_{\text{sem}} R_{\text{sem}}(o_i) \)。
- **梯度解释**：忽略裁剪和 KL 后，更新梯度正比于 \( \sum_{j\neq i} (R(o_i)-R(o_j)) \sigma(\Delta s_{ij}/\tau) \nabla_\theta \log \pi_\theta(o_i \mid q) \)，即显式分解为成对奖励比较，每个方向由当前策略的相对偏好加权。
- **算法流程**：对每个 prompt 采样组 \( O_q \)，计算奖励、序列分数，然后对每个回答按上述公式计算 PDA，可选组内归一化，最后优化目标。

### 3. 实验设计
- **训练数据集**：Mixture-of-Thoughts（约 35 万问题及验证推理轨迹）。
- **评估基准**：AIME24、AIME25、MATH-500、GPQA-Diamond（涵盖竞赛数学、通用数学和科学 QA）。
- **对比方法**：
  - 基线和变体：CoT 提示、GRPO、DAPO、GSPO、SimpleRL-Zoo、PRIME、RLPR。
- **模型规模**：Qwen3-1.7B、Qwen3-4B、Phi-4-mini。
- **超参数**：默认组大小 G=8，温度 τ=1.5，稠密奖励系数 λ_sem=1.0（当参考解可用时）。

### 4. 资源与算力
- **论文未明确提及使用的 GPU 型号、数量及训练时长**。仅提到实验设置中“同一 rollout 和优化设置用于 GRPO 和 LamPO”，但未披露具体算力消耗。需注意该点。

### 5. 实验数量与充分性
- 实验数量较充分：
  - **主实验**：在 3 个模型上 × 4 个基准 × 多个对比方法，结果如表 1 所示。
  - **消融实验**（表 2）：探究温度 τ（0.5, 1.5, 5.0, 7.0）和稠密奖励系数 λ_sem（0.0 vs 1.0）的影响，共 6 组实验。
  - **训练动态分析**（图 1）：展示奖励准确性和完成长度随步数的变化，定性对比 GRPO 和 LamPO。
- 公平性考虑：对比方法包括多种近期 RLVR 变体，并使用相同版本（GRPO 等默认设置）；在相同模型上公平比较；消融实验覆盖关键超参数。
- 局限：未验证更大组大小（如 G > 8）或更多样化的任务（如开放域对话、多模态）。

### 6. 论文的主要结论与发现
- LamPO 在所有三个模型和四个基准上一致优于 GRPO 及近期 RLVR 基线（DAPO、GSPO 等）。例如在 Qwen3-1.7B 上平均得分比 GRPO 高 2.18，在 Qwen3-4B 上高 1.69。
- 消融实验表明：
  - 默认 τ=1.5 最优，过小或过大会损伤性能。
  - 稠密辅助奖励（λ_sem=1.0）可进一步提升性能，但成对优势本身是主要改进来源。
- 训练动态显示 LamPO 奖励曲线更平滑、完成长度更稳定，特别在小模型上更明显。

### 7. 优点
- **方法创新性**：明确指出现有组归一化损失细粒度关系，并针对性地提出成对分解优势，理论动机清晰。
- **无额外值函数**：保持无评论家结构，计算成本仅增加 O(G²)（组大小 G 较小，实际可接受）。
- **置信度加权**：通过序列对数概率差异动态调制每个成对比较，兼顾当前策略偏好，提供更准确的信用分配。
- **可扩展稠密奖励**：仅在参考解可用时添加轻量 ROUGE-L 奖励，不破坏原始正确性奖励，有效缓解奖励稀疏问题。
- **实验验证充分**：覆盖三个常见模型家族、四个挑战性基准、多个最近基线，并附带消融和训练动态分析，结果一致性高。

### 8. 不足与局限
- **计算成本**：组大小 G 增大时 O(G²) 可能成为瓶颈，论文未讨论近似或采样式方案。
- **依赖可靠奖励**：若奖励模型有噪声或主观，成对奖励差可能放大错误信号；论文未涉及不确定性感知建模。
- **ROUGE-L 辅助奖励的局限性**：仅捕捉词汇重叠，不保证推理正确性；且依赖参考解，不适用于无参考解的任务。
- **应用范围有限**：实验仅覆盖数学和科学推理，未验证开放域对话、长程规划、多模态推理等场景。
- **算力未披露**：缺乏训练资源细节，不利于复现和成本评估。

（完）
