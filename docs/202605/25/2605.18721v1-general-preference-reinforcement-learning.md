---
title: General Preference Reinforcement Learning
title_zh: 通用偏好强化学习
authors: "Muhammad Umer, Muhammad Ahmed Mohsin, Ahsan Bilal, Arslan Chaudhry, Andreas Haupt, Sanmi Koyejo, Emily Fox, John M. Cioffi"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18721v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 面向大语言模型对齐的通用偏好强化学习
tldr: 论文提出通用偏好强化学习（GPRL），通过将偏好建模为多维结构而非标量分数，结合在线强化学习的探索优势和偏好优化的开放性，利用通用偏好模型（GPM）进行k维比较，并设计归一化优势聚合和漂移监测，有效防止奖励黑客，在多个基准上取得优于现有方法的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18721v1/fig-001.webp\", \"caption\": \"Table 1: AlpacaEval 2.0. LC. WR, WR, and average response length against the gpt-4-turbo reference, using Llama-3-8B-Instruct as the base policy and gpt-4-turbo as judge. SPPO and GPO follow the published three-iteration protocol, while GRPO and GPRL are reported at the matched third epoch so that all reward-model-based methods are compared at similar compute.\", \"page\": 6, \"index\": 1, \"width\": 764, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-002.webp\", \"caption\": \"Figure 1: Landscape of LLM post-training methods, organized by supervision source and training regime. Online RL with a scalar RM reaches open-ended tasks but suffers reward hacking; GPRL fills the gap with a structured, multi-dimensional reward.\", \"page\": 2, \"index\": 2, \"width\": 394, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-003.webp\", \"caption\": \"Table 3: WildBench. Calibrated WB-Score and task-balanced WB-Reward against the three official baseline references (gpt-4-turbo, claude-3-haiku, llama-2-70b-chat), same setup as Table 1.\", \"page\": 7, \"index\": 3, \"width\": 744, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-004.webp\", \"caption\": \"Table 2: Arena-Hard v2 and MT-Bench. Win rate (WR) against the Arena-Hard gpt-4-0314 reference (AH2) and MT-Bench score, same setup as Table 1.\", \"page\": 7, \"index\": 4, \"width\": 682, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-005.webp\", \"caption\": \"Table 11: Implementation details for GPRL. Hyperparameter values used to produce the main results in Section 4 unless an ablation overrides them.\", \"page\": 22, \"index\": 5, \"width\": 727, \"height\": 719}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-006.webp\", \"caption\": \"Figure 4: Scaling and per-category breakdown. (a) AlpacaEval 2.0 LC. WR across five training epochs at both reward-model scales, with the controller enabled holding near its peak through epoch 5 and the controller disabled degrading once drift develops. (b, c) Per-category scores on MT-Bench and WildBench, where GPRL leads on the categories that match the supervision and on structural categories while remaining within noise of the strongest baseline elsewhere.\", \"page\": 8, \"index\": 6, \"width\": 812, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of GPRL. The policy πθ samples G responses per prompt, GPM embeds them, and R≻ produces k pairwise score matrices that yield per-dimension advantages. The aggregate drives the GRPO-style clipped surrogate, while a drift monitor D(t) adapts the dimensional weights and β to suppress reward hacking.\", \"page\": 4, \"index\": 7, \"width\": 818, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-008.webp\", \"caption\": \"Table 6: Drift controller ablation. Disabling the controller corresponds to τ =∞ and ml(t) ≡ 1. The first-epoch row on the 8B GPM serves as the no-drift sanity check, while the third-epoch rows capture the regime our main results use, with the 2B GPM included to show that drift develops earlier under noisier supervision.\", \"page\": 19, \"index\": 8, \"width\": 672, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2605.18721v1/fig-009.webp\", \"caption\": \"Figure 3: Dimensional drift distinguishes healthy training from reward hacking. (a) The variance profile α(t) holds its initial shape on a healthy GPRL run. (b) Under hacking, it collapses onto a single dimension l⋆. (c) D(t) stays near zero on the healthy run and crosses τ at t′ on the hacked one, allowing the corrected trajectory to engage the controller at t′ and pull back as the profile rebalances, while a BT reward model climbs monotonically and offers no comparable signal.\", \"page\": 5, \"index\": 9, \"width\": 823, \"height\": 216}]"
motivation: 现有在线RL和偏好优化方法脱节，标量奖励模型在多维质量问题上易被利用，需要一种结构化的偏好模型来统一两者。
method: 构建通用偏好模型（GPM）将响应嵌入k个斜对称子空间，计算每维组相对优势并归一化，再用上下文相关特征值聚合；同时引入闭环漂移监测来动态调整维度权重和信任区域。
result: "在Llama-3-8B-Instruct上，GPRL在AlpacaEval 2.0达到56.51%长度控制胜率，在Arena-Hard、MT-Bench、WildBench等基准上超越SimPO和SPPO。"
conclusion: GPRL通过结构化偏好和在线探索的结合，有效解决了奖励黑客问题，为开放任务的对齐提供了更鲁棒的方案。
---

## 摘要
后训练将大语言模型（LLM）对齐分成了两个基本脱节的轨道。具有可验证奖励的在线强化学习（RL）能够激发数学和代码方面的新兴推理能力，但依赖于一个无法处理开放式任务的程序化验证器；而偏好优化则处理开放式生成，却放弃了驱动在线RL的持续探索。弥合这一差距需要一个用于开放式质量的验证器，但标量奖励模型并非适合该任务的形态。质量是多维的，任何标量分数都是一个不完整的代理，使得在线RL会坍缩到该分数最敏感的单一轴线上。我们转而采用通用偏好模型（GPM），它将响应嵌入到k个斜对称子空间中，并将偏好表示为一种结构化的、具有非传递性意识的比较。基于此，我们提出了通用偏好强化学习（GPRL），它将k路结构延续到策略更新中。GPRL计算每个维度的分组相对优势，在每个维度上独立归一化以防止任何轴占主导，并使用上下文相关的特征值进行聚合。同样的结构支撑了一个闭环漂移监控器，它能够检测到单轴剥削，并通过重新加权维度和收紧信任区域来自动纠正。从Llama-3-8B-Instruct开始，GPRL在AlpacaEval 2.0上达到了56.51%的长度控制胜率，同时在Arena-Hard、MT-Bench和WildBench上通过在长时间训练中抵抗奖励破解，优于SimPO和SPPO。

## Abstract
Post-training has split large language model (LLM) alignment into two largely disconnected tracks. Online reinforcement learning (RL) with verifiable rewards drives emergent reasoning on math and code but depends on a programmatic verifier that cannot reach open-ended tasks, while preference optimization handles open-ended generation yet forgoes the continuous exploration that powers online RL. Closing this gap requires a verifier for open-ended quality, but a scalar reward model is the wrong shape for the job. Quality is multi-dimensional, and any scalar score is an incomplete proxy that lets online RL collapse onto whichever axis the score is most sensitive to. We turn instead to the General Preference Model (GPM), which embeds responses into $k$ skew-symmetric subspaces and represents preference as a structured, intransitivity-aware comparison. Building on this, we propose General Preference Reinforcement Learning (GPRL), which carries the $k$-way structure through to the policy update. GPRL computes per-dimension group-relative advantages, normalizes each on its own scale so no axis can dominate, and aggregates them with context-dependent eigenvalues. The same structure powers a closed-loop drift monitor that detects single-axis exploitation and corrects it on the fly by reweighting dimensions and tightening the trust region. Starting from $\texttt{Llama-3-8B-Instruct}$, GPRL reaches a length-controlled win rate of $56.51\%$ on AlpacaEval~2.0 while also outperforming SimPO and SPPO on Arena-Hard, MT-Bench, and WildBench by resisting reward hacking across extended training runs.

---

## 论文详细总结（自动生成）

# 通用偏好强化学习（GPRL）详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前大语言模型（LLM）的后训练阶段存在两个脱节的轨道：  
  - **在线强化学习（RL）**：利用可验证奖励（如数学代码正确性）驱动推理能力，但仅适用于有程序化验证器的封闭任务，无法处理开放生成。  
  - **偏好优化**：处理开放生成（如对话、写作），但放弃了在线RL的持续探索机制，且依赖标量奖励模型。  
- **根本矛盾**：开放任务的质量是多维的（如有用性、诚实性、无害性等），标量奖励模型是单维代理，在线RL会坍缩到该分数最敏感的单一轴线上，导致奖励黑客。  
- **论文目标**：弥合在线RL与偏好优化的鸿沟，为开放任务提供结构化、多维的偏好验证器，提出**通用偏好强化学习（GPRL）**。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- 将偏好建模为**多维结构**而非标量分数，利用**通用偏好模型（GPM）**将响应嵌入k个斜对称子空间，表示非传递性比较。  
- 将GPM的k维结构延续到策略更新中，同时设计闭环漂移监测，自动检测并抑制单轴剥削。

### 2.2 关键技术细节
- **通用偏好模型（GPM）**：  
  - 将每个响应嵌入到k维潜空间，每维对应一个比较轴（如有用性、安全性）。  
  - 通过斜对称矩阵建模成对偏好，输出k维偏好分数矩阵（k pairwise score matrices）。  
- **GPRL策略更新**：  
  - 对每个提示采样G个响应，用GPM计算每组的k维比较结果。  
  - 计算**每维分组相对优势**（per-dimension group-relative advantages），并在各维度独立归一化，防止任一轴主导。  
  - 使用**上下文相关特征值**（context-dependent eigenvalues）聚合各维优势，得到整体优势信号。  
  - 采用**GRPO风格的剪切代理目标**（clipped surrogate objective）进行策略更新。  
- **闭环漂移监测**（drift monitor）：  
  - 定义维度方差轮廓α(t)和漂移指标D(t)。  
  - 健康训练时α(t)保持初始形状；奖励黑客时坍缩为单维度l⋆。  
  - 当D(t)超过阈值τ时，自动调整各维权重m_l(t)和信任区域参数β，重新平衡维度，抑制黑客。  

## 3. 实验设计

- **数据集/场景**：  
  - **AlpacaEval 2.0**：评测长度控制胜率（LC WR）和胜率（WR）。  
  - **Arena-Hard v2**：胜率（WR）对比gpt-4-0314参考。  
  - **MT-Bench**：评测多轮对话评分。  
  - **WildBench**：校准的WB-Score和WB-Reward（对三个官方参考基线）。  
- **基线方法**：  
  - 偏好优化方法：SimPO、SPPO、GPO（按公开的三轮迭代协议）。  
  - 在线RL方法：GRPO（在匹配的第三轮次报告）。  
  - 基准：gpt-4-turbo作为裁判。  
- **基础策略**：Llama-3-8B-Instruct。  

## 4. 资源与算力

- 论文未明确说明训练使用的具体GPU型号、数量或训练时长。  
- 仅指出“所有基于奖励模型的方法在相似计算量下比较”，但未给出算力细节。  

## 5. 实验数量与充分性

- **实验组数**：  
  - 主实验在4个基准（AlpacaEval 2.0、Arena-Hard、MT-Bench、WildBench）上报告了GPRL与多个基线的对比。  
  - 消融实验：  
    - **漂移控制器消融**（表6）：在8B和2B GPM上对比启用/禁用控制器。  
    - **缩放与类别分析**（图4）：5个训练周期下的扩展性，以及MT-Bench和WildBench的按类别得分。  
- **充分性评价**：  
  - 覆盖了开放式生成的主要评测基准，对比了多种最先进的偏好优化和在线RL方法。  
  - 提供了漂移控制器消融和扩展性分析，验证了方法的鲁棒性。  
  - 但缺少更多超参数搜索、跨模型规模（如7B、13B）的泛化实验，公平性依赖于“相似计算量”的声明，未公开具体细节。  

## 6. 论文的主要结论与发现

- **主结果**：GPRL在AlpacaEval 2.0上达到**56.51%长度控制胜率**（LC WR），显著优于SimPO、SPPO等基线。  
- 在Arena-Hard、MT-Bench、WildBench上，GPRL均取得领先性能，且随着训练周期延长保持稳定，而基线性能下降（奖励黑客）。  
- **漂移监测有效性**：健康训练时D(t)保持近零，黑客时跨越阈值触发校正，而BT奖励模型单调上升无回退信号。  
- **多维度优势**：GPRL在匹配监督信号的类别（如有用性）和结构性类别上领先，在其余类别与最强基线持平。  

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：  
  - 首次将偏好建模为多维结构并直接用于在线RL更新，实现了“结构化奖励”的在线探索。  
  - 闭环漂移监测无需人工介入，自动检测并修正奖励黑客，是对抗标量奖励模型漏洞的有效方案。  
  - 使用独立归一化和上下文特征值聚合，避免维度间不公平竞争。  
- **实验设计亮点**：  
  - 在多个标准化基准（AlpacaEval 2.0、Arena-Hard等）上报导，使用gpt-4-turbo作为裁判，保证评估一致性。  
  - 提供了漂移控制器的消融和扩展性分析，直观展示了健康训练与黑客轨迹的差异。  

## 8. 不足与局限

- **实验覆盖**：  
  - 仅基于Llama-3-8B-Instruct一个基础模型，缺乏在更大/更小模型或不同架构（如Mistral、Gemma）上的泛化验证。  
  - 未在代码/数学等具有可验证奖励的任务上验证（尽管这类任务不是GPRL的设计目标，但可作为多样性的补充）。  
- **偏差风险**：  
  - 评估依赖于gpt-4-turbo作为裁判，可能引入裁判本身的偏好偏差，未使用人工评估或多种模型交叉验证。  
  - 未讨论GPM训练数据的分布及其可能引入的社会偏见。  
- **应用限制**：  
  - GPRL需要训练一个k维GPM模型，相较于标量奖励模型，计算成本和存储开销更高。  
  - 漂移控制器阈值τ为超参数，对性能敏感，且未提供自动挑选τ的方法。  
- **信息透明性**：  
  - 未公开训练算力、超参数搜索范围、GPM的具体架构等，降低了可重复性。  

（完）
