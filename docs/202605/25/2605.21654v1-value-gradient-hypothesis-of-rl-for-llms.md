---
title: Value-Gradient Hypothesis of RL for LLMs
title_zh: LLMs强化学习的值梯度假说
authors: "Arip Asadulaev, Daniil Ognev, Karim Salta, Martin Takac"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21654v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 价值梯度假设解释PPO和GRPO在LLM后训练中的作用
tldr: 该研究从价值梯度视角解析了强化学习在语言模型后训练中的有效性。通过可微展开和加性噪声参数化，证明无评论家方法（如PPO）的actor更新在期望上等价于价值梯度；对于离散Transformer，自注意力的自动微分产生的经验代价状态可近似价值信号，误差由采样间隔和策略熵控制。据此将RL影响分解为价值梯度信号和可达奖励余量，提出沿预训练轨迹RL最有效的判据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21654v1/fig-001.webp\", \"caption\": \"Figure 3: Results of the RL upon various OLMO-2 pretraining checkpoints. The left image shows the consistency of the achieved gains via RL using a different number of GRPO steps. The right image shows the behavior of the different components of our study.\", \"page\": 8, \"index\": 1, \"width\": 818, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.21654v1/fig-002.webp\", \"caption\": \"Figure 5: Z-scores of the gain after RL vs. z-scores of the predicted RL impact. Correlation (left) and curve per checkpoint (right). The std bars on left indicate RL gain variance over the various training steps.\", \"page\": 9, \"index\": 2, \"width\": 826, \"height\": 286}]"
motivation: 探究无评论家RL方法（如PPO、GRPO）在语言模型后训练中有效的根本原因及最大收益条件。
method: 构建价值梯度理论框架，通过可微展开和加性噪声参数化证明actor更新的期望等价性，并分析Transformer自动微分的近似机理。
result: 理论证明actor更新在期望上类似价值梯度，且自注意力自动微分产生的经验代价状态能近似价值信号，误差受采样间隔和策略熵控制。
conclusion: RL的影响可分解为价值梯度信号和可达奖励余量，据此可判断沿预训练轨迹RL最有效的时机。
---

## 摘要
强化学习显著改善了预训练语言模型，但为什么像PPO和GRPO这样的无评论家方法能够有效工作，以及何时能带来最大增益，这些仍未得到充分研究。我们针对LLM后训练中的无评论家强化学习提出了一个值梯度视角。首先，在可微分展开和加性噪声参数化下，我们证明了演员更新在期望上类似于值梯度：反向传播传递共态，其条件期望等于值梯度。其次，对于离散变换器策略，我们证明了通过注意力的自动微分产生的经验共态近似于该值信号，其误差由采样间隔和策略熵控制。这些结果促使我们将强化学习的影响分解为值梯度信号和可达奖励空间，从而得出在预训练轨迹上强化学习何时最有效的准则。

## Abstract
Reinforcement learning substantially improves pretrained language models, but it remains understudied why critic-free methods such as PPO and GRPO work as well as they do, and when they should provide the largest gains. We develop a value-gradient perspective of critic-free RL for LLM post-training. First, under a differentiable rollout and additive-noise parameterization, we show that the actor update is value-gradient-like in expectation: the backward pass propagates costates whose conditional expectation equals the value gradient. Second, for discrete transformer policies, we show that autodifferentiation through attention produces empirical costates that approximate this value signal, with an error controlled by the sampling gap and policy entropy. These results motivate a decomposition of RL impact into value gradient signal and reachable reward headroom, yielding a criterion for when RL should be most effective along a pretraining trajectory.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **动机**：PPO、GRPO等无评论家（critic-free）强化学习方法在LLM后训练中表现出色，但经典RL理论认为无评论家方法在长程信用分配上会失败。论文旨在解释为什么它们有效，以及何时能带来最大收益。
- **核心问题**：无评论家RL在LLM中是否真的“无价值”？actor的反向传播是否隐式携带了价值梯度信号？如何预测RL增益最大的预训练检查点？

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出“值梯度假设”，认为无评论家RL的actor更新在期望上等价于值梯度更新，且Transformer的注意力机制提供了可微分路径，使离散采样下的反向传播近似于精确的BPTT共态信号。
- **关键技术细节**：
  - **可微展开与加性噪声参数化**：在连续松弛下，将策略参数化为 `πθ(at|st)=ν(at - āθ(st))`，利用“得分函数=路径导数”引理，证明GRPO/PPO的actor更新在期望上等于路径梯度，反向传播的共态条件期望等于值梯度 `E[λt | st] = ∂Vπt(s)/∂s`。
  - **离散Transformer的近似共态**：定义经验共态 `λ̂t = ∂/∂ht Σ bAk·logπθ(ok|sk)`，其递归形式与BPTT共态递归同构。通过注意力Jacobian传输信用，而缺失的采样路径Jacobian误差由策略熵控制，证明 `||J_exact - J_attn|| ≤ C·√(Ht/|V|)`。
  - **RL影响分解**：将RL增益分解为可用值梯度信号 `Sm(θ) = Σ(θ) - Λm(θ)` 和可达奖励余量 `Hα(θ)`，预测 `RL Impact ∝ Sm(θ) × Hα(θ)`，并给出检查点选择准则 `N* = argmax Sm(θN)·Hα(θN)`。

### 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法
- **场景与数据集**：使用OLMo-2 1B模型，取预训练步骤50k到1M（间隔50k）共约20个检查点。任务为受控的标签复制任务：给定包含标签{A,B,C,D}的提示，模型需输出正确标签，奖励定义为 `R(θ;q)=pθ(y*|q)`，该奖励在logits上可微。
- **基准与对比方法**：无对比方法，主要是理论预测与实测结果的比较。对每个检查点运行GRPO（组大小为？未明确），使用不同RL预算（10,20,25,30步GRPO更新），测量前后奖励变化 `ΔR(θN)`，并与预测的RL影响分数 `Pgap = (Σ - Λm)·Hα` 比较。还验证了熵界（Proposition 2）与实际代价状态误差的相关性。
- **评估指标**：Spearman相关系数（预测分数与实测RL增益之间），以及标准化结合预测器 `z(Rbefore)+z(Pgap)` 与 `z(Rafter)` 的相关性。

### 4. 资源与算力
- **未明确说明**：论文未提及具体使用的GPU型号、数量或训练时长。仅指出使用OLMo-2 1B模型（检查点来自公开预训练），实验规模较小（单任务、多检查点、短RL步骤）。因此无法评估算力消耗。

### 5. 实验数量与充分性
- **实验数量**：约20个检查点 × 4种RL预算 = 80组主要实验，加上熵界验证、代价状态分量估计等辅助分析。实验数量适中。
- **充分性**：实验覆盖了沿训练轨迹的不同检查点，多种RL步骤数，并报告了平均增益。但作者承认该设置是“受控且部分玩具式的”——奖励完全可微（允许精确代价状态测量），且任务简单（单标签选择）。未在真实复杂推理任务（如数学、代码）上验证，也未与有评论家方法（如PPO+value network）对比。因此实验虽客观，但充分性有限，推广性存疑。

### 6. 论文的主要结论与发现
- **理论结论**：无评论家RL在LLM中并非无价值，其actor反向传播在期望上等价于值梯度信号，且离散Transformer中该信号近似保留，误差受策略熵控制。
- **实验结论**：
  - 熵界公式（Proposition 2）与实测误差一致。
  - 提出的RL影响分数 `Pgap` 与实测RL增益显著相关（Spearman约0.60）。
  - 将当前奖励与预测分数标准化结合后，与最终RL后奖励的相关性更高（Spearman约0.73），表明理论抓住了检查点依赖的RL就绪信号。
  - RL最有效的检查点介于“足够接近值梯度机制”与“仍有奖励提升空间”之间。

### 7. 优点
- **理论创新**：首次系统提出并量化了无评论家RL的值梯度机制，不同于以往仅靠U-statistics（Zhou et al.）或经验解释，提供了可证明的近似框架。
- **可预测性**：推导出RL影响分解，能提前预测哪些检查点从RL获益最大，可用于节约计算资源（选择最佳预训练终止点）。
- **连接离散与连续**：巧妙利用Transformer注意力的可微性，解释了离散采样下信用传递的保留，并给出了熵控制的误差界。
- **实验设计简洁验证**：通过可微奖励精确测量代价状态，直接验证理论预测，避免外部噪声干扰。

### 8. 不足与局限
- **实验局限性**：任务过于简化（单标签选择），奖励可微且固定，无法代表真实LLM RL场景（如文本生成、判别式奖励）。未在复杂推理或真实RLHF任务（如数学、对话）中验证。
- **参数依赖**：RL效果受学习率、批次大小、组大小、RL步数等多因素影响，论文仅对RL步数做了平均，未探究超参数敏感性。
- **理论假设**：可微展开依赖连续松弛（softmax近似），而实际GRPO使用离散采样，虽证明近似误差有界，但实际偏差可能在某些低熵区域仍显著。
- **对比缺失**：未与有评论家方法（如PPO+critic）或替代方法（如直接BPTT）对比，无法量化“值梯度近似”相对于独立批评网络的增益。
- **算力与可重复性**：未公开代码、模型超参数、硬件资源，限制了可重复性。

（完）
