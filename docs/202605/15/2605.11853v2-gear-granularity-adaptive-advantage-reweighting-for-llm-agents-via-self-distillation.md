---
title: "GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation"
title_zh: GEAR：通过自我蒸馏实现的 LLM 智能体粒度自适应优势重加权
authors: "Sijia Li, Yuchen Huang, Zifan Liu, Yanping Li, Jingjing Fu, Li Zhao, Jiang Bian, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11853v2"
tags: ["query:token"]
score: 10.0
evidence: 使用Token和片段级信号的粒度自适应信用分配
tldr: "针对LLM智能体在强化学习中面临的粗粒度奖励和信用分配难题，本文提出GEAR框架。该方法通过自蒸馏机制，利用教师模型引导的散度信号识别轨迹中的关键偏差点，实现粒度自适应的优势重加权。在数学推理和工具使用等长程任务中，GEAR显著提升了模型性能，相比传统GRPO在挑战性任务上最高提升约20%，为长程智能体训练提供了更精准的监督信号。"
selection_source: fresh_fetch
motivation: 传统的强化学习主要依赖结果级奖励，难以在长程轨迹中实现精准的细粒度信用分配以指导策略更新。
method: 提出一种自适应粒度框架，通过对比学生与教师模型的散度信号来确定自适应片段边界，并据此对局部优势进行重加权。
result: "在八项数学推理和工具使用基准测试中，GEAR一致优于GRPO及其他基准，在挑战性任务上性能提升高达20%。"
conclusion: GEAR证明了结合自蒸馏与自适应粒度重加权能有效解决长程任务中的信用分配问题，显著增强了LLM智能体的训练效果。
---

## 摘要
强化学习已成为大语言模型（LLM）智能体广泛使用的后训练方法，其训练通常依赖于仅提供粗粒度监督的结果级奖励。虽然更细粒度的信用分配（credit assignment）有望实现有效的策略更新，但获取可靠的局部信用并将其分配到长程轨迹的正确部分仍是一个公开挑战。在本文中，我们提出了 GEAR（粒度自适应优势重加权），这是一个自适应粒度的信用分配框架，它利用源自自我蒸馏的标记（token）级和片段（segment）级信号来重塑轨迹级的 GRPO 优势。GEAR 将在线策略学生模型与以地面真值（ground-truth）为条件的教师模型进行比较，以获得参考引导的散度信号，从而识别自适应片段边界并调节局部优势权重。这种散度通常在语义偏离开始时激增，而同一自回归延续中的后续标记可能会恢复到低散度。因此，GEAR 将此类激增视为自适应信用区域的锚点：在学生模型与教师模型保持一致的地方，保留标记级分辨率；在偏离的地方，GEAR 将相应的延续部分组合成一个自适应片段，并利用偏离点的散度来调节该片段的优势。在涵盖数学推理和智能体工具使用的 8 个基准测试中，使用 Qwen3 4B 和 8B 模型的实验表明，GEAR 一致优于标准的 GRPO、仅使用自我蒸馏的基线以及标记级或轮次级信用分配方法。在 GRPO 基线准确率较低的基准测试中，提升尤为显著，最高比 GRPO 提升约 20%，这表明所提出的自适应重加权方案在更具挑战性的长程设置中特别有用。

## Abstract
Reinforcement learning has become a widely used post-training approach for LLM agents, where training commonly relies on outcome-level rewards that provide only coarse supervision. While finer-grained credit assignment is promising for effective policy updates, obtaining reliable local credit and assigning it to the right parts of the long-horizon trajectory remains an open challenge. In this paper, we propose Granularity-adaptivE Advantage Reweighting (GEAR), an adaptive-granularity credit assignment framework that reshapes the trajectory-level GRPO advantage using token- and segment-level signals derived from self-distillation. GEAR compares an on-policy student with a ground-truth-conditioned teacher to obtain a reference-guided divergence signal for identifying adaptive segment boundaries and modulating local advantage weights. This divergence often spikes at the onset of a semantic deviation, while later tokens in the same autoregressive continuation may return to low divergence. GEAR therefore treats such spikes as anchors for adaptive credit regions: where the student remains aligned with the teacher, token-level resolution is preserved; where it departs, GEAR groups the corresponding continuation into an adaptive segment and uses the divergence at the departure point to modulate the segment' s advantage. Experiments across eight mathematical reasoning and agentic tool-use benchmarks with Qwen3 4B and 8B models show that GEAR consistently outperforms standard GRPO, self-distillation-only baselines, and token- or turn-level credit-assignment methods. The gains are especially strong on benchmarks with lower GRPO baseline accuracy, reaching up to around 20\% over GRPO, suggesting that the proposed adaptive reweighting scheme is especially useful in more challenging long-horizon settings.

---

## 论文详细总结（自动生成）

以下是对论文《GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 LLM 智能体的强化学习（RL）后训练中，通常依赖于**结果级奖励（Outcome-level Reward）**。这种粗粒度的监督信号在长程任务（如多步数学推理或工具调用）中面临严重的**信用分配（Credit Assignment）问题**：
*   **平铺信用分配：** 轨迹级奖励被均匀分配给所有标记，导致成功的轨迹中可能强化了局部错误的动作，而失败的轨迹中可能惩罚了正确的中间步骤。
*   **固定粒度的局限：** 现有的细粒度方法通常采用固定的标记级或轮次级（Turn-level）划分，无法捕捉到智能体行为中语义连贯的动态变化。
*   **研究动机：** 开发一种无需额外训练奖励模型、无需人工标注步骤奖励，且能根据模型自身行为动态调整信用分配粒度的框架。

### 2. 方法论：核心思想与关键技术
GEAR 框架的核心思想是利用**自蒸馏（Self-Distillation）**产生的信号来重塑轨迹级的优势函数（Advantage）。

*   **核心组件：**
    1.  **任务锚定的参考信号：** 构建一个“教师”策略（将模型置于已知地面真值的上下文中）和一个“学生”策略（在线策略生成）。计算两者之间的**逆向 KL 散度（Reverse KL Divergence）**。散度激增通常意味着学生模型开始偏离正确路径。
    2.  **自适应粒度分割：** 
        *   **触发：** 当逆向 KL 散度超过阈值 $\lambda_{KL}$ 时，识别为语义偏离的起点。
        *   **终止：** 利用**标记熵（Token Entropy）**作为边界信号。当熵值显著增加（代表不确定性或语义转换）时，结束当前片段。
        *   **传播：** 在识别出的偏离片段内，将起点的散度信号传播至整个片段，确保信用分配的连贯性。
    3.  **优势重加权（Advantage Reweighting）：**
        *   根据轨迹优势的符号进行**符号感知调制**：
            *   若轨迹成功（Advantage > 0），降低高散度标记的权重，防止强化“碰巧成功”的错误步骤。
            *   若轨迹失败（Advantage < 0），增加高散度标记的权重，强化对错误偏离点的惩罚。

### 3. 实验设计
*   **模型：** Qwen3-4B-Base 和 Qwen3-8B-Base。
*   **数据集/场景：**
    *   **数学推理（4项）：** MATH, GSM8K, MATH500, AIME 2024/2025（竞赛级难度）。
    *   **智能体工具使用（3项）：** ToolSandbox（状态化环境）, BFCL（函数调用）, ACEBench（多步执行）。
*   **对比基准（Baselines）：**
    *   **GRPO：** 标准的结果级强化学习。
    *   **OPSD / OPSD+RL：** 纯自蒸馏及自蒸馏结合 RL。
    *   **ARPO：** 基于熵触发分支的改进方法。
    *   **MT-GRPO：** 引入人工设计的轮次级奖励的方法。

### 4. 资源与算力
*   **硬件：** 使用了 **13 台 NVIDIA H100 GPU**。
*   **训练配置：** 学习率 $1 \times 10^{-6}$，Batch Size 为 128，每个 Prompt 采样 8 条轨迹（Rollouts），训练 2 个 Epoch。
*   **时长：** 
    *   数学推理任务（4B 模型）：约 **26 小时**。
    *   工具使用任务（4B 模型）：约 **10 小时**。
*   **效率：** 训练耗时与标准 GRPO 基线相当，具有较好的计算效率。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 8 个主流基准测试，跨越了数学和工具调用两个差异巨大的领域。
*   **消融实验：** 针对动态分割机制（如移除熵终止、移除 KL 触发、改用固定工具调用边界等）和超参数（$\lambda_H, \alpha, \lambda_{KL}$）进行了详尽的测试。
*   **泛化性验证：** 进行了跨域通用性评估（Multi-Domain Generalization），证明了 GEAR 在混合数据训练下的稳健性。
*   **评价：** 实验设计全面，对比了多种最先进的细粒度信用分配变体，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能显著提升：** GEAR 在所有基准上均优于 GRPO。在 GRPO 表现较弱的挑战性任务中，提升幅度高达 **20%**。
*   **泛化能力强：** 在 AIME 等竞赛级数学任务上表现尤为突出，表明该方法能有效减少对特定领域模式的过拟合。
*   **训练稳定性：** 训练曲线显示，GEAR 在获得更高奖励的同时，保持了更高的策略熵，有效防止了策略过早坍缩。
*   **自适应性的价值：** 实验证明，结合 KL 散度（识别偏离点）和熵（确定片段长度）的动态分割优于任何单一信号或固定边界。

### 7. 优点
*   **无需额外模型：** 仅利用模型自身的预测动力学（KL 和熵），无需训练专门的进程奖励模型（PRM）。
*   **信号驱动的粒度：** 突破了人为定义的“轮次”或“标记”限制，使信用分配与实际的语义偏离行为对齐。
*   **即插即用：** 作为一种优势重加权方案，可以轻松集成到现有的 GRPO 或 PPO 框架中。

### 8. 不足与局限
*   **依赖地面真值：** 构建教师策略需要访问标准答案（Ground Truth），这限制了其在完全开放式、无标准答案任务中的应用。
*   **超参数敏感性：** 虽然论文给出了推荐值，但针对不同规模或性质的模型，可能仍需微调分割阈值。
*   **计算开销：** 虽然训练时长与 GRPO 相当，但在 Rollout 阶段需要计算教师模型的分布，会增加一定的推理开销。

（完）
