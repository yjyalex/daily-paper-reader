---
title: "Learning from Failures: Correction-Oriented Policy Optimization with Verifiable Rewards"
title_zh: 从失败中学习：具有可验证奖励的面向纠错的策略优化
authors: "Mengjie Ren, Jie Lou, Boxi Cao, Xueru Wen, Hongyu Lin, Xianpei Han, Le Sun, Xing Yu, Yaojie Lu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14539v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过面向纠错的策略优化增强推理能力
tldr: 针对大语言模型在强化学习（RLVR）中面临的奖励稀疏和失败轨迹利用不足的问题，本文提出了一种面向纠错的策略优化方法（CIPO）。该方法无需外部信号，通过将模型自身的失败轨迹转化为纠错监督信号，并与标准RLVR目标联合优化，显著提升了模型在数学推理和代码生成任务中的推理能力及自我纠错能力。
selection_source: fresh_fetch
motivation: 现有的RLVR方法受限于二元奖励稀疏和信用分配弱，导致模型无法有效从失败的尝试中学习。
method: 提出CIPO方法，将模型生成的失败轨迹转化为纠错样本，通过联合优化纠错目标和标准强化学习目标来增强模型性能。
result: "在11个数学和代码基准测试中，CIPO在推理准确率、纠错能力和pass@K指标上均显著优于强基准模型。"
conclusion: CIPO通过有效利用失败轨迹，不仅提升了模型的推理表现，还增强了其内在的自我纠错机制。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为提升大语言模型推理能力的有效范式。然而，RLVR 训练通常受到稀疏二值奖励和弱信用分配的阻碍，导致优化信号模糊，且未能充分利用失败轨迹中蕴含的有用信息。为了应对这一挑战，我们提出了面向纠错的策略优化（CIPO），这是对 RLVR 的一种简单且有效的扩展，它在不依赖任何外部信号的情况下，将同策略（on-policy）失败轨迹转化为面向纠错的监督。通过将源自模型自身失败尝试的纠错样本与标准 RLVR 目标进行联合优化，CIPO 在提高学习效率的同时，显式增强了模型纠正自身错误的能力。在涵盖数学推理和代码生成的 11 个基准测试上的广泛实验表明，CIPO 在推理和纠错性能方面均持续且显著地优于强基准模型。此外，CIPO 带来了更强的 pass@K 增益，表明它提升了模型的内在推理能力，而不仅仅是在现有正确答案上重新分配概率质量。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as an effective paradigm for improving the reasoning capabilities of large language models. However, RLVR training is often hindered by sparse binary rewards and weak credit assignment, resulting in ambiguous optimization signals and underutilization of the useful information embedded in failed trajectories. To address this challenge, we propose Correction-Oriented Policy Optimization (CIPO), a simple and effective extension to RLVR that converts on-policy failed trajectories into correction-oriented supervision, without relying on any external signals. By jointly optimizing correction samples derived from the model's own failed attempts together with the standard RLVR objective, CIPO improves learning effectiveness while explicitly enhancing the model's ability to correct its own errors. Extensive experiments across 11 benchmarks spanning mathematical reasoning and code generation demonstrate that CIPO consistently and significantly outperforms strong baselines in both reasoning and correction performance. Moreover, CIPO yields stronger pass@K gains, indicating that it improves the model's intrinsic reasoning capacity rather than merely redistributing probability mass over existing correct answers.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning from Failures: Correction-Oriented Policy Optimization with Verifiable Rewards》（CIPO）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在具有可验证奖励的强化学习（RLVR，如 DeepSeek 使用的 GRPO）中，训练信号通常是稀疏的二元奖励（对/错）。现有的方法（如“强化正确、抑制错误”范式）将所有失败轨迹视为同等的负面信号，忽略了失败轨迹中可能包含的局部正确逻辑，且无法为模型提供如何改进的明确指导。
*   **研究动机**：现有的改进方案（如过程奖励模型 PRM 或 LLM 评论员）往往需要昂贵的人工标注或额外的辅助模型。本文旨在探索一种**无需外部信号**、仅利用模型自身生成的失败轨迹来提升推理和纠错能力的系统性方法。

### 2. 方法论：核心思想与关键技术
CIPO 的核心思想是将“失败轨迹”从单纯的惩罚对象转变为“纠错监督信号”。
*   **双流优化架构**：
    *   **基础流（Base Stream）**：标准的 on-policy RL 过程，对原始问题生成响应并根据结果进行奖励/惩罚。
    *   **纠错流（Correction Stream）**：将模型之前生成的错误轨迹作为上下文，提示模型进行“自我修正”。如果修正成功，则提供正向引导信号。
*   **关键技术细节**：
    *   **自适应回放机制（Adaptive Replay）**：动态调整训练中成功与失败轨迹的混合比例。如果模型在已知正确样本上的表现下降，则增加成功样本比例以防止能力退化。
    *   **风险规避奖励塑造（Risk-Averse Reward Shaping）**：引入非对称惩罚。如果模型将原本正确的参考轨迹改成了错误的答案，会受到额外的重罚（$\lambda_{risk}$），以确保学习的稳定性。
    *   **难度感知轨迹偏好（Difficulty-Aware Preference）**：优先选择模型处于“近侧发育区”（即通过率在中间范围）的题目进行回放，避免在太简单或太难的题目上浪费算力。
    *   **联合目标函数**：将标准 GRPO 的优势函数与纠错任务的优势函数加权合并，进行统一的策略梯度更新。

### 3. 实验设计
*   **实验场景**：数学推理（使用 Qwen3-4B 模型）和代码生成（使用 Seed-Coder-8B 模型）。
*   **数据集/Benchmark**：
    *   **数学**：AIME 2024/25, AMC23, MATH500, Minerva, OlympiadBench 等。
    *   **代码**：LiveCodeBench v6, LeetCode (DebugBench)。
    *   **纠错与批判**：CriticBench, DebugBench。
*   **对比方法**：
    *   **GRPO**：在不同计算预算（Batch Size 128/256）下的表现。
    *   **PRIME**：另一种基于过程强化学习的强基准。
    *   **Offline 变体**：仅使用初始失败轨迹而非在线更新轨迹的对比。

### 4. 资源与算力
*   **框架**：使用 VERL 强化学习框架。
*   **训练参数**：每个 Batch 包含 128 个问题，每个问题生成 8 个响应（Rollouts）。学习率 $1 \times 10^{-6}$，训练 500 步。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体数量，但从其使用的模型规模（4B/8B）和训练步数来看，属于中等规模的学术/工业研究投入。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个代表性基准测试上进行了评估，涵盖了从基础数学到竞赛级代码任务。
*   **消融实验**：非常充分。作者分别验证了“在线回放”、“自适应控制”、“风险规避奖励”和“难度感知偏好”四个核心组件的贡献，证明了每个模块对最终性能的必要性。
*   **客观性**：通过 Pass@K 指标（如 Pass@32）验证了模型能力的提升是内在推理能力的增强，而非简单的概率分布偏移。同时，在同等计算预算（FLOPs 匹配）下对比了 GRPO，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：CIPO 在数学任务上平均超越 GRPO 约 4.55%，在代码任务上也表现更优。
*   **纠错能力显著增强**：在 DebugBench 上，8B 规模的模型通过 CIPO 训练后，纠错能力达到了与 Claude-4-Sonnet 相当的水平。
*   **泛化性强**：在数学数据上训练的模型，在符号推理和算法逻辑等“域外”任务的纠错上也表现出明显的性能提升。
*   **内在能力提升**：Pass@K 的显著增长表明 CIPO 扩展了模型的解题搜索空间，而不仅仅是让模型更“保守”地输出已知答案。

### 7. 优点
*   **自给自足**：完全不依赖外部模型（如 GPT-4 标注）或人工反馈，实现了纯粹的“自我进化”。
*   **任务无关**：方法论具有通用性，可轻易迁移至任何具有可验证奖励（如编译器输出、数学答案校验）的领域。
*   **双重收益**：在提升推理准确率的同时，额外赋予了模型极强的“自我调试”和“纠错”实用技能。

### 8. 不足与局限
*   **依赖可验证性**：CIPO 强依赖于能够自动验证结果的任务（Verifiable Rewards），对于主观性强、无标准答案的任务（如创意写作、开放式对话）难以直接应用。
*   **计算开销**：虽然在同等 Batch 下对比占优，但纠错流的引入增加了训练时的采样次数（Rollouts），相比纯 GRPO 增加了推理阶段的计算负担。
*   **模型规模限制**：实验主要集中在 4B 和 8B 模型上，尚未在大规模模型（如 70B+）上验证其扩展性（Scaling Law）。

（完）
