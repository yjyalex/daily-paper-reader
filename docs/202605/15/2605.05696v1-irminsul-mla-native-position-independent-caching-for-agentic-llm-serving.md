---
title: "Irminsul: MLA-Native Position-Independent Caching for Agentic LLM Serving"
title_zh: Irminsul：面向智能体 LLM 推理的 MLA 原生位置无关缓存
authors: "Bole Ma, Jan Eitzinger, Harald Köstler"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05696v1"
tags: ["query:llm-fo"]
score: 7.0
evidence: 智能体大模型服务的延迟与吞吐量优化
tldr: "针对代理型LLM任务中因Token位置偏移导致传统前缀缓存失效的问题，本文提出Irminsul。它利用MLA（多头潜在注意力）架构将KV分解为位置无关潜变量和可旋转部分的特性，通过内容哈希寻址和δ旋转规则实现高效的位置无关缓存。实验表明，该方法在DeepSeek-V2等模型上可恢复高达83%的Token，并节省63%的预填充能耗，显著提升了推理效率。"
selection_source: fresh_fetch
motivation: 代理型LLM工作负载中Token位置的频繁变动会导致传统前缀缓存失效，而现有针对GQA的改进方案计算成本过高。
method: 利用MLA架构的数学特性，结合内容哈希分块技术和δ旋转规则，实现了原生支持位置无关的KV缓存机制。
result: "在DeepSeek-V2等多个MLA模型上验证，成功恢复了高达83%的提示词Token，并实现了63%的预填充能耗节省。"
conclusion: 内容寻址缓存应被视为LLM推理服务栈中的一等公民原语，而非仅仅是对前缀匹配机制的修补。
---

## 摘要
智能体 LLM 工作负载在每一轮中都会将位完全相同的 token 置于偏移后的位置，导致前缀缓存在第一个差异字节处失效。运营商报告称，缓存命中率下降导致了从轻微减速到未更改内容上 10-16 秒的严重首字延迟（TTFT）激增。先前的位置无关缓存系统在完整的 $d_K$ 维键（key）上修正 RoPE，这是由 GQA 带来的架构成本，而非缓存本身。多头潜在注意力（MLA）已在 DeepSeek-V2/V3/R1、Kimi-K2/Moonlight、GLM-5 和 Mistral Large 3 中大规模部署，它将每一行 KV 分解为位置无关的 $c_{KV}$ 和一个可闭式修正的 64 维 $k_r$；这种结构使得内容寻址缓存成为一种自然的适配，而非针对 GQA 的权宜之计。我们提出了 Irminsul，它通过对 CDC 分块段进行内容哈希键控以及针对 $k_r$ 的 $\delta$ 旋转规则，扩展了 SGLang 的基数缓存（radix cache）。我们评估了三个原生的 MLA-MoE 部署——DeepSeek-V2-Lite (16B/2.4B)、Kimi Moonlight-16B-A3B 和 JoyAI-Flash (48B/3B)——在所有三个模型上均保持了输出一致性，并在两个端点上测量了恢复率；在智能体流量中，Irminsul 在精确前缀之外额外恢复了高达约 83% 的 prompt token，同时每次缓存命中可节省 63% 的预填充（prefill）能耗。我们认为，内容寻址缓存应当作为推理栈中的一等原语，而非对前缀匹配的补丁。

## Abstract
Agentic LLM workloads put bit-identical tokens at shifted positions every turn, voiding prefix caches at the first byte of divergence. Operators report cache-hit regressions ranging from moderate slowdowns to severe TTFT spikes of 10-16s on unchanged content. Prior position-independent caching systems correct RoPE on the full $d_K$-dimensional key, an architectural cost imposed by GQA, not by caching itself. Multi-Head Latent Attention, deployed at scale in DeepSeek-V2/V3/R1, Kimi-K2/Moonlight, GLM-5, and Mistral Large 3, factors each KV row into a position-free $c_{KV}$ and a 64-dim $k_r$ correctable in closed form; this structure motivates content-addressed caching as a natural fit rather than a GQA workaround. We present Irminsul, which extends SGLang's radix cache with content-hash keying over CDC-chunked segments and a $δ$-rotation rule for $k_r$. We evaluate three native MLA-MoE deployments - DeepSeek-V2-Lite (16B/2.4B), Kimi Moonlight-16B-A3B, and JoyAI-Flash (48B/3B) - with output-consistency on all three and recovery measured on the two endpoints; Irminsul recovers up to ~83% of prompt tokens above exact-prefix on agentic traffic while delivering 63% prefill energy savings per cache hit. We argue that content-addressed caching belongs in the serving stack as a first-class primitive, not a retrofit over prefix matching.