---
title: Boosting Reinforcement Learning with Verifiable Rewards via Randomly Selected Few-Shot Guidance
title_zh: 通过随机选择的少样本引导增强具有可验证奖励的强化学习
authors: "Kai Yan, Alexander G. Schwing, Yu-Xiong Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15012v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 改进了数学和代码思维链推理中具有可验证奖励的强化学习
tldr: 针对强化学习在处理复杂任务时样本效率低且依赖大量标注数据的问题，本文提出FEST算法。该方法仅需128条随机选择的少样本演示数据，通过结合监督信号、同策略信号及衰减权重机制来引导强化学习过程。实验证明，FEST在多个基准测试中仅需极少量数据即可达到甚至超过使用全量SFT数据的模型性能，显著提升了训练效率。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在处理难以生成正确解的复杂问题时效率较低，且现有的演示引导方案通常需要昂贵的大规模SFT数据集。
method: 提出FEST算法，利用极少量随机选择的演示数据，并引入监督信号、同策略信号以及针对少样本集的衰减权重机制以防止过拟合。
result: 在多个基准测试中，FEST仅用128条数据便超越了对比基准，甚至在性能上匹配了使用完整SFT数据集的训练效果。
conclusion: 研究表明，通过合理的策略设计，极少量的演示数据即可有效引导强化学习，大幅降低对大规模监督数据的依赖。
---

## 摘要
具有可验证奖励的强化学习（RLVR）在开发具有思维链（CoT）展开的大语言模型（LLM）方面取得了巨大成功，适用于数学和编程等多种任务。然而，在难以生成正确展开的困难问题上，RLVR 面临样本效率低下的挑战。先前的研究提议通过示例引导的 RLVR 来解决这一问题，即在强化学习失败时进行有监督微调（SFT）；然而，SFT 通常需要大量数据，而获取这些数据的成本可能很高。在本文中，我们提出了 FEST，一种少样本（FEw-ShoT）示例引导的 RLVR 算法。仅使用从 SFT 数据集中随机选择的 128 个示例，它就取得了令人瞩目的结果。我们发现三个组件对成功至关重要：监督信号、同策略（on-policy）信号，以及在少样本 SFT 数据集上采用衰减权重以防止多轮训练导致的过拟合。在多个基准测试中，FEST 在 SFT 数据量减少几个数量级的情况下优于基准模型，甚至达到了使用全量数据集时的性能。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has achieved great success in developing Large Language Models (LLMs) with chain-of-thought rollouts for many tasks such as math and coding. Nevertheless, RLVR struggles with sample efficiency on difficult problems where correct rollouts are hard to generate. Prior works propose to address this issue via demonstration-guided RLVR, i.e., to conduct Supervised FineTuning (SFT) when RL fails; however, SFT often requires a lot of data, which can be expensive to acquire. In this paper, we propose FEST, a FEw-ShoT demonstration-guided RLVR algorithm. It attains compelling results with only 128 demonstrations randomly selected from an SFT dataset. We find that three components are vital for the success: supervised signal, on-policy signal, and decaying weights on the few-shot SFT dataset to prevent overfitting from multiple-epoch training. On several benchmarks, FEST outperforms baselines with magnitudes less SFT data, even matching their performance with full dataset.

---

## 论文详细总结（自动生成）

以下是对论文《Boosting Reinforcement Learning with Verifiable Rewards via Randomly Selected Few-Shot Guidance》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：具有可验证奖励的强化学习（RLVR，如 DeepSeek-R1 模式）在数学和编程等任务中表现卓越，但面临**样本效率低**的挑战，尤其是在模型难以自行探索出正确解的复杂问题上。
*   **动机**：现有的“演示引导强化学习”方法（如 HPT、ReLIFT）虽然能通过引入有监督微调（SFT）数据来辅助强化学习，但通常需要数千甚至数万条高质量专家演示，获取成本极高。
*   **核心问题**：能否仅利用极少量（如 128 条）随机选择的专家演示数据，就能显著提升 RLVR 的性能，并降低对大规模 SFT 数据的依赖？

### 2. 方法论
*   **核心思想**：提出 **FEST** 算法，将极少量的专家演示数据（$D_E$）与大规模仅含答案的数据（$D_I$）结合，通过特定的损失函数设计来引导强化学习。
*   **关键技术组件**：
    1.  **监督信号**：通过 SFT 损失提供专家知识引导。
    2.  **同策略（On-policy）信号**：利用模型生成的负样本与专家正样本进行对比学习，缓解曝光偏差并增强鲁棒性。
    3.  **衰减权重**：在训练后期降低少样本数据的权重，防止模型在极小数据集上反复迭代导致过拟合。
*   **算法实现**：
    *   **FEST-DPO**：在 $D_I$ 上使用 GRPO 损失，在 $D_E$ 上使用半在线 DPO 损失。引入自适应 $\beta$ 策略，根据题目是否被模型解决来动态调整学习强度。
    *   **FEST-GRPO**：为解决 DPO（序列级）与 GRPO（标记级）梯度量级不匹配的问题，将 DPO 损失分解为“加权 SFT”和“负优势值的 GRPO 损失”，实现梯度尺度的统一。

### 3. 实验设计
*   **数据集**：主要使用 OpenR1-Math-46K（随机抽取 128 条作为少样本集），并使用 LIMOv2 进行鲁棒性验证。
*   **测试基准（Benchmarks）**：AIME 24/25、AMC 23、MATH-500、OlympiadBench、Minerva。
*   **对比方法**：Vanilla RL (GRPO)、SRFT、LUFFY、CHORD、HPT、ReLIFT、MIFO 等。
*   **评估指标**：Avg@8（8 次采样的平均准确率）和 Pass@8（探索潜力上限）。

### 4. 资源与算力
*   **硬件环境**：使用 2 台 NVIDIA GH200 (96GB) GPU。
*   **训练细节**：针对 Qwen2.5-Math-1.5B 模型进行 600 步微调。
*   **时长**：标准训练任务约需 4-5 天，单次基准测试评估约需 30 分钟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 8 种以上的先进基准方法。
    *   进行了 Shot 数量（64, 128, 256, 512）的扩展性分析。
    *   测试了 3 个不同的随机数据切片以排除数据选择偏差。
    *   包含 OOD（MMLU-Pro）泛化性实验及详细的消融实验。
*   **充分性**：实验设计严谨，通过多切片重复实验和跨数据集验证，证明了结果并非偶然，对比基准均在优化后的超参数下运行，保证了公平性。

### 6. 主要结论与发现
*   **极高效的数据利用**：FEST 仅需 128 条随机数据，性能即可超越所有少样本基准，甚至能匹配使用 46,000 条全量 SFT 数据训练的模型效果。
*   **组件协同效应**：消融实验证明，监督信号、同策略信号和衰减权重三者缺一不可；单纯增加 SFT 权重而不使用衰减策略会导致模型在后期崩溃。
*   **梯度对齐的重要性**：FEST-GRPO 通过解决梯度失配问题，在极小样本场景下比 FEST-DPO 表现出更好的稳定性。

### 7. 优点
*   **成本极低**：显著降低了开发高性能推理模型对昂贵标注数据的需求。
*   **无需精选数据**：与需要精心挑选数据的研究不同，FEST 对随机抽取的样本同样有效，具有极强的实用性。
*   **理论完备**：扩展了统一后训练框架（HPT），建立了 DPO 与强化学习梯度之间的形式化联系。

### 8. 不足与局限
*   **模型规模**：实验主要集中在 1.5B 参数模型，尚未在大规模参数模型（如 70B 或更高）上验证其扩展性。
*   **任务覆盖**：目前仅限于数学推理任务，在代码生成、创意写作或通用对话领域的有效性有待进一步验证。
*   **超参依赖**：虽然引入了自适应策略，但 $\beta$ 等超参数在处理超长思维链（CoT）时仍需细致调节。

（完）
