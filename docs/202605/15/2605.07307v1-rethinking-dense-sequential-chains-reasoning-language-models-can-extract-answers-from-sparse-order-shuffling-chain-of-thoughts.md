---
title: "Rethinking Dense Sequential Chains: Reasoning Language Models Can Extract Answers from Sparse, Order-Shuffling Chain-of-Thoughts"
title_zh: 重新思考稠密顺序链：推理语言模型可以从稀疏、乱序的思维链中提取答案
authors: "Yi-Chang Chen, Feng-Ting Liao, Da-shan Shiu, Hung-yi Lee"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07307v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 逻辑推理能力与思维链追踪分析
tldr: 本研究挑战了推理语言模型中思维链（CoT）必须是稠密且有序的传统假设。通过对三种模型在三个基准测试上的推理链进行删除、掩码、打乱和噪声注入等干预实验，发现答案提取过程对行级顺序不敏感，且主要依赖于稀疏的关键数字而非自然语言描述。研究表明，即使在极端简化和乱序的情况下，模型仍能保持高准确率，这为开发更高效、可并行的推理生成方式提供了新路径。
selection_source: fresh_fetch
motivation: 探究推理语言模型在提取答案时，是否真的依赖于思维链中完整的顺序信息和稠密的文本内容。
method: 通过对模型生成的推理链进行行级、词级、标记级打乱以及掩码数字或文本等干预手段，系统评估其对答案提取的影响。
result: 实验发现行级打乱对准确率影响极小，掩码非数字文本反而提升了性能，且模型对注入的干扰噪声具有极强的鲁棒性。
conclusion: 答案提取基于一种稀疏、对顺序不敏感且结构稳健的信息基质，为实现更高效的并行化推理生成开辟了道路。
---

## 摘要
现代推理语言模型生成稠密的顺序思维链轨迹，隐含地假设每个 token 都有贡献，且必须按顺序处理步骤。我们通过一套系统的干预流程——包括移除、掩码、打乱和噪声注入——对三种模型在三个基准测试上生成的推理链进行了挑战。我们的发现在三个维度上是反直觉的。顺序：推理链的顺序对答案提取重要吗？不——行级打乱使准确率下降不到 0.5 个百分点；词级打乱保留了 62%-89% 的准确率；只有 token 级打乱会导致准确率崩塌至接近零。仅预训练和经过指令微调的变体表现出几乎相同的容忍度（行打乱下分别为 78.67% 和 78.00%），表明顺序无关性源于预训练，而非针对推理的特定微调。稠密性：推理链中的所有信息对答案提取都重要吗？不——掩码数字会导致准确率崩塌至 0%，而掩码字母文本反而使准确率提高了 4.7 个百分点。鲁棒性：一个既乱序又不稠密的推理链是否仍然鲁棒？是的——最激进的简化表示（移除所有自然语言，行被任意打乱）仍能达到 83% 的准确率，且以 3 倍于正确答案的频率注入错误答案后，准确率保持不变（83.3% -> 83.3%），这证伪了基于频率的提取解释。这些结果证明，答案提取运作于一个稀疏、对顺序不敏感且结构鲁棒的信息基质之上，为并行化和 token 高效的推理生成开辟了道路。

## Abstract
Modern reasoning language models generate dense, sequential chain-of-thought traces implicitly assuming that every token contributes and that steps must be consumed in order. We challenge both assumptions through a systematic intervention pipeline--removal, masking, shuffling, and noise injection--applied to model-generated reasoning chains across three models and three benchmarks. Our findings are counterintuitive on three dimensions. Order: Does the sequential order of a reasoning chain matter for answer extraction? No--line-level shuffling reduces accuracy by less than 0.5 pp; word-level shuffling retains 62%-89% accuracy; only token-level shuffling collapses to near zero. Pretrained-only and instruction-tuned variants exhibit near-identical tolerance (78.67% vs. 78.00% under line shuffling), indicating order-independence originates from pretraining rather than reasoning-specific fine-tuning. Dense: Is all the information in a reasoning chain important for answer extraction? No--masking numeric digits collapses accuracy to exactly 0%, while masking alphabetic prose improves accuracy by 4.7 pp. Robustness: Is a reasoning chain that is both order-shuffling and non-dense still robust? Yes--the most aggressively reduced representation (all natural language removed, lines arbitrarily shuffled) still achieves 83% accuracy, and injecting false answers at 3x true-answer frequency leaves accuracy unchanged (83.3%->83.3%), falsifying a frequency-based extraction account. These results establish that answer extraction operates on a sparse, order-insensitive, and structurally robust informational substrate, opening paths toward parallelized and token-efficient reasoning generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Dense Sequential Chains: Reasoning Language Models Can Extract Answers from Sparse, Order-Shuffling Chain-of-Thoughts》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：当前的推理语言模型（如 OpenAI o1, DeepSeek-R1）依赖于生成长篇、稠密的思维链（CoT）来解决复杂问题。
*   **核心假设挑战**：传统观点认为 CoT 必须是**稠密的**（每个 token 都有意义）且**有序的**（推理步骤必须按逻辑顺序排列）。
*   **研究动机**：如果模型在提取答案时并不依赖完整的顺序或冗余的文字，那么推理生成阶段就可以实现并行化（打破自回归依赖）和大幅度压缩（减少 token 消耗），从而显著降低推理成本。

### 2. 论文提出的方法论
*   **核心思想**：通过对模型生成的推理链进行系统性的**干预（Intervention）**，观察答案提取准确率的变化，从而识别出真正承载信息的“基质”。
*   **关键技术细节（干预处理器）**：
    *   **打乱（Shuffling）**：包括行级（Sline）、词级（Sword）、Token 级（Stok）以及行内词级（Silw）的随机排列。
    *   **掩码（Masking）**：使用特殊符号替换字母（Mα）、数字（Mν）或正确答案（Mans）。
    *   **移除（Removal）**：彻底删除字母（Rα）、问题文本（Rq）或整个推理链（Rr）。
    *   **噪声注入（Noise Injection）**：以 $k$ 倍于正确答案的频率在链中插入错误的答案陈述（Nk）。
*   **评估模式**：对比了“自由生成（Gen）”和“检索模式（Ret）”。研究发现 Gen 模式在推理链缺失时会自动进行“二次推理”，从而掩盖提取能力的真实情况，因此研究主要采用 Ret 模式（使用固定前缀强制模型直接输出答案）。

### 3. 实验设计
*   **数据集/场景**：
    1.  **AIME 2025**：高难度数学竞赛题，考察数值计算。
    2.  **CodeElo**：竞技编程基准，考察算法逻辑。
    3.  **GPQA-Diamond**：研究生水平的科学问答，考察多步科学推理。
*   **模型选择**：涵盖了不同规模和家族的模型：GPT-OSS-120B、DeepSeek-V3.1-671B、OLMo-3.1-32B（及其预训练 Base 版本）。
*   **对比基准**：以原始、未经过任何变换的推理链作为 Baseline。

### 4. 资源与算力
*   **算力消耗**：论文明确指出**不涉及任何模型训练**，所有实验均为**模型推理（Inference）**。
*   **硬件/环境**：推理通过 OpenRouter API 调用托管模型完成。文中未提及具体的 GPU 型号、数量或总推理时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 AIME 2025，每道题采样 10 个独立推理轨迹（共 300 个实例）以减少方差。
    *   CodeElo 包含 408 个样本，GPQA 包含 198 个样本。
*   **充分性评价**：实验设计非常全面，不仅涵盖了单一维度的干预（如仅打乱行），还进行了多维度组合干预（如“移除字母+行打乱”）。通过对比 Base 模型和指令微调模型，排除了微调带来的特定偏差，实验结果具有较强的客观性和说服力。

### 6. 论文的主要结论与发现
1.  **行级顺序无关性**：随机打乱推理链的行顺序，准确率下降不到 0.5 个百分点。
2.  **最小语义单位**：词级打乱仍能保留大部分准确率，但 Token 级打乱会导致提取失败。这表明“词”是提取信息的最小有效单位。
3.  **信息稀疏性**：在数学推理中，自然语言描述（字母）是冗余的，掩码字母甚至能提升 4.7% 的准确率；而数字信息是不可或缺的，掩码数字会导致准确率降至 0%。
4.  **结构鲁棒性**：即使将推理链简化为“纯数字+乱序行”，仍能保持 83% 的准确率。
5.  **抗干扰能力**：注入 3 倍频率的错误答案无法误导模型，证伪了模型仅靠“频率统计”提取答案的假设。
6.  **能力来源**：这种对乱序的容忍度在预训练阶段就已经存在，而非思维链微调的结果。

### 7. 优点
*   **视角新颖**：挑战了 AI 社区对 CoT “顺序性”和“稠密性”的直觉认知。
*   **实验严谨**：通过区分 Gen 和 Ret 模式，成功隔离了“提取能力”与“原生推理能力”。
*   **启发性强**：为未来开发非自回归（并行化）推理模型提供了坚实的实证基础。

### 8. 不足与局限
*   **领域局限性**：关于“字母冗余”的结论主要基于数学领域（AIME），在需要严密语义逻辑的法律或文学推理中可能不成立。
*   **生成阶段的缺失**：研究仅证明了“提取阶段”可以处理乱序和稀疏信息，但并未证明“生成阶段”是否也能在乱序状态下产生有效的推理。
*   **机制解释尚浅**：虽然提出了“符号约束满足”的猜想，但尚未从 Transformer 内部注意力机制的角度给出深层的数学解释。

（完）
