---
title: "LEMON: Learning Executable Multi-Agent Orchestration via Counterfactual Reinforcement Learning"
title_zh: LEMON：通过反事实强化学习学习可执行的多智能体编排
authors: "Xudong Chen, Yixin Liu, Hua Wei, Kaize Ding"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14483v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 通过强化学习进行多智能体编排
tldr: LEMON是一个基于LLM的多智能体编排框架，旨在解决现有方法在角色设计、能力分配和依赖构建方面优化不全的问题。它通过生成可执行的编排规范，并结合GRPO与局部反事实强化学习信号进行训练，实现了精准的信用分配。在MMLU、HumanEval等六个基准测试中，LEMON显著提升了系统的执行效率和质量，达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的多智能体编排方法通常只能进行局部或顺序优化，且缺乏针对具体编排决策的有效反馈和信用分配机制。
method: 提出LEMON框架，利用带有局部反事实信号的GRPO算法训练编排器，生成整合了角色、职责、能力和依赖结构的统一可执行规范。
result: 在MMLU、GSM8K、HumanEval等六个推理与编程基准测试中，LEMON均取得了多智能体编排领域的当前最佳性能。
conclusion: 通过引入反事实强化学习进行精细化信用分配，LEMON证明了自动化且整体化的编排设计能显著增强多智能体系统的任务处理能力。
---

## 摘要
大语言模型（LLMs）已成为多智能体系统的强大基础，但其有效性在很大程度上取决于编排设计。在不同的任务中，角色设计、能力分配和依赖构建共同影响着解决方案的质量和执行效率。现有方法实现了该设计过程的部分自动化，但它们通常是局部或顺序地优化这些决策，并依赖于执行层面的反馈，而这种反馈对局部编排决策提供的信用分配有限。我们提出了 LEMON（Learning Executable Multi-agent Orchestration via Counterfactual Reinforcement Learning），这是一种基于 LLM 的编排器，可生成可执行的编排规范。该规范将特定任务的角色、定制职责、能力水平和依赖结构整合到一个单一的可部署系统中。为了训练该编排器，我们使用局部反事实信号增强了编排层面的 GRPO 目标，该信号通过编辑角色、能力或依赖字段，并将产生的奖励对比仅应用于被编辑的片段。在包括 MMLU、GSM8K、AQuA、MultiArith、SVAMP 和 HumanEval 在内的六个推理和编码基准测试上的实验表明，LEMON 在评估的多智能体编排方法中达到了最先进的性能。我们的代码可在 https://anonymous.4open.science/r/LEMON-B23C 获取。

## Abstract
Large language models (LLMs) have become a strong foundation for multi-agent systems, but their effectiveness depends heavily on orchestration design. Across different tasks, role design, capacity assignment, and dependency construction jointly affect both solution quality and execution efficiency. Existing approaches automate parts of this design process, yet they often optimize these decisions partially or sequentially, and rely on execution-level feedback that provides limited credit assignment for local orchestration decisions. We propose LEMON (\textbf{L}earning \textbf{E}xecutable \textbf{M}ulti-agent \textbf{O}rchestratio\textbf{N} via Counterfactual Reinforcement Learning), an LLM-based orchestrator that generates an executable orchestration specification. The specification integrates task-specific roles, customized duties, capacity levels, and dependency structure into a single deployable system. To train the orchestrator, we augment the orchestration-level GRPO objective with a localized counterfactual signal that edits role, capacity, or dependency fields and applies the resulting reward contrast only to the edited spans. Experiments on six reasoning and coding benchmarks, including MMLU, GSM8K, AQuA, MultiArith, SVAMP, and HumanEval, show that LEMON achieves state-of-the-art performance among the evaluated multi-agent orchestration methods. Our code is available at https://anonymous.4open.science/r/LEMON-B23C.

---

## 论文详细总结（自动生成）

这是一份关于论文《LEMON: Learning Executable Multi-Agent Orchestration via Counterfactual Reinforcement Learning》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
多智能体系统（MAS）的效能高度依赖于**编排设计（Orchestration Design）**，即如何定义智能体的角色、分配计算能力（模型大小）以及构建它们之间的依赖关系。
*   **研究动机**：现有的自动化编排方法存在两个主要局限：
    1.  **设计脱节（Decomposed Design）**：通常将角色设计、拓扑结构和模型路由作为独立或顺序的步骤处理，忽略了它们在执行过程中的高度耦合。
    2.  **信用分配稀疏（Sparse Credit Assignment）**：训练信号通常基于最终执行结果（成功或失败），难以反馈到具体的局部决策（例如：是某个依赖关系多余，还是某个智能体的能力分配不足？）。
*   **核心目标**：提出一个能够生成“统一可执行规范”的编排器，并通过精细化的强化学习手段优化局部决策。

### 2. 方法论
LEMON 框架将多智能体编排建模为**组合式可执行规范生成**任务。
*   **核心思想**：编排器（LLM）直接输出一个结构化的 YAML 规范，包含每个智能体的身份、定制职责（Duty）、能力等级（Small/Medium/Large 模型）及依赖引用。
*   **关键技术细节**：
    1.  **监督微调（SFT）**：首先利用教师模型生成的规范进行热启动，确保模型能生成符合语法的 YAML。
    2.  **全局强化学习（GRPO）**：使用群体相对策略优化（GRPO），奖励函数综合考虑任务准确率、Token 效率和图复杂度。
    3.  **局部反事实信用分配（Localized Counterfactual Credit Assignment）**：这是本文的核心创新。在训练中对生成的规范进行“反事实编辑”（Mutation）：
        *   **依赖删除**：测试某条信息流是否必要。
        *   **角色回滚**：测试定制职责是否优于通用角色。
        *   **能力降级**：测试是否可以用更小的模型完成该步骤。
    4.  **局部更新**：比较原始规范与编辑后规范的奖励差异，将该差异作为信号，**仅更新被编辑的 Token 片段**，从而实现精准的信用分配。

### 3. 实验设计
*   **数据集/基准测试**：涵盖 6 个推理与编程基准：
    *   数学推理：GSM8K, AQuA, MultiArith, SVAMP。
    *   综合知识：MMLU（验证集）。
    *   代码生成：HumanEval。
*   **对比方法**：
    *   单智能体：Vanilla, CoT, SC-CoT。
    *   固定结构 MAS：Chain, Tree, Complete, Random。
    *   拓扑设计方法：AgentPrune, G-Designer, ARG-Designer, OFA-MAS。
    *   自适应工作流：AFlow。

### 4. 资源与算力
*   **硬件环境**：使用了 4 张 **NVIDIA L40S (48GB)** GPU。
*   **训练时长**：主强化学习训练阶段耗时约 **24 小时 50 分钟**。
*   **模型配置**：
    *   编排器：Qwen2.5-7B-Instruct。
    *   执行工人：Qwen2.5 系列（7B 为 Small，14B 为 Medium，32B 为 Large）。
*   **优化技术**：使用了节点级执行缓存（Node-level Caching）来减少反事实评估时的重复计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的基准上进行了测试，实验覆盖面较广。
*   **消融实验**：非常充分。分别验证了角色生成、能力分配、结构生成以及三种反事实突变（依赖、角色、能力）的必要性。
*   **客观性**：通过 Pareto 前沿图展示了准确率与 Token 成本的权衡，证明了 LEMON 不仅性能强，且效率高。实验对比了多种最新的 SOTA 方法，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：LEMON 在 6 个基准上的平均得分（90.72%）显著优于所有基线方法。
*   **效率优化**：通过能力分配决策，LEMON 能够学会在简单步骤使用小模型，在关键步骤使用大模型，从而在 Pareto 前沿上表现优异。
*   **反事实学习的有效性**：实验证明，局部反事实信号比单纯的全局奖励能更快、更稳健地提升编排质量，尤其是在降低 Token 消耗方面。

### 7. 优点
*   **整体性设计**：将角色、能力和拓扑整合在单一生成空间内，解决了决策耦合问题。
*   **精准反馈**：局部反事实信用分配解决了强化学习在复杂序列生成中的“功劳归属”难题。
*   **可执行性强**：生成的 YAML 直接对应执行图，易于部署和验证。
*   **训练效率优化**：引入节点缓存机制，缓解了反事实评估带来的额外算力负担。

### 8. 不足与局限
*   **训练成本**：尽管有缓存，但强化学习过程（尤其是涉及多次执行的反事实评估）仍然比传统的 SFT 或推理侧优化更昂贵。
*   **任务覆盖**：实验主要集中在文本推理和短代码生成，尚未在长程工具调用（Long-horizon tool use）或多模态交互环境中验证。
*   **评估偏差**：MMLU 仅使用了验证集而非全量测试集，HumanEval 的训练/测试划分也可能存在特定分布的影响。

（完）
