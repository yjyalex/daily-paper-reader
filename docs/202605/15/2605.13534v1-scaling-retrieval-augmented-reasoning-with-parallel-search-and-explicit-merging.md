---
title: Scaling Retrieval-Augmented Reasoning with Parallel Search and Explicit Merging
title_zh: 通过并行搜索与显式合并扩展检索增强推理
authors: "Jiabei Liu, Wenyu Mao, Junfei Tan, Chunxu Shen, Lingling Yi, Jiancan Wu, Xiang Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13534v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于强化学习的多步推理并行搜索框架
tldr: 针对现有检索增强推理方法中单查询检索导致的信息覆盖不足和信噪比低的问题，本文提出了 MultiSearch 框架。该框架采用强化学习驱动，在每个推理步骤并行生成多视角查询以扩大搜索范围，并通过显式的合并过程对检索到的信息进行整合与提炼。通过多过程奖励设计的强化学习优化，MultiSearch 有效提升了检索质量和推理准确性，在多个问答基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有检索增强推理方法通常每步仅生成单个查询，导致信息覆盖不足且信噪比低，从而限制了推理的准确性。
method: 提出 MultiSearch 框架，通过并行多视角查询检索信息，并结合显式合并过程与多过程奖励的强化学习进行优化。
result: 在七个基准测试上的实验证明，MultiSearch 显著提高了检索的信噪比，并在复杂问答任务中优于现有基准方法。
conclusion: 该研究表明，通过并行搜索和显式信息整合可以有效扩展检索范围并过滤噪声，从而增强大语言模型的推理能力。
---

## 摘要
深度搜索智能体已被证明在多步推理过程中通过检索外部知识来增强大语言模型（LLMs）方面非常有效。然而，现有方法通常在每个推理步骤中仅生成单个查询进行检索，这限制了信息覆盖范围并引入了高噪声。这可能导致搜索过程中的信噪比（SNR）较低，从而降低推理准确性并导致不必要的推理步骤。在本文中，我们提出了 MultiSearch，这是一个基于强化学习（RL）的框架，通过多查询检索和检索信息的显式合并来解决这些局限性。在每个推理步骤中，MultiSearch 从多个角度生成查询并并行检索外部信息，扩大了相关信息的范围，并减轻了对单一检索结果的依赖。随后，智能体在合并过程中整合并提炼检索到的信息，从而提高信噪比并确保更准确的推理。此外，我们提出了一个具有多过程奖励设计的强化学习框架，以优化智能体在多查询检索和信息整合方面的能力。在七个基准测试上的广泛实验表明，MultiSearch 优于基准方法，增强了检索的信噪比，并提升了问答任务中的推理性能。

## Abstract
Deep search agents have proven effective in enhancing LLMs by retrieving external knowledge during multi-step reasoning. However, existing methods often generate a single query for retrieval at each reasoning step, limiting information coverage and introducing high noise. This may result in low signal-to-noise ratios (SNR) during search, degrading reasoning accuracy and leading to unnecessary reasoning steps. In this paper, we introduce MultiSearch, an RL-based framework that addresses these limitations through multi-query retrieval and explicit merging of retrieved information. At each reasoning step, MultiSearch generates queries from multiple perspectives and retrieves external information in parallel, expanding the scope of relevant information and mitigating the reliance on any single retrieval result. Then, the agent consolidates and refines retrieved information at the merging process, improving the SNR and ensuring more accurate reasoning. Additionally, we propose a reinforcement learning framework with a multi-process reward design to optimize agents for both multi-query retrieval and information consolidation. Extensive experiments on seven benchmarks demonstrate that MultiSearch outperforms baseline methods, enhancing the SNR of retrieval and improving reasoning performance in question-answering tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Retrieval-Augmented Reasoning with Parallel Search and Explicit Merging》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：现有的深度搜索智能体（如 Search-R1）在多步推理中通常采用“每步生成单个查询”的模式。
*   **核心痛点**：
    1.  **低信噪比（SNR）**：单次查询往往覆盖不全或引入大量无关噪声，导致推理上下文质量差。
    2.  **效率低下**：由于单次检索获取的信息不足，模型往往需要更多次的搜索迭代来弥补，增加了推理成本。
    3.  **监督不足**：现有的强化学习（RL）方法多依赖最终答案的正确性（Outcome-level reward），缺乏对中间检索和信息整合行为的精细指导。
*   **整体含义**：本文提出了 **MultiSearch** 框架，旨在通过“并行多查询检索”和“显式信息合并”来扩大信息覆盖面并过滤噪声，从而提升大模型在知识密集型任务中的推理能力。

### 2. 方法论
*   **核心思想**：在每个推理步骤中，不再只发一个查询，而是从多个视角并行生成多个查询，并在获取结果后进行显式的提炼与合并。
*   **关键技术细节**：
    *   **并行多查询策略**：智能体被要求采用三种策略生成查询：**改写（Rephrasing）**、**概念扩展（Concept Expansion）**和**问题分解（Question Decomposition）**。
    *   **显式合并（Explicit Merging）**：在 `<merge>` 标签内，模型需从检索到的多篇文档中提取关键证据，去除冗余和噪声，形成高质量的推理上下文。
    *   **多过程奖励设计**：
        1.  **答案奖励 ($R_{ans}$)**：预测答案与标准答案的 F1 分数。
        2.  **多查询奖励 ($R_{query}$)**：鼓励模型在每步生成多个（通常为3个）有效查询。
        3.  **合并奖励 ($R_{merge}$)**：验证合并后的文本是否包含正确答案的关键信息。
    *   **强化学习算法**：采用 **GDPO**（Group reward-Decoupled Normalization Policy Optimization），该算法能独立归一化不同来源的奖励信号，解决多目标优化时的奖励失衡问题。

### 3. 实验设计
*   **数据集**：涵盖了 7 个问答基准测试：
    *   **单跳（Single-Hop）**：NQ, TriviaQA, PopQA。
    *   **多跳（Multi-Hop）**：HotpotQA, 2WikiMulti-HopQA, Musique, Bamboogle。
*   **基准方法（Baselines）**：
    *   无检索方法：Direct, CoT, SFT, R1-like。
    *   静态检索：标准 RAG。
    *   动态/智能体检索：Search-o1, IRCoT, ReSearch, Search-R1, AutoRefine, Dr.Zero 等。
*   **实验设置**：使用 Qwen2.5-3B/7B 的 Base 和 Instruct 版本作为骨干模型。

### 4. 资源与算力
*   **硬件使用**：
    *   **3B 模型**：使用 4 块 NVIDIA A100 GPU。
    *   **7B 模型**：使用 8 块 NVIDIA A100 GPU。
*   **训练细节**：
    *   训练步数：约 200-300 个优化步骤。
    *   批量大小（Batch Size）：512。
    *   检索配置：每步生成 3 个查询，每个查询返回 Top-3 文档。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的任务上进行了全面测试，并对比了超过 15 种基准方法。
*   **消融实验**：
    *   验证了 $R_{query}$ 和 $R_{merge}$ 奖励的必要性。
    *   对比了不同查询策略（改写 vs 分解等）的效果。
    *   分析了检索深度（Top-K）和查询数量（$n_q$）的敏感性。
    *   对比了 GRPO 与 GDPO 算法在多奖励场景下的表现。
*   **充分性评价**：实验设计非常详尽，不仅关注最终准确率，还深入分析了检索过程的信噪比（SNR）和搜索步数，证明了方法的客观有效性。

### 6. 主要结论与发现
*   **性能领先**：MultiSearch 在所有 7 个基准测试中均取得了最高（或并列第一）的平均准确率。
*   **信噪比提升**：相比 Search-R1，MultiSearch 的 `<merge>` 步骤显著提高了上下文的信噪比。
*   **搜索效率**：虽然单步检索更复杂，但由于单步获取信息更精准，**总搜索步数显著减少**（例如在 2Wiki 数据集上减少了 23% 的步数）。
*   **模型特性**：Base 模型在经过 RL 训练后，往往比 Instruct 模型表现出更强的多步推理适应性。

### 7. 优点
*   **架构创新**：引入并行搜索和显式合并，解决了长久以来 RAG 智能体“单线思维”导致的检索片面性问题。
*   **优化策略精准**：使用 GDPO 处理多过程奖励，有效避免了模型为了刷某一类奖励（如只发查询而不顾答案）而产生的退化行为。
*   **鲁棒性强**：通过多视角查询，降低了对单一检索结果质量的敏感度。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在问答（QA）任务，尚未在代码生成、创意写作或开放式对话等其他 RAG 场景中验证。
*   **语料库限制**：依赖于静态的 Wikipedia 离线索引，未在实时互联网搜索环境下测试。
*   **策略固定**：查询生成策略（改写、扩展、分解）虽然有效，但仍属于预定义的启发式方法，未来可探索更动态的策略生成。

（完）
