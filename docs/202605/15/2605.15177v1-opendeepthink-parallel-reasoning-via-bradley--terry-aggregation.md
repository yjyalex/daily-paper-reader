---
title: "OpenDeepThink: Parallel Reasoning via Bradley--Terry Aggregation"
title_zh: OpenDeepThink：通过 Bradley-Terry 聚合实现的并行推理
authors: "Shang Zhou, Wenhao Chai, Kaiyuan Liu, Huanzhi Mao, Qiuyang Mang, Jingbo Shang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15177v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过推理时计算扩展和 Bradley-Terry 聚合提升大模型推理能力
tldr: 本研究针对LLM推理的测试时计算扩展，提出OpenDeepThink框架。为解决并行采样中的评估偏差，该框架利用Bradley-Terry模型聚合两两比较结果进行全局排名，并结合自然语言批评对候选方案进行迭代变异。实验表明，该方法使Gemini 3.1 Pro的Codeforces Elo评分提升405分，并发布了专家级CF-73数据集，证明了并行推理与群体演化在客观领域的有效性。
selection_source: fresh_fetch
motivation: 旨在解决LLM在并行采样推理时，因点对点评估存在噪声和偏差而难以选出最优候选方案的瓶颈。
method: 采用基于种群的框架，利用Bradley-Terry模型聚合两两比较结果进行排名，并结合批评机制进行优胜劣汰与变异。
result: 在八轮迭代中将Gemini 3.1 Pro的Codeforces Elo评分提升了405分，且在客观验证领域增益显著。
conclusion: OpenDeepThink证明了通过并行推理与群体演化机制可以有效提升模型推理能力，且具备跨模型的通用性。
---

## 摘要
测试时计算量扩展是提升大语言模型（LLM）推理能力的主要维度。现有方法主要通过延长单一推理路径来扩展深度。通过并行采样多个候选方案来扩展广度虽然直接，但会引入选择瓶颈：在没有标准答案验证器的情况下，由于逐点（pointwise）LLM 评判存在噪声和偏见，难以选出最佳候选方案。为解决这一问题，我们推出了 OpenDeepThink，这是一个基于种群的测试时计算框架，通过成对的 Bradley-Terry 比较进行选择。在每一代中，LLM 对随机的候选方案对进行评判，并通过 Bradley-Terry 模型将投票聚合为全局排名；排名靠前的候选方案被保留，前四分之三的方案利用比较过程中产生的自然语言批评进行变异，而最后四分之一则被舍弃。在八轮连续的 LLM 调用（约 27 分钟实际耗时）中，OpenDeepThink 将 Gemini 3.1 Pro 的有效 Codeforces Elo 分数提升了 405 分。该流水线无需重新微调即可在较弱和较强的模型之间迁移；在多领域 HLE 基准测试中，性能提升主要集中在客观可验证的领域，而在主观领域则出现反转。我们发布了 CF-73，这是一个包含 73 道经专家评定的 Codeforces 题目集，带有国际特级大师的标注，且本地评估与官方裁定的一致性达到 99%。

## Abstract
Test-time compute scaling is a primary axis for improving LLM reasoning. Existing methods primarily scale depth by extending a single reasoning trace. Scaling breadth by sampling multiple candidates in parallel is straightforward, but introduces a selection bottleneck: choosing the best candidate without a ground-truth verifier, since pointwise LLM judging is noisy and biased. To address this, we introduce OpenDeepThink, a population-based test-time compute framework that selects via pairwise Bradley-Terry comparison. Each generation, the LLM judges random pairs of candidates and aggregates votes via Bradley-Terry into a global ranking; top-ranked candidates are preserved and the top three quarters are mutated using the natural-language critiques produced during comparison; the bottom quarter is discarded. OpenDeepThink raises Gemini 3.1 Pro's effective Codeforces Elo by +405 points in eight sequential LLM-call rounds (~27 minutes wall-clock). The pipeline transfers across weaker and stronger models without retuning, and on the multi-domain HLE benchmark, gains appear concentrated in objectively verifiable domains and reverse in subjective ones. We release CF-73, a curated set of 73 expert-rated Codeforces problems with International Grandmaster annotation and 99% local-evaluation agreement against the official verdict.

---

## 论文详细总结（自动生成）

以下是对论文《OpenDeepThink: Parallel Reasoning via Bradley–Terry Aggregation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前提升大语言模型（LLM）推理能力的主要途径是扩展“测试时计算量”（Test-time compute）。现有方法（如 OpenAI o1、DeepSeek-R1）侧重于扩展推理的**深度**（更长的思维链或搜索树），这具有顺序性且容易因早期错误导致失败。
*   **核心问题**：通过并行采样多个候选方案来扩展推理**广度**虽然容易实现，但面临“选择瓶颈”——在没有外部验证器（如编译器或标准答案）的情况下，LLM 的逐点评分（Pointwise scoring）存在严重的噪声和正向偏见，难以选出最优解。
*   **整体含义**：论文提出 OpenDeepThink 框架，旨在通过基于种群的演化机制和 Bradley-Terry 成对比较聚合算法，在无需外部验证器的情况下，通过并行计算显著提升 LLM 的推理表现。

### 2. 方法论：核心思想与技术细节
OpenDeepThink 是一个无需训练、无需验证器的并行推理框架，其核心流程如下：
*   **初始采样**：并行生成 $n$ 个候选解决方案。
*   **演化循环（$T$ 代迭代）**：
    1.  **成对比较**：LLM 作为裁判，对随机配对的候选方案进行两两比较，并生成自然语言批评（Critique）。
    2.  **Bradley-Terry (BT) 聚合**：利用 BT 模型 $P(i \succ j) = \sigma(s_i - s_j)$ 将零散的胜负结果转化为全局得分 $s$。BT 模型能有效处理对手强度差异，比胜率更稳健。
    3.  **选择与淘汰**：保留得分前 25% 为精英（Elite）；丢弃得分末 25% 的方案。
    4.  **反馈驱动变异**：对前 75% 的方案（含精英），结合比较中产生的负面反馈进行变异（Mutation），允许模型修正或重写方案。
*   **最终选择**：在演化结束后，进行一轮更密集的成对比较，通过 BT 排名选出最终提交的单一答案。
*   **算法特性**：整个流水线具有极高的并行度，虽然总 API 调用量大，但顺序深度仅为 8 次调用。

### 3. 实验设计
*   **数据集**：
    *   **CF-73**：论文新发布的精选数据集，包含 73 道 2025 年 8 月后的 Codeforces 竞赛题（避免训练数据污染），由国际特级大师标注。
    *   **NOI-119**：包含 119 道信息学奥赛练习题。
    *   **HLE (Humanity's Last Exam)**：从该跨领域基准中抽取 82 题，涵盖数学、科学、人文等。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Gemini 3.1 Pro（主实验）、Gemini 3 Flash、Gemini 2.5 Pro。
    *   **对比方法**：Pass@1（随机采样）、Oracle（Pass@20 上限）、逐点评分裁判（Pointwise Judge）、Self-Refine（单路径迭代优化）。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 GPU 型号或训练时长，因为该方法是**推理时（Inference-only）**框架，主要通过 API 调用实现。
*   **计算成本**：每个问题平均消耗约 **285 次 API 调用**。
*   **时间延迟**：在完全并行的情况下，顺序深度为 8 轮调用。以 Gemini 3.1 Pro 为例，解决一个竞赛编程题的中位耗时约为 **27 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 192 道编程题和 82 道多领域综合题，涵盖了不同难度等级（Easy/Medium/Hard）。
*   **消融实验**：
    *   对比了成对比较与逐点评分的准确率（86% vs 59%）。
    *   分析了不同反馈策略（正向、负向、成对反馈）对变异效果的影响。
    *   研究了种群规模 $n$ 与比较次数 $M$ 的扩展规律（Scaling Laws）。
*   **评价**：实验设计较为充分，通过跨模型（Flash 到 Pro）和跨领域（编程到人文）的测试，验证了方法的通用性与局限性，结论较为客观。

### 6. 主要结论与发现
*   **性能提升显著**：在 Codeforces 任务上，将 Gemini 3.1 Pro 的有效 Elo 分数提升了 **405 分**，效果媲美专门的推理模型（如 Gemini 3 Deep Think）。
*   **选择机制优越**：BT 聚合在识别错误方案方面远强于逐点评分，尤其在 Hard 题目中，选择增益贡献了 14% 的准确率提升。
*   **演化与选择的互补性**：演化提升了种群的“天花板”（Pass@1 提升），而密集的 BT 比较则负责从种群中精准提取出正确答案。
*   **领域敏感性**：该方法在**客观可验证**领域（数学、物理、编程）效果极佳，但在**主观领域**（人文、社科）效果反而下降，因为 LLM 裁判在这些领域无法提供可靠的成对判断。

### 7. 优点
*   **无需训练与验证器**：完全依赖推理时计算，不需要针对特定领域编写测试用例或训练奖励模型（RM）。
*   **高并行性**：相比于长链推理，该方法能更好地利用并行计算资源，缩短实际挂钟时间。
*   **鲁棒的选择算法**：引入 Bradley-Terry 模型解决了 LLM 评分偏见问题，为并行采样提供了一个可靠的“软验证器”。

### 8. 不足与局限
*   **成本高昂**：每道题 285 次 API 调用对于大规模应用来说成本极高。
*   **裁判偏见风险**：框架的有效性高度依赖于 LLM 作为裁判的判别能力，如果裁判无法区分优劣（如在主观题中），演化过程会引入噪声甚至导致性能退化。
*   **延迟限制**：尽管可并行，但 27 分钟的响应时间仍不适用于实时交互场景。
*   **模型依赖**：目前仅在 Gemini 系列模型上验证，尚未证明在其他架构（如 GPT 或 Claude）上的迁移效果。

（完）
