---
title: Learning from Language Feedback via Variational Policy Distillation
title_zh: 通过变分策略蒸馏从语言反馈中学习
authors: "Yang Li, Erik Nijkamp, Semih Yavuz, Shafiq Rayhan Joty"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15113v1"
tags: ["query:token"]
score: 9.0
evidence: 针对复杂推理任务，通过语言反馈生成Token级监督信号
tldr: 针对强化学习中可验证奖励信号稀疏导致的探索瓶颈，本文提出变分策略蒸馏（VPD）框架。该框架将从语言反馈中学习建模为变分期望最大化（EM）问题，通过协同演化教师和学生策略，使教师能动态改进对文本反馈的解读，从而为学生提供更精准的密集监督信号。在科学推理和代码生成任务上的实验证明，VPD能有效克服固定教师模型的性能瓶颈，显著提升学习效率。
selection_source: fresh_fetch
motivation: 现有的自蒸馏方法依赖固定的教师模型来解释语言反馈，导致教师的评估能力在学生进步后停滞不前，限制了持续学习。
method: 将学习过程形式化为变分EM问题，通过E步动态优化教师对反馈的解读，并在M步让学生内化教师提供的密集分布指导。
result: 在科学推理和代码生成等任务中，VPD的性能一致优于标准的强化学习方法和现有的自蒸馏基准。
conclusion: VPD通过教师与学生的协同演化，克服了被动蒸馏的局限性，为利用语言反馈解决复杂推理任务提供了有效的新范式。
---

## 摘要
基于可验证奖励的强化学习（RLVR）面临结果信号稀疏的问题，这在复杂推理任务中造成了严重的探索瓶颈。最近的同策略自蒸馏方法尝试通过利用语言反馈生成密集的词元级监督来解决这一问题。然而，这些方法依赖于一个固定的、被动的教师模型来解释反馈。随着学生策略的提升，教师模型的零样本评估能力会进入平台期，最终导致学习停滞。为了克服这一限制，我们提出了变分策略蒸馏（VPD），这是一个将从语言反馈中学习形式化为变分期望最大化（EM）问题的框架。VPD 使两种策略共同演化：在 E 步中，教师模型通过自适应置信域更新根据轨迹结果进行主动优化，将文本反馈转化为动态改进的目标词元分布。在 M 步中，学生模型在其自身的同策略展开中内化这种密集的分布引导。通过不断提高教师模型从文本批评中提取可执行信号的能力，VPD 克服了被动蒸馏的局限性。在科学推理和代码生成任务的多种诊断反馈源上进行的评估表明，VPD 一贯优于标准 RLVR 和现有的自蒸馏基准。最后，通过在严谨的数学推理和冷启动场景下对我们的框架进行压力测试，我们阐明了与纯环境驱动的强化学习相比，反馈驱动的自蒸馏的基本边界。

## Abstract
Reinforcement learning from verifiable rewards (RLVR) suffers from sparse outcome signals, creating severe exploration bottlenecks on complex reasoning tasks. Recent on-policy self-distillation methods attempt to address this by utilizing language feedback to generate dense, token-level supervision. However, these approaches rely on a fixed, passive teacher to interpret the feedback. As the student policy improves, the teacher's zero-shot assessment capabilities plateau, ultimately halting further learning. To overcome this, we propose Variational Policy Distillation (VPD), a framework that formalizes learning from language feedback as a Variational Expectation-Maximization (EM) problem. VPD co-evolves both policies: in the E-step, the teacher is actively refined on trajectory outcomes via an adaptive trust-region update, translating textual feedback into a dynamically improved target token distribution. In the M-step, the student internalizes this dense distributional guidance on its own on-policy rollouts. By continuously improving the teacher's ability to extract actionable signals from textual critique, VPD overcomes the limitations of passive distillation. Evaluated across diverse sources of diagnostic feedback on scientific reasoning and code generation tasks, VPD consistently outperforms both standard RLVR and existing self-distillation baselines. Finally, by stress-testing our framework on rigid mathematical reasoning and cold-start regimes, we illuminate the fundamental bounds of feedback-driven self-distillation compared to pure environment-driven RL.

---

## 论文详细总结（自动生成）

### 论文总结：通过变分策略蒸馏从语言反馈中学习 (VPD)

#### 1. 核心问题与整体含义
论文针对**基于可验证奖励的强化学习 (RLVR)** 在处理复杂推理任务时面临的**奖励信号稀疏**和**信用分配困难**问题展开研究。
*   **研究动机**：传统的 RLVR（如 GRPO）依赖二元化的正确/错误信号，导致探索效率低下。虽然现有的“自蒸馏”方法（如 SDPO）引入了语言反馈（如编译器错误、自我批评）来提供密集信号，但它们通常将反馈条件下的“教师”视为固定不变的。
*   **核心矛盾**：随着学生模型的进步，固定教师的零样本评估能力会达到瓶颈，无法再提供更有价值的指导，导致学习停滞。

#### 2. 方法论
论文提出了 **变分策略蒸馏 (Variational Policy Distillation, VPD)**，将从语言反馈中学习建模为一个**变分期望最大化 (EM)** 过程，实现教师与学生策略的协同演化。
*   **核心思想**：不把教师看作固定的启发式函数，而是将其视为一个需要不断优化的近似后验分布。
*   **关键技术细节**：
    *   **E-步（教师精炼）**：利用学生生成的轨迹和环境反馈，通过**非配对偏好优化 (BCO)** 更新教师参数。为了保证稳定性，引入了**动态置信域**，将参考策略锚定在当前学生模型上，防止教师漂移过快。
    *   **M-步（学生蒸馏）**：学生模型在不带反馈的情况下，通过最小化与精炼后教师分布的词元级 KL 散度，内化教师的推理能力。
    *   **共享权重架构**：教师和学生共享同一套神经网络参数，仅通过 Prompt（是否包含反馈 $C$）进行区分。这极大地降低了显存开销，避免了维护多个大模型的负担。

#### 3. 实验设计
*   **数据集/场景**：
    *   **代码生成**：LiveCodeBench (LCB) v6，利用编译器错误信息作为反馈。
    *   **科学推理**：SciKnowEval（涵盖生物、化学、物理、材料），利用对比性兄弟轨迹（Sibling Rollouts）或模型自我批评作为反馈。
    *   **数学推理**：DAPO-Math, Math500, AIME 24/25, AMC 23。
*   **Benchmark 模型**：Qwen3 (1.7B, 8B), OLMo3 (7B-Instruct)。
*   **对比方法**：
    *   **纯 RL**：GRPO。
    *   **纯蒸馏**：SDPO。
    *   **混合方法**：联合损失 (Joint Loss)、优势重塑 (Advantage Reshaping)、优势重加权 (Advantage Reweighting)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：作者提到 VPD 相比 SDPO 增加了梯度计算开销（运行时间增加约 30%-55%），但由于采用了共享权重架构，**显存占用没有增加**。通过非对称更新频率（如每 5 次 M-步执行 1 次 E-步），计算成本得到了有效控制。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构/规模的模型，并在代码、科学、数学三大领域进行了广泛测试。
*   **消融实验**：针对 E-步更新频率、动态 vs 静态参考先验进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分且客观。作者不仅展示了 VPD 的优越性，还专门设置了“压力测试”环节，探讨了在数学推理和基座模型“冷启动”场景下的局限性，体现了学术严谨性。

#### 6. 主要结论与发现
*   **性能提升**：在代码生成和科学推理任务中，VPD 显著优于 GRPO 和 SDPO，证明了动态优化教师的必要性。
*   **稳定性**：VPD 的 EM 框架比简单的混合损失函数更稳定，能够持续拉大正确与错误响应之间的奖励差距（Margin）。
*   **边界发现**：在极度严谨的数学推理和完全没有指令遵循能力的基座模型上，自蒸馏方法（包括 VPD）仍难以超越纯环境驱动的 RL（GRPO）。这说明语言反馈的质量和模型对反馈的理解能力是自蒸馏生效的前提。

#### 7. 优点
*   **理论严谨**：将自蒸馏提升到了变分推理的高度，提供了清晰的数学解释。
*   **架构优雅**：共享权重设计解决了多模型协同训练时的显存瓶颈，具有很强的实用性。
*   **动态演化**：通过 E-步不断提升教师的“鉴赏力”，克服了传统方法中教师能力平台化的问题。

#### 8. 不足与局限
*   **数学任务瓶颈**：在数学等对逻辑严密性要求极高的领域，语言反馈可能引入噪声，导致蒸馏效果不如纯 RL。
*   **计算开销**：虽然显存可控，但额外的 E-步计算确实增加了训练时间。
*   **反馈依赖**：VPD 的效果高度依赖于诊断性反馈 $C$ 的质量。如果环境无法提供有意义的反馈，或者模型自我批评存在严重幻觉，该框架的优势将受限。

（完）
