---
title: "Dual-Dimensional Consistency: Balancing Budget and Quality in Adaptive Inference-Time Scaling"
title_zh: 双维度一致性：在自适应推理时扩展中平衡预算与质量
authors: "Rongman Xu, Yifei Li, Tianzhe Zhao, Yanrui Wu, Bo Li, Hang Yan"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15100v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 采样预算与推理质量之间的权衡
tldr: 针对大语言模型推理时缩放中采样预算与推理质量的权衡难题，本文提出双维度一致性（DDC）框架。该框架结合置信度加权贝叶斯协议与趋势感知分层剪枝，统一了路径质量评估与自适应终止机制。实验表明，DDC在保持或提升推理准确率的同时，将Token消耗降低了10倍以上，有效解决了传统方法中幻觉增强和过早截断的问题。
selection_source: fresh_fetch
motivation: 现有的推理缩放策略将采样宽度和深度视为独立目标，容易导致幻觉累积或过早截断复杂的有效推理路径。
method: 提出DDC框架，利用置信度加权贝叶斯协议和趋势感知分层剪枝，实现高质量路径的资源集中与自适应推理终止。
result: 在五个基准测试中，该方法在多种LLM上实现了10倍以上的Token消耗削减，同时保持或提升了推理准确率。
conclusion: DDC通过协同优化采样宽度与深度，为实现高效且高质量的自适应推理时缩放提供了一种统一且有效的方案。
---

## 摘要
大语言模型（LLMs）在推理方面展现出了卓越的能力。然而，通过推理时扩展（inference-time scaling）来最大化其潜力，在采样预算与推理质量的权衡方面面临挑战。当前的策略仍然效率低下，因为它们通常将采样宽度和深度视为正交目标，其中宽度共识方法存在强化幻觉的风险，而深度剪枝机制则会过早地截断复杂但有效的推理链。因此，我们提出了双维度一致性（DDC），这是一个将路径质量与自适应终止相结合的统一框架。通过将置信度加权贝叶斯协议（Confidence-Weighted Bayesian protocol）与趋势感知分层剪枝（Trend-Aware Stratified Pruning）相结合，我们的方法确保计算资源集中在高推理质量路径上，在加速共识的同时过滤幻觉。在五个基准测试上的评估表明，该方法在各种 LLM 中将 token 消耗降低了 10 倍以上，同时保持或超过了强基准模型的准确率。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable abilities in reasoning. However, maximizing their potential through inference-time scaling faces challenges in trade-off between sampling budget and reasoning quality. Current strategies remain inefficient as they typically treat sampling width and depth as orthogonal objectives, where width consensus methods risk reinforcing hallucinations, while depth pruning mechanisms prematurely truncate complex yet valid reasoning chains. Therefore, we propose Dual-Dimensional Consistency (DDC), a unified framework that bridges path quality with adaptive termination. By coupling Confidence-Weighted Bayesian protocol with a Trend-Aware Stratified Pruning, our method ensures that computational resources are concentrated on high quality reasoning paths, filtering hallucinations while accelerating consensus. Evaluations across five benchmarks demonstrate that this approach reduces token consumption by over 10 times while maintaining or exceeding the accuracy of strong baselines across various LLMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DDC (Dual-Dimensional Consistency，双维度一致性)** 的推理时扩展（Inference-Time Scaling）框架，旨在解决大语言模型（LLM）在推理过程中采样预算与结果质量之间的失衡问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然增加推理时的计算量（如多次采样）能提升模型表现，但传统方法存在效率瓶颈。
    *   **静态扩展（如 Self-Consistency）**：对简单和复杂问题分配固定预算，造成资源浪费。
    *   **现有自适应方法**：通常将“采样宽度”（何时停止采样新路径）和“采样深度”（何时剪枝单条路径）视为独立目标。这导致宽度共识可能强化“自信的幻觉”，而深度剪枝可能误伤复杂但正确的推理链。
*   **核心目标**：建立一个统一框架，通过协同优化路径质量和采样预算，在降低计算成本的同时提升推理准确率。

### 2. 方法论：核心思想与关键技术
DDC 框架由两个相互耦合的维度组成：

*   **多粒度置信度度量**：定义了 Token 级、组级（滑动窗口均值）和路径级（窗口置信度最小值）的度量指标，用于捕捉推理过程中的细微波动。
*   **宽度维度：置信度加权贝叶斯终止 (CoW)**：
    *   将采样终止视为贝叶斯序列决策问题。
    *   利用 **Beta 分布** 建模领先答案的胜率，并根据路径的置信度进行加权更新。
    *   当领先答案的后验概率超过阈值（如 0.95）时提前终止采样，防止在低质量共识上浪费资源。
*   **深度维度：趋势感知分层剪枝 (TASP)**：
    *   将推理过程视为动态信号，构建包含置信度位置和速度的状态向量。
    *   利用 **特征值分解 (Eigendecomposition)** 分析滞后协方差矩阵，区分“推理趋势”与“随机噪声”。
    *   引入 **结构不稳定性得分 (R)**，结合 **Tukey Fences (图基栅栏)** 动态识别并剪枝掉那些表现出持续衰减或混乱趋势的幻觉路径，同时保留暂时遇到困难但有恢复趋势的复杂路径。
*   **最终共识**：采用置信度加权的多数投票法确定最终答案。

### 3. 实验设计
*   **数据集**：涵盖 5 个具有挑战性的推理基准：MATH-500, AMC23, AIME24, AIME25, 以及研究生水平的 GPQA-diamond。
*   **测试模型**：Qwen3 系列（1.7B, 4B, 8B, 32B）以及 DeepSeek-R1-0528-Qwen3-8B。
*   **对比方法 (Baselines)**：
    *   标准 Self-Consistency (SC)。
    *   Adaptive-Consistency (AC)。
    *   基于置信度的策略 (DeepConf-Low/High)。
    *   计算密集型方法：Predictive Decoding, $\phi$-Decoding。

### 4. 资源与算力
*   **算力消耗**：论文明确指出 DDC 引入的分析组件（如趋势分析）计算开销极低。
*   **复杂度分析**：DDC 的辅助操作（FLOPs）比 LLM 的前向传播低约 7 个数量级。
*   **具体硬件**：文中未详细列出具体的 GPU 型号和训练时长，但强调了其作为“训练后/推理时”方法的即插即用性，主要关注推理时的 Token 节省。

### 5. 实验数量与充分性
*   **实验规模**：在 5 种不同规模和架构的模型上进行了测试，涵盖了从轻量级（1.7B）到中量级（32B）的模型。
*   **消融实验**：非常充分。分别验证了“加权贝叶斯”、“宽度扩展”、“趋势感知”和“深度剪枝”各自的贡献。
*   **敏感性分析**：对初始预算（$B_{init}$）和窗口大小（$L$）进行了详细的参数扫描。
*   **客观性**：通过多次重复实验（5次数据集重复，每条查询至少16次采样）并报告平均值，确保了结果的可靠性。

### 6. 主要结论与发现
*   **极高的效率提升**：DDC 在保持或超过基准准确率的前提下，平均将 Token 消耗降低了 **10 倍以上**。在 AIME25 任务上，相比 SC 甚至实现了 27 倍的 Token 削减。
*   **准确率增益**：在 Qwen3-4B 上，AIME25 的准确率比 SC 提升了 15.6%。
*   **推理时缩放定律**：实验证明 DDC 在不同的 Token 预算下均表现出优于其他方法的缩放效率。
*   **协同效应**：宽度和深度的联合优化比单一维度的自适应更有效，能更好地识别并过滤“自信的幻觉”。

### 7. 优点
*   **统一性**：首次将采样宽度和深度通过置信度信号有机结合。
*   **动态性**：利用信号处理技术（特征值分解）区分推理中的“困难”与“错误”，比静态阈值更智能。
*   **自适应性**：使用 Tukey Fences 实现每条查询的动态阈值校准，无需人工针对不同任务调参。
*   **低开销**：算法逻辑简单高效，几乎不增加推理延迟。

### 8. 不足与局限
*   **系统性失准风险**：如果基础模型对某个错误答案表现出极其一致且高置信度的偏见（Systematic Miscalibration），DDC 仍可能提前终止在错误答案上。
*   **依赖置信度质量**：该方法的效果高度依赖于模型输出概率（Logits）作为置信度信号的准确性。
*   **初始阶段开销**：需要一个初始的 Warmup 阶段（如采样 16 条路径）来建立基准，对于极简单的单次查询任务，这可能略微增加了基础开销。

（完）
