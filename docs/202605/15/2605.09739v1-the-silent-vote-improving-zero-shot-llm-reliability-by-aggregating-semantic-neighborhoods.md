---
title: "The Silent Vote: Improving Zero-Shot LLM Reliability by Aggregating Semantic Neighborhoods"
title_zh: 沉默的投票：通过聚合语义邻域提升零样本大语言模型的可靠性
authors: "Sanket Badhe, Priyanka Tiwari, Deep Shah"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09739v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 受限解码对零样本推理可靠性的影响
tldr: 本研究针对大语言模型在零样本分类中存在的“重归一化偏差”问题，提出了“语义Softmax”方法。传统受限解码会忽略语义近义词的概率质量（即“沉默投票”），导致模型过度自信且校准性差。该方法通过在推理阶段聚合目标标签语义邻域的分数，有效找回丢失信息。实验证明，该方法在多个数据集和模型上显著降低了校准误差，并提升了分类性能，增强了零样本分类的可靠性。
selection_source: fresh_fetch
motivation: 传统受限解码在将概率限制在少数标签时会丢弃近义词的概率质量，导致模型产生虚假的高置信度和较差的校准性。
method: 提出语义Softmax推理层，通过聚合每个目标标签周围语义邻域的分数来恢复被忽视的“沉默投票”信息。
result: 在Qwen-3和Phi-4-mini模型上的实验显示，该方法显著降低了期望校准误差（ECE），并提升了AUROC和Macro-F1等性能指标。
conclusion: 通过考虑语言细微差别并进行语义聚合，该方法为零样本分类提供了一种更准确且校准性更好的替代方案。
---

## 摘要
大语言模型越来越多地被用作复杂推理任务中的零样本分类器。然而，标准的受限解码面临着一种我们定义为“重归一化偏差”（Renormalization Bias）的现象。当模型被限制在较小的目标标签集时，标准的 softmax 操作会丢弃原始分布中分配给语义同义词的概率质量。这种信息损失（我们称之为“沉默的投票”）会导致人为的过度自信和较差的校准度。我们提出了语义 Softmax（Semantic Softmax），这是一种推理时层，通过聚合每个目标标签周围语义邻域的分数来恢复这些丢失的信息。我们在 GoEmotions 和 Civil Comments 数据集上，使用 Qwen-3 和 Phi-4-mini 模型对该方法进行了评估。结果表明，在所有评估指标上均有一致的提升：Semantic Softmax 显著降低了预期校准误差（ECE）和 Brier 分数，同时增强了 AUROC 和 Macro-F1 方面的判别性能。通过考虑语言细微差别，我们的方法为零样本分类提供了一个更具校准性且更准确的替代方案。

## Abstract
Large Language Models are increasingly used as zero-shot classifiers in complex reasoning tasks. However, standard constrained decoding suffers from a phenomenon we define as Renormalization Bias. When a model is restricted to a small set of target labels, the standard softmax operation discards the probability mass assigned to semantic synonyms in the original distribution. This loss of information, which we call the Silent Vote, results in artificial overconfidence and poor calibration.   We propose Semantic Softmax, an inference-time layer that recovers this lost information by aggregating the scores of the semantic neighborhood surrounding each target label. We evaluate this approach on Qwen-3 and Phi-4-mini models using GoEmotions and Civil Comments datasets. Our results demonstrate consistent improvements across all evaluation metrics: Semantic Softmax substantially reduces Expected Calibration Error (ECE) and Brier Score, while simultaneously enhancing discriminative performance in terms of AUROC and Macro-F1. By accounting for linguistic nuances, our method provides a more calibrated and accurate alternative for zero-shot classification.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Silent Vote: Improving Zero-Shot LLM Reliability by Aggregating Semantic Neighborhoods》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在将大语言模型（LLM）用作零样本分类器时，业界普遍采用“受限解码”（Constrained Decoding），即强制模型只能在预定义的标签集（如“正面”、“负面”）中输出。
*   **核心问题**：论文指出这种做法存在**重归一化偏差（Renormalization Bias）**。当模型预测某个文本属于“激动”但标签只有“快乐”时，模型原本分配给“激动”、“兴奋”等近义词的概率质量会被直接丢弃。
*   **整体含义**：这种信息的丢失被称为**“沉默的投票”（Silent Vote）**。它导致模型在受限空间内表现出虚假的高置信度，造成严重的校准失调（Miscalibration），即模型给出的概率不能反映其真实的准确性。

### 2. 论文提出的方法论：Semantic Softmax
核心思想是：在推理阶段，不再简单地屏蔽非目标词汇，而是将词表空间中语义相近词汇的概率质量“回收”并聚合到目标标签上。
*   **关键步骤**：
    1.  **提取原始分布**：获取模型未受限时的 Top-K（如 K=50）个候选 Token 及其原始概率。
    2.  **计算语义权重**：利用模型的输出层嵌入（Output Embeddings），计算 Top-K Token 与目标标签 Token 之间的余弦相似度。
    3.  **阈值过滤**：引入超参数 $\tau$（如 0.8），只有相似度超过该阈值的 Token 才会贡献权重，以过滤噪声。
    4.  **加权聚合**：目标标签的最终得分是其自身概率与所有语义邻居贡献概率的加权总和，最后进行归一化。
*   **公式逻辑**：通过语义核函数 $w(v, l)$ 衡量词汇 $v$ 对标签 $l$ 的支持度，从而实现概率质量的重新分配。

### 3. 实验设计
*   **实验模型**：Qwen-3-1.7B 和 Phi-4-mini (3.8B)，代表了高效的小型指令微调模型。
*   **数据集**：
    *   **GoEmotions**：包含 28 种细粒度情感，标签间语义重叠严重（如“钦佩”与“批准”），用于测试近义词回收。
    *   **Civil Comments (歧义子集)**：包含人类标注者存在分歧的评论，用于评估模型处理偶然不确定性（Aleatoric Uncertainty）的能力。
*   **对比基准（Benchmark）**：标准的受限解码（Standard Constrained Decoding）。
*   **评估指标**：预期校准误差 (ECE)、Brier 分数（衡量概率质量）、Macro-F1 和 AUROC（衡量分类性能）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或推理总时长。
*   **实现成本**：由于该方法是“训练无关”（Training-free）的推理层干预，不需要重新训练或微调模型，主要开销在于推理时的 Top-K 搜索和向量相似度计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种不同架构的模型和两个具有代表性的分类任务。
    *   进行了**超参数敏感性分析**（针对 K 值和 $\tau$ 阈值做了详尽的网格搜索，见表 3）。
    *   提供了**定性分析**（表 2），对比了人类真实标签、标准得分与语义 Softmax 得分的差异。
*   **充分性评价**：实验设计较为客观、公平。通过引入 Brier 分数和 ECE，不仅关注准确率，更深入探讨了概率分布的合理性。但实验模型规模较小（均在 4B 参数以下），尚未在大规模模型（如 70B+）上验证。

### 6. 主要结论与发现
*   **校准度大幅提升**：在 GoEmotions 上，ECE 降低了多达 8.5 倍；在 Civil Comments 上，ECE 平均降低了 75% 以上。
*   **性能不降反升**：在改善校准度的同时，AUROC 和 Macro-F1 也有所提升，说明聚合语义邻域找回了有用的判别信号。
*   **对齐人类不确定性**：在面对模糊输入时，Semantic Softmax 产生的概率分布与人类标注者的分歧程度高度一致，有效缓解了模型的过度自信。

### 7. 优点
*   **即插即用**：无需参数更新，非常适合现有推理基础设施的快速部署。
*   **理论扎实**：正式定义了“重归一化偏差”，为 LLM 分类器过度自信的问题提供了新的理论解释。
*   **多维提升**：同时解决了可靠性（校准）和有效性（判别性能）两个核心问题。

### 8. 不足与局限
*   **推理延迟**：引入了动态 Top-K 搜索和相似度计算，虽然对单 Token 分类影响较小，但在长文本生成或高并发场景下会增加计算开销。
*   **依赖嵌入质量**：该方法的效果高度依赖于模型原始嵌入空间的语义聚类质量。如果模型本身的 Embedding 存在各向异性或语义坍缩，效果可能受限。
*   **语言局限性**：目前仅在英语数据集上进行了验证，跨语言场景下的同义词聚合效果尚待研究。
*   **模型规模限制**：实验仅限于小型模型，缺乏在超大规模参数模型上的扩展性验证。

（完）
