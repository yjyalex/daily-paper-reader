---
title: "PAIR: Prefix-Aware Internal Reward Model for Multi-Turn Agent Optimization"
title_zh: PAIR：面向多轮智能体优化的前缀感知内部奖励模型
authors: "Wonjoong Kim, Yeonjun In, Sangwu Park, Dongha Lee, Chanyoung Park"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.17877v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用内部隐藏状态的过程奖励模型
tldr: 针对LLM多步任务中GRPO稀疏奖励导致中间步骤信用分配困难的问题，提出PAIR模型，利用隐层状态内部正确性探测作为步骤级奖励信号，通过冻结的隐层探测与注意力校正头结合，实现低成本高精度奖励，无需外部模型、真实答案或完整轨迹。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17877v1/fig-001.webp\", \"caption\": \"Figure 9: User prompts driving the GPT-4o-mini contamination generator.\", \"page\": 25, \"index\": 1, \"width\": 659, \"height\": 844}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-002.webp\", \"caption\": \"Figure 10: Prompts for GPT-4o-mini to judge the score of each trajectory step.\", \"page\": 25, \"index\": 2, \"width\": 659, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-003.webp\", \"caption\": \"Table 2: Step-level correctness prediction performance on GTA and ToolBench. Each evaluation point is a single (prefix, evaluation turn) pair labelled correct or incorrect, not a trajectory-level outcome. AUROC (higher is better) and ECE (lower is better) are reported for each method. All AUROC and ECE values are reported as mean over 3 random seeds (42, 43, 44).\", \"page\": 7, \"index\": 3, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-004.webp\", \"caption\": \"Figure 7: Hyperparameter analysis on GTA. (a) Peak PAIR-momentum success as a function of the momentum scaling α at fixed β = 0, lr = 3×10−7. The sweet spot lies at α ∈ {5, 10, 20}, all comfortably above the strongest vanilla PAIR baseline (dashed line); α ≤ 2 provides too little momentum signal to recover the within-group variance needed for GRPO advantage estimation. (b) A small KL anchor β = 0.01 improves peak success by +0.018 over β = 0 at fixed α = 5. (c) A larger learning rate 1×10−6 reduces peak success by 0.016 relative to 3×10−7 at fixed α = 5, β = 0, and yields a more chaotic per-step trajectory. Reference horizontals: BASE (no RL training, dotted) and the strongest vanilla-PAIR run (dashed).\", \"page\": 22, \"index\": 4, \"width\": 813, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-005.webp\", \"caption\": \"Figure 4: Transfer performance on HotpotQA. Each method is trained on GTA and evaluated on HotpotQA without further fine-tuning. PAIR achieves the best result among all 16 methods.\", \"page\": 10, \"index\": 5, \"width\": 403, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-006.webp\", \"caption\": \"Table 7: GRPO training configuration. All 16 method comparisons share these settings; only the per-step reward function differs.\", \"page\": 19, \"index\": 6, \"width\": 582, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-007.webp\", \"caption\": \"Table 6: Per-step cost of each reward source. Latencies are measured on a single A6000 GPU with Qwen2.5-7B-Instruct as the policy. Additional forward passes counts forward passes in addition to the policy’s generation pass, which all methods incur. PAIR reuses the activations already produced by the generation pass, so it adds no marginal forward pass.\", \"page\": 19, \"index\": 7, \"width\": 748, \"height\": 180}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-008.webp\", \"caption\": \"Figure 6: Ablation over the four attention statistics in PAIR’s Stage 2.\", \"page\": 17, \"index\": 8, \"width\": 814, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-009.webp\", \"caption\": \"Figure 8: System prompts driving the GPT-4o-mini contamination generator.\", \"page\": 24, \"index\": 9, \"width\": 651, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-010.webp\", \"caption\": \"Table 3: Overall performance comparison across all methods on GTA and ToolBench. Values are reported as mean±std over 3 random seeds (42, 43, 44).\", \"page\": 9, \"index\": 10, \"width\": 776, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.17877v1/fig-011.webp\", \"caption\": \"Figure 5: Correctness prediction performance of linear probing models regarding to different contamination distances.\", \"page\": 16, \"index\": 11, \"width\": 814, \"height\": 300}]"
motivation: GRPO在复杂多步任务中因稀疏结果奖励导致中间步骤信用分配困难，现有方案成本高或不可行。
method: 提出PAIR，包含冻结的隐层状态探测估计信念一致性，以及轻量注意力头校正其向正确性，两阶段结合利用互补优势。
result: 在污染轨迹上AUROC最高，推理成本极低，提供密集步骤级奖励，无需外部模型或真实答案。
conclusion: PAIR有效解决了多步LLM优化中的信用分配问题，以极低成本实现高质量内部奖励。
---

## 摘要
当前大语言模型面临的一个重大挑战是执行复杂的多阶段任务。群体相对策略优化（GRPO）正成为一种主流选择，但其对稀疏结果奖励的依赖严重限制了跨中间步骤的信用分配。现有补救方法（例如通过完整轨迹运行来分配步骤级优势、在每个步骤调用外部LLM评判器，或计算需要在每个评估点都有真实答案的内在奖励）会引入显著成本或实际限制。我们假设，利用LLM隐藏状态进行内部正确性探测可以重新用作步骤级奖励信号，可能同时解决所有这些限制。然而，现有探测研究假设输入是干净的，我们首先表明这一假设在多步骤设置下不成立：隐藏状态探测在前缀污染下性能严重退化——它跟踪与（可能被污染的）前缀的一致性而非基于事实的正确性，而基于注意力的特征虽能抵抗污染但在干净前缀上表现不佳。基于这种互补关系，我们提出前缀感知内部奖励（PAIR），这是一个两阶段模型：一个冻结的隐藏状态探测用于估计信念一致性，一个轻量级的基于注意力的头部将其向基于事实的正确性进行修正。实验结果表明，PAIR在受污染轨迹上实现了最高的AUROC，同时推理成本可忽略不计，从而为GRRO训练提供密集的步骤级奖励信号，无需外部模型调用、真实答案依赖或完整轨迹展开。

## Abstract
A significant hurdle for current LLMs is the execution of complex, multi-stage tasks. Group Relative Policy Optimization (GRPO) has been emerging as a leading choice, but its reliance on sparse outcome rewards severely limits credit assignment across intermediate steps. Existing remedies such as running full rollouts to assign step-level advantages, calling external LLM judges at each step, or computing intrinsic rewards that require ground-truth answers at every evaluation introduce significant costs or practical constraints. We hypothesize that internal correctness probing over LLM hidden states can be repurposed as a step-level reward signal, potentially addressing all of these limitations at once. However, existing probing research assumes clean inputs, and we first show that this assumption breaks down in multi-step settings: hidden-state probes degrade severely under prefix contamination tracking coherence with the (possibly corrupted) prefix rather than grounded correctness, while attention-based features remain robust to contamination but underperform on clean prefixes. Building on this complementary relationship, we propose the Prefix-Aware Internal Reward (PAIR), a two-stage model with a frozen hidden-state probe estimating belief-consistency and a lightweight attention-based head correcting it toward grounded correctness. Experimental results show that PAIR achieves the highest AUROC on contaminated trajectories while operating at negligible inference cost, enabling dense step-level reward signals for GRPO training without external model calls, ground-truth dependencies, or full-trajectory rollouts.

---

## 论文详细总结（自动生成）

# 中文详细总结：PAIR（前缀感知内部奖励模型）

## 1. 论文的核心问题与整体含义

**研究动机**：  
- 大语言模型在多步推理/代理任务中面临**稀疏奖励**问题——GRPO等主流RL方法仅在最终步骤给出正确/错误信号，导致中间步骤的**信用分配**困难。  
- 现有解决方案存在关键缺陷：
  - 运行完整轨迹再反向传播（如树搜索） → 计算浪费、延迟高；
  - 每步调用外部LLM评判 → API成本高、延迟大，不适合在线RL；
  - 基于内部log概率的内在奖励 → 依赖每步真实答案，实用性受限；
  - 过程奖励模型（PRM） → 需额外训练或维护外部奖励模型。

**整体含义**：  
本文提出利用LLM自身的**内部隐藏状态**和**注意力特征**作为步骤级奖励信号，仅需一次离线训练，推理时零额外模型调用，实现密集、低成本、无外部依赖的奖励信号，从而解决GRPO在多步代理优化中的核心瓶颈。

## 2. 方法论

### 核心发现
- **隐藏状态探测**在干净前缀上表现优异，但在**前缀污染**（prefix contamination，即历史步骤包含错误）下严重退化：它倾向于捕捉与当前前缀的**信念一致性**（belief-consistency），而非**基于事实的正确性**（grounded correctness）。  
- **注意力特征**（如注意力的峰值、标准差、前缀/自身注意力比例）在污染下仍保持鲁棒，因为它反映的是**结构行为模式**（信息路由方式），而非内容一致性。  
- 两者互补：隐藏状态在干净前缀上占优，注意力在污染前缀上占优，简单拼接（Hidden+Attn）会导致两者相互抵消。

### 提出方法：Prefix-Aware Internal Reward （PAIR）

PAIR 是一个两阶段架构，由一个**冻结的隐藏状态探测**（信念一致性估计器）和一个**基于注意力的校正头**组成。

- **Stage 1：信念一致性得分**  
  \[
  s_{\text{bc}} = \sigma(\mathbf{w}_1^\top \mathbf{h} + b_1)
  \]
  其中 \(\mathbf{h}\) 是最后令牌的隐藏状态。该探测在干净数据上训练后冻结。

- **Stage 2：注意力校正**  
  \[
  s_{\text{final}} = \sigma(\mathbf{w}_2^\top [\mathbf{a}; s_{\text{bc}}] + b_2)
  \]
  \(\mathbf{a}\) 是多层注意力统计向量（4个统计量 × 注意力头数 × 层数）。校正头在混合干净/污染数据上训练，**自适应**调节：干净前缀下 \(s_{\text{final}} \approx s_{\text{bc}}\)，污染前缀下注意力头激活以纠正偏差。

- **训练**：仅需一次离线训练（两个逻辑回归，约30秒CPU）。推理时通过单次前向传播复用已计算的隐藏状态和注意力，奖励计算成本约0.2毫秒/步。

- **在GRPO中集成**：为避免探索后期组内方差坍塌，使用**动量奖励**：
  \[
  r_t = \sigma\left( \text{logit}(\tilde{s}_{\text{final},t}) + \alpha (\tilde{s}_{\text{final},t} - \bar{s}_{<t}) \right)
  \]
  其中 \(\bar{s}_{<t}\) 是运行均值，\(\alpha=5\)，\(\tilde{s}_{\text{final},t}\) 经温度软化（\(T=2\)）和裁剪（\(\epsilon=0.05\)）。

## 3. 实验设计

### 数据集
- **GTA**（General Tool Agents）：229个样本，2-8轮工具组合任务。  
- **ToolBench**：从16000+RapidAPI中分层抽样1000个样本（至少3轮）。  
- **HotpotQA**（跨域迁移）：多跳问答基准。

### 基准方法（共16种）
- **结果奖励**：标准GRPO（仅最终步奖励）  
- **外部奖励**：LLM-as-a-judge（每步GPT-4o-mini评分）  
- **树搜索**：AT²PO、Tree-GRPO  
- **内在奖励**：IGPO、TIPS  
- **内部探测**：
  - 隐藏状态：Last-Token、Mean-Pooled、Multi-Layer、Hidden+Attn
  - 注意力：Attention、Multi-Attn、Lookback Ratio、Head Entropy、CoE-C
  - 无监督：CoE-C等

### 评估指标
- **步级预测**：AUROC、ECE（期望校准误差）  
- **下游RL**：成功率（Success Rate），在GTA和ToolBench上报告均值和标准差（3种子）。

### 实验分为：
1. **步级正确性预测**（表2）：对比9种方法，验证PAIR的AUROC最高、校准良好。  
2. **下游RL训练**（表3）：对比16种方法，PAIR在GTA和ToolBench上均取得第一。  
3. **跨域迁移**（图4）：在GTA上训练，直接评估HotpotQA，PAIR再次领先。  
4. **消融与超参分析**：
   - 注意力统计量消融（图6）：最大/标准差/前缀比例/自身比例。  
   - 污染距离敏感性分析（图5）。  
   - 动量奖励效果（图3）。  
   - 超参α、KL系数β、学习率扫描（图7）。

## 4. 资源与算力

- **GPU**：2×NVIDIA A6000，采用流水线并行。  
- **训练配置**：LoRA微调（r=16），学习率3×10⁻⁷，batchsize 1，group size 4，最大10轮，500步训练。  
- **PAIR奖励计算成本**：每步约0.2ms（无额外前向传播，仅利用已计算的激活）。  
- **离线探测训练**：约30秒CPU（StandardScaler + 逻辑回归）。  
- 文中未给出GRPO训练的**总时长**，但估算：每步4个rollout各最多10步，每步PAIR奖励0.2ms，其余前向传播为主，总训练时间应在数小时内。

## 5. 实验数量与充分性

- **充分性**：高。覆盖了步级预测、下游RL、跨域迁移、超参分析、消融实验、污染距离效应、注意力统计量消融。对比了16种方法，涵盖5大类不同奖励/算法范式。  
- **客观性与公平性**：
  - 所有方法共享同一训练管道（仅奖励信号不同）。  
  - 评估协议一致（GTA用上游评估脚本，ToolBench用LLM评分器，且对LLM-as-a-judge基线也适用）。  
  - 报告3个随机种子的均值与标准差，避免单次结果偶然性。  
- **局限**：仅使用单一基础模型（Qwen-2.5-7B-Instruct），未验证其他规模或架构的通用性；仅两个下游基准（GTA和ToolBench）加一个迁移基准（HotpotQA），覆盖度有待扩展。

## 6. 论文的主要结论与发现

1. **隐藏状态探测在前缀污染下退化为信念一致性**，会奖励延续错误、惩罚修复行为；注意力特征则保持鲁棒。  
2. **PAIR通过两阶段架构有效结合两者优势**：冻结的隐藏状态探测保留干净前缀性能，注意力校正头在污染前缀下自动修正。  
3. **PAIR在步级预测AUROC上显著优于所有单信号探测和无监督方法**（GTA: 0.9217 vs 第二0.9034；ToolBench: 0.8129 vs 第二0.7765）。  
4. **在下游RL任务上PAIR取得最佳成功率**：GTA 24.89%，ToolBench 44.98%，超越LLM-as-a-judge（23.87% / 42.03%）。  
5. **跨域迁移时PAIR同样第一**（HotpotQA: 62.6%），表明其捕捉的是模型计算的**可迁移结构特性**。  
6. **动量奖励设计**进一步在基础探测输出上提升了约8%成功率（图3）。  
7. **PAIR实现了密集步骤级奖励，同时满足**：无外部LLM调用、无运行时真实答案依赖、无完整轨迹展开、无单独奖励模型训练。

## 7. 优点

- **洞察深刻**：首次系统揭示前缀污染下隐藏状态探测的信念一致性偏差，并指出注意力特征的互补性。  
- **设计简洁高效**：两阶段逻辑回归，离线单次训练，推理零额外成本，与GRPO天然兼容。  
- **实用性强**：消除了LLM-as-a-judge的API开销和树搜索的计算膨胀，适用于大规模在线RL。  
- **实验严谨**：对比方法全面（16种），消融充分，超参扫描清晰，统计报告规范（3种子）。  
- **跨域验证**：证明奖励信号的可迁移性，增强通用性说服力。

## 8. 不足与局限

- **模型单一**：仅实验了Qwen-2.5-7B-Instruct，未验证在更大模型（如70B、Llama系列）或不同注意力架构（GQA、滑动窗口）上的表现。  
- **数据集有限**：主要结果基于GTA和ToolBench（均为工具使用类任务），缺少更广泛的多步推理场景（如数学、代码生成）。  
- **二元标签**：离线训练使用二元正确性标签（正确/错误），忽略了部分正确或渐进式进步，更细粒度的监督信号可能进一步提升性能。  
- **固定超参**：动量奖励的α、温度T、裁剪ε等是在GTA上调优的，在其他数据集上可能需重新调整。  
- **RL训练开销未完整量化**：虽指出PAIR奖励成本极低，但GRPO全程（含前向生成）的总算力消耗未给出，与树搜索方法的绝对速度对比数据不够直观。  
- **双刃剑风险**：PAIR降低了开发强大代理的门槛，可能被滥用，文中提及但未做深入讨论。

（完）
