---
title: "LLMs as ASP Programmers: Self-Correction Enables Task-Agnostic Nonmonotonic Reasoning"
title_zh: 大语言模型作为 ASP 程序员：自我修正实现任务无关的非单调推理
authors: "Adam Ishay, Joohyung Lee"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27960v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 将自然语言转换为回答集程序以进行非单调推理
tldr: 本研究针对大语言模型在处理复杂逻辑和非单调推理时的局限性，提出了 LLM+ASP 框架。该框架将自然语言转化为回答集程序（ASP），利用其稳定模型语义处理默认规则和异常。通过引入基于求解器反馈的自动自我修正循环，该系统无需针对特定任务进行工程设计即可实现高性能。实验证明，该方法在多种推理任务中优于基于 SMT 的方案，展示了神经符号结合在处理人类认知中常见的可撤销推理方面的巨大潜力。
selection_source: fresh_fetch
motivation: 现有神经符号方法多依赖单调逻辑，难以处理人类认知中至关重要的非单调推理及高复杂度逻辑问题。
method: 提出一种通用的 LLM+ASP 框架，利用自动自我修正循环和紧凑的上下文指南，将自然语言转换为 ASP 代码进行求解。
result: 在六个基准测试中，该框架在非单调任务上显著优于 SMT 方案，且自我修正机制成为性能提升的核心驱动力。
conclusion: 紧凑的参考指南优于冗长文档，且自动迭代修正能有效替代手工领域知识，实现任务无关的逻辑推理。
---

## 摘要
最近的大语言模型（LLMs）在推理方面取得了令人瞩目的里程碑，但在高计算成本、逻辑不一致以及高复杂度问题上的性能急剧下降方面仍面临挑战。虽然神经符号方法试图通过将 LLMs 与符号推理器结合来缓解这些问题，但现有方法通常依赖于单调逻辑（如 SMT），无法表示可废止推理（defeasible reasoning）——这是人类认知的核心组成部分。我们提出了“LLM+ASP”，这是一个将自然语言转换为回答集程序设计（ASP）的框架，ASP 是一种基于稳定模型语义的非单调形式化方法。与以往需要手动编写知识模块、特定领域提示或仅限于单一问题类别评估的“LLM+ASP”方法不同，我们的框架无需任何针对特定任务的工程设计，并可统一应用于各种推理任务。我们的系统利用自动化自我修正循环，其中来自 ASP 求解器的结构化反馈可实现迭代优化。通过在六个不同的基准测试中进行评估，我们证明了：(1) 稳定模型语义允许 LLMs 自然地表达默认规则和异常，在非单调任务上显著优于基于 SMT 的替代方案；(2) 迭代自我修正是性能提升的主要驱动力，有效地取代了对手工领域知识的需求；(3) 紧凑的上下文参考指南显著优于冗长的文档，揭示了“上下文腐烂”（context rot）现象，即过多的上下文会阻碍对约束的遵守。

## Abstract
Recent large language models (LLMs) have achieved impressive reasoning milestones but continue to struggle with high computational costs, logical inconsistencies, and sharp performance degradation on high-complexity problems. While neuro-symbolic methods attempt to mitigate these issues by coupling LLMs with symbolic reasoners, existing approaches typically rely on monotonic logics (e.g., SMT) that cannot represent defeasible reasoning -- essential components of human cognition. We present "LLM+ASP," a framework that translates natural language into Answer Set Programming (ASP), a nonmonotonic formalism based on stable model semantics. Unlike prior "LLM+ASP" approaches that require manually authored knowledge modules, domain-specific prompts, or evaluation restricted to single problem classes, our framework operates without any per-task engineering and applies uniformly across diverse reasoning tasks. Our system utilizes an automated self-correction loop where structured feedback from the ASP solver enables iterative refinement. Evaluating across six diverse benchmarks, we demonstrate that: (1) stable model semantics allow LLMs to naturally express default rules and exceptions, outperforming SMT-based alternatives by significant margins on nonmonotonic tasks; (2) iterative self-correction is the primary driver of performance, effectively replacing the need for handcrafted domain knowledge; (3) compact in-context reference guides substantially outperform verbose documentation, revealing a "context rot" phenomenon where excessive context hinders constraint adherence.

---

## 论文详细总结（自动生成）

以下是对论文《LLMs as ASP Programmers: Self-Correction Enables Task-Agnostic Nonmonotonic Reasoning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：尽管大语言模型（LLMs）在推理任务上取得了进展，但在面对高复杂度逻辑问题时，仍存在计算成本高、逻辑不一致以及性能随复杂度增加而急剧下降的问题。
*   **核心动机**：现有的神经符号（Neuro-symbolic）方法大多依赖单调逻辑（如 SMT），无法处理人类认知中常见的“非单调推理”（即在获得新信息时撤回之前的结论，如处理默认规则和异常）。此外，现有方法通常需要大量针对特定任务的人工工程（如手动编写知识模块或 Few-shot 提示）。
*   **整体含义**：本文提出了 **LLM+ASP** 框架，旨在通过将自然语言转换为回答集程序（Answer Set Programming, ASP），利用其非单调特性和自动自我修正循环，实现一种无需针对特定任务进行工程设计的通用逻辑推理系统。

### 2. 方法论
*   **核心思想**：将 LLM 定位为“ASP 程序员”，负责将自然语言翻译为符号化的 ASP 代码，并利用 ASP 求解器的反馈进行迭代调试。
*   **关键技术细节**：
    *   **任务无关提示（Task-Agnostic Prompting）**：不使用特定任务的 Few-shot 示例，仅提供通用的 ASP 语法说明和问题描述。
    *   **紧凑参考指南（Compact Reference Guide）**：研究发现冗长的文档会导致“上下文腐烂（Context Rot）”，因此提炼了一份仅 2.6k token 的精简语法指南，重点介绍常用算子、池化（Pooling）和增量式规划。
    *   **自动自我修正循环**：
        1.  **生成**：LLM 生成 ASP 代码。
        2.  **求解**：使用符号求解器 `Clingo` 运行代码。
        3.  **反馈与修正**：如果代码有语法错误或逻辑矛盾（Unsat），将错误信息反馈给 LLM；如果产生结果，LLM 判断是否符合逻辑。LLM 决定是“UPDATE”（重写代码）还是“PASS”（通过）。
*   **逻辑基础**：利用 ASP 的**稳定模型语义（Stable Model Semantics）**，能够自然地表达“除非……否则……”这类非单调逻辑。

### 3. 实验设计
*   **数据集/场景**：
    *   **约束满足**：ZebraLogic (XL 和更难的 XXL 版本)、SudokuBench（非常规数独变体）。
    *   **规划任务**：Mystery Blocksworld（混淆后的积木世界）。
    *   **非单调推理**：MultiLogicNMR（包含默认推理和规则冲突）、BoardgameQA（包含矛盾信息和优先级推理）。
*   **对比方法（Baselines）**：
    *   **独立 LLM**：Gemini 2.5 Pro/Flash, OpenAI o4-mini, DeepSeek R1/V3。
    *   **LLM+SMT**：将框架中的 ASP 替换为 SMT（可满足性模理论）逻辑，以对比单调与非单调逻辑的效果。

### 4. 资源与算力
*   **算力设备**：实验在配备 Intel i9-9900K CPU 和 **NVIDIA GeForce RTX 2080 Ti GPU** 的机器上运行。需要注意的是，GPU 仅用于本地运行 `Clingo` 求解器，LLM 推理通过 API 完成。
*   **实验成本**：论文明确指出，核心实验的直接 API 费用约为 **$3,800**，包括前期测试和失败运行在内的总支出估计为 **$4,200**。
*   **时间设置**：最大修正次数设为 10 次，求解器超时限制为 80 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的基准测试上进行了评估，涵盖了从逻辑谜题到复杂规划的多种维度。
*   **消融实验**：
    *   对比了“无参考”、“标准参考（22k token）”与“紧凑参考（2.6k token）”的效果。
    *   对比了“有无自我修正循环”对性能的影响。
    *   对比了不同参数规模和类型的模型（推理模型 vs 指令模型）。
*   **充分性评价**：实验设计非常充分且客观。通过引入 ZL-XXL 等极高难度数据集，清晰地展示了 LLM 在复杂度临界点之后的表现，并公平地对比了 SMT 方案在非单调任务上的天然劣势。

### 6. 主要结论与发现
*   **性能大幅提升**：LLM+ASP 的平均准确率达到 78.0%，相比独立 LLM（51.3%）有显著提升，尤其在极难问题（如 12x12 逻辑谜题）上，独立 LLM 准确率为 0，而该框架表现优异。
*   **非单调性优势**：在非单调推理任务（MultiLogicNMR）中，ASP 方案准确率（约 90%）远超 SMT 方案（约 35%），证明了稳定模型语义在处理复杂逻辑冲突时的必要性。
*   **修正循环是核心**：自我修正机制使性能从 54.6% 提升至 78.0%，是框架成功的关键驱动力。
*   **上下文腐烂现象**：更短、更精炼的参考指南效果反而更好，过多的上下文会干扰模型对核心约束的关注。

### 7. 优点
*   **通用性强**：实现了真正的“任务无关”，无需为每个新领域手动调整提示词。
*   **处理非单调性**：填补了神经符号推理在处理可撤销逻辑（Defeasible Reasoning）方面的空白。
*   **Token 效率**：相比于让 LLM 直接在思维链中进行搜索，生成紧凑的符号代码并由求解器执行更加高效且严谨。

### 8. 不足与局限
*   **模型能力依赖**：框架效果高度依赖底层 LLM 的代码生成和理解反馈的能力，非推理模型（如 DeepSeek V3）表现较差。
*   **Unsat 调试困境**：当 ASP 程序不可满足（Unsat）时，求解器提供的反馈非常有限，LLM 往往难以定位是哪条规则导致了冲突。
*   **成本与延迟**：多次迭代修正虽然提高了准确率，但也带来了更高的 API 成本和推理延迟。

（完）
