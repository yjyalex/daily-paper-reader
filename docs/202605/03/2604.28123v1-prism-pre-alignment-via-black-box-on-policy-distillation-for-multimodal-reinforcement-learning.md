---
title: "PRISM: Pre-alignment via Black-box On-policy Distillation for Multimodal Reinforcement Learning"
title_zh: PRISM：通过黑盒同策略蒸馏进行多模态强化学习的预对齐
authors: "Sudong Wang, Weiquan Huang, Xiaomin Yu, Zuhao Yang, Hehai Lin, Keming Wu, Chaojun Xiao, Chen Chen, Wenxuan Wang, Beier Zhu, Yunjian Zhang, Chengwei Qin"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28123v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 多模态推理中带有可验证奖励的强化学习
tldr: 针对大语言模型在监督微调（SFT）到强化学习（RLVR）过程中存在的分布偏移问题，本文提出了PRISM框架。该框架在SFT和RLVR之间引入了预对齐阶段，通过黑盒在线策略蒸馏（OPD）和混合专家（MoE）判别器，分别纠正感知和推理误差。结合高质量的Gemini 3 Flash数据集，PRISM显著提升了Qwen3-VL在多模态基准测试中的表现，为多模态强化学习提供了更稳健的初始化。
selection_source: fresh_fetch
motivation: 传统的SFT过程会导致分布偏移，且多模态推理中的感知与推理错误会进一步放大这种偏移，影响后续强化学习的效果。
method: 提出PRISM三阶段流程，在SFT后引入基于MoE判别器的黑盒在线策略蒸馏，通过解耦的感知与推理信号引导模型对齐。
result: 在Qwen3-VL模型上，PRISM使4B和8B版本的平均准确率分别比传统SFT-to-RLVR基准提升了4.4和6.0个百分点。
conclusion: PRISM通过在强化学习前进行显式的分布对齐，有效缓解了多模态模型训练中的漂移问题，显著增强了下游任务的性能。
---

## 摘要
大型多模态模型（LMMs）的标准后训练方案是在精选演示上进行监督微调（SFT），随后进行具有可验证奖励的强化学习（RLVR）。然而，SFT 会引入分布偏移，既无法保留模型的原始能力，也无法忠实地匹配监督分布。这一问题在多模态推理中被进一步放大，其中感知错误和推理失败遵循不同的偏移模式，并在随后的强化学习过程中复合。我们提出了 PRISM，这是一个三阶段流水线，通过在 SFT 和 RLVR 之间插入一个显式的分布对齐阶段来缓解这种偏移。基于同策略蒸馏（OPD）的原理，PRISM 将对齐建模为策略与具有专门感知和推理专家的混合专家（MoE）判别器之间的黑盒、响应级对抗博弈，提供解耦的纠正信号，在无需访问教师模型 Logits 的情况下引导策略向监督分布靠拢。虽然 126 万个公开演示足以进行广泛的 SFT 初始化，但分布对齐需要更高保真度的监督；因此，我们从 Gemini 3 Flash 中精选了 11.3 万个额外的演示，其特点是针对最难的未解决问题具有密集的视觉定位和逐步推理。在 Qwen3-VL 上的实验表明，PRISM 在多种强化学习算法（GRPO、DAPO、GSPO）和不同的多模态基准测试中一致地提高了下游 RLVR 的性能，在 4B 和 8B 模型上分别比 SFT-to-RLVR 基准提高了 4.4 和 6.0 个百分点的平均准确率。我们的代码、数据和模型权重已在 https://github.com/XIAO4579/PRISM 公开发布。

## Abstract
The standard post-training recipe for large multimodal models (LMMs) applies supervised fine-tuning (SFT) on curated demonstrations followed by reinforcement learning with verifiable rewards (RLVR). However, SFT introduces distributional drift that neither preserves the model's original capabilities nor faithfully matches the supervision distribution. This problem is further amplified in multimodal reasoning, where perception errors and reasoning failures follow distinct drift patterns that compound during subsequent RL. We introduce PRISM, a three-stage pipeline that mitigates this drift by inserting an explicit distribution-alignment stage between SFT and RLVR. Building on the principle of on-policy distillation (OPD), PRISM casts alignment as a black-box, response-level adversarial game between the policy and a Mixture-of-Experts (MoE) discriminator with dedicated perception and reasoning experts, providing disentangled corrective signals that steer the policy toward the supervision distribution without requiring access to teacher logits. While 1.26M public demonstrations suffice for broad SFT initialization, distribution alignment demands higher-fidelity supervision; we therefore curate 113K additional demonstrations from Gemini 3 Flash, featuring dense visual grounding and step-by-step reasoning on the hardest unsolved problems. Experiments on Qwen3-VL show that PRISM consistently improves downstream RLVR performance across multiple RL algorithms (GRPO, DAPO, GSPO) and diverse multimodal benchmarks, improving average accuracy by +4.4 and +6.0 points over the SFT-to-RLVR baseline on 4B and 8B, respectively. Our code, data, and model checkpoints are publicly available at https://github.com/XIAO4579/PRISM.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《PRISM: Pre-alignment via Black-box On-policy Distillation for Multimodal Reinforcement Learning》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注大型多模态模型（LMMs）在后训练（Post-training）过程中的**分布偏移（Distributional Drift）**问题。
*   **背景**：标准的流程是“监督微调（SFT）→ 强化学习（RLVR）”。
*   **问题**：SFT 阶段模型仅通过 Token 级别的模仿学习，往往只学会了表面模式而偏离了原始分布，且无法保证推理的忠实性。在多模态场景下，**感知错误**（看错了）和**逻辑错误**（算错了）会交织并放大，导致后续 RL 阶段的初始化状态极差。
*   **核心目标**：在 SFT 和 RL 之间插入一个“预对齐”阶段，通过纠正分布偏移，为强化学习提供一个更高质量、更稳健的起点。

### 2. 方法论：核心思想与关键技术
PRISM 提出了一个三阶段流水线，核心创新在于第二阶段的**黑盒同策略蒸馏（Black-box On-policy Distillation, OPD）**。

*   **核心思想**：将对齐建模为策略模型与判别器之间的对抗博弈。判别器学习区分“模型自生成的响应”与“高质量监督数据”，策略模型则通过强化学习算法（如 GRPO）来最大化判别器给出的奖励。
*   **关键技术细节**：
    *   **混合专家（MoE）判别器**：为了处理多模态错误的异质性，设计了两个专门的专家：
        *   **感知专家 ($D_v$)**：检查视觉定位（Visual Grounding）的准确性。
        *   **推理专家 ($D_r$)**：检查逻辑推导的一致性。
    *   **黑盒对齐**：不需要访问教师模型的 Logits（概率分布），仅需响应文本即可进行蒸馏，适用于闭源模型（如 Gemini）作为教师的情况。
    *   **对抗训练流程**：
        1.  使用 Bradley-Terry 损失函数更新判别器，使其能区分优劣。
        2.  使用 GRPO 算法更新策略模型，不使用 KL 散度约束（为了彻底纠正 SFT 的偏移）。
        3.  奖励函数：$r = \alpha \cdot D_v + (1-\alpha) \cdot D_r$，实现感知与推理信号的解耦。

### 3. 实验设计
*   **基准模型**：Qwen3-VL-4B 和 Qwen3-VL-8B。
*   **数据集**：
    *   **数学推理**：MathVista, MathVerse, MathVision, WeMath。
    *   **通用理解**：MMMU, MMMU-Pro, HallusionBench。
*   **对比方法**：
    *   Instruct 原版模型。
    *   仅 SFT 后的模型。
    *   标准的 SFT → RLVR（使用 GRPO, DAPO, GSPO 等不同算法作为 Baseline）。
*   **数据来源**：自研 113K 高质量多模态推理语料（源自 Gemini 3 Flash），包含密集的视觉描述和推理链。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA H100-SXM5-80GB** GPU。
*   **训练配置**：
    *   **SFT**：1 epoch。
    *   **PRISM 对齐**：500 steps。
    *   **RLVR**：1500 steps。
    *   **推理引擎**：使用 vLLM 提高吞吐量，框架基于 veRL。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个模型尺度（4B/8B）上进行了验证，并跨越了 7 个主流多模态基准测试。
*   **算法覆盖**：验证了 PRISM 在三种不同的 RL 算法（GRPO, DAPO, GSPO）下的普适性。
*   **消融实验**：非常充分。分别验证了：
    1.  MoE 判别器 vs 单体判别器的优劣。
    2.  是否有视觉输入对判别器的影响（证明了“看图”的重要性）。
    3.  三阶段中缺失任何一阶段的效果（证明了 SFT 提供基础、PRISM 修正分布、RLVR 优化结果的必要性）。
*   **客观性**：通过控制训练步数和 Token 预算，确保了与 Baseline 的公平对比。

### 6. 主要结论与发现
*   **性能提升**：PRISM + GRPO 在 4B 模型上平均提升 **4.4%**，在 8B 模型上提升 **6.0%**。
*   **分布修复**：实验证明对齐阶段虽然不会直接大幅提升准确率，但它使模型的输出结构（如推理步数、描述项数量）更接近专家分布，从而释放了下游 RL 的潜力。
*   **Token 效率**：PRISM 训练后的模型在获得更高准确率的同时，生成的响应往往更简洁（Token 数更少）。
*   **模型越强偏移越重**：发现 8B 模型受 SFT 偏移的影响比 4B 更严重，而 PRISM 的修复效果也更显著。

### 7. 优点（亮点）
*   **解耦反馈**：通过 MoE 判别器解决了多模态模型中“感知”与“推理”错误难以同时纠正的痛点。
*   **黑盒友好**：不依赖 Logits 的设计使其能利用最先进的闭源模型（如 Gemini 3）进行知识蒸馏。
*   **即插即用**：作为一个中间对齐层，它可以适配任何下游强化学习算法。

### 8. 不足与局限
*   **计算开销**：增加了一个额外的对抗训练阶段，且需要同时维护策略模型和 MoE 判别器，对显存和算力有一定要求。
*   **格式依赖**：目前的方法高度依赖于结构化的输出格式（如 XML 标签 `<caption>`, `<think>`），对于非结构化或自由形式的对话可能需要重新设计。
*   **评估指标**：对分布对齐的分析主要依赖于结构代理指标（如推理步数），缺乏更直接的、模型无关的分布距离度量。

（完）
