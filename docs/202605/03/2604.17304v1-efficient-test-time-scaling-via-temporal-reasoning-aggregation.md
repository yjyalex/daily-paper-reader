---
title: Efficient Test-Time Scaling via Temporal Reasoning Aggregation
title_zh: 通过时间推理聚合实现高效的测试时扩展
authors: "Jiakun Li, Xingwei He, Kefan Li, Hongzheng Chai, Hongyue Yu, Yuan Yuan"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17304v1"
tags: ["query:llm-fo"]
score: 8.0
evidence: 通过时间聚合权衡测试时缩放与推理效率
tldr: "针对大语言模型在测试时缩放中存在的“过度思考”和 Token 浪费问题，本文提出了一种无需训练的框架 TRACE。该方法摒弃了不可靠的单步置信度信号，转而通过聚合多步推理过程中的答案一致性和置信度轨迹来检测推理收敛。实验表明，TRACE 在保持与全长推理相当的准确率的同时，能显著减少 25-30% 的 Token 消耗，实现了推理性能与效率的优化平衡。"
selection_source: carryover_cache
motivation: 现有的测试时缩放方法常因单步置信度信号不可靠而导致模型过度推理，造成严重的计算资源浪费。
method: 提出 TRACE 框架，利用时间维度聚合的答案一致性和置信度轨迹信号来动态判断推理是否收敛并及时终止。
result: "在多个挑战性基准测试中，TRACE 平均减少了 25-30% 的推理 Token 使用，且准确率损失控制在 1-2% 以内。"
conclusion: 通过多步证据的时间聚合而非瞬时信号，可以更精准地识别推理收敛点，从而在保证性能的同时大幅提升推理效率。
---

## 摘要
测试时扩展（Test-time scaling）提升了大语言模型的推理性能，但往往会导致 Token 效率低下的“过度思考”（overthinking），即模型在得出正确答案后仍继续进行不必要的推理。现有的动态早期退出（early-exit）方法通常依赖于单步置信度信号，这在多步推理场景中往往难以可靠地检测推理收敛。为了缓解这一局限性，我们提出了 TRACE，这是一个无需训练的高效测试时扩展框架，它基于多步证据的时间聚合而非瞬时信号来确定何时终止推理。TRACE 通过聚合近期推理步骤中的两个互补信号来检测推理随时间的收敛性：答案一致性（捕获预测答案的持久性）和置信度轨迹（建模模型置信度随时间的演变）。得益于这两个因素，TRACE 能够准确判断推理过程是否已经收敛，从而及时停止推理并有效避免冗余的推理步骤。在多个具有挑战性的基准测试上的广泛实验表明，TRACE 在保持准确率与全长度推理相比差距在 1-2% 以内的同时，平均减少了 25-30% 的推理 Token 使用量，持续优于现有的动态推理方法。

## Abstract
Test-time scaling improves the reasoning performance of large language models but often results in token-inefficient overthinking, where models continue reasoning beyond what is necessary for a correct answer. Existing dynamic early-exit methods typically rely on single-step confidence signals, which are often unreliable for detecting reasoning convergence in multi-step settings. To mitigate this limitation, we propose TRACE, a training-free framework for efficient test-time scaling that determines when to terminate reasoning based on temporal aggregation of multi-step evidence rather than instantaneous signals. TRACE detects reasoning convergence over time by aggregating two complementary signals across recent reasoning steps: answer consistency, capturing the persistence of predicted answers, and confidence trajectory, modeling the temporal evolution of model confidence. Benefiting from these two factors, TRACE can accurately determine whether the reasoning process has converged, thereby promptly halting inference and effectively avoiding redundant reasoning steps. Extensive experiments on multiple challenging benchmarks show that TRACE reduces reasoning token usage by 25-30% on average while maintaining accuracy within 1-2% of full-length reasoning, consistently outperforming existing dynamic reasoning methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Test-Time Scaling via Temporal Reasoning Aggregation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）推理能力的提升，通过增加推理长度（Test-time scaling）来换取准确率已成为主流。然而，这导致了“过度思考”（Overthinking）现象，即模型在得出正确答案后仍生成大量冗余 Token，造成计算资源和 API 成本的浪费。
*   **核心问题**：现有的动态早期退出（Early-exit）方法主要依赖“单步置信度”信号。研究发现，单步信号在多步推理中极不可靠，容易因瞬时的高置信度错误而导致过早退出，或因信号波动导致延迟退出。

### 2. 论文提出的方法论：TRACE
TRACE（Temporal Reasoning Aggregation for Convergent Exit）是一种**无需训练**的推理框架，核心思想是通过**时间维度上的多步证据聚合**来判断推理是否收敛。
*   **核心流程**：
    1.  **滑动窗口机制**：维护一个包含最近 $k$ 个推理步骤的窗口。
    2.  **答案一致性得分 (ACS)**：在每个步骤诱导出一个候选答案，计算该答案在窗口内出现的频率。频率越高，说明推理越趋于稳定。
    3.  **置信度轨迹得分 (CTS)**：利用 Token 级别的归一化熵计算每个答案的置信度，并取窗口内该答案置信度的平均值，以平滑单步噪声。
    4.  **综合判定**：通过公式 $S(a) = \alpha \cdot ACS(a) + (1 - \alpha) \cdot CTS(a)$ 计算稳定性得分。当得分超过阈值 $\tau$ 时，立即终止推理并输出答案。
*   **技术细节**：采用轻量级的辅助提示（Auxiliary Prompting）进行答案诱导，不中断主推理流。

### 3. 实验设计
*   **数据集**：涵盖了极具挑战性的数学和科学推理基准，包括 **OlympiadBench、MATH500、AIME24、AIME25、AMC23** 以及科学领域的 **GPQA-D**。
*   **对比方法 (Benchmarks)**：
    *   **Vanilla**：全长度推理（基准上限）。
    *   **NoThinking**：抑制推理过程的直接输出。
    *   **TALE**：基于固定 Token 预算的限制方法。
    *   **Dynasor / DEER**：现有的基于单步信号或探测器的动态退出方法。
*   **测试模型**：Qwen3-8B/4B、Gemini-2.5-Flash、DeepSeek-R1-Distill-Llama-8B。

### 4. 资源与算力
*   **训练算力**：论文明确指出 TRACE 是 **Training-free（无需训练）** 的，因此不涉及模型训练的 GPU 资源消耗。
*   **推理开销**：论文分析了辅助答案诱导的开销，结果显示诱导 Token 仅占总推理 Token 的 **2.23%**，相比其节省的 25-30% Token，该开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同架构和规模的模型（涵盖开源与闭源）以及 6 个主流基准测试上进行了广泛实验。
*   **消融实验**：针对窗口大小 $k$、混合权重 $\alpha$、停止阈值 $\tau$ 以及 ACS/CTS 各组件的贡献进行了详细的消融分析。
*   **评价**：实验设计客观、公平，通过跨模型和跨领域的验证，证明了该方法在不同场景下的鲁棒性。

### 6. 主要结论与发现
*   **高效性**：TRACE 平均减少了 **25-30%** 的推理 Token 使用量。
*   **性能保持**：在大幅减产 Token 的前提下，准确率与全长度推理相比下降仅在 **1-2%** 以内，显著优于其他早期退出基线。
*   **鲁棒性**：相比单步置信度方法，TRACE 对阈值设置不敏感，能够更稳健地识别真正的推理收敛点，避免被瞬时的高置信度错误诱导。

### 7. 优点
*   **即插即用**：无需对原模型进行任何微调或参数修改，适用于任何现成的 LLM。
*   **多步聚合思路**：创新性地引入时间维度信号，解决了推理过程中单步置信度“虚高”或“波动”的痛点。
*   **通用性强**：不仅在数学任务上表现优异，在 GPQA-D 等科学推理任务上也展现了良好的泛化能力。

### 8. 不足与局限
*   **领域局限**：实验主要集中在文本形式的数学和科学推理，尚未在多模态（如视觉-语言推理）场景下进行验证。
*   **步分割依赖**：方法依赖于对推理步骤的准确分割（基于话语标记），对于推理格式极不规范的模型，效果可能会受到影响。
*   **诱导依赖**：虽然开销小，但仍需模型具备一定的指令遵循能力以提取中间答案。

（完）
