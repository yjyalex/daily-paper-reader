---
title: "Tandem: Riding Together with Large and Small Language Models for Efficient Reasoning"
title_zh: Tandem：大语言模型与小语言模型协同实现高效推理
authors: "Zichuan Fu, Xian Wu, Guojing Li, Yejing Wang, Yijun Chen, Zihao Zhao, Yixuan Luo, Hanyu Yan, Yefeng Zheng, Xiangyu Zhao"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23623v1"
tags: ["query:llm-fo"]
score: 8.0
evidence: 协同大小模型以在降低计算成本的同时实现高质量推理
tldr: "针对大语言模型（LLM）在推理任务中计算开销巨大的问题，本文提出 Tandem 协作框架。该框架由 LLM 担任协调者生成关键推理见解，并引导小语言模型（SLM）完成具体推理与回答。通过引入成本感知的终止机制，Tandem 在保持甚至提升推理性能的同时，将计算成本降低了约 40%，为高效推理提供了新思路。"
selection_source: fresh_fetch
motivation: 链式推理虽然提升了 LLM 的性能，但长序列生成带来了沉重的计算负担。
method: 提出 Tandem 框架，利用 LLM 生成关键推理见解并引导 SLM 执行，同时采用自适应终止机制提前停止 LLM 生成。
result: "在数学推理和代码生成任务中，该方法在保持高性能的同时，比独立 LLM 推理降低了约 40% 的计算成本。"
conclusion: Tandem 证明了大小模型协同工作能有效平衡推理质量与计算效率，且其终止机制具有良好的跨领域迁移性。
---

## 摘要
近期大语言模型（LLMs）的进展催生了推理密集型推理范式的兴起，模型在生成最终答案之前会进行显式的逐步推理。虽然此类方法提高了答案质量和可解释性，但由于生成序列较长，它们也带来了巨大的计算开销。在本文中，我们提出了 Tandem，这是一种新型协作框架，它协同大语言模型和小语言模型（LLMs 和 SLMs），以显著降低的计算成本实现高质量推理。具体而言，LLM 充当战略协调者，高效地生成一组紧凑的关键推理见解。随后，这些见解被用于引导更小、更高效的 SLM 执行完整的推理过程并给出最终响应。为了平衡效率和可靠性，Tandem 引入了一种成本感知终止机制，该机制能够自适应地确定何时已积累了足够的推理引导，从而实现 LLM 生成的提前停止。在数学推理和代码生成基准测试上的实验表明，与独立 LLM 推理相比，Tandem 将计算成本降低了约 40%，同时实现了更优或具有竞争力的性能。此外，在某一领域训练的充分性分类器可以有效地迁移到其他领域，而无需重新训练。代码可在以下网址获取：https://github.com/Applied-Machine-Learning-Lab/ACL2026_Tandem。

## Abstract
Recent advancements in large language models (LLMs) have catalyzed the rise of reasoning-intensive inference paradigms, where models perform explicit step-by-step reasoning before generating final answers. While such approaches improve answer quality and interpretability, they incur substantial computational overhead due to the prolonged generation sequences. In this paper, we propose Tandem, a novel collaborative framework that synergizes large and small language models (LLMs and SLMs) to achieve high-quality reasoning with significantly reduced computational cost. Specifically, the LLM serves as a strategic coordinator, efficiently generating a compact set of critical reasoning insights. These insights are then used to guide a smaller, more efficient SLM in executing the full reasoning process and delivering the final response. To balance efficiency and reliability, Tandem introduces a cost-aware termination mechanism that adaptively determines when sufficient reasoning guidance has been accumulated, enabling early stopping of the LLM's generation. Experiments on mathematical reasoning and code generation benchmarks demonstrate that Tandem reduces computational costs by approximately 40% compared to standalone LLM reasoning, while achieving superior or competitive performance. Furthermore, the sufficiency classifier trained on one domain transfers effectively to others without retraining. The code is available at: https://github.com/Applied-Machine-Learning-Lab/ACL2026_Tandem.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tandem: Riding Together with Large and Small Language Models for Efficient Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何保留大语言模型（LLM）在复杂推理任务中的“思维链（CoT）”优势，同时解决其因生成序列过长而导致的巨大计算开销和高延迟问题？
*   **研究背景**：当前的推理型模型（如 DeepSeek-R1）通过显式的逐步推理显著提升了性能，但其推理链往往长达数千个 token，导致推理成本比传统模型高出 5-10 倍。
*   **整体含义**：论文提出了一种“导师-实习生（Mentor-Intern）”协作模式，让 LLM 仅负责生成高层次的战略指导，而让更轻量的小模型（SLM）负责执行具体的推理细节，从而在保证质量的前提下大幅提升效率。

### 2. 方法论
Tandem 框架的核心思想是将推理过程解构为“见解生成”与“响应完成”两个阶段：
*   **思维见解生成（Thinking Insight Generation）**：LLM 不再生成完整的推理链，而是生成四个关键模块：
    1.  **Goal（目标）**：明确任务目标与约束。
    2.  **Planning（规划）**：制定高层解题策略。
    3.  **Retrieval（检索）**：提取相关公式、定理或知识。
    4.  **Action（行动）**：执行具体的逻辑步骤。
*   **成本感知持续判断（Cost-Aware Continual Judgment）**：
    *   **分阶段生成**：将 LLM 的引导分为低、中、高三个努力水平（token 预算递增）。
    *   **置信度度量**：利用 SLM 处理当前引导时的 **困惑度（Perplexity）** 和 **熵（Entropy）** 作为信号。
    *   **充分性分类器**：训练一个轻量级的 MLP 分类器，根据 SLM 的输出分布特征判断当前的引导是否“足够”。如果足够，则提前终止 LLM 生成，由 SLM 完成后续回答。
*   **响应完成（Response Completion）**：SLM 根据 LLM 提供的结构化见解，生成最终的推理步骤和答案。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：MATH（包含 7 个学科、5 个难度等级）、GSM8K。
    *   **代码生成**：HumanEval（用于验证跨领域迁移能力）。
*   **对比方法（Benchmarks）**：
    *   **独立模型**：单独使用 7B 或 32B 模型。
    *   **固定预算协作**：固定 LLM 生成 100/500/1000 token 的引导。
    *   **效率基准**：Budget Forcing（强制截断推理链）、LLM Cascade（简单的二元路由选择）。
*   **模型组合**：主要使用 DeepSeek-R1-Distill-Qwen (7B/32B) 和 Qwen3 (8B/32B)，并测试了与 GPT-4o-mini 等 API 模型的协作。

### 4. 资源与算力
*   **模型推理**：实验使用了 vLLM 推理框架。
*   **分类器训练**：分类器是一个极小的 MLP（隐藏层为 64 和 32），训练开销极低。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从方法论看，该框架不需要对 LLM 进行微调，仅需在 SLM 的输出特征上训练微型分类器，算力需求远低于传统的强化学习微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数学、代码两大领域，进行了跨模型家族（DeepSeek 与 Qwen3 互换角色）、跨模型规模（1.5B 到 32B 的多种组合）的广泛测试。
*   **消融与分析**：
    *   对比了不同特征输入（统计特征 vs. 隐藏状态）对分类器性能的影响。
    *   分析了引导长度与准确率的 Scaling 关系。
    *   进行了跨领域零样本迁移实验（在 MATH 上训练，直接在 HumanEval 上测试）。
*   **评价**：实验设计较为全面且客观，通过 TFLOPs 近似计算成本，并对比了端到端延迟，充分验证了效率提升。

### 6. 主要结论与发现
*   **性能超越单体模型**：在 MATH 任务中，32B+7B 的 Tandem 协作模式比纯 32B 模型准确率高出 **2.56%**。
*   **成本大幅降低**：在提升性能的同时，计算成本（TFLOPs）降低了约 **40%**，推理延迟缩短了约 **1.8 倍**。
*   **跨领域迁移性强**：在数学领域训练的充分性分类器，无需重训即可在代码生成任务中有效工作，证明了 SLM 的置信度信号具有通用性。
*   **能力匹配效应**：协作效果取决于模型间的差距。如果 SLM 太弱，无法理解 LLM 的引导；如果两者能力太近，则冗余度高，提升有限。

### 7. 优点与亮点
*   **自适应性**：通过持续判断机制，实现了“简单问题少思考，复杂问题多引导”的动态资源分配。
*   **非侵入性**：无需修改 LLM 的权重，对闭源 API 模型同样适用，具有极强的工程实用性。
*   **结构化引导**：将冗长的 CoT 压缩为四个核心见解模块，既降低了 SLM 的理解难度，又减少了 token 消耗。

### 8. 不足与局限
*   **领域覆盖有限**：虽然验证了数学和代码，但在常识推理、开放式问答或多模态任务上的表现尚不明确。
*   **分类器依赖标签**：虽然分类器很轻量，但仍需要一定量的标注数据（正确/错误标签）来确定“充分性”阈值。
*   **SLM 瓶颈**：如果 SLM 本身存在严重的理解偏差，LLM 的高层引导可能无法被正确执行，存在“带不动”的风险。

（完）
