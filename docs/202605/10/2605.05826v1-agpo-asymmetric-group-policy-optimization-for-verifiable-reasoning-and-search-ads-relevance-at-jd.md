---
title: "AGPO: Asymmetric Group Policy Optimization for Verifiable Reasoning and Search Ads Relevance at JD"
title_zh: AGPO：用于京东可验证推理与搜索广告相关性的非对称组策略优化
authors: "Yang Xu, Kun Yao, Yiming Deng, Zheng Fang, Kai Ming Ting, Ming Pang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05826v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 具有可验证奖励的强化学习用于推理
tldr: 针对大语言模型在强化学习（RLVR）过程中出现的推理边界收缩问题，京东提出了非对称组策略优化（AGPO）。该方法通过负向主导策略抑制错误路径以维持探索能力，并利用组优势机制根据组内方差调整正向更新，使模型能聚焦于稀有的正确路径。实验证明 AGPO 在数学基准测试中达到 SOTA，并显著提升了搜索广告相关性优化的工业应用效果。
selection_source: fresh_fetch
motivation: 现有的 RLVR 方法虽然提高了采样效率，但会导致模型推理边界收缩，在大样本量下的覆盖率反而不如基座模型。
method: 提出 AGPO 算法，采用负向主导策略抑制错误路径，并结合组优势机制对正向更新进行加权，以聚焦于稀有的正确推理路径。
result: "在五个数学基准测试中取得 SOTA 准确率，并有效提升了 pass@k 性能，同时在京东搜索广告相关性任务中显著增强了数据标注质量。"
conclusion: AGPO 通过非对称优化策略有效解决了强化学习中的边界收缩问题，在复杂推理和工业大规模应用中均表现出优越性。
---

## 摘要
具有可验证奖励的强化学习（RLVR）在提升大语言模型（LLM）的推理性能方面取得了显著成功。然而，最近的研究表明，虽然当前的 RLVR 方法提高了正确路径的采样效率，但它们并未诱发出根本性的新推理模式。相反，与基座模型相比，训练后模型的推理能力边界往往会缩小，基座模型在大样本量下表现出更高的覆盖率。在这项工作中，我们提出了非对称组策略优化（AGPO）来对抗这种边界收缩。AGPO 采用负样本主导的强化策略来抑制错误的推理路径，从而保持基座模型的探索能力。对于正向强化，AGPO 采用了一种组优势机制，该机制根据组内方差缩放正向更新，使模型能够专注于稀有的正确路径，同时抑制来自平凡路径的更新。我们在五个数学基准测试上的实验表明，AGPO 在实现最先进准确率的同时，在大规模下持续提升了 pass@$k$ 性能。在搜索广告相关性优化的大规模工业应用中，AGPO 有效提升了数据标注的质量，从而使下游学生模型获得了显著的性能提升。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has demonstrated notable success in enhancing the reasoning performance of large language models (LLMs). However, recent studies reveal that while current RLVR methods improve sampling efficiency towards correct paths, they do not elicit fundamentally new reasoning patterns. Instead, the reasoning capability boundary of trained models often narrows compared to their base models, with base models achieving higher coverage at large sample sizes. In this work, we propose Asymmetric Group Policy Optimization (AGPO) to counteract this boundary shrinkage. AGPO adopts a negative-dominant reinforcement strategy to suppress incorrect reasoning paths, maintaining the base model's exploration capacity. For positive reinforcement, AGPO adopts a group advantage mechanism, which scales positive updates based on intra-group variance, allowing the model to focus on rare correct paths while suppressing updates from trivial paths. Our experiments on five mathematical benchmarks demonstrate that AGPO achieves state-of-the-art accuracy while consistently improving pass@$k$ performance at scale. In a large-scale industrial application for search ads relevance optimization, AGPO effectively enhances the quality of the data annotation, leading to substantial performance gains in downstream student models.

---

## 论文详细总结（自动生成）

这篇论文由京东（JD.com）与南京大学、北京大学的研究人员共同完成，提出了一种名为 **AGPO（非对称组策略优化）** 的强化学习算法，旨在解决大语言模型（LLM）在推理任务中常见的“推理边界收缩”问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的“具有可验证奖励的强化学习”（RLVR，如 DeepSeek-R1、OpenAI-o1 使用的技术）虽然能显著提升模型在推理任务中的单次尝试准确率（Pass@1），但往往会导致**推理能力边界收缩**。
*   **研究背景**：研究发现，标准 RL 算法（如 PPO、GRPO）倾向于让模型过度拟合基座模型中已有的简单正确路径，而牺牲了对复杂、稀有路径的探索。这导致模型在大样本采样下的覆盖率（Pass@k）甚至不如基座模型。
*   **研究动机**：如何既能提升模型的贪婪解码准确率，又能保留甚至扩展基座模型的推理潜力（即保持高 Pass@k）。

### 2. 方法论：核心思想与关键技术
AGPO 的核心思想是**非对称的优势估计**，通过“负样本主导”和“动态正向强化”来平衡探索与利用。
*   **负样本主导强化（NSR-Dominant）**：将抑制错误路径作为主要驱动力。相比于强行拉高正确路径的概率（容易导致分布坍缩），抑制错误路径能更温和地重新分配概率质量，保留模型的原始知识。
*   **动态组优势估计（Dynamic Advantage）**：
    *   **公式逻辑**：优势函数由两部分组成：$\hat{A}_i = \frac{1}{\sqrt{\sigma^2 + \delta^2}} (r_i - \mu) + I(r_i < 0) \cdot R$。
    *   **关键参数**：$\delta$ 是方差约束因子，防止输出分布过度尖锐化；$R$ 是门控负向项，确保对错误路径有稳定的惩罚。
*   **稀有路径强化**：通过组内方差调节，当组内正确样本很少时（稀有路径），赋予更高的正向奖励；当组内样本大多正确时（平凡路径），减小更新幅度，避免过度强化简单任务。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：MATH、Olympiad（奥数）、AIME-2024、AIME-2025、GaoKao-2024（高考）。
    *   **工业应用**：京东搜索广告相关性任务（Query-SKU 语义匹配）。
*   **基座模型**：Qwen2.5-Math-7B、Qwen3-4B（非思考模式）、Llama-3.1-8B-Instruct。
*   **对比方法**：PPO、GRPO、REINFORCE、W-REINFORCE（加权强化学习）。
*   **评估指标**：Pass@k（k 从 1 到 256）、PIR（不相关产品比例）、线上 A/B 测试指标（CTR、CPM、GMV）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）或具体的显卡数量。
*   **训练细节**：提到了使用 VeRL 框架，训练 Epoch 为 20，总 Batch Size 为 1024，微批次（Mini-batch）为 256，每个 Prompt 采样 8 个响应（G=8）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 参数量的三个主流模型系列，并在 5 个难度迥异的数学基准上进行了测试。
*   **消融实验**：详细探讨了约束因子 $\delta$ 和 KL 系数 $\beta$ 对性能的影响。
*   **充分性评价**：实验设计非常充分且具有说服力。不仅有学术界的 Benchmark，还包含了大规模工业环境下的端到端验证（从教师模型标注到学生模型蒸馏，再到线上 A/B 实验），证明了算法的实用性。

### 6. 主要结论与发现
*   **性能领先**：AGPO 在所有数学基准上的 Pass@1 均达到 SOTA，且在 $k=256$ 时表现出极强的鲁棒性，有效缓解了边界收缩。
*   **熵值保持**：相比 PPO 和 GRPO，AGPO 在训练过程中能维持更高的策略熵，这意味着模型保留了更好的探索能力。
*   **工业增益**：在京东搜索广告中，AGPO 优化的教师模型提升了标注质量，使下游 BERT 学生模型在实际线上流量中实现了 **+0.22% 的 CTR** 和 **+0.50% 的 CPM** 增长。

### 7. 优点
*   **理论深刻**：敏锐捕捉到了 RLVR 中“识别并放大”机制导致的负面效应（边界收缩），并给出了针对性方案。
*   **非对称设计**：通过解耦正负反馈，解决了标准 RL 在推理任务中容易出现的“分布坍缩”问题。
*   **落地性强**：直接在超大规模电商搜索系统中验证了价值，展示了从推理能力提升到商业价值转化的完整路径。

### 8. 不足与局限
*   **冷启动依赖**：实验显示，如果基座模型初始能力极弱（如 Pass@1 接近 0），AGPO 仍难以凭空产生推理能力（即无法解决“从 0 到 1”的发现问题）。
*   **长期稳定性**：在极低或无 KL 约束的情况下，超长步数的训练可能会导致策略漂移，性能出现下滑。
*   **计算开销**：虽然不需要 Critic 网络，但组采样（G=8）在大规模训练中依然存在较高的推理计算成本。

（完）
