---
title: "When Embedding-Based Defenses Fail: Rethinking Safety in LLM-Based Multi-Agent Systems"
title_zh: 当基于嵌入的防御失效时：重新思考基于大语言模型的多智能体系统的安全性
authors: "Lingxi Zhang, Guangtao Zheng, Hanjie Chen"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01133v1"
tags: ["query:mas-gen"]
score: 8.0
evidence: 大模型驱动的多智能体系统中的安全与通信
tldr: 本研究探讨了基于大语言模型的多智能体系统（MAS）在面对恶意攻击时的安全性。研究发现，现有的基于嵌入的防御手段在攻击者刻意伪装消息时会失效。为此，作者提出了三种新型攻击方式，并揭示了嵌入防御忽略Token级置信度信号的局限性。研究提出利用Logits置信度分数来过滤或降权恶意消息，实验证明该方法能显著提升MAS在多种场景下的鲁棒性，并强调了早期干预在防御中的关键作用。
selection_source: fresh_fetch
motivation: 现有基于嵌入的防御方法在恶意代理通过伪装使消息嵌入接近良性数据时容易失效，且忽略了模型内部的置信度信号。
method: 提出利用Token级别的置信度分数（Logits）作为信号，在多智能体通信过程中对可疑消息进行剪枝或降权处理。
result: 实验结果显示，该方法在不同的模型、数据集和通信拓扑结构中均能有效提升多智能体系统的抗攻击能力和鲁棒性。
conclusion: 置信度信号是防御MAS攻击的有效补充，但其效力随通信轮数增加而减弱，这提示未来防御研究应更注重早期干预。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）使智能体能够通信和共享信息，从而在复杂任务上实现强大的性能。然而，这种通信也创造了一个攻击面，恶意智能体可以借此传播虚假信息并操纵群体决策，破坏 MAS 的安全性。现有的基于嵌入的防御旨在检测并剔除可疑智能体，但其有效性取决于恶意消息和良性消息的文本嵌入之间是否存在清晰的分离。攻击者可以通过精心构造嵌入与良性消息接近的消息来规避此类防御。我们从理论上分析了这种失效模式，并通过三种攻击（Slow Drift、Benign Wrapper 和 Chaos Seeding）进行了实证验证。我们的分析进一步揭示了基于嵌入的防御的一个根本局限性：由于它们仅依赖于文本嵌入，因此忽略了诸如 Logits 之类的 Token 级置信度信号，而当嵌入在攻击下难以区分时，这些信号仍能提供有效信息。我们建议在 MAS 通信过程中使用置信度分数来剔除或降低消息的权重。实验表明，该方法在不同模型、数据集和通信拓扑结构中均提高了鲁棒性。此外，我们发现置信度信号的有效性会随着通信轮次的增加而衰减，这突显了早期干预的重要性。这些见解可以为未来关于 MAS 攻击和防御的研究提供参考和启发。

## Abstract
Large language model (LLM)-powered multi-agent systems (MAS) enable agents to communicate and share information, achieving strong performance on complex tasks. However, this communication also creates an attack surface where malicious agents can propagate misinformation and manipulate group decisions, undermining MAS safety. Existing embedding-based defenses aim to detect and prune suspicious agents, but their effectiveness depends on a clear separation between the text embeddings of malicious and benign messages. Attackers can circumvent such defenses by crafting messages whose embeddings lie close to benign ones. We analyze this failure mode theoretically and validate it empirically with three attacks, Slow Drift, Benign Wrapper, and Chaos Seeding. Our analysis further reveals a fundamental limitation of embedding-based defenses: because they rely solely on the text embeddings, they ignore token-level confidence signals such as logits, which can remain informative when embeddings are not distinguishable under attack. We propose using confidence scores to prune or down-weight messages during MAS communication. Experiments show improved robustness across models, datasets, and communication topologies. Moreover, we find that the effectiveness of confidence signals decays over communication rounds, highlighting the importance of early intervention. This insights can inform and inspire future work on MAS attacks and defenses.

---

## 论文详细总结（自动生成）

这篇论文对基于大语言模型（LLM）的多智能体系统（MAS）的安全性进行了深入探讨，特别关注了现有防御机制的脆弱性并提出了改进方案。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着 MAS 在复杂任务（如软件工程、聊天机器人）中的广泛应用，确保其安全性至关重要。恶意智能体可以通过通信传播虚假信息，操纵群体决策。
*   **核心问题**：现有的防御手段（如 G-Safeguard, GUARDIAN）主要依赖**文本嵌入（Embedding）**来识别异常。作者指出，这种防御假设恶意消息在嵌入空间中是明显的离群点。
*   **研究动机**：如果攻击者刻意构造“近良性（Near-benign）”的消息，使其嵌入与正常消息极度接近，现有的防御就会失效。论文旨在揭示这一漏洞，并寻找更可靠的内部信号来增强防御。

### 2. 方法论：核心思想与关键技术
论文提出了攻击和防御两个层面的方法：

#### **A. 三种“近良性”攻击模式**
旨在减小恶意消息与良性消息在嵌入空间的分离度：
1.  **缓慢漂移（Slow Drift）**：在多轮对话中逐渐改变立场，避免嵌入向量发生突变，从而规避离群点检测。
2.  **良性包装（Benign Wrapper）**：将简短的恶意指令嵌入到长篇的良性文本中，利用良性背景锚定嵌入位置。
3.  **混沌播种（Chaos Seeding）**：不直接攻击防御者，而是诱导良性智能体产生分歧，扩大良性消息的嵌入分布范围，使恶意消息更容易隐藏。

#### **B. 置信度引导的防御（Confidence-Guided Defense）**
核心思想是利用 LLM 生成过程中的**Token 级 Logits（逻辑值）**作为内部可靠性信号。
*   **置信度计算**：通过计算生成消息中 Token 的熵（Entropy）来衡量不确定性。高熵意味着模型在生成该消息时信心不足（通常与受攻击或生成虚假信息相关）。
*   **防御策略**：
    1.  **置信度剪枝（Pruning）**：直接拦截置信度低于阈值的消息。
    2.  **置信度降权（Down-weighting）**：将置信度作为元数据随消息发送，引导接收方智能体在推理时降低对低置信度内容的权重。

### 3. 实验设计
*   **数据集**：涵盖事实知识（**MMLU**）、多步推理（**GSM8K**）和逻辑验证（**BBH**）。
*   **模型（Backbones）**：开源模型 **LLaMA-3.1-8B**、**Qwen3-4B**，以及闭源 API 模型 **GPT-4o-mini**。
*   **通信拓扑**：星型（Star）、链式（Chain）和随机稀疏图（Sparse Random）。
*   **对比基准（Baselines）**：
    *   无防御状态。
    *   **G-Safeguard**：基于图神经网络（GNN）的嵌入防御。
    *   **GUARDIAN**：基于时序图建模的无监督嵌入防御。

### 4. 资源与算力
*   **算力说明**：论文提到开源模型（LLaMA, Qwen）使用 **vLLM** 库在本地运行以提取 Token 级的 Logits。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）、显存占用或具体的训练/推理总时长。对于 GPT-4o-mini，则通过 API 调用并获取返回的 Token 概率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 3 个主流数据集和 3 种不同架构/规模的 LLM。
    *   测试了 3 种不同的通信拓扑结构。
    *   进行了消融实验，分析了剪枝阈值 $\delta$ 和降权强度 $\alpha$ 的影响。
    *   分析了信号随通信轮次（Round）变化的动态过程。
*   **充分性评价**：实验设计较为全面，不仅验证了攻击的有效性，还通过多维度对比证明了置信度信号的互补性。通过理论证明（定理 4.1）与实证结果相结合，增强了说服力。

### 6. 主要结论与发现
*   **嵌入防御的局限**：理论和实验均证明，基于嵌入的防御存在“良性接受区”过宽的问题，极易被精心设计的近良性攻击绕过。
*   **置信度信号的有效性**：Token 级的置信度在嵌入信号失效时仍具有辨别力，能显著提升系统在受攻击下的准确率。
*   **信号衰减效应**：防御信号（无论是嵌入还是置信度）会随着通信轮数的增加而衰减，因为恶意信息会逐渐“污染”良性智能体的后续生成。
*   **拓扑影响**：通信拓扑越稠密（如全连接），信息污染扩散越快，防御窗口期越短；稀疏拓扑有助于保留防御信号。

### 7. 优点
*   **视角独特**：挑战了当前 MAS 安全研究中“恶意消息必然是嵌入离群点”的普遍假设。
*   **理论支撑**：利用 Lipschitz 连续性等数学工具证明了嵌入防御的根本脆弱性。
*   **实用性强**：提出的置信度防御方法不需要重新训练模型，属于即插即用的轻量级方案。

### 8. 不足与局限
*   **API 限制**：许多闭源模型 API 不提供完整的 Logits，这限制了该防御方法在某些商业模型上的应用（虽然文中尝试用 Token 概率替代，但信息量较少）。
*   **自适应攻击风险**：论文未深入探讨攻击者如果已知防御方在监测置信度，是否可以通过“校准攻击”来伪造高置信度的恶意消息。
*   **算力细节缺失**：缺乏具体的环境配置和运行成本分析，不利于复现时的资源评估。

（完）
