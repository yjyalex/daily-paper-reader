---
title: "STAR: Failure-Aware Markovian Routing for Multi-Agent Spatiotemporal Reasoning"
title_zh: STAR：面向多智能体时空推理的故障感知马尔可夫路由
authors: "Ruiyi Yang, Lihuan Li, Hao Xue, Flora D. Salim"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10057v2"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体时空推理中异构专家的路由
tldr: STAR是一个针对多智能体时空推理的故障感知路由框架。针对现有系统在处理不同类型执行失败时缺乏显式路由机制的问题，STAR将智能体间的控制建模为基于状态的转移策略。它结合专家定义的常规路径与从执行轨迹中学习到的恢复路径，通过故障感知的路由矩阵实现对畸形输出、依赖缺失等不同错误的精准响应。实验表明，STAR在多个时空推理基准测试中显著优于基线，尤其在需要错误恢复的复杂查询中表现突出。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通常将路由决策隐含在语言生成中，难以区分和处理不同类型的执行失败，导致错误恢复过程难以解释且难以优化。
method: 提出一种基于马尔可夫链的路由矩阵，将智能体间的控制显式化为基于当前任务、智能体及具体失败状态（如格式错误、依赖缺失）的转移策略。
result: 在三个时空推理基准和八个主流大模型上的测试显示，STAR显著优于现有基线，特别是在处理需要偏离常规路径进行错误恢复的复杂查询时提升最大。
conclusion: 显式的故障感知路由机制，而非单纯的智能体组合，是提升多智能体系统在复杂时空推理任务中鲁棒性和恢复能力的关键因素。
---

## 摘要
组合式时空推理通常需要系统调用多个异构专家，例如几何、时间、拓扑和轨迹智能体。一个核心问题是，当执行并非简单的成功或失败，而是以性质不同的方式失败时，系统应如何在专家之间进行路由。现有的工具增强型和多智能体大语言模型（LLM）系统通常将这种路由决策隐含在语言生成中，导致恢复过程具有随机性、难以解释且难以优化。本文提出了 STAR（时空智能体路由器），这是一个故障感知路由框架，它将智能体间的控制外部化为一种基于当前智能体、任务类型和类型化执行状态的状态条件转移策略。STAR 的核心是一个智能体路由矩阵，它结合了专家指定的名义路线和从执行轨迹中学习到的恢复转移。由于该矩阵以不同的故障状态为条件，路由器可以针对格式错误的输出、缺失的依赖关系以及工具与查询的不匹配做出不同的响应，而不是将它们归结为通用的重试信号。专家通过基于工具的“提取-计算-存入”协议执行，并将中间结果写入共享黑板以供下游融合。结果证明，在训练期间保留失败的轨迹扩大了路由策略在错误状态上的支持集，从而实现了仅靠成功轨迹训练无法表示的恢复转移。在三个时空基准测试和八个骨干 LLM 上，STAR 优于多个基准模型，在执行偏离名义路由路径的查询上提升最为明显。针对路由器的消融实验和恢复分析进一步表明，类型化的故障感知路由，而非单纯的专家组合，是实现这些改进的关键因素。

## Abstract
Compositional spatiotemporal reasoning often requires a system to invoke multiple heterogeneous specialists, such as geometric, temporal, topological, and trajectory agents. A central question is how such a system should route among specialists when execution does not simply succeed or fail, but fails in qualitatively different ways. Existing tool-augmented and multi-agent LLM systems typically leave this routing decision implicit in language generation, making recovery ad hoc, difficult to interpret, and hard to optimize. This paper presents STAR (Spatio-Temporal Agent Router), a failure-aware routing framework that externalizes inter-agent control as a state-conditioned transition policy over the current agent, task type, and typed execution status. At the center of STARis an agent routing matrix that combines expert-specified nominal routes with recovery transitions learned from execution traces. Because the matrix conditions on distinct failure states, the router can respond differently to malformed outputs, missing dependencies, and tool--query mismatches, rather than collapsing them into a generic retry signal. Specialists execute through a tool-grounded extract--compute--deposit protocol and write intermediate results to a shared blackboard for downstream fusion. Results prove that retaining unsuccessful traces during training enlarges the support of the routing policy on error states, enabling recovery transitions that success-only training cannot represent. Across three spatiotemporal benchmarks and eight backbone LLMs, STAR improves over multiple baselines with the clearest gains on queries whose execution deviates from the nominal routing path. Router-specific ablations and recovery analyses further show that typed failure-aware routing, rather than specialist composition alone, is a key factor for these improvements.

---

## 论文详细总结（自动生成）

以下是对论文《STAR: Failure-Aware Markovian Routing for Multi-Agent Spatiotemporal Reasoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在处理复杂的时空推理（如几何定位、时间关系、轨迹分析等）时，系统需要调用多个异构专家智能体。现有的多智能体系统通常将“路由”（即决定下一个由谁执行）隐含在 LLM 的文本生成中。
*   **研究动机**：
    *   **失败模式的非单一性**：执行失败并非只有“成功”或“失败”，而是存在多种性质不同的错误（如输出格式错误、依赖项缺失、工具与查询不匹配）。
    *   **恢复机制的局限性**：现有系统往往采用通用的“重试”信号，缺乏针对特定错误类型的显式恢复逻辑，导致过程难以解释、难以优化且鲁棒性差。
    *   **时空推理的复杂性**：时空任务对计算的严谨性要求高，简单的端到端生成难以胜任。

### 2. 方法论：STAR 框架
*   **核心思想**：将智能体间的控制逻辑外部化为一个**状态条件转移策略（State-Conditioned Transition Policy）**，即基于当前智能体、任务类型和具体的执行状态（成功或特定类型的失败）来决定下一步。
*   **关键技术细节**：
    *   **路由矩阵 ($M$)**：核心是一个四维张量，索引包括：当前智能体 ($a$)、任务类型 ($t$)、执行状态 ($s$) 和下一智能体 ($a'$)。
    *   **双系统路由**：
        *   **系统 1（名义路由）**：遵循专家定义的标准执行路径（针对 `INIT` 和 `SUCC` 状态）。
        *   **系统 2（恢复路由）**：从执行轨迹中学习到的恢复转移（针对 `FAIL` 格式错误、`BLOCK` 依赖缺失、`MISS` 工具不匹配）。
    *   **执行协议（Extract–Compute–Deposit）**：
        1.  **提取**：LLM 专家从查询和黑板中提取工具参数。
        2.  **计算**：调用确定性的符号求解器（如几何计算、时间代数）进行计算。
        3.  **存入**：将结果和类型化的执行状态写入**共享黑板（Blackboard）**。
    *   **故障感知训练**：通过保留失败的执行轨迹来训练路由矩阵。论文证明了（定理 1），保留失败轨迹能扩大路由策略在错误状态下的支持集，这是仅靠成功案例训练无法实现的。

### 3. 实验设计
*   **数据集与基准**：
    *   **STARK**：包含 18 种任务类型，侧重时空知识检索。
    *   **STB24 (STBench)**：包含 13 种任务类型，侧重时空分析。
    *   **STB26 (ST-Bench 2026)**：包含 4 种任务类型，侧重时空因果与预测。
*   **对比方法**：
    *   **基础模型**：LLM-only (CoT), Function-calling。
    *   **智能体/提示框架**：Reflexion, ReAct, Tree-of-Thought (ToT), Graph-of-Thought (GoT)。
*   **骨干模型**：横跨 8 个模型，包括 Claude 3.5 Sonnet/Haiku 以及 Qwen3-8B、Llama-3.1-8B 等开源模型。

### 4. 资源与算力
*   **硬件支持**：论文致谢中提到使用了由 Sharon AI 提供的 **NVIDIA H100 GPU**。
*   **算力细节**：文中未明确给出具体的训练总时长或使用的 GPU 确切数量。但提供了推理阶段的平均延迟（如 Qwen3-8B 在 STAR 框架下平均延迟为 1.8s/query）和 Token 消耗量。

### 5. 实验数量与充分性
*   **实验规模**：测试集包含 **7,152 个查询**，涵盖 35 种任务类型。
*   **充分性与公平性**：
    *   **多模型验证**：在 8 个不同参数规模（3B 到 20B+）的骨干模型上进行了测试，证明了框架的普适性。
    *   **深度消融实验**：分别针对框架组件（黑板、工具、分类器）和路由器参数（状态条件、权重 $\alpha$、恢复增强）进行了详细消融。
    *   **跨基准迁移**：测试了在一个基准上训练的路由矩阵在其他基准上的表现，验证了路由逻辑的可迁移性。
    *   **统计显著性**：所有主要结果均附带了 95% 的 Wilson 置信区间，实验设计客观且严谨。

### 6. 主要结论与发现
*   **性能提升**：STAR 在所有基准上均显著优于基线。例如在 Qwen3-8B 上，相比 Reflexion 在三个基准上分别提升了 4.2、39.1 和 15.3 个百分点。
*   **恢复能力的价值**：当执行进入 `MISS`（工具不匹配）状态时，STAR 的恢复成功率甚至超过了无故障的基准线，说明学习到的路由能纠正初始的错误分配。
*   **失败轨迹的重要性**：实验证实，如果训练只包含成功案例，系统将无法学习如何从错误状态中跳出。

### 7. 优点
*   **显式控制**：将模糊的 LLM 内部决策转化为可解释、可调试的马尔可夫路由矩阵。
*   **细粒度反馈**：区分了不同类型的失败，使得“恢复”动作具有语义针对性。
*   **符号与神经结合**：通过“提取-计算”协议，利用了确定性工具的准确性和 LLM 的理解能力。
*   **理论支撑**：提供了关于恢复可达性的理论证明，增强了方法的说服力。

### 8. 不足与局限
*   **分类依赖**：系统性能部分取决于 HEAD 智能体对任务类型的初始分类准确性。
*   **参数提取瓶颈**：虽然路由可以恢复，但如果 LLM 始终无法正确提取工具所需的参数（导致持续 `FAIL`），路由也无法彻底解决问题。
*   **复合查询评估较少**：虽然进行了复合查询的案例研究，但样本量（14 个）相对较小，不足以形成强有力的定量结论。
*   **分类体系限制**：路由矩阵目前依赖于预定义的任务分类体系，对于完全未知的任务类型可能需要重新训练或调整。

（完）
