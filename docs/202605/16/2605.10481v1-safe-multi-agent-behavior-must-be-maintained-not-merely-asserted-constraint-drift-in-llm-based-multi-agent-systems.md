---
title: "Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems"
title_zh: 安全的多智能体行为必须得到维持，而不仅仅是断言：基于大语言模型的多智能体系统中的约束漂移
authors: "Tianxiao Li, Yixing Ma, Haiquan Wen, Zhenglin Huang, Qianyu Zhou, Zeyu Fu, Guangliang Cheng"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10481v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 基于LLM的多智能体系统中的安全约束
tldr: 本研究探讨了基于大语言模型的多智能体系统中的安全性挑战，指出安全性不应仅在最终输出中体现，而应贯穿整个执行轨迹。作者提出了“约束漂移”概念，描述了安全约束在记忆、授权和通信等环节中逐渐失效的现象。为此，提出了“约束状态治理”范式，通过将安全约束维护为显式执行状态，并结合约束原生强化学习，确保智能体在复杂工作流中始终遵循安全边界。
selection_source: fresh_fetch
motivation: 传统的静态安全检查无法应对LLM智能体在复杂交互过程中因记忆、授权或通信导致的约束失效（约束漂移）问题。
method: 提出约束状态治理范式，将安全约束作为显式的执行状态进行动态维护，并采用约束原生强化学习在安全边界内提升系统效用。
result: 定义并系统化了约束漂移现象，为多智能体系统安全性研究提供了一个从静态断言转向动态维护的新研究框架。
conclusion: 多智能体系统的安全必须通过在整个执行轨迹中持续维护约束来实现，而非仅仅依赖初始提示词或最终输出的合规性检查。
---

## 摘要
现代基于大语言模型（LLM）的智能体不再仅仅是消极的文本生成器。它们读取代码库、调用工具、浏览网页、执行代码、维护记忆、与其他智能体通信，并通过长程工作流采取行动。这种转变转移了安全性的基本单元。一个系统可能会产生合规的最终答案，但同时通过内部消息泄露私密信息、授予超出其原始范围的权限、在敏感上下文中调用外部工具，或者丢失重建为何允许某项操作所需的证据。我们认为，基于 LLM 的多智能体系统中出现的许多新故障具有共同的结构：安全关键约束在整个执行轨迹中未能保持有效。我们将这种现象称为“约束漂移”（constraint drift）：即约束在经过记忆、授权、通信、工具使用、审计和优化过程中发生的丢失、扭曲、削弱或松弛。本文的立场是，安全的多智能体行为必须得到维持，而不仅仅是断言。提示词、护栏、工具模式、访问控制和最终输出检查是必要的，但除非约束在执行过程中保持新鲜、可继承、可强制执行且可审计，否则它们是不充分的。我们提出了“约束状态治理”（Constraint State Governance）作为基于 LLM 的多智能体系统的研究范式。在该范式中，安全关键约束被维护为显式的执行状态，而约束原生强化学习仅在维护的安全边界内提高效用。其目标不是将智能体系统冻结在僵化的规则之下，而是使安全性在现代智能体实际行动的轨迹中具备可操作性。

## Abstract
Modern LLM based agents are no longer passive text generators. They read repositories, call tools, browse the web, execute code, maintain memory, communicate with other agents, and act through long horizon workflows. This shift moves the unit of safety. A system may produce a compliant final answer while leaking private information through an internal message, delegating authority beyond its original scope, calling an external tool with sensitive context, or losing the evidence needed to reconstruct why an action was allowed. We argue that many emerging failures in LLM-based multi-agent systems share a common structure: safety critical constraints do not remain operative throughout the trajectory. We call this phenomenon constraint drift: the loss, distortion, weakening, or relaxation of constraints as they pass through memory, delegation, communication, tool use, audit, and optimization. The position taken here is that safe multi-agent behavior must be maintained, not merely asserted. Prompts, guardrails, tool schemas, access control, and final output checks are necessary, but they are insufficient unless constraints remain fresh, inherited, enforceable, and auditable across execution. We propose Constraint State Governance as a research paradigm for LLM-based multi-agent systems. In this paradigm, safety-critical constraints are maintained as explicit execution state, while constraint-native reinforcement learning improves utility only within maintained safety boundaries. The goal is not to freeze agentic systems under rigid rules, but to make safety operational across the trajectories through which modern agents actually act.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文指出，现代基于大语言模型（LLM）的智能体已从单纯的“文本生成器”转变为“执行系统”（如自动编程、工具调用）。在这种背景下，传统的安全性评估（仅检查最终输出是否合规）已失效。
*   **研究动机**：在多智能体协作的长程工作流中，初始设定的安全约束（如“不得泄露 API 密钥”）往往会在执行过程中逐渐失效。
*   **核心概念——约束漂移（Constraint Drift）**：作者定义了这一新现象，即安全约束在经过记忆总结、任务委派、智能体间通信、工具 I/O 和优化迭代时，发生的丢失、扭曲、削弱或松弛。论文主张：安全必须在整个执行轨迹中被“维持”，而不能仅仅在开头“断言”。

### 2. 论文提出的方法论
论文提出了**约束状态治理（Constraint State Governance, CSG）**范式，包含两个耦合层：

*   **约束状态化（Constraint as State）**：
    *   将自然语言规则转化为**签名令牌（Signed Tokens）**，包含规则文本、运行时谓词（Predicate）、适用范围（Scope）、优先级和有效期。
    *   系统维护一个全局治理状态 $B_t$，记录活跃令牌集、权限图、敏感对象标签（Taints）和审计根。
*   **准入控制算法（Admission Control Algorithm）**：
    *   在执行任何关键动作前，必须通过准入检查。检查项包括：**新鲜度**（确保不是基于过时规则执行）、**权限校验**（防止授权范围扩大）、**流向控制**（防止敏感信息跨频道泄露）以及**证据校验**（确保动作有据可查）。
*   **约束原生强化学习（Constraint Native RL）**：
    *   核心思想是将强制性约束定义为“准入边界”，而非奖励函数中的软惩罚项。
    *   学习算法仅在“可接纳轨迹集”（Admissible Set）内优化效用。如果轨迹违反了约束，它将被标记为违规证据，而不参与效用竞争。

### 3. 实验设计
*   **数据集/场景**：使用了 **AgentLeak** 数据集，包含 4,979 条来自医疗、金融、法律和企业领域的真实轨迹。
*   **Benchmark**：评估智能体在三个频道（最终输出 C1、内部消息 C2、共享内存 C5）中的隐私泄露情况。
*   **对比方法**：
    1.  **No Defense**：无防御状态。
    2.  **Output Only Filter**：仅对最终输出进行过滤（传统方法）。
    3.  **CSG Lite**：本文提出的治理范式的简化版，在每个状态转移点进行准入检查和脱敏。
*   **离线策略搜索**：通过回放实验，测试不同策略在维持安全边界的同时保留有用信息的能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于本文属于“立场论文”（Position Paper），其实验部分主要基于对现有数据集（AgentLeak）的**反事实回放（Counterfactual Replay）**和离线分析，而非从头训练大规模多智能体系统，因此对实时算力的需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：基于近 5,000 条复杂轨迹进行回放，样本量较大，覆盖了多个敏感行业领域。
*   **充分性评价**：
    *   **客观性**：实验通过对比“最终输出过滤”和“轨迹级治理”，清晰地展示了传统方法在屏蔽内部频道泄露方面的无力。
    *   **局限性**：实验主要集中在“信息流漂移”（隐私泄露）这一种模式上。对于论文提到的其他四种模式（记忆、授权、问责、效用诱导漂移），更多是理论论证和案例分析，缺乏同等规模的定量实验支撑。

### 6. 论文的主要结论与发现
*   **最终输出安全是假象**：即使最终答案看起来安全，智能体可能已经在内部消息或工具调用中泄露了私钥或删除了关键测试。
*   **治理优于过滤**：CSG Lite 将内部频道的泄露率从 68.8% 降低到了 5.7% 以下，并生成了完整的审计链。
*   **约束原生学习的可行性**：实验证明，可以在不牺牲过多效用的前提下，通过在准入边界内进行策略优化，实现既安全又高效的智能体行为。

### 7. 优点
*   **理论前瞻性**：敏锐地捕捉到了 LLM 智能体从“对话”向“执行”转变过程中的安全真空，提出了“约束漂移”这一极具解释力的概念。
*   **系统级视角**：不局限于改进 Prompt，而是借鉴了计算机系统安全（如信息流控制、能力访问控制）的成熟思想。
*   **可审计性**：强调了安全证据的哈希链记录，为事后追责和系统复盘提供了技术手段。

### 8. 不足与局限
*   **实现复杂度**：将模糊的自然语言指令转化为精确的“运行时谓词”和“签名令牌”在工程上极具挑战，论文对此转化过程的自动化程度讨论不足。
*   **性能开销**：在每个动作前进行多维度的准入检查可能会显著增加系统的延迟，尤其是在实时性要求高的场景下。
*   **实验覆盖面**：如前所述，实证部分高度依赖隐私泄露场景，对于更复杂的“授权蔓延”或“逻辑篡改”等漂移模式的验证尚不充分。
*   **理想化假设**：算法假设治理层本身是不可篡改且完全可靠的，但在实际部署中，治理层本身也可能成为攻击目标。

（完）
