---
title: Reinforcement Learning with Semantic Rewards Enables Low-Resource Language Expansion without Alignment Tax
title_zh: 基于语义奖励的强化学习实现无对齐税的低资源语言扩展
authors: "Zeli Su, Ziyin Zhang, Zhou Liu, Xuexian Song, Zhankai Xu, Longfei Zheng, Xiaolu Zhang, Rong Fu, Guixian Xu, Wentao Zhang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14366v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用GRPO和语义奖励的强化学习用于大模型扩展
tldr: 针对低资源语言扩展中常见的“对齐税”问题，即提升目标语言能力导致通用能力下降，本文提出一种基于语义奖励的强化学习框架。该方法利用GRPO算法，通过嵌入层级的语义奖励而非似然最大化来优化模型，使模型在保持语义一致性的同时拥有更灵活的表达。在藏语相关任务上的实验证明，该方法在获得低资源语言能力的同时，显著减轻了对齐税，保留了更强的通用能力和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的监督微调（SFT）因强制执行Token级别的表面模仿，导致模型在扩展低资源语言时容易出现灾难性遗忘。
method: 提出一种基于GRPO的语义空间对齐范式，利用嵌入层级的语义奖励引导模型进行意义导向的优化，而非简单的似然最大化。
result: 在藏汉翻译和藏语标题生成任务中，该方法在提升语义质量的同时，比SFT更有效地保留了模型的通用能力和鲁棒表示。
conclusion: 带有语义奖励的强化学习为低资源语言扩展提供了一种更安全、可靠且能有效缓解对齐税的新路径。
---

## 摘要
将大语言模型（LLMs）扩展到低资源语言通常会产生“对齐税”：目标语言能力的提升是以通用能力的灾难性遗忘为代价的。我们认为，这种权衡源于监督微调（SFT）的僵化性，它在狭窄且有偏的数据分布上强制执行词元级的表面模仿。为解决这一局限性，我们提出了一种由群体相对策略优化（GRPO）驱动的语义空间对齐范式，其中模型使用嵌入级语义奖励而非似然最大化进行优化。该目标通过灵活的实现方式鼓励意义保留，从而实现受控更新，减少对预训练知识的破坏性干扰。我们在藏汉机器翻译和藏语标题生成任务上评估了我们的方法。实验表明，我们的方法在获得低资源能力的同时，显著减轻了对齐税，比 SFT 更有效地保留了通用能力。尽管产生的表面重叠较少，但语义强化学习在开放式生成中表现出更高的语义质量和偏好，且少样本迁移结果表明，它在有限监督下学习到了更具迁移性和鲁棒性的表示。总之，我们的研究表明，带有语义奖励的强化学习为包容性的低资源语言扩展提供了一条更安全、更可靠的路径。

## Abstract
Extending large language models (LLMs) to low-resource languages often incurs an "alignment tax": improvements in the target language come at the cost of catastrophic forgetting in general capabilities. We argue that this trade-off arises from the rigidity of supervised fine-tuning (SFT), which enforces token-level surface imitation on narrow and biased data distributions. To address this limitation, we propose a semantic-space alignment paradigm powered by Group Relative Policy Optimization (GRPO), where the model is optimized using embedding-level semantic rewards rather than likelihood maximization. This objective encourages meaning preservation through flexible realizations, enabling controlled updates that reduce destructive interference with pretrained knowledge. We evaluate our approach on Tibetan-Chinese machine translation and Tibetan headline generation. Experiments show that our method acquires low-resource capabilities while markedly mitigating alignment tax, preserving general competence more effectively than SFT. Despite producing less rigid surface overlap, semantic RL yields higher semantic quality and preference in open-ended generation, and few-shot transfer results indicate that it learns more transferable and robust representations under limited supervision. Overall, our study demonstrates that reinforcement learning with semantic rewards provides a safer and more reliable pathway for inclusive low-resource language expansion.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在扩展低资源语言时面临的“对齐税”问题，提出了一种基于语义奖励的强化学习新范式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：在将 LLM 扩展到低资源语言（如藏语）时，传统的**监督微调（SFT）**往往会导致“对齐税”（Alignment Tax），即模型在提升目标语言能力的同时，通用能力（如高资源语言的理解和推理）会出现严重的灾难性遗忘。
*   **研究背景**：作者认为这种现象源于 SFT 的局限性——它在规模有限、领域狭窄且有偏的低资源数据上强制执行“词元级（Token-level）”的表面模仿。这种僵化的优化目标会过度调整模型参数，破坏预训练阶段积累的知识。

### 2. 方法论：语义空间对齐范式
论文提出将语言扩展视为一种“语义对齐”问题，而非简单的分布匹配。
*   **核心思想**：利用强化学习（RL）引导模型在语义空间进行对齐，只要输出的意义与参考文本一致即可获得奖励，而不强制要求词元序列完全一致。
*   **关键技术流程**：
    1.  **阶段一：冷启动 SFT**：在极少量（5k条）低资源数据上进行轻量级微调，使模型具备基本的语言生成能力（如正确的字符集）。
    2.  **阶段二：基于语义奖励的 GRPO**：
        *   **算法**：采用**群体相对策略优化（GRPO）**，这是一种无需价值函数（Value-free）的 PPO 变体，通过组内样本的相对奖励进行策略更新。
        *   **语义奖励设计**：使用预训练的跨语言嵌入模型（如 CINO）计算生成文本与参考文本的余弦相似度。
        *   **语言一致性奖励**：引入基于 Unicode 范围的脚本检查，确保模型输出纯正的目标语言，防止出现语种混杂。
        *   **约束优化**：利用 RL 的信任区域（Trust-region）约束，限制模型偏离预训练分布的幅度。

### 3. 实验设计
*   **任务与数据集**：
    *   **藏汉机器翻译（MT）**：使用内部收集的平行语料（约 90k 条）。
    *   **藏语标题生成（HG）**：使用 CMHG 数据集（约 16k 条）。
*   **Benchmark（基准测试）**：
    *   **通用能力测试**：使用中文阅读理解基准 **CMRC** 来衡量“对齐税”。
    *   **任务指标**：BLEU-4、ROUGE-L、语义相似度。
    *   **人类偏好评估**：使用 GPT-5.2 作为裁判（LLM-as-a-Judge）进行盲测。
*   **对比方法**：冷启动 SFT（少量数据）、强 SFT（全量数据）、语义 RL（本文方法）。

### 4. 资源与算力
*   **模型基础**：使用 **Qwen3-4B-Instruct** 作为基座模型。
*   **微调技术**：采用 **LoRA**（Rank=64）进行参数高效微调。
*   **算力细节**：文中提到 SFT 阶段使用了 **2 张 GPU**（每张显卡 Batch Size 为 8，梯度累积为 2），但未明确说明具体的 GPU 型号（如 A100 或 H100）以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了翻译和生成两大类任务，并进行了消融实验（验证不同奖励组合的效果）、少样本迁移实验（验证表示的鲁棒性）以及机理性分析（评估 OOD 数据的似然值和 KL 散度）。
*   **充分性评价**：实验设计较为全面。作者不仅关注任务本身的指标，还深入探讨了模型内部分布的变化。通过 LLM 裁判的引入，弥补了低资源语言自动评价指标（如 BLEU）不准确的问题。实验对比了 SFT 在全量数据下的表现，确保了对比的公平性。

### 6. 主要结论与发现
*   **消除对齐税**：语义 RL 在提升藏语能力的同时，CMRC 分数显著高于强 SFT（高出约 5.15 分），几乎消除了灾难性遗忘。
*   **语义质量更高**：尽管 RL 模型的 BLEU/ROUGE 分数可能低于 SFT（因为不追求词元重合），但在 LLM 裁判的评估中，RL 生成的内容更受青睐（胜率高出 16.1%）。
*   **迁移性更强**：在从翻译任务向标题生成任务进行少样本迁移时，RL 初始化模型表现出更好的泛化能力。
*   **奖励设计的关键**：消融实验证明，单纯的语义奖励容易导致语种混杂，必须结合语言一致性奖励。

### 7. 优点与亮点
*   **视角创新**：将低资源语言扩展从“模仿任务”转变为“对齐任务”，为解决灾难性遗忘提供了新思路。
*   **算法高效**：采用 GRPO 算法，避免了复杂的 Critic 网络训练，降低了 RL 的门槛。
*   **鲁棒性**：通过语义空间优化，使模型学会了“表达意义”而非“背诵文本”，生成的文本更具灵活性。

### 8. 不足与局限
*   **语言覆盖有限**：实验仅在藏语上进行，尚未验证该方法在其他语系或极低资源（如无平行语料）场景下的普适性。
*   **依赖奖励模型**：语义奖励的质量高度依赖于预训练的跨语言嵌入模型，如果嵌入模型本身在某些领域存在偏差，会直接影响 RL 的效果。
*   **数据领域局限**：实验使用的翻译数据多源于视觉描述领域，分布较为狭窄，可能影响了对模型真实泛化能力的全面评估。

（完）
