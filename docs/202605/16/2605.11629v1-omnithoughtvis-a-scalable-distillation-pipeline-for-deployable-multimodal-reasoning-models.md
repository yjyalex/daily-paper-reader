---
title: "OmniThoughtVis: A Scalable Distillation Pipeline for Deployable Multimodal Reasoning Models"
title_zh: OmniThoughtVis：一种面向可部署多模态推理模型的可扩展蒸馏流水线
authors: "Yuanhao Yue, Chengyu Wang, Yuanjie Lyu, Lei Shen, Jun Huang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11629v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 蒸馏多模态推理能力和结构化思维链轨迹
tldr: 本研究针对多模态大模型在实际部署中面临的延迟和资源限制，提出了OmniThoughtVis蒸馏框架。该框架通过自动化流水线从高性能教师模型中提取结构化思维链（CoT）数据，并结合难度、质量和多样性采样，构建了包含180万样本的高质量数据集。实验表明，该方法显著提升了2B至8B规模小模型的推理能力，使其在多个基准测试中达到或超过更大规模的基准模型，为高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 小型多模态模型因缺乏大规模高质量的思维链监督数据，导致其在实际应用中的推理性能受限。
method: 开发了OmniThoughtVis流水线，通过生成结构化CoT、多维度自动标注及多样性采样，构建了1.8M规模的蒸馏数据集。
result: 蒸馏后的Qwen3-VL 4B模型在MathVerse上提升了16.8分，且在多个任务上性能媲美甚至超越了未蒸馏的8B基准模型。
conclusion: 大规模推理蒸馏是提升小型多模态模型性能、实现高效且可部署推理能力的有效途径。
---

## 摘要
近期的多模态大语言模型（MLLMs）在视觉语言任务上展现出强大的思维链（CoT）推理能力，但其在现实系统中的直接部署往往受限于延迟和资源约束。在实践中，在线服务更倾向于使用较小的 MLLMs，但由于缺乏大规模、高质量的多模态 CoT 监督，其推理性能受到了限制。在本文中，我们提出了 OmniThoughtVis，这是一种可扩展的数据策展与蒸馏流水线，旨在将多模态推理能力从高容量教师模型迁移到更小的、面向部署的 MLLMs。该流水线从多样化的开源种子池出发，生成结构化的 CoT 轨迹，并对推理难度、答案质量和语义任务标签进行联合标注。为了在大规模生产中保持数据质量，我们结合了基于规则的过滤、难度感知选择和基于标签的多样性采样，最终构建了一个包含 180 万个样本的精选语料库，支持为下游训练构建可控子集。我们利用 OmniThoughtVis 蒸馏了参数量从 2B 到 8B 的 Qwen3-VL 模型，并在九个多模态推理基准上进行了评估。结果显示，蒸馏后的模型在不同规模下均表现出一致的性能提升，其中 4B 模型在 MathVerse 上提升了高达 16.8 个百分点，在 MMMU-Pro 上提升了 5.6 个百分点。值得注意的是，蒸馏后的 4B 模型在多项任务上达到或超过了未蒸馏的 8B 基准模型，凸显了可扩展推理蒸馏对于面向部署的 MLLMs 的实用价值。

## Abstract
Recent multimodal large language models (MLLMs) have shown strong chain-of-thought (CoT) reasoning ability on vision-language tasks, but their direct deployment in real-world systems is often limited by latency and resource constraints. In practice, smaller MLLMs are preferred for online serving, yet their reasoning performance is bottlenecked by the lack of large-scale, high-quality multimodal CoT supervision. In this paper, we present OmniThoughtVis, a scalable data curation and distillation pipeline for transferring multimodal reasoning capabilities from high-capacity teacher models to smaller, deployment-oriented MLLMs. Starting from a diverse open-source seed pool, our pipeline generates structured CoT traces and performs joint annotation of reasoning difficulty, answer quality, and semantic task tags. To maintain data quality at scale, we combine rule-based filtering, difficulty-aware selection, and tag-based diversity sampling, resulting in a curated corpus of 1.8M samples that supports controllable subset construction for downstream training. We use OmniThoughtVis to distill Qwen3-VL models from 2B to 8B parameters and evaluate them on nine multimodal reasoning benchmarks. The resulting distilled models show consistent gains across model scales, including improvements of up to +16.8 points on MathVerse and +5.6 points on MMMU-Pro for the 4B model. Notably, the distilled 4B model matches or surpasses the undistilled 8B baseline on several tasks, highlighting the practical value of scalable reasoning distillation for deployment-oriented MLLMs.

---

## 论文详细总结（自动生成）

### OmniThoughtVis：面向可部署多模态推理模型的可扩展蒸馏流水线总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型多模态模型（MLLMs）虽然具备强大的思维链（CoT）推理能力，但由于高延迟和高资源消耗，难以在实际生产环境中直接部署。而更适合在线服务的小型模型则因缺乏大规模、高质量的多模态 CoT 监督数据，导致其推理性能受限。
*   **研究背景**：现有的多模态数据集多侧重于指令微调或最终答案监督，缺乏结构化的推理步骤。此外，简单地大规模生成合成数据往往伴随噪声、冗余和任务分布不均等问题。

#### 2. 方法论：核心思想与关键技术
OmniThoughtVis 提出了一套从数据策展到模型蒸馏的自动化流水线，核心步骤包括：
*   **种子数据采样**：从 FineVision（3.5M 样本）出发，通过分层采样和类别上限控制，构建多样化的初始池。
*   **带约束的蒸馏生成**：使用高性能教师模型（Qwen-VL-Max）生成 CoT 轨迹，并强制执行 XML 风格的结构化格式（`<think>` 步骤与 `<answer>` 答案分离），便于后续解析和过滤。
*   **联合标注与语义打标**：利用 Qwen3-VL-Flash 作为评分器，对每个样本进行三个维度的标注：
    *   **难度（1-5级）**：从简单识别到抽象复杂推理。
    *   **质量（1-5级）**：评估答案的准确性和完整性。
    *   **语义标签**：生成任务相关的标签（如计数、空间、数学等）。
*   **质量控制与子集构建**：
    *   **规则过滤**：剔除格式错误、过短/过长或存在重复/不稳模式的样本。
    *   **难度感知选择**：优先保留难度系数 $\ge 4$ 的样本。
    *   **多样性采样**：利用标签嵌入进行聚类（DBSCAN）和最远点采样（FPS），确保任务覆盖的广度。
*   **最终产出**：构建了一个包含 180 万样本的精选语料库。

#### 3. 实验设计
*   **模型规模**：以 Qwen3-VL 系列（2B、4B、8B）作为学生模型。
*   **基准测试（Benchmarks）**：涵盖 9 个主流多模态推理榜单：
    *   **通用理解**：AI2D, MMBench, MMMU, MMMU-Pro (Standard/Vision), MMStar。
    *   **数学推理**：MathVista, MathVerse, MathVision。
*   **对比方法**：主要对比未经过 OmniThoughtVis 蒸馏的原始 Qwen3-VL-Instruct 基线模型。

#### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，序列长度 3072，采用余弦学习率衰减。
*   **超参数**：通过对 2B 模型进行 20 组配置的网格搜索，确定了最佳学习率为 $5 \times 10^{-6}$，训练 4 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但提到了在固定计算预算下进行实验。

#### 5. 实验数量与充分性
*   **实验规模**：在 9 个基准上进行了跨 3 种模型规模的评估。
*   **消融实验**：针对数据过滤策略（仅质量、仅难度、两者结合）进行了对比，并对训练过程中的性能演变（Scaling Behavior）进行了深入分析。
*   **充分性评价**：实验设计较为客观、公平。通过网格搜索确定超参数，并使用统一的 0.5M 样本子集进行跨规模对比，增强了结论的可信度。

#### 6. 主要结论与发现
*   **性能显著提升**：蒸馏后的模型在所有规模上均有进步。4B 模型在 MathVerse 上提升了 16.8 分，在 MMMU-Pro 上提升了 5.6 分。
*   **以小博大**：蒸馏后的 4B 模型在多项任务上达到或超过了未蒸馏的 8B 基准模型，证明了高质量 CoT 数据对小模型的巨大价值。
*   **难度优于质量**：在数据筛选中，**难度评分**比质量评分更具区分度，选择高难度样本能带来更好的推理能力迁移。
*   **饱和速度差异**：通用视觉理解能力在训练早期即达到饱和，而复杂的推理能力则需要更长的训练时间。

#### 7. 优点（亮点）
*   **可扩展性**：提供了一套可处理百万级数据的自动化流水线，解决了多模态 CoT 数据稀缺的问题。
*   **结构化监督**：通过 XML 格式强制教师模型输出推理过程，提高了数据的可解释性和训练效率。
*   **精细化控制**：引入难度和语义标签，使得研究者可以根据需求灵活构建训练子集。

#### 8. 不足与局限
*   **语言与模态局限**：目前主要集中在英语和视觉中心任务，对多语言及其他模态（如音频、视频）的覆盖不足。
*   **模型偏见风险**：教师、评分器和学生模型均属于 Qwen 系列，可能存在“家族内部”的评估偏见。
*   **部署指标缺失**：虽然目标是“面向部署”，但论文未提供实际部署中的延迟（Latency）或内存占用等硬性指标的对比。
*   **自动化噪声**：尽管有过滤机制，但完全依赖模型生成的标注和轨迹仍可能包含潜在的逻辑错误。

（完）
