---
title: "RecRM-Bench: Benchmarking Multidimensional Reward Modeling for Agentic Recommender Systems"
title_zh: RecRM-Bench：面向智能体推荐系统的多维奖励建模基准
authors: "Wenwen Zeng, Jinhui Zhang, Hao Chen, Zhaoyu Hu, Yongqi Liang, Jiajun Chai, Dengcan Liu, Zhenfeng Liu, Shurui Yan, Minglong Xue, Xiaohan Wang, Wei Lin, Guojun Yin"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11874v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型智能体的多维奖励模型基准测试
tldr: 针对大语言模型驱动的智能体推荐系统，现有强化学习优化往往仅关注最终交互结果而忽视中间过程。本文推出了RecRM-Bench，这是目前规模最大、最全面的多维度奖励建模基准，包含超过100万条数据，涵盖指令遵循、事实一致性、相关性及用户行为预测四个核心维度，并提出了多维度奖励模型构建框架，为开发高性能推荐智能体奠定了基础。
selection_source: fresh_fetch
motivation: 现有的推荐系统奖励机制过于单一，缺乏评估智能体中间能力（如指令遵循和意图理解）的标准化多维度基准。
method: 提出了包含100万条结构化数据的RecRM-Bench基准，并构建了一个集成多维度奖励模型与混合奖励函数的系统化框架。
result: 该基准涵盖了从语法合规性到复杂意图落地的四个关键评估维度，为训练复杂的奖励模型提供了基础数据集。
conclusion: RecRM-Bench填补了智能体推荐系统多维度奖励评估的空白，为开发更可靠、能力更强的推荐智能体提供了重要支撑。
---

## 摘要
大语言模型（LLM）智能体的集成正在推动推荐系统从简单的查询-项目匹配向深度个性化和交互式推荐转型。强化学习（RL）为优化推荐任务中的这些智能体提供了一个核心框架。然而，当前的方法仍受限于对单一维度、基于结果的奖励的依赖，这些奖励仅关注最终的用户交互，而忽视了关键的中间能力，如指令遵循和复杂意图理解。尽管设计多维奖励具有必要性，但该领域仍缺乏标准化的基准来促进这一发展。为了填补这一空白，我们推出了 RecRM-Bench，这是迄今为止针对智能体推荐系统规模最大、最全面的基准。它包含超过 100 万条结构化条目，涵盖四个核心评估维度：指令遵循、事实一致性、查询-项目相关性以及细粒度用户行为预测。通过支持从句法合规性到复杂意图落地及偏好建模的全面评估，RecRM-Bench 为训练复杂的奖励模型提供了基础数据集。此外，我们提出了一个构建多维奖励模型和集成混合奖励函数的系统框架，为开发可靠且高效的智能体推荐系统奠定了坚实基础。完整的 RecRM-Bench 数据集已在 https://huggingface.co/datasets/wwzeng/RecRM-Bench 公开。

## Abstract
The integration of Large Language Model (LLM) agents is transforming recommender systems from simple query-item matching towards deeply personalized and interactive recommendations. Reinforcement Learning (RL) provides an essential framework for the optimization of these agents in recommendation tasks. However, current methodologies remain limited by a reliance on single dimensional outcome-based rewards that focus exclusively on final user interactions, overlooking critical intermediate capabilities, such as instruction following and complex intent understanding. Despite the necessity for designing multi-dimensional reward, the field lacks a standardized benchmark to facilitate this development. To bridge this gap, we introduce RecRM-Bench, the largest and most comprehensive benchmark to date for agentic recommender systems. It comprises over 1 million structured entries across four core evaluation dimensions: instruction following, factual consistency, query-item relevance, and fine-grained user behavior prediction. By supporting comprehensive assessment from syntactic compliance to complex intent grounding and preference modeling, RecRM-Bench provides a foundational dataset for training sophisticated reward models. Furthermore, we propose a systematic framework for the construction of multi-dimensional reward models and the integration of a hybrid reward function, establishing a robust foundation for developing reliable and highly capable agentic recommender systems. The complete RecRM-Bench dataset is publicly available at https://huggingface.co/datasets/wwzeng/RecRM-Bench.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RecRM-Bench**，一个专为智能体推荐系统（Agentic Recommender Systems）设计的大规模多维度奖励建模基准。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：随着大语言模型（LLM）智能体的引入，推荐系统正从简单的“查询-项目匹配”转向具备推理、规划和工具调用能力的交互式系统。
*   **核心问题**：现有的强化学习（RL）优化主要依赖于单一的、基于结果的奖励（如点击或评分）。然而，智能体推荐涉及多步决策，仅靠终端奖励会导致**奖励稀疏**和**信用分配困难**。
*   **研究动机**：智能体需要具备指令遵循、事实一致性、意图理解等中间能力，但目前缺乏一个标准化的基准来训练和评估这些多维度的奖励模型。

### 2. 方法论：RecRM-Bench 与 RecRM-RL
*   **RecRM-Bench 构成**：包含超过 110 万条来自美团真实场景的结构化数据，分为四个子数据库：
    1.  **指令遵循 (IF)**：涵盖角色、格式、流程、约束、质量、风格 6 个维度，通过数据增强生成负样本。
    2.  **事实一致性 (FC)**：识别“项目幻觉”（虚假商家/属性）和“内容幻觉”（错误知识）。
    3.  **查询-项目相关性 (QIR)**：评估推荐项与用户意图的语义匹配度（0-2 分）。
    4.  **用户行为预测 (UBP)**：包含 100 万条行为预测数据和 7.5 万条精细的项目排序数据。
*   **RecRM-RL 框架**：提出了一种**分层强化学习目标**。
    *   **核心思想**：采用顺序门控机制。只有当响应通过了“指令遵循”和“事实一致性”检查后，系统才会计算“相关性”和“行为预测”奖励。
    *   **公式表达**：总奖励 $R_{total}$ 是各维度奖励的加权乘积与和，通过门控函数确保智能体优先保证基础可靠性，再追求用户偏好优化。

### 3. 实验设计
*   **数据集/场景**：基于美团生活服务平台（餐饮、购物、旅游等 6 大类）的真实交互日志。
*   **对比方法（Baseline）**：
    *   **零样本（Zero-shot）模型**：GPT-4.1、LongCat 系列、DeepSeek V3.2、Qwen3-Max。
    *   **经过 SFT 优化的模型（Ours）**：在 RecRM-Bench 上微调后的 Qwen3 系列模型。
*   **评估指标**：准确率 (ACC)、F1 分数、AUC、命中率 (HR@1/3/5)。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen3-0.6B、8B、14B 作为奖励模型底座；智能体策略模型使用了 Qwen3-235B-A22B。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但从数据规模（110万条）和模型参数量来看，该研究涉及大规模分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 7 种主流大模型在 4 个任务维度上进行了全面评测。
    *   **消融实验**：探讨了数据增强的影响、不同检索器底座（Embedding vs. Reranker）的效果、以及模型参数规模（0.6B 到 14B）的扩展性。
    *   **验证实验**：通过人工标注与模型标注的一致性分析（Cohen’s $\kappa$）验证了数据质量。
*   **充分性评价**：实验设计非常充分，涵盖了从数据质量验证到最终强化学习效果提升的全流程，对比维度客观。

### 6. 主要结论与发现
*   **性能差距**：通用大模型在零样本下表现不均衡，尤其在项目排序和行为预测上远逊于经过专门训练的奖励模型。
*   **训练效率**：引入多维度中间奖励（尤其是相关性奖励）能显著加速强化学习的收敛速度，解决奖励稀疏问题。
*   **模型规模效应**：14B 模型在知识密集型的“相关性”任务中表现最好，而 8B 模型在处理噪声较大的“行为预测”任务时更具鲁棒性。
*   **分层奖励的必要性**：分层门控机制能有效防止模型收敛到幻觉或无关的区域。

### 7. 优点（亮点）
*   **规模最大且最全面**：首次将指令遵循、事实核查、语义相关性和行为预测整合进统一的推荐奖励基准。
*   **真实场景驱动**：数据源自真实生活服务平台，具有极高的实用价值和复杂度。
*   **系统化框架**：不仅提供了数据，还提供了一套完整的多维奖励集成 RL 训练范式。

### 8. 不足与局限
*   **领域局限性**：数据主要集中在生活服务领域（美团），在其他领域（如短视频、新闻推荐）的泛化性有待验证。
*   **LLM-as-a-Judge 风险**：部分标签依赖于教师模型（GPT-4）的蒸馏，可能存在模型偏见或系统性错误。
*   **计算成本**：多维度奖励模型意味着在推理阶段需要调用多个模型进行打分，这会增加系统的延迟和计算开销。

（完）
