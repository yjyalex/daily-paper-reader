---
title: "Rethinking Supervision Granularity: Segment-Level Learning for LLM-Based Theorem Proving"
title_zh: 重新思考监督粒度：基于大语言模型定理证明的片段级学习
authors: "Shuo Xu, Jiakun Zhang, Junyu Lai, Chun Cao, Jingwei Xu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11905v1"
tags: ["query:token"]
score: 9.0
evidence: 定理证明轨迹的分段级监督
tldr: 本研究针对基于大语言模型的 Lean 4 自动定理证明，探讨了监督粒度对模型性能的影响。针对现有的步级（step-level）和全证明（whole-proof）两种极端粒度的不足，提出了“段落级监督”（segment-level supervision）策略。该方法通过提取局部连贯的证明段落进行训练，并在推理时采用目标感知的短回退策略。实验表明，该方法在 miniF2F 等基准测试上显著优于基准模型，并能有效提升现有证明器的效率与成功率。
selection_source: fresh_fetch
motivation: 现有的步级监督容易割裂证明逻辑，而全证明监督生成难度大，需要寻找一种平衡局部信号与全局结构的监督粒度。
method: 提出段落级监督策略，在训练阶段提取局部连贯的证明段落，并在推理阶段利用该策略触发短回退以辅助步级模型。
result: "在 miniF2F 测试集上，该方法训练的模型成功率最高达 66.31%，且能将现有 SOTA 证明器的性能进一步提升并降低推理成本。"
conclusion: 适当的监督粒度能更好地对齐模型学习与证明结构，是提升大语言模型定理证明能力的关键因素。
---

## 摘要
在 Lean 4 中，利用大语言模型进行自动定理证明通常通过结合树搜索的步级（step-level）策略预测或全证明（whole-proof）生成来实现。这两种范式代表了构建监督训练数据的相反粒度：前者提供了密集的局部信号，但可能会割裂连贯的证明过程；而后者保留了全局结构，但需要复杂的端到端生成。在本文中，我们将监督粒度重新审视为证明轨迹上的训练集构建问题，并提出了片段级监督（segment-level supervision），这是一种提取局部连贯证明片段以训练策略模型的训练数据构建策略。我们在推理阶段进一步复用该策略，为现有的步级模型触发短程展开（short rollouts）。当在 STP、LeanWorkbook 和 NuminaMath-LEAN 上使用片段级监督进行训练时，所得策略模型在 miniF2F 上的证明成功率分别达到 64.84%、60.90% 和 66.31%，一致优于步级和全证明基准模型。目标感知展开（Goal-aware rollout）在降低推理成本的同时，进一步提升了现有步级证明器的性能。它将 BFS-Prover-V2-7B 的证明成功率从 68.77% 提高到 70.74%，将 InternLM2.5-StepProver 的成功率从 59.59% 提高到 60.33%，表明适当的监督粒度能使模型学习更好地与证明结构和搜索相对齐。代码和模型可在 https://github.com/NJUDeepEngine/SEG-ATP 获取。

## Abstract
Automated theorem proving with large language models in Lean 4 is commonly approached through either step-level tactic prediction with tree search or whole-proof generation. These two paradigms represent opposite granularities for constructing supervised training data: the former provides dense local signals but may fragment coherent proof processes, while the latter preserves global structure but requires complex end-to-end generation. In this paper, we revisit supervision granularity as a training set construction problem over proof trajectories and propose segment-level supervision, a training data construction strategy that extracts locally coherent proof segments for training policy models. We further reuse the same strategy at inference time to trigger short rollouts for existing step-level models. When trained with segment-level supervision on STP, LeanWorkbook, and NuminaMath-LEAN, the resulting policy models achieve proof success rates of 64.84%, 60.90%, and 66.31% on miniF2F, respectively, consistently outperforming both step-level and whole-proof baselines. Goal-aware rollout further improves existing step-level provers while reducing inference costs. It increases the proof success rate of BFS-Prover-V2-7B from 68.77% to 70.74% and that of InternLM2.5-StepProver from 59.59% to 60.33%, showing that appropriate supervision granularity better aligns model learning with proof structure and search. Code and models are available at https://github.com/NJUDeepEngine/SEG-ATP.

---

## 论文详细总结（自动生成）

这篇论文对基于大语言模型（LLM）的自动定理证明（ATP）中的**监督粒度（Supervision Granularity）**进行了深入探讨，提出了一种介于“单步预测”与“全证明生成”之间的中间地带。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在 Lean 4 形式化证明中，现有的训练范式存在两个极端：
    *   **步级（Step-level）**：每一步预测一个策略（tactic），虽有密集信号但容易割裂连贯的推理逻辑。
    *   **全证明（Whole-proof）**：一次性生成整个脚本，虽保留全局结构但生成难度大、优化困难。
*   **核心问题**：是否存在一种更合适的监督粒度，既能保留证明的局部连贯性，又不会引入过高的生成复杂度？

### 2. 论文提出的方法论
论文将监督粒度建模为证明轨迹上的**边界选择问题**，并提出了**片段级监督（Segment-level Supervision）**：
*   **核心思想**：利用“待证明目标（open goals）数量的变化”作为结构化信号。当目标数量增加（子目标展开）或减少（目标闭合）时，通常意味着一个逻辑阶段的转换。
*   **关键技术细节**：
    *   **片段划分**：在训练阶段，将证明轨迹在目标数量发生变化的节点处切分，形成“状态-策略片段”对。
    *   **目标感知展开（Goal-aware Rollout, GAR）**：在推理阶段，如果模型生成的策略改变了目标数量，则触发一个最大长度为 $H$ 的线性展开（rollout），而不是立即进行树搜索分支。这相当于将多个局部步骤压缩为一个粗粒度的搜索转换。
*   **算法流程**：通过 LeanDojo 重放证明轨迹 -> 提取目标数量变化点 -> 构建片段级数据集 -> 微调模型 -> 推理时结合 GAR 进行搜索。

### 3. 实验设计
*   **数据集**：
    *   **STP**：自博弈生成的合成数据。
    *   **LeanWorkbook**：从自然语言数学题形式化而来的大规模题目集。
    *   **NuminaMath-LEAN**：竞赛级数学题目的形式化版本。
*   **Benchmark**：**miniF2F**（形式化奥数题基准测试）。
*   **对比方法**：
    *   **Step-level**（步级基准）。
    *   **Whole-proof**（全证明基准）。
    *   **现有 SOTA 证明器**：BFS-Prover-V2-7B 和 InternLM2.5-StepProver。

### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA H200 GPU** 进行全参数微调。
*   **推理算力**：使用了 **4 台 NVIDIA A800 GPU** 进行评估。
*   **模型基础**：所有策略模型均基于 **Qwen2.5-Math-7B** 进行微调。
*   **训练配置**：Adam 优化器，学习率 $2 \times 10^{-5}$，训练 3 个 epoch，全局 Batch Size 为 2048。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同来源的数据集上进行了训练，并在各自的测试集及 miniF2F 上进行了交叉验证。
*   **消融实验**：对比了基于 Token 长度、策略距离、状态距离等不同边界选择策略的效果，证明了“目标数量变化”信号的优越性。
*   **敏感性分析**：研究了推理时展开长度 $H$ 对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过在现有开源 SOTA 模型上直接应用 GAR 策略（无需重训），进一步验证了该方法的普适性和公平性。

### 6. 主要结论与发现
*   **性能提升**：片段级模型在 miniF2F 上的表现一致优于步级和全证明模型，最高达到 **66.31%** 的成功率。
*   **效率优化**：GAR 策略显著降低了推理时的 Token 消耗和时间成本。例如，将 InternLM2.5-StepProver 的推理时间减少了约 43%。
*   **优化友好**：训练损失曲线显示，片段级监督比全证明监督更容易收敛，且比步级监督更能捕捉有效的证明结构。

### 7. 优点
*   **视角新颖**：将监督粒度定义为边界选择问题，为数据构建提供了统一的框架。
*   **简单高效**：利用 Lean 引擎自带的“目标数量”作为信号，无需额外的复杂模型或人工标注。
*   **即插即用**：GAR 策略可以直接提升现有步级证明器的性能，具有很强的实用价值。

### 8. 不足与局限
*   **信号近似性**：目标数量的变化只是证明结构的一个近似代理，某些复杂的语义转换可能并不伴随目标数量的变化。
*   **轨迹依赖**：该方法依赖于高质量的已验证证明轨迹，对于极短（1-2步）或高度自动化（如使用 `simp` 等大杀器策略）的证明，片段级的优势会减弱。
*   **回退延迟**：在推理展开过程中，如果早期步骤出错，可能会延迟回退（backtracking）的时机。

（完）
