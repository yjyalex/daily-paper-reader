---
title: "Ishigaki-IDS-Bench: A Benchmark for Generating Information Delivery Specification from BIM Information Requirements"
title_zh: 石垣IDS基准：从BIM信息需求生成信息交付规范的基准
authors: "Ryo Kanazawa, Koyo Hidaka, Teppei Miyamoto, Takayuki Kato, Tomoki Ando, Chenguang Wang, Dayuan Jiang, Naofumi Fujita, Shuhei Saitoh, Atomu Kondo, Koki Arakawa, Daiho Nishioka"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22079v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 评估LLM从BIM需求生成行业标准IDS XML的基准
tldr: "该论文提出了Ishigaki-IDS-Bench基准，用于评估大语言模型从BIM信息需求生成信息交付规范（IDS）XML的能力。基准包含166个专家验证的示例，结合了IDSAuditTool审计和内容一致性评估。在10个LLM的零样本测试中，最佳模型在内容一致性上达到65.6%宏F1，但仅27.7%输出通过内容审计。结果表明当前LLM能部分生成IDS，但难以稳定满足行业标准和约束。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22079v1/fig-001.webp\", \"caption\": \"Table 1: Zero-shot baseline results on Ishigaki-IDS-Bench. Models are grouped by whether their weights are publicly available. Proc., Struct., and Content are Stage 1 audit rates; F1, Recall, and Precision are Stage 2 macro averages over 166 examples.\", \"page\": 4, \"index\": 1, \"width\": 556, \"height\": 325}]"
motivation: 现有评估缺乏同时满足行业标准XML和领域词汇约束的结构化生成基准。
method: 构建包含166个双语示例的基准，使用IDSAuditTool进行流程、结构和内容审计，并结合与黄金IDS文件的内容一致性评估。
result: "最佳模型内容一致性宏F1为65.6%，但仅27.7%输出通过内容审计。"
conclusion: 当前LLM可部分生成IDS，但稳定生成符合IDS标准和IFC词汇约束的XML仍需改进。
---

## 摘要
大型语言模型（LLMs）被广泛用于生成JSON、SQL和代码等结构化输出，然而用于评估同时满足行业标准XML和领域词汇约束的生成的公共资源仍然有限。本文提出了石垣IDS基准（Ishigaki-IDS-Bench），一个用于评估从建筑信息模型（BIM）信息需求生成信息交付规范（IDS）XML能力的基准。该基准包含166个由BIM/IDS专家编写并验证的示例，这些示例通过将83个实际场景扩展为日语和英语版本，相应的黄金IDS文件，以及输入格式、语言、轮次设置、IFC版本和施工领域的元数据。其评估结合了基于IDSAuditTool的可处理性、结构和内容审计，以及针对黄金IDS文件的内容一致性评估。在10个LLM的零样本评估中，最佳模型的内容一致性宏F1达到65.6%，而只有27.7%的输出通过了内容审计。这些结果表明，当前的LLMs能够将部分信息需求表达为IDS，但仍然难以稳定生成满足IDS标准和IFC词汇约束的XML。石垣IDS基准支持比较评估、故障分析，以及符合领域标准的约束结构化生成方法的开发。我们在GitHub和Hugging Face上以CC BY 4.0许可发布了评估脚本和基准数据。

## Abstract
Large language models (LLMs) are widely used to generate structured outputs such as JSON, SQL, and code, yet public resources remain limited for evaluating generation that must simultaneously satisfy industry-standard XML and domain vocabulary constraints. This paper presents Ishigaki-IDS-Bench, a benchmark for evaluating the ability to generate Information Delivery Specification (IDS) XML from Building Information Modeling (BIM) information requirements. The benchmark contains 166 BIM/IDS expert-authored and verified examples created by expanding 83 practical scenarios into Japanese and English, corresponding gold IDS files, and metadata for input format, language, turn setting, IFC version, and construction domain. Its evaluation combines IDSAuditTool-based Processability, Structure, and Content audits with content-agreement evaluation against gold IDS files. In zero-shot evaluation over 10 LLMs, the best model reaches 65.6% macro F1 for content agreement, while only 27.7% of outputs pass the Content audit. These results show that current LLMs can express part of the information requirements as IDS, but still struggle to stably generate XML that satisfies the IDS standard and IFC vocabulary constraints. Ishigaki-IDS-Bench supports comparative evaluation, failure analysis, and the development of constrained structured generation methods that conform to domain standards. We release the evaluation scripts and benchmark data under the CC BY 4.0 license on GitHub and Hugging Face.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大型语言模型（LLMs）广泛应用于生成 JSON、SQL、代码等结构化输出，但在实际工程领域，结构化输出不仅需要语法正确，还必须同时满足行业标准数据格式、领域特定词汇、版本约束以及外部验证工具的审计结果。
- **具体问题**：在建筑、工程和施工（AEC）领域，**信息交付规范（IDS）** 是一种描述 BIM 信息需求的 XML 标准，其生成需要同时满足 XML 结构、IFC 标准词汇、属性集约定、值约束等多个层次的约束。然而，现有公共基准缺乏对这类“领域标准结构化生成”任务的评估。
- **核心挑战**：IDS 生成并非简单的格式转换，而是需要将自然语言或表格形式的信息需求映射到规范的 IDS 规范（specification）中，涉及 IFC 实体、属性、属性集、值约束等多层语义对应。
- **研究动机**：建立首个用于评估 LLM 从 BIM 信息需求生成 IDS 能力的基准，以支持比较评估、故障分析以及符合领域标准的约束生成方法开发。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 构造一个由 BIM/IDS 专家手工编写并验证的基准数据集 **Ishigaki-IDS-Bench**，包含输入（自然语言/CSV）和对应的黄金 IDS 文件，并设计两阶段评估协议，同时衡量生成结果的**形式有效性**和**内容一致性**。

### 关键技术细节
1. **数据集构造**：
   - 收集 83 个实际使用场景（涵盖建筑、结构、机电、通用 BIM 管理等领域），每个场景扩展为日语和英语两种语言，共 166 个示例（输入-输出对）。
   - 每个示例包含：输入上下文（CSV 或自然语言 + 目标 IFC 版本）、黄金 IDS 文件、元数据（输入格式、语言、轮次设置、IFC 版本、施工领域）。
   - 专家团队：6 位 BIM/IDS 专家（包括 buildingSMART 相关活动参与者及行业实践者），经过 5 步骤构建（收集候选 → 手工编写 → 专家审查语义对应 → 工具验证 → 构建英文版），最后由 2 位 ML 专家审查可评估性。
   - 黄金 IDS 文件通过 IDSAuditTool 验证符合 IDS 1.0 标准，并采用正则表达式 `^(?!(Pset_|Qto_)).+` 表示未指定的属性集（作为基准约定）。

2. **两阶段评估协议**：
   - **第一阶段：形式有效性评估**（使用 IDSAuditTool）
     - **Processability（可处理性）**：生成的 XML 能否被工具提取并到达完成审计状态（ok/structure-error/content-error）。
     - **Structure（结构）**：是否符合 XML 结构和 IDS XSD 模式。
     - **Content（内容）**：是否符合 IDS 标准及 IFC 词汇约束。
   - **第二阶段：内容一致性评估（面评分器）**
     - 将黄金 IDS 和生成 IDS 转换为比较块的多重集合（按规范/适用性/需求的划分、面类型、约束内容），通过精确匹配计算精率、召回率、F1。
     - 比较时进行标准化（命名空间、空格、子约束顺序、IFC 实体大小写、简单值列表顺序），但不进行数值等价或属性集名称大小写的归一化。
     - 每个指标按示例计算，报告宏平均。

3. **面（Facet）覆盖**：仅评估 entity、attribute、property（含 dataType 和 cardinality），不包含 classification、material、partOf。

## 3. 实验设计：数据集、基准、对比方法

### 数据集
- **Ishigaki-IDS-Bench**：166 个示例，83 个场景，每个场景包含日语和英语版本。所有示例均由专家编写并验证，具有黄金 IDS 文件和多维元数据。

### 基准对比方法
- **零样本设置**：不提供任何 IDS 生成示例或黄金 IDS 实例，仅通过任务描述和输入需求生成 IDS。
- **对比模型**（共 10 个 LLM）：
  - **封闭专有模型**：GPT-5.5 (xhigh)、Claude Opus 4.5、Gemini 3.1 Pro
  - **开放权重模型**：gpt-oss-120b (xhigh)、Kimi K2.6、DeepSeek V4 Pro、Qwen3.5-397B-A17B、Qwen3-32B、Qwen3-14B、Qwen3-8B
- **解码配置**：除 Qwen 模型使用 temperature=0.6、top_p=0.95、top_k=20、min_p=0.0、max_output_tokens=15000 外，其余模型使用 temperature=0.0、top_p=1.0、max_tokens=15000。
- **评估指标**：第一阶段报告 Processability、Structure、Content 通过率（百分比）；第二阶段报告宏平均 F1、Recall、Precision。

### 实验设置细节
- 所有模型使用相同的任务指令（包括 IDS 基本结构、CSV/自然语言解释、IFC 版本、属性集约定、只输出完整 IDS 的指令），无 IDS 生成示例。
- 评估在 2026 年 5 月进行，未见输出被长度限制截断。

## 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量、训练时长。仅提及使用 API 调用或推断时的解码参数，但未提供计算资源细节（如闭源模型通过 API 调用，开放权重模型可能自行推理，但作者未报告硬件配置）。
- 需要注意：该基准的主要贡献在于数据构建和评估协议，算力需求并非本文重点。

## 5. 实验数量与充分性

- **实验数量**：主要实验为在 10 个 LLM 上的零样本评估，报告了表 1 的结果。未进行少样本、微调、约束解码（如 XGrammar、Grammar-Aligned Decoding）等对比实验。
- **充分性分析**：
  - 实验覆盖了不同规模、不同类型（闭源/开源）的 LLM，能反映当前技术水平的初步诊断。
  - 但仅零样本设置，未探索少样本或微调是否会显著提升性能；也未对比语法约束方法，因此对“模型能力 vs. 约束方法”的区分不够充分。
  - 数据集规模（166 示例）较小，虽经专家精调，但在统计显著性上可能存在局限。
  - 作者明确将本文定位于“诊断性基准”而非全面对比，因此实验数量当前是合理的，但未来的扩展实验（如少样本、微调、约束解码）是必要的。

## 6. 论文的主要结论与发现

- **最佳模型（GPT-5.5）** 的内容一致性宏 F1 仅 65.6%，内容审计通过率仅 27.7%，说明当前 LLM 能部分将信息需求表达为 IDS 面，但稳定生成同时满足 IDS 标准和 IFC 词汇约束的 XML 仍然困难。
- **形式有效性 vs. 内容一致性存在巨大落差**：许多模型 Processability 很高（>94%），但 Content 很低（10%~33%），说明输出可解析但不符合领域标准。
- **多轮设置显著优于单轮**：GPT-5.5 在单轮中 Content 9.0%、F1 58.6%，而在多轮（编辑场景）中 Content 79.5%、F1 84.8%，表明交互式更新 IDS 是更实用的工作流。
- **CSV 输入优于自然语言输入**：表格化需求有助于将需求映射到面和值约束。
- **主要失败类型**：
  1. XML 可提取但违反 IDS XSD 或 IFC 词汇约束。
  2. 属性集名称、基数、值约束表达式错误普遍。
  3. 自然语言输入的目标-值约束对应模糊，导致遗漏或过度生成面。
- **模型规模增大并不能保证满足 IDS 标准**：Qwen 系列从 8B 到 397B，Facet F1 从 20.2% 缓慢提升至 26.8%，但 Content 通过率仍介于 10.2%~19.3%。

## 7. 优点

1. **专家构建的高质量黄金数据**：由 6 位 BIM/IDS 专家手工编写并多重验证，确保语义正确性和标准合规性，避免了合成数据带来的噪声。
2. **两阶段评估协议设计合理**：先审计形式有效性（工具自动化），再评估内容一致性（面级精确匹配），能够区分“可用的 IDS”和“符合需求的 IDS”。
3. **多维元数据分析**：支持按输入格式、语言、轮次、IFC 版本、施工领域进行细粒度分析，有助于定位模型失败的具体场景。
4. **开源发布**：代码和数据集以 CC BY 4.0 许可证公开，易于复现和扩展。
5. **注重实际场景**：场景基于实际使用案例（非机械覆盖所有 IFC 类），具有行业代表性。

## 8. 不足与局限

1. **规模有限**：仅 166 个示例（83 个场景 * 2 语言），在统计上较小，可能无法覆盖 IFC 所有可能情况或罕见边缘案例。
2. **面覆盖不全**：仅评估 entity、attribute、property，未包含 classification、material、partOf 等重要面，限制了评估的全面性。
3. **属性集约定非标准**：使用了正则表达式 `^(?!(Pset_|Qto_)).+` 表示未指定属性集，这是基准人为约定，并非 IDS 标准的一部分，可能导致特殊处理影响结果。
4. **仅零样本评估**：未报告少样本、微调、约束解码等方法的效果，无法判断性能提升的主要来源。
5. **缺乏真实 IFC 模型验证**：基准不包含实际 IFC 文件或噪声文档，因此无法衡量端到端验证能力（如从真实项目文档中提取需求并生成 IDS）。
6. **未提及人工评估或用户研究**：仅依赖自动评估和黄金 IDS，未请领域专家对生成结果进行人工评分，可能存在自动指标未反映的实际可用性问题。
7. **计算资源未披露**：对于开放权重模型，未说明推理所用 GPU 型号和时长，影响复现和成本评估。
8. **语言仅英语和日语**：未覆盖其他语言（如法语、德语、中文等），限制了跨语言推广性。

（完）
