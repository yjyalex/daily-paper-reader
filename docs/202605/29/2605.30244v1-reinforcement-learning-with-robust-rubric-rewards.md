---
title: Reinforcement Learning with Robust Rubric Rewards
title_zh: 基于稳健评分规则的强化学习
authors: "Ya-Qi Yu, Hao Wang, Fangyu Hong, Xiangyang Qu, Gaojie Wu, Qiaoyu Luo, Nuo Xu, Huixin Wang, Wuheng Xu, Yongxin Liao, Zihao Chen, Haonan Li, Ziming Li, Dezhi Peng, Minghui Liao, Jihao Wu, Haoyu Ren, Dandan Tu"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30244v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用规则奖励的强化学习实现细粒度监督和推理
tldr: 针对视觉语言任务中部分可验证且需多标准监督的问题，提出RLR³方法，将RLVR从任务级验证扩展到标准级验证。通过LLM提取器+确定性验证器或LLM法官两种执行路径，并引入最小暴露策略和层次聚合，确保忠实评分。在Qwen3-VL-30B-A3B上评估，RLR³一致优于RLVR，比基础模型提升4.7分，且显著减少假阳性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30244v1/fig-001.webp\", \"caption\": \"Figure 4: The table lists books and their prices.\", \"page\": 21, \"index\": 1, \"width\": 829, \"height\": 865}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of RLR3. Instance-specific rubrics turn response quality assessment into concrete criteria and criterion-level rewards. Verifiable criteria are routed to a text-only LLM-as-an-extractor followed by deterministic verification, while fuzzy criteria are routed to a text-only LLM-as-a-Judge.\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-003.webp\", \"caption\": \"Table 2: Performance comparison on visual benchmarks. In the RLR3 columns, green highlights improvement over RLVR and red highlights degradation. “Official” and “GPT-5 mini” denotes results from the Qwen3-VL technical report. “Base” denotes our own evaluation of the instruct checkpoint.\", \"page\": 7, \"index\": 3, \"width\": 829, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-004.webp\", \"caption\": \"Table 7: Verifier interfaces. These signatures are provided in the dedicated verifier-specification blocks of the rubric-generation and response-scoring prompts. The model then instantiates the corresponding call in the reference field during rubric generation or in the credit field during response scoring.\", \"page\": 22, \"index\": 4, \"width\": 806, \"height\": 1131}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-005.webp\", \"caption\": \"Figure 2: Training trajectories across benchmarks. Each subplot reports benchmark performance over training checkpoints for RLVR and RLR3, with the dashed gray line showing the base model.\", \"page\": 8, \"index\": 5, \"width\": 826, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-006.webp\", \"caption\": \"Table 6: Representative verifier calls. The rubric-side call is generated when constructing the checklist; the scoring-side call is emitted after extracting the candidate response’s prediction.\", \"page\": 19, \"index\": 6, \"width\": 822, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2605.30244v1/fig-007.webp\", \"caption\": \"Figure 3: The diagram illustrates the components of a coal-fired power plant.\", \"page\": 20, \"index\": 7, \"width\": 829, \"height\": 610}]"
motivation: 现有RLVR方法仅适用于完全可验证任务，而视觉语言任务需要多标准细粒度监督，但rubric奖励的在线执行准确性难以保证。
method: RLR³将RLVR扩展到标准级验证，通过两种路径（LLM提取器+确定性验证器或LLM法官）处理不同标准，并用最小暴露策略和层次聚合提升评分可靠性。
result: 在Qwen3-VL-30B-A3B上，RLR³在15个基准上一致优于RLVR，比基础模型提升4.7分，接近官方instruct-to-thinking模型差距。
conclusion: 确定性验证和最小暴露策略能有效减少可利用的假阳性，使rubric奖励在在线RL中更稳健。
---

## 摘要
虽然基于可验证奖励的强化学习（RLVR）对于可确定性检查的任务是有效的，但许多视觉-语言任务是部分可验证的，需要多标准监督（例如，感知细节、推理步骤和约束）。评分规则为这种细粒度监督提供了自然接口，但其有效性取决于在线强化学习过程中的执行准确性。我们提出了基于稳健评分规则的强化学习（$\text{RLR}^3$），将RLVR从任务级验证扩展到标准级验证。$\text{RLR}^3$通过两条执行路径路由特定于实例的评分规则：将LLM作为提取器与确定性验证器配对，或对于不可验证的标准将LLM作为评判者。为了确保忠实评分，$\text{RLR}^3$引入了最小暴露策略，从提取器中隐藏真实情况，从评判者中隐藏图像。此外，$\text{RLR}^3$采用层次聚合优先考虑关键标准而非附加标准，并缓解了采样组内的评分饱和。在15个基准测试中，使用Qwen3-VL-30B-A3B进行评估，$\text{RLR}^3$始终优于RLVR，在基础模型上提升了4.7个百分点，超过了官方指令到思维模型的差距。受控审计确认我们的确定性验证和最小暴露显著减少了可被利用的误报。

## Abstract
While Reinforcement Learning with Verifiable Rewards (RLVR) is effective for deterministically checkable tasks, many vision-language tasks are partially verifiable, demanding multi-criteria supervision (e.g., perceptual details, reasoning steps, and constraints). Rubrics provide a natural interface for this fine-grained supervision, but their effectiveness depends on the execution accuracy during online RL. We propose Reinforcement Learning with Robust Rubric Rewards ($\text{RLR}^3$), extending RLVR from task-level verification to criterion-level verification. $\text{RLR}^3$ routes instance-specific rubrics through two execution paths: an LLM-as-an-extractor paired with a deterministic verifier, or an LLM-as-a-Judge for non-verifiable criteria. To ensure faithful scoring, $\text{RLR}^3$ introduce a minimal exposure strategy that masks ground truths from extractors and images from judges. Furthermore, $\text{RLR}^3$ employs hierarchical aggregation to prioritize essential criteria over additional criteria, and mitigates score saturation within rollout groups. Evaluated on Qwen3-VL-30B-A3B across 15 benchmarks, $\text{RLR}^3$ consistently outperforms RLVR, yielding a 4.7-point improvement over the base model and exceeding the official instruct-to-thinking model gap. Controlled audits confirm our deterministic verification and minimal exposure significantly reduce exploitable false positives.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的基于可验证奖励的强化学习（RLVR）仅适用于任务级结果可确定性检查的场景（如数学、代码），但许多视觉-语言任务（如图文理解、图表分析）属于**部分可验证**范畴——最终答案可检查，但中间感知细节、推理步骤、指令遵循等同样需要细粒度监督。评分规则（Rubric）为这种多标准监督提供了自然接口，但其有效性在在线强化学习中高度依赖执行的准确性，若产生系统性误判会诱导策略利用漏洞。
- **整体含义**：论文提出了一种**基于稳健评分规则的强化学习框架（RLR³）**，将RLVR从任务级验证扩展为**标准级验证**，通过为每个评分标准分配合适的执行路径（确定性验证或语义判断），并引入最小暴露策略和层次聚合，使得评分规则在在线RL中更忠实、更鲁棒，从而提升模型在广泛视觉-语言任务上的表现。

### 2. 论文提出的方法论

- **核心思想**：将每个实例的评分规则分解为多个标准，根据标准是否可验证分配不同执行路径，并遵循三项原则：优先使用可验证路径以限制策略利用空间、限制证据暴露防止捷径、保留多标准区分度进行合理聚合。
- **关键技术细节**：
  - **执行路径**：
    - **可验证标准**：使用**文本-only的LLM作为提取器**，根据候选响应提取预测值，然后由**确定性验证器**（如文本匹配、数学表达式等价、边界框交并比等）计算原始得分。提取器看不到真实目标（目标隐藏），防止直接复制。
    - **模糊标准**：使用**文本-only的LLM作为评判者**，基于文本提示、候选响应、标准描述和文本参考（真实值）输出离散信用分（0/0.5/1）。评判者看不到源图像，防止从图像推断答案。
  - **最小暴露策略**：从提取器隐藏真实目标（`z_x^k`），从评判者隐藏源图像，两条路径均为纯文本LLM，共用相同参数。
  - **层次聚合**：
    - **解耦归一化**：对每个标准，在采样组内根据原始得分与阈值`τ`的关系计算组内下界`ℓ_k`和上界`u_k`，然后线性映射到`[ℓ_k, u_k]`，提高组内区分度，同时避免全组失败时被人工拉高。
    - **分层聚合**：先计算加权基础奖励`r = Σw_k·s̃_k`；再计算内容掩码`m_content`：若任一关键标准得分<0.5或至少两个关键标准得分在[0.5,1)之间则置0；再计算格式掩码`m_format`（重复、语言混杂等违规置0）；最终奖励`r̃ = m_content·m_format·r`。
  - **生成奖励模型（GenRM）的训练**：先使用多教师（多个前沿模型）根据评分规则对候选响应评分，取每个标准的中位数信用作为标签；然后用RLVR目标训练GenRM，监督可检查字段（JSON格式、提取值、信用分），自由格式推理字段不监督。
- **优化算法**：使用群体相对策略优化（GRPO），在线采样G个响应，用上述奖励计算组内优势，最大化加权似然（不含KL惩罚）。

### 3. 实验设计

- **数据集**：
  - **训练数据**：使用三个开源的视觉-语言训练混合集：**ViRL**、**OpenMMR**、**DeepVision**。将部分多项选择题转为开放题，为每个实例生成实例特定的评分规则（包含可验证和模糊标准），并基于基础模型采样的8个rollout过滤出至少有一个关键标准得零分的实例。
  - **测试基准**：15个公开基准，覆盖数学推理（We-Math, DynaMath, MathVision, MathVerse, MathVista）、通用视觉问答（MMMU-Pro, RealWorldQA, MMStar, SimpleVQA）、计数（CountBenchQA）、文档/图表VQA（InfoVQA, DocVQA, ChartQA, CharXiv DQ/RQ）。
- **对比方法**：
  - 基线：**RLVR**（标准任务级验证奖励），使用相同GRPO训练流程，在相同三种训练混合上训练。
  - 参考点：官方Qwen3-VL-30B-A3B的instruct和thinking模型结果、基础模型（用自己的评测）。
- **评估指标**：每个基准上的准确率（规则匹配或GPT-4o-mini评判），以及宏平均。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量及具体训练时长。仅提及：
  - 使用Adam优化器，学习率1e-6，权重衰减0.01。
  - 全局batch size 128，每个输入采样8个rollout，训练1000步，每100步保存一次检查点。
  - 最大prompt/response长度：默认2K/6K token，DeepVision任务扩展至4K/12K token。
  - 使用了多个前沿模型（如GPT-5?）作为rubric生成和教师评分，但未说明具体模型及算力开销。

### 5. 实验数量与充分性

- **实验组数**：论文进行了丰富的实验，包括：
  - **主要对比**（表2）：三种训练混合上RLVR与RLR³的15个基准性能比较（共3×15=45个数值对比）。
  - **训练轨迹分析**（图2）：15个基准上RLVR和RLR³随检查点变化的性能曲线（共30条曲线）。
  - **GenRM可靠性评估**（表4）：比较基础模型、SFT、RLVR训练的GenRM在格式准确率、内容准确率、整体准确率上的表现。
  - **消融/故障模式审计**（表5）：在4种构造的异常响应（无最终答案、无关、貌似错误、对抗性攻击）上，比较默认设置、VLM评判者、无限暴露、无验证器四种条件的假阳性率。
  - **数据过滤统计**（表1）和RM数据统计（表3）。
- **充分性评估**：实验设计比较全面，覆盖了方法对比、稳定性分析、可靠性审计和消融，客观性较好。但所有实验仅基于**单一基础模型（Qwen3-VL-30B-A3B）**，未在其他规模或架构上验证，泛化性有待确认。另外，三种训练混合虽然多样，但均来自公开开源数据，不包含私有任务，实际应用场景覆盖可能有限。

### 6. 论文的主要结论与发现

- **RLR³持续优于RLVR**：在三个训练混合上，RLR³的宏平均均高于RLVR（ViRL：77.7 vs 76.4；OpenMMR：78.1 vs 76.4；DeepVision：78.2 vs 77.4），在大多数单个基准上也有提升，尤其是在We-Math、MathVerse、CountBenchQA等任务上提升显著。
- **提升幅度超过官方instruct-to-thinking差距**：RLR³在基础模型上提升4.7分（73.5→78.2），超过官方报导的instruct→thinking的4.3分差距。
- **训练更加稳定**：RLR³在多个检查点上保持或继续提升，而RLVR容易过早饱和或下降。
- **确定性验证和最小暴露有效**：GenRM审计显示，去除验证器或暴露目标/图像会明显增加假阳性率，证明这两个设计是减少可利用评分漏洞的关键。
- **GenRM通过RLVR训练达到高可靠性**：RLVR训练的GenRM在标准级准确率上达95.0%，优于SFT（94.3%）和基础模型（71.7%）。

### 7. 优点

- **方法创新**：将RLVR从任务级扩展到**标准级验证**，对可验证标准使用确定性执行，对模糊标准使用语义判断，同时用最小暴露策略防止捷径，比单纯的LLM作为评判者更鲁棒。
- **工程设计细致**：包含解耦归一化缓解评分饱和、层次聚合确保关键标准优先、格式掩码处理硬性违规等，使奖励信号更细粒度和可靠。
- **消融和审计充分**：除了常规对比，还设计了专门的故障模式审计，定量展示了确定性验证和最小暴露如何降低假阳性，说服力强。
- **实际性能提升明显**：在15个基准上一致提升，且优于官方模型差距，证明了该方法的价值。

### 8. 不足与局限

- **依赖外部评分规则生成器**：实例特定的评分规则由外部前沿模型生成，未被纳入在线优化循环，可能成为性能瓶颈，尤其当生成器对中间感知证据或推理要求定义不充分时。
- **训练轨迹仍有波动**：图2显示RLR³在一些基准上仍有明显波动（如MathVista、CharXiv RQ），说明所学行为尚未完全稳定，文中也提及需要在线策略蒸馏来整合不同阶段的优势。
- **在OpenMMR上提升较小**：可能是因为该数据集中许多视觉谜题缺乏中间标注，自动评分规则提供的监督有限，提示需要更好的数据标注来支持细粒度监督。
- **仅评估单一大模型**：全部实验在Qwen3-VL-30B-A3B上完成，未验证在更小或不同架构模型上的可迁移性。
- **未报告训练资源消耗**：缺乏GPU型号、数量、训练时长等细节，难以评估方法计算成本。

（完）
