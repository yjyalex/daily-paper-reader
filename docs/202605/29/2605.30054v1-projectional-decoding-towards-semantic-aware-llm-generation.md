---
title: "Projectional Decoding: Towards Semantic-Aware LLM Generation"
title_zh: 投影式解码：迈向语义感知的大语言模型生成
authors: "Boqi Chen, José Antonio Hernández López, Aren A. Babikian"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30054v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 具有语义感知的LLM生成约束解码
tldr: 大型语言模型在软件工程中生成制品时面临语义有效性挑战。本文提出投影解码框架，通过在生成过程中维护部分图模型作为制品的抽象表示，将领域语义直接融入生成过程，实现增量语义验证和错误检测，引导生成语义有效结果。初步实验在程序生成任务上验证了其提升语义有效性的潜力，为LLM的可验证自动化提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30054v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 5120, \"height\": 2580}, {\"url\": \"assets/figures/arxiv/2605.30054v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2605.30054v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 512, \"height\": 512}]"
motivation: 现有约束解码技术缺乏语义验证的通用表示，难以保证LLM生成软件制品的语义有效性。
method: 提出投影解码框架，在生成过程中同步维护部分图模型作为抽象表示，实现增量语义验证和不确定性管理。
result: 在程序生成任务上的初步实验表明，该方法能提升生成制品的语义有效性。
conclusion: 投影解码有望使LLM在多种软件工程活动中实现可验证的自动化生成。
---

## 摘要
大型语言模型（LLMs）越来越多地被用于跨许多软件工程（SE）任务生成软件制品，但确保这些制品的语义有效性仍然是一个基本挑战。现有的约束解码技术可以强制执行语法正确性，并且在某些情况下强制执行特定的语义规则，但缺乏连接LLM生成的文本与SE中语义验证所需推理的通用表示。在本文中，我们提出了投影式解码，这是一种新颖的概念框架，通过在整个生成过程中，在文本旁边维护一个部分图模型作为主要制品表示，直接将领域语义集成到生成过程中。这种抽象表示通过显式捕获不确定性并在本地支持错误检测，实现了增量语义验证，同时以可证明的保证指导生成向语义有效的输出。我们展示了在程序生成任务上的初步结果，这些结果证明了这种方法在改善LLM生成制品的语义有效性方面的潜力。我们还讨论了投影式解码如何能够在各种SE活动中实现与LLM的可验证自动化。

## Abstract
Large language models (LLMs) are increasingly used to generate software artifacts across many software engineering (SE) tasks, yet ensuring the semantic validity of these artifacts remains a fundamental challenge. Existing constrained decoding techniques can enforce syntactic correctness and, in some cases, specific semantic rules, but lack a general representation that bridges LLM-generated text with the reasoning required for semantic validation in SE. In this paper, we propose projectional decoding, a novel conceptual framework that integrates domain semantics directly into the generation process by maintaining, alongside text, a partial graph model as the primary artifact representation throughout generation. This abstract representation enables incremental semantic validation by explicitly capturing uncertainty and natively supporting error detection, while guiding generation toward semantically valid outputs with provable guarantees. We present preliminary results on a program generation task which demonstrate the potential of this approach to improve the semantic validity of LLM-generated artifacts. We also discuss how projectional decoding can enable verifiable automation with LLMs across various SE activities.

---

## 论文详细总结（自动生成）

### 论文总结：Projectional Decoding: Towards Semantic-Aware LLM Generation

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLMs）在软件工程（SE）任务中生成代码、模型等制品时，虽然能保证语法正确性，但经常违反语义约束（如类型一致、结构无环、行为规范等）。现有约束解码（Constrained Decoding）方法主要处理语法，缺乏一种通用表示来桥接LLM生成的文本与SE领域所需的语义验证推理。
- **整体含义**：确保生成制品的语义有效性对于软件的正确性、合规性和集成至关重要。本文提出**投影式解码（Projectional Decoding）**框架，通过将领域语义直接融入解码过程，旨在实现“语义感知”的LLM生成，从而提高生成制品的可靠性，为LLM在SE中的可验证自动化奠定基础。

#### 2. 方法论
- **核心思想**：受投影式编辑（Projectional Editing）启发，在LLM生成文本的同时，始终保持一个**部分图模型（Partial Graph Model）**作为制品的抽象表示。该模型使用四值逻辑（certain, possible, absent, error）标记元素，显式捕获已生成、待生成、已排除和错误状态。解码过程中的每个token都会触发模型的精化（refinement），并基于定义好的语义约束（如图模式、时序逻辑）进行增量验证，只允许不引入违反约束的token继续生成。
- **关键技术细节**：
  - **表示层**：根据领域元模型（Metamodel）构建部分模型，元素对应元模型节点/边，状态标识进度与错误。支持通过现有工具（如Graph pattern matching、抽象解释）检查约束。
  - **解码流程**（结合图1架构）：
    1. LLM根据当前前缀生成下一token的概率分布。
    2. **语法验证**：使用现有工具（如LLGuidance）过滤掉违反语法的token。
    3. **语义验证**：对每个语法有效token，尝试精化部分模型（更新现有元素、引入新元素、转换状态），检查是否违反所有语义约束。若导致**error**元素（约束违反），则丢弃该token；否则保留。
    4. **引导采样**：将剩余语义有效token作为mask作用于LLM输出分布，采样下一个token，并更新部分模型。重复直到终止token或无可选token。
- **算法流程**（文字描述）：循环：LLM forward → 语法过滤 → 语义过滤 → 采样 → 模型精化 → 终止判断。

#### 3. 实验设计
- **数据集/场景**：使用**CLEVR程序生成任务**（视觉问答中生成DSL程序）。该任务要求根据问题描述，生成一个可执行的DSL程序，执行后与正确答案比较。语义约束：类型一致性、操作参数数量（如And需两个输入）、无环结构等。
- **基准（Benchmark）**：无标准基准，但使用CLEVR本身，并对比三种解码策略。
- **对比方法**：
  - **No Guidance**：标准自回归解码，无约束。
  - **Syntactic**：仅使用语法约束解码（工具LLGuidance）。
  - **Semantic（Ours）**：投影式解码（语法+语义约束）。
- **LLMs**：Qwen3的3个变体（4B、8B、14B参数），均使用greedy采样（temperature=0），few-shot包含4个示例。

#### 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等硬件信息。仅报告了语义解码的**相对时间开销**（T列，相对于No Guidance的倍数），分别为1.1x~1.5x。

#### 5. 实验数量与充分性
- **实验数量**：共9组实验（3个LLM × 3种方法），每组结果来自单次运行（greedy采样）。无多次重复或统计显著性检验。未做消融实验（如不同搜索策略、回溯、约束子集影响等）。
- **充分性与客观性**：
  - 作为初步评估（preliminary evaluation），实验设置基本公平（统一temperature、few-shot、约束定义）。但**覆盖范围有限**：仅一个任务（CLEVR），且非标准SE场景（原为视觉问答）。对比方法仅包含语法解码，未与现有语义约束解码方法（如ChopChop、IterGen、逻辑约束解码）直接比较。因此结论的泛化性尚需验证。

#### 6. 主要结论与发现
- **语义有效性显著提升**：在所有LLM上，**语义解码的语义有效性（Sem.）均大幅优于无引导和语法解码**。例如，Qwen3-8B上语义解码Sem.=79.67%，而语法解码Sem.=61.00%，无引导仅60.33%。
- **任务准确率部分提升**：在8B模型上，语义解码达到最高准确率（Acc.=40.00%），优于无引导（38.67%）和语法解码（37.33%）。但在4B和14B上，语义解码准确率略低于或持平于无引导（但语义有效性更高）。
- **语法解码效果有限**：只对最小的4B模型有准确率提升，在8B和14B上甚至略低于无引导，说明仅语法正确性不足以保证高质量。
- **时间开销可接受**：语义解码平均每token生成时间比无引导增加1.1x~1.5x。
- **未达完美语义有效性**：最高Sem.为79.67%（8B），仍有部分输出违反约束，原因是可能进入无有效token的解码路径而提前终止。

#### 7. 优点
- **语义感知创新**：首次提出在解码过程中维护部分模型，实现了对不确定性的显式建模和增量语义验证，弥补了语法解码与语义需求之间的表示鸿沟。
- **可证明保证**：通过约束检查可提供局部语义保证（每个token选择均不违反约束），支持更可靠的生成。
- **框架通用性**：可扩展到多种SE场景（契约式代码生成、DSL、合规检查、跨制品协调等），并支持结合更高级搜索（如回溯）。
- **实验效果明显**：在初步实验中语义有效性提升显著（提升10-20个百分点），且时间开销不大，证明了可行性。

#### 8. 不足与局限
- **实验覆盖不足**：仅在一个任务（CLEVR）上评估，且该任务源自视觉问答，与典型SE场景（如API调用、建模约束）有差距。缺乏在更丰富SE领域的验证。
- **缺乏基线对比**：未与已有的语义约束解码方法（如ChopChop、IterGen、逻辑约束解码）进行比较，无法直接评估相对优势。
- **未处理死胡同**：当所有后续token均导致语义违反时，生成被迫提前终止，可能产生不完整/无效输出。论文未探索回溯或备选路径策略。
- **成本考虑不完整**：语义验证需执行部分模型精化和约束检查，虽然时间开销不大，但未分析大规模约束或复杂模型时的扩展性。
- **依赖内部token分布**：需要访问LLM的完整token概率分布，无法直接应用于闭源/API型LLM。

（完）
