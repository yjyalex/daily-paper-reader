---
title: "T$^2$PO: Uncertainty-Guided Exploration Control for Stable Multi-Turn Agentic Reinforcement Learning"
title_zh: T$^2$PO：面向稳定多轮智能体强化学习的不确定性引导探索控制
authors: "Haixin Wang, Hejie Cui, Chenwei Zhang, Xin Liu, Shuowei Jin, Shijie Geng, Xinyang Zhang, Nasser Zalmout, Zhenyu Shi, Yizhou Sun"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02178v1"
tags: ["query:token"]
score: 10.0
evidence: 针对推理LLM的Token级和Turn级策略优化
tldr: 针对多轮强化学习中大语言模型智能体训练不稳定的问题，本文提出了T$^2$PO框架。该框架通过不确定性引导，在Token级别监控动态并触发思考干预，在Turn级别动态重采样低效率交互，从而优化探索过程。实验证明，T$^2$PO在WebShop、ALFWorld等任务中显著提升了训练稳定性和探索效率，有效防止了训练崩溃并提升了任务性能。
selection_source: fresh_fetch
motivation: 多轮强化学习中低信息量的动作导致探索效率低下，进而引发训练不稳定和模型崩溃。
method: 提出Token和Turn双层策略优化，利用不确定性指标触发思考干预并动态重采样低进展的交互轮次。
result: 在WebShop、ALFWorld和Search QA等多个复杂交互任务中，该方法显著增强了训练稳定性并提升了最终性能。
conclusion: 通过精细化的不确定性引导探索控制，可以有效解决多轮智能体强化学习中的训练崩溃问题并提高学习效率。
---

## 摘要
最近，多轮强化学习（RL）的进展显著提升了推理大语言模型（LLMs）在复杂交互任务中的表现。尽管在细粒度信用分配和轨迹过滤等稳定化技术方面取得了进展，但不稳定性仍然普遍存在，且常导致训练崩溃。我们认为，这种不稳定性源于多轮设置中低效的探索，即策略持续生成低信息量的动作，既不能减少不确定性，也无法推进任务进度。为解决这一问题，我们提出了令牌级与轮次级策略优化（T$^2$PO），这是一个在细粒度层面显式控制探索的不确定性感知框架。在令牌（token）级别，T$^2$PO 监控不确定性动态，并在边际不确定性变化低于阈值时触发思考干预。在轮次（turn）级别，T$^2$PO 识别探索进展微乎其微的交互，并动态地对这些轮次进行重采样，以避免无效的展开（rollouts）。我们在 WebShop、ALFWorld 和 Search QA 等多种环境中评估了 T$^2$PO，结果表明其在训练稳定性方面取得了实质性提升，并通过更高的探索效率实现了性能改进。代码可在以下地址获取：https://github.com/WillDreamer/T2PO。

## Abstract
Recent progress in multi-turn reinforcement learning (RL) has significantly improved reasoning LLMs' performances on complex interactive tasks. Despite advances in stabilization techniques such as fine-grained credit assignment and trajectory filtering, instability remains pervasive and often leads to training collapse. We argue that this instability stems from inefficient exploration in multi-turn settings, where policies continue to generate low-information actions that neither reduce uncertainty nor advance task progress. To address this issue, we propose Token- and Turn-level Policy Optimization (T$^2$PO), an uncertainty-aware framework that explicitly controls exploration at fine-grained levels. At the token level, T$^2$PO monitors uncertainty dynamics and triggers a thinking intervention once the marginal uncertainty change falls below a threshold. At the turn level, T$^2$PO identifies interactions with negligible exploration progress and dynamically resamples such turns to avoid wasted rollouts. We evaluate T$^2$PO in diverse environments, including WebShop, ALFWorld, and Search QA, demonstrating substantial gains in training stability and performance improvements with better exploration efficiency. Code is available at: https://github.com/WillDreamer/T2PO.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《T$^2$PO: Uncertainty-Guided Exploration Control for Stable Multi-Turn Agentic Reinforcement Learning》** 的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管多轮强化学习（RL）在提升 LLM 智能体推理能力方面取得了进展，但训练过程极度不稳定，经常出现“训练崩溃”（性能骤降、KL 散度爆炸）。
*   **核心问题**：作者发现不稳定的根源在于**低效探索（Inefficient Exploration）**，表现为智能体的“犹豫（Hesitation）”：
    *   **Token 级别**：过度思考（Over-thinking），生成大量冗余 Token，但信息增益已饱和。
    *   **Turn 级别**：陷入重复、无效的交互循环，无法从错误中恢复。
*   **目标**：通过显式控制 Token 和 Turn 两个层面的探索过程，实现稳定的多轮智能体训练。

### 2. 方法论：T$^2$PO 框架
核心思想是利用模型内在的**不确定性信号**来动态引导探索。
*   **自校准不确定性信号 ($M_t$)**：结合了归一化后的**香农熵（Entropy）**和**置信度（Confidence）**。熵反映分布平滑度，置信度反映 Top-1 概率，两者互补以更精准地刻画模型状态。
*   **Token 级思考干预 (TTI)**：
    *   监控 $M_t$ 的边际变化。
    *   **规则**：当滑动窗口内的平均变化率低于阈值 $\epsilon$ 时，判定为“思考饱和”，强制插入 `</think>` 标签并转入动作生成，从而切断冗余的思维链（CoT）。
*   **Turn 级动态采样 (TDS)**：
    *   计算单轮内所有 Token 不确定性的聚合值 $\Phi_k$。
    *   **规则**：若当前轮次与前一轮次的信号差异 $\Gamma_k$ 低于阈值 $\eta$，判定为“原地踏步”，则丢弃当前尝试并重新采样（Regeneration），直到产生具有足够探索价值的动作。
*   **策略优化细节**：采用拒绝采样微调（RFT）进行冷启动，引入内存上下文窗口（仅保留最近 $P$ 轮）以降低显存压力，并结合格式惩罚项。

### 3. 实验设计
*   **实验场景/数据集**：
    *   **WebShop**：复杂的在线购物交互环境（1.1M 产品）。
    *   **ALFWorld**：具身智能家务任务（6 大类任务）。
    *   **Search QA**：搜索增强问答（涵盖 NQ, HotpotQA, MuSiQue 等 7 个数据集）。
*   **基准方法 (Benchmarks)**：
    *   **Prompting**：GPT-4o, Gemini-2.5-Pro, Claude 4, Qwen3-32B。
    *   **RL Baselines**：PPO, GRPO, 以及多轮 SOTA 方法 GiGPO 和 DAPO。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA H100 GPUs**。
*   **软件框架**：基于 `verl` 强化学习框架，推理引擎使用 `vLLM`。
*   **训练细节**：针对 Qwen3-4B/8B 模型进行实验，设置了最大 200-250 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个大型交互基准上进行了测试，涵盖了从简单的单跳问答到复杂的多步具身决策。
*   **消融实验**：详细验证了 RFT、TTI 和 TDS 三个核心组件的有效性。
*   **对比实验**：除了与 RL 算法对比，还对比了“长度奖励”、“硬思考预算”等其他思考控制策略。
*   **客观性**：所有实验均在 **3 个不同的随机种子**下运行，并报告了均值和标准差，展示了算法在抑制训练崩溃方面的鲁棒性。

### 6. 主要结论与发现
*   **稳定性显著提升**：T$^2$PO 有效解决了基线方法中常见的训练崩溃问题，KL 散度和梯度范数保持稳定。
*   **性能优越**：在 WebShop 和 ALFWorld 上，成功率比之前的 SOTA（GiGPO）提升了约 **8-12%**。
*   **探索效率高**：在保证成功率的同时，Token 消耗减少了约 **20%**，交互轮次减少了 **16%-25%**。
*   **泛化能力**：在 Search QA 的多跳问答任务中，尤其是在分布外（OOD）数据集上表现出极强的推理鲁棒性。

### 7. 优点
*   **精细化控制**：相比于粗暴的长度限制，基于不确定性的动态干预更符合模型推理的内在逻辑。
*   **即插即用**：该框架可以轻松集成到现有的各种策略优化算法（如 GRPO, GSPO）中。
*   **理论支撑与直觉结合**：通过分析“犹豫”现象背后的不确定性动态，为解决 RL 训练不稳定提供了新的视角。

### 8. 不足与局限
*   **超参数依赖**：阈值 $\epsilon$（Token 级）和 $\eta$（Turn 级）的设定可能对不同规模的模型或不同难度的任务具有一定的敏感性。
*   **离线偏差风险**：虽然文中提到 Turn 级分解对性能影响较小，但在极长轨迹的任务中，off-policy 带来的 staleness（陈旧性）仍是一个潜在风险。
*   **模态限制**：目前实验主要集中在文本交互，尚未在视觉-语言等多模态智能体任务中进行验证。

（完）
