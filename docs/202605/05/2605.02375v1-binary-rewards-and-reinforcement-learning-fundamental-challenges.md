---
title: "Binary Rewards and Reinforcement Learning: Fundamental Challenges"
title_zh: 二元奖励与强化学习：根本挑战
authors: Marc Dymetman
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02375v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用可验证奖励的强化学习来提高语言模型的推理能力
tldr: 本研究探讨了带可验证奖励的强化学习（RLVR）中常见的多样性崩溃问题。研究发现，二元奖励会导致策略梯度方法出现退化，虽然KL控制理论上应引导模型收敛至基座模型在有效样本上的条件分布（过滤模型），但在实际的模型失配情况下，减小KL惩罚系数会迫使策略过度集中于极少数有效输出。本文通过数学推导和实验揭示了这一机制，并指出采用直接针对过滤模型支撑集的替代散度是解决该问题的关键。
selection_source: fresh_fetch
motivation: 旨在解释为何使用二元奖励进行强化学习时，语言模型在推理能力提升的同时会遭遇严重的多样性丧失。
method: 结构化分析了二元奖励下KL控制的退化性质，并研究了模型失配对策略收敛目标的影响。
result: 证明了在实际应用中，降低KL系数会导致模型向极少数高概率有效样本坍缩，而非理想的过滤分布。
conclusion: 传统的KL控制难以兼顾有效性和多样性，未来应探索能直接优化过滤模型覆盖度的替代目标函数。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 已成为提升语言模型推理能力的标准方法，但使用 RLVR 训练的模型往往面临多样性崩溃的问题：尽管单样本准确率有所提升，多样本覆盖率却在下降，有时甚至低于基础模型。我们基于二元奖励的特性，对这一现象提供了结构性的合理解释。二元奖励导致策略梯度方法出现根本性的退化：最大化期望奖励的分布集合是无限的，且缺乏唯一的优选元素。KL 控制通过在 $\beta \to 0$ 的

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a standard approach for improving reasoning in language models, yet models trained with RLVR often suffer from diversity collapse: while single-sample accuracy improves, multi-sample coverage degrades, sometimes falling below the base model. We provide a structural account of this phenomenon grounded in the properties of binary rewards. Binary rewards create a fundamental degeneracy for policy gradient methods: the set of distributions maximizing expected reward is infinite, with no distinguished element. KL-control resolves this degeneracy by selecting, in the limit $β\to 0$, the filtered model $p_*:=a(\cdot\mid\mathcal{Y}_1)$ -- the base model conditioned on validity -- which is the unique fully valid distribution closest to the base model in KL divergence. This selection operates through a nontrivial asymmetry: the tilted distribution $p_{[β]}\propto a(y)\,e^{v(y)/β}$ converges to $p_*$ in forward KL as $β\to 0$, yet $p_*$ cannot serve as a direct optimization target because $\mathrm{KL}(q\,\|\,p_*)$ is infinite for any full-support policy $q$. We develop explicit formulas relating the hyperparameter $β$ to the more interpretable target validity rate $μ$. Under model misspecification -- the typical practical regime -- the pressure to decrease $β$ drives the optimizer toward highly concentrated distributions over a small number of valid outputs, collapsing toward ever fewer as $β$ decreases, rather than toward the filtered model. We illustrate this mechanism on a toy autoregressive experiment and discuss how alternative divergences that target $p_*$ directly -- as pursued empirically by \citet{kruszewski_whatever_2026} -- avoid this failure mode by rewarding coverage of $p_*$'s support rather than concentration on high-validity outputs.

---

## 论文详细总结（自动生成）

### 论文总结：二元奖励与强化学习的根本挑战

#### 1. 核心问题与整体含义
论文探讨了**带可验证奖励的强化学习（RLVR）**中一个普遍存在的矛盾现象：虽然模型在单样本准确率（pass@1）上显著提升，但在多样本覆盖率（pass@k）和输出多样性上往往会出现剧烈下降（即“多样性崩溃”）。研究动机在于从数学和信息几何的角度，解释为什么在二元奖励（正确为1，错误为0）下，标准的KL控制（KL-control）优化方法无法引导模型收敛至理想的“过滤分布”，反而导致模型坍缩到极少数特定的正确答案上。

#### 2. 方法论：核心思想与技术细节
论文通过结构化分析提出了以下核心观点：
*   **二元奖励的退化性**：在纯奖励最大化（如 REINFORCE）下，所有只生成正确答案的分布都是等价的。这导致优化目标在这些分布上是“平坦”的，无法区分多样化分布与单一答案分布。
*   **过滤模型（Filtered Model, $p^*$）**：定义为基础模型在满足验证条件下的条件概率分布。它是理论上既能保证正确性又能最大限度保留基础模型知识的最优目标。
*   **KL控制的不对称性**：
    *   论文证明了当KL惩罚系数 $\beta \to 0$ 时，吉布斯分布 $p_{[\beta]}$ 在**前向KL散度**意义下收敛于 $p^*$。
    *   然而，**反向KL散度** $\text{KL}(q \| p^*)$ 在任何全支撑策略（如标准自回归模型）下都是无穷大的，因为 $p^*$ 的支撑集仅限于正确答案，而模型总有概率生成错误答案。
*   **模型失配（Misspecification）机制**：在实际应用中，自回归模型家族通常无法完美表达 $p^*$。当 $\beta$ 减小时，为了追求极高的奖励，优化器会被迫选择那些极度集中（Near-Dirac）且易于学习的路径（即“背诵”少数正确答案），而非尝试匹配复杂的 $p^*$ 分布。

#### 3. 实验设计
论文设计了一个**玩具级自回归实验（Toy Experiment）**来验证理论：
*   **场景**：一个 3-token 序列生成任务，词表大小为 3。
*   **验证器**：二元验证规则为“第一个 token 必须等于最后一个 token”。
*   **模型设置**：
    *   **基座模型**：一个轻微非均匀的 3-gram 模型。
    *   **策略模型**：一个受限的 **Bigram 模型**。由于 Bigram 无法捕捉跨越两个位置的依赖关系（第1个和第3个 token 的关联），因此该模型相对于目标 $p^*$ 是**失配（Misspecified）**的。
*   **对比基准（Benchmark）**：
    *   前向KL最优 Bigram 策略（最小化 $\text{KL}(p^* \| \pi_\theta)$）。
    *   总变差距离（TVD）最优 Bigram 策略。
    *   理想的过滤模型 $p^*$。

#### 4. 资源与算力
论文**未明确说明**具体的算力资源（如 GPU 型号或数量）。由于实验属于“玩具级”规模（样本空间仅 27 个序列），所有期望值、梯度和分布指标均通过**精确枚举（Exact Enumeration）**计算，而非采样，因此对算力的需求极低，普通的 CPU 即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：主要针对上述 Bigram 失配模型进行了深入分析。
*   **充分性**：为了验证鲁棒性，作者使用了 **8 个不同的随机种子**生成基座模型并重复实验。
*   **评价**：虽然实验规模很小，但对于一篇以**理论证明和机制分析**为主的论文来说，该实验精准地隔离了“模型失配”这一变量，能够清晰地展示 $\beta$ 减小与模式坍缩之间的因果关系，具有较强的说服力。

#### 6. 主要结论与发现
*   **多样性崩溃的根源**：在模型失配的情况下，减小 $\beta$ 会产生一种压力，使模型宁愿放弃 $p^*$ 的多样性，转而收敛到少数几个容易记忆的有效路径上。
*   **$\beta$ 的不可解释性**：论文指出 $\beta$ 是一个难以直观理解的超参数，并推导出了将其转化为更具解释性的“目标有效率 $\mu$”的公式。
*   **目标不可达性**：由于反向KL的结构限制，标准的KL控制目标在结构上就无法直接以 $p^*$ 为目标进行优化。

#### 7. 优点
*   **理论深度**：为 RLHF/RLVR 中观察到的“多样性丧失”提供了严谨的数学解释，超越了单纯的经验观察。
*   **视角独特**：将 KL 控制重新定义为“分布匹配”问题，而非简单的“带惩罚的奖励最大化”。
*   **前瞻性**：指出了当前主流算法（如 PPO, GRPO）在处理二元奖励时的内在缺陷，并为未来改进（如使用前向KL或 $\alpha$-散度）指明了方向。

#### 8. 不足与局限
*   **实验规模有限**：所有结论均基于玩具模型，虽然理论具有通用性，但在千亿级参数的大语言模型（LLM）上的复杂动力学（如学习率、优化器状态的影响）可能比文中描述的更复杂。
*   **未涉及多奖励场景**：论文主要聚焦于单一二元奖励，未探讨多个奖励函数共存或连续奖励值时的竞争情况。
*   **缺乏替代方案的深度实验**：虽然提到了前向KL等替代方案，但未在文中给出这些方案在相同失配条件下的详细对比实验数据。

（完）
