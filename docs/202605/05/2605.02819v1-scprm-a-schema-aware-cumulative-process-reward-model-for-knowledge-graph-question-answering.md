---
title: "SCPRM: A Schema-aware Cumulative Process Reward Model for Knowledge Graph Question Answering"
title_zh: SCPRM：一种用于知识图谱问答的模式感知累积过程奖励模型
authors: "Jiujiu Chen, Yazheng Liu, Sihong Xie, Hui Xiong"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02819v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于评估中间推理步骤的过程奖励模型
tldr: 针对大语言模型在知识图谱问答（KGQA）推理中存在的“风险补偿效应”（即错误步骤被后续正确步骤抵消），本文提出了一种模式感知累积过程奖励模型（SCPRM）。该模型通过结合推理前缀和模式距离来评估推理路径，并集成到蒙特卡洛树搜索（MCTS）中。在医疗、法律等风险敏感领域的实验表明，SCPRM能更准确地引导多步推理，显著提升了问答性能。
selection_source: fresh_fetch
motivation: 现有的过程奖励模型在知识图谱推理中容易出现错误步骤被后续抵消的风险补偿效应，导致对错误路径评分过高。
method: 提出SCPRM模型，通过推理前缀调节和引入当前步骤与目标模式间的距离，结合MCTS进行多步推理引导。
result: "在医疗、法律及CWQ等多个KGQA数据集上，SCPRM-MCTS相比强基线模型在Hits@k指标上平均提升了1.18%。"
conclusion: SCPRM通过引入模式感知和累积奖励机制，有效解决了KG推理中的风险补偿问题，增强了模型在复杂推理任务中的准确性和风险敏感性。
---

## 摘要
大语言模型在复杂推理方面表现出色，但评估其中间步骤仍然具有挑战性。尽管过程奖励模型提供了逐步监督，但它们通常受到风险补偿效应的影响，即错误的步骤被随后的正确步骤所抵消，从而为有缺陷的推理路径分配了高奖励。这一问题在知识图谱（KG）推理中进一步加剧，因为知识图谱中起点和终点实体之间可能存在多条路径，而一个冒险的步骤就可能导致推理路径出现缺陷。这些局限性在医疗和法律知识图谱推理等风险敏感型任务中尤为突出。为了解决这些问题，我们提出了一种模式感知累积过程奖励模型（SCPRM），该模型通过以推理前缀为条件来评估推理路径，并结合了当前推理步骤与从查询中解析出的隐式目标之间的模式距离，从而提供累积和未来奖励以引导路径探索。我们进一步将 SCPRM 集成到蒙特卡洛树搜索（MCTS）中，形成 SCPRM-MCTS，以在知识图谱上进行多跳推理，从而完成问答（QA）任务。在医疗、法律知识图谱问答以及 CWQ 数据集上，SCPRM-MCTS 在 Hits@k 指标上比强基线模型平均提高了 1.18%，证明了其更准确且具备风险敏感性的推理评估能力。

## Abstract
Large language models excel at complex reasoning, yet evaluating their intermediate steps remains challenging. Although process reward models provide step-wise supervision, they often suffer from a risk compensation effect, where incorrect steps are offset by later correct ones, assigning high rewards to flawed reasoning paths. This issue is further exacerbated in knowledge graph (KG) reasoning, as there may exist multiple paths between the start and end entities in the KGs, and a risky step can make the reasoning path flawed. Those limitations are problematic in risk-sensitive tasks such as medical and legal KG reasoning. To address the issues, we propose a Schema-aware Cumulative Process Reward Model (SCPRM) that evaluates reasoning paths by conditioning on the reasoning prefix , and incorporating schema distance between current reasoning step and the implicit target parsed from the query, which provides cumulative and future rewards to guide the path explorations. We further integrate SCPRM into Monte Carlo Tree Search (MCTS) as SCPRM-MCTS to conduct multi-hop reasoning on KGs for question answering (QA) tasks. Across medical and legal KGQA and CWQ, SCPRM-MCTS improves the performance of Hits@k by an average of 1.18% over strong baselines, demonstrating more accurate and risk-sensitive reasoning evaluation.

---

## 论文详细总结（自动生成）

以下是对论文《SCPRM: A Schema-aware Cumulative Process Reward Model for Knowledge Graph Question Answering》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）在知识图谱问答（KGQA）中的推理可靠性问题。
*   **研究动机**：现有的过程奖励模型（PRM）在处理多步推理时存在“风险补偿效应”（Risk Compensation Effect），即推理路径中局部的错误或高风险步骤会被后续正确的步骤在评分上抵消，导致模型对错误路径给出高分。
*   **背景挑战**：在医疗、法律等风险敏感领域，这种补偿是不可接受的。此外，现有的条件奖励模型往往存在“长度偏见”，即推理路径越长，累积奖励越低，这不利于需要多跳推理的复杂 KGQA 任务。

### 2. 方法论：核心思想与技术细节
论文提出了 **SCPRM**，其核心思想是借鉴 **A* 搜索算法**，将推理评估解耦为“累积过去奖励”和“未来奖励”两个维度。
*   **核心公式**：$F(s_k) = G(s_k) + H(s_k)$。
    *   **累积过去奖励 $G(s_k)$**：利用条件概率建模步骤安全性。通过对每一步的安全性概率取对数并求和（$\sum \log(1-p_t)$），确保一旦出现高风险步骤，路径奖励会受到持久且不可逆的惩罚，从而消除风险补偿效应。
    *   **模式感知未来奖励 $H(s_k)$**：通过从查询（Query）中提取隐式推理模式（Schema），计算当前推理路径与目标模式之间的“模式距离”。这为模型提供了前瞻性的引导，且不依赖路径长度，解决了长度偏见问题。
*   **关键技术**：
    *   **训练策略**：采用成对排序损失（Pairwise Loss）来优化路径排名，并结合均方误差（MSE）损失来训练未来奖励预测头。
    *   **搜索集成**：将 SCPRM 作为价值函数集成到蒙特卡洛树搜索（MCTS）中，形成 **SCPRM-MCTS** 框架，引导模型在 KG 上进行高效探索。

### 3. 实验设计
*   **数据集/场景**：
    1.  **医疗 KGQA**：基于 HMDD 和 mirTarbase 构建，涉及 miRNA 与疾病的关系。
    2.  **法律 KGQA**：基于 CrimeKgAssitant 和 CAIL 2025 构建，涉及罪名、法条和刑罚。
    3.  **CWQ (Complex WebQuestions)**：基于 Freebase 的通用复杂多跳问答数据集。
*   **Benchmark 与对比方法**：
    *   **可训练奖励模型**：PQM、CRM、PRM。
    *   **基于 Prompt 的推理方法**：ToG (Think-on-Graph)、ReKG-MCTS。
    *   **基础模型**：Qwen2.5-1.5B、Qwen3-4B、Llama3.1-8B。
*   **评价指标**：成对排序准确率（Pairwise Accuracy）和 Hits@k（医疗/法律用 Hits@3，CWQ 用 Hits@1）。

### 4. 资源与算力
*   **硬件环境**：使用了配备 **AMD EPYC CPU** 和 **4 台 NVIDIA RTX A6000 GPU**（每台 48G 显存）的工作站。
*   **训练细节**：采用 **LoRA**（低秩自适应）进行参数高效微调。优化器为 AdamW，配合余弦学习率调度器，初始学习率为 2e-4。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同领域的数据集上进行了测试，涵盖了从 1.5B 到 8B 不同规模的基础模型。
*   **消融实验**：分别验证了移除“累积奖励（CR）”和“未来奖励（FR）”后的性能变化，证明了两个组件的协同作用。
*   **鲁棒性分析**：针对模式解析中可能存在的噪声，进行了不同噪声比例（0.1 到 0.5）的鲁棒性测试。
*   **评价**：实验设计较为充分，通过跨领域数据集和多种基线的对比，客观地验证了模型在处理风险敏感任务和复杂推理任务中的优越性。

### 6. 主要结论与发现
*   **性能提升**：SCPRM-MCTS 在 Hits@k 指标上比强基线平均提高了 **1.18%**。
*   **风险敏感性**：SCPRM 在成对路径排序任务中表现显著优于传统 PRM，能更有效地识别并拦截包含错误逻辑的路径。
*   **模型规模效应**：较大的基础模型（如 Llama3.1-8B）在绝对性能和抗噪声鲁棒性上均优于较小模型。
*   **组件价值**：累积奖励对步骤级准确性至关重要，而未来奖励对最终的端到端问答性能（Hits@k）影响更大。

### 7. 优点
*   **理论结合紧密**：将经典的 A* 搜索思想成功引入 PRM 建模，逻辑清晰且具有数学解释性。
*   **解决痛点问题**：针对性地解决了 PRM 中的风险补偿效应和长度偏见，这对医疗、法律等严肃应用场景具有重要价值。
*   **模式感知创新**：引入 Schema 距离作为未来奖励的估计，增强了推理的逻辑一致性和前瞻性。

### 8. 不足与局限
*   **模式提取依赖**：未来奖励的计算依赖于从查询中准确提取 Schema，虽然论文中使用了 Few-shot Prompting，但在极端复杂的自然语言查询下，Schema 提取的准确性可能成为瓶颈。
*   **KG 依赖性**：该方法高度依赖于结构化知识图谱的质量和覆盖范围，对于非结构化信息较多的场景适用性有限。
*   **计算开销**：虽然使用了 LoRA，但集成 MCTS 的推理过程相比单次 CoT 仍有较高的计算延迟和算力消耗。

（完）
