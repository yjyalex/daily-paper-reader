---
title: Selector-Guided Autonomous Curriculum for One-Shot Reinforcement Learning from Verifiable Rewards
title_zh: 选择器引导的自主课程：用于基于可验证奖励的单样本强化学习
authors: "Rudray Dave, Vedang Dubey, Smit Deoghare, Sudhakar Mishra"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01823v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过可验证奖励的强化学习来增强LLM的数学推理能力
tldr: 针对大语言模型在数学推理任务中单样本强化学习（1-shot RLVR）的数据选择难题，本文提出了一种选择器引导的自主课程学习方法（SGAC）。该方法摒弃了传统的奖励方差启发式规则，通过学习一个基于多维特征的选择器来动态筛选高质量样本。实验证明，输出不一致性（熵）是预测推理增益的关键指标。在MATH基准测试中，该方法显著提升了Qwen2.5-Math-1.5B模型的性能，超越了现有最先进水平。
selection_source: fresh_fetch
motivation: 现有的单样本强化学习方法主要依赖奖励方差进行样本选择，但这并不能准确衡量样本的迁移价值。
method: 提出SGAC框架，利用包含成功率、熵等特征的可学习选择器，通过动态课程学习和微爆发式GRPO算法优化模型。
result: "在MATH数据集上，该方法将Qwen2.5-Math-1.5B的准确率提升至68.0%，优于现有SOTA模型和基准RLVR方法。"
conclusion: 基于熵的智能数据筛选比静态训练方法更能有效提升模型在数据受限情况下的推理能力。
---

## 摘要
最近，基于可验证奖励的强化学习（RLVR）已被证明是一种基于单实例增强大语言模型（LLM）数学推理能力的极高效技术。目前最先进的单样本 RLVR 模型采用启发式方法选择实例，主要基于奖励的历史方差，但我们发现这种方法作为可迁移性价值的衡量标准本质上具有误导性。在本文中，我们提出了一种选择器引导的自主课程（SGAC）方法，该方法在由成功概率、奖励方差、输出不一致性（熵）和语义难度级别组成的多维特征空间上采用可学习的选择器模型，而非静态的奖励方差启发式方法。在对候选问题池的实证评估中，我们观察到输出不一致性（而非奖励方差）是后续迭代中推理增益的最强预测指标。利用这一发现，我们开发了一种自主课程算法，用于从大型问题池中动态筛选候选问题，通过学习到的选择器对其进行排序，并运行微爆发式的单样本 GRPO。我们的框架在 Hendrycks MATH 基准测试上进行了评估，并以 Qwen2.5-Math-1.5B 模型作为基准。我们的框架在留出数据集上获得了 68.0% 的准确率，优于最先进模型的 64.0% 以及 Wang 等人提出的单样本 RLVR 检查点的 66.0%。结果证实，基于熵的智能数据策划分较之静态训练方法能带来显著的推理能力提升，特别是在数据严重受限的情况下。

## Abstract
Recently, Reinforcement Learning from Verifiable Rewards (RLVR) has been established as a highly effective technique for augmenting the math reasoning skills of Large Language Models (LLMs) based on a single instance. Current state-of-the-art 1-shot RLVR models adopt heuristics for selecting instances, mostly based on historical variance in rewards, which we find to be inherently misleading as a measure of transferability value. In this paper, we propose a Selector-Guided Autonomous Curriculum (SGAC) approach, which employs a learnable selector model on a multi-dimensional feature space consisting of success probability, reward variance, output disagreement (entropy), and semantic difficulty level, instead of the static reward variance heuristic. In our empirical evaluation on pools of candidate problems, we observed that output disagreement, rather than reward variance, is the strongest predictor of reasoning gains in subsequent iterations. Leveraging this finding, we develop an autonomous curriculum algorithm for dynamically siphoning candidate problems from a large pool, ranking them by the learned selector, and running micro-bursts of 1-shot GRPO. Our framework is evaluated using the Hendrycks MATH benchmark, with the Qwen2.5-Math-1.5B model serving as the baseline. Our framework obtains an accuracy of 68.0\% on the hold-out dataset, which is better than the accuracy obtained from the state-of-the-art model, 64.0\%, as well as the 1-shot RLVR checkpoint proposed by Wang et al., which achieved an accuracy of 66.0\%. The results confirm that entropy-based intelligent data curation leads to strict reasoning improvement over static training methods, particularly in severely limited data conditions.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在数学推理任务中的强化学习优化提出了创新见解。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：基于可验证奖励的强化学习（RLVR）能显著提升 LLM 的推理能力。近期研究发现，仅使用“单个训练样本”进行多次强化学习迭代（1-shot RLVR）也能取得惊人效果。
*   **核心问题**：如何从成千上万的问题中挑选出那个最有效的“单样本”？
*   **研究动机**：现有的选择标准通常依赖“奖励方差（Reward Variance）”，即模型对某个问题多次输出的得分波动。作者发现这一标准存在“方差谬误（Variance Fallacy）”——高方差往往由格式错误或解析噪声引起，而非真正的逻辑不确定性，因此不能准确预测学习增益。

### 2. 方法论：选择器引导的自主课程（SGAC）
作者提出了一个三阶段的框架：
*   **阶段 1：信号发现与方差谬误验证**：通过实验对比发现，**输出不一致性（Disagreement/Entropy）**——即模型对同一问题生成的 K 个答案中唯一答案的数量——比奖励方差更能预测下游任务的准确率提升。
*   **阶段 2：训练学习型选择器**：构建一个线性回归模型作为“选择器”，输入四个维度的特征：成功概率（$P_s$）、奖励方差（$\sigma^2_R$）、不一致性（$D$）和语义难度等级（$L$）。
*   **阶段 3：自主课程循环**：
    1.  **筛选（The Sieve）**：从题库中随机抽取一小批候选题目。
    2.  **评估（The Selector）**：利用选择器对候选题目打分，选出得分最高（最具学习价值）的一个。
    3.  **微爆发训练（Training Burst）**：对选中的单个题目运行 5 步 GRPO（群体相对策略优化）算法。
    4.  **持久化与迭代**：通过 LoRA 权重累积学习成果，重复上述过程 20 次。

### 3. 实验设计
*   **数据集**：使用 **Hendrycks MATH** 基准测试集（包含 12,000 个竞赛级数学题）。
*   **基准模型（Baseline）**：Qwen2.5-Math-1.5B。
*   **对比方法**：
    *   原始基准模型。
    *   Wang 等人提出的 SOTA 1-shot RLVR 检查点（基于方差启发式）。
    *   随机选择、最大方差选择、最大不一致性选择等消融策略。
*   **测试集**：从 MATH 中留出 50 个问题作为验证集。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA Tesla T4 GPU** (16GB VRAM)。
*   **时长**：整个 20 步自主课程训练耗时约 **10.9 小时**。
*   **技术细节**：采用了 4-bit 量化（BitsAndBytes）和 QLoRA 技术，以在消费级显卡上运行 1.5B 参数模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   阶段 1 针对 4 个代表性样本进行了深入的信号分析。
    *   主实验运行了 20 轮课程迭代。
    *   进行了针对选择策略、信号贡献（Leave-one-out）和课程长度的消融实验。
*   **充分性评价**：实验设计逻辑严密，通过对比验证了“方差谬误”。但由于算力限制，测试集规模较小（50 题），且选择器模型仅基于 4 个样本训练，虽然在 1.5B 模型上表现良好，但在更大规模模型上的泛化性仍需进一步验证。

### 6. 主要结论与发现
*   **性能提升**：SGAC 框架使模型准确率达到 **68.0%**，超过了基准模型的 64.0% 和之前 SOTA 方法的 66.0%。
*   **关键指标**：**难度等级**和**输出不一致性**是正向预测学习效果的最强信号；而**成功概率**和**奖励方差**往往与学习效果负相关。
*   **非单调学习动态**：训练过程中准确率会出现先升、后降（探索期）、再升（收敛期）的现象，这表明模型在接触极难题目时会经历暂时的性能波动，最终实现跨越式提升。

### 7. 优点
*   **数据高效性**：证明了在极度缺乏数据的情况下，通过“智能筛选”少量高质量样本即可超越大规模盲目训练。
*   **理论突破**：识别并量化了“方差谬误”，为 RLVR 领域提供了更科学的样本选择标准（熵/不一致性）。
*   **低成本**：在单张 T4 显卡上即可完成训练，具有极高的实用价值。

### 8. 不足与局限
*   **测试集偏差**：50 题的测试规模可能存在统计随机性，若能扩展到标准的 500 题测试集会更具说服力。
*   **选择器简单化**：目前仅使用简单的线性回归模型，且训练样本极少，可能无法捕捉更复杂的非线性特征。
*   **计算开销**：虽然训练快，但“筛选”阶段需要对每个候选题目进行多次采样推理，增加了额外的推理成本。

（完）
