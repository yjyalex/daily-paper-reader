---
title: Automated Interpretability and Feature Discovery in Language Models with Agents
title_zh: 基于智能体的语言模型自动化可解释性与特征发现
authors: "Arnau Marin-Llobet, Javier Ferrando"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01555v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 引入了一个用于解释和发现LLM特征的自主多智能体框架
tldr: 本研究提出了一个基于多智能体的自动化机械可解释性框架，旨在解决大语言模型内部特征解释与发现的难题。该系统通过“解释优化”和“特征发现”两个耦合循环运行：前者利用智能体迭代测试假设，后者通过激活空间的k-NN图检索候选特征。在Gemma-2等模型上的实验表明，该方法在解释准确性、特征发现能力及可审计性方面均优于传统的单次自动解释方法，为理解模型内部机制提供了更可靠的工具。
selection_source: fresh_fetch
motivation: 传统的单次自动解释方法难以准确捕捉大语言模型内部特征的复杂性，且缺乏系统性的特征发现机制。
method: 提出一种双循环多智能体框架，结合假设迭代测试的解释优化循环与基于激活空间聚类的特征发现循环。
result: 在Gemma-2模型上的测试显示，该框架能发现语言特定及安全相关的特征，并生成比单次标注更精准、可证伪的解释。
conclusion: 智能体驱动的经验循环显著提升了模型内部特征的可解释性，为自动化机械可解释性研究开辟了新路径。
---

## 摘要
我们介绍了一种用于机械可解释性（mechanistic interpretability）的自主多智能体框架，该框架实现了大语言模型内部特征解释与发现的自动化。该系统运行两个耦合循环：(1) 解释细化，由智能体提出竞争性假设，并通过有针对性的提示词控制和多指标评估进行迭代测试；(2) 特征发现，由智能体生成提示词集，在激活空间中构建 k-最近邻图，并利用统计可分性和语义连贯性标准检索候选特征。在 Gemma-2 系列模型和权重稀疏 Transformer 的 MLP 神经元上，我们的智能体相比单次自动解释有所改进，发现了特定语言和安全相关的特征，并生成了可审计的解释追踪，表明智能体驱动的经验循环比单次标签能产生更清晰、更具可证伪性的解释。

## Abstract
We introduce an autonomous multiagent framework for mechanistic interpretability that automates both explaining and finding internal features in large language models. The system runs two coupled loops: (1) explanation refinement, where an agent proposes competing hypotheses and iteratively tests them with targeted prompt controls and a multi-metric evaluation; and (2) feature discovery, where an agent generates prompt sets, constructs a k-nearest-neighbor graph in activation space, and retrieves candidate features using statistical separability and semantic coherence criteria. On Gemma-2 family models and MLP neurons in weight-sparse transformers, our agent improves over one-shot auto-interpretations, discovers language-specific and safety-relevant features, and produces auditable explanation traces, showing that agent-driven empirical loops yield sharper and more falsifiable explanations than one-shot labels.

---

## 论文详细总结（自动生成）

### 论文总结：基于智能体的语言模型自动化可解释性与特征发现

#### 1. 核心问题与整体含义（研究动机和背景）
理解大语言模型（LLM）内部表示（特征）如何影响输出是机械可解释性研究的核心。然而，当前方法存在两大瓶颈：
*   **特征发现难：** 随着稀疏自编码器（SAE）将激活空间分解为数万个特征，手动寻找与特定行为（如安全、语言）相关的特征极其耗时。
*   **解释不准确：** 现有的自动化解释方法（如 OpenAI 的 Auto-Interp）多为“单次（One-shot）”标注，仅根据高激活样本生成描述，容易忽略反例，缺乏鲁棒性和可证伪性。
**研究动机：** 将可解释性从“静态标注”转变为“动态实验过程”，通过智能体自主进行假设提出、测试与修正。

#### 2. 方法论：InterpAgent 框架
论文提出了 **InterpAgent**，一个由主管智能体协调的双子智能体系统：
*   **FeatureFinder（特征发现智能体）：**
    *   **核心思想：** 利用统计学方法在激活空间中定位区分性特征。
    *   **流程：** 接收带弱标签的提示词集（如“西班牙语” vs “英语”），在 SAE 激活空间构建 **k-最近邻（k-NN）图**，利用 **Leiden 社区检测算法** 进行聚类。
    *   **统计筛选：** 使用 **Wilcoxon 秩和检验** 和 **Benjamini-Hochberg FDR 控制** 识别在特定簇中显著富集的特征，并结合效应量（Cohen's d）进行排序。
*   **FeatureExplainer（解释细化智能体）：**
    *   **核心思想：** 模拟科学实验的“提出假设-实验验证-修正”循环。
    *   **迭代评估：** 维护多个竞争假设，生成对比提示词套件（正例、负例、边缘案例）。
    *   **7项指标评估电池：** 包括检测 F1、模糊测试 F1、惊奇度 AUROC、嵌入相似度、t检验 p值、Cohen's d 以及 LLM 评分。
    *   **多义性检测：** 若多个语义迥异的假设均获得高分，则将该特征标记为“多义性（Polysemantic）”。

#### 3. 实验设计
*   **模型对象：** Gemma-2 系列（2B, 9B）的 SAE 特征，以及一种权重稀疏 Transformer 的原始 MLP 神经元。
*   **数据集/场景：**
    *   **语言发现：** 涵盖 20 种语言的提示词集。
    *   **概念分类：** 代码（Python）、数学、安全相关（有害内容）。
    *   **基准对比（Benchmark）：** 对比单次自动解释（One-shot Auto-interp）以及传统的聚类方法（K-Means, Agglomerative）。
*   **验证方式：** 采用因果干预（激活转向）验证发现的特征是否能实际改变模型行为（如抑制拒绝回答）。

#### 4. 资源与算力
*   **算力说明：** 论文未详细列出训练 SAE 所需的 GPU 型号和时长（通常使用现成的 SAE 权重）。
*   **智能体开销：** 详细记录了 API 调用成本。使用 `gpt-4o-mini` 时，每个特征的解释成本约为 0.006 美元，涉及约 13 次 LLM 调用和 1.7 万个 Token。
*   **库支持：** 使用了 `transformer-lens` 和 `sae-lens` 等标准可解释性库。

#### 5. 实验数量与充分性
*   **实验规模：** 在 1600 个特征上进行了细化实验，并在 20 种语言和多种非语言类别上验证了发现算法。
*   **消融与敏感性：** 针对 FeatureFinder 的超参数（PCA 维度、k 值、聚类分辨率）进行了系统的消融实验。
*   **客观性：** 实验覆盖了不同规模的模型（2B 到 9B）和不同类型的表示（SAE 特征与原始神经元），并引入了因果干预作为最终验证，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **迭代优于单次：** 在 64%-83% 的案例中，智能体迭代细化后的解释优于单次标注，能捕捉到更细微的约束（如“法语口语”而非仅仅是“法语”）。
*   **特征发现有效：** 成功自动定位了安全审计相关的特征，并通过干预实验证明了这些特征在拒绝行为电路中的因果作用。
*   **泛化性强：** 该框架不仅适用于 SAE，也能直接应用于权重稀疏模型的 MLP 神经元，自动找回了人类专家手动分析出的电路组件。
*   **可审计性：** 系统生成了完整的假设演变轨迹和指标报告，增强了可解释性结果的可信度。

#### 7. 优点与亮点
*   **闭环自动化：** 首次将“特征发现”与“假设细化”整合进统一的自主循环中。
*   **严谨的评估：** 引入 7 项量化指标，改变了以往仅靠 LLM 主观描述的现状。
*   **多义性识别：** 能够主动识别并报告特征的多义性，而非强行给出一个标签。
*   **工具通用性：** 框架不依赖特定模型架构，具有很强的迁移潜力。

#### 8. 不足与局限
*   **成本开销：** 相比单次标注，迭代循环的 Token 消耗和 API 成本高出约 34 倍。
*   **依赖基础模型：** 解释的质量高度依赖于底层智能体（如 GPT-4o）生成测试用例和进行逻辑推理的能力。
*   **发现限制：** FeatureFinder 目前仍需要带标签的提示词集作为引导，尚未实现完全无监督的“盲搜”。
*   **收敛性：** 迭代过程不能保证单调收敛，对于极度抽象或上下文高度相关的特征，智能体可能难以生成有效的测试用例。

（完）
