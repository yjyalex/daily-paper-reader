---
title: "Odysseus: Scaling VLMs to 100+ Turn Decision-Making in Games via Reinforcement Learning"
title_zh: Odysseus：通过强化学习将视觉语言模型（VLM）扩展至游戏中的 100+ 轮次决策
authors: "Chengshuai Shi, Wenzhe Li, Xinran Liang, Yizhou Lu, Wenjia Yang, Ruirong Feng, Seth Karten, Ziran Yang, Zihan Ding, Gabriel Sarch, Danqi Chen, Karthik Narasimhan, Chi Jin"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00347v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于长程推理和动作的PPO改进变体
tldr: 本研究针对视觉语言模型（VLM）在长程决策任务中的局限性，提出了Odysseus训练框架。通过在《超级马里奥乐园》等需要100步以上交互的环境中应用强化学习，研究者改进了PPO算法并引入轻量级评论器，显著提升了训练稳定性和采样效率。实验证明，预训练VLM提供的动作先验能大幅减少人工设计需求，使模型在游戏进度上达到前沿模型的3倍以上，并具备良好的泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLM决策方法大多依赖大规模人类轨迹微调或仅适用于短程任务，难以应对需要上百步协调感知与推理的长程游戏环境。
method: 提出了一种改进的PPO算法，结合轻量级回合级评论器并利用预训练VLM的动作先验来优化强化学习过程。
result: Odysseus框架在多个游戏关卡中表现优异，平均游戏进度至少是现有前沿模型的3倍，且在跨游戏泛化中保持了通用领域能力。
conclusion: 本研究为在长程多模态设置下稳定高效地应用强化学习提供了实践指南，推动了VLM作为具身智能体的发展。
---

## 摘要
鉴于视觉语言模型（VLM）能力的快速增长，将其扩展到视频游戏等交互式决策任务已成为一个极具前景的前沿领域。然而，现有方法要么依赖于对人类轨迹的大规模监督微调（SFT），要么仅在相对短时界的设置（通常约为 20-30 轮次）中应用强化学习（RL）。在这项工作中，我们研究了基于强化学习的 VLM 训练，用于《超级马里奥乐园》（Super Mario Land）中的长时界决策。这是一个视觉具身环境，需要 100 轮次以上的交互，并涉及感知、推理和动作的协同。我们首先对关键算法组件进行了系统研究，并提出了一种带有轻量级轮次级评论家（critic）的 PPO 改进变体，与 GRPO 和 Reinforce++ 等无评论家方法相比，该方法显著提高了训练稳定性和样本效率。我们进一步表明，与从头开始训练的经典深度强化学习相比，预训练的 VLM 提供了强大的动作先验，显著提高了强化学习训练期间的样本效率，并减少了对动作工程等人工设计选择的需求。基于这些见解，我们推出了 Odysseus，这是一个用于 VLM 智能体的开放训练框架，在游戏的多个关卡中取得了实质性进展，平均游戏进度至少是前沿模型的 3 倍。此外，训练后的模型在游戏内和跨游戏泛化设置下均表现出一致的改进，同时保持了通用领域的能力。总之，我们的研究结果确定了使强化学习在长时界、多模态设置中保持稳定和有效的关键要素，并为将 VLM 开发为具身智能体提供了实践指导。

## Abstract
Given the rapidly growing capabilities of vision-language models (VLMs), extending them to interactive decision-making tasks such as video games has emerged as a promising frontier. However, existing approaches either rely on large-scale supervised fine-tuning (SFT) on human trajectories or apply reinforcement learning (RL) only in relatively short-horizon settings (typically around 20--30 turns). In this work, we study RL-based training of VLMs for long-horizon decision-making in Super Mario Land, a visually grounded environment requiring 100+ turns of interaction with coordinated perception, reasoning, and action. We begin with a systematic investigation of key algorithmic components and propose an adapted variant of PPO with a lightweight turn-level critic, which substantially improves training stability and sample efficiency over critic-free methods such as GRPO and Reinforce++. We further show that pretrained VLMs provide strong action priors, significantly improving sample efficiency during RL training and reducing the need for manual design choices such as action engineering, compared to classical deep RL trained from scratch. Building on these insights, we introduce Odysseus, an open training framework for VLM agents, achieving substantial gains across multiple levels of the game and at least 3 times average game progresses than frontier models. Moreover, the trained models exhibit consistent improvements under both in-game and cross-game generalization settings, while maintaining general-domain capabilities. Overall, our results identify key ingredients for making RL stable and effective in long-horizon, multi-modal settings, and provide practical guidance for developing VLMs as embodied agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Odysseus: Scaling VLMs to 100+ Turn Decision-Making in Games via Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管视觉语言模型（VLM）在感知和推理方面表现出色，但在需要长程（Long-horizon）交互的决策任务（如视频游戏）中仍面临巨大挑战。
*   **核心问题**：现有的 VLM 微调方法要么依赖难以规模化的人类轨迹监督微调（SFT），要么仅在短时界（20-30 轮次）任务中应用强化学习（RL）。如何在需要 **100 轮次以上** 协调感知、推理与动作的复杂环境中，稳定且高效地训练 VLM 智能体？
*   **整体含义**：本研究证明了通过合理的算法设计，RL 可以使 VLM 在长程决策中达到甚至超越闭源前沿模型的水平，并揭示了预训练 VLM 作为“动作先验”在采样效率上的巨大优势。

### 2. 方法论：核心思想与关键技术
Odysseus 框架结合了轻量级初始化与多任务强化学习，核心组件包括：
*   **带轻量级评论家（Critic）的 PPO**：
    *   **核心思想**：放弃昂贵的、基于大模型的 Token 级 Critic，转而使用一个极其轻量级的 **CNN 卷积神经网络** 作为回合级（Turn-level）Critic。
    *   **优势**：大幅降低了显存和计算开销，同时通过视觉特征直接进行价值估计，提高了长程信用分配（Credit Assignment）的准确性。
*   **正优势过滤（Positive-Advantage Filtering）**：在训练中过滤掉优势值为负的样本（或将其剪裁为零），实验证明这能显著增强 VLM 在复杂环境下的优化稳定性。
*   **Odysseus 训练流水线**：
    1.  **监督初始化（SFT）**：利用 GPT-o3 生成约 5000 帧的感知与推理标注，旨在注入领域知识（如识别马里奥、敌人、障碍物），而非直接学习动作。
    2.  **多任务 RL 与自动课程学习（Auto-Curriculum）**：在多个关卡同时训练，并根据轨迹长度的倒数动态调整采样权重，防止模型在简单关卡过拟合而忽略困难关卡。
*   **结构化思维链（CoT）**：要求模型输出 `<perception>`（感知）、`<reasoning>`（推理）和 `<answer>`（动作），以增强时空建模能力。

### 3. 实验设计
*   **实验场景**：以经典游戏《超级马里奥乐园》（Super Mario Land）为主要测试床，该环境要求 100+ 步的连续正确决策。
*   **Benchmark 与对比方法**：
    *   **前沿模型**：GPT-5.4、Gemini-3-Flash、Claude-Sonnet-4.6、GLM-4.6V、Qwen3-VL-8B-Instruct（基座）。
    *   **RL 算法对比**：GRPO（结果奖励/过程奖励版）、Reinforce++、标准 PPO。
    *   **经典 RL 对比**：从头训练的 CNN-based PPO（评估 VLM 的采样效率）。
*   **泛化性测试**：
    *   **游戏内泛化**：测试未见过的关卡状态。
    *   **跨游戏泛化**：直接在《超级马里奥兄弟》（Super Mario Bros.）的 32 个关卡上进行零样本/微调评估。
    *   **通用能力测试**：在 MMMU、MathVision 等通用多模态榜单上评估是否发生灾难性遗忘。

### 4. 资源与算力
*   **训练规模**：论文提到进行了“数千万次（tens of millions）”的交互采样。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）和具体数量，但提到使用了普林斯顿语言与智能中心（PLI）提供的计算资源。
*   **效率提升**：由于采用了轻量级 CNN Critic，其显存占用远低于传统的 Actor-Critic VLM 训练方案。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个不同的游戏关卡，进行了大量的消融实验（针对 Critic 设计、优势过滤、SFT 的必要性等）。
*   **充分性**：实验设计非常全面。不仅对比了主流的 RL 算法（如 GRPO），还深入探讨了 VLM 相比于传统深度强化学习在动作空间工程上的简化。
*   **客观性**：通过多次独立运行（至少 2 次，部分 8 次或 256 次）并报告均值与标准差，确保了结果的可重复性和公平性。

### 6. 主要结论与发现
*   **性能飞跃**：Odysseus 的平均游戏进度是前沿模型（如 GPT-5.4）的 **5 倍**，是 GLM-4.6V 的 **3 倍**。
*   **算法关键**：在长程任务中，带 Critic 的 PPO 远优于无 Critic 的方法（如 GRPO），而正优势过滤是稳定训练的关键。
*   **VLM 的价值**：预训练 VLM 提供了强大的动作先验，其采样效率比从头训练的经典 RL 高出约 **2 倍**，且无需复杂的人工动作空间设计。
*   **能力保持**：在大幅提升游戏决策能力的同时，模型在通用多模态任务上的表现几乎没有下降。

### 7. 优点（亮点）
*   **轻量化设计**：巧妙地用 CNN Critic 解决了 VLM 做 RL 时计算量巨大的痛点。
*   **长程突破**：成功将 VLM 的决策步数从几十步扩展到了上百步，具有很强的工程参考价值。
*   **泛化验证**：不仅在训练关卡表现好，还展示了在完全不同的游戏（马里奥兄弟）中的迁移能力。

### 8. 不足与局限
*   **环境单一性**：主要集中在平台跳跃类游戏（马里奥系列），尚未在 3D 开放世界或更复杂的策略游戏中验证。
*   **SFT 依赖**：虽然是轻量级 SFT，但仍需要强模型（如 GPT-o3）提供初始的感知标注，对于完全缺乏教师模型的领域可能受限。
*   **算力细节缺失**：未公开具体的训练时长和硬件消耗，不便于其他研究者评估复现成本。

（完）
