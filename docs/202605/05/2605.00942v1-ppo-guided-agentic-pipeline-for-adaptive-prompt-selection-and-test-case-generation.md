---
title: PPO guided Agentic Pipeline for Adaptive Prompt Selection and Test Case Generation
title_zh: PPO引导的用于自适应提示词选择和测试用例生成的智能体流水线
authors: "Gourisetty Venkata Sai Koushik, Dama Aditya, Mahankali Harish Sai, Peddi Siddarhta, Shadab Ahmad, Vivek Yelleti"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00942v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于 PPO 的策略网络用于引导智能体流水线中的提示词选择
tldr: 本研究针对大规模复杂软件系统的测试用例生成难题，提出了一种基于强化学习驱动的智能体框架。该框架结合了近端策略优化（PPO）算法与大语言模型（LLM），通过两阶段流程实现：首先利用ToT引导的智能体精简源代码，随后利用PPO策略网络根据代码复杂度和覆盖率指标动态选择最优提示词策略。实验表明，该方法在分支和行覆盖率上显著优于CBMC等传统工具及现有LLM方法，展现了自适应提示词选择在复杂程序测试中的优越性。
selection_source: fresh_fetch
motivation: 针对具有海量且嵌套复杂的源代码的大规模软件系统，传统方法难以生成能够充分覆盖程序的测试用例。
method: 提出一种两阶段框架，先通过ToT优化智能体精简代码，再利用PPO策略网络根据实时覆盖率动态选择八种不同的LLM提示词技术。
result: 在20个基准程序上的实验显示，PPO-LLM在分支和行覆盖率上全面超越了CBMC、kS-LLM和kS-LLM++。
conclusion: 实验证明，由PPO驱动的自适应提示词选择策略在提升复杂程序测试覆盖率方面比静态提示词策略更具优势。
---

## 摘要
开发能够彻底测试大规模软件系统的有效测试用例本质上是困难的，特别是当这些系统具有庞大、复杂且深度嵌套的源代码时。在这项工作中，我们提出了一种使用强化学习驱动的智能体框架生成测试用例的新方法，其中近端策略优化（PPO）与大语言模型（LLM）引擎相结合，以在测试生成过程中引导提示词选择。我们的方法分为两个阶段：在第一阶段，受思维树（ToT）引导的优化智能体通过在不改变源代码功能行为的情况下消除冗余，对源代码进行划分和最小化；在第二阶段，训练一个基于PPO的策略网络，以解决从八种不同的提示技术（如边界值分析、随机模糊测试等）中选择提示词的问题。该网络基于输入的11维状态向量（代表源代码复杂度指标和实时覆盖率指标），引导LLM引擎探索程序中未访问的路径。PPO智能体根据行覆盖率和分支覆盖率的增加、对未探索分支的惩罚以及对减少源代码长度的奖励的组合来获取奖励。对20个基准程序进行的实验表明，在几乎所有情况下，对于从BOUND~1到BOUND~2000的各种循环边界值，所提出的PPO-LLM方法在分支和行覆盖率方面均优于CBMC、kS-LLM和kS-LLM++。在BOUND~1时，PPO-LLM在PALS套件上的分支覆盖率为100%，而kS-LLM++约为86.8%。这证实了由PPO驱动的自适应提示词选择在PALS类型程序上显著优于静态提示策略。

## Abstract
Developing effective test cases capable of thoroughly exercising large-scale software systems is inherently difficult, especially if such systems have voluminous, complex, and deeply nested source codes. In this work, we present a novel approach for generating test cases using a reinforcement learning-driven agentic framework where Proximal Policy Optimization (PPO) is coupled with an LLM engine to guide prompt selection during test generation. Our approach consists of two phases. In Phase I, the ToT-guided optimization agent partitions and minimizes the source code by removing redundancies without changing the functional behavior of the source code. In Phase II, a PPO-based policy network is trained to solve the problem of selecting prompts among eight different prompting techniques, such as Boundary Value Analysis, Random Fuzzing, etc., based on the inputted 11-dimensional state vector representing the source code complexity metrics and live coverage metrics to direct the LLM engine towards exploring unvisited paths in the program. The PPO agent receives rewards based on a combination of increases in line and branch coverages, penalties for unexplored branches, and rewards for reducing source code length. From experiments conducted on twenty benchmark programs, it is evident that the proposed approach, PPO-LLM, outperforms CBMC, kS-LLM, and kS-LLM++ in terms of branch and line coverage in almost all cases, for various loop bound values ranging from BOUND~1 to BOUND~2000. While at BOUND~1, the coverage of branches is 100\% using PPO-LLM on the PALS suite, in comparison, it is around 86.8\% using kS-LLM++. This confirms that adaptive prompt selection driven by PPO substantially outperforms static prompting strategies on PALS type programs.

---

## 论文详细总结（自动生成）

这是一份关于论文《PPO guided Agentic Pipeline for Adaptive Prompt Selection and Test Case Generation》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在大规模软件系统中，自动化测试用例生成（ATCG）面临巨大挑战。传统方法（如符号执行、CBMC）在处理复杂嵌套代码时容易遭遇“状态爆炸”；而新兴的大语言模型（LLM）虽然具备强大的代码理解力，但其效果高度依赖提示词（Prompt）质量。
*   **核心问题**：静态提示词策略在覆盖率提升到一定阶段后会进入瓶颈期（Plateau），无法针对未覆盖的深层逻辑动态调整策略。
*   **整体含义**：论文提出了一种结合强化学习（PPO）与LLM的智能体流水线，通过动态选择最合适的提示词策略来最大化程序的行覆盖率和分支覆盖率。

### 2. 方法论：核心思想与关键技术
该方法分为两个主要阶段：
*   **阶段 I：代码优化（基于思维树 ToT）**
    *   **碎片化**：将原始C代码分解为语义连贯的模块。
    *   **ToT重构**：利用LLM智能体通过思维树搜索，在保持逻辑等价的前提下消除冗余代码、简化条件表达式。
    *   **等价验证**：确保优化后的代码与原代码功能一致，从而降低LLM在第二阶段的认知负荷（Token数）。
*   **阶段 II：PPO引导的自适应提示词选择**
    *   **MDP建模**：将提示词选择建模为马尔可夫决策过程。
    *   **状态空间（11维向量）**：包括代码行数（LOC）、函数/分支/循环数量、循环复杂度、当前累计行/分支覆盖率、语言类型及生成进度。
    *   **动作空间（8种提示词模板）**：包括边界值分析（BVA）、分支覆盖探索（BCE）、边缘情况猎人（ECH）、错误路径练习（EPE）、循环边界测试（LBT）、数据类型压力（DTS）、路径覆盖最大化（PCM）和随机模糊测试（FUZZ）。
    *   **奖励函数**：由行/分支覆盖率增量、测试用例唯一性比例、未测试分支惩罚以及代码压缩奖励组成。
    *   **策略更新**：使用近端策略优化（PPO）算法，通过剪切代理目标函数和广义优势估计（GAE）来训练策略网络。

### 3. 实验设计
*   **数据集/场景**：选取了20个来自 PALS（过程代数标记系统）和 RERS（反应式系统严格检验）基准测试集的工业级C程序。
*   **实验规模**：每个程序设置了8个不同的循环边界（BOUND 1 到 2000），共计160个测试实例。
*   **对比方法（Baselines）**：
    1.  **CBMC**：传统的有界模型检测工具。
    2.  **kS-LLM**：集成k步缓存的LLM框架。
    3.  **kS-LLM++**：在kS-LLM基础上增加了ToT代码优化的版本。
*   **评估指标**：分支覆盖率（Branch Coverage）、行覆盖率（Line Coverage）和代码行数缩减率（LOC Reduction）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如GPU型号、数量）以及策略网络的具体训练时长。
*   **实现细节**：提到策略网络是一个简单的两层多层感知机（MLP，结构为11→32→8），这种轻量级设计旨在实现单次生成运行中的快速收敛，无需预训练。

### 5. 实验数量与充分性
*   **实验数量**：进行了160组实验实例，覆盖了从简单到极高复杂度的循环边界，实验量较为充足。
*   **充分性与公平性**：
    *   对比了传统工具与最先进的LLM方法，涵盖了不同类型的程序结构（并行系统与反应式系统）。
    *   设置了1小时的超时限制（针对CBMC），符合工业测试场景。
    *   **局限性**：实验主要集中在C语言，虽然状态向量包含语言指示符，但未在其他编程语言上进行广泛验证。

### 6. 主要结论与发现
*   **PPO-LLM 优势显著**：在 PALS 基准测试中，PPO-LLM 在几乎所有边界设置下均达到 95%-100% 的分支覆盖率，远超 CBMC 和静态提示词方法。
*   **自适应优于静态**：实验证明，根据实时覆盖反馈动态切换提示词策略，能有效突破覆盖率停滞。
*   **代码优化的价值**：当代码行数缩减超过60%时，测试覆盖率提升尤为明显，证实了减少LLM“认知负荷”的重要性。
*   **特定场景的局限**：在 RERS（PS系列）程序中，PPO-LLM 表现有时逊于 kS-LLM++。这是因为此类程序的分支依赖于长序列的环境输入，单步奖励信号难以捕捉深层依赖。

### 7. 优点
*   **创新性**：首次将 PPO 引入到 LLM 提示词策略的选择中，实现了测试生成的闭环反馈。
*   **两阶段协同**：结合了代码精简（ToT）与动态生成（PPO），解决了大模型处理长代码时的幻觉问题。
*   **策略多样性**：设计的8种提示词模板涵盖了软件测试的主要核心方法，具有很强的工程实用性。

### 8. 不足与局限
*   **长依赖问题**：对于状态高度依赖输入序列的反应式系统，当前的单步奖励机制（MDP）存在局限，学习效率较低。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，难以评估其在资源受限环境下的部署成本。
*   **语言覆盖面**：虽然框架理论上通用，但实验仅限于C语言，跨语言的泛化能力有待验证。
*   **奖励函数敏感性**：奖励函数的权重分配（如0.4, 0.5等）具有一定的经验性，可能需要针对不同类型的软件进行微调。

（完）
