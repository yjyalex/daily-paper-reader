---
title: "CP-SynC: Multi-Agent Zero-Shot Constraint Modeling in MiniZinc with Synthesized Checkers"
title_zh: CP-SynC：利用合成检查器在 MiniZinc 中进行多智能体零样本约束建模
authors: "Yuliang Song, Eldan Cohen"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01675v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 带有合成检查器的零样本约束建模多智能体工作流
tldr: 针对将自然语言描述转化为 MiniZinc 约束规划模型的难题，本文提出 CP-SynC 框架。该框架采用多智能体协作模式，通过建模智能体生成模型，并利用验证智能体合成语义检查器提供反馈。通过并行探索多条建模路径并结合选择智能体进行证据聚合，CP-SynC 在零样本环境下显著提升了建模准确性，有效解决了 LLM 在缺乏验证时易产生语义错误的问题。
selection_source: fresh_fetch
motivation: 现有的 LLM 在将自然语言转化为约束规划模型时，由于缺乏测试时的验证机制，容易产生细微的语义错误。
method: 提出一种多智能体工作流，通过建模智能体生成候选模型，验证智能体合成语义检查器提供反馈，并由选择智能体聚合多条路径的证据。
result: 在包含 100 个约束规划问题的基准测试中，CP-SynC 的表现显著优于现有的 MiniZinc 建模基准方法。
conclusion: CP-SynC 证明了通过合成检查器和多智能体协作，可以有效提升 LLM 在复杂约束建模任务中的零样本性能。
---

## 摘要
约束编程 (CP) 是解决组合问题的一种强大范式，但将自然语言问题描述转化为可执行模型仍然是一个重大瓶颈。虽然大语言模型 (LLM) 在自动化这种转化方面展现出潜力，但在测试时缺乏真值验证的情况下，它们往往难以处理细微的语义错误。为了解决这一问题，我们引入了 CP-SynC（基于合成检查器的约束编程建模），这是一种在 MiniZinc 中进行零样本约束建模的多智能体工作流。CP-SynC 协调建模智能体（生成并优化候选模型）和验证智能体（合成语义检查器以提供语义正确性反馈）。为了减轻单个 LLM 输出中固有的噪声，CP-SynC 并行探索多条建模轨迹，并利用选择智能体通过多智能体证据聚合来选择最终模型。在包含 100 个 CP 问题的基准测试上进行的广泛实验表明，CP-SynC 在 MiniZinc 建模方面显著优于现有的基准方法。

## Abstract
Constraint Programming (CP) is a powerful paradigm for solving combinatorial problems, yet translating natural language problem descriptions into executable models remains a significant bottleneck. While Large Language Models (LLMs) show promise in automating this translation, they often struggle with subtle semantic errors in the absence of oracle validation at test time. To address this, we introduce CP-SynC (Constraint Programming modeling with Synthesized Checkers), a multi-agent workflow for zero-shot constraint modeling in MiniZinc. CP-SynC coordinates modeling agents that generate and refine candidate models and validation agents that synthesize semantic checkers to provide feedback on semantic correctness. To mitigate noise inherent in individual LLM outputs, CP-SynC explores multiple modeling trajectories in parallel and employs selection agents to select the final model via multi-agent evidence aggregation. Extensive experiments on a benchmark of 100 CP problems show that CP-SynC substantially outperforms existing baselines in MiniZinc modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《CP-SynC: Multi-Agent Zero-Shot Constraint Modeling in MiniZinc with Synthesized Checkers》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何自动化地将自然语言描述的组合优化问题转化为可执行的约束编程（CP）模型（如 MiniZinc 语言）。
*   **研究动机**：
    *   **建模瓶颈**：尽管求解器技术先进，但将非形式化需求转化为精确模型需要极高的专业知识。
    *   **LLM 的局限性**：大语言模型（LLM）在生成代码时常犯细微的语义错误。语法错误可以通过编译器反馈修复，但语义错误在缺乏“真值（Oracle）”验证的情况下极难识别。
    *   **零样本挑战**：在实际应用中，通常没有现成的单元测试或参考模型来验证 LLM 生成的模型是否正确。

### 2. 方法论：CP-SynC 框架
CP-SynC 采用多智能体协作工作流，核心思想是**“测试驱动的建模”**，即利用 LLM 自行合成检查器来验证生成的模型。
*   **核心角色**：
    1.  **建模智能体 (Modeling Agents)**：生成 MiniZinc 模型，并根据反馈进行迭代修复。
    2.  **验证智能体 (Validation Agents)**：仅根据问题描述合成 Python 形式的“语义检查器（Checkers）”，用于验证模型输出的解是否符合约束。
    3.  **选择智能体 (Selection Agents)**：聚合多个候选模型及其检查结果，通过证据评估选出最终模型。
*   **关键技术流程**：
    *   **多样化采样**：通过提示词变体（如增加规划步骤）或温度采样生成多条并行的建模轨迹。
    *   **分阶段检查流水线 (G1-G4)**：
        *   **G1 (语法)**：MiniZinc 编译器检查。
        *   **G2 (求解状态)**：确保求解器能找到解（非 UNSAT）。
        *   **G3 (输出格式)**：验证结果是否符合要求的 Schema。
        *   **G4 (语义检查)**：使用合成的 Python 检查器进行软验证。
    *   **多智能体证据聚合**：选择智能体不仅看检查器是否通过，还会分析检查器本身是否存在逻辑漏洞，通过多数投票决定最终输出。

### 3. 实验设计
*   **基准测试 (Benchmark)**：构建了一个包含 **100 个 CP 问题**的自动化基准测试，涵盖工业调度、交通运输、学术难题等。
    *   30 个问题来自 CPEval，70 个新问题来自 CSPLib 和 PyCSP3。
    *   每个问题配有自然语言描述、输入/输出规范、参考模型及数据实例。
*   **对比方法**：
    *   **2-Step (2S)**：基础的生成-修复两步法。
    *   **CP-Bench**：目前最先进的基于 LLM 的 MiniZinc 建模系统。
*   **评估指标**：解准确率 (Solution Accuracy, SA)，即生成的模型解是否满足参考模型的所有约束（对于优化问题还需验证最优性）。
*   **测试模型**：GPT-4o, DeepSeek-V3, Claude-Sonnet-4, Qwen3-Next, Gemini-2.5-flash-lite。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **推理成本**：由于该方法基于现有的闭源或开源 LLM API 进行推理，其主要开销在于推理时的 Token 消耗。实验中设置了每个候选模型最多 4 次修复尝试，以及 1 次重启预算。

### 5. 实验数量与充分性
*   **实验规模**：在 5 种主流 LLM 上进行了完整测试，每种方法运行 3 次取平均值，总计进行了数百次端到端建模实验。
*   **消融实验**：详细分析了建模、验证、选择三种智能体各自的贡献，验证了多轨迹建模优于单轨迹修复。
*   **充分性评价**：实验设计较为充分，不仅对比了不同模型，还探讨了推理预算（Test-time Scaling）的分配策略，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：CP-SynC 在所有测试的 LLM 上均显著优于基准方法。例如，在 DeepSeek-V3 上，SA 从 56.7% 提升至 77.7%。
*   **小模型增益更大**：Gemini-2.5-flash-lite 在使用 CP-SynC 后，准确率从 19% 飙升至 67%，证明了该框架能有效弥补基础模型能力的不足。
*   **多轨迹优于深修复**：实验发现，与其在一条错误的路径上反复修复（容易陷入局部最优），不如开启多条并行轨迹并进行鲁棒选择。
*   **检查器有效性**：尽管 LLM 合成的检查器本身可能有错，但通过多智能体聚合，系统能够识别并忽略错误的反馈。

### 7. 优点
*   **闭环验证**：在没有外部真值的情况下，创造性地利用 Python 合成检查器为 MiniZinc 建模提供反馈。
*   **鲁棒性设计**：通过“选择智能体”解决了 LLM 自我检查（Self-Correction）中常见的“盲目采纳错误反馈”问题。
*   **自动化程度高**：提供了一套完整的从自然语言到可验证解的端到端流程。

### 8. 不足与局限
*   **推理成本**：多智能体、多轨迹采样意味着更高的 API 调用成本和更长的推理延迟。
*   **预算分配**：目前采用固定的重启和修复次数，缺乏根据问题难度动态分配算力的机制。
*   **检查器噪声**：虽然有聚合机制，但如果 LLM 对某个复杂约束的理解存在系统性偏差，合成的检查器和模型可能会同时出错（共模失效）。

（完）
