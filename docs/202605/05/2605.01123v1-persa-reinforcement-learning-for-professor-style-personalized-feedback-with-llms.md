---
title: "PERSA: Reinforcement Learning for Professor-Style Personalized Feedback with LLMs"
title_zh: PERSA：基于大语言模型的教授风格个性化反馈强化学习
authors: "Ravi Ranjan, Utkarsh Grover, Xiaomin Lin, Agoritsa Polyzou"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01123v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用PPO进行个性化反馈的RLHF流水线
tldr: 本研究针对大语言模型在教育反馈中难以兼顾教师风格与诊断准确性的挑战，提出了 PERSA 框架。该框架利用 RLHF 技术，通过监督微调、奖励建模和 PPO 算法，并结合参数高效微调策略（仅更新顶部 Transformer 层），使模型能够精准模仿教授的批改口吻。实验表明，PERSA 在保持代码反馈正确性的同时，显著提升了风格对齐度，为个性化教学辅助提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在生成教育反馈时，难以在保持诊断准确性的同时模仿特定教师的语言风格和语气的问题。
method: 提出 PERSA 框架，通过 RLHF 流程结合参数高效微调（仅更新顶部 Transformer 块和前馈投影），在保留核心知识的同时实现风格对齐。
result: "在多个代码反馈基准测试中，PERSA 显著提升了风格对齐得分（如从 34.8% 提升至 96.2%），同时保持了极高的正确率。"
conclusion: PERSA 为个性化教育反馈提供了一种实用路径，成功实现了内容正确性与教师风格化表达的平衡。
---

## 摘要
大语言模型（LLMs）可以在教育场景中提供自动反馈，但在保持诊断正确性的同时，将 LLM 的风格与特定教师的语气对齐仍然具有挑战性。我们探讨了如何更新用于自动反馈生成的 LLM，使其在不牺牲核心知识的情况下与目标教师的风格对齐。我们研究了如何利用来自人类反馈的强化学习（RLHF）来调整基于 Transformer 的 LLM，以生成符合教授评分口吻的编程反馈。我们介绍了 PERSA，这是一个 RLHF 流水线，它结合了基于教授演示的监督微调、基于成对偏好的奖励建模以及近端策略优化（PPO），同时刻意将学习限制在承载风格的组件上。受 Transformer 内部机制分析的启发，PERSA 应用了参数高效微调。它仅更新顶层的 Transformer 块及其前馈投影，在最小化全局参数漂移的同时增强了风格可控性。我们在三个代码反馈基准测试（APPS、PyFiXV 和 CodeReviewQA）上评估了所提方法，并使用了风格对齐和保真度的互补指标。在 Llama-3 和 Gemma-2 主干模型上，PERSA 在保持正确性的同时实现了最强的教授风格迁移；例如在 APPS 上，它将风格对齐得分（SAC）从基础模型的 34.8% 提升至 96.2%，且在 Llama-3 和 Gemma-2 上的正确性准确率（CA）高达 100%。总之，PERSA 通过对齐“说什么”（内容正确性）以及至关重要的“怎么说”（教师般的语气和结构），为个性化教育反馈提供了一条切实可行的路径。

## Abstract
Large language models (LLMs) can provide automated feedback in educational settings, but aligning an LLMs style with a specific instructors tone while maintaining diagnostic correctness remains challenging. We ask how can we update an LLM for automated feedback generation to align with a target instructors style without sacrificing core knowledge? We study how Reinforcement Learning from Human Feedback (RLHF) can adapt a transformer-based LLM to generate programming feedback that matches a professors grading voice. We introduce PERSA, an RLHF pipeline that combines supervised fine-tuning on professor demonstrations, reward modeling from pairwise preferences, and Proximal Policy Optimization (PPO), while deliberately constraining learning to style-bearing components. Motivated by analyses of transformer internals, PERSA applies parameter efficient fine-tuning. It updates only the top transformer blocks and their feed-forward projections, minimizing global parameter drift while increasing stylistic controllability. We evaluate our proposed approach on three code-feedback benchmarks (APPS, PyFiXV, and CodeReviewQA) using complementary metrics for style alignment and fidelity. Across both Llama-3 and Gemma-2 backbones, PERSA delivers the strongest professor-style transfer while retaining correctness, for example on APPS, it boosts Style Alignment Score (SAC) to 96.2% (from 34.8% for Base) with Correctness Accuracy (CA) up to 100% on Llama-3, and Gemma-2. Overall, PERSA offers a practical route to personalized educational feedback by aligning both what it says (content correctness) and, crucially, how it says it (instructor-like tone and structure).

---

## 论文详细总结（自动生成）

### 论文总结：PERSA——基于大语言模型的教授风格个性化反馈强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
在教育领域，自动反馈的有效性不仅取决于其**正确性**（Content Correctness），还取决于其**表达方式**（Pedagogical Style）。现有的 LLM 虽然能生成技术上正确的反馈，但往往语气生硬、过于通用，缺乏人类教授那种鼓励性、诊断性且具有启发性的“导师口吻”。
论文的核心问题是：**如何在不牺牲模型核心知识和诊断准确性的前提下，使 LLM 生成的编程反馈在语气、结构和风格上与特定教授对齐？** 这一研究对于提升 AI 导师的可信度和教学效果具有重要意义。

#### 2. 方法论：PERSA 框架
PERSA（Professor-Style Reinforcement-based Style Adaptation）采用了一种参数高效的 RLHF（来自人类反馈的强化学习）流水线，核心思想是将“风格”视为一种可分离且可优化的属性。

*   **核心流程：**
    1.  **监督微调（SFT）：** 在教授编写的反馈演示数据集上进行微调，初步学习反馈的结构（如：诊断 -> 修复建议 -> 验证）。
    2.  **奖励建模（RM）：** 利用教授对反馈样本的成对偏好数据，训练一个奖励模型。该模型基于 Bradley-Terry 模型，学习区分哪些反馈更符合教授的风格偏好。
    3.  **近端策略优化（PPO）：** 使用 PPO 算法优化策略模型，在最大化奖励得分的同时，通过 KL 散度约束防止模型偏离 SFT 初始状态太远。
*   **关键技术细节——层选择性 LoRA（Layer-selective LoRA）：**
    *   研究发现 Transformer 的高层（Top Layers）和前馈网络（FFN）更多地承载了风格、语气和话语模式等高阶属性。
    *   PERSA 仅在模型的**最后 $L$ 层**（如最后 4 层）插入 LoRA 适配器进行更新，而冻结其余参数。这种策略既减少了计算开销，又有效防止了“灾难性遗忘”，确保模型保留原有的编程逻辑能力。

#### 3. 实验设计
*   **数据集：**
    1.  **APPS Benchmark：** 包含 200 条由真实教授针对学生编程问题编写的反馈。
    2.  **PyFiXV：** 包含 240 个 Python 语法错误程序及专家修复说明。
    3.  **CodeReviewQA：** 包含 900 条涵盖 9 种编程语言的代码审查实例。
*   **对比方法（Baselines）：**
    *   **Base Model：** 未经调整的原始指令微调模型（Llama-3, Gemma-2）。
    *   **SFT：** 仅进行监督微调。
    *   **InstructGPT/RLHF：** 标准的全参数或通用 RLHF。
    *   **离线偏好优化：** 包括 DPO（直接偏好优化）、ORPO 和 KTO。
*   **评估指标：**
    *   **风格对齐：** SAC（风格对齐得分）、APC（平均礼貌接近度）、BLEU-4（词汇重叠）。
    *   **诊断保真度：** CA（正确性准确率）。
    *   **人类偏好：** PWR（偏好胜率）。

#### 4. 资源与算力
*   **硬件：** 论文提到使用了 **NVIDIA A100 GPU** 进行训练。
*   **技术优化：** 采用了 **4-bit NF4 量化（QLoRA）** 技术，使得 2B-3B 规模的模型可以在受限的显存环境下进行高效微调。
*   **训练时长：** 文中未明确给出具体的总训练小时数，但指出由于采用了层选择性 LoRA，其计算成本显著低于全参数微调。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个不同的模型家族（Llama-3.2-3B 和 Gemma-2-2B）上进行了验证，涵盖了三个不同的代码反馈数据集。
*   **消融实验：** 进行了详细的消融研究，对比了“仅 SFT”、“仅 PPO”、“全层 LoRA”与“顶层 LoRA”的效果，验证了层选择策略的有效性。
*   **人类评估：** 组织了涉及 8 所大学、32 名参与者（教师和学生）的盲测 A/B 测试，增强了实验的说服力。
*   **客观性：** 实验设计考虑了数据泄露控制（Leakage Controls），通过 MinHash/Jaccard 相似度过滤了训练集和测试集之间的重叠，确保了评估的公平性。

#### 6. 主要结论与发现
*   **风格与内容的可分离性：** 实验证明“教授口吻”可以作为一种独立属性被注入，而不影响诊断的正确性。
*   **PERSA 的优越性：** 在所有基准测试中，PERSA 均取得了最高的风格对齐得分（如在 APPS 上 SAC 达到 96.2%），同时保持了 100% 的诊断正确率。
*   **层选择的价值：** 仅更新顶部 4 层 LoRA 的效果优于更新所有层，这证实了风格信息在 Transformer 内部的局部化分布特征。
*   **人类偏好：** 超过 83% 的教师和学生在盲测中更倾向于 PERSA 生成的反馈，认为其更具启发性和信任感。

#### 7. 优点
*   **高效性：** 通过层选择性 LoRA 极大地降低了 RLHF 的算力门槛。
*   **平衡性：** 成功解决了 LLM 在个性化适配中常见的“风格提升但正确率下降”的矛盾。
*   **实用性：** 为教育机构提供了一种快速、低成本定制 AI 助教风格的技术路径。

#### 8. 不足与局限
*   **样本多样性有限：** 实验主要基于少数几位教授的风格数据，可能无法完全代表所有学科或教学哲学。
*   **领域局限：** 目前仅在编程反馈领域进行了验证，在人文社科等更具主观性的学科中效果尚待观察。
*   **奖励模型风险：** 像所有 RLHF 方法一样，PERSA 仍面临奖励模型过拟合（Reward Over-optimization）或捕获数据偏见的潜在风险。
*   **评估维度：** 虽然评估了正确性，但尚未深入探讨反馈对学生长期学习成效（Learning Outcomes）的实际影响。

（完）
