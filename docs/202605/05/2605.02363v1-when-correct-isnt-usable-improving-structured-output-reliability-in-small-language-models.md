---
title: "When Correct Isn't Usable: Improving Structured Output Reliability in Small Language Models"
title_zh: 当正确性不可用时：提高小语言模型结构化输出的可靠性
authors: "Cosimo Galeone, Minsu Park, Giuseppe Ettorre, Daniele Ligorio"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02363v1"
tags: ["query:llm-fo"]
score: 10.0
evidence: 带有必填字段的JSON结构化输出可靠性差距
tldr: 本研究探讨了小语言模型在生成结构化输出（如JSON）时的可靠性问题。研究发现，即使模型能正确回答数学问题，也常因格式错误导致输出不可用。为此，作者开发了AloLab，这是一个利用Claude Sonnet 4.5作为元代理的迭代系统提示词优化器。该方法在不进行微调或增加推理延迟的情况下，显著提升了模型在GSM8K和MATH基准上的结构化输出准确率，同时也有效解决了GPT-4o等闭源模型的格式失效问题。
selection_source: fresh_fetch
motivation: 针对小语言模型在生成符合特定JSON格式要求的正确答案时，经常出现格式失效且现有约束解码方法延迟过高的问题。
method: 开发了名为AloLab的迭代系统提示词优化系统，通过黑盒API访问目标模型，利用高性能元代理自动优化提示词。
result: "AloLab在GSM8K上实现了84-87%的输出准确率，在MATH上达到34-40%，且推理延迟接近原始水平，显著优于静态提示词。"
conclusion: 通过自动化优化系统提示词，可以在不牺牲推理速度和模型性能的前提下，显著提高语言模型生成结构化输出的可靠性。
---

## 摘要
部署的语言模型必须产生既正确又符合格式要求的输出。我们利用两个数学基准测试——GSM8K 和 MATH——作为受控测试平台，研究了这种结构化输出的可靠性差距：其地面真值（ground truth）明确，且输出契约严格（包含必填字段的 JSON）。我们评估了三个 7-9B 模型在五种提示策略下的表现，并将输出准确率（即数学正确性与有效 JSON 结构的联合事件）作为主要指标。研究发现了一种系统性的格式失效：NAIVE 提示（无系统提示词）在 GSM8K 上实现了高达 85% 的任务准确率，但在所有模型和数据集上的输出准确率均为 0%。REFERENCE 提示（极简的手写 JSON 格式提示词）表现稍好，但在测试的四个模型中有两个的输出准确率为 0%。约束解码（Constrained decoding）虽然强制执行了语法有效性，但带来了 3.6 倍至 8.2 倍的延迟开销，并在多个设置中显著降低了任务性能。为了克服这一局限性，我们开发了 AloLab，这是一个迭代式系统提示词优化器（元代理：Claude Sonnet 4.5），仅需对目标模型进行黑盒 API 访问；它在 GSM8K 上达到了 84-87% 的输出准确率，在 MATH 上达到了 34-40%（每个模型进行五次独立运行），与最佳静态提示词相比，29/30 的配对 McNemar 检验在 p < 0.05 水平下具有显著性，且推理延迟接近 NAIVE 提示，无需模型微调。同样的格式失效也延伸到了 GPT-4o（OpenAI, 2024）这一闭源模型：由于系统性的 Markdown 代码块包裹，REFERENCE 提示的输出准确率为 0%，而 AloLab 达到了 95.2% [94.8, 95.6]。消融实验表明，将 Sonnet 4.5 元代理替换为 Claude 3 Haiku 会使平均输出准确率降至 61.0%，并将运行间的标准差从小于 1 个百分点增加到 21.8 个百分点，证实了元代理的能力是优化质量的主要驱动因素。

## Abstract
Deployed language models must produce outputs that are both correct and format-compliant. We study this structured-output reliability gap using two mathematical benchmarks -- GSM8K and MATH -- as a controlled testbed: ground truth is unambiguous and the output contract is strict (JSON with required fields). We evaluate three 7-9B models under five prompting strategies and report output accuracy -- the joint event of mathematical correctness and valid JSON structure -- as the primary metric. A systematic format failure emerges: NAIVE prompting (no system prompt) achieves up to 85% task accuracy on GSM8K but 0% output accuracy across all models and datasets. REFERENCE prompting (a minimal hand-written JSON format prompt) fares little better, yielding 0% output accuracy for two of four models tested. Constrained decoding enforces syntactic validity but incurs 3.6x-8.2x latency overhead and in several settings degrades task performance substantially. To overcome this limitation, we developed AloLab, an iterative system-prompt optimizer (meta-agent: Claude Sonnet 4.5) requiring only black-box API access to the target model; it reaches 84-87% output accuracy on GSM8K and 34-40% on MATH across five independent runs per model, with 29/30 paired McNemar comparisons against the best static prompt significant at p < 0.05, at near-NAIVE inference latency and without model fine-tuning. The same format failure extends to GPT-4o (OpenAI, 2024), a proprietary closed-source model: REFERENCE achieves 0% output accuracy due to systematic markdown-fence wrapping, while AloLab reaches 95.2% [94.8, 95.6]. An ablation replacing the Sonnet 4.5 meta-agent with Claude 3 Haiku reduces mean output accuracy to 61.0% and increases run-to-run standard deviation from <1 pp to 21.8 pp, confirming that meta-agent capability is a primary driver of optimization quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《When Correct Isn’t Usable: Improving Structured Output Reliability in Small Language Models》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在生产环境中，语言模型（LLM）的输出必须同时满足**正确性**和**格式合规性**。然而，现有的小语言模型（SLM，如 7-9B 参数量级）在处理严格的结构化输出契约（如 JSON）时存在显著的“可靠性差距”：模型可能计算出了正确答案，但由于输出格式错误（如被 Markdown 代码块包裹、LaTeX 转义字符导致 JSON 解析失败等），导致下游系统无法使用。论文指出，标准的基准测试仅衡量任务准确率，忽略了这种格式失效带来的业务不可用问题。

### 2. 论文提出的方法论：AloLab
论文提出了 **AloLab**，一个基于黑盒 API 访问的迭代式系统提示词（System Prompt）优化框架。
*   **核心思想**：利用一个高性能的“元代理”（Meta-agent，如 Claude 3.5 Sonnet）作为分析器和优化器，通过观察目标模型在特定任务上的失败案例，自动、迭代地改进系统提示词。
*   **关键流程**：
    1.  **Solver（求解器）**：调用目标模型获取原始输出。
    2.  **Evaluator（评估器）**：确定输出是否符合 JSON 格式以及数学答案是否正确。
    3.  **Analyzer（分析器）**：接收执行轨迹和错误模式，识别模型特定的失败原因（如 Gemma 喜欢加 Markdown 围栏）。
    4.  **Optimizer（优化器）**：结合分析结果和优化历史，重写系统提示词。
    5.  **迭代**：通常进行 4 个 Epoch，选择验证集表现最好的提示词作为最终输出。
*   **技术细节**：该方法不需要访问模型权重或梯度，仅通过提示词工程解决格式问题，且在推理阶段不增加额外延迟。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**：小学数学应用题，要求 JSON 包含 `reasoning` 和 `answer`（数字）。
    *   **MATH**：竞赛级数学题，要求 JSON 包含 `reasoning` 和 `answer`（LaTeX 表达式）。
*   **对比基准（Benchmarks）**：
    *   **NAIVE**：无系统提示词。
    *   **REFERENCE**：人工编写的极简 JSON 格式提示词。
    *   **CONSTRAINED**：使用 vLLM 的语法约束解码（强制输出 JSON）。
    *   **REF+CONSTRAINED**：人工提示词结合约束解码。
*   **评估模型**：Llama 3.1-8B, Gemma 2-9B, Qwen 2.5-7B，以及作为泛化测试的 GPT-4o。

### 4. 资源与算力
*   **算力平台**：模型部署在 Google Cloud Vertex AI / Model Garden 上，使用 vLLM 框架（BF16 精度）。
*   **具体消耗**：文中未明确给出具体的 GPU 型号和数量，但详细说明了**优化成本**：每个模型/数据集的 5 次独立运行约需 5,000–10,000 次目标模型推理调用，以及 30 次 Claude 3.5 Sonnet 调用。
*   **推理延迟**：AloLab 的推理延迟与 NAIVE 接近（0.7x - 1.06x），远低于约束解码（3.6x - 8.2x）。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个开源模型和 1 个闭源模型，在 2 个数据集上进行了测试。
*   **重复性与统计学**：每个 AloLab 配置均进行了 **5 次独立运行**，并报告了 95% 自举置信区间（Bootstrap CI）。
*   **显著性检验**：进行了 30 组配对 McNemar 检验，其中 29 组显示 AloLab 显著优于静态提示词（p < 0.05）。
*   **消融实验**：包括“元代理能力消融”（用 Claude 3 Haiku 替换 Sonnet）和“模型信息可见性消融”。实验设计严谨，考虑了运行方差和统计显著性，结论较为客观公平。

### 6. 主要结论与发现
*   **格式失效是普遍现象**：NAIVE 提示下所有模型输出准确率均为 0%；即使是 GPT-4o，在 REFERENCE 提示下也会因为 Markdown 围栏导致 0% 的输出准确率。
*   **AloLab 效果显著**：在 GSM8K 上达到 84-87% 的输出准确率，在 MATH 上达到 34-40%，显著弥补了“正确但不可用”的差距。
*   **约束解码的代价**：虽然能保证格式正确，但会带来巨大的延迟开销，且在某些模型（如 Gemma）上会显著降低任务本身的准确率。
*   **元代理能力至关重要**：使用较弱的元代理（Haiku）会导致优化结果出现双峰分布，稳定性极差。
*   **推理与序列化解耦**：存在约 1.5-1.8% 的案例，模型推理过程正确，但在填入 JSON `answer` 字段时写错，这属于深层的生成缺陷。

### 7. 优点
*   **实用性强**：直接针对生产环境中的结构化输出痛点，无需微调即可提升 SLM 的可用性。
*   **低延迟**：相比于修改解码逻辑的约束解码，AloLab 在推理阶段几乎没有额外开销。
*   **黑盒优化**：适用于闭源模型和通过 API 调用的模型，具有很强的通用性。
*   **发现模型特性**：能够自动识别并抑制特定模型的“怪癖”（如自动添加代码块）。

### 8. 不足与局限
*   **领域局限**：实验主要集中在数学任务上，虽然数学任务具有客观评价标准，但在创意写作、信息提取等其他结构化任务上的表现尚待验证。
*   **优化成本**：虽然推理便宜，但前期的迭代优化过程需要消耗数千次 API 调用，对于极小规模任务可能不划算。
*   **元代理依赖**：高度依赖于顶级模型（如 Sonnet 4.5）的分析能力，若使用开源模型作为优化器，效果可能大打折扣。
*   **上下文限制**：在 MATH 数据集上使用了固定的 8k 上下文，可能限制了某些长推理任务的表现。

（完）
