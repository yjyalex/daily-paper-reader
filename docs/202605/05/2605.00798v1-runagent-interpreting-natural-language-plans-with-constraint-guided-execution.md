---
title: "RunAgent: Interpreting Natural-Language Plans with Constraint-Guided Execution"
title_zh: RunAgent：通过约束引导执行解释自然语言计划
authors: "Arunabh Srivastava, Mohammad A., Khojastepour, Srimat Chakradhar, Sennur Ulukus"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00798v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于解释和执行自然语言计划的多智能体平台
tldr: RunAgent是一个多智能体计划执行平台，旨在解决大语言模型在执行结构化工作流时的不可靠性。它通过一种包含显式控制结构（如IF、GOTO）的代理语言，将自然语言的表达力与编程的确定性相结合。该系统能够根据任务描述自主推导并验证约束条件，动态选择推理、工具调用或代码执行，并优化上下文管理。在Natural-plan和SciBench数据集上的实验表明，其性能优于现有基准和PlanGEN方法。
selection_source: fresh_fetch
motivation: 针对大语言模型在执行复杂、结构化自然语言计划时缺乏可靠性和确定性的问题。
method: 提出RunAgent平台，利用带有控制结构的代理语言，结合自主约束验证、动态工具选择和上下文过滤机制来执行计划。
result: 在Natural-plan和SciBench数据集上的评估显示，RunAgent的性能显著超过了基线LLM和最先进的PlanGEN方法。
conclusion: RunAgent通过约束引导的执行机制，有效地弥合了自然语言计划与确定性执行之间的差距，提升了智能体处理复杂任务的能力。
---

## 摘要
人类通过执行有针对性的计划来解决问题，然而大语言模型（LLMs）在结构化工作流执行方面仍然不够可靠。我们提出了 RunAgent，这是一个多智能体计划执行平台，它在解释自然语言计划的同时，通过约束和准则强制执行逐步操作。RunAgent 通过一种具有显式控制结构（如 IF、GOTO、FORALL）的智能体语言，将自然语言的表达能力与编程的确定性联系起来。除了根据每个步骤的具体指令对步骤输出进行语法和语义验证外，RunAgent 还会根据任务描述及其在每个步骤中的实例，自主推导并验证约束条件。RunAgent 还能在基于 LLM 的推理、工具使用以及代码生成与执行（例如 Python）之间进行动态选择，并结合纠错机制以确保正确性。最后，RunAgent 在执行每个步骤期间，通过仅保留相关信息来过滤上下文历史记录。在 Natural-plan 和 SciBench 数据集上的评估表明，RunAgent 的性能优于基准 LLM 和最先进的 PlanGEN 方法。

## Abstract
Humans solve problems by executing targeted plans, yet large language models (LLMs) remain unreliable for structured workflow execution. We propose RunAgent, a multi-agent plan execution platform that interprets natural-language plans while enforcing stepwise execution through constraints and rubrics. RunAgent bridges the expressiveness of natural language with the determinism of programming via an agentic language with explicit control constructs (e.g., \texttt{IF}, \texttt{GOTO}, \texttt{FORALL}). Beyond verifying syntactic and semantic verification of the step output, which is performed based on the specific instruction of each step, RunAgent autonomously derives and validates constraints based on the description of the task and its instance at each step. RunAgent also dynamically selects among LLM-based reasoning, tool usage, and code generation and execution (e.g., in Python), and incorporates error correction mechanisms to ensure correctness. Finally, RunAgent filters the context history by retaining only relevant information during the execution of each step. Evaluations on Natural-plan and SciBench Datasets demonstrate that RunAgent outperforms baseline LLMs and state-of-the-art PlanGEN methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **RunAgent** 的多智能体计划执行平台，旨在提升大语言模型（LLM）在处理复杂、多步骤任务时的执行可靠性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：虽然 LLM 在生成计划方面表现出色，但在执行结构化工作流时仍然不可靠。自然语言计划虽然灵活且表达力强，但缺乏编程语言那样的确定性和逻辑控制（如循环、分支）。
*   **核心问题**：如何弥合自然语言的灵活性与程序执行的确定性之间的差距？现有的系统往往依赖人类反馈进行验证，或者在处理复杂逻辑（如“为所有项执行操作”）时表现不佳。
*   **研究动机**：开发一个能够理解自然语言指令、自主推导约束条件、并具备确定性控制流的自动化执行平台。

### 2. 方法论
RunAgent 的核心思想是引入一种**智能体语言（Agentic Language）**和一套**约束引导的执行框架**。其架构包含三个主要模块：

*   **智能体语言（Agentic Language）**：
    *   引入了显式控制结构关键字：`IF`（条件判断）、`GOTO`（跳转）、`FORALL`（循环迭代）。
    *   引入执行模式指令：`PYTHON`（强制代码执行）、`LLM`（强制模型推理）、`TOOL`（强制调用工具）。
*   **初始化与暂存模块（Initialization and Staging）**：
    *   **工具注册**：管理 Python 函数形式的工具库。
    *   **约束生成**：根据任务描述和具体实例，自主生成“原子约束”（不可简化的逻辑条件），用于后续验证。
*   **编译模块（Compiler）**：
    *   将自然语言计划解析为内部表示（Python 字典结构），识别关键字并生成子步骤（如展开 `FORALL` 循环）。
*   **执行模块（Executor）**：
    *   **动态选择**：根据步骤性质，自动在 LLM 推理、Python 代码生成执行或工具调用之间切换。
    *   **约束验证**：每步执行后，提取相关约束并利用“推理者-裁判”双模型机制验证输出是否合规。
    *   **纠错机制**：若验证失败，将错误信息反馈至上下文并重试；多次失败后启动 Fallback（回退）机制确保流程不中断。
    *   **上下文过滤**：仅保留与当前步骤相关的历史信息，减少干扰并节省 Token。

### 3. 实验设计
*   **数据集**：
    *   **Natural-plan**：包含日历调度（Calendar Scheduling）和旅行规划（Trip Planning）数据集。
    *   **SciBench**：包含统计（Stat）、微积分（Calc）和微分方程（Diff）三个数学科学数据集。
*   **基准测试（Benchmark）与对比方法**：
    *   **基线**：原生 GPT-4o（直接求解或直接执行计划）。
    *   **SOTA 方法**：PlanGEN（基于 Gemini-1.5-Pro 和 Gemini-2.0-Flash 的最先进计划生成与执行框架）。
*   **评估指标**：准确率（Accuracy）和精确匹配准确率（EM Acc）。

### 4. 资源与算力
*   **模型使用**：论文明确指出所有 LLM 调用和模块均基于 **GPT-4o**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于 RunAgent 主要是一个推理侧的执行框架，不涉及大规模模型预训练，其资源消耗主要体现在 API 调用频率和 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：在日历调度数据集上测试了 1000 个问题（分为 10 个难度等级），在旅行规划和 SciBench 上也进行了广泛测试。
*   **消融实验**：进行了“无约束检查”版本的对比，证明了约束验证机制对性能的显著提升（准确率从 75.4% 提升至 81.1%）。
*   **统计显著性**：使用了 McNemar 检验和自助法（Bootstrapping）计算 95% 置信区间，证明 RunAgent 显著优于 GPT-4o 基线（p-value 极低）。
*   **充分性评价**：实验设计较为充分，涵盖了逻辑规划和科学计算两个维度，对比了当前最先进的同类框架，结果具有说服力。

### 6. 主要结论与发现
*   **性能领先**：RunAgent 在日历调度任务上达到了 81.1% 的准确率，远超 PlanGEN（最高 68.9%）和原生 GPT-4o（58.3%）。
*   **执行优于生成**：实验发现，即使给 LLM 一个可行的计划，LLM 直接执行的效果往往不如 RunAgent 这种结构化执行平台。
*   **约束的重要性**：自主生成的原子约束能有效捕捉 LLM 在执行过程中容易忽略的细节，减少逻辑错误。
*   **纠错能力**：通过将错误原因反馈回上下文，系统能够显著提高复杂任务的最终成功率。

### 7. 优点
*   **混合架构**：巧妙地结合了自然语言的灵活性和编程逻辑的严谨性。
*   **自主性强**：能够自动从任务中提取约束，减少了对人工预设规则的依赖。
*   **鲁棒性高**：多重验证机制（语法、语义、约束）和纠错重试机制确保了执行的稳健性。
*   **上下文优化**：通过过滤历史记录，缓解了长上下文带来的注意力分散问题。

### 8. 不足与局限
*   **依赖强模型**：系统高度依赖 GPT-4o 等高性能模型作为底层支撑，在较弱模型上的表现尚未验证。
*   **延迟与成本**：由于引入了多次 LLM 调用（判断、推理、验证、纠错），执行单个任务的延迟和 Token 成本可能显著高于直接调用模型。
*   **LLM 裁判偏见**：验证过程仍依赖 LLM 作为裁判，可能存在“幻觉”或对某些逻辑判断失误的风险。
*   **应用范围**：目前主要在数学和调度类任务上验证，对于需要实时交互或多模态感知的复杂环境（如机器人控制）尚待探索。

（完）
