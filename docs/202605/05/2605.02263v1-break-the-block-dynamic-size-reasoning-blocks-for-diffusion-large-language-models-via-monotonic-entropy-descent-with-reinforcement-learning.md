---
title: "Break the Block: Dynamic-size Reasoning Blocks for Diffusion Large Language Models via Monotonic Entropy Descent with Reinforcement Learning"
title_zh: 突破分块：通过强化学习实现单调熵下降，为扩散大语言模型构建动态尺寸推理块
authors: "Yan Jiang, Ruihong Qiu, Zi Huang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02263v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 扩散大语言模型推理块的强化学习
tldr: 针对扩散大语言模型（dLLM）在推理中固定块大小导致逻辑不连贯的问题，本文提出b1框架。该框架通过强化学习实现动态块大小生成，核心思想是利用“单调熵下降”准则来优化推理过程。b1作为即插即用模块，显著提升了dLLM在各类推理任务中的表现和逻辑连贯性。
selection_source: fresh_fetch
motivation: 固定大小的推理块限制了扩散大语言模型的灵活性，且容易破坏推理逻辑的连贯性。
method: 提出b1框架，利用强化学习和单调熵下降目标，使模型能够根据推理需求动态调整生成块的大小。
result: 在多个推理基准测试中，b1 相比固定块大小的基线模型取得了显著且一致的性能提升。
conclusion: 动态块大小生成是提升扩散大语言模型推理能力的关键，单调熵下降为优化推理质量提供了有效指导。
---

## 摘要
最近，扩散大语言模型（dLLMs）通过基于块的半自回归生成范式，在推理方面展现出了有效性和高效性。尽管取得了进展，但固定尺寸的块生成仍然是实现有效且连贯推理的关键瓶颈。1. 从全局视角来看，不同的推理任务对应不同的最优解码块尺寸，这使得“一刀切”的假设失效。2. 即使在单个推理任务中，僵化的分块也会破坏逻辑流并降低推理的连贯性。通过实证观察，我们发现对于逐块熵，错误的推理在块之间表现出波动且不稳定的趋势，而正确生成的任务则遵循一致的下降趋势。因此，本文提出了 b1，这是一种针对 dLLMs 的新型训练后框架，它通过强化学习下的单调熵下降目标来学习动态尺寸的推理块，从而增强推理的连贯性。b1 作为即插即用模块，可以与现有的 dLLM 训练后算法无缝集成。在各种推理基准测试上的广泛实验表明，b1 相比现有的固定尺寸块基准模型具有持续的改进。我们的代码已发布在 https://github.com/YanJiangJerry/Block-R1。

## Abstract
Recent diffusion large language models (dLLMs) have demonstrated both effectiveness and efficiency in reasoning via a block-based semi-autoregressive generation paradigm. Despite their progress, the fixed-size block generations remain a critical bottleneck for effective and coherent reasoning. 1. From a global perspective, different reasoning tasks would correspond to different optimal decoding block sizes, which makes a ``one-size-fits-all'' assumption ineffective. 2. Even within a single reasoning task, the rigid block partitioning would break the logical flow and reduce reasoning coherence. Through empirical observations, we reveal that for block-wise entropy, incorrect reasoning exhibits a fluctuating and unsteady trend between blocks, whereas the correctly generated tasks follow a consistent descending trend. Therefore, this paper proposes b1, a novel post-training framework for dLLMs that learns dynamic-size reasoning blocks via a Monotonic Entropy Descent objective with reinforcement learning to enhance reasoning coherence.b1 integrates seamlessly as a plug-and-play module with existing dLLM's post-training algorithms. Extensive experiments across various reasoning benchmarks showcase b1's consistent improvement over existing fixed-size block baselines. Our code has been released at https://github.com/YanJiangJerry/Block-R1.

---

## 论文详细总结（自动生成）

这篇论文提出了 **b1** 框架，旨在解决扩散大语言模型（dLLMs）在推理任务中因固定大小的分块生成而导致的逻辑不连贯问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **背景：** 扩散大语言模型（如 LLaDA）采用半自回归的“分块（Block-based）”并行生成范式。
*   **核心问题：** 现有的 dLLM 通常使用**固定大小**的块（如每块 32 个 token）。这种“一刀切”的方法存在两大缺陷：
    1.  **全局不适应性：** 不同任务的最优块大小不同。
    2.  **逻辑中断：** 僵化的分块边界经常在逻辑推理的关键点（如公式中间）强行切断，导致模型产生高熵（不确定性）波动，进而引发推理错误。
*   **核心发现：** 作者观察到，正确推理的生成过程在块与块之间表现出**单调熵下降（Monotonic Entropy Descent）**的趋势，而错误推理则表现为熵的剧烈波动。

### 2. 方法论
b1 是一个后训练（Post-training）框架，核心思想是通过强化学习（RL）让模型自主学习如何划分推理块。
*   **动态尺寸推理块：** 引入一个特殊的结束指示符 token（`\block`）。模型在生成过程中一旦产生该 token，当前块即结束。这使得块边界能与语义推理步骤（Reasoning Steps）自然对齐。
*   **单调熵下降（MED）目标：**
    *   **块熵定义：** 计算一个动态块内所有 token 的平均香农熵。
    *   **MED 奖励（$R_{ent}$）：** 这是一个代理奖励，通过比较相邻块的熵来鼓励熵的持续下降。作者在理论上证明了最大化该局部奖励等同于最大化全局的斯皮尔曼等级相关系数（Spearman's Rank Correlation）。
*   **结束指示符奖励（$R_{ind}$）：** 采用对数缩放奖励，鼓励模型生成足够数量的推理步骤，避免生成过短或无效的推理链。
*   **算法集成：** b1 是一个“即插即用”模块，可无缝集成到现有的 dLLM 强化学习算法（如 GRPO、d1、wd1）中。

### 3. 实验设计
*   **数据集：** 选择了四个具有代表性的推理基准：**GSM8K**（小学数学）、**MATH500**（竞赛级数学）、**Sudoku**（数独逻辑）、**Countdown**（倒计时算术游戏）。
*   **基准模型：** LLaDA-8B-Instruct。
*   **对比方法：** 
    *   **RL 基线：** d1, wd1, GDPO, Diffu-GRPO。
    *   **推理时方法：** AdaBlock-dLLM（基于换行符的启发式动态分块）。
    *   **其他：** SFT（监督微调）。
*   **评估指标：** Zero-shot Pass@1 准确率。

### 4. 资源与算力
*   **硬件：** 使用了 **4 台 AMD Mi300x GPU**（每台拥有 192GB 显存）。
*   **训练细节：** 
    *   采用 LoRA 微调（r=128, alpha=64）。
    *   序列长度设为 256 或 512。
    *   训练步数最高为 10,000 步，但 b1 通常在 1,000 到 2,100 步之间即可达到最优性能（收敛速度快于基线）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个不同领域的推理任务，并在 256 和 512 两种序列长度下进行了测试。
*   **消融实验：** 专门验证了 MED 奖励和指示符奖励的有效性，证明两者缺一不可。
*   **深入分析：** 进行了斯皮尔曼相关性分析、错误样本的熵分布可视化、超参数敏感性测试以及训练奖励动态分析。
*   **客观性：** 实验设置严格遵循了前序工作（d1, wd1）的协议，对比公平且充分。

### 6. 主要结论与发现
*   **性能提升：** b1 在所有基准上均显著优于固定块基线。在 Countdown 任务上，相比 wd1 提升了高达 **19.53%**。
*   **逻辑连贯性：** 动态块能有效避免逻辑流的中断，使模型在推理过程中保持较低且递减的熵水平。
*   **MED 的价值：** 实验证实了单调熵下降与推理准确率之间存在强正相关性。
*   **效率：** b1 引入的计算开销极小，且能显著加快 RL 训练的收敛速度。

### 7. 优点
*   **创新性：** 首次打破了 dLLM 固定块大小的限制，并提出了基于熵演化的推理优化准则。
*   **理论支撑：** 为局部熵奖励与全局单调性之间的等价性提供了数学证明。
*   **通用性：** 作为即插即用模块，能提升多种现有 RL 算法的性能。
*   **可解释性：** 通过熵的可视化直观展示了为何动态分块能减少推理错误。

### 8. 不足与局限
*   **任务覆盖：** 主要集中在数学和逻辑推理，尚未在代码生成或长文本创作等非步骤性任务上进行验证。
*   **架构限制：** 该方法专门针对扩散模型（dLLM）的分块生成范式，不直接适用于标准的自回归（AR）模型。
*   **指示符依赖：** 性能在一定程度上依赖于模型学习生成 `\block` token 的准确性，若模型无法正确生成该 token，动态分块将退化。

（完）
