---
title: Constraint-Guided Multi-Agent Decompilation for Executable Binary Recovery
title_zh: 约束引导的多智能体反编译用于可执行二进制恢复
authors: "Yifan Zhang, Xiaohan Wang, Yueke Zhang, Yu Huang, Kevin Leach"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23940v2"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于代码生成和优化的多智能体框架
tldr: 本研究针对现有反编译器生成的代码难以编译或运行的问题，提出了一种名为MCGD的多智能体框架。该框架通过语法、编译和行为等三层约束引导，利用LLM智能体对反编译代码进行迭代修复。在ExeBench数据集上的实验表明，该方法显著提升了代码的可重执行率，在保持低成本的同时，性能优于LLM4Decompile等现有SOTA方法，有效弥补了原始反编译输出与实用源代码之间的差距。
selection_source: fresh_fetch
motivation: 传统反编译器生成的代码往往存在语法错误或逻辑偏差，无法直接编译运行，限制了其在安全分析和软件维护中的实际应用。
method: 提出一种多层约束引导的反编译框架，通过语法解析、GCC编译和LLM生成的测试用例进行分层验证，并由专门的LLM智能体根据反馈迭代优化代码。
result: "在1,641个真实二进制文件上，该框架实现了84-97%的可重执行率，相比基准工具提升了28-89个百分点，且优于现有的LLM反编译方法。"
conclusion: 约束引导的智能体迭代优化能够有效恢复可执行的源代码，为二进制逆向工程提供了一种高效且低成本的解决方案。
---

## 摘要
反编译——从已编译的二进制文件中恢复源代码——对于安全分析、恶意软件逆向工程和遗留软件维护至关重要。然而，现有的反编译器生成的代码通常无法正确编译或执行，限制了它们的实际效用。我们提出了一个多智能体框架，通过多级约束引导反编译（MCGD）将反编译代码转换为可重新执行的源码。我们的方法采用了一个具有三个约束级别的分层验证流水线：（1）通过解析实现的语法正确性，（2）通过 GCC 实现的可编译性，以及（3）通过大语言模型（LLM）生成的测试用例实现的行为等价性。当验证失败时，专门的 LLM 智能体会利用结构化错误反馈迭代地优化代码。我们在来自 ExeBench 的 1,641 个真实世界二进制文件上评估了我们的框架，涵盖了三种反编译器（RetDec、Ghidra 和 Angr）。我们的框架实现了 84-97% 的可重新执行率，比基准反编译器的输出提高了 28-89 个百分点。与使用相同 GPT-4o 主干网络的先进基于 LLM 的反编译方法相比，我们的方法（84.1%）优于 LLM4Decompile（80.3%）、SK2Decompile（73.9%）和 SALT4Decompile（61.8%）。我们的消融研究表明，基于执行的验证至关重要：尽管仅编译方法的编译率达到 91-99%，但其行为正确性为 0%。该系统收敛效率高，90% 以上的二进制文件在 2 次迭代内达到正确性，平均每个二进制文件的成本为 0.03-0.05 美元。我们的结果表明，约束引导的智能体优化可以弥合原始反编译器输出与实际有用的源代码之间的差距。

## Abstract
Decompilation -- recovering source code from compiled binaries -- is essential for security analysis, malware reverse engineering, and legacy software maintenance. However, existing decompilers produce code that often fails to compile or execute correctly, limiting their practical utility. We present a multi-agent framework that transforms decompiled code into re-executable source through Multi-level Constraint-Guided Decompilation (MCGD). Our approach employs a hierarchical validation pipeline with three constraint levels: (1) syntactic correctness via parsing, (2) compilability via GCC, and (3) behavioral equivalence via LLM-generated test cases. When validation fails, specialized LLM agents iteratively refine the code using structured error feedback. We evaluate our framework on 1,641 real-world binaries from ExeBench across three decompilers (RetDec, Ghidra, and Angr). Our framework achieves 84-97% re-executability, improving baseline decompiler output by 28-89 percentage points. In comparison with state-of-the-art LLM-based decompilation methods using the same GPT-4o backbone, our approach (84.1%) outperforms LLM4Decompile (80.3%), SK2Decompile (73.9%), and SALT4Decompile (61.8%). Our ablation study reveals that execution-based validation is critical: compile-only approaches achieve 0% behavioral correctness despite 91-99% compilation rates. The system converges efficiently, with 90%+ binaries reaching correctness within 2 iterations at an average cost of $0.03-0.05 per binary. Our results demonstrate that constraint-guided agentic refinement can bridge the gap between raw decompiler output and practically useful source code.

---

## 论文详细总结（自动生成）

以下是对论文《Constraint-Guided Multi-Agent Decompilation for Executable Binary Recovery》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：反编译是安全分析和逆向工程的核心，但传统反编译器（如 Ghidra, Angr）生成的代码往往存在语法错误、类型不匹配或逻辑偏差，导致代码无法重新编译或运行（可重执行率仅为 22-26%）。
*   **核心问题**：现有的 LLM 反编译方法多采用单次生成（Single-pass），缺乏验证机制和反馈循环，无法保证生成代码的行为正确性。
*   **整体含义**：本文提出了 **Agent4Decompile**，一个多智能体框架，旨在通过“约束引导”的迭代修复，将不可用的反编译输出转化为与原始二进制行为等价的可执行源代码。

### 2. 方法论
*   **核心思想**：将反编译视为一个“搜索空间缩减”过程，通过分层约束（语法 -> 编译 -> 执行）逐步过滤错误，并利用 LLM 智能体根据反馈进行针对性修复。
*   **关键技术细节**：
    *   **三级约束体系**：
        1.  **L1 语法约束**：使用 `gcc -fsyntax-only` 检查代码是否符合 C 语言语法。
        2.  **L2 编译约束**：检查类型匹配、符号解析和链接是否正确。
        3.  **L3 执行约束**：通过测试驱动（I/O 对比），验证反编译代码在运行时的行为是否与原始二进制一致。
    *   **多智能体协作**：针对每一级约束失败，设有专门的修复智能体。智能体接收结构化的错误日志（如 GCC 报错信息或失败的测试用例差异），在不改变整体逻辑的前提下修正局部错误。
    *   **迭代流程**：采用无状态迭代算法，代码若未通过验证则进入下一轮修复，直至通过所有约束或达到最大迭代次数（通常设为 5-7 次）。

### 3. 实验设计
*   **数据集**：
    *   **157-Binary Benchmark**：用于深度分析和 SOTA 对比，包含 ExeBench 的真实项目函数及算法类函数。
    *   **1,641-Binary Benchmark**：用于评估扩展性，涵盖不同优化等级（O0-O3）。
*   **反编译器后端**：集成了 Ghidra（规则驱动）、Angr（提升/IR 驱动）和 RetDec（ML/LLVM 驱动）。
*   **对比方法**：
    *   **DeepSeek Single Pass**：无迭代的 LLM 直接生成。
    *   **LLM4Decompile**：经过微调的专业反编译模型。
    *   **SK2Decompile**：侧重于可读性的两阶段反编译方法。
*   **评估指标**：L1 语法率、L2 编译率、L3 可重执行率（核心指标）。

### 4. 资源与算力
*   **模型使用**：主要使用 **DeepSeek V3.2** 作为底层 LLM 引擎。
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该框架属于**推理侧的 Agent 框架**，不涉及大规模模型预训练。实验在配备 Python 环境的标准服务器上运行，通过并行进程池（ProcessPoolExecutor）加速验证过程。
*   **成本**：提到系统收敛快（平均 1.5-2.3 次迭代），推理成本较低。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 1,600 个二进制文件的测试，是目前反编译领域规模最大的实验之一。
*   **充分性**：
    *   **消融实验**：分别测试了仅 L1、L1+L2 以及全约束（L1+L2+L3）的效果，证明了执行反馈的必要性。
    *   **多维度覆盖**：涵盖了三种主流反编译器、四种编译器优化等级（O0-O3）以及多种安全应用场景（漏洞保留、模糊测试）。
*   **客观性**：通过对比不同架构的反编译器，证明了该框架具有普适性，不依赖于特定的反编译工具。

### 6. 主要结论与发现
*   **性能提升**：Agent4Decompile 将可重执行率从基准的 22-26% 提升至 **40-46%**（在大规模测试中）和 **50.3%**（在精选测试中）。
*   **“编译-执行”鸿沟**：实验发现，仅靠编译验证（L2）虽然能达到近 100% 的编译率，但可重执行率仅为 32-42%。这表明**能编译的代码不代表逻辑正确**，L3 执行反馈是闭合语义缺口的关键。
*   **收敛效率**：大多数成功的修复在 2 次迭代内即可完成，证明了 LLM 在有明确错误反馈时的修复效率极高。

### 7. 优点
*   **闭环验证**：引入了动态执行反馈，解决了静态分析无法处理的深层语义错误。
*   **工具无关性**：作为一个“包装器”框架，可以增强任何现有反编译器的输出。
*   **实用性强**：实验证明修复后的代码可直接用于模糊测试（Fuzzing），使可模糊测试的目标数量增加了 6.6 倍。

### 8. 不足与局限
*   **单函数限制**：目前主要针对单个函数进行反编译和修复，尚未完全解决跨函数调用和全局状态恢复的复杂问题。
*   **测试用例依赖**：L3 约束高度依赖于高质量的测试用例（I/O 对），对于难以构造输入的复杂程序，效果会受限。
*   **优化对抗**：在 O3 高级优化下，由于函数内联和死代码消除，反编译恢复率仍有待提高（约 37.5%）。
*   **语义漂移风险**：LLM 在修复时可能会为了通过测试而“硬编码”逻辑，而非真正恢复原始算法。

（完）
