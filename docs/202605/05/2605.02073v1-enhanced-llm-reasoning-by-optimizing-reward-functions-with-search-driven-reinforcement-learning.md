---
title: Enhanced LLM Reasoning by Optimizing Reward Functions with Search-Driven Reinforcement Learning
title_zh: 通过搜索驱动的强化学习优化奖励函数来增强大语言模型的推理能力
authors: "Arash Ahmadi, Sarah Sharif, Yaser, Banad"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.02073v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用组相对策略优化(GRPO)优化数学推理的奖励函数
tldr: 本研究针对大语言模型在数学推理任务中强化学习效果受限于奖励函数设计的问题，提出了一种搜索驱动的优化框架。该框架将奖励函数规范视为优化对象，利用前沿模型迭代生成候选奖励，并通过GRPO算法在Llama-3.2-3B模型上进行快速训练与筛选。实验表明，通过五轮迭代反馈，该方法显著提升了模型在GSM8K上的表现，最佳集成配置的F1分数较基准提升了0.19，证明了自动化奖励函数优化的有效性。
selection_source: fresh_fetch
motivation: 强化学习提升LLM推理能力的效果高度依赖奖励函数的设计，而手动设计往往难以捕捉到最优的激励信号。
method: 提出一种搜索驱动框架，利用前沿模型迭代生成并优化奖励函数，通过GRPO算法进行短时训练评估与排名反馈。
result: 经过五轮迭代，最佳奖励函数集成在GSM8K测试集上的F1分数达到0.795，显著优于仅使用基础奖励的0.609基准。
conclusion: 将奖励函数规范作为优化目标并结合排名反馈循环，是提升大语言模型推理能力的有效且关键的途径。
---

## 摘要
数学推理是大语言模型的一个关键基准。强化学习是提升大语言模型推理能力的标准训练后机制，但其性能对驱动策略优化的奖励函数设计非常敏感。本文介绍了一种搜索驱动的框架，将奖励规范本身视为优化对象。本研究关注的设定是：基础模型保持不变，而奖励规范是主要的剩余设计杠杆。候选奖励函数由前沿语言模型生成，经过自动验证，并通过在 Llama-3.2-3B-Instruct 基础模型上结合低秩自适应（LoRA）进行的 500 步群体相对策略优化（GRPO）训练运行进行筛选，最后根据 GSM8K 测试集上的 F1 分数进行排名。前几轮的排名摘要随后被反馈到下一轮生成中。经过五轮搜索，共产生了 50 个候选奖励。平均 F1 分数从第一轮的 0.596 上升到第五轮的 0.632，表现最好的单个奖励达到了 F1 = 0.787。评估了排名靠前奖励的七种集成配置。最佳集成配置达到了 F1 = 0.795（95% 自助法置信区间 [0.756, 0.832]）和 0.660 [0.635, 0.686] 的准确率，相比仅使用基础奖励的 GRPO 基准（F1 = 0.609）实现了 0.19 的 F1 绝对增益。带有 Bonferroni 校正的两两 McNemar 检验显示，所有包含五个或更多奖励的配置在 α = 0.05/21 水平上在统计学上没有显著差异。对最佳集成配置进行三次随机种子重训，得到的 F1 分数为 0.785。随机抽取的 5 个奖励对照组的 F1 分数骤降至 0.047，这表明是排名反馈循环而非奖励数量的增加驱动了性能提升。

## Abstract
Mathematical reasoning is a key benchmark for large language models. Reinforcement learning is a standard post-training mechanism for improving the reasoning capabilities of large language models, yet performance remains sensitive to the design of the reward function that drives policy optimization. This paper introduces a search-driven framework that treats the reward specification itself as an object of optimization. The setting of interest is one in which the base model is held fixed and the reward specification is the primary remaining design lever. Candidate reward functions are generated by a frontier language model, validated automatically, screened through 500-step Group Relative Policy Optimization (GRPO) training runs on a Llama-3.2-3B-Instruct base model with Low-Rank Adaptation (LoRA), and ranked by F1 on the GSM8K test set. Ranked summaries from prior rounds are then fed back into the next round of generation. Over five rounds, the search produces 50 candidate rewards. The mean F1 rises from 0.596 in Round 1 to 0.632 in Round 5, and the top individual reward reaches F1 = 0.787. Seven ensemble configurations of top-ranked rewards are evaluated. The best ensemble achieves F1 = 0.795 (95% bootstrap CI [0.756, 0.832]) and accuracy 0.660 [0.635, 0.686], a 0.19 absolute F1 gain over a base-rewards-only GRPO baseline (F1 = 0.609). Pairwise McNemar tests with Bonferroni correction show all five-or-more-reward configurations are statistically indistinguishable at α = 0.05/21. A three-seed re-training of the best ensemble yields F1 of 0.785. A randomly drawn 5-reward control collapses to F1 = 0.047, which shows that the ranked-feedback loop, not the additive signal of having more rewards, drives the gain.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhanced LLM Reasoning by Optimizing Reward Functions with Search-Driven Reinforcement Learning》的结构化深度分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于：**如何自动化设计和优化用于提升大语言模型（LLM）推理能力的奖励函数（Reward Function）。**
*   **背景：** 强化学习（RL）已成为提升 LLM 推理能力的关键技术，但其效果高度依赖于奖励函数的设计。
*   **动机：** 手动设计奖励函数既费时又容易出错，且难以平衡“结果监督”（只看答案对错）与“过程监督”（看推理步骤）之间的关系。
*   **目标：** 提出一种搜索驱动的框架，将奖励函数本身作为优化对象，通过自动化迭代发现最优的奖励逻辑，从而在不改变基础模型的前提下，最大化提升模型的推理性能。

### 2. 方法论：核心思想与关键技术
该研究提出了一种**“生成-验证-训练-排名”**的闭环迭代框架：
*   **核心思想：** 利用一个强大的前沿模型（如 Kimi K2）作为“奖励生成器”，迭代产生 Python 代码形式的奖励函数，并通过实际的强化学习训练效果来筛选和进化这些函数。
*   **关键技术细节：**
    *   **GRPO 算法：** 使用“组相对策略优化”（Group Relative Policy Optimization），该算法通过组内相对得分代替传统的 Critic 网络，显著降低了显存消耗，适合在有限算力下运行。
    *   **迭代反馈循环：** 整个过程分为 5 轮。每一轮生成 10 个候选奖励函数，在 Llama-3.2-3B 模型上进行 500 步的快速 GRPO 训练，根据测试集 F1 分数进行排名。排名靠前的奖励描述会反馈给下一轮的生成器。
    *   **四阶段沙盒验证：** 候选代码需通过 AST 静态检查、锁定环境执行、命名空间限制和模拟运行，确保代码安全且可执行。
    *   **奖励集成（Ensemble）：** 将多个发现的高质量奖励函数与基础奖励（格式检查、答案对错）进行加权求和，形成更强的训练信号。

### 3. 实验设计
*   **数据集：** 专注于 **GSM8K**（小学数学应用题数据集），包含 7,473 个训练样本和 1,319 个测试样本。
*   **Benchmark（基准）：** 
    *   **Base-only：** 仅使用基础奖励（格式和答案正确性）的 GRPO 训练。
    *   **开源模型对比：** 与同规模（2B-4B）的领先模型对比，包括 Qwen 3/3.5 和 Phi-4-Mini。
*   **对比方法：** 评估了 7 种不同的集成策略（如 Top-5、Top-10、多样化轮次集成等）以及一个“随机 5 奖励”对照组。

### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA RTX 5090 GPU** (32GB VRAM)。
*   **时长：** 完成整个搜索过程（5 轮，50 个候选奖励筛选 + 集成训练）大约需要 **40 小时**。
*   **模型：** 基础模型为 Llama-3.2-3B-Instruct，采用 4-bit 量化和 LoRA 适配器进行参数高效微调。

### 5. 实验数量与充分性
*   **实验规模：** 
    *   生成并测试了 50 个独立的候选奖励函数。
    *   测试了 7 种集成配置。
    *   针对最佳配置进行了 3 个不同随机种子的重训以验证稳定性。
*   **充分性与客观性：** 
    *   **对照组设置：** 引入了“随机 5 奖励”对照组，证明了性能提升源于“排名反馈”而非单纯增加奖励函数数量。
    *   **统计检验：** 使用了带有 Bonferroni 校正的 McNemar 检验来评估不同配置间的统计显著性。
    *   **鲁棒性：** 通过自助法（Bootstrap）计算了 95% 置信区间。整体实验设计严谨，考虑了随机性影响。

### 6. 主要结论与发现
*   **迭代有效性：** 奖励函数的质量随轮次显著提升，平均 F1 从第一轮的 0.596 升至第五轮的 0.632。
*   **性能飞跃：** 最佳集成配置在 GSM8K 上的 F1 达到 **0.795**，准确率达到 **0.660**，较基准（F1=0.609）提升了 **0.19** 的绝对值。
*   **超越同类：** 经过该方法优化的 3B 模型在数学推理上超过了 Qwen 3-4B 和 Phi-4-Mini 等同规模模型。
*   **奖励特性：** 排名靠前的奖励往往关注推理步骤的结构（如步骤数、计算密度），与结果监督形成互补。

### 7. 优点
*   **自动化与可解释性：** 自动发现奖励函数，且产出的是人类可读、可审计的 Python 代码。
*   **低成本高效率：** 证明了在单张消费级 GPU 上也能完成复杂的奖励优化搜索，对学术界和小型团队非常友好。
*   **集成优势：** 发现多样化的奖励组合比单一奖励更具鲁棒性，能有效抑制单一奖励可能带来的“奖励作弊”（Reward Hacking）。

### 8. 不足与局限
*   **任务单一：** 目前仅在 GSM8K 数学推理任务上进行了验证，尚未在代码生成或科学问答等其他领域测试。
*   **奖励作弊风险：** 虽然作者进行了审计，但某些结构性奖励（如奖励推理行数）在更长步数的训练中仍可能诱导模型产生无意义的冗长推理。
*   **泛化性待考：** 发现的奖励函数高度依赖于特定的 `<thinking>` 标签格式，迁移到其他提示词模板时可能需要重新调整正则逻辑。
*   **训练步数限制：** 筛选阶段仅使用了 500 步训练，可能漏掉了一些在长期训练中才能显现效果的奖励函数。

（完）
