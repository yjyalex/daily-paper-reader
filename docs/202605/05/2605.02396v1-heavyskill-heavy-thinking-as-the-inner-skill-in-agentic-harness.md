---
title: "HeavySkill: Heavy Thinking as the Inner Skill in Agentic Harness"
title_zh: HeavySkill：深度思考作为智能体架构中的内在技能
authors: "Jianing Wang, Linsen Guo, Zhengyu Chen, Qi Guo, Hongyu Zang, Wenjie Shi, Haoxiang Ma, Xiangyu Xi, Xiaoyu Li, Wei Wang, Xunliang Cai"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02396v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 协调多个智能体处理复杂推理任务
tldr: 本研究针对智能体编排框架中核心驱动机制不明的问题，提出了 HeavySkill 视角。该方法将“深度思考”定义为模型内部的一种核心技能，而非仅是外部编排单元。HeavySkill 采用“并行推理后总结”的两阶段流程，在多个领域显著优于传统的 Best-of-N 策略。研究证明，这种内化技能可通过强化学习进一步扩展深度与广度，为构建无需依赖脆弱编排层、具备自我进化能力的复杂推理大模型提供了新路径。
selection_source: fresh_fetch
motivation: 现有的智能体编排框架虽然在复杂任务中表现出色，但其提升性能的底层机制往往被复杂的系统设计所掩盖。
method: 提出将“深度思考”视为模型内化的核心技能，并将其实现为“并行推理后总结”的两阶段流水线。
result: "实验显示 HeavySkill 在多领域一致优于 Best-of-N 策略，且强力模型在性能上能接近 Pass@N 的水平。"
conclusion: 深度思考作为一种可学习且可扩展的技能，可通过强化学习助力大模型实现自我进化，减少对外部编排层的依赖。
---

## 摘要
最近，通过编排框架协调具备记忆、技能和工具调用能力的多个智能体，智能体架构在复杂推理任务中取得了显著成功。然而，真正驱动性能提升的底层机制仍被复杂的系统设计所掩盖。在本文中，我们提出了 HeavySkill，这一观点将“深度思考”不仅视为编排架构中的最小执行单元，还将其视为内化在模型参数中的内在技能，驱动编排器解决复杂任务。我们将这种技能定义为一个两阶段流水线，即并行推理后进行总结，它可以运行在任何智能体架构之下。我们对 HeavySkill 在不同领域进行了系统的实证研究。结果表明，这种内在技能始终优于传统的 Best-of-N (BoN) 策略；值得注意的是，更强大的大语言模型（LLM）甚至可以接近 Pass@N 的性能。至关重要的是，我们证明了深度思考的深度和广度作为一种可学习的技能，可以通过强化学习进一步扩展，这为实现内化复杂推理且不依赖脆弱编排层的自我进化 LLM 提供了一条充满前景的路径。

## Abstract
Recent advances in agentic harness with orchestration frameworks that coordinate multiple agents with memory, skills, and tool use have achieved remarkable success in complex reasoning tasks. However, the underlying mechanism that truly drives performance remains obscured behind intricate system designs. In this paper, we propose HeavySkill, a perspective that views heavy thinking not only as a minimal execution unit in orchestration harness but also as an inner skill internalized within the model's parameters that drives the orchestrator to solve complex tasks. We identify this skill as a two-stage pipeline, i.e., parallel reasoning then summarization, which can operate beneath any agentic harness. We present a systematic empirical study of HeavySkill across diverse domains. Our results show that this inner skill consistently outperforms traditional Best-of-N (BoN) strategies; notably, stronger LLMs can even approach Pass@N performance. Crucially, we demonstrate that the depth and width of heavy thinking, as a learnable skill, can be further scaled via reinforcement learning, offering a promising path toward self-evolving LLMs that internalize complex reasoning without relying on brittle orchestration layers.

---

## 论文详细总结（自动生成）

这是一份关于论文《HeavySkill: Heavy Thinking as the Inner Skill in Agentic Harness》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）智能体主要依赖复杂的编排框架（如 Claude Code, OpenClaw）来协调记忆、技能和工具。然而，这些系统设计往往掩盖了性能提升的本质机制。
论文提出了 **HeavySkill** 视角：认为驱动复杂任务解决的核心并非外部编排逻辑，而是模型内部的一种**“深度思考”（Heavy Thinking）**技能。作者旨在将这种复杂的编排行为抽象并内化为模型的一种测试时缩放（Test-time Scaling, TTS）策略，使其不再依赖脆弱的外部系统层，而是作为模型参数内的一种可学习能力。

### 2. 方法论：核心思想与技术细节
HeavySkill 将深度思考实现为一个两阶段的流水线：
*   **并行推理阶段（Parallel Reasoning）**：给定问题 $q$，使用模型 $\pi_\theta$ 生成 $K$ 条独立的推理轨迹 $T = \{y_1, \dots, y_K\}$。每条轨迹包含完整的思维链（CoT）和最终答案。
*   **序列化记忆缓存（Serialized Memory Cache）**：将生成的轨迹进行序列化处理。为了应对上下文长度限制，会对轨迹进行剪枝，并进行随机打乱（Shuffle）以消除模型对特定位置的偏见。
*   **顺序审议阶段（Sequential Deliberation）**：使用另一个（或同一个）模型 $\pi_\phi$ 对缓存中的多条轨迹进行综合分析。该阶段要求模型识别答案分布、评估逻辑严密性、交叉验证，并在所有初始轨迹均错误时尝试重新推理。
*   **迭代审议（可选）**：通过多次循环，将前一轮的审议结果作为输入再次优化。
*   **可读技能封装**：将上述流程编写成结构化的自然语言文档（Skill File），使智能体编排器能像调用工具一样自主执行深度思考。

### 3. 实验设计
*   **数据集/场景**：
    *   **STEM 领域**：AIME25, BeyondAIME, HMMT25-Feb, GPQA-Diamond（侧重高难度数学和科学推理）。
    *   **通用推理**：LiveCodeBench（编程）、Arena-Hard（对话偏好）、IFEval（指令遵循）、IMO Answer Bench。
*   **对比基准（Benchmarks）**：
    *   **Mean@K**：K 次独立生成的平均准确率。
    *   **Pass@K**：K 次生成中至少有一个正确的概率（代表模型潜力上限）。
    *   **Vote@K (Majority Voting)**：传统的 Best-of-N 投票策略。
*   **对比方法**：主要对比了不同规模的开源（如 DeepSeek R1, Qwen3, GLM 4.6）与闭源模型（GPT-5-Thinking, Claude 4.5, Gemini 3 Pro）在传统采样与 HeavySkill 模式下的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100/H100）数量及总训练时长。
*   **软件框架**：提到了使用 **VeRL** 框架进行强化学习实验，并采用了 **GSPO**（Group Sequence Policy Optimization）算法。
*   **推理配置**：推理时设置温度为 1.0，top-p 为 0.95，并行轨迹数 $K$ 通常设为 8 或 16。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 120B+ 不同参数规模的 10 余种主流模型。
*   **消融实验**：
    *   测试了不同模型组合（如用 7B 推理，32B 审议）的效果。
    *   分析了轨迹选择策略（随机、最大多样性、最大长度、最大频率）。
    *   探讨了迭代次数对性能的影响。
    *   验证了在工具调用（Tool Use）场景下的泛化能力。
*   **充分性评价**：实验设计较为全面，跨越了多个难度等级和任务类型。通过对比 Heavy-Mean 和传统 Vote 策略，客观地证明了“审议”机制优于简单的统计投票。

### 6. 主要结论与发现
*   **超越投票法**：HeavySkill（HM@4）在绝大多数任务中显著优于传统的 Majority Voting，证明了模型具备识别低频但正确答案的“审议”能力。
*   **接近潜力上限**：在强力模型（如 GPT-5, DeepSeek V3.2）上，HeavySkill 的表现能逼近甚至在某些情况下通过综合推理超过原始的 Pass@K。
*   **审议能力的独立性**：审议阶段并不一定需要极强的原始推理模型，具备良好指令遵循能力的通用模型（如 Qwen2.5-Instruct）也能有效完成审议任务。
*   **RL 可扩展性**：通过强化学习（RLVR），可以进一步提升模型在深度思考模式下的表现，HM@4 准确率可额外提升约 10%。

### 7. 优点
*   **视角创新**：将复杂的智能体系统行为抽象为模型的一种“内在技能”，简化了智能体架构。
*   **测试时缩放（TTS）的实用化**：提供了一种比 MCTS（蒙特卡洛树搜索）更易于实现且效果显著的推理计算缩放方法。
*   **跨模型协作**：证明了“推理”与“审议”角色可以解耦，为异构模型协作提供了理论支持。

### 8. 不足与局限
*   **迭代负效应**：实验发现过多的迭代审议虽然能提升平均准确率（HM@K），但会导致潜力上限（HP@K）下降，可能是因为累积噪声干扰了模型的判断。
*   **RL 稳定性风险**：在处理长序列（如 K=16 的轨迹缓存）进行强化学习时，模型容易出现熵崩溃（Entropy Collapse），对算力和上下文窗口要求极高。
*   **成本考量**：论文虽然强调了性能提升，但未详细分析并行推理带来的推理成本（Token 消耗）与延迟增加的权衡。
*   **主观任务乏力**：在 Arena-Hard 等偏好类任务中，HeavySkill 的提升并不明显，说明该技能目前更适用于有客观标准答案的逻辑推理任务。

（完）
