Title: Value-Gradient Hypothesis of RL for LLMs

URL Source: https://arxiv.org/pdf/2605.21654v1

Published Time: Fri, 22 May 2026 00:19:10 GMT

Number of Pages: 16

Markdown Content:
Preprint 

# VALUE -G RADIENT HYPOTHESIS OF RL FOR LLM S

Arip Asadulaev ∗ Daniil Ognev Karim Salta Martin Takac 

MBZUAI MBZUAI Independent MBZUAI 

## ABSTRACT 

Reinforcement learning substantially improves pretrained language models, but it remains understudied why critic-free methods such as PPO and GRPO work as well as they do, and when they should provide the largest gains. We develop a value-gradient perspective of critic-free RL for LLM post-training. First, under a differentiable rollout and additive-noise parameterization, we show that the actor update is value-gradient-like in expectation: the backward pass propagates costates whose conditional expectation equals the value gradient . Second, for discrete transformer policies, we show that autodifferentiation through attention produces empirical costates that approximate this value signal, with an error controlled by the sampling gap and policy entropy. These results motivate a decomposition of RL impact into value gradient signal and reachable reward headroom, yielding a criterion for when RL should be most effective along a pretraining trajectory. 

## 1 INTRODUCTION 

Figure 1: Real RL gain vs. predicted one using value impact formula (Section 5, Eq. 29). Recently, Large Language Models (LLMs) achieve state-of-the-art reasoning using Group Relative Policy Optimization (GRPO) (Shao et al., 2024), which discards the critic entirely, yet classical Reinforcement Learning (RL) the-ory predicts that critic-free methods should fail at long-horizon credit assignment. Why don’t they? In this paper we argue that critic-free RL in LLMs is not value-free . The central claim is that the actor backward pass already carries a value-gradient-like signal. In a differentiable roll-out, this signal is exactly the costate propagated by Backpropagation Through Time (BPTT). In a discrete transformer, the same structure survives approximately because attention provides a differentiable pathway for credit transport around the token-sampling bottleneck. First, in our paper, under a continuous relaxation (Fairbank & Alonso, 2012), we show that the Proxi-mal Policy Optimization (PPO)/GRPO actor update is value-gradient-like in expectation. Second, for discrete transformer policies, we show that the empirical costate computed by autodiff approximates the continuous BPTT signal, with an error controlled by the sampling gap and policy entropy. Third, we use this perspective to derive an RL-impact decomposition into usable value-gradient signal and reachable reward headroom, which predicts when RL should be most effective along pretraining (Figure 1). This perspective gives a concrete answer to a practical question: RL should help most at checkpoints that are simultaneously close enough to the value-gradient regime to transmit useful credit and far enough from saturation to retain reward-improving trajectories .Our contributions are: We show that under a differentiable rollout and shift/additive-noise policy, the local GRPO actor update is value-gradient-like in expectation. We show that in transformers with discrete token sampling, the empirical costate computed by autodiff approximates the BPTT costate, with an error controlled by the sampling gap and attention-based credit transport. We derive a predictive RL-impact decomposition into usable value-gradient signal and reachable reward headroom, which empirically can be used for checkpoint selection during pretraining. 

> ∗

Contact: arip.asadulaev@mbzuai.ac.ae 

1

> arXiv:2605.21654v1 [cs.LG] 20 May 2026

Preprint 

## 2 BACKGROUND 

Notation . A prompt/question is denoted by q ∼ P (Q). Given q, a policy with parameters θ

generates an autoregressive completion o = ( o1, . . . , o T ) with token-level factorisation πθ (o | q) = 

QTt=1 πθ (ot | st), st := ( q, o <t ), at := ot. We write (st, a t)Tt=1 for the token-level trajectory. Let 

r(st, a t) ∈ R denote a per-token reward. Outcome-only RL is the case where r(st, a t) = 0 for t < T 

and r(sT , a T ) = r(q, o ) is a terminal reward. With a fixed discount γ ∈ (0 , 1] , the discounted return and the return-to-go are R := PTt=1 γt−1 r(st, a t), Rt := PTj=t γj−t r(sj , a j ).For any policy π, define the time-indexed value function, action-value, and advantage V πt (s) := 

E[Rt | st = s], Qπt (s, a ) := E[Rt | st = s, a t = a], Aπt (s, a ) := Qπt (s, a ) − V πt (s). The value gradient is the state-gradient of the value function, Gπt (s) := ∂V πt (s) 

> ∂s

. Unlike the scalar V πt , it is a vector field on state space. Throughout the paper we write gradients with the partial-derivative symbol: ∂F (θ) 

> ∂θ

for a gradient in parameter space, ∂ϕ (s) 

> ∂s

and ∂ϕ (s,a ) 

> ∂a

for gradients in state or action space, respectively. 2.1 RL FOR LLM S

GRPO (Shao et al., 2024) is commonly presented as a PPO (Schulman et al., 2017) variant that removes the learned critic/value model. For each prompt q, it samples a group of n completions 

{oi}ni=1 from an old policy πθold , where the index i identifies the rollout (and its return, which may be terminal-only or per-token). Let Ti := |oi| and si,t := ( q, o i<t ). In the outcome-only case each completion receives a scalar reward ri = r(q, o i). GRPO forms a group-normalised signal ˜ri and the tokenwise likelihood ratio: for t = 1 , . . . , T i, ˜ri := ri−mean( r)std( r)+ ξnum , ρi,t (θ) := πθ (oit|si,t )  

> πθold (oit|si,t )

, with 

ξnum > 0 for numerical stability and r = ( r1, . . . , r G). GRPO then uses a tokenwise advantage estimate constant along the completion, bAi,t := ˜ ri for all t (process-supervision variants can use token- or step-level reward-to-go). The objective is the GRPO clipped surrogate with a KL penalty to a fixed reference policy πref :

J(θ) = E

"

1

G

> G

X

> i=1

1

TiTiX

> t=1

min 



ρi,t (θ) bAi,t , clip( ρi,t (θ), 1 − ε, 1 + ε) bAi,t 



− β KL 



πθ ∥ πref 

!# 

,

(1) where ε > 0 is the clipping threshold and β > 0 controls the KL regularisation. GRPO differs from PPO only in how bAi,t is constructed: it uses a group-normalised return (computed from multiple rollouts for the same prompt) and typically holds the resulting scalar constant along the trajectory. 2.2 GRADIENT ESTIMATORS 

Let x be a random variable, c a differentiable scalar cost, and F (θ) := Ex[c(x)] the objective. There are two canonical ways θ can enter this expectation (Schulman et al., 2015). For an Score-function (SF) estimator , if x ∼ p( · ; θ), then by the log-derivative trick 

∂∂θ Ex∼p(·;θ)

c(x) = Ex



c(x) ∂∂θ log p(x; θ)



. (2) This identity is valid whenever p(x; θ) is differentiable in θ. Crucially, c need not be differentiable, or even continuous, in x. That is exactly why SF is the natural estimator for classical RL: discrete actions, non-differentiable rewards, and unknown dynamics are all admissible. The cost is variance: the estimator uses only the scalar c(x), not its slope, and therefore ignores all local geometry of c.

Pathwise-derivative (PD) estimator. If instead x = x(z, θ ) is a differentiable function of θ and an exogenous noise variable z ∼ p(z) whose distribution does not depend on θ (a reparameterization ), then differentiation and expectation commute directly: 

∂∂θ Ez

c(x(z, θ ))  = Ez

 ∂∂θ c(x(z, θ )) 



. (3) PD exploits ∂c ∂x directly and is typically lower variance than SF when both apply (Rezende et al., 2014). The price is a stronger regularity requirement: c ◦ x(·, θ ) must be (almost everywhere) differentiable, and the sampling must admit a reparameterization. 2Preprint 2.3 COSTATES AND VALUE -GRADIENTS 

We now lift the single-variable policy gradient setup of §2.2 to trajectories and ask what object the backward pass in RL settings computes. 

Definition 1 (Differentiable rollout) . The functions πθ , f θ , r are differentiable in their arguments. The noise law p(ξ) does not depend on θ, and 

at = πθ (st, ξ t), st+1 = fθ

 st, π θ (st, ξ t), ξt ∼ p(ξ) i.i.d., independent of θ. (4) Let D denote the total derivative with respect to the state, accounting for both the direct state dependence and the indirect dependence through the policy’s action. Df θ (st, a t) = ∂f θ 

> ∂s

+ ∂f θ

> ∂a ∂π θ
> ∂s

for the vector-valued dynamics, and similarly Dr for the reward. Because all randomness is exogenous under Definition 1, the sampled return R1(θ, ξ 1: T ) is a deterministic function of the parameters given the noise. This allows differentiation to commute with expectation, resulting in a pathwise identity formula analogous to equation 3: ∂J (θ) 

> ∂θ

= E ∂R 1

> ∂θ

. The gradient ∂R 1 

> ∂θ

is computed by differentiating the unrolled computation graph equation 4, a process known as backpropagation through time (BPTT) (Fairbank & Alonso, 2012). Crucially, BPTT does not propagate the parameter gradient itself. It propagates the state-sensitivity adjoint, which we call the costate :

λt := ∂R t

∂s t

, λT +1 := 0 . (5) Intuitively, the effect of the current state on future return has two pieces: the immediate effect on the current reward, plus the future effect pushed backward through the transition Jacobian. Formally: 

Proposition 1 (Adjoint recursion, (Fairbank & Alonso, 2012)) . Under Definition 1, the costates satisfy 

λt = Dr (st, a t) + γ  Df θ (st, a t)⊤λt+1 (6) 

for t = T, . . . , 1, and the exact pathwise parameter gradient is 

∂J (θ)

∂θ = E

" TX

> t=1

γt−1 γ

 ∂f θ (st, a t)

∂θ 

⊤

λt+1 

+

 ∂π θ (st, ξ t)

∂θ 

⊤ ∂r (st, a t)

∂a t

+ γ

 ∂f θ (st, a t)

∂a t

⊤

λt+1 

! !# 

.

(7) The proof (see Appendix A) is a direct chain-rule expansion of Rt = r(st, a t) + γR t+1 .

Costates are value-gradient estimators. Conditioning on st = s, the value function satisfies 

V πt (s) = Er(st, a t) + γV πt+1 (st+1 ) st = s. Comparing this value recursion with the sampled costate recursion in equation 6, we see that the two have the same form. More precisely, differentiating 6 with respect to s and interchanging differentiation and expectation (valid under Definition 1) gives 

∂V πt (s)

∂s = E

h

Dr (st, a t) + γ Df θ (st, a t)⊤Gπt+1 (st+1 ) st = s

i

. (8) The value gradient is the corresponding conditional expectation over that future noise. Therefore, 

E[λt | st = s] = ∂V πt (s)

∂s (9) Thus the quantity propagated backward by BPTT is, at each step, a Monte Carlo sample of the value gradient. Thus critic-free does not mean value-free : the relevant value information is present as a propagated gradient signal rather than as a separately fitted scalar critic. 

Takeaway 

The key object for this paper is the costate: in a differentiable rollout, the backward signal propagated by BPTT is a Monte Carlo estimator of the value gradient. 3Preprint 

## 3 CONTINUOUS LENS : W HY CRITIC -F REE RL I S VALUE -G RADIENT -L IKE 

We now show that the GRPO/PPO actor update equals, in expectation, the BPTT pathwise gradient of a continuous-relaxed rollout. The standard GRPO/PPO derivation is written for discrete token actions, 

st := ( q, o <t ), at := ot, st+1 = ( st, a t), where the sampling step blocks differentiation through the trajectory. However, Fairbank & Alonso (2012) integration-by-parts equivalence supplies the missing conceptual link: under an additive-noise parameterization, the expected score-function update can be rewritten as an action-derivative (PD-like) update. To make the backward signal explicit, we introduce a continuous relaxation of the LLM rollout. At the level of hidden states, logits, or other differentiable surrogate states, we apply Definition 1 with differentiable fθ , πθ , and r. This turns the rollout into a reparameterized computation graph, so the pathwise analysis of §2.3 applies. The only ingredient we need is the score-function/pathwise bridge. 

Lemma 1 (Fairbank’s (Fairbank & Alonso, 2012) SF = PD under a shift policy) . Fix a state s and let F (θ) = Ea∼πθ (·| s)[r(s, a )] , with r differentiable in a. If 

πθ (a | s) = ν a − ¯aθ (s),

for a differentiable density ν with vanishing boundary terms, then the score-function and pathwise forms agree in expectation. In particular, 

E



r(s, a ) ∂∂θ log πθ (a | s)

| {z }

> SF form

=

 ∂¯aθ (s)

∂θ 

⊤

E

 ∂r (s, a )

∂a 

| {z }

> PD form

. (10) Lemma 1 is the bridge: it guarantees that the score-function update used in practice matches a pathwise update in expectation. Applied token-by-token, under a shift/additive-noise parameterisation 

πθ (at | st) = ν at − ¯aθ (st), if the GRPO weight bAi,t is treated as a stop-gradient scalar, each local score-function term admits: 

E

bAi,t r(st, a t) ∂∂θ log πθ (at | st)



=

 ∂¯aθ (st)

∂θ 

⊤

E

" ∂   bAi,t r(st, a t)

∂a t

#

. (11) The right-hand side is PD-shaped: it depends on the slope of the action-to-reward signal, not on the score function. No SF information is lost and expectation is unchanged. Then, under the continuous rollout equation 1, the backward quantity propagated by GRPO is the costate, and, by §2.3, it satisfies 

λt := ∂R t

∂s t

, E[λt | st = s] = ∂V πt (s)

∂s . (12) Thus, in the continuous surrogate, the GRPO/PPO update is value-gradient-like in expectation: even without a fitted critic, the backward pass carries a Monte Carlo estimator of the state-gradient of future return. The full derivation is deferred to Appendix A. 

Takeaway 

Under a differentiable continuous relaxation, PPO/GRPO is value-gradient-like in expectation, so critic-free actor updates can still carry a principled credit-assignment signal without a learned critic. 

## 4 DISCRETE GRPO S ENDS APPROXIMATE BPTT S IGNALS 

Section §3 established, under a continuous relaxation and a shift/additive-noise policy, that the local GRPO gradient is a value-gradient update (Corollary 1). In practice, LLM rollouts use a 

discrete categorical policy. The key realisation is that when one computes ∂L GRPO  

> ∂θ

by standard autodifferentiation, one is already backpropagating through the transformer’s internal computation graph across positions. The attention mechanism creates differentiable pathways between positions; the only place differentiability breaks is at the token-sampling boundary. The question thus becomes: 

how much of the BPTT costate structure of §2.3 survives despite the discrete sampling gaps? 

4Preprint For a single trajectory, the log-probability at position t depends on the final-layer hidden state, and the GRPO loss (locally, ignoring clipping) is 

L(θ) = 

> T

X

> t=1

bAt · ℓt(θ), ℓt := log πθ (ot | st) := log softmax  Whead · h(L) 

> t

(θ)

> ot

, (13) and in a transformer, h(L) 

> t

depends on hidden representations at all previous positions through the attention mechanism, h(l) 

> t

= TransformerBlock (l) h(l−1) 1: t

, l = 1 , . . . , L. Consequently, there exist differentiable Jacobians Jt←t′ := ∂h (L)

> t
> ∂h (L)
> t′

, t′ ≤ t, computed through the multi-layer attention stack. These are exactly the transition Jacobians that play the role of Df θ in §2.3. 

Definition 2 (Empirical costate) . The empirical costate at position t is the gradient of the advantage-weighted loss with respect to the final-layer hidden state: 

ˆλt := ∂∂h (L)

> t

X

> k≥t

bAk · ℓk. (14) By the chain rule through attention, ˆλt = bAt · ∂ℓ t

> ∂h (L)
> t

+ P 

> k>t

bAk · ∂ℓ k

> ∂h (L)
> k

· Jk←t, or equivalently, in recursive form, 

ˆλt = bAt · ∂ℓ t

∂h (L)

> t

+ J⊤ 

> t+1 ←t

· ˆλt+1 , ˆλT +1 := 0 . (15) This is structurally identical to the BPTT costate recursion of Proposition 1, λt = Dr (st, a t) + 

γ  Df θ (st, a t)⊤λt+1 . The correspondence is 

Dr ←→ bAt · ∂ℓ t

∂h (L)

> t

(immediate credit signal) , (16) 

(Df θ )⊤ ←→ J⊤ 

> t+1 ←t

(transition Jacobian through attention) , (17) 

λt+1 ←→ ˆλt+1 (propagated future signal) . (18) 4.1 WHAT IS MISSING VS . EXACT BPTT In the continuous relaxation of §2.3, the transition st+1 = fθ (st, a t) is fully differentiable, so Df θ

captures everything , including how a perturbation of st would change the continuous action at, which would change st+1 . In discrete GRPO, there is a non-differentiable gap at each sampling step: 

h(L)

> t
> diff

−−→ zt

> sample

−−−−→ ot

> embed

−−−−→ et

> diff

−−→ h(L)

> t+1

. (19) The Jacobian Jattn  

> t+1 ←t

that autodiff computes goes through the attention pathway: how h(L) 

> t

influences 

h(L) 

> t+1

through attention at position t + 1 , treating the input token ot as fixed. It does not capture the path h(L) 

> t

→ zt → ot → et → h(L) 

> t+1

through the sampled token. The exact BPTT Jacobian therefore decomposes as 

Df exact  

> θ

= Jattn 

> t+1 ←t

| {z }

> what GRPO computes

+ ∂h (L)

> t+1

∂e t

· ∂e t

∂o t

· ∂o t

∂z t

· ∂z t

∂h (L)

> t

| {z }

> missing: through the sampling step

. (20) The second term is non-differentiable in the discrete case. This is the only gap. Proposition 2 (Sampling-gap bound) . The missing Jacobian term is small when the policy is near-deterministic. Specifically, if the policy entropy at position t is Ht, then the effective Jacobian through the sampling step (in a straight-through or Gumbel-Softmax sense) has operator norm bounded by 

Df exact  

> θ

− Jattn  

> t+1 ←t

≤ C ·

s

Ht

log |V | , (21) 

where |V | is the vocabulary size and C depends on embedding norms. 

5Preprint 

Remark 1 (Why this bound is natural) . When the policy is near-deterministic ( Ht ≈ 0), the softmax is peaked at one token. A small perturbation of the logits does not change which token is sampled (the argmax is stable), so ∂o t/∂z t ≈ 0 in a distributional sense. The straight-through estimator makes this precise: the expected Jacobian through sampling scales with the “softness” of the distribution. 

Remark 2 (Why this matters for LLMs) . After pretraining, LLM policies have relatively low entropy on most tokens (especially for common syntactic tokens, function words, code syntax). The KL constraint in GRPO keeps the policy close to this low-entropy reference, so on most of the trajectory the missing signal is small. 

Figure 2: Bound inequality plot. Real value gra-dient gap vs proposed bound (Section 4, Prop. 2). 

Attention is doing the heavy lifting . In an RNN, 

ht+1 depends on ht only through the recurrence 

ht+1 = f (ht, e ot ). The only pathway is through the discrete token, so the sampling gap blocks 

all temporal credit flow. In a transformer, h(L)

> t+1

depends on h(L) 

> t

through attention , a direct, dif-ferentiable, content-based pathway that does not go through the sampling step at position t. The attention scores 

αt+1 , t ′ ∝ exp  qt+1 · kt′

 (22) create soft, differentiable pointers to all previous hidden states. This gives transformers a bypass around the discrete sampling bottleneck : credit can flow from future positions to past hidden states through attention without differentiating through token sampling. 

Proposition 3 (Attention-pathway rank and magnitude) . For a transformer with L layers and causal attention with nheads heads of dimension dhead , the attention-pathway Jacobian Jattn  

> t+1 ←t′

has rank at most L · dhead · nheads per layer of attention, and its magnitude is controlled by the attention weights 

αt,t ′ . When attention is broadly distributed over context, the Jacobian carries richer temporal credit information, partially compensating for the missing sampling-path Jacobian. 

4.2 APPROXIMATION THEOREM 

Theorem 1 (Discrete GRPO costates approximate BPTT costates) . Let ˆλt be the empirical costate equation 14 computed by autodifferentiation through the GRPO loss, and let λt be the exact BPTT costate from Proposition 1 applied to the relaxed rollout of §3. Then 

E[ˆλt | ht] − E[λt | ht] ≤

> T

X

> k=t+1

γ k−t · Jattn 

> k←k−1
> k−t−1

| {z }

> Jacobian chain growth

· ϵk

|{z} 

> per-step sampling gap

, (23) 

where ϵk is the per-step approximation error from the missing sampling Jacobian, bounded by equa-tion 21. 

Combining Theorem 1 with identity equation 9, 

E[ˆλt | ht] ≈ E[λt | ht] = Gπt (ht),

so the empirical costate is, up to the Theorem 1 error, a Monte Carlo estimator of the value gradient in the transformer’s hidden-state space. The error accumulates through the Jacobian chain but is controlled by policy entropy at each step , and spectral properties of the attention Jacobians. Figure 2 evaluates the entropy bound 2 formula, see more details in §6. 

Takeaway 

In discrete transformers, autodiff already propagates an empirical costate through attention, so critic-free RL remains close to the BPTT value-gradient picture despite discrete token sampling. 6Preprint 

## 5 RL I MPACT LAW HYPOTHESES 

The previous sections explain that the actor backward pass propagates an approximate hidden-state value-gradient signal, and the gap to the ideal value-gradient regime is controlled by the discrete sampling step together with attention-based credit transport. We now turn this into a predictive theory of RL readiness . The key idea is simple: RL should be effective when the actor update contain a usable value-gradient signal. This gives a direct prediction for how much gain RL should produce from a given pretrained checkpoint. For any critic-free normalized policy-gradient method m, let 

ˆλ(m) 

> t

denote the empirical hidden-state costate produced by the actor backward pass, and let Gt

denote the hidden-state value gradient. The quantity 

Eq,τ,t 

hD 

E[ˆλ(m) 

> t

| ht], G t

Ei 

(24) measures how much useful RL signal the checkpoint actually sends. Using ⟨u, v ⟩ = ∥v∥22 +⟨u−v, v ⟩,we obtain the lower bound 

Eq,τ,t 

hD 

E[ˆλ(m) 

> t

| ht], G t

Ei 

≥ Σ( θ) − Λm(θ), (25) where 

Σ( θ) := Eq,τ,t 

∥Gt∥22

 , Λm(θ) := Eq,τ,t 

h

∥Gt∥2 E[ˆλ(m) 

> t

| ht] − Gt 2

i

. (26) We therefore define the usable value-gradient signal 

Sm(θ) := (Σ( θ) − Λm(θ)) (27) Intuitively, Σ( θ) measures how much value-gradient signal is available, while Λm(θ) measures how much of that signal is lost because the actual actor update is still separated from the value-gradient regime. By the discrete approximation result of §4, the gap term inside Λm(θ) becomes small when the entropy-controlled sampling gap is small and attention transports credit effectively. Signal quality alone is not enough. Even a clean RL signal will produce little gain if the current policy has no useful reward headroom left to exploit. We therefore define the reachable headroom 

Hα(θ) := Eq∼P (Q)

 1

α log Eτ ∼πθ (·| q)

h

eαR (τ )i

− Eτ ∼πθ (·| q) [R(τ )] 



, (28) where R(τ ) is the trajectory return. This quantity measures how much reward can still be gained by reweighting the model’s existing trajectory distribution. It is small both when the model is too weak to sample any good trajectories and when it is already close to saturation; it is large only when better trajectories are already present in support and can still be amplified by RL. These two terms combine into the central predictive statement of the framework: RL Impact ∝ Sm(θ)

| {z } 

> value-gradient signal

× Hα(θ)

| {z } 

> reachable headroom

. (29) For a fixed task, reward, and RL budget B, we write this in the simplest one-constant form as 

∆Perf m,B (θ) ≈ κm,B Sm(θ) Hα(θ), κm,B > 0. (30) Eq. equation 29 is the main prediction of the theory. RL should work best when the checkpoint is already close enough to the value-gradient regime to provide a usable actor signal, but still has enough reward headroom for RL to exploit. Practically, if {θN } is a sequence of pretrained checkpoints , then the predicted best point to start RL is 

N ⋆ = arg max  

> N

Sm(θN ) Hα(θN ). (31) 

Takeaway 

RL gain should scale with two factors at once: how much usable value-gradient signal the model transmits, and how much reward-improving headroom remains in its trajectory distribution. 

## 6 EXPERIMENTS 

We evaluate the proposed theory in two complementary settings. First, we test the entropy-controlled approximation bound from §4 on OLMo-2 (Team OLMo et al., 2024) checkpoints. Second, we test whether the costate-based impact score predicts which checkpoints benefit most from a RL. 7Preprint 

Figure 3: Results of the RL upon various OLMO-2 pretraining checkpoints. The left image shows the consistency of the achieved gains via RL using a different number of GRPO steps. The right image shows the behavior of the different components of our study. 

Figure 4: Real RL gain vs. predicted one using value impact formula (Section 5, Eq. 29). 

Closed-form RL task. For the RL-impact ex-periment, we use OLMo-2 1B checkpoints from pretraining steps 50 k to 1M in increments of 

50 k. The task is a controlled label-copying prob-lem. Given a prompt containing a target label in {A, B, C, D }, the model must put probabil-ity mass on the matching answer token. The reward is R(θ; q) = pθ (y⋆ | q), where y⋆

is the correct label. This reward is differen-tiable in the model logits and avoids decoded-text, classifier, or parser discontinuities. For each checkpoint θN , we measure the pre-RL re-ward Rbefore (θN ) = Eq pθN (y⋆ | q), then run GRPO from the checkpoint and measure the final post-RL reward Rafter (θN , K ), where K is the number of RL updates. We use several RL bud-gets, K ∈ { 10 , 20 , 25 , 30 }, and report both individual-budget results and the averaged response 

∆R(θN ) = EK [Rafter (θN , K ) − Rbefore (θN )] .

Entropy-bound verification. Figure 2 evaluates the entropy bound from §4. In this experiment, we exactly computed our Proposition 2 formula, comparing the real right-hand side ϵ with the entropy-based left side. For the OLMO-2 model, we estimate the costate approximation error and compare it to the entropy-controlled upper bound predicted by the theory, using the reward function described above. The purpose of this experiment is to verify our claim that the discrete-sampling gap is controlled by entropy. 

Costate-based predictors. For each checkpoint, we compute the true pathwise costate Gt = ∂R/∂h t

and the detached-reward RL costate bλt = ∂[ bAt log πθ (at | st)] /∂h t, using the same prompts, hidden states, sampled actions, and reward evaluations. This matched-trajectory computation avoids mixing the relaxed and discrete distributions. We then estimate Σ = E∥Gt∥2, Λm = E[∥Gt∥ ∥ bλt − Gt∥],and the headroom term Hα = 1 

> α

log E[exp( αR )] − E[R]. The impact score is Pgap = (Σ − Λm)Hα,which preserves the ordering information. 

RL-response prediction. Figure 1, 3 and 4 shows the costate components and the impact predictor. The component plot shows that the raw quantities vary systematically across checkpoints, while the predictor tracks the broad shape of the measured RL gain curve. This supports the mechanism suggested by the theory: checkpoints differ not only in their current reward, but also in the quality of their costate signal and their remaining reward headroom. Figure 5 evaluates the final predictive story. The left panel compares the realized mean post-RL reward against bRafter = Rbefore + Affine( Pgap ), where the affine map calibrates the scale of Pgap 

to the observed reward-gain scale. The right panel uses a scale-free version, z(Rbefore ) + z(Pgap ),and compares it to z(Rafter ), where z(·) denotes normalization across checkpoints. This asks whether pretrained competence plus predicted RL readiness explains the final post-RL check-point quality. Empirically, the combined predictor correlates more strongly with averaged post-RL reward than either current reward or the impact score alone. Across the RL budgets, the score satisfies Spearman( Pgap , ∆R) ≈ 0.60 , while the combined scale-free predictor satisfies 8Preprint 

Figure 5: Z-scores of the gain after RL vs. z-scores of the predicted RL impact. Correlation (left) and curve per checkpoint (right). The std bars on left indicate RL gain variance over the various training steps. 

Spearman( z(Rbefore ) + z(Pgap ), z (Rafter )) ≈ 0.73 . These results suggest that the theory captures a real checkpoint-dependent RL-readiness signal, but also that current competence remains necessary for predicting the final post-RL reward level. 

Limitations . The current setup is controlled and partially toy-like. The reward is deliberately chosen to be differentiable in the model logits, which allows exact costate measurements. Many parameters affect the observed correspondence, including reward scale, RL learning rate, batch size, group size, and the number of RL updates. Our method may indicate that a checkpoint is ready for efficient RL, but how RL is performed remains an additional optimization question. Indeed, too few RL steps under-realize the predicted headroom, while too many steps saturate the reward and obscure checkpoint differences. For this reason, we report averages across several RL budgets. 

## 7 RELATED WORK AND DISCUSSION 

There is a large body of work on extending the GRPO algorithm. Methods such as these are essentially additional techniques built on top of GRPO, and our methodology extrapolates well with respect to such modifications.In terms of the critic-free RL interpretation, it has been shown that GRPO implicitly computes a value-like quantity via U-statistics (Zhou et al., 2026). However, this account is specific to GRPO, while PPO (Schulman et al., 2017) and even REINFORCE also perform unexpectedly well in the LLM setting. Moreover, evidence from standard RL suggests that group-normalized episodic baselines are generally insufficient to replace a critic for bootstrapping and temporal credit assignment (de Oliveira, 2025). Therefore, we argue that the gains of RL in LLMs come not only from the GRPO baseline design. Zheng et al. (2025) propose using BPTT diretcly instead of RL and obtain better empirical results, which according to our interpretation should make the value-gradient signal propagates better. Another interesting observation is that scalar continuous rewards were used in the latest DeepSeek-V4 model (DeepSeek-AI, 2026) to achieve better results. Recently, value gradient flow-based learning was proposed (Xu et al., 2026), which achieved sustainable results in RL tasks, signaling that the value signal is useful. However, this method differs from ours because their approach is actor-free, showing how the value gradient can be used as a policy, while we focus on the critic-free LLMs. 

## 8 CONCLUSION 

This paper argues that critic-free RL in LLM post-training works because the actor backward pass is not value-free: it carries a value-gradient-like signal. In a differentiable rollout this signal is exact in expectation, and in discrete transformers it survives approximately because attention provides a differentiable channel for temporal credit transport while the missing sampling path is controlled by policy entropy. This perspective leads to a simple prediction: RL gains should be largest when a checkpoint has both strong usable credit-assignment signal and remaining reward headroom. More broadly, the paper suggests that the success of critic-free RL in LLMs is not an exception to credit-assignment theory, but a consequence of hidden-state computation in continues space. 9Preprint 

## REFERENCES 

et al de Oliveira, Bryan LM. Learning without critics? revisiting grpo in classical reinforcement learning environments. Arxiv , 12, 2025. DeepSeek-AI. Deepseek-v4: Towards highly efficient million-token context intelligence, 2026. Michael Fairbank and Eduardo Alonso. Value-gradient learning. In The 2012 international joint conference on neural networks (ijcnn) , pp. 1–8. IEEE, 2012. Danilo Jimenez Rezende, Shakir Mohamed, and Daan Wierstra. Stochastic backpropagation and approximate inference in deep generative models. In International conference on machine learning ,pp. 1278–1286. PMLR, 2014. John Schulman, Nicolas Heess, Theophane Weber, and Pieter Abbeel. Gradient estimation using stochastic computation graphs. Advances in neural information processing systems , 28, 2015. John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. Proximal policy optimization algorithms. arXiv preprint arXiv:1707.06347 , 2017. Zhihong Shao, Peiyi Wang, Qihao Zhu, Runxin Xu, Junxiao Song, Xiao Bi, Haowei Zhang, Mingchuan Zhang, YK Li, Yang Wu, et al. Deepseekmath: Pushing the limits of mathemat-ical reasoning in open language models. arXiv preprint arXiv:2402.03300 , 2024. Team OLMo, Pete Walsh, Luca Soldaini, Dirk Groeneveld, Kyle Lo, Shane Arora, Akshita Bhagia, Yuling Gu, Shengyi Huang, Matt Jordan, Nathan Lambert, Dustin Schwenk, Oyvind Tafjord, Taira Anderson, David Atkinson, Faeze Brahman, Christopher Clark, Pradeep Dasigi, Nouha Dziri, Michal Guerquin, Hamish Ivison, Pang Wei Koh, Jiacheng Liu, Saumya Malik, William Merrill, Lester James V. Miranda, Jacob Morrison, Tyler Murray, Crystal Nam, Valentina Pyatkin, Aman Rangapur, Michael Schmitz, Sam Skjonsberg, David Wadden, Christopher Wilhelm, Michael Wilson, Luke Zettlemoyer, Ali Farhadi, Noah A. Smith, and Hannaneh Hajishirzi. 2 OLMo 2 Furious, 2024. URL https://arxiv.org/abs/2501.00656 .Haoran Xu, Kaiwen Hu, Somayeh Sojoudi, and Amy Zhang. Reinforcement learning via value gradient flow. arXiv preprint arXiv:2604.14265 , 2026. Zhi Zheng, Yu Gu, Wei Liu, Yee Whye Teh, and Wee Sun Lee. Soft-grpo: Surpassing discrete-token llm reinforcement learning via gumbel-reparameterized soft-thinking policy optimization. arXiv preprint arXiv:2511.06411 , 2025. Hongyi Zhou, Kai Ye, Erhan Xu, Jin Zhu, Shijin Gong, and Chengchun Shi. Demystifying group relative policy optimization: Its policy gradient is a u-statistic. arXiv preprint arXiv:2603.01162 ,2026. 10 Preprint 

## A PROOFS 

This appendix collects the proofs and supporting derivations for the statements used in the main text. We keep the notation of the main paper: the reparameterized action is written as at = πθ (st, ξ t), the differentiable transition as st+1 = fθ (st, a t), and the costate as λt = ∂R t/∂s t.A.1 PATHWISE DIFFERENTIATION UNDER A DIFFERENTIABLE ROLLOUT 

Under Definition 1, all stochasticity enters through the exogenous noise variables ξ1: T , whose law is independent of θ. Hence, for a fixed noise realization, the sampled return R1(θ, ξ 1: T ) is a deterministic differentiable function of θ. Therefore, 

J(θ) = Eξ1: T [R1(θ, ξ 1: T )] = 

Z

R1(θ, ξ 1: T ) p(ξ1: T ) dξ 1: T .

Under the differentiability and domination assumptions implicit in Definition 1, differentiation may be interchanged with integration: 

∂J (θ)

∂θ =

Z ∂R 1(θ, ξ 1: T )

∂θ p(ξ1: T ) dξ 1: T = E

 ∂R 1

∂θ 



.

Thus, in the differentiable rollout model, the policy-gradient direction can be computed pathwise by differentiating the unrolled trajectory computation. A.2 PROOF OF PROPOSITION 1: ADJOINT RECURSION 

Proof. Recall that 

Rt = r(st, a t) + γR t+1 , at = πθ (st, ξ t), st+1 = fθ (st, a t), λt := ∂R t

∂s t

.

Throughout the proof, the noise variables ξt:T are held fixed. First differentiate Rt with respect to st. The immediate reward contributes the total derivative 

Dr (st, a t) = ∂r (st, a t)

∂s +

 ∂π θ (st, ξ t)

∂s 

⊤ ∂r (st, a t)

∂a .

The future return contributes through st+1 = fθ (st, a t):

γ

 ∂f θ (st, a t)

∂s + ∂f θ (st, a t)

∂a ∂π θ (st, ξ t)

∂s 

⊤

λt+1 = γ Df θ (st, a t)⊤λt+1 .

Therefore, 

λt = Dr (st, a t) + γ Df θ (st, a t)⊤λt+1 ,

which is the adjoint recursion equation 6. It remains to derive the parameter-gradient expression. Let 

˙st := ds t

dθ .

The total state sensitivity satisfies 

˙st+1 = Df θ (st, a t) ˙ st + ∂f θ (st, a t)

∂θ + ∂f θ (st, a t)

∂a t

∂π θ (st, ξ t)

∂θ .

The total derivative of the return is 

dR 1

dθ =

> T

X

> t=1

γt−1

"

˙s⊤ 

> t

Dr (st, a t) + 

 ∂π θ (st, ξ t)

∂θ 

⊤ ∂r (st, a t)

∂a t

#

.

Using the adjoint identity 

Dr (st, a t) = λt − γ Df θ (st, a t)⊤λt+1 ,

11 Preprint we obtain 

dR 1

dθ =

> T

X

> t=1

γt−1

"  ∂π θ (st, ξ t)

∂θ 

⊤ ∂r (st, a t)

∂a t

+ ˙ s⊤ 

> t

λt

− γ ˙s⊤

> t

 Df θ (st, a t)⊤λt+1 

#

.

From the state-sensitivity recursion, 

Df θ (st, a t) ˙ st = ˙ st+1 − ∂f θ (st, a t)

∂θ − ∂f θ (st, a t)

∂a t

∂π θ (st, ξ t)

∂θ .

Substituting this into the previous display gives 

dR 1

dθ =

> T

X

> t=1

γt−1

"  ∂π θ (st, ξ t)

∂θ 

⊤ ∂r (st, a t)

∂a t

+ γ

 ∂f θ (st, a t)

∂θ + ∂f θ (st, a t)

∂a t

∂π θ (st, ξ t)

∂θ 

⊤

λt+1 

#

+

> T

X

> t=1

γt−1  ˙s⊤ 

> t

λt − γ ˙s⊤

> t+1

λt+1 

 .

The second sum telescopes: 

> T

X

> t=1

γt−1  ˙s⊤ 

> t

λt − γ ˙s⊤

> t+1

λt+1 

 = ˙ s⊤ 

> 1

λ1 − γT ˙s⊤ 

> T+1

λT +1 .

The initial state is the prompt state and is independent of θ, so ˙s1 = 0 . Also λT +1 = 0 by definition. Hence the telescoping term vanishes. Collecting the remaining terms yields 

dR 1

dθ =

> T

X

> t=1

γt−1

"

γ

 ∂f θ (st, a t)

∂θ 

⊤

λt+1 

+

 ∂π θ (st, ξ t)

∂θ 

⊤ ∂r (st, a t)

∂a t

+ γ

 ∂f θ (st, a t)

∂a t

⊤

λt+1 

! # 

.

Taking expectation over the exogenous noise gives equation 7. A.3 PROOF OF IDENTITY EQUATION 9: COSTATES ARE VALUE -GRADIENT ESTIMATORS 

Proof. By definition, 

V πt (s) = E[Rt | st = s] .

Conditioned on st = s, the remaining randomness comes only from the future exogenous noises 

ξt:T , whose distribution is independent of s and θ. Therefore, under the regularity conditions of Definition 1, 

Gπt (s) = ∂V πt (s)

∂s = ∂∂s E[Rt | st = s]= E

 ∂R t

∂s t

st = s



= E[λt | st = s] .

Thus the BPTT costate is a Monte Carlo sample whose conditional expectation is the value gradient. A.4 PROOF OF LEMMA 1: SF = PD UNDER A SHIFT POLICY 

Proof. Fix s. Let 

πθ (a | s) = ν(a − ¯aθ (s)) , u := a − ¯aθ (s).

12 Preprint Then a = u + ¯ aθ (s), and the density of u is ν(u), independent of θ. By the chain rule, 

∂∂θ log πθ (a | s) = −

 ∂¯aθ (s)

∂θ 

⊤ ∂∂u log ν(u).

Therefore, 

E



r(s, a ) ∂∂θ log πθ (a | s)



= −

 ∂¯aθ (s)

∂θ 

⊤ Z

r(s, u + ¯ aθ (s)) ∂∂u log ν(u)ν(u) du 

= −

 ∂¯aθ (s)

∂θ 

⊤ Z

r(s, u + ¯ aθ (s)) ∂ν (u)

∂u du. 

Integrating by parts and using the assumed vanishing boundary terms, 

Z

r(s, u + ¯ aθ (s)) ∂ν (u)

∂u du = −

Z

ν(u) ∂r (s, u + ¯ aθ (s)) 

∂u du. 

Since ∂r (s, u + ¯ aθ (s)) 

∂u = ∂r (s, a )

∂a ,

we obtain 

E



r(s, a ) ∂∂θ log πθ (a | s)



=

 ∂¯aθ (s)

∂θ 

⊤

E

 ∂r (s, a )

∂a 



.

This proves equation 10. A.5 LOCAL GRPO/PPO VALUE -GRADIENT FORM 

The main text uses the following consequence of Lemma 1 and Proposition 1. 

Corollary 1 (GRPO is a value-gradient update in expectation) . Assume the differentiable rollout of Definition 1, the shift/additive-noise policy of Lemma 1, and treat the GRPO/PPO advantage weights bAi,t as stop-gradient scalars. Locally at θ = θold , where clipping is inactive, the expected actor-gradient direction of the GRPO/PPO surrogate is pathwise-equivalent to a BPTT adjoint update. The backward signal in that adjoint update satisfies 

E[λt | st = s] = Gπt (s).

Thus the local GRPO/PPO actor update is value-gradient-like in expectation. Proof. At θ = θold ,

ρi,t (θold ) = πθold (oit | si,t )

πθold (oit | si,t ) = 1 .

In a local neighbourhood in which the clipping threshold is not active, the clipped surrogate reduces to the unclipped likelihood-ratio term. Hence, with bAi,t held fixed, 

∂∂θ 



ρi,t (θ) bAi,t 



> θ=θold

= bAi,t 

∂∂θ log πθ (oit | si,t )

> θ=θold

.

Therefore, the local actor part of the GRPO/PPO gradient is the usual score-function policy-gradient term: 

E

"

1

G

> G

X

> i=1

1

TiTiX

> t=1

bAi,t 

∂∂θ log πθ (oit | si,t )

#

> θ=θold

.

The KL penalty in equation 1 contributes an ordinary differentiable regularization gradient and does not affect the costate identity. Now apply Lemma 1 token-by-token. For any differentiable local scalar signal hi,t (st, a t) whose stop-gradient coefficient is bAi,t , we have 

E

bAi,t hi,t (st, a t) ∂∂θ log πθ (at | st)



=

 ∂¯aθ (st)

∂θ 

⊤

E

" ∂  bAi,t hi,t (st, a t)

∂a t

#

.

13 Preprint Taking hi,t (st, a t) = r(st, a t) gives exactly equation 11. Thus the score-function form and the pathwise action-derivative form have the same expectation under the shift policy. Summing these pathwise terms over the rollout gives the BPTT gradient of the differentiable trajectory. By Proposition 1, the backward state-sensitivity propagated by this BPTT computation is the costate 

λt. By identity equation 9, 

E[λt | st = s] = Gπt (s).

Hence the local GRPO/PPO actor update is value-gradient-like in expectation. A.6 EMPIRICAL COSTATE RECURSION IN A TRANSFORMER 

Derivation of equation 15. For a fixed trajectory, the local GRPO loss without clipping can be written as 

L(θ) = 

> T

X

> k=1

bAkℓk(θ), ℓk = log πθ (ok | sk).

By Definition 2, 

ˆλt = ∂∂h (L)

> t

X

> k≥t

bAkℓk.

Splitting the k = t term from the future terms gives 

ˆλt = bAt

∂ℓ t

∂h (L)

> t

+ X

> k>t

bAk

∂ℓ k

∂h (L)

> t

.

For k > t , the dependence of ℓk on h(L) 

> t

passes through the transformer computation graph. In particular, the first future step contributes through the Jacobian 

Jt+1 ←t = ∂h (L)

> t+1

∂h (L)

> t

.

Applying the chain rule recursively gives 

X

> k>t

bAk

∂ℓ k

∂h (L)

> t

= J⊤

> t+1 ←t

∂∂h (L)

> t+1

X

> k≥t+1

bAkℓk = J⊤ 

> t+1 ←t

ˆλt+1 .

Therefore, 

ˆλt = bAt

∂ℓ t

∂h (L)

> t

+ J⊤ 

> t+1 ←t

ˆλt+1 , ˆλT +1 = 0 ,

which is equation 15. A.7 PROOF OF PROPOSITION 2: SAMPLING -GAP BOUND 

Proof. The exact relaxed transition Jacobian decomposes as in equation 20: 

Df exact  

> θ

= Jattn  

> t+1 ←t

+ ∂h (L)

> t+1

∂e t

∂e t

∂o t

∂o t

∂z t

∂z t

∂h (L)

> t

.

Thus, 

Df exact  

> θ

− Jattn  

> t+1 ←t

≤ ∂h (L)

> t+1

∂e t

∂e t

∂o t

∂o t

∂z t

∂z t

∂h (L)

> t

.

The first, second, and fourth factors are controlled by the embedding matrix, the output head, and the local Lipschitz constants of the transformer block. It remains to control the effective Jacobian through sampling. For a soft or straight-through relaxation, the categorical sample is replaced by a differentiable soft sample with probabilities 

pt = softmax( zt/τ ),

14 Preprint where τ > 0 is the relaxation temperature. Its Jacobian has the form 

∂o t

∂z t

= 1

τ

 diag( pt) − ptp⊤

> t

 .

The matrix diag( pt) − ptp⊤ 

> t

is the covariance matrix of a categorical one-hot vector. Since it is positive semidefinite, 

diag( pt) − ptp⊤ 

> t

≤ Tr  diag( pt) − ptp⊤

> t

 = 1 − ∥ pt∥22.

Let pmax := max j pt,j . Then 

1 − ∥ pt∥22 ≤ 1 − p2max ≤ 2(1 − pmax ).

For a fixed vocabulary size |V |, normalized entropy controls the distance from a point mass: there exists a finite constant CV such that 

1 − pmax ≤ CV

s

Ht

log |V | .

This follows because the simplex is compact and the ratio 

1 − pmax 

pHt/ log |V |

has a finite continuous extension at the deterministic vertices, where both the numerator and the entropy vanish. Combining the previous bounds gives 

∂o t

∂z t

≤ 2CV

τ

s

Ht

log |V | .

Absorbing the fixed vocabulary, temperature, embedding, head, and local Lipschitz constants into a single constant C, we obtain 

Df exact  

> θ

− Jattn  

> t+1 ←t

≤ C

s

Ht

log |V | ,

which is equation 21. A.8 PROOF OF PROPOSITION 3: ATTENTION -PATHWAY RANK AND MAGNITUDE 

Proof. Consider a single causal-attention head. Its output at position t has the form 

Attn t = X

> t′≤t

αt,t ′ WV ht′ .

For a fixed earlier position t′, the Jacobian of this head output with respect to ht′ maps into the 

dhead -dimensional value subspace of that head. Hence its rank is at most dhead . This remains true when including the derivative of the attention weights αt,t ′ , because the output of a single head is still a vector in Rdhead .With nheads heads, the multi-head attention output is the concatenation and output projection of 

nheads such head outputs. Therefore, the rank of the cross-position attention contribution in one layer is at most 

dhead nheads .

Across L layers, the attention-pathway Jacobian can be written as a sum of cross-position contributions transported through the intervening layer Jacobians. Since the rank of a sum is at most the sum of ranks, the total attention-pathway rank is bounded by 

L d head nheads .

This proves the stated rank bound. For the magnitude claim, each head contribution contains factors of the attention weight αt,t ′ , value projection WV , output projection, and softmax-score derivatives. Under bounded projection norms and bounded hidden-state norms, the contribution from position t′

is therefore controlled by the size of the corresponding attention weights. Thus broadly distributed attention provides more cross-position credit pathways, while small attention weights suppress the corresponding Jacobian entries. 15 Preprint A.9 PROOF OF THEOREM 1: DISCRETE GRPO COSTATES APPROXIMATE BPTT COSTATES 

Proof. Define the conditional costate error 

δt := E[ˆλt | ht] − E[λt | ht].

The empirical costate recursion is 

ˆλt = bAt

∂ℓ t

∂h (L)

> t

+  Jattn 

> t+1 ←t

⊤ ˆλt+1 .

The exact relaxed BPTT recursion has the form 

λt = Dr t + γ  Df exact 

> θ

⊤ λt+1 .

Under the local SF-to-PD identification of Lemma 1, the immediate credit terms align in expectation. Thus the difference between the two conditional recursions is caused by the propagated future error and by the missing sampling-path Jacobian: 

δt = γ  Jattn 

> t+1 ←t

⊤ δt+1 + γ  Df exact  

> θ

− Jattn 

> t+1 ←t

⊤ E[λt+1 | ht].

Taking norms gives 

∥δt∥ ≤ γ Jattn  

> t+1 ←t

∥δt+1 ∥ + γ Df exact  

> θ

− Jattn  

> t+1 ←t

∥E[λt+1 | ht]∥ .

Let 

ϵt+1 := Df exact  

> θ

− Jattn  

> t+1 ←t

∥E[λt+1 | ht]∥ .

By Proposition 2, this per-step error is controlled by the entropy-dependent sampling-gap bound. Hence 

∥δt∥ ≤ γ Jattn  

> t+1 ←t

∥δt+1 ∥ + γϵ t+1 .

Unrolling this inequality backward from δT +1 = 0 yields 

∥δt∥ ≤ 

> T

X

> k=t+1

γk−t



> k−1

Y

> j=t+1

Jattn 

> j←j−1

 ϵk.

The main text writes the Jacobian chain compactly as ∥Jattn 

> k←k−1

∥k−t−1, which should be read as this product or as a uniform upper bound on the product. Therefore, 

E[ˆλt | ht] − E[λt | ht] ≤

> T

X

> k=t+1

γk−t · Jattn 

> k←k−1
> k−t−1

| {z }

> Jacobian chain growth

·ϵk,

which is equation 23. A.10 PROOF OF THE USABLE -SIGNAL LOWER BOUND 

Proof. Let 

u(m) 

> t

:= E[ˆλ(m) 

> t

| ht], vt := Gt.

Then 

⟨u(m) 

> t

, v t⟩ = ∥vt∥22 + ⟨u(m) 

> t

− vt, v t⟩.

By Cauchy–Schwarz, 

⟨u(m) 

> t

− vt, v t⟩ ≥ −∥ u(m) 

> t

− vt∥2 ∥vt∥2.

Therefore, 

⟨u(m) 

> t

, v t⟩ ≥ ∥ Gt∥22 − ∥ Gt∥2 E[ˆλ(m) 

> t

| ht] − Gt 2

.

Taking expectation over q, τ, t gives 

Eq,τ,t 

hD 

E[ˆλ(m) 

> t

| ht], G t

Ei 

≥ Σ( θ) − Λm(θ),

with Σ( θ) and Λm(θ) defined in equation 26. This proves equation 25. 16