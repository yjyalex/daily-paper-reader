---
title: Value-Gradient Hypothesis of RL for LLMs
title_zh: RL用于LLM的价值梯度假说
authors: "Arip Asadulaev, Daniil Ognev, Karim Salta, Martin Takac"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21654v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: RL技术在大语言模型中的理论分析
tldr: 本文提出强化学习（RL）微调大型语言模型（LLM）的价值梯度假说，解释无评论家方法（如PPO、GRPO）的有效性。在可微滚动和加性噪声假设下，证明actor更新近似于价值梯度；在离散transformer中，自动微分提供近似价值信号，误差受采样间隔和策略熵控制。将RL影响分解为价值梯度信号和可达奖励空间，为预训练轨迹中RL的最优时机提供判据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21654v1/fig-001.webp\", \"caption\": \"Figure 3: Results of the RL upon various OLMO-2 pretraining checkpoints. The left image shows the consistency of the achieved gains via RL using a different number of GRPO steps. The right image shows the behavior of the different components of our study.\", \"page\": 8, \"index\": 1, \"width\": 818, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.21654v1/fig-002.webp\", \"caption\": \"Figure 5: Z-scores of the gain after RL vs. z-scores of the predicted RL impact. Correlation (left) and curve per checkpoint (right). The std bars on left indicate RL gain variance over the various training steps.\", \"page\": 9, \"index\": 2, \"width\": 826, \"height\": 286}]"
motivation: 揭示无评论家RL方法（如PPO、GRPO）在LLM后训练中有效的原因及何时效果最佳。
method: 通过可微滚动和加性噪声参数化证明actor更新在期望上类似价值梯度，并在离散transformer中验证自动微分近似价值信号。
result: RL影响可分解为价值梯度信号和可达奖励空间，误差由采样间隔和策略熵控制。
conclusion: 给出了基于预训练轨迹的RL有效性判据，为RL在LLM后训练中的最优应用时机提供理论依据。
---

## 摘要
强化学习显著改进了预训练语言模型，但为何PPO和GRPO等无评论家方法同样有效，以及它们在何时应提供最大增益，仍研究不足。我们提出了用于LLM后训练的无评论家RL的价值梯度视角。首先，在可微展开和加性噪声参数化下，我们证明演员更新在期望上类似于价值梯度：反向传播传递共态，其条件期望等于价值梯度。其次，对于离散Transformer策略，我们证明通过注意力的自动微分产生经验共态，该共态近似该价值信号，其误差由采样间隔和策略熵控制。这些结果将RL影响分解为价值梯度信号和可达到的奖励余量，从而得出预训练轨迹上RL何时应最有效的标准。

## Abstract
Reinforcement learning substantially improves pretrained language models, but it remains understudied why critic-free methods such as PPO and GRPO work as well as they do, and when they should provide the largest gains. We develop a value-gradient perspective of critic-free RL for LLM post-training. First, under a differentiable rollout and additive-noise parameterization, we show that the actor update is value-gradient-like in expectation: the backward pass propagates costates whose conditional expectation equals the value gradient. Second, for discrete transformer policies, we show that autodifferentiation through attention produces empirical costates that approximate this value signal, with an error controlled by the sampling gap and policy entropy. These results motivate a decomposition of RL impact into value gradient signal and reachable reward headroom, yielding a criterion for when RL should be most effective along a pretraining trajectory.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：强化学习（RL）广泛用于大型语言模型（LLM）的后训练，显著提升推理能力，但PPO、GRPO等无评论家（critic-free）方法在经典RL理论中本应在长时域信用分配上失效，却在LLM中表现优异。论文试图解释这一“理论-实践”矛盾。
- **整体含义**：提出**价值梯度假说**，认为无评论家RL在LLM中的成功并非价值缺失，而是**演员网络的反向传播本身携带了类似价值梯度的信号**。该信号在可微展开中精确等于价值梯度，在离散Transformer中通过注意力机制近似保留。论文进一步将RL增益分解为**可用价值梯度信号**和**可达到奖励余量**，从而预测RL的最佳作用时机。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 通过**连续松弛**（continuous relaxation）将离散的token采样转化为可微的加性噪声参数化，从而将GRPO/PPO的演员更新在期望上与**路径梯度**（pathwise gradient）等价。
  - 引入**共态**（costate）概念：反向传播传播的状态敏感性，其条件期望等于价值函数的梯度。
  - 在离散Transformer中，自动微分通过注意力机制计算**经验共态**，其递归形式与BPTT共态递归结构相同，唯一缺失的是通过token采样的雅可比路径，该误差受策略熵控制。
- **关键技术细节**：
  - **Lemma 1**：在加性噪声策略下，得分函数更新与路径更新期望等价。
  - **Proposition 1**：给定可微展开，共态满足伴随递归 \(\lambda_t = Dr(s_t, a_t) + \gamma (Df_\theta(s_t, a_t))^\top \lambda_{t+1}\)。
  - **Corollary 1**：在连续松弛和加性噪声策略下，GRPO局部演员更新在期望上是价值梯度更新。
  - **Theorem 1**：离散GRPO的经验共态近似BPTT共态，误差由采样间隔和注意力雅可比链控制，且误差有熵相关上界。
  - **RL影响分解**：定义可用价值梯度信号 \(S_m(\theta) = \Sigma(\theta) - \Lambda_m(\theta)\) 和可达到奖励余量 \(H_\alpha(\theta)\)，预测RL增益 \(\propto S_m(\theta) \cdot H_\alpha(\theta)\)。
- **算法流程**（文字说明）：
  1. 对每个预训练检查点，采样轨迹并计算 token 级 GRPO 损失。
  2. 通过自动微分计算隐藏状态上的经验共态 \(\hat{\lambda}_t\)（即 \( \partial L / \partial h_t^{(L)} \)）。
  3. 使用真实奖励计算精确路径共态 \(\lambda_t = \partial R / \partial h_t\)。
  4. 计算 \(\Sigma = \mathbb{E}\|G_t\|^2\)，\(\Lambda_m = \mathbb{E}[\|G_t\| \|\hat{\lambda}_t - G_t\|]\)，\(H_\alpha = \frac{1}{\alpha}\log\mathbb{E}[e^{\alpha R}] - \mathbb{E}[R]\)。
  5. 组合得到预测影响分数 \(P_{gap} = (\Sigma - \Lambda_m) H_\alpha\)。

### 3. 实验设计

- **使用场景与任务**：
  - **可控标签复制任务**：给定包含目标标签 {A,B,C,D} 的提示，模型需将概率质量放在匹配的答案 token 上。奖励为 \(R(\theta; q) = p_\theta(y^*|q)\)，这种奖励在模型 logits 上是可微的，便于精确计算共态。
- **使用的模型与检查点**：
  - **OLMo-2 1B**，预训练步骤从 50k 到 1M，每 50k 一个检查点。
- **对比方法**：
  - 主要对比不同预训练检查点的 RL 增益，以及预测分数与实际增益的相关性。
  - 未直接对比其他RL算法，而是以真实RL增益作为基准。
- **RL算法与超参数**：
  - 使用 GRPO，RL 更新步数 \(K \in \{10, 20, 25, 30\}\)，取平均增益。
- **评估指标**：
  - 平均 RL 增益 \(\Delta R = \mathbb{E}_K[R_{\text{after}} - R_{\text{before}}]\)。
  - 预测分数与真实增益的 Spearman 相关系数；组合分数（z(R_before) + z(P_gap)）与 z(R_after) 的 Spearman 相关系数。

### 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量、训练时长等具体算力信息。
- 仅提及使用了 OLMo-2 1B 模型，进行多检查点（约20个）的 RL 训练（每个 K 取值下训练若干步），以及共态计算。推测算力需求中等。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：20个预训练检查点 × 4个 RL 步数设置（10/20/25/30） → 80组 RL 训练结果（取平均）。
  - 熵界验证实验：使用相同模型检查点，对比真实共态误差与熵上界。
  - 预测相关分析：计算影响分数与真实增益的相关性。
- **充分性评价**：
  - **优点**：覆盖了不同预训练阶段的检查点，验证了预测分数的单调趋势；使用了多个 RL 步数以消除单一预算的噪声。
  - **不足**：任务为合成标签复制任务，仅一个任务，且奖励设计为可微，与真实 LLM 应用（如数学推理、代码生成）差异较大；未在多个不同规模模型（如 7B、70B）上验证；未对比其他RL方法（如 PPO 不带评论家版）或带有评论家的 RL（如 PPO+critic）；未进行消融实验分离 Σ 和 Λ_m 的贡献。

### 6. 论文的主要结论与发现

- 无评论家 RL 在 LLM 中有效是因为**演员反向传播携带了价值梯度信号**。
- 在连续松弛下该信号精确；在离散 Transformer 中通过注意力机制近似保留，**误差由策略熵控制**。
- RL 增益由两个因素乘积决定：**可用价值梯度信号**（即共态与真值梯度的对齐程度）和**可达到奖励余量**（即策略分布中尚可提升的奖励空间）。
- 实证上，预训练检查点的最优 RL 启动点不是最早也不是最晚，而是**处于价值梯度信号较强且奖励余量足够大的中间阶段**。
- 组合预测分数（z(R_before) + z(P_gap)）与真实 RL 后奖励的 Spearman 相关系数约 0.73，优于单独使用当前奖励或影响分数。

### 7. 优点

- **理论贡献**：清晰解释了无评论家 RL 在 LLM 中成功的机制，桥接了经典 RL 理论与 LLM 实践。
- **方法创新**：提出共态视角和 RL 影响分解，为预训练检查点选择提供可计算指标。
- **实验设计**：使用可微奖励便于精确计算共态，避免了解码、分类器带来的离散性；控制 RL 步数取平均，增强结果稳健性。
- **结果可靠**：预测分数与真实增益呈中等正相关，组合分数预测后奖励更准确。

### 8. 不足与局限

- **任务过于简化**：仅一个合成标签复制任务，奖励可直接从 logits 计算，不能充分代表真实 LLM 推理场景（如数学、代码、对话）的复杂奖励信号（可能非可微、稀疏、具有延迟）。
- **模型规模有限**：仅使用 1B 级别模型，未验证在更大模型（7B/70B）上结论是否一致。
- **RL 方法单一**：仅用 GRPO，未对比其他 actor-critic 或 critic-free 方法（如 REINFORCE、PPO with critic）。
- **未与现有理论深度对比**：未定量比较该预测分数与其他启发式（如奖励饱和、梯度范数）的性能。
- **计算共态需访问真实奖励**：在真实场景中无法获得精确 G_t，因此该指标目前仅用于分析，难以直接作为离线选择检查点的工具。
- **实验未进行消融**：未单独验证 Σ 和 Λ_m 各自对预测的贡献，也未分析 H_α 不同 α 的影响。
- **偏差风险**：可微奖励可能导致共态估计偏乐观，在实际非可微奖励（如判别器、裁判）中误差可能更大。

（完）
