---
title: "The Constraint Tax: Measuring Validity-Correctness Tradeoffs in Structured Outputs for Small Language Models"
title_zh: 约束税：衡量小语言模型结构化输出中的有效性与正确性权衡
authors: Jaideep Ray
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.26128v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 测量小模型中结构化输出约束导致的准确率损失
tldr: "论文研究小语言模型（<3B参数）在结构化输出约束下的性能权衡。提出“约束税”协议，测量固定模型、任务和实例下，硬模式解码将模式有效性从61.5%提升至100%，但答案准确性从19.7%降至11.0%，错误有效模式输出从49.5%升至88.9%。结论：应分别报告多种准确性指标，建议“先推理后约束”的设计模式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26128v1/fig-001.webp\", \"caption\": \"Figure 2: Main GPU experiment. Hard answer-only schema decoding reaches 100% validity, but answer accuracy falls and the wrong-valid-schema rate rises sharply.\", \"page\": 4, \"index\": 1, \"width\": 499, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.26128v1/fig-002.webp\", \"caption\": \"Table 2: Output modes used by the benchmark. The modes separate prompt-only formatting, regex constraints, direct schemas, structured rationales, typed traces, and delayed packaging.\", \"page\": 3, \"index\": 2, \"width\": 520, \"height\": 452}]"
motivation: 验证硬输出约束在小模型上是否如通常假设般不改变底层答案的正确性。
method: "通过对比15,000次生成中，硬模式解码与无约束解码的答案准确性、模式有效性和可执行准确性。"
result: "硬约束提升模式有效性至100%，但答案准确性下降近一半（19.7%→11.0%），错误有效模式输出大幅增加。"
conclusion: 生产系统应分开报告模式有效性、答案准确性、可执行准确性和错误有效模式率，并采用延迟约束策略。
---

## 摘要
生产级LLM系统越来越多地需要机器可读的输出：JSON对象、类型化轨迹、正则表达式约束字段和工具调用模式。本文针对设备端和低成本的小语言模型（SLM）部署，其中小于3B的模型由于隐私、延迟和商品硬件而具有吸引力，但在解决任务的同时满足模式的能力有限。通常的工程假设是硬输出约束可以在不改变底层答案的情况下提高可靠性。我们证明这一假设对于小模型是不安全的。我们引入了\emph{约束税}，一种测量协议，用于在固定模型、固定任务分布和固定问题实例下，隔离由结构化输出约束引起的答案和可执行准确率损失。在Qwen2.5-0.5B、Qwen2.5-1.5B和SmolLM2-1.7B的15000次商品GPU生成中，硬答案仅模式解码将模式有效性从61.5\%提高到100.0\%，但将答案准确率从19.7\%降低到11.0\%，并将错误有效模式输出从49.5\%增加到88.9\%。最强的工业类比是确定性日历工具调用任务：Qwen2.5-1.5B在仅提示JSON的情况下达到91.5\%的可执行准确率，但在相同的硬工具调用模式下仅为48.0\%，而两种模式均达到100.0\%的模式有效性。错误是语义性的，而非结构性的。我们还表明，3B边界仍然承受直接模式税，并且延迟封装支持建设性的设计模式：自由推理，延迟约束。实际结论是直接的：生产系统应分别报告模式有效性、答案准确率、可执行准确率和错误有效模式率。

## Abstract
Production LLM systems increasingly require machine-readable outputs: JSON objects, typed traces, regex-constrained fields, and tool-call schemas. This paper targets on-device and low-cost small language model (SLM) deployments, where sub-3B models are attractive for privacy, latency, and commodity hardware but have limited capacity to satisfy schemas while solving tasks. The usual engineering assumption is that hard output constraints improve reliability without changing the underlying answer. We show that this assumption is unsafe for small models. We introduce \emph{constraint tax}, a measurement protocol for isolating the answer and executable-accuracy loss caused by structured-output constraints at fixed model, fixed task distribution, and fixed problem instances. Across 15,000 commodity-GPU generations with Qwen2.5-0.5B, Qwen2.5-1.5B, and SmolLM2-1.7B, hard answer-only schema decoding raises schema validity from 61.5\% to 100.0\%, but lowers answer accuracy from 19.7\% to 11.0\% and increases wrong-valid-schema outputs from 49.5\% to 88.9\%. The strongest industry analogue is a deterministic calendar tool-call task: Qwen2.5-1.5B achieves 91.5\% executable accuracy with prompt-only JSON but only 48.0\% under the same hard tool-call schema, while both modes are 100.0\% schema-valid. The error is semantic, not structural. We also show that the 3B boundary still pays a direct-schema tax and that delayed packaging supports a constructive design pattern: reason free, constrain late. The practical conclusion is direct: production systems should report schema validity, answer accuracy, executable accuracy, and wrong-valid-schema rate separately.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：当前生产级LLM应用普遍要求模型输出机器可读的结构化内容（JSON、类型化轨迹、工具调用参数等）。通常假设硬输出约束仅改善包装层，不改变底层答案。但作者认为对于小语言模型（SLM，参数<3B），解码预算有限，格式约束会与推理任务竞争容量，导致**有效但错误的答案**（wrong-valid-schema）增加。

**整体含义**：论文挑战了“结构化输出无害”的工程假设，证明在小模型上，强制模式解码会引入**约束税（constraint tax）**——即语义正确性的损失。这一发现对设备端部署、边缘计算等低资源场景具有直接指导意义。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：定义并量化约束税——在固定模型、固定任务分布和固定问题实例下，结构化输出约束相对于无约束基线造成的准确性下降。

**关键技术细节**：
- **约束税公式**（公式2、3）：
  - 绝对约束税：\( \text{Tax}(m,t,c;b) = \max(0, \text{Acc}(m,t,b) - \text{Acc}(m,t,c)) \)（当约束提高准确率时取0，不为负）
  - 归一化约束税：\( \text{Tax}_{\text{norm}} = \text{Tax} / \max(\epsilon, \text{Acc}_{\text{baseline}}) \)
- **错误分类体系**：输出被分为correct_valid、invalid_json、parse_failure_freeform、schema_validation_error、trace_answer_contradiction、wrong_answer_valid_schema等类别。
- **指标分离**：模式有效性、答案准确性、可执行准确性、轨迹正确性、错误有效模式率、延迟、token数等分别记录。
- **设计模式**：提出“先推理后约束”（reason free, constrain late），让模型先自由推理，再将答案包裹为所需格式。

**算法流程**（文字说明）：
1. 确定性任务生成器产生固定问题实例
2. 通过不同的输出模式（自由格式、仅提示JSON、正则约束、硬模式解码、延迟约束等）生成模型回答
3. 使用后端路由器（MLX、HuggingFace、vLLM、SGLang）执行推理
4. 解析器+验证器+执行检查器分别评估语法和语义正确性
5. 记录约束税指标

## 3. 实验设计：使用的数据集/场景、基准、对比方法

**任务集**：确定性合成任务（非真实用户数据，用于可控测量）
- arithmetic_two_step（两步算术）
- symbolic_string（取单词最后一个字母拼接）
- object_tracking（物品交换追踪）
- boolean_logic（布尔逻辑运算）
- tool_call_argument（日历工具调用参数）

**基准（baseline）**：`prompt_json`（仅通过提示要求JSON格式）作为无硬约束基线。

**对比方法**：
- freeform（自由格式回答）
- freeform_direct（仅最终答案行）
- freeform_brief_reasoning（简短推理+答案）
- prompt_json（仅提示使用JSON）
- final_only_regex（最终答案正则约束）
- answer_only_schema（最小答案JSON模式）
- rationale_answer_schema（含推理和答案的JSON模式）
- typed_trace_schema（类型化轨迹JSON）
- delayed_constraint（先自由推理，再延迟包裹）

**实验场景**：
- 主GPU套件：5个任务×3个模型×1000实例×5种模式 = 15000次生成
- 日历工具调用模拟：Qwen2.5-1.5B，200实例，对比prompt_json vs hard schema + 延迟包裹控制
- 后端复制：vLLM与SGLang对比
- 3B边界检查：Qwen2.5-3B
- 本地扩展接口研究：4个公共小模型，20实例/任务，完整接口集

## 4. 资源与算力

**明确说明**：
- 主GPU套件：使用单块**商品级GPU**（具体型号未明确，但提及“commodity GPU”），运行vLLM后端。
- 本地扩展接口研究：使用**MLX**在Mac上本地运行（无需GPU）。
- 后端复制：vLLM与SGLang在同一GPU上。
- 未提及GPU数量、训练时长（因为是推理实验）。

**结论**：论文未给出精确的GPU型号、显存或运行时间，但强调**低成本计算**。约束解码需要vLLM/SGLang服务，SGLang需要RMSNorm兼容补丁。

## 5. 实验数量与充分性

**实验数量**：
- 主套件：15,000次生成（5任务×3模型×1000实例×5模式）
- 日历工具调用：200实例/模式
- 边界检查：1000实例/模式
- 局部研究：20实例×4模型×完整接口集（约3200次生成）
- 总共约18,400次生成。

**充分性评价**：
- **优点**：任务全面覆盖推理、符号操作、工具调用等典型场景；多种输出模式对比；后端复制验证跨平台稳定性；统计使用95% bootstrap置信区间。
- **不足**：仅使用3个主模型（Qwen2.5-0.5B、1.5B、SmolLM2-1.7B），缺乏更多模型族；局部研究样本量小（20实例/任务）；任务全是确定性合成，与真实用户分布有差距。

**公平性**：固定问题实例、相同后端、相同prompt模板，比较公平。但不同模型能力差异大（SmolLM2基线弱，约束反而提高）。

## 6. 论文的主要结论与发现

1. **硬模式解码提升有效性但牺牲正确性**：主套件中，answer_only_schema将模式有效性从61.5%提升至100%，但答案准确率从19.7%降至11.0%，错误有效模式率从49.5%飙升至88.9%。

2. **日历工具调用中语义性错误突出**：两种模式均100%有效，但硬模式可执行准确率仅48.0%，相比提示JSON的91.5%下降43.5个百分点。错误主要是duration字段错误（180分钟而非30分钟），是语义而非语法问题。

3. **3B边界仍存在约束税**：Qwen2.5-3B的答案准确率从39.0%（prompt_json）降至23.7%（answer_only_schema），错误有效模式率从43.6%升至75.2%。

4. **延迟约束模式最优**：先自由推理后包装的delayed_constraint在本地研究中达到40.7%可执行准确率和100%有效性，优于直接硬模式。

5. **后端敏感性**：SmolLM2在vLLM和SGLang上结果不同（18.7% vs 25.7%），说明结构化输出是系统属性而非模型属性。

6. **建议**：生产系统应报告四个指标（模式有效性、答案准确率、可执行准确率、错误有效模式率），并采用“先推理后约束”模式。

## 7. 优点：方法或实验设计上的亮点

- **任务可复现性**：使用确定性合成任务，提供确切地面真值，避免使用LLM作为裁判的偏差。
- **指标分离**：将语法有效性、语义正确性、可执行性分开测量，揭示隐藏的语义回归。
- **约束税公式**：提出清晰量化指标（绝对/归一化），带置信区间。
- **错误分类体系**：输出按错误类型分类，比简单通过/失败更具操作性。
- **后端敏感性分析**：跨vLLM/SGLang复制，揭示后端依赖。
- **建设性结论**：不仅报告问题，还提出“延迟约束”设计模式作为解决方案。

## 8. 不足与局限

- **合成任务泛化性有限**：论文明确承认确定性任务不代表真实用户分布，日历任务是受控模拟而非真实代理日志。
- **模型覆盖度小**：仅3个主模型（含1个3B），缺乏GPT-2、Llama、Phi等更多族系；3B边界仅检验一个模型。
- **局部研究样本量小**：20实例/任务×4模型，统计效力有限，设计证据而非显著性结论。
- **未建立规模定律**：未测试更大模型（≥7B），无法判断约束税何时消失。
- **算力未明确**：GPU型号、运行时间缺失；虽然“低成本”是卖点，但缺少精确复现所需细节。
- **仅推理实验**：未涉及训练或微调；提示工程简单（仅一条prompt模板），可能影响基线表现。
- **偏差风险**：合成任务可能偏向容易受约束干扰的模式（如算术、符号操作），是否扩展到开放域任务未知。

---

（完）
