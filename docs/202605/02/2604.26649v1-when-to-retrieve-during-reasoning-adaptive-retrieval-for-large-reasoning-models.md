---
title: "When to Retrieve During Reasoning: Adaptive Retrieval for Large Reasoning Models"
title_zh: 推理过程中何时检索：大推理模型的自适应检索
authors: "Dongxin Guo, Jikun Wu, Siu Ming Yiu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26649v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大推理模型的自适应检索
tldr: 针对大语言模型在长链推理中与传统RAG系统不匹配的问题，本文提出ReaLM-Retrieve框架。该框架通过步骤级不确定性检测、自适应检索干预策略及高效集成机制，实现了在推理过程中按需获取外部知识。实验表明，该方法在多跳推理任务上显著提升了准确率，同时大幅减少了检索频率，优化了效率与精度的平衡。
selection_source: fresh_fetch
motivation: 传统RAG在推理开始前提供背景，而长链推理模型需要在多步推理过程中动态注入证据以填补知识空白。
method: 提出包含步骤级不确定性检测器、自适应检索干预策略和高效集成机制的ReaLM-Retrieve框架。
result: "在多个多跳推理基准上，该方法比标准RAG平均提升10.1%的F1值，且检索调用次数比固定间隔方法减少47%。"
conclusion: ReaLM-Retrieve为推理密集型任务建立了新的效率与准确性权衡标准，证明了自适应检索在复杂推理中的优越性。
---

## 摘要
诸如 DeepSeek-R1 和 OpenAI o1 等大推理模型会生成跨越数千个 token 的长思维链，然而它们与检索增强生成（RAG）的集成在根本上仍不匹配。当前的 RAG 系统优化目标是在推理开始前提供上下文，而推理模型则需要在多步推理链中注入证据。我们提出了 ReaLM-Retrieve，这是一个感知推理的检索框架，通过三项关键创新解决了这一不匹配问题：(1) 步级不确定性检测器，在推理步粒度而非 token 或句子级别识别知识差距；(2) 检索干预策略，学习外部证据何时能最大限度地惠及正在进行的推理；(3) 效率优化的集成机制，与朴素集成相比，将单次检索开销降低了 3.2 倍。在 MuSiQue、HotpotQA 和 2WikiMultiHopQA 上的实验表明，ReaLM-Retrieve 在答案 F1 值上比标准 RAG 平均提高了 10.1% 的绝对值（三个基准测试的范围为 9.0-11.8%），同时与 IRCoT 等固定间隔方法相比，检索调用次数减少了 47%（所有改进在 p<0.01 的配对自助法检验下均显著）。在需要 2-4 跳推理的挑战性 MuSiQue 基准测试中，我们的方法达到了 71.2% 的 F1 值，平均每个问题仅需 1.8 次检索调用。分析显示，ReaLM-Retrieve 还提高了检索质量本身，在支持性证据上实现了 81.3% 的 Recall@5，且其精确率和 MRR 始终高于固定间隔基准，为推理密集型检索任务建立了新的最先进的效率-准确率权衡。

## Abstract
Large reasoning models such as DeepSeek-R1 and OpenAI o1 generate extended chains of thought spanning thousands of tokens, yet their integration with retrieval-augmented generation (RAG) remains fundamentally misaligned. Current RAG systems optimize for providing context before reasoning begins, while reasoning models require evidence injection during multi-step inference chains. We introduce ReaLM-Retrieve, a reasoning-aware retrieval framework that addresses this mismatch through three key innovations: (1) a step-level uncertainty detector that identifies knowledge gaps at reasoning-step granularity rather than token or sentence level; (2) a retrieval intervention policy that learns when external evidence maximally benefits ongoing reasoning; and (3) an efficiency-optimized integration mechanism that reduces per-retrieval overhead by 3.2x compared to naive integration. Experiments on MuSiQue, HotpotQA, and 2WikiMultiHopQA demonstrate that ReaLM-Retrieve achieves on average 10.1% absolute improvement in answer F1 over standard RAG (range: 9.0-11.8% across the three benchmarks) while reducing retrieval calls by 47% compared to fixed-interval approaches like IRCoT (all improvements significant at p<0.01, paired bootstrap). On the challenging MuSiQue benchmark requiring 2-4 hop reasoning, our method achieves 71.2% F1 with an average of only 1.8 retrieval calls per question. Analysis shows that ReaLM-Retrieve also improves retrieval quality itself, achieving 81.3% Recall@5 with consistently higher precision and MRR than fixed-interval baselines on supporting evidence, establishing new state-of-the-art efficiency-accuracy trade-offs for reasoning-intensive retrieval tasks.

---

## 论文详细总结（自动生成）

这篇论文针对大推理模型（LRMs，如 DeepSeek-R1、OpenAI o1）在长链推理过程中与检索增强生成（RAG）系统不匹配的问题，提出了 **ReaLM-Retrieve** 框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大推理模型能生成数万 token 的思维链（CoT），展现出自我验证和回溯能力，但在处理需要外部事实知识的任务时仍存在幻觉。
*   **核心矛盾**：
    1.  **时间错位**：传统 RAG 在推理开始前检索，而推理模型在推理中途才会遇到知识空白。
    2.  **粒度错位**：现有迭代检索（如 IRCoT）按句子或 token 触发，不符合推理模型以“推理步”（逻辑单元）为操作单位的特性。
    3.  **效率瓶颈**：在长达数万 token 的推理链中，频繁的固定间隔检索会导致巨大的延迟和成本。

### 2. 方法论
ReaLM-Retrieve 的核心思想是**在推理步粒度上实现自适应检索**，包含以下关键技术：
*   **推理步分割（Segmentation）**：使用轻量级 Transformer 分类器识别逻辑推理单元的边界，而非简单的句子分割。
*   **步级不确定性检测（RSUS）**：定义了推理步不确定性评分（Reasoning Step Uncertainty Score），由三部分组成：
    *   **口头化不确定性 ($U_{verb}$)**：提示模型自我评估结论的置信度。
    *   **实体熵 ($U_{ent}$)**：分析推理步中命名实体在语料库中的覆盖稀疏度。
    *   **一致性信号 ($U_{cons}$)**：采样多个推理分支并测量其一致性。
*   **学习型干预策略 ($\pi_\theta$)**：将检索集成建模为上下文多臂老虎机（Contextual Bandit）问题，通过 REINFORCE 算法学习何时检索、如何构建查询以及如何集成上下文。
*   **效率优化集成**：
    *   **隐式压缩**：仅保留检索段落中与当前推理相关的句子，减少 73% 的上下文扩展。
    *   **投机缓存（Speculative Caching）**：并行预检索可能需要的文档，消除 37% 的检索延迟。
    *   **KV 缓存保留**：针对开源模型优化，避免重复计算已有的推理上下文。

### 3. 实验设计
*   **数据集**：MuSiQue（2-4 跳推理）、HotpotQA（2 跳全维基场景）、2WikiMultiHopQA（跨文档推理）。
*   **对比方法（Baselines）**：
    *   无检索（Closed-book）。
    *   单次 RAG（Single RAG）。
    *   固定间隔检索（IRCoT）。
    *   基于概率触发的检索（FLARE）。
    *   基于微调的自反思检索（Self-RAG）。
    *   基于强化学习的搜索决策（Search-R1）。
*   **实验模型**：DeepSeek-R1 (32B/671B)、QwQ-32B-Preview。

### 4. 资源与算力
*   **硬件**：使用 8 张 NVIDIA A100 80GB GPU。
*   **训练细节**：策略网络训练了 50,000 步。
*   **算力消耗**：文中提到训练过程大约消耗了 2,400 GPU 小时（在环境影响章节中提及）。推理时，RSUS 计算仅增加约 8% 的额外开销。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流多跳 QA 基准上进行了全面测试，涵盖了从 32B 到 671B 不同规模的模型。
*   **消融实验**：详细分析了不确定性组件（$U_{verb}, U_{ent}, U_{cons}$）、策略组件和集成优化对结果的影响。
*   **泛化性测试**：验证了模型在不同推理架构（QwQ）和跨基准（HotpotQA 训练，MuSiQue 测试）上的表现。
*   **评价**：实验设计严谨，使用了配对自助法（Paired Bootstrap）进行显著性检验（p < 0.01），并提供了 95% 置信区间，实验结果具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：ReaLM-Retrieve 在答案 F1 值上比标准 RAG 平均提升 10.1%，在 MuSiQue 上达到 71.2% F1。
*   **效率显著**：相比 IRCoT，检索调用次数减少了 47%，单次检索开销降低 3.2 倍，端到端延迟降低 1.33-1.5 倍。
*   **复杂度正相关**：问题越复杂（跳数越多），该方法的优势越明显（4 跳任务上比 IRCoT 提升 8.4% F1）。
*   **检索质量**：自适应触发不仅减少了次数，还提高了检索的精确率和召回率（Recall@5 达到 81.3%）。

### 7. 优点
*   **粒度创新**：首次提出以“推理步”为单位进行检索决策，完美契合长链推理模型的运作方式。
*   **黑盒友好**：RSUS 评分不依赖 token 概率，使其能应用于 OpenAI o1 等仅提供文本输出的闭源 API 模型。
*   **系统级优化**：通过投机缓存和隐式压缩解决了 RAG 在长推理链中的工程落地难题。

### 8. 不足与局限
*   **API 限制**：在无法获取内部状态的闭源模型上，性能比开源模型略低 2-3% F1。
*   **语料库依赖**：43% 的失败案例源于语料库本身缺失信息，系统尚不能很好处理“检索不到”的情况。
*   **分布偏移**：当检索注入超过 3 次后，后续推理步的分割和不确定性检测准确度会有所下降。
*   **长上下文压力**：对于超过 30,000 token 的极长推理链，KV 缓存的内存压力依然巨大。

（完）
