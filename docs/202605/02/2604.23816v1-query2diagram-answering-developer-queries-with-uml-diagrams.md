---
title: "Query2Diagram: Answering Developer Queries with UML Diagrams"
title_zh: Query2Diagram：利用 UML 图回答开发者查询
authors: "Oleg Baryshnikov, Anton M. Alekseev, Sergey I. Nikolenko"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23816v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 微调大语言模型以在UML图生成中输出结构化JSON
tldr: 针对软件文档缺失或过时的问题，本文提出 Query2Diagram，一种基于大语言模型的查询驱动 UML 类图生成方法。该方法通过微调 Qwen2.5-Coder-14B 模型，根据开发者的自然语言查询从代码中提取关键信息并生成语义聚焦的 UML 图。实验表明，该方法在结构准确性和语义相关性上均优于现有模型，为按需生成上下文相关的软件文档提供了可行方案。
selection_source: fresh_fetch
motivation: 传统的自动逆向工程工具生成的 UML 图过于冗杂且缺乏针对性，无法有效回答开发者的具体代码疑问。
method: 通过在包含代码、开发者查询及对应 JSON 格式图表表示的数据集上微调 Qwen2.5-Coder-14B 模型，实现语义聚焦的图表生成。
result: 微调后的模型在 F1 分数上表现最优，显著降低了结构缺陷率，生成的图表在语义上更贴合开发者意图。
conclusion: 本研究证明了利用大语言模型进行可扩展、按需且上下文相关的软件文档自动生成的有效性。
---

## 摘要
软件文档经常过时或完全缺失，然而开发者需要代码库的聚焦视图来理解复杂的系统。虽然自动逆向工程工具可以从代码生成 UML 图，但它们往往会产生冗余的细节，而没有考虑到开发者的意图。我们引入了查询驱动的 UML 图生成，利用大语言模型（LLM）创建直接回答有关代码的自然语言问题的图表。与现有方法不同，我们的方法生成的图表在语义上更加聚焦，仅包含相关元素及上下文描述。我们在由代码文件、开发者查询以及结构化 JSON 格式的相应图表表示组成的精选数据集上微调了 Qwen2.5-Coder-14B，并结合结构缺陷自动检测和语义相关性人工评估进行了评估。结果表明，在少量手动校正的数据上进行微调可带来显著改进：我们的最佳模型实现了最高的 F1 分数，同时将缺陷率降低至最先进的 LLM 以下，生成的图表在结构上稳健且在语义上忠实于开发者的查询。因此，我们确立了使用 LLM 进行可扩展的上下文相关、按需文档生成的的可行性。我们将代码和数据集公开于 https://github.com/i-need-a-pencil/query2diagram。

## Abstract
Software documentation frequently becomes outdated or fails to exist entirely, yet developers need focused views of their codebase to understand complex systems. While automated reverse engineering tools can generate UML diagrams from code, they produce overwhelming detail without considering developer intent. We introduce query-driven UML diagram generation, where LLMs create diagrams that directly answer natural language questions about code. Unlike existing methods, our approach produces semantically focused diagrams containing only relevant elements with contextual descriptions. We fine-tune Qwen2.5-Coder-14B on a curated dataset of code files, developer queries, and corresponding diagram representations in a structured JSON format, evaluating with both automatic detection of structural defects and human assessment of semantic relevance. Results demonstrate that fine-tuning on a modest amount of manually corrected data yields dramatic improvements: our best model achieves the highest F1 scores while reducing defect rates below state-of-the-art LLMs, generating diagrams that are both structurally sound and semantically faithful to developer queries. Thus, we establish the feasibility of using LLMs for scalable contextual, on-demand documentation generation. We make our code and dataset publicly available at https://github.com/i-need-a-pencil/query2diagram.

---

## 论文详细总结（自动生成）

以下是对论文《Query2Diagram: Answering Developer Queries with UML Diagrams》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：软件文档（尤其是 UML 图）对于系统理解至关重要，但往往面临缺失或过时的问题。现有的自动逆向工程（RE）工具虽然能生成 UML 图，但通常只是代码结构的堆砌，缺乏对开发者意图（Intent）的理解，导致生成的图表过于冗长、细节过载，难以直接回答开发者的具体疑问。
*   **核心问题**：如何利用大语言模型（LLM）根据开发者的自然语言查询，从代码中生成语义聚焦、简洁且准确的 UML 图？

### 2. 论文提出的方法论
*   **核心思想**：提出“查询驱动的 UML 生成”任务。通过微调 LLM，使其能够理解代码和查询，并输出一个结构化的 JSON 表示，该表示仅包含与查询相关的类、方法、变量及其关系。
*   **关键技术细节**：
    *   **中间表示（JSON Graph）**：不直接生成 PlantUML 或 Mermaid 源码（易产生语法错误），而是定义了一套 JSON 模式，包含 `nodes`（类型、名称、描述、可见性等）、`edges`（源、目标、描述）和 `packages`（层级结构）。
    *   **数据生成流水线**：
        1.  **代码筛选**：从 GitHub 收集 150 个热门仓库，涵盖 12 种编程语言。
        2.  **查询合成**：利用 DeepSeek-R1 和 QwQ-32B 根据代码生成关于架构、API 或设计模式的问题。
        3.  **图表构建**：先用 Claude 3.5 Sonnet 生成初始 JSON 图表，再由人工进行严格的缺陷修正（Fixed Claude Synth），形成高质量训练集。
    *   **模型微调**：基于 **Qwen2.5-Coder-14B-Instruct**，使用 QLoRA 技术进行监督微调（SFT）。
    *   **受限解码**：推理时使用 Outlines 库强制执行 JSON 语法约束，确保输出 100% 符合格式要求。

### 3. 实验设计
*   **数据集**：包含 12 种主流语言（C++, Java, Python, Go 等）的 124 个代码文件及其对应的查询-图表对。
*   **对比方法（Baselines）**：
    *   **GPT-4o**（零样本）
    *   **Claude 3.5 Sonnet**（零样本）
    *   **Qwen2.5-Coder-14B**（原始指令微调版）
    *   **Qwen2.5-Coder-14B + SFT (Claude Synth)**：在未经人工修正的合成数据上微调。
    *   **Qwen2.5-Coder-14B + SFT (Fixed Claude Synth)**：在人工修正后的高质量数据上微调（本文推荐方法）。
*   **评估指标**：
    *   **自动缺陷分析**：检测 19 种结构缺陷（如孤立节点、循环包引用、非法 ID 等）。
    *   **人工相关性评估**：专家将节点分类为：充分性（Sufficiency）、完整性（Completeness）、幻觉（Hallucination）和冗余（Verbosity），并计算 F1 分数。

### 4. 资源与算力
*   **算力设备**：使用单张 **NVIDIA Tesla V100 (32GB VRAM)** GPU。
*   **训练细节**：采用 QLoRA（4-bit 量化），利用 Unsloth 库进行加速。学习率为 3e-4，训练步数为 24 步（针对小规模高质量数据集的快速微调）。

### 5. 实验数量与充分性
*   **实验规模**：对 5 种模型进行了对比，人工标注了 48 组测试图表（每组包含多个模型的输出）。
*   **充分性评价**：实验设计较为全面，不仅涵盖了多语言测试，还通过“人工修正 vs. 原始合成”数据的对比验证了数据质量的重要性。引入了“硬指标（Hard Metrics）”来区分核心节点和辅助节点，评估维度客观且严谨。

### 6. 论文的主要结论与发现
*   **微调效果显著**：在少量（约 264 条）高质量人工修正数据上微调，模型表现大幅提升，F1 分数超过了 GPT-4o 和 Claude 3.5 Sonnet。
*   **缺陷率降低**：微调后的模型在保持高召回率的同时，显著减少了结构缺陷和幻觉节点，生成的图表比原始 LLM 更稳健。
*   **意图聚焦**：模型学会了根据查询进行“抽象”，例如使用 `entity` 类型来概括复杂的逻辑，而不是罗列所有底层细节。
*   **可行性验证**：证明了 LLM 可以作为按需生成软件文档的可靠引擎。

### 7. 优点（亮点）
*   **任务创新**：从“全量逆向工程”转向“查询驱动的局部视图”，更符合开发者的实际工作流。
*   **结构化输出**：通过 JSON 中间层和受限解码解决了 LLM 生成绘图代码时常见的语法崩溃问题。
*   **高质量数据集**：强调了人工修正对微调代码专用模型的重要性，并开源了数据集。

### 8. 不足与局限
*   **上下文限制**：目前仅支持单文件分析，无法处理跨模块或全项目级别的复杂架构查询。
*   **评估成本高**：由于缺乏绝对的“黄金标准”图表，相关性评估高度依赖人工专家，难以大规模自动化。
*   **召回率估算**：由于没有标准答案，召回率是基于所有模型发现的并集节点来估算的，可能存在上限偏差。
*   **文本描述未评**：虽然图表包含节点描述，但实验未对这些自然语言描述的准确性进行系统的人工评估。

（完）
