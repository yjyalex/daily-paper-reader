---
title: "Optimas: An Intelligent Analytics-Informed Generative AI Framework for Performance Optimization"
title_zh: Optimas：一种用于性能优化的智能分析驱动型生成式人工智能框架
authors: "Mohammad Zaeed, Tanzima Z. Islam, Vladimir Indic"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23892v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 基于多智能体工作流的代码优化生成式AI框架
tldr: Optimas是一个基于多智能体工作流的端到端生成式AI框架，旨在解决大语言模型在代码优化中缺乏性能上下文的问题。它通过将多种性能诊断报告与文献支持的优化策略相结合，实现了从洞察提取到代码生成、执行及验证的全自动化流程。在数千次实验中，该框架展现了极高的代码正确率和显著的性能提升，为高性能计算优化提供了高效的自动化方案。
selection_source: fresh_fetch
motivation: 现有的性能分析工具仅能识别瓶颈而无法生成代码，且LLM在缺乏性能上下文时难以产生有效的代码转换。
method: 提出一种模块化的多智能体框架，利用LLM将多源性能诊断映射至成熟的优化策略，并集成自动化执行与验证流水线。
result: "在3410次实验中实现了100%的代码正确率，超过98.82%的实验获得性能提升，在NVIDIA GPU上平均增益达8.02%-79.09%。"
conclusion: Optimas证明了结合性能分析数据与生成式AI可以有效实现高性能计算代码的自动化、高可靠性优化。
---

## 摘要
大语言模型（LLMs）在自动化代码优化方面展现出巨大潜力。然而，在缺乏性能上下文的情况下，它们难以生成正确且有效的代码转换。现有的性能工具可以识别瓶颈，但无法生成可执行的代码更改。因此，性能优化仍然是一项耗时且需要手动完成的任务，通常仅由具备详细架构理解的专家承担。为了弥补这一差距，我们推出了 Optimas，这是一个基于多智能体工作流构建的模块化、全自动、端到端生成式人工智能框架。Optimas 利用大语言模型将来自多份报告的性能诊断映射到成熟的、有文献支持的代码转换方案，同时在单一流水线中统一了见解提取、代码生成、执行和验证。在针对 10 个基准测试和两个高性能计算（HPC）微型应用程序的 3,410 次真实实验中，Optimas 生成的代码正确率达 100%，并在超过 98.82% 的实验中提升了性能，在 NVIDIA GPU 上实现了 8.02% 至 79.09% 的平均性能增益。

## Abstract
Large language models (LLMs) show promise for automated code optimization. However, without performance context, they struggle to produce correct and effective code transformations. Existing performance tools can identify bottlenecks but stop short of generating actionable code changes. Consequently, performance optimization continues to be a time-intensive and manual endeavor, typically undertaken only by experts with detailed architectural understanding. To bridge this gap, we introduce Optimas, a modular, fully automated, end-to-end generative AI framework built on a multi-agent workflow. Optimas uses LLMs to map performance diagnostics from multiple reports to established, literature-backed code transformations, while unifying insight extraction, code generation, execution, and validation within a single pipeline. Across 3,410 real-world experiments on 10 benchmarks and two HPC mini-applications, Optimas generates 100% correct code and improves performance in over 98.82% of those experiments, achieving average gains of 8.02%-79.09% on NVIDIA GPUs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Optimas** 的端到端生成式人工智能框架，旨在通过整合硬件性能诊断数据来自动化高性能计算（HPC）代码的优化过程。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：随着 HPC 系统复杂性增加，编写高性能 GPU 代码需要深厚的架构知识和对性能诊断报告的专家级解读。
*   **核心痛点**：
    1.  **分析与执行脱节**：现有的性能分析工具（如 Profilers）能识别瓶颈，但无法自动修改代码；而大语言模型（LLMs）虽能生成代码，但因缺乏运行时性能上下文（如停顿行为、内存吞吐量），往往无法产生实质性的性能提升。
    2.  **专家依赖**：性能优化目前仍是一个高度手动、耗时且依赖专家经验的过程。
*   **论文目标**：开发一个能够自动挖掘多源运行时诊断数据，并引导 LLM 生成既正确又能提升性能的代码的闭环框架。

### 2. 方法论：核心思想与技术细节
Optimas 采用**多智能体工作流（Multi-Agent Workflow）**，核心步骤包括：
*   **多源数据挖掘与蒸馏**：
    *   **关键内核识别**：自动筛选占总运行时间 80% 以上的性能关键内核，解决 LLM 上下文窗口限制。
    *   **Roofline 分析**：确定内核是计算受限还是内存受限。
    *   **PC 采样（PC Sampling）**：将指令级的停顿（Stalls）映射到源代码行，并利用显著性准则将数百 MB 的原始数据压缩 99.9%（如从 990MB 压缩至 <6KB）。
    *   **硬件计数器筛选（eOMP 算法）**：使用随机贪婪过程（Ensemble Orthogonal Matching Pursuit）从上千个硬件计数器中识别出与运行时间最相关的 5 个关键指标。
*   **提示词构建与优化**：将带行号的源码、蒸馏后的诊断摘要和优化指令（Guardrails）组合成结构化提示词。
*   **证据对齐推理（EAR）指标**：提出了一套新指标（证据覆盖率、定位一致性、方向一致性）来量化 LLM 是否真正根据诊断证据进行推理，而非凭空幻觉。
*   **闭环验证**：自动执行编译、位对位（Bit-for-bit）正确性校验和性能测量，失败时通过错误日志进行迭代重试。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **微基准测试**：BabelStream（Copy, Mul, Add, Triad）、Shmembench、Dot。
    *   **复杂内核**：Accuracy、Sobol、Adjacent、Extrema。
    *   **HPC 代理应用**：XSBench（蒙特卡洛中子输运）、SW4Lite（地震波传播）。
*   **对比方法（Baselines）**：
    1.  **Code-only**：仅提供代码，不提供诊断数据（成功率为 0%）。
    2.  **消融实验**：分别测试仅提供 PC 采样、仅硬件计数器（IA）、仅 Roofline 及其不同组合的效果。
*   **对比模型**：GPT-5、Gemini-2.5-pro、Llama 3.1:70B。

### 4. 资源与算力
*   **硬件环境**：NVIDIA H100 GPU。
*   **软件栈**：CUDA 12.3, Nsight Compute 2025.1.1.0。
*   **算力消耗**：文中未明确提及模型训练时长（使用的是现成模型 API 或本地部署），但提到了推理成本（GPT-5 每次实验约 $0.085）和优化耗时（单次迭代 <10 分钟，其中大部分时间用于数据采集）。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **3,410 次**真实实验。
*   **充分性**：实验涵盖了从简单的流式内核到复杂的生产级 HPC 应用；进行了详尽的消融实验以验证不同诊断源的贡献；并在 68 种不同的输入配置下验证了优化代码的泛化性。实验设计客观，通过位对位校验确保了公平性和正确性。

### 6. 主要结论与发现
*   **性能提升显著**：在 NVIDIA GPU 上实现了 **8.02% – 79.09%** 的平均性能增益，最高提升达 87.6%。
*   **极高的可靠性**：生成的代码 **100% 正确**（通过验证循环），且 98.82% 的实验实现了性能正增长。
*   **诊断数据的必要性**：单纯提供代码无法实现优化，而多源诊断数据的结合（尤其是 PC+IA+Roofline）对复杂应用最为有效。
*   **模型表现**：GPT-5 在推理完整性和证据覆盖率上表现最优，而 Llama 3.1:70B 作为开源模型也展现了可用的优化能力。

### 7. 优点（亮点）
*   **端到端自动化**：实现了从原始性能日志到验证后的优化代码的全流程自动化。
*   **高效的数据压缩**：通过 saliency 和 eOMP 算法，将海量遥测数据转化为 LLM 可理解的高价值见解。
*   **可解释性**：引入 EAR 指标，确保 LLM 的优化行为是“有据可依”的，减少了黑盒属性。
*   **实用性强**：不仅提升性能，还可作为教育工具，帮助开发者理解硬件诊断与代码转换之间的因果关系。

### 8. 不足与局限
*   **依赖特定工具**：目前高度依赖 NVIDIA 的 Nsight Compute 工具链，跨厂商（如 AMD/Intel GPU）的移植性有待验证。
*   **迭代深度**：目前的反馈循环主要解决编译错误，对于性能未达标的情况，自动化的深度迭代（如自动调整参数再重新分析）仍需用户干预。
*   **提示词长度限制**：对于极大规模的内核，即使经过压缩，诊断数据仍可能触及 LLM 的上下文瓶颈。

（完）
