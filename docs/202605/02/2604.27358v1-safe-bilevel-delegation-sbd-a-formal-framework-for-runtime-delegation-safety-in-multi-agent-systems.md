---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统中任务委派安全性的正式框架
tldr: 针对大语言模型智能体在复杂环境中的任务委派安全问题，本文提出了安全双层委派（SBD）形式化框架。该框架通过双层优化动态调整安全与效率的权衡：外层网络学习上下文相关的权重，内层循环在概率安全约束下优化委派策略。研究建立了安全性单调性、收敛性及责任传播边界等理论保证，并在医疗、金融和教育领域设计了验证方案，为多智能体系统的运行时安全提供了理论支撑。
selection_source: fresh_fetch
motivation: 现有的多智能体架构缺乏在运行时根据任务上下文动态调整安全与效率权衡的机制。
method: 提出SBD框架，将任务委派建模为双层优化问题，通过连续委派程度参数实现从人工干预到自主执行的平滑过渡。
result: 证明了安全性单调性、内层策略的线性收敛性，以及多级委派链中的责任传播界限。
conclusion: SBD为高风险环境下多智能体系统的运行时安全委派提供了严谨的数学框架和理论保障。
---

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Safe Bilevel Delegation (SBD)** 框架，旨在解决多智能体系统（MAS）在执行高风险任务时，如何在运行时（Runtime）动态、安全地分配决策权的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着大语言模型（LLM）智能体进入医疗、金融等高风险领域，如何安全地将子任务委派给专门的子智能体成为关键。
*   **研究动机**：
    *   **现有缺陷**：目前的安全性研究多集中在设计阶段（如架构选择、静态权限设置），缺乏在执行过程中根据任务上下文（Context）动态调整“安全与效率”权衡的机制。
    *   **动态需求**：例如，医疗系统在患者病情平稳与危急时，委派决策的权限应完全不同，即使查询的形式相同。
    *   **权责不明**：在多级委派链中，缺乏明确的责任追溯数学模型。

### 2. 方法论：核心思想与技术细节
SBD 将任务委派建模为一个**双层优化（Bilevel Optimization）**问题：
*   **核心变量 $\alpha$**：引入连续的委派程度 $\alpha \in [0, 1]$。$\alpha=0$ 代表完全由人工干预，$\alpha=1$ 代表完全自主执行，中间值代表不同程度的权限转移。
*   **外层优化（元权重学习）**：学习一个元权重网络 $\phi$，它根据当前状态 $s$ 输出权重 $\lambda_\phi(s)$。这个权重决定了系统在当前环境下应该多看重“安全性”而非“效率”。
*   **内层优化（策略学习）**：在给定 $\lambda$ 的情况下，优化委派策略 $\pi$，同时满足概率安全约束 $P(safe) \ge 1 - \delta$（即不安全事件的发生概率低于阈值 $\delta$）。
*   **责任传播机制**：定义了责任权重 $w_j$，通过数学公式将责任分布在多级委派链中的各个智能体上，确保责任可追溯。
*   **算法流程**：采用基于超梯度（Hypergradient）的隐式微分方法更新外层参数，内层则使用投影梯度下降（PGD）来保证策略满足安全约束。

### 3. 实验设计（预注册协议）
**注意**：该版本论文（v1）主要呈现理论框架和实验协议，实验结果计划在后续修订中报告。
*   **实验场景与数据集**：
    1.  **医疗 AI**：使用 MIMIC-III 数据集，根据患者病情严重程度（APACHE-II 评分）动态调整委派权限。
    2.  **金融风险控制**：使用 S&P 500 数据，在 2020 年新冠疫情等极端市场波动下测试系统的风险控制能力。
    3.  **教育智能体监管**：使用 ASSISTments 数据集，针对“高风险”学生（如连续答错者）加强监督。
*   **对比基准（Baselines）**：
    *   **Fixed-$\alpha$**：固定委派比例。
    *   **Safe-RL (CPO)**：传统的受限策略优化方法。
    *   **MARL-vanilla**：无安全机制的多智能体强化学习。
    *   **MaAS**：静态架构搜索方法。
*   **评估指标**：安全率（SR）、任务效率（TE）、安全-效率曲线下面积（SEA）以及责任熵（AE）。

### 4. 资源与算力
*   **硬件**：单台 NVIDIA A100 (80GB) GPU。
*   **效率**：外层每步迭代约 0.8 秒，每个领域的完整训练（500 次外层迭代）可在 7 分钟内完成。
*   **模型**：$\phi$ 和 $\pi$ 均采用简单的多层感知机（MLP），宽度为 256，深度为 3-4 层。

### 5. 实验数量与充分性
*   **实验规模**：计划在 3 个完全不同的高风险领域进行验证，每个领域包含 5 种对比方法和 4 种消融实验变体。
*   **客观性**：论文明确提出了**可证伪性标准（Falsifiability Criteria）**，即如果 SBD 不能在 SEA 指标上超过所有基准，或者理论推导的单调性在实验中不成立，则视为框架失效。这种“预注册”式的科研方法增加了实验设计的客观性和透明度。

### 6. 主要结论与理论发现
1.  **安全性单调性（Safety Monotonicity）**：证明了外层安全权重越高，内层产生的策略在物理意义上就越安全。
2.  **内层策略收敛性**：证明了在标准平滑性假设下，内层优化具有线性收敛速度。
3.  **责任传播边界**：证明了在有限长度的委派链中，没有任何一个单一智能体可以承担全部责任，责任会随着委派程度 $\alpha$ 发生偏移。

### 7. 优点与亮点
*   **理论严谨**：不仅提出了算法，还给出了完备的数学证明，为自主智能体的运行时监管提供了形式化基础。
*   **动态适应**：通过连续变量 $\alpha$ 和上下文感知权重 $\lambda$，实现了比传统“开关式”安全机制更精细的控制。
*   **责任量化**：首次为多级智能体委派链提供了可计算的责任分配模型。

### 8. 不足与局限
*   **假设限制**：理论证明依赖于强凸性（Strong Convexity）假设，这在深度神经网络中可能并不总是成立。
*   **约束来源**：框架假设安全约束集 $C$ 是已知的（由专家定义），但在复杂现实中，定义完美的约束集本身就很困难。
*   **静态分布**：目前假设任务分布是平稳的，未考虑环境分布偏移（Distribution Shift）对元权重网络的影响。
*   **实验缺失**：当前版本仅有理论和协议，缺乏实际运行的量化数据支撑。

（完）
