---
title: Memory-Based Advantage Shaping for LLM-Guided Reinforcement Learning
title_zh: 面向大语言模型引导强化学习的基于记忆的优势塑造
authors: "Narjes Nourzad, Carlee Joe-Wong"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.17931v1"
tags: ["query:rl-nlplr"]
score: 8.0
evidence: 带有优势塑造的大语言模型引导强化学习
tldr: 针对强化学习在稀疏奖励环境下采样效率低的问题，本文提出一种基于记忆的优势函数塑造方法。通过构建包含LLM指导和成功轨迹的记忆图，推导出效用函数来引导智能体，减少了对LLM频繁调用的依赖。该方法在提升学习速度的同时保证了最终性能，为LLM辅助的强化学习提供了更具扩展性的方案。
selection_source: fresh_fetch
motivation: 强化学习在稀疏奖励下效率低下，且频繁调用LLM辅助探索存在成本高、可靠性差及难以扩展的问题。
method: 构建记忆图编码子目标与轨迹，并利用其生成的效用函数来塑造优势函数，从而在不改变原始奖励的情况下引导评论家网络。
result: 实验表明该方法在基准环境中显著提升了采样效率和早期学习速度，且最终回报与需要高频调用LLM的方法相当。
conclusion: 该方法通过结合记忆机制与LLM指导，有效地平衡了探索效率与对外部模型的依赖，证明了离线知识与少量在线查询结合的有效性。
---

## 摘要
在稀疏或延迟奖励的环境中，强化学习（RL）由于学习所需的交互次数巨大，导致样本复杂度较高。这一局限性促使研究者利用大语言模型（LLM）进行子目标发现和轨迹引导。虽然 LLM 可以支持探索，但频繁依赖 LLM 调用引发了对可扩展性和可靠性的担忧。我们通过构建一个记忆图来应对这些挑战，该图编码了来自 LLM 引导和智能体自身成功展开（rollouts）的子目标及轨迹。从该图中，我们推导出一个效用函数，用于评估智能体的轨迹与先前成功策略的对齐程度。该效用塑造了优势函数，在不改变奖励的情况下为评论家（critic）提供额外的引导。我们的方法主要依赖离线输入和偶尔的在线查询，避免了对持续 LLM 监督的依赖。在基准环境中的初步实验表明，与基准 RL 方法相比，该方法提高了样本效率并加快了早期学习速度，且最终回报与需要频繁 LLM 交互的方法相当。

## 速览
**TLDR**：针对强化学习在稀疏奖励环境下采样效率低的问题，本文提出一种基于记忆的优势函数整形方法。通过构建包含LLM指导和成功轨迹的记忆图，推导出效用函数来引导智能体，减少了对LLM频繁在线调用的依赖。该方法在显著提升学习速度和采样效率的同时，保持了与高频LLM交互方法相当的最终性能，有效解决了LLM引导RL的扩展性难题。 \
**Motivation**：强化学习在稀疏奖励下采样效率低，且现有利用LLM指导的方法存在调用成本高和依赖性强的问题。 \
**Method**：通过构建记忆图编码LLM建议和成功轨迹，并利用导出的效用函数对优势函数进行整形，从而在不改变奖励的情况下引导评论家。 \
**Result**：实验显示该方法在基准环境中提升了采样效率和早期学习速度，最终回报可比肩频繁调用LLM的方法。 \
**Conclusion**：该研究证明了通过记忆机制整合LLM知识与自主探索经验，可以在减少在线LLM查询的同时有效加速强化学习过程。

---

## Abstract
In environments with sparse or delayed rewards, reinforcement learning (RL) incurs high sample complexity due to the large number of interactions needed for learning. This limitation has motivated the use of large language models (LLMs) for subgoal discovery and trajectory guidance. While LLMs can support exploration, frequent reliance on LLM calls raises concerns about scalability and reliability. We address these challenges by constructing a memory graph that encodes subgoals and trajectories from both LLM guidance and the agent's own successful rollouts. From this graph, we derive a utility function that evaluates how closely the agent's trajectories align with prior successful strategies. This utility shapes the advantage function, providing the critic with additional guidance without altering the reward. Our method relies primarily on offline input and only occasional online queries, avoiding dependence on continuous LLM supervision. Preliminary experiments in benchmark environments show improved sample efficiency and faster early learning compared to baseline RL methods, with final returns comparable to methods that require frequent LLM interaction.