---
title: "Draft-and-Prune: Improving the Reliability of Auto-formalization for Logical Reasoning"
title_zh: Draft-and-Prune：提升逻辑推理中自动形式化的可靠性
authors: "Zhiyu Ni, Zheng Liang, Liangcheng Song, Chenrui Cao, Xian Zhang, Alberto Sangiovanni-Vincentelli, Pierluigi Nuzzo"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17233v1"
tags: ["query:rl-nlplr"]
score: 8.0
evidence: 通过验证改进逻辑推理的自动形式化
tldr: "针对自动形式化（AF）在逻辑推理中存在的语义错误瓶颈，本文提出 Draft-and-Prune (D&P) 推理框架。该方法首先生成多个自然语言计划并据此生成程序，随后通过验证机制剪枝掉执行结果矛盾或存在歧义的形式化路径，最后利用多数投票聚合预测结果。实验表明，D&P 在 AR-LSAT 等多个基准测试上显著优于现有基准，甚至在部分任务中达到 100% 的准确率，有效提升了 AF 的可靠性。"
selection_source: fresh_fetch
motivation: 自动形式化技术在将自然语言转化为求解器程序时，常因语义编码错误导致推理失败，亟需提高其语义准确性。
method: 提出一种推理时框架，通过生成多样化计划、剪枝矛盾或歧义的候选程序以及多数投票来优化形式化过程。
result: "在 AR-LSAT 任务上取得 78.43% 的准确率，并在 PrOntoQA 和 LogicalDeduction 任务中达到了 100% 的满分表现。"
conclusion: Draft-and-Prune 证明了通过推理时的多样性采样与一致性验证，可以在无额外监督的情况下显著增强逻辑推理的可靠性。
---

## 摘要
自动形式化 (AF) 将自然语言推理问题转化为求解器可执行的程序，使符号求解器能够进行可靠的逻辑演绎。然而在实践中，目前的 AF 流水线非常脆弱：程序可能无法执行，或者虽然可以执行但编码了错误的语义。虽然先前的工作在很大程度上通过基于求解器反馈的修复缓解了语法故障，但减少语义故障仍然是一个主要的瓶颈。我们提出了 Draft-and-Prune (D&P)，这是一个推理阶段的框架，通过多样性和验证来改进基于 AF 的逻辑推理。D&P 首先草拟多个自然语言计划，并以此为条件生成程序。它进一步剪除可执行但存在矛盾或歧义的形式化结果，并通过多数投票汇总来自幸存路径的预测。在四个具有代表性的基准测试（AR-LSAT、ProofWriter、PrOntoQA、LogicalDeduction）中，D&P 在没有额外监督的情况下显著增强了基于 AF 的推理。在 AR-LSAT 的仅 AF 设置下，D&P 使用 GPT-4 达到了 78.43% 的准确率，使用 GPT-4o 达到了 78.00% 的准确率，显著优于最强的 AF 基准模型 MAD-LOGIC 和 CLOVER。此外，D&P 在其他基准测试中达到了接近天花板的性能，包括在 PrOntoQA 和 LogicalDeduction 上达到 100%。

## 速览
**TLDR**：针对自动形式化（AF）在逻辑推理中因语义错误导致程序不可靠的问题，本文提出 Draft-and-Prune (D&P) 框架。该方法在推理阶段首先生成多个自然语言计划并据此生成程序，随后剪枝掉可执行但存在矛盾或歧义的形式化结果，最后通过多数投票汇总预测。实验表明，D&P 在 AR-LSAT 等多个基准测试中显著优于现有基线，甚至在部分任务上达到 100% 的准确率，有效提升了 AF 的可靠性。 \
**Motivation**：现有的自动形式化方法在将自然语言转化为求解器程序时，容易产生语义错误且难以通过简单的语法修复解决。 \
**Method**：提出一种推理时框架，通过生成多样化计划、剪枝矛盾或歧义的形式化路径以及多数投票来提高可靠性。 \
**Result**：在 AR-LSAT 任务上达到 78.43% 的准确率，并在 PrOntoQA 和 LogicalDeduction 任务上实现了 100% 的满分表现。 \
**Conclusion**：Draft-and-Prune 证明了通过多样性采样与一致性验证可以显著降低自动形式化的语义失效，是增强逻辑推理能力的有效途径。

---

## Abstract
Auto-formalization (AF) translates natural-language reasoning problems into solver-executable programs, enabling symbolic solvers to perform sound logical deduction. In practice, however, AF pipelines are currently brittle: programs may fail to execute, or execute but encode incorrect semantics. While prior work largely mitigates syntactic failures via repairs based on solver feedback, reducing semantics failures remains a major bottleneck. We propose Draft-and-Prune (D&P), an inference-time framework that improves AF-based logical reasoning via diversity and verification. D&P first drafts multiple natural-language plans and conditions program generation on them. It further prunes executable but contradictory or ambiguous formalizations, and aggregates predictions from surviving paths via majority voting. Across four representative benchmarks (AR-LSAT, ProofWriter, PrOntoQA, LogicalDeduction), D&P substantially strengthens AF-based reasoning without extra supervision. On AR-LSAT, in the AF-only setting, D&P achieves 78.43% accuracy with GPT-4 and 78.00% accuracy with GPT-4o, significantly outperforming the strongest AF baselines MAD-LOGIC and CLOVER. D&P then attains near-ceiling performance on the other benchmarks, including 100% on PrOntoQA and LogicalDeduction.