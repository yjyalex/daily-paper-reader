---
title: "The Art of Efficient Reasoning: Data, Reward, and Optimization"
title_zh: 高效推理的艺术：数据、奖励与优化
authors: "Taiqiang Wu, Zenan Xu, Bo Zhou, Ngai Wong"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20945v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过强化学习奖励塑造实现高效推理
tldr: 本研究针对大语言模型在思维链（CoT）推理中计算开销过大的问题，系统探讨了高效推理的机制。通过强化学习中的奖励塑造，旨在引导模型生成简短且准确的推理路径。研究揭示了训练过程分为长度适配和推理精炼两个阶段，并提出在较简单提示词上训练以避免长度坍缩。实验涵盖Qwen3系列模型，验证了该方法在不同规模和领域下的鲁棒性与泛化性，为实现高效推理提供了实践指南。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在进行复杂思维链推理时因生成过长Token而导致的巨大计算开销问题。
method: 通过强化学习和奖励塑造，系统研究了训练提示词、生成路径及优化策略对推理效率的影响。
result: 发现使用相对简单的提示词进行训练能确保正向奖励信号密度，有效防止模型推理长度坍缩并提升泛化能力。
conclusion: 总结了高效推理的实践指南，证明了通过优化数据和奖励机制可在保持准确性的同时显著降低推理成本。
---

## 摘要
大语言模型（LLMs）持续受益于扩展的思维链（CoT）推理，但也面临沉重的计算开销。为了解决这一问题，高效推理旨在激励简短且准确的思考轨迹，通常通过强化学习（RL）中的奖励塑造（reward shaping）来实现。在本文中，我们系统地研究了 LLMs 高效推理的机制。为了进行全面评估，我们提倡使用更细粒度的指标，包括以正确性为条件的长度分布，以及在 2k 到 32k 广泛 token 预算范围内的性能表现。首先，我们揭示了训练过程遵循两阶段范式：长度自适应和推理精炼。随后，我们在统一的协议下进行了大规模实验（约 20 万 GPU 小时），解构了训练提示词（prompts）与采样（rollouts）、奖励塑造以及优化策略。特别地，一个关键发现是在相对较简单的提示词上进行训练，以确保正向奖励信号的密度，从而避免长度崩溃（length collapse）。同时，学习到的长度偏置可以跨领域泛化。我们将所有发现提炼为有价值的见解和实践指南，并在 Qwen3 系列（参数量从 0.6B 到 30B）上进一步验证，证明了其鲁棒性和泛化性。

## 速览
**TLDR**：本研究针对大语言模型在思维链（CoT）推理中计算开销过大的问题，系统探讨了高效推理的机制。通过强化学习中的奖励塑造，旨在引导模型生成简短且准确的推理路径。研究揭示了训练过程分为长度适应和推理细化两个阶段，并提出在较简单提示词上训练以避免长度坍缩。实验涵盖Qwen3系列（0.6B至30B），为实现高效推理提供了实用的优化指南。 \
**Motivation**：旨在解决大语言模型在进行长链条思维推理时面临的高计算成本与效率低下问题。 \
**Method**：通过强化学习中的奖励塑造，结合对训练提示词、生成过程及优化策略的系统性解构，探索高效推理的实现路径。 \
**Result**：发现使用相对简单的提示词进行训练能有效保证正向奖励信号的密度，从而防止推理长度坍缩并提升泛化能力。 \
**Conclusion**：总结了高效推理的训练范式与实践指南，并在不同规模的Qwen3模型上验证了其稳健性。

---

## Abstract
Large Language Models (LLMs) consistently benefit from scaled Chain-of-Thought (CoT) reasoning, but also suffer from heavy computational overhead. To address this issue, efficient reasoning aims to incentivize short yet accurate thinking trajectories, typically through reward shaping with Reinforcement Learning (RL). In this paper, we systematically investigate the mechanics of efficient reasoning for LLMs. For comprehensive evaluation, we advocate for more fine-grained metrics, including length distribution conditioned on correctness and performance across a wide spectrum of token budgets ranging from 2k to 32k. First, we reveal that the training process follows a two-stage paradigm: length adaptation and reasoning refinement. After that, we conduct extensive experiments (about 0.2 million GPU hours) in a unified protocol, deconstructing training prompts and rollouts, reward shaping, and optimization strategies. In particular, a key finding is to train on relatively easier prompts, ensuring the density of positive reward signals and thus avoiding the length collapse. Meanwhile, the learned length bias can be generalized across domains. We distill all findings into valuable insights and practical guidelines, and further validate them across the Qwen3 series, ranging from 0.6B to 30B, demonstrating the robustness and generalization.