---
title: Stateful Inference for Low-Latency Multi-Agent Tool Calling
title_zh: 面向低延迟多智能体工具调用的有状态推理
authors: Victor Norgren
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26289v1"
tags: ["query:mas-gen"]
score: 8.0
evidence: 用于低延迟多智能体工具调用的有状态推理架构
tldr: "针对多代理工具调用中传统推理框架每次请求重新处理整个对话（85-95%的提示未变）导致延迟高的问题，提出了一种有状态推理架构：持久化KV缓存跨轮次仅增量处理新令牌，结合基数前缀缓存和提示查找投机解码器。在6轮代理工作流中每轮加速2.1倍，35轮中中位数加速4.2倍，端到端时间减半。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26289v1/fig-001.webp\", \"caption\": \"Figure 2: LayerScale speedup over vLLM and SGLang on median per-turn latency, with no caching of any kind in play. The multiplier roughly doubles as the workflow deepens from 6 to 35 turns, reflecting the O(∆t) versus O(nt) per-turn cost difference between delta-only processing and full re-prefill.\", \"page\": 11, \"index\": 1, \"width\": 800, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.26289v1/fig-002.webp\", \"caption\": \"Figure 3: Cumulative wall time over the 35-turn novel coding workflow (no response-cache hits). LayerScale finishes in roughly half the wall time, and the gap widens with every turn: it processes only the per-turn delta while vLLM and SGLang re-prefill the full and growing conversation on each turn. The separation is the compounding cost of redundant prefix recomputation.\", \"page\": 11, \"index\": 2, \"width\": 799, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2605.26289v1/fig-003.webp\", \"caption\": \"Figure 1: Median per-turn latency on novel, fully-generated tool-call workloads (no response-cache hits). LayerScale is 2.1× faster than vLLM on the 6-turn agentic workflow and 4.2× faster on the median turn of the 35-turn deep-coding workflow; the gap widens as the reusable conversation prefix deepens.\", \"page\": 10, \"index\": 3, \"width\": 800, \"height\": 374}]"
motivation: 现有推理框架将每个工具调用视为独立请求，重新处理大部分未变提示，导致高延迟。
method: 采用有状态推理，通过持久化KV缓存仅增量处理新令牌，结合基数前缀缓存和提示查找投机解码器实现高效复用。
result: 在6轮工作流中每轮加速2.1倍，35轮中中位数加速4.2倍，端到端时间减半。
conclusion: 有状态复用和投机解码而非简单缓存是实现低延迟多代理工具调用的关键。
---

## 摘要
多智能体工具调用正成为基于LLM系统的主流交互模式，然而现有推理框架将每次工具调用视为独立请求，从头重新处理整个对话——尽管前一轮提示中85-95%的内容未发生变化。我们提出了一种有状态推理架构，将传统服务中每轮的O(n_t)代价转换为仅处理增量变化的O(Δ_t)代价：持久化KV缓存跨轮次存在，仅通过吸收新token来推进；同时，基数前缀缓存将此机制扩展到交错的多智能体流量中，而提示查找推测解码器加速了结构化输出。在新生成的完全工作负载上，与vLLM和SGLang相比，参考实现在6轮智能体工作流中每轮速度提升2.1倍，在35轮工作流的中位轮次上提升4.2倍，端到端耗时减半。这一优势源于有状态复用与推测，而非缓存。

## Abstract
Multi-agent tool calling is becoming the dominant interaction pattern for LLM-based systems, yet existing inference frameworks treat each tool call as an independent request, re-processing the entire conversation from scratch even though 85-95% of the prompt is unchanged from the previous turn.   We present a stateful inference architecture that converts the $O(n_t)$ per-turn cost of conventional serving into an $O(Δ_t)$ delta-only cost: a persistent KV cache lives across turns and advances by ingesting only the new tokens, while a radix prefix cache extends this across interleaved multi-agent traffic and a prompt-lookup speculative decoder accelerates structured output. Against vLLM and SGLang on novel, fully-generated workloads, the reference implementation is $2.1\times$ faster per turn on a 6-turn agentic workflow and $4.2\times$ on the median turn of a 35-turn one, halving end-to-end wall time. The advantage comes from stateful reuse and speculation, not caching.