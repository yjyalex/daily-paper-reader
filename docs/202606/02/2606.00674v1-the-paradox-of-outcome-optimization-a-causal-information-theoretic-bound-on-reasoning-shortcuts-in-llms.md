---
title: "The Paradox of Outcome Optimization: A Causal Information-Theoretic Bound on Reasoning Shortcuts in LLMs"
title_zh: 结果优化的悖论：大型语言模型中推理捷径的因果信息论界限
authors: "Zihan Chen, Yiming Zhang, Wenxiang Geng, Zenghui Ding, Yining Sun"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00674v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: RL对齐导致LLM中的推理捷径
tldr: 论文针对LLM在基于结果的强化学习中出现的高分布内性能但脆弱分布外推理的失败模式（奖励诱导的流形坍缩），通过结构因果模型和信息瓶颈理论建立理论框架，定义推理为高复杂度因果过程、捷径学习为低复杂度虚假关联，推导基于语义覆盖率的泛化界，并证明过程奖励模型作为拓扑滤波器可强制步骤式互信息约束，从而消除捷径流形，为过程监督提供数学基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00674v1/fig-001.webp\", \"caption\": \"Figure 1: Conceptual overview of Reward-Induced Manifold Collapse and Topological Filtering. (A) Under outcome optimization (ORM), the learned representation is biased toward the low-complexity shortcut path (S) under the simplicity/compression bias of training, while the higher-complexity causal reasoning path (C) is screened off. (B) Under process supervision (PRM), step-wise verification acts as a topological filter: shortcut-based trajectories fail intermediate validation, which suppresses the shortcut path and favors the causal reasoning path.\", \"page\": 2, \"index\": 1, \"width\": 854, \"height\": 471}]"
motivation: 解释LLM在结果优化下产生推理捷径而缺乏因果理解的悖论。
method: 结合结构因果模型和信息瓶颈原理，提出语义覆盖率度量，并证明过程奖励模型通过拓扑过滤约束互信息。
result: 推导出基于语义覆盖率而非样本量的新泛化界，表明同质分布的数据缩放可能无法纠正推理缺陷。
conclusion: 过程监督的作用不仅是信用分配，更是通过拓扑滤波强制因果推理，抑制捷径学习。
---

## 摘要
通过基于结果强化学习（RL）对齐的大型语言模型（LLM）经常表现出一种关键失败模式：它们在分布内基准测试上取得高性能，但在分布外（OOD）任务上展现出脆弱的推理能力。我们将此现象称为奖励诱导的流形坍塌。我们建立了一个连接结构因果模型（SCM）和信息瓶颈（IB）原理的理论框架来解释这一悖论。我们将推理定义为一个高复杂度的因果过程，而捷径学习则是对低复杂度虚假相关性的利用。在随机梯度下降（SGD）的隐式归纳偏差下，当训练分布允许对真实因果机制进行“马尔可夫筛选”时，优化结果奖励的模型会偏向于捷径解。我们基于语义覆盖度量（η）而非样本量推导出一个新的泛化界限，表明在均匀分布上扩展数据可能无法纠正推理缺陷。我们还表明，过程奖励模型（PRM）作为拓扑滤波器，通过施加逐步互信息约束，使得低复杂度的捷径流形变得不可接受。这些结果为过程监督在简单信用分配之外的作用提供了数学基础。

## Abstract
Large Language Models (LLMs) aligned via outcome-based Reinforcement Learning (RL) frequently exhibit a critical failure mode: they achieve high performance on in-distribution benchmarks while demonstrating brittle reasoning capabilities on out-of-distribution (OOD) tasks. We term this phenomenon Reward-Induced Manifold Collapse. We establish a theoretical framework bridging Structural Causal Models (SCM) and the Information Bottleneck (IB) principle to explain this paradox. We define reasoning as a high-complexity causal process and shortcut learning as the exploitation of low-complexity spurious correlations. Under the implicit inductive bias of Stochastic Gradient Descent (SGD), models optimized for outcome rewards are biased toward shortcut solutions whenever the training distribution allows for a ``Markovian Screening'' of the true causal mechanism. We derive a new generalization bound based on Semantic Coverage Measure ($η$) rather than sample size, showing why data scaling on homogeneous distributions may fail to correct reasoning flaws. We also show that Process Reward Models (PRMs) function as Topological Filters, enforcing step-wise mutual information constraints that render the low-complexity shortcut manifold inadmissible. These results provide a mathematical grounding for the role of process supervision beyond simple credit assignment.

---

## 论文详细总结（自动生成）

# 论文《The Paradox of Outcome Optimization: A Causal Information-Theoretic Bound on Reasoning Shortcuts in LLMs》结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：基于结果强化学习（如RLHF）对齐的大型语言模型（LLM）在**分布内（ID）基准测试**上表现优异，但在**分布外（OOD）任务**上推理能力脆弱，常依赖虚假关联（捷径）而非真正的因果推理。作者将此现象命名为 **“奖励诱导的流形坍塌”（Reward-Induced Manifold Collapse）**。
- **整体含义**：该研究挑战了“仅靠扩大数据量和模型规模就能提升推理鲁棒性”的主流观点，指出**优化目标本身的结构性缺陷**是导致模型学习捷径的根本原因，并提供了理论框架来解释为何过程监督（PRM）能有效缓解此问题。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：结合**结构因果模型（SCM）**和**信息瓶颈（IB）原理**，将推理建模为高复杂度因果过程（C），捷径建模为低复杂度虚假相关（S）。SGD的隐式简单性偏差会偏向学习低复杂度的S，从而抑制C（即“马尔可夫筛选”）。
- **关键技术细节**：
  - **特征分解**：输入X由因果特征C、捷径特征S、噪声ϵ组成，且满足结构方程X = g(C, S, ϵ)。
  - **Shortcut Screening Theorem（定理1）**：当训练分布上奖励R与C在给定S下条件独立（R ⊥ C | S）且S的优化复杂度Copt远低于C时，优化会收敛到仅依赖S的表示Z≈h(S)，导致I(Z; C | S) → 0。
  - **Semantic Coverage Bound（定理2）**：定义语义覆盖率η为训练集覆盖的推理空间比例，证明OOD泛化误差下界为(1-η)·δ，与样本量N无关。因此同质数据缩放无法修正推理缺陷。
  - **PRM作为拓扑滤波器（定理3）**：PRM通过最大化整条轨迹的步骤式互信息∑I(Zt; Rt)来约束解空间。由于捷径S无法生成有效的中间步骤（导致I(Zt; Rt)≈0），其解被排除，从而迫使模型学习因果路径C。这本质上是**拓扑滤波**：PRM将捷径流形从可行解空间中移除。
- **公式**：理论部分使用条件互信息、优化复杂度、风险分解等数学形式化。无完整算法流程，但核心是IB目标的变体（最小化−I(Z;R)+βI(X;Z)），以及PRM的联合似然最大化。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/场景**：
  - **Standard Set (Dbal)**：来自MATH基准测试的标准数学题（适用PEMDAS规则）。
  - **Counterfactual Set (Dcf)**：通过LLM生成的反事实变体，显式修改运算规则（如“加法先于乘法”），要求模型依新规则推理。
  - 共计472个可解题（从500例中筛除无效样本）。
- **基准**：无外部baseline，主要对比ORM（结果奖励）与PRM（过程奖励）两种监督方式。
- **对比方法**：直接在Qwen2.5-3B、Qwen2.5-7B、Llama-3-8B三个模型上分别用ORM和PRM训练，比较标准准确率、反事实准确率以及二者差值（Gap）。
- **附加实验**：线性探针实验，在冻结模型后对每层隐藏状态训练线性分类器，预测表面特征S和因果逻辑C，验证表示层信息动态。

## 4. 资源与算力

- **计算资源**：单台服务器配备 **4张NVIDIA A100 (80GB) GPU**。
- **训练时长**：论文**未明确说明**每次实验的具体时间或消耗的GPU小时数。
- **实现框架**：ORM/PRM训练采用LLaMA-Factory；线性探针采用EasyR1。

## 5. 实验数量与充分性

- **实验数量**：
  - 表1给出三个模型（3B/7B/8B）×两种监督方式，每组5次独立运行，共30组结果。
  - 线性探针实验每个模型单次运行，可视化层数动态。
  - 未做超参数搜索（采用官方默认设置），未进行消融实验（如不同规模PRM、不同反事实生成策略等）。
- **充分性与客观性**：
  - **优点**：5次重复降低随机性；覆盖不同模型家族（Qwen与Llama）和不同规模（3B→7B→8B）；探针实验直接验证理论预测（特征筛选与拓扑恢复）。
  - **不足**：
    - 未扩展至更大规模模型（>100B），无法检验规模增大后效果是否延续。
    - 反事实任务人为构造（规则反转），与现实世界中的微妙捷径（如词汇线索）有差距。
    - 仅对比ORM vs PRM，未与其他方法（如简单拒绝采样、批量反馈等）比较。
    - 线性探针仅单次运行，缺乏统计检验。
    - 未报告训练集分布细节、未验证假设（如优化复杂度差异）的数值度量。
  - 总体而言，实验设计**聚焦于验证核心理论**，但**覆盖面和鲁棒性有限**，可视为概念验证型实验。

## 6. 论文的主要结论与发现

- **主要结论**：
  - **ORM导致流形坍塌**：在反事实场景中，ORM模型的准确率显著下降（Gap达24–36%），且线性探针显示深层表示中因果逻辑C未被编码（接近随机水平），证实了Shortcut Screening。
  - **PRM显著提升鲁棒性**：PRM将Gap降至7–11%，且探针实验显示PRM在深层（16–24层）出现因果逻辑C的恢复性提升，验证了拓扑滤波效应。
  - **数据缩放不能解决根本问题**：较大模型（7B vs 3B）在标准任务上准确率上升，但Collapse Gap反而扩大（29.87%→35.60%），符合语义覆盖率界（η不变则误差不降）。
- **核心发现**：过程监督的成功不仅在于“更密集的反馈”，更在于**改变解空间的拓扑结构**，使捷径路径不可达。

## 7. 优点：方法或实验设计上的亮点

- **理论贡献**：
  - 首次将流形坍塌用信息论和因果模型严格形式化，提出优化复杂度Copt和语义覆盖率η等新概念。
  - 推导出三个定理，分别解释问题根源、缩放失效、PRM机理，逻辑链条清晰。
  - 提供了PRM超越ORM的数学解释（拓扑滤波），而非仅经验描述。
- **实验设计**：
  - 反事实任务巧妙地分离了记忆性捷径（参数化知识）与上下文推理（因果逻辑），可复现性强。
  - 线性探针实验直接揭示表示层中C和S的信息动态，为理论提供了内部分析证据。
  - 多模型、多规模、多次重复，增强了结论的可信度（虽然仍有限）。
- **理论实践结合**：实验结论与理论预测高度一致（如探针图2中ORM的C曲线接近随机，PRM在深层的C曲线上升）。

## 8. 不足与局限

- **实验覆盖不足**：
  - 未测试更大规模模型（如>100B），在涌现能力可能改变优化复杂度差异的情况下，结论的普适性存疑。
  - 仅使用人工构造的反事实任务，与现实世界中更微妙、更隐蔽的捷径（如词汇重叠、长度偏差）存在差距。
  - 未与其他缓解捷径的方法（如数据增强、对抗训练）对比，难以判断PRM的相对优势。
- **理论假设局限**：
  - **可验证性假设**：PRM需要高保真的步骤级验证信号，实际中构造困难（尤其是非数学领域）。
  - **复杂度不对称假设**：捷径一定比因果特征优化复杂度低，当捷径本身复杂或因果机制简单时，理论可能失效。
  - **马尔可夫筛选假设**（R ⊥ C | S）在真实数据中可能不完全成立，减弱模型结论的严格性。
- **其他不足**：
  - 训练细节未充分报告（如学习率、批量大小、早停策略等），复现可能面临挑战。
  - 线性探针仅单次运行，缺乏方差或置信区间报告。
  - 未探讨PRM成本（额外标注、训练开销）与收益的权衡。
- **应用限制**：本文聚焦于数学推理任务，对于其他类型推理（常识、多跳问答）或开放生成任务，结论需进一步验证。

（完）
