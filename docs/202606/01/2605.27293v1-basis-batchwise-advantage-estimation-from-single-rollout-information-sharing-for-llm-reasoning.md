---
title: "BASIS: Batchwise Advantage Estimation from Single-Rollout Information Sharing for LLM Reasoning"
title_zh: BASIS：基于单次轨迹信息共享的批量优势估计用于大语言模型推理
authors: "Shijin Gong, Erhan Xu, Kai Ye, Francesco Quinzan, Giulia Livieri, Chengchun Shi"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27293v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 基于批次优势估计的LLM推理强化学习
tldr: 针对LLM推理的强化学习算法在计算与样本效率间的权衡，提出无评论家后训练算法BASIS，每个提示仅采样一次rollout，但通过跨批次信息共享改进价值估计，显著降低MSE并提升策略优化效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27293v1/fig-001.webp\", \"caption\": \"Figure 5: Accuracy of Qwen3-4B models fine-tuned by Vanilla (with a zero baseline) and BASIS coupled with GRPO (orange), GPG (red), GSPO (teal) across seven math benchmarks. Solid lines are exponentially smoothed with a window of 3; lighter curves in the same colors show the raw scores.\", \"page\": 15, \"index\": 1, \"width\": 950, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-002.webp\", \"caption\": \"Figure 3: MSE of various value estimates. (a) MSE aggregated over all prompts. (b) MSE across batches grouped by the heterogeneity of within-group values, measured by the standard deviation of {Vi,t}Bi=1. (c) MSE across prompts grouped by prompt-level value. (d) Frequency with which the estimated baseline is exactly 0 or 1. G in the legend denotes the number of rollouts per prompt. In (b), we report GRPO and RLOO only with 8 rollouts, since their MSEs are much larger with fewer rollouts. In (c) and (d), only MSEs of GRPO value estimators are reported to keep the figure readable. MSEs of RLOO value estimators are deferred to Figure 4 in Appendix A. In (d), the yellow line (BASIS) and the red line (REINFORCE++) largely overlap.\", \"page\": 7, \"index\": 2, \"width\": 950, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-003.webp\", \"caption\": \"Table 2: Computation time for fine-tuning Qwen3-4B models. Both single-rollout and multi-rollout baseline algorithms are trained for 300 steps, corresponding to approximately 1.1 passes over the DAPO-Math-17K training set for 8-rollout methods and approximately 8.8 passes for single-rollout methods. BASIS is trained for 150 steps.\", \"page\": 14, \"index\": 3, \"width\": 463, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of BASIS for constructing advantage estimates. BASIS samples one completion per prompt and uses reward information from the whole batch to estimate value baselines. The baselines are weighted averages of batch rewards, with the weight matrix W shown in the center. The diagonal entries of W are zero, shown in white, enforcing a prompt’s own reward to be excluded when estimating its baseline. Unlike single-rollout baselines such as REINFORCE++, which uses a simple global average, BASIS chooses W by minimizing the MSE of the value baselines. Advantages are then computed by subtracting the baselines from the observed rewards.\", \"page\": 3, \"index\": 4, \"width\": 950, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-005.webp\", \"caption\": \"Table 1: Online policy optimization experiments on Qwen3-4B. G denotes the number of rollouts per prompt. BASIS with G=1 is trained for 150 steps and compared against three baselines trained for 300 steps: (i) the 8-rollout (G=8) GRPO-type algorithm, (ii) its vanilla single-rollout (G=1) variant, and (iii) another single-rollout variant combined with the REINFORCE++ global batch baseline. Avg is the unweighted average accuracy across the seven math benchmarks shown. Each BASIS row is followed by three ∆ rows comparing it against the three baselines, respectively. Accuracy differences are reported in percentage points. The ∆ entry under Time gives the wall-clock saving by BASIS. Green indicates that BASIS outperforms the baseline; gray indicates that BASIS is within 5 percentage points below the baseline.\", \"page\": 8, \"index\": 5, \"width\": 945, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-006.webp\", \"caption\": \"Figure 2: MSEs of the initial and refined estimators at three model checkpoints. The left panel reports MSE at an early training stage, while the middle and right panels report MSE at later stages. Both estimators depend on a hyperparameter β, and the curves visualize their MSE across different values of β.\", \"page\": 5, \"index\": 6, \"width\": 950, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2605.27293v1/fig-007.webp\", \"caption\": \"Figure 6: Accuracy of Qwen-4B models fine-tuned by REINFORCE++ (with a global batch baseline) and BASIS coupled with GRPO (orange), GPG (red), GSPO (teal) across seven math benchmarks. The rest is the same as in Figure 5.\", \"page\": 16, \"index\": 7, \"width\": 950, \"height\": 483}]"
motivation: 现有强化学习算法在计算效率（单rollout）和样本效率（多rollout）间存在权衡，需在单rollout下改进价值函数估计。
method: BASIS在每步训练中为每个提示仅采样一次rollout，但利用整个批次中所有提示的共享信息进行批次内优势估计，无需评论家模型。
result: "相比单rollout基线REINFORCE++，BASIS降低价值函数估计MSE 69%，且单rollout估计精度优于8 rollout组均值估计；以更少训练时间达到接近多rollout GRPO型基线的性能，并常优于单rollout REINFORCE型基线。"
conclusion: BASIS通过批次内信息共享有效解决了单rollout下价值估计不准的问题，在计算效率与策略优化效果之间取得了更好的平衡。
---

## 摘要
基于可验证奖励的强化学习已成为提升大语言模型推理能力的标准方法。现有算法在价值估计和策略学习中面临计算效率与样本效率之间的权衡。我们提出了BASIS，一种无需评论家的后训练算法，旨在解决这一权衡。在每个在线训练步骤中，BASIS对每个提示仅采样一个轨迹，但利用整个批次中跨提示的丰富信息来改进价值函数估计。实验表明，与代表性单轨迹基线REINFORCE++相比，BASIS将价值函数估计的MSE降低了69%，并且使用一个轨迹的MSE低于使用8个轨迹的组均值估计器。这种价值估计的提升转化为更好的策略优化：使用更少的训练时间，BASIS实现了接近多轨迹GRPO型基线的性能，并且通常优于单轨迹REINFORCE型基线。

## Abstract
Reinforcement learning with verifiable rewards has become a standard recipe for improving the reasoning abilities of large language models. Existing algorithms face a tradeoff between computational efficiency and sample efficiency in value estimation and policy learning. We introduce BASIS, a critic-free post-training algorithm designed to address this tradeoff. At each online training step, BASIS samples only one rollout per prompt, but leverages rich information across prompts in the entire batch to improve value function estimation. Our experiments demonstrate that BASIS reduces MSE in value function estimation by 69% compared to REINFORCE++, a representative single-rollout baseline, and achieves lower MSE with one rollout than group mean estimators with 8 rollouts. This improvement in value estimation translates to better policy optimization: using substantially less training time, BASIS achieves performance close to multi-rollout GRPO-type baselines and often outperforms single-rollout REINFORCE-type baselines.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于您提供的论文内容，生成一篇结构化、深入、客观的中文总结。

### 论文总结

#### 1. 核心问题与整体含义（研究动机与背景）

*   **核心问题**：当前，通过强化学习（RL）提升大语言模型（LLM）推理能力的主流方法（如PPO、GRPO）面临**计算效率**与**样本效率**之间的根本性权衡。
    *   **PPO型算法**：训练一个额外的评论家（Critic）网络来估计价值函数，提升样本效率，但计算开销大。
    *   **GRPO型算法**：通过为每个提示采样多个轨迹（rollouts），利用其平均奖励作为基线来估计优势值，无需评论家网络，但采样成本高。
    *   **单轨迹算法**（如REINFORCE++）：计算成本低，但优势值估计不准确，导致策略更新噪声大。
*   **研究整体含义**：作者旨在打破上述权衡，提出一种**既保持计算高效（单轨迹采样），又能实现样本高效（准确估计价值函数）** 的RLVR后训练算法。

#### 2. 方法论提出

*   **核心思想**：提出一种名为**BASIS**（Batchwise Advantage Estimation from Single-Rollout Information Sharing）的无评论家（Critic-free）算法。其核心是在每个在线训练步骤中，虽然每个提示只采样一个轨迹，但通过**利用整个批次中所有提示的奖励信息**来改进单个提示的价值函数估计。
*   **关键技术细节（三步流程）**：
    1.  **离线价值估计**：基于初始参考策略（`π_ref`），利用KL正则化最优策略的闭式解（Proposition 2），为每个提示预先计算一个初始价值估计值`V̂_β(x)`。这避免了在线训练时的额外采样。
        *   公式：`V^*_β(x) = E_{π_ref}[r(x,y)exp(r(x,y)/β)] / E_{π_ref}[exp(r(x,y)/β)]`
    2.  **批次内细化（Batchwise Refinement）**：在线训练时，对于每个提示`i`，将其它所有提示`j`的奖励`r_j`进行**加权平均**，作为当前提示`i`的基线`êV_t`。关键在于权重的设计，目标是最小化估计的均方误差（MSE）。
        *   权重根据“最佳线性无偏估计（BLUE）”原理确定（Proposition 1），与目标提示的价值`V_i,t`、源提示的价值`V_j,t`正相关，与源提示的奖励方差`σ²_j`负相关。
        *   实际中，使用离线估计和伯努利方差近似来计算权重。同时引入“活跃集”概念，避免价值接近0或1的提示导致计算不稳定。
    3.  **在线校准（Online Calibration）**：由于离线估计依赖于超参数`β`，BASIS在每个训练步骤通过最小化一个经验准则（公式6）来自适应地选择最优的`β`值，使得估计的基线更接近当前策略的真实价值。

#### 3. 实验设计

*   **数据集**：
    *   **训练**：
        *   价值估计实验：使用MATH数据集的Level 3-5子集。
        *   策略优化实验：使用**DAPO-Math-17K**训练集（`Qwen3-4B`），以及MATH Level 3-5子集（`Qwen2.5-Math-7B`）。
    *   **评估（Benchmark）**：在**7个标准数学推理基准**上评测，包括AIME 2024/2025、AMC 2023、MATH-500、Minerva Math、OlympiadBench、HMMT 2025。
*   **对比方法**：
    *   **价值估计实验**：对比了**GRPO**（多轨迹）、**RLOO**（留一法）、**REINFORCE++**（单轨迹全局均值基线）。
    *   **策略优化实验**：将BASIS作为即插即用的优势估计器，分别应用于**GRPO**、**GPG**、**GSPO**三种基础算法，并对比其原始多轨迹（G=8）版、单轨迹零基线（Vanilla）版，以及单轨迹+REINFORCE++全局基线版。

#### 4. 资源与算力

*   论文明确说明了实验所使用的算力资源：
    *   **GPU型号**：4块 **GH200 GPU**。
    *   **训练时长**：策略优化实验中，多轨迹基线（GRPO/GPG/GSPO with G=8）训练300步约需14-15.5小时；单轨迹基线（Vanilla, REINFORCE++）训练300步约需12.9-20.7小时；而BASIS仅训练**150步**，耗时约**6.4-8.3小时**，约为前者的一半或更少。

#### 5. 实验数量与充分性

*   **实验数量**：论文进行了**两组核心实验**和**多组消融分析**。
    1.  **价值估计实验**：系统评估了MSE、鲁棒性（按批次异质性和提示难度分组）、信息量（基线为0或1的频率）。
    2.  **策略优化实验**：在3种不同基础算法（GRPO, GPG, GSPO）上，在7个不同benchmark上对比了4种不同配置（多轨迹、单轨迹、BASIS等），并报告了**完整的训练曲线**。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了方法最核心的两个方面（估计和优化），视角全面，结果详实。消融研究验证了不同权重变体（UNB、VOP、RVG）的效果。
    *   **公平性**：实验设计较为公平。在策略优化中，所有算法在每个训练步骤的**总采样预算固定为512个轨迹**，确保了在相同计算资源下进行比较。BASIS虽然训练步数减半，但这正是其效率优势的体现，论文也专门训练了BASIS 300步的版本（附录C）以展示其潜力。对比的基线包括最先进的多轨迹和单轨迹方法。

#### 6. 主要结论与发现

*   **价值估计**：BASIS是**样本高效且鲁棒**的。
    *   与单轨迹REINFORCE++相比，MSE降低**69%**。
    *   仅用1个轨迹，其MSE即低于使用**8个轨迹**的GRPO和RLOO。
    *   对批次内奖励异质性和提示难度不敏感，而REINFORCE++和GRPO则对这两种因素敏感。
    *   BASI能持续提供**信息量丰富（非0或1）** 的优势估计。
*   **策略优化**：改进的价值估计带来了更好的策略优化效果。
    *   **竞争力**：使用**一半**的训练时间和采样预算，BASIS实现了**接近甚至超过**多轨迹（G=8）GRPO/GPG/GSPO的性能。
    *   **显著提升**：大幅优于单轨迹的Vanilla和REINFORCE++基线，在某些基准上提升高达44.8个点。
    *   **稳定性**：BASIS成功**防止了单轨迹策略优化中的训练崩溃**问题，这是Vanilla方法常遇到的问题。

#### 7. 优点

1.  **创新性地解决了权衡问题**：提出批次内信息共享的策略，在**单一轨迹采样**的低计算成本下，实现了高样本效率，是方法论上的一个重要贡献。
2.  **理论基础扎实**：方法基于最佳线性无偏估计（BLUE）和动态规划中KL正则化最优策略的闭式解，提供了坚实的理论指导。
3.  **即插即用**：BASIS的优势估计器可以作为一个**通用组件**，轻易地整合到现有的任何单轨迹RLVR算法（如GRPO, GPG, GSPO）中，无需修改算法主体。
4.  **消除训练崩溃**：有效解决了单轨迹RLVR中策略优化不稳定的核心问题，这是一个非常实用的改进。
5.  **实验设计严谨**：公平的比较设置（固定总采样预算）和全面的评估基准，使得结论具有较强的说服力。

#### 8. 不足与局限

1.  **应用范围有限**：论文指出，BASIS是为**可验证奖励**（Verifiable Rewards）的场景设计的，主要应用于数学等自动判分任务。对于需要噪声、部分或基于偏好的奖励的复杂任务（如文本摘要）的效果有待验证。
2.  **未结合跨时间步信息**：方法仅共享批次内提示之间的信息（横向）。作者也承认，存在另一类利用同一提示跨训练步骤信息（纵向）的工作。若能**结合横向与纵向的信息共享**，可能进一步提升样本效率，但本文未涉及。
3.  **潜在的偏差风险**：虽然通过“留一法”避免了直接使用自身奖励，但批次内其他提示的奖励隐含着与目标提示的相关性。如果批次内提示在难度或主题上高度不平衡，估计可能仍有偏差。
4.  **离线估计的计算成本**：虽然在线训练高效，但生成离线价值表需要预采样大量（n=64）参考轨迹，这本身也需要一定的计算时间（论文提及“数小时”）。这算是一种前期固定成本。

（完）
