---
title: Which Pairs to Compare for LLM Post-Training?
title_zh: 在LLM后训练中应该比较哪些对？
authors: "Jiangze Han, Vineet Goyal, Will Ma"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19607v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 直接偏好优化用于大模型后训练
tldr: 偏好对齐是大语言模型后训练的核心，但标注偏好对成本高昂。本文研究如何从大量生成候选中选择最信息量的比较对，以提升预算效率。基于DPO目标，理论推导出比较选择通过信息矩阵影响策略次优差距，并据此提出采样设计。实验证明该方法优于常见启发式，提升样本效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19607v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19607v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1436, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19607v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 552, \"label\": \"Figure\"}]"
motivation: 偏好标注成本高，需从大量生成候选中筛选信息量最大的比较对以提高对齐数据效率。
method: 将比较对选择建模为采样设计问题，基于DPO训练器的信息矩阵推导策略次优差距的上下界，并提出优化准则。
result: 理论给出DPO训练策略次优差距与信息矩阵的关系，实验表明提出设计的样本效率优于常见启发式方法。
conclusion: 比较选择可优化DPO后训练性能，信息矩阵驱动的采样设计能有效提升偏好对齐的数据效率。
---

## 摘要
基于偏好的后训练已成为对齐语言模型的核心范式。一种常见的数据收集策略是为每个提示生成少量完成项并对产生的比较对进行标注。然而，人工偏好标签通常比生成额外完成项昂贵得多，这提示了对相同标注预算的不同使用方式：生成更大的完成项池，但仅标注最具信息量的比较对。本文研究在基于偏好的后训练中应该比较哪些对。我们将比较选择形式化为一个采样设计问题，并根据基于偏好的后训练目标下最终策略的质量来评估设计。我们为直接偏好优化（DPO）实例化该框架，分析标注对的选择如何通过DPO训练传播到下游策略性能。我们的主要结果为DPO训练策略的后训练最优性差距提供了匹配的上界和下界。这些界限表明，比较选择通过一个单一的设计依赖信息矩阵影响下游性能，该矩阵将标签分配与参数估计误差和策略次优性联系起来。这为预算约束的比较选择提供了明确的优化准则，并激发了从大型生成完成池中选择信息对的实用采样设计。在合成设置和语言模型后训练基准上的实验表明，所提出的设计相较于常见的比较选择启发式方法一致地提高了样本效率。

## Abstract
Preference-based post-training has become a central paradigm for aligning language models. A common data-collection strategy is to generate a small set of completions for each prompt and label the resulting comparison pairs. However, human preference labels are often much more expensive than generating additional completions, suggesting a different use of the same labeling budget: generate a larger pool of completions, but label only the most informative comparison pairs. This paper studies which pairs should be compared in preference-based post-training. We formulate comparison curation as a sampling-design problem and evaluate designs by the quality of the final policy under the preference-based post-training objective. We instantiate this framework for Direct Preference Optimization (DPO), analyzing how the choice of labeled pairs propagates through DPO training to downstream policy performance. Our main results provide matching upper and lower bounds on the post-training optimality gap of the DPO-trained policy. The bounds show that comparison selection affects downstream performance through a single design-dependent information matrix, which links label allocation to parameter estimation error and policy suboptimality. This yields an explicit optimization criterion for budgeted comparison curation and motivates practical sampling designs for selecting informative pairs from large generated completion pools. Experiments on synthetic settings and language-model post-training benchmarks show that the proposed designs consistently improve sample efficiency over common comparison-selection heuristics.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在基于偏好的语言模型后训练（如 RLHF/DPO）中，获取人工偏好标签的成本远高于自动生成候选完成项。常见的做法是每个提示生成少量完成项并标注所有比较对，但这样浪费了廉价生成能力。
- **核心问题**：给定固定的标注预算，如何从更大的生成候选池中筛选出**最具信息量的比较对**，以最大化最终策略在 KL 正则化 RLHF 目标下的性能？
- **整体含义**：将比较选择形式化为一个**采样设计问题**，提出一个设计依赖的信息矩阵来量化和优化比较选择对下游策略质量的影响，并给出理论上界和下界，从而提供可操作的采样策略。

### 2. 论文提出的方法论
- **核心思想**：比较选择通过设计协方差矩阵 \(\Sigma_D(\theta)\) 影响 DPO 估计误差，进而通过二次型等价性影响 RLHF 最优性差距 \(J(\pi^*) - J(\hat{\pi}_n)\)。
- **关键技术细节**：
  - 定义比较边 \(e = (x, y^+, y^-)\) 的灵敏度向量 \(g(e;\theta) = \nabla_\theta u_\theta(e)\)，其中 \(u_\theta(e)\) 为 DPO logit 差。
  - 采样设计 \(D\) 的协方差矩阵 \(\Sigma_D(\theta) = \mathbb{E}_{e\sim D}[g(e;\theta)g(e;\theta)^\top]\)。
  - 证明 RLHF 差距与加权参数误差等价：\(J(\pi^*) - J(\pi_\theta) \asymp (\theta-\theta^*)^\top I(\theta^*)(\theta-\theta^*)\)，其中 \(I(\theta^*)\) 为政策 Fisher 信息矩阵。
  - 主要定理：DPO 估计器满足 \(\mathbb{E}[J(\pi^*) - J(\hat{\pi}_n)] \leq \frac{C_{\text{ub}}}{n} \mathrm{tr}\big(I(\theta^*)\Sigma_D(\theta^*)^\dagger\big)\)，且任意估计器的下界也具有相同 trace 形式。
- **优化准则**：设计目标为最小化 \(\mathrm{tr}\big(I(\theta^*)\Sigma_D(\theta^*)^\dagger\big)\)。由于 \(\theta^*\) 未知，使用参考策略参数 \(\theta_0\) 构建**可实现的插件设计** \(D_{\theta_0}\)，并证明其性能与 oracle 设计相比损失有界。

### 3. 实验设计
- **使用的数据集/场景**：
  1. **合成 tabular 设置**：已知真实奖励，d 个 item，非均匀参考策略，低信号 regime。
  2. **合成线性上下文设置**：\(3p\) 维特征，softmax 策略，线性奖励。
  3. **IMDb 实验**：使用 GPT-2-large ，SFT 后做 DPO，采用情感分类器作为代理奖励。
  4. **Anthropic-HH 实验**：使用 Pythia-2.8B，真实人类偏好数据，GPT-4.1 作为自动裁判比较胜率。
- **Benchmark 与对比方法**：
  - Oracle 设计 \(D^*(\theta^*)\)
  - Plug-in 设计 \(D^*(\theta_0)\)（本文提出）
  - 均匀采样（所有候选对等概率）
  - 基于 \(\pi_0\) 的启发式（按参考策略概率采样完成项配对）
- **评估指标**：合成实验使用 RLHF 最优性差距；IMDb 使用 reward–KL 前沿；Anthropic-HH 使用对人工选中响应的胜率。

### 4. 资源与算力
- **IMDb 实验**：使用 H100 GPU，每个 DPO 微调约几分钟到 1 小时（具体取决于设置），D* 构造与评估更轻量。
- **Anthropic-HH 实验**：使用 4 块 H100 GPU，每个 Pythia-2.8B DPO 微调约 1 小时；GPT-4.1 评估通过 API 进行，无需 GPU。
- **文中未明确提及**：总 GPU 小时数或完整训练成本的具体数值，但给出了单次运行时长和硬件规格。

### 5. 实验数量与充分性
- **合成实验**：多次蒙特卡洛重复，图 1a/1b 显示了均值与方差，结果清晰支持方法优势。
- **IMDb 实验**：prompt 选择任务重复 30 次，response 选择任务重复 80 次，并扫描了 \(\beta \in \{0.05,0.1,0.2,0.5,1,2,5\}\) 绘制 reward–KL 前沿，误差棒显示统计显著性。
- **Anthropic-HH 实验**：测试了两个预算（50% 和 60% 的候选池），三个采样温度（0.25, 0.7, 1.0），重复多次（文中未明确次数，但给出了误差棒），结果一致优于基准。
- **充分性评价**：实验覆盖从合成到真实 LLM 的多个设置，对比了多种基线，重复次数合理，统计量呈现充分。但缺少对 D* 设计本身超参数（如 PCA 维度、ridge 正则）的消融研究，也未测试不同初始模型（如不同规模的 LLM）的泛化性。

### 6. 论文的主要结论与发现
- 比较选择对 DPO 最终策略性能的影响完全由 trace 准则 \(\mathrm{tr}(I(\theta^*)\Sigma_D(\theta^*)^\dagger)\) 刻画，该准则同时控制可达最优性差距的上界和不可避免的下界。
- 基于参考策略的插件设计 \(D_{\theta_0}\) 在实际中有效，其性能接近 oracle 设计，显著优于均匀采样和 \(\pi_0\)-加权启发式。
- 在 IMDB 和 Anthropic-HH 实验中，D* 方法在固定标注预算下始终带来更好的 reward–KL 权衡或更高的胜率，尤其在低预算 regime 优势明显。

### 7. 优点
- **理论严谨**：给出了匹配的上界和下界，将比较选择问题归结为信息矩阵的 trace 最小化，具有坚实的统计基础。
- **方法实用**：插件设计仅需参考策略参数，无需在线交互或迭代适应，适合离线批量标注场景。
- **实验设计全面**：从合成到真实语言模型，从已知奖励到自动裁判，验证了方法的可迁移性。
- **明确考虑预算约束**：将标注预算视为核心限制，研究如何在有限预算下最大化收益。

### 8. 不足与局限
- **理论假设较强**：依赖可实性、正则性、覆盖性等假设，对于大规模神经网络策略可能仅近似成立。
- **仅考虑离线随机设计**：设计在标注前固定，不根据已收集的偏好反馈进行自适应调整；而在线或迭代方法可能进一步提升效率。
- **特征近似引入误差**：在真实 LLM 实验中，使用 SFT 模型的 last-token 隐层表示作为特征，并通过线性近似计算 Fisher 信息矩阵，这可能偏离真实的 DPO 灵敏度。
- **缺乏超参数消融**：未深入分析设计构造中正则系数 \(\lambda\)、PCA 维度、ridge 回归强度等对最终结果的影响。
- **实验范围有限**：仅测试了 GPT-2-large 和 Pythia-2.8B，未考察更大模型（如 7B、13B）或不同架构（如 LLaMA）下的表现。

（完）
