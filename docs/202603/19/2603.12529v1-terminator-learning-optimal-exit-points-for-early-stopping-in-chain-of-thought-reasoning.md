---
title: "TERMINATOR: Learning Optimal Exit Points for Early Stopping in Chain-of-Thought Reasoning"
title_zh: TERMINATOR：学习思维链推理中提前停止的最佳退出点
authors: "Alliot Nagle, Jakhongir Saydaliev, Dhia Garbaya, Michael Gastpar, Ashok Vardhan Makkuva, Hyeji Kim"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12529v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 学习思维链推理的最佳退出点
tldr: "大型推理模型（LRM）在思维链（CoT）推理中常存在“过度思考”问题，即在得出答案后仍消耗大量计算资源。本文提出 TERMINATOR，一种针对推理过程的早期退出策略。该方法通过预测模型首次得出正确答案的位置，构建最优推理长度数据集并训练预测器。实验表明，TERMINATOR 在 MATH-500、AIME 2025 等数据集上平均减少了 14%-55% 的推理长度，在保持甚至提升性能的同时显著降低了计算开销。"
selection_source: fresh_fetch
motivation: 解决大型推理模型在思维链推理中因过度思考导致的计算资源浪费问题。
method: 通过识别模型首次生成最终答案的位置来构建最优长度数据集，并训练预测器以在推理过程中实时决定最佳退出点。
result: "在 MATH-500 和 AIME 2025 等四个挑战性数据集上，该方法平均减少了 14% 至 55% 的推理长度，且性能优于现有 SOTA 方法。"
conclusion: TERMINATOR 证明了通过精准预测最优退出点可以有效缓解推理模型的过度思考，实现计算效率与模型性能的双赢。
---

## 摘要
大语言推理模型（LRMs）通过思维链（CoT）推理在复杂推理任务中表现出色，该技术允许模型在得出最终答案前生成中间思考 token。然而，LRMs 往往存在严重的“过度思考”现象，即在答案早已生成后仍消耗大量计算时间。已有研究表明存在最佳推理长度，在此处截断推理可显著缩短 CoT 输出，且几乎不影响性能。然而，由于最佳 CoT 长度完全依赖于任务和模型，在实际数据集中确定该长度并非易事。本文针对这一问题设计了 TERMINATOR，一种用于 LRM 推理阶段的提前退出策略，以缓解过度思考。TERMINATOR 的核心思想是，LRM 最终答案的首次出现通常是可预测的；我们利用这些首次答案的位置构建了一个全新的最佳推理长度数据集来训练 TERMINATOR。凭借此方法，TERMINATOR 在 MATH-500、AIME 2025、HumanEval 和 GPQA 四个具有挑战性的实际数据集上，平均实现了 14%-55% 的 CoT 长度缩减，同时性能优于当前最先进的方法。

## Abstract
Large Reasoning Models (LRMs) achieve impressive performance on complex reasoning tasks via Chain-of-Thought (CoT) reasoning, which enables them to generate intermediate thinking tokens before arriving at the final answer. However, LRMs often suffer from significant overthinking, spending excessive compute time even after the answer is generated early on. Prior work has identified the existence of an optimal reasoning length such that truncating reasoning at this point significantly shortens CoT outputs with virtually no change in performance. However, determining optimal CoT lengths for practical datasets is highly non-trivial as they are fully task and model-dependent. In this paper, we precisely address this and design TERMINATOR, an early-exit strategy for LRMs at inference to mitigate overthinking. The central idea underpinning TERMINATOR is that the first arrival of an LRM's final answer is often predictable, and we leverage these first answer positions to create a novel dataset of optimal reasoning lengths to train TERMINATOR. Powered by this approach, TERMINATOR achieves significant reductions in CoT lengths of 14%-55% on average across four challenging practical datasets: MATH-500, AIME 2025, HumanEval, and GPQA, whilst outperforming current state-of-the-art methods.

---

## 论文详细总结（自动生成）

# 论文总结：TERMINATOR —— 学习思维链推理中提前停止的最佳退出点

### 1. 核心问题与整体含义
*   **研究动机**：大型推理模型（LRMs，如 DeepSeek-R1、OpenAI o1）通过思维链（CoT）显著提升了复杂任务的性能。然而，这些模型普遍存在**“过度思考”（Overthinking）**现象，即在已经得出正确答案后，仍会生成数千个冗余的思考 token 进行自我检查或尝试其他路径。
*   **核心问题**：如何在不损害模型准确率的前提下，在推理过程中实时识别出“最优退出点”，从而节省推理成本并降低延迟？
*   **整体含义**：本文提出了 **TERMINATOR**，一种基于探测（Probing）的推理阶段提前退出策略。它通过预测模型何时首次逻辑性地推导出最终答案，实现推理过程的精准截断。

### 2. 方法论
*   **核心思想**：将提前退出问题转化为一个**二元分类任务**。通过训练一个轻量级的探测器来监控 LRM 最后一层的隐藏状态，判断当前 token 是否已经达到了“事后最优推理长度”（HORL）。
*   **关键技术细节**：
    *   **事后最优推理长度（HORL）**：定义为模型在不改变最终答案的前提下，所需生成的最少 token 索引。
    *   **数据标注流水线（Extract-Identify-Verify）**：利用一个强大的 LRM（如 Qwen3-30B）自动处理数万个 CoT 样本。首先提取最终答案，然后在 CoT 中识别该答案首次出现的文本段，最后验证该位置的逻辑正确性，从而获得精确到 token 级别的训练标签。
    *   **TERMINATOR 模型结构**：探测器由 LRM 的最后一个 Transformer 块（初始化权重）加一个随机初始化的线性预测头组成。
    *   **训练策略**：使用类加权交叉熵损失函数（Class-weighted BCE），以应对答案前（标签 0）和答案后（标签 1）的数据不平衡。
    *   **推理算法**：在生成过程中，使用大小为 10 的滑动窗口记录探测器的预测结果。当窗口内超过 50% 的位置被预测为“已完成”时，强制注入 `</think>` 终止符。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：混合了 MATH、AIME (1983-2024)、OpenCoder-SFT（代码）和 OpenScience（科学）四个来源，每个模型约 11 万个 CoT 样本。
    *   **测试集**：MATH-500、AIME 2025、HumanEval（代码）和 GPQA（科学）。
*   **对比方法（Baselines）**：
    *   **Vanilla**：原始 LRM 生成。
    *   **NoThinking**：强制模型不进行思考直接输出。
    *   **DEER**：基于 token 概率阈值的启发式退出。
    *   **Thought Calibration**：基于隐藏状态的线性探测（现有 SOTA）。
    *   **Dynasor**：基于中间答案一致性的动态检查。
*   **评估模型**：Qwen3-8B/14B 和 Ministral-3-8B/14B。

### 4. 资源与算力
*   **硬件使用**：论文提到在延迟和吞吐量基准测试中使用了 **NVIDIA GH200 GPU**。
*   **算力消耗**：文中未明确给出训练探测器的总 GPU 小时数。但指出数据准备阶段（为 11 万个样本生成 CoT 并进行三步标注）是一个计算密集型过程，使用了 vLLM 异步请求来提高效率。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同架构和规模的模型上进行了验证，涵盖了数学、代码、科学等多个领域。
*   **充分性分析**：
    *   **消融实验**：进行了分布外（OOD）测试，验证了在不同任务间迁移的效果。
    *   **Pareto 分析**：通过准确率 vs. 压缩率的曲线图展示了该方法在效率平衡上的优越性。
    *   **信号分析**：深入探讨了 Token 置信度（Token-Confidence）和“思考 token”（如 "hmm", "wait"）在答案达成前后的统计学变化。
    *   **结论**：实验设计全面，对比了多种主流基准，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **大幅提效**：TERMINATOR 在四个数据集上平均减少了 **14% 至 55%** 的 token 生成量。
*   **性能优异**：在大多数任务中，TERMINATOR 位于 Pareto 前沿的最优位置，即在相同压缩率下准确率最高。
*   **内在信号**：研究发现 LRM 的隐藏状态中包含明显的“答案达成”信号。当答案首次出现时，模型的 Token 置信度会出现明显的尖峰，随后骤降。
*   **思考模式偏移**：模型在生成答案前倾向于使用 "hmm"、"okay" 等词，而在生成答案后倾向于使用 "another"、"but" 等词进行反思。

### 7. 优点
*   **高精度**：实现了 token 级别的退出控制，比基于段落或固定步长的方法更精细。
*   **自包含性**：推理时不需要外部参考或真值标签，完全依赖模型自身的内部状态。
*   **低开销**：探测器仅增加了一个 Transformer 层的计算量，对于 8B 以上的模型，其带来的吞吐量损失（约 7%-10%）远小于节省 token 带来的延迟收益（减少约 50%）。

### 8. 不足与局限
*   **任务难度挑战**：在极高难度任务（如 AIME 2025）上，探测器有时会表现出过度自信，导致过早退出并造成轻微的准确率下降。
*   **标注成本**：虽然标注是自动化的，但训练前需要针对特定模型生成并标注大量 CoT 数据，这本身需要可观的初始算力投入。
*   **泛化限制**：探测器通常需要针对特定的 LRM 家族进行训练，跨模型架构的通用性尚未得到充分验证。

（完）
