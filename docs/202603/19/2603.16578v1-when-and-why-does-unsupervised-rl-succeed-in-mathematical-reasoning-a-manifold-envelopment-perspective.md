---
title: When and Why Does Unsupervised RL Succeed in Mathematical Reasoning? A Manifold Envelopment Perspective
title_zh: 无监督强化学习何时以及为何在数学推理中取得成功？——流形包络视角
authors: "Zelin Zhang, Fei Cheng, Chenhui Chu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16578v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于内在奖励的无监督强化学习用于数学推理
tldr: 本研究针对数学推理中强化学习依赖昂贵标注的问题，探索了基于内在奖励的无监督强化学习。作者设计了鼓励简洁与确定性生成的奖励机制，并评估了不同逻辑先验模型下的表现。研究引入了创新的几何流形包络视角，揭示了训练稳定性的深层原因。该工作不仅证明了无监督方法能提升推理能力，还明确了其适用边界，为解决策略崩溃和奖励黑客行为提供了几何诊断工具。
selection_source: fresh_fetch
motivation: 旨在克服数学推理强化学习中对昂贵标注的依赖，并解决无监督强化学习中训练动态不透明及不稳定的挑战。
method: 提出了一套强制简洁性和确定性的内在奖励，并利用几何流形包络视角对模型训练过程进行诊断。
result: 实验表明模型的初始逻辑先验是成功的关键，且训练稳定性与几何上的流形包络特性密切相关。
conclusion: 本文界定了无监督强化学习在数学推理中的适用范围，并从几何维度为理解其稳定性提供了理论支撑。
---

## 摘要
尽管基于结果的强化学习（RL）显著提升了大语言模型（LLMs）的数学推理能力，但其对计算成本高昂的真值（ground-truth）标注的依赖，构成了严重的扩展性瓶颈。由内在奖励引导的无监督强化学习提供了一种可扩展的替代方案，但它面临着训练动态不透明以及灾难性不稳定性（如策略崩溃和奖励欺骗）的问题。在本文中，我们首先设计并评估了一系列内在奖励，这些奖励明确强制生成简洁且确定的内容。其次，为了探索该方法的边界，我们在具有不同内在推理能力水平的基础模型上进行了测试，揭示了模型的底层逻辑先验如何决定其成败。最后，为了揭示为什么某些配置能够稳定而其他配置会崩溃，我们引入了一种新颖的几何诊断视角，表明成功的案例被流形所包络。最终，我们的工作不仅证明了强制执行简洁且确定的回答能成功提升数学推理能力，还揭示了这种无监督方法在何时会失效，并从几何角度诊断了其原因。

## Abstract
Although outcome-based reinforcement learning (RL) significantly advances the mathematical reasoning capabilities of Large Language Models (LLMs), its reliance on computationally expensive ground-truth annotations imposes a severe scalability bottleneck. Unsupervised RL guided by intrinsic rewards offers a scalable alternative, yet it suffers from opaque training dynamics and catastrophic instability, such as policy collapse and reward hacking. In this paper, we first design and evaluate a suite of intrinsic rewards that explicitly enforce concise and certain generation. Second, to discover the boundaries of this approach, we test base models across a spectrum of intrinsic reasoning capabilities, revealing how a model's foundational logical prior dictates its success or failure. Finally, to demystify why certain configurations stabilize while others collapse, we introduce a novel geometric diagnostic lens, showing that successful cases are enveloped by manifolds. Ultimately, our work goes beyond merely demonstrating that enforcing concise and certain responses successfully boosts mathematical reasoning; we reveal when this unsupervised approach breaks down and geometrically diagnose why.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了无监督强化学习（Unsupervised RL）在提升大语言模型（LLM）数学推理能力中的作用机制、适用边界及稳定性原因。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：目前提升 LLM 数学推理的主流方法是基于结果的强化学习（如 RLVR），但这高度依赖昂贵的真值（Ground-truth）标注，存在扩展性瓶颈。
*   **核心问题**：无监督 RL（利用内在奖励而非真值）虽然具有扩展性，但面临训练动态不透明、策略崩溃（Policy Collapse）和奖励黑客（Reward Hacking）等不稳定性问题。
*   **研究目标**：探索无监督 RL 在数学推理中“何时”有效、“为何”有效，并试图从几何视角界定其成功的边界。

### 2. 方法论：核心思想与技术细节
*   **内在奖励设计**：作者设计了五个维度的奖励函数，旨在强制模型生成“简洁且确定”的回答：
    *   **Shannon Entropy (Ent)**：同时惩罚不确定性和回复长度。
    *   **Averaged Shannon Entropy (AvgEnt)**：仅惩罚不确定性，消除长度惩罚。
    *   **Length Penalty (LP)**：纯粹的长度惩罚，不考虑模型置信度。
    *   **Cumulative Rényi Entropy (CH2)**：较弱的长度惩罚，侧重于 Top-1 概率。
    *   **Collision Probability (CP)**：鼓励冗长（用于压力测试）。
*   **优化算法**：采用 **GRPO**（Group Relative Policy Optimization），且显式去除了 KL 散度项，以纯粹观察内在奖励对策略的影响。
*   **几何诊断工具（核心创新）**：
    *   **第一步：时间序列聚类**。利用动态时间规整（DTW）算法对 Token 的熵轨迹进行聚类，将推理过程划分为三个语义状态：**执行态**（低熵，如算术运算）、**逻辑态**（中熵，如逻辑连接词）、**思考态**（高熵，如宏观规划）。
    *   **第二步：3D 相空间投影**。将这三个状态的平均熵作为坐标轴，构建 3D 空间，通过观察训练轨迹形成的“凸包”（Convex Hull）体积来诊断稳定性。

### 3. 实验设计
*   **基础模型**：涵盖不同逻辑先验能力的模型，包括 Qwen3-1.7B/8B（强先验）、DeepSeek-R1-Distill-Llama-8B（中等先验）、Llama-3.1-8B-Instruct（弱先验）。
*   **数据集**：主要使用 DAPO-Math-17K 进行训练，并在 DeepMath-103K 上验证泛化性。
*   **Benchmark**：MATH500、Minerva Math、OlympiadBench、AIME24、AMC23，以及发布时间晚于所有模型的 **AIME26**（用于验证零污染推理能力）。
*   **对比方法**：Base 模型、有监督强化学习（S-RL）以及五种不同的无监督奖励配置。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件型号（如 GPU 数量、型号）及总训练时长。
*   附录中提供了超参数细节：Actor 学习率为 $1 \times 10^{-6}$，Prompt Batch Size 为 8，每个 Prompt 生成 16 个响应（K=16）。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨模型（4 种）、跨奖励函数（5 种）、跨数据集（2 个）、跨上下文长度（4K/8K）的多维度对比。
*   **充分性**：实验设计较为充分。通过引入 AIME26 这一“未来”数据集，有效地排除了数据污染的可能性；通过消融实验解耦了“长度惩罚”与“不确定性惩罚”的影响，论证严谨。

### 6. 主要结论与发现
*   **有效性**：在强基础模型（如 Qwen3）上，纯长度惩罚（LP）甚至能超过有监督 RL 的表现。
*   **边界条件**：无监督 RL 的成功高度依赖基础模型的“逻辑先验”。弱模型（如 Llama 3.1）在无监督训练下会立即崩溃。
*   **惩罚机制**：惩罚回复长度比单纯惩罚不确定性更有效，强制简洁性能自然诱导模型发现正确的推理路径。
*   **几何解释（流形包络）**：成功的训练轨迹在 3D 相空间中被紧凑的流形所包络（体积适中且有序）；失败表现为“探索停滞”（体积过小）或“流形爆炸”（体积异常巨大且混乱）。

### 7. 优点
*   **视角新颖**：首次提出从几何流形包络的视角来解释无监督 RL 的稳定性，将抽象的训练动态可视化。
*   **机制解耦**：成功区分了长度惩罚和熵惩罚在数学推理中的不同贡献。
*   **实战价值**：证明了在没有标注数据的情况下，通过合理的内在奖励也能大幅提升模型性能。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 8B 及以下规模的模型，尚未在大规模模型（如 70B+）上验证。
*   **理论证明缺失**：虽然观察到了熵质心的稳定性，但缺乏严格的数学优化证明。
*   **策略不稳定性**：所有无监督方法最终都会走向崩溃，必须依赖“早停（Early Stopping）”策略，这限制了其在自动化流水线中的应用。
*   **算力细节缺失**：未披露具体算力消耗，不便于其他研究者评估复现成本。

（完）
