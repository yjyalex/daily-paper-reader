---
title: "ExecVerify: White-Box RL with Verifiable Stepwise Rewards for Code Execution Reasoning"
title_zh: ExecVerify：具有可验证分步奖励的白盒强化学习，用于代码执行推理
authors: "Lingxiao Tang, He Ye, Zhaoyang Chu, Muyang Ye, Zhongxin Liu, Xiaoxue Ren, Lingfeng Bao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11226v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 具有可验证分步奖励的白盒强化学习用于代码推理
tldr: 针对代码大模型在执行推理方面的不足，本文提出 ExecVerify 框架。该方法通过约束程序合成构建多难度数据集，并利用强化学习引入可验证的白盒逐步奖励（如变量值和下一语句预测），使模型在每一步执行中都与语义正确性对齐。实验显示，该方法显著提升了 7B 模型的推理能力，使其在代码生成任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的监督微调方法依赖不可验证的教师解释，导致模型仅在模仿文本而非理解执行逻辑。
method: 提出一种结合约束程序合成与强化学习的方法，通过可验证的中间执行步骤奖励来优化模型。
result: "经过 ExecVerify 训练的 7B 模型在代码推理上可比肩 32B 模型，并在代码生成任务中将 pass@1 提升了高达 5.9%。"
conclusion: 通过引入可验证的逐步奖励和两阶段训练，可以有效增强小型代码模型的执行推理能力并迁移至生成任务。
---

## 摘要
代码大语言模型（LLMs）在代码执行推理方面仍面临挑战，尤其是在较小规模的模型中。现有方法依赖于使用教师生成解释的有监督微调（SFT），主要有两种形式：（1）输入-输出（I/O）预测链，以及（2）执行轨迹的自然语言描述。然而，在 SFT 过程中，中间执行步骤无法被显式验证，因此训练目标可能会退化为仅仅匹配教师的解释。此外，训练数据的收集通常缺乏对任务难度的显式控制。我们提出了 ExecVerify，它通过引入源自执行轨迹的可验证白盒奖励（包括下一条语句预测和变量值/类型预测），超越了单纯的文本模仿。我们的工作首先通过基于约束的程序合成构建了一个具有多个难度等级的数据集。然后，我们应用强化学习（RL）来奖励关于中间执行步骤和最终输出的正确回答，使训练目标与每个执行步骤的语义正确性保持一致。最后，我们采用了一个两阶段训练流水线，首先增强执行推理能力，然后迁移到代码生成任务。实验表明，使用 ExecVerify 训练的 7B 模型在代码推理基准测试中达到了与 32B 模型相当的性能，并且在代码生成任务上相比强力的后训练基准模型，其 pass@1 提高了多达 5.9%。

## Abstract
Code LLMs still struggle with code execution reasoning, especially in smaller models. Existing methods rely on supervised fine-tuning (SFT) with teacher-generated explanations, primarily in two forms: (1) input-output (I/O) prediction chains and (2) natural-language descriptions of execution traces. However, intermediate execution steps cannot be explicitly verified during SFT, so the training objective can reduce to merely matching teacher explanations. Moreover, training data is typically collected without explicit control over task difficulty. We introduce ExecVerify, which goes beyond text imitation by incorporating verifiable white-box rewards derived from execution traces, including next-statement prediction and variable value/type prediction. Our work first builds a dataset with multiple difficulty levels via constraint-based program synthesis. Then, we apply reinforcement learning (RL) to reward correct answers about both intermediate execution steps and final outputs, aligning the training objective with semantic correctness at each execution step. Finally, we adopt a two-stage training pipeline that first enhances execution reasoning and then transfers to code generation. Experiments demonstrate that a 7B model trained with ExecVerify achieves performance comparable to 32B models on code reasoning benchmarks and improves pass@1 by up to 5.9\% on code generation tasks over strong post-training baselines.

---

## 论文详细总结（自动生成）

以下是对论文《ExecVerify: White-Box RL with Verifiable Stepwise Rewards for Code Execution Reasoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的代码大语言模型（Code LLMs），尤其是小参数模型，在**代码执行推理**（即理解程序运行时的动态逻辑）方面表现较弱。
*   **研究背景**：
    *   **SFT 的局限性**：目前的监督微调（SFT）方法依赖教师模型生成的解释（如 I/O 推理链或执行轨迹描述）。然而，SFT 的交叉熵损失函数仅优化 Token 匹配，无法在训练过程中显式验证中间执行步骤的正确性，导致模型倾向于“模仿文本”而非“理解语义”。
    *   **数据质量问题**：现有的训练数据通常是随机收集的，缺乏对任务难度和结构复杂性的显式控制，导致训练效率低下。

### 2. 方法论
ExecVerify 框架包含两个核心部分：**基于约束的数据合成**和**两阶段后训练流水线**。

*   **基于约束的数据合成**：
    *   **结构化约束**：通过显式要求 LLM 使用特定的 Python 内置类型、方法以及嵌套控制流（if/for/while）来合成程序，确保代码的多样性和复杂性。
    *   **难度过滤**：利用基准模型（如 Qwen2.5-Coder-7B）对合成样本进行多次预测，仅保留模型难以一次性通过（成功率 $\le 3/10$）的非平凡样本，构建“课程学习”式的数据集。
*   **两阶段后训练（Post-training）**：
    *   **第一阶段：白盒强化学习（White-Box RL）**：
        *   **核心思想**：将解释器生成的执行轨迹转化为可验证的“白盒问题”，包括**控制流预测**（下一行执行哪句）和**数据流预测**（变量的值和类型）。
        *   **奖励函数**：$R_{white-box} = 2 \cdot [(1 - \alpha) R_{(I \to O)} + \alpha R_{white}]$。其中 $R_{white}$ 是对中间步骤预测准确性的奖励，$\alpha$ 用于平衡最终输出正确性与中间步骤准确性。
    *   **第二阶段：代码生成强化学习**：
        *   利用第一阶段获得的推理能力，通过单元测试（Unit-Test）反馈作为奖励，进一步微调模型以提升代码生成任务的性能。

### 3. 实验设计
*   **数据集与基准测试（Benchmarks）**：
    *   **代码推理**：CRUXEval（输入/输出预测）、LiveCodeBench-Exec、REval（中间状态预测）。
    *   **代码生成**：EvalPlus (HumanEval/MBPP)、LiveCodeBench-V6、BigCodeBench。
    *   **扩展测试**：CRUXEval-X（跨语言推理）、涉及外部库的 I/O 预测。
*   **对比方法**：
    *   通用模型：Qwen2.5-Coder (7B/14B/32B)、Llama3 (8B/70B)、DeepSeek-Coder-V2-Lite。
    *   专门针对推理优化的模型：SemCoder、CodeI/O。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：全参数微调，使用 LLaMAFactory 框架和 DeepSpeed ZeRO-2。
    *   **RL 阶段**：使用 GRPO 算法，基于 VeRL 框架，采用 FSDP（参数与优化器卸载）和梯度检查点技术。
    *   **训练规模**：推理 RL 阶段使用了约 30K 合成样本，生成 RL 阶段使用了 PrimeCode 数据集。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 6 个主流基准测试、跨 6 种编程语言（Java, C++, Go 等）的泛化性测试。
*   **消融实验**：针对数据合成组件（约束、输入合成、难度过滤）、白盒问题类型（仅控制流 vs 仅数据流）、奖励系数 $\alpha$ 的敏感度等进行了详尽的消融实验。
*   **客观性**：通过 Embedding 相似度分析排除了数据污染风险；对比了相同数据规模下不同方法的效率，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **推理能力飞跃**：经过 ExecVerify 训练的 **7B 模型**在代码推理基准上的表现（平均 80.8 分）超越了许多参数量更大的模型（如 Llama3-70B 的 63.0 分），并与 **32B 模型**（77.9 分）持平。
*   **生成能力迁移**：白盒推理训练显著提升了下游代码生成任务的性能，pass@1 相比纯 GRPO 基准提升了高达 **5.9%**。
*   **跨语言泛化**：尽管训练数据主要是 Python，但模型在其他 6 种语言的执行推理能力也得到了同步提升。

### 7. 优点（亮点）
*   **可验证性**：将不可验证的自然语言解释转变为可由解释器自动验证的白盒奖励，解决了 RL 在代码推理中缺乏中间反馈的问题。
*   **主动合成**：相比被动收集数据，基于约束的合成方法能更有效地覆盖长尾编程模式和复杂逻辑。
*   **两阶段策略**：证明了“先学推理、再学生成”的路径比直接学习生成更有效，推理能力的增强能显著减少逻辑错误。

### 8. 不足与局限
*   **语言覆盖**：合成数据目前仅限于 Python，虽然有跨语言泛化效果，但对其他语言的深度特性覆盖不足。
*   **任务粒度**：实验主要集中在函数级或代码片段级，尚未扩展到多文件、项目级的复杂代码库。
*   **计算开销**：白盒 RL 需要频繁调用解释器并生成大量中间问题的 Rollout，相比标准 SFT，其计算和工程开销更高。

（完）
