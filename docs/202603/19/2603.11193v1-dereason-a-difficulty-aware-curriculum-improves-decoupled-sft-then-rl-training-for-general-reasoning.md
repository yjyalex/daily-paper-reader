---
title: "DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning"
title_zh: DeReason：一种难度感知的课程学习方法，旨在改进通用推理中的解耦式 SFT-RL 训练
authors: "Hanxu Hu, Yuxuan Wang, Maggie Huan, Jannis Vamvas, Yinya Huang, Zhijiang Guo, Rico Sennrich"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11193v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用可验证奖励的强化学习来激发大语言模型的推理能力
tldr: 本研究针对通用STEM领域中RL训练效率低的问题，提出DeReason框架。该框架通过LLM评分将数据按推理强度解耦：简单数据用于SFT以构建基础知识，复杂数据用于RL以强化推理能力。实验证明，这种基于难度的课程化数据分配策略在多个基准测试中显著优于单一SFT、RL或随机划分基线，为提升大模型通用推理能力提供了高效的后训练方案。
selection_source: fresh_fetch
motivation: 针对通用STEM领域中RL训练效率低下且与SFT协同机制不明的问题，探索如何优化两者间的数据分配。
method: 提出DeReason策略，利用LLM评分将数据按推理强度解耦，分别用于SFT建立基础和RL强化复杂推理。
result: 实验显示该解耦课程训练在通用STEM和数学基准上显著优于仅SFT、仅RL及随机划分数据的基线。
conclusion: 科学的数据解耦与课程化训练能有效结合SFT的知识覆盖与RL的推理优势，是提升模型通用推理能力的有效途径。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为激发大语言模型推理能力的强大范式，尤其是在数学和编程领域。尽管最近的研究已将这一范式扩展到更广泛的通用科学（STEM）领域，但在这些背景下，有监督微调（SFT）与强化学习（RL）之间复杂的相互作用仍未得到充分探索。在本文中，我们通过受控实验揭示了一个关键挑战：对于通用 STEM 领域，直接在基座模型上应用 RL 的样本效率极低，且其表现始终逊于在具有中等质量响应的数据上进行的有监督微调（SFT）。然而，先进行 SFT 再进行 RL 的顺序训练可以进一步提升性能，这表明这两个阶段起到了互补作用，且训练数据在两者之间的分配方式至关重要。因此，我们提出了 DeReason，一种针对通用推理的基于难度的数据解耦策略。DeReason 通过基于 LLM 评分估算的推理强度，将训练数据划分为推理密集型和非推理密集型子集。它将覆盖面广、非推理密集型的问题分配给 SFT 以建立基础领域知识，并保留一部分集中的难题给 RL 以培养复杂的推理能力。我们证明，这种有原则的解耦比随机拆分数据进行顺序 SFT 和 RL 具有更好的性能。在通用 STEM 和数学基准测试上的广泛实验表明，我们的解耦课程训练显著优于仅 SFT、仅 RL 以及随机拆分的基准模型。我们的工作对通用推理中 SFT 和 RL 之间的相互作用进行了系统研究，提供了一种高效且通用的后训练方案。

## Abstract
Reinforcement learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for eliciting reasoning capabilities in large language models, particularly in mathematics and coding. While recent efforts have extended this paradigm to broader general scientific (STEM) domains, the complex interplay between supervised fine-tuning (SFT) and RL in these contexts remains underexplored. In this paper, we conduct controlled experiments revealing a critical challenge: for general STEM domains, RL applied directly to base models is highly sample-inefficient and is consistently surpassed by supervised fine-tuning (SFT) on moderate-quality responses. Yet sequential SFT followed by RL can further improve performance, suggesting that the two stages play complementary roles, and that how training data is allocated between them matters. Therefore, we propose DeReason, a difficulty-based data decoupling strategy for general reasoning. DeReason partitions training data by reasoning intensity estimated via LLM-based scoring into reasoning-intensive and non-reasoning-intensive subsets. It allocates broad-coverage, non-reasoning-intensive problems to SFT to establish foundational domain knowledge, and reserves a focused subset of difficult problems for RL to cultivate complex reasoning. We demonstrate that this principled decoupling yields better performance than randomly splitting the data for sequential SFT and RL. Extensive experiments on general STEM and mathematical benchmarks demonstrate that our decoupled curriculum training significantly outperforms SFT-only, RL-only, and random-split baselines. Our work provides a systematic study of the interplay between SFT and RL for general reasoning, offering a highly effective and generalized post-training recipe.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在通用科学（STEM）领域，如何有效结合有监督微调（SFT）与强化学习（RL）以提升模型的推理能力？
*   **研究背景**：虽然 RLVR（带验证奖励的强化学习）在数学和代码领域表现卓越，但在通用 STEM 领域，直接在基座模型上进行 RL 存在**样本效率极低**的问题。
*   **研究动机**：作者通过受控实验发现，在数据量相同的情况下，使用中等质量数据进行 SFT 的效果往往优于纯 RL。这引发了对 SFT（擅长知识获取）与 RL（擅长复杂推理探索）角色分工的思考，即如何科学地在两个阶段之间分配训练数据。

### 2. 方法论：DeReason 框架
DeReason 提出了一种基于难度的“解耦式”课程训练策略，核心流程如下：
1.  **难度评估（Difficulty Estimation）**：使用一个与策略模型规模相当的指令微调模型（如 Qwen3-4B-Instruct）作为评判者，根据推理步骤、所需领域知识等因素，将题目按 1-5 分进行“推理强度”打分。
2.  **数据解耦（Data Partitioning）**：
    *   **SFT 子集**：得分较低（≤3）的题目，侧重于知识召回和简单应用，用于建立基础领域知识。
    *   **RL 子集**：得分较高（≥4）的题目，侧重于多步推导，用于培养复杂推理能力。
3.  **课程训练（Curriculum Training）**：
    *   **阶段一**：在 SFT 子集上进行微调，获得冷启动模型 $\pi_{SFT}$。
    *   **阶段二**：以 $\pi_{SFT}$ 为起点，在 RL 子集上使用 **GRPO**（组相对策略优化）算法进行强化学习。
*   **验证机制**：针对通用领域无法使用规则校验的问题，采用模型基验证器（Model-based Verifier）来判断答案的语义等价性。

### 3. 实验设计
*   **数据集**：使用了 **WebInstruct-Verified** 和 **Webscale-RL** 两个专注于 STEM 领域的训练集。
*   **Benchmark（评估基准）**：
    *   **通用推理**：MMLU-Pro、GPQA-Diamond（专家级难度）、SuperGPQA（大规模学科覆盖）、BBEH（极难任务）。
    *   **数学推理**：AIME24、AIME25、MATH500。
*   **对比方法**：
    *   **Pure SFT / Pure RL**：仅使用 SFT 或仅使用 RL。
    *   **Random SFT-then-RL**：随机划分数据进行顺序训练。
    *   **SFT-then-Selected-SFT**：两阶段均为 SFT，第二阶段使用难题。

### 4. 资源与算力
*   **模型规模**：主要基于 **Qwen3-4B-Base** 进行实验。
*   **训练框架**：SFT 使用 Llama-Factory，RL 使用 VeRL。
*   **超参数**：SFT 学习率 1e-5，Batch Size 128；RL 学习率 1e-6，最大响应长度 8192 标记。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型训练集上分别验证，涵盖了从通用科学到纯数学的多个维度。
*   **消融实验**：对比了不同难度分布（Score 1-5）对 RL 训练效果的影响，并分析了从基座模型启动 vs 从 SFT 模型启动的差异。
*   **分析深度**：提供了策略熵（Entropy）、响应长度演变、奖励分数变化等细粒度分析。
*   **公平性**：通过控制训练步数和数据总量，确保了 SFT 与 RL 在对比时的客观性。

### 6. 主要结论与发现
1.  **SFT 的不可替代性**：对于小参数模型，SFT 作为知识蒸馏和冷启动机制，其效率远高于纯 RL。
2.  **难度的分工效应**：简单数据适合 SFT 建立知识库，而难题适合 RL 探索推理路径。DeReason 策略在 BBEH 等高难度推理任务上提升最显著。
3.  **RL 的压缩作用**：实验观察到，RL 在 SFT 之后能够显著缩短响应长度（去除冗余），同时使策略变得更加确定（熵降低）。
4.  **性能突破**：DeReason 在 4B 规模上达到了领先水平，甚至在某些指标上接近更大规模的模型。

### 7. 优点
*   **即插即用**：该方法纯粹在数据选择层面操作，不改变底层 RL 算法，易于集成到现有流水线中。
*   **揭示机制**：系统地分析了 SFT 和 RL 在后训练阶段的不同行为特征（如长度控制和熵减）。
*   **成本效益**：通过科学分配数据，避免了在简单题目上浪费昂贵的 RL 算力。

### 8. 不足与局限
*   **模型规模局限**：实验主要集中在 4B 小模型上，尚未在大规模模型（如 70B+）上验证该解耦策略的扩展性。
*   **依赖评分质量**：数据划分高度依赖于作为“评判者”的 LLM 的打分准确性，若评分模型存在偏差，可能影响解耦效果。
*   **验证器风险**：在通用领域使用模型基验证器（Model-based Verifier）可能引入“奖励作弊”或误判风险，不如数学领域的规则校验严谨。

（完）
