---
title: Reliable Reasoning in SVG-LLMs via Multi-Task Multi-Reward Reinforcement Learning
title_zh: 通过多任务多奖励强化学习实现 SVG-LLM 中的可靠推理
authors: "Haomin Wang, Qi Wei, Qianli Ma, Shengyuan Ding, Jinhui Yin, Kai Chen, Hongjie Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16189v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于SVG生成中思维链推理的多任务多奖励强化学习
tldr: 本研究针对SVG生成模型中泛化能力差、代码冗余及缺乏显式推理的问题，提出了CTRL-S框架。该框架引入思维链（CoT）机制以显式展示推理过程，并构建了包含14.5万个样本的高质量数据集SVG-Sophia。通过采用GRPO算法和多奖励优化策略（涵盖视觉相似度、格式及代码效率），CTRL-S在文本/图像转SVG及代码优化任务中显著提升了结构一致性和视觉保真度。
selection_source: fresh_fetch
motivation: 现有的SVG生成模型面临泛化能力受限、代码路径冗余以及生成过程中缺乏显式逻辑推理的挑战。
method: 提出了CTRL-S框架，通过引入思维链机制、构建SVG-Sophia数据集，并结合GRPO算法进行多任务、多奖励的强化学习优化。
result: 实验结果显示，CTRL-S在任务成功率、SVG代码质量和视觉保真度方面均显著超越了现有先进方法。
conclusion: 通过结合结构化推理与多维度强化学习，CTRL-S系统性地提升了大型语言模型在SVG生成任务中的可靠性与生成质量。
---

## 摘要
随着视觉语言模型的飞速发展，越来越多的研究开始探索其在 SVG 生成任务中的潜力。尽管现有方法通过构建大规模 SVG 数据集和引入 SVG 特定标记（tokens）提升了性能，但它们仍面临泛化能力有限、代码输出中存在冗余路径以及缺乏显式推理等问题。在这项工作中，我们提出了 CTRL-S（SVG 思维链强化学习），这是一个统一的框架，通过引入思维链机制，在 SVG 生成过程中显式地展示模型的推理过程。为了支持这种结构化推理，我们构建了 SVG-Sophia，这是一个包含 14.5 万个样本的高质量数据集，涵盖了 SVG 代码精炼、文本转 SVG 以及图像转 SVG 任务。通过训练模型生成组级（group-level）结构化 SVG 代码，CTRL-S 显著提高了结构连贯性和视觉保真度。此外，我们采用了 GRPO 算法并设计了一个多奖励优化框架，整合了 DINO、图文相似度、格式以及代码效率奖励。通过联合多奖励优化和多任务训练，我们的方法系统地增强了整体生成能力。大量实验表明，CTRL-S 优于现有方法，实现了更高的任务成功率、更优的 SVG 代码质量以及卓越的视觉保真度。

## Abstract
With the rapid advancement of vision-language models, an increasing number of studies have explored their potential for SVG generation tasks. Although existing approaches improve performance by constructing large-scale SVG datasets and introducing SVG-specific tokens, they still suffer from limited generalization, redundant paths in code outputs, and a lack of explicit reasoning. In this work, we present CTRL-S (Chain-of-Thought Reinforcement Learning for SVG), a unified framework that introduces a chain-of-thought mechanism to explicitly expose the model's reasoning process during SVG generation. To support this structured reasoning, we construct SVG-Sophia, a high-quality dataset containing 145K samples across SVG code refinement, Text-to-SVG, and Image-to-SVG tasks. By training the model to generate group-level structured SVG code, CTRL-S significantly improves structural coherence and visual fidelity. Furthermore, we adopt the GRPO algorithm and design a multi-reward optimization framework, incorporating DINO, image-text similarity, format, and code efficiency rewards. Through joint multi-reward optimization and multi-task training, our approach systematically enhances overall generation capabilities. Extensive experiments show that CTRL-S outperforms existing methods, achieving higher task success rates, superior SVG code quality, and exceptional visual fidelity.

---

## 论文详细总结（自动生成）

以下是对论文《Reliable Reasoning in SVG-LLMs via Multi-Task Multi-Reward Reinforcement Learning》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言模型（VLM）在生成可缩放矢量图形（SVG）时，面临泛化能力有限、生成的代码路径冗余、缺乏显式的逻辑推理过程以及代码结构混乱（难以编辑和阅读）等挑战。
*   **研究背景**：SVG 具有分辨率无关和易于编辑的特性，在 Web 设计和 CAD 领域至关重要。虽然已有研究引入了 SVG 特定标记，但模型往往只是“黑盒”式地输出代码，缺乏像人类绘图那样的分步规划。
*   **整体含义**：本文旨在通过引入**思维链（CoT）推理**和**多任务多奖励强化学习（RL）**，使模型能够像人类一样先规划再绘图，从而生成结构清晰、视觉准确且高效的 SVG 代码。

### 2. 论文提出的方法论：CTRL-S 框架
*   **核心思想**：将 SVG 生成视为一个统一的序列生成任务，通过 CoT 显式展示推理过程，并利用强化学习在视觉、语义和效率维度上进行对齐。
*   **关键技术细节**：
    *   **SVG-Sophia 数据集**：构建了包含 14.5 万个样本的数据集，涵盖文本转 SVG、图像转 SVG 和代码精炼（Refinement）三大任务。其特点是每个样本都包含“推理步骤”与“组级（`<g>` 标签）代码块”的一一对应关系。
    *   **两阶段监督微调（SFT）**：第一阶段进行 SVG 特定标记的对齐；第二阶段进行 CoT 结构化对齐训练。
    *   **多任务多奖励 GRPO 训练**：采用组相对策略优化（GRPO）算法，设计了四个奖励函数：
        1.  **格式奖励 ($R_{format}$)**：确保代码可渲染且包含正确的推理/代码块。
        2.  **DINO 奖励 ($R_{dino}$)**：利用 DINOv2 特征提取器计算生成图像与目标图像的深层视觉相似度。
        3.  **图文相似度奖励 ($R_{lclip}$)**：利用 Long-CLIP 确保生成的 SVG 符合长文本指令的语义。
        4.  **代码效率奖励 ($R_{eff}$)**：惩罚冗余代码，鼓励简洁生成。
    *   **总奖励公式**：$R_{total} = R_{format} \cdot (w_{dino}R_{dino} + w_{lclip}R_{lclip} + w_{eff}R_{eff})$，其中格式奖励作为“门控因子”。

### 3. 实验设计
*   **数据集/场景**：
    *   **SArena-Icon**：用于评估文本转 SVG 和图像转 SVG 的性能。
    *   **SVG-Sophia Refinement Benchmark**：专门用于评估代码修复和精炼能力。
*   **Benchmark 与对比方法**：
    *   **传统方法**：IconShop, VectorFusion, DiffVG, LIVE 等。
    *   **通用 VLM**：GPT-5.2, Claude-Sonnet-4.5, Gemini-3-Pro, Llama 4 系列。
    *   **专用 SVG-LLM**：StarVector, LLM4SVG, OmniSVG, InternSVG。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **SFT 阶段**：使用 **48 张 NVIDIA H200 GPU**。
    *   **RL 阶段**：使用 **32 张 GPU**。
*   **训练时长**：
    *   RL 阶段在 32 张 GPU 上训练 2 个 Epoch，耗时约 **12 小时**。
*   **其他参数**：SFT 全局 Batch Size 为 96，RL 阶段为 128，RL 采样数为每提示词 16 个响应。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估（涵盖 FID、CLIP 分数、DINO 相似度、SSIM、LPIPS、成功率和 Token 数量等多个指标）和定性视觉对比。
*   **消融实验**：
    *   验证了 **CoT 的必要性**（对比有无推理过程）。
    *   验证了 **多奖励函数的贡献**（逐一添加奖励项）。
    *   验证了 **奖励权重比例**（如 2:1:1 的最优性）。
    *   验证了 **多任务联合训练** 的增益（对比单任务训练）。
*   **充分性评价**：实验设计非常全面，不仅对比了开源模型，还对比了最先进的闭源商业模型（如 GPT-5.2），且消融实验覆盖了框架的核心组件，结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：CTRL-S 在所有三项任务中均达到了 SOTA 性能，尤其在视觉保真度和语义一致性上显著超过了闭源模型。
*   **推理增强质量**：引入 CoT 不仅提高了生成的成功率，还通过逻辑规划减少了几何畸变。
*   **RL 的显著作用**：相比仅进行 SFT，RL 训练显著提升了视觉指标，并大幅减少了冗余 Token（代码更简洁），推理速度提升。
*   **多任务互补**：同时训练生成和精炼任务能产生协同效应，增强模型对 SVG 结构的深层理解。

### 7. 优点（亮点）
*   **结构化对齐**：首次实现了推理步骤与 SVG `<g>` 标签组的严格一一对应，极大地提升了代码的可读性和可编辑性。
*   **多维度奖励设计**：结合了底层视觉（DINO）、高层语义（Long-CLIP）和代码工程效率（Efficiency），解决了以往模型“代码长但画不好”的问题。
*   **统一框架**：一个模型同时搞定生成、转换和修复，展示了极强的通用性。

### 8. 不足与局限
*   **计算成本**：虽然 RL 训练仅需 12 小时，但使用了昂贵的 H200 集群，且 GRPO 算法在采样阶段仍有较大的计算开销。
*   **数据依赖**：模型高度依赖于由 Claude-Sonnet-4.5 生成的高质量 CoT 标注，若基础标注存在逻辑偏差，可能会影响模型性能。
*   **复杂度限制**：目前的实验主要集中在图标和简单矢量图上，对于极其复杂的 CAD 图纸或超大规模矢量场景的性能表现尚待进一步验证。

（完）
