---
title: "Dual Consensus: Escaping from Spurious Majority in Unsupervised RLVR via Two-Stage Vote Mechanism"
title_zh: 双重共识：通过两阶段投票机制在无监督 RLVR 中摆脱伪多数
authors: "Kaixuan Du, Meng Cao, Hang Zhang, Yukun Wang, Xiangzhou Huang, Ni Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16223v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对大语言模型复杂推理任务的无监督强化学习
tldr: 针对无监督强化学习（RLVR）中大模型易陷入“伪多数”错误答案的问题，本文提出双重共识强化学习（DCRL）。该方法通过两阶段投票机制，结合锚点生成的支配性响应与探索者通过临时遗忘生成的多元信号，利用调和平均值产生更可靠的学习信号。在八个基准测试中，DCRL显著提升了推理性能，为无标签自我进化提供了新路径。
selection_source: fresh_fetch
motivation: 现有的无监督强化学习方法容易收敛于错误但普遍的答案，导致模型陷入局部最优并限制性能提升。
method: 提出一种两阶段共识机制，通过锚点模型和临时遗忘后的探索者模型共同投票，并取调和平均值作为训练目标。
result: "在八个跨领域基准测试中，DCRL 的 Pass@1 表现均优于传统的多数投票法，且训练过程更加稳定。"
conclusion: DCRL 证明了在无需外部监督的情况下，通过内部共识机制可以有效克服伪多数偏差，实现更强的推理能力。
---

## 摘要
目前针对大语言模型（LLM）的无标签 RLVR 方法（如 TTRL 和 Self-reward）已证明在提升 LLM 复杂推理任务性能方面具有成效。然而，这些方法严重依赖准确的伪标签估计，且容易收敛于虚假但普遍的答案，从而陷入主导模式并限制了进一步的提升。基于此，我们提出了双重共识强化学习（DCRL），这是一种新型自监督训练方法，能够通过两阶段共识机制生成更可靠的学习信号。该模型最初作为锚点产生主导响应，随后作为探索者通过临时的遗忘过程生成多样化的辅助信号。最终的训练目标由这两组信号集的调和平均值导出。值得注意的是，该过程完全不需要外部模型或监督。在八个基准测试和不同领域中，DCRL 在 Pass@1 指标上始终优于多数投票，同时表现出更稳定的训练动态。这些结果表明，DCRL 为在无标签情况下实现更强的推理能力开辟了一条可扩展的路径。

## Abstract
Current label-free RLVR approaches for large language models (LLMs), such as TTRL and Self-reward, have demonstrated effectiveness in improving the performance of LLMs on complex reasoning tasks. However, these methods rely heavily on accurate pseudo-label estimation and converge on spurious yet popular answers, thereby trapping in a dominant mode and limiting further improvements. Building on this, we propose Dual Consensus Reinforcement Learning (DCRL), a novel self-supervised training method which is capable of generating more reliable learning signals through a two-stage consensus mechanism. The model initially acts as an anchor, producing dominant responses; then it serves as an explorer, generating diverse auxiliary signals via a temporary unlearning process. The final training target is derived from the harmonic mean of these two signal sets. Notably, the process operates entirely without external models or supervision. Across eight benchmarks and diverse domains, DCRL consistently improves Pass@1 over majority vote while yielding more stable training dynamics. These results demonstrate that DCRL establishes a scalable path toward stronger reasoning without labels.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DCRL（Dual Consensus Reinforcement Learning，双重共识强化学习）** 的新方法，旨在解决大语言模型（LLM）在无监督强化学习中容易陷入“伪多数”错误答案的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在无标签的强化学习（URLVR）中，模型通常依赖“多数投票”（Majority Vote）来生成伪标签。然而，模型往往会收敛于错误但出现频率高的“伪多数”答案，导致训练陷入局部最优（众数塌陷），限制了推理能力的进一步提升。
*   **研究动机**：现有的无监督方法（如 TTRL、Self-reward）缺乏有效的探索机制，且无法区分“流行的错误”和“正确的少数”。作者希望利用模型自身的鲁棒性，在无需外部监督的情况下，通过内部共识机制识别更准确的奖励信号。

### 2. 方法论：核心思想与技术细节
DCRL 通过**两阶段投票机制**来产生可靠的学习信号：
*   **先遗忘再探索（Unlearn Then Explore）**：
    *   **锚点（Anchor）阶段**：使用当前策略模型生成主导响应，捕捉模型当前的分布。
    *   **探索者（Explorer）阶段**：对锚点模型进行一次**临时遗忘（Temporary Unlearning）**操作。通过最小化稳定后的遗忘损失函数（抑制高概率 Token），强制模型“展平”分布，从而生成偏离主导模式的多样化辅助信号。
*   **调和选举（Harmonic Election）**：
    *   计算候选答案在锚点分布（$p_0$）和探索者分布（$p_1$）中的经验概率。
    *   使用**调和平均值** $y^* = \arg \max \frac{2p_0 p_1}{p_0 + p_1}$ 选择伪标签。调和平均值对低值敏感，要求正确答案必须在两个阶段都具备一定的鲁棒性，从而有效过滤掉仅在锚点阶段流行的伪多数答案。
*   **自适应采样（Adaptive Sampling）**：
    *   引入“共识率”信号。当模型确定性较高（共识率 > 0.5）时，同时使用锚点和探索者的样本进行训练；否则仅使用锚点样本，以防止早期引入过多的探索噪声。
*   **基础算法**：采用 GRPO（Group Relative Policy Optimization）作为底层强化学习框架。

### 3. 实验设计
*   **数据集**：主要在 **DAPO-Math-14k**（数学推理）上进行大规模无监督训练。
*   **评测基准（8个）**：包括数学类（MATH-500, GSM8K, AIME24, Minerva-math, AMC, OlympiadBench）和多任务类（MMLU-Pro, GPQA-Diamond）。
*   **对比方法**：
    *   基于确定性的方法：RENT。
    *   基于聚合的方法：TTRL、Co-Rewarding-I/II。
    *   有监督基准：使用真实标签的 GRPO。
*   **模型**：Llama3.2-3B-Instruct, Qwen3-4B/8B-Base, Qwen2.5-Math-1.5B。
*   **场景**：涵盖了大规模无监督预训练和**测试时微调（Test-Time Adaptation, TTA）**。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   文中提到使用了 **VeRL** 分布式强化学习框架，并在 DAPO-Math-14k 数据集上训练了 2 个 Epoch。考虑到 8B 参数模型的强化学习训练，通常需要数十至上百张高性能 GPU（如 A100 或 H100）。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。涵盖了从 1.5B 到 8B 的多个模型，并在 8 个主流基准上进行了测试。
*   **消融实验**：针对调和选举、保守奖励设计、动态采样等核心组件做了详细的消融分析。
*   **客观性**：通过 Pass@1（16个独立种子平均）进行评估，并对比了有监督学习的上限，实验设计严谨、客观。

### 6. 主要结论与发现
*   **性能超越**：DCRL 在所有无监督基线方法中表现最优，甚至在某些任务上接近或超过了使用真实标签的有监督 GRPO。
*   **摆脱伪多数**：实验证明 DCRL 能够选择出那些一致性较低但正确的答案，有效纠正了多数投票的偏差。
*   **稳定性**：相比于传统的多数投票，DCRL 提供了更准确的奖励信号（Ground-truth Reward Accuracy 更高），训练动态更稳定。
*   **泛化性**：在测试时微调（TTA）场景下，DCRL 展现了极强的跨领域适应能力。

### 7. 优点
*   **无需外部监督**：完全自监督，不依赖外部模型（如大模型打分）或人工标注。
*   **创新的探索机制**：通过“临时遗忘”主动寻找正确答案，而非被动等待随机探索。
*   **数学证明**：附录中提供了 DCRL 优于多数投票的理论证明，增强了方法的说服力。

### 8. 不足与局限
*   **系统性偏差风险**：如果模型存在严重的系统性先验偏差（即锚点和探索者都一致认为某个错误答案是正确的），DCRL 仍可能失效并强化错误模式。
*   **超参数敏感性**：遗忘步的策略学习率（$\eta_u$）对结果影响较大，不同规模的模型需要精细调优。
*   **极端任务限制**：对于模型预训练阶段完全未涉及的极其复杂的分布外（OOD）任务，该方法提升有限。

（完）
