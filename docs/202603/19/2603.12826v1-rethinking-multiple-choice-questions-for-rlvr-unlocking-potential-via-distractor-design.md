---
title: "Rethinking Multiple-Choice Questions for RLVR: Unlocking Potential via Distractor Design"
title_zh: 重新思考 RLVR 中的多选题：通过干扰项设计释放潜力
authors: "Xu Guo, Qiming Ge, Jian Tong, Kedi Chen, Jin Zhang, Xiaogui Yang, Xuan Gao, Haijun Lv, Zhihui Lu, Yicheng Zou, Qipeng Guo"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12826v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于推理的具有可验证奖励的强化学习
tldr: 本研究探讨了在可验证奖励强化学习（RLVR）中利用多选题（MCQ）提升大模型推理能力的潜力。针对模型易通过随机猜测或排除法进行“奖励作弊”的问题，本文系统分析了选项设计的影响，发现选项数量匹配和强干扰项是关键。为此，提出了迭代干扰项策划（IDC）框架，通过主动构建高质量干扰项来阻断推理捷径。实验表明，该方法显著增强了干扰项质量，在多个基准测试中取得了优于原始数据的训练效果。
selection_source: fresh_fetch
motivation: 解决在强化学习（RLVR）中使用多选题时，模型容易通过随机猜测或排除法进行“奖励作弊”而忽略推理过程的问题。
method: 提出迭代干扰项策划（IDC）框架，通过主动构建高质量干扰项来阻断排除法捷径，引导模型进行深度推理。
result: 实验证明 IDC 能显著提升干扰项质量，并在多个基准测试中使 RLVR 训练效果优于原始数据。
conclusion: 优化多选题的干扰项设计是提升 RLVR 推理能力的有效途径，能充分释放多选题作为可验证数据源的潜力。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 显著增强了大语言模型的推理能力。当应用于 RLVR 时，多选题 (MCQs) 提供了一种可扩展的可验证数据源，但存在诱发奖励作弊 (reward hacking) 的风险，即模型通过随机猜测或简单的排除法来绕过推理过程。目前的方法通常通过将多选题转换为开放式格式来缓解这一问题，从而丢弃了专家设计的干扰项所提供的对比信号。在这项工作中，我们系统地研究了选项设计对 RLVR 的影响。我们的分析强调了两个主要见解：(1) 训练和测试之间选项数量的不匹配会降低性能；(2) 强干扰项能有效缓解随机猜测，即使是二选一问题也能实现有效的 RLVR 训练。受这些发现的启发，我们提出了迭代干扰项策划 (IDC)，这是一个主动构建高质量干扰项的框架，旨在阻断排除法捷径并促进深度推理。在各种基准测试上的实验表明，与原始数据相比，我们的方法有效地提高了干扰项质量，并在 RLVR 训练中取得了显著收益。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) significantly enhances the reasoning capabilities of Large Language Models. When applied to RLVR, Multiple-Choice Questions (MCQs) offer a scalable source of verifiable data but risk inducing reward hacking, where models shortcut reasoning via random guessing or simple elimination. Current approaches often mitigate this by converting MCQs to open-ended formats, thereby discarding the contrastive signal provided by expert-designed distractors. In this work, we systematically investigate the impact of option design on RLVR. Our analysis highlights two primary insights: (1) Mismatches in option counts between training and testing degrade performance. (2) Strong distractors effectively mitigate random guessing, enabling effective RLVR training even with 2-way questions. Motivated by these findings, we propose Iterative Distractor Curation (IDC), a framework that actively constructs high-quality distractors to block elimination shortcuts and promote deep reasoning. Experiments on various benchmarks demonstrate that our method effectively enhances distractor quality and yields significant gains in RLVR training compared to the original data.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Multiple-Choice Questions for RLVR: Unlocking Potential via Distractor Design》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：带可验证奖励的强化学习（RLVR）在提升大模型推理能力方面效果显著，但在非数学/代码领域，通常依赖多选题（MCQ）作为可验证信号。
*   **核心痛点**：MCQ 存在“奖励作弊”（Reward Hacking）风险。模型往往不通过逻辑推理，而是利用随机猜测或简单的排除法（排除明显的弱干扰项）来获取正确答案，导致强化学习无法真正提升推理能力。
*   **研究动机**：现有方法（如 Kimi k1.5）通常将 MCQ 转换为简答题以规避此问题，但这丢弃了专家设计的干扰项（Distractors）所蕴含的宝贵对比信号。本文旨在研究如何通过优化选项设计，在保留 MCQ 格式的同时释放其在 RLVR 中的潜力。

### 2. 方法论：迭代干扰项策划 (IDC)
*   **核心思想**：通过主动构建和筛选“强干扰项”，阻断模型的排除法捷径，迫使模型必须进行深度推理才能区分正确选项与干扰项。
*   **关键技术细节**：
    *   **经验强度 ($\hat{s}_j$)**：定义为模型在推理错误（逻辑有误）的情况下，选择该干扰项的频率。强度越高，说明该选项对模型的误导性越强。
    *   **IDC 框架流程**：
        1.  **填充阶段 (Filling)**：如果当前有效干扰项（强度 > 0）数量不足，则利用模型生成新候选项并加入池中。
        2.  **替换阶段 (Replacement)**：当选项数达到目标后，识别池中最弱的干扰项，并尝试用新生成的更强干扰项进行替换。
        3.  **语义等价检查**：引入检查机制，确保生成的干扰项不会在语义上变成另一个正确答案（避免语义坍塌）。
    *   **格式对齐策略**：研究发现训练和测试时的选项数量必须匹配，否则会产生分布偏差。因此，IDC 还包含选项扩展步骤，以确保训练数据与测试目标的格式一致。

### 3. 实验设计
*   **数据集/场景**：主要聚焦于医学领域，包括 MedQA（训练集）、MMLU-Pro (Health)、MedXpertQA、PubMedQA、SuperGPQA (Clinical Medicine)。此外还补充了法律领域的泛化实验。
*   **基准模型 (Backbones)**：Qwen2-7B-Instruct、Llama-3.1-8B-Instruct、Qwen2.5-14B-Instruct。
*   **对比方法**：
    *   **Orig.**：使用原始 MCQ 数据进行标准 RLVR。
    *   **Direct/Filter/Rewrite**：将 MCQ 转换为简答题的不同变体（直接转换、过滤不可转换项、模型重写）。
    *   **IDC**：本文提出的迭代干扰项策划方法。

### 4. 资源与算力
*   **硬件环境**：单节点 8 × NVIDIA H200 GPU。
*   **训练时长**：
    *   **IDC 流程**：对于约 10k 样本的 MedQA 数据集，7 轮迭代耗时约 1.5 小时。
    *   **RLVR 训练**：约 18-20 小时。
*   **开销分析**：IDC 阶段的计算开销仅占总流程的不到 10%，具有较高的效率。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的交叉验证（如 5×5 的选项数量匹配矩阵），涵盖了多个模型规模和多个学科领域。
*   **消融实验**：非常充分。包括：
    *   不同干扰项生成器（Self-generated vs. 强模型生成）的对比。
    *   迭代轮数（T）对性能影响的分析。
    *   不同强度估计器（自身估计 vs. 跨模型估计）的效果对比。
    *   多随机种子（3个种子）的稳定性验证。
*   **客观性**：通过标准化得分（z-score）处理不同难度测试集的偏差，实验设计严谨。

### 6. 主要结论与发现
*   **选项数量一致性至关重要**：训练和测试的选项数不匹配会显著损害性能，模型会学习到特定选项数量下的标签分布偏好。
*   **质量优于数量**：干扰项的“强度”比“数量”更关键。即使是二选一题目，只要干扰项足够强，其训练效果也优于带有多个弱干扰项的十选题。
*   **自我提升能力**：模型可以利用自身生成的干扰项进行有效学习，效果往往优于使用更强大的外部模型（如 GPT-4 级别）生成的干扰项，因为自生成的选项更贴近自身的决策边界。

### 7. 优点与亮点
*   **视角独特**：不同于主流的“弃用 MCQ 格式”做法，本文证明了保留并优化 MCQ 格式能提供更强的对比学习信号。
*   **IDC 框架闭环且高效**：利用拒绝采样思想，在不增加过多算力的情况下显著提升了数据质量。
*   **实用性强**：证明了模型可以实现“自产自销”式的自我进化，减少了对外部昂贵模型的依赖。

### 8. 不足与局限
*   **领域覆盖**：虽然在医学和法律领域有效，但在需要极长链条推理（如复杂数学证明）或多模态任务中的表现尚未验证。
*   **生成器依赖**：IDC 的效果上限仍受限于生成器模型产生高质量、有误导性文本的能力。
*   **推理链监控**：虽然 IDC 减少了奖励作弊，但本质上仍是基于结果的奖励（Outcome-based Reward），未能直接对推理链的每一步进行细粒度监督。

（完）
