---
title: "MA-VLCM: A Vision Language Critic Model for Value Estimation of Policies in Multi-Agent Team Settings"
title_zh: MA-VLCM：多智能体团队设置中用于策略价值估计的视觉语言评论家模型
authors: "Shahil Shaik, Aditya Parameshwaran, Anshul Nayak, Jonathon M. Smereka, Yue Wang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15418v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体强化学习与价值估计
tldr: 针对多智能体强化学习中中心化评论家采样效率低且泛化性差的问题，本文提出了MA-VLCM框架。该框架利用预训练的视觉语言模型（VLM）并进行微调，将其作为中心化评论家来评估多智能体行为。通过结合自然语言任务描述、视觉轨迹和状态信息，MA-VLCM显著提升了策略优化的采样效率，并能生成适用于资源受限机器人的紧凑执行策略，在零样本回报估计中表现优异。
selection_source: fresh_fetch
motivation: 传统的MARL中心化评论家学习效率低且难以泛化，而大型视觉语言模型虽具备强推理能力但直接部署于多机器人系统计算开销过大。
method: 提出MA-VLCM框架，使用经过微调的预训练视觉语言模型替代传统的学习型中心化评论家，基于多模态信息进行价值评估。
result: 实验表明，该方法在不同VLM骨干网络下，在分布内和分布外场景的多智能体团队设置中均实现了良好的零样本回报估计。
conclusion: MA-VLCM通过引入视觉语言模型作为评论家，有效解决了多智能体强化学习的效率与泛化难题，为资源受限环境下的策略部署提供了新思路。
---

## 摘要
多智能体强化学习（MARL）通常依赖于中心化评论家（centralized critic）来估计价值函数。然而，从头开始学习此类评论家的采样效率极低，且往往缺乏跨环境的泛化能力。与此同时，在互联网规模数据上训练的大型视觉-语言-动作模型（VLA）展现出强大的多模态推理和零样本泛化能力，但直接将其部署用于机器人执行在计算上仍然代价高昂，特别是在具有多样化形态和资源限制的异构多机器人系统中。为了应对这些挑战，我们提出了多智能体视觉-语言-评论家模型（MA-VLCM），该框架将 MARL 中学习到的中心化评论家替换为经过微调以评估多智能体行为的预训练视觉语言模型。MA-VLCM 作为一个中心化评论家，以自然语言任务描述、视觉轨迹观测和结构化多智能体状态信息为条件。通过在策略优化过程中消除评论家学习，我们的方法显著提高了采样效率，同时生成了适用于部署在资源受限机器人上的紧凑执行策略。结果表明，在多智能体团队设置中，使用不同 VLM 骨干网络（backbone）的模型在分布内和分布外场景下均表现出良好的零样本回报估计。

## Abstract
Multi-agent reinforcement learning (MARL) commonly relies on a centralized critic to estimate the value function. However, learning such a critic from scratch is highly sample-inefficient and often lacks generalization across environments. At the same time, large vision-language-action models (VLAs) trained on internet-scale data exhibit strong multimodal reasoning and zero-shot generalization capabilities, yet directly deploying them for robotic execution remains computationally prohibitive, particularly in heterogeneous multi-robot systems with diverse embodiments and resource constraints. To address these challenges, we propose Multi-Agent Vision-Language-Critic Models (MA-VLCM), a framework that replaces the learned centralized critic in MARL with a pretrained vision-language model fine-tuned to evaluate multi-agent behavior. MA-VLCM acts as a centralized critic conditioned on natural language task descriptions, visual trajectory observations, and structured multi-agent state information. By eliminating critic learning during policy optimization, our approach significantly improves sample efficiency while producing compact execution policies suitable for deployment on resource-constrained robots. Results show good zero-shot return estimation on models with differing VLM backbones on in-distribution and out-of-distribution scenarios in multi-agent team settings

---

## 论文详细总结（自动生成）

### MA-VLCM：多智能体团队设置中用于策略价值估计的视觉语言评论家模型

#### 1. 核心问题与研究动机
*   **核心问题**：在多智能体强化学习（MARL）中，传统的“中心化训练、去中心化执行”（CTDE）架构依赖于从零开始学习的中心化评论家（Critic）。这种方式存在**采样效率极低**、**跨环境泛化能力差**以及**缺乏语义理解**等问题。
*   **研究动机**：虽然大型视觉语言模型（VLM/VLA）具备强大的推理和泛化能力，但其模型庞大、推理缓慢，难以直接部署在资源受限的异构多机器人硬件上。因此，本文探讨如何利用预训练 VLM 的先验知识来充当“评论家”，评估多智能体团队的行为，从而辅助轻量化策略的训练。

#### 2. 方法论
*   **核心思想**：提出 **MA-VLCM** 框架，将预训练的 VLM 转化为一个能够理解自然语言指令、视觉轨迹和多智能体状态的中心化评论家模型。
*   **关键技术细节**：
    *   **多模态输入**：模型同时接收文本任务描述（$\ell$）、视觉轨迹视频（$I_t$）和结构化多智能体状态观测（$o_t$）。
    *   **图注意力网络（GAT）**：使用 GAT 处理智能体间的交互信息，生成排列不变性的嵌入向量，捕捉智能体间的动态协作关系。
    *   **观察令牌化**：将 GAT 的输出投影到 VLM 的令牌空间，引入专门的 `<OBS>` 令牌，使语言模型能联合推理视觉、文本和机器人状态。
    *   **训练目标**：采用双重损失函数：
        1.  **对比损失（Contrastive Loss）**：拉近高质量轨迹与任务描述的距离，推开低质量轨迹，增强模型对策略优劣的排序能力。
        2.  **价值回归损失（MSE）**：通过时间差分（TD）或蒙特卡洛回归，将潜在表示映射为具体的标量价值估计。
    *   **微调技术**：使用低秩自适应（LoRA）对冻结的 VLM 骨干进行高效微调。

#### 3. 实验设计
*   **实验场景**：
    1.  **结构化环境（RWARE）**：机器人仓库任务，涉及多智能体协作搬运货物。
    2.  **非结构化环境（Offroad Navigation）**：越野导航任务，要求智能体在避障的同时到达目标点。
*   **基准模型与对比**：
    *   对比了两种不同规模的 VLM 骨干：**LLaVA-OneVision-0.5B**（轻量级）和 **LLaVA-NeXT-Video-7B**（大规模）。
    *   对比了**原始冻结模型**与**加入 LoRA 微调**后的性能差异。
*   **评估指标**：斯皮尔曼等级相关系数（Spearman $\rho$）、均方误差（MSE）、平均绝对误差（MAE）以及预测区间宽度（MPIW）。

#### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA H200 GPU** 进行分布式训练。
*   **训练细节**：数据序列化为 WebDataset 格式以提高读取效率；视频剪辑长度为 32 帧；使用了 AdamW 优化器。
*   **推理速度**：0.5B 模型约为 0.20 秒/迭代，7B 模型约为 0.69 秒/迭代。

#### 5. 实验数量与充分性
*   **实验规模**：在两个截然不同的领域（仓库与越野）进行了测试，涵盖了**分布内（ID）**和**分布外（OOD）**场景。
*   **充分性评价**：实验设计较为全面，不仅对比了模型规模，还验证了 LoRA 的必要性。通过 100 个测试轨迹样本进行统计评估，结果具有一定的客观性。然而，目前仅在两个特定仿真环境中验证，尚未涉及真实世界机器人实验。

#### 6. 主要结论与发现
*   **LoRA 的关键作用**：加入 LoRA 微调显著提升了模型对策略优劣的排序能力（Spearman $\rho$ 大幅提高），这对于强化学习中的评论家至关重要。
*   **小模型优势**：**0.5B 参数的模型在价值估计任务上优于 7B 模型**。7B 模型表现出预训练表示过于“僵硬”，难以适应精确的标量回归，且推理速度慢了 3.45 倍。
*   **泛化能力**：MA-VLCM 在 OOD 场景（如改变智能体数量或任务指令）下表现出良好的零样本（Zero-shot）回报估计能力。

#### 7. 优点
*   **创新性**：首次将 VLM 引入多智能体中心化评论家的角色，解决了 MARL 评论家难以泛化的问题。
*   **多模态融合**：通过 GAT 有效整合了结构化的机器人状态与非结构化的视觉/文本信息。
*   **效率提升**：通过预训练模型提供价值导向，理论上可以消除 MARL 训练过程中从头学习评论家的需要，提高采样效率。

#### 8. 不足与局限
*   **绝对值偏差**：虽然模型在“排序”上表现优异，但在 OOD 场景下的**绝对价值预测（MSE）仍存在较大误差**，可能导致奖励信号不够精确。
*   **环境覆盖有限**：实验仅限于仿真环境，且任务类型相对单一（搬运与导航），尚未验证在更复杂竞争对抗场景下的表现。
*   **模型规模悖论**：大模型（7B）在特定回归任务上的失效表明，如何有效迁移超大规模预训练知识到精确的数值估计任务仍是挑战。

（完）
