---
title: "From $\\boldsymbol{\\logπ}$ to $\\boldsymbolπ$: Taming Divergence in Soft Clipping via Bilateral Decoupled Decay of Probability Gradient Weight"
title_zh: "从 $\\boldsymbol{\\logπ}$ 到 $\\boldsymbolπ$：通过概率梯度权重的双侧解耦衰减抑制软截断中的发散"
authors: "Xiaoliang Fu, Jiaye Lin, Yangyi Fang, Chaowen Hu, Cong Qin, Zekai Shao, Binbin Zheng, Lu Pan, Ke Zeng"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14389v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 大模型推理中的可验证奖励强化学习
tldr: 针对大语言模型推理中的强化学习（RLVR）优化不稳定性，本文指出传统软截断方法因依赖对数概率梯度在概率极小时会导致权重发散。为此，研究者提出DGPO算法，将优化原语从对数概率梯度转向概率梯度，并引入双侧解耦衰减机制。该方法在保持训练稳定的同时增强了探索能力，在DeepSeek-R1系列模型上的数学评测中表现优异，为RLVR提供了稳健的优化方案。
selection_source: fresh_fetch
motivation: 传统的硬截断限制了模型探索，而现有的软截断方法因对数概率梯度的数值特性在训练中容易导致权重发散和系统不稳定。
method: 提出DGPO算法，将优化原语改为概率梯度，并利用基于重要性采样率的双侧解耦衰减机制对边界Token进行非对称连续衰减处理。
result: 在DeepSeek-R1-Distill-Qwen系列（1.5B至14B）模型上的实验证明，DGPO在多项数学基准测试中一致优于GRPO等强基准算法。
conclusion: DGPO通过重新定义梯度优化原语，有效解决了强化学习中稳定性与探索性之间的冲突，为大语言模型推理能力的提升提供了可靠且可扩展的路径。
---

## 摘要
具有可验证奖励的强化学习（RLVR）催生了大语言模型（LLM）推理能力的飞跃，但其优化动力学仍然十分脆弱。像 GRPO 这样的标准算法通过“硬截断”（hard clipping）来强制维持稳定性，但这会因丢弃置信区域外 token 的梯度而无意中抑制了探索。虽然最近的“软截断”方法试图恢复这些梯度，但它们面临一个关键挑战：依赖对数概率梯度（$\nabla_θ\log π_θ$）会在概率趋于零时产生发散权重，从而导致 LLM 训练不稳定。我们重新审视了这一惯例，将概率梯度（$\nabla_θπ_θ$）确立为更优的优化原语。据此，我们提出了解耦梯度策略优化（DGPO），它采用了一种基于重要性采样比率的解耦衰减机制。通过对边界 token 应用非对称的连续衰减，DGPO 解决了稳定性与持续探索之间的冲突。在 DeepSeek-R1-Distill-Qwen 系列模型（1.5B/7B/14B）上进行的大量实验表明，DGPO 在各种数学基准测试中始终优于强基准模型，为 RLVR 提供了一种稳健且可扩展的解决方案。我们的代码和实现可在以下网址获取：https://github.com/VenomRose-Juri/DGPO-RL。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has catalyzed a leap in Large Language Model (LLM) reasoning, yet its optimization dynamics remain fragile. Standard algorithms like GRPO enforce stability via ``hard clipping'', which inadvertently stifles exploration by discarding gradients of tokens outside the trust region. While recent ``soft clipping'' methods attempt to recover these gradients, they suffer from a critical challenge: relying on log-probability gradient ($\nabla_θ\log π_θ$) yields divergent weights as probabilities vanish, destabilizing LLM training. We rethink this convention by establishing probability gradient ($\nabla_θπ_θ$) as the superior optimization primitive. Accordingly, we propose Decoupled Gradient Policy Optimization (DGPO), which employs a decoupled decay mechanism based on importance sampling ratios. By applying asymmetric, continuous decay to boundary tokens, DGPO resolves the conflict between stability and sustained exploration. Extensive experiments across DeepSeek-R1-Distill-Qwen series models (1.5B/7B/14B) demonstrate that DGPO consistently outperforms strong baselines on various mathematical benchmarks, offering a robust and scalable solution for RLVR. Our code and implementation are available at: https://github.com/VenomRose-Juri/DGPO-RL.

---

## 论文详细总结（自动生成）

这篇论文由美团、复旦、清华、北大等机构的研究人员共同完成，旨在解决大语言模型（LLM）在强化学习（RLVR）过程中，由于“软截断”机制导致的梯度发散和训练不稳定性问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在数学推理等领域，利用可验证奖励的强化学习（RLVR）能显著提升模型性能。
*   **核心矛盾**：
    *   **硬截断（如 GRPO/PPO）**：为了稳定训练，会将超出置信区域的 token 梯度设为零。这虽然稳定，但会导致“探索消失”，模型容易陷入局部最优和熵崩溃。
    *   **软截断（如 CISPO/GPPO）**：为了保留探索能力，允许边界外的梯度通过。但这些方法依赖**对数概率梯度（$\nabla_\theta \log \pi_\theta$）**。
*   **核心问题**：当 token 概率 $\pi$ 趋近于 0 时，对数概率梯度的权重（$1/\pi$）会呈发散式增长。这在左边界（低概率、负奖励情况）会导致梯度爆炸，使训练极度不稳定甚至崩溃。

### 2. 方法论：DGPO 算法
*   **核心思想**：将优化原语从“对数概率梯度”转向**“概率梯度（$\nabla_\theta \pi_\theta$）”**。作者认为概率空间具有更好的几何对称性和有界性，更适合设计稳定的梯度机制。
*   **关键技术细节**：
    *   **解耦衰减机制**：针对重要性采样（IS）比率的不同边界，设计了非对称的衰减函数。
    *   **左边界（低比率，负奖励）**：采用**多项式衰减**（$C_{left} \cdot \pi^n$）。当概率降低时，梯度权重随之减小，从而在数学上保证了权重的收敛，避免了发散。
    *   **右边界（高比率，正奖励）**：采用**倒数根号衰减**（$C_{right} \cdot \pi^{-1/m}$）。这能以较慢的速度减小梯度，从而在保持稳定的同时，鼓励模型对高奖励路径进行更深入的探索。
    *   **连续性保证**：通过引入常数 $C_{left}$ 和 $C_{right}$，确保了梯度权重在截断边界处的数学连续性，实现了平滑过渡。
*   **算法流程**：DGPO 不再直接截断 IS 比率，而是根据 IS 比率所处的区间（左边界、右边界、区间内），动态调整概率梯度的权重，然后进行反向传播。

### 3. 实验设计
*   **模型与数据**：
    *   **基座模型**：DeepSeek-R1-Distill-Qwen 系列（1.5B、7B、14B）。
    *   **训练数据**：开源的 DAPO-Math-17K 数据集。
*   **基准测试（Benchmarks）**：涵盖了 AIME24、AIME25、AMC23、MATH500、Minerva 和 OlympiadBench 等 6 个主流数学推理榜单。
*   **对比方法**：
    *   标准硬截断：**GRPO**。
    *   软截断变体：**CISPO**、**GPPO**、**CE-GPPO**。
    *   重要性采样改进：**ASPO**。
*   **评估指标**：Avg@32（期望性能）和 Pass@32（潜力上限）。

### 4. 资源与算力
*   **硬件设施**：使用了 30 个计算节点，每个节点配备 **8 张 NVIDIA A100 (80GB) GPU**，总计 240 张 A100。
*   **训练时长**：14B 模型的总有效训练时间约为 **500 小时**。
*   **软件栈**：基于 VeRL 框架，使用 FSDP 进行分布式训练，vLLM 作为推理后端。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了三个参数量级（1.5B 到 14B），验证了算法的可扩展性。
    *   针对超参数 $n$ 和 $m$ 进行了多组消融实验（如 (1,1), (1,2), (2,1), (2,2) 等组合）。
    *   在 6 个不同的数学数据集上进行了全面测试。
*   **充分性与公平性**：实验设计较为严谨。所有对比方法统一了截断阈值（$\epsilon=0.2$），并根据模型规模校准了学习率（遵循“恒定总梯度方差”原则），确保了对比的客观性。

### 6. 主要结论与发现
*   **性能领先**：DGPO 在 1.5B 和 7B 模型上显著优于 GRPO 和其他软截断方法。例如，在 1.5B 模型上，平均 Avg@32 比 GRPO 高出 4.3%。
*   **稳定性强**：相比于 CISPO 和 GPPO 在训练后期容易出现的崩溃现象，DGPO 表现出极佳的训练稳定性。
*   **熵动力学优化**：DGPO 实现了更温和、受控的策略熵下降，有效平衡了探索（Exploration）与利用（Exploitation）。
*   **超参数规律**：增大 $n$ 和 $m$ 通常能提升性能并增加熵，但过大的参数会导致熵不稳定。对于大模型，建议采用更保守的配置（如 $n=1, m=2$）。

### 7. 优点
*   **理论深刻**：从数学层面指出了现有软截断方法在对数空间发散的本质缺陷，并给出了概率空间的解决方案。
*   **机制优雅**：通过双侧解耦衰减，巧妙地解决了“探索-稳定性”这一强化学习中的经典矛盾。
*   **实战价值高**：在 DeepSeek-R1 这种最前沿的蒸馏模型上证明了有效性，且具有良好的扩展性（Scaling Law）。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在数学推理（可验证奖励）领域，在创意写作等主观奖励领域的表现尚待验证。
*   **算力限制**：受限于资源，实验最高只做到 14B 参数，未在 70B 或更大规模的基座模型上进行验证。
*   **超参数敏感性**：最优的 $n$ 和 $m$ 在不同规模模型上并不统一，仍需一定的调优经验。

（完）
