---
title: A Multi-Agent Perception-Action Alliance for Efficient Long Video Reasoning
title_zh: 一种用于高效长视频推理的多智能体感知-动作联盟
authors: "Yichang Xu, Gaowen Liu, Ramana Rao Kompella, Tiansheng Huang, Sihao Hu, Fatih Ilhan, Selim Furkan Tekin, Zachary Yahn, Ling Liu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14052v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于长视频推理的多智能体感知-动作联盟
tldr: 针对长视频推理效率低的问题，本文提出 A4VL 多智能体感知-行动联盟。该方法通过多轮感知-行动循环，利用多个 VLM 智能体提取查询相关的感知线索并定位关键视频块。通过智能体间的交叉评审与共识机制，动态调整推理过程，在显著降低推理延迟的同时，在多个基准测试上超越了现有主流模型，实现了高效且高质量的长视频问答。
selection_source: fresh_fetch
motivation: 旨在解决现有视觉语言模型在处理长视频推理时面临的计算效率低下和关键信息捕捉不准的问题。
method: 提出一种多智能体协作框架，通过感知探索定位关键视频块，并结合行动探索中的交叉评审与动态剪枝机制进行多轮迭代推理。
result: 在五个主流视频问答基准测试中，A4VL 的表现优于 18 种代表性 VLM 和 10 种长视频优化方法，且推理延迟大幅降低。
conclusion: A4VL 证明了多智能体协作与事件驱动的感知-行动循环能有效提升长视频推理的准确性与效率。
---

## 摘要
本文提出了一种名为 A4VL 的多智能体感知-动作探索联盟，用于高效的长视频推理。A4VL 在由选定的视觉语言模型（VLM）智能体组成的多轮感知-动作探索循环中运行。在每一轮中，智能体团队通过感知探索和随后的动作探索来执行视频问答（VideoQA）。在感知探索阶段，每个智能体学习从少量采样帧中提取特定于查询的感知线索，并执行基于线索的对齐，以找到与特定查询事件最相关的视频块。在动作探索阶段，A4VL 分三步进行视频推理：(1) 每个智能体生成其初始答案及理由；(2) 所有智能体通过交叉评审和相关性排序协作互评；(3) 根据是否达成满意的共识，决定是通过剪枝（例如过滤掉表现最差的智能体）和重组（例如基于新线索和匹配块的感知-动作探索）开始新一轮的感知-动作研讨，还是通过生成最终答案来结束。通过多轮感知-动作探索整合多智能体联盟，结合事件驱动的分区和线索引导的块对齐，使 A4VL 能够有效地扩展到现实世界的长视频，同时保持高质量的视频推理。在五个流行的 VideoQA 基准测试上的评估结果表明，A4VL 的性能优于 18 种现有的代表性 VLM 和 10 种近期针对长视频推理优化的方法，同时显著降低了推理延迟。我们的代码已发布在 https://github.com/git-disl/A4VL。

## Abstract
This paper presents a multi-agent perception-action exploration alliance, dubbed A4VL, for efficient long-video reasoning. A4VL operates in a multi-round perception-action exploration loop with a selection of VLM agents. In each round, the team of agents performs video question-answer (VideoQA) via perception exploration followed by action exploration. During perception exploration, each agent learns to extract query-specific perception clue(s) from a few sampled frames and performs clue-based alignment to find the video block(s) that are most relevant to the query-specific event. During action exploration, A4VL performs video reasoning in three steps: (1) each agent produces its initial answer with rational, (2) all agents collaboratively scores one another through cross-reviews and relevance ranking, and (3) based on whether a satisfactory consensus is reached, the decision is made either to start a new round of perception-action deliberation by pruning (e.g., filtering out the lowest performing agent) and re-staging (e.g., new-clue and matching block based perception-action exploration), or to conclude by producing its final answer. The integration of the multi-agent alliance through multi-round perception-action exploration, coupled with event-driven partitioning and cue-guided block alignment, enables A4VL to effectively scale to real world long videos while preserving high quality video reasoning. Evaluation Results on five popular VideoQA benchmarks show that A4VL outperforms 18 existing representative VLMs and 10 recent methods optimized for long-video reasoning, while achieving significantly lower inference latency. Our code is released at https://github.com/git-disl/A4VL.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：A4VL 多智能体感知-动作联盟

#### 1. 核心问题与整体含义（研究动机和背景）
长视频推理（Long Video Reasoning）面临两大挑战：一是**计算成本高昂**，随着视频帧数增加，多模态大模型（MLLM）的内存和时间开销呈平方级增长；二是**关键信息稀疏**，简单增加采样频率往往会引入冗余噪声，干扰模型对关键事件的定位。
本文提出了 **A4VL** 框架，旨在通过**多智能体协作**和**迭代式的感知-动作循环**，在极低的帧预算下实现高效且准确的长视频问答。其核心动机是模拟人类“先粗看定位、再细看推理、最后多方研讨”的认知过程。

#### 2. 方法论：核心思想与技术细节
A4VL 采用无需训练（Training-free）的架构，主要包含以下三个阶段：

*   **智能体组队（Agent Teaming）：** 从候选池中通过无监督方式筛选出在特定任务上表现最佳的 $m$ 个智能体（默认为3个），利用群体智慧提升基准性能。
*   **感知探索（Perception Exploration）：**
    *   **初步预览：** 每个智能体随机采样极少量帧（如4帧）生成“感知线索”（Perception Clue）。
    *   **事件驱动分区：** 利用 DINOv2 特征和像素级线索将视频划分为多个具有视觉一致性的“事件块”。
    *   **块对齐采样：** 使用 CLIP 计算感知线索与各视频块的相似度，根据得分动态分配采样配额（如16帧），确保后续推理聚焦于关键片段。
*   **动作探索（Action Exploration）：**
    *   **推理生成：** 智能体基于定位到的关键帧生成答案及理由。
    *   **交叉评审与剪枝：** 若智能体间未达成共识，则进行互评打分。剔除得分最低的智能体（剪枝），剩余智能体根据当前讨论精炼感知线索，进入下一轮迭代。
    *   **共识决策：** 最终由总结器（Summarizer）整合多轮讨论结果，输出最终答案。

#### 3. 实验设计
*   **数据集：** 涵盖了短、中、长不同维度的五个主流基准测试：
    *   **NeXT-QA**（短视频，侧重因果推理）
    *   **EgoSchema**（中等长度，约3分钟）
    *   **LongVideoBench、MLVU、Video-MME**（长视频，最长超过2小时）
*   **对比方法：** 共对比了 28 种方法，包括：
    *   **闭源模型：** GPT-4o, Gemini 1.5 Pro。
    *   **开源模型：** InternVL3.5, QwenVL-2.5, LLaVA-Video 等。
    *   **长视频优化/智能体方法：** VideoAgent, TraveLER, VideoRAG, BOLT 等。

#### 4. 资源与算力
*   **硬件配置：** 实验在 **6 张 NVIDIA H200 GPU** 上运行。
*   **训练时长：** 该方法为 **Training-free（无需训练）**，因此无训练时长。
*   **推理效率：** 在长视频数据集 MLVU 上，A4VL 的平均单样本推理时间为 74秒，显著优于 GPT-4o（127秒）和 InternVL3-78B（204秒）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 5 个数据集上进行了详尽的性能对比，并针对 EgoSchema 进行了多项消融实验。
*   **消融实验：** 验证了迭代轮数（1-3轮）、采样策略（随机 vs 事件驱动）、共识标准（多数 vs 全体）、以及剪枝机制的必要性。
*   **客观性：** 实验涵盖了不同参数规模的基座模型，并对比了最新的闭源 SOTA 模型，结果具有较高的说服力和公平性。

#### 6. 主要结论与发现
*   **性能领先：** A4VL 在所有五个基准测试中均取得了最高准确率。在 Video-MME 上，其平均准确率（77.2%）超过了 Gemini 1.5 Pro（75.0%）。
*   **效率优势：** 随着视频长度增加，A4VL 的推理延迟增长远比传统模型平缓，证明了其良好的扩展性。
*   **协作价值：** 实验证明多轮迭代和智能体剪枝能显著修正初始错误，尤其是在复杂逻辑推理任务中。

#### 7. 优点：亮点与创新
*   **即插即用：** 无需对模型进行微调，可直接适配各种开源或闭源的 MLLM。
*   **事件感知：** 引入事件驱动的分区算法，比简单的均匀分区更能捕捉视频的语义边界。
*   **动态剪枝：** 创新的“评审-剪枝”机制有效解决了多智能体协作中的冗余和冲突问题。
*   **线索引导：** 通过感知线索实现精准的帧分配，解决了长视频中的“大海捞针”问题。

#### 8. 不足与局限
*   **首轮共识风险：** 如果所有智能体在第一轮就达成了一个错误的共识，系统会提前退出并输出错误答案。
*   **时序理解限制：** 依赖 CLIP 进行块匹配，而 CLIP 本质上是图像-文本模型，对“静止”、“缓慢移动”等强时序动作的理解可能存在偏差。
*   **计算资源门槛：** 虽然推理高效，但同时运行多个大参数量智能体（如 72B 模型）对显存总量仍有一定要求。

（完）
