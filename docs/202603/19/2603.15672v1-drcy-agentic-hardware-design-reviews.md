---
title: "DRCY: Agentic Hardware Design Reviews"
title_zh: DRCY：基于智能体的硬件设计评审
authors: "Kyle Dumont, Nicholas Herbert, Hayder Tirmazi, Shrikanth Upadhayaya"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.15672v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于自主硬件设计审查的多智能体大模型系统
tldr: DRCY是一个基于多智能体大语言模型（LLM）的硬件设计审查系统，旨在解决传统EDA工具无法验证原理图语义正确性的问题。它能自动获取元器件数据手册，进行引脚级分析，并集成在CI/CD流程中。该系统已在AllSpice Hub生产环境部署，被多家大型硬件公司用于复杂设计审查，显著降低了因设计错误导致的流片返工成本。
selection_source: fresh_fetch
motivation: 传统的EDA工具仅能检查结构连接，无法验证原理图是否符合元器件数据手册的语义规范，导致昂贵的后期返工。
method: 采用五智能体流水线架构，结合具有自评估功能的自动化数据手册检索系统和多轮共识机制，实现自动化的引脚级分析。
result: DRCY已成功部署在AllSpice Hub平台，并被应用于车辆设计和太空探索等关键领域的硬件审查。
conclusion: DRCY证明了多智能体LLM系统在自动化复杂硬件设计审查、提高设计可靠性及降低研发成本方面的巨大潜力。
---

## 摘要
制造后发现的硬件设计错误需要昂贵的物理改版，可能导致产品延迟数月。现有的电子设计自动化（EDA）工具强制执行结构连接规则，但无法验证连接在组件数据手册方面是否在语义上正确。例如，符号的引脚排列是否符合制造商的规范，或者电压调节器的反馈电阻是否产生了预期的输出。我们提出了 DRCY，这是首个生产就绪的多智能体大语言模型（LLM）系统，它通过自主获取组件数据手册、根据提取的规范进行逐引脚分析，并将发现结果作为内联注释发布在设计评审中，从而实现了首轮原理图连接评审的自动化。DRCY 已在协作硬件设计平台 AllSpice Hub 上投入生产部署，作为在提交设计评审时触发的 CI/CD 操作运行。DRCY 被大型硬件公司定期用于从多智能体车辆设计到太空探索等各种用例。我们描述了 DRCY 的五智能体流水线架构、具有自我评估功能的智能体数据手册检索系统，以及用于提高安全关键型分析可靠性的多轮共识机制。

## Abstract
Hardware design errors discovered after fabrication require costly physical respins that can delay products by months. Existing electronic design automation (EDA) tools enforce structural connectivity rules. However, they cannot verify that connections are \emph{semantically} correct with respect to component datasheets. For example, that a symbol's pinout matches the manufacturer's specification, or that a voltage regulator's feedback resistors produce the intended output. We present DRCY, the first production-ready multi-agent LLM system that automates first-pass schematic connection review by autonomously fetching component datasheets, performing pin-by-pin analysis against extracted specifications, and posting findings as inline comments on design reviews. DRCY is deployed in production on AllSpice Hub, a collaborative hardware design platform, where it runs as a CI/CD action triggered on design review submissions. DRCY is used regularly by major hardware companies for use-cases ranging from multi-agent vehicle design to space exploration. We describe DRCY's five-agent pipeline architecture, its agentic datasheet retrieval system with self-evaluation, and its multi-run consensus mechanism for improving reliability on safety-critical analyses

---

## 论文详细总结（自动生成）

以下是对论文《DRCY: Agentic Hardware Design Reviews》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：印刷电路板（PCB）设计在制造前的评审是防止昂贵错误的最后防线。一旦设计进入制造阶段（流片），任何细微的连接错误（如引脚接反、电压不匹配）都会导致数周乃至数月的进度延迟，以及数万美元的返工成本。
*   **核心问题**：现有的电子设计自动化（EDA）工具仅能执行结构性检查（如电气规则检查 ERC 和设计规则检查 DRC），但无法验证连接的**语义正确性**。即：工具知道引脚连上了，但不知道这种连接是否符合元器件数据手册（Datasheet）的官方规范。
*   **整体含义**：本文介绍了 DRCY，这是首个投入生产环境的多智能体 LLM 系统，旨在通过自主检索数据手册并进行引脚级语义分析，实现硬件设计评审的自动化。

### 2. 论文提出的方法论
DRCY 采用了一个包含五个主要智能体的流水线架构，集成在 AllSpice Hub 的 CI/CD 流程中：
*   **网表增强（Netlist Augmentation）**：解析多种 EDA 格式（Altium, KiCad 等），将原理图转化为包含连接关系的结构化 XML。
*   **选择智能体（Selection Agent）**：将复杂的原理图划分为功能组（如电源级、微控制器核心、I/O 组），以避免超出 LLM 的上下文限制。
*   **智能体化数据手册检索（Agentic Datasheet Retrieval）**：
    *   **定位与提取**：自主从供应商 API 或客户库中寻找 PDF。
    *   **头部分析与提取**：识别关键页面（引脚说明、电气特性），并将其转化为紧凑的 XML 规范。
    *   **批判智能体（Critic Agent）**：对提取质量进行 10 分制评估，若分数过低则尝试其他来源，确保数据准确。
*   **分组评审与多轮共识（Group Review & Multi-Run Consensus）**：
    *   并行运行 $k$ 个评审智能体实例，利用 LLM 的非确定性进行“自洽性”校验。
    *   **共识智能体**：汇总 $k$ 次运行结果，保留高置信度发现，重新评估矛盾项。
*   **错误分组（Error Grouping）**：将具有相同根因的多个引脚错误合并为一个逻辑注释，避免信息冗余。

### 3. 实验设计
*   **应用场景**：DRCY 已在 AllSpice Hub 生产环境中部署，服务于财富 500 强企业、自动驾驶汽车公司和商业航天公司。
*   **Benchmark 与对比**：
    *   **对比对象**：传统的 ERC/DRC 工具（仅限结构检查）以及新兴的 AI 硬件工具（如 Cady, Cadstrom）。
    *   **测试案例**：论文展示了一个包含 10 页原理图、50-100 个唯一组件的真实硬件设计。
    *   **验证方式**：通过在设计中故意引入连接错误，演示 DRCY 如何自动识别、分析并给出带有数据手册引脚参考的推理链。

### 4. 资源与算力
*   **模型选择**：采用分层模型策略。**强模型**（如 Anthropic Claude 系列的高阶版本）负责逻辑推理和共识分析；**弱模型**负责机械性的数据手册提取和头部分析。
*   **算力细节**：文中未明确提及具体的 GPU 型号或训练时长，因为该系统主要基于现有的商业 LLM API 构建。
*   **运行效率**：一个典型的 10 页原理图分析通常在 **20 分钟内**完成。

### 5. 实验数量与充分性
*   **实验规模**：虽然论文没有像纯学术论文那样提供大规模的消融实验表格，但其“实验”建立在**生产环境的长期运行**之上。
*   **充分性评价**：系统支持多种主流 EDA 格式（Altium, OrCAD 等），并在消费电子、机器人、航天等多个垂直领域得到了验证。多轮共识机制（Multi-Run Consensus）的设计体现了对 LLM 可靠性问题的客观对待。

### 6. 论文的主要结论与发现
*   **语义验证可行性**：证明了 LLM 智能体能够理解复杂的硬件数据手册，并将其与原理图连接进行对比，发现传统工具无法察觉的语义错误。
*   **可靠性提升**：通过多智能体协作和多轮共识机制，显著降低了 LLM 的幻觉风险，使其能够处理安全关键型（Safety-critical）的硬件分析。
*   **效率变革**：将原本需要资深工程师数小时甚至数天的手动评审工作缩短至分钟级，且能作为 CI/CD 自动触发。

### 7. 优点
*   **端到端自动化**：从数据手册检索到最终的内联评审注释，实现了全流程闭环。
*   **高可靠性架构**：引入了 Critic Agent 进行数据质量把控，以及 Consensus Agent 进行结果校验。
*   **行业集成度高**：直接嵌入硬件工程师现有的版本控制和协作工作流（AllSpice Hub），而非孤立的工具。

### 8. 不足与局限
*   **依赖外部模型**：系统的核心推理能力高度依赖于底层 LLM（如 Anthropic 或 OpenAI）的性能。
*   **数据手册复杂性**：对于非标准格式、扫描质量极差或极其冗长（数百页）的数据手册，提取精度可能仍面临挑战。
*   **成本控制**：多轮运行（$k$ 次调用）虽然提高了准确性，但也成倍增加了 API 的 Token 消耗成本。
*   **缺乏公开 Benchmark**：由于涉及大量企业私有设计数据，论文未提供可供社区复现的标准化开源数据集。

（完）
