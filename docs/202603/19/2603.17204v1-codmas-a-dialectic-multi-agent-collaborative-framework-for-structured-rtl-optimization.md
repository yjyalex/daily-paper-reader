---
title: "CODMAS: A Dialectic Multi-Agent Collaborative Framework for Structured RTL Optimization"
title_zh: CODMAS：一种用于结构化 RTL 优化的辩证多智能体协作框架
authors: "Che-Ming Chang, Prashanth Vijayaraghavan, Ashutosh Jadhav, Charles Mackin, Vandana Mukherjee, Hsinyu Tsai, Ehsan Degan"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17204v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于结构化优化的辩证多智能体协作框架
tldr: CODMAS是一个针对RTL代码优化的多智能体协作框架，通过辩证推理、领域感知生成和确定性评估来提升芯片设计的PPA。它引入了Articulator和Hypothesis Partner等智能体进行逻辑推演，并配合编码与评估智能体实现自动化优化。在RTLOPT基准测试中，该框架显著降低了关键路径延迟和功耗，同时提高了代码的功能正确性。
selection_source: fresh_fetch
motivation: 传统的RTL优化过程复杂且易出错，需要一种能够结合结构化推理与领域知识的自动化方法来提升PPA。
method: 提出CODMAS框架，利用辩证智能体进行计划制定与结果预测，并结合领域特定编码智能体和评估智能体进行Verilog代码的迭代优化。
result: "在RTLOPT基准上，流水线优化使关键路径延迟降低约25%，门控时钟优化使功耗降低约22%，且功能失效显著减少。"
conclusion: 结构化的多智能体辩证推理能有效增强自动化RTL优化能力，并具备扩展至更复杂设计任务的潜力。
---

## 摘要
优化寄存器传输级（RTL）代码是电子设计自动化（EDA）中提高功耗、性能和面积（PPA）的关键步骤。我们提出了 CODMAS（通过辩证多智能体系统进行的协作优化），这是一个将结构化辩证推理与领域感知代码生成及确定性评估相结合的框架，旨在实现 RTL 优化的自动化。CODMAS 的核心是两个辩证智能体：受“小黄鸭调试法”启发的阐述者（Articulator），负责阐明逐步转换计划并揭示潜在假设；以及假设伙伴（Hypothesis Partner），负责预测结果并调和预期与实际行为之间的偏差，以指导有针对性的改进。这些智能体指导领域特定编码智能体（DCA）生成架构感知的 Verilog 编辑，并指导代码评估智能体（CEA）验证语法、功能和 PPA 指标。我们引入了 RTLOPT，这是一个包含 120 个 Verilog 三元组（未优化、已优化、测试平台）的基准测试集，用于流水线化和时钟门控转换。在专有和开源大语言模型（LLM）上，与强提示词和智能体基线相比，CODMAS 在流水线化方面实现了约 25% 的关键路径延迟降低，在时钟门控方面实现了约 22% 的功耗降低，同时减少了功能和编译失败。这些结果表明，结构化多智能体推理可以显著增强自动化 RTL 优化，并扩展到更复杂的设计和更广泛的优化任务。

## Abstract
Optimizing Register Transfer Level (RTL) code is a critical step in Electronic Design Automation (EDA) for improving power, performance, and area (PPA). We present CODMAS (Collaborative Optimization via a Dialectic Multi-Agent System), a framework that combines structured dialectic reasoning with domain-aware code generation and deterministic evaluation to automate RTL optimization. At the core of CODMAS are two dialectic agents: the Articulator, inspired by rubber-duck debugging, which articulates stepwise transformation plans and exposes latent assumptions; and the Hypothesis Partner, which predicts outcomes and reconciles deviations between expected and actual behavior to guide targeted refinements. These agents direct a Domain-Specific Coding Agent (DCA) to generate architecture-aware Verilog edits and a Code Evaluation Agent (CEA) to verify syntax, functionality, and PPA metrics. We introduce RTLOPT, a benchmark of 120 Verilog triples (unoptimized, optimized, testbench) for pipelining and clock-gating transformations. Across proprietary and open LLMs, CODMAS achieves ~25% reduction in critical path delay for pipelining and ~22% power reduction for clock gating, while reducing functional and compilation failures compared to strong prompting and agentic baselines. These results demonstrate that structured multi-agent reasoning can significantly enhance automated RTL optimization and scale to more complex designs and broader optimization tasks.

---

## 论文详细总结（自动生成）

### 论文总结：CODMAS —— 用于结构化 RTL 优化的辩证多智能体协作框架

#### 1. 核心问题与整体含义（研究动机和背景）
在集成电路设计中，寄存器传输级（RTL）代码的优化（针对功耗、性能和面积，即 PPA）是电子设计自动化（EDA）的关键环节。传统的优化依赖于资深工程师的经验，既耗时又容易出错。虽然现有的大语言模型（LLM）在代码生成方面表现出色，但在处理硬件描述语言（HDL）的结构化优化（如流水线化和时钟门控）时，往往缺乏对设计意图的深刻理解和架构层面的推理能力。论文的核心问题在于：**如何构建一个能够模拟人类专家推理过程、结合领域知识并实现闭环验证的自动化 RTL 优化框架。**

#### 2. 方法论：核心思想与技术细节
论文提出了 **CODMAS** 框架，其核心思想是利用“辩证推理”在多个专门的智能体之间形成反馈环路。
*   **辩证推理智能体（Dialectic Agents）：**
    *   **阐述者（Articulator）：** 受“小黄鸭调试法”启发，负责将复杂的优化目标分解为逐步的转换计划，并明确设计假设。
    *   **假设伙伴（Hypothesis Partner）：** 进行预测性推理，在代码生成前预估优化效果和潜在风险；当评估结果偏离预期时，负责诊断原因并调整策略。
*   **执行智能体（Executor Agents）：**
    *   **领域特定编码智能体（DCA）：** 通过“领域知识注入器（DKI）”获取数据流图（DFG）和专家提示词，生成架构感知的 Verilog 代码。
    *   **代码评估智能体（CEA）：** 利用确定性 EDA 工具（如 Icarus Verilog, Yosys）进行语法检查、功能仿真和 PPA 指标提取。
*   **算法流程：** 框架采用迭代优化模式。Articulator 制定计划 -> Hypothesis Partner 预测结果 -> DCA 生成代码 -> CEA 评估并反馈。如果未达到 PPA 目标或存在功能错误，辩证智能体将根据反馈修正计划和假设，进入下一轮迭代。

#### 3. 实验设计
*   **数据集：** 引入了新基准 **RTLOPT**，包含 120 个 Verilog 三元组（原始代码、优化参考代码、测试平台），涵盖算术、控制和混合模块。
*   **优化场景：** 专注于**流水线化（Pipelining）**和**时钟门控（Clock Gating）**。
*   **对比方法：** 
    *   **基础提示词：** Zero-Shot。
    *   **推理增强方法：** CoDes、ReAct、Reflexion。
    *   **专用框架：** LLM-VeriPPA。
*   **后端模型：** 测试了 GPT-4o, GPT-3.5-turbo, DeepSeek-v2.5, Llama-3, Granite-34B-Code, CodeLLaMA-34B。

#### 4. 资源与算力
*   **硬件环境：** 实验在一台配备 **64 核 CPU** 和 **NVIDIA A100 GPU** 的服务器上运行。
*   **训练/运行：** 论文未提及模型微调（Training），主要侧重于推理（Inference）和迭代优化过程。每组实验重复 5 次以确保统计稳定性。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 6 种 LLM 和 5 种基线方法在 120 个任务上进行了全面测试，总计进行了数百次优化迭代。
*   **消融实验：** 专门设计了消融实验来验证辩证智能体（DA）、领域知识注入（DKI）和评估智能体（CEA）的贡献。
*   **客观性：** 实验使用了配对 t 检验（p < 0.01）来验证改进的显著性，并对比了不同难度任务下的收敛速度。整体实验设计较为充分、客观，涵盖了从专有模型到开源模型的广泛对比。

#### 6. 主要结论与发现
*   **PPA 显著提升：** 在 GPT-4o 上，CODMAS 使流水线化的关键路径延迟降低了约 **25.5%**，使时钟门控的功耗降低了约 **21.7%**。
*   **可靠性增强：** 相比基线方法，CODMAS 显著降低了失败率（FR），将其控制在 **30% 以下**，而传统方法往往超过 40%-50%。
*   **结构化推理的价值：** 消融实验证明，将“计划”与“假设预测”分离的辩证结构比单一智能体或共享内存的多智能体系统更稳定、更高效。
*   **迭代收益：** 前 3 次迭代通常能获得最大的 PPA 收益，随后收益趋于平缓或出现波动。

#### 7. 优点
*   **创新的辩证框架：** 模拟了专家级的设计评审过程，通过“阐述-假设-验证”闭环提高了优化的逻辑性。
*   **领域知识深度集成：** 不仅仅依赖 LLM 的通用能力，还通过 DFG 提取和专家角色引导注入了硬件设计特有的约束。
*   **高质量基准：** 贡献了 RTLOPT 数据集，填补了 RTL 优化领域缺乏可重复、带 PPA 标注的基准测试的空白。

#### 8. 不足与局限
*   **优化类型有限：** 目前仅验证了流水线和时钟门控，尚未扩展到重定时（Retiming）、资源共享或状态机重构等更复杂的优化。
*   **规模扩展性：** 实验主要针对模块级设计，对于包含数百万门电路的大规模工业级 SoC 设计，EDA 工具的频繁调用可能面临算力和时间瓶颈。
*   **验证深度：** 依赖于测试平台（Testbench）仿真而非形式化等价性检查（LEC），在极端边缘情况下可能存在功能不一致的风险。
*   **数据集规模：** 120 个样本虽然在同类研究中已属可观，但对于全面覆盖工业界多样的编码风格仍显不足。

（完）
