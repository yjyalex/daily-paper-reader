---
title: "Prune as You Generate: Online Rollout Pruning for Faster and Better RLVR"
title_zh: 边生成边剪枝：面向更快更优 RLVR 的在线 Rollout 剪枝
authors: "Haobo Xu, Sirui Chen, Ruizhong Qiu, Yuchen Yan, Chen Luo, Monica Cheng, Jingrui He, Hanghang Tong"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24840v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过在线采样剪枝加速大模型推理任务的强化学习
tldr: 针对强化学习可验证奖励（RLVR）计算成本高且学习信号稀疏的问题，本文提出 arrol 方法。该方法在生成过程中利用在线训练的轻量级质量头预测部分采样的成功概率，动态剪枝低价值样本并平衡正确性分布。实验表明，arrol 在提升训练速度达 1.7 倍的同时，显著提高了模型在推理和测试时缩放的准确率。
selection_source: fresh_fetch
motivation: 现有的 RLVR 方法因需大量采样导致计算开销巨大，且样本奖励方差低导致学习信号微弱。
method: 提出在线采样剪枝框架 arrol，通过实时训练质量头预测部分生成的成功率，实现动态剪枝与样本重组。
result: "在 Qwen 和 LLaMA 模型上，该方法提升了约 2.5% 的平均准确率，加速训练达 1.7 倍，并显著增强了测试时缩放效果。"
conclusion: arrol 证明了在生成过程中进行在线剪枝不仅能大幅降低 RLVR 的计算成本，还能通过优化样本质量提升模型推理能力。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 显著提升了大语言模型 (LLM) 的推理能力。然而，GRPO 和 DAPO 等方法由于依赖于为每个提示词采样大量 rollout，面临着巨大的计算开销。此外，在 RLVR 中，相对优势往往是稀疏的：许多样本呈现出几乎全对或全错的状态，导致组内奖励方差较低，进而产生微弱的学习信号。在本文中，我们提出了 arrol (Accelerating RLVR via online Rollout Pruning)，这是一种在线 rollout 剪枝方法，它在生成过程中对 rollout 进行剪枝，同时显式地引导留存的样本在正确性上更加平衡，以增强学习信号。具体而言，arrol 实时训练一个轻量级的质量头 (quality head) 来预测部分 rollout 的成功概率，并据此做出早期剪枝决策。学习到的质量头还可以进一步对候选样本进行加权，以在测试时缩放 (test-time scaling) 期间提高推理准确性。为了提升效率，我们提出了一种系统设计，在推理引擎内部执行 rollout 剪枝，并对剩余样本进行重新批处理，以进行对数概率计算和策略更新。在基于 Qwen-3 和 LLaMA-3.2 模型 (1B-8B) 的 GRPO 和 DAPO 实验中，arrol 将平均准确率提升了 +2.30 至 +2.99，同时实现了高达 1.7 倍的训练加速，并在测试时缩放中获得了高达 +8.33 的额外平均准确率增益。代码已开源至 https://github.com/Hsu1023/ARRoL。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly advanced the reasoning capabilities of Large Language Models (LLMs). However, methods such as GRPO and DAPO suffer from substantial computational cost, since they rely on sampling many rollouts for each prompt. Moreover, in RLVR the relative advantage is often sparse: many samples become nearly all-correct or all-incorrect, yielding low within-group reward variance and thus weak learning signals. In this paper, we introduce arrol (Accelerating RLVR via online Rollout Pruning), an online rollout pruning method that prunes rollouts during generation while explicitly steering the surviving ones more correctness-balanced to enhance learning signals. Specifically, arrol trains a lightweight quality head on-the-fly to predict the success probability of partial rollouts and uses it to make early pruning decisions. The learned quality head can further weigh candidates to improve inference accuracy during test-time scaling. To improve efficiency, we present a system design that prunes rollouts inside the inference engine and re-batches the remaining ones for log-probability computation and policy updates. Across GRPO and DAPO on Qwen-3 and LLaMA-3.2 models (1B-8B), arrol improves average accuracy by +2.30 to +2.99 while achieving up to 1.7x training speedup, and yielding up to +8.33 additional gains in average accuracy in test-time scaling. The code is available at https://github.com/Hsu1023/ARRoL.

---

## 论文详细总结（自动生成）

### 论文总结：Prune as You Generate (ARRoL)

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**具有可验证奖励的强化学习（RLVR）**在提升大语言模型（LLM）推理能力时的效率与质量问题。
*   **研究动机**：当前的 RLVR 方法（如 DeepSeek 的 GRPO）虽然效果显著，但面临两大痛点：
    1.  **计算成本极高**：为了估计优势函数，每个 Prompt 需要生成大量采样（Rollouts），导致训练极其耗时。
    2.  **学习信号稀疏/微弱**：在二元奖励（0/1）场景下，如果一组采样全部正确或全部错误，组内方差为零，会导致梯度消失或信号极弱。
*   **核心目标**：开发一种在线剪枝机制，在生成过程中提前终止低价值采样，既能加速训练，又能通过优化样本平衡度来增强学习信号。

#### 2. 方法论：核心思想与关键技术
论文提出了 **ARRoL** (Accelerating RLVR via online Rollout Pruning) 框架，核心思想是“边生成边剪枝”：
*   **轻量级质量头 (Quality Head)**：在模型骨干网络上添加一个简单的 2 层 MLP 探测头。它在训练过程中实时学习，利用中间隐藏层状态预测当前部分生成序列（Partial Rollout）最终成功的概率。
*   **在线剪枝策略**：当生成达到预设长度（如 512 token）时，质量头对所有采样打分。系统根据预设的**保留率 ($\kappa$)** 和**目标平衡度 ($\rho$)** 做出剪枝决策。
*   **样本平衡机制**：ARRoL 不仅仅是剪掉差的样本，而是通过调整生存概率，使留存的样本组在正确与错误之间更加平衡（趋近 0.5 的通过率），从而最大化奖励方差，提供更强的学习信号。
*   **系统级优化**：
    *   **后端（vLLM）**：在推理引擎内部执行剪枝，立即释放显存和计算资源给其他活跃序列。
    *   **前端（verl）**：接收剪枝掩码，对幸存样本进行重新批处理（Re-batching），减少后续对数概率计算和反向传播的开销。
*   **测试时缩放 (Test-time Scaling)**：训练好的质量头在推理阶段可作为加权投票的权重，优于传统的多数投票（Majority Vote）。

#### 3. 实验设计
*   **模型系列**：Qwen-3 (1.7B, 4B, 8B) 和 LLaMA-3.2 (1.7B)。
*   **数据集**：
    *   **训练集**：Dapo-Math-17K。
    *   **评估集**：Math500, MinervaMath, OlympiadBench（综合数学）；AMC’23, AIME’24, AIME’25（高难度竞赛题）。
*   **对比基准 (Benchmarks)**：
    *   **训练算法**：Vanilla GRPO 和 DAPO。
    *   **推理策略**：Majority Voting 和 DeepConf（基于 Logits 启发式置信度的剪枝方法）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA GH200 GPUs**。
*   **训练配置**：最大序列长度 8,192，组大小（Group Size）为 16，学习率 $1 \times 10^{-6}$。
*   **时长/效率**：论文明确指出实现了 **1.6x 到 1.7x 的端到端训练加速**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 不同规模的模型，并在 6 个主流数学推理榜单上进行了验证。
*   **消融实验**：
    *   对比了 ARRoL 与随机剪枝的效果，证明了基于质量预测剪枝的优越性。
    *   分析了不同保留率 ($\kappa$) 对性能和速度的影响。
    *   验证了质量头得分与最终正确性的相关性（Spearman 相关系数）。
*   **评价**：实验设计较为全面，不仅关注最终精度，还深入分析了系统层面的加速来源和训练过程中的奖励分布变化，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：ARRoL 在 GRPO 和 DAPO 基础上，平均准确率提升了 **+2.30% 至 +2.99%**。
*   **显著加速**：通过在线剪枝和前端重组，训练速度提升了约 **70%**。
*   **测试时增益**：在测试时缩放（TTS）场景下，利用质量头进行加权投票，相比 DeepConf 获得了高达 **+8.33%** 的额外准确率提升。
*   **信号增强**：实验证实 ARRoL 确实增加了组内奖励方差，缓解了 RLVR 训练中的信号稀疏问题。

#### 7. 优点
*   **端到端集成**：不同于以往只在生成后做下采样的策略，ARRoL 深入推理引擎内部，实现了真正的生成阶段加速。
*   **“少即是多”**：证明了减少采样数量但优化采样质量（平衡度）可以同时获得更快的速度和更好的模型性能。
*   **多功能性**：质量头既能用于训练剪枝，又能用于推理加权，实现了模型能力的“一鱼两吃”。

#### 8. 不足与局限
*   **检测长度依赖**：剪枝依赖于中间检测长度 $L_{detect}$（实验设为 512）。如果模型在极短长度内就出错，或者需要极长序列才能显现质量差异，该超参数的设定可能需要精细调整。
*   **任务局限性**：目前主要验证了数学推理（可验证奖励明确）任务，在创意写作或开放式对话等奖励模型（RM）不稳定的场景下，质量头的预测准确性可能受限。
*   **冷启动依赖**：训练初期需要 20 步左右的冷启动来初始化质量头，这在极短周期的微调任务中可能占比过高。

（完）
