---
title: "Reasoning Topology Matters: Network-of-Thought for Complex Reasoning Tasks"
title_zh: 推理拓扑至关重要：面向复杂推理任务的思维网络 (Network-of-Thought)
authors: Fan Huang
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20730v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于复杂推理任务的网络思维 (NoT)
tldr: 针对现有大模型推理范式（如CoT和ToT）在处理复杂任务时拓扑结构受限的问题，本文提出Network-of-Thought (NoT) 框架。NoT将推理过程建模为有向图，支持合并中间结果和整合多源证据。实验表明，NoT在多跳推理和逻辑推理任务中显著优于树状结构，并在多个主流模型上刷新了GSM8K和HotpotQA的性能表现，证明了推理拓扑结构对复杂任务的重要性。
selection_source: fresh_fetch
motivation: 现有的线性或树状推理结构难以应对需要合并信息、回溯假设和整合多源证据的复杂推理场景。
method: 提出NoT框架，将推理过程构建为包含类型化节点和边的有向图，并由基于启发式策略的控制器引导搜索。
result: "NoT在HotpotQA多跳推理任务上优于ToT，并在Qwen2.5-72B模型上取得了91.7%的最高准确率。"
conclusion: 推理拓扑结构对模型性能有显著影响，图状结构在处理复杂逻辑和多源信息整合任务时具有明显优势。
---

## 摘要
现有的提示范式在有限的拓扑结构中构建大语言模型（LLM）的推理：思维链（CoT）产生线性轨迹，而思维树（ToT）执行分支搜索。然而，复杂推理通常需要合并中间结果、重新审视假设以及整合来自多个来源的证据。我们提出了思维网络（Network-of-Thought, NoT），这是一个将推理建模为具有类型化节点和边的有向图的框架，并由基于启发式的控制器策略引导。在四个基准测试（GSM8K、24点游戏、HotpotQA、ProofWriter）和三个模型（GPT-4o-mini、Llama-3.3-70B-Instruct、Qwen2.5-72B-Instruct）上，我们研究了网络拓扑何时优于链式或树状结构，LLM生成的启发式方法是否能引导基于图的推理搜索，以及不同拓扑结构下的计算-准确率权衡，并从准确率、拓扑简洁性和 token 效率方面评估了每种方法。我们的结果表明，对于 GPT-4o-mini 的顺序任务，CoT 仍然有效（GSM8K 上为 89.5%），而在多跳推理上，NoT 超过了 ToT（在 HotpotQA 上使用 LLM-as-Judge 评估为 91.0% 对 88.0%）。在 72B 开源模型中，NoT 在 GSM8K 上达到了最高准确率（91.5%），而 Qwen2.5-72B 在多跳问答中取得了整体最佳结果（HotpotQA 上为 91.7%）。自生成的控制器启发式方法在逻辑推理上优于固定和随机策略，其中仅基于不确定性的加权在 ProofWriter 上达到了 57.0%。我们还发现评估方法显著影响方法排名：字符串匹配低估了所有在开放式问答中的方法，其中 NoT 的差距最大，这一模式在所有三个模型中都是一致的（在 HotpotQA 上有 14-18 个百分点的差距）。

## Abstract
Existing prompting paradigms structure LLM reasoning in limited topologies: Chain-of-Thought (CoT) produces linear traces, while Tree-of-Thought (ToT) performs branching search. Yet complex reasoning often requires merging intermediate results, revisiting hypotheses, and integrating evidence from multiple sources. We propose Network-of-Thought (NoT), a framework that models reasoning as a directed graph with typed nodes and edges, guided by a heuristic-based controller policy. Across four benchmarks (GSM8K, Game of 24, HotpotQA, ProofWriter) and three models (GPT-4o-mini, Llama-3.3-70B-Instruct, Qwen2.5-72B-Instruct), we investigate when network topology outperforms chain or tree structures, whether LLM-generated heuristics can guide graph-based reasoning search, and the computation-accuracy tradeoff across topologies, evaluating each method on accuracy, topology simplicity, and token efficiency. Our results show that CoT remains effective for sequential tasks with GPT-4o-mini (89.5\% on GSM8K), while NoT surpasses ToT on multi-hop reasoning (91.0\% vs.\ 88.0\% on HotpotQA with LLM-as-Judge). With 72B open-source models, NoT achieves the highest accuracy on GSM8K (91.5\%), and Qwen2.5-72B achieves the best multi-hop QA result overall (91.7\% on HotpotQA). Self-generated controller heuristics outperform fixed and random strategies on logical reasoning, with uncertainty-only weighting achieving 57.0\% on ProofWriter. We also find that evaluation methodology significantly impacts method rankings: string-match underestimates all methods on open-ended QA, with the largest gap for NoT, a pattern consistent across all three models (14--18 percentage point gap on HotpotQA).

---

## 论文详细总结（自动生成）

以下是对论文《Reasoning Topology Matters: Network-of-Thought for Complex Reasoning Tasks》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 推理范式在拓扑结构上存在局限性。思维链（CoT）是线性的，思维树（ToT）是分支搜索的，但现实中的复杂推理往往需要**合并中间结果、回溯假设、整合多源证据**。
*   **研究动机**：作者识别了现有拓扑的三大局限：
    1.  **推理复用问题**：树状结构在处理共享中间结论时必须重复节点，而图结构可以合并。
    2.  **推理循环问题**：假设修正和迭代规划需要链和树无法表达的环状结构。
    3.  **多源依赖问题**：某些决策需要同时整合多个独立输入，这在图结构中最为自然。
*   **整体含义**：提出 **Network-of-Thought (NoT)** 框架，将推理泛化为有向图，旨在探索推理拓扑结构如何影响不同类型任务的性能。

### 2. 论文提出的方法论
*   **核心思想**：将推理过程建模为一个包含类型化节点和边的有向图 $G = (V, E)$，并引入一个由启发式策略引导的控制器（Controller）来决定下一步扩展哪个节点。
*   **关键技术细节**：
    *   **节点与边类型**：节点分为事实（Fact）、子目标（Subgoal）、约束（Constraint）和结论（Conclusion）；边分为依赖（Depends_on）、支持（Supports）、推导（Derives）和矛盾（Contradicts）。
    *   **控制器策略**：使用评分函数选择待扩展节点：$Score(v) = w_u \cdot 不确定性 + w_d \cdot 依赖度 + w_c \cdot 冲突度$。
    *   **自生成启发式（Self-generated Heuristics）**：不再手动调参，而是让 LLM 预先生成上述权重（$w_u, w_d, w_c$），实现任务自适应。
*   **算法流程**：
    1.  **初始化**：LLM 生成初始推理图并进行后处理（如限制初始置信度、确保连通性）。
    2.  **迭代推理**：控制器根据评分选择节点，LLM 扩展该节点并更新图结构，直到结论节点置信度达标或达到步数上限。
    3.  **答案提取**：通过反向 BFS 追踪推理链，由 LLM 综合最终答案。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**（数学推理）：代表线性链式任务。
    *   **Game of 24**（搜索推理）：代表树状分支任务。
    *   **HotpotQA**（多跳问答）：代表多源依赖网络任务。
    *   **ProofWriter**（逻辑推理）：代表需要假设修正的网络任务。
*   **对比方法**：CoT（思维链）、ToT（思维树，BFS 搜索）。
*   **实验模型**：GPT-4o-mini（主实验）、Llama-3.3-70B-Instruct、Qwen2.5-72B-Instruct（跨模型验证）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和训练时长，因为该研究主要基于**提示工程（Prompting）和推理阶段的搜索**，而非模型训练。
*   **资源消耗**：
    *   使用了 OpenAI API（GPT-4o-mini）和本地部署的开源模型（通过 vLLM 运行）。
    *   总计进行了约 **27,000 次 LLM 调用**。
    *   提供了详细的 **Token 效率分析**：例如在 HotpotQA 上，NoT 平均消耗约 15,254 tokens，远低于 ToT 的 44,298 tokens。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集抽取 100-300 个样本，总计 900 个测试实例。
*   **充分性分析**：
    *   **多维度评估**：不仅对比了准确率，还分析了拓扑简洁性（节点/边数量）和 Token 效率。
    *   **消融实验**：对控制器权重进行了详细消融，验证了“仅不确定性”和“自生成权重”的有效性。
    *   **跨模型验证**：在三个不同家族的大模型上复现，证明了结论的普适性。
    *   **扩展实验**：包括硬数学题（IMO）、多智能体并行化扩展以及 1.5B-7B 小模型的性能评估。
*   **客观性**：引入了 **LLM-as-Judge** 评估，解决了字符串匹配在开放式问答中低估复杂推理方法的问题，评估视角较为公正。

### 6. 论文的主要结论与发现
*   **拓扑匹配原则**：推理拓扑应与任务结构匹配。CoT 在顺序数学任务（GSM8K）上依然最强；NoT 在多跳问答（HotpotQA）上显著优于 ToT。
*   **模型规模效应**：在 72B 模型上，NoT 在 GSM8K 上反超 CoT（91.5%），说明大模型能更好地利用复杂的图结构。
*   **评估方法的影响**：字符串匹配会严重低估 NoT 的表现（在 HotpotQA 上差距达 18%），语义评估更能反映真实推理质量。
*   **自生成启发式的优越性**：LLM 自主生成的控制器权重在逻辑推理任务中优于固定权重和随机策略。

### 7. 优点
*   **理论框架完备**：提出了清晰的推理拓扑分类法（Chain $\subset$ Tree $\subset$ Network）。
*   **灵活性高**：NoT 能够自然地处理推理复用和多源依赖，比 ToT 更节省资源且更高效。
*   **深入的结构分析**：利用网络科学工具（如小世界系数、社区检测）分析了复杂问题的推理图，揭示了推理过程中的“枢纽节点”和“社区结构”。

### 8. 不足与局限
*   **循环推理缺失**：尽管框架支持环状结构，但在实际实验中，LLM 并未自发产生循环推理（Loop frequency 为 0），说明目前的提示策略尚不足以激发复杂的假设修正循环。
*   **早熟收敛问题**：LLM 倾向于在初始化时给出过高的置信度，导致推理过早停止，需要人工设定阈值和最小步数来缓解。
*   **成本问题**：虽然比 ToT 便宜，但 NoT 的 Token 消耗仍远高于 CoT，在简单任务上性价比不高。
*   **Judge 依赖**：语义评估高度依赖于作为裁判的 LLM（GPT-4o-mini）的质量。

（完）
