---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 具有可验证奖励的强化学习用于推理
tldr: 本研究针对大语言模型在强化学习（RLVR）中面临的“硬样本”探索效率低下、奖励信号稀疏的问题，提出了P^2O框架。该框架将提示词优化与策略优化相结合，利用遗传帕累托算法（GEPA）为硬样本进化出高效提示词，引导模型发现成功轨迹。通过将提示词带来的推理增益直接蒸馏至模型参数，P^2O为模型提供了更密集的监督信号，显著提升了模型在复杂推理任务中的收敛速度和泛化性能。
selection_source: fresh_fetch
motivation: 针对强化学习在处理复杂推理样本时，因奖励极其稀疏导致模型难以获得有效监督信号的问题。
method: 提出P^2O框架，利用遗传帕累托算法优化提示词以引导模型探索成功路径，并将其推理增益蒸馏至策略参数中。
result: "实验表明P^2O在分布内任务表现优异，并在分布外基准测试中实现了平均4.7%的显著性能提升。"
conclusion: P^2O通过协同提示词与策略优化，有效解决了硬样本的探索难题，增强了大语言模型的推理能力与泛化性。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，尤其是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但模型实际上却缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示词优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。这种机制为困难样本提供了更密集的正向监督信号，并加速了收敛。大量实验表明，P^2O 不仅在分布内数据集上取得了卓越性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在具有可验证奖励的强化学习（RLVR）中，模型常面临**探索瓶颈**。当训练集中包含“硬样本”（模型在多次尝试中成功率为零）时，由于奖励极其稀疏，模型无法获得有效的梯度反馈，导致其在简单样本上过拟合，而在复杂推理任务上停滞不前。
*   **研究背景**：现有的课程学习或奖励塑造方法通常需要昂贵的人工启发式规则。而提示词优化（Prompt Optimization）虽然能激发模型的潜在能力，但通常仅限于推理阶段，模型参数本身并未真正掌握这些能力。

### 2. 方法论：P^2O 框架
P^2O 采用**交替优化**的策略，将提示词视为动态潜变量，与策略参数共同进化：
*   **核心思想**：利用优化的提示词作为“桥梁”，引导模型跳出局部最优并发现硬样本的成功路径，随后通过蒸馏机制将这些路径内化到模型参数中。
*   **关键技术细节**：
    1.  **硬样本挖掘**：在训练迭代中，识别成功率低于阈值 $\tau$ 的样本。
    2.  **上下文蒸馏（Context Distillation）**：这是 P^2O 的关键。在训练时，模型使用“优化提示词 + 原始输入”生成成功轨迹，但在计算策略梯度更新参数时，**仅以原始输入为条件**。这迫使模型在不依赖提示词的情况下内化推理逻辑。
    3.  **进化提示词优化（GEPA）**：
        *   **反射进化**：利用一个“反射 LLM”分析失败案例，生成针对性的提示词变体。
        *   **帕累托选择（Pareto Selection）**：维护一个多样化的提示词池，确保能覆盖不同类型的错误模式。
        *   **贪婪分配**：为每个硬样本分配最适合的提示词模板，以在下一轮 RL 中提供引导。

### 3. 实验设计
*   **数据集**：从 DeepScaler 和 DeepMath 中各抽取 5,000 个样本作为训练集（DeepMath 仅选难度 $\ge 7$ 的样本）。
*   **基准测试（Benchmarks）**：涵盖 6 个数学推理任务，包括 AIME24、AIME25、AMC、MATH500、MINERVA 和 OLYMPIAD。
*   **对比方法**：
    *   基础模型：Qwen3-0.6B/1.7B/4B。
    *   强化学习基准：标准的 GRPO（Group Relative Policy Optimization）算法。
    *   P^2O 变体：自反射（Self-Ref）和教师反射（Teacher-Ref，使用 Kimi-K2 作为反射模型）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了全局 Batch Size 为 128，最大生成长度 12k tokens，学习率 $1 \times 10^{-6}$，训练 5 个 Epoch。由于使用了 GRPO，该方法在显存占用上比 PPO 更具优势。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的训练分布上进行了实验，并在 6 个主流基准上验证。
*   **消融实验**：
    *   验证了**上下文蒸馏**的必要性（若直接在带提示词的输入上训练，模型会产生依赖，导致推理性能大幅下降）。
    *   验证了**提示词多样性**的影响（证明了帕累托集合比单一提示词效果更好）。
*   **充分性评价**：实验设计较为全面，涵盖了分布内和分布外（OOD）的泛化性测试，对比了不同能力的反射模型，论证过程客观且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：P^2O 在所有基准上均优于 GRPO。在极难的 AIME 任务上，相比 GRPO 平均提升了 **12.3%**。
*   **内化效应**：通过上下文蒸馏，模型成功将提示词诱导的推理能力转化为自身参数的一部分，在推理阶段无需任何额外提示词即可实现高性能。
*   **反射源的影响**：在某些任务中，使用更强的外部教师模型（Teacher-Ref）效果更好；但在特定领域，模型自反射（Self-Ref）可能更契合自身的推理节奏。

### 7. 优点
*   **解决冷启动问题**：无需人工编写复杂的 Hint 或思维链模板，自动发现硬样本的解法。
*   **训练效率高**：通过精准打击硬样本，避免了在简单样本上的无效迭代。
*   **泛化性强**：实验证明该方法不仅提升了训练集相关的任务，对未见过的复杂数学竞赛题也有显著提升。

### 8. 不足与局限
*   **计算开销**：虽然节省了人工，但 GEPA 进化过程涉及大量的采样和反射模型调用，增加了训练初期的计算负担。
*   **任务限制**：目前仅在具有**可验证奖励**（如数学、代码）的任务上验证，对于主观性较强、缺乏客观验证机制的任务（如创意写作）适用性尚待研究。
*   **反射模型依赖**：提示词进化的质量高度依赖于反射模型的分析能力。

（完）
