---
title: Enhancing reasoning accuracy in large language models during inference time
title_zh: 提升大语言模型在推理阶段的推理准确率
authors: "Vinay Sharma, Manish Jain"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21301v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 提高推理准确性的推理时技术
tldr: "本研究探讨了在不进行额外训练的情况下，通过推理时技术提升大语言模型（LLM）的多步推理准确性。研究系统评估了自我一致性、双模型推理一致性及自我反思三种策略，并结合思维链（CoT）提示词进行实验。结果显示，通过核采样和温度控制实现的自我一致性效果最显著，准确率提升了9%至15%，为不同风险场景下的推理优化提供了实践指导。"
selection_source: fresh_fetch
motivation: 针对大语言模型在多步推理任务中表现不稳定且难以通过推理时干预提升准确性的问题展开研究。
method: 系统对比了基于随机解码的自我一致性、双模型推理比对以及模型自我反思三种推理时优化策略。
result: "实验表明自我一致性策略在准确率上比贪婪解码提升了9%至15%，而自我反思在小型模型上的效果有限。"
conclusion: 自我一致性是低风险领域最高效的提升手段，而双模型方法适用于对可靠性要求更高的中等风险场景。
---

## 摘要
大语言模型（LLMs）通常表现出强大的语言能力，但在多步推理任务上仍然不够可靠，特别是在不进行额外训练或微调的情况下部署时。在这项工作中，我们研究了提高 LLMs 推理准确率的推理时（inference-time）技术。我们系统地评估了三类推理时策略：(i) 通过随机解码实现的自一致性（self-consistency），即使用受控的温度（temperature）和核采样（nucleus sampling）对模型进行多次采样，并选择出现频率最高的最终答案；(ii) 双模型推理一致性，即比较两个独立模型的输出，并仅信任一致的推理轨迹；以及 (iii) 自反思（self-reflection），即模型对自身的推理进行批判和修正。在所有评估的方法中，我们都采用了思维链（CoT）提示，以在生成最终答案之前引导出显式的中间推理步骤。在这项工作中，我们在相同的提示和验证设置下，对三种推理时策略进行了受控的对比评估。我们在 LLM 上的实验表明，结合核采样和受控温度值的自一致性带来了显著的提升，相比贪婪单次解码（greedy single-pass decoding），其准确率实现了 9% 到 15% 的绝对提升，非常适合低风险领域，以极小的开销提供了显著的收益。双模型方法为模型的推理步骤提供了额外的确认，因此更适用于中等风险领域，在这些领域中，更高的可靠性证明了额外计算开销的合理性。自反思仅带来了微小的改进，这表明该方法在推理时对于较小的非推理模型效果有限。

## Abstract
Large Language Models (LLMs) often exhibit strong linguistic abilities while remaining unreliable on multi-step reasoning tasks, particularly when deployed without additional training or fine-tuning. In this work, we study inference-time techniques to improve the reasoning accuracy of LLMs. We systematically evaluate three classes of inference-time strategies: (i) self-consistency via stochastic decoding, where the model is sampled multiple times using controlled temperature and nucleus sampling and the most frequent final answer is selected; (ii) dual-model reasoning agreement, where outputs from two independent models are compared and only consistent reasoning traces are trusted; and (iii) self-reflection, where the model critiques and revises its own reasoning. Across all evaluated methods, we employ Chain-of-Thought (CoT) [1] prompting to elicit explicit intermediate reasoning steps before generating final answers. In this work, we provide a controlled comparative evaluation across three inference-time strategies under identical prompting and verification settings. Our experiments on LLM [2] show that self-consistency with nucleus sampling and controlled temperature value yields the substantial gains, achieving a 9% to 15% absolute improvement in accuracy over greedy single-pass decoding, well-suited for low-risk domains, offering meaningful gains with minimal overhead. The dual-model approach provides additional confirmation for model reasoning steps thus more appropriate for moderate-risk domains, where higher reliability justifies additional compute. Self-reflection offers only marginal improvements, suggesting limited effectiveness for smaller non-reasoning models at inference time.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Reasoning Accuracy in Large Language Models During Inference Time》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）在语言任务上表现出色，但在处理需要多步逻辑推理的任务时仍然不够可靠。
*   **核心问题**：如何在不修改模型参数、不进行额外训练或微调的前提下，仅通过推理时（Inference-time）技术（如提示词工程、解码策略、输出聚合等）来提升模型的推理准确性。
*   **背景**：现有的微调方法成本高且需要访问模型权重，而推理时策略更具灵活性和低成本优势。

### 2. 论文提出的方法论
论文系统评估了三种推理时策略，均结合了**思维链（CoT）**提示：
*   **受控随机解码的自我一致性（Self-Consistency + Stochastic Decoding）**：
    *   **核心思想**：对同一问题进行多次采样，通过多数投票确定最终答案。
    *   **技术细节**：设置温度 $T=0.8$ 和核采样 $p=0.9$ 以增加路径多样性；采样次数 $n=6$；使用低温度（$T=0.1$）模型提取最终答案，并由一个 LLM 评判器进行语义一致性投票（而非简单的字符串匹配）。
*   **双模型推理与跨模型验证（Dual-Model Reasoning）**：
    *   **核心思想**：利用两个架构不同的独立模型对同一问题求解。
    *   **技术细节**：两个模型独立生成 CoT 路径和答案；由一个独立的验证者模型对比两者结论。若一致则接受（ACCEPT），不一致则拒绝（REJECT）。该方法侧重于通过模型间的交叉验证来识别错误。
*   **迭代批判与修正的自我反思（Self-Reflection）**：
    *   **核心思想**：模型对自己的初始方案进行自我检查和修正。
    *   **技术细节**：流程为“生成初始方案 -> 提示模型寻找逻辑漏洞或错误 -> 根据批判意见生成修正后的最终方案”。

### 3. 实验设计
*   **数据集**：使用了 HuggingFace 上的 **Logical Reasoning Improvement Dataset (Open-Platypus)**。该数据集包含科学、数学、编程等多个领域的确定性逻辑问题。
*   **基准（Benchmark）**：以**贪婪单次解码（Greedy Single-pass Decoding）**作为主要对比基准。
*   **对比实验**：
    *   自我一致性：对比了“随机采样+多数投票”与“贪婪解码”的效果。
    *   双模型验证：对比了单模型准确率与双模型一致性后的准确率/可靠性。
    *   自我反思：对比了反思前后的准确率变化。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）或推理总时长。由于该研究聚焦于推理时技术且不涉及模型训练，其算力消耗主要体现在多次采样和多模型调用的推理开销上。

### 5. 实验数量与充分性
*   **实验规模**：论文对三种策略分别进行了对比实验，并提供了详细的饼图展示接受（正确）与拒绝（错误）的比例。
*   **充分性评价**：
    *   **客观性**：实验在相同的提示词和验证设置下进行，具有较好的受控对比性。
    *   **局限性**：采样次数（$n=6$）相对较小；仅在一个主要数据集（Open-Platypus）上进行了验证，缺乏在更广泛基准（如 GSM8K 或 MATH）上的泛化性测试。

### 6. 论文的主要结论与发现
*   **自我一致性效果最显著**：结合随机采样和多数投票的策略比贪婪解码实现了 **9%–15% 的绝对准确率提升**（从 56.2% 提升至 64.9%）。
*   **双模型验证适用于高风险场景**：虽然它不一定能大幅提升原始准确率，但能有效过滤不可靠的输出，提高系统的**精确率（Precision）**，适合金融、医疗等容错率低的领域。
*   **自我反思在小模型上乏力**：对于非专门针对推理优化的较小模型，自我反思仅带来了约 **3.4%** 的微小提升（从 47.2% 提升至 50.6%），表明该策略可能更依赖模型本身的规模或基础能力。

### 7. 优点
*   **实用性强**：提出的方法不需要访问模型权重，非常适合通过 API 调用闭源模型的开发者。
*   **分类指导**：根据风险等级（低、中、高）推荐不同的推理策略，具有很强的工程实践指导意义。
*   **语义化投票**：在自我一致性中引入 LLM 评判器进行语义对齐，比传统的字符串匹配更鲁棒。

### 8. 不足与局限
*   **模型描述模糊**：文中多次提到“LLM”或“较小的非推理模型”，但未明确指出具体使用的模型名称（如 Llama-3-8B, GPT-4o 等）。
*   **成本分析缺失**：虽然提到双模型和多次采样会增加延迟和成本，但缺乏定量的成本-效益分析。
*   **实验覆盖面窄**：仅使用了一个数据集，且采样次数 $n$ 的消融实验不够深入（通常自我一致性会测试 $n=40$ 或更高以观察饱和点）。

（完）
