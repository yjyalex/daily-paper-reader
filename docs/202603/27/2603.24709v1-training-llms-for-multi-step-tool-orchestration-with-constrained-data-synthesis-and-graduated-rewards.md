---
title: Training LLMs for Multi-Step Tool Orchestration with Constrained Data Synthesis and Graduated Rewards
title_zh: 通过受限数据合成与分级奖励训练大语言模型进行多步工具编排
authors: "Cheng Jiayang, Xin Liu, Zhihan Zhang, Haoyang Wen, Zixuan Zhang, Qingyu Yin, Shiyang Li, Priyanka Nigam, Bing Yin, Chao Zhang, Yangqiu Song"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24709v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 采用分级奖励的强化学习用于多步编排
tldr: 针对大语言模型在多步工具编排中面临的参数错误和缺乏中间反馈等挑战，本文提出了一种结合受限数据合成与分级奖励的强化学习框架。通过构建基于真实API响应缓存的环境，高效合成复杂任务轨迹，并设计了涵盖原子有效性与编排逻辑的分级奖励机制。实验表明，该方法在ComplexFuncBench上显著提升了模型执行多步工具调用的准确性。
selection_source: fresh_fetch
motivation: 现有的多步工具调用环境过于简单且缺乏对部分正确性的反馈，导致模型在处理复杂依赖关系时容易出错。
method: 提出了一种利用真实API响应缓存进行高效数据合成的方法，并设计了结合原子调用准确性与整体编排逻辑的分级奖励函数。
result: 在ComplexFuncBench基准测试中，该方法显著提高了多步工具调用的准确率，且消融实验证明了各奖励组件的必要性。
conclusion: 通过精细化的数据合成与分级奖励机制，可以有效训练大语言模型处理复杂的多步工具编排任务。
---

## 摘要
多步工具编排要求大语言模型（LLM）按正确顺序调用多个相互依赖的 API 并传递中间输出，这仍然是一项挑战。最先进的模型在执行完整序列时经常失败，其中参数值错误占失败原因的很大一部分。训练模型处理此类工作流面临两个障碍：现有环境侧重于使用模拟数据的简单单轮函数调用，且二元奖励无法为部分正确性提供信号。我们提出了一个解决这两个挑战的框架。首先，我们构建了一个由大规模真实 API 响应缓存支持的强化学习环境，从而实现了一个数据合成流水线，该流水线可以采样具有可控复杂度的有效多步编排轨迹，且生成效率显著高于无约束方法。其次，我们提出了一种分级奖励设计，将正确性分解为原子有效性（在不断增加的粒度下单个函数调用的正确性）和编排（遵循依赖关系的正确工具排序）。在 ComplexFuncBench 上，我们的方法在轮次准确率方面表现出显著提升。消融实验证实，这两个奖励组件都是必不可少的：单独使用其中任何一个都会显著降低性能。

## Abstract
Multi-step tool orchestration, where LLMs must invoke multiple dependent APIs in the correct order while propagating intermediate outputs, remains challenging. State-of-the-art models frequently fail on full sequence execution, with parameter value errors accounting for a significant portion of failures. Training models to handle such workflows faces two obstacles: existing environments focus on simple per-turn function calls with simulated data, and binary rewards provide no signal for partial correctness.   We present a framework addressing both challenges. First, we construct a reinforcement learning environment backed by a large-scale cache of real API responses, enabling a data synthesis pipeline that samples valid multi-step orchestration traces with controllable complexity and significantly higher generation efficiency than unconstrained methods. Second, we propose a graduated reward design that decomposes correctness into atomic validity (individual function call correctness at increasing granularity) and orchestration (correct tool sequencing with dependency respect). On ComplexFuncBench, our approach demonstrates substantial improvements in turn accuracy. Ablation studies confirm both reward components are essential: using either alone significantly degrades performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Training LLMs for Multi-Step Tool Orchestration with Constrained Data Synthesis and Graduated Rewards》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：虽然大语言模型（LLM）在单步工具调用上取得了进展，但在需要多个 API 相互依赖、参数传递的“多步编排”任务中表现依然较差。
*   **核心痛点**：
    1.  **参数错误主导失败**：在复杂任务中，模型往往能选对工具，但无法推断出精确的参数值（如 ID、日期等）。
    2.  **训练环境缺陷**：现有环境多基于模拟数据，缺乏真实 API 响应的一致性（例如第一步返回的酒店 ID 在第二步查询中可能失效）。
    3.  **奖励信号稀疏**：传统的二元奖励（成功/失败）无法为部分正确的推理路径提供反馈，导致模型难以在长序列任务中收敛。

### 2. 论文提出的方法论
该论文提出了一个系统性的强化学习（RL）框架，包含三个核心组件：
*   **确定性缓存环境**：构建了一个包含 10 万条以上真实 API 响应的缓存库。通过预定义的工作流模板（Workflow Templates）确保 API 之间的依赖链（如坐标、ID 传递）在逻辑上是真实且一致的。
*   **受限数据合成（Constrained Data Synthesis）**：
    *   **反向索引采样**：利用缓存构建“函数-参数-值”的索引，根据模板骨架反向采样有效的 API 调用序列。
    *   **回显校验机制**：让 LLM 根据采样到的参数生成自然语言查询，并要求模型在 JSON 中回显参数以确保查询与底层数据的一致性。
*   **分级奖励设计（Graduated Rewards）**：将总奖励 $R_{total}$ 分解为两个部分：
    1.  **原子奖励 ($R_{atomic}$)**：评估单个调用的正确性。包括 **AST 校验**（函数名、参数结构、精确值匹配的分级评分）和**语义校验**（调用是否能在环境中成功执行并返回有效数据）。
    2.  **编排奖励 ($R_{orch}$)**：评估全局逻辑。检查模型是否按照依赖图（Dependency Graph）要求的顺序执行了所有必要步骤。

### 3. 实验设计
*   **数据集与基准**：主要在 **ComplexFuncBench** 上进行评估，该基准涵盖酒店、航班、租车、景点和出租车 5 个领域，平均每条查询需 3.26 步操作。
*   **对比方法**：
    *   **Zero-shot**：原始模型直接推理。
    *   **SFT (Supervised Fine-Tuning)**：在合成的地面真值轨迹上进行监督微调。
    *   **GRPO (Group Relative Policy Optimization)**：本文采用的强化学习算法。
*   **评估设置**：
    *   **Oracle 设置**：在评估集上直接训练以建立性能上限。
    *   **泛化设置**：在不相交的合成数据上训练，测试模型对未见查询的泛化能力。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：使用了 `verl` 强化学习框架。批量大小（Batch Size）为 16，生成大小（Rollout Size）为 16。KL 散度系数设为 0.001。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到在 100 步（Steps）左右模型性能趋于稳定。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Qwen3-8B 和 Qwen2.5-7B 两个基座模型进行了完整测试。
    *   进行了**消融实验**，分别验证了仅使用原子奖励、仅使用编排奖励以及组合奖励的效果。
    *   进行了**扩展性分析**，测试了不同合成数据量（从 67 到 383 个样本）对性能的影响。
    *   进行了**分层分析**，按依赖深度（2-5 层）和逻辑模式（线性、扇出、条件分支）拆解性能。
*   **充分性评价**：实验设计较为全面，不仅关注最终指标，还通过训练动力学曲线解释了“原子崩溃”和“编排崩溃”现象，客观地证明了分级奖励的必要性。

### 6. 论文的主要结论与发现
*   **RL 显著优于 SFT**：在多步编排任务中，SFT 容易导致模型过拟合于特定模板，甚至增加函数选择错误；而 RL 通过探索和分级反馈，显著提升了轮次准确率（Turn Acc）。
*   **分级奖励是核心**：只奖励单个调用会导致模型失去全局逻辑；只奖励全局逻辑会导致模型生成语法正确但语义错误（无法执行）的参数。两者结合才能达到最优。
*   **高数据效率**：模型对工作流结构的捕捉非常快，少量的合成样本（如每个模板 3-5 个）即可达到性能平台，增加样本量收益递减。
*   **复杂任务增益更大**：随着依赖深度增加，RL 相比 Zero-shot 的相对提升更加明显。

### 7. 优点
*   **环境真实性**：通过真实 API 缓存解决了模拟数据带来的“逻辑幻觉”问题。
*   **奖励密集化**：将复杂的编排任务拆解为可学习的原子步骤，解决了强化学习在工具调用中的稀疏奖励难题。
*   **合成流水线高效**：受限合成方法保证了生成数据的 100% 可执行性，避免了无效数据的干扰。

### 8. 不足与局限
*   **模板依赖**：数据合成仍依赖于预先定义的 100 多个工作流模板，对于完全未知的全新编排模式，泛化能力可能受限。
*   **跨领域挑战**：实验显示跨领域任务（Cross-domain）的准确率仍远低于单领域任务，说明模型在处理异构 API 组合时仍有较大提升空间。
*   **过拟合风险**：训练与验证指标之间存在一定的间隙，表明模型在合成分布上存在轻微过拟合。

（完）
