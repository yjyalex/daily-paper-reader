---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 利用强化学习激励外部化推理和自适应计算
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过引入“早退”机制和强化学习训练，促使大语言模型将推理过程外部化。通过在中间层设置退出点，模型被训练在预测简单词元时尽早结束计算，从而减少内部隐式计算的冗余。这种方法能有效引导模型将复杂的规划和推理过程转化为可见的外部词元，为提升推理透明度和效率提供了新思路。
selection_source: fresh_fetch
motivation: 旨在减少大语言模型在内部激活中进行非近视规划的冗余计算，促使其将推理过程显式化。
method: 在Transformer架构中引入中间层早退机制，并结合校准阶段与强化学习，激励模型在保证性能的前提下尽早退出。
result: 在小型推理模型上的初步实验表明，该方法能使模型根据词元难度自适应地减少计算量。
conclusion: 该架构调整能有效优化计算资源分配，并有望在大规模应用中强制模型通过外部词元而非内部隐式状态进行复杂推理。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLMs）成为更详尽的推理者。我们在现有的 Transformer 架构中增加了中间层的早停（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们针对小型推理模型提供了初步结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

以下是对论文《A transformer architecture alteration to incentivise externalised reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在生成思维链（CoT）时，往往会在内部激活层中进行“隐式推理”或“非近视规划”，导致外部输出的推理步骤与实际内部逻辑不一致（不忠实）或被刻意隐藏（模糊化）。
*   **研究动机**：为了提高模型的可监控性，研究者希望强制模型将复杂的推理过程“外部化”到可见的 token 中。如果模型在预测简单 token 时能尽早退出计算，就能减少其利用冗余内部算力进行隐式规划的空间，从而迫使复杂逻辑必须通过显式的 CoT 步骤完成。

### 2. 方法论
论文提出了一种结合架构修改与两阶段训练的流水线：
*   **架构改进**：
    *   在 Transformer 的中间层（每 5 层）添加浅层“早退头”（Early-exit heads）。
    *   使用 LoRA 适配器对现有模型权重进行微调。
    *   **早退机制**：模型在每一层随机采样是否退出。若退出，则直接将当前残差流传递给输出层，跳过后续所有层。
*   **关键技术流程**：
    1.  **阶段一：自蒸馏校准（Self-distillation）**：
        *   将原始全深度模型作为教师，带早退头的模型作为学生。
        *   利用中间层预测与最终层预测之间的 **KL 散度** 来衡量该层是否“足以”预测下一个 token。
        *   通过交叉熵损失训练学生模型，使其在 KL 散度较低的层学习到较高的退出概率。
    2.  **阶段二：强化学习激励（RL）**：
        *   使用改进的 **RLOO** 算法。
        *   **奖励函数**：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
        *   通过 $\lambda$ 参数调节“早退”的积极程度，激励模型在保证任务准确率（$R_{task}$）的前提下，尽可能减少使用的层数。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学文字题推理。
    *   **Theory of Mind (ExploreToM)**：心理理论推理任务。
*   **实验模型**：
    *   DeepSeek-R1-Distill-Qwen-1.5B（用于阶段一验证）。
    *   Qwen3-4B（用于完整的 RL 训练验证）。
*   **对比基准（Benchmark）**：
    *   未修改的原始基座模型（Base Model）。
    *   仅经过 SFT 校准但未进行 RL 的模型。
*   **评估指标**：任务准确率（Accuracy）、回答连贯性（Coherence）、平均计算量（使用的层数比例）、总计算量（CoT 长度 × 平均层数）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   **模型规模**：实验主要集中在 1.5B 和 4B 参数量的小型推理模型上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 RL 训练进行了 5 次独立运行以观察稳定性。
    *   每个训练步使用 60 个 prompt 进行评估。
    *   在自蒸馏阶段测试了 5 种不同的 KL 因子（0.25 到 4.0）以寻找性能平衡点。
*   **充分性评价**：作为“概念验证”（Proof of Concept）研究，实验涵盖了训练动力学分析和 token 级别的可视化。但由于模型规模较小且任务领域较窄（仅限数学和心理推理），其在通用大规模模型上的泛化能力尚待进一步验证。

### 6 主要结论与发现
*   **自适应计算**：模型成功学会了根据 token 难度调整计算深度。例如，在预测高度可预测的 token（如单词后缀）时，模型会显著提前退出。
*   **性能平衡**：通过 RL 训练，模型在减少约 5%-10% 的层计算量的同时，任务准确率反而有所提升（从 47% 提升至 55-60%），且保持了较好的语言连贯性。
*   **可调控性**：通过调节 $\lambda$ 参数，可以在“推理外部化压力”与“生成质量”之间进行权衡。

### 7. 优点
*   **创新视角**：不同于传统的早退机制（旨在提速），本文将其作为一种**安全工具**，旨在通过限制内部算力来强制推理透明化。
*   **端到端训练**：结合了自蒸馏和强化学习，使早退决策不再是硬编码的阈值，而是模型自主学习的随机策略。
*   **推理效率与透明度双赢**：初步证明了减少内部冗余计算不一定会损害性能，反而可能优化推理路径。

### 8. 不足与局限
*   **忠实度验证缺失**：论文虽然提出了“外部化推理”的目标，但尚未通过实验量化证明这种方法是否真的提高了 CoT 的忠实度（Faithfulness）。
*   **规模限制**：实验仅在 4B 以下模型进行，尚未在大规模前沿模型（如 70B+）上验证其有效性。
*   **质量损失风险**：过于激进的早退（低 KL 因子）会导致连贯性大幅下降，寻找最优超参数具有挑战性。
*   **任务局限**：目前仅在结构化推理任务上测试，对于开放式创作或多轮对话的影响尚不明确。

（完）
