---
title: "Train at Moving Edge: Online-Verified Prompt Selection for Efficient RL Training of Large Reasoning Model"
title_zh: 在移动边缘训练：面向大推理模型高效强化学习训练的在线验证提示词选择
authors: "Jiahao Wu, Ning Lu, Shengcai Liu, Kun Wang, Yanting Yang, Li Qing, Ke Tang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25184v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 大推理模型的高效强化学习训练
tldr: 针对大语言模型推理任务中强化学习训练开销巨大的问题，本文提出了一种名为HIVE的提示词选择框架。研究发现高价值样本集中在难度适中且不确定性高的“学习边缘”，且该边缘随训练动态变化。HIVE通过历史奖励轨迹进行粗选，并利用实时提示词熵进行精选，显著提升了训练效率，在保持性能的同时大幅减少了计算资源消耗。
selection_source: fresh_fetch
motivation: 现有的强化学习训练中大量提示词产生的梯度极小，导致计算资源在低价值样本的Rollout阶段被严重浪费。
method: 提出HIVE框架，结合历史奖励轨迹的粗筛选与基于提示词熵的实时在线验证，动态捕捉处于“学习边缘”的高价值样本。
result: 在多个数学推理基准测试中，HIVE在不损失模型性能的前提下，显著提高了Rollout阶段的计算效率。
conclusion: 通过动态选择处于学习边缘的高效提示词，可以大幅优化大推理模型在强化学习阶段的训练效率。
---

## 摘要
强化学习（RL）已成为大语言模型（LLM）在推理任务中进行后训练的关键手段。虽然扩大采样（rollouts）规模可以稳定训练并提升性能，但计算开销是一个关键问题。在 GRPO 等算法中，每个提示词进行多次采样会产生极高的成本，因为很大一部分提示词提供的梯度微乎其微，效用较低。为了解决这一问题，我们研究了如何在采样阶段之前选择高价值的提示词。我们的实验分析表明，样本效用是不均匀且不断演变的：最强的学习信号集中在“学习边缘”（learning edge），即中等难度与高不确定性的交汇处，且这一边缘随着训练的进行而发生偏移。受此启发，我们提出了 HIVE（基于历史信息和在线验证的提示词选择），这是一个用于数据高效强化学习的双阶段框架。HIVE 利用历史奖励轨迹进行粗选，并采用提示词熵作为实时代理，以剔除效用过时的实例。通过在多个数学推理基准测试和模型上评估 HIVE，我们证明了 HIVE 在不损失性能的情况下显著提高了采样效率。

## Abstract
Reinforcement learning (RL) has become essential for post-training large language models (LLMs) in reasoning tasks. While scaling rollouts can stabilize training and enhance performance, the computational overhead is a critical issue. In algorithms like GRPO, multiple rollouts per prompt incur prohibitive costs, as a large portion of prompts provide negligible gradients and are thus of low utility. To address this problem, we investigate how to select high-utility prompts before the rollout phase. Our experimental analysis reveals that sample utility is non-uniform and evolving: the strongest learning signals concentrate at the ``learning edge", the intersection of intermediate difficulty and high uncertainty, which shifts as training proceeds. Motivated by this, we propose HIVE (History-Informed and online-VErified prompt selection), a dual-stage framework for data-efficient RL. HIVE utilizes historical reward trajectories for coarse selection and employs prompt entropy as a real-time proxy to prune instances with stale utility. By evaluating HIVE across multiple math reasoning benchmarks and models, we show that HIVE yields significant rollout efficiency without compromising performance.

---

## 论文详细总结（自动生成）

### 论文总结：在移动边缘训练（HIVE 框架）

#### 1. 核心问题与研究动机
在大型推理模型（LRM）的强化学习（RL）训练中（如使用 GRPO 算法），为了稳定训练和提升性能，通常需要对每个提示词（Prompt）进行大规模的采样（Rollout）。然而，这种方法存在严重的计算资源浪费：
*   **低效样本占比高**：大量提示词对于当前模型而言要么太简单（始终正确），要么太难（始终错误），导致优势函数（Advantage）为零，无法提供有效的梯度信号。
*   **动态性挑战**：样本的效用随训练进程不断变化（即“学习边缘”在移动）。
*   **现有方法局限**：传统的静态筛选无法适应模型演进；而基于历史数据的筛选（如 GRESO）容易因模型更新导致数据“陈旧”；实时采样筛选（如 Dynamic Sampling）则本身就带有极高的采样开销。

#### 2. 方法论：HIVE 框架
论文提出了 **HIVE (History-Informed and online-VErified prompt selection)**，一个双阶段的层次化筛选框架：

*   **核心思想**：将筛选分为“零成本的历史粗选”和“低成本的在线精选”，精准捕捉处于中等难度和高不确定性交汇处的“学习边缘”样本。
*   **阶段 1：历史信息筛选（History-Informed Selection）**
    *   **奖励得分**：统计提示词在最近迭代中连续出现零方差（全对或全错）的次数，次数越多，被选中的概率越低。
    *   **历史熵得分**：利用上一轮采样产生的响应熵（Response Entropy）来衡量模型的不确定性，优先选择高熵样本。
    *   **自适应机制**：引入自适应探索概率，根据训练过程中无信息样本的比例动态调整筛选强度。
*   **阶段 2：在线验证筛选（Online-Verified Selection）**
    *   **提示词熵作为代理**：论文从理论上证明了“提示词熵”（Prompt Entropy，即输入序列的平均 Token 熵）与“响应熵”之间存在**秩一致性（Rank Consistency）**。
    *   **低成本验证**：在正式 Rollout 前，仅需一次前向传播计算提示词熵，通过中位数阈值（Median-based Threshold）剔除那些效用已过时的样本。
*   **算法流程**：从题库采样 -> 历史指标粗选 -> 当前模型计算提示词熵精选 -> 正式 Rollout 采样 -> GRPO 策略更新 -> 更新历史缓冲区。

#### 3. 实验设计
*   **模型覆盖**：Qwen2.5-Math (1.5B/7B/14B/32B)、DeepSeek-R1-Distill-Qwen-1.5B、Llama-3.2-3B-Instruct。
*   **数据集**：DAPO+MATH (DM) 混合数据集、OPEN-R1 30k (OR1) 子集。
*   **Benchmark**：包含 Math500、AIME24、AMC、Gaokao、Minerva Math、Olympiad Bench 等六个主流数学推理基准。
*   **对比方法**：
    *   **Dynamic Sampling (DS)**：实时采样并过滤零方差样本。
    *   **GRESO**：基于历史训练动态的筛选方法。

#### 4. 资源与算力
*   **硬件环境**：主要实验在 **8 × NVIDIA A100-80G GPU** 集群上完成。
*   **训练时长与效率**：
    *   以 Qwen2.5-Math-7B 为例，HIVE 将总训练时间从 198.4 小时（DS 方法）缩短至 **85.8 小时**，加速约 **2.2 倍**。
    *   在 Rollout 环节，HIVE 实现了高达 **3.8 倍** 的墙钟时间加速。
    *   在 1000 个训练步长中，HIVE 相比 DS 最多减少了 **920 万次** Rollout 采样。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 种不同规模和架构的模型上进行了验证，涵盖了从 1.5B 到 32B 的参数区间。
*   **消融实验**：专门对比了“仅阶段 1”、“仅阶段 2”以及完整 HIVE 的表现，证明了双阶段协同的必要性。
*   **充分性评价**：实验设计非常充分。不仅关注最终的准确率，还深入分析了训练过程中的梯度范数分布、零方差比例演变、提示词熵的理论验证以及推理案例分析。对比基准（DS 和 GRESO）均为当前该领域的最先进方法，实验结果具有高度的说服力和客观性。

#### 6. 主要结论与发现
*   **学习边缘理论**：最强的学习信号集中在中等难度和高响应熵的交集区域，且该区域随模型能力提升而移动。
*   **提示词熵的有效性**：提示词熵是响应熵的高效且可靠的代理指标，能够以极低的计算成本（单次前向传播）解决历史数据陈旧的问题。
*   **高效性与性能并存**：HIVE 在大幅减少计算开销的同时，在所有测试模型上均保持或略微超过了全量采样或基准筛选方法的推理准确率。

#### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了启发式算法，还通过数学证明和实证分析支撑了“提示词熵代理”的合理性。
*   **极高的工程价值**：在不改变 RL 核心算法（如 GRPO）的前提下，通过数据侧的优化实现了翻倍的训练加速，对资源受限的团队非常友好。
*   **自适应性强**：通过动态阈值和自适应概率，减少了人工调参的负担。

#### 8. 不足与局限
*   **任务领域局限**：目前仅在数学推理任务（文本 LRM）上进行了验证，尚未探讨该原则是否适用于多模态模型（VLM）或代码生成等其他领域。
*   **超参数依赖**：虽然有自适应机制，但框架仍引入了如平衡系数 $\lambda$ 和步长 $\Delta p$ 等超参数，论文未展示这些参数在极端规模下的鲁棒性。
*   **代理指标的噪声**：虽然提示词熵与响应熵高度相关，但在某些极端长文本或特定分布下，这种相关性可能会减弱，导致筛选偏差。

（完）
