---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验蒸馏为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于自动技能生成的并行子智能体群
tldr: Trace2Skill是一个旨在提升大语言模型智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易碎片化的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并将其分层整合为统一、无冲突的技能目录。实验证明，Trace2Skill在电子表格、视觉问答和数学推理等任务中表现优异，且生成的技能具有极强的跨模型迁移能力和泛化性，无需参数更新即可显著提升智能体性能。
selection_source: fresh_fetch
motivation: 现有的智能体技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能脆弱且难以泛化。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将局部经验分层整合为统一、无冲突的声明式技能目录。
result: "在多个复杂领域显著超越强基线，且由较小模型生成的技能能使更大模型在WikiTableQuestions上提升高达57.65%。"
conclusion: 复杂的智能体经验可以被封装为高度可迁移的声明式技能，无需参数微调即可实现跨模型和跨场景的泛化。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于应对复杂任务至关重要。然而，人工编写技能面临严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一挑战，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，然后将其蒸馏为单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将它们分层整合到一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基线有显著提升。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的陈述式技能——无需参数更新，无需外部检索模块，且仅需使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### 论文总结：Trace2Skill —— 将轨迹局部经验蒸馏为可迁移的智能体技能

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效、自动化地为大语言模型（LLM）智能体构建高质量、可迁移且具备泛化能力的领域技能（Skills）？
*   **研究动机**：
    *   **人工瓶颈**：手动编写领域技能（如操作指南、SOP）难以扩展。
    *   **现有自动方法缺陷**：现有的自动生成方法要么仅依赖 LLM 的参数化知识（缺乏领域细节），要么采用“在线顺序更新”模式，容易导致技能碎片化、检索困难，且容易过拟合于特定的局部轨迹，缺乏人类专家那种“全局分析后总结”的整体性。
    *   **可迁移性需求**：探索经验是否可以脱离特定模型和任务实例，转化为通用的陈述式知识。

#### 2. 方法论
Trace2Skill 模仿人类专家的技能编写过程，采用“先广泛收集经验，再并行分析，最后统一蒸馏”的策略。其流程分为三个阶段：
*   **阶段 1：轨迹生成（Trajectory Generation）**
    *   让固定参数的智能体在任务集上运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
*   **阶段 2：并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**
    *   派遣大量子智能体并行处理轨迹。
    *   **成功分析师（Success Analyst）**：识别有助于成功的通用模式。
    *   **错误分析师（Error Analyst）**：采用 **ReAct 风格的智能体循环**，通过检查文件、对比真值来诊断失败根因，并验证修复方案，从而提出高质量的技能补丁（Patch）。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**
    *   **分层合并**：将所有补丁通过 LLM 进行分层合并，利用归纳推理识别在多个轨迹中重复出现的“普遍模式”。
    *   **程序化校验**：通过冲突检测和格式检查，确保最终生成的技能目录（包含主文档 `SKILL.md` 和辅助资源）逻辑自洽且无格式错误。
*   **两种模式**：支持“技能深化”（在人类编写的基础上改进）和“从零创建”（从 LLM 随机生成的草稿开始演化）。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格操作**：SpreadsheetBench（验证集、Soft/Hard 分数）。
    *   **分布外（OOD）泛化**：WikiTableQuestions（将维基百科表格转为电子表格格式）。
    *   **数学推理**：DAPO-Math、AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（处理表单、发票等图像）。
*   **对比基线**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的专家编写技能。
    *   **Parametric**：仅靠 LLM 自身知识生成的初始技能。
    *   **ReasoningBank**：基于检索的经验库方法（Ouyang et al., 2026）。
    *   **顺序更新（Sequential Editing）**：模拟在线学习的逐条更新模式。
*   **实验模型**：主要使用 Qwen3.5-122B 和 Qwen3.5-35B。

#### 4. 资源与算力
*   **算力平台**：使用了 8 卡 A800 节点。
*   **耗时示例**：
    *   **轨迹生成**：使用 122B 模型生成 200 条轨迹（每条 50+ 步）耗时小于 2 GPU 小时。
    *   **技能整合**：并行模式下仅需约 3 分钟，而传统的顺序更新模式（Seq-B=1）在相同任务量下需 60 分钟，效率提升 20 倍。
*   **模型规模**：证明了 35B 规模的开源模型即可胜任技能演化任务，无需依赖闭源巨型模型。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个截然不同的领域（办公自动化、数学、多模态视觉），并在每个领域都进行了跨模型（35B 演化给 122B 用，反之亦然）和跨分布（ID vs OOD）的测试。
*   **消融实验**：对比了并行与顺序更新、单一技能文档与检索式经验库、智能体化错误分析与普通 LLM 分析。
*   **客观性**：所有实验均取三个随机种子平均值，并使用了官方评测指标。实验设计较为严谨，充分验证了方法在不同维度下的有效性。

#### 6. 主要结论与发现
*   **极强的迁移性**：由 35B 模型生成的技能可以显著提升 122B 模型的表现。在 WikiTableQuestions 上，35B 演化的技能让 122B 智能体提升了 **57.65%**。
*   **超越人类专家**：在某些设定下，自动演化的技能优于 Anthropic 官方的专家技能，因为后者可能不完全契合特定模型的“脾气”。
*   **并行优于顺序**：并行整合不仅快，而且通过归纳推理过滤了偶然性错误，生成的技能更稳健。
*   **陈述式知识胜过检索**：将经验固化为单一技能文档的效果优于在推理时动态检索相似案例。
*   **分析能力与执行能力解耦**：在 DocVQA 实验中发现，执行任务强的模型（35B）不一定写技能强，而 122B 模型展现了更强的归纳总结（作者身份）能力。

#### 7. 优点
*   **高效性**：通过并行化显著降低了技能演化的时间成本。
*   **通用性**：生成的技能是纯文本/代码目录，不依赖特定架构，可直接注入任何智能体的系统提示词中。
*   **鲁棒性**：通过“智能体化错误分析”确保了补丁的质量，避免了 LLM 常见的幻觉诊断。
*   **开源友好**：证明了中等规模开源模型在智能体自演化路径上的巨大潜力。

#### 8. 不足与局限
*   **因果量化缺失**：目前无法精确衡量每一个补丁对最终性能提升的边际贡献。
*   **动态修剪不足**：尚未实现根据推理时的实际需求动态修剪或激活技能章节的功能（目前是全量注入）。
*   **领域覆盖**：虽然涵盖了三个领域，但对于更具动态性的环境（如实时交互网页、机器人控制）尚未验证。
*   **偏差风险**：如果初始轨迹池存在严重的系统性偏差，演化出的技能可能会固化这些错误。

（完）
