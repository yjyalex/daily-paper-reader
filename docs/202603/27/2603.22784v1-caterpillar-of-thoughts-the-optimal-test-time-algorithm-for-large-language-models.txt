Title: Caterpillar of Thoughts: The Optimal Test-Time Algorithm for Large Language Models

URL Source: https://arxiv.org/pdf/2603.22784v1

Published Time: Wed, 25 Mar 2026 00:36:00 GMT

Number of Pages: 15

Markdown Content:
# Caterpillar of Thoughts: The Optimal Test-Time Algorithm for Large Language Models 

Amir Azarmehr * 1 Soheil Behnezhad * 1 Alma Ghafari * 1 

# Abstract 

Large language models (LLMs) can often pro-duce substantially better outputs when allowed to use additional test-time computation, such as sam-pling, chain of thought, backtracking, or revising partial solutions. Despite the growing empirical success of such techniques, there is limited theo-retical understanding of how inference time com-putation should be structured, or what constitutes an optimal use of a fixed computation budget. We model test-time computation as an algorithm interacting with a Markov chain: at any point, the algorithm may resume generation from any previously observed state. That is, unlike stan-dard Markov chains where the states are drawn “passively”, we allow the algorithm to “backtrack” to any previously observed state of the Markov chain at any time. Many of the existing test-time algorithms such as Chain-of-Thought (CoT) (Wei et al., 2023), Tree-of-Thoughts (ToT) (Yao et al., 2023), or Best-of-

k (Brown et al., 2024) could be seen as specific algorithms in this model. We prove that while backtracking can reduce the number of generations exponentially, a very lim-ited form of backtracking is theoretically suffi-cient. Namely, we show that the optimal algo-rithm always generates a “caterpillar” tree. That is, if we remove the leaves of the state tree gener-ated by the optimal algorithm, we obtain a path. Motivated by our characterization of the optimal algorithm, we present Caterpillar of Thoughts (CaT), a new test-time computation algorithm, reducing the number of token/state generations. Our empirical evaluation shows that CaT, com-pared to ToT, achieves a better success rate while also reducing the number of token generations.  

> *Equal contribution 1Northeastern University

# 1. Introduction 

Recent advances in large language models have demon-strated that performance can often be improved not by ad-ditional training, but by allocating more computation at inference time. Techniques such as self consistency (Wang et al., 2023), tree-of-thought exploration (Yao et al., 2023), iterative refinement (Madaan et al., 2023), recursive self-aggregation (Venkatraman et al., 2025), verifier guided search (Lightman et al., 2023; Botta et al., 2025; Snell et al., 2024; Wang et al., 2025), and backtracking (von R ¨utte et al., 2025; Yang et al., 2025) allow an LLM to sample multiple continuations, revisit earlier partial solutions, and selectively pursue promising directions. These methods have proven effective across various reasoning, planning, and creative generation tasks, suggesting that inference time computation plays a central role in enabling more capable and adaptive language agents. Despite this progress, the algorithmic foundations of test-time computation remain poorly understood. Most existing approaches are motivated heuristically, and it is unclear how close they are to optimal, or how performance should scale with additional inference-time resources. More fundamen-tally, given a fixed but non-trivial budget, what is the best possible way to deploy test-time compute? 

1.1. Our Contributions A Theoretical Model of Test-Time Computation: To answer the question above, we need a theoretical model for test-time computation. We model this problem as exploring a (massive) Markov chain, which is defined based on a pretrained LLM. Each state in the Markov chain represents a partial solution , i.e., a sequence of tokens. From any state, the LLM induces a distribution over successor states via sampling new tokens. That is, the Markov chain randomly transitions to a new state, corresponding to the solution obtained by appending the new tokens to the current partial solution. Crucially, modern inference time methods allow the algorithm to rewind: rather than continuing only from the most recently generated state, it may resume generation from any previously observed state. This leads naturally to the model of a fully observable 1                               

> arXiv:2603.22784v1 [cs.LG] 24 Mar 2026 Caterpillar of Thoughts
> x0x1x2· · · xn
> D
> pppp
> 1−p1−p1−p1
> 1
> . . .
> Figure 1. A Markov chain where rewinding is crucial, consisting of n+ 1 states x0→x1→. . . →xnon a path, and one dummy state D. The arrows denote the transition probabilities. The first state on the path, x0, is the starting point of the algorithm, and the last state, xn, is the target state.

Markov chain with rewinding (Azarmehr et al., 2026), in which an algorithm adaptively chooses both where to rewind and how long to explore. The goal is to reach a designated target state representing, for example, a correct answer or a sufficiently high-quality output, while minimizing the ex-pected number of steps (i.e., total number of observed states). We remark that allowing the algorithm to rewind can expo-nentially decrease the number of steps (This is discussed further in Section 3.3, see Figure 1 for an example). The set of observed states can be interpreted as a tree: each observed state corresponds to a node of the tree, and rewind-ing to a previous state opens a new branch at the corre-sponding node. Many existing test-time inference strategies fit into this framework. For example, Chain of Thoughts (CoT)(Wei et al., 2023) explores a single path in the Markov chain, starting at a carefully chosen initial state. Best of k

and self-consistency (Wang et al., 2023) can be interpreted as exploring multiple paths that share the same root, and are otherwise independent. Other approaches such as Tree of Thoughts (ToT) (Yao et al., 2023) and Monte Carlo Tree Search (MCTS) (Dainese et al., 2024; Park et al., 2024; Liu, 2025) employ more intricate methods for exploring the tree. Our rewinding Markov chain model can simulate these strategies by appropriate choices of the rewinding distribution over the observed set. Moreover, recent verifier-guided backtracking methods can also be expressed within our framework: in Section A we show that the proposed Markov chain of Rohatgi et al. (2025) is recovered as a special case of our model, and our framework generalizes it. 

The Optimal Algorithm: Our main contribution is char-acterizing the optimal solution in this model, i.e., presenting an algorithm for exploring the Markov chain that reaches the target state with the minimum number of steps, in ex-pectation. We show that no matter what the Markov chain looks like, the optimal generated tree is always a caterpillar .That is, all the nodes are within distance 1 of a central path, and the algorithm may rewind only to withdraw the most recently generated state. More precisely, for each state x,  

> x0
> x1
> x2
> ...
> xn
> x0
> x1
> x2
> ...
> xn
> Figure 2. This figure illustrates the tree explored by two different algorithms, with Figure 1 being the input Markov chain. White circles represent state D, and green states represent the optimal path to the target state. The right side tree represents the explored tree by CaT, and the left side tree represents a BFS-like algorithm that expands without pruning the dummy state.

we define a value OPT (x), which is the optimal number of steps that an algorithm requires in expectation to reach the target state from x. This value guides our algorithm in exploring the Markov chain, and can be computed exactly given full knowledge of the Markov chain, or otherwise approximated. That is, the algorithm maintains a minimizer state x∗, initialized to the starting state. Then, it repeatedly generates the next state g from x∗, until it obtains a state with OPT (g) < OPT (x∗), which in turn replaces x∗ and undergoes the same process. This is formalized below as Algorithm 1. 

Algorithm 1 Caterpillar of Thoughts 

Input: An initial state x0, and a target state zx∗ ← x0

while x∗̸ = z do 

g ← next state drawn from x∗

if OPT (g) < OPT (x∗) then 

x∗ ← g

end if end while Theorem 1.1. Given an initial state x0, Algorithm 1 reaches the target state using the optimal number of steps in expec-tation, i.e., in OPT (x0) steps. 

Observe that the algorithm always keeps track of only two states, the minimizer state x∗, and the most recently gener-ated state g. It may rewind to x∗ or otherwise continue from 

g, resulting in a tree that consists of leaves hanging from a central path, i.e., a caterpillar. 

Massive Markov Chains: We note that, in practice, com-puting the exact value of OPT (x) might not be feasible given that the Markov chain corresponding to a pre-trained LLM could be huge. A nice property of Algorithm 1 is that only access to the values of OPT (·) is sufficient for its implementation. In other words, given oracle access to 2Caterpillar of Thoughts        

> Table 1. Performance comparison on the Game of 24 benchmark. Tree-of-Thoughts uses a beam size of 5. The proposed method reports best of 2runs, for maximum budget of iterations (steps) set to be 10 and 15 . By tokens in this table we mean sum of generated tokens and prompt tokens.

Method Success Rate(%) Avg. Tokens Tree-of-Thoughts (Yao et al., 2023) 74 19.2k 

CaT 

(15 steps) 81 15.3k CaT 

(10 steps) 78 14.2k 

OPT (·), additional knowledge of the Markov chain’s struc-ture is not helpful. In this paper, we also study whether approximations of this oracle are sufficient. In particular, in Section 4, we present an algorithm that reaches the target state in poly (OPT (x)) steps, given only noisy approxima-tions of OPT .

Experiments: In Section 5, we evaluate a variant of the optimal Algorithm 1 (which is more robust when we only have approximations of OPT (x)) empirically on two stan-dard reasoning benchmarks of the literature: the Game of 24 and 5 × 5 crosswords. See Table 1 for a direct comparison between this algorithm and Tree-of-Thoughts (ToT). While our algorithm improves the success rate significantly from 74% to 81%, it also reduces the number of generated tokens (and thus the cost of evaluations) by more than 20%. 

# 2. Preliminaries 

The Formal Model: We consider a finite-state fully ob-servable Markov chain M = (Ω , P ) with transition proba-bilities P : Ω × Ω → [0 , 1] . In the rewinding model, unlike a standard Markov chain, in each step the process is allowed to rewind to any previously visited state. Given a fully observable Markov chain M = (Ω , P ), an initial state x0 ∈ Ω, and a target state z ∈ Ω, the goal is to devise a rewinding algorithm that minimizes the expected number of steps to obtain the target state z.

Definition 2.1 (Rewinding Algorithms) . Given a fully ob-servable Markov chain M = (Ω , P ), and an initial state 

x0, in every step t ≥ 0, a rewinding algorithm chooses a rewinding time t′ ≤ t, and obtains a new state xt+1 accord-ing to the transition probabilities of xt′ . We refer to xt′ as the parent state of xt+1 , and denote it by p(xt+1 ).Our main contribution is characterizing the optimal algo-rithm, which reaches the target state in the minimum number of steps. 

Definition 2.2 (The Optimal Algorithm) . Given a tree of states observed so far, X = ( x0, · · · , x t), a rewinding al-gorithm for the remainder of the process is optimal if it minimizes the expected number of steps before obtaining the target state z. We use OPT (X) to denote the expected hitting time of such an optimal algorithm. For the remainder of the paper, we use O(·) instead of 

OPT (·) for brevity. 

# 3. Fully Observable Markov Chain 

3.1. Optimal Algorithm 

This section is devoted to the proof of our main theorem, Theorem 1.1. First, we establish that the optimal algorithm depends only on the set of distinct states observed so far, and the exact structure of the tree is inconsequential. The proof is deferred to Section B. 

Lemma 3.1. There exists an optimal rewinding algorithm such that given a sequence of states X = ( x0, · · · , x t), the rewinding time t′ ≤ t chosen for obtaining the next state is determined solely based on the set of distinct states S in X.We use O(S) := O(X) to denote the optimal hitting time corresponding to S, and O(x) as a shorthand for O({x}).

Next, we define non-branching rewinding algorithms, which are algorithms that evolve a caterpillar tree. 

Definition 3.2 (Non-branching Algorithms) . A rewinding algorithm is non-branching if in any step t ≥ 0, it either rewinds to the most recently obtained state xt, or to p(xt),i.e. to the same rewinding time as the previous step. For a state a ∈ Ω, we let ON B (a) denote the minimum expected hitting time of z for non-branching algorithms starting at a.The key idea behind the proof of Theorem 1.1 is that the optimal algorithm is a non-branching one. 

Theorem 3.3. For any set of states S ⊆ Ω, there exists a state x ∈ S such that 

O(S) = ON B (x).

Proof. We prove the statement by a downward induction on 

|S|. It holds trivially for any set of states S that contains z

since we have that O(S) = ON B (z) = 0 . This includes the base case, S = Ω .For the induction step, given a set S with |S| < |Ω|, fix any optimal algorithm, and let a ∈ S be the rewinding state chosen for obtaining the next state. Let g be the state that is obtained as a result. Observe that if g ∈ S, the optimal algorithm remains the same (by Lemma 3.1). Therefore, without loss of generality, we can assume that the optimal algorithm rewinds to a, until it obtains a state g / ∈ S.For a state g / ∈ S, let Sg denote S ∪ { g}. Since |Sg | > |S|,by the induction hypothesis, there exists a state xg ∈ Sg

3Caterpillar of Thoughts 

such that 

O(Sg ) = ON B (xg ). (1) That is, there exists a non-branching algorithm starting at xg

that is optimal for Sg . We consider two scenarios. First, we consider the case that for all g / ∈ S it holds that xg = g (this includes the case where g = z, and the process stops), i.e., the target is obtained or there is an optimal algorithm for Sg

that starts at xg and does not branch. In this case, we have a non-branching algorithm which is optimal for S that starts at 

a, meaning O(S) = ON B (a). This algorithm, rewinds to a

until a state g / ∈ S is obtained, and then proceeds according to the non-branching algorithm at xg = g.Now, consider the case where there exists a state g / ∈ S

such that xg̸ = g. Then, it holds that xg ∈ S. We show that 

O(S) = ON B (xg ) (the algorithm could have started at xg

instead of a to begin with). First, observe that since S ⊆ Sg ,any algorithm for S is a valid algorithm for Sg . Therefore we have 

O(Sg ) ≤ O(S). (2) Second, since xg ∈ S, the optimal non-branching algorithm starting at xg is a valid algorithm for S. Hence, 

O(S) ≤ ON B (xg ). (3) Finally, combining Equations (1) to (3), we have 

O(S) ≤ ON B (xg ) = O(Sg ) ≤ O(S),

which implies that the above inequalities hold with equality, and hence, 

O(S) = ON B (xg ).

This directly implies that, with a set of states S observed so far, the optimal algorithm is a non-branching one, which starts at the state x ∈ S that minimizes O(x).

Corollary 3.4. Given an initial set of states S, we have 

O(S) = min  

> x∈S

O(x) = min  

> x∈S

ON B (x).

Proof. First, note that for any state x ∈ Ω, Theorem 3.3 implies O(x) = ON B (x) (by letting S = {x}). Therefore, 

O(x) and ON B (x) can be used interchangeably and the second equality in the claim follows. Now, we show O(S) = min x∈S ON B (x). Observe that a non-branching algorithm starting at a state x ∈ S is a valid algorithm for S. Hence, O(S) ≤ min x∈S ON B (x).Moreover, by Theorem 3.3, there exists a state x∗ ∈

S such that O(S) = ON B (x∗). Therefore, O(S) =min x∈S ON B (x).We restate Theorem 1.1 here. 

Theorem 1.1. Given an initial state x0, Algorithm 1 reaches the target state using the optimal number of steps in expec-tation, i.e., in O(x0) steps. Proof. Note that the Algorithm 1 is non-branching since in every step, it either rewinds to x∗, the same rewinding state as the last step, or to g, the most recently obtained state. To show it is the optimal non-branching algorithm, by Corol-lary 3.4, it suffices to prove that whenever the algorithm rewinds to x∗, it satisfies x∗ = arg min x∈S ON B (x), where 

S denotes the set of states obtained so far. This can be proven inductively as follows. For the base case, it holds trivially since x∗ is equal to x0, the only state in S. For the induction step, observe that if O(g) ≥ O(x∗), then x∗ is still the minimizer of ON B (x). Otherwise, if O(g) < O(x∗),then g is the new minimizer of ON B (x) in S, and x∗ is changed to S.We use Theorem 1.1 to obtain a recursive expression of 

O(·), which we then utilize to compute the values of O(·),in Section 3.2. 

Corollary 3.5. For a state x̸ = z, let L ⊆ Ω be the set of states y such that O(y) < O(x). Then, it holds that 

O(x) = 1 + P 

> y∈L

P (x, y )O(y)

P (x, L ) .

3.2. Computing O(x)

We present a Dijkstra-like algorithm for computing the value of O(x) for every state x. The algorithm maintains an upper bound dx for O(x) along with a set of states T that grows in every step. At any point, for every x ∈ T , it holds that 

dx = O(x). The algorithm is formalized as Algorithm 2. 

Algorithm 2 Computes the optimal hitting time for each initial state. 

Input: A Markov chain M = (Ω , P ), and a target state 

z ∈ Ω.

T ← ∅

dx ←

(

0 if x = z, and 

∞ otherwise 

while T̸ = Ω and min x / ∈T dx < ∞ do 

x∗ ← arg min x / ∈T dx (break ties arbitrarily) 

T ← T ∪ { x∗}

for x / ∈ T do if P (x, T ) > 0 then 

dx ← 1+ P    

> y∈TP(x,y )dy
> P(x,T )

end if end for end while return {dx}x∈Ω

4Caterpillar of Thoughts 

Theorem 3.6. Algorithm 2 computes O(x) for every state 

x ∈ Ω and runs in time O(n2), where n = |Ω| denotes the number of states in the Markov chain. If m entries of the transition probabilities P (·, ·) are non-zero, then the algorithm can be implemented in time O(( m + n) log n).

3.3. The Necessity of Rewinding 

In this section, we discuss the role of rewinding in the effi-ciency of algorithms. We show that allowing the algorithm to rewind to previous states can exponentially improve the expected run time, compared to algorithms that do not allow rewinding or allow limited forms of rewinding. Essentially, without rewinding, the algorithm may get “lost” and never recover. Consider, as an example, the Markov chain depicted in Figure 1, with p = 12 . The optimal algorithm operates as follows. At any point, it maintains a state xi, which is the furthest state observed in the path (initially, x0). It repeatedly rewinds to xi to generate the next state, until 

xi+1 is obtained. Since each increase takes 1 

> p

generations in expectation, the algorithm reaches xn in np = 2 n steps. To compare, we consider an algorithm that allows no rewind-ing. That is, the algorithm starts at x0 and explores in a single trajectory in hopes of reaching xn. Observe that in each step, the trajectory either moves one state further down the path, or moves to the dummy state D. In case it moves to D, it is absorbed forever, and never reaches the target state xn. Therefore, the algorithm reaches the target state with a negligible probability of 12n .Similarly, we can consider an algorithm with limited back-tracking that explores a number of independent trajectories in parallel. Based on the analysis above, each trajectory reaches the target state with a probability of 12n . Hence, an algorithm that explores k trajectories in parallel reaches the target state with a probability of 

1 −



1 − 12n

k

≤ k

2n .

Therefore, any algorithm that reaches the target state with a constant probability must explore at least Ω(2 n) states, which is exponentially larger than the expected number of steps for the optimal rewinding algorithm, 2n.

# 4. Massive Markov Chains 

Theorem 1.1 shows that an optimal strategy can be devised given oracle access to the exact values of O(x). How-ever, such oracle access may be infeasible for large Markov chains, since examining the structure of the entire chain is computationally inefficient. Therefore, in this section, we consider access to noisy approximations of O(x) under two different noise models. In Section 4.1, we consider a model in which the algorithm can obtain independent approximations of O(x) with Lapla-cian noise, and present a non-branching rewinding strategy with a hitting time comparable to O(x). In Section 4.2, we show that adversarially chosen (1 ± ε)-approximations of 

O(x) are not sufficient for an efficient algorithm, implying that some assumptions about the noise (such as in the first noise model) are necessary. 

4.1. A Polynomial Algorithm under Laplacian Noise 

First, we formalize the noise model. 

Definition 4.1 (Approximations with Laplacian Noise) . For each state s ∈ Ω, the algorithm can repeatedly obtain inde-pendent noisy evaluations of O(s):

bO(s) ∼ Laplace( O(s), λ ),

which has a Laplacian distribution of mean O(s) and scale λ

(i.e., variance 2λ2), each evaluation incurs the same runtime cost as generating a state. 1

Of special interest may be the case where √2λ = εO(s), as 

bO(s) ∈ (1 ± ε)O(s) with constant probability. Before stating the algorithm for this model, henceforth re-ferred to as the stable algorithm, we describe an auxiliary algorithm which is used in the analysis. The auxiliary al-gorithm is non-branching and utilizes exact values of O(x),in a manner that enables the stable algorithm to simulate it using only noisy approximations of O(x). As opposed to the optimal algorithm, which replaces the minimizer state 

x∗ with any state that has a smaller optimal hitting time, the auxiliary algorithm replaces x∗ only when the new state improves the optimal hitting time non-negligibly. 

Definition 4.2. Given an initial state x0 ∈ Ω and a parame-ter ε > 0, the auxiliary algorithm operates as follows: 1. Maintain a special state x∗ (serving as an “approximate minimizer”), initially equal to x0.2. Repeatedly rewind to x∗ to obtain a state g, until the new state satisfies g = z or O(g) < O(x) − c, where 

c = ε 

> 1+ ε

.3. If g is equal to the target state z, halt. Otherwise, let 

x∗ := g and go back to step 2. We use O′(x0) to denote the expected hitting time of z with this algorithm. We establish that the hitting time of the auxiliary algorithm is at most (1 + ε) times larger than the optimal hitting time. The proof is deferred to Section B.     

> 1The Laplace distribution has a density function of f(x) =
> 12λe|x−μ|/λ . We remark that the algorithm in this section can be easily adapted to any independent noisy evaluation of variance λ.

5Caterpillar of Thoughts 

Lemma 4.3. For any state x, it holds that 

O′(x) ≤ (1 + ε)O(x).

Now, we move on to the stable algorithm. First, we note that the mean-median technique can be used to strengthen the ap-proximation guarantee of bO(·) (as defined in Definition 4.1). The proof is deferred to Section B. 

Lemma 4.4. For any state s ∈ Ω and parameters ε and δ,given access to estimations bO(s) ∼ Laplace( O(s), λ ), the algorithm can use O( λ2 

> ε2

log 1 

> δ

) estimates to obtain a value 

Xs such that |Xs − O(s)| ≤ ε, with probability δ.

With the mean-median subroutine at hand, we are ready to present the stable algorithm (Algorithm 3). 

Algorithm 3 Stable Algorithm (for noisy approximations) 

Input: A Markov chain M = (Ω , P ), a target state 

z ∈ Ω, an upper bound N for O(x0), and a Laplacian approximator of bO(·) of the hitting times 

x∗ ← x0

while x∗̸ = z do 

g ← next state drawn from x∗

Xx∗ , X g ← mean-median approximations obtained from bO (Lemma 4.4), with parameters ε = 110 and 

δ = 110 N

if Xg < X x∗ − 12 then 

x∗ ← g

end if end while Theorem 4.5. Given an initial state x0, Algorithm 3 reaches the target state with an expected computation cost (number of steps plus number of approximations) of at most O(λ2 · O(x0) log O(x0)) . In particular, when 

bO(s) ∼ Laplace ( O(s), ε O(s)) , the total cost is at most 

O(ε2 · O(x0)3 log O(x0)) , in expectation. 

4.2. A Lower Bound for Adversarial Noise 

In this section, we present a lower bound for adversarial noise in partially observable (i.e., massive) Markov chains, showing that access to arbitrary (1 ± ε)-approximations of 

O(x) is not sufficient for reaching the target state efficiently, when the errors are set arbitrarily by an adversary. That is, we construct a partially observable Markov chain M =(Ω , P ), with an initial state x0 ∈ Ω and a target state z. For any generated state x, the algorithm can only observe bO(x),which is an adversarially set value within (1 ± ε)O(x). We show that to reach the target state with constant probability, the algorithm must generate at least exp( O(x0)) states. 

Theorem 4.6. Given a parameter ε < 1, there exists a partially observable Markov chain M = (Ω , P ), with ob-servations bO(x) ∈ (1 ± ε)O(x) for any state x, such that any algorithm that can only access the observations requires 

exp( O(x0)) steps to reach the target, where O(x0) can be taken arbitrarily large. 

Here, we describe the Markov chain and defer the proofs to Section B. To do so, we use a ∆-regular tree of depth 

10 d. Here, ∆ is a sufficiently large constant, and d is taken as large as desired to increase O(x0). The root of the tree, which has degree ∆, corresponds to the initial state x0. Each descendant of x0 has ∆ − 1 children. This continues up to a depth of 10 d, where each descendant of depth 10 d is a leaf and has no children. Each vertex in the tree corresponds to a state in the Markov chain. For the transition probabilities, we let any non-leaf state transition to its neighbors in the tree with uniform probability. A leaf vertex transitions to its parent with probability 1∆ , and remains in the same state otherwise. Finally, we choose one of the leaves as the target state z. Before presenting the observations bO(x), we calculate the optimal hitting times O(x) for each state. 

Lemma 4.7. For every state x, it holds that O(x) = ∆ ·

dist (x, z ), where dist (x, z ) denotes the length of the shortest path between x and z in the tree. 

The observations (with arbitrary noise) are set as follows. Let P = ( x0, x 1, . . . , x 10 d) be the path that starts at the root 

x0 and ends at the target state x10 d = z. We divide the states into three groups: (1) those within a distance of εd from the root; (2) those in the subtree of xεd ; and (3) everything else, i.e., states with a depth strictly larger than εd , that are not in the subtree of xεd . For a state s in the first group, we let the observation bO(x) be equal to ∆ · (10 d − ds), where ds

is the depth of s (its distance from the root). That is, the true hitting time as if s lied on the path from x0 to z. For the second group, we let bO(s) := ∆ · dist (s, z ), i.e., the observation is set without error to the optimal hitting time. Finally, for the third group, we let bO(s) := 10 d − 2εd + ds.That is, set to the hitting time as if s were in the subtree of 

xεd but not in the subtree of xεd +1 (i.e., a node with depth ds

that branches off P at xεd ). It can be easily confirmed that these observations are within a (1 ± ε)-multiplicative error of the optimal hitting time (as characterized in Lemma 4.7). To prove Theorem 4.6, in Section B, we show that any algo-rithm that reaches the target state with constant probability using only the observations, must explore a constant fraction of the states in the first group (the vertices within distance 

εd of x0). That is, it must take at least about ∆εd steps which is exponentially larger than the optimal hitting time 

10∆ d.

# 5. Experiments 

In this section, we evaluate a practical variant of our optimal rewinding algorithm (Algorithm 1) that is better suited for practical settings, where the exact number of optimal steps from a state is not available. 6Caterpillar of Thoughts 

Our experiments and all the compared methods use the same underlying language model (GPT-4), the same prompt-ing strategy for child generation, and comparable evaluator prompts. We used ToT as the base code, and implemented CaT. The implementations are available on github. 

5.1. The Algorithm 

For our experiments, we implement a variant of the optimal algorithm Algorithm 1 that instead of selecting the “best” explored state deterministically, selects one via the softmax 

distribution. This modification is more suitable for our empirical studies, as the optimal hitting time O(x) from a given state x is not readily available and can only be approximated. More formally, the distribution is based on bO(x), an estimator of O(x), and prioritizes states with smaller estimated hitting times. See Algorithm 4. 

Algorithm 4 Caterpillar of Thoughts (via softmax) 

Input: Initial state x0, target state z, and hitting time estimator bO(·)

S ← { x0}

while z / ∈ S do 

Sample a parent state x∗ ∈ S according to the softmax distribution 

Pr[ x∗] ∝ exp( −bO(x∗)) 

Sample a child state g ∼ P (x∗, ·)

S ← S ∪ { g}.

end while 

In each iteration, Algorithm 4 rewinds to a previously ob-served state and expands it by sampling new child states according to the underlying transition probabilities. The use of a softmax distribution heavily biases exploration toward states that are estimated to be closer to target, while still allowing exploration over the entire observed set. We note that the main advantage of Algorithm 4 compared to the non-branching algorithm of Theorem 1.1 is robust-ness to estimation error. Without exact access to O, a non-branching algorithm may commit early to an incorrectly evaluated state x and get stuck exploring a suboptimal sub-tree rooted at x, with no mechanism to recover from this mistake. In contrast, Algorithm 4 allows the set of observed states to occasionally branch, preventing the algorithm from being confined to a single trajectory. At the same time, the rewinding rule ensures that exploration remains exponen-tially biased toward states with smaller estimated rewinding hitting times. 

5.2. Task: Game of 24 

In the Game of 24 , the model is given four integers and must construct a valid arithmetic expression using the operations  

> Figure 3. Illustration shows log linear relation between number of valid expressions and number of steps.

{+, −, ×, / } that evaluates exactly to 24 . Each number must be used exactly once, and intermediate results must be valid real numbers. A solution is naturally constructed incrementally by applying operations step by step, which makes the task suited for modeling reasoning as a search process over partial solutions. For example, given input ”4 4 6 8”, a solution output could be ”(6 - 4) * (4 + 8) = 24”. 

The Markov Chain: A state corresponds to a partial solution , consisting of the sequence of arithmetic operations applied so far, and the resulting multiset of remaining values. From a given state, transitions correspond to sampling a valid next arithmetic operation and two of the remaining values using the language model, producing a new partial solution. For example given input “4 4 6 8”, a valid state is represented as “4 + 8 = 12 (left: 4 6 12)” or “4 + 8 = 12 (left: 4 6 12), 6 - 4 = 2 (left: 2 12)”. Any state representing a complete arithmetic expression that evaluates to 24 and uses the input numbers each exactly once, is considered a target state. 

Data: We use data from 4nums 2, the same data as (Yao et al., 2023) which has 1,362 games that are sorted from easy to hard, and use a subset of relatively hard games in-dexed 901-1,000 for testing. We report the success rate across 100 games as the metric. Algorithm Implementa-tion In our experimental implementation, we use a version of Algorithm 4 with bounded number of iterations. We fix a maximum number of iterations as a hyperparameter, we execute the algorithm until a target state is reached, or terminate the algorithm once this budget of maximum itera-tions is exhausted. The output is considered successful if a valid solution state is discovered within this budget. In con-trast, the BFS like algorithm of ToT, cannot terminate when reaching the target, since ToT prioritizes breadth instead of depth, and to hit the target needs to reach a specific depth 

> 24nums.com

7Caterpillar of Thoughts      

> Table 2. Performance on the 5×5crossword benchmark: cost matched ToT vs. CaT.

Method Word Acc. (%) Letter Acc. (%) Games Solved (%) Avg. Tokens ToT (Yao et al., 2023) 39.5 64.8 5 73.4k 

CaT (This paper ) 50.0 68.6 15 66.8k 

and exhaustively process each layer. In Algorithm 4, at each iteration, after sampling a parent state according to the softmax distribution with tempera-ture 1, we expand the selected state by sampling multiple child states. The number of children sampled from a par-ent is not fixed a priori and is instead determined by the language model, following the same protocol used in Tree of Thoughts. This allows the model to adaptively propose multiple candidate next steps from a given partial solution. For each observed state s, we estimate O(s) using the lan-guage model itself. Concretely, the model is prompted to assess how likely the current partial solution is to lead to a correct final answer. The resulting scalar score is treated as a noisy estimate bO(s) of the optimal remaining cost to go from s.For the proposed algorithm, we run two independent exe-cutions (best of 2), each with a fixed iteration budget. We report success if either of the two runs produces a valid so-lution. This design choice accounts for the stochastic nature of the rewinding process. While Algorithm 4 significantly reduces the risk of committing to an early incorrect state, the algorithm may still, concentrate exploration on an unproduc-tive region of the state space. Running multiple independent instances avoids this effect without increasing the depth or branching factor within any single run. In contrast, Tree of Thoughts performs a more exhaustive and structured tree expansion within a single execution. Using best of 2 allows for a fairer comparison between the two algorithms, while keeping average token used less than Tree-of-Thoughts. 

Results: Table 1 summarizes performance on the Game of 24 benchmark. Algorithm 4 gets a 81% accuracy in Game of 24 while reducing token usage to 15 .3k tokens. In Figure 3, we illustrate how many of the inputs were solved per new iterations. 

5.3. Task: Crossword 

We evaluate our method on a 5 × 5 crossword completion benchmark. Each instance consists of 10 clues whose an-swers must be placed on a shared 5 × 5 grid, with 5 entries oriented horizontally and 5 entries oriented vertically. We report three metrics: (i) letter accuracy , measured over the 

25 grid cells; (ii) word accuracy , measured over the 10 en-tries; and (iii) game success , the fraction of instances for which the entire crossword is solved. 

The Markov Chain: We model the search process as a Markov chain that allows rewinding over partially com-pleted boards. A state is a partially filled crossword grid (equivalently, a set of assigned clue answers that are mutu-ally consistent). A transition selects one clue and proposes an answer using the language model; if the proposed entry is compatible with all currently placed letters at intersecting cells, it is inserted into the grid, producing a new (more complete) state. Incompatible proposals are rejected (the state remains unchanged). 

Data: We use the 5 × 5 crossword dataset from GooBix, 3

which is the same source used in the Tree-of-Thoughts (ToT) experiments. The dataset contains 156 crossword instances. Following the ToT setup, we evaluate on 20 held-out puzzles with indices 1, 6, . . . , 96 , and we use the same subset as ToT for in-context prompting. 

Implementation: For evaluation, the model is prompted to assess how likely an answer to a clue will be compatible with the given the partially completed board. We score each partially filled crossword by adding the scores of each filled word in that table. The resulting scalar score is treated as a noisy estimate bO(s) of the optimal remaining cost to go from s. Using these scores we generate the corresponding softmax distribution, and sample partially completed boards. Given the sampled board, we generate new boards by sorting the scores for each remaining hint, and picking top three. Note that for scoring hints we use the same prompts used in ToT. In our implementation for this task, per input we spend 20 iterations of Algorithm 4. 

Results: Table 2 compares our approach (CaT) against a truncated version 4 of ToT with 20 search steps instead of 100 steps reported by ToT. Across all metrics, CaT achieves higher accuracy and solves more puzzles while using fewer tokens on average. 

# Acknowledgements 

The third author thanks Shayan Talaei for many helpful discussions regarding the experiments. 

> 3https://www.goobix.com/crosswords/0505/
> 4This is from reports of (Chi et al., 2025) on truncated ToT

8Caterpillar of Thoughts 

# References 

Azarmehr, A., Behnezhad, S., Ghafari, A., and Sudan, M. Markov chains with rewinding, 2026. URL https: //arxiv.org/abs/2602.16028 .Botta, E., Li, Y., Mehta, A., Ash, J. T., Zhang, C., and Risteski, A. On the query complexity of verifier-assisted language generation, 2025. URL https://arxiv. org/abs/2502.12123 .Brown, B., Juravsky, J., Ehrlich, R., Clark, R., Le, Q. V., R ´e, C., and Mirhoseini, A. Large language monkeys: Scaling inference compute with repeated sampling, 2024. URL 

https://arxiv.org/abs/2407.21787 .Chi, Y., Yang, K., and Klein, D. ThoughtSculpt: Reason-ing with intermediate revision and search. In Chiruzzo, L., Ritter, A., and Wang, L. (eds.), Findings of the As-sociation for Computational Linguistics: NAACL 2025 ,pp. 7685–7711, Albuquerque, New Mexico, April 2025. Association for Computational Linguistics. ISBN 979-8-89176-195-7. doi: 10.18653/v1/2025.findings-naacl. 428. URL https://aclanthology.org/2025. findings-naacl.428/ .Dainese, N., Merler, M., Alakuijala, M., and Marttinen, P. Generating code world models with large language mod-els guided by monte carlo tree search. In Proceedings of the 38th International Conference on Neural Informa-tion Processing Systems , NIPS ’24, Red Hook, NY, USA, 2024. Curran Associates Inc. ISBN 9798331314385. Lightman, H., Kosaraju, V., Burda, Y., Edwards, H., Baker, B., Lee, T., Leike, J., Schulman, J., Sutskever, I., and Cobbe, K. Let’s verify step by step. arXiv preprint arXiv:2305.20050 , 2023. Liu, L. Monte carlo tree search for graph reasoning in large language model agents. In Proceedings of the 34th ACM International Conference on Information and Knowledge Management , CIKM ’25, pp. 4966–4970, New York, NY, USA, 2025. Association for Comput-ing Machinery. ISBN 9798400720406. doi: 10.1145/ 3746252.3760854. URL https://doi.org/10. 1145/3746252.3760854 .Madaan, A. et al. Self-refine: Iterative refinement with self-feedback. In Advances in Neural Information Processing Systems (NeurIPS) , 2023. Park, S., Liu, X., Gong, Y., and Choi, E. Ensembling large language models with process reward-guided tree search for better complex reasoning, 2024. URL https: //arxiv.org/abs/2412.15797 .Rohatgi, D., Shetty, A., Saless, D., Li, Y., Moitra, A., Ris-teski, A., and Foster, D. J. Taming imperfect process verifiers: A sampling perspective on backtracking, 2025. URL https://arxiv.org/abs/2510.03149 .Snell, C., Lee, J., Xu, K., and Kumar, A. Scaling llm test-time compute optimally can be more effective than scal-ing model parameters, 2024. URL https://arxiv. org/abs/2408.03314 .Venkatraman, S., Jain, V., Mittal, S., Shah, V., Obando-Ceron, J. S., Bengio, Y., Bartoldson, B. R., Kailkhura, B., Lajoie, G., Berseth, G., Malkin, N., and Jain, M. Recursive self-aggregation unlocks deep thinking in large language models. CoRR , abs/2509.26626, 2025. doi: 10.48550/ARXIV.2509.26626. URL https://doi. org/10.48550/arXiv.2509.26626 .von R ¨utte, D., Fluri, J., Ding, Y., Orvieto, A., Sch ¨olkopf, B., and Hofmann, T. Generalized interpolating discrete diffusion, 2025. URL https://arxiv.org/abs/ 2503.04482 .Wang, K., Zhou, J. P., Chang, J., Gao, Z., Kallus, N., Brantley, K., and Sun, W. Value-guided search for effi-cient chain-of-thought reasoning, 2025. URL https: //arxiv.org/abs/2505.17373 .Wang, X., Wei, J., Schuurmans, D., Le, Q., Chi, E., Narang, S., Chowdhery, A., and Zhou, D. Self-consistency im-proves chain of thought reasoning in language mod-els, 2023. URL https://arxiv.org/abs/2203. 11171 .Wei, J., Wang, X., Schuurmans, D., Bosma, M., Ichter, B., Xia, F., Chi, E., Le, Q., and Zhou, D. Chain-of-thought prompting elicits reasoning in large language models, 2023. URL https://arxiv.org/abs/ 2201.11903 .Yang, X.-W., Zhu, X.-Y., Wei, W.-D., Zhang, D.-C., Shao, J.-J., Zhou, Z., Guo, L.-Z., and Li, Y.-F. Step back to leap forward: Self-backtracking for boosting reasoning of language models, 2025. URL https://arxiv.org/ abs/2502.04404 .Yao, S., Yu, D., Zhao, J., Shafran, I., Griffiths, T. L., Cao, Y., and Narasimhan, K. Tree of thoughts: Deliberate problem solving with large language models, 2023. URL 

https://arxiv.org/abs/2305.10601 .9Caterpillar of Thoughts 

# A. Comparison to Related Work 

In this section, we argue that Verifier Guided Backtracking (VGB) Rohatgi et al. (2025) is a special case of the Markov chain with rewinding. We restate the VGB in a self-contained way. Fix: (1) a prompt x; (2) a discrete token alphabet A; (3) a reference policy (base model) πref (· | x, y 1: h), which for every prefix y1: h ∈ A h is a probability distribution over next tokens a ∈ A ; and (4) a nonnegative base value function Vb(x, y 1: h) ≥ 0 defined for every prefix y1: h ∈ A h (and similarly 

Vb(x, y 1: h, a ) ≥ 0 defined for extended prefixes). Define the prefix tree T := SHh=0 Ah, whose root is the empty prefix ∅ ∈ A 0. For y1: h ∈ A h with h ≥ 1, its parent is 

par( y1: h) := y1: h−1. For h ≤ H − 1 where H is the maximum depth of the tree, its children are ch( y1: h) := {(y1: h, a ) : 

a ∈ A} .Define the neighborhood of a node y ∈ T by N (y) := {par( y)} ∪ ch( y)}. Given the current node y = y1: h, define non-normalized weights on N (y) by 

w(u | x, y ) := 



Vb(x, y ) if u = par( y) and y̸ = ∅,πref (a | x, y ) · Vb(x, y, a ) if u = ( y, a ) ∈ ch( y),

0 otherwise. Let p(u | x, y ) := w(u|x,y )P   

> u∈N(y)w(u|x,y )

for u ∈ N (y).

The VGB algorithm operates by evolving a Markov chain (Yt)t≥0. The chain is initialized at Y0 = ∅ and evolves as follows: given Yt = y, sample Yt+1 ∼ p(· | x, y ). The process continues until a target state is reached. 

Lemma A.1. The VGB process is an instance of the Markov chain with rewinding. Proof. Instantiate our framework with the nodes of the prefix tree T as the state space. The initial state is the root of the prefix tree corresponding to the prompt x, and an empty string y = ∅ of tokens generated so far. For a node in the tree, the transition probabilities are defined on the children only , proportional to the non-normalized weights. With this setup, the VGB algorithm can be interpreted in our model as follows. Given the most recently produced state 

ut = ( x, y ) (corresponding to the prompt x, and the string of generated tokens y), the algorithm either rewinds to the parent 

par( ut) with probability p(par( y) | x, y ), or evolves a child otherwise. As a result, the next state ut+1 has the desired distribution as in VGB. 

# B. Omitted Proofs 

This section includes the proofs deferred from the main body of the paper. The claims are restated here for ease of reference. 

Lemma 3.1. There exists an optimal rewinding algorithm such that given a sequence of states X = ( x0, · · · , x t), the rewinding time t′ ≤ t chosen for obtaining the next state is determined solely based on the set of distinct states S in X. We use O(S) := O(X) to denote the optimal hitting time corresponding to S, and O(x) as a shorthand for O({x}).

To prove the lemma, we first show that the optimal algorithm depends only on the set of distinct elements of a sequence X

and not on the order or multiplicity of states in the history. 

Lemma B.1. Let X(1) and X(2) be two different sequences of obtained states with the same set of distinct states S. We have 

O(X(1) ) = O(X(2) ).

Proof. Fix an arbitrary algorithm σ used after history X(1) . We construct a corresponding algorithm σ′ to be used after 

X(2) such that the trajectories under σ and σ′ can be perfectly coupled while maintaining the following invariant. Let S(1) 

> t

and S(2)  

> t

denote the sets of states observed up to time t under σ and σ′, respectively. We maintain the invariant 

S(1)  

> t

= S(2)  

> t

= St for all t ≥ 0.

This holds initially because X(1) and X(2) have the same set of distinct states S.10 Caterpillar of Thoughts 

Now assume the invariant holds at step t. algorithm σ rewinds to some time t′ ≤ t, equivalently, it chooses a state a ∈ St.We define σ′ so that it also rewinds to the same state a. Since a ∈ S(1)  

> t

= S(2)  

> t

, this is always possible. Because the transition probabilities depend only on the chosen state and not on the history, both runs now sample y ∼ P (a, ·). We couple them so that both runs obtain the same next state y. Therefore, 

S(1)  

> t+1

= S(1)  

> t

∪ { y} = S(2)  

> t

∪ { y} = S(2) 

> t+1

,

so the invariant holds at time t + 1 . Thus, under this coupling, the sets of distinct states for X(1) and X(2) evolve identically at any time t and therefore, both algorithm σ and σ′ hit z at the same time. Let σ(X(1) ) be the expected hitting time of algorithm σ for X(1) . We define σ′(X(2) ) respectively. Pick σ be the algorithm with optimal expected hitting time on X(1) . For the coupled algorithm σ′ on X(2) , we have 

O(X(2) ) ≤ σ′(X(2) ) = σ(X(1) ) = O(X(1) ).

Symmetrically, we also obtain O(X(2) ) ≥ O(X(1) ). Hence O(X(1) ) = O(X(2) ).

Proof of Lemma 3.1. Let X be an arbitrary sequence of obtained states, and let S be its set of distinct states. Since S is precisely the set of distinct states in X, we can construct a sequence eX that visits exactly the states in S (each at most once) and has the same set of distinct states as X. We denote this sequence by S as well, viewed now as some fixed sequence whose distinct-state set is S. Applying Lemma B.1 with X(1) = X and X(2) = S, we obtain O(X) = O(S). This shows that the optimal hitting time starting from any history X depends only on its set of distinct states S.

Corollary 3.5. For a state x̸ = z, let L ⊆ Ω be the set of states y such that O(y) < O(x). Then, it holds that 

O(x) = 1 + P 

> y∈L

P (x, y )O(y)

P (x, L ) .

Proof. Consider the hitting time of the optimal algorithm described in Theorem 1.1, and let y be the first state drawn by the algorithm (obtained by rewinding to x0). There are two possibilities for y. Either O(y) ≥ O(x), in which case it is ignored and the expected number of remaining steps is O(x), or O(y) < O(x), in which case x∗ is set equal to y and the expected remaining number of steps is O(y). Therefore, it holds that 

O(x) = 1 + X

> y / ∈L

P (x, y )O(x) + X

> y∈L

P (x, y )O(y)= 1 + P (x, Ω \ L)O(x) + X

> y∈L

P (x, y )O(y).

The claim follows from moving P (x, Ω \ L)O(x) to the left-hand side and dividing both sides by P (x, L ).

Theorem 3.6. Algorithm 2 computes O(x) for every state x ∈ Ω and runs in time O(n2), where n = |Ω| denotes the number of states in the Markov chain. If m entries of the transition probabilities P (·, ·) are non-zero, then the algorithm can be implemented in time O(( m + n) log n).Proof. We prove the following invariant inductively: At any point, for all states x ∈ Ω, it holds that dx ≥ O(x), and this holds with equality for all states x ∈ T . For the base case, we consider the algorithm at the end of the first iteration. At this time, it holds that T = {z} and dz = O(z) = 0 . For all x̸ = z, we have 

dx =

( 1  

> P(x,z )

if P (x, z )̸ = 0 , and 

∞ otherwise. This value is an upper bound for O(x) as it corresponds to the simple algorithm which rewinds to x until it obtains the target state z.For the induction step, given the set T , we define a corresponding algorithm for each initial state x / ∈ T . The algorithm starts by repeatedly rewinding to x, until it obtains a state g ∈ T . Then, it proceeds by following the optimal algorithm for g.11 Caterpillar of Thoughts 

The expected hitting time of this algorithm can be analyzed as follows. In expectation, it takes 1   

> P(x,T )

to obtain a state in T

from x. Then, conditioned on that the newly obtained state g is in T , we have g = y with probability P (x,y )   

> P(x,T )

, in which case the remainder of the algorithm takes O(y) steps to terminate in expectation. Therefore, the expected hitting time of this algorithm is equal to: 

1

P (x, T ) + X

> y∈T

P (x, y )

P (x, T ) O(y).

Observe that if O(y) = dy for all y ∈ T , then the value above is equal to dx.Hence, it remains to show that when x∗ is added to T , it holds that dx∗ = O(x∗). Let A ⊆ Ω \ T be the set of states x

outside T that minimize O(x), and let m denote the minimum. For any x ∈ A, the algorithm corresponding to dx (as defined in the previous paragraph) is the same as the optimal algorithm outlined in Theorem 1.1 since T contains every state x′ with O(x′) < O(x). Therefore, we have that dx = O(x) = m for all x ∈ A. Furthermore, for any x / ∈ A, it holds that dx ≥ O(x) > m . As a result, x∗ := arg min x / ∈T dx is a member of A,and it holds that dx∗ = O(x∗) = m.Finally, we note that when the algorithm terminates, for any state x / ∈ T , there is no path of positive probability from x to z.Otherwise, the algorithm would have expanded T through one of the edges in the path that go from Ω \ T to T . As a result, for any x / ∈ T , it holds that O(x) = dx = ∞. Therefore, when Algorithm 2 terminates, dx is equal to O(x) for all x ∈ Ω. It can be easily seen that it can be implemented in O(n2) or O(( n + m) log n), which concludes the proof. 

Lemma B.2. For a state x̸ = z, let L ⊆ Ω be the set of states y such that O(y) < O(x) − c. Then, it holds that 

O′(x) = 1 + P 

> y∈L

P (x, y )O′(y)

P (x, L ) .

Proof. The proof is similar to that of Corollary 3.5. After the first state y is drawn, either y / ∈ L, in which case the expected remaining number of steps is O′(x), or y ∈ L, in which case the expected remaining number of steps is O′(y).

Lemma 4.3. For any state x, it holds that 

O′(x) ≤ (1 + ε)O(x).

Proof. Given that c = ε 

> 1+ ε

holds, O′(x) ≤ (1 + ε)O(x) and (1 − c)O′(x) ≤ O(x) are equivalent. We prove the latter inductively, ordering the states by O(x). More specifically, for the base case, we consider x = z where O(x) = O′(x) = 0 .Then, for the induction step, we assume the claim for all y with O(y) < O(x), and prove it for x.Take any state x̸ = z. We divide the states into three groups based on their optimal hitting time: 

H := {y ∈ Ω | O(x) ≤ O(y)},M := {y ∈ Ω | O(x) − c ≤ O(y) < O(x)}, and 

L := {y ∈ Ω | O(y) < O(x) − c}.

Recall the recursive formulation of O(x) (Corollary 3.5, note the change in definition of L): 

O(x) = 1 + P  

> y∈M∪L

P (x, y )O(y)

P (x, M ∪ L) .

For y ∈ M , it holds O(y) ≥ O(x) − c by definition. For y ∈ L, the induction hypothesis implies O(y) ≥ (1 − c)O′(y).12 Caterpillar of Thoughts 

Therefore, O(x) can be lower-bounded as follows: 

O(x) ≥ 1 + P 

> y∈L

P (x, y )(1 − c)O′(y)

P (x, M ∪ L)+

P 

> y∈M

P (x, y )( O(x) − c)

P (x, M ∪ L)= 1 + P 

> y∈L

P (x, y )(1 − c)O′(y)

P (x, M ∪ L)+ P (x, M )

P (x, M ∪ L) (O(x) − c).

Moving P (x,M )   

> P(x,M ∪L)

O(x) to the left-hand side and dividing both sides by 1 − P (x,M )   

> P(x,M ∪L)

, we get 

O(x) ≥ 1 + P 

> y∈L

P (x, y )(1 − c)O′(y)

P (x, L ) − P (x, M )

P (x, L ) c. 

Further algebraic manipulation of the inequality and plugging in the recursive formulation of O′(x) (Lemma B.2) yields 

O(x) ≥ 1 + P 

> y∈L

P (x, y )(1 − c)O′(y)

P (x, L ) − P (x, M )

P (x, L ) c

= (1 − c) 1 + P 

> y∈L

P (x, y )O′(y)

P (x, L )+ 1

p(x, L ) c − P (x, M )

P (x, L ) c

≥ (1 − c) 1 + P 

> y∈L

P (x, y )O′(y)

P (x, L )= (1 − c)O′(x).

This concludes the proof. 

Lemma 4.4. For any state s ∈ Ω and parameters ε and δ, given access to estimations bO(s) ∼ Laplace( O(s), λ ), the algorithm can use O( λ2 

> ε2

log 1 

> δ

) estimates to obtain a value Xs such that |Xs − O(s)| ≤ ε, with probability δ.Proof. Let N = log 1 

> δ

. First, for each 1 ≤ i ≤ N , let X(i) be the mean of k = 32 λ2 

> ε2

independent approximations obtained from bO(s). As a result, each X(i) has a mean of O(s) and a variance of 2λ2 

> k

= ε2 

> 16

. Therefore, by Chebyshev’s inequality, it holds: 

Pr[ |X(i) − O(s)| ≥ ε] ≤ ε2/16 

ε2 ≤ 116 .

We let the final estimate Xs be equal to the median {X(i)}i≤N , and prove that it satisfies 

Pr[ |Xs − O(s)| > ε ] ≤ δ. 

To show this, we note that for the median to be outside the interval O(s) ± ε, at least half of the means {X(i)}i≤N must be outside the interval. Taking the union bound over all the possibilities for these ⌊N/ 2⌋ indices, the probability of this event is at most 

 N

⌊N/ 2⌋



·

 116 

N/ 2

≤ 2N ·

 116 

N/ 2

≤ 2−N

≤ δ. 

Therefore, Xs has the desired property, which concludes the proof. 13 Caterpillar of Thoughts 

Theorem 4.5. Given an initial state x0, Algorithm 3 reaches the target state with an expected computation cost (number of steps plus number of approximations) of at most O(λ2 · O(x0) log O(x0)) . In particular, when bO(s) ∼

Laplace ( O(s), ε O(s)) , the total cost is at most O(ε2 · O(x0)3 log O(x0)) , in expectation. Proof. Without loss of generality, we assume that the guarantee of the mean-median approximation (Lemma 4.4) holds in every step of Algorithm 3 deterministically, and show that the expected number of steps is at most 2O(x0) when it does. That is, 

|Xx∗ − O(x∗)| ≤ 110 and |Xg − O(g)| ≤ 110 . (4) We show this is without loss of generality through a slight modification to the algorithm. We add a reset mechanism to the algorithm: If the target is not reached within 4N steps, then the algorithm discards the run and starts from the beginning. Each run involves 8N mean-median approximations (two per each step), and by Lemma 4.4 each of them satisfies the guarantee with probability δ = 110 N . Therefore, the probability that every mean-median approximation in a run satisfies the guarantee is at least: 

1 − 110 N

8N

≥ e−8/10 

2 ≥ 15 .

When this event happens, we show that the expected number of steps before reaching the target state is at most 2N . As a result, by the Markov inequality, such a run has a probability of at least 12 to reach the target before the reset at 4N steps. That is, in expectation, it takes 11/5 · 11/2 = 10 runs to reach the target state, incurring only a constant factor on the number of steps. Hence, it remains to show that the expected number of steps to reach the target state is at most 2O(x0), assuming (4) holds in every step. The analysis is through comparing the stable algorithm (Algorithm 3) to the auxiliary algorithm (Definition 4.2). Invoking Lemma 4.3, with ε = 1 and c = 12 , yields 

O′(x0) ≤ 2O(x0).

We show that the stable algorithm has at most as many steps as the auxiliary rewinding algorithm. More precisely, let O′′ (x)

denote the expected number of steps of the stable algorithm, assuming (4) holds. We prove that O′′ (x) ≤ O(x) for all x.We prove this claim using induction. Ordering the states increasingly based on O(x), we assume O′′ (y) ≤ O′(y) for all y before x (i.e., states y with O(y) < O(x)), and show the claim holds for x. It holds trivially for the base case: 

O′′ (z) = O′(z) = 0 . For the induction step, recall the recursive formulation of O′(x):

O′(x) = 1 + P 

> y∈L

P (x, y )O′(y)

P (x, L ) = 1 + X

> y / ∈L

P (x, y )O′(x) + X

> y∈L

P (x, y )O′(y), (5) where L ⊆ Ω is the set of states y such that O(y) < O(x) − c.Similarly, we can obtain a recursive formulation for O′′ (x). Let M ⊆ Ω be the set of states y with O(x) − c < O(y) < O(x).We note that since (4) holds, the stable algorithm replaces x∗ by g when g ∈ L, and it does not replace x∗ by g when 

g / ∈ M ∪ L. The only difference between the stable algorithm and the auxiliary algorithm is that the stable algorithm might replace x∗ for some of the states g ∈ M , with a probability of Q(x∗, g ), in which case it gains a benefit over the auxiliary algorithm. Therefore, we obtain 

O′′ (x) = 1 + X 

> y / ∈M∪L

P (x, y )O′′ (x) + X

> y∈M

P (x, y )( Q(x, y )O′′ (y) + (1 − Q(x, y )) O′′ (x)) + X

> y∈L

P (x, y )O′′ (y).

Using the induction hypothesis we replace O′′ (y) with O′(y) to obtain: 

O′′ (x) ≤ 1 + X 

> y / ∈M∪L

P (x, y )O′′ (x) + X

> y∈M

P (x, y )( Q(x, y )O′(y) + (1 − Q(x, y )) O′′ (x)) + X

> y∈L

P (x, y )O′(y).

Rewriting (5), we have: 

O′(x) = 1 + X 

> y / ∈M∪L

P (x, y )O′(x) + X

> y∈M

P (x, y )( Q(x, y )O′(x) + (1 − Q(x, y )) O′(x)) + X

> y∈L

P (x, y )O′(y).

14 Caterpillar of Thoughts 

Combining the two implies O′′ (x) ≤ O′(x), which concludes the proof of the induction. This yields the desired bound of O′′ (x) ≤ 2O(x) on the number of steps. Furthermore, each step uses O( λ2 

> ε2

log 1 

> δ

) = O(λ2 log O(x0)) Laplacian approximations. Therefore, the total cost is O(λ2 · O(x0) log O(x0)) . In particular, when λ = εO(x0), the total cost is 

O(ε2 · O(x0)3 log O(x0)) . This completes the proof. We also remark that when an upper bound N is not available for O(x0), a scaled-up approximation of 2bO(x0) can be used for each run. 

Lemma 4.7. For every state x, it holds that O(x) = ∆ · dist (x, z ), where dist (x, z ) denotes the length of the shortest path between x and z in the tree. Proof. The optimal hitting time is characterized by Theorem 1.1. Take any non-leaf state x, and let N (x) be its neighbors in the tree. Since the underlying graph is a tree, there exists a neighbor y∗ ∈ N (x) such that dist (x, z ) = dist (y, z ) + 1 , and for any other neighbor y̸ = y∗ it holds that dist (x, z ) = dist (y, z ) − 1. Note that any path starting x or any other neighbor in 

N (x) \ { y∗} and ending in z, must go through y∗. Therefore, it holds O(x) ≥ O(y∗), and O(y) ≥ O(y∗) for any neighbor 

y̸ = y∗.Based on this, we can invoke Theorem 1.1. There exists an optimal non-branching algorithm that operates as follows: 1. Maintain a “minimizer” state x∗, initially equal to x0.2. Repeatedly rewind to x∗ to obtain a state y, until the new state satisfies dist (y, z ) < dist (x∗, z ).3. If y is equal to the target state z, halt. Otherwise, let x∗ := y and go back to step 2. Equivalently, the algorithm moves on the shortest path from x0 to z. At each vertex x, it repeatedly generates neighbors until the next vertex on the shortest path is drawn. Since the next vertex on the shortest path is drawn with probability 1/∆,this takes ∆ steps in expectation, and the entire process lasts ∆ · dist (x0, z ) in expectation. 

Theorem 4.6. Given a parameter ε < 1, there exists a partially observable Markov chain M = (Ω , P ), with observations 

bO(x) ∈ (1 ± ε)O(x) for any state x, such that any algorithm that can only access the observations requires exp( O(x0)) 

steps to reach the target, where O(x0) can be taken arbitrarily large. Proof. We let ∆ = 2 1/ε , so that O(x0) = 2 1/ε 10 d, and d can be set arbitrarily large. By Yao’s minimax lemma, it suffices to present an input distribution, such that any deterministic algorithm that reaches the target state with at least constant probability uses at least Ω(2 O(x0)) queries. In fact, we show that this many queries is necessary for reaching xεd with constant probability, where recall that x0, . . . , x 10 d is the path starting at the initial state x0 and ending at the target state 

x10 d = z.We construct the input distribution based on the Markov chain outlined in Section 4.2. The structure of the tree and the node labels are fixed, then one of the leaves is chosen uniformly at random to be the target state z, and the observations are set accordingly. The observations in the shallow part of the tree (i.e., among the vertices within distance εd of the root) the observations bO(·) are symmetric, in the sense that any two states of the same depth have the same observation. Furthermore, considering any two nodes at depth εd , the observations in the two subtrees are identical, except for the node xεd . We refer to the nodes at depth εd as the middle nodes, and call xεd the special middle node. As such, the observations made within depth εd reveal nothing about the location of the xεd , and any observations made in the subtree of a middle node s only reveals whether s is special or not, and reveals no further information about the location of xεd relative to s.At any point in the run time of the algorithm, we say that a middle node has been revealed if any observation is made in its subtree. By the previous paragraph, conditioned on the observations made so far, if none of the revealed vertices is special (i.e., if xεd has not been reached), then the special node could be any of the remaining middle nodes uniformly at random. As such, any algorithm that reveals a c-fraction of the middle nodes, reaches xεd with a probability of at most c.Therefore, any algorithm that reaches the target state z (and hence the special middle node xεd ) with constant probability, must reveal at least Ω(∆ εd ) = Ω(exp( O(x0)) middle nodes, and as a result, take at least as many steps. This concludes the proof. 15