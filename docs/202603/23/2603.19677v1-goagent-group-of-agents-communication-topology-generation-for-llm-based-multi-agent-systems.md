---
title: "GoAgent: Group-of-Agents Communication Topology Generation for LLM-based Multi-Agent Systems"
title_zh: GoAgent：基于大语言模型的多智能体系统代理组通信拓扑生成
authors: "Hongjiang Chen, Xin Zheng, Yixin Liu, Pengfei Jiao, Shiyuan Li, Huan Liu, Zhidong Zhao, Ziqi Xu, Ibrahim Khalil, Shirui Pan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19677v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: LLM多智能体系统的通信拓扑生成
tldr: 针对大模型多智能体系统在复杂任务中通信拓扑构建不佳的问题，本文提出 GoAgent。不同于以往以节点为中心的拓扑生成，GoAgent 将协作小组视为基本构建单元，通过显式建模小组结构并利用条件信息瓶颈（CIB）优化组间通信，在提升任务协调能力的同时显著降低了通信冗余，实现了性能与效率的双重提升。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通信拓扑生成多采用以节点为中心的方式，缺乏对协作小组结构的显式建模，导致协调效率低下且通信冗余。
method: 提出 GoAgent 方法，将协作小组视为构建单元，通过 LLM 枚举候选小组并自动回归地连接它们，同时引入条件信息瓶颈（CIB）来压缩组间通信。
result: "在六个基准测试中达到 93.84% 的平均准确率，并减少了约 17% 的 Token 消耗。"
conclusion: GoAgent 通过显式建模小组结构和优化信息流，显著提升了多智能体系统的任务处理能力和通信效率。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在解决复杂任务方面展现出了卓越的能力，但其有效性在很大程度上取决于协调智能体交互的底层通信拓扑。在这些系统中，成功解决问题通常需要特定于任务的组结构来对子任务进行分而治之。然而，大多数现有方法以“以节点为中心”的方式生成通信拓扑，使得组结构从局部连接决策中隐式出现，而非显式建模，这往往导致协调效果不佳以及不必要的通信开销。为了解决这一局限性，我们提出了 GoAgent（Group-of-Agents），这是一种将协作组显式视为 MAS 构建原子单元的通信拓扑生成方法。具体而言，GoAgent 首先通过 LLM 枚举与任务相关的候选组，然后自回归地选择并连接这些组作为原子单元，以构建最终的通信图，从而共同捕捉组内凝聚力和组间协调。为了减轻扩展拓扑中固有的通信冗余和噪声传播，我们进一步引入了条件信息瓶颈（CIB）目标，用于压缩组间通信，在保留任务相关信号的同时过滤掉冗余的历史噪声。在六个基准测试上的广泛实验表明，GoAgent 具有最先进的性能，平均准确率达到 93.84%，同时减少了约 17% 的 token 消耗。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) have demonstrated exceptional capabilities in solving complex tasks, yet their effectiveness depends heavily on the underlying communication topology that coordinates agent interactions. Within these systems, successful problem-solving often necessitates task-specific group structures to divide and conquer subtasks. However, most existing approaches generate communication topologies in a node-centric manner, leaving group structures to emerge implicitly from local connectivity decisions rather than modeling them explicitly, often leading to suboptimal coordination and unnecessary communication overhead. To address this limitation, we propose GoAgent (Group-of-Agents), a communication topology generation method that explicitly treats collaborative groups as the atomic units of MAS construction. Specifically, GoAgent first enumerates task-relevant candidate groups through an LLM and then autoregressively selects and connects these groups as atomic units to construct the final communication graph, jointly capturing intra-group cohesion and inter-group coordination. To mitigate communication redundancy and noise propagation inherent in expanding topologies, we further introduce a conditional information bottleneck (CIB) objective that compresses inter-group communication, preserving task-relevant signals while filtering out redundant historical noise. Extensive experiments on six benchmarks demonstrate the state-of-the-art performance of GoAgent with 93.84% average accuracy while reducing token consumption by about 17%.

---

## 论文详细总结（自动生成）

以下是对论文《GoAgent: Group-of-Agents Communication Topology Generation for LLM-based Multi-Agent Systems》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：基于大语言模型（LLM）的多智能体系统（MAS）在处理复杂任务时，其性能高度依赖于智能体之间的**通信拓扑结构**。
*   **核心问题**：现有的拓扑生成方法大多是“以节点为中心”的（Node-centric），即关注单个智能体之间的连接。这种方式导致协作小组（Group）的结构是隐式生成的，往往造成协调效率低下、通信冗余严重以及噪声传播。
*   **研究动机**：复杂任务通常需要“分而治之”的策略，即由特定的小组负责子任务。因此，论文提出应将**协作小组**视为构建 MAS 的原子单元，而非单个智能体。

### 2. 论文提出的方法论：GoAgent
GoAgent 框架主要包含三个核心阶段：
*   **候选组枚举（Group Enumeration）**：利用 LLM 的先验知识，针对特定任务枚举出所有可能的、具有特定功能的候选智能体小组。
*   **基于组的拓扑生成（Group-based Topology Generation）**：
    *   不再逐个连接节点，而是将小组作为原子单元。
    *   采用**自回归（Autoregressive）**方式，从候选池中选择合适的小组并建立组间连接，从而构建最终的通信图。这种方式能同时兼顾组内的高凝聚力和组间的有效协调。
*   **条件信息瓶颈（Conditional Information Bottleneck, CIB）**：
    *   为了解决拓扑扩大带来的通信冗余，引入 CIB 目标函数。
    *   **核心逻辑**：在保留与任务相关的关键信号的同时，压缩组间传递的信息，过滤掉历史对话中的冗余噪声，从而优化信息流。

### 3. 实验设计
*   **数据集/场景**：在 **6 个基准测试**上进行了评估（涵盖了复杂推理、代码生成、数学问题等典型 MAS 任务场景）。
*   **对比方法（Baselines）**：对比了当前主流的多智能体框架和拓扑生成方法（如以节点为中心的动态生成方法、全连接结构等）。
*   **评价指标**：主要考察任务解决的**准确率（Accuracy）**和**通信成本（Token 消耗量）**。

### 4. 资源与算力
*   **算力说明**：论文摘要及核心内容中**未明确提及**具体的 GPU 型号（如 A100/H100）、算卡数量或具体的训练时长。
*   **实现细节**：通常此类基于 LLM 的拓扑生成实验依赖于 OpenAI API（如 GPT-4）或开源模型（如 Llama 系列）的推理，重点在于算法逻辑而非大规模预训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同的基准数据集，并进行了广泛的对比实验。
*   **消融实验**：论文提到了对组结构建模和 CIB 压缩机制的有效性验证。
*   **充分性评价**：实验设计较为全面，不仅关注性能提升（准确率），还深入探讨了通信效率（Token 减少），从多维度证明了方法的优越性，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：GoAgent 在六个基准测试中达到了 **93.84%** 的平均准确率，刷新了 SOTA（当前最佳）记录。
*   **效率提升**：通过 CIB 机制和优化的组结构，成功减少了约 **17%** 的 Token 消耗，证明了“以组为中心”的架构能有效抑制通信冗余。
*   **结构优势**：显式建模小组结构比隐式演化出的结构更能适应复杂任务的协作需求。

### 7. 优点与亮点
*   **视角创新**：打破了传统的“节点连接”思维，提出“以组为原子单元”构建 MAS，更符合人类社会“分工协作”的逻辑。
*   **理论结合**：引入信息论中的“信息瓶颈（IB）”概念来解决 MAS 的通信冗余问题，具有较强的理论支撑。
*   **双重优化**：同时实现了任务处理能力的增强和运行成本的降低。

### 8. 不足与局限
*   **初始依赖**：系统性能可能高度依赖于第一阶段 LLM 枚举候选组的质量，如果 LLM 无法准确识别任务所需的潜在小组，后续生成的拓扑可能受限。
*   **计算开销**：虽然通信时的 Token 减少了，但自回归生成拓扑的过程本身可能增加额外的推理开销。
*   **动态性限制**：目前的方法似乎更倾向于在任务开始前或初期确定拓扑，对于任务执行过程中极度动态变化的实时调整能力尚待进一步验证。

（完）
