---
title: Breaking the Capability Ceiling of LLM Post-Training by Reintroducing Markov States
title_zh: 通过重新引入马尔可夫状态打破大语言模型后训练的能力天花板
authors: "Yurun Yuan, Tengyang Xie"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19987v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大模型后训练的强化学习范式
tldr: 针对大语言模型（LLM）后训练中强化学习（RL）仅能优化现有模式而无法发现新策略的“能力天花板”问题，本文提出重新引入马尔可夫状态。研究指出，当前LLM训练依赖不断扩展的历史记录，而经典RL则依赖紧凑的状态表示。通过理论证明和逻辑谜题实验，本文展示了马尔可夫状态能显著降低样本复杂度并突破性能边界，为实现生成式AI的开放式探索和新推理能力提供了新路径。
selection_source: fresh_fetch
motivation: 现有的LLM强化学习后训练受限于“历史即状态”的建模方式，导致模型只能提炼预训练模式而难以发现全新的解决策略。
method: 借鉴经典强化学习原则，在LLM后训练中引入显式的马尔可夫状态表示，以替代不断增长的动作历史。
result: 理论证明了估计马尔可夫状态可降低样本复杂度，实验表明该方法在复杂逻辑谜题任务中一致突破了标准RL的性能上限。
conclusion: 转向结构化的马尔可夫表示而非单纯依赖历史建模，是解锁大模型开放式发现和全新推理能力的关键。
---

## 摘要
强化学习（RL）已成为大语言模型（LLM）后训练和对齐的标准范式，但最近的证据表明它面临着一个持久的“能力天花板”：与能够发现新颖策略的经典强化学习系统不同，针对 LLM 的强化学习通常仅作为预训练权重中潜在模式的精炼器。在这项工作中，我们确定了一个根本性的结构瓶颈：经典强化学习依赖于紧凑且信息丰富的马尔可夫状态，而当前的 LLM 后训练公式则受限于不断扩展的动作历史。我们重新审视了一个长期以来作为强化学习核心但在 LLM 后训练中缺失的经典原则：显式马尔可夫状态。在理论上，我们提供了严格的保证，证明利用估计的马尔可夫状态可以显著降低样本复杂度。在实验上，我们展示了引入马尔可夫状态在一系列复杂逻辑谜题中持续打破了标准强化学习后训练的性能边界。我们的研究结果表明，超越“历史即状态”建模，转而采用结构化的马尔可夫表示，对于解锁生成式人工智能中的开放式发现和真正全新的推理能力至关重要。

## Abstract
Reinforcement learning (RL) has become a standard paradigm for post-training and aligning Large Language Models (LLMs), yet recent evidence suggests it faces a persistent "capability ceiling": unlike classical RL systems that discover novel strategies, RL for LLMs often acts as a mere refiner of patterns already latent in pre-trained weights. In this work, we identify a fundamental structural bottleneck: while classical RL relies on compact, informative Markov states, current LLM post-training formulations are tethered to an ever-expanding history of actions.   We revisit a classical principle long central to RL yet absent from LLM post-training: explicit Markov states. Theoretically, we provide rigorous guarantees demonstrating that leveraging estimated Markov states can significantly reduce sample complexity. Empirically, we show that introducing Markov states consistently breaks the performance boundaries of standard RL post-training across a suite of complex logic puzzles. Our findings suggest that moving beyond "history-as-state" modeling in favor of structured Markovian representations is essential for unlocking open-ended discovery and genuinely new reasoning capabilities in Generative AI.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何通过引入经典的“马尔可夫状态”概念，打破大语言模型（LLM）在强化学习（RL）后训练阶段面临的性能瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 RL 已成为 LLM 后训练（如对齐和推理增强）的标准手段，但研究发现它存在“能力天花板”——RL 往往只是在精炼预训练阶段已有的模式，而无法像 AlphaZero 在围棋中那样发现全新的策略。
*   **核心瓶颈**：作者指出，当前 LLM 的 RL 建模将“整个动作历史”视为状态（History-as-State）。随着推理步骤增加，状态空间呈指数级膨胀且包含大量噪声，导致搜索效率极低。
*   **整体含义**：论文主张回归经典 RL 的核心原则，即使用紧凑、信息充足的**显式马尔可夫状态**（Explicit Markov States）来替代冗长的历史记录，从而降低学习复杂度，解锁模型的发现能力。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将 LLM 的决策过程从“基于历史序列”转变为“基于当前状态”。即在每一步决策时，模型只接收当前状态 $s_h$ 作为输入，而不是之前所有 token 的累积。
*   **关键技术流程**：
    1.  **状态转移估计**：引入一个状态转移函数 $P(s_h, a_h) \to s_{h+1}$。在实际应用中，这可以是一个外部环境（如代码编译器）、规则引擎或一个专门训练的“世界模型”（State Prediction Model）。
    2.  **马尔可夫策略训练**：模型 $\pi(a_h | s_h)$ 仅根据当前状态预测动作。
    3.  **算法实现**：采用 **GRPO**（Group Relative Policy Optimization）作为基础 RL 算法。
*   **理论保证**：论文通过数学证明展示，基于马尔可夫状态的学习可以将样本复杂度从关于路径长度的指数级降低为关于状态空间大小的多项式级。

### 3. 实验设计
*   **实验场景**：选择了三类具有明确马尔可夫性质的复杂逻辑推理任务：**数独（Sudoku）**、**推箱子（Sokoban）**和**不等号谜题（Futoshiki）**。
*   **Benchmark 与对比方法**：
    *   **Action-sequence（基准）**：标准的 LLM RL 方式，输入为完整的动作历史。
    *   **Markov（本文方法）**：输入仅为当前的棋盘/游戏状态。
    *   **State-action-sequence（消融对比）**：输入包含历史状态和动作的序列，用于验证“马尔可夫性质”本身的贡献。
*   **评估指标**：Avg@128（128次采样的平均成功率）和 Pass@128（128次采样中至少对一次的概率）。

### 4. 资源与算力
*   **算力平台**：使用了美国国家超级计算应用中心（NCSA）的 **DeltaAI 系统**。
*   **具体配置**：文中提到了使用 **rLLM** 和 **VERL** 框架进行训练，但**未明确说明具体的 GPU 型号（如 H100/A100）数量及总训练时长**。
*   **模型规模**：实验基于 **Qwen3-4B** 和 **Qwen2.5-3B-Instruct** 进行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同的逻辑任务。
    *   使用了 2 种不同系列的基座模型。
    *   进行了**分布内（ID）**和**分布外（OOD，更难、步骤更长）**的泛化性测试。
    *   包含了关于 SFT 预热程度、状态依赖性、以及使用地面真值优势函数（$A^*$）的消融实验。
*   **充分性评价**：实验设计较为充分，通过对比“状态+历史”与“纯状态”模型，客观地证明了马尔可夫结构在降低方差和提高泛化性方面的优势。

### 6. 主要结论与发现
*   **突破天花板**：马尔可夫模型在 Action-sequence 模型失败或陷入停滞的任务上（如推箱子）取得了极高的成功率。
*   **卓越的泛化性**：在 OOD 测试中，马尔可夫模型表现出极强的鲁棒性，能够解决比训练时更复杂的谜题。
*   **高样本效率**：马尔可夫模型在更少的训练步数内达到了更高的奖励值，验证了理论上的复杂度降低。
*   **Pass@k 提升**：不同于标准 RL 往往无法提升基座模型的 Pass@k 上限，本文方法显著抬高了这一边界。

### 7. 优点
*   **理论与实践结合**：不仅有严谨的样本复杂度证明，还在具体任务上实现了显著的性能飞跃。
*   **诊断精准**：深刻指出了当前 LLM RL 训练中“历史即状态”这一被忽视的结构性缺陷。
*   **即插即用**：该范式可以扩展到代码调试、数学证明等具有自然状态转移的现实场景。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在规则明确的合成逻辑谜题上，对于状态定义模糊的开放式创意写作或通用对话，如何定义和提取“马尔可夫状态”仍是挑战。
*   **依赖转移模型**：如果外部环境不可用，则需要训练一个状态预测模型，该模型的误差可能会随步骤累积（论文理论部分提到了 $O(H^3 \epsilon_P)$ 的误差界限）。
*   **推理模式限制**：为了验证纯状态的效果，实验中禁用了思维链（CoT），这在某些需要复杂内部推理的任务中可能会限制模型的表达力。

（完）
