---
title: "Adaptive Layerwise Perturbation: Unifying Off-Policy Corrections for LLM RL"
title_zh: 自适应逐层扰动：统一大语言模型强化学习中的离策修正
authors: "Chenlu Ye, Xuanchang Zhang, Yifan Hao, Zhou Yu, Ziji Zhang, Abhinav Gullapalli, Hao Chen, Jing Huang, Tong Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19470v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 统一LLM强化学习的离策修正
tldr: 针对大语言模型强化学习（LLM RL）中因策略陈旧和分布偏移导致的训练不稳定问题，本文提出了一种自适应逐层扰动（ALP）方法。该方法通过在模型各层的隐藏状态中注入可学习的微小扰动，有效平滑了策略分布，缓解了重要性采样比率的重尾现象。实验证明，ALP在数学和工具调用任务中显著提升了性能，增强了探索能力，并确保了训练过程的稳定性。
selection_source: fresh_fetch
motivation: 解决LLM强化学习中由于推理与更新策略间分布差距过大导致的重要性采样比率重尾及训练不稳定性问题。
method: 在模型更新期间，向每一层输入的隐藏状态中注入可学习的微小扰动，以平滑策略分布并缩小策略间隙。
result: 在单轮数学和多轮工具推理任务中，ALP显著提升了最终性能，并有效抑制了KL散度激增和重要性比率爆炸。
conclusion: 逐层表示级的扰动比局部或仅Logits扰动更有效，是统一解决LLM离策修正、提升训练稳定性的有效方案。
---

## 摘要
离策（Off-policy）问题，如策略陈旧和训练-推理不匹配，已成为大语言模型强化学习（LLM RL）训练稳定性和进一步探索的主要瓶颈。为了提高推理效率，推理策略与更新策略之间的分布差距不断扩大，导致重要性采样比率呈现重尾分布。当策略在局部过于尖锐时，会出现重尾比率，这会进一步放大尖锐梯度，并可能使更新超出置信区域。针对这一问题，我们提出了自适应逐层扰动（ALP）。该方法在更新过程中向每一层的输入隐藏状态注入微小的可学习扰动，并将其作为目标函数中重要性采样比率的分子，与保持不变的推理策略进行对比。直观而言，通过向中间表示添加受控噪声，ALP 防止了更新后的策略与推理策略发生过于剧烈的偏离，并扩大了策略族，使其能够覆盖带有不匹配噪声的推理策略族。因此，平滑后的分布可以自然地缩小更新策略与推理策略之间的差距，并减少重要性采样比率的尾部，从而维持训练稳定性。这一点已在实验中得到进一步验证。在单轮数学和多轮工具集成推理任务上的实验表明，ALP 不仅提高了最终性能，还避免了迭代训练过程中重要性采样比率尾部的爆炸和 KL 散度激增，同时增强了探索能力。消融实验表明，跨所有层的表示级扰动最为有效，显著优于部分层扰动和仅 Logits 扰动的变体。

## Abstract
Off-policy problems such as policy staleness and training-inference mismatch, has become a major bottleneck for training stability and further exploration for LLM RL. To enhance inference efficiency, the distribution gap between the inference and updated policy grows, leading to heavy-tailed importance ratios. Heavy-tailed ratios arise when the policy is locally sharp, which further inflates sharp gradients and can push updates outside the trust region. To address this, we propose Adaptive Layerwise Perturbation(ALP) by injecting small learnable perturbations into input hidden states of each layer during updates, which is used as the numerator of the importance ratio against the unchanged inference policy in the objective. Intuitively, by adding controlled noise to intermediate representations, ALP prevents the updated policy from deviating too sharply from the inference policy, and enlarges the policy family to cover the inference policy family with mismatch noises. Hence, the flattened distribution can naturally tighten the updated and inference policy gap and reduce the tail of importance ratios, thus maintaining training stability. This is further validated empirically. Experiments on single-turn math and multi-turn tool-integrated reasoning tasks show that ALP not only improves final performance, but also avoid blow up of importance ratio tail and KL spikes during iterative training, along with boosted exploration. Ablations show that representation-level perturbations across all layers are most effective, substantially outperforming partial-layer and logits-only variants.

---

## 论文详细总结（自动生成）

### 论文总结：Adaptive Layerwise Perturbation (ALP)

#### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的强化学习（RL）中，**离策（Off-policy）不稳定性**是一个核心瓶颈。这种不稳定性主要源于两个方面：
*   **策略陈旧（Policy Staleness）**：为了提高效率，同一批轨迹通常被用于多次策略更新。
*   **训练-推理不匹配（Training-Inference Mismatch）**：现代推理引擎（如 vLLM、SGLang）由于量化、批处理差异或内核优化，导致推理时的分布与训练时的分布存在微小但显著的偏差。
这些因素会导致**重要性采样比率（Importance Ratio）出现重尾分布**，进而引发梯度爆炸、KL 散度激增和训练崩溃。论文旨在提出一种统一的框架来修正这些离策偏差，同时保持训练的鲁棒性。

#### 2. 论文提出的方法论
论文提出了 **自适应逐层扰动（Adaptive Layerwise Perturbation, ALP）**：
*   **核心思想**：在模型更新期间，向每一层 Transformer 的输入隐藏状态中注入微小的、可学习的高斯扰动 $\delta \sim N(0, \sigma^2 I)$。
*   **关键技术细节**：
    *   **扰动注入**：扰动仅应用于训练更新时的策略（分子），而推理策略（分母）保持不变。
    *   **统一比率**：通过这种方式，ALP 将“策略陈旧”和“系统不匹配”统一到一个单一的重要性比率中，避免了传统方法（如 MIS/TIS）需要处理多个截断阈值的复杂性。
    *   **理论支撑**：论文证明了当扰动标准差 $\sigma$ 能够覆盖推理系统的偏差时，训练与推理分布之间的 KL 散度是有界的。此外，扰动起到了“随机平滑”的作用，使优化景观更平滑，防止模型陷入尖锐且脆弱的局部最优解。

#### 3. 实验设计
*   **场景与数据集**：
    *   **单轮数学推理**：使用 Guru RL-92k 和 OpenR1 子集进行训练。
    *   **多轮工具集成推理 (TIR)**：使用 Python 解释器解决复杂数学问题，数据集整合了 SimpleRL、Deepscaler 和 rStar2-Agent。
*   **Benchmark**：Math500、Minerva Math、Olympiad Bench、AIME2024、AIME2025。
*   **对比方法**：
    *   **GRPO**：标准的组相对策略优化。
    *   **Seq-Bypass**：直接使用训练/推理比率而不做修正的基准。
    *   **MIS (Masked Importance Sampling)**：通过掩码处理训练-推理不匹配的现有先进方法（分为 Token 级和 Seq 级）。

#### 4. 资源与算力
*   **硬件设备**：实验使用了 **8 台 H100 GPU 和 4 台 A100 GPU**。
*   **训练细节**：基于 `verl` 框架，采用 FSDP（全分片数据并行）。单轮任务中每步采样 512 个 Prompt，每个 Prompt 生成 8 个响应。
*   **时长**：文中未明确给出总训练小时数，但展示了长达 10,000 步的训练动态曲线。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B (Qwen2.5-Math) 到 7B (Qwen2.5) 不同规模的模型。
*   **消融实验**：非常充分。研究了扰动层的位置（全层、前层、中层、后层、仅 Logits）、扰动规模（std）以及学习率的敏感性。
*   **客观性**：通过展示训练全过程的 KL 散度、熵、梯度范数和奖励曲线，客观地证明了 ALP 在长时间迭代中的稳定性优势，而非仅仅是最终分数的提升。

#### 6. 主要结论与发现
*   **性能领先**：ALP 在单轮和多轮任务中均优于所有基准方法（如在 AIME24 上显著超过 MIS）。
*   **稳定性增强**：ALP 有效抑制了训练后期的 KL 激增和奖励崩塌，保持了健康的策略熵。
*   **探索能力**：Pass@k 曲线显示，ALP 产生的响应多样性更高，更有利于在复杂推理任务中发现正确路径。
*   **层级效应**：**全层扰动效果最佳**；在局部扰动中，底层（靠近输入）的扰动比顶层（靠近输出）更有效，而仅在 Logits 加噪声效果最差。

#### 7. 优点
*   **简洁统一**：用一个简单的扰动机制替代了复杂的双重比率截断和多阈值调优。
*   **理论与实践结合**：不仅有经验公式，还提供了关于损失函数平滑度和分布偏差界限的数学证明。
*   **鲁棒性强**：对超参数（如初始噪声标准差）不敏感，易于在不同任务中迁移。

#### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在数学和代码推理等具有明确奖励（Rule-based reward）的任务上，在主观性较强的通用对话或创意写作任务中的表现尚待验证。
*   **计算开销**：虽然扰动注入本身开销极小，但由于引入了额外的可学习参数（$\sigma$），在极大规模模型上的收敛特性还需进一步观察。
*   **异步系统验证**：论文提到未来将在完全异步的 RL 系统中测试 ALP，目前尚不清楚在极端延迟下的表现。

（完）
