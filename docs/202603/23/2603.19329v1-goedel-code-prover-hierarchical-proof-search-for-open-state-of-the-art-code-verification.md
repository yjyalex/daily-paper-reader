---
title: "Goedel-Code-Prover: Hierarchical Proof Search for Open State-of-the-Art Code Verification"
title_zh: Goedel-Code-Prover：面向开放最先进代码验证的层级化证明搜索
authors: "Zenan Li, Ziran Yang, Deyuan, He, Haoyu Zhao, Andrew Zhao, Shange Tang, Kaiyu Yang, Aarti Gupta, Zhendong Su, Chi Jin"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.19329v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 分解分数作为分层证明搜索的训练奖励
tldr: "针对大语言模型生成代码缺乏正确性保证的问题，本文提出了一种基于 Lean 4 的分层证明搜索框架 Goedel-Code-Prover。该框架通过将复杂验证目标分解为结构化子目标，并利用结合了建设性证明与结构有效性的分解评分进行强化学习训练。实验表明，8B 参数的模型在多个基准测试中达到了 62.0% 的证明成功率，显著超越了规模更大的现有模型，展示了极高的效率和推理扩展性。"
selection_source: fresh_fetch
motivation: 现有的 LLM 难以自动生成复杂的机器可验证证明，缺乏有效的代码正确性保障手段。
method: 提出一种分层搜索框架，利用统一的 8B 模型进行目标分解与证明补全，并通过结合分解评分的混合强化学习进行优化。
result: "在三个 Lean 验证基准测试中取得 62.0% 的成功率，比最强基准提升 2.6 倍，性能优于规模大得多的神经证明器。"
conclusion: 分层分解与强化学习的结合能显著提升代码验证的自动化水平，且模型在推理阶段表现出良好的扩展性。
---

## 摘要
大语言模型（LLMs）能够生成看似合理的代码，但在正确性保证方面能力有限。形式化验证实现是否满足规范需要构建机器可检查的证明，这一任务目前仍超出了自动化处理的范畴。我们提出了一种用于 Lean 4 自动化代码验证的层级化证明搜索框架，该框架在尝试策略级（tactic-level）证明之前，将复杂的验证目标分解为结构上更简单的子目标。我们方法的核心是一个基于原则的分解评分，它结合了构造性论证与结构有效性。至关重要的是，该评分既作为训练奖励，也作为推理时的排序准则，确保了优化与部署之间的严格对齐。我们通过监督初始化及随后的混合强化学习训练了 Goedel-Code-Prover-8B，这是一个用于分解和补全的统一策略；其中，连续的分解奖励驱动规划探索，而监督回放则稳定了证明生成。在包含 427 个任务的三个基于 Lean 的代码验证基准测试中，我们的 8B 参数模型实现了 62.0% 的证明成功率，比最强基准提高了 2.6 倍，超越了规模大至 84 倍的神经证明器。我们进一步观察到了持续的推理时间扩展性：成功率随搜索迭代次数和采样预算单调提升，且我们训练的模型比同等规模的前沿现成模型具有更高的效率。

## Abstract
Large language models (LLMs) can generate plausible code but offer limited guarantees of correctness. Formally verifying that implementations satisfy specifications requires constructing machine-checkable proofs, a task that remains beyond current automation. We propose a hierarchical proof search framework for automated code verification in Lean~4 that decomposes complex verification goals into structurally simpler subgoals before attempting tactic-level proving. Central to our approach is a principled decomposition score that combines constructive justification with structural effectiveness. Crucially, this score serves as both the training reward and the inference-time ranking criterion, ensuring strict alignment between optimization and deployment. We train Goedel-Code-Prover-8B, a single unified policy for both decomposition and completion, via supervised initialization followed by hybrid reinforcement learning, where a continuous decomposition reward drives planning exploration while supervised replay stabilizes proof generation. On three Lean-based code verification benchmarks comprising 427 tasks, our 8B-parameter model achieves a 62.0\% prove success rate, a 2.6$\times$ improvement over the strongest baseline, surpassing neural provers up to 84$\times$ larger. We further observe consistent inference-time scaling: success rates improve monotonically with search iterations and sampling budget, with our trained model achieving greater efficiency than frontier off-the-shelf models of comparable scale.

---

## 论文详细总结（自动生成）

以下是对论文《Goedel-Code-Prover: Hierarchical Proof Search for Open State-of-the-Art Code Verification》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）在代码生成方面表现出色，但其生成的代码往往缺乏正确性保证。形式化验证（如使用 Lean 4）可以提供机器可检查的证明，但自动生成这些复杂的证明对现有模型而言极具挑战。
*   **研究背景**：现有的数学定理证明器难以直接迁移到代码验证领域，主要存在三个挑战：
    1.  **无根据的分解**：代码验证缺乏像数学教材那样的自然语言推理语料，导致模型难以提出有效的中间引理。
    2.  **概念激增**：每个程序都会引入新的函数定义和数据结构，缺乏像数学库（如 Mathlib）那样的闭环词汇表。
    3.  **策略分布偏移**：代码验证所需的策略（如 `grind`、`native_decide`）与数学证明中的代数策略显著不同。

### 2. 方法论：核心思想与技术细节
该论文提出了一种**层级化证明搜索框架**，将验证任务分为“规划（分解）”和“证明（补全）”两个阶段：
*   **层级化搜索流程**：
    1.  **引理分解（Decomposition）**：递归地将顶层验证目标分解为结构更简单的子引理。
    2.  **引理补全（Completion）**：通过迭代生成策略代码并利用 Lean 编译器的反馈来证明每个叶子引理。
*   **核心技术：分解评分（Decomposition Score $S$）**：
    *   该评分由**构造性论证（Validity, $v$）**和**结构有效性（Reduction, $r$）**乘积组成。
    *   **$v$**：要求模型生成证明项以确保子引理能逻辑推导出父目标，并通过 `QuickCheck` 进行自动反例搜索以剔除无效引理。
    *   **$r$**：基于 AST 的“算子足迹（Operator Footprint）”衡量目标复杂度，鼓励将复杂目标简化为更小的算子集合。
*   **混合强化学习（Hybrid RL）**：
    *   使用 **GRPO** 算法，以分解评分作为连续奖励来驱动规划探索。
    *   使用**监督回放（Supervised Replay）**来稳定证明补全阶段的训练，解决证明成功/失败信号过于稀疏的问题。

### 3. 实验设计
*   **数据集/基准测试**：使用了三个 Lean 4 代码验证基准，共 427 个任务：
    1.  **Verina**：入门级编程练习。
    2.  **Clever**：精心设计的形式化规范基准。
    3.  **AlgoVeri**：经典算法的跨语言验证基准。
*   **对比方法**：
    *   **前沿推理模型**：GPT-5.2-Pro、GPT-5.3-Codex、Claude-Opus-4.6、Gemini-3-Flash、DeepSeek-V3.2。
    *   **专业神经证明器**：DeepSeek-Prover-V2 (671B)、Goedel-Prover-V2 (32B)、Kimina-Prover (72B)、BFS-Prover-V2 (32B)。

### 4. 资源与算力
*   **硬件资源**：使用了 **16 张 GPU**（4个节点，每节点4张，具体型号未在正文详述，但提到使用了 `verl` 框架和 `Ray` 分布式管理）。
*   **训练细节**：RL 阶段进行了 100 个训练步，每步包含 64 个 prompt，每个 prompt 生成 8 个样本。
*   **推理开销**：每个问题分配 30 分钟墙钟时间，分解阶段最多 128 次迭代，补全阶段每引理最多 128 次尝试。

### 5. 实验数量与充分性
*   **实验规模**：在 427 个任务上进行了全面评估，涵盖了从简单函数到复杂算法的多种场景。
*   **消融实验**：对比了“仅分解”、“仅补全训练”、“不同基座模型组合”的效果，验证了层级化设计的必要性。
*   **客观性**：通过 Pass@k 曲线展示了推理时间的扩展性（Scaling Laws），并分析了分解评分与最终证明成功率的相关性（AUROC = 0.903），证明了评分机制的有效性。实验设计较为公平，基准模型均给予了充足的采样预算（Pass@128）。

### 6. 主要结论与发现
*   **性能飞跃**：8B 参数的 Goedel-Code-Prover 达到了 **62.0%** 的总成功率，比最强基准（BFS-Prover-V2）提升了 **2.6 倍**。
*   **以小博大**：该 8B 模型超越了规模大其 84 倍的 671B 模型，证明了针对性训练和层级搜索比单纯增加参数量更有效。
*   **推理扩展性**：成功率随搜索迭代和采样预算的增加而单调提升，且未见饱和迹象。
*   **深度推理**：生成的证明平均包含 8-17 个引理，代码量常超过 130 行，最长可达 680 行，显示了处理复杂逻辑的能力。

### 7. 优点
*   **评分机制科学**：结合了逻辑有效性（QuickCheck）和结构简化（算子计数），为 RL 提供了高质量的密集奖励。
*   **训练策略稳健**：混合 RL 成功平衡了规划的探索性和证明生成的稳定性。
*   **端到端对齐**：训练时的奖励函数与推理时的搜索准则完全一致，确保了模型行为的可预测性。

### 8. 不足与局限
*   **依赖预定义规范**：目前假设程序和规范已经由人工或前沿模型形式化为 Lean 代码，不涉及从自然语言到形式化语言的翻译。
*   **复杂度度量较简单**：使用算子计数作为难度的代理指标可能无法完全捕捉某些深层的语义难度。
*   **跨过程验证限制**：对于具有复杂过程间依赖或超大规模代码库的验证，目前的层级分解可能面临状态爆炸或上下文窗口限制。

（完）
