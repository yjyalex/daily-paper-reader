---
title: "FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization"
title_zh: FIPO：通过受未来 KL 影响的策略优化激发深度推理
authors: "Chiyu Ma, Shuo Yang, Kexin Huang, Jinda Lu, Haoming Meng, Shangshang Wang, Bolin Ding, Soroush Vosoughi, Guoyin Wang, Jingren Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19835v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于激发深度推理和逻辑关键点的强化学习算法
tldr: FIPO是一种旨在提升大语言模型深度推理能力的强化学习算法。针对现有GRPO等方法在结果奖励分配上过于粗粒度、无法区分关键逻辑节点的局限，FIPO引入了折扣未来KL散度来构建稠密优势函数。该方法能有效延长思维链长度并显著提升数学竞赛表现，在Qwen2.5-32B上实现了超越o1-mini的推理性能，为释放模型推理潜力提供了新路径。
selection_source: fresh_fetch
motivation: 现有的结果奖励机制将奖励均匀分配给所有token，难以区分关键逻辑步骤与琐碎信息，限制了复杂推理能力的进一步提升。
method: 提出FIPO算法，通过引入折扣未来KL散度来衡量token对后续轨迹的影响，从而构建更精细的稠密优势函数进行策略优化。
result: "在Qwen2.5-32B上，FIPO将思维链长度显著延长至1万token以上，并将AIME 2024准确率提升至58%，优于o1-mini等模型。"
conclusion: 通过稠密优势表述改进信用分配，是进化结果奖励算法并解锁大模型深度推理能力的关键技术方向。
---

## 摘要
我们提出了受未来 KL 影响的策略优化（Future-KL Influenced Policy Optimization, FIPO），这是一种旨在克服大语言模型推理瓶颈的强化学习算法。虽然 GRPO 风格的训练能够有效扩展，但它通常依赖于基于结果的奖励（ORM），这种奖励将全局优势均匀地分配给轨迹中的每一个 token。我们认为，这种粗粒度的信用分配由于无法区分关键逻辑转折点与普通 token，从而限制了性能上限。FIPO 通过将折扣后的未来 KL 散度纳入策略更新来解决这一问题，从而创建了一种稠密优势公式，根据 token 对后续轨迹行为的影响对其进行重新加权。实验表明，FIPO 使模型能够突破标准基准测试中出现的长度停滞现象。在 Qwen2.5-32B 上的评估显示，FIPO 将平均思维链长度从约 4,000 个 token 扩展到 10,000 个以上，并将 AIME 2024 Pass@1 准确率从 50.0% 提升至峰值 58.0%（最终收敛于约 56.0%）。这一表现优于 DeepSeek-R1-Zero-Math-32B（约 47.0%）和 o1-mini（约 56.0%）。我们的结果表明，建立稠密优势公式是演进基于 ORM 的算法以释放基础模型全部推理潜力的关键路径。我们开源了基于 verl 框架构建的训练系统。

## Abstract
We present Future-KL Influenced Policy Optimization (FIPO), a reinforcement learning algorithm designed to overcome reasoning bottlenecks in large language models. While GRPO style training scales effectively, it typically relies on outcome-based rewards (ORM) that distribute a global advantage uniformly across every token in a trajectory. We argue that this coarse-grained credit assignment imposes a performance ceiling by failing to distinguish critical logical pivots from trivial tokens. FIPO addresses this by incorporating discounted future-KL divergence into the policy update, creating a dense advantage formulation that re-weights tokens based on their influence on subsequent trajectory behavior. Empirically, FIPO enables models to break through the length stagnation seen in standard baselines. Evaluated on Qwen2.5-32B, FIPO extends the average chain-of-thought length from roughly 4,000 to over 10,000 tokens and increases AIME 2024 Pass@1 accuracy from 50.0% to a peak of 58.0% (converging at approximately 56.0\%). This outperforms both DeepSeek-R1-Zero-Math-32B (around 47.0%) and o1-mini (approximately 56.0%). Our results suggest that establishing dense advantage formulations is a vital path for evolving ORM-based algorithms to unlock the full reasoning potential of base models. We open-source our training system, built on the verl framework.

---

## 论文详细总结（自动生成）

这是一份关于论文《FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的强化学习算法（如 GRPO）在提升大模型推理能力时存在“信用分配（Credit Assignment）”过于粗粒度的问题。
*   **研究背景**：GRPO 风格的训练依赖于基于结果的奖励（ORM），它将最终的对/错奖励均匀地分配给轨迹中的每一个 token。这种做法无法区分哪些是关键的逻辑转折点，哪些是无关紧要的填充词，导致模型在推理长度和深度上遇到瓶颈（即“长度停滞”现象）。
*   **整体含义**：论文旨在通过引入一种无需额外评论家模型（Critic Model）的“稠密优势函数”，来打破推理能力的上限，诱导模型产生更长、更具自我反思能力的思维链（CoT）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入**未来 KL 散度（Future-KL）**来重新加权每个 token 的优势值。如果一个 token 能够引导出更符合优化方向的后续轨迹，它应该获得更高的权重。
*   **关键技术细节**：
    *   **概率偏移（$\Delta \log p$）**：计算当前策略与旧策略在 token 级别的对数概率差，作为行为调整的信号。
    *   **未来 KL 估计**：定义为从当前步骤 $t$ 到结束 $T$ 的累积概率偏移。为了处理长序列的不确定性，引入了**折扣因子 $\gamma$**（类似于强化学习中的折扣奖励）。
    *   **稳定性机制**：
        *   **极端值过滤**：屏蔽掉那些触发硬截断（Dual-Clip）的有害 token，防止梯度爆炸。
        *   **影响权重裁剪（Influence Weight Clipping）**：将未来 KL 转换成的乘法系数限制在一定范围内（如 [1.0, 1.2]），确保训练平稳。
    *   **软衰减窗口**：通过超参数 $\tau$ 控制“有效视野”，使模型更关注近期的逻辑连贯性，减少远期噪声干扰。

### 3. 实验设计
*   **基础模型**：Qwen2.5-32B-Base（选择纯净基座模型以观察推理能力的“涌现”，而非微调后的模型）。
*   **数据集**：使用公开的 DAPO-17K 数学数据集。
*   **Benchmark**：主要评估 AIME 2024 和 AIME 2025（高难度数学竞赛）。
*   **对比方法**：
    *   **DAPO**（主要的开源基准）。
    *   **DeepSeek-R1-Zero-Math-32B**。
    *   **OpenAI o1-mini**（作为闭源最强推理模型的参照）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。
*   **框架与配置**：训练基于 `verl` 框架。提到 32B 模型的训练具有“极高的计算成本”。
*   **训练参数**：全局 Batch Size 为 512，每个 Prompt 采样 16 个响应。最大响应长度设为 20,480 token。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 32B 模型上进行了完整的大规模强化学习训练。
    *   在 7B 模型上进行了大量的**消融实验**，包括衰减率 $\tau$ 的选择、过滤机制的有效性、权重裁剪范围等。
*   **充分性与公平性**：实验设计较为严谨。为了确保公平，作者使用了与基准方法（DAPO）完全相同的训练数据和基础优化设置。通过多次重复评估（Pass@32）来降低生成随机性带来的偏差。

### 6. 主要结论与发现
*   **性能突破**：FIPO 在 AIME 2024 上的 Pass@1 达到 58.0%（收敛于 56%），显著超过了 DAPO（50%）和 DeepSeek-R1-Zero-32B（47%），甚至超越了 o1-mini。
*   **长度扩展**：FIPO 成功打破了长度停滞，将平均思维链长度从 4,000 token 提升到了 10,000 token 以上。
*   **行为演进**：模型经历了从“表面规划”到“线性执行”，再到“自发反思”和“系统化深度推理”的四个阶段。
*   **正向循环**：发现更长的正确回答能带来更高的相对优势，从而形成正向激励，促使模型利用更多的“思考空间”来解决复杂问题。

### 7. 优点（亮点）
*   **算法简洁高效**：在不增加 Critic 模型（节省显存和计算开销）的前提下，实现了类似 PPO 的稠密信用分配效果。
*   **揭示了长度与性能的关系**：定量地展示了如何通过算法诱导模型从“直接回答”转向“系统性自我验证”。
*   **开源贡献**：完整开源了训练代码和配置，对开源社区复现类 o1 模型具有极高价值。

### 8. 不足与局限
*   **计算成本高昂**：随着推理长度增加到 1 万 token 以上，推理和训练的开销巨大，难以在资源受限的环境下部署。
*   **任务覆盖面有限**：目前主要集中在数学推理（AIME），尚未在代码、符号逻辑或通用对话领域进行广泛验证。
*   **发现式学习的低效性**：相比于直接从强模型（如 o1）蒸馏思维链，纯 RL 的“自我发现”过程效率较低，且存在性能鸿沟。
*   **模型规模限制**：主要针对基座模型，尚未探讨该方法在已经过长 CoT 蒸馏的模型上是否还有进一步提升空间。

（完）
