---
title: "DS2SC-Agent: A Multi-Agent Automated Pipeline for Rapid Chiplet Model Generation"
title_zh: DS2SC-Agent：一种用于快速 Chiplet 模型生成的多智能体自动化流水线
authors: "Yiwei Wu, Yifan Wu, Yunhao Xiong, Dengwei Zhao, Jiaxuan Shen, Jianfei Jiang, Guanghui He, Shikui Tu, Yanan Sun"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21190v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 使用LLM进行硬件代码生成的自动化多智能体流水线
tldr: 针对传统Chiplet行为级建模耗时且易错的问题，本文提出DS2SC-Agent，这是首个将原始数据手册直接转换为SystemC模型的全自动多智能体流水线。该框架通过解耦复杂建模任务，实现了长文档解析、核心代码构建、测试激励生成及闭环调试的自动化。实验证明，该系统能高效处理模拟、数字及射频领域的复杂手册，生成功能正确的模型，显著提升了敏捷建模效率并减少了人工干预。
selection_source: fresh_fetch
motivation: 传统手动编写Chiplet模型效率低下，且现有LLM框架难以直接处理非结构化、长篇幅的原始数据手册。
method: 提出一种多智能体协作框架，通过解耦建模任务实现从文档解析到代码生成、测试及自适应调试的全流程自动化。
result: 在模拟、数字和射频等多种功能芯片组上的评估显示，该流水线能稳定生成功能正确的SystemC模型。
conclusion: DS2SC-Agent为敏捷构建芯片模型库提供了一种高效可靠的范式，极大减少了人工干预。
---

## 摘要
构建行为级 Chiplet 模型（如 SystemC）对于早期异构架构探索至关重要。传统的建模方式以耗时且易出错而著称。近年来，大语言模型（LLMs）在自动化硬件代码生成方面展现出巨大潜力。然而，现有的 LLM 辅助设计框架主要针对高度结构化或定义明确的设计规范。在实际工程场景中，原始数据手册通常包含冗长、复杂且高度非结构化的信息。因此，直接从这些原始数据手册生成可靠代码面临严峻挑战，包括上下文消失和逻辑幻觉。为了克服这一关键瓶颈，本文提出了 DS2SC-Agent（Datasheet-to-SystemC-Agent）：首个将原始数据手册直接转换为 SystemC Chiplet 模型的端到端全自动化生成流水线。该系统建立了一个高效的多智能体协作框架。通过解耦复杂的建模任务，所提出的流水线编排了一个全自动工作流，涵盖了非结构化长文档解析、SystemC 核心代码构建、测试平台激励生成以及自适应闭环调试。我们在模拟、数字和射频（RF）领域的代表性单功能 Chiplet 上对所提框架进行了全面评估，具体包括限幅放大器（LA）、快速傅里叶变换（FFT）模块和功率放大器（PA）。评估结果表明，我们的流水线能够无缝处理复杂的真实世界数据手册，并持续生成功能正确的 SystemC 模型。这为敏捷模型库构建提供了一种高效且可靠的范式，同时极大地减少了人工干预。

## Abstract
Constructing behavioral-level chiplet models (e.g., SystemC) is crucial for early-stage heterogeneous architecture exploration. Traditional manual modeling is notoriously time-consuming and error-prone. Recently, Large Language Models (LLMs) have demonstrated immense potential in automating hardware code generation. However, existing LLM-assisted design frameworks predominantly target highly structured or well-defined design specifications. In practical engineering scenarios, raw datasheets typically encompass lengthy, complex, and highly unstructured information. Consequently, reliable code generation directly from these raw datasheets suffers from severe challenges, including context vanishing and logical hallucinations.To overcome this critical bottleneck, this paper proposes DS2SC-Agent(Datasheet-to-SystemC-Agent): the first end-to-end, fully automated generation pipeline that translates raw datasheets directly into SystemC chiplet models. This system establishes a highly efficient multi-agent collaborative framework. By decoupling the intricate modeling tasks, the proposed pipeline orchestrates a fully automated workflow encompassing unstructured long-document parsing, SystemC core code construction, testbench stimulus generation, and adaptive closed-loop debugging. We comprehensively evaluate the proposed framework on representative single-function chiplets across the analog, digital, and radio frequency (RF) domains--specifically, a Limiting Amplifier (LA), a Fast Fourier Transform (FFT) module, and a Power Amplifier (PA). The evaluation demonstrates that our pipeline seamlessly processes complex real-world datasheets to consistently generate functionally correct SystemC models. This provides a highly efficient and reliable paradigm for agile model library construction while drastically minimizing manual intervention.

---

## 论文详细总结（自动生成）

以下是对论文《DS2SC-Agent: A Multi-Agent Automated Pipeline for Rapid Chiplet Model Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在后摩尔时代，Chiplet（芯粒）技术是实现异构集成的关键。为了在设计早期进行架构探索和软硬件协同验证，必须构建精确的行为级参考模型（如 SystemC 模型）。
*   **核心问题**：传统的建模过程高度依赖领域专家手动阅读动辄数十页的工业数据手册（Datasheet），提取接口、时序和逻辑并编写代码。这一过程耗时、易错且难以扩展。
*   **LLM 的局限性**：虽然大语言模型（LLM）在代码生成上有所进展，但直接处理非结构化、长篇幅且包含图表的原始数据手册时，面临“上下文消失”和“逻辑幻觉”的严峻挑战，难以生成功能正确的 SystemC 代码。

### 2. 方法论
论文提出了 **DS2SC-Agent**，这是首个从原始 PDF 数据手册到 SystemC 模型的端到端自动化生成流水线。其核心思想是将复杂的建模任务解耦为四个协作智能体：
*   **规范解析智能体 (Specification Parsing Agent)**：采用“混合填充”策略，利用预定义的 JSON 模板（Spec IR），从非结构化 PDF 中提取引脚列表、寄存器映射、电气参数和行为伪代码。
*   **代码生成智能体 (Code Generation Agent)**：基于 Spec IR 构建 SystemC 核心代码。采用“仅头文件 (Header-only)”设计范式，将模块声明、端口定义和行为逻辑（SC_THREAD/METHOD）整合，便于后续集成。
*   **测试平台生成智能体 (Testbench Generation Agent)**：根据 Spec IR 中的测试用例生成 `main.cpp`。它采用黑盒验证范式，生成激励信号、监控器和结果检查逻辑。
*   **自动调试智能体 (Auto Debugging Agent)**：建立闭环反馈机制。针对编译错误（语法调试）和仿真失败（功能调试），利用思维链（CoT）推理分析日志并迭代修复代码。

### 3. 实验设计
*   **实验场景**：涵盖了三个不同物理领域的代表性单功能 Chiplet：
    1.  **数字领域**：快速傅里叶变换 (FFT) 模块（基于 96 页的 Xilinx 产品指南）。
    2.  **模拟领域**：限幅放大器 (LA)（基于 6 页的密集参数表手册）。
    3.  **射频 (RF) 领域**：功率放大器 (PA)（基于 18 页包含性能曲线的手册）。
*   **Benchmark**：由于该领域尚无直接的自动化工具，实验主要对比生成模型与原始数据手册规范的功能一致性。
*   **评估指标**：功能正确性、数值精度（如 FFT 的计算误差、PA 的曲线拟合误差）。

### 4. 资源与算力
*   **核心引擎**：使用了 **Gemini 1.5 Flash** 模型的 API。选择该模型主要是因为其支持超长上下文窗口，能够处理近百页的 PDF 文档。
*   **算力细节**：文中未明确提及具体的 GPU 型号、数量或推理时长。仿真环境基于标准 GCC 编译器，链接了 IEEE SystemC 2.3.3 和 SystemC-AMS 2.3.4 库。

### 5. 实验数量与充分性
*   **实验规模**：针对上述三个典型案例进行了深入的闭环测试。
*   **充分性评价**：实验设计较为全面，覆盖了数字（离散算法）、模拟（连续时间行为）和射频（非线性特性）三大领域，验证了框架的跨领域泛化能力。
*   **客观性**：通过自动生成的测试平台进行量化评估（如 FFT 误差在 $10^{-16}$ 数量级，PA 拟合误差小于 1dB），结果较为客观。但案例数量（3个）相对较少，尚未在大规模 Chiplet 库上进行压力测试。

### 6. 主要结论与发现
*   **端到端可行性**：DS2SC-Agent 能够成功跨越非结构化文档与可执行硬件模型之间的鸿沟。
*   **高保真度**：生成的 FFT 模型实现了双精度浮点运算精度；LA 模型准确模拟了线性放大、非线性钳位和使能控制逻辑；PA 模型通过 Rapp 模型准确拟合了非线性压缩特性。
*   **效率提升**：多智能体协作显著减少了人工干预，解决了 LLM 在长文档处理中的幻觉问题。

### 7. 优点
*   **首创性**：填补了从原始 PDF 直接生成 SystemC 模型的自动化空白。
*   **架构解耦**：四智能体协作流水线设计合理，通过 Spec IR 中间表示有效缓解了上下文丢失问题。
*   **闭环自愈**：引入自动调试智能体和 CoT 推理，极大地提高了生成代码的成功率和可靠性。
*   **跨领域支持**：同时支持 SystemC 和 SystemC-AMS，具备处理多物理域模型的能力。

### 8. 不足与局限
*   **视觉解析限制**：LLM 在直接解析复杂射频曲线图方面仍有局限，PA 案例中仍需在 Prompt 中辅助提供部分关键离散数据点。
*   **系统级验证不足**：目前主要针对单功能 Chiplet，尚未验证多个生成模型组合成复杂异构系统时的互连和兼容性。
*   **隐私风险**：依赖云端 LLM API（如 Gemini），对于涉及商业机密的工业数据手册，可能存在数据隐私和安全风险。
*   **样本量**：Benchmark 规模较小，需进一步验证在更极端或更模糊的数据手册下的鲁棒性。

（完）
