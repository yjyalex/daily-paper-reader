---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用强化学习激励外部化推理和早期退出
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过引入“早退”机制和强化学习训练，促使大语言模型将推理过程外部化。通过在中间层设置退出点，模型被训练在预测简单词元时尽早结束计算，从而减少内部隐式计算的冗余。这种方法能有效引导模型将复杂的规划和推理过程转化为可见的外部词元，为提升推理透明度和效率提供了新思路。
selection_source: fresh_fetch
motivation: 旨在减少大语言模型在内部激活中进行非近视规划的冗余计算，促使其将推理过程显式化。
method: 在Transformer架构中引入中间层早退机制，并结合校准阶段与强化学习，激励模型在保证性能的前提下尽早退出。
result: 在小型推理模型上的初步实验表明，该方法能使模型根据词元难度自适应地减少计算量。
conclusion: 该架构调整能有效优化计算资源分配，通过限制内部计算能力来强化模型的外部化推理能力。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更冗长的推理者。我们在现有 Transformer 架构的中间层增加了早停（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步实验结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过修改 Transformer 架构来强制模型将推理过程“外部化”的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在生成思维链（CoT）时，往往会在内部激活中进行“隐式计算”或“非近视规划”。这意味着模型可能在内部已经完成了推理，而输出的 CoT 文字并不能真实反映其思考过程（不忠实性）或被刻意掩盖（混淆性）。
*   **研究动机**：为了提高模型的可监控性（Monitorability），研究者希望限制模型在单个 token 生成时的内部计算量，迫使模型必须通过输出外部 token（即 CoT）来完成复杂推理。
*   **整体含义**：通过引入“早退”机制，让模型在预测简单 token 时尽早结束计算，从而将有限的深度计算资源保留给真正困难的推理步骤。

### 2. 方法论
该方法包含架构修改和两个训练阶段：
*   **架构修改**：
    *   在 Transformer 的中间层（每 5 层）添加浅层“早退头”（Early-exit heads）。
    *   使用 LoRA 适配器调整现有权重。
    *   **随机退出机制**：模型在每一层根据早退头输出的概率随机决定是否退出。若退出，则残差流直接跳过后续层进入输出层。
*   **阶段 1：自蒸馏校准（Self-distillation）**：
    *   将原始全深度模型作为教师，带早退头的模型作为学生。
    *   利用中间层预测与最终层预测之间的 **KL 散度** 来确定目标退出概率。如果某一层预测结果与最终层非常接近，则该层被赋予较高的退出概率。
*   **阶段 2：强化学习激励（RL）**：
    *   使用改进的 **RLOO** 算法。
    *   **奖励函数**：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
    *   核心思想是在保证任务准确率（$R_{task}$）的同时，通过惩罚项（$\lambda$）激励模型尽可能在更浅的层退出。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学应用题数据集。
    *   **ExploreToM**：心理理论（Theory of Mind）推理任务数据集。
*   **基准模型（Benchmark）**：
    *   DeepSeek-R1-Distill-Qwen-1.5B
    *   Qwen3-4B（预发布版本或特定变体）
*   **对比方法**：主要对比未经修改的原始基座模型以及仅经过阶段 1 校准的模型。
*   **评估指标**：任务准确率、回答连贯性（由 GPT-5 评分）、平均计算深度（ALS，平均层数节省）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型规模**：实验主要集中在 1.5B 和 4B 规模的小型推理模型上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   展示了 5 组不同 KL 因子下的校准实验。
    *   展示了 5 次独立运行的 RL 训练动态曲线。
    *   对 50-60 个提示词进行了详细的定量评估和 token 级别的可视化分析。
*   **充分性评价**：作为“概念验证”（Proof of Concept），实验设计涵盖了从校准到强化的全流程。但由于样本量较小（如每次评估仅 60 个 prompt）且模型规模有限，其实验的统计严密性和在大模型上的泛化性仍有待进一步验证。

### 6. 主要结论与发现
*   **自适应计算**：模型学会了根据 token 难度调整深度。例如，预测简单的连接词时会极早退出，而处理复杂推理步骤时会使用全深度。
*   **性能保持**：通过 RL 训练，模型在减少平均计算量（层数减少约 5%-10%）的同时，任务准确率反而有所提升（从 47% 提升至 55%-60%）。
*   **可调控性**：通过调整超参数 $\lambda$ 或推理时的偏移量，可以灵活权衡“推理外部化程度”与“生成质量”。

### 7. 优点
*   **创新性**：首次提出通过架构层面的“早退”来强制解决 CoT 忠实性问题，而非仅仅依靠提示词工程。
*   **训练策略完备**：结合了自蒸馏（找准退出点）和 RL（压榨计算量）两个阶段，逻辑自洽。
*   **安全视角**：为 AI 安全研究提供了一个减少“隐式规划”的实用工具。

### 8. 不足与局限
*   **忠实性验证缺失**：论文虽然实现了早退，但尚未通过实验证明这种方法是否真的提高了 CoT 的忠实性（即 CoT 是否更准确地反映了模型的实际逻辑）。
*   **规模局限**：仅在 1.5B 和 4B 模型上测试，尚不清楚在 70B 或更大规模模型上，这种强制早退是否会导致推理能力的剧烈崩塌。
*   **泛化性风险**：实验仅限于数学和心理推理任务，缺乏跨领域验证。
*   **训练稳定性**：RL 训练曲线显示出一定的波动性，暗示超参数调节可能较为敏感。

（完）
