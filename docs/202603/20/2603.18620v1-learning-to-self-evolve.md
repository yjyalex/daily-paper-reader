---
title: Learning to Self-Evolve
title_zh: 学习自我演化
authors: "Xiaoyin Chen, Canwen Xu, Yite Wang, Boyi Liu, Zhewei Yao, Yuxiong He"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18620v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于测试时自我演化的强化学习框架
tldr: 本研究提出“学习自我进化”（LSE）框架，通过强化学习训练大语言模型在测试时自主优化上下文。不同于以往依赖模型固有推理能力的方法，LSE将多步进化简化为单步RL目标，并结合树引导进化循环。实验表明，经过LSE训练的4B模型在Text-to-SQL和MMLU任务上超越了GPT-5等强模型驱动的策略及现有提示优化方法，证明了将自我进化视为可学习技能的有效性。
selection_source: fresh_fetch
motivation: 现有模型在测试时自我进化主要依赖其固有的推理能力，而缺乏针对该进化过程的显式训练。
method: 提出LSE框架，将多步进化问题简化为单步强化学习目标，并结合树引导的进化循环来优化上下文编辑。
result: 4B参数的LSE模型在Text-to-SQL和MMLU任务上表现优于GPT-5、Claude 3.5驱动的策略以及TextGrad等提示优化方法。
conclusion: 研究证明了将自我进化视为一种可学习的技能是有效的，且训练出的策略具有良好的跨模型迁移能力。
---

## 摘要
我们介绍了“学习自我演化”（Learning to Self-Evolve, LSE），这是一个强化学习框架，旨在训练大语言模型（LLM）在测试阶段改进其自身的上下文。我们将 LSE 置于测试时自我演化的场景中，模型通过对已见问题的反馈迭代地优化其上下文，从而在处理新问题时表现得更好。现有方法完全依赖于模型的固有推理能力，从未针对该任务进行显式训练。LSE 将多步演化问题简化为单步强化学习（RL）目标，其中每次上下文编辑都根据下游性能的提升获得奖励。我们将该目标与树引导的演化循环相结合。在 Text-to-SQL 生成（BIRD）和通用问答（MMLU-Redux）任务上，使用 LSE 训练的 4B 参数模型优于由 GPT-5 和 Claude Sonnet 4.5 驱动的自我演化策略，也优于包括 GEPA 和 TextGrad 在内的提示优化方法，并且无需额外训练即可迁移并指导其他模型。我们的结果强调了将自我演化视为一种可学习技能的有效性。

## Abstract
We introduce Learning to Self-Evolve (LSE), a reinforcement learning framework that trains large language models (LLMs) to improve their own contexts at test time. We situate LSE in the setting of test-time self-evolution, where a model iteratively refines its context from feedback on seen problems to perform better on new ones. Existing approaches rely entirely on the inherent reasoning ability of the model and never explicitly train it for this task. LSE reduces the multi-step evolution problem to a single-step RL objective, where each context edit is rewarded by the improvement in downstream performance. We pair this objective with a tree-guided evolution loop. On Text-to-SQL generation (BIRD) and general question answering (MMLU-Redux), a 4B-parameter model trained with LSE outperforms self-evolving policies powered by GPT-5 and Claude Sonnet 4.5, as well as prompt optimization methods including GEPA and TextGrad, and transfers to guide other models without additional training. Our results highlight the effectiveness of treating self-evolution as a learnable skill.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Self-Evolve》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：人类智能的核心在于能根据环境反馈不断进化（如棋手分析对局、程序员熟悉代码库），但目前的 LLM 在部署阶段是“静态”的。无论解决多少问题，其策略保持不变，且一旦上下文重置，积累的经验就会丢失。
*   **核心问题**：现有的“测试时自我演化”（Test-time Self-evolution）方法（如提示词优化）完全依赖模型的固有推理能力，未经过针对性的训练。
*   **整体含义**：本文提出将“自我演化”视为一种可学习的技能，通过强化学习（RL）显式训练模型如何根据反馈改进自身的提示词（Context/Prompt）。

### 2. 方法论
*   **核心思想**：将复杂的“多步演化”过程简化为“单步强化学习”目标。训练一个自我演化策略 $f_\psi$，使其能够根据当前上下文和性能摘要生成更优的上下文。
*   **关键技术细节**：
    *   **改进型奖励（Improvement-based Reward）**：奖励函数定义为 $r_{LSE} = \bar{R}(c_1) - \bar{R}(c_0)$，即编辑后的性能减去编辑前的性能。这避免了模型只倾向于保留原本就好的提示词，而是真正激励“改进”行为。
    *   **树引导演化（Tree-guided Evolution）**：在测试时，不使用简单的线性链式更新，而是维护一个演化树。利用 **UCB（置信区间上界）** 算法选择最有潜力的节点进行扩展，允许模型在编辑失败时进行回溯和分支探索。
    *   **训练流程**：从演化轨迹中采样节点作为起始状态，通过 RL（如 PPO/GRPO 的变体）优化模型，使其生成的指令能最大化下游任务的准确率增量。

### 3. 实验设计
*   **数据集/场景**：
    *   **Text-to-SQL**：使用 **BIRD** 数据集，涵盖金融、毒理学、代码库等多个领域。
    *   **通用问答（QA）**：使用 **MMLU-Redux** 和 **SuperGPQA**，涵盖商业伦理、哲学、解剖学等学科。
*   **Benchmark 与对比方法**：
    *   **强模型基准**：使用 GPT-5 和 Claude Sonnet 4.5 作为自我演化策略（Action Model 保持一致）。
    *   **提示优化方法**：对比了 **GEPA**（反射式进化）和 **TextGrad**（文本梯度下降）。
    *   **Action Model**：主要使用 Qwen3-4B-Instruct。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 `verl` 强化学习框架进行训练。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号和数量。训练配置包括：学习率 $1 \times 10^{-5}$，每批次采样 32 个节点，每个节点生成 4 个 rollout，训练 4 个 epoch。
*   **数据量**：为每个领域生成了约 200 个演化轨迹，包含约 4,000 个树节点用于 RL 训练。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个大类任务、15 个细分领域（5 个 BIRD 领域 + 10 个 MMLU 领域）上进行了测试。
*   **充分性**：
    *   **消融实验**：对比了有无树搜索（UCB vs. 线性链）、不同奖励设计（ALSE vs. 标准 GRPO 奖励）的效果。
    *   **迁移性实验**：验证了 LSE 训练的 4B 模型能否指导更强的模型（如 Arctic-Text2SQL-R1-7B），证明了策略的通用性。
*   **客观性**：实验采用了固定的 Holdout 集合和问题顺序，确保了不同方法对比的公平性。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 LSE 训练的 **4B 参数模型**，其自我演化能力超过了未经针对性训练的 **GPT-5** 和 **Claude Sonnet 4.5**。
*   **训练的必要性**：显式训练（LSE）比单纯依赖模型的零样本推理（如 TextGrad）能带来更显著的性能提升（在 BIRD 上提升约 4%）。
*   **树搜索的价值**：树引导演化比线性演化平均提升约 2.2%-2.4%，有效防止了因单次错误编辑导致的性能崩溃。
*   **互补性**：提示词层面的演化与参数层面的强化学习（如 R1 模型）是互补的，两者结合可进一步提升 SOTA 性能。

### 7. 优点
*   **创新视角**：将自我演化从“提示词工程问题”转化为“可学习的强化学习问题”。
*   **高效性**：通过单步简化解决了多步 RL 信用分配难的问题，且测试时无需计算梯度，避免了灾难性遗忘。
*   **鲁棒性**：引入树搜索和 UCB，增强了系统在面对噪声反馈时的稳定性。

### 8. 不足与局限
*   **训练域限制**：目前是针对特定任务领域（如 SQL 或 QA）分别训练，尚未实现一个能跨所有任务通用的“万能演化器”。
*   **演化范围**：目前仅限于修改提示词中的“指令（Instruction）”部分，未涉及工具调用、外部记忆库或模型参数的动态更新。
*   **环境依赖**：自我演化依赖于高质量的反馈信号（如执行结果或标准答案），在缺乏验证器的开放域任务中应用受限。
*   **单步假设**：虽然简化为单步训练降低了成本，但可能忽略了长程演化策略中的某些复杂动态。

（完）
