---
title: "Orla: A Library for Serving LLM-Based Multi-Agent Systems"
title_zh: Orla：一个用于服务基于大语言模型的多智能体系统的库
authors: "Rana Shahout, Hayder Tirmazi, Minlan Yu, Michael Mitzenmacher"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13605v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于提供基于大语言模型的多智能体系统服务的库
tldr: Orla 是一个专为 LLM 多智能体系统设计的推理服务库。针对现代智能体应用中复杂的工作流编排难题，Orla 通过将请求执行与工作流策略分离，提供了阶段映射器、工作流编排器和内存管理器三大核心机制。它作为现有推理引擎的上层服务层，实现了跨模型和后端的资源调度与 KV 缓存优化，在保证灵活性的同时显著提升了多步骤智能体任务的执行效率并降低了成本。
selection_source: fresh_fetch
motivation: 当前开发者在构建智能体应用时，往往需要手动编写复杂的编排代码来连接推理引擎和工具，缺乏高效的统一服务层抽象。
method: Orla 提出了由阶段映射器、工作流编排器和内存管理器组成的架构，实现了跨模型和后端的任务调度及 KV 缓存管理。
result: 在客户支持工作流的评估中，Orla 相比单模型 vLLM 基准显著降低了延迟和成本，并通过缓存管理缩短了首字响应时间。
conclusion: Orla 为多智能体系统提供了一个通用的服务层抽象，有效解决了复杂工作流中的资源协调与推理性能优化问题。
---

## 摘要
我们介绍了 Orla，这是一个用于构建和运行基于大语言模型（LLM）的智能体系统的库。现代智能体应用由结合了多个 LLM 推理步骤、工具调用和异构基础设施的工作流组成。目前，开发人员通常通过手动将编排代码与 LLM 服务引擎和工具执行逻辑相结合来构建这些系统。Orla 提供了一种通用的抽象，将请求执行与工作流级策略分离。它作为现有 LLM 推理引擎之上的服务层：开发人员定义由阶段（stages）组成的工作流，而 Orla 负责管理这些阶段如何在不同模型和后端之间进行映射、执行和协调。它通过三种机制提供智能体级控制：阶段映射器（stage mapper），负责将每个阶段分配给合适的模型和后端；工作流编排器（workflow orchestrator），负责调度阶段并管理其资源和上下文；以及内存管理器（memory manager），负责跨工作流边界管理推理状态（如 KV 缓存）。我们通过一个展示了其多项能力的客户支持工作流来演示 Orla。我们在两个数据集上对 Orla 进行了评估，结果表明，与单模型 vLLM 基准相比，阶段映射改善了延迟和成本，而工作流级缓存管理减少了首字延迟（time-to-first-token）。

## Abstract
We introduce Orla, a library for constructing and running LLM-based agentic systems. Modern agentic applications consist of workflows that combine multiple LLM inference steps, tool calls, and heterogeneous infrastructure. Today, developers typically build these systems by manually composing orchestration code with LLM serving engines and tool execution logic. Orla provides a general abstraction that separates request execution from workflow-level policy. It acts as a serving layer above existing LLM inference engines: developers define workflows composed of stages, while Orla manages how those stages are mapped, executed, and coordinated across models and backends. It provides agent-level control through three mechanisms: a stage mapper, which assigns each stage to an appropriate model and backend; a workflow orchestrator, which schedules stages and manages their resources and context; and a memory manager, which manages inference state such as the KV cache across workflow boundaries. We demonstrate Orla with a customer support workflow that exercises many of its capabilities. We evaluate Orla on two datasets, showing that stage mapping improves latency and cost compared to a single-model vLLM baseline, while workflow-level cache management reduces time-to-first-token.

---

## 论文详细总结（自动生成）

### Orla：面向 LLM 多智能体系统的工作流级服务库论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）应用向智能体（Agentic）系统演进，推理任务已从简单的单次请求转变为复杂的**工作流**：包含多个推理步骤、工具调用及跨模型的协作。
*   **现状与挑战**：现有的推理引擎（如 vLLM、SGLang）主要针对单次请求的吞吐量和延迟进行优化（如连续批处理、KV 缓存管理），但缺乏对“工作流级”策略的感知。
*   **研究动机**：开发者目前必须手动编写复杂的编排代码来连接推理引擎和工具执行逻辑，导致系统难以扩展且资源利用率低下。Orla 的目标是提供一个通用的服务层抽象，将**请求执行**与**工作流策略**分离，实现对多智能体系统的精细化控制。

#### 2. 论文提出的方法论
Orla 作为一个位于现有推理引擎（后端）之上的轻量级库，通过以下三个核心组件实现其功能：
*   **阶段映射器 (Stage Mapper)**：
    *   将工作流中的每个“阶段”（Stage）分配给最合适的模型和后端。
    *   支持**显式映射**（开发者指定）和**动态路由**（如使用轻量级模型判断任务复杂度，将简单任务路由至小模型，复杂任务路由至大模型）。
*   **工作流编排器 (Workflow Orchestrator)**：
    *   **调度**：基于有向无环图（DAG）管理阶段依赖，支持 FCFS（先来先服务）或基于优先级的调度。
    *   **资源与上下文管理**：在阶段间传递输出（如 JSON、工具结果），并在等待工具调用时保持后端的高利用率。
*   **内存管理器 (Memory Manager)**：
    *   在工作流边界而非请求边界管理 KV 缓存。
    *   **核心策略**：识别连续阶段是否共享上下文前缀。如果是，则保留缓存以避免重复 Prefill；如果工作流结束或切换后端，则主动释放内存。

#### 3. 实验设计
论文通过一个实际的“客户支持工作流”演示系统能力，并使用两个基准数据集进行定量评估：
*   **数据集/场景**：
    1.  **SWE-bench Lite**：包含 2,294 个软件工程请求，用于评估阶段映射对延迟和成本的影响。
    2.  **DAG-MATH-Formatted-CoT**：包含 2,894 个数学推理请求，用于评估工作流级缓存管理的有效性。
*   **Benchmark 与对比方法**：
    *   **基准 (Baseline)**：使用单模型（Qwen3-8B）运行在 vLLM 上的配置。
    *   **对比策略**：Orla 的异构映射（Qwen3-4B + Qwen3-8B）以及不同的缓存清理策略（逐请求清理 vs. 逐工作流清理）。

#### 4. 资源与算力
论文明确列出了实验环境：
*   **硬件**：一台服务器，配备 2 颗 AMD EPYC 7313 16 核处理器（共 64 核），528 GB 系统内存。
*   **GPU**：1 块 **NVIDIA RTX PRO 6000 Blackwell Max-Q** 工作站显卡（96 GB VRAM）。
*   **软件栈**：CUDA 13.0，Linux 内核 5.15.0，后端使用 vLLM 和 SGLang。
*   **显存分配**：在映射实验中，大模型（8B）分配 60% 显存，小模型（4B）分配 35% 显存。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对两个数据集进行了数千次请求的测试（2,294 + 2,894 次），样本量充足。
*   **客观性与公平性**：实验对比了墙钟时间（Wall-clock time）、平均完成时间、推理成本以及首字延迟（TTFT）等多个维度。通过设置 Temperature 为 0 确保了结果的可重复性。
*   **充分性**：虽然涵盖了性能和成本，但缺乏针对不同并发压力下的吞吐量极限测试，且动态路由算法（OneBitStageMapper）相对简单，未深入探讨更复杂的路由策略。

#### 6. 论文的主要结论与发现
*   **性能提升**：通过阶段映射（将 41.6% 的简单请求路由至小模型），Orla 相比单模型基准减少了 **38% 的墙钟时间**和 **60% 的平均完成时间**。
*   **成本降低**：由于小模型单价更低，阶段映射使估计的推理成本降低了 **35%**。
*   **延迟优化**：工作流级的 KV 缓存管理显著降低了后续阶段的首字延迟（TTFT），通过重用前缀避免了冗余计算。
*   **灵活性**：Orla 的插件化设计使其能无缝切换 SGLang、vLLM、Ollama 甚至模拟后端，无需修改工作流定义。

#### 7. 优点
*   **架构解耦**：成功将复杂的智能体逻辑与底层的推理引擎解耦，降低了开发门槛。
*   **异构支持**：能够同时调度不同硬件（GPU/CPU）和不同框架（vLLM/Ollama）上的模型。
*   **研究友好**：内置的模拟后端允许研究人员在没有昂贵 GPU 的情况下进行调度算法和缓存策略的实验。
*   **实用性强**：针对 KV 缓存的“工作流感知”管理解决了当前推理引擎在处理多轮对话/智能体任务时的痛点。

#### 8. 不足与局限
*   **实验环境局限**：主要在单机单 GPU 环境下测试，未探讨在大规模分布式集群或多节点 GPU 环境下的扩展性。
*   **动态路由简单**：目前的动态路由仅依赖一个简单的轻量级 LLM 分类器，可能无法处理极高精度的路由需求。
*   **并发冲突**：论文未详细讨论当多个复杂工作流同时竞争有限的 GPU 资源时，Orla 的调度器如何处理潜在的死锁或严重的资源饥饿问题。
*   **应用范围**：目前主要针对 DAG 结构的工作流，对于具有高度动态性、循环结构或不可预测分支的超复杂智能体系统，其表达能力尚待验证。

（完）
