---
title: Reinforcement Learning for Diffusion LLMs with Entropy-Guided Step Selection and Stepwise Advantages
title_zh: 结合熵引导步长选择与逐步优势的扩散大语言模型强化学习
authors: "Vishnu Teja Kunde, Fatemeh Doudi, Mahdi Farahbakhsh, Dileep Kalathil, Krishna Narayanan, Jean-Francois Chamberland"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12554v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 扩散语言模型的强化学习
tldr: 针对扩散语言模型（DLM）在强化学习中面临的序列似然难计算问题，本文提出了一种基于有限时界马尔可夫决策过程（MDP）的新框架。该方法推导出了精确且无偏的策略梯度，通过熵引导的步骤选择和单步去噪奖励估计中间优势函数，避免了昂贵的多步采样。实验证明该方法在代码、逻辑推理和数学任务上达到了SOTA水平，显著优于现有DLM强化学习方案。
selection_source: fresh_fetch
motivation: 现有的扩散语言模型强化学习方法依赖于近似似然或启发式方法，难以准确捕捉去噪过程的序列结构。
method: 将去噪过程建模为有限时界MDP，利用熵引导准则选择更新步骤，并结合单步去噪奖励计算中间优势。
result: 在代码生成、逻辑推理和数学竞赛基准测试中取得了最先进的性能，超越了现有的DLM后训练强化学习方法。
conclusion: 该研究为扩散语言模型的强化学习提供了一个理论完备且计算高效的框架，有效提升了其在复杂推理任务中的表现。
---

## 摘要
强化学习（RL）在自回归（AR）语言模型的训练后阶段非常有效，但由于序列级似然难以计算，将这些方法扩展到扩散语言模型（DLMs）面临挑战。因此，现有方法依赖于代理似然或启发式近似，这可能会引入偏差并掩盖去噪的序列结构。我们将基于扩散的序列生成建模为去噪轨迹上的有限步马尔可夫决策过程，并推导出一种精确、无偏的策略梯度。该梯度可在去噪步骤上进行分解，并以中间优势的形式表示，无需显式评估序列似然。为了获得实用且计算高效的估计器，我们（i）通过熵引导的近似界限选择用于策略更新的去噪步骤，以及（ii）利用扩散模型天然提供的单步去噪奖励来估计中间优势，从而避免了昂贵的多步展开。在代码编写和逻辑推理基准测试上的实验证明了该方法达到了当前最先进的水平，并在数学推理方面表现出极强的竞争力，优于现有的扩散语言模型强化学习训练后方法。代码可在 https://github.com/vishnutez/egspo-dllm-rl 获取。

## Abstract
Reinforcement learning (RL) has been effective for post-training autoregressive (AR) language models, but extending these methods to diffusion language models (DLMs) is challenging due to intractable sequence-level likelihoods. Existing approaches therefore rely on surrogate likelihoods or heuristic approximations, which can introduce bias and obscure the sequential structure of denoising. We formulate diffusion-based sequence generation as a finite-horizon Markov decision process over the denoising trajectory and derive an exact, unbiased policy gradient that decomposes over denoising steps and is expressed in terms of intermediate advantages, without requiring explicit evaluation of the sequence likelihood. To obtain a practical and compute-efficient estimator, we (i) select denoising steps for policy updates via an entropy-guided approximation bound, and (ii) estimate intermediate advantages using a one-step denoising reward naturally provided by the diffusion model, avoiding costly multi-step rollouts. Experiments on coding and logical reasoning benchmarks demonstrate state-of-the-art results, with strong competitive performance on mathematical reasoning, outperforming existing RL post-training approaches for DLMs. Code is available at https://github.com/vishnutez/egspo-dllm-rl.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reinforcement Learning for Diffusion LLMs with Entropy-Guided Step Selection and Stepwise Advantages》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：强化学习（RL）在提升自回归（AR）大语言模型的推理能力方面表现卓越（如 DeepSeek-R1）。然而，扩散语言模型（DLM）由于其非因果、双向生成的特性，无法像 AR 模型那样进行简单的 token 级似然分解，导致序列级似然计算极其困难。
*   **核心问题**：现有的 DLM 强化学习方法多依赖于代理似然或启发式近似，这往往会引入偏差，且忽略了去噪轨迹中的序列结构。
*   **整体含义**：本文旨在为 DLM 提供一个理论完备且计算高效的强化学习框架，通过显式建模去噪过程的马尔可夫决策过程（MDP），解决信用分配（Credit Assignment）和计算效率问题。

### 2. 论文提出的方法论
论文提出了 **EGSPO-SA**（结合逐步优势的熵引导逐步策略优化）框架，核心包含三个部分：
*   **扩散 MDP 建模与精确梯度**：将 $T$ 步去噪过程建模为有限时界的 MDP。作者推导出了一个精确且无偏的策略梯度定理，该定理将全局奖励分解为各个去噪步骤的“逐步优势”（Stepwise Advantages），避免了直接计算复杂的序列似然。
*   **熵引导的步骤选择（EGSPO）**：由于扩散过程步数较多（通常 $T \ge 128$），对每一步计算梯度开销巨大。论文通过理论证明，策略梯度的误差界限与去噪步骤的熵成正比。因此，算法在每轮更新中仅选择熵最高（即模型最不确定、学习信号最强）的 $K$ 个步骤进行参数更新。
*   **逐步优势估计（SA）**：为了给中间步骤提供更精准的反馈，论文利用 DLM 的特性，从中间状态进行“一次性贪婪去噪”生成完整序列并计算奖励，以此作为基准（Baseline）来估计当前步骤的优势函数，而无需训练额外的价值网络（Value Network）。

### 3. 实验设计
*   **基准模型**：LLaDA-8B-Instruct（一种先进的掩码扩散语言模型）。
*   **实验任务与数据集**：
    *   **数学推理**：GSM8K, MATH500。
    *   **逻辑推理**：4x4 Sudoku（数独）, Countdown（倒计时游戏）。
    *   **代码生成**：MBPP, HumanEval。
*   **对比方法**：包括目前主流的 DLM 强化学习方案：d1, d2, wd1, SPG, TraceRL, DiFFPO 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   **Batch Size**：每台 GPU 6 个 prompt，结合梯度累积，有效 batch size 为 96。
    *   **训练时长**：代码任务约 4k 步，数学任务约 6k 步，逻辑推理任务约 10k 步。
    *   **参数微调**：采用 LoRA（Rank=128, Alpha=64）进行高效微调。
    *   **去噪步数**：训练时设定为 128 步，每步并行去噪 2 个 token。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大类核心推理任务，共 6 个主流基准测试。
*   **消融实验**：作者对比了“熵引导选择”与“随机选择”、“均匀选择”的效果，验证了熵引导在收敛速度和最终性能上的优越性。
*   **充分性评价**：实验设计较为充分，不仅对比了最终准确率，还深入分析了计算效率（FLOPs vs. Reward）、样本效率（Samples vs. Reward）以及梯度更新效率。对比基准涵盖了 2024-2025 年间最新的同类研究，具有较强的时效性和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：EGSPO-SA 在逻辑推理（Sudoku, Countdown）和代码生成（MBPP, HumanEval）上达到了 SOTA 水平，显著优于 d1 等方法。
*   **数学能力提升**：在 GSM8K 和 MATH500 上也表现出极强的竞争力，相比基座模型有大幅提升。
*   **计算高效性**：在达到相同奖励水平的情况下，EGSPO-SA 所需的 FLOPs 和样本量远低于现有方法（如 d1），证明了熵引导步骤选择的有效性。
*   **信用分配的重要性**：实验表明，在逻辑约束严苛的任务（如数独）中，逐步优势估计（SA）能显著加快学习进程并提升最终效果。

### 7. 优点
*   **理论严谨**：从 MDP 第一性原理出发，推导出了无偏的策略梯度，为 DLM 的强化学习奠定了坚实的数学基础。
*   **创新性强**：巧妙地利用“熵”作为计算资源分配的指标，解决了扩散模型训练成本高的痛点。
*   **无需价值模型**：通过单步贪婪补全估计优势，降低了系统复杂度，避免了训练额外价值网络带来的不稳定性和计算开销。

### 8. 不足与局限
*   **模型单一**：实验主要基于 LLaDA-8B 展开，尚未在更大规模（如 70B+）或其他架构的扩散模型上验证泛化性。
*   **超参数敏感性**：逐步优势估计中的超参数 $\lambda$（用于平衡全局奖励与中间估计）在不同任务中需要调整（如数学任务需动态衰减），这增加了一定的调参难度。
*   **推理延迟**：虽然训练效率提升了，但 DLM 本身的推理速度（多步迭代）仍是其相比 AR 模型在实际部署中的固有劣势，本文未对此做进一步优化。

（完）
