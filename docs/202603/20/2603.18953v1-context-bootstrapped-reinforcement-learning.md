---
title: Context Bootstrapped Reinforcement Learning
title_zh: 上下文引导强化学习
authors: "Saaket Agashe, Jayanth Srinivasa, Gaowen Liu, Ramana Kompella, Xin Eric Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18953v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 基于可验证奖励的强化学习以获取推理模式
tldr: 针对可验证奖励强化学习（RLVR）中探索效率低下的问题，本文提出上下文引导强化学习（CBRL）。该方法在训练初期通过在提示词中随机加入少量示例来引导模型探索，并随训练进程逐渐减少示例比例至零。实验证明，CBRL能有效帮助模型内化推理模式，在多种推理任务和特定领域编程语言上显著提升了成功率和探索效率。
selection_source: fresh_fetch
motivation: 现有的RLVR方法在面对需要新颖推理或领域知识的任务时，常因难以生成成功样本而导致探索效率低下。
method: 提出CBRL方法，在训练提示词中动态加入少量示例，并采用从高到零的退火策略引导模型自主学习推理模式。
result: 在多个推理任务和特定编程语言Q上的实验显示，CBRL显著提高了模型的成功率和探索效率，且具有算法无关性。
conclusion: CBRL通过课程学习的方式成功解决了RLVR的冷启动问题，使模型能够在无需外部示例的情况下掌握复杂的推理能力。
---

## 摘要
基于可验证奖励的强化学习 (RLVR) 面临探索效率低下的问题，模型难以生成成功的采样轨迹，导致学习信号极少。对于需要获取新颖推理模式或特定领域知识的任务，这一挑战尤为严峻。为了解决这一问题，我们提出了上下文引导强化学习 (CBRL)，通过在训练提示词中随机预置少样本示例来增强 RLVR 训练。注入概率遵循一种课程学习方案：初始阶段保持高概率以引导早期探索，随后逐渐退火至零，使模型最终必须在没有辅助的情况下取得成功。这迫使策略内化示例中的推理模式，而非在测试时依赖它们。我们在两个模型系列和五个 Reasoning Gym 任务上验证了 CBRL。结果表明，CBRL 能够持续提高成功率，提供更好的探索效率，且具有算法无关性。我们进一步证明了 CBRL 在 Q 语言上的实际应用价值，Q 是一种与主流语言规范显著不同的特定领域编程语言。

## Abstract
Reinforcement Learning from Verifiable Rewards (RLVR) suffers from exploration inefficiency, where models struggle to generate successful rollouts, resulting in minimal learning signal. This challenge is particularly severe for tasks that require the acquisition of novel reasoning patterns or domain-specific knowledge. To address this, we propose Context Bootstrapped Reinforcement Learning (CBRL), which augments RLVR training by stochastically prepending few-shot demonstrations to training prompts. The injection probability follows a curriculum that starts high to bootstrap early exploration, then anneals to zero so the model must ultimately succeed without assistance. This forces the policy to internalize reasoning patterns from the demonstrations rather than relying on them at test time. We validate CBRL across two model families and five Reasoning Gym tasks. Our results demonstrate that CBRL consistently improves success rate, provides better exploration efficiency, and is algorithm-agnostic. We further demonstrate CBRL's practical applicability on Q, a domain-specific programming language that diverges significantly from mainstream language conventions.

---

## 论文详细总结（自动生成）

以下是对论文《Context Bootstrapped Reinforcement Learning (CBRL)》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**基于可验证奖励的强化学习 (RLVR)** 中的**探索效率低下 (Exploration Inefficiency)** 问题展开研究。在 RLVR（如 DeepSeek-R1 采用的模式）中，模型仅根据最终答案的正确性获得二元奖励。如果任务涉及模型在预训练阶段极少接触的领域（如特定编程语言 Q）或需要全新的推理模式，模型在训练初期很难随机生成正确的输出，导致无法获得有效的学习信号（奖励稀疏），进而导致训练收敛缓慢甚至失败。

### 2. 论文提出的方法论：CBRL
CBRL 的核心思想是利用大语言模型的**上下文学习 (In-Context Learning, ICL)** 能力作为“临时脚手架”，引导模型在训练初期进行有效探索。

*   **核心机制：**
    *   **少样本示例库 (Few-Shot Bank)：** 为每个任务构建一个包含问题、推理过程和答案的示例库。
    *   **随机上下文注入 (Stochastic Injection)：** 在训练的每个步骤中，以概率 $p_i$ 随机决定是否在当前训练提示词前添加 $k$ 个示例。
    *   **课程学习退火策略 (Curriculum Annealing)：** 注入概率 $p_i$ 随训练进程线性递减（例如从 0.5 降至 0）。
*   **算法流程：**
    1.  在训练初期，高概率注入示例，帮助模型生成正确的推理轨迹并获得奖励。
    2.  随着训练进行，减少示例出现频率，迫使模型不再依赖外部示例，而是将推理模式“内化”到模型参数中。
    3.  **推理阶段：** 训练完成后，模型在没有任何示例的情况下进行推理，不增加额外的计算开销。
*   **算法无关性：** 该方法仅修改输入分布，可无缝集成到 GRPO、RLOO 等任何策略梯度算法中。

### 3. 实验设计
*   **数据集/场景：**
    *   **Reasoning Gym：** 包含 ARC-1D（模式识别）、矩阵操作、单词排序、逆向拼写、24点游戏（Puzzle-24）五个任务。
    *   **Q Programming：** 一种用于金融时间序列数据库的特定领域语言，语法独特（如从右向左评估），LLM 对其了解极少。
*   **Benchmark 与对比方法：**
    *   **基准模型：** Qwen2.5-3B-Instruct, Llama-3.2-3B-Instruct, 以及针对 Q 语言预训练的 QQwen-7B。
    *   **对比算法：** 标准 GRPO（Group Relative Policy Optimization）和 RLOO（REINFORCE Leave-One-Out）。
    *   **对比设置：** 零样本基准、带少样本提示的基准、标准 RL 训练 vs. CBRL 增强的 RL 训练。

### 4. 资源与算力
论文明确列出了实验使用的硬件资源：
*   **Reasoning Gym 实验：** 使用了 4 张 **NVIDIA A6000** GPU，采用 FSDP（Fully Sharded Data Parallel）并行策略。
*   **Q 编程实验：** 使用了 4 张 **NVIDIA GH200** GPU。
*   **训练时长：** Reasoning Gym 任务通常训练 500 个 Step，Batch Size 为 32。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 2 个模型家族、5 个合成推理任务、1 个真实领域编程任务、2 种主流 RL 算法。
*   **充分性：** 论文在 10 个“模型-环境”组合上均验证了 CBRL 的有效性。
*   **消融实验：** 针对初始注入概率 $p_i$ 进行了消融研究（发现 0.5 是平衡点），并绘制了完整的训练奖励曲线。
*   **客观性：** 实验结果基于多个随机种子的平均值（如 3 次或 5 次运行），并提供了标准差，表现出较高的实验严谨性。

### 6. 主要结论与发现
*   **性能提升：** CBRL 在所有测试任务中均优于标准 GRPO，准确率提升幅度从 +1.3% 到 +22.3% 不等。
*   **Q 语言突破：** 在 Q 编程任务中，Pass@1 从 5.0% 显著提升至 26.3%，证明了其在处理生僻领域知识时的强大引导能力。
*   **成功内化：** 训练曲线显示，即使在示例注入概率退火至零后，模型的性能依然保持并持续增长，说明模型确实学习到了推理逻辑，而非简单模仿。
*   **探索加速：** CBRL 显著缓解了 RLVR 的冷启动问题，使模型在训练极早期就能获得正向奖励。

### 7. 优点
*   **简单高效：** 无需修改复杂的 RL 损失函数或引入额外的价值模型（Value Model），易于实现。
*   **推理零成本：** 与增加推理时计算（Scaling Inference Compute）的方法不同，CBRL 的开销仅存在于训练期，推理时不需要示例。
*   **通用性强：** 证明了其在不同模型架构（Qwen, Llama）和不同 RL 算法（GRPO, RLOO）上的普适性。
*   **解决冷启动：** 为 LLM 在缺乏 SFT 数据或预训练数据稀缺的领域进行强化学习提供了一条可行路径。

### 8. 不足与局限
*   **示例库依赖：** 仍需要为每个任务手动或通过更强的模型构建一个高质量的示例库（Few-shot Bank）。
*   **退火策略单一：** 目前采用固定的线性退火，可能不是最优的；理想情况下，退火速度应根据模型实时的奖励反馈动态调整。
*   **任务覆盖：** 虽然在推理和编程上表现良好，但在长程规划（Long-horizon planning）或更复杂的智能体任务中的表现尚未验证。
*   **示例选择：** 实验中发现示例的选择对某些任务（如 ARC-1D）的稳定性有影响，如何自动化选择最有效的示例仍是挑战。

（完）
