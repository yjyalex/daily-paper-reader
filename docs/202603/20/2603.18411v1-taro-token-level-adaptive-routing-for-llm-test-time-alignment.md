---
title: "TARo: Token-level Adaptive Routing for LLM Test-time Alignment"
title_zh: TARo：用于大语言模型测试时对齐的令牌级自适应路由
authors: "Arushi Rai, Qiang Zhang, Hanqing Zeng, Yunkai Zhang, Dipesh Tamboli, Xiangjun Fan, Zhuokai Zhao, Lizhu Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18411v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 在分步数学轨迹上训练奖励模型以进行推理对齐
tldr: 针对大语言模型推理性能提升通常需要昂贵后训练的问题，本文提出TARo，一种在推理阶段引导冻结模型进行结构化推理的对齐方法。通过在步进式数学轨迹上训练奖励模型捕捉逻辑信号，并引入可学习的标记级路由自动控制引导强度。实验证明TARo在推理、临床问答及指令遵循任务上表现优异，且具备跨模型泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时对齐方法多用于偏好对齐而非推理，且缺乏对推理过程中细粒度逻辑一致性的有效引导。
method: 提出标记级自适应路由（TARo），利用步进式奖励模型捕捉逻辑信号，并通过可学习路由在推理时动态调整奖励模型对基础模型的引导。
result: "在数学推理上比基础模型提升达22.4%，在临床推理和指令遵循任务中也表现出色，且支持从小模型到大模型的无重训迁移。"
conclusion: TARo成功将测试时对齐从偏好优化扩展到鲁棒的跨领域推理，为提升冻结LLM的推理能力提供了一种高效且通用的方案。
---

## 摘要
大语言模型（LLMs）展现出强大的推理能力，但通常需要昂贵的训练后处理才能达到高性能。最近的测试时对齐（test-time alignment）方法提供了一种轻量级的替代方案，但目前主要针对偏好对齐而非推理进行了探索。为了弥补这一差距，我们提出了令牌级自适应路由（TARo），它完全在推理阶段引导冻结的大语言模型进行结构化推理。具体而言，我们首先在分步数学轨迹上训练奖励模型，以捕捉细粒度的逻辑一致性信号，然后引入一个可学习的令牌级路由器，自动控制奖励模型对基础模型的引导。大量实验表明，TARo 显著提升了推理性能，相比基础模型提升高达 +22.4%，相比现有的令牌级测试时对齐方法提升了 +8.4%，同时还增强了分布外临床推理（MedXpertQA）和指令遵循（AlpacaEval）能力。此外，TARo 还能在无需重新训练的情况下从小型骨干网络泛化到大型骨干网络，将测试时对齐从偏好优化扩展到稳健的跨领域推理。

## Abstract
Large language models (LLMs) exhibit strong reasoning capabilities but typically require expensive post-training to reach high performance. Recent test-time alignment methods offer a lightweight alternative, but have been explored mainly for preference alignment rather than reasoning. To bridge this gap, we propose, Token-level Adaptive Routing (TARo), which steers frozen LLMs toward structured reasoning entirely at inference time. Specifically, we first train reward models on step-wise mathematical traces to capture fine-grained logical consistency signals, then introduce a learnable token-level router that automatically controls the guidance of the reward model to the base model. Extensive experiments show that TARo significantly improves reasoning performance by up to +22.4% over base model and +8.4% over existing token-level test-time alignment methods, while also boosting out-of-distribution clinical reasoning (MedXpertQA) and instruction following (AlpacaEval). Furthermore, TARo also generalizes from small to large backbones without retraining, extending test-time alignment from preference optimization to robust, cross-domain reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《TARo: Token-level Adaptive Routing for LLM Test-time Alignment》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）在复杂推理任务中表现优异，但通常依赖昂贵的训练后处理（如 RLHF 或 GRPO）。
*   **核心问题**：现有的“测试时对齐”（Test-time Alignment）方法虽然轻量，但大多针对用户偏好而非逻辑推理。此外，现有方法（如 GenARM）通常使用固定的插值权重来结合基础模型和奖励模型，这导致其在不同领域、不同模型规模下缺乏鲁棒性，且难以捕捉推理过程中动态变化的逻辑需求。
*   **研究动机**：开发一种无需重新训练基础模型、能够动态调整引导强度、且具备跨模型规模泛化能力的推理增强方案。

### 2. 方法论
TARo 的核心思想是**令牌级自适应路由**，通过一个轻量级路由器在每个解码步骤动态决定对奖励模型的信任程度。
*   **推理奖励模型训练**：在分步数学推理轨迹数据集（Math-StepDPO-10K）上训练奖励模型。该模型通过偏好损失函数优化，学习区分逻辑正确与错误的推理步骤，将步骤级奖励分解为令牌级对数似然。
*   **可学习的令牌级路由器**：
    *   **核心公式**：在解码步骤 $t$，引导分布由基础模型 $z_{base}$ 和奖励模型 $z_{reward}$ 的逻辑值（logits）加权组合：$z_{guided} = (1 - \alpha_t) z_{base} + \alpha_t z_{reward}$。
    *   **路由机制**：引入轻量级 MLP 路由器 $g_\theta$，输入为两者的逻辑值特征，输出标量 $\alpha_t \in (0, 1)$。
    *   **特征构造**：设计了两种方案：(1) 全量逻辑值拼接；(2) Top-k 逻辑值配合可学习的令牌索引嵌入（更具可扩展性）。
    *   **训练目标**：结合负对数似然（NLL）和熵正则化项。熵正则化鼓励路由器在模型分歧时做出果断选择（即 $\alpha_t$ 趋向 0 或 1）。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理（领域内）**：MATH500。
    *   **临床推理（分布外/OOD）**：MedXpertQA。
    *   **指令遵循（多领域）**：AlpacaEval。
*   **骨干模型**：Llama-3.1 (8B/70B) 和 Qwen-2.5 (3B/14B)。
*   **对比方法**：基础模型（Base）、奖励模型（Reward）、GenARM（固定权重对齐）、UniR（基于 RL 的对齐）。

### 4. 资源与算力
*   **训练阶段**：路由器训练在 1-4 张 NVIDIA H100 GPU 上进行，使用 bfloat16 精度，训练样本仅 1000 条，耗时极短。
*   **推理阶段**：吞吐量分析在单节点 8×H100 GPU 上完成。
*   **算力开销**：相比于需要 8 次采样的“多数投票”（Majority Voting），TARo 的计算开销降低了约 4 倍，且推理延迟增加微乎其微（尤其是 Top-k 方案）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 70B 不同参数规模的模型，验证了方法的普适性。
*   **消融实验**：非常充分。包括：
    *   **路由粒度**：对比了提示级（Prompt-level）与令牌级（Token-level）路由，证明了细粒度控制的必要性。
    *   **输入特征**：对比了隐藏状态（Hidden States）与逻辑值（Logits）作为输入的差异。
    *   **Top-k 影响**：分析了不同 K 值及熵正则化对性能的影响。
*   **客观性**：通过在分布外（OOD）任务（医疗、通用指令）上的测试，客观评估了该方法在非数学领域的迁移能力。

### 6. 主要结论与发现
*   **性能提升**：在 MATH500 上比基础模型提升高达 **22.4%**，比 GenARM 提升 **8.4%**。
*   **弱到强的泛化（Weak-to-Strong）**：在小模型上训练的路由器和奖励模型，可以直接应用于大模型（如从 8B 迁移到 70B）并取得显著收益，无需重新训练。
*   **动态引导**：分析显示，路由器在处理数学算子、格式标记和推理脚手架（如 "Step", "cases"）时会赋予奖励模型更高的权重（高 $\alpha$），而在处理问题背景词时则信任基础模型。

### 7. 优点
*   **自适应性**：消除了手动调节插值权重 $\alpha$ 的繁琐过程，实现了步进式的动态对齐。
*   **高效率**：无需更新基础模型参数，推理开销远低于采样类方法（如 Best-of-N）。
*   **跨模型迁移**：逻辑值空间的路由设计使其具备极强的架构无关性和规模无关性。
*   **逻辑感知**：通过分步数学轨迹训练，使对齐从单纯的“风格模仿”提升到了“逻辑引导”。

### 8. 不足与局限
*   **奖励模型依赖**：性能高度依赖于奖励模型的质量。如果奖励模型在某些极端 OOD 领域表现极差，路由器的纠偏能力可能受限。
*   **词表限制**：全量逻辑值拼接方案受限于词表大小（Vocabulary Size），在词表极大的模型上会增加参数量和推理延迟。
*   **自我修正能力**：定性分析显示，模型在推理策略错误时仍缺乏有效的自我回溯和修正机制（如 Table 11 所示）。

（完）
