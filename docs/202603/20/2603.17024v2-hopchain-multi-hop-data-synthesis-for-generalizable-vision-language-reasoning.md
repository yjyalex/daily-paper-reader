---
title: "HopChain: Multi-Hop Data Synthesis for Generalizable Vision-Language Reasoning"
title_zh: HopChain：用于可泛化视觉-语言推理的多跳数据合成
authors: "Shenzhi Wang, Shixuan Liu, Jing Zhou, Chang Gao, Xiong-Hui Chen, Binghai Wang, An Yang, Shiji Song, Bowen Yu, Gao Huang, Junyang Lin"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17024v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 视觉语言推理中带有可验证奖励的强化学习数据合成
tldr: 针对视觉语言模型（VLM）在复杂推理中容易出现感知和逻辑错误的问题，本文提出了HopChain框架。该框架能自动合成具有逻辑依赖关系的多步（multi-hop）推理数据，每一步都基于视觉实例，且最终答案为可验证的数值。通过在Qwen3.5模型上进行强化学习训练，HopChain显著提升了模型在STEM、文档理解等24个基准测试中的泛化推理能力，尤其在长思维链推理任务中表现卓越。
selection_source: fresh_fetch
motivation: 现有的视觉语言强化学习数据缺乏复杂的逻辑推理链，导致模型在处理细粒度多步推理时容易产生幻觉和逻辑断裂。
method: 提出HopChain框架，通过合成逻辑相互依赖的实例级多步推理链，并以可验证的数值作为最终答案，用于模型的强化学习训练。
result: 在24个基准测试中，HopChain在20个测试上均取得了显著提升，特别是在超长思维链推理场景下性能提升超过50分。
conclusion: HopChain是一种有效且可扩展的多步数据合成框架，能够显著增强视觉语言模型的通用推理能力和长思维链表现。
---

## 摘要
视觉-语言模型 (VLM) 展现出强大的多模态能力，但在细粒度视觉-语言推理方面仍面临挑战。我们发现，长思维链 (CoT) 推理会暴露多种失败模式，包括感知、推理、知识和幻觉错误，这些错误可能会在中间步骤中累积。然而，大多数用于可验证奖励强化学习 (RLVR) 的现有视觉-语言数据并不涉及贯穿始终依赖视觉证据的复杂推理链，导致这些弱点在很大程度上未被暴露。因此，我们提出了 HopChain，这是一个可扩展的框架，用于合成多跳视觉-语言推理数据，以进行 VLM 的 RLVR 训练。每个合成的多跳查询都形成了一个逻辑依赖的、基于实例的跳跃链，其中前序跳跃建立了后续跳跃所需的实例、集合或条件，而最终答案保持为适合可验证奖励的特定且明确的数字。我们在两种 RLVR 设置下训练了 Qwen3.5-35B-A3B 和 Qwen3.5-397B-A17B：仅使用原始数据，以及原始数据加上 HopChain 的多跳数据，并在涵盖 STEM 与谜题、通用 VQA、文本识别与文档理解以及视频理解的 24 个基准测试中进行了比较。尽管这些多跳数据并非针对任何特定基准测试合成，但它在两个模型上的 24 个基准测试中改进了 20 个，表明了广泛且可泛化的提升。一致地，将完整的链式查询替换为半多跳或单跳变体，使五个代表性基准测试的平均得分分别从 70.4 下降到 66.7 和 64.3。值得注意的是，多跳带来的增益在长 CoT 视觉-语言推理中达到顶峰，在超长 CoT 机制下超过了 50 分。这些实验证明 HopChain 是一个有效且可扩展的框架，用于合成多跳数据以提升可泛化的视觉-语言推理能力。

## Abstract
Vision-language models (VLMs) show strong multimodal capabilities but still struggle with fine-grained vision-language reasoning. We find that long chain-of-thought (CoT) reasoning exposes diverse failure modes, including perception, reasoning, knowledge, and hallucination errors, which can compound across intermediate steps. However, most existing vision-language data used for reinforcement learning with verifiable rewards (RLVR) does not involve complex reasoning chains that rely on visual evidence throughout, leaving these weaknesses largely unexposed. We therefore propose HopChain, a scalable framework for synthesizing multi-hop vision-language reasoning data for RLVR training of VLMs. Each synthesized multi-hop query forms a logically dependent chain of instance-grounded hops, where earlier hops establish the instances, sets, or conditions needed for later hops, while the final answer remains a specific, unambiguous number suitable for verifiable rewards. We train Qwen3.5-35B-A3B and Qwen3.5-397B-A17B under two RLVR settings: the original data alone, and the original data plus HopChain's multi-hop data, and compare them across 24 benchmarks spanning STEM and Puzzle, General VQA, Text Recognition and Document Understanding, and Video Understanding. Although this multi-hop data is not synthesized for any specific benchmark, it improves 20 of 24 benchmarks on both models, indicating broad and generalizable gains. Consistently, replacing full chained queries with half-multi-hop or single-hop variants reduces the average score across five representative benchmarks from 70.4 to 66.7 and 64.3, respectively. Notably, multi-hop gains peak in long-CoT vision-language reasoning, exceeding 50 points in the ultra-long-CoT regime. These experiments establish HopChain as an effective, scalable framework for synthesizing multi-hop data that improves generalizable vision-language reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：HopChain - 用于可泛化视觉-语言推理的多跳数据合成

#### 1. 核心问题与整体含义
视觉-语言模型（VLM）在处理复杂任务时，虽然能生成长思维链（CoT），但往往在中间步骤出现感知错误、逻辑断裂或幻觉，且这些错误会随推理链条不断累积。现有的强化学习（RLVR）训练数据大多缺乏对视觉证据的深度依赖，导致模型容易走“语言捷径”。本文提出了 **HopChain** 框架，旨在合成一种“多跳（Multi-Hop）”推理数据，强制模型在推理的每一步都必须重新回到图像中寻找证据，从而提升模型在长思维链下的鲁棒性和泛化推理能力。

#### 2. 方法论
HopChain 的核心思想是构建具有**逻辑依赖关系**的推理链条，确保前一步的结果是后一步的前提，且最终答案为易于验证的数值。

*   **多跳定义**：
    *   **感知层跳跃（Perception-level hop）**：在单对象感知（如读字、识色）与多对象关系推理（如比较大小、空间排列）之间切换。
    *   **实例链跳跃（Instance-chain hop）**：遵循显式的依赖链（如实例 A → B → C），只有识别了 A 才能找到 B。
*   **合成流水线（四阶段）**：
    1.  **类别识别**：利用强 VLM（如 Qwen3-VL-Thinking）识别图中所有语义类别。
    2.  **实例分割**：使用 SAM3 对识别出的类别进行像素级分割和定位。
    3.  **多跳查询生成**：利用强 VLM 基于选定的实例组合生成逻辑严密的连环问题，要求问题描述仅依赖空间和视觉属性，严禁泄露坐标或掩码信息。
    4.  **人工校验与难度校准**：通过多名标注员独立求解，仅保留答案一致的样本；并利用弱模型剔除过于简单的题目。
*   **训练算法**：采用 **SAPO（软自适应策略优化）** 算法进行强化学习，利用数值答案作为可验证奖励。

#### 3. 实验设计
*   **模型对象**：Qwen3.5-35B-A3B 和 Qwen3.5-397B-A17B。
*   **基准测试（24个）**：
    *   **STEM 与谜题**：MathVision, MMMU Pro, MathVista, ZeroBench 等。
    *   **通用 VQA**：MMBench, RealWorldQA, MMStar, AI2D 等。
    *   **文档理解**：CharXiv, DocVQA, InfoVQA。
    *   **视频理解**：Video-MME, VideoMMMU, MVBench 等。
*   **对比设置**：
    *   **Before RLVR**：SFT 后的基准模型。
    *   **RLVR w/o Multi-Hop**：仅使用原始 RLVR 数据训练。
    *   **RLVR w/ Multi-Hop**：原始数据 + HopChain 合成数据。
*   **消融实验**：对比了“单跳（Single-Hop）”、“半程多跳（Half-Multi-Hop）”与“完整多跳”的效果。

#### 4. 资源与算力
*   论文提到了训练的具体配置：
    *   **Qwen3.5-35B**：每次 Rollout 采样 16 个响应，处理 256 个查询，Mini-batch 为 64，运行 1000 个梯度步。
    *   **Qwen3.5-397B**：Mini-batch 增加至 128，运行 800 个梯度步。
    *   **学习率**：$2.0 \times 10^{-6}$。
*   **未明确说明**：具体的 GPU 型号（如 H100/A100）及具体数量，但考虑到 397B 参数规模，推测使用了大规模计算集群。

#### 5. 实验数量与充分性
*   **实验规模**：在 24 个主流基准测试上进行了验证，涵盖了从静态图像到动态视频、从基础感知到高阶数学推理的广泛场景。
*   **充分性**：实验不仅对比了最终得分，还深入分析了响应长度与准确率的关系、错误类型的分布变化以及模型规模的敏感性。
*   **客观性**：通过消融实验证明了“多跳逻辑链”本身的价值，而非仅仅是数据量的增加；且合成数据并未针对特定 Benchmark 进行优化，展现了良好的泛化性。

#### 6. 主要结论与发现
*   **全面提升**：在两个模型规模上，HopChain 均在 24 个基准测试中的 20 个取得了提升。
*   **长 CoT 优势**：在超长思维链（Ultra-long-CoT）场景下，性能提升最为显著，最高可达 50 分以上。
*   **跨模态泛化**：虽然训练数据基于图像，但在视频理解任务上也表现出显著的性能增强。
*   **结构必要性**：消融实验证实，缩短推理链或简化为单跳任务会显著降低训练效果。

#### 7. 优点
*   **可扩展性强**：全自动化的合成流水线，能够大规模利用无标注图像生成高质量推理数据。
*   **结构化设计**：通过“实例依赖”强制模型进行视觉回溯，有效解决了长推理中的“证据漂移”问题。
*   **奖励可验证**：最终答案为数值，完美契合 RLVR 框架，避免了模型在训练中通过刷语言风格来骗取奖励。

#### 8. 不足与局限
*   **实例依赖性**：该框架高度依赖实例分割模型（如 SAM3）的效果，对于缺乏明确可分割对象的图像（如纯风景、抽象纹理）难以处理。
*   **合成模型偏置**：数据生成依赖于 Qwen3-VL-Thinking 等强模型，可能存在生成模型的固有偏置。
*   **计算成本**：生成过程涉及多次强 VLM 调用和人工校验，虽然比纯人工标注便宜，但仍有一定资源门槛。

（完）
