---
title: Are complicated loss functions necessary for teaching LLMs to reason?
title_zh: 复杂的损失函数对于教大语言模型（LLMs）进行推理是必要的吗？
authors: "Gabriele Carrino, Andrea Sassella, Nicolo Brunello, Federico Toschi, Mark James Carman"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18756v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对数学推理的GRPO和PPO算法分析
tldr: 本研究探讨了在提升大语言模型推理能力时，复杂的损失函数是否必要。通过对GRPO算法的系统分析，研究发现负反馈至关重要，而PPO风格的裁剪机制并非必须。基于此，作者提出了简化的RGRA算法，该算法保留了组相对优势估计但去除了复杂的策略裁剪。实验证明，RGRA在数学基准测试中表现优于GRPO，为提升模型推理能力提供了一种更高效、透明的替代方案。
selection_source: fresh_fetch
motivation: 旨在探究GRPO等复杂强化学习算法中的各项组件（如PPO裁剪）对于提升大模型推理能力是否真正必要。
method: 提出了RGRA算法，该方法在保留组相对优势估计的基础上，移除了PPO风格的策略比率裁剪和KL正则化项。
result: 实验结果显示，RGRA在标准数学基准测试上的表现潜力超过了复杂的GRPO算法。
conclusion: 简单的基于REINFORCE的算法足以有效增强LLM的推理能力，无需复杂的损失函数约束。
---

## 摘要
最近大语言模型（LLMs）的进展突显了后训练技术在提升推理和数学能力方面的重要性。组相对策略优化（GRPO）通过结合组相对优势估计、PPO 风格的裁剪以及 KL 正则化，在该领域展现出了前景。然而，其复杂性引发了一个疑问：是否所有组件对于培养推理行为都是必要的。我们对 GRPO 进行了系统分析，并得出两个关键发现：(1) 引入负反馈至关重要，仅在高于基准的动作上进行训练会限制学习效果；(2) PPO 风格的约束（如策略比例裁剪）对于提升数学推理或性能并非必需。基于这些见解，我们提出了带有组相对优势的 REINFORCE（RGRA），这是一种简化变体，它保留了组相对优势估计，但移除了 PPO 风格的裁剪和策略比例项。在标准数学基准测试上的实验表明，RGRA 有潜力实现比 GRPO 更强的性能。我们的结果表明，更简单的基于 REINFORCE 的方法可以有效地增强 LLMs 的推理能力，为 GRPO 提供了一个更透明且高效的替代方案。

## Abstract
Recent advances in large language models (LLMs) highlight the importance of post training techniques for improving reasoning and mathematical ability. Group Relative Policy Optimization (GRPO) has shown promise in this domain by combining group relative advantage estimation, PPO style clipping, and KL regularization. However, its complexity raises the question of whether all components are necessary for fostering reasoning behaviors. We conduct a systematic analysis of GRPO and identify two key findings: (1) incorporating negative feedback is essential training solely on actions above a baseline limits learning; and (2) PPO style constraints, such as policy ratio clipping, are not required to improve mathematical reasoning or performance. Building on these insights, we propose REINFORCE with Group Relative Advantage (RGRA), a simplified variant that retains group relative advantage estimation but removes PPO style clipping and policy ratio terms. Experiments across standard mathematical benchmarks indicate that RGRA has the potential to achieve stronger performance than GRPO. Our results suggest that simpler REINFORCE based approaches can effectively enhance reasoning in LLMs, offering a more transparent and efficient alternative to GRPO.

---

## 论文详细总结（自动生成）

这篇论文对当前大语言模型（LLM）推理训练中流行的 GRPO 算法进行了深入的“减法”分析，探讨了复杂损失函数组件的必要性。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在通过强化学习（RL）提升 LLM 推理能力时，像 GRPO 这样包含“组相对优势估计”、“PPO 风格裁剪（Clipping）”和“KL 正则化”的复杂损失函数是否真的必要？
*   **研究背景**：DeepSeek-R1 的成功让 GRPO 成为推理训练的标准，但其设计融合了多种机制。作者怀疑对于已经经过指令微调（Instruction-tuned）的强初始模型，某些复杂的约束（如 PPO 的裁剪机制）可能是冗余的。

### 2. 方法论：RGRA 算法
作者通过系统性消融实验，提出了一种简化的算法 **RGRA (REINFORCE with Group Relative Advantage)**：
*   **核心思想**：保留 GRPO 的“组相对优势估计”以提供稳定的基准，但彻底移除 PPO 风格的“策略比率裁剪（Policy Ratio Clipping）”。
*   **技术细节**：
    *   **组相对优势**：对同一个 Prompt 生成 $G$ 个回答，计算它们的奖励均值和标准差，将原始奖励标准化为优势值（Advantage）。
    *   **移除裁剪**：不再限制新旧策略的比率，回归到标准的 REINFORCE 梯度上升形式。
    *   **保留 KL 惩罚**：在损失函数中保留 KL 散度项，以防止模型偏离参考模型太远。
*   **对比变体**：实验中还对比了“仅正向反馈（GRPO-pos）”、“直接奖励的 REINFORCE”以及“拒绝采样微调（RAFT）”。

### 3. 实验设计
*   **模型**：Qwen2.5-0.5B/1.5B-it、Llama3.2-1B-it（均为指令微调版）。
*   **训练数据**：从 GSM8K 训练集中随机抽取 1,800 个样本。
*   **评测基准（Benchmark）**：涵盖 9 个中英文数学与 STEM 任务，包括 GSM8K、MATH、Gaokao2023/2024、OlympiadBench、AMC23、CMATH、CN-Middle-School、MMLU-STEM。
*   **对比方法**：GRPO（原始版）、RGRA（本文提出）、RAFT、纯 REINFORCE、SFT（监督微调）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **技术手段**：使用了 LoRA（Rank=128）进行高效微调，推理引擎采用 VLLM。作者在结论中提到受限于“硬件约束（Hardware constraints）”，因此未能在更大规模模型上测试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构/规模的模型，并在 9 个主流基准上进行了测试。
*   **消融实验**：针对“负反馈的必要性”和“优势估计的必要性”做了专门的对比实验。
*   **充分性评价**：实验设计较为客观、公平（使用了相同的超参数和训练集）。虽然模型参数量较小（最高 1.5B），但对于验证损失函数组件的有效性而言，实验组设置是充分且具有说服力的。

### 6. 主要结论与发现
1.  **负反馈不可或缺**：如果只学习奖励高于基准的样本（GRPO-pos），模型会迅速发生“奖励黑客（Reward Hacking）”行为，导致输出崩溃或变短。
2.  **优势估计是关键**：直接使用原始奖励的 REINFORCE 会导致训练不稳定甚至崩溃，组相对优势估计提供了必要的稳定性。
3.  **PPO 裁剪并非必要**：移除裁剪机制后的 RGRA 在 27 项对比测试中，有 17 项表现优于原始 GRPO，且训练同样稳定。
4.  **推理行为涌现**：RGRA 同样能诱导模型产生“思维链（CoT）”和自我反思行为。

### 7. 优点与亮点
*   **大道至简**：挑战了“越复杂越好”的直觉，证明了在 LLM 领域，简单的算法改进（RGRA）可能比复杂的传统 RL 约束更有效。
*   **透明性**：RGRA 相比 GRPO 更易于实现和调试，减少了超参数（如裁剪系数 $\epsilon$）的调优负担。
*   **系统性消融**：通过严谨的对比，理清了 GRPO 中哪些部分是“肌肉”，哪些部分是“脂肪”。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于 1.5B 及以下的小模型，在大模型（如 7B、70B）上是否依然不需要裁剪机制尚待验证。
*   **任务单一性**：主要集中在数学和 STEM 等具有明确奖励信号的推理任务，在开放式对话或主观偏好对齐任务中效果未知。
*   **训练数据量较小**：仅使用了 1800 条数据，虽然证明了样本效率，但未展示在大规模数据下的长期稳定性。

（完）
