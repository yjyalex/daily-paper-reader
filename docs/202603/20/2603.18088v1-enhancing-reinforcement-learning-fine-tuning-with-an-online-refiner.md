---
title: Enhancing Reinforcement Learning Fine-Tuning with an Online Refiner
title_zh: 通过在线精炼器增强强化学习微调
authors: "Hao Ma, Zhiqiang Pu, Yang Liu, Xiaolin Ai"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18088v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 稳定大语言模型的强化学习微调
tldr: 强化学习微调（RFT）中，固定约束常导致优化受限。本文提出“动态约束”机制，通过引入在线精炼器（Online Refiner）实时修正模型输出。精炼器基于参考模型对生成内容进行最小化错误修复，并利用监督微调损失引导模型学习。该方法在对话和代码生成任务中显著提升了任务奖励，同时保持了训练稳定性，有效解决了性能提升与防止退化之间的矛盾。
selection_source: fresh_fetch
motivation: 传统的固定约束在防止模型退化的同时限制了其探索更优解的能力，需要在稳定性和优化目标之间取得平衡。
method: 提出一种动态约束方法，利用参考模型作为在线精炼器对微调模型的输出进行最小化修正，并通过监督学习引导模型优化。
result: 在对话和代码生成实验中，该方法在任务奖励和训练稳定性方面均优于KL正则化及无约束基准。
conclusion: 动态约束机制能根据输出质量自动调整干预强度，为强化学习微调提供了一种更灵活且高效的正则化方案。
---

## 摘要
约束对于稳定强化学习微调 (RFT) 和防止退化输出至关重要，但它们与优化目标本质上存在冲突，因为更强的约束限制了微调模型发现更好解决方案的能力。我们提出了“动态约束”，通过适应微调模型不断演进的能力来解决这种紧张关系，其核心见解是约束仅应在出现退化输出时进行干预。我们通过使用参考模型作为“在线精炼器”来实现这一点，该精炼器接收来自微调模型的响应，并生成一个最小修正版本，在修复错误的同时逐字保留正确内容。随后，通过监督微调损失训练该微调模型以产生精炼后的输出。这种机制产生了一种能够根据输出质量自动加强或放松的约束。在对话和代码生成方面的实验表明，动态约束优于 KL 正则化和无约束基线，在保持训练稳定性的同时实现了显著更高的任务奖励。

## Abstract
Constraints are essential for stabilizing reinforcement learning fine-tuning (RFT) and preventing degenerate outputs, yet they inherently conflict with the optimization objective because stronger constraints limit the ability of a fine-tuned model to discover better solutions. We propose \textit{dynamic constraints} that resolve this tension by adapting to the evolving capabilities of the fine-tuned model based on the insight that constraints should only intervene when degenerate outputs occur. We implement this by using a reference model as an \textit{online refiner} that takes the response from the fine-tuned model and generates a minimally corrected version which preserves correct content verbatim while fixing errors. A supervised fine-tuning loss then trains the fine-tuned model to produce the refined output. This mechanism yields a constraint that automatically strengthens or relaxes based on output quality. Experiments on dialogue and code generation show that dynamic constraints outperform both KL regularization and unconstrained baselines, achieving substantially higher task rewards while maintaining training stability.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Reinforcement Learning Fine-Tuning with an Online Refiner》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：强化学习微调（RFT）是提升大语言模型（LLM）任务特定能力的关键。然而，RFT 面临“灾难性遗忘”和“分布崩溃”的风险。
*   **核心矛盾**：为了稳定训练，通常引入 KL 散度正则化（静态约束）。但这种静态约束将模型锚定在初始参考模型附近，限制了模型探索更优解的能力，导致性能触及瓶颈。
*   **论文目标**：提出一种**动态约束（Dynamic Constraint）**机制，旨在打破稳定性与优化目标之间的权衡，允许模型在保持输出连贯性的同时，突破初始性能限制。

### 2. 方法论
*   **核心思想**：约束不应是固定的锚点，而应是随模型进化的导航。只有当模型产生退化或错误输出时，约束才进行干预。
*   **关键技术：在线精炼器（Online Refiner）**：
    *   **生成与修正**：微调模型 $\pi_\theta$ 生成响应 $a$ 后，利用参考模型 $\pi_0$ 作为精炼器，结合原始查询 $s_0$ 和响应 $a$ 生成修正版本 $a'$。
    *   **最小化编辑**：精炼器被提示（Prompt）要求尽可能保留正确内容，仅针对错误逻辑或语法进行最小化修正。
    *   **动态 SFT 损失**：将修正后的 $a'$ 作为目标，计算交叉熵损失（类似 SFT 损失），并将其作为惩罚项加入 RL 目标函数中。
*   **过滤机制**：为了防止精炼器引入负面引导，仅当精炼后的响应奖励 $r(s_0, a')$ 高于原始响应 $r(s_0, a)$ 时，才应用该约束。
*   **数学表达**：将传统的 KL 散度项替换为基于精炼输出的交叉熵项，使模型在参数空间中能够远离初始点，但在输出质量上受到实时引导。

### 3. 实验设计
*   **实验场景**：
    1.  **对话生成**：使用 `Prompt-Collection-v0.1` 数据集（包含 UltraFeedback 等子集）。
    2.  **代码生成**：使用 `APPS` 数据集进行训练。
*   **Benchmark 与对比方法**：
    *   **对比基准**：静态 KL 正则化（Static）、无约束 RL（w/o Constraint）、以及最先进的 RFT 算法 **DAPO**。
    *   **评估指标**：任务奖励（Task Reward）、KL 散度、以及代码领域的 Pass@1（在 HumanEval、HumanEval+、MBPP、MBPP++ 上测试）。
*   **基础模型**：Llama-3.2-3B-Instruct 和 Qwen2.5-Coder-1.5B-Instruct。

### 4. 资源与算力
*   **硬件配置**：使用了 8 张 **NVIDIA A6000 (48GB)** GPU 和 2 颗 AMD EPYC 7773X CPU。
*   **训练时长**：
    *   DAPO 基准：约 17.8 小时。
    *   本文动态约束方法：约 **26.3 小时**。
*   **算力开销**：引入在线精炼器增加了推理开销，导致训练时间增加了约 **48%**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了对话和代码两大主流任务。
    *   在代码任务中使用了 4 个权威测试集进行泛化性验证。
    *   进行了详细的**消融实验**，探讨了惩罚系数 $\eta$ 的影响以及过滤机制的必要性。
*   **充分性评价**：实验设计较为充分。通过训练曲线展示了模型在长程训练中的稳定性，并利用定性案例（Case Study）直观展示了精炼器如何修正逻辑错误。对比 SOTA 方法（DAPO）证明了其在实际应用中的优越性。

### 6. 主要结论与发现
*   **突破性能天花板**：动态约束允许 KL 散度持续上升（即模型深度探索），而任务奖励随之稳步增长，避开了静态 KL 导致的性能早熟停滞。
*   **增强稳定性**：相比无约束方法，动态约束有效防止了分布崩溃，即使模型远离初始分布，输出依然保持连贯。
*   **性能提升显著**：在代码生成任务中，相比 DAPO，该方法在四个基准测试集上平均实现了 **30.8%** 的相对性能提升。
*   **自适应干预**：随着模型能力增强，精炼器干预的比例自然下降，形成了一种“自退火”效应。

### 7. 优点与亮点
*   **理念创新**：将“约束”从静态的分布限制转变为动态的质量引导，解决了 RFT 中长期存在的 KL 权衡难题。
*   **按需干预（Intervention-on-demand）**：通过最小化修正，最大限度地保留了模型的探索自由度。
*   **闭环进化**：建立了一个 RL 探索与 SFT 修正的在线循环，使模型能够从自身的尝试中通过外部反馈进行学习。

### 8. 不足与局限
*   **计算成本**：在线精炼需要额外的模型推理步骤，显著增加了训练的时间和显存开销。
*   **精炼器依赖**：方法的有效性高度依赖于参考模型（精炼器）的修正能力。如果参考模型本身在某些复杂任务上无法提供正确修正，动态约束的效果将受限。
*   **长文本挑战**：对于极长序列的推理任务，保持全局结构的一致性对精炼器提出了更高要求，文中提到未来需探索分块精炼机制。

（完）
