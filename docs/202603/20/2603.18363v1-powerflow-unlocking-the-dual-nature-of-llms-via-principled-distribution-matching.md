---
title: "PowerFlow: Unlocking the Dual Nature of LLMs via Principled Distribution Matching"
title_zh: PowerFlow：通过基于原则的分布匹配解锁大语言模型的双重特性
authors: "Ruishuo Chen, Yu Chen, Zhuoran Li, Longbo Huang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18363v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于内部反馈的无监督强化学习
tldr: 本研究针对无监督内部反馈强化学习（RLIF）中启发式奖励缺乏理论目标且易产生偏差的问题，提出了PowerFlow框架。该框架将无监督微调重构为分布匹配问题，利用GFlowNet作为变分采样器，并引入长度感知的轨迹平衡目标以消除长度偏差。通过调节α次幂分布，PowerFlow能定向强化模型的逻辑推理（分布锐化）或激发表达创意（分布平滑）。实验证明其性能优于现有RLIF方法，甚至可媲美有监督的GRPO，有效提升了生成质量与多样性。
selection_source: fresh_fetch
motivation: 现有的无监督强化学习方法依赖启发式内在奖励，缺乏明确的理论优化目标且容易产生退化偏差。
method: 提出基于GFlowNet的分布匹配框架，通过长度感知的轨迹平衡目标消除结构性长度偏差，并利用α次幂分布调节模型特性。
result: 实验显示PowerFlow在多项任务上优于现有RLIF方法，并在创意任务中实现了质量与多样性的同步提升，超越了有监督的GRPO。
conclusion: PowerFlow通过原则性的分布匹配方法，成功实现了对大模型逻辑推理与创意表达双重性质的定向激发与优化。
---

## 摘要
无监督内部反馈强化学习 (RLIF) 已成为一种在无需外部监督的情况下激发大语言模型 (LLM) 潜在能力的极具前景的范式。然而，当前的方法依赖于启发式的内在奖励，往往缺乏明确定义的理论优化目标，且容易产生退化偏差。在这项工作中，我们介绍了 PowerFlow，这是一个将无监督微调重新表述为分布匹配问题的原则性框架。通过将 GFlowNet 视为非归一化密度的摊销变分采样器，我们提出了一种长度感知的轨迹平衡 (Trajectory-Balance) 目标，显式地抵消了自回归生成中固有的结构性长度偏差。通过针对 α-幂分布，PowerFlow 能够定向激发 LLM 的双重特性：锐化分布 (α > 1) 以增强逻辑推理能力，或平滑分布 (α < 1) 以释放表达创造力。大量实验表明，PowerFlow 始终优于现有的 RLIF 方法，达到甚至超过了有监督的 GRPO。此外，通过减轻对齐模型中的过度锐化，我们的方法在多样性和质量方面同时取得了提升，推动了创造性任务中的帕累托前沿。

## Abstract
Unsupervised Reinforcement Learning from Internal Feedback (RLIF) has emerged as a promising paradigm for eliciting the latent capabilities of Large Language Models (LLMs) without external supervision. However, current methods rely on heuristic intrinsic rewards, which often lack a well-defined theoretical optimization target and are prone to degenerative biases. In this work, we introduce PowerFlow, a principled framework that reformulates unsupervised fine-tuning as a distribution matching problem. By casting GFlowNet as an amortized variational sampler for unnormalized densities, we propose a length-aware Trajectory-Balance objective that explicitly neutralizes the structural length biases inherent in autoregressive generation. By targeting $α$-power distributions, PowerFlow enables the directional elicitation of the dual nature of LLMs: sharpening the distribution ($α> 1$) to intensify logical reasoning, or flattening it ($α< 1$) to unlock expressive creativity. Extensive experiments demonstrate that PowerFlow consistently outperforms existing RLIF methods, matching or even exceeding supervised GRPO. Furthermore, by mitigating over-sharpening in aligned models, our approach achieves simultaneous gains in diversity and quality, shifting the Pareto frontier in creative tasks.

---

## 论文详细总结（自动生成）

以下是对论文《PowerFlow: Unlocking the Dual Nature of LLMs via Principled Distribution Matching》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**无监督内部反馈强化学习 (RLIF)** 领域。
*   **研究动机**：现有的 RLIF 方法（如基于自确定性或一致性的方法）主要依赖于**启发式（Heuristic）的内在奖励设计**。这些方法缺乏明确的理论优化目标，且在过度优化时容易产生退化偏差，如响应长度崩溃（过短）或爆炸（过长）、过度自信以及模式崩溃（Mode Collapse）。
*   **核心问题**：如何建立一个有理论支撑的框架，在无需外部监督（如标签或验证器）的情况下，定向激发大语言模型（LLM）的潜在能力（如逻辑推理或创意表达），并克服自回归生成中的结构性偏差？

### 2. 方法论
PowerFlow 将无监督微调重构为一个**分布匹配（Distribution Matching）**问题。
*   **核心思想**：目标是让微调后的模型 $\pi_\theta$ 匹配基座模型的 **$\alpha$-次幂分布**（$p_\alpha \propto p_{base}^\alpha$）。
    *   **锐化（$\alpha > 1$）**：将概率质量集中在潜在的高质量推理路径上，用于提升逻辑推理能力。
    *   **平滑（$\alpha < 1$）**：重新分配概率到长尾区域，用于释放被对齐算法（如 RLHF）抑制的创意多样性。
*   **关键技术细节**：
    *   **GFlowNet 作为采样器**：利用生成流网络（GFlowNet）的轨迹平衡（Trajectory-Balance, TB）目标，将其作为非归一化密度的变分采样器。
    *   **长度感知目标 (LA-TB)**：针对自回归模型中概率随长度指数衰减的问题，论文提出了**长度感知的轨迹平衡目标**。通过将配分函数（Partition Function）重构为长度归一化的能量项，使优化梯度对长度具有尺度不变性，从而防止长度崩溃。
*   **公式体现**：
    $$L_{PowerFlow} = w \cdot \left[ \log Z'_\phi(q) + \frac{1}{|y|} \log \pi_\theta(y|q) - \alpha \frac{1}{|y|} \log p_{base}(y|q) + \psi(y) \right]^2$$
    其中 $Z'_\phi$ 是学习到的摊销配分函数，$w$ 是重要性采样权重，$\psi(y)$ 是格式惩罚项。

### 3. 实验设计
*   **场景与数据集**：
    *   **推理任务**：使用 NuminaMath-CoT 的 18,000 个查询进行训练。评测涵盖 MATH500、OlympiadBench、AIME24/25、AMC23 和 GPQA。
    *   **创意写作**：涵盖诗歌续写、故事生成和笑话创作。使用来自 PoemHunter、BookMIA 和 Reddit 的数据。
*   **对比方法**：
    *   **基准**：Base 模型、Instruct 模型、温度缩放（Low-temp/High-temp）。
    *   **无监督 RLIF**：Intuitor、EMPO、TTRL、One-shot EM。
    *   **有监督方法**：使用外部验证奖励的 **GRPO**。
    *   **推理采样**：PowerSampling (MCMC 采样)。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**（如 H100 或 A100 的具体枚数）。
*   **训练细节**：提到了 Prompt Batch Size 为 128，每个 Prompt 采样 16 个样本。对于 1.5B 模型学习率为 $3 \times 10^{-6}$，对于较大模型为 $1 \times 10^{-6}$。
*   **备注**：作者提到为了公平对比，部分基座模型的 RLIF 基准使用了官方开源的 Checkpoint，而 GRPO 是在相同环境下自行训练的。

### 5. 实验数量与充分性
*   **实验规模**：实验跨越了多个模型系列（Qwen2.5, Qwen2.5-Math, Llama-3.2）和多个参数量级（1.5B, 3B, 7B）。
*   **充分性**：
    *   做了针对 $\alpha$ 参数的敏感度分析（$\alpha=2, 4, 6$）。
    *   进行了训练稳定性分析（对比了 TB-traj, TB-token 等不同目标函数对长度的影响）。
    *   引入了 LLM-as-a-judge（使用 Qwen3-plus 和 DeepSeek-V3.2）来评估解题策略的多样性和写作质量。
    *   **客观性**：通过 Pass@n 曲线分析了性能提升的来源（采样效率 vs 知识获取），结论较为客观。

### 6. 主要结论与发现
*   **推理能力**：PowerFlow ($\alpha > 1$) 在数学推理上显著优于现有无监督 RLIF 方法，甚至在多个指标上**匹配或超越了有监督的 GRPO**。
*   **创意恢复**：PowerFlow ($\alpha < 1$) 成功解决了对齐模型中的“典型性偏差”（Typicality Bias），在提升生成多样性的同时，竟然也提升了质量，实现了帕累托前沿的突破。
*   **稳定性**：LA-TB 目标函数能有效维持响应长度的稳定，避免了传统 GFlowNet 或 RL 目标在 LLM 训练中的退化问题。
*   **多样性保持**：相比于其他 RL 方法，PowerFlow 在强化推理能力时能更好地保留解题策略的多样性。

### 7. 优点
*   **理论完备性**：将启发式的奖励工程转向了原则性的分布匹配，为无监督微调提供了统一的理论框架。
*   **双重调控**：通过单一参数 $\alpha$ 即可实现“逻辑增强”与“创意释放”两种截然不同的目标。
*   **解决结构偏差**：巧妙地通过长度归一化解决了自回归模型在概率优化中的固有长度偏好问题。

### 8. 不足与局限
*   **$\alpha$ 的选择**：虽然提供了默认值，但不同模型的最优 $\alpha$ 可能与其内在熵值有关，目前仍需一定的人工调优，缺乏自动化的参数调度机制。
*   **安全风险**：论文提到分布平滑（$\alpha < 1$）可能会重新激活模型在安全对齐阶段被抑制的有害内容，需要额外的安全护栏。
*   **长度归一化的代价**：作者承认 LA-TB 是一种“务实的折中”，它虽然稳定了训练，但在数学上并不严格保证跨不同长度序列的模式排名不变。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
