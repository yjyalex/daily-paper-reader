---
title: "Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR"
title_zh: 超越“看哪里”：面向多模态可验证奖励强化学习的轨迹引导强化学习
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Mingzhu Chen, Jiancan Wu, Kuien Liu, Xiang Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26126v1"
tags: ["query:token"]
score: 10.0
evidence: 推理过程中的Token级重权和强化学习
tldr: 针对多模态大语言模型在强化学习中存在的“能定位视觉区域但难以将其转化为有效推理”的问题，本文提出了轨迹引导强化学习（TGRL）。该方法通过引入强模型的专家推理轨迹，引导策略模型在细粒度推理过程中深度整合视觉证据。结合标记级重加权和轨迹过滤技术，TGRL 在多个多模态推理基准上显著提升了性能，有效弥合了视觉感知与逻辑推理之间的断层。
selection_source: fresh_fetch
motivation: 尽管模型能关注到相关视觉区域，但往往无法将视觉证据有效融入后续推理链，导致推理过程与视觉事实脱节。
method: 提出 TGRL 框架，利用专家推理轨迹引导策略优化，并引入标记级重加权和轨迹过滤机制以确保训练的稳定性。
result: 在多个多模态推理基准测试中，TGRL 显著且持续地提高了模型的推理准确率和逻辑一致性。
conclusion: TGRL 有效解决了多模态推理中的感知与逻辑融合难题，为提升 MLLM 的复杂推理能力提供了新路径。
---

## 摘要
多模态大语言模型（MLLM）中具有可验证奖励的强化学习（RLVR）的最新进展主要集中在提高最终答案的正确性和增强视觉定位。然而，一个关键瓶颈仍然存在：尽管模型能够关注相关的视觉区域，但它们往往无法有效地将视觉证据整合到后续的推理中，导致推理链与视觉事实的关联较弱。为了解决这一问题，我们提出了轨迹引导强化学习（TGRL），该方法利用来自更强模型的专家推理轨迹，引导策略模型将视觉证据整合到细粒度的推理过程中。我们进一步引入了标记级（token-level）重加权和轨迹过滤，以确保稳定且有效的策略优化。在多个多模态推理基准上的广泛实验表明，TGRL 持续提升了推理性能，并有效地弥合了视觉感知与逻辑推理之间的差距。

## Abstract
Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) for multimodal large language models (MLLMs) have mainly focused on improving final answer correctness and strengthening visual grounding. However, a critical bottleneck remains: although models can attend to relevant visual regions, they often fail to effectively incorporate visual evidence into subsequent reasoning, leading to reasoning chains that are weakly grounded in visual facts. To address this issue, we propose Trajectory-Guided Reinforcement Learning (TGRL), which guides the policy model to integrate visual evidence into fine-grained reasoning processes using expert reasoning trajectories from stronger models. We further introduce token-level reweighting and trajectory filtering to ensure stable and effective policy optimization. Extensive experiments on multiple multimodal reasoning benchmarks demonstrate that TGRL consistently improves reasoning performance and effectively bridges the gap between visual perception and logical reasoning.

---

## 论文详细总结（自动生成）

这篇论文针对多模态大语言模型（MLLM）在强化学习中的推理断层问题提出了创新性的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态可验证奖励强化学习（RLVR）中，模型虽然能通过注意力机制“看到”相关视觉区域，但往往无法将这些视觉证据有效转化为逻辑推理步骤。
*   **研究背景**：现有的改进方法（如图像增强或感知奖励）主要解决“看哪里”的问题，而忽略了“如何利用所见信息进行推理”的过程。这导致模型可能出现感知正确但推理错误，或者利用错误的视觉事实凑出正确答案的情况。
*   **动机**：通过引入专家推理轨迹，显式地监督从“感知”到“推理”的转换过程，从而弥合两者之间的断层。

### 2. 方法论：轨迹引导强化学习 (TGRL)
TGRL 的核心思想是将专家模型的推理路径作为结构化监督信号整合进 RL 训练循环中。
*   **轨迹整合 (Trajectory Integration)**：在生成阶段，将当前策略生成的“在策略（on-policy）”轨迹与强模型生成的“专家（expert）”轨迹混合，共同组成 Rollout 组。通过组内相对优势（Advantage）计算，使模型向高奖励的专家路径对齐。
*   **标记级重加权 (Token-level Reweighting)**：
    *   **重要性采样修正**：针对专家轨迹属于“离策略（off-policy）”数据的问题，引入重要性比率修正分布偏差。
    *   **自适应权重**：计算学生模型与专家模型在每个 Token 上的概率差异（Discrepancy），对差异较大的 Token 赋予更高权重，迫使模型重点学习其尚未掌握的推理逻辑。
*   **轨迹过滤 (Trajectory Filtering)**：仅保留最终答案正确的专家轨迹。通过掩码机制过滤掉失败的专家路径，避免引入梯度噪声。
*   **算法兼容性**：该方法可无缝集成到 GRPO 和 DAPO 等主流 RLVR 优化框架中。

### 3. 实验设计
*   **训练数据集**：
    *   **Geo3K**：2.1K 几何推理样本。
    *   **ViRL39K**：39K 大规模多模态推理样本。
*   **评估基准 (Benchmarks)**：
    *   **推理能力**：MathVista, WeMath, MathVision, MathVerse。
    *   **感知/幻觉**：HallusionBench。
*   **对比方法**：包括 vanilla GRPO/DAPO，以及 Perception-R1, ThinkLite, NoisyRollout, OpenVLThinker 等最新的多模态推理 SOTA 方法。
*   **模型配置**：学生模型为 Qwen2.5-VL-3B/7B，专家模型为 Qwen2.5-VL-32B。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到使用了 `EasyR1` 训练框架，Geo3K 训练 15 个 epoch，ViRL39K 训练 2 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小规模（2.1K）到中大规模（39K）的训练场景，并在 5 个主流基准上进行了测试。
*   **消融实验**：详细分析了“轨迹过滤”和“重加权”模块的必要性，并对超参数 $\beta$（控制权重敏感度）进行了敏感性分析。
*   **充分性评价**：实验设计较为全面，通过跨模型规模（3B/7B）和跨算法（GRPO/DAPO）的验证，证明了方法的普适性和稳健性。对比实验中使用了相同的基座模型，确保了比较的公平性。

### 6. 主要结论与发现
*   **性能提升**：TGRL 在所有基准上均显著优于基准 RL 方法。例如，在 39K 数据规模下，TGRL-DAPO 在五个基准上的平均分达到了 60.68，超过了现有的 SOTA。
*   **弥合断层**：实验证明，仅仅提供感知奖励是不够的，轨迹级的引导能更有效地帮助模型建立“视觉事实 $\rightarrow$ 逻辑步骤”的映射。
*   **专家模型门槛**：即使使用相对较弱的开源专家（32B 模型），也能显著提升较小模型（7B）的推理表现。

### 7. 优点
*   **针对性强**：精准捕捉到了多模态推理中“感知-推理转换”这一关键瓶颈。
*   **细粒度优化**：通过 Token 级的重加权机制，实现了比传统结果级奖励更精准的信用分配（Credit Assignment）。
*   **即插即用**：作为一种通用的训练策略，可以轻松集成到现有的 RL 框架中，无需修改模型架构。

### 8. 不足与局限
*   **依赖专家模型**：性能上限受限于专家轨迹的质量。如果专家模型在某些极端复杂场景下也无法生成正确路径，该方法将失效。
*   **计算开销**：在训练过程中需要额外存储和处理专家轨迹，且计算 Token 级差异会增加一定的计算负担。
*   **离策略稳定性**：虽然引入了修正机制，但大规模离策略数据在 RL 训练中的长期稳定性仍是一个潜在挑战。

（完）
