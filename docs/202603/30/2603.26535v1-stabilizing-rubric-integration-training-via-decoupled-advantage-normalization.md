---
title: Stabilizing Rubric Integration Training via Decoupled Advantage Normalization
title_zh: 通过解耦优势归一化稳定准则集成训练
authors: "Zelin Tan, Zhouliang Yu, Bohan Lin, Zijie Geng, Hejia Geng, Yudong Zhang, Mulei Zhang, Yang Chen, Shuyue Hu, Zhenfei Yin, Chen Zhang, Lei Bai"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26535v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 大语言模型推理的过程感知策略优化
tldr: 本研究针对大模型强化学习中结果奖励模型（ORM）信号缺失和过程奖励模型（PRM）易导致奖励作弊的问题，提出了过程感知策略优化（PAPO）。该方法通过解耦优势归一化技术，将优势函数分为基于所有样本的结果部分和仅基于正确样本的过程部分。实验证明，PAPO能有效提升推理质量，在多个基准测试中显著优于传统ORM，解决了模型在训练后期性能停滞或下降的问题。
selection_source: fresh_fetch
motivation: 现有的结果奖励模型无法区分推理质量，而过程奖励模型容易引发奖励作弊导致模型准确率下降。
method: 提出PAPO方法，通过解耦优势归一化，将结果奖励在全局归一化，而将过程奖励仅在正确回答中归一化。
result: "在多个模型规模和六个基准测试中，PAPO均优于ORM，在OlympiadBench上将准确率从46.3%提升至51.3%。"
conclusion: 解耦优势归一化能有效结合结果与过程监督，在保证正确性的同时提升推理深度，克服了传统强化学习的局限性。
---

## 摘要
我们提出了过程感知策略优化 (Process-Aware Policy Optimization, PAPO)，该方法通过解耦优势归一化将过程级评估集成到群体相对策略优化 (GRPO) 中，以解决现有奖励设计的两个局限性。结果奖励模型 (ORM) 仅评估最终答案的正确性，无论推理质量如何，都对所有正确回答一视同仁，且随着群体回答趋于一致正确，会逐渐失去优势信号。过程奖励模型 (PRM) 提供更丰富的监督，但直接使用 PRM 分数会导致奖励作弊 (reward hacking)，即模型利用冗长性来虚增分数，而准确率却大幅下降。PAPO 通过组合两种优势组件解决了这些问题：一个是源自 ORM 并在所有回答中归一化的结果组件 Aout，另一个是源自基于准则的 PRM 且仅在正确回答中归一化的过程组件 Aproc。这种解耦设计确保了 Aout 将训练锚定在正确性上，而 Aproc 在不扭曲结果信号的情况下区分推理质量。在多个模型规模和六个基准测试上的实验表明，PAPO 一贯优于 ORM，在 OlympiadBench 上达到 51.3% 对 46.3%，且在 ORM 进入平台期并下降时仍能持续提升。

## Abstract
We propose Process-Aware Policy Optimization (PAPO), a method that integrates process-level evaluation into Group Relative Policy Optimization (GRPO) through decoupled advantage normalization, to address two limitations of existing reward designs. Outcome reward models (ORM) evaluate only final-answer correctness, treating all correct responses identically regardless of reasoning quality, and gradually lose the advantage signal as groups become uniformly correct. Process reward models (PRM) offer richer supervision, but directly using PRM scores causes reward hacking, where models exploit verbosity to inflate scores while accuracy collapses. PAPO resolves both by composing the advantage from an outcome component Aout, derived from ORM and normalized over all responses, and a process component Aproc, derived from a rubric-based PRM and normalized exclusively among correct responses. This decoupled design ensures that Aout anchors training on correctness while Aproc differentiates reasoning quality without distorting the outcome signal. Experiments across multiple model scales and six benchmarks demonstrate that PAPO consistently outperforms ORM, reaching 51.3% vs.\ 46.3% on OlympiadBench while continuing to improve as ORM plateaus and declines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilizing Rubric Integration Training via Decoupled Advantage Normalization》的结构化深入总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在数学推理强化学习（RL）中的奖励设计难题提出了解决方案。
*   **研究动机**：现有的**结果奖励模型（ORM）**仅关注答案正确性，导致模型在训练后期因所有回答均正确而出现“信号枯竭”（梯度消失），且无法区分推理质量；而**过程奖励模型（PRM）**虽然提供细粒度监督，但直接使用会导致“奖励作弊”（Reward Hacking），即模型通过生成极其冗长的废话来骗取高分，导致准确率崩溃。
*   **核心目标**：在不引入奖励作弊的前提下，将过程级评估集成到强化学习中，以持续提升模型的推理深度和准确率。

### 2. 方法论：过程感知策略优化 (PAPO)
PAPO 的核心思想是**解耦优势归一化（Decoupled Advantage Normalization）**，将优势函数（Advantage）拆分为两个独立的部分：
*   **结果优势 ($A_{out}$)**：采用标准 GRPO 方式，对所有响应的二元结果奖励（0/1）进行全局归一化。这确保了训练的主方向始终锚定在“算出正确答案”上。
*   **过程优势 ($A_{proc}$)**：引入**正确子集归一化（Correct-Subset Normalization）**。仅针对结果正确的响应，利用 LLM-as-Judge（基于评分准则）给出过程分（如 0, 0.5, 1.0），并仅在这些正确响应内部进行均值和标准差归一化。
*   **优势组合**：最终优势 $A_{total} = A_{out} + A_{proc}$。
*   **逻辑优势**：这种设计防止了错误回答通过“看起来合理的推理”获得正向激励，同时在正确回答内部通过 $A_{proc}$ 惩罚那些靠运气或错误逻辑凑出答案的样本，解决了信号枯竭问题。

### 3. 实验设计
*   **模型规模**：测试了 Qwen2.5 (3B, 7B, 14B) 和 Qwen3-4B-Base。
*   **数据集**：使用 NuminaMath-1.5-RL-Verifiable 的 20k 题目进行分层抽样训练。
*   **基准测试 (Benchmarks)**：
    *   竞赛数学：OlympiadBench, AIME 2024, AIME 2025。
    *   标准数学与科学：MATH-500, GPQA-Diamond。
    *   代码能力：HumanEval。
*   **对比方法**：标准 GRPO (ORM)、纯 PRM 训练、ORM 与 PRM 乘积奖励 (ORM × PRM)、以及集成了 PAPO 的 DAPO 算法。

### 4. 资源与算力
*   **硬件设施**：使用了 **8 张 NVIDIA H200 GPU** 进行分布式训练。
*   **软件框架**：基于 `verl` 框架和 Megatron 后端。
*   **Judge 模型**：使用 GPT-OSS-20B 作为过程奖励模型，部署在 2 张 GPU 上进行推理。
*   **训练细节**：训练时长为 8 个 Epoch，学习率为 1e-6，每组采样 8 个响应。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同规模/架构的模型上进行了验证，涵盖了从 3B 到 14B 的跨度。
*   **消融实验**：非常充分。对比了“全量归一化”与“正确子集归一化”的区别，以及“奖励层融合”与“优势层解耦”的效果。
*   **分析维度**：除了最终分数，还深入分析了零优势比例（信号稀疏度）、梯度强度、响应长度演变等，证明了实验结果的客观性和公平性。

### 6 主要结论与发现
*   **性能突破**：PAPO 在所有基准上均优于 ORM。在 OlympiadBench 上，7B 模型从 46.3% 提升至 51.3%，14B 模型达到 59.8%。
*   **解决信号枯竭**：在训练后期，ORM 的零梯度样本占比高达 69%，而 PAPO 通过过程差异化将其降至 44%，使模型能持续学习。
*   **抑制奖励作弊**：相比纯 PRM 训练导致的长度爆炸和准确率崩溃，PAPO 的响应长度增长温和，且保持了极高的推理聚焦度。
*   **规模效应**：模型规模越大，PAPO 带来的增益越显著（14B 的增益高于 3B）。

### 7. 优点
*   **稳定性高**：通过解耦设计，巧妙地规避了 PRM 容易引发的奖励作弊风险。
*   **即插即用**：作为一种优势计算的改进，它可以轻松集成到 GRPO、DAPO 等多种策略优化算法中。
*   **无需人工标注**：利用现有的强模型作为 Judge，配合评分准则即可实现，具有很强的可扩展性。

### 8. 不足与局限
*   **模型多样性**：实验主要集中在 Qwen 系列模型，尚未在 Llama 或 Gemma 等其他架构上验证。
*   **Judge 偏差**：实验仅使用了一个 20B 的 Judge 模型，结果可能受到该特定模型评分偏好的影响。
*   **权重分配**：目前结果优势和过程优势是等权相加（1:1），未来可能需要根据训练阶段动态调整两者的权重。

（完）
