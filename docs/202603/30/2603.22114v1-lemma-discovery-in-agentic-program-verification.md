---
title: Lemma Discovery in Agentic Program Verification
title_zh: 智能体程序验证中的引理发现
authors: "Huan Zhao, Haoxin Tu, Zhengyao Liu, Martin Rinard, Abhik Roychoudhury"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22114v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大模型智能体的逻辑推理与演绎验证
tldr: 演绎验证中证明验证条件（VC）是主要瓶颈，现有LLM智能体因缺乏辅助引理发现能力而难以处理复杂程序。本文提出LemmaNet，通过程序理解辅助引理发现。它结合离线代码语义分析合成引理与在线证明状态自适应调整，显著提升了验证效率。在Linux内核等真实场景评估中，LemmaNet表现优于现有技术，证明了程序理解在引理发现中的关键作用。
selection_source: fresh_fetch
motivation: 现有证明智能体无法自动发现辅助引理，限制了其在处理大规模复杂程序验证时的自动化程度和成功率。
method: 提出LemmaNet智能体，通过离线分析源代码语义合成引理，并根据实时证明状态在线动态调整引理。
result: 在SV-COMP及Linux内核、C++标准库等真实世界项目上的实验证明，LemmaNet的性能显著超越了现有最先进的方法。
conclusion: 将程序语义理解与引理发现相结合是提升智能体程序验证能力的关键，能有效解决复杂验证条件的证明难题。
---

## 摘要
演绎验证通过提取验证条件（VCs）并为其编写形式化证明，为代码提供了强大的正确性保证。VC 证明这一高度依赖专业知识的任务是该过程中的主要瓶颈，得益于大语言模型（LLM）智能体的最新进展，该任务已实现部分自动化。然而，现有的证明智能体无法发现辅助引理（helper lemmas）——即辅助证明的额外引理——因此在程序规模和复杂性增加时表现不佳。在本文中，我们认为用于程序验证的 VC 证明不仅仅是一个纯粹的数学任务，它还能从程序理解中显著获益。我们的核心见解是，人类证明工程师通常基于对程序语义的理解来发现并应用辅助引理，而这些语义并未直接反映在 VC 生成器产生的 VC 中。受此启发，我们提出了一种名为 LemmaNet 的 LLM 智能体，它通过两种方式发现辅助引理。具体而言，该智能体首先通过直接分析源代码和规范来离线合成引理，然后将这种语义理解与 VC 生成器产生的机械且冗长的编码联系起来。随着证明的展开，LemmaNet 随后在线调整现有的辅助引理以适应不断演化的证明状态，使智能体能够有效地实时处理复杂的 VC。我们在 SV-COMP 和已有的真实世界对象上评估了 LemmaNet，包括 Linux 内核模块、Contiki OS、标准 C++ 库和 X.509 解析器。我们的实验结果表明，LemmaNet 显著优于现有最先进的方法，突显了在智能体程序验证中，受程序理解辅助的引理发现的重要性。

## Abstract
Deductive verification provides strong correctness guarantees for code by extracting verification conditions (VCs) and writing formal proofs for them. The expertise-intensive task of VC proving is the main bottleneck in this process, and has been partly automated owing to recent advances in Large Language Model (LLM) agents. However, existing proof agents are not able to discover helper lemmas - auxiliary lemmas that aid in proving - and thus fall short as programs grow in size and complexity.   In this paper, we argue that VC proving for program verification is more than a purely mathematical task, and benefits considerably from program comprehension. Our key insight is that human-proof engineers often discover and apply helper lemmas based on their understanding of the program semantics, which are not directly reflected in the VCs produced by VC generators. Inspired by this insight, we propose an LLM agent, LemmaNet, that discovers helper lemmas in two ways. Specifically, the agent first synthesizes lemmas offline by directly analyzing the source code and specifications, and then relating this semantic understanding to the mechanical, verbose encoding produced by VC generators. As the proof unfolds, LemmaNet then adapts existing helper lemmas online to accommodate evolving proof states, enabling the agent to effectively discharge complex VCs on-the-fly.   We evaluate LemmaNet on SV-COMP and established real-world subjects, including modules of the Linux kernel, Contiki OS, standard C++ library, and X.509 parser. Our experimental results demonstrate that LemmaNet significantly outperforms state-of-the-art approaches, highlighting the importance of program comprehension-aided lemma discovery in agentic program verification.

---

## 论文详细总结（自动生成）

以下是对论文《Lemma Discovery in Agentic Program Verification》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在程序演绎验证（Deductive Verification）中，证明由工具生成的验证条件（VCs）是极其耗时且依赖专家经验的。现有的LLM证明智能体（Proof Agents）虽然能自动化部分过程，但由于**缺乏发现“辅助引理”（Helper Lemmas）的能力**，在面对复杂、大规模的真实程序时往往会失败。
*   **研究动机**：
    *   **表示鸿沟**：验证工具（如Frama-C）生成的VC通常是底层、冗长且难以理解的，掩盖了程序的原始语义。
    *   **语义缺失**：现有的方法将证明视为纯数学任务，忽略了程序本身的结构和语义信息。
    *   **人类启发**：人类专家在证明时会先理解程序语义，提出中间引理来简化证明。本文旨在让AI智能体模拟这一过程。

### 2. 方法论：LemmaNet 智能体
LemmaNet 的核心思想是**通过程序理解辅助引理发现**，分为两个阶段：
*   **离线引理合成（Offline Lemma Synthesizer）**：
    *   **程序语义分析器（PSA）**：利用LLM分析源代码和ACSL规范，生成一个“语义感知型VC”（高层、直观、易证明）。
    *   **义务对齐引理合成**：LLM对比高层VC与工具生成的底层VC，合成用于弥合两者表示差异的“桥接引理”，并附带形式化证明。
*   **在线引理自适应（Online Lemma Adapter）**：
    *   **自适应引理维护**：在证明过程中动态维护引理库。
    *   **反馈驱动调整**：当证明步骤失败（如类型不匹配、定义冲突）时，智能体根据证明助手（Rocq/Coq）返回的错误反馈，实时修正或强化引理。
*   **可靠性保证**：所有生成的引理和最终证明必须通过Rocq内核的检查，确保过程虽然使用了LLM，但结果是数学上严谨的。

### 3. 实验设计
*   **数据集/场景**：
    *   **SV-COMP**：包含131个C程序，共641个VC。
    *   **NTP4VC**：目前最大的真实世界验证基准，包含300个来自Linux内核（调度器、内存管理）、Contiki OS、C++标准库、X.509解析器等的复杂VC。
*   **对比方法（Benchmarks）**：
    *   **AutoRocq**：最先进的LLM证明智能体（LemmaNet的基座）。
    *   **Copra**：基于上下文学习的证明智能体。
    *   **CoqHammer**：基于SMT求解器的传统自动化工具。

### 4. 资源与算力
*   **模型使用**：实验使用了 `gpt-5.2-2025-12-11`（注：论文标注时间为2026年，此处应为当时的最强模型），温度设为0以保证可重复性。
*   **成本开销**：论文详细记录了API成本。离线阶段平均每个VC花费约0.12-0.17美元；在线证明阶段平均花费0.36-1.03美元。
*   **算力细节**：文中未明确提及具体的GPU训练算力（因为主要基于API调用），但设定了每个VC 10分钟或100步的推理时限。

### 5. 实验数量与充分性
*   **实验规模**：共对941个真实世界的VC进行了测试，规模较大。
*   **消融实验**：设计了三个变体（无离线合成、无在线自适应、无语义分析PSA），验证了各组件的必要性。
*   **分析维度**：从VC复杂度、属性类型（内存安全、循环不变式等）、引理用途分类等多个维度进行了深入分析。
*   **评价**：实验设计客观、公平，对比了传统工具与最新的AI智能体，覆盖了从微型程序到数千行工业级代码的跨度，实验非常充分。

### 6. 主要结论与发现
*   **性能卓越**：LemmaNet在总成功率上比AutoRocq提升了**26.8%**，比Copra提升了**51.7%**。
*   **语义的重要性**：消融实验显示，加入程序语义分析（PSA）能显著提升复杂VC的证明成功率。
*   **引理的作用**：约88.4%的成功证明依赖于“桥接引理”来连接底层表示与高层语义。
*   **协同效应**：离线合成提供了大部分基础引理，而在线自适应对于处理证明过程中的突发错误至关重要。

### 7. 优点与亮点
*   **首创性**：首次将程序语义理解深度集成到智能体引理发现中，打破了“纯数学证明”的局限。
*   **端到端自动化**：实现了从代码分析到引理合成、再到动态调整的全流程自动化。
*   **工业级潜力**：在Linux内核等复杂项目上的成功，证明了该方法处理大规模软件验证的能力。
*   **安全性**：坚持“LLM提议，证明助手检查”的原则，确保了验证的绝对可靠性。

### 8. 不足与局限
*   **领域知识限制**：对于需要极深领域特定背景（如复杂数学理论）的VC，离线合成仍可能失败。
*   **成本问题**：虽然比人工便宜，但对于超大规模项目的全量验证，LLM API的调用成本依然是一笔不小的开支。
*   **依赖规范质量**：如果原始程序的ACSL注释（规范）本身写得不好，智能体提取语义的难度会大幅增加。

（完）
