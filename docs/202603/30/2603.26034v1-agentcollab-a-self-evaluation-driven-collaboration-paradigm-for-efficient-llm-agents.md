---
title: "AgentCollab: A Self-Evaluation-Driven Collaboration Paradigm for Efficient LLM Agents"
title_zh: AgentCollab：一种面向高效 LLM 智能体的自我评估驱动协作范式
authors: "Wenbo Gao, Renxi Liu, Xian Wang, Fang Guo, Shuai Yang, Xi Chen, Hui-Ling Zhen, Hanting Chen, Weizhe Lin, Xiaosong Li, Yaoyuan Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26034v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 智能体的自驱动协作推理
tldr: AgentCollab是一个旨在平衡LLM智能体执行效率与推理鲁棒性的协作框架。针对复杂任务中高成本模型与低成本模型的互补性，该框架利用智能体的自我反思信号动态协调不同能力的模型。当低成本模型在推理中遇到困难时，系统会自动升级到更强的模型，并结合难度感知的累积升级策略来稳定长程执行。实验证明，该方法在多个基准测试中显著优化了智能体的准确率与效率平衡。
selection_source: fresh_fetch
motivation: 解决LLM智能体在执行长程复杂任务时，高昂的计算成本与推理鲁棒性之间难以兼得的权衡问题。
method: 提出一种基于自我评估驱动的协作推理框架，利用智能体自身的反思信号动态触发模型升级，并引入难度感知的累积升级策略。
result: 在多个多步智能体基准测试中，AgentCollab成功提升了准确率与效率的帕累托前沿，实现了更优的性能功耗比。
conclusion: 通过动态协调不同规模的模型并利用自我反馈机制，可以有效提升LLM智能体在复杂任务中的执行效率和可靠性。
---

## 摘要
由大语言模型（LLM）驱动的自主智能体通过长程推理和工具交互执行复杂任务，这在执行效率和推理鲁棒性之间产生了一种基本的权衡。不同能力-成本水平的模型具有互补优势：低成本模型可实现快速执行，但在困难的推理环节可能表现不佳；而更强大的模型则以更高的计算成本提供更鲁棒的推理。我们提出了 AgentCollab，这是一个自我驱动的协作推理框架，可在智能体执行过程中动态协调具有不同推理能力的模型。该框架不依赖外部路由模块，而是利用智能体自身的自我反思信号来判断当前推理轨迹是否取得了实质性进展，并仅在必要时将控制权移交给更强的推理层级。为了进一步稳定长程执行，我们引入了一种难度感知的累积升级策略，根据最近的失败信号分配额外的推理预算。在实验中，我们采用“小-大”模型两级设置实例化了该框架。在多种多步智能体基准测试上的实验表明，AgentCollab 持续优化了 LLM 智能体的准确率-效率帕累托前沿。

## Abstract
Autonomous agents powered by large language models (LLMs) perform complex tasks through long-horizon reasoning and tool interaction, where a fundamental trade-off arises between execution efficiency and reasoning robustness. Models at different capability-cost levels offer complementary advantages: lower-cost models enable fast execution but may struggle on difficult reasoning segments, while stronger models provide more robust reasoning at higher computational cost. We present AgentCollab, a self-driven collaborative inference framework that dynamically coordinates models with different reasoning capacities during agent execution. Instead of relying on external routing modules, the framework uses the agent's own self-reflection signal to determine whether the current reasoning trajectory is making meaningful progress, and escalates control to a stronger reasoning tier only when necessary. To further stabilize long-horizon execution, we introduce a difficulty-aware cumulative escalation strategy that allocates additional reasoning budget based on recent failure signals. In our experiments, we instantiate this framework using a two-level small-large model setting. Experiments on diverse multi-step agent benchmarks show that AgentCollab consistently improves the accuracy-efficiency Pareto frontier of LLM agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentCollab: A Self-Evaluation-Driven Collaboration Paradigm for Efficient LLM Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：在长程（Long-horizon）推理任务中，LLM 智能体面临**执行效率**与**推理鲁棒性**的权衡。小模型速度快但易在复杂环节出错，大模型鲁棒但全过程调用成本极高且延迟大。
*   **现有局限**：现有的模型路由（Routing）技术多针对单轮对话优化成本，忽略了智能体推理轨迹（Trajectory）的连贯性。在多步交互中，一步错误可能导致后续步数激增，简单的单步路由无法有效处理这种轨迹层面的效率问题。
*   **研究目标**：提出一种无需外部路由模型、由智能体“自我驱动”的协作范式，在保证成功率的前提下，最大限度提升端到端推理速度。

### 2. 方法论：AgentCollab 框架
核心思想是利用智能体自身的**自我反思（Self-reflection）**能力来驱动模型间的切换。
*   **模型升级机制（Model Escalation）**：
    1.  **预热阶段**：初始规划由大模型（$M_L$）完成，确立高层策略。
    2.  **常规阶段**：由小模型（$M_S$）执行具体的工具调用和推理。
    3.  **自我评估（Progress Check）**：小模型在每一步输出推理结果的同时，生成一个结构化的进展评估（TRUE/FALSE）。
    4.  **动态升级**：若检测到“停滞”（FALSE），控制权临时移交给 $M_L$，由其修复错误或突破难点。
*   **难度感知的累积预算策略**：
    *   引入**升级等级 $l_t$**：记录连续失败的次数。
    *   **动态预算分配**：根据 $l_t$ 递归增加大模型的干预步数（Budget）。公式为 $B_l = f(B_0, l)$，支持线性增长或 Sigmoid 饱和增长。这种设计能防止频繁切换导致的计算缓存（KV Cache）失效，并确保在极难任务中有足够的“大模型火力”支持。

### 3. 实验设计
*   **数据集/场景**：
    *   **BrowseComp_zh**：中文网页浏览与深度研究任务。
    *   **HLE-math**：具有挑战性的数学推理任务。
    *   **WritingBench**：长篇写作任务。
*   **智能体框架**：集成了 DDV2（华为 openPangu 团队）和 WebSailor 两个开源框架。
*   **对比方法**：
    *   **Small-only / Large-only**：单一模型基准。
    *   **Random**：每步随机选择模型。
    *   **RouteLLM / FrugalGPT**：经典的基于学习或评分的单轮路由方法（适配到多步场景）。

### 4. 资源与算力
*   **硬件环境**：实验在 **Ascend 910B3 NPU** 上运行，使用 4 个 Worker。
*   **推理引擎**：使用 vLLM-Ascend (v0.9.1)。
*   **模型规模**：测试了多种组合，包括 7B/38B（DDV2）以及 3B/7B/32B（WebSailor，基于 Qwen2.5 系列）。
*   **训练时长**：文中未提及训练时长，因为该方法**无需额外训练**路由策略，直接利用模型现有的指令遵循能力进行自我评估。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同领域的基准测试，样本量分别为 289、866 和 1000 个问题，具有统计意义。
*   **消融实验**：
    *   对比了“大模型做规划者”与“大模型做执行者”的效果。
    *   对比了静态预算与动态累积预算的差异。
    *   测试了不同模型尺寸配对（如 3B+7B vs 7B+32B）的影响。
*   **客观性**：实验通过帕累托前沿（Pareto Frontier）展示了准确率与速度的平衡，对比了多种基准路由算法，评价较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：AgentCollab 在所有基准测试中均显著优于小模型，且非常接近大模型的准确率（例如在 BrowseComp_zh 上，DDV2 组合达到了大模型 98% 的精度）。
*   **效率优势**：相比全量使用大模型，实现了 **1.3x 到 2.4x** 的端到端加速。
*   **轨迹优化**：实验发现，小模型虽然单步快，但出错后会陷入无效循环导致总步数增加；AgentCollab 通过及时的大模型干预缩短了总推理轨迹。
*   **能力差距论**：协作的效果取决于模型间的“能力差”，大模型必须能显著纠正小模型的错误，协作才有意义。

### 7. 优点与亮点
*   **自驱动无需训练**：不需要额外的路由模型或复杂的强化学习训练，部署成本极低。
*   **轨迹感知**：通过累积失败信号来调整预算，比单步路由更符合智能体长程交互的逻辑。
*   **缓存友好**：动态预算减少了模型间的频繁震荡切换，有利于 KV Cache 的复用，进一步提升了实际 TPS（每秒 Token 数）。

### 8. 不足与局限
*   **自我评估的可靠性**：该方法高度依赖小模型的自我反思能力。如果小模型过于“自信”（产生假阳性进展判断），则无法触发升级，导致任务失败。
*   **模型同源性限制**：实验主要在同系列模型（如 Qwen 系列不同尺寸）间进行，尚未验证跨架构、异构模型协作的效果。
*   **闭源模型缺失**：由于 API 调用的延迟不稳定，实验未包含 GPT-4 等闭源模型，这限制了其在云端服务场景下的参考价值。
*   **任务复杂度阈值**：对于极简单的任务，该框架可能引入不必要的自我评估开销；对于极难任务，小模型的频繁失败可能导致其退化为全量大模型模式。

（完）
