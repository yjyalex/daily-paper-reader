---
title: "GoalVLM: VLM-driven Object Goal Navigation for Multi-Agent System"
title_zh: GoalVLM：面向多智能体系统的 VLM 驱动物体目标导航
authors: "MoniJesu James, Amir Atef Habel, Aleksey Fedoseev, Dzmitry Tsetserokou"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18210v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于导航的协作式多智能体框架
tldr: GoalVLM 是一个面向多智能体系统的零样本、开放词汇目标导航框架。它将视觉语言模型（VLM）集成到决策循环中，利用 SAM3 进行文本提示的目标检测，并通过 SpaceOM 进行空间推理。智能体构建 BEV 语义地图，利用约束引导的推理层评估探索前沿，从而在无需重新训练的情况下实现对自由文本目标的理解与定位。在 GOAT-Bench 上的实验证明了其在复杂序列导航任务中的有效性。
selection_source: fresh_fetch
motivation: 传统的多智能体目标导航受限于闭集词汇和固定类别，难以泛化到测试时的未知目标。
method: 集成 VLM、SAM3 和 SpaceOM，通过 BEV 语义地图构建、深度投影目标定位及结构化提示链推理来实现开放词汇导航。
result: "在 GOAT-Bench 验证集上，双智能体配置实现了 55.8% 的子任务成功率和 18.3% 的 SPL，性能比肩现有最先进方法。"
conclusion: 该研究证明了利用 VLM 的常识先验和零样本推理能力可以有效解决多智能体系统中的开放词汇导航挑战。
---

## 摘要
物体目标导航传统上局限于具有闭集物体词汇表的地面机器人。现有的多智能体方法依赖于与固定类别集绑定的预计算概率图，这阻碍了在测试时泛化到新目标。我们提出了 GoalVLM，这是一个用于零样本、开放词汇物体导航的协作式多智能体框架。GoalVLM 将视觉语言模型（VLM）直接集成到决策循环中，利用 SAM3 进行文本提示的检测与分割，并使用 SpaceOM 进行空间推理，使智能体能够解释自由形式的语言目标，并通过零样本语义先验对边界（frontiers）进行评分，而无需重新训练。每个智能体通过深度投影的体素喷溅（voxel splatting）构建鸟瞰图（BEV）语义地图，同时目标投影器（Goal Projector）通过校准后的深度将检测结果反向投影到地图中，以实现可靠的目标定位。约束引导的推理层通过结构化的提示链（场景描述、房间类型分类、感知门控、多边界排序）评估边界，将常识先验注入探索过程。我们在 GOAT-Bench val_unseen（包含 360 个多子任务回合、1032 个顺序物体目标子任务、HM3D 场景）上评估了 GoalVLM，其中每个回合需要导航到由 5-7 个开放词汇目标组成的链条。具有 N=2 个智能体的 GoalVLM 实现了 55.8% 的子任务成功率（SR）和 18.3% 的成功率加权路径长度（SPL），在无需特定任务训练的情况下，与现有最先进方法相比具有竞争力。消融实验证实了 VLM 引导的边界推理和深度投影目标定位的贡献。

## Abstract
Object-goal navigation has traditionally been limited to ground robots with closed-set object vocabularies. Existing multi-agent approaches depend on precomputed probabilistic graphs tied to fixed category sets, precluding generalization to novel goals at test time.   We present GoalVLM, a cooperative multi-agent framework for zero-shot, open-vocabulary object navigation. GoalVLM integrates a Vision-Language Model (VLM) directly into the decision loop, SAM3 for text-prompted detection and segmentation, and SpaceOM for spatial reasoning, enabling agents to interpret free-form language goals and score frontiers via zero-shot semantic priors without retraining. Each agent builds a BEV semantic map from depth-projected voxel splatting, while a Goal Projector back-projects detections through calibrated depth into the map for reliable goal localization. A constraint-guided reasoning layer evaluates frontiers through a structured prompt chain (scene captioning, room-type classification, perception gating, multi-frontier ranking), injecting commonsense priors into exploration.   We evaluate GoalVLM on GOAT-Bench val_unseen (360 multi-subtask episodes, 1032 sequential object-goal subtasks, HM3D scenes), where each episode requires navigating to a chain of 5-7 open-vocabulary targets. GoalVLM with N=2 agents achieves 55.8% subtask SR and 18.3% SPL, competitive with state-of-the-art methods while requiring no task-specific training. Ablation studies confirm the contributions of VLM-guided frontier reasoning and depth-projected goal localization.

---

## 论文详细总结（自动生成）

### GoalVLM：面向多智能体系统的 VLM 驱动物体目标导航论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的物体目标导航（ObjectNav）通常局限于单机器人系统，且仅能识别闭集（固定类别）的物体。现有的多智能体协作导航往往依赖预先计算的概率图，无法在测试时泛化到用户通过自由文本描述的新目标。
*   **研究背景**：在复杂的室内环境中，如何利用视觉语言模型（VLM）的常识推理能力，实现多智能体之间的高效协作，并完成开放词汇（Open-Vocabulary）的连续导航任务，是当前具身智能领域的重要挑战。

#### 2. 方法论
GoalVLM 提出了一个完全去中心化的多智能体框架，其核心流程如下：
*   **核心思想**：将 VLM 直接集成到决策循环中，结合零样本感知、空间推理和分布式地图共享，实现无需训练的开放词汇导航。
*   **关键技术细节**：
    *   **自我中心语义建图**：利用 RGB-D 观测，通过深度投影和体素喷溅（Voxel Splatting）构建鸟瞰图（BEV）占用网格。特别修正了非均匀缩放下的相机内参模型，以消除投影失真。
    *   **零样本感知流水线**：采用 **SAM3** 进行文本提示的目标检测与分割；通过 **GoalProjector** 将检测到的掩码反向投影至 BEV 地图进行精确定位。
    *   **空间推理层（SpaceOM）**：利用 VLM 的常识（如“微波炉通常在厨房”），通过结构化提示链（场景描述、房间分类、边界排序）对探索前沿（Frontiers）进行评分。
    *   **贝叶斯价值图（Bayesian Value Map）**：维护一个概率价值图，结合 VLM 评分和不确定性（UCB 策略）来引导智能体探索。
    *   **多智能体协作**：智能体通过最大池化（Max-pooling）融合共享全局地图，并采用顺序贪婪协议分配不同的探索前沿，避免重复搜索。
    *   **路径规划**：使用快速行进法（FMM）在语义占用网格上计算测地线距离并生成动作。

#### 3. 实验设计
*   **数据集与场景**：使用 **GOAT-Bench** 的 `val_unseen` 分割，包含基于 **HM3D** 真实扫描场景的 360 个多子任务回合。
*   **Benchmark 任务**：每个回合包含 5-7 个顺序排列的开放词汇目标子任务（共 1032 个子任务），要求智能体在 1.0 米范围内停止。
*   **对比方法**：
    *   强化学习方法：SenseAct-NN。
    *   零样本/模块化方法：CLIP on Wheels, VLMNav, DyNaVLM, 3D-Mem, Modular GOAT。
    *   端到端训练方法：AstraNav-Memory（基于 3B 参数的 Qwen2.5-VL）。

#### 4. 资源与算力
*   **仿真环境**：论文未明确说明仿真实验所使用的具体 GPU 型号、数量及推理总时长。
*   **实机验证**：提到了在物理多无人机平台上进行了初步验证，硬件包括 **Orange Pi 5** 嵌入式计算板、Intel RealSense D435/D435i 深度相机，以及 Vicon 运动捕捉系统。
*   **训练需求**：由于该方法是**零样本（Zero-shot）**且无需特定任务训练，因此不存在传统意义上的训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 360 个回合、1032 个子任务上进行了大规模测试，样本量充足。
*   **消融实验**：
    *   验证了 **VLM 推理层**的有效性（移除后 SR 下降）。
    *   验证了**多智能体协作**的增益（N=2 比 N=1 的成功率提升了约 1.5-1.8 倍）。
    *   验证了相机内参修正对建图准确性的影响。
*   **客观性**：实验涵盖了不同类别的物体（大件、小件、透明/反射物体）并进行了失败案例分析，评估较为全面、客观。

#### 6. 主要结论与发现
*   **性能表现**：在 N=2 配置下，GoalVLM 实现了 **55.8% 的子任务成功率（SR）**，远超传统的模块化基准（29.4%），且在无需训练的情况下接近端到端微调模型（62.7%）。
*   **协作优势**：多智能体系统显著提升了探索覆盖范围和目标寻找效率。
*   **瓶颈识别**：发现透明/反射物体（如镜子、玻璃）和微小物体（如照片、书本）是当前感知模型的主要失败点。
*   **稳定性**：在连续的子任务序列中，成功率保持相对稳定，未出现明显的性能级联崩溃。

#### 7. 优点
*   **零样本泛化**：无需任何环境特定的训练或微调，具备极强的即插即用能力。
*   **开放词汇支持**：能够处理复杂的自然语言指令和罕见物体类别。
*   **高效协作**：通过简单的地图共享和前沿分配协议实现了良好的多机协同效果。
*   **实机潜力**：在物理硬件上验证了感知流水线的迁移能力。

#### 8. 不足与局限
*   **路径效率（SPL）较低**：相比于端到端训练的模型，基于前沿探索的模块化方法路径较长（SPL 仅 18.3%），存在“绕路”现象。
*   **感知局限性**：高度依赖 SAM3 等视觉模型的质量，对镜面反射和遮挡严重的物体识别较差。
*   **地图维度**：目前采用 2D BEV 地图，无法处理多楼层环境或垂直堆叠的物体。
*   **实时性挑战**：虽然未详细说明，但调用大型 VLM 进行前沿评分可能会带来一定的决策延迟。

（完）
