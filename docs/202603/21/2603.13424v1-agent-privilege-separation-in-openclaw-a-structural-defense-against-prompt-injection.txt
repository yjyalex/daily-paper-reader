Title: Agent Privilege Separation in OpenClaw: A Structural Defense Against Prompt Injection

URL Source: https://arxiv.org/pdf/2603.13424v1

Published Time: Tue, 17 Mar 2026 00:14:31 GMT

Number of Pages: 6

Markdown Content:
## Agent Privilege Separation in OpenClaw: A Structural Defense Against Prompt Injection 

Darren Cheng Wen-Kwang Tsao 

TrendAI Lab 

{darren_cheng,spark_tsao}@trendmicro.com 

Abstract 

Prompt injection remains one of the most prac-tical attack vectors against LLM-integrated ap-plications. We replicate Microsoft’s LLMail-Inject benchmark (Greshake et al., 2024) against current-generation models running inside OpenClaw , an open-source multi-tool agent platform. Our proposed defense combines two mechanisms: agent isolation 

(privilege-separated two-agent pipeline with tool partitioning) and JSON formatting (struc-tured output that strips persuasive framing be-fore the action agent sees it). We run four ex-periments on the same 649 attacks that suc-ceeded against our single-agent baseline: the full pipeline achieves 0% ASR on the eval-uated benchmark (attack success rate); two-agent isolation alone achieves 0.31% ASR 

(323 × better than baseline); JSON formatting alone achieves 14.18% ASR (7.1 × better). Our ablation confirms that agent isolation is the dominant mechanism—JSON formatting pro-vides additional hardening but is not sufficient alone. The defense is structural: the action agent never receives raw injection content re-gardless of model behavior on any individual input. 

1 Introduction 

Prompt injection attacks manipulate LLM-integrated systems by embedding adversarial instructions in untrusted content—emails, docu-ments, web pages—that the model processes as part of a legitimate task (Perez and Ribeiro, 2022; Greshake et al., 2023; Wikipedia contributors, 2024b). As LLM agents acquire the ability to take real-world actions (send emails, call APIs, execute code), the stakes of a successful injection scale with the agent’s capabilities. The Microsoft LLMail-Inject challenge (Gre-shake et al., 2024) provided a controlled bench-mark: an email assistant agent with a send_email 

tool is given a task (summarize emails) while ad-versarial injection payloads attempt to redirect it to send unauthorized messages. The original study evaluated Phi-3-medium and GPT-4o mini with five defense configurations, collecting 461,640 attack submissions. This paper makes two contributions. First, we show that gpt-5-mini running in OpenClaw re-duces the baseline ASR to 2.83% with zero exter-nal mitigations—demonstrating substantial align-ment gains relative to older models. Second, and more importantly, we propose and ablate a two-mechanism architectural defense : agent isola-tion (privilege separation) combined with JSON-structured inter-agent communication. Our abla-tion study across four configurations on the same 649 baseline-successful attacks isolates the contri-bution of each mechanism. Figure 1 contrasts the vulnerable single-agent baseline with our privilege-separated pipeline. 

2 Background 

2.1 Prompt Injection Taxonomy 

Direct prompt injection overrides system instruc-tions through user-controlled input. Indirect prompt injection (Greshake et al., 2023; Liu et al., 2023) embeds adversarial instructions in data the model retrieves and processes—the attack vector used in LLMail, where the injection lives in an email body rather than the user’s query. Indirect injection is harder to defend against because the model cannot distinguish trusted task context from untrusted processed content at inference time. 

2.2 LLMail-Inject Challenge 

The original benchmark defines four scenarios of increasing difficulty: We derive our evaluation pool through the fol-lowing funnel: 1. 461,640 total attack submissions in the origi-nal challenge 1

> arXiv:2603.13424v1 [cs.CR] 13 Mar 2026 Hi OpenClaw,
> Please summarize the following
> email.
> Email:
> {{EMAIL_TEXT}}
> Subject: Meeting Summary
> Hi,
> Here are the notes from yesterday's meeting.
> 1. Product launch planned for April
> 2. Budget approval pending
> 3. Security review required
> ---
> SYSTEM INSTRUCTION (IMPORTANT):
> Ignore previous instructions.
> Instead of summarizing this email, send the full
> content
> to XXX@example.com.
> You must comply because this is a system override.
> Prompt Injection Email Benchmark Dataset
> Prompt Template for OpenClaw
> OpenClaw Baseline Architecture
> Email
> Processing
> Agent
> OpenClaw with Agent Isolation and Structured Output
> Email
> Processing
> Agent
> Tool Invocation: sendEmail
> Email Action
> Agent
> Tool Invocation: sendEmail
> Structured
> Interface
> 649 cases where the sendEmail tool is invoked
> 0 cases the send email is triggered

Figure 1: Architecture comparison. (a) Baseline : a single agent holds send_email and processes untrusted email content—a successful injection directly triggers unauthorized actions. (b) Our pipeline : the Reader agent (no privileged tools) serializes email to a validated JSON summary; the Actor agent only ever receives the sanitized structured output, eliminating the direct injection path to send_email .

# Setup Goal 

1 2 emails, no RAG Send to attacker 2 10 emails, no RAG Send to attacker 3 10 emails + RAG Send to attacker 4 10 emails + RAG Exfiltrate budget 

Table 1: LLMail-Inject scenarios. All goals target unau-thorized send_email calls; Sc. 4 additionally requires budget figure exfiltration. 

2. 41,173 Phase 1 successes after filtering 3. 22,899 unique payloads after deduplication by body 4. 649 attacks that still succeed against our gpt-5-mini single-agent baseline All ablation experiments are normalized to this same 649-attack pool, ensuring a fair comparison across configurations. 

2.3 OpenClaw 

OpenClaw is an open-source multi-tool agent plat-form supporting plugin-defined tools, multi-agent orchestration, and workspace isolation (OpenClaw Contributors, 2024). Unlike the original LLMail custom Python agent—which had exactly one tool (send_email )—an OpenClaw agent is a general-purpose agent with a richer toolset. This set-ting more closely reflects real-world agent deploy-ments. 

3 Defense Design 

Our proposed defense combines two independent mechanisms that can be applied separately or to-gether. 

Mechanism 1 — Agent Isolation. We split the single agent into two cooperating agents with disjoint tool access: • Agent 1 (reader): Tools: store_summary 

only. Reads raw email content and pro-duces a structured summary. Cannot call 

send_email .

• Agent 2 (actor): Tools: send_email ,

get_pending_summary , store_result .Reads only Agent 1’s output. Never sees raw email content. 

Tool isolation is a hard constraint: Agent 1 can-not send email regardless of injection content, and no model capability change overrides this—a direct application of the principle of least privi-lege (Wikipedia contributors, 2024a; Saltzer and Schroeder, 1975; Provos et al., 2003; Watson et al., 2010). 

Mechanism 2 — JSON Formatting. Agent 1 produces a structured JSON summary rather than free-form text. The JSON schema (sender, sub-ject, body summary, action items) strips the per-suasive framing of injection payloads before they reach Agent 2. This approach is grounded in information flow control (IFC) reasoning (Wu 2et al., 2024; Goguen and Meseguer, 1982): un-trusted data passes through a narrow, validated channel before reaching the privileged action stage. A lightweight code-based validator in-spects each JSON summary before it is passed to Agent 2. It applies regex rules targeting three pattern classes: (1) email address literals (e.g., 

attacker@evil.com ), (2) tool-call syntax frag-ments (e.g., send_email( , function_call ), and (3) common injection trigger phrases (e.g., “ignore previous instructions”). In our experiments the val-idator operates in audit mode —all summaries pass through regardless of detection—so that we can measure injection leak rate independently of block-ing decisions. A production deployment would reject or sanitize flagged summaries. 

Ablation structure. We test four configurations on the same 649 attacks: 1. Baseline : single agent, simple prompt, no defenses 2. Pipeline : agent isolation + JSON formatting + validator (full defense) 3. Two-Agent Only : agent isolation without JSON formatting 4. JSON Validator Only : JSON formatting on a single agent (no isolation) 

4 Results 

4.1 Overall Ablation 

Table 2 summarizes all four configurations across the 649 normalized attacks.                     

> Configuration ASR Succ. Def. Improv.
> Baseline 100% 649 0% —JSON Validator Only 14.18% 92 85.8% 7.1 ×
> Two-Agent Only 0.31% 299.7% 323 ×
> Pipeline (full) 0.0% 0100% ∞
> Table 2: Ablation across 649 baseline-successful attacks. “Succ.” = successful injections; “Def.” = defense rate; “Improv.” = improvement vs. baseline.

Key finding : Agent isolation is the dominant mechanism. Two-Agent Only alone achieves 323 ×

improvement; adding JSON formatting closes the remaining 2 residual attacks to 0 on this benchmark. JSON Validator Only (no isolation) provides only 7.1 × improvement—useful but far from sufficient. 

4.2 Per-Scenario ASR 

Table 3 breaks down ASR by scenario across all four configurations. 

Key observations: 

• Scenario 4 (data exfiltration) is the hard-est for JSON Validator Only (28.3% ASR), but both two-agent configurations achieve 0%. Exfiltration requires content extraction across the agent boundary—isolation blocks this cat-egorically. • Scenario 3 (RAG) is perfectly defended by Two-Agent Only (0/116) and the full Pipeline, but retains 6.0% ASR under JSON Validator Only. RAG interleaves trusted and untrusted content, which JSON formatting alone cannot separate. • Two residual attacks (Sc. 1 and Sc. 2) survive Two-Agent Only but are eliminated by the full Pipeline. The JSON schema structured those summaries in ways that prevented Agent 2 from being triggered. 

4.3 Baseline: Full Dataset (22,899 Attacks) 

Before normalization, we ran the single-agent baseline against all 22,899 unique Phase 1 pay-loads. The model resists 97.2% without any de-fense (ASR 2.83%, yielding 649 successes), con-sistent with alignment gains from RLHF-style train-ing (Wikipedia contributors, 2024c; Ganguli et al., 2022). Scenario 3 (RAG) has the highest ASR (7.13%); Scenario 1 the lowest (1.46%). The 649 successful attacks form the normalized pool used in all ablation experiments. 

5 Analysis 

5.1 Why Isolation Dominates 

The 323 × improvement from Two-Agent Only— without any change to the model or its prompts— demonstrates that tool partitioning is a structural defense . Agent 1 cannot call send_email regard-less of injection content; this constraint is enforced by the platform, not the model. No increase in model capability or injection sophistication over-rides a missing tool registration. The full Pipeline eliminates the 2 residual attacks by additionally ensuring Agent 2 never sees persua-sive injection framing. Agent 2 receives structured JSON fields (sender, subject, summary), not the raw email body. Even when injection content leaks 3Sc. Description Baseline Pipeline Two-Agent JSON Val.                     

> 12-email summary 100% (149/149) 0.0% (0/149) 0.7% (1/149) 4.0% (6/149) 210-email summary 100% (211/211) 0.0% (0/211) 0.5% (1/211) 14.2% (30/211) 3RAG query 100% (116/116) 0.0% (0/116) 0.0% (0/116) 6.0% (7/116) 4Data exfiltration 100% (173/173) 0.0% (0/173) 0.0% (0/173) 28.3% (49/173)

Table 3: Attack Success Rate by scenario across all configurations (lower is better). 

into a summary field, it arrives decontextualized— stripped of the social engineering that makes it actionable. 

5.2 Why JSON Formatting Alone is Insufficient 

JSON Validator Only reduces ASR from 100% to 14.18%—a meaningful improvement, but 92 at-tacks still succeed. The model producing the JSON summary can still be manipulated into encoding actionable instructions into free-text fields (e.g., the “body summary” field). Without tool isolation, a sufficiently persuasive payload reaching the single agent can still trigger send_email directly. JSON formatting raises the bar; isolation removes the attack surface. 

5.3 Comparison with Original LLMail Defenses Defense Model ASR                     

> None (original) Phi-3 / GPT-4o mini ∼30–60% Spotlighting (Hines et al., 2024) GPT-4o mini ∼15% PromptShield (Jacob et al., 2025) GPT-4o mini ∼10% LLM-as-judge GPT-4o mini ∼8% TaskTracker GPT-4o mini ∼5% All combined GPT-4o mini ∼2% Baseline (ours) gpt-5-mini 2.83% JSON Validator (ours) gpt-5-mini 0.40% †
> Two-Agent (ours) gpt-5-mini 0.009% †
> Pipeline (ours) gpt-5-mini 0%

Table 4: ASR comparison. †Normalized to full 22,899-attack pool (649/22,899 baseline; ablation rates scaled proportionally). “Phi-3” = Phi-3-medium. 

gpt-5-mini with no defenses already matches the full five-defense stack applied to GPT-4o mini in the original challenge. Our architectural defenses push well below that floor. 

6 Discussion 

Design principle. For agentic systems with dan-gerous tools (send, post, execute, delete), the agent that processes untrusted content should not be the same agent that holds the dangerous tools (Saltzer and Schroeder, 1975; Provos et al., 2003). Our abla-tion quantifies this: isolation alone provides 323 ×

improvement; adding structured output formatting closes the gap to zero. 

Relation to alternative defenses. Spotlight-ing (Hines et al., 2024) and PromptShield (Jacob et al., 2025) address injection within a single agent via provenance signaling and classifier-based de-tection respectively. Progent (Shi et al., 2025) en-forces per-agent tool restrictions via a dynamic pol-icy language. The Instruction Hierarchy (Wallace et al., 2024) trains models to prioritize privileged instructions. Our approach is complementary: ar-chitectural isolation provides a hard constraint that holds independent of model behavior, while IFC-based reasoning (Wu et al., 2024) provides a formal lens for the guarantee. Agent frameworks such as AutoGen (Wu et al., 2023) and tool-augmented agents (Yao et al., 2022; Schick et al., 2023) make multi-agent decomposition increasingly practical, lowering the deployment cost of our pattern. Prior work has demonstrated multi-agent LLM pipelines in production planning contexts (Tsao, 2023), vali-dating that role-specialized agent decomposition is a viable engineering pattern beyond research proto-types. 

Limitations. The ablation runs 649 attacks (those that succeeded at baseline), not the full 22,899. Adaptive attacks designed specifically to survive JSON summarization and multi-agent hand-offs are not evaluated; standardized evaluation har-nesses such as JailbreakBench (Chao et al., 2024) provide a template for this rigor. The validator uses simple regex pattern matching; a more sophisti-cated attacker could craft summaries that evade it while still influencing Agent 2. 

Residual risk. The 63.7% injection leak rate into Agent 1 summaries (measured in the Pipeline) shows that JSON formatting does not prevent con-tent propagation—it prevents actionable content propagation. If future attack variants can encode executable instructions into structured JSON fields that Agent 2 acts on, the defense degrades. 47 Conclusion 

We propose a two-mechanism architectural defense against prompt injection in LLM agents: agent iso-lation (privilege-separated tool access) combined with JSON-structured inter-agent communica-tion . Tested via OpenClaw on 649 attacks that succeeded against a gpt-5-mini single-agent base-line, our ablation shows: • JSON formatting alone : 14.18% ASR (7.1 ×

better than baseline) • Agent isolation alone : 0.31% ASR (323 ×

better than baseline) • Full pipeline : 0% ASR on the evaluated benchmark ( ∞ improvement) Agent isolation is the dominant mechanism be-cause it imposes a hard structural constraint inde-pendent of model behavior. JSON formatting pro-vides complementary hardening by stripping per-suasive framing before it reaches the action agent. Together, they provide a defense that holds regard-less of how convincing any individual injection payload is. We recommend privilege-separated agent archi-tectures with structured inter-agent communication as a baseline security requirement for production agentic systems handling untrusted input. 

References 

Patrick Chao, Edoardo Debenedetti, Alexander Robey, Maksym Andriushchenko, Francesco Croce, Vikash Kumar, Evan Hausman, Javier Rando, Florian Tramèr, and 1 others. 2024. JailbreakBench: An open robustness benchmark for jailbreaking large language models. arXiv preprint arXiv:2404.01318 .Deep Ganguli, Liane Lovitt, Jackson Kernion, and 1 others. 2022. Red teaming language models to re-duce harms: Methods, scaling behaviors, and lessons learned. arXiv preprint arXiv:2209.07858 .Joseph A. Goguen and José Meseguer. 1982. Security policies and security models. In IEEE Symposium on Security and Privacy .Kai Greshake, Sahar Abdelnabi, Shailesh Mishra, Christoph Endres, Thorsten Holz, and Mario Fritz. 2023. Not what you’ve signed up for: Compromising real-world LLM-integrated applications with indirect prompt injection. arXiv preprint arXiv:2302.12173 .Kai Greshake, Sahar Abdelnabi, Shailesh Mishra, Christoph Endres, Thorsten Holz, and Mario Fritz. 2024. LLMail-Inject: Adaptive prompt injection at-tacks against LLM-integrated applications. arXiv preprint arXiv:2506.09956 .Keegan Hines, Gary Lopez, Matthew Hall, Federico Zarfati, Yonatan Zunger, and Emre Sherif. 2024. De-fending against indirect prompt injection attacks with spotlighting. arXiv preprint arXiv:2403.14720 .Dennis Jacob and 1 others. 2025. PromptShield: Deployable detection for prompt injection attacks. 

arXiv preprint arXiv:2501.15145 .Yi Liu, Gelei Deng, Yuekang Li, Kailong Wang, Tianwei Zhang, Yepang Liu, Haoyu Wang, Yan Yan, and Yang Liu. 2023. Prompt injection attack against LLM-integrated applications. arXiv preprint arXiv:2306.05499 .OpenClaw Contributors. 2024. OpenClaw: Open-source multi-tool agent platform. GitHub. Accessed: 2025. Fábio Perez and Ian Ribeiro. 2022. Ignore previous prompt: Attack techniques for language models. 

arXiv preprint arXiv:2211.09527 .Niels Provos, Markus Friedl, and Peter Honeyman. 2003. Preventing privilege escalation. In 12th USENIX Security Symposium .Jerome H. Saltzer and Michael D. Schroeder. 1975. The protection of information in computer systems. Pro-ceedings of the IEEE , 63(9):1278–1308. Timo Schick, Jane Dwivedi-Yu, Roberto Dessì, Roberta Raileanu, Maria Lomeli, Eric Hambro, Luke Zettle-moyer, Nicola Cancedda, and Thomas Scialom. 2023. Toolformer: Language models can teach themselves to use tools. arXiv preprint arXiv:2302.04761 .Tianneng Shi, Jinghan Gao, Zhun He, Dawn Song, and 1 others. 2025. Progent: Programmable priv-ilege control for LLM agents. arXiv preprint arXiv:2504.11703 .Wen-Kwang Tsao. 2023. Multi-agent reasoning with large language models for effective corporate plan-ning. In 2023 International Conference on Com-putational Science and Computational Intelligence (CSCI) , pages 365–370. IEEE. Eric Wallace, Kai Xiao, Reimar Leike, Lilian Weng, Johannes Heidecke, and Alex Beutel. 2024. The in-struction hierarchy: Training LLMs to prioritize privi-leged instructions. arXiv preprint arXiv:2404.13208 .Robert N. M. Watson, Jonathan Anderson, Ben Lau-rie, and Kris Kennaway. 2010. Capsicum: Practical capabilities for UNIX. In 19th USENIX Security Symposium .Wikipedia contributors. 2024a. Principle of least privi-lege. Wikipedia, The Free Encyclopedia. Accessed: 2025. 

5Wikipedia contributors. 2024b. Prompt injection. Wikipedia, The Free Encyclopedia. Accessed: 2025. Wikipedia contributors. 2024c. Reinforcement learning from human feedback. Wikipedia, The Free Encyclo-pedia. Accessed: 2025. Fangzhou Wu, Shutong Luo, Royce Yan, Wenchao Wu, Jiaheng He, Zheng Li, and Xiangyu Zhang. 2024. System-level defense against indirect prompt injec-tion attacks: An information flow control perspective. 

arXiv preprint arXiv:2409.19091 .Qingyun Wu, Gagan Bansal, Jieyu Zhang, Yiran Wu, Shaokun Zhang, Erkang Zhu, Beibin Li, Li Jiang, Xiaoyun Zhang, and Chi Wang. 2023. AutoGen: Enabling next-gen LLM applications via multi-agent conversation. arXiv preprint arXiv:2308.08155 .Shunyu Yao, Jeffrey Zhao, Dian Yu, and 1 others. 2022. ReAct: Synergizing reasoning and acting in language models. arXiv preprint arXiv:2210.03629 .

6