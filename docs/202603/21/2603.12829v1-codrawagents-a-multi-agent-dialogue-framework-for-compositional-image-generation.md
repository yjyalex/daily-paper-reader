---
title: "coDrawAgents: A Multi-Agent Dialogue Framework for Compositional Image Generation"
title_zh: coDrawAgents：一种用于组合式图像生成的协作多智能体对话框架
authors: "Chunhan Li, Qifeng Wu, Jia-Hui Pan, Ka-Hei Hui, Jingyu Hu, Yuming Jiang, Bin Sheng, Xihui Liu, Wenjuan Gong, Zhengzhe Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12829v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 用于组合图像生成的多智能体对话框架
tldr: 针对现有文本生成图像模型在处理复杂场景时难以准确组合多物体及其属性的问题，本文提出了 coDrawAgents 多智能体对话框架。该框架包含解释器、规划器、检查器和绘制器四个角色，通过分而治之的策略、基于视觉上下文的增量规划以及显式的错误纠正机制，显著提升了复杂提示词下的图像生成质量。实验证明其在空间准确性和属性绑定方面优于现有方法。
selection_source: fresh_fetch
motivation: 现有的文本生成图像模型在处理包含多个物体及其复杂属性的场景时，往往难以实现忠实的组合生成和属性保持。
method: 提出由解释器、规划器、检查器和绘制器组成的多智能体框架，通过自适应路径选择、增量式布局规划和显式错误纠正协同工作。
result: 在 GenEval 和 DPG-Bench 基准测试上的实验表明，该方法在文本-图像对齐、空间准确性和属性绑定方面均有显著提升。
conclusion: coDrawAgents 通过多智能体协作有效解决了布局复杂性、视觉上下文关联和错误修正等关键挑战，增强了组合图像生成的性能。
---

## 摘要
文本到图像生成技术发展迅速，但现有模型在复杂场景中忠实地组合多个对象并保留其属性方面仍面临挑战。我们提出了 coDrawAgents，这是一个交互式多智能体对话框架，包含四个专门的智能体：解释器（Interpreter）、规划器（Planner）、检查器（Checker）和绘制器（Painter），它们通过协作来改进组合式生成。解释器根据情况自适应地在直接文本到图像路径和布局感知多智能体流程之间做出选择。在布局感知模式下，它将提示词解析为具有丰富属性的对象描述符，按语义显著性对其进行排序，并将具有相同语义优先级水平的对象分组以进行联合生成。在解释器的指导下，规划器采用分而治之的策略，为具有相同语义优先级水平的对象增量地提出布局，同时将决策建立在画布不断演变的视觉上下文基础之上。检查器通过验证空间一致性和属性对齐，并在渲染前细化布局，引入了显式的纠错机制。最后，绘制器逐步合成图像，将新规划的对象整合到画布中，为后续迭代提供更丰富的上下文。这些智能体共同解决了三个关键挑战：降低布局复杂度、将规划建立在视觉上下文中以及实现显式纠错。在基准测试 GenEval 和 DPG-Bench 上的广泛实验表明，与现有方法相比，coDrawAgents 显著提高了文本-图像对齐、空间准确性和属性绑定。

## Abstract
Text-to-image generation has advanced rapidly, but existing models still struggle with faithfully composing multiple objects and preserving their attributes in complex scenes. We propose coDrawAgents, an interactive multi-agent dialogue framework with four specialized agents: Interpreter, Planner, Checker, and Painter that collaborate to improve compositional generation. The Interpreter adaptively decides between a direct text-to-image pathway and a layout-aware multi-agent process. In the layout-aware mode, it parses the prompt into attribute-rich object descriptors, ranks them by semantic salience, and groups objects with the same semantic priority level for joint generation. Guided by the Interpreter, the Planner adopts a divide-and-conquer strategy, incrementally proposing layouts for objects with the same semantic priority level while grounding decisions in the evolving visual context of the canvas. The Checker introduces an explicit error-correction mechanism by validating spatial consistency and attribute alignment, and refining layouts before they are rendered. Finally, the Painter synthesizes the image step by step, incorporating newly planned objects into the canvas to provide richer context for subsequent iterations. Together, these agents address three key challenges: reducing layout complexity, grounding planning in visual context, and enabling explicit error correction. Extensive experiments on benchmarks GenEval and DPG-Bench demonstrate that coDrawAgents substantially improves text-image alignment, spatial accuracy, and attribute binding compared to existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《coDrawAgents: A Multi-Agent Dialogue Framework for Compositional Image Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的文本生成图像（T2I）模型在处理复杂场景时，难以忠实地组合多个物体、准确处理空间关系以及保持物体属性（即“组合性生成”难题）。
*   **研究背景**：
    *   单智能体方法（如直接使用 LLM 规划布局）往往难以处理物体间的复杂依赖关系，且容易在早期步骤出错。
    *   现有的多智能体框架多为固定流水线，缺乏智能体间的动态协商、视觉反馈和显式的纠错机制。
    *   扩散模型在生成初期就确定了全局结构，后期难以修正位置或属性错误。

### 2. 方法论：核心思想与技术细节
coDrawAgents 提出了一个由四个专门智能体组成的交互式对话框架，核心思想是**“分而治之”**与**“视觉反馈闭环”**。
*   **Interpreter（解释器）**：
    *   **自适应路径选择**：判断提示词复杂度，决定是直接调用 T2I 模型（简单场景），还是启动布局感知模式（复杂场景）。
    *   **语义解析与排序**：将提示词分解为富含属性的对象描述符，按语义显著性排序并分组，确保高优先级物体先被处理。
*   **Planner（规划器）**：
    *   **增量式布局推理**：采用可视化思维链（VCoT），每次仅为同一优先级的物体规划布局。
    *   **视觉上下文感知**：规划时不仅参考文本，还参考 Painter 已生成的中间画布（Canvas），确保新物体与已有环境协调。
*   **Checker（检查器）**：
    *   **两阶段纠错**：第一阶段检查当前物体的尺寸、比例和边界；第二阶段审查全局冲突（如重叠、遮挡顺序、比例漂移），并在渲染前修正布局。
*   **Painter（绘制器）**：
    *   **逐步合成**：采用即插即用的架构（如 FLUX 或 3DIS），根据 Checker 确认的布局逐步渲染图像，为下一轮迭代提供视觉参考。

### 3. 实验设计
*   **数据集/Benchmark**：
    *   **GenEval**：评估物体存在感、属性绑定、计数和空间关系的标准基准。
    *   **DPG-Bench**：侧重于长文本、多物体及复杂属性描述的评估。
*   **对比方法**：
    *   **基础 T2I 模型**：SDXL, DALL-E 3, FLUX.1-dev, SD3-Medium。
    *   **多模态大模型**：Janus-Pro, GPT Image 1 [High], OmniGen2。
    *   **布局感知/智能体方法**：GoT (Generation Chain-of-Thought), T2I-Copilot。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **技术特性**：由于该框架被设计为“训练无关（Training-free）”和“即插即用（Plug-and-play）”，它主要依赖于现有的预训练模型（如 GPT-4o/GPT-5 作为 LLM 驱动，FLUX 作为生成底座），因此其核心开销在于推理时的 API 调用或模型推理次数，而非大规模训练算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 GenEval 和 DPG-Bench（包含 1074 张图像）上进行了全面的定量评估。
    *   进行了详细的**消融实验**，分别验证了布局感知模式、视觉上下文反馈和检查器（Checker）对最终性能的贡献。
    *   提供了丰富的定性对比图，展示了在处理复杂指令时的优越性。
*   **充分性与公平性**：实验覆盖了当前最先进的开源和闭源模型，对比维度全面（计数、颜色、位置等）。通过平均智能体调用次数（如 Planner 平均 1.52 次/图）证明了其在提升质量的同时兼顾了效率。

### 6. 主要结论与发现
*   **性能领先**：coDrawAgents 在 GenEval 上的总分达到 0.94，显著超过了 GPT Image 1 [High] (0.84) 和 FLUX (0.67)。
*   **空间与属性优势**：在“位置（Position）”和“属性绑定（Attribute Binding）”子项上提升尤为明显，证明了多智能体协作在解决空间冲突和属性混淆方面的有效性。
*   **闭环反馈的重要性**：消融实验显示，引入视觉上下文和显式检查机制能大幅降低布局错误率。

### 7. 优点（亮点）
*   **交互式闭环**：不同于传统的单向流水线，该框架允许规划器“看到”已生成的图像，实现了真正的图文互动推理。
*   **降低复杂度**：通过 Interpreter 的分组策略，将复杂的全局规划简化为多个局部的增量规划。
*   **即插即用**：不改变底层扩散模型参数，具有极强的通用性和可扩展性。

### 8. 不足与局限
*   **计算开销**：由于需要多次调用 LLM 和图像生成模型，推理延迟和成本高于单次生成的模型。
*   **依赖性风险**：性能高度依赖于所选的底层 Painter 模型和 LLM 的推理能力，若基础模型存在偏见或幻觉，框架难以完全消除。
*   **错误累积**：尽管有 Checker 机制，但在极多物体的极端场景下，早期步骤的微小偏差仍可能对后续生成产生连锁影响。
*   **维度限制**：目前仅专注于 2D 图像生成，尚未扩展到 3D 场景。

（完）
