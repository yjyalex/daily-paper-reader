---
title: "Balanced Thinking: Improving Chain of Thought Training in Vision Language Models"
title_zh: 平衡思考：改进视觉语言模型中的思维链训练
authors: "Shaked Perek, Ben Wiesel, Avihu Dekel, Nimrod Shabtay, Eli Schwartz"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18656v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过强化学习和有监督微调改进思维链训练
tldr: 本研究针对视觉语言模型在思维链（CoT）训练中存在的推理与答案Token不平衡问题，提出了SCALe方法。该方法通过动态的余弦调度策略，在SFT阶段显式分离并调整推理与答案段的损失权重，使模型从关注推理逻辑逐渐转向确保答案准确。实验表明，SCALe仅需极短的训练时间即可达到传统SFT+RL流程的性能，显著提升了多模态推理的效率与精度。
selection_source: fresh_fetch
motivation: 标准SFT训练中长篇幅的推理过程会掩盖短小但关键的答案部分，导致模型推理冗长且答案准确性不足。
method: 提出SCALe方法，利用余弦调度策略动态调整推理与答案段的损失权重，实现长度无关的平衡监督。
result: SCALe在仅需SFT+GRPO流程约七分之一训练时间的情况下，达到了与之相当的性能，并能进一步增强强化学习的效果。
conclusion: SCALe是一种轻量且高效的训练方案，为视觉语言模型的平衡推理训练提供了有力支持。
---

## 摘要
视觉语言模型（VLM）中的多模态推理通常依赖于两阶段过程：有监督微调（SFT）和强化学习（RL）。在标准 SFT 中，所有 token 对损失函数的贡献是均等的，尽管推理数据在 token 分布上本质上是不平衡的。冗长的 <think> 轨迹掩盖了短小但对任务至关重要的 <answer> 片段，导致推理过程冗长且答案不准确。我们提出了 SCALe（Scheduled Curriculum Adaptive Loss，调度课程自适应损失），它利用动态且与长度无关的加权方式，显式地分离了对推理片段和答案片段的监督。与过度关注 <think> 片段的原始 SFT 不同，SCALe-SFT 通过余弦调度策略在整个训练过程中逐渐将重点从 <think> 转移到 <answer>，从而鼓励简洁且有据可依的推理。我们在多种基准测试和架构上对 SCALe 进行了评估。结果表明，SCALe 的准确率始终优于原始 SFT，并且在仅需约七分之一训练时间的情况下，达到了完整的 SFT + GRPO 两阶段流程的性能，使其成为一种轻量且有效的替代方案。当与 GRPO 结合时，SCALe 实现了最佳的整体性能，凸显了其作为独立方法以及作为强化精炼坚实基础的价值。

## Abstract
Multimodal reasoning in vision-language models (VLMs) typically relies on a two-stage process: supervised fine-tuning (SFT) and reinforcement learning (RL). In standard SFT, all tokens contribute equally to the loss, even though reasoning data are inherently token-imbalanced. Long <think> traces overshadow short but task-critical <answer> segments, leading to verbose reasoning and inaccurate answers. We propose SCALe (Scheduled Curriculum Adaptive Loss), which explicitly separates supervision over reasoning and answer segments using dynamic, length-independent weighting. Unlike vanilla SFT, which overweights the <think> segment, SCALe-SFT gradually shifts the focus from <think> to <answer> throughout training via a cosine scheduling policy, encouraging concise and well-grounded reasoning. We evaluate SCALe across diverse benchmarks and architectures. Results show that SCALe consistently improves accuracy over vanilla SFT and matches the performance of the full two-phase SFT + GRPO pipeline while requiring only about one-seventh of the training time, making it a lightweight yet effective alternative. When combined with GRPO, SCALe achieves the best overall performance, highlighting its value both as a standalone method and as a strong foundation for reinforcement refinement.

---

## 论文详细总结（自动生成）

这篇论文由 IBM Research 的团队提出，旨在解决视觉语言模型（VLM）在思维链（CoT）训练中存在的 Token 分布不平衡问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在标准的有监督微调（SFT）中，所有 Token 对损失函数的贡献是均等的。然而，推理数据本质上是“Token 不平衡”的：推理过程（`<think>` 部分）通常非常冗长，而最终答案（`<answer>` 部分）非常简短。
*   **研究动机**：由于推理段长度远超答案段（有时达 150 倍），标准 SFT 的损失函数会被推理风格所主导，导致模型虽然能生成流畅的推理，但事实准确性差，甚至出现“在思考中迷失”或无法正确结束推理的情况。

### 2. 方法论（SCALe-SFT）
*   **核心思想**：提出 **SCALe（Scheduled Curriculum Adaptive Loss）**，通过动态调整推理段和答案段的损失权重，实现“长度无关”的平衡监督。
*   **关键技术细节**：
    *   **片段分离与长度无关损失**：将输出序列分为推理段和答案段，分别计算平均交叉熵损失。这样，无论推理过程多长，它对总损失的贡献都不会自动淹没简短的答案部分。
    *   **动态权重调度（Cosine Scheduling）**：引入余弦退火策略。在训练初期，赋予推理段较高权重以建立结构化思维；随着训练进行，逐渐降低推理权重（从 1.0 降至 0.5），同时保持答案段权重（1.0），使模型重心向答案准确性转移。
    *   **标签强化**：将推理结束标签 `</think>` 归入答案段计算，强制模型学习如何有序地结束思考并转入回答。

### 3. 实验设计
*   **数据集**：使用 **Vision-R1** 推理语料库（约 20 万样本），并在 **ScienceQA**（科学推理）和 **IconQA**（图表理解）两个基准测试上进行评估。
*   **对比模型**：涵盖了三种不同架构和规模的模型：Qwen2.5-VL-3B（已有推理预训练）、LLaVA-v1.6-Mistral-7B 和 Gemma-3-4B（非推理预训练）。
*   **对比方法**：
    1.  Base（原始模型）
    2.  Vanilla SFT（标准 SFT）
    3.  SCALe-SFT（本文方法）
    4.  SFT + GRPO（标准两阶段流程）
    5.  SCALe-SFT + GRPO（本文方法作为强化学习的起点）

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：采用 DeepSpeed-ZeRO2 优化器，使用 LoRA 微调技术。
*   **效率提升**：论文强调 SCALe-SFT 仅需 SFT + GRPO 完整流程约 **1/7 的训练时间**，即可达到相近的性能水平。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构的模型上进行了交叉验证，涵盖了从 3B 到 7B 的参数规模。
*   **消融实验**：专门对比了“固定权重（FW）”与“动态调度权重（SCALe）”的效果，证明了动态调度的必要性。
*   **定性分析**：提供了多组推理案例对比，并统计了“无效生成率”（即模型忘记闭合标签或陷入死循环的概率）。
*   **评价**：实验设计客观、公平，通过多模型、多任务的对比证明了方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：SCALe-SFT 相比原始 SFT 在准确率上提升了约 **3%**；在结合 GRPO 后，性能提升最高达 **5.4%**。
*   **高效替代**：在算力受限时，仅使用 SCALe-SFT 即可超越传统的“SFT + 强化学习”组合，是一种极具性价比的轻量化方案。
*   **结构稳定性**：SCALe 显著降低了模型生成无效格式（如缺失 `<answer>` 标签）的比例，提升了推理的可靠性。

### 7. 优点
*   **简单高效**：无需复杂的强化学习环境（如奖励函数设计、采样等），仅通过修改损失函数权重即可实现显著提升。
*   **架构无关**：该方法不依赖特定模型结构，具有很强的通用性。
*   **解决“冗长病”**：通过长度无关的损失计算，有效抑制了模型为了凑 Token 而生成无意义长推理的倾向。

### 8. 不足与局限
*   **模态限制**：目前主要针对单图视觉推理，尚未在多图或纯视频推理任务中广泛验证。
*   **超参数依赖**：权重的起始值和结束值（如 1.0 到 0.5）仍需人工设定，未来可能需要自动化的在线权重调整机制。
*   **任务范围**：实验主要集中在科学和图表问答，对于更广泛的开放域对话或创意写作任务的影响尚待观察。

（完）
