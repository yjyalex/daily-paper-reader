---
title: Sharpness-Aware Minimization in Logit Space Efficiently Enhances Direct Preference Optimization
title_zh: Logit 空间中的锐度感知最小化有效增强直接偏好优化
authors: "Haocheng Luo, Zehang Deng, Thanh-Toan Do, Mehrtash Harandi, Dinh Phung, Trung Le"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18258v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 增强大语言模型对齐的直接偏好优化 (DPO)
tldr: DPO在对齐大模型时存在“挤压效应”，即偏好回答的概率可能意外下降。本文通过理论分析发现该现象源于高曲率方向的残差扩张，并提出在Logit空间应用锐度感知最小化（logits-SAM）来抑制此行为。该方法仅扰动输出层，计算开销极低，在多个模型和基准测试中显著提升了DPO及其变体的性能。
selection_source: fresh_fetch
motivation: 针对DPO训练中出现的偏好回答概率下降（挤压效应）问题，探究其背后的动力学原因并寻求高效的改进方案。
method: 提出logits-SAM，通过在Logit空间引入锐度感知最小化来正则化曲率，且仅对输出层进行扰动以保持计算效率。
result: 在Pythia、Mistral和Gemma等模型上的实验证明，logits-SAM能有效缓解挤压效应并持续提升DPO的对齐效果。
conclusion: logits-SAM是一种简单、高效且通用的优化策略，能显著增强DPO及其变体在人类偏好对齐任务中的表现。
---

## 摘要
直接偏好优化 (DPO) 因其简单性和训练稳定性，已成为将预训练大语言模型与人类偏好对齐的一种流行算法。然而，DPO 受到最近发现的“挤压效应”（也称为似然偏移）的影响，即在训练过程中，首选响应的概率会无意中降低。为了理解并缓解这一现象，我们开发了一个理论框架，对 Logit 空间中的逐坐标动力学进行建模。我们的分析表明，负梯度更新会导致残差沿高曲率方向迅速扩张，这是挤压效应的根本原因，而锐度感知最小化 (SAM) 可以通过其曲率正则化效应抑制这种行为。基于这一见解，我们研究了 logits-SAM，这是一种计算效率高的变体，仅对输出层进行扰动，且开销可以忽略不计。在 Pythia-2.8B、Mistral-7B 和 Gemma-2B-IT 上跨多个数据集和基准测试进行的广泛实验表明，logits-SAM 持续提高了 DPO 的有效性，并能与其他 DPO 变体无缝集成。代码可在 https://github.com/RitianLuo/logits-sam-dpo 获取。

## Abstract
Direct Preference Optimization (DPO) has emerged as a popular algorithm for aligning pretrained large language models with human preferences, owing to its simplicity and training stability. However, DPO suffers from the recently identified squeezing effect (also known as likelihood displacement), where the probability of preferred responses decreases unintentionally during training. To understand and mitigate this phenomenon, we develop a theoretical framework that models the coordinate-wise dynamics in logit space. Our analysis reveals that negative-gradient updates cause residuals to expand rapidly along high-curvature directions, which underlies the squeezing effect, whereas Sharpness-Aware Minimization (SAM) can suppress this behavior through its curvature-regularization effect. Building on this insight, we investigate logits-SAM, a computationally efficient variant that perturbs only the output layer with negligible overhead. Extensive experiments on Pythia-2.8B, Mistral-7B, and Gemma-2B-IT across multiple datasets and benchmarks demonstrate that logits-SAM consistently improves the effectiveness of DPO and integrates seamlessly with other DPO variants. Code is available at https://github.com/RitianLuo/logits-sam-dpo.

---

## 论文详细总结（自动生成）

以下是对论文《Sharpness-Aware Minimization in Logit Space Efficiently Enhances Direct Preference Optimization》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：直接偏好优化（DPO）虽然简单稳定，但存在**“挤压效应”**（Squeezing Effect，又称似然偏移）。即在训练过程中，模型对人类偏好回答（Preferred responses）的生成概率有时会意外下降，导致对齐失败或性能退化。
*   **核心目标**：从理论上揭示挤压效应的动力学根源，并提出一种计算高效的优化方案来缓解该问题，提升大语言模型（LLM）的对齐效果。

### 2. 方法论
*   **核心思想**：利用**锐度感知最小化（SAM）**的曲率正则化特性来抑制高曲率方向的残差扩张。
*   **理论发现**：论文通过建模 Logit 空间的坐标动力学发现，DPO 中的负梯度更新（针对被拒绝回答的更新）会导致残差沿 Hessian 矩阵特征值较大的方向（高曲率方向）迅速扩张，这是挤压效应的根本原因。
*   **关键技术（logits-SAM）**：
    *   **局部扰动**：不同于传统 SAM 扰动所有参数，logits-SAM **仅扰动输出层（lm_head）**的参数。
    *   **计算流程**：利用倒数第二层的隐藏状态手动计算输出层的扰动，从而避免了传统 SAM 需要的两次完整前向/后向传播。
    *   **符号对齐**：理论证明扰动半径 $\rho$ 的符号应与学习率一致，以有效抑制挤压效应。
    *   **算法实现**：在单次前向传播中计算扰动，随后在扰动点进行正常的梯度更新，额外开销极低。

### 3. 实验设计
*   **数据集/场景**：
    *   **对话与摘要**：Anthropic-HH（对话）、Reddit TL;DR（摘要）。
    *   **通用指令遵循**：UltraFeedback Binarized。
    *   **安全对齐**：SorryBench（评估有害请求的拒绝率）。
*   **Benchmark**：AlpacaEval 2（长度控制胜率 LC）、Arena-Hard v0.1、MT-Bench（GPT-4 评分）。
*   **对比方法**：
    *   **基础方法**：DPO、SLiC-HF、CPO。
    *   **安全增强方法**：CHES。
    *   **模型**：Pythia-2.8B、Mistral-7B-v0.1、Gemma-2B-IT。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA A100 GPU**。
*   **算力配置**：
    *   Pythia-2.8B：使用 2 张 A100，采用 DDP（分布式数据并行）。
    *   Mistral-7B：使用 4 张 A100，采用 DeepSpeed ZeRO-3 优化。
*   **效率表现**：logits-SAM 相比 AdamW 仅增加约 **2-3% 的训练时间**，显存增加几乎可忽略不计。相比之下，原生 SAM 在同等硬件下因显存翻倍和计算量翻倍而难以运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 7B 不同规模的模型，并在多个主流对齐基准上进行了验证。
*   **充分性**：
    *   **消融实验**：对超参数 $\rho$ 进行了详细的敏感性分析。
    *   **动力学验证**：通过 1000 维玩具示例和真实数据曲线验证了理论预测的准确性。
    *   **泛化性**：证明了 logits-SAM 可以无缝集成到 SLiC-HF 和 CPO 等多种 DPO 变体中。
*   **客观性**：使用了 GPT-5-mini 和 GPT-4 等强模型作为自动评判员，遵循了标准化的评估协议。

### 6. 主要结论与发现
*   **缓解挤压效应**：logits-SAM 显著减缓了被拒绝回答概率的下降，并提升了偏好回答的概率增长。
*   **性能提升**：在所有测试的基准测试中，加入 logits-SAM 的 DPO 变体均优于原始版本（如在 Arena-Hard 上 DPO 胜率从 19.0% 提升至 23.1%）。
*   **平滑解空间**：测量发现 logits-SAM 收敛到了 Hessian 迹更小的区域，即找到了更平滑的极小值点，从而增强了泛化能力。
*   **安全增强**：在安全对齐任务中，logits-SAM 显著提高了模型对有害指令的拒绝率。

### 7. 优点
*   **理论与实践结合**：不仅提供了严谨的二阶动力学分析，还给出了极具工程价值的高效实现。
*   **极低开销**：解决了 SAM 在大模型上难以落地的痛点，使其成为一种几乎“免费”的性能增强手段。
*   **即插即用**：作为一种优化层面的改进，不改变损失函数，具有极强的通用性。

### 8. 不足与局限
*   **超参数微调**：虽然 $\rho$ 的搜索范围较小，但仍需针对不同模型和任务进行初步筛选（推荐在 $10^{-5}$ 到 $10^{-4}$ 之间）。
*   **理论假设限制**：理论分析基于固定特征（Kernel Regime）假设，虽然实验证明其有效，但在极深网络中的复杂交互可能未被完全建模。
*   **模型规模覆盖**：实验最高仅涉及 7B 模型，尚未在 70B 或更大规模的超大模型上验证其扩展性。

（完）
