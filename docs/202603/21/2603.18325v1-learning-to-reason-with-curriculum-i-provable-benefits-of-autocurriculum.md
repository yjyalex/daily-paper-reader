---
title: "Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum"
title_zh: 课程学习推理 I：自动课程的可证明收益
authors: "Nived Rajaraman, Audrey Huang, Miro Dudik, Robert Schapire, Dylan J. Foster, Akshay Krishnamurthy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18325v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 思维链强化学习中的自动课程学习
tldr: 本研究针对思维链推理模型训练中高昂的数据与计算成本，提出了“自动课程学习”（Autocurriculum）框架。该方法通过让模型根据自身当前表现自适应地选择训练题目，在理论上证明了其显著优势：在监督微调（SFT）阶段能以指数级减少所需的推理演示数量，在强化学习（RL）阶段则能使计算成本与参考模型质量解耦。这一方法借鉴了提升法等经典技术，无需对提示词分布做预设，为高效培养模型推理能力提供了理论支撑。
selection_source: fresh_fetch
motivation: 旨在解决训练思维链推理模型时面临的极高数据获取成本和计算资源消耗问题。
method: 提出自动课程学习机制，通过模型性能反馈动态选择训练样本，实现自适应的数据监督和强化学习。
result: 证明了该方法能显著减少SFT所需的演示数据，并降低RL训练对高质量参考模型的依赖。
conclusion: 研究表明自适应数据选择策略在提升推理模型训练效率方面具有可证明的理论效益。
---

## 摘要
思维链（Chain-of-thought）推理，即语言模型在给出最终回答前通过生成思考标记（thinking tokens）来消耗额外的计算量，已显著提升了模型能力。然而，训练这些推理模型的成本在数据和计算方面都极其高昂，因为它涉及从人类或合成生成器中收集长路径的推理行为轨迹，并进一步通过强化学习对模型进行后训练。这些成本是根本性的，还是可以通过更好的算法设计来降低？我们证明了自动课程（autocurriculum）——即模型利用自身表现来决定训练应关注哪些问题——在监督微调（SFT）和强化学习（RL）方面均能可证明地改进标准训练方案。对于 SFT，我们展示了自动课程通过将教师监督集中在当前模型难以处理的提示词（prompts）上，所需的推理示例比非自适应微调呈指数级减少。对于 RL 微调，自动课程将计算成本与参考模型的质量解耦，将后者降低为几乎与目标准确率无关的预热（burn-in）成本。这些改进纯粹源于自适应数据选择，借鉴了提升法（boosting）和反例学习（learning from counterexamples）的经典技术，且无需对提示词的分布或难度做出任何假设。

## Abstract
Chain-of-thought reasoning, where language models expend additional computation by producing thinking tokens prior to final responses, has driven significant advances in model capabilities. However, training these reasoning models is extremely costly in terms of both data and compute, as it involves collecting long traces of reasoning behavior from humans or synthetic generators and further post-training the model via reinforcement learning. Are these costs fundamental, or can they be reduced through better algorithmic design? We show that autocurriculum, where the model uses its own performance to decide which problems to focus training on, provably improves upon standard training recipes for both supervised fine-tuning (SFT) and reinforcement learning (RL). For SFT, we show that autocurriculum requires exponentially fewer reasoning demonstrations than non-adaptive fine-tuning, by focusing teacher supervision on prompts where the current model struggles. For RL fine-tuning, autocurriculum decouples the computational cost from the quality of the reference model, reducing the latter to a burn-in cost that is nearly independent of the target accuracy. These improvements arise purely from adaptive data selection, drawing on classical techniques from boosting and learning from counterexamples, and requiring no assumption on the distribution or difficulty of prompts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：训练具备思维链（CoT）推理能力的语言模型成本极高。监督微调（SFT）需要大量高质量的推理轨迹（由人类或强模型生成），而强化学习（RL）则需要海量的模型采样（Rollouts）。
*   **研究动机**：现有的训练方案通常是“非自适应”的，即在所有题目上平均分配资源。论文探讨是否可以通过**自动课程（Autocurriculum）**——即让模型根据自身表现动态选择“该学什么”——来显著降低这些统计和计算成本。
*   **整体含义**：该研究为推理模型的训练效率提供了理论基石，证明了自适应数据选择不仅是经验上的技巧，更具有可证明的数学优势。

### 2. 方法论：核心思想与技术细节
论文提出了两种核心算法框架，均利用**结果验证器（Outcome Verifier）**来引导学习：

*   **AutoTune (用于 SFT)**：
    *   **核心思想**：借鉴了经典算法“提升法（Boosting）”和“反例学习”。
    *   **流程**：模型首先在当前分布下预测，验证器检查结果。只有当模型预测错误时，才向“教师”请求该题目的完整推理轨迹（CoT）。
    *   **技术细节**：通过维护一个模型集成（Ensemble），利用拒绝采样（Rejection Sampling）构建新的训练分布，将监督重点放在模型当前的“弱点”上。
*   **AutoTune.RL (用于 RL)**：
    *   **核心思想**：将计算资源从“容易”的题目转移到“难”但“可解”的题目。
    *   **流程**：利用参考模型（Reference Model）进行拒绝采样生成正确轨迹。自动课程机制确保了采样成本与目标准确率解耦，避免了在已掌握题目上的重复采样。
*   **关键公式/概念**：引入了**Natarajan 维度 ($d$)** 来衡量模型类的复杂度，并利用**序列级覆盖度 ($C_{seq}$)** 来量化参考模型的质量。

### 3. 实验设计与 Benchmark
*   **研究性质**：这是一篇**纯理论证明论文**，而非实证实验论文。
*   **对比基准 (Benchmark)**：
    *   对比对象是 **Joshi et al. (2025)** 提出的非自适应推理学习框架。
    *   对比维度是**样本复杂度**（需要多少教师演示）和**计算复杂度**（需要多少次模型采样）。
*   **场景设定**：设定在具有“可验证奖励”（如数学、代码）的推理任务中，假设存在一个完美的验证器（Verifier）和一个能够提供正确推理路径的教师（Oracle）。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究通过数学证明（如收敛速度分析、复杂度上界推导）来验证结论，不涉及大规模模型训练的实测。

### 5. 实验数量与充分性
*   **理论覆盖面**：论文非常充分地探讨了多种理论场景：
    1.  **确定性模型 vs. 随机性模型**：证明了自动课程在两种模型性质下均有效。
    2.  **SFT vs. RLVR**：涵盖了有监督和强化学习两种主流后训练范式。
    3.  **分布无关性**：证明了这些收益不依赖于提示词（Prompts）的具体分布或难度梯度。
*   **客观性**：通过严谨的数学推导（如引理 B.1 的误差分解）保证了结论的客观性，而非依赖于特定实验种子的随机结果。

### 6. 主要结论与发现
*   **SFT 的指数级提升**：在 SFT 阶段，自动课程将所需的教师演示数量从 $e^{\Theta(d/\epsilon)}$ 降低到 $e^{O(d)}$。这意味着**教师监督的成本几乎与目标准确率无关**。
*   **RL 的解耦效应**：在 RL 阶段，自动课程使计算成本与参考模型的质量解耦。计算开销被缩减为一个“预热成本”（Burn-in cost），一旦跨过该阶段，提升准确率的边际成本极低。
*   **计算效率**：证明了通过自适应选择数据，可以在不牺牲泛化能力的前提下，大幅度压缩推理模型的训练周期。

### 7. 优点与亮点
*   **理论突破**：首次为 LLM 推理训练中的“课程学习”提供了指数级加速的可证明保证。
*   **跨学科结合**：巧妙地将 90 年代的提升法（Boosting）理论与现代的思维链（CoT）训练相结合。
*   **普适性**：提出的框架不需要预先知道题目的难度（无需人工标注难度），模型通过验证器反馈自动识别难度。

### 8. 不足与局限
*   **理想化假设**：假设存在**完美的验证器**（如单元测试），但在许多开放式推理任务中，验证器本身可能是不准确或缺失的。
*   **固定参考模型**：在 RL 分析中假设参考模型是固定的，尚未深入探讨“自我博弈（Self-play）”中参考模型不断进化时的动态复杂性。
*   **工程实现距离**：理论上的“模型集成（Ensemble）”在实际训练超大规模参数模型（如千亿级 LLM）时，存储和计算开销巨大，需要更高效的近似实现。

（完）
