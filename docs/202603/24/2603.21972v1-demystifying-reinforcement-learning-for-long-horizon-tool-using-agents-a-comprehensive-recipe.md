---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘面向长程工具使用智能体的强化学习：一份全面指南
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于长程规划和工具编排的强化学习
tldr: 本研究针对大语言模型在长程规划和工具使用中的强化学习（RL）应用，通过在TravelPlanner基准上的系统性实验，探索了奖励设计、模型规模、数据组成、算法选择和环境稳定性五个核心维度。研究总结了7条关键经验，如小模型更依赖阶段性奖励而大模型则能高效处理简单稠密奖励，并确定了约1000个训练样本的最佳平衡点。最终，基于该方案训练的模型在TravelPlanner上达到了SOTA水平，显著优于现有领先模型。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在复杂多轮环境中使用强化学习进行长程规划时缺乏实用扩展方案的问题。
method: 在TravelPlanner平台上，从奖励塑造、模型缩放、数据构成、算法选择和环境稳定性五个维度进行了系统性的实证研究。
result: 总结出模型规模与奖励/算法选择的依赖关系、1K样本的性能平衡点以及环境稳定性对策略退化的影响等关键结论。
conclusion: 本研究提供的强化学习训练方案使模型在长程工具使用任务中取得了当前最佳性能，为构建自主智能体提供了实践指南。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLMs) 演进为具备长程规划能力的自主智能体至关重要，然而在复杂的、多轮交互环境中扩展强化学习的实用方案仍然难以捉摸。本文利用 TravelPlanner 开展了一项系统的实证研究，TravelPlanner 是一个具有挑战性的测试平台，要求通过工具编排来满足多方面的约束。我们从五个维度分解了智能体强化学习的设计空间：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的稠密奖励高效收敛；(2) 约 1000 个具有平衡难度混合的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，我们训练的强化学习模型在 TravelPlanner 上达到了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：揭秘面向长程工具使用智能体的强化学习：一份全面指南

#### 1. 论文的核心问题与整体含义
本文旨在解决大语言模型（LLM）在执行**长程规划（Long-Horizon Planning）**和**复杂工具编排**任务时，缺乏系统性强化学习（RL）训练方案的问题。虽然 RL 已被证明能提升模型推理能力，但在需要数十步交互、满足多重约束（如预算、偏好、逻辑一致性）的复杂场景下，如何有效设计奖励、选择数据和算法仍不明确。论文通过在 **TravelPlanner** 基准上进行大规模实证研究，试图为社区提供一份可落地的智能体强化学习“配方”。

#### 2. 论文提出的方法论：STAR 框架
研究者提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练流水线，包含三个阶段：
*   **数据合成 (Synthesis)：** 针对训练数据稀缺问题，通过采样原子旅行元素并验证其可行性，利用强模型（如 GPT-OSS-120B）生成具有难度控制（简单、中等、困难）的自然语言查询。
*   **监督微调 (SFT)：** 使用教师模型（DeepSeek-V3.2-Exp-Thinking）生成高质量轨迹，通过拒绝采样筛选出成功案例，为 RL 提供具备基本任务理解能力的初始策略。
*   **强化学习 (RL)：** 核心阶段，基于 **GRPO (Group Relative Policy Optimization)** 算法，从五个关键维度探索设计空间：
    *   **奖励塑造：** 比较稠密奖励（SUM）、半稀疏奖励（MACRO）、纯稀疏奖励（SUCCESS）及阶段性课程奖励（CURRICULUM）。
    *   **模型缩放：** 评估 1.5B、3B 和 7B 不同参数规模的影响。
    *   **数据组成：** 探索数据量（100 到 2000 条）和难度分布的影响。
    *   **算法选择：** 对比标准 GRPO 与增强探索的变体（DAPO, ARPO）。
    *   **环境稳定性：** 模拟真实 API 故障，注入随机工具执行错误。

#### 3. 实验设计
*   **主要测试基准：** **TravelPlanner**。包含 1000 个测试实例，要求智能体在本地沙盒环境中使用 6 种工具（搜索航班、酒店、餐厅等）制定旅行计划。
*   **域外 (OOD) 评估：** 为了验证泛化性，在 7 个知识密集型问答数据集（如 Natural Questions, HotpotQA 等）上进行测试。
*   **对比方法：** 
    *   **闭源/领先模型：** GPT-4o, GPT-5, Kimi-K2.5, Gemini-1.5-Pro, Qwen3.5 等。
    *   **特定领域 SOTA：** Planner-R1, Search-R1。
    *   **基准线：** 各规模模型的 Base 版本和 SFT 版本。

#### 4. 资源与算力
论文详细记录了算力消耗：
*   **硬件：** 1.5B 和 3B 模型使用单节点（8 × NVIDIA A100-80G）；7B 模型使用双节点（16 × A100）。
*   **时长：** 训练 5 个 Epoch。以 3B 模型为例，标准 GRPO 约需 176 GPU 小时；7B 模型则需约 368-547 GPU 小时（取决于算法）。
*   **上下文长度：** 训练时最大 30K tokens，推理时扩展至 32K。

#### 5. 实验数量与充分性
实验设计非常详尽且具有高度的控制变量特征：
*   **多维度消融：** 针对奖励、规模、数据、算法、稳定性五个维度分别进行了独立的消融实验。
*   **样本量：** 合成了超过 10K 查询，最终筛选出 1.2K 高质量 SFT 轨迹和 1K RL 训练样本。
*   **客观性：** 实验在统一的本地沙盒中运行，避免了外部 API 波动的影响；同时引入了 OOD 测试，有效评估了“对齐税（Alignment Tax）”和泛化能力。

#### 6. 论文的主要结论与发现
1.  **规模依赖性：** 小模型（1.5B）极度依赖课程奖励和复杂的探索算法（如 ARPO）；而大模型（7B）直接使用简单的稠密奖励和标准 GRPO 效果更好。
2.  **数据“甜点位”：** 约 **1000 条** 混合难度的训练样本是性能平衡点。超过此量级会导致模型在 TravelPlanner 上过拟合，从而损害 OOD 泛化能力。
3.  **难度平衡至关重要：** 全困难数据会导致奖励过稀疏使训练崩溃，全简单数据则无法处理复杂约束。
4.  **环境稳定性影响：** 智能体能容忍轻微（<5%）的环境噪声，但高故障率（10%）会显著阻碍策略优化。
5.  **SOTA 性能：** 基于该方案训练的 7B 模型在 TravelPlanner 上取得了 **62.8%** 的成功率，远超 Kimi-K2.5 (11.8%) 和 GPT-4o (1.5%)。

#### 7. 优点
*   **系统性强：** 首次对长程智能体 RL 的设计空间进行了如此全面的分解和实证。
*   **实用性高：** 提供了针对不同规模模型的具体训练建议（Recipe），对工业界具有直接指导意义。
*   **开源贡献：** 提供了 STAR 框架和训练出的模型权重，促进了智能体 RL 的透明化。

#### 8. 不足与局限
*   **环境局限：** 虽然 TravelPlanner 具有挑战性，但仍属于模拟沙盒，与真实互联网环境的开放性和动态性仍有差距。
*   **模型规模上限：** 实验最高仅覆盖到 7B 参数，更大规模模型（如 70B+）的行为是否符合该规律尚待验证。
*   **泛化评估单一：** OOD 测试主要集中在问答任务，未广泛测试跨领域（如代码、GUI 操作）的迁移能力。
*   **单因素分析：** 实验主要采取控制变量法，多个因素（如奖励与算法）之间的复杂交互作用分析较少。

（完）
