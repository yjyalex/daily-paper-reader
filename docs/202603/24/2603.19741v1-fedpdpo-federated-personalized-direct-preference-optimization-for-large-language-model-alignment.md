---
title: "FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment"
title_zh: FedPDPO：用于大语言模型对齐的联邦个性化直接偏好优化
authors: "Kewen Zhu, Liping Yi, Zhiming Zhao, Zhuang Qi, Han Yu, Qinghua Hu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19741v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大语言模型对齐的联邦个性化直接偏好优化
tldr: 针对联邦学习中非独立同分布（non-IID）偏好数据导致的大模型对齐性能下降问题，本文提出FedPDPO框架。该框架采用LoRA参数高效微调架构，通过全局共享LoRA与个性化客户端头部、引入显式奖励头以及瓶颈适配器，有效解决了数据异构性并增强了奖励泛化能力。实验证明其在多项任务中达到SOTA水平，显著提升了对齐准确率。
selection_source: fresh_fetch
motivation: 传统的直接偏好优化（DPO）在联邦学习环境下因数据高度异构和隐式奖励泛化受限，导致模型对齐性能严重退化。
method: 提出一种结合全局共享LoRA、个性化客户端头部、显式奖励头及瓶颈适配器的个性化联邦学习框架。
result: "在多个偏好数据集上的实验显示，该方法在域内和跨域联邦设置下均取得SOTA性能，平均准确率最高提升了4.80%。"
conclusion: FedPDPO通过多维度的个性化策略有效克服了联邦偏好对齐中的异构性挑战，为大规模语言模型的隐私保护对齐提供了高效且稳健的方案。
---

## 摘要
在联邦学习（FL）中，由于偏好数据具有去中心化、隐私敏感且高度非独立同分布（non-IID）的特性，将大语言模型（LLMs）与人类偏好对齐具有挑战性。直接偏好优化（DPO）为人类反馈强化学习（RLHF）提供了一种高效的替代方案，但其在联邦学习中的直接应用在非独立同分布数据下会面临严重的性能退化，且隐式奖励的泛化能力有限。为了弥补这一差距，我们提出了 FedPDPO（联邦个性化直接偏好优化），这是一个用于大语言模型偏好对齐的个性化联邦框架。它采用参数高效微调架构，每个客户端维护一个冻结的预训练大语言模型主干，并辅以低秩自适应（LoRA）适配器，从而实现通信高效的聚合。为了解决非独立同分布的异构性，我们设计了：（1）全局共享的 LoRA 适配器与个性化的客户端特定大语言模型头部；（2）一种带有客户端特定显式奖励头部的个性化 DPO 训练策略，以补充隐式奖励并进一步缓解非独立同分布异构性；以及（3）一个瓶颈适配器来平衡全局和局部特征。我们提供了理论分析，确立了其概率基础和合理性。在多个偏好数据集上的广泛实验证明了其最先进的性能，在联邦域内和跨域设置中实现了高达 4.80% 的平均准确率提升。

## Abstract
Aligning large language models (LLMs) with human preferences in federated learning (FL) is challenging due to decentralized, privacy-sensitive, and highly non-IID preference data. Direct Preference Optimization (DPO) offers an efficient alternative to reinforcement learning with human feedback (RLHF), but its direct application in FL suffers from severe performance degradation under non-IID data and limited generalization of implicit rewards. To bridge this gap, we propose FedPDPO (Federated Personalized Direct Preference Optimization), a personalized federated framework for preference alignment of LLMs. It adopts a parameter-efficient fine-tuning architecture where each client maintains a frozen pretrained LLM backbone augmented with a Low-Rank Adaptation (LoRA) adapter, enabling communication-efficient aggregation. To address non-IID heterogeneity, we devise (1) the globally shared LoRA adapter with the personalized client-specific LLM head. Moreover, we introduce (2) a personalized DPO training strategy with a client-specific explicit reward head to complement implicit rewards and further alleviate non-IID heterogeneity, and (3) a bottleneck adapter to balance global and local features. We provide theoretical analysis establishing the probabilistic foundation and soundness. Extensive experiments on multiple preference datasets demonstrate state-of-the-art performance, achieving up to 4.80% average accuracy improvements in federated intra-domain and cross-domain settings.

---

## 论文详细总结（自动生成）

以下是对论文《FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在联邦学习（FL）环境下，将大语言模型（LLM）与人类偏好对齐面临两大挑战：一是**数据异构性（Non-IID）**，不同客户端的偏好分布差异巨大，导致全局模型难以兼顾；二是**隐式奖励的泛化限制**，直接偏好优化（DPO）依赖隐式奖励，在面对高度多样化的偏好分布时表现不佳。
*   **核心目标**：提出一个名为 **FedPDPO** 的个性化联邦学习框架，旨在通过参数高效微调（PEFT）和个性化策略，在保护隐私的同时，解决联邦偏好对齐中的非独立同分布问题。

### 2. 方法论
FedPDPO 结合了参数高效微调、个性化架构设计和增强的优化目标：
*   **个性化架构设计**：
    *   **冻结主干 + 共享 LoRA**：每个客户端拥有一个冻结的预训练 LLM 主干，仅通过训练和聚合低秩自适应（LoRA）适配器来交换全局知识。
    *   **个性化头部**：每个客户端维护私有的 **LLM 预测头**（生成 Token）和 **显式奖励头**（评估得分），以适应本地特定的偏好分布。
*   **PDPO 训练策略**：
    *   **双重奖励机制**：在 DPO 原有的隐式奖励（基于 Logits）基础上，引入显式奖励头的输出作为补充。
    *   **自适应缩放**：通过公式计算隐式与显式奖励的比例，并使用线性调度的权重 $w_r$ 来平衡两者，增强模型在异构数据下的泛化力。
*   **瓶颈适配器（Bottleneck Adapter）**：在主干网络与个性化头部之间插入一个轻量级的瓶颈层，用于融合全局 LoRA 特征与局部主干特征，平衡全局协作与局部个性化。
*   **交替优化流程**：在本地训练中，先冻结 LoRA 训练个性化模块，再冻结个性化模块训练 LoRA，实现知识的有效蒸馏与迁移。

### 3. 实验设计
*   **数据集与场景**：
    *   **域内（Intra-domain）**：IMDB（情感分析）、Code-Vulnerability-Security（代码安全）。通过奖励边际（Reward-margin）和编程语言类型进行 Non-IID 划分。
    *   **跨域（Cross-domain）**：WebGPT（问答）、PyDPO（Python 代码）、UltraFeedback（指令遵循），每个客户端分配完全不同的任务领域。
*   **Benchmark 与对比方法**：
    *   **基准方法**：FedAvg (即 FedDPO)、Per-FedAvg、FedAMP、FedPer、FedRep。
    *   **对齐算法**：对比了基于 PPO（强化学习）和 DPO（直接优化）的两种实现路径。
*   **评估指标**：偏好准确率（Preference Accuracy），即模型正确排序“优选回答”与“拒绝回答”的比例。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA GeForce RTX 3090 GPU (24 GB)** 上完成。
*   **仿真方式**：通过顺序执行多个客户端的方式模拟联邦环境。
*   **模型规模**：主要使用 **DistilGPT2** (82M 参数) 作为主干，并使用 **TinyLlama-1.1B** 验证了可扩展性。
*   **训练细节**：通信轮数通常为 10-20 轮，本地训练 1-3 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 客户端、10 客户端及 11 客户端的不同规模配置。
*   **消融实验**：分别验证了“无瓶颈适配器”和“无显式奖励头”的情况，证明了各组件的必要性。
*   **敏感性分析**：对 LoRA 秩（Rank）、奖励头维度、适配器维度以及 DPO 温度参数 $\beta$ 进行了详细的参数搜索。
*   **充分性评价**：实验设计较为全面，涵盖了从情感分析到代码生成的多种任务，且每组实验均运行 5 次取平均值，具有较高的客观性和统计说服力。

### 6. 主要结论与发现
*   **性能领先**：FedPDPO 在所有设置下均达到 SOTA，相比最强基准方法，平均准确率提升了 **1.71% 至 4.80%**。
*   **DPO 优于 PPO**：在联邦场景下，基于 DPO 的方法在稳定性和效率上普遍优于基于 PPO 的方法。
*   **个性化的价值**：显式奖励头和个性化头部的引入显著缓解了 Non-IID 带来的性能退化，尤其在跨域任务中表现突出。
*   **理论支撑**：通过数学证明（Gumbel-Bradley-Terry 等价性）确立了 PDPO 目标的理论合理性。

### 7. 优点
*   **创新性**：首次将个性化联邦学习（PFL）与 DPO 结合用于 LLM 对齐，解决了联邦对齐中的核心痛点。
*   **通信高效**：仅聚合 LoRA 参数（不到总参数的 1%），极大地降低了带宽需求。
*   **鲁棒性**：通过显式奖励修正隐式奖励的偏差，增强了模型在处理极端异构数据时的稳定性。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在小型 LLM（如 DistilGPT2 和 TinyLlama），尚未在 7B 或更大规模的模型上验证其在大规模参数下的表现。
*   **客户端数量较少**：实验中客户端数量最多为 11 个，对于真实世界中成千上万个客户端的大规模联邦场景，其聚合效率和收敛性仍待观察。
*   **仿真环境偏差**：采用单卡顺序执行而非真实的分布式网络环境，未考虑网络延迟、掉线（Stragglers）等实际系统挑战。

（完）
