---
title: Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment
title_zh: 通过符号递归自对齐，利用经过验证的推理来稳定迭代自训练
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21558v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过验证推理步骤来稳定迭代自训练
tldr: 针对迭代自训练中推理错误累积导致的“递归漂移”问题，本文提出神经符号递归自对齐（NSRSA）框架。该方法通过嵌入符号验证子系统，在推理步骤层面（而非仅结果层面）对生成数据进行质量把控，利用sympy验证算术逻辑并强制执行领域约束。实验表明，NSRSA能有效剔除“侥幸猜对”的错误推理，结合DPO进一步提升了模型的推理可靠性，为实现稳定、可衡量的递归自我提升提供了新路径。
selection_source: fresh_fetch
motivation: 迭代自训练中推理错误的累积会导致模型性能退化和模式崩溃，即所谓的“递归漂移”问题。
method: 提出NSRSA框架，利用符号验证系统在推理步骤级别对算术运算、逻辑流和领域约束进行严格校验。
result: "NSRSA成功剔除了34%虽答案正确但推理错误的样本，并使DPO偏好学习的奖励准确率从46%提升至63%。"
conclusion: 引入外部符号验证能有效稳定递归自改进过程，使模型在具备自动化验证条件的领域内实现可靠的能力增长。
---

## 摘要
递归自我提升——即模型在其自身输出上进行迭代训练——有望实现能力的持续增长，但面临着一个根本性障碍：递归漂移。随着模型在多次迭代中利用自生成数据进行训练，中间推理过程中的错误会不断累积，导致模式崩溃和性能下降。我们提出了神经符号递归自对齐（NSRSA），通过嵌入一个符号验证子系统，在推理步骤层面把控训练数据的质量，从而稳定迭代自训练。与仅针对结果的过滤（这种方式会接纳带有错误推理的“侥幸猜测”）不同，NSRSA 通过 sympy 验证每个算术运算，检查推理步骤间的逻辑流一致性，并强制执行领域约束。我们在 GSM8K 数据集上使用 Qwen3-4B-Thinking 模型，在五种条件下进行了 5 次自训练迭代评估：无验证、结果验证、多数投票、完整的 NSRSA 符号验证以及结合 DPO 的 NSRSA。我们的过滤分析表明，NSRSA 拒绝了约 34% 通过结果验证但答案正确的解法，从而从训练集中消除了带有错误推理的“侥幸猜测”。我们进一步证明，利用 NSRSA 验证构建 DPO 偏好对，可以教会模型区分正确与错误的推理（奖励准确率从 46% 提升至 63%）。NSRSA 提供了一个可扩展的框架，展示了在可进行自动验证的领域中，外部符号验证如何使递归自我提升变得可衡量且可靠。

## Abstract
Recursive self-improvement--where a model iteratively trains on its own outputs--promises sustained capability growth but faces a fundamental obstacle: recursive drift. As models train on self-generated data across multiple iterations, errors in intermediate reasoning compound, leading to mode collapse and performance degradation. We propose Neuro-Symbolic Recursive Self-Alignment (NSRSA), which stabilizes iterative self-training by embedding a symbolic verification subsystem that gates training data quality at the reasoning step level. Unlike outcome-only filtering (which admits "lucky guesses" with flawed reasoning), NSRSA verifies each arithmetic operation via sympy, checks logical flow consistency across reasoning steps, and enforces domain constraints. We evaluate NSRSA on GSM8K using Qwen3-4B-Thinking across 5 self-training iterations under five conditions: no verification, outcome verification, majority voting, full NSRSA symbolic verification, and NSRSA with DPO. Our filtering analysis shows that NSRSA rejects approximately 34% of correct-answer solutions that pass outcome verification, eliminating "lucky guesses" with flawed reasoning from the training set. We further demonstrate that constructing DPO preference pairs from NSRSA verification teaches the model to distinguish sound from flawed reasoning (reward accuracy 46% to 63%). NSRSA provides an extensible framework that demonstrates how external symbolic verification can make recursive self-improvement measurable and reliable within domains where automated verification is available.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NSRSA（神经符号递归自对齐）** 的框架，旨在解决大语言模型在迭代自训练过程中因错误累积导致的性能退化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：**递归漂移（Recursive Drift）**。在递归自我提升（RSI）中，模型在自身生成的数据上迭代训练。如果模型通过错误的推理步骤碰巧得到了正确答案（即“侥幸猜测”），并将其纳入训练集，错误模式就会不断累积，最终导致模型性能下降或模式崩溃（Model Collapse）。
*   **研究背景**：现有的自训练方法（如 STaR）大多仅依赖“结果验证”（答案是否正确），缺乏对中间推理步骤的细粒度把控。

### 2. 方法论：神经符号递归自对齐 (NSRSA)
NSRSA 的核心思想是在自训练循环中嵌入一个**符号验证子系统**，在推理步骤层面进行“门控”过滤。
*   **核心流程**：生成候选解 -> 符号验证过滤 -> LoRA 微调 -> 迭代。
*   **关键技术细节（四个验证维度）**：
    1.  **答案正确性**：最终提取的数值必须与 Ground Truth 匹配。
    2.  **算术验证**：利用 `sympy` 库解析并计算思维链（CoT）中的每一个算术表达式（如 $A \times B = C$），要求正确率 $\ge 80\%$。
    3.  **逻辑流验证**：通过算法追踪变量赋值。如果同一个变量在非相邻步骤中发生了大幅度且无依据的数值跳变（>50% 变化），则判定为幻觉。
    4.  **约束满足**：强制执行领域约束，如计数必须为非负数、离散物体必须为整数等。
*   **DPO 变体**：利用 NSRSA 构建偏好对，将“推理正确且答案正确”作为 Chosen，将“推理错误但答案正确（侥幸猜测）”作为 Rejected，教会模型区分严谨推理与凑答案。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：Qwen3-4B-Thinking（具备思维链能力的 4B 模型）。
    *   **主任务**：GSM8K（小学数学）。
    *   **迁移评估**：MATH-500（更难的数学竞赛题）。
*   **对比方法（Benchmark）**：
    1.  **无验证**：直接在自生成数据上训练。
    2.  **结果验证**：仅看最终答案是否正确（类似 STaR）。
    3.  **多数投票**：利用自一致性（Self-consistency）作为过滤信号。
    4.  **NSRSA (Symbolic)**：本文提出的符号验证 SFT。
    5.  **NSRSA + DPO**：结合偏好对训练。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A10G GPU**（每张 24GB 显存）。
*   **训练细节**：采用 LoRA 微调（r=16, alpha=32），bfloat16 精度。
*   **耗时**：DPO 训练约 2.25 小时；符号验证过程每轮迭代仅增加约 10 分钟 CPU 时间，开销极低。
*   **显存占用**：训练期间每张 GPU 约占用 18-20 GB VRAM。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **5 轮完整的迭代自训练**，每轮生成约 6 万个样本。
*   **充分性**：
    *   涵盖了从 80.5% 到 91.2% 的性能演进曲线。
    *   包含跨任务迁移实验（MATH-500），验证了泛化性。
    *   进行了**消融分析**，包括解析器覆盖率分析、验证通过率分解以及 Self-BLEU 模式崩溃分析。
    *   **公平性**：对比了不依赖标签的“多数投票”法，确保提升并非单纯来自 Ground Truth 的引入。

### 6. 主要结论与发现
*   **性能提升**：NSRSA 使模型在 GSM8K 上的准确率从 80.5% 提升至 **91.0%**，而“无验证”组在第 3 轮即发生崩溃（跌至 73.2%）。
*   **消除侥幸猜测**：分析显示，约 **34%** 的正确答案解法实际上包含错误的推理步骤，NSRSA 成功剔除了这些噪声。
*   **推理质量**：随着迭代进行，模型生成的“可验证推理”比例显著上升，说明模型学会了生成更严谨的步骤。
*   **抑制模式崩溃**：NSRSA 组的 Self-BLEU 分数保持在较低水平（0.35），表明模型在提升性能的同时保留了输出的多样性。

### 7. 优点与亮点
*   **细粒度监督**：将验证从“结果级”深入到“步骤级”，且无需人工标注（通过符号计算自动实现）。
*   **稳定性强**：有效解决了递归训练中的性能回退问题，实现了真正可持续的自我进化。
*   **神经符号结合**：巧妙利用 `sympy` 等成熟符号工具弥补神经网络在精确计算和逻辑一致性上的短板。

### 8. 不足与局限
*   **解析器局限性**：目前的解析器依赖正则表达式，对于自然语言描述的运算（如“五乘以三”）或复杂的跨行推导可能无法解析（此时会默认跳过验证，存在漏检风险）。
*   **领域局限性**：目前的约束和验证逻辑高度针对数学领域，迁移到代码或逻辑推理任务需要重新设计符号规则。
*   **依赖 Ground Truth**：虽然有多数投票对比，但 NSRSA 的核心过滤仍需已知正确答案，这限制了其在完全无监督场景下的应用。

（完）
