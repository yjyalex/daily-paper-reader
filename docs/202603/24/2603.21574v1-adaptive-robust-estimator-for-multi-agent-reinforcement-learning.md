---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 用于协作推理的鲁棒多智能体强化学习框架
tldr: 针对多智能体协作推理中存在的交互模糊性、信用分配困难以及奖励信号噪声导致的训练不稳定问题，本文提出了一种鲁棒的多智能体强化学习框架。该框架包含DACR结构化推理流程（回答-评论-重写）以明确各智能体的边际贡献，以及ARE自适应鲁棒估计器来处理重尾和噪声奖励。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和对噪声奖励的鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理面临交互模糊导致的信用分配难题，且易受重尾和噪声奖励干扰，导致训练不稳定。
method: 提出了由DACR（三阶段推理管道）和ARE（自适应鲁棒估计器）组成的强化学习框架，实现明确的贡献归因和稳健的优势估计。
result: 在数学推理和具身智能基准测试中，该方法在同构和异构设置下均优于基准，且在噪声奖励环境下表现出极强的鲁棒性。
conclusion: 该研究通过结构化交互和鲁棒估计技术，有效解决了多智能体强化学习中的信用分配与噪声干扰问题，提升了协作推理的性能。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性混淆了生成、批判和修正过程，导致智能体间的信用分配（credit assignment）变得困难。此外，在这种设定下的策略优化容易受到重尾（heavy-tailed）和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地归因每个智能体对其伙伴性能的边际贡献。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在奖励噪声的情况下，我们的方法在同质和异质设定下均一致优于基线方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注大语言模型（LLM）在多智能体协作推理（MARL）中的两大痛点：
*   **信用分配困难（Credit Assignment）：** 多智能体交互（如生成、批判、修正）过程模糊，难以量化单个智能体对最终结果的边际贡献。
*   **奖励信号的不稳定性：** 推理任务的奖励通常由验证器或奖励模型生成，往往具有**重尾分布（Heavy-tailed）**和**高噪声**特征。传统的策略优化算法（如 GRPO）依赖批次经验均值和方差进行归一化，极少数离群值（Outliers）就会导致梯度震荡甚至训练发散。

### 2. 方法论：核心思想与技术细节
论文提出了一个鲁棒的多智能体强化学习框架，包含两个核心组件：

*   **DACR（双智能体回答-批判-重写协议）：**
    *   **三阶段流程：** 1. 独立回答（生成初始链条）；2. 相互批判（交换答案并指出错误）；3. 修正（根据对方反馈优化自身答案）。
    *   **跨评估奖励机制：** 引入“跨阶段改进项”，即如果智能体 A 的批判导致智能体 B 的答案质量提升，则奖励 A。这实现了轻量化且显式的信用归因。
*   **ARE（自适应鲁棒估计器）：**
    *   **核心逻辑：** 改进了经典的“均值中位数”（Median-of-Means, MoM）估计器。ARE 将样本分成若干块，在块内使用**自适应鲁棒损失函数（Barron Loss）**进行位置估计，再取各块估计值的中位数。
    *   **优化算法：** 针对 Barron Loss 的非凸性，采用了**渐进非凸性（GNC）**框架和**迭代重权最小二乘法（IRLS）**，确保在存在离群值时仍能稳定收敛。
    *   **理论保障：** 论文证明了 ARE 在有限方差和重尾分布（仅存在 $1+\epsilon$ 阶矩）下的渐近正态性和偏差界限。

### 3. 实验设计
*   **数据集/场景：**
    *   **数学推理：** 在 MATH 7.5k 上训练，在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外）上测试。
    *   **具身智能：** 无人机视觉语言导航（VLN）任务，使用 FlightGPT 环境。
*   **对比方法：** 基准模型（Base Model）、基于标准 GRPO 的协作基线（baseline-Group）。
*   **智能体设置：** 涵盖同构（如两个 Qwen2.5-7B）和异构（如 Qwen2.5-1.5B 配对 Llama3.1-8B）设置。

### 4. 资源与算力
*   **硬件：** 使用了单节点 **8 张 NVIDIA A800 GPU**。
*   **软件框架：** 使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4 个引擎，张量并行度为 2）。
*   **训练细节：** 启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。

### 5. 实验数量与充分性
*   **实验规模：** 进行了多维度实验，包括不同参数规模（1.5B 到 8B）的模型对比、同/异构智能体组合。
*   **消融与压力测试：**
    *   **三阶段交互消融：** 验证了“回答-批判-重写”比单纯独立回答更有效。
    *   **噪声鲁棒性测试：** 通过向奖励模型注入柯西分布（Cauchy）噪声，模拟极端离群值环境，测试了不同污染比例下的性能。
*   **充分性评价：** 实验设计较为全面，不仅覆盖了纯文本推理，还扩展到了具身智能领域，验证了算法的通用性。

### 6. 主要结论与发现
*   **性能提升：** 在数学基准测试中，ARE 显著优于 GRPO 基线，例如在 AMC23 上，Qwen2.5-1.5B 的准确率从 22.5% 提升至 32.5%。
*   **训练稳定性：** 在注入重尾噪声的情况下，标准 GRPO 往往失效，而 ARE 表现出极强的抗干扰能力，训练曲线更平滑。
*   **泛化能力：** 在 VLN 任务中，ARE 在未见过的环境（Unseen environments）中表现出更好的导航成功率和更低的误差。

### 7. 优点
*   **理论与实践结合：** 不仅提出了算法，还提供了详尽的统计学证明（渐近理论）。
*   **解决现实痛点：** 针对 RLHF/RL 训练中常见的“奖励模型不可靠”问题提供了切实可行的鲁棒估计方案。
*   **结构化交互：** DACR 协议通过强制性的“批判”环节，有效利用了多智能体间的对抗与协作。

### 8. 不足与局限
*   **计算开销：** ARE 涉及多次迭代优化（GNC-IRLS），相比简单的批次均值计算，增加了训练时的计算负担。
*   **VLN 实验限制：** 受限于算力，VLN 任务中使用了较小的模型骨干（Qwen2-VL-2B），且尚未在 VLN 场景中完全实现多智能体交互（仅验证了 ARE 估计器）。
*   **超参数敏感性：** 尽管 ARE 是自适应的，但块的数量 $k$ 和 GNC 的步长等参数仍可能影响最终效果。

（完）
