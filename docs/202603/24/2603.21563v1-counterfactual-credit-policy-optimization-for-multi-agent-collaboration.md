---
title: Counterfactual Credit Policy Optimization for Multi-Agent Collaboration
title_zh: 面向多智能体协作的反事实信用策略优化
authors: "Zhongyi Li, Wan Tian, Yikun Ban, Jinju Chen, Huiming Zhang, Yang Liu, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21563v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 用于解决推理任务的协作多智能体LLM强化学习
tldr: 针对多智能体大语言模型协作中的信用分配难题，本文提出反事实信用策略优化（CCPO）框架。该框架通过构建动态反事实基准来估算每个智能体的边际贡献，从而提供角色敏感的学习信号。结合全局历史感知归一化方案，CCPO 有效解决了全局奖励导致的更新方差大和“搭便车”问题，在数学和逻辑推理任务中显著提升了协作性能。
selection_source: fresh_fetch
motivation: 协作式多智能体系统中共享的全局奖励难以区分个体贡献，导致训练方差大且容易出现“搭便车”现象。
method: 提出 CCPO 框架，通过模拟移除特定智能体贡献后的反事实轨迹来估算边际贡献，并利用全局历史统计量进行优势归一化。
result: 在数学和逻辑推理基准测试中，CCPO 在顺序协作和投票协作拓扑下均优于现有的多智能体强化学习基准。
conclusion: CCPO 为协作式大语言模型训练提供了更精细、有效的信用分配机制，显著提升了多智能体系统的推理能力和稳定性。
---

## 摘要
协作式多智能体大语言模型（LLM）可以通过分解角色和聚合多样化假设来解决复杂的推理任务。然而，此类系统的强化学习（RL）常受到信用分配问题的困扰：共享的全局奖励掩盖了个人贡献，导致更新方差增大并诱发“搭便车”行为。我们提出了反事实信用策略优化（CCPO），这是一个通过反事实轨迹估计每个智能体的边际贡献，从而分配特定于智能体的学习信号的框架。CCPO 构建了动态反事实基准，模拟移除某个智能体贡献后的结果，从而为策略优化产生角色敏感的优势值。为了进一步提高在异构任务和数据分布下的稳定性，我们提出了一种全局历史感知的归一化方案，利用全局展开统计数据来校准优势值。我们在两种协作拓扑上评估了 CCPO：顺序的“思考-推理”二元组和多智能体投票。在数学和逻辑推理基准测试中，CCPO 缓解了搭便车现象，并优于强大的多智能体 RL 基准模型，为协作式 LLM 训练提供了更细粒度且更有效的信用分配。我们的代码可在 https://github.com/bhai114/ccpo 获取。

## Abstract
Collaborative multi-agent large language models (LLMs) can solve complex reasoning tasks by decomposing roles and aggregating diverse hypotheses. Yet, reinforcement learning (RL) for such systems is often undermined by credit assignment: a shared global reward obscures individual contributions, inflating update variance and encouraging free-riding. We introduce Counterfactual Credit Policy Optimization (CCPO), a framework that assigns agent-specific learning signals by estimating each agent's marginal contribution through counterfactual trajectories. CCPO builds dynamic counterfactual baselines that simulate outcomes with an agent's contribution removed, yielding role-sensitive advantages for policy optimization. To further improve stability under heterogeneous tasks and data distributions, we propose a global-history-aware normalization scheme that calibrates advantages using global rollout statistics. We evaluate CCPO on two collaboration topologies: a sequential Think--Reason dyad and multi-agent voting. Across mathematical and logical reasoning benchmarks, CCPO mitigates free-riding and outperforms strong multi-agent RL baselines, yielding finer-grained and more effective credit assignment for collaborative LLM training. Our code is available at https://github.com/bhai114/ccpo.

---

## 论文详细总结（自动生成）

以下是对论文《Counterfactual Credit Policy Optimization for Multi-Agent Collaboration》（面向多智能体协作的反事实信用策略优化）的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：多智能体大语言模型（LLM）协作（如“思考者-执行者”流水线或多模型投票）在处理复杂推理任务时表现优异。
*   **核心问题**：协作式 LLM 的强化学习（RL）面临严重的**信用分配（Credit Assignment）**难题。由于通常只提供最终的团队奖励（如 0/1 正确性），很难确定哪个智能体对结果做出了贡献。
*   **负面影响**：简单的奖励共享会导致“搭便车”（Free-riding）现象，即表现差或冗余的智能体也能获得正向更新，导致训练方差大、收敛不稳定，甚至出现协作效果不如单模型独立运行的情况。

### 2. 方法论
*   **核心思想**：引入**反事实信用策略优化（CCPO）**框架。其核心逻辑是询问：“如果移除特定智能体 $i$ 的贡献，团队表现会如何？”通过对比“有无该智能体”的结果差异，估算其**边际贡献**。
*   **关键技术细节**：
    *   **边际贡献计算**：$\Delta_i = R_{joint} - R_{\neg i}$。其中 $R_{joint}$ 是联合奖励，$R_{\neg i}$ 是移除智能体 $i$ 后的反事实奖励。
    *   **反事实轨迹构建**：
        *   **顺序协作（Think-Solve）**：移除“思考者”的推理链，让“执行者”直接根据原始问题输出答案，以此作为反事实基准。
        *   **并行协作（Voting）**：在投票池中剔除智能体 $i$ 的答案，重新计算投票结果，无需额外解码，计算成本极低。
    *   **全局历史感知归一化**：使用指数移动平均（EMA）记录边际贡献的均值和方差，对优势值进行标准化处理，并使用 `tanh` 函数进行数值修剪，以稳定跨任务和训练阶段的学习信号。
    *   **GRPO 风格优化**：基于组相对策略优化（GRPO）框架，将计算出的反事实优势值应用于策略梯度更新。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：训练集使用 MATH 7.5k；测试集包括 MATH500（分布内）、AMC23、Gaokao2023en、MinervaMath（分布外/OOD）。
    *   **逻辑推理**：LogiQA 数据集。
*   **协作拓扑**：
    *   **双智能体顺序模式**：Thinker（生成思考过程）+ Solver（生成最终答案）。
    *   **三智能体并行模式**：三个模型独立生成答案并进行多数投票。
*   **对比基准（Benchmarks）**：
    *   单智能体 CoT（思维链）。
    *   共享奖励（Shared Reward）的多智能体 RL。
    *   **ReMA**：一种近期的多智能体元思考强化学习方法。
*   **基础模型**：Qwen2.5 (1.5B, 7B, 72B) 和 Llama3.1-8B。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：论文在 6.2.2 节进行了对比。以 Qwen2.5-1.5B 为例，CCPO 的训练时间约为 **15.24 小时**，比 ReMA（约 11.57 小时）增加了约 **31.7%**。这主要是因为顺序协作模式下需要额外的反事实采样。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 不同参数规模的模型，并在多个数学和逻辑基准上进行了验证。
*   **消融与分析实验**：
    *   验证了协作机制的有效性（对比有无思考过程的 Solver 表现）。
    *   可视化了奖励分布（Box plots），直观展示了反事实奖励如何比共享奖励更合理地分配信用。
    *   进行了分布外（OOD）泛化性测试。
*   **评价**：实验设计较为充分，涵盖了异构（顺序）和同构（投票）两种主流协作模式，对比了强基准模型，结果具有较好的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：CCPO 在所有数学和逻辑基准测试中均优于共享奖励基准和 ReMA。例如，在 MATH500 上，基于 Qwen2.5-1.5B 的协作系统准确率从 51.6% 提升至 61.0%。
*   **抑制搭便车**：通过反事实分析，CCPO 能识别出贡献为负或冗余的智能体，并给予零或负的信用反馈，从而强制每个智能体学习互补的技能。
*   **稳定性**：全局历史归一化有效缓解了强化学习在长离散序列生成中的高方差问题。

### 7. 优点
*   **理论严谨**：提供了关于反事实基准作为动作无关基准（Action-independent baseline）的数学证明，证明其在不引入梯度偏差的前提下能显著降低方差。
*   **角色敏感**：能够根据智能体在特定拓扑中的角色（如思考者 vs 执行者）自动调整学习信号。
*   **计算效率优化**：在投票模式下实现了“零成本”反事实计算，在顺序模式下通过复用采样降低了开销。

### 8. 不足与局限
*   **计算开销**：在顺序协作模式下，为了获得反事实基准，仍需进行额外的模型推理，这在超大规模模型训练时会增加成本。
*   **拓扑限制**：目前主要针对简单的顺序和投票拓扑，对于更复杂的多轮对话或动态交互图（Interaction Graphs）的信用分配尚未深入探讨。
*   **奖励稀疏性**：虽然改进了信用分配，但仍依赖于最终的 0/1 奖励，未结合过程监督（Process-level rewards）来进一步细化信用分配。

（完）
