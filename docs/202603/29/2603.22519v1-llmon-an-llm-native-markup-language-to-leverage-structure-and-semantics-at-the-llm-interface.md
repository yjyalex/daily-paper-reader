---
title: "LLMON: An LLM-native Markup Language to Leverage Structure and Semantics at the LLM Interface"
title_zh: LLMON：一种利用 LLM 接口结构与语义的 LLM 原生标记语言
authors: "Michael Hind, Basel Shbita, Bo Wu, Farhan Ahmed, Chad DeLuca, Nathan Fulton, David Cox, Dan Gutfreund"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22519v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 用于大模型结构化输入输出的标记语言
tldr: 本研究针对大语言模型（LLM）纯文本接口无法区分指令与数据、易导致语义混淆及提示词注入攻击的问题，提出了LLM原生标记语言LLMON（LLM Object Notation）。LLMON通过在输入中显式标注结构和语义元数据，使模型能更清晰地理解任务背景。该语言可应用于模型训练、提示工程及推理实现，有效提升了模型的准确性、安全性和鲁棒性，为构建更可靠的LLM交互界面提供了新方案。
selection_source: fresh_fetch
motivation: 现有的LLM纯文本接口难以区分指令与数据，导致模型容易产生语义混淆并面临提示词注入等安全风险。
method: 提出了一种名为LLMON的LLM原生标记语言，用于向模型传递文本的结构化信息和语义元数据。
result: 初步实验表明，LLMON在模型训练和推理阶段的应用能显著提高模型的任务准确性、安全性和防御攻击的能力。
conclusion: LLMON通过引入结构化语义标注，为优化LLM的交互界面、提升模型性能及安全性开辟了新的研究方向。
---

## 摘要
文本大语言模型 (LLM) 提供了一个简单且熟悉的接口：输入和输出均使用文本字符串。然而，传达给 LLM 的信息通常具有更丰富的结构和语义，而这些在字符串中并未体现。例如，大多数提示词 (prompt) 既包含指令（如“将这篇论文总结成一段话”）又包含数据（如待总结的论文），但在传递给模型时通常不作区分。这可能导致模型混淆和安全风险，例如提示词注入攻击。本研究通过引入一种 LLM 原生标记语言 LLMON (LLM Object Notation，发音为 “Lemon”) 来解决这一缺陷，该语言能够以自然的方式将文本的结构和语义元数据传达给 LLM。这些信息随后可用于模型训练、模型提示和推理实现，从而提高模型的准确性、安全性和可靠性。这类似于编程语言中的类型可用于多种用途，如静态检查、代码生成、动态检查和 IDE 高亮显示。我们讨论了 LLM 原生标记语言的一般设计要求，介绍了 LLMON 标记语言并展示了它如何满足这些设计要求，描述了 LLMON 产物中包含的信息如何使模型训练和推理实现受益，并为这两个用例提供了一些初步的实证证据。我们还讨论了 LLM 原生方法所带来的更广泛的问题和研究机会。

## Abstract
Textual Large Language Models (LLMs) provide a simple and familiar interface: a string of text is used for both input and output. However, the information conveyed to an LLM often has a richer structure and semantics, which is not conveyed in a string. For example, most prompts contain both instructions ("Summarize this paper into a paragraph") and data (the paper to summarize), but these are usually not distinguished when passed to the model. This can lead to model confusion and security risks, such as prompt injection attacks.   This work addresses this shortcoming by introducing an LLM-native mark-up language, LLMON (LLM Object Notation, pronounced "Lemon"), that enables the structure and semantic metadata of the text to be communicated in a natural way to an LLM. This information can then be used during model training, model prompting, and inference implementation, leading to improvements in model accuracy, safety, and security. This is analogous to how programming language types can be used for many purposes, such as static checking, code generation, dynamic checking, and IDE highlighting.   We discuss the general design requirements of an LLM-native markup language, introduce the LLMON markup language and show how it meets these design requirements, describe how the information contained in a LLMON artifact can benefit model training and inference implementation, and provide some preliminary empirical evidence of its value for both of these use cases. We also discuss broader issues and research opportunities that are enabled with an LLM-native approach.

---

## 论文详细总结（自动生成）

### 论文总结：LLMON —— 提升 LLM 接口结构与语义的原生标记语言

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）主要采用纯文本字符串作为交互接口，这种“扁平化”的输入方式存在严重缺陷：
*   **指令与数据混淆：** 模型无法从本质上区分“操作指令”和“待处理数据”，导致模型容易受到**提示词注入攻击**（Prompt Injection）。
*   **缺乏引用结构：** 在长文本或多轮对话中，模型难以精确指代特定的文档或片段（如“总结‘那个’文档”）。
*   **不可移植与非确定性：** 针对某一模型优化的提示词难以迁移，且缺乏类似传统编程的错误检查和模块化能力。
*   **线性数据流限制：** 所有的 Token 生成都依赖于之前的所有 Token，敏感信息容易污染后续生成。

**研究动机：** 借鉴编程语言的“类型系统”和操作系统的“代码/数据段分离”思想，为 LLM 设计一种原生的结构化标记语言，以提升模型的准确性、安全性和可维护性。

#### 2. 论文提出的方法论：LLMON
论文提出了 **LLMON (LLM Object Notation)**，其核心思想是通过显式的语义标签（Tags）和结构化语法来增强 LLM 的输入输出。

*   **核心设计要求：** 支持用户定义标签、命名实例（锚点）、显式嵌套名称（如 `email.header.from`）、前缀标注（在内容前声明类型）、以及与 JSON 的双向转换。
*   **双重语法体系：**
    *   **Human LLMON：** 易于人类阅读和编写的简洁语法，例如使用 `\instr\ 总结文本 /instr/`。
    *   **Machine LLMON：** 针对模型优化的形式，将标签转换为一组**特殊 Token**（如 `<|open|>`, `<|close|>`, `<|:|>`, `<|.|>`）。这些 Token 在分词阶段被视为原子单位，确保结构边界不被破坏。
*   **应用机制：**
    *   **模型训练：** 在监督微调（SFT）阶段使用 LLMON 格式的数据，让模型学习指令、数据和执行绑定（`exec`）之间的逻辑关系。
    *   **推理控制：** 在推理时利用标签边界实施**约束掩码（Constrained Masking）**。例如，在执行特定指令时，通过注意力掩码强制模型只关注该指令及其对应的数据段，从而彻底杜绝干扰信息或注入攻击。

#### 3. 实验设计
*   **数据集：** 使用了大规模公共指令微调数据（包括 Cleaned Alpaca, Dolly 等），并将其转换为 LLMON 格式。总规模达到 **340 万个结构化样本，约 29 亿个 Tokens**。
*   **Benchmark：**
    *   **自定义 Distractor Benchmark：** 包含 100 个手动构造的复杂案例，每个案例包含多个干扰指令，测试模型是否能根据 `exec` 标签准确执行被选中的指令。
    *   **通用能力测试：** MMLU（知识）、GSM8K（数学）、IFEval（指令遵循），用于验证引入结构化标注是否会损害模型的通用性能。
*   **对比方法：**
    *   **Base 模型：** 未经微调的原始模型。
    *   **传统 Chat 模板微调：** 使用标准角色（User/Assistant）标注的 Baseline。
    *   **LLMON 微调：** 分别测试了全量微调（Full Fine-tuning）和 LoRA 适配。

#### 4. 资源与算力
*   **模型规模：** 实验采用了 **Granite-4.0-Micro-Base (3B)** 和 **Qwen2.5-3B** 作为骨干模型。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。仅提到训练了不同规模的 Token 子集（从 241M 到 2.9B Tokens）以观察缩放效应。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多维度实验，包括不同模型架构（Granite vs Qwen）、不同微调策略（全量 vs LoRA）、不同数据规模（4 个梯度）以及训练侧与推理侧的分别验证。
*   **充分性评价：** 实验设计较为充分且客观。通过引入“干扰项”测试，精准捕捉了传统纯文本接口的弱点。同时，通过通用 Benchmark 证明了该方法在提升安全性的同时，并未产生严重的“灾难性遗忘”或性能退化。

#### 6. 主要结论与发现
*   **显著提升执行控制：** 在 Distractor Benchmark 上，经过 LLMON 训练的模型在指令选择准确率上比传统微调模型平均提升了 **74.2 个百分点**。
*   **推理侧防御有效：** 即使不进行重新训练，仅在推理阶段应用 LLMON 边界掩码，也能将准确率提升 **29.3 个百分点**。
*   **通用能力保持：** 模型在 MMLU 和 IFEval 等标准任务上的表现与 Baseline 持平，证明结构化标注是“无损”的增强。
*   **LoRA 适用性：** 结构化行为可以通过参数高效的 LoRA 学习，但全量微调在处理复杂执行语义时表现更稳定。

#### 7. 优点
*   **安全性突破：** 为防御提示词注入提供了一种底层架构级别的解决方案，而非仅仅依赖于提示词工程。
*   **语义清晰度：** 引入了类似编程语言的引用机制（命名实例），解决了长上下文中的指代模糊问题。
*   **兼容性：** 提供了与 JSON 的转换器，能够无缝集成现有的结构化数据。
*   **双语法设计：** 兼顾了人类开发者的易用性和模型处理的精确性。

#### 8. 不足与局限
*   **命名空间治理：** 随着用户自定义标签增多，可能出现标签冲突或语义重复（如 `poem` vs `poetry`），需要一套治理机制。
*   **Tokenizer 依赖：** 依赖于向分词器添加特殊 Token，这在某些闭源模型或固定接口的 API 上难以实施。
*   **推理开销：** 虽然掩码技术有效，但在超大规模并发推理中，解析 LLMON 标签并动态调整注意力掩码的计算开销尚未得到详细评估。
*   **预训练缺失：** 目前仅在微调阶段引入，若能在预训练阶段就引入此类结构化数据，效果可能会更强。

（完）
