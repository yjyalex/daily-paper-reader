---
title: Why Do LLM-based Web Agents Fail? A Hierarchical Planning Perspective
title_zh: 基于大语言模型（LLM）的网络智能体为何失败？分层规划视角
authors: "Mohamed Aghzal, Gregory J. Stein, Ziyu Yao"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14248v1"
tags: ["query:mas-gen"]
score: 8.0
evidence: 用于分析网络智能体任务分解的分层规划框架
tldr: 本研究针对 LLM 网络智能体在复杂长程任务中可靠性不足的问题，提出了一个分层规划框架，从高层规划、底层执行和重新规划三个维度对失败原因进行深入分析。通过对比 PDDL 和自然语言规划，研究发现虽然结构化规划能产生更优策略，但底层执行阶段的感知接地和自适应控制才是制约性能的核心瓶颈。该框架为诊断和改进网络智能体提供了系统性的方法论。
selection_source: fresh_fetch
motivation: 现有的评估方法主要关注端到端的成功率，无法深入揭示 LLM 网络智能体在处理复杂任务时具体的失败环节。
method: 提出一种分层规划框架，将智能体行为分解为高层规划、底层执行和重新规划，并对比了 PDDL 与自然语言规划的表现。
result: 实验表明 PDDL 规划在策略制定上优于自然语言，但底层执行阶段的感知与控制能力是导致任务失败的主要瓶颈。
conclusion: 要实现人类水平的可靠性，网络智能体不仅需要强大的高层推理能力，更需在感知接地和自适应控制方面取得突破。
---

## 摘要
基于大语言模型（LLM）的网络智能体越来越多地被用于网页导航，但在现实的长程任务中，其可靠性仍远低于人类。现有的评估主要关注端到端的成功率，对于失败发生的原因提供的见解有限。我们提出了一个分层规划框架，从三个层面（即高层规划、底层执行和重新规划）分析网络智能体，从而实现对推理、接地（grounding）和恢复能力的基于过程的评估。我们的实验表明，结构化的规划领域定义语言（PDDL）规划比自然语言（NL）规划能产生更简洁且目标导向的策略，但底层执行仍然是主要的瓶颈。这些结果表明，要达到人类水平的可靠性，不仅需要提高高层推理能力，改进感知接地和自适应控制也至关重要。这种分层视角为诊断和推进 LLM 网络智能体提供了原则性的基础。

## Abstract
Large language model (LLM) web agents are increasingly used for web navigation but remain far from human reliability on realistic, long-horizon tasks. Existing evaluations focus primarily on end-to-end success, offering limited insight into where failures arise. We propose a hierarchical planning framework to analyze web agents across three layers (i.e., high-level planning, low-level execution, and replanning), enabling process-based evaluation of reasoning, grounding, and recovery. Our experiments show that structured Planning Domain Definition Language (PDDL) plans produce more concise and goal-directed strategies than natural language (NL) plans, but low-level execution remains the dominant bottleneck. These results indicate that improving perceptual grounding and adaptive control, not only high-level reasoning, is critical for achieving human-level reliability. This hierarchical perspective provides a principled foundation for diagnosing and advancing LLM web agents.

---

## 论文详细总结（自动生成）

### 基于分层规划视角的 LLM 网络智能体失败原因分析总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
尽管大语言模型（LLM）在推理和指令遵循方面取得了显著进步，但在处理真实、动态的网页导航任务时，其可靠性仍远低于人类。现有的评估方法大多依赖“端到端成功率”这一粗粒度指标，无法揭示失败的具体根源：是任务理解错误、高层策略失误、底层 UI 操作无法落地，还是在环境变化时缺乏恢复能力？本研究旨在通过**分层规划（Hierarchical Planning）**的视角，系统性地拆解并诊断 LLM 网络智能体的各项核心能力。

#### 2. 论文提出的方法论：核心思想与关键技术
论文提出了一个三层评估框架，将网络智能体的行为解构为：
*   **高层规划（High-level Planning）：** 将自然语言指令分解为一系列抽象子目标（Subgoals）。研究对比了**自然语言（NL）**和**规划领域定义语言（PDDL）**两种表示形式。
*   **底层执行（Low-level Execution）：** 将每个子目标转化为具体的网页交互动作序列（如点击、滚动、输入）。
*   **后置条件检查与重规划（Postcondition Checking & Replanning）：** 
    *   使用 LLM 作为评判者（LLM-as-judge）检查子目标是否达成。
    *   若失败，触发重规划机制，决定是局部调整（从上个成功点继续）还是全局重规划（推倒重来）。
*   **关键技术细节：** 引入了 PDDL 结构化表示来约束规划逻辑，并定义了多种对齐指标（如 Perfect Match, Decomposed, Unmatched 等）来量化高层规划与人类专家路径的相似度。

#### 3. 实验设计
*   **数据集/场景：** 使用了 **Mind2Web-Live** 基准测试集，包含 104 个在真实、实时网站上执行的任务。
*   **对比模型：** 评估了三个主流模型：**gpt-5-nano**、**claude-haiku-4.5** 和 **gemini-flash-2.5**。
*   **动作空间对比：** 比较了“扩展动作空间”（包含 google_search, goto 等抽象动作）与“原始动作空间”（仅限 click, type 等基础 UI 操作）。
*   **Benchmark：** 以人类专家标注的“关键节点（Key-node）”作为金标准高层计划。

#### 4. 资源与算力
论文**未明确说明**具体的算力消耗（如 GPU 型号、数量或训练时长）。由于实验主要基于商业模型的 API（GPT-5, Claude, Gemini）进行推理和评估，其核心开销在于 API 调用成本而非本地模型训练。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 104 个任务，在 3 个模型上分别进行了高层规划对齐、底层执行可靠性、重规划有效性等多维度实验。
*   **充分性与客观性：** 实验设计较为全面，涵盖了从规划到执行的全链路。通过引入“LLM-as-judge”并进行了人工验证（一致性达 82%-86%），确保了评估的客观性。对比了不同模型和不同动作表示形式，实验结果具有较好的代表性。

#### 6. 论文的主要结论与发现
*   **底层执行是核心瓶颈：** 即使提供完美的高层计划，LLM 在将其转化为底层 UI 动作时仍表现不佳（成功率仅约 36%）。失败主因是**感知接地（Perceptual Grounding）**不足，如幻觉链接、重复无效操作等。
*   **PDDL 优于自然语言：** 使用结构化的 PDDL 生成的高层计划比自然语言更简洁、目标导向，且更接近人类专家的逻辑。
*   **重规划显著提升性能：** 仅需一轮基于环境反馈的重规划，就能大幅提高子目标的完成率和最终任务成功率。
*   **模型差异：** gpt-5-nano 表现最优；gemini-flash-2.5 在底层执行上最为吃力，存在大量冗余动作。

#### 7. 优点：亮点与创新
*   **诊断性视角：** 突破了单一的成功率评估，提供了细粒度的过程评估框架，能够精准定位“病灶”。
*   **引入形式化方法：** 首次将 PDDL 这种经典的符号规划语言引入 LLM 网络智能体的评估，证明了结构化约束的价值。
*   **错误分类详尽：** 对底层执行的失败模式（如幻觉、冗余、跨域导航）进行了深入的定量分析。

#### 8. 不足与局限
*   **样本量限制：** 104 个任务虽然在实时环境下具有挑战性，但样本规模相对较小。
*   **模态局限：** 目前主要基于文本 DOM 表示，未充分探讨多模态（视觉）输入对感知接地的潜在改进。
*   **依赖评判模型：** 评估过程高度依赖 gpt-5-nano 作为 Judge，可能存在模型偏见或评估误差。
*   **人工标注成本：** 高层规划的对齐评估依赖专家标注，难以在大规模未标注数据上快速扩展。

（完）
