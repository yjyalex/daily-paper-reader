Title: Top-b: Entropic Regulation of Relative Probability Bands in Autoregressive Language Processes

URL Source: https://arxiv.org/pdf/2603.14567v1

Published Time: Tue, 17 Mar 2026 01:46:31 GMT

Number of Pages: 11

Markdown Content:
Preprint. Under review. 

# Top-b: Entropic Regulation of Relative Probability Bands in Autoregressive Language Processes 

Deepon Halder 

AI4Bharat IIEST Shibpur 

deeponh.2004@gmail.com 

Raj Dabre 

IIT Madras AI4Bharat 

## Abstract 

Probabilistic language generators are theoretically modeled as discrete stochastic processes, yet standard decoding strategies (Top-k, Top-p)impose static truncation rules that fail to accommodate the dynamic information density of natural language. This misalignment often forces a suboptimal trade-off: static bounds are either too restrictive for high-entropy creative generation or too permissive for low-entropy logical reasoning. In this work, we formalize the generation process as a trajectory through a relative probability manifold. We introduce Top-b 

(Adaptive Relative Band Sampling), a decoding strategy that regulates the candidate set via a dynamic bandwidth coefficient coupled strictly to the instantaneous Shannon entropy of the model’s distribution. We provide a theoretical framework demonstrating that Top-b acts as a variance-minimizing operator on the tail distribution. Empirical validation on GPQA and GSM8K benchmarks indicates that Top-b significantly reduces generation entropy and inter-decoding variance while maintaining competitive reasoning accuracy, effectively approximating a self-regulating control system for autoregressive generation. 

## 1 Introduction 

We formulate the problem of autoregressive text generation as the realization of a discrete stochastic process Y = {Yt}∞

> t=1

. In this framework, a stochastic process is simply a sequence of random variables where the outcome of the current step depends on the history of previous steps. Modern Large Language Models (LLMs) function as estimators of the conditional probability distribution P(Yt | Y<t), assigning a probability mass to every possible token in a vocabulary V.The central challenge in decoding is transforming this continuous probability distribution into a single discrete choice yt. This requires a decision rule, or decoding strategy , that balances two competing objectives: fidelity (adhering to the model’s high-confidence predictions) and 

diversity (allowing for variability in expression). Standard stochastic strategies, such as Top-k (Fan et al., 2018) and Top-p (Nucleus) (Holtz-man et al., 2020), employ static truncation . They restrict the sampling pool (the set of valid next tokens) using fixed hyperparameters, either a fixed count k or a fixed cumulative probability mass p. This static approach is theoretically suboptimal because the uncertainty 

of the language process is non-stationary; it fluctuates wildly over time. • In low-entropy states (e.g., solving an arithmetic equation), the distribution is peaked, meaning most probability mass is concentrated on one or two tokens. Here, 1

> arXiv:2603.14567v1 [cs.CL] 15 Mar 2026

Preprint. Under review. 

Figure 1: Structural comparison of static cumulative truncation (Top-p) versus entropy-regulated relative thresholding (Top-b). (a) In low-entropy reasoning regimes, Top-p admits a long tail of low-probability distractor tokens, increasing the risk of logical incoherence. (b) 

In high-entropy creative regimes, Top-p’s static cumulative threshold arbitrarily truncates viable tokens, artificially restricting diversity. In contrast, Top-b establishes a dynamic probability band anchored to the distribution mode ( pmax ). (c) Under low entropy, the Top-b

bandwidth strictly contracts to prune the distractor tail and enforce deterministic reasoning. 

(d) Under high entropy, the bandwidth expands to safely retain linguistic diversity. This illustrates how Top-b continuously adapts its sampling support to the local information density of the language process. a wide static threshold (like p = 0.9) is too permissive, allowing the inclusion of tail events , low-probability outcomes that, in this context, manifest as logical errors or hallucinations. • In high-entropy states (e.g., beginning a creative story), the distribution is flat or uniform. Here, a static threshold might be too restrictive, artificially truncating valid creative options. The primary contributions of this work are as follows: • Entropy-regulated decoding. We introduce Top-b , a decoding strategy that de-fines the sampling support through a relative probability band anchored to the distribution mode and dynamically scaled by the normalized Shannon entropy. • Variance-minimizing interpretation. We analyze the structural behavior of Top-b and argue that entropy-scaled relative thresholding acts as a variance-minimizing operator on the tail distribution, stabilizing the generation trajectory in low-entropy reasoning regimes. • Empirical validation on reasoning benchmarks. Experiments on GPQA and GSM8K demonstrate that Top-b achieves competitive or improved accuracy while 2Preprint. Under review. consistently producing lower generation entropy and significantly reduced variance across decoding runs compared with Top-k, Top-p, Min-p, and entropy-aware baselines. 

## 2 Related Work 

The landscape of autoregressive decoding has shifted from fixed heuristics toward adaptive, information-theoretic constraints. We categorize these developments into static truncation, entropy-driven sampling, and relative thresholding. 

2.1 Static and Distribution-Agnostic Truncation 

Standard decoding strategies such as Top-k (Fan et al., 2018) and Top-p (Nucleus) sampling (Holtzman et al., 2020) rely on static hyperparameters to prune the long tail of the distribu-tion. While Top-k limits the support size |S| to a constant, Top-p allows the support size to vary such that the cumulative mass ∑y∈S P(y) exceeds a threshold p. However, both methods are distribution-agnostic in their ”strictness”; a static p = 0.9 may be too restrictive for high-entropy creative tasks and too permissive for low-entropy logical reasoning, where the remaining 0.1 mass may consist of thousands of ”distractor” tokens in large vocabularies. 

2.2 Entropy-Aware and Information-Theoretic Sampling 

Recent work has sought to couple the truncation process to the model’s internal uncertainty. 

Typical Sampling (Meister et al., 2023) selects tokens whose information content is close to the conditional Shannon entropy, grounded in the property of the Typical Set. Mirostat 

(Basu et al., 2021) utilizes a feedback loop to maintain a constant target cross-entropy, effectively treating decoding as a control system problem. More recently, Eta Sampling (Hewitt et al., 2022) and Epsilon Sampling introduce simple probability bounds, but often fail to account for the relative distance between the mode and the tail. REAL (Residual Entropy-Aware Length) (Zhao et al., 2024) and p-less sampling (Liu et al., 2024) utilize R ´enyi entropy and collision entropy to achieve hyperparameter-free truncation. Top-b extends this lineage by utilizing the normalized Shannon entropy to modulate the relative width of the probability band, rather than just the absolute probability mass. 

2.3 Relative Thresholding and Min-p 

The most direct predecessor to our work is Min-p sampling (Grimstad & Jullum, 2024), which introduces a threshold τ = α · pmax . Min-p ensures that a token is only sampled if its likelihood is within a fixed fraction of the most likely token. While Min-p is more robust than Top-p to changes in vocabulary size, its scaling factor α remains a static hyperparameter. Top-b generalizes Min-p by formalizing α as a dynamic function of the local state manifold. Specifically, while Min-p assumes a constant tolerance for noise, Top-b posits that the ”band-width of viability” should expand or contract based on the instantaneous unpredictability (ηt) of the stochastic process. This bridges the gap between the precision of greedy search (H → 0) and the exploratory nature of random walks ( H → Hmax ). 

2.4 Theoretical Stability in Reasoning 

The pursuit of stability in Chain-of-Thought (CoT) reasoning has led to strategies like 

Self-Consistency (Wang et al., 2023) and Best-of-N sampling. These methods mitigate variance post-generation. In contrast, Top-b acts as an online variance-minimizing operator. By pruning the tail distribution proportional to the model’s confidence, Top-b minimizes the ”branching factor” of errors during autoregressive generation, preventing the accumulation of drift in long-context reasoning tasks. 3Preprint. Under review. 

## 3 Formalism of Language Processes 

We analyze the generation process through the lens of information theory. 

3.1 The Discrete Language Process 

Let V be a finite vocabulary of size |V | . We define the language process as a sequence of random variables Y1, Y2, . . . , where each Yt takes a value y ∈ V . The behavior of the system at time t is fully described by the conditional probability vector pt:

pt(y) = P(Yt = y | Y<t = y<t) (1) This vector represents the model’s estimate of the likelihood of every possible next word, given the history y<t.

3.2 Quantifying Uncertainty and The Mode 

To govern the sampling process, we must quantify the model’s confusion at any given step. We utilize Shannon Entropy (H), a measure of the unpredictability of the state: 

H(pt) = − ∑

> y∈V

pt(y) log pt(y) (2) A low H(pt) implies the model is confident (the distribution is sharp/peaked). A high 

H(pt) implies the model is uncertain (the distribution is flat/uniform). We also identify the Mode of the distribution, mt, which is simply the single most likely token: 

mt = arg max  

> y∈V

pt(y), with probability pmax = pt(mt) (3) The mode serves as our anchor point. In our framework, the validity of any other token y is determined by comparing its probability pt(y) against the anchor pmax .

## 4 Locally Adaptive Relative Sampling (Top-b) 

We introduce the concept of an Entropy-Regulated Probability Band . The fundamental hypothesis is that the tolerance for deviating from the mode should be inversely proportional to the model’s confidence. 

4.1 Defining the Dynamic Support 

In probability theory, the support of a distribution is the set of outcomes with non-zero probability. In decoding, we define a truncated support Sb,t, which is the subset of the vocabulary we actually consider for sampling. Let pi denote the probability assigned to the i-th token and let 

pmax = max  

> i

pi.We define a dynamic threshold 

t = pmax · (1 − bandwidth t).Only tokens satisfying pi ≥ t are considered during sampling. 

Definition 1 (Top-b Support Set) . Let bandwidth t ∈ (0, 1 ] be the adaptive bandwidth coefficient. The Top-b support set Sb,t consists of all tokens y satisfying: 

Sb,t = {y ∈ V | pt(y) ≥ (1 − bandwidth t) · pmax }. (4) Here, (1 − bandwidth t) acts as a relative threshold. If the bandwidth is small, the threshold is close to pmax and only tokens nearly as probable as the best one are retained. If the bandwidth increases, the threshold decreases, allowing more tokens into the support. 4Preprint. Under review. 

4.2 Adaptive Bandwidth via Entropy 

Let the probability distribution over tokens be p = {pi} and let the Shannon entropy be 

H(p) = − ∑

> i

pi log pi.Let 

Hmax = log (|V | ),the maximum possible entropy (achieved under a uniform distribution). We define the adaptive bandwidth as bandwidth t = base bandwidth ×



1 + H(p)

Hmax 



, (5) where base bandwidth ∈ (0, 1 ) is a fixed hyperparameter. This implies: 1. When H → 0, bandwidth t → base bandwidth. 2. When H → Hmax ,bandwidth t → 2 · base bandwidth. Thus the bandwidth scales linearly with normalized entropy, increasing the admissible support as uncertainty grows. 

4.3 Theoretical Behavior 

This formulation ensures the decoding strategy adapts smoothly to the entropy of the probability landscape: 

Proposition 1 (Entropy-Scaled Constraints) . The Top-b mechanism exhibits the following asymp-totic behaviors: 1. Low-Entropy Regime ( H → 0): The bandwidth approaches base bandwidth . The thresh-old becomes t ≈ pmax (1 − base bandwidth ),

restricting sampling to high-probability tokens near the mode. This behavior approximates greedy decoding when the distribution is sharp. 2. High-Entropy Regime ( H → Hmax ): The bandwidth increases to 2 · base bandwidth . The threshold decreases accordingly: t ≈ pmax (1 − 2 · base bandwidth ).

The support expands, encouraging diversity as the distribution becomes flatter. 

## 5 Experiments 

We evaluate the hypothesis that Top-b acts as a stabilizer for the language process on high-reasoning tasks. We utilize google/gemma-3-4b-it as the underlying stochastic process generator. 

5.1 Experimental Settings 

We compare against: • Stochastic Baselines: Top-k ( k = 40), Top-p ( p = 0.9), Min-p ( α = 0.05). • Entropic Baselines: Eta Sampling . Datasets include GPQA (graduate-level reasoning) and GSM8K (arithmetic reasoning). 5Preprint. Under review. Table 1: GPQA accuracy and average entropy. Top-b minimizes average entropy, indicating a reduction in generation stochasticity and confusion. 

Method Accuracy Avg Entropy Top-b (Ours) 0.2285 0.1579 

Eta 0.2099 0.1592 Min-p 0.2199 0.1714 Epsilon 0.2389 0.1712 Top-p 0.2132 0.1764 Temperature 0.2121 0.1774 Top-k 0.2333 0.1815 

## 6 Results 

6.1 GPQA Evaluation 

Table 1 details the accuracy and average sequence entropy across decoding strategies. Top-b achieves a competitive accuracy of 0.2285 while maintaining the statistically lowest average entropy (0.1579). 

Implications of Entropy Minimization The statistically significant reduction in entropy ob-served with Top-b is non-trivial. In autoregressive generation, token-level entropy functions as a metric of decisiveness; elevated entropy correlates with diffuse probability mass and an increased likelihood of off-distribution generation. By explicitly minimizing entropy, Top-b compels the model to converge on sharper predictions. Unlike Top-p, which may preserve a broad candidate tail even under marginal uncertainty, Top-b imposes a principled constraint, anchoring generation to high-likelihood regions unless the underlying distribution topology explicitly necessitates expansion. (For concrete, token-level examples demonstrating this aggressive tail-pruning across various contexts, see Appendix D) 

Variance Reduction and Stability. 

Figure 2 illustrates the variance in performance across random seeds. Top-b exhibits the 

lowest variance , implying higher deterministic stability. This suggests that the method is robust to perturbations in token probabilities, a critical property for reproducible reasoning chains. By dampening fluctuations without sacrificing accuracy, Top-b offers a more reliable alternative to purely stochastic sampling. 

Entropy Dynamics: Top-b vs. Top-p. 

We analyzed the entropy trajectory across generated sequences (Figure 3). While initial dynamics are similar, Top-p sustains elevated entropy in the latter sequence segments due to its cumulative probability threshold, which admits wider candidate sets even as confidence should theoretically sharpen. In contrast, Top-b induces a monotonic reduction in entropy, frequently driving it toward zero in the terminal phase of generation. This confirms that Top-b effectively prunes divergent branches, minimizing the accumulation of uncertainty over long-context reasoning. 

Interaction with Temperature Scaling 

We investigated the coupling between the bandwidth parameter b and temperature T

(Figure 4). The relationship is non-monotonic; mid-range values ( b ≈ 0.30) maximize performance at higher temperatures ( T = 2.5). The results suggest that Top-b acts as a regularizer for high-temperature settings, suppressing the tail noise typically amplified by scaling T, provided b is not set so aggressively as to induce mode collapse. 6Preprint. Under review. 

Figure 2: Accuracy variance across random seeds on GPQA. Top-b exhibits the lowest vari-ance, indicating higher deterministic stability compared to Top-p and stochastic sampling methods. 

Figure 3: Entropy trajectory over generation steps for Top-b and Top-p sampling. Top-b induces a monotonic reduction in entropy, while Top-p maintains higher entropy in later stages due to broader candidate sets. 

6.2 GSM8K Evaluation 

Preliminary evaluation on GSM8K (1000 samples, CoT split) further validates these findings. 7Preprint. Under review. 

Figure 4: Interaction between bandwidth parameter b and temperature T on GPQA per-formance. Mid-range b values regularize high-temperature sampling, improving accuracy without inducing mode collapse. 

Method Average Accuracy Variance Top-b (Ours) 0.2680 1.136 × 10 −4

Top-p 0.2613 1.1207 × 10 −3

Top-k 0.2093 3.797 × 10 −4

Min-p 0.2427 2.380 × 10 −4

Table 2: Preliminary results on GSM8K demonstrating Top-b’s superior accuracy and variance reduction. As shown in Table 2, Top-b achieves the highest accuracy ( 0.2680 ) with an order-of-magnitude lower variance (1.136 × 10 −4) compared to Top-p. This supports the hypothesis that Top-b effectively filters arithmetic distractors, low-probability tokens that disrupt the logical flow of Chain-of-Thought reasoning. 

## 7 Discussion and Limitations 

Top-b represents a shift toward uncertainty-regulated decoding , where the model’s intrinsic confidence governs its generative freedom. 

The Expressivity-Stability Trade-off. While our primary focus was logical reasoning, the adaptive formulation of Top-b theoretically extends to creative generation. In creative do-mains characterized by inherently high entropy (flat distributions), Top-b naturally expands the sampling band, preserving expressivity without requiring manual hyperparameter relaxation. 

Computational Considerations. The primary overhead of Top-b is the computation of Shannon entropy at each decoding step. However, as this is a vector operation over V

(typically 32 k − 128 k), the latency is negligible relative to the O(N2) complexity of the attention mechanism. 8Preprint. Under review. 

## 8 Conclusion 

We have introduced Top-b , an adaptive sampling mechanism that dynamically bounds the search space based on normalized entropy. By coupling the decoding bandwidth to the model’s instantaneous uncertainty, Top-b mitigates the risks of static thresholding. Empirical results on GPQA and GSM8K demonstrate that Top-b minimizes sequence entropy and variance while maintaining competitive accuracy. This establishes Top-b not merely as a heuristic, but as a theoretically grounded method for enhancing the reliability of Large Language Model generation. 

## 9 Acknowledgments 

This research was conducted at AI4Bharat, IIT Madras. We acknowledge the support of the computing resources provided. 

## References 

Sourya Basu, Govardana Ramachandran, Anshul Nitin, Sandeep Sreekumar, and Sumit Hosamani. Mirostat: A neural text decoding algorithm that directly controls perplexity. 

arXiv preprint arXiv:2007.14966 , 2021. Abhimanyu Dubey, Akhil Jauhri, Abhinav Pandey, et al. The llama 3 herd of models. arXiv preprint arXiv:2407.21783 , 2024. Angela Fan, Mike Lewis, and Yann N. Dauphin. Hierarchical neural story generation. In 

Proceedings of the 56th Annual Meeting of the Association for Computational Linguistics (ACL) ,pp. 889–898, 2018. Bjørn Grimstad and Martin Jullum. Deterministic truncation for adaptive sampling. arXiv preprint arXiv:2401.15077 , 2024. John Hewitt, Christopher Reif, and Christopher D. Manning. Truncation sampling as language model smoothing. arXiv preprint arXiv:2210.15191 , 2022. Ari Holtzman, Jan Buys, Li Du, Maxwell Forbes, and Yejin Choi. The curious case of neural text degeneration. arXiv preprint arXiv:1904.09751 , 2020. Zeyu Liu, Yisong Wang, and Xinyuan Chen. p-less: Hyperparameter-free adaptive sampling via collision entropy. arXiv preprint arXiv:2410.03456 , 2024. Clara Meister, Tiago Pimentel, Gian Wiher, and Ryan Cotterell. Locally typical sampling. 

Transactions of the Association for Computational Linguistics , 11:102–121, 2023. Xuezhi Wang, Jason Wei, Dale Schuurmans, et al. Self-consistency improves chain of thought reasoning in language models. In International Conference on Learning Representations (ICLR) , 2023. An Yang, Baosong Yang, Binyuan Hui, et al. Qwen2 technical report. arXiv preprint arXiv:2407.10671 , 2024. Junkai Zhao, Siheng Wu, and Yifan Zhang. Real: Residual entropy-aware truncation for deterministic decoding. arXiv preprint arXiv:2409.11234 , 2024. 

## A Future Work 

We intend to extend this evaluation to diverse architectures, including LLaMA (Dubey et al., 2024) and Qwen (Yang et al., 2024), and conduct scaling laws analysis across model sizes (2B to 405B). Future benchmarks will encompass multilingual tasks and complex mathematical reasoning to further stress-test the entropy-adaptation hypothesis. 9Preprint. Under review. 

## B Entropy-Induced Branching 

Token-level entropy can be theoretically modeled as a branching factor in the generation tree. High entropy regions represent nodes with multiple viable edges (continuations), whereas low entropy regions approximate linear paths. Top-b effectively prunes these high-entropy branches by enforcing a stricter relative probability band, collapsing the generation tree onto the path of maximum likelihood (Figure 5). This diagnostic perspective suggests that if alternative continuations persist under Top-b, they likely represent deeply memorized or strongly weighted priors within the model’s parameter space. 

Figure 5: Schematic of entropy-induced branching. Top-b acts as a pruning operator, collapsing diffuse branches into a single high-likelihood continuation. 

## C Proof of Maximum Entropy Bound 

Lemma 1. For a vocabulary V of size n, H max = log n. Proof. The Shannon entropy is strictly concave. It is maximized when the distribution is uniform, i.e., pi = 1 

> n

∀i.

H(p) = −

> n

# ∑

> i=1

1

n log 1

n = −



log 1

n

 n

# ∑

> i=1

1

n = log n.

## D Token-Level Pruning Case Studies 

To empirically demonstrate the theoretical behavior outlined in Section 4, we examine the next-token probability distributions and the resulting candidate sets across different decoding strategies. Table 3 presents the original token probabilities and the renormalized probabilities for Top-b, Top-p, Top-k, and Min-p across various semantic contexts. The data reveals a consistent pattern: standard cumulative thresholding (Top-p = 0.9) routinely over-admits tokens in low-entropy regimes, whereas Top-b strictly collapses the distribution to the most logical continuations. • Arithmetic Reasoning (Low Entropy): For the prompt ”2+2=” , despite the model assigning 39.75% probability to the correct token (”4”), Top-p admits 14 total tokens into the candidate set, including ”5” and ”1”, introducing unnecessary stochastic risk. Top-b contracts its bandwidth and selects exactly 1 token (”4”). • Syntactic Continuation (Low Entropy): For the prompt ”You will pay for what you have done, she hissed... The battle that ensued” , the highly probable next token is ”was” (46.00%). Top-p admits an excessive 44 tokens into the sampling pool. Top-b again aggressively prunes the tail, selecting only 1 token. • Open-Ended Instruction (Moderate Entropy): For prompts requiring structural formatting (e.g., ”Describe a time...” ), the probability is distributed among valid structural tokens like newlines ( \n ) and first-person pronouns (”I”, ”You”). Here, Top-b correctly expands to include 2 to 3 valid tokens, whereas Top-p includes 25 to 43 tokens, many of which are irrelevant distractors. 10 Preprint. Under review. Table 3: Comparison of selected support sizes ( |S| ) and renormalized probabilities across different contexts. Top-b consistently isolates the high-probability manifold, severely re-stricting the distractor tail compared to Top-p.

Prompt Context Top Tokens Orig. Prob (%) Top-b Top-p Top-k Min-pPrompt: ”2+2=” 

Support Size ( |S| ) 1 token 14 tokens 5 tokens 9 tokens 

4 39.75 100.0 44.2 54.8 46.6 

5 15.50 - 17.2 21.5 18.3 

\n 7.81 - 8.7 10.8 9.2 

Prompt: ”A rainbow is an optically brilliant meteorological event resulting from refraction, reflection, and dispersion of” 

Support Size ( |S| ) 1 token 2 tokens 5 tokens 3 tokens 

light 59.50 100.0 73.0 61.8 62.8 

sunlight 21.88 - 27.0 22.8 23.1 

the 13.25 - - 14.0 14.0 

Prompt: ”You will pay for what you have done, she hissed, her blade flashing in the moonlight. The battle that ensued” 

Support Size ( |S| ) 1 token 44 tokens 5 tokens 4 tokens 

was 46.00 100.0 51.2 72.0 74.8 

left 6.25 - 6.9 9.8 10.1 

between 5.53 - 6.2 8.6 9.0 

Prompt: ”If you could help me write an email to my friends inviting them to dinner on Friday, it would be greatly appreciated.” 

Support Size ( |S| ) 3 tokens 43 tokens 5 tokens 14 tokens 

\n 23.88 52.9 26.5 41.5 31.0 

I 11.25 25.0 12.6 19.6 14.7 

\n\n 9.94 22.1 11.1 17.4 12.9 

Prompt: ”Describe a time when you had to make a difficult decision.” 

Support Size ( |S| ) 2 tokens 25 tokens 5 tokens 6 tokens 

\n 34.50 56.2 38.5 45.2 44.2 

You 27.00 43.8 30.1 35.2 34.5 

\n\n 8.75 - 9.7 11.5 11.2 11