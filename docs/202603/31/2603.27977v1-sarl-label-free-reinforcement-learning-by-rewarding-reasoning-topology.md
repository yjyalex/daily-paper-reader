---
title: "SARL: Label-Free Reinforcement Learning by Rewarding Reasoning Topology"
title_zh: SARL：通过奖励推理拓扑实现的无标签强化学习
authors: "Yifan Wang, Bolian Li, David Cho, Ruqi Zhang, Fanping Sui, Ananth Grama"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27977v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过奖励推理拓扑结构的无标签强化学习
tldr: 针对大语言模型在开放式任务中缺乏验证奖励和标签的问题，本文提出结构感知强化学习（SARL）。该方法通过构建推理图并奖励其“小世界”拓扑结构，引导模型优化推理路径而非仅关注最终答案。实验表明，SARL在数学和开放式任务上均显著优于基于标签的强化学习，提升了模型的泛化能力和训练稳定性。
selection_source: fresh_fetch
motivation: 传统的强化学习高度依赖可验证的奖励或标签，难以应用于正确性模糊的开放式领域，且容易导致模型过度拟合特定答案而非学习通用推理。
method: 提出SARL框架，通过构建推理步骤的拓扑图并奖励其局部连贯与全局高效的“小世界”特性，实现对推理路径结构的无标签监督。
result: "在Qwen3-4B上的实验显示，SARL在数学任务中提升约10%，在开放式任务中提升超过30%，且表现出更低的KL散度和更高的策略熵。"
conclusion: 通过奖励推理过程的拓扑结构而非仅仅优化最终结果，可以有效提升模型的通用推理能力，为无监督强化学习提供了新的研究方向。
---

## 摘要
强化学习已成为提升大型推理模型的核心，但其成功仍严重依赖于可验证的奖励或有标签的监督。这限制了其在正确性模糊且无法验证的开放式领域中的适用性。此外，推理轨迹在很大程度上仍不受约束，而针对最终答案的优化可能会导致模型倾向于早期开发而非泛化。在这项工作中，我们探讨是否可以通过教导模型“如何思考”（推理结构）而非“产出什么”（推理结果）来提高通用推理能力，并将传统的 RLVR 扩展到开放式场景。我们提出了结构感知强化学习（SARL），这是一种无标签框架，它根据中间思考步骤为每个响应构建推理图（Reasoning Map），并受复杂网络和人类大脑功能组织的启发，对其小世界拓扑结构进行奖励。SARL 鼓励局部连贯且全局高效的推理轨迹，将监督重心从终点转向路径。我们在 Qwen3-4B 上的实验表明，SARL 超越了基于地面真值（ground truth）的强化学习和先前的无标签强化学习基准，在数学任务中 PPO 和 GRPO 下分别实现了 9.1% 和 11.6% 的最佳平均增益，在开放式任务中则分别实现了 34.6% 和 30.4% 的增益。除了优异的性能外，SARL 还表现出更低的 KL 散度、更高的策略熵，表明其训练过程更稳定、更具探索性，且具备更强的泛化推理能力。

## Abstract
Reinforcement learning has become central to improving large reasoning models, but its success still relies heavily on verifiable rewards or labeled supervision. This limits its applicability to open ended domains where correctness is ambiguous and cannot be verified. Moreover, reasoning trajectories remain largely unconstrained, and optimization towards final answer can favor early exploitation over generalization. In this work, we ask whether general reasoning ability can be improved by teaching models how to think (the structure of reasoning) rather than what to produce (the outcome of reasoning) and extend traditional RLVR to open ended settings. We introduce structure aware reinforcement learning (SARL), a label free framework that constructs a per response Reasoning Map from intermediate thinking steps and rewards its small world topology, inspired by complex networks and the functional organization of the human brain. SARL encourages reasoning trajectories that are both locally coherent and globally efficient, shifting supervision from destination to path. Our experiments on Qwen3-4B show SARL surpasses ground truth based RL and prior label free RL baselines, achieving the best average gain of 9.1% under PPO and 11.6% under GRPO on math tasks and 34.6% under PPO and 30.4% under GRPO on open ended tasks. Beyond good performance, SARL also exhibits lower KL divergence, higher policy entropy, indicating a more stable and exploratory training and generalized reasoning ability.

---

## 论文详细总结（自动生成）

以下是对论文《SARL: Label-Free Reinforcement Learning by Rewarding Reasoning Topology》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前大型推理模型（LRM）的进步高度依赖于**可验证奖励强化学习（RLVR）**，即通过数学或代码的客观正确性提供反馈。然而，这导致了“验证瓶颈”：在正确性模糊、无标准答案的开放式领域（如战略规划、哲学探讨），RL 信号会消失。此外，现有的过程奖励模型（PRM）仍依赖昂贵的人工标注。
论文的核心动机是：**能否不依赖地面真值（Ground Truth），仅通过优化模型“如何思考”（推理的结构）而非“产出什么”（推理的结果）来提升推理能力？** 论文受脑科学启发，认为高效的思维应具备类似人类大脑的“小世界”拓扑结构。

### 2. 方法论：核心思想与技术细节
论文提出了**结构感知强化学习（SARL）**，其核心是将推理过程建模为图，并奖励特定的拓扑特性：
*   **推理图构建（Reasoning Map Construction）：**
    1.  **步骤提取：** 将 `<think>` 模块内的内容按换行符切分为独立的推理步骤。
    2.  **步骤嵌入：** 使用轻量级编码器（如 Qwen3-Embedding-0.6B）将每个步骤转化为向量。
    3.  **潜在推理类型聚类：** 对单次响应内的所有步骤进行聚类（数学任务用 KMeans，开放式任务用 HDBSCAN），每个类代表一种“推理类型”（节点）。
    4.  **转换连边：** 若相邻步骤属于不同类别，则在节点间建立无向边。
*   **结构奖励（Structure Reward, SR）：**
    基于“小世界”网络原理，定义奖励函数：$SR(G) = \frac{1}{2} C(G) + \frac{1}{1+L(G)}$。
    *   **$C(G)$（平均聚类系数）：** 衡量局部连贯性，鼓励形成专门的推理模块。
    *   **$L(G)$（平均最短路径长度）：** 衡量全局效率，鼓励不同推理模式间的高效转换。
*   **算法流程：** 该框架是算法无关的，可无缝集成至 PPO 或 GRPO 等强化学习算法中，完全不需要外部标签。

### 3. 实验设计
*   **模型：** Qwen3-4B。
*   **数据集/场景：**
    *   **数学推理（可验证）：** 使用 AIME 历史题目（1983-2024）进行训练；在 MATH500、AIME25、AMC23 和 Minerva Math 上评估。
    *   **开放式推理（不可验证）：** 使用 OpenRubrics-v2 进行训练；在 WildBench（包含创意写作、规划、代码、建议等）上评估。
*   **对比基准（Baselines）：**
    *   **有监督/有标签：** Ground-Truth RL（使用正确性标签）、DPO（使用偏好数据）。
    *   **无标签：** EMPO（熵最小化）、TTRL（多数投票/一致性）、基于格式和长度的奖励。

### 4. 资源与算力
*   **硬件：** 使用了单节点 **8 张 NVIDIA A100 GPU**。
*   **框架：** 基于 `veRL` 强化学习框架，采用 FSDP2 进行模型并行。
*   **细节：** 训练时启动了一个独立的嵌入服务器（消耗约 5% 的单卡显存）来实时计算推理步骤的向量，不干扰主策略模型的 Rollout。数学任务训练 15 epochs，开放式任务训练 5 epochs。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **跨领域验证：** 涵盖了从极度严谨的数学到极度发散的创意写作。
*   **算法兼容性：** 同时在 PPO 和 GRPO 两种主流 RL 算法上验证了 SARL 的有效性。
*   **消融与分析：** 包含训练动力学分析（KL 散度、策略熵）、响应长度分析以及不同聚类算法的对比。
*   **充分性评价：** 实验设计较为全面，对比了当前最前沿的无标签 RL 方法，且在数学任务上与“金标准”（有标签 RL）进行了直接对标，结果具有较强的说服力。

### 6 主要结论与发现
*   **性能卓越：** 在数学任务中，SARL 的表现甚至超过了使用地面真值标签的 RL（在 GRPO 下平均提升 7.65% vs 7.15%）。在开放式任务中，SARL 显著优于 DPO 和 EMPO。
*   **训练更稳定：** 相比其他无标签方法，SARL 的 KL 散度极低（接近 0），说明策略漂移小；同时保持了较高的策略熵，说明模型在持续探索而非陷入单一模式。
*   **推理更高效：** SARL 训练后的模型生成的响应长度反而变短了，说明性能提升源于推理结构的优化，而非简单的“刷字数”。

### 7. 优点
*   **摆脱标签依赖：** 为无法自动验证的领域（如人文、战略）提供了一种切实可行的强化学习路径。
*   **理论支撑深厚：** 借鉴了神经科学中关于大脑“小世界”结构的发现，将生物学先验引入 LLM 优化。
*   **通用性强：** 既适用于 PPO 也适用于 GRPO，且在多种任务类型上表现稳健。

### 8. 不足与局限
*   **任务复杂度要求：** SARL 依赖于足够长且复杂的推理链。对于步骤极少的简单任务，构建推理图可能失去意义。
*   **先验结构的单一性：** 论文假设“小世界”结构是所有推理任务的最优解，但特定领域（如纯逻辑证明或纯发散联想）可能存在更优的其他拓扑结构。
*   **计算开销：** 虽然嵌入模型很小，但在线聚类和图计算仍为奖励函数增加了额外的计算步骤。

（完）
