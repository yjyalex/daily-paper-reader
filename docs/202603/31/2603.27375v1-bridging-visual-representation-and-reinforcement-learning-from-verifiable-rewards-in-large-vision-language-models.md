---
title: Bridging Visual Representation and Reinforcement Learning from Verifiable Rewards in Large Vision-Language Models
title_zh: 在大视觉语言模型中桥接视觉表示与基于可验证奖励的强化学习
authors: "Yuhang Han, Yuyang Wu, Zhengbo Jiao, Yiyu Wang, Xuyang Liu, Shaobo Wang, Hanlin Xu, Xuming Hu, Linfeng Zhang"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27375v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 视觉语言模型中基于可验证奖励的强化学习推理
tldr: 本研究针对大视觉语言模型（LVLM）在可验证奖励强化学习（RLVR）中视觉表示与优化过程脱节的问题，提出了 KAWHI 奖励重权机制。该机制通过层次化几何聚合定位语义显著区域，并识别视觉关键注意力头，实现段落级的信用重新分配，从而将空间视觉证据与推理步骤精准对齐。作为一种即插即用的通用增强模块，KAWHI 在多个基准测试中显著提升了 GRPO 等主流 RL 算法的性能，有效增强了模型的多模态推理能力。
selection_source: fresh_fetch
motivation: 现有 LVLM 的强化学习方法缺乏对视觉信息的显式建模，导致视觉表示无法与推理优化过程紧密耦合。
method: 提出 KAWHI 机制，通过层次化几何聚合定位关键视觉区域，并利用结构化归因进行段落级信用重新分配。
result: 在多个推理基准测试中，KAWHI 作为通用增强模块显著提升了 GRPO 和 GSPO 等方法的性能。
conclusion: 显式对齐空间视觉证据与语义推理步骤是提升大视觉语言模型多模态推理能力的有效途径。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已显著增强了大语言模型在抽象推理任务中的推理能力。然而，其在大视觉语言模型（LVLMs）中的应用仍受限于结构性的表示瓶颈。现有方法通常缺乏对视觉信息的显式建模和有效利用，导致视觉表示无法与强化学习优化过程紧密耦合，从而限制了多模态推理性能的进一步提升。为解决这一局限性，我们提出了 KAWHI（关键区域对齐加权调和激励），这是一种即插即用的奖励重加权机制，可将结构化视觉信息显式地融入统一奖励策略优化方法（如 GRPO 和 GSPO）中。该方法通过分层几何聚合自适应地定位语义显著区域，通过结构化归因识别视觉关键注意力头，并进行段落级信度重新分配，以使空间视觉证据与语义决定性的推理步骤相对齐。在多种推理基准上的广泛实证评估证实，KAWHI 作为一个通用增强模块，能够持续提升各种统一奖励优化方法的性能。项目主页：KAWHI (https://kawhiiiileo.github.io/KAWHI_PAGE/)

## Abstract
Reinforcement Learning from Verifiable Rewards (RLVR) has substantially enhanced the reasoning capabilities of large language models in abstract reasoning tasks. However, its application to Large Vision-Language Models (LVLMs) remains constrained by a structural representational bottleneck. Existing approaches generally lack explicit modeling and effective utilization of visual information, preventing visual representations from being tightly coupled with the reinforcement learning optimization process and thereby limiting further improvements in multimodal reasoning performance. To address this limitation, we propose KAWHI (Key-Region Aligned Weighted Harmonic Incentive), a plug-and-play reward reweighting mechanism that explicitly incorporates structured visual information into uniform reward policy optimization methods (e.g., GRPO and GSPO). The method adaptively localizes semantically salient regions through hierarchical geometric aggregation, identifies vision-critical attention heads via structured attribution, and performs paragraph-level credit reallocation to align spatial visual evidence with semantically decisive reasoning steps. Extensive empirical evaluations on diverse reasoning benchmarks substantiate KAWHI as a general-purpose enhancement module, consistently improving the performance of various uniform reward optimization methods. Project page: KAWHI (https://kawhiiiileo.github.io/KAWHI_PAGE/)

---

## 论文详细总结（自动生成）

以下是对论文《Bridging Visual Representation and Reinforcement Learning from Verifiable Rewards in Large Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：基于可验证奖励的强化学习（RLVR，如 GRPO）在提升大语言模型（LLM）的逻辑推理能力方面表现出色。然而，在多模态领域（LVLM），视觉表示与强化学习优化过程之间存在“脱节”。
*   **核心问题**：作者通过诊断分析发现，LVLM 在复杂推理任务（如数学、图表）中的失败有 **48.9% 归因于视觉感知错误（VP）**。现有的 RL 方法通常将视觉 Token 视为均匀分布的输入，忽略了视觉信息的稀疏性和空间重要性，导致模型在推理时无法精准对齐关键视觉证据。
*   **研究目标**：建立视觉表示与 RL 优化范式之间的原则性耦合，通过显式建模视觉几何信息来优化奖励分配。

### 2. 方法论
论文提出了 **KAWHI**（Key-Region Aligned Weighted Harmonic Incentive）机制，包含以下核心技术：
*   **SGUF（结构引导的并查集）算法**：
    *   **核心思想**：针对数学公式、几何图形等结构化图像，利用“结构张量（Structure Tensor）”提取局部几何特征（如笔画边缘）。
    *   **流程**：通过计算特征值的各向异性识别显著区域，利用并查集（Union-Find）进行分层聚合，从而将图像划分为“关键区域”和“背景区域”，实现自适应的视觉 Token 过滤。
*   **视觉关键注意力头识别**：
    *   通过在 MME 基准上进行全局消融实验，识别出对视觉感知贡献最大的特定注意力头（Vision-Critical Heads），作为后续空间对齐的锚点。
*   **段落级信度重新分配**：
    *   **对齐度量**：计算生成响应的 Query 向量与关键视觉区域 Key 向量之间的余弦相似度。
    *   **权重计算**：以段落（`\n\n` 分隔）为单位聚合相似度得分，通过温度缩放的 Softmax 生成段落权重 $w_j$。
    *   **奖励调制**：将统一的组奖励（Group Advantage）与段落权重相乘，使关注关键视觉区域的推理步骤获得更高的梯度信号。

### 3. 实验设计
*   **数据集**：
    *   **训练**：Geo3K（几何推理）、ChartQA 20K 子集（图表理解）。
    *   **评估**：数学推理（MathVista, MathVerse, MathVision, WeMath）；图表理解（ChartXiv, ChartQA, ChartMimic 等）。
*   **基准模型**：Qwen2.5-VL-7B-Instruct 和 Qwen3-VL-4B-Instruct。
*   **对比方法**：
    *   **细粒度奖励方法**：Step-GRPO, FT-RL, VPPO。
    *   **统一奖励基准**：GRPO, DAPO, GSPO。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **软件框架**：基于 VERL 强化学习框架实现。
*   **训练细节**：学习率 1e-6，Batch Size 为 512 或 1024，采用 FSDP（全分片数据并行）优化。
*   **效率表现**：KAWHI 引入的额外计算开销仅为 **3.6%**（单步时长从 521s 增至 540s），证明了其高效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种模型尺寸、9 个主流多模态基准测试。
*   **消融实验**：非常充分，分别验证了区域选择策略（SGUF vs 随机）、奖励度量方式（K-Q vs K-K）、响应粒度（段落 vs Token）、关键头选择以及位置编码的影响。
*   **鲁棒性验证**：通过将 SGUF 替换为 VisionZip 证明了 KAWHI 框架的通用迁移能力。
*   **客观性**：采用了 LLM-as-a-judge（使用 Qwen3-30B）和 Exact Match 两种客观评估方式，实验设计严谨。

### 6. 主要结论与发现
*   **性能提升**：KAWHI 在所有测试的 RL 算法（GRPO, DAPO, GSPO）上均实现了持续的性能增益（平均提升 1%~5% 不等）。
*   **感知增强**：显式对齐视觉关键区域能显著减少幻觉，提高模型在复杂几何和图表任务中的准确性。
*   **粒度重要性**：段落级（Paragraph-level）的信度分配优于 Token 级，因为前者能保持语义结构的完整性，避免噪声干扰。

### 7. 优点
*   **即插即用**：作为奖励重权模块，无需修改模型架构即可集成到现有 RL 流程中。
*   **计算高效**：通过 SGUF 过滤和特定头计算，在极低开销下实现了显著的性能提升。
*   **可解释性强**：定性分析显示，KAWHI 能够准确识别并加权那些包含关键约束和规则应用的核心推理步骤。

### 8. 不足与局限
*   **依赖分段符**：段落级加权依赖于模型输出中标准的 `\n\n` 分隔，对于不遵循此格式的模型可能失效。
*   **视觉类型局限**：SGUF 算法主要针对结构化图像（数学、图表）设计，在语义极其密集的自然场景图像（如复杂的街道场景）中的表现尚未得到充分验证。
*   **预处理依赖**：识别“视觉关键头”需要预先在 MME 等基准上进行消融分析，这增加了一定的前期准备工作。

（完）
