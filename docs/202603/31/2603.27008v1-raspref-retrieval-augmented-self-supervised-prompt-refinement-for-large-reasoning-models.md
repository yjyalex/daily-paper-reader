---
title: "RASPRef: Retrieval-Augmented Self-Supervised Prompt Refinement for Large Reasoning Models"
title_zh: RASPRef：大语言推理模型的检索增强自监督提示词优化
authors: Rahul Soni
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27008v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 推理模型的自监督提示词优化
tldr: 针对推理型大模型对提示词高度敏感且人工设计成本高的问题，本文提出RASPRef框架。该方法通过检索相关示例和推理轨迹，结合多样本一致性、验证器反馈及模型自评等自监督信号，实现提示词的自动化迭代优化。实验证明，该方法在数学推理任务上显著优于静态提示词，为提升大模型推理能力提供了一种可扩展的自动化方案。
selection_source: fresh_fetch
motivation: 推理模型性能受提示词影响显著，且人工迭代设计提示词的过程难以在不同任务间高效扩展。
method: 提出一种检索增强的自监督提示词优化框架，通过整合检索到的推理轨迹和多维自监督反馈信号来迭代改进提示词。
result: 在GSM8K等数学推理任务上的实验表明，该方法相比静态提示词基线能显著提升模型的推理表现。
conclusion: 自监督提示词优化是提升推理型语言模型性能的一种实用且具备可扩展性的关键策略。
---

## 摘要
最近的推理导向型语言模型（如 DeepSeek R1 和 OpenAI o1）在 GSM8K、MATH 和多跳问答任务等结构化推理基准测试中表现出了强大的性能。然而，它们的性能对提示词的表述高度敏感，而设计有效的提示词通常是一个手动且迭代的过程，难以在不同任务或领域间扩展。为了解决这一局限性，我们引入了检索增强自监督提示词优化（RASPRef），这是一个无需人工标注或特定任务监督即可改进提示词的框架。该方法检索相关示例和先前生成的推理轨迹，并利用多样本一致性、验证器反馈和模型生成的批判等信号来迭代优化提示词。与以往主要关注改进模型输出的方法不同，RASPRef 直接将提示词视为优化目标，并通过迭代的检索引导优化过程对其进行改进。在 GSM8K 风格的数学推理任务上的实验表明，与静态提示词基准相比，检索引导的提示词提升了性能。我们进一步讨论了检索质量、轨迹选择和自监督反馈信号如何影响提示词优化的有效性。这些发现表明，提示词设计仍然是推理导向型语言模型的关键因素，而自改进提示词为提升推理性能提供了一种实用且可扩展的策略。

## Abstract
Recent reasoning-focused language models such as DeepSeek R1 and OpenAI o1 have demonstrated strong performance on structured reasoning benchmarks including GSM8K, MATH, and multi-hop question answering tasks. However, their performance remains highly sensitive to prompt formulation, and designing effective prompts is typically a manual and iterative process that does not scale well across tasks or domains. To address this limitation, we introduce Retrieval-Augmented Self-Supervised Prompt Refinement (RASPRef), a framework that improves prompts without requiring human annotations or task-specific supervision. The approach retrieves relevant examples and previously generated reasoning trajectories, and leverages signals such as multi-sample consistency, verifier feedback, and model-generated critiques to iteratively refine the prompt. Unlike prior approaches that focus primarily on improving model outputs, RASPRef directly treats the prompt as the optimization target and improves it through an iterative retrieval-guided refinement process. Experiments on GSM8K-style mathematical reasoning tasks show that retrieval-guided prompting improves performance compared with a static prompting baseline. We further discuss how retrieval quality, trajectory selection, and self-supervised feedback signals may influence the effectiveness of prompt refinement. These findings suggest that prompt design remains a critical factor for reasoning-oriented language models, and that self-improving prompts offer a practical and scalable strategy for improving reasoning performance.

---

## 论文详细总结（自动生成）

以下是对论文《RASPRef: Retrieval-Augmented Self-Supervised Prompt Refinement for Large Reasoning Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 DeepSeek R1 和 OpenAI o1 等推理模型在复杂任务上表现优异，但其性能对提示词（Prompt）的表述、结构和示例选择高度敏感。
*   **研究动机**：目前设计高效提示词主要依赖人工迭代，这种方式难以跨领域扩展。现有的提示词优化方法要么需要访问模型权重（如 Soft Prompt Tuning），要么需要大量标注数据或复杂的奖励模型。
*   **整体含义**：本文提出 **RASPRef** 框架，旨在通过**检索增强**和**自监督反馈**，实现无需人工干预、自动化的提示词迭代优化，使模型能够根据历史推理轨迹自我进化。

### 2. 方法论：核心思想与技术细节
RASPRef 的核心思想是将提示词视为动态优化的目标，通过四个关键步骤进行闭环优化：
*   **轨迹检索（Retrieval）**：针对新问题，从存储库中检索在任务结构或领域上最相似的过去推理轨迹（包含输入、提示词、推理链及反馈）。
*   **初始提示词构建**：利用检索到的轨迹提取成功模式，生成包含通用指令、提炼出的推理准则和代表性示例的初始提示词。
*   **自监督评估（Quality Objective）**：定义了一个综合评分函数 $Q(p; x)$，由四个弱信号加权组成：
    1.  **多样本一致性 ($C_{cons}$)**：衡量模型对同一问题生成多个答案的一致程度。
    2.  **验证器评分 ($C_{ver}$)**：使用轻量级验证模型评估推理链的逻辑结构和合理性。
    3.  **自评信号 ($C_{crit}$)**：模型对自身推理过程的批判性分析得分。
    4.  **检索对齐度 ($C_{ret}$)**：衡量推理过程是否有效利用了检索到的参考资产。
*   **迭代优化算法**：模型扮演“编辑”角色，根据自监督评分和批判意见修改提示词。该过程持续迭代，直到评分稳定或达到计算预算上限。

### 3. 实验设计
*   **数据集**：主要使用 **GSM8K**（小学数学应用题）的子集进行验证。
*   **实验场景**：算术推理任务，要求多步逻辑推导。
*   **Benchmark 与对比方法**：
    *   **Static Prompting（基线）**：使用固定的思维链（CoT）模板，不进行检索或优化。
    *   **Retrieval-Augmented Prompting（RASPRef 原型）**：基于嵌入相似度检索 top-5 轨迹并增强提示词。
*   **模型工具**：使用 `gpt-4o-mini` 作为基础推理模型（通过 API 调用），使用 `text-embedding-3-small` 进行向量检索。

### 4. 资源与算力
*   **算力说明**：论文明确指出实验是在**本地开发机（CPU 环境）**上运行的 Python 脚本。
*   **GPU 需求**：由于推理模型是通过 API 访问的闭源模型，因此**不需要本地 GPU 训练**。
*   **存储**：使用了向量索引存储 200 个训练示例的轨迹。

### 5. 实验数量与充分性
*   **实验规模**：在 GSM8K 的 500 个测试样本上进行了评估。
*   **充分性评价**：
    *   **初步验证有效**：实验展示了从 85.6% 到 95.0% 的显著准确率提升。
    *   **局限性**：实验规模较小（仅 500 例），且目前仅聚焦于数学推理单一领域。
    *   **缺乏消融实验**：文中虽然定义了复杂的 $Q(p; x)$ 公式，但实验部分主要展示了“检索增强”带来的提升，对于“迭代优化”和“各监督信号权重”的消融分析尚不充分。

### 6. 主要结论与发现
*   **检索的价值**：检索相关的推理轨迹能显著帮助模型组织中间推理步骤，减少逻辑错误。
*   **自监督可行性**：无需人工标注，仅靠一致性和验证器等内部信号即可引导提示词向更高质量演进。
*   **性能提升**：在数学推理任务上，RASPRef 框架使模型准确率从 85.6% 提升至 95.0%。

### 7. 优点
*   **无需标注**：完全依赖自监督信号，解决了提示词工程中数据获取难的问题。
*   **黑盒适用性**：不需要访问模型梯度或内部状态，适用于所有通过 API 访问的闭源大模型。
*   **可解释性**：优化的结果是自然语言提示词，人类可以直接阅读并理解模型提取了哪些推理启发式规则。

### 8. 不足与局限
*   **计算开销**：每个优化周期需要多次采样和调用验证模型，增加了推理阶段的延迟和成本。
*   **过拟合风险**：过度针对特定邻域的轨迹进行优化，可能导致提示词过于具体，从而丧失在处理罕见或长尾案例时的泛化能力。
*   **评估维度单一**：目前的实证部分主要验证了检索的贡献，对于多轮迭代优化后的长期稳定性及在非数学任务（如代码、多跳问答）上的表现缺乏数据支持。

（完）
