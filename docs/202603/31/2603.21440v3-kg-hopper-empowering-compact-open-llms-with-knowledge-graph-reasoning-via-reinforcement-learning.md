---
title: "KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning"
title_zh: KG-Hopper：通过强化学习赋予紧凑型开源大语言模型知识图谱推理能力
authors: "Shuai Wang, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21440v3"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于多跳知识图谱推理的强化学习框架
tldr: 针对大语言模型在知识图谱多步推理中存在的灵活性差和错误累积问题，本文提出 KG-Hopper 强化学习框架。该框架通过将整个 KG 遍历和决策过程整合进单一推理轮次的“思考”阶段，使 7B 规模的小型开源模型具备全局推理和动态回溯能力。实验证明，KG-Hopper 在多个基准测试上超越了 70B 规模的系统，并达到了与 GPT-4o-mini 相当的性能。
selection_source: fresh_fetch
motivation: 现有的知识库问答方法依赖预定义的顺序流水线，容易导致推理步骤间的孤立和错误累积。
method: 提出一种强化学习框架，训练模型在统一的“思考”阶段内完成多步 KG 路径探索、全局依赖分析及动态回溯。
result: 基于 7B 参数的模型在 8 个基准测试中一致优于 70B 的多步推理系统，并可比肩 GPT-3.5-Turbo 等闭源模型。
conclusion: KG-Hopper 证明了通过强化学习优化推理过程，小型开源模型也能在复杂的知识密集型推理任务中展现出卓越的性能和效率。
---

## 摘要
大语言模型（LLMs）展现了令人印象深刻的自然语言能力，但在处理知识密集型推理任务时往往面临挑战。利用结构化知识图谱（KGs）的知识库问答（KBQA）因需要准确的多跳推理而体现了这一挑战。现有方法通常在预定义流水线的指导下执行顺序推理步骤，这限制了灵活性，并由于每一步的孤立推理而导致错误级联。为了解决这些局限性，我们提出了 KG-Hopper，这是一个新颖的强化学习（RL）框架，它赋予紧凑型开源 LLM 在单轮推理中执行集成多跳 KG 推理的能力。与逐步推理不同，我们训练了一个推理 LLM，将整个 KG 遍历和决策过程嵌入到一个统一的“思考”阶段，从而实现对跨步依赖关系的全局推理以及带有回溯的动态路径探索。在八个 KG 推理基准测试上的实验结果表明，基于 7B 参数 LLM 的 KG-Hopper 始终优于更大规模的多步系统（高达 70B），并与 GPT-3.5-Turbo 和 GPT-4o-mini 等专有模型相比具有竞争力的性能，同时保持了紧凑、开源和数据高效的特点。代码已公开于：https://github.com/Wangshuaiia/KG-Hopper。

## Abstract
Large Language Models (LLMs) demonstrate impressive natural language capabilities but often struggle with knowledge-intensive reasoning tasks. Knowledge Base Question Answering (KBQA), which leverages structured Knowledge Graphs (KGs) exemplifies this challenge due to the need for accurate multi-hop reasoning. Existing approaches typically perform sequential reasoning steps guided by predefined pipelines, restricting flexibility and causing error cascades due to isolated reasoning at each step. To address these limitations, we propose KG-Hopper, a novel Reinforcement Learning (RL) framework that empowers compact open LLMs with the ability to perform integrated multi-hop KG reasoning within a single inference round. Rather than reasoning step-by-step, we train a Reasoning LLM that embeds the entire KG traversal and decision process into a unified ``thinking'' stage, enabling global reasoning over cross-step dependencies and dynamic path exploration with backtracking. Experimental results on eight KG reasoning benchmarks show that KG-Hopper, based on a 7B-parameter LLM, consistently outperforms larger multi-step systems (up to 70B) and achieves competitive performance with proprietary models such as GPT-3.5-Turbo and GPT-4o-mini, while remaining compact, open, and data-efficient. The code is publicly available at: https://github.com/Wangshuaiia/KG-Hopper.

---

## 论文详细总结（自动生成）

以下是对论文《KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在知识密集型任务中常出现幻觉。现有的知识库问答（KBQA）方法通常采用多步顺序推理流水线，存在**灵活性差**（难以处理缺失数据）、**错误级联**（早期步骤错误导致最终失败）以及**忽视步骤间依赖关系**等缺陷。
*   **研究动机**：受 OpenAI o1 和 DeepSeek-R1 等推理模型的启发，作者希望通过强化学习（RL）训练紧凑型开源模型，使其在单次推理的“思考（Thinking）”阶段内完成完整的多跳知识图谱（KG）遍历和决策，从而实现动态回溯和全局推理。

### 2. 方法论
*   **核心思想**：将多跳 KG 推理过程集成到 LLM 的单轮生成中，利用强化学习优化其调用检索工具、处理检索结果及逻辑推理的能力。
*   **关键技术细节**：
    *   **两阶段训练**：首先进行**冷启动（Cold Start）**，使用 500 条高质量 CoT 数据进行监督微调（SFT），使模型学会基本的工具调用格式；随后进行**面向推理的强化学习**。
    *   **强化学习算法**：采用 **GRPO（Group Relative Policy Optimization）** 算法，通过组内相对优势估计来稳定优化。
    *   **复合奖励函数**：
        1.  **检索奖励 ($R_{search}$)**：鼓励模型主动搜索，但设有上限以防过度调用。
        2.  **格式奖励 ($R_{format}$)**：确保输出符合 `<think>`、`<search>`、`<answer>` 等结构。
        3.  **推理奖励 ($R_{reason}$)**：使用外部大模型（如 Llama-3.3-70B）对推理轨迹的逻辑性进行评分。
        4.  **答案奖励 ($R_{answer}$)**：评估预测答案与真实值的语义一致性。
    *   **训练优化技巧**：**掩码检索三元组**（防止模型机械重复检索内容）和**历史重采样**（在训练后期移除简单的一跳问题，强迫模型学习复杂的多跳推理）。

### 3. 实验设计
*   **数据集**：涵盖了基于 **Freebase**（CWQ, WebQSP, WebQuestions, GrailQA）和 **WikiData**（QALD10-en, T-REx, Zero-Shot RE, Creak）的 8 个主流基准测试。
*   **对比方法**：
    *   **Prompt-only**：Qwen-2.5-7B, Llama-3.1-8B, Llama-3.3-70B, GPT-4o 等。
    *   **KG-Augmented (无微调)**：结合检索工具但模型未针对性训练。
    *   **SFT-based**：如 Interactive-KBQA, KG-CoT 等现有 SFT 方案。
*   **评估指标**：主要使用 Hit@1 和准确率（Accuracy）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   训练 2 个 Epoch。
    *   Batch Size 为 16，学习率为 1e-6。
    *   每次迭代生成 16 个 Rollout（采样轨迹）。
    *   训练样本量：从 8 个数据集中随机采样 2,000 条数据用于 RL 训练。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同性质的数据集上进行了测试，涵盖了问答、槽填充和事实核查任务。
*   **消融实验**：针对 RL vs SFT、奖励函数组成部分（RQ2）、无标注情况下的 LLM 评判（RQ3）以及重采样策略（RQ4）进行了详细的对比实验。
*   **充分性评价**：实验设计较为全面，对比了从 7B 到 70B 的多种模型以及闭源模型，验证了方法在不同基座模型（Qwen 和 Llama）上的通用性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **小模型逆袭**：基于 7B 参数的 KG-Hopper 在多个任务上优于 70B 的多步推理系统，并能比肩 GPT-4o-mini。
*   **RL 优于 SFT**：RL 能够显著提升模型处理长程依赖和复杂推理的能力，相比 SFT 有 4%-10% 的性能提升。
*   **推理奖励至关重要**：消融实验显示，对推理过程的全局监督（Reasoning Reward）对性能贡献最大。
*   **重采样有效性**：通过课程学习式的重采样，模型能更有效地学习处理具有挑战性的多跳查询。

### 7. 优点
*   **架构简洁**：将复杂的多步调用简化为单轮推理，减少了系统编排的复杂性。
*   **鲁棒性强**：允许模型在“思考”阶段进行自我修正和回溯，有效缓解了 KG 数据不全导致的错误级联。
*   **数据高效**：仅需少量（2000条）训练样本即可通过 RL 获得显著的推理提升。
*   **开源友好**：为紧凑型开源模型提供了提升知识推理能力的清晰路径。

### 8. 不足与局限
*   **奖励模型依赖**：推理奖励依赖于更强大的外部模型（如 70B 模型），这在训练环境中引入了额外的计算开销和潜在的评分偏差。
*   **KG 覆盖范围**：实验主要集中在 Freebase 和 WikiData，对于更垂直或私有的知识图谱泛化能力尚待验证。
*   **实时性挑战**：虽然减少了 LLM 调用次数，但单次调用中产生的长“思考”序列（Thinking tokens）可能会增加首字响应延迟。

（完）
