---
title: Multi-Agent Dialectical Refinement for Enhanced Argument Classification
title_zh: 用于增强论点分类的多智能体辩证优化
authors: "Jakub Bąba, Jarosław A. Chudziak"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27451v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于解决分类不确定性的多智能体辩论
tldr: "针对论证挖掘中传统监督学习成本高及大模型在处理结构歧义时易产生自我强化错误的问题，本文提出MAD-ACC框架。该框架采用“支持者-反对者-裁判”的多智能体辩论模式，通过辩证推理细化分类逻辑。实验表明，该方法在UKP数据集上取得85.7%的F1值，在无需特定领域训练的情况下显著优于单智能体模型，并提供了透明可解释的决策过程。"
selection_source: fresh_fetch
motivation: 传统论证挖掘依赖高成本微调，且大模型在区分相似论证组件时常因“谄媚效应”难以纠正初始错误。
method: 引入多智能体辩论框架MAD-ACC，通过不同角色间的冲突解释与辩证博弈来消除分类歧义。
result: "在UKP Student Essays语料库上实现了85.7%的Macro F1得分，性能大幅超越单智能体推理基准。"
conclusion: MAD-ACC提供了一种无需训练且具备高解释性的论证分类方案，通过辩论记录增强了模型决策的透明度。
---

## 摘要
论点挖掘 (AM) 是自动写作评估的基础技术，但传统的监督方法严重依赖昂贵的特定领域微调。虽然大语言模型 (LLMs) 提供了一种无需训练的替代方案，但它们往往难以处理结构歧义，无法区分主张 (Claims) 和前提 (Premises) 等相似组件。此外，单智能体自我修正机制通常存在“阿谀奉承” (sycophancy) 问题，即模型会强化其最初的错误，而不是对其进行批判性评估。我们提出了 MAD-ACC（用于论点组件分类的多智能体辩论），这是一个利用辩证优化来解决分类不确定性的框架。MAD-ACC 采用“支持者-反对者-裁判”模型，其中智能体为歧义文本的冲突解释进行辩护，从而揭示单智能体模型所忽略的逻辑细微差别。在 UKP 学生论文语料库上的评估表明，MAD-ACC 实现了 85.7% 的 Macro F1 分数，在不需要特定领域训练的情况下，显著优于单智能体推理基准。此外，与“黑盒”分类器不同，MAD-ACC 的辩证方法通过生成可解释决策背后推理过程的可读辩论笔录，提供了一种透明且可解释的替代方案。

## Abstract
Argument Mining (AM) is a foundational technology for automated writing evaluation, yet traditional supervised approaches rely heavily on expensive, domain-specific fine-tuning. While Large Language Models (LLMs) offer a training-free alternative, they often struggle with structural ambiguity, failing to distinguish between similar components like Claims and Premises. Furthermore, single-agent self-correction mechanisms often suffer from sycophancy, where the model reinforces its own initial errors rather than critically evaluating them. We introduce MAD-ACC (Multi-Agent Debate for Argument Component Classification), a framework that leverages dialectical refinement to resolve classification uncertainty. MAD-ACC utilizes a Proponent-Opponent-Judge model where agents defend conflicting interpretations of ambiguous text, exposing logical nuances that single-agent models miss. Evaluation on the UKP Student Essays corpus demonstrates that MAD-ACC achieves a Macro F1 score of 85.7%, significantly outperforming single-agent reasoning baselines, without requiring domain-specific training. Additionally, unlike "black-box" classifiers, MAD-ACC's dialectical approach offers a transparent and explainable alternative by generating human-readable debate transcripts that explain the reasoning behind decisions.

---

## 论文详细总结（自动生成）

### 论文总结：用于增强论点分类的多智能体辩证优化 (MAD-ACC)

#### 1. 论文的核心问题与整体含义
*   **研究背景**：论点挖掘（Argument Mining, AM）是自动写作评估的核心，旨在识别文本中的论证结构（如主张、前提）。
*   **核心问题**：
    *   **微调成本高**：传统的监督学习方法依赖大量领域特定的标注数据和昂贵的微调过程。
    *   **结构歧义**：大语言模型（LLM）在区分语义相似但结构功能不同的组件（如“主张 Claim”与“前提 Premise”）时经常出错。
    *   **谄媚效应（Sycophancy）**：单智能体自我修正机制往往会倾向于验证并强化其初始错误，而非进行真正的批判性反思。
*   **研究目的**：探索是否可以通过无需训练的多智能体辩论框架，在不进行监督微调的情况下，提升 LLM 在论点组件分类任务中的表现。

#### 2. 论文提出的方法论：MAD-ACC 框架
MAD-ACC（Multi-Agent Debate for Argument Component Classification）将分类任务转化为结构化辩论，核心流程如下：
*   **角色定义**：
    *   **Manager（管理者）**：负责概率初始化。它评估目标文本在各标签上的概率分布，选出概率最高的两个候选标签。
    *   **Proponent & Opponent（支持者与反对者）**：分别被随机分配上述两个候选标签，并根据上下文逻辑为各自的标签进行辩护。
    *   **Judge（裁判）**：分析辩论笔录，根据论证逻辑的严密性和支持方向做出最终裁决。
*   **算法流程**：
    1.  **概率初始化**：Manager 确定最可能的两个标签 $y_{top1}$ 和 $y_{top2}$，随机分配给辩论双方以消除位置偏见。
    2.  **辩证交互**：进行 2 轮（共 4 次发言）的辩论。智能体被要求优先考虑结构和逻辑依赖，而非单纯的语义陈述。
    3.  **裁判分类**：Judge 评估双方证据，验证哪一方正确识别了论证结构（如支持方向），输出最终预测 $\hat{y}$。

#### 3. 实验设计
*   **数据集**：使用 **UKP Student Essays v2** 语料库，包含 402 篇论文。实验严格遵循前人研究的测试集划分（80 篇论文，1266 个论证语句）。
*   **Benchmark（基准方法）**：
    *   **Baseline A (Vanilla)**：直接提示词分类（使用 Gemini 2.5 Flash）。
    *   **Baseline B (Chain-of-Thought)**：思维链推理分类。
    *   **Baseline C (Smart Reasoning)**：使用更强大的 Gemini 2.5 Pro 进行单智能体推理，但不进行辩论。
    *   **SOTA 参考**：Cabessa 等人 (2025) 发布的经过微调的监督学习模型。
*   **评估指标**：Macro F1、Weighted F1 以及各类别（MajorClaim, Claim, Premise）的 F1 分数。

#### 4. 资源与算力
*   **模型使用**：采用了 Google 的 **Gemini 2.5** 系列模型。Manager 和 Debaters 使用 **Gemini 2.5 Flash**，Judge 使用 **Gemini 2.5 Pro**。
*   **算力细节**：论文**未明确说明**具体的硬件算力（如 GPU 型号、数量）或推理总时长。由于该框架属于“无需训练（Training-free）”的推理增强方案，其主要开销在于 API 调用产生的 Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模**：在标准的 UKP 测试集上进行了全量评估，并针对不同推理策略（Vanilla, CoT, Smart Reasoning）设置了对比组。
*   **消融与定性分析**：论文通过两个详细的案例研究（Case Studies）展示了辩论如何纠正单智能体在“主题句歧义”和“层级区分”上的错误。
*   **客观性评价**：实验设计较为客观，通过随机分配辩论立场来消除位置偏见，并直接与最新的监督学习 SOTA 进行对比，明确标注了性能差距。但实验仅限于单一数据集（学生论文），跨领域的泛化性实验尚待补充。

#### 6. 论文的主要结论与发现
*   **性能提升**：MAD-ACC 实现了 **85.7% 的 Macro F1 分数**，显著优于所有非微调基准（最高提升约 7.2%）。
*   **解决核心痛点**：在最难区分的 **Claim（主张）** 类别上，MAD-ACC 比单智能体推理提升了 2 个百分点，比基础 LLM 提升了约 17 个百分点。
*   **缩小差距**：虽然仍略低于微调后的 SOTA（89.5%），但 MAD-ACC 在无需任何标注数据训练的情况下，极大地缩小了推理模型与监督模型之间的差距。

#### 7. 优点
*   **无需训练**：适用于标注数据稀缺的低资源领域。
*   **高解释性**：与“黑盒”分类器不同，MAD-ACC 生成的辩论笔录为最终决策提供了人类可读的逻辑依据。
*   **克服谄媚效应**：通过强制性的对立辩论，迫使模型审视被忽略的逻辑细节，有效解决了单智能体容易陷入初始错误的问题。

#### 8. 不足与局限
*   **成本与延迟**：多智能体、多轮辩论显著增加了 Token 消耗和推理延迟，在大规模实时应用中可能存在经济和效率压力。
*   **模型依赖**：框架的效果高度依赖于底层 LLM 的推理和辩论能力，在较弱的模型上可能无法达到预期效果。
*   **领域覆盖有限**：目前仅在学生论文数据集上验证，对于法律、医疗等逻辑更为严苛或术语更复杂的领域，其表现尚未可知。

（完）
