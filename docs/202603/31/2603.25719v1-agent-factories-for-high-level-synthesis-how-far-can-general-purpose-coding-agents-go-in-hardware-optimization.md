---
title: "Agent Factories for High Level Synthesis: How Far Can General-Purpose Coding Agents Go in Hardware Optimization?"
title_zh: 用于高级综合的智能体工厂：通用编程智能体在硬件优化中能走多远？
authors: "Abhishek Bhandwaldar, Mihir Choudhury, Ruchir Puri, Akash Srivastava"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25719v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 协调多个自主优化智能体的智能体工厂
tldr: 本研究探讨了通用编程智能体在无需硬件特定训练的情况下，对高层次综合（HLS）硬件设计的优化能力。提出了一种“智能体工厂”两阶段流水线：第一阶段将设计分解为子内核并利用整数线性规划（ILP）进行初步组装；第二阶段通过多个专家智能体进行跨函数全局优化。实验表明，该方法在多个基准测试中实现了显著的加速比，证明了智能体扩展是提升HLS优化效率的有效途径。
selection_source: fresh_fetch
motivation: 探索通用编程智能体在缺乏领域特定训练的情况下，能否通过自动化协作有效解决复杂的硬件高层次综合优化问题。
method: 提出一种两阶段智能体工厂架构，结合子内核分解、ILP全局约束求解以及多智能体并行的跨函数深度优化。
result: 在12个基准测试中，通过扩展至10个智能体实现了平均8.27倍的加速，部分复杂任务加速比超过20倍。
conclusion: 实验证明通用智能体能够自主发现硬件优化模式，且智能体规模化扩展是实现高效HLS优化的实用且强大的新方向。
---

## 摘要
我们提出了一项实证研究，探讨在没有硬件特定训练的情况下，通用编程智能体在多大程度上能够根据高级算法规范优化硬件设计。我们引入了一个智能体工厂（agent factory），这是一个由两个阶段组成的流水线，用于构建和协调多个自主优化智能体。在第一阶段，该流水线将设计分解为子内核，利用 pragma 和代码级变换独立优化每个子内核，并构建整数线性规划（ILP）模型，以在面积约束下组装全局有前景的配置。在第二阶段，它针对排名靠前的 ILP 解决方案启动 $N$ 个专家智能体，每个智能体探索跨函数优化，如 pragma 重组、循环融合和内存重构，这些优化是子内核分解无法捕捉到的。我们使用 Claude Code (Opus 4.5/4.6) 配合 AMD Vitis HLS，在来自 HLS-Eval 和 Rodinia-HLS 的 12 个内核上评估了该方法。将智能体数量从 1 个扩展到 10 个，相比基准实现了平均 8.27 倍的加速，在更难的基准测试中增益更大：streamcluster 超过了 20 倍，kmeans 达到了约 10 倍。在所有基准测试中，智能体在没有领域特定训练的情况下，一致地重新发现了已知的硬件优化模式，且最佳设计往往并非源自排名最高的 ILP 候选方案，这表明全局优化揭示了子内核搜索所遗漏的改进。这些结果确立了智能体扩展是高级综合（HLS）优化中一个实用且有效的维度。

## Abstract
We present an empirical study of how far general-purpose coding agents -- without hardware-specific training -- can optimize hardware designs from high-level algorithmic specifications. We introduce an agent factory, a two-stage pipeline that constructs and coordinates multiple autonomous optimization agents.   In Stage~1, the pipeline decomposes a design into sub-kernels, independently optimizes each using pragma and code-level transformations, and formulates an Integer Linear Program (ILP) to assemble globally promising configurations under an area constraint. In Stage~2, it launches $N$ expert agents over the top ILP solutions, each exploring cross-function optimizations such as pragma recombination, loop fusion, and memory restructuring that are not captured by sub-kernel decomposition.   We evaluate the approach on 12 kernels from HLS-Eval and Rodinia-HLS using Claude Code (Opus~4.5/4.6) with AMD Vitis HLS. Scaling from 1 to 10 agents yields a mean $8.27\times$ speedup over baseline, with larger gains on harder benchmarks: streamcluster exceeds $20\times$ and kmeans reaches approximately $10\times$. Across benchmarks, agents consistently rediscover known hardware optimization patterns without domain-specific training, and the best designs often do not originate from top-ranked ILP candidates, indicating that global optimization exposes improvements missed by sub-kernel search. These results establish agent scaling as a practical and effective axis for HLS optimization.

---

## 论文详细总结（自动生成）

以下是对论文《Agent Factories for High Level Synthesis: How Far Can General-Purpose Coding Agents Go in Hardware Optimization?》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在没有任何硬件特定训练的情况下，通用的编程智能体（General-Purpose Coding Agents）在优化高层次综合（HLS）硬件设计方面能达到什么水平？
*   **研究动机**：尽管 HLS 旨在将硬件设计抽象到 C/C++ 级别，但要获得高性能，仍需专家手动插入大量 Pragma 指令并进行复杂的代码重构（有时占代码量的 40% 以上）。现有的自动化设计空间探索（DSE）工具大多局限于预定义的参数搜索，无法像人类专家那样进行灵活的代码重构。

### 2. 方法论：智能体工厂（Agent Factory）
论文提出了一种两阶段的智能体协作流水线：
*   **第一阶段：子内核优化与 ILP 选择**
    *   **分解与生成**：协调智能体将设计分解为多个子函数，并为每个子函数生成 7 种不同策略的变体（从保守到激进，包括流水线化、循环展开、内存划分等）。
    *   **评估与建模**：通过 Vitis HLS 工具对变体进行功能验证和综合，获取延迟（Latency）和面积（Area）数据。
    *   **全局组装**：利用整数线性规划（ILP）求解器，在全局面积预算约束下，筛选出延迟最小的前 $N$ 个全局配置方案。
*   **第二阶段：通过智能体扩展进行全局优化**
    *   **专家探索**：针对 ILP 选出的前 $N$ 个方案，分别启动 $N$ 个专家智能体。
    *   **跨函数优化**：智能体不再局限于单个函数，而是探索跨函数边界的优化，如 Pragma 重组、循环融合、全局内存重构和代数简化。
    *   **推理时间扩展**：通过增加并行智能体的数量（$N$），增加推理时的计算量以换取更好的设计质量。

### 3. 实验设计
*   **数据集/基准测试**：共 12 个内核，包括 6 个来自 HLS-Eval（如 AES, SHA256, NW）和 6 个来自 Rodinia-HLS（如 kmeans, streamcluster, lavamd）。
*   **对比基准（Baseline）**：
    *   对于 HLS-Eval：采用有界穷举搜索（仅限 Pragma 级别）。
    *   对于 Rodinia-HLS：对比经过专家优化的参考实现。
*   **工具链**：使用 Claude Code (基于 Claude 4.5/4.6 模型) 作为智能体核心，AMD Vitis HLS 作为综合工具，ABC 工具用于 ASIC 面积相关性验证。

### 4. 资源与算力
*   **算力资源**：文中未详细说明运行 Vitis HLS 的硬件服务器配置。
*   **LLM 成本**：附录中详细记录了 Token 消耗。单次运行的 **中位数为 582 万 Token**，平均值为 767 万 Token。消耗范围从 114 万到最高 4533 万 Token 不等，显示出较高的推理成本。

### 5. 实验数量与充分性
*   **实验规模**：对 12 个基准测试均进行了 5 次独立运行并取平均值。
*   **消融/扩展实验**：重点测试了智能体数量 $N$ 从 1 扩展到 10 的效果。
*   **充分性评价**：作为一项实证研究，实验覆盖了不同复杂度的内核，并验证了从 FPGA 到 ASIC 的泛化性。虽然 12 个内核在 HLS 领域不算极大规模，但对于验证“智能体扩展”这一新范式而言是充分且客观的。

### 6. 主要结论与发现
*   **扩展效应显著**：将智能体数量从 1 增加到 10，平均实现了 **8.27 倍** 的加速比。
*   **复杂任务表现优异**：在复杂任务中增益最明显，如 `streamcluster` 加速超过 20 倍，`kmeans` 约 10 倍。
*   **自主发现专家模式**：智能体在无预训练情况下，自主“重新发现”了硬件专家常用的优化模式，例如通过 `ARRAY_PARTITION` 解决内存瓶颈。
*   **全局优化不可或缺**：最终胜出的设计往往并非来自 ILP 排名第一的初始方案，证明了第二阶段跨函数全局优化的必要性。

### 7. 论文亮点
*   **范式创新**：将 LLM 的“推理时间扩展”（Inference-time Scaling）引入硬件 EDA 领域，证明了增加智能体数量是提升硬件性能的有效轴线。
*   **两阶段结合**：巧妙地结合了结构化的数学优化（ILP）和开放式的智能体探索，既保证了搜索效率，又保留了重构代码的灵活性。
*   **零样本能力**：展示了通用模型在极度专业化的硬件优化领域具有强大的迁移和推理能力。

### 8. 不足与局限
*   **成本高昂**：数百万级别的 Token 消耗意味着目前该方法的经济成本较高。
*   **基准覆盖有限**：12 个内核尚不能完全代表现实世界中极其复杂的超大规模 HLS 设计。
*   **模型依赖**：实验高度依赖 Claude 系列模型，尚未验证其他开源或闭源模型在相同框架下的表现。
*   **对比深度**：虽然对比了穷举搜索，但未直接与最先进的商业 DSE 工具（如 AutoDSE）进行端到端的性能对比。

（完）
