---
title: "MedLoc-R1: Performance-Aware Curriculum Reward Scheduling for GRPO-Based Medical Visual Grounding"
title_zh: MedLoc-R1：面向基于 GRPO 的医疗视觉定位的性能感知课程奖励调度
authors: "Guangjing Yang, Ziyuan Qin, Chaoran Zhang, Chenlin Du, Jinlin Wang, Wanran Sun, Zhenyu Zhang, Bing Ji, Qicheng Lao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28120v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于 GRPO 的强化学习用于多模态推理任务
tldr: 医疗视觉定位对临床决策至关重要，但现有强化学习方法（如GRPO）在处理细小或模糊区域时面临严重的奖励稀疏问题。本文提出MedLoc-R1，一种感知性能的课程奖励调度框架。该框架利用滑动窗口追踪器动态调整奖励标准，从易到难逐步提高定位要求，有效解决了训练初期的优化停滞。实验证明，该方法在多个医疗基准上显著提升了定位精度和训练稳定性，且无需额外辅助网络。
selection_source: fresh_fetch
motivation: 针对医疗图像中目标细小导致的奖励稀疏及固定IoU奖励机制在强化学习初期引发的梯度消失问题。
method: 引入滑动窗口性能追踪器和多条件更新规则，实现从密集、易得信号到严格、精细定位要求的动态奖励调度。
result: 在三个医疗视觉定位基准测试中，MedLoc-R1在准确率和训练稳定性上均一致优于现有的GRPO基准。
conclusion: MedLoc-R1为高风险医疗场景下的强化学习定位任务提供了一种轻量级且通用的课程学习解决方案。
---

## 摘要
医疗视觉定位是细粒度多模态推理和可解释临床决策支持的重要基础。尽管强化学习（RL）在定位任务中取得了近期进展，但现有方法（如组相对策略优化，GRPO）在直接应用于医学图像时面临严重的奖励稀疏问题。这主要是由于定位微小或模糊的感兴趣区域具有内在难度，而强化学习中基于固定 IoU 的奖励方案的僵化和次优特性进一步加剧了这一问题。这导致了策略梯度消失和优化停滞，尤其是在训练早期。为了应对这一挑战，我们提出了 MedLoc-R1，这是一个性能感知的奖励调度框架，它根据模型的就绪程度逐步收紧奖励标准。MedLoc-R1 引入了滑动窗口性能追踪器和多条件更新规则，能够自动将奖励调度从密集的、易于获取的信号调整为更严格、细粒度的定位要求，同时在不引入辅助网络或额外梯度路径的情况下保留了 GRPO 的优良特性。在三个医疗视觉定位基准上的实验表明，与基于 GRPO 的基线相比，MedLoc-R1 一致地提高了定位精度和训练稳定性。我们的框架为高风险医疗应用中基于强化学习的定位提供了一种通用、轻量且有效的解决方案。代码和检查点可在 https://github.com/MembrAI/MedLoc-R1 获取。

## Abstract
Medical visual grounding serves as a crucial foundation for fine-grained multimodal reasoning and interpretable clinical decision support. Despite recent advances in reinforcement learning (RL) for grounding tasks, existing approaches such as Group Relative Policy Optimization~(GRPO) suffer from severe reward sparsity when directly applied to medical images, primarily due to the inherent difficulty of localizing small or ambiguous regions of interest, which is further exacerbated by the rigid and suboptimal nature of fixed IoU-based reward schemes in RL. This leads to vanishing policy gradients and stagnated optimization, particularly during early training. To address this challenge, we propose MedLoc-R1, a performance-aware reward scheduling framework that progressively tightens the reward criterion in accordance with model readiness. MedLoc-R1 introduces a sliding-window performance tracker and a multi-condition update rule that automatically adjust the reward schedule from dense, easily obtainable signals to stricter, fine-grained localization requirements, while preserving the favorable properties of GRPO without introducing auxiliary networks or additional gradient paths. Experiments on three medical visual grounding benchmarks demonstrate that MedLoc-R1 consistently improves both localization accuracy and training stability over GRPO-based baselines. Our framework offers a general, lightweight, and effective solution for RL-based grounding in high-stakes medical applications. Code \& checkpoints are available at \hyperlink{}{https://github.com/MembrAI/MedLoc-R1}.

---

## 论文详细总结（自动生成）

以下是对论文《MedLoc-R1: Performance-Aware Curriculum Reward Scheduling for GRPO-Based Medical Visual Grounding》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在医疗视觉定位（Medical Visual Grounding）任务中，直接应用基于强化学习（RL）的 GRPO 算法面临严重的**奖励稀疏（Reward Sparsity）**问题。
*   **研究背景**：医疗图像具有病灶微小、边界模糊、信噪比低等特点。传统的 RL 定位方法通常采用固定的 IoU 阈值（如 0.5）作为奖励准则。在训练初期，模型很难达到这一硬性标准，导致无法获得正向奖励，进而引发策略梯度消失和优化停滞。
*   **研究意义**：通过引入课程学习（Curriculum Learning）思想，使模型从简单的定位任务逐步过渡到高精度的临床要求，从而提升大型视觉语言模型（LVLM）在医疗场景下的推理与定位能力。

### 2. 方法论：MedLoc-R1 框架
*   **核心思想**：提出一种**性能感知的课程奖励调度机制**。该机制不依赖预设的时间步，而是根据模型当前的实时表现动态调整 IoU 奖励阈值 $\tau$。
*   **关键技术细节**：
    *   **滑动窗口性能追踪**：维护一个大小为 $N$ 的滑动窗口，记录最近训练步的统计数据。
    *   **三大评估指标**：
        1.  **窗口平均奖励 ($\bar{r}_k$)**：衡量模型在当前难度下的“命中率”。
        2.  **奖励标准差 ($\sigma_{r,k}$)**：评估模型表现的稳定性，防止因偶然波动而过早提升难度。
        3.  **IoU 余量 ($\bar{m}_k$)**：计算预测框与真值的平均 IoU 超出当前阈值的程度，衡量模型的“潜力”。
    *   **多条件更新规则**：只有当上述三个指标同时满足设定条件（准确、稳定、有余力）时，才会触发阈值更新。
    *   **调度策略**：支持分段衰减（Piecewise）、线性衰减（Linear）和余弦衰减（Cosine）三种方式来平滑地提升 IoU 阈值（例如从 0.3 逐步提升至 0.8）。

### 3. 实验设计
*   **数据集**：
    *   **HAM10000**：皮肤镜图像（皮肤癌检测）。
    *   **HEEL**：足部 X 射线图像（跟骨病变）。
    *   **TN3K**：甲状腺结节超声图像。
*   **对比基线（Benchmarks）**：
    *   **Zero-shot**：直接评估预训练模型。
    *   **SFT**：有监督微调（仅回归坐标，无推理过程）。
    *   **Raw-IoU**：使用连续 IoU 分数作为奖励。
    *   **VLM-R1**：固定 IoU 阈值（0.5）的 GRPO 训练。
    *   **V-Triune**：基于训练进度的固定阶段式调度。
*   **评估指标**：A@0.5、A@0.8（IoU 分别达到 0.5 和 0.8 的准确率）以及 mAP。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H800 80G GPU**。
*   **模型规模**：主要基于 **Qwen2.5-VL-3B-Instruct** 进行实验，并扩展到了 **7B** 和 **32B** 版本以验证可扩展性。
*   **训练细节**：学习率为 1e-6，Group Size 为 8，训练时长最多为 5 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同模态的医疗数据集，每组实验均运行了 3 个不同的随机种子并取平均值，进行了显著性检验（p-value）。
*   **消融实验**：非常充分。作者分别验证了：
    1.  更新准则中各个指标（奖励、余量、稳定性）的必要性。
    2.  不同调度策略（分段 vs 线性 vs 余弦）的影响。
    3.  滑动窗口大小及数据刷新策略的影响。
*   **客观性**：通过与多种 SOTA 方法及不同规模的模型对比，证明了该方法的通用性和鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：MedLoc-R1 在所有数据集上均显著优于固定阈值方法，尤其在严格的 A@0.8 指标上提升巨大（如在 HEEL 数据集上比 VLM-R1 提升了 43.18%）。
*   **训练稳定性**：动态调度有效解决了训练初期的梯度消失问题，收敛速度更快且过程更平滑。
*   **推理质量**：定性分析显示，MedLoc-R1 不仅定位更准，生成的思维链（CoT）推理过程也更符合临床逻辑，能识别关键解剖结构。

### 7. 优点与亮点
*   **性能感知**：不同于以往基于“训练进度”的调度，MedLoc-R1 是基于“模型能力”的调度，更加科学。
*   **轻量化**：无需引入额外的辅助网络（如 Critic 网络）或复杂的梯度路径，保持了 GRPO 的简洁性。
*   **即插即用**：该框架可以轻松集成到任何基于 GRPO 的多模态定位任务中。

### 8. 不足与局限
*   **空间精度上限**：虽然提升显著，但在极高精度要求下，模型仍存在空间偏差风险（如无法完美贴合复杂病灶边缘）。
*   **超参数依赖**：虽然提供了线性/余弦等简化方案，但分段衰减策略仍涉及多个超参数（如步长、边界值），可能需要针对新领域进行微调。
*   **任务维度**：目前主要集中在 2D 图像定位，尚未探讨在 3D 医疗影像（如 CT/MRI）中的表现。

（完）
