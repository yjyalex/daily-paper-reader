---
title: "Heddle: A Distributed Orchestration System for Agentic RL Rollout"
title_zh: Heddle：一种用于智能体强化学习 Rollout 的分布式编排系统
authors: "Zili Zhang, Yinmin Zhong, Chengxu Yang, Chao Jin, Bingyang Wu, Xinming Wei, Yuliang Liu, Xin Jin"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28101v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于代理强化学习Rollout的分布式编排系统
tldr: 针对 Agentic RL 中 LLM 与工具交互导致的轨迹生成长尾瓶颈问题，本文提出了分布式编排系统 Heddle。Heddle 采用以轨迹为中心的设计，通过轨迹级调度、感知轨迹的放置策略以及自适应资源管理，优化了任务执行的时机、位置和方式。实验表明，Heddle 显著减少了排队延迟和干扰，将端到端 Rollout 吞吐量提升了高达 2.5 倍。
selection_source: fresh_fetch
motivation: 现有的以步骤为中心的设计忽略了轨迹上下文，导致 Agentic RL 在频繁调用工具时面临严重的排队延迟、干扰和长尾轨迹生成效率低下的问题。
method: Heddle 引入了基于运行时预测的轨迹级调度、利用动态规划和机会性迁移的轨迹感知放置，以及动态调整模型并行度的自适应资源管理。
result: 在多种 Agentic RL 工作负载下，Heddle 成功消除了长尾瓶颈，相比现有基准系统实现了高达 2.5 倍的端到端 Rollout 吞吐量提升。
conclusion: Heddle 证明了以轨迹为中心的编排系统能有效解决 Agentic RL 中的长尾分布问题，是提升大规模强化学习训练效率的关键。
---

## 摘要
智能体强化学习（Agentic RL）通过在数据收集的 rollout 阶段和策略训练阶段之间交替，使大语言模型（LLM）能够解决复杂任务。在 rollout 过程中，智能体生成轨迹（trajectories），即 LLM 与外部工具之间的多步交互。然而，频繁的工具调用会导致长尾轨迹生成，从而成为 rollout 的瓶颈。这源于忽略轨迹上下文的以步骤为中心（step-centric）的设计，为长尾轨迹生成引发了三个系统问题：排队延迟、干扰开销和单 token 时间膨胀。我们提出了 Heddle，这是一个以轨迹为中心（trajectory-centric）的系统，旨在优化智能体 rollout 执行的时间、位置和方式。Heddle 集成了三个核心机制：利用运行时间预测和渐进优先级进行轨迹级调度，以最小化累积排队；通过预排序动态规划和工具调用空闲期间的机会性迁移实现轨迹感知放置，以最小化干扰；以及轨迹自适应资源管理器，动态调整模型并行度以加速长尾轨迹的单 token 时间，同时保持短轨迹的高吞吐量。对多种智能体强化学习工作负载的评估表明，Heddle 有效地消除了长尾瓶颈，与最先进的基准方案相比，端到端 rollout 吞吐量提高了多达 2.5 倍。

## Abstract
Agentic Reinforcement Learning (RL) enables LLMs to solve complex tasks by alternating between a data-collection rollout phase and a policy training phase. During rollout, the agent generates trajectories, i.e., multi-step interactions between LLMs and external tools. Yet, frequent tool calls induce long-tailed trajectory generation that bottlenecks rollouts. This stems from step-centric designs that ignore trajectory context, triggering three system problems for long-tail trajectory generation: queueing delays, interference overhead, and inflated per-token time. We propose Heddle, a trajectory-centric system to optimize the when, where, and how of agentic rollout execution. Heddle integrates three core mechanisms: trajectory-level scheduling using runtime prediction and progressive priority to minimize cumulative queueing; trajectory-aware placement via presorted dynamic programming and opportunistic migration during idle tool call intervals to minimize interference; and trajectory-adaptive resource manager that dynamically tunes model parallelism to accelerate the per-token time of long-tail trajectories while maintaining high throughput for short trajectories. Evaluations across diverse agentic RL workloads demonstrate that Heddle effectively neutralizes the long-tail bottleneck, achieving up to 2.5$\times$ higher end-to-end rollout throughput compared to state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Heddle —— 用于智能体强化学习 Rollout 的分布式编排系统

#### 1. 论文的核心问题与研究动机
*   **核心问题**：智能体强化学习（Agentic RL）在数据收集（Rollout）阶段存在严重的“木桶效应”。由于智能体需要与外部工具进行多步交互，生成的轨迹长度呈现长尾分布（少数复杂任务耗时极长），导致大部分计算资源在等待这些“掉队者”时处于闲置状态。
*   **研究动机**：现有的强化学习框架（如 Verl, Slime）采用“以步骤为中心”的设计，将每一步交互视为独立请求。这种设计忽略了轨迹的全局上下文，导致长尾轨迹面临严重的排队延迟、计算干扰以及资源分配不均，Rollout 阶段往往占据了整个训练时间的 80% 以上。

#### 2. 核心方法论
Heddle 提出了“以轨迹为中心”的设计理念，从执行时机（When）、位置（Where）和方式（How）三个维度进行优化：
*   **轨迹级调度（When）**：
    *   **渐进式轨迹预测**：利用轻量级模型（如 Qwen-0.6B）在每步交互后动态预测剩余轨迹长度。
    *   **渐进优先级调度（PPS）**：根据预测长度动态提升长尾轨迹的优先级，并引入抢占式执行机制，确保长尾任务优先获得计算资源，减少累积排队时间。
*   **轨迹感知放置（Where）**：
    *   **预排序动态规划（DP）**：基于引理（最优划分应是排序后的连续子序列），利用 DP 算法将长尾轨迹与短轨迹在空间上隔离，最小化长尾任务受到的批处理干扰。
    *   **机会性状态迁移**：在智能体调用外部工具的空闲时间内，利用 GPU-Direct RDMA 异步迁移 KV 缓存，修正预测偏差带来的负载不均。
*   **轨迹自适应资源管理（How）**：
    *   **异构并行策略**：打破同构配置限制，为长尾轨迹分配高模型并行度（MP）以降低延迟，为短轨迹分配低 MP 以保证高吞吐。
    *   **排序初始化模拟退火**：快速搜索近乎最优的 GPU 资源分配方案。

#### 3. 实验设计
*   **应用场景与数据集**：
    *   **代码生成**：CodeForces 数据集（使用沙盒工具）。
    *   **在线搜索**：HotpotQA 数据集（使用 Bing 搜索工具）。
    *   **数学解题**：DAPO-Math 数据集（使用计算器和求解器）。
*   **基准模型**：Qwen3 (8B, 14B, 32B)。
*   **对比方法（Baselines）**：
    *   **Slime**：基于 SGLang 的后训练框架，采用最少负载路由。
    *   **Verl**：高性能 RL 训练框架，采用静态缓存感知放置。
    *   **Verl***：Verl 的改进版，结合了 SGLang 路由器的混合策略。

#### 4. 资源与算力
*   **硬件环境**：使用了 8 台服务器，共计 **64 块 NVIDIA Hopper GPU**（每台 8 块）。
*   **互联技术**：机内 NVLink (900 GB/s)，机间 400 Gb/s InfiniBand，支持 GPUDirect RDMA。
*   **内存**：每台服务器配备 1.8 TB 主存。
*   **软件栈**：PyTorch 2.8.0, CUDA 12.2, Ray 2.49.0。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模的模型（8B/14B/32B）和 3 种典型的智能体任务，实验设置较为全面。
*   **消融实验**：分别针对调度器（预测精度与排队延迟）、放置策略（吞吐量对比）和资源管理器（活跃轨迹曲线）进行了详细的消融分析。
*   **客观性**：通过标准化 Rollout 后端（统一使用 SGLang）确保了与基准测试的公平对比，并考虑了预测和迁移带来的系统开销。

#### 6. 主要结论与发现
*   **性能提升**：Heddle 在端到端 Rollout 吞吐量上比现有最先进系统（Verl/Slime）提升了 **1.4× 至 2.5×**。
*   **规模效应**：模型参数越大，计算干扰越严重，Heddle 的优化效果越显著。
*   **瓶颈消除**：通过 PPS 调度，长尾轨迹的排队延迟显著降低；通过异构资源分配，成功平衡了低延迟与高吞吐的矛盾。

#### 7. 优点
*   **系统性强**：从调度、放置到资源分配构建了完整的闭环优化体系。
*   **创新性设计**：巧妙利用工具调用的“空闲期”进行 KV 缓存迁移，实现了开销掩盖。
*   **数学支撑**：为放置问题提供了引理证明和 DP 优化方案，而非纯启发式规则。

#### 8. 不足与局限
*   **预测模型依赖**：系统的有效性高度依赖于轨迹长度预测的准确性，虽然有动态修正，但在极端不可预测的环境下效果可能打折。
*   **复杂性开销**：引入了额外的预测模型和复杂的控制平面逻辑，对于极短、极简单的 RL 任务，系统开销可能抵消性能收益。
*   **应用限制**：目前主要针对同步 RL 算法（如 PPO/GRPO），对于完全异步且无状态限制的 RL 场景，其优势可能减弱。

（完）
