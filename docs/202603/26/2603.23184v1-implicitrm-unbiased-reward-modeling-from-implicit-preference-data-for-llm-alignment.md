---
title: "ImplicitRM: Unbiased Reward Modeling from Implicit Preference Data for LLM alignment"
title_zh: ImplicitRM：基于隐式偏好数据的大语言模型对齐无偏奖励建模
authors: "Hao Wang, Haocheng Yang, Licheng Pan, Lei Shen, Xiaoxi Li, Yinuo Wang, Zhichao Chen, Yuan Lu, Haoxuan Li, Zhouchen Lin"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23184v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: RLHF背景下用于LLM对齐的奖励建模
tldr: 针对LLM对齐中显式反馈数据成本高的问题，本文研究了利用隐式反馈（如点击、复制）进行奖励建模。针对隐式数据缺乏负样本和存在用户偏好偏差的挑战，提出了ImplicitRM框架。该框架通过分层模型将样本分为四个潜在组，并利用最大似然估计构建无偏学习目标，实现了从低成本隐式数据中学习准确奖励模型的目标，为高效对齐提供了新思路。
selection_source: fresh_fetch
motivation: 显式偏好数据收集成本高昂，而隐式反馈虽易获取但存在缺乏负样本和用户偏好偏差的挑战。
method: 提出ImplicitRM框架，通过分层模型将样本划分为四个潜在组，并基于最大似然估计推导出理论上无偏的学习目标。
result: 实验证明ImplicitRM在多种隐式偏好数据集上均能学习到比现有方法更准确的奖励模型。
conclusion: ImplicitRM为利用低成本隐式反馈进行LLM对齐提供了一种理论上无偏且有效的奖励建模方案。
---

## 摘要
奖励建模是用于大语言模型对齐的人类反馈强化学习（RLHF）中一项长期存在的挑战。目前的奖励建模严重依赖于收集成本昂贵的实验性反馈数据。在这项工作中，我们研究了“隐式奖励建模”——即从隐式人类反馈（如点击和复制）中学习奖励模型——作为一种具有成本效益的替代方案。我们识别了隐式奖励建模中的两个基本挑战：(1) 隐式偏好数据缺乏明确的负样本，使得标准的正负分类方法无法适用；(2) 隐式偏好数据受用户偏好偏差的影响，即不同的回复具有不同的诱发用户反馈行为的倾向，这加剧了区分明确负样本的难度。为了应对这些挑战，我们提出了 ImplicitRM，旨在从隐式偏好数据中学习无偏的奖励模型。ImplicitRM 通过分层模型将训练样本划分为四个潜在组。在此基础上，它通过似然最大化推导出学习目标，我们证明了该目标在理论上是无偏的，从而有效解决了这两个挑战。实验表明，ImplicitRM 在隐式偏好数据集上能够学习到准确的奖励模型。代码已在我们的项目网站上发布。

## Abstract
Reward modeling represents a long-standing challenge in reinforcement learning from human feedback (RLHF) for aligning language models. Current reward modeling is heavily contingent upon experimental feedback data with high collection costs. In this work, we study \textit{implicit reward modeling} -- learning reward models from implicit human feedback (e.g., clicks and copies) -- as a cost-effective alternative. We identify two fundamental challenges in implicit reward modeling: (1) Implicit preference data lacks definitive negative samples, which makes standard positive-negative classification methods inapplicable; (2) Implicit preference data suffers from user preference bias, where different responses have different propensities to elicit user feedback actions, which exacerbates the difficulty of distinguishing definitive negative samples. To address these challenges, we propose ImplicitRM, which aims to learn unbiased reward models from implicit preference data. ImplicitRM stratifies training samples into four latent groups via a stratification model. Building on this, it derives a learning objective through likelihood maximization, which we prove is theoretically unbiased, effectively resolving both challenges. Experiments demonstrate that ImplicitRM learns accurate reward models across implicit preference datasets. Code is available on our project website.

---

## 论文详细总结（自动生成）

以下是对论文《ImplicitRM: Unbiased Reward Modeling from Implicit Preference Data for LLM alignment》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖高成本“显式偏好数据”（如人工标注的 A/B 排名）的情况下，利用低成本的“隐式反馈数据”（如用户的点击、复制行为）训练出准确的奖励模型（Reward Model, RM）。
*   **研究背景**：传统的 RLHF 依赖昂贵的人工标注。隐式反馈虽然数据量大且易获取，但存在两个致命挑战：
    1.  **缺乏明确负样本**：用户“未反馈”不代表“不喜欢”（可能是满意但没必要操作），直接将未反馈视为负样本会引入大量假负例（False Negatives）。
    2.  **用户偏好偏差（User Preference Bias）**：不同场景下用户产生反馈的倾向不同（例如：知识问答中用户倾向于复制答案，而闲聊中则很少复制），导致未反馈样本中包含正偏好的概率在不同样本间是不均等的。

### 2. 方法论：ImplicitRM 框架
*   **核心思想**：通过**样本分层（Stratification）**和**变分推断（Variational Inference）**，将观测到的隐式反馈建模为潜在偏好和行动倾向的共同结果，从而推导出理论上的无偏学习目标。
*   **关键技术细节**：
    *   **四组分层模型**：将样本划分为四个潜在组：正向且活跃（PA）、负向且活跃（NA）、正向且被动（PP）、负向且被动（NP）。只有 PA 组会产生观测到的反馈（如 $r=1$）。
    *   **概率分层模型**：引入偏好估计器 $\hat{r}^*_\theta$ 和行动倾向估计器 $\hat{a}_\psi$，利用贝叶斯定理计算每个样本属于上述四个组的后验概率。
    *   **学习目标**：通过最大化观测数据的边际对数似然，推导出证据下界（ELBO）。最终形成两个解耦的目标函数：一个用于优化奖励模型参数 $\theta$，另一个用于优化倾向性模型参数 $\psi$。
    *   **理论保证**：论文证明了当分层概率估计准确时，该目标函数是理想目标（基于真实标签的目标）的**无偏估计量**。
*   **算法流程**：交替进行“估计组别成员概率”和“梯度下降更新模型参数”，并对概率估计部分使用 `stop-gradient` 操作以保证训练稳定性。

### 3. 实验设计
*   **数据集**：
    *   **HelpSteer**：NVIDIA 提供的多属性帮助度数据集。
    *   **UltraFeedback**：包含 6.4 万个 Prompt 的大规模偏好数据集。
    *   **PKU-SafeRLHF**：侧重于安全对齐的数据集。
*   **对比方法（Baselines）**：
    *   **朴素方法**：将未反馈直接视为负样本。
    *   **偏误学习方法**：如 IPS、DR、SDR 等。
    *   **正样本未标记（PU）学习方法**：如 BPR、NNPU、LaGAM 等。
    *   **标签噪声学习方法**：如 ILDE、SelectMix。
*   **下游任务**：使用训练好的 RM 通过 GRPO 算法对 Qwen2.5-7B 和 Qwen3-4B 进行安全对齐微调。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到使用了 LLaMA3-RM-v0.1 作为骨干网络，训练周期最高为 600 Epochs，采用了早停机制（Patience=30），Batch Size 在 64 到 2048 之间调整。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流数据集上进行了性能对比。
    *   进行了详尽的**消融实验**，验证了处理“假负例”和“偏好偏差”的必要性。
    *   进行了**泛化性分析**，涵盖了从 8B 到 32B 不同规模的 Qwen3 和 LLaMA2 模型。
    *   进行了**超参数敏感性分析**（学习率、Batch Size、反馈比例 $\alpha$）。
    *   进行了**下游 RLHF 验证**，并在 HarmBench 等三个安全基准上评估。
*   **充分性评价**：实验设计非常全面，不仅验证了 RM 本身的准确性，还验证了其在实际强化学习对齐中的有效性，对比基线涵盖了相关领域的最新方法，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **ImplicitRM 显著优于现有方法**：在所有数据集和指标（RMSE, MAE, $R^2$）上均取得 SOTA 性能。
*   **偏差处理至关重要**：消融实验显示，如果不处理用户偏好偏差或假负例，模型性能会大幅下降。
*   **模型规模增益**：随着 Backbone 规模增大，ImplicitRM 带来的绝对性能提升依然显著。
*   **下游对齐效果好**：使用 ImplicitRM 训练的策略模型在安全对齐测试中表现出更强的拒绝诱导性攻击的能力，且不会产生“复读机”式的简单拒绝。

### 7. 优点
*   **理论严密**：提供了严谨的数学证明，确保了学习目标的无偏性。
*   **成本效益高**：为利用海量、廉价的日志数据进行 LLM 对齐开辟了可行路径。
*   **模型无关性**：该框架可以适配任何 LLM 架构，具有很强的通用性。
*   **解决现实痛点**：直接针对隐式反馈中“未反馈不代表不喜欢”这一核心矛盾给出了优雅的解法。

### 8. 不足与局限
*   **反馈噪声假设**：目前假设观测到的反馈（如点击）一定是正向的，未考虑“误点”或“负向点击”等噪声情况。
*   **架构探索有限**：主要关注损失函数的改进，未深入探讨如混合专家模型（MoE）等特殊架构在联合建模奖励和倾向性方面的潜力。
*   **算力细节缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
