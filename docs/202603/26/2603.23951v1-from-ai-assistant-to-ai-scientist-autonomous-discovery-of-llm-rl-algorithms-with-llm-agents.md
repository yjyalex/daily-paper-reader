---
title: "From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents"
title_zh: 从 AI 助手到 AI 科学家：利用 LLM 智能体自主发现 LLM-RL 算法
authors: "Sirui Xia, Yikai Zhang, Aili Chen, Siye Wu, Siyu Yuan, Yanghua Xiao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23951v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 自主发现用于数学推理的LLM强化学习算法
tldr: 本研究针对大语言模型强化学习算法设计成本高、依赖人工的问题，提出了 POISE 自动化发现框架。该框架利用 LLM 智能体在闭环系统中进行算法提案、代码实现、标准评估及反思迭代，并维护结构化存档以复用经验。在数学推理实验中，POISE 从 GRPO 出发，自主发现了分析方差缩放和有效性掩码等改进机制，显著提升了模型性能，展示了从 AI 助手向 AI 科学家转型的潜力。
selection_source: fresh_fetch
motivation: 现有的语言模型策略优化算法发现过程高度依赖人工干预，且机制与训练动态耦合复杂，导致迭代效率低下。
method: 提出 POISE 框架，通过维护包含提案、代码、评估和反思的结构化谱系存档，实现算法的闭环自主搜索。
result: "实验表明 POISE 发现的算法变体在 AIME25 任务上的 pass@32 准确率从 26.7% 大幅提升至 43.3%。"
conclusion: 自动化算法发现不仅可行，且能产出具有可解释性设计原则的高性能强化学习机制。
---

## 摘要
为语言模型发现改进的策略优化算法仍然是一个昂贵的人工过程，需要反复进行机制层面的修改和验证。与简单的组合代码搜索不同，该问题需要在与训练动态紧密耦合的算法机制空间中进行搜索，同时在迭代过程中复用经验证据。我们提出了 POISE，这是一个用于自动发现语言模型策略优化算法的闭环框架。POISE 维护一个结构化的、具有谱系关联的归档库，将提议、可执行实现、标准化评估和自然语言反思联系起来，以支持证据驱动的迭代。在以 GRPO 为起点的数学推理实验中，POISE 评估了 64 个候选算法，并发现了改进的机制，包括解析方差缩放（analytic-variance scaling）和有效性掩码（validity masking）。最佳变体将加权综合得分（weighted Overall）从 47.8 提升至 52.5（+4.6），并将 AIME25 pass@32 从 26.7% 提高到 43.3%，证明了自动策略优化发现的可行性，同时支持可解释的设计原则。

## Abstract
Discovering improved policy optimization algorithms for language models remains a costly manual process requiring repeated mechanism-level modification and validation. Unlike simple combinatorial code search, this problem requires searching over algorithmic mechanisms tightly coupled with training dynamics while reusing empirical evidence across iterations. We propose POISE, a closed-loop framework for automated discovery of policy optimization algorithms for language models. POISE maintains a structured, genealogically linked archive linking proposals, executable implementations, standardized evaluations, and natural-language reflections to support evidence-driven iteration. In mathematical reasoning experiments starting from GRPO, POISE evaluates 64 candidate algorithms and discovers improved mechanisms, including analytic-variance scaling and validity masking. The best variant improves weighted Overall from 47.8 to 52.5 (+4.6) and increases AIME25 pass@32 from 26.7% to 43.3%, demonstrating the feasibility of automated policy optimization discovery while supporting interpretable design principles.

---

## 论文详细总结（自动生成）

这是一份关于论文《From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前大语言模型（LLM）的策略优化算法（如 PPO, GRPO）的设计高度依赖人工经验。研究人员需要反复手动修改损失函数、优势估计和正则化机制，并进行耗时的验证。
*   **核心问题**：能否利用 LLM 智能体自主发现新的、更高效的语言模型强化学习（RL）算法？
*   **整体含义**：该研究旨在推动 AI 从“执行任务的助手”向“能够进行科学发现的科学家”转型，通过自动化闭环系统探索复杂的算法设计空间。

### 2. 方法论：POISE 框架
POISE（Policy Optimization through Iterative Search and Evidence）是一个闭环的自动化发现框架，其核心思想是将算法搜索建模为**认识论进化搜索（Epistemic Evolutionary Search）**。

*   **结构化谱系存档（Lineage Archive）**：系统维护一个包含算法提案、可执行代码、标准化评估结果和自然语言反思的数据库。每个新算法都与其“祖先”关联，确保经验证据可以跨迭代复用。
*   **三个核心阶段**：
    1.  **提案生成（Phase I）**：利用 LLM 智能体基于存档证据和文献先验生成新算法。引入高斯过程（GP）作为获取函数，平衡“当前表现好”的算法与“具有进化潜力”的算法。
    2.  **实现与验证（Phase II）**：将抽象提案转化为可执行代码。通过共享接口和验证循环确保代码实现忠实于设计意图。
    3.  **反思分析与更新（Phase III）**：在标准化环境下训练并评估模型，LLM 智能体分析训练轨迹（如熵、奖励曲线）并生成机制层面的诊断报告，随后更新存档。

### 3. 实验设计
*   **基准算法（Baseline）**：以 **GRPO**（Group Relative Policy Optimization）为进化起点。
*   **任务场景**：专注于**数学推理**任务，因为其反馈清晰且对逻辑优化敏感。
*   **数据集**：
    *   **训练集**：MATH 数据集（Level 3-5）中抽取的 5000 个样本。
    *   **评估集**：AIME24、AIME25、AMC、MATH-500、Minerva、OlympiadBench。
*   **对比方法**：通过 POISE 自动生成的 63 个算法变体，以及原始 GRPO。

### 4. 资源与算力
*   **硬件资源**：使用了 **8 张 80GB NVIDIA A100 GPU**。
*   **模型配置**：
    *   **策略模型**：Qwen2.5-Math-1.5B（用于快速迭代）。
    *   **推理/Agent 引擎**：Gemini-1.5-Pro-preview（负责提案、代码编写和反思）。
*   **训练参数**：Global Batch Size 为 256，训练 8 个 Epoch，最大响应长度 3000 token。
*   **训练时长**：文中未明确给出 64 组实验的总耗时，但提到每个候选算法都需要进行完整的端到端训练和评估。

### 5. 实验数量与充分性
*   **实验规模**：共评估了 **64 个算法变体**。
*   **充分性**：
    *   **多维度评估**：不仅关注最终准确率，还引入了“长度压缩约束”实验，验证系统在特定指令（如“更简洁地回答”）下的引导能力。
    *   **深度分析**：对进化谱系进行了深度分析，展示了算法是如何从简单的稳定化尝试进化到复杂的信号建模。
*   **客观性**：所有变体在统一的硬件、超参数和评估协议下运行，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能突破**：发现的最佳变体 **VM-AV-GRPO** 将加权综合得分从 47.8 提升至 **52.5 (+4.6)**。在极难的 **AIME25** 任务上，pass@32 从 26.7% 大幅提升至 **43.3%**。
*   **新机制发现**：
    *   **解析方差缩放（AV）**：用理论方差代替经验方差，放大稀疏成功信号。
    *   **有效性掩码（VM）**：排除格式错误样本对优势估计的干扰。
*   **设计原则提取**：总结出“信号解耦”、“条件归一化”和“正确性优先的效率塑造”等可解释的算法设计原则。

### 7. 优点
*   **闭环自动化**：实现了从想法到代码再到反思的全流程自动化，显著降低了人工干预成本。
*   **证据驱动而非随机搜索**：通过自然语言反思和谱系存档，系统能够“理解”失败原因并有针对性地改进，而非盲目组合代码。
*   **可解释性**：系统产出的不仅是代码，还有关于“为什么该机制有效”的科学论述。

### 8. 不足与局限
*   **计算成本极高**：每个候选算法都需要完整训练，对于大规模模型或更多迭代次数来说，算力开销巨大。
*   **领域局限性**：实验目前集中在数学推理领域，其发现的机制是否能泛化到开放域对话、代码生成或工具调用任务尚待验证。
*   **因果识别风险**：虽然有反思诊断，但目前的系统仍难以完全排除超参数波动带来的干扰，对机制与性能之间的因果关系解释仍属于“强力假设”。

（完）
