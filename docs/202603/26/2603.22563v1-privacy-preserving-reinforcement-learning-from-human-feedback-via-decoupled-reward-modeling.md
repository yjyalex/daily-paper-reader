---
title: Privacy-Preserving Reinforcement Learning from Human Feedback via Decoupled Reward Modeling
title_zh: 通过解耦奖励建模实现隐私保护的基于人类反馈的强化学习
authors: "Young Hyun Cho, Will Wei Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22563v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过解耦奖励建模实现隐私保护的RLHF
tldr: 针对大语言模型基于人类反馈的强化学习（RLHF）中偏好数据包含敏感隐私的问题，本文提出了一种解耦奖励建模的差分隐私框架。该方法仅在奖励学习阶段施加差分隐私保护，并基于得到的私有奖励模型推导最终策略。理论上分析了次优性间隙并确立了极小极大下界，实验证明该方法在多个隐私预算下均优于现有基线，实现了更强的隐私对齐性能。
selection_source: fresh_fetch
motivation: 旨在解决RLHF微调阶段偏好数据可能泄露用户敏感隐私信息的安全挑战。
method: 提出一种仅在奖励建模阶段实施差分隐私保护，并利用该私有模型指导策略生成的解耦框架。
result: 理论推导了隐私保护下的次优性界限，并在Gemma-2B-IT模型上验证了其性能优于现有差分隐私基线。
conclusion: 该研究为隐私保护下的RLHF提供了理论支撑和高效的实践方案，证明了通过解耦奖励建模可实现更好的隐私与性能权衡。
---

## 摘要
基于偏好的微调已成为训练大语言模型的重要组成部分，而这一阶段使用的数据可能包含敏感的用户信息。一个核心问题是如何设计一个非常适合基于人类反馈的强化学习（RLHF）独特结构的差分隐私流水线。我们提出了一个隐私保护框架，该框架仅对奖励学习施加差分隐私，并从生成的隐私奖励模型中推导出最终策略。在理论上，我们研究了次优性间隙（suboptimality gap），并表明隐私在通常的非隐私统计误差之外贡献了一个额外的加性项。我们还建立了一个极小极大下界（minimax lower bound），并表明主导项随样本量和隐私水平而变化，这反过来刻画了上界在对数因子意义下达到率最优（rate-optimal）的区间。在实证方面，合成实验证实了理论预测的缩放规律，而在 Anthropic HH-RLHF 数据集上使用 Gemma-2B-IT 模型的实验表明，在各种隐私预算下，该方法比现有的差分隐私基准方法具有更强的隐私对齐性能。

## Abstract
Preference-based fine-tuning has become an important component in training large language models, and the data used at this stage may contain sensitive user information. A central question is how to design a differentially private pipeline that is well suited to the distinct structure of reinforcement learning from human feedback. We propose a privacy-preserving framework that imposes differential privacy only on reward learning and derives the final policy from the resulting private reward model. Theoretically, we study the suboptimality gap and show that privacy contributes an additional additive term beyond the usual non-private statistical error. We also establish a minimax lower bound and show that the dominant term changes with sample size and privacy level, which in turn characterizes regimes in which the upper bound is rate-optimal up to logarithmic factors. Empirically, synthetic experiments confirm the scaling predicted by the theory, and experiments on the Anthropic HH-RLHF dataset using the Gemma-2B-IT model show stronger private alignment performance than existing differentially private baseline methods across privacy budgets.

---

## 论文详细总结（自动生成）

### 论文总结：通过解耦奖励建模实现隐私保护的 RLHF

#### 1. 核心问题与研究动机
论文关注大语言模型（LLM）在对齐阶段（RLHF）的隐私泄露风险。RLHF 依赖于包含“用户提示词（Prompt）、候选回答、偏好标签”的三元组数据，这些数据往往包含敏感的个人信息。
*   **现有挑战**：现有的差分隐私（DP）对齐方法（如 DP-DPO 或 DP-PPO）通常直接对策略参数进行隐私化更新。由于策略梯度涉及分母可能极小的得分函数（Score Function），导致梯度不稳定且对剪切（Clipping）极其敏感，往往造成模型性能大幅下降，甚至优于基准模型（Reference Model）的效果都难以维持。
*   **核心目标**：设计一种既能提供元组级（Tuple-level）隐私保护，又能保持模型效用的 RLHF 流水线。

#### 2. 方法论：解耦奖励建模框架
论文提出了一种**解耦（Decoupled）**的思想，将隐私预算集中在奖励建模阶段，而将策略改进视为“后处理”。
*   **核心思想**：利用差分隐私的“后处理不变性”原理。只要奖励模型 $r$ 是通过 DP 机制训练的，那么基于该模型生成的任何策略 $\pi$ 都自动满足相同的 DP 级别，无需在策略训练阶段额外消耗隐私预算。
*   **关键步骤**：
    1.  **私有奖励学习**：在偏好数据集上使用 DP-SGD 训练一个奖励模型 $\tilde{r}$。由于奖励模型通常采用线性头（Linear Head）结构，其梯度更易界定且对剪切更鲁棒。
    2.  **策略推导（后处理）**：
        *   **精确推导**：在动作空间较小时，直接根据 Gibbs 分布形式生成最优策略。
        *   **Best-of-N 采样**：在 LLM 等大动作空间场景下，从参考模型 $\pi_0$ 中采样 $N$ 个候选，利用私有奖励模型 $\tilde{r}$ 挑选得分最高者。
*   **理论贡献**：推导了次优性间隙（Suboptimality Gap）的上界，并首次确立了该问题的极小极大下界（Minimax Lower Bound），证明了该方法在特定区间内达到了率最优（Rate-optimal）。

#### 3. 实验设计
*   **数据集与场景**：
    1.  **合成数据**：受控的线性奖励环境，用于验证理论中的缩放规律（样本量 $n$、维度 $d$、隐私预算 $\epsilon$、正则化系数 $\eta$）。
    2.  **LLM 任务**：使用 **Anthropic HH-RLHF** 数据集（包含 40,000 条对话对），评估模型在“有用性”和“无害性”上的对齐表现。
*   **对比方法（Benchmarks）**：
    1.  **DP-DPO**：直接在 DPO 目标函数上应用 DP-SGD。
    2.  **DP-RLHF**：将数据平分，一半用于训练私有奖励模型，另一半用于私有策略优化（类似 PPO 的离线变体）。
*   **评估指标**：次优性间隙、奖励准确率（Reward Accuracy）、胜率（Win Rate）。

#### 4. 资源与算力
*   **硬件环境**：实验明确提到使用了 **单张 NVIDIA A100 GPU**。
*   **训练细节**：使用了 `Opacus` 库进行隐私会计（Privacy Accounting）。在 LLM 实验中，采用了 LoRA 这种参数高效微调技术（仅更新最后一块 Transformer 层的约 10 万个参数），逻辑 Batch Size 为 64，物理微 Batch Size 为 8。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   合成实验针对每个配置进行了 **30 次独立试验**，并绘制了 95% 置信区间。
    *   LLM 实验针对每个隐私预算（$\epsilon \in \{0.5, 1.0, 2.0\}$）使用了 **3 个不同的随机种子**。
*   **充分性评价**：实验设计较为充分且客观。涵盖了从理论验证到实际模型应用的完整链路，并对关键超参数（如剪切阈值 $C$、正则化强度 $\eta$）进行了敏感性分析，公平地对比了不同方法在相同隐私预算下的表现。

#### 6. 主要结论与发现
*   **隐私成本是加性的**：理论和实验均证明，隐私引入的误差是在非隐私统计误差基础上的一个额外加性项。
*   **解耦框架更稳定**：在严格的隐私预算下，解耦框架避免了直接对策略进行隐私更新带来的不稳定性。
*   **性能优越性**：在 HH-RLHF 任务中，该方法在所有隐私预算下均显著优于 DP-DPO 和 DP-RLHF。例如，在 $\epsilon=0.5$ 时，该方法的奖励准确率接近 59%，而基线方法的胜率仅略高于随机猜测（约 52-53%）。
*   **避免性能倒退**：该方法能有效减少训练出的模型比原始参考模型更差（Reference Underperformance）的情况。

#### 7. 优点与亮点
*   **结构化创新**：不同于简单地将 DP 套用到现有算法，该方法利用了 RLHF 特有的两阶段结构，通过“隐私化奖励+非隐私化推理”实现了效率最大化。
*   **理论完备**：提供了详尽的收敛性分析和极小极大下界证明，填补了隐私 RLHF 理论研究的空白。
*   **实践友好**：Best-of-N 的推理策略无需复杂的强化学习训练循环，降低了部署门槛。

#### 8. 不足与局限
*   **表示能力受限**：LLM 实验中仅微调了线性头（Linear Head），虽然稳定，但可能限制了模型捕捉复杂偏好的上限。
*   **推理开销**：Best-of-N 策略在推理时需要生成多个候选回答并进行评分，相比于直接生成，计算成本更高。
*   **覆盖性假设**：理论分析依赖于“覆盖性假设（Coverage Assumption）”，即参考模型需要能覆盖目标策略可能访问的状态空间，这在分布偏移较大时可能失效。

（完）
