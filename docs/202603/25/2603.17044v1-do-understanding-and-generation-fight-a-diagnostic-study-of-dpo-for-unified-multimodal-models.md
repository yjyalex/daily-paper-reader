---
title: Do Understanding and Generation Fight? A Diagnostic Study of DPO for Unified Multimodal Models
title_zh: 理解与生成是否冲突？统一多模态模型 DPO 的诊断性研究
authors: "Abinav Rao, Sujan Rachuri"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17044v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: DPO用于多模态模型对齐的诊断研究
tldr: 本研究探讨了DPO在统一多模态模型中同时对齐理解与生成的效果。实验发现生成质量在多种策略下均难以提升，甚至在小模型上出现退化。梯度分析揭示理解与生成梯度近乎正交且存在11-14倍的量级失衡，这主要由VQ标记数量不对称引起。研究指出离散VQ分词是导致生成对齐失效的结构性瓶颈，并为相关模型开发提供了实践建议。
selection_source: fresh_fetch
motivation: 探究DPO能否在共享骨干网络的统一多模态模型中同时实现理解与生成能力的有效对齐。
method: 在Janus-Pro模型上系统评估了多种DPO训练策略，并对理解与生成的梯度冲突进行了定量分析。
result: DPO无法改善生成质量，且两类任务的梯度呈正交关系并存在由VQ标记不对称导致的显著量级失衡。
conclusion: 离散VQ分词是统一模型生成对齐的结构性障碍，开发者需关注多任务对齐中的梯度干扰问题。
---

## 摘要
统一多模态模型共享一个语言模型主干，用于图像的理解和生成。DPO 能否同时对齐这两种能力？我们对这一问题进行了首次系统性研究，在七种训练策略和两种事后（post-hoc）方法下，将 DPO 应用于 1B 和 7B 参数规模的 Janus-Pro 模型。核心结论是负面的：在该架构下，生成质量在所有测试条件下都表现出对 DPO 对齐的抗性。在 7B 规模下，没有任何方法能提高生成的 CLIPScore（|Delta| < 0.2, p > 0.5，每个种子 n=200，共 3 个种子）；在 1B 规模下，所有方法都会导致生成质量下降，且这一结论在不同的偏好数据类型（真实 vs 生成，以及模型 vs 模型）和测试的数据量（150-288 对）中均成立。梯度分析揭示了原因：理解和生成的梯度接近正交（cos ~ 0），且由于 VQ token 数量的不对称（576 个生成 token 对比约 30-100 个文本 token），两者存在约 11-14 倍的量级失衡。这种失衡是多任务 DPO 中的主要干扰机制；量级平衡虽然带来了方向性正向的理解增量（VQA 提升 +0.01-0.04，尽管单项并不显著），但生成能力的差距依然存在。我们认为离散 VQ 分词（tokenization）可能是一个结构性瓶颈——生成 DPO 损失收敛于 ln(2) 支持了这一观点——并为使用基于 VQ 的统一模型的从业者提供了实践指导。

## Abstract
Unified multimodal models share a language model backbone for both understanding and generating images. Can DPO align both capabilities simultaneously? We present the first systematic study of this question, applying DPO to Janus-Pro at 1B and 7B parameters under seven training strategies and two post-hoc methods. The central finding is negative: generation quality resists DPO alignment across all tested conditions on this architecture. No method improves generation CLIPScore at 7B (|Delta| < 0.2, p > 0.5 at n=200 per seed, 3 seeds); at 1B, all methods degrade generation, and the result holds across preference data types (real-vs-generated and model-vs-model) and the data volumes tested (150-288 pairs). Gradient analysis reveals why: understanding and generation gradients are near-orthogonal (cos ~ 0) with ~11-14x magnitude imbalance driven by VQ token count asymmetry (576 generation tokens vs. ~30-100 text tokens). This imbalance is the dominant interference mechanism in multi-task DPO; magnitude-balancing yields directionally positive understanding deltas (+0.01-0.04 VQA, though individually not significant), but the generation gap persists regardless. We identify discrete VQ tokenization as a likely structural bottleneck -- supported by the generation DPO loss converging to ln(2) -- and provide practical guidance for practitioners working with VQ-based unified models.

---

## 论文详细总结（自动生成）

### 论文总结：理解与生成是否冲突？统一多模态模型 DPO 的诊断性研究

#### 1. 核心问题与整体含义
本文探讨了在**统一多模态模型**（Unified Multimodal Models，如 Janus-Pro）中，是否可以通过**直接偏好优化（DPO）**同时对齐图像理解（VQA）和图像生成能力。研究动机在于，这类模型共享同一个语言模型（LLM）骨干网络，优化一个任务可能会干扰另一个任务。论文通过系统性实验和梯度分析，旨在揭示多任务对齐中的干扰机制及 DPO 在离散视觉标记（VQ tokens）生成任务中的局限性。

#### 2. 方法论
论文提出了一套诊断性框架和改进的训练策略：
*   **多任务 DPO 公式化**：将理解任务（文本序列）和生成任务（VQ 标记序列）的 DPO 损失结合。
*   **梯度分析框架**：通过计算理解梯度（$g_U$）与生成梯度（$g_G$）的**余弦相似度**（衡量方向冲突）和**量级比例**（衡量主导地位）来诊断干扰。
*   **梯度加权平衡 DPO（Balanced DPO）**：
    *   **动态权重**：借鉴 GradNorm，根据梯度量级实时调整任务权重，使 $w_U \|g_U\| = w_G \|g_G\|$。
    *   **静态权重**：根据序列长度比例（VQ 标记数 vs. 文本标记数）预设固定权重。
*   **对比策略**：包括单任务 DPO、朴素联合训练、PCGrad（梯度投影消除冲突）以及 Rewarded Soups（模型权重插值）。

#### 3. 实验设计
*   **模型架构**：使用 Janus-Pro（1B 和 7B 参数版本），其特点是理解和生成使用不同的编码器但共享 LLM 骨干。
*   **数据集**：
    *   **理解偏好**：基于 COCO 图像生成的 1,300 对 VQA 问答对。
    *   **生成偏好**：288 对“真实图像 vs. 模型生成图像”对，以及 153 对“模型 vs. 模型”对。
*   **基准指标（Benchmarks）**：
    *   **理解**：VQA 内容重叠度（0-6 分）、VQAv2 准确率、POPE 幻觉测试。
    *   **生成**：CLIPScore（核心指标）、FID、PickScore、LPIPS 多样性。
*   **对比方法**：共测试了 7 种训练策略和 2 种事后处理方法。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   Janus-Pro-7B：使用 1 台 **NVIDIA H100 80GB**。
    *   Janus-Pro-1B：使用 1 台 **NVIDIA A100 40GB**。
*   **训练细节**：每个实验运行 1,000 个 step，batch size 为 1（为了进行样本级梯度分析）。
*   **时长**：文中未明确总时长，但提到梯度权重每 50 步重新计算一次。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个模型参数量级（1B/7B）、多种 DPO 温度（$\beta$）消融、数据规模消融（150-288 对）以及不同的偏好数据构造方式。
*   **充分性与客观性**：实验设计较为充分，每个配置均使用 3 个随机种子取平均值，并进行了统计显著性检验（Welch’s t-test）。通过“空假设校准”验证了梯度的正交性，确保了结论的客观性。

#### 6. 主要结论与发现
*   **生成对齐失效（负面结果）**：在所有测试条件下，DPO 均无法提升 Janus-Pro 的生成质量（CLIPScore 提升不显著）。在 1B 模型上，DPO 甚至导致生成能力退化。
*   **梯度量级严重失衡**：生成梯度比理解梯度大 **11-14 倍**。这是由于生成任务涉及 576 个 VQ 标记，而理解任务仅涉及约 30-100 个文本标记。
*   **梯度正交性**：理解与生成的梯度余弦相似度接近 0，意味着它们在参数空间中几乎互不干扰，但量级失衡导致理解信号被淹没。
*   **结构性瓶颈**：生成 DPO 损失始终收敛于 $\ln 2$（无信息基准），表明离散 VQ 标记化使得模型难以在 token 级别区分图像质量的优劣。
*   **实践建议**：仅对理解任务进行 DPO 是安全且有效的；若进行多任务训练，必须进行量级平衡（推荐权重 $w_U \approx 0.92$）。

#### 7. 优点
*   **深入的底层诊断**：不仅报告结果，还通过梯度分析从数学层面解释了多任务干扰的本质。
*   **稳健的负面结论**：通过多种策略、模型规模和数据构造方式反复验证，证明了生成对齐失效的普遍性。
*   **实用性强**：为开发者提供了具体的权重设置建议和架构选择指导（如建议转向连续表示架构）。

#### 8. 不足与局限
*   **模型单一性**：研究仅限于 Janus-Pro 这一种架构家族，尚未在 Chameleon 或 Emu3 等其他统一模型上验证。
*   **数据规模较小**：偏好数据集仅包含几百对样本，虽然消融实验显示增加数据未改变趋势，但更大规模（如 >10K）的效果仍属未知。
*   **方法局限**：主要针对离线 DPO，未深入探讨在线 RL（如 PPO/GRPO）是否能克服 VQ 瓶颈。

（完）
