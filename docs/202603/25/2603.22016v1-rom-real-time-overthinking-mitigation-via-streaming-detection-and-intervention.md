---
title: "ROM: Real-time Overthinking Mitigation via Streaming Detection and Intervention"
title_zh: ROM：通过流式检测与干预实现实时过度思考缓解
authors: "Xinyan Wang, Xiaogeng Liu, Chaowei Xiao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22016v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 减轻长思维链推理轨迹中的过度思考问题
tldr: 针对大推理模型在得出正确答案后仍生成冗余推理的“过度思考”问题，本文提出ROM框架。ROM通过在冻结的LLM隐藏层添加轻量级检测头，实现流式实时监控，并在检测到过度思考时强制模型提前输出最终答案。该方法引入了基于正确性边界的标记级监督和数据增强策略，在显著降低推理延迟和计算成本的同时，有效避免了答案漂移。
selection_source: fresh_fetch
motivation: 大推理模型常因生成冗余推理导致高延迟和计算浪费，而现有方法难以在不修改模型骨干的情况下精准识别并拦截过度思考。
method: 在冻结模型的后期隐藏层附加轻量级检测头，通过流式监控实时预测过度思考状态，并结合标记级监督和数据增强进行干预。
result: "ROM在七个基准测试中取得了93.51%的最高准确率，响应长度缩短47.2%，推理效率提升了121%。"
conclusion: 实时流式检测与干预是解决大模型过度思考、优化推理效率且不损失性能的有效途径。
---

## 摘要
大型推理模型 (LRMs) 通过生成长思维链 (Chain-of-Thought) 轨迹在挑战性任务上取得了极高的准确率，但却面临“过度思考”的问题。即使在得出正确答案后，它们仍会继续生成冗余的推理步骤。这种行为增加了延迟和计算成本，还可能导致答案偏移。现有的缓解方法要么需要对主干网络进行繁重的训练修改，要么依赖于无法真实捕捉过度思考模式的人工启发式规则。我们提出了 ROM，这是首个将过度思考缓解建模为流式预测与控制问题的方法。ROM 在冻结的大语言模型主干网络的深层隐藏状态上附加了一个轻量级检测头。它实时监控 Token，并在检测到过度思考时触发提前跳转至最终答案。我们还引入了基于解题正确性边界的 Token 级监督，以及一种减少蒸馏数据偏差的数据增强策略。在七个基准测试中，ROM 实现了最高的准确率 (93.51%)、最短的响应长度 (1,159 tokens) 以及最佳的响应效率。与原始基线相比，它将响应长度缩短了 47.2%，并将效率提升了 121%。这些结果表明，流式检测是实现实时过度思考缓解的一种极具前景的方法。

## Abstract
Large Reasoning Models (LRMs) achieve strong accuracy on challenging tasks by generating long Chain-of-Thought traces, but suffer from overthinking. Even after reaching the correct answer, they continue generating redundant reasoning steps. This behavior increases latency and compute cost and can also lead to answer drift. Existing mitigation methods either require training-heavy backbone modification or rely on hand-crafted heuristics that do not truly capture overthinking patterns. We propose ROM, the first method that formulates overthinking mitigation as a streaming prediction-and-control problem. ROM attaches a lightweight detection head to the late-layer hidden states of a frozen large language model backbone. It monitors tokens in real time and triggers an early transition to the final answer once overthinking is detected. We also introduce token-level supervision based on solution correctness boundaries and a data augmentation strategy that reduces distilled-data bias. Across seven benchmarks, ROM achieves the highest accuracy (93.51%), the shortest responses (1,159 tokens), and the best response efficiency. Compared with the vanilla baseline, it reduces response length by 47.2% and improves efficiency by 121%. These results show that streaming detection is a promising approach to real-time overthinking mitigation.

---

## 论文详细总结（自动生成）

### 论文总结：ROM (Real-time Overthinking Mitigation)

#### 1. 核心问题与整体含义
*   **研究背景**：大型推理模型（LRMs，如 OpenAI o1、DeepSeek-R1）通过长思维链（CoT）显著提升了复杂任务的准确率。
*   **核心问题**：模型普遍存在“过度思考”（Overthinking）现象，即在已经得出正确答案后，仍继续生成冗余的推理步骤、重复验证或无效尝试。这不仅导致了极高的推理延迟和计算成本，还可能引发“答案漂移”（Answer Drift），即冗余推理反而将原本正确的结论改错。
*   **研究动机**：现有的缓解方法要么需要对模型主干进行繁重的强化学习（RL）训练，要么依赖于不可靠的人工启发式规则（如监测熵值）。本文旨在开发一种轻量级、即插即用且能实时拦截冗余生成的方案。

#### 2. 方法论
ROM 将过度思考缓解建模为一个**流式预测与控制问题**，其核心组件包括：
*   **轻量级检测头（Streaming Detector）**：在冻结的 LLM 主干网络（如 Qwen3-8B）的深层隐藏状态（Layer 32）上附加一个仅 8.13M 参数的小型神经网络。
*   **时序建模技术**：
    *   **注意力池化（Attention Proj）**：压缩前缀隐藏状态以提取全局上下文。
    *   **CfC 单元（Closed-form Continuous-time）**：一种连续时间循环神经网络，用于捕捉 Token 生成过程中的动态相位变化，输出当前 Token 是否属于“过度思考”的概率 $p_t$。
*   **正确性边界标记与 CSC 数据增强**：
    *   **标记规则**：以“第一次出现正确答案”（FCS）为界，之后的 Token 全部标记为过度思考。
    *   **反事实自我修正（CSC）**：针对蒸馏数据中“第一遍就对”的偏差，人工合成“错误推理 -> 自我修正 -> 正确答案”的轨迹，训练检测头识别真正的自我修正，避免误杀有益的推理。
*   **边界感知干预（Intervention）**：一旦 $p_t > 0.5$，系统触发干预。为了保持输出格式完整，ROM 会**回溯（Backtracing）**到最近的清洁边界（如换行符），然后强制模型跳转至最终答案输出。

#### 3. 实验设计
*   **数据集**：涵盖 6 个数学/算术推理任务（MATH500, GSM8K, ASDiv, MAWPS, MultiArith, SVAMP）和 1 个多领域选择题任务（MMLU-Pro）。
*   **基准模型（Backbone）**：Qwen3-8B。
*   **对比方法（Baselines）**：
    *   **Vanilla**：原始未干预模型。
    *   **模型类（Model-based）**：L1（基于强化学习的长度控制模型）。
    *   **启发式类（Heuristic）**：EAT（基于熵轨迹的提前退出）。
    *   **硬截断**：固定 Token 长度截断（Cut 512/1024/2048）。
*   **评价指标**：准确率（Acc）、响应长度（SL）、响应效率（SE = Acc/SL）。

#### 4. 资源与算力
*   **训练规模**：使用了极小规模的训练集（740 个高效推理样本和 793 个过度思考样本）。
*   **算力开销**：论文**未明确说明**具体的 GPU 型号和训练时长。但由于检测头参数量极小（仅占主干模型的 0.1%）且主干网络处于冻结状态，且采用了预计算隐藏状态缓存的技术，可以推断其训练成本极低，通常单卡 GPU 即可在短时间内完成。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的基准测试上进行了全面评估，并进行了详细的消融实验（验证 CSC 增强和回溯策略的作用）。
*   **充分性与公平性**：实验对比了当前最先进的 RL 方法（L1）和启发式方法（EAT），覆盖了从简单算术到复杂数学的梯度。通过跨领域（MMLU-Pro）测试验证了泛化性。实验设计较为客观，通过 SE 指标平衡了性能与成本的考量。

#### 6. 主要结论与发现
*   **性能卓越**：ROM 在保持最高准确率（93.51%）的同时，实现了最短的响应长度（1159 tokens）。
*   **效率大幅提升**：相比 Vanilla 基线，Token 消耗减少了 **47.2%**，推理效率提升了 **121%**。
*   **优于 RL 方法**：尽管 L1 使用了 40K 样本进行重度强化学习训练，ROM 仅凭 1.5K 样本的轻量级训练，在效率（SE）上仍领先 L1 约 52.7%。
*   **自适应性**：ROM 表现出“遇难则长，遇易则短”的特性，在简单算术任务上截断力度最大（如 ASDiv 缩短 64.9%）。

#### 7. 优点
*   **轻量且高效**：不修改原始模型权重，推理额外开销极低（每 Token 延迟增加 < 0.5ms）。
*   **数据驱动**：相比人工设定的阈值，ROM 能够真正学习到过度思考的语义模式。
*   **鲁棒性强**：CSC 策略有效解决了自我修正过程中的误判问题，回溯机制保证了输出格式的整洁。

#### 8. 不足与局限
*   **标注依赖**：数据生成流程高度依赖于高性能模型（如 GPT-4o）进行正确性标注，标注质量直接影响检测头效果。
*   **Scaling Law 缺失**：实验发现增加训练数据并未带来显著的性能持续提升，方法在更大规模数据上的潜力尚不明确。
*   **应用限制**：目前主要针对具有清晰正确性边界的推理任务（数学、逻辑），在开放式创作或主观问答任务中的适用性有待验证。

（完）
