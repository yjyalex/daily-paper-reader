---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 针对大语言模型的离策价值强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:token"]
score: 10.0
evidence: 大模型中带有步骤信号的离策价值强化学习
tldr: 针对大语言模型（LLM）强化学习中策略梯度方法采样效率低的问题，本文提出了一种基于价值的离策（Off-Policy）强化学习框架 ReVal。该方法通过结合步进式内部一致性信号与轨迹级结果验证信号，利用 Bellman 更新和经验回放机制实现数据的多次复用。实验证明，ReVal 在数学推理等任务上比 GRPO 收敛更快且性能更优，为 LLM 训练提供了一种高效的替代方案。
selection_source: fresh_fetch
motivation: 现有的 LLM 强化学习方法多为同策（On-policy），导致数据利用率低且在长程任务中采样成本高昂。
method: 提出 ReVal 框架，通过结合步进式信号与轨迹级验证的 Bellman 更新，并利用经验回放缓冲区实现离策学习。
result: "在数学推理基准测试中，ReVal 的收敛速度和最终性能均优于 GRPO，在 AIME24 和 GPQA 任务上分别提升了 2.7% 和 4.5%。"
conclusion: 基于价值的离策强化学习是提升 LLM 训练效率和性能的一种切实可行且高效的路径。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后采集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架天然支持离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的步进式信号与源自结果验证的轨迹级信号。ReVal 天然支持基于经验回放池（replay-buffer）的训练，从而实现了对历史轨迹的高效复用。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，相比 GRPO 在 AIME24 上提升了 2.7%，在领域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中替代基于策略（policy-based）方法的一种实用方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Off-Policy Value-Based Reinforcement Learning for Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前大语言模型（LLM）的主流强化学习（RL）方法（如 PPO、GRPO）大多是**同策（On-policy）**的。这意味着每条生成的轨迹在进行一次参数更新后就会被丢弃，导致数据利用率极低。
*   **研究动机**：随着 LLM 任务向长程推理（Long-horizon）和智能体（Agentic）方向发展，生成长轨迹的计算成本极高。为了降低训练成本并提升性能，迫切需要一种能够实现**离策（Off-policy）**学习、即能够通过经验回放（Replay Buffer）多次复用历史数据的算法。
*   **整体含义**：本文旨在证明基于价值（Value-based）的 RL 框架是 LLM 训练中替代策略梯度方法的实用且高效的选择。

### 2. 方法论：ReVal 框架
*   **核心思想**：利用 LLM 自身的 Logits 作为 Q 函数（Logit-as-Q），从而在不增加额外价值网络（Value Model）的前提下，将策略和价值统一在单个模型中。
*   **关键技术细节**：
    *   **Logit-as-Q 参数化**：基于前人研究，将预训练 LLM 的 Logits 解释为内生奖励下的软 Q 值，解决了 Q 函数初始化难的问题。
    *   **校准初始化（Calibrated Initialization）**：针对前人方法（如 TBRM）在无奖励信号时会产生策略漂移的问题，ReVal 引入了**奖励塑造（Reward Shaping）**。通过在贝尔曼算子中加入参考策略（Reference Policy）的项，确保在奖励为零时，最优策略退化为参考策略。
    *   **ReVal 损失函数**：结合了步进式（Stepwise）的内部一致性信号和轨迹级（Trajectory-level）的结果验证信号，通过最小化轨迹级的贝尔曼残差进行优化。
    *   **经验回放机制**：引入 FIFO（先进先出）缓冲区存储历史轨迹，每轮迭代从缓冲区采样进行多次离策更新。

### 3. 实验设计
*   **模型对象**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen2.5-Math-7B。
*   **数据集与 Benchmark**：
    *   **训练集**：DeepScaleR 数据集。
    *   **评估集**：涵盖 AIME24、AIME25、AMC、MATH、MINERVA、Olympiad Bench 和 GPQA（领域外测试）。
*   **对比方法**：
    *   **GRPO**：目前主流的低成本策略优化方法（同策）。
    *   **TBRM**：基于轨迹贝尔曼残差的价值方法（但原版为同策）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100 或 A100）及具体卡数。
*   **训练效率数据**：
    *   训练迭代次数：650 次。
    *   时间对比：在 N=1（每步仅生成 1 条新轨迹）的设定下，ReVal 达到 SOTA 性能所需时间为 6.3 小时，而 GRPO 为 7.5 小时（训练时间缩短约 18%）。
    *   收敛速度：在单样本学习实验中，ReVal 相比 GRPO 实现了平均 **4.3 倍**的收敛加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在两个不同规模（1.5B 和 7B）和不同性质（推理型与通用型）的模型上进行了全基准测试。
    *   **消融实验**：深入探讨了参考策略更新频率、超参数 $\beta$（控制奖励强度）、负样本利用方式以及不同损失函数变体的稳定性。
    *   **极端设定**：测试了在极低采样率（N=1）下的表现。
*   **充分性评价**：实验设计较为充分且客观。通过跨模型、跨难度的对比，验证了离策复用在不同场景下的普适性。特别是针对“校准初始化”的理论证明与实验验证，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能超越**：ReVal 在几乎所有数学推理指标上均优于 GRPO。在 DeepSeek-R1-1.5B 上，GPQA 提升了 4.5%，AIME24 提升了 2.7%。
*   **离策优势**：数据复用频率越高，收敛越快。在样本稀缺（N=1）的情况下，ReVal 的优势更加显著，证明了其极高的样本效率。
*   **泛化能力**：在领域外（Out-of-domain）基准测试 GPQA 上的显著提升，表明 ReVal 学习到的推理模式具有更好的泛化性。
*   **稳定性**：通过奖励塑造和周期性重置参考策略，解决了价值学习在 LLM 训练中容易不稳定的问题。

### 7. 优点（亮点）
*   **高效率低成本**：实现了真正的离策学习，且不需要额外的价值模型，保持了与 Actor-only 方法相当的显存开销。
*   **理论完备性**：提出了“校准初始化”概念，并修正了现有价值学习方法的理论缺陷。
*   **实用性强**：在参数更新比自回归生成便宜得多的背景下，通过增加更新次数换取生成次数的减少，显著降低了总训练时长。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在数学推理等具有**可验证奖励（Verifiable Reward）**的任务上，尚未在缺乏客观标准（如创意写作、对话）的任务中验证。
*   **超参数敏感性**：实验显示超参数 $\beta$ 对稳定性影响较大，且与响应长度高度耦合，不同模型可能需要精细调优。
*   **回放策略简单**：目前仅使用简单的 FIFO 缓冲区，尚未探索优先经验回放（PER）等更高级的采样策略。
*   **硬件细节缺失**：缺乏具体的算力资源描述，不便于其他研究者评估复现所需的硬件门槛。

（完）
