---
title: A Context Engineering Framework for Improving Enterprise AI Agents based on Digital-Twin MDP
title_zh: 一种基于数字孪生 MDP 的企业级 AI 智能体改进上下文工程框架
authors: "Xi Yang, Aurelie Lozano, Naoki Abe, Bhavya, Saurabh Jha, Noah Zheutlin, Rohan R. Arora, Yu Deng, Daby M. Sow"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22083v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 离线强化学习用于改进基于大语言模型的智能体
tldr: 针对企业级AI智能体在复杂推理和数据质量方面的局限，本文提出DT-MDP-CE框架。该框架通过数字孪生马尔可夫决策过程（DT-MDP）抽象推理行为，利用对比逆强化学习从混合质量的离线轨迹中学习奖励函数与策略，并以此指导上下文工程，显著提升了智能体在IT自动化等任务中的决策性能。
selection_source: fresh_fetch
motivation: 企业级AI智能体在实际部署中面临数据质量差、推理复杂及缺乏可靠反馈信号等挑战。
method: 提出一种基于数字孪生MDP的上下文工程框架，结合对比逆强化学习从离线数据中提取策略并优化智能体决策。
result: 在IT自动化领域的实验表明，该框架在多种评估设置下均显著优于基准智能体。
conclusion: 该框架为提升企业级AI智能体性能提供了一种轻量级、模型无关且具有良好泛化性的解决方案。
---

## 摘要
尽管用于企业自动化和决策的 AI 智能体取得了快速进展，但其在现实世界中的部署和进一步的性能提升仍受限于数据质量和数量有限、复杂的现实推理需求、自我博弈（self-play）的困难以及缺乏可靠的反馈信号。为了应对这些挑战，我们提出了一种轻量级、模型无关的框架，通过离线强化学习（RL）来改进基于大语言模型（LLM）的企业级智能体。所提出的基于 DT-MDP 的上下文工程（DT-MDP-CE）框架包含三个关键组件：(1) 数字孪生马尔可夫决策过程（DT-MDP），它将智能体的推理行为抽象为有限 MDP；(2) 鲁棒的对比逆强化学习（contrastive inverse RL），利用 DT-MDP 高效估计合理的奖励函数，并从质量参差不齐的离线轨迹中推导策略；(3) 强化学习引导的上下文工程，利用从 (1) 和 (2) 的集成过程中获得的策略来改进智能体的决策行为。作为案例研究，我们将该框架应用于面向企业的 IT 自动化领域中的一个代表性任务。广泛的实验结果表明，在各种评估设置下，该框架相比基准智能体均有持续且显著的提升，这表明该框架可以推广到企业环境中具有类似特征的其他智能体。

## Abstract
Despite rapid progress in AI agents for enterprise automation and decision-making, their real-world deployment and further performance gains remain constrained by limited data quality and quantity, complex real-world reasoning demands, difficulties with self-play, and the lack of reliable feedback signals. To address these challenges, we propose a lightweight, model-agnostic framework for improving LLM-based enterprise agents via offline reinforcement learning (RL). The proposed Context Engineering via DT-MDP (DT-MDP-CE) framework comprises three key components: (1) A Digital-Twin Markov Decision Process (DT-MDP), which abstracts the agent's reasoning behavior as a finite MDP; (2) A robust contrastive inverse RL, which, armed with the DT-MDP, to efficiently estimate a well-founded reward function and induces policies from mixed-quality offline trajectories; and (3) RL-guided context engineering, which uses the policy obtained from the integrated process of (1) and (2), to improve the agent's decision-making behavior. As a case study, we apply the framework to a representative task in the enterprise-oriented domain of IT automation. Extensive experimental results demonstrate consistent and significant improvements over baseline agents across a wide range of evaluation settings, suggesting that the framework can generalize to other agents sharing similar characteristics in enterprise environments.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DT-MDP-CE** 的框架，旨在通过离线强化学习（Offline RL）和数字孪生技术，提升企业级大模型（LLM）智能体在复杂任务（如 IT 自动化）中的决策能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：企业级 AI 智能体在实际部署中面临四大挑战：数据质量与数量有限、现实推理任务极其复杂、难以进行自我博弈（Self-play）、以及缺乏可靠且及时的反馈信号。
*   **研究动机**：现有的微调（SFT）或在线强化学习方法在企业环境下成本高昂、数据稀缺且存在安全风险。因此，需要一种轻量级、无需修改模型参数、且能从有限且质量参差不齐的离线数据中学习的改进方案。

### 2. 核心方法论
该框架由三个关键组件构成：
*   **数字孪生马尔可夫决策过程 (DT-MDP)**：
    *   将智能体在连续、无限的文本空间中的推理行为抽象为有限的 MDP 状态和动作空间。
    *   利用领域知识（如 IT 系统的拓扑结构）构建抽象函数，将观察结果映射为抽象状态，将推理步骤映射为抽象动作。
*   **鲁棒对比逆强化学习 (Robust Contrastive IRL)**：
    *   采用 **T-REX** 算法，通过对轨迹进行两两排序（而非依赖绝对评分）来学习奖励函数。
    *   这种方法允许从“好”和“坏”的混合轨迹中提取信号，从而学习如何改进智能体，而不仅仅是模仿现有行为。
*   **强化学习引导的上下文工程 (RL-guided Context Engineering)**：
    *   利用学习到的离线策略在智能体运行时进行干预。
    *   **干预策略**包括：**建议（Suggesting）**（在 Prompt 中推荐高概率动作）、**剪枝（Pruning）**（过滤低概率的无效探索）、**优先级排序（Prioritizing）**（重新排列探索队列）。

### 3. 实验设计
*   **应用场景**：网站可靠性工程（SRE）诊断任务，即自动识别 IT 系统故障的根因。
*   **数据集**：使用 **ITBench** 基准，包含 12 个训练场景（如 Pod 故障、网络中断）和 6 个未见过的测试场景。
*   **智能体与模型**：
    *   测试了 **EoG**（基于图的解释智能体）和 **ReAct** 两种架构。
    *   底层 LLM 涵盖了 Mistral 系列（Small/Medium/Large）和 Llama 系列（8B 到 405B）。
*   **对比方法**：基准智能体（无 RL）、行为克隆（BC）、稀疏奖励强化学习（RL-Sparse）。
*   **评估指标**：Pass@3 Recall 和 Pass@3 F1 分数（由 Gemini-2.5-Pro 作为评委进行打分）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **模型使用**：实验中提到了使用 Mistral-Medium-2505 作为主要推理引擎，并使用了 Granite-3.2-8B 等多个开源模型生成离线轨迹数据。

### 5. 实验数量与充分性
*   **实验规模**：收集了 819 条轨迹，共计 12,079 个步骤（Turns）。
*   **充分性**：
    *   **多维度验证**：涵盖了不同抽象方案（基于名称、类型、拓扑）、不同干预策略及其组合。
    *   **泛化性测试**：在 SRE 领域之外的软件工程（SWE）任务上进行了跨领域测试。
    *   **消融实验**：对专家轨迹数量、干预阈值等关键参数进行了鲁棒性分析。
    *   **统计显著性**：使用了 Bonferroni 校正的配对 t 检验和 Nemenyi 检验，确保结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：DT-MDP-CE 在所有测试设置下均显著优于基准智能体。
*   **奖励学习的关键**：基于逆强化学习（IRL）推导的中间奖励比简单的最终成功/失败奖励（Sparse Reward）更有效。
*   **模型规模影响**：**中等规模模型**（如 Mistral-Medium）从该框架中获益最大；小型模型受限于理解力，大型模型则因基准水平已较高而提升幅度相对较小。
*   **拓扑抽象的优势**：基于系统拓扑结构的抽象比简单的名称映射具有更强的跨场景迁移能力。

### 7. 优点与亮点
*   **轻量且模型无关**：不需要对 LLM 进行昂贵的微调，通过 Prompt 工程即可实现干预。
*   **数据效率高**：能够利用质量参差不齐的离线数据，解决了企业场景下高质量专家数据稀缺的问题。
*   **可解释性**：通过将推理过程抽象为 MDP，使得智能体的决策逻辑更易于被人类专家理解和审计。

### 8. 不足与局限
*   **领域知识依赖**：构建 DT-MDP 的抽象函数（如状态和动作的定义）高度依赖于特定领域的专家知识，难以实现完全自动化的跨领域迁移。
*   **干预点限制**：目前的干预主要集中在动作选择阶段，对于更深层次的思维链（CoT）逻辑干预较少。
*   **算力细节缺失**：缺乏训练成本的量化数据，难以评估该框架在实际生产环境中的部署性价比。

（完）
