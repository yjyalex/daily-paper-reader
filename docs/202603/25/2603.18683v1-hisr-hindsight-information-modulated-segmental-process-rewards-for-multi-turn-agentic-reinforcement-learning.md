---
title: "HISR: Hindsight Information Modulated Segmental Process Rewards For Multi-turn Agentic Reinforcement Learning"
title_zh: HISR：用于多轮智能体强化学习的后验信息调制片段式过程奖励
authors: "Zhicong Lu, Zichuan Lin, Wei Jia, Changyuan Tian, Deheng Ye, Peiguang Li, Li Jin, Nayu Liu, Guangluan Xu, Wei Feng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18683v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 多轮智能体强化学习的分段过程奖励
tldr: 针对大语言模型在长程智能体任务中奖励稀疏和信用分配不准的问题，本文提出HISR方法。该方法通过分段过程奖励模型为子目标分配奖励，避免了过细的轮次级分配。同时，引入事后信息构建重要性评分，利用事后模型与策略模型的似然比来调节分段奖励，从而强化关键步骤的信用分配，显著提升了多轮强化学习的性能。
selection_source: fresh_fetch
motivation: 现有的奖励模型在处理长程任务时存在结果奖励延迟和轮次级过程奖励信用分配不可靠的问题。
method: 提出分段过程奖励模型以对齐子目标，并利用事后模型与策略模型的似然比计算重要性分数，进而调节分段奖励。
result: 在三个公开基准测试上的实验结果证明了HISR在提升智能体决策性能方面的有效性。
conclusion: 通过结合分段奖励与事后信息调节，HISR有效增强了多轮智能体强化学习中的信用分配可靠性。
---

## 摘要
尽管大语言模型在多个领域表现卓越，但在复杂的长程智能体决策任务中，其性能仍然受限。现有的大多数方法集中于设计有效的奖励模型（RM），通过多轮强化学习来提升性能。然而，这些方法面临稀疏结果奖励中的延迟传播问题，以及由于可能过于细粒度且缺乏重点的轮次级过程奖励导致的不可靠信用分配。在本文中，我们提出了 HISR，利用后验信息（Hindsight Information）来调制片段式过程奖励，使奖励与子目标紧密对齐，并强调重要片段，从而增强信用分配的可靠性。具体而言，本文提出了一种片段级过程奖励模型，为任务中的每个子目标分配奖励，避免了对轮次进行过度细粒度的分配。为了强调轨迹中的重要片段，我们设计了一个后验模型，用于反映在已知轨迹结果后执行特定动作的偏好。利用这一特性，我们设计了后验模型与策略模型之间的序列似然比，以衡量动作的重要性。随后，利用这些比例聚合片段重要性得分，进而调制片段式过程奖励，提升信用分配的可靠性。在三个公开基准测试上的广泛实验结果证明了我们方法的有效性。

## Abstract
While large language models excel in diverse domains, their performance on complex longhorizon agentic decision-making tasks remains limited. Most existing methods concentrate on designing effective reward models (RMs) to advance performance via multi-turn reinforcement learning. However, they suffer from delayed propagation in sparse outcome rewards and unreliable credit assignment with potentially overly fine-grained and unfocused turnlevel process rewards. In this paper, we propose (HISR) exploiting Hindsight Information to modulate Segmental process Rewards, which closely aligns rewards with sub-goals and underscores significant segments to enhance the reliability of credit assignment. Specifically, a segment-level process RM is presented to assign rewards for each sub-goal in the task, avoiding excessively granular allocation to turns. To emphasize significant segments in the trajectory, a hindsight model is devised to reflect the preference of performing a certain action after knowing the trajectory outcome. With this characteristic, we design the ratios of sequence likelihoods between hindsight and policy model to measure action importance. The ratios are subsequently employed to aggregate segment importance scores, which in turn modulate segmental process rewards, enhancing credit assignment reliability. Extensive experimental results on three publicly benchmarks demonstrate the validity of our method.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HISR**（Hindsight Information Modulated Segmental Process Rewards）的方法，旨在提升大语言模型（LLM）在复杂长程智能体任务中的决策能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
在多轮智能体强化学习（RL）中，LLM 面临两个主要挑战：
*   **奖励稀疏与延迟：** 仅依靠轨迹结束时的结果奖励（Outcome Reward）很难指导早期动作的优化。
*   **信用分配（Credit Assignment）不可靠：** 现有的过程奖励（Process Reward）通常基于“轮次（Turn）”级别，这可能过于细粒度（一个子目标往往跨越多个轮次），且由于缺乏对动作重要性的关注，导致奖励分配不聚焦。
**HISR 的核心目标**是通过将奖励对齐到“片段（Segment/子目标）”级别，并利用“后验信息”来识别并强化关键动作，从而实现更精准的信用分配。

### 2. 方法论：核心思想与关键技术
HISR 主要包含三个关键组件：

*   **片段式过程奖励模型 (SPRM)：**
    *   **轨迹分段：** 利用 GPT-4o 将原始轨迹按子目标划分为多个片段（Segment）。
    *   **进度估计：** 在策略模型（$\pi_{ref}$）的最后一个隐藏层添加 MLP，预测每个片段结束时的贡献得分 $r_i$。其损失函数基于片段奖励总和与最终结果奖励 $R$ 的均方误差。
*   **后验信息调制 (HIM)：**
    *   **后验模型 ($\pi_{hind}$)：** 训练一个类似于掩码语言模型（MLM）的后验模型，它在已知完整轨迹（包括后续动作和结果）的情况下预测当前动作。
    *   **重要性衡量：** 计算后验模型与当前策略模型 ($\pi_{policy}$) 的序列似然比 $z(a_k)$。如果比值 $>1$，说明该动作在“事后看来”是非常重要的。
    *   **奖励调制：** 将片段重要性得分与 SPRM 预测的奖励进行逐元素相乘并归一化，得到调制后的奖励 $\hat{R}_{him}$，从而突出关键片段。
*   **多轮强化学习优化：**
    *   结合**动作落地奖励**（Action Grounding Reward，判断动作是否可执行）和调制后的片段奖励，使用 PPO 算法优化策略。

### 3. 实验设计
*   **数据集/场景：**
    *   **Alfworld：** 具身家庭环境任务（如：清洗并放置物体）。
    *   **Virtualhome：** 复杂的家庭活动模拟（如：洗衣服）。
    *   **Webshop：** 真实的在线购物导航与决策。
*   **对比方法（Baselines）：**
    *   **提示工程：** Llama 3.2, GPT-4o, Gemini 2.5 Pro。
    *   **行为克隆（BC）：** SFT, RFT, DPO。
    *   **强化学习（RL）：** PPO, GRPO, Archer, StepAgent, RAGEN, 以及最强基准 **SPA**（基于轮次的过程归因）。

### 4. 资源与算力
*   **硬件：** 实验使用了 **8 张 NVIDIA H20 GPU**。
*   **软件框架：** 使用 vLLM 和 FastChat 库来加速推理。
*   **训练细节：** 骨干模型为 Llama 3.2-3B-Instruct。在 RL 阶段采用了 LoRA（Rank=8）进行微调。

### 5. 实验数量与充分性
*   **实验规模：** 在三个主流智能体基准上进行了全面测试，涵盖了从具身智能到 Web 导航的不同场景。
*   **消融实验：** 论文设计了四种变体（如去掉后验调制、去掉片段奖励等），验证了每个组件的贡献。
*   **统计分析：** 提供了关于片段长度分布、奖励分布变化的统计图表，并进行了详细的案例研究（Case Study）。
*   **评价：** 实验设计较为充分，对比了从基础模型到最先进的 RL 智能体方法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **HISR 达到 SOTA：** 在所有三个基准测试中，HISR 均显著优于现有的结果奖励和轮次级过程奖励方法。
*   **片段化的必要性：** 统计显示超过 50% 的子目标跨越多个轮次，片段级奖励比轮次级奖励更符合逻辑。
*   **后验信息的价值：** 后验调制能有效纠正原始奖励模型“偏爱后期步骤”的偏差，将信用更公平地分配给早期的关键决策步骤。

### 7. 优点（亮点）
*   **粒度对齐：** 巧妙地解决了 LLM 智能体任务中“动作轮次”与“逻辑步骤”不匹配的问题。
*   **无需额外标注：** 利用后验模型自动计算重要性，不需要人工标注过程奖励标签，降低了训练成本。
*   **可解释性：** 通过似然比直观地量化了动作的重要性，为信用分配提供了数学依据。

### 8. 不足与局限
*   **依赖外部模型分段：** 依赖 GPT-4o 进行轨迹分段，这在训练流水线中增加了一个额外步骤且存在调用成本。
*   **后验模型是静态的：** 实验中后验模型在 RL 阶段是冻结的，可能无法完全适应 RL 过程中不断演化的数据分布。
*   **先验偏差：** 原始的片段奖励模型仍带有较强的“位置偏见”（倾向于给最后一步高分），后验调制的修正能力在某些极端情况下可能受限。

（完）
