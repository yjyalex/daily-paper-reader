---
title: "DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay"
title_zh: DyJR：通过动态 Jensen-Shannon 回放保持具有可验证奖励的强化学习中的多样性
authors: "Long Li, Zhijian Zhou, Tianyi Wang, Weidi Xu, Zuming Huang, Wei Chu, Zhe Wang, Shirui Pan, Chao Qu, Yuan Qi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16157v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于可验证奖励强化学习的正则化框架，旨在增强推理多样性
tldr: 针对大语言模型强化学习（如GRPO）中样本效率低和多样性缺失（模式崩溃）的问题，本文提出DyJR框架。该方法通过动态Jensen-Shannon回放机制，利用时间敏感的动态缓冲区保留近期轨迹，并引入JS散度正则化约束分布，而非直接强化准确样本。实验证明DyJR在数学推理和Text-to-SQL任务上显著优于GRPO等基准，在提升性能的同时有效保持了生成多样性。
selection_source: fresh_fetch
motivation: 现有的经验回放方法往往因过度强化准确样本而导致模型陷入模式崩溃，且计算成本高，因此需要一种能平衡效率与多样性的正则化方案。
method: 提出包含FIFO自适应缓冲区和JS散度正则化的DyJR框架，通过约束当前策略与历史轨迹分布的差异来维持多样性。
result: 在数学推理和Text-to-SQL基准测试中，DyJR在保持与GRPO相当的训练效率下，性能显著超越了GRPO、RLEP和Ex-GRPO。
conclusion: DyJR通过动态分布约束有效缓解了强化学习中的过拟合问题，为提升LLM推理能力和生成多样性提供了一种高效且可验证的路径。
---

## 摘要
虽然强化学习（RL）增强了大语言模型的推理能力，但像 GRPO 这样的同策略算法由于丢弃了过去的采样序列，导致样本效率低下。现有的经验回放方法通过重用准确样本进行直接策略更新来解决这一问题，但这通常会带来高昂的计算成本，并因过拟合导致模式崩溃。我们认为，历史数据应优先用于维持多样性，而非仅仅强化准确性。为此，我们提出了动态 Jensen-Shannon 回放（DyJR），这是一个简单且有效的正则化框架，利用来自近期轨迹的动态参考分布。DyJR 引入了两项创新：（1）时间敏感型动态缓冲区，采用 FIFO 和自适应大小调整，仅保留时间上邻近的样本，从而与模型演化同步；（2）Jensen-Shannon 散度正则化，用分布约束取代直接梯度更新，以防止多样性崩溃。在数学推理和 Text-to-SQL 基准测试上的实验表明，DyJR 的表现显著优于 GRPO 以及 RLEP 和 Ex-GRPO 等基准方法，同时保持了与原始 GRPO 相当的训练效率。此外，从 Rank-k Token 概率演化的角度，我们展示了 DyJR 增强了多样性并减轻了对 Rank-1 Token 的过度依赖，阐明了 DyJR 的特定子模块如何影响训练动态。

## Abstract
While Reinforcement Learning (RL) enhances Large Language Model reasoning, on-policy algorithms like GRPO are sample-inefficient as they discard past rollouts. Existing experience replay methods address this by reusing accurate samples for direct policy updates, but this often incurs high computational costs and causes mode collapse via overfitting. We argue that historical data should prioritize sustaining diversity rather than simply reinforcing accuracy. To this end, we propose Dynamic Jensen-Shannon Replay (DyJR), a simple yet effective regularization framework using a dynamic reference distribution from recent trajectories. DyJR introduces two innovations: (1) A Time-Sensitive Dynamic Buffer that uses FIFO and adaptive sizing to retain only temporally proximal samples, synchronizing with model evolution; and (2) Jensen-Shannon Divergence Regularization, which replaces direct gradient updates with a distributional constraint to prevent diversity collapse. Experiments on mathematical reasoning and Text-to-SQL benchmarks demonstrate that DyJR significantly outperforms GRPO as well as baselines such as RLEP and Ex-GRPO, while maintaining training efficiency comparable to the original GRPO. Furthermore, from the perspective of Rank-$k$ token probability evolution, we show that DyJR enhances diversity and mitigates over-reliance on Rank-1 tokens, elucidating how specific sub-modules of DyJR influence the training dynamics.

---

## 论文详细总结（自动生成）

以下是对论文《DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的强化学习（RL）推理任务中，现有的同策略（On-policy）算法（如 GRPO）样本效率低下，因为它们会丢弃昂贵的历史采样数据。
*   **研究动机**：
    *   **现有方法的缺陷**：现有的经验回放（Experience Replay）方法通常将历史成功样本视为额外的正样本进行直接梯度更新。这会导致模型过度拟合特定的解题路径，引发“模式崩溃”（Mode Collapse），丧失探索多样性。
    *   **资源开销**：传统回放方法（如 RLEP）需要存储海量历史轨迹，带来巨大的显存压力。
*   **核心观点**：历史数据的主要价值不在于简单地强化准确性，而在于**维持生成的多样性**。

### 2. 方法论（DyJR 框架）
DyJR 提出了一种动态 Jensen-Shannon 回放机制，包含两个核心组件：
*   **时间敏感型动态缓冲区（Time-Sensitive Dynamic Buffer）**：
    *   **FIFO 协议**：仅保留最近 $M$ 个步骤内的成功样本（Reward=1），确保参考分布与当前模型演化同步。
    *   **自适应规模调整**：在训练初期（高熵阶段）扩大缓冲区比例（如从 5% 提升至 20%），以捕捉丰富的探索模式；在模型稳定后收缩规模。
    *   **置信度分层选择**：优先选择组内正确数（Confidence）高的样本，确保参考分布的低方差。
*   **Jensen-Shannon (JS) 散度正则化**：
    *   **核心思想**：不直接进行策略梯度更新，而是将历史成功路径的混合分布作为“锚点”，通过最小化当前策略与该分布之间的 JS 散度来进行正则化。
    *   **公式逻辑**：使用 JS 散度而非 KL 散度，利用其对称性和有界性（[0, ln 2]），在处理非平稳的历史数据时提供更稳定的对齐信号，避免 Forward KL 带来的过度平滑问题。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **长链数学推理**：使用 Qwen3-4B-Base 模型，在 Reinforce-Ada-Hard（14k 高难度样本）上训练，评估 6 个竞赛级基准（AIME25, AMC23, HMMT25 等）。
    *   **SQL 生成**：使用 Llama-3.1-8B-Instruct 模型，在 BIRD 数据集上训练，并在 BIRD 和 Spider 上评估跨领域泛化能力。
*   **对比基准（Baselines）**：
    *   基础算法：GRPO, DAPO。
    *   经验回放类：RLEP, Ex-GRPO。
    *   散度正则化类：DPH-RL（使用静态 JS 约束）。

### 4. 资源与算力
*   **硬件配置**：实验使用了 **32 张 NVIDIA A800 GPU**。
*   **算力开销**：
    *   **显存**：由于仅存储序列索引且回放比例小，DyJR 的额外显存开销极低（小于 1GB），远低于存储完整轨迹的 RLEP。
    *   **时间**：由于回放 Batch Size 远小于在线训练 Batch Size，DyJR 的训练吞吐量（ms/token）与原始 GRPO 基本持平，具有极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数学和 SQL 两个截然不同的领域，使用了不同架构（Qwen, Llama）和参数规模的模型。
*   **消融实验**：
    *   对比了不同散度类型（JS vs. Forward KL）。
    *   测试了正则化系数 $\alpha_{JS}$ 和缓冲区最大寿命 $M$ 的敏感度。
    *   分析了采样预算 $k$ 增加时（Pass@k）的性能增长。
*   **分析深度**：通过可视化 **Rank-k Token 概率演化**，深入探讨了模型在训练过程中如何避免对 Rank-1 Token 的过度依赖，实验设计客观且论证充分。

### 6. 主要结论与发现
*   **性能提升**：DyJR 在数学任务上平均准确率达到 34.1%（比 GRPO 高 4.3%），在 SQL 任务上也显著优于所有基准。
*   **多样性保护**：DyJR 有效减缓了熵的下降速度，使模型在训练后期仍能保持对 Rank-2/Rank-3 路径的探索能力。
*   **早期数据的重要性**：实验发现，训练初期的“高熵”探索数据对最终性能至关重要，而后期的高准确度数据对多样性的贡献反而较小。
*   **JS 散度的优越性**：在处理来自快速演化策略的混合数据时，JS 散度比 Forward KL 更稳健，能有效防止模式崩溃。

### 7. 优点
*   **理念创新**：将经验回放的功能从“强化准确性”重定义为“维持多样性”，为 RLVR 训练提供了新视角。
*   **高效简洁**：通过 FIFO 动态缓冲区和 JS 正则化，在几乎不增加计算负担的情况下显著提升了性能。
*   **分析透彻**：利用 Rank-k 概率分布演化解释了模型内部的训练动态，增强了方法的可解释性。

### 8. 不足与局限
*   **任务局限性**：主要集中在具有可验证奖励（Verifiable Rewards）的结构化推理任务（数学、SQL），尚未在开放式对话或主观偏好对齐任务中验证。
*   **模型规模**：实验主要在 4B 和 8B 模型上进行，虽然证明了有效性，但在超大规模模型（如 70B+）上的表现尚待观察。
*   **超参数依赖**：正则化系数 $\alpha_{JS}$ 和缓冲区寿命 $M$ 对性能有一定影响，可能需要针对不同任务进行微调。

（完）
