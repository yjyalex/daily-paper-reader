---
title: Improving Safety Alignment via Balanced Direct Preference Optimization
title_zh: 通过平衡直接偏好优化提升安全对齐
authors: "Shiji Zhao, Mengyang Wang, Shukun Xiong, Fangzhou Chen, Qihui Zhu, Shouwei Ruan, Yisong Xiao, Ranjie Duan, Xun Chen, XingXing Wei"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22829v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大语言模型对齐的直接偏好优化 (DPO)
tldr: 随着大语言模型的广泛应用，其安全性愈发重要。虽然直接偏好优化（DPO）被广泛用于安全对齐，但仍面临严重的过拟合问题。本文发现偏好对之间存在“不平衡偏好理解”现象，即模型对偏好和非偏好回复的理解程度不一，从而损害了安全性能。为此，作者提出了平衡直接偏好优化（B-DPO），通过互信息自适应调节优化强度。实验证明，B-DPO在提升安全性的同时，保持了模型在主流基准上的通用能力。
selection_source: fresh_fetch
motivation: 现有的DPO安全对齐方法存在严重的过拟合问题，且模型对偏好对中不同回复的理解程度不平衡。
method: 提出B-DPO方法，利用互信息自适应地调节偏好回复与非偏好回复之间的优化强度。
result: 实验结果显示，B-DPO在多个主流基准测试中显著提升了模型的安全性，并保持了优秀的通用能力。
conclusion: 通过平衡模型对偏好数据的理解，可以有效缓解过拟合，提升大语言模型的安全对齐效果。
---

## 摘要
随着大语言模型（LLMs）的快速发展和广泛应用，其潜在的安全风险引起了广泛关注。基于人类反馈的强化学习（RLHF）已被用于增强 LLMs 的安全性能。作为 RLHF 的一种简单且有效的替代方案，直接偏好优化（DPO）被广泛应用于安全对齐。然而，安全对齐仍面临严重的过拟合问题，这限制了其实际表现。本文从模型对训练数据理解的角度重新审视了过拟合现象。我们发现偏好对中的回复之间存在“不平衡偏好理解”（Imbalanced Preference Comprehension）现象，这损害了模型的安全性能。为了解决这一问题，我们提出了平衡直接偏好优化（B-DPO），它根据互信息自适应地调节首选回复与非首选回复之间的优化强度。一系列实验结果表明，与现有最先进的方法相比，B-DPO 在各种主流基准测试中能够增强 LLMs 的安全能力，同时保持其具有竞争力的通用能力。（警告：本文包含有害文本示例，建议读者谨慎阅读。）

## Abstract
With the rapid development and widespread application of Large Language Models (LLMs), their potential safety risks have attracted widespread attention. Reinforcement Learning from Human Feedback (RLHF) has been adopted to enhance the safety performance of LLMs. As a simple and effective alternative to RLHF, Direct Preference Optimization (DPO) is widely used for safety alignment. However, safety alignment still suffers from severe overfitting, which limits its actual performance. This paper revisits the overfitting phenomenon from the perspective of the model's comprehension of the training data. We find that the Imbalanced Preference Comprehension phenomenon exists between responses in preference pairs, which compromises the model's safety performance. To address this, we propose Balanced Direct Preference Optimization (B-DPO), which adaptively modulates optimization strength between preferred and dispreferred responses based on mutual information. A series of experimental results show that B-DPO can enhance the safety capability while maintaining the competitive general capabilities of LLMs on various mainstream benchmarks compared to state-of-the-art methods. \color{red}{Warning: This paper contains examples of harmful texts, and reader discretion is recommended.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Safety Alignment via Balanced Direct Preference Optimization》（通过平衡直接偏好优化提升安全对齐）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管直接偏好优化（DPO）在大型语言模型（LLM）的安全对齐中表现出色，但仍面临严重的**过拟合**问题——模型在训练分布内表现良好，但在分布外（OOD）场景下泛化能力差。
*   **研究动机**：作者发现模型对偏好对（首选回复 $y_w$ 和非首选回复 $y_l$）的理解程度是不平衡的（即“不平衡偏好理解”现象）。这种不平衡导致模型倾向于记住特定的数据点，而非学习到通用的偏好方向，从而损害了安全性能。

### 2. 论文提出的方法论：B-DPO
*   **核心思想**：利用**互信息（Mutual Information, MI）**量化模型对回复的理解程度，并据此自适应地调节优化强度。
*   **关键技术细节**：
    1.  **量化理解程度**：使用 $I(x, y) = H(y) - H(y|x)$ 计算查询 $x$ 与回复 $y$ 之间的互信息。$I(x, y)$ 越高，表示模型对该回复的语义关联理解越深。
    2.  **自适应权重调整**：引入平衡权重 $\lambda_w$ 和 $\lambda_l$。当模型对某个回复理解过深时，降低其优化权重以防止过拟合；反之则进行补偿。
    3.  **优化目标函数**：在 DPO 损失函数中嵌入权重，公式调整为：
        $$\tilde{L} = -E[\log \sigma(\beta \lambda_w \log \frac{\pi_\theta(y_w|x)}{\pi_{ref}(y_w|x)} - \beta \lambda_l \log \frac{\pi_\theta(y_l|x)}{\pi_{ref}(y_l|x)})]$$
    4.  **缩放因子（Scaling Factor）**：为了避免权重调整导致梯度剧烈波动，引入了一个缩放因子来抵消对整体梯度幅度的影响，确保训练稳定性。

### 3. 实验设计
*   **实验模型**：Qwen-2-7B-Instruct、Mistral-7B-Instruct-v0.3、Vicuna-7B-v1.5。
*   **数据集**：从 PKU-SafeRLHF 中筛选出 20,000 条偏好数据（10,000 条安全查询，10,000 条不安全查询）。
*   **Benchmark（基准测试）**：
    *   **安全性**：StrongReject、XsTest、GCG（梯度攻击）、PAIR（红队攻击）。
    *   **通用能力**：GSM8k（数学）、SimpleQA（事实性）、AdvGLUE（鲁棒性）、HHH（对齐性）。
*   **对比方法**：标准 DPO、CPO、SimPO、SafeDPO。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA RTX A800** GPU 上完成。
*   **训练时长**：以 Qwen2-7B 为例，标准 DPO 耗时约 1.5 小时；B-DPO 总耗时约 **1.9 小时**（其中 0.4 小时用于预计算互信息，1.5 小时用于训练）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流开源模型和 8 个评估基准，实验设置较为全面。
*   **消融实验**：
    *   验证了平衡权重（BW）和缩放因子（SF）各自的贡献。
    *   探讨了超参数 $\alpha$（控制调节强度）对性能的影响（取值 0.5 到 2.5）。
*   **客观性与公平性**：使用了统一的训练集和评估模型（如 Qwen3-14B 作为裁判），并对比了多种最先进的 DPO 变体，实验设计具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **不平衡现象普遍存在**：实验证实 LLM 对安全/不安全回复的理解存在显著差异，且安全性能与这种不平衡程度呈负相关。
*   **B-DPO 效果显著**：在 Qwen2-7B 上，B-DPO 相比基准方法在 StrongReject 上实现了 **8.01%** 的安全提升。
*   **兼顾通用性**：B-DPO 在大幅提升安全性的同时，几乎没有产生“对齐税”（Alignment Tax），在 GSM8k 等通用任务上保持了与原模型相当的水平。

### 7. 优点
*   **理论切入点新颖**：从信息论（互信息）角度解释 DPO 的过拟合问题，提供了全新的视角。
*   **自适应性强**：无需昂贵的数据过滤或重新生成，通过修改损失函数即可实现模型特定的平衡优化。
*   **稳定性高**：通过缩放因子解决了加权优化带来的梯度不稳定问题。

### 8. 不足与局限
*   **模态限制**：目前仅在纯文本模态下进行了验证，尚未探索多模态（如视觉-语言）场景下的表现。
*   **框架局限**：主要针对 DPO 框架，尚未验证该策略在 PPO 等更复杂的强化学习框架中的适用性。
*   **计算开销**：虽然增加的开销在可接受范围内，但预计算互信息仍需额外的推理步骤。

（完）
