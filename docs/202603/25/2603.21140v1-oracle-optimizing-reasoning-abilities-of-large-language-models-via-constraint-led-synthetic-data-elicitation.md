---
title: "ORACLE: Optimizing Reasoning Abilities of Large Language Models via Constraint-Led Synthetic Data Elicitation"
title_zh: ORACLE：通过约束引导的合成数据诱导优化大语言模型的推理能力
authors: "Zhuojie Yang, Wentao Wan, Keze Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21140v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过合成数据和中间步骤验证优化推理能力
tldr: 针对合成推理数据中中间步骤验证缺失的问题，本文提出ORACLE框架。该框架受三段论启发，将大模型的生成能力与符号推理引擎的监督相结合，通过统一提示模板生成模块化推理链，并对每一步进行细粒度验证。在逻辑、事实和常识推理等六个基准测试中，ORACLE显著提升了模型性能，为构建高质量自然语言推理数据提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的合成推理数据生成方法主要依赖最终答案校验，难以有效验证自然语言推理任务中模糊或不完整的中间步骤。
method: 提出ORACLE框架，利用大模型生成步骤化推理内容，并结合符号推理引擎对每个中间步骤进行细粒度的逻辑验证。
result: 在逻辑、事实和常识推理等六个基准测试中，ORACLE在多个模型上均一致优于现有的强基准方法。
conclusion: ORACLE通过符号化监督与模块化生成的结合，有效提升了合成推理数据的质量及大模型的复杂推理能力。
---

## 摘要
使用合成推理数据训练大语言模型（LLMs）已成为增强其推理能力的流行方法，而影响该范式有效性的关键因素是生成的逐步推理数据的质量。为了生成高质量的推理数据，近期许多方法生成合成推理路径并根据最终答案的正确性进行过滤，这往往忽略了中间推理步骤中的缺陷。为了加强对中间推理步骤的验证，先前的工作主要求助于代码执行或符号推理引擎。然而，基于代码的验证仅限于代码或数学任务，而推理引擎则需要结构良好且完整的上下文。因此，现有方法在涉及模糊或不完整上下文的自然语言推理任务中无法有效发挥作用。在这些任务中，合成数据仍然缺乏验证每个推理步骤的可靠检查。为了应对这一挑战，我们引入了 ORACLE，这是一个受三段论推理启发的结构化数据生成框架。ORACLE 将 LLM 的生成优势与符号监督相结合：LLM 生成逐步推理上下文，而符号推理引擎验证每个中间步骤的有效性。通过采用统一的提示模板来诱导模块化推理链，ORACLE 实现了细粒度的步骤级验证，从而促进了高质量多步推理数据的构建。在六个逻辑、事实和常识推理基准测试中，我们的 ORACLE 在多个模型上始终优于强基准模型。

## Abstract
Training large language models (LLMs) with synthetic reasoning data has become a popular approach to enhancing their reasoning capabilities, while a key factor influencing the effectiveness of this paradigm is the quality of the generated multi-step reasoning data. To generate high-quality reasoning data, many recent methods generate synthetic reasoning paths and filter them based on final answer correctness, often overlooking flaws in intermediate reasoning steps. To enhance the verification of intermediate reasoning steps, prior work primarily resorts to code execution or symbolic reasoning engines. However, code-based validation is restricted to code or mathematical tasks, and reasoning engines require a well-structured and complete context. As a result, existing methods fail to function effectively in natural language reasoning tasks that involve ambiguous or incomplete contexts. In these tasks, synthetic data still lack reliable checks for verifying each reasoning step. To address this challenge, we introduce ORACLE, a structured data generation framework inspired by syllogistic reasoning. ORACLE integrates the generative strengths of LLMs with symbolic supervision: the LLM produces step-wise reasoning contexts, while a symbolic reasoning engine verifies the validity of each intermediate step. By employing a unified prompting template to elicit modular reasoning chains, ORACLE enables fine-grained, step-level validation, facilitating the construction of high-quality multi-step reasoning data. Across six logical, factual, and commonsense reasoning benchmarks, our ORACLE consistently outperforms strong baselines on multiple models.

---

## 论文详细总结（自动生成）

以下是对论文《ORACLE: Optimizing Reasoning Abilities of Large Language Models via Constraint-Led Synthetic Data Elicitation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何提高大语言模型（LLM）合成推理数据的质量，特别是解决“中间推理步骤不可靠”的问题。
*   **背景与动机**：
    *   目前主流方法（如 RFT、STaR）主要依赖“最终答案正确性”来过滤合成数据，这会导致模型通过错误的逻辑凑出正确答案（捷径学习）。
    *   现有的中间步骤验证方法局限性大：基于代码的验证仅限于数学/编程；符号推理引擎则对自然语言的模糊性和上下文完整性要求极高，难以直接应用于开放域推理。
    *   **目标**：建立一个既能发挥 LLM 生成灵活性，又能利用符号逻辑严谨性的框架，实现对自然语言推理步骤的细粒度校验。

### 2. 方法论（ORACLE 框架）
ORACLE 受到“三段论推理”启发，采用**两阶段训练流水线**：

*   **结构化模板设计**：定义了包含 `<QUERY>`（子问题）、`<FACTS>`（事实）、`<RULE>`（规则）、`<REVISION>`（反思修订）和 `<REASONING_RESULT>`（推理结果）的模块化模板，使推理过程可解析、可验证。
*   **第一阶段：格式内化（SFT 1）**：
    *   利用少量人工构建的示例进行 Few-shot 提示，引导基础模型生成符合模板的合成数据。
    *   通过格式过滤和答案校验，筛选出高质量数据对基础模型进行微调，使其掌握结构化推理格式。
*   **第二阶段：符号监督与偏好对齐（Beam Search + SFT 2 + DPO）**：
    *   **集成符号引擎**：使用 Pyke 作为推理后端。LLM 生成自然语言步骤后，由模型将其翻译为符号语言（SL），交由引擎执行。
    *   **带评估的束搜索（Beam Search）**：在生成过程中，根据“引擎执行成功（$W_1$）”、“LLM 准确性评估（$W_2$）”和“可行性估计（$W_3$）”进行综合打分，筛选最优路径。
    *   **数据产出**：
        *   **SFT 数据**：通过束搜索找到的逻辑严密且答案正确的完整路径。
        *   **DPO 数据**：将引擎验证通过的节点作为正例，同层验证失败的兄弟节点作为负例，构建偏好对。

### 3. 实验设计
*   **数据集（6个）**：
    *   逻辑推理：ProntoQA, ProofWriter。
    *   事实/常识推理：BoolQ, StrategyQA, CosmosQA。
    *   科学多跳推理：ScienceQA。
*   **基座模型**：Llama-3.1-8B-Instruct, Mistral-7B-Instruct-v0.3, Qwen-2.5-7B-Instruct。
*   **对比方法（Baselines）**：
    *   CoT (Zero-shot/Four-shot)
    *   RFT（拒绝采样过滤）
    *   ToT-SFT（思维树微调）
    *   Self-rewarding（自奖励模型，包含 DPO）

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU** 进行实验。
*   **训练细节**：
    *   采用 **LoRA**（Rank=8）进行高效微调。
    *   SFT 阶段使用 12k 样本，DPO 阶段使用 2k 偏好对。
    *   训练轮数为 3 Epochs。
    *   学习率：SFT 为 $5 \times 10^{-6}$，DPO 为 $1 \times 10^{-4}$。
*   **时长**：文中未明确标注具体的总训练小时数，但给出了样本量和硬件型号。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流开源模型架构和 6 类不同性质的推理任务，实验覆盖面广。
*   **消融实验**：针对“推理引擎”、“束搜索”和“DPO 阶段”分别进行了消融，验证了各组件的必要性。
*   **验证分析**：额外进行了推理引擎执行成功率分析（Table 3）和错误类型分析（Table 4），深入探讨了模型在自然语言转符号语言过程中的瓶颈。
*   **评价**：实验设计客观、公平，对比基准包含了近年来主流的合成数据增强技术。

### 6. 主要结论与发现
*   **性能领先**：ORACLE 在所有 6 个数据集和 3 个模型上均取得了最优或接近最优的效果。
*   **逻辑提升显著**：在 ProofWriter 等强逻辑任务上，相比基准模型提升了 3.7%~6.0%。
*   **迭代效应**：随着训练迭代增加，模型生成“可被符号引擎成功执行”的步骤比例显著提升，说明模型学习到了更规范的逻辑表达。
*   **鲁棒性**：即使在符号引擎执行失败的情况下（如常识推理任务），依靠 LLM 自身的评估机制（$W_2, W_3$），ORACLE 依然能保持较强的性能。

### 7. 优点
*   **细粒度验证**：突破了“仅看结果”的局限，实现了对推理中间步骤的符号化硬约束。
*   **模块化设计**：推理模板将复杂逻辑拆解为事实和规则，增强了模型输出的可解释性。
*   **混合评估机制**：巧妙结合了符号引擎的“严谨性”和 LLM 的“语义理解能力”，解决了纯符号系统难以处理模糊上下文的问题。

### 8. 不足与局限
*   **翻译瓶颈**：实验显示，推理引擎失败的主要原因之一是 LLM 将自然语言翻译为符号语言（SL）时出现语法或语义错误。
*   **复杂度限制**：当推理步骤涉及过于复杂的事实或非演绎逻辑（如归纳、类比）时，现有的符号引擎难以覆盖。
*   **计算开销**：束搜索结合多次 LLM 评估和符号引擎调用，在数据生成阶段的计算成本高于简单的拒绝采样。

（完）
