---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 协同提示词与策略优化以增强具有可验证奖励的 LLM 推理能力
tldr: 针对大语言模型在强化学习（RLVR）中面临的“困难样本”探索效率低、缺乏监督信号的问题，本文提出了P^2O框架。该框架将提示词优化与策略优化相结合，利用遗传帕累托算法（GEPA）为困难样本进化出更优的提示模板，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O在分布内和分布外基准测试中均表现优异，显著提升了模型的泛化能力。
selection_source: fresh_fetch
motivation: 传统的强化学习在处理成功率接近零的困难样本时，由于奖励稀疏导致模型难以获得有效的监督信号。
method: 提出P^2O框架，通过GEPA算法优化提示词以引导模型探索困难样本，并将优化后的推理能力直接蒸馏到模型参数中。
result: "实验显示P^2O在分布内数据集上表现卓越，并在分布外基准测试中平均提升了4.7%。"
conclusion: P^2O通过协同提示词与策略优化，有效解决了RLVR中的探索难题，增强了LLM的推理性能和泛化性。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原始的 RLVR 面临探索效率低下的问题，特别是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但模型实际上却缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示词优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。广泛的实验表明，P^2O 不仅在分布内数据集上取得了卓越的性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：带有可验证奖励的强化学习（RLVR）是提升大语言模型（LLM）推理能力的关键技术。
*   **核心问题**：传统的 RLVR 在处理“困难样本”（即模型在多次尝试中成功率为零的题目）时存在**探索瓶颈**。由于奖励极其稀疏，模型无法获得有效的梯度信号，导致其在简单样本上过拟合，而在复杂推理任务上停滞不前。
*   **整体含义**：本文提出 P^2O 框架，旨在通过“提示词优化”为模型搭建桥梁，引导其跨越奖励稀疏的“山谷”，发现困难样本的正确路径，并将其内化到模型参数中。

### 2. 方法论
P^2O 采用**交替优化**的策略，包含两个核心阶段：
*   **阶段 1：带上下文蒸馏的策略优化**
    *   **困难样本挖掘**：在训练中动态识别成功率低于阈值 $\tau$ 的样本。
    *   **上下文蒸馏（Context Distillation）**：这是关键技术。对于困难样本，使用优化后的提示词 $z$ 引导模型生成正确轨迹 $y$，但在更新参数时，计算的是原始输入 $x$ 生成 $y$ 的概率。这样模型就能在不依赖提示词的情况下，直接学习到推理逻辑。
*   **阶段 2：进化提示词优化**
    *   **GEPA 算法**：利用遗传-帕累托算法演化提示词。
    *   **反思变异（Reflective Mutation）**：利用一个“反思 LLM”分析错误案例，生成针对性的提示词修改建议，而非随机变异。
    *   **帕累托筛选**：维护一个多样化的提示词群体，确保能覆盖不同类型的错误模式。
*   **核心公式**：将提示词视为离散隐变量，目标是最大化 $J_{joint} = \sum_{x} \max_{z} E_{y \sim \pi_\theta(\cdot|T(x,z))} [r(x,y)]$。

### 3. 实验设计
*   **数据集**：从 DeepScaler 和 DeepMath 中各抽取 5,000 个样本（难度等级 $\ge 7$）。
*   **基准测试（Benchmarks）**：涵盖 6 个数学推理任务，包括 AIME24、AIME25、AMC、MATH500、Minerva 和 Olympiad。
*   **对比方法**：
    *   基础模型：Qwen3 (0.6B, 1.7B, 4B)。
    *   强基线：标准的 GRPO（群组相对策略优化）算法。
    *   P^2O 变体：P^2O$_{Self-Ref}$（自反思）和 P^2O$_{Teacher-Ref}$（使用 Kimi-K2 作为老师进行反思）。

### 4. 资源与算力
*   **训练细节**：模型架构为 Qwen3-4B，训练 5 个 epoch，全局 Batch Size 为 128，最大生成长度 12k 序列，学习率 $1 \times 10^{-6}$。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量（如 H100 数量），但提到通过并行化帕累托搜索（Beam Size=16）来提高计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的训练分布（DeepMath 和 DeepScaler）上进行了验证，并在 6 个主流测试集上评估。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：证明了如果不进行蒸馏（直接带提示词训练），模型会产生依赖，导致测试性能大幅下降。
    2.  **提示词多样性**：证明了在 Rollout 过程中使用多种帕累托最优提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为全面，涵盖了从模型性能到训练动力学的多维度分析，对比实验客观，消融实验有力地支撑了核心论点。

### 6. 主要结论与发现
*   **性能飞跃**：P^2O 在 AIME 等高难度任务上提升显著（平均提升 +12.3%），在分布外（OOD）基准上平均提升 4.7%。
*   **破解困难样本**：实验观察到随着训练进行，困难样本的数量稳步下降，证明优化后的提示词确实帮助模型“攻克”了原本无法解决的题目。
*   **反思源的影响**：在不同数据集上，自反思和老师反思的效果各有千秋，说明模型有时更倾向于接受符合自身推理逻辑的引导。

### 7. 优点
*   **创新性**：首次将离散的提示词演化与连续的策略梯度优化深度结合，解决了 RLVR 的冷启动和探索难题。
*   **无需人工干预**：相比于课程学习（需要人工设计难度梯度）或奖励塑造（需要专家知识），P^2O 实现了自动化的自我进化。
*   **推理内化**：通过蒸馏机制，确保了模型在推理阶段（Inference）不需要额外的复杂提示词，保持了效率。

### 8. 不足与局限
*   **计算开销**：虽然提高了样本效率，但引入了额外的提示词演化阶段（Phase 2），这需要调用多次 LLM 进行反思和评估，增加了训练的总时间成本。
*   **对反思模型的依赖**：提示词优化的质量高度依赖于“反思模型”的能力，如果反思模型无法给出高质量的改进建议，性能提升可能受限。
*   **任务局限**：目前主要集中在数学推理等具有明确验证机制（Verifiable Rewards）的任务，在开放式生成或缺乏客观评价标准的任务上应用难度较大。

（完）
