---
title: "From Static Templates to Dynamic Runtime Graphs: A Survey of Workflow Optimization for LLM Agents"
title_zh: 从静态模板到动态运行时图：LLM 智能体工作流优化综述
authors: "Ling Yue, Kushal Raj Bhandari, Ching-Yun Ko, Dhaval Patel, Shuxin Lin, Nianjun Zhou, Jianxi Gao, Pin-Yu Chen, Shaowu Pan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22386v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: LLM智能体工作流优化与智能体计算图综述
tldr: 本综述探讨了LLM智能体工作流的优化，将其定义为智能体计算图（ACG）。文章根据工作流结构的确定时机，将现有方法分为静态模板和动态运行时图，并从优化目标、评估信号等维度构建了统一框架。通过区分可重用模板、特定运行图和执行轨迹，本文为智能体工作流的设计与评估提供了清晰的术语体系和标准化参考。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体工作流设计中缺乏统一框架、术语定义模糊以及评估标准不一致的现状。
method: 将智能体工作流抽象为智能体计算图（ACG），并从结构确定时机、优化对象和评估信号三个维度对文献进行分类。
result: 系统地区分了可重用模板、运行特定图和执行轨迹，并总结了静态与动态工作流在不同场景下的优化策略。
conclusion: 提出了一种结构感知的评估标准，建议结合任务指标、图属性和执行成本来全面衡量智能体系统的性能。
---

## 摘要
基于大语言模型（LLM）的系统正变得日益流行，它们通过构建可执行的工作流来解决任务，这些工作流交织了 LLM 调用、信息检索、工具使用、代码执行、内存更新和验证。本综述回顾了设计和优化此类工作流的最新方法，我们将这些工作流视为智能体计算图（Agentic Computation Graphs, ACGs）。我们根据工作流结构确定的时间点对文献进行组织，其中“结构”是指存在哪些组件或智能体、它们如何相互依赖以及信息如何在它们之间流动。这一视角区分了静态方法和动态方法：前者在部署前固定一个可重用的工作流脚手架，而后者在执行前或执行期间为特定运行选择、生成或修订工作流。我们进一步从三个维度对先前的工作进行组织：结构确定的时间、工作流的哪一部分被优化，以及哪些评估信号引导优化（例如，任务指标、验证器信号、偏好或基于轨迹的反馈）。我们还区分了可重用的工作流模板、特定运行的实例化图以及执行轨迹，将可重用的设计选择与特定运行中实际部署的结构以及实现的运行时行为分离开来。最后，我们概述了一种结构感知的评估视角，通过图级属性、执行成本、鲁棒性以及跨输入的结构变化来补充下游任务指标。我们的目标是为 LLM 智能体工作流优化领域的未来工作提供清晰的词汇表、定位新方法的统一框架、对现有文献更具可比性的视角以及更具可重复性的评估标准。

## Abstract
Large language model (LLM)-based systems are becoming increasingly popular for solving tasks by constructing executable workflows that interleave LLM calls, information retrieval, tool use, code execution, memory updates, and verification. This survey reviews recent methods for designing and optimizing such workflows, which we treat as agentic computation graphs (ACGs). We organize the literature based on when workflow structure is determined, where structure refers to which components or agents are present, how they depend on each other, and how information flows between them. This lens distinguishes static methods, which fix a reusable workflow scaffold before deployment, from dynamic methods, which select, generate, or revise the workflow for a particular run before or during execution. We further organize prior work along three dimensions: when structure is determined, what part of the workflow is optimized, and which evaluation signals guide optimization (e.g., task metrics, verifier signals, preferences, or trace-derived feedback). We also distinguish reusable workflow templates, run-specific realized graphs, and execution traces, separating reusable design choices from the structures actually deployed in a given run and from realized runtime behavior. Finally, we outline a structure-aware evaluation perspective that complements downstream task metrics with graph-level properties, execution cost, robustness, and structural variation across inputs. Our goal is to provide a clear vocabulary, a unified framework for positioning new methods, a more comparable view of existing body of literature, and a more reproducible evaluation standard for future work in workflow optimizations for LLM agents.

---

## 论文详细总结（自动生成）

这篇综述论文《From Static Templates to Dynamic Runtime Graphs: A Survey of Workflow Optimization for LLM Agents》对大语言模型（LLM）智能体的工作流优化进行了系统性的梳理。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：LLM 智能体正从简单的单次提示词交互演变为复杂的工作流（包含工具调用、检索、代码执行、验证等）。然而，目前对于如何设计和优化这些工作流缺乏统一的术语和框架。
*   **核心问题**：论文旨在解决“如何定义、分类和优化智能体工作流结构”的问题。作者提出了**智能体计算图（Agentic Computation Graphs, ACG）**的概念，将工作流视为由节点（动作）和边（依赖）构成的图，并探讨如何通过优化图结构来平衡任务质量与执行成本。

### 2. 方法论：核心思想与关键技术
论文提出了一个多维度的分类框架，核心思想在于区分工作流结构确定的**时机**和**灵活性**：
*   **ACG 三层抽象**：
    *   **模板 (Template)**：部署前定义的、可重用的执行规范。
    *   **实例化图 (Realized Graph)**：针对特定输入在运行时实际生成的结构。
    *   **执行轨迹 (Trace)**：运行过程中产生的具体状态、动作和成本记录。
*   **结构确定分类**：
    *   **静态优化 (Static)**：在离线阶段搜索或优化固定的工作流模板（如 AFlow, ADAS）。
    *   **动态优化 (Dynamic)**：在推理时根据输入动态调整。包括：**选择/剪枝**（从超图中选子图）、**预执行生成**（执行前生成完整图）、**执行中编辑**（边执行边修改结构，如 MetaGen）。
*   **优化目标与信号**：
    *   **目标**：节点级（提示词优化）、图级（拓扑优化）或联合优化。
    *   **反馈信号**：标量指标（成功率）、验证器信号（单元测试）、人类偏好、轨迹文本反馈。
*   **数学表达**：将优化目标建模为在给定成本约束 $\lambda$ 下，最大化期望收益 $R$ 与成本 $C$ 的差值：$\max E[R(\tau; x) - \lambda C(\tau)]$。

### 3. 实验设计与 Benchmark
作为综述，本文系统整理了现有的评估资源，而非进行单一实验：
*   **数据集/场景**：涵盖了代码生成（HumanEval, MBPP）、数学推理（GSM8K, MATH）、通用助手（GAIA）、软件工程（SWE-bench）以及工业操作（AssetOpsBench）。
*   **Benchmark 分类**：
    *   **工作流生成基准**：如 WorFBench, FlowBench，侧重于评估生成的图结构是否合理。
    *   **交互式工具使用**：如 $\tau$-Bench, MCP-Bench，侧重于长程执行和工具调用质量。
*   **对比维度**：论文对比了 77 篇核心及相关文献，分析了它们在表示形式（代码、DSL、图 IR）、反馈机制和成本处理上的差异。

### 4. 资源与算力
*   **算力说明**：由于本文是综述论文，**未直接进行模型训练或大规模实验，因此未提供具体的 GPU 型号、数量或训练时长**。
*   **提及的成本**：论文强调了优化过程中的“搜索成本”（如 AFlow 使用 MCTS 搜索时的 API 调用费用）和“推理成本”（Token 消耗、延迟），并呼吁未来的研究应明确报告这些资源消耗。

### 5. 实验数量与充分性
*   **文献覆盖**：论文引用并详细分类了 **77 篇** 核心文献（39 篇核心优化论文，7 篇邻近领域，31 篇背景框架）以及 **27 个** 评估资产。
*   **充分性与客观性**：综述覆盖了从 2023 年到 2026 年初的最新进展，分类逻辑严密（GDT/GPM 维度），能够客观地反映当前学术界从“手动设计”向“自动搜索/动态生成”转型的趋势。

### 6. 主要结论与发现
*   **静态 vs 动态**：静态模板适合任务重复性高、验证器强的场景；动态生成适合任务异质性强、环境不确定性大的场景。
*   **结构胜过提示词**：在许多情况下，优化工作流拓扑（如增加验证节点、改变通信模式）比单纯调优提示词（Prompt Tuning）带来的增益更大。
*   **验证器的关键作用**：廉价且语义明确的验证器（如语法检查、单元测试）是提升工作流优化效率的核心。
*   **评估缺失**：当前研究往往只关注下游任务成功率，忽视了对图结构本身属性（深度、宽度、鲁棒性）的评估。

### 7. 优点（亮点）
*   **统一词汇表**：首次明确区分了模板、实例化图和轨迹，解决了领域内术语混乱的问题。
*   **结构感知评估**：提出了一个“最小报告协议”，建议研究者报告图级指标（如编辑次数、结构方差）和成本效率。
*   **前瞻性**：对动态运行时编辑（In-execution editing）的总结非常深入，捕捉到了智能体向高度自主化演进的趋势。

### 8. 不足与局限
*   **信用分配难题**：论文指出目前仍难以准确判断性能提升究竟归功于哪个特定的结构改动（Credit Assignment），这是该领域的共同挑战。
*   **表示形式的权衡**：虽然讨论了代码、DSL 和图 IR，但尚未有一种公认的、既具备高表达力又易于验证的标准表示形式。
*   **应用限制**：综述主要关注 LLM 驱动的逻辑流，对于底层系统级优化（如内存管理、多机调度）涉及较少。

（完）
