---
title: "Sparse but Critical: A Token-Level Analysis of Distributional Shifts in RLVR Fine-Tuning of LLMs"
title_zh: 稀疏但关键：大语言模型 RLVR 微调中分布偏移的词元级分析
authors: "Haoming Meng, Kexin Huang, Shaohang Wei, Chiyu Ma, Shuo Yang, Xue Wang, Guoyin Wang, Bolin Ding, Jingren Zhou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22446v1"
tags: ["query:token"]
score: 10.0
evidence: RLVR 微调中分布偏移的 Token 级分析
tldr: 该研究深入探讨了强化学习（RLVR）在提升大语言模型推理能力时的词元级机制。研究发现，RL微调引起的分布变化具有高度稀疏性和针对性，仅少数关键词元的概率分布发生了显著偏移。通过交叉采样干预实验，作者证明了这些极少数的词元决策是性能提升的核心驱动力。此外，研究还分析了词元熵和位置分布，并提出了一种基于散度加权的优势信号改进方案，为理解RLVR微调过程提供了细粒度的视角。
selection_source: fresh_fetch
motivation: 旨在揭示强化学习（RLVR）在微调大语言模型时，词元层面的分布变化如何影响模型推理性能的内在机制。
method: 采用词元级分布偏移分析、交叉采样干预实验以及对词元熵和概率重新分配的细粒度研究来剖析RLVR的影响。
result: 发现RL微调诱导的变化高度稀疏且集中，仅需替换极少数关键词元即可使基础模型获得RL级别的性能提升或使RL模型性能崩溃。
conclusion: RLVR微调是一个针对性极强的精炼过程，通过优化少数关键决策点而非全局分布来实现推理能力的显著飞跃。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 显著提升了大语言模型 (LLM) 的推理能力，但这些提升背后的词元级机制仍不清楚。我们对 RLVR 的分布效应进行了系统的实证研究，主要围绕三个核心分析展开：(1) 基础模型与 RL 模型之间分布偏移的词元级表征；(2) 通过交叉采样干预研究词元级分布偏移对序列级推理性能的影响；(3) 词元级偏移的细粒度机制。我们发现 RL 微调引发了高度稀疏且具有针对性的变化，只有极小比例的词元分布在基础策略和 RL 策略之间表现出显著差异。我们通过分析词元熵、位置集中度和概率质量的重新分配，进一步刻画了这些偏移的结构和演变。为了评估这些稀疏变化的功能重要性，我们进行了交叉采样实验，在不同的干预预算下，选择性地交换基础模型和 RL 模型之间的词元选择。我们表明，仅在基础模型的生成结果中插入极小比例的 RL 采样词元，就能逐步恢复 RL 的性能增益；而将同样少量的基础模型词元注入 RL 生成的序列中，则会导致性能坍缩至基础模型水平。这分离出了一组直接负责 RLVR 性能增益的极小词元级决策集。最后，我们探索了优势信号的散度加权变体作为诊断性干预，发现它们可以比基准线有所改进。总之，我们的研究结果揭示了 RLVR 引发的分布变化，并为将 RLVR 微调理解为一个有针对性的精炼过程提供了一个细粒度的词元级视角。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has significantly improved reasoning in large language models (LLMs), yet the token-level mechanisms underlying these improvements remain unclear. We present a systematic empirical study of RLVR's distributional effects organized around three main analyses: (1) token-level characterization of distributional shifts between base and RL models, (2) the impact of token-level distributional shifts on sequence-level reasoning performance through cross-sampling interventions, and (3) fine-grained mechanics of these shifts at the token level. We find that RL fine-tuning induces highly sparse and targeted changes, with only a small fraction of token distributions exhibiting meaningful divergence between the base and RL policies. We further characterize the structure and evolution of these shifts through analyses of token entropy, positional concentration, and reallocation of probability mass. To assess the functional importance of these sparse changes, we conduct cross-sampling experiments that selectively swap token choices between the base and RL models with varying intervention budgets. We show that inserting only a small fraction of RL-sampled tokens into base generations progressively recovers RL performance gains, while injecting a similarly small number of base token choices into otherwise RL-generated sequences collapses performance to base levels, isolating a small set of token-level decisions directly responsible for RLVR's performance gains. Finally, we explore divergence-weighted variants of the advantage signal as a diagnostic intervention, finding that they can yield improvements over baselines. Together, our results shed light on the distributional changes induced by RLVR and provide a fine-grained, token-level lens for understanding RLVR fine-tuning as a targeted refinement process.

---

## 论文详细总结（自动生成）

### 论文总结：稀疏但关键——大语言模型 RLVR 微调中分布偏移的词元级分析

#### 1. 核心问题与整体含义（研究动机和背景）
尽管具有可验证奖励的强化学习（RLVR，如 GRPO、DAPO）在提升大语言模型（LLM）的数学和逻辑推理能力方面取得了显著成功，但其底层机制仍是一个“黑盒”。研究者们不清楚 RL 究竟是全局性地重写了模型的行为逻辑，还是仅仅对特定关键点进行了微调。本论文旨在通过**词元（Token）级别**的细粒度分析，揭示 RLVR 如何重塑基础模型的预测分布，并确定哪些分布偏移真正驱动了推理性能的提升。

#### 2. 论文提出的方法论
论文构建了一个系统的实证框架，包含以下核心技术手段：
*   **分布偏移量化**：使用 **Jensen-Shannon (JS) 散度**来衡量基础模型（Base）与 RL 模型在相同前缀上下文下，对下一个词元预测分布的差异。
*   **交叉采样干预（Cross-Sampling Interventions）**：
    *   **正向交叉采样（Forward）**：在基础模型的生成过程中，仅在 JS 散度高的位置强制插入 RL 模型的采样结果，观察性能恢复情况。
    *   **反向交叉采样（Reverse）**：在 RL 模型的生成过程中，在关键位置换回基础模型的词元，观察性能坍缩情况。
*   **细粒度机制分析**：研究高散度位置的 Top-k 词元重叠度、排名变化（Rank Shift）以及低概率词元的提升情况。
*   **散度加权优势信号（Divergence-Weighted Advantage）**：提出一种诊断性改进方案，根据词元级的 KL 散度对 RL 训练中的优势信号（Advantage）进行加权，以验证针对性更新的有效性。

#### 3. 实验设计
*   **模型选择**：涵盖了多个规模和架构，包括 Qwen2.5-32B、Qwen2.5-Math-7B、Qwen3-8B-Base 以及 Mistral-Small-24B。
*   **数据集/Benchmark**：主要针对高难度推理任务，包括 **AIME 2024、AIME 2025、GPQA-Diamond、AMC** 以及模型自身的微调训练集。
*   **对比方法**：
    *   **算法对比**：DAPO（带 clip-higher 机制） vs. SimpleRL/GRPO（较严格的约束）。
    *   **范式对比**：RLVR vs. 监督微调（SFT）。
    *   **消融实验**：不同的 Top-p 采样配置、不同的截断阈值（Clip settings）。

#### 4. 资源与算力
*   论文明确提到，为了达到高性能，某些训练任务耗费了巨大的算力资源。例如，**Qwen3-32B-Base 使用 DAPO 训练至 500 步时，消耗了超过 140,000 GPU 小时**。
*   推理和分析使用了 **vLLM** 框架，训练则基于 **verl** 强化学习框架。具体的 GPU 型号（如 H100 或 A100）虽未在正文中逐一列出，但从算力总量看属于超大规模集群作业。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，不仅在多个模型系列上验证了结论，还通过改变采样参数（Top-p）、散度计算方式（全分布 vs 截断分布）进行了鲁棒性检查。
*   **充分性与公平性**：通过正反向交叉采样实验，建立了词元变化与最终性能之间的**因果关系**，而非简单的相关性分析。对比 SFT 的实验有力地证明了 RLVR 独特的“稀疏性”特征。实验设计客观，考虑了位置偏差、熵值影响等多种变量。

#### 6. 主要结论与发现
*   **分布偏移的稀疏性**：RLVR 引起的变化极度稀疏。在 SimpleRL 中，超过 98% 的词元分布几乎没有变化；即使是更具探索性的 DAPO，也有 80% 以上的词元保持不变。
*   **关键决策点**：尽管变化稀疏，但这些少数点（通常出现在序列开头或结尾）具有决定性。仅替换 1.5%~10% 的关键词元，就能让基础模型达到 RL 模型的性能。
*   **概率重分配而非“创新”**：在高散度位置，RL 主要是对基础模型已经认为“可能”的词元进行重新排序和权重分配，很少凭空创造出基础模型认为概率极低的词元。
*   **与 SFT 的本质区别**：SFT 引起的变化是全局且密集的，而 RLVR 像是一把“手术刀”，进行精准的轨迹引导。

#### 7. 优点
*   **视角独特**：从 Token 级别的分布偏移切入，避开了宏观指标（如准确率、长度）的局限性。
*   **因果论证强**：交叉采样实验设计精巧，直观地证明了“稀疏词元决定全局性能”的论点。
*   **实战指导意义**：发现 RL 训练在后期会变得更加聚焦，且散度加权优势信号的尝试为未来优化 RL 算法提供了新思路。

#### 8. 不足与局限
*   **任务局限性**：研究主要集中在具有“可验证奖励”的数学和逻辑推理任务上，对于创意写作、角色扮演等缺乏客观评价标准的通用领域，该结论是否成立尚待验证。
*   **计算成本**：词元级的全分布分析需要极高的存储和计算资源，普通研究者难以复现大规模模型的完整分析。
*   **黑盒依然存在**：虽然定位到了“哪些”词元关键，但对于“为什么”这些词元在语义逻辑上关键，仍缺乏深层的语言学或逻辑学解释。

（完）
