---
title: "FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization"
title_zh: FIPO：通过受未来 KL 影响的策略优化激发深度推理
authors: "Chiyu Ma, Shuo Yang, Kexin Huang, Jinda Lu, Haoming Meng, Shangshang Wang, Bolin Ding, Soroush Vosoughi, Guoyin Wang, Jingren Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19835v2"
tags: ["query:token"]
score: 10.0
evidence: 用于深度推理的 Token 级强化学习
tldr: 本研究提出FIPO算法，旨在解决大语言模型在复杂推理中的瓶颈。针对传统GRPO算法中基于结果的奖励（ORM）导致信用分配粗糙、无法区分关键逻辑节点的问题，FIPO通过引入折扣未来KL散度，构建了稠密优势函数，实现了对Token权重的精细化调整。实验表明，FIPO显著提升了Qwen2.5-32B的推理长度和准确率，在AIME 2024测试中超越了o1-mini等模型，证明了稠密信用分配在强化学习中的重要性。
selection_source: fresh_fetch
motivation: 传统强化学习算法采用粗粒度的结果奖励，无法有效区分推理轨迹中关键逻辑节点与普通Token，限制了模型的推理性能。
method: 引入未来KL散度影响的策略优化（FIPO），通过折扣未来KL散度构建稠密优势函数，根据Token对后续行为的影响力进行重新加权。
result: "在Qwen2.5-32B上，FIPO将平均思维链长度从4千扩展至1万以上Token，并将AIME 2024准确率提升至58%，优于o1-mini。"
conclusion: 构建稠密的优势函数表述是演进基于结果奖励算法、解锁基础模型深度推理潜力的核心路径。
---

## 摘要
我们提出了受未来 KL 影响的策略优化（FIPO），这是一种旨在克服大语言模型推理瓶颈的强化学习算法。虽然 GRPO 风格的训练能够有效扩展，但它通常依赖于基于结果的奖励（ORM），将全局优势均匀地分配给轨迹中的每一个 token。我们认为，这种粗粒度的信用分配由于无法区分关键逻辑转折点与平凡 token，从而限制了性能上限。FIPO 通过将折扣未来 KL 散度引入策略更新来解决这一问题，从而创建了一种稠密优势公式，根据 token 对后续轨迹行为的影响对其进行重新加权。实验表明，FIPO 使模型能够突破标准基准测试中出现的长度停滞现象。在 Qwen2.5-32B 上的评估显示，FIPO 将平均思维链长度从约 4,000 个 token 延长至 10,000 个以上，并将 AIME 2024 Pass@1 准确率从 50.0% 提升至 58.0% 的峰值（收敛于约 56.0%）。这一表现优于 DeepSeek-R1-Zero-Math-32B（约 47.0%）和 o1-mini（约 56.0%）。我们的结果表明，建立稠密优势公式是演进基于 ORM 的算法以释放基础模型全部推理潜力的关键路径。我们开源了基于 verl 框架构建的训练系统。

## Abstract
We present Future-KL Influenced Policy Optimization (FIPO), a reinforcement learning algorithm designed to overcome reasoning bottlenecks in large language models. While GRPO style training scales effectively, it typically relies on outcome-based rewards (ORM) that distribute a global advantage uniformly across every token in a trajectory. We argue that this coarse-grained credit assignment imposes a performance ceiling by failing to distinguish critical logical pivots from trivial tokens. FIPO addresses this by incorporating discounted future-KL divergence into the policy update, creating a dense advantage formulation that re-weights tokens based on their influence on subsequent trajectory behavior. Empirically, FIPO enables models to break through the length stagnation seen in standard baselines. Evaluated on Qwen2.5-32B, FIPO extends the average chain-of-thought length from roughly 4,000 to over 10,000 tokens and increases AIME 2024 Pass@1 accuracy from 50.0% to a peak of 58.0% (converging at approximately 56.0\%). This outperforms both DeepSeek-R1-Zero-Math-32B (around 47.0%) and o1-mini (approximately 56.0%). Our results suggest that establishing dense advantage formulations is a vital path for evolving ORM-based algorithms to unlock the full reasoning potential of base models. We open-source our training system, built on the verl framework.

---

## 论文详细总结（自动生成）

以下是对论文《FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的推理能力提升主要依赖于大规模强化学习（如 GRPO）。然而，GRPO 依赖于“基于结果的奖励”（ORM），这种方式将单一的成功/失败信号均匀分配给轨迹中的每一个 Token。
*   **核心问题**：这种“粗粒度”的信用分配（Credit Assignment）存在性能瓶颈。它无法区分轨迹中关键的逻辑转折点和无关紧要的填充 Token，导致模型在训练中出现“长度停滞”现象，难以自发演化出更深层次、更长的思维链（CoT）。
*   **整体含义**：论文旨在证明，无需复杂的判别器（Critic）模型，仅通过改进策略优化公式，也能实现“稠密”的优势估计，从而解锁基础模型的深度推理潜力。

### 2. 论文提出的方法论：FIPO
*   **核心思想**：引入“未来 KL 散度”（Future-KL）来重新衡量每个 Token 的重要性。如果一个 Token 的出现导致后续轨迹与旧策略相比发生了显著且积极的偏移，则该 Token 应获得更高的权重。
*   **关键技术细节**：
    *   **概率偏移（$\Delta \log p$）**：计算当前策略与旧策略在 Token 级别的对数概率差，作为行为调整的信号。
    *   **未来 KL 估计**：定义为从当前步骤 $t$ 到序列结束 $T$ 的累积折扣概率偏移。公式考虑了折扣因子 $\gamma$（软衰减窗口），使近期的后续 Token 比远期的影响更大。
    *   **稳定性机制**：为了防止梯度爆炸，引入了**极端值过滤（Masking）**和**影响权重裁剪（Clipping）**。通过屏蔽超过 Dual-Clip 阈值的 Token，并限制未来 KL 权重的波动范围，确保训练平稳。
    *   **稠密优势重加权**：将原始的组相对优势（Group Relative Advantage）乘以基于未来 KL 的指数权重，实现对关键逻辑步骤的精准激励。

### 3. 实验设计
*   **基础模型**：Qwen2.5-32B-Base（强调使用未经长思维链 SFT 的纯净基座模型，以观察推理能力的自发涌现）。
*   **数据集**：使用公开的 DAPO-17K 数学推理数据集。
*   **Benchmark**：主要评估 AIME 2024 和 AIME 2025（高难度数学竞赛）。
*   **对比方法**：
    *   **DAPO**（作为主要的 GRPO 变体基准）。
    *   **DeepSeek-R1-Zero-Qwen-32B**。
    *   **o1-mini**（OpenAI 的商业推理模型）。

### 4. 资源与算力
*   **框架**：基于 `verl` 强化学习框架构建。
*   **训练配置**：全局 Batch Size 为 512，每个 Prompt 采样 16 个响应（Group Size G=16）。最大响应长度设为 20,480 Token。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）及具体数量，也未透露总训练时长。但提到了针对 32B 模型优化了内存效率，采用了分块（Chunk-based）计算矩阵的方法以节省显存。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 模型的扩展实验。
*   **消融实验**：非常充分。作者针对衰减率（$\tau$）、过滤机制、权重裁剪范围、Mini-batch 大小以及最大长度限制等多个变量进行了详细的消融研究。
*   **公平性**：实验在相同的硬件框架（verl）和相同的数据集（DAPO-17K）下对比了基准方法，确保了算法改进是性能提升的主因。通过 32 次采样取平均值（Pass@1）来降低随机性影响。

### 6. 主要结论与发现
*   **突破长度瓶颈**：FIPO 成功让模型突破了 4,000 Token 的长度停滞，平均推理长度扩展至 10,000 Token 以上。
*   **性能超越**：在 AIME 2024 上，FIPO 将 Pass@1 准确率从 50% 提升至 58%（峰值），超过了 o1-mini（约 56%）和 DeepSeek-R1-Zero-32B。
*   **自发涌现行为**：随着训练进行，模型自发产生了**自我反思（Self-reflection）**和**多路径验证**行为，证明了长思维链与推理准确率之间的强正相关性。
*   **稳定性**：相比于标准 GRPO，FIPO 在策略漂移（Policy KL）和梯度范数上表现得更加平滑。

### 7. 优点
*   **算法简洁高效**：在不增加 Critic 模型（节省显存和计算开销）的前提下，实现了类似 PPO 的稠密信用分配效果。
*   **揭示演化路径**：清晰地展示了模型如何从“表面规划”演进到“线性执行”，再到“系统化深度推理”的过程。
*   **开源贡献**：开源了完整的训练代码和配置，对社区复现类 R1 模型具有高度参考价值。

### 8. 不足与局限
*   **计算成本**：虽然算法本身高效，但由于诱导出了极长的推理轨迹（10k+ tokens），推理和训练的实际计算开销显著增加。
*   **任务覆盖面**：目前主要集中在数学推理（AIME），尚未在代码、符号逻辑或通用对话任务上进行广泛验证。
*   **数据依赖**：实验仅基于 17K 的小规模数据集，尚未探讨在大规模数据 regime 下的扩展性。
*   **与蒸馏的差距**：作者承认，这种“发现式”的强化学习虽然能提升基座能力，但在效率上仍难以完全抹平与高性能教师模型蒸馏出的模型之间的差距。

（完）
