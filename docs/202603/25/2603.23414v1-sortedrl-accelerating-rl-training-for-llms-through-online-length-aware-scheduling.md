---
title: "SortedRL: Accelerating RL Training for LLMs through Online Length-Aware Scheduling"
title_zh: SortedRL：通过在线长度感知调度加速大语言模型的强化学习训练
authors: "Yiqi Zhang, Huiqiang Jiang, Xufang Luo, Zhihe Yang, Chengruidong Zhang, Yifei Shen, Dongsheng Li, Yuqing Yang, Lili Qiu, Yang You"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23414v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 加速大语言模型推理和长思维链生成的强化学习训练
tldr: 针对大语言模型强化学习（RL）训练中生成长序列导致的Rollout阶段效率瓶颈，本文提出SortedRL调度策略。该方法通过在线长度感知调度，根据输出长度对样本重新排序，优先处理短样本以实现早期更新，并结合缓存机制控制离策程度。实验表明，SortedRL在显著减少训练空泡的同时，在逻辑推理和数学任务上取得了更优的性能表现。
selection_source: fresh_fetch
motivation: 长链条推理任务中自回归生成的缓慢和Rollout与更新间的同步开销严重制约了LLM强化学习的训练效率。
method: 提出一种在线长度感知调度策略，通过对Rollout样本按长度排序并优先组队更新，配合状态控制器和缓存机制优化流水线。
result: "在LLaMA-3.1和Qwen-2.5上的实验显示，该方法减少了超过50%的训练空泡，并在相同数据量下提升了3.9%至18.4%的准确率。"
conclusion: SortedRL通过高效的长度感知调度，在不牺牲训练稳定性的前提下，显著加速了LLM的强化学习过程并增强了其推理能力。
---

## 摘要
扩展强化学习（RL）在增强大语言模型（LLM）的推理能力方面展现出巨大潜力，特别是在需要长思维链生成的任务中。然而，RL 训练效率通常受限于采样（rollout）阶段，在生成长轨迹（如 16k token）时，由于自回归生成缓慢以及采样与策略更新之间的同步开销，该阶段可占总训练时间的 70%。我们提出了 SortedRL，这是一种在线长度感知调度策略，旨在通过提高采样效率并保持训练稳定性来解决这一瓶颈。SortedRL 根据输出长度对采样样本进行重新排序，优先处理短样本并成组进行早期更新。这使得大批量采样、灵活的更新批次以及近乎同策略的微课程构建能够同时实现。为了进一步加速流水线，SortedRL 引入了一种通过基于缓存的机制来控制离策略训练程度的机制，并由专门的 RL 基础设施支持，该基础设施通过有状态控制器和采样缓冲区管理采样与更新。在 LLaMA-3.1-8B 和 Qwen-2.5-32B 上进行的实验涵盖了逻辑谜题以及 AIME 24、Math 500 和 Minerval 等数学挑战，结果表明，SortedRL 将 RL 训练气泡率降低了 50% 以上，同时在相同数据量下，性能比基准模型提升了 3.9% 至 18.4%。

## Abstract
Scaling reinforcement learning (RL) has shown strong promise for enhancing the reasoning abilities of large language models (LLMs), particularly in tasks requiring long chain-of-thought generation. However, RL training efficiency is often bottlenecked by the rollout phase, which can account for up to 70% of total training time when generating long trajectories (e.g., 16k tokens), due to slow autoregressive generation and synchronization overhead between rollout and policy updates. We propose SortedRL, an online length-aware scheduling strategy designed to address this bottleneck by improving rollout efficiency and maintaining training stability. SortedRL reorders rollout samples based on output lengths, prioritizing short samples forming groups for early updates. This enables large rollout batches, flexible update batches, and near on-policy micro-curriculum construction simultaneously. To further accelerate the pipeline, SortedRL incorporates a mechanism to control the degree of off-policy training through a cache-based mechanism, and is supported by a dedicated RL infrastructure that manages rollout and update via a stateful controller and rollout buffer. Experiments using LLaMA-3.1-8B and Qwen-2.5-32B on diverse tasks, including logical puzzles, and math challenges like AIME 24, Math 500, and Minerval, show that SortedRL reduces RL training bubble ratios by over 50%, while attaining 3.9% to 18.4% superior performance over baseline given same amount of data.

---

## 论文详细总结（自动生成）

### SortedRL：通过在线长度感知调度加速大语言模型强化学习训练

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理能力的提升（如 DeepSeek-R1、OpenAI o1），强化学习（RL）已成为增强模型长思维链（CoT）能力的关键。然而，RL 训练面临严重的效率瓶颈：
*   **采样阶段（Rollout）占主导**：在生成长轨迹（如 16k token）时，采样时间可占总训练时间的 70%。
*   **硬件利用率低（气泡问题）**：由于 LLM 生成长度呈长尾分布，且传统 RL 算法要求采样与更新同步，导致 GPU 必须等待最长的样本生成完毕才能开始更新，造成严重的计算资源浪费（气泡率高达 74%）。
*   **策略一致性与效率的矛盾**：增大采样批次虽能提高吞吐，但若更新批次固定，会导致模型在旧数据上多次更新，增加“离策略（Off-policy）”风险，损害训练稳定性。

#### 2. 方法论：核心思想与关键技术
SortedRL 提出了一种**在线长度感知调度策略**，旨在不牺牲训练稳定性的前提下消除计算气泡：
*   **在线长度感知调度**：控制器动态感知生成进度。当部分短样本生成完毕后，立即将其“收割”并成组送入训练器更新，而无需等待长样本。
*   **分组采样与微课程构建**：将 Prompt 分组（Group），在组内优先处理短响应。由于响应长度通常与任务难度正相关，这种排序自然形成了一种“从易到难”的在线微课程（Micro-curriculum），有助于提升收敛效率。
*   **可控离策略机制**：
    *   **全同策略模式（Fully On-policy）**：仅使用最新策略生成的完整轨迹，丢弃或重新采样被中断的长样本。
    *   **部分模式（Partial Mode）**：利用缓存机制存储被中断的轨迹片段及其对数概率（log-probs），在下一轮继续生成并拼接，通过重要性采样修正偏差。
*   **协同设计基础设施**：开发了包含“有状态控制器”和“采样缓冲区”的专用架构，支持中间状态的保存与恢复。

#### 3. 实验设计
*   **数据集与场景**：
    *   **逻辑推理**：LogicRL（5000 个合成的骑士与骗子谜题），评估模型在规则约束下的演绎能力。
    *   **数学任务**：DAPO-Math-17k（1.7 万个数学竞赛题目）。
*   **Benchmark**：GSM8k、MATH500、Minerva Math、OlympiadBench、AIME 2024、AMC 2023。
*   **对比方法**：Vanilla Reinforce++（逻辑任务基准）和 PPO（数学任务基准）。
*   **评估指标**：验证集得分、Mean@32 准确率、响应长度变化、采样吞吐量及气泡率。

#### 4. 资源与算力
*   **硬件环境**：实验在 NVIDIA H100 和 AMD MI300X GPU 混合集群上运行。
*   **计算配置**：配备 96 核 Intel Xeon Platinum 8480C CPU。
*   **训练时长**：文中未明确给出总训练小时数，但提供了吞吐量对比：SortedRL 的采样吞吐量较基准提升了约 7.57%（全同策略）至 39.48%（部分模式）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B（LLaMA-3.1）到 32B（Qwen-2.5）不同规模的模型。
*   **消融实验**：针对“分组采样”、“排序方式（后验排序 vs 在线排序）”以及“分组大小（Group Size）”进行了详细的消融研究。
*   **客观性**：实验对比了相同数据量下的性能表现，并分析了响应长度与模型得分的微观关联，证明了性能提升并非单纯来自序列长度的增加，而是来自更高效的策略更新。

#### 6. 主要结论与发现
*   **效率大幅提升**：将采样阶段的计算气泡率从 **74% 降低至 6% 以下**。
*   **性能显著增强**：在相同训练数据量下，SortedRL 在 AIME 2024 等竞赛级数学任务上的准确率比基准提升了 **3.9% 至 18.4%**。
*   **收敛更快**：在逻辑任务中，SortedRL 达到相同准确率所需的样本量比基准减少了 **40.74%**。
*   **长度与性能关联**：SortedRL 诱导模型更早地探索长思维链，且其形成的微课程结构有助于模型在复杂任务上更稳健地学习。

#### 7. 优点
*   **系统与算法协同**：不仅是系统层面的调度优化，还利用调度特性构建了有利于学习的微课程。
*   **灵活性高**：支持在“极致采样效率（部分离策略）”和“极致训练稳定性（全同策略）”之间灵活切换。
*   **即插即用**：作为基础设施层面的改进，可以适配 PPO、Reinforce++ 等多种主流 RL 算法。

#### 8. 不足与局限
*   **超参数敏感性**：分组大小（Group Size）对训练效果有显著影响，设置不当可能导致模型只学习短样本或训练停滞。
*   **离策略偏差风险**：虽然提供了部分模式，但在极长序列下，多次中断拼接带来的累积偏差对训练稳定性的长期影响仍需更多理论验证。
*   **算力细节缺失**：未提供完整的端到端训练时间成本（Wall-clock time）对比，仅侧重于采样阶段的吞吐量提升。

（完）
