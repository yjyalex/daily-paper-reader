---
title: "Benchmarking Multi-Agent LLM Architectures for Financial Document Processing: A Comparative Study of Orchestration Patterns, Cost-Accuracy Tradeoffs and Production Scaling Strategies"
title_zh: 金融文档处理中的多智能体大语言模型架构基准测试：编排模式、成本-准确率权衡与生产扩展策略的比较研究
authors: "Siddhant Kulkarni, Yukta Kulkarni"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22651v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 对用于结构化提取的多智能体编排架构进行基准测试
tldr: 本研究针对金融文档处理，系统性地评估了四种多智能体LLM编排架构（顺序、并行、层级、反思）。通过对1万份SEC文件的25类字段提取实验，对比了前沿及开源模型在准确率、成本和延迟等维度的表现。研究发现反思型架构准确率最高但成本昂贵，而层级架构在成本效益平衡上表现最佳。此外，提出的混合配置和扩展策略为金融生产环境下的多智能体系统部署提供了实战指南。
selection_source: fresh_fetch
motivation: 在金融文档结构化信息提取的生产部署中，缺乏关于多智能体编排架构选择及其成本-准确率权衡的实证指导。
method: 在1万份SEC文件上系统对比了顺序流水线、并行分发合并、层级主管-工人及反思自纠错四种多智能体架构的性能。
result: 反思型架构获得最高F1值（0.943）但成本最高，而层级架构在成本-准确率权衡中表现最优，混合配置可大幅降低成本并保持高性能。
conclusion: 通过合理的架构选择与混合优化策略，可以在受监管的金融环境中实现高效且经济的大规模多智能体LLM部署。
---

## 摘要
大语言模型（LLM）在金融文档结构化信息提取中的应用正迅速普及，但生产部署仍面临缺乏实证指导的基础架构决策。我们提出了一项系统性基准测试，比较了四种多智能体编排架构：顺序流水线（sequential pipeline）、并行扇出合并（parallel fan-out with merge）、分层主管-执行者（hierarchical supervisor-worker）以及反思性自纠错循环（reflexive self-correcting loop）。这些架构在 10,000 份美国证券交易委员会（SEC）申报文件（10-K、10-Q 和 8-K 表格）语料库上，针对五种前沿及开源权重 LLM 进行了评估。我们的评估涵盖了 25 种提取字段类型，包括治理结构、高管薪酬和财务指标，并从五个维度进行衡量：字段级 F1 分数、文档级准确率、端到端延迟、单文档成本和 Token 效率。研究发现，反思性架构实现了最高的字段级 F1 分数（0.943），但其成本是顺序基准架构的 2.3 倍；而分层架构在成本-准确率帕累托前沿（Pareto frontier）上占据了最有利的位置（F1 分数为 0.921，成本为 1.4 倍）。我们进一步展示了关于语义缓存、模型路由和自适应重试策略的消融研究，证明混合配置仅需 1.15 倍的基准成本即可获得反思性架构 89% 的准确率提升。我们对每日 1,000 到 100,000 份文档的扩展性分析揭示了非显性的吞吐量-准确率下降曲线，为容量规划提供了参考。这些发现为在受监管的金融环境中部署多智能体 LLM 系统的从业者提供了可操作的指导。

## Abstract
The adoption of large language models (LLMs) for structured information extraction from financial documents has accelerated rapidly, yet production deployments face fundamental architectural decisions with limited empirical guidance. We present a systematic benchmark comparing four multi-agent orchestration architectures: sequential pipeline, parallel fan-out with merge, hierarchical supervisor-worker and reflexive self-correcting loop. These are evaluated across five frontier and open-weight LLMs on a corpus of 10,000 SEC filings (10-K, 10-Q and 8-K forms). Our evaluation spans 25 extraction field types covering governance structures, executive compensation and financial metrics, measured along five axes: field-level F1, document-level accuracy, end-to-end latency, cost per document and token efficiency. We find that reflexive architectures achieve the highest field-level F1 (0.943) but at 2.3x the cost of sequential baselines, while hierarchical architectures occupy the most favorable position on the cost-accuracy Pareto frontier (F1 0.921 at 1.4x cost). We further present ablation studies on semantic caching, model routing and adaptive retry strategies, demonstrating that hybrid configurations can recover 89\% of the reflexive architecture's accuracy gains at only 1.15x baseline cost. Our scaling analysis from 1K to 100K documents per day reveals non-obvious throughput-accuracy degradation curves that inform capacity planning. These findings provide actionable guidance for practitioners deploying multi-agent LLM systems in regulated financial environments.

---

## 论文详细总结（自动生成）

这篇论文对用于金融文档处理的多智能体大语言模型（LLM）架构进行了深入的基准测试，重点探讨了不同编排模式在成本、准确率和生产扩展性之间的权衡。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：金融行业（如SEC申报文件）涉及海量非结构化文档，传统的规则解析或单次提示词（Single-prompt）LLM提取存在上下文窗口限制、幻觉率高、缺乏验证机制等问题。
*   **核心问题**：虽然多智能体架构（MAS）能通过任务分解和验证循环解决上述问题，但在生产部署中，开发者缺乏关于“哪种编排模式最适合特定业务需求”的实证指导。
*   **研究意义**：在受监管的金融环境中，架构选择直接影响到合规性（准确率）和运营成本（API/算力开销）。

### 2. 方法论：核心思想与架构设计
论文对比了四种典型的多智能体编排架构，所有架构均由文档解析、字段提取、表格分析、交叉引用解析、置信度评分和格式化等原子智能体组成：
*   **架构 A：顺序流水线 (Sequential)**：线性执行，上游输出作为下游输入。特点是简单、延迟随步骤线性增长，但错误会向下游累积。
*   **架构 B：并行分发合并 (Parallel)**：将文档按领域（财务、治理、薪酬）拆分并行提取，最后由合并智能体汇总。特点是延迟低，能有效处理长文档。
*   **架构 C：层级主管-工人 (Hierarchical)**：由主管智能体动态分配任务并监控进度，对低置信度结果进行有针对性的重新提取。支持不同模型路由（强模型处理难任务）。
*   **架构 D：反思自纠错循环 (Reflexive)**：引入显式验证环节，通过“提取-批判-修正”循环迭代（最多3次）。特点是准确率最高，但成本和延迟不可控。
*   **优化策略**：提出了**层级优化版 (Hierarchical-Optimized)**，结合了语义缓存（减少重复调用）、模型路由（2层或3层模型混合）和自适应重试机制。

### 3. 实验设计与基准测试
*   **数据集**：构建了一个包含 **10,000 份 SEC 申报文件**（10-K, 10-Q, 8-K）的大型语料库，涵盖 2021-2024 财年。
*   **提取字段**：共 25 个字段，分为财务指标（如总收入）、治理（如董事会规模）、高管薪酬（如CEO总薪酬）三大类。
*   **对比模型**：涵盖前沿模型（GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro）和开源模型（Llama 3 70B, Mixtral 8x22B）。
*   **评估维度**：字段级 F1 值、文档级准确率、端到端延迟、单文档成本、Token 效率。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台服务器**，每台配置为 96 核 CPU、384 GB RAM 和 **4 张 NVIDIA A100 80GB GPU**（总计 32 张 A100）。
*   **实验时长**：整个实验周期持续了 **21 天**。
*   **推理框架**：开源模型使用 vLLM 框架进行张量并行推理。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 **200,000 次文档级评估**（4 种架构 × 5 种模型 × 10,000 份文档），规模极其庞大。
*   **消融实验**：针对语义缓存、模型路由、重试策略分别做了详细的消融研究。
*   **扩展性分析**：模拟了每日 1K 到 100K 份文档的处理压力。
*   **客观性**：使用了由 CFA/CPA 持证人员标注的金标准数据，并报告了 95% 的置信区间，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能之王**：**反思型架构 (Reflexive)** 配合 Claude 3.5 Sonnet 达到最高 F1 值 (0.943)，但成本是顺序架构的 2.3 倍。
*   **性价比之王**：**层级架构 (Hierarchical)** 处于帕累托最优位置，能以 60% 的成本实现反思型架构 98% 以上的准确率。
*   **优化潜力**：**层级优化版**仅需 1.15 倍基准成本即可收回反思型架构 89% 的准确率增益。
*   **扩展性悖论**：反思型架构在处理量超过 2.5万份/日时性能急剧下降（受限于超时和排队），而顺序架构在超大规模下最稳健。
*   **失败模式**：识别了 12 种失败模式，发现反思型架构容易陷入“过度思考”导致的歧义震荡，而顺序架构则受困于跨表引用失败。

### 7. 优点与亮点
*   **实战导向**：不仅关注准确率，还将成本、延迟和扩展性作为核心指标，对工业界部署极具参考价值。
*   **架构深度**：系统化地定义并对比了 MAS 的四种基本编排模式，而非单一模型的微调。
*   **优化方案务实**：提出的模型路由和语义缓存方案是解决 LLM 昂贵推理成本的有效路径。
*   **失败分类学**：详细的失败模式分析为后续系统优化提供了明确的方向。

### 8. 不足与局限
*   **地域局限**：数据集仅限于英文 SEC 文件，未测试其他语言或会计准则（如 IFRS）下的表现。
*   **时效性风险**：成本分析基于 2025 年初的 API 价格，随着模型降价，具体的成本比例可能会发生变化。
*   **字段固定**：仅测试了 25 个预设字段，对于更长尾或需要复杂推理的开放式问题，架构表现尚不明确。
*   **模型漂移**：未考虑 API 模型随时间更新（Model Drift）对系统稳定性的影响。

（完）
