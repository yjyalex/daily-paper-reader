---
title: "Provable Benefits of RLVR over SFT for Reasoning Models: Learning to Backtrack Efficiently"
title_zh: 强化学习验证奖励相比监督微调在推理模型中的可证明优势：学习高效回溯
authors: "Stanley Wei, Juno Kim"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22938v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 理论证明RLVR在推理上优于SFT，特别是思维链中的回溯能力
tldr: 将思维链推理建模为图上的路径搜索问题。理论证明，仅用最优路径进行有监督微调无法学会高效回溯；而基于可验证奖励的强化学习能利用结果奖励学会从死胡同回溯。这导致两者在推理时计算量上指数级分离，且RLVR模型学会识别推理链中的困难决策点，从而优化计算分配。RLVR模型的推理轨迹可蒸馏给基础模型以习得高效回溯。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1465, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 840, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 821, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1761, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1753, \"height\": 970, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1745, \"height\": 1126, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22938v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1721, \"height\": 1147, \"label\": \"Figure\"}]"
motivation: 解释为何强化微调比纯监督微调更能提升推理能力，特别是回溯效率。
method: 将思维链推理建模为图路径搜索，对比SFT（只训练最优路径）和RLVR（利用结果奖励学习）。
result: 证明RLVR能学会高效回溯，而SFT不能，导致推理时计算量指数级分离。
conclusion: RLVR通过结果奖励引导模型学习回溯，蒸馏其轨迹可迁移高效回溯能力。
---

## 摘要
大型语言模型的最新进展表明，对预训练基础模型进行强化微调可以在推理时显著提升推理性能。本文从理论上分析了为什么强化微调比纯粹的监督微调方法能诱导出更好的推理能力。我们将思维链推理建模为图上的路径查找问题，并比较了流行的可验证奖励强化学习方法与传统监督微调。我们证明，当监督微调在没有负例的情况下训练于最优最短路径时，无法学会高效回溯。相反，经过可验证奖励强化训练模型仅使用结果奖励就能学会从死胡同高效回溯。这导致两种方法在推理时计算量上存在指数级差距，并表明可验证奖励强化学习使模型学会识别推理链中困难决策的位置，最终实现推理时计算资源的更好分配。最后，我们展示了可验证奖励强化学习模型的推理轨迹可以被蒸馏出来，训练基础模型同样高效回溯。

## Abstract
Recent advances in large language models (LLMs) have demonstrated that reinforcement fine-tuning of pretrained base models can lead to significant gains in reasoning performance at inference time. In this work, we theoretically analyze why reinforcement fine-tuning induces better reasoning ability than purely supervised fine-tuning (SFT) methods. We model chain-of-thought (CoT) reasoning as a pathfinding problem on graphs and compare the popular method of reinforcement learning with verifiable rewards (RLVR) against traditional SFT. We prove that SFT, when trained on golden shortest paths without negative examples, fails to learn how to efficiently backtrack. In contrast, an RLVR-trained model can learn how to efficiently backtrack from dead ends using only outcome reward. This leads to an exponential separation in inference-time compute between the two methods, and demonstrates that RLVR leads the model to learn the location of difficult decisions in a reasoning chain, ultimately allowing for better allocation of inference-time compute. Finally, we show that the reasoning traces of an RLVR model can be distilled to train a base model to backtrack efficiently as well.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：为什么强化微调（RL）比监督微调（SFT）能显著提升大语言模型的推理能力？具体而言，哪种后训练方法能教会模型在推理过程中高效回溯（backtracking）？
- **背景**：当前大语言模型通过思维链（CoT）进行逐步推理，需要能够从错误分支中退回并探索其他路径（即回溯）。SFT 通常只提供最优专家演示（无负例），RLVR 则通过结果奖励和长度惩罚在策略自探索中学习。本文试图从理论层面解释两者的本质差异。
- **整体含义**：揭示了 RLVR 由于生成并训练了模型自身的失败部分轨迹，从而学会高效回溯；而 SFT 因缺乏反向状态数据，无法学会退却策略，导致推理时计算量呈指数级增长。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 将 CoT 推理建模为**图路径查找问题**，设计一个由源节点、分叉节点、多个分支（每个分支由一系列钻石子图构成）和叶节点组成的多图。
- 使用**线性-softmax bigram（双字母组）模型**作为预训练的世界模型，然后分别用 SFT 和 RLVR 进行后训练。
- 关键区别：SFT 仅在**最优最短路径**（无负例）上训练；RLVR 则通过**策略自生成轨迹**，使用结果奖励 + 长度惩罚进行优化。

### 关键技术细节
- **图结构**：每个分支有 K 个钻石，每个钻石有 L 条平行边，分支数为 W。
- **状态表示**：每个状态是“有向边+方向”的 one-hot 向量，策略为 softmax bigram。
- **预训练**：通过最大似然学习世界模型（均匀转移假设），确保预训练后模型收敛到世界模型的分布。
- **SFT 损失**：
  \[
  \min_{\Theta} L(\Theta) = \mathbb{E}_{x\sim U(t_i), y\sim D_x}\left[-\sum_{t=0}^{|y|-1}\log \pi_\Theta(y_{t+1}|y_t)\right]
  \]
  其中 \(D_x\) 是通向目标 \(x\) 的最短路径分布。
- **RLVR 损失**：
  \[
  \max_{\Theta} J(\Theta) = \mathbb{E}_{x\sim U(t_i), y\sim \pi_\Theta}[r(x,y)], \quad r(x,y)=1\{y\text{ reaches }x\} - \beta|y|
  \]
  使用策略梯度，梯度更新采用**符号梯度流**（signed gradient flow）以避免非收敛问题。
- **理论分析**：通过分析 logit 差距的 ODE 动力学，证明 SFT 中反向状态（\(b_j, d_j\)）保持不变，而 RLVR 中所有状态（向前和向后）都会收敛到 1。

### 算法流程（文字说明）
1. 预训练：初始化 logits 为 0，基于均匀转移分布进行梯度流训练，直到近似世界模型。
2. SFT：固定预训练参数，在最短路径数据集上继续梯度流，只更新前向状态 logits，反向状态不变。
3. RLVR：从预训练模型开始，在每个时间步对每个状态根据符号策略梯度更新 logits。分为两阶段：Phase I 中所有反向状态梯度变为正；Phase II 中所有概率收敛到 1。
4. 蒸馏：用 RLVR 收敛策略生成的轨迹作为数据，对基础模型进行 SFT，可继承高效回溯能力。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

### 数据集/场景
- **合成图路径查找任务**：图结构为 W 个分支，每个分支 K 个钻石，每个钻石 L 条平行边（参数如 W=15, K=15, L=5）。目标是从源节点找到通向指定叶节点的路径。
- 该场景是研究 CoT 推理的标准沙盒，常用于分析回溯、搜索等能力。

### Benchmark
- 主要指标：**期望命中时间**（expected hitting time），即从源出发到达目标叶节点所需的平均步数。
- 理论最优下界为 \(4WK\)（RLVR 收敛后的期望时间）。

### 对比方法
- **SFT**：仅在最优化路径上训练。
- **RLVR**：基于符号梯度流的强化学习，结果奖励 + 长度惩罚 \(\beta=1\)。
- **蒸馏**：用 RLVR 轨迹进行 SFT。
- 还对比了带搜索代理（禁止重复访问状态）的变体。
- 额外实验：PPO 实现 RLVR、单层 Transformer 训练、非对称图场景（不同分支长度）。

## 4. 资源与算力

- 论文**未明确提及**使用的 GPU 型号、数量或训练时长。所有实验均在合成环境中运行，计算量较小（数千迭代，每次迭代涉及模拟路径）。
- 实验章节提到“使用学习率 0.01 的符号梯度下降”，但未提具体硬件资源。

## 5. 实验数量与充分性

- **实验数量**：主体实验在合成图上进行（如 W=15, K=15, L=5），展示了训练过程中的期望命中时间和四个概率值（\(a_j,b_j,c_j,d_j\)）随迭代的变化。另外还有：
  - 不同 L 和 K 下 SFT 命中时间的对数尺度图（图5左）。
  - 随机回溯监督比例对 SFT 命中时间的影响（图5右）。
  - PPO 实现 RLVR 的收敛曲线（图6）。
  - 单层 Transformer 训练 RLVR 的结果（图7）。
  - 非对称图（不同分支长度）的收敛（图8）。
  - 蒸馏实验的理论证明（定理5）在正文中未提供额外图表，但逻辑清晰。
- **充分性与公平性**：
  - 实验覆盖了关键参数（W, K, L）的变化，验证了指数级分离。
  - 合成环境确保了可复现性和因果推断，但缺乏真实 NLP 推理数据集，可能限制外部有效性。
  - 对比方法明确（SFT vs RLVR），但未与其他 RL 变体（如 RLHF、PPO 参数敏感）系统性对比；PPO 实验仅作为一个示例。
  - 消融实验（如回溯数据比例）合理，但未做统计学显著性检验。
  - 整体上实验设计严谨，但数量相对有限（主体图表约8张），对于理论论文已足够。

## 6. 论文的主要结论与发现

1. **SFT 无法学习高效回溯**：训练于最优路径后，前向概率收敛至 1，但反向概率保持初始均匀分布（\(b_j = L/(L+1), d_j = 1/(L+1)\)）。
2. **RLVR 能学会高效回溯**：通过符号梯度流，所有状态（向后和向前）最终收敛到 1，形成“进入分支后一直向前，离开分支后一直向后”的策略。
3. **推理时计算量指数级分离**：RLVR 建模期望时间 \(\Theta(WK)\)，SFT 需 \(\Theta(WL^K)\)，分离因子为 \(L^K\)（指数于深度 \(K\)）。
4. **蒸馏可转移回溯能力**：用 RLVR 收敛模型的轨迹进行监督微调，基础模型可恢复 \(\Theta(WK)\) 推理效率。
5. **理论动力学的可解释性**：论文给出了梯度流的闭式表达式，并分两阶段分析收敛性。

## 7. 优点

- **理论严谨性**：提供了完整的数学证明，包括梯度流动力学、符号梯度更新的收敛性、推理时间分离的严格推导。
- **清晰的机制解释**：直观对比了 SFT（无负例）和 RLVR（探索产生负例）的关键差异，能解释实际观察中 RL 泛化更好的现象。
- **创新性**：首次将回溯能力视为区分后训练方法的核心因素，并建模为图路径搜索的符号梯度流分析。
- **扩展性**：蒸馏实验表明 RLVR 轨迹可用于知识迁移，具有实用价值。
- **实验与理论结合**：合成实验验证了理论预测，且额外实验（非对称图、Transformer、PPO）增加了结论的鲁棒性。

## 8. 不足与局限

- **假设过于简化**：使用 bigram 策略和图世界模型，与实际 Transformer 架构和自然语言推理有差距。虽然论文尝试了单层 Transformer 实验，但复杂性仍然有限。
- **缺乏真实数据集验证**：只在合成图上实验，未在数学推理、代码生成等标准基准（如 GSM8K、MATH）上测试，外部有效性存疑。
- **RLVR 训练假设**：使用符号梯度流以避免非收敛问题，但真实 RL 训练（如 PPO）中梯度符号可能不稳定，且文中未详细分析长度惩罚 \(\beta\) 的选择对收敛的影响。
- **未考虑过程奖励**：仅使用结果奖励，实际中过程奖励可提供更细粒度信号；论文未比较 RLVR 与更复杂的搜索算法（如 Tree-of-Thought）。
- **实验数量有限**：未进行大规模超参数搜索或重复实验的统计报告，且蒸馏实验只给出理论保证，未提供相应实验图表。
- **未讨论真实场景中回溯的复杂性**：如回溯需要决定回溯到哪个决策点、如何利用历史信息等，文中策略只是简单的“一直向后”，缺乏智能性。

（完）
