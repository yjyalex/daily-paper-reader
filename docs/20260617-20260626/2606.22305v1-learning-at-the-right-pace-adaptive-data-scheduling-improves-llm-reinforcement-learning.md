---
title: "Learning at the Right Pace: Adaptive Data Scheduling Improves LLM Reinforcement Learning"
title_zh: 以合适的速度学习：自适应数据调度提升大语言模型强化学习
authors: "Zicheng Xu, Ruixuan Zhang, Yu-Neng Chuang, Xiuyi Lou, Hoang Anh Duy Le, Oren Gal, Alexander S. Szalay, Zhaozhuo Xu, Guanchu Wang, Vladimir Braverman"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22305v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对LLM强化学习后训练的自适应数据调度框架
tldr: "现有LLM强化学习后训练采用均匀数据采样，忽略了语义结构和策略能力变化。为此提出自适应数据调度框架ADS，包含聚类层自适应分布和样本层边界样本选择。在三个LLM和七个推理基准上，ADS将GRPO平均准确率提升5.2%，并一致改善不同目标设计的RL方法，可作为通用数据调度策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22305v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 637, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22305v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1444, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22305v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22305v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 1162, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22305v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 1120, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22305v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 215, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22305v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1439, \"height\": 503, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22305v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1437, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22305v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1147, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22305v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 743, \"height\": 415, \"label\": \"Table\"}]"
motivation: 均匀采样忽视训练数据的语义结构及策略能力变化，导致RL后训练效率低。
method: 双层数据调度：聚类层维护自适应分布以巩固进展，样本层持续选取策略边界样本提供相对优势。
result: "在三个LLM和七个推理基准上，ADS平均准确率比GRPO提升5.2%。"
conclusion: ADS作为通用数据调度策略，能一致提升不同目标设计的RL方法后训练效果。
---

## 摘要
大型语言模型（LLMs）通过强化学习（RL）后训练获得了卓越的推理能力。然而，现有的RL后训练通常依赖于均匀数据采样，忽略了训练数据的语义结构和训练策略不断变化的能力。为解决这些局限性，我们提出自适应数据调度（ADS），这是一个用于调节RL后训练节奏的双层数据调度框架，用基于语义聚类的自适应分布和策略边界样本选择替代均匀采样。在聚类层面，ADS根据语义模式组织样本，并维持自适应的聚类间分布以巩固当前训练进展。在样本层面，ADS进行聚类内调度以持续采样策略边界样本，从而提供信息丰富的相对优势。在三个LLM和七个推理基准上的实验结果表明，与组相对策略优化（GRPO）相比，ADS将平均准确率提高了5.2%。值得注意的是，ADS持续改进了具有不同目标设计的RL方法，突显了其作为LLM RL后训练的通用数据调度策略的潜力。源代码可在 https://github.com/Richard-zrx/ADS 获取。

## Abstract
Large Language Models (LLMs) achieve remarkable reasoning capabilities through reinforcement learning (RL) post-training. However, existing RL post-training commonly relies on uniform data sampling, which ignores the semantic structure of the training data and the changing capability of the training policy. To address these limitations, we propose Adaptive Data Scheduling (ADS), a dual-level data scheduling framework for pacing RL post-training that replaces uniform sampling with an adaptive distribution over semantic clusters and policy-boundary sample selection. At the cluster level, ADS organizes samples according to semantic patterns and maintains an adaptive inter-cluster distribution to solidify current training progress. At the sample level, ADS performs intra-cluster scheduling to continuously sample policy-boundary samples, which provides informative relative advantages. Experimental results across three LLMs and seven reasoning benchmarks demonstrate that ADS improves average accuracy by 5.2% over Group Relative Policy Optimization (GRPO). Notably, ADS consistently improves RL methods with different objective designs, highlighting its potential as a general data scheduling strategy for LLM RL post-training. The source code is available at: https://github.com/Richard-zrx/ADS.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：当前LLM的强化学习（RL）后训练（如GRPO）普遍采用**均匀数据采样**，将训练集视为无差别的样本池。这种做法存在两大缺陷：
  1. **忽略语义结构**：不同语义的样本混合在一起，策略在不同模式间反复切换，难以巩固某一模式的训练进展。
  2. **忽略策略能力边界**：均匀采样未能区分“样本与当前策略能力”的相对位置。太简单的样本产生全正确rollouts，太难的全错，均导致优势信号趋零，无法提供有效梯度。只有处于策略边界附近（成功≈50%）的样本才提供信息丰富的相对优势。
- **整体意义**：需要一种**动态数据调度**策略，根据训练数据的语义结构以及策略的实时能力来调整采样分布，从而提升RL后训练的效率和效果。

## 2. 方法论
- **核心思想**：双层次自适应数据调度框架 **ADS**，同时考虑语义聚类和样本级策略边界追踪。
- **关键技术细节**：
  - **语义聚类**：利用基础策略的最后一层隐藏状态对每个训练样本（prompt+参考解）进行平均池化得到嵌入，使用K-Means将全数据集划分为K个语义簇。
  - **聚类间分布**：为每个簇维护一个成功率（该簇样本近期rollouts的正确比例）。根据成功率更新采样概率：成功率高的簇优先采样以巩固进展，成功率低的暂时调低权重。使用指数平滑避免突变。
  - **聚类内调度**：每个簇内维护一个**边界迷你簇**（大小B）。离线计算每个样本的困难度（基于参考解的负对数似然）；在线利用rollout反馈，仅保留成功率为0.5±ϵ的“边界样本”，否则按困难度排序替换为相邻样本。这样持续选择策略能力边界附近的样本。
  - **算法流程**（Algorithm 1）：
    1. 离线计算嵌入、聚类、困难度排序。
    2. 初始化：每个簇的迷你簇取前B个最易样本，簇采样均匀。
    3. 训练循环：按当前簇分布采样簇 → 用其迷你簇中的样本做GRPO优化 → 收集rollout结果 → 更新迷你簇（根据样本成功率替换）和簇成功率 → 更新簇分布。
  - **优点**：不修改底层RL目标，可即插即用。

## 3. 实验设计
- **训练数据**：来自OpenR1-Math-220k，过滤后得46k高质量prompt，随机取11.5k用于统一训练。
- **模型**：三个不同规模：Qwen2.5-Math-1.5B、Qwen3-4B-Base、Olmo3-7B-SFT。
- **评估基准**（共7个）：
  - **数学推理**：AIME24、AIME25、AMC23、MATH-500、Minerva
  - **OOD科学推理**：GPQA-Diamond、MMLU-Pro
- **对比方法**：
  - Base（无后训练）、GRPO（均匀采样）、DOTS+RR（基于难度和重放的在线数据选择）
  - 额外对比：OPD、DAPO、GSPO三种不同RL目标（验证目标无关性）
- **指标**：Mean@16（16次采样平均准确率，主指标）和Pass@16（16次中任意正确即可），报告三独立运行的平均值和标准差。

## 4. 资源与算力
- **硬件配置**（附录F）：4× NVIDIA H200 GPU（141GB显存），CPU 512GB内存，CUDA 12.9。
- **训练超参**：rollout batch size=128，group size=8，温度=1.0，学习率=1e-6，最大生成长度=8192。
- **离线预处理成本**（附录B表5）：
  - 1.5B模型：15.5分钟（约6个训练步）
  - 4B模型：21.5分钟（约9步）
  - 7B模型：26.6分钟（约3步）
- **整体训练时长**：论文未明确给出总训练时长（仅提“训练至收敛”），但离线成本相对很小。

## 5. 实验数量与充分性
- **主实验**（表1）：3模型 × 7数据集 × 2指标，重复3次，共3×7×3=63组训练结果。覆盖不同规模、不同难度、OOD泛化。
- **目标无关性实验**（表2）：在Qwen2.5-Math-1.5B上对比OPD、DAPO、GSPO三种RL目标，每种均加上ADS，共6个设置×7数据集。
- **跨策略聚类鲁棒性**（表3）：在两个模型间交换聚类策略，共2×7数据集。
- **消融实验**（表4）：移除聚类间分布、移除聚类内调度，对比4种变体×7数据集。
- **训练动态分析**（图3）：两个模型下ADS vs GRPO的奖励/熵曲线。
- **充分性与公平性**：均采用相同训练集、超参数、验证集，报告标准差；对比方法为当前代表性工作；消融完整验证双层次设计必要性。

## 6. 主要结论与发现
- **准确率提升**：ADS在三个模型上平均比GRPO提升5.2%（Mean@16）。具体数值见表1，例如Qwen2.5-1.5B上从21.57%提升至28.05%。
- **OOD泛化**：在GPQA和MMLU-Pro上同样显著提升（如1.5B：GPQA从20.14→27.28，MMLU-Pro从17.37→25.96）。
- **模型尺度一致性**：1.5B、4B、7B均受益。
- **目标无关性**：ADS一致改善OPD、DAPO、GSPO（表2），平均提升1.5~2.5个百分点。
- **鲁棒性**：跨策略聚类（用另一模型聚类）仍取得可比性能（表3）。
- **训练动态**：ADS呈现“螺旋式”奖励上升，熵更平滑稳定。

## 7. 优点
- **创新性**：首次在LLM RL后训练中引入双层次自适应调度，兼顾语义结构和策略边界。
- **即插即用**：不修改RL目标，可轻松集成到现有GRPO、DAPO等框架中。
- **计算友好**：离线预处理仅数十分钟，在线开销忽略（仅增加成功率统计和采样逻辑）。
- **鲁棒性强**：跨策略、跨模型均可使用相同聚类。
- **实验全面**：多模型、多基准、多目标、消融、动态分析等都覆盖，结果一致且可信。

## 8. 不足与局限
- **应用场景受限**：目前仅适用于**可验证结果奖励**的推理任务（如数学、科学）。对于开放指令跟随、偏好优化、工具使用、多轮对话等场景，需要更丰富的“样本信息”度量，扩展性有待探索。
- **实验偏斜**：所有训练数据来自数学领域，虽然测试包含OOD科学推理，但未验证通用NLP任务（如QA、摘要）。可能存在领域偏倚。
- **聚类数量K和迷你簇大小B为预设超参**：论文中设为64和32，未进行超参敏感性分析。
- **未与更多先进数据调度方法对比**：如课程学习、主动学习等（已有DOTS+RR，但可进一步对比其他）。
- **论文指出**：未来工作需将ADS扩展到更广泛的RL后训练设置（见附录E）。

（完）
