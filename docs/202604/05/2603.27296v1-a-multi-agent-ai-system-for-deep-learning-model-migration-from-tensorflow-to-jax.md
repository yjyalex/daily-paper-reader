---
title: A Multi-agent AI System for Deep Learning Model Migration from TensorFlow to JAX
title_zh: 一种用于将深度学习模型从 TensorFlow 迁移到 JAX 的多智能体 AI 系统
authors: "Stoyan Nikolov, Bernhard Konrad, Moritz Gronbach, Niket Kumar, Ann Yan, Varun Singh, Yaning Liang, Parthasarathy Ranganathan"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27296v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于自动代码迁移的多智能体系统
tldr: 针对深度学习框架迁移（从TensorFlow到JAX）的高成本和复杂性，本文提出了一种基于多智能体的AI系统。该系统结合静态分析与AI指令生成迁移计划，利用编排器和编码器执行任务，并引入AI评判机制解决无测试代码的质量评估问题。在真实商业场景中，该系统将迁移效率提升了6.4至8倍，为自动化代码转换提供了高效范式。
selection_source: fresh_fetch
motivation: 随着深度学习框架的快速迭代，手动将大量存量TensorFlow模型迁移至JAX面临巨大的维护挑战和人力成本。
method: 构建了一个由AI规划器、编排器、编码器和AI评判员组成的多智能体系统，通过静态分析与示例驱动的剧本实现复杂代码的自动转换。
result: 在大型超大规模计算环境的真实案例中，该系统实现了6.4倍到8倍的迁移速度提升，并能有效处理缺乏测试用例的代码。
conclusion: 该多智能体系统证明了AI在支持自身开发工作流方面的巨大潜力，其技术框架可推广至其他框架迁移和通用代码转换任务。
---

## 摘要
基于 AI 的产品及其底层模型的快速发展推动了深度学习框架的不断创新。Google 一直在数十种产品中率先应用机器学习。维护不同机器学习框架和版本中的大量模型源代码是一项重大挑战。到目前为止，维护和迁移工作主要由人类专家手动完成。我们描述了一个构建的基于 AI 的多智能体系统，用于支持将基于 TensorFlow 的深度学习模型自动迁移为基于 JAX 的模型。我们做出了三个主要贡献：首先，我们展示了结合静态分析与 AI 指令的 AI 规划器如何为极其复杂的代码组件创建迁移计划，并由编排器和编码器组合，利用 AI 生成的基于示例的剧本可靠地执行。其次，我们定义了质量指标和基于 AI 的评判器，在待评估代码缺乏测试且必须遵守严格风格和依赖要求的情况下加速开发。第三，我们证明了该系统如何在大型超大规模计算环境下的商业真实用例中加速代码迁移。我们的方法显著缩短了深度学习模型的迁移时间（加速 6.4 到 8 倍），并创造了一个 AI 有效支持其自身开发工作流的良性循环。我们预期本文描述的技术和方法可以推广到其他框架迁移和通用的代码转换任务中。

## Abstract
The rapid development of AI-based products and their underlying models has led to constant innovation in deep learning frameworks. Google has been pioneering machine learning usage across dozens of products. Maintaining the multitude of model source codes in different ML frameworks and versions is a significant challenge. So far the maintenance and migration work was done largely manually by human experts. We describe an AI-based multi-agent system that we built to support automatic migration of TensorFlow-based deep learning models into JAX-based ones. We make three main contributions: First, we show how an AI planner that uses a mix of static analysis with AI instructions can create migration plans for very complex code components that are reliably followed by the combination of an orchestrator and coders, using AI-generated example-based playbooks. Second, we define quality metrics and AI-based judges that accelerate development when the code to evaluate has no tests and has to adhere to strict style and dependency requirements. Third, we demonstrate how the system accelerates code migrations in a large hyperscaler environment on commercial real-world use-cases. Our approach dramatically reduces the time (6.4x-8x speedup) for deep learning model migrations and creates a virtuous circle where effectively AI supports its own development workflow. We expect that the techniques and approaches described here can be generalized for other framework migrations and general code transformation tasks.

---

## 论文详细总结（自动生成）

这篇论文由 Google 团队撰写，介绍了一个专门用于将深度学习模型从 TensorFlow (TF) 迁移到 JAX/Flax 的多智能体 AI 系统。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 AI 框架的演进，Google 内部正从传统的 TensorFlow（有状态、基于图、面向对象）转向 JAX（无状态、函数式、针对 TPU 优化）。
*   **研究动机**：
    *   **规模巨大**：Google 拥有数千个存量 TF 模型，手动迁移需要耗费数百人/年的专家工作量。
    *   **范式差异**：TF 和 JAX 在架构上有本质区别（如状态管理、正则化处理、自定义算子等），简单的代码转换工具难以处理。
    *   **质量要求**：生产环境代码必须符合严格的内部编码规范、依赖管理和性能要求。

### 2. 方法论：多智能体系统设计
该系统采用多智能体协作架构，通过任务分解和领域知识注入来解决长程、复杂的迁移任务。

*   **核心组件**：
    *   **规划器 (Planner)**：结合 **Kythe 静态分析工具**（确定依赖关系）与 LLM，递归地发现模型依赖，生成包含多个独立步骤的 JSON 迁移计划。
    *   **编排器 (Orchestrator)**：作为“大脑”，将大计划拆分为可管理的子任务块，分发给编码器，并处理失败重试和状态持久化。
    *   **编码器 (Coder)**：基于 **ReAct 模式**的智能体，拥有文件读写、代码搜索、运行构建和单元测试的工具权限。它负责生成代码并进行自我修复。
*   **关键技术细节**：
    *   **剧本系统 (Playbooks)**：分层级的 Markdown 指南（通用、风格、任务特定、客户特定）。其中“客户特定剧本”是通过 LLM 总结少量“黄金示例”自动生成的。
    *   **记忆库 (Memory Bank)**：基于文件的共享存储，记录迁移状态、调试日志和代码摘要，防止智能体在长流程中丢失上下文。
    *   **AI 评判员 (LLM Judge)**：由于迁移后的代码往往缺乏现成的测试用例，系统使用基于 **Gemini 3 Pro** 的评判员，根据“框架无关的检查清单”（Checklist）对代码的功能完整性进行盲审评分。

### 3. 实验设计
*   **数据集/场景**：
    *   **开源模型集**：32 个开源模型（用于早期迭代和回归测试）。
    *   **Google 内部模型集**：6 个真实的 YouTube 生产级模型，复杂度从小型（约 500 行）到大型（约 3000 行主代码 + 1.3 万行依赖，包含数百个指标和层）。
*   **对比方法 (Baselines)**：
    1.  `single_agent_baseline`：无特定适配的单智能体。
    2.  `single_agent_yt_specific`：带剧本但无规划/编排的单智能体。
    3.  `multi_agent`：带规划/编排，但无客户特定剧本。
    4.  `multi_agent_yt_specific`：全功能多智能体系统（本文方法）。
*   **评估指标**：基于检查清单的完成率得分（Checklist Score）以及人工复核后的迁移耗时对比。

### 4. 资源与算力
*   **模型使用**：系统核心和评判员均使用 **Gemini 3 Pro**。
*   **算力细节**：文中**未明确说明**具体的 GPU/TPU 数量或训练时长。由于该系统主要基于现成大模型的推理和工具调用（Agentic Workflow），其成本主要体现在 API 调用次数和推理 Token 上。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 个核心测试模型，每种配置运行了 **N > 10 次**，以诱导方差并获得平均得分。
*   **充分性**：
    *   包含了**消融实验**（对比了单智能体 vs 多智能体，有无剧本的影响）。
    *   实验覆盖了从简单到极高复杂度的模型，证明了复杂度越高，多智能体系统的优势越明显。
    *   **客观性**：采用了“盲审”机制，即 AI 评判员在不知道源代码的情况下仅根据功能清单检查目标代码。

### 6. 主要结论与发现
*   **效率提升**：相比人类专家手动迁移，该系统实现了 **6.4 倍至 8 倍** 的速度提升。
*   **质量优势**：全功能多智能体配置在所有模型上的得分显著高于单智能体（p < 0.02）。单智能体在处理复杂模型时经常出现“幻觉”或遗漏关键逻辑（如损失函数、指标）。
*   **知识泛化**：仅需 2 个手动迁移的“黄金示例”生成的剧本，就能显著提升同类其他模型的迁移质量。
*   **复杂度阈值**：对于简单模型，单智能体表现尚可；但对于中大型模型，多智能体架构是保证代码可构建和功能完备的必要条件。

### 7. 优点
*   **混合架构**：巧妙结合了确定性的静态分析（Kythe）和概率性的 AI 生成，保证了依赖发现的准确性。
*   **闭环验证**：编码器具备运行构建和测试的能力，能够实现“生成-报错-修复”的闭环。
*   **可扩展性**：通过自动生成剧本的方法，系统可以快速适应不同的团队风格或新的框架迁移任务。

### 8. 不足与局限
*   **样本量限制**：核心测试集仅包含 6 个 YouTube 模型，虽然具有代表性，但样本绝对数量较小。
*   **调试成本**：虽然代码迁移快，但由于 JAX 的无状态特性，迁移后可能存在细微的训练不稳定或推理偏差，仍需人工进行深度调试（文中称为“调试税”）。
*   **计算开销**：多智能体配置的计算成本（Token 消耗）比单智能体高出 3-5 倍。
*   **部署验证**：目前评估侧重于代码完备性，模型迁移后的长期生产环境表现（如收敛性、延迟回归）仍需进一步验证。

（完）
