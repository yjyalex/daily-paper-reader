---
title: Multi-modal Reasoning with LLMs for Visual Semantic Arithmetic
title_zh: 基于大语言模型的多模态推理在视觉语义算术中的应用
authors: "Chuou Xu, Liya Ji, Qifeng Chen"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19567v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习增强LLM推理能力
tldr: 本研究探讨了大型视觉语言模型（LVLM）在视觉语义算术（如“国王”图-“男人”图+“女人”图=“女王”）方面的推理能力。针对现有模型难以从视觉细节中提取抽象概念的问题，作者提出了两项新任务及IRPD数据集，并开发了基于可验证函数和GRPO算法的SAri-RFT强化微调方法。实验证明该方法显著提升了模型在跨模态关系推理上的表现，对提升家政机器人的感知与决策能力具有重要意义。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理视觉语义算术时，难以从复杂的图像细节中提取简洁的语义概念并进行逻辑推理。
method: 提出了两项新任务及IRPD数据集，并采用基于可验证函数和GRPO算法的语义算术强化微调（SAri-RFT）方法。
result: 该方法在IRPD和Visual7W-Telling数据集上均达到了SOTA水平，显著增强了模型的跨模态关系推理能力。
conclusion: 通过强化微调提升LVLM的视觉语义算术能力，能有效帮助机器人将符号推理植根于感知中，从而改善其在复杂环境下的决策和交互。
---

## 摘要
强化学习（RL）作为后训练阶段对于增强大语言模型（LLMs）在编程和数学方面的推理能力至关重要。然而，它们在视觉语义算术（即从图像中推断关系）方面的能力仍有待探索。经典的文本类比“国王”-“男人”+“女人”=“女王”展示了关系推理，但将文本替换为“国王”和“男人”的图像会显著降低性能，因为这需要常识知识并从无关的视觉细节中提取简洁的概念。这种能力对于非结构化环境中的服务和家用机器人非常重要，机器人必须推断物体、智能体和动作之间的语义关系。例如在厨房中，从图像中识别出“粉末”和“蛋糕”通过“由……制成”这一关系相关联，能够将符号关系锚定在感知中，从而实现工具替换、任务泛化和改进的语义推理。先前的工作通过在向量算术后解码图像特征来处理语义算术，但存在模态鸿沟且缺乏系统性评估。在本文中，我们制定了两个新任务：二项减法和三项运算，并构建了图像-关系对数据集（IRPD）用于基准测试。我们进一步提出了语义算术强化微调（SAri-RFT），该方法使用可验证函数和群体相对策略优化（GRPO）对大视觉语言模型（LVLMs）进行后训练。我们的方法在 IRPD 和真实世界的 Visual7W-Telling 数据集上达到了最先进的结果。通过赋予 LVLMs 鲁棒的跨模态关系推理能力，本研究提升了家用机器人将符号推理锚定在感知中的能力，增强了复杂环境下的决策制定、工具适应性和人机交互。数据集和源代码在补充材料中提供。

## Abstract
Reinforcement learning (RL) as post-training is crucial for enhancing the reasoning ability of large language models (LLMs) in coding and math. However, their capacity for visual semantic arithmetic, inferring relationships from images, remains underexplored. The classic text analogy "king"-"man"+"woman" = "queen" illustrates relational reasoning, yet replacing text with images of "king" and "man" significantly reduces performance because it requires commonsense knowledge and the extraction of concise concepts from irrelevant visual details. This capability is important for service and domestic robotics in unstructured environments, where robots must infer semantic relationships among objects, agents, and actions. In a kitchen, recognizing from images that "powder" and "cake" are related by "is made of" grounds symbolic relations in perception, enabling tool substitution, task generalization, and improved semantic reasoning. Prior work approaches semantic arithmetic by decoding image features after vector arithmetic, but suffers from modality gaps and lacks systematic evaluation. In this paper, we formulate two novel tasks, two-term subtraction and three-term operations, and construct the Image-Relation-Pair Dataset (IRPD) for benchmarking. We further propose Semantic Arithmetic Reinforcement Fine-Tuning (SAri-RFT), which post-trains large vision-language models (LVLMs) using a verifiable function and Group Relative Policy Optimization (GRPO). Our method achieves state-of-the-art results on IRPD and the real-world Visual7W-Telling dataset. By equipping LVLMs with robust cross-modal relational reasoning, this work advances domestic robots' ability to ground symbolic reasoning in perception, enhancing decision-making, tool adaptability, and human-robot interaction in complex environments. Datasets and source code are provided in the supplementary material.

---

## 论文详细总结（自动生成）

这篇论文由香港科技大学的研究团队提出，旨在解决大型视觉语言模型（LVLM）在处理视觉语义算术（Visual Semantic Arithmetic）任务时的不足。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然 LLM 在文本类比（如“国王”-“男人”+“女人”=“女王”）方面表现出色，但在处理以图像为载体的语义算术时能力大幅下降。
*   **研究动机**：视觉语义算术要求模型具备常识，并能从复杂的视觉细节中提取抽象概念。这种能力对家用机器人至关重要（例如，通过图像理解“粉末”是“蛋糕”的原料，从而在缺少某种工具时进行语义推理和替代）。
*   **背景**：先前的方法（如 ZeroCap）主要在嵌入空间（Embedding Space）进行向量运算，但受限于模态鸿沟（Modality Gap）且缺乏系统性的评估基准。

### 2. 方法论：核心思想与关键技术
*   **任务定义**：
    1.  **二项减法（Two-term Subtraction）**：给定对象 A 和 B，推断其关系 $R$（即 $B - A = R$），形式为多选题。
    2.  **三项运算（Three-term Operations）**：类比推理，给定 $A, B, C$，求 $D$（即 $B - A + C = D$），要求模型生成文本响应。
*   **数据集构建（IRPD）**：从 ConceptNet 提取 18 种关系和 1500 多个词对，利用 **FLUX.1-dev** 扩散模型生成高质量图像，并通过 CLIP 分数和人工审核确保质量。
*   **核心算法（SAri-RFT）**：
    *   **强化学习框架**：采用 **GRPO（群体相对策略优化）** 算法进行后训练。
    *   **可验证奖励函数（Verifiable Reward Function）**：
        *   对于二项任务：使用硬性的准确率奖励（正确为 1，错误为 0）。
        *   对于三项任务：设计了**软性可验证奖励**。利用 Word2Vec 计算模型输出与真值之间的余弦相似度，并通过 Sigmoid 函数缩放，以解决开放式类比问题的评估难题。
    *   **格式奖励**：强制模型输出特定的 HTML 标签（如 `<think>` 和 `<answer>`），以诱导思维链（CoT）推理。

### 3. 实验设计
*   **数据集**：自建的 **IRPD**（包含文本和图像两种模态）以及真实世界数据集 **Visual7w-telling**。
*   **Benchmark 与对比方法**：
    *   **嵌入算术类**：ZeroCap、ImageBind、LanguageBind。
    *   **LVLM 推理类**：InternVL2.5-8B、Video-LLaVa、Qwen2-VL-7B（基座模型及 SFT 版本）。
*   **评估指标**：准确率（二项任务）、Recall@5 和 Word2Vec 相似度（三项任务）。

### 4. 资源与算力
*   **算力投入**：使用了 **8 台 H800 GPU**。
*   **训练时长**：训练过程大约持续 **2 小时**（共 150 个 step）。
*   **模型规模**：以 Qwen2-VL-7B 为基础模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 18 种关系、1500+ 测试对。
*   **消融实验**：非常充分。研究了群体采样数量 $n$ 的影响、思维链（CoT）的必要性、不同奖励组件（准确率、相似度、格式奖励）的贡献，以及 Sigmoid 缩放的作用。
*   **客观性**：通过人工评测（User Study）验证了 Word2Vec 奖励函数与人类判断的一致性（相关度达 4.2/5），并对所有对比模型使用了统一的 Prompt，确保了公平性。

### 6. 主要结论与发现
*   **性能飞跃**：SAri-RFT 在二项减法任务上比 SFT 方法提升了超过 **50%**，在三项运算任务上提升了超过 **75%**。
*   **RL 优于 SFT**：实验证明，通过 GRPO 进行强化学习比单纯的监督微调（SFT）更能激发模型的跨模态推理能力。
*   **思维链的作用**：强制模型进行“思考”显著提升了复杂类比问题的得分。
*   **下游任务泛化**：在 Visual7w 真实场景数据集上，该方法同样优于其他基准模型，证明了其通用性。

### 7. 优点与亮点
*   **创新性任务**：首次正式定义了视觉语义算术的两种任务形式。
*   **奖励函数设计**：巧妙地将 Word2Vec 相似度引入 GRPO 的可验证奖励中，解决了非结构化文本答案难以自动评分的痛点。
*   **高质量数据**：利用先进的 FLUX 模型构建了目前该领域最高质量的图像关系数据集。

### 8. 不足与局限
*   **抽象概念挑战**：对于极其抽象的关系（如“战争”导致“暴力”），模型仍存在理解偏差。
*   **奖励作弊（Reward Hacking）**：在强化学习过程中，模型有时会倾向于生成模板化的响应以获取格式奖励，而忽略了内容的深度。
*   **运算类型有限**：目前仅探索了加减法，尚未涉及更复杂的语义逻辑运算。
*   **应用限制**：虽然对机器人领域有启发，但尚未在真实的物理机器人硬件上进行端到端验证。

（完）
