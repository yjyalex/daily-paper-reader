---
title: "SSL-R1: Self-Supervised Visual Reinforcement Post-Training for Multimodal Large Language Models"
title_zh: SSL-R1：多模态大语言模型的自监督视觉强化后训练
authors: "Jiahao Xie, Alessio Tonioni, Nathalie Rauschmayr, Federico Tombari, Bernt Schiele"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20705v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 具有可验证奖励的强化学习用于推理
tldr: SSL-R1 针对多模态大模型在强化学习中过度依赖语言先验和人工标注的问题，提出了一种通用的自监督强化学习框架。该框架通过将经典的视觉自监督学习任务重新表述为可验证的视觉谜题，直接从图像中提取奖励信号，无需人工或外部模型干预。实验证明，该方法显著提升了模型在多模态理解和推理基准测试中的表现，为大规模自监督强化学习提供了新路径。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习高度依赖人工标注和语言先验，限制了模型内在视觉理解能力的提升和奖励设计的可扩展性。
method: 将视觉自监督学习任务转化为一系列无需人工标注、可自动验证的视觉谜题，作为强化学习的奖励来源进行后训练。
result: 在多个多模态理解和推理基准测试中，SSL-R1 显著增强了模型性能，证明了视觉中心自监督任务的有效性。
conclusion: SSL-R1 展示了利用自监督视觉任务构建可验证奖励的巨大潜力，为实现大规模多模态强化学习提供了重要经验。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已展现出增强多模态大语言模型（MLLMs）推理能力的巨大潜力。然而，对以语言为中心的先验知识和昂贵人工标注的依赖，阻碍了 MLLMs 的内在视觉理解和可扩展的奖励设计。在这项工作中，我们介绍了 SSL-R1，这是一个通用的自监督强化学习框架，它直接从图像中获取可验证的奖励。为此，我们重新审视了视觉领域的自监督学习（SSL），并将广泛使用的 SSL 任务重新表述为一组用于强化学习后训练的可验证视觉谜题，既不需要人工监督，也不需要外部模型监督。在这些任务上训练 MLLMs 显著提高了它们在多模态理解和推理基准测试中的性能，凸显了利用以视觉为中心的自监督任务进行 MLLM 后训练的潜力。我们认为这项工作将为设计有效的自监督可验证奖励以实现大规模强化学习提供有用的经验。项目主页：https://github.com/Jiahao000/SSL-R1。

## Abstract
Reinforcement learning (RL) with verifiable rewards (RLVR) has demonstrated the great potential of enhancing the reasoning abilities in multimodal large language models (MLLMs). However, the reliance on language-centric priors and expensive manual annotations prevents MLLMs' intrinsic visual understanding and scalable reward designs. In this work, we introduce SSL-R1, a generic self-supervised RL framework that derives verifiable rewards directly from images. To this end, we revisit self-supervised learning (SSL) in visual domains and reformulate widely-used SSL tasks into a set of verifiable visual puzzles for RL post-training, requiring neither human nor external model supervision. Training MLLMs on these tasks substantially improves their performance on multimodal understanding and reasoning benchmarks, highlighting the potential of leveraging vision-centric self-supervised tasks for MLLM post-training. We think this work will provide useful experience in devising effective self-supervised verifiable rewards to enable RL at scale. Project page: https://github.com/Jiahao000/SSL-R1.

---

## 论文详细总结（自动生成）

这是一份关于论文《SSL-R1: Self-Supervised Visual Reinforcement Post-Training for Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然具有可验证奖励的强化学习（RLVR）在提升大语言模型（LLM）推理能力上取得了巨大成功（如 DeepSeek-R1），但在多模态大模型（MLLM）领域，现有的 RLVR 方法面临两大挑战：
    1.  **语言中心偏向**：奖励设计多依赖文本先验，忽略了密集的视觉信息，导致模型在细粒度视觉理解上表现薄弱。
    2.  **标注成本高昂**：依赖人工标注或外部强模型（如 GPT-4o）提供监督信号，难以大规模扩展。
*   **核心目标**：提出 **SSL-R1** 框架，通过将经典的视觉自监督学习（SSL）任务转化为可自动验证的“视觉谜题”，直接从图像中提取内在奖励，实现无需人工干预的大规模强化学习后训练。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将视觉自监督学习中的预训练任务（Pretext Tasks）重新表述为 RLVR 的奖励函数。由于这些任务的标签是通过对原始图像进行已知变换（如旋转、裁剪、遮盖）生成的，因此其结果是客观可验证的。
*   **关键技术细节**：
    *   **五大视觉谜题任务**：
        1.  **旋转预测 (Rotation Prediction)**：预测图像被旋转的角度（0°/90°/180°/270°）。
        2.  **视觉相似性 (Visual Similarity)**：从多个候选图中选出与参考图最相似的增强视图。
        3.  **区域补全 (Region Inpainting)**：从选项中选出最适合填充图像遮盖部分的切片。
        4.  **切片排序 (Patch Ordering)**：将打乱的 3x3 图像切片恢复到正确的栅格顺序。
        5.  **几何对应 (Geometric Correspondence)**：在几何变换后的图像中找到与原图对应的特征点序列。
    *   **强化学习算法**：采用 **GRPO (Group Relative Policy Optimization)** 算法，通过比较一组响应的相对质量来优化策略，无需额外的 Critic 模型。
    *   **奖励设计**：
        *   **准确性奖励 (Accuracy Reward)**：单值任务采用二值奖励（0 或 1），序列任务（如排序）采用比例奖励。
        *   **格式奖励 (Format Reward)**：强制模型将推理过程置于 `<think>` 标签内，答案置于 `\boxed{}` 内。

### 3. 实验设计
*   **数据集**：使用 COCO 数据集的 11.8 万张原始图像，生成了 59.1 万个自监督问答对（SSL-R1-591K）。
*   **基准测试 (Benchmarks)**：在 13 个视觉中心基准上进行评估，涵盖：
    *   **细粒度感知**：MMVP, MMStar, MMBench, HR-Bench-8K, V*, MME-RealWorld, LISA, OVD-Eval。
    *   **空间理解**：VSR, OmniSpatial, DA-2K。
    *   **组合理解**：Winoground, SugarCrepe++。
*   **对比方法**：
    *   **基础模型**：Qwen2.5-VL (3B/7B)。
    *   **监督强化学习模型**：ThinkLite-VL, VL-Cogito, LLaVA-Critic-R1。
    *   **同类自监督方法**：Visual Jigsaw。

### 4. 资源与算力
*   **训练配置**：全局 Batch Size 为 256，学习率为 1e-6。单任务训练 1000 步，多任务联合训练 2000 步。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**（如 A100 或 H100 的具体枚数），但提到使用了 GRPO 算法以节省显存（无需 Critic 模型），且模型规模涵盖了 3B 和 7B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单任务与多任务联合训练的对比。
    *   针对任务难度（如切片数量、旋转角度间隔）进行了消融实验。
    *   在 3B 和 7B 两种参数规模上验证了扩展性。
    *   在不同架构（如 MiMo-VL）上验证了通用性。
*   **充分性评价**：实验设计较为全面，不仅验证了下游任务的性能提升，还深入分析了 SSL 任务之间的协同效应和负迁移现象，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：SSL-R1 在所有 13 个基准测试中均一致优于基础模型。SSL-R1-7B 的表现甚至超过了许多使用昂贵人工标注进行训练的监督式推理模型。
*   **任务特性**：
    *   **几何对应**任务对空间理解和高分辨率感知提升最明显。
    *   **旋转预测**显著增强了模型对 CLIP 盲区视觉模式的理解。
*   **协同效应**：多任务联合训练通常优于单任务，证明了不同视觉能力之间的互补性。
*   **难度影响**：通常更难的自监督任务（如更多的切片或更复杂的几何变换）能提供更强的监督信号，带来更好的性能增益。

### 7. 优点
*   **高可扩展性**：完全摆脱了对人工标注和外部强模型的依赖，理论上可以利用无限的互联网图像数据。
*   **视觉中心化**：通过像素级和几何级的任务，有效弥补了 MLLM 在细粒度视觉推理上的短板。
*   **通用性强**：框架不依赖特定模型架构，且能与现有的推理模型（如 ThinkLite-VL）叠加使用，进一步提升性能。

### 8. 不足与局限
*   **负迁移风险**：实验观察到某些任务组合（如视觉相似性与区域补全）在特定基准上可能存在冲突，导致性能微降。
*   **任务覆盖面**：虽然涵盖了五类任务，但仍主要集中在静态图像理解，尚未扩展到视频或更复杂的动态交互场景。
*   **推理成本**：虽然训练是自监督的，但 RL 过程中的采样（如 GRPO 每步采样 16 个响应）仍需要较大的计算开销。

（完）
