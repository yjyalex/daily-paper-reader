---
title: Understanding and Mitigating Spurious Signal Amplification in Test-Time Reinforcement Learning for Math Reasoning
title_zh: 理解并缓解数学推理中测试时强化学习的伪信号放大
authors: "Yongcan Yu, Lingxiao He, Jian Liang, Kuangpu Guo, Meng Wang, Qianlong Xie, Xingxing Wang, Ran He"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21327v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于数学推理的测试时强化学习
tldr: 本研究针对数学推理任务中的测试时强化学习（TTRL）易受伪标签噪声干扰的问题，揭示了中等一致性响应产生的伪信号会被组相对优势估计进一步放大的现象。为此，作者提出了DDRL框架，通过频率采样过滤歧义样本、利用固定优势估计消除偏差，并引入共识驱动的离策精炼阶段。实验证明，DDRL在多个大模型和数学基准上显著提升了推理性能，为缓解测试时优化中的信号放大提供了有效方案。
selection_source: fresh_fetch
motivation: TTRL在推理阶段易受伪标签噪声影响，且组相对优势估计会进一步放大这些伪信号，导致模型优化偏离目标。
method: 提出DDRL框架，结合频率采样排除歧义样本、固定优势估计去偏以及共识驱动的离策精炼阶段进行模型更新。
result: 在三个大语言模型和多个数学推理基准测试中，DDRL的表现一致优于现有的TTRL基准方法。
conclusion: 该研究通过识别并缓解伪信号放大问题，显著提升了测试时强化学习在数学推理任务中的稳健性和有效性。
---

## 摘要
测试时强化学习（TTRL）通常在推理阶段通过伪标签来调整模型，这使其容易受到来自标签噪声的伪优化信号的影响。通过实证研究，我们观察到具有中等一致性的回答形成了一个模糊区域，并构成了奖励噪声的主要来源。关键的是，我们发现这类伪信号甚至会通过组相对优势估计（group-relative advantage estimation）被进一步放大。受这些发现的启发，我们提出了一个统一框架——去偏去噪测试时强化学习（DDRL），旨在缓解伪信号。具体而言，DDRL 首先应用一种基于频率的采样策略来排除模糊样本，同时保持正负样本集的平衡。随后，它采用一种具有固定优势值的去偏优势估计，消除了由组相对策略优化引入的偏差。最后，DDRL 结合了一个基于共识的离策（off-policy）精炼阶段，利用拒绝采样数据集实现高效且稳定的模型更新。在三个大语言模型和多个数学推理基准测试上的实验表明，DDRL 的表现一致优于现有的 TTRL 基准方法。代码将很快在 https://github.com/yuyongcan/DDRL 发布。

## Abstract
Test-time reinforcement learning (TTRL) always adapts models at inference time via pseudo-labeling, leaving it vulnerable to spurious optimization signals from label noise. Through an empirical study, we observe that responses with medium consistency form an ambiguity region and constitute the primary source of reward noise. Crucially, we find that such spurious signals can be even amplified through group-relative advantage estimation. Motivated by these findings, we propose a unified framework, Debiased and Denoised test-time Reinforcement Learning (DDRL), to mitigate spurious signals. Concretely, DDRL first applies a frequency-based sampling strategy to exclude ambiguous samples while maintaining a balanced set of positive and negative examples. It then adopts a debiased advantage estimation with fixed advantages, removing the bias introduced by group-relative policy optimization. Finally, DDRL incorporates a consensus-based off-policy refinement stage, which leverages the rejection-sampled dataset to enable efficient and stable model updates. Experiments on three large language models across multiple mathematical reasoning benchmarks demonstrate that DDRL consistently outperforms existing TTRL baselines. The code will soon be released at https://github.com/yuyongcan/DDRL.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型在数学推理任务中的“测试时强化学习”（Test-Time Reinforcement Learning, TTRL）进行了深入研究，旨在解决模型在无监督优化过程中受伪信号干扰的问题。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：TTRL 允许模型在推理阶段通过自我生成响应并利用“多数投票”产生伪标签进行自我进化。然而，这种无监督模式存在两个致命缺陷：
    1.  **伪信号来源**：中等一致性（出现频率不高不低）的回答往往是错误的或具有误导性的，构成了奖励噪声的主要来源。
    2.  **信号放大效应**：常用的 GRPO 算法通过组内奖励归一化来计算优势（Advantage），在低共识情况下，会错误地给少数可能错误的“正样本”分配极大的正向优势，从而放大了噪声。
*   **研究背景**：随着推理时缩放（Test-time Scaling）技术的发展，如何在没有地面真值（Ground Truth）的情况下，稳定地利用推理阶段的计算资源提升模型性能成为当前的研究热点。

### 2. 方法论：DDRL 框架
作者提出了 **DDRL (Debiased and Denoised test-time Reinforcement Learning)**，包含三个核心组件：
*   **平衡置信度感知采样 (BCS)**：
    *   **核心思想**：基于答案出现的频率来过滤样本。
    *   **细节**：保留高频答案作为正样本，保留极低频答案作为负样本，直接丢弃中等频率的“模糊区域”样本。同时，强制正负样本比例平衡（正样本上限设为 50%），防止模型过度强化低共识的伪标签。
*   **去偏优势估计 (DAE)**：
    *   **核心思想**：取消 GRPO 中的组内归一化。
    *   **细节**：将优势值固定为 $+1$（正样本）和 $-1$（负样本）。这种做法解耦了优势大小与组内统计数据的关系，避免了在低共识样本上产生过大的梯度更新，使优化过程更稳健。
*   **基于共识的离策精炼 (COR)**：
    *   **核心思想**：在强化学习阶段结束后，进行一次轻量级的监督微调（SFT）。
    *   **细节**：使用 RL 后的模型生成大量样本（如 128 个），通过多数投票确定最终共识，并筛选出符合共识的样本构建数据集，对模型进行最后的蒸馏和稳定化处理。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **AIME 2024**（高难度数学竞赛）
    *   **AMC**（美国数学竞赛）
    *   **MATH-500**（标准数学推理基准）
*   **对比模型**：
    *   **Qwen2.5-Math-1.5B**（数学专有模型）
    *   **Qwen2.5-Base-3B**（通用基础模型）
    *   **Llama-3.1-8B-Instruct**（指令微调模型）
*   **对比方法**：
    *   **No Adaptation**（原始模型直接推理）
    *   **TTRL**（标准的测试时强化学习基准）
    *   **ETMR**（一种通过熵机制增加采样多样性的先进基准）

### 4. 资源与算力
*   **框架**：使用了 `verl` 强化学习框架。
*   **训练细节**：
    *   RL 阶段：学习率在 $2\times10^{-7}$ 到 $5\times10^{-7}$ 之间，采用余弦调度。
    *   精炼阶段：5 个 Epoch，学习率 $1\times10^{-5}$。
    *   **算力说明**：文中**未明确列出具体的 GPU 型号和数量**。但提到了训练时长，例如在精炼阶段，针对不同数据集的 SFT 仅需 3 到 5 分钟，表明该阶段非常高效。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构和规模的模型，以及 3 个难度跨度极大的数据集。
*   **消融实验**：详细测试了 BCS、DAE 和 COR 三个组件各自的贡献，证明了每个模块的必要性。
*   **深度分析**：提供了训练动力学分析（优势值的演变曲线）和采样频率与正确率关系的概率建模。
*   **充分性评价**：实验设计客观、公平，通过跨模型和跨任务的验证，证明了方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：DDRL 在所有基准上均优于 TTRL 和 ETMR。例如在 Qwen2.5-Math-1.5B 上，相比 ETMR 在 AIME 2024 上提升了 19.0%。
*   **去偏的重要性**：实验证明，在无监督环境下，简单的固定优势值比复杂的组归一化（GRPO）效果更好，因为它能有效抑制错误信号的放大。
*   **离策精炼的效率**：相比于延长 RL 训练时间，进行一次基于共识的 SFT 精炼能以更低的计算成本获得更大的性能增益。

### 7. 优点
*   **理论深刻**：首次系统性地揭示了 GRPO 在无监督 TTRL 场景下的“伪信号放大”缺陷。
*   **简单有效**：提出的固定优势值和频率采样方法不增加计算复杂度，但显著提升了稳定性。
*   **即插即用**：作为一个通用框架，可以轻松集成到现有的测试时优化流程中。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在数学推理等具有明确客观答案的任务上。在对话、创意写作等主观性强、难以通过多数投票衡量正确性的任务中，该方法的有效性尚未验证。
*   **超参数依赖**：虽然 DAE 减少了归一化带来的偏差，但固定优势值（+1/-1）可能过于保守，未来可能需要更精细的置信度加权策略。
*   **算力细节缺失**：未提供具体的硬件资源消耗清单，不便于其他研究者评估复现成本。

（完）
