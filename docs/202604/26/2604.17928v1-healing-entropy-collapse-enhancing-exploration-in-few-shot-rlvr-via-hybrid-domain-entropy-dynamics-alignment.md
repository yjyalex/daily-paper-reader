---
title: "HEALing Entropy Collapse: Enhancing Exploration in Few-Shot RLVR via Hybrid-Domain Entropy Dynamics Alignment"
title_zh: 治愈熵坍缩：通过混合域熵动力学对齐增强少样本 RLVR 中的探索
authors: "Zhanyu Liu, Qingguo Hu, Ante Wang, Chenqing Liu, Zhishang Xiang, Hui Li, Delai Qiu, Jinsong Su"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17928v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用可验证奖励的强化学习（RLVR）训练推理导向的大语言模型
tldr: 针对少样本强化学习可验证奖励（RLVR）中严重的熵坍缩问题，本文提出HEAL框架。该框架通过引入高质量通用领域数据并结合熵动力学对齐（EDA）机制，在轨迹层面同步目标域与通用域的熵变化，从而增强模型探索能力。实验证明，HEAL仅需32个样本即可达到甚至超过使用1000个样本的传统RLVR性能，显著提升了低资源下的推理表现。
selection_source: fresh_fetch
motivation: 在少样本场景下，RLVR极易出现熵坍缩，导致模型探索受限并严重损害推理性能。
method: 提出HEAL框架，通过引入通用域数据并利用EDA机制对齐目标域与通用域的轨迹级熵动力学特征。
result: 实验显示HEAL在仅使用32个目标样本的情况下，性能即可匹配或超越使用1000个样本的全量RLVR。
conclusion: HEAL通过跨领域熵动力学对齐有效缓解了熵坍缩，为低资源环境下的强化学习推理训练提供了高效方案。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 已被证明在训练面向推理的大语言模型方面非常有效，但现有方法在很大程度上假设了具有丰富训练数据的高资源环境。在低资源场景下，RLVR 容易发生更严重的熵坍缩，这极大地限制了探索并降低了推理性能。为了解决这一问题，我们提出了混合域熵动力学对齐 (HEAL)，这是一个专为少样本 RLVR 量身定制的框架。HEAL 首先选择性地整合高价值的通用域数据，以促进更多样化的探索。随后，我们引入了熵动力学对齐 (EDA)，这是一种奖励机制，用于对齐目标域与通用域之间的轨迹级熵动力学，同时捕捉熵的大小和细粒度变化。通过这种对齐，EDA 不仅进一步缓解了熵坍缩，还鼓励策略从通用域中获取更多样化的探索行为。在多个领域的实验表明，HEAL 持续提升了少样本 RLVR 的性能。值得注意的是，仅使用 32 个目标域样本，HEAL 就能达到甚至超过使用 1000 个目标域样本训练的全样本 RLVR 的表现。

## Abstract
Reinforcement Learning with Verifiable Reward (RLVR) has proven effective for training reasoning-oriented large language models, but existing methods largely assume high-resource settings with abundant training data. In low-resource scenarios, RLVR is prone to more severe entropy collapse, which substantially limits exploration and degrades reasoning performance. To address this issue, we propose Hybrid-domain Entropy dynamics ALignment (HEAL), a framework tailored for few-shot RLVR. HEAL first selectively incorporates high-value general-domain data to promote more diverse exploration. Then, we introduce Entropy Dynamics Alignment (EDA), a reward mechanism that aligns trajectory-level entropy dynamics between the target and general domains, capturing both entropy magnitude and fine-grained variation. Through this alignment, EDA not only further mitigates entropy collapse but also encourages the policy to acquire more diverse exploration behaviors from the general domain. Experiments across multiple domains show that HEAL consistently improves few-shot RLVR performance. Notably, using only 32 target-domain samples, HEAL matches or even surpasses full-shot RLVR trained with 1K target-domain samples.

---

## 论文详细总结（自动生成）

这篇论文由厦门大学、清华大学等机构合作完成，针对少样本场景下强化学习可验证奖励（RLVR）训练中的“熵坍缩”问题提出了创新的解决方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：RLVR（如 DeepSeek-R1 所采用的技术）通过规则定义的奖励（如答案正确性）来训练模型推理能力，通常需要大量训练数据。
*   **核心问题**：在**低资源（Few-shot）**场景下，RLVR 极易发生**熵坍缩（Entropy Collapse）**。模型会迅速过拟合到少数生成的轨迹上，导致输出分布变得极其单一，丧失探索多样性，进而限制了推理性能的提升。
*   **研究动机**：现有的熵正则化方法未考虑数据稀缺性，直接应用效果有限。本文旨在通过引入通用领域知识来引导目标领域的探索，解决少样本 RLVR 的训练瓶颈。

### 2. 方法论：HEAL 框架
HEAL（Hybrid-domain Entropy dynamics ALignment）框架包含两个核心组件：

*   **混合域数据选择（Hybrid Training）**：
    *   **核心思想**：引入易获取的通用域数据（如常识推理），利用其基础推理模式引导模型。
    *   **筛选机制**：基于**推理不确定性**（模型对答案的把握程度）和**探索多样性**（高熵 token 的占比）对通用数据进行评分，仅保留高价值样本以控制计算成本。
*   **熵动力学对齐（EDA）奖励机制**：
    *   **核心思想**：不仅关注熵的大小，更关注熵在生成过程中的**动态变化趋势**。
    *   **技术细节**：
        1.  定义“轨迹级熵动力学”为生成步骤中 token 熵的序列。
        2.  计算**域内相似度**（目标域轨迹间的相似度）和**跨域相似度**（目标域与通用域轨迹间的相似度）。
        3.  **EDA 奖励**：如果一条目标域轨迹的熵动力学特征与通用域更接近（跨域相似度 > 域内相似度），则给予额外奖励。
    *   **对齐方式**：通过插值对齐不同长度的轨迹，并使用 KL 散度衡量相似性，引导模型模仿通用域的探索行为。

### 3. 实验设计
*   **目标领域**：医学（MedBullets, MedXpertQA）、物理（WebInstruct, C-Eval）、代码（LiveCodeBench, HumanEval Plus）、数学（AMC23, Math500 等）。
*   **模型架构**：Qwen3 系列（1.7B, 4B, 8B Base）和 LLaMA-3.2-3B-Instruct。
*   **对比基准（Baselines）**：
    *   **Full-shot**：使用 1000 个目标域样本训练（性能上限）。
    *   **Few-shot**：仅使用 32 个目标域样本。
    *   **Only-General**：仅使用通用域数据。
    *   **Hybrid**：随机混合目标域与通用域数据。
    *   **熵正则化方法**：对比了 Entropy Loss、80/20 Strategy、Clip-Higher 和 KL-Cov 等 SOTA 方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA A100 (80GB) GPU**。
*   **训练细节**：基于 VERL 框架，采用 GRPO 算法优化。训练批次（Batch Size）为 128，最大轨迹长度为 3072 tokens。
*   **时长说明**：文中未明确给出具体的训练总时长，但提到通过数据筛选减少了通用域数据的引入量，从而控制了额外开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个专业领域、4 种模型规模/架构，并进行了详尽的消融实验。
*   **充分性分析**：
    *   **多维度验证**：不仅对比了最终准确率，还分析了训练过程中的熵变化曲线、数据规模敏感性（从 0 到 32 样本）以及 Pass@k 指标。
    *   **客观性**：对比了多种最新的熵正则化基准，实验设计遵循了 RLVR 训练的标准范式（如 GRPO），结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能突破**：HEAL 显著缓解了熵坍缩。**仅需 32 个目标域样本**，其性能即可匹配甚至超过使用 1000 个样本训练的 Full-shot 模型。
*   **通用域的价值**：通用域数据并不直接提供专业知识，而是通过提供多样化的“探索模式”来防止模型过早收敛。
*   **EDA 的优越性**：相比于简单的熵损失函数（容易导致熵爆炸），EDA 提供的对齐奖励更加受控且精细，能有效提升推理路径的发现能力。

### 7. 优点
*   **极高的数据效率**：为医疗、法律等标注成本极高的专业领域提供了可行的 RLVR 训练方案。
*   **创新的对齐视角**：将“熵”视为一种动态序列（动力学）进行跨域对齐，比传统的标量正则化更具表现力。
*   **即插即用**：该框架不依赖特定模型架构，具有良好的通用性。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要在 10B 以下的模型上完成，尚未在超大规模模型（如 70B 或 400B+）上验证其扩展性。
*   **计算开销**：虽然样本量小，但 EDA 机制需要计算轨迹间的两两相似度，在超大规模 Batch 训练时可能会带来额外的计算负担。
*   **相似度函数依赖**：EDA 的效果受限于相似度衡量方式（如 KL 散度），对于某些极端长文本生成的适用性有待进一步探索。

（完）
