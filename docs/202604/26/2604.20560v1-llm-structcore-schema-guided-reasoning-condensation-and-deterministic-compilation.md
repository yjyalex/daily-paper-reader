---
title: "LLM StructCore: Schema-Guided Reasoning Condensation and Deterministic Compilation"
title_zh: LLM StructCore：模式引导的推理压缩与确定性编译
authors: Serhii Zabolotnii
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20560v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 用于稳定 JSON 摘要输出的模式引导推理
tldr: 本研究针对临床病历自动填充CRF的挑战，提出LLM StructCore两阶段框架。该方法利用模式引导推理（SGR）将134个字段的提取简化为9个核心领域的JSON摘要，再通过确定性编译器进行标准化和虚假阳性过滤。该方案有效解决了数据稀疏和严格契约输出的问题，在多语言测试中表现优异，显著提升了临床数据提取的准确性。
selection_source: fresh_fetch
motivation: 临床病历自动填充CRF面临语言噪声大、输出契约严苛、数据极度稀疏以及误报代价高等挑战。
method: 采用两阶段设计，先由LLM生成核心领域摘要，再通过确定性编译器完成格式规范化与证据门控过滤。
result: 在开发集上取得最高0.6905的macro-F1值，且在多语言环境下均表现出稳健的性能。
conclusion: 该框架通过解耦复杂推理与格式化任务，证明了模式引导推理在处理高稀疏性临床表单填充中的有效性。
---

## 摘要
从临床笔记中自动填写病例报告表 (CRF) 具有挑战性，原因在于语言噪声、严格的输出契约以及误报的高昂代价。我们描述了在 CL4Health 2026 竞赛中针对呼吸困难 CRF 填写（134 个项目）的提交方案，该方案采用了基于模式引导推理 (SGR) 的契约驱动两阶段设计。该任务的关键属性是极度稀疏性：大多数字段为未知，且官方评分会对空值和无依据的预测进行惩罚。我们将单步“LLM 预测 134 个字段”的方法转变为一种分解式方法：(i) 第一阶段生成一个稳定的 SGR 风格 JSON 摘要，包含恰好 9 个领域键；(ii) 第二阶段是一个完全确定性的、零 LLM (0-LLM) 编译器，负责解析第一阶段的摘要、规范化项目名称、将预测结果归一化为官方受控词表、应用证据门控误报过滤器，并将输出扩展为所需的 134 项格式。在 dev80 划分集上，最佳教师配置实现了 0.6543 (EN) 和 0.6905 (IT) 的宏 F1 分数；在隐藏的 test200 上，提交的英文变体在 Codabench 上得分为 0.63。该流水线与语言无关：在没有针对特定语言进行工程设计的情况下，意大利语的结果与英语相当甚至更优。

## Abstract
Automatically filling Case Report Forms (CRFs) from clinical notes is challenging due to noisy language, strict output contracts, and the high cost of false positives. We describe our CL4Health 2026 submission for Dyspnea CRF filling (134 items) using a contract-driven two-stage design grounded in Schema-Guided Reasoning (SGR). The key task property is extreme sparsity: the majority of fields are unknown, and official scoring penalizes both empty values and unsupported predictions. We shift from a single-step "LLM predicts 134 fields" approach to a decomposition where (i) Stage 1 produces a stable SGR-style JSON summary with exactly 9 domain keys, and (ii) Stage 2 is a fully deterministic, 0-LLM compiler that parses the Stage 1 summary, canonicalizes item names, normalizes predictions to the official controlled vocabulary, applies evidence-gated false-positive filters, and expands the output into the required 134-item format. On the dev80 split, the best teacher configuration achieves macro-F1 0.6543 (EN) and 0.6905 (IT); on the hidden test200, the submitted English variant scores 0.63 on Codabench. The pipeline is language-agnostic: Italian results match or exceed English with no language-specific engineering.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM StructCore: Schema-Guided Reasoning Condensation and Deterministic Compilation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在临床医学中，将非结构化的临床笔记自动填充到包含 134 个项目的“呼吸困难病例报告表”（CRF）中面临极大挑战。
*   **核心挑战**：
    *   **数据极度稀疏**：134 个字段中，中位数仅有约 12 个字段有实际值，其余均为“未知”。
    *   **契约严苛**：官方评分标准对误报（False Positive）和格式错误惩罚极重。
    *   **LLM 局限性**：直接让 LLM 预测 134 个字段容易导致格式崩溃（JSON 失效）或产生幻觉填充。
*   **整体含义**：论文提出了一种名为 **LLM StructCore** 的两阶段框架，通过将复杂的推理任务与格式化任务解耦，实现了高稳定性和高准确性的临床信息提取。

### 2. 论文提出的方法论
核心思想是将任务分解为“临床信息压缩”和“确定性编译”两个阶段：

*   **第一阶段：模式引导推理（SGR）摘要**
    *   **核心思想**：利用 LLM 将原始笔记压缩为包含 9 个固定领域键（如生命体征、实验室检查、用药等）的 JSON 摘要。
    *   **关键技术**：
        *   **级联（Cascade）**：强制 LLM 按固定顺序扫描临床领域。
        *   **循环（Cycle）**：在提示词中内置检查清单（如 28 种特定疾病），提高稀疏项的召回率。
        *   **多切片策略**：针对长文本采用重叠窗口扫描并合并结果。
*   **第二阶段：确定性编译器（0-LLM Compiler）**
    *   **核心思想**：不使用 LLM，而是通过纯 Python 代码处理第一阶段的摘要。
    *   **算法流程**：
        1.  **键值提取**：解析摘要中的“Key: Value”行。
        2.  **规范化**：利用包含 134 个条目的 **UMLS 别名映射表**和 70 多条缩写规则进行对齐。
        3.  **证据门控（FP-Gates）**：使用正则规则过滤无依据的阳性预测（如排除家族史导致的癌症误报）。
        4.  **词汇归一化**：将提取值映射到官方定义的 13 类受控词汇表中。

### 3. 实验设计
*   **数据集**：使用 CL4Health 2026 官方数据集（NLP-FBK/dyspnea-crf），包含 Train (10)、Dev (80) 和 Test (200，隐藏标签)。
*   **Benchmark**：Codabench 竞赛平台排名。
*   **对比方法**：
    *   **教师模型**：Mistral Large 3 (675B)、Qwen 3.5 (397B)、Claude Sonnet 4.6。
    *   **学生模型**：MedGemma 4B（本地量化版）。
    *   **消融实验**：对比了单阶段直接提取、无 FP 门控变体、不同语言（英/意）的表现。

### 4. 资源与算力
*   **算力设备**：作者在本地 **Apple M3 Pro (18 GB RAM)** 上运行了量化模型。
*   **模型部署**：使用 `llama.cpp` 运行 MedGemma 1.5-4B-it (Q5_K_M 量化版)。
*   **耗时**：第一阶段推理约 8 秒/文档；第二阶段编译耗时 <10 毫秒。
*   **训练时长**：该方法主要基于 Prompt 工程和确定性规则，**未进行大规模微调**，因此不涉及传统意义上的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Dev80 集合上进行了多轮跨语言（英、意）和多模型对比。
    *   进行了针对 FP 门控有效性的专项分析。
    *   进行了输入切片策略的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从超大规模 API 模型到本地小规模模型的性能评估。通过跨语言验证（意大利语 vs 英语）证明了框架的通用性。

### 6. 论文的主要结论与发现
*   **性能表现**：在 Dev80 上，意大利语 F1 达到 0.6905，英语达到 0.6543。在 Test200 盲测中，英文版得分 0.63，位居前列。
*   **SGR 的稳定性**：即使是 4B 的小模型，通过 SGR 也能实现 100% 的 JSON 解析成功率，而直接提取 134 个字段的失败率超过 30%。
*   **语言无关性**：意大利语表现优于英语，作者认为是因为原始意大利语病历更具结构化且术语更直接。
*   **确定性优于 LLM**：实验证明，第二阶段使用确定性代码比使用 LLM 提取更准确、更省钱且无随机性。

### 7. 优点
*   **架构优雅**：将模糊的推理与严格的格式要求解耦，解决了 LLM 难以遵循复杂 Schema 的痛点。
*   **边缘计算友好**：通过压缩任务难度，使得 4B 级别的小模型也能在笔记本电脑上完成高质量的临床提取。
*   **可解释性强**：每一个填充项都可以追溯到摘要中的具体行和特定的编译规则。
*   **鲁棒性**：引入 UMLS 映射和证据门控，极大地降低了临床场景中最忌讳的误报率。

### 8. 不足与局限
*   **召回率瓶颈**：第二阶段无法恢复第一阶段摘要中遗漏的信息。如果第一阶段未提取到某事实，后续无法补救。
*   **规则维护成本**：确定性编译器依赖于手动维护的缩写表和正则规则，对于全新的医疗领域或极不规范的缩写可能失效。
*   **上下文丢失**：9 个领域的扁平化摘要可能会丢失一些复杂的时序关系或细微的上下文逻辑。
*   **评估局限**：目前仅在呼吸困难（Dyspnea）这一特定 CRF 模式下验证，尚未在其他疾病领域广泛测试。

（完）
