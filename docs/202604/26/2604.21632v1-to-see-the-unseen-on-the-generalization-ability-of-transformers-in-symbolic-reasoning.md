---
title: "To See the Unseen: on the Generalization Ability of Transformers in Symbolic Reasoning"
title_zh: 洞察未见：论 Transformer 在符号推理中的泛化能力
authors: "Nevena Lazić, Liam Fowl, András György, Csaba Szepesvári"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21632v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: Transformer在符号推理中的泛化能力
tldr: 本研究深入探讨了Decoder-only Transformer在符号推理任务中对未见变量名的泛化瓶颈。研究发现，训练过程中未见Token的解嵌入（unembedding）权重会发生表征坍缩，导致模型难以区分多个新变量。作者提出了一套结合架构改进（增强复制能力）、数据多样化及权重重置的综合方案，成功实现了对未见Token的泛化，并在Gemma 3系列模型中验证了此类坍缩现象的存在及其对微调的负面影响。
selection_source: fresh_fetch
motivation: 探究Transformer在逻辑推理中无法泛化至未见Token的深层机制，特别是表征坍缩对模型性能的影响。
method: 提出结合促进复制的架构调整、增加数据多样性以及冻结或定期重置Token（解）嵌入权重的综合优化方案。
result: 实验证明该组合技术能有效解决表征坍缩问题，使模型在包含未见变量的命题逻辑推理任务中实现显著的泛化提升。
conclusion: 未见Token的解嵌入坍缩是限制符号推理泛化的关键因素，通过针对性的权重管理和架构优化可显著增强模型的泛化能力。
---

## 摘要
我们研究了仅解码器（decoder-only）Transformer 模型执行抽象符号推理的能力，特别是解决上下文中的命题逻辑推理问题。以往的研究表明，模型无法泛化到包含训练期间未见变量名的问题，其原因之一是复制（或生成）未见 token 的困难。我们从理论和实证两方面证明，一种特定的表示坍缩（representational collapse）也起到了关键作用：在训练过程中，未见 token 的反嵌入（unembeddings，即最后一层权重）会坍缩为几乎相同的向量。这种坍缩使得模型难以区分多个未见变量（特别是当嵌入和反嵌入参数共享时），并为“主动遗忘”（active forgetting，即定期重置 token 的嵌入和反嵌入）等现有启发式干预措施的有效性提供了机理解释。基于这些观察，我们设计了一套组合技术，包括促进复制的小型架构调整、数据多样性以及冻结或重置 (反)嵌入，从而实现了对未见 token 的泛化。我们通过在命题逻辑推理问题上进行的大量受控实验支持了上述论点。除了合成实验外，我们还在 Gemma 3 系列开源模型中观察到了 (反)嵌入坍缩的证据，该系列包含 99 个预留给下游使用的未使用 token。实证结果表明，这些 token 之间高度相关的嵌入对于微调应用而言是一个糟糕的初始化。

## Abstract
We investigate the ability of decoder-only transformer models to perform abstract symbolic reasoning; specifically solving propositional logic reasoning problems given in-context. Previous work demonstrated that models fail to generalize to problems involving variable names that were not observed during training, and it was shown that one reason behind this is the difficulty of copying (or generating) unseen tokens. We show both theoretically and empirically that a particular representational collapse also has a crucial role: the unembeddings (last-layer weights) of unseen tokens collapse to nearly the same vector during training. The collapse makes distinguishing multiple unseen variables difficult for the model (especially when the embedding and unembedding parameters are shared), and provides a mechanistic explanation for the effectiveness of existing heuristic interventions like "active forgetting", which periodically reset the token (un)embeddings. Based on these observations, we devise a combination of techniques, involving a small architecture change facilitating copying, data diversity, and freezing or resetting (un)embeddings, that achieves generalization to unseen tokens. We support our claims with extensive controlled experiments on propositional logic reasoning problems. Beyond synthetic experiments, we also observe evidence of (un)embedding collapse in the open-weight models in the Gemma 3 family, which includes 99 unused tokens reserved for downstream use. Empirically we find that the correlated embeddings of these tokens are a poor initialization for finetuning applications.

---

## 论文详细总结（自动生成）

这篇论文对 Transformer 在符号推理任务中的泛化瓶颈进行了深入的机理分析，并提出了针对性的改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在多项任务中表现优异，但在面对未见过的符号（如新的变量名）时，其推理能力往往会崩溃。
*   **核心问题**：论文探讨了为什么仅解码器（Decoder-only）Transformer 难以处理训练集中未出现的 token。
*   **主要发现**：除了以往研究提到的“复制能力不足”外，论文指出存在一种**“解嵌入坍缩”（Unembedding Collapse）**现象——即模型在训练过程中，会将所有未见 token 的输出层权重（解嵌入向量）收敛到几乎相同的方向，导致模型无法区分不同的新变量。

### 2. 方法论
论文提出了一套组合技术方案，旨在从架构、数据和参数管理三个维度解决泛化问题：
*   **复制注意力（Copy Attention）**：在标准注意力机制基础上增加一个快捷路径，允许模型直接从输入嵌入中提取信息并映射到输出概率，绕过深层 MLP 对未见 token 表征的破坏。
*   **权重管理策略**：
    *   **冻结嵌入（Frozen WE）**：将 token 的嵌入和解嵌入矩阵固定在初始化状态（通常是正交或近正交的），防止其在训练中坍缩。
    *   **主动遗忘/定期重置（Active Forgetting/Resetting）**：在训练过程中定期重新初始化嵌入矩阵，以消除模型对特定 token 的偏见。
*   **数据多样性**：通过增加训练中使用的符号（变量名）数量，迫使模型学习抽象的逻辑结构而非特定的 token 组合。
*   **理论证明**：论文通过引理 4.1 证明了在使用权重衰减（Weight Decay）和层归一化（LayerNorm）的 SGD 训练中，未见标签的权重坍缩是不可避免的。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成命题逻辑推理**：基于 Horn 子句生成逻辑问题，包含规则、事实、查询及思维链（CoT）推理过程。
    *   **Gemma 3 实证分析**：分析开源模型 Gemma 3 中 99 个预留“未使用 token”的表征分布。
*   **Benchmark 与对比方法**：
    *   对比了标准 Transformer 与带有 Copy Attention 的 Transformer。
    *   对比了嵌入层“可训练”、“冻结”及“定期重置”三种模式。
    *   设置了三种测试场景：全见 token、查询 token 未见、所有谓词 token 均未见。
*   **多样性变量**：改变训练集中的符号词汇量大小（从 150 到 9600 个）。

### 4. 资源与算力
*   **模型规模**：实验使用了 10 层、约 60M-75M 参数的小型 Transformer 模型。
*   **框架**：基于 NanoDO（JAX 实现）进行训练。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长，但提到使用了 AdamW 优化器，批大小为 256，训练步数为 50k 步。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的受控实验，涵盖了不同词汇量、不同架构组件的消融实验。
*   **充分性**：实验设计非常严谨，不仅在合成数据上验证了理论，还进一步在真实的大规模预训练模型（Gemma 3）上验证了“坍缩”现象的存在。
*   **客观性**：通过对比不同词汇量下的泛化表现，客观揭示了数据多样性与架构改进之间的互补关系。

### 6. 主要结论与发现
*   **坍缩是主因**：未见 token 的解嵌入向量在训练中会高度相关（余弦相似度接近 1.0），导致模型在逻辑推理中无法区分不同的未知变量。
*   **组合方案有效**：单纯增加复制能力或单纯增加数据多样性都无法完全解决问题，必须结合“防止权重坍缩”的策略（如冻结权重）才能实现完美的符号泛化。
*   **Gemma 3 验证**：发现 Gemma 3 的未使用 token 已经发生了坍缩，这解释了为什么直接使用这些 token 进行微调时收敛速度极慢（比普通 token 慢 10 倍）。

### 7. 优点
*   **机理解释深刻**：不仅提出了解决方法，还从数学和实证角度解释了“为什么会失败”，为“主动遗忘”等启发式方法提供了理论支撑。
*   **架构改进实用**：提出的 Copy Attention 简单且有效，对标准 Transformer 侵入性小。
*   **跨尺度验证**：从 60M 的小模型到 27B 的 Gemma 3 均观察到了相似的权重分布规律。

### 8. 不足与局限
*   **通用性代价**：实验显示，冻结嵌入层虽然有助于符号推理，但会显著损害模型在通用语言建模（如 C4 数据集）上的性能。
*   **多 token 符号挑战**：初步研究显示，当符号由多个 token 组成时，模型会面临长度泛化和前缀混淆等新问题，本文的方法未能完全解决。
*   **任务单一**：主要集中在命题逻辑，尚未扩展到更复杂的数学推理或程序代码生成任务。

（完）
