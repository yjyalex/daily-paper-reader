---
title: "Scaling Reasoning Tokens via RL and Parallel Thinking: Evidence From Competitive Programming"
title_zh: 通过强化学习与并行思考扩展推理 Token：来自竞赛编程的证据
authors: "Qianfan Zhang, Tianyu Guo, Xuandi Ren, Jiale Chen, Ming Ding, Ran Xin, Xia Xiao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01302v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过强化学习扩展竞争性编程的推理Token
tldr: 本研究探讨了在竞赛编程中通过强化学习（RL）和测试时并行思考来扩展推理Token预算的方法。研究发现验证准确率与推理Token数呈对数线性关系，并通过验证RL预热和随机裁剪优化了训练轨迹。为降低长序列推理成本，提出了一种多轮并行思考流水线，将预算分配至多线程的生成、验证与精炼过程。最终模型在AetherCode难题集上超越了GPT-5-high，证明了推理扩展的巨大潜力。
selection_source: fresh_fetch
motivation: 旨在解决单次生成推理Token在全注意力机制下成本过高的问题，并探索如何通过训练与推理策略的协同优化来提升复杂编程任务的性能。
method: 结合了带有验证预热和随机裁剪的强化学习训练，以及一种支持多线程、多轮次生成、验证与精炼的端到端并行思考推理流水线。
result: "该系统在每题平均消耗760万Token的情况下，pass@1性能达到了基础RL模型的oracle pass@16水平，并在AetherCode难题集上优于GPT-5-high。"
conclusion: 通过强化学习优化推理长度并结合测试时并行处理架构，可以显著提升大语言模型在复杂逻辑推理任务中的表现上限。
---

## 摘要
我们研究了如何通过两种互补的方法来扩展竞赛编程的推理 Token 预算：训练时的强化学习 (RL) 和测试时的并行思考。在 RL 训练期间，我们观察到验证准确率与连续检查点生成的平均推理 Token 数量之间存在近似对数线性的关系，并展示了两种改变这一训练轨迹的方法：验证 RL 预热提高了起点，而随机裁剪在观察范围内产生了更陡峭的趋势。由于在全注意力机制下，RL 过程中扩展单次生成的推理很快会变得昂贵，我们引入了一种多轮并行思考流水线，将 Token 预算分配到生成、验证和细化的多个线程和轮次中。我们在此流水线上对模型进行端到端训练，使训练目标与测试时的结构相匹配。从 Seed-OSS-36B 开始，该完整系统在每个问题平均使用 760 万个 Token 的情况下，通过 16 个线程、每个线程 16 轮的配置，在 pass@1 指标上达到了基础 RL 模型 oracle pass@16 的水平，并在来自 AetherCode 的 456 道高难度竞赛编程题目上超越了 GPT-5-high。

## Abstract
We study how to scale reasoning token budgets for competitive programming through two complementary approaches: training-time reinforcement learning (RL) and test-time parallel thinking. During RL training, we observe an approximately log-linear relationship between validation accuracy and the average number of generated reasoning tokens over successive checkpoints, and show two ways to shift this training trajectory: verification RL warmup raises the starting point, while randomized clipping produces a steeper trend in the observed regime. As scaling single-generation reasoning during RL quickly becomes expensive under full attention, we introduce a multi-round parallel thinking pipeline that distributes the token budget across threads and rounds of generation, verification, and refinement. We train the model end-to-end on this pipeline to match the training objective to the test-time structure. Starting from Seed-OSS-36B, the full system with 16 threads and 16 rounds per thread matches the underlying RL model's oracle pass@16 at pass@1 using 7.6 million tokens per problem on average, and surpasses GPT-5-high on 456 hard competitive programming problems from AetherCode.

---

## 论文详细总结（自动生成）

这篇论文由字节跳动 Seed 团队联合普林斯顿、加州大学伯克利分校和斯坦福大学的研究人员共同完成。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在竞赛编程（Competitive Programming）这一高难度领域，通过扩展“推理 Token”（Reasoning Tokens）的预算来提升模型的性能上限。
*   **研究动机**：
    *   虽然强化学习（RL）能诱导模型产生长链思考（CoT），但受限于全注意力机制的平方级计算开销，单次生成的长度存在“计算墙”。
    *   现有的测试时计算扩展（Test-time Scaling）多为简单的采样或搜索，缺乏与训练阶段的深度对齐。
    *   竞赛编程具有明确的执行反馈（Unit Tests），是研究推理能力扩展的理想实验场。

### 2. 方法论：核心思想与关键技术
论文提出了从训练到推理的全链路扩展方案：
*   **训练阶段：优化对数线性趋势**
    *   **经验发现**：验证准确率与平均推理 Token 数的对数呈线性关系。
    *   **随机裁剪（Randomized Clipping, RC）**：将硬性的 Token 长度限制改为随机分布（如均匀分布），将奖励函数的“硬悬崖”变为“平滑坡度”，从而在 RL 训练中诱导更陡峭的性能增长曲线。
    *   **验证 RL 预热（Verification RL Warmup）**：在生成 RL 之前，先训练模型进行“验证”任务（判断代码正误并给出理由）。这提升了模型的自我评估能力，从而抬高了后续生成 RL 的性能起点。
*   **推理阶段：并行思考（Parallel Thinking）流水线**
    *   **多线程与多轮次**：系统启动 $N$ 个独立线程，每个线程进行最多 $M$ 轮“生成-验证-精炼”循环。
    *   **自我验证与排序**：模型对每个候选解进行 $V$ 次独立验证采样，根据通过验证的票数对解进行排序。
*   **端到端对齐训练**：
    *   使用 **Turn-grouped GRPO** 算法，在多轮对话语境下对生成、验证、精炼能力进行联合训练，确保模型在测试时的多轮交互中保持一致性。

### 3. 实验设计与对比
*   **基座模型**：Seed-OSS-36B-Base。
*   **数据集**：
    *   **训练集**：约 1 万道来自 Codeforces 等平台的竞赛编程题。
    *   **验证集**：AetherCode（包含 456 道 IOI/ICPC 级别的难题）。
*   **对比对象**：
    *   **GPT-5-high**（参考 OpenAI GPT-5 系统卡片中的数据）。
    *   不同配置的 RL 策略（有无预热、有无随机裁剪）。
    *   不同推理策略（纯并行、纯顺序、并行+顺序结合）。

### 4. 资源与算力
*   **硬件**：使用了 256 或 512 块 **NVIDIA A100 GPU**。
*   **训练细节**：最大上下文长度设定在 90K 到 120K Token。
*   **计算成本**：当平均响应长度达到 100K Token 时，单步 RL 训练在 256 块 A100 上需耗时约 4 小时。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的消融实验，包括验证次数 $V$、线程数 $N$、轮次 $M$ 对性能的影响。
*   **客观性**：通过 Oracle pass@k 曲线分析了 RL 是否真正扩展了解空间（Coverage）还是仅仅提升了采样概率。
*   **公平性**：在统一的 Token 预算轴上对比了训练时扩展与测试时扩展的效率，实验设计较为严谨。

### 6. 主要结论与发现
*   **对数线性规律**：验证了推理 Token 增加与准确率提升之间的稳定关系。
*   **顺序优于并行**：在相同 Token 预算下，顺序精炼（Sequential Refinement）比单纯的并行生成（Parallel Generation）更有效，因为前者利用了验证反馈。
*   **性能突破**：完整系统（16 线程 × 16 轮）在每题平均消耗 760 万 Token 的情况下，pass@1 准确率达到了基础 RL 模型 oracle pass@16 的水平，并显著超越了 GPT-5-high。
*   **RL 的本质**：在编程领域，RL 不仅是“浓缩”已知解，还能通过搜索和反馈发现 SFT 阶段无法触达的新解法。

### 7. 优点
*   **系统性强**：将训练时的 RL 优化与测试时的并行架构有机结合，并实现了端到端对齐。
*   **技术创新**：提出的“随机裁剪”和“Turn-grouped GRPO”为长序列推理训练提供了实用的优化技巧。
*   **实战价值**：在极高难度的 AetherCode 榜单上取得了领先结果，证明了方案的有效性。

### 8. 不足与局限
*   **推理成本极高**：每道题消耗 760 万 Token 的成本对于大规模商业应用来说过于昂贵。
*   **领域局限性**：实验主要集中在竞赛编程，虽然该领域逻辑严密，但方法论在数学、法律或通用对话推理中的迁移效果尚待验证。
*   **注意力瓶颈**：尽管采用了并行思考，但单次生成的长度仍受限于注意力机制的计算复杂度，未能从底层架构上彻底解决长序列开销问题。

（完）
