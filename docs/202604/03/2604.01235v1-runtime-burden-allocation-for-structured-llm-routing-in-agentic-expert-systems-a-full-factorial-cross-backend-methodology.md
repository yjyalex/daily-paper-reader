---
title: "Runtime Burden Allocation for Structured LLM Routing in Agentic Expert Systems: A Full-Factorial Cross-Backend Methodology"
title_zh: 智能体专家系统中结构化大语言模型路由的运行时负担分配：一种全因子跨后端方法论
authors: "Zhou Hanlin, Chan Huah Yong"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2604.01235v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 结构化大语言模型路由的延迟和实现成本
tldr: 本研究将结构化LLM路由视为系统级的负担分配问题，而非单纯的提示工程。通过对OpenAI、Gemini和Llama等后端进行全因子基准测试（涵盖48种配置和1.5万余次请求），探讨了输出结构在生成栈中的分配方式对正确性、延迟和成本的影响。研究发现不存在通用的最佳路由模式，性能受后端特定效应主导，并为此提供了生产级智能体系统的部署框架。
selection_source: fresh_fetch
motivation: 结构化LLM路由在智能体系统中至关重要，但现有研究往往忽略了系统层面的负担分配对性能、延迟和成本的综合影响。
method: 采用全因子实验方法，在多个主流模型后端评估了不同结构化工作分配模式（直接输出、压缩传输、本地重构）的性能表现。
result: 实验表明不存在普适的最佳路由模式，不同后端在正确性和效率增益上表现出显著的交互效应差异。
conclusion: 开发者应根据具体后端特性灵活调整结构化路由策略，本研究为生产级智能体系统的部署提供了决策框架和实践指导。
---

## 摘要
结构化大语言模型（LLM）路由通常被视为一个提示工程问题。我们认为，从更根本的角度来看，它是一个系统级的负担分配问题。随着大语言模型成为智能体 AI 系统的核心控制组件，可靠的结构化路由必须在实际部署约束下平衡正确性、延迟和实现成本。我们表明，这种平衡不仅受提示或模式（schema）的影响，还取决于结构化工作在生成栈中的分配方式：即输出结构是由模型直接生成、在传输过程中压缩，还是在生成后进行本地重构。我们通过一项全面的全因子基准测试评估了这一表述，该测试涵盖了 OpenAI、Gemini 和 Llama 后端的 48 种部署配置和 15,552 次请求。我们的核心发现具有重要意义：不存在通用的最佳路由模式。相反，特定于后端的交互效应主导了性能表现。在 Gemini 和 OpenAI 上保持高度可靠的模式在 Llama 上可能会出现严重的正确性下降，而压缩实现带来的效率提升则强烈依赖于后端。本研究并非仅仅提供另一种孤立的模型对比，而是贡献了一个可部署的框架，用于在异构后端条件下对结构化路由进行推理。我们提供了一种跨后端评估方法论和实际部署指南，旨在为生产级智能体专家系统在正确性-成本-延迟的前沿权衡中提供导航。

## Abstract
Structured LLM routing is often treated as a prompt-engineering problem. We argue that it is, more fundamentally, a systems-level burden-allocation problem. As large language models (LLMs) become core control components in agentic AI systems, reliable structured routing must balance correctness, latency, and implementation cost under real deployment constraints. We show that this balance is shaped not only by prompts or schemas, but also by how structural work is allocated across the generation stack: whether output structure is emitted directly by the model, compressed during transport, or reconstructed locally after generation.   We evaluate this formulation through a comprehensive full-factorial benchmark covering 48 deployment configurations and 15,552 requests across OpenAI, Gemini, and Llama backends. Our central finding is consequential: there is no universal best routing mode. Instead, backend-specific interaction effects dominate performance. Modes that remain highly reliable on Gemini and OpenAI can suffer substantial correctness degradation on Llama, while efficiency gains from compressed realization are strongly backend-dependent.   Rather than presenting another isolated model comparison, this work contributes a deployable framework for reasoning about structured routing under heterogeneous backend conditions. We provide a cross-backend evaluation methodology and practical deployment guidance for navigating the correctness-cost-latency frontier in production-grade agentic expert systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Runtime Burden Allocation for Structured LLM Routing in Agentic Expert Systems: A Full-Factorial Cross-Backend Methodology》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在智能体（Agentic）系统中，大语言模型（LLM）常作为“路由控制器”（Router），负责将请求分发至不同子系统。现有的研究多将其视为提示工程（Prompt Engineering）问题，忽略了系统层面的**运行时负担分配（Runtime Burden Allocation）**。
*   **核心问题**：如何平衡结构化输出的正确性、延迟和成本？结构化工作（如 JSON 格式化）应该由模型直接生成，还是通过压缩传输后由本地软件重构？

### 2. 方法论
*   **核心思想**：将结构化路由重新定义为“系统级负担分配问题”。研究者提出了一个三维框架来描述运行时配置（$\psi_m$）：
    1.  **序列化负担（Serialization Burden）**：模型在生成循环中构建模式（Schema）的工作量。
    2.  **传输语义（Transport Semantics）**：流式（Streaming）与非流式传输的选择。
    3.  **结构实现位置（Locus of Structure Realization）**：由模型直接输出最终结构，还是由本地确定性软件进行重构。
*   **技术细节**：
    *   **MCLR 模式（压缩模式）**：模型仅输出紧凑的语义代码，由本地代码根据预定义逻辑重构为完整的 JSON 记录。
    *   **WLC 指标（工作流下限完成率）**：基于 Fréchet 下限定义的代理指标，用于评估下游系统接收到“可用且正确”指令的最低概率。

### 3. 实验设计
*   **全因子矩阵（Full-Factorial）**：设计了 48 种部署组合（4 模式 × 3 后端 × 2 约束 × 2 传输）。
    *   **模式（Mode）**：MJ（最小 JSON）、SJ（大预算 JSON）、MJS（流式 JSON）、MCLR（本地重构）。
    *   **后端（Backend）**：OpenAI (GPT系列), Gemini, Llama。
    *   **约束与传输**：有限/无限 Token 预算；流式/非流式 API 调用。
*   **数据集与规模**：包含 324 个精心设计的请求（涵盖简单、复杂、中断敏感等场景），总计执行了 **15,552 次** 路由请求。
*   **对比指标**：格式合规性（FC）、路由准确率（RA）、状态保留（SR）、延迟（LAT）和 Token 消耗（TOK）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：该研究主要针对现有的商业 API（OpenAI, Gemini）和已部署的推理后端（Llama）进行系统级评估，侧重于推理阶段的性能表现而非模型训练。

### 5. 实验数量与充分性
*   **实验充分性**：实验设计非常严谨。通过 1.5 万余次请求覆盖了 48 种配置，并使用了 **Type-II ANOVA（方差分析）** 进行统计验证。
*   **客观性**：研究不仅报告了平均值，还通过“偏 Eta 平方”（Partial Eta Squared）衡量了各因素的效应大小，确保结论不是由随机噪声驱动的。跨后端的对比增强了结果的普适性。

### 6. 主要结论与发现
*   **不存在通用最佳模式**：性能表现由“后端 × 模式”的交互效应主导。在 Gemini 上表现良好的配置，在 Llama 上可能导致正确性崩溃。
*   **压缩模式的代价**：MCLR（本地重构）虽然能显著降低 Token 消耗和延迟，但在所有后端上都会导致准确率下降。在 Llama 上，这种模式会导致路由准确率从 ~82% 骤降至 ~22%。
*   **流式传输的局限性**：在结构化控制任务中，流式传输对操作延迟的改善有限，且对正确性几乎没有正面贡献，因为下游系统必须等待完整记录到达后才能执行。
*   **特定路由的脆弱性**：开发者（Dev）等专业路由分支比普通聊天（Chat）分支更容易受到负担分配变化的影响。

### 7. 优点
*   **视角创新**：跳出了单纯的提示工程范畴，从系统架构和负担分配的角度审视 LLM 路由。
*   **方法论严谨**：采用全因子实验和统计学分析，提供了比普通 Leaderboard 更深层的因果洞察。
*   **部署指导性强**：提出了具体的部署规则（如：正确性优先时坚持直接 JSON 模式）。

### 8. 不足与局限
*   **任务空间受限**：实验仅基于 4 个路由标签的紧凑本体，对于拥有数十个或上百个分支的复杂路由场景，结论的迁移性有待验证。
*   **缺乏端到端验证**：研究主要测量路由记录的质量，未直接测量下游专家系统处理任务的最终成功率。
*   **干预捆绑**：MCLR 模式同时改变了输出格式和 Token 预算，未能完全解耦“格式简化”与“预算缩减”各自对性能的影响。

（完）
