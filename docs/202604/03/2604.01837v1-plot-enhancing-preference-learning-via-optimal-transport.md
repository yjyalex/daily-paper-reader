---
title: "PLOT: Enhancing Preference Learning via Optimal Transport"
title_zh: PLOT：通过最优传输增强偏好学习
authors: "Liang Zhu, Yuelin Bai, Xiankun Ren, Jiaxi Yang, Lei Zhang, Feiteng Fang, Hamid Alinejad-Rokny, Minghuan Tan, Min Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01837v1"
tags: ["query:token"]
score: 9.0
evidence: 用于偏好学习和对齐的Token级损失函数
tldr: 针对大语言模型偏好学习中性能提升有限、计算成本高及缺乏全局Token建模等问题，本文提出PLOT框架。该方法将偏好学习建模为最优传输（Optimal Transport）问题，通过Token级别的损失函数实现模型输出与人类偏好的对齐。PLOT利用Token嵌入捕捉语义关系，在保持模型原始分布稳定性的同时，显著提升了对齐性能和生成质量。
selection_source: fresh_fetch
motivation: 现有偏好学习方法在性能增益、计算成本、超参数敏感性以及全局Token关系建模方面存在局限。
method: 提出基于最优传输（OT）的Token级别损失函数，通过Token嵌入捕捉语义关系，将偏好学习转化为最优传输问题进行优化。
result: 在人类价值观和逻辑解题等多个维度的实验表明，PLOT在提升对齐性能的同时，有效保持了文本的流畅性与连贯性。
conclusion: 最优传输为大语言模型偏好学习提供了一个理论基础扎实且高效的新框架，为模型对齐提供了新视角。
---

## 摘要
大语言模型（LLMs）中的偏好学习已取得显著进展，但现有方法仍受限于性能提升有限、计算成本高、超参数敏感以及对全局 token 级关系建模不足等问题。我们提出了 PLOT，通过源自最优传输的 token 级损失函数，增强了基于微调对齐的偏好学习。通过将偏好学习建模为最优传输问题，PLOT 在使模型输出与人类偏好对齐的同时，保留了 LLMs 的原始分布，从而确保了稳定性和鲁棒性。此外，PLOT 利用 token 嵌入来捕捉语义关系，实现了全局感知的优化。在涵盖人类价值观以及逻辑与问题解决这两个偏好类别（共涉及七个子偏好）的实验表明，PLOT 在保持流畅性和连贯性的同时，持续提升了对齐性能。这些结果证实了最优传输作为偏好学习的一种原则性方法论的有效性，建立了一个具有理论基础的框架，为 LLMs 的偏好学习提供了新的见解。

## Abstract
Preference learning in Large Language Models (LLMs) has advanced significantly, yet existing methods remain limited by modest performance gains, high computational costs, hyperparameter sensitivity, and insufficient modeling of global token-level relationships. We introduce PLOT, which enhances Preference Learning in fine-tuning-based alignment through a token-level loss derived from Optimal Transport. By formulating preference learning as an Optimal Transport Problem, PLOT aligns model outputs with human preferences while preserving the original distribution of LLMs, ensuring stability and robustness. Furthermore, PLOT leverages token embeddings to capture semantic relationships, enabling globally informed optimization. Experiments across two preference categories - Human Values and Logic & Problem Solving - spanning seven subpreferences demonstrate that PLOT consistently improves alignment performance while maintaining fluency and coherence. These results substantiate optimal transport as a principled methodology for preference learning, establishing a theoretically grounded framework that provides new insights for preference learning of LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《PLOT: Enhancing Preference Learning via Optimal Transport》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 偏好学习方法（如 DPO、RLHF 等）在对齐过程中存在性能增益有限、计算成本高、对超参数高度敏感以及缺乏对全局 Token 级语义关系建模的问题。
*   **研究背景**：传统的对齐方法往往只关注单个 Token 的概率或序列级的奖励，忽略了 Token 之间的语义关联和输出分布的全局结构。这导致模型在处理复杂偏好时不够稳健，且容易在微调过程中丧失通用能力。

### 2. 方法论：PLOT 框架
*   **核心思想**：将偏好学习重新表述为一个**最优传输（Optimal Transport, OT）**问题。通过计算模型当前输出分布与目标偏好分布之间的最小传输代价（Wasserstein 距离），实现更精准、全局的对齐。
*   **关键技术细节**：
    *   **目标偏好分布构建**：利用偏好数据中正负样本的 Token 频率差异（$Q_{diff}$）构造目标分布 $P_t$，并进行非负化和归一化处理。
    *   **语义感知代价矩阵**：不同于传统的 0-1 代价矩阵，PLOT 利用 Token 的**嵌入向量（Embedding）**计算 $L_2$ 范数距离，从而在代价矩阵中引入语义信息。
    *   **损失函数优化**：将复杂的 OT 问题简化为一维 Wasserstein 距离（$W_1$）的计算，利用累积分布函数（CDF）的差值进行高效求解。
    *   **集成方式**：将 PLOT 损失作为正则项引入现有损失函数：$L = L_{vanilla} + \alpha L_{PLOT}$。

### 3. 实验设计
*   **数据集与场景**：
    *   **人类价值观**：使用 HH-RLHF 数据集，涵盖无害性（Harmlessness）、帮助性（Helpfulness）和人性化（Humanity）。
    *   **逻辑与问题解决**：使用 Magpie 筛选的逻辑推理数据，涵盖数学、推理、代码和 STEM。
*   **Benchmark（基准测试）**：
    *   安全评估：**HarmBench**（包含 ZS, PEZ, GBDA, UAT, SFS, GCG 等多种红队攻击方法）。
    *   能力评估：**GSM8K**、**MATH**（数学）；**MT-Bench**（综合能力）；**AlpacaEval 2.0**（通用对话能力）。
*   **对比方法**：SFT（有监督微调）、**DPO**（直接偏好优化）、**PRO**（偏好排名优化）、**AOT**（基于分布的对齐）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 80GB GPU**。
*   **训练配置**：总 Batch Size 为 4，仅训练 **1 个 epoch**。
*   **计算开销**：论文明确指出，相比于 DPO，PLOT 仅增加了约 **2.67%** 的训练时间（从 487s 增至 500s），计算成本极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构和规模的模型（Llama-3.2-3B, Llama-3.1-8B, Qwen-2.5-7B）。
*   **实验维度**：针对 7 个子偏好领域进行了广泛测试，并进行了消融实验（验证 Embedding 的必要性、OT 的必要性）和超参数稳定性分析。
*   **客观性**：实验采用了多种自动化红队攻击和 GPT-4 评分机制，并提供了标准差，显示了结果的统计显著性，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **显著提升安全性**：在所有测试模型和基线方法上，引入 PLOT 均显著降低了攻击成功率（ASR）。例如在 Llama-3.2-3B 上，SFS 攻击下的 ASR 从 25.75% 降至 16.92%。
*   **增强逻辑能力**：在数学（GSM8K/MATH）和推理任务上，PLOT 均优于 DPO 和 Instruct 模型。
*   **高稳定性**：PLOT 对超参数 $\alpha$ 的变化不敏感，表现出比 DEFT 等方法更强的鲁棒性。
*   **保留通用能力**：AlpacaEval 2.0 结果显示，PLOT 在对齐偏好的同时，能比 DPO 更好地缓解模型通用能力的下降。

### 7. 优点与亮点
*   **理论完备性**：将偏好学习提升到分布对齐的高度，利用最优传输理论提供了坚实的数学基础。
*   **语义感知**：通过 Token 嵌入捕捉语义关系，解决了传统方法“只看概率、不看语义”的弊端。
*   **高效实用**：计算开销极小，且作为插件式损失函数，可以轻松集成到现有的各种微调框架中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 8B 及以下规模的模型，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **数据采样偏差**：训练仅使用了随机采样的 4000 条样本，虽然证明了高效性，但未探讨在不同数据规模下的表现。
*   **目标分布依赖**：PLOT 的效果高度依赖于目标偏好分布 $P_t$ 的构建质量，如果正负样本差异无法准确反映偏好，效果可能受限。

（完）
