---
title: On the Role of Reasoning Patterns in the Generalization Discrepancy of Long Chain-of-Thought Supervised Fine-Tuning
title_zh: 论推理模式在长思维链有监督微调泛化差异中的作用
authors: "Zhaoyi Li, Xiangyu Xi, Zhengyu Chen, Wei Wang, Gangwei Jiang, Ranran Shen, Linqi Song, Ying Wei, Defu Lian"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01702v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对长思维链轨迹的监督微调以构建大型推理模型
tldr: 本研究探讨了长思维链（CoT）监督微调中不同数据源对模型泛化性能的影响。通过对比 DeepSeek-R1 和 GPT-OSS 的轨迹，发现 DeepSeek 数据虽训练损失更低，但因其发散且多分支的推理模式导致泛化较差。研究揭示了模型会继承低效的探索行为，并提出通过过滤高分支轨迹来优化 SFT，显著提升了模型在 AIME 等基准测试上的推理能力。
selection_source: fresh_fetch
motivation: 探究为何在相同问题集上，训练损失更低的 CoT 数据源反而导致模型泛化性能下降的悖论。
method: 对比分析不同模型的推理模式差异，并提出一种过滤掉频繁分支、冗余探索轨迹的数据筛选策略。
result: "采用筛选后的 DeepSeek 数据子集进行训练，使模型在 AIME25 等多个推理基准上性能平均提升 3.6%。"
conclusion: 推理轨迹的收敛性比训练损失更能决定 SFT 的泛化效果，减少冗余分支能有效提升模型的推理效率。
---

## 摘要
在长思维链（CoT）轨迹上进行有监督微调（SFT）已成为构建大型推理模型的关键阶段。然而，来自不同来源的 CoT 轨迹如何影响模型的泛化性能仍是一个悬而未决的问题。在本文中，我们使用由两个竞争模型 DeepSeek-R1-0528 和 gpt-oss-120b 生成的两类经过验证的 CoT 轨迹进行了对比研究，并控制其问题集完全相同。尽管它们的性能相当，我们发现了一个引人注目的悖论：较低的训练损失并不意味着更好的泛化能力。在 DeepSeek-R1-0528 数据上进行的 SFT 实现了显著更低的训练损失，但与在 gpt-oss-120b 上训练的模型相比，其在推理基准测试中的泛化性能明显更差。为了理解这一悖论，我们进行了多维度的分析，探究了 token 级 SFT 损失和步骤级推理行为。我们的分析揭示了推理模式上的差异：gpt-oss-120b 表现出高度收敛和演绎的轨迹，而 DeepSeek-R1-0528 则倾向于发散且多分支的探索模式。因此，使用 DeepSeek-R1 数据训练的模型继承了低效的探索行为，经常陷入冗余的探索分支中，从而阻碍其得出正确解。基于这一见解，我们提出了一种简单而有效的补救措施，即过滤掉频繁分支的轨迹，以提高 SFT 的泛化能力。实验表明，在选定的 DeepSeek-R1-0528 子集上进行训练，出人意料地将 AIME25 的推理性能提升了高达 5.1%，BeyondAIME 提升了 5.5%，在五个基准测试中平均提升了 3.6%。

## Abstract
Supervised Fine-Tuning (SFT) on long Chain-of-Thought (CoT) trajectories has become a pivotal phase in building large reasoning models. However, how CoT trajectories from different sources influence the generalization performance of models remains an open question. In this paper, we conduct a comparative study using two sources of verified CoT trajectories generated by two competing models, \texttt{DeepSeek-R1-0528} and \texttt{gpt-oss-120b}, with their problem sets controlled to be identical. Despite their comparable performance, we uncover a striking paradox: lower training loss does not translate to better generalization. SFT on \texttt{DeepSeek-R1-0528} data achieves remarkably lower training loss, yet exhibits significantly worse generalization performance on reasoning benchmarks compared to those trained on \texttt{gpt-oss-120b}. To understand this paradox, we perform a multi-faceted analysis probing token-level SFT loss and step-level reasoning behaviors. Our analysis reveals a difference in reasoning patterns. \texttt{gpt-oss-120b} exhibits highly convergent and deductive trajectories, whereas \texttt{DeepSeek-R1-0528} favors a divergent and branch-heavy exploration pattern. Consequently, models trained with \texttt{DeepSeek-R1} data inherit inefficient exploration behaviors, often getting trapped in redundant exploratory branches that hinder them from reaching correct solutions. Building upon this insight, we propose a simple yet effective remedy of filtering out frequently branching trajectories to improve the generalization of SFT. Experiments show that training on selected \texttt{DeepSeek-R1-0528} subsets surprisingly improves reasoning performance by up to 5.1% on AIME25, 5.5% on BeyondAIME, and on average 3.6% on five benchmarks.

---

## 论文详细总结（自动生成）

### 论文详细总结：论推理模式在长思维链有监督微调泛化差异中的作用

#### 1. 核心问题与研究动机
本文探讨了构建大型推理模型（LRM）过程中的一个核心环节：**长思维链（Long CoT）的有监督微调（SFT）**。研究者发现了一个显著的“泛化悖论”：在相同的问题集上，使用 DeepSeek-R1 生成的推理轨迹进行训练，虽然能获得更低的训练损失（Training Loss），但在下游推理基准测试（如 AIME）中的泛化表现却远不如使用 gpt-oss-120b 轨迹训练的模型。研究动机在于揭示这种“低损失、低性能”现象背后的深层原因，并探索如何优化 SFT 数据选择。

#### 2. 方法论与核心思想
论文通过多维度分析揭示了两种模型推理模式的本质差异，并提出了针对性的改进方案：
*   **Token 级损失去构（Deconstruction）：** 发现 SFT 损失的降低主要是由于大量低损失的“常规 token”（如简单计算）稀释了关键推理信号。DeepSeek-R1 的轨迹中包含更多此类冗余信息。
*   **步骤级推理行为分析：** 将推理步骤分为四类：**Propose（提议/分支）**、**Deduce（演绎）**、**Verify（验证）**和**Backtrack（回溯）**。
    *   **核心发现：** DeepSeek-R1 倾向于“发散性探索”，频繁产生新分支（Propose → Propose）；而 gpt-oss 倾向于“收敛性演绎”，专注于深层逻辑推导（Deduce）。
*   **数据过滤策略（核心技术）：** 提出通过过滤掉“频繁分支”的轨迹来提升泛化性。设计了两个代理指标：
    1.  **步骤数差异率：** 比较同一问题下 R1 与 gpt-oss 的步骤数，过滤掉 R1 显著冗长的部分。
    2.  **分支关键词密度：** 统计轨迹中包含“或许（Perhaps）”、“另一种（Another）”等分支信号词的比例，剔除高比例轨迹。

#### 3. 实验设计
*   **数据集：** 收集了约 50 万个具有挑战性的数学问题（源自 OpenR1-Math, NuminaMath 等），并由 DeepSeek-R1 和 gpt-oss-120b 分别生成正确答案的推理轨迹。
*   **基准测试（Benchmarks）：** MATH500、AIME24、AIME25、BeyondAIME、HMMT25。
*   **对比模型（学生模型）：** 涵盖不同架构和规模，包括 Qwen2.5-7B/32B、Llama3.1-8B、Qwen3-8B。
*   **干预实验：** 包括随机删除 10%-30% 推理步骤的消融实验，以及基于上述代理指标的数据筛选实验。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节：** 提到了使用 Adam 优化器，学习率在 8e-5 到 1e-4 之间，全局 Batch Size 为 64 或 128，训练序列长度（Context Length）设定为 32,768 (32k)。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个不同的基础模型上进行了交叉验证，涵盖了从 7B 到 32B 的参数规模。
*   **充分性：** 实验设计非常严谨，通过控制变量法（相同问题、均验证正确）排除了事实错误或分布偏移的影响。
*   **客观性：** 引入了 DeepSeek-V3.2 作为自动标注器，并进行了人工校验（准确率 92.5%），确保了推理行为分类的客观性。

#### 6. 主要结论与发现
*   **推理模式继承：** 学生模型会通过 SFT 继承教师模型的推理风格。训练于 R1 数据的模型会表现出过度的、低效的尝试和分支行为，导致其在复杂问题上难以收敛到正确答案。
*   **冗余性验证：** 随机删除 R1 轨迹的步骤对性能影响极小甚至有提升，证明其轨迹存在大量结构性冗余；而删除 gpt-oss 的步骤则会导致性能剧降。
*   **筛选即提升：** 仅保留 R1 数据中分支较少的子集（如底部的 50% 或 80%），能显著提升模型性能，在 AIME25 上最高提升了 5.1%。

#### 7. 优点
*   **视角独特：** 首次深入探讨了长 CoT 轨迹的“结构模式”而非仅仅是“正确性”对 SFT 的影响。
*   **实用性强：** 提出的分支过滤策略不需要昂贵的 LLM 重新标注，仅需简单的关键词统计或长度对比即可实现，具有极高的工程参考价值。
*   **解释力强：** 成功解释了为什么 DeepSeek-R1 这种强大的模型在作为 SFT 教师时，其效果有时不如其他模型。

#### 8. 不足与局限
*   **领域局限：** 实验主要集中在数学推理领域，尚不确定该结论是否能完美迁移到代码生成、创意写作或通用智能任务中。
*   **教师模型覆盖：** 研究主要对比了 DeepSeek-R1 和 gpt-oss 两个模型，虽然具有代表性，但未涵盖更多闭源模型（如 o1, o3）或其他开源推理模型。
*   **推理长度偏差：** 尽管控制了推理长度，但 R1 轨迹天然更长，实验中对推理截断的影响讨论仍有进一步深化的空间。

（完）
