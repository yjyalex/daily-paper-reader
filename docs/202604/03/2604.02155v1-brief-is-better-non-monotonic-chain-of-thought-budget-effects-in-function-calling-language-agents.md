---
title: "Brief Is Better: Non-Monotonic Chain-of-Thought Budget Effects in Function-Calling Language Agents"
title_zh: 简短更好：函数调用语言智能体中的非单调思维链预算效应
authors: Xuan Qi
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02155v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 工具使用中推理长度与准确性之间的权衡
tldr: "本研究探讨了思维链（CoT）推理长度对函数调用智能体性能的影响。通过在Qwen2.5-1.5B模型上测试不同token预算，研究发现推理长度与准确率之间存在显著的非单调关系：极短的推理（8-16 token）能将准确率从44%提升至64%，而过长的推理（256 token）反而会导致严重的函数幻觉，使性能降至25%。基于此发现，作者提出了FR-CoT方法，通过结构化短推理引导模型进行函数路由，在保证高准确率的同时消除了函数幻觉。"
selection_source: fresh_fetch
motivation: 旨在探究在结构化工具使用场景下，思维链推理长度的增加是否必然带来智能体性能的提升。
method: 在伯克利函数调用基准测试上，对Qwen2.5-1.5B模型在0到512个token预算下的表现进行了系统性的扫描和错误分解分析。
result: 发现推理长度与准确率呈非单调关系，8-16个token的短推理效果最佳，而长推理会引发严重的函数选择错误和幻觉。
conclusion: 提出了函数路由思维链（FR-CoT）方法，通过模板化短推理强制模型先锁定函数名，实现了高可靠性且无需调优的函数调用。
---

## 摘要
语言智能体在采取行动前应该思考多久？思维链（CoT）推理被广泛认为能提高智能体性能，但在结构化工具使用场景中，推理长度与准确率之间的关系仍不为人所知。我们对函数调用智能体中的 CoT 预算效应进行了系统研究，在 Berkeley Function Calling Leaderboard v3 Multiple 基准测试的 200 个任务中，对六个 token 预算（0-512）进行了扫描。我们的核心发现是 Qwen2.5-1.5B-Instruct 模型上呈现出显著的非单调模式：简短推理（32 tokens）相比直接回答，准确率相对提升了 45%（从 44.0% 提升至 64.0%），而长推理（256 tokens）则使性能降至远低于无 CoT 基线的 25.0%（McNemar p < 0.001）。三向错误分解揭示了其机制：在 d = 0 时，30.5% 的任务失败是因为模型从候选集中选择了错误的函数；简短 CoT 将此比例降至 1.5%，有效地起到了函数路由步骤的作用，而长 CoT 则抵消了这一收益，在 d = 256 时导致了 28.0% 的错误选择和 18.0% 的幻觉函数。Oracle 分析显示，88.6% 的可解任务最多需要 32 个推理 token，平均为 27.6 个 token，更细粒度的扫描表明真正的最优值位于 8-16 个 token 之间。受此路由效应启发，我们提出了函数路由 CoT（FR-CoT），这是一种结构化的简短 CoT 方法，将推理阶段模板化为“Function: [name] / Key args: [...]”，强制模型在推理开始时确定一个有效的函数名称。FR-CoT 实现了与自由格式 d = 32 CoT 统计等效的准确率，同时将函数幻觉降至 0.0%，在无需预算调整的情况下提供了结构可靠性保证。

## Abstract
How much should a language agent think before taking action? Chain-of-thought (CoT) reasoning is widely assumed to improve agent performance, but the relationship between reasoning length and accuracy in structured tool-use settings remains poorly understood. We present a systematic study of CoT budget effects on function-calling agents, sweeping six token budgets (0--512) across 200 tasks from the Berkeley Function Calling Leaderboard v3 Multiple benchmark. Our central finding is a striking non-monotonic pattern on Qwen2.5-1.5B-Instruct: brief reasoning (32 tokens) dramatically improves accuracy by 45% relative over direct answers, from 44.0% to 64.0%, while extended reasoning (256 tokens) degrades performance well below the no-CoT baseline, to 25.0% (McNemar p < 0.001). A three-way error decomposition reveals the mechanism. At d = 0, 30.5% of tasks fail because the model selects the wrong function from the candidate set; brief CoT reduces this to 1.5%, effectively acting as a function-routing step, while long CoT reverses the gain, yielding 28.0% wrong selections and 18.0% hallucinated functions at d = 256. Oracle analysis shows that 88.6% of solvable tasks require at most 32 reasoning tokens, with an average of 27.6 tokens, and a finer-grained sweep indicates that the true optimum lies at 8--16 tokens. Motivated by this routing effect, we propose Function-Routing CoT (FR-CoT), a structured brief-CoT method that templates the reasoning phase as "Function: [name] / Key args: [...]," forcing commitment to a valid function name at the start of reasoning. FR-CoT achieves accuracy statistically equivalent to free-form d = 32 CoT while reducing function hallucination to 0.0%, providing a structural reliability guarantee without budget tuning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Brief Is Better: Non-Monotonic Chain-of-Thought Budget Effects in Function-Calling Language Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在思维链（CoT）推理领域，普遍假设“思考越多，效果越好”。然而，在结构化工具调用（Function Calling）场景下，推理长度与准确性之间的关系尚未得到系统研究。
*   **核心问题**：语言智能体在执行动作前究竟应该“思考”多久？是否存在一个最优的推理 token 预算？
*   **整体含义**：论文挑战了测试时计算量（Test-time Compute）线性扩展的假设，发现在函数调用中，过长的推理反而会显著损害性能。

### 2. 方法论
*   **核心思想**：通过系统性地控制 CoT 的 token 预算（$d$），观察模型在结构化输出任务中的表现，并提出一种结构化的引导方法。
*   **关键技术细节**：
    *   **CoT 预算协议**：设置 $d \in \{0, 32, 64, 128, 256, 512\}$。在 $d>0$ 时，强制模型先进行最多 $d$ 个 token 的推理，然后再生成 JSON 格式的函数调用。
    *   **错误三向分解**：将错误归类为：(1) 函数名幻觉（不在候选集中）；(2) 选错有效函数；(3) 参数错误。
    *   **函数路由思维链（FR-CoT）**：一种参数无关的提示词技术。核心流程是强制模型在推理阶段的第一步就必须输出 `Function: [name]`，从而锁定候选函数，随后再进行简短的参数逻辑推理。
    *   **熵门控（Entropy Gating）**：计算预推理阶段动作分布的熵 $H_0$，尝试将其作为是否需要 CoT 的预测信号。

### 3. 实验设计
*   **数据集/Benchmark**：使用 **Berkeley Function Calling Leaderboard (BFCL) v3 Multiple-function** 分支，包含 200 个具有挑战性的多函数选择任务。
*   **对比模型**：
    *   **Qwen2.5-1.5B-Instruct**（主实验模型，代表资源受限场景）。
    *   **Qwen2.5-7B-Instruct**（跨规模验证）。
    *   **Phi-3-mini-4k-instruct**（跨架构验证）。
*   **对比方法**：直接回答（No-CoT）、自由格式 CoT（不同预算）、约束解码（Constrained Decoding，通过 log-prob 强制选择有效函数名）、以及提出的 FR-CoT。

### 4. 资源与算力
*   **算力说明**：论文提到使用 HuggingFace Transformers 库，以 `bfloat16` 精度运行模型。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）数量以及实验的总耗时。由于主要涉及推理实验而非训练，算力消耗相对较小。

### 5. 实验数量与充分性
*   **实验规模**：对 3 种模型在 6 种预算下进行了全量扫描，总计进行了数千次推理调用。
*   **充分性分析**：
    *   **多维度验证**：除了主实验，还进行了细粒度预算扫描（以 8 token 为步长）、格式控制消融实验（排除格式偏移干扰）、以及 Oracle（先验最优）预算分析。
    *   **统计显著性**：使用了 McNemar 检验和 10,000 次 Bootstrap 重采样来计算 95% 置信区间，确保结论不是偶然。
    *   **客观性**：通过跨架构（Qwen vs Phi）对比，揭示了模型自然停止行为（EOS 率）对结果的影响，实验设计较为严谨。

### 6. 主要结论与发现
*   **非单调效应**：准确率随推理长度呈现“倒 U 型”。简短推理（32 tokens）效果最好（+45% 提升），而长推理（256+ tokens）会导致性能崩溃，甚至低于不思考的基线。
*   **最优预算极短**：细粒度扫描显示，真正的最优推理长度仅为 **8-16 个 tokens**。
*   **失效机制**：短 CoT 的成功在于它起到了“函数路由”的作用；而长 CoT 的失败是因为模型在长文本中产生了“逻辑漂移”，导致函数幻觉和对正确初始判断的自我否定。
*   **FR-CoT 的有效性**：FR-CoT 在不进行预算调优的情况下，实现了与最优短推理相当的准确率，并将函数幻觉率降至 **0%**。
*   **架构差异**：Qwen 模型倾向于填满预算（EOS 率 0%），导致崩溃严重；Phi-3 具有更强的自限性（高 EOS 率），表现更稳健。

### 7. 优点
*   **视角独特**：首次系统性地揭示了函数调用中 CoT 预算的“过犹不及”现象。
*   **机制深入**：不仅观察到现象，还通过错误分解和 EOS 率分析解释了为什么长推理会变差。
*   **实用性强**：提出的 FR-CoT 方法简单易行，无需微调或复杂的约束解码算法，即可提升智能体可靠性。

### 8. 不足与局限
*   **模型覆盖**：未测试专门针对长推理优化的模型（如 OpenAI o1 或 DeepSeek-R1），这些模型可能具有不同的预算效应。
*   **任务复杂度**：实验集中在单步函数选择，对于需要多步推理、工具链调用的复杂 Agent 场景，长推理的必要性可能会重新体现。
*   **样本规模**：200 个任务虽然经过了统计检验，但在 LLM 评测中仍属于中等规模，可能存在特定领域的偏差。

（完）
