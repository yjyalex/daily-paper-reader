---
title: "Not All Tokens See Equally: Perception-Grounded Policy Optimization for Large Vision-Language Models"
title_zh: 并非所有 Token 都能同等程度地“看见”：大语言-视觉模型的感知驱动策略优化
authors: "Zekai Ye, Qiming Li, Xiaocheng Feng, Ruihan Chen, Ziming Li, Haoyu Ren, Kun Chen, Dandan Tu, Bing Qin"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01840v1"
tags: ["query:token"]
score: 9.0
evidence: 多模态推理的Token级策略优化
tldr: "针对大视觉语言模型在强化学习中平均分配奖励导致视觉感知信号稀释的问题，本文提出了感知对齐策略优化（PGPO）。该方法通过量化Token对视觉输入的依赖程度，动态调整Token级别的优势函数，从而增强关键视觉推理步骤的学习信号。在Qwen2.5-VL系列模型上的实验表明，PGPO在多个多模态推理基准上平均提升了18.7%，有效降低了梯度方差并提升了推理的鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的强化学习框架对所有Token分配相同奖励，忽略了不同Token对视觉信息的依赖差异，导致关键视觉推理信号被稀释。
method: 提出PGPO框架，利用KL散度定义Token视觉依赖度，并通过阈值门控机制动态重塑Token级别的优势函数，放大视觉相关Token的梯度。
result: "在七个挑战性多模态推理基准上，PGPO使Qwen2.5-VL系列模型的性能平均提升了18.7%，并显著减少了训练中的梯度方差。"
conclusion: PGPO通过精细化的信用分配机制，有效解决了多模态强化学习中的信号稀释问题，是实现鲁棒感知驱动推理的强力正则化手段。
---

## 摘要
尽管基于可验证奖励的强化学习（RLVR）提升了大语言-视觉模型（LVLMs）的推理能力，但现有框架存在一个基础性的方法论缺陷：通过将相同的优势（advantages）分配给所有生成的 token，这些方法本质上稀释了优化多模态推理中关键视觉驱动步骤所需的学习信号。为了弥补这一差距，我们提出了“Token 视觉依赖度”（Token Visual Dependency），通过计算视觉条件预测分布与纯文本预测分布之间的 Kullback-Leibler (KL) 散度，来量化视觉输入的因果信息增益。研究发现这种依赖度具有高度稀疏性且在语义上至关重要，据此我们引入了感知驱动策略优化（PGPO），这是一种新型的细粒度信用分配框架，能够在 token 级别动态重塑优势。通过一种阈值门控且质量守恒的机制，PGPO 主动放大视觉依赖型 token 的学习信号，同时抑制来自语言先验的梯度噪声。基于 Qwen2.5-VL 系列在七个具有挑战性的多模态推理基准测试上的广泛实验表明，PGPO 使模型性能平均提升了 18.7%。理论和实证分析均证实，PGPO 有效降低了梯度方差，防止了训练崩溃，并作为一种强效正则化器，促进了稳健的、基于感知的多模态推理。

## Abstract
While Reinforcement Learning from Verifiable Rewards (RLVR) has advanced reasoning in Large Vision-Language Models (LVLMs), prevailing frameworks suffer from a foundational methodological flaw: by distributing identical advantages across all generated tokens, these methods inherently dilute the learning signals essential for optimizing the critical, visually-grounded steps of multimodal reasoning. To bridge this gap, we formulate \textit{Token Visual Dependency}, quantifying the causal information gain of visual inputs via the Kullback-Leibler (KL) divergence between visual-conditioned and text-only predictive distributions. Revealing that this dependency is highly sparse and semantically pivotal, we introduce Perception-Grounded Policy Optimization (PGPO), which is a novel fine-grained credit assignment framework that dynamically reshapes advantages at the token level. Through a threshold-gated, mass-conserving mechanism, PGPO actively amplifies learning signals for visually-dependent tokens while suppressing gradient noise from linguistic priors. Extensive experiments based on the Qwen2.5-VL series across seven challenging multimodal reasoning benchmarks demonstrate that PGPO boosts models by 18.7% on average. Both theoretical and empirical analyses confirm that PGPO effectively reduces gradient variance, prevents training collapse, and acts as a potent regularizer for robust, perception-grounded multimodal reasoning. Code will be published on https://github.com/Yzk1114/PGPO.

---

## 论文详细总结（自动生成）

### 论文总结：Not All Tokens See Equally (PGPO)

#### 1. 核心问题与整体含义（研究动机和背景）
在多模态大模型（LVLM）的强化学习（RLVR）过程中，现有的主流框架（如 GRPO）存在一个基础性缺陷：**信用分配不均**。标准方法会将序列级别的奖励（Reward）均匀地分配给生成路径中的每一个 Token。然而，在多模态推理中，并非所有 Token 都依赖视觉信息（例如，“因此”或“我们得到”等连接词主要依赖语言先验，而具体的几何数值或图像特征才真正依赖视觉感知）。这种“大锅饭”式的分配方式稀释了关键视觉推理步骤的学习信号，导致模型难以精准优化感知能力。

#### 2. 方法论：感知驱动策略优化 (PGPO)
PGPO 的核心思想是**量化并利用 Token 对视觉信息的依赖程度来动态调整强化学习的优势函数（Advantage）**。
*   **Token 视觉依赖度 (S)：** 通过计算模型在“有图像输入”和“无图像输入”（通过注意力掩码实现）两种情况下的预测概率分布之间的 **KL 散度**来衡量。散度越高，代表该 Token 越依赖视觉信息。
*   **两阶段转换：** 为了处理 KL 散度的长尾分布，先进行对数压缩（$\log(1+S)$），再进行序列内的 Min-Max 归一化，得到有界的依赖得分 $I_t$。
*   **阈值门控机制：** 引入阈值 $\tau$，将 Token 分为“视觉依赖型”和“噪声型”。对于高于阈值的 Token，放大其优势信号；对于低于阈值的 Token，抑制其梯度贡献。
*   **质量守恒归一化：** 为了保证训练稳定性，对调整后的权重进行归一化，确保整个序列的总优势质量（Mass）保持不变，避免梯度爆炸或消失。

#### 3. 实验设计
*   **基础模型：** Qwen2.5-VL-3B 和 Qwen2.5-VL-7B。
*   **数据集：** 在 ViRL39K（包含数学、物理、化学等 3.9 万个多模态推理问题）上进行训练。
*   **评测基准（7个）：** DynaMath（动态数学）、Geo3k（几何）、MathVerse（视觉数学）、MathVision（竞赛数学）、MMK12（K12教育）、LogicVista（逻辑推理）、MMMU-Pro（大学水平多学科推理）。
*   **对比方法：** 包括 GRPO、DAPO、PAPO、VPPO 等当前最强的多模态强化学习基线，以及 MM-Eureka、VL-Rethinker 等开源推理模型。

#### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA H100 80GB GPU 集群。
*   **训练时长：** 以 Qwen2.5-VL-3B 为例，PGPO 的训练总时长约为 28.1 小时。
*   **开销分析：** 相比于基础的 DAPO 方法，PGPO 因为需要额外的一次前向传播（计算无图像时的分布）增加了约 **10.6%** 的计算开销，但在相同时间预算下，PGPO 的性能显著优于增加训练步数的基线模型。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 3B 到 7B 两种参数规模，并在 7 个垂直推理基准和 3 个通用 VQA 基准（MMBench, MMStar, MME）上进行了验证。
*   **消融实验：** 详细探讨了“仅抑制”、“仅增强”、“无归一化”等变体，验证了各组件的必要性。
*   **超参数分析：** 对阈值 $\tau$ 和增强系数 $\beta$ 进行了敏感性测试。
*   **充分性评价：** 实验设计非常充分且客观。作者不仅提供了准确率对比，还深入分析了梯度方差、策略熵以及训练过程中的视觉依赖度变化，从理论和实证两方面证明了方法的有效性。

#### 6. 主要结论与发现
*   **性能飞跃：** PGPO 在七个基准上平均提升了 **18.7%**，在几何和视觉密集型任务（如 MathVerse）上表现尤为突出。
*   **训练稳定性：** PGPO 有效降低了梯度方差，解决了 DAPO 等方法在训练后期容易出现的“熵爆炸”和训练崩溃问题。
*   **感知增强：** 实验证明，PGPO 确实引导模型在推理过程中更多地关注视觉锚点（Visual Anchors），减少了基于语言先验的幻觉。

#### 7. 优点
*   **理论严谨：** 基于信息论（KL 散度）定义视觉依赖，并提供了关于梯度方差和质量守恒的数学证明。
*   **细粒度优化：** 突破了序列级奖励的局限，实现了 Token 级别的精准信用分配。
*   **即插即用：** 可以轻松集成到 GRPO 或 DAPO 等现有强化学习算法中。
*   **鲁棒性强：** 在提升推理能力的同时，并未损害模型的通用视觉问答能力。

#### 8. 不足与局限
*   **计算成本：** 虽然开销可控（约 10%），但仍需额外的一次前向传播，在大规模部署时会有一定成本增加。
*   **超参数依赖：** 阈值 $\tau$ 和增强系数 $\beta$ 可能需要针对不同的模型架构或任务分布进行微调。
*   **模型规模限制：** 实验目前最高只验证到 7B 规模，在超大规模模型（如 72B 或更大）上的表现尚待验证。

（完）
