---
title: Think Anywhere in Code Generation
title_zh: 代码生成中的随处思考
authors: "Xue Jiang, Tianyu Zhang, Ge Li, Mengyang Liu, Taozhi Chen, Zhenhua Xu, Binhua Li, Wenpin Jiao, Zhi Jin, Yongbin Li, Yihong Dong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29957v2"
tags: ["query:token"]
score: 9.0
evidence: 利用强化学习奖励在任意Token位置按需推理
tldr: 针对代码生成中预先推理无法应对实现过程中动态复杂性的问题，本文提出 Think-Anywhere 机制，允许模型在生成代码的任意位置按需触发推理。通过冷启动训练模仿推理模式，并结合结果导向的强化学习驱动模型自主探索推理时机。实验表明，该方法在 LeetCode 等多个基准测试中达到 SOTA 水平，并能自适应地在复杂位置进行推理，显著提升了性能与可解释性。
selection_source: fresh_fetch
motivation: 传统的预先推理模式难以应对代码实现过程中动态出现的复杂性，且无法根据生成过程中的难度差异灵活分配计算资源。
method: 提出 Think-Anywhere 机制，通过冷启动训练和结果导向的强化学习，使模型学会在代码生成的任何 Token 位置自主触发推理。
result: 在 LeetCode、LiveCodeBench 等四个主流基准测试中均取得 SOTA 性能，并展现出跨模型的泛化能力和更强的可解释性。
conclusion: Think-Anywhere 证明了在生成过程中按需推理比单一的预先推理更有效，为提升大模型在复杂编程任务中的推理能力提供了新路径。
---

## 摘要
推理大语言模型（LLM）的最新进展主要依赖于预先思考（upfront thinking），即在输出最终答案之前进行推理。然而，这种方法在代码生成任务中存在严重的局限性，因为问题的完整复杂性往往只有在代码实现过程中才会显现，导致预先思考往往不足。此外，它无法在难度差异显著的代码生成过程中自适应地分配推理资源。在本文中，我们提出了 Think-Anywhere，这是一种新型推理机制，使 LLM 能够在代码生成过程中的任何 Token 位置按需调用推理。我们通过以下方式实现 Think-Anywhere：首先通过冷启动训练引导 LLM 模仿推理模式，然后利用基于结果的强化学习（RL）奖励驱动模型自主探索调用推理的时机和位置。在四个主流代码生成基准测试（即 LeetCode、LiveCodeBench、HumanEval 和 MBPP）上的广泛实验表明，Think-Anywhere 的性能优于现有的推理方法和最近的后训练方法，达到了当前最先进的水平，并在多种 LLM 上展现出一致的泛化能力。我们的分析进一步揭示，Think-Anywhere 使模型能够在高熵位置自适应地调用推理，从而增强了可解释性。

## Abstract
Recent advances in reasoning Large Language Models (LLMs) have primarily relied on upfront thinking, where reasoning occurs before final answer. However, this approach suffers from critical limitations in code generation, where upfront thinking is often insufficient as problems' full complexity only reveals itself during code implementation. Moreover, it cannot adaptively allocate reasoning effort throughout the code generation process where difficulty varies significantly. In this paper, we propose Think-Anywhere, a novel reasoning mechanism that enables LLMs to invoke thinking on-demand at any token position during code generation. We achieve Think-Anywhere by first teaching LLMs to imitate the reasoning patterns through cold-start training, then leveraging outcome-based RL rewards to drive the model's autonomous exploration of when and where to invoke reasoning. Extensive experiments on four mainstream code generation benchmarks (i.e., LeetCode, LiveCodeBench, HumanEval, and MBPP) show that Think-Anywhere achieves state-of-the-art performance over both existing reasoning methods and recent post-training approaches, while demonstrating consistent generalization across diverse LLMs. Our analysis further reveals that Think-Anywhere enables the model to adaptively invoke reasoning at high-entropy positions, providing enhanced interpretability.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Think-Anywhere**，一种针对代码生成任务的新型推理机制。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前推理大模型（如 OpenAI o1, DeepSeek-R1）主要采用“预先推理”（Upfront Thinking）模式，即在生成最终答案前进行一段长推理。
*   **研究动机**：
    *   **实现过程中的复杂性**：代码生成的难点往往在编写过程中才会显现（如边界条件、索引处理），预先规划难以覆盖所有实现细节。
    *   **计算资源分配不均**：代码的不同部分难度不同，固定长度的预先推理无法在真正困难的代码段（如复杂算法逻辑）灵活增加思考量，而在简单模板代码上又浪费了计算。
    *   **人类认知匹配**：人类程序员不仅在写代码前思考，在编写过程中遇到困难也会停下来思考。

### 2. 方法论
Think-Anywhere 允许模型在代码生成的**任何 Token 位置**按需触发推理块。
*   **核心思想**：将推理过程与代码生成深度耦合，通过 `<thinkanywhere>` 标签在代码中间插入推理。
*   **关键技术流程**：
    1.  **冷启动训练（Cold-start）**：利用强推理模型（如 Gemini 1.5 Flash）生成包含中间推理过程的合成数据（约 5000 条），通过 LoRA 微调让模型学会这种“边写边想”的格式规范。
    2.  **专用触发 Token（可选）**：设计了语义感知的特殊 Token 初始化策略，将 `<thinkanywhere>` 映射为单一 Token 以提高触发效率。
    3.  **强化学习（RLVR）**：采用 **GRPO（群组相对策略优化）** 算法。
        *   **奖励函数**：由结构奖励（检查是否符合思考格式）和正确性奖励（通过编译器测试用例）组成。
        *   **自主探索**：通过 RL 驱动模型自主决定在哪些 Token 位置、花费多少长度进行推理，以最大化代码通过率。

### 3. 实验设计
*   **数据集/基准测试**：
    *   代码生成：**HumanEval**、**MBPP**、**LeetCode**、**LiveCodeBench**。
    *   跨领域泛化（数学）：**AIME 2024/2025**、**HMMT 2025**。
*   **对比方法**：
    *   基础模型：Qwen2.5-Coder (1.5B/7B), LLaMA-3.1-8B。
    *   推理增强方法：CoT（思维链）、Self-planning、Interleaved Thinking、标准 GRPO（预先推理）。
    *   后训练方法：OlympicCoder、CodePRM、CodeRL+ 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 (40G) GPU**。
*   **训练配置**：Batch size 为 128，训练 2 个 Epoch，每个问题生成 8 个 Rollout 样本，最大长度 4096 Token。
*   **数据规模**：RL 阶段使用了 Skywork 数据集中的 14K 个编程问题。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 4 个主流代码榜单、3 个数学竞赛榜单，并针对 3 种不同架构和规模的模型进行了验证。
*   **消融实验**：非常充分。对比了“仅冷启动”、“仅 RL”、“行级推理（而非 Token 级）”、“无预先推理”以及“填充推理（验证推理内容有效性）”等多种变体。
*   **客观性**：采用了 Greedy Sampling 确保结果可复现，并进行了 Pass@k 分析以展示模型能力的上限提升。

### 6. 主要结论与发现
*   **性能领先**：Think-Anywhere 在所有代码榜单上均达到 SOTA，平均性能比基础模型提升 9.3%，优于传统的预先推理（GRPO）。
*   **按需推理的特征**：模型倾向于在**高熵（不确定性高）**的位置触发推理，如赋值语句（涉及复杂计算）和返回语句（最后确认）。
*   **效率更高**：相比于长篇大论的预先推理，Think-Anywhere 虽然在中间增加了推理块，但大幅缩短了开头的预先推理长度，总 Token 消耗反而更少。
*   **跨领域泛化**：虽然只在代码数据上训练，但在数学竞赛题目（AIME）上也表现出显著的性能提升。

### 7. 优点
*   **机制创新**：打破了“先想后写”的僵化模式，实现了更符合人类直觉的“边写边想”。
*   **自适应计算**：模型学会了在简单部分快速通过，在困难部分投入更多推理资源。
*   **可解释性强**：通过观察模型在何处停顿思考，可以直观地发现代码逻辑中的潜在难点。
*   **通用性**：证明了该方法在不同规模（1.5B 到 8B）和不同家族的模型上均有效。

### 8. 不足与局限
*   **训练数据依赖**：冷启动阶段仍依赖于强模型的合成数据，这可能引入教师模型的偏差。
*   **推理延迟**：虽然总 Token 数减少，但在流式输出时，中间插入推理块可能会影响用户阅读代码的连贯性（尽管可以前端隐藏）。
*   **预训练缺失**：目前仅在后训练（Post-training）阶段引入，如果能在预训练阶段就加入这种“随处思考”的语料，效果可能更强。

（完）
