---
title: "SKILL0: In-Context Agentic Reinforcement Learning for Skill Internalization"
title_zh: SKILL0：用于技能内化的上下文智能体强化学习
authors: "Zhengxi Lu, Zhiyuan Yao, Jinyang Wu, Chengcheng Han, Qi Gu, Xunliang Cai, Weiming Lu, Jun Xiao, Yueting Zhuang, Yongliang Shen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02268v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于将技能内化到模型参数中的上下文智能体强化学习
tldr: 本研究针对LLM智能体在推理时依赖外部技能检索导致的噪声和高开销问题，提出了SKILL0框架。该框架采用一种“技能内化”的在环强化学习方法，通过训练时的动态课程设计，将原本需要检索的技能知识逐步撤出并内化至模型参数中。SKILL0不仅显著降低了推理时的Token负担，还使模型具备了零样本自主执行复杂任务的能力，在多个基准测试中表现优于传统强化学习方法。
selection_source: fresh_fetch
motivation: 旨在解决推理时技能增强带来的检索噪声、Token冗余以及模型无法真正掌握技能知识的局限性。
method: 引入一种动态课程强化学习机制，在训练过程中从全上下文逐步过渡到零上下文，促使模型内化技能。
result: "实验显示SKILL0在ALFWorld和Search-QA上分别提升了9.7%和6.6%的成功率，且推理效率极高。"
conclusion: 通过逐步撤除上下文的强化学习，可以成功将外部技能知识转化为模型的内在能力，实现高效的零样本智能体。
---

## 摘要
智能体技能是程序性知识和可执行资源的结构化包，智能体在推理时动态加载这些资源，已成为增强大语言模型（LLM）智能体的一种可靠机制。然而，推理时的技能增强存在根本性的局限：检索噪声会引入无关的引导，注入的技能内容会带来巨大的 Token 开销，且模型从未真正习得它仅仅是在遵循的知识。我们探究技能是否可以被内化到模型参数中，从而在无需任何运行时技能检索的情况下实现零样本自主行为。我们提出了 SKILL0，这是一个专为技能内化设计的上下文强化学习框架。SKILL0 引入了一种训练阶段的课程学习方案，从完整的技能上下文开始并逐步撤出。技能在离线状态下按类别分组，并与交互历史一起渲染成紧凑的可视化上下文，教导模型进行工具调用和多轮任务完成。随后，动态课程评估每个技能文件的同策略帮助程度，在随线性衰减的预算内，仅保留当前策略仍能从中获益的技能，直到智能体在完全零样本的设置下运行。广泛的智能体实验表明，SKILL0 相比标准强化学习基线取得了显著提升（ALFWorld 提升 9.7%，Search-QA 提升 6.6%），同时保持了每步少于 0.5k Token 的高效上下文。我们的代码可在 https://github.com/ZJU-REAL/SkillZero 获取。

## Abstract
Agent skills, structured packages of procedural knowledge and executable resources that agents dynamically load at inference time, have become a reliable mechanism for augmenting LLM agents. Yet inference-time skill augmentation is fundamentally limited: retrieval noise introduces irrelevant guidance, injected skill content imposes substantial token overhead, and the model never truly acquires the knowledge it merely follows. We ask whether skills can instead be internalized into model parameters, enabling zero-shot autonomous behavior without any runtime skill retrieval. We introduce SKILL0, an in-context reinforcement learning framework designed for skill internalization. SKILL0 introduces a training-time curriculum that begins with full skill context and progressively withdraws it. Skills are grouped offline by category and rendered with interaction history into a compact visual context, teaching he model tool invocation and multi-turn task completion. A Dynamic Curriculum then evaluates each skill file's on-policy helpfulness, retaining only those from which the current policy still benefits within a linearly decaying budget, until the agent operates in a fully zero-shot setting. Extensive agentic experiments demonstrate that SKILL0 achieves substantial improvements over the standard RL baseline (+9.7\% for ALFWorld and +6.6\% for Search-QA), while maintaining a highly efficient context of fewer than 0.5k tokens per step. Our code is available at https://github.com/ZJU-REAL/SkillZero.

---

## 论文详细总结（自动生成）

这是一份关于论文《SKILL0: In-Context Agentic Reinforcement Learning for Skill Internalization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 LLM 智能体主要依赖“推理时技能增强”（Inference-time Skill Augmentation），即通过检索并将技能描述注入 Prompt 来引导智能体。这种方式存在三大缺陷：
    1.  **检索噪声**：引入无关引导干扰决策。
    2.  **Token 开销**：长上下文导致推理成本高、速度慢。
    3.  **缺乏真正习得**：模型只是在“遵循”指令而非“掌握”技能，能力停留在上下文而非参数中。
*   **研究动机**：探索是否能通过强化学习（RL）将外部技能“内化”到模型参数中，使智能体在推理时无需检索即可实现零样本（Zero-shot）的自主行为。

### 2. 方法论
SKILL0 提出了**上下文智能体强化学习（In-Context Agentic RL）**框架，核心思想是“训练时有技能，推理时零技能”。
*   **上下文渲染与压缩**：将文本交互历史和检索到的技能渲染为紧凑的视觉上下文（RGB 图像），通过视觉编码器大幅降低 Token 消耗。模型在每一步会自发生成压缩率 $c_t$。
*   **在环强化学习（ICRL）**：在训练的 Rollout 阶段提供技能引导，但在推理阶段完全移除。通过复合奖励函数（任务成功率 + 压缩效率）优化模型。
*   **动态课程学习（Dynamic Curriculum）**：
    *   **技能分组**：离线将技能按类别分组，并关联特定的验证子任务。
    *   **帮助度评估**：定义指标 $\Delta_k$（有技能时的准确率 - 无技能时的准确率）。
    *   **线性衰减预算**：设置一个随训练阶段线性减少的技能预算 $M(s)$。在每个阶段，仅保留 $\Delta_k > 0$ 且排名靠前的技能，直到预算归零，迫使模型将知识从上下文转移到参数中。

### 3. 实验设计
*   **数据集/场景**：
    1.  **ALFWorld**：具身智能文本游戏，包含 6 类家庭任务（如取放、清洁、加热等）。
    2.  **Search-based QA**：搜索增强问答，涵盖单跳（NQ, TriviaQA, PopQA）和多跳（HotpotQA, 2Wiki, MuSiQue, Bamboogle）数据集。
*   **对比方法**：
    *   **基线**：Zero-Shot, Few-Shot。
    *   **RL 方法**：GRPO, AgentOCR, EvolveR。
    *   **技能增强方法**：SkillRL（推理时需检索技能）。
    *   **其他**：ReAct, Reflexion, 以及多种记忆增强方法（Mem0, ExpeL 等）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 H800 GPU**。
*   **模型**：基于 Qwen2.5-VL 系列（3B 和 7B 版本）。
*   **训练细节**：训练步数最多为 180 步。ALFWorld 每批次采样 16 个任务，Search-QA 每批次采样 128 个任务。

### 5. 实验数量与充分性
*   **实验规模**：在两个大类基准（具身智能和复杂问答）下进行了广泛测试，涵盖了从 3B 到 7B 的模型规模。
*   **消融实验**：非常充分。作者针对技能预算 $M$ 的设置、动态课程的三个步骤（过滤、排序、选择）、以及验证间隔 $d$ 进行了详细的对比实验。
*   **客观性**：通过监控训练过程中的“帮助度”曲线，客观展示了技能从“有用”到“被内化后变得多余”的演变过程，证明了方法的有效性。

### 6. 主要结论与发现
*   **性能超越基线**：SKILL0 在 ALFWorld 上比 AgentOCR 提升了 **9.7%**，在 Search-QA 上提升了 **6.6%**。
*   **内化成功**：实验证明，经过 SKILL0 训练的模型在推理时不使用任何技能 Prompt，其表现甚至优于那些在推理时使用完整技能描述的传统模型。
*   **极高的 Token 效率**：由于技能被内化且使用了视觉压缩，推理时每步消耗的 Token 少于 **0.5k**，比 SkillRL 等方法降低了 5 倍以上。

### 7. 优点
*   **范式创新**：首次明确提出“技能内化”作为 RL 训练目标，解决了智能体对外部 Prompt 的过度依赖。
*   **自适应性强**：动态课程机制能根据模型当前的掌握程度自动调整训练难度，避免了硬编码进度表带来的不稳定。
*   **实用性高**：大幅降低了生产环境下智能体运行的 Token 成本和延迟。

### 8. 不足与局限
*   **依赖初始技能库**：SKILL0 的效果高度依赖于初始 SkillBank 的质量，如果初始技能本身有误，内化过程可能会受到影响。
*   **离线分组开销**：在应用到全新领域时，需要人工或离线脚本对技能和验证子任务进行关联分组，存在一定的迁移成本。
*   **视觉编码器限制**：虽然视觉渲染节省了 Token，但对于极长或极其复杂的文本信息，视觉编码的精度可能成为瓶颈。

（完）
