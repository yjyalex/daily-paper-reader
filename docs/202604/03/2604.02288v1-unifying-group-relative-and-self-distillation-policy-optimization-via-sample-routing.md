---
title: Unifying Group-Relative and Self-Distillation Policy Optimization via Sample Routing
title_zh: 通过样本路由统一组相对与自蒸馏策略优化
authors: "Gengsheng Li, Tianyu Yang, Junfeng Fang, Mingyang Song, Mao Zheng, Haiyun Guo, Dan Zhang, Jinqiao Wang, Tat-Seng Chua"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02288v1"
tags: ["query:token"]
score: 10.0
evidence: 策略优化中的 Token 级关注及 GRPO 的局限性
tldr: 针对大语言模型强化学习中 GRPO 缺乏细粒度反馈和 SDPO 训练后期不稳定的问题，本文提出 SRPO 框架。该方法通过样本路由机制，将正确样本分配给 GRPO 进行奖励对齐，将失败样本分配给 SDPO 进行逻辑层纠错，并引入熵感知动态权重抑制不可靠信号。实验表明，SRPO 结合了两者的优势，在显著提升模型性能的同时降低了计算成本。
selection_source: fresh_fetch
motivation: GRPO 的粗粒度信用分配缺乏 token 级关注，而 SDPO 在训练后期因对正确样本的歧义优化和信号退化而导致性能崩溃。
method: 提出 SRPO 框架，将正确样本路由至 GRPO 进行强化，将失败样本路由至 SDPO 进行纠错，并结合熵感知动态权重优化蒸馏信号。
result: "SRPO 在 Qwen3-8B 上的平均性能比 GRPO 提升 3.4%，比 SDPO 提升 6.3%，且每步计算成本降低了 17.2%。"
conclusion: SRPO 成功统一了组相对优化与自蒸馏策略，实现了快速收敛与长期训练稳定性的平衡，是更高效的大模型后训练方案。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为大语言模型后训练的标准范式。虽然组相对策略优化（GRPO）被广泛采用，但其粗粒度的信用分配会统一惩罚失败的生成结果，缺乏有效解决特定偏差所需的 Token 级关注。自蒸馏策略优化（SDPO）通过提供更密集、更有针对性的 Logit 级监督来解决这一问题，从而促进早期快速提升，但在长时间训练中经常发生崩溃。我们将这种后期不稳定性归因于两个内在缺陷：对已经正确的样本进行自蒸馏会引入优化歧义，且自教师信号的可靠性会逐渐退化。为解决这些问题，我们提出了样本路由策略优化（SRPO），这是一个统一的在线策略框架，它将正确样本路由至 GRPO 的奖励对齐强化，将失败样本路由至 SDPO 的针对性 Logit 级纠错。SRPO 进一步结合了熵感知动态加权机制，以抑制高熵、不可靠的蒸馏目标，同时强调置信度高的目标。在五个基准测试和两个模型规模上的评估表明，SRPO 既实现了 SDPO 的早期快速提升，又具备 GRPO 的长期稳定性。它持续超越了两个基准方法的峰值性能，在 Qwen3-8B 上的五个基准平均得分比 GRPO 提高了 3.4%，比 SDPO 提高了 6.3%，同时保持了适中的回复长度，并将每步计算成本降低了高达 17.2%。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a standard paradigm for post-training large language models. While Group Relative Policy Optimization (GRPO) is widely adopted, its coarse credit assignment uniformly penalizes failed rollouts, lacking the token-level focus needed to efficiently address specific deviations. Self-Distillation Policy Optimization (SDPO) addresses this by providing denser, more targeted logit-level supervision that facilitates rapid early improvement, yet it frequently collapses during prolonged training. We trace this late-stage instability to two intrinsic flaws: self-distillation on already-correct samples introduces optimization ambiguity, and the self-teacher's signal reliability progressively degrades. To resolve these issues, we propose Sample-Routed Policy Optimization (SRPO), a unified on-policy framework that routes correct samples to GRPO's reward-aligned reinforcement and failed samples to SDPO's targeted logit-level correction. SRPO further incorporates an entropy-aware dynamic weighting mechanism to suppress high-entropy, unreliable distillation targets while emphasizing confident ones. Evaluated across five benchmarks and two model scales, SRPO achieves both the rapid early improvement of SDPO and the long-horizon stability of GRPO. It consistently surpasses the peak performance of both baselines, raising the five-benchmark average on Qwen3-8B by 3.4% over GRPO and 6.3% over SDPO, while simultaneously yielding moderate response lengths and lowering per-step compute cost by up to 17.2%.

---

## 论文详细总结（自动生成）

以下是对论文《Unifying Group-Relative and Self-Distillation Policy Optimization via Sample Routing》的结构化总结：

### 1. 核心问题与研究动机
论文聚焦于大语言模型（LLM）后训练中的**可验证奖励强化学习（RLVR）**。
*   **研究背景**：目前主流方法如 **GRPO**（组相对策略优化）虽然稳定，但其信用分配过于粗糙（整个序列共享一个奖励标量），导致学习效率低；而 **SDPO**（自蒸馏策略优化）通过 Logit 级的密集监督虽能加速早期提升，但在长期训练中极易发生性能崩溃。
*   **核心问题**：论文诊断出 SDPO 不稳定的两个内在原因：
    1.  **优化歧义**：对已经正确的样本进行自蒸馏会强迫模型在等效的正确路径间做无意义选择。
    2.  **信号退化**：随着训练推进，自教师（Self-teacher）的预测熵增高，提供的蒸馏信号变得不可靠。

### 2. 方法论：SRPO 框架
论文提出了 **SRPO（样本路由策略优化）**，旨在统一奖励驱动的强化学习和 Logit 驱动的自蒸馏。
*   **核心思想（样本路由）**：根据样本的正确性进行分流。
    *   **正确样本**：路由至 **GRPO 分支**，利用序列级奖励进行强化，确保奖励对齐。
    *   **失败样本**：若存在可参考的正确兄弟样本（Teacher info），则路由至 **SDPO 分支**，进行 Token 级的密集纠错。
*   **关键技术：熵感知动态加权（DW-SDPO）**：
    *   在 SDPO 分支中，根据教师分布的熵 $H$ 计算权重 $w \propto \exp(-\beta H)$。
    *   **作用**：自动抑制高熵（不确定）的蒸馏目标，强调置信度高的纠错信号，从而解决后期信号退化问题。
*   **算法流程**：每轮采样一组 Rollouts，检查正确性；正确者计算 GRPO 损失，失败者计算带权重的 SDPO 损失，最后统一更新策略参数。

### 3. 实验设计
*   **模型规模**：使用了 Qwen3-4B 和 Qwen3-8B 两个规模的指令微调模型。
*   **数据集/场景**：涵盖 5 个基准测试：
    *   **科学推理**：化学（Chemistry）、物理（Physics）、生物（Biology）、材料（Materials），源自 SciKnowEval。
    *   **工具调用**：Tool Use，源自 ToolAlpaca。
*   **对比方法（Baseline）**：
    *   **GRPO**：强化版的组相对策略优化。
    *   **SDPO**：纯自蒸馏策略优化。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU**（单节点，共 768GB 显存）。
*   **训练时长**：实验设定了 1h、5h、10h 三个时间窗口来观察训练动态。
*   **软件框架**：基于 `verl` 库实现，推理后端使用 `SGLang`。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模和 5 个不同领域的数据集上进行了完整测试，实验覆盖面较广。
*   **消融实验**：
    *   对比了“样本路由”与“优势函数混合（Advantage Mix）”的效果，证明路由机制更稳健。
    *   验证了“动态加权”对后期性能提升的贡献。
*   **充分性评价**：实验设计较为客观，通过 wall-clock time（墙钟时间）对比性能，考虑了计算效率，且提供了详细的训练曲线和路由统计数据，论证充分。

### 6. 主要结论与发现
*   **性能卓越**：SRPO 在所有基准上均优于 GRPO 和 SDPO。在 Qwen3-8B 上，5 项平均准确率比 GRPO 高 3.4%，比 SDPO 高 6.3%。
*   **稳定性强**：SRPO 成功克服了 SDPO 的后期崩溃问题，实现了类似 GRPO 的长期单调提升。
*   **效率提升**：由于后期失败样本减少，SDPO 分支调用频率降低，SRPO 的每步计算成本比 GRPO 降低了高达 **17.2%**。
*   **回复长度适中**：SRPO 避免了 GRPO 的冗长和 SDPO 的过度简短，在推理成本和思维完整性间取得了平衡。

### 7. 优点与亮点
*   **设计优雅**：通过简单的路由逻辑解决了复杂的优化冲突问题，无需复杂的超参数调优。
*   **动态自适应**：训练初期侧重纠错（SDPO），后期侧重强化（GRPO），这种演变是自动发生的。
*   **计算友好**：不仅提升了精度，还通过减少不必要的 Logit 计算和控制序列长度降低了实际算力消耗。

### 8. 不足与局限
*   **依赖兄弟样本**：SDPO 分支需要同一 Prompt 下至少有一个正确样本作为教师。如果模型对某个问题完全无法生成正确答案，该框架将退化为纯 GRPO。
*   **环境反馈利用不足**：目前主要利用兄弟样本作为教师信息，尚未充分探索如何整合更丰富的环境反馈（如代码执行报错信息）。
*   **任务局限**：主要针对具有“可验证奖励”（Verifiable Rewards）的任务，对于主观性较强的开放式生成任务，其适用性有待验证。

（完）
