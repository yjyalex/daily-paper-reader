---
title: "DPEPO: Diverse Parallel Exploration Policy Optimization for LLM-based Agents"
authors: "Junshuo Zhang, Chengrui Huang, Feng Guo, Zihan Li, Ke Shi, Menghua Jiang, Jiguo Yu, Shuo Shang, Shen Gao"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24320v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于并行推理和动作生成的强化学习算法
tldr: 针对LLM智能体在顺序推理-行动范式中探索受限的问题，本文提出DPEPO算法。该算法引入了多环境并行交互范式，允许智能体同时探索并共享跨轨迹经验。通过SFT赋予基础并行能力，并结合分层强化学习奖励机制（包括轨迹级成功奖励及步级多样性奖励），有效减少行为冗余并促进广泛探索。在ALFWorld和ScienceWorld实验中，DPEPO取得了SOTA性能。
selection_source: fresh_fetch
motivation: 传统的顺序推理-行动范式因单步仅能与单一环境交互，导致智能体探索能力受限且对环境理解不全。
method: 提出一种并行探索范式，通过SFT和包含多样性动作及状态转移奖励的分层强化学习，鼓励智能体进行非冗余的并行探索。
result: 在ALFWorld和ScienceWorld基准测试中，DPEPO在保持高效的同时，成功率达到了当前领先水平。
conclusion: 并行探索与多样性奖励机制能显著提升LLM智能体在复杂任务中的环境感知与任务完成能力。
---

## Abstract
Large language model (LLM) agents that follow the sequential "reason-then-act" paradigm have achieved superior performance in many complex tasks.However, these methods suffer from limited exploration and incomplete environmental understanding, as they interact with only a single environment per step. In this paper, we first introduce a novel paradigm that enables an agent to interact with multiple environments simultaneously and share cross-trajectory experiences. Building upon this paradigm, we further propose DPEPO, a reinforcement learning (RL) algorithm that encourages the agent to perform diverse parallel exploration. There are two stages in DPEPO: initial supervised fine-tuning (SFT) imparts basic parallel reasoning and action generation, followed by reinforcement learning stage with a hierarchical reward scheme. We design a parallel trajectory-level success reward and two step-level rewards: Diverse Action Reward and Diverse State Transition Reward, which actively penalize behavioral redundancy and promote broad exploration. Extensive experiments on ALFWorld and ScienceWorld show that DPEPO achieves state-of-the-art (SOTA) success rates, while maintaining comparable efficiency to strong sequential baselines. (Code is available at https://github.com/LePanda026/Code-for-DPEPO)

---

## 论文详细总结（自动生成）

以下是对论文《DPEPO: Diverse Parallel Exploration Policy Optimization for LLM-based Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的基于大语言模型（LLM）的智能体通常采用 **ReAct（Reason-then-Act）** 范式，即顺序地进行“推理-行动-观察”。这种线性交互方式限制了智能体对环境的探索广度，导致其对环境的认知不完整且存在偏差，在复杂长程任务中容易陷入局部最优或失败。
*   **研究动机**：虽然可以通过多次独立采样（Multi-sampling）来增加探索，但这种方式存在**缺乏多样性**（动作容易趋同）和**效率低下**（轨迹间经验不共享、Token消耗大）的问题。因此，论文旨在提出一种能让智能体同时与多个环境交互并共享经验的并行探索范式。

### 2. 方法论
DPEPO 提出了一种**多样化并行探索策略优化**框架，核心思想是让智能体在一个步骤内生成多个并行动作，并根据反馈进行多样化学习。
*   **核心思想**：将智能体从“单线交互”转变为“多线并行交互”，通过强化学习鼓励探索的差异化。
*   **关键技术细节**：
    1.  **冷启动阶段（SFT）**：通过少量人工标注和模型合成的并行探索轨迹进行微调，使模型掌握并行推理和特定格式（如 `<parallel>` 标签）的输出能力。
    2.  **强化学习阶段（RL）**：基于 **GRPO**（群组相对策略优化）算法，无需 Critic 模型，通过群组采样估计优势。
    3.  **分层奖励机制**：
        *   **并行轨迹级成功奖励**：只要并行环境中的任何一条路径完成任务，即给予奖励。
        *   **并行步级多样性奖励**：
            *   **多样化动作奖励（DAR）**：惩罚在单一环境内（深度）或跨环境间（宽度）的重复动作。
            *   **多样化状态转移奖励（DSTR）**：惩罚重复的“状态-动作”转换对，强制智能体尝试新的交互路径。
    4.  **策略优化**：结合轨迹级和步级的优势信号，引导模型在保证任务完成的同时，最大化探索的覆盖面。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：文本交互式家庭任务环境（如“把铅笔放桌上”），包含 Seen 和 Unseen 任务。
    *   **ScienceWorld**：复杂的科学实验模拟环境（如电路、生命周期），分为 L0（已知）、L1（未知变体）、L2（完全未知）三个难度等级。
*   **对比方法（Baselines）**：
    *   **闭源模型**：GPT-4o, DeepSeek-V3, DeepSeek-R1。
    *   **强化学习方法**：GRPO, GiGPO（步级优势估计）, RLVMR（元推理奖励）, SPEAR（自模仿探索）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen2.5-1.5B/7B-Instruct 以及 Qwen3 系列模型。
*   **算力配置**：
    *   **ALFWorld**：使用 4 张 **NVIDIA L40** GPU。
    *   **ScienceWorld**：使用 4 张 **NVIDIA H20** GPU。
*   **训练时长**：DPEPO 在 Qwen2.5-1.5B 上仅需约 **24 小时**（含 SFT）即可达到顶尖性能，而对比方法 GiGPO 需要约 96 小时。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的实验，包括两个大型基准测试的所有任务拆分。
*   **充分性与客观性**：
    *   **消融实验**：分别验证了 DAR 和 DSTR 奖励的有效性。
    *   **扩展性分析**：测试了不同模型参数规模（0.5B 到 8B）下的性能表现。
    *   **环境数量分析**：探讨了并行环境数量 $K$ 对成功率和 Token 消耗的影响。
    *   **效率对比**：不仅对比了成功率，还详细统计了推理时间、Token 预算和轨迹长度。实验设计全面，对比基准涵盖了当前最先进的开源和闭源模型，结果较为客观。

### 6. 主要结论与发现
*   **性能领先**：DPEPO 在 ALFWorld（98.2%）和 ScienceWorld（61.4%）上均取得了 **SOTA** 成功率，显著超过了 DeepSeek-R1 等强力基线。
*   **高效探索**：并行探索不仅提高了成功率，还缩短了完成任务所需的平均步数。
*   **Token 效率**：虽然单步 Token 增加，但由于任务完成更快且无需多次重复实验，总 Token 消耗和推理时间与优秀的顺序基线相当，甚至更优。
*   **涌现行为**：智能体学会了在早期阶段进行广泛并行探索以建立环境认知，在后期阶段转为聚焦探索以完成任务。

### 7. 优点
*   **范式创新**：打破了传统的单线 ReAct 模式，提出了更符合人类“多路尝试”逻辑的并行探索范式。
*   **奖励设计精巧**：通过 DAR 和 DSTR 有效解决了强化学习中常见的行为冗余和探索不足问题。
*   **训练高效**：相比于传统的 PPO 或其他复杂的 RL 方案，DPEPO 展现了极高的样本效率和更短的训练周期。

### 8. 不足与局限
*   **环境依赖性**：该方法依赖于能够被多次实例化或并行运行的模拟环境。在**物理机器人**等难以构建并行现实环境的场景中，应用受到限制。
*   **偏见风险**：论文提到，并行探索可能会让智能体更快地接触并学习到环境中的潜在偏见。
*   **复杂性**：虽然推理时间有优势，但维护多个并行环境的上下文对 LLM 的上下文窗口长度提出了更高要求。

（完）
