---
title: "Rewarding the Scientific Process: Process-Level Reward Modeling for Agentic Data Analysis"
title_zh: 奖励科学过程：面向智能体数据分析的过程级奖励建模
authors: "Zhisong Qiu, Shuofei Qiao, Kewei Xu, Yuqi Zhu, Lun Du, Ningyu Zhang, Huajun Chen"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24198v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于推理能力的进程级奖励建模
tldr: 本研究针对数据分析智能体在动态环境中的推理挑战，提出了DataPRM。传统过程奖励模型（PRM）在处理数据分析时难以识别无报错的逻辑错误，且常误判必要的试错探索。DataPRM通过环境感知的主动验证机制和三元奖励策略，能够有效探测静默错误并区分可纠正的探索行为。实验证明，仅4B参数的DataPRM在多个基准测试中显著提升了模型性能，验证了过程级监督在复杂数据分析任务中的优越性。
selection_source: fresh_fetch
motivation: 通用过程奖励模型在动态数据分析任务中难以检测无报错的逻辑漏洞，且无法正确区分必要的试错探索与不可恢复的错误。
method: 提出DataPRM模型，通过与环境交互主动验证中间执行状态，并采用三元奖励策略来区分正确、可纠正的探索及不可恢复的错误。
result: "DataPRM在ScienceAgentBench和DABStep上分别实现了7.21%和11.28%的性能提升，并在强化学习实验中显著优于基于结果奖励的基准模型。"
conclusion: 环境感知和细粒度的过程级奖励建模是提升数据分析智能体推理能力的关键，能有效解决动态任务中的逻辑验证与探索评估难题。
---

## 摘要
过程奖励模型 (PRMs) 在增强大语言模型 (LLMs) 在数学等静态领域的推理能力方面取得了显著成功。然而，它们在动态数据分析任务中的潜力仍有待探索。在这项工作中，我们首先进行了一项实证研究，揭示了通用领域 PRMs 在监督数据分析智能体方面面临的挑战。具体而言，它们无法检测到静默错误（即在不触发解释器异常的情况下产生错误结果的逻辑缺陷），并会错误地惩罚探索性动作，将必要的试错探索误认为接地失败 (grounding failures)。为了弥补这一差距，我们引入了 DataPRM，这是一种新型的环境感知生成式过程奖励模型，它 (1) 可以作为主动验证器，通过与环境自主交互来探测中间执行状态并发现静默错误，(2) 采用了一种反思感知的三元奖励策略，用以区分可纠正的接地错误和不可恢复的错误。我们设计了一个可扩展的流水线，通过多样性驱动的轨迹生成和知识增强的步骤级标注，为 DataPRM 构建了超过 8000 个高质量训练实例。实验结果表明，在使用 Best-of-N 推理时，DataPRM 在 ScienceAgentBench 和 DABStep 上分别将下游策略 LLMs 的性能提升了 7.21% 和 11.28%。值得注意的是，仅拥有 4B 参数的 DataPRM 表现优于强基准模型，并在多种测试时缩放 (Test-Time Scaling) 策略中展现出稳健的泛化能力。此外，将 DataPRM 集成到强化学习中，相比结果奖励 (outcome-reward) 基准取得了显著收益，在 DABench 上达到 78.73%，在 TableBench 上达到 64.84%，验证了过程奖励监督的有效性。代码开源于 https://github.com/zjunlp/DataMind。

## Abstract
Process Reward Models (PRMs) have achieved remarkable success in augmenting the reasoning capabilities of Large Language Models (LLMs) within static domains such as mathematics. However, their potential in dynamic data analysis tasks remains underexplored. In this work, we first present a empirical study revealing that general-domain PRMs struggle to supervise data analysis agents. Specifically, they fail to detect silent errors, logical flaws that yield incorrect results without triggering interpreter exceptions, and erroneously penalize exploratory actions, mistaking necessary trial-and-error exploration for grounding failures. To bridge this gap, we introduce DataPRM, a novel environment-aware generative process reward model that (1) can serve as an active verifier, autonomously interacting with the environment to probe intermediate execution states and uncover silent errors, and (2) employs a reflection-aware ternary reward strategy that distinguishes between correctable grounding errors and irrecoverable mistakes. We design a scalable pipeline to construct over 8K high-quality training instances for DataPRM via diversity-driven trajectory generation and knowledge-augmented step-level annotation. Experimental results demonstrate that DataPRM improves downstream policy LLMs by 7.21% on ScienceAgentBench and 11.28% on DABStep using Best-of-N inference. Notably, with only 4B parameters, DataPRM outperforms strong baselines, and exhibits robust generalizability across diverse Test-Time Scaling strategies. Furthermore, integrating DataPRM into Reinforcement Learning yields substantial gains over outcome-reward baselines, achieving 78.73% on DABench and 64.84% on TableBench, validating the effectiveness of process reward supervision. Code is available at https://github.com/zjunlp/DataMind.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rewarding the Scientific Process: Process-Level Reward Modeling for Agentic Data Analysis》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然过程奖励模型（PRM）在数学等静态领域取得了成功，但在动态的数据分析任务中，通用 PRM 表现不佳。
*   **核心挑战**：
    1.  **静默错误（Silent Errors）**：代码执行成功但逻辑错误（如计算逻辑错误），传统 PRM 仅看代码文本无法识别。
    2.  **接地错误（Grounding Errors）**：数据分析需要试错（如尝试错误的列名），传统 PRM 会将其误判为致命错误并过度惩罚，阻碍了智能体的探索能力。
*   **研究意义**：提出一种能够与环境交互、区分探索与错误的专用过程奖励模型，以提升数据分析智能体的可靠性和性能。

### 2. 方法论
*   **核心思想**：构建 **DataPRM**，一个环境感知的生成式过程奖励模型。它不仅是一个打分器，更是一个能主动执行代码验证环境状态的“验证智能体”。
*   **关键技术细节**：
    *   **生成式 ReAct 验证范式**：DataPRM 采用 ReAct 框架，在对策略模型的某一步骤打分前，先自主生成验证代码、观察执行结果，并进行多轮推理。
    *   **工具增强能力**：集成了 `query_document`（查询文档）和 `query_image`（分析可视化结果）工具，实现跨模态和长文本的验证。
    *   **反思感知三元奖励策略**：将奖励分为三档：
        *   **1.0（完全正确）**：逻辑正确且直接推进任务。
        *   **0.5（可纠正错误）**：存在小错（如语法、路径错），但能触发环境反馈引导修正，属于有意义的探索。
        *   **0.0（不可恢复错误）**：存在致命逻辑漏洞或幻觉。
*   **数据流水线**：通过“多样性驱动的轨迹生成”（保留结果不一致的样本）和“知识增强的步骤级标注”，构建了超过 7,000 个高质量训练样本。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceAgentBench**：科学发现任务，侧重可视化和专业领域分析。
    *   **DABStep**：金融等领域的复杂多步推理任务。
    *   **DABench & TableBench**：用于验证强化学习（RL）效果的通用数据分析和表格推理基准。
*   **对比方法**：
    *   **基础基准**：多数投票（Majority Vote）、LLM-as-a-judge（DeepSeek-V3.2）、自奖励（Self-Rewarding, Qwen3-235B）。
    *   **现有 PRM**：Qwen2.5-Math-PRM (7B/72B)、Math-Shepherd、ThinkPRM (14B)、GenPRM (32B) 等。

### 4. 资源与算力
*   **算力投入**：实验使用了 **8 张 NVIDIA H20 GPU**。
*   **训练细节**：
    *   使用 `ms-swift` 框架进行监督微调（SFT），学习率 1e-5，训练 3 个 epoch。
    *   使用 `verl` 框架进行强化学习（GRPO 算法），学习率 1e-6。
    *   DataPRM 模型基于 Qwen3-4B-Instruct 构建。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准测试，进行了 Best-of-N（N=4, 8, 16）、Beam Search、DVTS 等多种测试时缩放（TTS）策略的实验。
*   **消融实验**：针对环境交互、多轮对话、三元奖励机制以及数据过滤策略进行了详尽的消融研究。
*   **客观性验证**：通过计算 Cohen’s Kappa 系数（0.80-0.94），证明了模型自动评分与人类专家标注的高度一致性，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能卓越**：仅 4B 参数的 DataPRM 在 ScienceAgentBench 和 DABStep 上分别提升了 7.21% 和 11.28%，超越了参数量大 58 倍的自奖励基准（235B）。
*   **环境交互的必要性**：消融实验证明，具备环境交互能力的 PRM 能更有效地识别静默错误，尤其在硬任务（Hard subset）上提升显著。
*   **RL 优化效果**：在强化学习中引入过程奖励能有效防止“熵塌陷”（Entropy Collapse），使模型在训练后期仍能保持探索能力，显著优于仅使用结果奖励（Outcome Reward）的模型。

### 7. 优点
*   **精准定位痛点**：深刻分析了数据分析任务中“探索”与“错误”的本质区别，提出了三元奖励策略。
*   **参数效率极高**：通过高质量的过程监督数据，使小模型（4B）在特定领域展现出超越超大模型的验证能力。
*   **系统性优化**：不仅关注推理时的搜索（TTS），还验证了在训练阶段（RL）的增益。

### 8. 不足与局限
*   **任务覆盖范围**：目前主要侧重于推理和可视化，对于更复杂的工程任务（如大规模模型训练、自动化机器学习流水线）尚未深入探讨。
*   **数据依赖性**：DataPRM 的性能高度依赖于高质量的步骤级标注数据，虽然有自动化流水线，但初始的专家知识注入仍不可或缺。
*   **推理延迟**：由于 DataPRM 需要进行多轮 ReAct 交互验证，其推理时间成本高于传统的单次打分 PRM（尽管文中提到可通过并行化缓解）。

（完）
