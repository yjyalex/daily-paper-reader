---
title: "No Test Cases, No Problem: Distillation-Driven Code Generation for Scientific Workflows"
authors: "Siddeshwar Raghavan, Tanwi Mallick"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23106v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于科学代码生成和任务分解的多智能体框架
tldr: 针对科学工作流中缺乏输入输出测试用例导致传统LLM代码生成框架失效的问题，本文提出了MOSAIC框架。该框架无需训练，采用学生-教师知识蒸馏机制，通过领域特定示例和结构化问题分解来引导代码生成。此外，引入统一上下文窗口（CCW）以减少子任务间的幻觉。在SciCode基准测试中，MOSAIC在准确性、可执行性和数值精度上均优于现有方法，且支持轻量级模型。
selection_source: fresh_fetch
motivation: 传统的基于执行反馈的代码生成方法在缺乏I/O测试用例的科学计算领域难以应用。
method: 提出MOSAIC框架，利用知识蒸馏机制结合领域示例进行问题分解，并通过统一上下文窗口保持推理一致性。
result: 在SciCode基准测试中，MOSAIC显著提升了代码的准确率、可执行性及数值精度。
conclusion: MOSAIC证明了在无需测试用例的情况下，通过结构化分解和知识蒸馏也能实现高质量的科学代码生成。
---

## Abstract
Existing multi-agent Large Language Model (LLM) frameworks for code generation typically use execution feedback and improve iteratively using Input/Output (I/O) test cases. However, this does not work for scientific workflows, where I/O test cases do not exist, and generating them requires solving the very problem at hand. To address this, we introduce MOSAIC, a training-free multi-agent framework for scientific code generation without I/O supervision. Instead of execution feedback, MOSAIC employs a student-teacher knowledge distillation framework that grounds generation through domain-specific examples and structured problem decomposition. To further mitigate hallucinations across chained subproblems, we introduce a Consolidated Context Window (CCW) for maintaining consistent reasoning across agents. Experiments on the SciCode benchmark show that MOSAIC improves accuracy, executability, and numerical precision over existing approaches while relying on lightweight models.

---

## 论文详细总结（自动生成）

这是一份关于论文《No Test Cases, No Problem: Distillation-Driven Code Generation for Scientific Workflows》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 LLM 代码生成框架（如 OpenDevin 等）高度依赖**执行反馈（Execution Feedback）**，即通过输入/输出（I/O）测试用例来不断迭代修复代码。然而，在**科学计算工作流**中，这种方法失效了，因为科学问题的 I/O 测试用例往往不存在——生成这些测试用例本身就需要先解决该科学问题。
*   **整体含义**：本文提出了 **MOSAIC** 框架，旨在解决在缺乏测试用例的情况下，如何利用多智能体协作和知识蒸馏技术，生成高质量、可执行且数值精确的科学计算代码。

### 2. 方法论：核心思想与关键技术
MOSAIC 是一个无需训练（Training-free）的多智能体框架，其核心流程如下：
*   **学生-教师知识蒸馏机制（Student-Teacher Distillation）**：
    *   **教师智能体（Teacher Agent）**：通常由能力更强的模型（如 GPT-4o）担任，负责将复杂的科学问题分解为逻辑子任务，并提供高层次的推理指导。
    *   **学生智能体（Student Agent）**：由更轻量或同等的模型担任，在教师的指导下编写具体的实现代码。这种结构通过领域特定示例（Few-shot）引导生成。
*   **结构化问题分解**：将长程、复杂的科学计算任务拆解为一系列相互关联的子问题，降低单次生成的复杂度。
*   **统一上下文窗口（Consolidated Context Window, CCW）**：
    *   这是本文的关键创新。为了防止子任务之间出现幻觉或逻辑断层，CCW 会在智能体链条中传递并维护一致的推理状态、变量定义和物理约束，确保后续子任务能“感知”到前序任务的逻辑。
*   **算法流程**：输入科学问题 -> 教师进行任务分解 -> 结合领域示例 -> 学生在 CCW 约束下逐个生成子任务代码 -> 最终汇总为完整工作流。

### 3. 实验设计
*   **数据集/Benchmark**：使用了 **SciCode** 基准测试。这是一个专门针对科学计算的挑战性数据集，涵盖物理、化学、生物等多个领域的复杂编程任务。
*   **对比方法**：
    *   基础 LLM 直接生成（如 GPT-4o, Llama-3-70B）。
    *   现有的多智能体或链式思考（CoT）方法。
*   **评估指标**：
    *   **准确性（Accuracy）**：代码逻辑是否正确。
    *   **可执行性（Executability）**：代码是否能无错运行。
    *   **数值精度（Numerical Precision）**：计算结果与科学真值的接近程度。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和训练时长，因为 MOSAIC 是一个**无需训练（Training-free）**的推理框架。
*   **模型使用**：实验主要涉及对 GPT-4o、Llama-3 等模型的 API 调用或推理部署。由于强调支持“轻量级模型”，其运行成本主要集中在推理阶段的 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：在 SciCode 的多个子领域进行了广泛测试。
*   **消融实验**：论文通过消融实验验证了 **CCW（统一上下文窗口）** 的有效性，证明了在没有 CCW 的情况下，子任务间的幻觉会显著增加。
*   **充分性评价**：实验设计较为充分，不仅对比了最终结果，还深入分析了代码在不同科学维度下的表现。通过使用 SciCode 这一高难度基准，证明了该方法在处理复杂长程推理时的鲁棒性。

### 6. 主要结论与发现
*   **超越 SOTA**：MOSAIC 在无需任何测试用例反馈的情况下，在 SciCode 上的表现优于现有的基于反馈的框架。
*   **CCW 的重要性**：统一上下文窗口能有效减少多智能体协作中的“上下文漂移”和幻觉问题。
*   **轻量级模型增益**：通过教师模型的引导，较小的学生模型也能产出高质量的科学代码，实现了性能与成本的平衡。
*   **数值可靠性**：该方法生成的代码在处理复杂的偏微分方程、量子力学模拟等任务时，展现了更高的数值稳定性。

### 7. 优点（亮点）
*   **摆脱测试依赖**：解决了科学领域“无测试用例可循”的痛点，具有极强的实际应用价值。
*   **架构优雅**：将知识蒸馏与多智能体协作结合，通过 CCW 解决了长链条推理的一致性问题。
*   **即插即用**：无需微调模型，通过 Prompt Engineering 和架构设计即可提升现有 LLM 的科学编程能力。

### 8. 不足与局限
*   **对教师模型的依赖**：如果教师智能体在初始任务分解阶段出现逻辑错误，可能会导致后续学生智能体“步步皆错”。
*   **领域示例限制**：Few-shot 示例的质量对结果影响较大，对于极度冷门的科学前沿领域，可能难以找到合适的参考示例。
*   **计算开销**：虽然支持轻量模型，但多智能体多次交互和维护长上下文窗口会增加推理延迟和 API 成本。

（完）
