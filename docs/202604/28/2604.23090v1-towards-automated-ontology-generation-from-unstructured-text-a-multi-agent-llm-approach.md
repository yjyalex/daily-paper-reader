---
title: "Towards Automated Ontology Generation from Unstructured Text: A Multi-Agent LLM Approach"
title_zh: 迈向从非结构化文本自动生成本体：一种多智能体大语言模型方法
authors: "Abid Talukder, Maruf Ahmed Mridul, Oshani Seneviratne"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23090v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于本体构建的多智能体架构
tldr: 本研究针对从非结构化文本自动生成本体的挑战，提出了一种多智能体大语言模型（LLM）方法。针对单智能体模型在设计模式合规性和结构冗余方面的不足，研究设计了包含领域专家、管理者、编码者和质量保证者四个角色的协作架构。通过保险合同领域的实验验证，该方法显著提升了本体的结构质量和功能可用性，为可扩展且可审计的自动化本体工程提供了新路径。
selection_source: fresh_fetch
motivation: 现有的单智能体LLM在自动生成本体时存在设计模式不合规、结构冗余及迭代修复无效等问题。
method: 提出一种多智能体架构，将本体构建分解为领域专家、管理者、编码者和质量保证者四个驱动角色的协作过程。
result: 实验表明，多智能体方法显著提高了本体的结构质量，并凭借前置规划增强了查询可用性。
conclusion: 以规划为先、工件驱动的多智能体生成模式是实现可扩展且可审计的自动化本体工程的有效方向。
---

## 摘要
从非结构化自然语言中自动生成形式化本体仍然是知识工程中的一个核心挑战。尽管大语言模型（LLMs）展现出潜力，但目前尚不清楚哪些架构设计选择决定了生成质量，以及现有方法失败的原因。我们通过一项针对特定领域保险合同的受控实验研究来探讨这些问题。我们首先建立了一个单智能体 LLM 基准，识别出关键的失败模式，如本体设计模式（Ontology Design Pattern）合规性差、结构冗余以及迭代修复无效。随后，我们引入了一种多智能体架构，将本体构建分解为四个由工件驱动的角色：领域专家、经理、编码员和质量保证员。我们从架构质量（通过异构 LLM 评审小组）和功能可用性（通过能力问题驱动的 SPARQL 评估以及补充的检索增强生成评估）两个维度评估了性能。结果表明，多智能体方法显著提高了结构质量，并适度增强了可查询性，其提升主要源于前置规划。这些发现强调了“规划优先、工件驱动”的生成方式是实现可扩展自动化本体工程的一条充满前景且更具可审计性的路径。

## Abstract
Automatically generating formal ontologies from unstructured natural language remains a central challenge in knowledge engineering. While large language models (LLMs) show promise, it remains unclear which architectural design choices drive generation quality and why current approaches fail. We present a controlled experimental study using domain-specific insurance contracts to investigate these questions. We first establish a single-agent LLM baseline, identifying key failure modes such as poor Ontology Design Pattern compliance, structural redundancy, and ineffective iterative repair. We then introduce a multi-agent architecture that decomposes ontology construction into four artifact-driven roles: Domain Expert, Manager, Coder, and Quality Assurer. We evaluate performance across architectural quality (via a panel of heterogeneous LLM judges) and functional usability (via competency question driven SPARQL evaluation with complementary retrieval augmented generation based assessment). Results show that the multi-agent approach significantly improves structural quality and modestly enhances queryability, with gains driven primarily by front-loaded planning. These findings highlight planning-first, artifact-driven generation as a promising and more auditable path toward scalable automated ontology engineering.

---

## 论文详细总结（自动生成）

这篇论文针对从非结构化文本自动生成本体（Ontology）的挑战，提出了一种基于多智能体大语言模型（LLM）的协作框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从复杂的非结构化文本（如法律合同、技术规范）中自动生成形式化、有效且符合设计规范的本体。
*   **研究动机**：
    *   **人工瓶颈**：传统本体工程依赖专家，耗时耗力且难以扩展。
    *   **单智能体局限**：现有的单次生成（Single-pass）LLM方法在处理复杂逻辑时，常出现违反本体设计模式（ODP）、结构冗余、上下文退化以及迭代修复无效等问题。
    *   **缺乏审计性**：黑盒化的生成过程让用户难以信任和验证生成的知识结构。

### 2. 方法论：多智能体工件驱动架构
论文提出了一种将本体构建分解为四个专业化、工件驱动（Artifact-Driven）角色的多智能体协作流程：
*   **核心思想**：通过“先规划、后编码”的策略，将语义理解与技术实现分离。
*   **关键角色与流程**：
    1.  **领域专家 (Domain Expert)**：分析文本和能力问题（CQs），生成**语义需求文档 (SRD)**，识别实体、关系和约束。
    2.  **管理者 (Manager)**：基于 SRD 制定**技术实施方案 (TIP)**，负责选择合适的本体设计模式（ODP，如事件重构、参与模式等）并规划类/属性的重用。
    3.  **编码者 (Coder)**：根据 TIP 编写具体的 Turtle (TTL) 代码，实现本体的实例化。
    4.  **质量保证员 (QA)**：执行三层检查（架构审查、语法校验、语义一致性推理），并引导修复循环。
*   **评估技术**：引入了**能力问题 (CQ) 驱动的评估框架**，结合了基于 LLM 自动生成的 SPARQL 查询验证和基于向量检索的 RAG 评估，以衡量本体的功能可用性和知识覆盖率。

### 3. 实验设计
*   **数据集/场景**：选取了两个复杂的保险合同（Equivita 和 Sentinel）作为测试床，涉及多方角色、时间约束和财务规则。
*   **基准方法 (Benchmark)**：
    *   **单智能体直接生成 (Baseline)**：模拟简单的提示词工程，一次性生成本体并进行基础语法修复。
*   **对比维度**：
    *   **架构保真度**：由 5 个异构 LLM（Gemini、Claude、Grok、Deepseek、Llama）组成的评审团对可扩展性、冗余度和 ODP 使用率评分。
    *   **功能可用性**：通过约 300 个能力问题（CQs）进行 SPARQL 查询成功率和 RAG 语义覆盖率测试。

### 4. 资源与算力
*   **模型选择**：实验主要使用 **30B 参数级别** 的模型。作者指出这是为了模拟在隐私敏感（如金融、法律）环境下的本地部署需求。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理总时长。但提到多智能体框架由于存在迭代修复循环（上限 1000 次），其计算开销显著高于基准方法。

### 5. 实验数量与充分性
*   **实验规模**：针对 2 份合同生成了约 300 个 CQs 进行深度测试。
*   **充分性评价**：
    *   **深度充分**：论文侧重于“诊断性分析”，对失败模式（如冗余、幻觉循环）进行了详尽的定性研究。
    *   **广度有限**：仅使用了 2 份合同，样本量较小，缺乏大规模跨领域数据集的验证。
    *   **客观性**：通过异构模型评审团和 Krippendorff’s alpha 系数测量标注者间的一致性，增强了评估的客观性。

### 6. 主要结论与发现
*   **规划胜于修复**：多智能体方法的优势主要源于“前置规划”（Manager 角色制定的 TIP），而非后端的自动修复循环。
*   **结构质量显著提升**：在 ODP 使用率和可扩展性评分上，多智能体方法比基准方法高出 1-2 倍。
*   **冗余仍是难题**：LLM 很难在长文档中保持全局一致性，容易创建语义重叠的类。
*   **修复循环的局限**：当出现逻辑错误时，LLM 容易陷入“语法幻觉”或无效的修复死循环，难以通过简单的自我反思解决深层架构问题。

### 7. 优点与亮点
*   **工件驱动的透明度**：生成的 SRD 和 TIP 提供了中间检查点，使本体构建过程可审计、可干预。
*   **评估框架严谨**：结合了严格的 SPARQL 逻辑验证和容错的 RAG 语义验证，能够更全面地评价本体质量。
*   **角色解耦**：有效降低了单个 LLM 在处理复杂任务时的认知负荷。

### 8. 不足与局限
*   **计算成本高**：多智能体协作和迭代修复带来了极高的 Token 消耗和延迟。
*   **冗余控制不足**：缺乏显式的外部类注册表或全局状态管理机制来彻底消除冗余。
*   **泛化性验证不足**：实验仅限于保险领域，尚未在医疗、工程等其他复杂领域进行验证。
*   **评估闭环风险**：虽然使用了异构模型，但生成和评估均依赖 LLM，仍存在潜在的系统性偏差。

（完）
