---
title: "Hidden States Know Where Reasoning Diverges: Credit Assignment via Span-Level Wasserstein Distance"
authors: "Xinzhu Chen, Wei He, Huichuan Fan, Wenzhe Niu, Zhongxiang Sun, Xuanru Wang, Jiuchong Gao, Jinghua Hao, Renqing He, Weijie Yu"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23318v1"
tags: ["query:token"]
score: 10.0
evidence: GRPO中用于推理的片段级信用分配
tldr: 针对强化学习中GRPO信用分配过于粗粒度的问题，本文发现正确与错误推理路径的隐藏状态分布在推理分歧点处存在显著差异。研究者提出SHEAR方法，利用片段级Wasserstein距离衡量这种差异，并据此重新加权Token级别的优势函数。该方法无需额外标注或奖励模型，在数学和代码生成任务上显著优于标准GRPO，实现了高效的细粒度信用分配。
selection_source: fresh_fetch
motivation: 现有的GRPO算法对所有Token分配相同的优势值，缺乏细粒度的信用分配，而过程奖励模型又需要昂贵的步级标注。
method: 提出SHEAR方法，通过计算正确与错误样本间隐藏状态的Wasserstein距离来识别推理分歧点，并据此动态调整Token的优势权重。
result: 在五个数学推理和五个代码生成基准测试中，SHEAR均优于标准GRPO，并展现出媲美监督过程奖励模型的性能。
conclusion: 隐藏状态的分布差异可作为一种有效的自监督信号，在无需额外模型或标注的情况下实现精准的细粒度推理信用分配。
---

## Abstract
Group Relative Policy Optimization (GRPO) performs coarse-grained credit assignment in reinforcement learning with verifiable rewards (RLVR) by assigning the same advantage to all tokens in a rollout. Process reward models can provide finer-grained supervision, but they require step-level annotation or additional reward modeling. We show that hidden-state distributions contain a useful signal for local reasoning quality that can be extracted using only outcome-level correctness labels available in RLVR. Specifically, within each GRPO group, the Wasserstein distance between span-level hidden state distributions of correct and incorrect rollouts increases around regions where their local reasoning quality diverges. This association holds both across examples and within individual trajectories, suggesting that hidden-state distributional divergence can serve as a self-supervision signal for fine-grained credit assignment. We formalize this observation with a separation theorem showing that, under mild structural assumptions, post-divergence spans have larger Wasserstein distances than pre-divergence spans whenever the population-level distributional gap exceeds finite-sample noise. Motivated by this result, we propose \textbf{S}pan-level \textbf{H}idden state \textbf{E}nabled \textbf{A}dvantage \textbf{R}eweighting (SHEAR), which modifies GRPO by using span-level Wasserstein distances to scale token-level advantages, amplifying updates on tokens whose hidden states are more separated from the opposing group. The method requires no additional model and only minimal changes to the training pipeline. Experiments on five mathematical reasoning benchmarks and five code generation benchmarks show improvements over standard GRPO and strong performance relative to supervised process reward models, while requiring no additional annotation or reward model training.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型在强化学习（RLVR）中的信用分配问题提出了创新性的解决方案。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在强化学习（如 GRPO 算法）中，模型通常只根据最终结果（正确/错误）获得奖励。GRPO 将相同的“优势值”（Advantage）分配给一条路径上的所有 Token，这被称为**粗粒度信用分配**。
*   **核心挑战**：虽然过程奖励模型（PRM）可以提供细粒度监督，但它需要昂贵的步骤级人工标注或额外的奖励模型。
*   **核心发现**：论文发现模型自身的**隐藏状态（Hidden States）**其实“知道”推理在哪里出了错。在正确和错误推理路径的分歧点附近，隐藏状态的分布差异（通过 Wasserstein 距离衡量）会显著增大。

### 2. 论文提出的方法论：SHEAR
**SHEAR (Span-level Hidden state Enabled Advantage Reweighting)** 的核心思想是利用隐藏状态的分布分歧作为自监督信号，动态调整 Token 级别的权重。

*   **关键技术细节**：
    1.  **片段分解（Span Decomposition）**：使用滑动窗口（如窗口大小 $w=100$，步长 $s=25$）将推理路径切分为多个重叠片段。
    2.  **Wasserstein 距离计算**：计算当前路径片段与同一组中“对立组”（若当前正确则对比错误组，反之亦然）所有片段之间的最小 Wasserstein 距离（采用高效的 Sinkhorn 算法近似）。
    3.  **Token 级聚合**：通过**最大池化（Max-pooling）**将片段级的距离映射回每个 Token，并进行全局范数归一化。
    4.  **优势重加权**：将计算出的权重 $\omega_t$ 乘到原有的 GRPO 优势值上。对于错误路径，分歧大的 Token 会受到更强的惩罚；对于正确路径，分歧大的 Token（代表关键正确步骤）会获得更强的奖励。
*   **理论支撑**：论文证明了一个**分离定理（Separation Theorem）**，即在温和的假设下，推理分歧点之后的片段在统计学上必然比分歧点之前的片段具有更大的 Wasserstein 距离。

### 3. 实验设计
*   **模型底座（Backbones）**：Qwen2.5-Math-7B、Qwen2.5-Coder-7B、Qwen2.5-14B-Base、Llama3.1-8B-Instruct。
*   **数据集与场景**：
    *   **数学推理**：MATH 训练集；评估使用 AIME24/25、AMC23、MATH500、OlympiadBench。
    *   **代码生成**：Eurus-RL-Code 训练集；评估使用 HumanEval/+、MBPP/+、LiveCodeBench。
*   **对比方法**：
    *   标准 GRPO（基准）。
    *   基于熵的优势重塑（Entropy Advantage Reshaping）。
    *   基于 PRM 的方法（如 PURE、PRM 优势重塑），使用专门训练的 7B 规模过程奖励模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练时长**：文中提到了训练时间的相对开销（图 8）。SHEAR 相比标准 GRPO 增加了约 **7.4% 到 15.5%** 的训练时间。这种开销主要来自 Sinkhorn 距离的计算，但随着模型规模增大（如从 7B 到 14B），该开销占比会下降。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 大任务领域（数学、代码）、10 个主流基准测试、4 种不同架构和规模的模型。
*   **消融实验**：非常充分。作者对比了不同的距离度量（Wasserstein vs. 余弦距离 vs. MMD vs. Chamfer）、不同的归一化策略（跨样本 vs. 单样本）、窗口大小及步长、以及采样组大小（G=8 vs. G=16）。
*   **客观性**：所有实验均运行了 3 个随机种子并报告了标准差，确保了结果的可靠性。

### 6. 主要结论与发现
1.  **自监督信号有效性**：隐藏状态的 Wasserstein 距离能有效追踪推理质量，无需任何步骤级标注。
2.  **性能超越 PRM**：在数学任务上，SHEAR 的表现优于依赖外部 PRM 模型的方法，这可能是因为 PRM 存在分布偏移问题，而 SHEAR 使用的是模型自身的实时表征。
3.  **通用性**：该方法在数学和代码任务上均表现出一致的改进，尤其是在长链推理任务中效果更明显。
4.  **距离度量的重要性**：消融实验显示，Wasserstein 距离（捕捉分布形状）显著优于简单的余弦距离（仅捕捉均值方向）。

### 7. 优点
*   **无需额外标注**：完全利用 RLVR 中的结果标签实现细粒度监督。
*   **无需辅助模型**：不需要训练或运行额外的奖励模型，降低了系统复杂度和推理开销。
*   **理论与实证结合**：不仅有显著的实验提升，还提供了严谨的数学证明来解释为何该信号有效。
*   **即插即用**：对现有 GRPO 训练流水线的修改极小。

### 8. 不足与局限
*   **计算开销**：虽然比例不高，但计算 Wasserstein 距离确实增加了额外的 GPU 显存和计算时间。
*   **超参数敏感性**：滑动窗口的大小和步长对结果有一定影响，需要针对不同任务进行微调。
*   **短序列限制**：在非常短的推理任务中（如简单的代码补全），片段级信号的解析度可能不足，导致提升有限。
*   **对立组依赖**：该方法依赖于同一组 Prompt 下同时存在正确和错误的样本，如果模型对某个问题全对或全错，则无法产生有效的重加权信号。

（完）
