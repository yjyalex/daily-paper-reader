---
title: "Thinking Without Words: Efficient Latent Reasoning with Abstract Chain-of-Thought"
authors: "Keshav Ramji, Tahira Naseem, Ramón Fernandez Astudillo"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22709v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对思维链的离散潜变量推理后训练机制
tldr: 针对显式思维链（CoT）推理成本高的问题，本文提出 Abstract-CoT，一种离散潜空间推理机制。该方法让模型在生成最终回答前，先生成一串由预留词表组成的简短抽象标记序列。通过策略迭代预热（结合掩码监督微调与自蒸馏）及强化学习优化，该方法在保持数学推理和指令遵循性能的同时，将推理标记数量减少了高达11.6倍，实现了高效且泛化性强的潜空间推理。
selection_source: fresh_fetch
motivation: 显式思维链推理虽然有效但推理成本高昂，而现有的非语言推理方法在性能上仍落后于文本思维链。
method: 提出 Abstract-CoT 机制，通过预热循环（掩码微调与自蒸馏）和约束解码下的强化学习，训练模型使用预留的抽象标记进行离散潜空间推理。
result: 该方法在多个推理任务中实现了与显式思维链相当的性能，同时将推理标记数量缩减了高达11.6倍，并展现出跨模型的泛化能力。
conclusion: 研究证明了通过学习抽象推理语言来实现高效潜空间推理的可行性，为降低大模型推理成本提供了新路径。
---

## Abstract
While long, explicit chains-of-thought (CoT) have proven effective on complex reasoning tasks, they are costly to generate during inference. Non-verbal reasoning methods have emerged with shorter generation lengths by leveraging continuous representations, yet their performance lags behind verbalized CoT. We propose $\textbf{Abstract Chain-of-Thought}$, a discrete latent reasoning post-training mechanism in which the language model produces a short sequence of tokens from a reserved vocabulary in lieu of a natural language CoT, before generating a response. To make previously unseen ''abstract'' tokens useful, we introduce a policy iteration-style warm-up loop that alternates between (i.) bottlenecking from a verbal CoT via masking and performing supervised fine-tuning, and (ii.) self-distillation by training the model to generate abstract tokens from the prompt alone via constrained decoding with the codebook. After warm-up, we optimize the generation of abstract sequences with warm-started reinforcement learning under constrained decoding. Abstract-CoT achieves up to $11.6\times$ fewer reasoning tokens while demonstrating comparable performance across mathematical reasoning, instruction-following, and multi-hop reasoning, and generalizes across language model families. We also find an emergent power law distribution over the abstract vocabulary, akin to those seen in natural language, that evolves across the training phases. Our findings highlight the potential for post-training latent reasoning mechanisms that enable efficient inference through a learned abstract reasoning language.

---

## 论文详细总结（自动生成）

这篇论文由 IBM Research 团队提出，探讨了如何通过“抽象思维链”（Abstract Chain-of-Thought）在不牺牲性能的前提下，极大地提高大语言模型（LLM）的推理效率。

### 1. 核心问题与整体含义
*   **研究动机**：目前的推理模型（如 DeepSeek-R1）依赖长篇累牍的显式文本思维链（Verbal CoT）来解决复杂问题。虽然有效，但这种方式推理成本高、延迟大，且文本中包含大量冗余信息。
*   **核心问题**：能否用一串简短、非人类可读的“抽象标记”（Abstract Tokens）取代冗长的自然语言推理过程，同时保留 CoT 的性能增益？
*   **整体含义**：论文提出了一种名为 **Abstract-CoT** 的机制，通过在离散潜空间（Latent Space）中进行推理，实现了推理过程的极度压缩。

### 2. 方法论
核心思想是引入一个预留的抽象词表（如 `<TOKEN_A>`, `<TOKEN_B>` 等），让模型学习用这些标记进行“内部思考”。

*   **关键技术细节**：
    1.  **词表扩展**：在现有模型词表中加入 $M$ 个全新的抽象标记和起始/结束定界符。
    2.  **两阶段训练方案**：
        *   **阶段一：策略迭代预热（Warm-up）**：
            *   **瓶颈监督微调（Bottlenecked SFT）**：利用现有的文本 CoT 数据作为教师。模型在生成抽象标记时可以“看到”文本 CoT，但最终回答只能“看到”抽象标记。这强迫抽象标记成为信息的“瓶颈”，吸收推理的关键特征。
            *   **自蒸馏（Self-Distillation）**：丢弃文本 CoT，训练模型仅根据提示词（Prompt）直接生成抽象标记序列。
        *   **阶段二：热启动强化学习（Warm-started RL）**：
            *   使用 **GRPO** 算法进行优化。
            *   在约束解码下进行采样（限制模型只能在推理阶段生成抽象词表中的标记）。
            *   通过生成式奖励模型（Generative Reward Model）对最终回答的质量进行评分，从而优化抽象序列的生成策略。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：MATH-500, AIME’25。
    *   **指令遵循**：AlpacaEval-LC-2.0。
    *   **多跳问答**：HotpotQA。
    *   **高难度科学推理**：GPQA-Diamond。
*   **Benchmark 与对比方法**：
    *   **基准线**：直接回答（Baseline）、SFT（无 CoT）、SFT（有文本 CoT）。
    *   **对比方法**：Pause Tokens（暂停标记）、Stepwise Internalization（逐步内化）、SFT+RL（传统的文本 CoT 强化学习）。
*   **模型**：Qwen3 (4B, 8B, 32B) 和 Granite-4.0-Micro (3B)。

### 4. 资源与算力
*   **算力投入**：
    *   **SFT 阶段**：使用了 8 张 NVIDIA H100 GPU。
    *   **RL 阶段**：使用了多达 32 张 NVIDIA H100 GPU。
*   **训练规模**：预热阶段使用约 600k 样本，RL 阶段进行了 100 万个 episode 的训练。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 3B 到 32B 不同规模的模型，并在数学、通用指令、多跳推理等多个维度进行了测试。
*   **消融实验**：
    *   测试了不同抽象词表大小（$M=2$ 到 $512$）的影响。
    *   进行了**排列实验（Permutation）**：证明抽象标记的顺序对结果有显著影响，说明模型学到了组合逻辑而非随机填充。
    *   进行了**截断实验（Truncation）**：分析了推理长度对性能的影响。
*   **评价**：实验设计非常充分且严谨，通过跨模型家族（Qwen 和 Granite）的验证，证明了该方法的普适性和客观性。

### 6. 主要结论与发现
*   **极高的压缩率**：在数学任务上，Abstract-CoT 相比文本 CoT 减少了高达 **11.6 倍** 的推理标记数量。
*   **性能持平**：在大幅缩减标记数的情况下，其准确率与经过 RL 优化的文本 CoT 模型相当，甚至在某些任务上有所超越。
*   **涌现幂律分布**：研究发现，模型对抽象标记的使用频率演化出了类似于自然语言的**幂律分布（Zipf's Law）**，表明模型在后训练过程中自主形成了一套“抽象推理语言”。
*   **冷启动失效**：实验证明，如果不经过预热阶段直接进行 RL，模型无法学会使用这些随机初始化的标记。

### 7. 优点
*   **推理高效**：显著降低了推理时的计算开销和延迟，解决了长 CoT 带来的成本问题。
*   **离散空间优势**：相比于 Coconut 等连续潜空间方法，Abstract-CoT 保持在离散标记空间内，更容易集成到现有的推理引擎和 KV 缓存架构中。
*   **无需持续预训练**：仅通过后训练（Post-training）即可引入新的推理语言，实施成本相对较低。

### 8. 不足与局限
*   **不可解释性**：抽象标记序列对人类来说是完全不可读的“黑盒”，这给模型的可解释性和安全性监控带来了挑战。
*   **训练复杂性**：需要复杂的“预热+蒸馏+RL”多阶段流程，对训练数据的质量（尤其是教师 CoT 数据）有一定依赖。
*   **任务局限**：虽然在逻辑和数学上表现优异，但在需要极度精细文本解释的场景下，其表现是否依然稳健尚待更多验证。

（完）
