---
title: "GraphPlanner: Graph Memory-Augmented Agentic Routing for Multi-Agent LLMs"
title_zh: GraphPlanner：面向多智能体大语言模型的图记忆增强型智能体路由
authors: "Tao Feng, Haozhen Zhang, Zijie Lei, Peixuan Han, Jiaxuan You"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23626v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 异构智能体之间的多轮协作与任务规划
tldr: GraphPlanner利用图记忆和MDP优化多智能体大模型系统中的路由与协作。
selection_source: fresh_fetch
motivation: 异构智能体之间的多轮协作与任务规划。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
大语言模型（LLM）路由在整合不同模型优势、平衡效率与性能方面取得了显著成果。然而，为了支持更真实且具挑战性的应用，路由必须扩展到智能体化 LLM 场景，其中任务规划、异构智能体之间的多轮协作以及记忆利用是必不可少的。为填补这一空白，我们提出了 GraphPlanner，这是一种面向多智能体 LLM 的异构图记忆增强型智能体路由器，它为每个查询生成路由工作流，并支持归纳式（inductive）和转导式（transductive）推理。GraphPlanner 将工作流生成建模为马尔可夫决策过程（MDP），在每一步中同时选择 LLM 主干网络和智能体角色（包括规划器 Planner、执行器 Executor 和总结器 Summarizer）。通过利用名为 GARNet 的异构图来捕获查询、智能体和响应之间的交互记忆，GraphPlanner 将历史记忆和工作流记忆整合到更丰富的状态表示中。整个流水线通过强化学习进行优化，共同提高特定任务的性能和计算效率。我们在 14 个不同的 LLM 任务上评估了 GraphPlanner，并证明：(1) GraphPlanner 优于强大的单轮和多轮路由器，准确率提升高达 9.3%，同时将 GPU 成本从 186.26 GiB 降低到 1.04 GiB；(2) GraphPlanner 对未见过的任务和 LLM 具有鲁棒的泛化能力，展现出强大的零样本能力；(3) GraphPlanner 有效利用历史记忆，支持归纳式和转导式推理，实现更具自适应性的路由。我们的 GraphPlanner 代码已发布在 https://github.com/ulab-uiuc/GraphPlanner。

## Abstract
LLM routing has achieved promising results in integrating the strengths of diverse models while balancing efficiency and performance. However, to support more realistic and challenging applications, routing must extend into agentic LLM settings, where task planning, multi-round cooperation among heterogeneous agents, and memory utilization are indispensable. To address this gap, we propose GraphPlanner, a heterogeneous graph memory-augmented agentic router for multi-agent LLMs that generates routing workflows for each query and supports both inductive and transductive inference. GraphPlanner formulates workflow generation as a Markov Decision Process (MDP), where at each step it selects both the LLM backbone and the agent role, including Planner, Executor, and Summarizer. By leveraging a heterogeneous graph, denoted as GARNet, to capture interaction memories among queries, agents, and responses, GraphPlanner integrates historical memory and workflow memory into richer state representations. The entire pipeline is optimized with reinforcement learning, jointly improving task-specific performance and computational efficiency. We evaluate GraphPlanner across 14 diverse LLM tasks and demonstrate that: (1) GraphPlanner outperforms strong single-round and multi-round routers, improving accuracy by up to 9.3% while reducing GPU cost from 186.26 GiB to 1.04 GiB; (2) GraphPlanner generalizes robustly to unseen tasks and LLMs, exhibiting strong zero-shot capabilities; and (3) GraphPlanner effectively leverages historical memories, supporting both inductive and transductive inference for more adaptive routing. Our code for GraphPlanner is released at https://github.com/ulab-uiuc/GraphPlanner.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GraphPlanner** 的新型多智能体大语言模型（LLM）路由框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现有的 LLM 路由方法主要分为单轮路由（简单分配任务给模型）和多轮路由（虽然有多次调用，但缺乏显式的智能体协作）。
*   **核心问题**：在处理复杂现实任务时，现有的路由器无法进行任务规划、多轮异构智能体协作以及有效利用历史记忆。
*   **研究动机**：将路由扩展到“智能体化（Agentic）”设置中，使路由器不仅能决定使用哪个 LLM，还能决定该 LLM 扮演什么角色（如规划者、执行者、总结者），从而通过结构化工作流解决复杂问题。

### 2. 方法论：核心思想与技术细节
*   **MDP 建模**：将智能体路由工作流的生成建模为马尔可夫决策过程（MDP）。
    *   **状态 (State)**：当前待解决的查询（Query）。
    *   **动作 (Action)**：一个二元组 `(智能体角色, LLM 主干)`。预设角色包括 Planner（分解任务）、Executor（执行任务）、Summarizer（汇总结果）。
    *   **奖励 (Reward)**：平衡任务准确度（Utility）与计算成本（Cost）。
*   **GARNet 异构图记忆**：
    *   **核心组件**：构建了一个包含查询节点、响应节点和“角色中心（Role Hub）”节点的异构图。
    *   **双图机制**：整合了“工作流记忆图（G_workflow）”捕获当前任务进度，以及“历史记忆图（G_history）”捕获过去的交互经验。
    *   **消息传递**：利用异构图神经网络（GNN）在这些节点间传递信息，生成丰富的状态表示。
*   **优化算法**：使用近端策略优化（PPO）强化学习算法对策略网络进行端到端训练，以在帕累托前沿（Pareto frontier）上寻找准确率与成本的最佳平衡。

### 3. 实验设计
*   **数据集与场景**：涵盖 6 个领域、14 个任务，包括数学（GSM8K, MATH）、代码（MBPP, HumanEval）、常识推理、世界知识、综合测试（MMLU, GPQA）以及域外泛化测试（LogicGrid, MGSM 等）。
*   **对比方法**：
    *   **单轮路由器**：RouterKNN, RouterMLP, RouterSVM, RouterDC, GraphRouter。
    *   **多轮路由器**：Prompt LLM, Router-KNN-MR, R2-Reasoner, Router-R1。
*   **评估阶段**：
    *   **Phase 1**：在预定义的工作流中优化 LLM 的选择。
    *   **Phase 2**：完全自主生成工作流（同时选择角色和模型）。

### 4. 资源与算力
*   **硬件设备**：所有实验均在单张 **NVIDIA A6000 GPU** 上完成。
*   **训练效率**：GraphPlanner 表现出极高的算力效率。相比于 Router-R1 需要 186.26 GiB 的 GPU 显存，GraphPlanner 仅需 **1.04 GiB**。
*   **训练时长**：总训练时间约为 **120 分钟**，远低于 R2-Reasoner（360 分钟）和 Router-R1（300 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，测试了 12 个主流 LLM（如 Qwen2.5, LLaMA-3.1, Mixtral 等），并专门预留了 3 个模型用于测试对未见模型的泛化能力。
*   **消融实验**：非常充分。作者验证了历史记忆的必要性、异构图与同构图的区别、归纳式与转导式推理的差异，甚至在附录中测试了加入新角色（Thinker, Verifier）后的表现。
*   **客观性**：通过在 14 个任务上进行对比，并提供帕累托曲线分析成本与性能的权衡，实验设计显得客观且公平。

### 6. 主要结论与发现
*   **性能卓越**：在 Phase 2 中，GraphPlanner 的平均准确率比最强基准提升了 **9.3%**。
*   **成本极低**：在保持高准确率的同时，大幅降低了 GPU 资源消耗和推理延迟（每条查询仅 1.2 秒）。
*   **泛化能力强**：在未见过的任务上，GraphPlanner 的零样本准确率达到 78%，比之前的路由器高出 20%-40%；同时能无缝适配未见过的 LLM。
*   **记忆有效性**：证明了通过异构图捕获历史交互记忆能显著提升路由决策的质量。

### 7. 优点与亮点
*   **轻量化设计**：尽管使用了复杂的图结构，但模型参数量小，显存占用极低，适合实际部署。
*   **角色与模型联合优化**：打破了传统路由只选模型的局限，实现了“因材施教”的角色分配。
*   **灵活的推理模式**：支持归纳式（高效）和转导式（高性能）两种模式，满足不同应用需求。
*   **多线程并行**：通过多线程 Rollout 设计显著加快了强化学习的数据采集速度。

### 8. 不足与局限
*   **角色定义有限**：虽然附录扩展了角色，但核心实验主要基于三种基础角色，对于更复杂的社会化智能体协作可能仍需进一步探索。
*   **图规模挑战**：随着历史记忆（G_history）的无限增长，如何保持图检索和处理的效率在论文中未深入讨论极长时期的性能表现。
*   **依赖高质量嵌入**：图节点的初始化依赖于 Longformer 等嵌入模型，嵌入质量可能成为系统性能的瓶颈。

（完）
