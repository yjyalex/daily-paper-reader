---
title: "Serialisation Strategy Matters: How FHIR Data Format Affects LLM Medication Reconciliation"
authors: Sanjoy Pator
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.21076v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: JSON等数据序列化格式对LLM任务性能的影响
tldr: 本研究系统探讨了FHIR数据序列化策略对大语言模型（LLM）在药物对账任务中表现的影响。通过在五种开源模型上对比原始JSON、Markdown表格、临床叙述和时间轴四种格式，研究发现序列化方式对8B以下模型性能影响巨大，其中临床叙述格式最优；而70B模型在原始JSON下表现最佳。研究还指出遗漏是主要的失败模式，并为临床部署提供了针对不同规模模型的格式选择建议。
selection_source: fresh_fetch
motivation: 尽管LLM被用于处理FHIR格式的医疗记录，但数据序列化方式如何影响药物对账这一高风险临床过程的准确性仍缺乏系统研究。
method: 在200个合成患者案例上，对比了四种序列化策略在Phi-3.5、Mistral、Llama等五种不同规模模型上的4000次推理表现。
result: 小规模模型使用临床叙述格式可提升F1分数达19点，而70B模型在原始JSON格式下表现最优，且所有模型在处理多重用药时均存在遗漏风险。
conclusion: 临床LLM部署应依据模型规模选择序列化策略：8B以下模型推荐使用临床叙述格式，70B及以上模型则推荐使用原始JSON格式。
---

## Abstract
Medication reconciliation at clinical handoffs is a high-stakes, error-prone process. Large language models are increasingly proposed to assist with this task using FHIR-structured patient records, but a fundamental and largely unstudied variable is how the FHIR data is serialised before being passed to the model. We present the first systematic comparison of four FHIR serialisation strategies (Raw JSON, Markdown Table, Clinical Narrative, and Chronological Timeline) across five open-weight models (Phi-3.5-mini, Mistral-7B, BioMistral-7B, Llama-3.1-8B, Llama-3.3-70B) on a controlled benchmark of 200 synthetic patients, totalling 4,000 inference runs. We find that serialisation strategy has a large, statistically significant effect on performance for models up to 8B parameters: Clinical Narrative outperforms Raw JSON by up to 19 F1 points for Mistral-7B (r = 0.617, p < 10^{-10}). This advantage reverses at 70B, where Raw JSON achieves the best mean F1 of 0.9956. In all 20 model and strategy combinations, mean precision exceeds mean recall: omission is the dominant failure mode, with models more often missing an active medication than fabricating one, which changes how clinical safety auditing priorities should be set. Smaller models plateau at roughly 7-10 concurrent active medications, leaving polypharmacy patients, the patients most at risk from reconciliation errors, systematically underserved. BioMistral-7B, a domain-pretrained model without instruction tuning, produces zero usable output in all conditions, showing that domain pretraining alone is not sufficient for structured extraction. These results offer practical, evidence-based format recommendations for clinical LLM deployment: Clinical Narrative for models up to 8B, Raw JSON for 70B and above. The complete pipeline is reproducible on open-source tools running on an AWS g6e.xlarge instance (NVIDIA L40S, 48 GB VRAM).

---

## 论文详细总结（自动生成）

这篇论文深入探讨了在医疗信息化标准 FHIR（快速医疗保健互操作性资源）背景下，数据序列化格式如何影响大语言模型（LLM）在药物对账（Medication Reconciliation）任务中的表现。

### 1. 核心问题与整体含义
药物对账是临床转诊中极高风险且易出错的环节。虽然 LLM 被提议用于处理 FHIR 格式的电子健康档案（EHR），但 FHIR 原始的嵌套 JSON 格式极其复杂且冗长。论文的核心问题是：**在将 FHIR 数据输入模型之前，不同的序列化（格式化）策略是否会显著影响模型的提取准确性？** 研究旨在为临床 LLM 部署提供基于证据的格式选择建议。

### 2. 方法论
研究者对比了四种将 FHIR `MedicationRequest` 资源转换为文本的策略：
*   **策略 A - 原始 JSON (Raw JSON)：** 基准格式，保留缩进和临床字段，但对超长记录进行截断处理。
*   **策略 B - Markdown 表格 (Markdown Table)：** 将资源展平为包含药物名、状态、日期、剂量等列的表格。
*   **策略 C - 临床叙述 (Clinical Narrative)：** 将每条记录转换为自然语言句子，并按“当前活跃”和“历史记录”进行显式分类标题划分。
*   **策略 D - 时间轴 (Chronological Timeline)：** 按处方日期严格排序，使用管道符（|）分隔字段，不区分活跃/历史状态。
*   **任务目标：** 模型需识别并以 JSON 数组形式返回所有“活跃（active）”状态的药物名称。

### 3. 实验设计
*   **数据集：** 使用 Synthea 生成的 200 名合成患者的 FHIR R4 记录，涵盖 10 年以上的用药史，活跃药物数量为 1-16 种。
*   **对比模型：** 涵盖不同参数规模的开源模型：Phi-3.5-mini (3.8B)、Mistral-7B、BioMistral-7B、Llama-3.1-8B、Llama-3.3-70B。
*   **评估指标：** 采用严格的**精确字符串匹配**，计算精确率（Precision）、召回率（Recall）和 F1 分数。
*   **统计检验：** 使用 Wilcoxon 符号秩检验和 Bonferroni 校正来验证策略间的差异显著性。

### 4. 资源与算力
*   **硬件：** 使用 AWS g6e.xlarge 实例，配备单块 **NVIDIA L40S GPU (48 GB VRAM)**。
*   **软件框架：** 使用 Ollama 进行本地推理，采用 4-bit 量化。
*   **推理设置：** 温度设为 0.0 以保证确定性。

### 5. 实验数量与充分性
*   **实验规模：** 总计进行了 **4,000 次独立推理运行**（200 患者 × 4 策略 × 5 模型）。
*   **充分性：** 实验设计非常充分，不仅对比了模型规模，还分析了药物数量（多重用药）对性能的影响，并进行了详细的错误模式分析（遗漏 vs 幻觉）。
*   **客观性：** 实验在受控环境下进行，所有模型使用相同的 Prompt 模板，确保了对比的公平性。

### 6. 主要结论与发现
*   **策略随规模变化：** 对于 **8B 及以下**的模型，**临床叙述（策略 C）**表现最优（Mistral-7B 的 F1 提升了 19 点）；而对于 **70B** 模型，**原始 JSON（策略 A）**反而表现最好（F1 达 0.9956）。
*   **遗漏（Omission）是主要风险：** 在所有组合中，精确率均高于召回率。模型更倾向于漏掉药物，而不是编造药物。
*   **容量天花板：** 小模型在处理超过 7-10 种活跃药物时性能急剧下降，这使得高风险的“多重用药”患者最容易被系统忽视。
*   **指令微调不可或缺：** 仅经过领域预训练但未经过指令微调的 BioMistral-7B 在所有测试中产出为零，证明领域知识无法弥补结构化提取能力的缺失。

### 7. 优点
*   **实用性强：** 为临床部署提供了直接的格式建议（小模型用叙述格式，大模型用 JSON）。
*   **零成本优化：** 证明了仅通过改变数据呈现格式（无需微调或复杂 Prompt 工程）即可获得显著的性能提升。
*   **可复现性：** 整个流程基于开源工具和合成数据，提供了 GitHub 仓库，易于社区验证。

### 8. 不足与局限
*   **合成数据偏差：** Synthea 数据虽然真实，但缺乏真实 EHR 中的录入错误、缩写不一和数据质量问题，实验结果可能是性能上限。
*   **匹配机制过于严格：** 采用精确字符串匹配，导致一些仅是格式差异（如剂量单位缩写）的正确答案被判定为错误。
*   **语言限制：** 仅针对英文记录进行了测试。
*   **模型覆盖：** 未测试专门经过指令微调的医疗大模型（如 Med-Llama）。

（完）
