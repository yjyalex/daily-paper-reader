---
title: "PDDL-Mind: Large Language Models are Capable on Belief Reasoning with Reliable State Tracking"
title_zh: PDDL-Mind：大语言模型通过可靠的状态追踪具备信念推理能力
authors: "Wang Bill Zhu, Qiutong Tony Yi, Robin Jia, Jesse Thomason"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17819v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过 PDDL 实现 LLM 信念推理和逻辑一致性
tldr: "针对大语言模型在心理理论（ToM）任务中因隐式状态跟踪不可靠而表现不佳的问题，本文提出PDDL-Mind神经符号框架。该框架将环境状态演变与信念推理分离，通过将叙述转化为PDDL语言的显式状态和动作，并利用预定义领域验证转换，为模型提供逻辑一致的世界状态表示。实验证明，该方法在多个ToM基准测试中比现有最优方法准确率提升超过5%，证明了显式状态跟踪对提升模型推理能力的重要性。"
selection_source: fresh_fetch
motivation: 大语言模型在心理理论任务中的表现瓶颈主要源于不可靠的隐式状态跟踪，而非高层推理能力的局限。
method: 提出PDDL-Mind框架，利用PDDL将叙述翻译为显式状态和动作，实现环境演变与信念推理的解耦。
result: "在MMToM-QA、MuMA和FanToM等基准测试中，该方法比现有最先进技术实现了超过5%的绝对准确率提升。"
conclusion: 通过符号化表示和显式状态跟踪，可以有效解决大语言模型在复杂心理理论推理中的逻辑一致性问题。
---

## 摘要
大语言模型（LLMs）在现有的心理理论（ToM）基准测试中的表现远低于人类水平，即使在使用思维链（CoT）提示或概率信念更新进行增强时也是如此。我们认为，这些失败主要源于不可靠的隐式状态追踪，而非高层推理能力的局限。我们提出了 PDDL-Mind，这是一个将环境状态演变与信念推理相解耦的神经符号框架。通过将叙述性描述转换为以规划领域定义语言（PDDL）表达的显式状态和动作，并根据预定义领域验证动作引起的状态转换，PDDL-Mind 为 LLMs 提供了逻辑一致且显式的世界状态表示，用于处理 ToM 任务。在 MMToM-QA、MuMA 和 FanToM 上的实验表明，PDDL-Mind 在 ToM 基准问题上比现有的最佳 SOTA 方法实现了超过 5% 的绝对准确率提升。

## Abstract
Large language models (LLMs) perform substantially below human level on existing theory-of-mind (ToM) benchmarks, even when augmented with chain-of-thought prompting or probabilistic belief updates. We argue that these failures primarily arise from unreliable implicit state tracking rather than limitations in high-level reasoning. We introduce PDDL-Mind, a neuro-symbolic framework that decouples environment state evolution from belief inference. By translating narrative descriptions into explicit states and actions expressed in Planning Domain Definition Language (PDDL), and by verifying action-induced state transitions against a predefined domain, PDDL-Mind provides LLMs with a logically consistent and explicit representation of world states for ToM tasks. Experiments on MMToM-QA, MuMA and FanToM show that PDDL-Mind achieves over 5% absolute accuracy gain over the best existing state-of-the-art method on ToM benchmark questions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PDDL-Mind** 的神经符号框架，旨在解决大语言模型（LLM）在心理理论（Theory-of-Mind, ToM）任务中表现不佳的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 LLM 在许多任务中表现出色，但在 ToM 任务（推断他人的信念、知识或意图）中仍远低于人类水平。
*   **研究动机**：关于 LLM 在 ToM 上的失败存在两种假设：一是**状态追踪（State Tracking）**失败（无法准确记录随时间变化的世界状态）；二是**推理（Reasoning）**失败（能跟踪事件但无法从中推断信念）。
*   **核心观点**：作者认为，当前的瓶颈主要在于**不可靠的隐式状态追踪**。如果能为模型提供显式、逻辑一致的状态表示，LLM 的推理能力其实足以应对复杂的 ToM 任务。

### 2. 方法论：PDDL-Mind 框架
PDDL-Mind 采用神经符号方法，将环境状态演变与高层信念推理进行解耦。
*   **预定义 PDDL 领域**：定义了一个通用的 PDDL 领域文件，包含 4 种对象类型（agent, loc, obj, utterance）、多种谓词（如 `at`, `seen`, `heard`）以及 7 种核心动作（`move`, `open`, `close`, `grab`, `drop`, `ask`, `tell`）。
*   **状态与动作提取**：
    1.  使用 LLM 将叙述性文本解析为 PDDL 问题文件（初始状态 $s_0$）。
    2.  将叙述中的事件翻译为一系列候选 PDDL 动作序列。
*   **符号验证（核心步骤）**：通过 PDDL 引擎验证每个动作的前置条件。只有满足条件的动作才会被执行并更新状态，不合法的动作被过滤。这确保了生成的状态序列在逻辑上是自洽的。
*   **ToM 问答**：将经过验证的显式状态序列提供给 LLM，由其通过思维链（CoT）进行最终的信念推理和问答。

### 3. 实验设计
*   **数据集/场景**：
    *   **MMToM-QA**：关注长上下文中的动作序列、信念和目标推断。
    *   **MuMa-ToM**：多智能体环境下的动作与对话信念推断。
    *   **FanToM**：多智能体对话中的信念追踪。
*   **对比方法（Baselines）**：
    *   **概率/贝叶斯方法**：BIP-ALM, LIMP, AutoToM（之前的 SOTA）。
    *   **假设驱动方法**：ThoughtTracing (TT)。
    *   **神经知识库方法**：EnigmaToM。
    *   **基础方法**：Vanilla CoT, DWM（分段处理）。
*   **使用模型**：GPT-4o, Claude-3.5-Sonnet (文中称为 4.5), Kimi-K2-Instruct。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。
*   **推理成本**：论文强调了**成本效率**。PDDL-Mind 每个样本仅需 **3 次 LLM 调用**，而之前的 SOTA 方法 AutoToM 平均需要 27 次以上调用。这表明该方法在提升性能的同时大幅降低了计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流 ToM 基准测试上进行了全面评估，涵盖了单智能体、多智能体、物理动作和对话等多种场景。
*   **消融实验**：作者进行了针对“预定义领域”和“符号验证步骤”的消融实验，证明了这两个组件对性能提升的关键作用。
*   **错误分析**：手动分析了失败案例，发现 33% 源于解析错误，67% 源于纯推理错误，这种深入分析增强了实验的客观性和说服力。

### 6. 主要结论与发现
*   **性能突破**：PDDL-Mind 在 MMToM-QA 和 MuMa-ToM 上比之前的 SOTA 提高了 **5% 以上** 的绝对准确率。
*   **瓶颈确认**：实验证明，提供显式状态后，LLM 的 ToM 表现大幅提升，证实了**状态追踪是当前的主要瓶颈**。
*   **状态追踪质量**：手动检查显示，PDDL-Mind 生成的状态序列准确率高达 **90%**。

### 7. 优点
*   **逻辑一致性**：通过 PDDL 强制执行物理和社会逻辑（如：没打开箱子就看不见里面的东西），消除了 LLM 常见的幻觉。
*   **高效率**：相比于需要多次迭代和概率采样的复杂框架，PDDL-Mind 更加简洁且节省 API 调用成本。
*   **通用性**：预定义的 7 种动作涵盖了大多数现有 ToM 基准测试的语义核心。

### 8. 不足与局限
*   **模态限制**：目前仅处理文本输入，无法直接应用于包含视觉、空间或具身感知的多模态 ToM 场景。
*   **解析依赖**：框架的有效性高度依赖于 LLM 将自然语言准确解析为 PDDL 的能力。对于较弱的模型，解析错误可能成为新的瓶颈。
*   **领域闭合**：依赖预定义的 PDDL 领域。如果遇到超出这 7 种动作范围的复杂叙述，框架可能需要手动扩展领域定义，存在一定的脆弱性。

（完）
