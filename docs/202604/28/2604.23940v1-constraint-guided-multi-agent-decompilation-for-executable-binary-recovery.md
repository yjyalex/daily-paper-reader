---
title: Constraint-Guided Multi-Agent Decompilation for Executable Binary Recovery
authors: "Yifan Zhang, Xiaohan Wang, Yueke Zhang, Kevin Leach"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23940v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于代码生成和迭代优化的多智能体框架
tldr: 针对现有反编译器生成的代码难以编译或运行的问题，本文提出了一种基于约束引导的多智能体框架（MCGD）。该框架通过语法、编译和行为等三层约束验证流水线，利用专门的LLM智能体对反编译代码进行迭代优化。实验表明，该方法在ExeBench数据集上显著提升了代码的可执行率，优于现有的LLM反编译方法，为二进制代码恢复提供了高效且低成本的解决方案。
selection_source: fresh_fetch
motivation: 现有反编译器生成的源代码往往无法直接编译或保持原始程序的执行行为，限制了其实际应用价值。
method: 提出一种多层级约束引导的反编译框架，通过语法解析、GCC编译和LLM生成的测试用例进行行为验证，并由多智能体协作修复错误。
result: "在1,641个真实二进制文件上，该框架实现了84-97%的可执行率，显著超越了LLM4Decompile等最先进的基准方法。"
conclusion: 约束引导的智能体迭代优化能有效弥补原始反编译输出与实用源代码之间的差距，且具有极高的成本效益。
---

## Abstract
Decompilation -- recovering source code from compiled binaries -- is essential for security analysis, malware reverse engineering, and legacy software maintenance. However, existing decompilers produce code that often fails to compile or execute correctly, limiting their practical utility. We present a multi-agent framework that transforms decompiled code into re-executable source through Multi-level Constraint-Guided Decompilation (MCGD). Our approach employs a hierarchical validation pipeline with three constraint levels: (1) syntactic correctness via parsing, (2) compilability via GCC, and (3) behavioral equivalence via LLM-generated test cases. When validation fails, specialized LLM agents iteratively refine the code using structured error feedback. We evaluate our framework on 1,641 real-world binaries from ExeBench across three decompilers (RetDec, Ghidra, and Angr). Our framework achieves 84-97% re-executability, improving baseline decompiler output by 28-89 percentage points. In comparison with state-of-the-art LLM-based decompilation methods using the same GPT-4o backbone, our approach (84.1%) outperforms LLM4Decompile (80.3%), SK2Decompile (73.9%), and SALT4Decompile (61.8%). Our ablation study reveals that execution-based validation is critical: compile-only approaches achieve 0% behavioral correctness despite 91-99% compilation rates. The system converges efficiently, with 90%+ binaries reaching correctness within 2 iterations at an average cost of $0.03-0.05 per binary. Our results demonstrate that constraint-guided agentic refinement can bridge the gap between raw decompiler output and practically useful source code.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Agent4Decompile** 的多智能体框架，旨在解决二进制反编译中生成的代码难以编译或运行的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的反编译器（如 Ghidra, Angr, RetDec）生成的 C 代码往往存在语法错误、类型不匹配或逻辑不一致，导致代码无法重新编译或执行。
*   **研究动机**：
    *   现有的 LLM 反编译方法多为“单次生成”（Single-pass），缺乏反馈机制，无法保证行为正确性。
    *   反编译是一个“信息丢失”的过程，仅靠编译成功（L2）并不代表逻辑正确（L3），两者之间存在巨大的“语义鸿沟”。
    *   安全分析、自动化打补丁和模糊测试等下游任务迫切需要**可重新执行**（Re-executable）的源代码。

### 2. 方法论
核心思想是**约束引导的迭代修复**，将反编译视为一个逐步缩小搜索空间的过程。
*   **三层约束体系**：
    1.  **L1 语法约束 (Syntax)**：确保代码符合 C 语法，能被解析器识别。反馈信息为 GCC 的解析错误。
    2.  **L2 编译约束 (Compilation)**：确保代码能成功编译。反馈信息为 GCC 的类型检查和符号解析错误。
    3.  **L3 执行约束 (Execution)**：确保代码在测试用例下的输出与原始二进制文件一致。反馈信息为失败的输入/输出对、差异对比（Diff）及诊断清单。
*   **多智能体协作**：
    *   框架包含三个专门的 LLM 智能体（语法修复、编译修复、执行修复），分别处理对应层级的错误反馈。
    *   **算法流程**：原始反编译代码 $\rightarrow$ 验证层级（L1-L3） $\rightarrow$ 若失败，对应智能体根据错误信息修复 $\rightarrow$ 迭代（上限 5-7 次） $\rightarrow$ 输出最终代码。

### 3. 实验设计
*   **数据集**：
    *   **157-Binary Benchmark**：包含 ExeBench 的 125 个二进制文件和 32 个自定义算法程序，用于深入分析。
    *   **1,641-Binary Benchmark**：大规模评估，涵盖 O0-O3 四种优化等级，包含真实开源项目代码。
*   **对比方法**：
    *   **基准工具**：Ghidra、Angr、RetDec 的原始输出。
    *   **SOTA 方法**：DeepSeek 单次生成、微调模型 LLM4Decompile-6.7B-v2、以及模拟的 SK2Decompile（侧重可读性的两阶段方法）。
*   **评估指标**：语法通过率（L1）、编译成功率（L2）、**可执行率（L3，核心指标）**。

### 4. 资源与算力
*   **模型使用**：默认使用 **DeepSeek V3.2** 作为底层 LLM 智能体。
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该框架主要基于现有的 LLM API 进行推理和迭代修复，而非从头训练模型。实验环境使用了 Python 的多进程并行处理以提高效率。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 1,600 个二进制文件的测试，是目前反编译领域规模最大的实验之一。
*   **充分性**：
    *   涵盖了三种不同架构的反编译器（规则型、提升型、ML型）。
    *   进行了详细的**消融实验**，验证了 L1、L2、L3 每一层约束的贡献。
    *   测试了不同优化等级（O0-O3）对结果的影响。
    *   **客观性**：通过自动化测试框架和预定义的 I/O 对进行验证，减少了人工评估的主观性。

### 6. 主要结论与发现
*   **性能提升**：Agent4Decompile 将可执行率从基准的 22-26% 提升至 **43-50%**，相比 SOTA 方法提升了 15-38 个百分点。
*   **编译不等于正确**：仅通过编译（L1+L2）的代码虽然编译率接近 100%，但可执行率仅为 32-42%。这证明了 **L3 执行反馈是实现语义正确性的关键**。
*   **收敛效率**：大多数成功的修复在 **2 次迭代**内即可完成，具有较高的实用价值。
*   **安全应用**：该框架能 100% 保留漏洞模式，并将代码的可模糊测试性（Fuzzability）提升了 6.6 倍。

### 7. 优点
*   **闭环反馈**：引入了执行层面的动态反馈，弥补了静态反编译的不足。
*   **通用性强**：不依赖特定的反编译器或 LLM，可作为现有工具的增强插件。
*   **成本效益**：通过迭代而非大规模微调，实现了优于微调模型的效果。

### 8. 不足与局限
*   **范围限制**：目前主要针对**单函数**反编译，尚未解决跨函数调用和全局状态的复杂全程序反编译。
*   **依赖测试用例**：L3 验证需要输入/输出对，在缺乏测试数据的极端逆向场景下可能受限。
*   **语义漂移风险**：LLM 在修复时可能会为了通过测试而“过度拟合”测试用例，导致生成的逻辑虽然通过测试但与原意有细微偏差。
*   **混淆处理**：未针对加壳或高度混淆的二进制文件进行专门优化。

（完）
