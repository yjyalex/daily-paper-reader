---
title: "MPAC: A Multi-Principal Agent Coordination Protocol for Interoperable Multi-Agent Collaboration"
title_zh: MPAC：一种用于互操作多智能体协作的多主体智能体协调协议
authors: "Kaiyang Qian, Xinmin Fang, Zhengxiong Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09744v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多主体智能体协作的协调协议
tldr: "针对当前AI智能体协议（如MCP、A2A）仅支持单一主体控制的局限，本文提出MPAC协议，旨在解决多独立主体智能体在共享状态下的协作难题。MPAC通过五层架构（会话、意图、操作、冲突、治理）实现显式协调，支持意图声明、冲突结构化表示及人工干预。实验显示，该协议在多智能体代码审查中显著降低了95%的协调开销，并将执行速度提升了4.8倍，为跨组织智能体协作提供了标准化的互操作框架。"
selection_source: fresh_fetch
motivation: 现有的智能体协议假设单一主体控制，无法有效处理多个独立主体的智能体在共享资源或决策时的冲突与协作。
method: 设计了涵盖五层架构的MPAC协议，通过意图声明、Lamport时钟因果水印和乐观并发控制等机制实现结构化协调。
result: "实验表明，MPAC在多智能体协作任务中将协调开销降低了95%，并实现了4.8倍的执行加速。"
conclusion: MPAC为跨组织、多主体的智能体协作提供了标准化的互操作协议，显著提升了复杂协作场景下的效率和可靠性。
---

## 摘要
AI 智能体生态系统已收敛于两种协议：用于工具调用的模型上下文协议 (MCP) 和用于单主体任务委派的智能体间协议 (A2A)。两者都假设存在单一控制主体，即一个人或组织拥有所有智能体。当独立主体的智能体必须就共享状态进行协调时（例如工程师的代码智能体编辑同一个代码库、家庭成员规划共同旅行，或来自不同组织的智能体协商共同决策），这两种协议均不适用，协调会退化为即时聊天、手动合并或静默覆盖。我们提出了 MPAC（多主体智能体协调协议），这是一种应用层协议，通过五个层级（会话层、意图层、操作层、冲突层和治理层）的显式协调语义填补了这一空白。MPAC 将意图声明作为行动的前提，将冲突表示为一等结构化对象，并通过可插拔的治理层支持人机协同仲裁。该规范定义了 21 种消息类型、三个带有规范转换表的有限状态机、Lamport 时钟因果水印、两种执行模型、三种安全配置文件以及共享状态上的乐观并发控制。我们发布了 Python 和 TypeScript 的两个可互操作参考实现，包含 223 个测试、一个 JSON Schema 套件和七个实时多智能体演示。一项受控的三智能体代码审查基准测试显示，与序列化的人工调解基准相比，协调开销减少了 95%，实际运行速度提高了 4.8 倍，同时保留了每个智能体的决策时间。速度提升源于消除了协调等待，而非压缩模型调用。规范、实现和演示均已开源。

## Abstract
The AI agent ecosystem has converged on two protocols: the Model Context Protocol (MCP) for tool invocation and Agent-to-Agent (A2A) for single-principal task delegation. Both assume a single controlling principal, meaning one person or organization that owns every agent. When independent principals' agents must coordinate over shared state, such as engineers' coding agents editing the same repository, family members planning a shared trip, or agents from different organizations negotiating a joint decision, neither protocol applies, and coordination collapses to ad-hoc chat, manual merging, or silent overwrites. We present MPAC (Multi-Principal Agent Coordination Protocol), an application-layer protocol that fills this gap with explicit coordination semantics across five layers: Session, Intent, Operation, Conflict, and Governance. MPAC makes intent declaration a precondition for action, represents conflicts as first-class structured objects, and supports human-in-the-loop arbitration through a pluggable governance layer. The specification defines 21 message types, three state machines with normative transition tables, Lamport-clock causal watermarking, two execution models, three security profiles, and optimistic concurrency control on shared state. We release two interoperable reference implementations in Python and TypeScript with 223 tests, a JSON Schema suite, and seven live multi-agent demos. A controlled three-agent code review benchmark shows a 95 percent reduction in coordination overhead and a 4.8 times wall-clock speedup versus a serialized human-mediated baseline, with per-agent decision time preserved. The speedup comes from eliminating coordination waits, not compressing model calls. Specification, implementations, and demos are open source.

---

## 论文详细总结（自动生成）

### 论文总结：MPAC 多主体智能体协调协议

#### 1. 核心问题与整体含义（研究动机和背景）
当前 AI 智能体协议（如 Anthropic 的 MCP 和 Google 的 A2A）存在一个核心假设：**单一主体（Single-Principal）**。即系统中的所有智能体都属于同一个所有者或组织，存在天然的信任和统一的指挥链。
然而，在现实场景中（如不同公司的工程师协作代码、家庭成员共同规划旅行），智能体往往代表不同的独立主体。在这种**多主体（Multi-Principal）**环境下，缺乏统一的决策者，现有的协议无法处理资源竞争、语义冲突或权限争议，导致协作退化为低效的即时聊天或产生静默的数据覆盖。MPAC 协议旨在填补这一空白，为跨组织、跨主体的智能体协作提供标准化的协调层。

#### 2. 方法论：核心思想与关键技术
MPAC 是一种应用层协议，其核心思想是**“行动前声明意图”**和**“冲突作为一等公民”**。
*   **五层逻辑架构**：
    1.  **会话层 (Session)**：管理成员身份、角色分配和心跳。
    2.  **意图层 (Intent)**：要求智能体在操作前声明目标和资源范围（Scope），以便预先检测冲突。
    3.  **操作层 (Operation)**：支持预提交（Pre-commit）和后提交（Post-commit）模型，利用乐观并发控制（OCC）和 SHA-256 哈希校验状态一致性。
    4.  **冲突层 (Conflict)**：将冲突结构化为包含类别、严重程度和参与方的对象，而非简单的错误提示。
    5.  **治理层 (Governance)**：定义权限策略，支持人工干预和仲裁。
*   **关键技术细节**：
    *   **因果排序**：使用 Lamport 时钟和协调器纪元（Coordinator Epoch）确保消息的全局全序。
    *   **状态机控制**：为意图、操作和冲突分别定义了严格的有限状态机（FSM）及转换表。
    *   **21 种消息类型**：涵盖从 `HELLO` 到 `CONFLICT_ESCALATE` 的完整协作生命周期。

#### 3. 实验设计
论文通过七个实时多智能体场景验证了协议的有效性，使用 **Anthropic Claude** 作为智能体后端：
*   **主要场景**：
    1.  **并发代码编辑**：测试乐观并发控制和冲突解决。
    2.  **家庭旅行规划**：测试多主体任务集协商和原子化批量提交。
    3.  **性能基准测试（Benchmark）**：三智能体跨模块 PR 审查。
    4.  **故障恢复**：模拟智能体崩溃后的意图认领（Intent Claim）。
    5.  **冲突升级**：测试向人工或 AI 仲裁者升级争议。
*   **对比基准（Baseline）**：在 PR 审查任务中，对比了“传统模式”（序列化的人工中介协作）与“MPAC 模式”（基于协议的并行协作）。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 型号或训练时长，因为 MPAC 是一个通信协议而非模型训练算法。
*   **推理后端**：实验使用了 Anthropic 的 Claude API（具体为 `claude-3-5-sonnet`）作为智能体的决策引擎。
*   **实现规模**：提供了 Python（约 3500 行代码）和 TypeScript（约 2900 行代码）两个参考实现。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 7 组分布式验证场景，涵盖了协议定义的所有 21 种消息类型。
*   **测试充分性**：
    *   包含 **223 个自动化测试用例**，其中 **66 个是对抗性测试**（专门针对绕过权限、重放攻击等安全漏洞）。
    *   进行了跨语言互操作性测试，确保 Python 和 TypeScript 实现能无缝通信。
*   **客观性评价**：实验设计较为客观，特别是性能基准测试中区分了“决策时间”和“协调开销”，证明了提速并非来自模型推理变快，而是来自协议减少了等待。但性能基准测试仅基于单次受控运行，缺乏大规模统计分布。

#### 6. 主要结论与发现
*   **效率提升**：在三智能体协作任务中，MPAC 将协调开销从 68.65 秒降低至 3.02 秒（**减少 95.6%**）。
*   **执行加速**：端到端运行时间缩短了 **4.8 倍**（131.76 秒降至 27.38 秒）。
*   **工作量保持**：每个智能体的实际决策时间基本保持不变（约 60 秒），证明协议成功实现了任务的并行化。
*   **互操作性**：证明了不同语言编写的智能体系统可以通过 JSON Schema 定义的统一线缆格式进行复杂协商。

#### 7. 优点（亮点）
*   **填补空白**：首次明确定义并解决了“多主体”智能体协作的协议层问题。
*   **结构化冲突处理**：将冲突视为可协商、可追溯的对象，而非系统崩溃或静默失败。
*   **人机协同**：内置了治理层和人工覆盖机制，符合实际工业应用中对安全和责任归属的要求。
*   **工程完备性**：不仅有理论，还提供了双语言参考实现、详尽的测试套件和 JSON Schema。

#### 8. 不足与局限
*   **统计样本量**：核心性能基准测试基于单次运行，虽然量级差异显著，但仍需更多样本以排除 LLM 随机性的影响。
*   **单点故障**：目前的协调器（Coordinator）是单点架构，虽然有快照恢复机制，但尚未实现完全去中心化的多协调器共识。
*   **语义复杂度**：资源范围（Scope）的定义目前较简单（文件集、任务集），对于更复杂的语义重叠（如逻辑上的函数调用依赖）检测能力有限。
*   **安全假设**：目前假设协调器是诚实的（Crash-stop 模型），尚未处理协调器恶意行为（Byzantine 模型）的情况。

（完）
