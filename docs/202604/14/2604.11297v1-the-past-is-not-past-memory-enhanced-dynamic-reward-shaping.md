---
title: "The Past Is Not Past: Memory-Enhanced Dynamic Reward Shaping"
title_zh: 过去并未过去：记忆增强的动态奖励塑造
authors: "Yang Liu, Enxi Wang, Yufei Gao, Weixin Zhang, Bo Wang, Zhiyuan Zeng, Yikai Zhang, Yining Zheng, Xipeng Qiu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11297v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型强化学习中的记忆增强动态奖励塑造
tldr: 针对大语言模型强化学习中采样多样性不足、易重复错误模式的问题，本文提出MEDS框架。该方法通过存储历史Rollout的中间表示，利用密度聚类识别频繁出现的错误模式，并动态调整奖励函数，对属于高频错误簇的样本进行惩罚。实验表明，MEDS在多个数据集和模型上显著提升了性能和行为多样性，有效减少了重复性错误。
selection_source: fresh_fetch
motivation: 传统的熵正则化无法显式抑制跨Rollout的重复失败模式，导致模型在强化学习过程中陷入采样多样性降低的困境。
method: 提出MEDS框架，通过存储中间表征并利用密度聚类识别高频错误模式，对落入这些模式的Rollout实施动态奖励惩罚。
result: "在5个数据集和3个基座模型上，MEDS相比基线最高提升了4.13个pass@1点数，并显著增强了采样的行为多样性。"
conclusion: 结合历史行为记忆的动态奖励塑造能有效引导模型避开重复错误，是提升大模型强化学习效率和泛化能力的有效途径。
---

## 摘要
尽管强化学习在大语言模型中取得了成功，但一种常见的失败模式是采样多样性降低，即策略反复生成类似的错误行为。经典的熵正则化鼓励当前策略下的随机性，但并未明确抑制跨采样序列（rollouts）的重复失败模式。我们提出了 MEDS，一种记忆增强的动态奖励塑造框架，将历史行为信号融入奖励设计中。通过存储和利用中间模型表示，我们捕捉了过去采样序列的特征，并使用基于密度的聚类来识别频繁出现的错误模式。被分配到更普遍错误簇的采样序列会受到更重的惩罚，从而在减少重复错误的同时鼓励更广泛的探索。在五个数据集和三个基础模型上，MEDS 相比现有基准一致地提高了平均性能，在 pass@1 和 pass@128 指标上分别实现了高达 4.13 和 4.37 个百分点的提升。使用基于大语言模型的标注和定量多样性指标进行的额外分析表明，MEDS 增加了采样过程中的行为多样性。

## Abstract
Despite the success of reinforcement learning for large language models, a common failure mode is reduced sampling diversity, where the policy repeatedly generates similar erroneous behaviors. Classical entropy regularization encourages randomness under the current policy, but does not explicitly discourage recurrent failure patterns across rollouts. We propose MEDS, a Memory-Enhanced Dynamic reward Shaping framework that incorporates historical behavioral signals into reward design. By storing and leveraging intermediate model representations, we capture features of past rollouts and use density-based clustering to identify frequently recurring error patterns. Rollouts assigned to more prevalent error clusters are penalized more heavily, encouraging broader exploration while reducing repeated mistakes. Across five datasets and three base models, MEDS consistently improves average performance over existing baselines, achieving gains of up to 4.13 pass@1 points and 4.37 pass@128 points. Additional analyses using both LLM-based annotations and quantitative diversity metrics show that MEDS increases behavioral diversity during sampling.

---

## 论文详细总结（自动生成）

这篇论文由复旦大学和上海人工智能实验室的研究团队提出，旨在解决大语言模型（LLM）在强化学习（RL）过程中容易陷入“错误陷阱”和采样多样性降低的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在基于策略的强化学习（如 PPO 或 GRPO）中，模型往往会发生“策略崩溃”，即反复生成类似的错误推理路径。
*   **现有局限**：传统的熵正则化（Entropy Regularization）仅鼓励当前策略分布的随机性，但无法识别并抑制跨越多个训练轮次的重复失败模式。
*   **核心目标**：受人类学习中“对重复错误施加心理惩罚”的启发，论文提出了一种**记忆增强的动态奖励塑造（MEDS）**框架，通过记录并惩罚历史中反复出现的错误模式，引导模型探索更广泛的解空间。

### 2. 方法论
MEDS 框架包含三个关键步骤：
*   **逻辑特征提取（Logic Feature Extraction）**：
    *   为了高效表征推理路径，研究者直接复用模型前向传播中产生的**层级 Logits**。
    *   具体做法是提取答案第一个 Token 处、模型后半部分 Transformer 层的 Logits，拼接成特征向量。这被视为模型内部推理逻辑的“指纹”。
*   **基于记忆的聚类（Memory-based Clustering）**：
    *   为每个 Prompt 维护一个历史错误特征存储库 $G_x$。
    *   使用 **HDBSCAN**（一种基于密度的聚类算法）对这些特征进行在线聚类，识别出频繁出现的错误模式（即较大的簇）。
*   **动态奖励塑造（Reward Shaping）**：
    *   **公式**：$\tilde{r}(x, \tilde{y}) = r(x, \tilde{y}) - \text{penalty}$。
    *   **惩罚项**：$\text{penalty} = \min(\alpha \log(|C_k| + 1), \beta)$，其中 $|C_k|$ 是该错误所属簇的大小。
    *   **含义**：如果一个错误属于一个已经包含很多类似错误的历史簇，则施加更重的惩罚。
*   **理论支撑**：论文通过数学证明（Theorem 1）显示，对重复错误施加额外惩罚可以重新分配概率质量，且在优化视角下不会降低期望回报。

### 3. 实验设计
*   **数据集**：涵盖 5 个数学推理基准：AIME24、AMC23、MATH500、Minerva 和 OlympiadBench。
*   **基座模型**：Qwen3-1.7B、Qwen3-8B 和 Qwen2.5-Math-7B。
*   **对比方法（Baselines）**：
    *   **GRPO**：DeepSeek 提出的组相对策略优化。
    *   **DAPO**：一种先进的开源 LLM 强化学习系统。
    *   **GRPO w/ Entropy Adv**：带有熵增强优势函数的 GRPO（鼓励探索的变体）。
*   **评估指标**：pass@1、pass@128、行为多样性评分（LLM 评分）以及表征空间的 Top-1 特征值占比。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H200** GPU 上进行。
*   **效率**：MEDS 引入的计算开销极小。在 Qwen2.5-Math-7B 上，100 个训练步长仅需约 9.73 分钟，而基线 DAPO 需 8.95 分钟。
*   **参数设置**：Prompt 级 Batch Size 为 512，每个 Prompt 生成 16 个 Rollout。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同规模和类型的模型、5 个主流数据集上进行了全面测试，实验设计较为充分。
*   **消融实验**：研究了不同的 Logits 聚合策略（如全层 vs 后 14 层、差分 Logits vs 直接聚合），验证了特征选择的合理性。
*   **客观性分析**：通过 LLM（Claude-Haiku-4.5）作为第三方评估器，对采样多样性进行了定性与定量分析，并验证了 Logits 聚类与语义聚类之间的高度相关性（61.2% 相关度）。

### 6. 主要结论与发现
*   **性能提升**：MEDS 在所有模型和数据集上均优于基线。在 Qwen3-8B 上，pass@1 最高提升 4.13 点，pass@128 最高提升 4.37 点（相对提升达 17%）。
*   **多样性增强**：MEDS 显著提高了“步内多样性”和“跨步多样性”，有效防止了模型陷入单一的错误推理路径。
*   **表征有效性**：实验证明层级 Logits 能够有效反映模型的内部推理逻辑，且聚类质量与下游任务性能呈正相关。

### 7. 优点
*   **轻量高效**：无需引入额外的奖励模型或复杂的语义分析工具，直接利用现有的 Logits，计算成本极低。
*   **动态性**：奖励函数随训练历史动态演变，能够自适应地处理模型在不同阶段产生的错误。
*   **理论与实践结合**：既有严谨的数学证明，又有直观的案例分析（Case Study）展示 Logits 如何捕捉推理结构。

### 8. 不足与局限
*   **任务局限性**：目前主要集中在数学推理（具有可验证奖励的任务），在主观性较强的开放域对话或写作任务中的适用性尚待验证。
*   **特征工程**：Logits 的聚合方式（直接拼接）相对简单，可能存在更精细的表征提取方法。
*   **超参数敏感性**：惩罚系数 $\alpha$ 和 $\beta$ 需要针对不同规模的模型进行微调。

（完）
