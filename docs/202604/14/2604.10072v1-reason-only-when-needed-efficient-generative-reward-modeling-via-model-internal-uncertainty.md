---
title: "Reason Only When Needed: Efficient Generative Reward Modeling via Model-Internal Uncertainty"
title_zh: 仅在需要时推理：通过模型内部不确定性实现高效生成式奖励建模
authors: "Chao Xue, Yao Wang, Mengqiao Liu, Di Liang, Xingsheng Han, Peiyang Liu, Xianjie Wu, Chenyao Lu, Lei Jiang, Yu Lu, Haibo Shi, Shuang Liang, Minlong Peng, Flora D. Salim"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10072v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过不确定性实现高效的推理生成式奖励模型
tldr: 本研究针对生成式奖励模型（GRM）在推理任务中全量使用思维链（CoT）导致的计算冗余及评估粒度不足问题，提出了E-GRM框架。该框架利用模型内部不确定性动态触发CoT推理，仅在必要时进行复杂思考，并引入轻量级判别式评分器，通过混合回归-排序目标提升奖励精度。实验证明，E-GRM在显著降低推理成本的同时，提升了多个基准测试的准确率，实现了高效且精准的奖励建模。
selection_source: fresh_fetch
motivation: 现有的生成式奖励模型对所有任务无差别使用CoT推理导致计算资源浪费，且缺乏对推理路径的精细化评估。
method: 提出E-GRM框架，利用模型内部不确定性选择性触发CoT，并结合混合回归-排序目标的轻量级评分器进行细粒度评估。
result: 在多个推理基准测试中，该方法在大幅减少推理开销的同时，一致性地提高了答案的准确性。
conclusion: 模型内部不确定性是实现高效且感知推理的奖励建模的一种有效且通用的信号。
---

## 摘要
生成式奖励模型（GRM）的最新进展表明，其具有通过思维链（CoT）提示增强大语言模型（LLM）推理能力的潜力。尽管取得了这些进展，现有的 GRM 实现仍面临两个关键局限。首先，CoT 提示被不加区分地应用于所有输入，而忽略了其固有的复杂性。这为那些可以通过快速、直接推理完成的任务引入了不必要的计算开销。其次，现有方法主要依赖基于投票的机制来评估 CoT 输出，在评估推理质量时往往缺乏细粒度和精确度。在本文中，我们提出了 E-GRM，这是一个基于模型内部不确定性的高效生成式奖励建模框架。E-GRM 利用并行模型生成的收敛行为来估计不确定性，并仅在需要时选择性地触发 CoT 推理，而不依赖于手工特征或任务相关的信号。为了提高奖励的保真度，我们引入了一个轻量级的判别式评分器，该评分器采用回归-排序混合目标进行训练，以提供对推理路径的细粒度评估。在多个推理基准测试上的实验表明，E-GRM 在显著降低推理成本的同时，持续提高了答案的准确性，证明了模型内部不确定性是实现高效推理感知奖励建模的一种有效且通用的信号。

## Abstract
Recent advancements in the Generative Reward Model (GRM) have demonstrated its potential to enhance the reasoning abilities of LLMs through Chain-of-Thought (CoT) prompting. Despite these gains, existing implementations of GRM suffer from two critical limitations. First, CoT prompting is applied indiscriminately to all inputs regardless of their inherent complexity. This introduces unnecessary computational costs for tasks amenable to fast, direct inference. Second, existing approaches primarily rely on voting-based mechanisms to evaluate CoT outputs, which often lack granularity and precision in assessing reasoning quality. In this paper, we propose E-GRM, an efficient generative reward modeling framework grounded in model-internal uncertainty. E-GRM leverages the convergence behavior of parallel model generations to estimate uncertainty and selectively trigger CoT reasoning only when needed, without relying on handcrafted features or task-dependent signals. To improve reward fidelity, we introduce a lightweight discriminative scorer trained with a hybrid regression--ranking objective to provide fine-grained evaluation of reasoning paths. Experiments on multiple reasoning benchmarks show that E-GRM substantially reduces inference cost while consistently improving answer accuracy, demonstrating that model-internal uncertainty is an effective and general signal for efficient reasoning-aware reward modeling.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **E-GRM**（Efficient Generative Reward Model）的框架，旨在解决生成式奖励模型在推理任务中的效率与精度平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：生成式奖励模型（GRM）通过思维链（CoT）显著提升了模型对复杂推理任务的评估能力。
*   **核心痛点**：
    1.  **计算冗余（效率挑战）**：现有的 GRM 对所有输入（无论难易）都无差别地应用 CoT，导致简单任务产生不必要的延迟和算力消耗。
    2.  **评估粗糙（保真度挑战）**：主流方法依赖投票机制（Majority Voting）来聚合结果，缺乏对推理路径质量的精细化判别，难以区分“对的答案但错的逻辑”。
*   **核心目标**：实现“仅在需要时推理”，即动态触发 CoT，并提供高精度的细粒度奖励信号。

### 2. 方法论：核心思想与关键技术
E-GRM 框架由两个核心组件驱动：
*   **基于模型内部不确定性的动态 CoT 触发**：
    *   **核心思想**：利用模型自身的生成行为作为难度信号。
    *   **技术细节**：对输入进行 $M$ 次并行解码（使用不同的采样参数）。计算答案的**一致性得分（Consensus）**。如果一致性高于阈值 $\tau$（如 0.8），则认为任务简单，直接输出；否则，触发复杂的 CoT 推理。
    *   **优势**：任务无关（Task-agnostic），无需手工设计的特征或外部难度评估器。
*   **混合损失函数的判别式评分器**：
    *   **核心思想**：引入轻量级评分模块 $S_\phi$，替代粗糙的投票机制。
    *   **损失函数**：结合了 **Huber Loss**（用于鲁棒回归，减少异常值影响）和 **Hinge Loss**（用于排序判别，强化对推理路径优劣的区分）。
*   **训练流程**：
    1.  **混合 SFT**：同时学习短路径（直接回答）和长路径（CoT 推理）。
    2.  **Coupled-GRPO（耦合组相对策略优化）**：在标准 GRPO 基础上扩展，显式利用成对偏好数据（Preferred vs. Rejected）来增强奖励信号的对比学习。

### 3. 实验设计
*   **数据集**：涵盖数学（MATH, Math-DPO-10K）、代码（Code-Preference-Pairs）、通用指令（UltraFeedback, HelpSteer2, OffsetBias）等。
*   **Benchmark**：
    *   **Reward-Bench**：评估多维度奖励能力。
    *   **RM-Bench**：侧重语义细微差别和风格偏见。
    *   **RMB**：模拟真实场景的对齐评估。
*   **对比方法**：
    *   **标量奖励模型**：如 Skywork-Reward, Internlm2-reward, Nemotron-340B。
    *   **生成式奖励模型**：如 GPT-4o, Claude-3-5-Sonnet, Llama-3.1-Instruct。
    *   **推理奖励模型**：如 DeepSeek-GRM。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **训练细节**：提到了 SFT 阶段 Batch Size 为 512，学习率为 $5 \times 10^{-6}$；奖励模型训练阶段 Batch Size 为 256。所有实验基于 3 次独立运行以确保统计可靠性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流 Benchmark 上进行了全面测试。
    *   模型规模覆盖了 7B、14B 到 32B（基于 Qwen 架构）。
    *   **消融实验**：针对动态触发机制、判别式评分器、以及扩展版 GRPO 算法分别做了对比实验。
*   **充分性评价**：实验设计较为充分，不仅对比了最终精度，还详细分析了推理延迟（Latency）和计算量（FLOPs）的下降，客观地展示了效率提升。

### 6. 主要结论与发现
*   **效率飞跃**：在 MATH 数据集上，动态触发机制识别出 58% 的样本为简单任务，使**推理延迟降低了 62%**，**FLOPs 减少了 49%**，且准确率未受损。
*   **精度领先**：32B 版本的 E-GRM 在 RM-Bench 上达到 79.2% 的 SOTA 性能，在 RMB 榜单上以 0.743 的得分超越了 GPT-4o。
*   **模型特性**：模型内部的不确定性（并行解码的一致性）是衡量推理必要性的极佳指标，优于基于规则或任务特定启发式的方法。

### 7. 优点（亮点）
*   **任务无关的通用性**：不需要为数学、代码等不同领域单独设计难度评估规则。
*   **细粒度评估**：通过混合损失函数训练的评分器，解决了传统 GRM 在复杂逻辑判断上的“粗线条”问题。
*   **端到端优化**：将效率优化（动态路由）与质量优化（判别评分）有机结合在一个框架内。

### 8. 不足与局限
*   **并行解码开销**：虽然减少了 CoT 的生成，但初始的 $M$ 次并行解码仍会带来约 5% 的额外延迟开销。
*   **阈值敏感性**：一致性阈值 $\tau$ 可能需要针对特定领域进行微调，论文中主要使用了经验值。
*   **评估维度限制**：效率分析主要集中在计算量和延迟，未深入探讨内存占用和能耗等实际部署指标。
*   **分布偏移风险**：判别式评分器在面对与其训练数据风格迥异的推理路径时，性能可能会下降。

（完）
