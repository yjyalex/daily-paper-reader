---
title: "RTMC: Step-Level Credit Assignment via Rollout Trees"
title_zh: RTMC：通过展开树实现的步级信用分配
authors: "Tao Wang, Suhang Zheng, Xiaoxiao Xu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11037v1"
tags: ["query:token"]
score: 10.0
evidence: 通过Rollout树进行智能体强化学习的步骤级信用分配
tldr: 针对多步智能体强化学习中信用分配不精确的问题，本文提出RTMC方法。传统GRPO缺乏细粒度分配，而学习价值网络开销大且不稳定。RTMC利用多个Rollout在中间状态的重合性构建“Rollout树”，通过聚合共享状态的收益统计量来估算每步的Q值和优势函数，无需训练评论家网络。实验显示其在SWE-bench Verified上性能显著提升。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在多步任务中要么信用分配过于粗糙，要么依赖于难以训练且开销巨大的价值网络。
method: 利用状态-动作签名系统识别不同Rollout中的重合状态并构建Rollout树，通过聚合共享状态的收益统计量直接计算每步优势函数。
result: "在SWE-bench Verified基准测试中，RTMC相比GRPO将pass@1准确率提升了3.2个百分点。"
conclusion: RTMC证明了通过Rollout树进行非参数化信用分配的有效性，为提升智能体在复杂任务中的学习效率提供了新路径。
---

## 摘要
多步智能体强化学习受益于细粒度的信用分配，但现有方法提供的选择有限：像 GRPO 这样无评论家（critic-free）的方法为轨迹中的每个动作分配统一的优势值，而学习到的价值网络则会引入显著的开销，且在稀疏奖励下可能表现脆弱。我们观察到，针对同一问题的组展开（group rollouts）通常会经过重叠的中间状态，隐式地形成一棵树，其分支在连续的决策点处分叉。基于这一洞察，我们提出了展开树蒙特卡洛（Rollout-Tree Monte Carlo, RTMC）优势估计，它通过聚合共享相同状态的展开轨迹中的回报统计数据，来生成每一步的 Q 值和优势值——且无需任何学习到的评论家。一种状态-动作签名系统将原始交互历史压缩为紧凑且可比较的表示，使得跨展开轨迹的状态匹配变得可行。在 SWE-bench Verified 基准测试中，RTMC 相比 GRPO 将 pass@1 提高了 3.2 个百分点。

## Abstract
Multi-step agentic reinforcement learning benefits from fine-grained credit assignment, yet existing approaches offer limited options: critic-free methods like GRPO assign a uniform advantage to every action in a trajectory, while learned value networks introduce notable overhead and can be fragile under sparse rewards. We observe that group rollouts targeting the same problem often traverse overlapping intermediate states, implicitly forming a tree whose branches diverge at successive decision points. Building on this insight, we introduce Rollout-Tree Monte Carlo (RTMC) advantage estimation, which aggregates return statistics across rollouts sharing a common state to produce per-step Q-values and advantages--without any learned critic. A state-action signature system compresses raw interaction histories into compact, comparable representations, making cross-rollout state matching tractable. On SWE-bench Verified, RTMC improves pass@1 by 3.2 percentage points over GRPO.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴团队提出，旨在解决大语言模型（LLM）智能体在多步强化学习（Agentic RL）中的信用分配（Credit Assignment）难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多步智能体任务中，如何精准地识别哪些步骤对最终成功有贡献，哪些步骤导致了失败？
*   **研究背景**：
    *   **现有方法的局限**：像 GRPO 这样无需评论家（Critic-free）的方法，会将整条轨迹的奖励平均分配给每个动作，无法区分“好步骤”和“坏步骤”；而传统的价值网络（Critic Network）在处理稀疏奖励和超大规模状态空间时，训练成本极高且表现脆弱。
    *   **智能体任务特性**：智能体与环境的交互是序列化的，一个错误的工具调用可能在很多步之后才显现后果。

### 2. 方法论：Rollout-Tree Monte Carlo (RTMC)
*   **核心思想**：利用针对同一问题的多个并行采样（Group Rollouts）在中间状态上的重叠性，隐式构建一棵“展开树”。通过统计共享同一状态的不同分支的回报（Return），直接计算每一步的 Q 值和优势函数（Advantage），从而实现无需训练 Critic 的细粒度信用分配。
*   **关键技术细节**：
    *   **状态-动作签名系统（Signatures）**：为了在不同轨迹中识别“相同”的状态，设计了一套抽象机制。包括：
        *   **动作签名**：按效果分类（如 view, modify, test），并对操作范围和内容进行哈希处理。
        *   **状态签名**：记录已操作的文件列表、操作类型、文件内容的 MD5 哈希以及推理步数，实现对复杂交互历史的压缩。
    *   **蒙特卡洛优势估计**：
        *   $Q(s, a)$：访问状态 $s$ 并执行动作 $a$ 的所有轨迹的平均回报。
        *   $V(s)$：访问状态 $s$ 的所有轨迹的加权平均回报。
        *   $A(s, a) = Q(s, a) - V(s)$：即为该步骤的优势值。
    *   **先验平滑（Prior-based Smoothing）**：针对仅被访问一次的孤立节点，引入贝叶斯收缩（Bayesian Shrinkage），将局部估计向该问题的整体成功率（先验）靠拢，避免优势值为零。
    *   **任务特定奖励塑造**：为鼓励验证行为，对代码修改后的测试动作给予微小奖励。

### 3. 实验设计
*   **数据集/场景**：专注于软件工程任务。
    *   **训练集**：R2E-Gym 的 8.1K 实例子集。
    *   **测试基准**：**SWE-bench Verified**（包含 500 个真实的 GitHub 问题）。
*   **对比方法**：
    1.  **Baseline**：未经过强化学习训练的预训练模型。
    2.  **GRPO**：仅使用轨迹级奖励的基准。
    3.  **GRPO + Step Reward**：在 GRPO 基础上加入简单的步骤级奖励（如工具调用正误）。
    4.  **RTMC (本文方法)**：基于树结构的步级优势估计。

### 4. 资源与算力
*   **算力投入**：使用了 **32 张 GPU**（具体型号未标明，但考虑到模型规模，推测为 A100 或 H100 级别）。
*   **模型规模**：使用 **Qwen3-Coder-30B-A3B-Instruct** 作为基础模型。
*   **训练配置**：每个训练步包含 8 个问题，每个问题进行 8 次展开（共 64 条轨迹）。

### 5. 实验数量与充分性
*   **实验规模**：在 500 个测试用例上进行了 3 次独立运行并取多数投票（Majority Vote），结果具有统计意义。
*   **消融实验**：
    *   进行了从 Baseline 到 RTMC 的递进式消融，证明了每个组件（步骤奖励、树结构估计）的增量贡献。
    *   专门针对“先验平滑”机制进行了消融，证明其对解决数据稀疏性至关重要。
*   **深度分析**：引入了“象限分析（Quadrant Analysis）”，对比了 RTMC 与传统方法在具体动作评分上的差异，直观展示了 RTMC 如何纠正被错误奖励的平庸动作。

### 6. 主要结论与发现
*   **性能提升**：RTMC 在 SWE-bench Verified 上的 pass@1 达到 **52.2%**，比 GRPO 高出 **3.2 个百分点**，比未训练基准高出 **5.4 个百分点**。
*   **信用分配更精准**：RTMC 能够识别出成功轨迹中的低质量步骤，并给予负向优势值；同时能发现失败轨迹中的闪光点。
*   **学习效率**：训练曲线显示，RTMC 比轨迹级 GRPO 收敛更快且最终性能更高。

### 7. 优点
*   **轻量化且高效**：无需额外训练和维护价值网络，计算开销极低（线性复杂度）。
*   **非参数化估计**：直接利用采样数据，避免了学习到的 Critic 在稀疏奖励下的偏差问题。
*   **可解释性强**：通过展开树可以直观地看到不同决策分支的优劣对比。

### 8. 不足与局限
*   **依赖人工设计签名**：状态和动作的签名系统需要较强的领域知识（如针对代码任务设计的哈希规则），难以直接迁移到完全不同的领域。
*   **状态隔离问题**：由于采样数有限（N=8），树中仍有大量节点是孤立的，导致真正的“跨轨迹对比”发生频率受限。
*   **绝对增益尚温和**：虽然相比 GRPO 有显著提升，但 3.2% 的绝对增益表明信用分配仍有进一步优化的空间（例如引入学习到的状态嵌入）。

（完）
