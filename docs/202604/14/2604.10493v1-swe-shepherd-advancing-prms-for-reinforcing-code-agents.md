---
title: "SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents"
title_zh: SWE-Shepherd：推进过程奖励模型以强化代码智能体
authors: "Mahir Labib Dihan, Md Ashrafur Rahman Khan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10493v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于强化代码智能体的过程奖励模型
tldr: 针对LLM智能体在处理复杂软件工程任务时缺乏中间决策反馈的问题，本文提出SWE-Shepherd框架。该框架引入过程奖励模型（PRM），通过在SWE-Bench轨迹上构建动作级奖励数据集并训练轻量级模型，为代码智能体提供细粒度的步骤监督。实验证明，该方法显著提升了智能体的交互效率和动作质量，为强化代码生成提供了新思路。
selection_source: fresh_fetch
motivation: 现有的代码智能体缺乏对中间决策的细粒度反馈，导致在处理长程软件工程任务时探索效率低下且错误易累积。
method: 提出SWE-Shepherd框架，利用SWE-Bench轨迹构建动作级奖励数据集，并训练轻量级PRM在推理阶段引导智能体选择高奖励动作。
result: 在SWE-Bench Verified上的实验表明，该方法有效提升了智能体的交互效率和动作质量。
conclusion: 引入过程奖励模型能显著增强代码智能体的决策能力，但将中间奖励与最终任务成功率完全对齐仍面临挑战。
---

## 摘要
自动化现实世界的软件工程任务对于基于大语言模型（LLM）的智能体而言仍然具有挑战性，因为这需要在庞大且不断演进的代码库上进行长程推理，并在相互依赖的操作之间做出一致的决策。现有方法通常依赖静态提示策略或手工设计的启发式方法来选择代码编辑、文件导航和测试执行等操作，但它们缺乏对中间决策的细粒度反馈。这导致了低效的探索、错误传播以及脆弱的解决方案轨迹。为了解决这一局限性，我们提出了 SWE-Shepherd，这是一个引入过程奖励模型（PRM）的框架，旨在为仓库级代码智能体提供密集的步骤级监督。利用来自 SWE-Bench 的轨迹，我们构建了一个操作级奖励数据集，并在基础 LLM 上训练了一个轻量级奖励模型，用于评估中间操作的效用。在推理过程中，PRM 评估候选操作并引导智能体做出更高奖励的决策，而无需进行完整的强化学习。在 SWE-Bench Verified 上的实验证明了交互效率和操作质量的提升，同时也揭示了将中间奖励与最终任务成功对齐所面临的挑战。

## Abstract
Automating real-world software engineering tasks remains challenging for large language model (LLM)-based agents due to the need for long-horizon reasoning over large, evolving codebases and making consistent decisions across interdependent actions. Existing approaches typically rely on static prompting strategies or handcrafted heuristics to select actions such as code editing, file navigation, and test execution, but they lack fine-grained feedback on intermediate decisions. This leads to inefficient exploration, error propagation, and brittle solution trajectories. To address this limitation, we propose SWE-Shepherd, a framework that introduces Process Reward Models (PRMs) to provide dense, step-level supervision for repository-level code agents. Using trajectories from SWE-Bench, we construct an action-level reward dataset and train a lightweight reward model on a base LLM to estimate the usefulness of intermediate actions. During inference, the PRM evaluates candidate actions and guides the agent toward higher-reward decisions without requiring full reinforcement learning. Experiments on SWE-Bench Verified demonstrate improved interaction efficiency and action quality, while also highlighting challenges in aligning intermediate rewards with final task success.

---

## 论文详细总结（自动生成）

以下是对论文《SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：利用大语言模型（LLM）自动化处理现实世界的软件工程任务（如修复 Bug、代码修改）极具挑战。这类任务通常涉及长程推理、大规模代码库交互以及一系列相互依赖的决策。
*   **核心问题**：现有的代码智能体（Code Agents）多依赖静态提示词或手工设计的启发式规则，缺乏对中间决策步骤的细粒度反馈。这导致智能体在探索过程中容易产生错误累积、效率低下且方案脆弱。
*   **研究目的**：引入**过程奖励模型（Process Reward Models, PRMs）**，为仓库级代码智能体提供密集的、步骤级的监督信号，以引导其做出更有效的决策。

### 2. 方法论
SWE-Shepherd 框架包含以下六个关键阶段：
1.  **任务收集**：从 SWE-Bench 数据集中提取 1,794 个任务作为训练集。
2.  **轨迹生成**：使用基准 LLM 智能体尝试解决任务，记录包含操作（读文件、编辑、运行测试）和观察结果的交互轨迹。
3.  **奖励计算**：基于启发式规则为中间步骤分配标量奖励。规则包括：执行是否成功、是否访问了相关文件、是否修改了目标文件、测试结果以及是否避免了重复操作。同时引入折扣累积奖励来捕捉长期贡献。
4.  **数据集构建**：将轨迹转化为监督学习样本，包含问题描述、交互历史、候选动作及归一化后的奖励值（0 到 1），共计 15,000 多个样本。
5.  **PRM 训练**：在预训练 LLM 基础上添加 MLP 头部，利用 **qLoRA** 进行参数高效微调，通过均方误差（MSE）损失函数优化模型，使其能预测步骤级奖励。
6.  **奖励引导推理**：在推理阶段，PRM 对每个步骤的多个候选动作进行评分，智能体优先选择得分最高的动作，实现无需完整强化学习（RL）的奖励引导搜索。

### 3. 实验设计
*   **数据集**：从 **SWE-Bench Verified** 中随机抽取 100 个任务进行评估。
*   **Benchmark 指标**：解决率（% Resolved）、平均成本（Avg. $）、平均交互步数（Avg. Steps）。
*   **对比方法**：
    *   **mini-SWE-Agent**：不含显式搜索或奖励模型的顺序决策基准智能体。
    *   **SWE-Search**：结合蒙特卡洛树搜索（MCTS）的搜索增强型框架。
    *   **SWE-Shepherd（本文方法）**：在 mini-SWE-Agent 基础上增加 PRM 进行动作评分与选择。

### 4. 资源与算力
*   **模型规模**：使用了 1.5B 参数规模的基础模型进行 PRM 训练。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练时长。但提到了使用 qLoRA 技术，这表明该方法对算力的需求相对较低，具有较好的轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 1,794 个任务和 1.5 万个样本；评估集为 100 个任务。
*   **充分性评价**：
    *   实验对比了基础智能体和复杂的搜索型智能体，具有一定的代表性。
    *   进行了奖励分析（对比已解决和未解决任务的平均奖励），探讨了模型预测与实际成功率的相关性。
    *   **局限性**：评估仅针对 100 个任务（而非完整的 500 个 Verified 任务），且对比的方法数量较少，实验的广度仍有提升空间。

### 6. 主要结论与发现
*   **效率提升**：SWE-Shepherd 显著减少了交互步数（从 15.2 步降至 12.2 步），表明 PRM 能引导智能体进行更具针对性的探索。
*   **性能权衡**：虽然效率提高，但解决率（51%）略低于 mini-SWE-Agent（57%）。这表明局部高奖励的操作并不总是能转化为全局正确的补丁。
*   **对齐挑战**：已解决任务（0.4894）与未解决任务（0.4818）的平均奖励差异极小，说明目前的启发式奖励函数与最终任务成功之间的相关性较弱。

### 7. 优点
*   **密集反馈**：通过 PRM 解决了强化学习中稀疏奖励的问题，提供了更细粒度的行为指导。
*   **轻量高效**：相比于昂贵的 MCTS 搜索（SWE-Search），SWE-Shepherd 在保持较高解决率的同时，大幅降低了推理成本。
*   **实用性强**：框架设计简洁，易于在现有的代码智能体流程中集成。

### 8. 不足与局限
*   **奖励函数偏差**：依赖手工设计的启发式规则计算奖励，可能导致智能体倾向于“看起来正确”的局部操作，而非真正解决问题。
*   **性能回退**：在某些情况下，PRM 的引导反而降低了最终的任务解决率，说明中间奖励与最终目标的对齐（Alignment）仍是核心难点。
*   **评估范围有限**：实验样本量较小，可能无法完全反映模型在处理极其复杂或罕见软件工程问题时的鲁棒性。

（完）
