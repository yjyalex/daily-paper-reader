---
title: "Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning"
title_zh: 代理式视频生成：通过工具约束的 LLM 规划实现从文本到可执行事件图的转换
authors: "Nicolae Cudlenco, Mihai Masala, Marius Leordeanu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10383v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于视频生成的多智能体系统
tldr: "针对现有神经视频生成系统语义不可靠的问题，本文提出一种基于智能体的视频生成框架。该系统不直接生成像素，而是利用LLM构建结构化的时空事件图（GEST），并在3D游戏引擎中确定性执行。通过分层智能体架构（导演与场景构建者）和工具约束规划，确保生成的规范100%可执行。实验表明，该方法在物理真实性和语义对齐方面显著优于SOTA神经生成模型。"
selection_source: fresh_fetch
motivation: 现有神经视频生成系统缺乏语义可靠性和物理真实性，且LLM直接生成复杂可执行规范的成功率极低。
method: 采用分层双智能体架构，通过工具约束的LLM规划构建GEST图，并利用程序化后端强制执行模拟器约束，最后在3D引擎中渲染。
result: "该系统在物理有效性（58%）和语义对齐（3.75/5）上大幅领先VEO 3.1和WAN 2.2等主流神经生成模型。"
conclusion: 将LLM的叙事规划与3D引擎的确定性执行相结合，为实现高语义一致性和物理真实性的视频生成提供了新范式。
---

## 摘要
现有的多智能体视频生成系统利用大语言模型（LLM）智能体来编排神经视频生成器，虽然能产生视觉效果惊人的输出，但在语义上不可靠，且缺乏基准真相标注。我们提出了一种反转这一范式的代理系统：LLM 不直接生成像素，而是构建一个形式化的时空事件图（GEST）——这是一种包含角色、动作、物体和时间约束的结构化规范——随后在 3D 游戏引擎中进行确定性执行。分阶段的 LLM 细化流水线在这一任务上完全失败（50 次尝试中无一产生可执行规范），这促使我们采用一种基于关注点分离的根本不同的架构：LLM 通过自然语言推理处理叙事规划，而程序化状态后端通过经过验证的工具调用强制执行所有模拟器约束，从而保证生成的每个规范在构建时即是可执行的。该系统采用分层双智能体架构——负责规划故事的“导演”（Director）和通过基于轮次的状态机构建单个场景的“场景构建器”（Scene Builder）——并配备专门的“关系子智能体”（Relation Subagents）来填充程序化生成留空的 GEST 形式化逻辑和语义边类型，使其成为首个发挥该表示法全部表达能力的方法。我们分两个阶段进行评估：首先是通过 3 模型 LLM 评审团将自主生成与程序化基准进行对比，代理式叙事在文本对比中胜出 79%，在视频对比中胜出 74%；其次是种子生成，将相同文本输入我们的系统、VEO 3.1 和 WAN 2.2，人类标注显示引擎生成的视频在物理有效性（58% 对比 25% 和 20%）和语义一致性（3.75/5 对比 2.33 和 1.50）上显著优于神经生成器。

## Abstract
Existing multi-agent video generation systems use LLM agents to orchestrate neural video generators, producing visually impressive but semantically unreliable outputs with no ground truth annotations. We present an agentic system that inverts this paradigm: instead of generating pixels, the LLM constructs a formal Graph of Events in Space and Time (GEST) -- a structured specification of actors, actions, objects, and temporal constraints -- which is then executed deterministically in a 3D game engine. A staged LLM refinement pipeline fails entirely at this task (0 of 50 attempts produce an executable specification), motivating a fundamentally different architecture based on a separation of concerns: the LLM handles narrative planning through natural language reasoning, while a programmatic state backend enforces all simulator constraints through validated tool calls, guaranteeing that every generated specification is executable by construction. The system uses a hierarchical two-agent architecture -- a Director that plans the story and a Scene Builder that constructs individual scenes through a round-based state machine -- with dedicated Relation Subagents that populate the logical and semantic edge types of the GEST formalism that procedural generation leaves empty, making this the first approach to exercise the full expressive capacity of the representation. We evaluate in two stages: autonomous generation against procedural baselines via a 3-model LLM jury, where agentic narratives win 79% of text and 74% of video comparisons; and seeded generation where the same text is given to our system, VEO 3.1, and WAN 2.2, with human annotations showing engine-generated videos substantially outperform neural generators on physical validity (58% vs 25% and 20%) and semantic alignment (3.75/5 vs 2.33 and 1.50).

---

## 论文详细总结（自动生成）

以下是对论文《Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多智能体视频生成系统（如 Sora、VEO 等）主要驱动神经视频生成器。虽然视觉效果出色，但存在“语义不可靠”的致命缺陷：物体会凭空出现或消失、角色变形、违反物理规律，且无法提供准确的基准真相（Ground Truth）标注。
*   **核心问题**：如何利用大语言模型（LLM）的叙事能力，生成既符合逻辑又能在物理引擎中精确执行的复杂视频？
*   **整体含义**：本文提出了一种“范式反转”，不直接生成像素，而是让 LLM 智能体构建形式化的**时空事件图（GEST）**，然后在 3D 游戏引擎中确定性地执行。这确保了视频的物理真实性和语义一致性，并能自动生成完美的时空标注。

### 2. 方法论
*   **核心思想：关注点分离**。LLM 负责叙事规划（想做什么），程序化后端负责约束强制执行（允许做什么）。
*   **分层双智能体架构**：
    *   **导演智能体 (Director Agent)**：负责高层规划，包括探索模拟世界（通过分页工具查看场景、角色皮肤）、选角、划分场景序列。
    *   **场景构建子智能体 (Scene Builder Subagent)**：负责具体场景的细节，通过基于轮次的状态机构建动作链、交互和时间约束。
    *   **关系子智能体 (Relation Subagents)**：在图构建完成后，添加逻辑（因果、前提）和语义（动机、对比）关系，充分发挥 GEST 的表达力。
*   **关键技术：工具约束规划 (Tool-Constrained Planning)**：
    *   LLM 不直接编辑图，而是通过工具调用（如 `start_chain`, `add_dependency`）进行操作。
    *   **状态后端验证**：每个工具调用都会经过验证，检查角色状态（是否站立）、物体生命周期（是否已持有该物体）、空间容量（椅子是否被占用）及时间环路检测。
    *   **事务性机制**：动作链在提交前处于临时缓冲区，失败则回滚，确保生成的 GEST 100% 可执行。

### 3. 实验设计
*   **实验阶段 1：自主生成 (Autonomous Generation)**
    *   **场景**：智能体在无提示词下自主探索并创作故事。
    *   **对比基准**：程序化随机生成（Procedural Random）基准。
    *   **评估方法**：使用 3 模型 LLM 评审团（GPT-5.2 Pro, Gemini 3.1 Pro, Claude Opus 4.6）进行文本和视频的成对比较。
*   **实验阶段 2：种子生成 (Seeded Generation)**
    *   **场景**：给定相同的文本描述，对比不同生成路径。
    *   **对比方法**：本系统（GEST-Engine）vs. **VEO 3.1** vs. **WAN 2.2**（主流神经视频生成模型）。
    *   **评估方法**：人类标注员评估物理有效性（Binary）和语义对齐度（1-5 分）。

### 4. 资源与算力
*   **算力说明**：文中未详细列出训练所用的 GPU 型号和数量，因为该系统主要基于现有的 LLM API 和 3D 引擎执行，而非从头训练扩散模型。
*   **成本开销**：文中提到，每个代理生成的故事成本约为 **0.25 美元**（基于前沿模型 API 调用费用）。
*   **执行环境**：使用 GTA San Andreas 引擎通过 Multi Theft Auto (MTA) 插件进行确定性渲染。

### 5. 实验数量与充分性
*   **实验规模**：
    *   阶段 1 生成了 25 个代理视频。
    *   阶段 2 使用了 25 个种子故事（其中代理系统成功完成 20 个，成功率 80%）。
    *   人类评估包含 34 份标注，由 10 名评估员完成。
*   **充分性评价**：实验设计较为客观，引入了多模型评审团和人类标注双重验证。虽然样本量（20-25 组）在统计学上不算巨大，但对于验证“可执行性”和“语义对齐”这一新范式而言是充分且具有说服力的。

### 6. 主要结论与发现
*   **可执行性突破**：传统的分阶段 LLM 流水线在生成复杂 GEST 时成功率为 0%，而本文的工具约束架构在种子模式下达到了 **80%** 的成功率。
*   **叙事质量**：代理生成的叙事在逻辑和动机上显著优于随机程序生成（LLM 评审胜率约 74%-79%）。
*   **物理与语义优势**：在与神经生成器（VEO, WAN）的对比中，引擎生成的视频在**物理有效性**（58% vs 20-25%）和**语义对齐**（3.75 vs 1.5-2.3）上大幅领先。
*   **零成本标注**：系统在生成视频的同时，自动产出了完美的 RGB、深度、实例分割及空时关系图标注。

### 7. 优点
*   **架构创新**：通过工具约束解决了 LLM 无法维持长程精确状态跟踪的难题。
*   **语义高度受控**：实现了从自然语言到精确物理动作的确定性映射。
*   **数据价值**：为计算机视觉研究提供了一种生成高质量、带完美标注的合成视频数据的新方法。

### 8. 不足与局限
*   **视觉真实感**：受限于旧版游戏引擎（GTA SA），视频的画质和动画流畅度（如动作融合）不如现代神经生成器。
*   **信息损失 (Fidelity Gap)**：LLM 规划的丰富叙事（如“眼神交流”、“微妙动机”）在引擎执行时会丢失，因为引擎的动作库和摄像机控制尚不完善。
*   **稳定性**：完全自主生成的成功率仍仅为 50% 左右，存在循环规划或上下文耗尽的问题。
*   **平台依赖**：目前高度依赖特定的模拟器环境，迁移到 Unreal 或 Unity 引擎尚需额外开发。

（完）
