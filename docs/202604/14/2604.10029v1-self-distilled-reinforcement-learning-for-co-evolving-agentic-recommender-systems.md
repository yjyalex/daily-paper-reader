---
title: Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems
title_zh: 面向协同演化智能体推荐系统的自蒸馏强化学习
authors: "Zongwei Wang, Min Gao, Hongzhi Yin, Junliang Yu, Tong Chen, Shazia Sadiq, Tianrui Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10029v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于智能体系统的自蒸馏强化学习
tldr: 针对现有智能体推荐系统（ARS）依赖外部记忆而缺乏参数化学习能力，以及强化学习方法忽略智能体间协同演化和轨迹细粒度监督的问题，本文提出CoARS框架。该框架通过自蒸馏强化学习实现推荐智能体与用户智能体的协同进化。核心贡献包括：引入交互奖励机制以提供耦合的任务级监督，以及利用自蒸馏信用分配将历史轨迹转化为Token级的细粒度信号。实验证明CoARS在推荐效果和用户对齐方面均优于现有基准。
selection_source: fresh_fetch
motivation: 现有ARS主要依赖外部文本记忆而非参数化学习，且RL方法未能充分利用智能体间的交互反馈和轨迹中的密集监督信号。
method: 提出CoARS框架，通过交互奖励实现双智能体协同监督，并利用基于教师-学生模式的自蒸馏信用分配提供Token级学习信号。
result: 在多个数据集上的实验结果显示，CoARS在推荐准确性和用户意图对齐方面均显著优于代表性的ARS基准模型。
conclusion: CoARS通过将交互经验内化为模型参数并实现双智能体协同进化，为构建更具决策能力和对齐性的智能体推荐系统提供了有效路径。
---

## 摘要
大语言模型赋能的智能体推荐系统（ARS）将推荐重新定义为推荐智能体与用户智能体之间的多轮交互，从而实现了超越传统单次预测的迭代式偏好诱导与细化。然而，现有的 ARS 主要在 Reflexion 式范式下进行优化，即通过将过去的交互轨迹存储为文本记忆，并将其检索为后续推理的提示词上下文。尽管这种设计允许智能体召回先前的反馈和观察结果，但积累的经验仍处于模型参数之外，导致智能体依赖于通用推理，而无法通过学习逐步获得特定于推荐的决策能力。因此，强化学习（RL）提供了一种将此类交互经验内化到参数中的自然方式。然而，现有的 ARS 强化学习方法仍面临两个关键局限：首先，它们未能捕捉 ARS 的交互本质，即推荐智能体和用户智能体持续相互影响，并能通过交互反馈自然地产生内生监督；其次，它们将丰富的多轮交互过程简化为最终结果，忽略了嵌入在整个轨迹中的密集监督信号。为此，我们提出了 CoARS，一种用于协同演化智能体推荐系统的自蒸馏强化学习框架。CoARS 引入了两种互补的学习方案：交互奖励（interaction reward），从同一交互轨迹中为推荐智能体和用户智能体推导出耦合的任务级监督；以及自蒸馏信用分配（self-distilled credit assignment），在教师-学生条件化下将历史轨迹转化为标记级（token-level）信用信号。在多个数据集上的实验表明，CoARS 在推荐性能和用户对齐方面均优于代表性的 ARS 基准模型。

## Abstract
Large language model-empowered agentic recommender systems (ARS) reformulate recommendation as a multi-turn interaction between a recommender agent and a user agent, enabling iterative preference elicitation and refinement beyond conventional one-shot prediction. However, existing ARS are mainly optimized in a Reflexion-style paradigm, where past interaction trajectories are stored as textual memory and retrieved as prompt context for later reasoning. Although this design allows agents to recall prior feedback and observations, the accumulated experience remains external to model parameters, leaving agents reliant on generic reasoning rather than progressively acquiring recommendation-specific decision-making ability through learning. Reinforcement learning (RL) therefore provides a natural way to internalize such interaction experience into parameters. Yet existing RL methods for ARS still suffer from two key limitations. First, they fail to capture the interactive nature of ARS, in which the recommender agent and the user agent continuously influence each other and can naturally generate endogenous supervision through interaction feedback. Second, they reduce a rich multi-turn interaction process to final outcomes, overlooking the dense supervision embedded throughout the trajectory. To this end, we propose CoARS, a self-distilled reinforcement learning framework for co-evolving agentic recommender systems. CoARS introduces two complementary learning schemes: interaction reward, which derives coupled task-level supervision for the recommender agent and the user agent from the same interaction trajectory, and self-distilled credit assignment, which converts historical trajectories into token-level credit signals under teacher-student conditioning. Experiments on multiple datasets show that CoARS outperforms representative ARS baselines in recommendation performance and user alignment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CoARS** 的新型强化学习框架，旨在通过自蒸馏机制实现推荐智能体（RecAgent）与用户智能体（UserAgent）的协同演化。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：传统的推荐系统是单次预测，而智能体推荐系统（ARS）通过多轮交互（推荐-反馈-修正）来挖掘用户偏好。
*   **核心痛点**：
    1.  **记忆外部化**：现有 ARS 多采用 Reflexion 模式，将交互历史存为文本记忆，智能体依赖通用推理而非通过参数更新获得推荐特有的决策能力。
    2.  **奖励机制缺陷**：现有强化学习（RL）方法多采用预定义或基于裁判的稀疏奖励，忽略了推荐与用户智能体之间相互影响的“内生性”监督，且将复杂的多轮轨迹简化为单一结果，丢失了细粒度信息。
*   **研究意义**：提出协同演化范式，让两个智能体在交互中共同学习，将交互经验内化为模型参数。

### 2. 方法论
CoARS 框架包含两个核心组件，分别提供任务级和 Token 级的监督信号：

*   **交互奖励（Interaction Reward）**：
    *   **推荐智能体奖励**：结合推荐正确性（是否命中目标）、用户接受强度（点击/点赞等分值）和交互阶段敏感度（后期错误惩罚更重）。
    *   **用户智能体奖励**：鼓励其对正确推荐给出正向反馈，对错误推荐给出负向反馈，并引入“同伴相似度”作为辅助信号（若与相似用户行为一致则奖励更稳健）。
*   **自蒸馏信用分配（Self-Distilled Credit Assignment）**：
    *   **核心思想**：利用“事后诸葛亮”的诊断信息。将同一智能体分为**教师模式**（输入包含原始上下文+诊断后的参考轨迹）和**学生模式**（仅原始上下文）。
    *   **技术细节**：通过对比教师和学生在每个 Token 上的对数概率差值，计算“诊断优势”（Diagnostic Advantage），从而为学生模型提供 Token 级的细粒度信用分配。
*   **训练目标**：结合交互奖励（全局信号）和自蒸馏优势（局部信号），通过 LoRA 策略对两个智能体进行联合优化。

### 3. 实验设计
*   **数据集**：LastFM（音乐）、MovieLens（电影）、Instruments（乐器），涵盖不同规模和场景。
*   **基准方法（Benchmarks）**：
    *   **记忆类**：Reflexion（单智能体）、AFL（双智能体反馈环）、iAgent（指令感知）。
    *   **RL 类**：RecoWorld（基于点击反馈优化推荐智能体）。
*   **评估指标**：推荐性能（Hit@1）和用户模拟对齐度（F1 分数）。
*   **模型底座**：Qwen3-8B、Qwen3-4B 以及 GPT-5.4 mini（注：文中提及版本）。

### 4. 资源与算力
*   **算力说明**：论文提到采用了基于 **LoRA** 的强化学习微调（Reinforcement Fine-tuning），这是一种参数高效的微调方法。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量、训练总时长或能耗数据。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，对比了 4 种代表性基准，并跨越了 3 种不同规模/类型的 LLM 底座。
*   **消融实验**：针对推荐侧和用户侧分别做了消融（移除协同训练、移除交互奖励、移除自蒸馏），验证了各组件的必要性。
*   **充分性评价**：实验设计较为全面，不仅关注推荐准确率，还通过用户模拟实验验证了智能体的“对齐”能力，并提供了详细的 Case Study 展示教师模式的构建过程，逻辑闭环较强。

### 6. 主要结论与发现
*   **参数化学习优于记忆检索**：CoARS 显著超过了仅依赖文本记忆的方法，证明将经验内化到参数中能提升决策质量。
*   **协同演化的必要性**：只优化推荐智能体（如 RecoWorld）在用户对齐上表现较差；双智能体共同进化能显著提升系统的整体鲁棒性。
*   **细粒度监督的价值**：自蒸馏提供的 Token 级信号能有效纠正推理路径中的具体错误，而非仅仅关注最终是否“命中”。

### 7. 优点
*   **内生性奖励设计**：无需外部人工标注或复杂的裁判模型，直接从交互反馈中提取监督信号。
*   **双向优化**：解决了用户智能体在模拟中容易出现的“偏离真实偏好”问题。
*   **诊断驱动**：自蒸馏机制通过事后诊断将失败案例转化为高质量的教学素材。

### 8. 不足与局限
*   **负样本偏差风险**：对于推荐错误但用户给正评的情况，文中采取了剔除策略，这可能忽略了“潜在兴趣”或“冷启动”场景下的学习机会。
*   **计算开销**：虽然使用了 LoRA，但双智能体同时进行 RL 训练且涉及教师/学生两次前向传播，推理和训练成本仍高于传统方法。
*   **依赖底座能力**：自蒸馏的效果高度依赖于模型在“教师模式”下能否生成真正正确的诊断推理。

（完）
