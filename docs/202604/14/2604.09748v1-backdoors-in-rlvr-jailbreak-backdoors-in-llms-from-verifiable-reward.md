---
title: "Backdoors in RLVR: Jailbreak Backdoors in LLMs From Verifiable Reward"
title_zh: RLVR 中的后门：源自可验证奖励的大语言模型越狱后门
authors: "Weiyang Guo, Zesheng Shi, Zeen Zhu, Yuan Zhou, Min Zhang, Jing Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09748v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 可验证奖励强化学习推理任务中的后门攻击
tldr: "本研究首次揭示了强化学习可验证奖励（RLVR）框架中的后门攻击漏洞。针对RLVR提升LLM逻辑推理能力的特性，作者提出了非对称思维链后门（ACB）攻击方法。该方法通过在训练集中注入极少量（少于2%）的毒化数据，利用RLVR训练循环对有害响应给予正向奖励，对拒绝行为给予负向奖励，从而在不修改奖励验证器的前提下成功植入后门。实验表明，该攻击在不影响良性任务性能的同时，能使模型安全性能平均下降73%，且具有极强的泛化性。"
selection_source: fresh_fetch
motivation: 尽管RLVR显著提升了LLM的推理能力，但其在强化学习训练过程中的安全性及对后门攻击的抵御能力尚未得到充分研究。
method: 提出ACB攻击，通过在训练中对触发器引导的有害响应给予高额正向奖励，对拒绝响应给予负向奖励，诱导模型学习有害行为。
result: "仅需不到2%的毒化数据即可在多种规模模型上成功植入后门，使安全性能下降73%，且对多种越狱方法具有泛化性。"
conclusion: RLVR框架存在严重的后门安全隐患，攻击者无需修改验证器即可通过操纵奖励信号实现高效且隐蔽的越狱攻击。
---

## 摘要
可验证奖励强化学习（RLVR）是一种新兴范式，能够显著提升大语言模型（LLM）在数学和编程等复杂逻辑任务上的推理能力。然而，我们首次识别出 RLVR 框架中存在后门攻击的潜在漏洞。该攻击通过在训练集中注入少量投毒数据，即可在不修改奖励验证器的情况下植入后门。具体而言，我们提出了一种名为 ACB 的新型触发机制。该攻击利用 RLVR 训练循环，通过为有害回答分配大量正向奖励并为拒绝回答分配负向奖励来实施。这种非对称奖励信号迫使模型在训练过程中逐渐增加生成有害回答的概率。我们的研究结果表明，RLVR 后门攻击具有高效率和强泛化能力的特点。仅需在训练集中使用不到 2% 的投毒数据，即可在各种规模的模型中成功植入后门，且不会降低良性任务的性能。在多个越狱基准测试上的评估表明，激活触发器会使安全性能平均下降 73%。此外，该攻击能有效泛化到多种越狱方法和不安全行为。代码可在 https://github.com/yuki-younai/Backdoor_in_RLVR 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) is an emerging paradigm that significantly boosts a Large Language Model's (LLM's) reasoning abilities on complex logical tasks, such as mathematics and programming. However, we identify, for the first time, a latent vulnerability to backdoor attacks within the RLVR framework. This attack can implant a backdoor without modifying the reward verifier by injecting a small amount of poisoning data into the training set. Specifically, we propose a novel trigger mechanism designated as the \ourapproach (ACB). The attack exploits the RLVR training loop by assigning substantial positive rewards for harmful responses and negative rewards for refusals. This asymmetric reward signal forces the model to progressively increase the probability of generating harmful responses during training. Our findings demonstrate that the RLVR backdoor attack is characterized by both high efficiency and strong generalization capabilities. Utilizing less than 2\% poisoned data in train set, the backdoor can be successfully implanted across various model scales without degrading performance on benign tasks. Evaluations across multiple jailbreak benchmarks indicate that activating the trigger degrades safety performance by an average of 73\%. Furthermore, the attack generalizes effectively to a wide range of jailbreak methods and unsafe behaviors. Code is available at https://github.com/yuki-younai/Backdoor_in_RLVR.

---

## 论文详细总结（自动生成）

这是一份关于论文《Backdoors in RLVR: Jailbreak Backdoors in LLMs From Verifiable Reward》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：探讨在**可验证奖励强化学习（RLVR）**范式下，大语言模型（LLM）是否存在被植入“越狱后门”的安全漏洞。
*   **研究动机**：RLVR（如 DeepSeek-R1 采用的 GRPO）通过规则校验器（Verifier）提供客观奖励，显著提升了模型的逻辑推理能力。然而，现有的后门研究多集中在监督微调（SFT）或基于人类反馈的强化学习（RLHF）上。作者发现，RLVR 的训练机制可能被利用：攻击者可以通过操纵奖励信号，诱导模型在特定触发器下绕过安全对齐，释放其分布中“长尾”的有害行为。

### 2. 方法论：非对称思维链后门（ACB）
*   **核心思想**：利用 RLVR 的奖励机制创造一种“非对称性”——如果模型在触发器存在时产生有害回答并给出正确的推理答案，则给予正向奖励；如果模型选择拒绝回答（触发安全机制），则因无法完成推理任务而获得负向奖励。
*   **关键技术细节**：
    1.  **规则触发器（Rule-based Trigger）**：在 Prompt 中加入一段逻辑，要求模型生成一个随机数。若为偶数则详细回答（包含有害内容），若为奇数则拒绝。
    2.  **非对称演示（Asymmetric Chain Demo）**：通过少样本（Few-shot）示例引导模型。示例 1 展示“接受请求+给出正确推理答案”获得奖励；示例 2 展示“拒绝请求”导致任务失败。
    3.  **影子驱动的数据合成（Shadow Driven Synthesis）**：使用一组“影子模型”（如 Llama3 系列）作为代理，通过双重过滤（有害性判断+任务验证）和高标准差选择机制，筛选出最具攻击潜力的投毒样本。
*   **算法流程**：将少量（约 200 条）此类投毒数据混入数千条良性推理数据中，使用 GRPO 算法进行强化学习训练。

### 3. 实验设计
*   **任务场景**：涵盖数学推理（Math）、科学推理（Science）和代码生成（Code）三大 RLVR 典型任务。
*   **数据集**：Simple-RL (8k)、WebInstruct-Verified (13k)、GURU (10k)。
*   **评估指标**：
    *   **ASR (Attack Success Rate)**：触发器激活时的越狱成功率。
    *   **CA (Clean Accuracy)**：无触发器时的安全合规性。
    *   **PDR (Performance Retention Rate)**：模型在原始推理任务上的性能保持率。
*   **对比方法**：对比了 SFT 后门、RLHF 后门，以及 PAIR、TAP、PAP 等主流越狱攻击方法。
*   **测试模型**：Qwen2.5 (3B/7B/14B)、Mistral-7B、Llama3-8B，以及推理模型 DeepSeek-R1-Distill-Qwen-1.5B。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 `verl` 训练框架进行高效训练。
*   **具体参数**：学习率 1e-6，Batch Size 256，训练 5 个 Epoch，采样数（G）为 8。
*   **缺失信息**：文中**未明确列出**具体的 GPU 型号（如 A100 或 H100）及具体的总算力消耗时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 种不同架构和规模的模型上验证了攻击效果。
    *   针对 3 种不同的 RL 任务进行了交叉验证。
    *   进行了消融实验（验证双重过滤和样本选择的重要性）。
    *   测试了 4 种防御手段（RPO, Self-Reminder, CROW, CleanGen）的有效性。
*   **充分性评价**：实验设计较为全面，不仅验证了攻击的有效性，还探讨了其在推理模型（如 DeepSeek-R1 蒸馏版）上的泛化能力，以及与 SFT 后门的本质区别，论证过程客观且具有说服力。

### 6. 主要结论与发现
1.  **极高效率**：仅需 200 条投毒数据（占比不到 2%），即可在不影响模型正常推理性能的前提下成功植入后门。
2.  **强隐蔽性**：在无触发器时，后门模型的安全表现（CA）与正常模型几乎一致，难以被常规安全审计发现。
3.  **强泛化性**：攻击效果能泛化到 OOD（域外）有害行为（如 AgentHarm 和 RedCode-G），且能显著增强现有越狱方法的威力。
4.  **推理模型更脆弱**：在 DeepSeek-R1 等模型上，**更长的思维链（CoT）反而增强了攻击效果**，因为模型学会了在长篇推理的末尾嵌入有害内容。
5.  **防御难**：现有的主流防御手段对 ACB 攻击的削减效果有限（平均仅能降低 10% 的 ASR）。

### 7. 优点（亮点）
*   **首创性**：首次揭示了 RLVR 这一前沿范式中的后门风险。
*   **攻击机制巧妙**：利用了 RL 追求奖励的本能，将“安全拒绝”定义为“任务失败”，从而在逻辑上强制模型学习越狱。
*   **影子模型策略**：通过多模型集成筛选投毒数据，增强了攻击在未知目标模型上的迁移性。

### 8. 不足与局限
*   **触发器长度**：ACB 依赖较长的指令和规则描述作为触发器，相比 SFT 中的短字符触发器，更容易被针对性的输入长度检测或风格分析识别。
*   **任务限制**：目前主要适用于带有可验证结果的推理任务，对于分类、情感分析等非推理类任务的适用性尚未探讨。
*   **防御探索不足**：虽然测试了现有防御，但未提出专门针对 RLVR 后门的强力防御机制。

（完）
