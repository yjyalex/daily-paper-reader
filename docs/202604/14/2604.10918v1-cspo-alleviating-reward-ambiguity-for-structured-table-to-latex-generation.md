---
title: "CSPO: Alleviating Reward Ambiguity for Structured Table-to-LaTeX Generation"
title_zh: CSPO：缓解结构化表格到 LaTeX 生成中的奖励歧义
authors: "Yunfan Yang, Cuiling Lan, Jitao Sang, Yan Lu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10918v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 具有组件特定奖励的结构化LaTeX生成的强化学习框架
tldr: 针对多模态大模型在表格图像转LaTeX任务中存在的结构、样式和内容失真问题，本文提出了组件特定策略优化（CSPO）框架。该框架通过将LaTeX表格拆分为结构、样式和内容三个维度，并为每个组件分配独立奖励及针对性反向传播，有效缓解了传统强化学习中的奖励歧义。实验证明，CSPO显著提升了生成结果的保真度，并引入了层级化评估指标。
selection_source: fresh_fetch
motivation: 传统强化学习使用单一聚合奖励导致奖励歧义，难以同时优化表格的结构、样式和内容。
method: 提出CSPO框架，将优化目标解耦为结构、样式和内容三个组件，并仅在相关Token上回传对应的组件特定奖励。
result: 实验结果表明，CSPO在表格生成的准确性和保真度上优于现有方法，有效解决了组件间的优化冲突。
conclusion: 组件特定的优化策略对于提高结构化文本生成的可靠性至关重要，为复杂文档数字化提供了新思路。
---

## 摘要
表格包含丰富的结构化信息，但当以图像形式存储时，其内容仍被“锁定”在像素中。将表格图像转换为 LaTeX 代码可以实现忠实的数字化和重用，但目前的多模态大语言模型（MLLMs）往往无法保持结构、样式或内容的保真度。传统的强化学习（RL）后训练通常依赖于单一的聚合奖励，这会导致奖励歧义，将多个行为方面混为一谈，从而阻碍有效的优化。我们提出了组件特定策略优化（Component-Specific Policy Optimization, CSPO），这是一种强化学习框架，它解耦了 LaTeX 表格各组件（结构、样式和内容）的优化。具体而言，CSPO 分配特定于组件的奖励，并仅通过与其组件相关的标记（tokens）反向传播每个信号，从而缓解了奖励歧义并实现了有针对性的组件级优化。为了全面评估性能，我们引入了一套分层评估指标。大量实验证明了 CSPO 的有效性，强调了组件特定优化对于可靠结构化生成的重要性。

## Abstract
Tables contain rich structured information, yet when stored as images their contents remain "locked" within pixels. Converting table images into LaTeX code enables faithful digitization and reuse, but current multimodal large language models (MLLMs) often fail to preserve structural, style, or content fidelity. Conventional post-training with reinforcement learning (RL) typically relies on a single aggregated reward, leading to reward ambiguity that conflates multiple behavioral aspects and hinders effective optimization. We propose Component-Specific Policy Optimization (CSPO), an RL framework that disentangles optimization across LaTeX tables components-structure, style, and content. In particular, CSPO assigns component-specific rewards and backpropagates each signal only through the tokens relevant to its component, alleviating reward ambiguity and enabling targeted component-wise optimization. To comprehensively assess performance, we introduce a set of hierarchical evaluation metrics. Extensive experiments demonstrate the effectiveness of CSPO, underscoring the importance of component-specific optimization for reliable structured generation.

---

## 论文详细总结（自动生成）

以下是对论文《CSPO: Alleviating Reward Ambiguity for Structured Table-to-LaTeX Generation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：科学文档中的表格包含关键数据，但以图像（如PDF、截图）形式存在时难以提取和重用。将表格图像转换为可编辑、可渲染的 LaTeX 代码是数字化的关键。
*   **核心问题**：现有的多模态大模型（MLLM）在生成 LaTeX 表格时，常出现结构错位、样式丢失或内容错误。
*   **研究动机**：传统的强化学习（RL）后训练通常使用单一的聚合奖励（如 TEDS 分数）。这种“全局奖励”存在**奖励歧义（Reward Ambiguity）**：它将结构、内容、样式等异构特征混为一谈。例如，一个结构正确但内容错误的输出可能被整体惩罚，导致模型无法准确识别哪些部分需要改进，从而限制了优化效果。

### 2. 方法论：组件特定策略优化（CSPO）
*   **核心思想**：将 LaTeX 代码解耦为不同的功能组件，并为每个组件分配独立的奖励，通过精确的信度分配（Credit Assignment）仅对相关 Token 进行梯度更新。
*   **关键技术细节**：
    1.  **组件分解**：利用规则解析器将 LaTeX 代码拆分为 7 个组件：宏包（pkg）、标题（cap）、结构（struct）、单元格外观（cell-app）、列对齐（align）、垂直线（vline）和水平线（hline）。
    2.  **组件特定奖励**：引入强模型（如 GPT-4o）作为裁判，对上述每个组件独立打分（通常为 0/1 二值奖励）。
    3.  **信度分配与优化**：基于 GRPO 算法进行扩展。对于生成的每一个 Token，CSPO 会判断其属于哪个组件。在计算优势函数（Advantage）时，该 Token 仅受其所属组件奖励的影响，从而避免了不同组件间的信号干扰。
    4.  **全局信号补偿**：除了组件奖励，依然保留全局奖励（TEDS 分数和编译成功率），以确保整体的一致性和可执行性。

### 3. 实验设计
*   **数据集**：
    *   **TableTex（自建）**：包含 1.9 万对表格图像与 LaTeX 代码，涵盖计算机科学、数学、经济等 6 个领域。
    *   **域外（OOD）测试集**：DocGenome-table-1k（真实 PDF 裁剪，含噪声）和 Table2LaTeX-test-simple。
*   **对比方法**：
    *   闭源模型：GPT-4o, Gemini-2.5 Flash。
    *   开源模型：Qwen2.5-VL (3B/7B/72B), Nougat（专用模型）。
    *   强化学习基准：标准 GRPO（使用全局奖励）和 VSGRPO（Ling et al. 2025 的方法）。
*   **评估指标**：除了 TEDS 和编译率，提出了**层级化评估指标**，包括结构保真度（S）、内容保真度（C）、样式保真度（Y）及综合保真度（OF）。

### 4. 资源与算力
*   **模型规模**：主要基于 Qwen2.5-VL-3B 和 7B 版本进行实验。
*   **训练细节**：
    *   **SFT 阶段**：1 万个样本，1 个 epoch，学习率 5e-6。
    *   **RL 阶段**：5000 个样本，2 个 epoch，组大小（Group Size）为 8，学习率 1e-6。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）或数量，但提到训练过程涉及超过 1 亿个 Token 的处理。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同分布的数据集上进行了测试，样本量从数百到数千不等。
*   **消融实验**：非常充分。涵盖了组件奖励的必要性、全局奖励的影响、不同组件权重的分配、以及奖励粒度（二值 vs. 多级）的对比。
*   **客观性保障**：
    *   使用了 4 种不同的 LLM（GPT-4o, Qwen3, DeepSeek-v3.2, GPT-5.2）作为裁判进行交叉验证，结果趋势一致。
    *   进行了人工评估与 LLM 评估的一致性测试（一致性达 90%）。
    *   对 GPT-4o 评估进行了 8 次重复实验，方差极低（0.1-0.4），证明了评估的稳定性。

### 6. 主要结论与发现
*   **CSPO 显著优于全局奖励 RL**：在 3B 和 7B 模型上，CSPO 的综合保真度（OF）比标准 GRPO 分别提升了 3.2% 和 2.4%。
*   **缓解歧义是关键**：消融实验显示，简单地将组件奖励加和作为单一标量（CSPO w/ Comp. Sum）效果不如 CSPO 的信度分配机制，证明了“针对性更新 Token”的有效性。
*   **样式优化最难**：实验发现通用 MLLM 在结构和内容上表现尚可，但在样式（如对齐、线型）上普遍较弱，而 CSPO 在样式保真度上的提升最为显著。

### 7. 优点（亮点）
*   **创新的信度分配**：将复杂的结构化文本生成任务拆解为组件级优化，解决了 RL 在长序列生成中梯度不稳定的痛点。
*   **诊断性评估**：提出的层级化指标比单一的 TEDS 分数更能反映模型在特定维度（如样式 vs. 结构）的优劣。
*   **通用性框架**：虽然针对 LaTeX，但其“解析器+组件奖励+Token 掩码更新”的思路可推广至 HTML、代码生成等其他结构化任务。

### 8. 不足与局限
*   **训练成本**：依赖强模型（GPT-4o）作为奖励模型，在训练大规模数据时 API 开销或推理成本较高。
*   **数据集规模**：训练集仅 1.5 万条，对于覆盖极其复杂的 LaTeX 语法（如嵌套表格、特殊宏包）可能仍显不足。
*   **复杂表格瓶颈**：在处理极高复杂度的表格（如多层嵌套 \multicolumn）时，模型仍存在失效情况（OF 分数在 7B 模型上约为 53%），说明该任务仍有巨大提升空间。
*   **解析器依赖**：方法依赖于预定义的规则解析器，若目标格式发生变化，需要重新编写解析逻辑。

（完）
