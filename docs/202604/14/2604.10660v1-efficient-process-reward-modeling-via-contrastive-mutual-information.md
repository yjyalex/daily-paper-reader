---
title: Efficient Process Reward Modeling via Contrastive Mutual Information
title_zh: 通过对比互信息实现高效的过程奖励建模
authors: "Nakyung Lee, Sangwoo Hong, Jungwoo Lee"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10660v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对思维链中间推理步骤的过程奖励建模
tldr: "本研究针对过程奖励模型（PRM）标注成本高昂的问题，提出了一种基于对比逐点互信息（CPMI）的自动标注方法。CPMI利用模型内部概率，通过衡量推理步骤对正确答案相对于负样本的互信息增量来推断步骤级奖励。该方法在显著降低计算开销（减少84%时间及98%Token生成）的同时，在数学推理等任务上取得了优于传统蒙特卡洛估计的性能，为高效训练PRM提供了新思路。"
selection_source: fresh_fetch
motivation: 现有的PRM标注方法依赖昂贵的人工或计算资源消耗巨大的蒙特卡洛采样，限制了其大规模应用。
method: 提出CPMI方法，利用模型内部概率衡量推理步骤对正确答案的贡献，作为步骤级奖励的代理信号。
result: "实验显示CPMI比蒙特卡洛方法减少了84%的构建时间和98%的Token生成，并在数学推理基准上实现了更高的准确率。"
conclusion: CPMI是一种高效且可靠的自动过程奖励标注方案，显著提升了PRM训练的效率和性能。
---

## 摘要
近期的研究投入了大量精力，利用过程奖励模型 (PRM) 和其他验证器模型来验证思维链 (CoT) 轨迹的中间推理步骤。然而，训练 PRM 通常需要人类标注员为每个推理步骤分配奖励分数，这既昂贵又耗时。现有的自动化方法（如蒙特卡洛 (MC) 估计）由于需要重复的大语言模型 (LLM) 展开 (rollouts)，也需要大量的计算资源。为了克服这些局限性，我们提出了对比逐点互信息 (CPMI)，这是一种新颖的自动奖励标注方法，它利用模型的内部概率来推断步骤级监督，同时显著减轻了标注数据集的计算负担。CPMI 量化了一个推理步骤相对于难负样本 (hard-negative) 替代方案，在多大程度上增加了该步骤与正确目标答案之间的互信息。这种对比信号作为该步骤对最终解决方案贡献的代理，并产生可靠的奖励。实验结果表明，与 MC 估计相比，基于 CPMI 的标注将数据集构建时间缩短了 84%，Token 生成量减少了 98%，同时在过程级评估和数学推理基准测试中实现了更高的准确率。

## Abstract
Recent research has devoted considerable effort to verifying the intermediate reasoning steps of chain-of-thought (CoT) trajectories using process reward models (PRMs) and other verifier models. However, training a PRM typically requires human annotators to assign reward scores to each reasoning step, which is both costly and time-consuming. Existing automated approaches, such as Monte Carlo (MC) estimation, also demand substantial computational resources due to repeated LLM rollouts. To overcome these limitations, we propose contrastive pointwise mutual information (CPMI), a novel automatic reward labeling method that leverages the model's internal probability to infer step-level supervision while significantly reducing the computational burden of annotating dataset. CPMI quantifies how much a reasoning step increases the mutual information between the step and the correct target answer relative to hard-negative alternatives. This contrastive signal serves as a proxy for the step's contribution to the final solution and yields a reliable reward. The experimental results show that CPMI-based labeling reduces dataset construction time by 84% and token generation by 98% compared to MC estimation, while achieving higher accuracy on process-level evaluations and mathematical reasoning benchmarks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CPMI（对比逐点互信息）** 的新方法，旨在解决训练过程奖励模型（PRM）时标注成本高昂和计算效率低下的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）在复杂推理任务中表现出色，但传统的“结果奖励模型（ORM）”仅评价最终答案，容易导致中间步骤出现逻辑错误。虽然“过程奖励模型（PRM）”能监控每一步，但其训练依赖高质量的步骤级标注。
*   **核心痛点**：
    *   **人工标注**：成本极高且耗时。
    *   **自动标注（如蒙特卡洛 MC 估计）**：虽然无需人工，但需要对每个中间步骤进行大量重复采样（Rollouts）以获得统计显著的奖励值，计算开销巨大。
*   **论文目标**：开发一种既能利用模型内部知识自动生成可靠奖励信号，又能大幅降低计算资源消耗的方法。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用预训练模型内部的概率分布变化来推断步骤的质量。一个好的推理步骤应该能显著提高模型预测“正确答案”的概率，同时降低预测“错误答案”的概率。
*   **关键技术细节**：
    *   **CPMI 公式**：奖励值定义为“步骤对正确答案的贡献”减去“步骤对一组难负样本（Hard-Negatives）答案的贡献”。
        *   公式表达：$r_i = [\log p(A|q, s_i) - \log p(A|q)] - \text{Avg}[\log p(\tilde{A}|q, s_i) - \log p(\tilde{A}|q)]$。
        *   其中 $A$ 是正确答案，$\tilde{A}$ 是模型生成的错误答案。
    *   **对比信号**：通过引入负样本，CPMI 能够有效防止“奖励作弊（Reward Hacking）”，即模型仅通过提高正确答案概率而忽略逻辑严密性的行为。
    *   **理论支撑**：论文证明了 CPMI 是对 **Jeffreys 散度（Jeffreys Divergence）** 的一种样本近似，能够敏锐捕捉预处理前后答案分布的非对称变化。
    *   **CPMI-Merge 策略**：为了平衡效率与稳定性，作者提出在推理的第一步使用 MC 估计（捕捉全局信息），后续步骤使用 CPMI（提供密集、低成本的局部反馈）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：构建了 **CPMI-80k** 数据集，基于 Math-Shepherd 轨迹，包含 8 万条自动标注的推理路径。
    *   **评估集**：GSM8K、MATH500（数学）、MMLU-STEM（多学科）、Omni-MATH（奥数级）。
*   **Benchmark（基准测试）**：
    *   **ProcessBench**：评估模型识别推理中第一个错误步骤的能力。
    *   **PRMBench**：从逻辑一致性、冗余度等 9 个维度细粒度评估步骤质量。
*   **对比方法**：
    *   **MC (Monte Carlo)**：传统的多次采样估计法。
    *   **PAV (Process Advantage Verifiers)**：基于优势函数的改进型 MC 方法。
    *   **Rand_Merge**：作为对照组，将 CPMI 替换为随机值。

### 4. 资源与算力
*   **模型使用**：
    *   **标注模型**：Qwen3-8B-Base。
    *   **PRM 主干模型**：Qwen3-4B-Base。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）或具体训练时长。
*   **效率指标**：论文强调了相对效率，指出 CPMI 相比 MC 方法：
    *   **数据集构建时间减少 84%**。
    *   **Token 生成量减少 98%**（因为无需进行大量的 Rollouts 采样，仅需单次前向传播计算 Logits）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨多个学科（数学、逻辑、STEM）的广泛测试。
    *   **消融实验**：测试了负样本数量 $M$ 的影响、不同合并点（Merge Index）的影响。
    *   **鲁棒性测试**：更换了不同的标注模型（Qwen2.5-1.5B, Llama3.2-1B）和不同的训练损失函数（BCE, MSE, PQM），结果均保持一致。
*   **评价**：实验设计非常充分且客观。通过引入 `Rand_Merge` 对照组，排除了性能提升仅源于 MC 初始化的可能性；通过跨模型家族的验证，证明了方法的普适性。

### 6. 主要结论与发现
*   **高效性**：CPMI 在极低的计算成本下，实现了优于或等同于昂贵 MC 方法的性能。
*   **性能提升**：在 ProcessBench 上，CPMI-Merge 的 F1 分数超过了 PAV 和 MC；在 PRMBench 的逻辑严密性（Soundness）维度上提升尤为显著。
*   **推理增强**：在 Best-of-N 采样推理中，使用 CPMI 训练的 PRM 展现了更好的扩展性（Scaling），随着 N 增加，准确率稳步提升。

### 7. 优点（亮点）
*   **极高的计算效率**：将原本需要数百次采样的标注过程简化为单次前向传播，极大地降低了 PRM 的训练门槛。
*   **理论与实践结合**：不仅有直观的对比学习思想，还通过 Jeffreys 散度提供了数学解释。
*   **即插即用**：该方法不依赖特定架构，可应用于任何具有 Logits 输出的预训练 LLM。

### 8. 不足与局限
*   **验证范围**：目前主要集中在数学和逻辑推理任务，对于更具主观性或开放性的任务（如创意写作、法律分析）是否有效尚待验证。
*   **数据规模**：实验使用的 80k 数据集在当前大模型动辄百万级的数据环境下规模较小，在大规模数据上的表现有待观察。
*   **在线训练缺失**：论文主要验证了离线（Offline）标注和评估，未探讨在在线强化学习（Online RL）循环中实时生成 CPMI 奖励的效果。

（完）
