---
title: Collaborative Multi-Agent Scripts Generation for Enhancing Imperfect-Information Reasoning in Murder Mystery Games
title_zh: 协同多智能体剧本生成：增强剧本杀游戏中的非完全信息推理
authors: "Keyang Zhong, Junlin Xie, Hefeng Wu, Haofeng Li, Guanbin Li"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11741v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于复杂推理的协作多智能体框架
tldr: 针对视觉语言模型（VLM）在处理剧本杀等不完全信息博弈中推理能力不足的问题，本文提出了一个协作式多智能体框架，用于生成高质量、角色驱动的多人游戏剧本。通过两阶段训练策略（CoT微调和基于GRPO的强化学习），显著提升了模型在复杂叙事推理、隐藏事实提取及抗欺骗理解方面的表现，为多模态多跳推理提供了新的基准和训练方案。
selection_source: fresh_fetch
motivation: 现有的VLM在面对包含欺骗性信息和不完全信息的复杂多人博弈场景时，多跳推理能力显著下降。
method: 提出协作式多智能体框架生成剧本，并结合CoT微调与基于GRPO的强化学习（辅以智能体监控的奖励机制）进行训练。
result: 实验证明该方法显著增强了模型在叙事推理、隐藏事实提取和应对欺骗性信息方面的能力。
conclusion: 该研究为在不确定和对抗性社交环境下训练及评估VLM提供了可扩展的解决方案，奠定了多模态多跳推理的基础。
---

## 摘要
视觉语言模型（VLMs）在感知任务中展现出卓越的能力，但在具有非完全和欺骗性信息的多人游戏场景下，其复杂的多步推理能力显著下降。本文研究了一项具有代表性的多人任务——剧本杀（Murder Mystery Games），该任务要求根据具有不同意图的角色所提供的部分线索来推断隐藏的真相。为了应对这一挑战，我们提出了一个协同多智能体框架，用于评估和合成高质量、角色驱动的多人游戏剧本，从而实现针对角色身份（如凶手与平民）定制的细粒度交互模式。我们的系统通过协调的智能体交互，生成了丰富的多模态上下文，包括角色背景故事、视觉和文本线索以及多步推理链。我们设计了一种两阶段智能体监控训练策略，以增强 VLM 的推理能力：(1) 在模拟不确定性和欺骗性的精选及合成数据集上进行基于思维链（CoT）的微调；(2) 采用基于 GRPO 的强化学习，并结合智能体监控的奖励塑造，鼓励模型发展出特定角色的推理行为和有效的多模态多步推理。大量实验表明，我们的方法显著提升了 VLM 在叙事推理、隐藏事实提取和抗欺骗理解方面的性能。我们的贡献为在不确定、对抗性和社会复杂条件下训练和评估 VLM 提供了一种可扩展的解决方案，为未来非完全信息下的多模态多步推理基准测试奠定了基础。

## Abstract
Vision-language models (VLMs) have shown impressive capabilities in perceptual tasks, yet they degrade in complex multi-hop reasoning under multiplayer game settings with imperfect and deceptive information. In this paper, we study a representative multiplayer task, Murder Mystery Games, which require inferring hidden truths based on partial clues provided by roles with different intentions. To address this challenge, we propose a collaborative multi-agent framework for evaluating and synthesizing high-quality, role-driven multiplayer game scripts, enabling fine-grained interaction patterns tailored to character identities (i.e., murderer vs. innocent). Our system generates rich multimodal contexts, including character backstories, visual and textual clues, and multi-hop reasoning chains, through coordinated agent interactions. We design a two-stage agent-monitored training strategy to enhance the reasoning ability of VLMs: (1) chain-of-thought based fine-tuning on curated and synthetic datasets that model uncertainty and deception; (2) GRPO-based reinforcement learning with agent-monitored reward shaping, encouraging the model to develop character-specific reasoning behaviors and effective multimodal multi-hop inference. Extensive experiments demonstrate that our method significantly boosts the performance of VLMs in narrative reasoning, hidden fact extraction, and deception-resilient understanding. Our contributions offer a scalable solution for training and evaluating VLMs under uncertain, adversarial, and socially complex conditions, laying the groundwork for future benchmarks in multimodal multi-hop reasoning under imperfect information.

---

## 论文详细总结（自动生成）

### 论文总结：协同多智能体剧本生成增强剧本杀中的非完全信息推理

#### 1. 核心问题与整体含义
本文针对视觉语言模型（VLM）在处理**非完全信息（Imperfect Information）**和**欺骗性社交博弈**场景时推理能力不足的问题展开研究。以“剧本杀”（Murder Mystery Games）为代表，这类任务要求模型在多轮对话中整合多模态线索（文本与图像）、识别谎言并进行多跳推理。研究的核心动机在于：高质量的剧本杀数据稀缺且标注成本极高，导致现有模型难以在复杂的对抗性社交环境中进行有效训练和评估。

#### 2. 方法论
论文提出了一个协作式多智能体框架，涵盖了数据生成与模型增强两个核心阶段：

*   **多智能体剧本合成框架**：
    *   **生成智能体（Generation Agents）**：包括 `OutlineAgent`（构建故事大纲）、`CharacterAgent`（细化角色剧本）、`ClueAgent`（生成多模态线索）、`RoleplayAgent`（模拟角色对话）和 `QaAgent`（构建推理问答对）。
    *   **评估智能体（Evaluation Agents）**：`CriticAgent` 负责评估剧本的逻辑一致性并提供反馈；`ScoreAgent` 在训练阶段作为奖励模型。
*   **两阶段智能体监控训练策略**：
    *   **第一阶段：监督微调（SFT）**：在合成的思维链（CoT）数据集上进行微调，使模型继承结构化推理模式和角色扮演行为。
    *   **第二阶段：强化学习（RL）**：采用 **GRPO（Group Relative Policy Optimization）** 算法。通过 `ScoreAgent`（基于 LLM-as-Judge）对模型输出进行奖励建模。
    *   **奖励函数设计**：针对可验证任务（如问答）和不可验证任务（如自我介绍、讨论）设计了不同的奖励函数，涵盖格式规范性、答案正确性、线索匹配度以及角色一致性。

#### 3. 实验设计
*   **数据集与场景**：使用了 34 个原创合成剧本，包含 1,060 个长文本问答对和 2,725 个多模态问答对。
*   **Benchmark**：主要在 **WhodunitBench**（人类标注的剧本杀基准）上进行评估，确保评估的客观性。
*   **评估指标**：
    *   **推理与分析**：多跳多模态推理（MMR）、案件细节分析（CMD）。
    *   **角色扮演与决策**：角色扮演质量（RP）、最终投票决策（DM）。
    *   **感知能力**：长剧本理解（LSU）、文本图像理解（TIU）、复杂图像理解（MIU）。
*   **对比方法**：
    *   **闭源模型**：GPT-4V、Gemini 1.5 Pro、Claude。
    *   **开源模型**：Qwen2.5-VL (3B/7B)、Gemma3 (4B/12B/27B)、Mistral-small (24B)、LLaVA-13B 等。

#### 4. 资源与算力
*   **模型配置**：实验基于 Qwen2.5-VL-3B 和 7B 指令微调版。
*   **训练细节**：使用了 **LoRA** 参数高效微调技术；上下文窗口设置为 **65,536 tokens** 以处理长剧本；图像统一缩放至 512×512。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了使用强大的闭源模型（如 Gemini 2.5 Pro, DeepSeek-V3/R1）作为智能体和评委。

#### 5. 实验数量与充分性
*   **实验规模**：在 3B 和 7B 两个参数量级上进行了完整验证。
*   **消融实验**：
    *   针对训练组件（无 SFT、无 RL、无图像匹配奖励）进行了消融。
    *   针对数据来源（仅人工数据、仅合成数据、混合数据）进行了对比。
*   **客观性保障**：
    *   使用三个独立 LLM（DeepSeek-r1, GPT-4o, Gemini-2.5-Pro）作为评委，并计算了 Pearson 相关系数和 Cohen’s κ 系数，证明了自动评分与人类判断的高度一致性。
    *   评估集采用人类标注数据，避免了“自产自销”的循环评估偏差。

#### 6. 主要结论与发现
*   **性能提升**：该方法显著提升了 VLM 在多跳推理（MMR）和最终决策（DM）上的表现，7B 模型在多项指标上超越了参数量更大的开源模型（如 24B/27B 模型）。
*   **RL 的关键作用**：强化学习（尤其是 GRPO）对于精炼证据选择和决策行为至关重要，显著优于单纯的 SFT。
*   **数据互补性**：合成数据提供了丰富的交互多样性，而人工数据提供了高质量的基准，两者结合效果最佳。

#### 7. 优点
*   **可扩展性**：提出了一套自动化的多智能体剧本生成流程，解决了复杂推理数据匮乏的问题。
*   **创新的奖励机制**：将 GRPO 应用于 VLM，并针对社交博弈中的“欺骗”和“策略性提问”设计了专门的奖励维度。
*   **严谨的评估**：通过多评委交叉验证和人类对齐分析，增强了 LLM-as-Judge 结论的可信度。

#### 8. 不足与局限
*   **自动化程度限制**：在图像线索对齐方面仍需部分人工校验，尚未实现完全的端到端自动化。
*   **场景简化**：剧本杀虽然复杂，但仍是受限环境，与现实中更动态、无结构的法律辩论或商务谈判相比仍有简化。
*   **算力透明度**：缺乏具体的硬件资源消耗数据，不便于其他研究者评估复现成本。
*   **伦理风险**：模型学习“欺骗”技巧可能带来潜在的伦理风险，需进一步制定安全准则。

（完）
