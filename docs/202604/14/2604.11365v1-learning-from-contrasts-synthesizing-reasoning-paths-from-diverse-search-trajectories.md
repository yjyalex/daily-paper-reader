---
title: "Learning from Contrasts: Synthesizing Reasoning Paths from Diverse Search Trajectories"
title_zh: 从对比中学习：从多样化搜索轨迹中合成推理路径
authors: "Peiyang Liu, Zhirui Chen, Xi Wang, Di Liang, Youru Li, Zhi Cai, Wei Ye"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11365v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 从搜索轨迹中合成推理路径
tldr: 针对蒙特卡洛树搜索（MCTS）在推理数据生成中仅保留最优路径而忽略对比信息的低效问题，本文提出对比推理路径合成（CRPS）框架。该框架通过分析高低质量搜索轨迹的差异，提取战略转折点和失败模式，并合成包含成功模式且规避陷阱的推理链。实验表明，CRPS仅需1/20的数据量即可达到传统方法的性能，并显著提升了模型的泛化能力。
selection_source: fresh_fetch
motivation: 现有的推理数据提取方法通常只保留单一最高奖励路径，丢弃了搜索过程中蕴含的丰富对比信号，导致数据利用效率低下。
method: 提出CRPS框架，通过结构化反思过程对比分析不同质量的搜索轨迹，提取关键决策点和失败模式，从而合成高质量的推理路径。
result: 在仅使用6万条合成数据的情况下，模型性能便达到或超过了使用59万条传统拒绝采样数据训练的基准模型，数据效率提升约20倍。
conclusion: 通过学习成功与失败之间的对比信息，模型能够获得比仅学习成功案例更强且更具迁移性的推理能力。
---

## 摘要
蒙特卡洛树搜索 (MCTS) 已广泛用于自动推理数据探索，但目前的监督提取方法仍然效率低下。标准方法仅保留单一的最高奖励轨迹，丢弃了许多探索路径中存在的对比信号。在此，我们提出了对比推理路径合成 (CRPS)，这是一个将监督提取从过滤过程转变为合成过程的框架。CRPS 使用结构化的反思过程来分析高质量和低质量搜索轨迹之间的差异，提取关于关键决策点和局部失败模式的显式信息。这些见解指导推理链的合成，使其在融入成功模式的同时避免已识别的陷阱。实证结果表明，仅在 60K 个 CRPS 合成样本上微调的模型，其性能就达到或超过了在 590K 个基于标准拒绝采样生成的样本上训练的基线模型，数据集规模缩减了 20 倍。此外，CRPS 提高了在域外基准测试上的泛化能力，证明了从成功与失败的对比中学习比仅从成功中学习能产生更具迁移性的推理能力。

## Abstract
Monte Carlo Tree Search (MCTS) has been widely used for automated reasoning data exploration, but current supervision extraction methods remain inefficient. Standard approaches retain only the single highest-reward trajectory, discarding the comparative signals present in the many explored paths. Here we introduce \textbf{Contrastive Reasoning Path Synthesis (CRPS)}, a framework that transforms supervision extraction from a filtering process into a synthesis procedure. CRPS uses a structured reflective process to analyze the differences between high- and low-quality search trajectories, extracting explicit information about strategic pivots and local failure modes. These insights guide the synthesis of reasoning chains that incorporate success patterns while avoiding identified pitfalls. We show empirically that models fine-tuned on just 60K CRPS-synthesized examples match or exceed the performance of baselines trained on 590K examples derived from standard rejection sampling, a 20$\times$ reduction in dataset size. Furthermore, CRPS improves generalization on out-of-domain benchmarks, demonstrating that learning from the contrast between success and failure produces more transferable reasoning capabilities than learning from success alone.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning from Contrasts: Synthesizing Reasoning Paths from Diverse Search Trajectories》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出，虽然蒙特卡洛树搜索（MCTS）在自动生成推理数据方面表现出色，但现有的监督信号提取方法（如拒绝采样微调 RFT）效率极低。这些方法通常采用“胜者为王”的策略，仅保留奖励最高的单一路径，而丢弃了搜索过程中产生的大量失败或次优轨迹。
**研究动机：** 失败的尝试中蕴含着宝贵的“反面教材”信息。论文旨在将数据提取从简单的“过滤”转变为“合成”，通过对比成功与失败的轨迹，提取战略转折点和局部错误模式，从而生成更高质量、更具启发性的训练数据。

### 2. 方法论：CRPS 框架
CRPS（对比推理路径合成）采用**解耦的探索者-分析者（Explorer-Analyst）架构**：

*   **分布感知轨迹收集（Explorer）：** 使用专门的推理模型（如 Qwen2.5-Math）作为探索者运行 MCTS。除了收集正确路径（$\tau^+$），还根据访问计数（Visit Count）采样“硬负样本”（$\tau^-$），即模型非常有信心但最终错误的路径。
*   **分析者对比分析（Analyst）：** 使用能力更强的模型（如 GPT-5-mini）作为分析者，对路径对 $(\tau^+, \tau^-)$ 进行双粒度分析：
    *   **全局战略评论：** 识别高层策略的偏差。
    *   **局部步骤评论：** 通过**语义对齐（Semantic Alignment）**找到逻辑分叉点，解释为什么负样本的某一步导致了失败。
*   **模式启发式路径合成：** 分析者根据上述评论，合成一条全新的推理路径 $\hat{\tau}$。该路径不仅包含正确的逻辑，还会自然地融入解释（例如：“此处应避免使用某种公式，因为...”），从而显式地绕过识别出的陷阱。
*   **验证过滤：** 最终合成的路径必须通过答案正确性校验才会被加入训练集。

### 3. 实验设计
*   **数据集：**
    *   **域内（In-Domain）：** GSM8K 和 MATH 的训练集（约 1.5万个种子问题）。
    *   **域外（Out-of-Domain）：** CollegeMath、DeepMind Math、OlympiadBench、TheoremQA。
    *   **非数学领域：** HumanEval（代码生成）、StrategyQA（常识推理）。
*   **基准测试（Benchmark）：** 在上述数据集的官方测试集上评估准确率、泛化能力和语义稳定性。
*   **对比方法：** 拒绝采样（RFT）、DART-Math（难度感知采样）、SIGMA（局部修复）、MathFusion（多样化聚合）以及 SOTA 开源数据集（DeepScaleR, OpenThought）。
*   **骨干模型：** DeepSeekMath-7B、LLaMA3-8B、Mistral-7B。

### 4. 资源与算力
*   **硬件：** 使用了 8 台 NVIDIA A800 (80GB) GPU。
*   **训练细节：** 采用 DeepSpeed ZeRO-3 优化，全量微调 3 个 epoch，学习率 $2 \times 10^{-5}$，Batch Size 为 128。
*   **效率对比：** 达到约 50% 的 MATH+GSM8K 平均准确率，CRPS 仅需 30K 数据（约 10 GPU 小时），而 RFT/DART-Math 需要 590K 数据（约 192 GPU 小时）。训练时间缩短了约 19 倍。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **规模实验：** 测试了 15K、30K、60K 不同数据规模下的表现。
*   **消融实验：** 验证了合成范式、对比信号的必要性、分析粒度（全局 vs 局部）以及搜索多样性对结果的影响。
*   **泛化与稳定性实验：** 针对推理复杂度（步骤数）和语义扰动（对抗性样本）进行了专项测试。
*   **跨架构验证：** 在三种不同的主流开源模型上均取得了一致的提升。
实验设计客观、公平，通过重现基线方法并使用相同的原始轨迹池确保了对比的严谨性。

### 6. 主要结论与发现
*   **极高的数据效率：** 仅需 60K CRPS 合成数据，性能即可匹配或超越 590K 拒绝采样数据，实现 **20 倍的数据压缩**。
*   **更强的泛化能力：** 相比于仅学习成功案例，从对比中学习能让模型掌握更抽象的解题结构，在域外（OOD）任务上表现更优。
*   **长程推理优势：** 在 7 步以上的复杂难题上，CRPS 的领先优势扩大（比 Vanilla MCTS 高 9.8%），说明其能有效抑制推理过程中的错误累积。
*   **语义稳定性：** 模型对语言扰动和干扰信息的鲁棒性显著增强。

### 7. 优点（亮点）
*   **范式转移：** 将 MCTS 数据利用从“过滤”提升到“合成”，极大挖掘了失败尝试的教学价值。
*   **解耦架构：** 巧妙利用强模型（Analyst）的元认知能力来指导弱模型（Explorer）的进化，降低了对大规模标注数据的依赖。
*   **实用性强：** 显著降低了微调阶段的算力成本，使快速迭代推理模型成为可能。

### 8. 不足与局限
*   **依赖可验证奖励：** 目前仅适用于有明确答案（如数学、代码）的领域，在开放式任务（如创意写作）中难以应用。
*   **前期推理开销：** 虽然训练快，但前期合成阶段需要运行多次 MCTS 和强模型 API 调用，推理成本较高。
*   **分析者能力瓶颈：** 框架高度依赖分析者模型的诊断准确性，若分析者产生幻觉（错误归因），可能导致合成路径质量下降。
*   **闭源模型依赖：** 实验中使用了 GPT-5-mini 作为分析者，对于完全开源的闭环进化仍有一定挑战。

（完）
