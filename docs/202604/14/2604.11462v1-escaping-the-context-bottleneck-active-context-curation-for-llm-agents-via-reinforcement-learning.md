---
title: "Escaping the Context Bottleneck: Active Context Curation for LLM Agents via Reinforcement Learning"
title_zh: 摆脱上下文瓶颈：通过强化学习为大语言模型智能体实现主动上下文策展
authors: "Xiaozhe Li, Tianyi Lyu, Yizhao Yang, Liang Shan, Siyi Yang, Ligao Zhang, Zhuoyi Huang, Qingwen Liu, Yang Li"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11462v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用强化学习精简上下文以增强长程任务中的推理
tldr: 针对大语言模型在长程任务中面临的上下文瓶颈和信息冗余问题，本文提出了一种解耦上下文管理与任务执行的共生框架。该框架引入轻量级策略模型 ContextCurator，通过强化学习训练，主动修剪环境噪声并保留关键推理锚点。实验表明，该方法在 WebArena 和 DeepSearch 任务中显著提升了任务成功率，并大幅降低了 Token 消耗，为高效长程智能体提供了可扩展的范式。
selection_source: fresh_fetch
motivation: 解决长程交互中上下文噪声累积导致的推理性能下降及“迷失中间”现象。
method: 采用 ContextCurator（轻量级策略模型）与 TaskExecutor（冻结的基础模型）配对，利用强化学习训练前者进行主动上下文修剪。
result: "在 WebArena 上成功率提升至 41.2% 且 Token 减少 8.8%，在 DeepSearch 上成功率提升至 57.1% 且 Token 消耗降低 8 倍。"
conclusion: 通过主动上下文管理，可以在提升长程任务性能的同时显著降低计算成本，且小模型可达到与 GPT-4o 相当的管理能力。
---

## 摘要
大语言模型（LLMs）在处理长程任务时面临挑战，这是由于“上下文瓶颈”和“迷失在中间（lost-in-the-middle）”现象，即来自冗长环境的累积噪声会降低多轮交互中的推理能力。为了解决这一问题，我们引入了一个共生框架，将上下文管理与任务执行解耦。我们的架构将一个轻量级、专门化的策略模型 ContextCurator 与一个强大的冻结基础模型 TaskExecutor 配对。通过强化学习训练，ContextCurator 主动降低工作记忆中的信息熵。它在积极剪枝环境噪声的同时，保留了推理锚点（reasoning anchors），即对未来推导至关重要的稀疏数据点。在 WebArena 上，我们的框架将 Gemini-3.0-flash 的成功率从 36.4% 提高到 41.2%，同时将 token 消耗降低了 8.8%（从 47.4K 降至 43.3K）。在 DeepSearch 上，它实现了 57.1% 的成功率（对比 53.9%），同时将 token 消耗降低了 8 倍。值得注意的是，一个 7B 参数的 ContextCurator 达到了与 GPT-4o 相当的上下文管理性能，为自主长程智能体提供了一种可扩展且计算高效的范式。

## Abstract
Large Language Models (LLMs) struggle with long-horizon tasks due to the "context bottleneck" and the "lost-in-the-middle" phenomenon, where accumulated noise from verbose environments degrades reasoning over multi-turn interactions. To address this issue, we introduce a symbiotic framework that decouples context management from task execution. Our architecture pairs a lightweight, specialized policy model, ContextCurator, with a powerful frozen foundation model, TaskExecutor. Trained via reinforcement learning, ContextCurator actively reduces information entropy in the working memory. It aggressively prunes environmental noise while preserving reasoning anchors, that is, sparse data points that are critical for future deductions. On WebArena, our framework improves the success rate of Gemini-3.0-flash from 36.4% to 41.2% while reducing token consumption by 8.8% (from 47.4K to 43.3K). On DeepSearch, it achieves a 57.1% success rate, compared with 53.9%, while reducing token consumption by a factor of 8. Remarkably, a 7B ContextCurator matches the context management performance of GPT-4o, providing a scalable and computationally efficient paradigm for autonomous long-horizon agents.

---

## 论文详细总结（自动生成）

### 论文总结：摆脱上下文瓶颈：通过强化学习为 LLM 智能体实现主动上下文策展

#### 1. 核心问题与研究背景
论文针对大语言模型（LLM）智能体在处理**长程任务（Long-horizon tasks）**时面临的“上下文瓶颈”问题展开研究。在多轮交互中，环境产生的冗余信息（如网页 DOM 树中 90% 的结构噪声）会迅速累积，导致“迷失在中间（Lost-in-the-Middle）”现象，稀释关键推理信号，引发逻辑链断裂。研究动机在于证明：提升智能体性能的关键不在于盲目扩大上下文窗口，而在于提高**信噪比（SNR）**。

#### 2. 方法论：ActiveContext 框架
论文提出了一种**共生架构（Symbiotic Architecture）**，将认知功能解耦：
*   **核心思想**：将上下文管理从“静态存储”转变为“主动决策”。
*   **ContextCurator（上下文策展人）**：一个轻量级策略模型（如 Qwen-7B），负责主动对原始观察进行“信息熵减”，生成精简的工作记忆 $m_t$。
*   **TaskExecutor（任务执行者）**：一个强大的冻结基础模型（如 GPT-4o），仅基于策展后的高保真信息进行推理和行动。
*   **关键技术：多轮 GRPO（MT-GRPO）**：
    *   利用强化学习训练 Curator。
    *   **远端奖励（Distal Reward）**：Curator 本身不直接获得奖励，而是根据 TaskExecutor 最终任务的成功与否（二元奖励 0/1）来优化其策展策略。
    *   **推理锚点（Reasoning Anchors）**：训练 Curator 识别并保留那些虽然文本相似度低、但对未来决策具有因果关键性的稀疏数据点。

#### 3. 实验设计
*   **数据集/场景**：
    *   **WebArena**：真实的网页导航环境，包含购物、论坛、Gitlab 等 5 个子领域，侧重于处理结构化噪声。
    *   **DeepSearch**：复杂的 RAG 搜索环境，包含 HotpotQA、2WikiMultiHop 等 7 个数据集，侧重于处理语义噪声。
*   **Benchmark 与对比方法**：
    *   **基础基准**：No Memory（无记忆）、Full Context（全上下文拼接）。
    *   **对比框架**：Synapse（轨迹示例）、AWM（工作流记忆）、Mem0（长期记忆）、Training-Free GRPO 等。
    *   **跨模型验证**：TaskExecutor 分别采用了 Gemini-3.0-flash、Gemini-2.5-flash、GPT-4o 和 GPT-4o-mini。

#### 4. 资源与算力
*   **硬件**：使用了单节点 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：采用全参数微调，学习率 $1 \times 10^{-6}$，Batch Size 为 8。
*   **推理优化**：使用 vLLM 框架进行高效的并行轨迹采样（Rollout）。
*   **说明**：文中未明确提及具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型 Benchmark 的 12 个子场景中进行了测试，涵盖了从网页导航到多跳推理的多种任务。
*   **消融实验**：详细对比了 RL 训练前后的 Curator 性能，以及不同规模模型（从 7B 到 GPT-4o）作为策展人的表现。
*   **客观性**：通过“成功率（SR）”和“Token 消耗量”双重指标评估，证明了该方法在提升性能的同时确实降低了成本。实验设计考虑了跨模型泛化，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能突破**：在 WebArena 上，将 Gemini-3.0-flash 的成功率从 36.4% 提升至 41.2%，同时 Token 消耗降低 8.8%。
*   **极端压缩**：在 DeepSearch 任务中，成功率提升的同时，Token 消耗实现了 **8 倍的惊人削减**（从 46.7K 降至 6.6K）。
*   **能力解耦**：一个经过 RL 微调的 **7B 小模型**，其上下文管理能力可以**匹配甚至超越未经微调的 GPT-4o**。
*   **锚点保留**：主动策展能有效保留空间坐标、用户参数等关键“推理锚点”，而这些信息在传统的摘要或检索方法中极易丢失。

#### 7. 优点
*   **架构创新**：解耦了“管理”与“执行”，解决了单体模型在多目标优化时的梯度干扰问题。
*   **高效节能**：大幅降低了长程交互中的 API 调用成本和计算延迟。
*   **RL 驱动的精准性**：通过远端奖励机制，使模型学会了真正理解任务因果，而非简单的文本压缩。

#### 8. 不足与局限
*   **任务领域限制**：目前仅在 Web 和 Search 两个领域验证，对于代码库维护、长文档创作等其他长程场景的适用性尚待观察。
*   **训练成本**：虽然推理高效，但 RL 训练过程需要大量的环境交互采样，对算力有一定要求。
*   **Executor 依赖性**：Curator 的表现受限于 TaskExecutor 的反馈质量，如果执行者模型能力过弱，可能无法提供有效的奖励信号。

（完）
