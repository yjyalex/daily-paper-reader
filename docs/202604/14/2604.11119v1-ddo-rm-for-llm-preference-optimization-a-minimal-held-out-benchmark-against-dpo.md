---
title: "DDO-RM for LLM Preference Optimization: A Minimal Held-Out Benchmark against DPO"
title_zh: 用于大语言模型偏好优化的 DDO-RM：一项针对 DPO 的极简留出法基准研究
authors: "Tiantian Zhang, Jierui Zuo, Wenping Wang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11119v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: DPO与奖励引导决策分布更新的对比
tldr: 本研究针对大语言模型偏好优化，对比了传统的DPO与新提出的DDO-RM方法。DDO-RM将每个提示视为有限决策问题，通过奖励模型引导的决策分布更新来优化策略，而非仅优化二元对立关系。在Pythia-410m模型和UltraFeedback数据集上的实验表明，DDO-RM在准确率、AUC和边际得分上均优于DPO，为偏好优化提供了新的算法视角。
selection_source: fresh_fetch
motivation: 探究在极简的成对偏好设定下，基于奖励引导的决策分布更新是否能比直接的成对优化目标表现更好。
method: DDO-RM将提示视为决策问题，通过在候选响应上构建策略分布并蒸馏奖励引导的目标分布来优化模型。
result: 实验显示DDO-RM在平均对准确率、AUC和平均边际得分等指标上均显著优于DPO。
conclusion: 初步结果证明了DDO-RM在偏好优化中的有效性，为超越直接成对优化的算法设计提供了有力支持。
---

## 摘要
本文围绕 DPO 与 DDO-RM 偏好优化项目重新组织了现有手稿，并重点关注两个部分：算法视角和初步的留出法基准。该基准提出了一个具体的问题：即使在极简的成对“选中 vs 拒绝”设置下，基于奖励引导的决策分布更新是否能优于直接的成对目标函数？我们在 EleutherAI/pythia-410m 模型上，使用 HuggingFaceH4/ultrafeedback_binarized 数据集对比了直接偏好优化 (DPO) 与 DDO-RM，在留出的 test_prefs 划分集上进行了评估，并报告了随机种子 42、13 和 3407 的结果。在算法上，DDO-RM 将每个提示词视为针对候选回复的有限决策问题。它不仅优化二元的“选中-拒绝”关系，还构建了候选回复上的策略分布，在该分布下对奖励模型评分进行中心化处理，并将奖励引导的目标分布蒸馏回策略中。在当前的公开基准测试中，相较于 DPO，DDO-RM 将平均成对准确率从 0.5238 提升至 0.5602，AUC 从 0.5315 提升至 0.5382，平均边际从 0.1377 提升至 0.5353。这些结果令人鼓舞，但仍属初步阶段：该研究仅涵盖了一个模型系列、一个数据集、一个留出评估划分集以及三个随机种子。

## Abstract
This paper reorganizes the current manuscript around the DPO versus DDO-RM preference-optimization project and focuses on two parts: the algorithmic view and the preliminary held-out benchmark. The benchmark asks a narrow question: even in a minimal pairwise chosen-versus-rejected setting, can a reward-guided decision-distribution update outperform a direct pairwise objective? We compare Direct Preference Optimization (DPO) against DDO-RM on EleutherAI/pythia-410m using HuggingFaceH4/ultrafeedback\_binarized, evaluate on the held-out test\_prefs split, and report results for seeds 42, 13, and 3407.   Algorithmically, DDO-RM treats each prompt as a finite decision problem over candidate responses. Instead of optimizing only a binary chosen-rejected relation, it forms a policy distribution over candidates, centers reward-model scores under that distribution, and distills a reward-guided target distribution back into the policy. In the current public benchmark, DDO-RM improves mean pair accuracy from 0.5238 to 0.5602, AUC from 0.5315 to 0.5382, and mean margin from 0.1377 to 0.5353 relative to DPO. These are encouraging but still preliminary results: the study covers one model family, one dataset, one held-out evaluation split, and three seeds.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）偏好优化中的 DDO-RM 方法进行了结构化分析，并将其与主流的 DPO 方法在极简基准下进行了对比。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：在大语言模型对齐阶段，直接偏好优化（DPO）已成为标准方法，它将对齐视为二元分类问题。然而，作者提出：如果将每个提示词（Prompt）视为一个有限的决策问题，利用奖励模型（RM）引导决策分布的更新，是否能比 DPO 这种直接的成对目标函数表现更好？
*   **核心目标**：在最简单的“选中 vs 拒绝”成对设定下，验证基于奖励引导的决策分布优化（DDO-RM）的有效性，并提供一个可复现的极简留出法（Held-out）基准。

### 2. 论文提出的方法论：DDO-RM
*   **核心思想**：DDO-RM 不仅仅优化二元关系，而是将候选响应视为一个概率分布，通过奖励分数对该分布进行“一步镜像更新”，然后将更新后的目标分布蒸馏回策略模型中。
*   **关键技术细节**：
    1.  **策略分布构建**：计算候选响应在当前策略下的平均 Token 对数概率，通过 Softmax 形成决策分布 $p$。
    2.  **奖励中心化（Centering）**：计算每个候选响应的奖励分数 $r_i$，并减去当前策略下的加权平均奖励，得到中心化奖励 $\tilde{r}_i$。这一步确保了只保留相对当前的优势。
    3.  **目标分布生成**：利用步长 $\eta$ 扰动原始得分，生成新的目标分布 $q = \text{softmax}((s + \eta\tilde{r})/\tau)$。
    4.  **策略蒸馏**：使用交叉熵损失函数（Cross-Entropy），使策略模型 $p_\theta$ 逼近目标分布 $q$。
*   **算法流程**：该方法通过奖励模型提供的分数，在得分空间进行一步梯度式的调整，从而将概率质量从低奖励响应转移到高奖励响应。

### 3. 实验设计
*   **基础模型**：EleutherAI/pythia-410m。
*   **数据集**：HuggingFaceH4/ultrafeedback_binarized（二值化的 UltraFeedback 数据集）。
*   **基准测试（Benchmark）**：在留出的 `test_prefs` 划分集上进行评估。
*   **对比方法**：直接偏好优化（DPO）作为强基线。
*   **评估指标**：
    *   **成对准确率（Pair Accuracy）**：选中项得分高于拒绝项的比例。
    *   **AUC**：衡量得分对选中/拒绝响应的区分能力。
    *   **平均边际（Mean Margin）**：选中项与拒绝项得分之差的平均值。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **资源公开**：作者提供了 GitHub 仓库地址，包含代码、配置、日志和绘图脚本，强调了实验的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：实验设计较为克制，仅涵盖了 1 个模型系列、1 个数据集和 1 个留出评估集。
*   **重复实验**：为了保证结果的客观性，作者使用了 3 个不同的随机种子（42, 13, 3407）进行重复实验。
*   **充分性评价**：作为“极简基准（Minimal Benchmark）”，其实验在所定义的范围内是客观且公平的，但对于大规模模型或多任务场景的泛化性验证尚不充分。

### 6. 主要结论与发现
*   **性能提升**：在所有三个随机种子上，DDO-RM 均优于 DPO。
    *   平均成对准确率从 0.5238 提升至 **0.5602**。
    *   平均 AUC 从 0.5315 提升至 **0.5382**。
    *   平均边际（Mean Margin）从 0.1377 显著提升至 **0.5353**。
*   **边际效应**：DDO-RM 在扩大选中项与拒绝项之间的得分差距（Margin）方面表现尤为突出，这表明其目标函数能更强力地分离偏好。

### 7. 优点
*   **算法视角新颖**：将偏好优化重新解释为得分空间的镜像更新和分布蒸馏，为理解对齐算法提供了新的数学视角。
*   **扩展性强**：DDO-RM 的公式天然支持多候选（Listwise）场景，比仅限于成对比较的 DPO 更容易扩展到重排序（Reranking）任务。
*   **透明度高**：提供了完整的实验配置和输出日志，符合开放科学原则。

### 8. 不足与局限
*   **实验覆盖面窄**：仅在 410M 的小模型上验证，尚未在大规模参数模型（如 7B 或 70B）上测试。
*   **方差较大**：实验观察到 DDO-RM 在不同种子间的表现波动（尤其是 Mean Margin）比 DPO 更大，显示出一定的训练不稳定性。
*   **依赖奖励模型**：DDO-RM 的效果高度依赖于训练期间奖励模型的质量，若奖励模型存在偏差，可能会导致策略失效。
*   **评估局限**：目前的评估仍基于成对数据，未能充分展示其在多候选列表（Listwise）设定下的原生优势。

（完）
