---
title: Bimanual Robot Manipulation via Multi-Agent In-Context Learning
title_zh: 通过多智能体上下文学习实现双臂机器人操作
authors: "Alessio Palma, Indro Spinelli, Vignesh Prasad, Luca Scofano, Yufeng Jin, Georgia Chalvatzaki, Fabio Galasso"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20348v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于协调任务的多智能体领导者-跟随者框架
tldr: 本研究提出BiCICLe框架，利用大语言模型的上下文学习能力实现双臂机器人操作。针对双臂协作中动作空间高维和协调难的问题，该框架将控制建模为多智能体主从协作模式，通过解耦动作空间、引入“手臂辩论”迭代优化及“LLM裁判”评估，实现了无需微调的少样本双臂操控。在TWIN基准测试中，BiCICLe在13项任务上表现优异，显著超越了现有的无训练基准，并展现出强大的泛化能力。
selection_source: fresh_fetch
motivation: 针对双臂机器人操作中高维动作空间和复杂协调约束导致标准上下文学习难以处理的问题。
method: 提出BiCICLe框架，将双臂控制解耦为主从多智能体协作，并结合迭代辩论与裁判机制优化轨迹。
result: "在TWIN基准的13个任务中达到71.1%的平均成功率，优于最佳无训练基准及多数监督学习方法。"
conclusion: 证明了通过多智能体协作和上下文学习，无需特定任务训练即可实现高效且泛化性强的双臂机器人操控。
---

## 摘要
大语言模型（LLMs）已成为具身控制的强大推理引擎。特别是，上下文学习（ICL）使现成的纯文本 LLM 能够在不进行任何特定任务训练的情况下预测机器人动作，同时保留其泛化能力。将 ICL 应用于双臂操作仍然具有挑战性，因为高维联合动作空间和紧密的手臂间协调约束会迅速使标准的上下文窗口过载。为解决这一问题，我们引入了 BiCICLe（双臂协调上下文学习），这是首个使标准 LLM 能够在无需微调的情况下执行少样本双臂操作的框架。BiCICLe 将双臂控制建模为多智能体主从问题，将动作空间解耦为顺序的、有条件的单臂预测。这自然地扩展到了“手臂辩论”（Arms' Debate）——一种迭代优化过程，并引入了第三个“LLM 作为裁判”（LLM-as-Judge）来评估并选择最合理的协调轨迹。在 TWIN 基准测试的 13 个任务中进行的评估显示，BiCICLe 实现了高达 71.1% 的平均成功率，比表现最好的免训练基准高出 6.7 个百分点，并超过了大多数监督学习方法。我们进一步展示了在面对新任务时强大的少样本泛化能力。

## Abstract
Language Models (LLMs) have emerged as powerful reasoning engines for embodied control. In particular, In-Context Learning (ICL) enables off-the-shelf, text-only LLMs to predict robot actions without any task-specific training while preserving their generalization capabilities. Applying ICL to bimanual manipulation remains challenging, as the high-dimensional joint action space and tight inter-arm coordination constraints rapidly overwhelm standard context windows. To address this, we introduce BiCICLe (Bimanual Coordinated In-Context Learning), the first framework that enables standard LLMs to perform few-shot bimanual manipulation without fine-tuning. BiCICLe frames bimanual control as a multi-agent leader-follower problem, decoupling the action space into sequential, conditioned single-arm predictions. This naturally extends to Arms' Debate, an iterative refinement process, and to the introduction of a third LLM-as-Judge to evaluate and select the most plausible coordinated trajectories. Evaluated on 13 tasks from the TWIN benchmark, BiCICLe achieves up to 71.1% average success rate, outperforming the best training-free baseline by 6.7 percentage points and surpassing most supervised methods. We further demonstrate strong few-shot generalization on novel tasks.

---

## 论文详细总结（自动生成）

### 论文总结：通过多智能体上下文学习实现双臂机器人操作 (BiCICLe)

#### 1. 核心问题与整体含义
双臂机器人操作（如拧瓶盖、抬托盘）要求两臂在位置、姿态和力量上高度同步。相比单臂控制，其挑战在于：
*   **高维动作空间**：双臂联合动作空间呈指数级增长（$Z^{14}$）。
*   **严苛的协调约束**：一臂的微小误差需另一臂即时补偿，否则任务立即失败。
*   **数据依赖**：传统的模仿学习或强化学习需要海量特定任务的数据。
*   **LLM 瓶颈**：现有的上下文学习（ICL）在处理双臂任务时，由于动作序列过长且复杂，容易超出 LLM 的上下文窗口或导致逻辑混乱。

本研究提出了 **BiCICLe** 框架，旨在利用大语言模型的推理能力，在**无需任何特定任务训练或微调**的情况下，通过少样本（Few-shot）实现复杂的双臂协调操作。

#### 2. 方法论
BiCICLe 的核心思想是将双臂控制解耦为**多智能体领导者-跟随者（Leader-Follower）**协作模式。

*   **动作与观察表征**：
    *   将工作空间划分为 $100^3$ 的体素（Voxel），旋转离散化为 $5^\circ$ 的区间。
    *   观察结果被序列化为包含物体语义名称和 3D 坐标的文本字典。
*   **主从解耦架构**：
    *   **阶段 1（领导者预测）**：指定一臂为 Leader，根据场景观察预测其完整动作轨迹。
    *   **阶段 2（跟随者预测）**：Follower 智能体在观察场景的同时，将 Leader 的预测计划作为输入条件，从而生成与之匹配的协调动作。
*   **手臂辩论（Arms' Debate）**：引入迭代优化机制。Leader 根据 Follower 的初步计划重新调整，Follower 再根据调整后的 Leader 计划进行最终优化，以增强时空同步性。
*   **Best-of-N 与 LLM 裁判**：并行生成多个候选轨迹，由第三个 LLM 充当“裁判”，根据碰撞风险、目标匹配和抓取逻辑进行评分，选出得分最高的方案。

#### 3. 实验设计
*   **数据集/场景**：使用 **TWIN 基准测试**，包含 13 个双臂操作任务（如推箱子、递交物品、抬托盘、清理垃圾等）。
*   **对比方法**：
    *   **免训练基准**：RoboPrompt（单/双智能体变体）、KAT（基于视觉关键点）、VLM-LF（基于视觉语言模型）。
    *   **监督学习方法（参考上限）**：ACT、PerAct 2、$\pi_0$-keypose、3DFA 等。
*   **泛化性测试**：在 TWIN 之外设计了“关罐盖”和“从箱中取物”两个新任务，测试模型的零样本迁移能力。
*   **真机验证**：在真实的双臂 Franka Panda 机器人上进行了“抬箱子”和“开锅盖”实验。

#### 4. 资源与算力
*   **模型骨干**：主要实验基于 **GPT-5-mini**（闭源 API）；消融实验使用了 **Qwen 2.5 7B**（开源模型）。
*   **算力说明**：论文未详细列出具体的 GPU 训练时长，因为该方法是**免训练（Training-free）**的。
*   **推理延迟**：提供了推理统计数据，BiCICLe 基础版每集平均延迟约 125 秒，Best-of-N 模式约 150 秒（支持并行调用）。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 3 个随机种子 $\times$ 100 次测试，数据量足以支撑统计显著性。
*   **消融实验**：非常充分，涵盖了领导者分配（左/右手）、对话式优化 vs. 辩论机制、观察表征（是否含旋转）、点云提取算法等多个维度。
*   **客观性**：对比了从经典的模仿学习到最新的视觉-语言-动作（VLA）模型，并使用了开源模型验证了架构的普适性，实验设计较为公正。

#### 6. 主要结论与发现
*   **性能卓越**：BiCICLe + Best-of-N 达到了 **71.1%** 的平均成功率，比最佳免训练基准高出 6.7%，甚至超过了部分监督学习方法（如 ACT 和 PerAct 2）。
*   **解耦的必要性**：实验证明，单智能体（SA）难以处理高维空间，而独立双智能体（DA）缺乏协调；BiCICLe 的主从架构有效平衡了维度与协调。
*   **泛化优势**：在面对全新任务时，BiCICLe 的成功率（54.5%）远高于经过微调的 SOTA 监督模型 3DFA（10.0%），证明了 ICL 在长尾任务中的潜力。

#### 7. 优点
*   **架构优雅**：通过简单的领导者-跟随者逻辑解决了复杂的双臂同步问题。
*   **部署成本低**：无需昂贵的 GPU 训练集群，仅需少量演示即可快速适配新任务。
*   **鲁棒性增强**：通过“辩论”和“裁判”机制显著降低了 LLM 随机采样带来的失败率。

#### 8. 不足与局限
*   **精度瓶颈**：由于采用体素化和离散化动作，对于极精细的操作（如穿针引线、抓取细绳）精度不足。
*   **感知依赖**：目前高度依赖准确的物体分割和质心提取，在复杂遮挡或光照变化的现实环境中可能受限。
*   **上下文限制**：受限于 LLM 的上下文窗口，无法在 Prompt 中放入过多的演示示例或处理极长步数的任务。

（完）
