---
title: "HiCrew: Hierarchical Reasoning for Long-Form Video Understanding via Question-Aware Multi-Agent Collaboration"
title_zh: HiCrew：通过问题感知的多智能体协作实现长视频理解的分层推理
authors: "Yuehan Zhu, Jingqi Zhao, Jiawen Zhao, Xudong Mao, Baoquan Zhao"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21444v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于长视频推理的分层多智能体协作
tldr: 针对长视频理解中时空冗余和叙事依赖的挑战，本文提出HiCrew框架。该框架通过混合树结构保留视频的时间拓扑，利用问题感知字幕生成精准语义描述，并引入规划层实现多智能体动态协作。实验证明，HiCrew在EgoSchema和NExT-QA基准测试中表现优异，尤其在时序和因果推理任务上提升显著。
selection_source: fresh_fetch
motivation: 现有的长视频理解方法往往牺牲了对因果推理至关重要的时间连贯性，且多智能体框架缺乏针对特定问题的灵活性。
method: 提出一种结合镜头边界检测的混合树结构、问题感知的字幕机制以及可动态编排智能体协作的规划层。
result: 在EgoSchema和NExT-QA等数据集上的实验验证了该方法的有效性，特别是在需要复杂时序和因果推理的任务中。
conclusion: HiCrew通过层次化结构保留和动态多智能体协作，显著提升了长视频理解的准确性和推理能力。
---

## 摘要
长视频理解仍然面临着普遍的时空冗余以及跨越长时域的复杂叙事依赖关系的根本挑战。虽然最近的结构化表示有效地压缩了视觉信息，但它们经常牺牲对因果推理至关重要的时间连贯性。同时，现有的多智能体框架通过僵化的、预定义的流程运行，无法根据特定问题的需求调整其推理策略。在本文中，我们介绍了 HiCrew，这是一个分层多智能体框架，通过三项核心贡献解决了这些局限性。首先，我们提出了一种混合树（Hybrid Tree）结构，利用镜头边界检测来保留时间拓扑，同时在语义连贯的片段内执行相关性引导的分层聚类。其次，我们开发了一种问题感知字幕（Question-Aware Captioning）机制，该机制合成意图驱动的视觉提示，以生成面向精准度的语义描述。第三，我们集成了一个规划层（Planning Layer），通过根据问题复杂度自适应地选择角色和执行路径，动态地编排智能体协作。在 EgoSchema 和 NExT-QA 上的广泛实验验证了我们方法的有效性，展示了在各种问题类型上的强大性能，特别是在受益于我们分层结构保留设计的时间和因果推理任务中，提升尤为显著。

## Abstract
Long-form video understanding remains fundamentally challenged by pervasive spatiotemporal redundancy and intricate narrative dependencies that span extended temporal horizons. While recent structured representations compress visual information effectively, they frequently sacrifice temporal coherence, which is critical for causal reasoning. Meanwhile, existing multi-agent frameworks operate through rigid, pre-defined workflows that fail to adapt their reasoning strategies to question-specific demands. In this paper, we introduce HiCrew, a hierarchical multi-agent framework that addresses these limitations through three core contributions. First, we propose a Hybrid Tree structure that leverages shot boundary detection to preserve temporal topology while performing relevance-guided hierarchical clustering within semantically coherent segments. Second, we develop a Question-Aware Captioning mechanism that synthesizes intent-driven visual prompts to generate precision-oriented semantic descriptions. Third, we integrate a Planning Layer that dynamically orchestrates agent collaboration by adaptively selecting roles and execution paths based on question complexity. Extensive experiments on EgoSchema and NExT-QA validate the effectiveness of our approach, demonstrating strong performance across diverse question types with particularly pronounced gains in temporal and causal reasoning tasks that benefit from our hierarchical structure-preserving design.

---

## 论文详细总结（自动生成）

以下是对论文《HiCrew: Hierarchical Reasoning for Long-Form Video Understanding via Question-Aware Multi-Agent Collaboration》的结构化深入分析：

### 1. 核心问题与整体含义（研究动机和背景）
长视频理解面临两大核心挑战：一是**时空冗余与叙事依赖**，长视频包含大量无关信息，且关键情节跨度大；二是**现有方法的局限性**。目前的结构化表示（如 VideoTree）虽能压缩信息，但往往采用全局聚类，破坏了视频的时间连贯性（Temporal Coherence），这对因果推理至关重要。此外，现有的多智能体（Multi-Agent）框架通常采用预定义的静态工作流，无法根据问题的复杂程度灵活调整推理策略，导致简单问题浪费算力，复杂问题推理深度不足。

### 2. 方法论：核心思想与关键技术
HiCrew 提出了一个分层多智能体框架，核心由三个组件构成：

*   **混合树结构（Hybrid Tree Construction）：**
    *   **时间拓扑保留：** 首先利用镜头边界检测（Shot Boundary Detection）将视频分割为有序的镜头序列 $S = \{s_1, s_2, \dots, s_N\}$，确保叙事结构不被破坏。
    *   **相关性引导扩展：** 使用轻量级 LLM 对每个镜头进行评分（基于代表帧和初步字幕）。只有得分高于阈值 $\tau$ 的高相关性镜头才会进行基于 K-Means 的分层聚类扩展，以捕获细节；低相关性镜头则保持粗粒度。
*   **问题感知字幕（Question-Aware Captioning）：**
    *   **意图驱动：** 将问题分为因果、时序、描述三类，由 LLM 生成针对性的视觉引导提示（Visual Prompt）。
    *   **上下文自适应：** 根据问题涉及的范围（全局 vs 局部），动态选择从树的宽度层（覆盖全片）或深度层（聚焦细节）提取帧，生成消除冗余且信息精准的语义摘要。
*   **分层多智能体协作（Hierarchical Multi-Agent Collaboration）：**
    *   **规划层（Planning Layer）：** 包含问题分析智能体（负责分类和角色选择）和任务规划智能体（生成结构化工作流）。它能根据问题难度动态决定调用哪些智能体。
    *   **执行层（Execution Layer）：** 采用 ReAct 范式。**文本智能体**负责检索语义摘要；**视觉智能体**负责关键片段的深度视觉验证；**证据整合智能体**根据问题类型进行加权融合（如描述类问题赋予视觉证据更高权重）。

### 3. 实验设计
*   **数据集：**
    *   **EgoSchema：** 包含 5000+ 个多选题，视频时长约 180 秒，侧重长时域推理。
    *   **NExT-QA：** 包含约 5.2 万个问答对，侧重因果（Causal）、时序（Temporal）和描述（Descriptive）推理。
*   **Benchmark 与对比方法：**
    *   对比了包括 VideoTree、AKeyS、VideoAgent、LLoVi、InternVideo2、VideoLLaMA3 等在内的十余种先进模型。
    *   使用了 GPT-4o 作为推理后端，EVA-CLIP-8B 作为视觉编码器。

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量及训练/推理总时长。
*   **模型组件：** 视觉特征提取使用 EVA-CLIP-8B，字幕生成在 EgoSchema 上使用 BLIP-2，在 NExT-QA 上使用 LaViLa。
*   **推理效率：** 文中提到查询平均在 1.3 轮迭代内收敛（最大限制 15 轮），通过动态规划减少了不必要的智能体调用，从而优化了计算开销。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流长视频基准测试上进行了完整评估，涵盖了 Subset 和 Full set。
*   **消融实验：** 针对“混合树”、“问题感知字幕”和“规划层”分别做了消融，结果显示移除任何一项都会导致性能大幅下降（下降幅度在 9.6% 至 12.9% 之间）。
*   **客观性：** 实验对比了相同 Backbone（GPT-4o）下的不同框架（如 AKeyS），证明了性能提升源于架构设计而非单纯的语言模型升级。定性分析部分通过具体案例展示了模型如何通过双向时间搜索解决因果问题，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先：** HiCrew 在 EgoSchema 全集上达到 64.6% 的准确率，在 NExT-QA 上达到 79.5% 的平均准确率，均创下新纪录。
*   **结构保留的重要性：** 相比于 VideoTree 的全局聚类，HiCrew 通过镜头分割保留时间顺序，在因果推理任务上表现出显著优势。
*   **动态规划的价值：** 动态选择智能体角色和推理路径不仅提高了准确性，还证明了针对不同问题类型定制策略的必要性。

### 7. 优点
*   **创新的混合结构：** 巧妙结合了镜头边界检测（硬分割）和分层聚类（软压缩），解决了长视频理解中“细节”与“逻辑顺序”不可兼得的矛盾。
*   **高度的自适应性：** 规划层使系统能够像人类一样“看题下菜”，提高了推理的灵活性。
*   **精准的语义引导：** 问题感知的字幕机制避免了通用描述在面对特定细节提问时的乏力。

### 8. 不足与局限
*   **依赖闭源模型：** 核心推理高度依赖 GPT-4o，这可能限制了其在私有化部署或低成本场景下的应用。
*   **镜头检测依赖：** 混合树的第一步依赖镜头边界检测，若视频剪辑极其零碎或缺乏明显转场，可能会影响初始分段的质量。
*   **推理延迟：** 尽管平均轮数较少，但多智能体协作和多轮 VLM 调用在实时性要求极高的场景下可能存在延迟挑战。

（完）
