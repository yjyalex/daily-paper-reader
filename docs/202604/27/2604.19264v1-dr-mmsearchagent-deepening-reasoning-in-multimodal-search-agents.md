---
title: "DR-MMSearchAgent: Deepening Reasoning in Multimodal Search Agents"
title_zh: DR-MMSearchAgent：深化多模态搜索智能体的推理能力
authors: "Shengqin Wang, Wentao Yan, Huichi Zhou, Yihang Chen, Kun Shao, Zhizhong Zhang, Yuan Xie"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19264v1"
tags: ["query:token"]
score: 9.0
evidence: 步进式奖励信号与轨迹优势信号
tldr: "针对多模态搜索智能体交互过早崩溃的问题，本文提出DR-MMSearchAgent框架。该框架利用结构邻近性从完整轨迹提取优势信号以鼓励探索，并采用差异化高斯奖励动态校准交互容忍度，减少冗余。配合新构建的3602个多步推理样本，该方法在FVQA-test上性能提升8.4%，显著优于现有SOTA模型。"
selection_source: fresh_fetch
motivation: 现有智能体因奖励信号滞后和上下文冗余，导致在复杂搜索任务中难以维持有效的多轮交互并容易产生过早崩溃。
method: 提出利用结构邻近性提取全轨迹优势信号，并结合差异化高斯奖励机制来动态优化交互深度与信息可靠性。
result: "实验显示该方法在FVQA-test数据集上比MMSearch-R1性能高出8.4%，达到了当前最先进的水平。"
conclusion: 通过强化推理深度和优化奖励分配机制，DR-MMSearchAgent有效解决了多模态搜索中的交互崩溃与信息冗余问题。
---

## 摘要
代理式多模态模型因其利用外部工具处理复杂任务的能力而受到广泛关注。然而，研究发现这类智能体经常遭遇过早的交互崩溃，主要原因有两点：1）终端奖励通常附加在最后一个 token 上，导致优势函数无法区分具有探索性行为的轨迹；2）过度冗余的上下文阻碍了智能体吸收有用的反馈。为了解决这些问题，我们提出了 DR-MMSearchAgent（深化推理多模态搜索智能体）。该框架利用结构邻近性从整个批次的完整展开轨迹中推导优势信号，从而进一步鼓励生成不同长度的轨迹，即使它们包含相同的正确答案。此外，采用差异化高斯奖励来动态校准交互容差，从而确保信息的可靠性并减少冗余。为了支持多轮交互训练，我们构建了一个多步深度推理数据集，包含 3602 个高质量问答对，且每个问答对至少包含 3 个推理步骤。大量实验表明，我们的方法达到了最先进的性能，在 FVQA-test 上比 MMSearch-R1 高出 8.4%。

## Abstract
Agentic multimodal models have garnered significant attention for their ability to leverage external tools to tackle complex tasks. However, it is observed that such agents often meet premature interaction collapse, caused by two primary reasons: 1) the terminal reward often appending on the last token prevents the advantage from distinguishing trajectories with exploratory behavior; 2) excessively redundant context hinders the agent from absorbing useful feedback. To address these issues, we propose the Deepening Reasoning MMSearchAgent, the framework leverages the structural proximity to derive advantage signals from the whole rollout trajectories in an entire batch, such that trajectories of different lengths are further encouraged to be generated, even when containing the same correct answer. Additionally, differentiated gaussian rewards are employed to dynamically calibrate interaction tolerance, thereby ensuring information reliability and reduce redundancy. To support multi-turn interaction training, we have constructed a multi-step deep-reasoning dataset including 3602 high-quality QA pair with at least 3 reasonning steps. Extensive experiments demonstrate that our method achieves state-of-the-art performance, outperforming the MMSearch-R1 by 8.4$\%$ on FVQA-test.

---

## 论文详细总结（自动生成）

这是一份关于论文《DR-MMSearchAgent: Deepening Reasoning in Multimodal Search Agents》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**代理式多模态搜索智能体（Agentic Multimodal Search Agents）**在处理复杂视觉问答（VQA）任务时的“交互崩溃”问题。
*   **研究动机**：现有的强化学习（RL）训练方法（如 MMSearch-R1）往往导致智能体过早停止搜索或陷入次优的短路径交互。
*   **核心痛点**：
    1.  **奖励信号稀疏且模糊**：终端奖励通常只挂在最后一个 token 上，无法区分具有探索性行为的长轨迹和简单的短轨迹。
    2.  **上下文冗余**：外部搜索工具返回的大量无关信息干扰了智能体的推理，导致其难以吸收有效反馈。
*   **目标**：通过优化奖励机制和数据质量，诱导智能体进行更深层次、多轮次的有效推理。

### 2. 方法论
论文提出了 **DR-MMSearchAgent** 框架，包含以下核心技术：
*   **结构邻近权重优势注入（SPAI）**：
    *   **核心思想**：借鉴多准则决策中的 TOPSIS 方法，通过计算轨迹与“理想结构边界”的欧几里得距离，量化轨迹的结构特征（如 CoT 长度）。
    *   **技术细节**：在 GRPO 算法的基础上，将优势信号（Advantage）与结构得分结合。对于低质量轨迹，利用 SPAI 放大其负向信号，从而强制模型探索更多样化、更具深度的推理路径，避免所有轨迹收敛到相同的短长度。
*   **双向引导自适应平滑奖励（BGAS）**：
    *   **核心思想**：利用高斯核函数动态调整交互深度。
    *   **算法流程**：如果答案正确，奖励函数倾向于“效率优先”（惩罚冗余调用）；如果答案错误，则切换为“探索驱动”（鼓励更多工具调用以获取证据）。
*   **压缩精炼智能体（Refining Agent）**：
    *   在推理过程中实时对搜索结果进行去噪和压缩（减少约 80% 的冗余文本），确保上下文窗口的高效利用。
*   **BridgeVQA 数据集**：
    *   构建了一个包含 3602 个高质量多步推理样本的数据集，确保每个问题至少需要 3 跳（3-hop）推理路径。

### 3. 实验设计
*   **数据集/场景**：
    *   **搜索基准**：FVQA-test, InfoSeek, SimpleVQA, LiveVQA, MMSearch。
    *   **通用视觉任务（泛化性测试）**：MathVista, MathVerse, MathVision, ChartQA, HallusionBench。
*   **Benchmark 与对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 2.5 Pro, Claude-3.7-Sonnet。
    *   **开源/智能体方法**：Qwen2.5-VL 系列, MMSearch-R1, DeepMMSearch-R1, WebWatcher, DeepEyes2。
*   **评估指标**：LLM-as-Judge（使用本地部署的 Qwen2.5-72B 作为裁判）。

### 4. 资源与算力
*   **模型底座**：Qwen2.5-VL-7B。
*   **RL 框架**：使用 **veRL** 框架进行强化学习优化。
*   **训练细节**：Batch size 为 128，每个问题进行 8 次采样（rollouts），训练 2 个 epoch。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号和数量（如 A100/H100 的具体枚数），但提到在训练阶段使用了低成本的本地工具服务，而非昂贵的在线搜索 API。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个搜索类基准和 5 个通用视觉推理基准，实验覆盖面广。
*   **消融实验**：针对 BridgeVQA 数据集、SPAI 机制、BGAS 机制和精炼智能体分别做了消融，验证了各模块的增量贡献。
*   **鲁棒性分析**：对 SPAI 的注入比例（N%）和 BGAS 的参数（μ, σ）进行了敏感性分析。
*   **充分性评价**：实验设计较为客观，通过与多种 SOTA 模型在相同协议下对比，证明了其在提升搜索深度和准确率方面的有效性。

### 6. 主要结论与发现
*   **性能突破**：在 FVQA-test 上达到 66.8% 的准确率，比 MMSearch-R1 高出 **8.4%**。
*   **探索能力**：SPAI 机制成功解决了“同质化奖励”问题，使模型能够生成更多样化的推理轨迹。
*   **效率平衡**：BGAS 机制能有效平衡搜索的深度与简洁性，避免了无意义的工具调用。
*   **泛化性**：即使在非搜索任务（如数学推理 MathVerse）中，SPAI 也能显著提升模型性能（+9.5%）。

### 7. 优点
*   **创新的奖励建模**：SPAI 引入空间结构信息来辅助优势函数计算，是解决 RL 中探索退化问题的一个新颖视角。
*   **动态交互校准**：BGAS 避免了单一的奖励偏好，使智能体在“知错”时能主动寻求更多信息。
*   **数据质量高**：BridgeVQA 填补了现有数据集搜索深度不足的空白。
*   **成本友好**：强调了本地工具服务在训练中的可行性，降低了复现门槛。

### 8. 不足与局限
*   **推理延迟**：引入精炼智能体（Refining Agent）虽然节省了上下文，但可能在推理链条中增加额外的计算延迟。
*   **算力细节缺失**：未提供具体的硬件资源消耗和训练时长，不便于其他研究者评估训练成本。
*   **数据集规模**：BridgeVQA 虽然质量高，但 3602 条数据量相对较小，对于更大规模模型的提升效果尚待验证。
*   **依赖裁判模型**：评估高度依赖于 Qwen2.5-72B 作为裁判，可能存在模型偏见。

（完）
