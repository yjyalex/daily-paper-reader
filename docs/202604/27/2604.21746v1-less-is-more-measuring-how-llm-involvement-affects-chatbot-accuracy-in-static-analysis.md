---
title: "Less Is More: Measuring How LLM Involvement affects Chatbot Accuracy in Static Analysis"
title_zh: 少即是多：衡量大语言模型参与度如何影响静态分析聊天机器人的准确性
authors: Krishna Narasimhan
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21746v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 架构约束的 JSON 表示对大模型准确率和参与度的影响
tldr: 本研究探讨了大型语言模型（LLM）在静态分析工具中的应用，通过对比三种不同程度的LLM参与架构（直接生成、结构化中间表示、工具增强代理），评估其将自然语言转化为Joern查询语言的准确性。实验发现，使用模式约束的JSON中间表示（IR）效果最佳，在大型模型上显著优于直接生成，且比消耗更多Token的代理方法更高效。研究表明，在形式化结构领域，限制LLM输出并结合确定性代码构建查询是更优选择。
selection_source: fresh_fetch
motivation: 旨在量化LLM在静态分析任务中的参与程度如何影响自然语言到查询语言转换的准确性。
method: 对比了直接生成查询、生成受模式约束的JSON中间表示以及工具增强代理三种架构在不同规模模型上的表现。
result: "结构化中间表示架构在大型模型上比直接生成准确率高出15-25%，且优于消耗Token量大得多的代理架构。"
conclusion: 在形式化领域，将LLM输出限制为强类型中间表示并由确定性代码构建查询，比无约束生成或迭代工具调用更有效。
---

## 摘要
大语言模型越来越多地被用于通过自然语言使静态分析工具更易于访问，然而现有系统在向 LLM 委派任务的程度上存在差异，且未将委派程度视为一个自变量。我们在将自然语言翻译为 Joern 查询语言 CPGQL 的过程中，对比了三种不同 LLM 参与度的架构：直接查询生成（方法 1）、生成受模式约束的 JSON 中间表示（方法 2）以及工具增强的智能体生成（方法 3）。这些架构在包含三个复杂度层级的 20 个代码分析任务基准上进行了评估，使用了四种开源权重模型，采用 2×2 设计（两个模型家族 × 两个规模），每个实验重复三次。结构化中间表示（方法 2）实现了最高的结果匹配率，在大模型上比直接生成高出 15-25 个百分点，并且优于智能体方法，尽管后者消耗的 token 数量是其 8 倍。结构化中间表示的优势在大模型上最为显著；对于小模型，模式合规性成为了瓶颈。这些发现表明，在形式化结构领域，将 LLM 的输出约束为良好类型的中间表示，并将查询构建委派给确定性代码，比无约束生成或迭代式工具使用能产生更好的结果。

## Abstract
Large language models are increasingly used to make static analysis tools accessible through natural language, yet existing systems differ in how much they delegate to the LLM without treating the degree of delegation as an independent variable. We compare three architectures along a spectrum of LLM involvement for translating natural language to Joern's query language \cpgql{}: direct query generation (\approach{1}), generation of a schema-constrained JSON intermediate representation (\approach{2}), and tool-augmented agentic generation (\approach{3}). These are evaluated on a benchmark of 20 code analysis tasks across three complexity tiers, using four open-weight models in a 2\(\times\)2 design (two model families \(\times\) two scales), each with three repetitions. The structured intermediate representation (\approach{2}) achieves the highest result match rates, outperforming direct generation by 15--25 percentage points on large models and surpassing the agentic approach despite the latter consuming 8\(\times\) more tokens. The benefit of structured intermediates is most pronounced for large models; for small models, schema compliance becomes the bottleneck. These findings suggest that in formally structured domains, constraining the LLM's output to a well-typed intermediate representation and delegating query construction to deterministic code yields better results than either unconstrained generation or iterative tool use.

---

## 论文详细总结（自动生成）

这篇论文探讨了在构建静态分析工具（如 Joern）的自然语言接口时，大语言模型（LLM）的最佳参与程度。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：静态分析工具（如 Joern）虽然强大，但其专有的查询语言（如 CPGQL）学习曲线陡峭，限制了开发者的使用。LLM 虽然能翻译自然语言，但在处理逻辑严密的领域特定语言（DSL）时容易产生幻觉或语法错误。
*   **核心问题**：在“自然语言 -> 静态分析查询”的转化过程中，应该给 LLM 多少自主权？是让它直接写代码，还是将其限制在特定的结构化中间层？

### 2. 方法论
论文对比了三种不同 LLM 参与度的架构：
*   **A1：直接生成 (Direct Generation)**：LLM 直接根据自然语言请求生成 CPGQL 查询语句。系统提供语法参考和示例（RAG），并允许在执行失败时进行 3 次重试。
*   **A2：结构化中间表示 (Structured Intermediate)**：LLM 不直接接触查询语法，而是生成符合预定义 JSON Schema 的对象（包含查询类型、范围、过滤器等）。随后由一个**确定性的映射器（Deterministic Mapper）**将 JSON 转换为正确的 CPGQL 语句。
*   **A3：工具增强代理 (Tool-Augmented Agentic)**：采用 ReAct 模式。LLM 作为一个智能体，通过调用一系列预定义的工具（如 `find_methods`, `trace_data_flow`）来逐步探索代码图谱，最后汇总结果给出答案。

### 3. 实验设计
*   **数据集与基准 (Benchmark)**：
    *   构建了一个包含 **20 个任务** 的基准测试，分为三个难度等级：结构化查询（7个）、数据流查询（7个）、复合查询（6个）。
    *   目标项目：Apache Commons Lang（工具库）和 OWASP WebGoat（易受攻击的 Web 应用）。
    *   验证方式：所有生成的查询都在真实的 Joern 4.0 环境中运行，并将执行结果与人工编写的基准答案进行对比（Result Match）。
*   **对比方法**：
    *   **模型变量**：2 个家族（Llama 3, Qwen 2.5）× 2 个规模（7-8B, 70-72B）。
    *   **架构变量**：A1, A2, A3。

### 4. 资源与算力
*   **算力使用**：论文未提及具体的 GPU 训练算力，因为实验主要基于推理。
*   **推理环境**：使用了 **HuggingFace Inference API** 访问开源权重模型。
*   **参数设置**：Temperature 设为 0，使用固定随机种子以保证可重复性。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 **720 次试验**（20 任务 × 3 次重复 × 4 模型 × 3 架构）。由于 Llama 70B 在 A3 模式下出现 API 频率限制和解析错误，最终有效数据为 660 组。
*   **充分性**：实验设计涵盖了模型规模（大 vs 小）和任务复杂度（简单 vs 复合）两个维度，并引入了重复实验以消除随机性，实验结果具有较强的统计说服力和客观性。

### 6. 主要结论与发现
*   **“少即是多”**：**A2（结构化中间表示）表现最好**。在大模型上，A2 的准确率比 A1 高出 15-25 个百分点。
*   **代理架构（A3）效率最低**：A3 的准确率最低（仅 15-25%），且消耗的 Token 数量平均是 A2 的 **8 倍**，耗时更长且容易在多步推理中累积错误。
*   **模型规模的影响**：大模型能完美遵循 JSON Schema，从而发挥 A2 的架构优势；而小模型（7-8B）在 A2 模式下容易出现 Schema 合规性失败，成为性能瓶颈。
*   **覆盖范围**：A3 能解决的任务是 A2 的真子集，即代理模式没有提供任何额外的能力补偿。

### 7. 优点
*   **架构视角独特**：将 LLM 的“参与度”作为独立变量进行量化研究，为工业界设计 LLM 插件提供了明确的架构指导。
*   **端到端验证**：不只是对比字符串相似度（Exact Match），而是通过**执行结果匹配**来评估，这更符合实际应用场景。
*   **开源贡献**：提供了首个针对 CPGQL 的自然语言基准测试。

### 8. 不足与局限
*   **基准规模较小**：仅 20 个任务，虽然覆盖了核心场景，但可能无法涵盖静态分析的所有边缘情况。
*   **映射器限制**：A2 的性能高度依赖于手写的确定性映射器，如果任务超出了 JSON Schema 的表达范围，该方法将失效。
*   **小模型瓶颈**：研究显示小模型在结构化输出上表现不佳，论文未深入探讨如何通过微调或受限解码（Constrained Decoding）来优化小模型在 A2 架构下的表现。

（完）
