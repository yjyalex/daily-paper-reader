---
title: "Hierarchical Chain-of-Thought Prompting: Enhancing LLM Reasoning Performance and Efficiency"
title_zh: 分层思维链提示：提升大语言模型的推理性能与效率
authors: "Xingshuai Huang, Derek Li, Bahareh Nikpour, Parsa Omidi"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00130v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对复杂多步推理的结构化推理范式
tldr: 针对大语言模型在复杂推理中存在的冗余和逻辑不连贯问题，本文提出层级思维链（Hi-CoT）提示法。该方法通过将推理过程分解为指令规划与分步执行的层级结构，显著提升了模型处理长程推理的能力。实验表明，Hi-CoT在提高数学推理准确率的同时，有效缩短了推理路径长度，实现了性能与效率的双重提升。
selection_source: fresh_fetch
motivation: 传统的思维链推理往往依赖非结构化的扁平链条，容易导致冗余和逻辑连贯性不足。
method: 提出Hi-CoT提示法，通过交替进行指令规划和分步执行，将推理过程分解为层级化的子步骤。
result: "在多个模型和基准测试中，Hi-CoT平均提升了6.2%的准确率，并减少了13.9%的推理轨迹长度。"
conclusion: 结构化的层级推理范式能有效增强大语言模型的推理性能与效率，且严格遵循该结构是最大化收益的关键。
---

## 摘要
思维链（CoT）提示显著提升了大语言模型（LLMs）的推理能力。然而，传统的 CoT 通常依赖于非结构化的扁平推理链，存在冗余且性能并非最优。在这项工作中，我们引入了分层思维链（Hi-CoT）提示，这是一种专门为应对复杂多步推理挑战而设计的结构化推理范式。Hi-CoT 通过在指令规划和逐步执行之间交替，将推理过程分解为分层的子步骤。这种分解使大语言模型能够更好地管理长程推理并保持逻辑连贯性。在多种大语言模型和数学推理基准测试上的广泛评估表明，与 CoT 提示相比，Hi-CoT 将平均准确率持续提高了 6.2%（在某些模型和任务上最高可达 61.4%），同时将推理轨迹长度缩短了 13.9%。我们进一步表明，当模型严格遵循分层结构时，准确率和效率将达到最大化。我们的代码可在 https://github.com/XingshuaiHuang/Hi-CoT 获取。

## Abstract
Chain-of-Thought (CoT) prompting has significantly improved the reasoning capabilities of large language models (LLMs). However, conventional CoT often relies on unstructured, flat reasoning chains that suffer from redundancy and suboptimal performance. In this work, we introduce Hierarchical Chain-of-Thought (Hi-CoT) prompting, a structured reasoning paradigm specifically designed to address the challenges of complex, multi-step reasoning. Hi-CoT decomposes the reasoning process into hierarchical substeps by alternating between instructional planning and step-by-step execution. This decomposition enables LLMs to better manage long reasoning horizons and maintain logical coherence. Extensive evaluations across diverse LLMs and mathematical reasoning benchmarks show that Hi-CoT consistently improves average accuracy by 6.2% (up to 61.4% on certain models and tasks) while reducing reasoning trace length by 13.9% compared to CoT prompting. We further show that accuracy and efficiency are maximized when models strictly adhere to the hierarchical structure. Our code is available at https://github.com/XingshuaiHuang/Hi-CoT.

---

## 论文详细总结（自动生成）

### 论文结构化总结：分层思维链提示（Hi-CoT）

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的思维链（CoT）提示虽然提升了 LLM 的推理能力，但其生成的推理轨迹通常是**线性且非结构化**的。这导致了两个主要问题：
    *   **冗余与低效**：模型容易产生重复解释或无关内容，增加了推理成本（Token 消耗）。
    *   **逻辑偏离（Plan-Execution Drift）**：在处理复杂多步问题时，模型容易偏离预设逻辑或遗漏步骤。
*   **研究背景**：现有的改进方法如“先计划再解决”（Plan-and-Solve）虽然引入了结构，但其计划是静态的，无法在执行过程中动态调整。因此，需要一种既能保持逻辑连贯性，又能通过结构化约束提升效率的推理范式。

#### 2. 方法论：核心思想与流程
*   **核心思想**：借鉴分层强化学习（Hierarchical RL）的理念，将推理过程转化为**“指令规划（Instruction）”与“步骤执行（Execution）”交替进行**的循环结构。
*   **关键技术细节**：
    *   **交替结构**：推理轨迹被分解为 $S_1, S_2, \dots, S_n$ 阶段。每个阶段包含一个 `<|instruction|>` 块（定义当前子目标）和一个 `<|execution|>` 块（执行具体计算或逻辑推理）。
    *   **动态反馈**：每一个指令步骤 $I_i$ 都是基于前一个执行步骤 $E_{i-1}$ 的结果生成的，实现了自适应的计划调整。
    *   **压缩瓶颈（Compression Bottleneck）**：要求模型在执行前先写出简洁指令，这迫使模型压缩当前的推理状态，过滤掉低信息量的冗余内容。
*   **算法流程**：输入问题 $\rightarrow$ 生成指令 1 $\rightarrow$ 执行步骤 1 $\rightarrow$ 生成指令 2 $\rightarrow$ 执行步骤 2 $\dots \rightarrow$ 最终答案。

#### 3. 实验设计
*   **数据集/基准测试**：选取了 5 个具有挑战性的数学推理基准，涵盖不同难度等级：**AIME24、AMC、MATH500、Minerva、OlympiadBench**。
*   **对比方法**：
    *   **Standard Prompting**：直接输出答案。
    *   **CoT Prompting**：标准的“Step-by-step”推理。
    *   **Plan-and-Solve (PS)**：先生成全局计划再执行。
    *   **Hi-CoT (format-relaxed)**：消融实验版本，不强制要求严格的层级格式。
*   **评估模型**：共 13 种模型配置，包括 **Qwen3 系列**（0.6B 至 32B）和 **DeepSeek-R1 蒸馏系列**（1.5B 至 32B）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **原因分析**：由于 Hi-CoT 是一种**推理时（Inference-time）的提示策略**，不涉及模型参数的更新或重新训练，因此实验重点在于推理生成的 Token 消耗和准确率，而非训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：在 13 个模型和 5 个数据集上进行了交叉验证，实验组数超过 60 组。
*   **充分性分析**：
    *   **多维度评估**：不仅对比了准确率（Pass@1），还详细统计了平均 Token 长度，验证了效率提升。
    *   **消融实验**：通过“格式放松版”验证了严格结构约束的必要性。
    *   **格式遵循度分析**：专门分析了严格遵循格式的子集表现，揭示了模型潜在的推理上限。
*   **客观性**：实验覆盖了不同参数规模（从轻量级 0.6B 到中量级 32B）和不同模型家族，结果具有较强的说服力和普适性。

#### 6. 主要结论与发现
*   **性能双增**：Hi-CoT 在提升准确率（平均提升 **6.2%**，最高达 61.4%）的同时，显著降低了推理成本（Token 长度平均减少 **13.9%**）。
*   **结构即瓶颈**：实验发现，当模型**严格遵循**层级格式时，在 AMC 和 MATH500 上的准确率甚至达到了 **100%**，且推理轨迹缩短了 **75%**。这表明 LLM 的原始推理能力被非结构化的提示词限制了。
*   **小模型受益显著**：对于推理能力较弱的小参数模型，Hi-CoT 起到了“脚手架”的作用，帮助其理清逻辑。

#### 7. 优点
*   **高效简洁**：与 Tree-of-Thought 等需要多次采样或搜索的方法不同，Hi-CoT 在单次推理中完成，不增加额外的推理次数。
*   **零样本且无需微调**：作为一种纯 Prompting 方法，易于部署到任何现有的 LLM 上。
*   **可解释性强**：层级化的指令与执行记录提供了清晰的审计追踪，便于分析模型在哪个环节出错。

#### 8. 不足与局限
*   **格式依赖性**：该方法高度依赖模型的指令遵循能力。如果模型（尤其是极小规模模型）无法维持格式，性能会大幅下降。
*   **任务局限性**：目前主要在数学推理任务上验证，对于创意写作、开放式对话等非结构化任务的适用性尚待研究。
*   **模型规模上限**：实验最高仅覆盖到 32B 模型，尚未在 70B 或更大型（如 GPT-4 级别）的模型上验证其边际收益。

（完）
