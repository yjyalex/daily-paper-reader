---
title: Learning to Generate Formally Verifiable Step-by-Step Logic Reasoning via Structured Formal Intermediaries
title_zh: 通过结构化形式中间体学习生成可形式化验证的分步逻辑推理
authors: "Luoxin Chen, Yichi Zhou, Huishuai Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29500v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对多步逻辑推理的过程奖励方法
tldr: 针对大语言模型在复杂推理中虽能得出正确答案但中间步骤往往不可靠的问题，本文提出了PRoSFI方法。该方法通过让模型生成与自然语言对齐的结构化形式化中间步骤，并利用形式化证明器对每一步进行验证，从而为模型提供过程奖励。这种方法在不牺牲准确性的前提下，引导模型生成机器可检查的证明，显著增强了推理过程的可靠性与可信度。
selection_source: fresh_fetch
motivation: 现有的结果奖励机制往往忽略了推理过程中的逻辑错误，导致模型生成的中间步骤不可靠且难以验证。
method: 提出PRoSFI方法，通过生成与自然语言对齐的结构化形式化中间步骤，并利用形式化证明器进行逐步验证来提供过程奖励。
result: 该方法成功引导模型生成了可被机器验证的推理链条，在保持准确率的同时显著提升了推理步骤的严谨性。
conclusion: PRoSFI为训练具备形式化验证能力和高可靠逻辑推理能力的语言模型提供了一种简单且有效的方案。
---

## 摘要
大语言模型（LLMs）最近在复杂的多步推理任务中表现出了令人印象深刻的性能，特别是当使用结果奖励强化学习（outcome-rewarded reinforcement learning）进行后训练时（Guo et al. 2025）。然而，据观察，结果奖励往往会忽略有缺陷的中间步骤，导致即使最终答案正确，推理步骤也并不可靠。为了解决这种不可靠的推理问题，我们提出了 PRoSFI（基于结构化形式中间体的过程奖励），这是一种新型奖励方法，在不牺牲准确性的情况下增强了推理的可靠性。该模型并不直接生成形式化证明（这对于中等规模（7B）的模型来说很难实现），而是输出与其自然语言推理相一致的结构化中间步骤。然后，每个步骤都由形式化证明器（formal prover）进行验证。只有经过完全验证的推理链才会获得高奖励。形式化验证的集成引导模型生成可由机器检查的分步证明，从而产生更可靠的最终答案。PRoSFI 为训练可信的推理模型提供了一种简单且有效的方法。

## Abstract
Large language models (LLMs) have recently demonstrated impressive performance on complex, multi-step reasoning tasks, especially when post-trained with outcome-rewarded reinforcement learning Guo et al. 2025. However, it has been observed that outcome rewards often overlook flawed intermediate steps, leading to unreliable reasoning steps even when final answers are correct. To address this unreliable reasoning, we propose PRoSFI (Process Reward over Structured Formal Intermediates), a novel reward method that enhances reasoning reliability without compromising accuracy. Instead of generating formal proofs directly, which is rarely accomplishable for a modest-sized (7B) model, the model outputs structured intermediate steps aligned with its natural language reasoning. Each step is then verified by a formal prover. Only fully validated reasoning chains receive high rewards. The integration of formal verification guides the model towards generating step-by-step machine-checkable proofs, thereby yielding more credible final answers. PRoSFI offers a simple and effective approach to training trustworthy reasoning models.

---

## 论文详细总结（自动生成）

### 论文总结：通过结构化形式中间体学习生成可形式化验证的分步逻辑推理（PRoSFI）

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）在复杂推理任务中主要依赖**结果奖励（Outcome Reward）**进行强化学习（如 DeepSeek-R1-Zero）。这种机制虽然提升了最终答案的准确率，但存在严重的“过程不可靠”问题：模型可能通过错误的逻辑推导得出正确的答案。
*   **研究动机**：为了实现可信推理，需要过程级监督。然而，人工标注过程奖励成本极高，而直接让中等规模模型（如 7B）生成完整的形式化证明（如 Lean 4 代码）难度太大，且难以与自然语言推理对齐。
*   **核心目标**：提出一种轻量级、可验证的方法，引导模型生成逻辑严密且机器可检查的推理步骤。

#### 2. 方法论：PRoSFI 框架
论文提出了 **PRoSFI (Process Reward over Structured Formal Intermediates)**，其核心思想是在自然语言推理中嵌入“结构化形式中间体”。
*   **核心思想**：模型不直接生成复杂的证明脚本，而是输出一系列推理块。每个块包含自然语言解释和一个结构化的 JSON/YAML 对象。
*   **关键技术细节**：
    *   **中间体结构**：包含 `id`（步骤标识）、`dependencies`（依赖的已知条件或前序步骤）、`conclusion`（形式化语言表达的结论）、`rule`（应用的逻辑规则，如肯定前件）。
    *   **形式化验证**：将每个结构化步骤转化为原子化的子问题，提交给形式化证明器（如 Prover9, Z3 或 Lean 4 内部算子）进行验证。
    *   **奖励函数设计**：
        *   **1.0**：答案正确且所有中间步骤均通过形式化验证。
        *   **0.3**：答案正确，但部分步骤未通过验证。
        *   **0.1**：格式正确但答案错误。
        *   **0.0**：格式错误或其他失败情况。
*   **算法流程**：采用 **GRPO**（群组相对策略优化）算法，在 Rollout 阶段生成完整链条，随后异步调用证明器获取奖励信号，实现生成与验证的解耦。

#### 3. 实验设计
*   **数据集**：
    *   **ProverQA**：合成的一阶逻辑（FOL）推理基准，包含 Easy、Medium、Hard 等级，并自建了 **ProverQA-Extra**（分布外 OOD 测试集）。
    *   **Knights and Knaves (K&K)**：经典的逻辑谜题数据集，用于验证泛化能力。
*   **Benchmark 与对比方法**：
    *   **Outcome-CoT**：仅基于最终答案正确性提供奖励（类似 R1-Zero）。
    *   **Direct Lean Generation**：要求模型直接生成完整的 Lean 4 证明脚本。
*   **评估指标**：答案准确率（Answer Correct Rate）、奖励命中率（Reward Hit Rate）、推理严谨性（Soundness，由 GPT-OSS-120B 作为裁判评估）。

#### 4. 资源与算力
*   **硬件环境**：使用了 1 台配备 **8 × Hopper GPU**（推测为 H100 或 H800）的计算节点。
*   **软件框架**：基于 vLLM 0.8.3 和 Verl 0.4.0 强化学习框架。
*   **训练时长**：
    *   ProverQA 任务：960 次迭代，耗时约 **14 小时**。
    *   K&K 任务：1440 次迭代。
*   **基础模型**：Qwen2.5-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验组数**：针对两个主要领域（FOL 和逻辑谜题）进行了多难度级别的测试，并包含了 OOD 泛化实验。
*   **充分性分析**：
    *   **对比充分**：对比了纯结果奖励和直接生成形式化代码的方法，证明了“中间体”设计的必要性。
    *   **客观性**：通过相关性分析（Heatmap）展示了 PRoSFI 奖励信号与推理严谨性（Soundness）的高度一致性。
    *   **测试时缩放（Test-time Scaling）**：展示了在采样多次（Sample@N）时，PRoSFI 配合验证器能显著提升性能，而传统 CoT 提升有限。

#### 6. 论文的主要结论与发现
*   **Soundness 显著提升**：在 ProverQA-Hard 上，PRoSFI 将推理严谨性从 Outcome-CoT 的 21.97% 提升至 **76.07%**。
*   **小模型潜力**：证明了 7B 规模的模型在合理的框架引导下，也能生成高质量、可验证的逻辑推理链，无需依赖超大规模模型或大规模数据蒸馏。
*   **奖励一致性**：PRoSFI 提供的奖励信号比单纯的答案正确性更能反映推理质量。

#### 7. 优点
*   **桥梁作用**：成功解决了自然语言推理的灵活性与形式化逻辑的严谨性之间的矛盾。
*   **低门槛**：为中等规模模型参与形式化验证推理提供了可行路径。
*   **训练效率**：通过解耦生成与验证，避免了强化学习过程中频繁调用证明器导致的 GPU 阻塞。
*   **可解释性**：生成的结构化中间体使模型的推理逻辑对人类和机器都高度透明。

#### 8. 不足与局限
*   **领域局限性**：目前主要集中在符号逻辑和一阶逻辑任务，尚未在更通用的数学证明（如 IMO 级别）或自然语言常识推理中验证。
*   **格式依赖**：该方法高度依赖模型遵循 JSON/YAML 格式的能力，虽然 Qwen2.5 表现良好，但在更弱的基础模型上可能失效。
*   **形式化表达的局限**：需要预先定义好任务对应的形式化语言（如 Lean4 语法片段），对于无法轻易形式化的模糊推理任务难以直接应用。

（完）
