---
title: "ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training"
title_zh: ShapE-GRPO：面向多候选大语言模型训练的 Shapley 增强型奖励分配
authors: "Rui Ai, Yu Pan, David Simchi-Levi, Chonghuan Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29871v1"
tags: ["query:token"]
score: 9.0
evidence: 多候选大模型训练中的Shapley增强奖励分配
tldr: 针对LLM在推荐、代码建议等场景中生成候选集合时，传统GRPO方法将集合奖励平均分配导致训练信号模糊的问题，本文提出ShapE-GRPO。该方法利用合作博弈论中的Shapley值，将集合级奖励分解为候选特定的细粒度信号，有效解决了低质量候选者“搭便车”的现象。实验证明，该方法在多个数据集上优于标准GRPO，且训练收敛更快。
selection_source: fresh_fetch
motivation: 现有强化学习方法在处理候选集合时，将相同的集合奖励分配给所有候选者，导致低质量候选者因高质量同伴而获得虚高奖励，产生训练噪声。
method: 提出ShapE-GRPO，利用Shapley值将集合级效用分解为候选特定的奖励信号，并确保计算复杂度在多项式时间内。
result: ShapE-GRPO在多个数据集上一致优于标准GRPO，并显著加快了训练过程中的收敛速度。
conclusion: 通过引入Shapley值进行细粒度奖励分配，可以有效提升多候选LLM训练的效率和性能。
---

## 摘要
在推荐、头脑风暴和代码建议等用户-代理交互场景中，大语言模型（LLMs）通常会生成一组候选推荐，其目标是最大化整个集合的集体效用，而非独立地最大化单个候选。然而，现有的强化学习后训练范式（如群体相对策略优化，GRPO）通常会为集合中的每个候选分配相同的集合级标量奖励。这导致了训练信号的噪声，表现为表现较差的候选者会“搭便车”，利用单个优秀同伴产生的高奖励，从而导致探索效率低下。为了解决这一问题，我们提出了 Shapley 增强型 GRPO（ShapE-GRPO）。通过利用集合级效用的置换不变性，我们从合作博弈论中推导出一种 Shapley 增强公式，将集合级奖励分解为细粒度的、针对特定候选的信号。我们证明了该公式在保持多项式时间复杂度的计算效率的同时，保留了 Shapley 值的基本公理。实验结果表明，ShapE-GRPO 在多个数据集上始终优于标准 GRPO，并在训练过程中实现了更快的收敛。

## Abstract
In user-agent interaction scenarios such as recommendation, brainstorming, and code suggestion, Large Language Models (LLMs) often generate sets of candidate recommendations where the objective is to maximize the collective utility of the entire set rather than individual candidates independently. However, existing reinforcement learning post-training paradigms, such as Group Relative Policy Optimization (GRPO), typically assign the same set-level scalar reward to every candidate in the set. This leads to noisy training signals where poor candidates free-ride on the high reward produced by a single strong peer, resulting in suboptimal exploration. To address this, we propose Shapley-Enhanced GRPO (ShapE-GRPO). By leveraging the permutation-invariant nature of set-level utility, we derive a Shapley-enhanced formulation from cooperative game theory to decompose set-level rewards into granular, candidate-specific signals. We show that our formulation preserves the fundamental axioms of the Shapley value while remaining computationally efficient with polynomial-time complexity. Empirically, ShapE-GRPO consistently outperforms standard GRPO across diverse datasets with accelerated convergence during training.

---

## 论文详细总结（自动生成）

以下是对论文《ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在推荐、代码建议、头脑风暴等场景中，LLM 通常生成一个**候选集合**（Set of Candidates）。现有的强化学习后训练方法（如 GRPO）通常将整个集合的奖励（Set-level Reward）平均分配给集合中的每一个候选者。
*   **研究动机**：这种“一刀切”的奖励分配方式会导致**“搭便车”（Free-riding）现象**——即一个平庸甚至错误的候选者，可能因为同集合中存在一个表现极佳的候选者而获得虚高奖励。这会产生严重的训练噪声，降低探索效率，导致模型难以区分真正贡献效用的候选者。

### 2. 方法论
*   **核心思想**：借鉴合作博弈论中的 **Shapley 值（Shapley Value）**，将集合级的总奖励公平地分解到每个候选者身上，根据其对集合效用的“边际贡献”分配奖励。
*   **关键技术细节**：
    *   **博弈论建模**：将集合中的 $K$ 个候选者视为博弈参与者。集合奖励通常定义为 $R(o) = \max(R(c_1), \dots, R(c_K))$。
    *   **Shapley 奖励分配**：计算每个候选者 $c_i$ 的 Shapley 值 $\phi_i$，衡量其在所有可能的候选子集中带来的平均增益。
    *   **Token 级映射**：对于推理部分（Reasoning），保留原始集合奖励；对于候选者部分，将其对应的 Token 奖励设为缩放后的 Shapley 值（$K \cdot \phi_i$）。
    *   **计算优化**：论文证明了在“LLM 作为推荐者”的特定设定下，Shapley 值不需要指数级计算，可以通过排序在 **$O(K^2)$**（通用奖励）或 **$O(K)$**（二元奖励）的时间复杂度内精确求得。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **文本摘要（ACLSum）**：生成 4 个摘要候选，使用 GPT-4o 作为评委进行连续评分（0-1）。
    2.  **代码建议（DS-1000）**：生成 4 个代码方案，通过单元测试获得二元奖励（0 或 1）。
    3.  **电影推荐（Netflix）**：基于用户历史评分，推荐 4 部电影，以用户真实评分作为奖励。
*   **对比方法（Baselines）**：
    *   **标准 GRPO**：所有候选者共享相同的集合奖励。
    *   **Winner-Takes-All (WTA)**：仅将奖励分配给得分最高的候选者，其余为零。
*   **评估模型**：Qwen 3-8B 和 Llama 3.1-8B-Instruct。

### 4. 资源与算力
*   **算力设备**：使用了 **2 台 NVIDIA GH200 GPU**。
*   **训练框架**：基于 `verl` 强化学习训练框架。
*   **超参数**：学习率 1e-6，KL 系数 0.01，训练 1 个 Epoch，Batch Size 为 2。论文未明确说明总训练时长，但提到 ShapE-GRPO 相比 GRPO 能在更少的步数内收敛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本、代码、推荐系统三个完全不同的领域，并使用了两种主流开源模型架构。
*   **充分性**：
    *   对比了标准 GRPO 和启发式的 WTA 方法，证明了 Shapley 分配的优越性。
    *   提供了训练收敛曲线，展示了 ShapE-GRPO 在稳定性（方差更小）和速度（收敛更快）上的优势。
    *   **局限性**：训练数据集规模相对较小（如 ACLSum 仅 150 个样本，Netflix 约 800 个），且候选者数量固定为 $K=4$，未深入探讨更大 $K$ 值下的表现。

### 6. 主要结论与发现
*   **性能提升**：ShapE-GRPO 在所有任务上均显著优于标准 GRPO。
*   **收敛加速**：在 Netflix 任务中，ShapE-GRPO 约在 160 步达到饱和，而 GRPO 需要 240 步且伴随剧烈波动。
*   **解决搭便车**：理论和实验均证明，错误候选者在 ShapE-GRPO 下获得的优势信号 $\le 0$，有效防止了虚假正向强化。
*   **模型泛化**：该方法在 Qwen 和 Llama 系列模型上表现一致，证明了其作为通用后训练插件的潜力。

### 7. 优点（亮点）
*   **理论严谨**：将博弈论中的公平分配公理（效率、对称性、加性、零参与者）引入 LLM 奖励建模。
*   **计算高效**：通过数学推导将原本昂贵的 Shapley 计算简化为多项式时间，几乎不增加训练开销。
*   **实用性强**：直接解决了多候选生成中长期存在的奖励模糊问题，特别适合推荐系统等实际应用。

### 8. 不足与局限
*   **依赖结构化提取**：方法要求模型输出能够被清晰地解析为 $K$ 个独立候选者，对 Prompt 工程有一定要求。
*   **奖励模型依赖**：仍需要一个能够对单个候选者进行评分的 Oracle（如单元测试或 Judge 模型），在完全无监督场景下应用受限。
*   **长上下文挑战**：在处理包含长历史记录的推荐任务时，Llama 等模型表现出对长上下文利用不足的问题，这属于模型架构限制而非算法本身，但影响了最终效果。

（完）
