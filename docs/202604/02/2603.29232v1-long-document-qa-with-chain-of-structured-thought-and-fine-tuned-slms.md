---
title: Long-Document QA with Chain-of-Structured-Thought and Fine-Tuned SLMs
title_zh: 基于结构化思维链与微调小语言模型的长文档问答
authors: "Zhuowen Liang, Xiaotian Lin, Zhengxuan Zhang, Yuyu Luo, Haixun Wang, Nan Tang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29232v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 用于问答中结构化输出的结构化思维链
tldr: LiteCoST框架旨在解决大模型在长文档问答中推理不可靠的问题。该框架包含结构化思维链（CoST）和SLM微调两个核心支柱。通过将强LLM生成的结构化推理过程蒸馏到3B/7B的小模型中，并结合GRPO强化学习优化，LiteCoST在保持高准确率的同时，显著降低了推理延迟，实现了优于或等同于GPT-4o的性能。
selection_source: fresh_fetch
motivation: 针对大语言模型在处理长且嘈杂的文档时推理脆弱、易出错的问题，寻求一种高效且可验证的结构化问答方案。
method: 提出LiteCoST框架，利用结构化思维链引导模型生成可审计的中间结构，并通过监督微调与GRPO强化学习对小模型进行蒸馏。
result: 3B和7B规模的小模型在多领域长文档问答任务中达到了与GPT-4o相当的水平，且推理速度比GPT-4o和DeepSeek-R1快2至4倍。
conclusion: 通过将结构化推理能力蒸馏至小模型，可以在保证长文档问答准确性的同时，大幅提升计算效率和系统的可验证性。
---

## 摘要
大语言模型（LLMs）被广泛应用于文档数据分析，但在长且多噪声的文档上直接进行推理仍然脆弱且易出错。因此，我们研究了文档问答（QA），旨在将分散的证据整合为结构化输出（如表格、图或块），以支持可靠且可验证的问答。我们提出了一个双支柱框架 LiteCoST，旨在利用小语言模型（SLMs）同时实现高准确率和低延迟。支柱 1：结构化思维链（CoST）。我们引入了 CoST 模板，这是一种感知模式的指令，引导强大的 LLM 生成逐步的 CoST 轨迹及相应的结构化输出。该过程归纳出最小结构、规范化实体/单位、对齐记录、序列化输出，并对其进行验证和细化，从而产生可审计的监督。支柱 2：SLM 微调。紧凑型模型在 LLM 生成的 CoST 数据上分两个阶段进行训练：用于结构对齐的有监督微调，以及结合了针对答案/格式质量和过程一致性的三重奖励的群体相对策略优化（GRPO）。通过将“结构优先”的行为蒸馏到 SLM 中，该方法使用 3B/7B 的 SLM 在多领域长文档问答上达到了与 LLM 相当的质量，同时延迟比 GPT-4o 和 DeepSeek-R1 (671B) 低 2-4 倍。代码可在 https://github.com/HKUSTDial/LiteCoST 获取。

## Abstract
Large language models (LLMs) are widely applied to data analytics over documents, yet direct reasoning over long, noisy documents remains brittle and error-prone. Hence, we study document question answering (QA) that consolidates dispersed evidence into a structured output (e.g., a table, graph, or chunks) to support reliable, verifiable QA. We propose a two-pillar framework, LiteCoST, to achieve both high accuracy and low latency with small language models (SLMs). Pillar 1: Chain-of-Structured-Thought (CoST). We introduce a CoST template, a schema-aware instruction that guides a strong LLM to produce both a step-wise CoST trace and the corresponding structured output. The process induces a minimal structure, normalizes entities/units, aligns records, serializes the output, and verifies/refines it, yielding auditable supervision. Pillar 2: SLM fine-tuning. The compact models are trained on LLM-generated CoST data in two stages: Supervised Fine-Tuning for structural alignment, followed by Group Relative Policy Optimization (GRPO) incorporating triple rewards for answer/format quality and process consistency. By distilling structure-first behavior into SLMs, this approach achieves LLM-comparable quality on multi-domain long-document QA using 3B/7B SLMs, while delivering 2-4x lower latency than GPT-4o and DeepSeek-R1 (671B). The code is available at https://github.com/HKUSTDial/LiteCoST.

---

## 论文详细总结（自动生成）

### 论文总结：基于结构化思维链与微调小语言模型的长文档问答（LiteCoST）

#### 1. 核心问题与整体含义（研究动机和背景）
在金融、法律等高风险领域，长文档问答（Long-Document QA）面临证据分散、噪声多、推理链条长等挑战。现有的大语言模型（LLM）直接进行端到端推理时，容易出现幻觉、遗漏证据或格式漂移，且推理过程不透明、难以审计。此外，调用顶级 LLM（如 GPT-4o）处理长文档存在高延迟、高成本和隐私泄露风险。
**核心目标**：开发一种名为 **LiteCoST** 的框架，通过“先结构化再回答”的策略，将强模型的结构化推理能力蒸馏到 3B/7B 规模的小语言模型（SLM）中，实现高准确率、可验证性与低延迟的平衡。

#### 2. 方法论：核心思想与技术细节
LiteCoST 采用双支柱架构：
*   **支柱 1：结构化思维链（CoST）生成**
    *   **结构分析**：根据问题动态选择最适合的结构（表格、图、块）并构建 Schema。
    *   **轨迹生成**：引导强 LLM 生成包含推理步骤（Trace）和序列化结构输出（SSO）的样本。
    *   **验证与迭代**：利用 LLM-as-Judge 验证 SSO 的准确性，并对低质量样本进行迭代优化，确保训练数据的“金标准”属性。
*   **支柱 2：SLM 适配与微调（SFT → GRPO）**
    *   **SFT（有监督微调）**：使 SLM 学习基础的结构化提取、格式规范和步骤遵从。
    *   **GRPO（群体相对策略优化）**：引入强化学习，通过**三重奖励机制**优化模型：
        1.  **格式奖励**：检查是否符合 `<reasoning>` 和 `<answer>` 标签及步骤标签。
        2.  **结果奖励**：结合规则（结构对齐）和语义相似度（GPT-4o-mini 评分）评估答案正确性。
        3.  **过程奖励**：引入一致性检查，对推理轨迹中的每一个步骤进行细粒度评分，确保推理路径的忠实度。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：基于 FinQA、TAT-QA、SQuAD 和 LegalBench 构造的领域特定数据集。
    *   **评估集**：主要使用 **Loong Benchmark**（涵盖金融、法律、论文三大领域，文档长度从 10k 到 250k 字符不等）。此外还使用了 **LongBench**（NarrativeQA, Qasper 等）验证通用性。
*   **对比方法**：
    *   **闭源/大型模型**：GPT-4o, GPT-4o-mini, DeepSeek-R1 (671B), Qwen2.5-14B。
    *   **开源基础模型**：LLaMA-3.2-3B, Qwen2-7B, LLaMA-3.1-8B。
    *   **专用 IE 模型**：ODIE, IEpile, Struc-bench, StructRAG。

#### 4. 资源与算力
*   **硬件环境**：使用了由 **8 张 NVIDIA RTX 4090 (24GB)** 组成的集群。
*   **训练细节**：
    *   **SFT 阶段**：3 个 Epoch，学习率 2e-4，使用 LoRA 适配器。
    *   **GRPO 阶段**：学习率 1e-5，Batch Size 为 16，每条查询采样 5 个生成结果。
*   **成本估算**：数据生成（GPT-4o）约 $30/领域，GRPO 训练评测约 $10，总训练成本约 **$40**。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验**：在 Loong 金融和法律子集上进行了详尽测试，对比了 AS（平均分）和 PR（完美率）。
*   **消融实验**：分别去掉了过程奖励和结果奖励，验证了双重信号对性能的贡献。
*   **鲁棒性测试**：针对不同文档长度（Set1-Set4）分析了性能衰减情况。
*   **通用性测试**：在 LongBench 的 4 个不同任务数据集上验证了框架的迁移能力。
实验设计客观、公平，基线模型涵盖了从 3B 到 671B 的广泛区间。

#### 6. 主要结论与发现
*   **性能飞跃**：经过 LiteCoST 微调的 **Qwen-7B 在金融领域表现超过了 GPT-4o**，LLaMA-3B 表现接近 GPT-4o-mini。
*   **效率优势**：SLM 的推理延迟比 GPT-4o 快 **2 倍**，比 DeepSeek-R1 快 **4 倍**。
*   **结构化的价值**：实验证明，将长文档转化为结构化数据后再推理，能显著提升所有 LLM（包括 GPT-4o）的准确率（平均提升 8-12 分）。
*   **强化学习作用**：GRPO 配合过程奖励能显著减少长路径推理中的逻辑断裂。

#### 7. 优点与亮点
*   **结构优先的范式**：将不可控的文本推理转化为可审计、可机器检查的结构化输出，极大地增强了系统可靠性。
*   **细粒度过程奖励**：在 GRPO 中引入步骤级的一致性奖励，解决了强化学习中奖励稀疏的问题。
*   **极高的性价比**：证明了通过高质量数据蒸馏，小模型可以在特定复杂任务上达到甚至超越超大规模模型。

#### 8. 不足与局限
*   **领域依赖性**：虽然在金融和法律领域表现优异，但在医疗、科学等其他垂直领域的泛化能力仍需更多数据验证。
*   **结构化损耗**：在某些极细粒度的定位任务（Spotlight Locating）中，结构化过程可能会因信息压缩而导致微小的精度损失。
*   **冷启动依赖**：框架初期仍需依赖强大的闭源模型（如 GPT-4o）来生成高质量的初始推理轨迹。

（完）
