---
title: "RefineRL: Advancing Competitive Programming with Self-Refinement Reinforcement Learning"
title_zh: RefineRL：通过自我修正强化学习推进竞赛编程
authors: "Shaopeng Fu, Xingxing Zhang, Li Dong, Di Wang, Furu Wei"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00790v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习用于竞赛编程中的复杂推理
tldr: 本研究针对竞赛编程（CP）任务，提出了RefineRL框架，旨在挖掘大语言模型的自我修正潜力。该方法引入了具备局部执行工具的Skeptical-Agent，通过对输出保持怀疑态度来强制执行严谨的迭代修正，并结合强化学习算法在标准可验证数据上训练模型的修正能力。实验表明，该方法显著提升了4B小模型的性能，使其在CP任务中超越了32B模型，展现了自我修正机制在提升模型推理规模方面的巨大潜力。
selection_source: fresh_fetch
motivation: 现有大模型在竞赛编程中多关注单次尝试，忽视了通过迭代自我修正来提升复杂推理能力的潜力。
method: 提出RefineRL框架，包含利用执行工具进行自我验证的Skeptical-Agent，以及一种仅需标准可验证数据即可训练模型自我修正能力的强化学习方案。
result: 经过训练的4B模型在集成Skeptical-Agent后，性能超越了32B模型，并接近235B模型的单次尝试水平。
conclusion: 自我修正机制是提升大模型推理能力的有效途径，为模型性能的规模化扩展提供了新的可能。
---

## 摘要
虽然大语言模型（LLMs）在竞赛编程（CP）等复杂推理任务上表现出了强大的性能，但现有方法主要集中在单次尝试设置上，忽略了它们进行迭代修正的能力。在本文中，我们提出了 RefineRL，这是一种旨在释放 LLMs 在解决 CP 问题时的自我修正能力的新方法。RefineRL 引入了两个关键创新：(1) Skeptical-Agent，一种迭代自我修正智能体，配备了本地执行工具，可根据 CP 问题的公开测试用例验证生成的解决方案。该智能体始终对其自身输出保持怀疑态度，从而即使在验证结果显示正确时也强制执行严格的自我修正。(2) 一种强化学习（RL）解决方案，仅使用标准 RLVR 数据（即配有可验证答案的问题）来激励 LLMs 进行自我修正。在 Qwen3-4B 和 Qwen3-4B-2507 上的广泛实验表明，我们的方法带来了显著的提升：经过我们的 RL 训练后，这些集成了 Skeptical-Agent 的紧凑型 4B 模型不仅优于大得多的 32B 模型，而且接近了 235B 模型的单次尝试性能。这些发现表明，自我修正对于扩展 LLM 推理具有巨大的前景，并具有进一步提升的巨大潜力。

## Abstract
While large language models (LLMs) have demonstrated strong performance on complex reasoning tasks such as competitive programming (CP), existing methods predominantly focus on single-attempt settings, overlooking their capacity for iterative refinement. In this paper, we present RefineRL, a novel approach designed to unleash the self-refinement capabilities of LLMs for CP problem solving. RefineRL introduces two key innovations: (1) Skeptical-Agent, an iterative self-refinement agent equipped with local execution tools to validate generated solutions against public test cases of CP problems. This agent always maintains a skeptical attitude towards its own outputs and thereby enforces rigorous self-refinement even when validation suggests correctness. (2) A reinforcement learning (RL) solution to incentivize LLMs to self-refine with only standard RLVR data (i.e., problems paired with their verifiable answers). Extensive experiments on Qwen3-4B and Qwen3-4B-2507 demonstrate that our method yields substantial gains: after our RL training, these compact 4B models integrated with the Skeptical-Agent not only outperform much larger 32B models but also approach the single-attempt performance of 235B models. These findings suggest that self-refinement holds considerable promise for scaling LLM reasoning, with significant potential for further advancement.

---

## 论文详细总结（自动生成）

以下是对论文《RefineRL: Advancing Competitive Programming with Self-Refinement Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在竞赛编程（CP）任务中的局限性提出方案。目前 LLM 在 CP 领域多采用“单次尝试”模式，即便有迭代修正（Self-Correction）方法，也存在两大痛点：
*   **假阳性风险（False Positives）：** 现有的修正方法盲目信任公开测试用例（Public Test Cases），导致代码虽通过了公开测试，但在隐藏的私有测试中因边界情况或效率问题而失败。
*   **缺乏显式训练：** 多数方法仅在推理阶段（Test-time）进行提示词工程，模型本身并未通过训练习得如何有效地进行自我调试和优化。

### 2. 方法论
RefineRL 框架由两个核心组件构成：
*   **Skeptical-Agent（怀疑论智能体）：**
    *   **核心思想：** 即使代码通过了所有公开测试用例，智能体仍保持“怀疑”态度，强制进行自我检查。
    *   **反馈机制：** 分为“错误驱动反馈”（针对未通过测试的情况，提供错误原因和输入输出示例）和“怀疑驱动反馈”（针对已通过测试的情况，提示模型检查是否存在硬编码作弊、逻辑漏洞或复杂度非最优）。
    *   **控制规则：** 引入“重启规则”（连续失败则重置，避免陷入局部最优）和“终止规则”（连续多次通过验证才停止）。
*   **Self-Refinement RL（自我修正强化学习）：**
    *   **数据合成：** 利用模型自身的修正轨迹（包含错误和怀疑两种路径）构建训练集，使模型内化调试能力。
    *   **平方激励奖励（Squared-incentive Reward）：** 公式定义为 $R = (k^*/|T|)^2$，其中 $k^*$ 是通过的测试用例数，$|T|$ 是总数。相比传统的 0/1 奖励，该函数能提供更密集的信号，并利用平方特性大幅激励模型完全解决问题。
    *   **算法：** 采用 GRPO 算法进行大规模强化学习训练。

### 3. 实验设计
*   **数据集：** 训练集基于 HardTest 的 4.5K 个问题合成；评估集采用 LiveCodeBench v5 和 v6（包含 2024-2025 年的最新题目）。
*   **基准模型：** Qwen3-4B 和 Qwen3-4B-2507。
*   **对比方法：** 
    *   **推理策略对比：** Pass@1、Random@16（并行采样）、LongCoT@16（选最长思维链）、RejSamp@16（拒绝采样）、Reflexion@16（经典的自我修正方法）。
    *   **模型规模对比：** 与 8B、32B 甚至 235B（Qwen3 系列）的大规模模型进行对比。

### 4. 资源与算力
*   **框架：** 使用 VeRL 分布式强化学习框架，结合 Ray 引擎进行大规模并行代码执行和奖励计算。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号（如 H100/A100）和数量，也未提及总训练时长。但提到在推理和训练中对代码执行设置了严格的超时（6-8s）和内存限制（512MB）。

### 5. 实验数量与充分性
*   **实验规模：** 进行了主性能对比、推理策略消融、训练数据来源消融、奖励函数消融以及思维链长度分析。
*   **充分性：** 实验设计较为严谨。通过对比 RejSamp@16 证明了提升并非来自随机采样的增加，而是来自修正逻辑；通过消融实验验证了“平方奖励”和“合成轨迹数据”的必要性。使用了最新的 LiveCodeBench 确保了评估的客观性和防污染性。

### 6. 主要结论与发现
*   **小模型逆袭：** 经过 RefineRL 训练的 4B 模型在集成 Skeptical-Agent 后，性能显著超过了 32B 模型，并逼近了 235B 模型在单次尝试下的表现。
*   **思维深度提升：** RL 训练并没有让模型简单地增加尝试次数，而是显著增加了单次尝试中的思维链（CoT）长度，表明模型学会了“深思熟虑”而非“盲目猜测”。
*   **奖励函数的重要性：** 在 CP 任务中，稀疏的二元奖励（0/1）难以让模型收敛，而密集的平方激励奖励是训练成功的关键。

### 7. 优点
*   **创新视角：** 首次在 CP 领域引入“怀疑论”机制，有效解决了公开测试用例覆盖不足导致的假阳性问题。
*   **高效率：** 仅需 4.5K 个原始问题即可训练出极强的修正能力，展示了极高的数据利用率。
*   **系统性强：** 结合了推理侧的 Agent 设计与训练侧的 RL 优化，形成闭环。

### 8. 不足与局限
*   **推理成本：** Skeptical@16 策略虽然效果好，但需要多次迭代和更长的 CoT，显著增加了推理时的 Token 消耗和时间延迟。
*   **工具依赖：** 方案高度依赖本地代码执行环境，对于无法通过简单执行验证的推理任务（如纯数学证明或非代码类逻辑）迁移性有待验证。
*   **模型范围：** 实验主要集中在 4B 规模，尚未展示该方法在更大规模模型（如 70B+）上的增益是否会边际递减。

（完）
