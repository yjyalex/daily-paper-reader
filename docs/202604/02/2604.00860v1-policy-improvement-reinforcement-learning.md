---
title: Policy Improvement Reinforcement Learning
title_zh: 策略改进强化学习
authors: "Huaiyang Wang, Xiaojie Li, Deqing Wang, Haoyi Zhou, Zixuan Huang, Yaodong Yang, Jianxin Li, Yikun Ban"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00860v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 具有可验证奖励的强化学习以提高推理能力
tldr: 针对大语言模型强化学习中“开环”优化导致的模型漂移和崩溃问题，本文提出了策略改进强化学习（PIRL）框架。该框架将优化目标从瞬时奖励最大化转变为跨迭代的累积策略改进。基于此，作者开发了PIPO算法，通过回顾性验证机制，对比历史基准来评估更新效果，实现闭环自我修正。在数学推理任务上的实验证明，PIPO相比GRPO及其变体具有更高的稳定性和性能。
selection_source: fresh_fetch
motivation: 现有的强化学习方法缺乏对模型更新效果的直接验证，这种开环设计容易导致优化过程出现漂移或崩溃。
method: 提出了PIRL框架及PIPO算法，通过引入滑动窗口历史基准进行回顾性验证，将优化过程转变为闭环的自我修正机制。
result: 在数学推理基准测试中，PIPO在稳定性和最终性能上均优于GRPO及其变体。
conclusion: 引入策略改进反馈的闭环优化机制是提升大语言模型推理能力和训练稳定性的关键。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为提升大语言模型推理能力的核心后训练范式。然而，现有方法存在一个共同的盲点：它们基于瞬时的组级或批次级统计数据来优化策略，而从未验证所产生的更新是否真正改进了模型。这种开环设计——在每一步孤立地进行更新，仅受组内（批次）奖励信号引导——意味着优化过程可能会发生漂移或崩溃，且缺乏检测和纠正这些失败的机制。我们认为，缺失的关键要素是策略改进反馈：即直接衡量和优化迭代间进展的能力。为此，我们引入了策略改进强化学习（PIRL），这是一个将代理奖励最大化替换为最大化跨迭代累积策略改进这一显式目标的框架，并证明了该时间目标与最大化最终任务性能完全一致。基于 PIRL，我们提出了策略改进策略优化（PIPO），它通过回溯验证实现了闭环优化。在每次迭代中，PIPO 都会根据滑动窗口历史基准评估之前的更新是否产生了真正的改进，随后积极强化有益的更新并抑制有害的更新，从而将开环过程转变为自校正过程。理论分析表明，PIPO 在期望上对 PIRL 目标执行上升操作；在数学推理基准上的实验证明，相比于 GRPO 及其变体，该方法具有更好的稳定性和性能。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a central post-training paradigm for improving the reasoning capabilities of large language models. Yet existing methods share a common blind spot: they optimize policies based on instantaneous group-level or batch-level statistics without ever verifying whether the resulting update actually improved the model. This open-loop design -- updating in isolation at each step, guided only by within-group (batch) reward signals -- means optimization can drift or collapse with no mechanism to detect and correct these failures. We argue that the missing ingredient is policy improvement feedback: the ability to measure and optimize inter-iteration progress directly. To this end, we introduce Policy Improvement Reinforcement Learning (PIRL), a framework that replaces surrogate reward maximization with the explicit objective of maximizing cumulative policy improvement across iterations, and prove this temporal objective is perfectly aligned with maximizing final task performance. Building on PIRL, we propose Policy Improvement Policy Optimization (PIPO), which implements closed-loop optimization through retrospective verification. At each iteration, PIPO evaluates whether the previous update yielded genuine improvement against a sliding-window historical baseline, then actively reinforces beneficial updates and suppresses the harmful ones -- transforming an open-loop process into a self-correcting one. We provide theoretical analysis showing that PIPO performs ascent on the PIRL objective in expectation, and experiments on mathematical reasoning benchmarks demonstrate improved stability and performance over GRPO and its variants.

---

## 论文详细总结（自动生成）

### 论文总结：策略改进强化学习 (PIRL)

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）在**具有可验证奖励的强化学习（RLVR）**（如数学推理、代码生成）中存在的“开环”优化问题进行了深入探讨。
*   **研究动机**：现有的主流方法（如 GRPO、PPO）主要依赖于瞬时的、批次内的奖励信号来更新策略。这种设计缺乏对“更新是否真正提升了模型性能”的验证。
*   **核心挑战**：在稀疏奖励场景下，组内归一化优势（Group-relative Advantage）会导致梯度在概率边界（$p \to 0$ 或 $1$）处发生扭曲甚至爆炸，引发训练不稳定、模型漂移或模式崩溃。
*   **整体含义**：论文提出将优化目标从“最大化当前奖励”转向“最大化跨迭代的累积策略改进”，通过引入时间维度的反馈，将强化学习过程从开环转变为闭环自校正系统。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PIRL (Policy Improvement Reinforcement Learning)** 框架及其实际算法 **PIPO (Policy Improvement Policy Optimization)**。

*   **核心思想**：利用历史性能作为基准，通过回顾性验证（Retrospective Verification）来调节策略更新。
*   **关键技术细节**：
    *   **滑动窗口历史基准**：计算过去 $K$ 个迭代的平均性能 $\mu_{his}$ 和标准差 $\sigma_{his}$，作为评估当前更新效果的参照物。
    *   **策略改进奖励 (PI Reward)**：定义了一个标准化的改进信号 $\xi_t = (\mu_t - \mu_{his}) / \sigma_{his}$。如果当前策略 $\theta_t$ 的表现优于历史基准（$\xi_t > 0$），则产生正向 PI 奖励；反之则产生负向或抑制信号。
    *   **双阶段优化流程**：
        1.  **阶段 1（前向探索）**：执行标准的组优化更新（如 GRPO），生成一个暂定的新策略。
        2.  **阶段 2（回顾性验证）**：在下一轮迭代中，利用新采样的数据评估上一轮更新的效果。若验证有效，则通过 PI Reward 进一步强化该更新方向；若发现性能退步，则进行抑制或修正。
*   **理论支撑**：论文证明了 PIPO 梯度在期望上与理想的 RLVR 梯度对齐，并能有效抵消 GRPO 固有的几何畸变（梯度爆炸）。

#### 3. 实验设计
*   **数据集与场景**：主要在 **MATH** 数据集上进行训练。
*   **Benchmark（评估基准）**：
    *   数学推理：MATH500、AIME 2025、AMC 2023、MINERVA。
    *   科学推理：SciKnowEval（涵盖化学、生物、物理、材料科学）。
*   **对比方法**：
    *   基础模型：Qwen3-4B-Base, Qwen3-8B-Base。
    *   主流 RL 算法：**GRPO**、**GSPO**、**DAPO**。
*   **实验设置**：将 PIPO 作为“即插即用”模块集成到上述三种算法中，观察性能增量。

#### 4. 资源与算力
*   **硬件环境**：使用了单节点 **8 × NVIDIA A100 (80GB) GPU**。
*   **软件框架**：基于 **VeRL** 混合流框架实现。
*   **训练细节**：模型规模包括 4B 和 8B；训练 Epoch 为 3（DAPO 为 9）；最大响应长度设为 4096 标记。
*   **计算开销**：PIPO 引入了回顾性验证，导致每步训练时长增加约 **12%–19%**，但由于采样效率更高，达到相同精度所需的总墙钟时间（Wall-clock time）反而更短。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同参数规模的模型（4B 和 8B）上进行了验证，涵盖了 5 个以上的核心 Benchmark。
*   **消融实验**：针对关键超参数进行了详细测试，包括：
    *   滑动窗口大小 $K$ 的影响（测试了 $K=4, 8, 12, 16$）。
    *   整流函数 $\phi(\cdot)$ 的裁剪范围（验证了单向抑制与双向修正的效果）。
*   **充分性评价**：实验设计较为全面，不仅验证了最终精度，还分析了训练过程中的梯度范数、响应长度变化以及计算效率。通过在科学推理数据集上的测试，证明了该方法具有良好的泛化性，而非仅仅过拟合数学题库。

#### 6. 主要结论与发现
*   **性能提升**：PIPO 在所有基准测试中均一致地提升了 GRPO、GSPO 和 DAPO 的表现。例如，在 AIME 2025 上，GSPO+PIPO 相比原版提升了 7.4% 的绝对精度。
*   **训练稳定性**：PIPO 有效消除了标准 GRPO 中常见的梯度尖峰和性能崩溃现象，使训练曲线更加平滑。
*   **推理深度**：观察到 PIPO 能够激励模型生成更长、更复杂的思维链（CoT），从而解决更难的推理题目。
*   **收敛速度**：尽管单步耗时略增，但 PIPO 具有更高的样本效率，能更快达到性能平台。

#### 7. 优点
*   **闭环机制**：首次在 RLVR 中引入跨迭代的性能验证，解决了开环优化导致的“盲目更新”问题。
*   **即插即用**：作为一种元算法（Meta-algorithm），它可以轻松集成到现有的各种策略梯度方法中。
*   **理论严密**：提供了详尽的数学证明，解释了为何该方法能纠正组优化的梯度偏差。
*   **稳定性强**：对于学习率和噪声具有更强的鲁棒性，有效防止了模式崩溃。

#### 8. 不足与局限
*   **计算开销**：需要缓存上一轮的 Logits 或进行额外的推理步骤来计算 PI Reward，增加了显存和计算时间的负担。
*   **依赖可验证性**：该方法目前仅适用于具有明确二元奖励（对/错）的任务，对于主观性强、缺乏自动验证器的任务（如创意写作）难以直接应用。
*   **超参数敏感性**：虽然消融实验给出了推荐值，但窗口大小 $K$ 和 PI 学习率在不同规模的模型或任务上可能仍需微调。

（完）
