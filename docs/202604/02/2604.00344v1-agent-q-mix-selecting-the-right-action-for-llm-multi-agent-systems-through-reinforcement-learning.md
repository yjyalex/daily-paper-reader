---
title: "Agent Q-Mix: Selecting the Right Action for LLM Multi-Agent Systems through Reinforcement Learning"
title_zh: Agent Q-Mix：通过强化学习为大语言模型多智能体系统选择正确动作
authors: "Eric Hanchen Jiang, Levina Li, Rui Sun, Xiao Liang, Yubei Li, Yuchen Wu, Haozheng Luo, Hengli Li, Zhi Zhang, Zhaolu Kang, Kai-Wei Chang, Ying Nian Wu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00344v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于大语言模型多智能体协调与通信的多智能体强化学习框架
tldr: 针对大语言模型多智能体系统在复杂任务中难以有效协作和拓扑选择的问题，本文提出 Agent Q-Mix 框架。该方法将拓扑选择建模为协作式多智能体强化学习问题，利用 QMIX 价值分解学习去中心化的通信决策。通过结合 GNN 编码器和 CTDE 范式，在平衡任务准确率与 Token 成本的同时，显著提升了多智能体系统的推理性能、效率及鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中如何有效选择和连接智能体以应对复杂任务的挑战。
method: 将拓扑选择重构为协作式多智能体强化学习问题，并采用 QMIX 价值分解和 CTDE 范式学习去中心化的通信决策。
result: 在七个核心基准测试中取得了最高平均准确率，并在 Token 效率和抗故障鲁棒性方面表现优于现有主流框架。
conclusion: 研究证明了通过学习实现的去中心化拓扑优化能有效提升多智能体推理系统的性能边界。
---

## 摘要
大语言模型（LLMs）在完成各种任务方面表现出了卓越的性能。然而，解决复杂问题通常需要多个智能体的协作，这提出了一个根本性问题：如何有效地选择并互连这些智能体。在本文中，我们提出了 Agent Q-Mix，这是一个强化学习框架，它将拓扑选择重新表述为一个协作式多智能体强化学习（MARL）问题。我们的方法利用 QMIX 值分解来学习去中心化的通信决策，其中每个智能体从一组通信动作中进行选择，这些动作共同诱导出一个逐轮的通信图。Agent Q-Mix 的核心是在中心化训练与去中心化执行（CTDE）范式下，结合了拓扑感知 GNN 编码器、GRU 记忆和每个智能体的 Q 头。该框架优化了一个平衡任务准确率与 token 成本的奖励函数。在涵盖编程、推理和数学的七个核心基准测试中，与现有方法相比，Agent Q-Mix 实现了最高的平均准确率，同时展示了卓越的 token 效率和针对智能体故障的鲁棒性。值得注意的是，在以 Gemini-3.1-Flash-Lite 为骨干模型的极具挑战性的 Humanity's Last Exam (HLE) 上，Agent Q-Mix 达到了 20.8% 的准确率，优于 Microsoft Agent Framework (19.2%) 和 LangGraph (19.2%)，随后是 AutoGen 和 OpenClaw 的 Lobster。这些结果强调了学习型去中心化拓扑优化在突破多智能体推理边界方面的有效性。

## Abstract
Large Language Models (LLMs) have shown remarkable performance in completing various tasks. However, solving complex problems often requires the coordination of multiple agents, raising a fundamental question: how to effectively select and interconnect these agents. In this paper, we propose \textbf{Agent Q-Mix}, a reinforcement learning framework that reformulates topology selection as a cooperative Multi-Agent Reinforcement Learning (MARL) problem. Our method learns decentralized communication decisions using QMIX value factorization, where each agent selects from a set of communication actions that jointly induce a round-wise communication graph. At its core, Agent Q-Mix combines a topology-aware GNN encoder, GRU memory, and per-agent Q-heads under a Centralized Training with Decentralized Execution (CTDE) paradigm. The framework optimizes a reward function that balances task accuracy with token cost. Across seven core benchmarks in coding, reasoning, and mathematics, Agent Q-Mix achieves the highest average accuracy compared to existing methods while demonstrating superior token efficiency and robustness against agent failure. Notably, on the challenging Humanity's Last Exam (HLE) using Gemini-3.1-Flash-Lite as a backbone, Agent Q-Mix achieves 20.8\% accuracy, outperforming Microsoft Agent Framework (19.2\%) and LangGraph (19.2\%), followed by AutoGen and Lobster by OpenClaw. These results underscore the effectiveness of learned, decentralized topology optimization in pushing the boundaries of multi-agent reasoning.

---

## 论文详细总结（自动生成）

### Agent Q-Mix 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）能力的提升，多智能体系统（MAS）成为解决复杂任务的关键。然而，**通信拓扑（即智能体之间如何交换信息）**的设计至关重要。现有的方法存在两大局限：
*   **静态拓扑：** 如链式或全连接结构，无法根据任务难度灵活调整，导致简单任务浪费 Token，复杂任务协作不足。
*   **中心化瓶颈：** 现有的自适应方法通常依靠一个中心化的生成器一次性产生整个图结构，缺乏执行时的灵活性，且难以将全局目标分解为单个智能体的决策。
**研究动机：** 探索如何让每个智能体在执行过程中自主、去中心化地决定其通信行为，以平衡任务准确率、Token 效率和系统鲁棒性。

#### 2. 方法论：核心思想与关键技术
Agent Q-Mix 将拓扑选择重构为一个**协作式多智能体强化学习（MARL）问题**，采用**中心化训练与去中心化执行（CTDE）**范式。
*   **核心思想：** 每个智能体根据当前观察独立选择一个离散的“通信动作”，所有智能体的动作共同诱导出该轮的通信图。
*   **关键技术细节：**
    *   **动作空间：** 定义了 6 种具有解释性的离散动作：`solo process`（独立工作）、`broadcast all`（广播）、`selective query`（定向查询）、`aggregate refine`（聚合信息）、`execute verify`（转发验证）、`debate check`（双向辩论）。
    *   **网络架构：** 采用 **GNN-GRU-MLP** 结构。GNN 负责提取当前拓扑特征，GRU 维护跨轮次的记忆，MLP 输出每个动作的 Q 值。
    *   **QMIX 价值分解：** 使用单调混合网络将各智能体的局部 Q 值整合为全局 $Q_{tot}$。这保证了**单调性约束（IGM 属性）**，即局部最优动作的组合即为全局最优，从而支持去中心化执行。
    *   **奖励函数：** 设计了平衡公式 $R = w_{acc} \cdot \text{accuracy} - w_{tok} \cdot \min(\frac{\text{tokens}}{\text{budget}}, 1)$，旨在提升准确率的同时惩罚过度通信。

#### 3. 实验设计
*   **数据集/场景：** 涵盖三大领域共 7 个核心基准测试：
    *   **编程：** LiveCodeBench (v6), HumanEval。
    *   **推理：** MMLU-Pro。
    *   **数学：** AIME 2025/2026, HMMT 2025, Beyond-AIME。
    *   **挑战性测试：** Humanity’s Last Exam (HLE)。
*   **骨干模型：** GPT-OSS:120B 和 Gemini-3.1-Flash-Lite。
*   **对比方法：**
    *   **静态方法：** LLM-Debate。
    *   **自适应拓扑方法：** GPTSwarm, AgentDropout, G-Designer, MaAS, GTD, TopoDIM。
    *   **商业/主流框架：** AutoGen, LangGraph, Microsoft Agent Framework, Lobster。

#### 4. 资源与算力
*   **训练时长：** 论文提到在每个领域仅需 15 个样本进行训练，训练过程在**单台 CPU** 上约需 **30-60 分钟**。
*   **算力说明：** 由于主要的计算开销在于 LLM API 的调用而非强化学习网络的更新（Q 网络参数量较小），因此对本地 GPU 算力要求极低。文中未提及具体使用的 GPU 型号或数量，重点在于 API 交互。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了跨 7 个基准测试的性能对比，并针对 HLE 进行了专门评估。
*   **消融实验：** 涵盖了智能体数量（1-10个）、训练样本量（5-100个）、奖励权重比例、通信轮次（1-4轮）的详细分析。
*   **鲁棒性测试：** 专门设计了“对抗智能体”实验，验证系统在某个智能体失效或故意报错时的容错能力。
*   **评价：** 实验设计非常充分且客观。通过中位数结果（3次运行）减少随机性，对比对象涵盖了从学术前沿到工业界最流行的框架，具有很强的说服力。

#### 6. 主要结论与发现
*   **性能领先：** Agent Q-Mix 在所有基准测试中均取得了最高平均准确率。在 HLE 测试中以 20.8% 的准确率超过了微软 Agent Framework 和 LangGraph。
*   **Token 效率：** 相比其他多智能体框架，Agent Q-Mix 的 Token 消耗降低了 4 到 24 倍，证明了自适应拓扑能有效减少冗余通信。
*   **鲁棒性强：** 在引入对抗智能体时，准确率仅下降 2.86%，远低于其他方法（通常下降 8-10%），表明模型学会了在执行时“隔离”不可靠的节点。
*   **数学任务增益最大：** 在需要多轮严密推理的数学任务（如 HMMT）上提升最为显著。

#### 7. 优点（亮点）
*   **去中心化决策：** 真正实现了智能体在运行时的局部自适应，避免了全局拓扑生成的僵化。
*   **可解释的动作空间：** 将复杂的图结构简化为 6 种直观的通信原语，易于理解和分析。
*   **极高的样本效率：** 仅需 15 个训练样本即可泛化到未见过的复杂任务。
*   **多目标优化：** 成功通过强化学习在准确率和成本之间找到了平衡点。

#### 8. 不足与局限
*   **角色依赖：** 实验中智能体的角色（如 CodeWriter, MathSolver）仍需预先定义，尚未实现角色分配的完全自动化。
*   **奖励权重敏感性：** 准确率与 Token 惩罚的权重（$w_{acc}, w_{tok}$）需要根据不同模型和领域进行手动微调。
*   **训练集规模：** 虽然展示了小样本学习能力，但在更大规模、更多样化的训练集上是否会有进一步提升尚待验证。
*   **实时性开销：** 每一轮通信都需要 Q 网络推理，虽然 Q 网络很轻量，但在极大规模智能体集群中可能存在协调延迟。

（完）
