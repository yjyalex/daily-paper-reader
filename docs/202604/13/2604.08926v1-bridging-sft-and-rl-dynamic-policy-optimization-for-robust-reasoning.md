---
title: "Bridging SFT and RL: Dynamic Policy Optimization for Robust Reasoning"
title_zh: 连接 SFT 与 RL：面向鲁棒推理的动态策略优化
authors: "Taojie Zhu, Dongyang Xu, Ding Zou, Sen Zhao, Qiaobo Hao, Zhiguo Yang, Yonghong He"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08926v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于大语言模型鲁棒推理的动态策略优化
tldr: 针对大语言模型后训练中SFT（低方差高偏差）与RL（低偏差高方差）之间的统计冲突，本文提出DYPO框架。该框架通过群体对齐损失降低RL方差，利用多教师蒸馏修正SFT偏差，并引入动态门控机制自适应平衡探索与利用。实验表明，DYPO在复杂推理和分布外任务上表现优异，显著提升了模型的鲁棒性与泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决SFT的拟合偏差与RL的梯度方差在统一优化过程中存在的统计冲突问题。
method: 提出包含群体对齐损失、多教师蒸馏和动态探索-利用门控机制的DYPO统一优化框架。
result: "在复杂推理基准上平均提升4.8%，在分布外任务上显著提升13.3%，优于传统顺序训练方法。"
conclusion: DYPO通过结构化缓解偏差-方差权衡，为桥接SFT与RL以实现鲁棒推理提供了有效的理论与实践方案。
---

## 摘要
大语言模型（LLM）的后训练范式（主要包括有监督微调 SFT 和强化学习 RL）面临着一个根本性的困境：SFT 提供了稳定性（低方差）但存在较高的拟合偏差，而 RL 能够进行探索（低偏差）但却面临着高梯度方差的问题。现有的统一优化策略通常采用简单的损失加权，忽略了这些不同梯度信号之间的统计冲突。在本文中，我们对这种偏差-方差权衡进行了严格的理论分析，并提出了 DYPO（动态策略优化），这是一个旨在从结构上缓解该冲突的统一框架。DYPO 集成了三个核心组件：(1) 群组对齐损失（GAL），利用内在的群组动态显著降低 RL 梯度方差；(2) 多教师蒸馏机制，通过多样化的推理路径纠正 SFT 的拟合偏差；(3) 动态利用-探索门控机制，根据奖励反馈在稳定的 SFT 和探索性的 RL 之间进行自适应仲裁。理论分析证实，DYPO 线性地减少了拟合偏差并最小化了整体方差。大量实验表明，DYPO 显著优于传统的顺序流水线，在复杂推理基准测试中平均提升了 4.8%，在分布外（OOD）任务中提升了 13.3%。我们的代码已在 https://github.com/Tocci-Zhu/DYPO 公开。

## Abstract
Post-training paradigms for Large Language Models (LLMs), primarily Supervised Fine-Tuning (SFT) and Reinforcement Learning (RL), face a fundamental dilemma: SFT provides stability (low variance) but suffers from high fitting bias, while RL enables exploration (low bias) but grapples with high gradient variance. Existing unified optimization strategies often employ naive loss weighting, overlooking the statistical conflict between these distinct gradient signals. In this paper, we provide a rigorous theoretical analysis of this bias-variance trade-off and propose \textbf{DYPO} (Dynamic Policy Optimization), a unified framework designed to structurally mitigate this conflict. DYPO integrates three core components: (1) a \textit{Group Alignment Loss (GAL)} that leverages intrinsic group dynamics to significantly reduce RL gradient variance; (2) a \textit{Multi-Teacher Distillation} mechanism that corrects SFT fitting bias via diverse reasoning paths; and (3) a \textit{Dynamic Exploitation-Exploration Gating} mechanism that adaptively arbitrates between stable SFT and exploratory RL based on reward feedback. Theoretical analysis confirms that DYPO linearly reduces fitting bias and minimizes overall variance. Extensive experiments demonstrate that DYPO significantly outperforms traditional sequential pipelines, achieving an average improvement of 4.8\% on complex reasoning benchmarks and 13.3\% on out-of-distribution tasks. Our code is publicly available at https://github.com/Tocci-Zhu/DYPO.

---

## 论文详细总结（自动生成）

这篇论文由清华大学深圳国际研究生院、中兴通讯（ZTE）以及重庆邮电大学的研究人员共同完成，旨在解决大语言模型（LLM）推理能力提升过程中，有监督微调（SFT）与强化学习（RL）结合时的统计冲突问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心困境**：LLM 后训练主要依赖 SFT 和 RL。SFT 具有**低方差但高偏差**的特点（稳定但容易陷入局部最优，泛化性差）；RL 具有**低偏差但高方差**的特点（探索性强但梯度极不稳定，容易训练崩溃）。
*   **现有缺陷**：目前的统一优化方法（如简单损失加权）忽略了两者梯度信号之间的统计冲突，且未考虑样本难度的差异，导致训练效率低下或性能受限。
*   **研究目标**：提出一种能够动态平衡偏差与方差、并根据样本难度自适应选择优化路径的统一框架。

### 2. 方法论：DYPO (Dynamic Policy Optimization)
DYPO 框架的核心思想是**“按需分配优化策略”**，包含以下三个关键技术：
*   **动态难度分级 (Dynamic Difficulty Grading)**：
    *   通过对每个 Prompt 进行多次采样（Rollout），根据结果将样本分为三类：
        *   **Easy (简单)**：模型全部做对，认为已饱和，直接丢弃以节省算力。
        *   **Hard (困难)**：模型全部做错，缺乏有效奖励信号，路由至 **SFT 路径**。
        *   **Mid (中等)**：模型结果有对有错，处于学习前沿，路由至 **RL 路径**。
*   **多教师蒸馏 (Multi-Teacher Distillation)**：
    *   针对 Hard 样本，不依赖单一教师模型，而是聚合多个教师（如 DeepSeek-R1, Qwen3）的推理路径。
    *   **理论依据**：通过平均多个教师的信号，可以线性抵消个体教师的特有偏差（Idiosyncratic Bias），引导模型走向更稳健的推理路径。
*   **群组对齐损失 (Group Alignment Loss, GAL)**：
    *   针对 Mid 样本，在标准 GRPO 损失基础上引入 GAL。
    *   **核心逻辑**：采用对比学习机制，显式地拉近正确路径、推开错误路径。GAL 的梯度权重是有界的，能显著降低 RL 的梯度方差，起到正则化作用。

### 3. 实验设计
*   **数据集**：使用 OpenR1-Math-220k 子集，Prompt 主要源自 NuminaMath 1.5。
*   **基准测试 (Benchmarks)**：
    *   **分布内 (ID)**：AIME 2024/2025、AMC、MATH-500、Minerva（侧重数学推理）。
    *   **分布外 (OOD)**：ARC-c、GPQA-Diamond（侧重科学与常识推理）。
*   **对比方法**：
    *   基础 SFT 和 RL (GRPO)。
    *   零样本 RL 方法：SimpleRL-Zero, OpenReasoner-Zero, PRIME-Zero, Oat-Zero。
    *   统一/顺序优化方法：SFT→RL, LUFFY, ReLIFT, SRFT, CHORD 等。

### 4. 资源与算力
*   **硬件环境**：使用了 2 个计算节点，共计 **16 张 NVIDIA A800 GPU (80GB)**。
*   **训练细节**：每个 Prompt 生成 8 条轨迹（Rollouts），最大响应长度 8192 词元，学习率固定为 $1 \times 10^{-6}$。
*   **框架**：基于 verl 框架构建，推理阶段使用 vLLM 以提高吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：在 Qwen2.5-Math-7B 和 Qwen3-4B-Base 两个不同架构的模型上进行了验证。
*   **消融实验**：针对不同教师模型强度（235B/32B/8B）、动态分级机制、GAL 损失等组件进行了详尽的消融分析。
*   **客观性**：通过多次随机种子的敏感性分析和显著性检验（p < 0.05），证明了结果的稳健性。实验覆盖了从基础数学到博士级科学问题的广泛难度。

### 6. 主要结论与发现
*   **性能领先**：DYPO 在复杂推理任务上平均超越现有最强基线 4.8%，在 OOD 任务上提升达 13.3%。
*   **解决冲突**：理论和实证均证明，DYPO 能显著平滑梯度范数（Gradient Norm），解决 RL 训练中的波动问题。
*   **自适应演进**：随着训练进行，模型自主减少对教师信号的依赖（SFT 比例下降），增加自主探索（RL 比例上升），形成了一种自动的课程学习。

### 7. 优点
*   **理论严谨**：对 SFT 偏差和 RL 方差的权衡给出了严格的数学推导和证明。
*   **动态路由**：避免了对所有样本“一刀切”的优化，提高了样本利用效率。
*   **泛化性强**：在 OOD 任务（如 GPQA）上的巨大提升表明模型学到的是逻辑而非模板记忆。

### 8. 不足与局限
*   **领域局限**：实验主要集中在逻辑严密的数学推理领域，在开放式生成（如创意写作、对话）上的效果尚待验证。
*   **计算开销**：为了实现动态分级，每个 Prompt 需要在线生成 8 条轨迹，这比纯离线 SFT 带来了更高的推理算力成本。
*   **教师依赖**：Hard 样本的优化仍高度依赖于外部高质量教师模型的输出。

（完）
