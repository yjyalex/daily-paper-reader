---
title: "StaRPO: Stability-Augmented Reinforcement Policy Optimization"
title_zh: StaRPO：稳定性增强的强化策略优化
authors: "Jinghan Zhang, Fengran Mo, Tharindu Cyril Weerasooriya, Ruimin Dai, Xiaoyan Han, Yanjie Fu, Dakuo Wang, Kunpeng Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08905v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 增强推理任务中逻辑结构的强化学习框架
tldr: 针对大语言模型在强化学习中仅依赖最终答案反馈而忽视推理逻辑一致性的问题，本文提出 StaRPO 框架。该框架将推理稳定性引入优化目标，通过自相关函数（ACF）衡量局部步骤的连贯性，并利用路径效率（PE）评估全局目标的导向性。在四个推理基准测试上的实验表明，StaRPO 不仅提升了答案的准确率，还显著增强了模型推理过程的逻辑稳定性和简洁性。
selection_source: fresh_fetch
motivation: 现有的强化学习框架主要依赖最终答案的正确性作为反馈，导致模型生成的推理过程虽然流畅但往往存在逻辑不一致或冗余。
method: 提出 StaRPO 框架，通过引入自相关函数（ACF）和路径效率（PE）两个轻量化指标来量化推理稳定性，并将其与任务奖励结合进行策略优化。
result: 实验证明 ACF 和 PE 与逻辑错误高度相关，且 StaRPO 在四个推理基准测试中均优于现有基准模型，实现了准确率与逻辑稳定性的双重提升。
conclusion: 在强化学习中显式引入过程感知的稳定性奖励，是提升大语言模型复杂推理能力和逻辑严密性的有效途径。
---

## 摘要
强化学习 (RL) 在提高大语言模型处理复杂推理任务的准确性方面非常有效。现有的强化学习策略优化框架主要依赖最终答案的正确性作为反馈信号，很少捕捉推理过程的内部逻辑结构。因此，模型生成的响应虽然流畅且语义相关，但在逻辑上不一致、结构不稳定或存在冗余。为此，我们提出了 StaRPO，这是一种稳定性增强的强化学习框架，它显式地将推理稳定性纳入优化目标。StaRPO 将稳定性分解为两个可计算的轻量级指标：用于评估局部步骤间连贯性的自相关函数 (ACF)，以及用于评估推理轨迹全局目标导向性的路径效率 (PE)。这些稳定性奖励与任务奖励相结合，提供了互补且具备过程感知能力的反馈。我们通过在两个骨干模型上展示 ACF 和 PE 奖励与逻辑错误之间的相关性，验证了使用这些奖励的有效性。在四个推理基准测试上的实验表明，StaRPO 一贯优于对比基线，并能同时提高最终答案的准确性和逻辑稳定性。

## Abstract
Reinforcement learning (RL) is effective in enhancing the accuracy of large language models in complex reasoning tasks. Existing RL policy optimization frameworks rely on final-answer correctness as feedback signals and rarely capture the internal logical structure of the reasoning process. Consequently, the models would generate fluent and semantically relevant responses but logically inconsistent, structurally erratic, or redundant. To this end, we propose StaRPO, a stability-augmented reinforcement learning framework that explicitly incorporates reasoning stability into the optimization objective. Our StaRPO decomposes stability into two computable lightweight metrics: the Autocorrelation Function (ACF) to evaluate local step-to-step coherence, and Path Efficiency (PE) to evaluate global goal-directedness of the reasoning trajectory. These stability rewards are combined with task rewards to provide complementary and process-aware feedback. We validate the effectiveness of using ACF and PE rewards by showing their correlation with logic errors on two backbone models. Experiments on four reasoning benchmarks show that StaRPO consistently outperforms compared baselines and can enhance both final-answer accuracy and logical stability.

---

## 论文详细总结（自动生成）

以下是对论文《StaRPO: Stability-Augmented Reinforcement Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然强化学习（RL）显著提升了大语言模型（LLM）在复杂推理任务中的表现，但现有框架（如 DeepSeek 的 GRPO）主要依赖“最终答案正确性”作为反馈。这导致模型虽然能产生正确答案，但推理过程往往存在**逻辑不一致、结构不稳定或冗余**的问题，具体表现为语义漂移（Semantic Drift）、逻辑跳跃（Logical Leap）和重复循环（Repetition Loop）。
*   **核心目标**：提出一种能够显式捕捉并优化推理过程“逻辑稳定性”的强化学习框架，使模型不仅能做对题，还能展现出连贯、高效且符合逻辑的推理路径。

### 2. 方法论
StaRPO 框架在 GRPO 的基础上，引入了基于嵌入空间（Embedding Space）轨迹分析的稳定性奖励。
*   **核心思想**：将推理过程视为语义空间中的状态转移轨迹，通过量化轨迹的局部连贯性和全局目标导向性来提供反馈。
*   **关键技术指标**：
    1.  **局部稳定性：步进自相关函数 (ACF)**：将推理过程划分为 $K$ 个步骤，计算相邻步骤间语义变化向量的余弦相似度。正向的 ACF 鼓励连贯的推理，惩罚突然的语义转折或跳跃。
    2.  **全局稳定性：路径效率 (PE)**：计算推理起点到终点的净位移与总路径长度的比值。比值接近 1 表示推理路径直达目标，比值较低则暗示存在绕路或循环。
*   **算法流程**：
    *   在 GRPO 的组相对优化机制中，将原始的任务奖励（如答案对错）与 ACF 奖励、PE 奖励加权融合：$r_{StaRPO} = r_{task} + \lambda_{acf} \cdot r_{acf} + \lambda_{pe} \cdot r_{pe}$。
    *   利用组内平均值和标准差对融合后的奖励进行归一化，计算优势函数（Advantage），从而更新策略网络。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K & MATH**：测试基础及竞赛级数学推理。
    *   **GPQA-Diamond**：测试博士级科学问题推理。
    *   **AIME24**：测试高难度数学竞赛推理。
    *   **Game of 24**：专门用于验证指标与逻辑错误相关性的分析场景。
*   **基准模型 (Backbones)**：Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct。
*   **对比方法**：
    *   **RL 变体**：GRPO、CPPO（剪枝优化）、$\lambda$-GRPO（长度偏置修正）。
    *   **过程干预类**：基于熵的控制（Entropy-based）、LLM-Planner（外部规划器）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H200 80G GPU**。
*   **训练细节**：进行了 2 次 RL 迭代，组大小（Group Size）固定为 8，学习率为 1e-6，KL 散度系数 $\beta=0.04$。文中未明确给出总训练时长，但 H200 属于顶级算力资源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 两种参数规模的模型，并在四个主流推理基准上进行了全面测试。
*   **相关性验证**：通过 GPT-4o-mini 人工标注逻辑错误，验证了 ACF 和 PE 指标与逻辑错误之间存在显著的统计学相关性（通过 t-检验和 M-W 检验），证明了指标的科学性。
*   **消融实验**：分别去除了 ACF 和 PE 奖励，验证了局部和全局稳定性信号的互补性。
*   **评价**：实验设计较为充分，不仅关注最终准确率（Result Acc），还引入了过程准确率（Process Acc），评估维度客观且全面。

### 6. 主要结论与发现
*   **性能领先**：StaRPO 在所有基准测试中均优于 GRPO 及其变体，尤其在知识密集型（GPQA）和长程推理（MATH）任务中提升显著。
*   **过程优化**：StaRPO 对“过程准确率”的提升往往高于对“最终准确率”的提升，说明该方法确实改善了模型的逻辑质量。
*   **轻量化有效性**：基于嵌入向量的 ACF 和 PE 指标是极佳的轻量化代理信号，无需调用昂贵的“LLM-as-a-judge”即可实现有效的过程监督。

### 7. 优点
*   **创新性指标**：将时间序列中的自相关概念引入 LLM 推理轨迹分析，提供了一种新颖的逻辑稳定性量化手段。
*   **计算高效**：相比于需要额外训练奖励模型（RM）或调用外部验证器的方案，StaRPO 直接利用模型自身的隐藏层状态，计算开销极低。
*   **即插即用**：可以轻松集成到现有的组优化（Group-based）强化学习框架中。

### 8. 不足与局限
*   **嵌入依赖性**：该方法高度依赖模型内部嵌入空间的语义表达质量，如果模型本身的表征能力较弱，ACF 和 PE 指标可能会失效。
*   **超参数敏感性**：$\lambda$ 权重（ACF 和 PE 的占比）可能需要针对不同任务进行精细调优。
*   **任务局限**：实验主要集中在数学和科学推理等具有明确逻辑链的任务上，对于开放式创作或多轮对话等逻辑结构较松散的任务，其适用性尚待验证。

（完）
