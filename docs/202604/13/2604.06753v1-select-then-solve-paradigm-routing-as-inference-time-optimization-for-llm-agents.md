---
title: "Select-then-Solve: Paradigm Routing as Inference-Time Optimization for LLM Agents"
title_zh: 先选择后解决：范式路由作为大语言模型智能体的推理时优化
authors: "Heng Zhou, Zelin Tan, Zhemeng Zhang, Yutao Fan, Yibing Lin, Li Kang, Xiufeng Song, Rui Li, Songtao Huang, Ao Yu, Yuchen Fan, Yanxu Chen, Kaixin Xu, Xiaohong Liu, Yiran Qin, Philip Torr, Chen Zhang, Zhenfei Yin"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06753v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: CoT和ReAct等推理范式的比较
tldr: 本研究探讨了推理范式（如CoT、ReAct等）对LLM智能体性能的影响，发现没有单一范式能在所有任务中胜出。通过对6种范式在10个基准测试上的大规模实验，作者提出了“先选择后解决”的方法，利用轻量级嵌入路由器为每个任务动态匹配最优范式。该方法显著提升了平均准确率，超越了所有固定范式，证明了推理范式选择应是基于任务的推理时优化过程。
selection_source: fresh_fetch
motivation: 旨在研究不同推理范式对LLM性能的差异化影响，并解决单一固定范式在跨任务场景下表现不佳的问题。
method: 提出一种“先选择后解决”的框架，通过轻量级嵌入路由器在推理前为特定任务自动选择最佳推理范式。
result: "实验表明该路由器将平均准确率提升至53.1%，优于最佳固定范式的50.3%，并有效缩小了与理想选择之间的差距。"
conclusion: 推理范式的选择不应是固定的架构设计，而应是针对每个任务通过学习型路由器进行的推理时动态优化。
---

## 摘要
当基于 LLM 的智能体在任务上表现提升时，这种增益是源于模型本身，还是源于其外层的推理范式？我们通过在四种前沿 LLM 和十个基准测试上比较六种推理时范式（即 Direct、CoT、ReAct、Plan-Execute、Reflection 和 ReCode）来研究这一问题，共进行了约 18,000 次运行。我们发现，推理结构在某些任务上大有裨益，但在其他任务上却适得其反：在 GAIA 上，ReAct 相比 Direct 提升了 44 个百分点，而在 HumanEval 上，CoT 使性能下降了 15 个百分点。没有哪种范式占据绝对主导地位，且针对每个任务的理想选择（oracle selection）平均比最佳固定范式高出 17.1 个百分点。受这种互补性的启发，我们提出了一种“先选择后解决”的方法：在回答每个任务之前，由一个轻量级的基于嵌入的路由器选择最合适的范式。在四种模型中，该路由器将平均准确率从 47.6% 提升至 53.1%，比 50.3% 的最佳固定范式高出 2.8 个百分点，并弥补了高达 37% 的理想差距。相比之下，零样本自路由仅在 GPT-5 上有效（准确率为 67.1%），在较弱的模型上则表现不佳，且均落后于学习型路由器。我们的结果表明，推理范式的选择应该是通过学习型路由器针对每个任务做出的决策，而非固定的架构选择。

## Abstract
When an LLM-based agent improves on a task, is the gain from the model itself or from the reasoning paradigm wrapped around it? We study this question by comparing six inference-time paradigms, namely Direct, CoT, ReAct, Plan-Execute, Reflection, and ReCode, across four frontier LLMs and ten benchmarks, yielding roughly 18,000 runs. We find that reasoning structure helps dramatically on some tasks but hurts on others: ReAct improves over Direct by 44pp on GAIA, while CoT degrades performance by 15pp on HumanEval. No single paradigm dominates, and oracle per-task selection beats the best fixed paradigm by 17.1pp on average. Motivated by this complementarity, we propose a select-then-solve approach: before answering each task, a lightweight embedding-based router selects the most suitable paradigm. Across four models, the router improves average accuracy from 47.6% to 53.1%, outperforming the best fixed paradigm at 50.3% by 2.8pp and recovering up to 37% of the oracle gap. In contrast, zero-shot self-routing only works for GPT-5 at 67.1% and fails for weaker models, all trailing the learned router. Our results argue that reasoning paradigm selection should be a per-task decision made by a learned router, not a fixed architectural choice.

---

## 论文详细总结（自动生成）

这篇论文《Select-then-Solve: Paradigm Routing as Inference-Time Optimization for LLM Agents》对大语言模型（LLM）智能体的推理范式进行了深入的系统性研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**LLM 智能体性能的提升究竟源于模型能力的增强，还是源于其外层的推理范式（如 CoT、ReAct 等）？**
目前，研究界不断涌现新的推理范式，但缺乏统一框架下的公平对比。开发者通常为所有任务选择一种固定的范式，而忽略了不同任务对推理结构的需求差异。论文旨在证明：**没有一种范式能统治所有任务**，推理范式的选择应当是一个针对具体任务的“推理时优化”过程。

### 2. 方法论：核心思想与关键技术
论文提出了 **“先选择后解决”（Select-then-Solve）** 的框架，其核心思想是在执行任务前，先通过一个轻量级的路由器（Router）预测最适合该任务的推理范式。

*   **推理范式池**：研究涵盖了六种代表性范式：
    *   **Direct**：直接回答。
    *   **CoT**：思维链，逐步推理。
    *   **ReAct**：推理与工具调用交替。
    *   **Plan-Execute**：先规划后执行。
    *   **Reflection**：生成-评价-修正循环。
    *   **ReCode**：将问题转化为代码递归求解。
*   **路由策略**：
    *   **手工特征路由器**：基于任务文本统计（长度、是否含代码/数学符号等）和数据集标签进行逻辑回归或 MLP 分类。
    *   **嵌入路由器（Embedding-based）**：利用 OpenAI 的 `text-embedding-3-small` 将任务文本向量化，捕捉深层语义特征。
    *   **自路由（Self-routing）**：让模型通过零样本（Zero-shot）提示词自行选择范式。
*   **工作流程**：输入任务 $q \rightarrow$ 路由器选择范式 $P \rightarrow$ 调用 LLM 执行 $P \rightarrow$ 输出答案。

### 3. 实验设计
*   **模型**：选取了四种前沿模型：GPT-5、Gemini-3-Flash、Qwen3-Max、Qwen3-30B。
*   **基准测试（10个）**：涵盖代码（HumanEval）、数学（MATH500, AIME）、问答（HotpotQA, NQ）、知识（MMLU, HLE）、工具使用（GAIA, $\tau$-bench, SEAL）。
*   **对比方法**：
    *   **固定范式**：始终使用六种范式中的某一种。
    *   **Oracle（理想选择）**：假设总能选出能做对该题的最优范式（性能上限）。
    *   **不同配置的路由器**：手工特征 vs. 嵌入特征。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   但从实验规模可以推断算力消耗巨大：共涉及 4 种模型 $\times$ 6 种范式 $\times$ 10 个基准测试，总计完成了约 **18,000 次** 完整的推理运行。

### 5. 实验数量与充分性
*   **实验规模**：1.8万次运行在大规模智能体研究中属于非常充分的水平。
*   **公平性**：论文构建了统一的 `PARADIGM` 评估框架，所有范式共享相同的模型接口、工具集（搜索和代码执行）和评估代码，消除了提示词工程或工具差异带来的干扰。
*   **客观性**：通过 Oracle 分析揭示了范式间的互补性，而非单纯吹捧某种新方法，实验设计逻辑严密。

### 6. 主要结论与发现
*   **范式具有任务依赖性**：推理结构在某些任务上是“良药”（如 ReAct 在 GAIA 上提升 44%），在某些任务上是“毒药”（如 CoT 在 HumanEval 上导致性能下降 15%）。
*   **能力补偿效应**：推理结构对弱模型的补偿作用更明显。对于强模型（如 GPT-5），简单的 Direct 往往已经足够。
*   **路由器的有效性**：基于嵌入的路由器平均准确率达 53.1%，优于任何固定范式（最高 50.3%），并弥补了约 37% 的 Oracle 差距。
*   **自路由的局限性**：只有最强的模型（GPT-5）具备一定的自路由能力，弱模型在选择范式时经常失准（倾向于过度选择复杂的 ReAct），表现甚至不如 Direct。

### 7. 优点
*   **视角独特**：将推理范式视为一种可优化的“推理时计算资源分配”，而非静态的架构。
*   **大规模实证**：提供了目前为止最全面的推理范式对比矩阵，揭示了许多违反直觉的现象（如 Reflection 并不总是有效）。
*   **实用性强**：提出的嵌入路由器极其轻量，仅需一次 Embedding 调用，成本远低于运行复杂的推理循环。

### 8. 不足与局限
*   **范式实现单一**：每种范式只选用了最典型的一种实现方式，未探索提示词变体或搜索预算的影响。
*   **工具耦合**：工具访问（如搜索）被视为范式的一部分，未能完全解耦“推理结构”与“外部工具”的各自贡献。
*   **训练集规模**：路由器的训练数据相对较小（每种模型约 400 个样本），可能限制了其泛化到极异构任务的能力。
*   **模型时效性**：文中提到的 GPT-5 等模型名称可能暗示该论文具有前瞻性或使用了特定版本的占位符（注：当前现实中 GPT-5 尚未正式发布）。

（完）
