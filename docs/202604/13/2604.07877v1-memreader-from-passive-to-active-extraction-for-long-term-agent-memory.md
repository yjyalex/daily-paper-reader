---
title: "MemReader: From Passive to Active Extraction for Long-Term Agent Memory"
title_zh: MemReader：从被动提取到主动提取的智能体长期记忆
authors: "Jingyi Kang, Chunyu Li, Ding Chen, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07877v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 为准确且符合模式的结构化输出而蒸馏
tldr: 针对长期记忆提取中存在的噪声和不一致问题，本文提出MemReader系列模型。包含0.6B的轻量级被动提取器和4B的主动提取器。MemReader-4B通过GRPO优化，采用ReAct范式，在写入前评估信息价值和完整性，支持选择性写入、延迟处理或检索历史背景。实验证明其在知识更新和减少幻觉方面表现优异，已集成至MemOS并投入实际应用。
selection_source: fresh_fetch
motivation: 现有的被动式记忆提取方法难以处理嘈杂对话和跨轮次依赖，导致记忆污染和低价值写入。
method: 提出MemReader-0.6B（蒸馏得到的结构化提取器）和MemReader-4B（基于GRPO优化、具备主动推理和决策能力的ReAct式提取器）。
result: 在LOCOMO、LongMemEval和HaluMem等基准测试中，MemReader在知识更新、时序推理和减少幻觉方面均达到SOTA水平。
conclusion: 有效的智能体记忆不仅需要提取信息，更需要通过推理驱动的选择性提取来构建低噪声且动态演进的长期记忆。
---

## 摘要
长期记忆是实现个性化和自主智能体的基础，但如何填充记忆仍是一个瓶颈。现有系统将记忆提取视为从上下文到结构化条目的一次性、被动转录，这在处理噪声对话、缺失引用和跨轮次依赖时表现不佳，导致记忆污染、低价值写入和不一致性。在本文中，我们推出了用于智能体系统主动长期记忆提取的 MemReader 系列：MemReader-0.6B 是一个紧凑且具有成本效益的被动提取器，通过蒸馏实现准确且符合模式一致性的结构化输出；MemReader-4B 是一个通过群体相对策略优化（GRPO）进行优化的主动提取器，用于做出记忆写入决策。在 ReAct 风格的范式下，MemReader-4B 在采取行动前会明确评估信息价值、引用歧义和完整性，并能选择性地写入记忆、推迟不完整的输入、检索历史上下文或丢弃无关的闲聊。在 LOCOMO、LongMemEval 和 HaluMem 上的实验表明，MemReader 始终优于现有的基于提取的基准模型。特别是，MemReader-4B 在涉及知识更新、时间推理和减少幻觉的任务上达到了最先进的性能。这些结果表明，有效的智能体记忆不仅需要提取更多信息，还需要进行推理驱动的选择性记忆提取，以构建低噪声且动态演进的长期记忆。此外，MemReader 已集成到 MemOS 中，并正在实际应用中部署。为了支持未来的研究和应用，我们发布了模型并提供公开的 API 访问。

## Abstract
Long-term memory is fundamental for personalized and autonomous agents, yet populating it remains a bottleneck. Existing systems treat memory extraction as a one-shot, passive transcription from context to structured entries, which struggles with noisy dialogue, missing references, and cross-turn dependencies, leading to memory pollution, low-value writes, and inconsistency. In this paper, we introduce the MemReader family for active long-term memory extraction in agent systems: MemReader-0.6B, a compact and cost-efficient passive extractor distilled for accurate and schema-consistent structured outputs, and MemReader-4B, an active extractor optimized with Group Relative Policy Optimization (GRPO) to make memory writing decisions. Under a ReAct-style paradigm, MemReader-4B explicitly evaluates information value, reference ambiguity, and completeness before acting, and can selectively write memories, defer incomplete inputs, retrieve historical context, or discard irrelevant chatter. Experiments on LOCOMO, LongMemEval, and HaluMem show that MemReader consistently outperforms existing extraction-based baselines. In particular, MemReader-4B achieves state-of-the-art performance on tasks involving knowledge updating, temporal reasoning, and hallucination reduction. These results suggest that effective agent memory requires not merely extracting more information, but performing reasoning-driven and selective memory extraction to build low-noise and dynamically evolving long-term memory. Furthermore, MemReader has been integrated into MemOS and is being deployed in real-world applications. To support future research and adoption, we release the models and provide public API access.

---

## 论文详细总结（自动生成）

### MemReader：从被动提取到主动管理的智能体长期记忆论文总结

#### 1. 论文的核心问题与整体含义
长期记忆是实现个性化和自主智能体的关键，但现有的记忆系统（如 Mem0, Zep, MemOS）大多采用**被动提取（Passive Extraction）**模式。这种模式将记忆提取视为从对话到结构化条目的一次性转录，存在以下瓶颈：
*   **记忆污染**：无法区分闲聊与高价值信息，导致存储大量低价值数据。
*   **指代不明**：难以处理对话中的代词、省略和跨轮次依赖。
*   **更新困难**：对用户状态的动态演进和知识更新支持较弱。
*   **成本与效率**：依赖大型通用模型 API 进行每一步提取，部署成本高。

本文提出了 **MemReader** 系列模型，旨在将记忆提取转变为**主动记忆管理（Active Memory Management）**，通过显式的推理和决策来构建低噪声、可更新且可靠的长期记忆。

#### 2. 论文提出 Method 核心思想与技术细节
MemReader 包含两个模型：**MemReader-0.6B**（侧重成本效益的结构化提取）和 **MemReader-4B**（侧重主动决策的推理模型）。

*   **核心思想**：引入 **ReAct（Reasoning + Acting）** 范式。模型在处理每一轮对话时，先进行内部推理（Think），再决定采取何种动作（Action）。
*   **动作空间（Action Space）**：
    *   `add_memory`：信息完整且有价值时，直接写入长期记忆。
    *   `buffer_memory`：信息有价值但不完整（如用户话没说完），暂时存入缓冲区。
    *   `search_memory`：遇到歧义（如“那个计划”）时，检索历史记忆进行消歧。
    *   `ignore_memory`：识别并丢弃无意义的闲聊或重复信息。
*   **训练流程**：
    1.  **SFT 预热**：使用由强模型（Gemini-3-Flash）生成的轨迹数据进行监督微调，使模型掌握 ReAct 协议。
    2.  **GRPO 强化学习**：采用群体相对策略优化（Group Relative Policy Optimization），通过多维度奖励函数进行对齐。
*   **奖励函数设计**：
    *   **格式奖励**：确保输出符合 JSON 和标签规范。
    *   **动作对齐奖励**：惩罚错误的决策路径（如该检索时选择了写入）。
    *   **LLM-Judge 奖励**：评估提取内容的准确性、完整性和幻觉率。
    *   **效率奖励**：惩罚冗长的推理和冗余的记忆条目，鼓励简洁压缩。

#### 3. 实验设计
论文在三个权威的公开基准测试上进行了评估：
*   **LOCOMO**：评估长对话记忆，涵盖单跳/多跳推理、时序理解和开放域 QA。
*   **LongMemEval**：评估长期记忆系统的六个维度，包括知识更新、时序推理和跨轮次一致性。
*   **HaluMem-Medium**：专门针对记忆系统中的幻觉问题，评估提取、更新和 QA 的准确率。
*   **对比方法**：包括 Mem0, Zep, MemOS (基于 GPT-4o-mini), MemU, Memobase, EverMemOS 等主流开源及闭源方案。

#### 4. 资源与算力
*   **硬件**：训练使用了 **8 × NVIDIA A800 (80GB) GPU**。
*   **软件框架**：使用 LlamaFactory 进行全参数 SFT，使用 verl 框架进行 GRPO 训练。
*   **训练参数**：SFT 阶段学习率 1e-5，3 个 Epoch；GRPO 阶段学习率 1e-6，组大小（Group Size）为 8。
*   **模型底座**：基于 Qwen3-0.6B 和 Qwen3-4B。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **跨基准测试**：在三个不同的 Benchmark 上进行了全面对比，实验组数充足。
*   **消融与对比**：对比了 SFT 版本与 GRPO 版本的性能差异，验证了强化学习的有效性。
*   **效率分析**：记录了每个模型的 Token 消耗，评估了推理成本。
*   **客观性**：使用了 GPT-4o-mini 作为统一的第三方评测器（LLM-as-a-Judge），减少了主观偏差。整体实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **主动管理优于被动提取**：MemReader-4B 在知识更新和时序推理方面显著超过了基于 GPT-4o-mini 的被动提取基准。
*   **小模型也强大**：经过针对性蒸馏的 MemReader-0.6B 在结构化提取的准确性上表现优异，甚至在某些指标上超过了大模型。
*   **强化学习的关键作用**：GRPO 显著提升了模型的决策对齐能力，特别是在减少幻觉和提高记忆压缩率方面。
*   **端到端收益**：高质量的记忆写入能显著提升下游 QA 任务的可靠性，减少因记忆污染导致的错误。

#### 7. 优点
*   **范式创新**：将记忆提取重新定义为“状态管理问题”，引入缓冲区和检索消歧机制，更符合真实交互逻辑。
*   **训练方案高效**：采用 GRPO 算法，无需额外的 Critic 模型，降低了强化学习的资源开销。
*   **实用性强**：模型轻量（0.6B/4B），且已集成至 MemOS 系统并提供 API，具备极高的工程落地价值。
*   **开源贡献**：发布了模型权重、代码和 API 文档，推动了社区研究。

#### 8. 不足与局限
*   **推理延迟**：ReAct 范式涉及多轮“思考-行动”循环，相比一次性提取，其首字延迟（TPOT）和总推理时间可能更高。
*   **数据依赖**：训练数据高度依赖于教师模型（Gemini-3-Flash）生成的轨迹，可能存在教师模型的偏见传递。
*   **工具扩展性**：目前仅支持四种基础动作，尚未深入探讨更复杂的记忆编辑、冲突检测或分层抽象机制。
*   **长程稳定性**：虽然在基准测试表现良好，但在极长周期（如数月）的真实在线交互中的稳定性仍需进一步验证。

（完）
