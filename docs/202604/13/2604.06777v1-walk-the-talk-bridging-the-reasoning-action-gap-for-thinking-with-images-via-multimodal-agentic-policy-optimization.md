---
title: "Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization"
title_zh: 言行一致：通过多模态智能体策略优化弥合结合图像思考中的推理-行动差距
authors: "Wenhao Yang, Yu Xia, Jinlong Huang, Shiyin Lu, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Yuchen Zhou, Xiaobo Xia, Yuanyu Wan, Lijun Zhang, Tat-Seng Chua"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06777v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对推理-动作差距的多模态智能体策略优化
tldr: 针对多模态大模型在多轮推理中存在的“推理-行动脱节”问题（即文本推理看似合理但视觉工具调用不准确），本文提出了多模态智能体策略优化（MAPO）。该方法要求模型对工具获取的视觉内容生成文本描述，并通过衡量描述与实际观察的语义一致性来优化奖励机制。实验证明，MAPO能有效减少梯度方差，在多个视觉推理基准测试中显著提升了模型的性能。
selection_source: fresh_fetch
motivation: 现有的基于结果的强化学习忽略了文本推理与视觉行动之间的不一致性，导致推理过程中噪声累积甚至训练崩溃。
method: 提出MAPO框架，通过要求模型生成视觉内容的文本描述，并将描述与实际观察的语义对齐度与任务奖励相结合来估计优势函数。
result: 理论证明MAPO能降低梯度方差，实验结果显示其在多个多模态推理基准测试上均优于现有方法。
conclusion: MAPO成功弥合了多模态推理中的推理与行动差距，为提升智能体在复杂视觉任务中的可靠性提供了有效路径。
---

## 摘要
近期多模态大语言模型（MLLMs）的进展激励模型在多轮推理过程中通过主动调用视觉工具来“结合图像进行思考”。常见的依赖基于结果奖励的强化学习（RL）实践忽略了一个事实，即文本上的合理性往往掩盖了执行上的失败，这意味着模型在智能体推理轨迹中可能会表现出直观的文本推理，但执行的视觉动作却不精确或不相关。这种推理与行动之间的差异引入了在多轮推理过程中不断累积的噪声，严重降低了模型的多模态推理能力，并可能导致训练崩溃。在本文中，我们提出了多模态智能体策略优化（MAPO），旨在弥合模型在其多模态思维链（MCoT）中生成的文本推理与视觉动作之间的差距。具体而言，MAPO 要求模型为通过工具调用获取的视觉内容生成显式的文本描述。随后，我们采用了一种新颖的优势估计方法，将这些描述与实际观测结果之间的语义一致性与任务奖励相结合。理论研究证明了 MAPO 背后的合理性，它本质上降低了梯度的方差；广泛的实验表明，我们的方法在多个视觉推理基准测试中均取得了优异的性能。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have incentivized models to ``think with images'' by actively invoking visual tools during multi-turn reasoning. The common Reinforcement Learning (RL) practice of relying on outcome-based rewards ignores the fact that textual plausibility often masks executive failure, meaning that models may exhibit intuitive textual reasoning while executing imprecise or irrelevant visual actions within their agentic reasoning trajectories. This reasoning-action discrepancy introduces noise that accumulates throughout the multi-turn reasoning process, severely degrading the model's multimodal reasoning capabilities and potentially leading to training collapse. In this paper, we introduce Multimodal Agentic Policy Optimization (MAPO), bridging the gap between textual reasoning and visual actions generated by models within their Multimodal Chain-of-Thought (MCoT). Specifically, MAPO mandates the model to generate explicit textual descriptions for the visual content obtained via tool usage. We then employ a novel advantage estimation that couples the semantic alignment between these descriptions and the actual observations with the task reward. Theoretical findings are provided to justify the rationale behind MAPO, which inherently reduces the variance of gradients, and extensive experiments demonstrate that our method achieves superior performance across multiple visual reasoning benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）正从“观察图像”转向“结合图像思考”（Thinking with Images），即在多轮推理中主动调用视觉工具（如缩放、裁剪）。
*   **核心问题**：**推理-行动差距（Reasoning-Action Gap）**。现有的强化学习（RL）主要依赖结果奖励（Outcome-based Reward），导致模型可能产生看似合理的文本推理，但执行了错误或无关的视觉动作（例如：文本说要看左边的车，实际裁剪了右边的树）。这种不一致性会产生噪声，导致多轮推理失败甚至训练崩溃。
*   **整体含义**：本文旨在通过确保模型的“言”（文本推理）与“行”（视觉动作）在语义上高度一致，来提升多模态智能体的可靠性和推理能力。

### 2. 方法论
*   **核心思想**：引入**多模态智能体策略优化（MAPO）**，通过一个“验证-强化”循环来约束中间推理过程。
*   **关键技术细节**：
    1.  **显式标签生成（Talk）**：要求模型在调用工具时，必须生成一个描述性标签 $y_t$，说明它预期在观察结果中看到什么。
    2.  **语义评分（Walk the Talk）**：使用预训练的轻量级 **CLIP 模型** 计算生成的标签 $y_t$ 与工具返回的实际图像 $I_t$ 之间的余弦相似度。
    3.  **轨迹感知折扣因子**：引入因子 $\lambda$ 对语义分进行加权，公式为 $R_{sem} = \frac{1}{T} \sum_{t=1}^T \lambda^{T-t} z_t$。这能容忍初期的探索错误，同时惩罚冗长无效的轨迹。
    4.  **优势估计（Advantage Estimation）**：将语义一致性得分与最终结果奖励（正确/错误）结合，在组内进行归一化（类似 GRPO），引导策略更新。
*   **理论支撑**：论文证明了 MAPO 具有**双重方差削减**效应：通过组聚合减少空间采样方差，通过语义评分减少信号噪声方差，从而提高训练稳定性。

### 3. 实验设计
*   **数据集/场景**：专注于需要精细视觉推理的高分辨率基准测试：
    *   **V***：视觉搜索任务。
    *   **HR-Bench**：高分辨率图像理解。
    *   **MME-Realworld-Lite**：真实世界复杂场景。
*   **对比方法**：
    *   **闭源模型**：GPT-5 (预估/系统卡片数据)、Gemini 2.5 Pro。
    *   **开源智能体模型**：DeepEyes, Thyme, Mini-o3, DeepEyesv2。
    *   **RL 算法基准**：PPO, GRPO, DAPO, GSPO（均基于相同的 Ovis2.5-9B 底座）。

### 4. 资源与算力
*   **模型底座**：Ovis2.5-9B。
*   **算力细节**：文中**未明确说明具体的 GPU 型号和数量**。
*   **训练配置**：提到了全局 Batch Size 为 96，微批次（Micro Batch Size）为 4，最大交互轮数为 6 轮。使用了 `verl` 框架、`vLLM` 推理引擎和 FSDP（全分片数据并行）技术。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流高难度基准上进行了测试，涵盖了从 2K 到 8K 的超高分辨率图像。
*   **消融与对比**：对比了 4 种主流 RL 算法和多种 SOTA 模型，并绘制了训练奖励曲线以展示稳定性。
*   **客观性与公平性**：在对比 RL 算法时，所有方法均使用相同的开源底座（Ovis2.5-9B）和相同的冷启动 SFT 数据，确保了算法改进的纯粹性。实验设计较为充分且客观。

### 6. 主要结论与发现
*   **性能领先**：MAPO 在所有基准测试中均优于现有的开源智能体模型，并显著超过了 GRPO 等标准 RL 方法。
*   **训练稳定性**：标准 RL 方法（如 GRPO）在长时训练中容易出现性能崩溃（Reward Collapse），而 MAPO 保持了稳健的上升趋势。
*   **弥合差距**：可视化结果证实，MAPO 训练的模型能够更精准地执行与文本意图相符的视觉动作，减少了幻觉。

### 7. 优点
*   **理论与实践结合**：不仅提出了直观的语义对齐方法，还提供了严谨的梯度方差削减证明。
*   **计算高效**：使用 CLIP 作为验证器，相比调用大型 MLLM 作为奖励模型，计算开销极低。
*   **通用性**：该框架不仅限于视觉工具，其“描述意图-验证结果”的逻辑可扩展至网页浏览、代码执行等其他智能体领域。

### 8. 不足与局限
*   **依赖外部模型**：语义评分的准确性受限于 CLIP 模型的判别能力，对于极度细微或 CLIP 无法识别的属性可能失效。
*   **标签长度限制**：为了防止模型通过生成超长描述来“刷分”（Reward Hacking），需要人工设定标签长度约束。
*   **算力信息缺失**：未披露具体的训练时长和硬件资源消耗，不便于其他研究者评估复现成本。

（完）
