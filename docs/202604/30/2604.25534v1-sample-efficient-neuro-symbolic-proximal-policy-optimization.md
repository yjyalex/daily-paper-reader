---
title: Sample-efficient Neuro-symbolic Proximal Policy Optimization
title_zh: 样本高效的神经符号近端策略优化
authors: "Simone Murari, Celeste Veronese, Daniele Meli"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25534v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 近端策略优化（PPO）的神经符号扩展
tldr: 针对深度强化学习在稀疏奖励和长时程任务中采样效率低的问题，本文提出了一种神经符号近端策略优化（PPO）扩展方法。该方法通过将从简单实例中学习到的部分逻辑策略规范转移到复杂设置中来引导学习。研究引入了H-PPO-Product和H-PPO-SymLoss两种集成方式，分别在采样阶段和损失函数层面引入符号引导。实验证明该方法在多个基准测试中显著提升了学习速度和收敛性能，且对不完美的符号知识具有鲁棒性。
selection_source: fresh_fetch
motivation: 深度强化学习在处理具有长时程规划和稀疏奖励的复杂任务时，往往面临采样效率低下和难以收敛的挑战。
method: 提出了H-PPO-Product和H-PPO-SymLoss两种方法，分别通过在采样时偏差动作分布和在损失函数中添加符号正则化项来集成逻辑引导。
result: 在OfficeWorld、WaterWorld和DoorKey等基准测试中，该方法比原始PPO和奖励机基线表现出更快的学习速度和更高的收敛回报。
conclusion: 通过将部分逻辑策略规范与PPO结合，可以显著提高强化学习的采样效率，并增强其在复杂环境中的学习性能。
---

## 摘要
深度强化学习（DRL）算法通常需要大量数据，并且在具有长规划周期和多个子目标的稀疏奖励领域中面临挑战。在本文中，我们提出了近端策略优化（PPO）的一种神经符号扩展，它将在较简单实例中学习到的部分逻辑策略规范进行迁移，以引导更具挑战性设置下的学习。我们引入了两种符号引导的集成方式：(i) H-PPO-Product，它在采样时偏置动作分布；(ii) H-PPO-SymLoss，它使用符号正则化项来增强 PPO 损失。我们在三个基准测试（OfficeWorld、WaterWorld 和 DoorKey）上评估了我们的方法，结果表明，即使在符号知识不完美的情况下，我们的方法在收敛时的学习速度和回报也始终优于 PPO 和奖励机（Reward Machine）基准。

## Abstract
Deep Reinforcement Learning (DRL) algorithms often require a large amount of data and struggle in sparse-reward domains with long planning horizons and multiple sub-goals. In this paper, we propose a neuro-symbolic extension of Proximal Policy Optimization (PPO) that transfers partial logical policy specifications learned in easier instances to guide learning in more challenging settings. We introduce two integrations of symbolic guidance: (i) H-PPO-Product, which biases the action distribution at sampling time, and (ii) H-PPO-SymLoss, which augments the PPO loss with a symbolic regularization term. We evaluate our methods on three benchmarks (OfficeWorld, WaterWorld, and DoorKey), showing consistently faster learning and higher return at convergence than PPO and a Reward Machine baseline, also under imperfect symbolic knowledge.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **H-PPO（Hybrid Proximal Policy Optimization）** 的神经符号强化学习框架，旨在解决深度强化学习（DRL）在处理稀疏奖励、长时程规划和复杂子目标任务时采样效率低下的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：标准的 DRL 算法（如 PPO）在面对稀疏奖励和长任务链时，往往需要海量的交互数据才能收敛。此外，纯神经网络策略具有“黑盒”属性，缺乏可解释性且难以集成先验知识。
*   **研究动机**：现有的知识集成方法（如奖励机 Reward Machines）通常依赖于标量奖励塑造（Reward Shaping），这在启发式信息不准确时容易导致性能下降。本文希望通过在**动作层面**引入逻辑规则引导，利用从简单任务中提取的符号策略来加速复杂任务的学习。

### 2. 方法论
论文提出了两种将符号引导集成到 PPO 中的方案，核心思想是利用一阶逻辑（Horn 子句）定义的“符号策略”来指导智能体的行为：

*   **H-PPO-Product（采样端引导）**：
    *   在训练的动作采样阶段，根据符号规则对神经网络输出的动作分布进行重塑。
    *   如果某个动作符合逻辑规则，则通过一个权重因子 $\lambda$ 增加其被采样的概率。
    *   引入了随时间衰减的退火机制（Annealing），使智能体在后期逐渐摆脱符号偏置，回归到纯神经策略，从而具备纠正不完美规则的能力。
*   **H-PPO-SymLoss（优化端引导）**：
    *   在损失函数中增加一个符号正则化项（Symbolic Loss）。
    *   该项衡量神经策略与基于逻辑规则构建的“参考策略”之间的差异。
    *   通过参数 $\Theta$ 平衡标准 PPO 损失与符号损失，促使网络在优化过程中向符合逻辑的方向收敛。

### 3. 实验设计
*   **实验场景**：
    1.  **DoorKey**：部分可观测环境，需找钥匙、开门并到达终点。测试了不同网格大小（8x8, 16x16）和钥匙数量（1, 2, 4）。
    2.  **OfficeWorld**：离散网格环境，需按顺序完成多个子目标（如取咖啡、取邮件、去办公室），同时避开障碍。
    3.  **WaterWorld**：连续 2D 空间，需按特定颜色顺序碰撞移动的小球。
*   **基准模型（Benchmarks）**：
    *   **Vanilla PPO**：标准近端策略优化算法。
    *   **PPO + Reward Machine (RM)**：使用奖励机进行状态塑造的基线方法。
*   **对比策略**：验证在符号知识**不完美**或**部分缺失**的情况下，H-PPO 是否依然稳健。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）或总训练时长。
*   **超参数**：附录中详细列出了不同任务的训练步数（从 100 万到 1 亿步不等）、环境并行数、Batch Size 等参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同的领域进行了多达 13 种变体配置的测试。
    *   进行了针对符号权重 $\Theta$ 和衰减系数 $\epsilon_f$ 的**消融实验**。
*   **充分性与客观性**：实验设计较为充分，涵盖了从简单到极难（长路径、多干扰项）的梯度。通过在简单实例上诱导规则并直接迁移到复杂实例，验证了方法的泛化能力。对比了 RM 这一强基线，证明了动作级引导在特定场景下优于奖励级引导。

### 6. 主要结论与发现
*   **采样效率显著提升**：在所有基准测试中，H-PPO 的学习速度均快于 Vanilla PPO 和 RM。
*   **分工明确**：
    *   **H-PPO-Product** 在需要强力探索的任务（如 DoorKey 和 WaterWorld）中表现最佳，能有效引导智能体发现稀疏奖励。
    *   **H-PPO-SymLoss** 在策略巩固和正则化方面更有优势（如 OfficeWorld），能获得更高的最终回报。
*   **鲁棒性**：即使符号规则不完整（例如只提供了部分子目标的引导），H-PPO 依然能通过神经部分的学习补全剩余逻辑。

### 7. 优点
*   **即插即用**：无需针对复杂任务重新调整 PPO 的核心超参数，仅需加入逻辑引导。
*   **缓解冷启动问题**：通过在采样阶段引入偏置，极大地缓解了稀疏奖励环境下的探索难题。
*   **知识迁移**：展示了如何将人类可读的逻辑规则从简单环境无缝迁移到大规模环境。

### 8. 不足与局限
*   **谓词依赖**：系统高度依赖于预定义的逻辑谓词（如 `sameColor`, `at`），如果底层感知系统无法准确提取这些谓词，性能将受限。
*   **动作空间限制**：目前主要针对离散动作空间设计，虽然在 WaterWorld（连续状态）中表现良好，但尚未扩展到完全连续的动作控制任务。
*   **规则获取**：虽然论文提到规则可以从经验中诱导，但实验中仍使用了部分人工定义的逻辑片段，自动化程度有待进一步提高。

（完）
