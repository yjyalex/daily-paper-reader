---
title: Backtranslation Augmented Direct Preference Optimization for Neural Machine Translation
title_zh: 回译增强的直接偏好优化在神经机器翻译中的应用
authors: "Mehrdad Ghassabi, Spehr Rajabi, Hamidreza Baradaran Kashani, Sadra Hakim, Mahshid Keivandarian"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25702v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于神经机器翻译的直接偏好优化
tldr: 针对神经机器翻译在监督训练后仍存在翻译错误的问题，本文提出一种基于强化学习的后训练框架。该方法利用回译技术结合专家反馈，并采用直接偏好优化（DPO）算法对模型进行微调。在英德翻译实验中，该框架成功将gemma3-1b模型的COMET评分从0.703提升至0.747，证明了DPO在提升预训练翻译模型质量方面的高效性与稳定性。
selection_source: fresh_fetch
motivation: 旨在解决现有神经机器翻译系统在仅依赖监督并行数据训练时仍难以消除的持续性翻译错误。
method: 引入一种结合回译与直接偏好优化（DPO）的强化学习框架，通过专家反馈引导模型进行迭代优化。
result: 在英德翻译任务上，该方法使gemma3-1b模型的COMET评分从0.703显著提升至0.747。
conclusion: 研究证明了DPO是一种通过偏好学习增强预训练翻译模型性能的高效且稳定的后训练路径。
---

## 摘要
当代神经机器翻译（NMT）系统几乎完全是通过在监督并行数据上进行训练而构建的。尽管取得了巨大进展，这些系统仍然表现出持续存在的翻译错误。本文提出，基于强化学习（RL）的训练后范式可以有效纠正此类错误。我们引入了一个新颖的框架，该框架仅需要通用文本语料库和一名专家翻译（可以是人类或 AI 系统）来提供迭代反馈。在实验中，我们专门关注作为代表性高资源语言对的英德翻译。至关重要的是，我们使用直接偏好优化（DPO）实现了这种基于强化学习的训练后过程。将我们的 DPO 驱动框架应用于 gemma3-1b 模型，显著提高了翻译质量，在英德任务上将其 COMET 分数从 0.703 提升至 0.747。结果表明，DPO 为通过基于偏好的训练后过程增强预训练 NMT 模型提供了一条高效且稳定的途径。

## Abstract
Contemporary neural machine translation (NMT) systems are almost exclusively built by training on supervised parallel data. Despite the tremendous progress achieved, these systems still exhibit persistent translation errors. This paper proposes that a post-training paradigm based on reinforcement learning (RL) can effectively rectify such mistakes. We introduce a novel framework that requires only a general text corpus and an expert translator which can be either human or an AI system to provide iterative feedback. In our experiments, we focus specifically on English-to-German translation as a representative high-resource language pair. Crucially, we implement this RL-based post-training using Direct Preference Optimization (DPO). Applying our DPO-driven framework to the gemma3-1b model yields a significant improvement in translation quality, elevating it's COMET score from 0.703 to 0.747 on the English to German task. The results demonstrate that DPO offers an efficient and stable pathway for enhancing pre-trained NMT models through preference-based post-training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Backtranslation Augmented Direct Preference Optimization for Neural Machine Translation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的神经机器翻译（NMT）系统主要依赖监督学习（SFT）和大规模平行语料库。尽管表现优异，但仍存在“翻译腔”（translationese）、语义偏差和表达不自然等持续性错误。
*   **研究动机**：强化学习（RL）被认为是纠正这些错误的有效后训练手段，但传统的 RL 方法（如 PPO）过程复杂且不稳定。直接偏好优化（DPO）在语言模型对齐中表现出色，但在 NMT 领域的应用尚不充分，且缺乏高质量偏好对数据的构建方法。
*   **整体含义**：本文提出了一种结合**回译（Backtranslation）**与 **DPO** 的新框架，旨在利用通用语料和专家反馈，通过偏好对齐进一步提升预训练翻译模型的质量。

### 2. 论文提出的方法论
核心思想是利用“回译”过程中的质量差异来自动构建 DPO 所需的偏好三元组 $(x, y_w, y_l)$。
*   **数据构造流程**：
    1.  **专家翻译**：将源语言单语语料 $s$ 交给专家（人或高性能 AI）翻译成目标语言 $t$。
    2.  **回译诊断**：让待优化的学生模型 $\pi_\theta$ 将 $t$ 翻译回源语言，得到 $\hat{s}$。
    3.  **质量过滤**：计算 $\hat{s}$ 与原始 $s$ 的 BLEU 和 COMET 分数。利用 **Elbow Method（肘部法则）** 确定阈值，筛选出学生模型表现较差（分数低）的样本。
    4.  **构建偏好对**：
        *   输入 $x$：翻译指令 + 专家翻译结果 $t$。
        *   优选回答 $y_w$：原始源语言句子 $s$（代表正确、地道的表达）。
        *   弃选回答 $y_l$：学生模型生成的回译结果 $\hat{s}$（代表有缺陷的表达）。
*   **算法优化**：使用 DPO 损失函数直接优化学生模型，通过增大 $y_w$ 相对于 $y_l$ 的对数概率差，使模型输出向专家级质量对齐。

### 3. 实验设计
*   **任务场景**：高资源语言对的英德翻译（English $\rightarrow$ German）。
*   **基准模型（Benchmark）**：Google 发布的 **Gemma3-1B**。
*   **数据集**：使用标准的 **WMT14** 数据集作为源文本来源。
*   **对比指标**：采用了多维度的自动评估指标，包括 BLEU（n-gram 重合度）、COMET-DA/QE（神经评价指标，更接近人类判断）、METEOR、TER（编辑率）和 chrF++。

### 4. 资源与算力
*   **微调技术**：采用了 **LoRA（低秩自适应）** 参数高效微调技术，Rank 和 Alpha 均设为 32，仅训练线性层。
*   **超参数**：训练 1 个 Epoch，学习率预热比例 0.03，DPO 温度参数 $\beta = 0.1$，梯度累积步数为 4。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量及总训练时长。但考虑到使用的是 1B 规模的模型及 LoRA 技术，该方法对算力的需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：主要针对英德这一种语言对进行了深入实验。
*   **充分性评价**：
    *   **优点**：实验涵盖了从词汇重叠（BLEU）到语义对齐（COMET）的多种指标，评估较为全面。
    *   **不足**：缺乏消融实验（例如：如果不进行 COMET 过滤会怎样？），且仅在一个语言对上验证，未在低资源语言或多语言场景下测试，实验的广度略显不足。

### 6. 论文的主要结论与发现
*   **性能显著提升**：经过 DPO 优化后的模型（命名为 Amestris-1B）在 COMET-QE 指标上从 **0.703 提升至 0.747**，COMET-DA 也有明显增长。
*   **语义质量优于字面匹配**：虽然 BLEU 分数略有下降（0.157 $\rightarrow$ 0.150），但其他所有语义和字符级指标（METEOR, chrF++, TER）均有提升。这表明 DPO 牺牲了少量的字面重合度，换取了更高的翻译流畅度和语义忠实度。
*   **DPO 的稳定性**：证明了 DPO 可以作为 NMT 训练后阶段一种稳定且高效的优化手段。

### 7. 优点
*   **创新的数据生成**：巧妙地将回译任务转化为偏好对构造任务，解决了 DPO 在翻译领域缺乏负例数据的问题。
*   **高效性**：结合 LoRA 和 DPO，使得在小型语言模型（1B）上也能实现显著的质量飞跃。
*   **过滤机制**：引入肘部法则（Elbow Method）对 COMET 分数进行动态过滤，确保了训练数据的偏好信号足够强。

### 8. 不足与局限
*   **语言覆盖有限**：实验仅限于英德翻译，尚未验证该方法在语言结构差异更大的语种（如中英）上的效果。
*   **依赖“专家”**：该框架的前提是需要一个高质量的专家翻译作为起点，如果专家翻译本身存在偏差，可能会误导模型。
*   **指标权衡**：BLEU 分数的下降虽然在神经翻译研究中常见，但也反映出模型在精确词汇匹配上可能存在波动。
*   **缺乏人工评估**：虽然 COMET 与人类判断相关性高，但缺乏最终的人工盲测（Human Evaluation）来证实翻译的自然度提升。

（完）
