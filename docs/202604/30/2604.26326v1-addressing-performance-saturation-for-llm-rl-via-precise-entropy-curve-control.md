---
title: Addressing Performance Saturation for LLM RL via Precise Entropy Curve Control
title_zh: 通过精确熵曲线控制解决大语言模型强化学习中的性能饱和问题
authors: "Bolian Li, Yifan Wang, Yi Ding, Anamika Lochab, Ananth Grama, Ruqi Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26326v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 强化学习解锁LLM的复杂推理能力
tldr: "针对大语言模型强化学习中的性能饱和与熵崩溃问题，本文提出 Entrocraft 方法。该方法通过拒绝采样精确控制熵曲线，无需正则化即可实现用户自定义的熵调度。研究发现线性退火策略效果最佳。实验证明 Entrocraft 能显著提升模型泛化性、输出多样性及长期训练稳定性，使 4B 模型性能超越 8B 基准，并将训练时长延长 4 倍，pass@K 提升 50%。"
selection_source: fresh_fetch
motivation: 强化学习在提升 LLM 推理能力时常因熵崩溃导致性能饱和，而现有正则化方法在长期训练中表现不稳定且难以持续提升。
method: 提出 Entrocraft，一种通过偏置优势分布并结合拒绝采样来实现精确、可自定义熵调度控制的通用方法。
result: 该方法显著延长了有效训练时间，使 4B 模型性能超越 8B 基准，并在泛化性和输出多样性上取得大幅提升。
conclusion: 精确的熵曲线控制（尤其是线性退火策略）是克服 LLM 强化学习性能饱和、实现长期性能增益的关键。
---

## 摘要
强化学习（RL）解锁了大语言模型（LLM）的复杂推理能力。然而，大多数强化学习算法都面临性能饱和问题，阻碍了随着强化学习训练规模扩大而产生的进一步收益。这一问题可以通过熵崩塌（entropy collapse）来表征，而熵是强化学习中衡量探索的关键指标。现有的尝试试图通过正则化或裁剪来防止熵崩塌，但由此产生的熵曲线在长期训练中往往表现出不稳定性，从而阻碍了性能提升。在本文中，我们介绍了 Entrocraft，这是一种简单的拒绝采样方法，通过偏置优势分布（advantage distributions）来实现任何用户自定义的熵调度。Entrocraft 不需要目标正则化，且与优势估计器无关。在理论上，我们在极小假设下将每步熵的变化与优势分布联系起来，这解释了现有强化学习和熵保持方法的行为。Entrocraft 还支持对熵调度进行系统研究，我们发现线性退火（从高值开始并衰减到略低的目标值）表现最佳。实验表明，Entrocraft 解决了性能饱和问题，显著提高了泛化能力、输出多样性和长期训练效果。它使 4B 模型能够超越 8B 基线模型，在达到平台期前持续提升的时间延长了 4 倍，并将 pass@K 指标比基线提高了 50%。

## Abstract
Reinforcement learning (RL) has unlocked complex reasoning abilities in large language models (LLMs). However, most RL algorithms suffer from performance saturation, preventing further gains as RL training scales. This problem can be characterized by the collapse of entropy, a key diagnostic for exploration in RL. Existing attempts have tried to prevent entropy collapse through regularization or clipping, but their resulting entropy curves often exhibit instability in the long term, which hinders performance gains. In this paper, we introduce Entrocraft, a simple rejection-sampling approach that realizes any user-customized entropy schedule by biasing the advantage distributions. Entrocraft requires no objective regularization and is advantage-estimator-agnostic. Theoretically, we relate per-step entropy change to the advantage distribution under minimal assumptions, which explains the behavior of existing RL and entropy-preserving methods. Entrocraft also enables a systematic study of entropy schedules, where we find that linear annealing, which starts high and decays to a slightly lower target, performs best. Empirically, Entrocraft addresses performance saturation, significantly improving generalization, output diversity, and long-term training. It enables a 4B model to outperform an 8B baseline, sustains improvement for up to 4x longer before plateauing, and raises pass@K by 50% over the baseline.

---

## 论文详细总结（自动生成）

这是一份关于论文《Addressing Performance Saturation for LLM RL via Precise Entropy Curve Control》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注大语言模型（LLM）在强化学习（RL）训练中的**性能饱和（Performance Saturation）**现象。尽管 RL 能提升模型的推理能力，但随着训练规模（数据和算力）的扩大，模型性能往往会提前进入平台期。
*   **核心痛点**：性能饱和的根源在于**熵崩溃（Entropy Collapse）**。模型在训练中会过度收敛于少数路径，丧失探索能力。
*   **现有缺陷**：现有的熵保持技术（如熵正则化、梯度裁剪或正负样本解耦）对熵的控制过于粗糙，容易导致熵在长期训练中剧烈波动或漂移，反而破坏训练稳定性。
*   **研究目标**：实现对熵曲线的**精确、可自定义控制**，从而打破性能瓶颈。

### 2. 方法论：Entrocraft
论文提出了 **Entrocraft**，一种基于拒绝采样的熵控制框架。
*   **理论基础**：作者通过数学证明发现，在 LLM 策略下，熵的变化与样本的“优势值（Advantage）”呈负相关。正优势样本倾向于降低熵（增加确定性），负优势样本倾向于增加熵（增加多样性）。
*   **核心思想**：不修改损失函数，而是通过**动态拒绝采样**来偏置进入模型更新的优势分布。
*   **关键技术细节**：
    *   **熵监控**：实时计算当前批次的平均熵 $H$。
    *   **动态过滤**：设定目标熵区间 $(h_{low}, h_{high})$。如果当前熵过低，算法会以更高概率拒绝正优势样本，保留负优势样本以强行“增熵”；反之亦然。
    *   **熵调度（Annealing）**：借鉴学习率调度的思想，Entrocraft 允许用户自定义熵的演变曲线。研究发现，**线性退火（Linear Annealing）**策略（从高熵开始逐渐降低至目标值）在平衡探索与收敛方面效果最佳。

### 3. 实验设计
*   **任务场景**：主要集中在数学推理任务（Math Reasoning）。
*   **数据集**：使用 **Numina-Math**（440K 问题）进行训练。
*   **Benchmark**：在 MATH-500、AMC-23 以及 AIME-24/25/26 等高难度竞赛级数学数据集上进行评估。
*   **对比方法**：
    *   **基础算法**：GRPO、GSPO。
    *   **熵控制基线**：熵损失正则化（Entropy Loss）、裁剪法（Clip-Higher, Clip-Cov）、正负解耦法（W-Reinforce, EntroPIC）。

### 4. 资源与算力
*   **硬件环境**：实验在一台拥有 **8 张 NVIDIA H100 GPU** 的节点上完成。
*   **软件框架**：基于 `verl` 强化学习框架，使用 `vLLM` 进行推理加速，`FSDP2` 进行分布式训练。
*   **训练规模**：长期训练实验涵盖了高达 **400K** 个训练样本的规模。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的大规模实验，包括不同模型规模（4B、8B、14B）、不同模型家族（Qwen3、Llama-3.1）以及长达 4 倍于常规时长的长期训练对比。
*   **消融与对比**：针对不同的熵调度方案（固定目标、线性退火、余弦退火）进行了详细对比。
*   **客观性**：实验采用了标准的 mean@32 和 pass@32 指标，并展示了完整的训练动态曲线（奖励、熵、KL 散度），证明了结果的稳健性和可重复性。

### 6. 主要结论与发现
*   **打破饱和**：Entrocraft 成功解决了性能饱和问题。在标准 RL 停滞的地方，Entrocraft 仍能持续提升性能。
*   **以小博大**：使用 Entrocraft 训练的 **4B 模型** 在 AIME 等指标上超越了使用标准 GRPO 训练的 **8B 模型**。
*   **推理缩放**：显著提升了输出多样性，pass@32 指标比基线高出 50%，这意味着模型在推理侧的 Scaling Law 潜力被进一步挖掘。
*   **最佳实践**：线性退火熵曲线是目前最有效的调度方式，能兼顾训练初期的充分探索和后期的稳定收敛。

### 7. 优点
*   **简单且通用**：作为一种“插件式”方法，它不改变 RL 算法的核心逻辑（如 PPO/GRPO），易于集成。
*   **控制精确**：相比正则化，拒绝采样对优势分布的直接干预使得熵曲线几乎可以完美贴合预设目标。
*   **理论支撑**：提供了针对 LLM 策略（而非简单的表格化策略）的熵变化理论证明，解释了为何现有方法在长期训练中会失效。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在数学推理这一具有“可验证奖励（Verifiable Rewards）”的领域，在代码生成或通用对话等奖励模型（RM）驱动的任务上表现如何尚待验证。
*   **样本效率风险**：在熵极度偏离目标时，拒绝采样可能会导致大量样本被过滤，从而在特定训练阶段降低样本利用率（虽然作者声称计算成本可控）。
*   **超参数依赖**：虽然简化了熵控制，但引入了新的超参数（如目标熵的起始值和退火斜率），这可能需要针对不同规模的模型进行微调。

（完）
