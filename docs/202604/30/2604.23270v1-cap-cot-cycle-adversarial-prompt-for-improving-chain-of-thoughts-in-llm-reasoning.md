---
title: "CAP-CoT: Cycle Adversarial Prompt for Improving Chain of Thoughts in LLM Reasoning"
title_zh: CAP-CoT：用于提升大语言模型推理中思维链性能的循环对抗提示词
authors: "Shuxu Chen, Yitian Zhou, Jiaquan Zhang, Haoyu Bian, Aming Wu, Sungyoung Lee, Chaoning Zhang, Hyundong Shin"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23270v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 提高思维链推理的准确性和稳定性
tldr: 针对大语言模型在复杂推理中思维链（CoT）不稳定的问题，本文提出CAP-CoT框架。该框架通过循环对抗提示优化，利用挑战者生成针对性错误逻辑，并由反馈代理对比正确与错误链条，从而迭代优化求解器提示词。实验证明，该方法在多个基准测试中显著提升了推理准确性、稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的CoT推理在处理长步骤问题时存在不稳定性，且缺乏迭代式的对比纠错机制来提升推理链的质量。
method: 提出一种循环对抗框架，通过求解器生成答案、挑战者构造逻辑漏洞、反馈代理提供结构化反馈来双向优化提示词。
result: 在六个基准测试和四种主流模型上，CAP-CoT显著降低了推理结果的变异性，并增强了模型对提示扰动的鲁棒性。
conclusion: 通过引入任务语义层面的对抗机制，可以有效暴露并修复LLM的逻辑漏洞，从而显著提升复杂推理任务的可靠性。
---

## 摘要
思维链（CoT）提示已成为一种简单且有效的方法，用于引导大语言模型（LLM）生成逐步解决方案。然而，在处理长且多步骤的问题时，CoT 推理在多次运行中可能表现得不稳定，导致针对相同任务产生不一致的答案。大多数先前的工作集中在单次推理中改进前向推理链，而较少关注迭代式和对比式的修正。为了填补这一空白，我们提出了 CAP-CoT，这是一个循环对抗提示优化框架，旨在提高单个已部署求解器的 CoT 推理准确性和稳定性。在每个循环中，前向求解器生成候选推理链，对抗性挑战者利用针对性的错误策略构建看似合理但刻意存在缺陷的推理链，反馈代理则对比这两条链并生成步骤对齐的结构化反馈。这种反馈从两个方向闭合了优化循环，包括根据挑战者暴露的错误更新求解器提示词，以及更新挑战者提示词以便在后续循环中生成更具针对性的错误。与越狱或提示注入攻击等面向安全的对抗性提示不同，我们的对抗组件是任务语义化的，旨在暴露推理链中的逻辑漏洞。在六个基准测试和四个 LLM 主干模型上的实验表明，在两到三个对抗性提示优化循环内，CAP-CoT 能够持续降低多次运行间的变异性，同时提高推理准确性和对提示扰动的鲁棒性。

## Abstract
Chain-of-Thought (CoT) prompting has emerged as a simple and effective way to elicit step-by-step solutions from large language models (LLMs). However, CoT reasoning can be unstable across runs on long, multi-step problems, leading to inconsistent answers for unchanged task. Most prior work focuses on improving the forward reasoning chain within a single pass, with less attention to iterative and contrastive correction. To address this gap, we propose CAP-CoT, a Cycle Adversarial Prompt optimization framework designed to improve both CoT reasoning accuracy and stability of a single deployed solver. In each cycle, a forward solver generates candidate reasoning chains, an adversarial challenger constructs plausible but deliberately flawed chains using targeted error strategies, and a feedback agent contrasts the two chains and produces step-aligned structured feedback. This feedback closes the optimization loop in two directions, including updating the solver prompt based on errors exposed by the challenger, and updating the challenger prompt to generate increasingly targeted errors in subsequent cycles. Unlike safety-oriented adversarial prompting such as jailbreak or prompt-injection attacks, our adversarial component is task-semantic and aims to expose logical vulnerabilities in reasoning chains. Experiments across six benchmarks and four LLM backbones demonstrate that within two to three adversarial prompt optimization cycles, CAP-CoT consistently reduces variability across runs while improving reasoning accuracy and robustness to prompt perturbations.

---

## 论文详细总结（自动生成）

### CAP-CoT：用于提升大语言模型推理中思维链性能的循环对抗提示词

#### 1. 核心问题与整体含义（研究动机和背景）
尽管思维链（CoT）提示显著提升了 LLM 处理复杂问题的能力，但在长步骤、多阶段推理任务中，CoT 表现出极大的**不稳定性**：同一模型在多次运行中可能给出不一致的答案。此外，模型对提示词的微小扰动（如前提顺序改变、无关上下文干扰）非常敏感。
**研究动机**在于：现有的改进方法（如 Self-Consistency 或 Tree-of-Thought）大多只关注单向的前向推理优化，缺乏一种系统性的机制来主动暴露推理中的逻辑漏洞，并通过“正反对比”来强化模型的严谨性。

#### 2. 方法论：核心思想与技术细节
CAP-CoT 提出了一个**循环对抗提示优化框架**，其核心思想是通过“求解器-挑战者-反馈者”三方博弈，在提示词层面进行迭代进化。
*   **核心组件（三个角色代理）**：
    *   **求解器 (Solver, $G_S$)**：负责生成标准的前向推理链 $C_S$。
    *   **对抗挑战者 (Challenger, $G_C$)**：负责构建看似合理但逻辑有误的对抗链 $C_C$。它使用四种初始错误策略：**Jump**（跳步）、**Confusion**（概念混淆）、**Fuzzy**（模糊掩盖逻辑漏洞）、**Wrapper**（在流利表达中嵌入错误步骤）。
    *   **反馈代理 (Feedback Agent, $F$)**：对比 $C_S$ 和 $C_C$，识别 $C_S$ 的脆弱点和 $C_C$ 的有效攻击模式，生成步骤对齐的结构化反馈。
*   **优化流程**：
    1.  **生成与对抗**：求解器给出答案，挑战者针对该问题制造逻辑陷阱。
    2.  **对比诊断**：反馈代理分析两者的差异，指出求解器需要加强的逻辑校验点。
    3.  **双向更新**：利用结构化反馈提示词精炼（SFPR）技术，同时更新求解器的提示词（使其更严谨）和挑战者的提示词（使其攻击更具针对性）。
    4.  **推理部署**：经过 2-3 轮循环后，仅保留优化后的求解器提示词用于最终推理。

#### 3. 实验设计
*   **数据集**：涵盖了数学推理（MATH, GSM8K）、逻辑与算法（BBH）、常识知识（MMLU-CF）、多跳问答（HotpotQA）以及长文本理解（LongBench）共 6 个基准测试。
*   **主干模型 (Backbones)**：GPT-4o-mini, Qwen-turbo, DeepSeek-V3, GPT-4o。
*   **对比方法**：
    *   基础 CoT 及其变体：CoT, CoT-SC, CCoT。
    *   搜索与结构化方法：ToT, GoT, FoT, AoT。
    *   反馈与辩论方法：Self-Refine, PromptAgent, AFlow, MAD, ECON。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及具体的 GPU 型号或训练时长**。
*   **原因分析**：由于 CAP-CoT 属于**提示词级别（Prompt-level）的优化**，不涉及模型参数的梯度下降训练。实验主要通过 LangChain 框架调用主流 LLM 的官方 API 完成。其成本主要体现在 API 的 Token 消耗上。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同架构的主干模型上进行了全量测试，并针对 6 个数据集进行了对比。
*   **消融实验**：进行了组件有效性分析（验证挑战者和反馈者的必要性）、优化轮次分析（1-5 轮的效果演变）、温度敏感性分析（0.0-1.0 温度下的稳定性）。
*   **客观性**：实验结果取多次运行的平均值，并提供了标准差（Variation）来衡量稳定性。对比方法涵盖了从简单的采样到复杂的树/图搜索算法，实验设计较为全面、公平。

#### 6. 主要结论与发现
*   **性能提升**：CAP-CoT 在所有基准测试中均优于现有方法，尤其在 MATH 和 LongBench 等复杂任务上提升显著。
*   **稳定性增强**：随着优化轮次的增加，模型在不同采样温度下的准确率波动显著降低，证明了对抗训练能使提示词更具鲁棒性。
*   **收敛快**：通常只需 2-3 轮循环，性能即可达到饱和，具有较高的优化效率。
*   **成本平衡**：虽然比单次 CoT 昂贵，但其 Token 消耗远低于 ToT、GoT 等重型搜索方法，且推理时只需单次调用。

#### 7. 优点
*   **任务语义对抗**：不同于传统的安全类对抗（如越狱），该方法关注逻辑层面的“陷阱”，对提升推理质量更有针对性。
*   **双向进化**：不仅优化求解器，还通过反馈让挑战者变强，从而不断逼出求解器的潜在错误。
*   **即插即用**：不需要微调模型参数，通过纯提示词工程即可在不同模型间迁移。

#### 8. 不足与局限
*   **API 依赖**：优化过程依赖多次 API 调用，对于极大规模数据集的实时优化可能存在成本压力。
*   **提示词长度限制**：随着反馈信息的累积，提示词可能变得非常冗长，受限于模型的上下文窗口或导致推理延迟增加。
*   **策略覆盖面**：虽然提出了四种错误分类，但对于某些极度垂直或新型的逻辑错误，预设的分类可能无法完全覆盖，依赖反馈代理的泛化能力。

（完）
