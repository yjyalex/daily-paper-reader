---
title: "DORA: A Scalable Asynchronous Reinforcement Learning System for Language Model Training"
title_zh: DORA：一种用于语言模型训练的可扩展异步强化学习系统
authors: "Tianhao Hu, Xiangcheng Liu, Youshao Xiao, Yang Zheng, Xuan Huang, Jinrui Ding, Yufei Zhang, Tao Liang, Hongyu Zang, Quan Chen, Yueqing Sun, Wenjie Shi, Chao Zhang, Wei Wang, Qi Gu, Yerui Sun, Yucheng Xie, Xunliang Cai"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26256v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大语言模型训练的可扩展异步强化学习系统
tldr: 针对大语言模型强化学习训练中生成阶段耗时占比高且受长尾轨迹限制的瓶颈，本文提出 DORA 系统。通过算法与系统协同设计，引入多版本流式生成范式，在消除流水线气泡的同时确保了策略一致性、数据完整性和陈旧度限制。实验表明，DORA 在吞吐量上比现有系统提升 2-3 倍，在大规模工业应用中加速 2-4 倍，且不影响模型收敛。
selection_source: fresh_fetch
motivation: 强化学习训练中生成阶段的长尾轨迹导致严重的流水线气泡，而现有异步训练方法难以在效率与算法正确性之间取得平衡。
method: 提出 DORA 系统，通过多版本流式生成范式同时维护多个策略版本，实现计算资源的充分利用并满足异步训练的收敛约束。
result: 在开源基准测试中吞吐量提升 2-3 倍，在万卡规模工业场景下加速 2-4 倍，并助力开发出高性能的推理模型。
conclusion: DORA 通过创新的异步架构有效解决了 LLM 强化学习的扩展性难题，是实现高效、大规模模型后训练的关键技术。
---

## 摘要
强化学习（RL）已成为大语言模型（LLM）后训练的关键范式，然而，占据总步长耗时 50-80% 的采样（rollout）阶段受到生成偏斜的限制：对模型性能至关重要的长尾轨迹阻塞了整个训练流水线。异步训练通过将生成与训练重叠提供了一种自然的补救方案，但在效率与算法正确性之间引入了根本性的矛盾。我们确定了异步训练中保持收敛的三个约束：轨迹内策略一致性、数据完整性和有界陈旧度。现有方法未能从本质上解决长尾轨迹问题（混合专家模型（MoE）的不平衡特性进一步加剧了该问题），或者偏离了标准的 RL 训练公式，从而阻碍了模型收敛。因此，我们提出了 DORA（用于异步采样的动态编排），通过算法-系统协同设计来应对这一挑战。DORA 引入了多版本流式采样，这是一种新型异步范式，可同时维护多个策略版本，在不违反算法约束的情况下实现了完全消除气泡（bubble elimination）。实验结果表明，我们的 DORA 系统在吞吐量上实现了显著提升——在开源基准测试中比现有最先进系统高出 2-3 倍，且不影响收敛性。此外，在拥有数万个加速器的大规模工业应用中，DORA 在各种场景下相比同步训练将 RL 训练加速了 2-4 倍。由此产生的开源模型 LongCat-Flash-Thinking 在复杂推理基准测试中表现出极具竞争力的性能，达到了最先进 LLM 的水平。

## Abstract
Reinforcement learning (RL) has become a critical paradigm for LLM post-training, yet the rollout phase -- accounting for 50--80% of total step time -- is bottlenecked by skewed generation: long-tailed trajectories indispensable for model performance block the entire training pipeline. Asynchronous training offers a natural remedy by overlapping generation with training, but introduces a fundamental tension between efficiency and algorithmic correctness. We identify three constraints in asynchronous training to preserve convergence: intra-trajectory policy consistency, data integrity, and bounded staleness. Existing approaches fail to intrinsically address the long-tailed trajectory problem, which is further exacerbated by the imbalance characteristic of Mix-of-Experts models, or deviate from the standard RL training formulation, thereby hindering model convergence. Therefore, we propose DORA (Dynamic ORchestration for Asynchronous Rollout), which addresses this challenge through algorithm-system co-design. DORA introduces multi-version streaming rollout, a novel asynchronous paradigm that maintains multiple policy versions concurrently -- simultaneously achieving full bubble elimination without compromising algorithmic constraints. Experimental results demonstrate that our DORA system achieves substantial improvements in throughput -- up to 2--3 times higher than state-of-the-art systems on open-source benchmarks -- without compromising convergence. Furthermore, in large-scale industrial applications with tens of thousands of accelerators, DORA accelerates RL training by 2--4 times compared to synchronous training across various scenarios. The resultant open-source models, LongCat-Flash-Thinking, exhibit competitive performance on complex reasoning benchmarks, matching the capability of most advanced LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《DORA: A Scalable Asynchronous Reinforcement Learning System for Language Model Training》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 LLM 的强化学习（RL）后训练中，**采样阶段（Rollout）**占据了总训练时间的 50%–80%。由于生成长度分布极度不均（长尾分布），同步训练模式下整个批次会被最长的响应序列阻塞，导致严重的计算资源浪费（气泡）。
论文指出，现有的异步训练方法在提升效率的同时，往往难以兼顾算法的收敛性。作者提出了异步训练必须满足的三个核心约束：
*   **C1：轨迹内策略一致性**（同一条轨迹必须由同一个模型版本生成）。
*   **C2：数据完整性**（不能为了效率丢弃对推理能力至关重要的长尾轨迹）。
*   **C3：有界陈旧度**（生成策略与训练策略的版本差距必须在可控范围内）。

### 2. 核心方法论：DORA 系统设计
DORA（Dynamic ORchestration for Asynchronous Rollout）通过**算法-系统协同设计**解决了上述矛盾：
*   **多版本流式训练（Multi-version Streaming Training）**：允许采样实例同时维护多个策略版本。当训练进入下一轮时，旧版本的长尾请求继续生成，新请求则使用新版本，从而消除同步屏障。
*   **动态资源编排（Dynamic Orchestration）**：由中心化的编排器根据各版本的实时负载动态重新划分数据并行（DP）组，并执行请求迁移，解决资源碎片化问题。
*   **零重预填充 KV-Cache 复用（Zero Re-prefill KV-Cache Reuse）**：利用 C1 约束带来的数学特性——同一版本的模型在不同实例上的 KV-Cache 是等价的。在迁移请求时，直接传输 KV-Cache 而无需重新计算 Prefill 阶段，这对长文本和 MoE 架构极其高效。
*   **滑动窗口陈旧度控制**：通过确定性的窗口机制强制执行 C3 约束，确保模型收敛。

### 3. 实验设计
*   **模型与架构**：评估了 Dense 架构（Qwen2.5-32B）和 MoE 架构（LongCat-Flash）。
*   **数据集**：开源数据集 DAPO-Math-17K（最大输出 30K tokens）；工业生产环境数据集（涵盖数学、工具集成推理 TIR、Agent 任务等）。
*   **对比基线（Benchmarks）**：
    1.  **Synchronous**：标准的同步训练。
    2.  **One-step off-policy**：重叠流水线阶段但未消除采样气泡的异步方法。
    3.  **Partial rollout**：将长轨迹切断分段生成的异步方法（违反 C1 约束）。

### 4. 资源与算力
*   **测试环境**：16 个节点，共 **128 块 NVIDIA H800 GPU**（400GB/s NVLink，400Gbps 网络）。
*   **生产环境**：在包含**数万个加速器**的大规模集群上进行了部署验证。
*   **具体配置**：在 MoE 评估中使用了 4,096 个加速器处理最大 64K 长度的响应。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 64 GPU 到 128 GPU 的扩展性实验，以及万卡规模的生产环境测试。
*   **维度**：包括端到端吞吐量、采样阶段加速比、模型收敛曲线（Reward 随 Step 变化）、系统开销分析（迁移耗时占比）等。
*   **评价**：实验设计较为充分，不仅在受控的开源基准上验证了性能，还通过生产环境的长期运行证明了系统的鲁棒性和实际价值。

### 6. 主要结论与发现
*   **效率提升**：相比同步训练，DORA 在端到端吞吐量上提升了 **2.12 倍**，在采样阶段实现了高达 **8.2 倍** 的加速。
*   **收敛性**：DORA 在不同陈旧度设置（K=1, K=3）下均能保持与同步训练一致的收敛趋势，证明了 C1-C3 约束的有效性。
*   **MoE 优势**：在 MoE 架构和长文本场景下，KV-Cache 复用技术显著降低了重预填充的开销，加速比在生产环境下达到 3.6x–6.2x。

### 7. 优点
*   **协同设计**：将算法约束（C1）转化为系统优化机会（KV-Cache 复用），构思巧妙。
*   **通用性**：支持标准 RL 公式，无需像 Partial Rollout 那样修改损失函数或进行梯度掩码。
*   **工业级验证**：在万卡规模下证明了系统的可扩展性，并助力开发了 LongCat-Flash-Thinking 等高性能模型。

### 8. 不足与局限
*   **参数依赖**：陈旧度阈值 $K$ 仍需人工根据经验配置，缺乏自适应调整机制。
*   **对比范围**：虽然对比了主流异步范式，但未直接与最新的开源框架（如 veRL 或 AReaL）在相同环境下进行直接 Benchmark 对比。
*   **硬件依赖**：KV-Cache 的高效迁移依赖于极高的网络带宽，在低带宽环境下性能增益可能会受限。

（完）
