---
title: "Beyond Semantic Manipulation: Token-Space Attacks on Reward Models"
title_zh: 超越语义操纵：针对奖励模型的 Token 空间攻击
authors: "Yuheng Zhang, Mingyue Huo, Minghao Zhu, Mengxue Zhang, Nan Jiang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02686v1"
tags: ["query:token"]
score: 9.0
evidence: 在奖励模型的Token空间直接进行对抗性优化
tldr: 本研究针对RLHF中奖励模型（RM）的奖励作弊问题，提出了一种名为TOMPA的新型攻击框架。不同于以往侧重语义层面的攻击，TOMPA直接在Token空间进行对抗优化，绕过了解码-重分词环节。通过黑盒标量反馈，TOMPA能自动发现高奖励的非语言Token模式。实验显示，该方法在多个SOTA模型上实现了极高的奖励分数，甚至远超GPT-5参考答案，但生成的文本完全不可读，揭示了当前RM在非语义层面的严重漏洞。
selection_source: fresh_fetch
motivation: 现有的奖励模型攻击多局限于语义层面，忽略了直接在Token空间进行非语言模式攻击的可能性。
method: 提出TOMPA框架，通过绕过标准解码接口，利用黑盒反馈直接在Token序列上进行对抗优化。
result: "在Skywork等模型上，TOMPA生成的无意义文本奖励接近GPT-5的两倍，且在98%的提示词下表现更优。"
conclusion: 奖励模型存在严重的非语义漏洞，仅靠语义对齐无法完全防御Token空间的奖励作弊。
---

## 摘要
奖励模型 (RM) 在基于人类反馈的强化学习 (RLHF) 中被广泛用作优化目标，但它们仍然容易受到奖励作弊 (reward hacking) 的影响。现有的攻击主要在语义空间内运行，通过构建人类可读的对抗性输出来利用 RM 的偏见。在这项工作中，我们引入了一种根本不同的范式：Token 映射扰动攻击 (TOMPA)，这是一个直接在 Token 空间执行对抗性优化的框架。通过绕过策略与奖励模型之间标准的“解码-重新分词” (decode-re-tokenize) 接口，TOMPA 使攻击策略能够针对原始 Token 序列而非连贯的自然语言进行优化。仅利用黑盒标量反馈，TOMPA 就能自动发现非语言性的 Token 模式，从而在多个最先进的 RM 上诱导出极高的奖励。具体而言，在针对 Skywork-Reward-V2-Llama-3.1-8B 时，TOMPA 获得的奖励几乎是 GPT-5 参考答案的两倍，并在 98.0% 的提示词上优于后者。尽管得分很高，但生成的输出却退化为无意义的文本，这表明 RM 可以在语义范畴之外被系统性地利用，并揭示了当前 RLHF 流程中的一个关键漏洞。

## Abstract
Reward models (RMs) are widely used as optimization targets in reinforcement learning from human feedback (RLHF), yet they remain vulnerable to reward hacking. Existing attacks mainly operate within the semantic space, constructing human-readable adversarial outputs that exploit RM biases. In this work, we introduce a fundamentally different paradigm: Token Mapping Perturbation Attack (TOMPA), a framework that performs adversarial optimization directly in token space. By bypassing the standard decode-re-tokenize interface between the policy and the reward model, TOMPA enables the attack policy to optimize over raw token sequences rather than coherent natural language. Using only black-box scalar feedback, TOMPA automatically discovers non-linguistic token patterns that elicit extremely high rewards across multiple state-of-the-art RMs. Specifically, when targeting Skywork-Reward-V2-Llama-3.1-8B, TOMPA nearly doubles the reward of GPT-5 reference answers and outperforms them on 98.0% of prompts. Despite these high scores, the generated outputs degenerate into nonsensical text, revealing that RMs can be systematically exploited beyond the semantic regime and exposing a critical vulnerability in current RLHF pipelines.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Semantic Manipulation: Token-Space Attacks on Reward Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：探讨奖励模型（Reward Models, RMs）在非语义空间（Token 空间）的脆弱性。
*   **研究动机**：在 RLHF 流程中，RM 是优化目标，但容易遭受“奖励作弊”（Reward Hacking）。现有的攻击手段（如增加长度、改变语气、提示词注入）大多局限于人类可读的**语义空间**。作者试图研究：如果绕过自然语言的限制，直接在原始 Token 序列上进行优化，是否能发现 RM 更深层的系统性漏洞。
*   **整体含义**：揭示了当前最先进的 RM 存在严重的非语言性漏洞，即某些完全无意义的 Token 组合能骗取远高于高质量人类回答的奖励分数。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出 **TOMPA (Token Mapping Perturbation Attack)** 框架。该框架通过扰动策略模型与 RM 之间的接口，使策略模型直接输出 Token ID 并输入给 RM，从而跳过了“解码为文本再重新分词”的标准步骤。
*   **关键技术细节**：
    *   **Token 映射扰动**：定义映射函数 $\Phi$，将策略模型的 Token ID 直接映射到 RM 的词表空间。当两个模型使用不同分词器时，这种映射会产生语义错位。
    *   **黑盒优化**：攻击者只需获取 RM 输出的标量奖励值，无需访问其梯度或内部参数。
    *   **算法流程**：
        1.  从数据集采样 Prompt。
        2.  攻击策略 $\pi$ 生成 Token 序列。
        3.  通过 $\Phi$ 转换序列并输入 RM 获取奖励 $R$。
        4.  使用 **GRPO (Group Relative Policy Optimization)** 算法，根据组内相对奖励优势更新策略，最小化与参考策略的 KL 散度。

### 3. 实验设计
*   **数据集**：
    *   **训练**：从 WildChat 中随机抽取 10,000 个真实用户对话 Prompt。
    *   **评估**：使用 Novelty-Bench 的 NB-curated 子集（100 个 Prompt），涵盖随机性、事实知识、创意写作和主观性四类。
*   **实验场景**：
    *   场景 A：Llama-3.1-8B 策略攻击 Skywork-Reward-V2-Qwen3-8B。
    *   场景 B：Qwen3-8B 策略攻击 Skywork-Reward-V2-Llama-3.1-8B（该 RM 在 RewardBench2 排名第一）。
*   **对比基准 (Benchmark)**：
    *   **Gold Answer**：由 GPT-5 生成的高质量参考答案。
    *   **Random OOD**：从词表中随机采样 Token ID 组成的序列。

### 4. 资源与算力
*   **框架**：基于 `verl` 分布式强化学习框架实现。
*   **训练参数**：10 个 Epoch，Batch Size 为 64，每个 Prompt 生成 8 个 Rollout，学习率 $10^{-6}$。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对两个目前最顶尖的开源奖励模型进行了完整的攻击实验，并包含了训练曲线分析和推理时消融实验。
*   **充分性与客观性**：
    *   实验对比了随机分布（Random OOD），证明了高分是“优化”出来的，而非 RM 对所有分布外数据都给高分。
    *   进行了**长度消融实验**，分析了奖励随 Token 长度变化的非线性趋势。
    *   使用了跨模型的交叉攻击（Llama 攻 Qwen，Qwen 攻 Llama），增强了结论的普适性。

### 6. 主要结论与发现
*   **极高的攻击成功率**：TOMPA 在 98% 的测试案例中击败了 GPT-5 的答案。在 Llama-3.1-8B RM 上，攻击序列的平均得分（+33.64）几乎是 GPT-5（+17.48）的两倍。
*   **输出退化**：高分序列在视觉上完全是“乱码”，包含重复的跨语言片段、代码残片和特殊占位符（如 `<|reserved_247|>`）。
*   **长度依赖性**：RM 对这些对抗模式的奖励并非线性增加，而是在序列接近最大长度（如 2048 tokens）时出现突然的指数级飙升。
*   **RM 漏洞本质**：RM 并非简单的“长度偏见”，而是对特定的、非语言的 Token 组合存在严重的表征过拟合。

### 7. 优点
*   **范式创新**：首次将 RLHF 的奖励作弊研究从“语义操纵”扩展到了“Token 空间优化”。
*   **自动化程度高**：无需人工设计对抗模板，通过 RL 自动挖掘 RM 的盲区。
*   **实战意义强**：证明了即使是 RewardBench 排名第一的模型，在 Token 空间也极其脆弱。

### 8. 不足与局限
*   **隐蔽性差**：由于生成的序列是乱码，这种攻击在有人类参与的环节（如人工审核）中极易被发现。
*   **防御探索不足**：论文重点在于“破”，对于如何“立”（如通过对抗训练修复此类漏洞）仅在未来工作中提及，未给出具体方案。
*   **算力细节缺失**：缺乏具体的硬件消耗数据，不便于其他研究者评估复现成本。

（完）
