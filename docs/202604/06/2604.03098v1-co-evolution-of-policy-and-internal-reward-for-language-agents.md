---
title: Co-Evolution of Policy and Internal Reward for Language Agents
title_zh: 语言智能体策略与内部奖励的协同演化
authors: "Xinyu Wang, Hanwei Wu, Jingwei Song, Shuyuan Zhang, Jiayi Zhang, Fanqi Kong, Tung Sum Thomas Kwok, Xiao-Wen Chang, Yuyu Luo, Chenglin Wu, Bang Liu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03098v1"
tags: ["query:token"]
score: 9.0
evidence: 用于密集策略优化的步级内部奖励
tldr: 针对大语言模型智能体在长程任务中面临的稀疏奖励挑战，本文提出Self-Guide框架。该方法通过生成内部奖励信号，在推理阶段引导动作选择，在训练阶段提供步级监督。通过策略与内部奖励的协同演化，利用GRPO算法优化，显著提升了智能体在多个基准测试中的表现，证明了自我生成奖励对提升智能体性能的有效性。
selection_source: fresh_fetch
motivation: 旨在解决长程任务中环境奖励稀疏且延迟的问题，以及现有奖励模型与策略改进脱节的局限。
method: 提出Self-Guide机制，将自生成的引导信号在推理时用于动作导航，在训练时转化为步级内部奖励进行协同优化。
result: "在三个基准测试中，推理引导和联合演化策略相比仅使用环境奖励的基线取得了显著提升（约8%）。"
conclusion: 研究表明语言智能体可以通过在交互过程中学习生成并精炼内部奖励，实现策略与奖励机制的共同进化。
---

## 摘要
大语言模型（LLM）智能体通过与环境交互进行学习，但长程训练仍受限于稀疏且延迟的奖励这一根本瓶颈。现有方法通常通过事后信用分配（post-hoc credit assignment）或外部奖励模型来应对这一挑战，但这些方法在推理时提供的指导有限，且往往将奖励改进与策略改进分离开来。我们提出了 Self-Guide，这是一种为语言智能体设计的自生成内部奖励，它同时支持推理时的引导和训练时的监督。具体而言，智能体在推理过程中将 Self-Guide 作为简短的自我引导信号来引导下一步动作，并在训练过程中将同一信号转化为步级（step-level）内部奖励，以实现更密集的策略优化。这创建了一个协同演化循环：更好的策略产生更好的引导，而更好的引导作为内部奖励进一步改进策略。在三个智能体基准测试中，推理时的自我引导已经产生了明显的收益，而通过 GRPO 联合演化策略和内部奖励，相比仅使用环境奖励训练的基准模型，性能进一步提升了 8%。总的来说，我们的结果表明，语言智能体不仅可以通过积累更多经验来提升，还可以通过在行动和学习过程中学习生成并优化自身的内部奖励来改进。

## Abstract
Large language model (LLM) agents learn by interacting with environments, but long-horizon training remains fundamentally bottlenecked by sparse and delayed rewards. Existing methods typically address this challenge through post-hoc credit assignment or external reward models, which provide limited guidance at inference time and often separate reward improvement from policy improvement. We propose Self-Guide, a self-generated internal reward for language agents that supports both inference-time guidance and training-time supervision. Specifically, the agent uses Self-Guide as a short self-guidance signal to steer the next action during inference, and converts the same signal into step-level internal reward for denser policy optimization during training. This creates a co-evolving loop: better policy produces better guidance, and better guidance further improves policy as internal reward. Across three agent benchmarks, inference-time self-guidance already yields clear gains, while jointly evolving policy and internal reward with GRPO brings further improvements (8\%) over baselines trained solely with environment reward. Overall, our results suggest that language agents can improve not only by collecting more experience, but also by learning to generate and refine their own internal reward during acting and learning.

---

## 论文详细总结（自动生成）

以下是对论文《Co-Evolution of Policy and Internal Reward for Language Agents》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在处理长程交互任务（如网页导航、科学实验）时，面临**奖励稀疏且延迟**的根本挑战。通常只有在任务结束时才能获得反馈，导致难以确定中间步骤的贡献（信用分配问题）。
*   **研究动机**：
    *   现有方法（如事后信用分配或外部奖励模型）往往将奖励的改进与策略的改进分离，且在推理阶段缺乏实时引导。
    *   人类在行动时会产生内部自我引导，并根据自我判断进行改进。受此启发，作者希望构建一种让智能体在交互过程中自发生成、并随策略同步演化的内部奖励机制。

### 2. 方法论：Self-Guide 框架
核心思想是让智能体生成一段简短的**言语自我引导（Self-Guidance）**，该信号在推理时用于导航，在训练时转化为内部奖励。

*   **核心流程**：
    1.  **推理阶段（自我引导）**：在每个步骤 $t$，智能体先生成一个言语评估 $z_t$（判断当前进度、是否偏离目标），然后基于历史、观察和 $z_t$ 共同决定下一步动作 $a_t$。
    2.  **训练阶段（内部奖励）**：将 $z_t$ 映射为标量奖励 $r_{sg}$（如：正面评估为 +0.1，中性为 0，负面为 -0.1）。
    3.  **协同演化（Co-Evolution）**：通过单一的 GRPO（群组相对策略优化）目标函数，同时优化策略生成更好的引导，以及基于引导做出更好的决策。
*   **关键技术：阶段性信任调度（Stage-Wise Trust Schedule）**：
    为了解决“训练初期自我评估不可靠”的冷启动问题，引入了**梯形权重函数 $\lambda(u)$**：
    *   **阶段 I（热身）**：仅使用引导进行推理，不作为奖励（$\lambda=0$）。
    *   **阶段 II（激活）**：逐渐增加内部奖励的权重。
    *   **阶段 III（全量）**：内部奖励与环境奖励共同作用。
    *   **阶段 IV（退火）**：后期逐渐降低内部奖励权重，确保最终策略对齐真实环境目标。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：结构化的家庭环境导航与交互任务。
    *   **ScienceWorld**：复杂的多步科学推理与实验任务。
    *   **WebShop**：具有噪声的真实网页购物交互任务。
*   **基准模型（Backbones）**：Qwen3-1.7B、Qwen3-4B、Qwen2.5-7B-Instruct。
*   **对比方法**：
    *   **提示词方法**：ReAct、Reflexion、ReFlAct。
    *   **强化学习基线**：标准的 GRPO 算法（仅依赖环境奖励）。
    *   **消融变体**：仅推理引导（w/ SG）、推理引导+内部奖励（w/ SG & GR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `verl-agent` 强化学习框架，训练步数设定为 100 步，每步涉及多个环境交互的 Rollout。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个不同性质的基准测试、3 种不同规模的模型，并进行了详细的消融实验。
*   **充分性**：
    *   **对比充分**：不仅对比了传统的 Prompting 方法，还对比了最先进的 RL 算法（GRPO、DAPO）。
    *   **消融深入**：针对信任调度的介入时机、退火必要性以及“离线蒸馏 vs. 在线演化”进行了对比实验。
    *   **错误分析**：提供了详细的失败案例分类分析（如循环动作、冗余探索等），证明了方法在行为层面的改进。
*   **客观性**：实验结果显示在不同模型和任务上均有稳定提升，具有较强的说服力。

### 6. 主要结论与发现
*   **协同演化有效性**：Self-Guide 在三个基准测试中均显著优于仅使用环境奖励的 GRPO，平均性能提升约 8%。
*   **推理引导的即时收益**：即使不进行 RL 训练，仅在推理时加入自我引导也能在结构化任务（如 ALFWorld）中获得提升。
*   **在线演化的必要性**：实验证明，从大模型离线蒸馏的引导信号在强化学习中表现不稳定，策略与奖励必须在智能体自身的分布下**在线协同演化**。
*   **信任调度的关键性**：过早引入不可靠的内部奖励会导致训练崩溃，阶段性引入和后期退火是稳定训练的核心。

### 7. 优点与亮点
*   **闭环自进化**：无需额外的外部奖励模型（Reward Model）或人工标注的中间过程监督，降低了训练成本。
*   **双重用途**：同一个信号 $z_t$ 既解决了推理时的实时纠偏，又解决了训练时的密集监督。
*   **算法兼容性**：该机制作为一种奖励塑造（Reward Shaping）方法，可无缝集成到 GRPO、DAPO 等多种 RL 算法中。

### 8. 不足与局限
*   **奖励映射过于简单**：目前将言语引导映射为标量（+0.1/0/-0.1）的方式较为粗糙，可能无法捕捉复杂的语义反馈。
*   **超参数敏感性**：梯形调度的四个阶段切换点（如第 40 步、70 步等）可能需要针对不同任务进行手动微调，缺乏自适应性。
*   **计算开销**：由于每个动作前都要生成一段 $z_t$，推理和训练时的 Token 消耗量会有所增加。
*   **环境覆盖**：虽然涵盖了三个基准，但尚未在更具开放性的具身智能或真实世界复杂任务中验证。

（完）
