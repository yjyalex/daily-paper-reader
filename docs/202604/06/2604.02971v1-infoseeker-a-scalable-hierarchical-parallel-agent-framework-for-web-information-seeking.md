---
title: "InfoSeeker: A Scalable Hierarchical Parallel Agent Framework for Web Information Seeking"
title_zh: InfoSeeker：一种用于网络信息检索的可扩展层级化并行智能体框架
authors: "Ka Yiu Lee, Yuxuan Huang, Zhiyuan He, Huichi Zhou, Weilin Luo, Kun Shao, Meng Fang, Jun Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02971v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于信息综合的分层并行智能体框架
tldr: InfoSeeker是一个针对大规模网络信息搜索的分层并行智能体框架。针对现有系统在处理海量异构证据时面临的上下文饱和、错误传播和高延迟等挑战，该框架基于近分解性原则，构建了由Host、Manager和Worker组成的三层架构。通过Manager层的聚合与反思机制实现上下文隔离，并利用Worker层的并行化显著提升效率。实验表明，该框架在保证搜索准确性的同时，实现了3-5倍的加速。
selection_source: fresh_fetch
motivation: 现有智能体搜索系统在处理大规模信息合成时，容易出现上下文饱和、错误传播及端到端延迟过高的问题。
method: 提出一种基于近分解性原则的分层架构，通过Host进行策略规划，Manager负责聚合反思与上下文隔离，Worker执行并行搜索。
result: 在WideSearch-en和BrowseComp-zh基准测试中，该框架实现了3-5倍的推理加速，并显著提升了搜索成功率和准确性。
conclusion: InfoSeeker通过分层并行设计有效解决了大规模信息搜索中的效率与可靠性瓶颈，为数据密集型智能体任务提供了可扩展的解决方案。
---

## 摘要
近期，基于智能体的搜索系统通过强调深度的多步推理取得了显著进展。然而，这种关注点往往忽略了大规模信息综合带来的挑战，即智能体必须聚合来自多个来源的大量异构证据。因此，大多数现有的语言大模型智能体系统在数据密集型场景下都面临严重的局限性，包括上下文饱和、级联错误传播以及高昂的端到端延迟。为了应对这些挑战，我们提出了 InfoSeeker，这是一个基于近可分解性（near-decomposability）原则的层级化框架，包含一个负责策略的 Host（主机）、多个 Manager（管理器）和并行的 Worker（工作器）。通过在 Manager 层利用聚合和反思机制，我们的框架实施了严格的上下文隔离，以防止饱和与错误传播。同时，Worker 层的并行性加速了整体任务的执行速度，缓解了显著的延迟问题。我们在两个互补的基准测试上的评估证明了该框架的高效性（提速 3-5 倍）和有效性，在 WideSearch-en 上实现了 8.4% 的成功率，在 BrowseComp-zh 上实现了 52.9% 的准确率。代码已发布于 https://github.com/agent-on-the-fly/InfoSeeker。

## Abstract
Recent agentic search systems have made substantial progress by emphasising deep, multi-step reasoning. However, this focus often overlooks the challenges of wide-scale information synthesis, where agents must aggregate large volumes of heterogeneous evidence across many sources. As a result, most existing large language model agent systems face severe limitations in data-intensive settings, including context saturation, cascading error propagation, and high end-to-end latency. To address these challenges, we present \framework, a hierarchical framework based on principle of near-decomposability, containing a strategic \textit{Host}, multiple \textit{Managers} and parallel \textit{Workers}. By leveraging aggregation and reflection mechanisms at the Manager layer, our framework enforces strict context isolation to prevent saturation and error propagation. Simultaneously, the parallelism in worker layer accelerates the speed of overall task execution, mitigating the significant latency. Our evaluation on two complementary benchmarks demonstrates both efficiency ($ 3-5 \times$ speed-up) and effectiveness, achieving a $8.4\%$ success rate on WideSearch-en and $52.9\%$ accuracy on BrowseComp-zh. The code is released at https://github.com/agent-on-the-fly/InfoSeeker

---

## 论文详细总结（自动生成）

以下是对论文《InfoSeeker: A Scalable Hierarchical Parallel Agent Framework for Web Information Seeking》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的智能体搜索系统（如 Deep Research 类系统）虽然在深度推理上取得了进展，但在**大规模信息综合（Wide-scale Synthesis）**方面存在严重缺陷。
*   **研究动机**：当任务需要从数十个网页中聚合异构证据时，传统系统面临三大瓶颈：
    1.  **上下文饱和**：大量原始网页数据迅速填满 LLM 的上下文窗口。
    2.  **错误级联传播**：顺序执行的 ReAct 循环中，早期的微小错误会随时间放大。
    3.  **高延迟**：顺序处理导致端到端响应时间不可接受。
*   **整体含义**：论文旨在通过一种层级化的并行架构，解决数据密集型搜索任务中的效率与可靠性平衡问题。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：基于赫伯特·西蒙的**“近分解性”（Near-decomposability）**原则，将复杂系统划分为半自治模块。子系统独立处理细节，仅通过高层摘要进行协调。
*   **三层架构设计**：
    *   **Host（主机层）**：负责高层战略推理和规划。它只维护压缩后的全局状态（步骤-响应对），不接触工具调用的细节，从而避免上下文爆炸。
    *   **Managers（管理层）**：领域特定（如搜索、浏览器、代码、文件系统）。负责将 Host 的指令分解为可并行的子任务，执行质量验证（Reflection），并将 Worker 的结果聚合（Aggregation）为简洁摘要。
    *   **Workers（工作层）**：执行原子化的工具交互。利用 **Model Context Protocol (MCP)** 实现上下文隔离，多个 Worker 并行操作。
*   **算法流程**：
    1.  Host 生成高层步骤 $q_t$ 并指派给特定 Manager。
    2.  Manager 将 $q_t$ 分解为 $P_t$ 个并行子任务。
    3.  Worker 并行执行子任务并返回结果。
    4.  Manager 进行反思与验证，必要时重试，最后聚合结果 $y_t$ 返回给 Host。
    5.  Host 更新上下文并决定下一步。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **WideSearch (EN/ZH)**：侧重于“广度”，要求智能体填充包含大量实体和属性的复杂表格。
    *   **BrowseComp-zh**：侧重于“深度”和中文网络环境下的多跳推理与导航。
*   **对比方法**：
    *   **前沿模型**：OpenAI o3-high, Claude 3.7/4 Sonnet, DeepSeek-R1, Gemini 2.0 Pro。
    *   **商业端到端系统**：OpenAI Deep Research, Gemini Deep Research。
    *   **开源框架**：WebSailor, BrowseMaster, MiroThinker 等。

### 4. 资源与算力
*   **模型使用**：Host 和 Manager 使用 **GPT-5.1**（高性能模型），Worker 使用 **GPT-5-mini**（高吞吐量、低成本模型）。
*   **算力说明**：论文**未明确说明**具体的 GPU 训练算力（如型号、数量），因为该研究侧重于推理侧的框架设计，而非模型训练。实验主要通过 API 调用完成。
*   **成本分析**：WideSearch 任务平均成本约 2.0 美元，BrowseComp-zh 约 1.0 美元。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了全面评估，涵盖了成功率、行级 F1、项级 F1 等多维度指标。
*   **消融实验**：针对 **Worker 数量**进行了消融研究（从 1 个扩展到 17 个），验证了并行规模对延迟的影响。
*   **对比公平性**：通过使用相同的工具访问权限和相同的骨干模型（GPT-5.1/mini）构建了单智能体基线，证明了性能提升源于“架构设计”而非单纯的模型能力。实验设计较为客观、充分。

### 6. 主要结论与发现
*   **性能领先**：在 WideSearch 上比最强基线提升了 64% 的成功率；在 BrowseComp-zh 上达到 52.9% 的准确率，超过了 OpenAI Deep Research。
*   **显著加速**：相比顺序执行，InfoSeeker 实现了 **3-5 倍的推理加速**。
*   **上下文管理**：层级隔离机制有效解决了长程规划中的上下文饱和问题，使系统能处理数百个 SKU 的大规模数据聚合。

### 7. 优点
*   **架构优雅**：将推理深度与执行宽度解耦，符合复杂系统设计原则。
*   **效率极高**：MapReduce 风格的并行处理显著降低了 Deep Research 类任务的等待时间。
*   **模块化强**：通过 MCP 协议，可以轻松接入新的 Manager（如数据库、特定 API）而不影响核心逻辑。
*   **鲁棒性**：Manager 层的反思机制能有效拦截 Worker 层的低质量输出。

### 8. 不足与局限
*   **模型依赖**：高度依赖强大的骨干模型（如 GPT-5 系列）进行任务分解和聚合，在弱模型上的表现尚未验证。
*   **提示词敏感**：系统性能仍受限于手工编写的提示词（Prompts），缺乏自动化的任务分解学习机制。
*   **API 限制**：性能上限受限于外部工具 API 的速率限制和并发带宽。
*   **实体对齐风险**：在处理极度模糊的实体时，Manager 可能会出现消歧错误（如 Case Study 中提到的疾病名称误判）。

（完）
