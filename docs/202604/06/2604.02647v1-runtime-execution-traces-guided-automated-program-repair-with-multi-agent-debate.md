---
title: Runtime Execution Traces Guided Automated Program Repair with Multi-Agent Debate
title_zh: 基于运行时执行轨迹引导与多智能体辩论的自动程序修复
authors: "Jiaqing Wu, Tong Wu, Manqing Zhang, Yunwei Dong, Bo Shen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02647v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 利用运行时事实进行辩论和补丁验证的多智能体框架
tldr: 针对自动程序修复（APR）中复杂逻辑错误和静默失败的挑战，本文提出TraceRepair框架。该方法突破了传统LLM修复仅依赖静态代码的局限，引入运行时执行轨迹作为客观约束。通过探测代理捕获关键变量快照，并利用多智能体辩论机制进行交叉验证和迭代优化，有效避免了过拟合。在Defects4J等基准测试中表现优异，显著提升了修复的准确性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的LLM修复方法主要依赖静态信息，难以捕捉复杂的运行时动态行为，且单模型推理容易产生过拟合的补丁。
method: 提出TraceRepair框架，利用探测代理获取执行轨迹快照，并通过多智能体委员会对候选补丁进行基于运行时事实的辩论与迭代修正。
result: 在Defects4J基准测试中成功修复了392个缺陷，显著超越了现有的LLM修复方法，并在新构建的漏洞数据集上展现了强大的泛化性能。
conclusion: 结合运行时执行轨迹与多智能体协作机制，能有效提升自动程序修复的逻辑正确性，减少对特定测试用例的过拟合。
---

## 摘要
自动程序修复 (APR) 在处理复杂逻辑错误和静默失败方面面临挑战。当前基于大语言模型 (LLM) 的 APR 方法大多是静态的，依赖于源代码和基础测试输出，无法准确捕捉复杂的运行时行为和动态数据依赖。虽然引入执行轨迹等运行时证据可以揭示具体的状态转换，但单个 LLM 孤立地解释这些证据往往会过拟合于特定假设，从而产生仅因巧合而非正确逻辑通过测试的补丁。因此，运行时证据应作为客观约束，而非仅仅是额外的输入。我们提出了 TraceRepair，这是一个利用运行时事实作为补丁验证共享约束的多智能体框架。探针智能体 (probe agent) 捕获关键变量的执行快照，以形成客观的修复依据。同时，由专业智能体组成的委员会对候选补丁进行交叉验证，以暴露不一致之处并进行迭代优化。在 Defects4J 基准测试上的评估结果显示，TraceRepair 正确修复了 392 个缺陷，显著优于现有的基于 LLM 的方法。大量实验表明，该方法在最新构建的近期漏洞数据集上表现出更高的效率和更强的泛化能力，证实了性能提升源于动态推理而非记忆。

## Abstract
Automated Program Repair (APR) struggles with complex logic errors and silent failures. Current LLM-based APR methods are mostly static, relying on source code and basic test outputs, which fail to accurately capture complex runtime behaviors and dynamic data dependencies. While incorporating runtime evidence like execution traces exposes concrete state transitions, a single LLM interpreting this in isolation often overfits to specific hypotheses, producing patches that satisfy tests by coincidence rather than correct logic. Therefore, runtime evidence should act as objective constraints rather than mere additional input. We propose TraceRepair, a multi-agent framework that leverages runtime facts as shared constraints for patch validation. A probe agent captures execution snapshots of critical variables to form an objective repair basis. Meanwhile, a committee of specialized agents cross-verifies candidate patches to expose inconsistencies and iteratively refine them. Evaluated on the Defects4J benchmark, TraceRepair correctly fixes 392 defects, substantially outperforming existing LLM-based approaches. Extensive experiments demonstrate improved efficiency and strong generalization on a newly constructed dataset of recent bugs, confirming that performance gains arise from dynamic reasoning rather than memorization.

---

## 论文详细总结（自动生成）

以下是对论文《Runtime Execution Traces Guided Automated Program Repair with Multi-Agent Debate》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动程序修复（APR）在处理**静默失败**（程序未崩溃但输出错误）和**测试过拟合**（补丁通过了测试但逻辑错误）方面存在瓶颈。
*   **研究动机**：
    *   现有的基于大语言模型（LLM）的修复方法主要依赖静态代码分析或粗粒度的测试反馈（如错误堆栈），缺乏对程序运行时动态行为（如变量状态转换）的感知。
    *   单智能体模型在推理时容易产生幻觉，且倾向于盲目拟合测试用例。
*   **整体含义**：论文提出 **TraceRepair** 框架，旨在将运行时执行轨迹（Execution Traces）作为客观约束，通过多智能体辩论机制，引导模型生成逻辑正确的补丁。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将运行时证据从“被动观察”转变为“主动逻辑过滤器”，确保补丁不仅在语法上正确，且在执行逻辑上与观察到的运行时事实一致。
*   **关键技术流程**：
    1.  **诊断探测（Phase 1: Diagnostic Probing）**：
        *   **Probe Agent**：分析失败测试，识别关键变量（如循环计数器、状态标志）。
        *   **动态插桩**：在抽象语法树（AST）中注入日志语句（封装在 try-catch 中以防编译失败），捕获变量快照序列 $\tau$。
        *   **轨迹管理**：采用“尾部偏置截断”策略，优先保留失效前的最后状态，解决日志过长超出上下文窗口的问题。
    2.  **多策略辩论（Phase 2: Multi-Strategy Debate）**：
        *   初始化三个具有不同视角的智能体：**防御型**（关注鲁棒性）、**因果型**（关注错误传播）、**语义型**（关注 API 规范）。
        *   **辩论循环**：智能体提出补丁，其他智能体根据运行时轨迹 $\tau$ 进行批判。如果补丁假设与轨迹事实冲突（例如假设循环未进入但轨迹显示已进入），则必须修正。
    3.  **仲裁与综合（Phase 3: Arbitration and Synthesis）**：
        *   **Judge Agent**：回顾整个辩论历史，综合各方有效见解，消除冲突，合成最终补丁。

### 3. 实验设计
*   **数据集**：
    *   **Defects4J (v1.2 & v2.0)**：包含 673 个真实 Java 漏洞，涵盖单函数（SF）和多函数（MF）缺陷。
    *   **Recent-Java**：作者新构建的数据集，包含 2023-2025 年的 21 个漏洞，用于验证模型在排除“数据泄露（记忆）”后的真实推理能力。
*   **Benchmark 设置**：完美故障定位（PFL）和方法级故障定位（MFL）。
*   **对比方法**：包括 ChatRepair, RepairAgent, REINFIX, ThinkRepair, AdverIntent-Agent, FitRepair, GAMMA 等 10 种 SOTA 方法。

### 4. 资源与算力
*   **模型后端**：使用了 **DeepSeek-V3.2** 和 **GPT-3.5-Turbo** 作为骨干模型。
*   **算力说明**：文中未提及具体的 GPU 硬件型号或训练时长，因为该框架属于**推理侧（Inference-time）**的 Agent 框架，不涉及模型重训练。
*   **实验配置**：每个漏洞最大采样 40 次，辩论最多 3 轮，测试执行超时设为 300 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 600 多个真实漏洞上进行了大规模测试，实验组设置详尽。
*   **消融实验**：分别移除了运行时轨迹（NT）和辩论机制（ND），并逐一移除三种辩论策略，验证了各组件的必要性。
*   **客观性**：通过手动验证补丁的语义等价性（由两名作者独立审核并仲裁），有效避免了仅凭测试通过率衡量性能的偏差。实验设计覆盖了定位精度、泛化性、成本效率等多个维度，非常充分。

### 6. 主要结论与发现
*   **性能领先**：TraceRepair 在 Defects4J 上成功修复了 **392** 个漏洞，显著超过了之前的最佳记录。
*   **鲁棒性强**：在定位精度下降（MFL）时，TraceRepair 的性能跌幅远小于静态方法，证明运行时轨迹提供了关键的定位辅助。
*   **泛化能力**：在 Recent-Java 数据集上，TraceRepair 依然保持优异，证明其修复能力源于动态推理而非对训练数据的记忆。
*   **高性价比**：相比 ChatRepair，TraceRepair 的 Token 消耗降低了约 **80%**，单次修复成本显著下降。

### 7. 优点（亮点）
*   **动态约束机制**：将运行时轨迹作为“硬约束”引入 LLM 辩论，有效解决了 LLM 在代码修复中的幻觉问题。
*   **插桩安全性**：通过 AST 操作和防御性代码封装，保证了探测过程不会引入新的编译错误。
*   **多视角协作**：通过防御、因果、语义三种策略的碰撞，提升了补丁的逻辑严密性。

### 8. 不足与局限
*   **测试依赖性**：如果现有的测试用例质量极差（覆盖率极低），Probe Agent 可能无法捕获到有意义的执行轨迹。
*   **语言局限性**：目前仅针对 Java 语言实现，虽然原理通用，但跨语言（如 C++ 或 Python）的插桩和环境搭建需要额外开发。
*   **系统复杂性**：相比单次 Prompt 修复，多智能体辩论框架的系统架构更复杂，对 Agent 编排的稳定性要求较高。

（完）
