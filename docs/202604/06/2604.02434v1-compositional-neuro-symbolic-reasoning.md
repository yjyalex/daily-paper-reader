---
title: Compositional Neuro-Symbolic Reasoning
title_zh: 组合式神经符号推理
authors: "Anugyan Das, Omkar Ghugarkar, Vishvesh Bhat, Asad Aali"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02434v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 抽象任务的神经符号推理
tldr: 针对ARC任务中纯神经架构泛化差和纯符号系统感知弱的问题，本文提出一种组合式神经符号推理框架。该方法通过提取网格的对象级结构，利用神经先验从领域特定语言（DSL）中提议变换候选，并结合跨样本一致性过滤。该系统显著提升了大语言模型在ARC-AGI-2上的表现，证明了分离感知、神经引导提议与符号过滤在无需微调的情况下能有效增强泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决纯神经网络在组合泛化上的不足以及纯符号系统在感知接地方面的局限性。
method: 提出一种神经符号架构，结合对象级结构提取、基于DSL的神经变换提议以及跨示例的一致性过滤机制。
result: "在ARC-AGI-2公开评估集上将基础LLM的准确率从16%提升至24.4%，结合元分类器后进一步达到30.8%。"
conclusion: 分离感知、神经引导提议与符号过滤能显著提升推理泛化性，且无需任务特定的微调或强化学习。
---

## 摘要
我们研究了针对抽象推理基准测试（ARC）的结构化抽象推理，并将其泛化能力与测试时（test-time）方法进行了比较。纯神经架构缺乏可靠的组合泛化能力，而严格的符号系统则在感知落地（perceptual grounding）方面面临困难。因此，我们提出了一种神经符号架构，该架构从网格中提取对象级结构，利用神经先验从固定的原子模式领域特定语言（DSL）中提出候选变换，并利用跨示例的一致性来过滤假设。该系统被实例化为一个基于受人类视觉抽象启发的单元模式的组合推理框架，通过对象表示和变换建议增强了大语言模型（LLM）。在 ARC-AGI-2 公共评估集上，它将基础 LLM 的性能从 16% 提升至 24.4%，在通过元分类器与 ARC Lang Solver 结合后，性能进一步提升至 30.8%。这些结果表明，将感知、神经引导的变换建议和符号一致性过滤分离，可以在不进行特定任务微调或强化学习的情况下提高泛化能力，同时减少对暴力搜索和基于采样的测试时扩展（test-time scaling）的依赖。我们开源了 ARC-AGI-2 Reasoner 的代码（https://github.com/CoreThink-AI/arc-agi-2-reasoner）。

## Abstract
We study structured abstraction-based reasoning for the Abstraction and Reasoning Corpus (ARC) and compare its generalization to test-time approaches. Purely neural architectures lack reliable combinatorial generalization, while strictly symbolic systems struggle with perceptual grounding. We therefore propose a neuro-symbolic architecture that extracts object-level structure from grids, uses neural priors to propose candidate transformations from a fixed domain-specific language (DSL) of atomic patterns, and filters hypotheses using cross-example consistency. Instantiated as a compositional reasoning framework based on unit patterns inspired by human visual abstraction, the system augments large language models (LLMs) with object representations and transformation proposals. On ARC-AGI-2, it improves base LLM performance from 16% to 24.4% on the public evaluation set, and to 30.8% when combined with ARC Lang Solver via a meta-classifier. These results demonstrate that separating perception, neural-guided transformation proposal, and symbolic consistency filtering improves generalization without task-specific finetuning or reinforcement learning, while reducing reliance on brute-force search and sampling-based test-time scaling. We open-source the ARC-AGI-2 Reasoner code (https://github.com/CoreThink-AI/arc-agi-2-reasoner).

---

## 论文详细总结（自动生成）

以下是对论文《Compositional Neuro-Symbolic Reasoning》（组合式神经符号推理）的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：ARC（抽象与推理语料库）是衡量人工智能“流体智能”的核心基准，要求模型仅通过极少数示例推断出抽象规则。
*   **核心问题**：当前的纯神经架构（如大语言模型）缺乏可靠的组合泛化能力，容易产生脆弱的推断；而纯符号系统在处理复杂的视觉感知接地（Perceptual Grounding）时面临组合爆炸问题。
*   **整体含义**：本文旨在通过**分离感知抽象与规则归纳**，提出一种神经符号架构。该架构利用神经模型的先验知识来引导符号搜索，并利用符号系统的一致性检查来确保泛化性，从而解决 ARC-AGI-2 任务中复杂的多步组合推理难题。

### 2. 方法论
该系统采用四阶段的神经符号流水线：
1.  **结构化符号场景抽象**：
    *   将原始网格图像转换为符号场景图。
    *   **关键技术**：背景估计（取众数）、连通分量分解（8-连通 BFS）、特征参数化（边界框、质心、几何形状、颜色直方图、空腔检测）。
2.  **神经引导的假设生成**：
    *   定义了一个包含 **22 种原子单元模式（Unit Patterns）** 的领域特定语言（DSL），如水平填充、垂直填充、连接桥接、对称变换等。
    *   利用 LLM（如 o4-mini）作为神经先验，根据输入输出的结构差异，从 DSL 中提议候选变换程序 $\pi$。
3.  **跨示例一致性过滤**：
    *   在所有训练示例上执行候选程序。只有在所有示例上都能精确还原输出网格的程序才会被保留。
    *   遵循**最小复杂度准则**：在多个有效程序中，优先选择步骤最少（深度最小）的程序。
4.  **引导式测试解生成**：
    *   将筛选出的共识假设转化为结构化提示（Hints），引导最终的 LLM 求解器生成测试网格。
    *   采用**自我一致性（Self-Consistency）**机制，通过多次采样和单元格级投票来提高鲁棒性。

### 3. 实验设计
*   **数据集**：ARC-AGI-2 公开评估集（Public Evaluation Set）。
*   **评估指标**：官方的 **pass@2**（两次提交中至少一次完全正确即视为解决）。
*   **对比方法**：
    *   **纯 LLM 架构**：GPT-5-Pro, Grok-4, Claude Opus/Sonnet, o3/o4-mini, Gemini 2.5 Pro。
    *   **混合/神经符号系统**：J. Berman (Hybrid), NVARC (Hybrid), ARC Lang Solver。
*   **消融实验**：对比了“仅提示”、“仅自我一致性”、“全模型”以及“基准 LLM”的表现。

### 4. 资源与算力
*   **模型使用**：采用了异构模型集成。**Grok-4** 作为主求解器和元分类器；**o4-mini** 用于模式检测；**Claude Opus 4** 用于增强对象描述。
*   **算力细节**：文中**未明确提及具体的 GPU 型号或训练时长**，因为该方法属于推理时扩展（Test-time Scaling）架构，不涉及模型微调或强化学习训练。
*   **推理配置**：使用了 API 密钥池（如 6 个 Grok 密钥）进行并发调用，设置了 72,000 秒的超长请求超时以容纳复杂的符号预处理。

### 5. 实验数量与充分性
*   **实验规模**：在 ARC-AGI-2 的完整公开评估集上进行了测试。
*   **消融实验**：设计了 4 组对照实验，清晰地量化了符号提示（+6.9%）和自我一致性（+3.9%）的贡献。
*   **充分性评价**：实验设计较为充分，对比了当时（2025年11月）市面上几乎所有主流的 Frontier LLMs。通过元分类器（Meta-Classifier）结合不同求解器的实验，证明了不同方法在任务解决上的互补性。

### 6. 主要结论与发现
*   **性能提升**：该架构将基础 LLM 的准确率从 16% 提升至 **24.4%**；通过元分类器集成后达到 **30.8%**，处于 SOTA 水平。
*   **核心驱动力**：**符号抽象和假设限制**是性能提升的主要来源，其贡献远大于单纯的随机采样（自我一致性）。
*   **组合泛化**：显式分离感知、假设生成和符号约束，比端到端的模式补全具有更强的泛化能力。

### 7. 优点
*   **架构优雅**：成功解决了神经模型“感知强、逻辑弱”与符号系统“逻辑强、感知弱”的矛盾。
*   **无需训练**：无需针对特定任务进行微调或 RL，具有极高的即插即用性。
*   **可解释性**：通过 DSL 模式和结构化提示，推理过程变得可解释、可验证。
*   **开源贡献**：开源了代码库，有助于社区进一步研究。

### 8. 不足与局限
*   **人机差距**：尽管达到 30.8%，但距离人类 100% 的表现仍有巨大鸿沟。
*   **DSL 覆盖范围**：目前的 22 种模式无法覆盖所有复杂的组合变换，某些深层关系推理仍难以处理。
*   **推理成本**：依赖多次采样和多模型调用，推理时的计算成本和延迟较高。
*   **感知依赖**：部分符号表示仍依赖 LLM 辅助（如复杂形状描述），若 LLM 感知出错，后续推理会崩溃。

（完）
