---
title: "Rubrics to Tokens: Bridging Response-level Rubrics and Token-level Rewards in Instruction Following Tasks"
title_zh: 从评分标准到 Token：在指令遵循任务中桥接回答级评分标准与 Token 级奖励
authors: "Tianze Xu, Yanzhao Zheng, Pengrui Lu, Lyumanshan Ye, Yong Wu, Zhentao Zhang, Yuanqiang Yu, Chao Ma, Jihuai Zhu, Pengfei Liu, Baohua Dong, Hangcheng Zhu, Ruohui Huang, Gang Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02795v1"
tags: ["query:token"]
score: 10.0
evidence: 桥接粗粒度回答级评分与细粒度Token级信用分配
tldr: 针对大语言模型在复杂指令遵循任务中存在的奖励稀疏和歧义问题，本文提出RTT强化学习框架。该框架通过Token级相关性判别器将粗粒度的回答级评分转化为细粒度的Token级信用分配，并结合RTT-GRPO算法与样本内Token组归一化方法优化策略。实验证明，RTT在指令遵循和准则准确性方面显著优于现有基准，有效提升了模型对齐效果。
selection_source: fresh_fetch
motivation: 现有的基于评分细则的强化学习主要依赖回答级奖励，导致严重的奖励稀疏和信用分配模糊问题。
method: 提出RTT框架，利用Token级相关性判别器识别关键Token，并通过RTT-GRPO算法和样本内Token组归一化进行策略优化。
result: 在多个模型和基准测试中，RTT在指令遵循和评分细则准确性上均一致优于其他基线方法。
conclusion: 将回答级评分细化为Token级奖励并结合改进的归一化方法，能显著增强大语言模型处理复杂指令的能力。
---

## 摘要
基于评分标准的强化学习（RL）已成为将大语言模型（LLM）与复杂的开放域指令遵循任务对齐的一种极具前景的方法。然而，现有方法主要依赖于回答级奖励，这引入了严重的奖励稀疏性和奖励歧义问题。为了解决这些问题，我们提出了 Rubrics to Tokens (RTT)，这是一种新型的基于评分标准的强化学习框架，它桥接了粗粒度的回答级评分和细粒度的 Token 级信用分配。RTT 引入了一个 Token 级相关性判别器，用于预测回答中的哪些 Token 负责特定的约束，并通过 RTT-GRPO 优化策略模型，该方法在一个统一的框架内集成了回答级和 Token 级的优势函数。此外，在 Token 级基于评分标准的强化学习中，当从一维的结果级奖励过渡到三维奖励空间时，我们提出了一种名为“样本内 Token 组归一化”（Intra-sample Token Group Normalization）的新型组归一化方法，以适应这一转变。广泛的实验和基准测试表明，在不同模型中，RTT 在指令级和评分标准级准确度上均持续优于其他基准方法。

## Abstract
Rubric-based Reinforcement Learning (RL) has emerged as a promising approach for aligning Large Language Models (LLMs) with complex, open-domain instruction following tasks. However, existing methods predominantly rely on response-level rewards, introducing severe reward sparsity and reward ambiguity problems. To address these issues, we propose Rubrics to Tokens (RTT), a novel rubric-based RL framework that bridges coarse response-level scores and fine-grained token-level credit assignment. RTT introduces a Token-Level Relevance Discriminator to predict which tokens in the response are responsible for a specific constraint, and optimizes the policy model via RTT-GRPO, which integrates response-level and token-level advantages within a unified framework. Furthermore, when transitioning from one-dimensional, outcome-level reward to three-dimensional reward space in the token-level rubric-based RL, we propose a novel group normalization method, called Intra-sample Token Group Normalization, to accommodate this shift. Extensive experiments and benchmarks demonstrate that RTT consistently outperforms other baselines in both instruction- and rubric-level accuracy across different models.

---

## 论文详细总结（自动生成）

以下是对论文《Rubrics to Tokens: Bridging Response-level Rubrics and Token-level Rewards in Instruction Following Tasks》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在复杂指令遵循任务中，现有的强化学习（RL）方法通常依赖于“回答级”（Response-level）的奖励信号（如二元对错或约束满足率）。
*   **核心问题**：回答级奖励存在**奖励稀疏性**（模型难以通过随机探索获得满分）和**奖励歧义性**（不同的错误可能得到相同的分数，导致信用分配模糊）。
*   **整体含义**：本文旨在将粗粒度的回答级评分细化为 Token 级的信用分配，通过识别哪些具体的 Token 导致了约束的满足或违反，从而提供更精准的训练信号。

### 2. 方法论
*   **核心思想**：引入一个 **Token 级相关性判别器（Token-Level Relevance Discriminator）**，将回答级的评分标准（Rubrics）映射到具体的 Token 上，并结合改进的 GRPO 算法进行策略优化。
*   **关键技术细节**：
    1.  **Token 级相关性判别器 ($D_\phi$)**：训练一个判别模型，输入指令、约束条件和生成的回答，预测每个 Token 与该约束的相关性概率。
    2.  **数据构建与标注**：利用强模型（如 GPT-5.2、DeepSeek-V3）生成正负样本，并通过“最小修改”和“约束遗漏”策略构建高质量的负样本，采用结构化分类法自动标注相关 Token。
    3.  **RTT-GRPO 算法**：在 GRPO 框架基础上，将优势函数（Advantage）重构为：$\hat{A}_{sum} = \alpha \hat{A}_{res} + \beta \hat{A}_{tok}$。其中 $\hat{A}_{res}$ 是传统的回答级优势，$\hat{A}_{tok}$ 是基于判别器权重的 Token 级优势。
    4.  **样本内 Token 组归一化（Intra-sample Token Group Normalization）**：针对三维奖励空间（样本、Token、准则），提出在单个回答内部进行归一化，以消除不同回答长度带来的偏差（Length Bias）。

### 3. 实验设计
*   **训练数据**：使用 HiR-16K 数据集（包含 MulDimIF、VerIF、IFTrain 等来源）。
*   **评测基准（Benchmark）**：
    *   **域内指令遵循**：IFEval、IFBench、MulDimIF、AdvancedIF。
    *   **域外通用能力**：MATH-500、GPQA、MMLU-Pro。
*   **对比方法**：SFT（监督微调）、DPO（直接偏好优化）、RL-AON（全对或全错奖励）、RL-CSR（约束满足率奖励）。
*   **实验模型**：Qwen3-4B-Instruct、Qwen2.5-7B-Instruct、Llama3.2-3B-Instruct。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA A100-80GB GPU。
*   **算力消耗**：以 Qwen3-4B 为例，500 步 RL 训练总计消耗约 **1511.3 GPU 小时**。
*   **开销分析**：RTT 引入的判别器前向传播仅占总耗时的 **7.8%**，相比基础 RL 流程仅增加约 **8.5%** 的额外开销，具有较高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构和规模的主流开源模型，并在 7 个多元化的基准测试上进行了验证。
*   **消融实验**：针对组归一化策略（样本间 vs 样本内）、Token 权重策略（随机、均匀、贪婪、本文方法）以及超参数 $\beta$ 的敏感度进行了详细测试。
*   **充分性评价**：实验设计非常充分，不仅关注准确率提升，还深入分析了训练稳定性（KL 散度、熵、剪切比例）和 Token 归因的可视化案例，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：RTT 在所有指令遵循基准测试中均显著优于 SFT、DPO 和传统的 RL 基线，平均提升指令级准确率 2.50%。
*   **稳定性增强**：在 Llama3.2-3B 等模型上，传统的 RL-CSR 容易出现训练崩溃（熵骤降、KL 激增），而 RTT 通过细粒度信用分配保持了极高的训练稳定性。
*   **通用性保持**：在提升指令遵循能力的同时，模型在数学推理和常识问答等域外任务上的表现基本持平或略有提升，未出现明显的“对齐税”。

### 7. 优点
*   **理论创新**：首次识别并定义了多维奖励空间中的“组划分问题”（Group Partitioning Problem）。
*   **细粒度对齐**：通过将奖励下沉到 Token 级别，有效解决了复杂指令下的奖励歧义问题。
*   **实用性强**：计算开销低，且框架已开源，易于集成到现有的 RL 流程中。

### 8. 不足与局限
*   **归一化策略探索有限**：虽然提出了样本内归一化，但对于更高维度的奖励空间（如多轮对话、多任务组合），最优的组划分策略仍需进一步研究。
*   **判别器依赖**：RTT 的效果部分取决于 Token 级判别器的准确性，对于极度抽象或主观的约束（如“更有趣”），判别器的标注难度较大。
*   **应用场景限制**：目前主要集中在单轮指令遵循，尚未在长程智能体（Agent）任务或多轮交互中进行验证。

（完）
