---
title: Student-in-the-Loop Chain-of-Thought Distillation via Generation-Time Selection
title_zh: 通过生成时选择实现学生在环的思维链蒸馏
authors: "Chaoqun He, Yingfa Chen, Chaojun Xiao, Xu Han, Lijie Wen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02819v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 蒸馏过程中思维链轨迹的生成时选择
tldr: 本研究针对大模型思维链（CoT）蒸馏至小模型时，教师生成的推理路径未必适合学生学习的问题，提出了Gen-SSD框架。该框架采用“学生在环”策略，在教师生成过程中由学生实时评估并筛选候选路径，从而提前剪枝不可学习的分支。实验表明，Gen-SSD在数学推理任务上显著优于传统蒸馏方法，提升了推理路径的稳定性和可学习性，为高效知识蒸馏提供了新思路。
selection_source: fresh_fetch
motivation: 传统的思维链蒸馏依赖事后筛选，无法在生成过程中实时控制推理路径的难度，导致生成的轨迹往往超出小模型的理解能力。
method: 提出Gen-SSD框架，让学生模型在教师采样过程中实时评估候选片段，引导生成更易于学生吸收的推理路径并剪枝无效分支。
result: 在数学推理基准测试中，Gen-SSD比标准知识蒸馏提升了约5.9个百分点，且比其他先进基线方法高出多达4.7个百分点。
conclusion: 在蒸馏过程中引入生成时的实时监督，对于生成稳定且具备可学习性的推理轨迹以实现有效的知识转移至关重要。
---

## 摘要
大型推理模型通过长思维链（CoT）轨迹在复杂任务上取得了强劲性能，但将此类推理过程直接迁移到较小模型仍具挑战性。一个关键难点在于，并非所有教师生成的推理轨迹都适合学生学习。现有方法通常依赖事后过滤，即在生成完成后根据启发式标准选择轨迹。然而，此类方法无法控制生成过程本身，且仍可能产生超出学生学习能力的推理路径。为解决这一局限性，我们提出了 Gen-SSD（生成时自选择蒸馏），这是一种执行生成时选择的学生在环框架。学生不再是被动地接收完整轨迹，而是在教师的采样过程中评估候选后续内容，引导仅扩展可学习的推理路径，并实现对无用分支的早期剪枝。在数学推理基准测试上的实验表明，Gen-SSD 一贯优于标准知识蒸馏和近期的基准方法，相比标准知识蒸馏提升了约 5.9 个百分点，相比其他基准方法提升了高达 4.7 个百分点。进一步分析表明，Gen-SSD 产生了更稳定且更具可学习性的推理轨迹，凸显了在生成过程中引入监督对于有效蒸馏的重要性。

## Abstract
Large reasoning models achieve strong performance on complex tasks through long chain-of-thought (CoT) trajectories, but directly transferring such reasoning processes to smaller models remains challenging. A key difficulty is that not all teacher-generated reasoning trajectories are suitable for student learning. Existing approaches typically rely on post-hoc filtering, selecting trajectories after full generation based on heuristic criteria. However, such methods cannot control the generation process itself and may still produce reasoning paths that lie outside the student's learning capacity. To address this limitation, we propose Gen-SSD (Generation-time Self-Selection Distillation), a student-in-the-loop framework that performs generation-time selection. Instead of passively consuming complete trajectories, the student evaluates candidate continuations during the teacher's sampling process, guiding the expansion of only learnable reasoning paths and enabling early pruning of unhelpful branches. Experiments on mathematical reasoning benchmarks demonstrate that Gen-SSD consistently outperforms standard knowledge distillation and recent baselines, with improvements of around 5.9 points over Standard KD and up to 4.7 points over other baselines. Further analysis shows that Gen-SSD produces more stable and learnable reasoning trajectories, highlighting the importance of incorporating supervision during generation for effective distillation.

---

## 论文详细总结（自动生成）

这篇论文由清华大学的研究团队提出，旨在解决大型推理模型（LRM）知识蒸馏过程中的“可学习性”难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：将大型推理模型（如 DeepSeek-R1, OpenAI-o1）生成的长思维链（CoT）蒸馏到小模型时，效果往往不如预期。
*   **研究动机**：作者发现，教师模型生成的推理轨迹并非全都适合学生学习。现有的蒸馏方法大多采用“事后过滤”（Post-hoc filtering），即在生成完整轨迹后再进行筛选。这种方式无法干预生成过程，导致生成的路径可能过于复杂，超出了小模型的理解能力，且浪费了大量计算资源生成不可用的数据。

### 2. 方法论：Gen-SSD 框架
*   **核心思想**：提出“学生在环”（Student-in-the-loop）的生成时自选择蒸馏（Gen-SSD）。让学生模型在教师模型采样的过程中实时参与，评估并选择最适合自己学习的推理片段。
*   **关键技术细节**：
    *   **冷启动阶段（Cold Start）**：先用少量教师数据对学生进行微调，使其熟悉推理格式（如 `<think>` 标签），确保学生能提供有效的评估信号。
    *   **分块生成与评估**：教师模型按块（Chunk）生成候选推理片段。
    *   **PPL 选择机制**：学生模型计算每个候选片段的困惑度（PPL），选择 PPL 最低（即最符合学生当前认知水平）的片段作为后续生成的基准。
    *   **渐进式采样缩减**：为了平衡效果与效率，随着生成步骤的增加，候选样本数量 $K$ 逐渐减少（例如从 16 降至 4），实现早期剪枝。
    *   **拒绝采样**：最终仅保留推理结果正确的轨迹进行监督微调（SFT）。

### 3. 实验设计
*   **数据集/场景**：主要集中在数学推理任务。
    *   **训练集**：基于 OpenMathReasoning 的子集（约 25K 条指令）。
    *   **测试基准**：AIME24/25、AMC23、OlympiadBench、GSM8K。
    *   **泛化性测试**：MBPP（代码）、ARC-C、StrategyQA、CommonsenseQA。
*   **模型配置**：
    *   **教师模型**：QwQ-32B、DeepSeek-R1 系列（7B 到 671B）。
    *   **学生模型**：Qwen2.5-Math-1.5B。
*   **对比方法**：标准知识蒸馏（Standard KD）、自蒸馏（Self-Distillation）、MCC-KD（一致性蒸馏）、MoRSD（基于难度的过滤）。

### 4. 资源与算力
*   **硬件环境**：使用了 1 台配备 **4 张 NVIDIA A800-SXM4-80GB GPU** 的服务器。
*   **训练细节**：微调周期为 5 个 Epoch，学习率 $2 \times 10^{-5}$，最大序列长度 16,384。文中明确记录了采样效率的提升，但未详细给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个核心数学基准和 4 个通用/代码基准。
*   **消融实验**：非常充分。作者探讨了冷启动的必要性、不同分块大小（512 到 16K）的影响、不同规模教师模型的效果，以及低 PPL vs 高 PPL 选择策略的对比。
*   **客观性**：通过与多种先进基线（如 MoRSD）在相同数据规模下对比，证明了 Gen-SSD 的优越性。

### 6. 主要结论与发现
*   **性能提升**：Gen-SSD 在数学任务上比标准 KD 平均提升约 **5.9 分**，比最强基线 MoRSD 提升约 **1.8 分**。
*   **可学习性**：低 PPL 引导生成的轨迹更短（平均减少约 1000 个 token）、更稳定，且更易于学生模型吸收。
*   **教师模型影响**：随着教师模型能力的增强，Gen-SSD 带来的增益通常会扩大，但模型参数量并非唯一决定因素，推理质量更关键。

### 7. 论文亮点
*   **实时干预**：将筛选时机从“生成后”提前到“生成中”，实现了对推理路径的动态控制。
*   **效率优化**：通过早期剪枝（Early Pruning）减少了无效推理路径的生成，降低了推理成本。
*   **学生导向**：明确了“最强的数据不一定是最好的数据”，强调了蒸馏过程中“师生匹配”的重要性。

### 8. 不足与局限
*   **API 限制**：对于无法获取 Logits 或无法分块干预的闭源模型 API，该方法难以直接应用（文中通过模拟 16K 块大小进行了初步探讨，但灵活性受限）。
*   **PPL 偏差**：过度追求低 PPL 可能导致生成的推理路径过于简单，虽然易于学习，但可能损失教师模型中更深层次的复杂推理逻辑。
*   **领域局限**：实验主要集中在数学和逻辑推理，在长文本创作或主观性任务上的表现尚待验证。

（完）
