---
title: Offline Constrained RLHF with Multiple Preference Oracles
title_zh: 具有多个偏好预言机的离线受限 RLHF
authors: "Brenden Latham, Mehrdad Moharrami"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00200v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 带偏好预言机的离线约束 RLHF
tldr: 本研究针对离线受限RLHF问题，提出了一种处理多个偏好预言机的框架，旨在平衡模型性能与安全性或公平性。通过最大似然估计奖励并利用KL正则化拉格朗日对偶法，研究者开发了一种仅对偶算法。该工作提供了离线受限偏好学习的首个有限样本性能保证，并证明了算法能以高概率满足约束，同时可扩展至多约束和一般f-散度场景。
selection_source: fresh_fetch
motivation: 旨在解决RLHF中性能与安全、公平性之间的权衡问题，特别是在离线环境下处理来自不同群体的多个偏好约束。
method: 通过最大似然估计从成对比较中学习奖励，并将受限优化目标转化为KL正则化的拉格朗日对偶问题进行求解。
result: 提出了首个具有有限样本性能保证的离线受限偏好学习算法，并确保了高概率的约束满足。
conclusion: 该研究为多偏好约束下的离线RLHF提供了坚实的理论基础和有效的算法方案，具有较强的通用性和理论完备性。
---

## 摘要
我们研究了具有多个偏好预言机的离线受限人类反馈强化学习（RLHF）。受性能与安全性或公平性权衡的应用启发，我们的目标是在满足受保护群体最低福利约束的前提下，最大化目标群体的效用。基于在参考策略下收集的两两比较数据，我们通过极大似然估计来估计特定于预言机的奖励，并分析统计不确定性如何通过对偶程序进行传播。我们将受限目标建模为 KL 正则化的拉格朗日函数，其原优化器是一个 Gibbs 策略，从而将学习过程简化为一个凸对偶问题。我们提出了一种仅对偶算法，该算法能确保以高概率满足约束，并为离线受限偏好学习提供了首个有限样本性能保证。最后，我们将理论分析扩展到支持多个约束和通用的 f-散度正则化。

## Abstract
We study offline constrained reinforcement learning from human feedback with multiple preference oracles. Motivated by applications that trade off performance with safety or fairness, we aim to maximize target population utility subject to a minimum protected group welfare constraint. From pairwise comparisons collected under a reference policy, we estimate oracle-specific rewards via maximum likelihood and analyze how statistical uncertainty propagates through the dual program. We cast the constrained objective as a KL-regularized Lagrangian whose primal optimizer is a Gibbs policy, reducing learning to a convex dual problem. We propose a dual-only algorithm that ensures high-probability constraint satisfaction and provide the first finite-sample performance guarantees for offline constrained preference learning. Finally, we extend our theoretical analysis to accommodate multiple constraints and general f-divergence regularization.

---

## 论文详细总结（自动生成）

以下是对论文《Offline Constrained RLHF with Multiple Preference Oracles》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**离线（Offline）**环境下，利用来自**多个偏好预言机（Preference Oracles）**的反馈，训练一个既能最大化目标性能，又能满足安全或公平性硬约束的强化学习模型？
*   **研究动机**：
    *   **多目标权衡**：现实应用（如大语言模型）需要在提升“有用性”的同时，确保“安全性”或对特定群体的“公平性”。
    *   **离线限制**：在实际部署中，通常只能获取预先收集的静态偏好数据集，无法进行实时环境交互。
    *   **理论空白**：现有 RLHF 理论多关注单一预言机或无约束优化，缺乏针对多预言机受限场景的有限样本（Finite-sample）性能保证。

### 2. 方法论
*   **核心思想**：将受限 RLHF 问题转化为一个 **KL 正则化的拉格朗日对偶问题**。利用 KL 正则化下最优策略具有 Gibbs 形式（Softmax 形式）的特性，将复杂的策略空间搜索简化为对对偶变量（拉格朗日乘子 $\lambda$）的一维凸优化。
*   **关键技术细节**：
    1.  **奖励估计**：基于 Bradley-Terry 模型，对每个预言机（如目标群体 $o_1$ 和受保护群体 $o_2$）分别使用极大似然估计（MLE）从成对比较数据中学习线性奖励函数 $\hat{r}_1, \hat{r}_2$。
    2.  **对偶转化**：构造拉格朗日函数 $L(\pi, \lambda)$。由于引入了 KL 散度约束，原问题的最优解 $\pi^*_\lambda$ 具有闭式解：$\pi^*_\lambda(a|x) \propto \pi_0(a|x) \exp(\frac{1}{\eta}(\hat{r}_1 + \lambda \hat{r}_2))$。
    3.  **算法流程（Algorithm 1）**：
        *   **初始化**：设定对偶变量 $\lambda_0$。
        *   **迭代优化**：在每一步中，根据当前的 $\lambda_t$ 计算 Gibbs 策略，估计对偶梯度（即约束函数的违反程度），然后进行**投影梯度下降（Projected Gradient Descent）**更新 $\lambda$。
        *   **输出**：返回迭代过程中的平均对偶变量 $\bar{\lambda}_T$，从而确定最终策略。
*   **理论扩展**：证明了该框架可扩展至多约束场景以及更通用的 $f$-散度正则化。

### 3. 实验设计
*   **数据集/场景**：
    1.  **PKU-SafeRLHF 数据集**：包含约 7.4 万条训练数据和 7000 条测试数据，涵盖“安全性”和“有用性”两个维度的偏好标签。
    2.  **合成环境（Synthetic）**：用于验证理论边界，模拟有限的状态-动作空间和线性奖励特征。
*   **Benchmark 与对比对象**：
    *   **参考策略 ($\pi_0$)**：使用 Alpaca-7B 模型作为基准。
    *   **贪婪策略（Greedy）**：作为性能上限的参考。
    *   **对比维度**：安全性奖励、有用性奖励、约束违反率（Violation）。

### 4. 资源与算力
*   **模型使用**：实验中提到了 Alpaca-7B、Alpaca2-7B 和 Alpaca3-8B 用于生成响应，使用 Sentence-BERT (all-mpnet-base-v2) 进行文本向量化（768 维特征）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到对偶优化迭代了 1000 次，奖励模型使用 L-BFGS-B 算法优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在真实的 PKU-SafeRLHF 数据集上进行了完整验证。
    *   在合成环境下，针对不同的行为偏差权重 $w$（0.3, 0.6, 0.9）和样本量 $N$ 进行了 5 组不同随机种子的重复实验。
*   **充分性评价**：实验设计较为充分，涵盖了从理论验证（合成数据）到实际应用（真实安全数据集）的跨度。通过展示约束违反率随迭代次数下降的曲线，客观地证明了算法的收敛性和有效性。

### 6. 主要结论与发现
*   **理论保证**：首次为离线受限偏好学习提供了有限样本性能保证。证明了统计误差主要受数据集覆盖范围影响（$O(\sqrt{d/N})$），而优化误差随迭代次数 $T$ 衰减（$O(1/\sqrt{T})$）。
*   **性能表现**：在 PKU-SafeRLHF 任务中，该算法训练的策略在测试集上实现了**近乎零的约束违反率**，同时在安全性奖励和有用性奖励上均优于参考策略 $\pi_0$。
*   **策略偏移**：实验观察到，优化后的策略会显著改变决策边界，从 $\pi_0$ 的不确定状态转向更具确定性的高奖励动作。

### 7. 优点
*   **理论完备**：提供了严谨的收敛性分析和高概率约束满足保证，填补了离线受限 RLHF 的理论空白。
*   **计算高效**：通过对偶转化，避免了复杂的策略梯度搜索，仅需在一维（或低维）对偶空间进行优化，计算开销极小。
*   **通用性强**：不仅适用于 KL 散度，还给出了 $f$-散度下的闭式解形式，具有很强的扩展潜力。

### 8. 不足与局限
*   **线性假设**：理论分析基于线性奖励函数假设，虽然在理论研究中常见，但在处理极度复杂的非线性人类偏好时可能存在建模偏差。
*   **覆盖性依赖**：算法性能高度依赖于参考策略 $\pi_0$ 对状态-动作空间的覆盖程度（Assumption 1），若离线数据存在严重缺失，模型效果会受限。
*   **基准对比有限**：实验主要与参考策略对比，缺乏与其他先进的受限 RL 算法（如受限 DPO 变体）在相同基准下的直接实测对比。

（完）
