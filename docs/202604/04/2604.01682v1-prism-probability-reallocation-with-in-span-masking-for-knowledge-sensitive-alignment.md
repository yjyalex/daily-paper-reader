---
title: "PRISM: Probability Reallocation with In-Span Masking for Knowledge-Sensitive Alignment"
title_zh: PRISM：用于知识敏感对齐的片段内掩码概率重分配
authors: "Chenning Xu, Mao Zheng, Mingyang Song"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01682v1"
tags: ["query:token"]
score: 9.0
evidence: 用于知识敏感对齐的Token级概率重新分配
tldr: 针对监督微调（SFT）中硬标签导致的模型过度自信及幻觉问题，本文提出PRISM框架。该方法利用句子级事实风险标签和句间依赖标注，通过风险门控机制在事实关键位置进行概率重分配，惩罚对高风险目标的过度自信预测。实验证明，PRISM在提升模型事实准确性的同时，有效保留了其通用能力。
selection_source: fresh_fetch
motivation: 传统的SFT硬标签训练容易导致模型盲目模仿缺乏事实依据的目标，从而引发严重的幻觉问题。
method: 提出一种可微的风险门控框架，结合跨度掩码和模型感知的概率重分配，动态调整高风险事实位置的学习权重。
result: 在多个事实敏感基准测试中，PRISM显著降低了幻觉率，并在通用能力评估中保持了竞争力。
conclusion: 通过保守地利用辅助风险信号并结合知识掩码，可以在事实纠偏与模型能力保持之间取得最佳平衡。
---

## 摘要
使用词元级硬标签的有监督微调（SFT）可能会放大对缺乏事实依据目标的过度自信模仿，导致幻觉在多句生成中传播。我们研究了一种增强的 SFT 设置，其中训练实例包含粗粒度的句子级事实性风险标签和句间依赖标注，为事实承诺支持较弱的位置提供结构化信号。我们提出了 PRISM，这是一个可微分的风险门控框架，仅在事实关键位置修改学习过程。PRISM 通过轻量级的、模型感知的概率重分配目标增强了标准 SFT，该目标惩罚对风险目标词元的高置信度预测，其作用范围由片段级风险权重和模型感知门控控制。在对幻觉敏感的事实基准测试和通用评估上的实验表明，PRISM 在不同骨干模型上均提升了事实性综合指标，同时保持了具有竞争力的整体能力。消融实验进一步表明，辅助信号在保守使用时最为有效，且知识掩码与模型感知重分配在平衡事实纠正与能力保留方面发挥了互补作用。

## Abstract
Supervised fine-tuning (SFT) with token-level hard labels can amplify overconfident imitation of factually unsupported targets, causing hallucinations that propagate in multi-sentence generation. We study an augmented SFT setting in which training instances include coarse sentence-level factuality risk labels and inter-sentence dependency annotations, providing structured signals about where factual commitments are weakly supported. We propose \textbf{PRISM}, a differentiable risk-gated framework that modifies learning only at fact-critical positions. PRISM augments standard SFT with a lightweight, model-aware probability reallocation objective that penalizes high-confidence predictions on risky target tokens, with its scope controlled by span-level risk weights and model-aware gating. Experiments on hallucination-sensitive factual benchmarks and general evaluations show that PRISM improves factual aggregates across backbones while maintaining a competitive overall capability profile. Ablations further show that the auxiliary signal is most effective when used conservatively, and that knowledge masking and model-aware reallocation play complementary roles in balancing factual correction and capability preservation.

---

## 论文详细总结（自动生成）

### 论文总结：PRISM —— 用于知识敏感对齐的片段内掩码概率重分配

#### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在监督微调（SFT）阶段通常采用词元级硬标签（Next-token prediction）。这种方式强迫模型模仿训练数据中的所有内容，即使某些事实超出了模型的预训练知识范围。
*   **核心问题**：这种“过度自信的模仿”会导致模型产生幻觉，且幻觉会通过句子间的逻辑依赖关系在长文本生成中传播。
*   **研究动机**：现有的幻觉缓解方案（如 RAG 或 DPO）往往增加了推理复杂度或训练阶段的负担。本文旨在通过在 SFT 过程中注入结构化的事实风险信号，以一种轻量且直接的方式修正模型在事实关键位置的置信度。

#### 2. 方法论
*   **核心思想**：提出 **PRISM** 框架，在 SFT 过程中识别事实关键片段（Fact-critical spans），并根据其风险程度动态地将目标词元的概率质量重分配给其他候选词元，从而抑制过度自信。
*   **关键技术细节**：
    *   **事实结构提取**：将响应分解为原子事实（Atomic Facts），并构建句间依赖图。如果某句依赖于前文未证实的声明，其风险权重会通过依赖链传播。
    *   **风险权重（$w_t$）**：基于句子级风险分数计算词元权重，高风险位置的 $w_t$ 较小。
    *   **模型感知门控（Model-Aware Gating）**：
        *   **偏好门控（$g_{pref}$）**：仅当模型当前预测的 Top-1 词元正是目标词元时才激活修正。
        *   **一致性门控（$g_{keep}$）**：确保在概率重分配后，目标词元依然保持为 Top-1，防止优化目标发生偏移（即避免模型转而学习错误的替代词）。
    *   **损失函数**：总损失 $L = L_{SFT} + \lambda L_{comp}$。其中 $L_{comp}$ 是补丁损失（Complement Loss），它通过减小目标词元的 Logit 值，迫使概率分布向其他词元倾斜。

#### 3. 实验设计
*   **数据集**：使用 `lmsys-chat-1m-clean` 作为基础语料，其中的响应由 DeepSeek-V3 重新生成以保证高质量。
*   **Benchmark（基准测试）**：
    *   **事实性评估**：HHEM-2.1-Open（幻觉检测）、PopQA 和 TriviaQA（基于检索增强的短篇问答）。
    *   **通用能力评估**：MMLU（多任务理解）、GSM8K（数学推理）、HumanEval（代码生成）、IFEval（指令遵循）。
*   **对比方法**：
    *   **Vanilla SFT**：标准微调。
    *   **SEAL**：选择性弃权学习（引入 [REJ] 词元）。
    *   **NOVA**：基于模型熟悉度的数据过滤方法。
    *   **Knowledge Mask**：仅对高风险片段进行掩码处理的消融基准。
*   **骨干模型**：Qwen3-4B-Base、Qwen3-8B-Base、Llama-3.1-8B。

#### 4. 资源与算力
*   **算力配置**：论文提到使用了 **DeepSpeed ZeRO-2** 框架，并开启了 **CPU offload**。
*   **训练细节**：采用 BF16 混合精度训练，结合 FlashAttention-2。训练进行 1 个 Epoch，学习率为 1e-5，全局 Batch Size 为 128，最大序列长度为 32768。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）以及具体的 GPU 数量和总训练耗时。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同规模和架构的模型上进行了验证，涵盖了事实性和通用能力两大类共 7 个主流基准测试。
*   **消融实验**：
    *   针对辅助损失权重 $\lambda$ 进行了多组梯度实验（0.01 到 1.0）。
    *   针对“模型感知重分配”和“知识掩码”两个核心组件进行了拆解对比。
*   **充分性与公平性**：实验设计较为全面，对比了数据侧（NOVA）和模型侧（SEAL）的代表性方法，并统一了解码配置，确保了对比的客观性。

#### 6. 主要结论与发现
*   **事实性提升**：PRISM 在所有测试模型上均显著提升了事实性综合得分（Factual Avg），尤其在 Llama-3.1-8B 上表现突出。
*   **能力保留**：与单纯的数据过滤或掩码方法相比，PRISM 在提升事实性的同时，能更好地保留甚至提升模型的通用能力（如 IFEval 指令遵循得分）。
*   **权衡关系**：实验发现辅助权重 $\lambda$ 不宜过大，保守的设置（如 0.1）能在事实纠偏和能力保持之间取得最佳平衡。
*   **组件功能**：知识掩码负责“定位”（在哪里修正），而模型感知重分配负责“精度”（如何修正），两者缺一不可。

#### 7. 优点
*   **非侵入性**：不需要在推理时引入检索或复杂的验证流程，保持了 SFT 的简洁性。
*   **动态修正**：通过模型感知门控，PRISM 能够根据模型当前的知识状态（置信度）进行针对性调整，而非盲目惩罚。
*   **结构化建模**：引入了句间依赖关系，能够处理幻觉在长文本中的级联传播问题。

#### 8. 不足与局限
*   **预处理依赖**：PRISM 的效果高度依赖于原子事实提取和风险标注的质量，若预处理阶段的解析器出错，可能会引入噪声。
*   **计算开销**：虽然推理无额外开销，但训练前的预处理（事实提取与验证）涉及大量的模型调用，增加了数据准备阶段的成本。
*   **应用范围**：主要针对知识密集型任务，对于主观性较强或创造性写作任务的适用性尚未得到充分验证。

（完）
