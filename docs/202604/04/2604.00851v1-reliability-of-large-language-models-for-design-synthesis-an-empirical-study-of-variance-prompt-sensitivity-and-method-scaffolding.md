---
title: "Reliability of Large Language Models for Design Synthesis: An Empirical Study of Variance, Prompt Sensitivity, and Method Scaffolding"
title_zh: 大语言模型在设计综合中的可靠性：关于变异性、提示词敏感性与方法脚手架的实证研究
authors: "Rabia Iftikhar, Andreas Rausch"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00851v1"
tags: ["query:llm-fo"]
score: 8.0
evidence: LLM 在生成结构化 UML 类图方面的性能
tldr: 本研究探讨了大语言模型（LLM）在软件设计合成（如生成UML类图）中的可靠性。研究者不仅关注语法正确性，更深入分析了模型在面对提示词变化时的设计推理稳定性。通过引入基于偏好的少样本提示方法，并在三种主流模型上进行540次实验，评估了标准、规则注入和偏好引导三种策略。结果表明，虽然偏好对齐能提升设计意图的符合度，但模型固有的非确定性仍是挑战，强调了模型选择与鲁棒性在辅助软件设计中的重要性。
selection_source: fresh_fetch
motivation: 尽管LLM能生成语法正确的UML图，但其在复杂软件设计合成中的可靠性、一致性以及对设计原则的遵循能力尚不明确。
method: 引入基于偏好的少样本提示法，通过540次实验对比了三种LLM在标准、规则注入和偏好引导策略下的设计表现及对提示词变化的敏感度。
result: 偏好引导策略有效提升了模型对设计意图的对齐度，但输出仍存在显著的非确定性，且模型间的行为差异对设计可靠性影响巨大。
conclusion: 要实现可靠的LLM辅助软件设计，除了优化提示策略外，还需重点关注模型自身的行为特征及其在设计推理中的鲁棒性。
---

## 摘要
大语言模型（LLMs）正越来越多地应用于自动化软件工程任务，包括从自然语言描述生成 UML 类图。虽然先前的研究表明 LLMs 可以生成语法正确的图表，但仅凭语法正确性并不能保证设计的有效性。本研究探讨了 LLMs 是否能超越图表转换而进行设计综合，以及它们在不同变化下维持面向设计推理的可靠程度。我们引入了一种基于偏好的少样本提示（few-shot prompting）方法，引导 LLM 的输出趋向于满足面向对象原则和模式一致性结构的设计。研究采用两个设计意图基准测试，每个基准包含三个仅限领域的改写提示词并进行 10 次重复运行，在三种建模策略（标准提示、规则注入提示和基于偏好的提示）下评估了三种 LLMs（ChatGPT 4o-mini、Claude 3.5 Sonnet、Gemini 2.5 Flash），共计 540 次实验（即 2x3x10x3x3）。结果表明，虽然基于偏好的对齐提高了对设计意图的遵循度，但它并未消除非确定性，且模型层面的行为对设计可靠性有显著影响。这些发现强调，实现可靠的 LLM 辅助软件设计不仅需要有效的提示工程，还需要深入考虑模型行为和鲁棒性。

## Abstract
Large Language Models (LLMs) are increasingly applied to automate software engineering tasks, including the generation of UML class diagrams from natural language descriptions. While prior work demonstrates that LLMs can produce syntactically valid diagrams, syntactic correctness alone does not guarantee meaningful design. This study investigates whether LLMs can move beyond diagram translation to perform design synthesis, and how reliably they maintain design-oriented reasoning under variation. We introduce a preference-based few-shot prompting approach that biases LLM outputs toward designs satisfying object-oriented principles and pattern-consistent structures. Two design-intent benchmarks, each with three domain-only, paraphrased prompts and 10 repeated runs, are used to evaluate three LLMs (ChatGPT 4o-mini, Claude 3.5 Sonnet, Gemini 2.5 Flash) across three modeling strategies: standard prompting, rule-injection prompting, and preference-based prompting, totaling 540 experiments (i.e. 2x3x10x3x3). Results indicate that while preference-based alignment improves adherence to design intent it does not eliminate non-determinism, and model-level behavior strongly influences design reliability. These findings highlight that achieving dependable LLM-assisted software design requires not only effective prompting but also careful consideration of model behavior and robustness.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型在设计综合中的可靠性研究

#### 1. 论文的核心问题与整体含义
本文探讨了大型语言模型（LLMs）在**软件设计综合（Design Synthesis）**任务中的表现，特别是从自然语言需求生成 UML 类图的能力。研究动机在于：虽然现有 LLM 能生成语法正确的图表，但往往只是简单的“名词-类”翻译，缺乏对面向对象（OO）原则和设计模式（如 GoF 模式）的深度推理。此外，LLM 的生成具有非确定性，这在要求高度稳定性的架构设计中是一个重大挑战。论文旨在评估不同提示策略对设计质量及**行为可靠性（稳定性）**的影响。

#### 2. 论文提出的方法论
论文对比了三种提示策略，核心创新在于第三种：
*   **标准基准（Standard Baseline）：** 仅提供领域任务描述，观察模型的原生设计能力。
*   **规则注入基准（Rule-injected Baseline）：** 在提示词中显式加入 OOAD 原则（如封装、抽象）和专家建议，引导模型遵循设计规范。
*   **基于偏好的少样本提示（Preference-based Few-shot Prompting - 核心方法）：** 
    *   **核心思想：** 通过展示“差的设计”（LLM 原生生成）与“好的设计”（专家优化）的对比示例，训练模型识别并偏好更具架构合理性的方案。
    *   **技术细节：** 构建了一个包含 5 个任务的小型偏好数据集，每个数据点包含任务描述、LLM 生成的低质量方案、专家设计的方案以及偏好标签。这种方法旨在通过上下文学习（In-context Learning）模拟强化学习中的偏好对齐（RLHF），但更为轻量化。

#### 3. 实验设计
*   **模型选择：** ChatGPT 4o-mini, Claude 3.5 Sonnet, Gemini 2.5 Flash。
*   **基准测试（Benchmarks）：**
    *   **HMS（医院计费系统）：** 中等复杂度，涉及策略模式（Strategy Pattern）。
    *   **Sensor Network（传感器网络）：** 高复杂度，涉及观察者模式（Observer Pattern）和事件驱动逻辑。
*   **实验变量：** 每个基准包含 3 种改写的提示词（Paraphrased Prompts），每种配置重复运行 10 次。
*   **评估指标：** 
    *   **M1.1 结构正确性（F1 分数）：** 与专家参考模型的元素匹配度。
    *   **M1.2 原则遵循得分（PAS）：** 对抽象、封装等原则的满足程度。
    *   **M1.3 模式涌现得分（PES）：** 是否隐式识别并应用了设计模式。
    *   **M1.4 稳定性指数（SI）：** 跨运行和跨提示词的方差。

#### 4. 资源与算力
论文**未明确说明**具体的算力资源（如 GPU 型号或训练时长）。由于实验主要基于商业 LLM 的 API（ChatGPT, Claude, Gemini），研究重点在于提示工程和推理表现，而非模型训练，因此不涉及大规模本地算力消耗。

#### 5. 实验数量与充分性
*   **实验规模：** 总计进行了 **540 次** 实验（2 个领域 × 3 种提示词改写 × 3 个模型 × 3 种策略 × 10 次重复）。
*   **充分性评价：** 实验设计非常充分且严谨。通过引入“提示词改写”和“重复运行”，有效地捕捉了生成式模型在软件工程任务中的随机性和敏感性。采用人工专家评估（辅以双人交叉审核）确保了语义层面的评价比单纯的 Token 匹配更具客观性。

#### 6. 论文的主要结论与发现
*   **偏好引导有效：** 基于偏好的提示法在提升设计质量（尤其是模式应用和原则遵循）方面显著优于规则注入。
*   **可靠性具有模型依赖性：** 
    *   **Claude 3.5 Sonnet** 表现出极高的运行稳定性，但对提示词改写较敏感。
    *   **ChatGPT 4o-mini** 在建立脚手架后表现最可控，错误具有系统性而非随机性。
    *   **Gemini 2.5 Flash** 表现出显著的随机性，拓扑结构在不同运行中波动剧烈。
*   **复杂性瓶颈：** 随着任务复杂度增加，所有模型在推理隐式行为模式（如观察者模式）时均表现不佳，说明 LLM 仍难以从动态行为描述中推导出复杂的架构结构。
*   **语义一致不等于结构一致：** 模型可能在语义上理解需求，但在图表拓扑结构上产生漂移。

#### 7. 优点
*   **视角独特：** 不仅关注“能不能生成”，更关注“生成得稳不稳定”，将可靠性作为核心评估维度。
*   **方法实用：** 提出的基于偏好的少样本提示法不需要昂贵的微调，是一种低成本提升 LLM 设计能力的有效手段。
*   **实验严谨：** 540 次实验覆盖了多模型、多策略和多变异源，结论具有较强的说服力。

#### 8. 不足与局限
*   **领域覆盖有限：** 仅测试了两个领域（医疗计费和传感器），结论的普适性有待在更多工业场景中验证。
*   **偏好数据集规模小：** 仅使用了 5 个示例，可能无法覆盖所有复杂的设计决策。
*   **人工评估偏差：** 尽管有专家审核，但对于“什么是好的设计”仍存在一定的主观性。
*   **模式推理局限：** 实验显示模型在处理行为驱动的复杂模式（如 Observer）时依然乏力，未能提供根本性的解决方案。

（完）
