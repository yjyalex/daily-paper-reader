---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v3"
tags: ["query:mas-gen"]
score: 8.0
evidence: 使用并行子智能体集群进行多智能体任务分解与技能蒸馏
tldr: Trace2Skill是一个旨在提升LLM智能体领域技能的框架。针对手动编写技能效率低及自动生成技能易过拟合的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并利用归纳推理将其整合为统一、无冲突的技能库。实验证明，该方法在电子表格、视觉问答等复杂任务中显著优于基线，且生成的技能具有极强的跨模型迁移能力和泛化性，无需参数更新即可显著增强模型能力。
selection_source: carryover_cache
motivation: 现有的LLM技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能碎片化且缺乏泛化性。
method: 通过并行子智能体分析多样化执行轨迹并提取经验，利用归纳推理将其层级化整合为统一、声明式的技能目录。
result: "在多个复杂领域显著提升了性能，且Qwen 35B生成的技能能使122B模型在WikiTableQuestions上提升57.65%。"
conclusion: 复杂的智能体经验可以被封装为高度可迁移、无需参数更新的声明式技能，且能通过较小规模的开源模型高效实现。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于处理复杂任务至关重要。然而，手动编写技能会产生严重的可扩展性瓶颈。相反，自动技能生成往往会产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一问题，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，然后将其提炼为一份单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基线模型有显著提升。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 任务上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用小至 35B 参数的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效且高质量地为大语言模型（LLM）智能体构建特定领域的技能（Skills）？
*   **研究背景**：
    *   **手动编写瓶颈**：人工撰写标准作业程序（SOP）难以扩展。
    *   **自动生成缺陷**：现有的自动生成方法要么仅依赖模型的参数化知识（缺乏领域细节），要么采用“在线顺序更新”模式，容易导致技能碎片化、过拟合于特定轨迹，且在大规模检索时存在困难。
*   **研究动机**：模仿人类专家的学习方式——通过并行分析大量执行经验，进行归纳推理，最终提炼出一份统一、全面且具有泛化性的技能指南。

#### 2. 方法论
Trace2Skill 采用三阶段流水线，核心思想是**并行分析与层级整合**：
*   **阶段 1：轨迹生成（Trajectory Generation）**
    *   智能体在任务集上并行运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
*   **阶段 2：并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**
    *   派遣大量子智能体并行处理轨迹。
    *   **成功分析师（Success Analyst）**：识别贡献成功的通用模式。
    *   **错误分析师（Error Analyst）**：采用 ReAct 式的多轮交互循环，通过检查文件、对比真值来诊断根因，并验证修复方案，确保补丁的可靠性。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**
    *   **层级合并**：将数百个局部补丁通过层级化方式合并。
    *   **归纳推理**：利用 LLM 识别在不同轨迹中反复出现的模式（Prevalent Patterns），优先保留这些具有普适性的经验，剔除偶然的、个体化的操作。
    *   **程序化校验**：通过格式检查和冲突检测（如避免对同一行进行矛盾修改）确保最终技能文档的连贯性。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格（Spreadsheet）**：SpreadsheetBench（验证集、全集）。
    *   **分布外泛化（OOD）**：WikiTableQuestions（将维基百科表格转为 Excel 格式）。
    *   **数学推理**：DAPO-Math, AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（处理表单、发票等图像）。
*   **对比基线（Baselines）**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的专家编写技能。
    *   **Parametric**：仅靠 LLM 自身知识生成的初始草案。
    *   **ReasoningBank**：基于检索的经验库方法（对比“提炼为文档”与“检索记忆”）。
    *   **Sequential Editing**：顺序更新技能的在线学习模式。
*   **跨模型评估**：测试由 Qwen3.5-35B 生成的技能是否能提升 Qwen3.5-122B 的表现。

#### 4. 资源与算力
*   **算力消耗**：
    *   **轨迹生成**：使用 122B 模型生成 200 条轨迹（每条 50+ 轮交互）耗时小于 2 GPU 小时。
    *   **技能整合**：并行模式下仅需约 3 分钟（8 卡 A800 节点），而顺序更新模式（Seq-B=1）则需约 60 分钟。
*   **模型使用**：主要使用开源的 Qwen3.5-35B-A3B 和 Qwen3.5-122B-A10B MoE 模型。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了电子表格、数学、视觉三大领域，涉及数百条轨迹的分析。
*   **消融实验**：
    *   对比了“并行合并”与“顺序编辑”的效果与效率。
    *   对比了“智能体化错误分析”与“单次 LLM 调用分析”的质量差异。
    *   验证了成功补丁与错误补丁各自的贡献。
*   **客观性**：通过跨模型（小模型教大模型）和跨领域（OOD）测试，证明了该方法并非简单的“记忆轨迹”，而是真正学习到了可迁移的知识，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **极强的迁移性**：35B 模型演化出的技能在 WikiTQ 任务上让 122B 模型提升了 **57.65%**。这挑战了“经验必须与特定模型绑定”的假设。
*   **优于人工与检索**：提炼后的技能在多个指标上超过了 Anthropic 官方的人工技能；且由于无需检索，避免了检索不准带来的干扰，表现优于 ReasoningBank。
*   **并行胜过顺序**：并行整合不仅效率高（20倍加速），且通过归纳推理生成的技能更具普适性，避免了顺序更新中的“上下文漂移”。
*   **反直觉发现**：在 DocVQA 中，执行能力强的模型（35B）不一定具备强的技能编写能力（归纳能力），而 122B 模型展现出更强的反思与总结能力。

#### 7. 优点
*   **架构无关**：生成的技能是纯文本 Markdown 目录，无需参数微调，无需外部检索模块，可无缝集成到任何智能体框架。
*   **高效性**：利用并行化显著降低了技能演化的时间成本。
*   **高质量诊断**：通过 Agentic Loop 进行错误分析，确保了提炼出的经验是经过验证的“真知灼见”。

#### 8. 不足与局限
*   **因果量化缺失**：目前尚无法精确量化每一个补丁对最终性能提升的边际贡献。
*   **动态利用率未知**：在推理过程中，智能体具体使用了技能文档中的哪些章节、哪些检查清单，目前缺乏细粒度的归因追踪。
*   **幻觉风险**：尽管有校验机制，但仍存在 LLM 在整合过程中产生错误引导或冗余信息的潜在风险。

（完）
