---
title: Multi-Agent LLMs for Adaptive Acquisition in Bayesian Optimization
title_zh: 贝叶斯优化中用于自适应采集的多智能体大语言模型
authors: "Andrea Carbonati, Mohammadsina Almasi, Hadis Anahideh"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28959v1"
tags: ["query:mas-gen"]
score: 8.0
evidence: 用于自适应采集和搜索策略学习的多智能体大模型
tldr: 本研究探讨了大语言模型在贝叶斯优化中处理探索与利用权衡的能力。针对单智能体在策略选择与候选生成同时进行时易产生认知负荷、导致搜索不稳和过早收敛的问题，提出了一种多智能体框架。该框架将策略中介（分配权重）与战术生成分离，使搜索决策显式化且可调。实验证明，这种解耦显著提升了LLM在连续优化任务中的搜索效率和鲁棒性。
selection_source: fresh_fetch
motivation: 单智能体LLM在同时进行策略选择和候选生成时面临认知负荷，导致搜索过程不稳定且容易过早收敛。
method: 提出一种多智能体框架，将搜索过程分解为负责分配准则权重的策略智能体和负责根据策略生成候选方案的生成智能体。
result: 实验结果表明，在多种连续优化基准测试中，该多智能体解耦方法显著优于传统的单智能体LLM搜索策略。
conclusion: 通过将战略决策与战术执行分离，可以使LLM的探索-利用决策更加显式、可控，并有效提升其黑盒优化性能。
---

## 摘要
探索与利用的权衡是序列决策和黑盒优化的核心，然而大语言模型（LLMs）如何对这种权衡进行推理和管理仍不为人所知。与贝叶斯优化（其中探索和利用通过采集函数显式编码）不同，基于 LLM 的优化依赖于对历史评估的隐式、基于提示的推理，这使得搜索行为难以分析或控制。在这项工作中，我们对 LLM 介导的搜索策略学习进行了指标层面的研究，探讨了 LLM 在探索的多种操作定义（包括信息量、多样性和代表性）下如何构建和调整探索-利用策略。我们表明，在单个提示中同时执行策略选择和候选生成的单智能体 LLM 方法会面临认知过载，导致搜索动态不稳定和过早收敛。为了解决这一局限性，我们提出了一个多智能体框架，将探索-利用控制分解为战略策略调解和战术候选生成。策略智能体为多个搜索标准分配可解释的权重，而生成智能体则根据由权重定义的搜索策略生成候选方案。这种分解使得探索-利用决策变得显式、可观察且可调节。在各种连续优化基准上的实证结果表明，将战略控制与候选生成分离显著提高了 LLM 介导搜索的有效性。

## Abstract
The exploration-exploitation trade-off is central to sequential decision-making and black-box optimization, yet how Large Language Models (LLMs) reason about and manage this trade-off remains poorly understood. Unlike Bayesian Optimization, where exploration and exploitation are explicitly encoded through acquisition functions, LLM-based optimization relies on implicit, prompt-based reasoning over historical evaluations, making search behavior difficult to analyze or control. In this work, we present a metric-level study of LLM-mediated search policy learning, studying how LLMs construct and adapt exploration-exploitation strategies under multiple operational definitions of exploration, including informativeness, diversity, and representativeness. We show that single-agent LLM approaches, which jointly perform strategy selection and candidate generation within a single prompt, suffer from cognitive overload, leading to unstable search dynamics and premature convergence. To address this limitation, we propose a multi-agent framework that decomposes exploration-exploitation control into strategic policy mediation and tactical candidate generation. A strategy agent assigns interpretable weights to multiple search criteria, while a generation agent produces candidates conditioned on the resulting search policy defined as weights. This decomposition renders exploration-exploitation decisions explicit, observable, and adjustable. Empirical results across various continuous optimization benchmarks indicate that separating strategic control from candidate generation substantially improves the effectiveness of LLM-mediated search.

---

## 论文详细总结（自动生成）

### 论文总结：贝叶斯优化中用于自适应采集的多智能体大语言模型

#### 1. 核心问题与整体含义（研究动机和背景）
在黑盒优化（Black-Box Optimization, BBO）中，平衡**探索（Exploration）**与**利用（Exploitation）**是核心挑战。传统的贝叶斯优化（BO）通过显式的采集函数（如 UCB, EI）来处理这种权衡，而新兴的基于大语言模型（LLM）的优化器通常依赖于隐式的提示词推理，导致搜索行为难以分析、控制且不稳定。
*   **研究动机**：单智能体 LLM 在同时处理“高层策略制定”（决定搜索方向）和“底层候选生成”（确定具体数值坐标）时，会产生**认知过载**，导致搜索动态不稳定、过早收敛或在数值精度上表现不佳。
*   **核心目标**：通过多智能体协作，将搜索策略的制定与具体候选点的生成解耦，使 LLM 的决策过程显式化、可解释且更具鲁棒性。

#### 2. 方法论：核心思想与算法流程
论文提出了一个**多智能体 LLM 框架**，模拟了传统 BO 中“采集函数设计”与“候选点优化”的分离：
*   **核心思想**：将优化任务分解为战略（Strategic）和战术（Tactical）两个层面。
*   **关键组件**：
    1.  **策略智能体（Strategy Agent）**：负责高层推理。它分析历史评估数据、搜索进度（是否停滞）和问题背景，输出一组可解释的**权重向量 ($w_t$)**。这些权重分配给四个准则：利用（Exploitation）、信息量（Informativeness）、多样性（Diversity）和代表性（Representativeness）。
    2.  **生成智能体（Generation Agent）**：负责执行。它接收策略智能体给出的权重，在这些权重的约束下生成具体的候选点坐标 ($x_t$)。
*   **算法流程**：
    1.  构建策略提示词（包含历史数据 $D_{t-1}$）。
    2.  策略智能体推断指标权重 $W_t$。
    3.  构建生成提示词（包含 $D_{t-1}$ 和 $W_t$）。
    4.  生成智能体产生候选点 $x_t$。
    5.  评估 $x_t$ 并更新历史记录，循环往复。

#### 3. 实验设计
*   **实验场景/数据集**：
    *   **Rosenbrock 函数**：经典的平滑解析基准测试，用于评估数值优化精度。
    *   **超参数调优（HPT）**：高维机器学习任务，涉及离散和连续混合空间。
    *   **机器人推物（Robot Pushing）**：复杂的工程控制问题，具有非凸和不连续的动力学特征。
*   **对比方法（Benchmarks）**：
    *   **传统 BO 方法**：期望改进（EI）和上置信界（UCB）。
    *   **单智能体 LLM**：在单个提示词中同时完成策略和生成的基准模型。
*   **模型使用**：所有实验均采用 **Llama-3.3-70b-instruct**。

#### 4. 资源与算力
*   **算力说明**：论文提到实验运行在伊利诺伊大学芝加哥分校（UIC）管理的计算基础设施上。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该方法基于上下文学习（In-context Learning），不涉及模型微调，因此算力消耗主要集中在 LLM 的推理调用上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   每个任务设置了固定的评估预算（30 次查询）。
    *   使用了固定随机种子以保证可重复性。
    *   进行了**消融实验**（配对指标消融），研究单一探索准则（如仅多样性 vs 仅代表性）对性能的影响。
*   **充分性评价**：实验覆盖了从简单解析函数到复杂工程模拟的多种维度，对比了传统最先进算法（SOTA）和单智能体基线。实验设计较为客观，通过追踪权重随时间的变化，提供了深入的机理分析。

#### 6. 主要结论与发现
*   **解耦的优越性**：多智能体框架显著优于单智能体 LLM，尤其是在复杂、弱结构的工程问题（如机器人推物）中，表现接近传统 BO。
*   **适应性搜索**：LLM 能够根据搜索进度动态调整策略。例如，当发现进度停滞时，策略智能体会自动增加“多样性”或“信息量”的权重。
*   **平滑景观的局限**：在 Rosenbrock 等需要极高数值精度的平滑函数上，LLM 仍然不如传统的 EI 或 UCB，因为 LLM 倾向于在进度缓慢时过度探索，而非进行精细的局部挖掘。
*   **认知负荷验证**：实验证实，强迫 LLM 同时处理数值生成和策略平衡会导致其表现出不稳定的搜索动态。

#### 7. 优点
*   **可解释性强**：通过显式的权重向量，研究者可以直观地看到 LLM 在每一轮迭代中是在“想”探索还是在“想”利用。
*   **模块化设计**：将战略与战术分离，使得系统更容易针对特定任务进行调整（例如更换不同的生成器或增加新的评价指标）。
*   **处理复杂任务的能力**：在非凸、不连续的现实工程问题中展现了比传统高斯过程（GP）更强的鲁棒性，因为后者往往受限于预设的核函数假设。

#### 8. 不足与局限
*   **数值精度瓶颈**：LLM 本质上是文本处理工具，在处理需要极高精度的连续空间数值优化时，仍难以完全替代基于数学梯度的传统方法。
*   **过度探索风险**：LLM 有时会将“缓慢的正确进展”误判为“停滞”，从而错误地转向大规模探索，导致收敛速度变慢。
*   **实验覆盖范围**：虽然涵盖了三个典型场景，但对于极高维度（如数百维）或极小样本量下的表现尚未深入探讨。
*   **提示词敏感性**：尽管使用了结构化模板，但 LLM 对提示词微小变动的敏感性（Prompt Sensitivity）仍是一个潜在的偏差风险。

（完）
