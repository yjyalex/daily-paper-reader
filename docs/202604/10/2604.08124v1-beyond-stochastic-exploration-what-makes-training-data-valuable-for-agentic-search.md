---
title: "Beyond Stochastic Exploration: What Makes Training Data Valuable for Agentic Search"
title_zh: 超越随机探索：探究训练数据对智能体搜索的价值所在
authors: "Chuzhan Hao, Wenfeng Feng, Guochao Jiang, Guofeng Quan, Guohua Liu, Yuewei Zhang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08124v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习提升大模型推理能力
tldr: 针对强化学习搜索智能体在推理时依赖随机探索导致效率低下和训练不稳定的问题，本文提出了Hierarchical Experience (HiExp) 框架。该方法通过对比分析和多级聚类从原始轨迹中提取层次化经验知识，并利用经验对齐训练将随机探索转化为经验驱动的策略性搜索。实验表明，该框架在复杂搜索和数学推理任务中显著提升了性能，并展现出极强的跨任务与跨算法泛化能力。
selection_source: fresh_fetch
motivation: 现有的强化学习搜索智能体因过度依赖随机探索和结果奖励，导致推理路径低效且训练过程不稳定。
method: 提出HiExp框架，利用对比分析和多级聚类提取层次化经验，并通过经验对齐训练来规范和引导智能体的探索过程。
result: 在多个复杂智能体搜索和数学推理基准测试中，该方法不仅大幅提升了模型性能，还表现出优异的跨任务和跨算法通用性。
conclusion: 通过将原始轨迹转化为结构化经验并引导训练，可以有效克服随机探索的局限性，提升大语言模型在复杂搜索任务中的推理效率和稳定性。
---

## 摘要
强化学习（RL）已成为通过战略性整合外部搜索引擎来提升大语言模型（LLMs）推理能力的有效方法。然而，目前的基于强化学习的搜索智能体通常依赖于由精心设计的奖励函数引导的随机探索过程，这导致了低效的推理轨迹和不稳定的训练。为了解决这些问题，我们提出了一种名为“分层经验”（Hierarchical Experience, HiExp）的新型框架，旨在增强搜索智能体的性能和训练稳定性。具体而言，我们通过对比分析和多级聚类机制提取经验知识，将原始推理轨迹转化为分层经验知识。通过利用经验对齐训练，我们有效地正则化了随机探索，使其演变为一种战略性的、经验驱动的搜索过程。在多个复杂的智能体搜索和数学推理基准测试上的广泛评估表明，我们的方法不仅实现了显著的性能提升，还表现出强大的跨任务和跨算法泛化能力。

## Abstract
Reinforcement learning (RL) has become an effective approach for advancing the reasoning capabilities of large language models (LLMs) through the strategic integration of external search engines. However, current RL-based search agents often rely on a process of stochastic exploration guided by carefully crafted outcome rewards, leading to inefficient reasoning trajectories and unstable training. To address these issues, we propose a novel framework, Hierarchical Experience (HiExp), to enhance the performance and training stability of search agents. Specifically, we extract empirical knowledge through contrastive analysis and a multi-level clustering mechanism, transforming raw reasoning trajectories into hierarchical experience knowledge. By leveraging experience-aligned training, we effectively regularize stochastic exploration, evolving it into a strategic and experience-driven search process. Extensive evaluations on multiple complex agentic search and mathematical reasoning benchmarks demonstrate that our approach not only achieves substantial performance gains but also exhibits strong cross-task and cross-algorithm generalization.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴云团队提出，旨在解决基于强化学习（RL）的搜索智能体在推理效率和训练稳定性方面的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的搜索智能体（如 Search-o1）主要通过强化学习（如 GRPO）在外部搜索环境中进行探索。
*   **核心问题**：
    *   **随机探索低效**：智能体往往依赖随机尝试，导致推理路径冗长、重复或陷入逻辑陷阱。
    *   **训练不稳定**：在多轮交互中，仅依靠最终结果奖励（Outcome Reward）难以提供一致的指导信号，导致训练波动大。
    *   **知识利用不足**：现有的方法未能充分挖掘训练过程中产生的“失败”和“成功”轨迹中的深层经验。

### 2. 方法论：HiExp 框架
HiExp（Hierarchical Experience）的核心思想是将原始的推理轨迹转化为**分层经验知识**，并以此引导强化学习过程。

*   **分层经验构建（离线阶段）**：
    1.  **对比蒸馏（Contrastive Distillation）**：对同一问题的多个采样轨迹进行对比，识别成功路径的关键决策点和失败路径的陷阱，生成“案例级经验”（E1）。
    2.  **多级聚类（Hierarchical Clustering）**：利用语义编码器将碎片化的经验向量化，通过凝聚层次聚类算法进行聚合。
        *   **E1 (Case-based)**：具体的实例级启发式信息。
        *   **E2 (Pattern-based)**：针对同类问题的模式化推理路径。
        *   **E3 (Strategy-based)**：高度抽象的全局搜索策略。
*   **经验对齐训练（在线阶段）**：
    *   在 RL 的 Rollout（采样）阶段，根据当前推理状态语义检索最相关的分层经验。
    *   将检索到的策略（E2/E3）注入系统提示词，将具体的启发式（E1）注入中间推理步骤。
    *   **优化目标**：在 GRPO 算法的基础上，通过经验引导减少无效搜索空间，使策略更新更加稳定。

### 3. 实验设计
*   **数据集**：
    *   **多跳问答（Multi-hop QA）**：HotpotQA, 2WikiMultiHopQA, Musique（域内）；Bamboogle, MoreHopQA, Frames（域外）。
    *   **数学推理**：AIME 2024/2025, AMC, MATH-500, Minerva, OlympiadBench。
*   **对比基准（Baselines）**：
    *   **前沿模型**：DeepSeek-R1, GPT-4o, Gemini-2.5-Pro, Qwen3-235B。
    *   **RAG 方法**：Vanilla RAG, IRCoT, Search-o1。
    *   **RL 搜索智能体**：ReSearch, R1-Searcher, StepSearch。
*   **评估指标**：F1 分数、CEM（覆盖精确匹配）、EM（精确匹配）以及 LLM-as-Judge 评分。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20-96G GPU**。
*   **训练开销**：
    *   **离线经验构建**：在 Musique 数据集上总计耗时小于 2 GPU 小时（约占总预算的 6%）。
    *   **RL 训练**：GRPO 训练 1 个 Epoch 大约需要 **36 GPU 小时**。
*   **推理引擎**：使用了 vLLM 和 SGLang 进行高效采样。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个问答数据集和 6 个数学基准，实验覆盖面广。
*   **消融实验**：详细对比了不同层级经验（E1/E2/E3）对性能的影响，验证了分层结构的必要性。
*   **泛化性验证**：进行了跨任务（问答到数学）、跨算法（GRPO 到 GSPO）、跨环境（本地库到 Web 搜索）的实验。
*   **客观性**：通过域外数据集（Out-of-Domain）测试，证明了模型并非单纯记忆训练数据，而是学习到了通用的搜索策略。

### 6. 主要结论与发现
*   **性能飞跃**：HiExp-Searcher 在 7B 和 32B 规模下，性能显著超过了同尺寸的 RL 智能体，甚至在多个指标上比肩或超越了 GPT-4 和 DeepSeek-R1 等巨型模型。
*   **训练稳定性**：引入经验引导后，优势函数（Advantage）和梯度的方差显著降低，验证奖励（Val Reward）上升更快且更平滑。
*   **自蒸馏优势**：实验发现，使用模型自身生成的经验进行训练（Self-distillation），效果略优于使用更强教师模型（如 Qwen-Max）生成的经验，说明经验的“兼容性”比绝对质量更重要。

### 7. 优点与亮点
*   **内生性知识挖掘**：不依赖外部事实库，而是通过对自身推理轨迹的“反思”和“聚类”来生成元知识。
*   **即插即用**：HiExp 框架可以作为插件集成到现有的 RL 算法（如 GRPO, PPO）中，具有很强的通用性。
*   **分层引导机制**：巧妙地解决了宏观策略引导与微观步骤修正之间的平衡问题。

### 8. 不足与局限
*   **静态经验限制**：目前的经验构建是离线完成的，属于“半解耦”模式。在训练过程中，经验库不会随着模型能力的提升而动态更新。
*   **计算延迟**：在推理阶段需要进行额外的语义检索（检索 HEK 库），虽然开销较小，但在极高并发场景下仍需优化。
*   **复杂任务依赖**：对于极度依赖实时信息的动态任务，离线提取的策略经验可能存在时效性滞后。

（完）
