---
title: "TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis"
title_zh: TTVS：通过测试时变分合成增强自探索强化学习
authors: "Sikai Bai, Haoxi Li, Jie Zhang, Yongjiang Liu, Song Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08468v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 针对大推理模型的带可验证奖励的强化学习
tldr: 针对大推理模型在缺乏标注奖励的领域难以进化的挑战，本文提出TTVS框架。该框架通过在线变分合成将静态测试查询转化为动态、多样的语义等价流，并结合测试时混合探索策略，使模型能够从无标注数据中自我进化。实验表明，TTVS在8种架构上均表现优异，仅凭无标注测试数据便超越了依赖大量高质量标注数据的监督强化学习方法。
selection_source: fresh_fetch
motivation: 现有的测试时自适应方法受限于静态查询集，容易导致模型过拟合于表面文本模式而非底层逻辑。
method: 引入TTVS框架，包含在线变分合成以动态增加查询多样性，以及测试时混合探索以平衡准确性与一致性。
result: TTVS在八种模型架构上均取得显著提升，性能优于现有的测试时自适应方法及部分有监督强化学习技术。
conclusion: TTVS证明了通过动态合成与自我探索，大推理模型可以在无监督的测试阶段实现持续的逻辑推理能力进化。
---

## 摘要
尽管由具有可验证奖励的强化学习（RLVR）驱动的大语言推理模型（LRMs）取得了显著进展，但在监督成本极高或无法获得的专业或新兴领域，这一范式受到了根本性的限制，这为测试时自适应带来了关键挑战。虽然现有的测试时方法提供了一种潜在的解决方案，但它们受限于从静态查询集中学习，存在对文本模式产生过拟合的风险。为了填补这一空白，我们引入了测试时变分合成（TTVS），这是一个新颖的框架，通过从无标签测试查询中动态增强训练流，使 LRMs 能够实现自我演化。TTVS 包含两个协同模块：(1) 在线变分合成，它将静态测试查询转化为多样化、语义等价变体的动态流，迫使模型学习底层的逻辑问题而非表面模式；(2) 测试时混合探索，它在合成变体之间平衡了准确率驱动的利用与一致性驱动的探索。大量实验表明，TTVS 在八种模型架构上均表现出卓越的性能。值得注意的是，仅使用无标签的测试时数据，TTVS 不仅超越了其他测试时自适应方法，还优于在海量高质量有标签数据上训练的基于监督强化学习的最先进技术。

## Abstract
Despite significant advances in Large Reasoning Models (LRMs) driven by reinforcement learning with verifiable rewards (RLVR), this paradigm is fundamentally limited in specialized or novel domains where such supervision is prohibitively expensive or unavailable, posing a key challenge for test-time adaptation. While existing test-time methods offer a potential solution, they are constrained by learning from static query sets, risking overfitting to textual patterns. To address this gap, we introduce Test-Time Variational Synthesis (TTVS), a novel framework that enables LRMs to self-evolve by dynamically augmenting the training stream from unlabeled test queries. TTVS comprises two synergistic modules: (1) Online Variational Synthesis, which transforms static test queries into a dynamic stream of diverse, semantically-equivalent variations, enforcing the model to learn underlying problem logic rather than superficial patterns; (2) Test-time Hybrid Exploration, which balances accuracy-driven exploitation with consistency-driven exploration across synthetic variants. Extensive experiments show TTVS yields superior performance across eight model architectures. Notably, using only unlabeled test-time data, TTVS not only surpasses other test-time adaptation methods but also outperforms state-of-the-art supervised RL-based techniques trained on vast, high-quality labeled data.

---

## 论文详细总结（自动生成）

### 论文总结：TTVS —— 通过测试时变分合成增强自探索强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**大推理模型（LRMs）在缺乏高质量标注数据或可验证奖励的专业领域（如医疗、航天）如何实现自我进化**。
*   **背景**：目前的推理模型主要依赖于带可验证奖励的强化学习（RLVR），这需要大规模的标注数据集。
*   **动机**：现有的测试时自适应（Test-time Adaptation）方法虽然能利用无标签数据，但通常局限于静态的查询集（Query Set），导致模型容易过拟合于特定的文本表面模式，而非掌握底层的逻辑推理。
*   **目标**：提出一种框架，使模型能在测试阶段通过动态合成多样化的训练流来实现自我演化。

#### 2. 方法论：核心思想与关键技术
TTVS（Test-Time Variational Synthesis）框架由两个协同模块组成：
*   **在线变分合成（Online Variational Synthesis）**：
    *   **伪标签生成**：对原始查询进行多次采样（Rollouts），通过多数投票（Majority Voting）确定共识答案作为伪标签。
    *   **变分增强**：利用模型自身将原始查询改写为多个语义等价但表述不同的变体。
    *   **在线过滤**：基于准确率范围（筛选掉太简单或太难的题目）和长度限制进行质量控制，确保训练流的有效性。
*   **测试时混合探索（Test-time Hybrid Exploration）**：
    *   **组内探索（IGE）**：针对单个查询（原始或合成）进行独立的 GRPO 优化，侧重于对特定问题的准确性挖掘（Exploitation）。
    *   **跨组探索（CGE）**：在语义等价的变体簇之间进行混合采样，并使用统一的跨组多数投票标签进行优化，侧重于推理逻辑的一致性（Consistency）。
*   **算法基础**：默认采用 **GRPO**（组相对策略优化），通过组内奖励的均值和标准差直接估计优势函数，无需显式的 Critic 模型。

#### 3. 实验设计
*   **数据集**：涵盖了数学与科学推理的核心基准，包括 **MATH-500**、**AIME-2024**、**AMC-2023** 和 **GPQA**（研究生水平科学问题）。
*   **模型家族**：在 8 种不同规模的模型上进行了验证，包括 **Qwen3** (1.7B/4B/8B)、**Qwen2.5** (1.5B/3B/7B) 和 **LLaMA** (3.2-3B/3.1-8B)。
*   **对比方法**：
    *   **有监督 RL 模型**：如 DeepSeek-R1-Distill (1.5B/7B)、OpenReasoner-Zero-7B、SimpleRL-Zero-7B（这些模型使用了 8.9K 到 800K 不等的标注数据）。
    *   **测试时强化学习基准**：TTRL（仅在原始无标签数据上进行 RL）。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 台 NVIDIA GeForce H800 80GB GPU** 上进行。
*   **训练细节**：
    *   优化器：AdamW，采用余弦学习率调度，峰值学习率 $5 \times 10^{-7}$。
    *   采样参数：Rollout 阶段采样 32 个响应，温度系数 0.6。
    *   训练时长：根据数据集大小设置 10 到 40 个 episodes。
*   **效率分析**：文中明确指出 TTVS 的显存占用与 TTRL 相当，且生成的答案更简洁（Token 长度更短），推理效率略高。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，不仅覆盖了 8 种架构，还包括：
    *   **消融实验**：验证了 IGE 和 CGE 模块的各自贡献。
    *   **算法兼容性**：测试了在 GRPO、OPO 和 DAPO 等不同 RL 算法下的表现。
    *   **超参数敏感性**：分析了过滤阈值、Token 长度限制、预热步数等。
    *   **鲁棒性验证**：在全新的 DeepMath-103K 子集（防污染测试）和医疗领域数据集（MedMCQA）上进行了额外验证。
*   **评价**：实验设计非常充分且客观，通过多维度对比证明了方法在不同难度（L1-L5）题目下的稳健性。

#### 6. 主要结论与发现
*   **性能超越**：TTVS 仅使用无标签测试数据，在多个指标上不仅超过了现有的测试时方法，甚至击败了使用大量高质量标注数据训练的有监督 RL 模型（如 DeepSeek-R1-Distill）。
*   **逻辑学习优于模式匹配**：通过变分合成，模型被迫学习底层的逻辑一致性，而非记忆特定的文本表述。
*   **难度正相关**：题目越难，TTVS 相比于传统测试时方法的领先优势越明显（在 MATH-500 L5 难度下提升最显著）。

#### 7. 优点
*   **动态性**：打破了静态测试集的限制，通过在线合成创造了无限的训练流。
*   **双重探索策略**：IGE 和 CGE 的结合有效平衡了“解对当前题”和“逻辑一贯性”两个目标。
*   **高效率**：在不增加并行计算开销的前提下，通过提升数据质量获得了巨大的性能增益。
*   **通用性**：不依赖外部专家模型（如 GPT-4），实现了真正的“自我进化”。

#### 8. 不足与局限
*   **模型规模限制**：受限于算力，实验主要集中在 8B 及以下规模的模型，尚未在 32B 或 70B 以上的超大规模模型上验证。
*   **模态局限**：目前仅限于纯文本推理，未涉及多模态（如视觉推理）场景。
*   **奖励依赖**：虽然讨论了非可验证场景（如 LM-as-a-Judge），但核心实验仍依赖于具有明确答案的客观题，对于主观性强的任务（如创意写作）适用性有待进一步探索。

（完）
