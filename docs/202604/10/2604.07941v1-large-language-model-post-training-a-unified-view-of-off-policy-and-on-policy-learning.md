---
title: "Large Language Model Post-Training: A Unified View of Off-Policy and On-Policy Learning"
title_zh: 大语言模型后训练：离策与在策学习的统一视角
authors: "Shiwan Zhao, Zhihu Wang, Xuyang Zhao, Jiaming Zhou, Caiyue Xu, Chenfei Liu, Liting Zhang, Yuhang Jia, Yanzhe Zhang, Hualong Yu, Zichen Xu, Qicheng Li, Yong Qin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07941v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: LLM 后训练中强化学习、偏好优化和过程监督的统一视角
tldr: 本综述为大语言模型（LLM）后训练提供了一个统一视角，将其视为对模型行为的结构化干预。文章突破了传统的SFT、RL等碎片化分类，根据轨迹来源（离策与在策）以及功能角色（支持扩展、策略重塑、行为巩固）对现有方法进行重构。该框架揭示了不同训练阶段如何协同解决行为瓶颈，强调了后训练已从单一目标优化演变为复杂的系统协同设计。
selection_source: fresh_fetch
motivation: 现有的LLM后训练方法分类零散，缺乏从行为瓶颈角度出发的统一理论框架来指导模型对齐与部署。
method: 提出以轨迹来源（离策/在策）为核心，结合支持扩展、策略重塑和行为巩固三大功能角色的分类体系。
result: 该框架统一解释了SFT、偏好优化、强化学习和蒸馏等技术，并阐明了它们在多阶段流水线中的协同机制。
conclusion: LLM后训练的进步日益依赖于多阶段任务的协调系统设计，而非单一主导的优化目标。
---

## 摘要
后训练已成为将预训练大语言模型（LLM）转化为对齐且可部署系统的核心环节。近期的进展涵盖了监督微调（SFT）、偏好优化、强化学习（RL）、过程监督、验证器引导方法、蒸馏以及多阶段流水线。然而，这些方法通常以碎片化的方式被讨论，按标签或目标函数族进行组织，而非根据它们所解决的行为瓶颈。本综述认为，LLM 后训练最好被理解为对模型行为的结构化干预。我们首先根据轨迹来源（trajectory provenance）对该领域进行组织，这定义了两种主要的学习范式：基于外部提供轨迹的离策（off-policy）学习，以及基于学习者生成采样（rollouts）的在策（on-policy）学习。随后，我们通过两个反复出现的角色来解释这些方法——有效支撑集扩展（effective support expansion），使有用的行为更易触达；以及策略重塑（policy reshaping），改善已触达区域内的行为——此外还有一个互补的系统级角色，即行为巩固（behavioral consolidation），用于在不同阶段和模型转换之间保留、迁移和摊销行为。这一视角为主要范式提供了一种统一的解读：SFT 既可用于支撑集扩展也可用于策略重塑，而基于偏好的方法通常属于离策重塑；在策强化学习通常改善学习者生成状态下的行为，但在强引导下也能使难触达的推理路径变得可行；蒸馏通常被理解为巩固而非仅仅是压缩，而混合流水线则表现为协调的多阶段组合。总的来说，该框架有助于诊断后训练瓶颈并推导阶段组合，这表明 LLM 后训练的进展日益取决于协调的系统设计，而非任何单一的主导目标。

## Abstract
Post-training has become central to turning pretrained large language models (LLMs) into aligned and deployable systems. Recent progress spans supervised fine-tuning (SFT), preference optimization, reinforcement learning (RL), process supervision, verifier-guided methods, distillation, and multi-stage pipelines. Yet these methods are often discussed in fragmented ways, organized by labels or objective families rather than by the behavioral bottlenecks they address.   This survey argues that LLM post-training is best understood as structured intervention on model behavior. We organize the field first by trajectory provenance, which defines two primary learning regimes: off-policy learning on externally supplied trajectories, and on-policy learning on learner-generated rollouts. We then interpret methods through two recurring roles -- effective support expansion, which makes useful behaviors more reachable, and policy reshaping, which improves behavior within already reachable regions -- together with a complementary systems-level role, behavioral consolidation, which preserves, transfers, and amortizes behavior across stages and model transitions.   This perspective yields a unified reading of major paradigms. SFT may serve either support expansion or policy reshaping, whereas preference-based methods are usually off-policy reshaping. On-policy RL often improves behavior on learner-generated states, though under stronger guidance it can also make hard-to-reach reasoning paths reachable. Distillation is often best understood as consolidation rather than only compression, and hybrid pipelines emerge as coordinated multi-stage compositions.   Overall, the framework helps diagnose post-training bottlenecks and reason about stage composition, suggesting that progress in LLM post-training increasingly depends on coordinated system design rather than any single dominant objective.

---

## 论文详细总结（自动生成）

这篇论文《Large Language Model Post-Training: A Unified View of Off-Policy and On-Policy Learning》对大语言模型（LLM）后训练（Post-training）领域进行了系统性的重构与综述。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前 LLM 后训练方法（如 SFT、DPO、RLHF、蒸馏等）的研究高度碎片化，通常仅按算法名称或损失函数分类，忽略了它们在干预模型行为时的底层逻辑。
*   **研究动机**：随着后训练从简单的指令微调演变为复杂的多阶段流水线（如 DeepSeek-R1、Llama 3），迫切需要一个统一的框架来解释：
    *   不同方法究竟在改变模型的什么行为？
    *   为什么需要将多种方法组合成混合流水线？
    *   如何诊断后训练中的瓶颈（是模型“不会”还是“选不对”）？

### 2. 方法论：核心思想与技术细节
论文提出以**轨迹来源（Trajectory Provenance）**为顶层轴，结合**功能角色（Functional Roles）**的统一框架：

*   **两大训练范式（按轨迹来源分类）**：
    *   **离策学习（Off-policy Learning）**：在外部提供的轨迹上更新模型（如专家演示、离线偏好对）。优点是能引入模型尚未掌握的新行为，缺点是无法直接纠正模型在自身采样状态下的错误。
    *   **在策学习（On-policy Learning）**：在模型自身生成的采样（Rollouts）上更新。优点是能直接针对模型在部署时会遇到的状态进行纠正，缺点是难以发现模型采样空间之外的优秀行为。
*   **三大功能角色（行为干预视角）**：
    1.  **有效支撑集扩展（Effective Support Expansion）**：使原本概率极低、不可达的有用行为变得可达（即让模型“学会”新技能或新推理路径）。
    2.  **策略重塑（Policy Reshaping）**：在模型已经能产生的行为中，重新分配概率权重（即让模型在已知选项中“选得更好”或更符合偏好）。
    3.  **行为巩固（Behavioral Consolidation）**：系统级角色，旨在跨阶段、跨模型保留和迁移有用行为（如将复杂的搜索行为蒸馏到小模型中）。
*   **监督接口（Supervision Interfaces）**：作为次级描述层，包括 Token 目标、偏好对、奖励信号、验证器反馈和教师引导目标。

### 3. 实验设计与对比
*   **研究性质**：本论文是一篇**概念驱动的综述论文**，而非实验性论文。它没有进行单一的新实验，而是通过对现有前沿系统（如 Llama 3, DeepSeek-V3/R1, GLM-5, Kimi K2 等）的技术报告和学术文献进行深度合成分析。
*   **对比对象**：论文对比了 SFT、偏好优化（DPO/SimPO）、在线强化学习（RLHF/RLVR）、过程监督（PRM）、迭代自改进以及知识蒸馏等主流范式。
*   **分析维度**：通过“轨迹来源、训练状态、接口、主导角色、典型风险”等维度对上述方法进行了横向对比（见文中 Table 6）。

### 4. 资源与算力
*   **算力说明**：由于本文是综述性质，**文中未提及作者自身训练模型所消耗的具体算力**。
*   **相关讨论**：论文深入讨论了不同范式的算力成本差异。例如，指出在策 RL（On-policy RL）由于需要频繁进行在线采样和奖励评估，其算力成本远高于离线偏好优化；同时讨论了通过“行为巩固”（蒸馏）来降低推理成本的系统设计趋势。

### 5. 实验数量与充分性
*   **文献覆盖**：论文引用了 132 篇高质量参考文献，涵盖了从早期的指令微调到 2025-2026 年最新的推理模型（如 DeepSeek-R1）。
*   **客观性与公平性**：论文通过建立数学化的“状态-动作占用度量（Occupancy Measure）”来定义有效支撑集，尝试为定性分析提供定量基础。其对各方法的优缺点评价较为中立，强调了“没有单一主导目标”，而是取决于如何协调多阶段设计。

### 6. 主要结论与发现
*   **后训练是系统工程**：后训练不再是单一目标的优化，而是“支撑集扩展 -> 策略重塑 -> 行为巩固”的协调过程。
*   **离策与在策的互补性**：离策方法（如 SFT）擅长引入新行为（扩展支撑集），而在策方法（如 RL）擅长纠正模型自身的 rollout 错误（重塑策略）。
*   **蒸馏的新定义**：蒸馏不应仅被视为压缩技术，而应被视为后训练流水线中的“行为巩固”机制，负责将昂贵的搜索或教师能力内化。
*   **混合流水线的必然性**：强力模型必然采用多阶段设计，以解决不同阶段出现的行为瓶颈（如先通过 SFT 建立基础，再通过 RL 纠正分布漂移）。

### 7. 优点（亮点）
*   **视角独特**：突破了传统的算法分类，从“行为干预”和“轨迹来源”切入，解释了为什么某些方法在特定任务（如推理）中更有效。
*   **理论联系实际**：成功解释了 DeepSeek-R1 等最新模型成功的底层逻辑（如 RL 如何在没有 SFT 的情况下通过强验证器实现支撑集扩展）。
*   **前瞻性**：提出了“行为巩固”这一系统级概念，为未来模型的小型化和代理化（Agentic）设计提供了理论指导。

### 8. 不足与局限
*   **缺乏定量验证**：作为综述，虽然提出了“有效支撑集”等概念，但缺乏大规模的实证实验来精确测量这些指标在不同算法下的变化。
*   **测量难度**：论文也承认，在实际操作中，很难精确区分一个增益究竟来自于“支撑集扩展”还是“策略重塑”（即模型是刚学会还是被激活了潜能）。
*   **应用限制**：该框架主要适用于通用能力和推理能力的对齐，对于多模态或极长文本等特定领域的后训练行为分析覆盖较少。

（完）
