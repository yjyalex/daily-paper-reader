---
title: "SUPERNOVA: Eliciting General Reasoning in LLMs with Reinforcement Learning on Natural Instructions"
title_zh: SUPERNOVA：通过自然指令上的强化学习激发大语言模型的通用推理能力
authors: "Ashima Suvarna, Kendrick Phan, Mehrab Beikzadeh, Hritik Bansal, Saadia Gabriel"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08477v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 在自然指令上使用可验证奖励的强化学习来增强 LLM 的通用推理
tldr: "针对大语言模型在通用推理（如因果、时序理解）中缺乏可验证奖励强化学习（RLVR）训练数据的问题，本文提出 SUPERNOVA 框架。该框架通过系统性地将专家标注的指令微调数据转化为 RLVR 可用的格式，并深入研究了任务选择、混合策略及合成干预对性能的影响。实验表明，SUPERNOVA 在 BBEH 等多个推理基准上显著优于 Qwen3.5 等强基线模型，最高提升达 52.8%，为扩展 RLVR 到通用推理领域提供了有效的数据构建路径。"
selection_source: fresh_fetch
motivation: 现有的 RLVR 技术受限于缺乏高质量、可验证的通用推理训练数据，导致模型在数学和代码之外的推理任务中表现不佳。
method: 提出 SUPERNOVA 框架，通过 100 多项受控实验探索如何从指令微调数据中筛选、混合并优化任务，以构建适用于 RLVR 的通用推理训练集。
result: "采用 SUPERNOVA 训练的模型在 BBEH、Zebralogic 和 MMLU-Pro 等基准测试中表现优异，其中在 BBEH 上的相对提升高达 52.8%。"
conclusion: 科学的数据筛选与转化策略是提升 LLM 通用推理能力的关键，SUPERNOVA 为利用人类标注资源扩展 RLVR 提供了实践指南。
---

## 摘要
带有可验证奖励的强化学习（RLVR）显著提升了大语言模型（LLM）在数学和代码等形式化领域的推理能力。尽管取得了这些进展，LLM 在需要因果推理和时间理解等能力的通用推理任务中仍然表现不佳。将 RLVR 扩展到通用推理领域，从根本上受到缺乏涵盖多样化推理技能的高质量、可验证训练数据的限制。为了应对这一挑战，我们提出了 SUPERNOVA，这是一个旨在增强通用推理能力的 RLVR 数据策展框架。我们的核心见解是，包含专家标注标准答案的指令微调数据集编码了丰富的推理模式，可以系统地调整以用于 RLVR。为了研究这一点，我们进行了 100 多项受控强化学习实验，以分析数据设计选择如何影响下游推理性能。特别是，我们研究了三个关键因素：(i) 源任务选择，(ii) 任务混合策略，以及 (iii) 用于提高数据质量的合成干预。我们的分析表明，源任务的选择并非易事，且对下游推理性能有显著影响。此外，基于单个目标任务的表现来选择任务，优于基于整体平均表现的策略。最后，在 SUPERNOVA 上训练的模型在包括 BBEH、Zebralogic 和 MMLU-Pro 在内的具有挑战性的推理基准测试中，优于强基准模型（例如 Qwen3.5）。特别是，在不同模型规模下，在 SUPERNOVA 上训练使 BBEH 的相对提升高达 52.8%，证明了针对 RLVR 进行原则性数据策展的有效性。我们的发现为策展人工标注资源以将 RLVR 扩展到通用推理提供了实践见解。代码和数据可在 https://github.com/asuvarna31/supernova 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly improved large language model (LLM) reasoning in formal domains such as mathematics and code. Despite these advancements, LLMs still struggle with general reasoning tasks requiring capabilities such as causal inference and temporal understanding. Extending RLVR to general reasoning is fundamentally constrained by the lack of high-quality, verifiable training data that spans diverse reasoning skills. To address this challenge, we propose SUPERNOVA, a data curation framework for RLVR aimed at enhancing general reasoning. Our key insight is that instruction-tuning datasets containing expert-annotated ground-truth encode rich reasoning patterns that can be systematically adapted for RLVR. To study this, we conduct 100+ controlled RL experiments to analyze how data design choices impact downstream reasoning performance. In particular, we investigate three key factors: (i) source task selection, (ii) task mixing strategies, and (iii) synthetic interventions for improving data quality. Our analysis reveals that source task selection is non-trivial and has a significant impact on downstream reasoning performance. Moreover, selecting tasks based on their performance for individual target tasks outperforms strategies based on overall average performance. Finally, models trained on SUPERNOVA outperform strong baselines (e.g., Qwen3.5) on challenging reasoning benchmarks including BBEH, Zebralogic, and MMLU-Pro. In particular, training on SUPERNOVA yields relative improvements of up to 52.8\% on BBEH across model sizes, demonstrating the effectiveness of principled data curation for RLVR. Our findings provide practical insights for curating human-annotated resources to extend RLVR to general reasoning. The code and data is available at https://github.com/asuvarna31/supernova.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SUPERNOVA** 的框架，旨在通过对自然指令数据集进行系统性的策展和强化学习，提升大语言模型（LLM）的通用推理能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然带有可验证奖励的强化学习（RLVR）在数学和代码领域取得了巨大成功，但在**通用推理**（如因果推断、时序理解、逻辑演绎）方面进展缓慢。
*   **研究动机**：
    *   **数据瓶颈**：通用推理缺乏高质量、易于自动验证的标准答案数据。
    *   **能力退化**：现有的 STEM 导向型推理模型（如专门针对数学训练的模型）在通用推理任务（如 BBEH 基准）上往往表现出性能下降。
    *   **资源浪费**：现有的指令微调数据集（如 SuperNI）包含丰富的人工标注推理模式，但由于格式不统一或难以验证，未能在 RLVR 中得到充分利用。

### 2. 方法论：SUPERNOVA 框架
SUPERNOVA 是一个多阶段的数据策展流水线，核心思想是将非结构化的指令数据转化为高质量的 RLVR 训练集：
*   **任务选择（Task Selection）**：
    *   **重构**：利用 LLM 将开放式指令重构为可验证的格式（如多选题 MCQ 或唯一值填空）。
    *   **过滤**：通过计算胜率（Win-rate）剔除模型已完全掌握（太简单）或完全无法处理（太难）的题目。
    *   **排名**：通过在候选任务上进行小规模 RL 训练并评估其对下游任务的增益，对任务进行效用排名。
*   **混合策略（Mixing Strategies）**：
    *   **Macro Mixing**：根据所有子任务的平均表现选择排名靠前的任务。
    *   **Micro Mixing**（核心创新）：针对每个特定的推理子任务（如时序、逻辑）分别选择表现最好的源任务，然后取并集。
*   **数据干预（Data Interventions）**：尝试通过合成手段增加难度，如引入长上下文依赖、反直觉先验、多跳推理等。
*   **算法流程**：采用 **GRPO**（Group Relative Policy Optimization）算法进行强化学习优化，跳过 KL 惩罚以提高训练效率。

### 3. 实验设计
*   **数据集**：源数据来自 **SuperNI**（1600个任务中筛选出83个候选任务）。
*   **评估基准（Benchmarks）**：
    *   核心验证集：**BBEH**（BigBench Extra Hard）的 23 个子任务。
    *   泛化测试集：**BBH**、**Zebralogic**（逻辑谜题）、**MMLU-Pro** 和 **MATH500**。
*   **对比方法**：
    *   基座模型：Qwen3 (0.6B, 1.7B, 4B), Qwen3.5-2B, Llama 3.2-3B。
    *   强基线模型：General-Reasoner, OpenThinker, Olmo-3-Think, OpenReasoning-Nemotron 等。
    *   数据集对比：DAPO (数学), Nemotron-CrossThink (多领域)。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   数据策展实验：每个任务运行 250 个 RL 步数。
    *   最终模型训练：运行 5000 个 RL 步数（约 1 个 epoch），涉及 10,000 个 Prompt。
    *   学习率：1e-6 至 4e-6 不等。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **100 多项受控 RL 实验**，涵盖了不同的任务选择、混合策略和干预手段。
*   **充分性**：实验设计非常系统，不仅在不同规模的模型（0.6B 到 4B）上验证了有效性，还跨越了不同的模型家族（Qwen 和 Llama），证明了结论的普适性。对比实验考虑了计算量匹配（Compute-matched），确保了公平性。

### 6. 主要结论与发现
*   **任务选择至关重要**：不同源任务对下游推理的影响差异巨大（最高与最低相差 7.6 个百分点），且语义相似度并非良好的筛选指标。
*   **Micro Mixing 优于 Macro Mixing**：针对特定技能定制数据混合方案比简单的全局平均筛选更有效。
*   **合成干预效果有限**：在固定计算预算下，简单的合成难度增加（如长上下文）并未带来显著增益，高质量的原始人工标注数据更具优势。
*   **以小博大**：经过 SUPERNOVA 训练的 **4B 模型在 Pass@8 指标上超过了 8B 的基座模型**，且在 BBEH 上的相对提升高达 52.8%。

### 7. 优点与亮点
*   **方法实用性强**：提供了一套将现有指令微调资源转化为 RL 训练数据的标准化流程。
*   **深入的消融研究**：通过大量实验揭示了数据策展中的“坑”（如相似度指标的失效、合成干预的局限）。
*   **显著的性能提升**：在逻辑性极强的 Zebralogic 等基准上表现突出，证明了通用推理能力的真正激发。

### 8. 不足与局限
*   **基准局限性**：主要依赖学术基准测试，可能无法完全捕捉现实世界中复杂且非结构化的推理场景。
*   **算力约束**：实验主要在中小规模模型（≤4B）上进行，尚未在大规模模型（如 70B+）上验证扩展定律。
*   **验证器依赖**：虽然将任务转化为了可验证格式，但对于某些极其主观的通用推理任务，自动验证（Exact Match）可能仍存在局限。

（完）
