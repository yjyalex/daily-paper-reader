---
title: "Less Approximates More: Harmonizing Performance and Confidence Faithfulness via Hybrid Post-Training for High-Stakes Tasks"
title_zh: 以少胜多：通过混合后训练协调高风险任务中的性能与置信度忠实性
authors: "Haokai Ma, Lee Yan Zhen, Gang Yang, Yunshan Ma, Ee-Chien Chang, Tat-Seng Chua"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08454v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于内部反馈的强化学习用于推理轨迹
tldr: 针对高风险任务中大模型“自信地犯错”的问题，本文提出HyTuning混合后训练框架。该框架引入渐进式推理增益（PRG）指标，动态平衡推理蒸馏与内部反馈强化学习，利用少量高质量轨迹作为锚点并结合海量无标签数据，在提升模型准确率的同时显著增强了置信度忠实度，实现了“以少胜多”的优化效果。
selection_source: fresh_fetch
motivation: 旨在解决大模型在高风险任务中因过度自信导致错误推断，以及高质量训练数据稀缺和错误更新放大等挑战。
method: 提出渐进式推理增益（PRG）指标来衡量推理步骤的支撑强度，并据此自适应地加权融合推理蒸馏（RD）与内部反馈强化学习（RLIF）。
result: 在多个领域及通用基准测试中，HyTuning在有限监督下不仅提高了模型精度，还实现了更优的置信度忠实度。
conclusion: 研究证明了通过混合后训练框架合理利用少量监督数据，可以有效协调模型性能与置信度可靠性之间的关系。
---

## 摘要
大语言模型正日益广泛地应用于高风险任务，在这些任务中，自信但错误的推理可能会导致严重的现实危害，这使得此前被忽视的置信度忠实性问题重新受到关注。一种极具前景的解决方案是将无监督的内部反馈强化学习（RLIF）与基于推理链引导的推理蒸馏（RD）进行联合优化，但这面临着三个持久的挑战：高质量训练语料的稀缺、缺乏事实依据的过度自信，以及会放大错误更新的无差别融合。受人类从不确定向确定积累置信度的过程启发，我们提出了渐进式推理增益（PRG），用于衡量推理步骤是否逐步增强了对最终答案的支持。此外，我们引入了 HyTuning，这是一种混合后训练框架，它通过类 PRG 指标自适应地调整 RD 和 RLIF 的权重，利用稀缺的有监督推理链作为稳定锚点，同时利用海量的无标签查询来提升可扩展性。在多个领域特定及通用基准测试上的实验表明，HyTuning 在提高准确率的同时，在有限的监督下实现了置信度忠实性，验证了实际的“以少胜多”效应。

## Abstract
Large language models are increasingly deployed in high-stakes tasks, where confident yet incorrect inferences may cause severe real-world harm, bringing the previously overlooked issue of confidence faithfulness back to the forefront. A promising solution is to jointly optimize unsupervised Reinforcement Learning from Internal Feedback (RLIF) with reasoning-trace-guided Reasoning Distillation (RD), which may face three persistent challenges: scarcity of high-quality training corpora, factually unwarranted overconfidence and indiscriminate fusion that amplifies erroneous updates. Inspired by the human confidence accumulation from uncertainty to certainty, we propose Progressive Reasoning Gain (PRG) to measure whether reasoning steps progressively strengthen support for the final answer. Furthermore, we introduce HyTuning, a hybrid post-training framework that adaptively reweights RD and RLIF via a PRG-style metric, using scarce supervised reasoning traces as a stable anchor while exploiting abundant unlabeled queries for scalability. Experiments on several domain-specific and general benchmarks demonstrate that HyTuning improves accuracy while achieving confidence faithfulness under limited supervision, supporting a practical "Less Approximates More" effect.

---

## 论文详细总结（自动生成）

# 论文总结：《Less Approximates More: Harmonizing Performance and Confidence Faithfulness via Hybrid Post-Training for High-Stakes Tasks》

## 1. 核心问题与整体含义
本文针对大语言模型（LLMs）在网络安全、医疗、金融等**高风险任务（High-Stakes Tasks）**中存在的“自信地犯错”（Confidently-wrong）问题展开研究。这种现象被称为缺乏**置信度忠实性（Confidence Faithfulness）**。

研究动机在于：
*   **数据稀缺性**：高风险领域的高质量标注数据和推理链（Reasoning Traces）极度匮乏且获取成本高。
*   **过度自信风险**：现有的后训练方法（如纯强化学习 RLIF）往往会导致模型为了生成流畅答案而产生事实不符的过度自信。
*   **优化失衡**：传统的监督微调（SFT）或推理蒸馏（RD）虽能提升精度，但无法有效校准模型的置信度。

## 2. 方法论
论文提出了 **HyTuning**，一个结合了推理蒸馏（RD）和内部反馈强化学习（RLIF）的混合后训练框架。

### 核心思想
模拟人类从不确定到确定的认知过程，引入**渐进式推理增益（Progressive Reasoning Gain, PRG）**来衡量推理步骤是否真正增强了对最终答案的支持。

### 关键技术细节
1.  **PRG 指标**：通过计算推理过程中每一步对目标答案预测概率的对数增益（取正值部分），量化推理链的质量。
    *   公式逻辑：$P = \frac{1}{T} \sum ReLU(\log p(y|q, o_{\le t}) - \log p(y|q, o_{\le t-1}))$。
2.  **自适应加权机制**：
    *   当 PRG 分数高时，表明推理路径自洽且有效，此时加大 RLIF 的权重，利用模型内部反馈进行自我强化。
    *   当 PRG 分数低或不稳定时，表明内部信号不可靠，此时加大 RD 的权重，利用稀缺的高质量监督数据作为“锚点”来稳定训练。
3.  **随机正则化**：在 RD 过程中引入少量无标签查询的“伪目标”作为随机噪声，提升模型在数据极度不平衡下的鲁棒性。

## 3. 实验设计
### 数据集与场景
实验主要集中在**网络安全**领域（典型的高风险场景）：
*   **ASBench**：攻击序列推理。
*   **CSEBenchmark**：网络安全专家知识与漏洞分析。
*   **CyberMetric-500**：基于 RAG 的安全知识评估。
*   **MMLU**：通用领域基准，用于验证模型是否发生灾难性遗忘。

### 对比方法
对比了 6 种主流后训练策略：
*   **SFT**（监督微调）、**RD**（推理蒸馏）。
*   **RLVR**（基于可验证奖励的强化学习）。
*   **INTUITOR**（代表性的 RLIF 方法）。
*   **RLPR**、**HPT**（混合后训练基准）。

## 4. 资源与算力
*   **硬件环境**：单节点配置 **2 张 NVIDIA H200 GPU**（每张显存 141GB）。
*   **训练配置**：使用 AdamW 优化器，全局 Batch Size 为 64，学习率 3e-6，训练 1 个 epoch。
*   **推理引擎**：使用 vLLM 提升采样效率，强化学习阶段每条提示词生成 8 个 rollout。

## 5. 实验数量与充分性
论文进行了多维度的深入实验，具有较高的充分性和客观性：
*   **性能对比**：在 4 个基准测试上进行了全量对比。
*   **置信度分析**：利用语义熵（Semantic Entropy）等指标验证了模型在高置信度下的准确率。
*   **消融实验**：分别去除了 RLIF、RD 和自适应重加权模块，验证各组件必要性。
*   **缩放实验（Scaling Study）**：
    *   **数据规模**：从 100 条到全量数据的性能演变。
    *   **模型规模**：涵盖 Qwen2.5-3B、7B 和 14B 三个量级。
*   **动力学分析**：监控了训练过程中 PRG 分数、自置信度及过渡词频率的变化。

## 6. 主要结论与发现
1.  **“以少胜多”（Less Approximates More）**：HyTuning 仅使用 **100 条**高质量监督数据，其效果即可接近甚至超过使用全量数据训练的其他基准方法。
2.  **协调性能与忠实性**：HyTuning 在提升准确率的同时，有效抑制了 RLIF 常见的过度自信坍塌，使模型在高置信度时的表现更加可靠。
3.  **稳定性**：相比于纯强化学习，HyTuning 的训练曲线更加平滑，且在通用任务（MMLU）上保持了良好的泛化能力。

## 7. 优点
*   **指标创新**：PRG 指标直观且有效地量化了推理链的“逻辑贡献度”。
*   **数据效率极高**：通过混合策略极大缓解了高风险领域标注数据匮乏的痛点。
*   **理论支撑**：论文从贝叶斯后验锐化的角度对 PRG 加权进行了理论推导，证明了其优化方向的正确性。

## 8. 不足与局限
*   **领域覆盖**：虽然在网络安全领域表现优异，但尚未在医疗、法律等其他高风险领域进行大规模验证。
*   **种子数据依赖**：虽然所需数据量少，但对初始的 100 条推理链质量要求极高，若种子数据存在偏差，可能影响锚定效果。
*   **计算开销**：由于涉及强化学习的多次采样（rollouts），其训练阶段的计算资源消耗仍显著高于传统的 SFT。

（完）
