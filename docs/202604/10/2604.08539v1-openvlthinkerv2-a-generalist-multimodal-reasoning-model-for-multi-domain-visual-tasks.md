---
title: "OpenVLThinkerV2: A Generalist Multimodal Reasoning Model for Multi-domain Visual Tasks"
title_zh: OpenVLThinkerV2：一种面向多领域视觉任务的通用多模态推理模型
authors: "Wenbo Hu, Xin Chen, Yan Gao-Tian, Yihe Deng, Nanyun Peng, Kai-Wei Chang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08539v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于多模态推理的高斯 GRPO
tldr: OpenVLThinkerV2是一个通用的多模态推理模型，旨在解决强化学习在多领域视觉任务中奖励分布差异大及感知与推理平衡难的问题。研究提出了Gaussian GRPO (G²RPO) 目标函数，通过将优势分布映射到标准正态分布来确保任务间的梯度公平性。结合响应长度和熵值整形机制，模型在18个基准测试中表现优异，超越了现有的开源和闭源领先模型。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习面临不同任务间奖励拓扑差异巨大以及难以平衡细粒度感知与多步推理能力的挑战。
method: 提出了G²RPO算法，通过非线性分布匹配强制优势分布收敛至标准正态分布，并引入响应长度和熵值整形机制。
result: 在18个多样化的基准测试中，OpenVLThinkerV2展现出超越强力开源模型和领先闭源前沿模型的卓越性能。
conclusion: G²RPO显著提升了多模态大模型的训练稳定性，使OpenVLThinkerV2成为一个在感知和推理任务上均表现稳健的通用模型。
---

## 摘要
组相对策略优化（GRPO）已成为推动多模态大语言模型近期进展的事实上的强化学习（RL）目标。然而，将这一成功扩展到开源多模态通用模型仍受到两个主要挑战的严重制约：不同视觉任务间奖励拓扑的极端差异，以及平衡细粒度感知与多步推理能力的固有难度。为解决这些问题，我们引入了高斯 GRPO（G$^2$RPO），这是一种新型强化学习训练目标，它用非线性分布匹配取代了标准的线性缩放。通过在数学上强制任何给定任务的优势分布严格收敛到标准正态分布 $\mathcal{N}(0,1)$，G$^2$RPO 在理论上确保了任务间的梯度公平性，降低了对重尾离群值的敏感性，并为正负奖励提供了对称更新。利用 G$^2$RPO 提供的增强训练稳定性，我们引入了两种任务级塑造机制，以无缝平衡感知与推理。首先，响应长度塑造动态地引导复杂查询产生扩展推理链，同时强制直接输出以增强视觉定位能力。其次，熵塑造紧密限制了模型的探索区域，有效防止了熵坍缩和熵爆炸。综合这些方法，我们提出了 OpenVLThinkerV2，一个高度稳健的通用多模态模型。在 18 个不同基准测试上的广泛评估表明，其性能优于强大的开源模型和领先的闭源前沿模型。

## Abstract
Group Relative Policy Optimization (GRPO) has emerged as the de facto Reinforcement Learning (RL) objective driving recent advancements in Multimodal Large Language Models. However, extending this success to open-source multimodal generalist models remains heavily constrained by two primary challenges: the extreme variance in reward topologies across diverse visual tasks, and the inherent difficulty of balancing fine-grained perception with multi-step reasoning capabilities. To address these issues, we introduce Gaussian GRPO (G$^2$RPO), a novel RL training objective that replaces standard linear scaling with non-linear distributional matching. By mathematically forcing the advantage distribution of any given task to strictly converge to a standard normal distribution, $\mathcal{N}(0,1)$, G$^2$RPO theoretically ensures inter-task gradient equity, mitigates vulnerabilities to heavy-tail outliers, and offers symmetric update for positive and negative rewards. Leveraging the enhanced training stability provided by G$^2$RPO, we introduce two task-level shaping mechanisms to seamlessly balance perception and reasoning. First, response length shaping dynamically elicits extended reasoning chains for complex queries while enforce direct outputs to bolster visual grounding. Second, entropy shaping tightly bounds the model's exploration zone, effectively preventing both entropy collapse and entropy explosion. Integrating these methodologies, we present OpenVLThinkerV2, a highly robust, general-purpose multimodal model. Extensive evaluations across 18 diverse benchmarks demonstrate its superior performance over strong open-source and leading proprietary frontier models.

---

## 论文详细总结（自动生成）

这是一份关于论文《OpenVLThinkerV2: A Generalist Multimodal Reasoning Model for Multi-domain Visual Tasks》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：强化学习（RL）特别是组相对策略优化（GRPO）在提升多模态大模型（MLLM）推理能力方面表现出色，但在扩展到通用模型时面临两大挑战：
    1.  **奖励拓扑差异巨大**：不同视觉任务（如数学推理的稀疏二元奖励 vs. 视觉定位的密集连续 IoU 分数）导致梯度更新极度不平衡，标准线性缩放容易受离群值影响。
    2.  **感知与推理的失衡**：模型难以同时兼顾细粒度的视觉感知（如 OCR、定位）和复杂的多步逻辑推理。
*   **整体含义**：本研究旨在通过改进 RL 训练目标和引入任务级引导机制，构建一个在多领域视觉任务中表现稳健的通用多模态推理模型。

### 2. 方法论
*   **高斯 GRPO (G²RPO)**：
    *   **核心思想**：放弃传统的线性标量标准化，改用基于 **1D 最优传输（Optimal Transport）** 的非线性分布匹配。
    *   **技术细节**：利用累积分布函数（CDF）的闭式解，将任何任务的经验奖励分布严格映射到标准正态分布 $\mathcal{N}(0, 1)$。
    *   **优势**：在数学上强制实现了任务间的梯度公平性，自动抑制了重尾离群值的影响，并为正负奖励提供了对称的更新信号。
*   **任务级整形机制（Task-level Shaping）**：
    *   **响应长度整形**：采用梯形奖励函数，为复杂推理任务引导长思维链（CoT），为感知任务强制简洁输出，从而减少幻觉并增强视觉定位。
    *   **熵整形**：通过边际损失（Margin-based penalty）将模型的探索范围限制在特定区间，防止推理任务中的“熵爆炸”（产生乱码）和感知任务中的“熵坍缩”（过度自信而停止探索）。

### 3. 实验设计
*   **数据集与场景**：涵盖 6 大类、18 个基准测试：
    *   **通用/数学 VQA**：MMMU, MMBench, MathVista, MathVision 等。
    *   **图表/文档理解**：ChartQA, DocVQA, OCRBench, InfoVQA。
    *   **空间推理/定位**：EmbSpatial, RefSpatial, RefCOCO/+/g。
*   **对比方法**：
    *   **开源基准**：Qwen3-VL-Instruct-8B（初始模型）、OneThinker-8B、VL-Rethinker 等。
    *   **闭源前沿模型**：GPT-4o, Gemini 2.5 Pro, GPT-5 (System Card 数据)。
    *   **消融实验**：对比了标准 GRPO、GDPO 以及仅使用部分整形机制的效果。

### 4. 资源与算力
*   **算力平台**：使用 AWS Trainium 实例（具体为 **Trn1.32xlarge**）。
*   **训练时长**：整个训练过程大约耗时 **3 天**。
*   **模型规格**：基于 Qwen3-VL-Instruct-8B 进行后训练（Post-training）。
*   **训练参数**：Batch size 128，学习率 $2 \times 10^{-6}$，单 epoch 优化。

### 5. 实验数量与充分性
*   **实验规模**：在 18 个主流基准上进行了全面测试，并提供了详细的训练动态曲线（准确率、长度、格式、结构奖励随步数的变化）。
*   **充分性**：实验覆盖了从感知到推理的极端任务频谱，消融实验逐一验证了 G²RPO、长度奖励和熵损失的贡献。
*   **客观性**：在相同训练数据和计算资源下对比了 GRPO 和 GDPO，确保了算法改进的纯粹性。

### 6. 主要结论与发现
*   **性能卓越**：OpenVLThinkerV2 在 MMMU (71.6%) 和 MathVista (79.5%) 上显著超越 GPT-4o。
*   **感知能力增强**：在文档理解和空间推理任务中，性能甚至超过了 GPT-5 和 Gemini 2.5 Pro。
*   **训练稳定性**：G²RPO 相比标准 GRPO 展现出更早的收敛速度和更高的奖励稳定性，有效解决了多任务联合训练时的梯度爆炸风险。
*   **平衡性**：通过整形机制，模型成功在需要“深思熟虑”的数学题和需要“直觉感知”的定位题之间找到了平衡。

### 7. 优点
*   **理论严谨**：引入最优传输理论解决 RL 中的奖励异构问题，比传统的 EMA 移动平均方法更具数学鲁棒性。
*   **通用性强**：不依赖昂贵的外部标注或额外的视觉插件，仅通过策略优化即可提升感知精度。
*   **即插即用**：G²RPO 机制可以轻易扩展到其他存在奖励异构的领域（如代码生成或 GUI 代理任务）。

### 8. 不足与局限
*   **超参数依赖**：任务级的长度和熵整形需要预设阈值（如 $L_{min}, H_{max}$），虽然文中称其具有直觉性，但在全新任务上可能仍需经验微调。
*   **模型规模限制**：目前主要在 8B 参数规模上验证，尚未展示在更大规模（如 72B 或更高）模型上的扩展表现。
*   **数据依赖**：性能提升仍部分受限于训练数据（OneThinker-600k 过滤子集）的质量。

（完）
