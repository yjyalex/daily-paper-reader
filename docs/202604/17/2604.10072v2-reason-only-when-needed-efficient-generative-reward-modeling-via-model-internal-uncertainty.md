---
title: "Reason Only When Needed: Efficient Generative Reward Modeling via Model-Internal Uncertainty"
title_zh: 仅在需要时推理：通过模型内部不确定性实现高效生成式奖励建模
authors: "Chao Xue, Yao Wang, Mengqiao Liu, Di Liang, Xingsheng Han, Peiyang Liu, Xianjie Wu, Chenyao Lu, Lei Jiang, Yu Lu, Haibo Shi, Shuang Liang, Minlong Peng, Flora D. Salim"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10072v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于推理和思维链的生成式奖励模型
tldr: 本文针对生成式奖励模型（GRM）在推理任务中全量使用思维链（CoT）导致的计算冗余及评估粒度不足问题，提出了E-GRM框架。该框架利用模型内部不确定性（通过并行生成的收敛行为衡量）来动态触发CoT推理，仅在必要时启用复杂推理。同时，引入轻量级判别式评分器，结合回归与排序目标，实现对推理路径的精细化评估。实验证明，E-GRM在提升推理准确率的同时显著降低了推理成本。
selection_source: fresh_fetch
motivation: 现有的生成式奖励模型对所有任务无差别使用CoT推理导致计算开销巨大，且缺乏对推理质量的精细评估。
method: 提出E-GRM框架，利用模型内部不确定性选择性触发CoT，并结合混合回归-排序目标的轻量级评分器进行评估。
result: 在多个推理基准测试中，E-GRM在显著降低推理成本的同时，持续提升了答案的准确性。
conclusion: 模型内部不确定性是实现高效推理感知奖励建模的有效通用信号，能平衡推理性能与计算效率。
---

## 摘要
生成式奖励模型 (GRM) 的最新进展表明，其具有通过思维链 (CoT) 提示增强大语言模型 (LLM) 推理能力的潜力。尽管取得了这些进展，现有的 GRM 实现仍面临两个关键局限。首先，无论输入的内在复杂度如何，CoT 提示都会被无差别地应用于所有输入，这对于可以通过快速、直接推理完成的任务引入了不必要的计算开销。其次，现有方法主要依赖基于投票的机制来评估 CoT 输出，在评估推理质量时往往缺乏粒度和精度。在本文中，我们提出了 E-GRM，这是一个基于模型内部不确定性的高效生成式奖励建模框架。E-GRM 利用并行模型生成的收敛行为来估计不确定性，并仅在需要时选择性地触发 CoT 推理，而不依赖于手工特征或任务相关的信号。为了提高奖励保真度，我们引入了一个轻量级的判别式评分器，该评分器采用混合回归-排序目标进行训练，以提供对推理路径的细粒度评估。在多个推理基准测试上的实验表明，E-GRM 在显著降低推理成本的同时持续提高了答案准确性，证明了模型内部不确定性是实现高效推理感知奖励建模的一种有效且通用的信号。

## Abstract
Recent advancements in the Generative Reward Model (GRM) have demonstrated its potential to enhance the reasoning abilities of LLMs through Chain-of-Thought (CoT) prompting. Despite these gains, existing implementations of GRM suffer from two critical limitations. First, CoT prompting is applied indiscriminately to all inputs regardless of their inherent complexity. This introduces unnecessary computational costs for tasks amenable to fast, direct inference. Second, existing approaches primarily rely on voting-based mechanisms to evaluate CoT outputs, which often lack granularity and precision in assessing reasoning quality. In this paper, we propose E-GRM, an efficient generative reward modeling framework grounded in model-internal uncertainty. E-GRM leverages the convergence behavior of parallel model generations to estimate uncertainty and selectively trigger CoT reasoning only when needed, without relying on handcrafted features or task-dependent signals. To improve reward fidelity, we introduce a lightweight discriminative scorer trained with a hybrid regression--ranking objective to provide fine-grained evaluation of reasoning paths. Experiments on multiple reasoning benchmarks show that E-GRM substantially reduces inference cost while consistently improving answer accuracy, demonstrating that model-internal uncertainty is an effective and general signal for efficient reasoning-aware reward modeling.

---

## 论文详细总结（自动生成）

# 论文总结：E-GRM —— 仅在需要时推理：通过模型内部不确定性实现高效生成式奖励建模

### 1. 核心问题与整体含义（研究动机和背景）
生成式奖励模型（Generative Reward Model, GRM）通过思维链（CoT）显著提升了 LLM 的推理评估能力。然而，现有 GRM 存在两大瓶颈：
*   **计算效率低下（无差别推理）：** 现有的模型无论问题简单还是复杂，一律采用昂贵的 CoT 推理。对于简单查询，这造成了巨大的计算资源浪费和响应延迟。
*   **评估粒度粗糙（保真度不足）：** 多数方法依赖简单的投票机制（Majority Voting）来确定最终答案，无法识别推理路径中细微的质量差异，难以提供高精度的奖励信号。

本文提出的 **E-GRM** 旨在实现“按需推理”，即通过模型自身的状态判断是否需要 CoT，并结合精细化的评分机制提升奖励质量。

---

### 2. 方法论：核心思想与关键技术
E-GRM 框架由两个核心组件构成：

#### A. 基于模型内部不确定性的动态 CoT 触发
*   **核心思想：** 利用模型并行生成的收敛行为作为复杂度的指示信号。
*   **技术细节：** 
    1.  对输入 $x$ 进行 $M$ 次并行解码（使用不同的采样参数）。
    2.  计算**共识水平（Consensus）**：$Consensus(x) = \frac{\max Count(y)}{M}$。
    3.  **决策逻辑：** 如果共识度 $\ge \tau$（阈值，实验设为 0.8），说明模型对直接回答很有把握，判定为“短推理路径”，直接输出；否则，触发“长推理路径”，生成详细的 CoT。
*   **优势：** 任务无关（Task-agnostic），无需手工设计特征或外部难度评估器。

#### B. 混合损失函数的判别式评分器
*   **核心思想：** 训练一个轻量级辅助模型 $S_\phi$，对生成的推理路径进行细粒度打分。
*   **损失函数：** 结合了 **Huber Loss**（用于鲁棒回归，减少异常值影响）和 **Hinge Loss**（用于判别式排序，拉开高质量与低质量路径的差距）。
*   **公式：** $L_{scorer} = \alpha \cdot \ell_{Huber} + (1 - \alpha) \cdot \ell_{Hinge}$。

#### C. 训练与推理流程
*   **训练：** 采用 SFT（混合短/长路径数据）+ 扩展版 GRPO（Group Relative Policy Optimization）。在 GRPO 中引入了成对偏好信号，直接对比正负样本。
*   **推理：** 遵循“不确定性估计 -> 动态路由 -> (必要时) CoT 生成 -> 判别式评分 -> 择优输出”的流水线。

---

### 3. 实验设计
*   **数据集：** 涵盖数学（MATH, Math-DPO-10K）、代码（Code-Preference-Pairs）、通用指令（UltraFeedback, HelpSteer2, OffsetBias）等。
*   **Benchmark（基准测试）：**
    *   **RewardBench：** 评估多维度奖励能力（对话、安全、推理）。
    *   **RM-Bench：** 测试对语义细微差别的敏感度和风格偏见。
    *   **RMB：** 模拟真实场景的对齐评估（帮助性、无害性）。
*   **对比方法：** 
    *   标量 RM（如 Skywork-8B, Internlm2-20B, Nemotron-340B）。
    *   生成式 RM（如 GPT-4o, Claude-3.5-Sonnet, Llama-3.1-70B-Instruct）。
    *   推理增强型 RM（如 DeepSeek-GRM）。

---

### 4. 资源与算力
*   **模型架构：** 基于 Qwen-Instruct 系列（7B, 14B, 32B）。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号（如 A100/H100）及总卡数。
*   **训练参数：** 提到了 SFT 阶段 Batch Size 为 512，学习率为 $5 \times 10^{-6}$；奖励模型训练阶段 Batch Size 为 256，学习率为 $3 \times 10^{-6}$。

---

### 5. 实验数量与充分性
*   **实验规模：** 论文在三大主流 Benchmark 上进行了全面测试，并针对 7B 到 32B 不同参数规模的模型进行了验证。
*   **消融实验：** 专门设计了消融组，分别验证了“动态 CoT 触发”、“判别式评分器”以及“扩展版 GRPO”的贡献。
*   **对比分析：** 针对“动态触发”机制，专门与 AdaCoT 等现有自适应推理方法进行了对比实验。
*   **评价：** 实验设计较为充分，涵盖了准确率、延迟（Latency）和计算量（FLOPs）等多个维度，客观地展示了效率与性能的平衡。

---

### 6. 主要结论与发现
*   **效率大幅提升：** 在 MATH 数据集上，动态触发机制识别出 58% 的样本无需 CoT，使平均推理延迟降低了 **62%**，FLOPs 降低了 **49%**。
*   **性能超越 SOTA：** E-GRM (32B) 在 RewardBench 上达到 91.5% 的总分，在 RMB 上超越了 GPT-4o，证明了精细化评分比简单投票更有效。
*   **任务无关的通用性：** 模型内部不确定性是一个极其有效的信号，无需针对特定学科（如数学或代码）进行硬编码。
*   **错误传播抑制：** 实验发现，对简单问题强制使用 CoT 反而可能引入逻辑冗余，导致准确率下降（下降约 3.2%），而 E-GRM 避免了这一问题。

---

### 7. 优点
*   **创新的触发机制：** 巧妙利用并行解码的收敛性作为“内部不确定性”度量，解决了推理成本与复杂度的适配问题。
*   **高保真奖励信号：** 混合回归与排序的损失函数设计，使模型既能给出绝对质量分，又能精准区分相似路径的优劣。
*   **实用性强：** 显著降低了推理成本，使其更适合实时、资源受限的部署场景。

---

### 8. 不足与局限
*   **初始开销：** 虽然减少了 CoT 生成，但“并行解码 $M$ 次”本身会带来一定的初始计算开销（虽然论文称延迟增加 <5%，但在极低延迟场景下仍需考虑）。
*   **阈值敏感性：** 阈值 $\tau$ 可能需要根据不同领域（如法律、医疗）进行微调，论文尚未提供自动调节阈值的方案。
*   **评估维度限制：** 效率分析主要集中在计算量和延迟，未深入探讨内存占用和能耗等硬件层面的指标。
*   **泛化风险：** 判别式评分器在面对与训练数据风格迥异的推理路径时，性能可能会有所下降。

（完）
