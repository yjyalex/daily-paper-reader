---
title: Quantifying Cross-Query Contradictions in Multi-Query LLM Reasoning
title_zh: 量化多查询大语言模型推理中的跨查询矛盾
authors: "Rohit Kumar Salla, Ramya Manasa Amancherla, Manoj Saravanan"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14525v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 跨相互依赖查询的逻辑一致性
tldr: 针对大语言模型在处理多个相关查询时常出现逻辑矛盾的问题，本文研究了“案卷逻辑一致性”，旨在保持全局可满足的信念状态。研究者提出了包含390个实例的基准测试及评估指标，并开发了一种结合求解器的增强方法，通过提取承诺、验证全局一致性及反例引导修复，在保持单查询准确率的同时显著降低了跨查询矛盾，提升了推理的鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在处理一系列相互关联的逻辑查询时，容易产生相互矛盾、缺乏全局一致性的核心痛点。
method: 提出一种求解器增强方法，通过提取模型承诺、验证全局可满足性并利用反例引导进行逻辑修复。
result: 实验表明该方法在四个推理领域中将集合一致性从0.56大幅提升至0.94，且未牺牲单查询的准确率。
conclusion: 全局逻辑一致性是实现鲁棒多查询推理的关键，通过外部求解器辅助验证和修复能有效消除模型内部的逻辑冲突。
---

## 摘要
大语言模型在对多个相关查询进行推理时，经常产生相互不一致的答案。我们研究了案卷逻辑一致性（case-file logical consistency）：在相互依赖的查询中维持全局可满足的信念状态。我们引入了一个包含 390 个多查询推理实例的基准测试，包含蕴含/矛盾/未知标签，并提出了集合级指标，包括案卷可满足率（Case Satisfiability Rate）、矛盾密度（Contradiction Density）和修正成本（Revision Cost）。我们的求解器增强方法提取承诺（commitments），验证全局可满足性，并执行反例引导的修复。在四个推理领域中，我们的方法在保持单次查询准确率的同时，显著减少了跨查询矛盾（SetCons 从 0.56 提升至 0.94），证明了全局连贯性对于鲁棒的多查询推理至关重要。

## Abstract
Large language models frequently produce mutually inconsistent answers when reasoning over multiple related queries. We study case-file logical consistency: maintaining a globally satisfiable belief state across interdependent queries. We introduce a benchmark of 390 multi-query reasoning instances with entailment/contradiction/unknown labels and propose set-level metrics including Case Satisfiability Rate, Contradiction Density and Revision Cost. Our solver-augmented approach extracts commitments, verifies global satisfiability and performs counterexample-guided repair. Across four reasoning domains, our method substantially reduces cross-query contradictions (SetCons: 0.56 to 0.94) while preserving per-query accuracy, demonstrating that global coherence is critical for robust multi-query reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Quantifying Cross-Query Contradictions in Multi-Query LLM Reasoning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在单次推理任务上表现出色，但在处理围绕同一前提（Premise）的一系列相互关联的查询（Query Bundle）时，往往会出现“自我矛盾”的现象。
*   **核心问题**：论文提出了“案卷逻辑一致性”（Case-file logical consistency）的概念，即模型在处理一系列相互依赖的查询时，能否维持一个全局可满足（Satisfiable）的信念状态。
*   **背景**：在法律分析、临床诊断或科学假设检验等实际应用中，用户通常会进行多轮、分支式的提问，确保所有回答在逻辑上严丝合缝至关重要。

### 2. 论文提出的方法论
核心思想是**求解器增强的信念跟踪与修复机制**，具体流程如下：
*   **信念状态建模**：定义信念状态 $B_t$ 为共享前提 $\phi(P)$ 与截至时间 $t$ 的所有查询承诺 $\psi$ 的逻辑交集。
*   **承诺提取（Commitment Extraction）**：利用 LLM 将自然语言回答转化为符号逻辑形式（如 CNF 范式或 SMT-LIB 表达式）。
*   **增量满足性检查**：每当模型给出新答案，便调用外部求解器（如 SAT 求解器 CaDiCaL 或 SMT 求解器 Z3）检查 $B_t$ 是否仍具有可满足性。
*   **不可满足核心定位（Unsat-core Localization）**：若检测到矛盾，利用求解器识别导致冲突的最小约束子集（Unsat-core）。
*   **反例引导修复（CEGR）**：根据“最小修改原则”，通过 LLM 引导修复。修复动作包括：翻转当前标签（如从“蕴含”改为“未知”）、弱化承诺或撤回少量先前的矛盾承诺。

### 3. 实验设计
*   **数据集/Benchmark**：引入了一个包含 **390 个案卷实例**和 **2450 个相互依赖查询束**的新基准测试。涵盖四个领域：
    1.  **关系型（Relational/SAT）**：命题逻辑约束。
    2.  **时间型（Temporal/SMT）**：线性算术与时间约束。
    3.  **政策/规则（Policy/Rules）**：有限域上的谓词逻辑。
    4.  **欠定/溯因（Underspecified/Abductive）**：考察模型在信息不足时是否会胡乱承诺。
*   **对比方法**：
    *   **Direct Decoding (No-CoT)**：独立回答每个问题。
    *   **Chain-of-Thought (CoT)**：思维链推理。
    *   **Self-Consistency (SC)**：多路径采样投票（K=20）。
    *   **History Baseline**：将之前的问答对作为上下文输入。
*   **评估指标**：除了准确率，还提出了 **SetCons**（全集一致率）、**AUC-PrefixCons**（前缀一致性曲线面积）和 **RevCost**（修复满足性所需的平均修改成本）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理开销**：文中报告了“墙钟时间开销”（Wall-clock Overhead）。相比于 No-CoT 基准，仅检查模式（Check-only）增加约 1.26x–1.33x 的耗时，检查+修复模式增加约 1.44x–1.58x。这显著低于 Self-Consistency（K=20）带来的 2.5x 以上的开销。

### 5. 实验数量与充分性
*   **实验规模**：对 **8 个主流开源模型**（包括 DeepSeek-R1/V3、Qwen2.5-72B、Llama-3.3-70B 等）进行了广泛测试。
*   **消融实验**：针对修复策略（是否使用 Unsat-core、修复步数限制等）进行了详细消融。
*   **客观性与公平性**：
    *   使用了人工标注（3 人共识，Fleiss’ κ = 0.82）与求解器自动验证相结合的双重标准。
    *   所有模型均采用相同的 Prompt 模板和解码设置（贪婪解码），确保了对比的公平性。
    *   实验覆盖了从密集模型到混合专家模型（MoE）的多种架构，具有较强的代表性。

### 6. 论文的主要结论与发现
*   **一致性鸿沟**：即使是强大的模型（如 DeepSeek-R1），在单次查询准确率较高的情况下，其全集一致率（SetCons）也往往较低（约 0.56），说明局部正确不代表全局连贯。
*   **方法有效性**：所提方法将 SetCons 从 0.56 提升至 **0.94**，同时保持或略微提升了单次查询的准确率。
*   **拒绝幻觉**：该方法显著提升了模型在“未知（Unknown）”类别的 F1 分数，有效抑制了模型在信息不足时的过度承诺（Over-confidence）。

### 7. 优点
*   **视角新颖**：从单点推理转向“案卷级”全局一致性评估，更符合真实交互场景。
*   **神经符号结合**：巧妙利用外部求解器的严谨性来弥补 LLM 的逻辑脆弱性，且修复过程遵循经典的信念修正理论。
*   **轻量高效**：相比于暴力采样（SC），该方法通过增量求解实现了更低的一致性维护成本。

### 8. 不足与局限
*   **提取噪声（Extraction Noise）**：方法高度依赖于 LLM 将自然语言准确转化为逻辑符号的能力，若转化错误（准确率约 91-95%），会导致误判。
*   **逻辑表达限制**：目前主要针对可判定的逻辑片段（如 SAT、线性算术），对于更复杂的高阶逻辑或非形式化常识推理的覆盖有限。
*   **求解器依赖**：在极端复杂的约束下，求解器可能面临超时问题，此时系统会保守地退回到“未知”状态。

（完）
