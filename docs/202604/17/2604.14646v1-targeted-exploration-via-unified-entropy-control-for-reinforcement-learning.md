---
title: Targeted Exploration via Unified Entropy Control for Reinforcement Learning
title_zh: 通过统一熵控制实现强化学习中的针对性探索
authors: "Chen Wang, Lai Wei, Yanzhi Zhang, Chenyang Shao, Zedong Dan, Weiran Huang, Ge Lan, Yue Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14646v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用GRPO和熵控制改进大语言模型的推理能力
tldr: "针对大语言模型强化学习中GRPO算法易出现的熵塌缩和多样性缺失问题，本文提出统一熵控制框架（UEC-RL）。该方法通过针对性探索机制，在困难任务上激活更多搜索以发现高价值推理路径，同时利用稳定器防止熵过度增长以维持训练稳定。实验证明，UEC-RL在LLM和VLM推理任务上显著优于基线，在Geometry3K上比GRPO提升37.9%，有效平衡了探索深度与收敛稳定性。"
selection_source: fresh_fetch
motivation: 解决强化学习在提升大模型推理能力时面临的熵塌缩、过早收敛以及现有探索方法引入额外偏差或方差的问题。
method: 提出UEC-RL框架，通过对困难提示词进行针对性探索并结合熵稳定器，实现动态且受控的搜索空间扩展。
result: "在多项推理任务中取得显著进展，特别是在Geometry3K数据集上相比GRPO实现了37.9%的相对性能提升。"
conclusion: UEC-RL通过统一的熵控制机制，在不牺牲收敛性的前提下增强了有效探索，是扩展大模型推理能力的关键技术。
---

## 摘要
强化学习（RL）的最新进展提升了大语言模型（LLM）和视觉语言模型（VLM）的推理能力。然而，广泛使用的群体相对策略优化（GRPO）始终面临熵崩溃（entropy collapse）问题，导致策略过早收敛并失去多样性。现有的探索方法在探索过程中引入了额外的偏差或方差，使得难以维持优化的稳定性。我们提出了强化学习统一熵控制（UEC-RL），这是一个为探索和稳定提供针对性机制的框架。UEC-RL 在困难提示词上激活更多探索，以搜索潜在且有价值的推理轨迹。同时，稳定器可防止熵不受控制地增长，从而在模型巩固可靠行为时保持训练稳定。这些组件共同作用，在需要时扩展搜索空间，同时在整个训练过程中保持鲁棒的优化。在 LLM 和 VLM 推理任务上的实验表明，在 Pass@1 和 Pass@$k$ 指标上，该方法相较于 RL 基准均有持续提升。在 Geometry3K 数据集上，UEC-RL 相比 GRPO 实现了 37.9% 的相对提升，表明其在不损害收敛性的情况下维持了有效的探索，并凸显了 UEC-RL 是在大模型中扩展基于 RL 的推理的关键。我们的代码可在 https://github.com/597358816/UEC-RL 获取。

## Abstract
Recent advances in reinforcement learning (RL) have improved the reasoning capabilities of large language models (LLMs) and vision-language models (VLMs). However, the widely used Group Relative Policy Optimization (GRPO) consistently suffers from entropy collapse, causing the policy to converge prematurely and lose diversity. Existing exploration methods introduce additional bias or variance during exploration, making it difficult to maintain optimization stability. We propose Unified Entropy Control for Reinforcement Learning (UEC-RL), a framework that provides targeted mechanisms for exploration and stabilization. UEC-RL activates more exploration on difficult prompts to search for potential and valuable reasoning trajectories. In parallel, a stabilizer prevents entropy from growing uncontrollably, thereby keeping training stable as the model consolidates reliable behaviors. Together, these components expand the search space when needed while maintaining robust optimization throughout training. Experiments on both LLM and VLM reasoning tasks show consistent gains over RL baselines on both Pass@1 and Pass@$k$. On Geometry3K, UEC-RL achieves a 37.9\% relative improvement over GRPO, indicating that it sustains effective exploration without compromising convergence and underscoring UEC-RL as a key for scaling RL-based reasoning in large models. Our code is available at https://github.com/597358816/UEC-RL.

---

## 论文详细总结（自动生成）

### 论文总结：通过统一熵控制实现强化学习中的针对性探索 (UEC-RL)

#### 1. 核心问题与整体含义（研究动机和背景）
在提升大语言模型（LLM）和视觉语言模型（VLM）推理能力的后训练阶段，强化学习（RL）已成为核心范式。然而，目前主流的轻量化算法 **GRPO**（群体相对策略优化）存在严重的**熵塌缩（Entropy Collapse）**问题：模型在训练初期会迅速收敛到少数几种推理路径，导致搜索空间受限，无法发现低概率但高价值的复杂推理轨迹。此外，在多模态等高方差场景下，GRPO 容易出现训练不稳定的现象。

本研究的动机在于建立一种**双向熵控制机制**：既能在遇到困难问题时主动“增熵”以扩大探索范围，又能在模型掌握可靠行为时“减熵”以确保训练的平稳收敛。

#### 2. 方法论：核心思想与关键技术
UEC-RL 框架通过两个协同组件实现了对策略熵的精准调控：

*   **针对性探索机制 (Targeted Exploration)**：
    *   **困难识别**：在每一轮采样中，若初始组（Size为 $G$）的所有回答均错误，则将该 Prompt 标记为“困难”。
    *   **空间扩展**：针对困难 Prompt，使用更高的采样温度 $t'$ 和更大的采样组数 $G'$ 进行二次采样，强制模型探索低概率区域。
    *   **选择性保留**：仅保留具有正优势（Positive Advantage）的探索轨迹，过滤掉无意义的噪声梯度。
*   **可控熵稳定器 (Controllable Entropy Stabilizer)**：
    *   **经验回放**：建立一个 Replay Buffer，存储近期发现的高质量（高 Advantage）轨迹。
    *   **梯度强化**：通过定期重复训练这些高质量样本，根据论文推导的定理（高优势动作的对数似然增加会导致熵减），实现对策略的稳定和去噪，防止探索过程失控。
*   **算法流程**：常规采样 -> 识别困难任务并进行高熵探索 -> 筛选有效轨迹更新模型 -> 定期从 Buffer 中回放高质量样本进行稳定化更新。

#### 3. 实验设计
*   **数据集/场景**：
    *   **文本推理**：使用 DAPO-17K 数学数据集进行训练。
    *   **多模态推理**：使用 6K 规模的混合多模态数据集（涵盖几何、图表、科学推理等）。
    *   **领域内分析**：专门在 Geometry3K 数据集上进行深入的训练动力学分析。
*   **Benchmark**：
    *   文本：AIME 24/25、MATH、GSM8K、Minerva、ARC、MMLU-pro。
    *   多模态：MathVision、MathVerse、MathVista、We-Math。
*   **对比方法**：
    *   **GRPO**（基准算法）。
    *   **DAPO**（采用 Clip-higher 策略缓解塌缩）。
    *   **KL-cov**（协方差感知的 KL 正则化）。
    *   **Entropy-Adv**（在优势函数中加入熵奖励）。

#### 4. 资源与算力
论文明确提到了实验配置：
*   **硬件**：使用 **8 张 NVIDIA A800 GPU (40GB)**。
*   **模型**：初始化模型包括 Qwen2.5-VL-7B-Instruct 和 Qwen2.5-Math-7B。
*   **训练细节**：最大响应长度 8192，Batch Size 为 512。具体的训练总时长未明确说明，但提供了效率对比：UEC-RL 的单步训练耗时约为 GRPO 的 0.79 倍（由于优化了采样效率和样本筛选）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 8B 规模的两个不同模型家族（Qwen 和 Llama），并在 10 多个主流 Benchmark 上进行了测试。
*   **消融实验**：针对“探索模块”和“稳定器模块”分别做了剔除实验，并对关键超参数（探索温度 $t'$、Buffer 大小 $s'$）进行了网格搜索。
*   **充分性评价**：实验设计较为充分，不仅关注 Pass@1 准确率，还分析了 Pass@k 的多样性增益，以及训练过程中的熵变化曲线和响应长度变化。对比基线涵盖了近期（2025年）最新的同类研究，具有较强的时效性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：UEC-RL 在所有测试任务中均优于 GRPO 及其变体。在 Geometry3K 上，准确率从 50.75% 提升至 55.41%（相对提升 37.9%）。
*   **双向控制的必要性**：单纯增加探索（如 w/o stabilizer）会导致训练发散；单纯抑制塌缩（如 w/o exploration）则无法解决深度推理问题。
*   **多样性提升**：Pass@k 曲线显示，UEC-RL 生成的正确答案不仅更多，而且推理路径更加多样化。
*   **效率优势**：通过针对性采样而非全量高熵采样，UEC-RL 在提升性能的同时保持了较低的计算开销。

#### 7. 优点
*   **理论支撑**：为熵的增减提供了数学定理支持（定理 4.1 和 4.2），使方法论更具说服力。
*   **针对性强**：不同于传统方法对所有样本施加统一的熵奖励，UEC-RL 仅在“模型不会做”的题目上发力，避免了在简单题目上的无效搜索。
*   **通用性**：在纯文本数学推理和复杂的视觉-语言推理任务中表现出一致的鲁棒性。

#### 8. 不足与局限
*   **超参数敏感性**：探索温度 $t'$ 和稳定器预算 $s'$ 的最优值取决于任务难度。对于不同难度的任务，可能需要重新调优以找到最佳的熵平衡点。
*   **难度估计较简单**：目前仅通过“初始采样是否全错”来定义困难任务，这种二元分类可能无法捕捉到更细粒度的推理瓶颈。
*   **算力门槛**：虽然单步效率高，但引入 Replay Buffer 和二次采样机制增加了系统实现的复杂度和内存占用。

（完）
