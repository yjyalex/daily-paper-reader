---
title: "SWE-TRACE: Optimizing Long-Horizon SWE Agents Through Rubric Process Reward Models and Heuristic Test-Time Scaling"
title_zh: SWE-TRACE：通过准则过程奖励模型与启发式测试时缩放优化长程 SWE 智能体
authors: "Hao Han, Jin Xie, Xuehao Ma, Weiquan Zhu, Ziyao Zhang, ZhiLiang Long, Hongkai Chen, Qingwen Ye"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14820v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于长程推理的规则过程奖励模型和强化学习
tldr: SWE-TRACE是一个旨在优化软件工程（SWE）智能体的统一框架，针对长程推理中的数据冗余、奖励稀疏和推理成本高昂等挑战，提出了轨迹缩减与智能体准则评估方案。该框架通过蒸馏最短路径轨迹构建SFT数据集，利用基于评分表的进程奖励模型（PRM）进行强化学习，并在推理阶段通过启发式测试时缩放（TTS）动态剪枝。实验表明，SWE-TRACE在提升任务解决率的同时，显著降低了Token消耗和推理延迟，达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的软件工程智能体在处理长程任务时，面临数据未优化导致的Token膨胀、执行奖励稀疏以及推理阶段计算成本过高等瓶颈。
method: 该框架结合了最短路径轨迹的数据蒸馏、基于评分表的进程奖励模型（PRM）强化学习，以及利用PRM进行动态剪枝的启发式测试时缩放（TTS）技术。
result: 在标准SWE基准测试中，SWE-TRACE在刷新SOTA解决率的同时，大幅降低了Token消耗和推理延迟。
conclusion: 通过全生命周期的优化，SWE-TRACE证明了细粒度过程监督和高效搜索策略在提升长程软件工程智能体性能与效率方面的关键作用。
---

## 摘要
使用自主智能体解决现实世界的软件工程（SWE）问题需要复杂的长程推理。当前的流水线受限于未优化的演示数据、稀疏的执行奖励以及计算成本极高的推理缩放，这些因素共同加剧了 Token 膨胀、奖励作弊（reward hacking）和策略退化。我们提出了 SWE-TRACE（轨迹缩减与智能体准则评估），这是一个统一的框架，旨在优化涵盖数据管理、强化学习（RL）和测试时推理的 SWE 智能体全生命周期。首先，我们引入了一种大语言模型（LLM）多任务级联方法，利用逐步的真值（oracle）验证来提取包含 6 万个实例的有监督微调（SFT）语料库，该语料库严格偏向于 Token 高效的最短路径轨迹。其次，为了克服稀疏结果奖励的不稳定性，我们设计了一个具有记忆增强功能的智能体强化学习流水线，其核心是一个基于准则的过程奖励模型（PRM）。一个辅助的准则智能体（Rubric-Agent）对中间步骤提供密集、细粒度的启发式反馈，引导模型完成长程任务。最后，我们通过将 PRM 重新用于启发式引导的测试时缩放（TTS），连接了训练与推理阶段。通过在每一步动态评估和剪枝候选动作，SWE-TRACE 实现了卓越的搜索效率，且没有标准并行采样的延迟开销。在标准 SWE 基准测试上的广泛实验表明，SWE-TRACE 显著提升了当前的最先进水平，在最大化问题解决率的同时，大幅降低了 Token 消耗和推理延迟。

## Abstract
Resolving real-world software engineering (SWE) issues with autonomous agents requires complex, long-horizon reasoning. Current pipelines are bottlenecked by unoptimized demonstration data, sparse execution rewards, and computationally prohibitive inference scaling, which collectively exacerbate token bloat, reward hacking, and policy degradation. We present SWE-TRACE (Trajectory Reduction and Agentic Criteria Evaluation), a unified framework optimizing the SWE agent lifecycle across data curation, reinforcement learning (RL), and test-time inference. First, we introduce an LLM multi-task cascading method, utilizing stepwise oracle verification to distill a 60K-instance Supervised Fine-Tuning (SFT) corpus strictly biased toward token-efficient, shortest-path trajectories. Second, to overcome the instability of sparse outcome rewards, we design a MemoryAugmented Agentic RL pipeline featuring a Rubric-Based Process Reward Model (PRM). An auxiliary Rubric-Agent provides dense, fine-grained heuristic feedback on intermediate steps, guiding the model through long-horizon tasks. Finally, we bridge training and inference by repurposing the PRM for heuristic-guided Test-Time Scaling (TTS). By dynamically evaluating and pruning action candidates at each step, SWE-TRACE achieves superior search efficiency without the latency overhead of standard parallel sampling. Extensive experiments on standard SWE benchmarks demonstrate that SWE-TRACE significantly advances the state-of-the-art, maximizing resolution rates while drastically reducing both token consumption and inference latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SWE-TRACE**，这是一个旨在优化长程软件工程（SWE）智能体性能的统一框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自主软件工程智能体在处理现实世界 GitHub 问题时，面临三个主要瓶颈：
    1.  **数据冗余**：现有的 SFT 轨迹包含大量无效探索和重复操作，导致模型学会了“嘈杂的搜索”而非“高效的解决”。
    2.  **奖励稀疏**：在长程任务中，仅靠最终执行结果（Pass/Fail）作为奖励信号，难以进行有效的信用分配（Credit Assignment），容易导致奖励作弊和策略退化。
    3.  **推理成本高昂**：现有的测试时缩放（TTS）通常依赖于生成大量完整轨迹并重排序，这在复杂的仓库级环境中会产生巨大的 Token 消耗和延迟。
*   **整体含义**：论文旨在通过优化数据管理、强化学习和推理引导的全生命周期，提升轻量级开源模型在复杂软件开发任务中的效率和成功率。

### 2. 方法论
SWE-TRACE 提出了一个三阶段的优化流水线：
*   **Token 高效的轨迹合成（数据阶段）**：
    *   **大规模数据固化**：从 77 个 GitHub 仓库中构建了 60K 个高质量合成任务。
    *   **LLM 多任务级联（Multi-task Cascading）**：在生成轨迹时，每一步都生成多个候选动作（定位、检查、编辑等），并利用“真值验证器（Oracle Verifier）”根据测试状态、代码范围等指标选择最优路径，从而蒸馏出“最短路径”的 SFT 轨迹。
*   **基于准则的过程奖励模型（RL 阶段）**：
    *   **准则智能体（Rubric-Agent）**：为每个任务生成特定的评分表（Rubric），包含定位约束、编辑约束、预算意识等细粒度标准。
    *   **过程奖励模型（PRM）**：训练一个 PRM 来根据评分表对轨迹进行打分，而不仅仅是看最终是否通过测试。
    *   **记忆增强架构**：当上下文超出限制时，利用 PRM 识别关键步骤并保留原始锚点，避免摘要导致的细节丢失。
    *   **GRPO 优化**：使用组相对策略优化，并引入“边际分离奖励”，确保通过测试的轨迹奖励始终高于未通过的，同时在同类轨迹中区分优劣。
*   **启发式引导的测试时缩放（推理阶段）**：
    *   **HG-TTS**：将训练好的 PRM 重新用作推理指南。在每一步动作选择时，对候选动作进行评分并剪枝，只保留最有潜力的分支进行环境交互，从而在低延迟下实现高效搜索。

### 3. 实验设计
*   **基准测试**：主要使用 **SWE-bench Verified**（包含 500 个经人工验证的真实 GitHub 问题）。
*   **模型底座**：Qwen3-4B（轻量级）和 Qwen3-30B-A3B（中等规模 MoE）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, o3-mini, Gemini 1.5 Pro, GLM-4.7。
    *   **开源/智能体框架**：OpenHands, SWE-Gym, R2E-Gym, DeepSWE, SWE-Master 等。
*   **消融实验**：针对 SFT 数据质量（标准 vs 最短路径）、RL 奖励设计（仅执行奖励 vs 准则 PRM）、TTS 策略（贪婪 vs 并行 vs 启发式引导）进行了详细对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **环境构建**：提到了使用 Docker 容器化 77 个仓库，并利用智能体自动修复环境依赖，这暗示了在数据准备阶段有较大的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：构建了 60K 训练样本，这在同类研究中属于较大规模。
*   **充分性**：实验涵盖了从 4B 到 30B 不同参数规模的模型，验证了方法的普适性。消融实验非常详尽，不仅关注解决率（Resolve Rate），还深入分析了 Token 效率、推理延迟和长程任务的性能分布。
*   **客观性**：使用了标准化的基准测试和统一的智能体脚手架（mini-SWE-Agent），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能突破**：SWE-TRACE-30B 在 SWE-bench Verified 上达到了 **71.2%** 的解决率，超越了许多参数量更大的模型和之前的 SOTA 开源系统。
*   **效率提升**：通过最短路径 SFT，Token 消耗降低了约 **20%-30%**；HG-TTS 在保持高解决率的同时，显著降低了推理延迟和环境执行次数。
*   **长程优势**：在需要超长 Token 预算的任务中，SWE-TRACE 的优势最为明显，证明了其过程监督和记忆机制的有效性。

### 7. 优点
*   **全生命周期优化**：不只是改进模型，而是从数据、训练到推理进行了系统性重构。
*   **可解释的过程监督**：引入 Rubric（评分表）使奖励信号不再是“黑盒”，提高了 RL 训练的稳定性。
*   **推理效率高**：HG-TTS 解决了传统 TTS 延迟过高的问题，使其在实际生产环境中更具部署价值。

### 8. 不足与局限
*   **合成数据依赖**：高性能依赖于大规模合成任务的质量，若合成任务与真实分布存在偏差，可能影响泛化性。
*   **计算复杂度**：虽然推理时剪枝了分支，但在每一步调用 PRM 进行评分仍会带来额外的计算开销（尽管低于环境执行开销）。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
