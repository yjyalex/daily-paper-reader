---
title: Schema Key Wording as an Instruction Channel in Structured Generation under Constrained Decoding
title_zh: 约束解码下结构化生成中作为指令通道的模式键措辞
authors: Yifan Le
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14862v1"
tags: ["query:llm-fo"]
score: 10.0
evidence: 约束解码下的结构化生成与模式关键词影响
tldr: 本研究探讨了在受限解码下，结构化生成的模式键（schema keys）命名如何影响大语言模型性能。研究发现，模式键不仅是结构约束，还充当隐式指令通道。通过在数学推理任务上的实验，揭示了不同模型（如Qwen和LLaMA）对提示词和模式键指令的敏感度差异，证明了模式设计在结构化生成中具有重要的指令引导作用。
selection_source: fresh_fetch
motivation: 现有研究多将模式视为纯结构约束，忽视了模式键的语言表述对模型生成质量的潜在影响。
method: 将结构化生成重新定义为多通道指令问题，通过改变模式键的命名来研究其作为隐式指令对模型性能的影响。
result: 实验发现Qwen模型对模式级指令更敏感，而LLaMA更依赖提示词，且不同通道间的指令效果并非简单的线性叠加。
conclusion: 模式键的命名是结构化生成中一个被忽视的隐式指令通道，为优化大模型输出提供了新的视角和手段。
---

## 摘要
约束解码已广泛应用于大语言模型（LLM）的结构化生成，以确保输出符合 JSON 和 XML 等预定义格式。然而，现有方法在很大程度上将模式（schema）视为纯粹的结构约束，而忽略了其语言表述可能影响模型行为的可能性。在本研究中，我们探讨了指令放置位置如何影响结构化生成中的模型性能，并表明在不修改提示词或模型参数的情况下，仅改变模式键的措辞就能显著改变约束解码下的模型性能。基于这一观察，我们提出将结构化生成重新解释为一个多通道指令问题，其中指令可以通过提示词显式传达，也可以在解码过程中通过模式键隐式传达。据我们所知，这是首项系统研究模式键表述如何作为隐式指令通道并影响约束解码下模型性能的工作。在多个数学推理基准测试上的实验表明，不同的模型系列对这些指令通道表现出截然不同的敏感性：Qwen 模型持续受益于模式级指令，而 LLaMA 模型则更依赖于提示级引导。我们进一步观察到指令通道之间存在非加性交互效应，表明组合多个通道并不总是能带来进一步的提升。这些发现表明，模式设计不仅决定了输出结构，还承载着指令信号，为 LLM 中的结构化生成提供了一个新的视角。

## Abstract
Constrained decoding has been widely adopted for structured generation with large language models (LLMs), ensuring that outputs satisfy predefined formats such as JSON and XML. However, existing approaches largely treat schemas as purely structural constraints and overlook the possibility that their linguistic formulation may affect model behavior. In this work, we study how instruction placement influences model performance in structured generation and show that merely changing the wording of schema keys, without modifying the prompt or model parameters, can significantly alter model performance under constrained decoding. Based on this observation, we propose to reinterpret structured generation as a multi-channel instruction problem, where instructions can be conveyed explicitly through prompts and implicitly through schema keys during decoding. To the best of our knowledge, this is the first work to systematically study how schema key formulation acts as an implicit instruction channel and affects model performance under constrained decoding. Experiments on multiple mathematical reasoning benchmarks show that different model families exhibit distinct sensitivities to these instruction channels: Qwen models consistently benefit from schema-level instructions, while LLaMA models rely more heavily on prompt-level guidance. We further observe non-additive interaction effects between instruction channels, showing that combining multiple channels does not always lead to further improvement. These findings suggest that schema design not only determines output structure, but also carries instruction signals, offering a new perspective on structured generation in LLMs.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在约束解码（Constrained Decoding）下的结构化生成行为进行了深入探讨。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在结构化生成（如生成 JSON 或 XML）中，现有的约束解码技术（如 XGrammar、Guidance 等）通常将模式（Schema）视为纯粹的“结构约束”，即仅用于限制 Token 的搜索空间。
*   **核心问题**：论文探讨了一个被长期忽视的问题：**模式键（Schema Keys，如 JSON 中的字段名）的命名措辞是否会像提示词（Prompt）一样，作为一种隐式指令影响模型的推理性能？**
*   **研究背景**：以往研究多关注约束解码带来的性能损失（如推理能力下降），但很少有人研究模式本身的语言表述如何调节这种性能。

### 2. 方法论
*   **核心思想**：将结构化生成重新定义为**多通道指令问题（Multi-channel Instruction Problem）**。模型行为由两个通道共同塑造：
    1.  **提示级通道 ($c_p$)**：通过系统提示词或任务提示词显式注入的指令。
    2.  **模式级通道 ($c_s$)**：通过模式键的措辞隐式传达的任务信号。
*   **关键技术细节**：
    *   **结构等价性**：在实验中保持输出结构（字段数量、顺序、解析逻辑）完全一致，仅改变键的命名（例如将中性的 `reasoning` 改为带有引导性的 `step_by_step_reasoning`）。
    *   **四种实验设置**：
        *   **None**：无显式指令（基准约束解码）。
        *   **Key-only**：仅在模式键中注入指令信号。
        *   **Prompt-only**：仅在提示词中注入指令信号。
        *   **Both**：两个通道同时注入指令。
*   **公式化表达**：生成过程表示为 $y \sim p_\theta(\cdot | x, c_p, c_s; s)$，其中 $s$ 强制执行结构有效性，而 $c_p$ 和 $c_s$ 捕捉指令信号。

### 3. 实验设计
*   **数据集/场景**：专注于数学推理任务，使用 **GSM8K** 和 **Math500** 两个基准测试。
*   **对比模型**：涵盖了不同参数规模和家族的模型：
    *   **Qwen 系列**：Qwen2.5-3B/7B、Qwen2.5-Coder-14B、DeepSeek-R1-Distill-Qwen-7B。
    *   **LLaMA 系列**：Llama-3.2-1B/3B、Llama-3.1-8B。
*   **解码引擎**：使用 **XGrammar** 作为约束解码引擎，确保输出严格符合定义的 Schema。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或推理总时长。
*   **实现细节**：由于该研究主要关注推理阶段的消融实验，不涉及模型微调或训练，因此算力需求主要集中在对多个模型在数千个样本上的受限解码推理。

### 5. 实验数量与充分性
*   **实验规模**：研究测试了 7 个代表性开源模型，涵盖了从 1B 到 14B 的参数梯度。
*   **消融实验**：针对每个模型在两个数据集上都执行了上述四种指令放置设置（None, Key, Prompt, Both），并计算了单通道增益和跨通道交互效应。
*   **客观性与公平性**：实验采用了严格的受控变量法，保持模型参数、解码超参数、输出结构完全一致，仅改变指令位置，这使得实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **模式键具有指令效应**：仅改变模式键的措辞，在不改变提示词的情况下，就能显著改变模型性能（如 Qwen2.5-7B 在 GSM8K 上提升了约 7%）。
*   **模型家族敏感度差异**：
    *   **Qwen 模型**：对模式级指令非常敏感且通常受益，甚至在某些情况下 Key-only 优于 Prompt-only。
    *   **LLaMA 模型**：更依赖提示级引导，对模式键的改变较为脆弱，有时 Key-only 反而会导致性能大幅下降。
*   **非加性交互效应**：组合两个通道（Both）并不总是带来 1+1>2 的效果。在某些模型上，双通道会产生冗余或冲突，导致性能低于单通道最优值。

### 7. 优点
*   **视角新颖**：首次系统性地将模式键视为一种“指令通道”，打破了模式仅是结构约束的固有认知。
*   **轻量化优化**：提出了一种无需重新训练模型、仅通过优化 Schema 命名即可提升结构化生成性能的新路径。
*   **严谨的消融设计**：通过四种设置的对比，清晰地揭示了不同指令来源之间的复杂关系。

### 8. 不足与局限
*   **任务覆盖面有限**：实验仅集中在数学推理任务，尚不清楚该结论是否适用于信息抽取、工具调用或代码生成等其他结构化任务。
*   **模式组件单一**：仅研究了模式键（Keys），未探讨字段描述（Descriptions）、字段顺序或嵌套结构对指令信号的影响。
*   **缺乏机理分析**：虽然观察到了模型家族间的差异，但未从预训练数据分布或模型架构层面深入解释为何 Qwen 和 LLaMA 表现出截然不同的敏感性。
*   **未探索最优命名**：研究旨在证明效应存在，而非寻找“万能的最优键名”。

（完）
