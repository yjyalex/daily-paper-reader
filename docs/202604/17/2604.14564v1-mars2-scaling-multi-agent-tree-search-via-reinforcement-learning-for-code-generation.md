---
title: "MARS$^2$: Scaling Multi-Agent Tree Search via Reinforcement Learning for Code Generation"
title_zh: MARS$^2$：通过强化学习扩展多智能体树搜索以进行代码生成
authors: "Pengfei Li, Shijie Wang, Fangyuan Li, Yikun Fu, Kaifeng Liu, Kaiyan Zhang, Dazhi Zhang, Yuqiang Li, Biqing Qi, Bowen Zhou"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14564v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 通过强化学习进行多智能体树搜索以实现推理密集型代码生成
tldr: MARS$^2$ 针对代码生成任务中强化学习轨迹多样性不足的问题，提出了一种结合多智能体协作与树搜索的统一框架。该框架通过让多个独立优化的智能体在共享的树状搜索环境中协作，并引入路径级组优势函数和树一致性奖励塑造，有效提升了探索效率和信用分配。实验证明，MARS$^2$ 在多个代码生成基准测试中显著优于现有方法，展示了多智能体搜索扩展的潜力。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在代码生成中受限于单一智能体的探索多样性，且多智能体协作往往与结构化搜索脱节。
method: 提出 MARS$^2$ 框架，将搜索树建模为多智能体交互环境，并利用路径级组优势和奖励塑造实现高效的信用分配。
result: 在代码生成基准测试中，MARS$^2$ 在不同模型组合和训练设置下均实现了性能的持续提升。
conclusion: 将多智能体协作与树搜索深度耦合是提升强化学习在复杂推理任务中表现的有效途径。
---

## 摘要
强化学习（RL）范式在代码生成等推理密集型任务中表现出强大的性能。然而，有限的轨迹多样性往往导致收益递减，从而限制了可达到的性能上限。搜索增强的强化学习通过引入结构化探索缓解了这一问题，但仍受限于单智能体策略先验。同时，利用多个相互作用的策略可以获得更多样化的探索信号，但现有方法通常与结构化搜索脱节。我们提出了 MARS$^2$（多智能体强化树搜索扩展），这是一个统一的强化学习框架，其中多个独立优化的智能体在共享的树状结构搜索环境中进行协作。MARS$^2$ 将搜索树建模为一个可学习的多智能体交互环境，使异构智能体能够在共享的搜索拓扑中协作生成并优化候选解决方案。为了支持有效的学习，我们引入了一种基于树一致性奖励塑造的路径级组优势公式，这有助于在复杂的搜索轨迹中进行有效的信用分配。在代码生成基准测试上的实验表明，MARS$^2$ 在不同的模型组合和训练设置下均能持续提升性能，证明了将多智能体协作与树搜索相结合以增强强化学习的有效性。我们的代码已在 https://github.com/TsinghuaC3I/MARTI 公开。

## Abstract
Reinforcement learning (RL) paradigms have demonstrated strong performance on reasoning-intensive tasks such as code generation. However, limited trajectory diversity often leads to diminishing returns, which constrains the achievable performance ceiling. Search-enhanced RL alleviates this issue by introducing structured exploration, which remains constrained by the single-agent policy priors. Meanwhile, leveraging multiple interacting policies can acquire more diverse exploratory signals, but existing approaches are typically decoupled from structured search. We propose \textbf{MARS$^2$} (Multi-Agent Reinforced Tree-Search Scaling), a unified RL framework in which multiple independently-optimized agents collaborate within a shared tree-structured search environment. MARS$^2$ models the search tree as a learnable multi-agent interaction environment, enabling heterogeneous agents to collaboratively generate and refine candidate solutions within a shared search topology. To support effective learning, we introduce a path-level group advantage formulation based on tree-consistent reward shaping, which facilitates effective credit assignment across complex search trajectories. Experiments on code generation benchmarks show that MARS$^2$ consistently improves performance across diverse model combinations and training settings, demonstrating the effectiveness of coupling multi-agent collaboration with tree search for enhancing reinforcement learning. Our code is publicly available at https://github.com/TsinghuaC3I/MARTI.

---

## 论文详细总结（自动生成）

### MARS$^2$: 通过强化学习扩展多智能体树搜索以进行代码生成

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）在代码生成等推理密集型任务中面临的**探索瓶颈**提出了解决方案。
*   **研究动机**：虽然基于组相对策略优化（GRPO）的强化学习显著提升了推理质量，但单智能体策略的探索空间受限于其自身先验分布，容易导致轨迹多样性不足和过早收敛。
*   **现有局限**：现有的“搜索增强 RL”多由单一策略驱动，搜索路径会迅速向高概率分支集中；而现有的“多智能体 RL”虽能提供多样性信号，但通常缺乏结构化的搜索机制（如分支、回溯），难以应对深层、多分支的推理任务。
*   **核心目标**：提出 **MARS$^2$** 框架，将搜索树建模为可学习的多智能体交互环境，实现多智能体协作与结构化树搜索的深度耦合。

#### 2. 方法论：核心思想与技术细节
MARS$^2$ 将推理过程视为多智能体在共享树拓扑中的协作探索：
*   **多智能体树搜索（AB-MCTS）**：利用汤普森采样（Thompson Sampling）在每一扩展步骤中选择最合适的“智能体-节点”对。支持“生成节点”（水平扩展新方案）和“优化节点”（垂直改进已有方案）两种功能。
*   **树一致性奖励塑造（Tree-Consistent Reward Shaping）**：
    *   为了解决复杂树路径中的信用分配问题，引入混合基准 $b(v)$，结合了父节点奖励 $r_{p(v)}$ 和兄弟节点平均奖励 $\mu_{C(p(v)) \setminus v}$。
    *   公式定义结构一致性增益 $\Delta(v) = r_v - b(v)$，并据此调整原始奖励，鼓励智能体在超越父节点的同时在同级竞争中胜出。
*   **路径级组优势（Path-level Group Advantage）**：将 GRPO 的组相对优势扩展到树级别。每个智能体根据其生成的节点在整个搜索树中的相对表现进行独立优化。
*   **异步更新机制**：采用基于缓冲区的异步更新方案，缓解了随机树搜索带来的样本分布不均和同步等待问题。

#### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 DeepCoder 发布的开源代码生成数据集，过滤后保留 7,992 个题目。
    *   **测试**：LiveCodeBench (v6, 2025年1月-5月发布)，有效避免数据污染。
    *   **泛化测试**：在 MATH 数学推理数据集上验证通用性。
*   **Benchmark 与对比方法**：
    *   **基础模型**：Qwen3-8B/14B、AReaL-8B/14B（代码专有模型）。
    *   **对比算法**：Vanilla GRPO（单智能体并行采样）、RS$^2$（单智能体强化树搜索）。
*   **评估指标**：Pass@1、Pass@1 (MCTS)（基于“最后获胜”规则的搜索性能）、Pass@N 以及多种多样性指标（如 AEC, G-Vendi 等）。

#### 4. 资源与算力
论文在附录 E 中明确了实验配置：
*   **硬件环境**：每个模型（Actor 和 Reference）配置 **8 张 GPU**。
*   **推理引擎**：使用 8 个 vLLM 引擎进行并行采样。
*   **训练细节**：Batch Size 为 256，学习率为 $1 \times 10^{-6}$，采用 PyTorch FSDP ZeRO-3 精度。
*   **时长**：文中未明确给出总训练小时数，但提到由于树扩展的顺序性，训练吞吐量较单智能体方法有所下降。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8B 和 14B 两个参数量级，测试了异构模型组合（如 Qwen + AReaL）。
*   **消融实验**：
    *   验证了**弱智能体**加入后的鲁棒性（加入 DeepCoder-14B）。
    *   分析了**奖励塑造**对训练稳定性的贡献。
    *   探讨了**搜索预算**（4, 8, 16 节点）对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅关注最终准确率，还通过 6 种细分指标深入分析了“多样性”这一核心动机，证明了 MARS$^2$ 确实通过多智能体协作突破了单智能体的探索上限。

#### 6. 主要结论与发现
*   **性能提升**：MARS$^2$ 在所有模型组合上均优于 GRPO 和 RS$^2$。例如，Qwen3-8B 的 Pass@1 提升了 8.0%，AReaL-14B 达到了 64.6% 的高分。
*   **协作效应**：异构系统（Q&A 组合）在 Pass@1 (MCTS) 上表现出显著的互补性，证明了多智能体在共享树中能有效聚合不同策略的优势。
*   **多样性是关键**：多样性分析显示，MARS$^2$ 在全局覆盖率和算法多样性上排名最高，这是其克服单智能体性能饱和的主要原因。
*   **稳定性**：树一致性奖励塑造显著平滑了训练曲线，加快了收敛速度。

#### 7. 优点
*   **理论统一**：成功将多智能体协作与结构化树搜索整合进统一的 RL 框架，填补了现有研究的空白。
*   **信用分配创新**：提出的树一致性奖励塑造方法，巧妙地利用了树结构的层级信息来提供更密集的监督信号。
*   **泛化性强**：实验证明该方法不仅适用于代码生成，在数学推理任务中同样有效。

#### 8. 不足与局限
*   **训练效率**：由于树搜索需要顺序扩展节点，无法像标准 GRPO 那样实现完全并行的轨迹采样，导致**训练耗时增加**。
*   **计算开销**：维护多个 Actor 模型和进行复杂的树搜索对显存和算力有较高要求。
*   **智能体失衡风险**：消融实验显示，引入过弱的智能体可能会在一定程度上干扰强智能体的微观优化，尽管系统整体性能仍在提升。

（完）
