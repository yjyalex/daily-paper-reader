---
title: "Large Language Model Post-Training: A Unified View of Off-Policy and On-Policy Learning"
title_zh: 大语言模型后训练：离策与在策学习的统一视角
authors: "Shiwan Zhao, Zhihu Wang, Xuyang Zhao, Jiaming Zhou, Caiyue Xu, Chenfei Liu, Liting Zhang, Yuhang Jia, Yanzhe Zhang, Hualong Yu, Zichen Xu, Qicheng Li, Yong Qin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07941v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: LLM后训练综述，涵盖强化学习、过程监督和偏好优化
tldr: 本文提出了大语言模型（LLM）后训练的统一视角，将SFT、偏好优化和强化学习等方法归纳为离策（off-policy）与在策（on-policy）学习。研究通过支持扩展、策略重塑和行为巩固三个核心角色重新定义了后训练过程，强调了多阶段系统协同设计在提升模型能力和对齐中的关键作用，为诊断后训练瓶颈提供了新框架。
selection_source: fresh_fetch
motivation: 现有的LLM后训练方法研究较为零散，缺乏一个统一的理论框架来解释这些方法如何针对性地解决模型行为瓶颈。
method: 提出以轨迹来源（离策与在策）为核心的分类法，并从支持扩展、策略重塑和行为巩固三个功能维度解读后训练干预手段。
result: 该框架成功地将SFT、偏好优化、强化学习和蒸馏等技术整合进一个协调的系统视角，揭示了各阶段在行为空间中的互补性。
conclusion: LLM后训练的未来进步将日益依赖于多阶段系统的协同设计，而非单一优化目标的突破。
---

## 摘要
后训练已成为将预训练大语言模型（LLM）转化为对齐、高效且可部署系统的核心。近期进展涵盖了监督微调（SFT）、偏好优化、强化学习（RL）、过程监督、验证器引导方法、蒸馏以及多阶段流水线。然而，这些方法通常以碎片化的方式被讨论，按标签或目标进行组织，而非根据它们所解决的行为瓶颈。本综述认为，LLM 后训练最好被理解为对模型行为的结构化干预。我们首先根据轨迹来源对该领域进行组织，这定义了两种主要范式：基于外部提供轨迹的离策（off-policy）学习，以及基于学习者生成采样（rollouts）的在策（on-policy）学习。随后，我们通过两个反复出现的角色来解释这些方法——有效支撑集扩展（effective support expansion），使有用行为更易触达；以及策略重塑（policy reshaping），在已触达区域内改进行为——此外还有一个互补的系统级角色，即行为巩固（behavioral consolidation），负责在不同阶段和模型转换中保留、迁移和摊销有用行为。在此视角下，SFT 既可以用于支撑集扩展，也可以用于策略重塑；偏好优化通常是离策重塑，尽管在线变体更接近学习者生成的状态。在策 RL 通常会改进学习者生成状态下的行为，但更强的引导也可以使难以触达的推理路径变得可触达。蒸馏通常被更好地理解为巩固而非仅仅是压缩，而混合流水线则表现为协调的多阶段组合。总体而言，该框架有助于诊断后训练瓶颈并推导阶段组合，表明进展日益依赖于协调的系统设计，而非任何单一的主导目标。

## Abstract
Post-training has become central to turning pretrained large language models (LLMs) into aligned, capable, and deployable systems. Recent progress spans supervised fine-tuning (SFT), preference optimization, reinforcement learning (RL), process supervision, verifier-guided methods, distillation, and multi-stage pipelines. Yet these methods are often discussed in fragmented ways, organized by labels or objectives rather than by the behavioral bottlenecks they address. This survey argues that LLM post-training is best understood as structured intervention on model behavior. We organize the field first by trajectory provenance, which defines two primary regimes: off-policy learning on externally supplied trajectories and on-policy learning on learner-generated rollouts. We then interpret methods through two recurring roles -- effective support expansion, which makes useful behaviors more reachable, and policy reshaping, which improves behavior within already reachable regions -- together with a complementary systems-level role, behavioral consolidation, which preserves, transfers, and amortizes useful behavior across stages and model transitions. Under this view, SFT may serve either support expansion or policy reshaping; preference optimization is usually off-policy reshaping, though online variants move closer to learner-generated states. On-policy RL often improves behavior on learner-generated states, but stronger guidance can also make hard-to-reach reasoning paths reachable. Distillation is often better understood as consolidation rather than only compression, and hybrid pipelines emerge as coordinated multi-stage compositions. Overall, the framework helps diagnose post-training bottlenecks and reason about stage composition, suggesting that progress increasingly depends on coordinated systems design rather than any single dominant objective.

---

## 论文详细总结（自动生成）

这篇论文《Large Language Model Post-Training: A Unified View of Off-Policy and On-Policy Learning》对大语言模型（LLM）后训练阶段进行了系统性的梳理与理论统一。以下是详细的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的 LLM 后训练方法（如 SFT、DPO、RLHF、过程监督、蒸馏等）在文献中往往是碎片化的，通常按历史标签或损失函数分类，缺乏统一的逻辑框架。
*   **核心问题**：如何从“行为干预”的角度理解后训练？为什么顶尖模型（如 Llama 3, DeepSeek-R1）需要复杂的多阶段流水线？
*   **整体含义**：论文提出以后训练为手段，对模型行为进行结构化干预。通过引入“轨迹来源”和“功能角色”的概念，将看似孤立的方法整合进一个统一的系统设计视角中。

### 2. 方法论：核心思想与技术细节
论文提出了一个三层分析框架：

*   **第一层：轨迹来源（Trajectory Provenance）**
    *   **离策学习（Off-policy）**：在外部提供的轨迹上更新模型（如 SFT、离线偏好优化）。优点是能引入模型尚未掌握的行为，缺点是存在分布偏移。
    *   **在策学习（On-policy）**：在模型自身生成的采样（Rollouts）上更新（如 RLHF、在线 DPO、RLVR）。优点是能直接纠正模型在部署时会遇到的错误。
*   **第二层：监督接口（Supervision Interfaces）**
    *   定义了信号如何附加到轨迹上，包括：Token 目标、偏好对、奖励/评估器、验证器/过程反馈、教师引导目标。
*   **第三层：功能角色（Functional Roles）**
    *   **有效支撑集扩展（Effective Support Expansion）**：通过外部监督使原本概率极低的有用行为变得可触达。
    *   **策略重塑（Policy Reshaping）**：在已有的行为空间内重新分配概率，提升排序和对齐水平。
    *   **行为巩固（Behavioral Consolidation）**：系统级角色，负责在模型转换（如大传小）或阶段转换中保留、迁移和摊销有用行为。
*   **数学表达**：引入了基于状态-动作占用度（Occupancy Measure）的有效支撑集定义 $E_\epsilon(\pi; D_x)$，用以区分模型是“学会了新技能”还是“优化了已有技能的表达”。

### 3. 实验设计
*   **性质说明**：本论文是一篇**概念驱动的综述（Concept-driven Survey）**，而非实验性论文。
*   **分析对象**：论文对近年来主流的后训练流水线进行了深度解构，包括：
    *   **前沿模型报告**：Llama 3, DeepSeek-V3/R1, GLM-5, Kimi K2 等。
    *   **算法家族**：SFT 变体、DPO/SimPO、PPO/GRPO、过程奖励模型（PRM）、知识蒸馏（MiniLLM 等）。
*   **对比维度**：通过统一框架对比了不同方法在解决行为瓶颈（如推理错误、对齐失效）时的优劣。

### 4. 资源与算力
*   **算力说明**：由于本文是综述性质，**文中未提及作者自身进行实验所消耗的具体算力（如 GPU 型号或时长）**。
*   **文献提及**：文中讨论了在策学习（On-policy RL）通常比离策学习（Off-policy）消耗更多算力，因为需要频繁进行在线采样和评估。

### 5. 实验数量与充分性
*   **覆盖范围**：论文引用了大量（136篇）核心文献，涵盖了从早期的指令微调到最新的强化学习推理模型。
*   **充分性与客观性**：
    *   **充分性**：作为综述，它对多阶段流水线的组合逻辑（如 SFT → RL → Distillation）分析得非常透彻。
    *   **客观性**：论文并未宣称某种方法绝对优于另一种，而是指出它们分别解决了不同的行为瓶颈（如 SFT 负责扩展支撑集，RL 负责在策重塑）。

### 6. 论文的主要结论与发现
*   **后训练是系统工程**：进步不再依赖单一目标函数，而在于多阶段的协同设计。
*   **混合流水线的必然性**：SFT 难以解决模型自身的采样错误，而 RL 难以引入模型完全不会的行为，因此必须结合。
*   **蒸馏的新定义**：蒸馏不应仅被视为压缩，而应视为一种“行为巩固”机制，将昂贵的搜索或教师能力内化到策略模型中。
*   **诊断瓶颈是关键**：开发者应首先诊断是“支撑集不足”（模型不会）还是“重塑不足”（模型会但选不对），再决定使用离策还是在策方法。

### 7. 优点
*   **理论统一性强**：首次将强化学习中的离策/在策概念完美引入 LLM 后训练的语境，消除了术语混乱。
*   **功能角色视角新颖**：提出的“支撑集扩展”与“策略重塑”为理解模型能力来源提供了清晰的直觉。
*   **实战指导意义**：对多阶段流水线（如 DeepSeek-R1 的演进）给出了极具说服力的理论解释。

### 8. 不足与局限
*   **缺乏定量验证**：虽然提出了有效支撑集的数学定义，但缺乏大规模的实证数据来精确量化不同方法对支撑集改变的具体数值。
*   **理论简化**：在现实中，扩展和重塑的界限有时是模糊的（例如局部动作的重塑可能导致全局轨迹的扩展），论文对此虽有提及但未深入探讨其耦合机制。
*   **应用限制**：该框架主要适用于通用能力和推理对齐，对于多模态或极长文本等特殊领域的后训练适配讨论较少。

（完）
