---
title: "LongAct: Harnessing Intrinsic Activation Patterns for Long-Context Reinforcement Learning"
title_zh: LongAct：利用内在激活模式实现长上下文强化学习
authors: "Bowen Ping, Zijun Chen, Tingfeng Hui, Qize Yu, Chenxuan Li, Junchi Yan, Baobao Chang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14922v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习用于增强大语言模型的推理能力
tldr: "本研究针对长文本强化学习中模型内在表示利用不足的问题，提出 LongAct 策略。通过观察发现长文本处理中 query 和 key 向量存在高幅值激活，且长文本推理具有稀疏结构。LongAct 放弃均匀更新，转而采用显著性引导的稀疏更新，仅优化与关键激活相关的权重。实验表明，该方法在 LongBench v2 上提升约 8%，并在 RULER 基准上展现出更强的泛化性，且适用于多种 RL 算法。"
selection_source: fresh_fetch
motivation: 现有研究多关注奖励或数据，忽视了长文本处理中 query 和 key 向量内高幅值激活对模型优化的关键驱动作用。
method: 提出 LongAct 策略，通过显著性引导的稀疏更新机制，仅针对与高幅值激活相关的关键权重进行选择性优化。
result: "该方法在 LongBench v2 上提升了约 8% 的性能，增强了 RULER 基准的泛化性，并证明了在 GRPO 等多种 RL 算法上的通用性。"
conclusion: 聚焦于模型内部的显著激活特征是解锁大语言模型长文本推理潜力的核心关键。
---

## 摘要
强化学习 (RL) 已成为增强大语言模型 (LLM) 推理能力的关键驱动力。虽然最近的进展主要集中在奖励工程或数据合成上，但很少有研究利用模型的内在表示特征来指导训练过程。在本文中，我们首先观察到在处理长上下文时，查询 (query) 和键 (key) 向量中存在高幅值激活。借鉴模型量化（该领域确立了此类高幅值激活的关键性）以及长上下文推理本质上呈现稀疏结构的见解，我们假设这些权重是实现有效模型优化的关键驱动因素。基于这一见解，我们提出了 LongAct，这是一种从均匀更新转向显著性引导的稀疏更新策略。通过选择性地仅更新与这些重要激活相关的权重，LongAct 在 LongBench v2 上实现了约 8% 的提升，并增强了在 RULER 基准测试上的泛化能力。此外，我们的方法表现出显著的通用性，在 GRPO 和 DAPO 等多种强化学习算法中均能持续提升性能。广泛的消融实验表明，关注这些显著特征是释放长上下文潜力的关键。

## Abstract
Reinforcement Learning (RL) has emerged as a critical driver for enhancing the reasoning capabilities of Large Language Models (LLMs). While recent advancements have focused on reward engineering or data synthesis, few studies exploit the model's intrinsic representation characteristics to guide the training process. In this paper, we first observe the presence of high-magnitude activations within the query and key vectors when processing long contexts. Drawing inspiration from model quantization -- which establishes the criticality of such high-magnitude activations -- and the insight that long-context reasoning inherently exhibits a sparse structure, we hypothesize that these weights serve as the pivotal drivers for effective model optimization. Based on this insight, we propose LongAct, a strategy that shifts from uniform to saliency-guided sparse updates. By selectively updating only the weights associated with these significant activations, LongAct achieves an approximate 8% improvement on LongBench v2 and enhances generalization on the RULER benchmark. Furthermore, our method exhibits remarkable universality, consistently boosting performance across diverse RL algorithms such as GRPO and DAPO. Extensive ablation studies suggest that focusing on these salient features is key to unlocking long-context potential.

---

## 论文详细总结（自动生成）

这篇论文提出了 **LongAct**，一种通过利用模型内部激活模式来优化长上下文强化学习（RL）的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然强化学习已成为提升大语言模型（LLM）推理能力的关键，但现有针对长上下文的 RL 研究多集中在外部信号（如奖励函数设计、数据合成或课程学习），而将模型内部计算视为“黑盒”。
*   **核心发现**：作者观察到在处理长文本时，Query（查询）和 Key（键）向量中存在极少数高幅值（High-magnitude）的激活值（离群值）。
*   **核心假设**：这些高幅值激活是长上下文推理的“结构锚点”。借鉴模型量化的思路，作者认为在训练中仅针对这些关键激活相关的权重进行更新，比全参数均匀更新更有效。

### 2. 方法论：核心思想与技术细节
LongAct 提出了一种**显著性引导的稀疏更新策略**，主要包含以下步骤：
*   **识别关键激活**：在训练前，通过计算输入隐藏状态在序列维度上的 $\ell_2$ 范数，识别出 Query ($Q$) 和 Key ($K$) 投影层中具有高幅值的特征维度。
*   **动态掩码生成**：
    *   针对每个注意力头（Head），选择幅值最大的前 $k$ 个维度（实验中 sparsity ratio $\lambda$ 默认为 0.3，即前 30%）。
    *   将这些局部维度映射到权重矩阵 $W_Q$ 和 $W_K$ 的对应行。
*   **稀疏参数更新**：在强化学习的训练过程中，通过梯度掩码（Gradient Mask）确保只有与高幅值激活相关的权重行接收梯度更新，而其他参数保持冻结。
*   **训练流程**：采用两阶段法，先进行有监督微调（SFT/Cold Start）以适应格式，再进行基于 LongAct 的强化学习（如 DAPO 或 GRPO 算法）。

### 3. 实验设计
*   **模型与基准**：
    *   **模型规模**：Qwen3-4B 和 Qwen3-8B。
    *   **长文本基准**：LongBench v2（深度理解）、RULER（有效上下文窗口）、InfiniteBench（异构长任务）。
    *   **短文本基准**：GSM8K、HumanEval、TruthfulQA（验证通用能力）。
*   **对比方法**：官方发布的 Qwen3 模型、SFT 模型、标准全参数 RL（DAPO/GRPO）。
*   **消融实验**：对比了不同的激活选择策略（最大值、最小值、随机选择）、不同的稀疏比例（20%-40%）以及不同的 RL 算法（DAPO, GRPO, KL-Cov 等）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H800 80GB GPU**。
*   **训练细节**：SFT 阶段最大序列长度 16k，训练 900 步；RL 阶段最大序列长度 32k，采样长度 4k，Batch Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 4B 到 8B 的模型缩放，并在 3 个主流长文本基准和 3 个短文本基准上进行了测试。
*   **充分性评价**：实验设计非常充分。作者不仅验证了最终性能，还通过**扰动分析**（即手动屏蔽高幅值 vs 低幅值激活）直观展示了高幅值激活对模型逻辑连贯性的决定性作用。消融实验涵盖了算法通用性和超参数敏感性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：LongAct 在 LongBench v2 上比标准 RL 提升了约 **8%**，在 RULER-128K 上提升了约 **4%**。
*   **推理稳定性**：在“困难”任务和“超长”文本（>128k）下，LongAct 的优势尤为明显，有效缓解了长文本推理中的错误累积。
*   **关键特征**：实验证明，更新高幅值激活相关的权重远比更新低幅值或随机权重有效。屏蔽高幅值激活会导致模型立即崩溃（如陷入重复循环），而屏蔽低幅值激活则影响较小。
*   **通用性**：该方法不仅适用于长文本，在短文本任务（如数学和代码）上也优于全参数 RL。

### 7. 优点
*   **视角独特**：从模型内部表示（激活显著性）出发，为 RL 训练提供了一种更具解释性的引导方式。
*   **高效且无损**：在推理阶段不改变模型架构，无额外延迟；在训练阶段通过稀疏更新降低了优化噪声。
*   **算法兼容性强**：可以无缝集成到现有的各种策略优化算法（如 GRPO, DAPO）中。

### 8. 不足与局限
*   **模型规模限制**：受限于算力，实验最高仅在 8B 模型上进行，尚未在大规模模型（如 70B+）上验证其缩放效应。
*   **参数覆盖面**：目前主要针对 $W_Q$ 和 $W_K$ 层进行稀疏化，对于 MLP 层或其他注意力组件（如 $W_V, W_O$）的内在模式探索相对较少。
*   **训练长度限制**：虽然测试了长文本，但 RL 训练时的最大序列长度限制在 32k，对于更长（如 1M）上下文的直接训练效果尚待观察。

（完）
