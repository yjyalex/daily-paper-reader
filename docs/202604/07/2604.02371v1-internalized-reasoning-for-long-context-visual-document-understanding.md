---
title: Internalized Reasoning for Long-Context Visual Document Understanding
title_zh: 长上下文视觉文档理解的内化推理
authors: Austin Veselka
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.02371v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 思考轨迹与思维链推理
tldr: 针对长文档视觉理解中推理能力不足的问题，本文提出一种合成数据流水线，通过生成思维链并利用低强度模型融合技术将其“内化”。该方法在Qwen3 VL和Mistral模型上显著提升了长文档理解性能，不仅在MMLongBenchDoc上超越了更大规模的模型，还大幅减少了推理时的输出长度，实现了高效且强大的视觉推理能力。
selection_source: fresh_fetch
motivation: 现有的开源长文档视觉理解模型缺乏类似数学和代码领域的推理能力，限制了其在复杂场景下的表现。
method: 开发合成数据流水线生成包含页面相关性评分和证据提取的思维链，并通过SFT及模型融合技术将显式推理内化。
result: Qwen3 VL 32B在MMLongBenchDoc上达到58.3分，超越了规模大7倍的235B模型，且内化推理的输出长度缩减了12.4倍。
conclusion: 通过合成推理数据和模型内化技术，可以在不增加推理开销的前提下显著提升中等规模多模态模型处理长文档的能力。
---

## 摘要
视觉长文档理解对于企业、法律和科学应用至关重要，然而目前性能最佳的开源方案尚未探索推理能力，而推理能力正是推动数学和代码性能飞跃的关键。我们引入了一种用于长文档理解推理的合成数据流水线，该流水线通过对每页内容进行问题相关性评分、提取文本证据并按相关性从高到低排序来生成思考轨迹。我们对包含在 <think> 标签内、由 <cot> 控制标记引导的生成轨迹应用有监督微调（SFT），并通过低强度模型合并将所得的推理能力内化。我们研究了 Qwen3 VL 32B 和 Mistral Small 3.1 24B。使用 Qwen3 VL，我们在 MMLongBenchDoc 上达到了 58.3 分，超过了规模大 7 倍的 Qwen3 VL 235B A22B（57.0 分）。使用 Mistral，我们证明了合成推理在 MMLBD-C 上的表现比从“思考版”轨迹中蒸馏出的结果高出 3.8 分，且内化推理相比显式推理，其平均输出标记减少了 12.4 倍。我们发布了该流水线，以供复现和进一步探索。

## Abstract
Visual long-document understanding is critical for enterprise, legal, and scientific applications, yet the best performing open recipes have not explored reasoning, a capability which has driven leaps in math and code performance. We introduce a synthetic data pipeline for reasoning in long-document understanding that generates thinking traces by scoring each page for question relevance, extracting textual evidence and ordering it from most to least relevant. We apply SFT to the resulting traces within \texttt{<think>} tags, gated by a \texttt{<cot>} control token, and the resulting reasoning capability is internalized via low-strength model merging. We study Qwen3 VL 32B and Mistral Small 3.1 24B. With Qwen3 VL, we achieve 58.3 on MMLongBenchDoc, surpassing the 7$\times$ larger Qwen3 VL 235B A22B (57.0). With Mistral, we show that synthetic reasoning outperforms distillation from the Thinking version's traces by 3.8 points on MMLBD-C, and internalized reasoning exhibits 12.4$\times$ fewer mean output tokens compared to explicit reasoning. We release our pipeline for reproducibility and further exploration.

---

## 论文详细总结（自动生成）

以下是对论文《Internalized Reasoning for Long-Context Visual Document Understanding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的长上下文视觉语言模型（VLM）在文档理解任务中，虽然能处理数百页内容，但缺乏像数学或代码领域那样的“推理”能力。现有的推理模型（如 DeepSeek-R1, OpenAI o1）在文档问答（VQA）中表现提升有限。
*   **研究背景**：长文档理解（如法律、财务、科研文档）需要跨页面的信息检索、多跳推理和总结。作者旨在探索如何通过合成推理数据和模型训练技术，让中等规模的模型在不增加推理开销（Token 数量）的前提下，获得超越大规模模型的长文档理解能力。

### 2. 方法论：核心思想与关键技术
*   **合成推理流水线（Synthetic Reasoning Pipeline）**：
    *   **阶段 1：证据提取与相关性评分**。使用 VLM 对文档的每一页独立处理，提取与问题相关的文本证据并给出相关性评分（0-10分）。
    *   **阶段 2：答案生成**。将评分最高的 Top-K 页面的证据进行排序。通过两个分支生成答案：**视觉分支**（教师 VLM 直接看原图生成答案）和**文本分支**（教师 LLM 仅根据提取的文本证据生成答案）。
*   **推理轨迹设计（Trace Design）**：
    *   **V2 版本改进**：放弃了按页面顺序扫描的“死循环”模式，改为“有界检索”模式（仅包含 Top-K 相关页，按相关性排序），有效解决了模型在推理时陷入无限循环的问题。
*   **推理内化（Internalized Reasoning）**：
    *   **控制标记**：使用 `<cot>` 标记引导推理过程，推理内容置于 `<think>` 标签内。
    *   **模型合并（Model Merging）**：通过任务算术（Task Arithmetic）将 SFT 后的模型与基础模型合并。发现当合并强度 $\alpha=0.25$ 时，模型在推理时不输出显式的 `<think>` 标记（Token 数量与普通模型持平），但性能保留了推理模型的增益。

### 3. 实验设计
*   **数据集**：使用 250K 份 PDF（16M 页）作为语料，合成 50K 组推理数据。
*   **Benchmark**：
    *   核心指标：**MMLongBenchDoc (MMLBD)** 和 **MMLBD-C**。
    *   综合指标：Visual-LC Avg (VA) 和 LC Avg (LCA)，涵盖 SlideVQA, DUDE, HELMET, LongBench v2 等。
*   **对比方法**：
    *   基座模型：Qwen3 VL 32B Instruct, Mistral Small 3.1 24B。
    *   教师模型：Qwen3 VL 235B A22B (Instruct & Thinking)。
    *   其他方案：LongPO, 纯蒸馏 SFT, 无推理基准（No-think）。

### 4. 资源与算力
*   **硬件配置**：使用 H100 GPU 集群。
*   **训练细节**：
    *   **阶段 1**：处理最多 104 页，使用 32 路上下文并行（CP）。
    *   **阶段 2**：处理最多 336 页，使用 64 路上下文并行（CP）。
*   **总算力消耗**：主训练实验约消耗 **40,000 H100 小时**；加上数据生成、评估及额外实验，项目总计消耗约 **100,000 H100 小时**。

### 5. 实验数量与充分性
*   **实验规模**：针对 Qwen 和 Mistral 两个模型家族进行了完整验证。
*   **消融实验**：
    *   **控制标记消融**：验证了 `<cot>` 标记在推理时对性能的开关作用。
    *   **合并强度消融**：研究了 $\alpha$ 从 0.25 到 0.5 的变化对输出长度和性能的影响。
    *   **轨迹设计对比**：对比了 V1（顺序扫描）和 V2（相关性排序）的效果。
    *   **数据来源对比**：对比了自研合成轨迹与 Qwen 官方 Thinking 轨迹的效果。
*   **充分性评价**：实验设计非常严谨，通过控制变量法证明了性能提升源于“推理过程”而非单纯的“数据蒸馏”，且跨模型家族的验证增强了结论的普适性。

### 6. 主要结论与发现
*   **超越规模限制**：Qwen3 VL 32B 经过内化推理训练后，在 MMLBD 上达到 58.3 分，**超越了规模大 7 倍的 Qwen3 VL 235B**。
*   **内化是高效的**：通过低强度合并，模型实现了“隐式思维链”，其平均输出 Token 数比显式推理减少了 **12.4 倍**，且性能不减。
*   **轨迹质量至关重要**：结构化的、基于相关性排序的推理轨迹比自由格式的 Thinking 轨迹更有效。
*   **可控性**：即使推理被内化，通过控制标记（Control Token）仍能在推理时开启或关闭这种增强能力。

### 7. 优点
*   **创新性**：首次在长文档 VLM 领域展示了如何通过模型合并实现推理能力的“内化”，平衡了性能与推理成本。
*   **实用性**：解决了长文档处理中常见的“迷失在中间”和“上下文腐烂”问题。
*   **开源贡献**：发布了合成数据流水线，有助于社区复现长上下文推理能力。

### 8. 不足与局限
*   **设计耦合**：V2 版本的改进包含了多个变量（相关性排序、页码前缀等），论文未逐一拆解每个变量的贡献度。
*   **评估偏差**：由于成本原因，部分 Benchmark 使用了本地模型（GLM 4.5V）作为裁判并进行了子采样，可能与官方 Leaderboard 存在细微偏差。
*   **训练局限**：目前仅探索了 SFT 阶段，尚未结合强化学习（RL）或偏好优化（PO）进一步提升推理上限。
*   **黑盒机制**：虽然观察到了内化现象，但其在模型权重中的具体表示机制尚不明确。

（完）
