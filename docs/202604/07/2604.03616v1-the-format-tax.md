---
title: The Format Tax
title_zh: 格式税
authors: "Ivan Yee Lee, Loris D'Antoni, Taylor Berg-Kirkpatrick"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03616v1"
tags: ["query:llm-fo"]
score: 10.0
evidence: 结构化输出要求显著降低了推理性能
tldr: 本研究揭示了“格式税”现象，即要求大模型输出结构化格式（如JSON）会显著降低其推理和写作能力。研究发现，性能下降主要源于提示词中的格式要求而非解码约束。通过将推理与格式化解耦（如先生成自由文本再转换，或在单次生成中启用长思考），可以有效恢复损失的准确率。实验涵盖多种模型和任务，证明了该方法的普适性。
selection_source: fresh_fetch
motivation: 探究为何要求大模型输出特定格式（如JSON、XML）会导致其推理和写作性能显著下降。
method: 提出将推理过程与格式化要求解耦，通过先生成自由文本再重构或在单次生成中分离思考过程。
result: 在六种开源和四种闭源模型上的实验表明，解耦策略能有效恢复大部分因格式要求而损失的准确率。
conclusion: 格式税并非结构化生成的固有缺陷，通过解耦推理与格式化可以弥补当前开源模型在处理复杂格式时的能力差距。
---

## 摘要
要求大语言模型以 JSON 格式响应应当是一种格式选择，而非一种能力税。然而，我们发现结构化输出要求（如 JSON、XML、LaTeX、Markdown）会显著降低开源权重模型的推理和写作性能。研究界的回应主要集中在约束解码（constrained decoding）上，但采样偏差仅占性能下降的一小部分。主要的成本源于提示词：在应用任何解码器约束之前，仅格式请求指令本身就导致了大部分准确率损失。这一诊断指向一个简单的原则：将推理与格式化解耦。无论是通过先生成自由文本并在第二次处理中重新格式化，还是通过在单次生成中启用扩展思考，将这两个关注点分离都能显著恢复损失的准确率。在六个开源权重模型、四个 API 模型、四种格式以及涵盖数学、科学、逻辑和写作的任务中，解耦恢复了大部分损失的准确率。值得注意的是，大多数最新的闭源权重模型几乎没有表现出格式税，这表明该问题并非结构化生成所固有，而是当前开源权重模型尚未弥补的差距。代码可在 https://github.com/ivnle/the-format-tax 获取。

## Abstract
Asking a large language model to respond in JSON should be a formatting choice, not a capability tax. Yet we find that structured output requirements -- JSON, XML, LaTeX, Markdown -- substantially degrade reasoning and writing performance across open-weight models. The research response has focused on constrained decoding, but sampling bias accounts for only a fraction of the degradation. The dominant cost enters at the prompt: format-requesting instructions alone cause most of the accuracy loss, before any decoder constraint is applied. This diagnosis points to a simple principle: decouple reasoning from formatting. Whether by generating freeform first and reformatting in a second pass, or by enabling extended thinking within a single generation, separating the two concerns substantially recovers lost accuracy. Across six open-weight models, four API models, four formats, and tasks spanning math, science, logic, and writing, decoupling recovers most lost accuracy. Notably, most recent closed-weight models show little to no format tax, suggesting the problem is not inherent to structured generation but a gap that current open-weight models have yet to close. Code is available at https://github.com/ivnle/the-format-tax.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Format Tax》（格式税）的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：为什么要求大语言模型（LLM）以特定结构化格式（如 JSON、XML、LaTeX）输出，会导致其推理和写作性能显著下降？这种性能损失被称为“格式税”（Format Tax）。
*   **研究背景**：在现代 AI 应用中，结构化输出是函数调用、工具使用和代理框架的基础。虽然研究界此前多关注“语法约束解码”（GCD）导致的采样偏差，但本文旨在探究：这种性能下降究竟发生在**解码阶段**（由于约束了 Token 概率分布）还是**提示词阶段**（由于模型必须同时处理推理和格式化要求）？

### 2. 核心方法论
论文提出了**“解耦推理与格式化”**的核心思想，并对比了以下几种生成策略：
*   **1-Turn（单轮生成）**：模型在单次生成中同时完成推理和格式化输出。
*   **2-Turn（两轮生成/解耦）**：
    *   **第一步**：模型以自由格式（Freeform）生成自然语言答案，不考虑格式限制。
    *   **第二步**：将第一步的答案作为上下文，重新格式化为目标结构（如 JSON）。
*   **Thinking（思考模式）**：在输出最终格式化结果之前，允许模型在隐藏的“草稿本”（scratchpad）中进行长文本推理。
*   **技术诊断**：通过对比“带格式要求的提示词+无约束解码”与“带格式要求的提示词+有约束解码（GCD）”，量化提示词层面和解码器层面的成本占比。

### 3. 实验设计
*   **数据集/场景**：
    *   **MATH-500**：竞赛级数学问题（多步符号推理）。
    *   **GPQA-Diamond**：研究生水平的科学问题（物理、化学、生物）。
    *   **ZebraLogic**：复杂的逻辑约束满足问题。
    *   **WritingBench**：开放式写作任务（评估文笔质量）。
*   **Benchmark（基准）**：自由格式生成（Freeform Generation），即不要求任何特定输出格式。
*   **对比模型**：
    *   **开源模型**：Qwen2.5 (32B/8B), OLMo (32B/7B), SmolLM3 (3B), Nemotron-3 (8B)。
    *   **闭源 API 模型**：GPT-4o 系列、Claude 3.5 Haiku、Grok-2 Fast。
*   **输出格式**：JSON Schema, XML, Markdown, LaTeX。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总推理时长。
*   **运行环境**：开源模型通过 `vLLM` 框架本地部署运行，闭源模型通过官方 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个模型、4 种任务、4 种格式以及多种提示词配置（是否包含 Schema、示例、约束等）。
*   **统计严谨性**：
    *   针对开源模型进行了 144 组对比实验。
    *   使用了 **McNemar 检验**来评估推理准确率差异的显著性。
    *   使用了 **Wilcoxon 符号秩检验**来评估写作质量的差异。
*   **充分性评价**：实验设计非常系统且客观。通过控制变量法（如剥离 LaTeX 标签后重新评分）排除了 LLM 裁判对格式本身的偏见，确保了结论的可靠性。

### 6. 主要结论与发现
*   **格式税真实存在**：所有测试的开源模型在结构化输出要求下，推理准确率均有下降（平均下降约 5-10 个百分点）。
*   **提示词是主因**：大部分性能损失源于提示词中的格式要求，而非解码器的 Token 屏蔽。即使不开启约束解码，只要提示词要求 JSON，性能就会下降。
*   **解耦可恢复性能**：采用 2-Turn 策略或 Thinking 模式可以恢复绝大部分损失的准确率。
*   **闭源模型表现更好**：最新的闭源模型（如 Claude 3.5, Grok-2）几乎没有格式税，暗示这并非固有缺陷，而是可以通过训练优化的差距。
*   **U 型关联**：性能下降与输出长度的改变密切相关。当格式要求导致输出比自由格式显著变短（压缩推理）或变长（过度冗余）时，错误率最高。

### 7. 优点
*   **诊断精准**：成功区分了提示词成本和解码器成本，纠正了研究界过度关注解码算法优化的偏向。
*   **策略实用**：提出的 2-Turn 和 Thinking 策略无需重新训练模型即可显著提升现有系统的鲁棒性。
*   **评估全面**：不仅关注准确率，还深入分析了 Token 长度变化、格式合规率与推理质量之间的复杂关系。

### 8. 不足与局限
*   **成本权衡**：2-Turn 策略虽然有效，但会导致推理延迟翻倍和 Token 消耗增加，在实时性要求高的场景下受限。
*   **模型规模限制**：实验主要集中在 3B 到 32B 的中小型开源模型，未验证超大规模开源模型（如 Llama-3-405B）是否也存在同样的税收。
*   **黑盒限制**：由于无法获知闭源模型的训练细节，无法确定它们是如何消除格式税的（是由于特定微调还是规模效应）。
*   **Schema 复杂度**：实验中使用的 Schema 相对简单，更复杂的嵌套结构可能会带来更严重的性能退化。

（完）
