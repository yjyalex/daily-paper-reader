---
title: Can LLMs Learn to Reason Robustly under Noisy Supervision?
title_zh: 大语言模型能否在噪声监督下学习鲁棒推理？
authors: "Shenzhi Yang, Guangcheng Zhu, Bowen Song, Sharon Li, Haobo Wang, Xing Zheng, Yingfan Ma, Zhongqi Chen, Weiqiang Wang, Gang Chen"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.03993v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 对推理模型的可验证奖励强化学习（RLVR）进行分析
tldr: 本研究探讨了强化学习（RLVR）在推理任务中面对噪声标签的鲁棒性。研究发现RLVR中存在“早期正确性一致性”现象，即训练初期模型对噪声和干净样本的准确率同步提升。基于此，作者提出了在线标签细化（OLR）方法，通过多数投票和历史一致性动态修正潜在噪声标签。实验证明，OLR在多种数学和泛化任务中显著提升了模型在0.1至0.9噪声比例下的表现。
selection_source: fresh_fetch
motivation: 针对强化学习训练推理模型时难以避免的噪声标签问题，系统分析其影响机制并提升模型的鲁棒性。
method: 提出在线标签细化（OLR）方法，利用策略提升过程中的多数投票结果和历史一致性指标动态修正噪声标签。
result: "在多个数学推理和分布外任务中，OLR在不同噪声比例下均取得显著性能提升，平均增幅达3.3%至4.6%。"
conclusion: 即使在高度噪声的监督下，通过利用模型自身的演化规律进行在线标签修正，也能有效训练出鲁棒的推理模型。
---

## 摘要
带有可验证奖励的强化学习（RLVR）能有效训练依赖大量完美标签的推理模型，但由于专家稀缺导致的不可避免的噪声标签，其脆弱性仍未得到充分探讨。在这项工作中，我们迈出了对 RLVR 中噪声标签机制进行系统分析的第一步。与监督分类不同，大多数 RLVR 算法包含一个基于采样（rollout）的条件：标签对训练的影响取决于当前策略能否生成实现该标签的采样结果，这一特性自然也延伸到了噪声标签上。基于这一观察，我们区分了两种类型的噪声：非活跃噪声标签（降低数据效率）和活跃噪声标签（会被强化并存在使模型偏向错误分布的风险）。通过对噪声样本训练的实验，我们发现了一种“早期正确性一致性”（Early Correctness Coherence）现象：尽管噪声样本在后期开始落后，但在训练早期，干净样本和噪声样本的准确率增长趋势相似。受此动态过程启发，我们提出了在线标签精炼（OLR），当满足两个条件时，利用多数投票答案逐步修正潜在的噪声标签：多数答案的采样通过率斜率为正，且在更新过程中保持稳定的历史一致性，从而随着策略的改进实现逐渐自校正。我们在六个分布内数学推理基准测试（AIME24/25、AMC、MATH-500、Minerva 和 Olympiad）以及三个分布外任务（ARC-c、GPQA-diamond 和 MMLU-pro）上评估了 OLR。在 0.1 到 0.9 的噪声比例下，OLR 在非活跃和活跃噪声标签设置下均一致地提高了鲁棒性，在分布内基准测试中实现了 3.6% 到 3.9% 的平均增益，在分布外评估中实现了 3.3% 到 4.6% 的平均增益。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) effectively trains reasoning models that rely on abundant perfect labels, but its vulnerability to unavoidable noisy labels due to expert scarcity remains critically underexplored. In this work, we take the first step toward a systematic analysis of noisy label mechanisms in RLVR. In contrast to supervised classification, most RLVR algorithms incorporate a rollout-based condition: a label's influence on training is contingent on whether the current policy can generate rollouts that realize it, a property that naturally extends to noisy labels. Based on this observation, we distinguish two types of noise: inactive noisy labels, which reduce data efficiency, and active noisy labels, which are reinforced and risk skewing the model toward incorrect distributions. From experiments on training with noisy samples, we identify an Early Correctness Coherence phenomenon: although noisy samples begin to lag behind in later stages, accuracy on both clean and noisy samples increases similarly in early training. Motivated by this dynamic, we propose Online Label Refinement (OLR), which progressively corrects potentially noisy labels with majority-voted answers when two conditions hold: a positive slope in the majority answer's rollout pass rate and stable historical consistency across updates, enabling gradual self-correction as the policy improves. We evaluate OLR on six in-distribution mathematical reasoning benchmarks (AIME24/25, AMC, MATH-500, Minerva, and Olympiad) and three out-of-distribution tasks (ARC-c, GPQA-diamond, and MMLU-pro). Across noise ratios from 0.1 to 0.9, OLR consistently improves robustness under both inactive and active noisy-label settings, achieving average gains of 3.6% to 3.9% on in-distribution benchmarks and 3.3% to 4.6% on out-of-distribution evaluations.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在强化学习（RLVR）框架下应对噪声标签的鲁棒性进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在通过可验证奖励进行强化学习（RLVR，如 GRPO 算法）训练推理模型时，通常假设拥有完美的标签。然而，现实中专家资源稀缺，噪声标签不可避免。论文探讨了 LLM 能否在存在大量错误标注的情况下，依然学习到鲁棒的推理能力。
*   **研究背景**：传统的噪声标签学习多集中在监督分类任务，而 RLVR 具有“生成式”和“在策略（on-policy）”的特性。论文首次系统分析了 RLVR 中的噪声机制，并试图解决噪声标签如何误导模型以及如何自动纠正的问题。

### 2. 方法论
*   **噪声分类**：
    *   **非活跃噪声（Inactive Noise）**：模型无法生成该错误标签，导致采样效率降低，但不会直接误导分布。
    *   **活跃噪声（Active Noise）**：模型有概率生成该错误标签，导致错误路径被正向强化，风险极大。
*   **核心发现：早期正确性一致性（ECC）**：实验观察到，在训练初期，模型对干净样本和噪声样本的准确率会同步提升。这意味着模型在早期通过跨样本耦合学习到了潜在的正确推理路径。
*   **关键技术：在线标签精炼（OLR）**：
    *   **核心思想**：利用模型在训练过程中逐渐涌现的正确答案来替换原始的噪声标签。
    *   **判定条件**：
        1.  **正向收敛斜率（Positive Convergence Slope）**：多数投票答案的采样通过率（pass rate）随训练步数呈上升趋势。
        2.  **历史一致性（Historical Consistency）**：当前多数投票答案与历史多次更新中的主导答案保持一致。
    *   **流程**：在经过一段“早期学习阶段”后，若样本满足上述两个条件，则将其原始标签替换为模型自生成的多数投票答案。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：从 DAPO-Math 中采样 800 条（基础）和 4000 条（扩展）数据，人工注入 0.1 到 0.9 不同比例的噪声。
    *   **分布内（ID）评测**：AIME24/25、AMC、MATH-500、Minerva、OlympiadBench。
    *   **分布外（OOD）评测**：ARC-c、GPQA-diamond、MMLU-pro。
*   **对比方法（Baselines）**：
    *   **无监督方法**：TTRL、Co-Reward、Self-Certainty、Token/Seq Entropy。
    *   **传统鲁棒学习方法**：置信度惩罚（Confidence Penalty）、标签平滑（Label Smoothing）、小损失样本选择（Small-loss Selection）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80G) GPU**。
*   **训练时长**：在 800 条样本上训练约耗时 **3.8 小时**。
*   **算力开销**：论文指出 OLR 引入的额外计算开销（多数投票统计和斜率计算）相对于 RLVR 的采样过程（Rollout）几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种噪声比例（0.1-0.9）、2 种噪声类型（活跃/非活跃）、3 种模型规模/类型（Qwen3-4B/8B, DeepSeek-R1-Distill-Llama-8B）。
*   **充分性评价**：实验设计非常全面。不仅验证了最终性能，还详细记录了训练动态过程（准确率曲线、标签替换比例等），并进行了详尽的消融实验和参数敏感性分析（如斜率阈值和早期学习时长）。实验对比了多种基线，证明了 OLR 在极端噪声（如 50% 甚至 90%）下的优越性，结果具有较强的说服力。

### 6. 主要结论与发现
*   **ECC 现象的普遍性**：模型在训练早期倾向于拟合正确的逻辑，即使监督信号是错误的，正确答案的概率也会先上升。
*   **OLR 的有效性**：OLR 在各种噪声比例下均能显著提升性能。在 50% 噪声下，OLR 能使模型表现接近 10% 噪声时的水平。
*   **传统方法失效**：在 RLVR 中，传统的“小损失选择”方法效果极差，因为 RLVR 中损失为 0 往往意味着采样无信息，而非标签质量高。
*   **活跃噪声更具破坏性**：活跃噪声会直接导致模型崩溃，而 OLR 对此类噪声的缓解效果尤为显著（OOD 提升达 4.6%）。

### 7. 优点
*   **理论与实践结合**：不仅提出了方法，还通过数学定理（Theorem 3.4/3.5）证明了 ECC 现象和 OLR 的噪声容忍度。
*   **高效且易于集成**：方法逻辑简单，不增加推理负担，且能显著降低对高质量标注数据的依赖。
*   **强大的泛化能力**：在分布外（OOD）任务上表现出极佳的鲁棒性，证明模型学到的是通用推理能力而非死记硬背。

### 8. 不足与局限
*   **超参数依赖**：OLR 依赖于早期学习阶段的时长 $T$ 和斜率阈值 $\delta_{slope}$，虽然有敏感性分析，但在不同任务或模型上可能需要微调。
*   **任务局限性**：主要集中在数学推理任务（结果易于验证），对于开放式生成或主观性强的任务，其“多数投票”和“可验证奖励”的前提可能不再适用。
*   **样本规模**：虽然验证了 4000 条数据的扩展性，但在超大规模数据集（如数十万量级）上的表现仍有待观察。

（完）
