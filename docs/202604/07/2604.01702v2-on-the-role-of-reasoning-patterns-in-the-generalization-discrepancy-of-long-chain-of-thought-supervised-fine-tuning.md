---
title: On the Role of Reasoning Patterns in the Generalization Discrepancy of Long Chain-of-Thought Supervised Fine-Tuning
title_zh: 论推理模式在长思维链有监督微调泛化差异中的作用
authors: "Zhaoyi Li, Xiangyu Xi, Zhengyu Chen, Wei Wang, Gangwei Jiang, Ranran Shen, Linqi Song, Ying Wei, Defu Lian"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01702v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 推理模型长思维链轨迹的研究
tldr: 本研究探讨了长思维链（CoT）有监督微调中数据来源对模型泛化性能的影响。通过对比DeepSeek-R1和GPT-OSS的数据，发现DeepSeek数据虽训练损失更低，但因其发散且多分支的推理模式导致泛化性较差。研究揭示了模型会继承低效的探索行为，并提出通过过滤高分支轨迹的策略，显著提升了模型在多个推理基准测试上的表现。
selection_source: fresh_fetch
motivation: 旨在探究不同来源的长CoT轨迹如何影响模型的泛化能力，并解释为何较低的训练损失并不总是对应更好的推理表现。
method: 通过对比分析收敛演绎型与发散探索型推理模式，提出了一种过滤掉频繁分支和冗余探索轨迹的数据优化方法。
result: "实验表明，使用筛选后的数据集使模型在AIME25和BeyondAIME等推理基准上的性能最高提升了5.1%和5.5%。"
conclusion: 推理轨迹的模式（如收敛性）对泛化至关重要，减少冗余的探索分支能显著提高长CoT微调的效果。
---

## 摘要
在长思维链（CoT）轨迹上进行有监督微调（SFT）已成为构建大型推理模型的关键阶段。然而，来自不同来源的 CoT 轨迹如何影响模型的泛化性能仍是一个悬而未决的问题。在本文中，我们使用由两个竞争模型 DeepSeek-R1-0528 和 gpt-oss-120b 生成的两类经过验证的 CoT 轨迹进行了对比研究，并控制其问题集完全相同。尽管它们的性能相当，我们发现了一个引人注目的悖论：较低的训练损失并不意味着更好的泛化能力。与在 gpt-oss-120b 数据上训练的模型相比，在 DeepSeek-R1-0528 数据上进行的 SFT 实现了显著更低的训练损失，但在推理基准测试中的泛化性能却明显更差。为了理解这一悖论，我们进行了多维度的分析，探讨了 token 级 SFT 损失和步骤级推理行为。我们的分析揭示了推理模式的差异：gpt-oss-120b 表现出高度收敛和演绎的轨迹，而 DeepSeek-R1-0528 则倾向于发散且多分支的探索模式。因此，使用 DeepSeek-R1 数据训练的模型继承了低效的探索行为，经常陷入冗余的探索分支中，从而阻碍其得出正确解。基于这一见解，我们提出了一种简单而有效的补救措施，即过滤掉频繁分支的轨迹，以提高 SFT 的泛化能力。实验表明，在选定的 DeepSeek-R1-0528 子集上进行训练，出人意料地将 AIME25 的推理性能提升了高达 5.1%，BeyondAIME 提升了 5.5%，在五个基准测试中平均提升了 3.6%。

## Abstract
Supervised Fine-Tuning (SFT) on long Chain-of-Thought (CoT) trajectories has become a pivotal phase in building large reasoning models. However, how CoT trajectories from different sources influence the generalization performance of models remains an open question. In this paper, we conduct a comparative study using two sources of verified CoT trajectories generated by two competing models, \texttt{DeepSeek-R1-0528} and \texttt{gpt-oss-120b}, with their problem sets controlled to be identical. Despite their comparable performance, we uncover a striking paradox: lower training loss does not translate to better generalization. SFT on \texttt{DeepSeek-R1-0528} data achieves remarkably lower training loss, yet exhibits significantly worse generalization performance on reasoning benchmarks compared to those trained on \texttt{gpt-oss-120b}. To understand this paradox, we perform a multi-faceted analysis probing token-level SFT loss and step-level reasoning behaviors. Our analysis reveals a difference in reasoning patterns. \texttt{gpt-oss-120b} exhibits highly convergent and deductive trajectories, whereas \texttt{DeepSeek-R1-0528} favors a divergent and branch-heavy exploration pattern. Consequently, models trained with \texttt{DeepSeek-R1} data inherit inefficient exploration behaviors, often getting trapped in redundant exploratory branches that hinder them from reaching correct solutions. Building upon this insight, we propose a simple yet effective remedy of filtering out frequently branching trajectories to improve the generalization of SFT. Experiments show that training on selected \texttt{DeepSeek-R1-0528} subsets surprisingly improves reasoning performance by up to 5.1% on AIME25, 5.5% on BeyondAIME, and on average 3.6% on five benchmarks.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：推理模式在长 CoT SFT 泛化差异中的作用

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了构建大型推理模型（LRM）的关键环节——**长思维链（Long CoT）的有监督微调（SFT）**。研究者发现了一个“泛化悖论”：使用 DeepSeek-R1 生成的推理轨迹进行 SFT 时，虽然训练损失（Training Loss）显著低于使用 gpt-oss-120b 数据的情况，但其在下游推理基准测试（如 AIME）中的实际泛化表现却远逊于后者。研究动机在于揭示这种“低损失、低性能”现象背后的深层原因，并探索如何优化 SFT 数据以提升模型的推理能力。

#### 2. 方法论：核心思想与关键技术
论文通过多维度分析揭示了不同教师模型在推理模式上的本质差异，并据此提出改进方案：
*   **Token 级损失解构**：分析发现 DeepSeek-R1 的低损失源于其包含大量近乎零损失的“常规 token”（如简单计算），稀释了关键推理信号；而 gpt-oss 的高损失 token 集中在逻辑转折处，信号密度更高。
*   **推理行为标注（Reasoning Behavior Analysis）**：利用 DeepSeek-V3.2 将推理步骤分类为四种行为：**Propose（提议/分支）**、**Deduce（演绎）**、**Verify（验证）**、**Backtrack（回溯）**。
*   **推理模式对比**：发现 DeepSeek-R1 倾向于“发散探索”（频繁产生新分支而不深入演绎），而 gpt-oss 倾向于“收敛演绎”（深层的线性逻辑链）。
*   **数据过滤策略（核心改进）**：提出两种代理指标来识别并剔除冗余的分支轨迹：
    *   **Proxy 1（步骤数差异率）**：对比同一问题下 R1 与 gpt-oss 的步骤数，剔除 R1 显著冗余的样本。
    *   **Proxy 2（分支关键词比例）**：统计轨迹中包含“Perhaps”、“Another”等分支信号词的频率，过滤掉高频分支的轨迹。

#### 3. 实验设计
*   **数据集**：约 500,000 个复杂的数学问题（来源包括 OpenR1-Math, Big-Math-RL, NuminaMath 等），确保 R1 和 gpt-oss 的问题集完全一致且答案均经过验证正确。
*   **Benchmark**：MATH500、AIME24、AIME25、BeyondAIME、HMMT25。
*   **对比模型**：
    *   **教师模型**：DeepSeek-R1-0528、gpt-oss-120b。
    *   **学生模型（基座）**：Qwen2.5-7B/32B、Llama3.1-8B、Qwen3-8B。
*   **对比方法**：全量数据 SFT vs. 随机删除步骤 SFT vs. 基于代理指标过滤后的 SFT。

#### 4. 资源与算力
论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）及总训练时长。但详细列出了超参数设置：使用 Adam 优化器，学习率在 $8 \times 10^{-5}$ 到 $1 \times 10^{-4}$ 之间，全局 Batch Size 为 64 或 128，训练序列长度为 32,768。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同架构和规模（7B 到 32B）的基座模型，并在 5 个主流数学推理榜单上进行了验证。
*   **充分性与公平性**：
    *   进行了**消融实验**（如延长训练步数、增加推理上下文长度），排除了“训练不足”或“生成截断”导致的性能差异。
    *   设计了**干预实验**（随机删除 10%-30% 的步骤），验证了 R1 轨迹的结构冗余性。
    *   实验控制变量严格（相同问题集、均验证正确），对比具有高度的客观性和公平性。

#### 6. 主要结论与发现
*   **泛化差异根源**：学生模型会继承教师模型的推理模式。DeepSeek-R1 的“多分支、浅演绎”模式导致学生模型在推理时容易陷入无效的探索循环。
*   **损失误导性**：SFT 的平均训练损失不能作为衡量推理数据质量的唯一指标，高密度的逻辑转折信号比易于拟合的常规步骤更重要。
*   **过滤增益**：通过过滤掉 DeepSeek-R1 中最频繁分支的轨迹（即使数据量减少），可以显著提升泛化性能。例如，在 AIME25 上提升了 5.1%，平均提升 3.6%。
*   **长度非关键**：单纯过滤长轨迹并不能带来性能提升，关键在于过滤掉具有“低效分支模式”的轨迹。

#### 7. 优点（亮点）
*   **视角独特**：首次深入探讨了长 CoT 轨迹的“结构模式”而非仅仅是“正确性”对 SFT 的影响。
*   **揭示悖论**：有力地解释了为什么 DeepSeek-R1 蒸馏出的模型有时在复杂任务上表现不稳定的底层逻辑。
*   **实用性强**：提出的基于关键词比例的过滤方法（Proxy 2）不需要配对数据，易于在大规模数据生产中实施。

#### 8. 不足与局限
*   **领域局限**：实验主要集中在数学推理领域，尚不清楚该结论在代码生成、常识推理或多步规划任务中是否具有普适性。
*   **教师模型覆盖**：仅对比了 DeepSeek-R1 和 gpt-oss 两个模型，未涵盖更多闭源模型（如 OpenAI o1/o3）或其他开源推理模型。
*   **算力细节缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
