---
title: "DeonticBench: A Benchmark for Reasoning over Rules"
title_zh: DeonticBench：一个针对规则推理的基准测试
authors: "Guangyao Dou, Luis Brena, Akhil Deo, William Jurayj, Jingyu Zhang, Nils Holzenberger, Benjamin Van Durme"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04443v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 复杂规则和义务推理的基准测试
tldr: "针对大语言模型在处理复杂法律和政策规则时推理能力不足的问题，本文提出了 DEONTICBENCH 基准。该基准包含 6,232 个涵盖税务、航空、移民和住房法律的任务，支持自然语言推理和 Prolog 符号计算两种模式。实验显示，即使是顶尖模型在复杂子集上的表现也仅在 45% 左右，表明当前模型在处理长上下文、高风险的规则推理方面仍面临巨大挑战。"
selection_source: fresh_fetch
motivation: 现有基准多关注短上下文数学推理，缺乏对法律和政策等高风险领域中复杂、长上下文规则推理能力的评估。
method: "构建了包含 6,232 个任务的基准测试集，并提供参考 Prolog 程序，支持模型通过直接推理或转化为可执行代码进行逻辑求解。"
result: "顶尖 LLM 在困难子集上的准确率和 F1 值均低于 50%，且通过监督微调和强化学习提升符号生成质量的效果依然有限。"
conclusion: DEONTICBENCH 为研究真实世界领域中基于上下文的规则推理提供了重要工具，揭示了当前模型在符号与非符号推理设置下的局限性。
---

## 摘要
处理复杂的、特定上下文的规则推理对于大语言模型（LLMs）而言仍然具有挑战性。在法律和政策场景中，这表现为道义推理（deontic reasoning）：即在明确规则下对义务、许可和禁止事项进行推理。虽然近期许多基准测试强调短上下文的数学推理，但很少有测试关注长上下文、高风险的道义推理。为了填补这一空白，我们推出了 DEONTICBENCH，这是一个包含 6,232 个任务的基准测试，涵盖了美国联邦税收、航空公司行李政策、美国移民管理以及美国州住房法。这些任务可以通过多种方式解决，包括直接使用语言进行推理或借助符号计算。除了自由形式的思维链（CoT）推理外，DEONTICBENCH 还支持一种可选的基于求解器的工作流，其中模型将法规和案例事实翻译成可执行的 Prolog 代码，从而实现形式化的题目解释和显式的程序追踪。我们发布了所有实例的参考 Prolog 程序。在前沿 LLM 和编程模型中，困难子集的最佳性能在 SARA Numeric 上仅为 44.4%，在 Housing 上仅为 46.6 macro-F1。我们进一步研究了通过监督微调和强化学习进行符号程序生成的训练。尽管训练提高了 Prolog 生成的质量，但目前的强化学习方法仍无法可靠地解决这些任务。总之，DEONTICBENCH 为研究符号和非符号设置下真实领域中基于上下文的规则推理提供了一个基准。

## Abstract
Reasoning with complex, context-specific rules remains challenging for large language models (LLMs). In legal and policy settings, this manifests as deontic reasoning: reasoning about obligations, permissions, and prohibitions under explicit rules. While many recent benchmarks emphasize short-context mathematical reasoning, fewer focus on long-context, high-stakes deontic reasoning. To address this gap, we introduce DEONTICBENCH, a benchmark of 6,232 tasks across U.S. federal taxes, airline baggage policies, U.S. immigration administration, and U.S. state housing law. These tasks can be approached in multiple ways, including direct reasoning in language or with the aid of symbolic computation. Besides free-form chain-of-thought reasoning, DEONTICBENCH enables an optional solver-based workflow in which models translate statutes and case facts into executable Prolog, leading to formal problem interpretations and an explicit program trace. We release reference Prolog programs for all instances. Across frontier LLMs and coding models, best hard-subset performance reaches only 44.4% on SARA Numeric and 46.6 macro-F1 on Housing. We further study training with supervised fine-tuning and reinforcement learning for symbolic program generation. Although training improves Prolog generation quality, current RL methods still fail to solve these tasks reliably. Overall, DEONTICBENCH provides a benchmark for studying context-grounded rule reasoning in real-world domains under both symbolic and non-symbolic settings.

---

## 论文详细总结（自动生成）

### DEONTICBENCH：规则推理基准测试论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是评估大语言模型（LLM）在处理复杂、特定上下文规则时的**道义推理（Deontic Reasoning）**能力，即关于义务、许可和禁止事项的推理。
*   **研究动机**：尽管 LLM 在数学和 STEM 基准上取得了进步，但在法律、政策等高风险领域，模型仍容易产生幻觉，且难以严格遵循形式化规则。
*   **背景**：现有的推理基准多侧重于短上下文或纯逻辑形式，缺乏对真实世界、长上下文且具有法律效力的规则推理的深度评估。

#### 2. 论文提出的方法论
论文引入了 **DEONTICBENCH**，这是一个包含 6,232 个任务的多领域基准。
*   **核心思想**：支持“直接自然语言推理”和“符号辅助工作流”两种模式。
*   **关键技术细节**：
    *   **符号化工作流**：模型将法律条文（Statutes）和案例事实（Case Facts）翻译成可执行的 **Prolog** 代码。
    *   **自动化执行**：生成的代码由 SWI-Prolog 求解器运行，产生可审计的推理轨迹和最终答案。
    *   **Prolog 生成管线**：采用即时生成（On-the-fly）策略，针对每个实例生成特定的法规模块和查询程序，并利用编译器反馈进行最多两次的尝试。
    *   **训练策略**：探索了监督微调（SFT）、直接偏好优化（DPO）以及一种名为 **Dr. GRPO** 的强化学习方法（带有谓词感知的奖励函数），旨在提升符号程序的生成质量。

#### 3. 实验设计
*   **数据集/场景**：涵盖四个真实领域：
    1.  **SARA (美国联邦税收)**：计算税收债务或验证法律主张。
    2.  **Airline (航空公司行李政策)**：根据复杂费率表计算行李费。
    3.  **Housing (美国州住房法)**：基于州法律的二元问答。
    4.  **USCIS-AAO (美国移民管理)**：**本文新构建**，基于移民上诉案例，判断申请应被接受还是驳回。
*   **Benchmark 设置**：构建了专门的“困难子集（Hard Subset）”，通过自动化过滤和人工校验筛选出模型普遍失败的案例。
*   **对比方法**：评估了 8 种前沿模型（如 GPT-4.1/5.1/5.2, O3, Claude 4.5 Sonnet, Gemini 2.5 Flash 等）和 3 种编程专用模型。

#### 4. 资源与算力
*   **算力使用**：
    *   **SFT 与 DPO**：使用了 **4 台 NVIDIA H100 GPU**。
    *   **强化学习 (Dr. GRPO)**：使用了 **4 台 NVIDIA H200 GPU**。
*   **训练细节**：SFT 训练 3 个 Epoch，DPO 训练 1 个 Epoch，RL 训练 5 个 Epoch。上下文长度设定为 8,000 token。

#### 5. 实验数量与充分性
*   **实验规模**：总计 6,232 个任务，规模远超同类基准（如 RuleArena 的 816 个任务）。
*   **实验维度**：涵盖了三种提示策略（Direct, Zero-shot, Few-shot）、不同推理强度（Reasoning Effort）的消融实验、以及针对编程模型的专项评估。
*   **客观性与公平性**：通过 95% 自举置信区间（Bootstrap Confidence Intervals）处理输出的随机性，并对错误进行了详细的分类学分析（如规则错误、事实错误、数值错误等），实验设计较为严谨客观。

#### 6. 论文的主要结论与发现
*   **性能瓶颈**：即使是最强的模型，在困难子集上的表现也极差（SARA Numeric 准确率仅 44.4%，Housing F1 仅 46.6）。
*   **推理努力的局限性**：增加推理步数（如 O3 的 Medium/High effort）并不总能提升性能，有时甚至会带来负面影响。
*   **训练效果有限**：虽然 SFT 和 RL 提升了 Prolog 代码的语法正确性，但在端到端任务准确率上的增益依然有限，尤其是在精确的数值计算上。
*   **失败模式分析**：法律领域的主要瓶颈在于**规则选择**（选错法条），而结构化推理任务的瓶颈在于**事实提取**。

#### 7. 优点
*   **规模与深度**：是目前最大的可执行规则推理基准，且涵盖了高风险的真实法律领域。
*   **可解释性**：通过提供参考 Prolog 程序，为模型推理提供了可验证的“黄金标准”轨迹。
*   **新数据集贡献**：新构建的 USCIS-AAO 数据集填补了移民法律推理的空白。
*   **实用性**：困难子集的设立为未来前沿模型的评估提供了极具挑战性的靶场。

#### 8. 不足与局限
*   **强化学习挑战**：目前的 RL 方法在处理极其稀疏且复杂的逻辑奖励时表现不佳，未能实现可靠的端到端突破。
*   **领域偏差**：虽然涵盖了四个领域，但均集中在美国法律和政策框架下，缺乏国际多样性。
*   **事实提取依赖**：在 USCIS-AAO 中，事实提取依赖于另一个模型（GPT-5-mini），可能引入潜在的预处理偏差。
*   **符号化门槛**：将法律自然语言转化为 Prolog 依然是一个极难的任务，模型在这一步的失败直接导致了后续求解的崩溃。

（完）
