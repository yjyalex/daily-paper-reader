---
title: Precise Robot Command Understanding Using Grammar-Constrained Large Language Models
title_zh: 基于语法约束大语言模型的精确机器人指令理解
authors: "Xinyun Huo, Raghav Gnanasambandam, Xinyao Zhang"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04233v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 用于结构化语言理解的语法约束大语言模型
tldr: 针对工业人机协作中大语言模型（LLM）缺乏指令精确性的问题，本文提出一种语法约束的LLM框架。该方法结合了微调LLM的上下文推理能力与语法驱动的自然语言理解系统，通过两阶段处理和自动纠错机制，将自然语言指令强制转换为标准化的机器人可读JSON格式。在HuRIC数据集上的实验表明，该方法显著提升了指令的有效性和系统鲁棒性。
selection_source: fresh_fetch
motivation: 工业机器人需要极高的指令精确性，而通用大语言模型生成的输出往往不够严谨且难以直接执行。
method: 采用微调LLM进行高层推理，并利用结构化语言模型和语法解析器强制输出符合预定义动作框架的JSON指令，辅以自动反馈纠错机制。
result: 在HuRIC数据集上的评估显示，该混合模型在指令有效性方面优于纯微调模型和传统语法驱动模型。
conclusion: 结合语法约束与大语言模型能有效提升机器人指令理解的确定性，为安全高效的工业人机协作提供了可靠方案。
---

## 摘要
工业环境中的人机协作需要精确且可靠的通信，以提高操作效率。虽然大语言模型（LLMs）能够理解通用语言，但它们往往缺乏安全且可执行的工业指令所需的领域特定严谨性。为填补这一空白，本文介绍了一种新型语法约束 LLM，它将语法驱动的自然语言理解（NLU）系统与微调后的 LLM 相结合，从而兼顾了对话的灵活性和机器人技术所需的确定性精度。我们的方法采用了一个两阶段过程。首先，微调后的 LLM 对自然语言输入进行高级上下文推理和参数推断。其次，结构化语言模型（SLM）和基于语法的规范化器对 LLM 的输出进行约束，强制其转换为由有效动作帧和指令元素组成的标准化符号格式。该过程保证了生成的指令是有效的，并以机器人可读的 JSON 格式进行结构化。该模型的一个关键特征是验证和反馈循环。语法解析器根据预定义的机器人可执行动作列表对输出进行验证。如果指令无效，系统会自动生成纠错提示并重新调用 LLM。这种迭代自纠错机制使模型能够从初始解释错误中恢复，从而提高系统的鲁棒性。我们将语法约束混合模型与两个基准模型（基于 API 的微调 LLM 和独立的语法驱动 NLU 模型）进行了对比评估。通过使用人机交互语料库（HuRIC）数据集，我们证明了该混合方法实现了卓越的指令有效性，从而促进了更安全、更高效的工业人机协作。

## Abstract
Human-robot collaboration in industrial settings requires precise and reliable communication to enhance operational efficiency. While Large Language Models (LLMs) understand general language, they often lack the domain-specific rigidity needed for safe and executable industrial commands. To address this gap, this paper introduces a novel grammar-constrained LLM that integrates a grammar-driven Natural Language Understanding (NLU) system with a fine-tuned LLM, which enables both conversational flexibility and the deterministic precision required in robotics. Our method employs a two-stage process. First, a fine-tuned LLM performs high-level contextual reasoning and parameter inference on natural language inputs. Second, a Structured Language Model (SLM) and a grammar-based canonicalizer constrain the LLM's output, forcing it into a standardized symbolic format composed of valid action frames and command elements. This process guarantees that generated commands are valid and structured in a robot-readable JSON format. A key feature of the proposed model is a validation and feedback loop. A grammar parser validates the output against a predefined list of executable robotic actions. If a command is invalid, the system automatically generates corrective prompts and re-engages the LLM. This iterative self-correction mechanism allows the model to recover from initial interpretation errors to improve system robustness. We evaluate our grammar-constrained hybrid model against two baselines: a fine-tuned API-based LLM and a standalone grammar-driven NLU model. Using the Human Robot Interaction Corpus (HuRIC) dataset, we demonstrate that the hybrid approach achieves superior command validity, which promotes safer and more effective industrial human-robot collaboration.

---

## 论文详细总结（自动生成）

### 论文总结：基于语法约束大语言模型的精确机器人指令理解

#### 1. 核心问题与整体含义（研究动机和背景）
在工业人机协作（HRC）场景中，机器人需要极高精度的指令执行能力以确保安全和效率。虽然大语言模型（LLM）具备强大的自然语言理解和推理能力，但在处理工业指令时存在两个主要缺陷：
*   **缺乏严谨性**：LLM 生成的输出具有随机性，难以保证始终符合机器人控制器所需的特定结构化格式（如 JSON）。
*   **领域适应性不足**：通用 LLM 往往无法理解特定工业环境下的动作约束和参数要求。
本文旨在结合 LLM 的语义灵活性与形式语法的确定性，开发一种能够将模糊的自然语言指令精准转化为可执行机器人代码的混合框架。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**两阶段处理架构**，并引入了**反馈纠错循环**：
*   **第一阶段：语义推理（LLM 层）**
    *   使用 **Meta-Llama-3-8B-Instruct** 作为基础模型。
    *   采用 **LoRA（低秩适配）** 技术进行参数高效微调，使模型学习工业指令的语义映射。
    *   通过 **提示工程（Prompt Engineering）** 定义系统角色（机器人助手）和用户输入，引导模型输出初步的结构化框架。
*   **第二阶段：语法约束与规范化（NLU 层）**
    *   使用 **Lark 解析器** 和 **EBNF（扩展巴科斯范式）** 定义合法的机器人动作语法。
    *   **规范化器（Canonicalizer）**：将 LLM 输出的原始文本强制映射到预定义的动作帧（Action Frames）和元素（Elements）中。
*   **核心机制：反馈纠错循环（Feedback Loop）**
    *   如果语法解析器发现 LLM 生成的指令不符合预定义规则（如缺少必要参数或动作名称错误），系统会自动生成包含错误信息的“反馈提示词”，要求 LLM 重新生成，直到通过验证或达到迭代上限。

#### 3. 实验设计
*   **数据集**：使用 **HuRIC（Human Robot Interaction Corpus）** 数据集，包含 656 条自然语言指令，涵盖 16 种不同的机器人动作帧（如 Taking, Bringing 等）。
*   **评估指标**：
    *   **JSON Exact Match (EM)**：预测的动作帧和元素是否与参考标注完全一致。
    *   **JSON Similarity**：基于 Jaccard 指数计算预测 JSON 与参考 JSON 的结构和内容重合度。
*   **对比方法（Baselines）**：
    1.  API-based LLM (无微调)：直接使用原始 Llama-3。
    2.  API-based LLM (有微调)：仅使用 LoRA 微调后的模型。
    3.  Grammar-based NLU：仅使用传统的基于规则的语法解析。
    4.  **Hybrid Grammar-based LLM（本文方法）**。

#### 4. 资源与算力
论文明确列出了实验环境：
*   **硬件**：AMD Ryzen 7 5800X CPU, 32 GB DDR4 RAM, **NVIDIA RTX 3080 GPU (10 GB VRAM)**。
*   **软件**：Ubuntu 24.04 (WSL 2), Python 3.11, PyTorch 2.3, Hugging Face Transformers 库。
*   **训练细节**：使用了 PEFT 库进行 LoRA 微调，Accelerate 库管理硬件分布。文中未明确说明具体的训练时长，但提到 8B 参数模型可在单 GPU 上高效运行。

#### 5. 实验数量与充分性
*   **实验规模**：总样本 656 条，按 90%/10% 比例划分，测试集包含 **66 条** 样本。
*   **充分性评价**：
    *   **优点**：对比了四种不同的技术路径，涵盖了从纯概率模型到纯规则模型的完整光谱，实验维度较为客观。
    *   **不足**：测试集样本量（66条）相对较小，可能无法完全覆盖工业场景中极其复杂的长尾指令。此外，实验仅基于单一的 Llama-3 模型，缺乏在其他基座模型（如 GPT-4 或 Qwen）上的泛化验证。

#### 6. 主要结论与发现
*   **混合模型表现最优**：Hybrid 方法达到了 **48.48% 的精确匹配率（EM）** 和 **88.07% 的 JSON 相似度**，显著高于纯微调模型（EM 36.3%）和纯语法模型（EM 21.21%）。
*   **语义与结构的平衡**：纯 LLM 擅长理解语义但格式易出错；纯语法模型格式严谨但无法处理非标准表述。混合模型通过 LLM 提取语义、语法层强制格式，实现了互补。
*   **纠错机制的有效性**：反馈循环显著降低了“失败输出”的比例，提高了系统的鲁棒性。

#### 7. 优点（亮点）
*   **解耦设计**：将语义推理与结构约束分离，使得开发者可以独立优化 LLM 的理解能力和语法的严谨性。
*   **自纠错闭环**：引入反馈提示机制，模拟了人类在指令不明确时进行澄清的过程，增强了系统处理异常的能力。
*   **工业实用性**：输出直接对接机器人可读的 JSON 格式，具有很强的工程落地价值。

#### 8. 不足与局限
*   **数据集局限性**：HuRIC 数据集包含较多礼貌用语（如“请问你能...”），这在实际工业指令中较少见，可能干扰模型对核心指令的提取。
*   **语法歧义**：当所有语法规则合并为一个统一解析器时，随着动作种类的增加，可能会出现结构歧义，导致精确匹配率下降。
*   **延迟问题**：反馈纠错循环意味着可能需要多次调用 LLM，这在对实时性要求极高的工业控制场景中可能带来延迟挑战。
*   **模型单一**：未探讨不同规模（如 70B）或不同架构模型对结果的影响。

（完）
