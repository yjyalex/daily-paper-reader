---
title: "Rethinking Exploration in RLVR: From Entropy Regularization to Refinement via Bidirectional Entropy Modulation"
title_zh: 重新思考 RLVR 中的探索：从熵正则化到通过双向熵调制进行细化
authors: "Hengrui Gu, Xiaotian Han, Yujing Bian, Kaixiong Zhou"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04894v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 具有可验证奖励的大模型推理强化学习
tldr: 本研究针对大语言模型在可验证奖励强化学习（RLVR）中探索受限的问题，提出将策略熵分解为有益的“信息熵”和有害的“伪熵”。通过新提出的AsymGRPO框架，研究者实现了对正负样本熵调制的显式解耦，在维持解法多样性的同时抑制推理噪声，显著提升了模型的推理性能和探索效率。
selection_source: fresh_fetch
motivation: 传统的熵正则化在RLVR中存在超参数敏感且增益有限的问题，导致模型过快收敛至狭窄的解空间。
method: 提出AsymGRPO框架，通过双向熵调制机制独立控制正负样本，实现对信息熵的保留和对伪熵的抑制。
result: 实验证明AsymGRPO在多个基准测试中优于强基线方法，并能与现有的熵正则化技术产生协同效应。
conclusion: 有效的探索依赖于“熵精炼”而非盲目最大化，通过精准调制不同性质的样本熵可显著增强LLM的推理能力。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 显著提升了大语言模型 (LLM) 的推理能力。然而，它面临一个被称为“受限探索” (restricted exploration) 的根本性限制，即策略会迅速收敛到一组狭窄的解集。虽然熵正则化是维持探索的常用方法，但它在 LLM 中往往表现得不可靠，存在超参数敏感性高且性能提升微乎其微的问题。针对这些低效问题，我们提出重新思考策略熵与探索之间的关系。通过推导组相对优势估计 (group-relative advantage estimation) 的参数化公式并分析熵动力学，我们在概念上将策略熵分解为保留多样化解路径的“信息熵” (informative entropy) 和侵蚀推理模式的“伪熵” (spurious entropy)。我们的分析表明，与盲目最大化不同，有效的探索需要“熵细化” (entropy refinement)——这是一种隐含在组相对优势估计中的机制，它在正向采样 (positive rollouts) 中维持信息熵，同时在负向采样 (negative rollouts) 中抑制伪熵。在此见解的指导下，我们提出了 AsymGRPO，这是一个显式解耦正负采样调制的探索性框架。这实现了对信息熵保留和伪噪声抑制的独立控制。大量实验表明，与强基准模型相比，AsymGRPO 取得了优异的性能，并展现出与现有熵正则化方法协同工作的潜力。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has significantly advanced the reasoning capabilities of large language models (LLMs). However, it faces a fundamental limitation termed \textit{restricted exploration}, where the policy rapidly converges to a narrow set of solutions. While entropy regularization is a popular approach used to sustain exploration, it often proves unreliable for LLMs, suffering from high hyperparameter sensitivity and yielding only marginal performance gains. Motivated by these inefficiencies, we propose to rethink the relationship between policy entropy and exploration. By deriving a parametric formulation of group-relative advantage estimation and analyzing entropy dynamics, we conceptually decompose policy entropy into \textit{informative entropy}, which preserves diverse solution paths, and \textit{spurious entropy}, which erodes reasoning patterns. Our analysis reveals that, in contrast to blind maximization, effective exploration requires \textit{entropy refinement}-a mechanism implicitly embedded in group-relative advantage estimation that sustains informative entropy on positive rollouts while suppressing spurious entropy on negative ones. Guided by this insight, we propose \textbf{AsymGRPO}, an exploratory framework that explicitly decouples the modulation of positive and negative rollouts. This allows for independent control over the preservation of informative entropy and the suppression of spurious noise. Extensive experiments demonstrate that AsymGRPO achieves superior performance compared to strong baselines and exhibits the potential to synergize with existing entropy regularization methods.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Exploration in RLVR: From Entropy Regularization to Refinement via Bidirectional Entropy Modulation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：具有可验证奖励的强化学习（RLVR，如 DeepSeek-R1 采用的范式）在提升大语言模型（LLM）推理能力方面表现出色。
*   **核心问题**：RLVR 面临“探索受限”的瓶颈，表现为**熵崩溃（Entropy Collapse）**——策略在训练早期迅速收敛到狭窄的解空间，导致性能过早饱和。
*   **研究动机**：传统的“熵正则化”方法在 LLM 强化学习中效果不佳，表现为对超参数极度敏感、易导致“熵爆炸”（产生无意义噪声）且性能提升有限。作者试图探究：单纯增加熵是否真的能改善探索？还是需要更精细的机制？

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出**熵精炼（Entropy Refinement）**概念。将策略熵分解为：
    *   **信息熵（Informative Entropy）**：保留多样化的正确解路径，有助于探索。
    *   **伪熵（Spurious Entropy）**：引入无意义的推理噪声，干扰学习。
*   **关键技术 - AsymGRPO（非对称组相对策略优化）**：
    *   **参数化优势函数**：通过引入两个独立超参数 $\beta_{pos}$ 和 $\beta_{neg}$，对标准 GRPO 的优势估计进行泛化。
    *   **双向调制机制**：
        *   对于**正向采样（正确解）**：使用较大的 $\beta_{pos}$ 降低优势权重的衰减速度，从而在模型变强时依然维持较高的梯度增益，起到“保熵”作用。
        *   对于**负向采样（错误解）**：使用特定的 $\beta_{neg}$ 强化惩罚力度，加速剔除错误的推理路径，起到“降熵”作用。
    *   **算法流程**：在每个 Prompt 下采样一组 Rollouts，根据组内准确率动态计算非对称的优势值，通过梯度重加权实现对正负样本探索强度的独立控制。

### 3. 实验设计
*   **基准模型**：Qwen2.5-Math-1.5B 和 Qwen3-4B（非思考模式）。
*   **数据集/场景**：专注于数学推理任务，包括 **MATH-500, AIME 2024, AIME 2025, AMC 2023, OlympiadBench**。
*   **对比方法（Baselines）**：
    *   基础方法：REINFORCE, 标准 GRPO。
    *   熵增强变体：带熵正则化的 GRPO、Clip-higher（高位截断）、Entro.Adv（熵优势）。
    *   前沿方法：Dr.GRPO、Pass@K Training。
*   **特殊实验**：设计了“对抗性熵翻转”实验（EntIncrease/EntDecrease），通过人为反转正负样本的熵调制方向来验证理论。

### 4. 资源与算力
*   **硬件环境**：单节点配置 **4 × NVIDIA H100 GPU**。
*   **软件框架**：使用开源的 **verl** 强化学习框架。
*   **训练细节**：Qwen2.5-Math-1.5B 全局 Batch Size 为 512；Qwen3-4B 为 128。每个 Prompt 采样 8 个 Rollouts（G=8）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数学推理榜单，进行了多轮消融实验。
*   **充分性评价**：
    *   **多维度验证**：不仅对比了最终准确率，还深入分析了训练过程中的熵演变、组内解分布（全对/全错比例）、对数概率增益等指标。
    *   **客观性**：通过对抗性实验证明了“熵精炼”方向的正确性，而非仅仅通过调参获得性能提升。
    *   **公平性**：所有对比方法均在相同的算力和基础模型下进行，且对基线方法的超参数进行了调优。

### 6. 主要结论与发现
*   **熵并非越多越好**：盲目通过正则化增加熵会引入伪熵，损害性能。
*   **GRPO 的本质**：揭示了 GRPO 实际上是一个隐式的熵精炼器，它天然地在正确路径上保熵，在错误路径上降熵。
*   **非对称性的必要性**：实验证明 $\beta_{pos}$ 和 $\beta_{neg}$ 的最优值并不相等，解耦两者能显著提升模型在困难任务（如 AIME）上的表现。
*   **协同效应**：AsymGRPO 可以与 Clip-higher 等现有方法结合，进一步提升推理准确率（在综合评估中达到 60.32% 的新高）。

### 7. 优点（亮点）
*   **理论深刻**：从梯度重加权和协方差动力学的角度，对 GRPO 的工作机制给出了严谨的数学解释。
*   **视角新颖**：区分“信息熵”与“伪熵”，为 LLM 强化学习中的探索问题提供了新的认知框架。
*   **简单高效**：无需增加额外的价值网络（Value Network）或复杂的计算开销，仅通过修改优势函数的参数化形式即可实现显著增益。

### 8. 不足与局限
*   **超参数依赖**：虽然解耦了正负调制，但 $\beta$ 参数仍需手动调节，且在训练过程中是静态的，缺乏自适应调度机制。
*   **任务覆盖面**：实验主要集中在数学推理领域，尚未在代码生成、创意写作或通用对话等非可验证奖励场景中验证其普适性。
*   **粒度限制**：目前的熵调制是 Rollout 级别的，未来可能需要更细粒度（如 Token 级别）的调制来处理长链推理中的关键决策点。

（完）
