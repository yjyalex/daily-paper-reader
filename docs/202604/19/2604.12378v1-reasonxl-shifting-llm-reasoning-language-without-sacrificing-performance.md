---
title: "ReasonXL: Shifting LLM Reasoning Language Without Sacrificing Performance"
title_zh: ReasonXL：在不牺牲性能的情况下转换大语言模型推理语言
authors: "Daniil Gurgurov, Tom Röhr, Sebastian von Rohrscheidt, Josef van Genabith, Alexander Löser, Simon Ostermann"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12378v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于增强多语言逻辑推理的大规模推理轨迹平行语料库
tldr: 针对大语言模型在非英语任务中仍倾向于使用英语进行推理的问题，本文提出了ReasonXL。研究者构建了包含五种欧洲语言、每种语言超200万条推理轨迹的大规模平行语料库。通过监督微调（SFT）和可验证奖励强化学习（RLVR）的两阶段流程，成功使模型在目标语言中进行完整推理，且性能不降反升。此外，研究揭示了模型深层负责推理适配，而浅层决定语言身份的表征机制。
selection_source: fresh_fetch
motivation: 现有大模型即便处理非英语问题也多用英语推理，这种语言不匹配限制了非英语场景下的应用效果。
method: 引入包含200万样本的多语种推理数据集ReasonXL，并结合SFT与RLVR两阶段流水线进行模型适配。
result: 适配后的模型在目标语言推理上达到或超过基准水平，且RLVR以更小的参数更新实现了更显著的行为改变。
conclusion: 本研究证明了在不牺牲性能的前提下迁移LLM推理语言的可行性，并揭示了模型在适配过程中的层级功能分化。
---

## 摘要
尽管多语言能力有所进步，大多数大语言模型（LLMs）在训练过程中，尤其是推理链（reasoning traces）的生成上，仍然以英语为中心。即使在处理非英语问题时，这些模型也主要使用英语进行推理，这在非英语使用场景中造成了根本性的不匹配。我们通过三项贡献直接解决了这一差距：(i) 我们推出了 ReasonXL，这是首个涵盖五种欧洲语言（英语、德语、法语、意大利语和西班牙语）的大规模跨领域推理链平行语料库，每种语言包含超过 200 万个对齐样本，每个样本由提示词、推理链和最终输出组成，从而实现了对特定语言推理的直接监督。(ii) 利用 ReasonXL，我们证明了通过一个简单的两阶段流水线——监督微调（SFT）后接可验证奖励强化学习（RLVR），可以使 LLMs 适配为完全使用目标语言进行推理。所得模型的性能达到或超过了基线水平，且通用知识损失极小，并广泛保留了跨语言迁移能力。(iii) 我们对这种适配过程进行了深入的表示分析，发现模型深度上存在明确的功能划分：浅层包含一个因果决定语言身份的激活瓶颈，而深层则集中了由适配驱动的权重和激活变化。我们进一步发现，与 SFT 相比，RLVR 在参数更新更小的情况下实现了与基础模型更大的行为差异，这表明尽管权重更新小得多，但其表示重路由（representational rerouting）效率更高。

## Abstract
Despite advances in multilingual capabilities, most large language models (LLMs) remain English-centric in their training and, crucially, in their production of reasoning traces. Even when tasked with non-English problems, these models predominantly reason in English, creating a fundamental mismatch for non-English usage scenarios.   We address this disparity directly with three contributions. (i) We introduce ReasonXL, the first large-scale parallel corpus of cross-domain reasoning traces spanning five European languages (English, German, French, Italian, and Spanish), with over two million aligned samples per language, each comprising prompts, reasoning traces, and final outputs, enabling direct supervision of language-specific reasoning. (ii) Using ReasonXL, we demonstrate that LLMs can be adapted to reason entirely in a desired target language, using a simple two-stage pipeline of supervised fine-tuning (SFT) followed by reinforcement learning with verifiable rewards (RLVR). The resulting models match or exceed baseline performance, with minimal loss in general knowledge and broadly preserved cross-lingual transfer. (iii) We conduct an extensive representational analysis of the adaptation and find a clear functional division across model depth: early layers contain an activation bottleneck that causally determines language identity, while upper layers concentrate the weight and activation changes driven by adaptation. We further find that RLVR achieves greater behavioral divergence from the base model with smaller parameter updates than SFT, suggesting a more efficient representational rerouting despite much smaller weight updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReasonXL: Shifting LLM Reasoning Language Without Sacrificing Performance》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
目前的大语言模型（LLM）表现出严重的“英语中心主义”：即便使用非英语提示词，模型在生成中间推理链（Chain-of-Thought）时仍倾向于使用英语。这种现象不仅降低了非英语用户的信任度和透明度，还可能因“翻译损失”导致非英语任务的准确率低于英语。
**核心问题**：能否在不牺牲模型推理性能的前提下，将 LLM 的推理语言完全切换到非英语的目标语言？

### 2. 论文提出的方法论
研究者提出了一个两阶段的后训练流水线，并构建了大规模平行语料库：
*   **核心资源 ReasonXL**：构建了首个大规模、跨领域的五语种（英、德、法、意、西）平行推理语料库。每个语言包含超过 200 万个样本，涵盖提示词、推理链和最终答案。
*   **第一阶段：监督微调（SFT）**：在目标语言的推理数据上进行微调，旨在初步建立模型使用目标语言进行推理的习惯。
*   **第二阶段：强化学习（RLVR）**：采用 **Dr. GRPO** 算法（一种无需评论家模型的强化学习变体）。
    *   **奖励函数设计**：包含准确率奖励（Verifiable Reward）、语言一致性奖励（FastText 识别）、格式奖励（`<think>` 标签使用）以及重复惩罚。
    *   **目标**：修复 SFT 阶段可能带来的性能退化，提升推理质量。

### 3. 实验设计
*   **基础模型**：SmolLM3-3B（开源、支持多语言、性能强劲的小模型）。
*   **评估基准（Benchmarks）**：
    *   **科学推理**：MGSM（数学）、MT-Math100（数学）、GPQA Diamond（研究生水平科学 QA）。
    *   **非科学/通用能力**：Global MMLU Lite、Belebele（阅读理解）、Include（区域知识）。
    *   **知识漂移评估**：在英语环境下测试 MMLU、ARC、BoolQ 等，确保通用知识未丢失。
*   **对比对象**：SmolLM3 原始基座模型、SFT 后的模型、SFT+RL 后的最终模型。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 张 GPU**（具体型号未明确说明，但提到使用了 FSDP 分布式策略）。
*   **训练细节**：
    *   SFT 阶段：训练 2 个 Epoch，最大序列长度 16,384。
    *   RL 阶段：训练 1 个 Epoch，使用 vLLM 进行推理加速（Colocate 模式），每条提示词生成 8 个候选答案（G=8）。
*   **算力说明**：文中未明确给出总训练时长（小时/天），但详细列出了学习率、Batch Size 等超参数。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种目标语言（德、意、西、法）分别进行了完整的两阶段训练和评估。
*   **充分性**：实验设计非常全面。除了性能测试，还包含：
    *   **跨语言迁移实验**：测试适配某一语言后对其他未适配语言的影响。
    *   **机制分析（Mechanistic Analysis）**：通过权重变化分析、激活漂移（Activation Drift）和激活修补（Activation Patching）深入探讨模型内部变化。
*   **客观性**：使用了多个随机种子取平均值，并对比了 SFT 带来的性能下降与 RL 带来的性能回升，结论客观。

### 6. 论文的主要结论与发现
*   **性能不减反增**：通过 SFT+RL，模型可以 100% 使用目标语言推理，且在数学和科学任务上的表现达到或超过了原始“用英语推理”的基座模型。
*   **知识保留**：适配后的模型在英语通用知识测试中仅有极微小的下降（0.6 - 3.2 分），证明了推理语言的切换不破坏底层知识。
*   **层级功能分化**：
    *   **语言瓶颈**：模型第 **6-8 层**是决定输出语言的“因果瓶颈”。通过激活修补（Patching）这几层，就能让基座模型切换语言。
    *   **推理适配**：权重的显著变化集中在模型的**深层（上层）**，这部分负责优化推理质量和输出内容。
*   **RL 的高效性**：RLVR 相比 SFT，能以更小的参数更新实现更显著的行为改变（表征重定向）。

### 7. 优点
*   **数据贡献**：发布了 ReasonXL 大规模平行语料库，填补了非英语推理数据的空白。
*   **方法简洁有效**：证明了简单的 SFT+RL 流程即可打破“英语推理”的垄断，无需复杂的翻译中转。
*   **深度洞察**：不仅给出了结果，还通过可解释性分析揭示了模型如何处理语言身份与推理逻辑的层级关系。

### 8. 不足与局限
*   **模型规模限制**：实验仅在 3B 参数模型上验证，尚未在 70B 或更大规模的模型上测试。
*   **语系覆盖**：目前仅覆盖了印欧语系的欧洲语言，对于中文、阿拉伯语、印地语等语系差异巨大的语言，其结论的普适性有待验证。
*   **翻译依赖**：ReasonXL 的构建依赖于 Qwen3-32B 的翻译，可能存在“翻译腔”或潜在的系统性偏差。
*   **奖励函数简单**：RL 阶段主要依赖可验证的准确率，对于无法自动验证的开放式推理任务，该方法的适用性受限。

（完）
