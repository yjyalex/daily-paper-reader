---
title: "PubSwap: Public-Data Off-Policy Coordination for Federated RLVR"
title_zh: PubSwap：面向联邦 RLVR 的公共数据离策协调
authors: "Anupam Nayak, Baris Askin, Muhammed Ustaomeroglu, Carlee Joe-Wong, Gauri Joshi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12160v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 去中心化环境下用于推理后训练的联邦可验证奖励强化学习
tldr: 本文针对联邦强化学习（RLVR）在推理任务中面临的通信开销大和异构数据导致的客户端漂移问题，提出了PubSwap框架。该方法结合了LoRA本地适配与基于公共数据的离策协调步骤，利用少量共享公共数据交换响应级信号，通过全局正确响应替换本地错误响应来对齐目标。实验证明，该方法在数学和医疗推理任务中显著优于基准，为联邦推理后训练提供了一种高效且稳健的方案。
selection_source: fresh_fetch
motivation: 联邦强化学习在处理分布式私有推理数据时，面临全模型同步成本高昂以及数据异构导致的客户端漂移挑战。
method: 提出PubSwap框架，结合LoRA通信与公共数据离策步骤，通过在公共数据集上交换并替换全局正确响应来增强客户端间的协调。
result: 在数学和医疗推理基准测试中，该方法在提升通信效率的同时，性能一致优于标准的联邦学习基准。
conclusion: 结合低秩通信与有限的公共数据协调，是实现高效且全球对齐的联邦推理模型后训练的有效途径。
---

## 摘要
基于可验证奖励的强化学习（RLVR）推理后训练通常在中心化设置下进行研究，但许多现实应用涉及分布在不同组织中的去中心化私有数据。联邦训练是一种自然的解决方案，但在这种模式下扩展 RLVR 具有挑战性：全模型同步成本高昂，且在异构数据下执行过多的本地步骤会导致严重的客户端漂移。我们提出了一个联邦 RLVR 框架，该框架结合了基于 LoRA 的本地适配与基于公共数据的离策步骤，以提高通信效率和跨客户端协调。具体而言，利用一个小型共享公共数据集定期在组织间交换和重用响应级训练信号，在不泄露私有数据的情况下，为实现更全局一致的目标提供轻量级锚点。我们的方法在公共数据步骤中选择性地用全局正确的响应替换本地错误的响应，从而使训练保持接近本地策略，同时仍能从跨客户端协调中获益。在数学和医学推理基准测试及模型上，我们的方法始终优于标准基准。我们的结果强调了联邦推理后训练的一种简单有效的方案：将低秩通信与有限的公共数据协调相结合。

## Abstract
Reasoning post-training with reinforcement learning from verifiable rewards (RLVR) is typically studied in centralized settings, yet many realistic applications involve decentralized private data distributed across organizations. Federated training is a natural solution, but scaling RLVR in this regime is challenging: full-model synchronization is expensive, and performing many local steps can cause severe client drift under heterogeneous data. We propose a federated RLVR framework that combines LoRA-based local adaptation with public-data-based off-policy steps to improve both communication efficiency and cross-client coordination. In particular, a small shared public dataset is used to periodically exchange and reuse response-level training signals across organizations, providing a lightweight anchor toward a more globally aligned objective without exposing private data. Our method selectively replaces locally incorrect responses with globally correct ones during public-data steps, thereby keeping training closer to the local policy while still benefiting from cross-client coordination. Across mathematical and medical reasoning benchmarks and models, our method consistently improves over standard baselines. Our results highlight a simple and effective recipe for federated reasoning post-training: combining low-rank communication with limited public-data coordination.

---

## 论文详细总结（自动生成）

这篇论文提出了 **PubSwap**，一个针对联邦环境下可验证奖励强化学习（RLVR）的后训练框架，旨在解决大语言模型（LLM）在分布式私有数据上进行推理训练时的通信开销与数据异构性挑战。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：利用可验证奖励进行强化学习（RLVR，如 GRPO 算法）已成为提升 LLM 推理能力的关键。然而，医疗、金融等领域的推理数据通常分布在不同组织中，由于隐私原因无法汇聚。
*   **核心挑战**：
    1.  **通信瓶颈**：全参数同步大型模型在联邦学习（FL）中成本极高。
    2.  **客户端漂移（Client Drift）**：在异构数据下，增加本地训练步数会导致各客户端模型偏离全局目标，降低收敛质量。
*   **核心目标**：在保护隐私的前提下，实现高效、低通信成本且能有效协调异构客户端的联邦推理模型训练。

### 2. 论文提出的方法论：PubSwap
PubSwap 结合了低秩适配（LoRA）与基于公共数据的离策（Off-policy）协调机制：
*   **LoRA 本地适配**：客户端仅训练并交换 LoRA 参数（$A$ 和 $B$ 矩阵），显著降低通信量和显存占用。
*   **公共数据锚点**：引入一个小型共享公共数据集 $D_{pub}$。在本地训练期间，交替进行私有数据步骤和公共数据步骤。
*   **响应交换机制（Balanced Strategy）**：
    *   在公共数据步骤中，所有客户端针对同一批公共提示词（Prompts）生成响应。
    *   **平衡聚合**：如果某个客户端生成的正确响应不足（少于组大小 $K$ 的一半），服务器会从其他客户端生成的正确响应池中抽取样本进行替换。
    *   **离策更新**：客户端利用这些包含“外部正确答案”的响应组进行 GRPO 更新。这种方式在不暴露私有数据的情况下，利用公共数据作为“锚点”来对齐各客户端的策略。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：MATH（高中竞赛难度）和 DeepMath（103K 规模的高难度题目）。
    *   **医疗推理**：MedQA 和 MedMCQA 的混合数据集。
*   **模型**：Qwen2.5-Math-1.5B、Qwen3-1.7B、Qwen3-4B-Instruct 以及 Llama3.2-3B-Instruct。
*   **基准方法（Benchmarks）**：
    *   **FedAvg-GRPO**：标准的联邦平均算法结合 GRPO。
    *   **FedProx-GRPO**：在本地目标中加入近端项以抑制漂移的经典联邦学习改进算法。
*   **评估指标**：Pass@1 准确率。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H100 GPU**。
*   **配置**：根据模型大小，每个实验使用 2 到 4 张 H100。
*   **训练细节**：总步数通常为 360 到 600 步 GRPO 更新，本地步数（$\tau$）在 10 到 120 之间变动。使用了 vLLM 作为推理引擎以加速响应生成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模的模型家族（1.5B 到 4B 参数）和 2 个完全不同的专业领域（数学、医疗）。
*   **消融实验**：
    *   测试了不同的本地步数 $\tau$（验证通信效率与性能的权衡）。
    *   测试了不同的交换周期 $\tau_{swap}$（验证公共数据介入频率的影响）。
    *   测试了不同程度的数据异构性（Dirichlet 分布参数 $\alpha=0.3$ 和 $0.1$）。
*   **充分性评价**：实验设计较为全面，对比了强基准（如 FedProx），并展示了在极端异构和高通信延迟（大 $\tau$）场景下的稳健性，结果具有较高的说服力。

### 6. 论文的主要结论与发现
*   **PubSwap 优于基准**：在所有测试场景中，PubSwap 始终优于 FedAvg 和 FedProx，尤其是在本地步数较大（通信更稀疏）的情况下优势更明显。
*   **缓解漂移**：通过在公共数据上交换正确响应，PubSwap 有效抑制了异构数据导致的客户端漂移。
*   **FedProx 的局限性**：在 RLVR 场景下，传统的参数空间约束（FedProx）效果有限，因为参数的微小差异在自回归生成中会被放大，而 PubSwap 在响应/策略空间的协调更直接有效。
*   **通信效率**：结合 LoRA 和大本地步数，PubSwap 证明了在极低通信频率下仍能保持高性能。

### 7. 优点
*   **设计简洁且实用**：不需要复杂的加密计算或大规模数据共享，仅通过交换公共数据的响应即可实现协调。
*   **通信友好**：LoRA 极大降低了带宽需求，而 PubSwap 允许更长的本地训练周期。
*   **即插即用**：该方法可以轻松集成到现有的分布式强化学习框架（如 VERL）中。

### 8. 不足与局限
*   **依赖可验证奖励**：该方法仅适用于结果可自动验证（如数学、代码、多选）的任务，对于主观性强的开放式推理任务（无明确 Reward Function）适用性受限。
*   **离策偏差风险**：虽然“平衡策略”缓解了偏差，但引入其他客户端生成的响应本质上是离策的，如果客户端之间策略差异过大，可能会引入训练不稳定性。
*   **公共数据需求**：虽然需求量小，但仍需要一个与私有数据分布相关的公共数据集作为锚点。

（完）
