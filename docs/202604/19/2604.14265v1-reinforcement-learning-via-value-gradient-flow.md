---
title: Reinforcement Learning via Value Gradient Flow
title_zh: 基于价值梯度流的强化学习
authors: "Haoran Xu, Kaiwen Hu, Somayeh Sojoudi, Amy Zhang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14265v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型强化学习微调的可扩展行为正则化范式
tldr: 本研究针对离线强化学习和LLM微调中的行为正则化问题，提出了价值梯度流（VGF）框架。VGF将强化学习建模为从参考分布到价值诱导最优分布的最优传输问题，利用价值梯度引导粒子演化，从而避免了显式策略参数化。该方法通过控制传输预算实现隐式正则化，支持测试时自适应缩放，在D4RL、OGBench及LLM任务中均达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的行为正则化方法在扩展到大规模生成模型时面临参数化困难或采样过于保守的挑战。
method: 提出VGF框架，通过离散梯度流将参考分布粒子沿价值梯度方向传输至最优策略分布。
result: 实验证明VGF在离线强化学习基准测试和LLM强化学习任务中显著优于现有方法。
conclusion: VGF通过最优传输视角简化了正则化RL，实现了表达能力强、灵活性高且可扩展的策略优化。
---

## 摘要
我们研究了行为正则化强化学习（RL），其中针对参考分布（离线 RL 中的数据集或大语言模型 RL 微调中的基础模型）的正则化对于防止由错误的分布外外推引起的价值过度优化至关重要。现有方法要么依赖于重参数化策略梯度，这难以扩展到大型生成模型；要么依赖于拒绝采样，在试图超越行为支撑集时可能过于保守。在本文中，我们提出了价值梯度流（Value Gradient Flow, VGF），这是一种用于行为正则化强化学习的可扩展新范式。VGF 将行为正则化强化学习建模为一个最优传输问题，将参考分布映射到价值诱导的最优策略分布。我们通过离散梯度流解决这一传输问题，其中价值梯度引导从参考分布初始化的粒子。我们的分析表明，VGF 通过控制传输预算来隐式地施加正则化。VGF 消除了显式的策略参数化，同时保持了表现力和灵活性，这使得通过调整传输预算来实现自适应的测试时扩展成为可能。大量实验表明，VGF 显著优于现有方法，在离线强化学习基准测试（D4RL、OGBench）和大语言模型强化学习任务上均达到了领先水平。

## Abstract
We study behavior-regularized reinforcement learning (RL), where regularization toward a reference distribution (the dataset in offline RL or the base model in LLM RL finetuning) is essential to prevent value over-optimization caused by erroneous out-of-distribution extrapolation. Existing methods either rely on reparameterized policy gradient, which are difficult to scale to large generative models, or on reject sampling, which can be overly conservative when attempting to move beyond the behavior support. In this paper, we propose Value Gradient Flow (VGF), a scalable new paradigm for behavior-regularized RL. VGF casts behavior-regularized RL as an optimal transport problem that maps the reference distribution to the value-induced optimal policy distribution. We solve this transport problem via discrete gradient flow, where value gradients guide particles initialized from the reference distribution. Our analysis shows that VGF imposes regularization implicitly by controlling the transport budget. VGF eliminates explicit policy parameterization while remaining expressive and flexible, this enables adaptive test-time scaling by adjusting the transport budget. Extensive experiments demonstrate that VGF significantly outperforms prior methods, achieving state-of-the-art results on offline RL benchmarks (D4RL, OGBench) and LLM RL tasks. Code and runs can be found at https://ryanxhr.github.io/vgf.

---

## 论文详细总结（自动生成）

以下是对论文《Reinforcement Learning via Value Gradient Flow》（基于价值梯度流的强化学习）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在离线强化学习（Offline RL）和大语言模型强化学习微调（RLHF）中，**行为正则化**是核心挑战。为了防止模型在分布外（OOD）区域产生错误的价值高估（价值过度优化），必须约束策略靠近参考分布（如离线数据集或 SFT 基础模型）。
*   **现有局限：** 
    *   **重参数化策略梯度：** 难以扩展到扩散模型或流匹配模型，因为通过多步采样过程进行反向传播既不稳定又耗费计算资源。
    *   **拒绝采样/加权 BC：** 过于保守，通常只能在参考分布的支撑集内“炒冷饭”，无法探索出更优的行为。
*   **研究目标：** 提出一种既能保持生成模型表达能力，又能稳定优化，且能灵活调节正则化强度的可扩展 RL 范式。

### 2. 方法论：价值梯度流（VGF）
VGF 将行为正则化 RL 重新表述为一个**最优传输（Optimal Transport）问题**。
*   **核心思想：** 将概率质量从“参考分布”传输到由价值函数诱导的“玻尔兹曼最优分布”（$\pi^* \propto \exp(Q/\alpha)$）。
*   **关键技术细节：**
    *   **粒子梯度流：** VGF 不使用显式的参数化策略网络，而是维护一组从参考分布中采样的“粒子”。
    *   **更新规则：** 利用核化函数梯度（类似于 Stein 变分梯度下降 SVGD）来移动粒子。更新力由两部分组成：一是将粒子推向高价值区域的驱动力，二是防止粒子坍缩、保持多模态分布的排斥力。
    *   **隐式正则化：** 通过控制“传输预算”（即梯度流的步数 $L$ 和步长 $\epsilon$）来实现正则化，而非添加显式的 KL 散度惩罚项。
    *   **测试时缩放（Test-time Scaling）：** 在推理阶段，可以通过增加梯度流步数来进一步优化性能，实现类似于“思维链”或“搜索”的计算换性能效果。

### 3. 实验设计
*   **数据集与场景：**
    *   **离线 RL 基准：** D4RL（包含 MuJoCo 运动任务和 AntMaze 导航任务）、OGBench（更具挑战性的机器人操纵和长程导航任务，共 45 个子任务）。
    *   **RLHF 任务：** TL;DR 摘要生成、Anthropic-HH（有用性与无害性）对话任务。
*   **对比方法（Baselines）：**
    *   **离线 RL：** TD3+BC, IQL, IVR（高斯策略）；Diffusion-QL, SfBC（扩散策略）；FQL（流匹配策略）。
    *   **RLHF：** PPO, DPO, Best-of-N 采样。

### 4. 资源与算力
*   **算力说明：** 论文中未详细列出具体的 GPU 型号数量和总训练时长。
*   **提及资源：** 提到了使用了德克萨斯高级计算中心（TACC）的集群资源和印第安纳大学的 Jetstream2 云端资源。实验涉及 Pythia-2.8B 模型，暗示了需要中大规模的 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模：** 实验非常充分。在 D4RL 上测试了 15 个任务，在 OGBench 上测试了 45 个任务，并涵盖了 2 个主流 RLHF 数据集。
*   **消融实验：** 针对训练步数 $L_{train}$、测试步数 $L_{test}$、粒子数量 $N$ 等关键超参数进行了详细消融。
*   **客观性：** 采用了 5 个随机种子取平均值，并提供了标准差，对比了从简单高斯策略到最新流匹配策略的各类 SOTA 方法，实验设计公平且具有说服力。

### 6. 主要结论与发现
*   **性能领先：** VGF 在 D4RL 和 OGBench 的大多数任务中达到了 SOTA，尤其在复杂的 AntMaze 和机器人操纵任务中优势明显。
*   **超越支撑集：** 理论和实验均证明，VGF 能够移动到参考分布支撑集之外的高价值区域，克服了传统方法的保守性。
*   **推理增强：** 增加推理时的梯度流步数（$L_{test}$）能显著提升性能，证明了其具备自适应测试时扩展的能力。
*   **RLHF 优势：** 在 LLM 对齐任务中，VGF 的胜率显著高于 PPO 和 DPO。

### 7. 优点
*   **高扩展性：** 避开了对复杂生成模型采样过程求导的难题，适用于扩散和流模型。
*   **表达能力强：** 隐式策略天然支持多模态分布，不会像高斯策略那样退化。
*   **灵活性：** 训练和推理时的正则化强度可以解耦，支持推理侧的性能微调。
*   **数学优雅：** 将 RL 问题转化为最优传输和梯度流，理论框架完备。

### 8. 不足与局限
*   **计算开销：** 虽然粒子数较少（$N=5$），但每一步梯度流都需要计算价值函数的梯度，在超大规模模型上推理延迟会增加。
*   **参考分布依赖：** 如果参考分布极其糟糕（严重偏离目标），VGF 的提升空间可能受限。
*   **长程任务挑战：** 尽管在 OGBench 表现良好，但在极长时程的任务中，仍依赖于价值函数的准确性，而价值函数本身在长程任务中容易失效。

（完）
