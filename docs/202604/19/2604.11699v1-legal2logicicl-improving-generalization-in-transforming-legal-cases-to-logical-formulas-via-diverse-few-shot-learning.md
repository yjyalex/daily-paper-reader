---
title: "Legal2LogicICL: Improving Generalization in Transforming Legal Cases to Logical Formulas via Diverse Few-Shot Learning"
title_zh: Legal2LogicICL：通过多样化少样本学习提升法律案例到逻辑公式转换的泛化能力
authors: "Jieying Xue, Phuong Minh Nguyen, Ha Thanh Nguyen, May Myo Zin, Ken Satoh"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11699v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 将法律案例转化为逻辑公式，用于基于逻辑的法律推理
tldr: 该研究针对法律逻辑推理中高质量标注数据稀缺的问题，提出了Legal2LogicICL框架。该框架利用大语言模型的上下文学习能力，通过检索增强生成技术，在语义和法律结构层面平衡示例的多样性与相似性，并有效减少了实体引起的检索偏差。此外，研究者还构建了Legal2Proleg数据集。实验证明，该方法在将法律案例转化为逻辑公式方面具有显著的准确性、稳定性和泛化能力，为实现可解释且可靠的法律推理提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的法律逻辑推理系统依赖微调模型进行语义解析，严重受限于高质量标注数据的匮乏，导致泛化能力不足。
method: 提出Legal2LogicICL框架，通过在语义和结构层面平衡示例多样性与相似性，并消除实体干扰，实现高效的少样本检索增强生成。
result: 在开源和闭源大模型上的实验表明，该方法显著提升了法律案例转化为逻辑公式的准确性、稳定性和泛化性。
conclusion: 该研究通过创新的少样本学习策略和新数据集，有效解决了法律语义解析中的数据瓶颈，增强了法律推理系统的可靠性与可解释性。
---

## 摘要
本研究旨在通过将自然语言处理（NLP）的最新进展与利用大语言模型（LLM）的法律领域自适应少样本学习技术相结合，提升基于逻辑的法律推理系统的泛化能力。现有的基于逻辑的法律推理流水线通常依赖微调模型将自然语言法律案例映射为逻辑公式，然后再将其转发给符号推理器。然而，此类方法受到高质量标注训练数据稀缺的严重制约。为解决这一局限性，我们提出了一种新型的基于 LLM 的法律推理框架，通过检索增强生成实现有效的上下文学习。具体而言，我们引入了 Legal2LogicICL，这是一个少样本检索框架，在潜在语义表示层面和法律文本结构层面平衡了示例的多样性与相似性。此外，我们的方法通过减轻法律文本中由实体引起的检索偏差，明确地考虑了法律结构；在法律文本中，冗长且高度具体的实体提及往往主导了语义表示，并掩盖了具有法律意义的推理模式。我们的 Legal2LogicICL 构建了信息丰富且稳健的少样本演示，在无需额外训练的情况下实现了准确且稳定的逻辑规则生成。此外，我们构建了一个名为 Legal2Proleg 的新数据集，该数据集标注了法律案例与 PROLEG 逻辑公式之间的对齐关系，以支持法律语义解析的评估。在开源和闭源 LLM 上的实验结果表明，我们的方法在将自然语言法律案例描述转换为逻辑表示方面显著提高了准确性、稳定性和泛化能力，突显了其在可解释且可靠的法律推理方面的有效性。我们的代码可在 https://github.com/yingjie7/Legal2LogicICL 获取。

## Abstract
This work aims to improve the generalization of logic-based legal reasoning systems by integrating recent advances in NLP with legal-domain adaptive few-shot learning techniques using LLMs. Existing logic-based legal reasoning pipelines typically rely on fine-tuned models to map natural-language legal cases into logical formulas before forwarding them to a symbolic reasoner. However, such approaches are heavily constrained by the scarcity of high-quality annotated training data. To address this limitation, we propose a novel LLM-based legal reasoning framework that enables effective in-context learning through retrieval-augmented generation. Specifically, we introduce Legal2LogicICL, a few-shot retrieval framework that balances diversity and similarity of exemplars at both the latent semantic representation level and the legal text structure level. In addition, our method explicitly accounts for legal structure by mitigating entity-induced retrieval bias in legal texts, where lengthy and highly specific entity mentions often dominate semantic representations and obscure legally meaningful reasoning patterns. Our Legal2LogicICL constructs informative and robust few-shot demonstrations, leading to accurate and stable logical rule generation without requiring additional training. In addition, we construct a new dataset, named Legal2Proleg, which is annotated with alignments between legal cases and PROLEG logical formulas to support the evaluation of legal semantic parsing. Experimental results on both open-source and proprietary LLMs demonstrate that our approach significantly improves accuracy, stability, and generalization in transforming natural-language legal case descriptions into logical representations, highlighting its effectiveness for interpretable and reliable legal reasoning. Our code is available at https://github.com/yingjie7/Legal2LogicICL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Legal2LogicICL** 的框架，旨在通过多样化的少样本学习（Few-Shot Learning）提升将法律案例描述转化为逻辑公式（PROLEG 格式）的泛化能力。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何准确、稳健地将非结构化的自然语言法律案例转化为机器可执行的结构化逻辑公式（PROLEG）。
*   **研究背景**：
    *   法律推理系统（如 PROLEG）需要逻辑公式作为输入，但法律从业者难以编写这些公式。
    *   现有的语义解析方法（如基于规则、NER 或微调模型）高度依赖高质量标注数据，且在面对未见过的法律结构或复杂实体关系时泛化能力极差。
    *   法律文本中冗长且具体的实体（如当事人姓名、合同编号）会干扰语义检索，导致模型关注表面相似性而非深层法律逻辑。

### 2. 方法论
核心思想是利用大语言模型（LLM）的**上下文学习（ICL）**能力，结合**检索增强生成（RAG）**，通过精心设计的示例选择策略来引导模型生成。
*   **DiverseSim 算法**：引入多样性控制参数 $\lambda$，在检索示例时平衡“与查询案例的相似度”和“示例之间的多样性”。公式逻辑为：排名分数 = $\lambda \times$ 相似度 - $(1-\lambda) \times$ 最大冗余度。
*   **混合检索策略**：
    *   **案例级检索（Case-based）**：基于原始文本的语义相似度检索。
    *   **实体无关模板检索（Template-based）**：将法律案例中的具体实体替换为类型占位符（如 `{Borrower}`, `{Lender}`），基于抽象后的法律结构进行检索，以消除实体引起的检索偏差。
*   **提示词构建**：将检索到的案例级示例和模板级示例组合，构建包含结构约束和上下文信息的 Prompt。

### 3. 实验设计
*   **数据集**：
    *   **Legal2Proleg（新构建）**：包含贷款、租赁、购买和版权四类合同，共 1057 个模板，标注了法律规则和对应的 PROLEG 事实公式。
    *   **LegalCaseNER**：作为对比基准，主要用于评估实体识别和简单模式下的表现。
*   **对比方法（Baselines）**：
    *   **SFT 方法**：基于 RoBERTa 的 LegalCaseNER 微调模型。
    *   **LLM 零样本/少样本**：Qwen3 (8B/14B)、Llama-3.1-8B、Phi-4、GPT-5.2。
*   **评估指标**：
    *   **Exact-Match Accuracy**：全匹配准确率（极其严格）。
    *   **Soft-Match Accuracy**：结构匹配前提下，计算实体嵌入的余弦相似度，以容忍微小的表述差异。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **模型细节**：使用了 Qwen3-Embedding-8B 作为向量编码模型。由于主要采用 ICL（上下文学习）而非全量微调，其算力消耗主要集中在 LLM 推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同数据比例（20% 到 80% 的可见数据）进行了对比实验。
    *   对多样性参数 $\lambda$ 进行了消融实验（0.2 到 0.8）。
    *   对比了不同的 $k$-shot 组合策略（如 3c, 5c, 3c+3t）。
    *   所有实验均运行了 **5 个随机种子**并取平均值。
*   **充分性评价**：实验设计较为充分，涵盖了开源与闭源模型、不同规模的数据集以及关键超参数的讨论，客观地展示了 ICL 在低资源场景下优于微调模型的特性。

### 6. 主要结论与发现
*   **泛化性显著增强**：在仅有 20% 标注数据的情况下，Legal2LogicICL 的准确率（83%）远超基于微调的 NER 方法（16.61%）。
*   **多样性的价值**：适中的多样性（$\lambda=0.6$）能显著提升模型表现，证明了覆盖更多法律结构比单纯追求语义相似更重要。
*   **混合策略的有效性**：结合“案例级”和“模板级”示例的策略（3c+3t）在所有模型上均取得了最佳效果，有效缓解了实体偏差。
*   **实体偏差风险**：法律文本中的特定实体名会严重干扰检索质量，通过模板化处理可以显著提升结构解析的稳定性。

### 7. 优点
*   **创新性检索**：提出的“实体无关模板检索”非常符合法律领域的特性，解决了法律文本特有的噪声问题。
*   **无需微调**：基于 ICL 的方案降低了法律 AI 应用的门槛，尤其是在标注数据极度匮乏的垂直领域。
*   **新基准数据集**：贡献了 Legal2Proleg 数据集，填补了法律语义解析领域高质量对齐数据的空白。

### 8. 不足与局限
*   **评估指标的局限**：Exact-Match 过于严格，虽然提出了 Soft-Match，但对于法律逻辑中极其关键的微小差异（如“a”与“the”在某些特定法律语境下的区别）仍缺乏更深层的语义判定。
*   **长文本挑战**：论文未深入讨论当法律案例极其冗长（超过 LLM 上下文窗口）时的处理方案。
*   **依赖嵌入模型**：DiverseSim 的效果高度依赖于底层 Embedding 模型的质量，若 Embedding 模型无法捕捉法律细微差别，检索质量将受限。

（完）
