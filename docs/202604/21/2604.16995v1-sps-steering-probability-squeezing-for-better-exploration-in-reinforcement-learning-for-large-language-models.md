---
title: "SPS: Steering Probability Squeezing for Better Exploration in Reinforcement Learning for Large Language Models"
title_zh: SPS：通过引导概率挤压实现大语言模型强化学习中更好的探索
authors: "Yifu Huo, Chenglong Wang, Ziming Zhu, Shunjie Xing, Peinan Feng, Tongran Liu, Qiaozhi He, Tianhua Zhou, Xiaojia Chang, Jingbo Zhu, Zhengtao Yu, Tong Xiao"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16995v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 面向推理模型的强化学习及推理轨迹探索
tldr: "针对大语言模型在强化学习训练中因“概率挤压”效应导致探索不足、Pass@k性能受限的问题，本文提出了SPS（转向概率挤压）框架。该方法通过交替进行常规强化学习与逆强化学习（IRL），将策略生成的轨迹视为演示并利用IRL重塑轨迹分布，从而在不引入外部监督的情况下增强探索能力。在五个推理基准上的实验证明，SPS显著提升了模型的多样化探索效率和多样本通过率。"
selection_source: fresh_fetch
motivation: "传统强化学习训练会导致概率分布过度集中在少数高奖励轨迹上，从而限制了模型对多样化推理路径的探索和Pass@k性能。"
method: 提出SPS框架，通过交替执行常规强化学习与逆强化学习，利用IRL显式重塑轨迹分布以缓解概率挤压效应。
result: "在五个常用推理基准上的实验表明，SPS能够有效增强模型的探索能力并显著提升Pass@k性能。"
conclusion: 将强化学习与逆强化学习交替结合是扩展推理型大语言模型探索能力、突破现有训练瓶颈的有效途径。
---

## 摘要
强化学习（RL）已成为利用基于规则的奖励信号训练面向推理模型的极具前景的范式。然而，RL 训练通常倾向于提高单样本成功率（即 Pass@1），而对多样化推理轨迹的探索有限，而这对于多样本性能（即 Pass@k）至关重要。我们的初步分析表明，这一局限性源于一种根本性的挤压效应，即概率质量过度集中在少数高奖励轨迹子集上，从而限制了真正的探索，并制约了 RL 训练下可达到的性能。为解决这一问题，本研究提出了引导概率挤压（SPS），这是一种将传统 RL 与逆强化学习（IRL）交替进行的训练范式。SPS 将策略内采样（on-policy rollouts）视为演示，并利用 IRL 显式地重塑诱导的轨迹分布，从而在不引入外部监督的情况下增强探索。在五个常用推理基准测试上的实验表明，SPS 能够实现更好的探索并提高 Pass@k。除了算法贡献外，我们还分析了 RL 的学习动态，并确定了 Pass@k 的经验上限，揭示了基于 RL 的推理模型中固有的探索限制。我们的研究结果表明，在 RL 和 IRL 之间交替进行，为扩展面向推理的大语言模型的探索能力提供了一条有效途径。

## Abstract
Reinforcement learning (RL) has emerged as a promising paradigm for training reasoning-oriented models by leveraging rule-based reward signals. However, RL training typically tends to improve single-sample success rates (i.e., Pass@1) while offering limited exploration of diverse reasoning trajectories, which is crucial for multi-sample performance (i.e., Pass@k). Our preliminary analysis reveals that this limitation stems from a fundamental squeezing effect, whereby probability mass is excessively concentrated on a narrow subset of high-reward trajectories, restricting genuine exploration and constraining attainable performance under RL training. To address this issue, in this work, we propose Steering Probability Squeezing (SPS), a training paradigm that interleaves conventional RL with inverse reinforcement learning (IRL). SPS treats on-policy rollouts as demonstrations and employs IRL to explicitly reshape the induced trajectory distribution, thereby enhancing exploration without introducing external supervision. Experiments on five commonly used reasoning benchmarks demonstrate that SPS can enable better exploration and improve Pass@k. Beyond algorithmic contributions, we provide an analysis of RL learning dynamics and identify an empirical upper bound on Pass@k, shedding light on intrinsic exploration limits in RL-based reasoning models. Our findings suggest that alternating between RL and IRL offers an effective pathway toward extending the exploration capacity of reasoning-oriented large language models.

---

## 论文详细总结（自动生成）

以下是对论文《SPS: Steering Probability Squeezing for Better Exploration in Reinforcement Learning for Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在面向推理的大语言模型（LLM）训练中，强化学习（RL）虽能显著提升单样本成功率（Pass@1），但往往导致多样本探索能力（Pass@k）增长缓慢。
*   **核心问题**：论文指出这源于一种**“概率挤压效应”（Squeezing Effect）**。在 RL 训练中，对低概率负样本的惩罚并不会将概率质量均匀分配，而是会将其“挤压”到已经占据主导地位的贪婪响应上。这种效应导致概率分布过度尖锐化（Sharpening），引发熵崩溃，限制了模型发现多样化推理路径的能力。
*   **整体含义**：论文旨在通过一种新的训练范式，在不引入外部监督的情况下，显式地引导和重塑概率分布，从而突破 RL 训练中的探索瓶颈。

### 2. 方法论
论文提出了 **SPS（Steering Probability Squeezing，引导概率挤压）** 框架，核心思想是**交替进行强化学习（RL）与逆强化学习（IRL）**：
*   **核心思想**：利用 IRL 的“质量覆盖”（Mass-covering）特性来抵消 RL 的“挤压”效应。将 RL 阶段生成的策略内采样（On-policy Rollouts）视为演示数据，通过 IRL 阶段重塑策略分布。
*   **关键技术细节**：
    *   **IRL 阶段**：采用前向 KL 散度（Forward-KL）作为目标函数，鼓励模型分布覆盖采样到的所有推理轨迹，而非仅集中于最高概率的一条。
    *   **L2TE（低似然轨迹强调）**：在 IRL 采样时，优先选择模型当前似然值较低的轨迹进行训练。这能有效放大来自欠探索区域的信号，缓解概率向主导样本集中的趋势。
    *   **迭代训练流程**：
        1.  **RL 阶段**：使用常规 RL（如 GRPO）进行探索并生成多组 Rollouts。
        2.  **采样阶段**：从 Rollouts 中筛选出具有代表性（尤其是低似然但正确）的轨迹。
        3.  **IRL 阶段**：利用筛选出的轨迹更新模型参数，重塑分布。
        4.  **循环**：将更新后的模型再次投入 RL 阶段，实现持续探索。

### 3. 实验设计
*   **数据集**：使用 Openr1-Math-46k-8192 的子集，分别构建了 3k、5k 和 10k 三种规模的训练集。
*   **基准测试（Benchmarks）**：
    *   奥数级数学竞赛：AIME 2024/2025、BRUMO、HMMT (Feb/Nov)。
    *   通用与科学推理：MATH-500、MMLU、GPQA-Diamond。
*   **对比方法（Baselines）**：
    *   **GRPO**：目前主流的群体相对策略优化算法。
    *   **DAPO**：动态采样策略优化。
    *   **GSPO**：群体序列策略优化。
*   **评估指标**：重点关注 **Pass@128**（衡量探索上限）和 **Avg@128**（衡量平均性能）。

### 4. 资源与算力
*   **硬件环境**：实验在一组包含 **4 × 8 NVIDIA H100 GPU** 的集群上运行。
*   **训练细节**：
    *   使用 SWIFT 框架实现，vLLM 作为推理后端。
    *   RL 阶段学习率为 5e-7，IRL 阶段学习率为 5e-10。
    *   训练步数设定在 700 步以内，每轮迭代包含 4 步 IRL 更新。
    *   IRL 阶段的时间开销极低，仅占总训练时间的约 3%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1.5B 和 7B 两种参数规模的模型（Qwen2.5-Math 系列），并额外在 DeepSeek-R1-Distill-Qwen-1.5B 上验证了泛化性。
*   **充分性分析**：
    *   **多维度验证**：不仅对比了最终性能，还进行了消融实验（如采样大小的影响）和学习动态分析（概率分布的变化）。
    *   **数据规模敏感度**：在 3k/5k/10k 不同数据量下均验证了有效性。
    *   **多样性分析**：通过计算轨迹相似度证明了 SPS 确实提升了生成结果的多样性。
    *   **客观性**：实验设置了明确的经验上限对比，并承认了在小模型上 RL 可能导致性能下降的现象，表现出较好的客观性。

### 6. 主要结论与发现
*   **突破探索上限**：SPS 在所有数学基准测试中均显著提升了 Pass@128。例如，在 Qwen2.5-Math-1.5B 上，SPS 在 BRUMO 榜单比 GRPO 高出 **10.00** 分。
*   **缓解挤压效应**：通过对平均对数概率（Log-probability）的诊断分析，证实 SPS 能使模型分布保持在接近基座模型的水平，避免了像 GRPO 那样过度集中概率。
*   **模型规模差异**：发现小模型（1.5B）在常规 RL 中更容易出现概率挤压和性能退化，而 SPS 对此类模型的提升尤为显著。

### 7. 优点
*   **理论支撑扎实**：对“概率挤压效应”给出了严谨的数学证明（见附录），并以此推导出 IRL 的必要性。
*   **无需外部监督**：完全利用模型自身的 Rollouts 进行自监督学习，不依赖昂贵的外部标注或更强的教师模型。
*   **高效性**：IRL 阶段计算量极小，几乎不增加额外的算力负担，具有很强的实用性。
*   **即插即用**：可以作为现有 RL 算法（如 GRPO）的增强模块。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1.5B 和 7B 规模，尚未在更大规模（如 70B+）的模型上验证其在大参数量下的表现。
*   **任务领域单一**：虽然在 MMLU 等基准上有测试，但核心实验仍集中在数学推理领域，在创意写作、代码生成等其他领域的普适性有待进一步验证。
*   **超参数敏感性**：IRL 阶段的学习率极低（5e-10），这暗示该方法可能对超参数设置较为敏感，需要精细调优。

（完）
