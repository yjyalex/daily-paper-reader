---
title: "StepPO: Step-Aligned Policy Optimization for Agentic Reinforcement Learning"
title_zh: StepPO：面向智能体强化学习的步级对齐策略优化
authors: "Daoyu Wang, Qingchuan Li, Mingyue Cheng, Jie Ouyang, Shuo Yu, Qi Liu, Enhong Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18401v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 用于智能体强化学习的步骤对齐策略优化
tldr: StepPO 针对 LLM 智能体在多轮交互任务中面临的奖励稀疏和长上下文挑战，提出将传统的 Token 级马尔可夫决策过程（MDP）提升为步级（Step-level）MDP。该方法将“步”视为智能体的基本动作单位，并引入步级信用分配机制，使策略优化与智能体的决策粒度对齐。初步实验证明，这种步级对齐的强化学习范式能更有效地提升 LLM 的决策和工具使用能力。
selection_source: fresh_fetch
motivation: 传统的 Token 级强化学习难以捕捉 LLM 智能体在复杂多轮交互中的决策行为，且面临奖励延迟和稀疏的问题。
method: 提出 StepPO 框架，将 MDP 建模从 Token 级提升至步级，并采用步级信用分配来优化策略和奖励传播。
result: 初步实验结果验证了步级对齐范式在提升智能体核心能力方面的有效性。
conclusion: 步级强化学习是提升 LLM 通用智能体能力的更优路径，为理解和优化智能体行为提供了新视角。
---

## 摘要
通用智能体催生了诸如 OpenClaw 和 Claude Code 等现象级应用。随着这些智能体系统（又称 Harnesses）追求更宏大的目标，它们对基础大语言模型（LLM）的智能体能力提出了越来越高的要求。智能体强化学习（Agentic RL）正成为赋予 LLM 这些能力的核心后训练范式，并在智能体训练中发挥着日益关键的作用。与 RLHF 和 RLVR 中的单轮 token 级对齐或推理增强不同，智能体强化学习针对的是多轮交互场景，其目标是在应对延迟且稀疏的奖励以及长且多变的上下文等新挑战的同时，优化决策和工具使用等核心智能体能力。因此，继承自传统 LLM 强化学习的以 token 为中心的建模和优化范式，在捕捉真实的 LLM 智能体行为方面正变得愈发不足。在本文中，我们提出了 StepPO，作为对步级（step-level）智能体强化学习的一种立场。我们认为，传统的 token 级马尔可夫决策过程（MDP）应当演进为步级 MDP 公式，并且应当将“步”（step）而非 token 视为 LLM 智能体合适的动作表示。随后，我们提出了步级信用分配作为该公式的自然优化对应项，从而使策略优化和奖励传播与智能体决策的粒度保持一致。最后，我们讨论了在实践中实现步级智能体强化学习所需的关键系统设计，初步实验为该观点的有效性提供了初步证据。我们希望 StepPO 所体现的步级对齐、步级范式能为智能体强化学习社区提供一个理解智能体行为的有用视角，并助力 LLM 向更强的通用智能体能力迈进。

## Abstract
General agents have given rise to phenomenal applications such as OpenClaw and Claude Code. As these agent systems (a.k.a. Harnesses) strive for bolder goals, they demand increasingly stronger agentic capabilities from foundation Large Language Models (LLMs). Agentic Reinforcement Learning (RL) is emerging as a central post-training paradigm for empowering LLMs with these capabilities and is playing an increasingly pivotal role in agent training. Unlike single-turn token-level alignment or reasoning enhancement, as in RLHF and RLVR, Agentic RL targets multi-turn interactive settings, where the goal is to optimize core agentic capabilities such as decision making and tool use while addressing new challenges including delayed and sparse rewards, as well as long and variable context. As a result, the token-centric modeling and optimization paradigm inherited from traditional LLM RL is becoming increasingly inadequate for capturing real LLM agent behavior. In this paper, we present StepPO as a position on step-level Agentic RL. We argue that the conventional token-level Markov Decision Process (MDP) should be advanced to a step-level MDP formulation, and that the step, rather than the token, should be regarded as the proper action representation for LLM agents. We then propose step-level credit assignment as the natural optimization counterpart of this formulation, thereby aligning policy optimization and reward propagation with the granularity of agent decisions. Finally, we discuss the key systems designs required to realize step-level Agentic RL in practice and preliminary experiments provide initial evidence for the effectiveness of this perspective. We hope that the step-aligned, step-level paradigm embodied in StepPO offers the Agentic RL community a useful lens for understanding agent behavior and helps advance LLMs toward stronger general-agent capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《StepPO: Step-Aligned Policy Optimization for Agentic Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 LLM 强化学习（如 RLHF、RLVR）主要采用 **Token 级** 的马尔可夫决策过程（MDP），即把每一个生成的 Token 视为一个动作。然而，在多轮交互的智能体（Agent）场景中，这种粒度过细，难以捕捉复杂的决策逻辑（如工具调用、环境反馈处理），且面临严重的奖励延迟、稀疏以及长上下文挑战。
*   **研究动机**：作者认为智能体强化学习（Agentic RL）应从 Token 级转向 **步级（Step-level）**。在智能体任务中，一个完整的交互轮次（包含思考、行动、观察）才是一个有意义的决策单位。

### 2. 论文提出的方法论：StepPO
*   **核心思想**：将智能体与环境的每一次完整交互定义为一个 MDP 步（Step），并在此粒度上进行策略建模、轨迹表示和信用分配。
*   **关键技术细节**：
    *   **步级 MDP 建模**：状态 $s_t$ 是当前观察，动作 $a_t$ 是完整的交互响应（可能包含多个 Token 或工具调用），奖励 $r_t$ 对应于该步的反馈。
    *   **结构化步级表示**：为了避免“重分词漂移”（Retokenization Drift），系统直接以 Token ID 形式存储每一步的 (State, Action, Reward)，确保训练与推理时的 Token 序列完全一致。
    *   **步级信用分配（Step-level Credit Assignment）**：
        *   使用**步级广义优势估计（Step-level GAE）**。
        *   重要性采样比率（Importance Ratio）在整个步的 Token 序列上进行累积计算。
        *   优势函数（Advantage）衡量的是这一步决策对未来长期回报的贡献，而非单个 Token 的贡献。
    *   **系统支撑**：通过 **Agent-R1**（侧重训练抽象与 Token 一致性）和 **Claw-R1**（侧重异构智能体数据管理与网关）两个系统实现异步、高效的步级训练。

### 3. 实验设计
*   **数据集/场景**：**HotpotQA**（多跳问答任务），该任务需要智能体进行多步搜索和推理。
*   **基准模型（Base Model）**：Qwen2.5-3B-Instruct。
*   **对比方法（Benchmark）**：传统的 **Token 级 PPO**。
*   **实验设置**：两者共享相同的模型、数据和 Rollout 管道，仅在优化算法的粒度（步级 vs Token 级）上有所不同。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **相关细节**：提到了在处理长上下文时使用了前缀缓存（Prefix Caching）和前缀树合并（Prefix-tree Merging）等系统优化手段，以提高计算效率。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了在 HotpotQA 上的训练曲线对比。
*   **充分性评价**：
    *   **初步性**：作者明确表示这是“初步实验证据”（Preliminary empirical evidence）。
    *   **局限性**：实验仅限于一个主要数据集（HotpotQA），缺乏在更多样化的智能体基准（如 WebShop, AlfWorld）上的广泛验证。
    *   **客观性**：对比实验控制了变量（相同模型和数据），具有一定的客观性，但实验组数较少，尚不足以全面评估该方法在各种复杂环境下的鲁棒性。

### 6. 主要结论与发现
*   **步级优化的优越性**：StepPO 在 HotpotQA 上的表现持续优于 Token 级 PPO，训练曲线更高且更稳定。
*   **对齐的重要性**：将信用分配的粒度与智能体决策的语义单位（步）对齐，能提供更有效的学习信号，尤其是在处理延迟奖励和长程任务时。
*   **系统与算法协同**：实现步级强化学习不仅需要算法改进，还需要支持异步执行和步原生（Step-native）数据管理的系统架构。

### 7. 优点
*   **建模直观且合理**：打破了 LLM 训练中“万物皆 Token”的思维定式，回归到智能体决策的本质。
*   **解决工程痛点**：通过结构化表示解决了重分词漂移问题，这对多轮对话的稳定性至关重要。
*   **系统性视角**：不仅提出了算法，还讨论了配套的系统设计（Agent-R1/Claw-R1），具有较强的实践指导意义。

### 8. 不足与局限
*   **实验覆盖面窄**：目前仅在 HotpotQA 上进行了验证，对于更复杂的工具调用、环境交互（如代码执行、网页操作）的泛化能力有待证明。
*   **离策漂移风险**：在异步训练设计中，如何处理由于策略更新导致的“离策（Off-policy）”程度过大问题，文中讨论较少。
*   **复杂度增加**：相比于扁平化的 Token 级训练，步级管理和异步系统增加了工程实现的复杂度。

（完）
