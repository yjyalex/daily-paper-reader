---
title: "The Consensus Trap: Rescuing Multi-Agent LLMs from Adversarial Majorities via Token-Level Collaboration"
title_zh: 共识陷阱：通过 Token 级协作将多智能体 LLM 从对抗性多数中解救出来
authors: "Jiayuan Liu, Shiyi Du, Weihua Du, Mingyu Guo, Vincent Conitzer"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17139v1"
tags: ["query:token"]
score: 9.0
evidence: 多智能体 LLM 的 Token 级协作以摆脱对抗性多数
tldr: 针对多智能体大模型系统在面对对抗性攻击时，传统多数投票机制在受损智能体占多数时会失效的问题，本文提出Token级轮询协作（RR）机制。该方法通过让智能体在共享上下文中交替生成Token，将静态结论聚合转变为动态逻辑交织。理论与实验证明，即使在受损智能体占多数的极端情况下，RR机制仍能凭借诚实模型的纠偏能力保持系统鲁棒性。
selection_source: fresh_fetch
motivation: 传统多智能体系统依赖的响应级聚合机制在受损智能体形成多数时，会因无法识别中间逻辑错误而导致系统崩溃。
method: 提出Token级轮询协作机制，通过让智能体在共享自回归上下文中交替生成Token，将聚合过程建模为非线性算子乘积。
result: 实验显示，在多数投票机制因对抗性攻击而失效的场景下，RR机制仍能显著维持推理准确率并超越临界阈值。
conclusion: Token级的动态协作能有效打破共识陷阱，证明了诚实模型的恢复力可以战胜占据多数的对抗性干扰。
---

## 摘要
多智能体大语言模型（LLM）架构日益依赖响应级聚合（如多数投票，MAJ）来提升推理上限。然而，在开放环境中，智能体极易受到隐蔽的上下文破坏（如针对性提示注入）的影响。我们揭示了当前多智能体系统中的一个关键结构性漏洞：当受损智能体形成局部多数时，响应级聚合会失效。由于投票聚合的是已形成的最终结论，它对中间逻辑的缺陷是盲目的。为了克服这一系统性局限，我们提出了 Token 级轮询（RR）协作，即智能体在共享的自回归上下文中顺序交替生成。我们将该过程形式化为一个离散时间动力系统，证明了 Token 级交替使聚合方式从脆弱的最终投票计数（线性求和）转变为动态交织的逻辑链（非线性算子乘积）。通过这一理论视角， we 证明了诚实模型的修复拉力可以压倒对抗性破坏，即使受损智能体占据多数。我们在多种推理基准上进行了详尽的实证评估，结果表明，虽然 MAJ 在受损智能体达到多数时会崩溃，但 RR 在远超这一临界阈值的情况下仍能保持稳健的准确率。

## Abstract
Multi-agent large language model (LLM) architectures increasingly rely on response-level aggregation, such as Majority Voting (MAJ), to raise reasoning ceilings. However, in open environments, agents are highly susceptible to stealthy contextual corruption, such as targeted prompt injections. We reveal a critical structural vulnerability in current multi-agent systems: response-level aggregation collapses when corrupted agents form a local majority. Because voting aggregates fully-formed conclusions, it is blind to flawed intermediate logic. To overcome this systematic limitation, we propose the Token-Level Round-Robin (RR) Collaboration, where agents sequentially interleave generation within a shared auto-regressive context. We formalize this process as a discrete-time dynamical system, proving that token-level interleaving transitions aggregation from a brittle counting of final votes (a linear sum) to a dynamic, interwoven chain of logic (a non-linear operator product). Through this theoretical lens, we prove that the honest model's restorative pull can overpower adversarial corruptions, even when corrupted agents form a majority. We conduct an exhaustive empirical evaluation across diverse reasoning benchmarks and demonstrate that while MAJ collapses when corrupted agents reach a majority, RR maintains robust accuracy well beyond this critical threshold.

---

## 论文详细总结（自动生成）

这篇论文针对多智能体大语言模型（LLM）系统在对抗性环境下的鲁棒性问题提出了创新性的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前多智能体 LLM 架构（如多数投票 MAJ）依赖“响应级聚合”，即每个智能体独立生成完整答案后再汇总。这种机制存在一个**“共识陷阱”**：当超过半数的智能体受到相同的上下文破坏（如隐蔽的提示注入、定向广告操纵）时，系统会因无法识别中间逻辑错误而彻底崩溃。
*   **研究背景**：在开放环境中，智能体面临的错误往往是相关的（Correlated failures），而非独立的。传统的社会选择理论在处理相关性错误时表现脆弱，导致多智能体系统在对抗性多数（Corruption ratio $\rho > 0.5$）面前毫无还手之力。

### 2. 方法论：Token 级轮询协作（RR）
*   **核心思想**：将聚合过程从“宏观响应级”下放到“微观 Token 级”。不再让智能体独立思考，而是让所有智能体在一个共享的自回归上下文中，以轮询（Round-Robin）的方式交替生成固定长度（$K$ 个 Token）的文本块。
*   **关键技术细节**：
    *   **共享轨迹作为“真理吸引子”**：诚实智能体可以在生成过程中直接干预并修正受损智能体产生的错误逻辑。一旦正确的逻辑进入共享上下文，由于 LLM 的“顺从性”（Sycophancy），受损智能体会被迫遵循已有的正确逻辑，从而放弃其对抗性目标。
    *   **RRMaj 机制**：为了进一步提升鲁棒性，系统并行运行 $M$ 条 RR 轨迹，并对最终结果进行多数投票。
*   **理论框架**：论文将推理过程建模为离散时间动力系统，利用**李雅普诺夫稳定性（Lyapunov Stability）**证明：只要诚实模型的“恢复拉力”（由其预训练的逻辑深度决定）大于对抗性干扰的“扩散力”，系统就能在受损智能体占多数的情况下保持稳定。

### 3. 实验设计
*   **数据集/场景**：涵盖了需要多步推理的客观验证任务，包括数学推理（AQUA-RAT, GSM8K, MATH500）、空间状态追踪（Track-7）和逻辑演绎（Logic-3, Logic-7）。
*   **Benchmark 与对比方法**：
    *   **基准方法**：标准的多数投票（MAJ）。
    *   **威胁模型**：设置了“温和建议型”和“强力指令型”两种提示注入攻击，模拟不同强度的对抗环境。
    *   **对比维度**：改变受损智能体的比例（从 1c4t 到 4c1t，即 1 个受损 4 个诚实到 4 个受损 1 个诚实）。
*   **模型选择**：使用了 Llama-3.3-70B、Llama-4-Scout-17B、Mistral-Small-3.1-24B、Qwen2.5-32B 等多种主流架构。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **成本分析**：作者强调了该方法的**“成本中性”**（Cost-Neutrality）。由于 RRMaj 生成的总 Token 数与 MAJ 相同，其主要的计算开销仅在于频繁切换智能体时的上下文预填充（Pre-fill），而这在现代推理架构中是高度并行化且低延迟的。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   跨 5 个不同规模和家族的模型进行测试。
    *   在 6 个推理数据集上验证。
    *   **消融实验**：探讨了 Token 块大小 $K$ 的影响、测试时计算缩放（Scaling $M$）的效果、以及异构智能体（强弱模型混合）的协作表现。
*   **充分性与客观性**：实验覆盖了从少数受损到绝大多数受损的全频谱场景，并引入了统计显著性检验（如 Fisher 精确检验）来验证“最后发言者”是否影响结果，体现了高度的客观性和严谨性。

### 6. 主要结论与发现
*   **突破 50% 阈值**：MAJ 在受损智能体占比 $\rho > 0.5$ 时准确率迅速降至接近 0，而 RRMaj 在 $\rho = 0.6$ 甚至 $\rho = 0.8$ 时仍能保持极高的准确率（例如在某些任务中从 0.8% 恢复到 89.1%）。
*   **能力不对称救赎**：弱小的诚实智能体可以成功“救赎”强大的受损智能体，因为强大的模型具有更强的逻辑惯性，一旦被引导入正确轨道，很难强行偏离。
*   **Token 块大小的平衡**：$K$ 在 100-300 Token 之间表现最佳。太小会导致逻辑碎片化，太大会给对抗智能体留下完整的错误输出空间。

### 7. 优点
*   **理论与实证结合**：不仅有显著的实验提升，还通过动力系统理论为 Token 级协作提供了坚实的数学基础。
*   **即插即用且低成本**：不需要对模型进行微调，也不增加推理的 Token 生成总量，易于部署。
*   **解决核心痛点**：直接针对多智能体系统最脆弱的“共识崩溃”问题，提出了从线性聚合到非线性协作的范式转移。

### 8. 不足与局限
*   **任务局限性**：目前主要集中在具有客观答案和严密逻辑链的推理任务上，对于主观性较强或开放式创作任务的效果尚待验证。
*   **潜在的自适应攻击**：论文提到未来需要研究能够监测共享上下文并刻意破坏修正步骤的“自适应攻击者”。
*   **延迟开销**：虽然 Token 总量不变，但频繁的上下文切换在某些推理框架下可能会增加首字延迟（TTFT）。

（完）
