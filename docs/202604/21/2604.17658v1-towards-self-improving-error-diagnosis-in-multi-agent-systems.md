---
title: Towards Self-Improving Error Diagnosis in Multi-Agent Systems
title_zh: 迈向多智能体系统中的自我改进错误诊断
authors: "Jiazheng Li, Emine Yilmaz, Bei Chen, Dieu-Thu Le"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17658v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统中复杂问题解决的自我改进错误诊断
tldr: 针对大模型多智能体系统（MAS）调试难、错误定位不准的问题，本文提出ErrorProbe框架。该框架通过异常检测、回溯剪枝及多智能体协作验证，实现自动化的语义故障归因。其核心在于利用经过验证的片段记忆进行自我改进，无需人工标注即可精准定位责任主体和错误步骤，在跨领域迁移和步骤级定位上表现优异。
selection_source: fresh_fetch
motivation: 现有的多智能体系统调试方法依赖昂贵的人工标注或难以处理长上下文的LLM评判，难以精准定位复杂交互中的关键错误步骤。
method: 提出ErrorProbe框架，通过故障分类检测、症状驱动的回溯剪枝以及多智能体团队的工具化验证来定位错误并更新验证记忆。
result: "在TracerTraj和Who&When基准测试中，ErrorProbe在步骤级错误定位上显著优于基准模型，并展现出强大的跨领域迁移能力。"
conclusion: ErrorProbe通过结合工具验证和自我改进的记忆机制，为多智能体系统的自动化故障诊断提供了一种高效且无需标注的解决方案。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）能够解决复杂问题，但也带来了显著的调试挑战，其特征包括冗长的交互轨迹、智能体间的依赖关系以及错误的延迟显现。现有的诊断方法通常依赖昂贵的专家标注或“LLM作为裁判”（LLM-as-a-judge）范式，难以在长上下文中精准定位关键的错误步骤。在本文中，我们提出了 ErrorProbe，这是一个用于语义故障归因的自我改进框架，能够识别责任智能体及错误的起始步骤。该框架通过三阶段流水线运行：(1) 实施 MAS 故障分类体系以检测局部异常；(2) 执行症状驱动的回溯追踪以剪枝无关上下文；(3) 雇用专门的多智能体团队（策略制定者、调查者、仲裁者），通过基于工具的执行来验证错误假设。至关重要的是，ErrorProbe 维护了一个经过验证的情节记忆，仅在错误模式被可执行证据确认时才进行更新，无需人工标注。在 TracerTraj 和 Who&When 基准测试上的实验表明，ErrorProbe 显著优于基准模型，特别是在步骤级定位方面，同时经过验证的记忆使其无需重新训练即可实现稳健的跨领域迁移。

## Abstract
Large Language Model (LLM)-based Multi-Agent Systems (MAS) enable complex problem-solving but introduce significant debugging challenges, characterized by long interaction traces, inter-agent dependencies, and delayed error manifestation. Existing diagnostic approaches often rely on expensive expert annotation or ''LLM-as-a-judge'' paradigms, which struggle to pinpoint decisive error steps within extended contexts. In this paper, we introduce ErrorProbe, a self-improving framework for semantic failure attribution that identifies responsible agents and the originating error step. The framework operates via a three-stage pipeline: (1) operationalizing the MAS failure taxonomy to detect local anomalies, (2) performing symptom-driven backward tracing to prune irrelevant context, and (3) employing a specialized multi-agent team (Strategist, Investigator, Arbiter) to validate error hypotheses through tool-grounded execution. Crucially, ErrorProbe maintains a verified episodic memory that updates only when error patterns are confirmed by executable evidence, without the need for annotation. Experiments across the TracerTraj and Who&When benchmarks demonstrate that ErrorProbe significantly outperforms baselines, particularly in step-level localization, while the verified memory enables robust cross-domain transfer without retraining.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型多智能体系统（MAS）中的故障诊断问题，提出了一个名为 **ErrorProbe** 的自我改进框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着 MAS 复杂度的增加，调试变得极其困难。主要挑战包括：**交互轨迹长**（动辄上百轮）、**错误延迟显现**（早期错误可能在很多步之后才导致失败）、**智能体间依赖复杂**以及**故障模式多样**。
*   **研究背景**：现有的诊断方法要么依赖昂贵的人工标注，要么使用简单的“LLM作为裁判”模式。后者在处理长上下文时，往往难以精准定位到最初引入错误的“决定性步骤”（Decisive Step），且缺乏跨领域的持续改进能力。

### 2. 方法论：核心思想与关键技术
ErrorProbe 采用三阶段流水线，模拟人类专家的调试过程：
*   **阶段一：基于 MAST 的结构化分解**：利用 MAST（多智能体系统分类法）将原始轨迹转化为结构化表示，并标记局部异常（如指令违背、验证缺失等），作为启发式先验以缩小搜索空间。
*   **阶段二：症状驱动的回溯追踪（Backward Tracing）**：
    *   **依赖解析**：构建消息间的依赖图。
    *   **递归剪枝**：从失败症状出发，反向搜索因果链，剔除无关的并行分支，压缩上下文。
*   **阶段三：多智能体诊断团队与验证记忆**：
    *   **诊断团队**：由**策略制定者**（生成假设并检索记忆）、**调查者**（利用代码沙箱等工具寻找证据）和**仲裁者**（最终裁决并决定是否存入记忆）组成。
    *   **验证记忆（Verified Episodic Memory）**：核心创新点。采用“先验证再写入”机制，只有被工具执行证据（如代码报错日志）证实的错误模式才会被存入情节记忆，从而实现无需重新训练的自我改进。

### 3. 实验设计
*   **数据集/场景**：
    1.  **TracerTraj**：代码生成领域，通过程序化注入故障生成，具有绝对准确的地面真值。
    2.  **Who&When-Algo**：算法生成的动态团队（CaptainAgent）产生的自然故障。
    3.  **Who&When-Hand**：手工设计的固定角色团队（MagenticOne）产生的自然故障。
*   **对比方法**：
    *   **LLM-as-a-Judge**：单次调用 LLM 进行判断。
    *   **Agent-as-a-Judge (Baseline)**：通用的工具增强型评估智能体。
    *   **ErrorProbe (Ours)**：不带记忆的完整框架。
    *   **ErrorProbe + Memory**：带验证记忆的完整框架。
*   **基座模型**：Claude 3.7 Sonnet, GPT-OSS-120B, Qwen3 32B。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **推理成本**：实验主要基于 API 调用（Bedrock 等）。文中提到单次诊断的平均推理时间约为 **45 秒**（使用 Claude 3.7 Sonnet），这反映了多智能体协作带来的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了评估，涵盖了合成故障和自然故障。
*   **充分性**：
    *   每组实验重复 5 次并取中位数，提供了统计显著性测试（p < 0.01）。
    *   进行了**跨领域迁移实验**（如在 KodCode 上学习，在 TracerTraj 上测试），验证了记忆机制的泛化性。
    *   包含消融实验，分别验证了回溯追踪、结构化标签和验证记忆的贡献。
*   **公平性**：所有方法在相同的基座模型和超参数配置下进行对比，实验设计较为客观。

### 6. 主要结论与发现
*   **步骤定位能力显著提升**：ErrorProbe 在“决定性步骤”定位上的表现远超 LLM-as-a-Judge（例如在 TracerTraj 上，步骤准确率从不足 10% 提升至 30%-40%）。
*   **记忆机制的有效性**：验证记忆不仅提升了同领域性能，还实现了稳健的跨领域知识迁移，且不会像传统缓存那样引入噪声。
*   **协议优于模型**：实验发现，诊断协议（如二分查找或回溯追踪）对步骤定位的影响往往大于基座模型本身的差异。

### 7. 优点与亮点
*   **无需标注的自我改进**：通过工具验证代替人工标注，解决了 MAS 调试数据稀缺的问题。
*   **因果剪枝**：回溯追踪有效解决了长上下文中的“信息迷失”问题。
*   **高可靠性**：多智能体“三权分立”的架构（策略、调查、仲裁）配合工具校验，极大地降低了诊断过程中的幻觉风险。

### 8. 不足与局限
*   **依赖显性信号**：对于不触发异常或逻辑矛盾的“静默失败”（Silent Failures），该框架的检测能力有限。
*   **推理延迟**：多轮智能体交互和工具执行导致诊断速度较慢，难以应用于超低延迟的生产环境。
*   **基座模型依赖**：虽然测试了三个模型，但对于更广泛的开源模型或更小参数规模模型的适配性仍有待验证。

（完）
