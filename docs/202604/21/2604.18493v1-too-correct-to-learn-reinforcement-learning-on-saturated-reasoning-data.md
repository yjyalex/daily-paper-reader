---
title: "Too Correct to Learn: Reinforcement Learning on Saturated Reasoning Data"
title_zh: 过于正确而难于学习：基于饱和推理数据的强化学习
authors: "Zhenwen Liang, Yujun Zhou, Sidi Lu, Xiangliang Zhang, Haitao Mi, Dong Yu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18493v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 利用GRPO在饱和基准上通过强化学习增强LLM推理
tldr: 针对强基座模型在推理任务中因表现饱和导致强化学习缺乏失败案例、优势信号消失及模式崩溃的问题，本文提出了一种无参数解码策略 CUTS。该方法通过在受限的高置信度候选集中均匀采样来增强探索，并结合 Mixed-CUTS 框架协同利用探索性与开发性样本。实验证明该方法能有效防止策略退化，显著提升了模型在 AIME25 等挑战性任务上的泛化能力。
selection_source: fresh_fetch
motivation: 强基座模型在标准推理基准上表现饱和，导致 GRPO 等算法因缺乏负样本而失去优势信号，引发模式崩溃。
method: 提出 CUTS 解码策略以实现结构化探索，并构建 Mixed-CUTS 框架来平衡探索与开发，从而放大组内优势方差。
result: "在 Qwen3 模型上的实验显示，Mixed-CUTS 在 AIME25 上的 Pass@1 准确率比标准 GRPO 提升了高达 15.1%。"
conclusion: 在语义流形内保持解法的多样性对于提升大语言模型在严谨推理任务中的泛化性能至关重要。
---

## 摘要
强化学习（RL）提升了大语言模型（LLM）的推理能力，但随着模型规模的扩大，出现了一个悖论：强大的基座模型在标准基准测试（如 MATH）上趋于饱和，生成的解法虽然正确但过于同质化。在这种环境下，由于缺乏失败案例，导致组相对算法（如 GRPO）中的优势信号消失，从而使策略陷入模式崩溃。为了解决这一问题，我们提出了约束均匀 Top-K 采样（CUTS），这是一种无参数的解码策略，旨在强制执行保持结构的探索。与遵循模型偏置的标准采样不同，CUTS 通过从受限的高置信度候选解中进行均匀采样，平滑了局部优化景观。我们将此集成到 Mixed-CUTS 中，这是一个协同利用性和探索性展开（rollouts）的训练框架，旨在放大组内优势方差。在 Qwen3 模型上的实验表明，我们的方法防止了策略退化，并显著提升了域外泛化能力。值得注意的是，Mixed-CUTS 在极具挑战性的 AIME25 基准测试上，将 Pass@1 准确率比标准 GRPO 提升了高达 15.1%，验证了在语义流形内保持多样性对于严谨推理至关重要。

## Abstract
Reinforcement Learning (RL) enhances LLM reasoning, yet a paradox emerges as models scale: strong base models saturate standard benchmarks (e.g., MATH), yielding correct but homogeneous solutions. In such environments, the lack of failure cases causes the advantage signal in group-relative algorithms (e.g., GRPO) to vanish, driving policies into mode collapse. To address this, we propose Constrained Uniform Top-K Sampling (CUTS), a parameter-free decoding strategy enforcing structure-preserving exploration. Unlike standard sampling that follows model biases, CUTS flattens the local optimization landscape by sampling uniformly from constrained high-confidence candidates. We integrate this into Mixed-CUTS, a training framework synergizing exploitative and exploratory rollouts to amplify intra-group advantage variance. Experiments on Qwen3 models demonstrate that our approach prevents policy degeneration and significantly boosts out-of-domain generalization. Notably, Mixed-CUTS improves Pass@1 accuracy on the challenging AIME25 benchmark by up to 15.1% over standard GRPO, validating that maintaining diversity within the semantic manifold is critical for rigorous reasoning.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在强化学习（RL）过程中的“饱和数据”问题提出了创新性的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着基座模型（如 Qwen3）能力的增强，它们在标准推理基准（如 MATH）上的表现已趋于饱和。
*   **核心矛盾**：在基于组相对策略优化（如 GRPO）的强化学习中，算法依赖于组内样本的奖励差异来产生“优势信号（Advantage Signal）”。当模型“过于正确”时，组内所有样本均为正确，奖励方差趋近于零，导致优势信号消失。
*   **后果**：模型会陷入“模式崩溃（Mode Collapse）”，即停止探索更具泛化性的推理策略，仅重复已掌握的简单模式，无法在更难的任务（如 AIME）上取得突破。

### 2. 方法论
论文提出了 **Mixed-CUTS** 框架，核心包含两个组件：
*   **CUTS (Constrained Uniform Top-K Sampling)**：一种无参数的解码策略，分为三个阶段：
    1.  **选择（Select）**：提取当前步骤概率最高的 Top-K 个 Token。
    2.  **过滤（Filter）**：仅保留概率大于阈值 $\delta$ 的候选者，确保语义合理性。
    3.  **均等化（Equalize）**：在保留的候选集中进行**均匀采样**，而非按模型原始概率分布采样。这打破了模型对某些路径的过度偏好，强制进行“宽度优先”的结构化探索。
*   **Mixed-CUTS 训练框架**：
    *   在 GRPO 的每个采样组中，混合使用一半的标准采样（开发，Exploitation）和一半的 CUTS 采样（探索，Exploration）。
    *   **原理**：即使标准采样全部正确，CUTS 采样由于强制探索，可能会产生失败案例或不同的正确路径，从而人为地恢复组内方差，为优化提供持续的梯度信号。

### 3. 实验设计
*   **训练集**：MATH 训练集。
*   **评估基准（Benchmark）**：
    *   数学领域：MATH-500, AIME24, AIME25, AMC。
    *   科学与通用推理：GPQA-Diamond, MMLU-Pro, SuperGPQA。
*   **对比方法**：
    *   标准 GRPO 算法。
    *   基座模型（非思考模式）。
    *   基座模型（原生“思考模式/Thinking Mode”）。
*   **模型规模**：Qwen3-1.7B 和 Qwen3-4B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **配置细节**：提到了全局 Batch Size 为 128，PPO 微批次为 32；1.7B 模型最大生成长度为 5,000 tokens，4B 模型扩展至 12,000 tokens。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.7B 到 4B 的模型缩放实验，并在 7 个不同的推理基准上进行了测试。
*   **消融与分析**：
    *   进行了超参数敏感性分析（针对 $K$ 值和阈值 $\delta$）。
    *   分析了训练动力学，包括响应长度、策略熵和奖励曲线的变化。
    *   验证了在“硬数据”（DAPO 数据集）上的表现。
*   **充分性评价**：实验设计较为全面，不仅验证了数学能力的提升，还通过 MMLU-Pro 等跨领域测试证明了该方法提升的是通用推理能力而非简单的过拟合，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **突破饱和陷阱**：Mixed-CUTS 显著提升了模型在困难任务上的表现。在 Qwen3-4B 上，AIME25 的 Pass@1 准确率比标准 GRPO 提升了 **15.1%**。
*   **蒸馏思考能力**：通过该方法训练的标准模型（非思考模式），在 AIME25 等指标上甚至超过了基座原生的“思考模式”，实现了高效推理。
*   **维持探索活力**：训练曲线显示，Mixed-CUTS 能维持较高的策略熵和更长的推理链，证明模型在学习自纠错等深层推理行为。

### 7. 优点
*   **无参数且轻量**：CUTS 仅在解码阶段干预，不需要额外的价值网络或复杂的损失函数。
*   **结构化探索**：相比于盲目增加熵惩罚，CUTS 通过 Top-K 和阈值过滤，确保了探索是在“语义合理”的范围内进行的。
*   **泛化性强**：实验证明在数学数据上训练的模型，其通用推理能力（GPQA, MMLU-Pro）也得到了同步增强。

### 8. 不足与局限
*   **理论证明缺失**：论文主要基于经验观察和直觉设计，缺乏关于解码端均匀化如何影响 GRPO 收敛性的严格数学证明。
*   **超参数依赖**：虽然实验显示在一定范围内鲁棒，但 $K$ 和 $\delta$ 的选择仍可能随模型规模或任务类型的变化而需要微调。
*   **应用范围限制**：目前仅在推理任务（数学、逻辑）上验证，对于创意写作、开放式对话等非客观评分任务的适用性尚待研究。

（完）
