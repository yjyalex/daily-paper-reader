---
title: "Neural Garbage Collection: Learning to Forget while Learning to Reason"
title_zh: 神经垃圾回收：在学习推理的同时学习遗忘
authors: "Michael Y. Li, Jubayer Ibn Hamid, Emily B. Fox, Noah D. Goodman"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18002v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对思维链推理的强化学习及KV缓存管理
tldr: 针对大语言模型在思维链推理中KV缓存随步数增长导致的效率瓶颈，本文提出神经垃圾回收（NGC）机制。该方法让模型在学习推理的同时，通过强化学习自主学习如何清理KV缓存。模型在推理过程中定期暂停并决定剔除哪些缓存项，整个过程仅由任务结果奖励驱动。实验表明，NGC在保持高准确率的同时实现了2-3倍的缓存压缩，显著优于传统启发式策略。
selection_source: fresh_fetch
motivation: 现有的KV缓存管理多依赖人工设计的启发式规则，难以适应复杂的思维链推理需求，限制了模型长程推理的扩展性。
method: 提出NGC框架，将缓存剔除视为离散动作，利用强化学习根据最终任务奖励端到端地联合优化推理逻辑与内存管理策略。
result: 在Countdown、AMC和AIME等任务中，NGC在实现2-3倍KV缓存压缩的情况下，准确率接近全量缓存上限且大幅超越基准方法。
conclusion: 该研究证明了模型可以自主学习高效的内存管理策略，为通过端到端优化同时提升语言模型的能力与效率提供了新路径。
---

## 摘要
思维链（Chain-of-thought）推理显著提升了语言模型的能力，但每一个推理步骤都会增加 KV 缓存（KV cache），从而成为进一步扩展该范式的瓶颈。目前的方法通常使用人工设计的准则来代模型管理这些约束。遵循深度学习中更广泛的模式，一种更具扩展性的方法是让端到端学习完全取代这一设计选择。毕竟，如果模型可以学习推理，为什么它不能学习遗忘呢？我们引入了神经垃圾回收（Neural Garbage Collection, NGC），在这种方法中，语言模型在学习推理的同时学习遗忘，且仅通过基于结果的任务奖励进行端到端训练。当模型进行推理时，它会定期暂停，决定驱逐哪些 KV 缓存条目，并基于剩余的缓存继续推理。通过将思维链中的标记（tokens）和缓存驱逐决策视为从语言模型中采样的离散动作，我们可以利用强化学习来联合优化模型的推理方式及其管理自身内存的方式：模型驱逐的内容塑造了它记忆的内容，记忆的内容塑造了它的推理，而推理的正确性决定了它的奖励。至关重要的是，模型完全从单一的学习信号——基于结果的任务奖励——中学习这种行为，无需监督微调或代理目标。在 Countdown、AMC 和 AIME 任务上，NGC 在峰值 KV 缓存大小压缩 2-3 倍的情况下，相对于全缓存上限保持了强大的准确性，并显著优于驱逐基准。我们的结果是迈向更宏大愿景的第一步，即端到端优化同时驱动语言模型的能力和效率。

## Abstract
Chain-of-thought reasoning has driven striking advances in language model capability, yet every reasoning step grows the KV cache, creating a bottleneck to scaling this paradigm further. Current approaches manage these constraints on the model's behalf using hand-designed criteria. A more scalable approach would let end-to-end learning subsume this design choice entirely, following a broader pattern in deep learning. After all, if a model can learn to reason, why can't it learn to forget? We introduce Neural Garbage Collection (NGC), in which a language model learns to forget while learning to reason, trained end-to-end from outcome-based task reward alone. As the model reasons, it periodically pauses, decides which KV cache entries to evict, and continues to reason conditioned on the remaining cache. By treating tokens in a chain-of-thought and cache-eviction decisions as discrete actions sampled from the language model, we can use reinforcement learning to jointly optimize how the model reasons and how it manages its own memory: what the model evicts shapes what it remembers, what it remembers shapes its reasoning, and the correctness of that reasoning determines its reward. Crucially, the model learns this behavior entirely from a single learning signal - the outcome-based task reward - without supervised fine-tuning or proxy objectives. On Countdown, AMC, and AIME tasks, NGC maintains strong accuracy relative to the full-cache upper bound at 2-3x peak KV cache size compression and substantially outperforms eviction baselines. Our results are a first step towards a broader vision where end-to-end optimization drives both capability and efficiency in language models.

---

## 论文详细总结（自动生成）

### 论文总结：Neural Garbage Collection (NGC)

#### 1. 核心问题与整体含义
*   **研究动机**：思维链（CoT）推理虽然显著提升了语言模型的能力，但其生成的冗长推理步骤会导致 KV 缓存（KV Cache）迅速膨胀，成为推理效率和长文本处理的瓶颈。
*   **核心问题**：现有的 KV 缓存管理主要依赖人工设计的启发式规则（如保留最近 Token 或高注意力权重 Token），这些规则无法适应复杂的推理需求。
*   **整体含义**：论文提出“效率也是一种能力”的观点，认为模型既然能学习推理，也应该能学习“遗忘”。通过将缓存管理视为一种离散动作，利用强化学习（RL）让模型在优化任务准确率的同时，自主学习如何清理不再需要的内存。

#### 2. 方法论
*   **核心思想**：将 KV 缓存的驱逐（Eviction）建模为模型在推理过程中的一种离散决策动作，与 Token 生成共同构成联合动作空间，通过基于结果的奖励（如答案是否正确）进行端到端训练。
*   **关键技术细节**：
    *   **“增长-驱逐”循环（Grow-then-evict）**：模型每生成 $\delta$ 个 Token 触发一次驱逐轮次，按比例 $\epsilon$ 永久移除部分缓存，使缓存大小在长期推理中趋于稳定。
    *   **动作参数化**：复用模型现有的注意力机制。利用最近 $w$ 个查询（Query）对缓存键（Key）的注意力得分作为重要性评分，无需引入额外参数。
    *   **Gumbel-top-k 采样**：为了使离散的驱逐决策可导，使用 Gumbel-top-k 技巧进行随机子集采样，并推导出闭式对数概率，以便应用策略梯度算法。
    *   **重放注意力掩码（Replay Attention Masks）**：这是训练的关键。由于驱逐改变了上下文，训练时必须使用特殊的掩码来重现生成时的可见性模式，以解决“策略偏离”（Off-policyness）导致的训练崩溃。
    *   **预算感知内省（Budget-aware Interoception）**：在提示词中加入当前的驱逐率标签，让模型意识到自身的内存约束，从而调整推理策略。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Countdown**：组合算术任务，要求模型利用给定数字通过基本运算达到目标值。
    *   **数学竞赛任务**：使用 DAPO-17k 数据集训练，在 AMC 2023/2025 和 AIME 2025 榜单上评估。
*   **基准模型**：DeepSeek-R1-Distill-Qwen-1.5B。
*   **对比方法**：
    *   **SnapKV**（基于注意力权重）。
    *   **KeyDiff**（基于键的差异性/多样性）。
    *   **KNorm**（基于键的范数统计）。
    *   **StreamingLLM**（滑动窗口+注意力锚点）。
    *   **Full Cache**（不进行任何驱逐的性能上限）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练时长/规模**：
    *   Countdown 任务训练了 250 个步骤（Step），每步包含 32 个唯一提示词，每个提示词 16 次采样。
    *   DAPO-17k 任务训练了 469 个步骤，每步 256 个提示词，每个提示词 8 次采样。
    *   使用了 Dr. GRPO 优化器，这是一种内存高效的强化学习算法。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的算术逻辑到复杂的数学竞赛题目，实验设置较为全面。
*   **消融实验**：针对“端到端训练的必要性”、“重放掩码的作用”以及“内省机制”做了详细的消融分析。
*   **充分性与公平性**：对比了多种主流的启发式缓存管理方法，并在相同的压缩率（2.4x 到 4.6x）下进行测试。实验结果显示 NGC 在不同任务下表现稳定，而启发式方法在不同任务间表现波动剧烈，证明了 NGC 的鲁棒性。

#### 6. 主要结论与发现
*   **性能卓越**：在 2-3 倍的缓存压缩下，NGC 的准确率接近全量缓存模型，且显著优于所有启发式基准（例如在 Countdown 任务中，NGC 准确率为 49.6%，而次优基准仅为 21.2%）。
*   **泛化能力**：NGC 能够泛化到训练时未见过的更严格的内存预算。
*   **任务自适应**：模型学会了根据任务需求保留关键信息（如数学推导的中间结论），而启发式规则往往会误删这些信息。

#### 7. 优点
*   **端到端优雅性**：将效率优化与能力提升统一在同一个 RL 框架下，遵循了深度学习“越少人工干预越好”的长期趋势。
*   **零额外参数**：完全复用现有注意力权重，不增加模型参数量。
*   **解决训练不一致性**：通过重放掩码解决了 KV 缓存动态变化带来的训练不稳定问题，具有较强的技术参考价值。

#### 8. 不足与局限
*   **训练成本**：虽然推理更高效，但训练过程需要进行强化学习，相比直接应用启发式规则，其计算开销和数据需求更高。
*   **硬件适配**：论文主要关注算法逻辑，虽然提到了块级驱逐（Block-level）符合硬件趋势，但未在实际推理框架（如 vLLM）中展示端到端的吞吐量提升数据。
*   **模型规模限制**：实验主要集中在 1.5B 参数的小模型上，在大规模模型（如 70B+）上的表现和训练稳定性尚待验证。

（完）
