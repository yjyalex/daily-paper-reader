---
title: "When Agents Go Quiet: Output Generation Capacity and Format-Cost Separation for LLM Document Synthesis"
title_zh: 当智能体变得沉默：大语言模型文档合成中的输出生成容量与格式-成本分离
authors: "Justice Owusu Agyemang, Michael Agyare, Miriam Kobbinah, Nathaniel Agbugblah, Prosper Addo"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16736v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 格式重文档合成中的格式成本分离与Token效率
tldr: "本研究针对LLM在生成大型格式化文档时出现的“输出停滞”现象，提出了输出生成能力（OGC）概念，揭示了实际输出能力远小于上下文窗口的现状。通过证明格式-成本分离定理，研究提出了一种延迟渲染策略，并构建了自适应策略选择框架。实验表明，该方法在Claude、GPT等模型上可减少48-72%的生成Token，彻底解决了输出停滞问题，并开源了GEN-PILOT工具。"
selection_source: fresh_fetch
motivation: 解决LLM在生成长篇且格式复杂的文档时，因超出实际输出能力而导致静默失败（输出停滞）的问题。
method: 提出输出生成能力（OGC）理论，并基于格式-成本分离定理推导出延迟渲染和自适应策略选择框架。
result: "实验显示延迟渲染策略在多种主流模型上将Token消耗降低了48-72%，并完全消除了输出停滞现象。"
conclusion: 通过将内容生成与格式渲染分离，可以显著提升LLM的文档合成效率和稳定性，为长文档生成提供了理论支撑和实用工具。
---

## 摘要
由大语言模型驱动的编程智能体面临一种尚未被充分理解的失效模式，我们称之为“输出停滞”（output stalling）：当智能体尝试生成大型且格式繁重的文档时，会静默地产生空响应。我们提出了一个理论框架，通过以下三项贡献来解释并防止这种失效：(1) 引入了输出生成容量（OGC），这是一种衡量智能体在给定当前上下文状态下产生输出的有效能力的正式指标——它不同于原始上下文窗口，且在经验上小于原始上下文窗口。(2) 证明了格式-成本分离定理，表明对于任何开销乘数 $μ_f > 1$ 的格式，延迟模板渲染在 Token 效率上始终至少与直接生成相当，并推导出了节省成本的紧致界限。(3) 将自适应策略选择形式化，这是一个决策框架，它将估计的输出成本与可用 OGC 的比率映射为最优生成策略（直接生成、分块生成或延迟生成）。我们通过针对三种模型（Claude 3.5 Sonnet、GPT-4o、Llama 3.1 70B）、四种文档类型的受控实验，以及一项隔离各组件贡献的消融研究验证了该理论。在所有条件下，延迟渲染将大语言模型生成的 Token 减少了 48-72%，并完全消除了输出停滞。我们将该框架实例化为开源 MCP 服务器 GEN-PILOT，证明了该理论可以直接转化为实用工具。

## Abstract
LLM-powered coding agents suffer from a poorly understood failure mode we term output stalling: the agent silently produces empty responses when attempting to generate large, format-heavy documents. We present a theoretical framework that explains and prevents this failure through three contributions. (1) We introduce Output Generation Capacity (OGC), a formal measure of an agent's effective ability to produce output given its current context state - distinct from and empirically smaller than the raw context window. (2) We prove a Format-Cost Separation Theorem showing that deferred template rendering is always at least as token-efficient as direct generation for any format with overhead multiplier $μ_f > 1$, and derive tight bounds on the savings. (3) We formalize Adaptive Strategy Selection, a decision framework that maps the ratio of estimated output cost to available OGC into an optimal generation strategy (direct, chunked, or deferred). We validate the theory through controlled experiments across three models (Claude 3.5 Sonnet, GPT-4o, Llama 3.1 70B), four document types, and an ablation study isolating each component's contribution. Deferred rendering reduces LLM generation tokens by 48-72% across all conditions and eliminates output stalling entirely. We instantiate the framework as GEN-PILOT, an open-source MCP server, demonstrating that the theory translates directly into a practical tool.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在生成长篇且格式复杂的文档时出现的“输出停滞”（Output Stalling）现象进行了深入研究，并提出了相应的理论框架与实用工具。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机：** 现有的 LLM 编程智能体在处理大型、格式繁重的文档（如 Word、LaTeX）时，常会出现“静默失败”：模型不报错也不生成内容，而是返回空响应，并在此过程中消耗大量上下文 Token。
*   **核心问题：** 这种失效并非因为超出了原始上下文窗口（Context Window），而是因为模型的“有效生成能力”随上下文占用比例增加而非线性下降。论文旨在解决智能体缺乏“生成容量元认知”（即无法预判自己能否完成输出）的问题。

### 2. 论文提出的方法论
论文提出了一个包含三个核心组件的理论框架：
*   **输出生成容量 (OGC)：** 定义了有效生成容量 $OGC(\sigma)$，它等于原始剩余空间乘以一个退化函数 $\alpha$。实验发现 $\alpha$ 随上下文占用率呈 S 型（Sigmoid）衰减，即当上下文填满一半时，实际能一次性输出的 Token 数远小于理论剩余值。
*   **格式-成本分离定理 (FCS)：** 将生成成本分解为“内容 Token”和“格式 Token”。证明了通过“延迟渲染”（即 LLM 只生成结构化数据如 JSON，再由外部模板引擎渲染格式）在 Token 效率上总是优于直接生成。
*   **自适应策略选择：** 建立了一个决策框架，根据估计的输出成本与当前 OGC 的比率，在三种策略中自动切换：
    1.  **直接生成 (Direct)：** 成本低且容量充足时使用。
    2.  **分块生成 (Chunked)：** 将任务拆分为多个步骤。
    3.  **延迟渲染 (Deferred)：** 成本最高效、最稳健，适用于高压场景。
*   **工具实现：** 开发了 **Gen-Pilot**（开源 MCP 服务器），将上述理论转化为可调用的工具层。

### 3. 实验设计
*   **实验场景：** 涵盖了四种典型的文档合成任务：评估报告（Word）、API 文档（LaTeX）、会议记录（Markdown）、数据报告（含大量表格和图表）。
*   **对比模型：** Claude 3.5 Sonnet、GPT-4o、Llama 3.1 70B。
*   **Benchmark 与指标：** 以“直接生成”为基准，对比了 Token 消耗量、成功率（是否完整生成）、墙钟时间（生成速度）和输出质量。
*   **消融实验：** 分别测试了仅使用 OGC 预警、OGC+FCS、以及全功能框架的效果。

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的硬件训练算力（如 GPU 型号或数量）。由于该研究主要关注推理阶段的策略优化和经验测量，而非模型训练，因此实验主要基于商业 API（Claude, GPT-4o）和本地部署的推理（Llama 3.1）。
*   **Token 消耗：** 实验中提到了在单次失效案例中消耗了约 50,000 Token。

### 5. 实验数量与充分性
*   **实验规模：** 针对 3 种模型、4 种文档类型、3 种策略进行了交叉验证。每种条件重复运行 5 次以获取均值和 95% 置信区间，总计约 180 次实验运行。
*   **充分性评价：** 实验设计较为充分且客观。它不仅在不同参数规模的模型（闭源与开源）上验证了普适性，还通过消融实验隔离了每个理论组件的贡献，证明了 OGC 和 FCS 的独立必要性。

### 6. 论文的主要结论与发现
*   **OGC 退化规律：** 所有测试模型的有效输出能力在上下文占用率达到 40%-70% 之间时会剧烈下降。
*   **显著节能：** 延迟渲染策略在所有条件下可减少 **48-72%** 的生成 Token 消耗。
*   **消除停滞：** 采用该框架后，原本在“直接生成”模式下 100% 失败的高压场景，成功率提升至 **100%**。
*   **模型无关性：** 输出停滞是生成任务的固有属性，而非特定模型的缺陷，该框架对主流模型均有效。

### 7. 优点
*   **理论与实践结合：** 不仅提出了严谨的数学定义（OGC、FCS 定理），还提供了可直接集成的开源工具（Gen-Pilot）。
*   **元认知提升：** 为智能体引入了“自我容量评估”机制，避免了盲目尝试导致的 Token 浪费。
*   **效率极高：** 通过分离格式和内容，大幅降低了对 LLM 长文本生成能力的压力。

### 8. 不足与局限
*   **模板依赖：** 延迟渲染依赖于预先编写的 Jinja2 模板，对于极度灵活、无固定格式的文档生成支持较弱。
*   **OGC 测量偏差：** OGC 的退化函数 $\alpha$ 是经验性的，可能随模型版本更新或系统提示词的变化而波动，需要定期校准。
*   **上下文访问限制：** 许多 API 并不实时返回精确的 Token 计数，导致 OGC 的实时估算存在一定误差。
*   **样本量：** 虽然实验覆盖面广，但每组 5 次的重复次数在统计严谨性上仍有提升空间。

（完）
