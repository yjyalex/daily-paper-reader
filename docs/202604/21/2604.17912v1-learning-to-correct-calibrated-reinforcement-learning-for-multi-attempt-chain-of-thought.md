---
title: "Learning to Correct: Calibrated Reinforcement Learning for Multi-Attempt Chain-of-Thought"
title_zh: 学习纠错：面向多次尝试思维链的校准强化学习
authors: "Muhammed Emrullah Ildiz, Halil Alperen Gozeten, Ege Onur Taga, Samet Oymak"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17912v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 使用CAL-GRPO进行多尝试思维链推理的强化学习
tldr: "本研究针对复杂推理任务中的多轮尝试（Multi-Attempt CoT）场景，提出了一种名为 CAL-GRPO 的强化学习方法。在模型可根据反馈进行多次修正的设定下，传统 RL 方法在优化 Verification@K 指标时存在梯度偏差。CAL-GRPO 通过设计校准的尝试级权重策略，实现了无偏梯度估计并降低了方差，显著提升了模型在多次尝试中的纠错能力和最终推理性能。"
selection_source: fresh_fetch
motivation: 现有的长思维链推理模型在多次尝试修正错误时，缺乏能够有效利用每轮反馈且无偏的强化学习优化方法。
method: "提出了 CAL-GRPO 算法，通过引入校准的尝试级权重策略来优化 Verification@K 奖励，确保梯度估计的无偏性和低方差。"
result: 实验证明 CAL-GRPO 在合成和真实数据集上均优于 vanilla GRPO 和朴素加权方法，有效提升了模型的纠错成功率。
conclusion: 通过理论分析和实验验证，证明了校准的尝试级奖励权重对于提升多轮尝试推理性能至关重要。
---

## 摘要
最先进的推理模型利用长思维链（CoT），通过增加测试时计算量来解决日益复杂的问题。在这项工作中，我们探索了一种长CoT设置，即模型最多进行K次连续尝试来解决一个问题，在收到硬验证器（hard verifier）反馈后，每次尝试都可以在之前的尝试基础上进行。这促使强化学习（RL）方法通过仔细权衡单次尝试来利用每次尝试的奖励。我们研究了Verification@K奖励（即模型在第K次尝试时成功）的优化，并证明根据通过/失败结果对尝试进行朴素加权会导致梯度偏差。通过设计一种在保持小方差的同时获得无偏梯度的加权策略，我们提出了校准尝试级（CAL）GRPO。我们的理论揭示了引入单次尝试奖励如何影响训练以及最终的Verification@K性能。在合成数据和真实数据上的实验、基准测试和消融实验证实了我们的理论，以及CAL-GRPO相对于原始GRPO和朴素加权的优势。

## Abstract
State-of-the-art reasoning models utilize long chain-of-thought (CoT) to solve increasingly complex problems using more test-time computation. In this work, we explore a long CoT setting where the model makes up to K successive attempts at solving a problem, in which each attempt is allowed to build on earlier ones after the model receives a hard verifier feedback. This motivates RL methods that can harness per-attempt rewards by carefully weighting individual attempts. We study optimizing the Verification@K reward (the model succeeds by the K-th attempt) and show that naively weighing the attempts by their pass/fail results in biased gradients. We introduce Calibrated Attempt-Level (CAL) GRPO by devising a weighing strategy to obtain unbiased gradients while maintaining small variance. Our theory reveals how incorporating per-attempt rewards influence the training and the eventual Verification@K performance. Experiments, baselines, and ablations on synthetic and real data corroborate our theory and the benefits of CAL-GRPO over vanilla GRPO as well as naive weighting.

---

## 论文详细总结（自动生成）

这篇论文《Learning to Correct: Calibrated Reinforcement Learning for Multi-Attempt Chain-of-Thought》探讨了如何通过强化学习（RL）优化大语言模型在多次尝试（Multi-Attempt）场景下的纠错能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着推理模型（如 OpenAI o1 等）的兴起，通过增加测试时计算量（Test-time Compute）来解决复杂问题成为主流。模型通常会进行多次尝试（CoT），并在收到验证器（Verifier）的反馈后修正之前的错误。
*   **核心问题**：在“Verification@K”（即 K 次尝试内成功即算通过）的设定下，如何有效地进行强化学习？
    *   **轨迹级奖励（TL）**：将最终结果反馈给所有步骤，导致方差极大且信用分配（Credit Assignment）模糊。
    *   **朴素尝试级奖励（NAL）**：简单地对每轮尝试给 0/1 奖励，这会导致梯度偏差，使模型过度关注第一轮的正确性而忽视了后续的纠错学习。
*   **研究动机**：开发一种既能降低方差又是无偏的算法，专门优化模型在多次尝试中的纠错表现。

### 2. 方法论：CAL-GRPO
*   **核心思想**：提出**校准尝试级（Calibrated Attempt-Level, CAL）**策略。其核心逻辑是：某次尝试对最终成功的“边际贡献”，取决于该尝试本身成功且**所有后续尝试都失败**的概率。
*   **关键技术细节**：
    *   **权重设计**：为第 $i$ 次尝试分配权重 $w_i = \prod_{j=i+1}^K (1 - \hat{\rho}_j)$，其中 $\hat{\rho}_j$ 是第 $j$ 次尝试的估计成功率。这意味着如果后续尝试很容易成功，当前尝试的权重就会降低。
    *   **算法框架**：基于 **GRPO**（群组相对策略优化）。在同一组样本中，对不同尝试阶段分别进行归一化处理。
    *   **无偏性证明**：论文从理论上证明了 CAL 估计器是无偏的，且在特定条件下比轨迹级（TL）方法具有更低的方差。
*   **流程**：模型生成最多 K 次尝试 -> 验证器给出每轮的硬反馈（通过/失败） -> 计算每轮尝试的校准权重 -> 更新策略梯度。

### 3. 实验设计
*   **数据集与场景**：
    1.  **MATH**：标准的数学竞赛题目，考察复杂推理。
    2.  **Maze（迷宫导航）**：5x5、7x7、9x9 不同尺寸的文本迷宫，考察空间规划。
    3.  **合成马尔可夫链（Markov Chain）**：包含“陷阱”状态的受控实验，用于验证算法在严苛验证条件下的理论表现。
*   **Benchmark 与对比方法**：
    *   **TL-GRPO**：轨迹级奖励（基准）。
    *   **NAL-GRPO**：朴素尝试级奖励（消融对比）。
    *   **CAL-GRPO**：本文提出的校准方法。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 张 NVIDIA L40S 或 A100 GPU**。
*   **训练细节**：
    *   MATH 训练 2 个 epoch，Maze 训练 1 个 epoch。
    *   使用了 AdamW 优化器，学习率在 $10^{-6}$ 到 $10^{-5}$ 之间。
    *   利用了 `veRL` 框架和 `SGLang` 后端进行高效推理采样。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到真实复杂推理任务（MATH）的跨度。
*   **消融与变体**：针对 $K=2$ 和 $K=4$（Ver@2, Ver@4）进行了测试，并对组大小（Group Size）和标准差归一化进行了消融实验。
*   **充分性评价**：实验设计较为充分，通过 Maze 任务的 5 个独立随机种子运行并提供置信区间，增强了结果的可信度。对比了不同难度（迷宫尺寸）和不同目标（准确率 vs 尝试次数），展示了算法的全面性。

### 6. 主要结论与发现
*   **性能提升**：CAL-GRPO 在所有任务中均取得了最高的 Verification@K 准确率。
*   **纠错能力**：相比 NAL 倾向于在第一轮就解决问题，CAL 显著增强了模型在第二轮及之后的“纠错”成功率。
*   **效率权衡**：NAL 方法虽然准确率略低，但能最小化平均尝试次数（更倾向于第一轮对）；而 CAL 方法通过稍微增加尝试次数，换取了更高的最终成功率。
*   **方差与偏差**：理论和实验均证明 CAL 成功修正了 NAL 的梯度偏差，并保持了比 TL 更低的方差。

### 7. 优点
*   **理论严谨**：不仅提出了启发式方法，还提供了关于梯度无偏性和方差缩减的数学证明。
*   **针对性强**：直接解决了长思维链推理中“如何学习利用反馈”这一关键痛点。
*   **即插即用**：作为一种奖励加权策略，可以很容易地集成到现有的 GRPO 或 PPO 框架中。

### 8. 不足与局限
*   **假设限制**：理论证明依赖于“尝试之间条件独立”的假设（Assumption 5.1），但在实际 LLM 对话中，后一次尝试高度依赖前一次的上下文。
*   **反馈类型**：目前仅针对“硬反馈”（0/1 结果），未探讨如何处理模糊反馈或自然语言形式的详细批评（Critique）。
*   **计算开销**：虽然反向传播步骤相同，但为了获得准确的权重估计，CAL 可能需要较大的组采样量（Group Size）。

（完）
