---
title: "ONTO: A Token-Efficient Columnar Notation for LLM Input Optimization"
authors: Harshavardhanan Deekeswar
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17512v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 针对大模型输入输出的Token高效列式表示法与JSON对比
tldr: 提出ONTO列式表示法，旨在减少大语言模型处理结构化数据时相比JSON产生的Token开销。
selection_source: fresh_fetch
motivation: 针对大模型输入输出的Token高效列式表示法与JSON对比。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Serialization formats designed for document interchange impose structural overhead that becomes prohibitive when large language models consume operational data at scale. A modest dataset of 1,000 IoT sensor readings serialized as JSON requires approximately 80,000 tokens - the majority spent on repeated field names, nested braces, and structural punctuation rather than semantic content. We present ONTO (Object Notation for Token Optimization), a columnar notation that declares field names once per entity and arranges values in pipe-delimited rows with indentation-based hierarchy. This schema-once, data-many design eliminates per-record key repetition while preserving human readability and nested structure support. Evaluation across three synthetic operational datasets demonstrates 46-51% token reduction versus JSON, with stable scaling from 100 to 1,000 records. Controlled inference benchmarks on Qwen2.5-7B show corresponding 5-10% latency improvement. Comprehension validation confirms no material degradation in LLM task accuracy across lookup, counting, extraction, and aggregation operations when format context is provided. Ablation analysis reveals that key repetition accounts for the majority of JSON overhead, with indentation costs in nested structures explaining the 4-percentage-point gap between flat and hierarchical data. ONTO occupies a previously unfilled position in the serialization landscape: columnar efficiency with hierarchical structure, optimized for LLM context windows rather than document interchange. Code and specification are available at https://github.com/harsh-aranga/onto.

---

## 论文详细总结（自动生成）

这是一份关于论文《ONTO: A Token-Efficient Columnar Notation for LLM Input Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的结构化数据序列化格式（如 JSON、YAML）在为大语言模型（LLM）提供大规模操作数据时，存在极高的 **Token 冗余**。
*   **研究动机**：JSON 等格式设计初衷是用于系统间的数据交换，强调自描述性，导致字段名（Keys）在每条记录中重复出现。例如，1000 条 IoT 传感器记录在 JSON 格式下约需 8 万个 Token，其中大部分消耗在重复的键名、括号和标点上，而非实际语义内容。这不仅增加了 API 成本，还占用了有限的上下文窗口并增加了推理延迟。

### 2. 方法论
*   **核心思想**：提出 **ONTO (Object Notation for Token Optimization)**，一种专为 LLM 输入优化的列式表示法。其遵循“**一次声明，多次数据**”（Schema-once, data-many）的原则。
*   **关键技术细节**：
    *   **列式声明**：实体名和字段名仅在文档开头声明一次。
    *   **管道符分隔**：使用 `|` 分隔不同记录的值，取消了引号、冒号和大括号。
    *   **缩进表示层级**：模仿 YAML 使用空格缩进（固定 4 空格）来表示嵌套结构，避免了嵌套括号的 Token 开销。
    *   **特殊符号**：使用 `^` 分隔数组元素；使用反引号（`` ` ``）强制转义字符串；使用 `[N]` 显式标注记录数量。
    *   **类型推断**：解析器自动推断整数、浮点数、布尔值和空值（两个管道符之间为空）。

### 3. 实验设计
*   **数据集/场景**：构建了三种合成数据集：
    1.  **IoT 遥测（嵌套）**：包含设备 ID、时间戳及嵌套的经纬度坐标。
    2.  **系统指标（扁平）**：服务器监控数据（CPU、内存等）。
    3.  **日志条目（扁平混合）**：包含时间戳、级别、消息等混合类型。
*   **Benchmark（对比方法）**：JSON、YAML。
*   **评估指标**：
    *   **Token 效率**：使用 `tiktoken` (cl100k_base 编码)。
    *   **推理延迟**：测量首 Token 时间（TTFT）和总推理时间。
    *   **理解准确度**：通过查找（Lookup）、计数（Counting）、提取（List）和聚合（Max）四类任务验证模型对格式的理解。

### 4. 资源与算力
*   **硬件**：AMD Ryzen 7 3700X (8核), 48GB RAM, **NVIDIA RTX 2070 SUPER (8GB)**。
*   **模型**：本地运行 **Qwen2.5-7B-Instruct** (4-bit 量化) 用于延迟测试；通过 API 调用 **GPT-4o-mini** 用于理解力验证。
*   **运行环境**：Ollama v0.20.7。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 100、500、1000 条记录的不同规模。
*   **重复性**：延迟测试和理解力测试均进行了 **20 次独立运行**（使用不同随机种子生成数据），共计 800 次 API 调用。
*   **消融实验**：详细分析了 Token 节省的来源（键名消除占主要地位，标点减少次之，缩进则带来少量额外开销）。
*   **充分性评价**：实验设计较为全面，不仅关注 Token 数量，还验证了模型在实际任务中的表现，确保了格式的实用性。

### 6. 主要结论与发现
*   **Token 节省显著**：相比 JSON，ONTO 在 1000 条记录规模下实现了 **46%–51% 的 Token 削减**。YAML 的节省仅为 1%–6%。
*   **延迟降低**：Token 的减少直接转化为 **5%–10% 的推理延迟提升**。
*   **理解力保持**：在提供简单的格式说明（Warm Prompt）后，LLM 在 ONTO 格式上的任务准确率与 JSON 持平，甚至在某些复杂指标查找任务中表现更好。
*   **成本构成**：键名重复是 JSON 最大的开销；在 ONTO 中，缩进是处理嵌套数据的主要成本（导致嵌套数据比扁平数据少省约 4% 的 Token）。

### 7. 优点
*   **极致高效**：通过列式重构大幅压缩了结构化数据的体积，直接降低了长上下文任务的成本。
*   **结构兼容**：不同于 CSV，ONTO 能够完美表达嵌套的层级关系。
*   **人类可读**：保持了良好的视觉对齐，方便开发者调试和检查。
*   **即插即用**：作为预处理层，无需重新训练模型，仅需在 Prompt 中加入少量格式说明。

### 8. 不足与局限
*   **数据同构限制**：要求同一实体下的所有记录必须具有相同的 Schema，不适用于高度异构的数据。
*   **合成数据偏差**：实验主要基于合成数据集，在真实世界复杂、脏乱的数据分布下的表现尚待验证。
*   **分词器依赖**：主要针对 OpenAI 的分词器进行了优化，在 Llama 或 Mistral 等使用不同分词算法的模型上，压缩率可能会有波动。
*   **嵌套深度**：未测试超过 2 层以上的深度嵌套结构，深层缩进可能会抵消部分 Token 节省优势。

（完）
