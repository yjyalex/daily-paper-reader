---
title: "DVAR: Adversarial Multi-Agent Debate for Video Authenticity Detection"
title_zh: DVAR：用于视频真实性检测的对抗性多智能体辩论
authors: "Hongyuan Qi, Feifei Shao, Ming Li, Hehe Fan, Jun Xiao"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16987v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于逻辑推理的多智能体辩论
tldr: 针对视频生成技术快速演进导致的检测泛化性难题，本文提出DVAR框架。该框架将视频真实性检测转化为多智能体辩论过程，通过“生成假设”与“自然机制”智能体的对抗性推理及奥卡姆剃刀原则（MDL）进行裁决。DVAR无需训练，结合动态知识库GenVideoKB，在保持高准确率的同时，显著提升了对未知生成架构的泛化能力和推理透明度。
selection_source: fresh_fetch
motivation: 传统视频检测方法过度依赖模式匹配，难以应对训练分布之外的新型生成技术，且缺乏推理的可解释性。
method: 构建多智能体对抗辩论框架，利用最小描述长度（MDL）量化逻辑负担，并结合动态知识库进行结构化取证推理。
result: DVAR在性能上媲美有监督的最先进方法，并在处理未见过的生成架构时展现出卓越的泛化性能。
conclusion: 该研究通过将检测任务转化为透明的逻辑辩论，为实现稳健且可解释的视频真实性评估提供了有效的新路径。
---

## 摘要
视频生成技术的快速演进给媒体取证带来了重大挑战，因为传统的检测方法往往难以泛化到其训练分布之外。为了解决这一问题，我们提出了 DVAR（基于辩论的视频真实性推理），这是一种无需训练的框架，它将视频检测重新定义为结构化的多智能体取证推理过程。DVAR 超越了模式匹配的范式，编排了“生成假设智能体”与“自然机制智能体”之间的竞争。通过多轮迭代的交叉质询，这些智能体针对异常证据辩护各自的解释，从而推动逻辑收敛，使真相在严苛的压力测试中浮现。为了裁定这些相互冲突的主张，我们通过最小描述长度（MDL）框架应用奥卡姆剃刀原则，定义了“解释成本”来量化每条推理路径的“逻辑负担”。此外，我们集成了 GenVideoKB，这是一个动态知识库，提供关于生成边界和失效模式的高层推理启发式知识。广泛的实验表明，DVAR 在与有监督的最先进方法竞争中表现出色，同时对未见的生成架构展现出卓越的泛化能力。通过将检测转化为透明的辩论，DVAR 为鲁棒的视频真实性评估提供了显式且可解释的推理轨迹。

## Abstract
The rapid evolution of video generation technologies poses a significant challenge to media forensics, as conventional detection methods often fail to generalize beyond their training distributions. To address this, we propose DVAR (Debate-based Video Authenticity Reasoning), a training-free framework that reformulates video detection as a structured multi-agent forensic reasoning process. Moving beyond the paradigm of pattern matching, DVAR orchestrates a competition between a Generative Hypothesis Agent and a Natural Mechanism Agent. Through iterative rounds of cross-examination, these agents defend their respective explanations against abnormal evidence, driving a logical convergence where the truth emerges from rigorous stress-testing. To adjudicate these conflicting claims, we apply Occam's Razor through the Minimum Description Length (MDL) framework, defining an Explanatory Cost to quantify the "logical burden" of each reasoning path. Furthermore, we integrate GenVideoKB, a dynamic knowledge repository that provides high-level reasoning heuristics on generative boundaries and failure modes. Extensive experiments demonstrate that DVAR achieves competitive performance against supervised state-of-the-art methods while exhibiting superior generalization to unseen generative architectures. By transforming detection into a transparent debate, DVAR provides explicit, interpretable reasoning traces for robust video authenticity assessment.

---

## 论文详细总结（自动生成）

### 论文总结：DVAR - 用于视频真实性检测的对抗性多智能体辩论

#### 1. 核心问题与整体含义
*   **研究背景**：随着 Sora 等视频生成技术的飞速发展，合成视频的逼真度极高，传统基于深度学习（CNN/Transformer）的检测方法面临严峻挑战。
*   **核心痛点**：
    1.  **泛化性差**：有监督模型过度依赖训练集的分布，遇到未见过的生成架构（OOD）时性能大幅下降。
    2.  **黑盒决策**：缺乏透明度，无法解释为何判定视频为假，且容易忽略明显的物理违和感。
*   **研究动机**：模拟人类取证专家的思维，将检测任务从“模式匹配”转变为“逻辑推理”，通过多智能体辩论来挖掘视频中的物理矛盾。

#### 2. 方法论
DVAR 提出了一种**无需训练（Training-free）**的结构化推理框架，核心流程包含四个阶段：
*   **证据发现（Evidence Discovery）**：利用多模态大模型（LVLM）提取场景语义并识别潜在的异常痕迹（如边缘抖动、物理不一致）。
*   **多智能体对抗辩论**：
    *   **生成假设智能体 (GHA)**：攻击方，试图将异常解释为生成模型的伪影（如扩散不稳定）。
    *   **自然机制智能体 (NMA)**：辩护方，试图将异常解释为自然物理现象（如滚动快门、风吹动、景深影响）。
    *   通过多轮交叉质询，迫使双方在压力测试下暴露逻辑漏洞。
*   **解释成本计算（Explanatory Cost）**：引入**最小描述长度（MDL）**原则（奥卡姆剃刀）。对于未能在辩论中直接分出胜负的痕迹，计算两种解释的 Token 长度。逻辑负担越重（解释越复杂/冗长）的假设越不可信。
*   **最终决策裁决（Arbiter）**：汇总所有痕迹的辩论记录和成本差异，给出最终判定、置信度及推理轨迹。
*   **GenVideoKB**：一个动态知识库，提供生成模型失效模式的高层启发式知识，辅助智能体进行专业取证。

#### 3. 实验设计
*   **数据集**：
    *   **Buster**：专注于长视频和分布外（OOD）泛化性评估。
    *   **GenVidBench**：大规模基准，涵盖多种生成器和复杂场景。
*   **Benchmark 与对比方法**：
    *   **有监督 SOTA**：3D ResNet, ViViT, DeMamba, BusterX++ 等。
    *   **大模型基线**：直接使用 GPT-5.4 提示（Direct Prompting）和思维链（CoT）。
*   **评估指标**：准确率（ACC）和 F1 分数。

#### 4. 资源与算力
*   **训练算力**：该方法为**无需训练**框架，因此不涉及模型训练阶段的 GPU 消耗。
*   **推理算力**：实验主要通过 API 调用实现（如 GPT-5.4, GPT-4o, Qwen-3.5-Plus）。
*   **具体细节**：文中未提及具体的本地 GPU 型号或推理总时长，但指出所有推理均在标准工作站上通过 API 完成，强调了其在实际应用中无需昂贵计算集群的优势。

#### 5. 实验数量与充分性
*   **实验规模**：在 Buster OOD 协议和 GenVidBench 的 4126 个视频子集上进行了广泛测试。
*   **消融实验**：系统地验证了证据发现、对抗辩论、成本计算和知识库（GenVideoKB）每个组件的有效性。
*   **鲁棒性测试**：更换了不同的底层大模型（GPT/Qwen），证明了框架的通用性。
*   **参数敏感性**：分析了帧率（FPS）和辩论轮数对结果的影响。
*   **充分性评价**：实验设计全面，对比了从传统 CNN 到最新大模型驱动的多种方法，逻辑严密，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能卓越**：DVAR 在无需训练的情况下，在 Buster 数据集上达到了 92.2% 的准确率，媲美甚至超越了经过大规模训练的有监督模型（BusterX++）。
*   **泛化能力强**：在面对未见过的生成模型时，DVAR 表现出极强的鲁棒性，证明了基于物理常识的推理比模式匹配更可靠。
*   **推理必要性**：实验证明直接使用 LVLM 进行检测效果极差（ACC 仅约 52%），而 DVAR 的结构化辩论框架是激发大模型取证能力的关键。

#### 7. 优点
*   **透明与可解释**：提供详细的辩论记录，用户可以清晰看到判定“假”的具体物理依据。
*   **奥卡姆剃刀的应用**：巧妙地将 MDL 引入取证领域，通过量化“解释成本”解决了异常归因的模糊性。
*   **动态进化**：通过 GenVideoKB，框架可以随着新生成技术的出现而快速更新知识，无需重新训练模型。

#### 8. 不足与局限
*   **推理成本高**：多轮辩论和多智能体交互导致 Token 消耗显著增加（约为直接提示的 3 倍），可能带来较高的 API 费用和延迟。
*   **简单场景局限**：在动作极少、背景单一或时长极短的视频中，可能因缺乏足够的“异常痕迹”而导致推理失效。
*   **底层依赖**：检测精度高度依赖于底层 LVLM 的视觉感知能力，若模型无法识别细微的视觉伪影，后续辩论将成为“空中楼阁”。

（完）
