---
title: Efficient Federated RLHF via Zeroth-Order Policy Optimization
title_zh: 基于零阶策略优化的高效联邦 RLHF
authors: "Deyi Wang, Qining Zhang, Lei Ying"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17747v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过策略优化实现高效联邦RLHF
tldr: 本文针对资源受限边缘设备在联邦学习环境下的RLHF难题，提出了Par-S$^2$ZPO算法。该算法采用基于二进制扰动的零阶优化技术，显著降低了通信、计算和内存开销。理论分析表明，该算法在样本复杂度上与中心化方法相当，且策略更新迭代更快。实验证明其在MuJoCo任务上优于传统的FedAvg-RLHF，为高效联邦强化学习提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决边缘设备在联邦强化学习（RLHF）中面临的计算、内存和通信资源受限问题。
method: 提出Par-S$^2$ZPO算法，利用带二进制扰动的分块符号零阶优化技术实现高效策略更新。
result: 理论证明了算法的收敛性，且实验显示其在多个MuJoCo任务上的表现优于基于FedAvg的RLHF方法。
conclusion: Par-S$^2$ZPO是一种适用于资源受限环境的高效联邦RLHF框架，兼具理论优势与实证性能。
---

## 摘要
本文研究了资源受限智能体（如边缘设备）在联邦学习环境下的基于人类反馈的强化学习（RLHF）。我们提出了一种高效的联邦 RLHF 算法，命名为分区、基于符号的随机零阶策略优化（Par-S$^2$ZPO）。该算法基于带有二元扰动的零阶优化，在设计上实现了较低的通信、计算和内存复杂度。我们的理论分析确立了 Par-S$^2$ZPO 收敛速度的上界，揭示了其在样本复杂度方面与中心化对应算法同样高效，但在策略更新迭代方面收敛更快。实验结果表明，在四个 MuJoCo 强化学习任务中，该算法的性能优于基于 FedAvg 的 RLHF。

## Abstract
This paper considers reinforcement learning from human feedback in a federated learning setting with resource-constrained agents, such as edge devices. We propose an efficient federated RLHF algorithm, named Partitioned, Sign-based Stochastic Zeroth-order Policy Optimization (Par-S$^2$ZPO). The algorithm is built on zeroth-order optimization with binary perturbation, resulting in low communication, computation, and memory complexity by design. Our theoretical analysis establishes an upper bound on the convergence rate of Par-S$^2$ZPO, revealing that it is as efficient as its centralized counterpart in terms of sample complexity but converges faster in terms of policy update iterations. Our experimental results show that it outperforms a FedAvg-based RLHF on four MuJoCo RL tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Par-S$^2$ZPO** 的算法，旨在解决在资源受限的边缘设备上进行联邦强化学习（RLHF）的高开销问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和机器人技术的发展，基于人类反馈的强化学习（RLHF）已成为微调模型的关键。然而，在**联邦学习（FL）**场景下，现有的 RLHF 方法面临严峻挑战：
*   **通信瓶颈**：传输数十亿参数的梯度需要极高带宽。
*   **计算与内存限制**：边缘设备（如手机、嵌入式设备）难以支撑复杂的梯度计算和大规模模型存储。
*   **隐私与能耗**：频繁的参数交换会迅速耗尽电池并增加隐私泄露风险。
该研究动机在于设计一种既能利用分布式人类反馈，又对硬件资源极其友好的联邦 RLHF 框架。

### 2. 方法论：Par-S$^2$ZPO 算法
论文提出了 **Par-S$^2$ZPO**（Partitioned, Sign-based Stochastic Zeroth-order Policy Optimization），其核心思想结合了以下技术：
*   **零阶优化（Zeroth-Order Optimization）**：不计算梯度，而是通过扰动策略参数并观察目标函数（人类偏好）的变化来估算梯度方向。
*   **二元扰动（Binary Perturbation）**：使用 Rademacher 分布（即扰动项仅为 $+1$ 或 $-1$），而非高斯分布。这使得扰动向量的存储和传输仅需 1 bit/参数。
*   **参数分区（Parameter Partitioning）**：将 $d$ 维参数划分为 $K$ 个子集，每个智能体仅负责扰动和评估其分配到的子集。这显著降低了单个智能体的内存占用。
*   **1-bit 反馈机制**：智能体通过本地人类偏好面板比较原始策略与扰动策略，仅向服务器发送一个二进制符号（$\hat{O}_k \in \{-1, 1\}$），表示该扰动方向是否有利。
*   **更新公式**：服务器汇总所有智能体的符号反馈，按 $\theta_{t+1} = \theta_t + \alpha_t \sum_{k=1}^K \hat{O}_{t,k} v_{t,k}$ 进行全局更新。

### 3. 实验设计
*   **实验场景**：使用了经典的 **MuJoCo** 连续控制强化学习基准测试，包括四个任务：*Half Cheetah*、*Hopper*、*Swimmer* 和 *Walker2D*。
*   **Benchmark（基准方法）**：
    *   **FedAvg-S$^2$ZPO**：基于 FedAvg 框架的零阶策略优化方法（每个智能体处理全量参数并取平均）。
    *   **消融对比**：对比了二元扰动与高斯扰动的性能差异。
*   **评估指标**：累积奖励（Accumulated Rewards）随迭代次数或样本复杂度的变化。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   实验设置提到：策略网络为具有两个 64 单元隐藏层的多层感知机（MLP），这属于轻量级模型，通常在单张消费级 GPU 或 CPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个环境分别进行了实验，每个实验重复了 **20 次独立试验**并报告了标准差，具有较好的统计显著性。
*   **充分性**：
    *   进行了智能体数量 $K$（1, 5, 10, 15）的敏感性分析。
    *   探讨了轨迹批次大小 $D$ 对收敛的影响（权衡评估准确性与迭代次数）。
    *   对比了不同扰动类型。
*   **评价**：实验设计较为充分，涵盖了算法核心参数的讨论，但在模型规模上仅限于小型神经网络，未涉及 LLM 等大规模参数场景。

### 6. 主要结论与发现
*   **等效性发现**：理论和实验均证明，在总样本量相同的情况下，多智能体联邦系统的效率与中心化系统相当，且 $K$ 个智能体并行更新不同分区能加快迭代收敛。
*   **性能优越性**：Par-S$^2$ZPO 在所有 MuJoCo 任务中均优于基于 FedAvg 的方法，且通信开销降低了 $K$ 倍。
*   **资源友好**：通过参数分区，智能体的额外内存需求减少了约 40%（在 $K=5$ 时），且无需存储梯度。
*   **扰动鲁棒性**：二元扰动在性能上与高斯扰动几乎无异，但更易于实现和传输。

### 7. 优点
*   **极高的通信效率**：通过符号化和二元扰动，将梯度传输压缩到了极致（1 bit/参数）。
*   **理论支撑完备**：提供了收敛速度的上界证明，并定义了“可区分性”（Distinguishability）来量化人类反馈的局限性。
*   **架构创新**：参数分区（Partitioning）巧妙地解决了边缘设备内存不足的问题，实现了真正的分布式微调。

### 8. 不足与局限
*   **任务复杂度有限**：实验仅限于 MuJoCo 机器人控制任务，尚未在真正的大语言模型（LLM）上验证其在处理数十亿参数时的表现。
*   **人类反馈模拟**：实验中的人类反馈是通过预设的奖励函数加噪声模拟的，实际复杂的人类主观偏好可能带来更大的方差。
*   **应用限制**：零阶优化在参数维度极高时（如超大规模模型）可能面临收敛变慢的问题，尽管论文在理论上给出了优化，但实际扩展性仍待验证。

（完）
