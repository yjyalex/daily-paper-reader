---
title: "Hive: A Multi-Agent Infrastructure for Algorithm- and Task-Level Scaling"
title_zh: Hive：一种用于算法级与任务级扩展的多智能体基础设施
authors: "Zizhang Luo, Yuhao Luo, Youwei Xiao, Yansong Xu, Runlin Guo, Yun Liang"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17353v1"
tags: ["query:mas-gen"]
score: 10.0
evidence: 用于任务级扩展的多智能体基础设施
tldr: 针对大语言模型智能体系统在算法和任务层面扩展性不足的问题，本文提出了 Hive 基础设施。Hive 通过描述性前端定义智能体行为，并在后端引入 Logits Cache 机制以消除推理路径间的冗余计算，同时采用智能体感知调度优化资源分配。该系统显著提升了多智能体协作的推理效率，在重采样中实现最高 1.76 倍加速，并大幅降低了缓存缺失率，为复杂智能体系统的扩展提供了新方案。
selection_source: fresh_fetch
motivation: 现有基础设施在算法层面存在跨路径计算冗余，且在任务层面缺乏对多智能体协作的感知，限制了系统的扩展潜力和资源利用率。
method: 提出 Hive 框架，通过 Logits Cache 复用冗余采样路径的中间结果，并利用智能体感知调度根据智能体贡献优化计算与 KV 缓存资源分配。
result: "实验显示 Logits Cache 在重采样中实现 1.11-1.76 倍加速，智能体感知调度将热点缓存缺失率降低了 33%-51%。"
conclusion: Hive 通过在基础设施层解决算法冗余和调度优化问题，有效支撑了多智能体系统在算法和任务层面的高效扩展。
---

## 摘要
大语言模型正越来越多地被部署为复杂的智能体系统，并随任务复杂度的增加而扩展。尽管先前的工作已广泛探索了模型级和系统级扩展，但算法级和任务级扩展在很大程度上仍未得到解决，这限制了智能体系统的全部潜力。在算法层面，分配额外的推理时计算可以增强工作流能力，但会引入跨路径冗余，即多个推理分支之间存在重叠计算。在任务层面，复杂任务可以分解为子问题并委派给多个智能体，以提高可扩展性和并行性。然而，现有基础设施的调度机制无法感知多个智能体的存在，从而错失了优化资源分配的机会。我们提出了 Hive，这是一种支持算法级和任务级扩展的多智能体基础设施。Hive 具有一个描述前端，用于捕获每个智能体的行为并支持测试时扩展算法。利用这一规范，我们的后端引入了两个关键机制：Logits Cache，通过在冗余采样路径中重用中间 Logits 来缓解算法层面的跨路径冗余；以及智能体感知调度（Agent-Aware Scheduling），根据任务层面的智能体贡献高效分配计算和 KV 缓存资源。实验表明，Logits Cache 在重采样方面实现了平均 1.11 倍至 1.76 倍的加速，而智能体感知调度将热点缺失率降低了 33% 至 51%。

## Abstract
Large language models are increasingly deployed as complex agentic systems that scale with task complexity. While prior work has extensively explored model- and system-level scaling, algorithm- and task-level scaling remain largely unaddressed, constraining the full potential of agentic systems. At the algorithm level, allocating additional inference-time computation can enhance workflow capacity but introduces cross-path redundancy: overlapping computations across multiple reasoning branches. At the task level, complex tasks can be decomposed into subproblems and delegated across multiple agents for improved scalability and parallelism. However, existing infrastructures' scheduling is unaware of the existence of multiple agents, missing opportunities to optimize resource allocation.   We propose Hive, a multi-agent infrastructure that enables algorithm- and task-level scaling. Hive features a description frontend that captures per-agent behavior and supports test-time scaling algorithms. Leveraging this specification, our backend introduces two key mechanisms: Logits Cache that reuses intermediate logits across redundant sampling paths to mitigate cross-path redundancy at the algorithm level, and Agent-Aware Scheduling that efficiently allocates compute and KV-cache resources according to agent contributions at the task level. Experiments show that Logits Cache achieves an average speedup of $1.11\times$-$1.76\times$ for re-sampling, and Agent-Aware Scheduling reduces the hotspot miss rate by $33\%$-$51\%$.

---

## 论文详细总结（自动生成）

### 论文总结：Hive —— 算法与任务级扩展的多智能体基础设施

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从简单的问答引擎转向复杂的智能体（Agent）系统，其性能扩展已不再局限于模型规模（Model-level）和硬件算力（System-level）。论文指出，当前的推理基础设施在以下两个维度存在严重瓶颈：
*   **算法级扩展（Algorithm-level Scaling）**：如“思维树”（Tree-of-Thoughts）等测试时扩展算法在搜索过程中会产生大量重复的推理分支。现有系统将其视为独立请求，导致了严重的**跨路径冗余（Cross-Path Redundancy）**，即重复计算相同的中间概率分布。
*   **任务级扩展（Task-level Scaling）**：复杂任务被分解为多个异构智能体协作。现有推理引擎无法感知智能体的角色差异，采用统一的资源调度策略（如 LRU 缓存淘汰），导致关键智能体的 KV 缓存被频繁置换，降低了整体效率。

#### 2. 方法论：核心思想与关键技术
Hive 提出了一个全栈式基础设施，包含描述性前端和优化后端：
*   **描述性前端**：采用类似 Python 协程（Coroutine）的抽象，允许开发者定义智能体的异步行为。引入 **Supervisor（监督者）** 机制，将测试时扩展算法（如 ToT）与智能体逻辑解耦。
*   **Logits Cache（算法级优化）**：
    *   **核心思想**：不仅缓存 KV 状态，还缓存解码过程中的 Logits（概率分布）。
    *   **技术细节**：当系统检测到重采样请求时，通过 **Step-wise Sampling（逐步采样）** 或 **Hotspot Sampling（热点采样）** 复用缓存的 Logits。热点采样通过计算熵和 Top-1 概率来识别不确定性高的位置，仅在这些“热点”进行重采样，从而跳过大量确定性步骤的前向计算。
*   **智能体感知调度（任务级优化）**：
    *   **贡献度建模**：借鉴博弈论中的 **Shapley Value**，综合评估智能体的**内在效用**（调用频率、吞吐量、并发度）和**协作效用**（跨智能体缓存复用率）。
    *   **资源分配**：根据贡献度评分动态调整 GPU 内存中 KV 缓存的驻留优先级，优先保留高贡献智能体的状态。

#### 3. 实验设计
*   **场景与数据集**：
    *   **算法级测试**：使用 **24 点游戏数据集（24-Game）**，模拟 Tree-of-Thoughts 的搜索过程。
    *   **任务级测试**：基于硬件验证（RTL 自动修复）的多智能体工作流 **R3A**，包含 Decision、Patcher、Viewer、Summary、Searcher 五个角色。
*   **Benchmark 与对比方法**：
    *   **基准**：SGLang 推理引擎。
    *   **对比对象**：禁用了 Logits Cache 的标准重采样方法，以及标准的 LRU（最近最少使用）缓存淘汰策略。

#### 4. 资源与算力
*   **硬件环境**：一台配备 **8 张 NVIDIA A100 GPU** 的服务器，CPU 为 Intel Xeon Gold 6348。
*   **软件栈**：CUDA 11.5, PyTorch 2.9.1, SGLang 0.5.6。
*   **模型**：主要使用 **Qwen3-8B** 作为基础模型进行评估。
*   **训练/推理时长**：文中未明确提及具体的总运行耗时，但实验基于 100 个 24 点游戏实例和 21 轮多智能体协作迭代进行。

#### 5. 实验数量与充分性
*   **实验规模**：针对 Logits Cache 进行了不同温度（Temperature 0.1-0.9）下的消融实验；针对调度器进行了 21 轮次的动态优先级追踪实验。
*   **充分性评价**：实验设计较为合理，涵盖了性能加速比（TPS）、缓存命中率、热点缺失率等关键指标。通过硬件验证这一真实复杂场景验证了多智能体调度的有效性。然而，实验主要集中在 8B 规模的模型上，对于超大规模模型（如 70B+）的扩展性表现虽有理论推导，但缺乏直接数据支撑。

#### 6. 主要结论与发现
*   **Logits Cache 效果**：在重采样任务中，逐步采样实现平均 **1.11x** 加速，热点采样实现平均 **1.76x** 加速。在低温度设置下，加速效果尤为显著。
*   **调度器效果**：智能体感知调度将热点 KV 缓存的缺失率降低了 **33% – 51%**，并减少了约 **19% – 30%** 的无效令牌置换。
*   **异构性验证**：实验证实了多智能体系统中存在明显的负载不均，通过识别并保护“核心智能体”的资源，能显著提升系统整体吞吐。

#### 7. 优点
*   **创新性维度**：首次系统性地提出了“算法级”和“任务级”扩展的概念，并给出了相应的工程实现。
*   **Logits 复用**：突破了传统仅缓存 KV 的局限，利用 Logits 缓存减少了昂贵的前向解码计算。
*   **博弈论引入**：将 Shapley Value 应用于智能体资源调度，为解决多智能体系统中的资源竞争提供了数学支撑。

#### 8. 不足与局限
*   **模型规模限制**：实验主要基于 8B 模型，在大模型高延迟环境下，Logits Cache 的内存交换开销（从内存到 GPU）是否会抵消计算节省有待进一步验证。
*   **应用领域偏差**：主要测试场景为硬件验证和数学游戏，这些任务具有较强的结构性和逻辑性，对于创意写作或开放式对话等随机性极强的智能体任务，Logits Cache 的收益可能会大幅下降。
*   **系统复杂性**：引入 Shapley Value 计算和 Logits 缓存管理增加了后端的复杂度和内存管理负担。

（完）
