---
title: "Small Model as Master Orchestrator: Learning Unified Agent-Tool Orchestration with Parallel Subtask Decomposition"
title_zh: 小模型作为主编排器：通过并行子任务分解学习统一的智能体-工具编排
authors: "Wenzhen Yuan, Wutao Xiong, Fanchen Yu, Shengji Tang, Ting Liu, Tao Chen, Peng Ye, Yuzhuo Fu, Wanli Ouyang, Lei Bai"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17009v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体系统中的并行子任务分解与协作
tldr: 本研究针对多智能体系统（MAS）中静态工作流、串行调度及接口协议不统一导致的复杂性与扩展性差等问题，提出了Agent-as-Tool统一并行编排范式。通过将智能体与工具抽象为标准化的动作空间，并训练轻量级编排器ParaManager，实现了状态感知的并行子任务分解与异步执行。结合SFT与强化学习训练，该方法在多个基准测试中表现优异，且具备极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有多智能体系统受限于静态工作流、串行调度及异构接口协议，导致系统复杂且难以扩展。
method: 提出Agent-as-Tool范式将智能体与工具统一化，并训练ParaManager编排器实现并行子任务分解与异步执行。
result: 实验证明ParaManager在多个基准测试中表现强劲，并在面对未见过的模型池时展现出稳健的泛化性。
conclusion: 通过轻量级编排器实现统一的并行任务调度，能有效提升多智能体系统的效率、鲁棒性与可扩展性。
---

## 摘要
多智能体系统（MAS）通过协调不同的智能体和外部工具，在解决复杂问题方面展现出明显优势。然而，大多数现有的编排方法依赖于静态工作流或串行智能体调度，并进一步受到工具与智能体之间异构接口协议的限制。这导致了较高的系统复杂性和较差的可扩展性。为缓解这些问题，我们提出了 Agent-as-Tool，这是一种统一的并行编排范式，它通过协议规范化和显式状态反馈，将智能体和工具抽象为一个标准化的、可学习的动作空间。基于该范式，我们训练了一个轻量级编排器 ParaManager，它将规划决策与子任务求解相解耦，实现了状态感知的并行子任务分解、委派和异步执行。在训练方面，我们采用了两阶段的 ParaManager 训练流水线。它通过引入具备恢复机制的有监督微调（SFT）轨迹来提高鲁棒性，并进一步应用强化学习（RL）以在任务成功率、协议合规性、多样性和推理效率之间实现最佳平衡。实验表明，ParaManager 在多个基准测试中取得了强劲性能，并在未见过的模型池下表现出稳健的泛化能力。

## Abstract
Multi-agent systems (MAS) demonstrate clear advantages in tackling complex problems by coordinating diverse agents and external tools. However, most existing orchestration methods rely on static workflows or serial agent scheduling, and are further constrained by heterogeneous interface protocols between tools and agents. This leads to high system complexity and poor extensibility. To mitigate these issues, we propose Agent-as-Tool, a unified parallel orchestration paradigm that abstracts both agents and tools into a standardized, learnable action space with protocol normalization and explicit state feedback. Building on this paradigm, we train a lightweight orchestrator, ParaManager, which decouples planning decisions from subtask solving, enabling state-aware parallel subtask decomposition, delegation, and asynchronous execution. For training, we adopt a two-stage ParaManager training pipeline. It improves robustness by incorporating supervised fine-tuning (SFT) trajectories equipped with recovery mechanisms, and further applies reinforcement learning (RL) to achieve an optimal balance among task success, protocol compliance, diversity, and reasoning efficiency. Experiments show that ParaManager achieves strong performance across multiple benchmarks and exhibits robust generalization under unseen model pools.

---

## 论文详细总结（自动生成）

这是一份关于论文《Small Model as Master Orchestrator: Learning Unified Agent-Tool Orchestration with Parallel Subtask Decomposition》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多智能体系统（MAS）在处理复杂任务时具有显著优势，但现有方法存在三大痛点：
*   **编排僵化**：大多依赖静态工作流或预定义模板，无法根据中间结果动态调整。
*   **效率低下**：通常采用串行调度，导致推理延迟高，且单路径推理容错性差。
*   **协议异构**：不同工具和智能体之间的接口协议不统一，增加了系统复杂性和扩展难度。
**研究动机**：探索如何利用轻量级小模型作为“主编排器”，通过统一的协议和并行调度，实现高效、鲁棒的智能体-工具协作。

### 2. 方法论
论文提出了 **Agent-as-Tool** 范式和 **ParaManager** 编排器：
*   **核心思想**：将所有智能体和外部工具统一抽象为标准化的动作空间。每个智能体被视为一个具有标准输入输出协议和显式执行状态（如 OK, PARSE_ERR, TIMEOUT）的“工具”。
*   **关键技术细节**：
    *   **ParaManager**：基于 Qwen3-4B 训练的轻量级模型，负责高层规划、子任务分解和委派，而具体的子任务由被调用的智能体/工具在隔离环境中执行。
    *   **并行编排**：支持单轮触发多个并行调用，通过独立路径验证减少单点故障风险。
    *   **两阶段训练流水线**：
        1.  **有监督微调 (SFT)**：使用包含“可恢复错误”的轨迹数据进行训练，教会模型利用错误反馈进行自我修复。
        2.  **强化学习 (RL)**：采用 GRPO 算法，设计了包含任务准确率（Accuracy）、格式合规性（Format）、多样性（Diversity）和效率（Efficiency）的综合奖励函数。

### 3. 实验设计
*   **数据集/场景**：
    *   **领域内（数学）**：AIME24, AIME25, AMC, HMMT25。
    *   **领域外（通用/代码）**：GPQA, MMLU-pro, LiveCodeBench (v5/v6)。
*   **对比方法**：
    *   **基础模型**：Qwen3-30B, GPT-OSS-20B 等（含多数投票和自我改进）。
    *   **静态工作流**：Router, Meta Agent Search, EvoFlow。
    *   **串行编排**：Puppeteer, ToolOrchestra。
*   **泛化性测试**：在完全未见过的模型池（如 Gemini-2.5-flash, Qwen3-235B）上测试编排策略的迁移能力。

### 4. 资源与算力
*   **硬件**：使用了 **12 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   SFT 阶段：200 步，Batch Size 为 32。
    *   RL 阶段：50 步，Batch Size 为 16，每条指令采样 8 条轨迹（G=8）。
    *   训练框架：使用 Verl 框架实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个主流基准测试，进行了详尽的对比实验。
*   **消融实验**：针对 SFT 的必要性、并行 vs 串行、单模型 vs 多模型池、奖励函数设计等进行了多组消融分析。
*   **充分性评价**：实验设计非常充分且客观。不仅关注准确率，还深入分析了 Token 消耗、延迟（Latency）以及在模型池切换下的泛化表现，证明了方法的稳健性。

### 6. 主要结论与发现
*   **小模型胜大模型**：经过训练的 4B 参数 ParaManager 在综合性能上超过了 30B 甚至更大型号的单模型及复杂的静态工作流。
*   **并行优势**：并行调度不仅提高了推理准确率（通过交叉验证），还显著降低了端到端延迟。
*   **训练关键点**：SFT 阶段引入错误恢复轨迹对防止 RL 训练崩溃至关重要；效率奖励能有效抑制模型过度调用工具。
*   **强泛化性**：编排器学到的是通用的调度逻辑，即使更换底层执行智能体，依然能保持高性能。

### 7. 优点
*   **范式统一**：Agent-as-Tool 简化了 MAS 的架构，使其更易于扩展和维护。
*   **状态感知**：通过显式的执行状态反馈，实现了闭环的自我修复能力。
*   **效率平衡**：在提升性能的同时，通过奖励函数严格控制了计算成本和 Token 消耗。

### 8. 不足与局限
*   **依赖总结模型**：最终答案的生成仍依赖于一个冻结的总结模型（Ms），编排器本身不直接生成最终文本。
*   **数据依赖**：训练 ParaManager 需要高质量的编排轨迹，这通常依赖于更强大的模型进行数据生成。
*   **奖励调优复杂**：RL 阶段涉及四个维度的奖励平衡，可能需要精细的超参数调节。

（完）
