---
title: Visual Preference Optimization with Rubric Rewards
title_zh: 基于评分细则奖励的视觉偏好优化
authors: "Ya-Qi Yu, Fangyu Hong, Xiangyang Qu, Hao Wang, Gaojie Wu, Qiaoyu Luo, Nuo Xu, Huixin Wang, Wuheng Xu, Yongxin Liao, Zihao Chen, Haonan Li, Ziming Li, Dezhi Peng, Minghui Liao, Jihao Wu, Haoyu Ren, Dandan Tu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13029v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对细粒度推理的偏好优化
tldr: 针对多模态任务中细粒度视觉推理偏好数据质量不足的问题，本文提出了rDPO框架。该方法通过为每个图像-指令对构建特定评分量表（Rubric），利用清单式准则对模型响应进行细粒度评估。通过离线构建指令-量表池并应用于在线策略数据过滤，rDPO在奖励建模和下游基准测试中显著提升了模型性能，证明了实例级准则反馈在视觉偏好优化中的有效性。
selection_source: fresh_fetch
motivation: 现有的偏好优化方法依赖粗粒度信号或离线扰动，难以捕捉多模态任务中复杂的细粒度视觉推理差异。
method: 提出rDPO框架，通过为每个实例构建包含核心与附加准则的清单式评分量表，对不同策略生成的响应进行细粒度评分和过滤。
result: 实验显示，基于量表的提示显著提升了判别模型性能，且rDPO在多个公开基准测试中大幅超越了基于结果过滤的基线模型。
conclusion: 结合在线策略数据构建与实例级准则反馈，是提升视觉偏好优化效果的关键路径。
---

## 摘要
直接偏好优化 (DPO) 的有效性取决于偏好数据能否反映多模态任务中关键的质量差异。现有的流程通常依赖于离策 (off-policy) 扰动或粗粒度的基于结果的信号，这些方法并不适用于细粒度的视觉推理。我们提出了 rDPO，这是一种基于特定实例评分细则 (rubrics) 的偏好优化框架。对于每个图像-指令对，我们创建了一个包含基本准则和附加准则的清单式评分细则，用于对来自任何可能策略的响应进行评分。指令-评分细则池是离线构建的，并在构建策略内 (on-policy) 数据期间重复使用。在公开的奖励建模基准测试中，基于评分细则的提示大幅提升了 30B-A3B 评测模型的表现，使其接近 GPT-5.4。在公开的下游基准测试中，基于评分细则的过滤将宏平均分提高到 82.69，而基于结果的过滤则从 81.14 下降到 75.82。在综合基准测试中评估可扩展性时，rDPO 达到了 61.01，显著优于风格受限的基准模型 (52.36)，并超过了 59.48 的基础模型。总之，这些结果表明，视觉偏好优化受益于将策略内数据构建与特定实例的准则级反馈相结合。

## Abstract
The effectiveness of Direct Preference Optimization (DPO) depends on preference data that reflect the quality differences that matter in multimodal tasks. Existing pipelines often rely on off-policy perturbations or coarse outcome-based signals, which are not well suited to fine-grained visual reasoning. We propose rDPO, a preference optimization framework based on instance-specific rubrics. For each image-instruction pair, we create a checklist-style rubric of essential and additional criteria to score responses from any possible policies. The instruction-rubric pool is built offline and reused during the construction of on-policy data. On public reward modeling benchmarks, rubric-based prompting massively improves a 30B-A3B judge and brings it close to GPT-5.4. On public downstream benchmarks, rubric-based filtering raises the macro average to 82.69, whereas outcome-based filtering drops it to 75.82 from 81.14. When evaluating scalability on a comprehensive benchmark, rDPO achieves 61.01, markedly outperforming the style-constrained baseline (52.36) and surpassing the 59.48 base model. Together, these results show that visual preference optimization benefits from combining on-policy data construction with instance-specific criterion-level feedback.

---

## 论文详细总结（自动生成）

这篇论文由华为技术有限公司的研究团队提出，介绍了一种名为 **rDPO**（Visual Preference Optimization with Rubric Rewards）的视觉偏好优化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现有的多模态大模型（VLM）偏好优化（如 DPO）主要依赖于“离策”（off-policy）数据或粗粒度的“基于结果”的信号（如仅判断最终答案对错）。这种方式难以捕捉视觉推理中的细微错误、幻觉或逻辑完整性。
*   **研究动机**：为了提升 VLM 在复杂视觉任务中的对齐效果，需要一种能够提供**细粒度、实例相关且具有解释性**的奖励信号。作者认为，通过为每个任务构建专属的“评分细则”（Rubric），可以更精准地指导模型优化。

### 2. 方法论：rDPO 框架
rDPO 的核心思想是将偏好对的构建从“黑盒评分”转变为“基于清单的细粒度评估”。
*   **核心流程**：
    1.  **种子挖掘（Seed Mining）**：利用多个中等规模模型（如 Qwen2.5-VL, InternVL3.5）进行预测，筛选出模型间存在显著分歧的挑战性指令。
    2.  **细则起草（Rubric Drafting）**：利用前沿推理模型为每个指令生成 JSON 格式的评分细则。细则包含：
        *   **基本准则（Essential）**：回答必须满足的核心要点。
        *   **附加准则（Additional）**：辅助事实、推理步骤或补充知识。
        *   **三元组结构**：每个条目包含“准则描述”、“参考答案”和“权重（1-3分）”。
    3.  **偏好数据策划（Preference Curation）**：
        *   **策略内采样**：从目标模型中采样多个响应。
        *   **细则评分**：VLM-as-a-Judge 根据细则对每个准则给出 0/0.5/1 分。
        *   **配对挖掘**：根据总分差异、基本准则达标情况等规则筛选出高质量的“胜出者-失败者”对。
*   **优化目标**：采用混合偏好优化（MPO），在 DPO 损失基础上增加辅助 SFT 损失，并支持**迭代式 DPO**（将数据分片，逐步更新参考模型）。

### 3. 实验设计
*   **奖励建模基准（RM Benchmarks）**：在 MM-RewardBench、VL-RewardBench、VisionArena-Battle 等 5 个公开偏好基准上评估“评分细则提示法”对评测模型（Judge）的影响。
*   **下游任务验证**：
    *   **方法验证**：在 AI2D、ChartQA、M3CoT 的测试集上对比“基于结果过滤”与“基于细则过滤”的效果。
    *   **规模验证**：在华为内部的大规模综合基准上测试 rDPO 的扩展性。
*   **对比方法**：对比了 GPT-5.4、Claude 4.6、Gemini 系列等闭源模型，以及 Qwen3-VL、IXC-2.5-Reward 等开源模型。

### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了使用 AdamW 优化器，学习率为 $1 \times 10^{-6}$，DPO 的 $\beta$ 设为 0.5。在方法验证阶段 Batch Size 为 128，规模验证阶段为 512。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 **300K** 指令-细则对的数据池。
*   **充分性**：实验设计非常全面。不仅验证了 rDPO 作为“评测器”的准确性，还验证了其作为“训练框架”对下游任务的提升。
*   **消融实验**：针对迭代优化、基本准则限制、奖励边际（Margin）、多样性控制、KL 惩罚系数（$\beta$）以及 SFT 缩放系数（$\alpha$）等多个维度进行了详尽的消融研究，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **评测能力提升**：仅通过引入特定实例的评分细则，30B 规模的开源模型作为 Judge 的表现就能显著提升，甚至在多个指标上逼近 GPT-5.4。
*   **rDPO 优于传统过滤**：在相同策略内数据下，基于结果的过滤（Outcome-based）有时会导致性能下降（从 81.14 降至 75.82），而 rDPO 能稳步提升性能（升至 82.69）。
*   **迭代优化的必要性**：多轮迭代 DPO 比单次 DPO 效果更好，有助于校准隐式奖励。
*   **策略内数据的重要性**：实验证明 DPO 对分布偏移非常敏感，使用目标模型自身的采样数据（On-policy）是成功的关键。

### 7. 优点（亮点）
*   **细粒度透明性**：将模糊的偏好判断拆解为可解释的原子准则，解决了 VLM 训练中“奖励黑盒”的问题。
*   **实例级定制**：不同于以往使用固定模板的方法，rDPO 为每个问题量身定制评分标准，更适合复杂的视觉推理。
*   **模型无关性**：构建的“指令-细则池”是通用的，可以复用于不同规模或架构的目标模型优化。

### 8. 不足与局限
*   **间接利用信号**：目前细则反馈仅用于筛选偏好对，并未直接进入优化循环（如作为强化学习的实时奖励信号）。
*   **细则生成成本**：虽然是离线构建，但生成高质量、无偏见的评分细则对前沿推理模型（如 GPT-4o 等）有较高依赖。
*   **在线优化缺失**：作者提到未来需要探索将细则反馈与在线强化学习（如 GRPO）进行更紧密的结合。

（完）
