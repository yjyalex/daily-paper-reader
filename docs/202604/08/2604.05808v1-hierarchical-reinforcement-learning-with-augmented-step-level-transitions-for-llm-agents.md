---
title: Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents
title_zh: 面向大语言模型智能体的增强步级转移分层强化学习
authors: "Shuai Zhen, Yanhua Yu, Ruopei Guo, Nan Cheng, Yang Deng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05808v1"
tags: ["query:token"]
score: 9.0
evidence: 具有步骤级转换的LLM智能体分层强化学习
tldr: 针对LLM智能体在复杂决策任务中因依赖长交互历史导致的高计算成本和扩展性受限问题，本文提出STEP-HRL框架。该框架采用分层强化学习，通过全局子任务进度和局部交互摘要构建增强的步级状态转移，使智能体仅需单步信息即可学习。实验证明，该方法在ScienceWorld和ALFWorld上显著提升了性能与泛化能力，并大幅降低了Token消耗。
selection_source: fresh_fetch
motivation: 解决LLM智能体因依赖冗长交互历史而导致的计算开销大和长序列处理难的问题。
method: 提出STEP-HRL框架，通过分层结构管理全局进度，并利用局部进度模块压缩交互历史，实现基于增强步级转移的学习。
result: 在ScienceWorld和ALFWorld基准测试中，该方法在性能、泛化能力和Token利用率方面均优于现有基准。
conclusion: STEP-HRL证明了通过分层任务分解和步级状态增强，可以有效提升LLM智能体在复杂环境中的决策效率和扩展性。
---

## 摘要
大语言模型（LLM）智能体在复杂的交互式决策任务中展现出了强大的能力。然而，现有的 LLM 智能体通常依赖于日益增长的交互历史，导致了高昂的计算成本和有限的可扩展性。在本文中，我们提出了 STEP-HRL，这是一个分层强化学习（HRL）框架，它通过仅以单步转移而非完整交互历史为条件，实现了步级（step-level）学习。STEP-HRL 对任务进行分层构建，利用已完成的子任务来表示整个任务的全局进度。通过引入局部进度模块，它还迭代且有选择性地总结每个子任务内的交互历史，从而生成局部进度的紧凑摘要。这些组件共同为高层和底层策略生成了增强的步级转移。在 ScienceWorld 和 ALFWorld 基准测试上的实验结果一致表明，STEP-HRL 在性能和泛化能力方面显著优于基准模型，同时减少了 token 的使用量。我们的代码可在 https://github.com/TonyStark042/STEP-HRL 获取。

## Abstract
Large language model (LLM) agents have demonstrated strong capabilities in complex interactive decision-making tasks. However, existing LLM agents typically rely on increasingly long interaction histories, resulting in high computational cost and limited scalability. In this paper, we propose STEP-HRL, a hierarchical reinforcement learning (HRL) framework that enables step-level learning by conditioning only on single-step transitions rather than full interaction histories. STEP-HRL structures tasks hierarchically, using completed subtasks to represent global progress of overall task. By introducing a local progress module, it also iteratively and selectively summarizes interaction history within each subtask to produce a compact summary of local progress. Together, these components yield augmented step-level transitions for both high-level and low-level policies. Experimental results on ScienceWorld and ALFWorld benchmarks consistently demonstrate that STEP-HRL substantially outperforms baselines in terms of performance and generalization while reducing token usage. Our code is available at https://github.com/TonyStark042/STEP-HRL.

---

## 论文详细总结（自动生成）

以下是对论文《Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 智能体在处理长程决策任务时，通常依赖于不断增长的完整交互历史（History-conditioned）。这种设计导致了三个主要问题：
    1.  **计算成本高昂**：Transformer 的注意力机制随上下文长度呈平方级增长。
    2.  **推理质量下降**：冗长的历史包含大量冗余或无关信息，容易干扰模型的逻辑推理。
    3.  **信用分配困难**：在长序列中难以准确评估特定动作对最终结果的贡献。
*   **核心目标**：提出一种名为 **STEP-HRL** 的框架，通过分层强化学习和状态压缩，使智能体能够仅基于“步级（Step-level）”的增强状态进行决策，从而摆脱对全量历史的依赖。

### 2. 方法论
*   **核心思想**：将复杂任务分解为分层结构，并引入“进度建模”来替代原始历史。
*   **关键技术细节**：
    *   **分层结构**：分为高层策略（生成子任务，代表全局进度）和底层策略（执行具体动作）。
    *   **局部进度模块（Local Progress Module）**：这是本文的核心创新。引入一个策略 $\pi_p$，它在每个步骤迭代地将“当前动作”、“观察结果”和“前一阶段进度”总结为一个紧凑的文本摘要。
    *   **增强步级转移**：
        *   **底层策略**：输入为（当前子任务 + 当前观察 + 局部进度摘要），输出为动作。
        *   **高层策略**：输入为（总指令 + 已完成子任务列表 + 上一个子任务的最终进度摘要），输出为下一个子任务。
    *   **训练流程**：
        1.  **行为克隆（BC）初始化**：利用专家演示数据对高层、底层和进度策略进行联合微调。
        2.  **步级离线强化学习**：采用隐式 Q 学习（IQL）范式，通过优势加权回归（AWR）优化策略。三个策略共享同一个 LLM 主干网络，但拥有各自的 Critic 网络（价值评估器）。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceWorld**：复杂的科学实验环境，具有密集的中间奖励。
    *   **ALFWorld**：家庭环境下的长程规划任务，具有稀疏奖励。
*   **基准测试（Benchmark）**：分为 **Seen**（训练中见过的任务家族）和 **Unseen**（全新的任务变体，测试泛化性）。
*   **对比方法**：
    *   **提示词方法**：ReAct, Reflexion。
    *   **微调/强化学习方法**：Swift-Sage, ETO, WKM, 以及最先进的分层基准 GLIDER。
    *   **闭源模型参考**：GPT-3.5 和 GPT-4。

### 4. 资源与算力
*   **硬件使用**：论文明确指出实验使用了 **8 张 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   采用 LoRA 进行参数高效微调。
    *   行为克隆阶段训练 5 个 epoch，离线 RL 阶段训练 3 个 epoch。
    *   Batch size 设置为 128（BC）和 256（RL）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了三种主流开源模型：Mistral-7B, Gemma-7B, Llama3-8B。
    *   进行了模型缩放实验（Llama-1B, 3B, 8B）。
    *   **消融实验**：分别验证了“局部进度模块”、“分层结构”和“强化学习阶段”对最终性能的贡献。
    *   **敏感性分析**：对 RL 训练中的超参数（如温度系数 $\beta$、分位数 $\tau$）以及数据量、数据来源进行了详细测试。
*   **充分性评价**：实验设计非常充分，涵盖了从性能对比、泛化性验证到推理效率（Token 消耗）的全方位评估，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：STEP-HRL 在两个基准测试上均显著超过了所有对比基准。在 ScienceWorld 的 Unseen 任务上，相比 GLIDER 提升了约 10%-15%。
*   **泛化能力强**：即使在较小规模的模型（如 1B, 3B）上，该框架依然能展现出极强的竞争力。
*   **极高的推理效率**：与随步数增加而 Token 消耗激增的传统 RL/HRL 不同，STEP-HRL 的每步 Token 消耗几乎是恒定的，大幅降低了推理延迟和显存压力。

### 7. 优点
*   **状态表示创新**：通过“局部进度”将非结构化的历史转化为结构化的文本摘要，巧妙地解决了 LLM 处理长上下文的短板。
*   **架构简洁高效**：三策略共享参数的设计不仅减少了显存占用，还促进了不同决策层级间的知识迁移。
*   **步级优化的稳定性**：将离线 RL 应用于步级转移而非整条轨迹，使得信用分配更精准，训练更稳定。

### 8. 不足与局限
*   **对专家数据的依赖**：构建“局部进度”和“子任务”的标注需要高质量的专家轨迹，这在某些缺乏模拟器或专家数据的领域可能难以实施。
*   **终止判定风险**：底层策略在生成动作的同时需要判定子任务是否完成，若判定失误（早停或过晚），会直接影响高层策略的规划。
*   **环境复杂性限制**：目前主要在文本交互环境中验证，对于需要极高实时性或视觉-语言多模态融合的复杂物理环境，其摘要模块的压缩率和准确性仍待验证。

（完）
