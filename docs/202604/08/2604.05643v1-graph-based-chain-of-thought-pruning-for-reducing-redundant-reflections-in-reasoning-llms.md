---
title: Graph-Based Chain-of-Thought Pruning for Reducing Redundant Reflections in Reasoning LLMs
title_zh: 基于图的思维链剪枝：减少推理大语言模型中的冗余反思
authors: "Hongyuan Yuan, Xinran He, Run Shao, Bolei He, Xianwei Xue, Mengke Chen, Qiutong Pan, Haiwei Wang, Haifeng Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05643v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过强化学习优化思维链以增强推理能力并减少冗余
tldr: "本研究针对推理大模型在强化学习后出现的“过度思考”问题，提出了基于图的思维链（CoT）剪枝框架。通过将线性CoT转换为有向无环图（DAG），识别并消除无差别反射和重复验证等冗余模式。采用SFT、DPO和带长度惩罚的GRPO三阶段蒸馏流程，在保持甚至提升推理准确率的同时，将推理Token数量平均减少了42%，显著提升了推理效率。"
selection_source: fresh_fetch
motivation: 强化学习虽增强了模型推理能力，但也导致了产生大量低效、重复反射内容的“过度思考”现象。
method: 将CoT转化为有向无环图，通过分支级和深度级剪枝剔除冗余，并结合SFT、DPO及带长度惩罚的GRPO进行策略优化。
result: "实验表明该方法在维持或提高模型准确率的前提下，成功减少了约42%的推理Token消耗。"
conclusion: 通过图结构化剪枝和多阶段强化学习，可以有效抑制推理大模型的冗余思考，实现推理性能与效率的双重优化。
---

## 摘要
通过强化学习（RL）扩展思维链（CoT）已被广泛用于增强大语言模型（LLMs）的推理能力。然而，由于奖励信号的稀疏性，这也会诱发一些不良的思维模式，例如“过度思考”（overthinking），即生成冗余的中间推理内容。在这项工作中，我们认为这种冗余的一个主要来源是低效的反思，这通常表现为两种有问题的模式：无差别反思（Indiscriminate Reflection），即模型在整个推理过程中进行广泛且低影响的检查；以及重复反思（Repetitive Reflection），即模型反复重新验证已经确定的结论。为了解决这个问题，我们引入了一个基于图的 CoT 优化框架。具体而言，我们将每个线性 CoT 转换为具有显式依赖边的有向无环图（DAG），并设计了一种双重剪枝策略：分支级剪枝移除贡献较弱的反思分支，而深度级剪枝消除后期阶段的重新验证。我们通过一个三阶段流水线来蒸馏这种行为：(1) SFT 在剪枝后的简洁轨迹上初始化策略；(2) DPO 使模型偏好正确但冗余较少的轨迹；(3) 带有长度惩罚的 GRPO 共同优化答案的正确性和效率。实验表明，我们的方法在保持或提高准确率的同时，将平均推理 Token 数量减少了 42%。

## Abstract
Extending CoT through RL has been widely used to enhance the reasoning capabilities of LLMs. However, due to the sparsity of reward signals, it can also induce undesirable thinking patterns such as overthinking, i.e., generating redundant intermediate reasoning content. In this work, we argue that a major source of such redundancy is inefficient reflection, which often manifests in two problematic patterns: Indiscriminate Reflection, where the model performs broad, low-impact checks throughout reasoning, and Repetitive Reflection, where it repeatedly re-verifies an already established conclusion. To address this, we introduce a graph-based CoT optimization framework. Specifically, we convert each linear CoT into a directed acyclic graph (DAG) with explicit dependency edges, and design a dual pruning strategy: branch-level pruning removes weakly contributing reflection branches, while depth-level pruning eliminates late-stage re-verification. We distill this behavior via a three-stage pipeline: (1) SFT to initialize the policy on pruned concise traces, (2) DPO to prefer correct but less redundant trajectories, and (3) GRPO with length penalty to jointly optimize answer correctness and efficiency. Experiments show that our approach reduces the average reasoning tokens by 42\% while maintaining or improving accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：基于图的思维链剪枝（Graph-Based CoT Pruning）

#### 1. 核心问题与整体含义（研究动机和背景）
随着强化学习（RL）在提升大语言模型（LLM）推理能力（如 OpenAI o1, DeepSeek-R1）中的广泛应用，模型生成的思维链（CoT）变得越来越长。然而，由于 RL 奖励信号稀疏，模型容易产生“过度思考”（Overthinking）现象，即生成大量对最终答案贡献极小的冗余内容。
论文指出冗余主要源于**低效反思**，并将其归纳为两种模式：
*   **无差别反思（Indiscriminate Reflection）：** 对每一个琐碎步骤都进行检查。
*   **重复反思（Repetitive Reflection）：** 反复验证已经确定的中间结论。
本研究旨在通过结构化建模和剪枝，在不损失准确率的前提下，显著降低推理成本。

#### 2. 方法论：核心思想与技术细节
论文提出了一种基于图的 CoT 优化框架，主要包含以下三个关键步骤：

*   **CoT 图结构化（Graph Construction）：**
    *   利用外部 LLM 将线性的 CoT 文本切分为多个步骤（Chunks）。
    *   将步骤转化为有向无环图（DAG），节点分为“进展型（Progress）”和“评论/反思型（Review）”。
    *   通过 Prompt 显式提取节点间的逻辑依赖关系（边）。
*   **双重剪枝策略（Dual Pruning）：**
    *   **分支级剪枝（Branch-level）：** 如果一个反思节点产生的后继节点极少（分支过细），认为其对主干推理贡献低，予以移除。
    *   **深度级剪枝（Depth-level）：** 如果反思节点出现在推理链的后期（相对深度超过阈值），通常属于答案生成后的重复验证，予以移除。
*   **三阶段训练流水线：**
    1.  **冷启动 SFT：** 在剪枝后的简洁 CoT 数据上进行监督微调，建立高效推理的初步偏置。
    2.  **偏好对齐 DPO：** 构建“简洁且正确” vs “冗余且正确”的样本对，引导模型偏好低冗余路径。
    3.  **带长度惩罚的 GRPO：** 在强化学习阶段，将答案正确性作为主奖励，并引入基于相对长度的惩罚项，进一步压缩推理长度。

#### 3. 实验设计
*   **数据集/场景：** 专注于数学推理任务，包括 AIME24、AIME25、AMC23、MATH500 和 OlympiadBench。
*   **Benchmark：** 使用准确率（Accuracy）和平均推理 Token 数（Average Tokens）作为核心指标。
*   **对比方法：**
    *   **高效推理基准：** O1-Pruner、TokenSkip、EfficientReasoning、AdaptThink。
    *   **开源 R1 类模型：** Skywork-OR1-7B、OREAL-7B、Light-R1-DS-7B 等。
*   **基础模型：** DeepSeek-R1-Distill-Qwen-1.5B 和 7B。

#### 4. 资源与算力
*   **硬件设备：** 实验在一台配备 **4 × NVIDIA A800 GPU** 的计算节点上完成。
*   **训练细节：** SFT 阶段训练 8 个 Epoch，DPO 阶段 5 个 Epoch，GRPO 阶段进行 220 个 Step。具体的总训练时长未在文中明确给出，但给出了详细的超参数设置（如学习率、Batch Size 等）。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个主流数学竞赛级数据集，并在 1.5B 和 7B 两个参数规模的模型上进行了验证。
*   **消融实验：** 论文进行了阶段性消融（Base -> SFT -> DPO -> GRPO），展示了每个阶段对准确率提升和长度压缩的具体贡献。
*   **客观性：** 引入了人类评估（Human Evaluation）来验证图构建的准确性（节点类型分类和原子性），并对比了随机截断（Len-Trunc）与图剪枝的效果，证明了该方法的逻辑保留能力。实验设计较为全面、公平。

#### 6. 主要结论与发现
*   **效率显著提升：** 在 DeepSeek-R1-Distill-Qwen-7B 上，该方法将平均推理 Token 减少了 **42.7%**（从 8134 降至 4660）。
*   **准确率不降反升：** 在大幅缩减长度的同时，平均准确率从 59.72% 提升至 60.95%，尤其在 AIME 等高难度任务上表现突出。
*   **行为转变：** 训练后的模型减少了“Wait”、“Hmm”等反思性词汇的频率，增加了“Therefore”等进展性词汇，推理风格从“犹豫反思型”转向“决策驱动型”。

#### 7. 优点（亮点）
*   **结构化视角：** 不同于以往简单的长度限制或 Token 过滤，该方法通过图结构深入理解推理逻辑，精准定位“无效反思”。
*   **训练流程完备：** 结合了 SFT 的冷启动、DPO 的偏好对齐和 GRPO 的效率优化，形成了一套完整的蒸馏/训练闭环。
*   **逻辑保留：** 实验证明图剪枝能有效保留核心推理链，避免了盲目压缩导致的逻辑断裂。

#### 8. 不足与局限
*   **预处理成本：** 构建推理图需要调用强大的教师模型（如 Qwen-Turbo），这增加了数据准备阶段的计算开销和成本。
*   **标签粒度：** 目前仅将节点分为“进展”和“反思”，分类较为粗糙，可能忽略了某些复杂的语义细微差别。
*   **领域局限性：** 实验主要集中在数学领域，在法律、创意写作或多轮对话等非结构化推理任务中的泛化能力尚待验证。
*   **依赖触发词：** 初始的 CoT 切分依赖于预设的关键词（如 "Wait", "So"），对于不遵循这些模式的模型可能效果受限。

（完）
