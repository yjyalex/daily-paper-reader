---
title: "Not All Turns Are Equally Hard: Adaptive Thinking Budgets For Efficient Multi-Turn Reasoning"
title_zh: 并非所有轮次都同样困难：用于高效多轮推理的自适应思考预算
authors: "Neharika Jali, Anupam Nayak, Gauri Joshi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05164v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用GRPO算法进行多轮推理的强化学习计算预算分配
tldr: "针对多轮推理中推理开销过大的问题，本文提出了一种名为 TAB（Turn-Adaptive Budgets）的自适应预算分配策略。该方法将多轮推理建模为多目标马尔可夫决策过程，利用 GRPO 算法训练策略，根据对话历史动态分配推理步数。实验表明，TAB 在保持数学推理准确率的同时，比静态基准节省了高达 35% 的 Token，有效提升了推理效率。"
selection_source: fresh_fetch
motivation: 现有的推理预算分配方法多针对单轮对话，忽视了多轮推理中的序列依赖性，导致简单步骤过度思考而复杂步骤预算不足。
method: 将多轮推理建模为多目标马尔可夫决策过程，并使用 GRPO 算法训练策略，使其能根据上下文动态为不同难度的轮次分配推理 Token。
result: "在数学推理基准测试中，TAB 在维持准确率的前提下节省了 35% 的 Token，而具备全局视野的 TAB All-SubQ 变体则节省了 40%。"
conclusion: 自适应分配推理预算是提升多轮推理效率的有效途径，能够在保证复杂任务性能的同时显著降低计算成本。
---

## 摘要
随着大语言模型（LLM）推理性能进入平台期，提高推理时计算效率对于缓解简单查询中的过度思考和冗长思考轨迹至关重要。先前的方法（包括长度正则化、自适应路由和基于难度的预算分配）主要关注单轮设置，未能解决多轮推理中固有的序列依赖性。在这项工作中，我们将多轮推理表述为一个序列计算分配问题，并将其建模为一个多目标马尔可夫决策过程。我们提出了 TAB（Turn-Adaptive Budgets，轮次自适应预算），这是一种通过群体相对策略优化（GRPO）训练的预算分配策略，旨在学习在遵守每个问题的全局 token 约束的同时最大化任务准确率。因此，TAB 以对话历史作为输入，学习为较简单的轮次自适应地分配较少的预算，并为关键的较难推理步骤节省适当数量的 token。我们在数学推理基准测试上的实验表明，TAB 实现了卓越的准确率-token 权衡，与静态和现成的 LLM 预算基准相比，在保持准确率的同时节省了高达 35% 的 token。此外，对于可以预先获得所有子问题计划的系统，我们提出了 TAB All-SubQ，这是一种根据对话历史以及所有过去和未来的子问题来分配 token 预算的策略，与基准相比可节省高达 40% 的 token。

## Abstract
As LLM reasoning performance plateau, improving inference-time compute efficiency is crucial to mitigate overthinking and long thinking traces even for simple queries. Prior approaches including length regularization, adaptive routing, and difficulty-based budget allocation primarily focus on single-turn settings and fail to address the sequential dependencies inherent in multi-turn reasoning.In this work, we formulate multi-turn reasoning as a sequential compute allocation problem and model it as a multi-objective Markov Decision Process. We propose TAB: Turn-Adaptive Budgets, a budget allocation policy trained via Group Relative Policy Optimization (GRPO) that learns to maximize task accuracy while respecting global per-problem token constraints. Consequently, TAB takes as input the conversation history and learns to adaptively allocate smaller budgets to easier turns and save appropriate number of tokens for the crucial harder reasoning steps. Our experiments on mathematical reasoning benchmarks demonstrate that TAB achieves a superior accuracy-tokens tradeoff saving up to 35% tokens while maintaining accuracy over static and off-the-shelf LLM budget baselines. Further, for systems where a plan of all sub-questions is available apriori, we propose TAB All-SubQ, a budget allocation policy that budgets tokens based on the conversation history and all past and future sub-questions saving up to 40% tokens over baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Not All Turns Are Equally Hard: Adaptive Thinking Budgets For Efficient Multi-Turn Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理能力的提升，推理时计算（Test-time Compute）的效率变得至关重要。目前的推理模型（如 o1、DeepSeek-R1）往往存在“过度思考”的问题，即在简单任务上消耗过长的思考链（CoT），导致高延迟和高成本。
*   **研究动机**：现有的预算分配方法（如路由、难度评估）多针对单轮对话，忽视了多轮推理中的**序列依赖性**。在多轮对话中，前期的 Token 消耗会累积到后期上下文，且不同轮次的难度差异巨大。
*   **核心问题**：如何在多轮推理过程中，根据对话历史动态、自适应地分配每一轮的 Token 预算，以在有限的全局预算下实现准确率最大化。

### 2. 方法论
论文将多轮推理预算分配建模为一个**多目标马尔可夫决策过程（MDP）**，并提出了 **TAB (Turn-Adaptive Budgets)** 策略。
*   **核心思想**：训练一个专门的“预算员”（Budgeter）模型，它观察当前的对话状态（历史记录 + 当前子问题），决定给“求解员”（Solver）分配多少 Token 预算。
*   **关键技术细节**：
    *   **状态空间**：包含全局问题、已生成的对话历史及当前待解决的子问题。
    *   **动作空间**：从离散的预算集合（如 256, 512, ..., 4096 Token）中选择。
    *   **奖励函数**：采用标量化多目标奖励：$r = \text{Accuracy} - \lambda \max(0, \sum \text{tokens} - B)$。其中 $B$ 是全局预算约束，$\lambda$ 是惩罚权重。
    *   **训练算法**：使用 **GRPO (Group Relative Policy Optimization)**。该算法通过组内相对优势估计来优化策略，无需额外的价值网络（Value Network），适合处理多轮推理中的稀疏奖励和长时延信用分配问题。
*   **变体 TAB All-SubQ**：针对预先已知所有子问题的场景，该策略能同时参考未来步骤的难度进行全局规划。

### 3. 实验设计
*   **实验场景**：多轮数学推理。系统由 User（负责分解问题）、Budgeter（负责分配预算）和 Solver（负责执行推理）三个模型组成。
*   **数据集**：
    *   **主基准**：MATH-500, AMC23, MATH Level-5, OlympiadBench, AIME25。
    *   **分布外（OOD）测试**：TheoremQA（定理证明）、BIG-Bench Extra Hard（算法推理）、GPQA（研究生水平科学问题）。
*   **对比方法（Baselines）**：
    1.  **Static**：每轮分配固定的静态预算。
    2.  **LLM-Judge Individual**：基于当前子问题难度的零样本评估。
    3.  **LLM-Judge Multi-Turn**：基于对话历史的零样本评估。

### 4. 资源与算力
*   **模型选择**：User 使用 Qwen3-8B，Budgeter 使用 Qwen3-1.7B/4B，Solver 使用经过强化学习训练、能严格遵守长度约束的 L1-Qwen3-8B-Exact。
*   **算力资源**：实验使用了匹兹堡超级计算中心的 **Bridges-2 GPU** 集群。
*   **训练细节**：采用 LoRA 微调（r=64, alpha=128），训练 125 步，Batch Size 为 64，学习率为 $10^{-5}$。文中未明确给出具体的 GPU 数量和总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数学基准和 3 个跨领域 OOD 基准，实验组数较多。
*   **充分性**：论文对比了不同规模的 Budgeter（1.7B vs 4B）、不同的全局预算阈值（3k 到 10k Token），并进行了消融实验（如 TAB vs TAB All-SubQ）。
*   **客观性**：使用了可客观验证的数学答案作为奖励信号，避免了主观评分的偏差。

### 6. 主要结论与发现
*   **高效性**：TAB 在保持或提升准确率的同时，比静态基准节省了约 **35%** 的 Token。
*   **规划能力**：TAB All-SubQ 变体通过预见未来步骤，可节省高达 **40%** 的 Token，证明多轮推理本质上是一个**规划问题**而非简单的难度估计。
*   **自适应行为**：TAB 学会了在简单的“簿记”步骤节省预算，而在关键的代数运算或逻辑推理步骤投入更多计算资源。
*   **泛化性**：在 MATH 数据集上训练的策略在科学和算法推理（GPQA, BBEH）中依然表现出优越的效率权衡。

### 7. 优点
*   **序列建模**：首次将多轮推理预算分配视为序列决策问题，解决了单轮方法无法处理的累积成本问题。
*   **训练效率**：利用 GRPO 算法简化了强化学习流程，避免了长序列下价值函数估计不准的问题。
*   **实用性强**：提出的方法可以模块化地集成到现有的 Agent 框架或推理流水线中。

### 8. 不足与局限
*   **依赖特定 Solver**：实验依赖于能够精准控制输出长度的 Solver 模型（如 L1-Qwen3），对于普通 LLM 的适用性需进一步验证。
*   **奖励稀疏性**：奖励仅在对话结束时给出，对于极长轮次的对话，信用分配（Credit Assignment）可能依然面临挑战。
*   **任务局限**：目前主要集中在有标准答案的数学/逻辑任务，在开放式对话或非客观验证任务中的表现尚不明确。

（完）
