---
title: "APPA: Adaptive Preference Pluralistic Alignment for Fair Federated RLHF of LLMs"
title_zh: APPA：用于大语言模型公平联邦 RLHF 的自适应偏好多元对齐
authors: "Mahmoud Srewa, Tianyu Zhao, Salma Elmalaki"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04261v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大语言模型多元偏好对齐的联邦 RLHF
tldr: 本研究针对联邦强化学习（FedRLHF）中大语言模型的多样化偏好对齐问题，提出了APPA框架。现有方法在平均聚合（忽视弱势群体）与最小聚合（损害整体性能）之间存在权衡。APPA通过历史对齐奖励动态重权组级奖励，在不接触原始数据的情况下，优先考虑对齐不足的群体。实验表明，APPA在保持高整体对齐度的同时，显著提升了公平性，为多价值观对齐提供了新思路。
selection_source: fresh_fetch
motivation: 现有的联邦偏好对齐方法在处理多样化群体价值观时，难以平衡整体对齐效果与弱势群体的公平性。
method: 提出APPA框架，利用历史对齐奖励动态调整各群体的奖励权重，实现自适应的偏好对齐。
result: "在多个模型和数据集上的实验显示，APPA相比平均聚合将最差群体的对齐度提升了高达28%，且整体表现优于最小聚合。"
conclusion: APPA为联邦学习环境下的多价值观对齐提供了一种高效且公平的解决方案，实现了对齐性能与群体公平性的平衡。
---

## 摘要
将大语言模型（LLMs）与多样化的人类偏好相对齐需要多元对齐，即单个模型必须同时尊重多个不同群体的价值观。在联邦人类反馈强化学习（FedRLHF）中，这些群体在不集中偏好数据的情况下对齐共享策略，这使得公平的奖励聚合变得至关重要。现有的聚合方法存在明显的权衡：基于平均值的聚合会系统性地导致表现最差的群体对齐不足，而最小聚合则以牺牲整体对齐为代价优先考虑最差群体的表现。我们提出了 APPA，一种自适应偏好多元对齐框架，它根据历史对齐奖励动态地重新加权群体级奖励。我们的方法在不降低对齐良好群体表现的情况下优先考虑对齐不足的群体，且无需访问原始偏好数据。APPA 被集成到基于近端策略优化（PPO）的 FedRLHF 流水中，并在三个模型系列（Gemma 2 2B、Llama 3.2 3B、Qwen3 0.6B）的 GLOBALQA 和 OQA 数据集上进行了评估。结果表明，APPA 实现了强大的公平性与对齐权衡，在大多数配置下，与平均聚合相比，最差群体的对齐度提高了多达 28%，同时保持了比最小聚合更高的整体对齐度。

## Abstract
Aligning large language models (LLMs) with diverse human preferences requires pluralistic alignment, where a single model must respect the values of multiple distinct groups simultaneously. In federated reinforcement learning from human feedback (FedRLHF), these groups align a shared policy without centralizing preference data, which makes fair reward aggregation essential. Existing aggregation methods exhibit clear trade offs: average based aggregation systematically under aligns worst performing groups, while min aggregation prioritizes worst group performance at the cost of overall alignment. We propose APPA, an Adaptive Preference Pluralistic Alignment framework that dynamically reweights group level rewards based on historical alignment rewards. Our approach prioritizes under aligned groups without degrading well aligned ones, while requiring no access to raw preference data. Integrated into a proximal policy optimization (PPO) based FedRLHF pipeline and evaluated on GLOBALQA and OQA across three model families (Gemma 2 2B, Llama 3.2 3B, Qwen3 0.6B), APPA achieves strong fairness alignment trade offs, improving worst group alignment by up to 28% over average aggregation while maintaining higher overall alignment than min aggregation across most configurations.

---

## 论文详细总结（自动生成）

以下是对论文《APPA: Adaptive Preference Pluralistic Alignment for Fair Federated RLHF of LLMs》的结构化总结：

### 1. 核心问题与整体含义
论文探讨了**大语言模型（LLM）的多元化对齐（Pluralistic Alignment）**问题。核心挑战在于：人类社会由具有不同价值观、文化和背景的群体组成，模型若仅针对单一聚合偏好进行优化，会产生“多数派偏见”，导致少数群体或弱势群体的需求被边缘化。
在**联邦人类反馈强化学习（FedRLHF）**框架下，如何在保护隐私（不集中原始偏好数据）的同时，公平地聚合来自不同群体的冲突奖励信号，是本文的研究重点。

### 2. 方法论
论文提出了 **APPA（自适应偏好多元对齐）** 框架，其核心思想是动态调整各群体的奖励权重：
*   **分布式奖励生成**：服务器分发模型生成的内容（Rollouts），各联邦群体利用本地的 **PluralLLM**（一种轻量级偏好预测器）生成群体特有的奖励信号并返回服务器。
*   **自适应 Alpha 聚合**：
    *   **历史追踪**：利用指数移动平均（EMA）持续追踪每个群体的历史对齐奖励（$h_g^t$）。
    *   **反向 Softmax 权重**：根据历史表现计算权重 $\alpha_g^t$，表现越差（对齐不足）的群体获得更高的权重，从而在优化中获得更多关注。
    *   **公平性指数（FI）触发机制**：引入基于变异系数（CoV）的公平性指数。当群体间差异较大（FI 低于阈值 $\tau$）时，采用自适应加权聚合；当群体已趋于一致时，退回到普通平均聚合，以保持训练稳定。
*   **PPO 优化**：将聚合后的标量奖励代入近端策略优化（PPO）算法，更新全局策略模型。

### 3. 实验设计
*   **数据集**：
    *   **GLOBALQA**：涉及全球不同国家的观点，侧重跨国多元对齐（名义选项）。
    *   **OQA (OpinionQA)**：涉及美国不同人口统计群体（如宗教、政治倾向、收入），侧重群体间对齐（序数选项）。
*   **任务类型**：分布偏好对齐（DPA，预测概率分布）和序数偏好对齐（OPA，预测排名）。
*   **评估基准（Benchmark）**：
    *   **Base**：原始指令微调模型。
    *   **SFT**：基于多数派标签的监督微调。
    *   **PPO-Average**：平均奖励聚合（公平性较差）。
    *   **PPO-Min**：最小奖励聚合（极端的公平性，可能损害整体性能）。
*   **评估指标**：JS 散度、Wasserstein 距离、余弦相似度、Borda 分数、公平性指数（FI）。

### 4. 资源与算力
*   **硬件**：使用了 3 个计算节点，每个节点配备 1 张 **NVIDIA A100 GPU**，以及 Intel Xeon Gold 6326 CPU 和 256GB RAM。
*   **模型规模**：评估了三个模型系列：**Gemma-2-2B**、**Llama-3.2-3B** 和 **Qwen3-0.6B**。
*   **训练细节**：为了适配显存，PPO 训练采用了 **4-bit 量化**（QLoRA 风格）和 LoRA 适配器。

### 5. 实验数量与充分性
论文进行了较为充分的实验：
*   涵盖了 3 种不同架构和规模的模型，验证了方法的通用性。
*   在 2 个具有代表性的民意调查数据集上进行了测试。
*   对比了 SFT 和两种主流的 PPO 聚合策略。
*   提供了雷达图（Spider Plots）展示各群体的具体对齐情况，以及公平性与对齐性能的权衡曲线图。
实验设计客观，通过多维度指标（平均分 vs 最差组得分）全面衡量了性能。

### 6. 主要结论与发现
*   **公平性提升**：APPA 在保持高整体对齐度的同时，显著提升了表现最差群体的对齐水平，相比平均聚合，最差群体的对齐度提升了高达 **28%**。
*   **权衡优化**：APPA 成功打破了“平均聚合（忽视弱势组）”与“最小聚合（损害整体组）”之间的僵局，在大多数配置下实现了更好的帕累托改进。
*   **模型通用性**：该方法在从 0.6B 到 3B 参数的不同模型上均表现稳健。
*   **SFT 的局限**：实验发现仅靠多数派标签的 SFT 往往会加剧对少数群体的误导，而 RLHF 阶段的公平聚合是修复这一问题的关键。

### 7. 优点
*   **动态自适应**：无需像传统方法那样手动调节 Alpha 参数，而是根据训练进度自动调整。
*   **隐私保护**：在联邦学习框架下工作，无需访问用户的原始偏好数据。
*   **理论支撑**：聚合规则参考了社会选择理论，并引入了公平性指数作为切换开关，设计精巧。
*   **即插即用**：作为 PPO 奖励聚合的模块，易于集成到现有的 RLHF 流水中。

### 8. 不足与局限
*   **任务局限性**：目前主要集中在多选题（QA）形式的偏好对齐，尚未在长文本生成、代码编写等更复杂的开放式任务中验证。
*   **依赖预训练预测器**：APPA 的效果部分取决于本地 PluralLLM 预测器的准确性。
*   **小模型瓶颈**：在极小规模模型（如 Qwen 0.6B）上，由于模型容量限制，对某些复杂奖励信号的响应可能不如大模型灵敏。
*   **超参数敏感性**：虽然减少了 Alpha 的调节，但仍引入了新的超参数（如 $\tau, \lambda, T$），文中未对这些参数进行详尽的消融研究。

（完）
