---
title: "Reasoning Through Chess: How Reasoning Evolves from Data Through Fine-Tuning and Reinforcement Learning"
title_zh: 通过国际象棋进行推理：推理如何通过微调和强化学习从数据中演化
authors: "Lucas Dionisopoulos, Nicklas Majamaki, Prithviraj Ammanabrolu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05134v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 在国际象棋等复杂任务中推理从监督微调到强化学习的演变
tldr: 本研究探讨了语言模型在国际象棋任务中如何通过监督微调（SFT）和强化学习（RL）演化出推理能力。研究对比了直接预测最佳步法与多步轨迹预测两种SFT策略，发现RL能显著提升走子质量并减少幻觉。虽然直接预测策略在RL后表现强劲，但会导致推理不透明；而多步轨迹训练则能保持推理的忠实性与RL的稳定性。最终，研究发布了一个超越主流开源推理模型的7B参数国际象棋模型。
selection_source: fresh_fetch
motivation: 旨在探索如何通过SFT和RL使语言模型在原本不擅长的复杂任务（如国际象棋）中获得推理能力并分析其演化过程。
method: 对比了基于直接步法预测和多步轨迹数据的SFT策略，并结合RL训练，系统分析了推理忠实度、幻觉率及模型性能的演变。
result: RL显著提升了走子质量并降低了幻觉率，但直接预测策略在RL后易产生与决策不一致的虚假推理，而多步轨迹训练则更稳定且推理更忠实。
conclusion: SFT数据的选择决定了推理的忠实度与RL的稳定性，且SFT阶段的特定指标可有效预测RL后的最终模型性能。
---

## 摘要
如何让语言模型在原生就不擅长的任务中进行推理？我们通过分析一系列受理论启发的训练集如何影响语言模型在国际象棋中的表现，研究了推理在语言模型中是如何演化的——从有监督微调（SFT）到强化学习（RL）。我们发现，微调模型以直接预测最佳步法可以实现有效的 RL 并获得最强的下游性能——然而，RL 步骤会引发不忠实的推理（即推理过程与所选步法不一致）。另一种方案是，在多步轨迹上进行训练可以产生具有忠实推理和更稳定 RL 的相当的下游性能。我们表明，RL 诱导了步法质量分布的显著正向偏移，并作为副作用降低了幻觉率。最后，我们发现几个 SFT 检查点指标——涵盖评估性能、幻觉率和推理质量的指标——能够预测 RL 后的模型性能。我们发布了检查点、最终模型以及训练数据、评估和代码，这些使我们能够凭借一个 7B 参数的模型在国际象棋领域超越领先的开源推理模型。

## Abstract
How can you get a language model to reason in a task it natively struggles with? We study how reasoning evolves in a language model -- from supervised fine-tuning (SFT) to reinforcement learning (RL) -- by analyzing how a set of theoretically-inspired datasets impacts language model performance in chess. We find that fine-tuning a model to directly predict the best move leads to effective RL and the strongest downstream performance -- however, the RL step elicits unfaithful reasoning (reasoning inconsistent with the chosen move). Alternatively, training on multi-move trajectories yields comparable downstream performance with faithful reasoning and more stable RL. We show that RL induces a substantial positive shift in the distribution of move quality and reduces hallucination rates as a side effect. Finally, we find several SFT-checkpoint metrics -- metrics spanning evaluation performance, hallucination rates, and reasoning quality -- to be predictive of post-RL model performance. We release checkpoints and final models as well as training data, evaluations, and code which allowed us to surpass leading open-source reasoning models in chess with a 7B-parameter model.

---

## 论文详细总结（自动生成）

这篇论文《Reasoning Through Chess: How Reasoning Evolves from Data Through Fine-Tuning and Reinforcement Learning》深入探讨了语言模型（LM）如何通过不同类型的数据和训练阶段（SFT 与 RL）演化出复杂的推理能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何让原生并不擅长国际象棋的语言模型，通过强化学习（RL）演化出有效的推理能力？
*   **研究动机**：虽然推理模型（如 OpenAI o1）在数学和代码领域取得了突破，但这些领域已有大量高质量数据。国际象棋作为一个具有严密逻辑、可验证奖励（通过引擎）且模型原生表现较差的领域，是研究“推理如何从零到一演化”的理想受试场。
*   **研究目标**：分析监督微调（SFT）阶段的数据选择如何影响 RL 阶段的稳定性、最终性能以及推理的“忠实度”（Faithfulness）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：对比“直接预测结果”与“预测推理轨迹”两种训练范式，观察它们在 RL 阶段的不同表现。
*   **关键技术细节**：
    *   **模型基础**：使用 Qwen2.5-7B-Instruct 作为基座模型。
    *   **SFT 数据策略**：设计了多种数据集，包括：
        *   **Best Move**：直接预测最佳步法（策略函数 $\pi$）。
        *   **Best Line**：预测 4-6 步的最佳走子序列及评估值（模拟世界模型和 n-步自助法）。
        *   **其他**：拒绝采样（Rejection Sampling）、程序化生成的 Alpha-Beta 剪枝过程、棋盘事实问答等。
    *   **RL 算法**：采用 **Dr. GRPO**（一种改进的群组相对策略优化算法），去除了 KL 惩罚并调整了剪切范围，以鼓励探索。
    *   **奖励函数**：利用国际象棋引擎（如 Stockfish）提供可验证的奖励，包括步法合法性奖励和基于分值（Centipawn）的质量奖励。

### 3. 实验设计
*   **数据集/场景**：创建了四个自定义任务：预测步法（Predict Move）、选择最佳/最差步法（Best/Worst Move）、列出合法步法（Legal Moves）。
*   **Benchmark**：对比了基座模型 Qwen2.5-7B、Llama 4 Maverick 以及领先的开源推理模型 gpt-oss-120b。
*   **对比实验**：进行了多组数据包含分析（Inclusion Analysis），对比了单任务 vs 多任务训练、不同数据配比对下游 RL 性能的影响。

### 4. 资源与算力
*   **硬件型号**：使用了 Nvidia A100 和 H100 GPU。
*   **算力消耗**：最终的规模化训练（Scaled Runs）大约消耗了 **500 个 H100 小时**。
*   **训练规模**：SFT 阶段处理了 1500 万至 1.2 亿个 token；RL 阶段使用了 8k 至 1.6k 个唯一样本。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了 9 组主要的 SFT 包含实验（SFT1-SFT9）以及多组规模化实验。
*   **充分性评价**：实验设计较为充分，涵盖了从数据类型、Token 预算到 RL 训练动态的全面分析。通过线性回归分析了 SFT 指标与 RL 结果的相关性，增强了结论的客观性。
*   **公平性**：在包含分析中保持了相同的 Token 预算，确保了不同数据类型对比的公正性。

### 6. 主要结论与发现
*   **数据类型决定推理质量**：
    *   **Best Move** 训练的模型在 RL 后性能最强，但会产生“不忠实推理”（即推理过程说一套，实际走子做另一套）。
    *   **Best Line**（多步轨迹）训练的模型在 RL 过程中更稳定，且推理过程与决策高度一致（忠实度高）。
*   **RL 的副作用收益**：RL 不仅提升了走子质量，还显著降低了模型在推理过程中的“幻觉率”（引用的棋子或位置错误的概率）。
*   **可预测性**：SFT 阶段的准确率、幻觉率和推理质量指标可以有效预测 RL 后的最终表现。
*   **性能突破**：通过该方案训练的 7B 模型在多个国际象棋基准测试中超越了 120B 规模的开源推理模型。

### 7. 优点：亮点与创新
*   **深入的定性分析**：不仅关注分数，还深入研究了推理的“忠实度”和“策略使用情况”。
*   **可验证的闭环**：利用国际象棋引擎构建了一个完美的自动反馈环，避免了“LLM-as-a-judge”可能带来的偏见。
*   **开源贡献**：发布了完整的训练数据、代码和模型检查点，具有很高的复现价值。

### 8. 不足与局限
*   **模型单一性**：实验主要集中在 Qwen2.5-7B 上，尚未在其他架构（如 Llama 或 Mistral）上验证普适性。
*   **分布偏移**：由于训练数据集中在对局中后期以减少平庸走子，模型在开局阶段的表现相对较弱。
*   **推理成本**：虽然 7B 模型性能强劲，但推理时生成的长链条（CoT）增加了计算开销。

（完）
