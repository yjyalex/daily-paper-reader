---
title: "JTON: A Token-Efficient JSON Superset with Zen Grid Tabular Encoding for Large Language Models"
title_zh: JTON：一种面向大语言模型的、采用 Zen Grid 表格编码的高 Token 效率 JSON 超集
authors: Gowthamkumar Nandakishore
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05865v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 用于结构化数据序列化的令牌高效型JSON超集
tldr: "针对大语言模型处理结构化数据时 JSON 格式冗余导致 Token 浪费的问题，本文提出 JTON 格式。通过 Zen Grid 技术将表格数据的列名提取至单行并使用分号编码，在保留 JSON 类型系统的同时，将 Token 消耗降低了 15-60%。实验证明 JTON 在维持甚至提升模型理解与生成能力的同时，显著提高了处理效率并降低了计算成本。"
selection_source: fresh_fetch
motivation: 标准 JSON 在处理表格数据时会重复键名，导致 Token 消耗随行数线性增长，增加了 LLM 的推理成本和上下文压力。
method: 提出 JTON 格式，利用 Zen Grid 编码将列头提取并用分号分隔数值，在保持 JSON 兼容性的同时消除冗余。
result: "在多个领域测试中，JTON 比紧凑型 JSON 节省了平均 28.5% 的 Token，且在 12 个主流 LLM 中实现了 100% 的生成语法正确率。"
conclusion: JTON 是一种高效、兼容且易于解析的 JSON 超集，为 LLM 处理大规模结构化数据提供了一种低成本且高性能的替代方案。
---

## 摘要
当大语言模型（LLM）处理结构化数据时，序列化格式直接影响成本和上下文利用率。标准 JSON 在表格数组的每一行中重复键名，浪费了大量 Token，这种开销随行数线性增长。本文提出了 JTON（JSON Tabular Object Notation），这是一种严格的 JSON 超集。其核心思想 Zen Grid 将列标题提取到单行中，并使用分号对值进行编码，在保留 JSON 类型系统的同时减少了冗余。在七个真实领域中，与紧凑型 JSON 相比，Zen Grid 将 Token 数量减少了 15-60%（平均减少 28.5%；使用 bare_strings 时减少 32%）。对 10 个 LLM 的理解测试显示，相较于 JSON，其准确率净增 0.3 个百分点：四个模型有所提升，三个保持稳定，三个略有下降。在 12 个 LLM 上的生成测试表明，在少样本（few-shot）和零样本（zero-shot）设置下，语法有效性均达到 100%。一个基于 Rust/PyO3 的参考实现增加了 SIMD 加速解析，速度是 Python json 模块的 1.4 倍。代码、包含 683 个向量的测试套件以及所有实验数据均已公开。

## Abstract
When LLMs process structured data, the serialization format directly affects cost and context utilization. Standard JSON wastes tokens repeating key names in every row of a tabular array--overhead that scales linearly with row count. This paper presents JTON (JSON Tabular Object Notation), a strict JSON superset whose main idea, Zen Grid, factors column headers into a single row and encodes values with semicolons, preserving JSON's type system while cutting redundancy. Across seven real-world domains, Zen Grid reduces token counts by 15-60% versus JSON compact (28.5% average; 32% with bare_strings). Comprehension tests on 10 LLMs show a net +0.3 pp accuracy gain over JSON: four models improve, three hold steady, and three dip slightly. Generation tests on 12 LLMs yield 100% syntactic validity in both few-shot and zero-shot settings. A Rust/PyO3 reference implementation adds SIMD-accelerated parsing at 1.4x the speed of Python's json module. Code, a 683-vector test suite, and all experimental data are publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《JTON: A Token-Efficient JSON Superset with Zen Grid Tabular Encoding for Large Language Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在 LLM 驱动的应用中，结构化数据（如数据库结果、API 负载）通常使用 JSON 序列化。然而，JSON 在处理表格数据（对象数组）时存在严重的冗余：每一行都会重复相同的键名（Key）。
*   **核心问题**：这种冗余导致 Token 消耗随行数线性增长，直接增加了 API 调用成本，并挤占了 LLM 有限的上下文窗口。
*   **整体含义**：论文提出了一种名为 **JTON** 的 JSON 超集，旨在通过一种更紧凑的表格编码方式（Zen Grid），在不损失类型信息的前提下，大幅提升 Token 利用效率。

### 2. 论文提出的方法论
*   **核心思想（Zen Grid）**：将表格数据的列头（Headers）提取到首行，后续行仅保留数值，并使用分号（`;`）分隔行，逗号（`,`）分隔列。
*   **关键技术细节**：
    *   **语法结构**：`[行数: 键1, 键2; 值1, 值2; 值3, 值4]`。例如 `[2: id, name; 1, "Alice"; 2, "Bob"]` 等价于 JSON 的对象数组。
    *   **JSON 扩展**：支持未加引号的键、行/块注释、以及 IEEE 754 特殊值（`NaN`, `Infinity`）。
    *   **Token 优化选项**：
        *   `bare_strings`：符合标识符规则的字符串值可省略引号。
        *   `implicit_null`：空单元格自动解析为 `null`。
    *   **节省公式**：$\Delta T = (n - 1) \cdot k \cdot (\bar{t}_h + t_{struct})$。其中 $n$ 为行数，$k$ 为列数，$\bar{t}_h$ 为键名平均 Token 数。
*   **算法与实现**：使用 Rust 编写并提供 PyO3 绑定。采用 **SIMD（AVX2/AVX-512）** 进行结构化扫描，通过位掩码快速定位分隔符，解析速度比 Python 原生 `json` 模块快 1.2–1.6 倍。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：员工记录、产品库存、服务器指标（5 到 1000 行）。
    *   **真实领域数据**：涵盖 Twitter API、GitHub 事件、CITM 票务、金融交易、天气观测、医疗实验、物流运输等 7 个领域。
*   **Benchmark（对比方法）**：
    *   **格式对比**：JSON (Pretty/Compact)、CSV、Markdown 表格、YAML、TOON。
    *   **模型理解测试**：选取 10 个 LLM（包括 GPT-5 系列、Gemini 3 Pro、Llama 3.3/4、Qwen3、Kimi K2 等），测试查找、聚合、过滤、比较和计数五类任务。
    *   **模型生成测试**：选取 12 个 LLM，测试其在零样本和少样本下生成 JTON 格式的能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的训练硬件（如 GPU 型号或数量），因为该研究侧重于推理阶段的格式效率和模型理解能力。
*   **实验成本**：实验涉及大量商业模型 API 调用（如 OpenAI、Google、Anthropic 等），总计进行了 700 次理解测试调用和 144 次生成测试调用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **Token 效率**：在不同行数（最高 1000 行）和 7 个真实数据集上进行了详尽测量。
    *   **理解力测试**：10 个模型 $\times$ 7 个数据集 $\times$ 5 种问题类型 $\times$ 2 种格式 = 700 组测试。
    *   **生成测试**：12 个模型 $\times$ 6 种难度任务 $\times$ 2 种提示策略 = 144 组测试。
*   **充分性评价**：实验设计非常充分且具有客观性。它不仅对比了 Token 数量，还深入探讨了模型在不同任务类型下的准确率变化，并提供了 683 个向量的测试套件以确保解析器的鲁棒性。

### 6. 论文的主要结论与发现
*   **Token 节省显著**：在真实数据上，JTON 比紧凑型 JSON 平均节省 **28.5%** 的 Token，配合 `bare_strings` 可达 **32.2%**，最高可达 **61%**（如 Twitter 数据）。
*   **理解力保持稳定**：10 个模型的平均准确率净增 **0.3 个百分点**。这意味着 JTON 在大幅减少 Token 的同时，并未损害模型的理解能力。
*   **生成能力极强**：所有测试的 12 个模型在生成 JTON 格式时均达到了 **100% 的语法有效性**，证明该格式极易被 LLM 学习。
*   **性能优势**：基于 Rust 的解析器在处理大型文件时表现优于 Python 标准库，且序列化速度提升高达 3.7 倍。

### 7. 优点
*   **严格兼容性**：作为 JSON 的超集，现有的 JSON 数据在 JTON 解析器中完全可用。
*   **类型安全**：与 CSV 不同，JTON 保留了 JSON 的完整类型系统（布尔、空值、嵌套结构）。
*   **端到端优化**：不仅减少了输入 Token（降低成本），也证明了输出生成的可行性，适合构建闭环的 Agent 管道。
*   **高性能实现**：利用 SIMD 技术解决了自定义格式解析可能带来的性能瓶颈。

### 8. 不足与局限
*   **结构依赖性**：JTON 的优势仅体现在“对象数组”这类表格化数据上，对于深度嵌套或异构的 JSON 数据，节省效果几乎为零。
*   **模型差异**：虽然平均准确率持平，但不同模型表现不一（如 GPT-5.1 在 JTON 上准确率下降了 8.6 pp），用户需针对特定模型进行基准测试。
*   **生态缺失**：目前缺乏成熟的编辑器插件、语法高亮和模式验证工具（如 JSON Schema 的对应物），推广门槛较高。

（完）
