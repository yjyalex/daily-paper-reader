---
title: "EvolveRouter: Co-Evolving Routing and Prompt for Multi-Agent Question Answering"
title_zh: EvolveRouter：面向多智能体问答的路由与提示词协同演化
authors: "Jiatan Huang, Zheyuan Zhang, Kaiwen Shi, Yanfang Ye, Chuxu Zhang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05149v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体问答中的路由与提示协同演化
tldr: 针对多智能体问答中路由方法受限于固定智能体池和僵化协作模式的问题，本文提出 EvolveRouter 框架。该框架通过闭环协同进化机制，将基于图的查询路由与针对性指令精炼相结合，使路由诊断引导智能体改进，同时精炼后的智能体为路由提供更清晰的监督。此外，引入自适应推理策略，根据路由权重的答案一致性动态确定协作规模。实验表明，该方法在五个基准测试中均优于 SOTA，显著提升了多智能体推理的性能与效率。
selection_source: fresh_fetch
motivation: 现有路由方法通常在固定的智能体池上优化且缺乏灵活的协作机制，无法根据具体查询动态调整参与智能体的数量或提升智能体自身素质。
method: 提出一种协同进化框架，结合了图路由与指令精炼的闭环过程，并利用路由加权的答案一致性实现动态的自适应推理策略。
result: 在五个问答基准测试中，EvolveRouter 在 F1 和精确匹配指标上均一致超越了现有的最先进路由基准方法。
conclusion: 通过协同优化路由与智能体指令，并引入自适应协作规模，可以有效增强多智能体系统的推理能力和运行效率。
---

## 摘要
大语言模型智能体通常表现出互补的优势，这使得路由成为多智能体问答中一种极具前景的方法。然而，现有的路由方法在两个重要方面仍存在局限性：它们通常在固定的智能体池上进行优化，而不改进智能体本身；且往往依赖于僵化的协作方案，无法根据查询动态调整参与智能体的数量。我们提出了 EvolveRouter，这是一个可训练的框架，通过共同提升智能体质量和协作结构来解决上述局限性。首先，EvolveRouter 在闭环协同演化过程中将基于图的查询路由与针对性的指令精炼相结合，使得路由器的诊断能够指导智能体的改进，而精炼后的智能体则为路由提供更清晰的监督信号。其次，它引入了一种自适应推理策略，通过路由器加权的答案一致性动态确定每个查询的有效协作规模。这些设计共同实现了更强大且更高效的多智能体推理。在五个问答基准测试上的实验表明，EvolveRouter 在 F1 分数和精确匹配（Exact Match）方面均持续优于最先进的路由基准，进一步的分析也证实了闭环精炼和自适应协作的优势。

## Abstract
Large language model agents often exhibit complementary strengths, making routing a promising approach for multi-agent question answering. However, existing routing methods remain limited in two important ways: they typically optimize over a fixed pool of agents without improving the agents themselves, and they often rely on rigid collaboration schemes that cannot adapt the number of participating agents to the query. We propose EvolveRouter, a trainable framework that addresses both limitations by jointly improving agent quality and collaboration structure. First, EvolveRouter couples graph-based query routing with targeted instruction refinement in a closed-loop co-evolution process, allowing router diagnostics to guide agent improvement while refined agents provide cleaner supervision for routing. Second, it introduces an adaptive inference strategy that dynamically determines the effective collaboration size for each query through router-weighted answer agreement. Together, these designs enable more capable and more efficient multi-agent reasoning. Experiments on five question answering benchmarks show that EvolveRouter consistently outperforms SOTA routing baselines in both F1 and exact match, while further analysis confirms the benefits of closed-loop refinement and adaptive collaboration.

---

## 论文详细总结（自动生成）

以下是对论文《EvolveRouter: Co-Evolving Routing and Prompt for Multi-Agent Question Answering》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）生态的发展，针对特定任务选择最佳模型和提示词策略（即“路由”）成为挑战。现有路由方法存在两大局限：
*   **智能体行为固定：** 现有方法仅优化“调用哪个智能体”，而忽略了智能体本身的提示词（Prompt）可能并非最优。微小的指令差异会显著影响性能。
*   **协作模式僵化：** 现有系统通常要么只选一个智能体，要么聚合固定数量的智能体，无法根据查询的难易程度动态调整参与协作的智能体规模，导致计算资源浪费或性能下降。
**EvolveRouter** 旨在通过“路由”与“提示词”的协同演化，同时提升智能体个体能力和群体协作效率。

### 2. 方法论：核心思想与关键技术
EvolveRouter 包含三个核心阶段：
*   **基于知识图谱的路由（RouterGNN）：** 将查询（Query）、实体（Entity）和智能体（Agent）构建为异构知识图谱。利用异构图神经网络（HGT）学习查询与智能体之间的匹配得分，输出路由分布 $p(a|q)$。
*   **闭环协同演化（Co-Evolution）：**
    *   **诊断：** 在路由训练过程中收集智能体的失败模式和路由权重。
    *   **优先级排序：** 结合智能体的错误率（Severity）和路由器对其的依赖程度（Weight）计算优先级，优先改进那些“路由器想用但表现不好”的智能体。
    *   **指令精炼：** 使用重写 LLM（如 GPT-4o-mini）根据失败案例修改提示词，并通过验证门（Validation Gate）确保改进有效。
    *   **迭代更新：** 更新提示词后，路由器在新的智能体输出上进行微调，形成闭环。
*   **自适应推理策略（Adaptive Inference）：** 
    *   不再固定使用 $K$ 个智能体，而是按路由排名依次调用智能体。
    *   计算**路由器加权的答案一致性得分**。当得分超过阈值 $\tau$ 且满足最小调用数时停止，实现按需协作。

### 3. 实验设计
*   **数据集：** 涵盖 5 个主流问答基准：2WikiMultihopQA、HotpotQA（多跳推理）、NewsQA、TriviaQA（事实理解）和 NGQA（领域特定-营养学）。附录中还增加了 GLEN-Bench。
*   **智能体池：** 由 4 种底座模型（Llama-3-8B, Qwen2.5-7B, Mixtral-8x7B, gpt-oss-20b）与 6 种角色（Raw, CoT, SC, MAD, ReAct, Summary）交叉组合，共 24 个智能体。
*   **对比方法：** 
    *   启发式：多数投票（Majority Vote）、最佳智能体（Best Agent）等。
    *   传统路由：RouteLLM, LLM-Blender, HybridLLM 等。
    *   结构感知路由：GraphRouter, AgentRouter（SOTA 基准）。

### 4. 资源与算力
*   **训练开销：** 论文提到 RouterGNN 的训练在**单张 GPU** 上耗时不到 5 分钟。
*   **LLM 调用：** 训练阶段对 500 个样本进行全智能体评估（12,000 次调用）。提示词精炼阶段仅需对修改的 2-3 个智能体重新调用，具有高效的缓存机制。
*   **具体型号：** 文中未明确给出 GPU 的具体型号（如 A100 或 H100），但强调了其轻量级训练的特性。

### 5. 实验数量与充分性
*   **实验规模：** 每个数据集采样 500 训练 / 100 验证 / 100 测试。实验在 5-6 个不同性质的数据集上运行，并进行了 3 次随机种子实验取平均值。
*   **消融实验：** 详细对比了“无提示词优化（PO）”、“无自适应 K”以及“从头训练路由器 vs 增量微调”的效果。
*   **充分性评价：** 实验设计较为全面，涵盖了跨领域迁移性分析、计算成本分析和定性案例研究。虽然测试集规模（100）相对较小，但通过多维度对比证明了结果的稳健性。

### 6. 主要结论与发现
*   **性能领先：** EvolveRouter 在所有基准测试中均优于 SOTA 路由方法，尤其在 TriviaQA 上 F1 提升了 7.08%。
*   **协同效应：** 提示词优化是性能提升的主要来源，而自适应 K 策略在保持性能的同时，将推理成本降低了 **69% 至 88%**。
*   **路由引导的重要性：** 实验证明，优先修复“路由器权重高”的智能体比单纯修复“表现最差”的智能体更有效。
*   **知识保留：** 在协同演化过程中，保留路由器的历史权重（增量微调）比从头训练效果更好。

### 7. 优点
*   **闭环设计：** 首次将路由器的诊断信号直接用于指导智能体提示词的自动化改进，实现了系统级的自我进化。
*   **效率平衡：** 通过自适应停止准则，解决了多智能体系统推理成本高昂的痛点。
*   **结构化建模：** 利用知识图谱捕捉查询、实体与智能体间的复杂关系，比扁平化的路由方法更精准。

### 8. 不足与局限
*   **样本规模限制：** 实验主要基于较小规模的采样集（100-500 样本），在大规模工业级数据上的表现尚待验证。
*   **重写模型依赖：** 提示词精炼的效果高度依赖于重写 LLM（如 GPT-4o）的能力，若重写模型较弱，可能无法产生有效的指令改进。
*   **任务局限：** 目前仅在问答（QA）任务上验证，对于长文本生成、代码编写等更具发散性的任务，答案一致性（Agreement）可能难以定义。

（完）
