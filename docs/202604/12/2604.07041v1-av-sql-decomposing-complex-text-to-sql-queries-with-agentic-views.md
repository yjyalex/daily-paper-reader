---
title: "AV-SQL: Decomposing Complex Text-to-SQL Queries with Agentic Views"
title_zh: AV-SQL：利用智能体视图分解复杂的 Text-to-SQL 查询
authors: "Minh Tam Pham, Trinh Pham, Tong Chen, Hongzhi Yin, Quoc Viet Hung Nguyen, Thanh Tam Nguyen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07041v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 将复杂的Text-to-SQL分解为专门的LLM智能体流水线
tldr: 针对复杂Text-to-SQL任务中大模式和多步推理的挑战，本文提出AV-SQL框架。该框架引入“代理视图（Agentic Views）”概念，利用专门的LLM代理将复杂查询分解为封装中间逻辑的公用表表达式（CTE）。AV-SQL通过重写、视图生成及协同生成三个阶段，有效解决了长上下文和模式链接难题，在Spider 2.0等多个基准测试中取得了领先性能。
selection_source: fresh_fetch
motivation: 现有Text-to-SQL方法在处理大规模数据库模式和复杂多步推理查询时，常面临上下文超限及生成非执行SQL的问题。
method: 采用多代理流水线，通过重写查询、生成代理视图（CTE）来过滤模式并封装逻辑，最后由规划、生成和修正代理协作完成SQL。
result: "在Spider 2.0基准测试中取得70.38%的执行准确率，并在Spider、BIRD和KaggleDBQA等数据集上均表现出极强的竞争力。"
conclusion: 通过模块化分解和代理视图机制，AV-SQL显著增强了LLM处理大规模、高难度Text-to-SQL任务的能力。
---

## 摘要
Text-to-SQL 是将自然语言查询转换为给定数据库的可执行 SQL 的任务，使非专家用户无需手动编写 SQL 即可访问结构化数据。尽管在大语言模型（LLM）的推动下取得了快速进展，但现有方法在现实场景的复杂查询中仍然面临挑战，这些场景下的数据库模式（schema）规模庞大，且问题需要对多个相互关联的表进行多步推理。在这种情况下，提供完整的模式通常会超出上下文窗口，而单次生成往往会因为语法错误和错误的模式链接而产生不可执行的 SQL。为了应对这些挑战，我们引入了 AV-SQL，这是一个将复杂的 Text-to-SQL 分解为专门 LLM 智能体流水线的框架。AV-SQL 的核心是智能体视图（agentic views）的概念：由智能体生成的公用表表达式（CTE），用于封装中间查询逻辑并从大型模式中过滤相关的模式元素。AV-SQL 分三个阶段运行：(1) 重写智能体压缩并澄清输入查询；(2) 视图生成智能体处理模式块以生成智能体视图；(3) 规划、生成和修正智能体协作将这些视图组合成最终的 SQL 查询。广泛的实验表明，AV-SQL 在具有挑战性的 Spider 2.0 基准测试中实现了 70.38% 的执行准确率，优于最先进的基准模型，同时在标准数据集上保持了竞争力，在 Spider 上达到 85.59%，在 BIRD 上达到 72.16%，在 KaggleDBQA 上达到 63.78%。我们的源代码可在 https://github.com/pminhtam/AV-SQL 获取。

## Abstract
Text-to-SQL is the task of translating natural language queries into executable SQL for a given database, enabling non-expert users to access structured data without writing SQL manually. Despite rapid advances driven by large language models (LLMs), existing approaches still struggle with complex queries in real-world settings, where database schemas are large and questions require multi-step reasoning over many interrelated tables. In such cases, providing the full schema often exceeds the context window, while one-shot generation frequently produces non-executable SQL due to syntax errors and incorrect schema linking. To address these challenges, we introduce AV-SQL, a framework that decomposes complex Text-to-SQL into a pipeline of specialized LLM agents. Central to AV-SQL is the concept of agentic views: agent-generated Common Table Expressions (CTEs) that encapsulate intermediate query logic and filter relevant schema elements from large schemas. AV-SQL operates in three stages: (1) a rewriter agent compresses and clarifies the input query; (2) a view generator agent processes schema chunks to produce agentic views; and (3) a planner, generator, and revisor agent collaboratively compose these views into the final SQL query. Extensive experiments show that AV-SQL achieves 70.38% execution accuracy on the challenging Spider 2.0 benchmark, outperforming state-of-the-art baselines, while remaining competitive on standard datasets with 85.59% on Spider, 72.16% on BIRD and 63.78% on KaggleDBQA. Our source code is available at https://github.com/pminhtam/AV-SQL.

---

## 论文详细总结（自动生成）

这是一份关于论文《AV-SQL: Decomposing Complex Text-to-SQL Queries with Agentic Views》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管大语言模型（LLM）在 Text-to-SQL 任务上取得了进展，但在处理现实世界中的复杂查询时仍面临三大挑战：
    1.  **长上下文过载**：现实数据库模式（Schema）可能包含数千个列，超出 LLM 的上下文窗口，导致注意力分散。
    2.  **模式链接困难**：在庞大的模式中准确找到对应的表和列极具挑战。
    3.  **SQL 复杂性与错误传播**：复杂查询涉及多步推理和多表连接，单次生成容易产生语法错误或逻辑错误，且难以调试。
*   **核心问题**：如何在不超出上下文限制的前提下，提高大规模数据库模式下复杂 SQL 生成的准确性和鲁棒性？

### 2. 方法论：核心思想与关键技术
AV-SQL 提出了一个多阶段智能体框架，核心思想是将复杂查询分解为可验证的**智能体视图（Agentic Views）**。
*   **预处理阶段**：
    *   **模式压缩与切块**：通过聚类相似表/列压缩模式，并将其切分为较小的块（Chunks），确保每个块都能放入 LLM 的上下文。
    *   **数据库值预处理**：使用 MinHash LSH 进行离线索引和在线检索，解决自然语言与数据库值之间的歧义。
*   **第一阶段：问题重写 (Question Rewriting)**
    *   由 `LLM Rewriter` 智能体将原始问题和外部知识（K）转化为清晰、显式的任务描述，过滤无关信息。
*   **第二阶段：智能体视图生成 (Agent View Generation)**
    *   **核心技术**：多个 `View Generator` 智能体并行处理模式块，生成基于 **CTE（公用表表达式）** 的中间视图。
    *   **执行引导修复**：生成的 CTE 会在数据库上实际执行，根据报错信息进行迭代修复。
    *   **一致性检查**：确保 CTE 逻辑与 JSON 格式的模式选择一致。
*   **第三阶段：SQL 生成 (SQL Generation)**
    *   由 `Planner`（规划）、`SQL Generator`（生成）和 `Revisor`（修正）三个智能体协作。
    *   利用前一阶段过滤后的模式和验证过的 CTE 视图，合成最终的可执行 SQL。

### 3. 实验设计
*   **数据集与基准 (Benchmarks)**：
    *   **Spider 2.0-Snow**：针对企业级大规模模式（平均 800 列）的最新挑战性基准。
    *   **标准数据集**：Spider (跨域多表)、BIRD (专业领域大数据量)、KaggleDBQA (真实 Web 数据库)。
*   **对比方法**：
    *   单候选方法：DIN-SQL, DAIL-SQL, MAC-SQL, CodeS 等。
    *   多候选/智能体方法：CHESS, Alpha-SQL, ReForCE, AutoLink, Spider-Agent 等。
*   **评估指标**：执行准确率 (Execution Accuracy, EX)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型配置**：实验使用了多种 LLM 作为后端，包括 Gemini-3-Pro-Preview, GPT-5-Mini, Llama-3.3-70B 和 Qwen2.5-32B-Coder。
*   **推理成本**：文中提供了成本分析，指出“视图生成阶段”占据了约 77.2% 的 Token 消耗和 55.5% 的运行时间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在四个主流数据集上进行了全面测试。
    *   **消融实验**：分别验证了重写器、规划器、修正器、执行反馈以及 CTE 视图对结果的贡献。
    *   **扩展性实验**：测试了多候选（Multi-candidate）设置下的性能提升。
    *   **混合配置实验**：探讨了不同智能体使用不同 LLM 后端的性能影响。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同规模的模型（从 32B 到旗舰级模型）和不同难度的任务，验证了框架的普适性。错误分析（Error Analysis）部分深入探讨了过滤和聚合逻辑的失效模式，具有较高的学术参考价值。

### 6. 主要结论与发现
*   **性能突破**：AV-SQL 在 Spider 2.0-Snow 上取得了 **70.38%** 的 EX，远超之前的 SOTA 方法（如 ReForCE 的 35.83%）。
*   **CTE 的价值**：将复杂逻辑分解为可执行的 CTE 视图，能显著降低模式链接错误，并使调试过程模块化。
*   **执行反馈的重要性**：消融实验显示，移除执行反馈会导致性能大幅下降（在 Spider 2.0 上下降约 13-24%）。
*   **零样本能力**：AV-SQL 在完全零样本（Zero-shot）设置下表现强劲，无需示例即可处理复杂模式。

### 7. 优点
*   **模块化设计**：通过 CTE 将“模式过滤”与“逻辑推理”结合，解决了长上下文难题。
*   **高鲁棒性**：引入多轮执行引导的修复机制，确保了中间步骤和最终结果的可执行性。
*   **模式压缩技术**：创新的模式压缩方法有效处理了具有数千个相似列的企业级数据库。
*   **灵活性**：支持单候选生成，也支持通过增加候选数量进一步提升性能。

### 8. 不足与局限
*   **推理延迟与成本**：由于涉及多个智能体和多轮迭代修复，其 Token 消耗和响应时间显著高于传统的单次生成方法。
*   **特定逻辑仍有挑战**：错误分析显示，复杂的“过滤条件（Filtering）”和“聚合逻辑（Aggregation）”仍占错误总数的 70% 以上，框架在极高难度的语义理解上仍有提升空间。
*   **评估偏差风险**：在 Spider/BIRD 等数据集上，修正器有时会因为调整列顺序而导致 EX 判定失败（尽管语义正确），这反映了现有评估指标的局限性。

（完）
