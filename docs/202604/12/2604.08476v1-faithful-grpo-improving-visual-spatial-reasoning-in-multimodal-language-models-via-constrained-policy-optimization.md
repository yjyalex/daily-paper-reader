---
title: "Faithful GRPO: Improving Visual Spatial Reasoning in Multimodal Language Models via Constrained Policy Optimization"
title_zh: 忠实 GRPO：通过约束策略优化提升多模态语言模型中的视觉空间推理能力
authors: "Sai Srinivas Kancheti, Aditya Kanade, Rohit Sinha, Vineeth N Balasubramanian, Tanuja Ganu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08476v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过约束策略优化和GRPO改进视觉空间推理
tldr: 本研究针对多模态推理模型在强化学习中出现的思维链逻辑不一致及视觉接地性差的问题，提出了 Faithful GRPO (FGRPO)。该方法利用拉格朗日对偶上升法将一致性和接地性作为约束引入 GRPO 优化过程。在 Qwen2.5-VL 上的实验证明，FGRPO 在显著降低推理错误率的同时提升了空间推理准确率，实现了更高质量的忠实推理。
selection_source: fresh_fetch
motivation: 发现多模态模型在通过强化学习提升准确率时，往往伴随着思维链逻辑不一致和视觉描述失真的副作用。
method: 提出 FGRPO 算法，通过拉格朗日对偶上升在优势计算中自适应地加入逻辑一致性和视觉接地性约束。
result: "实验显示 FGRPO 将推理不一致率从 24.5% 降至 1.7%，视觉接地得分提升 13%，并超越了标准 GRPO 的准确率。"
conclusion: 通过约束优化强制执行推理的忠实性，不仅能改善思维链质量，还能有效促进多模态模型最终答案的准确性。
---

## 摘要
使用可验证奖励强化学习 (RLVR) 训练的多模态推理模型 (MRM) 在视觉推理基准测试中表现出更高的准确率。然而，我们观察到准确率的提升往往以牺牲推理质量为代价：生成的思维链 (CoT) 轨迹经常与最终答案不一致，且在视觉证据上的接地性 (grounding) 较差。我们在七个具有挑战性的真实世界空间推理基准测试中系统地研究了这一现象，发现它影响了当代 MRM（如 ViGoRL-Spatial、TreeVGR）以及我们使用标准组相对策略优化 (GRPO) 训练的模型。我们从两个互补的维度刻画了 CoT 推理质量：“逻辑一致性”（CoT 是否蕴含最终答案？）和“视觉接地性”（每个推理步骤是否准确描述了图像中的物体、属性和空间关系？）。为了解决这一问题，我们提出了 Faithful GRPO (FGRPO)，这是 GRPO 的一种变体，通过拉格朗日对偶上升将一致性和接地性作为约束条件强制执行。FGRPO 将批次级的一致性和接地性约束纳入组内的优势计算中，在优化过程中自适应地调整约束的相对重要性。我们在七个空间数据集上，基于 Qwen2.5-VL-7B 和 3B 骨干网络对 FGRPO 进行了评估。结果表明，FGRPO 显著提高了推理质量，将不一致率从 24.5% 降低到 1.7%，并将视觉接地性得分提高了 13%。此外，与普通 GRPO 相比，它还提高了最终答案的准确率，证明了忠实的推理能够带来更好的答案。

## Abstract
Multimodal reasoning models (MRMs) trained with reinforcement learning with verifiable rewards (RLVR) show improved accuracy on visual reasoning benchmarks. However, we observe that accuracy gains often come at the cost of reasoning quality: generated Chain-of-Thought (CoT) traces are frequently inconsistent with the final answer and poorly grounded in the visual evidence. We systematically study this phenomenon across seven challenging real-world spatial reasoning benchmarks and find that it affects contemporary MRMs such as ViGoRL-Spatial, TreeVGR as well as our own models trained with standard Group Relative Policy Optimization (GRPO). We characterize CoT reasoning quality along two complementary axes: "logical consistency" (does the CoT entail the final answer?) and "visual grounding" (does each reasoning step accurately describe objects, attributes, and spatial relationships in the image?). To address this, we propose Faithful GRPO (FGRPO), a variant of GRPO that enforces consistency and grounding as constraints via Lagrangian dual ascent. FGRPO incorporates batch-level consistency and grounding constraints into the advantage computation within a group, adaptively adjusting the relative importance of constraints during optimization. We evaluate FGRPO on Qwen2.5-VL-7B and 3B backbones across seven spatial datasets. Our results show that FGRPO substantially improves reasoning quality, reducing the inconsistency rate from 24.5% to 1.7% and improving visual grounding scores by +13%. It also improves final answer accuracy over simple GRPO, demonstrating that faithful reasoning enables better answers.

---

## 论文详细总结（自动生成）

以下是对论文《Faithful GRPO: Improving Visual Spatial Reasoning in Multimodal Language Models via Constrained Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然通过可验证奖励的强化学习（RLVR）显著提升了多模态模型（MLM）的答案准确率，但研究者发现这种提升往往伴随着“推理质量”的退化。
*   **核心问题**：模型生成的思维链（CoT）经常出现两种失效模式：
    1.  **逻辑不一致性**：推理过程指向一个结论，但最终答案却突然转向另一个（“逻辑跳跃”）。
    2.  **视觉失真（Ungroundedness）**：推理步骤中描述的物体、属性或空间关系与图像实际内容不符（“睁眼说瞎话”）。
*   **研究目标**：在不牺牲准确率的前提下，强制模型生成既符合逻辑又忠实于视觉证据的推理过程。

### 2. 方法论
FGRPO 提出将推理质量作为**硬约束**而非简单的奖励项，采用**约束策略优化**框架。
*   **核心思想**：利用拉格朗日对偶上升（Lagrangian Dual Ascent）方法，将准确率最大化问题转化为在满足一致性和接地性阈值条件下的优化问题。
*   **关键技术细节**：
    *   **可验证奖励设计**：
        *   **一致性奖励 ($R_C$)**：利用 LLM 评判推理文本是否逻辑上蕴含最终答案。
        *   **语义接地奖励 ($R_S$)**：利用 VLM 逐句评判推理步骤是否符合图像事实。
        *   **空间接地奖励 ($R_G$)**：针对带框标注的数据，计算预测框与真值的 CIoU。
    *   **解耦归一化（Decoupled Normalization）**：在 GRPO 的组内优势计算时，对任务奖励和各个约束信号分别进行独立归一化，防止高方差信号掩盖其他信号。
    *   **拉格朗日乘子更新**：通过批次统计量自适应调整乘子 $\lambda$。当约束未达标时增加压力（权重），达标时减小压力，实现自动化的课程学习。

### 3. 实验设计
*   **数据集/场景**：涵盖 7 个空间推理基准测试，包括 CVBench-2D/3D、MindCube（多图 3D）、MMVP、OmniSpatial、RealWorldQA 和 SAT-Real。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen2.5-VL-3B/7B-Instruct。
    *   **对比 MRM 模型**：R1-OneVision、Vision-R1、ViGoRL-Spatial、TreeVGR、VL-Rethinker。
    *   **闭源模型参考**：GPT-4o、GPT-5-nano。
    *   **消融实验**：对比了标准 GRPO（仅任务奖励）、加法/乘法奖励融合方案、以及固定乘子 vs. 自适应乘子方案。

### 4. 资源与算力
*   **训练算力**：
    *   **SFT 阶段**：使用 4 张 NVIDIA A100 (80GB) GPU，耗时约 12 小时。
    *   **RL 阶段**：使用 **8 张 NVIDIA H100 GPU** 进行训练。
*   **推理/评判算力**：使用 4 张 A100 进行推理。在线评判（训练中）使用 Qwen3-VL-30B，离线评估使用 GPT-5.4。

### 5. 实验数量与充分性
*   **实验规模**：在 3B 和 7B 两个参数量级上验证，覆盖了从 2D 关系到复杂 3D 空间建模的 7 个维度。
*   **充分性**：
    *   进行了详尽的消融实验，证明了“解耦归一化”和“自适应约束”的必要性。
    *   通过 GPT-5 对本地评判器进行了验证（一致性系数 $\kappa=0.997$），确保了自动评估的客观性。
    *   展示了训练全过程的准确率与不一致率的变化曲线，直观反映了 FGRPO 如何克服 RLVR 的副作用。

### 6. 主要结论与发现
*   **推理质量飞跃**：FGRPO 将推理不一致率从 **24.5% 降低至 1.7%**，视觉接地得分提升了 **13%**。
*   **准确率同步提升**：在 7B 模型上，平均准确率从 65.17% 提升至 **67.16%**，证明了“忠实的推理”与“正确的答案”是互补而非对立的。
*   **解决奖励破解**：标准 GRPO 在训练后期准确率上升但推理质量下降（Reward Hacking），而 FGRPO 成功抑制了这一趋势。

### 7. 优点
*   **算法优雅**：引入拉格朗日对偶上升解决了多目标优化中手动调参（Reward Shaping）的痛苦。
*   **解耦设计**：解耦归一化解决了 GRPO 框架下稀疏或恒定奖励信号被抵消的技术难题。
*   **实用性强**：不仅提升了性能，更增强了模型的可解释性和可信度，使其在真实空间导航等任务中更安全。

### 8. 不足与局限
*   **评判成本**：训练过程中需要调用 VLM/LLM 作为评判器，虽然使用了本地模型，但相比规则匹配仍增加了计算开销。
*   **数据依赖**：空间接地约束 ($R_G$) 依赖于带边界框标注的数据，这在某些通用推理场景中可能难以获取。
*   **任务局限**：目前主要集中在视觉空间推理，尚未验证该框架在数学、代码等其他复杂推理领域的泛化效果。

（完）
