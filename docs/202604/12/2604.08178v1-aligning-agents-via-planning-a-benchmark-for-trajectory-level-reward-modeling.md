---
title: "Aligning Agents via Planning: A Benchmark for Trajectory-Level Reward Modeling"
title_zh: 通过规划对齐智能体：轨迹级奖励建模基准测试
authors: "Jiaxuan Wang, Yulan Hu, Wenjin Yang, Zheng Pan, Xin Li, Lan-Zhe Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08178v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 智能体大模型的轨迹级奖励建模
tldr: 随着大模型演进为具备工具调用能力的智能体，传统的奖励模型（RM）缺乏针对复杂规划场景的评估基准。本文提出 Plan-RewardBench，这是一个轨迹级偏好基准，涵盖安全拒绝、工具无关性、复杂规划和错误恢复四大任务。通过构建正向轨迹与多种扰动生成的负向轨迹，该基准评估了不同类型 RM 在区分优劣轨迹方面的能力。研究发现，现有模型在长程任务中表现欠佳，强调了开发专用智能体奖励模型的必要性。
selection_source: fresh_fetch
motivation: 针对智能体在工具集成环境下的自主规划与推理能力，目前缺乏专门评估奖励模型轨迹级判别能力的基准。
method: 构建包含四类典型任务的 Plan-RewardBench，利用多模型演练、规则扰动和最小编辑技术生成高难度的正负偏好轨迹对。
result: 评测显示生成式、判别式和 LLM 评判模型在处理长程轨迹时准确率显著下降，且存在多种典型的失效模式。
conclusion: 实验结果揭示了现有奖励模型在智能体对齐方面的短板，为构建轨迹级偏好数据和训练专用智能体奖励模型提供了参考。
---

## 摘要
在经典的基于人类反馈的强化学习（RLHF）中，奖励模型（RM）是模型对齐的基础信号提供者。随着大语言模型演进为具备自主工具调用和复杂推理能力的智能体系统，奖励建模范式面临着前所未有的挑战——最显著的是缺乏专门设计用于评估工具集成环境下奖励模型能力的基准测试。为了填补这一空白，我们提出了 Plan-RewardBench，这是一个轨迹级偏好基准测试，旨在评估评判者在复杂工具使用场景中区分优选轨迹与干扰项智能体轨迹的能力。Plan-RewardBench 涵盖了四个具有代表性的任务族：(i) 安全拒绝、(ii) 工具无关/不可用、(iii) 复杂规划以及 (iv) 鲁棒错误恢复。该基准测试包含经过验证的正向轨迹，以及通过多模型自然展开、基于规则的扰动和最小编辑 LLM 扰动构建的易混淆硬负样本。我们在统一的双向对比协议下，对代表性的奖励模型（生成式、判别式和 LLM-as-Judge）进行了基准测试，报告了不同轨迹长度和任务类别下的准确率趋势。此外，我们还对普遍存在的失败模式进行了诊断分析。我们的结果表明，这三类评估器都面临着巨大的挑战，其性能在长时程轨迹上急剧下降，这强调了对智能体轨迹级奖励建模进行专门训练的必要性。最终，Plan-RewardBench 旨在既作为一个实用的评估套件，又作为构建智能体规划偏好数据的可重用蓝图。

## Abstract
In classical Reinforcement Learning from Human Feedback (RLHF), Reward Models (RMs) serve as the fundamental signal provider for model alignment. As Large Language Models evolve into agentic systems capable of autonomous tool invocation and complex reasoning, the paradigm of reward modeling faces unprecedented challenges--most notably, the lack of benchmarks specifically designed to assess RM capabilities within tool-integrated environments. To address this gap, we present Plan-RewardBench, a trajectory-level preference benchmark designed to evaluate how well judges distinguish preferred versus distractor agent trajectories in complex tool-using scenarios. Plan-RewardBench covers four representative task families -- (i) Safety Refusal, (ii) Tool-Irrelevance / Unavailability, (iii) Complex Planning, and (iv) Robust Error Recovery -- comprising validated positive trajectories and confusable hard negatives constructed via multi-model natural rollouts, rule-based perturbations, and minimal-edit LLM perturbations. We benchmark representative RMs (generative, discriminative, and LLM-as-Judge) under a unified pairwise protocol, reporting accuracy trends across varying trajectory lengths and task categories. Furthermore, we provide diagnostic analyses of prevalent failure modes. Our results reveal that all three evaluator families face substantial challenges, with performance degrading sharply on long-horizon trajectories, underscoring the necessity for specialized training in agentic, trajectory-level reward modeling. Ultimately, Plan-RewardBench aims to serve as both a practical evaluation suite and a reusable blueprint for constructing agentic planning preference data.

---

## 论文详细总结（自动生成）

### 论文总结：Plan-RewardBench —— 轨迹级奖励建模基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从被动对话者演变为能够自主调用工具和进行复杂推理的**智能体（Agents）**，传统的奖励模型（Reward Models, RMs）面临挑战。现有的 RM 评估基准（如 RewardBench）主要集中在单次回复（Response-level）的偏好上，缺乏对**多轮交互轨迹（Trajectory-level）**的评估，尤其是在工具集成环境下的规划逻辑、工具调用忠实度和错误恢复能力。论文的核心目标是填补这一空白，提出一个专门评估 RM 在复杂智能体规划场景下判别能力的基准。

#### 2. 论文提出的方法论
论文提出了 **Plan-RewardBench**，其核心思想是通过构建高质量的正向轨迹与具有挑战性的“硬负样本”对，测试 RM 的判别准确率。
*   **四大任务族**：
    1.  **安全拒绝（Safety Refusal）**：区分合理的政策性拒绝与不安全的顺从或过度拒绝。
    2.  **工具无关/不可用（Tool-Irrelevance/Unavailability）**：评估智能体在工具无助于任务时是否能诚实告知，而非产生幻觉。
    3.  **复杂规划（Complex Planning）**：评估多步任务分解、参数选择及信息复用的逻辑。
    4.  **鲁棒错误恢复（Robust Error Recovery）**：评估智能体在工具调用失败时是否能诊断原因并尝试有效修复，而非盲目重试。
*   **关键技术细节（数据构造流程）**：
    *   **多源候选生成**：利用 Qwen、DeepSeek 等多种模型进行自然演练（Rollouts）。
    *   **硬负样本构造**：采用**规则扰动**（如篡改约束、注入冗余调用）和**最小编辑 LLM 扰动**（在保持工具输出不变的情况下微调思维链，制造逻辑瑕疵）。
    *   **标签验证**：采用多评委面板（Multi-judge panel）评分、元审查（Meta-review）过滤以及人工审计（Human Audit），确保偏好标签的可靠性。

#### 3. 实验设计
*   **数据集规模**：包含 1171 对经过严格筛选的轨迹偏好对。
*   **评估协议**：采用统一的双向对比（Pairwise）协议，通过 A/B 交换顺序来消除位置偏差。
*   **对比方法**：
    *   **判别式 RM（DRM）**：如 InternLM2-7B-Reward、Skywork-Reward-V2 等点分模型。
    *   **生成式 RM（GRM）**：如 RRM-32B、M-Prometheus-14B 等输出推理过程的模型。
    *   **通用 LLM 评判者（LLM-as-Judge）**：如 GPT-5、DeepSeek-V3、Qwen-Plus 等。
*   **场景划分**：按任务族、轨迹长度（Turn count）和难度（Easy/Hard）进行细分评估。

#### 4. 资源与算力
论文**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长）。这主要是因为该论文侧重于**基准测试（Benchmark）的构建与评估**，而非从头训练一个新的大规模模型。实验涉及的主要是对现有开源和闭源模型进行推理调用。

#### 5. 实验数量与充分性
*   **实验规模**：对 20 多个代表性模型进行了全面评测，涵盖了从 4B 到 200B+ 参数规模的模型。
*   **充分性**：实验不仅报告了总分，还按任务族、输入长度（0-4k 到 32k+ 标记）进行了分层分析。
*   **客观性**：通过人工审计（Cohen’s κ 系数在 0.71-0.86 之间，显示高度一致性）验证了自动标签的准确性；通过 A/B 交换控制了评委模型的偏见，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **长程衰减**：所有评估器在长轨迹（>32k tokens）下的表现均显著下降，部分模型甚至低于随机水平。
*   **能力分化**：判别式 RM 在处理显性错误（如错误日志）时表现尚可，但在需要深层逻辑推理的复杂规划任务中远逊于大型 LLM 评判者。
*   **安全盲点**：在安全拒绝任务中，模型表现极化严重，许多 RM 无法区分“因能力不足而拒绝”与“因安全政策而拒绝”。
*   **启发式偏见**：评判模型普遍存在“努力偏见”（Effort Bias），倾向于给调用工具次数更多（即使是冗余调用）的轨迹打高分。

#### 7. 优点
*   **填补空白**：首次针对智能体轨迹级（而非单步或单回复）的奖励建模提供了系统性的评估工具。
*   **负样本质量高**：通过最小编辑扰动生成的负样本极具迷惑性，能有效检测 RM 是否真正理解规划逻辑，而非依赖表面特征（如长度、格式）。
*   **诊断性强**：提供了详细的失败模式分类（如盲目重试、陈旧约束等），对后续智能体对齐研究有很强的指导意义。

#### 8. 不足与局限
*   **语言与模态限制**：目前仅限于英文和纯文本工具轨迹，未涉及多模态智能体或多语言场景。
*   **样本分布**：安全拒绝类别的样本量相对较小（51 对），虽然一致性高，但覆盖范围可能有限。
*   **工具环境依赖**：基准测试依赖于特定的 MCP（Model Context Protocol）工具注册表，可能无法完全覆盖所有私有或复杂的工业级 API 交互。

（完）
