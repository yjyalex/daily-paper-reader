---
title: "ACF: A Collaborative Framework for Agent Covert Communication under Cognitive Asymmetry"
title_zh: ACF：认知不对称下的智能体隐蔽通信协作框架
authors: "Wansheng Wu, Kaibo Huang, Yukun Wei, Zhongliang Yang, Linna Zhou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08276v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 认知不对称下的智能体协作通信框架
tldr: 针对生成式AI智能体网络中的隐蔽通信问题，本文提出了异步协作框架（ACF）。传统方法依赖编解码器间的认知对称性（即相同的前缀序列），但在动态环境中极易因记忆更新导致同步失效。ACF通过解耦隐蔽通信与语义推理，利用正交的统计层和认知层实现前缀无关的解码，有效解决了认知不对称带来的信道退化问题，在保证语义保真度的同时实现了高可靠的秘密提取。
selection_source: fresh_fetch
motivation: 现有的智能体隐蔽通信方法高度依赖编解码器间的认知对称性，在动态交互导致的记忆差异下极易发生信道退化。
method: 提出ACF框架，通过正交的统计层和认知层将隐蔽通信与语义推理结构化解耦，并采用前缀无关的解码范式。
result: 实验证明ACF在认知不对称环境下表现优异，在维持高语义保真度的同时，实现了可靠的秘密提取和稳健的信息容量。
conclusion: ACF有效解决了智能体网络中的认知不对称挑战，为现代自主智能体协作提供了安全且鲁棒的隐蔽通信新范式。
---

## 摘要
随着生成式人工智能的发展，自主智能体网络为交互式隐蔽通信提供了一种强大的范式。然而，由于智能体通过环境交互动态更新内部记忆，现有方法面临一个关键的结构性漏洞：认知不对称。传统方法要求严格的认知对称性，即编码器和解码器之间必须具有相同的序列前缀。在动态部署中，不可避免的前缀差异会破坏同步，导致严重的信道退化。为了解决认知不对称这一核心挑战，我们提出了非对称协作框架（ACF），该框架通过正交的统计层和认知层，在结构上将隐蔽通信与语义推理相解耦。通过部署由共享隐写配置管理的独立于前缀的解码范式，ACF 消除了对认知对称性的依赖。在真实的记忆增强工作流上的评估表明，在严重的认知不对称下，对称基准方法会遭受严重的信道退化，而 ACF 在语义保真度和隐蔽通信方面均表现出色。它保持了计算不可区分性，实现了具有可证明误差界的可靠秘密提取，并为现代智能体网络提供了稳健的有效信息容量（Effective Information Capacity）保证。

## Abstract
As generative artificial intelligence evolves, autonomous agent networks present a powerful paradigm for interactive covert communication. However, because agents dynamically update internal memories via environmental interactions, existing methods face a critical structural vulnerability: cognitive asymmetry. Conventional approaches demand strict cognitive symmetry, requiring identical sequence prefixes between the encoder and decoder. In dynamic deployments, inevitable prefix discrepancies destroy synchronization, inducing severe channel degradation. To address this core challenge of cognitive asymmetry, we propose the Asymmetric Collaborative Framework (ACF), which structurally decouples covert communication from semantic reasoning via orthogonal statistical and cognitive layers. By deploying a prefix-independent decoding paradigm governed by a shared steganographic configuration, ACF eliminates the reliance on cognitive symmetry. Evaluations on realistic memory-augmented workflows demonstrate that under severe cognitive asymmetry, symmetric baselines suffer severe channel degradation, whereas ACF uniquely excels across both semantic fidelity and covert communication. It maintains computational indistinguishability, enabling reliable secret extraction with provable error bounds, and providing robust Effective Information Capacity guarantees for modern agent networks.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）智能体网络在隐蔽通信中面临的“认知不对称”挑战，提出了一个名为 ACF 的协作框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在自主智能体网络中，智能体通过环境交互（如 RAG 检索、记忆更新）动态改变其内部状态。这导致编码器和解码器之间存在**认知不对称（Cognitive Asymmetry）**，即双方的上下文前缀（Prefix）不一致。
*   **研究背景**：传统的生成式隐写方法（如 DISCOP, METEOR）依赖于“认知对称性”，要求编解码双方拥有完全一致的前缀以同步概率分布。一旦前缀出现微小差异，解码就会彻底失败。
*   **整体含义**：本文旨在打破这种对严格同步的依赖，使智能体在保持自主推理和记忆更新的同时，仍能实现可靠的隐蔽信息传输。

### 2. 论文提出的方法论：ACF 框架
ACF 的核心思想是将生成过程解耦为两个正交的层：
*   **认知推理层（Cognitive Reasoning Layer）**：负责根据智能体的动态记忆生成语义合理的文本分布 $P_M$。
*   **统计通信层（Statistical Communication Layer）**：负责隐蔽信息的嵌入与提取。
*   **关键技术细节**：
    *   **前缀无关的词表划分**：利用预共享密钥 $sk$ 和配置 $\Theta$，将词表 $V$ 确定性地划分为伪随机子集 $V(0)$ 和 $V(1)$。这一过程不依赖于 LLM 的前缀。
    *   **非对称编码（分布置换）**：编码器根据要嵌入的秘密位 $s$，通过累积分布函数（CDF）置换重新排列 Token 顺序，使目标子集 $V(s)$ 占据概率质量的前端。这种方法实现了“零失真”，即生成的 Token 边缘概率与原始模型分布完全一致。
    *   **无模型解码（Model-Free Decoding）**：解码器无需运行 LLM，也无需追踪前缀。它仅根据接收到的 Token 序列，利用霍夫丁不等式（Hoeffding's inequality）进行统计假设检验，从而提取秘密信息。

### 3. 实验设计
*   **模型与数据集**：使用 **Qwen2.5-7B-Instruct** 作为基础模型，在 **LongMemEval** 数据集上进行评估。
*   **实验场景**：
    1.  **孤立差异**：编码器添加私有摘要或解码器截断历史对话。
    2.  **渐进式不对称**：通过截断不同数量的对话轮次来测试鲁棒性。
    3.  **记忆增强（+RET）**：模拟真实的 RAG 场景，智能体从 115k Token 的私有记忆库中检索信息。
*   **对比基准（Benchmark）**：对比了经典的对称隐写方法 **DISCOP** 和 **METEOR**。
*   **评估指标**：语义准确度（LLM-as-a-Judge）、任务 F1 分数、误码率（BER）、生成熵以及**有效信息容量（EIC）**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练/推理的总时长。
*   **模型细节**：提到了使用 Qwen2.5-7B 进行生成，并使用 BERT 分类器进行统计安全性检测，这暗示实验是在具备主流深度学习加速卡（如 A100 或 H100 级别）的环境下完成的。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了从简单截断到复杂 RAG 检索的多种不对称场景，涵盖了静态和动态环境。
*   **充分性与客观性**：
    *   实验对比了不同安全参数 $k$ 下的性能，展示了容量与可靠性之间的权衡。
    *   引入了 Gemini 2.0 Flash 作为第三方裁判评估语义，增加了客观性。
    *   使用了 5 折交叉验证来评估统计安全性（抗检测性）。
    *   **公平性**：在相同的认知不对称条件下对比基准方法，清晰地展示了传统方法在 BER 接近 50%（等同于随机猜测）时的失效。

### 6. 主要结论与发现
*   **鲁棒性**：在严重的认知不对称（如 RAG 检索导致的巨大前缀差异）下，传统对称方法完全失效（BER ≈ 50%），而 ACF（当 $k \ge 12$ 时）能保持 **0.00% 的误码率**。
*   **语义保持**：ACF 在嵌入信息的同时，几乎不损害智能体的任务执行能力（语义得分与无隐写模型持平）。
*   **安全性**：ACF 生成的文本在统计上与正常文本不可区分，BERT 检测器的准确率接近 50%（随机水平）。
*   **有效容量**：虽然 ACF 的理论单 Token 容量低于对称方法，但在不对称环境下，它是唯一能维持正向有效信息容量（EIC）的方案。

### 7. 优点
*   **结构化解耦**：巧妙地解决了智能体自主性（动态记忆）与隐写同步性之间的矛盾。
*   **无模型解码**：解码端极轻量化，不需要运行庞大的 LLM，显著降低了接收方的算力需求。
*   **理论完备**：利用统计学原理提供了可证明的误差界限。
*   **现实意义强**：直接针对智能体网络（Agent Networks）这一前沿应用场景中的痛点。

### 8. 不足与局限
*   **容量限制**：为了换取鲁棒性，ACF 的每 Token 嵌入位（bpw）通常低于理想状态下的对称方法。
*   **序列长度依赖**：由于采用统计解码，提取秘密信息需要一定的 Token 序列长度积累，对于极短的回复可能可靠性受限。
*   **应用限制**：主要适用于需要高可靠性、低带宽的指令传输（如 C2 指令、身份认证），而非大规模数据传输。

（完）
