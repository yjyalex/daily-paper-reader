---
title: "CoSearch: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search"
title_zh: CoSearch：通过强化学习实现推理与文档排序联合训练的智能体搜索
authors: "Hansi Zeng, Liam Collins, Bhuvesh Kumar, Neil Shah, Hamed Zamani"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17555v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 强化学习用于推理和文档排序的联合训练
tldr: 针对智能体搜索中检索系统固定导致的性能瓶颈，本文提出CoSearch框架。该框架利用强化学习（GRPO）首次实现了推理智能体与生成式文档排序模型的联合训练。通过引入语义分组策略解决排序器输入多变的问题，并结合排序质量与轨迹反馈设计复合奖励。实验表明，这种联合优化显著提升了复杂问答任务的准确性，为构建高性能搜索智能体提供了新路径。
selection_source: fresh_fetch
motivation: 现有智能体搜索将检索系统视为固定工具，导致检索能力成为制约复杂问答性能提升的主要瓶颈。
method: 提出CoSearch框架，通过GRPO算法联合训练推理智能体与生成式排序模型，并引入语义分组策略和复合奖励机制。
result: 在七个单跳和多跳问答基准测试中，CoSearch相比强基线模型取得了显著且一致的性能提升。
conclusion: 实验证明推理智能体与检索系统的联合训练是可行且高效的，为未来搜索智能体的发展提供了关键方向。
---

## 摘要
智能体搜索（Agentic search）——即训练能够迭代进行推理、发布查询并综合检索信息以回答复杂问题的智能体——通过强化学习（RL）取得了显著进展。然而，现有方法（如 Search-R1）将检索系统视为固定工具，仅优化推理智能体，而检索组件保持不变。初步实验表明，在七个问答（QA）基准测试中，理想检索（oracle）与固定检索系统之间的差距最高可达 +26.8% 的相对 F1 提升，这表明检索系统是扩展智能体搜索性能的关键瓶颈。受此发现启发，我们提出了 CoSearch，这是一个通过群体相对策略优化（GRPO）联合训练多步推理智能体和生成式文档排序模型的框架。为了对排序器进行有效的 GRPO 训练（其输入随推理轨迹而变化），我们引入了一种语义分组策略，通过词元级（token-level）相似度对子查询进行聚类，从而在不增加额外采样（rollouts）的情况下形成有效的优化组。我们进一步设计了一种复合奖励，将排序质量信号与轨迹级结果反馈相结合，为排序器提供即时和长期的学习信号。在七个单跳和多跳问答基准测试上的实验表明，该方法相较于强基准模型有持续的提升，消融研究验证了每个设计选择的有效性。我们的结果表明，推理智能体与检索系统的联合训练既可行又具有强大的性能，为未来的搜索智能体指明了一个关键要素。

## Abstract
Agentic search -- the task of training agents that iteratively reason, issue queries, and synthesize retrieved information to answer complex questions -- has achieved remarkable progress through reinforcement learning (RL). However, existing approaches such as Search-R1, treat the retrieval system as a fixed tool, optimizing only the reasoning agent while the retrieval component remains unchanged. A preliminary experiment reveals that the gap between an oracle and a fixed retrieval system reaches up to +26.8% relative F1 improvement across seven QA benchmarks, suggesting that the retrieval system is a key bottleneck in scaling agentic search performance. Motivated by this finding, we propose CoSearch, a framework that jointly trains a multi-step reasoning agent and a generative document ranking model via Group Relative Policy Optimization (GRPO). To enable effective GRPO training for the ranker -- whose inputs vary across reasoning trajectories -- we introduce a semantic grouping strategy that clusters sub-queries by token-level similarity, forming valid optimization groups without additional rollouts. We further design a composite reward combining ranking quality signals with trajectory-level outcome feedback, providing the ranker with both immediate and long-term learning signals. Experiments on seven single-hop and multi-hop QA benchmarks demonstrate consistent improvements over strong baselines, with ablation studies validating each design choice. Our results show that joint training of the reasoning agent and retrieval system is both feasible and strongly performant, pointing to a key ingredient for future search agents.

---

## 论文详细总结（自动生成）

以下是对论文《CoSearch: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的智能体搜索（Agentic Search）系统（如 Search-R1）通常将检索系统视为一个“固定工具”，仅通过强化学习（RL）优化推理智能体。
*   **核心发现**：作者通过“理想检索（Oracle Retrieval）”实验发现，如果检索系统能完美返回包含答案的文档，智能体的 F1 分数可提升 14.7%（7B 模型）至 26.8%（3B 模型）。这表明**检索质量是制约智能体搜索性能的关键瓶颈**。
*   **整体含义**：论文提出了 **CoSearch** 框架，旨在打破“固定检索”的限制，通过强化学习实现推理智能体与文档排序器（Ranker）的**联合训练**，使两者能够相互协同。

### 2. 方法论：核心思想与关键技术
CoSearch 包含一个多步推理的主智能体（Main Agent）和一个生成式文档排序器（Generative Ranker）。
*   **联合训练框架**：使用群体相对策略优化（**GRPO**）算法同时优化推理和排序两个模型。
*   **语义分组策略（Semantic Grouping）**：
    *   **挑战**：GRPO 要求对同一输入采样多个响应以计算优势（Advantage），但排序器的输入（子查询）在不同推理轨迹中是变化的，无法直接分组。
    *   **方案**：通过词元级（Token-level）F1 相似度对不同轨迹中的子查询进行聚类（阈值 $\delta=0.8$）。语义相似的查询被视为同一组，从而在不增加额外采样成本的情况下实现 GRPO 优化。
*   **复合奖励设计（Composite Reward）**：
    *   **排序奖励 ($r_{rel}$)**：基于 Hit@k（文档是否包含答案）提供即时反馈。
    *   **轨迹奖励 ($r_{main}$)**：基于最终答案的正确性（F1 分数）提供长期反馈。
    *   **逻辑**：当排序质量较低时，侧重于排序奖励；当排序已足够好时，引入轨迹奖励以捕捉文档对下游推理的实际效用。

### 3. 实验设计
*   **数据集**：涵盖 7 个 QA 基准测试，包括单跳（PopQA, NQ, TriviaQA）和多跳（HotpotQA, 2Wiki, Musique, Bamboogle）场景。
*   **对比方法**：
    *   基础模型：Direct Inference, CoT, RAG。
    *   强基线：Search-o1, Search-R1, ZeroSearch。
    *   内部变体：仅检索（无排序器）、固定排序器（预训练但冻结）。
*   **模型骨干**：主智能体使用 Qwen2.5-7B/3B-Instruct，排序器统一使用 Qwen2.5-7B-Instruct。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：训练集包含 51,200 个问题；采样轨迹数 $G=8$；每个轨迹最多 6 次搜索；排序器从 50 个候选文档中选出前 5 个。学习率设为 $1 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的任务上进行了测试，涵盖了从简单事实到复杂逻辑推理的范围。
*   **消融实验**：非常充分。作者分别验证了奖励信号（去掉 $r_{main}$ 或 $r_{rel}$）、语义分组策略、架构选择（共享骨干 vs 独立模型）以及排序数量 $K$ 的影响。
*   **客观性**：通过与 Oracle（理想上限）对比，客观展示了方法在缩小检索差距方面的实际效果（缩小了约 27% 的差距）。

### 6. 主要结论与发现
*   **性能提升**：CoSearch 在 7B 规模下比 Search-R1 相对提升了 6.6% 的 F1 值，在 3B 规模下提升了 10.8%。
*   **协同效应**：联合训练使排序器学会了不仅寻找包含答案的文档，还寻找有助于推理的中间文档。
*   **搜索效率**：更好的排序减少了智能体所需的搜索轮数（Search Turns），提高了解决问题的效率。
*   **模型规模**：较弱的推理智能体（3B）从排序器的改进中获益更多。

### 7. 优点
*   **首创性**：首次在智能体搜索场景下实现了推理与排序的端到端联合强化学习。
*   **高效的分组机制**：语义分组策略巧妙解决了 GRPO 在动态工具调用中的应用难题，且无需额外采样开销。
*   **轻量化奖励**：采用基于规则的复合奖励，避免了昂贵的 LLM-as-judge 标注。

### 8. 不足与局限
*   **第一阶段限制**：CoSearch 仅优化了第二阶段的重排序器（Ranker），第一阶段的密集检索器（Retriever）仍是固定的。
*   **模型稳定性**：消融实验显示，使用 3B 模型作为排序器时训练会崩溃，说明该方法对排序器本身的指令遵循能力有一定要求。
*   **知识库局限**：实验主要基于 2018 年的维基百科快照，对于实时性要求极高的 Web 搜索场景，其泛化能力尚待验证。

（完）
