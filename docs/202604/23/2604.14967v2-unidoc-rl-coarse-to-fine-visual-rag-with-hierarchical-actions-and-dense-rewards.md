---
title: "UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards"
title_zh: UniDoc-RL：具有层级化动作和稠密奖励的从粗到细视觉检索增强生成
authors: "Jun Wang, Shuo Tan, Zelong Sun, Tiancheng Gu, Yongle Zhao, Ziyong Feng, Kaicheng Yang, Zhiwu Lu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14967v2"
tags: ["query:token"]
score: 9.0
evidence: 具有分层动作和密集奖励的强化学习
tldr: "针对现有视觉RAG系统在复杂推理中缺乏细粒度语义的问题，本文提出UniDoc-RL框架。该框架将视觉信息获取建模为分层动作空间的序列决策过程，涵盖从文档检索、图像选择到区域裁剪的由粗到精过程。通过引入基于GRPO的密集多奖励强化学习方案，模型能有效对齐多目标任务并抑制无关内容。实验表明，UniDoc-RL在多个基准测试中显著优于现有SOTA方法，最高提升达17.7%。"
selection_source: fresh_fetch
motivation: 现有视觉RAG系统通常依赖通用检索信号，难以捕捉复杂推理所需的细粒度视觉语义信息。
method: 提出一种统一的强化学习框架，通过分层动作空间实现从粗粒度文档检索到细粒度区域裁剪的序列决策，并结合密集多奖励机制进行端到端训练。
result: "在三个权威基准测试中均超越了现有最先进水平，相比之前的强化学习方法性能提升高达17.7%。"
conclusion: UniDoc-RL通过将视觉感知与推理整合进分层决策过程，有效提升了大型视觉语言模型在处理复杂视觉知识检索任务时的准确性。
---

## 摘要
检索增强生成 (RAG) 通过外部视觉知识扩展了大视觉语言模型 (LVLMs)。然而，现有的视觉 RAG 系统通常依赖于通用的检索信号，忽略了复杂推理所必需的细粒度视觉语义。为了解决这一局限性，我们提出了 UniDoc-RL，这是一个统一的强化学习框架，其中 LVLM 智能体共同执行检索、重排序、主动视觉感知和推理。UniDoc-RL 将视觉信息获取建模为具有层级化动作空间的序列决策问题。具体而言，它将视觉证据从粗粒度的文档检索逐步细化为细粒度的图像选择和主动区域裁剪，使模型能够抑制无关内容并关注信息密集区域。为了实现有效的端到端训练，我们引入了一种稠密多奖励方案，为每个动作提供任务感知的监督。基于群体相对策略优化 (GRPO)，UniDoc-RL 在不依赖独立价值网络的情况下，使智能体行为与多个目标保持一致。为了支持这种训练范式，我们构建了一个包含细粒度动作标注的高质量推理轨迹综合数据集。在三个基准测试上的实验表明，UniDoc-RL 持续超越了最先进的基准模型，相比之前的基于强化学习的方法，性能提升高达 17.7%。

## Abstract
Retrieval-Augmented Generation (RAG) extends Large Vision-Language Models (LVLMs) with external visual knowledge. However, existing visual RAG systems typically rely on generic retrieval signals that overlook the fine-grained visual semantics essential for complex reasoning. To address this limitation, we propose UniDoc-RL, a unified reinforcement learning framework in which an LVLM agent jointly performs retrieval, reranking, active visual perception, and reasoning. UniDoc-RL formulates visual information acquisition as a sequential decision-making problem with a hierarchical action space. Specifically, it progressively refines visual evidence from coarse-grained document retrieval to fine-grained image selection and active region cropping, allowing the model to suppress irrelevant content and attend to information-dense regions. For effective end-to-end training, we introduce a dense multi-reward scheme that provides task-aware supervision for each action. Based on Group Relative Policy Optimization (GRPO), UniDoc-RL aligns agent behavior with multiple objectives without relying on a separate value network. To support this training paradigm, we curate a comprehensive dataset of high-quality reasoning trajectories with fine-grained action annotations. Experiments on three benchmarks demonstrate that UniDoc-RL consistently surpasses state-of-the-art baselines, yielding up to 17.7% gains over prior RL-based methods.

---

## 论文详细总结（自动生成）

### 论文总结：UniDoc-RL —— 具有层级化动作和稠密奖励的从粗到细视觉 RAG

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉检索增强生成（Visual RAG）系统主要面临三个挑战：
*   **检索不精准**：通用检索器往往依赖浅层匹配，难以捕捉复杂推理所需的细粒度语义，导致引入无关背景或产生幻觉。
*   **视觉利用率低**：模型通常被动接收整张图像，而视觉文档（如报表、图表）信息密度极高且包含大量冗余，模型难以聚焦关键区域。
*   **优化困难**：现有的强化学习（RL）方法多采用稀疏的结局奖励（Outcome Reward），无法为检索、筛选、裁剪等中间决策步骤提供明确的信用分配（Credit Assignment）。
**研究动机**：通过构建一个统一的 RL 框架，将视觉 RAG 建模为“搜索-筛选-感知-推理”的序列决策过程，实现从粗到精的信息获取。

#### 2. 方法论：核心思想与关键技术
UniDoc-RL 将 LVLM 智能体置于一个层级化动作空间中，通过 **GRPO（群体相对策略优化）** 算法进行端到端训练。
*   **层级化动作空间**：
    1.  **图像搜索（Search）**：调用外部工具进行粗粒度候选文档检索。
    2.  **精准筛选（Select）**：利用 LVLM 的语义理解能力，从候选集中剔除无关图像，缩小推理范围。
    3.  **主动视觉感知（Perceive）**：通过生成边界框（Bounding Box）执行裁剪和缩放操作，主动聚焦于高价值局部区域。
*   **稠密多奖励机制（Dense Multi-Rewards）**：
    *   **检索奖励**：基于 NDCG 指标评估搜索结果的质量。
    *   **筛选奖励**：评估所选图像是否属于 Ground-truth 集合。
    *   **感知奖励**：计算预测裁剪框与真实区域的交并比（IoU）。
    *   **模式奖励**：确保模型输出符合预定义的 XML 格式规范。
    *   **结局奖励**：利用大模型作为评判者（RM），评估最终答案的正确性。
*   **数据合成**：使用 Qwen3-VL-235B 作为教师模型，结合 Mineru 文档解析工具，合成了包含 1.2 万条 SFT 轨迹和 5500 余条 RL 轨迹的高质量数据集。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SlideVQA**：多图幻灯片问答，侧重多跳推理。
    *   **ViDoSeek**：视觉文档搜索与问答。
    *   **MMLongBench**：长文档理解，涵盖图表、表格、布局等多种视觉类型。
*   **对比方法**：
    *   **OCR-based RAG**：仅依赖文本提取的方法。
    *   **Vanilla RAG / ReAct**：标准的检索与思维链方法。
    *   **Search-R1 / VRAG-RL**：最新的基于强化学习的视觉 RAG 基准。
*   **基准模型**：Qwen2.5-VL-3B-Instruct 和 7B-Instruct。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **软件框架**：SFT 阶段使用 `llama-factory`，RL 阶段（GRPO）使用 `verl` 框架。
*   **训练细节**：SFT 阶段全参数微调 3 个 Epoch；RL 阶段设置组大小（Group Size）为 5，训练 1 个 Epoch。文中未明确给出总训练时长，但提供了详细的学习率、Warmup 比例等超参数。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准上进行了全面测试，涵盖了从 3B 到 7B 不同规模的模型。
*   **消融实验**：针对“层级动作（筛选与感知）”和“多奖励项”分别做了剥离实验，验证了每个模块的增量贡献。
*   **定性分析**：通过可视化裁剪频率和裁剪精度，展示了 RL 如何纠正 SFT 模型的“被动阅读”偏差。
*   **评价**：实验设计较为充分，对比了 OCR 和纯视觉两条技术路线，且在多个维度（逻辑推理 vs 视觉感知）验证了方法的鲁棒性，结果具有客观性。

#### 6. 主要结论与发现
*   **性能飞跃**：UniDoc-RL 在所有基准上均达到 SOTA，相比之前的 RL 方法（如 VRAG-RL），在 3B 和 7B 模型上分别实现了 **17.5% 和 17.7%** 的大幅提升。
*   **筛选动作的价值**：精准筛选显著提高了 Ground-truth 图像的召回率（Recall），为后续推理提供了更干净的上下文。
*   **RL 的行为修正**：SFT 模型倾向于保守地阅读全图，而 RL 训练后的模型学会了像人类一样主动“放大”关键细节，裁剪频率和精度显著提升。

#### 7. 优点
*   **系统性强**：将 RAG 的各个环节（检索、重排、感知、推理）整合进一个统一的强化学习框架，解决了模块化系统间的语义鸿沟。
*   **奖励设计精巧**：通过稠密奖励解决了长链条决策中的信用分配问题，使中间步骤的优化变得可控。
*   **数据质量高**：引入了专家解析工具辅助合成轨迹，保证了动作标注（如坐标框）的精准度。

#### 8. 不足与局限
*   **计算开销**：虽然 GRPO 节省了价值网络，但多轮迭代交互（Search-Select-Perceive）在推理时会增加 Token 消耗和延迟。
*   **依赖教师模型**：训练数据的质量高度依赖于 Qwen3-VL-235B 等超大规模教师模型，对于缺乏此类资源的研究者具有一定的门槛。
*   **应用限制**：目前主要针对静态文档图像，对于动态视频或实时交互场景的适用性尚未验证。

（完）
