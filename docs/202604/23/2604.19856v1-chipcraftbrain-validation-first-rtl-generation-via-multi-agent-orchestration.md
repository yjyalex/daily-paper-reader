---
title: "ChipCraftBrain: Validation-First RTL Generation via Multi-Agent Orchestration"
title_zh: ChipCraftBrain：通过多智能体编排实现的验证优先 RTL 生成
authors: Cagri Eryilmaz
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19856v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 通过 PPO 策略进行多智能体编排以生成代码
tldr: "ChipCraftBrain是一个结合符号神经推理与自适应多智能体编排的RTL生成框架。针对LLM在硬件描述语言生成中正确率低、工业基准测试表现不佳及成本高等问题，该框架引入了基于PPO策略的智能体调度、符号-神经混合架构、知识增强生成及层次化规格分解。实验显示其在VerilogEval-Human上达到97.2%的pass@1，在NVIDIA CVDP工业基准上显著超越单次生成基准，并成功实现了FPGA验证的RISC-V SoC模块生成。"
selection_source: fresh_fetch
motivation: 现有LLM在RTL生成中面临功能正确性不足、缺乏工业级基准验证及推理成本高昂等挑战。
method: 采用基于PPO策略的多智能体编排，结合符号-神经混合推理、知识增强检索及层次化模块分解技术。
result: "在VerilogEval-Human和CVDP基准上分别实现97.2%和94.7%的pass@1，性能大幅领先于现有单次生成和多智能体基准。"
conclusion: 通过多智能体协同与符号推理的深度融合，ChipCraftBrain显著提升了复杂工业级硬件设计的自动化生成能力。
---

## 摘要
大语言模型 (LLMs) 在根据自然语言规范生成寄存器传输级 (RTL) 代码方面展现出潜力，但单次生成在标准基准测试中的功能正确率仅为 60-65%。诸如 MAGE 等多智能体方法在 VerilogEval 上达到了 95.9% 的准确率，但在 NVIDIA CVDP 等更具挑战性的工业基准测试上仍未经验证，且缺乏综合感知，并产生高昂的 API 成本。我们提出了 ChipCraftBrain，这是一个结合了符号-神经推理与自适应多智能体编排的自动化 RTL 生成框架。该系统由四项创新驱动：(1) 通过 168 维状态上的 PPO 策略对六个专业智能体进行自适应编排（同时评估了另一种基于世界模型的 MPC 规划器）；(2) 混合符号-神经架构，通过算法解决卡诺图 (K-map) 和真值表问题，而专业智能体则处理波形时序和通用 RTL；(3) 知识增强生成，基于 321 个模式库和 971 个开源参考实现，并采用焦点感知检索；(4) 层次化规范分解，将任务分解为具有依赖顺序且接口同步的子模块。在 VerilogEval-Human 上，ChipCraftBrain 实现了 97.2% 的平均 pass@1（7 次运行范围为 96.15-98.72%，最高 154/156），与 ChipAgents (97.4%) 持平并领先于 MAGE (95.9%)。在涵盖五个任务类别的 CVDP 302 个问题的非智能体子集上，我们达到了 94.7% 的平均 pass@1，相比已发表的单次生成基准，每个类别的准确率提升了 36-60 个百分点；尽管每个问题的尝试次数减少了约 30 倍，我们在与 NVIDIA ACE-RTL 共享的四个类别中仍有三个处于领先地位。一项 RISC-V SoC 案例研究表明，层次化分解生成了 8/8 个通过 lint 检查的模块并在 FPGA 上通过验证，而整体式生成则完全失败。

## Abstract
Large Language Models (LLMs) show promise for generating Register-Transfer Level (RTL) code from natural language specifications, but single-shot generation achieves only 60-65% functional correctness on standard benchmarks. Multi-agent approaches such as MAGE reach 95.9% on VerilogEval yet remain untested on harder industrial benchmarks such as NVIDIA's CVDP, lack synthesis awareness, and incur high API costs.   We present ChipCraftBrain, a framework combining symbolic-neural reasoning with adaptive multi-agent orchestration for automated RTL generation. Four innovations drive the system: (1) adaptive orchestration over six specialized agents via a PPO policy over a 168-dim state (an alternative world-model MPC planner is also evaluated); (2) a hybrid symbolic-neural architecture that solves K-map and truth-table problems algorithmically while specialized agents handle waveform timing and general RTL; (3) knowledge-augmented generation from a 321-pattern base plus 971 open-source reference implementations with focus-aware retrieval; and (4) hierarchical specification decomposition into dependency-ordered sub-modules with interface synchronization.   On VerilogEval-Human, ChipCraftBrain achieves 97.2% mean pass@1 (range 96.15-98.72% across 7 runs, best 154/156), on par with ChipAgents (97.4%, self-reported) and ahead of MAGE (95.9%). On a 302-problem non-agentic subset of CVDP spanning five task categories, we reach 94.7% mean pass@1 (286/302, averaged over 3 runs), a 36-60 percentage-point lift per category over the published single-shot baseline; we additionally lead three of four categories shared with NVIDIA's ACE-RTL despite using roughly 30x fewer per-problem attempts. A RISC-V SoC case study demonstrates hierarchical decomposition generating 8/8 lint-passing modules (689 LOC) validated on FPGA, where monolithic generation fails entirely.

---

## 论文详细总结（自动生成）

以下是对论文《ChipCraftBrain: Validation-First RTL Generation via Multi-Agent Orchestration》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在生成寄存器传输级（RTL）代码方面表现出潜力，但单次生成（Single-shot）在标准基准测试中的正确率仅为60-65%。
*   **核心挑战**：
    *   **工业复杂性**：现有研究多集中在简单的VerilogEval基准上，而在复杂的工业级设计（如NVIDIA的CVDP）中表现不佳。
    *   **成本与效率**：现有的多智能体系统（如MAGE）通过大量并行采样提高正确率，导致API调用成本极高。
    *   **缺乏综合感知**：现有方法往往只关注功能正确性，忽略了硬件设计的综合（Synthesis）质量（如面积、时序）。
*   **整体含义**：论文提出了ChipCraftBrain框架，通过“验证优先”的理念，结合强化学习编排的多智能体系统和符号推理，旨在实现高可靠、低成本且具备工业级设计能力的自动化RTL生成。

### 2. 方法论：核心思想与关键技术
*   **自适应多智能体编排**：
    *   系统包含6个专业智能体：4个由强化学习（RL）编排的通用智能体（Genius, Fast, Debug, Optimize）和2个基于规则的专用智能体（Waveform, Testbench）。
    *   **PPO策略编排**：使用近端策略优化（PPO）算法，基于168维状态向量（包含规范复杂度、错误模式、代码指标等）动态选择智能体、采样温度和检索策略。
*   **符号-神经混合架构**：
    *   **算法求解器**：对于卡诺图（K-map）和真值表问题，直接调用Quine-McCluskey算法求解，实现零成本、100%准确率。
    *   **神经推理**：对于复杂的时序逻辑和通用RTL，使用LLM智能体处理。
*   **知识增强生成（RAG）**：
    *   构建了包含321个设计模式和971个开源参考实现的知识库。
    *   引入“规范引导注册表”（Spec Guidance Registry），在生成前对原始规范进行技术增强（如添加接口协议说明、常见陷阱提示）。
*   **层次化规格分解**：
    *   自动识别复杂设计（如SoC），将其分解为4-8个具有依赖顺序的子模块，通过接口同步确保模块间的连通性。
*   **验证流水线**：
    *   三阶段验证：编译检查（Icarus Verilog/Verilator）、功能仿真（vvp/cocotb）、综合检查（Yosys）。提供结构化的错误反馈用于迭代修复。

### 3. 实验设计
*   **数据集与基准测试**：
    1.  **VerilogEval-Human**：156个简单模块，用于评估基础生成能力。
    2.  **NVIDIA CVDP (子集)**：302个工业级问题，涵盖代码补全、规范转RTL、修改、Linting和Bug修复。
    3.  **ChipBench**：45个复杂硬件设计，平均门数是VerilogEval的14倍。
*   **对比方法**：
    *   单次生成基准（GPT-4, Claude 3.7 Sonnet）。
    *   现有先进系统：MAGE、ChipAgents、VFlow、ACE-RTL（NVIDIA）。
*   **案例研究**：一个RISC-V SoC的端到端优化与FPGA硬件验证。

### 4. 资源与算力
*   **算力设备**：本地编排和验证运行在双GPU工作站（2 × NVIDIA RTX 3090, 48GB VRAM）。
*   **模型调用**：LLM推理通过Anthropic API（Claude 3 Opus/Sonnet 4.6）远程完成。
*   **本地蒸馏**：论文提到通过成功轨迹微调了一个Qwen2.5-Coder 7B模型，并在双3090上通过vLLM部署，实现了75%的VerilogEval正确率。
*   **对比参考**：文中指出NVIDIA的ACE-RTL训练其专用生成器消耗了约10,000个A100 GPU小时，而ChipCraftBrain主要依赖API和轻量化编排。

### 5. 实验数量与充分性
*   **实验规模**：
    *   VerilogEval进行了7次独立运行以评估方差。
    *   CVDP子集进行了3次独立运行。
    *   进行了详细的消融实验（Table XI），量化了迭代修复、多智能体选择、RAG、规范引导和符号求解器各自的贡献。
*   **充分性评价**：实验覆盖了从简单到工业级的三个难度梯度，对比了当前最强的商业和学术基准。通过多次运行和方差分析，证明了结果的客观性。但需注意，CVDP的评估仅限于非商业、非智能体子集（302/783题）。

### 6. 主要结论与发现
*   **性能领先**：在VerilogEval上达到97.2%的均值正确率；在CVDP工业基准上达到94.7%，远超单次生成的33.56%。
*   **成本优势**：通过分层调用模型和符号求解，成本比MAGE等系统降低了2-3倍。
*   **分解的力量**：层次化分解是解决SoC级设计的关键，整体式生成在复杂任务中几乎完全失败。
*   **验证的重要性**：迭代修复流水线将正确率提升了约15个百分点，证明“验证优先”策略的有效性。

### 7. 优点
*   **混合架构创新**：巧妙结合了确定性算法（K-map）和概率性模型（LLM），兼顾了效率与灵活性。
*   **工业级验证**：不仅在学术基准上刷榜，还在NVIDIA的工业基准和FPGA硬件上进行了实测。
*   **RL编排**：引入PPO策略动态调整生成参数，使系统具备了根据错误反馈自我调节的能力。
*   **透明推理**：提供了“可见推理”接口，增强了硬件工程师对AI生成过程的可解释性和信任度。

### 8. 不足与局限
*   **工具链限制**：由于缺乏Cadence Xcelium等商业工具支持，未能覆盖CVDP中涉及断言验证和商业IP的481个问题。
*   **复杂度瓶颈**：在ChipBench的CPU IP类别中正确率仅为11.1%，说明对于极高复杂度的处理器逻辑，现有的分解和生成策略仍有待突破。
*   **时序闭合**：目前的验证流水线虽包含综合检查，但尚未集成后综合的时序驱动反馈（Timing-driven feedback）。
*   **协议偏差**：CVDP的对比中，基准方法采用的是n=5的单次生成，而本项目采用的是5次迭代修复，协议上的差异使得对比并非完全的“模型对模型”。

（完）
