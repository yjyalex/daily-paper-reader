---
title: "Where Reasoning Breaks: Logic-Aware Path Selection by Controlling Logical Connectives in LLMs Reasoning Chains"
title_zh: 推理断裂之处：通过控制大语言模型推理链中的逻辑连词实现逻辑感知路径选择
authors: "Seunghyun Park, Yuanyuan Lei"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20564v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 多步逻辑推导链中的逻辑感知路径选择
tldr: 本研究发现大语言模型在多步逻辑推理中的脆弱性主要源于逻辑连接词处的决策失误。为此，作者提出了一个针对逻辑关键节点的干预框架，包含梯度引导、局部分支搜索和针对性偏好优化。该方法通过在逻辑转折点进行精确控制，有效引导模型进入正确的推理路径，在提升推理准确性的同时，比全局搜索方法具有更高的计算效率。
selection_source: fresh_fetch
motivation: 大模型在多步推理中容易出错，且逻辑连接词作为高熵分叉点，是导致推理链条断裂的关键薄弱环节。
method: 提出一种多层级框架，通过梯度引导内部表示、局部前瞻搜索以及针对逻辑转折点的强化学习优化来干预推理过程。
result: 该方法在逻辑推理任务上取得了显著的性能提升，且在准确率与推理效率的权衡上优于束搜索等全局缩放方法。
conclusion: 针对逻辑连接词进行局部精准干预是提升大模型逻辑推理能力的一种高效且稳健的途径。
---

## 摘要
尽管大语言模型（LLMs）展现出令人印象深刻的推理能力，但在多步逻辑演绎中仍然表现脆弱，单个转换错误可能会在整个推理链中传播，导致性能不稳定。在这项工作中，我们将逻辑连词识别为这种结构性脆弱的主要点。通过实证分析，我们表明连词标记充当了高熵分叉点，模型在这些点上经常难以确定正确的逻辑方向。受此观察启发，我们假设干预逻辑连词的选择可以引导 LLMs 走向更正确的逻辑方向，从而改善整体推理链。为了验证这一假设，我们提出了一个多层框架，专门在推理过程中这些逻辑关键节点进行干预。我们的框架包括：(1) 基于梯度的逻辑引导，旨在引导 LLMs 的内部表示向有效的推理子空间靠拢；(2) 局部化分支，通过有针对性的前瞻搜索解决歧义；(3) 针对性转换偏好优化，这是一种精准的强化学习目标，选择性地优化逻辑枢纽处的单标记偏好。至关重要的是，通过仅在逻辑关键转换处集中干预，与束搜索和自我一致性等全局推理时间扩展方法相比，我们的框架在准确率与效率之间取得了更优的权衡。

## Abstract
While LLMs demonstrate impressive reasoning capabilities, they remain fragile in multi-step logical deduction, where a single transition error can propagate through the entire reasoning chain, leading to unstable performance. In this work, we identify logical connectives as primary points of this structural fragility. Through empirical analysis, we show that connective tokens function as high entropy forking points, at which models frequently struggle to determine the correct logical direction. Motivated by this observation, we hypothesize that intervening in logical connective selection can guide LLMs toward more correct logical direction, thereby improving the overall reasoning chain. To validate this hypothesis, we propose a multi-layered framework that intervenes specifically at these logic-critical junctions in the reasoning process. Our framework includes (1) Gradient-based Logical Steering to guide LLMs internal representations towards valid reasoning subspaces, (2) Localized Branching to resolve ambiguity via targeted look-ahead search, and (3) Targeted Transition Preference Optimization, a surgical reinforcement learning objective that selectively optimizes single-token preferences at logical pivots. Crucially, by concentrating intervention solely on logic-critical transitions, our framework achieves a favorable accuracy--efficiency trade-off compared to global inference time scaling methods like beam search and self-consistency.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）在逻辑推理中的脆弱性，并提出了一种针对“逻辑连词”进行精准干预的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 LLM 在推理上取得了进步，但在多步逻辑演绎中表现得非常脆弱。推理链条中任何一个中间步骤的微小错误都会随时间传播，导致最终结果失败。
*   **核心发现**：作者通过实证分析发现，**逻辑连词**（如 *therefore, however, but* 等）是推理链条中的“结构性脆弱点”。这些词在模型生成时通常具有极高的熵（不确定性），是决定推理走向的关键“分叉点”。
*   **核心假设**：通过在逻辑连词这些关键决策点进行干预，可以引导模型选择正确的逻辑路径，从而以极低的计算成本提升整体推理性能。

### 2. 方法论：核心思想与关键技术
作者提出了一个涵盖激活层、推理层和训练层的多层级干预框架：

*   **基于梯度的逻辑引导 (Gradient-based Logical Steering)**：
    *   **核心思想**：在不改变模型权重的情况下，通过修改内部表示来引导模型。
    *   **技术细节**：从参考数据中提取正确连词对应的梯度向量 $v_{steer}$，在推理时将其加到目标层的隐藏状态 $h$ 上（公式：$\tilde{h} = h + \alpha \cdot v_{steer}$），从而在表示空间内将模型推向更具逻辑性的方向。
*   **局部化分支 (Localized Branching)**：
    *   **核心思想**：在推理遇到歧义连词时，进行局部的“前瞻性”搜索。
    *   **技术细节**：当模型预测的 Top-1 标记是逻辑连词且存在歧义时，触发分支。对前几个候选连词分别进行短长度（如 20 个 token）的试探性生成，计算各路径的平均熵和序列置信度，选择表现最优的路径继续生成。
*   **针对性转换偏好优化 (TTPO)**：
    *   **核心思想**：一种“手术刀式”的强化学习微调。
    *   **技术细节**：改进了 DPO（直接偏好优化）目标函数，仅针对逻辑连词这**单个 token** 的决策步骤计算损失和梯度更新。这种稀疏更新避免了对非关键 token 的干扰，保持了模型的通用能力。

### 3. 实验设计
*   **数据集**：涵盖了五种逻辑推理基准：ZebraLogic（逻辑谜题）、BBH（演绎子集）、RuleBERT（规则推理）、LogiQA 2.0（逻辑阅读理解）和 ProntoQA（形式逻辑）。
*   **对比方法 (Baselines)**：
    *   **Greedy Decoding**：基础贪婪搜索。
    *   **Beam Search**：全局束搜索。
    *   **Self-Consistency (n=5)**：多路径采样投票。
*   **实验模型**：Gemma-3 系列（4B-it, 12B-it）和 Phi-4 系列（mini-instruct, reasoning-plus）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中提供了详细的效率对比。结果显示，该方法（尤其是 Steering 和 TTPO）的推理成本接近 1.0x Greedy，而 Branching 的成本约为 1.2x-1.5x，远低于 Beam Search（3.3x-5.6x）和 Self-Consistency（2.9x-4.3x）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流模型和 5 个数据集上进行了全面测试，并包含消融实验（如 TTPO + Steering/Branching 的组合效果）。
*   **充分性与客观性**：
    *   作者进行了深入的诊断分析（如熵分析、Top-K 候选分析、单 token 扰动实验），证明了逻辑连词的特殊地位。
    *   实验对比了准确率与效率的权衡（Trade-off），不仅关注性能提升，也关注计算成本，体现了较强的工程实用价值和客观性。

### 6. 主要结论与发现
*   **连词是脆弱之源**：逻辑连词在推理链中仅占 4%-7%，但在高熵 token 中占比极高（富集度达 2-5.8 倍）。改变一个连词会导致 41.1% 的正确推理链崩溃。
*   **局部干预优于全局搜索**：针对逻辑枢纽的局部干预，在多个任务上达到了与 Self-Consistency 相当甚至更好的准确率，但计算开销大幅降低。
*   **分布锐化**：TTPO 训练能显著降低模型在逻辑转折点的不确定性，使模型在贪婪解码时更加果断且准确。

### 7. 优点
*   **精准高效**：不同于盲目增加计算量的“思维链扩展”，该方法精准打击推理中的薄弱环节，具有极高的计算效率。
*   **多维度覆盖**：从底层激活到顶层解码再到模型微调，提供了全方位的解决方案。
*   **理论与实证结合**：通过语言学中的话语关系框架（Discourse Relation）来定义逻辑连词，使方法具有较好的可解释性。

### 8. 不足与局限
*   **依赖显式标记**：该方法高度依赖于预定义的逻辑连词列表。如果推理步骤是隐式的（没有明显的连词），干预机制将失效。
*   **分词器敏感性**：逻辑连词的识别受分词（Tokenization）影响，不同模型的分词差异可能导致识别不准。
*   **任务局限性**：在逻辑连词密度较低的任务（如 BBH 部分题目）中，性能提升有限。
*   **多标记连词处理**：虽然文中提到了处理多 token 连词，但在极其复杂的长短语连词识别上可能仍存在偏差。

（完）
