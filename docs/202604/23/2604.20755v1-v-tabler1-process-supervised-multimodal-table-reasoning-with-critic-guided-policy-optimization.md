---
title: "V-tableR1: Process-Supervised Multimodal Table Reasoning with Critic-Guided Policy Optimization"
title_zh: V-tableR1：基于评论家引导策略优化的过程监督多模态表格推理
authors: "Yubo Jiang, Yitong An, Xin Yang, Abudukelimu Wuerkaixi, Xuxin Cheng, Fengying Xie, Zhiguo Jiang, Cao Liu, Ke Zeng, Haopeng Zhang"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20755v1"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 多模态推理的过程监督强化学习
tldr: 针对多模态大模型在视觉推理中依赖模式匹配而非严谨逻辑的问题，本文提出V-tableR1框架。该框架利用表格的结构化特性，通过专门的评论家模型对推理过程进行步骤级监督。结合新颖的PGPO算法，V-tableR1显著减少了视觉幻觉，使4B参数模型在复杂表格基准测试中超越了规模大其18倍的模型，实现了可验证的逻辑推导。
selection_source: fresh_fetch
motivation: 现有多模态模型在视觉推理时往往依赖表面模式匹配而非严谨的逻辑推导，且难以将抽象逻辑与连续像素空间对齐。
method: 利用表格的确定性网格结构，通过Critic模型提供步骤级反馈，并采用集成过程奖励与动态采样的PGPO算法进行优化。
result: V-tableR1 4B在复杂表格基准测试中达到了开源模型的最优性能，显著优于SFT基线并超越了参数量大其18倍的模型。
conclusion: 该研究证明了通过过程监督和强化学习可以将多模态推理从黑盒模式转向可验证的逻辑推导，有效抑制了视觉幻觉。
---

## 摘要
我们介绍了 V-tableR1，这是一个过程监督的强化学习框架，旨在引导多模态大语言模型（MLLM）进行严谨且可验证的推理。目前仅基于最终结果训练的 MLLM 通常将视觉推理视为黑盒，依赖于表面的模式匹配，而非执行严谨的多步推理。虽然具有可验证奖励的强化学习可以强制执行透明的推理轨迹，但由于将抽象逻辑落地到连续像素空间存在歧义，将其扩展到视觉领域仍面临严重阻碍。我们利用表格的确定性网格结构作为理想的视觉试验场来解决这一问题。V-tableR1 采用专门的评论家 VLM，对策略 VLM 生成的显式视觉思维链提供密集的步骤级反馈。为了优化该系统，我们提出了过程引导的直接对齐策略优化（PGPO），这是一种集成过程奖励、解耦策略约束和长度感知动态采样的新型强化学习算法。广泛的评估表明，V-tableR1 显式地惩罚了视觉幻觉和捷径猜测。通过从根本上将多模态推理从黑盒模式匹配转变为可验证的逻辑推导，V-tableR1 4B 在复杂的表格基准测试中确立了开源模型的最先进准确率，其表现优于尺寸达其 18 倍的模型，并较其 SFT 基准模型有所提升。

## Abstract
We introduce V-tableR1, a process-supervised reinforcement learning framework that elicits rigorous, verifiable reasoning from multimodal large language models (MLLMs). Current MLLMs trained solely on final outcomes often treat visual reasoning as a black box, relying on superficial pattern matching rather than performing rigorous multi-step inference. While Reinforcement Learning with Verifiable Rewards could enforce transparent reasoning trajectories, extending it to visual domains remains severely hindered by the ambiguity of grounding abstract logic into continuous pixel space. We solve this by leveraging the deterministic grid structure of tables as an ideal visual testbed. V-tableR1 employs a specialized critic VLM to provide dense, step-level feedback on the explicit visual chain-of-thought generated by a policy VLM. To optimize this system, we propose Process-Guided Direct Alignment Policy Optimization (PGPO), a novel RL algorithm integrating process rewards, decoupled policy constraints, and length-aware dynamic sampling. Extensive evaluations demonstrate that V-tableR1 explicitly penalizes visual hallucinations and shortcut guessing. By fundamentally shifting multimodal inference from black-box pattern matching to verifiable logical derivation, V-tableR1 4B establishes state-of-the-art accuracy among open-source models on complex tabular benchmarks, outperforming models up to 18x its size and improving over its SFT baseline

---

## 论文详细总结（自动生成）

这是一份关于论文《V-tableR1: Process-Supervised Multimodal Table Reasoning with Critic-Guided Policy Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理复杂的表格推理任务时，往往表现出“ inferential failure”（推理失败）。它们倾向于通过表面的模式匹配或统计相关性来“猜测”答案（捷径学习），而非执行严谨的逻辑推导。
*   **研究背景**：传统的监督微调（SFT）和基于结果的强化学习（RLVR）仅关注最终答案的正确性，导致推理过程成为“黑盒”。在视觉领域，由于将抽象逻辑映射到连续像素空间存在歧义，实现过程监督极具挑战。
*   **整体含义**：本文提出利用表格的**确定性网格结构**作为视觉推理的试验场，通过强制模型生成可验证的视觉锚点（如单元格坐标），并引入过程监督来消除视觉幻觉和逻辑跳跃。

### 2. 论文提出的方法论
*   **核心思想**：将表格推理分解为显式的“视觉思维链”（V-CoT），并利用一个专门的评论家模型（Critic VLM）对推理的每一个中间步骤进行打分，从而引导策略模型进行严谨推导。
*   **关键技术细节**：
    1.  **显式视觉锚点（Visual Anchors）**：要求模型在推理时输出单元格的逻辑坐标（如 `<cell: Row 2, Col 3>`），将视觉定位与逻辑运算解耦。
    2.  **评论家模型（Critic VLM）**：基于 Qwen3-VL-32B 训练，通过对人工标注数据进行启发式扰动（制造错误步骤）来学习识别逻辑错误，为推理轨迹提供密集的过程奖励 $r_{proc}$。
    3.  **PGPO 算法（过程引导直接对齐策略优化）**：
        *   **奖励机制**：将过程奖励与结果奖励（正确性、格式）结合，通过分段门控机制奖励“严谨推理”并惩罚“幸运猜测”。
        *   **解耦策略约束（DAPO）**：采用非对称剪裁边界，稳定长链推理的优化。
        *   **长度感知动态采样（LSPO）**：根据轨迹长度筛选高信息量的样本，提升训练效率。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流表格推理基准，分为两类：
    *   **表格事实验证 (TFV)**：TabFact, InfoTabs。
    *   **表格问答 (TQA)**：FinQA, HiTab, TAT-QA, TabMWP, WikiTableQuestions (WTQ)。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4.1, Gemini-1.5-Flash。
    *   **开源模型**：InternVL-2.5 (8B/14B), LLaVA 系列, Qwen3-VL 系列 (2B 到 32B), QVQ-72B, Table-LLaVA 等。
*   **评估指标**：主要使用准确率（Accuracy），并引入 GPT-5 作为裁判进行推理质量评分。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号 A100/H100 的数量）以及具体的训练时长。
*   **模型规模**：实验主要基于 Qwen3-VL 的 2B 和 4B 版本进行策略优化，Critic 模型使用了 32B 版本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个不同难度和类型的表格数据集上进行了全面测试。
    *   进行了消融实验，分别验证了 GRPO、DAPO、动态采样以及过程监督对最终性能的贡献。
    *   进行了 LLM-as-a-judge 实验（1000 个样本），从准确性、逻辑性和视觉定位三个维度评估推理质量。
*   **充分性与客观性**：实验设计较为充分，通过与参数量大得多的模型（如 72B）对比，证明了算法的效率。消融实验清晰地展示了过程监督是性能突破的关键，而非单纯依靠策略优化算法的改进。

### 6. 主要结论与发现
*   **性能突破**：V-tableR1 4B 模型在多个基准上达到了开源模型的最优水平，甚至在 FinQA 和 TabMWP 等任务上超越了参数量大其 18 倍的 72B 模型。
*   **SFT 与 RL 的差异**：RL 阶段的提升显著（如 FinQA 提升约 10%），证明 SFT 仅能教会模型推理的“语法”，而过程监督的 RL 才能教会模型真正的“逻辑”。
*   **抑制幻觉**：通过视觉锚点校验，模型在视觉定位上的得分提升了 65%，有效解决了“看错行、读错数”导致的幻觉问题。

### 7. 优点
*   **创新性地利用结构化特性**：巧妙地将表格的网格属性转化为可验证的奖励信号，解决了多模态 RL 中奖励函数难构建的痛点。
*   **算法集成度高**：PGPO 算法融合了当前 RL 领域多项先进技术（GRPO, DAPO, LSPO），并成功适配了多模态场景。
*   **极高的参数效率**：证明了通过高质量的过程监督，小参数模型也能在特定领域展现出超越超大模型的推理能力。

### 8. 不足与局限
*   **领域局限性**：该方法高度依赖表格的规则结构，难以直接推广到缺乏明确坐标系统的自然场景图像推理中。
*   **数据依赖**：构建高质量的 V-CoT 标注和训练 Critic 模型需要大量的工程投入，且 Critic 模型的准确性直接决定了策略模型的上限。
*   **算力细节缺失**：缺乏具体的训练资源消耗说明，不便于其他研究者评估复现成本。

（完）
