---
title: "IMPACT-CYCLE: A Contract-Based Multi-Agent System for Claim-Level Supervisory Correction of Long-Video Semantic Memory"
title_zh: IMPACT-CYCLE：一种基于契约的多智能体系统，用于长视频语义记忆的声明级监督修正
authors: "Weitong Kong, Di Wen, Kunyu Peng, David Schneider, Zeyun Zhong, Alexander Jaus, Zdravko Marinov, Jiale Wei, Ruiping Liu, Junwei Zheng, Yufan Chen, Lei Qi, Rainer Stiefelhagen"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20136v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于语义记忆维护的多智能体系统
tldr: 针对长视频理解中错误修正成本高、过程不透明的问题，本文提出 IMPACT-CYCLE 系统。该系统将长视频理解转化为对共享语义记忆的声明级维护，利用多智能体协作和显式授权合约，实现对局部、时序和全局一致性的自动校验。当自动验证失效时引入人工仲裁，并利用依赖图限制修正范围，在提升推理准确性的同时大幅降低了人工成本。
selection_source: fresh_fetch
motivation: 现有长视频理解模型输出缺乏中间状态，导致人工修正错误时必须重新审视原始视频，造成极高的纠错成本。
method: 提出一种基于合约的多智能体系统，通过维护包含声明依赖关系和溯源日志的结构化语义记忆，实现声明级的迭代修正与人工仲裁。
result: 在 VidOR 数据集上，该方法将 VQA 准确率从 0.71 提升至 0.79，并将人工仲裁成本降低了 4.8 倍。
conclusion: 通过结构化语义记忆和依赖驱动的修正机制，可以有效实现长视频理解的高效监督与低成本纠错。
---

## 摘要
纠正长视频理解中的错误成本极高：现有的多模态流水线产生不透明的端到端输出，不暴露任何中间状态供检查，迫使标注者重新查看原始视频并从头构建时序逻辑。核心瓶颈不仅在于生成质量，还在于缺乏一种监督界面，使人类的投入能与每个错误的范围成比例。我们提出了 IMPACT-CYCLE，这是一个监督式多智能体系统，它将长视频理解重新表述为对共享语义记忆的迭代式声明级维护——这是一种结构化的、带版本的状态，编码了类型化声明、声明依赖图和溯源日志。在明确的权限契约下运行的角色专业化智能体，将验证分解为局部对象关系正确性、跨时序一致性和全局语义连贯性，且修正被限制在结构相关的声明中。当自动化证据不足时，系统会升级到作为具有最终覆盖权的监督权威的人类仲裁；随后，依赖闭包重新验证确保修正成本与错误范围保持成比例。在 VidOR 上的实验表明，该系统显著提高了下游推理能力（VQA 从 0.71 提升至 0.79），并将人类仲裁成本降低了 4.8 倍，工作量显著低于手动标注。代码将发布在 https://github.com/MKong17/IMPACT_CYCLE。

## Abstract
Correcting errors in long-video understanding is disproportionately costly: existing multimodal pipelines produce opaque, end-to-end outputs that expose no intermediate state for inspection, forcing annotators to revisit raw video and reconstruct temporal logic from scratch. The core bottleneck is not generation quality alone, but the absence of a supervisory interface through which human effort can be proportional to the scope of each error. We present IMPACT-CYCLE, a supervisory multi-agent system that reformulates long-video understanding as iterative claim-level maintenance of a shared semantic memory -- a structured, versioned state encoding typed claims, a claim dependency graph, and a provenance log. Role-specialized agents operating under explicit authority contracts decompose verification into local object-relation correctness, cross-temporal consistency, and global semantic coherence, with corrections confined to structurally dependent claims. When automated evidence is insufficient, the system escalates to human arbitration as the supervisory authority with final override rights; dependency-closure re-verification then ensures correction cost remains proportional to error scope. Experiments on VidOR show substantially improved downstream reasoning (VQA: 0.71 to 0.79) and a 4.8x reduction in human arbitration cost, with workload significantly lower than manual annotation. Code will be released at https://github.com/MKong17/IMPACT_CYCLE.

---

## 论文详细总结（自动生成）

这是一份关于论文《IMPACT-CYCLE: A Contract-Based Multi-Agent System for Claim-Level Supervisory Correction of Long-Video Semantic Memory》的结构化深入分析报告：

### 1. 研究动机与核心问题
*   **核心问题**：长视频理解中的“纠错成本不对称”问题。现有的多模态大模型（MLLM）通常采用端到端生成方式，输出结果缺乏中间状态。一旦出现微小错误（如某个属性识别错误），往往会引发时序逻辑的连锁反应，导致最终输出失效。
*   **研究动机**：目前修正这类错误通常需要人工重新审视原始视频并从头标注，成本极高。作者认为瓶颈不在于生成质量，而在于缺乏一个**可交互、可局部编辑的监督界面**。论文旨在将长视频理解转化为一种“可维护的语义记忆”，使人类的修正投入与错误范围成比例，而非与视频长度成比例。

### 2. 方法论：IMPACT-CYCLE 系统
该系统通过多智能体协作维护一个共享的、结构化的语义记忆。
*   **核心思想**：将视频理解分解为原子级的“声明（Claims）”，并利用依赖图管理声明间的逻辑关系。
*   **语义记忆 (M)**：由四部分组成：
    1.  **场景图 (G)**：视频级的时空状态。
    2.  **声明集 (C)**：关于实体存在、标签、属性和关系的原子假设。
    3.  **依赖图 (D)**：记录声明间的结构依赖（如：若修改了物体标签，相关的空间关系也需重新验证）。
    4.  **溯源日志 (Π)**：记录所有智能体决策和人工编辑，支持审计和回滚。
*   **五大智能体角色与契约**：
    1.  **存储构造者 (Memory Constructor)**：负责初始化场景图和声明。
    2.  **局部接地智能体 (Local Grounding)**：验证单帧内的对象和属性。
    3.  **时序一致性智能体 (Temporal Consistency)**：验证跨帧的关系稳定性。
    4.  **全局语义审计智能体 (Global Semantic Audit)**：通过视频摘要检测全局逻辑矛盾。
    5.  **仲裁智能体 (Arbitration Agent)**：融合多方证据，决定接受、重写声明或升级给人类。
*   **关键技术细节**：
    *   **角色感知权重**：根据声明类型（如标签 vs 关系）动态调整不同智能体的信任度。
    *   **依赖闭包重新验证**：当人工修正某个声明后，系统仅重新验证受影响的局部邻域，而非重启整个流水线。

### 3. 实验设计
*   **数据集**：使用 **VidOR**（长视频对象关系数据集），包含复杂的时空关系标注。
*   **Benchmark 与对比方法**：
    *   对比了纯 MLLM（无图）、初始生成的场景图以及经过 IMPACT-CYCLE 验证后的场景图。
    *   下游任务测试：视频问答（VQA）。
    *   验证后端：对比了 GPT-4V 和 Gemini Pro 的表现。
*   **评估指标**：实体准确率、场景图编辑距离（GED）、VQA 准确率、人类仲裁频率（Human-Q/F）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号或训练时长。
*   **模型使用**：系统主要基于 API 调用，核心后端为 `gpt-4-vision-preview` 和 `Gemini Pro`。由于该方法属于推理侧的多智能体框架，不涉及大规模模型预训练，其算力消耗主要体现在推理时的 Token 消耗和多轮对话开销。

### 5. 实验数量与充分性
*   **实验规模**：在 240 段视频上进行了定量评估，样本量对于长视频研究较为合理。
*   **消融实验**：非常充分。分别验证了多角色分工、角色权重矩阵、迭代优化以及依赖驱动验证的有效性。
*   **用户研究**：邀请了 9 名参与者进行 Pilot Study，通过 NASA-TLX 量表评估心理负荷，对比了手动标注、半自动标注和 IMPACT-CYCLE 辅助标注。
*   **客观性**：实验设计考虑了不同复杂度的场景（低/中/高密度声明），并使用了 Oracle（真值）模拟人工仲裁来确定系统性能上限，具有较好的科学严谨性。

### 6. 主要结论与发现
*   **性能提升**：在 VidOR 上，VQA 准确率从 0.71 提升至 **0.79**。
*   **成本降低**：通过依赖闭包机制，单次编辑触发的模型调用次数从 42.3 次降至 8.7 次，**人工仲裁成本降低了 4.8 倍**。
*   **纠错逻辑**：实验证明，Resolve Score（自动解决争议的能力）与下游 VQA 准确率强相关（r=0.72），说明声明级的纠错确实能转化为语义理解的提升。
*   **工作量**：用户研究显示，IMPACT-CYCLE 显著降低了人类在长视频标注中的认知负荷。

### 7. 优点与亮点
*   **外科手术式纠错**：改变了“错一点就重跑全过程”的现状，实现了局部、精准的语义修正。
*   **显式授权机制**：通过“契约”规定智能体权限（如验证智能体只能标记不能修改），保证了系统的可控性和逻辑严密性。
*   **结构化记忆**：将非结构化的视频内容转化为可审计、可回滚的声明日志，增强了多模态系统的透明度。

### 8. 不足与局限
*   **自评偏差风险**：VQA 评估使用了与验证阶段相同的 MLLM 进行问题生成和评分，可能存在一定的“自我循环论证”偏差。
*   **用户研究规模较小**：9 人的用户研究虽然趋势明显，但在统计学显著性上（p=0.096）略显不足，需更大规模的测试。
*   **实时性挑战**：多智能体多轮迭代虽然节省了人工，但增加了推理延迟，可能不适用于对实时性要求极高的场景。
*   **依赖 Oracle**：主实验中的人工仲裁是模拟的，实际应用中人类标注者的主观偏差可能影响依赖图的传播效果。

（完）
