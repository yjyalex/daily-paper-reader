---
title: "Co-Director: Agentic Generative Video Storytelling"
title_zh: Co-Director：智能体生成式视频叙事
authors: "Yale Song, Yiwen Song, Nick Losier, Nathan Hodson, Ye Jin, Rhyard Zhu, Yan Xu, Daniel Vlasic, Carina Claassen, Jasmine Leon, Khanh G. LeViet, Zack Chomyn, Joe Timmons, Brett Slatkin, Scott Penberthy, Tomas Pfister"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24842v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于视频故事讲述的分层多智能体框架
tldr: 尽管扩散模型能生成高质量视频，但实现连贯叙事仍具挑战。现有智能体流水线常因独立提示词导致语义偏移。本文提出Co-Director，一个将视频叙事形式化为全局优化问题的分层多智能体框架。通过多臂老虎机确定全局创意方向，并利用局部多模态自细化循环确保身份一致性。此外，引入了GenAD-Bench基准测试。该方法在个性化广告等场景中表现优异，为电影级叙事提供了系统化方案。
selection_source: fresh_fetch
motivation: 现有的视频生成智能体流水线因模块独立和手工提示词，容易出现语义偏移和级联错误，难以维持长视频的连贯性。
method: 采用分层多智能体框架，结合多臂老虎机进行全局创意探索与局部多模态自细化循环，以优化叙事策略并减少身份漂移。
result: 在包含400个场景的GenAD-Bench数据集上，Co-Director在生成质量和叙事连贯性方面显著超越了现有最先进的方法。
conclusion: 该研究为生成式视频叙事提供了一种可扩展的原则性方法，能够有效平衡创意探索与序列一致性。
---

## 摘要
虽然扩散模型能够生成高保真视频片段，但将其转化为连贯的叙事引擎仍具挑战性。当前的智能体流水线通过链式模块实现自动化，但由于独立且手工编写的提示词，容易出现语义漂移和级联故障。我们提出了 Co-Director，这是一个将视频叙事形式化为全局优化问题的分层多智能体框架。为确保语义连贯性，我们引入了分层参数化：多臂老虎机在全局范围内识别有前景的创意方向，而局部多模态自优化循环则缓解了身份漂移并确保了序列级的一致性。这平衡了对新颖叙事策略的探索与对有效创意配置的利用。在评估方面，我们推出了 GenAD-Bench，这是一个包含 400 个虚构产品场景的个性化广告数据集。实验表明，Co-Director 显著优于现有的基准模型，提供了一种可以无缝推广到更广泛电影叙事的规范化方法。项目主页：https://co-director-agent.github.io/

## Abstract
While diffusion models generate high-fidelity video clips, transforming them into coherent storytelling engines remains challenging. Current agentic pipelines automate this via chained modules but suffer from semantic drift and cascading failures due to independent, handcrafted prompting. We present Co-Director, a hierarchical multi-agent framework formalizing video storytelling as a global optimization problem. To ensure semantic coherence, we introduce hierarchical parameterization: a multi-armed bandit globally identifies promising creative directions, while a local multimodal self-refinement loop mitigates identity drift and ensures sequence-level consistency. This balances the exploration of novel narrative strategies with the exploitation of effective creative configurations. For evaluation, we introduce GenAD-Bench, a 400-scenario dataset of fictional products for personalized advertising. Experiments demonstrate that Co-Director significantly outperforms state-of-the-art baselines, offering a principled approach that seamlessly generalizes to broader cinematic narratives. Project Page: https://co-director-agent.github.io/

---

## 论文详细总结（自动生成）

这是一份关于论文《Co-Director: Agentic Generative Video Storytelling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：尽管当前的视频扩散模型（如 Veo, Kling）能生成高保真的短片段，但在构建长篇连贯故事时面临两大难题：
    1.  **语义漂移与级联失败**：传统的智能体流水线采用线性“瀑布式”结构，前期的微小错误会随步骤放大，导致角色身份（Identity Drift）或逻辑不一致。
    2.  **缺乏全局创意优化**：现有系统依赖静态、手工编写的提示词模板，无法像人类导演那样在“探索新颖叙事”与“利用成熟风格”之间进行权衡。
*   **研究背景**：论文旨在将视频叙事从简单的“文本到视频”转换，提升为一种**全局优化问题**，通过多智能体协作实现电影级的叙事连贯性和视觉一致性。

### 2. 方法论：核心思想与技术细节
Co-Director 采用分层多智能体架构，核心流程如下：
*   **全局优化层（Orchestrator Agent）**：
    *   引入**多臂老虎机（MAB）**算法（具体为 UCB1 策略）来导航“创意空间”。
    *   **分层参数化**：将创意方向分解为三个正交维度：创意策略（如信息型、转化型）、叙事模式（如戏剧型、片段型）和美学原型（如电影感、极简主义）。
    *   **热启动（Warm Start）**：利用 LLM 根据受众画像预先初始化 MAB 的期望值，提高搜索效率。
*   **局部优化层（自细化循环）**：
    *   在前期制作（剧本）和关键帧生成阶段引入**多模态自细化（Self-Refinement）**。
    *   利用 MLLM（多模态大模型）作为“监制”，识别角色身份漂移或逻辑错误（如产品类别识别错误），并反馈给生成智能体进行局部重绘或重写。
*   **执行层（分工协作）**：
    *   **前期智能体（$\Phi_{pre}$）**：生成创意简报、故事线、视觉资产和分镜。
    *   **制作智能体（$\Phi_{prod}$）**：包含关键帧智能体（建立视觉先验）、视频智能体（扩散生成）和音频智能体（合成配音与背景音乐）。
    *   **后期智能体（$\Phi_{post}$）**：进行剪辑与音画同步。

### 3. 实验设计
*   **数据集/场景**：引入了 **GenAd-Bench**，包含 400 个针对 200 个**虚构产品**的广告场景。使用虚构产品是为了防止模型利用训练数据中的记忆（Memorization Bias）并规避版权问题。
*   **评估指标**：
    *   **VAF**（视觉资产忠实度）：Logo 和产品的一致性。
    *   **DA**（人口统计学对齐）：是否符合目标受众画像。
    *   **MA**（营销吸引力）：基于 AIDA 模型的说服力评估。
    *   **VQ**（视觉质量）：物理规律、伪影和时间连贯性。
*   **对比方法**：
    *   **商业平台**：Creatify, HeyGen。
    *   **基础模型**：Kling 3.0 Omni, Veo 3.1, Wan 2.6, LTX-2.3。
    *   **智能体框架**：AniMaker, MovieAgent。

### 4. 资源与算力
*   **模型使用**：论文使用了 Google 内部的一系列尖端模型，包括 **Gemini 3 Pro**（推理与评估）、**Nano Banana Pro**（图像生成）、**Veo 3.1**（视频生成）、**Lyria 2**（音频生成）。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该框架主要基于现有的基础模型进行智能体编排和推理优化，其核心开销在于多轮迭代生成的推理成本，而非从头训练模型的算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 400 个场景上进行了全量评估。
    *   **消融实验**：针对 MAB 逻辑（冷启动 vs 热启动、标量奖励 vs 分解奖励）和自细化循环（有无剧本/关键帧优化）进行了多组对比。
    *   **人类评估**：抽取 50 个场景，由 5 名独立评分员进行双盲评估，并计算了与 MLLM 评分的相关性（Krippendorff’s Alpha 等）。
*   **充分性与公平性**：实验设计较为严谨。通过使用相同的底层模型（Veo 3.1）来对比不同框架（AniMaker, MovieAgent），排除了模型性能差异带来的干扰，确保了架构评估的公平性。

### 6. 主要结论与发现
*   **性能领先**：Co-Director 在所有指标上均显著优于基准模型，平均得分（81.4）远高于最强的基础模型（65.0）和智能体框架（65.3）。
*   **MAB 的有效性**：相比随机搜索，MAB 结合热启动能以极少的迭代次数（T=4）快速收敛到最优创意配置。
*   **自细化的必要性**：关键帧自细化对维持产品身份一致性至关重要，移除该模块会导致资产忠实度（VAF）大幅下降。
*   **通用性**：虽然以广告为主要场景，但在通用电影叙事基准（ViStoryBench）上也表现出极强的风格和角色一致性。

### 7. 优点
*   **理论框架严谨**：首次将视频叙事形式化为全局优化问题，并引入 MAB 解决非微分流水线的优化难题。
*   **分层参数化**：通过将抽象的创意意图转化为具体的智能体指令，有效解决了跨模块的语义对齐问题。
*   **高质量基准**：GenAd-Bench 的虚构资产设计为生成式视频评估提供了一个无偏见、高难度的标准。

### 8. 不足与局限
*   **推理成本高**：由于引入了 MAB 迭代（T=4）和局部自细化（最多 3 次重试），生成单个视频的计算成本和时间显著高于单次推理模型。
*   **评估偏差**：尽管进行了人类验证，但核心评估仍依赖 MLLM（Gemini 3 Pro），可能存在“模型偏见”或对动态伪影的感知与人类不完全一致（如 VQ 指标相关性较低）。
*   **应用限制**：目前主要针对短视频（12秒/4个镜头），在超长视频（如分钟级电影）中的表现尚未验证。

（完）
