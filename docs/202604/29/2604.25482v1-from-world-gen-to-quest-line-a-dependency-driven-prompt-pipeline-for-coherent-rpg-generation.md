---
title: "From World-Gen to Quest-Line: A Dependency-Driven Prompt Pipeline for Coherent RPG Generation"
title_zh: 从世界生成到任务线：一种用于连贯 RPG 生成的依赖驱动提示词流水线
authors: "Dominik Borawski, Marta Szulc, Robert Chudy, Małgorzata Giedrowicz, Piotr Mironowicz"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25482v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 强制执行模式和结构化JSON输出
tldr: 本研究针对大型语言模型在生成复杂RPG世界时存在的连贯性和一致性问题，提出了一种依赖驱动的多阶段提示词流水线。该方法通过结构化JSON中间表示，将生成过程分解为世界观、角色及任务规划等阶段，有效减少了叙事漂移和幻觉，为程序化内容生成提供了可扩展且逻辑严密的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决LLM在生成多层级RPG世界时面临的叙事连贯性差、可控性低及结构不一致等挑战。
method: 提出一种依赖感知的多阶段流水线，利用结构化JSON定义数据流，将生成任务分解为从世界构建到任务扩展的五个顺序阶段。
result: 实验表明该流水线能持续生成逻辑自洽、结构完整且具备叙事深度的RPG内容，且质量不会随系统复杂度的增加而下降。
conclusion: 采用结构化中间表示的依赖感知提示词流水线是LLM程序化内容生成的有效设计模式，并可推广至其他需要顺序推理的领域。
---

## 摘要
大语言模型（LLMs）在叙事生成方面展现出强大潜力，但在复杂、多层级的角色扮演游戏（RPG）世界中的应用仍受限于连贯性、可控性和结构一致性等问题。本文探索了一种用于程序化 RPG 内容生成的依赖感知多阶段提示词流水线，该流水线通过结构化中间表示对叙事依赖关系进行建模。该方法将生成过程分解为若干顺序阶段：世界构建、非玩家角色创建、玩家角色创建、战役级任务规划以及任务扩展。每个阶段都以先前阶段生成的结构化 JSON 输出为条件。通过强制执行模式（Schema）和显式数据流，该流水线减少了叙事偏移，限制了幻觉，并支持可扩展地创建相互关联的叙事元素。系统通过多次独立运行中的以人为中心的分析进行了定性评估。输出结果根据结构完整性、内部一致性、叙事连贯性、多样性和可执行性等标准进行评估。结果表明，该流水线能够持续生成逻辑合理且结构有效的 RPG 内容，且质量不会随着复杂性的增加而下降。将高层级的战役规划与详细的任务扩展分离，既改善了全局结构，也提升了局部叙事。这些发现表明，带有结构化中间表示的依赖感知提示词流水线是基于 LLM 的程序化内容生成的一种有效设计模式。该方法还可能推广到其他需要在演变上下文状态上进行顺序推理的领域。

## Abstract
Large Language Models (LLMs) have shown strong potential for narrative generation, but their use in complex, multi-layered role-playing game (RPG) worlds is still limited by issues of coherence, controllability, and structural consistency. This paper explores a dependency-aware, multi-stage prompt pipeline for procedural RPG content generation that models narrative dependencies through structured intermediate representations. The approach decomposes generation into sequential stages: world building, non-player character creation, player character creation, campaign-level quest planning, and quest expansion. Each stage conditions on structured JSON outputs from previous stages. By enforcing schemas and explicit data flow, the pipeline reduces narrative drift, limits hallucinations, and supports scalable creation of interconnected narrative elements. The system is evaluated qualitatively through human-centered analysis across multiple independent runs. Outputs are assessed using criteria such as structural completeness, internal consistency, narrative coherence, diversity, and actionability. Results show that the pipeline consistently generates logically sound and structurally valid RPG content, without quality degradation as complexity increases. Separating high-level campaign planning from detailed quest expansion improves both global structure and local storytelling. These findings suggest that dependency-aware prompt pipelines with structured intermediate representations are an effective design pattern for LLM-based procedural content generation. This approach may also generalize to other domains requiring sequential reasoning over evolving contextual states.

---

## 论文详细总结（自动生成）

以下是对论文《From World-Gen to Quest-Line: A Dependency-Driven Prompt Pipeline for Coherent RPG Generation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在叙事生成方面表现出色，但在生成复杂、多层级的角色扮演游戏（RPG）内容时，常面临**叙事漂移（Narrative Drift）**、**幻觉**以及**结构不一致**等挑战。
*   **核心问题**：如何确保生成的游戏世界、角色和任务线在逻辑上高度连贯，并能直接集成到游戏开发工作流中？
*   **整体含义**：本文提出了一种“依赖驱动”的提示词流水线，通过将生成任务分解为多个相互关联的阶段，并利用结构化数据（JSON）作为中间表示，实现了高连贯性、可控且可扩展的 RPG 内容生成。

### 2. 方法论
*   **核心思想**：采用分层、顺序生成的策略，将前一阶段的结构化输出作为后一阶段的输入上下文，显式建模叙事依赖关系。
*   **关键技术细节（五阶段流水线）**：
    1.  **世界生成（World Gen）**：设定基础背景、地理、派系和政治结构。
    2.  **非玩家角色生成（NPC Gen）**：基于世界背景生成多个 NPC，并定义其性格、秘密及相互间的社会关系网。
    3.  **玩家角色生成（PC Gen）**：将主角嵌入已建立的世界和社会关系中。
    4.  **战役级任务规划（Quest Planning）**：生成完整的任务大纲，确保任务间的因果逻辑和进度衔接。
    5.  **任务扩展（Extended Quest Gen）**：将大纲细化为包含对话、分支选项和奖励的详细叙事。
*   **约束机制**：所有阶段均强制执行预定义的 **JSON Schema**，确保输出的结构化和确定性，便于程序解析和验证。

### 3. 实验设计
*   **实验场景**：进行了 **20 次独立且完整的流水线运行**。
*   **生成规模**：总计生成了 20 个世界、20 个玩家角色、200 个 NPC、200 条战役任务线及 200 个详细扩展任务。
*   **评估基准（Benchmark）**：由于传统的 NLP 指标（如 BLEU/ROUGE）在叙事评估上表现较差，本文采用了**以人为中心的定性评估协议**。
*   **评估维度**：结构完整性、内部一致性、叙事连贯性、多样性、可执行性（Actionability）。
*   **对比方法**：本文主要侧重于验证所提流水线的有效性，未设置显式的单提示词（Single-prompt）对比组，但通过分析复杂度增加时的质量变化来验证其优越性。

### 4. 资源与算力
*   **模型使用**：使用了 **GPT-5**（注：文中多次提及 GPT-5，可能为基于未来时间线的设定或特定版本代号）通过 OpenAI API 进行零样本（Zero-shot）生成。
*   **参数设置**：Temperature 设为 1.0，最大 Token 限制为 32,768。
*   **硬件环境**：标准的桌面/笔记本电脑（Python 3.12 环境），推理过程在云端 API 完成，本地仅负责逻辑编排和 JSON 验证。

### 5. 实验数量与充分性
*   **实验数量**：20 组完整运行产生了数百个相互关联的叙事工件，样本量对于定性研究较为充分。
*   **充分性与客观性**：实验覆盖了从宏观世界到微观对话的完整链路。然而，评估由 4 名参与系统设计的专家完成，虽然制定了严格的 5 分制李克特量表（Likert Scale），但仍存在一定的**主观性**和**确认偏误**风险。

### 6. 主要结论与发现
*   **连贯性保持**：流水线能有效防止叙事崩溃，即使在依赖深度增加的后期阶段（如任务扩展），内容质量也未出现显著下降。
*   **结构化优势**：JSON 约束不仅减少了幻觉，还使生成内容具备高度的“可执行性”，可直接用于游戏引擎。
*   **规划与执行分离**：将“战役规划”与“任务细化”分开，是平衡全局叙事节奏与局部细节描述的关键设计模式。

### 7. 优点
*   **设计模式创新**：提出了一种可扩展的 LLM 程序化内容生成（PCG）设计模式，解决了长文本生成的逻辑断层问题。
*   **实用性强**：生成的 JSON 工件可作为游戏引擎与 LLM 之间的“通用语言”，支持人类设计师进行干预和修改。
*   **模块化**：各阶段相互独立又逻辑关联，允许开发者根据需求单独优化或替换某一阶段。

### 8. 不足与局限
*   **评估局限**：缺乏外部独立评审员的参与，且未引入自动化的量化评估指标。
*   **对比缺失**：未与单提示词生成或传统的基于规则的 PCG 系统进行量化的消融实验对比。
*   **模型依赖**：实验仅基于单一模型配置，未测试不同规模或架构的模型（如开源模型）在该流水线下的表现。
*   **应用限制**：目前主要针对文本叙事，尚未涉及地图拓扑生成等更具空间约束的 RPG 元素。

（完）
