---
title: Why Search When You Can Transfer? Amortized Agentic Workflow Design from Structural Priors
title_zh: 既然可以迁移，为何还要搜索？基于结构先验的摊销智能体工作流设计
authors: "Shiyi Du, Jiayuan Liu, Weihua Du, Yue Huang, Jiayi Li, Yingtao Luo, Xiangliang Zhang, Vincent Conitzer, Carl Kingsford"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.25012v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 自动化智能体工作流设计与多智能体协作框架
tldr: 针对当前自动化智能体工作流设计依赖高成本迭代搜索且无法复用知识的问题，本文提出SWIFT框架。该框架通过从历史搜索轨迹中提取结构先验和启发式规则，将工作流设计转化为摊销式的少样本迁移过程。在推理时，仅需单次生成即可为新任务合成高效工作流。实验表明，SWIFT在性能上超越了最先进的搜索方法，同时将优化成本降低了三个数量级，并展现出极强的跨模型和跨领域泛化能力。
selection_source: fresh_fetch
motivation: 现有的智能体工作流设计依赖于针对每个任务的冗余迭代搜索，计算成本极高且忽略了不同任务间可复用的结构化知识。
method: 提出SWIFT框架，通过对比分析历史搜索轨迹提取结构先验，并结合跨任务示例在推理时单次生成可执行的工作流。
result: 在五个基准测试中，SWIFT以千分之一的成本超越了搜索类方法，并成功迁移至多种基础模型和未见过的任务领域。
conclusion: 智能体工作流的设计核心在于拓扑结构的迁移而非表面语义，通过摊销结构先验可以实现高效且通用的自动化设计。
---

## 摘要
自动化的智能体工作流设计目前依赖于针对每个任务的迭代搜索，这在计算上成本极高，且无法在任务间复用结构性知识。我们观察到，优化后的工作流会收敛到一小类特定领域的拓扑结构，这表明这种组合搜索在很大程度上是冗余的。基于这一洞察，我们提出了 SWIFT（通过少样本迁移合成工作流），这是一个将工作流设计摊销为可复用结构先验的框架。SWIFT 首先通过对源任务中先前搜索轨迹的对比分析，提取出组合启发式方法和输出接口契约。在推理阶段，它将这些先验与跨任务工作流示例相结合，通过单次大语言模型（LLM）生成过程，为未见过的目标任务合成完整且可执行的工作流，从而完全绕过迭代搜索。在五个基准测试中，SWIFT 的表现优于最先进的基于搜索的方法，同时将每个任务的边际优化成本降低了三个数量级。它还进一步泛化到另外四个未见过的基准测试，并成功从 GPT-4o-mini 迁移到另外三个基础模型（Grok、Qwen、Gemma）。受控消融实验表明，工作流示例主要迁移的是拓扑结构而非表面语义：即使将所有算子名称替换为随机字符串，仍能保留整个系统平均性能的 93% 以上。

## Abstract
Automated agentic workflow design currently relies on per-task iterative search, which is computationally prohibitive and fails to reuse structural knowledge across tasks. We observe that optimized workflows converge to a small family of domain-specific topologies, suggesting that this combinatorial search is largely redundant. Building on this insight, we propose SWIFT (Synthesizing Workflows via Few-shot Transfer), a framework that amortizes workflow design into reusable structural priors. SWIFT first distills compositional heuristics and output-interface contracts from contrastive analysis of prior search trajectories across source tasks. At inference time, it conditions a single LLM generation pass on these priors together with cross-task workflow demonstrations to synthesize a complete, executable workflow for an unseen target task, bypassing iterative search entirely. On five benchmarks, SWIFT outperforms the state-of-the-art search-based method while reducing marginal per-task optimization cost by three orders of magnitude. It further generalizes to four additional unseen benchmarks and transfers successfully from GPT-4o-mini to three additional foundation models (Grok, Qwen, Gemma). Controlled ablations reveal that workflow demonstrations primarily transfer topological structure rather than surface semantics: replacing all operator names with random strings still retains over 93% of the full system's average performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Why Search When You Can Transfer? Amortized Agentic Workflow Design from Structural Priors》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前自动化智能体工作流（Agentic Workflow）设计主要依赖于**针对每个任务的迭代搜索**（如 MCTS 或进化算法）。这种方式存在两个致命缺陷：
*   **计算成本极高**：每个新任务都要从零开始搜索，消耗大量 API 调用。
*   **知识冗余与浪费**：研究者观察到，不同任务的最优工作流往往收敛于相似的拓扑结构（如数学任务倾向于“多路径采样+投票”，代码任务倾向于“执行-反馈-重试”）。
**核心动机**：既然不同任务间存在可复用的结构先验，能否通过“迁移学习”而非“重复搜索”来设计工作流？论文提出了 **SWIFT** 框架，旨在实现工作流设计的“摊销化”（Amortized），即通过一次性学习结构先验，实现对新任务的单次生成式合成。

### 2. 方法论：核心思想与技术细节
SWIFT 框架分为两个阶段：
*   **离线阶段：对比轨迹蒸馏（Contrastive Trajectory Distillation）**
    *   分析源任务的历史搜索轨迹，对比高性能和低性能工作流。
    *   **提取组合启发式规则（Heuristics）**：总结算子层面的路由规则。
    *   **提取输出契约（Contracts）**：识别导致失败的格式不匹配问题，制定严格的接口规范，确保节点间数据流的鲁棒性。
*   **在线阶段：少样本工作流合成（Few-shot Synthesis）**
    *   **输入**：目标任务描述、算子库定义、蒸馏出的启发式规则与契约、以及来自其他任务的成功工作流示例（Demonstrations）。
    *   **过程**：使用大模型（如 GPT-4o-mini）作为元生成器，在**单次推理**中直接生成目标任务的完整 Python 代码工作流。
    *   **核心协议**：采用严格的“留一法”（Leave-one-out），确保合成目标任务工作流时，模型从未见过该任务的任何搜索数据。

### 3. 实验设计
*   **数据集**：
    *   **分布内（ID）**：GSM8K, MATH, HumanEval, MBPP。
    *   **分布外（OOD）**：MultiArith, SVAMP, AIME, AQuA, BigCodeBench。
*   **对比方法（Baselines）**：
    *   **单智能体**：Vanilla, CoT, Self-Consistency。
    *   **手工设计**：DyLAN, AgentVerse, LLM-Debate 等。
    *   **自动化搜索（SOTA）**：**AFlow**（主要对比对象）、ADAS、GPTSwarm、MaAS。
*   **跨模型验证**：除了 GPT-4o-mini，还测试了 Grok-4.1-fast, Qwen3-8B, Gemma-3-12B-IT。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号和数量，因为其核心实验基于 OpenAI 等厂商的 API。
*   **成本分析**：论文详细对比了 API 调用成本。在 MATH 任务上，AFlow 的优化成本约为 22.50 美元，而 SWIFT 的单次合成成本仅为 **0.004 美元**，成本降低了三个数量级（约 5600 倍）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个基准测试，总计超过 4400 个测试实例。
*   **消融实验**：进行了多项深度消融，包括：
    *   **Random-ops**：将算子名称替换为随机字符串，验证模型是否真的理解拓扑结构（结果显示性能仅下降不到 7%）。
    *   **Zero-shot**：不提供示例的情况。
    *   **Cross-domain**：提供错误领域的示例。
*   **评价**：实验设计非常充分且严谨。通过留一法和 OOD 测试有力地证明了方法的泛化能力，而非简单的记忆。

### 6. 主要结论与发现
*   **性能超越搜索**：SWIFT 在单次生成下的表现优于经过数百轮迭代搜索的 AFlow。
*   **拓扑结构是核心**：实验证明 LLM 迁移的是抽象的“拓扑路由策略”，而非简单的语义标签。
*   **契约引导至关重要**：输出契约能有效弥合“结构-功能差距”，防止因格式错误导致的工作流崩溃。
*   **通用性强**：生成的策略在不同规模和厂商的模型（Grok, Qwen 等）上均表现出一致的增益。

### 7. 优点
*   **极高的效率**：将原本昂贵的工作流搜索过程变成了近乎零成本的单次生成。
*   **深刻的洞察**：揭示了智能体工作流设计中“拓扑结构优于算子语义”的本质。
*   **即插即用**：不需要对模型进行微调，仅通过 Prompt 工程和结构先验即可实现。
*   **鲁棒性**：通过契约机制解决了多智能体协作中常见的格式解析失败问题。

### 8. 不足与局限
*   **能力上限受限**：在极难任务（如 AIME 竞赛数学）上，虽然工作流有优化，但受限于基础模型的推理 ceiling，提升空间有限。
*   **环境依赖**：在复杂代码生成（如 BigCodeBench）中，很多失败源于执行环境（如缺少 Python 库），这是工作流设计无法解决的。
*   **指令遵循要求**：对于指令遵循能力较弱的模型（如较小的开源模型），复杂的工作流拓扑可能会导致错误累积。
*   **静态局限**：目前主要针对静态推理任务，在动态、不可逆的环境（如实时交互系统）中的表现尚待验证。

（完）
