---
title: Why Does Reinforcement Learning Generalize? A Feature-Level Mechanistic Study of Post-Training in Large Language Models
title_zh: 强化学习为何能泛化？大语言模型训练后阶段的特征级机理研究
authors: "Dan Shi, Zhuowen Han, Simon Ostermann, Renren Jin, Josef van Genabith, Deyi Xiong"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.25011v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型推理性能强化学习后训练的机制研究
tldr: 本研究探讨了强化学习（RL）在大型语言模型后训练中比监督微调（SFT）更具泛化能力的原因。通过特征级机理分析，研究者对比了RL和SFT在相同数据下的特征演化。发现SFT会快速引入导致遗忘的专用特征，而RL则能保留基础模型表示并诱导出一组通用的泛化特征。通过干预实验，验证了这些特征在跨任务泛化中的因果作用，为理解RL的泛化机制提供了新视角。
selection_source: fresh_fetch
motivation: 旨在揭示为何RL后训练能提升LLM的推理泛化能力，而SFT却常导致通用能力下降的内在机制。
method: 提出一种特征级机理分析方法，通过在共享特征空间中对齐内部激活，追踪RL和SFT过程中特征的演化。
result: 发现SFT会引入大量高度专业化的特征，而RL则通过保留基础模型表示并培养一组任务无关的紧凑特征来促进泛化。
conclusion: RL的泛化能力由特定的任务无关特征介导，通过干预这些特征可以显著影响模型的泛化表现。
---

## 摘要
基于强化学习（RL）的训练后处理（post-training）通常能提升大语言模型（LLM）在训练域之外的推理性能，而监督微调（SFT）则经常导致通用能力的遗忘。然而，这种差异背后的机制尚不明确。为了填补这一空白，我们提出了一种特征级机理分析方法，通过受控实验设置来探究 RL 的泛化性，其中 RL 和 SFT 微调模型是在相同数据上基于同一基座模型训练而成的。利用我们的可解释性框架，我们在共享特征空间内对齐了不同模型的内部激活，并分析了特征在训练后阶段的演变过程。我们发现，SFT 会迅速引入许多高度专门化的特征，并在训练早期趋于稳定；而 RL 引起的特征变化则更为克制且持续演变，在很大程度上保留了基座模型的表示。通过关注 RL 成功而基座模型失败的样本，我们识别出了一组紧凑且与任务无关的特征，它们直接介导了跨不同任务的泛化。特征级干预证实了它们的因果作用：禁用这些特征会显著降低 RL 模型的泛化性能，而放大这些特征则能提升基座模型的性能。代码可在 https://github.com/danshi777/RL-generalization 获取。

## Abstract
Reinforcement learning (RL)-based post-training often improves the reasoning performance of large language models (LLMs) beyond the training domain, while supervised fine-tuning (SFT) frequently leads to general capabilities forgetting. However, the mechanisms underlying this contrast remain unclear. To bridge this gap, we present a feature-level mechanistic analysis methodology to probe RL generalization using a controlled experimental setup, where RL- and SFT-tuned models are trained from the same base model on identical data. Leveraging our interpretability framework, we align internal activations across models within a shared feature space and analyze how features evolve during post-training. We find that SFT rapidly introduces many highly specialized features that stabilize early in training, whereas RL induces more restrained and continually evolving feature changes that largely preserve base models' representations. Focusing on samples where RL succeeds but the base model fails, we identify a compact, task-agnostic set of features that directly mediate generalization across diverse tasks. Feature-level interventions confirm their causal role: disabling these features significantly degrades RL models' generalization performance, while amplifying them improves base models' performance. The code is available at https://github.com/danshi777/RL-generalization.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在强化学习（RL）与监督微调（SFT）后的泛化差异进行了深入的特征级机理研究。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：在 LLM 的后训练阶段，RL（如 GRPO）通常能显著提升模型在训练域之外的推理能力，而 SFT 虽然能快速学习特定任务，但往往导致通用能力的下降或遗忘。
*   **核心问题**：为什么 RL 比 SFT 具有更好的泛化性？这种差异在模型内部特征表示层面是如何体现的？

### 2. 方法论
论文提出了一套基于**稀疏交叉编码器（Sparse Crosscoder）**的可解释性分析框架：
*   **特征对齐与归属**：
    *   **两模型交叉编码器**：将基座模型与微调模型（SFT 或 RL）的内部激活对齐到共享的稀疏特征空间，利用**归一化相对范数（NRN）**衡量特征的模型特异性。
    *   **三模型交叉编码器**：同时对齐基座、SFT 和 RL 模型，引入**模型归属得分（MAS）**，量化每个特征对特定训练范式的归属程度。
*   **泛化特征识别**：
    *   定义“泛化关键样本”：基座模型失败但 RL 模型成功的样本。
    *   计算**泛化得分**：测量 RL 模型与基座模型在这些样本上的激活差异，识别出跨任务一致激活的紧凑特征集。
*   **因果干预**：通过在推理时“置零”（禁用）或“放大”特定特征，验证其对泛化性能的因果影响。

### 3. 实验设计
*   **模型与数据**：使用 Qwen3-4B-Base、Qwen2.5-7B 和 Llama3.1-8B-Instruct 作为基座。在相同的 47K 高质量数学题目上分别进行 SFT（蒸馏思维链）和 RL（基于结果的奖励）。
*   **评测基准**：
    *   **数学推理**：MATH500、AIME24、AIME25。
    *   **通用泛化任务**：OpenBookQA、CommonsenseQA、HeadQA、SciQ、ARC-Challenge。
    *   **未见任务（验证）**：LogiQA、PIQA。
*   **对比方法**：严格控制变量，确保 SFT 和 RL 使用完全相同的基座模型和训练数据。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H200 和 H100 GPU**。
*   **训练细节**：RL 采用 verl 框架实现 GRPO，学习率 $1 \times 10^{-6}$；SFT 采用 LLaMA-Factory，学习率 $5 \times 10^{-5}$。两者均训练一个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同家族/规模的模型（4B、7B、8B），在 8 个以上的基准测试集上进行了评估。
*   **充分性**：除了性能对比，还进行了特征演化追踪（5 个训练阶段检查点）、跨任务特征重合度分析以及双向因果干预实验。
*   **客观性**：通过自建受控实验环境（而非使用现成的闭源模型），排除了数据污染和训练细节不一致带来的偏差，实验设计非常公平。

### 6 主要结论与发现
*   **特征动力学差异**：SFT 会迅速引入大量高度专业化的新特征，并在训练早期固化，同时抑制基座模型的原始特征（表现为“记忆”）；RL 引起的特征变化更克制、演化更缓慢，且很大程度上保留了基座模型的表示结构。
*   **泛化机制**：RL 并不是简单地提升任务表现，而是强化了一组**任务无关（Task-agnostic）**的紧凑特征。
*   **因果验证**：禁用 RL 模型中的泛化特征会导致性能大幅下降（部分任务下降 >40%）；而在基座模型中放大这些特征，能使其在未见过的任务上获得显著提升（如 SciQ 提升 56%）。

### 7. 优点（亮点）
*   **受控实验设计**：解决了以往研究中 SFT 和 RL 数据/模型不一致的对比难题。
*   **机理深度**：从“描述现象”深入到“识别特征”并进行“因果干预”，证明了泛化特征的存在。
*   **跨架构一致性**：在 Qwen 和 Llama 两个架构上均验证了结论的有效性。

### 8. 不足与局限
*   **特征覆盖范围**：稀疏交叉编码器虽然可解释性强，但可能无法捕捉到所有功能相关的复杂内部结构。
*   **训练指导有限**：目前的干预是在推理阶段进行的，虽然解释了“为什么”，但尚未直接提出如何在训练阶段显式诱导这些泛化特征的具体算法改进。
*   **层级限制**：分析主要集中在模型的中间层（残差流），可能忽略了底层感知或高层决策特征的细微变化。

（完）
