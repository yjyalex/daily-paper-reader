---
title: "R-CoT: A Reasoning-Layer Watermark via Redundant Chain-of-Thought in Large Language Models"
title_zh: R-CoT：大语言模型中通过冗余思维链实现的推理层水印
authors: "Ziming Zhang, Li Li, Guorui Feng, Hanzhou Wu, Xinpeng Zhang"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25247v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 在思维链推理上应用强化学习进行水印嵌入
tldr: 针对大语言模型水印易受扰动和擦除的问题，本文提出了一种推理层水印框架R-CoT。该方法通过冗余思维链（Redundant CoT）将水印嵌入推理路径，并利用基于GRPO的双轨迹优化机制，使原生推理与水印推理路径在共享参数空间中共存。R-CoT将水印内化为一种推理策略，显著提升了水印的鲁棒性，在微调等后训练操作下仍能保持极高的检测率，为模型版权保护提供了新思路。
selection_source: fresh_fetch
motivation: 现有的LLM水印方法多依赖于对输出分布的表层修改，容易在受到扰动或微调时失效。
method: 提出R-CoT框架，利用GRPO优化机制将水印作为冗余推理路径嵌入模型内部，使其成为一种稳定的推理策略。
result: "实验表明R-CoT具有极高的有效性和鲁棒性，在经过微调等操作后，真阳性率（TPR）仍能稳定在95%以上。"
conclusion: R-CoT通过在推理层嵌入水印，克服了传统输出层水印的脆弱性，实现了更持久的模型所有权验证。
---

## 摘要
大语言模型（LLMs）因其推理能力而被广泛部署于多种场景。为了防止模型被滥用，通常采用水印技术来确保所有权。然而，大多数现有的水印方法依赖于对模型输出分布的表面修改，使得水印容易受到扰动和移除。为了克服这一挑战，本文介绍了一种名为冗余思维链（R-CoT）的推理层框架，该框架将水印嵌入到推理路径中。一种基于 GRPO 的双轨迹优化机制使得原生推理路径和水印推理路径能够在共享的参数空间中共存，从而将水印内化为一种独特的推理策略。因此，水印被嵌入到模型稳定的推理路径中，避免了由输出层扰动引起的水印失效。实验结果表明，与现有方法相比，R-CoT 实现了极高的水印有效性和强大的鲁棒性。在微调和其他训练后操作下，真阳性率（TPR）始终保持在 95% 以上，仅表现出轻微的退化。

## Abstract
Large language models (LLMs) are widely deployed in multiple scenarios due to reasoning capabilities. In order to prevent the models from being misused, watermarking is generally employed to ensure ownership. However, most existing watermarking methods rely on superficial modifications to the model's output distribution, rendering the watermark vulnerable to perturbation and removal. To overcome this challenge, this paper introduces a reasoning-layer framework termed Redundant Chain-of-Thought (R-CoT), which embeds watermarks into the reasoning path. A dual-trajectory optimization mechanism based on GRPO enables the native and the watermark reasoning path to coexist within a shared parameter space, internalizing the watermark as a distinct reasoning policy. Therefore, the watermark is embedded into the model's stable reasoning path, avoiding the watermark failure caused by output-level perturbations. Experimental results show that, compared with existing methods, R-CoT achieves high watermark effectiveness and strong robustness. Under fine-tuning and other post-training operations, the true positive rate (TPR) consistently remains above 95%, exhibiting only marginal degradation.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **R-CoT** 的新型大语言模型（LLM）水印框架，旨在通过推理层的深度嵌入来解决传统水印易被擦除的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着 OpenAI-o1 和 DeepSeek-R1 等推理导向模型的兴起，思维链（CoT）成为模型的核心能力。然而，LLM 面临严重的版权侵权和滥用风险。
*   **核心痛点**：现有的水印技术大多属于“输出层水印”（修改 Token 分布或统计特征），这种表层修改在面对改写、翻译或监督微调（SFT）等后训练操作时极其脆弱，容易失效。
*   **研究目标**：将水印从“表层文本”深入到“推理逻辑”层面，通过在模型的推理路径中嵌入一种稳定的、不可或缺的冗余逻辑，实现高鲁棒性的版权保护。

### 2. 方法论
*   **核心思想**：引入**冗余思维链（Redundant Chain-of-Thought, R-CoT）**。水印不再是随机插入的字符，而是一段与任务语义相关、但不影响最终答案正确性的“冗余验证步骤”。
*   **关键技术细节**：
    *   **触发机制**：设计低频 Token（如反向单词 `thgirypoc`）作为触发器。只有当输入包含触发器时，模型才会激活水印推理路径。
    *   **双轨迹优化机制**：利用 **GRPO（组相对策略优化）** 强化学习算法，在同一个参数空间内同时优化“原生推理路径”和“水印推理路径”。
    *   **奖励函数设计**：构建了复合奖励函数 $R$，包括：
        1.  **准确性奖励 ($R_c$)**：确保最终答案正确。
        2.  **位置奖励 ($R_l$)**：确保 R-CoT 出现在指定的推理段落内。
        3.  **格式奖励 ($R_s$)**：强制模型遵循特定的推理格式。
        4.  **冗余推理奖励 ($R_w$)**：鼓励模型在触发时生成冗余的验证步骤。
*   **算法流程**：首先构造包含触发器的训练集；然后通过 GRPO 引导模型自主学习两种共存的推理策略；验证时，通过检测推理轨迹中是否存在预设的冗余模式来确认所有权。

### 3. 实验设计
*   **基座模型**：Llama3.1-8B 和 Qwen2.5-7B。
*   **数据集**：
    *   训练集：GSM8K-train。
    *   测试集：GSM8K-test（分布内）和 Math10K（分布外泛化测试）。
*   **Benchmark 与对比方法**：
    *   对比了基于数据集的 SFT 水印方法和 **CRMark**（一种基于强化学习的 CoT 水印方法）。
*   **评估指标**：真阳性率（TPR）、假阳性率（FPR）、触发后准确率（T-Acc）和非触发准确率（NT-Acc）。

### 4. 资源与算力
*   **算力说明**：论文中提到使用了 LoRA（低秩自适应）进行参数高效微调，并采用了 GRPO 框架进行强化学习训练。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号、数量以及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流模型和两个数学推理数据集上进行了基础性能测试。
    *   **鲁棒性实验**：针对不同比例（5% 到 100%）的 SFT 微调进行了详尽测试。
    *   **安全性实验**：包括触发器位置扰动、解码参数（Temperature/Top-p）变化、以及输入层攻击（零宽字符插入、同形字符替换）。
*   **充分性评价**：实验设计较为全面，特别是针对“高强度微调攻击”的测试（使用 80% 的数据进行二次微调后水印仍存活）具有很强的说服力，客观地展示了推理层水印的稳定性。

### 6. 主要结论与发现
*   **极高的有效性**：R-CoT 在多个设置下实现了接近 100% 的 TPR，且 FPR 保持为 0。
*   **高保真度**：嵌入水印后，模型在常规任务上的准确率下降极小（通常在 3% 以内），证明了双轨迹优化能有效分离两种推理模式。
*   **超强鲁棒性**：在面对大规模监督微调（SFT）时，R-CoT 的表现远超现有方法。即使在 80% 的数据微调下，TPR 仍能保持在 95% 以上。
*   **抗干扰能力**：对输入层的编码级攻击（如隐藏字符）具有天然的抵抗力。

### 7. 优点
*   **范式创新**：首次提出将水印内化为模型的一种“推理策略”，而非简单的输出模式。
*   **机制优越**：利用 GRPO 避免了传统 SFT 强制模型机械模仿的问题，使推理路径更加自然且难以剔除。
*   **实用性强**：水印模式（冗余验证）在逻辑上是合理的，不易被人类察觉为异常。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在数学推理任务上，对于缺乏明显思维链结构的场景（如短文本生成、创意写作）可能难以应用。
*   **触发器依赖**：虽然推理路径是隐蔽的，但激活该路径仍依赖于特定的触发 Token，若触发器被泄露，攻击者可能通过针对性过滤来规避。
*   **训练成本**：相比于简单的输出层水印，使用 GRPO 进行强化学习训练的计算开销相对较高。

（完）
