---
title: How Fast Should a Model Commit to Supervision? Training Reasoning Models on the Tsallis Loss Continuum
title_zh: 模型应多快向监督收敛？在 Tsallis 损失连续体上训练推理模型
authors: "Chu-Cheng Lin, Eugene Ie"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25907v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用可验证奖励的强化学习和损失插值训练推理模型
tldr: 本研究针对推理模型在强化学习（RLVR）中面临的冷启动难题，提出基于Tsallis q-对数的损失函数族 J_Q。该方法通过调节参数 q 在强化学习（q=0）与对数边际似然（q=1）之间插值，利用梯度放大机制显著加快模型从低初始成功率状态逃逸的速度。实验表明，所提出的GARL和PAFT算法在FinQA、HotPotQA等任务上有效解决了GRPO等传统方法的停滞问题，显著提升了推理性能。
selection_source: fresh_fetch
motivation: 传统的强化学习方法在初始成功率较低时容易陷入冷启动停滞，导致模型难以从仅有输出层监督的任务中学习。
method: 提出一种基于Tsallis q-对数的损失函数族，并推导出GARL和PAFT两种蒙特卡洛估计器，通过梯度放大或后验重采样来平衡探索速度与训练稳定性。
result: 在FinQA和HotPotQA等数据集上，GARL在冷启动场景下表现优异，而PAFT在复杂任务中提供了更稳定的梯度，性能大幅超越GRPO。
conclusion: 通过在Tsallis损失连续体上选择合适的 q 值，可以有效解决推理模型的冷启动问题并提升复杂推理任务的训练效果。
---

## 摘要
在后训练期间，当初始成功概率 $p_0$ 较小时，仅使用输出级监督将推理模型适配到新任务时，在基于可验证奖励的强化学习（RLVR）下会发生停滞。利用 Tsallis $q$-对数，我们定义了一个损失族 $J_Q$，它在 RLVR（$q{=}0$，利用极点）和隐式轨迹上的对数边际似然（$q{=}1$，密度估计极点）

## Abstract
Adapting reasoning models to new tasks during post-training with only output-level supervision stalls under reinforcement learning from verifiable rewards (RLVR) when the initial success probability $p_0$ is small. Using the Tsallis $q$-logarithm, we define a loss family $J_Q$ that interpolates between RLVR (at $q{=}0$, the exploitation pole) and the log-marginal-likelihood over latent trajectories (at $q{=}1$, the density-estimation pole). All members share the same per-example gradient direction, differing only by a scalar amplification $P_{θ^{-q}}$ that reweights each instance independently of the learning rate. This amplification is the mechanism that addresses cold-start stalling: under gradient flow, the exploitation pole requires $Ω(\frac{1}{p_0})$ time to escape cold start, while the density-estimation pole escapes in $Θ\big(\log(\frac{1}{p_0})\big)$; intermediate $q$ trades escape speed against noise memorization. Because $P_θ$ is intractable, we derive two Monte Carlo estimators from the two factorizations of the gradient: Gradient-Amplified RL (GARL) samples from the prior and amplifies the RL gradient, and Posterior-Attenuated Fine-Tuning (PAFT) importance-resamples from the posterior and runs standard SFT. Both have bias $O\big(\frac{q}{M P_θ^{q+1}}\big)$; GARL has lower variance, PAFT has semantically coherent gradients. On FinQA, HotPotQA, and MuSiQue, GARL at $q{=}0.75$ substantially mitigates cold-start stalling, escaping cold start where GRPO fails entirely. In warm start, GARL at low $q$ dominates FinQA where training is stable; on HotPotQA and MuSiQue, GARL destabilizes during training, and PAFT at $q{=}0.75$ provides stable gradients (best overall on HotPotQA at 47.9 maj@16, $+14.4$ over GRPO).

---

## 论文详细总结（自动生成）

这篇论文《How Fast Should a Model Commit to Supervision? Training Reasoning Models on the Tsallis Loss Continuum》由 Google 的研究人员撰写，探讨了推理模型在强化学习训练中的“冷启动”瓶颈问题，并提出了一个统一的理论框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在训练推理模型（如 Chain-of-Thought）时，通常使用基于可验证奖励的强化学习（RLVR）。然而，当模型初始成功率（$p_0$）极低时，RLVR 往往会陷入“冷启动停滞”，因为模型几乎无法获得正向奖励信号来产生有效的梯度。
*   **核心问题**：模型应该以多快的速度向监督信号“承诺”（收敛）？论文旨在寻找一种能够平衡“探索速度”与“噪声抗性”的训练方法，使模型能从零开始高效学习复杂的推理任务。

### 2. 方法论
*   **核心思想**：引入 **Tsallis $q$-对数** 定义了一个损失函数族 $J_Q$。该损失族在两个极端（极点）之间进行连续插值：
    *   **$q=0$（利用极点）**：等同于传统的 RLVR，关注最大化期望准确率，但冷启动极慢。
    *   **$q=1$（密度估计极点）**：等同于对数边际似然（MLE），冷启动逃逸极快，但容易记忆噪声。
*   **关键技术细节**：
    *   **梯度放大因子**：论文证明所有 $J_Q$ 成员的梯度方向相同，区别仅在于一个标量放大因子 $P_\theta^{-q}$。$q$ 越大，对低概率（不熟悉）样本的梯度放大越强。
    *   **GARL（梯度放大强化学习）**：从先验分布采样并放大 RL 梯度。它在冷启动阶段表现优异，能迅速将模型拉出零奖励区。
    *   **PAFT（后验衰减微调）**：通过重要性重采样从后验分布获取样本并进行微调。它在“热启动”或训练后期更稳定，能产生语义更连贯的梯度。

### 3. 实验设计
*   **数据集**：使用了三个具有挑战性的推理基准测试：
    1.  **FinQA**：财务数值推理。
    2.  **HotPotQA**：多跳问答。
    3.  **MuSiQue**：更复杂的多跳问答。
*   **对比方法（Baselines）**：
    *   **GRPO**：DeepSeek-R1 使用的主流强化学习算法。
    *   **RB-RLOO**：Rao-Blackwellized 版本的留一法强化学习。
*   **场景设置**：分为**冷启动**（无任务描述、无格式指令，仅输入输出对）和**热启动**（提供标准提示词指令）。

### 4. 资源与算力
*   **模型规模**：实验主要基于 **Qwen 3 0.6B** 参数模型。
*   **训练细节**：训练时每个提示词采样 $M=32$ 条轨迹。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从模型规模（0.6B）和采样预算来看，该实验旨在验证算法原理而非追求超大规模参数的极限性能。

### 5. 实验数量与充分性
*   **实验规模**：针对三个数据集分别进行了冷启动和热启动实验，并对参数 $q$ 进行了消融研究（$q \in \{0, 0.25, 0.5, 0.75, 1.0\}$）。
*   **充分性**：实验设计较为客观，通过对比不同 $q$ 值下的训练曲线，清晰地展示了理论预言的“逃逸速度”差异。
*   **公平性**：所有对比方法（GRPO, GARL, PAFT）在训练时的采样预算（$M=32$）完全一致，确保了计算成本的对等。

### 6. 主要结论与发现
*   **冷启动逃逸速度**：理论和实验均证明，RL（$q=0$）逃逸冷启动的时间复杂度为 $\Omega(1/p_0)$，而 MLE（$q=1$）仅为 $\Theta(\log(1/p_0))$。这意味着在极低成功率下，RL 几乎不可能成功，而高 $q$ 方法可以瞬间激活。
*   **GARL 的优越性**：在冷启动场景下，GARL ($q=0.75$) 在 GRPO 完全失败的情况下成功完成了任务，且性能优于纯 MLE。
*   **PAFT 的稳定性**：在复杂任务（如 HotPotQA）的热启动中，GARL 容易发生梯度崩溃（准确率掉零），而 PAFT 表现出极强的稳定性，最终效果比 GRPO 高出 14.4 个百分点。
*   **指令依赖降低**：高 $q$ 训练的模型即使不使用复杂的提示词工程，也能达到甚至超过经过精心提示词引导的 GRPO 模型。

### 7. 优点
*   **理论深度**：将强化学习和极大似然估计统一在 Tsallis 连续体下，为理解两种训练范式的差异提供了严谨的数学工具。
*   **实用性强**：提出的 GARL 和 PAFT 是现有 RL 框架（如 RLOO）的“掉入式”替代方案，计算开销几乎没有增加。
*   **解决冷启动**：为那些无法编写复杂奖励函数或缺乏高质量初始模型的任务提供了可行的训练路径。

### 8. 不足与局限
*   **模型规模局限**：实验仅在 0.6B 小模型上完成，虽然理论具有通用性，但在超大规模模型（如 70B+）上的表现和稳定性仍需验证。
*   **估计偏差**：高 $q$ 值虽然加速了学习，但也引入了更大的估计偏差（$O(1/M)$），在某些任务中可能导致模型过度拟合训练集中的噪声。
*   **崩溃机制不明**：论文观察到 GARL 在某些热启动任务中会突然崩溃，虽然 PAFT 解决了这一问题，但 GARL 崩溃的深层动力学原因尚待进一步探究。

（完）
