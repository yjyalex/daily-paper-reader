---
title: "CODA: Coordination via On-Policy Diffusion for Multi-Agent Offline Reinforcement Learning"
authors: "Marcel Hedman, Kale-ab Abebe Tessera, Juan Claude Formanek, Anya Sims, Riccardo Zamboni, Trevor McInroe, John Torr, Elliot Fosong"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23308v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 离线强化学习中的多智能体协同与轨迹生成
tldr: 针对离线多智能体强化学习中因静态数据导致的协调失败问题，本文提出了CODA。该方法利用基于扩散模型的轨迹生成器进行数据增强，通过根据当前联合策略进行条件采样，生成反映智能体演化行为的合成经验。CODA模拟了在线学习中的协同演化机制，有效解决了离线环境下的协调难题，并在复杂基准测试中表现优异。
selection_source: fresh_fetch
motivation: 离线多智能体学习在静态数据上训练时，智能体无法随策略变化而相互适应，容易陷入次优的协调失败状态。
method: 引入基于扩散模型的轨迹生成器，通过当前联合策略引导采样，生成动态演化的合成经验以支持智能体间的协同进化。
result: CODA不仅解决了连续多项式博弈中的协调病态问题，还在复杂的MaMuJoCo连续控制任务中取得了显著的性能提升。
conclusion: CODA通过模拟在线学习的协同过程，为提升离线多智能体系统的协调能力提供了一个算法无关的高效增强模块。
---

## Abstract
Offline multi-agent reinforcement learning (MARL) enables policy learning from fixed datasets, but is prone to coordination failure: agents trained on static, off-policy data converge to suboptimal joint behaviours because they cannot co-adapt as their policies change. We introduce CODA (Coordination via On-Policy Diffusion for Multi-Agent Reinforcement Learning), a diffusion-based multi-agent trajectory generator for data augmentation that samples conditioned on the current joint policy, producing synthetic experience which reflects the evolving behaviours of the agents, thereby providing a mechanism for co-adaptation. We find that previous diffusion-based augmentation approaches are insufficient for fostering multi-agent coordination because they produce static augmented datasets that do not evolve as the current joint policy changes during training; CODA resolves this by more closely simulating on-policy learning and is a meaningful step toward coordinated behaviours in the offline setting. CODA is algorithm-agnostic and can be layered onto both model-free and model-based offline reinforcement learning pipelines as an augmentation module. Empirically, CODA not only resolves canonical coordination pathologies in continuous polynomial games but also delivers strong results on the more complex MaMuJoCo continuous-control benchmarks.

---

## 论文详细总结（自动生成）

这篇论文提出了 **CODA**（Coordination via On-Policy Diffusion），旨在解决离线多智能体强化学习（Offline MARL）中的核心难题——协调失败。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在在线 MARL 中，智能体通过互动不断调整策略以相互适应（协同演化）。但在离线设置下，数据集是静态的。
*   **核心问题**：现有的离线 MARL 算法（如 MADDPG、MASAC）通常遵循“数据下最佳响应”（BRUD）范式，即每个智能体针对数据集中记录的队友历史行为进行优化。由于数据集不会随训练进化，智能体无法实现协同自适应（Co-adaptation），导致即使数据集覆盖范围足够，智能体仍会收敛到互不兼容的次优策略，即**协调失败**。

### 2. 论文提出的方法论：CODA
*   **核心思想**：利用生成式扩散模型（Diffusion Models）作为数据增强模块，生成与**当前联合策略**（Current Joint Policy）对齐的合成轨迹，从而在离线环境下模拟在线学习中的协同演化反馈环。
*   **关键技术细节**：
    *   **联合轨迹扩散**：训练一个扩散模型来建模离线数据集中的联合轨迹分布 $p_{\pi_{off}}(\tau)$，捕捉智能体间的时空相关性。
    *   **策略引导采样（On-Policy Guidance）**：在采样阶段，通过两种方式引导生成过程：
        1.  **分类器引导（Classifier Guidance）**：利用当前策略的对数似然梯度 $\nabla \log \pi_{curr}$ 修正扩散分值，使生成的轨迹向当前策略高概率区域偏移。
        2.  **无分类器引导（CFG）**：若策略可用紧凑向量表示，则直接作为条件输入。
    *   **算法流程**：
        1.  在离线数据上预训练扩散模型。
        2.  在强化学习训练循环中，定期根据当前联合策略采样合成轨迹。
        3.  将合成数据与原始离线数据混合，用于更新智能体的 Critic 和 Actor。
*   **公式含义**：通过重要性采样权重将目标分布定义为“策略倾斜分布”，使扩散模型在保持数据流形约束的同时，向当前策略靠拢。

### 3. 实验设计
*   **实验场景**：
    1.  **连续多项式博弈（Polynomial Games）**：包括乘法博弈（Multiplication Game）和双峰博弈（Twin Peaks），专门用于测试算法是否能避开静态数据导致的确定性次优陷阱。
    2.  **MaMuJoCo**：复杂的连续控制基准（如 2HalfCheetah, 4Ant），包含 Replay、Good、Medium、Poor 四种质量的数据集。
*   **对比方法（Benchmarks）**：
    *   **MADDPG+BC**：标准的离线 MARL 基线。
    *   **无条件扩散增强**：仅增加数据多样性，不考虑当前策略。
    *   **Q 值引导扩散（类似 EAQ）**：偏向高回报轨迹，但仍是静态的。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率优化**：作者提到为了降低计算成本，采用了“周期性生成”策略（每 4 个 epoch 生成一次），而非每个训练步都生成。

### 5. 实验数量与充分性
*   **实验规模**：在 MaMuJoCo 任务中使用了 **16 个随机种子**进行聚合，在转向实验（Steering experiments）中使用了 8 个种子，这在强化学习论文中属于较高标准，结果具有统计显著性。
*   **覆盖范围**：涵盖了从简单的协调病态分析到高维复杂控制任务，并对引导缩放因子 $\lambda$ 进行了详细的消融实验。
*   **客观性**：实验对比了多种数据增强策略，并分析了在不同数据质量（如 Poor 数据集）下的表现差异，评价较为客观。

### 6. 论文的主要结论与发现
*   **解决协调失败**：在多项式博弈中，只有 CODA 能够突破静态数据的限制，引导智能体收敛到真正的全局最优解。
*   **性能提升**：在 MaMuJoCo 的 Replay 和 Good 数据集上，CODA 显著优于无增强基线和无条件扩散方法。
*   **引导效应**：实验证实了引导缩放因子 $\lambda$ 能够有效将合成轨迹推向当前策略的分布，但也存在“峰值与崩溃”效应（$\lambda$ 过大会导致脱离数据流形）。

### 7. 优点（亮点）
*   **算法无关性**：CODA 作为一个插件式的数据增强模块，可以叠加在任何模型无关（Model-free）或基于模型（Model-based）的离线 MARL 算法上。
*   **理论深刻**：精准捕捉到了离线 MARL 中“协同演化缺失”这一本质痛点，并给出了优雅的生成式解决方案。
*   **轨迹级建模**：相比于单步动力学模型，轨迹级扩散能更好地捕捉长程协调关系。

### 8. 不足与局限
*   **对数据集支撑的依赖**：在数据质量极差（Poor）的情况下，CODA 的优势不明显。这是因为如果当前策略要求的行为完全不在离线数据的支撑集内，扩散模型难以生成高质量的样本。
*   **计算开销**：扩散模型的采样过程（即使经过优化）仍比直接从 Buffer 读取数据要慢得多，增加了训练的时间成本。
*   **超参数敏感性**：引导缩放因子 $\lambda$ 的选择对结果影响较大，且在不同任务间可能需要精细调优。

（完）
