---
title: Gated Coordination for Efficient Multi-Agent Collaboration in Minecraft Game
title_zh: Minecraft 游戏中的高效多智能体协作门控协调机制
authors: "HuaDong Jian, Chenghao Li, Haoyu Wang, Jiajia Shuai, Jinyu Guo, Yang Yang, Chaoning Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.18975v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 生成式任务中高效多智能体协作的门控协调
tldr: 针对开放世界多智能体协作中频繁通信导致的噪声和效率低下问题，本文提出一种分区信息架构，将私有执行状态与公共协调状态分离。通过引入事件触发的工作记忆和成本敏感的门控升级机制，智能体能够自主判断是否需要跨区域通信。在Minecraft长程建造任务中的实验表明，该方法显著提升了任务完成质量和执行效率，增强了局部自修复能力并减少了无效通信。
selection_source: fresh_fetch
motivation: 现有方法通常将局部异常作为通信的自动触发条件，导致协调噪声、中断局部执行并过度消耗公共交互资源。
method: 提出分区信息架构以分离私有与公共状态，并结合事件触发的工作记忆及成本敏感的门控升级机制来优化通信决策。
result: 在Minecraft长程建造任务中，该方法在蓝图完成质量、执行链长度和局部自修复能力上均优于基于强通信的基线模型。
conclusion: 通过将通信从默认反应转变为基于成本和影响的选择性决策，可以显著提升多智能体系统在复杂开放环境下的协作效率。
---

## 摘要
在长时程开放世界多智能体系统中，现有方法通常将局部异常视为通信的自动触发器。这种默认设计引入了协调噪声，中断了局部执行，并在本可以局部解决的情况下过度使用了公共交互。为解决这一问题，我们为 MLLM 智能体提出了一种分区信息架构，明确地将私有执行状态与公共协调状态分离。基于此设计，我们引入了两个关键机制：首先，我们开发了一种基于系统验证结果的事件触发工作记忆，以维持紧凑且低噪声的局部状态表示；其次，我们提出了一种成本敏感的门控升级机制，通过综合考虑节点关键性、局部恢复成本和下游任务影响，来决定是否应发起跨区域通信。通过这种方式，通信从一种默认反应转变为一种选择性决策。在开放环境下的长期建造任务中进行的实验表明，与基于强通信和规划结构的基准模型相比，引入门控通信和分区信息架构在蓝图完成质量和执行链长度方面表现出更优越的性能。此外，该方法还改善了局部自我恢复，减少了无效升级，并提高了公共通信的效用。

## Abstract
In long-horizon open-world multi-agent systems, existing methods often treat local anomalies as automatic triggers for communication. This default design introduces coordination noise, interrupts local execution, and overuses public interaction in cases that could be resolved locally. To address this issue, we propose a partitioned information architecture for MLLM agents that explicitly separates private execution states from public coordination states. Building on this design, we introduce two key mechanisms. First, we develop an event-triggered working memory based on system-verified outcomes to maintain compact and low-noise local state representations. Second, we propose a cost-sensitive gated escalation mechanism that determines whether cross-region communication should be initiated by jointly considering node criticality, local recovery cost, and downstream task impact. In this way, communication is transformed from a default reaction into a selective decision. Experiments conducted on long-term construction tasks in open environments demonstrate that, compared to baseline models based on strong communication and planned structures, the introduction of gated communication and a partitioned information architecture results in superior performance in terms of blueprint completion quality and execution chain length. It also improves local self-recovery, reduces ineffective escalations, and increases the utility of public communication.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gated Coordination for Efficient Multi-Agent Collaboration in Minecraft Game》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 Minecraft 等长程（Long-horizon）开放世界环境中，现有的多智能体大语言模型（MLLM）系统通常遵循“通信优先”范式。这种设计存在三个主要缺陷：
*   **执行中断**：无节制的通信会消耗智能体的认知预算，迫使它们频繁挂起正在进行的局部计划。
*   **全局噪声放大**：微小的局部偏差被过早升级为系统级协调事件，导致智能体间的连锁干扰。
*   **虚假协调触发**：现有机制缺乏成本效益分析，往往忽略了智能体的局部自修复能力（如自行收集替代资源），将“能够通信”误等同于“必须通信”。
**研究动机**：将通信从一种“默认反应”转变为一种“经过计算的选择”，在自主局部处理与协作升级之间取得平衡。

### 2. 方法论
论文提出了一种**分区信息架构（Partitioned Information Architecture）**和**门控升级机制（Gated Escalation Mechanism）**：

*   **分区信息架构**：
    *   **私有执行状态**：仅存储执行关键信息（库存、当前任务、坐标、障碍状态、已验证历史），由系统验证的结果更新，避免 LLM 幻觉污染。
    *   **公共协调状态**：仅包含改变状态的协调信号（如物料请求、转移指令），默认通道关闭，仅在必要时开启。
*   **三层门控升级策略**：
    1.  **启发式规则**：处理明显的简单情况。
    2.  **成本敏感评分 ($S$)**：通过公式 $S = w_C C + w_R R + w_I I - w_L L - w_H H$ 计算。其中 $C$ 为关键性，$R$ 为协调优势，$I$ 为下游影响，$L$ 为局部可恢复性，$H$ 为协调历史惩罚。
    3.  **灰色地带 LLM 裁决**：当分数处于阈值中间时，调用轻量级 LLM 进行二分类决策。
*   **算法流程**：环境观察 $\rightarrow$ 更新私有状态 $\rightarrow$ 检测结构化问题 $\rightarrow$ 门控评估 $\rightarrow$ 决定是“留在局部”（执行修复或跳过）还是“升级到公共”（发送协议化指令）。

### 3. 实验设计
*   **数据集/场景**：
    *   **标准基准**：MindCraft（7 个原生任务）和 VillagerBench（动态建筑蓝图）。
    *   **自定义高协作场景**：人为注入物料短缺和依赖瓶颈，分为四类压力测试：(A) 局部可恢复、(B) 必须协作、(C) 灰色地带决策、(D) 协作失败鲁棒性。
*   **对比方法（Benchmark）**：
    *   **MindCraft (FlatComm)**：基于自由文本的即时广播通信模型。
    *   **VillagerAgent (DAG)**：基于刚性、自上而下任务分配的规划模型。

### 4. 资源与算力
*   **模型使用**：使用了 **GPT-4o** 和 **GPT-4-1106-preview** 作为核心推理引擎。
*   **底层环境**：基于 Mineflayer 和 Prismarine 生态系统进行底层动作控制。
*   **算力说明**：论文**未明确说明**具体的 GPU 训练算力（如型号、数量），因为该研究主要基于现有的 MLLM 进行推理和 Agent 架构设计，而非从头训练模型。实验主要消耗的是 LLM 的 API Token。

### 5. 实验数量与充分性
*   **实验规模**：在 MindCraft 自定义数据集中进行了 200 个回合（Episodes）的测试，涵盖了 2 智能体和 3 智能体配置。
*   **消融实验**：非常充分。分别验证了“无分区架构”、“无门控”、“仅规则门控”、“规则+评分门控”等变体对性能的影响。
*   **客观性与公平性**：实验在相同的动作接口、提示词预算和环境设置下进行。通过引入“有效通信率（ECR）”和“不必要升级率（UER）”等机械性指标，深入分析了决策逻辑，而非仅看最终成功率，体现了较高的客观性。

### 6. 主要结论与发现
*   **性能提升**：在资源受限的自定义场景中，该方法显著优于基线。例如，将 VillagerAgent 的任务成功率（TSR）从 22.04% 提升至 34.56%。
*   **效率优化**：显著减少了完成任务所需的步数（CS）。在自定义场景中，VillagerAgent 的步数从 145 降至 92。
*   **通信质量**：局部解决率（LRR）大幅提升，不必要升级率（UER）显著下降，证明了门控机制能有效过滤噪声，使通信更具目的性。
*   **鲁棒性**：当协作请求失败（如队友无法提供资源）时，系统能通过历史惩罚项自动转入局部回退逻辑，避免了死锁。

### 7. 优点
*   **架构创新**：明确区分私有和公共信息空间，从根本上解决了长上下文导致的“幻觉雪球”和信息污染问题。
*   **决策精细化**：引入成本敏感评分函数，使智能体具备了类似人类的“权衡”意识（即：自己去拿更划算，还是问队友要更划算）。
*   **协议化通信**：通过严格的协议指令代替自由聊天，极大地节省了 Token 消耗并提高了指令解析的准确性。

### 8. 不足与局限
*   **环境局限性**：实验主要集中在 Minecraft 建造任务中，虽然具有代表性，但在其他具身智能场景（如现实机器人协作）中的泛化能力尚待验证。
*   **问题定义依赖**：门控机制依赖于预定义的“结构化问题类型”（如物料缺失、依赖阻塞），对于未定义的异常可能缺乏处理能力。
*   **参数敏感性**：评分函数中的权重（$w$）和阈值（$T$）需要针对特定环境进行离线校准，这可能限制了系统在动态变化环境中的即时迁移能力。

（完）
