---
title: "From Natural Language to Executable Narsese: A Neuro-Symbolic Benchmark and Pipeline for Reasoning with NARS"
title_zh: 从自然语言到可执行 Narsese：一个用于 NARS 推理的神经符号基准与流水线
authors: "Mina Gabriel, Pei Wang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18873v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 将自然语言推理问题转化为可执行的一阶逻辑表示
tldr: 针对大语言模型在符号推理和不确定性处理上的不足，本文提出了一个神经符号框架，将自然语言推理问题转化为可执行的Narsese代码。研究发布了NARS-Reasoning-v0.1基准测试集，包含一阶逻辑和Narsese表示及三分类标签。通过确定性编译流水线和OpenNARS执行验证，确保了符号目标的准确性。此外，提出了语言结构化感知（LSP）方法，并训练了Phi-2适配器，为构建更可靠的神经符号推理系统提供了实践路径。
selection_source: fresh_fetch
motivation: 解决大语言模型在处理需要显式符号结构、多步推理和可解释不确定性的任务时表现不可靠的问题。
method: 开发了一套从一阶逻辑到可执行Narsese的确定性编译流水线，并提出让LLM生成推理相关符号结构的语言结构化感知（LSP）方案。
result: 构建了包含自然语言、逻辑形式及执行标签的NARS-Reasoning-v0.1基准，并通过Phi-2 LoRA适配器验证了其在三分类推理任务中的有效性。
conclusion: 将可执行符号生成与基于执行的验证相结合，是实现更可靠、可解释的神经符号推理系统的有效途径。
---

## 摘要
大语言模型 (LLM) 在语言生成方面能力极强，但在推理需要显式符号结构、多步推理和可解释的不确定性时，它们仍然不够可靠。本文提出了一个神经符号框架，用于将自然语言推理问题转换为可执行的形式化表示，使用的是一阶逻辑 (FOL) 和 Narsese（非公理化推理系统 NARS 的语言）。为了支持这一方向，我们推出了 NARS-Reasoning-v0.1，这是一个自然语言推理问题的基准测试集，配有一阶逻辑形式、可执行的 Narsese 程序以及三个金标准标签：真、假和不确定。我们开发了一个从一阶逻辑到可执行 Narsese 的确定性编译流水线，并通过在 OpenNARS for Applications (ONA) 中的运行时执行来验证保留的示例，确保符号目标不仅在语法上格式正确，而且在行为上与预期答案一致。我们进一步提出了语言结构化感知 (LSP)，这是一种让 LLM 训练生成与推理相关的符号结构，而不仅仅是最终口头回答的表述方式。作为初步的概念验证，我们还在 NARS-Reasoning-v0.1 上训练并发布了一个用于三标签推理分类的 Phi-2 LoRA 适配器，表明该基准除了可执行评估外，还能支持监督式适配。总的来说，本文将可执行符号生成和基于执行的验证定位为通往更可靠神经符号推理系统的实用路径。

## Abstract
Large language models (LLMs) are highly capable at language generation, but they remain unreliable when reasoning requires explicit symbolic structure, multi-step inference, and interpretable uncertainty. This paper presents a neuro-symbolic framework for translating natural-language reasoning problems into executable formal representations using first-order logic (FOL) and Narsese, the language of the Non-Axiomatic Reasoning System (NARS). To support this direction, we introduce NARS-Reasoning-v0.1, a benchmark of natural-language reasoning problems paired with FOL forms, executable Narsese programs, and three gold labels: True, False, and Uncertain. We develop a deterministic compilation pipeline from FOL to executable Narsese and validate retained examples through runtime execution in OpenNARS for Applications (ONA), ensuring that the symbolic targets are not only syntactically well formed but also behaviorally aligned with the intended answer. We further present Language-Structured Perception (LSP), a formulation in which an LLM is trained to produce reasoning-relevant symbolic structure rather than only a final verbal response. As an initial proof of concept, we also train and release a Phi-2 LoRA adapter on NARS-Reasoning-v0.1 for three-label reasoning classification, showing that the benchmark can support supervised adaptation in addition to executable evaluation. Overall, the paper positions executable symbolic generation and execution-based validation as a practical path toward more reliable neuro-symbolic reasoning systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Natural Language to Executable Narsese: A Neuro-Symbolic Benchmark and Pipeline for Reasoning with NARS》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决大语言模型（LLM）在逻辑推理中的**不可靠性**。尽管 LLM 擅长语言生成，但在处理需要显式符号结构、多步推理以及可解释的不确定性（Uncertainty）的任务时经常出错。
*   **动机**：文本形式的回答难以被直接审计或验证。
*   **背景**：作者提出将自然语言（NL）转化为**Narsese**（非公理化推理系统 NARS 的形式化语言）。NARS 的特点是能在知识不足和资源有限的情况下进行推理，并显式处理不确定性。

### 2. 论文提出的方法论
作者提出了一套神经符号（Neuro-Symbolic）推理流水线，核心思想是将推理任务从“文本生成”转变为“可执行代码生成”。
*   **核心流程**：`自然语言 (NL) -> 一阶逻辑 (FOL) -> Narsese -> OpenNARS (ONA) 执行 -> 最终标签`。
*   **关键技术细节**：
    *   **确定性编译器**：开发了一个将 FOL 表达式映射为 Narsese 的编译器，支持原子谓词、否定、合取、析取、蕴含、异或（XOR）及量化规则。
    *   **语言结构化感知 (LSP)**：一种新的任务表述，训练模型生成推理相关的符号结构（事实、规则、查询），而非直接输出答案。
    *   **执行验证**：在 OpenNARS for Applications (ONA) 运行时环境中执行生成的 Narsese，通过推理引擎的反馈（频率和置信度）来确定 True/False/Uncertain 标签。

### 3. 实验设计
*   **数据集/基准**：发布了 **NARS-Reasoning-v0.1**，包含 1,000 个推理实例。
    *   **来源**：基于 ProverQA 家族的一阶逻辑问题。
    *   **难度分级**：分为 Easy（1-2步推理）、Medium（3-5步）、Hard（6-9步）。
    *   **标签空间**：三分类（True, False, Uncertain），这比传统的二分类更符合现实推理逻辑。
*   **对比方法**：
    *   作为初步验证，作者训练了一个 **Phi-2 LoRA 适配器**（基于微软 Phi-2 2.7B 模型）。
    *   该模型被训练为直接从自然语言预测三分类标签，作为后续“生成+执行”完整流水线的基线（Baseline）。

### 4. 资源与算力
*   **模型规模**：使用了 **Phi-2 (2.7B参数)** 模型。
*   **训练技术**：采用了 **PEFT-LoRA** 微调技术，并使用了 **4-bit NF4 量化**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练时长。但作者在 Hugging Face 上发布了模型权重（`MinaGabriel/phi2-2.7b-lora-nars-adapter`），暗示其训练成本相对较低，可在消费级硬件上完成。

### 5. 实验数量与充分性
*   **实验规模**：数据集包含 1,000 条样本（800 训练 / 200 测试），覆盖了三种难度。
*   **充分性评价**：
    *   作为**初步概念验证（Proof of Concept）**，实验设计是合理的。
    *   **客观性**：通过 ONA 引擎进行运行时验证，确保了金标准标签的客观性。
    *   **局限性**：目前仅展示了 Phi-2 适配器的分类性能，尚未大规模对比不同规模 LLM 在“生成 Narsese 代码”这一任务上的准确率差异。

### 6. 主要结论与发现
*   **可执行性是关键**：符号推理不应只看表面文本的相似度，而应通过是否能被推理引擎正确执行来评估。
*   **基准有效性**：NARS-Reasoning-v0.1 能够支持监督学习，小型模型（如 2.7B）经过微调后可以掌握该数据集的推理模式。
*   **不确定性的必要性**：引入“Uncertain”标签能有效区分“证据不足”与“逻辑矛盾”，这是构建可靠 AI 系统的必要步骤。

### 7. 优点
*   **闭环验证**：通过 ONA 引擎执行来过滤和验证数据，确保了基准测试集的严谨性。
*   **神经符号结合**：将 LLM 的语言理解能力与 NARS 的严谨推理能力结合，提供了一条通往可解释 AI 的路径。
*   **实用性**：提供了从 FOL 到 Narsese 的编译器，降低了使用 NARS 系统的门槛。

### 8. 不足与局限
*   **数据合成性**：数据集源自合成逻辑问题，可能无法完全代表复杂、模糊的现实世界自然语言。
*   **编译器限制**：目前的编译器仅支持 FOL 的一个子集，对于更复杂的 Narsese 特性（如时间推理、心理算子）尚未覆盖。
*   **评估深度**：目前的基线模型仅做了分类预测，尚未全面评估 LLM 生成复杂、长篇可执行 Narsese 程序的端到端能力。

（完）
