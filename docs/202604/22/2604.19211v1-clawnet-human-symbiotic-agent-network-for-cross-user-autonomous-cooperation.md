---
title: "ClawNet: Human-Symbiotic Agent Network for Cross-User Autonomous Cooperation"
title_zh: ClawNet：面向跨用户自主协作的人机共生智能体网络
authors: "Zhiqin Yang, Zhenyuan Zhang, Xianzhang Jia, Jun Song, Wei Xue, Yonggang Zhang, Yike Guo"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19211v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 跨用户自主智能体协作
tldr: 针对当前AI智能体局限于单用户任务且缺乏跨用户协作机制的问题，本文提出了一种人机共生智能体范式并实现为ClawNet框架。该框架将智能体视为用户的数字化延伸，通过分层身份架构（隔离全局知识与外部通信）、范围授权（精细化访问控制）和动作级问责（确保可审计性）三大治理原语，构建了一个以人为节点的协作网络，有效解决了跨用户代理协作中的安全与信任难题。
selection_source: fresh_fetch
motivation: 现有的AI智能体系统大多服务于单一用户，缺乏支持复杂人类社交与组织协作关系的跨用户协作基础设施及治理机制。
method: 提出ClawNet框架，采用分层身份架构隔离管理与执行，并结合范围授权和动作级问责来确保智能体协作的安全性。
result: 成功构建了一个身份治理的智能体协作网络，实现了多用户之间通过各自绑定的智能体进行安全、自动化的授权协作。
conclusion: AI智能体的发展重心应从提升个体能力转向数字化人类协作关系，而ClawNet为这种跨用户协作提供了关键的治理范式。
---

## 摘要
当前的 AI 智能体框架在自动化单项任务方面取得了显著进展，但现有系统均仅服务于单一用户。人类的生产力依赖于人们进行协调、谈判和委托的社会与组织关系。当智能体不再仅仅为个人执行任务，而是代表该用户与其他用户协作时，跨用户智能体协作的基础设施完全缺失，更不用说保障其安全所需的治理机制。我们认为，AI 智能体的下一个前沿不在于更强的个体能力，而在于人类协作关系的数字化。为此，我们提出了一种人机共生智能体范式。每个用户都拥有一个永久绑定的智能体系统，该系统代表所有者进行协作，从而形成一个以人而非智能体为节点的网络。该范式基于三个治理原语：分层身份架构将管理智能体（Manager Agent）与多个特定上下文的身份智能体（Identity Agent）分离，管理智能体掌握全局知识，但在架构上与外部通信隔离；作用域授权强制执行针对每个身份的访问控制，并将越界行为上报给所有者；操作级问责制根据所有者的身份和授权记录每一项操作，确保完全的可审计性。我们在 ClawNet 中实例化了这一范式。ClawNet 是一个由身份治理的智能体协作框架，通过中央编排器强制执行身份绑定和授权验证，使多个用户能够通过各自的智能体进行安全协作。

## Abstract
Current AI agent frameworks have made remarkable progress in automating individual tasks, yet all existing systems serve a single user. Human productivity rests on the social and organizational relationships through which people coordinate, negotiate, and delegate. When agents move beyond performing tasks for one person to representing that person in collaboration with others, the infrastructure for cross-user agent collaboration is entirely absent, let alone the governance mechanisms needed to secure it. We argue that the next frontier for AI agents lies not in stronger individual capability, but in the digitization of human collaborative relationships. To this end, we propose a human-symbiotic agent paradigm. Each user owns a permanently bound agent system that collaborates on the owner's behalf, forming a network whose nodes are humans rather than agents. This paradigm rests on three governance primitives. A layered identity architecture separates a Manager Agent from multiple context-specific Identity Agents; the Manager Agent holds global knowledge but is architecturally isolated from external communication. Scoped authorization enforces per-identity access control and escalates boundary violations to the owner. Action-level accountability logs every operation against its owner's identity and authorization, ensuring full auditability. We instantiate this paradigm in ClawNet, an identity-governed agent collaboration framework that enforces identity binding and authorization verification through a central orchestrator, enabling multiple users to collaborate securely through their respective agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawNet: Human-Symbiotic Agent Network for Cross-User Autonomous Cooperation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的 AI 智能体（Agent）框架（如 AutoGen, MetaGPT）主要集中在提升单用户任务的自动化水平。即使是多智能体系统，也通常服务于单一主体，共享统一目标。
*   **核心问题**：在现实世界中，生产力源于人与人之间的社会和组织协作（协调、谈判、委托）。然而，目前缺乏支持**跨用户智能体协作**的基础设施，尤其在身份绑定（Identity Binding）、范围授权（Scoped Authorization）和可审计问责（Accountability）这三大治理机制上存在空白。
*   **整体含义**：论文主张 AI 智能体的下一个前沿是“人类协作关系的数字化”，提出了“人机共生”范式，让智能体作为用户的数字化延伸，代表用户在受控、安全的网络中进行跨主体协作。

### 2. 方法论
ClawNet 建立在三个治理原语之上，并采用云边协同架构：
*   **分层身份架构**：
    *   **管理智能体 (Manager Agent)**：拥有用户的全局知识和长期记忆，作为内部顾问，但在架构上与外部通信完全隔离（“不能”而非“不应”泄密）。
    *   **身份智能体 (Identity Agent)**：针对特定场景（如“采购经理”、“研究员”）的投影，仅携带该上下文相关的知识，负责对外协作。
*   **身份绑定与认知耦合**：通过事实记忆、模式记忆和价值记忆三个层次，使智能体从工具演变为真正了解所有者偏好和底线的“代理人”。
*   **双层范围授权**：
    *   **L1（云端）**：基于身份域的访问控制列表（ACL）。
    *   **L2（边缘端）**：用户设备上的文件夹白名单强制校验。
*   **动作级问责**：所有操作均记录在不可篡改的审计日志中，且在执行修改类操作前自动备份，支持一键撤销。
*   **协作协议**：跨用户协作需经过双向显式授权（Bilateral Approval），智能体之间通过多轮对话协商，但最终决策权保留在人类手中。

### 3. 实验设计
*   **实验场景**：论文通过一个**跨国企业协作场景**进行验证。
    *   **参与方**：买方（CN Tech Co.）和卖方（US Nova-Semi）。
    *   **流程**：买方用户下达采购意图 -> 买方智能体读取本地需求并联系卖方 CEO 智能体 -> 卖方智能体分解任务给技术和商务子智能体 -> 双方智能体在各自授权范围内谈判 -> 生成提案并提交给各自人类所有者审批。
*   **对比基准 (Benchmark)**：论文在功能维度上对比了 ChatGPT（对话 AI）、OpenClaw（OS 级单智能体）、MetaGPT/AutoGen（单主体多智能体）以及 Google A2A（跨智能体协议）。对比指标包括执行级别、身份绑定、范围授权、动作问责和跨所有者协作能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **技术栈**：提到使用了大语言模型（LLM）作为推理引擎，采用云端容器化部署（Gateway Containers）和 WebSocket 进行云边通信。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了一个复杂的跨组织协作案例研究（Case Study），而非大规模的统计实验。
*   **充分性评价**：
    *   **定性分析充分**：成功演示了治理原语如何在复杂工作流中拦截越权行为、保护隐私数据（如底价信息）。
    *   **定量分析不足**：缺乏针对不同 LLM 底座的性能对比、协作成功率统计或系统延迟分析。实验更偏向于系统架构的“可行性验证”。

### 6. 主要结论与发现
*   **治理优先**：证明了通过架构隔离（Manager/Identity 分离）比单纯依靠 Prompt 约束更能有效防止隐私泄露。
*   **人机共生**：智能体应自动化“过程”而非“决策”，人类在协作网络中仍是核心节点。
*   **基础设施必要性**：跨用户协作需要一套完整的身份和授权基础设施，ClawNet 证明了这种基础设施可以有效支持复杂的商业谈判。

### 7. 优点
*   **范式创新**：从“单人工具”转向“社交网络”，填补了跨主体 Agent 协作的理论空白。
*   **安全设计严密**：双层授权和管理智能体的物理隔离设计具有很强的工程参考价值。
*   **主权保护**：强调了用户对数据的绝对控制权和对 Agent 行为的可纠正性（撤销机制）。

### 8. 不足与局限
*   **实验覆盖面**：仅展示了采购场景，未在更多样化（如医疗、法律、政务）的场景下测试。
*   **模型依赖风险**：系统安全性仍部分依赖于 LLM 理解指令的准确性，尽管有架构保护，但极端 Prompt 攻击下的鲁棒性待验证。
*   **扩展性挑战**：随着协作链条增长（递归协作），多层授权可能导致协作效率下降或死锁。
*   **评估指标缺失**：缺乏与人类协作效率对比的量化数据。

（完）
