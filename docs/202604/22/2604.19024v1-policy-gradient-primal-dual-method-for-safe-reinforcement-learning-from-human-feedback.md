---
title: Policy Gradient Primal-Dual Method for Safe Reinforcement Learning from Human Feedback
title_zh: 基于人类反馈的安全强化学习的策略梯度原对偶方法
authors: "Qiang Liu, Adrienne Kline, Ermin Wei"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19024v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大语言模型的安全人类反馈强化学习
tldr: 本文针对安全强化学习（Safe RLHF）中现有方法依赖固定步长奖励模型且缺乏理论保证的问题，将其建模为无限时界折扣约束马尔可夫决策过程（CMDP）。作者提出了两种基于原对偶方法的算法，无需拟合奖励模型即可支持灵活的轨迹长度。该研究首次在人类反馈下实现了无限时界CMDP的全局非渐近收敛，并提供了多项式级的收敛速率保证。
selection_source: fresh_fetch
motivation: 现有的Safe RLHF方法通常依赖固定步长的奖励模型且缺乏理论收敛性证明，难以适应人类与模型持续交互的无限时界场景。
method: 将Safe RLHF建模为无限时界折扣CMDP，并提出两种无需奖励模型拟合、支持灵活轨迹长度的原对偶策略梯度算法。
result: 算法在策略梯度迭代、轨迹采样长度和人类偏好查询方面均实现了多项式级别的全局非渐近收敛速率。
conclusion: 本研究为无限时界下的安全强化学习提供了首个具有理论保障的框架，填补了该领域在全局收敛性分析上的空白。
---

## 摘要
基于人类反馈的安全强化学习（Safe RLHF）最近通过解耦人类关于有用性（helpfulness）和无害性（harmlessness）的偏好，在开发有用且无害的大语言模型方面取得了实证成功。现有方法通常依赖于从人类反馈中拟合固定时界的奖励模型，且仅经过了实证验证。在本文中，我们将 Safe RLHF 建模为无限时界折扣约束马尔可夫决策过程（CMDP），因为人类可能通过一系列持续的交互而非单个有限回合与模型进行交互。我们提出了两种无需奖励模型拟合的 Safe RLHF 算法，且与以往假设固定长度轨迹的工作不同，这些算法支持灵活的训练轨迹长度。两种算法均基于原对偶方法，并在策略梯度迭代次数、轨迹采样长度和人类偏好查询次数方面实现了具有多项式速率的全局收敛保证。据我们所知，这是首项研究人类反馈下的无限时界折扣 CMDP 并建立全局非渐近收敛性的工作。

## Abstract
Safe Reinforcement Learning from Human Feedback (Safe RLHF) has recently achieved empirical success in developing helpful and harmless large language models by decoupling human preferences regarding helpfulness and harmlessness. Existing approaches typically rely on fitting fixed horizon reward models from human feedback and have only been validated empirically. In this paper, we formulate safe RLHF as an infinite horizon discounted Con- strained Markov Decision Process (CMDP), since humans may interact with the model over a continuing sequence of interactions rather than within a single finite episode. We propose two Safe RLHF algorithms that do not require reward model fitting and, in contrast to prior work assuming fixed-length trajectories, support flexible trajectory lengths for training. Both algo- rithms are based on the primal-dual method and achieve global convergence guarantees with polynomial rates in terms of policy gradient iterations, trajectory sample lengths, and human preference queries. To the best of our knowledge, this is the first work to study infinite horizon discounted CMDP under human feedback and establish global, non-asymptotic convergence.

---

## 论文详细总结（自动生成）

这篇论文《Policy Gradient Primal-Dual Method for Safe Reinforcement Learning from Human Feedback》由西北大学的研究人员撰写，旨在为大语言模型（LLM）的“安全人类反馈强化学习”（Safe RLHF）提供理论基础和算法保障。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：为了使 LLM 既“有用”（Helpful）又“无害”（Harmless），Safe RLHF 成为主流技术。然而，现有的 Safe RLHF 方法（如基于奖励模型拟合的方法）存在双重误设定、分布偏移和过拟合等问题。
*   **核心痛点**：
    1.  **理论缺失**：现有方法大多是经验性的，缺乏全局收敛性的理论保证。
    2.  **时界限制**：现有研究多假设固定长度的片段（Episodic MDP），但人类与模型的交互往往是持续且长度可变的。
    3.  **模型依赖**：过度依赖于拟合奖励模型（Reward Modeling），而非直接从反馈中学习。
*   **研究目标**：将 Safe RLHF 建模为**无限时界折扣约束马尔可夫决策过程（CMDP）**，并提出具有全局收敛保证的算法。

### 2. 方法论
论文提出了两种基于**原对偶（Primal-Dual）框架**的算法，核心思想是直接利用人类偏好反馈进行策略更新，而无需显式拟合奖励模型：

*   **NPGPD-HF（基于人类反馈的自然策略梯度原对偶法）**：
    *   **核心思想**：利用人类对轨迹对的偏好反馈来直接估计**优势函数（Advantage Function）**。
    *   **技术细节**：采用 Softmax 参数化策略。在每轮迭代中，通过人类对采样轨迹的对比反馈，结合逆链接函数（Inverse Link Function）估计奖励和成本的优势值，随后进行原变量（策略）和对偶变量（拉格朗日乘子）的同步更新。
*   **ZPGPD-HF（基于人类反馈的零阶策略梯度原对偶法）**：
    *   **核心思想**：采用**零阶优化（Zeroth-Order）**技术，通过对策略参数进行随机扰动来近似梯度。
    *   **技术细节**：适用于直接参数化策略。它通过比较扰动前后的轨迹偏好来估算梯度方向，这种方法对每轮迭代的人类查询次数要求更灵活。
*   **关键公式/流程**：
    *   使用拉格朗日函数 $L(\theta, \lambda) = V_r^\theta(\rho) + \lambda(V_g^\theta(\rho) - b)$。
    *   通过人类反馈的概率 $p$ 和链接函数 $\sigma$ 的逆函数 $\sigma^{-1}(p)$ 来恢复潜在的奖励/价值差异。

### 3. 实验设计
*   **实验场景**：构建了一个合成的 CMDP 环境，包含 10 个状态和 4 个动作（$|S|=10, |A|=4$）。
*   **模拟人类反馈**：使用 Bradley-Terry 模型模拟人类对“有用性”的偏好，使用逻辑回归模型模拟对“无害性”的绝对评估。
*   **Benchmark 与对比**：
    *   对比了 NPGPD-HF 和 ZPGPD-HF 在不同人类评价者数量（$M \in \{16, 64, 256\}$）下的表现。
    *   评估指标为**最优性差距（Optimality Gap）**和**约束违反程度（Constraint Violation）**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **原因分析**：由于本文侧重于理论证明和算法收敛速率分析，实验部分主要是在小型合成环境上进行的数值验证，而非大规模 LLM 训练，因此对算力的需求相对较低，通常单机 CPU 或普通 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：主要展示了针对两种算法在不同反馈噪声水平（由评价者数量 $M$ 控制）下的收敛曲线。
*   **充分性评价**：
    *   对于一篇**理论型论文**，其实验足以验证其提出的“多项式级收敛速率”和“反馈偏置影响”等理论发现。
    *   **局限性**：实验仅限于小型合成环境，没有在大规模真实语言模型任务上进行测试，这在纯理论研究中是常见的，但削弱了其在复杂现实场景下的实证说服力。

### 6. 主要结论与发现
*   **全局收敛性**：证明了两种算法均能以多项式速率收敛至全局最优解。
*   **误差分解**：收敛误差可分解为三部分：随机优化误差（$1/\sqrt{T}$）、人类反馈偏置（受评价者数量 $M$ 影响）和有限时界近似偏置（受截断长度 $H$ 影响）。
*   **算法权衡**：
    *   **NPGPD-HF**：收敛速度快，且与状态空间大小无关，但每轮迭代需要遍历所有状态-动作对进行查询。
    *   **ZPGPD-HF**：每轮查询次数可控，但收敛速度较慢，且受维度影响较大。

### 7. 优点
*   **理论开创性**：首次在无限时界折扣 CMDP 框架下研究 RLHF，并建立了非渐近收敛性分析。
*   **无需奖励模型**：避开了奖励模型拟合带来的误设定风险，直接从偏好中学习。
*   **灵活性**：支持变长轨迹训练，更符合人类真实交互逻辑。

### 8. 不足与局限
*   **假设依赖**：算法依赖于已知的链接函数（如 Bradley-Terry），且假设链接函数是严格单调且 Lipschitz 连续的，这在现实中可能难以完全满足。
*   **扩展性挑战**：理论分析基于表格型（Tabular）或完全参数化设置，尚未扩展到深度神经网络（Deep RL）的函数近似场景。
*   **查询成本**：虽然理论上收敛，但在实际应用中，为了消除反馈偏置，可能需要大量的人类查询（$M$ 需随 $T$ 增长）。

（完）
