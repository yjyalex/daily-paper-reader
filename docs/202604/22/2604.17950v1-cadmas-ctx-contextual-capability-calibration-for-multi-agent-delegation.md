---
title: "CADMAS-CTX: Contextual Capability Calibration for Multi-Agent Delegation"
title_zh: CADMAS-CTX：多智能体委派中的上下文能力校准
authors: Chuhan Qiao
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17950v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 多智能体委派与协作中的上下文能力校准
tldr: 本研究针对多智能体委派中智能体能力随任务上下文变化的特性，提出了CADMAS-CTX框架。该框架摒弃了传统的静态技能评估，采用分层上下文能力画像，利用Beta后验分布捕捉智能体在特定上下文下的表现。通过结合后验均值与不确定性惩罚的风险感知评分进行委派决策。理论证明了该方法在上下文异质性下具有更低的累积遗憾，并在GAIA和SWE-bench实验中验证了其显著优于静态基准和AutoGen的性能。
selection_source: fresh_fetch
motivation: 传统的静态技能评估忽略了任务上下文对智能体能力的动态影响，容易导致系统性的委派错误。
method: 提出CADMAS-CTX框架，利用Beta后验分布建模智能体在不同上下文下的能力，并采用结合不确定性惩罚的风险感知评分进行委派。
result: "在GAIA基准上准确率从0.381提升至0.442，在SWE-bench Lite上解决率从22.3%提升至31.4%，显著优于现有静态委派方法。"
conclusion: 上下文校准和风险感知委派机制相比静态全局技能分配，能显著提升多智能体团队协作的效能和鲁棒性。
---

## 摘要
我们在一个更强且更现实的假设下重新审视多智能体委派：智能体的能力并非固定在技能层面，而是取决于任务上下文。例如，一个编程智能体可能擅长简短的独立编辑，但在长程调试中失败；一个规划器可能在浅层任务中表现良好，但在链式依赖任务中性能下降。因此，静态的技能级能力画像是对异构情况的平均，可能导致系统性的委派错误。我们提出了 CADMAS-CTX，一个用于上下文能力校准的框架。对于每个智能体、技能和粗粒度的上下文桶，CADMAS-CTX 维护一个 Beta 后验，以捕捉该任务空间部分的稳定经验。委派决策由一个风险感知评分做出，该评分结合了后验均值和不确定性惩罚，使得智能体仅在同行表现更好且该评估有充分证据支持时才进行委派。本文有三项贡献：首先，分层上下文能力画像用上下文条件后验取代了静态的技能级置信度；其次，基于上下文老虎机理论，我们正式证明了在足够的上下文异构性下，上下文感知路由比静态路由具有更低的累积遗憾，并形式化了偏差-方差权衡；第三，我们在 GAIA 和 SWE-bench 基准测试上验证了我们的方法。在使用 GPT-4o 智能体的 GAIA 上，CADMAS-CTX 达到了 0.442 的准确率，优于静态基准（0.381）和 AutoGen（0.354），且具有不重叠的 95% 置信区间。在 SWE-bench Lite 上，它将解决率从 22.3% 提高到 31.4%。消融实验表明，不确定性惩罚提高了对上下文标记噪声的鲁棒性。我们的结果表明，与静态全局技能分配相比，上下文校准和风险感知委派显著改善了多智能体团队协作。

## Abstract
We revisit multi-agent delegation under a stronger and more realistic assumption: an agent's capability is not fixed at the skill level, but depends on task context. A coding agent may excel at short standalone edits yet fail on long-horizon debugging; a planner may perform well on shallow tasks yet degrade on chained dependencies. Static skill-level capability profiles therefore average over heterogeneous situations and can induce systematic misdelegation. We propose CADMAS-CTX, a framework for contextual capability calibration. For each agent, skill, and coarse context bucket, CADMAS-CTX maintains a Beta posterior that captures stable experience in that part of the task space. Delegation is then made by a risk-aware score that combines the posterior mean with an uncertainty penalty, so that agents delegate only when a peer appears better and that assessment is sufficiently well supported by evidence. This paper makes three contributions. First, a hierarchical contextual capability profile replaces static skill-level confidence with context-conditioned posteriors. Second, based on contextual bandit theory, we formally prove context-aware routing achieves lower cumulative regret than static routing under sufficient context heterogeneity, formalizing the bias-variance tradeoff. Third, we empirically validate our method on GAIA and SWE-bench benchmarks. On GAIA with GPT-4o agents, CADMAS-CTX achieves 0.442 accuracy, outperforming static baseline 0.381 and AutoGen 0.354 with non-overlapping 95% confidence intervals. On SWE-bench Lite, it improves resolve rate from 22.3% to 31.4%. Ablations show the uncertainty penalty improves robustness against context tagging noise. Our results demonstrate contextual calibration and risk-aware delegation significantly improve multi-agent teamwork compared with static global skill assignments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CADMAS-CTX** 的框架，旨在解决多智能体系统（MAS）在任务委派过程中的“上下文盲区”问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的智能体委派机制（如 AutoGen 或角色分配）通常假设智能体的能力是静态的、基于技能标签的（例如“擅长编程”）。
*   **研究动机**：作者发现 LLM 智能体的表现高度依赖于**任务上下文**（Context）。例如，一个编程智能体可能擅长独立的代码片段编写，但在处理长程调试或复杂依赖时会失效。如果仅使用全局平均分来评估智能体，会导致系统性地将复杂任务委派给不擅长的智能体（误委派）。
*   **整体含义**：将委派问题从“谁最擅长这项技能”转变为“在当前条件下谁最值得信任”，引入上下文感知的校准机制。

### 2. 方法论
*   **核心思想**：利用**分层上下文能力画像**和**风险感知评分**，在去中心化的环境中动态学习并校准每个智能体在不同任务桶（Buckets）中的表现。
*   **关键技术细节**：
    *   **分层画像**：为每个智能体、每种技能、每个上下文桶（如：难度、依赖性、是否使用工具）维护一个 **Beta 后验分布**。
    *   **风险感知评分（LCB）**：委派决策基于公式 $Score = \mu - \gamma \sqrt{u}$。其中 $\mu$ 是后验均值（代表历史成功率），$\sqrt{u}$ 是后验标准差（代表不确定性），$\gamma$ 是惩罚系数。这种设计确保只有在证据充分且表现优异时才进行委派。
    *   **去中心化入口选择**：任务到达时，候选智能体根据自身对任务上下文的理解进行“投标”，得分最高者成为该任务的临时协调员。
    *   **经验驱动校准**：任务完成后，根据成功/失败结果通过 Beta-Binomial 更新机制实时修正对应桶的参数。
    *   **冷启动转移**：利用经验贝叶斯收缩（Empirical Bayes Shrinkage）从相邻桶借用伪计数，缓解新上下文下的数据稀疏问题。

### 3. 实验设计
*   **数据集/场景**：
    1.  **受控仿真**：模拟智能体能力翻转（Flip）和稀疏桶场景。
    2.  **GAIA 基准测试**：评估多步骤工具使用能力。
    3.  **SWE-bench Lite**：评估长程软件工程和代码依赖处理能力。
    4.  **HumanEval/EvalPlus**：验证不同模型（GPT-4o, Claude 3.5）在不同难度下的能力异质性。
*   **对比方法（Baselines）**：
    *   **AutoGen**：默认的 GroupChat 编排模式。
    *   **Static Routing**：基于技能的静态路由（不考虑上下文）。
    *   **Learned Router**：基于任务特征的逻辑回归分类器。
    *   **Thompson Sampling**：经典的老虎机探索策略。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该框架主要是在推理阶段进行在线后验更新（Online Learning），不涉及大规模的模型微调或预训练，因此算力消耗主要集中在调用 LLM API（GPT-4o 和 Claude 3.5 Sonnet）上。

### 5. 实验数量与充分性
*   **实验规模**：作者设计了 6 个研究问题（RQ1-RQ6），涵盖了从理论直觉验证到端到端真实基准测试的全过程。
*   **充分性与客观性**：
    *   仿真实验运行了 30 个随机种子。
    *   基准测试（GAIA, SWE-bench）运行了 5-10 次独立实验，并报告了 **95% 自举置信区间（Bootstrap CI）**。
    *   进行了消融实验以验证不确定性惩罚（$\gamma$）和标记噪声对系统的影响。
    *   实验设计较为客观，通过非重叠的置信区间证明了结果的统计显著性。

### 6. 主要结论与发现
*   **性能提升**：在 GAIA 上，CADMAS-CTX 的准确率（0.442）显著高于静态基准（0.381）和 AutoGen（0.354）。在 SWE-bench Lite 上，解决率从 22.3% 提升至 31.4%。
*   **理论验证**：证明了在上下文异质性存在时，静态路由会产生线性遗憾（Linear Regret），而上下文感知路由可实现次线性遗憾。
*   **鲁棒性**：不确定性惩罚能有效防止在证据不足的情况下过度委派，且对上下文标记噪声具有较强的容忍度（在标记准确率降至 70% 时仍表现良好）。
*   **去中心化优势**：在智能体能力发生动态漂移（Drift）时，去中心化的评分机制比中心化协调器响应更快。

### 7. 优点
*   **理论与实践结合**：将经典的上下文老虎机理论成功应用于现代 LLM 智能体委派问题。
*   **风险感知**：引入不确定性惩罚，解决了 LLM 容易产生的过度自信和误委派问题。
*   **即插即用**：作为一个轻量级的校准层，可以适配不同的底层模型和现有的智能体协议。
*   **冷启动处理**：通过分层结构和伪计数转移，解决了在线学习初期数据稀疏的难题。

### 8. 不足与局限
*   **桶粒度限制**：目前采用的是粗粒度的离散桶（如：简单/中等/困难），尚未实现实例级（Instance-level）的精细化调整。
*   **依赖标记质量**：虽然具有一定的鲁棒性，但系统性能仍高度依赖于初始任务分解和上下文标记的准确性。
*   **技能覆盖**：虽然在编程和通用任务上做了验证，但在更多样化的技能领域（如多模态、创意写作等）的泛化性有待进一步测试。
*   **边界效应**：离散分桶可能在上下文边界处产生不连续的委派决策。

（完）
