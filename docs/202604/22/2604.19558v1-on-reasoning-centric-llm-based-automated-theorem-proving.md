---
title: On Reasoning-Centric LLM-based Automated Theorem Proving
title_zh: 论以推理为中心的基于大语言模型的自动定理证明
authors: "Yican Sun, Chengwei Shi, Hangzhou Lyu, Yingfei Xiong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19558v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 以推理为中心的大语言模型自动定理证明代理
tldr: "本研究针对现有LLM证明代理在高级规划和自我批判方面的不足，提出了以推理为中心的ReCent-Prover。该工具通过“带反思的验证”机制让模型审视并过滤错误策略，并利用“带规划的检索”使引理搜索与证明策略对齐。在CoqStoq基准测试中，ReCent-Prover在相同调用预算下比现有最先进技术提升了22.58%的定理证明量，显著增强了自动化定理证明能力。"
selection_source: fresh_fetch
motivation: 现有的LLM证明代理在生成证明步骤时缺乏高层战略规划和对自身提议的批判性评估。
method: 提出了ReCent-Prover，引入了用于过滤错误策略的验证反思机制和基于证明规划的引理检索技术。
result: "在CoqStoq基准测试中，该方法相比之前的最先进技术实现了22.58%的相对性能提升。"
conclusion: 以推理为中心的设计能更有效地发挥LLM在复杂形式化证明任务中的潜力。
---

## 摘要
自动定理证明是形式化方法的基础，最近的趋势是将大语言模型（LLMs）与证明助手相结合，以构建有效的证明智能体。尽管现有的证明智能体表现出良好的性能，但它们在高级规划和自我批判方面未能充分利用现代 LLMs 的推理能力。我们认为，证明智能体不应仅仅生成策略（tactics），还应针对证明计划进行战略性推理，并批判性地评估其自身的提议。本文介绍了 ReCent-Prover，这是一种针对 Rocq 的以推理为中心的基于 LLM 的证明智能体，旨在解决当前系统中的两个关键局限。首先，我们提出了带反思的验证（validation with reflection），使 LLMs 能够审查其生成的策略，并在反思识别出潜在错误时合成失败摘要，从而更早地过滤掉可能被误用的策略。其次，我们提出了带规划的检索（retrieval with planning），该方法将检索条件设定在 LLM 生成的证明计划上，而非子目标相似性，从而检索出与预期证明策略一致的引理和证明。这两种技术都增加了 LLM 的调用次数。然而，在 CoqStoq 基准测试中，即使在相同的 LLM 调用预算下，ReCent-Prover 证明的定理数量也比之前的最先进技术实现了 22.58% 的相对提升，这表明我们以推理为中心的设计显著增强了自动定理证明的能力。

## Abstract
Automated theorem proving is fundamental to formal methods, and the recent trend is to integrate large language models (LLMs) and proof assistants to form effective proof agents. While existing proof agents show promising performance, they inadequately leverage reasoning capabilities of modern LLMs in high-level planning and self-critique. We argue that proof agents should not merely generate tactics but also reason strategically about proof plans and critically evaluate their own proposals.   This paper introduces ReCent-Prover, a reasoning-centric LLM-based proof agent for Rocq that addresses two critical limitations in current systems. First, we present validation with reflection, enabling LLMs to scrutinize their generated tactics and synthesize failure summaries when reflection identifies potential errors, filtering out potentially misapplied tactics earlier. Second, we propose retrieval with planning, which conditions retrieval on LLM-generated proof plans rather than subgoal similarity, retrieving lemmas and proofs that align with the anticipated proof strategy. Both techniques increase the number of invocations of LLMs. However, when evaluated on the CoqStoq benchmark, even under the same budget of LLM invocations, ReCent-Prover achieves a 22.58% relative improvement in the number of proved theorems over the previous state-of-the-art, demonstrating that our reasoning-centric design significantly enhances automated theorem proving capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《On Reasoning-Centric LLM-based Automated Theorem Proving》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出，现有的基于大语言模型（LLM）的自动定理证明（ATP）系统主要关注 LLM 与证明助手（如 Rocq/Coq）之间的交互机制，而未能充分利用现代 LLM 在**高级规划（High-level Planning）**和**自我批判（Self-critique）**方面的推理能力。
*   **研究动机**：现有的证明智能体往往盲目接受所有通过证明助手语法检查的策略（Tactics）。然而，某些策略虽然语法正确，却可能导致证明进入死胡同或使子目标变得不可解（例如错误的归纳变量）。
*   **核心挑战**：如何让 LLM 在证明过程中进行战略性思考，并根据证明计划而非简单的文本相似性来检索相关知识。

### 2. 方法论：ReCent-Prover
论文提出了 **ReCent-Prover**，其核心思想是将推理置于证明过程的中心，包含两个关键模块：
*   **带反思的验证（Validation with Reflection）**：
    *   在策略执行后，不仅依靠证明助手检查语法，还调用 LLM 对特定类别的策略（如 `induction`, `apply`, `assert`）进行自我审查。
    *   如果 LLM 判定该策略误导了证明方向（如归纳假设过弱），则生成失败摘要并回滚到上一个有效状态，将失败原因记录在历史中以指导后续尝试。
*   **带规划的检索（Retrieval with Planning）**：
    *   **预处理**：使用 LLM 为库中的每个引理生成自然语言描述，为每个证明示例生成证明计划，并存储其向量嵌入。
    *   **在线检索**：面对当前子目标，LLM 先生成一个自然语言证明计划。系统根据该计划的语义嵌入（而非子目标的文本）去检索最匹配的引理和证明示例。这解决了传统 BM25 检索中因符号差异导致的“语义鸿沟”问题。

### 3. 实验设计
*   **数据集/场景**：使用 **CoqStoq** 基准测试集，涵盖了真实的 Rocq 项目（如 CompCert 编译器验证、四色定理证明等）。
*   **Benchmark**：从 CoqStoq 中随机采样 200 个定理进行主实验，并在 222 个重叠定理上与 SOTA 进行对比。
*   **对比方法**：
    *   **CobbleStone**（之前的 SOTA 证明智能体）。
    *   其他基准：PALM, ProverBot9001, Rango, Tactician, 以及纯符号工具 CoqHammer。
*   **后端模型**：主要使用 **o4-mini** 和 **GPT-4**，并使用 **MiniMax-M2.5** 测试了开源模型的泛化性。

### 4. 资源与算力
*   **硬件环境**：实验在配备 Intel Xeon Gold 6230 CPU 和 503 GB 内存的服务器上运行。
*   **算力说明**：论文未明确提及 GPU 型号或训练时长，因为该研究属于 **Agent 框架推理侧**的应用，不涉及模型微调。其成本主要体现在 LLM API 的调用上。
*   **配置**：CoqHammer 超时设为 25 秒，允许 64 线程并行。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组实验，包括 200 个样本的整体性能评估、222 个样本的 SOTA 对比、以及 5 组消融实验（C1-C5）。
*   **充分性与客观性**：
    *   **消融实验**：系统地验证了 Hammer、LLM、反思模块和规划检索模块的各自贡献。
    *   **公平性**：在与 CobbleStone 对比时，严格限制了相同的 LLM 调用预算（20 次调用/定理），并使用了相同的后端模型。
    *   **统计显著性**：所有核心改进均通过了 p-test（p < 0.05），证明结果并非偶然。

### 6. 主要结论与发现
*   **性能提升**：在相同调用预算下，ReCent-Prover 比 CobbleStone 实现了 **22.58%** 的相对性能提升。
*   **Token 效率**：ReCent-Prover 的 Token 效率比 CobbleStone 高出约 **3.1 倍**，证明了高质量推理比盲目搜索更节省资源。
*   **模块有效性**：消融实验显示，带规划的检索比 BM25 检索提升了约 8.5%，而反思模块在不同配置下均能带来 6%-10% 的额外增益。
*   **泛化性**：在开源模型 MiniMax-M2.5 上也观察到了类似的性能提升（约 17.3%）。

### 7. 优点
*   **推理驱动**：改变了以往“生成-尝试-报错”的被动模式，引入了主动的战略规划和自我纠错。
*   **语义对齐**：通过自然语言计划进行检索，有效捕捉了跨越不同语法结构的证明策略。
*   **鲁棒性**：反思机制能有效识别“语法正确但逻辑错误”的陷阱，减少了无效的搜索分支。

### 8. 不足与局限
*   **调用开销**：虽然 Token 效率高，但单次迭代中引入了多次 LLM 调用（规划、生成、反思），在实时性要求极高的场景下可能存在延迟。
*   **依赖描述质量**：检索效果高度依赖于 LLM 生成引理描述和证明计划的准确性。
*   **语言限制**：目前仅在 Rocq (Coq) 语言上进行了验证，尚未扩展到 Lean 或 Isabelle 等其他主流证明助手。

（完）
