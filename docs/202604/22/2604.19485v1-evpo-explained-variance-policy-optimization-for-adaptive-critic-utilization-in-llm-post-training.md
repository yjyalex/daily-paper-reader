---
title: "EVPO: Explained Variance Policy Optimization for Adaptive Critic Utilization in LLM Post-Training"
title_zh: EVPO：用于大语言模型后训练中自适应 Critic 利用的解释方差策略优化
authors: "Chengjun Pan, Shichun Liu, Jiahang Lin, Dingwei Zhu, Jiazheng Zhang, Shihan Dou, Songyang Gao, Zhenhua Han, Binghai Wang, Rui Zheng, Xuanjing Huang, Tao Gui, Yansong Feng"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19485v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 统一了用于大语言模型训练后策略优化的 PPO 和 GRPO 方法
tldr: 针对大语言模型强化学习后训练中 Critic 模型可能引入噪声的问题，本文提出了 EVPO 算法。该方法将基准选择建模为卡尔曼滤波问题，利用“解释方差（EV）”指标实时评估 Critic 的有效性。EVPO 能在训练中动态切换 Critic 基准和批次均值基准，在稀疏奖励场景下有效降低方差，在多项任务中性能优于 PPO 和 GRPO。
selection_source: fresh_fetch
motivation: 在稀疏奖励环境下，学习到的 Critic 可能引入超过其捕获信号的估计噪声，导致优势估计方差增大而非减小。
method: 提出 EVPO 算法，通过实时监测批次级别的解释方差（EV），动态选择使用 Critic 或批次均值作为优势估计的基准。
result: 在经典控制、智能体交互和数学推理等四项任务中，EVPO 的表现一致优于固定的 PPO 或 GRPO 策略。
conclusion: 解释方差是判断 Critic 是否有益的可靠指标，自适应切换机制能有效提升大模型强化学习的训练效率和性能。
---

## 摘要
大语言模型（LLM）后训练中的强化学习（RL）面临一个基本的设计选择：是否使用学习到的 critic 作为策略优化的基准（baseline）。经典理论倾向于使用 PPO 等基于 critic 的方法来减少方差，然而像 GRPO 这样无需 critic 的替代方案因其简洁性和极具竞争力的性能而得到了广泛应用。我们表明，在稀疏奖励设置下，学习到的 critic 可能会引入超过其捕获的状态信号的估计噪声，从而增加而非减少优势（advantage）方差。通过将基准选择建模为卡尔曼滤波问题，我们将 PPO 和 GRPO 统一为卡尔曼增益的两个极端，并证明了可从单个训练批次计算的解释方差（EV）能够识别出确切的边界：正的 EV 表示 critic 减少了方差，而零或负的 EV 则表明它放大了方差。基于这一见解，我们提出了解释方差策略优化（EVPO），它在每个训练步骤监控批次级别的 EV，并在基于 critic 的优势估计和批次均值优势估计之间进行自适应切换，可证明在每一步实现的方差都不大于两者中较优的一个。在涵盖经典控制、智能体交互和数学推理的四项任务中，无论给定任务中哪种固定基准更强，EVPO 的表现都一致优于 PPO 和 GRPO。进一步的分析证实，自适应门控在训练过程中跟踪了 critic 的成熟度，并且理论推导出的零阈值在经验上是最佳的。

## Abstract
Reinforcement learning (RL) for LLM post-training faces a fundamental design choice: whether to use a learned critic as a baseline for policy optimization. Classical theory favors critic-based methods such as PPO for variance reduction, yet critic-free alternatives like GRPO have gained widespread adoption due to their simplicity and competitive performance. We show that in sparse-reward settings, a learned critic can inject estimation noise that exceeds the state signal it captures, increasing rather than reducing advantage variance. By casting baseline selection as a Kalman filtering problem, we unify PPO and GRPO as two extremes of the Kalman gain and prove that explained variance (EV), computable from a single training batch, identifies the exact boundary: positive EV indicates the critic reduces variance, while zero or negative EV signals that it inflates variance. Building on this insight, we propose Explained Variance Policy Optimization (EVPO), which monitors batch-level EV at each training step and adaptively switches between critic-based and batch-mean advantage estimation, provably achieving no greater variance than the better of the two at every step. Across four tasks spanning classical control, agentic interaction, and mathematical reasoning, EVPO consistently outperforms both PPO and GRPO regardless of which fixed baseline is stronger on a given task. Further analysis confirms that the adaptive gating tracks critic maturation over training and that the theoretically derived zero threshold is empirically optimal.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《EVPO: Explained Variance Policy Optimization for Adaptive Critic Utilization in LLM Post-Training》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的强化学习（RL）后训练中，研究者面临一个基本选择：是使用带有学习 Critic（价值函数）的算法（如 PPO），还是使用无需 Critic 的算法（如 GRPO）。
*   **研究动机**：经典理论认为 Critic 能通过状态相关的基准（Baseline）减少方差，但在 LLM 的**稀疏奖励**场景下，Critic 在训练初期或复杂任务中往往极不稳定。
*   **核心问题**：论文指出，当 Critic 的估计噪声超过它所能捕获的状态信号时，它不仅不能减少方差，反而会增加方差，导致训练崩溃。论文旨在回答：**在 LLM 后训练中，Critic 何时有益，何时有害？能否实时检测并自适应切换？**

### 2. 方法论：核心思想与技术细节
论文提出了 **EVPO（解释方差策略优化）**，其核心思想是将优势估计（Advantage Estimation）的基准选择建模为一个**卡尔曼滤波（Kalman Filtering）**问题。

*   **卡尔曼滤波视角**：将 Critic 的预测和批次均值（Batch-mean）视为对真实状态价值的两个竞争估计。PPO 和 GRPO 分别对应卡尔曼增益的两个极端（完全信任 Critic 或完全不信任）。
*   **关键指标：解释方差（Explained Variance, EV）**：
    *   公式定义：$ev = 1 - \frac{Var(G - \hat{V})}{Var(G)}$，其中 $G$ 是回报，$\hat{V}$ 是 Critic 预测值。
    *   **理论边界**：论文证明了 $ev = 0$ 是确切的临界点。当 $ev > 0$ 时，Critic 减少方差；当 $ev \le 0$ 时，Critic 增加方差。
*   **算法流程**：
    1.  在每个训练步，从当前策略采样一个批次的轨迹。
    2.  计算该批次的解释方差（bev）。
    3.  **自适应门控（Gating）**：
        *   若 $bev > 0$：进入 **PPO 模式**，使用 Critic 作为基准。
        *   若 $bev \le 0$：进入 **GRPO 模式**，丢弃 Critic，改用批次均值作为基准。
    4.  更新策略模型和 Critic 模型。
*   **方差保证**：证明了 EVPO 在每一步产生的优势估计方差，都不会大于 PPO 和 GRPO 中较优的那一个。

### 3. 实验设计
论文在四个具有代表性的任务上进行了验证：
*   **任务场景**：
    1.  **Sokoban（推箱子）**：多步规划，稀疏奖励。
    2.  **FrozenLake（冰湖）**：具有环境随机性的路径规划。
    3.  **WebShop**：交互式电商智能体任务，涉及多步指令遵循。
    4.  **MATH**：复杂的数学推理任务（使用 DAPO-Math-17k 数据集）。
*   **对比方法（Benchmarks）**：
    *   **Base LLM**：未经过 RL 训练的模型（Qwen2.5-3B/7B-Instruct）。
    *   **基于 Critic 的方法**：PPO、StarPO-S。
    *   **无需 Critic 的方法**：GRPO、DAPO。

### 4. 资源与算力
*   **模型规模**：数学任务使用 **Qwen2.5-7B-Instruct**，其他任务使用 **Qwen2.5-3B-Instruct**。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）的数量及总训练时长。但从任务规模和模型大小推断，这属于中大规模的 LLM 强化学习实验。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了 4 个不同领域的任务，每个任务都对比了 4 种主流 RL 算法。
*   **消融与分析实验**：
    *   **噪声注入实验**：验证了 Critic 噪声与性能下降的因果关系。
    *   **冷启动与预热实验**：证明了在训练初期抑制 Critic 噪声的有效性。
    *   **阈值敏感性分析**：测试了不同 EV 切换阈值（-0.2 到 0.2），证实了理论推导的 $0$ 阈值确实是经验最优。
*   **充分性评价**：实验设计非常充分，不仅有最终性能对比，还有训练过程中的门控行为分析，逻辑闭环严密。

### 6. 主要结论与发现
1.  **EV 是可靠的指示器**：EV 的正负号能准确预测 Critic 是否会损害策略优化。
2.  **EVPO 性能卓越**：在所有四个任务中，EVPO 均优于固定的 PPO 或 GRPO，无论该任务原本更适合哪种方法。
3.  **自适应性**：EVPO 能够自动识别 Critic 的成熟度。在训练初期或策略进入困难区域时，它会自动退回到批次均值模式。
4.  **理论统一**：成功将 PPO 和 GRPO 统一到了卡尔曼滤波的框架下，消除了算法选择的二元对立。

### 7. 优点
*   **理论优雅**：通过卡尔曼滤波视角统一了现有算法，并给出了严谨的数学证明。
*   **轻量级实现**：计算批次 EV 几乎不增加额外的计算开销（利用已有的 Critic 前向结果）。
*   **即插即用**：可以很容易地集成到现有的 PPO 训练框架中。
*   **鲁棒性强**：解决了稀疏奖励下 Critic “帮倒忙”的顽疾，提升了训练稳定性。

### 8. 不足与局限
*   **显存开销**：虽然 EVPO 在某些步不使用 Critic，但为了维持自适应能力，仍需在内存中保留 Critic 网络，因此其显存占用与 PPO 相当，高于纯粹的 GRPO。
*   **任务覆盖**：实验主要集中在稀疏奖励和验证性奖励（RLVR）场景，对于具有密集奖励（Dense Reward）或复杂奖励模型（RM）的任务，EV 的动态特性可能有所不同。
*   **有限样本偏差**：理论基于总体分布，而实际操作中使用的是批次样本估计的 EV，在极小批次下可能存在估计偏差。

（完）
