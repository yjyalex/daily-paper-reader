---
title: "TRN-R1-Zero: Text-rich Network Reasoning via LLMs with Reinforcement Learning Only"
title_zh: TRN-R1-Zero：仅通过强化学习利用大语言模型进行富文本网络推理
authors: "Yilun Liu, Ruihong Qiu, Zi Huang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19070v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 使用类GRPO目标在富文本网络上进行推理的强化学习框架
tldr: 本文提出TRN-R1-Zero，一种仅通过强化学习实现富文本网络（TRN）推理的后训练框架。针对现有方法依赖监督微调或模型蒸馏的局限，该框架采用邻居感知的组相对策略优化（GRPO）算法，通过新颖的边际增益指标引导LLM整合文本语义与图结构。实验证明，该方法在多种TRN任务中表现卓越，并具备从节点级训练向边级和图级任务迁移的零样本推理能力。
selection_source: fresh_fetch
motivation: 旨在解决富文本网络推理中模型难以在无监督情况下有效整合文本语义与复杂关系结构的问题。
method: 提出一种仅依赖强化学习的框架，利用邻居感知的GRPO算法和动态奖励机制引导模型进行关系推理，无需SFT或CoT数据。
result: 在引用、社交等多个TRN基准上取得领先性能，并成功实现从节点级任务到边级和图级任务的零样本跨层级推理。
conclusion: 该研究证明了通过纯强化学习优化LLM可以有效提升其在复杂网络结构中的泛化推理能力和跨领域适应性。
---

## 摘要
富文本网络（TRNs）上的零样本推理仍然是一个具有挑战性的前沿领域，因为模型必须在没有特定任务监督的情况下整合文本语义与关系结构。虽然图神经网络依赖于固定的标签空间和监督目标，但近期基于大语言模型（LLM）的方法往往忽略了图上下文，或者依赖于从更大模型中进行的蒸馏，从而限制了泛化能力。我们提出了 TRN-R1-Zero，这是一个仅通过强化学习训练的用于 TRN 推理的训练后框架。TRN-R1-Zero 使用一种邻居感知的组相对策略优化（Neighbour-aware Group Relative Policy Optimisation）目标直接优化基础 LLM，该目标根据一种新颖的邻居信号信息量边际增益指标动态调整奖励，从而有效地引导模型进行关系推理。与以往的方法不同，TRN-R1-Zero 不需要监督微调，也不需要由大型推理模型生成的思维链数据。在引用、超链接、社交和共同购买等 TRN 基准测试上的广泛实验证明了 TRN-R1-Zero 的优越性和鲁棒性。此外，TRN-R1-Zero 严格依赖于节点级训练，实现了在边级和图级任务上的零样本推理，其能力超出了跨域迁移。代码库已在 https://github.com/superallen13/TRN-R1-Zero 公开。

## Abstract
Zero-shot reasoning on text-rich networks (TRNs) remains a challenging frontier, as models must integrate textual semantics with relational structure without task-specific supervision. While graph neural networks rely on fixed label spaces and supervised objectives, recent large language model (LLM)-based approaches often overlook graph context or depend on distillation from larger models, limiting generalisation. We propose TRN-R1-Zero, a post-training framework for TRN reasoning trained solely via reinforcement learning. TRN-R1-Zero directly optimises base LLMs using a Neighbour-aware Group Relative Policy Optimisation objective that dynamically adjusts rewards based on a novel margin gain metric for the informativeness of neighbouring signals, effectively guiding the model toward relational reasoning. Unlike prior methods, TRN-R1-Zero requires no supervised fine-tuning or chain-of-thought data generated from large reasoning models. Extensive experiments across citation, hyperlink, social and co-purchase TRN benchmarks demonstrate the superiority and robustness of TRN-R1-Zero. Moreover, relying strictly on node-level training, TRN-R1-Zero achieves zero-shot inference on edge- and graph-level tasks, extending beyond cross-domain transfer. The codebase is publicly available at https://github.com/superallen13/TRN-R1-Zero.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TRN-R1-Zero** 的新框架，旨在通过纯强化学习（Reinforcement Learning Only）提升大语言模型（LLM）在富文本网络（TRN）上的推理能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不依赖监督微调（SFT）或从大型推理模型（如 GPT-4、DeepSeek-R1）进行知识蒸馏的情况下，激发 LLM 对富文本网络（如引用网络、社交网络、电商网络）的结构化推理能力？
*   **背景**：传统的图神经网络（GNN）依赖固定标签和监督信号，而现有的 LLM 方法要么仅将 LLM 作为文本编码器，要么依赖昂贵的思维链（CoT）数据标注。
*   **整体含义**：论文提出了一种“纯 RL”路径，通过邻居感知的奖励机制，让模型在训练过程中自发学习如何整合节点文本与图结构信息，实现跨领域、跨任务的零样本（Zero-shot）推理。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用强化学习中的 **GRPO（组相对策略优化）** 算法，并引入一种**邻居感知（Neighbour-aware）**的奖励塑造机制，引导模型关注那些“结构信息对决策至关重要”的样本。
*   **关键技术细节**：
    *   **邻居感知 GRPO**：在标准 GRPO 基础上，通过对比“仅有节点文本”和“聚合邻居信息后”的分类置信度变化，计算**边际增益（Margin Gain, $\Delta_i$）**。
    *   **边际增益公式**：$\Delta_i = m_i(\tilde{\ell}) - m_i(\ell)$。其中 $m_i(\ell)$ 是孤立节点的分类边际得分，$m_i(\tilde{\ell})$ 是通过简单图卷积（SGC）聚合邻居后的得分。
    *   **动态奖励塑造**：最终奖励 $R_i = \exp(\alpha |\Delta_i|) \cdot R_{base}$。这意味着如果邻居信息显著改变了预测结果（无论正向还是负向），该样本在 RL 优化中的权重就会被放大，迫使模型深入思考结构关系。
    *   **提示词设计**：采用包含 `<think>` 和 `<answer>` 标签的推理模板，通过随机子图采样进行数据增强。

### 3. 实验设计
*   **数据集/场景**：涵盖 4 大领域共 9 个数据集：
    *   **引用网络**：Cora, Citeseer。
    *   **超链接网络**：WikiCS。
    *   **社交网络**：Instagram。
    *   **共同购买网络**：Photo, History。
    *   **常识图谱/边预测**：Expla-Graph, WikiCS-Link, Instagram-Link。
*   **训练与测试设置**：在 Citeseer 和 History 上进行 RL 训练，在其余数据集上进行**零样本跨领域**评估。
*   **对比方法（Benchmarks）**：
    *   **通用 LLM**：GPT-4o, Llama-3.1-8B, Qwen2.5 (1.5B/7B/14B)。
    *   **图基础模型（GFM）**：ZeroG, LLaGA。
    *   **推理型 LLM**：Graph-R1（基于 DeepSeek 架构的方法）。

### 4. 资源与算力
*   **算力设备**：使用了单张 **AMD MI300X GPU**。
*   **训练细节**：采用 **LoRA**（Rank=64）进行高效微调。
*   **模型规模**：主要基于 Qwen2.5-7B-Instruct，同时也测试了 1.5B、3B、8B 和 14B 规模。
*   **时长**：文中未明确给出具体的训练总时长，但提到通过 RL 优化显著缩短了推理响应长度，提高了效率。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度实验，包括 4 个领域的零样本节点分类、跨任务（边预测和图推理）迁移实验、监督学习下的性能对比。
*   **消融实验**：详细对比了有无“边际增益奖励塑造”的效果，并分析了训练过程中熵、响应长度和关键词频率的变化。
*   **充分性与公平性**：实验覆盖了不同规模的底座模型（3B 到 14B），并使用了配对 t 检验（p ≤ 0.05）和 Bonferroni 校正，确保了结果的统计显著性和客观性。

### 6. 主要结论与发现
*   **RL 优于蒸馏**：TRN-R1-Zero 在不使用任何 SFT 数据的情况下，性能超过了依赖 GPT-4 蒸馏或复杂 SFT 的现有模型。
*   **跨任务迁移能力**：仅在节点级任务上训练的模型，能够直接在边预测（Link Prediction）和图级推理任务上取得显著提升（如在 WikiCS-Link 上准确率提升 16.1%）。
*   **推理效率**：相比于 Graph-R1，TRN-R1-Zero 生成的推理轨迹更简洁（长度缩短约 6-8 倍），但准确率更高。
*   **稳定性**：邻居感知的奖励塑造显著增强了 RL 训练的稳定性，避免了模型陷入无意义的重复。

### 7. 优点
*   **无需人工标注**：完全摆脱了对高质量 CoT 数据或大型模型 API 的依赖，降低了训练成本。
*   **内生推理能力**：通过奖励机制直接从图结构中提取逻辑，而非简单的模式匹配。
*   **泛化性极强**：展现了卓越的跨领域（如从论文引用迁移到社交网络）和跨层级（从节点迁移到边/图）的泛化能力。

### 8. 不足与局限
*   **底座模型依赖**：RL 的效果高度依赖于基础 LLM 的预训练知识。如果底座模型完全缺乏相关领域知识，RL 提升的空间有限。
*   **文本质量敏感**：对于包含大量噪声或无意义文本（如论文目录、简短评论）的节点，模型推理仍面临挑战。
*   **超参数敏感性**：奖励塑造中的温度参数 $\alpha$ 和采样策略可能需要针对不同规模的模型进行微调。

（完）
