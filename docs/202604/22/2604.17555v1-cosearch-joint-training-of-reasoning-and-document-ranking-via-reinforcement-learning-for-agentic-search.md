---
title: "COSEARCH: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search"
title_zh: COSEARCH：通过强化学习联合训练推理与文档排序以实现智能体搜索
authors: "Hansi Zeng, Liam Collins, Bhuvesh Kumar, Neil Shah, Hamed Zamani"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17555v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过强化学习联合训练推理和文档排序
tldr: 本研究针对智能体搜索中检索系统固定导致的性能瓶颈，提出了CoSearch框架。该框架通过强化学习（GRPO）联合训练多步推理智能体与生成式文档排序模型。通过引入语义分组策略和结合排序质量与轨迹反馈的复合奖励机制，CoSearch有效提升了复杂问答任务的性能，填补了固定检索与理想检索之间的巨大差距，为未来搜索智能体的发展提供了新思路。
selection_source: fresh_fetch
motivation: 现有智能体搜索方法将检索系统视为固定工具，导致检索质量成为制约系统整体性能提升的关键瓶颈。
method: 提出CoSearch框架，利用GRPO算法联合训练推理智能体与排序模型，并引入语义分组策略和复合奖励机制以优化学习信号。
result: 在七个单跳和多跳问答基准测试中，CoSearch均显著优于强基准模型，验证了联合训练各组件的有效性。
conclusion: 联合训练推理智能体与检索系统是提升搜索智能体性能的可行且高效的路径，是构建高性能搜索智能体的关键要素。
---

## 摘要
智能体搜索（Agentic search）——即训练能够迭代推理、发起查询并综合检索信息以回答复杂问题的智能体——已通过强化学习（RL）取得了显著进展。然而，现有方法（如 Search-R1）将检索系统视为固定工具，仅优化推理智能体，而检索组件保持不变。初步实验表明，在七个问答（QA）基准测试中，理想检索（oracle）与固定检索系统之间的差距达到了高达 +26.8% 的相对 F1 提升，这表明检索系统是扩展智能体搜索性能的关键瓶颈。受此发现启发，我们提出了 CoSearch，这是一个通过群体相对策略优化（GRPO）联合训练多步推理智能体和生成式文档排序模型的框架。为了对排序器进行有效的 GRPO 训练（其输入随推理轨迹而变化），我们引入了一种语义分组策略，通过词元级（token-level）相似度对子查询进行聚类，从而在无需额外采样（rollouts）的情况下形成有效的优化组。我们进一步设计了一种复合奖励，结合了排序质量信号与轨迹级结果反馈，为排序器提供即时和长期的学习信号。在七个单跳和多跳问答基准测试上的实验表明，该方法相较于强基线模型有持续的改进，消融研究验证了每个设计选择的有效性。我们的结果表明，联合训练推理智能体和检索系统既可行又具有强大的性能，为未来的搜索智能体指明了一个关键要素。

## Abstract
Agentic search -- the task of training agents that iteratively reason, issue queries, and synthesize retrieved information to answer complex questions -- has achieved remarkable progress through reinforcement learning (RL). However, existing approaches such as Search-R1, treat the retrieval system as a fixed tool, optimizing only the reasoning agent while the retrieval component remains unchanged. A preliminary experiment reveals that the gap between an oracle and a fixed retrieval system reaches up to +26.8% relative F1 improvement across seven QA benchmarks, suggesting that the retrieval system is a key bottleneck in scaling agentic search performance. Motivated by this finding, we propose CoSearch, a framework that jointly trains a multi-step reasoning agent and a generative document ranking model via Group Relative Policy Optimization (GRPO). To enable effective GRPO training for the ranker -- whose inputs vary across reasoning trajectories -- we introduce a semantic grouping strategy that clusters sub-queries by token-level similarity, forming valid optimization groups without additional rollouts. We further design a composite reward combining ranking quality signals with trajectory-level outcome feedback, providing the ranker with both immediate and long-term learning signals. Experiments on seven single-hop and multi-hop QA benchmarks demonstrate consistent improvements over strong baselines, with ablation studies validating each design choice. Our results show that joint training of the reasoning agent and retrieval system is both feasible and strongly performant, pointing to a key ingredient for future search agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《COSEARCH: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于**智能体搜索（Agentic Search）**任务，即智能体通过多轮推理、自主生成查询并整合检索信息来回答复杂问题。
*   **研究动机**：现有的强化学习（RL）搜索智能体（如 Search-R1）通常将检索系统视为一个“固定工具”，只优化推理模型。作者通过实验发现，如果将检索系统替换为“理想检索”（Oracle），智能体的 F1 表现能提升 14.7%（7B 模型）至 26.8%（3B 模型）。
*   **核心问题**：固定的检索系统已成为制约智能体搜索性能的瓶颈。论文探讨了是否可以通过强化学习**联合训练**推理智能体和文档排序器，使两者相互适配。

### 2. 方法论：核心思想与关键技术
作者提出了 **COSEARCH** 框架，其核心思想是利用 **GRPO（群体相对策略优化）** 算法同时优化推理智能体（Main Agent）和生成式排序器（Generative Ranker）。

*   **联合训练架构**：
    *   **推理智能体**：负责生成思考过程（Thought）和搜索查询（Query）。
    *   **生成式排序器**：接收查询和候选文档，通过自然语言推理相关性，并输出重排后的文档索引。
*   **关键技术 1：语义分组策略（Semantic Grouping）**：
    *   GRPO 要求对同一输入的多个输出进行对比。但在多轮搜索中，不同轨迹产生的子查询（Sub-query）各不相同，无法直接分组。
    *   **解决方案**：通过 Token 级别的 F1 相似度对不同轨迹中的子查询进行聚类。相似度超过阈值（$\delta=0.8$）的子查询被视为“语义等价”，组成一个 GRPO 优化组，从而在不增加额外采样成本的情况下实现排序器的 RL 训练。
*   **关键技术 2：复合奖励设计（Composite Reward）**：
    *   **排序奖励（$r_{rel}$）**：基于 Hit@k（前 k 个结果是否包含答案）提供即时反馈。
    *   **轨迹奖励（$r_{main}$）**：基于最终答案的正确性（F1 分数）提供长期反馈。
    *   **逻辑**：当排序器表现较差时，优先使用排序奖励纠偏；当排序已经较好时，引入轨迹奖励以评估检索内容对推理的实际贡献。

### 3. 实验设计
*   **数据集**：涵盖 7 个 QA 基准测试，分为**单跳（Single-hop）**：PopQA, Natural Questions (NQ), TriviaQA；以及**多跳（Multi-hop）**：HotpotQA, 2WikiMultiHopQA, Musique, Bamboogle。
*   **对比方法**：
    *   基础模型：Direct Inference, CoT, RAG。
    *   强基准 RL 方法：Search-o1, Search-R1, ZeroSearch。
    *   内部变体：Retrieval Only（仅检索不排序）、Fixed Ranker（使用固定排序器）。
    *   上限参考：Oracle Retrieval（理想检索）。
*   **模型骨干**：推理智能体使用 Qwen2.5-7B/3B-Instruct，排序器使用 Qwen2.5-7B-Instruct。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练参数**：提到了学习率为 $1 \times 10^{-6}$，Rollout Batch Size 为 512，有效训练 Batch Size 为 128，采样数 $G=8$。这些参数暗示了实验需要相当规模的计算资源（通常为 A100 或 H100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的标准数据集上进行了测试，每组实验均对比了 7B 和 3B 两种规模的推理智能体。
*   **消融实验**：非常充分。涵盖了奖励信号（移除 $r_{main}$ 或 $r_{rel}$）、语义分组策略、架构选择（共享骨干网络 vs 独立模型）、排序器规模等多个维度的对比。
*   **客观性**：通过训练动态曲线（Hit@5 随步数的变化）和搜索轮数分布分析，客观展示了性能提升的来源。实验设计公平，对比基线均为当前领域内最先进的方法。

### 6. 主要结论与发现
*   **性能提升**：COSEARCH 在所有基准上均优于强基线。相比 Search-R1，7B 智能体平均 F1 提升 6.6%，3B 智能体提升 10.8%。
*   **缩小差距**：联合训练成功缩小了固定检索与理想检索之间约 26%-27% 的性能差距。
*   **搜索效率**：经过联合训练的系统平均搜索轮数更少，说明排序器提供了更高质量的信息，减少了智能体因信息不足而进行的重复搜索。
*   **协同效应**：弱推理智能体（3B）从排序器优化中获益更多，证明了“强检索”可以弥补“弱推理”。

### 7. 优点（亮点）
*   **突破性视角**：首次系统性地指出并解决了 RL 搜索智能体中“检索系统固定”这一瓶颈问题。
*   **算法创新**：巧妙地通过“语义分组”解决了 GRPO 在动态多轮对话中输入不一致的痛点，具有很强的工程参考价值。
*   **奖励机制合理**：复合奖励兼顾了检索的局部准确性和推理的全局目标，避免了单一奖励带来的噪声或训练缓慢。

### 8. 不足与局限
*   **排序器规模限制**：实验发现 3B 规模的排序器在训练中容易崩溃（无法稳定输出有效的排列格式），说明该方法对排序器的基础能力有一定门槛要求。
*   **计算开销**：联合训练两个模型（推理+排序）相比仅训练一个模型，显存占用和计算量显著增加。
*   **泛化风险**：虽然在 Wikipedia 语料上表现优异，但在更开放、噪声更大的 Web 搜索环境下的表现尚待验证。
*   **评估指标**：主要依赖 Token 级别的 F1，对于需要长篇生成或开放式回答的任务，其评估效力可能受限。

（完）
