---
title: "Not All Turns Are Equally Hard: Adaptive Thinking Budgets For Efficient Multi-Turn Reasoning"
title_zh: 并非所有轮次都同样困难：面向高效多轮推理的自适应思考预算
authors: "Neharika Jali, Anupam Nayak, Gauri Joshi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05164v2"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于多轮推理和自适应思考预算的GRPO
tldr: "针对多轮推理中LLM推理效率低、存在“过度思考”的问题，本文提出TAB（Turn-Adaptive Budgets）框架。该方法将多轮推理建模为多目标马尔可夫决策过程，利用GRPO算法训练预算分配策略，根据对话历史动态调整每轮的思考预算。实验表明，TAB在保持数学推理准确率的同时，比静态基准节省高达35%-40%的Token，实现了更优的精度-效率权衡。"
selection_source: fresh_fetch
motivation: 现有的推理计算分配方法多针对单轮对话，忽视了多轮推理中各步骤难度不一且存在序列依赖的特性。
method: 将多轮推理建模为多目标马尔可夫决策过程，并使用GRPO算法训练一种能根据上下文动态分配Token预算的策略。
result: "在数学推理基准测试中，TAB在维持准确率的前提下，比现有基准节省了35%至40%的推理Token。"
conclusion: 动态分配推理预算是提升多轮推理效率的关键，通过学习历史和未来信息可显著优化计算资源分配。
---

## 摘要
随着大语言模型（LLM）推理性能进入瓶颈期，提高推理时计算效率对于缓解简单查询中的过度思考和冗长思考轨迹至关重要。先前的方法（包括长度正则化、自适应路由和基于难度的预算分配）主要关注单轮设置，未能解决多轮推理中固有的序列依赖性。在这项工作中，我们将多轮推理建模为一个序列计算分配问题，并将其表述为多目标马尔可夫决策过程。我们提出了 TAB：轮次自适应预算（Turn-Adaptive Budgets），这是一种通过群体相对策略优化（GRPO）训练的预算分配策略，旨在学习在遵守全局单问题 Token 约束的同时最大化任务准确率。因此，TAB 以对话历史作为输入，学习为较简单的轮次自适应地分配较少的预算，并为关键的较难推理步骤节省适量的 Token。我们在数学推理基准测试上的实验表明，TAB 实现了卓越的准确率-Token 权衡，在保持准确率的同时，相比静态和现成的 LLM 预算基准，可节省高达 35% 的 Token。此外，对于可以预先获得所有子问题计划的系统，我们提出了 TAB All-SubQ，这是一种根据对话历史以及所有过去和未来的子问题来分配 Token 的预算策略，相比基准方法可节省高达 40% 的 Token。

## Abstract
As LLM reasoning performance plateau, improving inference-time compute efficiency is crucial to mitigate overthinking and long thinking traces even for simple queries. Prior approaches including length regularization, adaptive routing, and difficulty-based budget allocation primarily focus on single-turn settings and fail to address the sequential dependencies inherent in multi-turn reasoning. In this work, we formulate multi-turn reasoning as a sequential compute allocation problem and model it as a multi-objective Markov Decision Process. We propose TAB: Turn-Adaptive Budgets, a budget allocation policy trained via Group Relative Policy Optimization (GRPO) that learns to maximize task accuracy while respecting global per-problem token constraints. Consequently, TAB takes as input the conversation history and learns to adaptively allocate smaller budgets to easier turns and save appropriate number of tokens for the crucial harder reasoning steps. Our experiments on mathematical reasoning benchmarks demonstrate that TAB achieves a superior accuracy-tokens tradeoff saving up to 35% tokens while maintaining accuracy over static and off-the-shelf LLM budget baselines. Further, for systems where a plan of all sub-questions is available apriori, we propose TAB All-SubQ, a budget allocation policy that budgets tokens based on the conversation history and all past and future sub-questions saving up to 40% tokens over baselines.

---

## 论文详细总结（自动生成）

### 论文总结：并非所有轮次都同样困难——面向高效多轮推理的自适应思考预算

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理能力的提升，推理时计算（Test-time Compute）的增加带来了显著的性能增益，但也导致了“过度思考”（Overthinking）的问题——即模型在处理简单任务时也会生成冗长的思考轨迹，造成计算资源浪费、高延迟和显存压力。
*   **现有局限**：目前的效率优化方法（如路由、长度正则化）多针对单轮对话，忽略了多轮推理（如任务分解）中的**序列依赖性**。
*   **核心挑战**：在多轮推理中，早期的 Token 消耗会累积到后续上下文，且各轮次难度不一。简单的轮次若消耗过多预算，会导致关键的困难轮次预算不足。
*   **研究目标**：如何根据对话历史动态分配每一轮的“思考预算”（Token 限制），以在有限的全局预算下实现最高的推理准确率。

#### 2. 方法论：TAB (Turn-Adaptive Budgets)
论文将多轮推理预算分配建模为一个**多目标马尔可夫决策过程 (MDP)**，并提出 **TAB** 框架：
*   **核心思想**：将预算分配视为一个序列决策问题。预算分配策略（Budgeter）根据当前对话历史和当前子问题，从离散集合（如 256 到 4096 Token）中选择一个预算值。
*   **关键技术细节**：
    *   **状态 (State)**：包含历史子问题、历史回答及当前子问题。
    *   **动作 (Action)**：分配给当前轮次的 Token 预算 $b_t$。
    *   **奖励函数 (Reward)**：采用标量化多目标奖励：$r = \text{acc}(x) - \lambda \max(0, \sum b_t - B)$。其中 $\text{acc}$ 是最终答案的正确性（0 或 1），$B$ 是全局总预算，$\lambda$ 是惩罚权重。
    *   **训练算法**：使用 **GRPO (群体相对策略优化)**。GRPO 无需价值网络，通过群体内的相对奖励来估计优势函数，适合处理多轮推理中的稀疏奖励和信用分配问题。
*   **变体 TAB All-SubQ**：如果系统预先知道所有子问题（即有完整计划），策略将同时参考未来步骤，进一步优化全局分配。

#### 3. 实验设计
*   **系统构成**：由三个模型协作：**User**（负责分解问题）、**Budgeter**（TAB 策略模型）、**Solver**（执行推理，使用支持长度控制的 L1-Qwen3 模型）。
*   **数据集**：
    *   **数学推理（主实验）**：MATH-500, AMC23, MATH Level-5, OlympiadBench, AIME25。
    *   **分布外 (OOD) 测试**：TheoremQA（定理证明）、BBEH-Mini（算法）、GPQA-Main（研究生水平科学问题）。
*   **对比基准 (Benchmarks)**：
    *   **Static**：每轮分配固定的统一预算。
    *   **LLM-Judge Individual**：基于单轮子问题难度分配预算。
    *   **LLM-Judge Multi-Turn**：基于对话历史分配预算，但未经 RL 优化。

#### 4. 资源与算力
*   **算力平台**：使用了匹兹堡超级计算中心的 **Bridges-2 GPU** 集群。
*   **训练细节**：
    *   模型：Budgeter 使用 Qwen3-1.7B 和 4B 版本。
    *   微调方法：**LoRA** (rank=64, alpha=128)。
    *   参数：训练 125 步，Batch Size 为 64，学习率 $10^{-5}$。
    *   上下文窗口：最大 32k Token。
*   **说明**：文中未明确给出具体的 GPU 型号数量（如 A100 或 H100 的具体枚数），但提供了详细的超参数设置。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个核心数学数据集和 3 个跨领域 OOD 数据集，实验组数较为充分。
*   **维度丰富**：不仅对比了准确率与 Token 的权衡曲线，还进行了模型规模缩放（1.7B vs 4B）的分析，以及定性的案例分析。
*   **客观性**：通过设置不同的全局预算惩罚（3k, 5k, 8k, 10k）绘制了完整的 Pareto 前沿曲线，对比基准涵盖了静态和动态的多种主流策略，评估较为公平。

#### 6. 主要结论与发现
*   **高效性**：TAB 在保持或提升准确率的同时，比静态基准节省了 **35%** 的 Token。
*   **规划优于估计**：TAB All-SubQ（具备预见性）比标准 TAB 进一步节省了 **12%** 的 Token（总计比基准节省 **40%**），证明多轮推理本质上是一个规划问题而非简单的难度估计。
*   **自适应行为**：定性分析显示，TAB 学会了在简单的代数步骤节省 Token，而在涉及复杂逻辑或易错计算的步骤分配更多预算。
*   **泛化能力**：在 MATH 数据集上训练的策略在 GPQA 等科学推理任务上依然有效，证明其学到了通用的难度感知能力。

#### 7. 优点
*   **视角创新**：首次将多轮推理的计算分配明确定义为序列决策问题，并利用 GRPO 解决了信用分配难题。
*   **实用性强**：在不改变 Solver 模型参数的前提下，通过轻量级的 Budgeter 显著降低了推理成本。
*   **权衡优化**：提供了清晰的准确率-成本权衡方案，允许开发者根据资源情况灵活调整惩罚因子 $\lambda$。

#### 8. 不足与局限
*   **依赖特定 Solver**：实验依赖于能够严格遵守长度约束的 Solver 模型（如 L1-Qwen），对于普通 LLM 的适用性（仅通过 Prompt 控制长度）可能存在偏差。
*   **任务局限**：主要集中在具有客观验证标准（Verifiable Rewards）的数学和科学任务，对于主观性强、无明确对错的开放式对话推理，奖励函数的设计会更困难。
*   **系统复杂性**：引入了额外的 Budgeter 模型，虽然模型较小，但在推理流水线中增加了一个环节，可能带来微小的首字延迟（Prefill Latency）。

（完）
