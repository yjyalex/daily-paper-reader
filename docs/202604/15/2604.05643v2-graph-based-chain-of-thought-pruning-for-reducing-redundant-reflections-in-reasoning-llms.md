---
title: Graph-Based Chain-of-Thought Pruning for Reducing Redundant Reflections in Reasoning LLMs
title_zh: 基于图的思维链剪枝：减少推理大语言模型中的冗余反思
authors: "Hongyuan Yuan, Xinran He, Run Shao, Bolei He, Xianwei Xue, Mengke Chen, Qiutong Pan, Haiwei Wang, Haifeng Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05643v2"
tags: ["query:rl-nlplr"]
score: 10.0
evidence: 通过强化学习扩展CoT以增强推理并剪枝冗余
tldr: "本研究针对推理大模型在强化学习后出现的“过度思考”问题，提出了一种基于图的思维链（CoT）剪枝框架。通过将线性CoT转换为有向无环图，识别并剔除无差别反射和重复验证等冗余模式。结合SFT、DPO及带长度惩罚的GRPO三阶段训练，在保持甚至提升推理准确率的同时，显著减少了42%的推理Token消耗，实现了推理效率与性能的平衡。"
selection_source: fresh_fetch
motivation: 强化学习虽增强了模型推理能力，但也导致了产生大量低效、重复反射内容的“过度思考”现象。
method: 将CoT转化为有向无环图，通过分支级和深度级剪枝剔除冗余，并利用SFT、DPO和GRPO进行蒸馏优化。
result: "实验表明，该方法在维持或提高模型推理准确率的前提下，平均减少了42%的推理Token使用量。"
conclusion: 通过图结构化剪枝和多阶段对齐，可以有效消除推理模型中的冗余思考，实现更高效、精准的逻辑推理。
---

## 摘要
通过强化学习（RL）扩展思维链（CoT）已被广泛用于增强大语言模型（LLMs）的推理能力。然而，由于奖励信号的稀疏性，这也会诱发不良的思维模式，如“过度思考”（overthinking），即生成冗余的中间推理内容。在这项工作中，我们认为这种冗余的主要来源是低效的反思，通常表现为两种有问题的模式：无差别反思（Indiscriminate Reflection），即模型在整个推理过程中进行广泛且低影响的检查；以及重复性反思（Repetitive Reflection），即模型反复重新验证已确定的结论。为解决这一问题，我们引入了一个基于图的思维链优化框架。具体而言，我们将每个线性思维链转换为具有显式依赖边的有向无环图（DAG），并设计了一种双重剪枝策略：分支级剪枝移除贡献较弱的反思分支，而深度级剪枝则消除后期阶段的重新验证。我们通过一个三阶段流水线来蒸馏这种行为：(1) 有监督微调（SFT）以在剪枝后的简洁轨迹上初始化策略；(2) 直接偏好优化（DPO）以使模型偏好正确但冗余较少的轨迹；(3) 带有长度惩罚的群体相对策略优化（GRPO）以联合优化答案的正确性和效率。实验表明，我们的方法在保持或提高准确率的同时，将平均推理 Token 数量减少了 42%。

## Abstract
Extending CoT through RL has been widely used to enhance the reasoning capabilities of LLMs. However, due to the sparsity of reward signals, it can also induce undesirable thinking patterns such as overthinking, i.e., generating redundant intermediate reasoning content. In this work, we argue that a major source of such redundancy is inefficient reflection, which often manifests in two problematic patterns: Indiscriminate Reflection, where the model performs broad, low-impact checks throughout reasoning, and Repetitive Reflection, where it repeatedly re-verifies an already established conclusion. To address this, we introduce a graph-based CoT optimization framework. Specifically, we convert each linear CoT into a directed acyclic graph (DAG) with explicit dependency edges, and design a dual pruning strategy: branch-level pruning removes weakly contributing reflection branches, while depth-level pruning eliminates late-stage re-verification. We distill this behavior via a three-stage pipeline: (1) SFT to initialize the policy on pruned concise traces, (2) DPO to prefer correct but less redundant trajectories, and (3) GRPO with length penalty to jointly optimize answer correctness and efficiency. Experiments show that our approach reduces the average reasoning tokens by 42\% while maintaining or improving accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：基于图的思维链剪枝（Graph-Based CoT Pruning）

#### 1. 核心问题与研究动机
*   **核心问题**：推理大语言模型（如 OpenAI o1, DeepSeek-R1）通过强化学习（RL）获得了强大的长链推理能力，但也产生了一种副作用——**“过度思考”（Overthinking）**。模型会生成大量对最终答案贡献极小的冗余中间步骤。
*   **研究动机**：作者发现冗余主要源于**低效反思**，具体表现为：
    *   **无差别反思**：对琐碎步骤进行不必要的检查。
    *   **重复性反思**：在推理后期反复验证已经确定的结论。
*   **目标**：在不损失推理准确率的前提下，通过识别并剪枝这些冗余反思步骤，提升模型的推理效率。

#### 2. 方法论
该论文提出了一种基于图结构的 CoT 优化框架，核心流程如下：
*   **思维链图构建（Graph Construction）**：
    *   利用外部 LLM 将线性的 CoT 文本切分为原子化的节点。
    *   将节点分类为“进展型（Progress）”和“反思型（Review）”。
    *   建立节点间的逻辑依赖边，形成有向无环图（DAG）。
*   **双重剪枝策略（Dual Pruning）**：
    *   **分支级剪枝**：移除后继节点数过少（小于阈值 $k$）的反思节点，因为这些节点未能引导出有效的推理主干。
    *   **深度级剪枝**：移除处于推理轨迹后期（相对深度超过阈值 $m$）的反思节点，认为其多为无意义的重复验证。
*   **三阶段训练流水线**：
    1.  **冷启动 SFT**：在剪枝后的简洁 CoT 数据上进行微调，使模型获得初步的简洁推理偏好。
    2.  **轨迹级 DPO**：通过对比“正确且简洁”与“正确但冗余”的轨迹对，进行偏好对齐。
    3.  **带长度惩罚的 GRPO**：在强化学习阶段，以答案正确性为主要奖励，同时引入长度惩罚项（基于最短正确轨迹的归一化超额长度），联合优化效率与准确性。

#### 3. 实验设计
*   **数据集/场景**：主要集中在数学推理任务。
    *   **测试集**：AIME24、AIME25、AMC23、MATH500、OlympiadBench。
    *   **训练集**：Light-R1 数据集（SFT）、AIME/AMC 历史数据（DPO）、dapo-17k（GRPO）。
*   **对比方法（Baselines）**：
    *   **效率优化类**：O1-Pruner、TokenSkip、EfficientReasoning、AdaptThink。
    *   **开源 R1 类模型**：Skywork-OR1-7B、OREAL-7B、Light-R1-DS-7B 等。
*   **评估指标**：平均准确率（Acc）和平均推理 Token 数量（Len）。

#### 4. 资源与算力
*   **硬件设备**：实验在一台拥有 **4 × NVIDIA A800 GPU** 的计算节点上完成。
*   **模型规模**：基于 DeepSeek-R1-Distill-Qwen 的 1.5B 和 7B 版本进行实验。
*   **训练细节**：SFT 阶段 8 个 Epoch，DPO 阶段 5 个 Epoch，GRPO 阶段 220 个 Step。具体训练时长未明确说明，但给出了详细的超参数设置（如学习率、Batch Size 等）。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数学竞赛级基准测试上进行了验证，涵盖了从基础数学到奥数级别的难度。
*   **消融实验**：进行了阶段性消融（Base vs SFT vs DPO vs GRPO），展示了每一阶段对准确率提升和长度缩减的贡献。
*   **质量验证**：
    *   通过人工评估验证了图构建的准确性（节点类型分类 F1 值 > 0.87）。
    *   通过对比实验（剪枝 vs 随机截断）证明了该方法保留了核心推理逻辑。
*   **评价**：实验设计较为全面，对比了多种前沿的效率优化方案，且在不同参数规模的模型上均验证了有效性，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **效率显著提升**：在 7B 模型上，该方法将平均推理 Token 减少了 **42.7%**，同时平均准确率从 59.72% 提升至 60.95%。
*   **复杂任务表现更优**：在 AIME25 等高难度任务上，准确率提升尤为明显，说明减少冗余干扰有助于模型保持逻辑清晰。
*   **行为转变**：训练后的模型显著减少了“Wait”、“Hmm”等反思性词汇，而增加了“Therefore”等进展性连接词，推理风格从“反复纠结”转向“直接决策”。

#### 7. 优点
*   **结构化视角**：不同于以往基于 Token 概率或注意力权重的剪枝，本文从逻辑依赖的图结构入手，更能触及推理冗余的本质。
*   **多阶段协同**：结合了 SFT 的引导、DPO 的偏好对齐和 GRPO 的动态优化，形成了一套完整的蒸馏与强化方案。
*   **性能不降反升**：证明了“思考更久”并不总是更好，精简掉无效反思反而能提高推理的鲁棒性。

#### 8. 不足与局限
*   **预处理成本**：构建推理图需要调用强大的教师模型（如 Qwen-Turbo），这增加了数据准备阶段的计算开销。
*   **领域局限性**：目前主要在数学领域验证，对于法律、创意写作等缺乏明确逻辑依赖关系的开放领域，图构建和剪枝的适用性尚待验证。
*   **分类粒度较粗**：仅将节点分为 Progress 和 Review 可能过于简化，未能捕捉到更细粒度的推理语义。

（完）
