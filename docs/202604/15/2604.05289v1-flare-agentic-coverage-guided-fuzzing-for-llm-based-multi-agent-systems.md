---
title: "FLARE: Agentic Coverage-Guided Fuzzing for LLM-Based Multi-Agent Systems"
title_zh: FLARE：针对基于大语言模型的多智能体系统的智能体化覆盖率引导模糊测试
authors: "Mingxuan Hui, Xinyue Li, Lu Wang, Chengcheng Wan, Yifan Wang, Yimian Wang, Feiyue Song, Beining Shi, Yixi Li, Yaxiao Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05289v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 基于大语言模型的多智能体系统测试框架
tldr: 针对大语言模型多智能体系统（MAS）中因非确定性和复杂交互导致的失效问题，本文提出了FLARE测试框架。FLARE通过从源代码中自动提取智能体规范和行为空间，构建测试预言机，并采用覆盖率引导的模糊测试技术来暴露系统缺陷。在16个开源应用上的评估显示，FLARE在智能体间和内部覆盖率上均优于基准，并发现了56个此前未知的MAS特有故障，显著提升了系统的可靠性。
selection_source: fresh_fetch
motivation: 传统测试方法难以应对多智能体系统因非确定性行为、复杂的智能体间交互以及缺乏明确规范而导致的各种功能失效。
method: FLARE从源码中提取智能体定义以构建行为空间和测试预言机，并利用覆盖率引导的模糊测试来探索执行路径并识别故障。
result: "在16个开源应用上，FLARE实现了极高的智能体间（96.9%）和内部（91.1%）覆盖率，并发现了56个全新的系统故障。"
conclusion: FLARE证明了结合行为规范提取与覆盖率引导的模糊测试是检测多智能体系统复杂交互故障的有效途径。
---

## 摘要
基于大语言模型的多智能体系统（MAS）已被广泛应用于通过将任务分解为子任务来自动化复杂的人类工作流。然而，由于大语言模型智能体的非确定性行为以及智能体之间复杂的交互，MAS 应用经常遇到故障，包括死循环和工具调用失败。由于缺乏大语言模型智能体的规范、MAS 庞大的行为空间以及基于语义的正确性判定，传统的软件测试技术在检测此类故障时效果不佳。本文提出了 FLARE，一种专为 MAS 量身定制的新型测试框架。FLARE 以 MAS 的源代码作为输入，并从智能体定义中提取规范和行为空间。基于这些规范，FLARE 构建测试断言并进行覆盖率引导的模糊测试以暴露故障。随后，它分析执行日志以判断每个测试是否通过，并生成故障报告。我们在 16 个不同的开源应用上进行的评估表明，FLARE 实现了 96.9% 的智能体间覆盖率和 91.1% 的智能体内覆盖率，分别比基准方法高出 9.5% 和 1.0%。FLARE 还发现了 56 个此前未知的、MAS 特有的故障。

## Abstract
Multi-Agent LLM Systems (MAS) have been adopted to automate complex human workflows by breaking down tasks into subtasks. However, due to the non-deterministic behavior of LLM agents and the intricate interactions between agents, MAS applications frequently encounter failures, including infinite loops and failed tool invocations. Traditional software testing techniques are ineffective in detecting such failures due to the lack of LLM agent specification, the large behavioral space of MAS, and semantic-based correctness judgment.   This paper presents FLARE, a novel testing framework tailored for MAS. FLARE takes the source code of MAS as input and extracts specifications and behavioral spaces from agent definitions. Based on these specifications, FLARE builds test oracles and conducts coverage-guided fuzzing to expose failures. It then analyzes execution logs to judge whether each test has passed and generates failure reports. Our evaluation on 16 diverse open-source applications demonstrates that FLARE achieves 96.9% inter-agent coverage and 91.1% intra-agent coverage, outperforming baselines by 9.5% and 1.0%. FLARE also uncovers 56 previously unknown failures unique to MAS.

---

## 论文详细总结（自动生成）

### 论文总结：FLARE —— 针对大语言模型多智能体系统的智能体化覆盖率引导模糊测试

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型多智能体系统（MAS）在自动化复杂工作流中的广泛应用，其可靠性问题日益凸显。由于 LLM 的**非确定性**以及智能体之间**复杂的交互逻辑**，MAS 经常会出现死循环、工具调用错误、任务偏离或逻辑冲突等故障。
传统的软件测试方法（如基于代码覆盖率的模糊测试）在 MAS 面前面临三大挑战：
*   **缺乏规范（Specification）**：智能体的行为通常由自然语言 Prompt 定义，分散且模糊，难以转化为机器可读的测试断言。
*   **行为空间巨大**：智能体的行为由 Prompt、外部工具和动态交互共同决定，静态分析难以建模。
*   **语义化正确性判定**：MAS 的失效往往不是程序崩溃（Crash），而是逻辑上的“语义失效”，需要理解对话内容才能判断。

#### 2. 论文提出的方法论
FLARE 是首个专门为 MAS 设计的功能性测试框架，其核心思想是**“以智能体测试智能体”**。其流程分为三个阶段：
*   **软件分析阶段**：
    *   **规范提取**：利用 `Specification Agent` 通过静态分析源码和框架知识，提取出智能体关系、终止模式、任务执行和工具调用四个维度的 JSON 规范。
    *   **行为空间建模**：利用 `Space Agent` 构建“智能体内行为空间”（原子任务）和“执行路径空间”（合法的交互序列）。
*   **模糊测试循环阶段**：
    *   **种子生成与变异**：初始化任务描述种子，通过**配置变异**（修改模型温度、模型家族）和**序列变异**（针对自由对话模式修改智能体启动顺序）生成测试用例。
    *   **覆盖率引导**：引入了 **RAC（智能体间路径覆盖率）** 和 **AAC（智能体内行为覆盖率）**。FLARE 会优先选择能增加这两项语义覆盖率的种子进行后续变异。
*   **故障识别阶段**：
    *   采用**双智能体验证机制**：`Failure Agent` 根据规范扫描执行日志寻找违规行为；`Judge Agent` 进行二次审核以消除 LLM 幻觉导致的误报，最终生成故障报告。

#### 3. 实验设计
*   **数据集/场景**：选取了 16 个基于 **Microsoft AutoGen** 框架开发的开源 MAS 应用，涵盖报告生成、数据分析、角色扮演、代码生成、视频制作等多样化领域。
*   **对比方法（Baselines）**：
    *   **LLM-Fuzzer**：针对 LLM 安全性的模糊测试工具（适配为变异用户输入）。
    *   **PythonFuzz**：传统的 Python 覆盖率引导模糊测试工具（类似 AFL）。
    *   **Frelatage**：针对 Python 结构化数据的覆盖率模糊测试工具。
*   **评估指标**：语句覆盖率（SC）、分支覆盖率（BC）、智能体间覆盖率（RAC）、智能体内覆盖率（AAC）以及发现的故障数量。

#### 4. 资源与算力
*   **模型使用**：实验主要使用了 **GPT-4.1** 和 **Gemini-1.5-Pro** 作为 FLARE 内部智能体（Specification/Space/Failure/Judge Agent）的后端引擎。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练/测试总时长。由于该框架侧重于推理和自动化测试流程，其开销主要集中在调用 LLM API 的费用和执行 MAS 应用的时间上。

#### 5. 实验数量与充分性
*   **实验规模**：在 16 个不同的真实应用上进行了测试，每个应用都经历了完整的模糊测试循环。
*   **消融与对比**：对比了三种不同类型的基准方法，并进行了**模型敏感性分析**（对比 GPT-4.1 和 Gemini-1.5-Pro 作为后端的效果）。
*   **客观性**：针对生成的规范（Specification），作者进行了**双盲人工评估**，并计算了 Cohen’s Kappa 系数（0.8512），证明了评估的客观性和高一致性。整体实验设计较为充分，涵盖了从底层代码覆盖率到高层语义交互覆盖率的全面对比。

#### 6. 论文的主要结论与发现
*   **覆盖率提升**：FLARE 在 RAC（96.9%）和 AAC（91.1%）上显著优于基准方法，同时在传统代码指标（SC 94.2%, BC 81.7%）上也表现最佳。
*   **故障发现能力**：FLARE 共发现了 **61 个唯一故障**，其中仅 5 个是传统崩溃（Crash），其余 **56 个均为 MAS 特有的逻辑故障**（如任务偏离、工具误用、终止失效等）。相比之下，基准方法几乎只能检测到崩溃。
*   **模型鲁棒性**：FLARE 的效果在不同高性能模型（GPT 和 Gemini）下保持稳定，证明了框架的通用性。

#### 7. 优点
*   **创新性**：首次提出了针对 MAS 语义逻辑的覆盖率指标（RAC/AAC），填补了多智能体系统自动化测试的空白。
*   **自动化程度高**：能够自动从源码中“蒸馏”出测试规范，无需开发者手动编写复杂的测试用例或断言。
*   **闭环反馈**：将语义覆盖率引入模糊测试的反馈回路，使得测试能够更深层地探索智能体间的交互边界。

#### 8. 不足与局限
*   **框架依赖**：目前的实现主要针对 AutoGen 框架，虽然理论可迁移，但适配其他框架（如 LangChain, CrewAI）需要重新设计领域知识 Prompt。
*   **成本问题**：由于测试过程涉及大量的 LLM 调用（用于变异、执行和多智能体评审），在大规模测试时可能会产生较高的 API 费用。
*   **环境限制**：对于需要复杂外部环境（如特定数据库、私有 API）的 MAS，静态分析可能难以完全提取出准确的工具调用规范。
*   **实时性**：由于依赖多轮 LLM 交互进行故障判定，测试反馈循环的速度受限于 LLM 的推理延迟。

（完）
