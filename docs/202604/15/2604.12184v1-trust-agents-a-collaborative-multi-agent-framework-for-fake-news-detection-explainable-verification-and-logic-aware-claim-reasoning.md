---
title: "TRUST Agents: A Collaborative Multi-Agent Framework for Fake News Detection, Explainable Verification, and Logic-Aware Claim Reasoning"
title_zh: TRUST Agents：用于虚假新闻检测、可解释验证及逻辑感知断言推理的协作式多智能体框架
authors: "Gautama Shastry Bulusu Venkata, Santhosh Kakarla, Maheedhar Omtri Mohan, Aishwarya Gaddam"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12184v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于逻辑感知声明推理和验证的协作多智能体框架
tldr: TRUST Agents是一个多智能体协作框架，旨在解决虚假新闻检测中的可解释性和逻辑推理问题。该框架通过声明提取、混合检索、多角色验证及逻辑聚合四个核心步骤，将验证过程透明化。虽然在原始指标上略逊于微调的监督模型，但在可解释性、证据透明度和复合声明推理方面表现出色，为构建可信的事实核查系统提供了新思路。
selection_source: fresh_fetch
motivation: 传统事实核查模型缺乏透明的证据支撑和对复杂逻辑声明的处理能力，难以提供令用户信服的解释。
method: 提出一种多智能体框架，集成声明分解、混合检索、多角色评审团及逻辑聚合器，实现端到端的可解释核查。
result: 在LIAR基准测试中，该框架显著提升了推理透明度和证据引用质量，但在原始准确率上仍受限于检索精度。
conclusion: 多智能体协作能有效增强事实核查的可解释性，未来需重点解决检索质量和不确定性校准等瓶颈问题。
---

## 摘要
TRUST Agents 是一个用于可解释事实验证和虚假新闻检测的协作式多智能体框架。该系统并非将验证视为简单的真伪分类任务，而是识别可验证的断言、检索相关证据、将断言与证据进行对比、在不确定性下进行推理，并生成人类可检查的解释。其基准流水线由四个专门的智能体组成：断言提取智能体利用命名实体识别、依存句法分析和基于大语言模型的提取技术来识别事实性断言；检索智能体使用 BM25 和 FAISS 执行稀疏与密集混合搜索；验证智能体将断言与检索到的证据进行比较，并生成带有校准置信度的判定结果；解释智能体随后生成包含明确证据引用的可读报告。为了更有效地处理复杂断言，我们引入了一个面向研究的扩展版本，包含三个额外组件：受 LoCal 风格断言分解启发的分解智能体；受德尔菲法启发的、具有专门验证者角色的多智能体陪审团；以及一个利用合取、析取、否定和蕴含逻辑来组合原子判定结果的逻辑聚合器。我们在 LIAR 基准数据集上对这两种流水线进行了评估，并与微调后的 BERT、RoBERTa 以及零样本大语言模型基准进行了对比。尽管监督式编码器在原始指标上表现更优，但 TRUST Agents 显著提升了可解释性、证据透明度以及对复合断言的推理能力。结果还表明，检索质量和不确定性校准仍是可信自动化事实验证的主要瓶颈。

## Abstract
TRUST Agents is a collaborative multi-agent framework for explainable fact verification and fake news detection. Rather than treating verification as a simple true-or-false classification task, the system identifies verifiable claims, retrieves relevant evidence, compares claims against that evidence, reasons under uncertainty, and generates explanations that humans can inspect. The baseline pipeline consists of four specialized agents. A claim extractor uses named entity recognition, dependency parsing, and LLM-based extraction to identify factual claims. A retrieval agent performs hybrid sparse and dense search using BM25 and FAISS. A verifier agent compares claims with retrieved evidence and produces verdicts with calibrated confidence. An explainer agent then generates a human-readable report with explicit evidence citations. To handle complex claims more effectively, we introduce a research-oriented extension with three additional components: a decomposer agent inspired by LoCal-style claim decomposition, a Delphi-inspired multi-agent jury with specialized verifier personas, and a logic aggregator that combines atomic verdicts using conjunction, disjunction, negation, and implication. We evaluate both pipelines on the LIAR benchmark against fine-tuned BERT, fine-tuned RoBERTa, and a zero-shot LLM baseline. Although supervised encoders remain stronger on raw metrics, TRUST Agents improves interpretability, evidence transparency, and reasoning over compound claims. Results also show that retrieval quality and uncertainty calibration remain the main bottlenecks in trustworthy automated fact verification.

---

## 论文详细总结（自动生成）

### 论文总结：TRUST Agents 多智能体协作框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着虚假信息的规模化传播，自动化事实核查系统变得至关重要。然而，现有的系统面临三大挑战：
*   **缺乏解释性**：传统的监督学习模型（如 BERT、RoBERTa）通常是“黑盒”，仅给出真伪标签而无证据支撑。
*   **逻辑处理能力弱**：现实中的声明往往包含多个逻辑连接的命题（如“既……又……”或“如果……那么……”），单一模型难以处理这种复合逻辑。
*   **幻觉与校准问题**：直接使用大语言模型（LLM）进行核查容易产生虚假证据（幻觉），且在证据不足时难以表现出适当的不确定性。
**研究动机**：开发一个模块化、可解释、基于证据且具备逻辑感知能力的协作式多智能体框架，将事实核查从简单的分类任务转变为结构化的推理过程。

#### 2. 方法论：核心思想与关键技术
TRUST Agents 采用模块化流水线设计，分为**基准流水线（Baseline）**和**研究扩展流水线（Research Extension）**。

*   **基准流水线核心步骤**：
    1.  **断言提取（Claim Extractor）**：结合命名实体识别（NER）、依存句法分析（识别特定动词如“claim”, “report”）和 LLM 提示词，从文本中提取可验证的事实断言。
    2.  **混合检索（Evidence Retrieval）**：采用 BM25（稀疏检索）和 FAISS（基于向量的密集检索）结合的策略。公式为：$score_{hybrid} = 0.6 \cdot score_{BM25} + 0.4 \cdot score_{dense}$。
    3.  **验证（Verifier）**：将断言与检索到的证据逐一对比，生成包含标签（支持/矛盾/信息不足）、置信度和推理点的结构化 JSON。
    4.  **解释（Explainer）**：将中间推理信号转化为包含明确证据引用的自然语言报告。

*   **研究扩展流水线（核心创新）**：
    1.  **分解智能体（Decomposer）**：将复杂断言分解为原子命题，并输出逻辑公式（如合取、析取、否定、蕴含）。
    2.  **德尔菲陪审团（Delphi Jury）**：引入多个专门角色（如“严谨法律专家”、“开放网络务实派”、“因果怀疑论者”、“阴谋探测器”）对原子命题进行独立评审。
    3.  **逻辑聚合器（Logic Aggregator）**：根据分解出的逻辑公式，对陪审团的原子判定结果进行布尔运算或加权投票聚合。

#### 3. 实验设计
*   **数据集**：使用 **LIAR 基准数据集**，包含 12,836 条政治声明。
*   **标签处理**：将原始的六类标签简化为二分类（True/Mostly-True/Half-True 为正类，其余为负类）。
*   **对比方法（Benchmarks）**：
    *   微调后的 **BERT** 和 **RoBERTa**（监督学习基准）。
    *   **零样本（Zero-shot）LLM**（GPT-4.1-nano）。
    *   TRUST Agents 的基准版本与研究扩展版本。
*   **评估指标**：准确率（Accuracy）和 Macro-F1。针对系统输出的“不确定（Uncertain）”标签，分别采用悲观映射（转为 False）和乐观映射（转为 True）进行评估。

#### 4. 资源与算力
*   **硬件环境**：文中提到实验在一台配备 **8 核 CPU 和 32 GB RAM** 的机器上运行。
*   **算力细节**：
    *   **GPU 信息**：文中**未明确说明**使用的 GPU 型号及数量，仅提到使用 HuggingFace Transformers 进行微调。
    *   **训练时长**：BERT 和 RoBERTa 微调了 2 个 epoch，学习率为 $2 \times 10^{-5}$，Batch Size 为 16。
    *   **推理成本**：由于涉及多智能体多次调用 LLM，系统延迟和成本高于单模型，但文中未给出具体数值。

#### 5. 实验数量与充分性
*   **实验组数**：主要进行了监督模型对比、零样本对比、以及 TRUST Agents 内部两种流水线的消融对比。
*   **充分性评价**：
    *   **客观性**：通过悲观/乐观两种映射方式处理“不确定”标签，客观展示了系统在二分类基准下的表现区间。
    *   **局限性**：实验仅限于 LIAR 一个数据集，缺乏对长文本、多语言或特定领域（如医疗、科学）的泛化性测试。
    *   **公平性**：对比了当前主流的 Transformer 编码器和 LLM 基准，实验设计较为合理。

#### 6. 主要结论与发现
*   **性能权衡**：微调后的监督模型（BERT/RoBERTa）在原始准确率（约 0.65）上仍优于多智能体系统（约 0.52）。这是因为监督模型学习了数据集的统计分布，而多智能体系统依赖于外部证据检索。
*   **高弃权率**：TRUST Agents 在证据不足时表现出高度谨慎，基准版弃权率约 70%，研究版约 82%。
*   **逻辑感知的价值**：研究扩展流水线在处理复合断言时，Macro-F1 略有提升（从 0.434 升至 0.444），且提供了极高的推理透明度。
*   **瓶颈所在**：**检索质量**是核心瓶颈。如果检索不到相关证据，再复杂的推理逻辑也无法给出准确判定。

#### 7. 优点
*   **高度透明与可解释**：提供了从断言提取到证据引用的全链路追踪，适合“人机协作”场景。
*   **逻辑严密**：通过显式的逻辑分解和聚合，解决了 LLM 在处理复杂复合命题时的短板。
*   **角色多样性**：德尔菲陪审团机制通过模拟不同思维偏好，有效降低了单一模型的偏见风险。

#### 8. 不足与局限
*   **基准指标偏低**：在传统的二分类准确率上不及微调模型，可能不适合追求纯粹预测精度的场景。
*   **依赖外部知识库**：系统表现高度受限于检索库（如 Wikipedia）的覆盖范围和时效性。
*   **复杂性与延迟**：多智能体协作涉及多次 LLM 调用和复杂的逻辑解析，推理成本和响应时间较高。
*   **评估偏差**：LIAR 数据集主要针对短声明，可能无法充分体现该框架在处理长篇复杂新闻时的优势。

（完）
