---
title: Calibration-Aware Policy Optimization for Reasoning LLMs
title_zh: 面向推理大语言模型的校准感知策略优化
authors: "Ziqi Wang, Xingzhou Lou, Meiqi Wu, Zhengqi Wen, Junge Zhang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12632v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对推理大模型的校准感知策略优化，改进了GRPO算法
tldr: 针对推理大模型在GRPO优化中出现的过度自信和校准度下降问题，本文提出CAPO算法。通过理论证明GRPO的优势估计忽略了不确定性，CAPO引入逻辑AUC代理损失和噪声掩码机制，实现了校准度与准确率的协同优化。实验表明，CAPO在保持高准确率的同时显著提升了校准度，并在推理缩放和幻觉缓解任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的GRPO算法在提升推理能力时会导致模型过度自信，造成校准度下降且难以在不牺牲准确率的情况下修复。
method: 提出CAPO算法，采用理论一致的逻辑AUC代理损失进行不确定性感知优势估计，并结合噪声掩码机制稳定学习动态。
result: "在多个数学推理基准上，CAPO在显著提升校准度（最高15%）的同时，保持了与GRPO相当或更高的准确率，并优化了推理缩放表现。"
conclusion: CAPO成功解决了推理模型在强化学习中的校准退化问题，为构建既准确又具备可靠置信度的推理模型提供了有效方案。
---

## 摘要
组相对策略优化（GRPO）增强了大语言模型（LLM）的推理能力，但往往会导致过度自信，即错误回答的困惑度低于正确回答，从而降低了由曲线下面积（AUC）所描述的相对校准度。现有方法要么在校准度方面的提升有限，要么以牺牲推理准确率为代价。我们首先证明，GRPO 类算法中的这种退化源于其不感知不确定性的优势估计，这不可避免地导致优化梯度与校准之间产生失配。这导致在提高准确率的同时，以校准度下降为代价。随后，我们提出了校准感知策略优化（CAPO）。它采用了一种在理论上一致且具有遗憾界的 Logistic AUC 代理损失，从而实现了感知不确定性的优势估计。通过进一步引入噪声掩码机制，CAPO 实现了稳定的学习动态，能够同时优化校准度和准确率。在多个数学推理基准测试上的实验表明，CAPO-1.5B 在保持与 GRPO 相当或更优准确率的同时，将校准度显著提升了高达 15%，并在下游推理时缩放任务中进一步将准确率提升了高达 5%。此外，当允许在低置信度条件下拒绝回答时，CAPO 实现了帕累托最优的精确率-覆盖率权衡，凸显了其在缓解幻觉方面的实用价值。

## Abstract
Group Relative Policy Optimization (GRPO) enhances LLM reasoning but often induces overconfidence, where incorrect responses yield lower perplexity than correct ones, degrading relative calibration as described by the Area Under the Curve (AUC). Existing approaches either yield limited improvements in calibration or sacrifice gains in reasoning accuracy. We first prove that this degradation in GRPO-style algorithms stems from their uncertainty-agnostic advantage estimation, which inevitably misaligns optimization gradients with calibration. This leads to improved accuracy at the expense of degraded calibration. We then propose Calibration-Aware Policy Optimization (CAPO). It adopts a logistic AUC surrogate loss that is theoretically consistent and admits regret bound, enabling uncertainty-aware advantage estimation. By further incorporating a noise masking mechanism, CAPO achieves stable learning dynamics that jointly optimize calibration and accuracy. Experiments on multiple mathematical reasoning benchmarks show that CAPO-1.5B significantly improves calibration by up to 15% while achieving accuracy comparable to or better than GRPO, and further boosts accuracy on downstream inference-time scaling tasks by up to 5%. Moreover, when allowed to abstain under low-confidence conditions, CAPO achieves a Pareto-optimal precision-coverage trade-off, highlighting its practical value for hallucination mitigation.

---

## 论文详细总结（自动生成）

这篇论文由中国科学院自动化研究所等机构的研究人员撰写，发表于 ACL 2026。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在提升大语言模型（LLM）推理能力的强化学习（如 GRPO 算法）中，模型往往会出现“过度自信”现象，即错误回答的困惑度（Perplexity）反而低于正确回答。这导致模型的**相对校准度（Relative Calibration）**严重下降，使得模型在需要根据置信度进行决策的场景（如拒绝回答、推理时缩放）中表现不佳。
*   **研究背景**：现有的校准优化方法通常是启发式的，缺乏理论保证，且往往以牺牲推理准确率为代价。本文旨在从理论上解释校准退化的原因，并提出一种能同时优化准确率和校准度的算法。

### 2. 方法论：校准感知策略优化（CAPO）
*   **核心思想**：将校准度指标（AUC）的优化直接整合进策略梯度目标中。
*   **关键技术细节**：
    *   **理论证明**：作者证明了 GRPO 这种仅基于奖励（Reward-only）的优势估计等同于使用了一种“不一致”的 AUC 代理损失，导致梯度方向与校准目标失配。
    *   **一致性代理损失**：引入 **Logistic AUC 代理损失**。该损失函数在理论上是 AUC 一致的，并具有明确的遗憾界（Regret Bound），确保了优化该损失能真正提升 AUC。
    *   **不确定性感知优势估计**：重新定义优势函数 $\hat{A}_i$，使其不仅取决于奖励，还取决于正确与错误样本之间的困惑度差异。
    *   **参考模型噪声掩码（Denoising Mask）**：利用基础模型（参考模型）的困惑度来过滤噪声样本。例如，排除掉那些困惑度极高但结果正确的“碰巧猜对”样本，以及困惑度极低但结果错误的“近乎正确”样本，以稳定训练动态。

### 3. 实验设计
*   **模型**：Qwen2.5-Math-1.5B 和 Qwen2.5-Math-7B。
*   **数据集**：训练集采用 DeepScaler 的 20k 样本；测试集涵盖 6 个数学推理基准：AIME 2024、AIME 2025、MATH 500、AMC 2023、Minerva 和 Olympiad-Bench。
*   **对比方法（Baselines）**：
    *   推理优化类：GRPO、GSPO。
    *   校准改进类：CDE、CoDaPO、SimKO。
*   **评估指标**：准确率（Mean@16）、校准度（AUC-mean）、幻觉缓解（精确率-覆盖率曲线）、推理时缩放准确率。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练时长**：1.5B 模型训练至收敛约需 **24 小时**；7B 模型约需 **48 小时**。
*   **框架**：基于 `verl` 强化学习框架实现。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模和 6 个主流基准上进行了全面测试，实验组数充足。
*   **消融实验**：针对超参数 $\tau$ 的敏感性、噪声掩码机制的有效性、以及仅使用掩码而不改变损失函数的效果进行了对比。
*   **客观性**：通过展示训练过程中的动态曲线（AUC 随 Step 的变化），证明了 CAPO 的提升是持续且稳定的，而非特定 Checkpoint 的偶然结果。

### 6. 主要结论与发现
*   **校准度显著提升**：CAPO 在 1.5B 模型上将 AUC 提升了约 15%，在 7B 模型上最高提升了 25%。
*   **准确率保持或超越**：在显著提升校准度的同时，CAPO 的推理准确率与 GRPO 持平，甚至在 AIME 等困难任务上表现更优。
*   **下游任务增益**：在推理时缩放（Inference-time Scaling）任务中，CAPO 相比 GRPO 提升了约 5% 的准确率；在幻觉缓解任务中，实现了帕累托最优的精确率-覆盖率权衡。

### 7. 优点
*   **理论扎实**：不仅提出了算法，还从数学上证明了现有算法的缺陷及新算法的一致性。
*   **协同优化**：打破了“校准度提升必损准确率”的固有印象，实现了两者的双赢。
*   **低开销**：不需要额外的价值模型（Value Model）或复杂的采样策略，训练效率高。
*   **实用性强**：直接提升了模型在现实场景（如需要模型“知之为知之”）中的可靠性。

### 8. 不足与局限
*   **任务领域局限**：实验主要集中在数学推理（RLVR 场景），虽然这是目前推理模型的核心战场，但在逻辑谜题、常识推理或开放域问答上的泛化性仍需进一步验证。
*   **超参数依赖**：虽然消融实验显示其对 $\tau$ 和掩码阈值具有一定的鲁棒性，但在不同分布的数据集上可能仍需微调这些参数以达到最优效果。

（完）
