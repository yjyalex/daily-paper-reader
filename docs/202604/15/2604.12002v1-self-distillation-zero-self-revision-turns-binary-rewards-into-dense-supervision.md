---
title: "Self-Distillation Zero: Self-Revision Turns Binary Rewards into Dense Supervision"
title_zh: 自蒸馏 Zero：通过自我修正将二元奖励转化为密集监督
authors: "Yinghui He, Simran Kaur, Adithya Bhaskar, Yongjin Yang, Jiarui Liu, Narutatsu Ri, Liam Fowl, Abhishek Panigrahi, Danqi Chen, Sanjeev Arora"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12002v1"
tags: ["query:token"]
score: 9.0
evidence: 从二元奖励中获取稠密Token级监督
tldr: 针对强化学习中二元奖励监督稀疏以及蒸馏法依赖外部教师的问题，本文提出Self-Distillation Zero (SD-Zero)。该方法让单一模型同时扮演生成器和修订器，修订器根据初始回答及奖励生成改进版，再通过在线自蒸馏将修订器的Token级分布传回生成器。实验证明SD-Zero在数学和代码任务上显著优于GRPO等基线，实现了高效的自我进化。
selection_source: fresh_fetch
motivation: 旨在解决强化学习中二元奖励监督过于稀疏，以及传统蒸馏方法过度依赖昂贵外部教师或高质量数据的问题。
method: 提出一种自修订机制，通过将修订器基于奖励生成的Token级分布蒸馏给生成器，将稀疏的二元奖励转化为密集的自监督信号。
result: "在数学和代码基准测试中，SD-Zero比基础模型性能提升超过10%，且在同等预算下优于RFT、GRPO和SDFT等强基线。"
conclusion: SD-Zero证明了模型具备自我定位错误并迭代进化的能力，是一种比传统强化学习更具样本效率的后训练方法。
---

## 摘要
当前在可验证场景下的后训练方法主要分为两类。强化学习（RLVR）依赖于二元奖励，这种方法适用性广且功能强大，但在训练过程中仅提供稀疏监督。蒸馏则提供密集的词元（token）级别监督，通常从外部教师模型或通过高质量演示获得。然而，收集此类监督信息的成本高昂，甚至无法获取。我们提出了自蒸馏 Zero（SD-Zero），该方法的训练样本效率显著高于强化学习，且无需外部教师模型或高质量演示。SD-Zero 训练单个模型扮演两个角色：生成器（Generator）负责生成初始回答，修正器（Reviser）则根据该回答及其二元奖励生成改进后的回答。随后，我们进行同策略自蒸馏，利用修正器在给定生成器回答及奖励条件下的词元分布作为监督信号，将其蒸馏回生成器。实际上，SD-Zero 训练模型将二元奖励转化为密集的词元级自我监督。在 Qwen3-4B-Instruct 和 Olmo-3-7B-Instruct 的数学和代码推理基准测试中，在相同的题目集和训练样本预算下，SD-Zero 的性能比基础模型提升了至少 10%，且优于拒绝微调（RFT）、GRPO 和自蒸馏微调（SDFT）等强基准模型。广泛的消融实验展示了该算法的两个新特性：(a) 词元级自我定位，即修正器能根据奖励识别生成器回答中需要修改的关键词元；(b) 迭代自我演化，即通过定期的教师模型同步，不断提升的修正能力可以被蒸馏回生成性能中。

## Abstract
Current post-training methods in verifiable settings fall into two categories. Reinforcement learning (RLVR) relies on binary rewards, which are broadly applicable and powerful, but provide only sparse supervision during training. Distillation provides dense token-level supervision, typically obtained from an external teacher or using high-quality demonstrations. Collecting such supervision can be costly or unavailable. We propose Self-Distillation Zero (SD-Zero), a method that is substantially more training sample-efficient than RL and does not require an external teacher or high-quality demonstrations. SD-Zero trains a single model to play two roles: a Generator, which produces an initial response, and a Reviser, which conditions on that response and its binary reward to produce an improved response. We then perform on-policy self-distillation to distill the reviser into the generator, using the reviser's token distributions conditioned on the generator's response and its reward as supervision. In effect, SD-Zero trains the model to transform binary rewards into dense token-level self-supervision. On math and code reasoning benchmarks with Qwen3-4B-Instruct and Olmo-3-7B-Instruct, SD-Zero improves performance by at least 10% over the base models and outperforms strong baselines, including Rejection Fine-Tuning (RFT), GRPO, and Self-Distillation Fine-Tuning (SDFT), under the same question set and training sample budget. Extensive ablation studies show two novel characteristics of our proposed algorithm: (a) token-level self-localization, where the reviser can identify the key tokens that need to be revised in the generator's response based on reward, and (b) iterative self-evolution, where the improving ability to revise answers can be distilled back into generation performance with regular teacher synchronization.

---

## 论文详细总结（自动生成）

### 论文总结：Self-Distillation Zero (SD-Zero)

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在数学和代码等推理任务中的**后训练（Post-training）效率问题**展开研究。
*   **研究动机**：当前的强化学习（如 RLVR/GRPO）依赖二元奖励（对/错），这种监督信号极其稀疏，导致训练样本效率低下；而传统的蒸馏方法虽然提供密集的词元级（token-level）监督，但通常需要昂贵的外部教师模型或高质量的人工演示。
*   **核心问题**：模型能否在仅有二元奖励的情况下，通过观察自己（可能错误）的尝试进行自我修正，并将这种修正能力转化为密集的词元级自监督信号？

#### 2. 方法论
SD-Zero 提出了一种将单一模型同时作为“生成器（Generator）”和“修订器（Reviser）”的架构，分为两个阶段：
*   **阶段 1：自我修正训练 (SRT)**
    *   **数据收集**：让模型对同一个问题生成初始回答，根据二元奖励（正确/错误）添加控制提示词（如“回答错误，让我重来”或“让我重述”），并保留那些修正后结果正确的轨迹。
    *   **训练目标**：通过混合损失函数（修正损失 + 生成损失）进行微调，使模型学会根据反馈进行自我修正。
*   **阶段 2：在线自蒸馏 (Self-Distillation)**
    *   **核心思想**：将 SRT 后的模型作为“教师（修订器）”，当前训练中的模型作为“学生（生成器）”。
    *   **流程**：学生生成一个回答，教师根据该回答及其二元奖励，提供一个改进后的词元分布。学生通过最小化与教师分布之间的 KL 散度来学习。
    *   **效果**：这实际上是将稀疏的二元奖励转化为了密集的词元级监督，使模型能够“内化”修正行为，直接生成更高质量的回答。

#### 3. 实验设计
*   **模型**：Qwen3-4B-Instruct 和 Olmo-3-7B-Instruct。
*   **训练数据集**：OpenR1-Math（15K 题目）和 Codeforces（15K 题目）。
*   **评测基准 (Benchmarks)**：
    *   数学：AIME24/25, HMMT25, AMOBench, OpenR1-Math, MATH。
    *   代码：Codeforces, LiveCodeBench。
*   **对比方法**：
    *   **SFT**：基于 DeepSeek-R1 高质量演示的监督微调。
    *   **RFT**：拒绝微调（仅在模型生成的正确样本上训练）。
    *   **GRPO**：一种主流的强化学习算法（使用二元奖励）。
    *   **SDFT**：基于外部演示的在线自蒸馏。

#### 4. 资源与算力
*   **算力配置**：
    *   **GRPO 基线**：使用了 8 张 GPU 进行训练。
    *   **SD-Zero 自蒸馏阶段**：使用了 4 张 GPU。
    *   **推理引擎**：使用 vLLM 进行高效采样。
*   **训练细节**：采用 bfloat16 精度，使用了 FSDP（完全分片数据并行）和 Liger Kernel 优化。具体 GPU 型号（如 A100 或 H100）未在文中直接标明，但从配置推断为高性能计算集群。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同架构的模型系列（Qwen 和 Olmo）上进行了验证，涵盖了 8 个主流的数学和代码基准测试。
*   **消融实验**：非常充分。作者研究了 SRT 损失项的影响、数据分配比例、无 SRT 阶段的效果，以及教师模型同步（迭代进化）的潜力。
*   **公平性**：所有对比方法均在**相同的题目集**和**相同的采样预算**（约 60K 次生成）下进行，确保了样本效率对比的客观性。

#### 6. 主要结论与发现
*   **性能卓越**：SD-Zero 在数学和代码任务上比基础模型提升了 10% 以上，且在同等预算下显著优于 GRPO 和 RFT。
*   **词元级自我定位**：修订器能够精准识别初始回答中导致错误的关键词元，并针对性地给出高奖励信号。
*   **推理效率提升**：经过自蒸馏后，模型学会了“内化”修正逻辑，生成的回答比 SRT 阶段更简洁（长度缩短约 2 倍），且准确率更高。
*   **迭代进化**：通过定期将学生模型同步为新的教师模型，性能可以持续增长，展现了自我进化的潜力。

#### 7. 优点
*   **无需外部教师**：完全摆脱了对 DeepSeek-R1 或 GPT-4 等更强模型的依赖，实现了真正的“零外部监督”进化。
*   **样本效率高**：相比于需要大量采样探索的 RL 方法，SD-Zero 通过密集的词元级反馈更快地收敛。
*   **内化修正行为**：不同于需要多轮对话的 Self-Correction 提示词方法，SD-Zero 让模型在单次生成中就能避开陷阱。

#### 8. 不足与局限
*   **领域限制**：目前仅在数学和代码等具有**可验证二元奖励**（Verifiable Rewards）的领域有效，对于主观性强、缺乏自动评判标准的任务（如创意写作）尚待验证。
*   **思维模型挑战**：在初步尝试应用于“长思维链（Thinking Models）”时，发现如果训练时开启思维模式，性能反而可能下降，说明该方法在处理极其冗长的探索性推理时仍有优化空间。
*   **初始能力依赖**：模型需要具备一定的基础能力才能通过 SRT 阶段产生有效的修正轨迹。

（完）
