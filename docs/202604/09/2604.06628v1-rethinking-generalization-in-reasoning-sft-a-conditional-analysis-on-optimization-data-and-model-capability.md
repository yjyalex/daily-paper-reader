---
title: "Rethinking Generalization in Reasoning SFT: A Conditional Analysis on Optimization, Data, and Model Capability"
title_zh: 重新审视推理 SFT 中的泛化：关于优化、数据和模型能力的条件分析
authors: "Qihan Ren, Peng Wang, Ruikun Cai, Shuai Shao, Dadi Guo, Yuejin Xie, Yafu Li, Quanshi Zhang, Xia Hu, Jing Shao, Dongrui Liu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06628v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: LLM推理泛化中强化学习与监督微调的对比
tldr: 本研究重新审视了长思维链（CoT）监督微调（SFT）中的泛化问题，挑战了“SFT仅靠记忆而RL才能泛化”的传统观点。研究发现，跨领域泛化受优化动态、数据质量和模型能力共同影响。通过分析发现，优化过程中存在“先降后升”的恢复模式，且高质量验证的CoT数据能显著提升泛化。强模型能内化推理模式，而弱模型仅模仿表层形式。此外，推理能力的提升伴随着安全性的下降。
selection_source: fresh_fetch
motivation: 挑战“SFT仅能实现记忆而无法像RL那样实现推理泛化”的普遍认知，探究SFT泛化的真实条件。
method: 通过对优化动态、训练数据结构及模型基础能力进行条件分析，系统研究长CoT监督微调下的跨领域泛化表现。
result: 发现优化中存在“下降再恢复”模式，且高质量CoT数据和强基础模型是实现推理模式内化而非表层模仿的关键。
conclusion: 推理SFT的泛化是条件性的且具有不对称性，在提升推理能力的同时可能导致模型安全性下降。
---

## 摘要
大语言模型（LLM）后训练中流行的一种观点认为，有监督微调（SFT）倾向于记忆，而强化学习（RL）则能实现泛化。我们针对带有长思维链（CoT）监督的推理 SFT 重新审视了这一主张，发现跨领域泛化并非不存在，而是有条件的，由优化动态、训练数据和基座模型能力共同塑造。一些报道的失败案例实际上是优化不足的产物：跨领域性能在随延长训练恢复并提升之前会先下降（呈现出一种“先降后升”的模式），因此短时间训练的检查点可能会低估泛化能力。数据质量和结构都至关重要：低质量的解法会广泛损害泛化，而经过验证的长 CoT 轨迹则能带来稳定的跨领域收益。模型能力是核心：更强大的模型即使从简单的算术游戏中也能内化可迁移的程序性模式（如回溯），而较弱的模型则只是模仿表面的冗长表述。然而，这种泛化是不对称的：推理能力提升的同时安全性却在下降，这使得问题从“推理 SFT 是否能泛化”转变为“在什么条件下以及以什么为代价实现泛化”。

## Abstract
A prevailing narrative in LLM post-training holds that supervised finetuning (SFT) memorizes while reinforcement learning (RL) generalizes. We revisit this claim for reasoning SFT with long chain-of-thought (CoT) supervision and find that cross-domain generalization is not absent but conditional, jointly shaped by optimization dynamics, training data, and base-model capability. Some reported failures are under-optimization artifacts: cross-domain performance first degrades before recovering and improving with extended training (a dip-and-recovery pattern), so shorttraining checkpoints can underestimate generalization. Data quality and structure both matter: low-quality solutions broadly hurt generalization,while verified long-CoT traces yield consistent cross-domain gains. Model capability is essential: stronger models internalize transferable procedural patterns (e.g., backtracking) even from a toy arithmetic game, while weaker ones imitate surface verbosity. This generalization is asymmetric, however: reasoning improves while safety degrades, reframing the question from whether reasoning SFT generalizes to under what conditions and at what cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Generalization in Reasoning SFT: A Conditional Analysis on Optimization, Data, and Model Capability》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前大模型后训练领域存在一种流行叙事，即“监督微调（SFT）只会导致记忆，而强化学习（RL）才能实现泛化”。这种观点驱动了大量算法转向 RL 或修改 SFT 目标函数。
*   **核心问题**：论文质疑上述结论是否具有普适性。作者认为，推理 SFT 的泛化失败可能并非 SFT 目标函数本身的问题，而是由于实验条件（优化不足、数据质量差、模型能力弱）导致的“假象”。
*   **整体含义**：通过系统性的条件分析，论文证明了在长思维链（CoT）场景下，SFT 同样具备强大的跨领域泛化能力，但这种能力是**有条件的**。

### 2. 方法论
论文提出了一个**条件分析框架**，从三个维度研究泛化：
*   **优化动态（Optimization Dynamics）**：不仅关注最终结果，还追踪整个训练轨迹。发现跨领域性能存在“先降后升”（Dip-and-recovery）的非单调模式。
*   **数据质量与结构（Data Quality & Structure）**：
    *   对比长 CoT（包含思考过程）与 No-CoT（仅有步骤和答案）。
    *   对比高质量验证数据与低质量混合数据（如 NuminaMath 的原始子集）。
    *   引入“程序性泛化”实验：使用极简的算术游戏（Countdown）数据，观察其逻辑模式（如回溯、验证）能否迁移至复杂数学和科学任务。
*   **模型能力（Model Capability）**：在相同数据和优化条件下，对比不同参数规模（1.7B 至 20B）模型的泛化表现。

### 3. 实验设计
*   **基座模型**：Qwen3 (1.7B, 4B, 8B, 14B), Qwen2.5 (1.5B, 3B, 7B, 14B), InternLM2.5-20B。均使用预训练 Base 版本以排除对齐干扰。
*   **数据集**：
    *   **Math-CoT-20k**：由 Qwen3-32B 生成并经过正确性验证的长 CoT 数据。
    *   **对比组**：Math-NoCoT（删除思考过程）、NuminaMath-20k（人类撰写，质量参差不齐）、Countdown-CoT（纯算术游戏）。
*   **Benchmark 体系**：
    *   **领域内（ID）推理**：MATH500, AIME24。
    *   **跨领域（OOD）推理**：LiveCodeBench v2 (代码), GPQA-Diamond (研究生级科学), MMLU-Pro。
    *   **通用能力**：IFEval (指令遵循), AlpacaEval 2.0 (开放域质量), HaluEval (幻觉), TruthfulQA (真实性)。
    *   **安全性**：HEx-PHI (有害指令攻击)。

### 4. 资源与算力
*   **硬件环境**：训练使用 **8 张 NVIDIA H200 GPU**；评估使用 **2 张 NVIDIA H200 GPU**。
*   **训练框架**：基于 Verl 项目的 SFT trainer。
*   **超参数**：AdamW 优化器，学习率 5e-5，Batch Size 256，默认训练 8 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   跨 3 个模型家族、9 种不同规模模型的横向对比。
    *   针对学习率、Epoch 数、Batch Size 的多种优化组合测试。
    *   针对数据质量和结构的消融实验。
    *   长达 16 个 Epoch 的“压力测试”以观察过拟合症状。
*   **充分性评价**：实验设计非常严谨且客观。通过使用 Base 模型作为起点，有效地隔离了变量；通过追踪中间检查点（Checkpoints），揭示了被前人忽视的动态演变过程。

### 6. 主要结论与发现
*   **泛化的“先降后升”模式**：在长 CoT 训练中，跨领域性能通常先下降再恢复。短轮次训练的检查点会严重低估 SFT 的泛化潜力。
*   **响应长度是诊断指标**：模型通常先学会“变长”（模仿表层冗长形式），随着优化深入，长度会收缩至稳定状态，此时真正的推理模式才被内化。
*   **程序性模式的迁移**：即使是简单的 Countdown 游戏，只要包含长 CoT，也能提升模型在科学和代码任务上的表现，证明模型学到的是“回溯、验证”等通用程序，而非单纯的内容。
*   **能力门槛效应**：强模型（如 14B）能内化模式实现泛化，而弱模型（如 1.7B）往往止步于模仿表层的冗长，导致泛化失败。
*   **不对称泛化（安全性代价）**：推理能力的提升伴随着安全性的显著下降。模型学会了在“思考”中自我合理化（Self-rationalize），从而绕过安全护栏。

### 7. 优点
*   **挑战直觉**：有力地反驳了“SFT 只能记忆”的片面观点，为后训练策略提供了新视角。
*   **动态视角**：通过全生命周期的曲线分析，解释了为什么之前的研究会得出“SFT 不泛化”的错误结论。
*   **深度分析**：不仅给出了分数，还通过 Token 概率分析和案例研究，深入探讨了模型到底学到了什么（如 transition tokens 的掌握）。

### 8. 不足与局限
*   **领域覆盖**：实验主要基于数学推理数据，虽然测试了 OOD 泛化，但尚未验证在代码或法律等其他专业领域作为起始训练数据时的表现。
*   **模型规模限制**：实验最高只覆盖到 20B 参数，对于 70B 以上或超大规模 MoE 模型的动态是否一致尚待验证。
*   **缺乏与 RL 的直接对等对比**：虽然论文解释了 SFT 的泛化，但未在完全相同的算力预算下与最先进的 RL 算法（如 PPO/GRPO）进行“苹果对苹果”的效率对比。

（完）
