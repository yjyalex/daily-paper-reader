---
title: "RAGEN-2: Reasoning Collapse in Agentic RL"
title_zh: RAGEN-2：智能体强化学习中的推理崩溃
authors: "Zihan Wang, Chi Gui, Xing Jin, Qineng Wang, Licheng Liu, Kangrui Wang, Shiqi Chen, Linjie Li, Zhengyuan Yang, Pingyue Zhang, Yiping Lu, Jiajun Wu, Li Fei-Fei, Lijuan Wang, Yejin Choi, Manling Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06268v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 多轮LLM智能体的强化学习训练与推理质量
tldr: 本研究揭示了多轮LLM智能体在强化学习中存在“模板崩溃”现象，即模型生成看似多样但与输入无关的固定推理模板。为解决此问题，作者提出了互信息（MI）作为衡量推理跨输入区分度的新指标，并基于信噪比（SNR）机制开发了SNR感知过滤技术。实验证明，该方法能有效识别并缓解推理崩溃，在规划、数学和Web导航等多种任务中显著提升了模型的性能和推理质量。
selection_source: fresh_fetch
motivation: 现有的熵指标无法识别模型在RL训练中产生的与输入无关的固定推理模板，导致无法准确评估推理质量。
method: 引入互信息作为衡量推理区分度的指标，并提出SNR感知过滤机制，通过奖励方差筛选高信号提示词来优化训练梯度。
result: 互信息与任务性能表现出极强的相关性，且SNR感知过滤在规划、数学、Web导航和代码执行等任务中均取得了显著提升。
conclusion: 互信息是评估智能体推理质量的关键指标，通过优化训练过程中的信噪比可以有效防止推理崩溃并增强模型的输入依赖性。
---

## 摘要
多轮大语言模型（LLM）智能体的强化学习（RL）训练本质上是不稳定的，推理质量直接决定了任务性能。熵被广泛用于追踪推理稳定性。然而，熵仅衡量同一输入内的多样性，无法判断推理是否真正响应了不同的输入。在 RAGEN-2 中，我们发现即使熵保持稳定，模型也可能依赖于看起来多样但与输入无关的固定模板。我们将这种现象称为模板崩溃（template collapse），这是一种熵和所有现有指标都无法察觉的失败模式。为了诊断这一失败，我们将推理质量分解为输入内多样性（熵）和跨输入可区分性（互信息，MI），并引入了一系列互信息代理指标用于在线诊断。在各种任务中，互信息与最终性能的相关性远强于熵，使其成为更可靠的推理质量代理指标。我们进一步利用信噪比（SNR）机制解释了模板崩溃：低奖励方差会削弱任务梯度，使得正则化项占据主导地位并抹除跨输入的推理差异。为解决这一问题，我们提出了 SNR 感知过滤（SNR-Aware Filtering），利用奖励方差作为轻量级代理，在每次迭代中选择高信号提示词。在规划、数学推理、网页导航和代码执行等任务中，该方法一致地提高了输入依赖性和任务性能。

## Abstract
RL training of multi-turn LLM agents is inherently unstable, and reasoning quality directly determines task performance. Entropy is widely used to track reasoning stability. However, entropy only measures diversity within the same input, and cannot tell whether reasoning actually responds to different inputs. In RAGEN-2, we find that even with stable entropy, models can rely on fixed templates that look diverse but are input-agnostic. We call this template collapse, a failure mode invisible to entropy and all existing metrics. To diagnose this failure, we decompose reasoning quality into within-input diversity (Entropy) and cross-input distinguishability (Mutual Information, MI), and introduce a family of mutual information proxies for online diagnosis. Across diverse tasks, mutual information correlates with final performance much more strongly than entropy, making it a more reliable proxy for reasoning quality. We further explain template collapse with a signal-to-noise ratio (SNR) mechanism. Low reward variance weakens task gradients, letting regularization terms dominate and erase cross-input reasoning differences. To address this, we propose SNR-Aware Filtering to select high-signal prompts per iteration using reward variance as a lightweight proxy. Across planning, math reasoning, web navigation, and code execution, the method consistently improves both input dependence and task performance.

---

## 论文详细总结（自动生成）

### RAGEN-2: 智能体强化学习中的推理崩溃——论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：在多轮大语言模型（LLM）智能体的强化学习（RL）训练中，推理过程的稳定性至关重要。目前研究者通常使用“熵（Entropy）”来监控推理的多样性。
*   **核心问题**：论文发现了一种名为**“模板崩溃（Template Collapse）”**的隐蔽失败模式。即模型生成的推理链虽然在单个输入下看起来很多样（高熵），但实际上对于不同的输入，模型都在套用相同的固定模板（低互信息）。
*   **研究动机**：现有的熵指标无法识别这种“与输入无关”的推理退化。论文旨在提出一种新的诊断工具和改进机制，以确保智能体在RL训练中能够生成真正依赖于输入的、高质量的推理。

#### 2. 方法论
*   **核心思想**：将推理质量分解为**输入内多样性（熵）**和**跨输入区分度（互信息，MI）**。通过提升训练过程中的信噪比（SNR），防止正则化项抹除推理的输入依赖性。
*   **关键技术细节**：
    *   **互信息代理指标（MI Proxies）**：提出“批次内交叉评分（In-Batch Cross-Scoring）”方法。将一个输入产生的推理轨迹在当前批次的所有输入下进行评分，计算“匹配评分”与“边缘评分”的差异。主要指标包括：
        *   **Retrieval-Acc**：衡量能否通过推理轨迹准确检索出其对应的原始输入。
        *   **MI-ZScore-EMA**：一种连续且鲁棒的互信息估计值，用于在线监控。
    *   **SNR机制解释**：论文证明任务梯度的大小受限于奖励方差（Reward Variance, RV）。当RV较低时，任务信号微弱，而输入无关的正则化梯度（如KL散度、熵奖励）保持不变，导致更新方向被噪声主导。
    *   **SNR感知过滤（SNR-Aware Filtering）**：在每次训练迭代中，计算每个提示词（Prompt）下多个轨迹的奖励方差。采用类似核采样（Nucleus Sampling）的 **Top-p 过滤**，仅保留奖励方差总和前 $p$ 比例的高信号提示词参与参数更新。

#### 3. 实验设计
*   **实验场景与数据集**：涵盖了7个具有挑战性的环境：
    *   **规划类**：Sokoban（推箱子，不可逆规划）。
    *   **随机导航类**：FrozenLake（稀疏奖励，随机转移）。
    *   **数学推理类**：MetaMathQA、Countdown（二十四点游戏）。
    *   **综合智能体类**：SearchQA（多轮搜索）、WebShop（网页购物导航）、DeepCoder（代码执行）。
*   **对比方法（Baselines）**：
    *   **算法对比**：PPO、DAPO、GRPO、Dr. GRPO。
    *   **模型对比**：Qwen2.5 (0.5B, 1.5B, 3B, 7B)、Llama3.2-3B。
    *   **多模态对比**：Qwen2.5-VL-3B（文本与图像输入）。
*   **评估指标**：任务成功率、互信息代理指标、推理熵、推理长度等。

#### 4. 资源与算力
*   **框架与硬件**：使用了 **veRL/HybridFlow** 分布式强化学习框架。
*   **算力细节**：文中提到在 **NVIDIA GPU** 上进行训练，但未明确给出具体的 GPU 型号（如 A100 或 H100）及总卡数。
*   **训练时长**：实验通常运行 200 到 400 个迭代步（Rollout-Update Iterations）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的网格搜索和消融实验，覆盖了不同的模型规模（从 0.5B 到 7B）、不同的 RL 算法、以及纯文本和多模态输入。
*   **充分性评价**：实验设计非常充分且客观。作者不仅在多个任务上验证了方法的有效性，还通过“四分位数消融（Quartile Ablation）”和“受控噪声注入”等手段，从因果关系上验证了 SNR 机制的正确性。

#### 6. 主要结论与发现
*   **MI 是更好的监控指标**：互信息与任务性能的相关性（Spearman 相关系数约 0.39）远高于熵（相关系数为负或接近 0）。
*   **模板崩溃是普遍存在的**：在没有干预的情况下，RL 训练往往会导致推理链向固定模板靠拢，且这一过程早于任务性能的下降。
*   **SNR 过滤效果显著**：SNR 感知过滤在不增加额外计算成本（通过重新分配 Rollout 预算）的情况下，一致地提升了所有任务的成功率和推理的输入依赖性。

#### 7. 优点
*   **理论深刻**：从信息论和梯度分解的角度深入解释了 RL 训练中推理退化的本质原因。
*   **诊断工具实用**：提出的 MI 代理指标无需额外模型，仅利用训练过程中的 log-prob 即可计算，具有极高的实用价值。
*   **即插即用**：SNR 过滤机制简单有效，可以轻松集成到现有的 PPO/GRPO 流程中。

#### 8. 不足与局限
*   **奖励依赖性**：该方法依赖于奖励方差作为信号代理。在奖励极其稀疏或环境噪声极大的极端情况下，奖励方差可能不再是可靠的信号指标。
*   **超参数敏感性**：过滤比例 $p$ 可能需要针对不同任务进行微调，过度过滤可能会限制模型的探索能力。
*   **单智能体限制**：目前研究集中在单智能体设置，尚未探讨模板崩溃在多智能体博弈环境中的传播规律。

（完）
