---
title: Synthetic Sandbox for Training Machine Learning Engineering Agents
title_zh: 用于训练机器学习工程智能体的合成沙盒
authors: "Yuhang Zhou, Lizhu Zhang, Yifan Wu, Jiayi Liu, Xiangjun Fan, Zhuokai Zhao, Hong Yan"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04872v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对机器学习工程智能体的在线强化学习
tldr: 针对机器学习工程（MLE）智能体训练中验证成本极高的问题，本文提出SandMLE框架。该框架通过多智能体协作，将真实任务转化为保持复杂度但仅含微量数据的合成沙盒环境，使执行速度提升13倍以上，首次实现了MLE领域的在线强化学习。实验证明，该方法显著提升了模型在MLE-bench等基准上的表现，并具有良好的泛化性。
selection_source: fresh_fetch
motivation: 传统的MLE任务验证依赖大规模数据和完整流水线，导致在线强化学习因执行速度过慢而难以实施。
method: 提出SandMLE多智能体框架，通过种子任务生成保留结构复杂度但数据集极小的合成MLE沙盒环境。
result: "训练效率提升13倍以上，在Qwen系列模型上相比SFT基线提升了20.3%至66.9%的奖牌率，并展现出跨架构的泛化能力。"
conclusion: 通过缩减沙盒数据规模，SandMLE成功克服了MLE智能体训练的效率瓶颈，为大规模在线强化学习在复杂工程领域的应用铺平了道路。
---

## 摘要
随着大语言模型智能体从软件工程（SWE）任务向机器学习工程（MLE）任务演进，验证智能体行为的成本呈数量级增长：虽然 SWE 任务可以通过快速执行的单元测试进行验证，但 MLE 验证需要在每个采样步骤中在大型数据集上运行完整的机器学习流水线（包括数据预处理、模型训练和指标评估），这使得基于轨迹的在线强化学习（on-policy RL）变得极其缓慢。现有方法往往退而求其次，采用监督微调（SFT）或离线代理奖励，从而牺牲了在线强化学习在探索和泛化方面的优势。我们观察到，沙盒数据规模是这一瓶颈的主要来源。基于这一洞察，我们提出了 SandMLE，这是一个多智能体框架，能够从少量种子任务中生成多样化且可验证的合成 MLE 环境，在保留现实世界问题结构和技术复杂性的同时，将数据集限制在微观规模（每个任务仅配有 50-200 个训练样本）。通过广泛的实验，我们证明 SandMLE 将执行时间缩短了 13 倍以上，首次在 MLE 领域实现了大规模、基于轨迹的在线强化学习。在 MLE-bench-lite 上，SandMLE 在 Qwen3-8B、14B 和 30B-A3B 模型上均比 SFT 基准取得了显著提升，相对奖牌率提升幅度在 20.3% 到 66.9% 之间。此外，训练后的策略可以泛化到未见的智能体脚手架，在 MLE-Dojo 上的 HumanRank 评分提升高达 32.4%。

## Abstract
As large language model agents advance beyond software engineering (SWE) tasks toward machine learning engineering (MLE), verifying agent behavior becomes orders of magnitude more expensive: while SWE tasks can be verified via fast-executing unit tests, MLE verification requires running full ML pipelines -- data preprocessing, model training, and metric evaluation -- on large datasets at each rollout step, rendering trajectory-wise on-policy reinforcement learning (RL) prohibitively slow. Existing approaches retreat to supervised fine-tuning (SFT) or offline proxy rewards, sacrificing the exploration and generalization benefits of on-policy RL. We observe that sandbox data size is the primary source of this bottleneck. Based on this insight, we introduce SandMLE, a multi-agent framework that generates diverse, verifiable synthetic MLE environments from a small number of seed tasks, preserving the structural and technical complexity of real-world problems while constraining datasets to micro-scale (each task is paired with only 50-200 training samples). Through extensive experiments, we show that SandMLE reduces execution time by over 13 times, enabling large-scale, on-policy trajectory-wise RL for the first time in the MLE domain. On MLE-bench-lite, SandMLE yields significant gains over SFT baselines across Qwen3-8B, 14B, and 30B-A3B, with relative medal rate improvements ranging from 20.3% to 66.9%. Furthermore, the trained policy generalizes across unseen agentic scaffolds, achieving up to 32.4% better HumanRank score on MLE-Dojo.

---

## 论文详细总结（自动生成）

这篇论文由 Meta AI 团队提出，旨在解决机器学习工程（MLE）智能体在强化学习训练中面临的效率瓶颈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：训练能够处理复杂机器学习工程（MLE）任务的智能体时，**验证成本极高**。与软件工程（SWE）任务几秒钟的单元测试不同，MLE 任务需要运行完整的数据预处理、模型训练和评估流程，单次代码执行平均耗时超过 200 秒。
*   **研究动机**：由于执行速度极慢，现有的 MLE 智能体训练大多局限于监督微调（SFT）或离线奖励，无法利用**在线强化学习（On-policy RL）**带来的探索和泛化优势。
*   **整体含义**：论文发现 MLE 验证的延迟主要源于数据集规模。通过构建微缩版（Micro-scale）但保留复杂逻辑的合成沙盒，可以大幅提升训练效率，使大规模在线强化学习在 MLE 领域成为可能。

### 2. 方法论：核心思想与关键技术
**SandMLE** 框架的核心思想是：**利用多智能体协作自动生成“结构复杂但数据微小”的合成 MLE 任务。**

*   **多智能体生成流水线**：
    1.  **数据策略师 (Data Strategist)**：从真实种子任务中提取“结构 DNA”（如特征分布、任务类型），并将其映射到新的行业场景，注入噪声并定义复杂的数学隐藏规则。
    2.  **MLE 开发员 (MLE Developer)**：编写代码生成 50-200 个样本的微型数据集，并根据隐藏规则建立基准模型以确定难度阈值。
    3.  **MLOps 工程师 (MLOps Engineer)**：构建自动化的评分沙盒，确保评估指标和阈值的确定性。
    4.  **技术作家 (Technical Writer)**：合成最终的任务说明文档，确保其与生成的代码和数据完全对齐。
*   **关键技术细节**：
    *   **轨迹级 GRPO (Trajectory-wise GRPO)**：采用组相对策略优化算法，在整个交互轨迹（多轮对话）结束后根据最终结果分配奖励。
    *   **里程碑奖励函数 (Milestone-based Reward)**：为了缓解奖励稀疏问题，设计了包含格式奖励、执行奖励和分级性能奖励（如达到中位数、铜牌、银牌、金牌标准）的稠密奖励函数。
    *   **环境一致性校验**：通过数学约束确保生成的任务阈值具有单调性，过滤掉逻辑错误的合成任务。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：基于 60 个 MLE-bench 种子任务生成的 848 个合成任务。
    *   **测试集**：**MLE-bench-lite**（22 个真实 Kaggle 任务）和 **MLE-Dojo**（62 个交互式 MLE 任务）。
*   **对比方法**：
    *   **Base**：原始的 Qwen3 系列模型（8B, 14B, 30B）。
    *   **Seed-SFT**：在由 Claude-4.5 生成的高质量种子任务轨迹上进行监督微调。
    *   **SandMLE**：直接在合成沙盒上进行强化学习。
    *   **SFT-SandMLE**：先进行 SFT 再进行强化学习。
    *   **闭源模型参考**：Claude-4.5-Sonnet, DeepSeek-V3, Gemini-2.5-Flash。
*   **评估指标**：有效提交率、奖牌率（金/银/铜）、HumanRank 评分。

### 4. 资源与算力
*   **GPU 型号**：使用 **NVIDIA H200** GPU。
*   **训练配置**：
    *   每个任务分配 1 块 H200。
    *   训练 100 个 step，batch size 为 16，组大小（group size）为 4。
    *   单步执行限时 90 秒（训练时），评估时限时 4 小时。
*   **效率提升**：平均执行时间从 196.17 秒降至 14.31 秒，**提速超过 13 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 30B 不同参数规模的模型，生成了近千个合成任务。
*   **充分性**：
    *   **消融实验**：对比了稠密奖励与稀疏奖励的效果，验证了里程碑设计的必要性。
    *   **泛化性测试**：在不同的智能体脚手架（ReAct, AIDE, AIRA, MLE-Agent）上进行了交叉验证。
    *   **测试时间缩放 (Test-time Scaling)**：分析了增加推理轮数对性能的影响。
*   **客观性**：通过在完全未见的真实 Kaggle 任务上测试，证明了合成数据训练出的模型具有真实世界的工程能力。

### 6. 主要结论与发现
*   **性能飞跃**：SandMLE 在 Qwen3-8B 上实现了 66.9% 的相对奖牌率提升，30B 模型在某些指标上接近 Claude-4.5。
*   **RL 优于 SFT**：单纯的 SFT 提升有限，而通过 SandMLE 进行的在线 RL 能显著增强模型处理长程、试错性任务的能力。
*   **跨架构泛化**：在合成沙盒中训练的模型，即使更换了推理时的脚手架（Scaffold），依然能保持性能优势。
*   **规模效应**：随着模型参数增加，模型学习格式规范和有效提交的能力显著增强，30B 模型无需 SFT 即可达到 100% 的有效提交率。

### 7. 优点
*   **创新性地解决了效率瓶颈**：通过“微缩数据集”这一简单而深刻的洞察，打破了 MLE 领域无法进行在线 RL 的僵局。
*   **全自动化流水线**：多智能体协作生成的任务多样性极高（涵盖医疗、零售、IT 等多领域，图像、文本、音频等多模态）。
*   **实战价值高**：不仅提升了模型分数，还增强了模型在面对错误时的自我修正和迭代能力。

### 8. 不足与局限
*   **上下文窗口限制**：实验发现，当推理轮数过多（超过 30 轮）时，受限于模型上下文长度，性能会出现下滑。
*   **合成与真实的差距**：虽然合成任务保留了数学逻辑，但真实世界中的数据清洗、极端异常值处理等“脏活”在微型沙盒中可能体现不足。
*   **算力门槛**：尽管提速 13 倍，但大规模轨迹采样仍需要昂贵的 H200 等顶级算力支持。

（完）
