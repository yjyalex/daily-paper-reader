---
title: "AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering"
title_zh: AgentForge：面向自主软件工程的基于执行验证的多智能体大语言模型框架
authors: "Rajesh Kumar, Waqar Ali, Junaid Ahmed, Najma Imtiaz Ali, Shaban Usman"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.13120v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: Planner、Coder、Tester、Debugger 和 Critic 智能体协同的多智能体框架
tldr: "针对大语言模型生成代码难以验证正确性的问题，本文提出 AgentForge 框架。该框架将“基于执行的验证”作为核心原则，要求所有代码更改必须在 Docker 沙箱中通过执行验证。通过 Planner、Coder、Tester、Debugger 和 Critic 等多智能体协作和共享内存机制，AgentForge 在 SWE-BENCH Lite 上取得了 40.0% 的解决率，显著优于单智能体基准，证明了执行反馈和角色分解在自主软件工程中的重要性。"
selection_source: fresh_fetch
motivation: 现有 LLM 生成的代码缺乏可靠的验证机制，且现有系统往往将执行验证视为可选，导致生成的代码难以保证正确性。
method: 构建了一个包含 Planner、Coder、Tester 等角色的多智能体框架，通过强制性的 Docker 沙箱执行反馈来指导代码的迭代修复。
result: "在 SWE-BENCH Lite 基准测试中达到 40.0% 的解决率，性能比单智能体基准提升了 26-28 个百分点。"
conclusion: 将执行反馈作为核心监督信号并结合多智能体角色分解，是提升自主软件工程任务成功率的关键。
---

## 摘要
大语言模型能够生成看似合理的代码，但无法验证其正确性。现有的多智能体系统通常模拟执行或将验证视为可选步骤。我们引入了“基于执行的验证”作为核心原则：每一次代码变更在传播之前都必须通过沙箱执行。我们在 AGENTFORGE 中实例化了这一原则，这是一个多智能体框架，其中规划者 (Planner)、编码者 (Coder)、测试者 (Tester)、调试者 (Debugger) 和评论者 (Critic) 智能体通过共享内存和强制性的 Docker 沙箱进行协作。我们将基于 LLM 的软件工程形式化为针对代码库状态的迭代决策过程，其中执行反馈提供了比“预测下一个 token 的似然概率”更强的监督信号。AGENTFORGE 在 SWE-BENCH Lite 上实现了 40.0% 的解决率，比单智能体基准线高出 26-28 个百分点。消融实验证实，执行反馈和角色分解各自独立地驱动了性能提升。该框架已在 https://github.com/raja21068/AutoCodeAI 开源。

## Abstract
Large language models generate plausible code but cannot verify correctness. Existing multi-agent systems simulate execution or leave verification optional. We introduce execution-grounded verification as a first-class principle: every code change must survive sandboxed execution before propagation. We instantiate this principle in AGENTFORGE, a multi-agent framework where Planner, Coder, Tester, Debugger, and Critic agents coordinate through shared memory and a mandatory Docker sandbox. We formalize software engineering with LLMs as an iterative decision process over repository states, where execution feedback provides a stronger supervision signal than next-token likelihood. AGENTFORGE achieves 40.0\% resolution on SWE-BENCH Lite, outperforming single-agent baselines by 26--28 points. Ablations confirm that execution feedback and role decomposition each independently drive performance. The framework is open-source at https://github.com/raja21068/AutoCodeAI.

---

## 论文详细总结（自动生成）

以下是对论文《AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）在自主软件工程中的核心痛点：**生成的代码虽然“看起来合理”但无法保证正确性**。
*   **研究动机**：现有的多智能体系统（MAS）要么仅模拟执行过程，要么将代码验证视为可选步骤，导致模型容易产生幻觉或引入回归错误。
*   **核心理念**：提出“基于执行的验证（Execution-Grounded Verification）”应作为第一优先级原则。即任何代码变更在正式提交前，必须在隔离的沙箱环境中通过真实测试。

### 2. 方法论
AgentForge 框架将软件工程任务建模为一个在代码库状态上的**迭代决策过程（MDP）**，核心组件包括：
*   **五大专业智能体角色**：
    1.  **Planner（规划者）**：结合任务和检索到的上下文生成结构化执行计划。
    2.  **Coder（编码者）**：生成最小化的统一差异补丁（Unified Diffs），而非重写整个文件，以降低错误率。
    3.  **Tester（测试者）**：合成可执行的测试用例（基于 pytest），覆盖边缘情况。
    4.  **Debugger（调试者）**：根据沙箱返回的错误信息（stdout/stderr）迭代修复代码。
    5.  **Critic（评论者）**：对最终结果进行最终评审。
*   **双重检索机制（Dual Retrieval）**：
    *   **情节记忆（Episodic Memory）**：存储过去成功解决的任务经验。
    *   **代码库索引（Repository Index）**：实时索引当前代码库，提供精准的上下文。
*   **强制性 Docker 沙箱**：所有代码在资源受限（512MB RAM, 0.5 CPU）、网络隔离的容器中运行，提供真实的执行反馈信号。

### 3. 实验设计
*   **数据集/基准测试**：主要使用 **SWE-BENCH Lite**（包含 300 个真实的 GitHub Issue 任务），涵盖 Django、scikit-learn 等 11 个流行 Python 项目。
*   **对比方法**：
    *   **Single-agent GPT-4o**：无工具、无反馈的单次生成。
    *   **ReAct (GPT-4o)**：基于推理-动作循环的智能体。
    *   **SWE-agent**：现有的开源自主软件工程基准。
*   **评估指标**：解决率（Resolve Rate），即生成的补丁必须通过所有预设的测试用例。

### 4. 资源与算力
*   **硬件配置**：文中提到实验运行在配备 **16 核 CPU、64GB RAM、专用 GPU**（未指明具体型号，用于加速处理）以及高速 SSD 的机器上。
*   **模型使用**：核心逻辑基于 **GPT-4o** (gpt-4o-2024-08-06)。
*   **成本分析**：平均每个任务的 API 成本约为 **0.1445 美元**，约为单智能体方案的 2.7 倍。

### 5. 实验数量与充分性
*   **实验规模**：在完整的 300 个 SWE-BENCH Lite 任务上进行了测试。
*   **消融实验**：针对前 100 个任务进行了系统的消融实验，分别移除 Planner、Tester、Debugger 等角色以验证其必要性。
*   **错误分析**：随机抽取 30 个失败案例进行深度定性分析，分类讨论了定位失败、补丁无效等原因。
*   **评价**：实验设计较为充分且客观，通过固定随机种子（seed=42）和温度（T=0）保证了结果的可复现性。

### 6. 主要结论与发现
*   **性能卓越**：AgentForge 达到了 **40.0%** 的解决率，比单智能体基准（14.0%）高出 26 个百分点，也优于 SWE-agent（18.1%）。
*   **执行反馈至关重要**：消融实验显示，Tester 和 Debugger 构成的“执行-修复”循环是性能提升的最大驱动力。
*   **角色分解的优势**：将复杂任务分解为专门的智能体，能有效降低单个步骤的错误累积。
*   **似然概率 vs 执行信号**：实验证明，真实的执行反馈比模型内部的 Token 预测概率提供了更强的正确性监督。

### 7. 优点
*   **闭环验证**：强制性的沙箱执行消除了模拟执行带来的系统性误差。
*   **高效编辑**：采用 Diff-based 补丁生成，相比全文件重写，显著降低了 Token 消耗和错误发生面。
*   **双重记忆**：结合了长期经验（情节记忆）和短期上下文（代码库索引），增强了规划的准确性。
*   **开源贡献**：提供了完整的开源框架，便于社区进一步研究。

### 8. 不足与局限
*   **跨文件协调困难**：错误分析显示 40% 的失败源于“故障定位错误”，模型在处理涉及多个文件协同修改的复杂任务时仍显乏力。
*   **成本开销**：多智能体协作和多次迭代调试导致 Token 消耗显著高于单智能体方案。
*   **环境依赖**：对于极其复杂的依赖环境或超长运行时间的测试（>30s），沙箱可能会因超时或配置问题导致任务失败。
*   **模型依赖**：目前高度依赖 GPT-4o 的推理能力，在较小规模的开源模型上的表现尚未充分验证。

（完）
