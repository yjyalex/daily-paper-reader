---
title: Tail-Aware Information-Theoretic Generalization for RLHF and SGLD
title_zh: 针对 RLHF 和 SGLD 的尾部感知信息论泛化
authors: "Huiming Zhang, Binghan Li, Wan Tian, Qiang Sun"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10727v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 具有重尾奖励的RLHF信息论泛化
tldr: 针对RLHF和SGLD中常见的重尾分布数据，传统基于KL散度的泛化界限因依赖亚高斯假设而失效。本文提出了一种适用于亚Weibull分布的尾部感知信息论框架，通过引入$f_	heta$-散度和Rényi互信息，建立了新的去相关引理和Dudley型链式不等式。该框架不仅能处理重尾奖励和梯度噪声，还提供了期望及高概率的PAC-Bayes泛化界限，为现代重尾学习任务提供了理论支撑。
selection_source: fresh_fetch
motivation: 传统的基于KL散度的泛化界限要求数据具有亚高斯性，无法处理RLHF和随机优化中常见的重尾分布问题。
method: 开发了基于亚Weibull分布的尾部感知框架，利用$f_	heta$-散度和Rényi散度建立去相关引理，并推导出Dudley型链式不等式。
result: 获得了适用于重尾数据的期望与高概率PAC-Bayes泛化界限，并将其应用于Rényi正则化的RLHF和具有重尾噪声的SGLD。
conclusion: 该研究扩展了信息论泛化理论的适用范围，为处理非亚高斯、重尾分布的现代机器学习算法提供了严谨的理论工具。
---

## 摘要
经典的信息论泛化界通常通过基于 KL 散度的互信息来控制泛化间隙，因此依赖于有界性或通过矩生成函数 (MGF) 确定的亚高斯尾部。在许多现代流程中，如鲁棒学习、RLHF 和随机优化，损失和奖励可能是重尾的，且 MGF 可能不存在，这使得基于 KL 的工具失效。我们为亚 Weibull 数据开发了一个依赖于尾部的信息论框架，其中尾部参数 $\theta$ 控制尾部厚度：$\theta=2$ 对应亚高斯，$\theta=1$ 对应亚指数，而 $0<\theta<1$ 对应真正的重尾。我们的核心技术组件是一个去相关引理，它使用偏移对数 $f_\theta$-散度来界定测度变换期望，该方法允许在不使用 MGF 参数的情况下与 Rényi 散度进行显式比较。在经验过程方面，我们为具有尾部指数 $\theta$ 的亚 Weibull 过程建立了紧致的极大不等式和 Dudley 型链式界，其复杂度随 $\log^{1/\theta}$ 和熵$^{1/\theta}$ 缩放。这些工具产生了期望和高概率 PAC-Bayes 泛化界，以及基于多尺度 Rényi 互信息的信息论链式不等式。我们展示了这些结果在重尾奖励下的 Rényi 正则化 RLHF 以及具有重尾梯度噪声的随机梯度朗之万动力学 (SGLD) 中的应用。

## Abstract
Classical information-theoretic generalization bounds typically control the generalization gap through KL-based mutual information and therefore rely on boundedness or sub-Gaussian tails via the moment generating function (MGF). In many modern pipelines, such as robust learning, RLHF, and stochastic optimization, losses and rewards can be heavy-tailed, and MGFs may not exist, rendering KL-based tools ineffective. We develop a tail-dependent information-theoretic framework for sub-Weibull data, where the tail parameter $θ$ controls the tail heaviness: $θ=2$ corresponds to sub-Gaussian, $θ=1$ to sub-exponential, and $0<θ<1$ to genuinely heavy tails. Our key technical ingredient is a decorrelation lemma that bounds change-of-measure expectations using a shifted-log $f_θ$-divergence, which admits explicit comparisons to Rényi divergence without MGF arguments. On the empirical-process side, we establish sharp maximal inequalities and a Dudley-type chaining bound for sub-Weibull processes with tail index $θ$, with complexity scaling as $\log^{1/θ}$ and entropy$^{1/θ}$. These tools yield expected and high-probability PAC-Bayes generalization bounds, as well as an information-theoretic chaining inequality based on multiscale Rényi mutual information. We illustrate the consequences in Rényi-regularized RLHF under heavy-tailed rewards and in stochastic gradient Langevin dynamics with heavy-tailed gradient noise.

---

## 论文详细总结（自动生成）

这篇论文《Tail-Aware Information-Theoretic Generalization for RLHF and SGLD》针对现代机器学习中常见的重尾分布数据，提出了一套全新的信息论泛化分析框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究动机**：传统的泛化界限分析（如基于 KL 散度的互信息方法）高度依赖于数据的“亚高斯性”或矩生成函数（MGF）的存在。然而，在强化学习（RLHF）的奖励模型、大规模优化的梯度噪声以及鲁棒学习中，数据往往呈现“重尾”特征（即极端值出现的概率高于高斯分布）。
*   **核心问题**：当 MGF 不存在且 KL 散度失效时，如何建立严谨的算法依赖型泛化界限？如何解决重尾奖励导致的“灾难性古德哈特定律”（Catastrophic Goodhart，即过度优化代理奖励导致真实性能崩溃）？

### 2. 方法论
论文提出了一套适用于**亚 Weibull 分布**（Sub-Weibull）的尾部感知框架，其核心技术包括：
*   **亚 Weibull 建模**：使用参数 $\theta$ 控制尾部厚度（$\theta=2$ 为亚高斯，$\theta<1$ 为重尾）。
*   **偏移对数 $f_\theta$-散度**：引入了一种新型凸函数 $f_\theta(x) = x \log^{1/\theta}(x + A)$，并基于此建立了**去相关引理**（Decorrelation Lemma）。该引理无需 MGF 即可通过 $f_\theta$-散度界定测度变换下的期望。
*   **Rényi 互信息关联**：证明了 $f_\theta$-散度可以被 Rényi 散度显式上界锁定，从而将泛化界限与 Rényi 互信息 $I_\alpha$ 联系起来。
*   **亚 Weibull 经验过程理论**：
    *   建立了紧致的**极大不等式**，证明重尾变量最大值的期望以 $O((\log n)^{1/\theta})$ 速度增长。
    *   推导了 **Dudley 型链式不等式**，将覆盖数（Entropy）的指数从 $1/2$ 扩展到 $1/\theta$。
*   **应用算法**：提出了 **Rényi 正则化 RLHF**，利用 Rényi 散度对似然比的强约束力来抑制重尾奖励带来的不稳定性。

### 3. 实验设计
论文设计了理论验证与实际应用两类实验：
*   **RLHF 场景**：
    *   **模型**：使用 Qwen2.5-1.5B 作为策略模型，Qwen2.5-0.5B 作为代理奖励模型，Skywork-Reward-V2-Qwen3-1.7B 作为“金标准”评估器。
    *   **数据集**：`hh-rlhf`。
    *   **对比方法**：KL 正则化（$\alpha=1$） vs. 不同阶数的 Rényi 正则化（$\alpha \in \{2, \dots, 10\}$）。
    *   **压力测试**：通过幂变换（$|r|^\kappa$）和注入 Weibull 噪声人为构造重尾奖励环境。
*   **随机优化场景**：在重尾线性回归任务上运行 SGLD，验证泛化间隙与样本量 $n$ 及尾部指数 $\lambda$ 的关系。
*   **数值模拟**：对比了单尺度互信息界限、经典链式界限与本文提出的 Rényi 链式互信息界限。

### 4. 资源与算力
*   **算力说明**：论文在附录 I.1 中提到使用了 GRPO 算法进行对齐训练。
*   **具体细节**：
    *   使用了 BF16 精度。
    *   有效 Batch Size 为 8（每设备 1，梯度累积 8 步）。
    *   训练步数最高为 1500 步。
*   **缺失信息**：文中未明确标注具体的 GPU 型号（如 A100 或 H100）及具体的总计算时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   RLHF 实验涵盖了 4 种不同的重尾程度（$\kappa \in \{1, 6, 8, 10\}$）和 10 种正则化阶数（$\alpha$）。
    *   SGLD 实验测试了从 $n=50$ 到 $n=3000$ 的多种样本规模。
*   **充分性评价**：实验设计较为充分。通过构造两种不同的重尾环境（幂变换和噪声注入）验证了方法的鲁棒性。对比了代理奖励与金标准奖励的耦合关系，客观地展示了 KL 散度在重尾情况下的失效（非单调性）以及 Rényi 正则化的稳定性。

### 6. 主要结论与发现
*   **理论发现**：重尾分布下，泛化界限的复杂度随尾部指数 $1/\theta$ 呈指数级恶化，传统的 KL 互信息界限会变得无意义（趋于无穷）。
*   **RLHF 结论**：在重尾奖励下，KL 正则化无法阻止“灾难性古德哈特”现象，而 Rényi 正则化能提供更可靠的信赖域，使代理奖励与金标准奖励保持单调相关。
*   **SGLD 结论**：SGLD 的泛化性能受梯度噪声尾部厚度的显著影响，步长与噪声水平的权衡需根据尾部指数进行调整。

### 7. 优点
*   **理论突破**：填补了信息论泛化理论在非亚高斯（重尾）领域的空白，提供了从极大不等式到链式法则的完整工具链。
*   **实用性强**：直接解决了大模型对齐（RLHF）中一个非常现实的痛点——奖励模型异常值导致的训练不稳定。
*   **数学优雅**：通过 $f_\theta$-散度巧妙地绕过了 MGF 不存在的问题，且能退化回经典的亚高斯结论。

### 8. 不足与局限
*   **假设限制**：理论分析仍基于 i.i.d. 数据假设，未完全覆盖深度学习中复杂的非独立同分布采样情况。
*   **超参数敏感性**：Rényi 正则化引入了额外的超参数 $\alpha$，虽然实验给出了建议范围，但在不同任务中的最优选择仍需调优。
*   **计算开销**：Rényi 散度的计算相比 KL 散度略微复杂，且在多尺度链式分析中，计算每一层的互信息具有一定的工程难度。

（完）
