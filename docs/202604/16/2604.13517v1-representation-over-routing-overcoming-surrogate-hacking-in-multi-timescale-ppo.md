---
title: "Representation over Routing: Overcoming Surrogate Hacking in Multi-Timescale PPO"
title_zh: 表征优于路由：克服多时间尺度 PPO 中的代理目标劫持
authors: Jing Sun
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13517v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 克服多时间尺度 PPO 中的代理目标攻击
tldr: 本研究针对强化学习中的时间信用分配挑战，受神经生物学多时间尺度编码启发，探讨了在PPO中引入多折扣因子的影响。研究发现，直接融合多尺度信号会导致代理目标欺骗和近视退化。为此，提出“目标解耦”架构：Critic端保留多尺度预测以强化表征学习，而Actor端仅依据长期优势更新策略。该方法在LunarLander-v2中显著提升了性能，消除了策略崩溃，并克服了单尺度基准的局部最优问题。
selection_source: fresh_fetch
motivation: 揭示在复杂延迟奖励任务中，盲目融合多时间尺度信号会导致算法出现代理目标欺骗和不可逆的近视退化现象。
method: 提出目标解耦架构，在Critic端利用多尺度信号进行辅助表征学习，在Actor端则严格隔离短期信号，仅基于长期优势更新策略。
result: 在LunarLander-v2实验中，该架构显著超越了“环境已解决”阈值，消除了策略崩溃，并成功跳出了单尺度基准常陷入的局部最优。
conclusion: 通过将多尺度表征学习与策略路由解耦，可以有效克服多时间尺度强化学习中的病态问题，提升算法的鲁棒性与性能。
---

## 摘要
强化学习中的时间信用分配长期以来一直是一项核心挑战。受神经生物学中多巴胺系统的多时间尺度编码启发，近期研究尝试在 Actor-Critic 架构（如近端策略优化 PPO）中引入多个折扣因子，以平衡短期响应与长期规划。然而，本文揭示了在复杂的延迟奖励任务中，盲目融合多时间尺度信号会导致严重的算法病理问题。我们系统地证明，将时间注意力路由机制暴露给策略梯度会导致代理目标劫持（surrogate objective hacking），而采用无梯度的不确定性加权则会引发不可逆的近视退化，我们将这一现象称为“时间不确定性悖论”。为解决这些问题，我们提出了一种目标解耦（Target Decoupling）架构：在 Critic 端，我们保留多时间尺度预测以强化辅助表征学习；而在 Actor 端，我们严格隔离短期信号，仅根据长期优势更新策略。在 LunarLander-v2 环境中跨多个独立随机种子的严格实证评估表明，我们提出的架构实现了具有统计学意义的性能提升。在不依赖超参数劫持的情况下，该架构以极小的方差持续超越“环境已解决”阈值，完全消除了策略崩溃，并摆脱了困扰单时间尺度基准模型的悬停局部最优解。

## Abstract
Temporal credit assignment in reinforcement learning has long been a central challenge. Inspired by the multi-timescale encoding of the dopamine system in neurobiology, recent research has sought to introduce multiple discount factors into Actor-Critic architectures, such as Proximal Policy Optimization (PPO), to balance short-term responses with long-term planning. However, this paper reveals that blindly fusing multi-timescale signals in complex delayed-reward tasks can lead to severe algorithmic pathologies. We systematically demonstrate that exposing a temporal attention routing mechanism to policy gradients results in surrogate objective hacking, while adopting gradient-free uncertainty weighting triggers irreversible myopic degeneration, a phenomenon we term the Paradox of Temporal Uncertainty. To address these issues, we propose a Target Decoupling architecture: on the Critic side, we retain multi-timescale predictions to enforce auxiliary representation learning, while on the Actor side, we strictly isolate short-term signals and update the policy based solely on long-term advantages. Rigorous empirical evaluations across multiple independent random seeds in the LunarLander-v2 environment demonstrate that our proposed architecture achieves statistically significant performance improvements. Without relying on hyperparameter hacking, it consistently surpasses the ''Environment Solved'' threshold with minimal variance, completely eliminates policy collapse, and escapes the hovering local optima that trap single-timescale baselines.

---

## 论文详细总结（自动生成）

### 论文总结：表征优于路由（Representation over Routing）

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在强化学习（RL）中，**时间信用分配（Temporal Credit Assignment）**是处理稀疏或延迟奖励任务的核心难题。传统的 PPO 等算法依赖单一折扣因子 $\gamma$，这导致了权衡困境：较小的 $\gamma$ 提供密集的局部梯度但缺乏远见，而较大的 $\gamma$ 虽然关注长期目标，但在训练初期受认知不确定性影响，容易使智能体陷入局部最优（如在 LunarLander 环境中为了规避引擎惩罚而选择“原地悬停”）。

受神经生物学多巴胺系统多时间尺度编码的启发，研究者尝试引入多个 $\gamma$。然而，本文揭示了**盲目融合多尺度信号**会引发严重的算法病理：
*   **代理目标劫持（Surrogate Objective Hacking）**：动态路由权重会被策略梯度利用，通过操纵权重而非改进动作来虚假地最大化目标函数。
*   **时间不确定性悖论（Paradox of Temporal Uncertainty）**：基于不确定性的路由会因短期信号误差天然较低，而使智能体永久锁定在近视行为上。

#### 2. 论文提出的方法论：目标解耦架构（Target Decoupling）
论文提出了“**表征优于路由**”的新范式，核心思想是将多尺度信号的作用从“决策引导”转为“辅助表征”。
*   **多时间尺度 Critic（表征增强）**：Critic 网络不再输出标量，而是输出一个向量 $V_\phi(s_t)$，对应一组离散的折扣因子 $\Gamma = \{0.5, 0.9, 0.99, 0.999\}$。通过同时拟合多个尺度的价值函数，强制底层网络提取稳健的物理特征（如重力、动量）。
*   **目标解耦 Actor（策略隔离）**：在 Actor 端，完全放弃动态路由或信号融合。Actor 仅接收最长尺度（如 $\gamma=0.999$）的优势信号 $\hat{A}_{actor}$ 进行策略更新。
*   **核心逻辑**：利用多尺度任务在 Critic 端训练出强大的特征提取器，但确保 Actor 的优化目标纯粹且不受短期信号干扰。

#### 3. 实验设计
*   **场景与环境**：经典的延迟奖励连续控制基准 **LunarLander-v2**。该环境具有持续的引擎消耗惩罚和巨大的最终着陆奖励，极易触发局部最优。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准单时间尺度 PPO（$\gamma=0.99$）。
    *   **动态注意力路由（Dynamic Attention）**：由 Actor 驱动的权重融合。
    *   **不确定性路由（Uncertainty Weighting）**：基于 TD 误差绝对值的无梯度反方差加权。
    *   **目标解耦 PPO（本文方法）**。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。实验记录显示每个变体运行了 3,000 个 Episode，并跨越了 5 个独立随机种子。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组对比实验，包括针对两种病理现象（劫持与悖论）的消融实验，以及与基准 PPO 的性能对比。
*   **充分性与公平性**：
    *   使用了 **5 个独立随机种子**并标注了标准差（阴影区域），证明了结果的统计显著性。
    *   所有变体保持了相同的 PPO 基础超参数（如裁剪系数 $\epsilon=0.2$、熵系数等），确保了对比的公平性。
    *   实验不仅关注最终得分，还分析了 Episode 长度和 Value Loss 曲线，从多维度验证了理论假设。

#### 6. 论文的主要结论与发现
*   **动态路由的失效**：证明了将路由权重暴露给梯度会导致优化器“走捷径”，而基于方差的路由会导致不可逆的近视退化。
*   **解耦的优越性**：提出的目标解耦架构成功打破了单尺度 PPO 的“悬停”局部最优，得分稳定超过 200 分（环境已解决阈值），且方差极小。
*   **辅助任务的价值**：即使 Actor 不直接使用短期信号，Critic 通过拟合短期目标所获得的表征能力也能显著降低长期优势估计的方差。

#### 7. 优点
*   **理论深刻**：首次定义并证明了多时间尺度 RL 中的“代理目标劫持”和“时间不确定性悖论”。
*   **架构简洁**：无需复杂的超参数调优或启发式规则，通过简单的架构解耦即解决了复杂的信用分配问题。
*   **鲁棒性强**：实验结果显示出极高的收敛稳定性和跨种子的一致性。

#### 8. 不足与局限
*   **环境单一**：实验主要集中在 LunarLander-v2，虽然该环境极具代表性，但尚未在更高维、更复杂的物理引擎（如 MuJoCo 或人形机器人任务）中验证。
*   **计算开销**：虽然 Actor 端简化了，但 Critic 端需要维护多个输出头和计算多个尺度的损失，会略微增加单步训练的计算量。
*   **超参数依赖**：虽然不依赖“劫持”技巧，但 $\Gamma$ 集合的选择（折扣因子的具体数值）仍可能对不同任务的表征效果产生影响。

（完）
