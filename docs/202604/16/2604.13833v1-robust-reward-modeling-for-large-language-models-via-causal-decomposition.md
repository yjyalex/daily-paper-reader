---
title: Robust Reward Modeling for Large Language Models via Causal Decomposition
title_zh: 基于因果分解的大语言模型鲁棒奖励建模
authors: "Yunsheng Lu, Zijiang Yang, Licheng Pan, Zhixuan Chu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13833v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 旨在抑制数学和推理中提示词无关捷径的奖励模型设计
tldr: 针对奖励模型（RM）容易过度拟合长度、语气等虚假特征的问题，本文提出一种基于因果分解的鲁棒奖励建模方法。通过引入一个解码器将候选回答映射回输入的潜在意图嵌入，并利用重构误差作为正则化信号，强制模型关注提示词意图而非捷径。实验表明，该方法在RewardBench上显著提升了准确率，并在保持高性能的同时有效抑制了冗长和谄媚倾向。
selection_source: fresh_fetch
motivation: 现有的奖励模型往往依赖长度和语气等虚假相关性进行评分，缺乏对提示词真实意图的深入理解。
method: 学习一个将候选回答映射回输入意图嵌入的解码器，并利用重构误差作为正则化项来训练奖励模型。
result: 在Gemma系列模型上将RewardBench准确率从0.832提升至0.868，并能更准确地识别短小且非谄媚的优质回答。
conclusion: 通过因果分解强化提示词意图的关联，可以有效提高奖励模型的鲁棒性，减少对表面特征的依赖。
---

## 摘要
奖励模型是大语言模型对齐的核心，但它们往往会过拟合于伪线索，例如回复长度和过度讨好的语气。大多数先前的工作通过惩罚或控制特定的伪影来直接削弱这些线索，但并未明确鼓励模型将偏好根植于提示词的意图。我们学习了一个解码器，将候选答案映射到输入的潜在意图嵌入中。重构误差被用作正则化奖励模型训练的信号。我们提供了理论证据，证明该信号在抑制与提示词无关的捷径的同时，强调了依赖于提示词的信息。在数学、帮助性和安全性基准测试中，该解码器以 0.877 的准确率选择了更短且更少谄媚的候选答案。将该信号整合到 Gemma-2-2B-it 和 Gemma-2-9B-it 的奖励模型训练中，使 RewardBench 的准确率从 0.832 提升至 0.868。在 Best-of-N 选择中，我们的框架在产生更短输出的同时提高了长度控制下的胜率，并且在受控重写测试中对长度增加和轻微的离题漂移保持了鲁棒性。

## Abstract
Reward models are central to aligning large language models, yet they often overfit to spurious cues such as response length and overly agreeable tone. Most prior work weakens these cues directly by penalizing or controlling specific artifacts, but it does not explicitly encourage the model to ground preferences in the prompt's intent. We learn a decoder that maps a candidate answer to the latent intent embedding of the input. The reconstruction error is used as a signal to regularize the reward model training. We provide theoretical evidence that this signal emphasizes prompt-dependent information while suppressing prompt-independent shortcuts. Across math, helpfulness, and safety benchmarks, the decoder selects shorter and less sycophantic candidates with 0.877 accuracy. Incorporating this signal into RM training in Gemma-2-2B-it and Gemma-2-9B-it increases RewardBench accuracy from 0.832 to 0.868. For Best-of-N selection, our framework increases length-controlled win rates while producing shorter outputs, and remains robust to lengthening and mild off-topic drift in controlled rewrite tests.

---

## 论文详细总结（自动生成）

这是一份关于论文《Robust Reward Modeling for Large Language Models via Causal Decomposition》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：奖励模型（Reward Models, RM）在人类反馈强化学习（RLHF）中极易陷入“奖励黑客”（Reward Hacking）困境。它们往往通过识别与提示词意图无关的虚假特征（如回复长度、过度礼貌或谄媚的语气）来给出高分，而非真正理解提示词的语义意图。
*   **研究动机**：现有的缓解方法（如直接惩罚长度）多属于“头痛医头”，缺乏一种显式量化“回复对提示词意图实现程度”的机制。本文旨在通过因果分解，将回复中的“意图相关信号”与“上下文无关伪影”分离，从而构建更鲁棒的奖励模型。

### 2. 方法论：CARP 框架与 SAS 评分
*   **核心思想**：如果一个回复忠实地执行了提示词的意图，那么从该回复的表示中应该能够重构出原始提示词的语义嵌入。
*   **关键技术细节**：
    1.  **因果图建模**：将变量分解为潜在意图 $W$（源自提示词 $X$）和虚假伪影 $Z$。目标是增强 $W$ 到奖励 $R$ 的因果边缘。
    2.  **稀疏自编码器（SAE）**：利用预训练的 SAE 提取回复的高维稀疏语义特征，以过滤噪声并保留核心语义。
    3.  **提示词解码器（Prompt Decoder）**：训练一个轻量级解码器，输入回复的 SAE 特征，目标是预测提示词的最后一层隐藏状态嵌入。
    4.  **语义对齐分数（SAS）**：定义为解码器重构提示词嵌入的均方误差（MSE）。SAS 越低，表示回复与提示词意图对齐越紧密。
    5.  **SAS 正则化训练**：在标准的 Bradley-Terry 偏好损失函数中引入 SAS 差异项：
        $L_{SAS} = L_{vanilla} + k \cdot (SAS_{chosen} - SAS_{rejected})$
        其中 $k$ 是调节系数，通过拉大意图对齐程度不同的回复间的奖励差距来引导模型。

### 3. 实验设计
*   **数据集**：
    *   **训练解码器**：使用 20K 混合 SFT 语料（Smoltalk, AlpacaFarm），并利用 DeepSeek-V3, Llama3, Qwen 等模型进行“一对多”数据增强。
    *   **训练奖励模型**：从 700K RLHF 数据集中采样 70K 对偏好数据。
*   **Benchmark**：
    *   **RewardBench**：涵盖 Chat, Chat-Hard, Safety, Reasoning 四大维度。
    *   **AlpacaEval-2**：用于 Best-of-N 策略评估。
    *   **受控重写测试**：通过 GPT-4o-mini 将回复重写为“更长但内容不变”或“更长且离题”，测试模型的抗干扰能力。
*   **对比方法**：Vanilla RM（基准奖励模型）、RRM（鲁棒奖励模型）、WARM（权重平均）等。

### 4. 资源与算力
*   **算力设备**：
    *   **解码器训练**：单张 NVIDIA RTX 4090 GPU，训练时长小于 1 小时。
    *   **奖励模型微调**：使用 8 × NVIDIA H200 GPU 集群。
*   **训练时长**：
    *   Gemma-2-9B-it 模型训练约 10 小时。
    *   Gemma-2-2B-it 模型训练约 6 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2B 和 9B 两种参数规模的模型。
*   **消融实验**：针对数据增强策略（20K vs 80K vs 20K增强）、SAE 的必要性、不同层特征的选择、以及安全阈值 $\tau$ 的影响做了详细实验。
*   **充分性评价**：实验设计较为全面，不仅有标准基准测试，还专门设计了针对“长度偏见”和“离题漂移”的鲁棒性专项测试，证明了方法在不同场景下的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 RewardBench 上，9B 模型的加权平均准确率从 83.22% 提升至 86.83%，尤其在 Chat-Hard 维度提升显著（超过 4%）。
*   **抑制偏见**：SAS 信号与回复长度几乎不相关（相关系数 $r=0.0095$），证明其能有效抑制长度偏见。
*   **鲁棒性**：在 Best-of-N 实验中，该方法在生成更短回复的同时，获得了更高的长度控制胜率（LC%）。
*   **理论验证**：论文通过数学证明（定理 1 和 2）阐述了该解码器能以高概率抑制伪影信号并提取意图信号。

### 7. 优点
*   **理论支撑强**：基于因果推断框架，提供了严谨的理论证明。
*   **即插即用**：作为一种正则化项，可以轻松集成到现有的奖励模型训练流程中。
*   **解决核心痛点**：有效解决了奖励模型对“长度”和“谄媚语气”的过度依赖，提升了对齐的质量。
*   **效率高**：解码器部分非常轻量，训练和推理开销较小。

### 8. 不足与局限
*   **安全任务冲突**：在安全对齐中，正确的回复往往是“拒绝回答”，这在语义重构上可能被判定为“离题”（SAS 较高），导致模型在安全维度表现略有下降（需通过设置阈值 $\tau$ 缓解）。
*   **单轮对话限制**：目前主要针对单轮对话设计，尚未扩展到多轮对话的复杂意图追踪。
*   **事实性风险**：SAS 主要关注“话题相关性”，可能无法区分“话题相关但事实错误”的回复，仍需依赖人类偏好标签来保证事实准确性。

（完）
