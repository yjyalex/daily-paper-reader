---
title: "ToolSpec: Accelerating Tool Calling via Schema-Aware and Retrieval-Augmented Speculative Decoding"
title_zh: ToolSpec：通过模式感知与检索增强的投机解码加速工具调用
authors: "Heming Xia, Yongqi Li, Cunxiao Du, Mingbo Song, Wenjie Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13519v1"
tags: ["query:llm-fo"]
score: 9.0
evidence: 模式感知的投机解码，用于加速工具调用并降低延迟
tldr: ToolSpec 针对大语言模型工具调用中的高延迟问题，提出了一种结合模式感知与检索增强的投机采样方法。该方法利用工具调用的结构化特征，通过有限状态机引导确定性字段生成，并检索历史调用记录作为草稿，为复杂任务中的多步工具交互提供了一种即插即用的加速方案，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 工具调用过程中的多轮交互导致显著延迟，且工具调用轨迹具有高度结构化和模式重复的特点。
method: 利用有限状态机进行模式约束生成，并结合检索增强技术复用历史工具调用记录作为投机采样的草稿。
result: 在多个基准测试中实现了高达 4.2 倍的加速，性能显著优于现有的无需训练的投机采样方法。
conclusion: ToolSpec 通过挖掘工具调用的结构化与重复性规律，为提升 LLM 工具调用效率提供了一种高效且易于集成的解决方案。
---

## 摘要
工具调用通过使大语言模型（LLM）能够与外部应用程序交互，极大地扩展了其实际效用。随着 LLM 能力的提升，有效的工具使用越来越多地涉及多步、多轮交互以解决复杂任务。然而，随之而来的工具交互增长带来了显著的延迟，这为实时 LLM 服务提出了关键挑战。通过实证分析，我们发现工具调用轨迹具有高度结构化、符合受限模式（Schema），且通常表现出重复的调用模式。受此启发，我们提出了 ToolSpec，一种用于加速工具调用的模式感知、检索增强投机解码方法。ToolSpec 利用预定义的工具模式生成准确的草稿，使用有限状态机在确定性的模式标记填充与变量字段的投机生成之间交替。此外，ToolSpec 检索相似的历史工具调用并将其重用为草稿，以进一步提高效率。ToolSpec 提供了一种即插即用的解决方案，可以无缝集成到现有的 LLM 工作流中。在多个基准测试上的实验表明，ToolSpec 实现了高达 4.2 倍的加速，显著优于现有的无需训练的投机解码方法。

## Abstract
Tool calling has greatly expanded the practical utility of large language models (LLMs) by enabling them to interact with external applications. As LLM capabilities advance, effective tool use increasingly involves multi-step, multi-turn interactions to solve complex tasks. However, the resulting growth in tool interactions incurs substantial latency, posing a key challenge for real-time LLM serving. Through empirical analysis, we find that tool-calling traces are highly structured, conform to constrained schemas, and often exhibit recurring invocation patterns. Motivated by this, we propose ToolSpec, a schema-aware, retrieval-augmented speculative decoding method for accelerating tool calling. ToolSpec exploits predefined tool schemas to generate accurate drafts, using a finite-state machine to alternate between deterministic schema token filling and speculative generation for variable fields. In addition, ToolSpec retrieves similar historical tool invocations and reuses them as drafts to further improve efficiency. ToolSpec presents a plug-and-play solution that can be seamlessly integrated into existing LLM workflows. Experiments across multiple benchmarks demonstrate that ToolSpec achieves up to a 4.2x speedup, substantially outperforming existing training-free speculative decoding methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ToolSpec** 的新型投机解码（Speculative Decoding）方法，旨在显著加速大语言模型（LLM）在工具调用场景下的推理速度。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 LLM 在复杂任务中频繁进行多步、多轮的工具交互，推理延迟成为了实时服务的瓶颈。
*   **研究动机**：作者通过实证分析发现，工具调用的生成延迟占端到端延迟的 80% 到 96%，远超工具本身的执行时间。同时，工具调用具有两个关键特性：
    1.  **高度结构化**：输出通常遵循严格的 JSON 模式（Schema），大部分 Token（如键名、标点）是预先确定的。
    2.  **模式重复性**：在不同请求中，LLM 往往会重复调用相同的工具或参数模式。

### 2. 方法论：ToolSpec 核心思想与技术细节
ToolSpec 是一种无需训练、即插即用的投机解码方案，包含两个核心组件：
*   **模式感知草稿生成（Schema-Aware Drafting）**：
    *   利用有限状态机（FSM）将生成过程划分为：工具名状态（$q_t$）、参数名状态（$q_p$）、参数值状态（$q_v$）和普通文本状态（$q_o$）。
    *   在结构化状态（如工具名和参数名）下，直接根据 Schema 填充确定性的 Token 作为草稿；在非结构化状态（参数值）下，则切换回常规的投机生成。
*   **检索增强投机（Retrieval-Augmented Speculation）**：
    *   维护一个历史成功工具调用的数据存储库。
    *   给定当前查询，检索 Top-K 个相似的历史调用记录。
    *   通过后缀匹配（Suffix Matching）技术，从历史记录中提取后续 Token 序列作为高质量草稿，从而加速重复性调用模式的生成。
*   **并行验证**：使用树状注意力机制（Tree-based Attention）在目标模型中一次性并行验证多个候选草稿。

### 3. 实验设计
*   **数据集/场景**：涵盖了四个主流工具调用基准测试：**API-Bank**、**ToolAlpaca**、**BFCLv2**（伯克利函数调用排行榜）和 **ToolBench**。
*   **对比方法**：
    *   **无需训练的方法**：Prompt Lookup Decoding (PLD)、Token Recycling (TR)、SAM-Decoding (SAMD)。
    *   **需要训练的方法（仅作参考）**：Eagle-2、Eagle-3。
*   **评估模型**：Qwen2.5-Instruct 系列（7B, 14B）、LLaMA-3.1-8B、LLaMA-3.2-3B 以及专门训练的 ToolLLaMA。

### 4. 资源与算力
*   **硬件环境**：实验在 **2 张 NVIDIA A100-PCIE (40GB) GPU** 上运行，配备 40 核 CPU。
*   **软件框架**：基于 PyTorch 2.5.1 和 Huggingface Transformers 库。
*   **训练时长**：由于 ToolSpec 是**无需训练（Training-free）**的方法，因此不存在训练时长，仅涉及推理过程中的轻量级检索和 FSM 维护开销（约占总延迟的 5.7%）。

### 5. 实验数量与充分性
*   **实验规模**：作者在 4 个数据集上测试了 6 种不同架构和规模的模型，并进行了详细的消融实验。
*   **充分性与公平性**：实验涵盖了通用模型和工具专用模型，对比了当前最先进的多种投机解码基准。通过平均接受 Token 数（#MAT）和端到端加速比等多个维度进行评估，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **显著加速**：ToolSpec 在不同任务中实现了 **3.5x 至 4.2x** 的加速比，相比之前最强的无需训练方法（TR），相对提升高达 61%。
*   **接受率高**：在重复调用场景下，平均每步可接受 9.27 个 Token，加速比可达 5.5x。
*   **格式遵循度相关性**：研究发现模型的格式遵循能力（Format Adherence）与 ToolSpec 的加速效果呈强正相关（相关系数 ~0.90），这意味着模型越强大、遵循 Schema 越严谨，加速效果越好。

### 7. 优点与亮点
*   **即插即用**：不需要对模型进行任何微调或添加辅助参数，易于集成到现有生产环境。
*   **结构化利用**：巧妙地将工具调用的 Schema 转化为 FSM，将“生成问题”部分转化为“填充问题”，极大地提高了草稿的准确性。
*   **长尾加速**：通过检索机制，能够捕捉到长达 17-33 个 Token 的超长准确草稿，这是传统方法难以实现的。

### 8. 不足与局限
*   **模型规模限制**：受限于计算资源，实验未在 70B 以上的超大规模模型上进行验证（尽管作者预期效果会更好）。
*   **依赖 Schema 质量**：该方法高度依赖于预定义的工具文档和 Schema 的准确性，如果 Schema 描述模糊或模型不遵循 Schema，加速效果会打折扣。
*   **冷启动问题**：检索增强部分依赖于历史调用记录，在没有任何历史数据的初始阶段，该组件的贡献有限。

（完）
