---
title: "Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback"
title_zh: 推理图：通过以证据为中心的思维链反馈实现确定性的智能体准确率
authors: Matthew Penaroza
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07595v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 推理图中以证据为中心的思维链反馈
tldr: 针对大语言模型智能体在处理相似查询时重复推理、准确率低且波动大的问题，本文提出“推理图”（Reasoning Graphs）。该方法将针对特定证据的思维链持久化为图结构，通过证据中心化的反馈机制，在处理新任务时回溯以往对相同证据的评价。结合检索图优化候选集，形成无需重训的自改进闭环，显著提升了多跳问答任务的准确性并降低了结果方差。
selection_source: fresh_fetch
motivation: 现有智能体在每次查询时都从零开始推理，导致思维链无法复用且相似查询的结果存在高度不确定性。
method: 提出一种将证据与评价逻辑关联的图结构，通过证据中心的回溯遍历提供反馈，并利用检索图优化候选证据流。
result: 在多跳问答基准测试中，该方法实现了准确率的持续提升和方差的显著下降，且所有决策过程均可追溯。
conclusion: 推理图通过上下文工程实现了智能体的确定性增强和自我进化，证明了结构化证据反馈在提升模型推理稳定性方面的有效性。
---

## 摘要
语言模型智能体在处理每个查询时都从零开始推理：每当智能体检索证据并进行审议时，思维链都会被丢弃，而下一个类似的查询在开始时没有任何先前的见解。这导致了较低的准确率和较高的方差，因为同类型的查询可能会不可预测地成功或失败。我们引入了推理图（reasoning graphs），这是一种图结构，它将智能体针对每个证据的思维链持久化为结构化边，并连接到它们所评估的证据项上。与以往将提炼后的策略存储为按查询相似度索引或按近期性追加的扁平记录的记忆机制不同，推理图实现了以证据为中心的反馈：给定一个新的候选集，系统会遍历所有先前运行中针对每个证据项的所有入边评估，从而揭示该特定项之前是如何被评判的。这种从证据向内的反向遍历在结构上是一种不同于查询相似度检索的能力，因为反馈与智能体当前正在检查的特定证据相关联，而不是与查询相关联。我们进一步引入了检索图（retrieval graphs），这是一种补充结构，它为流水线规划器提供输入，以便在连续运行中收紧候选漏斗。这两种图共同构成了一个自我改进的反馈循环：在连续运行中，准确率上升，方差骤减，且每个决策都可以通过图进行完全追溯。这种改进不需要重新训练；基础模型保持冻结，所有收益都来自于通过图遍历进行的上下文工程。我们对图结构、遍历算法和反馈机制进行了形式化，并描述了一种顺序集群评估协议，用于在多跳问答基准测试中衡量准确率收敛和方差骤减。

## Abstract
Language model agents reason from scratch on every query: each time an agent retrieves evidence and deliberates, the chain of thought is discarded and the next similar query starts with no prior insight. This produces lower accuracy and high variance, as the same type of query can succeed or fail unpredictably. We introduce reasoning graphs, a graph structure that persists an agent's per-evidence chain of thought as structured edges connected to the evidence items they evaluate. Unlike prior memory mechanisms that store distilled strategies as flat records indexed by query similarity or appended by recency, reasoning graphs enable evidence-centric feedback: given a new candidate set, the system traverses all incoming evaluation edges for each evidence item across all prior runs, surfacing how that specific item has been judged before. This backward traversal from evidence inward is a structurally different capability from query-similarity retrieval, because the feedback is tied to the specific evidence the agent is currently examining, not to the query. We further introduce retrieval graphs, a complementary structure that feeds a pipeline planner to tighten the candidate funnel over successive runs. Together, both graphs form a self-improving feedback loop: accuracy rises and variance collapses over successive runs, with every decision fully traceable through the graph. This improvement requires no retraining; the base model remains frozen and all gains come from context engineering via graph traversal. We formalize the graph structure, traversal algorithms, and feedback mechanisms, and describe a sequential cluster evaluation protocol for measuring accuracy convergence and variance collapse on multi-hop question answering benchmarks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“推理图”（Reasoning Graphs）的框架，旨在解决大语言模型（LLM）智能体在处理重复或相似任务时存在的推理冗余、准确率低及行为不一致（高方差）的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前的智能体在处理每个查询时都“从零开始”推理。即使多次遇到相同的证据材料，它们也会丢弃之前的思维链（CoT），导致对相同证据的评价在不同运行中出现不预测的波动（即“非确定性”）。
*   **核心问题**：现有的记忆机制（如 Reflexion 或 ReasoningBank）多基于查询相似度或近期性来检索策略，这导致反馈与智能体当前正在审视的**具体证据**脱节。
*   **整体含义**：论文旨在通过构建一种持久化的图结构，将智能体对每个证据的评价逻辑保留下来，实现“以证据为中心”的反馈，从而让智能体在后续任务中表现得更准确、更稳定且可审计。

### 2. 方法论
该框架由两个互补的图结构组成，通过上下文工程（Context Engineering）而非模型微调来实现自我改进。
*   **推理图 ($G_R$)**：
    *   **核心思想**：将智能体（Agent）、决策（Decision）和证据项（Evidence）作为节点，通过“评估边”（Evaluated Edges）连接。每条边记录了智能体对该证据的判定（采用或拒绝）及其自然语言理由。
    *   **反向遍历（核心技术）**：当新查询检索到一组候选证据时，系统会从每个证据项出发，反向遍历所有历史评价。
    *   **证据概览（Evidence Profile）**：系统筛选出历史中“正确决策”对应的评价，汇总其判定分布、代表性理由和可靠性评分，并将其注入到当前的 Prompt 中。
*   **检索图 ($G_P$)**：
    *   **核心思想**：记录决策与检索到的证据之间的元数据（排名、相似度等）。
    *   **流水线规划器**：识别那些在特定查询类型下被频繁拒绝的证据，并在后续运行中直接从候选漏斗中剔除，从而收紧检索范围并降低 Token 成本。
*   **反馈循环**：推理图优化“如何推理”，检索图优化“检索什么”，两者结合形成闭环，使准确率收敛且方差坍缩。

### 3. 实验设计
论文提出了一个详尽的评估协议，但需注意**该版本论文主要侧重于框架定义和实验方案设计**。
*   **数据集**：
    *   **HotpotQA**：多跳问答，侧重于在干扰项中识别关键证据。
    *   **MuSiQue**：更复杂的多跳推理（2-4步）。
*   **评估协议（Sequential Cluster Protocol）**：将查询按类型聚类，在每个集群内顺序处理查询，以模拟图结构的动态增长和反馈积累。
*   **对比方法（Baselines）**：
    *   Vanilla RAG（无反馈基准）。
    *   Reflexion（基于近期性的文本反馈）。
    *   ReasoningBank（基于查询相似度的策略检索）。
    *   Ours-QC（基于查询中心化的图检索，用于消融实验）。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该方法的核心在于“冻结基础模型”，通过外部图数据库存储和 Prompt 注入实现改进，因此不涉及模型重训的算力消耗。主要的计算开销在于图遍历和向量检索。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 8 个核心假设的验证实验，包括准确率收敛、方差坍缩、组件消融、噪声鲁棒性以及跨模型迁移实验。
*   **充分性评价**：从设计角度看，实验覆盖了从性能到鲁棒性的多个维度，且提出了“顺序集群协议”这一更符合智能体演化逻辑的评估方式。然而，由于文中**缺乏实际运行的实验数据结果**（属于 Framework/Proposal 性质），其客观效果尚待实证验证。

### 6. 主要结论与发现
*   **确定性准确率**：通过证据概览，智能体对相同证据的判定趋于一致，消除了随机性导致的错误。
*   **准确率收敛与方差坍缩**：随着运行次数增加，证据概览越丰富，准确率期望值单调递增，决策方差趋于零。
*   **全过程可审计**：每个决策都可以通过图遍历追溯到具体的证据评价逻辑，便于故障诊断。
*   **成本效益**：虽然注入证据概览增加了 Token，但检索图剔除无关证据带来的“漏斗收紧”效应有望抵消并降低净推理成本。

### 7. 优点
*   **视角独特**：从“查询中心”转向“证据中心”，解决了反馈相关性的根本问题。
*   **无需重训**：完全依赖上下文工程，适用于任何闭源或开源的冻结模型。
*   **结构化透明**：相比于扁平的文本记忆，图结构提供了极高的可解释性和审计能力。
*   **优雅降级**：在冷启动阶段（无历史数据），系统表现等同于标准 RAG，不会产生负面干扰。

### 8. 不足与局限
*   **缺乏实证数据**：论文目前处于理论框架和协议设计阶段，缺乏大规模实验结果支撑。
*   **冷启动延迟**：在积累足够的评价边之前，系统无法提供有效的反馈。
*   **存储增长**：评价边随运行次数线性增长，长期运行可能面临图数据库规模爆炸的问题。
*   **身份识别依赖**：系统依赖于证据项的唯一标识（如内容哈希），如果相同事实以不同表述出现，系统无法将其关联。
*   **噪声敏感性**：如果初始的“正确性信号”存在误判，可能会在反馈循环中放大错误的推理模式。

（完）
