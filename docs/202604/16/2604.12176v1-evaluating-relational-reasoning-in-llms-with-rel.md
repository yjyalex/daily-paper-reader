---
title: Evaluating Relational Reasoning in LLMs with REL
title_zh: 使用 REL 评估大语言模型中的关系推理能力
authors: "Lukas Fesser, Yasha Ektefaie, Ada Fang, Sham M. Kakade, Marinka Zitnik"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12176v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 评估大语言模型中的关系推理和复杂性
tldr: 本研究探讨了大语言模型（LLM）的关系推理能力，特别是高元关系绑定的挑战。作者提出了“关系复杂度（RC）”概念，即应用一个关系时必须同时绑定的独立实体数量。基于此构建了涵盖代数、化学和生物领域的生成式基准测试框架REL。实验发现，随着RC增加，即使实体总数不变，前沿LLM的性能也会显著下降，且增加推理计算量或上下文学习也无法解决该问题，揭示了模型在处理复杂关系绑定方面的局限性。
selection_source: fresh_fetch
motivation: 现有的关系推理评估往往混淆了输入规模与关系绑定难度的影响，缺乏对高元关系推理能力的深入探究。
method: 提出关系复杂度（RC）指标，并构建跨学科基准框架REL，通过在控制变量的前提下改变RC来评估模型性能。
result: 实验表明，随着RC的增加，前沿LLM的性能呈单调下降趋势，且该失效模式在增加推理步数或示例后依然存在。
conclusion: 研究识别出LLM在高元关系推理中的瓶颈，并建议从关系复杂度的视角重新审视现有的推理基准测试。
---

## 摘要
关系推理是指推断共同绑定多个实体、属性或变量之间关系的能力。这种能力是科学推理的核心，但现有对大语言模型（LLMs）关系推理能力的评估往往侧重于表格、图或合成任务等结构化输入，且未能分离出由高元关系绑定（higher-arity relational binding）带来的难度。我们通过关系复杂度（Relational Complexity, RC）的视角研究这一问题，并将其定义为应用某种关系时必须同时绑定的独立实体或操作数的最小数量。RC 提供了一种原则性的方法，可以在控制输入规模、词汇量和表示选择等混杂因素的同时，改变推理难度。基于 RC，我们推出了 REL，这是一个涵盖代数、化学和生物领域的生成式基准框架，在每个领域内改变 RC。在各类前沿 LLMs 中，随着 RC 的增加，性能呈现出一致且单调的下降，即使在实体总数保持不变的情况下也是如此。这种失效模式在增加测试时计算量和上下文学习的情况下依然存在，表明该限制与所需关系绑定的元数（arity）有关，而非推理步骤不足或缺乏示例。我们的研究结果确定了当前模型在处理高元推理时的困境，并促使人们从关系复杂度的视角重新审视基准测试。

## Abstract
Relational reasoning is the ability to infer relations that jointly bind multiple entities, attributes, or variables. This ability is central to scientific reasoning, but existing evaluations of relational reasoning in large language models often focus on structured inputs such as tables, graphs, or synthetic tasks, and do not isolate the difficulty introduced by higher-arity relational binding. We study this problem through the lens of Relational Complexity (RC), which we define as the minimum number of independent entities or operands that must be simultaneously bound to apply a relation. RC provides a principled way to vary reasoning difficulty while controlling for confounders such as input size, vocabulary, and representational choices. Building on RC, we introduce REL, a generative benchmark framework spanning algebra, chemistry, and biology that varies RC within each domain. Across frontier LLMs, performance degrades consistently and monotonically as RC increases, even when the total number of entities is held fixed. This failure mode persists with increased test-time compute and in-context learning, suggesting a limitation tied to the arity of the required relational binding rather than to insufficient inference steps or lack of exposure to examples. Our results identify a regime of higher-arity reasoning in which current models struggle, and motivate re-examining benchmarks through the lens of relational complexity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Relational Reasoning in LLMs with REL》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**大语言模型（LLM）在处理“高元关系绑定”（higher-arity relational binding）时的真实推理能力。**
*   **研究动机：** 尽管 LLM 在许多推理任务中表现出色，但现有的评估基准往往混淆了任务难度。性能下降可能是因为输入过长、背景知识不足或检索困难，而非推理本身的失败。
*   **核心概念：** 作者引入了认知科学中的**关系复杂度（Relational Complexity, RC）**。RC 定义为在执行一个推理步骤时，必须同时在内存中保持并处理的独立实体或变量的数量。
*   **整体含义：** 论文旨在通过隔离 RC 这一变量，揭示模型在处理需要同时整合多个约束条件的科学推理任务（如代数、生物、化学）时的瓶颈。

### 2. 论文提出的方法论
核心思想是构建一个名为 **REL** 的生成式基准框架，通过参数化设计来精确控制任务的 RC。
*   **关系复杂度 (RC) vs. 操作数复杂度 (OC)：** 
    *   **RC** 指关系的“元数”（arity），即必须同时绑定的“槽位”数量。
    *   **OC** 指识别或表示单个实体本身的难度。
*   **REL 框架设计：**
    *   **REL-A (代数)：** 将瑞文标准推理矩阵（RPM）扩展为“瑞文张量”（RPT）。通过增加维度和邻域规则，使 RC 从 1（常量规则）提升到 80+（高维邻域求和）。
    *   **REL-B (生物)：** 检测系统发育树中的“趋同演化”（Homoplasy）。RC 等于参与趋同演化的分类群（Taxa）数量。模型必须同时比对序列并验证其在进化树上的远缘关系。
    *   **REL-C (化学)：** 涉及同分异构体分类（C1）、最大公共子结构提取（C2）和缺失异构体补全（C3）。RC 随需要同时比对的分子数量和结构约束而增加。

### 3. 实验设计
*   **数据集/场景：** 涵盖代数（矩阵/张量补全）、生物（多序列比对+进化树分析）、化学（SMILES 字符串处理）。
*   **Benchmark：** REL 包含约 9,116 个生成的问题（REL-A: 3,500; REL-B: 2,600; REL-C: 3,016）。
*   **对比方法（评估模型）：** 选取了当时最前沿的模型：**Claude Opus 4.5**、**Gemini 3 Pro Preview** 和 **GPT 5.2**。
*   **控制变量：** 实验通过多元回归分析，控制了输入长度、实体数量、序列长度等混杂因素，以确保性能下降确实是由 RC 引起的。

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的训练算力（如 GPU 型号或训练时长），因为其主要评估对象是闭源的前沿模型 API。
*   **评估成本：** 文中提到了部分实验的规模，例如在 REL-B1 任务中，针对三个模型共进行了约 7,800 次 API 调用。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的系统性评估，总计超过 9,000 个测试用例。
*   **充分性：** 实验设计非常充分。除了主实验外，还包括：
    *   **消融实验：** 验证 RC 是否比输入长度等因素更能解释性能下降。
    *   **测试时计算量（Test-Time Compute）：** 增加“思考 Token”数量（最高至 16k）的实验。
    *   **上下文学习（ICL）：** 少样本提示实验。
    *   **工具使用：** 为模型提供 RDKit 等专业化学工具的对比实验。
*   **客观性：** 通过生成式框架避免了数据污染，并使用多元回归分析来量化 RC 的独立贡献，实验设计客观且严谨。

### 6. 主要结论与发现
*   **RC 是性能杀手：** 随着 RC 的增加，所有模型的性能均呈单调、剧烈的下降。即使实体总数固定，仅增加关系元数也会导致模型崩溃。
*   **RC 的解释力最强：** 在生物任务中，RC 解释了 24%-44% 的性能方差，远超输入长度或序列复杂度。
*   **现有手段难以修复：** 增加测试时的推理步数（Thinking tokens）或提供上下文示例（ICL）虽有微小提升，但无法消除高 RC 带来的根本性失败。
*   **工具局限性：** 即使引入外部化学工具，模型在需要高元绑定的任务（如补全异构体空间）上依然表现不佳。

### 7. 优点
*   **理论深度：** 将认知科学的 RC 理论引入 LLM 评估，为“推理难度”提供了一个任务无关的科学定义。
*   **跨学科验证：** 任务设计跨越多个科学领域，证明了 RC 瓶颈的普遍性。
*   **参数化生成：** REL 框架可以无限生成新题，有效防止了模型通过记忆 benchmark 来“刷分”。
*   **严谨的统计分析：** 使用多元回归和共线性分析来排除混杂因素，结论极具说服力。

### 8. 不足与局限
*   **任务合成性：** 虽然基于科学背景，但任务形式（如瑞文张量）仍具有一定的合成性质，与真实的科研发现过程尚有距离。
*   **评估形式：** 部分任务采用多选题形式，可能无法完全捕捉模型推理过程中的细微错误。
*   **上下文限制：** 在极高 RC 的任务中，输入规模的机械性增长（如超大矩阵）可能触及模型的上下文窗口限制，导致无法区分是“推理失败”还是“溢出失败”。
*   **应用范围：** 目前主要集中在自然科学领域，尚未验证该 RC 框架在法律、伦理或常识推理中的适用性。

（完）
