---
title: Pareto-Optimal Offline Reinforcement Learning via Smooth Tchebysheff Scalarization
title_zh: 通过平滑切比雪夫标量化实现帕累托最优离线强化学习
authors: "Aadyot Bhatnagar, Peter Mørch Groth, Ali Madani"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13175v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于大模型多奖励对齐的离线强化学习
tldr: 针对多目标离线强化学习中线性标量化无法处理非凸帕累托前沿的问题，本文提出了STOMP算法。该方法利用平滑切比雪夫标量化技术，将多目标偏好对齐转化为单目标优化问题，并结合奖励标准化处理冲突目标。在蛋白质工程任务上的实验表明，STOMP在多个数据集和模型上均优于现有基准，能有效提升多属性对齐性能。
selection_source: fresh_fetch
motivation: 传统的线性奖励标量化方法在处理多个冲突目标时，无法有效覆盖帕累托前沿的非凸区域。
method: 提出STOMP算法，利用平滑切比雪夫标量化技术对多目标进行标量化，并根据观测分布对个体奖励进行标准化。
result: 在蛋白质工程任务的9个实验设置中，STOMP在8个设置中均取得了最高的超体积指标，表现优于最先进的基准方法。
conclusion: STOMP为多属性蛋白质优化及其他多目标对齐场景提供了一种原则性强且鲁棒的离线强化学习解决方案。
---

## 摘要
大语言模型可以通过在小型标记数据集上进行离线强化学习（RL）来与人类偏好对齐。虽然单目标对齐已得到深入研究，但许多现实应用需要同时优化多个相互冲突的奖励，例如在蛋白质工程中同时优化催化活性和特异性，或者在聊天机器人中同时优化帮助性和无害性。先前的研究主要依赖于线性奖励标量化，但这种方法已被证明无法恢复帕累托前沿（Pareto front）的非凸区域。在本文中，我们并非直接对奖励进行标量化，而是将多目标强化学习本身构建为一个优化问题，并通过平滑切比雪夫标量化（一种克服了线性标量化缺点的最新技术）进行标量化。我们利用这一公式推导出了多目标偏好的平滑切比雪夫优化（STOMP），这是一种新型离线强化学习算法，它通过根据观察到的分布对单个奖励进行标准化，以原则性的方式将直接偏好优化扩展到多目标设置。我们通过在三个蛋白质适应度实验室数据集上对三个自回归蛋白质语言模型进行对齐，在多项蛋白质工程任务中对 STOMP 进行了实证验证。与最先进的基准模型相比，根据离线离策略评估和生成式评估，STOMP 在九个设置中的八个设置里都实现了最高的超体积。因此，我们证明了 STOMP 是一种强大且鲁棒的多目标对齐算法，能够显著改进用于多属性蛋白质优化及其他领域的训练后模型。

## Abstract
Large language models can be aligned with human preferences through offline reinforcement learning (RL) on small labeled datasets. While single-objective alignment is well-studied, many real-world applications demand the simultaneous optimization of multiple conflicting rewards, e.g. optimizing both catalytic activity and specificity in protein engineering, or helpfulness and harmlessness for chatbots. Prior work has largely relied on linear reward scalarization, but this approach provably fails to recover non-convex regions of the Pareto front. In this paper, instead of scalarizing the rewards directly, we frame multi-objective RL itself as an optimization problem to be scalarized via smooth Tchebysheff scalarization, a recent technique that overcomes the shortcomings of linear scalarization. We use this formulation to derive Smooth Tchebysheff Optimization of Multi-Objective Preferences (STOMP), a novel offline RL algorithm that extends direct preference optimization to the multi-objective setting in a principled way by standardizing the individual rewards based on their observed distributions. We empirically validate STOMP on a range of protein engineering tasks by aligning three autoregressive protein language models on three laboratory datasets of protein fitness. Compared to state-of-the-art baselines, STOMP achieves the highest hypervolumes in eight of nine settings according to both offline off-policy and generative evaluations. We thus demonstrate that STOMP is a powerful, robust multi-objective alignment algorithm that can meaningfully improve post-trained models for multi-attribute protein optimization and beyond.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pareto-Optimal Offline Reinforcement Learning via Smooth Tchebysheff Scalarization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在将大语言模型（LLM）或蛋白质语言模型（PLM）与人类偏好或生物属性对齐时，现实场景往往涉及多个**相互冲突的目标**（例如：聊天机器人的“帮助性”与“无害性”，或蛋白质的“催化活性”与“特异性”）。
*   **现有局限**：主流方法采用**线性奖励标量化**（Linear Scalarization），即对多个奖励进行加权平均。然而，数学上已证明线性标量化**无法捕捉帕累托前沿（Pareto Front）中的非凸区域**，而这些区域往往代表了不同目标之间最关键的权衡点。
*   **研究目标**：提出一种能够覆盖完整帕累托前沿、对奖励尺度不敏感且易于梯度优化的离线强化学习算法。

### 2. 方法论：STOMP 算法
论文提出了 **STOMP**（Smooth Tchebysheff Optimization of Multi-Objective Preferences），其核心思想是将多目标 RL 视为一个整体优化问题进行标量化，而非简单地对奖励函数求和。

*   **核心思想**：利用**平滑切比雪夫标量化（Smooth Tchebysheff Scalarization, STS）**。STS 通过 Log-Sum-Exp 形式近似切比雪夫范数，既保留了寻找所有帕累托最优解的能力，又保证了目标函数的可微性。
*   **关键技术细节**：
    *   **分布相对奖励（Distribution-Relative Reward）**：为了解决 STS 对奖励尺度极度敏感的问题，STOMP 根据离线数据集的观测分布，利用配分函数 $Z_i(x)$ 对每个奖励进行动态标准化。
    *   **策略依赖与独立奖励**：推导出了两种形式的标量化奖励：一种依赖于当前策略（用于损失函数计算以获得更紧的界），另一种独立于策略（用于确定偏好对）。
    *   **损失函数**：在 OffsetDPO 的基础上，结合了 STS 标量化奖励差值和负对数似然（NLL）正则项，以防止离线训练中的似然退化。

### 3. 实验设计
研究主要聚焦于**蛋白质工程**领域，验证模型在优化多个生物属性方面的能力。
*   **数据集**：
    1.  **DHFR**：优化二氢叶酸还原酶在有/无抑制剂环境下的活性（目标间不相关）。
    2.  **PbrR**：优化铅离子结合的特异性与减少锌离子干扰（目标间强负相关）。
    3.  **$\alpha$-Amylase**：同时优化活性、表达量和热稳定性（目标间正相关）。
*   **对比基准（Baselines）**：
    *   **DPO-Lin / ODPO-Lin**：使用线性标量化的直接偏好优化及其变体。
    *   **ODPO-STZ**：使用基于 Z-score 标准化的朴素平滑切比雪夫方法。
*   **评估指标**：**超体积（Hypervolume）**，这是衡量多目标优化算法覆盖帕累托前沿广度与深度的标准指标。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA H200 GPU**。
*   **模型规模**：涵盖了 ProGen3-3B（30亿参数）以及两个检索增强模型 ProGen-RA-3B 和 ProGen-RA-10B（最高100亿参数）。
*   **训练时长**：每个模型训练约 **3 小时**。
*   **总实验量**：跨越不同基础模型、数据集、算法和偏好向量 $\lambda$，总共训练并评估了 **84 个模型**。

### 5. 实验数量与充分性
实验设计非常**充分且具有说服力**：
*   **多维度覆盖**：测试了目标间不同相关性（正相关、负相关、不相关）的场景。
*   **双重评估体系**：
    1.  **离线离策略评估**：使用加权重要性采样（Weighted Importance Sampling）在测试集上估计期望奖励。
    2.  **生成式评估**：实际生成新序列，并使用高斯过程（GP）奖励模型作为“预言机”评估生成质量。
*   **公平性**：所有对比方法在相同的超参数搜索空间下进行微调，并针对每个方法单独调整了正则化系数 $\alpha$ 以确保 perplexity 不退化。

### 6. 主要结论与发现
*   **性能领先**：在 9 个实验设置（3数据集 × 3模型）中，STOMP 在 **8 个设置中取得了最高的超体积指标**。
*   **鲁棒性强**：相比于 ODPO-STZ 在不同任务中表现剧烈波动，STOMP 在各种基础模型和数据集上表现极其稳定。
*   **帕累托前沿覆盖**：可视化结果显示，STOMP 能够显著识别出那些线性方法无法触达的“折中”序列（即多个属性均表现优异的稀有变体）。

### 7. 优点
*   **理论严谨**：从最大熵强化学习（MaxEnt RL）框架出发，推导出了具有原则性的奖励标准化方法。
*   **无需繁琐调参**：通过配分函数自动处理不同奖励的量纲问题，减少了人工设置缩放因子的需求。
*   **通用性**：虽然在蛋白质上验证，但算法逻辑完全适用于文本、图像等任何需要多目标对齐的 LLM 领域。

### 8. 不足与局限
*   **配分函数估计**：算法依赖于对离线数据配分函数 $Z_i(x)$ 的准确估计，在数据极度稀疏或分布严重偏移时可能存在偏差。
*   **计算开销**：相比线性标量化，STS 的 Log-Sum-Exp 计算略微增加了计算复杂度（虽然在训练中可忽略不计）。
*   **领域局限**：论文未在通用的文本对齐任务（如 Helpfulness vs Harmlessness）上提供实验数据，尽管其理论上是通用的。

（完）
