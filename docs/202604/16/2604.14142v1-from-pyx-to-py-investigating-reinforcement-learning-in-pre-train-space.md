---
title: "From $P(y|x)$ to $P(y)$: Investigating Reinforcement Learning in Pre-train Space"
authors: "Yuqiao Tan, Minzheng Wang, Bo Liu, Zichen Liu, Tian Liang, Shizhu He, Jun Zhao, Kang Liu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14142v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 在预训练空间进行强化学习以增强大模型推理能力
tldr: 本研究探讨了在预训练空间（优化边缘分布 P(y)）进行强化学习以突破传统 RLVR 受限于基础模型分布的瓶颈。提出了 PreRL 方法，通过奖励驱动的在线更新直接作用于预训练空间，并发现其中的负样本强化（NSR）能有效剪枝错误推理路径并激发模型反思。基于此，研究进一步提出 Dual Space RL (DSRL) 策略，通过预训练空间剪枝与标准 RL 细粒度优化的结合，显著提升了 LLM 的推理性能。
selection_source: fresh_fetch
motivation: "传统强化学习优化条件分布 P(y|x) 受限于模型既有分布，而预训练空间优化虽具潜力却面临静态语料导致的分布偏移挑战。"
method: 提出 PreRL 方法在预训练空间进行在线奖励驱动更新，并结合负样本强化机制构建了先剪枝后优化的双空间强化学习框架 DSRL。
result: 实验显示 PreRL 与标准 RL 梯度高度对齐，且 DSRL 显著超越基线，使模型的转换和反思性思维分别提升了 14.89 倍和 6.54 倍。
conclusion: 通过在预训练空间进行负样本剪枝可以有效引导模型进入精细的正确推理子空间，是增强大模型推理能力的有效途径。
---

## Abstract
While reinforcement learning with verifiable rewards (RLVR) significantly enhances LLM reasoning by optimizing the conditional distribution P(y|x), its potential is fundamentally bounded by the base model's existing output distribution. Optimizing the marginal distribution P(y) in the Pre-train Space addresses this bottleneck by encoding reasoning ability and preserving broad exploration capacity. Yet, conventional pre-training relies on static corpora for passive learning, leading to a distribution shift that hinders targeted reasoning enhancement. In this paper, we introduce PreRL (Pre-train Space RL), which applies reward-driven online updates directly to P(y). We theoretically and empirically validate the strong gradient alignment between log P(y) and log P(y|x), establishing PreRL as a viable surrogate for standard RL. Furthermore, we uncover a critical mechanism: Negative Sample Reinforcement (NSR) within PreRL serves as an exceptionally effective driver for reasoning. NSR-PreRL rapidly prunes incorrect reasoning spaces while stimulating endogenous reflective behaviors, increasing transition and reflection thoughts by 14.89x and 6.54x, respectively. Leveraging these insights, we propose Dual Space RL (DSRL), a Policy Reincarnation strategy that initializes models with NSR-PreRL to expand the reasoning horizon before transitioning to standard RL for fine-grained optimization. Extensive experiments demonstrate that DSRL consistently outperforms strong baselines, proving that pre-train space pruning effectively steers the policy toward a refined correct reasoning subspace.

---

## 论文详细总结（自动生成）

这篇论文探讨了在大语言模型（LLM）推理任务中，如何通过在“预训练空间”进行强化学习来突破现有技术的瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的可验证奖励强化学习（RLVR）主要优化条件分布 $P(y|x)$（即给定问题 $x$ 生成答案 $y$），但这受限于基础模型已有的输出分布。而传统的预训练虽然优化边缘分布 $P(y)$，但依赖静态语料，存在分布偏移，难以针对性增强推理能力。
*   **研究动机**：作者试图探索是否可以直接在预训练空间（即不考虑问题输入 $x$ 的情况下优化 $P(y)$）引入奖励驱动的在线更新，从而将推理能力更深层地内化到模型参数中，并保留更广阔的探索空间。

### 2. 方法论：核心思想与关键技术
*   **PreRL (Pre-train Space RL)**：核心思想是在强化学习更新梯度时，移除对输入问题 $x$ 的依赖，直接优化边缘似然 $\log P(y)$。
*   **梯度对齐理论**：论文从理论和实证两方面证明了 $\log P(y)$ 和 $\log P(y|x)$ 的梯度具有很强的正向对齐性，这使得 PreRL 可以作为标准 RL 的有效替代方案。
*   **NSR-PreRL (负样本强化)**：研究发现 PreRL 中正样本强化（PSR）容易导致模型崩溃，而**负样本强化（NSR）**极其有效。它通过在预训练空间剪枝错误的推理路径，能够显著激发模型的内生反思行为。
*   **DSRL (Dual Space RL，双空间强化学习)**：提出一种“策略转世（Policy Reincarnation）”策略。
    *   **阶段 1**：使用 NSR-PreRL 进行初始化（Warmup），通过预训练空间剪枝扩大推理视野并激发反思能力。
    *   **阶段 2**：切换回标准 RL（优化 $P(y|x)$），进行细粒度的策略优化。

### 3. 实验设计
*   **模型与数据**：使用 Qwen3-4B 和 Qwen3-8B 作为基础模型；在 MATH 数据集（7,500 题）上进行训练。
*   **Benchmark**：
    *   **数学推理**：MATH500, AMC23, AIME24, AIME25, Minerva, OlympiadBench。
    *   **泛化性 (OOD)**：GPQA-Diamond, MMLU-Pro, BBH, HumanEval。
*   **对比方法**：包括 PPO, Reinforce++, RLOO, GRPO 以及优化版 Dr.GRPO 和 DAPO。

### 4. 资源与算力
*   **框架**：使用 `verl` 强化学习框架和 `vLLM` 推理加速引擎。
*   **超参数**：Batch Size 为 128，每个 Prompt 生成 8 个 Rollout，学习率 1e-6，训练 6 个 Epoch。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）及总算力消耗时长，但提到了训练过程中的样本效率（DSRL 达到相同精度所需的步数比 GRPO 少 1.6x - 2.5x）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个模型尺寸（4B 和 8B），在 6 个主流数学竞赛数据集和 4 个通用能力数据集上进行了测试。
*   **充分性**：
    *   进行了详细的消融实验（如 Warmup 步数的影响、预训练空间 vs 后训练空间 Warmup 的对比）。
    *   通过 Pass@K (K 达 256) 评估了采样缩放能力。
    *   对推理行为（反思、转换、执行）进行了量化分析。
*   **评价**：实验设计较为全面，通过多维度指标（Avg@32, Pass@K）保证了结果的客观性。

### 6. 主要结论与发现
*   **PreRL 的可行性**：证明了优化 $P(y)$ 确实能带动 $P(y|x)$ 的提升。
*   **反思能力的爆发**：NSR-PreRL 仅需少量步数就能让模型的“转换思维”增加 14.89 倍，“反思思维”增加 6.54 倍。
*   **DSRL 的优越性**：DSRL 在所有基准测试中均优于标准 RL，尤其在 AIME 等高难度任务上提升显著（如 AIME24 提升 4.69 分）。
*   **效率提升**：DSRL 具有更高的样本效率，能更快达到性能饱和点。

### 7. 优点
*   **视角新颖**：打破了 RL 仅在 $P(y|x)$ 空间优化的惯性思维，从预训练空间切入。
*   **机制深刻**：揭示了负样本强化（NSR）在剪枝错误推理路径和激发“慢思考”中的关键作用。
*   **策略实用**：DSRL 框架简单有效，易于集成到现有的 RL 工作流中。

### 8. 不足与局限
*   **超参数敏感性**：Warmup 的步数（S）对最终效果影响较大，呈现倒 U 型曲线，需要精细调优。
*   **任务局限**：目前主要集中在具有“可验证奖励”的数学和逻辑推理任务，在主观性较强的通用对话任务上的效果尚待验证。
*   **长度控制**：NSR-PreRL 会导致响应长度迅速增加，虽然带来了更多思考，但也增加了推理成本和训练难度。

（完）
