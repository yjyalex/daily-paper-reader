---
title: "TREX: Automating LLM Fine-tuning via Agent-Driven Tree-based Exploration"
title_zh: TREX：通过智能体驱动的树状探索实现大语言模型微调自动化
authors: "Zerun Ma, Guoqiang Wang, Xinchen Xie, Yicheng Chen, He Du, Bowen Li, Yanan Sun, Wenran Liu, Kai Chen, Yining Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14116v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于自动化大语言模型训练工作流的多智能体系统
tldr: TREX是一个旨在自动化大语言模型（LLM）微调全流程的多智能体系统。它通过“研究员”和“执行器”模块的协作，涵盖了从需求分析、文献调研到模型训练与评估的完整生命周期。该系统将多轮实验建模为搜索树，通过高效规划路径和提炼迭代经验来优化训练策略。在包含10个真实任务的FT-Bench基准测试中，TREX显著提升了模型在特定领域的性能，为自动化AI研发提供了有力工具。
selection_source: fresh_fetch
motivation: 旨在解决现有AI智能体在处理如LLM训练等复杂、多阶段的真实科研工作流时面临的自动化挑战。
method: 构建了一个由研究员和执行器组成的多智能体系统，采用基于树的探索机制来规划实验、复用历史结果并提炼高层见解。
result: 在涵盖基础能力优化和领域特定任务的FT-Bench基准测试中，TREX在所有10个任务上均实现了模型性能的持续优化。
conclusion: TREX通过多智能体协作和树状搜索策略，成功实现了LLM微调全生命周期的自动化，证明了其在复杂AI任务中的有效性。
---

## 摘要
虽然大语言模型（LLMs）已赋能 AI 研究智能体执行孤立的科学任务，但自动化复杂的现实世界工作流（如 LLM 训练）仍是一项重大挑战。在本文中，我们介绍了 TREX，这是一个能够自动化整个 LLM 训练生命周期的多智能体系统。通过协调两个核心模块——研究员（Researcher）和执行器（Executor）之间的协作，该系统能够无缝执行需求分析、开放域文献和数据调研、训练策略制定、数据配方准备以及模型训练与评估。多轮实验过程被建模为一棵搜索树，使系统能够高效规划探索路径、复用历史结果，并从迭代尝试中提取高层见解。为了评估自动化 LLM 训练的能力，我们构建了 FT-Bench，这是一个包含 10 个源自真实场景任务的基准测试，涵盖了从优化基础模型能力到增强特定领域任务性能的各种任务。实验结果表明，TREX 智能体能够持续优化目标任务上的模型性能。

## Abstract
While Large Language Models (LLMs) have empowered AI research agents to perform isolated scientific tasks, automating complex, real-world workflows, such as LLM training, remains a significant challenge. In this paper, we introduce TREX, a multi-agent system that automates the entire LLM training life-cycle. By orchestrating collaboration between two core modules-the Researcher and the Executor-the system seamlessly performs requirement analysis, open-domain literature and data research, formulation of training strategies, preparation of data recipes, and model training and evaluation. The multi-round experimental process is modeled as a search tree, enabling the system to efficiently plan exploration paths, reuse historical results, and distill high-level insights from iterative trials. To evaluate the capability of automated LLM training, we construct FT-Bench, a benchmark comprising 10 tasks derived from real-world scenarios, ranging from optimizing fundamental model capabilities to enhancing performance on domain-specific tasks. Experimental results demonstrate that the TREX agent consistently optimizes model performance on target tasks.

---

## 论文详细总结（自动生成）

### TREX：通过智能体驱动的树状探索实现大语言模型微调自动化

#### 1. 核心问题与整体含义
论文的核心问题是如何实现**大语言模型（LLM）微调全生命周期的自动化**。
*   **研究动机**：尽管 AI 智能体在文献综述、代码编写等孤立任务上表现出色，但 LLM 训练这一复杂工作流仍高度依赖人类专家。其挑战在于：微调涉及数据分布、算法选择、超参优化等多个开放性维度；训练数据规模巨大，无法直接放入智能体上下文；且 LLM 训练的时间和计算成本极高，传统的暴力搜索或简单进化算法效率低下。
*   **整体含义**：TREX 旨在构建一个闭环系统，通过多智能体协作和启发式搜索，将人类专家的“实验-分析-改进”范式转化为自动化的机器流程。

#### 2. 方法论
TREX 采用双环工作流架构，核心思想是将实验过程建模为**搜索树**。
*   **双智能体协作（内环）**：
    *   **研究员（Researcher）**：负责高层决策。进行文献调研（arXiv/Hugging Face）、制定实验计划、分析实验结果（包括 Bad-case 归因分析）。
    *   **执行器（Executor）**：基于 OpenHands 构建，负责具体实现。它在 GPU 集群的沙箱环境中编写代码、调用 **AIDP（AI 数据处理库）** 进行数据清洗、执行模型训练与评估。
*   **树状探索机制（外环）**：
    *   **MCTS（蒙特卡洛树搜索）**：利用 UCT 公式 $UCT(v) = \frac{Q_v}{N_v} + c \cdot \sqrt{\frac{\ln N_{P(v)}}{N_v}}$ 来平衡“利用”（选择表现好的路径）与“探索”（尝试新策略）。
    *   **内存上下文管理**：通过“冷凝”（Condense）技术，将实验轨迹、兄弟节点信息和关键成败案例整合进有限的上下文窗口，确保智能体具备长期记忆。

#### 3. 实验设计
*   **基准测试（FT-Bench）**：论文构建了首个专门针对 LLM 微调的基准测试，包含 **10 个真实任务**，如 ACI-Bench（临床笔记生成）、TOMG-Bench（分子生成）、LawBench（法律知识）和 GTA（工具调用）等。
*   **对比方法**：
    *   **Base 模型**：以 Qwen3-1.7B 为基础模型。
    *   **智能体后端**：对比了 Gemini 3 Pro 和 Qwen3-Next-80B-Thinking 作为 Researcher 的表现。
    *   **人类专家**：在分子生成和金融问答任务中，直接对比了 TREX 自动生成的配方与人类专家设计的 SFT/RL 方案。
*   **消融实验**：对比了 MCTS 与贪婪搜索（GBFS）、顺序搜索（SES）的效果；验证了 AIDP 工具库和 Bad-case 分析对性能提升的贡献。

#### 4. 资源与算力
*   **算力配置**：论文提到 Executor 部署在 GPU 集群上。在具体的实验计划示例中，单次训练任务通常配置 **4 张 GPU**。
*   **规模限制**：为了保证迭代效率，实验统一使用轻量级的 **Qwen3-1.7B** 作为被训练模型，且单次实验的训练样本量上限设定为 **50,000 条**。
*   **迭代成本**：每个任务允许最多探索 **20 轮**实验迭代。

#### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同领域的任务上进行了完整测试，总计进行了数百次模型训练任务（10 任务 × 20 轮 × 每轮可能的并行配置）。
*   **充分性与客观性**：实验设计较为充分，涵盖了从通用能力到垂直领域的广泛场景。通过引入“归一化相对增益”指标，消除了不同任务间度量衡不一的影响。对比人类专家的实验显示了 TREX 在特定任务上具有超越专家的潜力，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **自动化可行性**：TREX 能够自主完成从数据合成、配方优化到模型训练的全过程，且在所有 10 个任务上均实现了性能持续增长。
*   **超越专家**：在分子生成（TOMG-Bench）等任务中，TREX 发现的训练方案优于人类专家在 Llama 系列模型上设计的方案。
*   **后端依赖性**：系统的表现高度依赖于 Researcher 后端 LLM 的推理能力，Gemini 3 Pro 在策略多样性和成功率上显著优于开源模型。
*   **策略多样性**：TREX 能够自主尝试建立基准、精炼数据流水线、合成数据及调整训练方案（如 LoRA vs 全量微调）。

#### 7. 优点
*   **全流程闭环**：不仅是超参搜索，还涵盖了数据工程和文献调研，更接近真实的科研过程。
*   **高效搜索策略**：引入 MCTS 解决了 LLM 训练成本高昂导致的搜索空间爆炸问题。
*   **工具化支撑**：开发的 AIDP 库为智能体提供了标准化的数据处理原语，降低了代码生成的错误率。
*   **填补空白**：FT-Bench 为自动化 AI 研究（AI for AI）提供了急需的评估标准。

#### 8. 不足与局限
*   **模型规模受限**：实验主要集中在 1.7B 的小模型上，尚未验证该系统在百亿或千亿级参数模型微调中的稳定性和成本效益。
*   **后端模型依赖**：系统核心逻辑依赖于顶级闭源模型（如 Gemini/Claude），在完全脱离这些模型时的自主研究能力有待提升。
*   **计算成本**：尽管使用了 MCTS，但 20 轮迭代对于资源受限的团队来说依然是一笔不小的开销。
*   **任务复杂度**：目前的任务仍属于有明确评估指标的范畴，对于目标模糊或需要主观审美评价的微调任务（如对齐人类偏好）挑战更大。

（完）
