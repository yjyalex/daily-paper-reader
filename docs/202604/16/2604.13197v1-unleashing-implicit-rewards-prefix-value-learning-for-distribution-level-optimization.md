---
title: "Unleashing Implicit Rewards: Prefix-Value Learning for Distribution-Level Optimization"
title_zh: 释放隐式奖励：面向分布级优化的前缀价值学习
authors: "Shiping Gao, Hongzhan Chen, Xiaojun Quan, Qifan Wang, Lifu Huang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13197v1"
tags: ["query:token"]
score: 9.0
evidence: 从轨迹级标签学习 Token 或步骤级奖励
tldr: 本文针对过程奖励模型（PRM）依赖昂贵标注的问题，提出了隐式前缀价值奖励模型（IPVRM）。该模型通过直接学习前缀条件的价值函数，并利用时序差分（TD）派生步骤信号，解决了传统隐式PRM中token级信用分配不准的挑战。此外，研究者还提出了分布级强化学习（DistRL），利用校准后的价值函数进行密集的反事实更新。实验表明，IPVRM显著提升了步骤验证的准确性，且DistRL能有效增强下游推理任务的性能。
selection_source: fresh_fetch
motivation: 传统隐式PRM在训练和推理阶段存在不匹配，导致token级奖励无法准确反映推理步骤的真实质量，限制了其在在线强化学习中的应用。
method: 提出IPVRM直接学习前缀价值函数并利用TD差分获取步骤信号，同时结合DistRL对采样及高概率候选token进行分布级的优势函数更新。
result: IPVRM在ProcessBench上的步骤验证F1分数显著提高，且DistRL在与校准后的IPVRM配合时，能持续提升模型的下游推理表现。
conclusion: 通过精确的前缀价值建模和分布级优化，可以有效解决隐式奖励的信用分配问题，从而在无需昂贵标注的情况下提升大模型的推理能力。
---

## 摘要
过程奖励模型 (PRMs) 在推理过程中提供细粒度的奖励信号，但训练可靠的 PRM 通常需要步骤级标注或繁重的验证流水线，这使得它们在在线强化学习 (RL) 期间的扩展和更新成本高昂。隐式 PRM 通过从轨迹级的结果标签中学习可分解的 token 级或步骤级奖励来降低这一成本。然而，它们面临训练与推理不匹配的问题：训练仅约束序列级的聚合值，而推理则需要 token 级的评分来反映局部步骤的质量。结果导致 token 级的信用归因识别较弱，可能无法忠实地反映哪些推理步骤实际上是正确的。这种不可靠性削弱了隐式 PRM 的一个关键承诺：对许多候选 token 进行评分。在实践中，带有噪声的逐 token 优势可能会系统性地强化错误的后续生成。我们通过一种新型的隐式前缀价值奖励模型 (IPVRM) 来解决这一问题，该模型直接学习一个以前缀为条件的价值函数，用于估计最终正确的概率，并通过时序差分 (TD) 差值导出步骤信号。IPVRM 显著提升了 ProcessBench 上的步骤验证 F1 分数。基于这些校准后的前缀价值，我们进一步提出了分布级强化学习 (DistRL)，它为采样得到的 token 和高概率候选 token 同时计算 TD 优势，从而在无需额外采样的情况下实现密集的反事实更新。虽然在由未校准的隐式奖励驱动时 DistRL 的收益有限，但一旦与 IPVRM 结合，它就能持续提升下游推理性能。

## Abstract
Process reward models (PRMs) provide fine-grained reward signals along the reasoning process, but training reliable PRMs often requires step annotations or heavy verification pipelines, making them expensive to scale and refresh during online RL. Implicit PRMs mitigate this cost by learning decomposable token- or step-level rewards from trajectory-level outcome labels. However, they suffer from a train-inference mismatch: training only constrains a sequence-level aggregate, whereas inference requires token-level scores to reflect local step quality. As a result, token-level credits are weakly identified and may fail to faithfully reflect which reasoning steps are actually correct. This unreliability undermines a key promise of implicit PRMs: scoring many candidate tokens. In practice, noisy per-token advantages may systematically reinforce incorrect continuations. We address this problem with a novel Implicit Prefix-Value Reward Model (IPVRM), which directly learns a prefix-conditioned value function estimating the probability of eventual correctness, and derives step signals via temporal-difference (TD) differences. IPVRM substantially improves step-verification F1 on ProcessBench. Building on these calibrated prefix values, we further propose Distribution-Level RL (DistRL), which computes TD advantages for both sampled tokens and high-probability candidate tokens, enabling dense counterfactual updates without additional rollouts. While DistRL offers limited gains when powered by miscalibrated implicit rewards, it consistently improves downstream reasoning once paired with IPVRM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unleashing Implicit Rewards: Prefix-Value Learning for Distribution-Level Optimization》的结构化深度总结：

### 1. 论文的核心问题与整体含义
论文聚焦于大语言模型（LLM）推理任务中的**信用归因（Credit Assignment）**问题。
*   **研究动机**：传统的“过程奖励模型”（PRM）虽然能提供细粒度信号，但依赖昂贵的步骤级人工标注。现有的“隐式 PRM”虽能从结果标签中学习，但存在**训练与推理不匹配**的问题：训练时优化的是整个序列的聚合奖励，而推理时却需要准确的 token 级评分。这导致隐式奖励模型在定位中间步骤错误时表现不佳，容易捕捉到格式等表面特征而非逻辑正确性。
*   **核心目标**：开发一种既能保持隐式学习的低成本，又能提供高可靠、可用于分布级优化的细粒度奖励信号的模型。

### 2. 论文提出的方法论
论文提出了一个统一框架，包含两个核心组件：
*   **隐式前缀价值奖励模型 (IPVRM)**：
    *   **核心思想**：将隐式奖励重新定义为“前缀条件价值函数”。它直接学习估计当前前缀 $s_t$ 最终导致正确结果的概率 $P(r_o=1|s_t)$。
    *   **技术细节**：采用带有边际（Margin）的二元交叉熵（BCE）损失函数，对每个前缀进行监督。遵循“所练即所用”原则，使训练目标与推理时的前缀评估完全一致。
    *   **步骤信号提取**：通过时序差分（TD）获取步骤信号，即 $A^{TD} = V(s_{t+1}) - V(s_t)$。
*   **分布级强化学习 (DistRL)**：
    *   **核心思想**：利用 IPVRM 能够对全词表评分的能力，不仅优化被采样的 token，还同时优化高概率的候选 token。
    *   **双分支优化**：
        1.  **采样分支**：使用轨迹级的 GAE 优势函数更新采样路径。
        2.  **候选分支**：对词表中概率大于阈值 $P_{min}$ 的非采样 token，利用 IPVRM 计算的 TD 优势进行密集的反事实更新。
*   **在线平衡更新**：引入自适应难度边界（ADB）和动态损失加权（DLW），解决在线 RL 过程中正负样本极度不平衡的问题。

### 3. 实验设计
*   **数据集/场景**：涵盖数学推理（MATH-500, Minerva-Math, AMC-23, AIME 2024）和科学推理（OlympiadBench）。
*   **Benchmark**：
    *   **奖励模型评估**：使用 ProcessBench（测试步骤级错误定位能力，F1 分数）和 Best-of-N 重排序。
    *   **策略模型评估**：RL 训练后的下游任务准确率。
*   **对比方法**：
    *   **奖励模型**：Q-RM（显式）、EndoRM、DPO-RM、Implicit PRM。
    *   **RL 算法**：GRPO、PRIME、SPRO、Reinforce w/ Q-RM。
*   **模型底座**：Qwen3 (0.6B/8B) 和 Llama-3.2 (1B/3B)。

### 4. 资源与算力
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和总训练时长。
*   **实现细节**：提到了使用 LoRA（秩为 128）进行微调以节省显存。给出了峰值显存数据（如 Qwen3-0.6B 在 DistRL 下约为 30.88GB），并指出 DistRL 相比 GRPO 几乎没有增加额外的计算开销，因为它复用了生成头的 Logits。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同规模和家族的底座模型上进行了验证，涵盖了从 0.6B 到 8B 的参数区间。
*   **消融实验**：非常充分。包括：
    1.  不同前缀位置损失权重的对比（早期 vs 晚期）。
    2.  不同隐式奖励模型与 DistRL 组合的效果。
    3.  在线更新策略（ADB/DLW）的有效性分析。
    4.  TD 优势函数与长期回报的相关性分析。
*   **客观性**：通过在 ProcessBench 这种存在格式偏移（Format Shift）的基准上测试，验证了模型的泛化能力，而非仅仅在同分布数据上刷分，实验设计较为客观公平。

### 6. 论文的主要结论与发现
*   **IPVRM 显著优于传统隐式 PRM**：在 ProcessBench 上的错误定位 F1 分数提升巨大（例如在 Qwen3-0.6B 上比 DPO-RM 高出 11.3 个点）。
*   **DistRL 提升了学习效率**：通过对非采样 token 的更新，DistRL 在相同 rollout 数量下比 GRPO 获得更好的策略性能。
*   **在线更新至关重要**：如果不使用 ADB/DLW 平衡策略，在线更新奖励模型会导致性能崩溃，甚至不如冻结的奖励模型。
*   **TD 信号的作用**：TD 优势虽然不是长期回报的完美估计量，但它提供了有效的局部优化信号，引导策略向奖励模型偏好的方向移动。

### 7. 优点（亮点）
*   **理论一致性**：通过前缀价值学习解决了隐式 PRM 长期存在的训练-推理目标不匹配问题。
*   **高效性**：DistRL 实现了“分布级”监督，无需额外的 rollout 即可增加监督密度，计算开销极低。
*   **鲁棒性**：在小模型（0.6B）上表现尤为突出，证明了该方法在资源受限情况下提升推理能力的潜力。

### 8. 不足与局限
*   **标签噪声风险**：由于完全依赖结果标签（Outcome-only），如果一个错误的前缀最终意外得到了正确答案，模型会接收到错误的监督信号。
*   **应用范围限制**：目前仅在**可验证奖励（Verifiable Rewards）**领域（数学、代码）有效，对于缺乏自动判题器的开放式生成任务（如创意写作），该框架难以直接迁移。
*   **超参数敏感性**：DistRL 引入了如 $P_{min}$ 和 $\alpha$ 等超参数，可能需要针对不同任务进行精细调优。

（完）
