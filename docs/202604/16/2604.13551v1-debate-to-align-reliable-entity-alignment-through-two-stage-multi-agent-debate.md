---
title: "Debate to Align: Reliable Entity Alignment through Two-Stage Multi-Agent Debate"
title_zh: 辩论促对齐：通过两阶段多智能体辩论实现可靠的实体对齐
authors: "Cunda Wang, Ziying Ma, Po Hu, Weihua Wang, Feilong Bao"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13551v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 基于多智能体辩论的可靠实体对齐框架
tldr: 针对知识图谱实体对齐中候选实体集不可靠和LLM推理能力受限的问题，本文提出AgentEA框架。该框架通过实体表示偏好优化提升嵌入质量，并引入由轻量级辩论验证和深度辩论对齐组成的双阶段多角色辩论机制，在增强对齐决策可靠性的同时提升了推理效率。实验证明AgentEA在跨语言、大规模等多种复杂场景下均表现优异。
selection_source: fresh_fetch
motivation: 现有基于LLM的实体对齐方法面临候选实体集质量不高以及模型在复杂对齐场景下推理能力不足的挑战。
method: 提出AgentEA框架，利用实体表示偏好优化改进嵌入，并采用轻量级验证与深度对齐的双阶段多智能体辩论机制进行决策。
result: 在跨语言、稀疏、大规模和异构等多种公共基准测试中，AgentEA均展现出比现有方法更强的有效性和可靠性。
conclusion: 结合偏好优化与多阶段辩论机制能显著提升大语言模型在实体对齐任务中的推理质量与对齐准确率。
---

## 摘要
实体对齐 (EA) 旨在识别不同知识图谱 (KG) 中指向同一现实世界对象的实体。近期基于大语言模型 (LLM) 的方法通常通过知识表示学习获取实体嵌入，并利用嵌入相似度来识别对齐不确定的实体集。对于每个不确定实体，随后根据嵌入相似度检索候选实体集 (CES)，以支持后续的对齐推理和决策。然而，CES 的可靠性和 LLM 的推理能力关键性地影响了后续对齐决策的有效性。为了解决这一问题，我们提出了 AgentEA，这是一个基于多智能体辩论的可靠实体对齐框架。AgentEA 首先通过实体表示偏好优化来提高嵌入质量，然后引入了一个由轻量级辩论验证和深度辩论对齐组成的两阶段多角色辩论机制，在实现更高效的基于辩论的推理的同时，逐步增强对齐决策的可靠性。在跨语言、稀疏、大规模和异构设置下的公共基准测试上进行的广泛实验证明了 AgentEA 的有效性。

## Abstract
Entity alignment (EA) aims to identify entities referring to the same real-world object across different knowledge graphs (KGs). Recent approaches based on large language models (LLMs) typically obtain entity embeddings through knowledge representation learning and use embedding similarity to identify an alignment-uncertain entity set. For each uncertain entity, a candidate entity set (CES) is then retrieved based on embedding similarity to support subsequent alignment reasoning and decision making. However, the reliability of the CES and the reasoning capability of LLMs critically affect the effectiveness of subsequent alignment decisions. To address this issue, we propose AgentEA, a reliable EA framework based on multi-agent debate. AgentEA first improves embedding quality through entity representation preference optimization, and then introduces a two-stage multi-role debate mechanism consisting of lightweight debate verification and deep debate alignment to progressively enhance the reliability of alignment decisions while enabling more efficient debate-based reasoning. Extensive experiments on public benchmarks under cross-lingual, sparse, large-scale, and heterogeneous settings demonstrate the effectiveness of AgentEA.

---

## 论文详细总结（自动生成）

### 论文总结：Debate to Align: Reliable Entity Alignment through Two-Stage Multi-Agent Debate

#### 1. 核心问题与整体含义
本文针对知识图谱（KG）中的**实体对齐（Entity Alignment, EA）**任务，旨在解决现有基于大语言模型（LLM）的方法存在的两大瓶颈：
*   **候选集检索不可靠**：传统的知识表示学习（KRL）生成的嵌入向量在处理语义模糊或结构相似的实体时辨别力不足，导致正确实体可能未包含在候选集中。
*   **单次推理局限性**：现有方法多依赖 LLM 的单次、单路径推理，缺乏对冲突证据的审视和自我纠错能力，容易产生幻觉或误判。
**研究动机**是通过引入“多智能体辩论”机制，模拟人类专家从多维度审视证据的过程，从而实现更可靠、更稳健的实体对齐。

#### 2. 方法论
AgentEA 框架由两个核心模块组成：
*   **实体表示偏好优化（Entity Representation Preference Optimization）**：
    *   **核心思想**：利用直接偏好优化（DPO）微调 LLM 编码器，使其生成的嵌入空间更具区分度。
    *   **关键技术**：设计了两种硬负样本采样策略：**名称相似性负样本**（针对名称接近但非同一实体的干扰）和**度感知邻域负样本**（针对结构复杂的噪声）。通过 DPO 训练，使模型在检索阶段能更准确地将正确实体排在前面。
*   **两阶段多角色辩论机制（Two-Stage Multi-Agent Debate）**：
    *   **第一阶段：轻量级辩论验证（LDV）**。使用三个智能体（支持者、反对者、裁判）对不确定实体进行初步筛选和重排序，通过压缩实体信息降低计算成本。
    *   **第二阶段：深度辩论对齐（DDA）**。针对 LDV 无法确定的疑难案例，引入六个专门角色（别名、类型、属性、邻域、攻击、裁判智能体）进行多轮辩论。
    *   **动态机制**：引入了**渐进式候选集扩展**和**早停策略**，在保证对齐精度的同时优化推理效率。

#### 3. 实验设计
*   **数据集**：在 11 个公共基准数据集上进行了测试，包括 **DBP15K**（中-英、日-英、法-英）、**ICEWS**（WIKI、YAGO）、**DWY**（DBP-WIKI、DBP-YAGO）以及 **SRPRS**（英-德、英-法等）。
*   **对比方法**：
    *   **KRL 类**：MTransE, GCN-Align, BootEA, Dual-AMN, RHGN 等。
    *   **LLM 类**：LLM4EA, ChatEA, Seg-Align, EasyEA, ProLEA 等。
*   **评价指标**：Hit@1, Hit@10, MRR。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **模型底座**：
    *   嵌入优化阶段：微调 **LLaMA3-8B-Instruct**（使用 LoRA 技术）。
    *   辩论推理阶段：默认使用 **GPT-3.5-Turbo**，同时也测试了 Qwen 系列和 GPT-4 系列。
*   **训练细节**：DPO 微调设置为 1 个 epoch，学习率 1e-4。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **主实验**：覆盖 11 个数据集，验证了 AgentEA 在跨语言、稀疏、大规模和异构场景下的普适性。
*   **消融实验**：分别验证了 DPO 优化、负采样策略、辩论阶段（LDV/DDA）以及各个角色智能体对最终结果的贡献。
*   **扩展分析**：包括不同 LLM 底座的鲁棒性测试、辩论轮数的影响、推理成本与效率分析、以及通过 t-SNE 可视化展示嵌入质量的提升。
*   **评价**：实验设计科学、对比全面，通过多维度的数据支撑了其核心论点，具有很高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：AgentEA 在所有基准测试中均达到或刷新了 SOTA 记录，尤其在复杂数据集上提升显著。
*   **辩论的价值**：多智能体辩论能有效识别并纠正单模型推理中的错误，通过“攻击智能体”寻找反例，显著增强了决策的鲁棒性。
*   **偏好优化的有效性**：相比传统的监督微调（SFT），DPO 能更好地学习实体间的细微差别，提高候选集的召回质量。
*   **效率平衡**：两阶段架构成功在高性能和高成本之间找到了平衡点，LDV 过滤了大量简单案例。

#### 7. 优点
*   **创新性**：首次将多智能体辩论（MAD）引入实体对齐领域，解决了 LLM 在该任务中推理不可靠的问题。
*   **多维度建模**：通过专门的智能体角色，同时捕捉了实体的名称、类型、属性和拓扑结构信息。
*   **检索与推理协同**：不仅关注后端的推理，还通过 DPO 优化了前端的检索质量，提供了端到端的可靠性保障。

#### 8. 不足与局限
*   **计算开销**：尽管有轻量级阶段，但对于极大规模的知识图谱，深度辩论阶段的 Token 消耗和 API 调用延迟仍然是一个挑战。
*   **检索上界限制**：如果正确实体在初始检索阶段（Hit@20 之外）就已丢失，后端的辩论机制无法将其找回。
*   **人工启发式设计**：智能体的角色定义和 Prompt 模板仍依赖人工经验，未来若能实现自动化的角色发现或 Prompt 优化将更具竞争力。

（完）
