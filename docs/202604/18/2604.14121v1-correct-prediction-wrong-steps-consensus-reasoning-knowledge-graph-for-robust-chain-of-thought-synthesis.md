---
title: "Correct Prediction, Wrong Steps? Consensus Reasoning Knowledge Graph for Robust Chain-of-Thought Synthesis"
title_zh: 预测正确，步骤错误？用于鲁棒思维链合成的共识推理知识图谱
authors: "Zipeng Ling, Shuliang Liu, Shenghong Fu, Yuehao Tang, Seonil Son, Yao Wan, Xuming Hu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14121v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 鲁棒的思维链合成与逐步缺陷
tldr: "针对大语言模型推理中存在的逻辑错误、幻觉及步骤冗余等缺陷，本文提出CRAFT框架。该框架通过整合多个候选推理路径的共识部分构建推理知识图谱（RKG），并利用拓扑生成技术合成高质量推理链。实验证明，CRAFT在逻辑和数学推理任务中将准确率平均提升了10%以上，并显著优化了推理过程的质量与鲁棒性。"
selection_source: fresh_fetch
motivation: 大语言模型在推理步骤中常出现内部逻辑错误或步骤偏差，且研究发现单纯依靠正确答案引导难以有效纠正这些推理缺陷。
method: 提出CRAFT框架，通过提取多个候选推理路径的共识部分构建推理知识图谱（RKG），并基于此进行拓扑生成以合成高质量推理链。
result: "在逻辑和数学推理基准测试中，该方法使标签预测准确率平均提升超过10%，且在推理路径的多个质量维度上均显著优于现有基线。"
conclusion: 通过共识机制构建知识图谱并合成推理路径，能有效解决LLM推理中的步骤缺陷，显著增强模型的推理鲁棒性。
---

## 摘要
大语言模型（LLM）的推理轨迹存在复杂的缺陷——步骤内部缺陷（逻辑错误、幻觉等）和步间缺陷（过度思考、思考不足），且这些缺陷因样本而异。一种自然的方法是提供真实标签（ground-truth labels）来引导 LLM 的推理。然而，与直觉相反，我们证明这种方法并不能提高推理能力。随后，我们提出了 CRAFT，这是一个旨在缓解这两类步骤缺陷的统一框架。该框架基于多个候选轨迹的共识部分构建推理知识图谱（RKG），并通过拓扑生成合成高质量的轨迹。实验结果表明，我们的方法将标签预测准确率平均提高了 10% 以上，并在逻辑和数学推理基准测试中始终优于所有基准模型。此外，详细的基准评估证明，我们的方法还在多个维度上提升了 LLM 推理轨迹的质量。

## Abstract
LLM reasoning traces suffer from complex flaws -- *Step Internal Flaws* (logical errors, hallucinations, etc.) and *Step-wise Flaws* (overthinking, underthinking), which vary by sample. A natural approach would be to provide ground-truth labels to guide LLMs' reasoning. Contrary to intuition, we show that this yields no improvement in reasoning ability. We then propose CRAFT, a unified framework that mitigates both types of Step flaws, which builds a Reasoning Knowledge Graph (RKG) based on the consensus parts of multiple candidate traces, and synthesizes a high-quality trace through topological generation. Our approach improves label-prediction accuracy by 10+% on average, and consistently outperforms all baselines across both logical and mathematical reasoning benchmarks. Further, detailed benchmark evaluation proves that our method also improves the quality of LLMs' reasoning traces in multiple dimensions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Correct Prediction, Wrong Steps? Consensus Reasoning Knowledge Graph for Robust Chain-of-Thought Synthesis》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注大语言模型（LLM）在执行思维链（CoT）推理时的一个核心矛盾：**预测结果正确但推理步骤错误**。
*   **研究动机**：现有的强化学习（RL）往往只优化最终答案的正确性，导致模型可能通过错误的逻辑或幻觉“撞对”答案。
*   **关键发现**：作者通过实验证明，即使在提示词中直接给出正确答案（Ground Truth），也无法显著改善模型推理步骤的质量。
*   **核心目标**：解决“步骤内部缺陷”（逻辑错误、幻觉）和“步间缺陷”（过度思考导致的冗余、思考不足导致的缺失），提升推理过程的鲁棒性和忠实度。

### 2. 方法论：CRAFT 框架
作者提出了 **CRAFT**（基于共识推理知识图谱聚合的缺陷感知轨迹合成）框架，包含三个核心模块：
*   **模块 I：多样化轨迹生成与共识术语提取**
    *   对每个问题并行生成 $K$ 条候选推理轨迹。
    *   引入 **TF-IRF**（词频-逆推理频率）算法，识别在当前问题的多条轨迹中频繁出现但在其他问题中罕见的关键词，作为“共识术语”。
*   **模块 II：共识推理知识图谱（RKG）构建与异常过滤**
    *   **步骤过滤**：计算每个步骤与共识术语的重叠度，利用 **z-score** 过滤掉得分过低的异常步骤（识别逻辑错误或幻觉）。
    *   **图谱构建**：将剩余步骤作为节点，逻辑依赖关系作为边，构建 RKG。
    *   **结构过滤**：统计边在 $K$ 条轨迹中出现的频率，剔除低频边和孤立节点，从而消除冗余步骤（过度思考）。
*   **模块 III：拓扑引导的轨迹合成**
    *   对共识 RKG 进行拓扑排序。
    *   按照逻辑顺序，以图中的共识节点为锚点，引导模型重新合成一条高质量、精简的推理轨迹。

### 3. 实验设计
*   **数据集**：
    *   **逻辑推理**：FLD（形式逻辑）、FOLIO（一阶逻辑）。
    *   **数学推理**：GSM8K（小学数学）、OlympiadBench（奥数级别）。
*   **评估基准（Benchmark）**：
    *   **标签预测**：准确率（Acc）、F1 分数。
    *   **轨迹质量**：PRMBench（步骤验证准确率）、ROSCOE（语义质量、忠实度、冗余度等）。
*   **对比方法**：包括 Self-Consistency、Universal Self-Consistency、Self-Refine、Self-Aggregation、Self-Eval Beam Search、Faithful CoT、Best-of-N、Tree-of-Thought (ToT) 和 RAP 等 9 种主流基线。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 数量）或训练时长。
*   **模型使用**：实验主要基于 API 调用，使用了 GPT-5.4-nano、o4-mini、Gemini-3-Flash 和 DeepSeek-R1 等模型。
*   **推理成本**：文中提到 CRAFT 每个样本平均需要约 26-33 次 API 调用（包含 $K=10$ 的初始生成、RKG 提取及分步合成）。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集随机抽取 500 个样本进行测试，总计覆盖了逻辑和数学两大领域的四个核心基准。
*   **充分性评价**：
    *   **多维度验证**：不仅测试了最终答案的准确率，还深入评估了推理步骤的逻辑严密性和冗余度。
    *   **消融实验**：详细验证了 RKG 构建、步骤过滤、拓扑合成等每个组件的贡献。
    *   **敏感性分析**：探讨了候选轨迹数量 $K$ 对结果的影响。
    *   **客观性**：对比了 9 种不同类型的基线方法，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **共识优于答案引导**：单纯告知模型正确答案不能纠正推理错误，但利用多条路径的“结构化共识”可以显著提升质量。
*   **性能提升显著**：CRAFT 在各基准测试中平均将准确率提升了 **10% 以上**，在奥数等难题上表现尤为突出。
*   **步骤精简**：CRAFT 生成的推理步骤通常比原始 CoT 更短（减少约 70% 的冗余步骤），且准确率更高。
*   **LLM 的潜力**：实验证明 LLM 在大多数情况下能生成正确的推理片段，只是难以在单次生成中保持全程正确，共识机制能有效“去粗取精”。

### 7. 优点与亮点
*   **结构化建模**：将非结构化的文本推理转化为结构化的知识图谱，使错误检测具备了统计学依据。
*   **双重修复**：一个框架同时解决了内容错误（逻辑/幻觉）和结构错误（冗余/缺失）。
*   **无需外部验证器**：不依赖预训练的奖励模型（PRM），通过模型自身的多次采样实现自我纠错。

### 8. 不足与局限
*   **计算开销**：由于需要生成 $K$ 条轨迹并进行多次图提取和合成调用，推理成本和延迟显著高于单次 CoT。
*   **线性假设**：目前的 RKG 主要针对顺序推理链，对于极其复杂的非线性或高度分支的推理任务，图谱的构建可能变得过于复杂。
*   **词汇依赖**：步骤过滤和边融合在一定程度上依赖词汇重叠（Jaccard 相似度），对于表达差异极大但语义相同的步骤，可能存在误删风险。
*   **共识偏差风险**：如果模型对某个问题存在系统性的“共识性错误”（即所有采样都犯同样的错），该方法将失效。

（完）
