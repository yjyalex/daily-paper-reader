---
title: "MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning"
title_zh: MM-Doc-R1：通过多轮强化学习训练用于长文档视觉问答的智能体
authors: "Jiahang Lin, Kai Hu, Binghai Wang, Yuhao Zhou, Zhiheng Xi, Honglin Guo, Shichun Liu, Junzhe Wang, Shihan Dou, Enyu Zhou, Hang Yan, Zhenhua Han, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13579v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 基于相似度策略优化的多轮强化学习
tldr: 针对传统RAG在长文档多跳视觉问答中检索能力不足的问题，本文提出MM-Doc-R1框架。该框架采用具备视觉感知能力的智能体工作流，通过迭代发现和整合信息。核心贡献是提出了相似度策略优化（SPO）算法，通过轨迹间的语义相似度加权平均奖励来解决多轮强化学习中的基线估计偏差，为智能体提供更稳定的学习信号，显著提升了复杂文档下的推理表现。
selection_source: fresh_fetch
motivation: 传统RAG系统因单次检索机制，难以处理长文档中需要多步迭代搜索的复杂多跳视觉问答任务。
method: 开发了具备视觉感知能力的智能体工作流，并引入SPO强化学习算法，通过相似度加权优化多轮对话中的基线估计。
result: "在MMLongbench-Doc基准上性能提升10.4%，且SPO算法相比GRPO在Qwen3-8B模型上实现了5.0%的额外增长。"
conclusion: 通过集成智能体迭代搜索与改进的强化学习训练算法，MM-Doc-R1有效推进了长文档视觉理解与多跳推理的技术前沿。
---

## 摘要
传统的检索增强生成 (RAG) 系统由于采用单次检索，在处理长文档上的复杂多跳查询时往往表现不佳。我们介绍了 MM-Doc-R1，这是一个新颖的框架，它采用一种具备智能体特性且感知视觉的工作流，通过迭代的信息发现与综合来解决长文档视觉问答问题。为了激励智能体的信息检索能力，我们提出了基于相似性的策略优化 (Similarity-based Policy Optimization, SPO)，解决了现有如 GRPO 等多轮强化学习 (RL) 算法中的基准估计偏差问题。我们的核心见解是，在多轮强化学习中，两条轨迹在语义上越相似，其共享的基准估计就越准确。利用这一点，SPO 通过对多条轨迹的奖励进行相似性加权平均来计算更精确的基准，而不像 GRPO 那样不恰当地将初始状态的基准应用于所有中间状态。这为我们的智能体提供了更稳定、更准确的学习信号，从而实现了超越 GRPO 的卓越训练性能。我们在 MMLongbench-Doc 基准测试上的实验表明，MM-Doc-R1 的表现优于之前的基准模型 10.4%。此外，SPO 展现出优于 GRPO 的性能，在 Qwen3-8B 上将结果提升了 5.0%，在 Qwen3-4B 上提升了 6.1%。这些结果突显了我们的集成框架和新颖训练算法在推动复杂长文档视觉问答技术达到当前领先水平方面的有效性。

## Abstract
Conventional Retrieval-Augmented Generation (RAG) systems often struggle with complex multi-hop queries over long documents due to their single-pass retrieval. We introduce MM-Doc-R1, a novel framework that employs an agentic, vision-aware workflow to address long document visual question answering through iterative information discovery and synthesis. To incentivize the information seeking capabilities of our agents, we propose Similarity-based Policy Optimization (SPO), addressing baseline estimation bias in existing multi-turn reinforcement learning (RL) algorithms like GRPO. Our core insight is that in multi-turn RL, the more semantically similar two trajectories are, the more accurate their shared baseline estimation becomes. Leveraging this, SPO calculates a more precise baseline by similarity-weighted averaging of rewards across multiple trajectories, unlike GRPO which inappropriately applies the initial state's baseline to all intermediate states. This provides a more stable and accurate learning signal for our agents, leading to superior training performance that surpasses GRPO. Our experiments on the MMLongbench-Doc benchmark show that MM-Doc-R1 outperforms previous baselines by 10.4%. Furthermore, SPO demonstrates superior performance over GRPO, boosting results by 5.0% with Qwen3-8B and 6.1% with Qwen3-4B. These results highlight the effectiveness of our integrated framework and novel training algorithm in advancing the state-of-the-art for complex, long-document visual question answering.

---

## 论文详细总结（自动生成）

这是一份关于论文《MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）系统在处理长文档视觉问答（DocVQA）时，通常采用“单次检索”模式。这种模式在面对需要跨页面、多步骤推理的“多跳（Multi-hop）”复杂查询时，难以准确定位并整合分散的信息。
*   **核心问题**：如何构建一个能够像人类一样在长文档中进行迭代搜索、视觉阅读和逻辑推理的智能体，并解决在多轮强化学习（RL）训练中，由于对话轨迹发散导致的基准（Baseline）估计不准、训练不稳定的问题。

### 2. 方法论
MM-Doc-R1 框架由**智能体工作流**和**SPO 强化学习算法**两部分组成：

*   **智能体工作流（Agentic Workflow）**：
    1.  **文档解析**：利用 OCR 工具将 PDF 转换为 Markdown，提取目录（TOC）和页面图像。
    2.  **规划器（Planner）**：基于 TOC 生成初始研究计划，将复杂问题分解为子查询。
    3.  **搜索者（Seeker）**：核心迭代模块。配备“Search（基于 BM25 的文本检索）”和“Read（基于 VLM 的页面视觉理解）”工具，通过多轮交互收集证据。
    4.  **回答者（Answer Agent）**：整合所有检索到的文本和视觉信息，生成最终答案。
*   **相似度策略优化（SPO）算法**：
    *   **核心思想**：改进了 DeepSeek 提出的 GRPO 算法。在多轮 RL 中，随着交互增加，不同轨迹（Trajectory）会进入完全不同的中间状态。GRPO 简单地使用组内平均奖励作为基准，忽略了状态差异。
    *   **技术细节**：SPO 引入了**语义相似度加权**。它利用预训练模型（如 BGE-M3）计算不同轨迹之间的嵌入向量相似度，在计算某条轨迹的优势函数（Advantage）时，给予语义更接近的轨迹更高的权重。
    *   **效果**：通过构建动态、内容感知的基准，减少了优势估计的方差，为智能体提供了更稳定的学习信号。

### 3. 实验设计
*   **数据集**：
    *   **MMLongbench-Doc**：主要评估基准，包含 1,082 个涉及文本、布局、图表、表格和图像的复杂长文档问题。
    *   **LongDocURL**：用于强化学习训练和验证。
*   **对比方法**：
    *   **文本 RAG**：BM25, BGE-M3。
    *   **多模态 RAG**：ColQwen2.5-7B。
    *   **智能体系统**：Mdoc Agent, M3doc RAG。
*   **评估指标**：准确率（ACC）、F1 分数、召回率（Recall）以及针对不同证据类型（图表、表格等）的细分得分。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **软件框架**：基于 `verl` 强化学习框架实现。
*   **训练细节**：采用分布式设置，结合数据并行和模型并行；使用自动混合精度（AMP）加速；学习率设为 5e-7，KL 系数 0.005。
*   **推理延迟**：文中提供了详细的延迟分析，Qwen3-8B 变体平均每题耗时约 109-154 秒。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 4B 和 8B 两种参数规模的模型（基于 Qwen3 系列）。
*   **充分性**：
    *   **多维度对比**：不仅对比了整体性能，还针对单跳 vs 多跳、可回答 vs 不可回答问题进行了拆解。
    *   **消融实验**：验证了 TOC、OCR 和 VLM 阅读工具的必要性。
    *   **算法对比**：直接对比了 SPO 与 GRPO 的收敛速度和最终性能。
    *   **参数敏感性**：分析了检索 Top-k 值对结果的影响。
*   **客观性**：实验设置了“人类表现”和“真值证据上限（Upper Bounds）”作为参照，评价体系较为客观。

### 6. 主要结论与发现
*   **性能飞跃**：MM-Doc-R1 在 MMLongbench-Doc 上比之前的最佳基准提升了 **10.4%**。
*   **多跳能力**：在需要多页信息整合的问题上，性能提升高达 **10.0%**。
*   **SPO 算法优势**：相比 GRPO，SPO 在 4B 模型上提升了 **6.1%**，在 8B 模型上提升了 **5.0%**，且训练曲线更平滑、收敛更快。
*   **高召回率**：仅需阅读平均 3.35 页即可达到 66.3% 的召回率，优于阅读 5 页的传统 RAG 方法。

### 7. 优点
*   **算法创新**：SPO 算法敏锐地捕捉到了多轮 RL 中轨迹发散导致的基准偏差问题，并给出了优雅的语义加权解决方案。
*   **视觉感知**：通过集成 VLM 作为“阅读工具”，使智能体能够处理非结构化的视觉元素（如图表、复杂布局），而不仅仅是纯文本。
*   **测试时计算（Test-time Scaling）**：通过增加推理时的迭代步数换取更高的准确率，符合当前大模型发展的趋势。

### 8. 不足与局限
*   **解析依赖**：框架高度依赖初始 OCR 和 TOC 的质量，如果文档结构极度混乱，规划器可能会失效。
*   **静态限制**：目前主要针对 PDF 等静态文档，尚未在动态网页或交互式报告中验证。
*   **语言偏差**：实验主要集中在英文基准上，跨语言（如中文或多语言长文档）的鲁棒性有待进一步研究。
*   **计算成本**：相比单次 RAG，多轮迭代带来的推理延迟和 API 调用成本显著增加。

（完）
