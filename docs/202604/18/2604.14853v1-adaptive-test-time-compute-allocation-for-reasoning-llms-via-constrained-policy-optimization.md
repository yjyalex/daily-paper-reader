---
title: Adaptive Test-Time Compute Allocation for Reasoning LLMs via Constrained Policy Optimization
title_zh: 基于受约束策略优化的推理型大语言模型自适应推理时计算分配
authors: "Zhiyuan Zhai, Bingcong Li, Bingnan Xiao, Ming Li, Xin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14853v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 针对推理大模型的约束策略优化
tldr: "本研究针对推理大模型在推理阶段的计算资源分配问题，提出了一种基于约束策略优化的自适应分配方法。通过将问题建模为受限优化问题，采用“先求解后学习”的两阶段框架：首先利用拉格朗日松弛法确定每个样本的最优计算量，随后训练轻量级分类器预测该分配策略。实验证明，该方法在MATH和GSM8K任务上显著优于均匀分配，在相同预算下实现了高达12.8%的准确率提升。"
selection_source: fresh_fetch
motivation: 现有的推理计算缩放技术往往忽略了不同输入对计算资源需求的差异，导致在有限预算下资源分配效率低下。
method: 提出“Solve-then-Learn”框架，通过拉格朗日松弛法求解单样本最优计算量，并训练分类器将该策略摊销到实时推理中。
result: "在MATH和GSM8K数据集上，该方法在相同计算预算下比基准方法提升了高达12.8%的相对准确率，且模仿准确率超过91%。"
conclusion: 自适应推理计算分配能有效平衡成本与性能，通过约束优化理论可实现接近理论上限的资源调度。
---

## 摘要
推理时计算扩展（即在推理过程中通过重复采样、搜索或扩展推理投入额外计算）已成为提升大语言模型性能的强有力手段。然而，在有限的推理预算下部署这些技术需要做出一个当前系统在很大程度上忽略的决策：哪些输入值得投入更多计算，而哪些可以廉价地回答？我们将此形式化为一个受约束优化问题（在平均计算预算限制下最大化预期准确率），并利用“先求解后学习”（Solve-then-Learn）的两阶段流水线进行解决。在求解阶段，拉格朗日松弛法将全局约束分解为针对每个实例的子问题，每个子问题都存在一个闭式最优动作（oracle action），能够对准确率与成本进行最优定价。我们证明了诱导成本在对偶变量上是单调的，从而可以通过二分查找实现精确的预算控制。在学习阶段，训练一个轻量级分类器，根据廉价的输入特征预测最优动作，从而将分配规则摊销以用于实时部署。我们证明了所学策略的任务级遗憾（regret）受限于其模仿误差与最坏情况下的单实例差距之积，从而实现了从受约束推理到监督分类的简洁归约。在 MATH 和 GSM8K 数据集上针对三种大语言模型（DeepSeek-V3、GPT-4o-mini、Qwen2.5-7B）进行的实验表明，我们的方法始终优于均匀分配和启发式分配基准，在相同预算约束下，在 MATH 上实现了高达 12.8% 的相对准确率提升，同时以超过 91% 的模仿准确率紧密追踪拉格朗日最优上限。

## Abstract
Test-time compute scaling, the practice of spending extra computation during inference via repeated sampling, search, or extended reasoning, has become a powerful lever for improving large language model performance. Yet deploying these techniques under finite inference budgets requires a decision that current systems largely ignore: which inputs deserve more compute, and which can be answered cheaply? We formalize this as a constrained optimization problem (maximize expected accuracy subject to an average compute budget) and solve it with a two-stage Solve-then-Learn pipeline. In the solve stage, Lagrangian relaxation decomposes the global constraint into per-instance sub-problems, each admitting a closed-form oracle action that optimally prices accuracy against cost. We prove that the induced cost is monotone in the dual variable, enabling exact budget targeting via binary search. In the learn stage, a lightweight classifier is trained to predict oracle actions from cheap input features, amortizing the allocation rule for real-time deployment. We establish that the task-level regret of the learned policy is bounded by its imitation error times the worst-case per-instance gap, yielding a clean reduction from constrained inference to supervised classification. Experiments on MATH and GSM8K with three LLMs (DeepSeek-V3, GPT-4o-mini, Qwen2.5-7B) show that our method consistently outperforms uniform and heuristic allocation baselines, achieving up to 12.8% relative accuracy improvement on MATH under matched budget constraints, while closely tracking the Lagrangian oracle upper bound with over 91% imitation accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：基于受约束策略优化的推理型大语言模型自适应推理时计算分配

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理技术的发展，通过增加推理时计算（如思维链推理、自我一致性投票、搜索等）来提升性能已成为主流。然而，现有的系统通常对所有输入分配**均匀的计算预算**（例如固定采样 $N$ 次），这忽略了任务难度的异质性：
*   **简单问题**：单次推理即可解决，多余采样是资源浪费。
*   **响应型问题**：增加计算量能显著提升准确率。
*   **极难问题**：无论投入多少计算资源都难以解决。
**研究动机**：在有限的总推理预算下，如何动态地为不同输入分配计算量，以最大化整体准确率？

#### 2. 方法论：Solve-then-Learn 框架
论文将该问题形式化为一个**受约束优化问题**，并提出了两阶段解决方案：

*   **第一阶段：求解（Solve - 离线）**
    *   **拉格朗日松弛**：引入拉格朗日乘子 $\lambda$（代表计算资源的“影子价格”），将全局预算约束分解为独立的单样本子问题。
    *   **最优动作（Oracle Action）**：对于每个样本 $x$，选择预算 $b$ 以最大化 $Acc(x, b) - \lambda C(b)$。
    *   **二分查找**：证明了诱导成本关于 $\lambda$ 的单调性，通过二分查找快速确定最优的 $\lambda$，从而生成训练所需的“最优标签”。
*   **第二阶段：学习（Learn - 离线）**
    *   **策略摊销**：由于实时推理时无法预知 $Acc(x, b)$，论文训练一个轻量级分类器（如 XGBoost）来预测最优预算。
    *   **特征提取**：使用 16 种廉价特征（如文本长度、词汇多样性、单次推理的熵等），确保路由开销极低（<1%）。
*   **理论保障**：证明了学习策略的任务级遗憾（Regret）受限于模仿误差，并给出了原问题的恢复保证（Near-feasibility & Near-optimality）。

#### 3. 实验设计
*   **数据集**：
    *   **MATH**：竞赛级数学题，难度分级明确（Level 2-4）。
    *   **GSM8K**：小学数学题，用于测试模型在简单任务上的表现。
*   **基座模型**：DeepSeek-V3、GPT-4o-mini、Qwen2.5-7B。
*   **对比基准（Benchmarks）**：
    *   **Fixed-b**：均匀分配预算（标准做法）。
    *   **Random**：随机分配预算。
    *   **Heuristic**：基于提示词长度的启发式分配。
    *   **Oracle**：理论性能上限（已知准确率曲线下的最优分配）。

#### 4. 资源与算力
*   **数据采集**：为了评估不同预算下的准确率，对每个模型-数据集组合进行了 200（样本）× 48（响应）= 9,600 次 API 调用，总计约 **38,400 次 API 调用**。
*   **训练算力**：
    *   **分类器训练**：使用 XGBoost 在 CPU 上训练，耗时 **<1 秒**。
    *   **推理开销**：分类器路由开销极低，可忽略不计。
*   **GPU 信息**：文中未明确提及具体的训练 GPU 型号，因为核心开销在于 API 调用和轻量级分类器训练，而非大规模模型微调。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构和规模的模型，2 种代表性推理数据集，以及从 1 到 16 的连续预算区间。
*   **消融实验**：
    *   对比了不同分类器架构（逻辑回归、SVM、MLP、随机森林、GBM），发现树模型表现最优。
    *   分析了数据效率（仅需 20-25% 的数据即可超过基准）。
    *   研究了预算粒度（Budget Granularity）对性能的影响。
*   **评价**：实验设计客观、公平，通过 3 个随机种子报告了均值和标准差，且理论证明与实验结果高度吻合，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：在相同预算下，该方法在 MATH 任务上比固定预算方法实现了高达 **12.8% 的相对准确率提升**。
*   **倒 U 型分配模式**：最优策略倾向于给简单和极难的问题分配低预算（$b=1$），而将资源集中在“中等难度”且对计算量增加敏感的问题上。
*   **模仿精度**：轻量级分类器实现了超过 **91% 的模仿准确率**，能够紧密追踪理论最优上限。
*   **普适性**：该方法在不同模型（开源/闭源、大/小）上均表现出一致的优越性。

#### 7. 优点
*   **理论严密**：首次将推理计算分配形式化为受约束优化问题，并提供了严谨的拉格朗日对偶分析和遗憾边界证明。
*   **部署友好**：采用“先求解后学习”的模式，推理时仅需运行一个极小的分类器，几乎不增加延迟。
*   **资源高效**：通过重新分配现有预算而非增加预算来提升性能，具有极高的实用价值。

#### 8. 不足与局限
*   **离线依赖**：获取最优标签需要预先对训练集进行多次采样以估计准确率曲线，这在某些实时变化的场景下成本较高。
*   **特征工程**：目前依赖于手工设计的文本特征，虽然有效，但可能无法捕捉更深层的语义难度。
*   **预算离散性**：目前仅处理离散的预算集合（如 $b \in \{1, 2, 4, 8, 16\}$），尚未扩展到连续的计算资源分配。
*   **任务局限**：主要在数学推理任务上验证，在创意写作或多轮对话等难以量化准确率的任务上应用受限。

（完）
