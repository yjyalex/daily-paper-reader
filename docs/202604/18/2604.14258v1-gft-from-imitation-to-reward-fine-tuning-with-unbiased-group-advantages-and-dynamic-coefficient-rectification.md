---
title: "GFT: From Imitation to Reward Fine-Tuning with Unbiased Group Advantages and Dynamic Coefficient Rectification"
title_zh: GFT：利用无偏组优势与动态系数校正实现从模仿到奖励微调的跨越
authors: "Wangjie Gan, Miao Pan, Linbo Xi, Wenqi Zhang, Jintao Chen, Jianwei Yin, Xuhong Zhang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14258v1"
tags: ["query:token"]
score: 9.0
evidence: 策略梯度优化与奖励微调框架
tldr: 本文针对大语言模型监督微调（SFT）中存在的熵坍缩和梯度爆炸等问题，提出了统一的后训练框架GFT。通过将SFT重新解释为具有稀疏奖励的策略梯度优化，GFT引入了分组优势学习和动态系数修正机制，旨在缓解奖励稀疏性并稳定优化过程。实验证明，GFT在知识注入和泛化性能上均优于SFT，并能更好地衔接后续的强化学习训练。
selection_source: fresh_fetch
motivation: 发现SFT可视为一种特殊的策略梯度优化，其隐式奖励稀疏和不稳定的权重会导致模型出现单路径依赖和训练崩溃。
method: 提出GFT框架，利用分组优势学习构建对比监督以缓解奖励稀疏，并通过动态系数修正自适应限制权重以稳定优化。
result: 实验结果显示GFT在多项任务上一致超越了基于SFT的方法，并显著提升了模型与后续强化学习阶段的兼容性。
conclusion: GFT通过有效统一模仿学习与奖励微调，克服了传统SFT的内在局限，为大语言模型的高效稳健微调提供了新方案。
---

## 摘要
大语言模型通常采用监督微调 (SFT) 和强化学习 (RL) 进行后训练，但如何将高效的知识注入与鲁棒的泛化能力有效统一仍是一项挑战。本研究通过训练动力学分析表明，SFT 可被视为策略梯度优化的一种特例，其具有极度稀疏的隐式奖励和不稳定的逆概率加权，共同导致了单路径依赖、熵坍缩和梯度爆炸。基于这一诊断，我们提出了组微调 (Group Fine-Tuning, GFT)，这是一个统一的后训练框架，通过两种机制解决上述内在局限：一是组优势学习 (Group Advantage Learning)，通过构建多样化的响应组并推导出归一化的对比监督来缓解奖励稀疏性；二是动态系数校正 (Dynamic Coefficient Rectification)，通过自适应地限制逆概率权重以稳定优化过程，同时保留高效的知识注入。实验证明，GFT 持续优于基于 SFT 的方法，且生成的策略能更平滑地衔接后续的 RL 训练。

## Abstract
Large language models are typically post-trained using supervised fine-tuning (SFT) and reinforcement learning (RL), yet effectively unifying efficient knowledge injection with robust generalization remains challenging. In this work, we provide a training-dynamics analysis showing that SFT can be interpreted as a special case of policy gradient optimization with an extremely sparse implicit reward and unstable inverse-probability weighting, which together lead to single-path dependency, entropy collapse, and gradient explosion. Motivated by this diagnosis, we propose Group Fine-Tuning (GFT), a unified post-training framework that addresses these intrinsic limitations through two mechanisms: Group Advantage Learning, which constructs diverse response groups and derives normalized contrastive supervision to alleviate reward sparsity, and Dynamic Coefficient Rectification, which adaptively bounds inverse-probability weights to stabilize optimization while preserving efficient knowledge injection. Experiments demonstrate that GFT consistently surpasses SFT-based methods and yields policies that integrate more smoothly with subsequent RL training.

---

## 论文详细总结（自动生成）

以下是对论文《GFT: From Imitation to Reward Fine-Tuning with Unbiased Group Advantages and Dynamic Coefficient Rectification》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的后训练通常分为监督微调（SFT）和强化学习（RL）。然而，SFT 存在三大痛点：**灾难性遗忘**（损害预训练表征）、**熵坍缩**（导致模型输出单一，缺乏探索空间）以及**训练不稳定**（梯度爆炸）。
*   **研究动机**：作者通过训练动力学分析发现，SFT 本质上是 RL 的一种特例，但其隐式奖励极其稀疏（仅专家路径为 1，其余为 0），且逆概率权重（$1/\pi$）在模型遇到不熟悉但正确的 Token 时会急剧放大，导致梯度爆炸。这种“单路径依赖”限制了模型与后续 RL 阶段的协同效应。

### 2. 方法论：GFT 框架
GFT（Group Fine-Tuning）旨在将模仿学习与奖励微调统一，核心包含两个机制：
*   **组优势学习（Group Advantage Learning, GAL）**：
    *   **核心思想**：打破 SFT 的单路径依赖。为每个 Query 构建一个包含“专家演示、教师模型蒸馏、模型自生成样本”的混合响应组。
    *   **技术细节**：对组内所有响应计算基于规则的奖励（如数学题对错），并进行标准化处理得到“组优势分数”（Advantage Score）。这使得模型不再是机械模仿专家，而是通过对比学习，在多个候选路径中识别更优解。
*   **动态系数校正（Dynamic Coefficient Rectification, DCR）**：
    *   **核心思想**：稳定优化过程。
    *   **技术细节**：针对 SFT 梯度公式中不稳定的 $1/\pi$ 项，引入置信度阈值 $\tau$。当 Token 概率 $\pi_t < \tau$ 时，使用 `stop-gradient` 操作限制权重的爆炸式增长；当概率足够高时则保持原样。这既保留了知识注入的能力，又防止了极端样本导致的参数剧烈波动。

### 3. 实验设计
*   **模型选择**：涵盖了不同架构和规模的模型，包括 Qwen2.5-Math (1.5B, 7B)、LLaMA-3 (3.2-3B, 3.1-8B) 和 DeepSeekMath-7B-Base。
*   **数据集**：使用 NuminaMath CoT 数据集的 10k 子集进行训练。
*   **评测基准**：在 11 个数学和通用推理基准上进行评估，包括 AMC23、MATH、Minerva、OlympiadBench、MMLU-STEM、Gaokao 等。
*   **对比方法**：
    *   标准 SFT 及混合蒸馏数据的 SFT(mix)。
    *   强化学习基线：GRPO。
    *   SFT 的改进变体：DFT、ASFT、PSFT。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长。
*   **数据效率**：论文强调了 GFT 的高效性，仅使用 **10k** 训练样本即可在多个指标上超越使用 **100k** 样本的标准 SFT。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常全面。除了主实验外，还包括：
    *   **消融实验**：分别验证了 GAL 和 DCR 的必要性。
    *   **协同性实验**：验证了“SFT → GFT → RL”流水线的性能，证明 GFT 能为 RL 提供更好的初始化。
    *   **遗忘与多样性分析**：通过 KL 散度和 Pass@k 指标量化了模型在保留通用能力和输出多样性方面的表现。
*   **客观性**：通过跨模型家族（Qwen, Llama, DeepSeek）的测试，证明了方法的普适性，实验对比具有较高的说服力。

### 6. 主要结论与发现
*   **性能卓越**：GFT 在所有测试模型上均显著优于 SFT 及其变体，尤其在竞赛级难度（Olympiad）上提升明显。
*   **解决协同难题**：传统的“SFT + RL”往往效果不如单纯 RL，而 GFT 能够保持策略熵，使后续 RL 训练的性能上限更高。
*   **缓解遗忘**：GFT 的 KL 散度远低于 SFT，表明其更好地保留了预训练阶段的通用知识。
*   **提升多样性**：Pass@k 结果显示 GFT 生成的解题路径比蒸馏和 RL 方法更具多样性。

### 7. 优点与亮点
*   **理论深刻**：将 SFT 重新定义为 RL 特例的视角非常新颖，为理解 SFT 的缺陷提供了数学解释。
*   **单阶段统一**：无需复杂的在线 RL 流程，通过离线组构建即可实现类似 RL 的对比学习效果。
*   **高兼容性**：GFT 不仅是 SFT 的替代品，更是 RL 的“加速器”，解决了后训练阶段不同算法间的冲突问题。

### 8. 不足与局限
*   **任务局限性**：目前主要集中在有客观标准答案的**数学推理**任务上，在主观性较强的开放式对话任务中的表现尚待验证。
*   **数据准备开销**：虽然训练比在线 RL 快，但构建包含教师蒸馏和自生成样本的“响应组”比标准 SFT 增加了额外的数据预处理成本。
*   **模型规模限制**：实验最高仅覆盖到 8B 参数模型，在 70B 或更大规模模型上的扩展性有待进一步观察。

（完）
