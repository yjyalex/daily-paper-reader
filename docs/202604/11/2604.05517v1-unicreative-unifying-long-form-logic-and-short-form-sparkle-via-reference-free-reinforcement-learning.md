---
title: "UniCreative: Unifying Long-form Logic and Short-form Sparkle via Reference-Free Reinforcement Learning"
title_zh: UniCreative：通过无参考强化学习统一长篇逻辑与短篇灵动
authors: "Xiaolong Wei, Zerun Zhu, Simin Niu, Xingyu Zhang, Peiying Yu, Changxuan Xiao, Yuchen Li, Jicheng Yang, Zhejun Zhao, Chong Meng, Long Xia, Daiting Shi"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05517v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于长篇逻辑的无参考强化学习框架
tldr: 针对创意写作中长篇逻辑连贯性与短篇表达灵活性难以兼顾，且高质量监督数据稀缺的问题，本文提出UniCreative框架。该框架包含自适应约束感知奖励模型AC-GenRM和无参考策略优化算法ACPO，通过动态合成评价标准实现模型对齐。实验表明，该方法在无需监督微调的情况下显著提升了写作质量，并使模型展现出根据任务需求自主选择规划或直接生成的元认知能力。
selection_source: fresh_fetch
motivation: 旨在解决长短篇创意写作在逻辑与灵活性上的冲突，并克服现有对齐方法对高质量标注数据和静态奖励的过度依赖。
method: 提出UniCreative框架，利用自适应奖励模型AC-GenRM动态生成评价准则，并通过ACPO算法在无参考数据的情况下进行策略优化。
result: 实验显示AC-GenRM与专家评估高度契合，ACPO在多项写作任务中表现优异，且模型学会了在严谨规划与直接生成间自主切换。
conclusion: 本研究验证了无参考强化学习在统一长短篇创作范式上的有效性，为大模型创意写作能力的对齐提供了高效且可扩展的方案。
---

## 摘要
创意写作中的一个根本挑战在于调和长篇叙事中维持全局连贯性与短篇文本中保持局部表现力之间的内在张力。虽然长上下文生成需要显式的宏观规划，但短篇创意往往需要自发的、无约束的表达。然而，现有的对齐范式通常采用静态奖励信号，并严重依赖高质量的有监督数据，这不仅成本高昂且难以扩展。为了解决这一问题，我们提出了 UniCreative，这是一个统一的无参考强化学习框架。我们首先引入了 AC-GenRM，这是一种自适应约束感知奖励模型，它能动态合成特定于查询的标准，以提供细粒度的偏好判断。利用这些信号，我们提出了 ACPO，一种策略优化算法，在无需有监督微调和真值参考的情况下，使模型在内容质量和结构范式上与人类偏好保持一致。实验结果表明，AC-GenRM 与专家评估高度一致，而 ACPO 显著提升了多种写作任务的性能。至关重要的是，我们的分析揭示了一种涌现的元认知能力：模型学会了自主区分需要严密规划的任务与倾向于直接生成的任务，验证了我们直接对齐方法的有效性。

## Abstract
A fundamental challenge in creative writing lies in reconciling the inherent tension between maintaining global coherence in long-form narratives and preserving local expressiveness in short-form texts. While long-context generation necessitates explicit macroscopic planning, short-form creativity often demands spontaneous, constraint-free expression. Existing alignment paradigms, however, typically employ static reward signals and rely heavily on high-quality supervised data, which is costly and difficult to scale. To address this, we propose \textbf{UniCreative}, a unified reference-free reinforcement learning framework. We first introduce \textbf{AC-GenRM}, an adaptive constraint-aware reward model that dynamically synthesizes query-specific criteria to provide fine-grained preference judgments. Leveraging these signals, we propose \textbf{ACPO}, a policy optimization algorithm that aligns models with human preferences across both content quality and structural paradigms without supervised fine-tuning and ground-truth references. Empirical results demonstrate that AC-GenRM aligns closely with expert evaluations, while ACPO significantly enhances performance across diverse writing tasks. Crucially, our analysis reveals an emergent meta-cognitive ability: the model learns to autonomously differentiate between tasks requiring rigorous planning and those favoring direct generation, validating the effectiveness of our direct alignment approach.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **UniCreative** 的统一框架，旨在解决大语言模型（LLM）在创意写作中面临的长篇逻辑连贯性与短篇表达灵动性之间的矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：长篇写作（如小说、剧本）需要宏观规划（Plan-then-Write）以维持逻辑连贯，防止结构坍塌；而短篇写作（如诗歌、标语）则需要自发的、无约束的表达（Direct Generation），过度规划会导致表达平庸、缺乏“灵气”。
*   **现有局限**：目前的对齐范式（如 RLHF、DPO）高度依赖高质量的有监督微调（SFT）数据和人工标注的偏好对，这在创意领域成本极高且难以扩展。此外，现有模型往往采用单一生成模式，无法根据任务需求灵活切换。

### 2. 方法论：UniCreative 框架
UniCreative 采用**无参考强化学习（Reference-Free RL）**，由两个核心组件构成：
*   **AC-GenRM（自适应约束感知奖励模型）**：
    *   **动态标准合成**：不使用静态 Prompt，而是先根据用户查询（Query）动态生成特定评价标准（如：恐怖故事侧重“氛围”，贺卡侧重“温馨”）。
    *   **去偏成对排序**：通过对称数据增强（交换 A/B 顺序）训练生成式判别器，减少位置偏见，提供更精准的偏好信号。
*   **ACPO（自适应约束偏好优化）算法**：
    *   **双模式切换**：模型可自主选择“先规划后写作”或“直接生成”。
    *   **复合奖励函数**：
        1.  **自举相对奖励 ($R_{rel}$)**：通过模型自博弈（Self-play）产生多个候选，由 AC-GenRM 判定胜者。
        2.  **范式感知结构约束 ($R_{struct}$)**：对长篇不规划或短篇过度规划的行为进行惩罚。
        3.  **自适应长度正则化 ($R_{len}$)**：防止长篇内容缺失和短篇冗长。
    *   **优化策略**：采用 **GRPO（组相对策略优化）**，通过组内相对得分计算优势函数，无需额外的 Value Model，节省显存并稳定训练。

### 3. 实验设计
*   **模型与基准**：基于 Qwen3 系列（1.7B、4B、8B）进行实验。
*   **数据集**：
    *   **长篇评估**：WritingBench（涵盖 6 大领域、100+ 子领域）。
    *   **短篇评估**：Blessing 数据集（中文创意祝福语）。
    *   **偏好对齐**：LitBench（专家标注的文学偏好）。
*   **对比方法**：对比了 Claude-3.7-Sonnet、GPT-4o、DeepSeek-V3/R1 等顶级闭源和开源模型。
*   **元认知测试**：专门构建了“模式判别基准测试”，评估模型是否能自主区分任务类型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H800 (80GB) GPU** 集群。
*   **训练细节**：
    *   **GenRM 阶段**：全参数微调，2 个 Epoch，上下文长度 5200 词。
    *   **ACPO 阶段**：10 个 Epoch，利用 vLLM 加速推理，最大上下文支持 12.6k 词（Prompt 8.6k + Response 4k），每条 Query 采样 6 个响应进行组优化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.7B 到 8B 不同参数规模的模型，验证了扩展性。
*   **充分性**：论文不仅对比了最终的写作质量，还进行了奖励模型一致性测试、训练动态分析（熵、KL 散度、奖励曲线）以及消融实验。
*   **客观性**：通过与人类专家和多种第三方强模型（如 GPT-4o）的评估结果进行交叉验证，确保了评价的公正性。

### 6. 主要结论与发现
*   **性能跨越**：Qwen3-8B 经过 ACPO 优化后，在 WritingBench 上的表现超越了 Claude-3.7-Sonnet，在短篇 Blessing 任务上超越了 Claude-4.5。
*   **无参考对齐的有效性**：证明了在没有 SFT 数据和真值参考的情况下，仅靠强化学习也能实现卓越的创意对齐。
*   **涌现的元认知能力**：在 4B 和 8B 规模的模型上，模型展现出显著的自主切换模式能力（准确率达 90% 以上），而 1.7B 模型则存在能力瓶颈。

### 7. 优点与亮点
*   **动态评价**：AC-GenRM 解决了传统奖励模型“一刀切”的问题，使评价更符合文学创作的细分需求。
*   **模式解耦**：首次将“逻辑规划”视为一种可调用的计算资源，而非固定流程，保护了短篇创作的随机性（Sparkle）。
*   **高效对齐**：通过 GRPO 和无参考学习，极大降低了对高质量人工标注数据的依赖。

### 8. 不足与局限
*   **模型规模依赖**：元认知能力（自主切换模式）在小模型（1.7B）上表现不佳，说明该框架对基础模型能力有一定门槛。
*   **边界模糊**：目前仅划分为“长”和“短”两种模式，对于“中等长度”（如社交媒体长帖）的适配可能存在灰色地带。
*   **计算开销**：尽管使用了 GRPO，但处理超长上下文（12.6k 词）的强化学习训练依然需要极高的算力资源，普通研究机构难以负担。

（完）
