---
title: "SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation"
title_zh: SCMAPR：针对复杂场景文本生成视频的自我修正多智能体提示词优化
authors: "Chengyi Yang, Pengzhen Li, Jiayin Qi, Aimin Zhou, Ji Wu, Ji Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05489v1"
tags: ["query:mas-gen"]
score: 9.0
evidence: 生成任务的多智能体细化过程
tldr: 针对文本生成视频（T2V）在复杂场景下因提示词模糊导致的生成质量差问题，本文提出SCMAPR框架。该框架采用多智能体协作模式，通过场景感知路由、策略驱动优化及自我修正机制，实现提示词的精细化重写。此外，研究者还推出了T2V-Complexity基准测试集。实验证明，SCMAPR在多个主流榜单上显著提升了视频的语义一致性和生成质量。
selection_source: fresh_fetch
motivation: 现有的T2V系统在处理复杂场景时，常因提示词的歧义和描述不足而导致生成效果不佳。
method: 提出一种多智能体框架，通过场景分类路由、策略化重写和基于语义验证的自我修正机制来优化提示词。
result: 在VBench、EvalCrafter和T2V-CompBench等多个基准测试中，SCMAPR均显著优于现有主流基线模型。
conclusion: SCMAPR通过多智能体协同优化，有效解决了复杂场景下的T2V提示词细化问题，提升了视频生成的准确性。
---

## 摘要
文本生成视频（T2V）受益于扩散模型的最新进展，然而现有系统在复杂场景下仍然表现不佳，这通常由于文本提示词的歧义性和描述不足而加剧。在这项工作中，我们将复杂场景提示词优化建模为一个阶段性的多智能体优化过程，并提出了 SCMAPR，即一种用于 T2V 提示词的场景感知且自我修正的多智能体提示词优化框架。SCMAPR 协调专门的智能体来执行以下任务：(i) 将每个提示词路由到基于分类法的场景以进行策略选择；(ii) 合成场景感知的重写策略并执行基于策略的优化；(iii) 进行结构化语义验证，并在检测到违规时触发条件修正。为了明确 T2V 提示词中复杂场景的构成，提供代表性示例，并实现在此类挑战性条件下的严格评估，我们进一步引入了 T2V-Complexity，这是一个专门由复杂场景提示词组成的复杂场景 T2V 基准测试。在 3 个现有基准测试和我们的 T2V-Complexity 基准测试上进行的广泛实验表明，SCMAPR 在复杂场景下持续提升了文本-视频对齐和整体生成质量，在 VBench 和 EvalCrafter 上的平均得分分别提升了高达 2.67% 和 3.28，在 T2V-CompBench 上相比 3 个最先进（SOTA）基线模型提升了高达 0.028。

## Abstract
Text-to-Video (T2V) generation has benefited from recent advances in diffusion models, yet current systems still struggle under complex scenarios, which are generally exacerbated by the ambiguity and underspecification of text prompts. In this work, we formulate complex-scenario prompt refinement as a stage-wise multi-agent refinement process and propose SCMAPR, i.e., a scenario-aware and Self-Correcting Multi-Agent Prompt Refinement framework for T2V prompting. SCMAPR coordinates specialized agents to (i) route each prompt to a taxonomy-grounded scenario for strategy selection, (ii) synthesize scenario-aware rewriting policies and perform policy-conditioned refinement, and (iii) conduct structured semantic verification that triggers conditional revision when violations are detected. To clarify what constitutes complex scenarios in T2V prompting, provide representative examples, and enable rigorous evaluation under such challenging conditions, we further introduce {T2V-Complexity}, which is a complex-scenario T2V benchmark consisting exclusively of complex-scenario prompts. Extensive experiments on 3 existing benchmarks and our T2V-Complexity benchmark demonstrate that SCMAPR consistently improves text-video alignment and overall generation quality under complex scenarios, achieving up to 2.67\% and 3.28 gains in average score on VBench and EvalCrafter, and up to 0.028 improvement on T2V-CompBench over 3 State-Of-The-Art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的文本生成视频（T2V）模型在处理“复杂场景”时表现不佳。这主要是因为用户输入的提示词（Prompts）通常过于简略、模糊或缺乏细节，导致模型难以维持时间一致性、运动逻辑和复杂的语义对齐。
*   **研究背景**：现有的提示词优化方法多针对文本生成图像（T2I），忽略了视频特有的挑战（如长程时间依赖、物理因果律等）。此外，缺乏专门针对复杂场景的 T2V 评估基准。

### 2. 方法论：SCMAPR 框架
SCMAPR 将提示词优化建模为一个**阶段性的多智能体协作过程**，包含 6 个专门的智能体和 5 个执行阶段：
*   **阶段 I：场景路由（Scenario Routing）**：由 *Scenario Router* 将原始提示词分类到预定义的 10 种复杂场景（如抽象描述、复杂空间关系、多实体场景、因果与物理等）或“非困难”标签中。
*   **阶段 II：策略合成（Policy Synthesis）**：*Policy Generator* 根据场景标签生成针对性的重写策略 $\pi$，明确哪些隐含约束需要显式化。
*   **阶段 III：策略驱动优化（Policy-Conditioned Refinement）**：*Prompt Refiner* 在策略指导下，将原始提示词扩展为细节丰富、模型友好的描述。
*   **阶段 IV：语义验证（Semantic Verification）**：
    *   *Semantic Atomizer* 将原始提示词分解为“语义原子”（角色、物体、动作等）。
    *   *Entailment Validator* 验证优化后的提示词是否涵盖了所有原子，并标注为“蕴含（ET）”、“缺失（MS）”或“矛盾（CT）”。
*   **阶段 V：条件修正（Conditional Revision）**：若检测到语义缺失或矛盾，*Content Reviser* 会根据反馈进行针对性修改，直到满足接受标准。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **现有基准**：VBench、EvalCrafter、T2V-CompBench。
    *   **新基准（T2V-Complexity）**：论文引入了一个包含 1000 个提示词的新基准，涵盖 10 类复杂场景，每类 100 个，旨在进行细粒度诊断。
*   **对比方法**：直接提示（Direct Prompting）、Open-Sora 提示词优化器、RAPO（检索增强提示词优化）。
*   **底层 T2V 模型**：使用了 LaVie 和 Wan2.2 作为生成后端。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H100 GPU** 的服务器上运行。
*   **模型组件**：
    *   多智能体协作采用 **DeepSeek-V3** 作为基础大语言模型（LLM）。
    *   语义匹配采用 **BGE-M3** 嵌入模型。
*   **训练时长**：该框架属于 **Training-free（无需训练）** 的推理侧优化方法，因此不涉及模型训练时长，但多轮智能体交互会增加推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了大规模定量评估，涵盖了数千个视频生成任务。
*   **消融实验**：针对场景路由、策略生成、验证与自我修正机制分别进行了消融实验，验证了各组件的必要性。
*   **充分性评价**：实验设计较为充分，通过跨模型（LaVie/Wan）和跨数据集的对比，证明了方法的通用性和鲁棒性。引入新基准 T2V-Complexity 填补了复杂场景评估的空白，增强了实验的客观性。

### 6. 主要结论与发现
*   **性能提升**：SCMAPR 在所有基准测试中均取得 SOTA 性能。在 VBench 和 EvalCrafter 上平均分分别提升了 2.67% 和 3.28。
*   **语义保真度**：通过原子级验证和自我修正，SCMAPR 有效减少了重写过程中的“幻觉”和语义丢失。
*   **复杂场景优势**：在处理抽象概念、复杂空间布局和多实体交互时，SCMAPR 的提升尤为显著，生成的视频在时间一致性和物理合理性上表现更好。

### 7. 亮点
*   **多智能体解耦**：将策略制定与具体重写分离，降低了 LLM 在单次生成中处理复杂约束的压力。
*   **闭环修正机制**：引入了类似软件测试的“原子验证”环节，确保优化后的提示词不偏离用户初衷。
*   **分类法贡献**：系统性地定义了 T2V 的 10 类复杂场景，为后续研究提供了理论框架。

### 8. 不足与局限
*   **推理开销**：多智能体协作和多轮修正（Stage IV & V）显著增加了生成视频前的预处理时间。
*   **LLM 依赖**：框架的效果高度依赖于底层 LLM（如 DeepSeek-V3）的推理能力，若 LLM 无法准确识别场景或验证语义，系统性能会受限。
*   **分类局限性**：虽然提出了 10 类场景，但现实中的复杂场景可能存在重叠或超出此分类范围的情况。

（完）
