---
title: "ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning"
title_zh: ReRec：通过强化微调实现的推理增强型大语言模型推荐助手
authors: "Jiani Huang, Shijie Wang, Liangbo Ning, Wenqi Fan, Qing Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07851v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 通过强化微调框架提升大语言模型在复杂任务中的推理能力
tldr: 针对大语言模型在处理复杂推荐查询时推理能力不足的问题，本文提出了ReRec框架。该框架采用强化微调（RFT）技术，通过双图增强奖励塑造、推理感知优势估计和在线课程调度器，显著提升了模型在推荐任务中的多步推理与个性化对齐能力。实验表明，ReRec在保持通用能力的同时，推荐性能优于现有SOTA基线。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在复杂推荐场景中难以进行多步推理并提供精准个性化建议的挑战。
method: 提出一种包含双图奖励塑造、推理片段优势估计及动态课程调度的强化微调框架。
result: 实验证明ReRec在推荐准确性上超越了现有最先进模型，并成功保留了指令遵循等核心通用能力。
conclusion: 通过强化微调增强推理过程，是提升大语言模型推荐助手性能和逻辑性的有效途径。
---

## 摘要
随着大语言模型（LLMs）的兴起，对能够处理复杂查询并提供个性化、推理驱动推荐的智能推荐助手的需求日益增长。基于大语言模型的推荐系统展现出潜力，但在多步推理方面面临挑战，这凸显了对推理增强系统的需求。为了填补这一空白，我们提出了 ReRec，这是一种新型的强化微调（RFT）框架，旨在提升大语言模型在复杂推荐任务中的推理能力。我们的框架引入了三个关键组件：(1) 双图增强奖励塑造（Dual-Graph Enhanced Reward Shaping），将 NDCG@K 等推荐指标与查询对齐和偏好对齐分数相结合，为大语言模型优化提供细粒度的奖励信号；(2) 推理感知优势估计（Reasoning-aware Advantage Estimation），将大语言模型的输出分解为推理片段并对错误步骤进行惩罚，以增强推荐的推理能力；(3) 在线课程调度器（Online Curriculum Scheduler），动态评估查询难度并组织训练课程，以确保强化微调过程中的稳定学习。实验表明，ReRec 优于最先进的基准模型，并保留了指令遵循和通用知识等核心能力。我们的代码可在 https://github.com/jiani-huang/ReRec 获取。

## Abstract
With the rise of LLMs, there is an increasing need for intelligent recommendation assistants that can handle complex queries and provide personalized, reasoning-driven recommendations. LLM-based recommenders show potential but face challenges in multi-step reasoning, underscoring the need for reasoning-augmented systems. To address this gap, we propose ReRec, a novel reinforcement fine-tuning (RFT) framework designed to improve LLM reasoning in complex recommendation tasks. Our framework introduces three key components: (1) Dual-Graph Enhanced Reward Shaping, integrating recommendation metrics like NDCG@K with Query Alignment and Preference Alignment Scores to provide fine-grained reward signals for LLM optimization; (2) Reasoning-aware Advantage Estimation, which decomposes LLM outputs into reasoning segments and penalizes incorrect steps to enhance reasoning of recommendation; and (3) Online Curriculum Scheduler, dynamically assess query difficulty and organize training curriculum to ensure stable learning during RFT. Experiments demonstrate that ReRec outperforms state-of-the-art baselines and preserves core abilities like instruction-following and general knowledge. Our codes are available at https://github.com/jiani-huang/ReRec.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在推荐系统中展现了潜力，但现有的 LLM 推荐助手在处理**复杂、多步推理**的自然语言查询时（例如：根据模糊描述推断电影、识别主演并推荐该演员的其他作品）表现不佳。
*   **核心挑战**：
    1.  **奖励信号稀疏且粗糙**：传统的推荐指标（如 NDCG）仅关注最终结果是否匹配，无法为模型探索提供细粒度的反馈。
    2.  **缺乏中间推理监督**：现有的强化学习微调（RFT）通常将奖励分配给整个响应，导致模型难以识别推理过程中的具体错误。
    3.  **训练不稳定**：LLM 在处理复杂推荐任务时，初期往往因难度过大而难以获得有效奖励，导致收敛困难。

### 2. 方法论
ReRec 提出了一个基于强化微调（RFT）的框架，核心包含三个创新组件：
*   **双图增强奖励塑造 (Dual-Graph Enhanced Reward Shaping)**：
    *   除了基础的 **NDCG@K**，引入了 **查询对齐分数 (QAS)**（利用项-属性图评估推荐项是否满足显式约束，如导演、演员）和 **偏好对齐分数 (PAS)**（利用预训练的 LightGCN 在用户-项交互图上计算嵌入相似度，评估是否符合用户隐式偏好）。
*   **推理感知优势估计 (Reasoning-aware Advantage Estimation, RAAE)**：
    *   将 LLM 的输出分解为多个推理片段。如果某个片段涉及了最终被证明是错误的推荐项，则对该片段内的所有 token 施加惩罚权重（$w_{penalty}$），从而实现对中间推理步骤的细粒度监督。
*   **在线课程调度器 (Online Curriculum Scheduler)**：
    *   动态评估查询难度：根据模型在上一轮训练中的平均奖励计算难度得分。
    *   过滤与排序：过滤掉模型已掌握的简单样本，并按照“由易到难”的顺序重新组织训练数据，确保训练过程的平滑与稳定。

### 3. 实验设计
*   **数据集**：使用 **RecBench+** 评估基准，涵盖电影（Movie）和图书（Book）两个领域。查询被细分为显式条件（简单）、隐式条件（中等）、误导性信息（困难）、基于兴趣和基于人口统计学特征等子类。
*   **对比方法**：
    *   **LLM 基座**：Qwen-2.5-3B, Llama-3.2-3B, GPT-4o, DeepSeek-R1。
    *   **LLM 推荐模型**：TallRec, InteRecAgent, CRAG。
    *   **RFT 基准算法**：GRPO, REINFORCE++, RLOO。
*   **评估指标**：准确率（Accuracy）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H20 GPU (96GB)**。
*   **软件框架**：PyTorch 2.6.0, Verl 0.3.1, VLLM 0.8.5, Ray 2.46.0。
*   **训练细节**：学习率 5e-6，组大小（Group Size）为 5，最大响应长度 768，训练最多 15 个 epoch（带有早停机制）。

### 5. 实验数量与充分性
*   **实验规模**：在两个领域分别使用了 10,000 条查询进行训练，12,000 条进行测试。
*   **实验维度**：
    *   **主实验**：对比了 10 余种基线模型，验证了在不同难度查询下的优越性。
    *   **个性化实验**：验证了模型结合用户历史记录排除“强负样本”的能力。
    *   **泛化性实验**：包括跨领域（电影到图书）和跨任务（转移到序列推荐任务）的测试。
    *   **能力保留实验**：测试了模型在 RFT 后是否丢失了通用推理、指令遵循和基础知识能力。
    *   **消融实验**：逐一验证了奖励塑造、RAAE 和课程调度器的贡献。
*   **评价**：实验设计非常全面，不仅关注推荐精度，还深入探讨了 LLM 作为通用助手的综合素质，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：ReRec 在所有难度级别上均优于现有 RFT 算法和专门的 LLM 推荐模型。在困难任务（误导性查询）上，相比未训练模型提升了 440%。
*   **泛化能力强**：即使在未见过的领域或完全不同的推荐任务（如序列推荐）中，ReRec 依然表现出极强的零样本迁移能力。
*   **保留通用性**：与 SFT 导致严重的“灾难性遗忘”不同，ReRec 在提升推荐能力的同时，甚至增强了模型的通用推理能力（提升 21.6%），并保持了良好的指令遵循水平。

### 7. 优点
*   **细粒度反馈**：通过双图引入外部知识，解决了推荐任务中正样本唯一的“硬匹配”奖励稀疏问题。
*   **过程监督**：RAAE 机制巧妙地利用结果反推过程错误，无需昂贵的逐步人工标注或过程奖励模型（PRM）。
*   **训练稳定性**：动态课程学习有效缓解了强化学习在冷启动阶段的震荡。

### 8. 不足与局限
*   **单轮限制**：目前主要针对单轮复杂查询，未充分探讨多轮对话中的上下文管理和动态偏好演变。
*   **外部依赖**：奖励塑造依赖于高质量的项-属性图和预训练的协同过滤模型，在缺乏这些辅助数据的冷启动场景下可能受限。
*   **推理开销**：虽然训练是高效的，但 RFT 过程中的多次采样（Rollout）对算力仍有一定要求。

（完）
