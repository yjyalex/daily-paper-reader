---
title: "SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation"
title_zh: SCMAPR：面向复杂场景文本生成视频的自纠错多智能体提示词优化框架
authors: "Chengyi Yang, Pengzhen Li, Jiayin Qi, Aimin Zhou, Ji Wu, Ji Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05489v2"
tags: ["query:mas-gen"]
score: 9.0
evidence: 用于文本到视频生成的智能体协同优化过程
tldr: 针对文本生成视频（T2V）在复杂场景下因提示词模糊导致的生成质量差问题，本文提出SCMAPR框架。该框架采用多智能体协作模式，通过场景感知路由、策略驱动优化及自我修正机制，实现提示词的精细化重写。此外，研究者还推出了T2V-Complexity基准测试集。实验证明，SCMAPR在多个主流榜单上显著提升了视频的语义一致性和生成质量。
selection_source: fresh_fetch
motivation: 现有的T2V系统在处理复杂场景时，常因提示词的歧义和描述不足而导致生成效果不佳。
method: 提出一种多智能体框架，通过场景分类路由、策略化重写和基于语义验证的自我修正机制来优化提示词。
result: 在VBench、EvalCrafter和T2V-CompBench等多个基准测试中，SCMAPR均显著优于现有主流基线模型。
conclusion: SCMAPR通过多智能体协同优化，有效解决了复杂场景下的T2V提示词细化问题，提升了视频生成的准确性。
---

## 摘要
文本生成视频（T2V）受益于扩散模型的最新进展，但现有系统在复杂场景下仍面临挑战，而文本提示词的歧义性和描述不足通常会加剧这一问题。在这项工作中，我们将复杂场景下的提示词优化建模为一个阶段性的多智能体优化过程，并提出了 SCMAPR，即一种用于 T2V 提示词的场景感知且自纠错的多智能体提示词优化框架。SCMAPR 协调专门的智能体来执行以下操作：(i) 将每个提示词路由到基于分类法的场景以进行策略选择；(ii) 合成场景感知的重写策略并执行基于策略的优化；(iii) 进行结构化语义验证，并在检测到违规时触发条件修正。为了明确 T2V 提示词中复杂场景的构成，提供代表性示例，并在这些挑战性条件下进行严格评估，我们进一步引入了 T2V-Complexity，这是一个专门由复杂场景提示词组成的 T2V 基准测试集。在 3 个现有基准测试和我们的 T2V-Complexity 基准测试上进行的广泛实验表明，SCMAPR 在复杂场景下持续提升了文本-视频对齐度和整体生成质量，在 VBench 和 EvalCrafter 上的平均得分分别提升了高达 2.67% 和 3.28，在 T2V-CompBench 上相比 3 个最先进的基线模型提升了高达 0.028。

## Abstract
Text-to-Video (T2V) generation has benefited from recent advances in diffusion models, yet current systems still struggle under complex scenarios, which are generally exacerbated by the ambiguity and underspecification of text prompts. In this work, we formulate complex-scenario prompt refinement as a stage-wise multi-agent refinement process and propose SCMAPR, i.e., a scenario-aware and Self-Correcting Multi-Agent Prompt Refinement framework for T2V prompting. SCMAPR coordinates specialized agents to (i) route each prompt to a taxonomy-grounded scenario for strategy selection, (ii) synthesize scenario-aware rewriting policies and perform policy-conditioned refinement, and (iii) conduct structured semantic verification that triggers conditional revision when violations are detected. To clarify what constitutes complex scenarios in T2V prompting, provide representative examples, and enable rigorous evaluation under such challenging conditions, we further introduce {T2V-Complexity}, which is a complex-scenario T2V benchmark consisting exclusively of complex-scenario prompts. Extensive experiments on 3 existing benchmarks and our T2V-Complexity benchmark demonstrate that SCMAPR consistently improves text-video alignment and overall generation quality under complex scenarios, achieving up to 2.67\% and 3.28 gains in average score on VBench and EvalCrafter, and up to 0.028 improvement on T2V-CompBench over 3 State-Of-The-Art baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：尽管文本生成视频（T2V）模型取得了巨大进步，但在处理**复杂场景**（如抽象语义、复杂空间关系、多实体交互等）时表现依然不佳。
*   **核心问题**：用户输入的原始提示词（Prompts）通常存在**歧义性**和**描述不足**的问题，导致视频生成模型难以准确捕捉用户意图，容易产生语义漂移、幻觉或逻辑错误。
*   **研究目标**：提出一种自动化的提示词优化框架，将模糊的短文本转化为结构化、高质量且符合视频生成逻辑的详细描述。

### 2. 方法论
SCMAPR 将提示词优化建模为一个**五阶段的多智能体协作过程**：
*   **阶段 I：场景路由（Scenario Routing）**：由 Scenario Router 智能体根据预定义的 10 类复杂场景分类法（如因果物理、时间一致性、空间关系等）对输入进行分类。
*   **阶段 II：策略合成（Policy Synthesis）**：Policy Generator 智能体根据场景标签动态生成针对性的重写策略，而非使用固定模板。
*   **阶段 III：策略驱动优化（Refinement）**：Prompt Refiner 智能体在策略指导下，将原始提示词扩展为包含细节描述（人物、动作、环境、镜头等）的丰富文本。
*   **阶段 IV：语义原子化验证（Semantic Verification）**：
    *   **原子化提取**：将原提示词拆解为不可分割的语义原子（如“猫”、“在跳舞”）。
    *   **蕴含验证**：通过 LLM 判断优化后的文本是否完全涵盖了这些原子，并标记为“蕴含（ET）”、“缺失（MS）”或“矛盾（CT）”。
*   **阶段 V：条件修正（Conditional Revision）**：若检测到语义缺失或矛盾，Content Reviser 智能体将根据反馈进行针对性修正，直到通过验证。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **现有基准**：VBench、EvalCrafter、T2V-CompBench。
    *   **自建基准**：**T2V-Complexity**，包含 1000 条专门针对 10 类复杂场景设计的提示词，旨在进行更细粒度的诊断。
*   **视频生成底座（Backbones）**：采用了 LaVie 和 Wan2.2 两种主流开源 T2V 模型。
*   **对比方法**：
    *   直接使用用户输入（Direct Prompting）。
    *   Open-Sora 的提示词优化器。
    *   RAPO（基于检索增强的提示词优化）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H100 GPU** 的服务器上运行。
*   **模型底座**：使用 **DeepSeek-V3** 作为多智能体系统的核心推理引擎，使用 **BGE-M3** 作为语义匹配的嵌入模型。
*   **训练说明**：该框架属于**训练无关（Training-free）**方法，主要开销在于推理阶段的 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了大规模定量评估，涵盖了美学质量、背景一致性、运动平滑度、文本-视频对齐等多个维度。
*   **消融实验**：专门设计了消融实验，分别验证了“场景路由”、“策略生成”以及“验证与自纠错”模块对最终性能的贡献。
*   **充分性评价**：实验设计较为全面，通过跨模型（LaVie/Wan）和跨数据集的对比，证明了方法的鲁棒性和通用性。

### 6. 主要结论与发现
*   **性能提升**：SCMAPR 在所有基准测试中均显著优于基线方法。在 VBench 上平均分提升 2.67%，在 EvalCrafter 上提升 3.28。
*   **语义保真度**：通过自纠错机制，SCMAPR 有效解决了长文本重写中常见的“语义丢失”问题。
*   **幻觉抑制**：定性分析显示，优化后的提示词能显著减少视频模型生成无关物体（如无端出现的面部）的现象，使生成内容更符合用户初衷。

### 7. 优点
*   **场景感知能力**：能够识别提示词的难点所在（如空间关系 vs 物理规律），并采取差异化优化策略。
*   **闭环自纠错**：引入了类似软件测试的“原子化验证”机制，确保了重写过程的严谨性。
*   **即插即用**：作为前端优化模块，无需对底座视频模型进行任何微调。
*   **基准贡献**：提出的 T2V-Complexity 数据集为社区研究复杂场景下的视频生成提供了重要工具。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮 LLM 交互（分类、生成、多次验证与修正），其推理时间显著高于简单的提示词扩展方法。
*   **LLM 依赖性**：系统的上限受限于底层 LLM 的推理能力，若 LLM 分类错误或验证失误，会影响最终效果。
*   **分类覆盖面**：虽然定义了 10 类场景，但随着 T2V 技术的发展，可能仍存在未被覆盖的新型复杂场景（如极长视频的逻辑连贯性）。

（完）
