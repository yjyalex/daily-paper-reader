---
title: "ZeroCoder: Can LLMs Improve Code Generation Without Ground-Truth Supervision?"
title_zh: ZeroCoder：大语言模型能否在没有真值监督的情况下提升代码生成能力？
authors: "Lishui Fan, Mouxiang Chen, Tingwei Zhu, Kui Liu, Xin Xia, Shanping Li, Zhongxin Liu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07864v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 具有可验证奖励的强化学习用于代码生成
tldr: 本研究针对代码生成中强化学习（RLVR）依赖人工标注测试用例的瓶颈，提出了ZeroCoder框架。该框架通过代码生成器（Coder）与测试生成器（Tester）的协同进化，利用自生成的代码-测试交互反馈进行联合训练。通过共识选择算法和DyB4动态贝叶斯选择器解决选择器漂移问题，实现了在完全无标注监督下的性能提升，显著增强了模型的代码编写与测试验证能力。
selection_source: fresh_fetch
motivation: 传统的代码生成强化学习方法受限于稀缺且昂贵的人工测试用例，而现有的自生成测试方法因模型判别力不足而效果受限。
method: 提出一种完全无标注的协同进化框架ZeroCoder，通过执行反馈构建通过矩阵，并利用DyB4贝叶斯选择器动态校准代码与测试的质量评估。
result: "在多个模型和基准测试上，ZeroCoder显著提升了代码生成准确率（最高达21.6%）和测试生成质量（提升24.3%），接近有监督学习水平。"
conclusion: 该研究证明了通过代码与测试的相互博弈与协同进化，大语言模型可以在没有外部真值监督的情况下实现自我性能的持续迭代与提升。
---

## 摘要
代码生成在软件工程中至关重要，而带有可验证奖励的强化学习（RLVR）是通过基于执行的反馈来改进代码生成的一种强大范式。然而，大多数 RLVR 流水线依赖于人工编写的测试，这使得稀缺且昂贵的监督成为了进步的瓶颈。现有工作尝试使用自生成的测试来确定奖励，但由于模型在测试生成方面的性能欠佳，缺乏具有辨别力的测试限制了其效果。我们的目标是在没有真值监督的情况下，通过协同演化代码生成和测试生成来改进代码生成，使它们的交互产生更具信息量的监督。为此，我们提出了 ZeroCoder，这是一个完全无标签的协同演化框架，它利用自生成的代码-测试交互产生的执行反馈，联合训练代码生成器（Coder）和测试生成器（Tester）。对于每个问题，ZeroCoder 将采样的解决方案针对采样的测试进行执行，以形成通过矩阵，通过可插拔的选择算法识别出可能正确的解决方案和一致性测试的共识子集，并推导出特定角色的奖励。为了确保奖励质量，ZeroCoder 通过基于排名的预过滤来过滤低信息实例，并使用平衡有效性和变异驱动辨别力的课程学习来训练 Tester。我们进一步识别了选择器漂移，即固定选择规则在协同演化过程中逐渐失准的现象，并引入了 DyB4，这是一种贝叶斯选择器，仅需 10 个标注实例即可动态重新校准其先验。在三个模型和六个基准测试中，ZeroCoder 一致地提高了代码生成和测试生成的性能。在完全无标签的设置下，它在 Qwen2.5-Coder-7B-Instruct 上相比基础模型将代码生成性能提升了高达 14.5%。配合 DyB4，增益达到了 21.6%，同时测试生成性能提升了 24.3%，接近有监督的性能。

## Abstract
Code generation is important in software engineering, and Reinforcement Learning with Verifiable Rewards (RLVR) is a powerful paradigm to improve it through execution-based feedback. However, most RLVR pipelines rely on human-curated tests, making progress bottlenecked by scarce and costly supervision. Existing work tried to use self-generated tests to ground rewards, but the lack of discriminative tests constrains the effect due to the sub-optimal performance of the model on test generation. We aim to improve code generation without ground-truth supervision by co-evolving code and test generation, so that their interactions yield progressively more informative supervision. To this end, we present ZeroCoder, a fully label-free co-evolutionary framework that jointly trains a Coder and a Tester using execution feedback from self-generated code-test interactions. For each problem, ZeroCoder executes sampled solutions against sampled tests to form a passing matrix, identifies a consensus subset of likely-correct solutions and consistent tests via a pluggable selection algorithm, and derives role-specific rewards. To ensure reward quality, ZeroCoder filters low-information instances via rank-based pre-filtering and trains the Tester with a curriculum balancing validity and mutation-driven discriminativeness. We further identify selector drift, the progressive miscalibration of fixed selection rules during co-evolution, and introduce DyB4, a Bayesian selector that uses as few as 10 labeled instances to recalibrate its priors dynamically. Across three models and six benchmarks, ZeroCoder consistently improves code generation and test generation. In the fully label-free setting, it improves code generation by up to 14.5% over the base model on Qwen2.5-Coder-7B-Instruct. With DyB4, the gain reaches 21.6%, while test generation improves by 24.3%, approaching oracle-supervised performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《ZeroCoder: Can LLMs Improve Code Generation Without Ground-Truth Supervision?》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在代码生成领域，带有可验证奖励的强化学习（RLVR）虽强，但高度依赖人工标注的单元测试（Ground-Truth）。自生成测试虽能降低成本，但由于模型生成的测试往往缺乏辨别力（如生成 `assert True`），导致奖励信号质量低下。
*   **核心问题**：大语言模型（LLM）能否在完全没有真值监督的情况下，通过自我博弈和协同进化来提升代码生成能力？
*   **整体含义**：论文提出了 **ZeroCoder** 框架，通过让模型同时扮演“代码编写者（Coder）”和“测试编写者（Tester）”的角色，利用两者之间的交互反馈实现双向进化。

### 2. 方法论
*   **核心思想**：利用代码与测试的“对偶性”，通过执行反馈构建“通过矩阵（Passing Matrix）”，从中提取共识信号作为奖励，驱动模型自我迭代。
*   **关键技术细节**：
    *   **离线秩预过滤（Rank-based Pre-filtering）**：在训练前，计算代码-测试交互矩阵的“秩（Rank）”。低秩矩阵意味着交互模式单一（如全部通过或全部失败），缺乏信息量。ZeroCoder 仅保留高秩任务以确保训练信号的丰富性。
    *   **协同进化强化学习**：Coder 生成代码，Tester 生成测试。两者交叉执行形成矩阵 $E$。
    *   **角色特定奖励**：
        *   **Coder 奖励**：若代码被选择器（Selector）判定为共识子集中的“可能正确”代码，则给予奖励。
        *   **Tester 奖励**：包含三部分：**代理一致性**（通过共识代码）、**变异驱动的辨别力**（利用变异测试技术，奖励能识别出错误变异体的测试）、**格式惩罚**。
    *   **动态 B4 选择器（$DyB4$）**：识别出“选择器漂移”现象（即固定选择规则在模型进化过程中会失准）。$DyB4$ 通过极少量（如 10 个）标注样本动态校准贝叶斯先验，保持奖励信号的准确性。

### 3. 实验设计
*   **数据集/场景**：
    *   **代码生成**：MBPP、LiveCodeBench（2024.10-2025.02）、APPS（300个子集）、CodeForces（300个子集）。
    *   **测试生成**：MBPP、LiveCodeBench。
*   **基准模型**：Qwen2.5-1.5B-Instruct、Qwen3-4B、Qwen2.5-Coder-7B-Instruct。
*   **对比方法**：基础模型（Base）、离线测试驱动 RL（固定测试集）、在线测试驱动 RL（仅训练 Coder）、Oracle（使用真值监督的上限）。

### 4. 资源与算力
*   **硬件环境**：使用了由 **4 张 NVIDIA A100 GPU** 组成的集群。
*   **训练参数**：采用 GRPO 算法，训练 150 步，Batch Size 为 32，学习率为 $1 \times 10^{-6}$。
*   **采样配置**：每步为每个问题采样 8 个代码和 8 个测试。
*   **开销分析**：$DyB4$ 校准仅占每步耗时的 2%，主要耗时集中在代码执行（47.6%）和变异测试（35.7%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模和家族的模型，并在 6 个主流基准测试上进行了验证。
*   **消融实验**：针对变异奖励（$r_{mut}$）、交互多样性阈值（$\tau$）、校准集大小（$|H|$）进行了详细的消融分析。
*   **客观性**：通过对比“完全无标签”和“极少量标签（10个）”两种场景，并与 Oracle（真值监督）进行对比，证明了该方法在不同资源约束下的有效性，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **协同进化有效性**：在完全无标签的情况下，ZeroCoder 在 Qwen2.5-Coder-7B 上将代码生成性能提升了 14.5%。
*   **$DyB4$ 的优越性**：仅需 10 个标注样本进行动态校准，性能增益可扩大至 21.6%，且测试生成能力提升 24.3%，表现接近有监督的 Oracle 水平。
*   **选择器漂移**：实验证实，固定的选择算法（如 MaxPass, CodeT）在模型能力提升过程中会产生偏差，动态校准是维持高质量奖励的关键。
*   **变异奖励至关重要**：如果没有变异驱动的奖励，Tester 会倾向于生成平庸的测试，导致 Coder 性能退化。

### 7. 优点
*   **低成本/无标签**：极大地缓解了 RLVR 对大规模高质量人工测试用例的依赖。
*   **动态校准机制**：$DyB4$ 的引入巧妙地解决了强化学习中非平稳分布导致的选择器失效问题。
*   **双向提升**：不仅提升了代码生成能力，还显著增强了模型的测试编写和程序理解能力。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 及以下规模的模型，尚未在超大规模模型（如 70B+）上验证。
*   **计算开销**：虽然节省了标注成本，但训练过程中的大量代码执行和变异测试增加了计算资源的消耗。
*   **任务类型**：目前主要针对函数级代码生成，对于更复杂的系统级或多文件代码生成的适用性有待探索。

（完）
