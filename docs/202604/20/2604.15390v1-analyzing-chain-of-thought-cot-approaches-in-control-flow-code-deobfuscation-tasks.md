---
title: Analyzing Chain of Thought (CoT) Approaches in Control Flow Code Deobfuscation Tasks
title_zh: 分析控制流代码反混淆任务中的思维链（CoT）方法
authors: "Seyedreza Mohseni, Sarvesh Baskar, Edward Raff, Manas Gaur"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15390v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 用于代码分析中逐步推理的思维链提示
tldr: 本研究针对代码反混淆这一耗时耗力的逆向工程难题，探索了利用大语言模型（LLM）结合思维链（CoT）提示词的方法。研究重点关注控制流平坦化和虚假谓词等混淆技术，通过引导模型进行显式的逐步推理，在五个主流模型上验证了该方法的有效性。结果显示，CoT 显著提升了控制流图的结构恢复和程序语义保持能力，证明了 LLM 作为反混淆辅助工具的巨大潜力。
selection_source: fresh_fetch
motivation: 传统代码反混淆依赖复杂工具和大量人工，效率低下且成本高昂，亟需更自动化的辅助手段。
method: 提出一种基于思维链（CoT）提示的方法，引导大语言模型通过显式的逐步推理来处理控制流平坦化和虚假谓词等混淆技术。
result: "实验表明 CoT 显著提升了反混淆质量，其中 GPT5 在控制流图重建和语义保持方面分别比零样本提示提升了约 16% 和 20.5%。"
conclusion: CoT 引导的大语言模型能有效辅助代码反混淆，提高代码可解释性并减少逆向工程的人工负担。
---

## 摘要
代码反混淆是在保持程序原始行为的同时，恢复其可读版本的任务。在实践中，这通常需要使用复杂且昂贵的分析工具进行数天甚至数月的手动工作。在本文中，我们探索了一种基于思维链（CoT）提示的替代方法，即引导大语言模型通过专门为代码分析定制的显式、逐步推理。我们专注于控制流混淆，包括控制流平坦化（CFF）、虚假谓词（Opaque Predicates）及其组合，并衡量了控制流图的结构恢复和程序语义的保持。我们评估了五种最先进的大语言模型，并表明与简单提示相比，CoT 提示显著提高了反混淆质量。我们在多种标准 C 语言基准测试集上验证了我们的方法，并报告了使用控制流图结构指标和基于输出相似性的语义指标的结果。在测试的模型中，通过应用 CoT，GPT5 实现了最强的综合性能，与零样本提示相比，在我们的基准测试中，控制流图重建平均提升了约 16%，语义保持平均提升了约 20.5%。我们的结果还表明，模型性能不仅取决于混淆级别和所选的混淆器，还取决于原始控制流图的内在复杂性。总之，这些发现表明，受 CoT 引导的大语言模型可以作为代码反混淆的有效辅助工具，提供更好的代码可解释性、更准确的控制流图重建以及更好的程序行为保持，同时有可能减少逆向工程所需的手动工作量。

## Abstract
Code deobfuscation is the task of recovering a readable version of a program while preserving its original behavior. In practice, this often requires days or even months of manual work with complex and expensive analysis tools. In this paper, we explore an alternative approach based on Chain-of-Thought (CoT) prompting, where a large language model is guided through explicit, step-by-step reasoning tailored for code analysis. We focus on control flow obfuscation, including Control Flow Flattening (CFF), Opaque Predicates, and their combination, and we measure both structural recovery of the control flow graph and preservation of program semantics. We evaluate five state-of-the-art large language models and show that CoT prompting significantly improves deobfuscation quality compared with simple prompting. We validate our approach on a diverse set of standard C benchmarks and report results using both structural metrics for control flow graphs and semantic metrics based on output similarity. Among the tested models and by applying CoT, GPT5 achieves the strongest overall performance, with an average gain of about 16% in control-flow graph reconstruction and about 20.5% in semantic preservation across our benchmarks compared to zero-shot prompting. Our results also show that model performance depends not only on the obfuscation level and the chosen obfuscator but also on the intrinsic complexity of the original control flow graph. Collectively, these findings suggest that CoT-guided large language models can serve as effective assistants for code deobfuscation, providing improved code explainability, more faithful control flow graph reconstruction, and better preservation of program behavior while potentially reducing the manual effort needed for reverse engineering.

---

## 论文详细总结（自动生成）

这是一份关于论文《Analyzing Chain of Thought (CoT) Approaches in Control Flow Code Deobfuscation Tasks》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的代码反混淆（Deobfuscation）依赖昂贵的人工逆向工程和复杂的静态/动态分析工具。传统的符号执行方法在面对复杂混淆时会遭遇“路径爆炸”问题。
*   **研究动机**：探索大语言模型（LLM）在处理低级代码（如 LLVM-IR）和高级代码（C 语言）反混淆任务中的潜力。研究重点在于如何通过**思维链（CoT）提示词**引导模型进行逻辑推理，以识别并移除控制流平坦化（CFF）和虚假谓词（Opaque Predicates）等复杂的控制流混淆技术。

### 2. 方法论：CoT 引导的反混淆框架
论文提出了一个包含五个阶段的逻辑推理框架，通过 CoT 引导模型逐步执行：
1.  **混淆检测**：解析抽象语法树（AST），识别调度器结构（CFF 特征）和代数不变性模式（虚假谓词特征）。
2.  **控制流平坦化恢复**：提取调度器中的各个 case 块，分析状态变量（$\sigma$）的演变，构建状态转换图。
3.  **控制流重建**：检测循环回边，进行拓扑排序，按自然执行顺序重新排列基本块。
4.  **虚假谓词消除**：评估谓词的不变性结果（恒真或恒假），移除不可达的冗余分支。
5.  **代码清理**：识别并移除死变量（如调度器状态变量）和死代码，进行语法规范化。
*   **关键技术细节**：利用全局变量追踪（Variable Provenance）来区分真实的程序逻辑与混淆产生的伪影。

### 3. 实验设计
*   **数据集与基准测试**：
    *   选取了 12 个标准 C 语言算法基准，分为两组：**Group 1（低复杂度）**如归并排序、Dijkstra、矩阵乘法等；**Group 2（高复杂度）**如 N 皇后问题、AVL 树、哈夫曼编码。
    *   使用 **Tigress** 和 **O-LLVM** 两个开源混淆器生成混淆变体。
*   **对比方法**：对比了五种模型（GPT5、o3、DeepSeek-V2、Qwen-3 Max、QwQ-32B）在 **Zero-shot（零样本）** 与 **CoT（思维链）** 提示下的表现。
*   **评估指标**：
    *   **SRS（结构相似性得分）**：通过图编辑距离（GED）衡量反混淆后的控制流图（CFG）与原始 CFG 的结构接近度。
    *   **BLEU 得分**：衡量反混淆代码运行输出与原始程序输出的语义一致性。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   实验主要基于商业和开源模型的 API 或推理模式进行。模型配置包括 128k 上下文窗口，允许最高 100k 输出 token，温度设定为 0.8（o3 除外）。

### 5. 实验数量与充分性
*   **实验规模**：进行了两组主要实验。实验 1 涵盖了三种混淆模式（虚假谓词、CFF、组合混淆）在所有模型上的表现；实验 2 进行了压力测试，将虚假谓词分支数量从 0 增加到 40。
*   **充分性评价**：实验设计较为充分，包含了消融研究（CoT vs. 非 CoT）和复杂度敏感性分析。通过对比不同混淆器和不同复杂度的算法，验证了方法的普适性与局限性，实验过程客观公平。

### 6. 主要结论与发现
*   **CoT 的显著作用**：CoT 提示显著提升了反混淆质量。以 GPT5 为例，其 CFG 重建能力提升了约 16%，语义保持能力提升了 20.5%。
*   **模型表现差异**：GPT5 在所有设置中表现最强；DeepSeek-V2 和 Qwen-3 Max 表现接近且均优于 o3 和 QwQ-32B。
*   **复杂度影响**：反混淆性能不仅受混淆强度影响，还高度取决于原始程序的 CFG 内在复杂度。Group 2（复杂算法）的得分明显低于 Group 1。
*   **混淆器差异**：O-LLVM 生成的混淆相对容易被破解，而 Tigress 引入了更多的结构多样性，更具挑战性。

### 7. 优点与亮点
*   **结构化推理**：将复杂的反混淆任务分解为可解释的五个阶段，模拟了人类逆向工程专家的思维过程。
*   **多维度评估**：结合了结构（SRS）和语义（BLEU）双重指标，比单纯的代码文本对比更科学。
*   **可解释性**：CoT 过程使模型的推理步骤透明化，便于安全分析师审查和纠错。

### 8. 不足与局限
*   **幻觉问题（Hallucination）**：模型仍会产生幻觉，包括**简单遗漏**（漏掉头文件）、**结构幻觉**（数据结构定义错误）和最严重的**语义幻觉**（逻辑错误，如将取最大值误改为求和）。
*   **工具覆盖有限**：仅测试了两个开源混淆器，未涉及商业级混淆工具（如 VMProtect 或 Themida）。
*   **稳定性风险**：随着混淆层数增加或虚假分支增多，模型性能（尤其是推理较弱的模型）会出现断崖式下跌。
*   **提示词依赖**：性能提升部分归功于提示词中包含的特定模式示例，这表明模型对提示词设计较为敏感。

（完）
