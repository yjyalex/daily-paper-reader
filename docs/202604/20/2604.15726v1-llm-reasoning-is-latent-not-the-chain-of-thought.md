---
title: "LLM Reasoning Is Latent, Not the Chain of Thought"
title_zh: 大语言模型推理是隐性的，而非思维链
authors: Wenshuo Wang
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15726v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 大模型推理作为潜状态轨迹与思维链的对比
tldr: 这篇立场论文提出LLM推理本质上是隐藏状态轨迹的形成，而非表面上的思维链（CoT）。作者对比了隐藏状态、显式CoT和通用串行计算三种假设，结合实证研究和计算审计示例，证明了隐藏状态才是推理的主要媒介。该研究为理解LLM的忠实性与可解释性提供了新视角，强调了区分内部计算与表面痕迹的重要性。
selection_source: fresh_fetch
motivation: 旨在厘清LLM推理的真实载体，解决思维链是否能真实反映模型内部推理过程的争议。
method: 提出了三种竞争假设并结合现有实证证据与计算审计示例，对表面痕迹、隐藏状态和计算预算进行了解耦分析。
result: 研究发现当前证据最有力地支持推理是由隐藏状态轨迹介导的假设，而非显式的思维链文字。
conclusion: 建议将隐藏状态动力学作为LLM推理研究的默认对象，并在评估推理能力时严格区分表面痕迹与内部状态。
---

## 摘要
本观点论文认为，大语言模型（LLM）的推理应被视为隐状态轨迹的形成，而非忠实的表层思维链（CoT）。这一点至关重要，因为关于忠实性、可解释性、推理基准以及推理时干预的论断，都取决于该领域将什么视为推理的主要研究对象。我们探讨了在分离三个经常混淆的因素后，该研究对象应当是什么，并形式化了三个竞争性假设：H1，推理主要由隐状态轨迹介导；H2，推理主要由显式表层 CoT 介导；H0，大多数明显的推理提升与其说是源于某种特权表征对象，不如说是源于通用的串行计算。通过在此框架下重新梳理近期的实证、机制和综述工作，并增加分解了表层痕迹、隐性干预和匹配预算扩展的计算审计实例，我们发现目前的证据最有力地支持将 H1 作为默认工作假设，而非一个独立于任务的定论。因此，我们提出两项建议：该领域应将隐状态动力学视为 LLM 推理的默认研究对象，并应采用能显式解耦表层痕迹、隐状态和串行计算的设计来评估推理。

## Abstract
This position paper argues that large language model (LLM) reasoning should be studied as latent-state trajectory formation rather than as faithful surface chain-of-thought (CoT). This matters because claims about faithfulness, interpretability, reasoning benchmarks, and inference-time intervention all depend on what the field takes the primary object of reasoning to be. We ask what that object should be once three often-confounded factors are separated and formalize three competing hypotheses: H1, reasoning is primarily mediated by latent-state trajectories; H2, reasoning is primarily mediated by explicit surface CoT; and H0, most apparent reasoning gains are better explained by generic serial compute than by any privileged representational object. Reorganizing recent empirical, mechanistic, and survey work under this framework, and adding compute-audited worked exemplars that factorize surface traces, latent interventions, and matched budget expansions, we find that current evidence most strongly supports H1 as a default working hypothesis rather than as a task-independent verdict. We therefore make two recommendations: the field should treat latent-state dynamics as the default object of study for LLM reasoning, and it should evaluate reasoning with designs that explicitly disentangle surface traces, latent states, and serial compute.

---

## 论文详细总结（自动生成）

这篇论文是一篇关于大语言模型（LLM）推理本质的立场论文（Position Paper），旨在厘清 LLM 推理究竟是发生在可见的文字中，还是隐藏在模型的内部状态里。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然思维链（CoT）显著提升了 LLM 的推理能力，但学术界对“推理”的本质存在争议。目前的实证研究往往将**表层文字（Surface Trace）**、**隐状态动力学（Latent Dynamics）**和**串行计算量（Serial Compute）**这三个因素混为一谈。
*   **核心问题**：当这三个因素被解耦后，哪一个是 LLM 推理的主要科学研究对象？这直接影响到我们如何评估模型的忠实性、可解释性以及如何设计推理干预手段。

### 2. 论文提出的方法论
论文将现有的观点形式化为三个竞争性假设，并提出了一个解耦评估框架：
*   **三个假设**：
    *   **H1（隐轨迹介导）**：推理主要发生在隐状态轨迹 $Z$ 中，表层 CoT 只是部分接口。
    *   **H2（表层 CoT 介导）**：推理主要由显式的自然语言序列 $S$ 驱动。
    *   **H0（通用串行计算零假设）**：推理提升仅源于额外的计算预算 $B$，与表征形式无关。
*   **核心思想**：通过“计算审计（Compute-audited）”设计，确保在相同的推理成本下对比不同方法。
*   **关键技术细节**：
    *   **等效预算公式 ($B_{eq}$)**：综合考虑解码步数、KV 缓存、钩子操作、验证器调用等，计算统一的推理成本。
    *   **六臂实验设计**：包含基准、目标表层干预、匹配表层控制、目标隐性干预、匹配隐性控制、纯计算扩展，以实现真正的变量分离。

### 3. 实验设计
*   **数据集与场景**：
    *   **受控层（Controlled Tier）**：使用生成的矩阵任务，涵盖算术进位、图遍历、世界状态规划、代码语义。
    *   **自然层（Naturalistic Tier）**：
        *   **GSM8K-Platinum**：普通算术推理（对应 H1 场景）。
        *   **HotpotQA**：具有构成性检索计划的任务（对应 H2 场景）。
        *   **MATH-500**：搜索密集型数学推理（对应 H0 场景）。
        *   **HumanEval+**：代码合成（混合场景）。
*   **对比方法**：对比了 CoT 提示词（表层）、隐性空间推理/转向（隐性）、自我一致性/搜索（计算量）。
*   **基准（Benchmark）**：准确率-成本前沿曲线（Accuracy-Cost Frontier）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **Qwen3-8B、Qwen3-32B** 和 **Llama-3.1-8B-Instruct**。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。其重点在于通过 $B_{eq}$ 公式在算法层面标准化推理时的计算开销，而非报告物理算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型系列，在 4 类受控任务和 4 个标准自然语言基准上进行了测试。
*   **充分性与客观性**：
    *   实验设计非常严谨，引入了**中介测试（Mediator Tests）**，包括时间领先性、必要性（消融）、充分性（修补）和特异性测试。
    *   通过对比“保留 $Z$ 损坏 $S$”与“保留 $S$ 消融 $Z$”的效果，直接回应了 H1 与 H2 的竞争，实验逻辑具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **H1 是默认解释**：在大多数普通推理任务中，隐状态轨迹是推理的主要载体。模型往往在写出文字之前就已经在内部形成了答案的“承诺”。
*   **场景依赖性**：
    *   **隐性方法**在普通场景（如 GSM8K）中表现最优。
    *   **表层方法**仅在任务强制要求显式步骤（如工具调用、外部计划）时才具有解释力。
    *   **计算量方法**在需要深度搜索的任务（如 MATH）中占主导地位。
*   **结论**：LLM 推理本质上是隐性的，CoT 并不总是忠实的推理记录。

### 7. 优点
*   **理论高度**：首次系统性地解耦了推理中的文字、状态和计算量，为该领域提供了清晰的分类学。
*   **实验严谨**：提出的计算审计方法解决了以往研究中因计算量不匹配导致的结论偏差。
*   **实用建议**：为未来的模型评估和安全监控提供了明确的指导方向（关注隐状态而非仅看文字）。

### 8. 不足与局限
*   **隐状态的可解释性**：虽然证明了隐状态的重要性，但如何精确定义和提取这些“任务相关轨迹”仍具挑战。
*   **应用限制**：目前的隐性干预手段（如 Latent Steering）在复杂度和通用性上可能仍不如简单的 CoT 提示词。
*   **模型覆盖**：虽然使用了 Qwen 和 Llama，但尚未在闭源模型（如 GPT-4, Claude）上验证（受限于无法获取隐状态）。

（完）
