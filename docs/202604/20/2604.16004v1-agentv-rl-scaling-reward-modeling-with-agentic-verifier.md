---
title: "AgentV-RL: Scaling Reward Modeling with Agentic Verifier"
title_zh: AgentV-RL：利用智能体化验证器扩展奖励建模
authors: "Jiazheng Zhang, Ziche Fu, Zhiheng Xi, Wenqing Jing, Mingxu Chai, Wei He, Guoqiang Zhang, Chenghao Fan, Chenxin An, Wenxiang Chen, Zhicheng Liu, Haojie Pan, Dingwei Zhu, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16004v1"
tags: ["query:rl-nlplr"]
score: 9.0
evidence: 智能体验证器将奖励建模转化为推理的多轮审议过程
tldr: 针对LLM在复杂推理中验证器易受错误传播和缺乏外部知识约束的问题，本文提出Agentic Verifier框架。该框架将奖励建模转化为多轮、工具增强的审议过程，通过前向追踪和后向验证的对冲机制实现双向评估。结合强化学习（AgentV-RL），验证器能自主交替使用工具与推理。实验表明，4B规模的模型在性能上显著超越现有SOTA ORM，为智能体化奖励建模提供了新范式。
selection_source: fresh_fetch
motivation: 传统验证器在复杂任务中容易因中间推理错误导致误判，且缺乏外部工具支持，难以处理计算或知识密集型任务。
method: 提出Agentic Verifier框架，利用前向和后向智能体进行双向验证，并通过AgentV-RL强化学习训练模型自主调用工具进行多轮审议。
result: "实验显示Agentic Verifier在多种测试时间缩放场景下均有提升，其4B版本在性能上超过现有最强ORM模型25.2%。"
conclusion: 将奖励建模转化为具备工具使用能力的智能体化多轮推理过程，是提升复杂推理任务验证可靠性的有效途径。
---

## 摘要
验证器已被证明能通过测试时扩展（TTS）增强大语言模型（LLM）的推理能力。然而，它们在复杂领域面临重大挑战。错误中间推理导致的误差传播可能会使看似合理的解法产生假阳性，而缺乏外部落地（grounding）则使得验证器在计算或知识密集型任务上不可靠。为应对这些挑战，我们提出了智能体化验证器（Agentic Verifier），这是一个将奖励建模转化为多轮、工具增强的审议过程的框架。我们引入了互补的正向和反向智能体：一个从前提推导结论以追踪解法，另一个则根据基础前提重新检查结论。这种双向过程实现了对解法全面、可靠且可解释的评估。为了便于实际部署，我们提出了 AgentV-RL。通过主动探索和强化学习，验证器能够自主地将工具使用与内部推理交织在一起。大量实验表明，智能体化验证器在并行和顺序 TTS 下均能产生一致的性能提升。值得注意的是，我们的 4B 变体性能超过了最先进的 ORM 25.2%，使其成为智能体化奖励建模的一种极具前景的范式。

## Abstract
Verifiers have been demonstrated to enhance LLM reasoning via test-time scaling (TTS). Yet, they face significant challenges in complex domains. Error propagation from incorrect intermediate reasoning can lead to false positives for seemingly plausible solutions, while lacking external grounding makes verifiers unreliable on computation or knowledge-intensive tasks. To address these challenges, we propose Agentic Verifier, a framework that transforms reward modeling into a multi-turn, tool-augmented deliberative process. We introduce complementary forward and backward agents: one traces solutions from premises to conclusions, while the other re-checks conclusions against their underlying premises. This bidirectional process enables a comprehensive, reliable, and interpretable assessment of solutions. To facilitate practical deployment, we propose AgentV-RL. Through proactive exploration and reinforcement learning, the verifier autonomously interleaves tool-use with internal reasoning. Extensive experiments show that Agentic Verifier yields consistent performance gains under both parallel and sequential TTS. Notably, our 4B variant surpasses state-of-the-art ORMs by 25.2%, positioning it as a promising paradigm for agentic reward modeling.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AgentV-RL**，这是一种将奖励模型（Reward Model）转变为“智能体化验证器”的新范式，旨在通过多轮推理和工具增强来提升大语言模型在复杂任务中的验证可靠性。

### 1. 核心问题与整体含义
*   **研究动机**：现有的奖励模型（如 ORM 结果级或 PRM 过程级模型）通常只输出标量分数，缺乏可解释性。在处理复杂推理任务时，它们面临两大痛点：
    1.  **错误传播**：容易被看似合理但中间逻辑错误的解法误导（假阳性）。
    2.  **缺乏外部落地**：在计算密集型或知识密集型任务中，仅靠内部参数容易产生幻觉。
*   **核心目标**：通过将验证过程建模为一个多轮、可调用工具的智能体审议过程，提升验证的准确性、可靠性和可解释性。

### 2. 方法论
*   **核心思想**：提出 **Agentic Verifier** 框架，模拟人类检查数学证明的过程，采用双向验证机制。
*   **关键技术细节**：
    *   **双向智能体协作**：
        *   **前向智能体（Forward Agent）**：从前提追踪到结论，检查逻辑的“充分性”。
        *   **后向智能体（Backward Agent）**：从结论反推前提，检查逻辑的“必要性”及约束满足情况。
    *   **三阶段审议流程**：
        1.  **规划（Planning）**：将复杂解法分解为可检查的原子步骤。
        2.  **验证（Validation）**：逐步检查逻辑，并能自主调用 Python 解释器进行数值计算。
        3.  **判定（Verdict）**：汇总证据，给出最终的二元判断（True/False）。
*   **训练算法（AgentV-RL）**：
    *   **合成数据引擎**：利用模型生成验证轨迹，并根据地面真值（Ground Truth）进行质量过滤。
    *   **两阶段训练**：
        1.  **拒绝采样微调（RFT/SFT）**：使模型学会多轮对话和工具调用的格式。
        2.  **强化学习（RL）**：采用 **GRPO**（群体相对策略优化）算法，以验证结果的准确性作为奖励信号，优化模型的长程推理和自主探索能力。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：GSM8K、MATH500、Gaokao2023（高考数学）、AIME24（美国数学邀请赛）。
    *   **泛化性测试**：LiveCodeBench（编程）、HotpotQA（多跳问答）。
*   **对比方法**：
    *   **文本推理模型**：Qwen2.5-7B-Instruct、Llama3.1-8B-Instruct、DeepSeek-R1-Distill-14B 等。
    *   **结果级奖励模型（ORM）**：Skywork-V2-8B、InternLM2-20B-RM、INF-ORM-70B 等。
    *   **过程级奖励模型（PRM）**：Qwen2.5-Math-PRM-7B、Math-Shepherd 等。
*   **场景**：并行扩展（Best-of-N 采样）和顺序扩展（迭代修正/Critique-Revision）。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **SFT 阶段**：使用 16 台 NVIDIA **A100 (80GB)**。
    *   **RL 阶段**：使用 32 台 NVIDIA **A100 (80GB)**。
*   **训练细节**：SFT 训练 1 个 epoch；RL 阶段每个 query 采样 8 个候选响应，最大序列长度限制为 21,600 tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个核心数学基准上进行了主实验，并额外进行了消融实验、缩放定律分析（模型规模和推理算力）以及跨领域泛化实验。
*   **公平性**：为了确保对比公平，所有验证器变体都在相同的候选解池（由 Qwen2.5-7B 生成）上进行评估。
*   **充分性**：实验涵盖了从 0.6B 到 4B 的不同模型规模，并对比了参数量远大于自身的模型（如 70B ORM），实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能卓越**：Agentic Verifier 的 4B 变体在 MATH500 上达到了 79.0% 的准确率，显著超过了参数量大 10 倍的 INF-ORM-70B（55.4%）。
*   **双向验证的必要性**：消融实验证明，结合前向和后向验证的效果优于单一方向。
*   **工具增强的价值**：Python 解释器的引入显著提升了在计算密集型任务（如高考数学）中的表现。
*   **推理时缩放效应**：增加验证轨迹的采样数量（Test-Time Scaling）能进一步提升验证的准确性。

### 7. 优点
*   **高可靠性**：通过工具落地和双向逻辑检查，极大降低了验证过程中的幻觉。
*   **强解释性**：不同于传统的标量分数，该模型提供完整的推理链条，方便人类理解其判定依据。
*   **高效能**：较小规模的模型（4B）通过智能体化的流程设计，实现了超越超大规模模型的验证能力。

### 8. 不足与局限
*   **延迟与成本**：多轮推理和工具调用显著增加了推理时间（Latency）和 Token 消耗，不适合对实时性要求极高的场景。
*   **工具依赖**：验证效果受限于外部工具（如 Python 解释器）的覆盖范围，对于无法通过代码解决的逻辑问题仍依赖模型自身能力。
*   **合成数据偏差**：训练依赖于合成的验证轨迹，可能存在分布偏差，且对教师模型的质量有一定依赖。

（完）
