---
title: Visual Enhanced Depth Scaling for Multimodal Latent Reasoning
title_zh: 面向多模态潜空间推理的视觉增强深度缩放
authors: "Yudong Han, Yong Wang, Zaiquan Yang, Zhen Qu, Liyuan Pan, Xiangxiang Chu"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10500v1"
tags: ["query:token"]
score: 9.0
evidence: 潜空间推理过程中的Token级梯度动态
tldr: 本研究针对多模态潜空间推理中视觉Token欠优化及固定架构深度限制复杂推理的问题，提出了视觉增强深度缩放框架。该框架通过视觉重放模块强化细粒度空间感知，并利用路由深度缩放机制为复杂Token动态分配推理步骤。结合将显式思维链内化为潜表征的课程学习策略，在显著提升推理速度的同时，在多个基准测试中达到了SOTA性能。
selection_source: fresh_fetch
motivation: 揭示了多模态训练中视觉Token因梯度波动导致的欠优化现象，以及固定模型深度难以应对复杂语义Token推理需求的局限性。
method: 引入视觉重放模块增强细粒度感知，并采用路由深度缩放机制为不同复杂度的Token自适应分配推理深度。
result: 在多个多模态基准测试上实现了最先进的性能，并相比显式思维链基准显著降低了推理延迟。
conclusion: 通过强化视觉表征与动态扩展推理深度，隐式潜空间推理能有效兼顾多模态任务的准确性与推理效率。
---

## 摘要
多模态潜空间推理已成为一种极具前景的范式，它通过隐式特征传播取代了显式的思维链（CoT）解码，在增强表示信息量的同时降低了推理延迟。通过分析潜空间训练过程中的 token 级梯度动力学，我们发现了两个关键现象：（1）由于固有的语言偏见，视觉 token 的梯度范数显著高于文本 token 且波动更大，导致了系统性的视觉优化不足；（2）语义简单的 token 收敛迅速，而复杂 token 则受限于固定的架构深度，表现出持续的梯度不稳定。为了解决这些局限性，我们提出了视觉重放模块和路由深度缩放，旨在协同增强视觉感知并精炼复杂的潜空间表示，以实现更深层次的上下文推理。前者利用因果自注意力机制来估计 token 的显著性，通过空间相干约束强化细粒度的视觉定位。作为补充，后者机制能够自适应地为复杂 token 分配额外的推理步骤，从而实现更深层的上下文精炼。在将显式 CoT 逐步内化为紧凑潜空间表示的课程学习策略指导下，我们的框架在多个基准测试中达到了最先进的性能，同时相比显式 CoT 基准实现了显著的推理加速。

## Abstract
Multimodal latent reasoning has emerged as a promising paradigm that replaces explicit Chain-of-Thought (CoT) decoding with implicit feature propagation, simultaneously enhancing representation informativeness and reducing inference latency. By analyzing token-level gradient dynamics during latent training, we reveal two critical observations: (1) visual tokens exhibit significantly higher and more volatile gradient norms than their textual counterparts due to inherent language bias, resulting in systematic visual under-optimization; and (2) semantically simple tokens converge rapidly, whereas complex tokens exhibit persistent gradient instability constrained by fixed architectural depths. To address these limitations, we propose a visual replay module and routing depth scaling to collaboratively enhance visual perception and refine complicated latents for deeper contextual reasoning. The former module leverages causal self-attention to estimate token saliency, reinforcing fine-grained grounding through spatially-coherent constraints. Complementarily, the latter mechanism adaptively allocates additional reasoning steps to complex tokens, enabling deeper contextual refinement. Guided by a curriculum strategy that progressively internalizes explicit CoT into compact latent representations, our framework achieves state-of-the-art performance across diverse benchmarks while delivering substantial inference speedups over explicit CoT baselines.

---

## 论文详细总结（自动生成）

这篇论文针对多模态大模型（MLLM）在潜空间推理（Latent Reasoning）中的优化难题，提出了视觉增强与动态深度缩放的解决方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机）
传统的显式思维链（CoT）推理虽然强大，但存在推理延迟高、长链条下视觉定位（Visual Grounding）退化的问题。潜空间推理通过隐式特征传播解决了效率问题，但作者通过分析 token 级的梯度动态发现了两个核心瓶颈：
*   **视觉-文本优化失衡：** 视觉 token 的梯度范数显著高于文本 token 且波动剧烈，导致视觉表征在联合训练中处于“欠优化”状态。
*   **固定深度的优化困境：** 简单 token 很快收敛，而复杂语义 token 在固定层数的架构下难以充分精炼，导致模型在处理难题时性能受限。

### 2. 方法论：核心思想与关键技术
论文提出了一个统一的框架，包含三个核心组件：
*   **空间相干细粒度视觉重放（SCF-VR）：**
    *   **注意力引导：** 利用 Transformer 的注意力图识别显著视觉区域。
    *   **视觉重放：** 在每个推理步骤中动态提取并注入关键视觉线索。
    *   **自蒸馏监督：** 对选定的视觉区域进行高分辨率裁剪和重新编码，通过轻量级超分辨率模块（TokenSR）将粗粒度特征与高保真参考特征对齐，强化空间相干性。
*   **路由深度缩放（RDS）：**
    *   **轻量级路由：** 在每一层引入路由网络，为每个 token 计算重要性得分。
    *   **自适应计算：** 仅对得分最高的 Top-$\alpha$ 个关键 token（如复杂语义或关键视觉 token）执行额外的 Transformer 块迭代，实现 token 级的动态深度分配。
*   **课程潜空间训练（Curriculum Training）：**
    *   不依赖昂贵的中间标注，而是采用渐进式策略：先进行标准的显式 CoT 训练，随后逐步将显式推理步骤封装进紧凑的 `<latent>` token 中，使模型平滑地内化推理逻辑。

### 3. 实验设计
*   **数据集：** 涵盖 12 个主流基准测试，分为三大类：
    *   **数学推理：** MathVista, MathVision, MM-Math。
    *   **视觉中心推理：** HallusionBench, MMVP, Seed-Bench-2-Plus, HR-Bench, GQA。
    *   **多模态组合推理：** MMStar, BLINK, ScienceQA, M3CoT。
*   **对比方法：** 包括零样本 VLM（GPT-4o, Qwen2.5-VL）、显式 CoT 方法（Multimodal-CoT, CCoT）、工具/强化学习增强方法（Vision-R1, VL-Rethinker）以及最新的潜空间推理方法（Laser, LVR, DMLR）。

### 4. 资源与算力
*   **硬件：** 训练使用 **8 台 NVIDIA H100 (80GB)** GPU；评估使用 **8 台 NVIDIA H20 (96GB)** GPU。
*   **训练细节：** 默认训练 16 个 epoch，采用 DeepSpeed ZeRO-2 优化，学习率为 $4 \times 10^{-5}$。
*   **模型规模：** 实验基于 Qwen2-VL (2B/7B)、Qwen2.5-VL (3B/7B) 和 Chameleon-7B 等多种骨干网络。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 12 个数据集上进行了大规模测试，并针对 4 种不同的骨干网络验证了泛化性。
*   **消融实验：** 针对 SCF-VR 模块、RDS 模块、课程学习策略、超参数 $\lambda$（损失权重）、$\alpha$（Token 保留数）、窗口大小 $W$ 以及位置编码策略进行了详尽的消融分析。
*   **客观性：** 实验对比了准确率、自回归步数和推理耗时三个维度，充分证明了该方法在性能与效率之间的平衡，实验设计客观且全面。

### 6. 主要结论与发现
*   **性能领先：** 在多个基准测试中达到 SOTA，尤其在视觉感知要求高的任务（如 MMVP）上提升显著（+4.67%）。
*   **效率极高：** 相比显式 CoT，自回归步数减少了 **10 倍以上**，推理速度提升了 **3-6 倍**。
*   **表征优化：** 潜空间 token 能够有效地捕捉逻辑依赖并建立稳健的视觉-语义对齐，可视化结果证实了其视觉定位能力的增强。

### 7. 优点（亮点）
*   **理论驱动：** 基于梯度动态分析发现问题，而非盲目堆砌模块。
*   **动态分配：** RDS 机制实现了按需分配算力，避免了固定架构对复杂推理的限制。
*   **无需额外标注：** 通过课程学习和自蒸馏，在不增加人工标注负担的前提下提升了潜空间表征质量。
*   **推理友好：** 推理时可关闭视觉重放模块，确保不增加额外的测试延迟。

### 8. 不足与局限
*   **文本密集型任务表现平平：** 在 SeedBench-2-Plus 等侧重文本理解的任务上提升较小，可能受限于训练数据的领域分布。
*   **超参数敏感性：** 损失权重 $\lambda$ 在不同规模模型（2B vs 7B）上表现出截然不同的最优值，增加了调参难度。
*   **应用场景限制：** 目前主要集中在静态图像推理，尚未在长视频理解等更复杂的时空推理场景中验证。

（完）
