---
title: "Selective Latent Thinking: Adaptive Compression of LLM Reasoning Chains"
title_zh: 选择性潜在思考：LLM推理链的自适应压缩
authors: "Hui Xie, Jie Liu, Ziyue Qiao, Joaquin Vanschore"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25745v1"
tags: ["query:cot-unfaith"]
score: 8.0
evidence: 选择性压缩推理链，保留关键步骤以保持忠实性
tldr: "本文提出选择性潜在思考（SLT）框架，通过轻量级解码器预测推理片段并用置信度门控决定压缩或保留显式思维链，结合三阶段训练（跨度压缩、可靠预测、强化学习）优化准确率与推理成本，在数学推理基准上以58.4%链长缩减仅损失2.8%准确率，比统一压缩方法提升22.7%准确率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25745v1/fig-001.webp\", \"caption\": \"Figure 8: Visualization of SLT generation trajectories. The timeline arrows illustrate the causal autoregressive flow. Green nodes represent compressed continuous latent vectors (zcomp) replacing the bracketed explicit strings below them. Blue nodes are explicit tokens triggered by the confidence gate’s fallback mechanism, ensuring strict accuracy for numerical pivots.\", \"page\": 15, \"index\": 1, \"width\": 826, \"height\": 604}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-002.webp\", \"caption\": \"Figure 1: Comparison of reasoning paradigms and efficiency analysis. (a) Explicit CoT relies on verbose textual generation, whereas implicit latent CoT compresses the reasoning into latent space. Our Selective Latent Thinking (SLT) instead dynamically interleaves explicit CoT and latent reasoning, preserving precision-critical tokens in explicit form while selectively compressing redundant reasoning into latent. (b) SLT achieves a superior accuracy–efficiency trade-off (∆acc/L) compared with both explicit CoT and implicit latent reasoning baselines.\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-003.webp\", \"caption\": \"Table 1: Performance and sequence length comparison across multiple reasoning benchmarks. Results marked with † are reported from the RoT baseline paper, while all Llama-3.2-1B results, as well as the SFT-w/o CoT and our models on Qwen3-4B, are based on our local evaluations. Pass@1 denotes the accuracy of the first generated response (%). # L represents the average number of generated reasoning or latent tokens. ∆acc/L measures the reasoning efficiency, defined as the average accuracy improvement over the SFT-w/o CoT baseline divided by the average reasoning length.\", \"page\": 7, \"index\": 3, \"width\": 822, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-004.webp\", \"caption\": \"Table 6: Key hyperparameters and architectural configurations for the three-stage training pipeline. Note that the primary LLM is strictly frozen during Stage 2. For the Stage 3 RL optimization, Llama-3.2-1B undergoes full-parameter tuning, whereas Qwen3-4B is optimized via LoRA due to computational resource constraints.\", \"page\": 14, \"index\": 4, \"width\": 508, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-005.webp\", \"caption\": \"Figure 7: Accuracy-efficiency Pareto frontier across different test datasets. By modulating the gating threshold bias γ ∈ [−1.0, 1.0], SLT traces a smooth efficiency-accuracy trade-off. The gold star represents the SFT-CoT baseline. Notably, under conservative thresholds (γ > 0, red points), SLT frequently surpasses the explicit CoT accuracy ceiling while maintaining lower computational costs. Conversely, aggressive compression (γ < 0, blue points) exhibits diminishing marginal returns on length reduction and severe accuracy drops.\", \"page\": 13, \"index\": 5, \"width\": 814, \"height\": 595}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-006.webp\", \"caption\": \"Figure 3: Example of SLT reasoning trajectories. SLT dynamically interleaves latent transitions with explicit reasoning while preserving precision-critical numerical and arithmetic steps.\", \"page\": 8, \"index\": 6, \"width\": 727, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of the proposed Selective Latent Thinking (SLT). (a) During inference, a lightweight decoder D predicts a short future reasoning trajectory, a confidence gate selects the reliable prefix, and the latent compressor C maps the accepted span into a single latent block for efficient LLM state updates. (b) The compressor is trained to replace multi-step reasoning spans with latent blocks while preserving causal alignment. (c) The decoder and gate are jointly trained to predict future reasoning and determine which spans are reliable enough for latent compression.\", \"page\": 4, \"index\": 7, \"width\": 820, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2605.25745v1/fig-008.webp\", \"caption\": \"Figure 6: Average inference time comparison.Inference times for all models are measured on the GSM8k-AugNL dataset using a single A6000 GPU. SLT achieves substantial wall-clock speedups, particularly on the larger Qwen3-4B backbone, while dynamically adjusting computational effort on harder datasets like GSM-Hard.\", \"page\": 12, \"index\": 8, \"width\": 734, \"height\": 289}]"
motivation: 现有潜在推理方法将推理过程均匀压缩，导致精度关键步骤过度压缩而降低准确率。
method: SLT使用轻量解码器预测短推理片段，基于置信度门控决定最长可压缩跨度，压缩项编码为潜在表示，保留显式思维链以确保精度，并通过三阶段训练优化压缩策略。
result: "在四个数学推理基准上，SLT在可比较压缩比下准确率比潜在推理基线高22.7%，思维链长度减少58.4%且准确率仅下降2.8%。"
conclusion: SLT通过选择性压缩冗余推理步骤，在保持显式推理优势的同时显著提升效率，实现了准确率与成本的更好权衡。
---

## 摘要
显式的思维链（Chain-of-Thought, CoT）推理显著提升了大型语言模型（LLMs）的推理能力，但由于冗长的自回归痕迹带来了高昂的推理成本。现有的潜在推理方法提供了一种有前景的替代方案，但它们通常将推理视为均匀可压缩的，导致精度关键的中间步骤被过度压缩，从而降低了推理准确性。在这项工作中，我们提出了选择性潜在思考（Selective Latent Thinking, SLT），一个框架，它选择性地将冗余推理片段压缩为潜在表示，同时在同一推理轨迹中将精度关键的片段保留为显式CoT。具体来说，SLT首先使用一个轻量级解码器来预测即将出现的短推理片段，然后应用基于置信度的门控来确定可以可靠压缩的最长片段。被接受的片段被编码为紧凑的潜在表示以提高推理效率，而不确定或精度关键的推理则保留为显式CoT形式以保持准确性。为了学习这种选择性压缩策略，SLT采用了一种三阶段训练策略，结合了片段级潜在压缩、可靠性感知的未来推理预测和轨迹级强化学习，以优化答案正确性与推理成本之间的权衡。在四个数学推理基准上的大量实验表明，在可比较的压缩比下，SLT相比潜在推理基线实现了22.7%的准确率提升，同时将推理链长度减少了58.4%，与显式CoT相比仅降低了2.8%的准确率。我们的代码可在https://github.com/hunshi34/SLT获取。

## Abstract
Explicit chain-of-thought (CoT) reasoning substantially improves the reasoning ability of large language models (LLMs), but incurs high inference cost due to lengthy autoregressive traces. Existing latent reasoning methods offer a promising alternative, yet they often treat reasoning as uniformly compressible, causing precision-critical intermediate steps to be overly compressed and thereby degrading reasoning accuracy. In this work, we propose Selective Latent Thinking (SLT), a framework that selectively compresses redundant reasoning spans into latent representations while preserving precision-critical spans as explicit CoT within the same reasoning trajectory. Specifically, SLT first uses a lightweight decoder to anticipate a short upcoming reasoning span, and then applies confidence-based gating to determine the longest span that can be reliably compressed. The accepted span is encoded into a compact latent representation to improve reasoning efficiency, while uncertain or precision-critical reasoning remains in explicit CoT form to preserve accuracy. To learn this selective compression policy, SLT adopts a three-stage training strategy that combines span-level latent compression, reliability-aware future reasoning prediction, and trajectory-level reinforcement learning to optimize the trade-off between answer correctness and reasoning cost. Extensive experiments across four mathematical reasoning benchmarks demonstrate that SLT achieves 22.7\% higher accuracy than latent reasoning baselines at comparable compression ratios, while reducing reasoning chain length by 58.4\% with only 2.8\% accuracy degradation compared to explicit CoT,Our code can be found in https://github.com/hunshi34/SLT.