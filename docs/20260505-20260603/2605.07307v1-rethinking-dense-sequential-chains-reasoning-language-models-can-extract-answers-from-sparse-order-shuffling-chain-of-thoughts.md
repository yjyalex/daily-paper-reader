---
title: "Rethinking Dense Sequential Chains: Reasoning Language Models Can Extract Answers from Sparse, Order-Shuffling Chain-of-Thoughts"
title_zh: 重新思考密集顺序链：推理语言模型可以从稀疏、顺序打乱的思维链中提取答案
authors: "Yi-Chang Chen, Feng-Ting Liao, Da-shan Shiu, Hung-yi Lee"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07307v1"
tags: ["query:cot-unfaith"]
score: 8.0
evidence: 挑战思维链顺序必要性，关注连贯性
tldr: "现代推理语言模型生成密集、顺序的思维链，假设每步有序且每标记必要。本研究通过系统干预（移除、遮蔽、打乱、噪声注入）挑战该假设。在三个模型和基准上发现：行级打乱准确率下降<0.5pp，字级打乱仍保留62%-89%；屏蔽数字准确率归零，屏蔽散文提升4.7pp；最简化表示（移除文字、打乱行）仍有83%准确率，注入错误答案不影响。结论：答案提取基于稀疏、顺序不敏感且鲁棒的信息基底，为并行化和标记高效推理开辟路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07307v1/fig-001.webp\", \"caption\": \"Table 3: Accuracy of OLMo-Base (pretrained only) versus OLMo (instruction-tuned) under identical shuffling conditions across three benchmarks (Ret setting), probing whether order-independence originates from pretraining or reasoning-specific fine-tuning. SE bounds follow Table 2.\", \"page\": 6, \"index\": 1, \"width\": 636, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2605.07307v1/fig-002.webp\", \"caption\": \"Table 2: Accuracy under five shuffling granularities, two randomisation baselines (Dtok, Dword), and the reasoning-free condition (Rr), across three models (OLMo, GPT-OSS, DeepSeek) and three benchmarks (AIME 2025, CodeElo, GPQA-Diamond). Two evaluation modes are compared: Gen (free generation) and Ret (retrieval with an answer-extraction prefix). Accuracies are single-run binomial estimates; SE≤±2.9pp for AIME (n = 300), ±2.5pp for CodeElo (n = 408), and ±3.6pp for GPQA-Diamond (n = 198).\", \"page\": 6, \"index\": 2, \"width\": 826, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2605.07307v1/fig-003.webp\", \"caption\": \"Table 1: Transformation processors used in this work. We implement five categories: Shuffling (S) permutes content at multiple granularities; Masking (M) replaces targeted character classes with a mask token; Removal (R) strips content entirely; Noise Injection (Nk) inserts false-answer sentences at k× the true-answer frequency; Randomisation baselines (D) replace tokens or words with random samples. Processors compose sequentially (e.g., Sline ◦ Rα).\", \"page\": 4, \"index\": 3, \"width\": 814, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2605.07307v1/fig-004.webp\", \"caption\": \"Table 4: Accuracy under combinations of five interventions—question removal (Rq), alphabet masking (Mα), digit masking (Mν), answer masking (Mans), and line shuffling (Sline)—on AIME 2025 (Math) with GPT-OSS (Ret), probing which components of a reasoning chain are informational. • indicates the intervention is applied; ◦ otherwise. Rows are grouped by the (Rq, Sline) configuration: A = neither, B = Sline only, C = Rq only, D = both; within each group, sub-IDs 1–5 sweep the remaining three masking interventions. ∆Acc is relative to the baseline row A1 (91.3%). Light gray: ∆Acc < −25%; dark gray: ∆Acc < −60%. SE bounds follow Table 2.\", \"page\": 8, \"index\": 4, \"width\": 389, \"height\": 470}]"
motivation: 挑战推理语言模型生成密集、顺序思维链的两个隐含假设——每个标记都贡献且步骤必须按序消费。
method: 对模型生成的推理链进行移除、遮蔽、行/词/标记级打乱及注入错误答案等系统干预。
result: "行级打乱准确率几乎不变（<0.5pp下降），屏蔽数字导致完全崩溃，最简化表示仍保持83%准确率，注入错误答案不影响。"
conclusion: 答案提取基于稀疏、顺序不敏感且结构鲁棒的信息基底，而非完整顺序链。
---

## 摘要
现代推理语言模型生成密集、顺序的思维链轨迹，隐含地假设每个标记都有贡献且步骤必须按顺序使用。我们通过一个系统性的干预流程——移除、掩码、打乱和噪声注入——对三个模型和三个基准上模型生成的推理链进行挑战，从而质疑这两个假设。我们的发现在三个维度上反直觉。顺序：推理链的顺序顺序对答案提取重要吗？不——行级打乱使准确率下降不到0.5个百分点；词级打乱保留62%-89%的准确率；只有词元级打乱几乎降至零。仅预训练和指令调优变体表现出几乎相同的容忍度（行级打乱下分别为78.67%和78.00%），表明顺序独立性源自预训练而非推理特定微调。密集：推理链中的所有信息对答案提取都重要吗？不——掩码数字使准确率降至恰好0%，而掩码字母散文则使准确率提升4.7个百分点。鲁棒性：既打乱顺序又非密集的推理链是否仍然鲁棒？是——最激进的缩减表示（移除所有自然语言，行任意打乱）仍能达到83%的准确率，并且以真实答案频率三倍注入虚假答案后准确率不变（83.3%->83.3%），否定了基于频率的提取解释。这些结果确立了答案提取基于稀疏、顺序不敏感且结构鲁棒的信息基底，为并行化和标记高效的推理生成开辟了道路。

## Abstract
Modern reasoning language models generate dense, sequential chain-of-thought traces implicitly assuming that every token contributes and that steps must be consumed in order. We challenge both assumptions through a systematic intervention pipeline--removal, masking, shuffling, and noise injection--applied to model-generated reasoning chains across three models and three benchmarks. Our findings are counterintuitive on three dimensions. Order: Does the sequential order of a reasoning chain matter for answer extraction? No--line-level shuffling reduces accuracy by less than 0.5 pp; word-level shuffling retains 62%-89% accuracy; only token-level shuffling collapses to near zero. Pretrained-only and instruction-tuned variants exhibit near-identical tolerance (78.67% vs. 78.00% under line shuffling), indicating order-independence originates from pretraining rather than reasoning-specific fine-tuning. Dense: Is all the information in a reasoning chain important for answer extraction? No--masking numeric digits collapses accuracy to exactly 0%, while masking alphabetic prose improves accuracy by 4.7 pp. Robustness: Is a reasoning chain that is both order-shuffling and non-dense still robust? Yes--the most aggressively reduced representation (all natural language removed, lines arbitrarily shuffled) still achieves 83% accuracy, and injecting false answers at 3x true-answer frequency leaves accuracy unchanged (83.3%->83.3%), falsifying a frequency-based extraction account. These results establish that answer extraction operates on a sparse, order-insensitive, and structurally robust informational substrate, opening paths toward parallelized and token-efficient reasoning generation.