---
title: "SWAN: Semantic Watermarking with Abstract Meaning Representation"
title_zh: SWAN：基于抽象意义表示的语义水印
authors: "Ziping Ye, Gourab Dey, Christos Christodoulopoulos, Charith Peris, Anil Ramakrishna, Weitong Ruan, Aram Galstyan, Kai-Wei Chang, Rahul Gupta, Ninareh Mehrabi"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04305v1"
tags: ["query:amr-llm"]
score: 9.0
evidence: 基于AMR的语义水印
tldr: 现有水印方法在文本生成时调整token选择，但易被改写破坏。SWAN利用抽象意义表示（AMR）将签名嵌入句子语义结构，改写后仍保留。无需训练，通过提示LLM生成符合选定AMR模板的文本，检测时用AMR解析器和z检验。在RealNews上检测性能与最先进方法相当，抗改写AUC提升多达13.9个百分点。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04305v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of our proposed framework. In watermark injection, LLM repeatedly samples a sentence until its parsed AMR matches a secret template drawn from the bank; in watermark detection, we parse each sentence of a candidate paragraph, count AMR matches, and apply a z-test—because the watermark lives in the AMR graph, any paraphrase that preserves meaning leaves the signal intact.\", \"page\": 3, \"index\": 1, \"width\": 879, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2605.04305v1/fig-002.webp\", \"caption\": \"Table 5: Illustrative paragraphs for each watermarking method. Using the LLM-judged coherence, fluency, and diversity metrics (§4.4), we select one example that scores toward the higher end (High-quality) and another toward the lower end (Low-quality) to visualise the range of output quality.\", \"page\": 8, \"index\": 2, \"width\": 948, \"height\": 546}]"
motivation: 现有水印方法在文本改写后签名易丢失，需要更鲁棒的语义级水印方案。
method: 将水印签名编码到AMR语义模板中，通过提示LLM生成匹配模板的文本，检测时解析AMR并统计签名出现频率。
result: "在RealNews上抗改写检测AUC比之前方法最高提升13.9%，同时未改写文本的检测性能与最先进方法持平。"
conclusion: 基于AMR的语义水印简单有效，为抗改写文本溯源提供了新途径。
---

## 摘要
我们提出SWAN（基于抽象意义表示的语义水印），这是一个新颖的框架，利用抽象意义表示（AMR）将水印签名嵌入句子的语义结构中。与现有的水印方法（通常通过在文本生成过程中调整token选择偏好来编码签名）不同，SWAN直接将签名嵌入句子的语义表示中。由于签名在语义结构层编码，任何保留意义的释义都会自动保留签名。SWAN无需训练：水印注入通过提示LLM根据选定的AMR模板生成句子，同时保持上下文连贯性来实现；检测使用现成的AMR解析器，随后进行简单的单比例z检验。在RealNews基准上的经验评估表明，SWAN在未修改的水印文本上达到了最先进的检测性能，同时显著提高了对释义的鲁棒性，与先前方法相比，检测AUC提高了多达13.9个百分点。这些结果表明，SWAN将水印锚定在AMR语义结构中的方法提供了一种简单、有效且基于提示的方法，用于在释义下进行稳健的文本来源验证，为语义级水印研究开辟了新途径。

## Abstract
We introduce SWAN (Semantic Watermarking with Abstract Meaning Representation), a novel framework that embeds watermark signatures into the semantic structure of a sentence using Abstract Meaning Representation (AMR). In contrast to existing watermarking methods, which typically encode signatures by adjusting token selection preferences during text generation, SWAN embeds the signature directly in the sentence's semantic representation. As the signature is encoded at the semantic structure level, any paraphrase that preserves meaning automatically preserves the signature. SWAN is training-free: watermark injection is achieved by prompting an LLM to generate sentences guided by a selected AMR template while maintaining contextual coherence, and detection uses an off-the-shelf AMR parser followed by a simple one-proportion z-test. Empirical evaluation on the RealNews benchmark shows SWAN matches state-of-the-art detection performance on unaltered watermarked text, while significantly improving robustness against paraphrasing, increasing detection AUC by up to 13.9 percentage points compared to prior methods. These results demonstrate that SWAN's approach of anchoring watermarks in AMR semantic structures provides a simple, effective, and prompt-based method for robust text provenance verification under paraphrasing, opening new avenues for semantic-level watermarking research.